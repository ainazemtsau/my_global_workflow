# Memory Index Template

status: template

## Memory Index

`memory_index_id`:

`direction_id`:

`entries`:

## Entry fields

`memory_artifact_ref`:

`tags`:

`applies_to`:

`load_when`:

`do_not_load_when`:

`source_refs`:

`refresh_or_expiry`:

## Boundary

Memory Index is a lookup surface for promoted Memory Artifacts. It does not replace exact source reads, accepted Direction state, or Memory Artifact promotion.

Raw notes, Result Packets, and chat summaries must not be indexed as always-load memory.

END_OF_FILE: workflow_v3/templates/MEMORY_INDEX_TEMPLATE.md
