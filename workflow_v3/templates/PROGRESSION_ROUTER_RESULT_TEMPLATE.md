# Progression Router Result Template

status: template

## Progression Router Result

`progression_router_result_id`:

`input_closure_signal_ids`:

`input_handler_result_ids`:

`blocking_handlers_first`:

`acceptance_required`:

`persistence_required`:

`direction_lifecycle_transition_candidate`:

`selected_front_or_node_candidate`:

`next_work_graph_node_candidate`:

`active_front_transition_candidate`:

`action_inbox_candidates`:

`primary_next_move`:

`secondary_candidates`:

`same_chat_allowed`:

`new_chat_needed`:

`transition_packet_type`:

`closure_signal`:

`transition_packet`:

`returns_to_current_chat`:

`next_material_chat_needed`:

`next_chat_prompt_if_needed`:

`stop_condition_if_any`:

`limitations`:

## Boundary

Progression Router output is candidate until accepted or explicitly launched.

The router must assemble the complete Transition Packet for the selected next step when transfer is needed.

END_OF_FILE: workflow_v3/templates/PROGRESSION_ROUTER_RESULT_TEMPLATE.md
