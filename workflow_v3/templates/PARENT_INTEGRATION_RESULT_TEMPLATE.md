# Parent Integration Result Template

status: template

## Parent Integration Result

`parent_integration_id`:

`parent_target_ref`:

`input_result_refs`:

`required_results_status`:

`evidence_fit`:

`conflicts`:

`missing_evidence`:

`decision`: continue | repair | replan | close | escalate | delta | stop

`graph_delta_ref_if_any`:

`upstream_escalation_ref_if_any`:

`downstream_delta_ref_if_any`:

`next_move_packet_ref`:

## Boundary

Parent Integration Result is a typed synthesis record. It does not accept parent state, synthesize missing evidence, mutate graph/state, or launch next work by itself.

END_OF_FILE: workflow_v3/templates/PARENT_INTEGRATION_RESULT_TEMPLATE.md
