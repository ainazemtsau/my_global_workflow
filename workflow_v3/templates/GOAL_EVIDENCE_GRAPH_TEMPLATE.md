# Goal Evidence Graph Template

status: template

## Goal Evidence Graph

`graph_id`:

`direction_id`:

`source_spine_ref`:

`direction_map_ref`:

`root_outcome_ref`:

`success_dimensions`:

`nodes`:

`active_paths`:

`active_unresolved_cut_ref`:

`evidence_index_ref`:

`decision_index_ref`:

`status`: candidate | accepted | superseded | stale

`limitations`:

## Boundary

Goal Evidence Graph is a Direction Map-associated steering/control artifact, not a semantic primitive, roadmap, backlog, accepted state, or launch authority.

Graph output remains candidate until accepted through an explicit acceptance/update path.

This template does not adopt a Direction, create runtime state, or authorize `directions_v3/<direction-id>/runtime/**`.

END_OF_FILE: workflow_v3/templates/GOAL_EVIDENCE_GRAPH_TEMPLATE.md
