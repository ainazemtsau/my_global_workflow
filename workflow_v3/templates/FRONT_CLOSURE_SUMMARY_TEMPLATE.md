# Front Closure Summary Template

status: template

## Front Closure Summary

`front_id`:

`closure_status`:

`exit_criteria_review`:

`work_graph_summary`:

`accepted_results`:

`evidence_refs`:

`unresolved_blockers`:

`direction_map_update_candidates`:

`candidate_next_fronts`:

`human_decisions_needed`:

`closure_signal`: active_front_complete or blocked_lifecycle_transition

`acceptance_question`:

`limitations`:

## Boundary

Front closure requires evidence and the applicable acceptance/update path.

It does not silently close an Active Front, update Direction Map, or open a new front.

END_OF_FILE: workflow_v3/templates/FRONT_CLOSURE_SUMMARY_TEMPLATE.md
