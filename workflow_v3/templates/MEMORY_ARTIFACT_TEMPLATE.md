# Memory Artifact Template

status: template

## Memory Artifact

`memory_artifact_id`:

`promoted_from_candidate_ref`:

`accepted_scope_of_use`:

`source_refs`:

`summary`:

`when_to_load`:

`when_not_to_use`:

`refresh_or_expiry_condition`:

`acceptance_decision_ref`:

## Boundary

Memory Artifact does not replace canonical state.

It is scoped reusable context only and must remain traceable to sources and acceptance.

If it conflicts with exact repository state or accepted records, exact repository state and accepted records win.

END_OF_FILE: workflow_v3/templates/MEMORY_ARTIFACT_TEMPLATE.md
