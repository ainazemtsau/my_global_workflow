# Work Graph Node Template

status: template

## Work Graph Node

`node_id`:

`work_graph_id`:

`front_id`:

`node_status`:

`node_purpose`:

`relation_to_front_exit_criteria`:

`dependencies`:

`in_scope`:

`out_of_scope`:

`expected_result`:

`evidence_required`:

`work_contract_refs`:

`run_refs`:

`evidence_refs`:

`decision_refs`:

`memory_candidate_refs`:

`closure_condition`:

`limitations`:

## Boundary

Work Graph Node is a bounded local unit under one Work Graph and Active Front.

It is not an unbounded project task, roadmap item, or Direction route claim.

Node closure and state changes require evidence and the applicable acceptance/update path.

Work Graph Nodes derive from a local Work Graph under one Active Front. They are not Direction Definition, setup-only root content, or backlog items.

END_OF_FILE: workflow_v3/templates/WORK_GRAPH_NODE_TEMPLATE.md
