# Upstream Escalation Packet Template

status: template

## Upstream Escalation Packet

`escalation_id`:

`source_ref`:

`affected_parent_ref`:

`issue_type`:

`impact_radius`: work_contract | work_graph | active_front | direction_map | direction_spine

`options`:

`recommendation`:

`human_decision_needed`:

`evidence_refs`:

`next_move_packet_ref`:

## Boundary

Upstream Escalation Packet asks the appropriate parent layer for a decision. It is not accepted state, route mutation, or launch authority.

END_OF_FILE: workflow_v3/templates/UPSTREAM_ESCALATION_PACKET_TEMPLATE.md
