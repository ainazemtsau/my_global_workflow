# Active Unresolved Cut Template

status: template

## Active Unresolved Cut

`cut_id`:

`graph_ref`:

`root_path_refs`:

`unresolved_node_refs`:

`selection_reason`:

`candidate_front_refs`:

`blocking_gates`:

`evidence_value_notes`:

`next_move_hint`:

`quality_check_owner`: workflow_v3/interfaces/14_GOAL_EVIDENCE_GRAPH_INTERFACE.md#quality-checks

## Boundary

Active Unresolved Cut selects unresolved graph nodes for focus consideration. It is not accepted state, a Work Graph, backlog, roadmap, or launch authority.

The cut may inform Active Front formation, but the Active Front still requires the explicit acceptance/update path when state changes.

END_OF_FILE: workflow_v3/templates/ACTIVE_UNRESOLVED_CUT_TEMPLATE.md
