# Check Job Template

status: template

## Check Job

`check_job_id`:

`question`:

`why_it_matters`:

`sources_to_check`:

`allowed_sources_or_paths`:

`forbidden_sources_or_paths`:

`expected_finding_format`:

`confidence_and_limitations`:

`return_destination`:

`next_move_if_blocked`:

## Boundary

Check Job is bounded verification.

Check Job findings are candidate evidence until reviewed through the explicit acceptance/update path.

END_OF_FILE: workflow_v3/templates/CHECK_JOB_TEMPLATE.md
