# Evidence Record Template

status: template

## Evidence Record

`evidence_id`:

`evidence_type`:

`source_run_or_check_job_ref`:

`related_work_contract`:

`related_node_or_front`:

`evidence_summary`:

`evidence_location_or_content`:

`validation_level_or_method`:

`confidence_and_limitations`:

`conflicts_or_gaps`:

`supports_decision_ref`:

## Boundary

Evidence does not accept state alone.

Evidence supports review, acceptance decisions, repair decisions, or blocked results.

Acceptance and state mutation require the explicit acceptance/update path.

END_OF_FILE: workflow_v3/templates/EVIDENCE_RECORD_TEMPLATE.md
