# Action Inbox Item Template

status: template

## Action Inbox/Q Item

`item_id`:

`title`:

`reason`:

`relation`:

`source_signal_id`:

`candidate_action`:

`priority`:

`when_to_run_or_stale_condition`:

`owner`:

`expected_output`:

`close_condition`:

`status`:

`limitations`:

## Boundary

Action Inbox stores candidate actions, not raw signals.

An Action Inbox/Q item is candidate only and does not mutate accepted state.

END_OF_FILE: workflow_v3/templates/ACTION_INBOX_ITEM_TEMPLATE.md
