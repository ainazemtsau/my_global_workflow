# Front Template

status: template

## Front

`front_id`:

`related_active_front_state_ref`:

`source_map_areas`:

`front_purpose`:

`in_scope`:

`out_of_scope`:

`exit_criteria`:

`work_graph_ref`:

`local_context_boundary`:

`evidence_index_ref`:

`decisions_index_ref`:

`closure_summary_ref`:

`status`:

`limitations`:

## Boundary

Front workspace material is local to one accepted or candidate Active Front.

It does not replace Direction Map, Direction Spine, accepted state, or acceptance decisions.

This template does not adopt a Direction, create runtime state, or authorize `directions_v3/<direction-id>/runtime/**`.

END_OF_FILE: workflow_v3/templates/FRONT_TEMPLATE.md
