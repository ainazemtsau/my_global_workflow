# Next Chat Prompt Template

status: template

## Next Chat Prompt

`title`:

`chat_role`:

`source_authority`:

`runtime_root`:

`parent_context`:

`entrypoint`:

`run_surface_type`:

`procedure_ref`:

`required_source_reads`:

`allowed_operations`:

`forbidden_operations`:

`target`:

`context_supplied`:

`in_scope`:

`out_of_scope`:

`expected_result`:

`closure_fact_required`:

`required_return_fields`:

`not_allowed_to_decide`:

`parent_recovery_note_if_applicable`:

`finish_packet_required`:

`return_destination`:

`copy_paste_packet`:

## Boundary

Use this only when a new chat is needed.

The prompt is candidate until accepted or explicitly launched.

Next chat prompts must include entrypoint, run surface, procedure ref, required source reads, allowed/forbidden operations, return destination, and copy-paste-ready packet text when transfer is needed.

END_OF_FILE: workflow_v3/templates/NEXT_CHAT_PROMPT_TEMPLATE.md
