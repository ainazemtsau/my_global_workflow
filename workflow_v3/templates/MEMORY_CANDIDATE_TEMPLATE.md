# Memory Candidate Template

status: template

## Memory Candidate

`memory_candidate_id`:

`source_result_or_evidence_ref`:

`candidate_summary`:

`why_it_may_be_useful_later`:

`scope_of_reuse`:

`source_limitations`:

`promotion_question`:

`status`:

## Boundary

Memory Candidate is not Memory Artifact and not accepted state.

It requires explicit review and promotion before becoming a Memory Artifact.

Promotion uses `workflow_v3/procedures/MEMORY_ARTIFACT_PROMOTION_PROCEDURE.md` and `workflow_v3/evals/MEMORY_ARTIFACT_PROMOTION_EVAL.md`.

It does not replace canonical Direction state or accepted records.

END_OF_FILE: workflow_v3/templates/MEMORY_CANDIDATE_TEMPLATE.md
