# Run Record Template

status: template

## Run Record

`run_id`:

`work_contract_id`:

`run_surface`:

`started_at_or_ref`:

`completed_at_or_ref`:

`input_packet_ref`:

`sources_used`:

`result_packet_ref`:

`evidence_refs`:

`validation_results`:

`limitations`:

`return_destination`:

## Boundary

Run is not Acceptance Decision.

A Run produces candidate result, evidence, and limitations only.

Adapters and human execution surfaces cannot accept their own output.

END_OF_FILE: workflow_v3/templates/RUN_RECORD_TEMPLATE.md
