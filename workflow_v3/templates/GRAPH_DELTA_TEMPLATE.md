# Graph Delta Template

status: template

## Graph Delta

`graph_delta_id`:

`graph_ref`:

`change_type`:

`affected_nodes`:

`evidence_refs`:

`candidate_status_updates`:

`acceptance_boundary`:

`downstream_delta_needed`:

`limitations`:

## Boundary

Graph Delta is a candidate change record for Goal Evidence Graph structure, relations, status, or evidence links.

It does not mutate accepted graph, map, front, or Direction state until accepted through the explicit acceptance/update path.

END_OF_FILE: workflow_v3/templates/GRAPH_DELTA_TEMPLATE.md
