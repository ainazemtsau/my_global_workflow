# Workflow v3 Project Packs

status: active_skeleton_namespace_corrected

## Purpose

`workflow_v3/project_packs/**` is the future pack source namespace for generated cache/upload convenience artifacts.

No actual pack files are generated in this slice.

## Future packs

Future pack names:

- `RUNTIME_PROJECT_SHELL_PACK.md`
- `RUNTIME_BASE_PACK.md`
- `RUNTIME_TRANSPORT_PACK.md`
- `RUNTIME_CONSOLE_QUALITY_PACK.md`

## Pack authority boundary

Packs are generated cache/upload convenience, not authority.

Canonical `workflow_v3/**` files win over packs.

If a pack conflicts with exact repository files under `workflow_v3/**`, the canonical files win and the pack should be regenerated by a later explicit package.

## Use model

Future packs may compact shared runtime model, transport, execution surface, setup, console, quality, and recovery guidance for Project Files/Sources.

Project Files/Sources remain cache/context only and do not create accepted state, Direction adoption, migration/import, or Project setup authority.

`workflow_v3/completion/**`, `workflow_v3/adoption/**`, `workflow_v3/procedures/**`, and `workflow_v3/evals/**` are source docs for repository operation and validation. They are not generated pack artifacts and are not uploaded as Project Files/Sources by default.

## Refresh reporting

Future pack packages must report these fields separately:

- `project_instruction_ui_update_required`;
- `project_sources_files_refresh_required`;
- `request_only_sources_refresh_required`;
- `do_not_upload_as_project_file`.

## Current slice boundary

No actual pack files are generated in this slice.

No current Project Files/Sources refresh is required.

No actual ChatGPT Project is updated.

END_OF_FILE: workflow_v3/project_packs/README.md
