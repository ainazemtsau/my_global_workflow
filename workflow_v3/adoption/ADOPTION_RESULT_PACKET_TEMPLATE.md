# Adoption Result Packet Template

status: template

## Result Packet

`result_packet_id`:

`direction_id`:

`adoption_mode`:

`clean_start_reason`:

`legacy_evidence_allowed`:

`legacy_evidence_paths`:

`accepted_initial_direction_spine`:

`accepted_initial_direction_map`:

`accepted_initial_active_front`:

`runtime_root_created`: true | false

`runtime_root_path_if_created`:

`project_setup_required`: true | false

`project_refresh_required`: true | false

`project_instruction_ui_update_required`:

`project_sources_files_refresh_required`:

`request_only_sources_refresh_required`:

`do_not_upload_as_project_file`:

`rollback/coexistence notes`:

`acceptance_decision_ref`:

`validation_results`:

`residual_risks`:

`exact_next_move`:

## Boundary

This packet is candidate evidence until reviewed and accepted. It is not accepted state, a migration, a Project UI update, a Project Files/Sources refresh, or a generated pack upload.

END_OF_FILE: workflow_v3/adoption/ADOPTION_RESULT_PACKET_TEMPLATE.md
