# Style And Conventions

The repository is mostly Markdown workflow/runtime source. Prefer small, scoped diffs. Preserve existing Markdown structure and canonical stage IDs. Do not touch `directions/**`, `docs/**`, `migration/**`, or unrelated stage prompts unless explicitly named. Use exact anchor text when a repository patch packet requires read-back anchors.