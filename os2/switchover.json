{
  "schema": "os2.switchover.v1",
  "edits": [
    {
      "file": "AGENTS.md",
      "old": "- Respect budgets when editing `os/**`: kernel ≤1500 words, a play ≤600, six state file types (see `os/MAINTENANCE.md`).",
      "new": "- Respect budgets when editing `os/**`: kernel ≤1500 words, a play ≤600, five state file types (see `os/MAINTENANCE.md`).",
      "why": "счёт видов состояния стал пять; AGENTS.md несёт тот же бюджет и обязан совпадать"
    },
    {
      "file": "os/BOOTSTRAP.md",
      "old": "   Ничего в CHARTER/TREE не записывается без твоего точного утверждения (G9).",
      "new": "   Ничего в CHARTER и в карточках `node` не записывается без твоего точного утверждения (G9).",
      "why": "G9's TREE target became the node cards; gate meaning unchanged (Russian file, Russian replacement)."
    },
    {
      "file": "os/BOOTSTRAP.md",
      "old": "Сбой чата не теряет сохранённое: `NOW.open_calls` хранит готовые, запущенные, ожидающие и заблокированные продолжения; `issues` — нерешённые проблемы; LOG/history — завершённое.",
      "new": "Сбой чата не теряет сохранённое: карточки `call` хранят готовые, запущенные, ожидающие и заблокированные продолжения; карточки `issue` — нерешённые проблемы; журналы карточек и `history/` — завершённое.",
      "why": "Names three containers that no longer exist (`NOW.open_calls`, `NOW.issues`, LOG.md); the recovery surfaces are call/issue cards, card journals and history/."
    },
    {
      "file": "os/BOOTSTRAP.md",
      "old": "`TREE.md` — единственный глобальный roadmap.",
      "new": "Карточки `node` — единственный глобальный roadmap.",
      "why": "TREE.md is gone; the single roadmap is the node cards."
    },
    {
      "file": "os/BOOTSTRAP.md",
      "old": "`NOW.tracks` при необходимости группирует только независимые исполнительные CALL внутри активного бета.",
      "new": "Карточки `track` при необходимости группируют только независимые исполнительные CALL внутри активного бета.",
      "why": "`NOW.tracks` is gone; lanes are track cards."
    },
    {
      "file": "os/BOOTSTRAP.md",
      "old": "Возникшая посторонняя работа сначала попадает в компактный `issues` с route/review trigger",
      "new": "Возникшая посторонняя работа сначала попадает в компактную карточку `issue` с route/review trigger",
      "why": "There is no `issues` list to fall into; stray work becomes a compact issue card."
    },
    {
      "file": "os/EXTENDING.md",
      "old": "the CALL/RESULT schemas, or the six state file types",
      "new": "the CALL/RESULT schemas, or the five state file types",
      "why": "счёт стал пять: CHARTER · NOW · cards/ · history/ · knowledge/. Шесть сходилось только если считать work/, а соседняя строка говорит, что work/ — не состояние"
    },
    {
      "file": "os/KERNEL.md",
      "old": "| `TREE.md` | one global outcome roadmap; future objectives remain visible | frame, map, shape, review |\n| `NOW.md` | at most one active objective, tasks, execution lanes/calls, open issues, direction forecast, recurring work, decisions | every leg |\n| `LOG.md` | append-only one-line leg index | every leg |",
      "new": "| `NOW.md` | pointer: bet node id, lane WIP limit | every leg |\n| `cards/` | one file per entity — bet/node/task/call/issue/decision/recurring/track/extra — each carrying its journal; `node` cards are the roadmap, future objectives visible; closed → `cards/closed/` | `node`: frame/map/shape/review; rest: every leg |",
      "why": "TREE.md and LOG.md no longer exist and NOW.md no longer holds tasks/calls/issues/decisions/forecast/recurring; the roadmap, the entity list and the per-entity journal now live in cards/, and node-card write authority (frame/map/shape/review) is preserved unchanged. [поправлено проверкой]"
    },
    {
      "file": "os/KERNEL.md",
      "old": "Read fresh `NOW.md`, the play and named evidence.",
      "new": "Read fresh `NOW.md`/cards, the play and named evidence.",
      "why": "After the move NOW.md is a three-line pointer, so reading it alone no longer gives a leg fresh state to open on. [поправлено проверкой]"
    },
    {
      "file": "os/KERNEL.md",
      "old": "`status: parked | shaped | active | done | dropped`, optional children.",
      "new": "`status: parked | shaped | active | done | dropped`, `_parent` (root: none) and `_pos`.",
      "why": "A node card carries no children list; its place in the tree is declared by `_parent` and `_pos` on the card itself. [поправлено проверкой]"
    },
    {
      "file": "os/KERNEL.md",
      "old": "A **bet** is the one active local objective in `NOW.md`.",
      "new": "A **bet** is the one active local objective.",
      "why": "The bet is an entity in cards/; NOW.md only names its node id, so pointing at NOW.md as the bet's home would send writers to the wrong file."
    },
    {
      "file": "os/KERNEL.md",
      "old": "`open_calls.status` decides dispatchability.",
      "new": "the call card's `status` decides dispatchability.",
      "why": "`open_calls` was a NOW.md section that no longer exists; dispatchability is read from the call card's own `status`."
    },
    {
      "file": "os/KERNEL.md",
      "old": "Future objectives stay outcome-level in TREE;",
      "new": "Future objectives stay outcome-level node cards;",
      "why": "G2 names TREE as the place future objectives sit; after the move they are node cards."
    },
    {
      "file": "os/KERNEL.md",
      "old": "**G9 (co-creation).** CHARTER/TREE change only after",
      "new": "**G9 (co-creation).** CHARTER/nodes/approvals change only after",
      "why": "G9 gates a file that no longer exists; the gated surface is now the node cards. [реестр подписей назван: раньше его накрывало слово TREE]"
    },
    {
      "file": "os/KERNEL.md",
      "old": "`day` never edits CHARTER/TREE or silently creates strategy.",
      "new": "`day` never edits CHARTER/nodes or silently creates strategy.",
      "why": "Same vanished file named in the day-save boundary; the ban must land on the node cards or `day` becomes free to edit the roadmap."
    },
    {
      "file": "os/KERNEL.md",
      "old": "## 3. Direction state — six file types, never more",
      "new": "## 3. Direction state — five file types, never more",
      "why": "счёт стал пять: CHARTER · NOW · cards/ · history/ · knowledge/. Шесть сходилось только если считать work/, а соседняя строка говорит, что work/ — не состояние"
    },
    {
      "file": "os/KERNEL.md",
      "old": "Budgets are absolute: kernel ≤1500 words, each play ≤600, six state file types.",
      "new": "Budgets are absolute: kernel ≤1500 words, each play ≤600, five state file types.",
      "why": "счёт стал пять: CHARTER · NOW · cards/ · history/ · knowledge/. Шесть сходилось только если считать work/, а соседняя строка говорит, что work/ — не состояние"
    },
    {
      "file": "os/MAINTENANCE.md",
      "old": "kernel ≤1500 words, a play ≤600, six state file types",
      "new": "kernel ≤1500 words, a play ≤600, five state file types",
      "why": "счёт стал пять: CHARTER · NOW · cards/ · history/ · knowledge/. Шесть сходилось только если считать work/, а соседняя строка говорит, что work/ — не состояние"
    },
    {
      "file": "os/adapters/SESSION_PAYLOAD.md",
      "old": "- Before every leg/important day answer reread fresh Git main: KERNEL, NOW, the\n  play and named evidence. Prior turns are conversation, never state.",
      "new": "- Before every leg/important day answer reread fresh Git main: KERNEL, NOW, the\n  direction's open cards, the play and named evidence. Prior turns are\n  conversation, never state.",
      "why": "Rereading NOW alone no longer reaches the state; the cards it points at carry it. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/SESSION_PAYLOAD.md",
      "old": "  plain-Russian brief from CHARTER/TREE/NOW/recent LOG/relevant knowledge:",
      "new": "  plain-Russian brief from CHARTER/NOW/cards/recent journals/relevant knowledge:",
      "why": "The day brief can no longer be derived from TREE.md or LOG.md; the sources are the cards and history. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/SESSION_PAYLOAD.md",
      "old": "  OS defect->maintenance. Day never edits CHARTER/TREE or invents strategy.",
      "new": "  OS defect->maintenance. Day never edits CHARTER, node cards or the\n  owner_approved card, and never invents strategy.",
      "why": "The roadmap day may not edit is now the node cards, not TREE.md. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/SESSION_PAYLOAD.md",
      "old": "- At most one active local objective exists. TREE is the only roadmap; future\n  objectives stay parked/shaped. Tracks are execution lanes only inside the\n  current bet. No bet means no non-recurring lanes or legacy dispatch; one",
      "new": "- At most one active local objective exists. Node cards are the only roadmap;\n  future objectives stay parked/shaped. Tracks are execution lanes only inside\n  the current bet. No bet means no non-recurring lanes or legacy dispatch; one",
      "why": "TREE.md is gone; the single roadmap is the node cards (rewrapped to keep the payload's line width)."
    },
    {
      "file": "os/adapters/SESSION_PAYLOAD.md",
      "old": "- NOW.issues holds only factual unresolved problems with stable id, owning\n  route, review trigger and evidence pointer. It is not a task backlog. Ideas\n  go to captures/TREE; owner choices to decisions; OS defects to maintenance.\n  A NEW row is a pointer, not an archive (~800 chars, template keys only):\n  owner words and analysis live in history/knowledge; rows written before that\n  rule stay valid as they are. A leg settling an issue routed to its own play\n  closes it in that RESULT with disposition+evidence — when that play's own\n  `Writes:` line covers NOW issues; otherwise day/pulse closes it.",
      "new": "- Issue cards hold only factual unresolved problems with stable id, owning\n  route, review trigger and evidence pointer. They are not a task backlog.\n  Ideas go to captures/node cards; owner choices to decision cards; OS defects\n  to maintenance. A NEW issue card is a pointer, not an archive (~800 chars,\n  template keys only): owner words and analysis live in history/knowledge;\n  cards written before that rule stay valid as they are. A leg settling an\n  issue routed to its own play closes it in that RESULT with\n  disposition+evidence — when that play's own `Writes:` line covers issue\n  cards; otherwise day/pulse closes it.",
      "why": "`NOW.issues` and its rows no longer exist: issues, decisions and parked goals are issue/decision/node cards; the ~800-char pointer discipline and the closing rule are carried over unchanged."
    },
    {
      "file": "os/adapters/SESSION_PAYLOAD.md",
      "old": "- Plain input resolves from NOW: mission->frame; roadmap/future goal->map;",
      "new": "- Plain input resolves from NOW and the cards: mission->frame; roadmap/future goal->map;",
      "why": "Routing plain input needs the tasks/calls/issues that left NOW.md for the cards."
    },
    {
      "file": "os/adapters/SESSION_PAYLOAD.md",
      "old": "  owner approval is required before CHARTER/TREE state_changes (G9).",
      "new": "  owner approval is required before CHARTER, node-card or owner_approved-card\n  state_changes (G9).",
      "why": "G9's TREE target is now the node cards; the gate itself is unchanged. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/autonomy.md",
      "old": "Decision inbox = `NOW.md -> decisions`, surfaced by sessions and pulse.",
      "new": "Decision inbox = the direction's decision cards, surfaced by sessions and pulse.",
      "why": "The `decisions` section of NOW.md no longer exists; decisions are cards."
    },
    {
      "file": "os/adapters/chatgpt-project.md",
      "old": "1. Paste the selected `NOW.open_calls` artifact or the local continuation from the previous `RESULT.next`.",
      "new": "1. Paste the selected open call card artifact or the local continuation from the previous `RESULT.next`.",
      "why": "`NOW.open_calls` is gone; the pasteable artifact is the open call card."
    },
    {
      "file": "os/adapters/claude-project.md",
      "old": "or rely on self-contained CALLs and pasted NOW.md — both work.",
      "new": "or rely on self-contained CALLs and pasted cards — both work.",
      "why": "Pasting NOW.md now conveys two pointer lines, not state; the state to paste is the cards."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "- Apply the RESULT's declared state-change intent atomically against fresh current state; append the log line once to LOG.md (lane-mode prefixes lane id after session-id); save the full RESULT once to `history/<date>-<session-id>.md`; commit with a descriptive message (`<direction>[/<track>] <play> <node/task>: <log line>`).",
      "new": "- Apply the RESULT's declared state-change intent atomically against fresh current state. **The writer never opens a card file to edit it by hand: every change to a card is an `osctl card new|set|block|unset|close|reopen` call naming the entity's stable id; CHARTER.md, NOW.md and `work/` stay hand-written files.** The end of a leg is one `osctl leg close --leg <id> --play <play> --scope <…> --log <line> --result-file <file> [--id <entity> …]`: a single transaction that saves the full RESULT once to `history/<date>-<session-id>.md` and appends the log line once to the journal of every named entity (lane-mode prefixes lane id after session-id). Commit with a descriptive message (`<direction>[/<track>] <play> <node/task>: <log line>`) — that message is itself the direction's journal line.",
      "why": "LOG.md is gone: the log line now lands in each entity's own journal, the end of a leg is one osctl leg close, and the writer edits no state file by hand. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "A history/LOG mismatch is state inconsistency and routes to repair",
      "new": "A history/journal mismatch is state inconsistency and routes to repair",
      "why": "there is no LOG.md to compare history/ against; the pair is now history/ plus the entity journals."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "Clear/upsert `open_calls` by stable call id:",
      "new": "Close/upsert `call` cards by stable card id:",
      "why": "open_calls is no longer a NOW section; a call is a card and clearing it means closing it."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "append LOG/history: key by session-id/history path and write once;",
      "new": "append a journal line and save the history report (one `osctl leg close`): key by session-id/history path and write once;",
      "why": "the append target is the entities' journals plus history/, written by one command, not LOG.md."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "create a complete work/CALL file: absent means create, byte-identical means no-op, different existing content is a collision unless the RESULT explicitly declared a patch to that file.",
      "new": "create a complete `work/` document (hand-written — `work/` is outputs and evidence, never current state): absent means create, byte-identical means no-op, different existing content is a collision unless the RESULT explicitly declared a patch to that file. A new CALL is not a file but a card (`osctl card new --kind call`), keyed the same way by card id.",
      "why": "the clause bundled a hand-written work/ file with a CALL; after the move only the first is still a file, the second is a card."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "Reject CHARTER.md/TREE.md changes whose RESULT carries no `owner_approved` mark (gate G9)",
      "new": "Reject CHARTER.md changes and changes to `node` cards or the `owner_approved` registry card whose RESULT carries no `owner_approved` mark (gate G9)",
      "why": "TREE.md no longer exists; G9 now guards CHARTER.md and the node cards. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "lane-mode NOW hygiene: without an active bet,",
      "new": "lane-mode state hygiene: without an active bet,",
      "why": "tasks, calls, decisions and tracks are cards now, so this hygiene is not about NOW.md."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "whose `for` resolves to a parked TREE node marked `outcome_kind: specification`",
      "new": "whose `for` resolves to a parked `node` card marked `outcome_kind: specification`",
      "why": "the node and its outcome_kind live on a card, not in TREE.md."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "changes no CHARTER/TREE, and contains only the agreed NOW/CALL delta",
      "new": "changes no CHARTER and no `node` card, and contains only the agreed card/NOW/CALL delta",
      "why": "the day-save boundary named TREE (gone) and NOW (no longer the carrier of the delta). [поправлено проверкой]"
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "resolve engineering validation from the returning `open_calls`\n    entry before consulting the latest contract.",
      "new": "resolve engineering validation from the returning `call`\n    card before consulting the latest contract.",
      "why": "the contract pin is read off the returning CALL's own card, not off a NOW section that no longer exists."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "Atomically swap its `open_calls` row for a current-pinned root recording `replaces`",
      "new": "Atomically swap its `call` card for a current-pinned root card recording `replaces`",
      "why": "the swapped record is a card, not a row inside NOW."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "authority to clear `NOW.open_calls` or mark done",
      "new": "authority to close the pending `call` card or mark done",
      "why": "NOW.open_calls no longer exists; clearing a call means closing its card."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "NOW hygiene (mechanical only): if state_changes write `NOW.md`, reject when `open_calls` keeps the returning CALL id or adds text explicitly marked `returned|done|superseded|cancelled`, or when `decisions` adds answered/accepted/declined text. An `issues` row's pointer form (schema §Issues) is `audit`'s to flag by row id, never the writer's to bounce: a row this RESULT adds or rewrites never blocks the apply on its shape. A removed pre-migration selector already present in NOW is inert residue: never read, validate, update, or dispatch it, and its presence alone does not block an unrelated apply. If it is the only carrier of outstanding work, route repair to register that CALL/pointer in `open_calls` before dispatch and delete the residue.",
      "new": "card hygiene (mechanical only): reject when state_changes leave the returning CALL's card open, when an open `call` card carries text explicitly marked `returned|done|superseded|cancelled` instead of being closed, or when a `decision` card is added already answered/accepted/declined. An `issue` card's pointer form (schema §Issues) is `audit`'s to flag by card id, never the writer's to bounce: a card this RESULT adds or rewrites never blocks the apply on its shape. A removed pre-migration selector still carried on a card is inert residue: never read, validate, update, or dispatch it, and its presence alone does not block an unrelated apply. If it is the only carrier of outstanding work, route repair to register that CALL/pointer as its own `call` card before dispatch and delete the residue.",
      "why": "every section this bullet policed (open_calls, decisions, issues) is a card kind now, and nothing is written into NOW.md any more, so its trigger and its targets both had to move. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "(the writer holds it in `open_calls` —",
      "new": "(the writer holds it on that CALL's own `call` card —",
      "why": "the independent copy of the CALL's done_when now lives on the call card."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "the signal the writer already holds in `open_calls` — never re-derived from the product repo",
      "new": "the signal the writer already holds on the `call` card — never re-derived from the product repo",
      "why": "same address change for the review-evidence signal."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "for `play: work|guide`, reject `TREE.md` edits,",
      "new": "for `play: work|guide`, reject `node`-card edits,",
      "why": "there is no TREE.md to forbid editing; the tree is the node cards."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "Review closes bets and harvests TREE;",
      "new": "Review closes bets and harvests the node cards;",
      "why": "the harvest target is the node cards."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "Every return keeps TREE parked and bet/tasks/tracks absent.",
      "new": "Every return keeps that node card parked and bet/tasks/tracks absent.",
      "why": "parked status is a field on the node's own card."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "Record issued CALLs in `NOW.md -> open_calls`; in lane-mode require lane/status/call and preserve every unrelated id. Clear the returning entry when its RESULT arrives. If that id remains, bounce.",
      "new": "Record issued CALLs as `call` cards (`osctl card new --kind call`); in lane-mode require lane/status/call and preserve every unrelated card. Close the returning card (`osctl card close`) when its RESULT arrives. If it is still open, bounce.",
      "why": "the registry of issued CALLs moved from a NOW section to one card per call, written only by command."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "then any apply that changes `tracks`, `open_calls`, `tasks`, `decisions`, or other mirrored state ALSO regenerates those live sections per that knowledge entry, in the same commit.",
      "new": "then any apply that changes `track`, `call`, `task`, `decision` cards, or other mirrored state ALSO regenerates those live sections per that knowledge entry, in the same commit.",
      "why": "the mirrored state is card kinds now, not NOW sections. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "The panel is a render over NOW+git",
      "new": "The panel is a render over the cards + NOW + git",
      "why": "NOW is only the direction pointer now; the panel's source is the cards."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "`collect` command: resolve only `open_calls` and pending `decisions`.",
      "new": "`collect` command: resolve only open `call` cards and pending `decision` cards.",
      "why": "collect reads cards, not NOW sections."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "schema/soft ceilings, TREE `why`/statuses,",
      "new": "schema/soft ceilings, node-card `why`/statuses,",
      "why": "audit can no longer read why/statuses out of TREE.md."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "cross-references, LOG/history pointers, and G1-G4.",
      "new": "cross-references, journal/history pointers, and G1-G4.",
      "why": "the pointers audit checks are journal entries plus history/, LOG.md being gone."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "read-only morning report since yesterday (or requested window): recent LOG outcomes,",
      "new": "read-only morning report since yesterday (or requested window): recent journal outcomes,",
      "why": "digest reads the entity journals plus git log, not LOG.md."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "and `live/<dir>/NOW.md` itself",
      "new": "and `live/<dir>/NOW.md` + `live/<dir>/cards/` itself",
      "why": "NOW.md alone is now a two-line pointer; the state a session must read itself is in cards/."
    },
    {
      "file": "os/adapters/coding-agent.md",
      "old": "Report that semantic conflict.",
      "new": "Report that semantic conflict. One entity is one file, so legs that touched different entities touched different files and Git merges them untouched: a semantic rebase is needed only inside a card two legs both changed.",
      "why": "названо, когда столкновения НЕТ: раньше ноги дрались за один файл, теперь за один файл дерутся только те, кто тронул одну сущность"
    },
    {
      "file": "os/adapters/other-platforms.md",
      "old": "start each chat by pasting, in this order — the payload, the play file, NOW.md, then the CALL last.",
      "new": "start each chat by pasting, in this order — the payload, the play file, NOW.md and the cards the CALL names, then the CALL last.",
      "why": "NOW.md no longer carries the context a bare chat needs; the named cards do. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/runtime.md",
      "old": "   CALL's durable launch claim is its `open_calls.status: running` receipt.",
      "new": "   CALL's durable launch claim is its call card's `status: running` receipt.",
      "why": "`open_calls.status` is a NOW.md path that no longer exists; the receipt lives on the call card."
    },
    {
      "file": "os/adapters/runtime.md",
      "old": "5. Owner gates remain gates: CHARTER/TREE approval, external/irreversible\n   effects, merge/push gates, and any play `(owner)` step require the owner's\n   explicit words or an owner-ack token.",
      "new": "5. Owner gates remain gates: CHARTER and node-card approval,\n   external/irreversible effects, merge/push gates, and any play `(owner)` step\n   require the owner's explicit words or an owner-ack token.",
      "why": "TREE approval now means approval of node cards; gate scope and wording are otherwise unchanged."
    },
    {
      "file": "os/adapters/runtime.md",
      "old": "The thread id is cache only; CHARTER, TREE, NOW, recent LOG/history and declared knowledge remain authority.",
      "new": "The thread id is cache only; CHARTER, NOW, the cards, recent history and declared knowledge remain authority.",
      "why": "TREE.md and LOG.md no longer exist; the authoritative reads are CHARTER, NOW, the cards and history."
    },
    {
      "file": "os/adapters/runtime.md",
      "old": "That leg saves only the exact agreed NOW/CALL delta, emits one RESULT",
      "new": "That leg saves only the exact agreed card/NOW delta, emits one RESULT",
      "why": "A day leg's agreed delta lands on cards; NOW carries only the bet pointer."
    },
    {
      "file": "os/adapters/worktrees.md",
      "old": "the owner's WIP limit caps what runs inside it; `open_calls` is the dispatch frontier)",
      "new": "the owner's WIP limit caps what runs inside it; the open call cards are the dispatch frontier)",
      "why": "`open_calls` was a NOW.md section; the dispatch frontier is now the open call cards."
    },
    {
      "file": "os/adapters/worktrees.md",
      "old": "and re-reads NOW.md before EACH write",
      "new": "and re-reads NOW.md plus the cards it will touch before EACH write",
      "why": "NOW.md keeps only the bet pointer, so re-reading it alone no longer refreshes the state being written."
    },
    {
      "file": "os/adapters/worktrees.md",
      "old": "Edit only your workstream's own regions; the `updated:` line and the shared decision / pending lists are the hot spots.",
      "new": "Edit only the cards your workstream owns: one entity is one file, so legs working different entities no longer meet in a shared file. The remaining shared surfaces are `NOW.md` and any card two legs both name.",
      "why": "The named hot spots lived inside one shared NOW.md; after one-entity-one-file they do not exist, and the honest remaining collision surface is NOW.md or a card two legs both name."
    },
    {
      "file": "os/adapters/worktrees.md",
      "old": "when present, `NOW.tracks` groups mergeable `open_calls` that all resolve to the current `NOW.bet` node/tasks. Future goals remain parked/shaped in TREE; unrelated work waits in issues until its owning play admits it.",
      "new": "when present, track cards group mergeable call cards that all resolve to the current `NOW.bet` node/tasks. Future goals remain parked/shaped as node cards; unrelated work waits in issue cards until its owning play admits it.",
      "why": "`NOW.tracks`, `open_calls` and TREE are gone; the same objects are now track/call/node/issue cards (`NOW.bet` survives)."
    },
    {
      "file": "os/adapters/worktrees.md",
      "old": "# ...semantic-rebase the RESULT delta, update LOG/history, maintain END_OF_FILE trailers...",
      "new": "# ...semantic-rebase the RESULT delta, apply it with osctl card/log/leg close (full RESULT lands in history/), maintain END_OF_FILE trailers...",
      "why": "There is no LOG.md to update; the apply step is now the osctl card/log/leg-close commands. [поправлено проверкой]"
    },
    {
      "file": "os/adapters/worktrees.md",
      "old": "3. Track routing lives in `NOW.md`; engineering write isolation lives in the product repo",
      "new": "3. Track routing lives in track cards (`NOW.md` keeps only the bet pointer and `track_wip_limit`); engineering write isolation lives in the product repo",
      "why": "Track routing left NOW.md for track cards; only the WIP limit stayed behind."
    },
    {
      "file": "os/plays/converge-arch.md",
      "old": "Reads: work/converge-<node>.md (closed §GLOSSARY/§WHAT), TREE.md, NOW.md, knowledge/contract-*, sibling canon.",
      "new": "Reads: work/converge-<node>.md (closed §GLOSSARY/§WHAT), node cards, NOW.md, knowledge/contract-*, sibling canon.",
      "why": "TREE.md no longer exists; the node graph it checks interactions against is now the node cards"
    },
    {
      "file": "os/plays/converge-arch.md",
      "old": "work/converge-<node>-arch.md; NOW.md (open_calls, decisions); LOG.md. Proposes contract canon",
      "new": "work/converge-<node>-arch.md; call and decision cards; entity journals. Proposes contract canon",
      "why": "open_calls/decisions are no longer NOW.md sections and LOG.md is gone; they are call and decision cards [поправлено проверкой]"
    },
    {
      "file": "os/plays/converge-verify.md",
      "old": "Reads: TREE.md, CHARTER.md, knowledge/ checklists/canon;",
      "new": "Reads: the node card, CHARTER.md, knowledge/ checklists/canon;",
      "why": "TREE.md no longer exists; the done_when it attacks lives on the node card"
    },
    {
      "file": "os/plays/converge-verify.md",
      "old": "Writes: NOW.md (decisions, open_calls — below the ceiling a finding bounces a row), LOG.md. Never writes knowledge/.",
      "new": "Writes: decision and call cards (below the ceiling a finding bounces a row) and journals. Never writes knowledge/.",
      "why": "decisions/open_calls are cards now and LOG.md is gone, so the write target is named wrong [поправлено проверкой]"
    },
    {
      "file": "os/plays/converge.md",
      "old": "Reads: TREE.md (node), CHARTER.md (lenses, risk_posture), NOW.md, knowledge/ + the node's history/.",
      "new": "Reads: cards/ (node), CHARTER.md (lenses, risk_posture), NOW.md, knowledge/ + the node's history/.",
      "why": "TREE.md no longer exists; the node it reads is now a node card in cards/"
    },
    {
      "file": "os/plays/converge.md",
      "old": "Writes: work/converge-<node>.md, NOW.md (open_calls, decisions), LOG.md. Proposes glossary canon",
      "new": "Writes: work/converge-<node>.md; call and decision cards. Proposes glossary canon",
      "why": "open_calls/decisions are no longer NOW.md sections and LOG.md is gone; they are call and decision cards"
    },
    {
      "file": "os/plays/day.md",
      "old": "Reads: CHARTER.md, TREE.md, NOW.md, recent LOG.md, due issue evidence,",
      "new": "Reads: CHARTER.md, NOW.md, cards/, recent card journals and `git log`, due issue evidence,",
      "why": "TREE.md and LOG.md are gone; the current view now comes from cards and the assembled journal"
    },
    {
      "file": "os/plays/day.md",
      "old": "Writes after explicit save only: NOW.md, LOG.md, and issued CALL artifacts. It never writes CHARTER.md or TREE.md.",
      "new": "Writes after explicit save only: cards and their journal lines, and issued CALL artifacts. It never writes CHARTER.md, node cards, or the `owner_approved` registry card.",
      "why": "the saved delta lands on cards, not NOW sections or LOG.md; the tree limit is restated against node cards [поправлено проверкой]"
    },
    {
      "file": "os/plays/frame.md",
      "old": "Writes: CHARTER.md, TREE.md (root node only), NOW.md, LOG.md.",
      "new": "Writes: CHARTER.md, cards/ (the root node card and the issued call), NOW.md.",
      "why": "TREE.md and LOG.md no longer exist; the root node and the map_evidence CALL frame issues are both cards."
    },
    {
      "file": "os/plays/frame.md",
      "old": "5. **Root node** — TREE.md gets ONLY the root: the mission as an outcome with done_when.",
      "new": "5. **Root node** — cards/ gets ONLY the root `node` card: the mission as an outcome with done_when.",
      "why": "Names the file the root is written into; after the move that is a node card in cards/."
    },
    {
      "file": "os/plays/frame.md",
      "old": "state_changes register a `map_evidence` research CALL in `NOW.open_calls` (research play; deep-research-capable model)",
      "new": "state_changes register a `map_evidence` research CALL as a `call` card (research play; deep-research-capable model)",
      "why": "NOW.open_calls no longer exists; a registered CALL is a call card."
    },
    {
      "file": "os/plays/frame.md",
      "old": "the issued map path is registered in `NOW.open_calls` and handed off by `RESULT.next`.",
      "new": "the issued map path is registered as a `call` card and handed off by `RESULT.next`.",
      "why": "The play's done-when pointed at a section of NOW.md that is gone."
    },
    {
      "file": "os/plays/guide.md",
      "old": "Reads: NOW.md (always); the CALL; the owner's screenshots and answers in-session.",
      "new": "Reads: NOW.md and the bet's cards (always); the CALL; the owner's screenshots and answers in-session.",
      "why": "NOW.md used to carry the task/call rows guide reads; shrunk to a bet pointer it no longer contains them."
    },
    {
      "file": "os/plays/guide.md",
      "old": "Writes: NOW.md (task status), LOG.md via state_changes.",
      "new": "Writes: cards/ (task status, journal entry) via state_changes.",
      "why": "Task status is a task card field; LOG.md is gone. [поправлено проверкой]"
    },
    {
      "file": "os/plays/guide.md",
      "old": "guide never edits TREE.md, closes the bet, or activates another node.",
      "new": "guide never edits `node` cards or the `owner_approved` card, closes the bet, or activates another node.",
      "why": "An authority boundary stated against a file that no longer exists — it must name the node cards to keep binding. [поправлено проверкой]"
    },
    {
      "file": "os/plays/map.md",
      "old": "Reads: CHARTER.md, TREE.md, NOW.md; latest map_evidence; owner-named sources.",
      "new": "Reads: CHARTER.md, cards/, NOW.md; latest map_evidence; owner-named sources.",
      "why": "The tree map reads is now the node cards, not TREE.md."
    },
    {
      "file": "os/plays/map.md",
      "old": "Writes: TREE.md, NOW.md (open_calls), LOG.md.",
      "new": "Writes: cards/ (nodes/calls/decisions).",
      "why": "All three named surfaces moved into cards/; map never set NOW's bet pointer, so it no longer writes NOW.md."
    },
    {
      "file": "os/plays/map.md",
      "old": "9. **Close (owner)** — after G9, save TREE + log;",
      "new": "9. **Close (owner)** — after G9, save the node cards + log;",
      "why": "There is no TREE file to save; the close saves node cards and their journal lines."
    },
    {
      "file": "os/plays/map.md",
      "old": "TREE.md holds an owner-approved tree; every non-root node has its why;",
      "new": "`node` cards hold an owner-approved tree; every non-root node has its why;",
      "why": "Done-when named the vanished file as the place the approved tree must be."
    },
    {
      "file": "os/plays/pulse.md",
      "old": "Reads: each direction's NOW.md, CHARTER/TREE headers, LOG tail; os/FRICTION.md.",
      "new": "Reads: each direction's NOW.md and cards/, CHARTER header, recent card journals and `git log`; os/FRICTION.md.",
      "why": "NOW.md is only a pointer now, TREE headers and the LOG tail no longer exist as sources"
    },
    {
      "file": "os/plays/pulse.md",
      "old": "Writes: NOW.md of affected directions, LOG.md, knowledge staleness marks.",
      "new": "Writes: NOW.md pointer and cards of affected directions with their journal lines, knowledge staleness marks.",
      "why": "hygiene repairs and triage now change cards, and LOG.md is gone [поправлено проверкой]"
    },
    {
      "file": "os/plays/repair.md",
      "old": "lane/call/issue frontier is incomplete; NOW contradicts TREE/evidence;",
      "new": "lane/call/issue frontier is incomplete; cards contradict NOW or the evidence;",
      "why": "The trigger named TREE.md; after the move the contradiction is between cards and NOW/evidence. (\"CALL contradicts NOW\" earlier in the same line is left alone — NOW still names the bet.)"
    },
    {
      "file": "os/plays/repair.md",
      "old": "Reads: NOW.md, TREE.md, CHARTER.md, LOG tail, recent history/git, named knowledge/work evidence.",
      "new": "Reads: NOW.md, cards/, CHARTER.md, card journals, recent history/git, named knowledge/work evidence.",
      "why": "TREE.md and the LOG tail are gone; the reconstruction sources are the cards and their journals."
    },
    {
      "file": "os/plays/repair.md",
      "old": "Writes: NOW.md; TREE/CHARTER hygiene trims only with G9 approval; LOG; compact knowledge/local-play tombstones and work-surface retirement banners only when they remove false routing authority; optional unrecoverable-state snapshot; optional LOG archive.",
      "new": "Writes: cards/, NOW.md; node-card/CHARTER hygiene trims only with G9 approval; card journals; compact knowledge/local-play tombstones and work-surface retirement banners only when they remove false routing authority; optional unrecoverable-state snapshot.",
      "why": "Repair's whole subject (calls, issues, decisions, lanes) is now cards; LOG and its archive no longer exist as a live surface."
    },
    {
      "file": "os/plays/repair.md",
      "old": "2. **Reconstruct** - newest-first through LOG/history/git/artifacts.",
      "new": "2. **Reconstruct** - newest-first through card journals/history/git/artifacts.",
      "why": "LOG.md is gone; the newest-first spine is the card journals plus git."
    },
    {
      "file": "os/plays/repair.md",
      "old": "elsewhere. Rotate over-ceiling LOG per schema: recent entries stay, older lines move verbatim to the one archive and one pointer remains.",
      "new": "elsewhere.",
      "why": "The rotation duty targeted LOG.md, which no longer exists; nothing replaces it (I did not invent a card-journal rotation rule — see notes)."
    },
    {
      "file": "os/plays/repair.md",
      "old": "CHARTER/TREE semantic changes route to frame/map/review, not repair.",
      "new": "CHARTER/node-card semantic changes route to frame/map/review, not repair.",
      "why": "A routing boundary stated against the vanished file; it must name node cards to keep binding."
    },
    {
      "file": "os/plays/repair.md",
      "old": "Hygiene needs none: dangling pointers, EOF trailers, LOG rotation, dedup, schema-shape fixes.",
      "new": "Hygiene needs none: dangling pointers, EOF trailers, dedup, schema-shape fixes.",
      "why": "\"LOG rotation\" lists a permission-free act on a file that no longer exists (and whose duty this same edit set removes)."
    },
    {
      "file": "os/plays/research.md",
      "old": "read CHARTER.md, TREE.md, NOW.md, knowledge/;",
      "new": "read CHARTER.md, NOW.md, cards/, knowledge/;",
      "why": "outside_review names TREE.md, which no longer exists"
    },
    {
      "file": "os/plays/review.md",
      "old": "update TREE with the owner and select what comes next.",
      "new": "update the tree with the owner and select what comes next.",
      "why": "Uppercase TREE reads as the vanished file; the tree itself survives as node cards."
    },
    {
      "file": "os/plays/review.md",
      "old": "Reads: CHARTER.md, TREE.md, NOW.md, target history/evidence.",
      "new": "Reads: CHARTER.md, cards/, NOW.md, target history/evidence.",
      "why": "TREE.md no longer exists."
    },
    {
      "file": "os/plays/review.md",
      "old": "Writes: TREE.md, NOW.md, LOG.md, knowledge/.",
      "new": "Writes: cards/, NOW.md, knowledge/.",
      "why": "Node/bet/task/lane dispositions are card writes; LOG.md is gone; review still clears NOW's bet pointer."
    },
    {
      "file": "os/plays/review.md",
      "old": "TREE/evidence agree; NOW has no stale work;",
      "new": "cards and evidence agree; no card holds stale work;",
      "why": "NOW no longer holds work at all, so the old condition would be vacuously true; the stale-work check belongs to the cards."
    },
    {
      "file": "os/plays/shape.md",
      "old": "Reads: CHARTER.md, TREE.md, NOW.md, relevant knowledge/.",
      "new": "Reads: CHARTER.md, cards/, NOW.md, relevant knowledge/.",
      "why": "The node shape recites now lives in cards/, not TREE.md."
    },
    {
      "file": "os/plays/shape.md",
      "old": "Writes: TREE.md (the node), NOW.md (the bet), LOG.md.",
      "new": "Writes: cards/ (node/bet/tasks/tracks/call), NOW.md (bet pointer).",
      "why": "The bet, its tasks, lanes and first CALL are cards; NOW.md keeps only the pointer to the bet node."
    },
    {
      "file": "os/plays/shape.md",
      "old": "registers its first-task CALL in `open_calls`, and hands it off through `RESULT.next`.",
      "new": "registers its first-task CALL as a `call` card, and hands it off through `RESULT.next`.",
      "why": "open_calls is gone; registration is a call card."
    },
    {
      "file": "os/plays/shape.md",
      "old": "NOW.md has an approved bet passing G1–G6; its first-task CALL is in `open_calls` and local `RESULT.next`.",
      "new": "NOW.md names an approved bet whose `bet` card passes G1–G6; its first-task CALL is a `call` card and in local `RESULT.next`.",
      "why": "The bet's content moved out of NOW.md into a bet card, while NOW keeps the pointer — the done-when has to name both halves. [поправлено проверкой]"
    },
    {
      "file": "os/plays/shape.md",
      "old": "split into 2–4 child outcome nodes in TREE.md (each a card with its why, owner-approved in-session — G9)",
      "new": "split into 2–4 child outcome `node` cards (each with its why, owner-approved in-session — G9)",
      "why": "Children are written as node cards, not into TREE.md."
    },
    {
      "file": "os/plays/work.md",
      "old": "Reads: TREE.md, NOW.md; files the CALL points to.",
      "new": "Reads: cards/, NOW.md; files the CALL points to.",
      "why": "The node work recites is a card now."
    },
    {
      "file": "os/plays/work.md",
      "old": "Writes: NOW.md call/task/issue state, LOG.md, and the versioned artifact named by the CALL through RESULT.state_changes.",
      "new": "Writes: cards/ call/task/issue state, and the versioned artifact named by the CALL through RESULT.state_changes.",
      "why": "Call/task/issue state moved into cards/ and LOG.md is gone; the call/task/issue wording is kept verbatim because schema §Issues makes this line the routing grant."
    },
    {
      "file": "os/plays/work.md",
      "old": "an issue this leg's own evidence settles leaves NOW with its disposition (schema §Issues)",
      "new": "an issue this leg's own evidence settles closes with its disposition (schema §Issues)",
      "why": "\"leaves NOW\" described departure from a hot section that no longer holds issues; the equivalent act is closing the issue card with its disposition."
    },
    {
      "file": "os/plays/work.md",
      "old": "a future objective stays in TREE.",
      "new": "a future objective stays in the tree.",
      "why": "Names the vanished file as the home of future objectives."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "## TREE.md\n\n```markdown\n# Goal tree: <direction-id>\n\nowner_approved: <date> — history/<file>.md\n\n- id: g-root\n  goal: <mission as outcome>\n  done_when: <verifiable>\n  children:\n    - id: g-xxxx\n      goal: <outcome, not activity>\n      done_when: <verifiable>\n      why: <one line: contribution to parent>\n      # outcome_kind: specification  # only when the approved spec artifact itself exhausts done_when\n      status: parked              # parked | shaped | active | done | dropped\n      # shaped/active add appetite + kill_by\n      # detail: history/<file>.md\n      children: []\n```\n\nRules: outcomes only; tasks never appear here. At most one non-root node is `active`: `NOW.bet.node`. Future objectives stay visible as `parked|shaped`; they are not execution lanes. Width ≤7 open children per node. Every non-root node has `why`. A dropped node keeps a compact reason/pointer; Git/history hold removed detail. `outcome_kind: specification` is an optional owner-approved card field; absence means ordinary build outcome. Its done_when ends at one exact versioned owner-approved specification, not implementation.",
      "new": "## cards/\n\nState lives in `live/<direction-id>/cards/` — one entity, one file, `<id>.md`. Kinds: `bet` · `node` · `task` · `call` · `issue` · `decision` · `recurring` · `track`; a top-level key with no assigned home keeps its own `extra` card named after the key, so nothing is lost silently. Carrier names lead with `_` — `_kind`, `_pos`, `_parent`, `_bet` — so an owner field may be named anything, including `kind` and `status`.\n\nA card is a short YAML header between `---` lines, then `## <name>` body blocks, then the `END_OF_FILE:` trailer. Header values are single-line and short; anything longer, multi-line, or a list/dict is a body block, lists and dicts under a YAML fence. `_pos` is the place among same-kind siblings; for a `node` the place is among siblings under the same `_parent`.\n\nEvery card carries a `## журнал` block: its own history, newest first, one line/≤2 short sentences per leg — `<date> · <what changed> · <history pointer or commit>`. It is an index, not a second summary; `osctl check` flags a journal past 20 lines.\n\nState changes only through `osctl`: `card new|set|block|unset|close|reopen`, `log add`, `leg close`. A card is never hand-edited. `card close` writes the reason into the journal and moves the file to `cards/closed/` in the same format; `card reopen` returns it. A closed card reads exactly like a live one, so closing loses nothing. `card new` refuses a card without the human fields it enforces — `label` and `hook` on a `node`, `description` on a `call` — so no card shows the owner a machine id.\n\nThe direction journal is assembled from these journals plus `git log`: the commit message is the journal line. Full leg reports stay in `history/`.\n\n### node\n\n```markdown\n---\nid: g-xxxx\n_kind: node\n_parent: g-0c26             # absent on the root\n_pos: 3                     # place among siblings\nlabel: <short human name>\nhook: <one line: what this is>\nstatus: parked              # parked | shaped | active | done | dropped\n# outcome_kind: specification  # only when the approved spec artifact itself exhausts done_when\n# shaped/active add appetite + kill_by\n# detail: history/<file>.md\n---\n\n## goal\n<outcome, not activity>\n## done_when\n<verifiable>\n## why\n<contribution to parent>\n```\n\nThe goal tree is the set of `node` cards: `_parent` says whose child a node is, `_pos` its place among siblings. Rules: outcomes only; tasks are never `node` cards. At most one non-root node is `active`: `NOW.bet`. Future objectives stay visible as `parked|shaped`; they are not execution lanes. Width ≤7 open children per node. Every non-root node has `why`. A dropped node keeps a compact reason/pointer; Git/history hold removed detail. `outcome_kind: specification` is an optional owner-approved card field; absence means ordinary build outcome. Its done_when ends at one exact versioned owner-approved specification, not implementation.\n\n### bet\n\nOne card while a bet is open; none between objectives (`NOW.bet: null`).\n\n```markdown\n---\nid: bet-g-xxxx\n_kind: bet\nnode: g-xxxx\nopened: <date>\n---\n\n## goal\n<recitation from the node card>\n## appetite\n2w (started <date>)\n## kill_by\n<threshold + date/event>\n## forecast\n<earliest signal + expected observation>\n## against\n<strongest contrary case + switch trigger>\n## cut_list\n<real exclusions>\n## lens_verdicts\nproduct: <task ids | not_needed: reason>\naudience: <...>\nbusiness: <...>\n```\n\n### task\n\nNo `task` card exists while `NOW.bet` is null; active ≤ the owner-set WIP limit.\n\n```markdown\n---\nid: t-1\n_kind: task\n_bet: g-xxxx\nstatus: open                # open | active | blocked | done\n# blocked adds unblock_when\n---\n\n## goal\n<outcome>\n## done_when\n<verifiable>\n```\n\n### issue\n\nUnresolved only; a pointer card, not a task backlog or an archive.\n\n```markdown\n---\nid: i-ab12                  # stable, never reused\n_kind: issue\nlevel: objective            # direction | roadmap | objective | execution\nroute: review               # frame | map | shape | review | work | repair | pulse\nevidence: <history/work/knowledge pointer>\n# blocks: <stable node/task/call>\n---\n\n## issue\n<one factual problem/unknown>\n## review_when\n<date/event>\n```\n\n### track, call, decision\n\n```markdown\n---\nid: gameplay\n_kind: track\nlabel: \"Gameplay proof\"\nfor: t-1                    # current bet node/task, or recurring id\n---\n```\n\n```markdown\n---\nid: c-117\n_kind: call\n_bet: g-xxxx\ntrack: gameplay             # required when tracks exist\nstatus: ready               # ready | running | waiting | blocked | paused\nto: executor                # session | research | executor\nfor: t-1\nissued: <date>\ncall: work/c-117-call.md\ndescription: <one line: what this call is for>\n# parent: c-116             # same-lane child\n# waiting_on: [c-117-a]\n# receipts: [history/<result>.md]\n# started: <date/time — launch receipt>     # running\n# unblock_when: <condition>                 # blocked\n# paused_by: <owner history pointer>        # paused\n# note: <one-line context/pointer>\n---\n```\n\n```markdown\n---\nid: d-1\n_kind: decision\ntrack: gameplay             # required when tracks exist\n---\n\n## q\n<question>\n## options\n<a, b, c>\n## recommendation\n<a, because ...>\n```\n\n### recurring\n\n```markdown\n---\nid: r-1\n_kind: recurring\ncadence: weekly\nlens: audience\nlast_done: <date>\n---\n\n## goal\n<standing obligation>\n## done_when\n<verifiable>\n```\n\n### extra\n\nA top-level key with no assigned home keeps its own card named after the key. Two exist: `owner_approved`, the approval registry, which is never dropped; and `direction_forecast`.\n\n```markdown\n---\nid: direction_forecast\n_kind: extra\n---\n\n## direction_forecast\nstatus: no_basis             # no_basis | forecast\ntarget: <one explicit dated success criterion>\nas_of: <date>\nbasis: <history/work/source pointer or exact missing basis>\ndrivers: [<major up/down drivers, max 4>]\nupdate_when: <material evidence/date that forces recompute>\n# status: forecast additionally requires:\n# chance: 35%                # central estimate; not completion percent\n# range: 20-50%\n# confidence: low            # low | medium | high\n# calibration: <empirical reference class/source + denominator>\n```",
      "why": "The TREE.md section describes a file that stops existing; the goal tree becomes `node` cards addressed by `_parent`/`_pos`, and the card kinds/service fields need a home."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "## NOW.md\n\n```markdown\n# NOW: <direction-id>          updated: <date> by <session-id>\n\nbet: null                     # legal between objectives\n# OR:\nbet:\n  node: g-xxxx\n  goal: <recitation from TREE>\n  appetite: 2w (started <date>)\n  kill_by: <threshold + date/event>\n  forecast: <earliest signal + expected observation>\n  against: <strongest contrary case + switch trigger>\n  cut_list: [<real exclusions>]\n  lens_verdicts:\n    product: <task ids | not_needed: reason>\n    audience: <...>\n    business: <...>\n\ntasks:                         # [] when bet:null; active ≤ the owner-set WIP limit\n  - id: t-1\n    goal: <outcome>\n    done_when: <verifiable>\n    status: open               # open | active | blocked | done\n    # blocked adds unblock_when\n\ndirection_forecast:\n  status: no_basis             # no_basis | forecast\n  target: <one explicit dated success criterion>\n  as_of: <date>\n  basis: <history/work/source pointer or exact missing basis>\n  drivers: [<major up/down drivers, max 4>]\n  update_when: <material evidence/date that forces recompute>\n  # status: forecast additionally requires:\n  # chance: 35%                # central estimate; not completion percent\n  # range: 20-50%\n  # confidence: low            # low | medium | high\n  # calibration: <empirical reference class/source + denominator>\n\nissues:                         # unresolved only; pointer rows (~800 chars), not a task backlog or an archive\n  - id: i-ab12                  # stable, never reused\n    issue: <one factual problem/unknown>\n    level: objective            # direction | roadmap | objective | execution\n    route: review               # frame | map | shape | review | work | repair | pulse\n    review_when: <date/event>\n    evidence: <history/work/knowledge pointer>\n    # blocks: <stable node/task/call>\n\ntrack_wip_limit: 4             # present only with tracks; owner-set, no fixed ceiling\ntracks:                         # optional execution lanes inside the current bet\n  - id: gameplay\n    label: \"Gameplay proof\"\n    for: t-1                    # current bet node/task, or recurring id\n\nopen_calls:\n  - id: c-117\n    track: gameplay             # required when tracks exist\n    status: ready               # ready | running | waiting | blocked | paused\n    to: executor                # session | research | executor\n    for: t-1\n    issued: <date>\n    call: work/c-117-call.md\n    # parent: c-116             # same-lane child\n    # waiting_on: [c-117-a]\n    # receipts: [history/<result>.md]\n    # started: <date/time — launch receipt>     # running\n    # unblock_when: <condition>                 # blocked\n    # paused_by: <owner history pointer>        # paused\n    # note: <one-line context/pointer>\n\nrecurring:\n  - id: r-1\n    goal: <standing obligation>\n    done_when: <verifiable>\n    cadence: weekly\n    lens: audience\n    last_done: <date>\n\ndecisions:\n  - id: d-1\n    track: gameplay             # required when tracks exist\n    q: <question>\n    options: [a, b, c]\n    recommendation: <a, because ...>\n```",
      "new": "## NOW.md\n\nA pointer to the direction, and nothing longer.\n\n```markdown\n# NOW: <direction-id>\n\nbet: g-xxxx                    # id of the active bet's node card, or null\ntrack_wip_limit: 4             # present only with tracks; owner-set, no fixed ceiling\n\nEND_OF_FILE: live/<direction-id>/NOW.md\n```\n\nEverything NOW once carried as sections — tasks, open_calls, issues, decisions, recurring, tracks, direction_forecast — is a card in `cards/`.",
      "why": "NOW stops holding sections; it becomes a pointer, and every section it listed is now a card whose template moved into `## cards/`."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "### NOW hygiene\n\nNOW is current state, not a diary. `open_calls`, `issues` and `decisions` contain only unresolved items. Returned/done/cancelled items leave hot state for LOG/history.",
      "new": "### Hot state hygiene\n\nLive cards are current state, not a diary. `call`, `issue` and `decision` cards stay live only while unresolved. Returned/done/cancelled entities leave hot state by `osctl card close`; their journals and `history/` hold the record.",
      "why": "The hygiene rule addressed NOW sections and LOG, both of which stop existing; the same rule now governs live cards and closing."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "`bet: null` is normal between objectives. Then `tasks: []`, and no non-recurring execution track/CALL may exist;",
      "new": "`bet: null` is normal between objectives. Then no live `task` card exists, and no non-recurring execution track/CALL may exist;",
      "why": "`tasks: []` names an empty list in a NOW section that no longer exists; the state it describes is now the absence of live task cards."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "History/LOG records each saved prior estimate; NOW holds only the latest.",
      "new": "History and the card's own journal record each saved prior estimate; the card holds only the latest.",
      "why": "LOG disappears and NOW no longer holds the forecast; prior estimates are kept by the forecast card's own journal."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "Ideas go to captures/TREE, owner choices to decisions,",
      "new": "Ideas go to captures/`node` cards, owner choices to decisions,",
      "why": "TREE is gone; an idea routed out of an issue lands on a `node` card."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "**and whose own `Writes:` line covers NOW issues**",
      "new": "**and whose own `Writes:` line covers issues — an unqualified state line counts, a line qualified to other sections does not**",
      "why": "The grant test points at a `Writes:` line covering a NOW section that no longer exists; it now covers issue cards. [поправлено проверкой]"
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "A row is a pointer, never an archive: template keys only, `issue`/`review_when` ≤2 short sentences each, `evidence` pointers only, ~800 characters per row.",
      "new": "A card is a pointer, never an archive: template keys only, `issue`/`review_when` ≤2 short sentences each, `evidence` pointers only, ~800 characters per card outside its `## журнал`.",
      "why": "The pointer-form budget was written for a NOW row; on a card it must exclude the journal, which grows by design."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "Future objectives remain in TREE; unrelated urgent work first routes through review/map/repair.",
      "new": "Future objectives remain `node` cards; unrelated urgent work first routes through review/map/repair.",
      "why": "TREE is gone; future objectives are `node` cards."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "`open_calls` is the sole durable dispatch frontier.",
      "new": "Live `call` cards are the sole durable dispatch frontier.",
      "why": "`open_calls` was a NOW section; the frontier is now the set of live `call` cards."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "## LOG.md\n\nNewest first, one line/≤2 short sentences per leg with a history pointer. It is an index, not a second summary.\n\nWhen over the soft ceiling, repair keeps roughly the newest half-ceiling and moves older lines verbatim into `history/LOG-archive-<direction-id>.md`. LOG keeps one bottom pointer: `archived: ... — sessions before <date>`. Later rotations append to that same archive.\n\n## history/\n\nOne immutable file per leg: `history/<date>-<session-id>.md`, full RESULT verbatim.",
      "new": "## history/\n\nOne immutable file per leg: `history/<date>-<session-id>.md`, full RESULT verbatim. `osctl leg close` writes that file and the matching journal line on every named entity as one move. Direction LOG lines written before the move stay verbatim in `history/LOG-archive-<direction-id>.md`.",
      "why": "LOG.md disappears; its index rule moves to the card journal, and the rotation mechanic has no target left, while the pre-move lines keep their named archive."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "Not in `NOW.issues`, which holds a problem or unknown, not a fact, and not ad-hoc keys invented on an issue or a TREE card.",
      "new": "Not on an `issue` card, which holds a problem or unknown, not a fact, and not ad-hoc keys invented on an issue or a `node` card.",
      "why": "`NOW.issues` and `TREE card` name two files that stop existing; the boundary itself is unchanged."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "## work/\n\nOutputs/evidence, not state. Large binaries use LFS/external storage with a small pointer.",
      "new": "## work/\n\nOutputs/evidence, not state. Being outside state, it is outside `osctl`: CALL briefs and `work/converge-*.md` documents stay hand-written files. Large binaries use LFS/external storage with a small pointer.",
      "why": "The new `## cards/` sentence \"state changes only through osctl\" invites reading work/ documents as osctl-owned; the schema already says work/ is not state, so it is named directly."
    },
    {
      "file": "os/schema/direction-files.md",
      "old": "Templates for the six state file types (KERNEL §3).",
      "new": "Templates for the five state file types (KERNEL §3).",
      "why": "счёт стал пять: CHARTER · NOW · cards/ · history/ · knowledge/. Шесть сходилось только если считать work/, а соседняя строка говорит, что work/ — не состояние"
    },
    {
      "file": "os/schema/packets.md",
      "old": "a parked TREE node marked `outcome_kind: specification`",
      "new": "a parked node card marked `outcome_kind: specification`",
      "why": "TREE.md is gone; the parked specification node is a node card."
    },
    {
      "file": "os/schema/packets.md",
      "old": "already registered in `open_calls` when v29 lands",
      "new": "already registered as an open call when v29 lands",
      "why": "`open_calls` is no longer a place; an open CALL is a call card."
    },
    {
      "file": "os/schema/packets.md",
      "old": "in `open_calls` when v29 activates",
      "new": "registered as open calls when v29 activates",
      "why": "The legacy snapshot is defined by open CALLs, not by a NOW.md section."
    },
    {
      "file": "os/schema/packets.md",
      "old": "old `open_calls` row for a new current-pinned root.",
      "new": "old root's open call card for a new current-pinned root.",
      "why": "There is no `open_calls` table row to swap; the registration is a card."
    },
    {
      "file": "os/schema/packets.md",
      "old": "  <exact edits: NOW.md task/track statuses, TREE.md node changes, files added to work/,",
      "new": "  <exact edits: task/track card statuses, node card changes, files added to work/,",
      "why": "state_changes now names entities, not the vanished NOW.md/TREE.md files."
    },
    {
      "file": "os/schema/packets.md",
      "old": "   Includes CALLs issued by this session with track/status, for NOW.md → open_calls,",
      "new": "   Includes CALLs issued by this session with track/status, as call cards to register,",
      "why": "Issued CALLs are registered as call cards, not written into a NOW.md section."
    },
    {
      "file": "os/schema/packets.md",
      "old": "log: <one line for LOG.md>",
      "new": "log: <one line — this leg's journal entry, recorded by `osctl leg close`>",
      "why": "LOG.md is gone; the line is the leg's journal entry written by `osctl leg close`."
    },
    {
      "file": "os/schema/packets.md",
      "old": "When `NOW.md` has tracks, each CALL, RESULT and pending decision names one.",
      "new": "When the direction has tracks, each CALL, RESULT and pending decision names one.",
      "why": "Tracks are track cards; NOW.md no longer contains them."
    },
    {
      "file": "os/schema/packets.md",
      "old": "may change an existing `open_calls` row `ready → running`",
      "new": "may change an existing open call card's status `ready → running`",
      "why": "The ready→running transition happens on the call card, not on a NOW.md row."
    },
    {
      "file": "os/schema/packets.md",
      "old": "  NOW.md: t-2 → done; clear c-117; register c-118 ready in open_calls.",
      "new": "  t-2 → done; clear c-117; register c-118 ready.",
      "why": "Worked example must show state_changes addressed by entity, with no file name."
    }
  ]
}
