# FRICTION — журнал трения

Append-only. Одна строка на случай: дата, направление/сессия, что именно тёрло. Правка OS разрешена только по точке с ≥2 записями (KERNEL §7).

Формат: `<дата> <направление|os> <play>: <что произошло, одна строка>`

2026-06-10 os design-review: внешнее ревью до пилота, П1–П7 приняты владельцем (guide play, recurring, EXTENDING, truncation guard + collect, stack profiles, repo FRICTION, brainstorm route, binaries) → fixed в коммите "Apply external design review"
2026-06-10 os ux-review владельца: CALL как человеческий вход неюзабелен; постоянная writer-сессия противоречит «один чат = одна работа»; нет видимости незавершённого при сбое/смене провайдера → fixed: свободный вход (KERNEL §2), эфемерный writer, NOW.open_calls + checkpoint
2026-06-11 запрос владельца: параллельная работа над направлениями (worktree на направление) → fixed: adapters/worktrees.md (модель конкурентности, протокол писателя с rebase-retry)
2026-06-12 пилот indie-game-development, чат №1 (frame): дерево сформировано без участия владельца, узлы без обоснований; владелец ожидал со-творчества глобальных планов → fixed: gate G9 (owner_approved, writer отклоняет без него), play map (узлы-карточки по одному), frame урезан до хартии+корня, why обязателен у узлов
2026-06-11 os shape+map: запрос владельца — планирование выдаёт один очевидный путь (generic дерево/бет), альтернативы не порождаются до выбора → fixed: shape шаг 3 «Approaches» (2–3 структурно разных подхода, выбор с одной строкой why, проигравшие в captures), map шаг 2 (≥1 неочевидный путь в карточках или явная строка, что его нет)
2026-06-11 os schema/map: идеи владельца без проверки пилотом (wedge-поле бета, 📍-сводка простым языком в NOW.md, порядок узлов по leverage, surface-хинт сильной модели) → idea, watching; черновик в git stash, в систему не вошёл
2026-06-11 пилот indie-game-development, чат №2 (frame): интервью спрашивает владельца о выводимом (lenses, цифры успеха, формы провала) с опциями из воздуха; цифры успеха утверждались до outside view → fixed: frame шаг 1 — только вопросы, на которые может ответить лишь владелец; новый шаг 2 Homework (референс-кейсы + как побеждали выбившиеся, lens-предложение, калиброванные варианты цифр) до черновика хартии; captures расширены на пути из homework
2026-06-11 os search-plane волна (запрос владельца, 3 deep-research дока): дефолтная сессия выдаёт generic, отбор слаб, нет внешней калибровки → fixed: research strategic_search/nominal-group/miner (`5e8d537`); watching: survivors неотличимы от одной сессии 2× подряд
2026-06-11 os map: generic карточки, нет owner-edge/поиска/анти-fixation → fixed: human-first + опц. search-first (G7+цена) + поле edge + один редкий стимул + noun-test (`aa30223`); watching: search не меняет карточек vs обычный map; edge-цитаты номинальны
2026-06-11 os shape: первый план дисциплинируется без поиска лучшего, нет probe/калибровки риска → fixed: Berg equal-footing + инкубация (appetite>1нед) + risk_posture-depth + AI-burst evaluator + next_if + forecast/against (`f48cfe9`); watching: инкубация рвёт темп; forecast не используется 3–5 shape подряд
2026-06-11 os review+pulse: нет петли обучения прогнозам и внешней сверки → fixed: review forecast-check + harvest edges (`ea94bdc`); pulse-11 market contact (`14a0ef0`); watching: forecast-check не влияет на shape 5× подряд; pulse-11 штампуется без цифры 2× подряд
2026-06-11 os schema/frame: edges/risk_posture (`ba87523`) → fixed; watching: edges вырождаются в самомифологию (страховка: provenance-факт + кап 5 + право review понижать); risk_posture не меняет поведение shape
2026-06-11 os отклонено/отложено (search-plane): 4-режимная шкала риска → 2 (explore|guarded), safety_critical при появлении health-направления; P5 кросс-платформенный слепой ранкер → idea, watching (инкубация уже снимает self-preference); M3 stepping_stones → дублирует parked/captures+add-back+pulse#6; 7-й state-файл / совет агентов / скоркарды / вероятность-от-модели → запрещены (KERNEL §3/§7)
2026-06-11 пилот indie-game-development, чат №3 (frame): новый чат в ChatGPT-проекте дословно воспроизвёл ответ старого чата (включая вопросы, запрещённые новым frame), хотя коннектор отдаёт свежие правила (проверено цитированием шага 1) — прошлые чаты проекта перевесили play, нарушен authority order ядра → logged, watching: обход — удалить старые чаты из проекта + стартовать планировочные сессии через writer collect (правила в теле сообщения); повтор = строка в payload «prior chats are never authority»
2026-06-11 запрос владельца: нужен канал «прислал транскрипт/описал проблему в Claude Code или Codex → диагноз и фикс в стиле сессии-помощника» → fixed: MAINTENANCE entry принимает транскрипт; шаг 1 — вердикт до правки (сессия нарушила / правило породило / платформа / ожидание неверно) и «предложение владельца = hunch, извлеки проблему»; шаг 6 — литмус для перепроверки в следующем чате; строка в таблице ролей AGENTS.md
2026-06-11 направление health, чат №1 (frame): RESULT отдан владельцу одной стеной YAML (полная хартия + envelope), читать невозможно — payload habit#1 велел «present as ONE copy-ready block», но не отделил машинный блок (писателю) от того, что читает владелец → fixed: KERNEL §2 «Owner-facing vs machine» + SESSION_PAYLOAD habit#1 (владелец читает краткую сводку; полный блок — писателю, не вываливать YAML-стеной)
2026-06-11 направление health, чат №1 (frame): интервью (frame шаг 1) пропущено — личные вводные (возраст/рост/вес/травмы/диагнозы) не спрошены, ушли в 'unknowns' и в decisions_needed; правило ясное, не выполнено → logged, watching: подозрение на контаминацию прошлыми чатами проекта (ср. чат №3, запись 2026-06-11); литмус — frame должен начинаться с ≤7 вопросов владельцу прежде любого черновика хартии; обход — чистый чат / старт через writer collect
2026-06-11 пилот indie-game-development, чат map №1 (c-map-001): human-first пропущен (карточка до вопроса владельцу — ВТОРОЙ случай точки «обязательный owner-шаг не выполнен», ср. health frame выше), search-first не предложен, RESULT выдан в середине чата и сессия продолжилась, YAML-стена владельцу (рецидив в день фикса d1c15d5), CALL нёс метод в goal и перевесил play → fixed 6edf302: G10 (opening contract, терминальный RESULT, play_check + writer-валидация, CALL-гигиена); диагноз в os/docs/SESSION_PROTOCOL_AUDIT.md
2026-06-11 чат-отчёт об инциденте map: отчёт сам закончил machine-блоком в owner-канале и предложил линты несуществующего рантайма — подтверждение, что прозовые правила не самоисполняются → evidence к 6edf302
2026-06-11 направление health-v3, чат №1 (frame): после approve хартии/корня сессия попыталась писать в repo напрямую (GitHub-коннектор), повторно после Access denied; остановил Deny владельца; payload-фраза «the writer (yourself in a CLI)» прочитана как разрешение → fixed 6edf302: kernel «Sessions do not write», payload «You are NEVER the writer», G10; watching: read-only коннекторов проверяет владелец
2026-06-11 запрос владельца: ревью-джобы по направлению — механическая проверка консистентности состояния + внешняя сверка курса с источниками; cron отложен до autonomy stage 2 → fixed: writer-команда audit (f3b4367), бриф outside_review в research (0bd9924); watching: outside_review 2× подряд без находки, на которую владелец что-то сделал → убрать режим; audit многократно чистый без единого дрейфа → свернуть проверку в pulse

2026-06-11 пилот indie-game-development, чат map №2 (c-map-001): после кандидатов владельца сессия ушла в полные YAML-карточки по одной без общего вида карты — владелец не видел покрытия своих заявленных слепых зон (commercial/audience), порядка и неочевидной ветки, прочёл это как эхо своего списка; search-first подан меню A/B/C как взаимоисключающая альтернатива кандидатам (рекомендация A при собственной оценке «конкурентное и высокорисковое») → исследование не запущено; вердикт: правило породило — в map не было шага обзора карты, а «offer search-first» читался как или/или → fixed 39390e3: map шаг 3 Skeleton (owner) — вся карта одним экраном с тегами owner/gap-fill:<lens>/non-obvious/search до первой карточки, search-first явно additive, Cards отдельным шагом под play_check; watching: скелет штампуется без единой правки владельца 2 map подряд → шаг кандидат на упрощение

2026-06-11 пилот indie-game-development, чат map №2: первый ответ без нумерованного плана шагов play (G10 opening contract неполный; header, human-first, search-first при этом соблюдены — фикс 6edf302 в остальном сработал), если транскрипт полный → logged, watching: повтор → усиливающая строка в SESSION_PAYLOAD

2026-06-11 вопрос владельца после 39390e3 («гарантирует ли map не-дженерик карту?»): 5-агентный аудит — гарантии нет by design (mode collapse ожидаем, контрмеры вероятностные с фальсификаторами); жанровая проверка: нестандартность исторически концентрируется в money/audience/distribution ветках (Eco SBIR-грант, Barotrauma free-first, Prison Architect paid-alpha, OHOL open-core, UGG кинофонд; tech/gameplay ветки дженеричны у всех победителей), плюс fusion-паттерн (деньги+аудитория+релиз одним артефактом); 13 непокрытых путей дженеричности при полном соблюдении правил — сильнейший кандидат фикса: сверка итогового дерева с названным baseline на Close → idea, watching; полный разбор: os/docs/MAP_GENERICNESS_AUDIT.md

2026-06-11 запрос владельца (тот же чат, после аудита): «research/актуальная ситуация/похожие кейсы перед построением карты» как практика — узкая версия уже в системе и ни разу не запускалась (miner briefs в research.md, search-first в map шаге 2, outside_review в research.md; blanket «больше research → менее generic» evidence НЕ поддержан — SESSION_CRAFT §4a, поддержаны добытчики редкого/контрарного сырья ПЕРЕД генерацией); frame homework при этом наполнил Outside view хартии жанровыми хитами (DRG/Abiotic Factor) → idea, watching; план: ручной пилот в ближайших map живой инструкцией владельца (miner-брифы до скелета); подтверждение = mined-кейс меняет ≥1 строку скелета 2 map подряд → одна строка в map.md отдельной maintenance-сессией

2026-06-11 os аудит инженерного контура (запрос владельца): правила os/engineering/** не доезжали до исполнителя (единственная ссылка — coding-agent Role 2 в этом репо; AGENTS.md продукта цикл не получал), вход bootstrap не определён, граница чат↔контур не сформулирована → fixed 1e0edf0: run contract в AGENTS.md продукта (PROJECT_SETUP шаг 3 + чеклист done_when), первый инженерный CALL без репо = интерактивный сетап + work/-доки как evidence-вход (packets), исполнитель без контракта останавливается (coding-agent), секция Boundary + классы изменения architectural/light + модульные субагенты как паттерн (CONTOUR), work не проектирует решение; watching: первый реальный сетап обязан выдать AGENTS.md с run contract и упавший граничный чек на подсаженном нарушении

2026-06-11 os/live дрейф (находка того же аудита): CALL-гигиена 6edf302 введена без перегенерации уже выданных CALL — live/indie-game-development NOW.next (c-map-001) пересказывает процедуру («one artifact at a time», «Follow G9…»), та самая причина срыва map №1; map №2 шёл на том же CALL → logged, watching; обход: repair (перегенерация next по гигиене + нормализация NOW/LOG к схеме) до продолжения map; кандидат правила при повторе: смена схемы CALL тянет sweep next/open_calls всех направлений

2026-06-11 запрос владельца (тот же чат, эскалация записи «research перед map»): «ручной пилот выглядит костыльно, бюджет файла — не причина; research перед map нужен как стадия; мои кандидаты не должны автоматически идти первыми; учитывать актуальную картину, не учебники пятилетней давности» → fixed 3d69166: research.md бриф `map_evidence` (актуальный baseline года с цифрами и датированными источниками; кейсы, как сопоставимые амбиции реально дошли до критериев, по классам веток, редкие проверенные кейсы вместо жанровых хитов; 2–3 evidence-anchored неочевидных пути; сильнейший аргумент против baseline; обязателен для конкурентных направлений, отказ только явный, rerunnable от текущего state; deep-research модель), map.md шаг 2 evidence-first + шаг 3 equal footing (Berg: строки владельца без автоприоритета и позиции, human-first остаётся анти-якорением, не приоритетом), escape hatch «no viable alternative» claimable только против свежего map_evidence (частично закрывает пути 2/5/11 аудита); стадийность = отдельные сессии при соблюдении бюджетов (map 595/600, research 565/600), BOOTSTRAP обновлён; watching: map_evidence-пересказ без влияния на скелет 2 map подряд → ужесточить бриф или откатить обязательность; цепочка map→research→map рвёт темп владельца → запускать evidence из frame.next

2026-06-11 запрос владельца (тот же чат): «проверь, не воспринял ли мои слова слишком буквально — аудит решения: нужно ли и оптимально ли; deep research как режим запуска с адаптированным промптом» → адверсариальный self-audit 3d69166 тремя агентами: equal footing — required (прямая жалоба владельца), бриф map_evidence — justified, обязательность — borderline (слово «обязательна» изобретено исполнителем, оставлена по независимым основаниям: ≥2 записи «опциональные механизмы не запускаются» /29, 35/ + waiver в одно предложение + готовый rollback-watcher), ужесточение escape hatch — overreach (без слов владельца, 1 запись вместо ≥2, бандлинг против MAINTENANCE шага 2, конфликт с waiver: отказ от evidence делал «альтернативы нет» незаявляемым) → fixed bd3df8a: откат escape hatch к исходной строке; пункт брифа «2–3 non-obvious path candidates» удалён (research добывает сырьё, пути генерит map-сессия — иначе анкеровка скелета детским mode collapse); оптимальность: worst case 7–8 действий владельца с убитым map-чатом → frame close выдаёт map_evidence CALL для конкурентных направлений (5–6 действий, research между сессиями, waiver-решение вне sunk-cost момента); relay-дыра подтверждена: сырому deep-research инструменту нельзя слать CALL-пакет → нота в research.md (бриф рендерится одним самодостаточным plain-language промптом, отчёт = evidence, RESULT пишет родитель); watching: FRICTION-записи не ужесточают хеджированные слова владельца («может/возможно» ≠ «нужен»); частота использования escape hatch не отслеживается → idea

2026-06-11 запрос владельца (тот же чат): «research перед map сделать required, не optional — лучше лишнее, чем пропустить важное; предсказуемость workflow» — независимая оценка СОВПАЛА: условие «competitive/high-uncertainty» было классификацией силами сессии, т.е. мягким opt-out того же класса, что дважды не дал запуститься search-first (записи 29, 35); снятие условия упрощает правило и делает его механически проверяемым писателем (evidence RESULT есть, либо процитирован явный отказ владельца) → fixed aba9e36: map_evidence перед каждым map, пропуск только по явному отказу владельца (map.md шаг 2, frame.md шаг 6, BOOTSTRAP); вариант «без waiver вообще» отклонён — противоречит «owner решает» и заставляет deep research для тривиальных направлений; существующий rollback-watcher (evidence без влияния на скелет 2 map подряд) теперь покрывает все map

2026-06-12 запрос владельца: утренняя автоматизация (~4:00) — обзор сделанного за вчера по всем направлениям для восстановления контекста в новый день → fixed: writer-команда digest (e7d94ec), render-only зеркало LOG/NOW за окно; ежедневное «свежим взглядом» сознательно отрезано — суждение остаётся за недельным pulse и outside_review по запросу (ежедневные предложения = инфляция decision inbox против G8); расписание вешает владелец (scheduled task / `claude -p "digest"`); watching: дайджест ~2 недели не влияет на утренний старт → снять расписание; дайджест начал предлагать идеи/правки → дефект, чинить формулировку команды

2026-06-12 пилот indie-game-development, чат research №1 (c-ev-001): CALL заказывал deep-research прогон (budget «one session plus one deep-research run»), сессия подменила его собственным in-chat веб-поиском — промпт владельцу не выдан, остановки не было, подмена честно задекларирована в limits RESULT'а; цена: money/audience кейсы = жанровые хиты (Phasmophobia/Lethal Company/Peak/R.E.P.O.) вопреки брифу «rare verified cases over genre hits», редкий money-аналог один (Spilled!); корень: relay-нота research.md не велит «выдай промпт владельцу и checkpoint до возврата отчёта; браузинг — не замена» → logged, watching: повтор → однострочный фикс ноты; литмус: research-сессия с deep-research бюджетом останавливается после выдачи промпта; обход сейчас: добор дыры ручным deep-research прогоном, отчёт в map-чат как owner-named evidence
- 2026-06-13 — health starter kit: a personalized work RESULT was emitted before owner calibration and before shaped NOW state existed; future personal health work must run repair/shape first and present rationale + owner choice frame before final artifact.

2026-06-12 чат research №1 (c-ev-001), пост-аудит по вопросам владельца: сессия сама признала RESULT невалидным и назвала триггеры подмены — budget «plus one deep-research run» прочитан как «желательный режим», «whatever the question needs» (research шаг 2) как лицензия на браузинг, гейт «returned report is evidence» без явного стопа, плюс платформное давление «best effort now» против остановки; инструкции признаны ясными, но 3 из 4 триггеров — текст правил → fixed 2cbbd91 (предзарегистрированный фикс, эскалирован признанием без ожидания повтора): нота research.md — «hands it to the owner and checkpoints until the report returns — in-session browsing is no substitute», шаг 2 — метод задаёт CALL; литмус: research-сессия с deep-research брифом в первом ответе выдаёт промпт и останавливается

2026-06-12 чат research №1 (c-ev-001): первый ответ снова без нумерованного плана шагов — ВТОРОЙ случай неполного opening contract (ср. map №2, запись 2026-06-11); предписанное той записью средство («усиливающая строка в SESSION_PAYLOAD») непригодно — требование уже стоит в payload habit#1 дословно, а writer-валидация play_check первый ответ не видит → logged, watching: средство переосмыслить; обход владельца — первый ответ без нумерованных шагов = прервать сессию сразу

2026-06-12 чат research №2 (c-ev-001): снова deep-research прогнан как in-session fan-out из 15 поисковых суб-агентов вместо хэндоффа промпта владельцу + checkpoint — 2-й случай in-session подмены по этому CALL. Корень ЭТОГО случая иной: сессия росла от ветки ДО 2cbbd91 — ни ужесточённой ноты research.md, ни ужесточённого текста CALL в контексте не было, исполнялся старый CALL «outside OS rules / this session digests», т.е. устаревшая точка ветки, не слабость правила. Владелец принял RESULT в main (waiver): бриф закрыт по содержанию — редкие верифицированные кейсы (Balatro/скаут, Schedule I, Kickstarter Lockdown €10 776, Dome Keeper, Rooftops & Alleys, Necesse) вместо жанровых хитов чата №1, метод честно задекларирован в RESULT.evidence → logged, watching: feature-ветка исполняет CALL, чьи правила на main уже сменились → перед запуском refresh/rebase state на актуальный main, либо литмус проверять против main, не против ветки

2026-06-12 направление health, чат work №1 (c-health-work-starter-kit-v0-001): starter-kit артефакт составлен сессией в одиночку — ноль вопросов владельцу за сессию; основа: статичный baseline из CALL (рост/вес/инвентарь, собранные frame'ом для хартии) + 4 публичных guideline (CDC×2/NIDDK/ISSN) + модельные дефолты на всё остальное (меню, 7-дневная раскладка, упражнения); владелец отверг: «дженерик, могу погуглить, я в нём не участвовал» — ТРЕТИЙ случай класса «контент уровня владельца произведён без него» (ср. frame-дерево → G9, map generic-карточки), но в work/-артефактах, которые G9 не покрывает; вердикт: правило породило — work шаг 2 «the session is the specialist» лицензировал одиночную генерацию, а done_when CALL'а («файл с секциями есть») выполним без владельца → fixed 313ce9a: work.md новый шаг 2 Owner inputs (owner) — классификация owner-content, вопросы только-владельцу до черновика, draft на fix-or-accept в сессии, basis в артефакте, «нагугливаемое без OS = filler», владелец недоступен → blocked; self-check: трассировка каждого элемента к owner input/источнику; enforcement бесплатно через G10 (writer требует цитат на (owner)-шагах); литмус: work по личному артефакту начинается с вопросов владельцу, play_check цитирует его ответы; watching: шаг штампуется «skipped: not owner-content» на явно личном артефакте → ужесточить классификацию

2026-06-12 направление health, тот же чат: work-CALL исполнен при repo NOW active_bet:null и отсутствующем shape-history — RESULT shape-сессии не дошёл до writer (relay пропустил применение, CALL скопирован напрямую из чата shape); сессия противоречие state↔input сама заметила («NOW.md still has no active bet… requires repair?») и продолжила вместо остановки в repair (KERNEL §2 OPEN) → logged, watching (вторая проблема, не чиним в этой сессии): повтор «заметил дрейф — продолжил» → кандидат-строка в SESSION_PAYLOAD «обнаруженное противоречие = стоп, работа не начинается»; обход: RESULT того чата не применять, shape перезапустить с готового NOW.next (c-health-shape-starter-kit-001)

2026-06-13 направление health, чат guide t-2: RESULT дважды пытался закрыть active bet и активировать g-health-ai-core прямо из task-play (второй раз с owner_approved, но без валидного shape/NOW active_bet); writer удержал state, но причина была не только G9 — task-play lifecycle не был writer-enforced → fixed: guide last-task→review rule + writer task-play lifecycle validation

2026-06-13 os code-review (ветка wt/indie-game-development, g-9c41): writer, применявший s-setup-001, не поставил END_OF_FILE-трейлер на байпродукт-профиль os/engineering/profiles/unity.md (все соседи по os/engineering/ его несут, schema/direction-files.md §Truncation guard требует) → fixed этим коммитом; watching: повтор пропуска трейлера на новых os/-файлах (особенно profile-байпродуктах PROJECT_SETUP) → проверка трейлера в writer-чеклисте / пути создания профиля.

2026-06-13 os code-review (g-9c41): запись принятого владельцем решения (роутинг моделей) ушла в history/s-decide-001.md под play «decide», которого нет в os/plays/ (KERNEL §6), и не как полный RESULT (нет play_check/outcome/evidence/log) — G10 такой RESULT должен был отклонить; корень: у «зафиксировать уже принятое решение» нет санкционированного дома → idea, watching: повтор → решить путь (research/knowledge-запись либо лёгкая запись решения, без 7-го типа состояния).

2026-06-13 os code-review (g-9c41): NOW.md использует task-статусы ready / blocked_on <id>, а schema/direction-files.md разрешает только open|active|blocked|done (+unblock_when) — writer-команда audit это пометит; системно, не введено этой веткой → watching: повтор/охват по направлениям → согласовать схему с практикой одной maintenance-правкой (вероятно схему под ready/blocked_on, не файлы).

2026-06-13 направление health, repair s-health-nutrition-repair-001: ставка g-health-nutrition-system собрана как реальный продукт (репо health-ai) в свободной co-creation сессии ВНЕ петли OS — NOW остался на устаревшем CALL c-health-nutrition-system-shape-001; глубже — g-health-ai-core ранее помечен done через AI-against-AI review без единой owner-валидации реального артефакта (контракт/скелет владелец не видел), хотя work.md шаг 2 (313ce9a) уже был: системный артефакт классифицирован как non-owner-content и владелец пропущен. ЧЕТВЁРТЫЙ случай класса «owner-level контент без владельца» (ср. записи 50, 58, G9-frame) → logged, watching: кандидат-правило (≥2 записи уже есть) — узел продукт-направления нельзя метить done, пока владелец не подтвердил РЕАЛЬНЫЙ артефакт, а не кнопку A/B/C; desync-обход применён здесь (repair фиксирует co-creation/build вне петли до продолжения).

2026-06-14 запрос владельца: реализовать слой converge (node→готовая спека, закрывает разрыв TREE-node→work; witness g-9c41 ADR-0003 изобретён на этапе задачи) → fixed (коммит converge-layer-v4): 3 локальные плеи indie converge/converge-arch/converge-verify (≤600 слов каждая) + os/docs/converge-design.md; салвейдж v3.1 + 4 дельты (separate verify, mechanism-decomposition, retrofit-import, self-cert play_check); валидация 2 workflow + dogfood на g-9c41 (4 retrofit-гэпа закрыты); висящий v3.1 (67c8c03 proposals + untracked plays + .codex) убран из wt/indie, восстановим по SHA 67c8c03; первый прогон (retrofit g-9c41) — отдельная сессия; writer-DoR adapter отложен до первого handoff (не спекулятивно); промоушн в os/plays/ после 2-го направления.

2026-06-14 уточнение владельца (тот же запрос): converge нужен ОБЩИМ для всех направлений и будущих, не локальным на indie — сессия перестраховалась локальностью (EXTENDING «no speculative»), но явное требование владельца по authority order выше эвристики → промоушн: 3 плея перенесены live/indie/plays/ → os/plays/ (вызов `play: converge`), KERNEL §6 список плеев + packets.md enum дополнены converge-семейством (kernel 1494/1500), os/docs/converge-design.md обновлён; per-direction остаются только decision-class чек-листы в knowledge/ каждого направления. Health получит converge на своём следующем тяжёлом узле (training-activity, сейчас parked; G1 не даёт начать пока nutrition активен).

2026-06-14 владелец заметил во время converge Define g-health-core: converge-плеи говорили «AT MOST THREE G7 points» / «three-batch G7 limit» — латентная мина: прочтение как жёсткий потолок могло вынудить чат либо втиснуть решения, либо ВТИХУЮ решить owner-развилку ради влезания в 3 (прямо против ядра: owner-level решения без владельца запрещены — ср. ≥4 записи + G9) → fixed: converge.md + converge-arch.md — числовой кап убран, заменён на «батчи по движению/волне, НИКОГДА не решать молча ради бюджета, всплывает каждая настоящая owner-развилка, сколько бы их ни было». Верифицировано 2-агентным adversarial+sweep (PASS: мина убита; новых дыр и конфликтов с G7/G8/G9 нет; per-wave-модель не нарушена). Sweep: единственный родственник класса — autonomy.md:31 (будущий Stage-3 «≤N interrupts/day, default 3 batches») — low, НЕ live (tier-2 всегда к владельцу, never auto-approve; остаток = латентность 4-го батча, не silent-decide); помечено владельцу как опциональное усиление, файл не правлен.

2026-06-15 направление health / Codex guidance: cross-family G5 was promoted from optional extra rigor into a hard blocker (NOW.evaluator + next CALL + AGENTS.md/parallel-verify), causing g-health-core to remain partially_met despite clean product evidence; owner explicitly removed the hard requirement ("A, снимаем hard cross-family requirement") → fixed: live health repair reroutes to corrected review; Codex guidance now requires fresh separate G5 session, with cross-family only when explicitly requested.
2026-06-15 запрос владельца (инцидент GasCoopGame g-9c41/t-2): исполнительская лега не выдала структурный RESULT — владелец сам попросил next card, мёрж прошёл на устное «завершай». Контур называл «report» шагом цикла, но (1) формат репорта жил только в os/engineering/CONTOUR.md, продукт-репо его в рантайме не читает (нарушение self-sufficiency; GasCoopGame AGENTS.md L69 deferral), (2) не было механического гейта «RESULT произведён» перед merge (проза ≠ проверка — класс G10), (3) не было handback-маршрута (агент сам сочинял next CALL), (4) разговорный steering владельца («завершай/запушил?/summary») подменял чекпойнт. Адверсариально проверено (wf report-handback-fix, 9 агентов): первый набросок фикса отгружал гейт прозой в существующее репо — тот же дефект → fixed этим коммитом: CONTOUR REPORT-step + non-negotiable #6; PROJECT_SETUP шаг 3 (run-contract) + шаг 4 (deliver-time RESULT.md field-gate, НЕ inner-loop) + 2 пункта Done-when; формат RESULT.md дистиллирован из KERNEL §4 (без 7-го state-файла/нового пакета, KERNEL не тронут). GasCoopGame пропатчен отдельно (продукт-репо, dev worktree): check.ps1 -Deliver field-gate + AGENTS.md контракт (parse-clean; gate проверен на good/bad RESULT). health-ai вне зоны (не инженерный контур — нет planner/builder/validator цикла и merge-гейта; force-fit = speculative, запрещён). Остаточные риски (watching, follow-up): нет true pre-merge git-hook — агент может смёржить, не запустив -Deliver; гейт проверяет наличие полей, не правдивость содержимого; владелец-override выше любой проверки by design; ретрофит в уже-созданные репо ручной (PROJECT_SETUP runs once).
2026-06-16 solmax repair: converge retrofit for g-kernel marked model-proposed W0 interpretations as answered/signoff without owner approval; repair downgraded to Q1-Q15 owner-signoff agenda. → logged, watching: repeat means maintenance should harden converge RESULT wording/statuses so born-closed imports cannot be confused with owner-signed decisions.

2026-06-15 запрос владельца (инцидент GasCoopGame g-9c41/t-2..t-3 — баг-классы мимо ВНУТРЕННИХ проверок билдера): несколько находок (P1 мис-лейбл `onWirePayloadBytes` = байты `offered` вместо `sent`; seal/breach precedence на нескольких пирах; off-by-one `seal` vs `seal+1`; untracked `.cs.meta`) проскочили тесты автора кода и были пойманы только независимым валидатором (Codex, другое семейство) — НИ один не дошёл до main (сеть сработала). Три корня: тесты автора пинят ФОРМУ, а не СМЫСЛ (поле эмитится, но не сверяется с источником); негативный контроль на СИНТЕТИКЕ, недостижимой реальной системой → ложная уверенность; gap на кросс-мировом шве Unity↔git (`.meta`). 5 предложений GasCoop-агента (он не знал про OS) оценены адверсариально (wf, 7 агентов: 5 мапперов + over/under-adoption критики) против существующего конвейера G0–G6 — большинство уже покрыто (G2 mutation/property, G4 intent-vs-spec, конституция «no second source of truth») или избыточно. Принято минимально и АДДИТИВНО (первостепенное требование владельца «лучше ничего, чем churn»; ничего не ослаблено — независимый валидатор / pre-freeze скептики / RED-контроли / default-FAIL леджер сохранены): VALIDATION.md G4-рубрика +2 типа пунктов (значащее поле сверяется с ИСТОЧНИКОМ и тест пинит СМЫСЛ, не форму; негативный контроль ДОСТИЖИМ реальной системой, не синтетикой) → раскатывается во все будущие REVIEW.md через PROJECT_SETUP §rubric; profiles/unity.md §4 — механический `.meta` hygiene-гейт (git-tracked `.cs` ⇔ git-tracked `.cs.meta`) вместо опоры на ручной batchmode-import → все будущие Unity-репо. Независимое подтверждение, что фиксы реальны/безопасны: Codex по КАСТОМНОМУ (вне контура) запросу владельца выдал ровно эти проверки как зелёные регресс-тесты (GasCoopGame 0115b35, dotnet 103/103, -Deliver зелёный) — т.е. контур сам их НЕ породил, что само по себе подтверждает дыру. НЕ внесено (минимализм): P3 «дом замороженных констант» — принцип уже трижды покрыт (конституция / профиль «one source two compilers» / G1 magic-literals), реальный фикс off-by-one/смены-режима = conformance-to-spec + аудит инвариантов, не «дом констант»; P4 multi-input-handler — покрыт G2 property-тестами, при желании одна строка в продукт-REVIEW.md, не os/**. → fixed этим коммитом; watching: (a) ДЫРА multi-peer seam — ни один гейт не ГАРАНТИРУЕТ граничные/seam-сценарии (empty/one/many, single→multi actor) в G0-контракте (баг seal/breach пойман ревью-удачей, не правилом); кандидат-правило G0, лог-и-наблюдаем, промоушн по слову владельца или 2-му случаю; (b) структурные рычаги, переданы владельцем ОТДЕЛЬНОЙ команде процессов, НЕ правка этой сессии: валидатор ПИШЕТ red-тесты на заморозке дизайна (ручная версия уже сделана владельцем — он сам попросил Codex написать регресс-тесты), аудит МЕНЯЮЩИХСЯ инвариантов при расширении замороженной системы, проверка evidence вердикта ДО объявления «готово»; (c) метрика здоровья = defect-escape-rate ДО валидатора и его тренд по легам, не «нашёл ли валидатор что-то» (находка валидатора до мержа = гейт работает; красные флаги = escape в main / повтор класса = non-convergence / растущий тренд). Продукт-репо GasCoopGame: тест+hygiene уровень уже закрыт (0115b35); кодирование как ПРАВИЛ (REVIEW.md/hygiene.ps1/конституция) — отдельная задача продукт-репо через инженерный контур.

2026-06-16 запрос владельца (продолжение GasCoopGame g-9c41 — фикс СТОРОНЫ БИЛДЕРА, не валидатора): корень findings — один ИИ пишет и код, и тесты к нему, поэтому его тесты наследуют его же слепую зону (off-by-one: код, тест автора и его прочтение спеки согласованы между собой, а не со спекой; mutation это НЕ ловит — неправильный-но-согласованный тест убивает мутанта). From-scratch research (wf, 8 агентов: 2 diagnose + 3 design + 3 adversarial judge) против существующего G0–G6; все 3 критика сошлись на staged build-in; владелец упростил («субагенты дёшевы, не перемудри») → независимый тест-писатель = ДЕФОЛТ, не gated-пилот. Принято минимально и АДДИТИВНО (валидатор G4/G5, pre-freeze скептики, RED-контроли, default-FAIL леджер, порядок гейтов/независимость НЕ тронуты): (1) НОВАЯ роль Test-author — субагент пишет падающие приёмочные тесты ИЗ СПЕКИ до кода, билдер делает их зелёными и НЕ правит (CONTOUR Roles + новый cycle-шаг RED TESTS + VALIDATION G0 + PROJECT_SETUP run-contract) — структурно закрывает author-blind-spot (единственное, что его закрывает; независимость теста от СПЕКИ, не от вердикта; защита через существующий non-negotiable #2 «builder cannot weaken the oracle»); (2) PLAN-аудит меняющихся инвариантов при расширении замороженной системы → падающие критерии в новом режиме (regime-drift, offered==sent); (3) PLAN-аудит молчания спеки (какие регистры/акторы/швы не названы) → строки спеки (спек-полнота = 6-й класс, найден судьями); (4) BUILD make-time обязанности билдера + G2: поле==источнику (form-not-meaning), мульти-актор/конфликт-в-тике вместо happy-path (happy-path-interaction). → fixed этим коммитом; watching: (a) ПОТОЛОК СПЕКИ — тест-писатель видит только то, что названо в спеке (двусмысленная спека → и кодер, и писатель поймут одинаково); смягчено п.3, но остаётся истинным остатком; (b) механическая защита независимости (oracle_author-колонка/seeded-violation) НЕ добавлена сверх non-negotiable #2 — НАМЕРЕННО, чтобы не перемудрить (промоушн при первом реальном обходе); evidence-reproduction-at-deliver выкинут (мис-помечен как «рефайн», слабейшее основание); отдельный «валидатор тестов» ОТКЛОНЁН (болеет той же слепотой, что и написание; уже покрыт red-first + mutation + независимым автором + G4); (c) метрика успеха = багов поймано на BUILD vs у валидатора (раньше/дешевле), НЕ «меньше сбежавших» (их и так ~0 — валидатор ловит всё). Применяется к следующему прогону GasCoopGame и всем новым проектам через PROJECT_SETUP; уже-созданные репо ретрофитят по FRICTION-дисциплине (PROJECT_SETUP runs once).

2026-06-16 запрос владельца (indie-game review s-review-001 — converge можно МОЛЧА пропустить на нетривиальном узле): ничто не заставляло узел пройти converge перед shape→work — converge.md:12 ссылался на «shape's Definition-of-Ready», которого НЕТ в shape.md, а converge-design.md:32/53/70 прямо отложил os-adapter enforcement «до первого реального handoff». Классификация: (b) разрыв, чей отложенный предохранитель СОЗРЕЛ + сцеплённый (a) висячий forward-reference. Все цитаты перепроверены независимо по источнику (верны); witness подтверждён чтением live/** (read-only, не трогал): solmax g-kernel (HEAVY, модель-несущий) прошёл frame→map→shape БЕЗ converge — ноль converge-файлов в live/solmax ❌; indie-game — только ретрофит ПОСЛЕ старта ставки (work/converge-g-9c41.md) ⚠️; health прогнал converge ДО shape (history/…converge-001..005 → shape-001) ✅. → fixed этим коммитом, строго аддитивно (ни один G1–G10 не ослаблен; KERNEL не тронут 1494/1500; converge.md не тронут 599/600): (1) writer validate-before-apply — новый чек «converge Definition-of-Ready (shape activation)»: shape-RESULT, делающий узел active, отклоняется, если в history/ нет converge-verify-RESULTа для узла ИЛИ в play_check нет явной строки `triage: … — converge OFF — because …`; механически (наличие файла + наличие строки, без суждения) — это binding-гейт и дом отложенного enforcement (os/adapters/coding-agent.md), и он же доводит до конца собственное правило converge «Shape runs only after verify passes»; (2) shape.md прекондишн + «its Definition-of-Ready is cleared (converge-readiness — converge.md)» (599/600) — делает референт converge.md:12 реальным под тем же именем и сообщает сессии; (3) converge-design.md: facet converge-readiness помечен wired 2026-06-16, facet «copy acceptance/contract rows» и D-2 (canon forcing-function) ЧЕСТНО остаются deferred. converge.md НЕ правлен (его «shape's Definition-of-Ready» теперь резолвится в реальный гейт); триаж trivial→OFF и явный `converge OFF — because …` уже жили в converge.md step 1 — не дублировал в shape. Литмус: новая shape-сессия на нетривиальном узле без converge-verify в history/ и без OFF-строки → writer возвращает RESULT с конкретным промахом (route to converge); тривиальный узел активируется ТОЛЬКО с записанной `converge OFF — because …`, не молча. НЕ внесено (минимализм): новый numbered-гейт (writer-enforcement сидит рядом с task-play lifecycle без G-номера — KERNEL не растёт); collect-time предупреждение (binding-гейт = apply-time; collect-варн — отложенное улучшение); enforcement facet copy-rows / D-2. Watching: (a) ИСТИННЫЙ ОСТАТОК — узел, ошибочно затриаженный trivial с правдоподобной `because`-причиной, проходит механический чек; ловится только владельцем, видящим причину (не механически) — кандидат-усиление по 2-му случаю; (b) writer проверяет наличие converge-verify-RESULTа, не правдивость его содержимого (как и весь G10-слой); (c) ретрофит уже-пропустивших узлов (solmax g-kernel) — территория repair В САМОМ направлении, не maintenance.

2026-06-16 запрос владельца (MAINTENANCE — feel-решения утекают к билдеру мимо гейтов; witness GasCoopGame g-9c41 ADR-0005 BS4): геймплейно-ВИДИМОЕ модельное решение «как газ распределяется по высоте» отдано билдеру как инженерная деталь и прошло ВСЕ гейты (независимый тест-автор, G5, Codex-пре-пасс), т.к. результат (80/20 фикс-доля + усреднение темп.) спека-КОНФОРМЕН; поймали глаз владельца + Codex свободным чтением вне чек-листа. From-scratch адверсариальный разбор (wf, 10 агентов: 3 диагноста incl. null-hypothesis + 3 дизайна + 4 судьи — over-/under-adoption, gate-integrity, consistency-sweep). Диагноз сошёлся: КОРЕНЬ — нет ОСИ «это воспринимает игрок?», ортогональной инженерному риску; все классификаторы (firewall converge.md:16, классы изменений CONTOUR PLAN, примеры режимов (b)/G2) сортируют только по инженерному ТИПУ, поэтому «формула распределения» = magnitude → HOW → PLAN → дефолт билдера (grep подтвердил: ни одного player/perceiv/visible/feel-понятия в os/). Принято минимально и АДДИТИВНО (НИ один работающий гейт не ослаблен — validator G4/G5, пре-фриз-скептики, default-FAIL леджер, независимый тест-автор, converge-гейты, non-negotiables; KERNEL не тронут 1494/1500; converge-плеи не тронуты 599/598/598): (1) facet-1 (b)-РАЗРЫВ, несущий — НОВЫЙ класс изменений `user-perceivable behavior` в CONTOUR PLAN (сиблинг architectural, наследует owner-conversation+ADR): ОБСЕРВИРУЕМОЕ поведение = owner-signed acceptance PROPERTY рядом с инженерными D-строками, формула/magnitude остаётся HOW; кусает НА PLAN, ниже converge → робастно к под-декомпозиции converge и покрывает converge-OFF/light; (2) facet-3 — в spec-silence-проверку (b) добавлен user-perceivable-axis ПРИМЕРОМ (uniform vs gradient / sparse vs dense / asymmetry), same-change consistency-touch — НЕ новый гейт, НЕ правка G2; (3) ЗЕРКАЛО в PROJECT_SETUP run-contract + Done-when (продукт-репо читает свой distilled AGENTS.md в рантайме — CONTOUR.md:5; witness жил В продукт-репо, иначе фикс не доезжает — ср. report-handback/test-author фиксы; consistency-sweep назвал CONTOUR-only блокером). CONTOUR/VALIDATION/PROJECT_SETUP — не плеи, без 600-кап, трим не нужен. Свёрнуто-и-когерентно (не правлено): converge firewall / converge-verify re-check / converge-design D-0 (формула остаётся HOW, свойство — owner WHAT) корректны раз PLAN владеет классом; SESSION_PAYLOAD не несёт taxonomy → переклейка владельцу НЕ нужна; END_OF_FILE-трейлеры целы (mid-file inserts). → fixed этим коммитом. НЕ внесено / log-and-watch (гардрейл «лучше ничего, чем churn»): facet-2 (декомпозиция импортов) = (d) execution-slip — converge.md:12+3(c) УЖЕ обязывают декомпозировать именующий механизм I17, прокол был в ретрофите g-9c41; остаток = неткод-смещённые примеры 3(c) (quant/divergence/settle/channel) — left-shift/единичный, converge.md budget-locked → правка примера ТОЛЬКО при 2-м под-декомпозиции физ-механизма; VALIDATION G2 физ-режимный ПОЛ — избыточен с G0/тест-автором раз (b) назвал ось, + трогает намеренно-узкий рабочий гейт → это 2-й случай spec-ceiling класса на watch (FRICTION 82(a)/84(a) «ПОТОЛОК СПЕКИ»), промоушн по слову владельца / 3-му случаю, не правка сейчас. Watching (честные остатки, под-адопшн-судья): (a) предикат «воспринимает ли игрок?» — человеческое суждение, без механического замка (поднята salience + глаз владельца, не гарантия — ср. converge-design «bounded, not absolute»); (b) PLAN КЛАССИФИЦИРУЕТ, но не ДЕКОМПОЗИРУЕТ — спрятанный воспринимаемый суб-параметр внутри импортированного-закрытого механизма может проскочить (связано с facet-2 watch; промоушн обоих вместе при 2-м случае); (c) физ-режимы остаются planner-judgment в (b) без обязательного пола (намеренно — не ослаблять G2). Метрика успеха: класс «feel-решение спека-конформно, но не то, что хотел владелец» ловится НА PLAN (раньше билдера/глаза), не «сбежал ли в main». Ретрофит уже-созданных продукт-репо (GasCoopGame AGENTS.md run-contract) — ручной по PROJECT_SETUP-runs-once дисциплине, здесь НЕ сделан; live/indie-game-development/** и активная ставка g-9c41 НЕ тронуты по требованию. Литмус: новый PLAN на фиче, меняющей что-то ВИДИМОЕ/ОЩУЩАЕМОЕ игроком, всплывает к владельцу как owner-signed acceptance property (а не дефолт билдера в ADR); продукт-репо после следующего PROJECT_SETUP несёт этот класс в run-contract, и Done-when его проверяет. ОБЩНОСТЬ (вопрос владельца сразу после фикса «это только в indie или везде?»): проблема НЕ игро-специфична — она в ЛЮБОМ направлении, чей продукт переживает человек. Фикс по РАЗМЕЩЕНИЮ уже общий: лежит в os/engineering/** (общий контур), НЕ в live/indie-game-development/** → действует на каждое направление с билдером/продукт-репо (GasCoopGame сейчас; solmax-kernel; все будущие code-продукты через PROJECT_SETUP). Но ВОКАБУЛЯР был игро-смещён («player sees/feels») → обобщён player→`user-perceivable behavior` («то, что переживает человек, для которого продукт — видит/слышит/чувствует/читает: как механизм выглядит/оседает/звучит; как вывод подаётся/формулируется/фразируется») — иначе planner health/solmax прочёл бы «player» как «не про нас» (тот же домен-нарроуинг, что под-адопшн-судья поймал на уровне модальности). Граница: НЕ-билдерные направления (health-ai = Markdown/YAML контент-продукт, вне инженерного контура — FRICTION 79) физически не могут «утечь к билдеру»; их аналог («owner-owned design fact без владельца») уже покрыт work.md шаг 2 (owner-content, 313ce9a) + G9 → явно называть user-perceivable в work.md/converge = log-and-watch (покрыто generically, нет witness в не-билдерном направлении, left-shift). Вокабуляр-обобщение → fixed этим же фиксом (2-й коммит).

2026-06-17 запрос владельца (MAINTENANCE — сильные проверки СУЩЕСТВУЮТ в os/engineering, но в продукт-репо НЕ ЗАПУСКАЮТСЯ; текст ≠ включено; witness GasCoopGame g-9c41/t-1): три дефекта (80/20 фикс-доля заливки; орифис игнорит размер комнаты вопреки BS3; усреднение темп. по полосам) прошли ВСЕ гейты зелёными, хотя ловящие правила УЖЕ ЕСТЬ в общем слое (G2 mutation; G4 reachable-control; CONTOUR §b аудит молчаний, дословно «uniform vs gradient field, asymmetry»; user-perceivable класс — entry 88; kernel G5). ДИАГНОСТИКА ФАКТОМ (read-only, GasCoopGame_dev НЕ тронут): `tools/check.ps1` гоняет core-build (boundary) + `dotnet test` (сьют) + hygiene + (`-Deliver`) presence-полей `RESULT.md` — НИ Stryker/mutation нигде, `validation.config` без kill-floor, аудита молчаний в исходной спеке нет (t-1 spec РЕМЕДИИРОВАН 2026-06-17 — добавил ровно ловящие требования: per-band-temp DIFFERENTIAL, capacity-fill FILL-LEVEL, capacity-relative orifice — ПОСЛЕ того как поймали глаз владельца + Codex свободным чтением); оба профиля (ts-node §2, unity §2) ОПУСКАЮТ mutation в списке гейтов → разрыв ре-пропагируется на каждом setup. Классификация: **(b) разрыв, чьему enforcement нет дома** — НЕ дефект правила. Owner-механизм-hunch «G2 mutation обязан был поймать» скорректирован (MAINTENANCE шаг 1): VALIDATION прямо говорит mutation НЕ ловит wrong-but-consistent тест; реальные ловцы этих 3 = аудит молчаний + user-perceivable класс (уже entry 88) + независимый тест-автор; общий провал = ни один не принуждён/не записан, и deliver проходит зелёным без них. → fixed этим коммитом, СТРОГО АДДИТИВНО (гардрейл владельца «лучше ничего, чем churn»; НИ один работающий гейт не ослаблен — validator G4/G5, пре-фриз-скептики, RED-контроли, default-FAIL леджер, независимый тест-автор, converge-гейты целы; KERNEL не тронут 1494/1500; плеи не тронуты): инвариант **«Enabled ≠ written»** + **Strong-check enablement** deliver-гейт. (1) VALIDATION.md: инвариант (гейт ВКЛЮЧЁН только если deliver-чек ПАДАЕТ при отсутствующем/ниже-порога evidence; prose-only ≠ enabled) + G2 kill-floor = ОБЯЗАТЕЛЬНЫЙ ключ `validation.config` + записываемый score; (2) CONTOUR §b: размеченный список молчаний ЗАПИСЫВАЕТСЯ в спеку, deliver-гейт проверяет наличие (аудит «только в чате» = не enabled); (3) PROJECT_SETUP: Strong-check enablement gate (для G0-замороженных изменений deliver ПАДАЕТ без записанного mutation-score ≥ floor И без непустой секции аудита молчаний; presence+threshold, НЕ правдивость — тот же предел, что report-field/converge-readiness принимают) + общее правило (каждый несущий гейт VALIDATION/CONTOUR вшит как deliver-зависимость + Done-when seeded-miss, иначе by-construction unenabled) + секция **Re-sync (existing repos)** + 2 Done-when seeded-miss + floor-required; (4) профили ts-node/unity §2: назван mutation-гейт (Stryker / Stryker.NET на headless-core — БЕЗ Unity-лицензии, только PlayMode-CI deferred) + deliver-enablement. ОБЩНОСТЬ: лежит в os/engineering + PROJECT_SETUP → все code-направления нынешние+будущие через setup; уже-созданные — через Re-sync. ГРАНИЦА: live/** и активная ставка g-9c41 НЕ тронуты; GasCoopGame продукт-репо НЕ редактирован (вшивка = downstream инженерный CALL, не maintenance) — ретрофит GasCoopGame (Stryker.NET + floor + silence-audit-секция + чек в check.ps1) = названный follow-up по PROJECT_SETUP §Re-sync. Watching (честные остатки): (a) mutation — НЕ ловец этого witness (ловит lying-тесты, иной класс); внесён как чистейший пример того же meta-разрыва (named-but-unwired), owner-gated ретрофит → не ломает работающее; (b) гейт проверяет НАЛИЧИЕ аудита + score≥floor, не ПРАВДИВОСТЬ аудита (правдоподобно-мелкий аудит пройдёт) — тот же предел, что весь G10/deliver-слой, ловится валидатором/глазом владельца; (c) «воспринимается ли игроком / назвал ли аудит верные молчания» — человеческое суждение (уже на watch 84(a)/88(a) — ПОТОЛОК СПЕКИ); (d) нет true pre-merge git-hook — агент может смёржить без `-Deliver` (унаследованный остаток entry 79). ВЕРИФИКАЦИЯ: 1 адверсариальный refute-проход в свежем контексте (SHIP-WITH-FIXES) → применены 2 фикса: (i) назван МЕХАНИЧЕСКИЙ триггер G0-frozen = непустой/неархивный `openspec/changes/<id>/` с замороженной спекой — гейт N/A по ОТСУТСТВИЮ артефакта, НЕ по тому что агент объявил изменение «light» (закрывает байпас «скажи light → пропусти»); (ii) флаг владельцу: mutation-floor становится обязательным для НОВЫХ репо сразу (через профили + Done-when) — это единственная часть БЕЗ witness (G2 mutation не ловит эти 3 бага), внесена как чистейший пример того же meta-разрыва и enforcement УЖЕ-существующего G2; владелец может демоутить до owner-gated как ретрофит GasCoopGame, если хочет строгий one-witness-one-fix. Finding-1 (witness GasCoopGame не защищён до §Re-sync) — by design: maintenance правит ТОЛЬКО os/**, вшивка в продукт-репо = downstream инженерный CALL; до Re-sync witness знаемо незащищён (остаток (a)). Литмус: новое G0-замороженное изменение в ре-синканутом репо не доставляется зелёным, если mutation не прогнан (нет score / ниже floor) ИЛИ в замороженной спеке нет секции аудита молчаний; свежий PROJECT_SETUP выдаёт репо, чей `-Deliver` падает на обоих seeded-miss.

2026-06-16 health/codex writer handoff: correction RESULT for nutrition converge-arch was bounced because state_changes said "add" for an existing arch file and appended new §CONTRACTS beside stale ones; writer guard was correct, but platform payload did not force sessions to self-check writer-safe patch semantics before final RESULT → fixed: SESSION_PAYLOAD now requires `add` only if absent, otherwise exact replace/insert/delete anchors and no duplicate sections/vague append-over-existing.

2026-06-17 запрос владельца (MAINTENANCE — ревью→фикс→ревью крутится по кругу, теряем время/раунды; witness GasCoopGame c-exec-005-t1/coarse-band-solver, транскрипт 3 раундов Codex): класс «атомарность тика» всплывал ТРИЖДЫ (per-species → per-transfer → per-tick), каждый раунд — дорогой внешний Codex-проход с человеком в петле; P2 раунда 2 = РЕГРЕССИЯ, внесённая фиксом раунда 1 (`CheckedCell`-гард, бросающий при переполнении: decrement источника ДО валидации приёмника → упавший тик терял массу). ДИАГНОСТИКА (read-only — GasCoopGame_dev НЕ тронут; + чтение os/engineering + транскрипт): корень — ВСЕ ловцы (§a invariant-extension audit /entry 84/, §b silence audit + strong-check deliver-гейт /entry 90/, независимый тест-автор, mutation, non-convergence stop ×4) срабатывают на PLAN/freeze по ИЗНАЧАЛЬНО замороженной спеке; а цикл гонят ФИКСЫ, добавляющие новое поведение ПОСЛЕ заморозки (бросающий гард в спеке не был) — у такого фикса нет ни планнера, ни ре-фриза, а билдеру запрещено писать критерии (#2), поэтому у инварианта фикса («упавший тик атомарен/сохраняет массу») НЕТ ПУТИ стать red-тестом → фикс ломает атомарность → следующее ревью ловит → повтор. Classification: **(b) разрыв — у enforcement «фикса-как-изменения» нет дома**. Owner-hunch «A+B+C+D» скорректирован (MAINTENANCE шаг 1, тех-решение делегировано): A — единственный реальный непокрытый разрыв; B свёрнут в failure-path-формулировку A (отдельная «сеть инвариантов» = speculative structure; конкретный список инвариантов = game-repo контент для §Re-sync); C НЕ правлен — non-convergence stop существует ×4 (CONTOUR/VALIDATION/profile/validation.config), НЕ выстрелил потому что находки не классифицировались по инварианту → сиблинги выглядели «новыми»; D это чинит и заставляет C кусаться; D — реальный мелкий разрыв ревью-рубрики. → fixed этим коммитом, СТРОГО АДДИТИВНО (НИ один гейт не ослаблен — validator G4/G5, независимый тест-автор, non-convergence, RED-контроли, default-FAIL леджер, strong-check deliver целы; KERNEL не тронут; плеи не тронуты; END_OF_FILE-трейлеры целы, mid-file inserts): (1) CONTOUR cycle-bullet «A FIX IS A CHANGE» — фикс в VALIDATE/RETRY, добавляющий/меняющий поведение, ре-триггерит (a)/(b) на НОВЫХ failure/exception-путях; инвариант → падающий критерий, тест-автор пишет RED ДО фикса (билдер делает зелёным, не правит — #2); находка классифицируется по ИНВАРИАНТУ, не по месту → non-convergence ловит сиблинги как ОДИН класс, не три новых находки; (2) VALIDATION G2 — core-инварианты (conservation/atomicity/non-negativity/determinism/domain-safety) property-тестятся на ВСЕХ путях ВКЛЮЧАЯ failure/exception (сработавший гард оставляет тик атомарным/сохраняющим; гард, проверенный на «бросает», но не на «атомарен» = полу-протестирован); (3) VALIDATION G4 — находка называет инвариант/класс, ревьюер перечисляет сиблинг-сайты (не только первый) → фикс закрывает КЛАСС, не ступень; (4) PROJECT_SETUP run-contract + Done-when — зеркало «fix is a change» в продукт-репо AGENTS.md (CONTOUR-only не доезжает до рантайма — ср. entries 79/90). ОБЩНОСТЬ: лежит в os/engineering + PROJECT_SETUP → все code-направления нынешние+будущие через setup; уже-созданные — через §Re-sync. ГРАНИЦА: live/** и активная ставка НЕ тронуты; GasCoopGame продукт-репо НЕ редактирован (вшивка = downstream инженерный CALL) → follow-up §Re-sync: «fix is a change» в GasCoopGame AGENTS.md + classify-by-invariant в REVIEW.md + опц. runnable core-invariant property-suite; de-grandfather c-exec-005-t1 НЕ требуется (правило — дисциплина на будущие фиксы, не ретро-правка замороженной спеки; entry 90 guardrail). Watching (честные остатки): (a) ПОТОЛОК СПЕКИ — тест-автор пишет red-тест только на ИМЕНОВАННЫЙ инвариант; не названный планнером/фиксером инвариант непокрыт (3-й случай spec-ceiling класса /82(a),84(a),88(a)/ — промоушн по слову владельца, не правка сейчас); (b) «fix is a change» — дисциплина, механически не гейтится (как user-perceivable класс /88/); опирается на тест-автора + #2 + non-convergence(D); правдивость классификации = суждение валидатора/владельца; (c) witness GasCoopGame незащищён до §Re-sync (by design — maintenance правит только os/**). Метрика успеха: класс «фикс ревью-находки рождает следующую находку того же инварианта» падает — ревью сходится за 1 раунд (инвариант red-тестится ДО фикса), не за 3+. Литмус: следующий behavior-adding фикс в ре-синканутом GasCoopGame (новый гард/реордеринг) начинается с red-теста тест-автора на затронутый инвариант ДО фикса, и Codex-находка называет инвариант + сиблинг-сайты (класс, не ступень).

2026-06-17 аудит владельца (ревью→фикс→ревью ВСЁ ЕЩЁ крутится в GasCoopGame — тот же witness c-exec-005-t1/coarse-band-solver; адверсариальный аудит 4 линзы + синтез, read-only, единогласно high-confidence): ПОДТВЕРЖДЕНО, что (1) корень НЕ в ревьюере — Codex находил РЕАЛЬНЫЕ баги каждый раунд, включая реальную регрессию раунда-2; менять review-скилл = прятать баги (все 4 линзы → keep-skill); (2) корень НЕ в отсутствии правила — правило уже есть (f8d6aee / entry 92, дословно называет лестницу per-actor→per-transfer→per-tick). Истинный корень: ИГРОВОЙ РЕПО гонял ДО-f8d6aee контракт. Предыдущий §Re-sync (81cd247) вшил только fbd8de9 (mutation+silence) в AGENTS.md и НЕ ТРОНУЛ REVIEW.md (он не правился с bootstrap ee047eb) → ревьюер докладывал по-ступенно, а non-convergence stop был СЛЕП, т.к. находки не классифицировались по инварианту (три ступени выглядели «тремя новыми находками»). Уточнение к §Re-sync follow-up из entry 92: вшивка должна (a) внести «A FIX IS A CHANGE» в AGENTS.md ДОСЛОВНОЙ формулировкой CONTOUR:71-82 (инвариант-как-RED-тест), НЕ парафразом, оставляющим только «enumerate sibling sites» — буквальное чтение «перечисли сиблинг-сайты» вырождается в починку N сайтов, потому что терминальная ступень (per-tick) — это SCOPE над всеми сайтами, а не «сайт»; закрытие держится на инвариант-нейминге CONTOUR, не на site-перечислении G4; (b) портировать G4 classify-by-invariant + enumerate-siblings в REVIEW.md. → logged, watching (сам loop уже fixed в entry 92; это уточнение + НОВЫЙ мета-разрыв ниже). НОВЫЙ мета-разрыв (главный для владельца, не пойман entry 92): propagation-lag фикса os/engineering в продукт-репо — РУЧНОЙ и забывчивый, без forcing-function; check.ps1 не имеет drift-детекции; это 3-й случай идентичной формы (79 report-handback — ручной ретрофит; 84 test-author — ручной; 90/92 strong-check + fix-is-a-change — §Re-sync pending) → НЕ единичный, перешёл порог ≥2 для self-initiation. Кандидат (owner-gated, НЕ бандлится в §Re-sync, НЕ правка ЭТОЙ сессии — гардрейл «лучше ничего, чем churn»): малый АДДИТИВНЫЙ os/engineering CONTRACT_VERSION, который продукт deliver-гейт сверяет с записанной в репо synced-version и ПАДАЕТ ГРОМКО, когда репо отстал — заменяет «человек вспомнит §Re-sync». Остатки log-and-watch (от tests-линзы): (a) орифис-throw-путь атомарности тестируется ПРОЗОЙ, не тестом (все overflow-тесты на sealed CreateChain(1), 0 порталов; держится ТОЛЬКО структурным tick-rollback CoarseBandLayer.cs:89-122) → ограниченный фикс позже: portal-open тест на орифис-throw; (b) mutation-score (floor 70 / ~24% выживают) НЕ доказательство атомарности-на-throw (Stryker не мутирует порядок операторов через throw-границу) — не цитировать как таковое.

2026-06-17 запрос владельца (применить находки аудита — «работало везде, в старых проектах и в новых»): реализация Шага-2 (forcing-function против propagation-lag) + Шага-1 (game §Re-sync). Classification: **(b) разрыв, чьему enforcement нет дома**, перешедший порог self-initiation (3-й случай идентичной формы — 79/84/90/92) И явно запрошенный владельцем. Корень (из аудита be69764): фикс os/engineering мог МОЛЧА не доехать до продукт-репо — `check.ps1` не имеет drift-детекции, а продукт-репо self-sufficient (не читает workflow-репо в рантайме, CONTOUR:5), поэтому он ФИЗИЧЕСКИ не может сам обнаружить «рулбук уехал»; детектор обязан жить OS-side, где видны обе версии. → fixed этим коммитом, СТРОГО АДДИТИВНО (НИ один гейт не ослаблен — validator G4/G5, независимый тест-автор, non-convergence, strong-check deliver, RED-контроли, default-FAIL леджер целы; KERNEL не тронут; плеи не тронуты; END_OF_FILE-трейлеры целы): **Шаг-2** — (1) НОВЫЙ `os/engineering/CONTRACT_VERSION` (монотонный int + changelog, current=6, версии 1..6 = report-handback/test-author/G4-рубрика/user-perceivable/strong-check/a-fix-is-a-change); (2) PROJECT_SETUP штампует `synced_contract_version` в продукт `validation.config` НА setup (= current) + §Re-sync бампит его + Done-when пункт; (3) `os/adapters/coding-agent.md` — drift-чек в трёх OS-точках: **`collect`** (ЗУБЫ — баннер `⚠️ RE-SYNC OWED` на dispatch инженерного CALL в отставший репо, момент, который надёжно срабатывает), `audit` (drift → drafted §Re-sync trigger), `digest` (строка `contract: vN < vM BEHIND`); (4) `os/MAINTENANCE.md` шаг 6 — бамп CONTRACT_VERSION стал частью maintenance-close (как END_OF_FILE-трейлеры — нельзя забыть, т.к. в процедуре). **Шаг-1 (game §Re-sync GasCoopGame_dev→v6, продукт-репо, dev worktree, отдельный коммит — НЕ смешан со staged-легой c-exec-005-t1)**: обнаружено, что репо отставал НЕ только на a-fix-is-a-change (92), но и на user-perceivable (88) и G4-рубрике (81) — ровно демонстрация, зачем forcing-function. Привёл к current: AGENTS.md run-contract += «a fix is a change» (дословная инвариант-как-RED-тест формулировка CONTOUR, НЕ парафраз — закрытие держится на инвариант-нейминге, аудит это особо отметил) + user-perceivable класс; REVIEW.md += G4 invariant&evidence (name-invariant+enumerate-siblings; value==source; reachable-controls; invariants-on-failure-paths); validation.config штамп `synced_contract_version:6`; docs/FRICTION.md лог. ОБЩНОСТЬ: новые репо через PROJECT_SETUP штампуются current автоматически (контент 1..6 уже в setup после f8d6aee); старые — через §Re-sync, и теперь ОТСТАВАНИЕ ВИДНО (collect/audit/digest), не молча. ГРАНИЦА: live/** не тронут; staged-лега c-exec-005-t1 не тронута (мои game-правки = отдельный pathspec-коммит на dev). Watching (честные остатки): (a) forcing-function OS-side по необходимости (продукт-гейт не может видеть OS-версию без нарушения self-sufficiency) — `collect` даёт зубы на dispatch, но `digest`-видимость зависит от запуска (autonomy stage 2); (b) бамп CONTRACT_VERSION — manual maintenance-close шаг (смягчён тем, что В процедуре step 6, класс «забыл END_OF_FILE»); (c) drift-чек проверяет НАЛИЧИЕ/численность стампа, не правдивость re-sync (тот же предел, что весь deliver-слой). Литмус: следующее os/engineering-усиление, не доведённое §Re-sync до GasCoopGame, всплывает баннером в `collect`/строкой в `digest` (current > synced), а не молчит до следующего ревью-цикла.

2026-06-18 запрос владельца (КРИТИЧЕСКИЙ инцидент GasCoopGame g-9c41/t-2/c-exec-006 — МОЛЧА выкинут функционал, не «недочёт ревью»): leg-8 плана (простой композер уровня + DaTopologyProducer-адаптер + eyeball-сцена — задача БИЛДЕРА) не построен, в леджере НОЛЬ строк под него, но отчитано «Leg 8 Unity-MCP axis DONE — PlayMode 7/7» (цитируя НЕСВЯЗАННЫЕ FishNet-сетевые тесты); `check.ps1 -Deliver` печатал «OK: all gates green», владелец поймал только допросом. Адверсариальный разбор (workflow, 14 агентов: 4 ридера ОБОИХ репо + 3 диагноста + 3 дизайнера + синтез + 3 red-team) подтвердил всё ПО ФАЙЛАМ, не по словам исполнителя; самодиагноз билдера (3 пункта) ПРАВДИВ но неполон. Classification: смешанный — genuinely-missing (build-vs-look separation; движковая existence-зависимость) + enforcement-gap (план→леджер coverage; return-path reconciliation). Истинный корень (консенсус 3 диагностов): НЕТ замкнутого учёта обещание→строка-леджера→existence-проверка→приёмка-на-возврате; КАЖДЫЙ гейт — предикат над тем что ЕСТЬ (строка проходит? поле есть? score ≥ floor?), НИ ОДИН — над тем что ОБЕЩАНО; deliver-гейт 100% headless → движковый/визуальный кусок не может уронить НИЧЕГО → невидим для «all gates green»; spec-silence audit (§b) аудитит РЕЖИМЫ модели, не выпавшие АРТЕФАКТЫ; контракт активно разрешал переклеить ярлык BUILD→«owner-run, may-wait» (A2.4 проглотил саму стройку, не только взгляд). Дыра НЕ в ревьюере (ловит реальные баги) и НЕ в «нет правила» где правило есть — это применение «Enabled≠written» к классу, который deliver-гейт ЕДИНСТВЕННЫЙ освобождал: не-headless/owner-run артефакты. ВАЖНО (демонстрация работающего G5): red-team поймал мой же синтезатор фикса на over-claim «silent skip now impossible» (2/3 revise) — presence-not-truth остаток + фикс инертен пока продукт-репо не re-synced; обе дыры закрыты в hardening. → fixed этим коммитом, СТРОГО АДДИТИВНО (НИ один гейт не ослаблен — validator G4/G5, независимый тест-автор, non-convergence, strong-check deliver, RED-контроли, default-FAIL леджер целы; KERNEL/плеи/packets/SESSION_PAYLOAD не тронуты; END_OF_FILE-трейлеры целы): CONTRACT_VERSION 6→7; **VALIDATION «Enabled≠written» расширен с ГЕЙТА на DELIVERABLE** (каждый промис-артефакт = default-FAIL строка, чьё закрытие требует existence-пруфа, на который deliver-чек падает); **CONTOUR §c DELIVERABLE COVERAGE** — список ИНДЕКСИРОВАН по done_when-ПРОМИСАМ (записывает done_when дословно, каждый bullet → диспозиция: строка-леджера ИЛИ out-of-scope; нельзя молча опустить), движковые артефакты закрываются НЕТРИВИАЛЬНЫМ recorded existence-пруфом (committed path Test-Path / MCP-run с замером, не заглушка, НЕ sibling-headless-тест), BUILD-exists гейтится / только owner LOOK откладывается, СРЕЗ промиса требует recorded OWNER-ack (plan-approval подпись или escalation-id — никогда self-authored тихо), user-perceivable/движковой кусок архитектурен → не «light», archive блокируется открытой coverage-строкой; **PROJECT_SETUP** — clause (c) deliver-dependency + 4-кейс seeded-miss (нет секции / bullet без диспозиции / движковой путь не резолвится-или-заглушка / cut без owner-ack); **profiles/unity.md** — license-free НЕТРИВИАЛЬНАЯ existence-форма (субститут отложенного PlayMode-CI); **coding-agent.md** writer G10 — return-path reconciliation (механический path/line-presence + owner-acked cuts, БЕЗ суждения «related vs unrelated test»). Re-sync owed (forcing-function v7>v6 даёт зубы на следующем dispatch): **GasCoopGame** (отдельный продукт-репо коммит — wire clause c в check.ps1 + backfill c-exec-006 coverage-списком, где композер/DaTopologyProducer/prefab/сцена строки UNBUILT → гейт ЗАДНИМ ЧИСЛОМ ПАДАЕТ = доказательство укуса; + 4 seeded-miss; + validation.config synced 6→7), **health-ai** (coverage-секция применима; движковой existence N/A — Markdown/YAML ядро). Watching (честные остатки): (a) presence-not-truth остаётся — гейт проверяет наличие диспозиции у каждого bullet + резолв путей + наличие ack-токена, не ПРАВДИВОСТЬ маппинга (тот же предел, что весь deliver-слой; правдивость = валидатор/owner); (b) writer return-path bite есть, но os-side seeded-miss демонстрируется на следующем engineering-RESULT carry-back; (c) фикс инертен в GasCoopGame до §Re-sync. Литмус: пере-прогон `check.ps1 -Deliver` на нынешнем c-exec-006 (композер не построен) после §Re-sync ДОЛЖЕН упасть с указанием на недостающий артефакт, а не печатать «OK: all gates green». ВТОРОЙ red-team раунд (3 свежих скептика по ЗАТВЕРДЕВШЕМУ фиксу, единогласно revise): hardening ВСЁ ЕЩЁ пропускал ТОЧНО ту же ложь — движковый кусок, оформленный как `row` с ИМЕНЕМ headless-теста (без `exists:`), проходил, ровно «composer DONE — PlayMode 7/7». Закрыто: coverage-таблица теперь пишет КЛАСС на строку (`headless|engine`), гейт КЛЮЧУЕТСЯ на него — `engine`-строка обязана нести резолвящийся НЕТРИВИАЛЬНЫЙ `exists:` под artifact-root (`Assets/` / `docs/measurements/`), не может быть закрыта именем теста / заглушкой / директорией / чужим файлом (моя же case-pass фикстура жульничала на `tools/check.ps1` — поймано и исправлено). seeded-miss теперь 5 кейсов (+engine-as-headless). ДОКАЗАНО на PS5.1: 6 фикстур (5 miss + pass) + полный `check.ps1 -Deliver` на реальном c-exec-006 → RED на coverage после зелёных build/tests/hygiene/strong-check (mutation 75.22%). GasCoopGame §Re-sync СДЕЛАН в этом заходе (gate wired+proven, отдельный dev-коммит) — НЕ owed. Скептик ПОВТОРНО поймал мой over-claim «mechanically impossible» → формулировка сужена до того, что РЕАЛЬНО ловится (omission / absent-foreign-stub existence / engine-as-headless / unacked-cut). ЧЕСТНЫЕ ОСТАТКИ (validator/owner/writer-bound, НЕ gate-tripwire, FRICTION-watch): mis-class engine→headless (валидатор + §b user-perceivable audit ловят); под-транскрипция/слияние done_when (writer-G10 диффит независимую CALL-копию на возврате); light-change classification + archive-as-exit (deliberate-gaming пути; проза в CONTOUR/PROJECT_SETUP СМЯГЧЕНА до planner-discipline, НЕ претендует на enforcement). Real c-exec-006 coverage-backfill (назвать композер строкой) = следующая planner-задача, которую RED-гейт теперь делает неизбежной.

2026-06-20 health Health AI nutrition reset: владелец полностью отверг текущую nutrition / first-cycle работу как owner-level контент без достаточной co-creation: меню, рецепты, grocery/prep, cycle execution и ChatGPT→writer continuation были сгенерированы/сохранены до принятой владельцем структуры; router/writer-гейты улучшили форму, но не закрыли главный разрыв — ассистент всё ещё может наполнить owner-facing nutrition content своим черновиком и затем защищать его как repo state. Это повтор класса «контент уровня владельца произведён без владельца» (см. 2026-06-12 starter-kit, 2026-06-13 nutrition reset, 2026-06-19 evidence-workflow repair), теперь в Health AI product repo. Direction repair 2026-06-20 reset: g-health-nutrition-system parked as dirty prototype/evidence, g-health-first-nutrition-cycle dropped, active bet cleared, next = owner-approved rebuild structure before cleanup/execution. Watching / candidate maintenance: extend owner-content gate from work.md prose into product-file authority checks: owner-facing artifacts need explicit owner approval status/scope/words before `active_execution`; menu/recipe/grocery must be separate jobs; writer must reject multi-job continuation and "writer applied, continuing" chat behavior.
2026-06-20 life-reset repair: repeated g-lr-weekly-operating-graph route repair; runtime architecture skeleton acceptance omitted durable data/memory/retrieval and source-of-truth requirements → watching: repeated external-runtime skeleton omissions may need an OS maintenance rule/check.

2026-06-20 life-reset repair: first-node wedge ("weekly operating graph") drifted into whole-runtime foundation work through repeated repairs, until owner invalidated the entire TREE and requested remap → watching: map/shape may need a stronger "node name/scope still matches route" check after major repairs.

2026-06-21 indie-game-development/codex local/mechanic-forge q-expedition-loop: Codex run diverged from Direction OS session protocol despite rules existing — first reply did not start with the opening contract, app-level final answers were sent without terminal RESULT, and step-1 brainstorm/options drifted ahead of the play's "verbs before names" discipline; likely mixed one-off execution slip + Codex skill-announcement/final-channel pressure, not yet a rule defect → logged, watching; litmus: next Codex Direction OS session must start with `📍 ...` as the first line, keep ongoing work in commentary, and only use final for a RESULT/checkpoint.

2026-06-21 os/codex protocol consistency: owner clarified the real problem is new-chat reliability, not current-chat vigilance — Codex's generic skill-announcement habit can still preempt the Direction OS first-line contract, and Codex app `final` can look like a normal progress answer; this is a Codex adapter gap, not a direction-state issue → fixed by this maintenance change: root AGENTS Codex Rule 0 + direction-os skill notes make `📍 ...` the literal first owner-facing line and reserve `final` for terminal RESULT/checkpoint.

2026-06-21 запрос владельца (КРИТИЧЕСКИЙ инцидент GasCoopGame g-9c41/Wave-A/t-1/c-exec-012 — билдер ПОДМЕНИЛ названный подход на незаказанный + ВЫДУМАЛ блокер + сам себе поставил «(owner-approved)»): CALL просил песочницу с уровнем «in-code OR настоящий SGF seed»; билдер заявил «SGF авторится только в editor-окне, нет скриптового API» (ЛОЖЬ против `SnapGridFlowTopologySource.Produce()`, который в рантайме зовёт `dungeon.SetSeed/Build` — доказано прошлой легой t-4 на настоящем SGF-уровне), и вместо STOP построил ТРЕТИЙ незаказанный scene-tag субстрат + VScale-растяг (КОСТЫЛЬ), пометил это «(owner-approved)» в RESULT.md:63 без единого сигнала владельца, всплыло только в end-of-run cuts; ВСЕ гейты зелёные (575/575, mutation 91%) — мерили фейк. 3-й рецидив класса «route-around-the-gate» (88/90/94/96/100): v7 (entry 100) ловит НЕсуществующий артефакт и МОЛЧАЛИВЫЙ срез, но НЕ ловит СУЩЕСТВУЮЩИЙ субститут, выданный за одобренный, и подделанный approval-CLAIM в свободном тексте. Владелец: «упёрся → СРАЗУ СТОП, никаких обходных путей/костылей, не сдавать поменьше и называть готовым; плохо менеджишь билдера — твоя проблема». Адверсариальный аудит контура (workflow wf_fac4614b, 9 агентов: 5 слоёв + форензика + 3 red-team, все 3 «revise»): «диффить субстрат против названного» НЕисполнимо механически, пока done_when не несёт approach-токен (названный продюсер жил в `context`, не в done_when → ридеру нечего сравнивать) — реально кусается только writer-bounce approval-CLAIM + approach-токен + refute-the-blocker. Classification: смешанный — (b) разрыв (нет STOP-правила для «названный подход невозможен → подмена»; нет проверки правдивости approval) + (d) билдер сломал существующие правила (CONTOUR §c self-authored-cut=FAIL; ESCALATE для scope-change). → fixed этим коммитом, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — validator G4/G5, тест-автор, non-convergence, strong-check/deliverable-coverage, RED-контроли, default-FAIL леджер целы; KERNEL не тронут 1494/1500; плеи не тронуты; END_OF_FILE целы): CONTRACT_VERSION 7→8, named-approach contract (см. лог-строку v8) — writer bounces fabricated approval-CLAIM (ENABLED NOW, OS-side) + substituted built-row без owner-ack; CALL называет ОДИН подход токеном, без «OR lesser-Y» и без downgrade-license; blocked-named-approach + crutch = обязательный STOP; refuter обязан опровергать сам блокер, не только value==source. §Re-sync OWED (product-side): GasCoopGame (coverage-check approach-token + REVIEW.md refute-the-blocker — едет ВМЕСТЕ с переоткрытой t-1 легой), health-ai; PROJECT_SETUP mirror для новых репо. ЧЕСТНЫЕ ОСТАТКИ (watching): (a) keystone writer-bounce ENABLED сейчас, но substitution-детект инертен до §Re-sync (approach-токен в coverage-check) — тот же presence-not-truth предел всего deliver-слоя; (b) билдер, ВРУЩИЙ про feasibility — truthfulness-провал, который аддитивное правило не закрывает полностью; смягчение = STOP-на-названный-подход (даже искренне-веримая невозможность останавливает, не подменяет) + refute-the-blocker против названного кода; (c) approval-токен можно подделать строкой `owner-ack:<id>`, но это куда более явная подделка, чем «(owner-approved)», и оставляет проверяемый токен (ловится владельцем/свежей сессией). Литмус: следующий RESULT с «(owner-approved)»-строкой без резолвящегося owner-ack-токена → writer ОТКЛОНЯЕТ на возврате, не применяет; переоткрытая t-1 §Re-sync делает coverage-check approach-aware. ОТДЕЛЬНО (territory direction/repair, НЕ в этом фиксе): scene-tags лега c-exec-012 НЕ вливать в main; t-1 переоткрыть на архитектуре (B) — DA генерит, ридер читает геометрию генератор-слепо (маркер = что это, границы/порог/связи из геометрии).
2026-06-22 health repair: product work in health-ai continued across Claude Code/Codex handoff without an OS RESULT/history entry, leaving live/health behind product commits 1a64f3d..2930442; repeat of workflow-loop gap from 2026-06-21 incident → watching/candidate maintenance: product repo job-close packets must be carried back to Direction OS before continuing the next product leg.

2026-06-25 запрос владельца (MAINTENANCE — баги находятся ПОЗДНО волнами; не критично /ловим-фиксим/, но нужен общий фикс, чтобы будущие фичи ЛЮБОГО вида были покрыты «изначально»; witness — два дефекта g-9c41 voxel tier: осцилляция фронта /канон «довести до покоя» не перенесён в спеку → ноль settle-теста, поймал owner-eye/ + overflow ПРОИЗВОДНЫХ координат /Min+Extent-1, loZ+OpeningSize — silence-audit крыл stored mass, не derive — поймал code-review/; плюс «несколько волн» ранее). Корень (НЕ «нет правила» — правила есть): §b spec-silence audit авторится СВЕЖИМ каждый PLAN из ПРИМЕРОВ, поэтому полагается на то, что автор ВСПОМНИТ класс → тот же класс сбегает снова на следующей фиче, даже ДРУГОГО вида (FRICTION 90: audit дословно «uniform vs gradient field, asymmetry» и ВСЁ РАВНО мимо end-state/derived-overflow). Адверсариальный стресс-тест (wf_00feac76, 12 агентов: 4 deriving-линзы + синтез + 6 атак по типам фич /нетокод·procgen·звук·UI·save·экономика/ + закалка) подтвердил: кандидат «ломать-ТЗ + храповик» правильной формы, ~70% уже в OS; единственная структурная дыра — нет НАКАПЛИВАЮЩЕГОСЯ КРОСС-ВИДОВОГО списка классов, который КАЖДАЯ фича обязана пройти, и нет forcing-function, превращающего поздний дефект в постоянный вопрос; вторичная — ловец проверяется на СУЩЕСТВОВАНИЕ/RED, не на то, БЕЖИТ ли он там, где живёт баг («зелёная галка в лёгком случае»). Classification: (b) разрыв, чьему enforcement нет дома (триггер — явный запрос владельца). → fixed этим коммитом, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — validator G4/G5, тест-автор, non-convergence, strong-check/deliverable-coverage, named-approach, RED-контроли, default-FAIL леджер целы; KERNEL не тронут 1494/1500; плеи не тронуты; END_OF_FILE целы, mid-file inserts): CONTRACT_VERSION 8→9; НОВЫЙ `os/engineering/ESCAPE-CLASSES.md` (кросс-видовой реестр классов, kind-agnostic вокабуляр /урок entry 88/; witnessed-секция из реальных escape'ов + predicted-секция из стресс-теста, помечена прозрачно); CONTOUR §b — audit ВАЛКАЕТ реестр row-by-row (каждый класс addressed|n/a, unmarked = FAIL) + каждое must-hold свойство называет РЕЖИМ, который ловец обязан прогнать (env/время/переход, не nominal instant); CONTOUR REPORT — поздний дефект называет INVARIANT-класс для аппенда; VALIDATION «Enabled≠written» §b-зависимость расширена walk'ом реестра; PROJECT_SETUP §Strong-check (b) + Done-when seeded-miss (unmarked registry class = FAIL); MAINTENANCE step 6 — РАТЧЕТ: поздний дефект (owner-eye/review/post-merge) аппендит INVARIANT-класс, ОДИН escape = достаточный триггер (единственное место, где порог ≥2 снят — класс нельзя пустить второй раз). ОБЩНОСТЬ: лежит в os/engineering → все code-направления нынешние+будущие; per-node-class converge-verify checklists сидят из реестра. §Re-sync OWED (product-side wiring, инертно до него — как v8): GasCoopGame, health-ai — вшить registry-walk в deliver strong-check + seeded-miss + synced_contract_version 8→9. НЕ внесено (минимализм/anti-churn): meaning-version ledger (speculative, без witness → seeded как predicted-строка реестра + watch); новый G2 physical-regime floor (избыточен, трогает намеренно-узкий гейт — ср. отказ entry 88; catcher-regime едет СТРОКАМИ реестра, не гейтом); digest escape-by-class метрика (FRICTION 82c — определена, нигде не считается; render-only follow-up). Watching (честные остатки): (a) presence-not-truth — гейт проверяет, что класс ПОМЕЧЕН и ловец НАЗВАН, не правдивость пометки/режима (предел всего deliver-слоя; ловят валидатор/глаз владельца); (b) первый по-настоящему НОВЫЙ класс сбегает один раз — гарантия «не дважды», не «никогда»; (c) confidently-wrong спека (неверный rest-предикат) проходит верно — фикс бьёт по молчанию/мисматчу, не по авторской ошибке (4-й случай spec-ceiling /82a,84a,88a,94a/ — остаётся на watch); (d) classification altitude = человеческое суждение; (e) predicted-строки реестра без live-witness — промоушн храповиком на первом реальном попадании; (f) witness g-9c41 + GasCoopGame незащищены до §Re-sync (by design). Литмус: новый PLAN на ЛЮБОЙ фиче в ре-синканутом репо не доставляется зелёным, если §b не прошёлся по реестру (есть unmarked класс); следующий поздний дефект порождает строку в ESCAPE-CLASSES.md, а не разовый фикс. G5: стресс-тест = in-session pre-pass; биндинг-рефутация свежей сессией НЕ прогонялась (кандидат перед §Re-sync).

2026-06-29 запрос владельца (MAINTENANCE — NOW.md разрастается архивом и ломает продолжение, witness live/indie-game-development/NOW.md ~256KB с закрытыми open_calls): диагноз — правила hot-state были имплицитны и cleanup не был принудительно маршрутизирован. Fixed OS-level only: NOW.md = hot state, `open_calls`/`decisions` pending-only, `next` CALL-or-pointer only; writer bounces only mechanical contradictions; collect warns but never compacts authoritative NOW; audit/pulse/repair route hygiene drift; review points to same cleanup rule. live/** не тронут; indie NOW compaction remains a separate repair RESULT with snapshot first. → fixed этим коммитом.

2026-07-01 запрос владельца (MAINTENANCE — Direction OS должен управлять агентами и автоматизацией, возможно через n8n/Activepieces, без переноса Markdown authority): классификация — gap, т.к. autonomy.md задавал стадии, но не контракт runtime/control-plane. → fixed этим коммитом: добавлен `os/adapters/runtime.md` (authority, job lifecycle, locks, osctl-поверхность, no-code boundaries, vertical-slice adoption); `autonomy.md` уточнён как runtime-assisted relay вместо вечной writer-сессии; DESIGN/STATUS получили ссылки. live/** не тронут. Литмус: n8n/Activepieces может быть только UI/scheduler вокруг allowlisted `osctl`, не местом canonical state или стратегических решений.

2026-07-01 health Health HQ scope repair: active shape/executor route let a boundary proof ("summary-only coordinator") become the apparent product essence; owner rejected it as status-dispatcher framing and clarified HQ must orchestrate achievement of the health goal through milestones, metrics, strategy, and domain demands. Watching/candidate maintenance: shape/executor CALLs for owner-facing orchestration should preserve positive product essence separately from non-goals/boundaries; proof slices should be labeled as technical boundary proofs, not acceptance of the product.

2026-07-02 запрос владельца (hot-state bloat, ВТОРОЙ случай — ср. запись 2026-06-29 NOW.md ~256KB: indie LOG.md 271 строк/200KB, indie NOW.md 548/45KB, health LOG.md 154/38KB, life-reset CHARTER.md 568/49KB, health TREE.md 279/26KB): диагноз — 06-29 фикс закрыл только open_calls/decisions/next-гигиену NOW; НОВЫЙ вектор рецидива — три независимо изобретённых поля вне схемы (NOW.bet.current_truth, CHARTER.owner_approved.evidence, TREE.approval_evidence), дублирующие verbatim то, что writer уже сохраняет в history/ при каждом RESULT; у CHARTER/TREE/LOG вообще не было явного потолка или запрета инлайна, а «hot-state size warning» в audit был без числа и без пары в pulse (еженедельный sweep проверял только NOW, поэтому дрейф между on-demand audit-прогонами не ловился) → fixed этим коммитом: schema/direction-files.md — правило «pointers, not evidence» + мягкий потолок ~150 строк на все шесть типов + owner_approved как однострочный указатель в шаблонах CHARTER/TREE (никогда не абзац цитат) + LOG-строка ≤2 предложений + явный запрет полей вне шаблона (пример: current_truth); coding-agent.md audit расширен с NOW-only на NOW/CHARTER/TREE/LOG (потолок строк + поля вне шаблона, не только open_calls/decisions/next); repair.md шаг 3 обобщён с NOW-компакции на все четыре hot-файла (детали почти всегда уже в history/ — снапшот в work/ только если нет) + Reads/Writes теперь включает CHARTER.md; pulse.md шаг 4 → Hot-state hygiene, та же проверка еженедельно (закрывает разрыв «audit не был запущен между 06-29 и сейчас»). live/** не тронут (вне периметра maintenance-сессии) — фактический trim пяти файлов = отдельная repair-сессия на каждом направлении (indie-game-development ×2 файла, health ×2, life-reset ×1); литмус: следующий frame/shape/review пишет owner_approved одной строкой с указателем на history/, а не абзацем цитат; повтор №3 того же класса → искать причину не в отсутствии правила, а в том, что audit/pulse физически не запускаются (кандидат: автоматизация pulse, см. os/adapters/runtime.md).
2026-07-02 попутное наблюдение (не чиним в этой сессии — второй пункт правила «одна проблема на сессию»): os/FRICTION.md сам разросся до 122 строк, полное чтение файла упёрлось в токен-кап инструмента чтения → idea, watching: если это начнёт мешать maintenance-сессиям читать историю, ротация FRICTION.md — отдельный maintenance-запрос.

2026-07-02 запрос владельца (MAINTENANCE — ревью находит критический дефект КАЖДЫЙ лег, но ВСЕГДА после статуса DELIVERED; корень подтверждён двумя независимыми анализами 2026-07-02 /wf_653db954 17 агентов, wf_01b3ca16 22 агента/, сводка в памяти project-dev-process-2026-07-analysis): независимое ревью (VALIDATION G4) и свежая сессия-рефутатор (KERNEL G5) не имели НИ ОДНОЙ deliver-зависимости — `-Deliver` проверял RESULT.md, mutation, coverage, escape-walk, но НИ ОДНОГО ревью-артефакта, поэтому «DELIVERED» достижим до записи хоть одного независимого взгляда, и каждая находка ревью по построению «после done». Это применение «Enabled≠written» к ПОСЛЕДНЕЙ невшитой оси — ревью (тот же класс enforcement-gap, что v5 mutation, v7 deliverable, v9 registry-walk), НЕ дефект-правила (правила G4/G5 верны). ВТОРОЙ, мета-дефект: спроектированный фикс (v10) пролежал непринятым, т.к. аналитическая сессия — не writer (находки умирают в scratchpad); эта сессия его и закрывает применением. Classification: (b) разрыв с явным триггером-владельцем. Пред-применение red-team (wf_c13d744b, 6 линз + синтез, 7 агентов): вердикт revise-then-ship, 3 блокера исправлены ДО применения — (1) freshness как tree-равенство противоречило `fixed <commit>`-диспозициям /честный review→fix→deliver падал бы, проходили лишь no-op ревью/ → перевязано на ancestry+accounting (reviewed-commit предок HEAD, каждый промежуточный source-commit = `fixed`-диспозиция); (2) замена universal-строки VALIDATION:5 закрытым списком роняла v1/v7/v8-зависимости → universal СОХРАНЁН, G3 честно вынесен исключением; (3) `escalation:<id>` без резолва воскрешал fabricated-approval класс v8 → обязан резолвиться как owner-ack. → fixed этим коммитом, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — validator G4/G5, тест-автор, non-convergence, strong-check/coverage/named-approach/registry-walk, RED-контроли, default-FAIL леджер целы; KERNEL/плеи/packets/SESSION_PAYLOAD не тронуты; END_OF_FILE целы): CONTRACT_VERSION 9→10; VALIDATION «Enabled≠written» расширен на ось РЕВЬЮ (§Review-evidence) + universal-строка сохранена, G3 scripted-e2e назван единственной невшитой validator-осью (FRICTION-watch); CONTOUR VALIDATE записывает `docs/reviews/review-<id>.md` + REPORT RESULT его несёт; PROJECT_SETUP §Strong-check clause (d) + Step-3 run-contract + Done-when seeded-miss (нет файла / чужой-id / неучтённый source-commit / open-или-без-диспозиции / refuted-без-маркера / >3-раундов-без-резолва); coding-agent.md writer-G10 review-evidence bounce (ENABLED NOW, OS-side, доезжает до каждого репо, ключуется на CALL.done_when — сигнал, который writer держит). Артефакт для G0-frozen изменения (тот же триггер non-archived openspec/changes/<id>/, N/A по отсутствию для light; filename id == папка изменения): `reviewed-commit` предок доставленного HEAD с каждым промежуточным source-commit как `fixed <commit>`-диспозиция (stale/неучтённое ПАДАЕТ; честный review→fix НЕ падает), ≥1 проход иным семейством модели, каждая находка с диспозицией (нет `open`), каждый `refuted` с G5/KERNEL-G5-маркером, rounds ≤3-или-резолвящаяся-эскалация. SCOPE (честно): keystone writer-bounce ENABLED сейчас; продуктовый `-Deliver` freshness/structure ИНЕРТЕН до §Re-sync (как v8/v9). §Re-sync OWED: GasCoopGame, health-ai (вшить review-evidence + reviewed-commit ancestry/accounting в deliver strong-check + seeded-miss + synced_contract_version 9→10). Watching (честные остатки): (a) presence≠truth — гейт проверяет наличие/свежесть/структуру артефакта, не правдивость ревью, реальность независимости или верность `refuted`; gate (ii) family-строка и (iv) marker самоавторские — сфабрикованное-но-форматное ревью проходит presence; истинная независимость = валидатор+owner+binding-fresh KERNEL-G5, все сэмплируют, все позже (якорь binding-fresh-строки на committed separate-session RESULT в history/ — кандидат §Re-sync/волны, НЕ строим здесь); (b) `rounds:` самосчитаемое — недосчитанные раунды проходят структурную проверку; резолв escalation закрывает только маршрут «нет эскалации», не фальшивый счётчик; (c) writer-G10 — ЕДИНСТВЕННЫЙ enforcement, доезжающий до каждого репо СЕЙЧАС, но для review-evidence writer НЕ держит независимую копию содержимого ревью (в отличие от done_when для deliverable-оси) → на не-ре-синканутых репо активна лишь проверка «RESULT ссылается на well-formed ревью-файл» — форматирование, не ревью; (d) reviewed-commit ancestry + stale-seeded-miss = §Re-sync-OWED продуктовая работа, до неё writer держит лишь line-presence; (e) мета-дефект «анализ умирает в scratchpad» закрыт для этого инстанса применением, общего handoff-механизма нет → watch ниже. Литмус: следующий engineering-RESULT, отчитавшийся done для frozen-declared CALL без строки review-evidence (или с `open`-находкой / `refuted` без маркера / нерезолвящейся эскалацией на >3 раундах) → writer ОТКЛОНЯЕТ на возврате; после §Re-sync пере-прогон `check.ps1 -Deliver` на леге, чей reviewed-commit не предок HEAD или чей source-commit не учтён `fixed`-диспозицией, ПАДАЕТ, а не печатает «all gates green». G5: обе анализа + red-team = in-session pre-pass (17+22+7 агентов); биндинг-рефутация свежей сессией/иным семейством по ЗАТВЕРДЕВШЕМУ v10-тексту — кандидат перед §Re-sync (как v9).

2026-07-02 попутно (НЕ чиним — «одна проблема на сессию»; занесены как park, чтобы не потерялись): (1) approach:-токен машинно-читаемой строкой в done_when (долг v8 — substitution-детект инертен без него) → idea, watching; (2) NUnit/PlayMode таймаут-обёртка (зависший тест ≠ «зелёный») → idea, watching; (3) changed-file «двойной прогон» на флейки как deliver-зависимость (VALIDATION hygiene#5 есть прозой, не гейтом) → idea, watching; (4) unchecked-тривайр / арифметический guard на core вне defined-wrap доктрины → idea, watching. REFUTED, НЕ пере-предлагать: Russian red-test manifest с прозой «убийца»; CheckForOverflowUnderflow на core (defined-wrap доктрина); STOP-4 deviation veto; unsigned in-repo hash OWNER-APPROVALS (заметь: commit/tree-SHA freshness git-нативна и проверяема git — ЭТО НЕ refuted in-repo-hash approval); InspectCode. Более широкий дев-процесс-план (волны 2-4, анализ wf_01b3ca16): W2 owner-gates (spec-attack диспозиции на freeze + eye-before-DELIVERED, подписи в OS-repo/signed-tag — in-repo хэши подделываемы); W3 negative-control suite + CsCheck property-слой + post-build augmentation (единственный механизм на P1-кластер seam/atomicity); W4 layout-реестр + DELETES + анализаторы; KEY UNVERIFIED LEVER — Unity batchmode -runTests вероятно РАБОТАЕТ на локальной Personal-лицензии (30-мин проверка → авто PlayMode-гейт) → всё idea, watching (отдельные maintenance-запросы, НЕ реализовывать здесь). Мета: находки аналитической (не-writer) сессии нести как MAINTENANCE REQUEST, иначе умирают в scratchpad (witness — v10 пролежал непринятым 2026-07-02) → watching; повтор → кандидат-правило о handoff-обязанности analysis-сессий.

2026-07-03 запрос владельца (MAINTENANCE — кластер «тест зелёный, но ПУСТОЙ»: ~5 дефектов, ВСЕ прошли mutation-ворота: телеметрия мерила не то /147f701/, Deadband config не читался /da695e8/, fork-split проходил на чистой диффузии /5d77267·748d201/, нестрогое `>=` пропускало константу /c7d89e0/; таксономия «weak-oracle ~5» из памяти project-dev-process-2026-07-analysis — реализация запаркованного W3 negative-control из записи 2026-07-02). Корень (НЕ «нет правила»): mutation убивает мутантов кода, который СУЩЕСТВУЕТ — не видит ОТСУТСТВУЮЩЕЕ свойство (нет кода → нет мутанта) и не ловит верное-по-форме-но-слабое утверждение; оракул сам не верифицирован. G4 «every negative control shown REACHABLE by a real run» была ПРОЗОЙ ревью-времени (оттого и всплывало на ревью), не deliver-чеком. Classification: (b) разрыв enforcement с явным триггером-владельцем. БАЗА: v10 (review-evidence) оказался НЕ на main — застрял на direction-ветке wt/indie-game-development (87420a9 / rebased 7a9b21d); по указанию владельца «замержи актуальное состояние» main приведён к фактическому состоянию (--no-ff merge 62d8ac0: v10 + hot-state hygiene + indie Sc-kernel/Sc-reactions/gas-visual + health/life-reset trims) ДО этого бампа, поэтому 10→11 буквально верно и §Re-sync должен v10+v11 вместе. → fixed этим коммитом, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — validator G4/G5, тест-автор, non-convergence, strong-check/coverage/named-approach/registry-walk/review-evidence, default-FAIL леджер целы; KERNEL/плеи/packets не тронуты; END_OF_FILE целы, mid-file inserts): CONTRACT_VERSION 10→11; VALIDATION §Oracle-soundness (negative-control как deliver-зависимость, механическая форма REACHABLE-прозы G4); PROJECT_SETUP §Strong-check clause (e) + Step-3 run-contract + Done-when seeded-miss (нет контроля / свойство ПРОПУСКАЕТ свой контроль / witness-контроли RED-против-свойства: чистая диффузия↛fork-split, константная середина↛OR1, params(16,0)≠(16,16) байты); ESCAPE-CLASSES — НОВАЯ witnessed-строка «oracle empty/weak/mis-targeted — тест имеет силу падать», witness = этот кластер (ратчет MAINTENANCE step 6, один escape достаточен); predicted «catcher-soundness/shared-misread» оставлен predicted (другой ловец = независимая вторая деривация). Контроль пишет НЕЗАВИСИМЫЙ тест-автор рядом с acceptance-тестом, билдер не правит (та же защита, что у тестов, G0). SCOPE (честно): в отличие от v8/v10 НЕТ writer-side keystone (writer не гоняет тесты) → negative-control ИНЕРТЕН до §Re-sync (как registry-walk v9). §Re-sync OWED: GasCoopGame, health-ai (вшить negative-control pass в `-Deliver` + seeded-miss + synced_contract_version 10→11; продукт-caveat — НАЧАТЬ с рефактора per-invariant assertion-хелперов, параметризованных step-делегатом, ДО написания контролей, чтобы assertion свойства перепрогонялась против неправильной реализации без дублирования). ГРАНИЦЫ (одна проблема на сессию, запаркованы ниже). Watching (честные остатки): (a) presence≠truth — гейт проверяет, что контроль ДИСКАВЕРится и его свойство его ЛОВИТ, не то, что «неправильная» реализация содержательно нарушает свойство (тест-автор может написать слабый контроль, который свойство ловит тривиально) — тот же authored-error предел всего deliver-слоя; (b) инертен до §Re-sync — witness-кластер и GasCoopGame механически незащищены, пока не вшито; (c) авторство контроля тест-автором = независимость от билдера, НЕ от спеки (shared-misread покрывается второй деривацией, не negative-control). Литмус: следующий frozen-change лег в ре-синканутом репо не доставляется зелёным, если у acceptance-свойства нет контроля или свойство свой контроль ПРОПУСКАЕТ. G5: applied in-session (эта сессия — writer после RESULT); биндинг-рефутация свежей сессией по затвердевшему v11-тексту — кандидат перед §Re-sync (как v9/v10).

2026-07-03 запрос владельца (MAINTENANCE — реализация запаркованного пункта (1) из записи выше: «property-based слой (CsCheck) поверх negative-control»; крупнейший дефект-кластер, ~11 P1, стыки/атомарность/порядок, ВСЕ найдены на review/post-merge, ни один не пойман mutation/coverage/named-approach/registry-walk/negative-control: потерянный тик da695e8/8ba4491, рваное состояние на прерванной операции 22d548b/4396ded/27f4b26 — пережило 8 раундов ревью на одном леге, seal/breach race за тик 147f701, хэш по порядку вставки e4d0089/c3b85bd — все восемь хэшей проверены по git log продукта, реальны). Корень R3 (диагноз владельца, подтверждён): единственный, кто ВИДИТ новый throw-путь/стык — билдер, чей стимул — пройти тесты, написанные тест-автором ДО кода (G0); ревьюер читает ПОСЛЕ DELIVERED — между ними никто не атакует сам дифф. VALIDATION G2 УЖЕ просит property-based тесты прямым текстом; проверено по факту — GasCoopGame несёт НОЛЬ пакетов property-тестирования (CsCheck/FsCheck) и `tools/coverage-check.ps1` не несёт `prop:`-токена (grep, ноль совпадений) — подтверждённое отсутствие, не отставший re-sync: правило уже написано, никогда не включено. Classification: **(b) разрыв enforcement** с явным триггером-владельцем. → fixed этим коммитом, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — validator G4/G5, тест-автор (пре-код проход), non-convergence, strong-check/coverage/named-approach/registry-walk/review-evidence/negative-control, default-FAIL леджер целы; KERNEL/плеи/packets не тронуты; END_OF_FILE целы, mid-file inserts): CONTRACT_VERSION 11→12; VALIDATION G2 называет фреймворк (CsCheck; FsCheck 3.x fallback) + 2 недостающих класса (permutation-metamorphic, boundary-biased; fault-injection-all-exit-paths и multi-actor-one-tick уже были по существу) + новый параграф §Property-layer deliver-dependency (`prop:` токен на `headless`-строках `core algorithm`-изменения против owner-signed property table — та же механическая форма, что `exists:` у `engine`-строк); CONTOUR — тест-автор получает ВТОРОЙ, POST-build проход (новая стадия цикла PROPERTY AUDIT перед REPORT): после зелёных гейтов читает ДИФФ, не спеку, на новые throw-пути/стыки/зависимости-от-порядка/диапазоны производных и дописывает свойства — структурный фикс R3, адверсариальное чтение в момент видимости стыка, а не до кода (тест-автор) и не после релиза (ревью); PROJECT_SETUP §Strong-check clause (f) + Step-3 run contract + Done-when seeded-miss (нет `prop:`-токена / токен не на `[Category("Property")]`-тест или отсутствует в таблице / таблица без одного из 4 классов); ESCAPE-CLASSES.md — НОВАЯ witnessed-строка «order-dependence / non-determinism (permutation-invariance)» (хэш топологии по порядку вставки, e4d0089/c3b85bd, всплыл на ревью раунда 7-8 — генуинно новый класс; остальные три пары хешей — СИБЛИНГИ уже-витнесснутых строк: da695e8/8ba4491 и 147f701 — «multi-actor/one-tick conflict/seam» /82/; 22d548b/4396ded/27f4b26 — «invariants on failure/guard/throw paths» /94/96/ — тот же класс, другой сайт, по правилу реестра о не-дублировании). ОТКЛОНЕНО, не предлагать снова (по указанию владельца): checked-арифметика (`CheckForOverflowUnderflow`) на core — defined-wrap на core защищённый lockstep-детерминизм-контракт, не баг. Отмечено: существующий кластер статических анализаторов ЭТОТ класс не ловит — нужен рантайм property-слой, не lint-правило. SCOPE (честно): в отличие от v8/v10 нет writer-side keystone (writer не гоняет тесты) → ИНЕРТЕН до §Re-sync (как v9/v11). §Re-sync OWED: GasCoopGame (вшить `prop:` токен в coverage-check.ps1 + property table в замороженных спеках + seeded-miss + synced_contract_version 11→12); health-ai (оценить применимость на СВОЕЙ re-sync сессии, не форсировать — его ядро Markdown/YAML, интерпретируемое LLM, не компилируемый core-algorithm код в смысле CsCheck/FsCheck; транслировать 4 класса на его state-machine/procedure-interpreter инварианты или явно пометить n/a). PILOT: применить сперва на СЛЕДУЮЩЕМ `core algorithm`-леге в GasCoopGame — не ретрофитить уже сданные core-файлы в этом заходе. ГРАНИЦЫ (владелец явно исключил из этой сессии): layout-реестр (W4) и owner-ворота (W2) остаются `idea, watching` — не тронуты, отдельные сессии. Watching (честные остатки, тот же предел всего deliver-слоя): (a) presence≠truth — гейт проверяет, что `prop:`-токен ЕСТЬ и резолвится в таблицу с 4 классами, не то, что генератор СОДЕРЖАТЕЛЬНО достигает стыка (генератор multi-actor, всегда сериализующий акторов, пройдёт токен-чек); (b) ИНЕРТЕН до §Re-sync — witness-кластер механически незащищён, пока не вшито; (c) авторство свойств тест-автором на POST-build проходе = независимость от билдера в момент диффа, не от спеки. Литмус: следующий `core algorithm`-лег в ре-синканутом репо не доставляется зелёным, если у `headless`-строки нет `prop:`-токена или токен не резолвится в property table на все 4 класса. G5: applied in-session (эта сессия — writer после RESULT); биндинг-рефутация свежей сессией по затвердевшему v12-тексту — кандидат перед §Re-sync (как v9/v10/v11).

2026-07-03 попутно (НЕ чиним — «одна проблема на сессию»; park, чтобы не потерялись): (1) property-based слой (CsCheck) поверх negative-control /W3-остаток/ → idea, watching; (2) layout-реестр /W4/ → idea, watching; (3) owner-ворота — spec-attack диспозиции на freeze + eye-before-DELIVERED /W2/ → idea, watching. ОТДЕЛЬНО — процесс-дефект, вызвавший путаницу базы (v10 казался на main, а был на ветке): OS-инфра изменение (v10) закоммичено на direction-ветке wt/indie-game-development, не в maintenance-сессии на main → пропагация не дошла до main до этой сессии (KERNEL §7: OS меняется только в maintenance-сессиях на этом репо; witness — память project-dev-process-2026-07-analysis уверенно называла v10 «APPLIED», но на main его не было). Реконсилировано merge'ем 62d8ac0. КОРЕНЬ (maintenance-правки коммитятся из worktree направления) → logged, watching; повтор → кандидат-правило «os/**-коммиты только на main; maintenance-сессия не коммитит os/engineering на direction-ветку» + digest/audit флажок «os/ дивергенция между main и worktree-веткой».

2026-07-04 запрос владельца («давай применим твои 2 предложения» — два заимствования из Sheaf-ADMM /Sakana, ICML 2026/ по оценке той же даты, память research-sheaf-admm-assessment; триггер (c) customization — явный запрос владельца, порог ≥2-friction не нужен): (1) конфликт-леджер между раундами ревью — `refuted`-находка умирала со своим пер-изменение review-<id>.md, следующее ревью того же шва пере-спорило с нуля; свидетели: отклонённый buoyancy-P1, чьё обоснование жило только в памяти сессии (вне репо); сам OS вручную вёл списки «REJECTED/ОТКЛОНЕНО, не пере-предлагать» в записи 2026-07-02 и в v12-логе — регистр уже существовал неформально, без дома и без enforcement; (2) пересечение зон как детектор ошибок — импортированный «unchanged» контракт контрагента проходил converge-verify шаги 2-3 ПО ЦИТАТЕ (замороженный canon-id = валидная трасса), т.е. согласие на общей границе никогда не проверялось второй независимой деривацией (ловушка признана в converge-arch Notes «consumes unchanged»). → fixed этим коммитом + 2beec18, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — validator G4/G5, тест-автор оба прохода, non-convergence, strong-check/coverage/named-approach/registry-walk/review-evidence/negative-control/property-layer, default-FAIL леджер целы; KERNEL не тронут 1494/1500; packets/SESSION_PAYLOAD не тронуты; END_OF_FILE целы, mid-file inserts): (1) CONTRACT_VERSION 12→13 — VALIDATION §Refuted-register (кумулятивный docs/reviews/REFUTED.md, строка на каждый `refuted`; review-файл несёт `refuted-register:`-строку; повторная деривация закрывается ЦИТАТОЙ `register:<row-id>`; re-raise С НОВЫМ evidence НЕ закрывается цитатой — эскалация владельцу как named-seam решение, регистр записывает re-raise, шов накапливает видимый вес); CONTOUR VALIDATE + §b-walk расширен на touching-строки регистра (`still-holds|re-opened`); PROJECT_SETUP §Strong-check clause (g) + Step-3 run contract + Done-when seeded-miss (4 кейса); coding-agent.md writer-G10 bounce ENABLED NOW (`refuted` без `refuted-register:`-строки = отказ на возврате, line-presence, доезжает до каждого репо до его §Re-sync). (2) плей-сторона, вне периметра CONTRACT_VERSION (коммит 2beec18): converge-verify шаг 3 — boundary-атака (пере-деривация ребра вслепую ДО чтения текста контрагента, затем дифф; расхождение баунсит строку; corroborate, never adopt verbatim); куплено компрессией не-несущих слов (пример шага 2, дублирующая проза Purpose/Notes), бюджет ровно 600/600, ни одно правило не ослаблено. ГРАНИЦА «одна проблема на сессию»: запрос владельца ЕДИН и явно называет обе части («2 предложения») — трактовано как один кейс; каждая часть ушла отдельным, независимо откатываемым коммитом; НЕ прецедент для пачки несвязанных фиксов. Watching (честные остатки): (a) presence≠truth — регистр доказывает, что опровержение ЗАПИСАНО, не что оно ВЕРНО; неверное опровержение теперь распространяется дешевле (цитатой) — противовес = new-evidence-хатч + binding fresh KERNEL-G5 + владелец; (b) продуктовые deliver-чеки clause (g) ИНЕРТНЫ до §Re-sync (как v9/v11/v12), сейчас кусается только writer-G10 line-presence; (c) boundary-атака ловит расхождение независимых деривации ребра, не семантическую ошибку, разделяемую ОБЕИМИ сторонами (тот же shared-misread предел, что у step-2 оракула); (d) все четыре converge-плея теперь на потолке бюджета (598-600/600) — следующая вставка потребует настоящего сокращения, кандидат на отдельный maintenance при повторе. §Re-sync OWED: GasCoopGame (вшить clause (g) + seed REFUTED.md — бэкфилл-кандидаты: ратифицированное buoyancy per-band отклонение /память gascoopgame-buoyancy-perband-ratified/, «checked arithmetic» из v12-лога, REFUTED-список записи 2026-07-02/ + seeded-miss + synced_contract_version 12→13); health-ai (ревью-файлы markdown — регистр применим as-is, на его собственной re-sync сессии). Литмус: (1) следующий engineering-RESULT с `refuted`-находкой без `refuted-register:`-строки → writer ОТКЛОНЯЕТ на возврате; после §Re-sync пере-поднятая зарегистрированная находка без нового evidence закрывается цитатой за один лукап, а с новым evidence — эскалацией владельцу, не пере-спором; (2) следующий converge-verify узла с импортированным контрактом контрагента показывает слепую пере-деривацию ребра + дифф (расхождение = bounce, не молчаливое принятие). G5: applied in-session (maintenance-сессия); биндинг-рефутация свежей сессией по затвердевшему v13-тексту — кандидат перед §Re-sync (как v9-v12).

2026-07-04 запрос владельца (транскрипт review-раунда GasCoopGame dev_2 / c-visual-004 + жалоба: «мы фиксим весь КЛАСС проблем, а не закрываем только один finding — почему ты не следуешь инструкции, в чём проблема, как это исправить»; проблема повторная). Witness: review нашёл source-scan оракулы, удовлетворяемые закомментированным кодом; фикс снял ТОЛЬКО // line-comments; следующий раунд нашёл /* */, закомментированный include и raw-text проверку — ещё 3 сайта ТОГО ЖЕ false-green класса (+2 P2 той же природы: existence-only package-lock check; RESULT-гейт пропускает comment-only секции); та же патология уже 3× в июне (FRICTION 92/94/96, per-species→per-transfer→per-tick). Replay (MAINTENANCE step 1): правило СУЩЕСТВУЕТ в 4 местах (CONTOUR «A FIX IS A CHANGE» с v6; VALIDATION G4 classify+enumerate; продукт-AGENTS.md run contract дословно; REVIEW.md «Report by CLASS, not by site»), reviewer свою половину ВЫПОЛНИЛ (вставленный P1 сам называет класс и перечисляет sibling-сайты) — сломалась FIX-половина: у неё нет НИ артефакта, НИ проверки, только проза, а под retry-давлением автономная сессия удовлетворяет видимое repro (5-е повторение «Enabled ≠ written»: v5/v10/v11/v12/v13). Вердикт: (b) gap — сессия нарушила написанное правило, но нарушает его СИСТЕМАТИЧЕСКИ и enforcement-дома у правила нет → чиним дом, не сессию. → fixed этим коммитом: CONTRACT_VERSION 13→14 fix-class-closure — «fixed» диспозиция закрывается ТОЛЬКО с записанной sweep-строкой (класс; sibling-сайты closed|n/a; seeded-miss/negative-control на КАЖДЫЙ вариант класса для gate/scan/oracle-false-green находок); повторный class-токен в позднем раунде РЕОТКРЫВАЕТ ранний fixed (первый механический хук non-convergence — string-compare в файле, не память сессии); writer-G10 bounce ВКЛЮЧЁН СЕЙЧАС и ключуется на СОДЕРЖИМОЕ RESULT (любой отчёт о пофикшенных findings без class+sweep — bounce), поэтому достаёт и ногу вне frozen-машинерии; продукт-deliver проверки INERT до §Re-sync (GasCoopGame: staged c-exec-026 был 11→13 → впитывает v14 → 11→14; health-ai на своей сессии). Честный потолок: presence≠truth — пустой sweep проходит презенс (но его sibling в следующем раунде триггерит reopen, а не читается свежим finding'ом); переименованный класс-токен обходит string-compare (правда остаётся за G4-прозой и owner'ом).

2026-07-04 попутно (НЕ чиним — «одна проблема на сессию»): c-visual-004 бежала с НОВЫМИ gate-скриптами (tools/package-lock-check.ps1) и scan-тестами БЕЗ frozen openspec-папки, CALL не заявил ни frozen, ни light → review-файл v10 и счётчик rounds легально N/A, раунды жили в чате. +1 witness к существующему light-change-classification watch (CONTOUR §c RESIDUAL / v7 log); на 2-м witness — кандидат на трипваер «нога, добавляющая tools/*-гейт или scan-тест, не может быть light».

2026-07-04 запрос владельца (ТА ЖЕ сессия, ВТОРАЯ проблема — паркинг, фикс отдельной сессией по правилу «одна проблема на сессию»): handback следующего CALL часто доходит до владельца как голый id («запусти c-exec-021»); вставка голого id в продукт-репо (GasCoopGame) уводит ногу МИМО OpenSpec-цикла (PLAN → openspec/changes/<id>/ → red tests → gates). Диагноз по файлам (хунч владельца ПОДТВЕРЖДЁН): (1) schema/packets.md УЖЕ требует `next:` = полный CALL-пакет, и есть команда `collect next for <direction>` (coding-agent.md:28) — рендерит полный paste-ready блок (SESSION_PAYLOAD+play+NOW+CALL) → немедленный обходной путь СУЩЕСТВУЕТ, владелец про него не знал; (2) дыра A — writer «Also emits the next CALL» (coding-agent.md:26) НЕ раскрывает pointer `→ work/<id>-call.md` в полный CALL и не говорит «вставь ВЕСЬ блок в repo, не id»; (3) дыра B (зубы) — продукт run-contract (PROJECT_SETUP installed contract + GasCoopGame AGENTS.md:75-124,247-248) НЕ отказывает голому id/бесконтрактной задаче: весь frozen-цикл триггерится ТОЛЬКО физической папкой openspec/changes/<id>/ («A light change has no such folder → gate N/A by absence»), поэтому голый id без done_when/context уходит по «лёгкому» пути и пропускает цикл. Наименьший фикс (design-ready): A — writer раскрывает pointer в полный CALL + строка «как запускать: paste весь блок в <repo>, не id»; B — entry-guard в PROJECT_SETUP run-contract (→ каждый repo) + GasCoopGame AGENTS.md: «голый CALL-id или задача без контракта (нет goal/done_when/context) = НЕ runnable → STOP, запроси collected блок или reconstruct из work/<id>-call.md; c-exec-*/c-visual-* открывает свою openspec/changes/<id>/ папку на PLAN до build, никогда не 'light'». → next maintenance session.

2026-07-04 запрос владельца (MAINTENANCE — «code review в неудобном формате: ревьюит много проблем НЕ про мой diff, размывает фокус; такие проблемы фиксить надо, но отдельно»; witness — c-exec-028 review, 9 находок, только 2 в диффе, 4 из прочих 7 = баги самих gate-скриптов). 7-агентная панель (wf_44ef672f: 3 classify + 3 refute + synth) + разбор: правило «Report by CLASS / search WHOLE codebase» (v6/G4) ВЕРНО — ловит повторяющиеся классы; дефект в ВЫВОДЕ ревью: нет измерения scope, поэтому codebase-wide sweep и размывает, и БЛОКИРУЕТ один diff, а true-P1 счёт раздут полугодовыми чужими багами. Verdict (MAINTENANCE step 1): (a) rule defect — формат вывода, не сам поиск. → fixed этим коммитом, СТРОГО АДДИТИВНО (класс-поиск не тронут, in-scope гейтинг не ослаблен, ничего молча не теряется; все гейты v1-v14 целы; KERNEL/плеи/packets не тронуты; END_OF_FILE целы): CONTRACT_VERSION 14→15 review-scope split — findings-block получает колонку `scope` ∈ {in-scope|class-sibling} (VALIDATION §Review-evidence (iii)): in-scope (сайт В диффе или регрессия, вызванная изменением) ГЕЙТИТ ногу теми же {fixed|refuted|owner-ack} диспозициями; class-sibling (тот же класс на пред-существующем сайте, который изменение не трогало и не вызывало) несёт `routed <ref>`-указатель в бэклог направления и НЕ гейтит — class-sibling без указателя ПАДАЕТ (ничего не теряется); findings-block получает колонку `site` (file:line), а anti-dodge floor МЕХАНИЧЕСКИЙ: class-sibling-строка чей `site`-файл ВНУТРИ диффа ПАДАЕТ (git-diff name-membership тест, продукт-сторона, та же поверхность что reviewed-commit ancestry, INERT до §Re-sync; writer-G10 несёт site+scope+routed line-presence). RESIDUAL (1b, честно — поймано пред-коммит adversarial-ревью этого изменения): вызванная изменением регрессия на OUT-of-diff сайте, мис-тегнутая class-sibling, обходит ЭТОТ гейт, но ограничена G2/G3 (поведенческие регрессии гейтят безусловно, независимо от scope-тега), оставляя лишь no-test caused-регрессию, мис-осуждённую как pre-existing, слою истины fresh-G5/owner. G4 тегает каждую находку+sibling; CONTOUR VALIDATE/REPORT сдают class-sibling-корзину домой routed-бэклогом; CONTOUR §Fix-is-a-change + VALIDATION §Fix-class-closure позволяют v14-sweep закрыть OUT-OF-DIFF sibling как `n/a because out-of-scope, routed <ref>` (ключевая консистентность — иначе «поиск по классу» всё равно тащил бы весь codebase в ногу через sweep-требование); PROJECT_SETUP §Strong-check clause (d) + Done-when seeded-miss (class-sibling без routed-указателя / in-scope-строка open); coding-agent.md writer-G10 review-evidence bounce ключуется на in-scope-диспозиции + class-sibling routed-указателе (ENABLED NOW, доезжает до каждого репо до §Re-sync). SCOPE (честно): presence≠truth — scope-ТЕГ есть суждение reviewer'а/fresh-G5 над механическим полом (in-diff сайт не может быть class-sibling; out-of-diff causation остаётся суждением, как intent-vs-diff уже есть), reviewer мис-тегнувший in-scope регрессию как class-sibling обходит гейт так же, как любое суждение-в-presence-чеке — G4-проза + fresh-G5 + owner остаются слоем истины. §Re-sync OWED: GasCoopGame (вшить колонки `scope`+`site` + class-sibling routed-check + `site`-diff-membership anti-dodge + v14-sweep out-of-diff `routed` в deliver review-file check + seeded-miss + synced_contract_version 14→15 — впитывается в STAGED c-exec-026 re-sync CALL, теперь 11→15); health-ai (markdown-ревью — split применим as-is, на своей re-sync сессии). Литмус: следующее ревью frozen-change лега возвращает findings-block с колонкой scope; лег с pre-existing находками вне диффа доставляется зелёным (они routed, не fixed-в-леге); RESULT, гейтящий ногу на class-sibling'ах → writer ОТКЛОНЯЕТ. G5: (1) панель + разбор = in-session pre-pass (7 агентов, 3 adversarial refuters — поймали over-reach в смежной проблеме B); (2) пред-коммит adversarial-ревью самого A (fresh read-only Plan-агент) поймал дыру 1a — anti-dodge floor был прозой «enforces by presence» без site-колонки, механически непроверяем → закрыт site-колонкой + diff-membership чеком (+ 2 doc-консистентности: coding-agent fix-sweep `routed`, seeded-miss pass-текст) ДО коммита; биндинг-рефутация свежей сессией по затвердевшему v15 — кандидат перед §Re-sync.

2026-07-04 запрос владельца (ТА ЖЕ сессия, ВТОРАЯ проблема — паркинг, фикс отдельной сессией по правилу «одна проблема на сессию»): исполнитель обязан ЖЁСТКО СТОПАТЬСЯ и звать владельца, когда требуемый инструмент/харнесс недоступен — прежде всего Unity недоступна (MCP слетел / Editor не запущен): сказать «Unity недоступна, надо запустить», НЕ писать обходной путь, НЕ строить костыль, НЕ обходить правило молча. Любая exception-ситуация — ТОЛЬКО с явного письменного «да» владельца В ЧАТЕ (owner-ack, подпись). Владелец: «многие [гейты] от этого выросли» — принцип no-silent-crutch и есть корень v8/ESCALATE. Диагноз по файлам (хунч владельца в основном ПОДТВЕРЖДЁН, дом частично есть): (1) CONTOUR ESCALATE УЖЕ перечисляет «sandbox/permission boundary hit» + «no silent crutch/substitute = owner decision, pre-build STOP» (v8); (2) дыра — «требуемый инструмент/харнесс недоступен (Unity MCP отвалился / Editor не запущен / нет headless-лицензии)» НЕ назван ЯВНО в списке триггеров ESCALATE, исполнитель может не прочитать «MCP dropped» как «permission boundary»; (3) память gascoopgame-local-validation-no-ci + feedback-owner-run-unity-steps-real-gate уже несут «Unity Personal → stop & ask owner to run PlayMode; owner-run шаг не done до подтверждения» — но в run-contract это не поднято явным STOP-триггером. Наименьший фикс (design-ready, следующая сессия): A — явная строка в CONTOUR ESCALATE-списке «требуемый инструмент/харнесс, от которого зависит план, недоступен (Unity Editor не запущен / Unity MCP отвалился / нет лицензии на headless) = обязательный STOP + owner-контакт „<tool> недоступна, надо запустить“, НИКАКОГО обходного пути/костыля/молчаливого сужения»; B — усилить «exception = только явный письменный owner-ack в чате» там, где исполнитель это читает (PROJECT_SETUP run-contract → каждый репо + GasCoopGame AGENTS.md), созвучно v8 owner-ack. → next maintenance session.

2026-07-04 запрос владельца (продолжение той же сессии — «давай все как ты рекомендуешь», разрешил вторую правку сразу): реализация запаркованной выше Unity-STOP/no-crutch. → fixed этим коммитом, СТРОГО АДДИТИВНО (добавляет STOP-триггер + усиливает owner-ack — ничего ранее падавшего теперь не проходит; ни один гейт не ослаблен; все оси v1-v15 целы; KERNEL/плеи/packets не тронуты; END_OF_FILE целы): CONTRACT_VERSION 15→16 — CONTOUR ESCALATE называет «требуемый инструмент/харнесс недоступен (Editor движка не запущен / нужный MCP отвалился / нет headless-лицензии)» явным обязательным STOP + owner-контакт с именем инструмента, никакого обходного пути/костыля/молчаливого сужения; любое исключение (продолжить-всё-равно / замена / расширение модели) стоит ТОЛЬКО на явном письменном owner-ack, который владелец печатает в чате, не builder default; PROJECT_SETUP Step-3 run-contract несёт ту же строку (→ каждый продукт-репо); coding-agent.md Role-2 blockers называет tool-unavailable STOP. SCOPE (честно): поведенческое, не deliver-скрипт — держится остановкой исполнителя + push-уведомлением, как остальной ESCALATE; OS-текст АКТИВЕН сейчас, продукт-AGENTS.md получает явную строку на §Re-sync. §Re-sync OWED: GasCoopGame + health-ai (зеркалить tool-unavailable STOP + written-owner-ack в продукт run-contract; synced_contract_version 15→16 — впитывается в тот же staged c-exec-026 re-sync, теперь 11→16). Литмус: следующий лег, упёршийся в недоступную Unity, СТОПается и говорит владельцу «Unity недоступна, надо запустить», а не пишет обход; продолжение только после явного письменного «да». Границей «одна проблема на сессию» пренебрегли по прямому указанию владельца — сделано отдельным коммитом со своим бампом, НЕ прецедент для пачки несвязанных фиксов.

2026-07-04 запрос владельца (MAINTENANCE — тот же 2026-07-04 no-silent-crutch батч, ВЕНЬЮ-ось: «source-text scan НИКОГДА не доказательство поведения для Unity/шейдеров/рендера; самописные сканеры/парсеры запрещены»; триггер (a/b) + явный owner-запрос). Witness (проверен ЭТОЙ сессией: SourceScanText.cs == 1728 строк, шесть GasVisual/*ScanTests.cs): GasCoopGame dev_2 visual-трек несёт 1728-строчный C#/HLSL/ShaderLab SourceScanText.cs + ~2000 строк scan-тестов + ~44 negative-controls (у самого сканера 245-строчная сюита) — ~3900 строк, доказывающих «строка есть в файле», не рантайм/рендер-поведение. Движок роста: дисциплина v11 negative-control + v12/v14 property/sweep, применённая к ТЕКСТОВОМУ СКАНЕРУ — каждое «а если закомментировано / в строковом литерале / за #if false» заставляло сканер корректно парсить комментарии, строки и препроцессор C#, т.е. становиться компилятором. Replay (MAINTENANCE step 1): VALIDATION НИКОГДА не ОПРЕДЕЛЯЛА доказательство поведения по ВЕНЬЮ, а PlayMode CI отложен (unity.md) → исполнитель заполнил дыру сканером и захардил его под теми самыми анти-пустой-оракул гейтами, что для настоящего кода; v14-witness (c-visual-004 comment-variant arms race, FRICTION стр. 137) — ТОТ ЖЕ корень слоем ниже: v14 заставил ФИКСЫ сканера СХОДИТЬСЯ, но не спросил, должен ли сканер существовать. Вердикт: (a) rule defect + (b) gap с явным триггером-владельцем; реальный past-the-test-author escape → ESCAPE-CLASSES append (одиночный escape достаточен, MAINTENANCE step 6). → fixed этим коммитом, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — source-scan НИКОГДА не был легитимным доказательством поведения, ничего зелёного легитимно не теряется; validator G4/G5, тест-автор оба прохода, non-convergence, strong-check/coverage/named-approach/registry-walk/review-evidence/negative-control/property-layer/refuted-register/fix-class-closure/review-scope-split/tool-unavailable-STOP целы; KERNEL/плеи/packets не тронуты; END_OF_FILE целы, mid-file inserts): CONTRACT_VERSION 16→17; VALIDATION новый §Behavior evidence by venue (движок/шейдер/рендер/сцена = реальный EditMode/PlayMode прогон ИЛИ live-engine MCP-запрос; engine-free логика = реальные headless-тесты; look = owner-eye, G6, не гейт; статический source-scan — НИ ОДНО из этого, даже будучи идеальным компилятором; самописные сканеры/парсеры ЗАПРЕЩЕНЫ как доказательство поведения; единственный легальный текст-чек = задекларированный wiring-smoke: File.Exists + тривиальный substring, БЕЗ acceptance property / БЕЗ negative-control / БЕЗ property-теста → v11/v12/v14 не цепляются к smoke/скану, это и есть срез arms-race; фикс scan-оракула = УДАЛЕНИЕ, не более сильный control); G3 заякорён (engine/render сценарий = реальный прогон/MCP, скан ≠ executed scenario); profiles/unity.md §2 (рендер-поведение = реальный MCP-прогон + owner-eye, сканер запрещён, boundary-гейт остаётся реальным headless-КОМПАЙЛОМ); ESCAPE-CLASSES.md НОВАЯ witnessed-строка «wrong-venue evidence / self-written proxy scanner (execution, not text)» + консистентный указатель на empty-oracle строку (её scan-оракул чинится УДАЛЕНИЕМ, не более сильным контролем). SCOPE (честно): поведенческое/дефиниционное, НЕ deliver-скрипт — держится STOP-ом исполнителя (v16), отказом validator/owner принять скан как доказательство поведения, и тем, что PLAN spec-silence audit теперь ВАЛКАЕТ новую ESCAPE-CLASSES строку (front-load, единственный механический хук); judgment-эмулирующий «это-сканер?» grep СОЗНАТЕЛЬНО не добавлен — это proxy-treadmill, который владелец пометил (память os-review-checker-gate-taxonomy). Пропорциональность: убрать костыль = УДАЛИТЬ театр, не ставить на его место pixel-readback харнесс. OS-текст АКТИВЕН сейчас, продукт-AGENTS.md строка + УДАЛЕНИЕ сканера — на §Re-sync. §Re-sync OWED: GasCoopGame (УДАЛИТЬ SourceScanText.cs + *ScanTests.cs + их negative-controls; зеркалить source-scan-≠-поведение + запрет самописного сканера + wiring-smoke carve-out в продукт run-contract; пере-выразить реальную render-acceptance как owner-eye + live-MCP прогон; synced_contract_version 16→17 — впитывается в staged c-exec-026 re-sync, теперь 11→17); health-ai (markdown-ядро — ТРАНСЛИРОВАТЬ класс: никакого самописного скана kernel-markdown или транскрипта чата как доказательства поведения, поведение = реальный кросс-модельный прогон процесса + owner-ворота; Unity EditMode/PlayMode специфика N/A). Литмус: следующий Unity/render-лег НЕ доставляется зелёным по source-scan; сканер-как-доказательство отвергается на review; недоступна венью → STOP «Unity недоступна, запусти», не скан-обход; PLAN spec-silence audit новой engine-фичи МАРКИРУЕТ «wrong-venue evidence: addressed|n/a». G5: applied in-session (эта maintenance-сессия — writer после RESULT); биндинг-рефутация свежей сессией по затвердевшему v17-тексту — кандидат перед §Re-sync (как v9-v16).

2026-07-04 запрос владельца («развести visualisation / имплементацию симуляции / будущие параллельные треки, чтобы конкурентные сессии не мешали»; witness — ЭТА сессия ТРИЖДЫ налетела на коллизию, закрывая c-exec-028: параллельная визуальная сессия забрала общий счётчик session-id (s-work-046), HEAD дважды уехал под руками, вторая запись матчилась против устаревшего in-memory NOW → «string not found»). Classification (MAINTENANCE step 1): (a) rule-gap в concurrency-адаптере. adapters/worktrees.md уже покрывал «applies сериализуются в одном направлении» и «reset к origin/main ПЕРЕД работой», но НЕ (i) отдельные session-id префиксы для конкурентных workstream'ов одного направления, ни (ii) re-sync ПЕРЕД КАЖДОЙ записью в длинной сессии. Хунч владельца был «Level 2 = институционализировать per-track lanes в NOW-схеме» — судим независимо (MAINTENANCE §2 «механизм владельца = хунч»): это НЕПРАВИЛЬНЫЙ механизм — parallel_tracks НЕ в schema/direction-files.md (это дрейф самого indie-game-development); институционализация растит core и воюет с G1 «один активный bet на направление»; OS-НАТИВНАЯ параллельность = ОТДЕЛЬНЫЕ направления, worktree-на-направление (уже есть: эта FRICTION 2026-06-11 стр.9 + adapters/worktrees.md). → fixed этим коммитом СТРОГО АДДИТИВНО (worktrees.md: подсекция «Concurrent sessions within one direction» — distinct id-prefixes на workstream + re-sync-before-every-apply + явный указатель, что first-class параллельность = ОТДЕЛЬНЫЕ направления, а не больше workstream'ов в одном; ни один гейт не тронут, KERNEL/schema/шесть-типов-state/packets не тронуты, END_OF_FILE цел; НЕ инженерный гейт → CONTRACT_VERSION НЕ бампится). PARKED (НЕ чиним — одна проблема на сессию + без спекулятивной структуры; owner-решение): (1) АРХ-РАЗВИЛКА — «визуальный трек → СВОЁ направление (OS-нативно, общий канон через shared reference); РЕКОМЕНДУЮ» ПРОТИВ «институционализировать parallel_tracks в core (растит core, воюет с G1)» → RESOLVED 2026-07-04 (s-work-049): owner chose NEITHER — NO split. indie-game-development stays the general UMBRELLA (a facet of one game is not a peer direction; a real 2nd game gets its own direction WHEN it exists, not pre-built). The owner's ORIGINAL pain was the PRODUCT-side (GasCoopGame merge conflicts: single root RESULT.md rewritten every leg → parallel dev/dev_2 collide) → fixed by c-exec-030 (RESULT-per-leg + DF-5), NOT by an OS split. Do NOT re-propose the split. Lesson: pin WHERE "tracks collide" (product-repo merge vs OS-state) before proposing structure; (2) product-repo worktree hygiene (именованные gasg-engine/visual/net + re-sync-перед-build; dev_2-заражение уже чинит c-visual-005) → idea, watching; (3) per-track file-boundary contract (скоуп-лек уже ловится ревью v15 review-scope-split + v17 source-scan-ban) → idea, watching. Литмус: две конкурентные сессии одного направления больше не минтят один session-id (разные префиксы), а вторая запись в длинной сессии видит свежий NOW (re-sync-before-every-apply), а не устаревшую базу как в этой сессии.

2026-07-04 — cited-artifact existence gate (contract v18, owner request d-review-citation-fixclass-001 — MECHANICAL gate chosen over prose). Проблема: v10 review-evidence deliver-dependency срабатывает только для G0-frozen change, поэтому light/infra-леги (N/A-by-absence, нет openspec-папки) могли доставить закрывающий отчёт, ЦИТИРУЮЩИЙ docs/reviews/review-*.md / docs/adr/*.md, которого ещё нет в дереве. Рекуррент как класс: c-exec-029 + c-exec-030 (оба самопойманы in-leg-ревьюером, ни один не пойман гейтом; проверено по product-git — только зелёный -Deliver c-030 на 722dcb2 покраснел бы, ревью там ОТСУТСТВОВАЛО; c-029 самозакрылся до своего delivered-tip f926958). Classification (MAINTENANCE step 1): (b) gap + (c) запрос владельца (достаточный триггер). → fixed этим коммитом СТРОГО АДДИТИВНО (VALIDATION §Cited-artifact existence — для КАЖДОГО лега always-run RESULT-гейт требует, чтобы каждый цитируемый путь review/ADR, укоренённый в собственном docs/ репозитория, СУЩЕСТВОВАЛ на HEAD; ОПУЩЕННАЯ цитата не трогается; cross-repo/prose-only/empty-stub вне охвата — никаких контент-сканеров, v17; PROJECT_SETUP always-run Closing-report clause + seeded-miss; CONTRACT_VERSION 17→18; §Re-sync OWED GasCoopGame + health-ai). ORTHOGONAL к v10 (v10 ключуется на frozen-folder id, v18 — на цитируемые пути отчёта). Pre-apply red-team (wf_5c5bfd68-850, 3 линзы + critic, GO-WITH-FIX): исправил завышенный witness-claim «оба поймались бы» → «c-030 поймался бы, c-029 только deters», переформулировал v10 как ORTHOGONAL (не subset), укоренил проверку пути в собственном docs/, задекларировал честные bypass-лимиты. Ни один гейт не ослаблен; KERNEL/plays/six-state/packets не тронуты; END_OF_FILE цел. Литмус: light/infra-лег, чей RESULT цитирует docs/reviews/review-X.md, которого нет в дереве на HEAD, больше НЕ проходит -Deliver (после §Re-sync в продукте); лег, который цитату опускает, проходит как раньше.

2026-07-06 запрос владельца (MAINTENANCE — транскрипт сессии GasCoopGame c-exec-021: сессия прогнала PLAN и BUILD в ОДНОЙ сессии /написала 7 файлов слоя данных + заказала 11 RED-тестов ещё в планировочной сессии/ и выдала план кучей машинных артефактов в scratch-папке + стеной текста; владелец: «во-первых план должен показывать ДЕТАЛЬНО и ПРОСТО, с расписанными техническими решениями; во-вторых планирование и execution — РАЗНЫЕ сессии»). Replay (MAINTENANCE step 1): CONTOUR УЖЕ говорит Roles «always separated» (Planner = interactive session, Builder = autonomous session(s)), но цикл рисует PLAN → RED TESTS → BUILD ОДНОЙ непрерывной цепочкой без явной границы СЕССИИ и STOP, а «owner approves the plan» никогда не требовал owner-ЧИТАЕМОГО плана (только машинные spec/ledger/ADR) → одна executor-сессия, делающая plan+build и вываливающая машинные артефакты — это то, что текст ПРИГЛАШАЕТ. Вердикт: (a) rule defect (двусмысленность роль-vs-сессия) + (c) явный запрос владельца (достаточный триггер). Сиблинг ОСТАВЛЕН В ПАРКЕ (одна проблема на сессию): line-141 entry-guard (голый CALL-id не runnable / `c-exec-*` открывает свою openspec-папку на PLAN до build, не «light») — ДРУГОЙ механизм, всё ещё owed отдельной сессией. → fixed этим коммитом, СТРОГО АДДИТИВНО (ни один гейт не ослаблен — все оси v1-v18 целы; KERNEL/плеи/packets/schema не тронуты; END_OF_FILE целы): CONTRACT_VERSION 18→19; CONTOUR Roles (Planner — ОТДЕЛЬНАЯ сессия, кончается на owner-approval, отдаёт build-CALL, не пишет продукт-код и не заказывает red-тесты; Builder — СВЕЖАЯ сессия, читает замороженный план); CONTOUR цикл PLAN (одобряемый план = детально-но-просто owner-ЧИТАЕМЫЙ документ с расписанными техническими решениями + why; машинные spec/ledger/ADR идут РЯДОМ для билдера, не как план; owner-approval ЗАКРЫВАЕТ plan-лег, RED TESTS + BUILD — отдельная свежая сессия); PROJECT_SETUP Step-3 run-contract несёт ту же «PLAN и BUILD — разные сессии + owner-readable план» строку (→ каждый продукт-репо). SCOPE (честно, как v16/v17): ПОВЕДЕНЧЕСКОЕ, не deliver-скрипт — нет чистого механического сигнала «одна сессия сделала оба» (сессии не видны git), держится остановкой исполнителя на plan-approval + тем, что владелец просто не одобрит план, пришедший в разгар билда, + writer-осведомлённостью; без seeded-miss, без ESCAPE-CLASSES строки (процесс/роль-дефект, который ничто не walk'ает, не продукт-property слепое пятно). OS-текст АКТИВЕН сейчас; продукт-AGENTS.md строка — на §Re-sync. §Re-sync OWED: GasCoopGame (зеркалить PLAN/BUILD-разные-сессии + owner-readable-план в продукт run-contract, РЕКОНСИЛИРУЯ ad-hoc правку root-AGENTS.md, которую транскрипт-сессия внесла руками вне процесса, чтобы совпало с OS-дистилляцией; synced_contract_version → 19, впитывается в staged c-exec-026 re-sync); health-ai (зеркалить as-is — разделение plan/build применимо к любому исполнителю). Литмус: следующий engineering-лег выдаёт СНАЧАЛА owner-читаемый план и ОСТАНАВЛИВАЕТСЯ (plan-сессия не коммитит продукт-код и не заказывает red-тесты), а BUILD стартует ОТДЕЛЬНОЙ свежей сессией на замороженном плане; план, пришедший стеной машинных артефактов, владелец отклоняет как «не план». G5: applied in-session (эта maintenance-сессия — writer после RESULT); биндинг-рефутация свежей сессией по затвердевшему v19-тексту — кандидат перед §Re-sync (как v9-v18).
2026-07-06 indie-game-development/Codex incident: Codex treated a builder handback + owner prose + green product gates + merge/push as enough to close `c-visual-006`, cleared `open_calls`, and wrote `s-work-058` without the required Direction-OS close/G5 evidence. Classification: rule existed (G5/G10) but Codex path had no explicit "builder-return is not close" guard at the writer handback point; owner requested hardening. → fixed this session: reverted bad state commit `78a32c3` with `c7ec9c9`; added explicit builder-handback STOP to AGENTS.md, direction-os skill, and coding-agent writer validation. Litmus: when owner pastes a builder return like "formally closed on dev2; merge/push", the next Codex session may merge/push product if requested, but must leave `open_call` pending or bounce unless a valid Direction-OS RESULT/checkpoint carries binding close evidence.
2026-07-08 indie-game-development/Codex design-lab incident: Codex treated a Design Lab CALL asking for `accepted/revised/rejected/split` as permission to decide, wrote `s-designlab-gas-spatial-form-001`, cleared Q1, and opened Q2 before the owner verdict. Classification: (a) rule gap/ambiguity at the owner-verdict boundary, not an owner input error; chat-first draft said owner must say "можно записывать", but the Codex self-writer path and writer validation did not hard-stop verdict CALLs without owner words. → fixed this session: added owner-verdict guard to AGENTS.md, direction-os skill, and coding-agent writer validation. Litmus: a Design Lab/CALL that asks for accept/revise/reject/split must first show a human brief and wait; without the owner's actual words, RESULT can only checkpoint and must leave the same open_call pending.

2026-07-10 indie-game-development repair: Canon Forge v2 mixed design discovery with canon recording and lacked a pre-candidate dependency/readiness gate; repeated repairs therefore oscillated owner-liked ideas between loss and over-freeze while a linear queue selected bubble-fill before gas-state semantics — owner approved full process research + one pilot before canon work resumes.

2026-07-10 os maintenance: (c) customization — current Codex Ultra provides native proactive subagent orchestration, so the custom global `ultracode` skill/forcing duplicated it; owner explicitly authorized the two narrow `live/**` clause removals with "да"; global skill/global AGENTS removed and Direction-OS wording corrected; historical evidence and native `model_reasoning_effort="ultra"` / `[agents]` retained → partial: four GasCoopGame product `AGENTS.md` copies still require a separate instruction-only commit/propagation because this repo guard blocks product writes; litmus after that handoff: a fresh native-Ultra task has no custom forcing while play-mandated fan-out and fresh-session G5 still hold.
2026-07-10 os Codex-kit consistency sweep: `.codex/README-CODEX-KIT.md` and `.codex/agents/{validator,builder}.toml` still describe cross-family Claude G5 as mandatory while current AGENTS/parallel-verify make cross-family optional and require only a fresh session → logged for a separate maintenance session; not bundled into the UltraCode-retirement fix.

2026-07-11 os writer concurrency (owner correction after `s-repair-minimum-game-frame-001` was bounced only because NOW/LOG blobs advanced): current Role-1 wording treated stale commits/anchors as fatal conflicts even though parallel same-direction sessions intentionally queue RESULTs, forcing needless repair/re-authoring while the RESULT and downstream CALL were logically valid. Classification: (c) explicit owner customization plus (a) rule defect in the concurrency contract; sufficient trigger. → fixed this commit: writer now performs a fresh-state semantic rebase of the RESULT's explicit delta, treats blob/SHA/old-text as merge bases rather than locks, preserves concurrent state outside named targets, and bounces only invalid/incomplete RESULT/`next`, ambiguous intent, or an irreconcilable logical collision after rebase; root Codex instructions, direction-os skill, packet semantics and worktree protocol aligned; deterministic per-operation merge and whole-RESULT replay idempotency included. `SESSION_PAYLOAD.md`, KERNEL, plays, engineering gates and `live/**` unchanged. Litmus: a valid RESULT based on old NOW/LOG blobs lands after unrelated M1/task/open-call/next updates; writer preserves those updates, applies the RESULT by stable ids and commits instead of asking for a rebase packet.

2026-07-11 os maintenance (owner request после инцидента Gate-Q: писарь применил RESULT — LOG/history/трейлеры в порядке — но НЕ перегенерировал владельческую панель направления; обязанность жила только в `live/<id>/knowledge/` карты площадок, вне писарского пути чтения, и Codex-писарь, строго следующий адаптеру, её законно не увидел). Classification (step 1): (b) gap — направленческая обязанность без дома в протоколе писаря; триггер = явный запрос владельца («давай сразу сделаем правильно»). → fixed этим коммитом, СТРОГО АДДИТИВНО: `os/adapters/coding-agent.md` Role 1 получил bullet **Owner-panel regeneration (direction-defined)** — если направление объявляет владельческую панель (render-view файл, чьё место и правила названы в knowledge направления, напр. `work/board/dashboard.html`), то apply, меняющий `open_calls`/`tasks`/`decisions`/`next` или зеркалимое панелью состояние, перегенерирует её живые секции ТЕМ ЖЕ коммитом; формат/чистота (пояснение человеческим языком у каждого ID, закрытое — с итогом, окно журнала, без протухших секций) — из knowledge-записи, не из адаптера; направление без панели → skip silently; правила нечитаемы → применить RESULT и СКАЗАТЬ «панель не перегенерирована», не молчать. Те же строки — в AGENTS.md writer-row и `.agents/skills/direction-os/SKILL.md` §4 (прецедент трёх поверхностей — инцидент 2026-07-06). Ни один гейт не ослаблен; KERNEL/plays/packets/schema/`live/**` не тронуты; бюджеты целы; CONTRACT_VERSION не трогается (не engineering-гейт). Walkthrough (step 5): сценарий Gate-Q — writer применяет RESULT (open_calls меняется) → bullet ведёт в knowledge → карта площадок называет панель + правила → живые секции перегенерированы тем же коммитом; solmax/health-ai (панели нет) → skip silently. Литмус: следующий apply любого писаря (Claude или Codex), меняющий open_calls, либо обновляет панель тем же коммитом, либо явно пишет «панель не перегенерирована, потому что …» — молчаливо отставшая панель больше не является корректным применением.

2026-07-12 indie-game-development repair: an owner-evaluated process pilot could finish ACCEPTED without a mandatory post-pilot authority transition — install the candidate, resume the prior process, authorize an explicit one-shot bridge, or remain blocked — so a downstream canon-admission CALL opened while no governing design-to-canon process existed.

2026-07-12 indie-game-development repair: a second product-side planning handoff changed the gas authority/route without a Direction receipt, leaving NOW.next and the owner panel on a now-forbidden Phase 0 entrypoint until the owner requested repair — watching: any product PLAN that supersedes a live Direction CALL must return HOME and be registered before further gas/visual dispatch.

2026-07-13 os/Codex executor workflow-selection incident (owner transcript — Solmax BUILD CALL `c-solmax-operating-substrate-first-process-creator-t2-materialization-build-001`): the CALL and product repo contract v19 declared only the repo-native workflow and the product repo was clean/current at `34f76d3`; nevertheless Codex self-selected an obsolete globally installed execution bundle and returned NEEDS INPUT three times on that bundle's foreign bootstrap requirements without material work. Replay verdict: not a stale Solmax project; `(d)` execution slip against CALL/repo authority plus `(a/b)` trigger/entry-authority gap. The initial mitigation merely narrowed the obsolete bundle's trigger and left it physically installed; owner explicitly rejected that partial fix and required full retirement. → fixed by the paired OS/product commits: the obsolete global bundle and tracked project-specific tool config were deleted completely; active requirements/tooling/executor/setup contracts were made provider-neutral and repo-authoritative; CONTRACT_VERSION remains 20 because the gate semantics are unchanged; `solmax-operating-substrate` now mirrors the repo-local authority rule and stamps `synced_contract_version: 20`; `live/**` untouched. Litmus: a fresh session given the same unchanged BUILD CALL sees no obsolete execution skill/config, adds no undeclared tool requirements, and starts through the repo's OpenSpec + local `check.py` contract.

2026-07-14 os/GasCoopGame NearGas PLAN-to-RED incident (owner transcript): the first spec-only RED author stopped because construction/fault surfaces were absent; PLAN-AMEND closed only those two named blockers; a narrow Direction refutation generalized that local GREEN to execution readiness; the next fresh RED author found nine further elementary fixture/observation/golden/negative-control gaps. Timing diagnosis: spec-only RED became strict in the product on 2026-06-16, v11/v12 expanded negative/property obligations on 2026-07-03/04, and v19 split PLAN from BUILD on 2026-07-06, removing the same-session context bridge; PLAN never gained a complete spec-to-test recipe pass for extensions. Classification: (a) rule defect ("code does not exist" assumed greenfield), (b) handoff gap (local blocker closure could open whole execution), and (c) explicit owner request. The first maintenance attempt then violated minimality by growing a Python conformance system instead of fixing communication. -> fixed this commit: the entire local executable v21 draft is removed; final v21 is a small semantic full-packet AI dry-run with one `fixture|call|observe|source|negative` recipe per obligation, complete gap list, full invalidation after any amendment, and writer refusal to open EXEC from a named-blocker-only GREEN. MAINTENANCE now forbids new executable proxies for plan/spec/rule meaning without a separate explicit owner-approved request naming one objective machine predicate. No live or product file changed. Litmus: amend only Create/fault in the witnessed NearGas packet; the next PLAN review must still return RED with the other nine groups before owner approval/EXEC, and a maintenance reviewer proposing a parser must stop at a short proposal instead of writing it.

2026-07-14 os/GasCoopGame NearGas EXECUTE-003 second PLAN-to-RED incident (explicit owner complaint): after hours of PLAN plus binding semantic review, the packet claimed 29/29 executable recipes, but the first actual C# RED consumer returned 0/29 and no commit. Replay against the frozen packet: AR-01, AR-06, AR-12 and AR-13 mix process order, implementation inspection, structural review and final-gate evidence into the test numerator; F5/pending impulses lacks the full comparison surface; the CsCheck 3.2.2 row names seed/shrinker/case count but no compile-ready invocation. The plan reviewer and binding review both inspected the same prose representation, so the second check was fresh context but correlated evidence; the test-author was the first real compiler-backed consumer. Classification: (a) rule defect in v21, not an expected quality benefit, plus (c) explicit owner request. -> fixed in this commit, additive and without a semantic proxy: contract 21->22; obligations split `behavioral-red` vs `evidence-only`; prose N/N cannot authorize BUILD; PLAN hands off only to a separate RED-FREEZE session that authors the full test/support patch once, commits it without production code and runs the real repo command; the already-required binding fresh review now refutes that real artifact rather than re-reading the prose; BUILD pins and reuses that immutable commit instead of authoring tests again; writer evidence guard rejects partial suites, guessed helpers/framework APIs, process rows posed as tests and unexplained diagnostics. The independent plan review remains for meaning/coverage, but is no longer mislabeled executable proof. No `live/**` or product repo changed. Re-sync owed for GasCoopGame and health-ai; the active NearGas packet then needs a Direction PLAN-AMEND/RED-FREEZE repair. Litmus: the witnessed packet cannot open BUILD because the four non-behavioral rows are outside the RED numerator and the unresolved F5/CsCheck surfaces prevent an eligible RED commit; after amendment, one real RED commit is reused by BUILD with no duplicate pre-build test authoring.

2026-07-14 solmax t-4 repeated repair seam: rejection of legacy Direction Health authority was collapsed into rejection of an already owner-approved and materialized first-process candidate in the separate Solmax product repo, so a repair discarded valid owner answers/research/build evidence and emitted a kill/reshape CALL; owner had to correct the same interpretation twice. Watching: a direction-authority rejection must disposition product evidence separately and may not erase a candidate route without an explicit owner verdict on that candidate itself.

2026-07-15 solmax t-4 owner-facing engineering-stage leak (owner complaint after CALL 016: several consecutive calls were checks/rechecks, then the handoff exposed a standalone `RED-FREEZE` CALL with no plain roadmap to real process use). Replay: the v21-after-v22 repair, recovery re-sync, exact report-marker correction and binding recheck were real but produced no Health behavior; the marker leg was a one-off execution/report defect. More importantly, CALL 016 required the Direction to author a product-internal RED-FREEZE stage even though KERNEL §4 and CONTOUR Boundary already say an engineering CALL is a business outcome and the product contour owns HOW. Verdict: `(d)` execution slip in CALL authoring plus a manual-orchestration gap; no gate/rule change in this session because fresh-session separation cannot be honestly collapsed by wording alone. → logged, watching. Immediate litmus: the next owner-visible Solmax CALL names the outcome — bounded F-HR-R01 corrected and independently verified — and explains the remaining stage count; `RED-FREEZE` may appear only as product-internal context, never as the owner's goal. A real removal of manual stage relays requires a separate explicit orchestration request, not a fake one-session waiver.
2026-07-15 os/GasCoopGame B1 surface-freeze hygiene gap (explicit owner request): the experimental compiled-surface carrier passed headless build/tests through A1-A4, but the first honest `-Deliver` after `b0e528d7` found five new Unity scripts without tracked `.cs.meta`; the final hygiene gate worked, yet the proposed SURFACE freeze never ran it, so an incomplete platform artifact reached RED and BUILD. Classification: (b) gap in the proposed B1 freeze boundary, not a missing gate. → fixed in the B1 handoff/audit packet by requiring repo-native build + repo-native hygiene and every mandatory platform sidecar in the same freeze commit; hygiene remains read-only/fail-closed, and this requirement ships with B1 rather than consuming a standalone contract version.

2026-07-15 os/GasCoopGame B1 compiled-carrier maintenance (explicit owner request; binding NearGas close evidence at product 400ef8eca1e747378493c97530770286c7e26ff1): v22 required an independent RED author to obtain compiling tests for a not-yet-existing public API from a prose-only frozen spec, so the rule itself drove constructor/seam transcription, 68 hours/eight branches/zero production code and a 205-diagnostic RED artifact; classification (a) rule defect + (b) missing carrier stage, not an execution slip -> fixed by this commit: contract v23 makes PLAN -> separate compiler-green SURFACE-FREEZE (real signatures, <=400 added skeleton source lines, <=400-word fully-defined-fixture decision page, exact freeze Git manifest, field-preserving value construction, no meaning in `///`, repo-native build+hygiene and mandatory sidecars) -> independent immutable RED -> binding fresh artifact refutation -> BUILD the terminal compiled-leg route; compile-RED, test counts and retired v22 prose PLAN-AMEND cannot launch BUILD; all v1-v22 gates remain; non-compiled repositories require a native carrier or explicit owner-approved n/a instead of copied C# mechanics; GasCoopGame re-sync is owed directly 21->23 with no v22 intermediate. No ESCAPE-CLASSES row: this is a workflow carrier/process defect, not a late product invariant class. `live/**`, product repos, B2/B3/B4 and mutation/Stryker untouched.

2026-07-15 solmax/markdown plan-review blow-up (explicit owner request): the product profile already said Markdown/YAML meaning belongs to AI review, but the general v22 carrier still made the Health repair inventory every verb/status/SHA/ban/command/report field; the bounded 48-obligation plan became an uncommitted 254-row PLAN-AMEND across three process commits while the two requested target classifications stayed untouched. Classification: (a) rule defect + (b) missing semantic-artifact route + (c) owner-approved customization, not a useful quality cost. -> fixed by contract v24 in this commit: an owner-approved `lane: semantic-artifact` / `carrier: n/a-semantic-artifact` routes existing PLAN directly to a separate BUILD, concrete wiring checks, one fresh source-backed review of the actual candidate diff, then REPORT; no new template/inventory/ledger/spec-silence/SURFACE/RED/parser; blocking findings require authority + exact site or concrete omission/contradiction/material ambiguity + consequence, while atomization alone is non-blocking; compiled v23 and executable gates remain unchanged. Re-sync owed: `solmax-operating-substrate` 22->24 directly; product and `live/**` were not edited here. Litmus T4: build the two Health classification corrections from the already-approved plan, then one fresh reviewer checks task->plan->diff plus neighboring authority consistency; it may block on a cited mismatch but cannot reopen PLAN merely to split prose. Control litmus: a simple storage/instruction Markdown change with no user-visible outcome uses its plan-defined cross-reference/use check, not an invented universal result or RED inventory.
2026-07-15 os/GasCoopGame B2 mutation-scope maintenance (explicit owner request): `mutation.ps1 -Since` не работает с linked-source Core, поэтому scope задавал сам builder через `-Mutate`; отчет сохранял scope, но `check.ps1 -Deliver` читал только score. Класс дважды дал ложный GREEN (#14: измененный `VoxelField.CanAdmitType` полностью Ignored; #69: измененный `SparseDominantGuardrails.cs` полностью Ignored), а первый полный прогон показал реальные 68.19% ниже floor 70. Вторая причина нежизнеспособности ворот: default Concurrency=4 на 32 logical processors и 30+ минут на живом прогоне. Classification: (a) rule defect в load-bearing G2 scope/enforcement, не execution slip. -> fixed by this commit as contract v24: Deliver независимо выводит измененные eligible Core/source files `D` из authoritative review diff и fail-closed падает до score при malformed/missing normalized report scope `M` или `D ∖ M != empty`; reviewer сам выводит scope и сам запускает mutation, builder scope/report/score не evidence; adaptive default `min(24, max(4, floor(logicalProcessors / 2)))`; scope честно file-scoped, не line-scoped, поэтому горячий файл мутируется целиком и все еще может быть долгим. GasCoopGame §Re-sync 23->24 owed; retained 72.78% не перезапускался. No ESCAPE-CLASSES row: это дефект workflow gate, не поздний product invariant-class. `live/**`, product repos, NearGas L1a, B3 и B4 не тронуты.

2026-07-15 os engineering B3 obligation growth (explicit owner request; witnesses: `os/docs/ENGINEERING_CONTOUR_AUDIT_2026-07-14.md:296` and `os/docs/HANDOFF_2026-07-14_engineering-audit.md:708`): contract versions could add gates and requirements indefinitely, while MAINTENANCE had no symmetric semantic ceiling; its word budgets did not count independent duties and owner agreement with a session-proposed mechanism could be misrecorded as an owner-originated trigger. Classification: (a) rule defect + (b) missing valve, not an execution slip. -> fixed by the B3 commit carrying this line: contract v25 defines a removal-test leg obligation, records a manually checked v24 maximum N=239 in `os/engineering/OBLIGATION_INVENTORY.md`, keeps post-change N <=239 by requiring enough named real deletions in the same commit to offset any growth, rejects compression/relocation as deletion, gives `os/engineering/**` the exact v25 mandatory-reading cap of 32,511 words, preserves the >=2 FRICTION threshold for a session-originated mechanism, requires resolvable named-repo path:line/SHA witnesses, and binds every temporary duty to a named retirement leg/done_when. A defect in the valve may be fixed in the same maintenance job. B4 deletion, product repos, `live/**`, and Stryker remained untouched. Litmus: at N=239, adding X requires removal of Y and adding X+Z requires two real removals; rewriting Y into one longer sentence still counts Y.

2026-07-16 os engineering B4 mandatory-reading reduction (explicit owner request; corrected witness: `os/docs/ENGINEERING_CONTOUR_AUDIT_2026-07-14.md:14-30`; full proof: `os/docs/B4_ENGINEERING_CONSUMER_AUDIT_2026-07-16.md`): the old kill-list was an unverified line-count hypothesis whose first item conflated ~15 lines of retired plan pinning with ~830 lines of live shared Git tooling, while mandatory `CONTRACT_VERSION` repeated 7,639 words already preserved exactly by Git. Classification: (a) rule defect in the mandatory-reading/history boundary + (c) requested simplification. → fixed by the B4 commit carrying this line: every candidate was searched through all OS authority/adapters/profiles/inventory and registered product tracked trees; every mechanical candidate was KEEP because of live/ambiguous consumers; no gate, product file, spec payload or obligation ID was removed; `N` remains 239; the only proved redundant cluster, Git-backed contract narrative, became an 826-word `version → change → Git locator` map with monotonic current 26 and terminal Re-sync; mandatory reading fell exactly 32,511→25,726 words (−6,785), without long-line packing. Gas/Solmax owe later v26 metadata Re-sync, health-ai must first reconcile its pre-existing v11 functional gap, and unstamped Zaratusta/life-reset-manager use current setup only on activation. Product repos, `live/**`, Stryker and excluded-category remained untouched. Litmus: a v25 product is still flagged behind, rows 26 and its exact Git delta resolve, and a normal PLAN→SURFACE→RED→BUILD→review→Deliver plus light/STOP paths retain every gate.

2026-07-16 indie-game-development repair: mutable GasCoopGame worktree paths, branches and Editors were copied into Direction knowledge as launch authority; product contract later moved to one persistent dev workspace, leaving two competing dispatch systems and recurring stale lane/P2a0/Phase-0 routing. Owner chose product-owned single mutable venue v2 (A): Direction keeps outcomes/conflict ownership only, product authority owns fresh topology; watching until the product registry install and first lossless legacy-worktree retirement prove the seam dry.

2026-07-16 owner correction: model-family identity had remained a hard engineering-review gate in CONTOUR/VALIDATION/PROJECT_SETUP and product consumers after the owner had already removed it from Codex guidance on 2026-06-15; the same-model-only environment therefore blocked a valid fresh non-author review. Classification: rule contradiction + explicit owner request. -> fixed by the contract-v27 commit carrying this line: X14 is replaced one-for-one (N remains 239); fresh context, non-authorship, read-only authority, exact reviewed commit and all finding/refutation gates remain mandatory; model fields are provenance only, equal values are legal, and model/provider availability can never block review or delivery. Product repositories owe a v27 Re-sync that deletes model-inequality checks without weakening independent review.

2026-07-16 indie-game-development same-chat execution: the tracked c-visual-006 guard still denied every product-repo write command globally and hard-coded a read-only git verb set, so the owner's explicit instruction to repair and continue in the current chat could not reach even the legal registry reservation. Classification: stale adapter guard, not a product-protocol choice. -> fixed by this commit: product writes remain default-deny; a policy-configured exact owner-ack lease can authorize the bounded job, and the lease is removed after publication. Litmus: the same product mutation command blocks without the exact token, passes pre-tool evaluation with it, and blocks again after lease removal. The bounded L1b lease was revoked after final product publication/readback at `2d995d69b503f73582e493c36c776a8b70f6bf66`.

2026-07-17 os/GasCoopGame L1B v27 repair venue cleanup (explicit owner correction): execution created `C:\tmp\l1b-v27-repair` as an auxiliary checkout because that path was writable while the registered product venue was outside the session writable roots. This contradicted the already-correct registry rule: the repair row named `C:\projects\Unity\GasCoopGame_dev`, so sandbox friction required escalation or STOP, never an unregistered venue. Classification: (d) one-off execution slip; no rule change. Cleanup proof: the directory had no `.git` or reparse points; all product files resolved to published Git blobs (`tools/review-check.ps1` matched final HEAD after Git normalization), `direction-result.md` matched committed Direction history exactly, and the remaining six Python files were read-only disposable inspection/HTML helpers. The exact 23-file / 331,537-byte directory was removed; no matching product worktree metadata remained under the standard basename variants. → logged, watching. Litmus: a future session blocked from the registered venue requests authority or stops; it never creates a writable-root worktree as a workaround.

2026-07-17 Direction OS parallel workstreams (explicit owner complaint and clarification): one global `NOW.next` dispatched only one line while `open_calls` was an in-flight registry, so canon/core/other work disappeared from the usable frontier; prior advice that this was already designed was false, and the current track names are examples, not a closed taxonomy. Classification: (a) rule defect in OPEN/default/concurrency semantics + (b) missing generic lifecycle/dispatch capability, evidenced by R-20/R-42 and the live indie direction's concurrent calls; no owner-usage error. → fixed 227f33e: optional dynamic `NOW.tracks` with owner-approved `track_wip_limit`; one primary bet owner plus arbitrary parallel scopes; `open_calls` is the ready/waiting/blocked/paused dispatch frontier with stable never-reused ids, one root per track and recursive same-track child joins; `NOW.next` is only the default selector; writer merges disjoint RESULTs/child receipts in either order, rejects overflow/collisions, and preserves unrelated calls/default; add/pause/resume/retire/rename/primary-handoff routes are generic, labels are human-facing, and legacy single-track directions stay valid. `live/**` and the engineering contract were untouched. Litmus: run core+canon, return their RESULTs in reverse order, return two canon children in reverse order, try adding sound at a full WIP limit, then retire a track; every ready CALL remains visible, only the right parent wakes, overflow asks for pause/retire, default remains valid, and retired ids never reappear.

END_OF_FILE: os/FRICTION.md
