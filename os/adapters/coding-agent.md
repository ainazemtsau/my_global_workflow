# Adapter: coding / working agents (Claude Code, Codex, Gemini CLI, ...)

Two distinct roles run on agentic tools. Any vendor works; switching vendors changes nothing outside this file.

## Role 1: writer (kind: mechanical)

Applies a RESULT's `state_changes` to `live/**` and commits.

Contract:
- Input: one complete RESULT packet. A bare `state_changes` section is incomplete because replay identity, G9/G10, owner-verdict/G5 and `next` validation require the full packet; bounce it before writes.
- Apply the RESULT's declared state-change intent atomically against fresh current state; append the log line once to LOG.md; save the full RESULT once to `history/<date>-<session-id>.md`; commit with a descriptive message (`<direction> <play> <node/task>: <log line>`).
- Maintain the `END_OF_FILE: <path>` trailer on every state file it writes (truncation guard, schema/direction-files.md).
- **Fresh-state semantic rebase is mandatory.** Re-read every target immediately before apply. Blob/SHA/commit ids, expected old text and exact anchors in `state_changes` are the base for a three-way rebase, not freshness locks. A mismatch by itself is information that concurrent state landed, never a reason to bounce. Derive only the RESULT's explicit delta, locate targets by stable path/id/key, apply that delta to the current files, and preserve every concurrent change outside those targets. `Preserve unchanged` means preserve the current value after rebase, not restore the packet's base. Compatible edits to a named target are merged; moved anchors are relocated by their stable identity.
- **Bounded integration judgment only.** The writer may resolve stale anchors and compatible parallel updates; it may not invent or alter outcomes, evidence, owner verdicts, gates, tasks, CALL meaning, or unrelated state. Compare base-to-RESULT and base-to-current at semantic-field level: shared path/id or whole-object inequality alone is not a collision.
- **Bounce threshold.** Bounce only when the RESULT or its `next` CALL is invalid/incomplete under the checks below, the intended delta is itself ambiguous, or a fresh-state rebase leaves mutually exclusive meanings for the same semantic field (for example, the returning call was consumed by a different RESULT, or a named target was deleted/retargeted so the declared outcome is no longer meaningful). Report that semantic conflict. Never bounce merely because commits, blobs, anchors, `updated:`, unrelated tasks/open calls, or global `NOW.next` changed meanwhile.
- **Whole-RESULT replay is idempotent.** Before normal apply, look up the committed history path for the incoming session-id. A byte-identical saved full RESULT is definitive proof that the whole atomic apply already completed, regardless of later state that superseded its postconditions: make no mutation and return the existing commit/outcome plus current valid routing (the incoming `next` if still pending, otherwise current `NOW.next`). Never restore old postconditions during replay. Different RESULT content under the same session-id is a logical collision. A history/LOG mismatch is state inconsistency and routes to repair; an interrupted uncommitted apply is discarded by the mandatory fresh-worktree reset and then applied normally.
- **Deterministic merge operations:**
  - `preserve unchanged` or an unnamed field: keep the fresh current value.
  - add/upsert a list or map entity by stable id: absent means add; existing means merge field-by-field. Preserve current-only changes, apply RESULT-only changes, and treat identical changes as a no-op; a field changed on both sides follows the scalar rule below.
  - remove by stable id: remove the fresh current entity. If absent, same-RESULT replay is a no-op; a different recorded consumer is a logical collision.
  - explicitly set a scalar: use true three-way comparison. If RESULT equals base, keep current; if current equals base, apply RESULT; if current equals RESULT, no-op; if both changed differently, bounce as a semantic collision. Two explicit exceptions are last-applier-wins: `updated:` always names the applying RESULT, and explicitly set `NOW.next` takes the applying RESULT's valid self-contained CALL/pointer. Unnamed or declared-unchanged `NOW.next` keeps current. Final merged state must still pass binding evidence, owner-verdict and hard-gate checks.
  - append LOG/history: key by session-id/history path and write once; identical existing content is a no-op, different content under that identity is a collision.
  - create a complete work/CALL file: absent means create, byte-identical means no-op, different existing content is a collision unless the RESULT explicitly declared a patch to that file.
- Reject CHARTER.md/TREE.md changes whose RESULT carries no `owner_approved` mark (gate G9) — reply with the missing approval instead of applying.
- Validate before applying (gate G10) — bounce with the specific miss, never apply partially:
  - RESULT fields complete per os/schema/packets.md, `play_check` included (one line per play step);
  - `(owner)`-marked steps in play_check carry the owner's actual words, not a bare "done";
  - **owner-verdict CALL guard (session RESULTs)** — when the returning CALL's `goal`/`done_when` asks for an owner-readable
    verdict (`accepted/revised/rejected/split`, approve/reject, choose, "можно записывать", or similar), a RESULT that
    clears the open_call or opens a downstream CALL must carry the owner's actual verdict words in `evidence` or
    `play_check`. A CALL packet, a recommendation, or the agent's phrase "accepted/revised" is not a verdict. Without
    owner words, bounce or checkpoint while leaving the same open_call pending.
  - **owner-verdict meaning (engineering PLAN)** - real words are necessary but not sufficient for EXECUTE. Only an
    explicit `accepted` verdict for the exact unchanged candidate `P`, TESTABILITY blob and PLAN receipt may lead through
    a later fresh finalizer to EXECUTE. `revised|split` invalidates that receipt and routes to a new PLAN candidate plus a
    full fresh binding-validator pass; `rejected` creates no EXECUTE. A downstream CALL inconsistent with those words
    bounces even though an owner quote is present.
  - the `next` CALL's goal is an outcome with no method/procedure paraphrase (CALL hygiene, schema/packets.md).
  - **CALL materialization (ENABLED NOW)** - engineering intent carries only the literal logical locator. Writer finalization adds schema-1 git-moving-ref resolver fixed to canonical `https://github.com/ainazemtsau/my_global_workflow` + `refs/heads/main` + `os/engineering/CONTRACT_VERSION`. It freshly queries/fetches the remote ref and reads that exact object. Current/wt/local-main/tracking refs never authorize; common-dir cache is readable only after exact equality to the fresh remote SHA, with no remote-less fallback. Executor repeats independently; CALL/env/product cannot override. Finalization records remote/ref@SHA query/fetch evidence and path/mode/blob/current. Adapter author/review pin an equivalent product-native resolver; install proves equality. Failure is `STOP - CONTRACT AUTHORITY UNAVAILABLE`. Original finalized receipt/entry observation stay byte-identical through install retries.
  - **engineering transaction identity (v21, ENABLED NOW)** - every engineering CALL has one normalized `SETUP|RE-SYNC|PLAN|EXECUTE|BUILD` phase and absolute `worktree`; `repo` is logical only. RE-SYNC also has `resync-stage: adapter-author|adapter-review|install`. PLAN/EXECUTE/BUILD and every RE-SYNC substage name exact branch/base and external `base-authority: sha:<sha>|ref:<ref>@<observed-sha>`; base equals that SHA and a ref resolves exactly. PLAN/EXECUTE/BUILD retain byte-identical stable parent `acceptance-source` and exact done_when `acceptance-hash`. Writer resolves both from Direction state before apply/collect; it never advances authority to HEAD or accepts a child-rewritten base/acceptance. SETUP alone is branch/base-free. Deliver/close are handback gate modes under the unchanged finalized BUILD CALL; changing its phase, entry HEAD or receipt at D/A bounces.
  - **SETUP preservation/bootstrap (ENABLED NOW)** - SETUP admits absent or virgin existing entry only. Existing virgin must be a clean initialized non-bare Git worktree with pinned branch/HEAD/index/inventory, both contract/stamp absent, and reachable product+Direction history proving no prior marker; non-git, unborn, dirty or unprovable state is target collision/corrupt. Absent entry pins parent plus repo-bootstrap. Atomic sibling temp-root mkdir is its first attempt-marker state; exact empty unmarked root may only receive the inner zero-byte marker, nonempty unmarked stops, and marked partial root may be discarded/rebuilt while target stays absent. Publish only the verified clean deterministic parentless repo. Interview/author/review are read-only; writer-finalized install alone writes declared contract/tool create/overlay/delete, marker/temp and Git journal rows.
  - **RE-SYNC boundary (ENABLED NOW)** - every substage carries `resync-entry-reason: lower|current-invalid`. Every existing target, including one with a syntactically complete contract/stamp pair, enumerates every reachable matching marker+receipt event in product HEAD/Direction ancestry, requires one linear nondecreasing ordinal chain and selects its unique descendant-most/highest event; conflict/decrease stops. When any trusted event exists, the on-disk ordinal must equal that recovered ordinal; mismatch is initialized-corrupt and routes only by the recovered lower/equal/future value, never the disk value. Deleting current marker/files cannot erase prior history, make virgin or launder a rollback. Recovered lower/equal/future routes lower/current-invalid/future respectively; no trustworthy ordinal is `STOP - CONTRACT STATE CORRUPT`. Author/review are read-only; finalized install alone writes exact manifest/Git transaction. Matching open inner marker, or the exact-empty absent-SETUP sibling temp-root first-marker exception, resumes first. If live advanced, exact terminal proof returns `MANIFEST-RECONCILED`, consumes the old call with its old receipt+retry observation, emits no current/feature receipt, then freshly routes lower/current-invalid/future/unavailable.
  - **PLAN entry/final (ENABLED NOW)** - author intent carries external id/base/acceptance and installed toolchain/conformance authority, but no future TESTABILITY pin or PLAN receipt. Planner commits exact ungated candidate P and returns PLAN-CANDIDATE with no verdict/GREEN. Writer persists that event and a mechanical finalizer; a fresh finalizer creates exact-P `PLAN/binding-validator`. The distinct read-only session independently derives obligations, generates scratch probes and runs the pinned real toolchain. Only its persisted PLAN-GATED GREEN event and another fresh finalizer create `PLAN/verdict`; only explicit accepted owner words for unchanged P/TESTABILITY/receipt and a later finalizer create EXEC. Revised/split re-enters PLAN with a new full binding pass; rejected creates no EXEC. Stale P, planner self-validation/probe source, validator product write, skipped/nonzero observation or approval-before-GREEN bounces to PLAN.
  - **fresh EXECUTE and authorized BUILD resume (ENABLED NOW)** - EXEC session supplies stable intent without run/pre-execution receipt. Writer verifies plan receipt/candidate and packet-only external-base delta, runs pre-execution, then adds/persists deterministic run-id, pre-red-head, receipt and observation hash before payload. First independent RED checkpoint is direct child of pre-red-head or an exact declared test-author-only chain; its receipt pins role/session, parent and exact test/oracle-registration-only delta. No source/frozen/toolchain/result/unmanifested change may enter R. BUILD resume requires same finalized run/plan/base/pins, protected RED revision and accepted progress; writer finalizes current resume observation only. Foreign diff, mixed source+test R, wrong parent/run/base, rewritten RED, unaccepted progress or pre-v21 checkpoint bounces.
  - **typed packet, complete bindings, fresh probes and oracle (NORMATIVE)** - every obligation has complete `construct|act|observe|negative|source|skeleton` routes plus a closed typed registry; no bare non-empty shortcut. Fully qualified signature/visibility/concrete owner/args/returns/observe/inject/delegate tokens resolve. Source artifacts, instances, callables, runtime resources and operation-owned slots are disjoint. The fresh binding-validator derives the union independently, generates non-persisted probes, expands the reachable compile/resource closures against exact base and proves real compile, unique discovery and exact filtered sentinels without a production stub. Missing loader/golden/full hidden-state observation/fault slot/helper/resource, kind conflation, substituted runtime, PASS or wrong sentinel is PLAN RED. The later test-author authors fresh spec-derived acceptance R without validator probe source; R pins regular rows, unique IDs, registration/filter/status and exact allowed delta. Later revisions parent exact accepted progress, preserve original test/oracle bytes/mapping and append only separately identified same-obligation variants. Deliver/close require protected original-plus-variant history and discovery/pass.
  - **result/archive/recovery (ENABLED NOW)** - base config declares result template; the finalized BUILD CALL pins canonical in-worktree `product-result-path`. At D the deliver gate computes `D:<that path>` blob in handback while the CALL remains byte-identical to BUILD entry; product result never self-hashes/commits. At A the close gate likewise records archive facts only in handback and verifies typed packet relocation, frozen external rows, base-only baseline rows, toolchain/oracle/result identities and one next archive commit. The routing receipt may be reconstructed after interruption under that same BUILD CALL: rerun deliver at D, or at clean A derive `D=A^` and D:path/blob before structural close. Missing stdout is not loss of authority and creates no DELIVER/CLOSE CALL.
  - **v21 event-to-child split (NORMATIVE over shorthand above)** - PLAN chronology is `C-plan-call < W-plan-candidate < W-binding-validator-call < W-plan-receipt < W-verdict-call < W-verdict-result(accepted exact P) < W-execute-call`. Planner RESULT creates only candidate event plus pending mechanical finalizer; fresh finalizer creates binding-validator. Validator RESULT creates only GREEN event plus pending finalizer; another fresh finalizer creates verdict. Only an accepted owner RESULT for unchanged P/TESTABILITY/receipt and a later finalizer create EXEC; revised/split re-enters full PLAN validation and rejected stops. Each RESULT has writer-compatible `history/<date>-<session-id>.md`, exact LOG delta and full CALL/pointer in `NOW.next`. The same event -> mechanical CALL -> RESULT -> child pattern applies after setup interview/adapter acceptance, conformance and accepted RED/progress/revision/light events. Bare mutation, self-validation, double finalize, extra state delta, changed goal/done_when, collect/owner/product work before the proper finalized child or receipt first appearing with words bounces.
  - **v21 conformance bootstrap (NORMATIVE)** - install receives writer challenge, accepted adapter, canonical remote-main resolver row and proposed equivalent product-native identity, but no final receipt. Writer runs install then non-install cases including wrong remote/ref, stale local/tracking/current/wt ref, query/fetch/object failure and packet override; it proves post-install equality, persists final receipt/event and runs excluded binding smoke. Only an exact fresh-current receipt enters PLAN/EXECUTE/BUILD.
  - **v21 install return (NORMATIVE over path shorthand)** - writer verifies every create, overlay and delete pre/postcondition, every unlisted pre-existing byte, declared temp cleanup, deterministic terminal ref/HEAD/index/clean state and no undeclared path. Exact path-set equality alone bounces.
  - **install attempt/reconciliation (NORMATIVE)** - writer persists 128-bit/32-lowerhex `install-nonce`, SHA-256 of canonical-LF intent, and SHA-256 of RFC8785 ordinary-manifest JSON after platform-normalized-path then operation sorting. Each platform-normalized path is unique across the entire manifest regardless of operation: create+delete, overlay+delete, any other cross-operation reuse and case-equivalent duplicates are invalid. Lowercase `attempt-id` is SHA-256 of the exact LF frame defined in packets.md; marker/temp/finalized CALL/receipt are excluded and every marker/temp path derives from the id. Initial run requires exact preimages. Recovery requires the matching inner marker, except that an exact-empty absent-SETUP sibling temp-root may create that inner marker as the sole first-marker exception. With it, a declared row temp of any bytes may be removed/regenerated from the pinned postimage; undeclared temp or final-row third state stops. Delete is exact contract/tool retirement through same-filesystem quarantine; production/feature/acceptance-test delete stops. Marker remains protected history.
  - **Git/retry evidence (NORMATIVE)** - finalized install journals pre ref/HEAD/index tree, exact post tree, branch, deterministic commit metadata/OID, unique temp index and declared ref/index locks. An existing-repo deterministic post commit has exactly one parent equal to the pinned pre-HEAD; only absent repo-bootstrap is parentless. Wrong-parent and multi-parent commits fail. Retry may rebuild temp index/commit object, compare-and-swap exact ref and install exact terminal index; unreachable exact commit object is harmless. Only pre/post Git states and declared temps/locks are legal; stale lock removal requires prior installer gone plus exclusive-open, otherwise stop. Every execution emits separate RFC8785 `install-reconcile-observation`+SHA-256 over immutable receipt, retry session, fresh live observation, marker/row/Git/protected pre+post, action/outcome. Never recompute entry receipt/observation or refinalize CALL.
  - Absent/proven clean-Git virgin SETUP and lower/current-invalid RE-SYNC are the only install routes. Every existing target evaluates trusted install history even when its pair is syntactically complete; if history exists, disk ordinal must equal the unique recovered descendant-most/highest ordinal. Mismatch is initialized-corrupt and routes by recovered lower/equal/future; without a unique trusted-chain recovery it stops corrupt. PLAN requires current-valid entry+final; EXECUTE requires fresh pass; BUILD requires current-valid v21 resume receipt.
  - **contract-version hard boundary (ENABLED NOW)** - authority is only a fresh query/fetch of canonical remote main. Exact absent or proven clean-Git virgin admits SETUP. Lower enters RE-SYNC lower. Equal is valid only with matching accepted event/receipt and installed identities; otherwise current-invalid. Every existing target, complete pair included, is checked against its trusted history; any disk/recovered-ordinal mismatch is corrupt and routes by the unique linear descendant-most/highest trusted event. Future never downgrades and unknown/conflicting corrupt stops. Only current-valid admits feature phases; stale packets never refresh.
  - **checker input ABI (NORMATIVE)** - packet/handback live in one writer-owned canonical file. A second writer-owned authority file maps events and carries canonical remote-main resolver/query/fetch evidence, accepted adapter/corpus and proposed product-native identity. Closed env remains six packet/authority path-mode-blob values plus install challenge; no ref/env/product fallback. Writer hashes both files; missing/wrong/cross-phase input, extra env, override or mutation fails.
  - In `audit`/`digest`, derive the canonical remote-main resolver ephemerally and read-only when no finalized CALL exists; never materialize packet authority. Matching open inner marker or exact-empty absent-SETUP sibling temp-root first-marker state reports resume first. Otherwise classify absent/proven virgin -> SETUP; lower -> RE-SYNC lower; current-invalid/trusted equal -> repair; current-valid -> silent; future -> future STOP; conflicting/unknown corrupt -> corrupt STOP; unavailable -> exact unavailable STOP.
  - **builder-return is not close (engineering RESULTs)** — a product-repo RESULT, owner playtest summary, merge/push request, or prose like "formally closed on dev/dev2" is evidence input, not authority to clear `NOW.open_calls` or mark done. The writer may clear the returning engineering CALL only when the Direction-OS RESULT/checkpoint carries the required close evidence: deliverable reconciliation plus the binding fresh-session G5/review (or stricter gate) named by the CALL/state. Product gates, screenshots, owner-eye, merge, and push may support that close; they do not replace it. A CALL phrase such as "no heavy multi-agent refutation" reduces fan-out only and is not a G5 waiver. If the close evidence is absent, bounce or write a checkpoint that leaves the `open_call` pending; never improvise a "merged therefore closed" state.
  - NOW hygiene (mechanical only): if state_changes write `NOW.md`, reject when `open_calls` keeps the returning CALL id or adds text explicitly marked `returned|done|superseded|cancelled`; when `decisions` adds text explicitly marked answered/accepted/declined; or when `next` is neither a CALL packet nor a one-line pointer to a self-contained CALL artifact under `work/`. Do not judge whether a compaction is semantically good — pulse/repair/review own that.
  - **deliverable reconciliation (engineering RESULTs only)** — diff the RESULT against the CALL's OWN `done_when` (the writer holds it in `open_calls` — the independent copy the product-repo deliver gate never sees, so this is where an UNDER-transcribed or merged promise is caught even if the product coverage list was sanitized to omit it). Every `done_when` bullet of the CALL must carry a disposition in the RESULT: a named evidence line, or an out-of-scope line with an owner-ack token (a plan-approval signature or escalation id). A bullet naming a built artifact (a scene/prefab/composer/adapter) needs an EXISTENCE evidence line citing a path or recorded run-artifact — not a test name (so this stays mechanical line-presence, never a judgment call on "related vs unrelated test"). A missing bullet, a build-bullet discharged only by "owner may look later" or by a headless test in place of an artifact, or a cut with no owner-ack = conflict → bounce with the specific bullet, not applied. Two FURTHER bounce cases, same mechanical line-presence check (never a judgment call): (a) **approval-claim** — any RESULT text asserting the owner approved a deviation, substitution, or cut (e.g. a bare `(owner-approved)`) that does NOT resolve to a real `owner-ack:<signature|escalation-id>` token elsewhere in the RESULT is the fabricated-approval class (same anti-fabrication principle as the `(owner)` play_check words check above) → bounce; (b) **approach-substitution** — when the CALL's `done_when` names the ONE approach/substrate the leg exists to prove (CONTOUR §Boundary) and the RESULT closes that bullet with a built artifact whose recorded `approach:` token differs from the named one, that is a self-authored substitution needing the same owner-ack token to stand → otherwise bounce. Return-path mirror of the dispatch-side contract-drift guard below: read the leg's promises, compare, surface loudly, never a silent pass.
  - **review-evidence (engineering RESULTs only, ENABLED NOW — reaches every repo before its §Re-sync)** — when the returning CALL's `done_when` declares a frozen-spec / strong-check change or names an `openspec/changes/<id>` change-id (the signal the writer already holds in `open_calls` — never re-derived from the product repo), the RESULT must carry a **review-evidence line**: a reference to `docs/reviews/review-<id>.md` with its finding dispositions and refutation markers present. Bounce when: no review-evidence reference is present; an **in-scope** finding is left `open` (or an in-scope row missing its disposition), or a **class-sibling** row missing its `routed <ref>` pointer or its `site` column (a routed-not-fixed class-sibling does NOT bounce the leg; the diff-membership anti-dodge — a class-sibling's `site` must be OUTSIDE the diff — is product-side, INERT until §Re-sync as with the reviewed-commit ancestry, so the writer carries only `site`+`scope`+`routed` line-presence, VALIDATION §Review-evidence (iii)); a `refuted`/false-positive disposition carries no refutation-verification marker (in-session G5, binding fresh-session KERNEL-G5, or a `register:<row-id>` citation — VALIDATION §Refuted-register); or the review-evidence reports ≥1 `refuted` disposition while carrying no `refuted-register:` line (the v13 register append — `none` is legal only alongside zero refuted rows); or the RESULT reports done while naming `rounds` >3 whose `escalation:<id>` does NOT resolve to a real escalation id elsewhere in the RESULT/history (same resolution as the `owner-ack` token above — a hollow escalation is the fabricated-approval class). A CALL whose `done_when` declares a light change (no frozen/openspec change) carries an explicit `review: n/a — light change` line instead; its ABSENCE on a frozen-declared change is itself the bounce. Mechanical line-presence against the CALL's own declaration — never a judgment on review quality or true independence (same bound as the deliverable-reconciliation and approval-claim checks above); the product `-Deliver` check adds the `reviewed-commit` ancestry/accounting freshness once the repo §Re-syncs, this is the writer-side teeth until then.
  - **fix-class-closure (engineering RESULTs only, ENABLED NOW — reaches every repo and every leg, frozen-declared or NOT)** — when the RESULT reports review findings fixed (any `fixed <commit>` disposition, or a review-repairs/"findings addressed" line), it must carry, PER fixed finding, its **class+sweep record**: the `invariant/class` token plus a `sweep:` line (sibling sites each closed|n/a — an out-of-diff sibling closes `routed <ref>`, a class-sibling per VALIDATION §Review-evidence, never an in-leg fix; for a gate/scan/oracle false-green finding, a seeded-miss/negative-control token per class variant) — inline in the RESULT or via the `docs/reviews/review-<id>.md` rows it references (VALIDATION §Fix-class-closure, contract v14). A fixed-finding report with no class+sweep record = bounce. This deliberately keys on the RESULT's own content, NOT on the CALL declaring a frozen change — the witnessed recurrence (dev_2 c-visual-004: `//` fixed, `/* */` + commented-include + raw-text found next round) ran on a leg with no openspec folder, where the review-evidence bullet above never fires. Mechanical line-presence; the writer never judges sweep quality or completeness (the reopen tripwire and the next review round own that).
  - task-play lifecycle: for `play: work|guide`, reject `TREE.md` edits, active_bet removal/done/retargeting, or activation of another node. If the RESULT marks the last open/active task of the current active bet done, `next` must be a `review` CALL for that same node. Review closes bets and harvests TREE; repair may reconstruct contradictory state under its own play.
  - converge Definition-of-Ready (shape activation): reject a `play: shape` RESULT that makes a node `active` (node->active, NOW gains the bet) unless that node is converge-ready — either the direction's `history/` holds a `converge-verify` RESULT for it (a clean set handed to shape), or the RESULT's `play_check` carries an explicit `triage: <type> — converge OFF — because <…>` line. Neither present → bounce with the miss (routes back to converge). This is the "shape's Definition-of-Ready" `os/plays/converge.md` names; it binds however the node reached shape (map/review/owner). Mechanical: file-presence + line-presence, no judgment — the owner-visible `because` reason is where a wrong "trivial" triage is caught, not here.
  A bounced RESULT routes back to its session (or repair) — same path as an irreconcilable logical collision after rebase.
- Also emits the `next` CALL back to the owner/orchestrator if one is present.
- Record CALLs issued in state_changes into `NOW.md → open_calls`; clear an entry when its RESULT arrives. If the same returning id remains in `open_calls`, bounce as a mechanical contradiction.
- **Owner-panel regeneration (direction-defined).** If the direction declares an owner panel — render-only view file(s) whose location and regeneration rules live in that direction's `knowledge/` (e.g. a lanes/venues map naming `work/board/dashboard.html`) — then any apply that changes `open_calls`, `tasks`, `decisions`, `next`, or other state the panel's live sections mirror ALSO regenerates those live sections per that knowledge entry, in the same commit. The panel is a render over NOW+git: regenerating it never alters state meaning and needs no judgment; format and cleanliness rules (plain-language explanation per ID, closed items with their outcome, journal window, no stale sections) come from the knowledge entry, not from here. No declared panel → skip silently. A declared panel whose rules are unreadable this apply → apply the RESULT anyway and say the panel was NOT regenerated in the reply — never a silent skip.
- `collect` command: on "collect next for <direction>" (or CALL id), emit one SESSION_PAYLOAD/play/NOW/CALL block. Preserve real NOW and warn on hygiene drift. **Engineering guard:** filesystem comes only from absolute `worktree`; authority comes only from canonical remote main query/fetch, using the persisted finalized row when present. Matching open inner marker or the exact-empty absent-SETUP sibling temp-root first-marker exception resumes first. Authority-advanced completion yields `MANIFEST-RECONCILED`, consumes the old call and freshly routes without receipts. Otherwise absent/proven clean-Git virgin -> SETUP; recovered lower -> RE-SYNC lower; recovered equal/current-invalid -> current-invalid; current-valid -> feature; recovered future/conflicting-corrupt/unavailable -> exact STOP. Every existing complete pair is first compared with its trusted recovered ordinal. Stale feature receipts never refresh. Non-engineering CALLs skip this guard.
- `audit` command: on "audit <direction>" - read-only consistency sweep of state/trailers/types/schema/cross-references/G1-G4. Product checks derive canonical remote-main resolver ephemerally if no finalized CALL and never persist it. Report install resume first; otherwise use the same virgin/lower/current-invalid/current-valid/future/corrupt/unavailable matrix and trusted-chain rule. Audit fixes nothing: state drift -> repair CALL, lower/current-invalid -> RE-SYNC trigger, virgin -> SETUP, future/corrupt/unavailable -> exact blocker, rule defect -> MAINTENANCE REQUEST. Flow health stays pulse's job.
- `digest` command: on "digest" (optionally direction/since date) - read-only morning report. Derive canonical remote-main authority ephemerally where no finalized CALL exists. Contract status is silent when current-valid; report `INSTALL RESUME OWED` only for a matching inner marker or the exact-empty absent-SETUP sibling temp-root first-marker exception, else SETUP, RE-SYNC lower/current-invalid, FUTURE STOP, CONTRACT STATE CORRUPT or exact unavailable STOP. End with windowed OS commits, one screen; never mutate or materialize authority.

**The writer is ephemeral — never a standing chat.** Every RESULT is self-contained, so the writer needs no memory; a long-lived writer session is accumulated noise and cost (one chat = one job applies to the writer too). Manual loop: open a fresh agent session → paste the RESULT → it applies, commits, hands back the next CALL → close it. Headless one-shot invocations (`claude -p` / `codex exec`) do the same without a chat at all; an orchestrator replaces the hop entirely at autonomy stage 2+.

**Concurrency.** Parallel directions: one git worktree per direction, writers of different directions run in parallel; within one direction RESULTs apply one at a time. A queued RESULT may therefore have stale bases and is semantically rebased when its turn arrives. Sync/push protocol and rules: `os/adapters/worktrees.md`.

For `collect`, `audit` and `digest`, this phase guard is normative. Freshly query/fetch canonical remote main; never use current/wt/local-main/tracking/CALL/env/product fallback. A finalized CALL keeps its resolver/receipt/entry observation immutable; audit/digest without one derive an ephemeral read-only resolver. Matching open inner marker or the exact-empty absent-SETUP sibling temp-root first-marker exception resumes before matrix. Reconcile uses declared row/Git states plus separate observation; `MANIFEST-RECONCILED` consumes an old-authority call before fresh routing. Otherwise use the exact matrix and linear trusted-event rule above, including disk/recovered-ordinal equality for every existing complete pair. Collect runs only finalized CALL, SETUP requires absent repo-bootstrap or proven clean-Git virgin, and RE-SYNC requires exact entry reason/base/events/manifest. Any mismatch, third state, nonconformant checker, nonzero exit or mutation emits no product payload.

An event-dependent child is finalized only by a persisted normal mechanical CALL and a fresh session RESULT. Role 1 validates that RESULT like any other: declared state_changes only, history + LOG + trailers + owner-panel regeneration, one atomic writer commit. The only legal semantic delta is removal of that finalization CALL and creation of the exact child with enumerated writer fields resolved from the strict-ancestor event. It never runs collect, owner dialogue or product work. Missing/stale intent, wrong id, stable-field edit, extra state change, second application or missing history/log bounces.

For any child collect, the finalized CALL must cite a real `history/<path>@<writer-commit>` event present in current Direction history; the event commit must be a strict ancestor of open_call commit and bind exact payload. Collect never accepts a receipt/timestamp supplied only by the child session.

## Role 2: executor (kind: engineering)

Receives a business task CALL whose `repo` is a logical product identity and whose absolute `worktree` is the sole filesystem/execution target. The full engineering process - plan gate, autonomous build, validation gates, retry/escalation, final report - is defined in `os/engineering/CONTOUR.md`; repo bootstrap in `os/engineering/PROJECT_SETUP.md`. The contract summary:

Contract:
- Execute only `packet-stage: finalized`. Before product write, freshly query/fetch canonical remote main and compare the writer row; current/wt/local-main/tracking/packet/product sources never authorize. Observe install recovery first: matching inner marker, or the exact-empty absent-SETUP sibling temp-root first-marker exception, runs only pinned row/Git journal and emits a separate reconcile observation while preserving original CALL receipt/entry observation. Authority-advanced terminal proof returns `MANIFEST-RECONCILED`, consumes that call and freshly routes without current/feature receipt. Otherwise absent/proven clean-Git virgin admits SETUP; recovered lower/current-invalid admits matching RE-SYNC; only current-valid admits feature; recovered future/conflicting-or-unknown corrupt stops. Every existing complete pair is compared with trusted recovered ordinal before routing. Author/review are read-only; install alone writes and proves product-native resolver equality. Stale packets never adopt receipts.
- The CALL gives goal / context / boundaries / done_when / return / budget. The agent owns everything else: design, implementation, refactoring, branch/PR mechanics.
- Repo-internal conventions (build, test, style, architecture rules) live in that repo's AGENTS.md / CLAUDE.md — command-first, written once per repo. The OS never duplicates them.
- **Execution authority is repo-local.** The CALL + product-repo `AGENTS.md`, frozen plan/spec, and `validation.config` define the workflow and its required tools. The independently resolved Direction-OS contract attestation above decides only whether this CALL is current enough to run; it cannot add product requirements or tools. A globally installed/discoverable skill or tool is support, not authority: it cannot replace the workflow or add project requirements. The required-tool STOP below applies only after one of those repo authorities explicitly requires the tool; discovery or installation alone is never a blocker.
- The installed root AGENTS governs after bootstrap; pre-install authority is canonical remote main, not product bytes. Exact absent or proven clean initialized non-bare Git virgin admits SETUP; non-git/unborn/dirty existing target stops. Every existing target, syntactically complete pair included, enumerates the unique linear descendant-most/highest trusted event or stops; when history exists, disk ordinal must equal it, and mismatch routes corrupt by recovered lower/equal/future. RE-SYNC admits lower/current-invalid, future never enters, and feature phases require current-valid. Install alone mutates declared contract/tool rows and journaled Git state; production/feature/acceptance-test deletion is forbidden.
- **At every boundary**, derive trust/typed rows independently before product code. PLAN renders/builds/discovers each no-implementation skeleton against external base; an absent new symbol must use exact base-compilable reflection/dynamic route without production stub. Before approval persist candidate/plan receipt, then obtain owner verdict. Fresh EXECUTE preserves pre-red-head. Test-author R has exact parent and test/oracle-only delta, unique discovery/filter and expected runtime RED; any production/mixed/unmanifested commit stops before builder. Later revisions follow the same exact-parent rule. Resume/deliver/close verify history and all receipts.
- Exit evidence carries immutable finalized CALL receipt/entry-observation hashes; canonical remote-main query/fetch evidence; stable authority and product return; PLAN/run/RED/progress/result identities. Each install execution adds its canonical `install-reconcile-observation`+hash with session, live, marker, create/overlay/delete, Git and protected pre/post/action/outcome; writer verifies it against pinned entry bytes but never recomputes those bytes as current or refinalizes CALL. SETUP/RE-SYNC also carries adapter/resolver equality, executable attempt preimage, Git terminal proof, repo-bootstrap publish evidence and trusted inventories. Product result never self-hashes.
- "Done" is the CALL's done_when verified by a runnable check, not the agent's assertion. A task that can't carry a runnable check should say how it is to be verified instead — at CALL time, not after.
- Blockers surface early: if the task looks infeasible or 2x over budget, stop and report — a fast "blocked, because" is a good result; days of silent struggle are not. A required tool/harness being UNAVAILABLE (engine Editor not running, a needed MCP disconnected, headless-test license absent) is a blocker: STOP and tell the owner which tool to launch — never a workaround or crutch around it; any exception stands only on the owner's explicit written ack (CONTOUR ESCALATE).

- For a frozen/OpenSpec execution, `TESTABILITY.md` is part of the frozen packet; neither the RED author nor builder may edit it.
- `TESTABILITY.md` contains routes, not planner-authored GREEN or validator probe source. GREEN is the separate ancestral binding-validator event; the fresh acceptance RED author cannot reuse scratch probe bytes.

## Sizing rule

One executor CALL ≤ a focused half-day of human-equivalent work. Bigger means the task needed splitting at shape time — send it back as such.

## Running thinking sessions here too

Any OS play can run in an agentic CLI session instead of a chat app: the agent reads `os/KERNEL.md`, the play file, and `live/<dir>/NOW.md` itself, and can act as its own writer for state changes (one relay hop instead of two, or zero with an orchestrator). Recommended default for shape / review / pulse / repair, which are state-heavy; chat apps stay attractive for frame and long work dialogs.

END_OF_FILE: os/adapters/coding-agent.md
