# os/engineering contract version — propagation guard

This monotonic integer names the current engineering contract:
`CONTOUR.md`, `VALIDATION.md`, `PROJECT_SETUP.md`, `ESCAPE-CLASSES.md`,
`OBLIGATION_INVENTORY.md`, and `profiles/**`. A load-bearing change or a new escape class bumps it
once. Product repositories record `validation.config: synced_contract_version`; `collect`, `audit`,
and `digest` compare that stamp with `current` and surface a missing terminal Re-sync.

current: 36

## Reading the map

Each row's Git locator resolves to the commit that introduced or completed that version; `git show
<locator>` retrieves the delta, and `git log <previous-locator>..<locator> -- os/engineering` covers a
version assembled by follow-ups (v20, v21). The former long narrative remains Git evidence at
`e0c4912d73aeb00beedbab8dbf76f2d41aa620c7:os/engineering/CONTRACT_VERSION`.

| Version | Date | Change / terminal Re-sync delta | Git locator |
|---:|---|---|---|
Versions 1-25 are retired narrative: each resolves through `git log -- os/engineering` and
`git show <locator>`. Their rules live in the current text above, not in this map.

| 26 | 2026-07-16 | B4 proves the old kill-list consumer-by-consumer, removes no gate/obligation ID, compacts Git-backed history, and lowers mandatory reading to 25,726 words. | `engineering-contract-v26^{commit}` |
| 27 | 2026-07-16 | Model-neutral review independence; removed: X14(v26); added: X14(v27); N remains 239. | `engineering-contract-v27^{commit}` |
| 28 | 2026-07-17 | Every engineering stage returns its current HOME handback; Direction alone issues successors. Removed: X41(v27); added: X41(v28); N remains 239. | `engineering-contract-v28^{commit}` |
| 29 | 2026-07-18 | Hot-safe CALL pinning plus one pair-candidate job replace retroactive upgrades and early carrier relay/line cap. Removed: X33a/X33b(v28); added: X33a/X33b(v29); reworded: G33, X07-X09, X18, X34a-X34b, X75-X76, E06-E07, E09-E12, E15, U20a-U20b; N=239. | `engineering-contract-v29^{commit}` |
| 30 | 2026-07-18 | One pinned root replaces Direction relays between fresh product stages. Removed: X41(v29); added: X41(v30); reworded: X33a-X33b, X44, E29; N=239. | `engineering-contract-v30^{commit}` |
| 31 | 2026-07-20 | Lifecycle-safe, exact-input review/venue evidence reuse plus permanent slot branches. Removed: G08, G21a, G43, X22, X38, X41(v30), X43a-X43c, X48(v30), E20(v30), U13(v30); added: X41a-X41g, X48a-X48b, X53a, E20(v31), U13(v31); reworded: E29; N=239. | `engineering-contract-v31^{commit}` |
| 32 | 2026-07-30 | Non-released frozen packages exact-match current HEAD before scope exemptions. Reworded: X18; N=239. | `engineering-contract-v32^{commit}` |
| 33 | 2026-07-30 | Asserted plan derivations discharge before dependent tests; one decision-page ruler; two-way control pairing; composed-gate exemption seeded miss; committed-ref review. Reworded: G21b, G25, X13a, X30b, X58; N=239. | `engineering-contract-v33^{commit}` |
| 34 | 2026-07-30 | Frozen replans use clean replacements; stage-local gates rerun only affected proofs. Reworded: G11, X18, X33b, X34b, X41a, X41d-X41f, X48a, X78, E29; N=239. | `engineering-contract-v34^{commit}` |
| 35 | 2026-07-31 | The frozen contract is code, not prose: no frozen `PLAN.md`, PLAN shares a session with PAIR-CANDIDATE, CALLs cap at three `done_when` bullets. Removed: X06, X11, X12, X77, E01; added: none; N=234. | `engineering-contract-v35^{commit}` |

| 36 | 2026-08-02 | PROBA is the default mode and OPORA is entered only by the owner's word; no hand-ticked ledger gates completion; adding a test beside a frozen one is legal and a spec's test list is a minimum. Removed: none; added: none (the mode exempts, it does not add); reworded: G0, X16, X18, X34b, CONTOUR#2; N=234. | `engineering-contract-v36^{commit}` |

## Terminal Re-sync to v36

- Install prior deltas; `re-sync:36` stamps 36 and degates `tasks.md` everywhere: completion comes
  from the receipt chain plus verified evidence, never from a hand-ticked checkbox.
- New legs default to PROBA. Only the owner's explicit word enters OPORA. Every ALREADY-issued root
  keeps its exact feature pin, frozen artifacts, stage, receipts and route — v36 reinterprets no
  legacy or pre-v36 lineage, and weakens nothing on the OPORA side.
- Surviving surfaces: all `live/**` direction state, every issued root receipt/manifest chain, and
  every frozen blob stay valid as written. This change only relaxes; it makes no existing state
  illegal and needs no repair route.
- Adding a new file beside a frozen one is legal in both modes; only a frozen blob's BYTES route
  through replacement.

- Preserve ref/receipts/manifests; carry exact-input evidence from the affected stage; exclude
  superseded files. New OPORA roots still cap the CALL at three `done_when` bullets. N=234.
- Unstamped repos use current `PROJECT_SETUP.md`.

END_OF_FILE: os/engineering/CONTRACT_VERSION
