RESULT s-life-reset-shape-run-v1-001 (call: c-life-reset-shape-run-v1-001)
direction: life-reset   play: shape   node/task: g-lr-run

outcome: |
  g-lr-run is shaped into the active v1 bet (owner-approved, G9: "да"). v1 is ONE tiny
  composed pulse on REAL owner data — NOT an engine/board/DAG. Two files + one real run:
  (1) a sealed-core file and (2) a week-file; then the owner runs ONE real week → day →
  review on live data. The two load-bearing gaps the research surfaced were resolved
  CO-CREATIVELY with the owner inside this shape:
  - recovery-vs-slide: distinguished NOT by activity but by THREE signs (named/sanctioned ·
    floors-still-stand · bounded-with-return). Anti-gaming without punishment: the owner may
    CALL anything recovery (asymmetry, default = treat-as-recovery, never punished), but the
    owner-authored floor tripwires watch the FLOORS, not the label — a fallen floor fires the
    tripwire as a PROTECT episode (reach-in / name the falling pillar), not a verdict.
  - integrate (present-minimal): the manager only READS a short neighbor summary (health,
    game); thin/missing → owner tops up in words; NEVER ingests raw, NEVER touches a
    neighbor's internal process (owner restated this himself); a stream conflict surfaces as
    an explicit line + a route. No tiny-probe fallback needed — both resolved in-session.
  Four owner floors locked (sleep · not-smoking-again · no binge-eating · not-vanishing/
  zoning-out-for-days). A 5th floor (training/practice discipline) is captured as a named
  future floor to add VIA THE GATE once a training process exists — the manager's designed
  first real mutation, not built now (keeps v1 at 4 floors, light).

evidence: |
  Shape produced a G6-valid bet: appetite + kill_by set; ≥1 real cut (the whole deferred-
  upgrade backlog); a lens-sweep verdict per all 9 charter lenses; a riskiest-assumption task
  (t-1) testing recovery-vs-slide AND whether the owner's tripwires fire on a REAL lapse.
  - Appetite (owner, R1): "вообще три поставим… на большую не так пойдёт… если что-нибудь
    надо больше — заменять" → ~3 focused hours to author the 2 v1 files; the run is
    owner-paced, NOT in the build budget; not extended (over-appetite → re-shape smaller).
  - Run model (owner): "Всё, что определили, всё делаем. Делаем до конца… без условных
    перебивок… остановимся и проверяем" → build to the end without mid-stream rebuilds;
    evaluate only at the checkpoint (folds invariant R12: a week is a bounded procedure, not
    re-opened mid-stream).
  - kill_by (owner, R4): "Давай на две недели, ещё 4 недели" → "пускай будет R4": soft 2 wk
    (pause & run), hard 4 wk (kill → re-shape a smaller run). Metric = real runs on live data.
  - Floors (owner, R2): "давай возьмём эти опоры" (the 4 proposed); discipline → "скорей
    всего будут меняться, когда сделаю тренировочный процесс… можно её и сейчас добавить" →
    deferred to a gated future mutation, captured.
  - Integrate (owner, R3): "мы менеджментом работаем, не управляем… источник правды там
    лежит… ни в коем случае не вмешиваемся… только читаем… данных нет — не выдумываем".
  - Approval (owner, R-final, G9): "да".
  All 10 research required-fixes folded as bet conditions (see tasks); the 3 reconciliations
  applied (ONE header layout; Q9 non-punishing-return hosted in the sealed core, not the
  override path; any future "earned" rewrite still passes Q4's invariant replay — captured,
  ladder itself deferred). In-session self-check confirmed no required-fix leaked, the cut is
  real, the lens sweep is complete, and the bet preserves the one-real-run gate + G1–G4 +
  stays-light. This is an in-session pre-pass, NOT the binding G5 (a fresh refutation session
  closes the run later).

state_changes: |
  TREE.md:
    - owner_approved.evidence += 'Owner approved (G9) shaping g-lr-run into the active v1 bet
      (appetite 3h, kill_by 2+4, 4 floors), 2026-06-21 s-life-reset-shape-run-v1-001: "да".'
    - root.children g-lr-run: status parked → active; + appetite; + kill_by (metric/threshold/
      soft 2026-07-05/hard 2026-07-19/owner-paced note); + bet pointer to this history file.
      (goal/done_when/why unchanged — no tree re-open; other 5 nodes stay parked.)
    - tree_validity.note updated: research done; g-lr-run shaped → active as the v1 bet;
      other 5 nodes parked; full bet in NOW.md + this shape history.
  NOW.md:
    - active_bet: status none → active; the v1 bet (node g-lr-run, goal, appetite, kill_by,
      done_when, forecast/against, the resolved gaps + 4 floors + folded required-fixes).
    - tasks: [] → t-1..t-5 (t-1 active, rest pending; ≤3 active honored, G1).
    - preserved_evidence += history/2026-06-21-s-life-reset-shape-run-v1-001.md
    - open_calls: [] (the t-1 CALL is `next`, not yet in-flight); decisions: [].
    - next: CALL c-life-reset-run-v1-t1-recovery-slide-001 (work on t-1).
  LOG.md: appended the 2026-06-21 shape line.
  history/: this file.

captures:
  - FUTURE FLOOR (gated mutation, not v1): "training/practice discipline — don't skip
    committed sessions", to be added to the sealed core VIA THE GATE once a real training
    process exists; reads from the health summary. Owner: "можно её и сейчас добавить" but
    chose to keep v1 at 4 observable floors. This is the manager's intended first real
    self-mutation (a live test for g-lr-learn).
  - RECONCILIATION (forward note for g-lr-learn): when the earned/demotable autonomy ladder
    is eventually built, an "act-unless-vetoed" (R2-rung) self-rewrite STILL passes Q4's
    post-rewrite invariant replay — no rung bypasses the gate. Ladder itself deferred (not v1).
  - DEFERRED-UPGRADE BACKLOG (the cut list, → g-lr-grow; evidence-earned, named not built):
    typed-commitment DAG / Kanban board (Q1); N-of-1 evaluate-and-swap loop (Q2); baseline-
    relative surge-governor / ACWR (Q3 → g-lr-protect); automated post-rewrite gate w/
    auto-revert (Q4); process-trace proxy "unreliable-narrator" detector (Q5); track-record
    ledger / per-domain trust accounts (Q6); two-key (subjective OR behavioral) hollow-veto
    (Q7); dormancy auto-prune of rules + hard WIP cap (Q8); gap-length tiered re-entry (Q9);
    fresh adversarial "try-to-fake-it" self-cert session (Q10); routed-request-to-source for
    neighbor summaries (integrate; v1 = read + owner top-up only).
  - UNDER-BUILT pieces sized minimally in v1, fuller versions deferred: day-derivation drift/
    rebuild + adaptive intensity (v1 = MIT-style top-down pull only); the review's analytic
    "find the weak link" + full {hold|mutate|kill|route|research|simplify} procedure (v1 =
    default {hold}, ≥1 decision); the non-caving WEIGHING POLICY ("достаточный довод") still
    unspecified; full self-rewrite engine = g-lr-learn (v1 authors the sealed core only, runs
    no rewrite).

decisions_needed: []   # all batched decisions resolved in-session (appetite, both gaps, floors, kill_by, approval).

play_check:
  - "1 Recite: done — restated g-lr-run goal/done_when + parent (manager-as-spine root) in plain words; goal is already an outcome (a running rhythm), no activity→outcome fix needed."
  - "2 Appetite first (owner): done — appetite fixed BEFORE tasks at ~3 focused hours to author the 2 v1 files, run owner-paced/uncounted, not extended (owner: 'вообще три поставим… на большую не так пойдёт… если что-то надо больше — заменять'); outside-view comparables cited (map=1 session, research=1 pass, a week-template prototype already exists from the prior dry-run)."
  - "3 Approaches, then minimal solution: done — the 3 structural approaches were established by the research (A flat position-ordered 4-layer list + one displaces line / B typed-commitment DAG / C Kanban lane-board); chose A + inherit the Direction-OS substrate (lightest, no new infra) with a one-line why-over-the-others; losers (DAG/board) → cut list. Sketched the smallest version (2 files + one run) and stated explicitly what it does NOT include (§ cut list)."
  - "4 Scope hammer: done — cut until it hurt; ≥1 real cut recorded (the entire deferred-upgrade backlog: DAG/board, swap-loop, surge-governor, auto-gate+auto-revert, trace-detector, ledger/accounts, two-key veto, auto-prune, gap-ladder, adversarial self-cert, routed-request) → captures / g-lr-grow, not into the bet."
  - "5 Lens sweep: done — verdict per all 9 charter lenses (below in NOW bet): weekly-execution/daily-discipline/cross-direction-integration/safety = tasks; improvement&learning = task (review→decision; self-rewrite deferred to g-lr-learn); system-quality = enforced via appetite-cap + kill_by + no-mid-stream-churn; spirit-and-transcendence / inner-work-and-disclosure / fatherhood-and-example = not_needed in v1 (terminal values/programs, not first-run mechanism)."
  - "6 Riskiest assumption (owner-personal): done — top assumption = the manager can distinguish recovery from slide on real data AND the owner-authored floor tripwires actually fire on a real lapse (unreliable-narrator week); gets the earliest cheapest test (t-1, a back-test on the owner's most recent REAL lapse — past data, not waiting for a future one). Calibrated to risk_posture (asymmetric rigor: never punish, catch early)."
  - "7 Tasks: done — 5 tasks, each ≤ half a focused day, verifiable done_when, ordered with the riskiest first (t-1); kinds marked (session / executor / owner-run); ≤3 active at once (only t-1 active now, G1)."
  - "8 Kill criteria (owner): done — kill_by metric=real runs on live data; soft 2 wk (2026-07-05, pause & run) / hard 4 wk (2026-07-19, kill→re-shape smaller); next_if_true (run closes the gate → next node opens / review unlocks g-lr-grow), next_if_false (no run by hard date → kill, smaller run / tiny-probe). Owner: 'Давай на две недели, ещё 4 недели' → 'пускай будет R4'."
  - "9 Close (owner): done — full bet shown on one screen; owner approved (G9) 'да'; node→active, NOW gets the bet, next = work CALL on t-1."

log: "shape: owner approved (G9, 'да') g-lr-run as the active v1 bet — one tiny real composed pulse (2 files + one real run on live data), appetite ~3h (run owner-paced, not extended), kill_by 2wk soft/4wk hard. Both load-bearing gaps resolved co-creatively: recovery-vs-slide = 3 signs (named/floors-stand/bounded) with tripwires watching floors-not-label (anti-gaming, non-punitive); integrate = read-only neighbor summaries + owner top-up + conflict→route, no raw. 4 owner floors locked (sleep/no-smoking/no-binge/no-vanishing); discipline floor captured for a gated future mutation. All 10 research required-fixes folded; cut list = the deferred-upgrade backlog; 9 lenses swept; riskiest-assumption task t-1 (recovery-vs-slide + tripwire back-test on a real past lapse). next c-life-reset-run-v1-t1-recovery-slide-001."

next: |
  CALL c-life-reset-run-v1-t1-recovery-slide-001
  to: session
  direction: life-reset
  play: work
  node: g-lr-run
  task: t-1
  goal: |
    Lock the recovery-vs-slide rule + the 4 owner floor-tripwires, and back-test them on the
    owner's most recent REAL lapse — proving the manager can tell protected recovery from a
    slide, and that the owner-authored tripwires would have fired on a real lapse (not a
    paper simulation). This is the bet's riskiest-assumption test and the input to the
    sealed-core file (t-2).
  context: |
    - live/life-reset/NOW.md (the v1 bet: the recovery-vs-slide 3 signs + the 4 floors + the
      anti-gaming "tripwires watch floors, not the label" design)
    - live/life-reset/work/life-reset-implementation-research-v1.md (Q5 collapse-grip + Q7
      recovery carve-out + Q9 non-punishing return — the protect mechanics)
    - live/life-reset/CHARTER.md (g3 recovery protected first-class; risk_posture; the
      documented crash loop: фиксация → опоры падают → обжорство/курение/залипание → откат)
    - live/life-reset/history/2026-06-21-s-life-reset-shape-run-v1-001.md (this bet)
  boundaries: |
    Co-create with the owner (his real past lapse, his words). Do NOT build the sealed-core
    file yet (that's t-2) and do NOT auto-surveil — tripwires are ONLY the owner's
    pre-committed observable floors checked against the rhythm's own trace. REAL lapse, no
    simulation. Keep the asymmetry: default = treat-as-recovery, never punish; a fired
    tripwire is a PROTECT episode (reach-in / name the falling pillar), not a verdict. No
    surfaced numbers (G2). The 4 floors are: sleep (not broken 2 nights running), not smoking
    again, no binge-eating, not vanishing/zoning-out for days. Stay light.
  done_when: |
    A work RESULT with: (a) the recovery-vs-slide rule written as the 3 observable signs +
    the anti-gaming clause (tripwire watches the floor, not the label); (b) the 4 floors each
    written as an observable tripwire ("if X → reach-in"); (c) a back-test narrative against
    ONE real recent lapse showing whether each tripwire would have fired and whether a genuine
    recovery would have CLEARED as protected (the recovery carve-out); (d) a one-line verdict:
    does the distinction hold on real data? If it does NOT hold → that is a real finding,
    route to a fix before t-2. next = t-2 (author the sealed-core file from this).
  return: RESULT with the rule, the 4 tripwires, the back-test, the verdict, next = t-2 CALL.
  budget: one work session (co-creation with the owner).

---

Full v1 bet (tasks t-1..t-5, kill_by, cut list, lens sweep): live/life-reset/NOW.md
Research product (the 10 mechanisms + required-fixes + thin slice): live/life-reset/work/life-reset-implementation-research-v1.md

END_OF_FILE: live/life-reset/history/2026-06-21-s-life-reset-shape-run-v1-001.md
