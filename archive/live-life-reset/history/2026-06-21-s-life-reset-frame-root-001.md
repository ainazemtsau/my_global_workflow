RESULT s-life-reset-frame-root-001 (call: c-life-reset-frame-manager-root-001)
direction: life-reset   play: frame   node/task: g-life-reset-root

outcome: |
  The life-reset root is re-framed and owner-approved. CHARTER.md now encodes the
  real scope: ONE strict, self-improving personal operating MANAGER (weekly +
  daily rhythm) as the SPINE into which directions + programs + backlog plug; the
  week is the manager's PULSE, not a contract artifact. Approved root artifacts,
  one at a time (G9):
    1. Mission (manager-as-spine; efficiency = method, not terminal value;
       strictness asymmetric — genuine recovery is a protected input).
    2. Success criteria — 4 core (one-real-run gate; owner-acceptance-after-use;
       consciously-composed week; provider portability). Three others (managed-
       extensions, process-incubation, self-improvement-fires) routed to the tree.
    3. Strict-personality principles (5): non-caving/efficiency-as-method;
       hard-on-wishes; aggressive-critical-default ADDITIVE over useful-criticism;
       stable-midweek-plan; rest-synergy-with-protected-real-recovery.
    4. Self-definition + self-improvement, gated: rule self-rewrite only via an
       explicit owner-review gate; owner override path inviolable.
    5. Guardrails (4) + operating invariants (R11 day-start review gate, R12
       bounded week / day = separate chat, R22 one-task-one-chat).
    6. Pre-mortem: 10 kept, 2 edited (#7 criticism, #10 architecture-without-run),
       3 added (burnout-loop; self-rewrite drift/lock-in; bureaucracy).
    7. Root node + lenses (9 kept; safety / system-quality / improvement extended).

  Two owner corrections shaped the result:
    - R1 efficiency refinement: efficiency is effective achievement of committed
      goals across plans, NOT throughput-on-one-task and NOT judging a chosen goal
      as "unproductive". Folded into mission + principle non-caving-manager.
    - Owner added R32 (technique-selection intake: first run picks working
      techniques fitting the owner — tasks first, then techniques; pomodoro/time-
      slots/kanban are examples, chosen by research, adaptive per week).

  Owner rejected a completion gate tied to "a lived week" or "N cycles": living
  real weeks is the manager's ONGOING WORK, not the build-acceptance gate. The
  root done_when (and criterion ①) were reshaped to ONE real end-to-end run on
  real inputs (form = test / compressed dry-run / one genuinely lived day, owner's
  choice), proven by four real durable artifacts (week / day / review / one system
  decision) surviving a chat restart + explicit owner acceptance it genuinely
  worked. Anti-paper-done clause kept ("design exists" does not count); calendar
  week / N cycles explicitly NOT required.

  TREE is now root-only: the old six nodes were removed (built on the invalid
  "weekly operating graph" framing); their substance is preserved in the vision
  capture (§2) and git history for the next map to reuse.

evidence: |
  Owner approvals in-session (key words):
  - Mission: "Да, утверждаем миссию как есть, всё принимаю".
  - R1 refinement: "это в общем эффективность по различным планам ... не то, что
    эффективность в преданности какой-то одной прям задаче ... ты правильно понял".
  - Criteria: "Совсем согласен, кроме защищённой недели" → ③ reworded (priority-
    order, not minimal week; build fast in 2-3 days) → "да"; owner-paced build
    "не ждём неделю ... за пару-тройку дней всё сделать" → reshaped → "да";
    further "всё делаем ... я там буду решать, когда ... будет получаться" →
    owner-paced brake + light tripwire → "да".
  - Principles: "со всем согласен".
  - R32 + self-improvement (artifact 4): "да".
  - Guardrails + invariants (artifact 5): "да".
  - Pre-mortem (artifact 6): "да".
  - Completion gate: "мы не можем залочиться ... на то, что там неделя пройдёт ...
    тест какой-то прогнать или симуляцию ... но не неделя" → reshaped one-real-run
    done_when → "да".
  Homework: two background workflows — (1) requirement-coverage map over all
  R1-R31 + 3 guardrails + adversarial tension audit + draft + adversarial gate-
  check (caught a G7 mission-fork gap, dropped R12/R11/R22, missing Solmax
  boundary, useful-criticism supersession — all folded in); (2) 3 candidate
  non-time done_when formulations + adversarial judge (recommended one-real-loop
  hardened with dry-run clause discipline; pre_mortem #11 kept alive by replacing
  the elapsed-week proxy with a real run, not deleting the brake).
  Charter file: live/life-reset/CHARTER.md (full revised root).

state_changes: |
  CHARTER.md: full rewrite to the manager-as-spine root — owner_approved evidence
    updated; mission replaced; outside_view realigned (manager core, week pulse;
    reference classes kept for the post-map research leg); success_criteria = 4
    core (sc-one-real-run, sc-owner-acceptance, sc-conscious-week, sc-provider-
    portability); operating_model realigned (manager_spine, intake_and_techniques
    with R32, weekly_pulse, daily_loop, review_and_self_improvement, backlog,
    process_extension); scope.in adds programs + technique-selection; scope.out +
    authority keep Solmax/raw-data boundaries (Solmax named explicitly);
    interaction_principles = 5 strict-personality + gated-self-rewriting + 5 kept
    working principles; NEW operating_invariants (R11/R12/R22); NEW guardrails
    (G-1..G-4); lenses (9, safety/system-quality/improvement extended);
    owner_edges (criticism edge sharpened); risk_posture updated; pre_mortem =
    13 items (10 kept, 2 edited, 3 added); product_repos [].

  TREE.md: owner_approved updated; tree_validity → root_only_pending_map; root
    goal + done_when replaced (manager-spine + one-real-run gate); children → [].

  NOW.md: active_bet status none, note = re-frame done + route to map; preserved_
    evidence adds this history; open_calls = c-life-reset-map-manager-tree-001;
    next = the map CALL.

  LOG.md: append the 2026-06-21 frame line.

  history/: this file.

captures:
  - Reshape applied to mission text: "прогоняет один настоящий недельный цикл" →
    "один настоящий цикл" to stay consistent with the owner's later rejection of
    the weekly gate (authority: live owner instruction > earlier wording).
  - CALL boundary said "PROTECT pre_mortem #11 (run one real cycle before more
    architecture)"; owner explicitly reshaped it (no calendar-week block). Honored
    per authority order (live owner instruction > CALL): the brake is PRESERVED in
    substance (one real run + owner-paced tripwire + anti-paper-done clause), only
    the elapsed-week proxy removed. Logged as friction, not a silent deletion.
  - Open design questions for the post-map research leg (route frame→map→research
    →shape): week-graph node/edge form; technique-selection (R32) method; balance
    minimum/routine/wishes/rest (R29); safe self-rewriting gate mechanism; the
    realism/representativeness bar of the one-real-run gate (so a cherry-picked
    easy day or a thin "test" cannot pass).

decisions_needed: []

play_check:
  - "1 Interview (owner): skipped — re-frame, the interview (R1-R31) was already
    captured and owner-confirmed ('Go') in the remap session; R32 added in-session."
  - "2 Homework (outside view): done — two background workflows (coverage map +
    tension audit + adversarial gate-check; then candidate done_when generation +
    adversarial judge). No implementation research run (deferred per boundary)."
  - "3 Charter draft (owner): done — mission, 4 success criteria, 5 principles,
    self-improvement, guardrails/invariants presented one artifact at a time and
    each explicitly owner-approved (G9). Owner words cited in evidence."
  - "4 Pre-mortem (owner): done — 13-item pre-mortem (10 kept, 2 edited, 3 added)
    approved: 'да'."
  - "5 Root node (owner): done — root goal + reshaped one-real-run done_when + lens
    confirmation approved: 'да'. Children deferred to map (tree root-only)."
  - "6 Close (owner): done — RESULT with owner_approved charter + root; next = map
    CALL directly (implementation research is the post-map leg, owner's route)."

log: "frame: owner approved the re-framed root — strict self-improving personal-manager-as-spine (mission, 4 criteria, 5 strict-personality principles + gated self-improvement, guardrails, R11/R12/R22, updated pre-mortem); efficiency = method-not-end (R1 refine); +R32 technique-selection; week/N-cycle completion gate rejected → reshaped to ONE real run (test/dry-run/lived-day) + owner-useful acceptance; TREE root-only; next c-life-reset-map-manager-tree-001."

next: |
  CALL c-life-reset-map-manager-tree-001
  to: session
  direction: life-reset
  play: map
  node: g-life-reset-root
  goal: |
    life-reset has an owner-approved goal TREE rebuilt on the revised root: a
    strict, self-improving personal operating MANAGER as the spine, with outcome
    nodes into which directions + programs + backlog plug (week = pulse, day from
    week), and the manager's own capabilities (intake/self-definition, weekly
    pulse, daily loop, review/self-improvement, backlog, process-extension).
  context: |
    Read, in this order:
    - os/KERNEL.md
    - os/plays/map.md
    - live/life-reset/CHARTER.md (the revised root — authority)
    - live/life-reset/TREE.md (root-only; children: [])
    - live/life-reset/NOW.md
    - live/life-reset/work/life-reset-manager-vision-capture-v1.md (hub model §2)
    - live/life-reset/history/2026-06-21-s-life-reset-frame-root-001.md (this RESULT)
  boundaries: |
    Outcomes only — no tasks (G2); every non-root node carries its one-line why.
    Do NOT run the implementation research inside map (week-graph form, technique-
    selection design, balances, safe self-rewrite mechanism) — that is the
    FOLLOWING research leg. One entity per chat; present skeleton + each node for
    explicit owner approval (G9). Keep source-direction boundaries; protect the
    one-real-run completion gate (no week / no N-cycle) and the guardrails.
  done_when: |
    TREE.md has an owner-approved set of outcome nodes on g-life-reset-root, each
    with its why; NOW.next is a research CALL for the §7 implementation questions
    (or shape, per map's close).
  return: |
    RESULT with the owner-approved tree, state_changes, evidence and next CALL.
  budget: one map session

END_OF_FILE: live/life-reset/history/2026-06-21-s-life-reset-frame-root-001.md
