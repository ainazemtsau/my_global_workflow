RESULT s-life-reset-map-manager-tree-001 (call: c-life-reset-map-manager-tree-001)
direction: life-reset   play: map   node/task: g-life-reset-root

outcome: |
  life-reset now has an owner-approved 6-node OUTCOME tree on the manager-as-spine
  root (G9, "да"). The six top-level outcome nodes (all status: parked):

    1. g-lr-run      — the weekly+daily rhythm actually runs (week composed in
                       strict order, day derived, R11 gate); default each week =
                       the accepted process.
    2. g-lr-protect  — asymmetric strictness actually protects: the documented
                       crash loop is cut at the fixation step (throttle the surge,
                       no silent displacement, recovery as a sanctioned reduced-load
                       STATE, a grip that holds when the owner is the unreliable
                       narrator / absent).
    3. g-lr-integrate— the three streams (directions-as-summaries, programs,
                       backlog) feed ONE rhythm with no loss and no scope capture;
                       conflicts surface explicitly and route; no raw neighbor data.
    4. g-lr-learn    — review -> safe self-rewrite: weekly review -> a decision
                       (default hold), the self-improvement loop fires >=1 for real,
                       a sealed protected core (rest/safety/floors + override)
                       survives every rewrite, autonomy is earned/demotable, and a
                       hollow-but-compliant week can be judged a failure.
    5. g-lr-trust    — the manager is a mentor the owner does not resist; trust
                       survives a wrong verdict and a multi-day rupture; a small
                       obeyed decided front; the LIVE running state is portable
                       across chat/provider.
    6. g-lr-grow     — the manager defines itself (first intake mandatory), grows
                       by evidence, and STAYS LIGHT (can shed dead rules); gated on
                       a proven v1.

  Order (owner-approved): run -> protect -> integrate -> learn -> trust -> grow.
  v1 implication recorded: the one-real-run gate (criterion 1) is a thin slice
  across run+protect+learn+portability(trust), with integrate present and grow
  excluded — guidance for the research/shape legs, not designed here.

  Two owner clarifications this session, both folded and consistent with the
  charter (no frame re-route):
   - One-real-run stays LIGHTWEIGHT: "started using it" closes the run once real
     use produced the real artifacts; the anti-paper floor is KEPT (architecture-
     exists does not count); the verdict is rendered through use (problems -> hard
     feedback -> fix | fine -> extend). Consistent with criteria 1 + 2.
   - Research CADENCE: first-run intake research is mandatory (once); the weekly
     review runs every week and yields a decision whose DEFAULT is hold (run the
     accepted process); process-changing research/mutation is OPTIONAL, fired only
     on dissatisfaction (= the {hold} vs {research/mutate} fork). Consistent with
     R13/R17/gated-self-rewrite/review_and_self_improvement.

  Structural forks resolved: g-lr-protect kept WHOLE (collapse-grip not split —
  owner accepted the card as shown); portability kept INSIDE g-lr-trust (not split
  — owner accepted card 5 as shown).

evidence: |
  Owner approvals in-session (key words):
  - Skeleton (orientation): "Так, в принципе, я с тобой согласен."
  - Card 1 g-lr-run: "Окей, выглядит нормально" + cadence clarification -> "accept".
  - Card 2 g-lr-protect: "accept" (kept whole).
  - Card 3 g-lr-integrate: "принимаю".
  - Cards 5 + 6: "Пятое, шестое карточек принимать"; card 4 re-explained simply ->
    "да понятнее принимаю".
  - Order + whole tree (G9 close): "да".

  Narrow map_evidence pass (option A, owner-chosen; owner waived his own candidates
  with "покажи свой скелет"): workflow wf_a02c5d01-bae — 5 finders (outlier-
  durability, failure-inversion, comparable-systems, AI-self-improve-governance,
  capability-list-critic) -> 6 non-obvious outcomes, ALL 6 survived adversarial
  verification (keep on all four tests: outcome-level / traces-to-criterion /
  placeholder / genuinely-non-obvious), + 7 structural blind spots. Every finding
  folded into the 6 nodes. Rare-example seeds (one per non-obvious outcome): AA
  sponsorship (accountability outside willpower); Rule of St. Benedict ~1500 yrs
  ("discretion" / sanctioned de-load); Graham Greene's fixed daily quota (cap the
  surge, not just floor the dip); provably-corrigible-agents (arXiv 2507.20964 —
  corrigibility invariant under self-modification); Dietvorst algorithm-aversion
  (one error -> abandonment unless trust is calibrated) + alliance rupture-repair;
  documented GTD limitation ("tells you what exists, not what matters").
  Note: the verifier cross-checked CHARTER.md and the prior
  history/2026-06-19-s-life-reset-map-evidence-001.md (the OLD root's 6 capability-
  level candidates) and confirmed the 6 non-obvious outcomes are absent there.

  Map play: skeleton shown as 3-6 tagged one-liners in order (orientation, not
  approval); cards elaborated (goal/done_when/why/edge/risk), one at a time then
  batched at owner request; per-card verdicts; order confirmed; depth top-level
  only (G2); lens sweep over all 9 charter lenses (below). The non-obvious-path
  requirement (>=1 card seeded from a rare outlier) is over-satisfied (protect /
  learn / trust each carry a verified rare seed).

state_changes: |
  TREE.md: owner_approved evidence += G9 6-node approval; tree_validity
    root_only_pending_map -> mapped (note rewritten: 6 nodes, parked, detail
    pointer, route to research); root.children [] -> 6 nodes (g-lr-run,
    g-lr-protect, g-lr-integrate, g-lr-learn, g-lr-trust, g-lr-grow), each with
    goal + done_when + one-line why + status parked + detail pointer to this file.
  NOW.md: active_bet.note rewritten (map done; one-real-run lightweight clarif;
    route to research); preserved_evidence += this history file; open_calls map
    call -> c-life-reset-research-build-manager-001; next = the research CALL.
  LOG.md: appended the 2026-06-21 map line.
  history/: this file.

captures:
  - One-real-run is lightweight/recorded, satisfied by real use beginning (not a
    ceremony) with the anti-paper floor kept; verdict rendered through use. If the
    charter wording ("владелец явно подтверждает") is ever felt as too heavy, that
    is a small FRAME touch — not needed now (substance holds via criteria 1 + 2).
  - Research cadence = {hold} default vs {research/mutate}-on-dissatisfaction;
    first intake mandatory once. Placed across g-lr-run (pulse default), g-lr-learn
    (optional-research trigger), g-lr-grow (first intake).
  - Deferred implementation/mechanism questions for the research leg (the WHAT is
    fixed by the tree; these are the HOW): week-graph node/edge form + day-
    derivation; technique-selection method (R32); balance minimum/routine/wishes/
    rest (R29); safe self-rewrite gate mechanism (sealed core + override-survives);
    collapse-grip DETECTION mechanism (inside the override path, not auto-
    surveillance); earned/demotable autonomy scoring rule; how a "hollow week" /
    aliveness is measured; ceremony/burden budget (stays light); non-punitive
    re-plan + non-punishing re-entry mechanism; the realism/representativeness bar
    of the one-real-run gate (so a cherry-picked easy day or a thin "test" cannot
    pass).
  - 7 structural blind spots from the evidence pass, all folded: bad-week/absence
    restart (protect); between-things trough (protect); live-running-state
    portability vs static facts (trust); earned/demotable autonomy (learn);
    witnessed self-improvement event (learn); system's own running cost / can-get-
    smaller (grow); non-punitive re-composition (run + learn).

decisions_needed: []

play_check:
  - "1 Recite: done — mission + 4 criteria + routed 5/6/7 + root-only tree state restated in plain words."
  - "2 Candidates & evidence (owner): done — owner WAIVED his own candidates ('покажи свой скелет') and chose narrow map_evidence ('A — узкий пас'); fresh map_evidence RESULT produced (workflow wf_a02c5d01-bae). Search-first NOT added (owner chose A, not C)."
  - "3 Skeleton (owner): done — 6 one-liners shown tagged (owner/gap-fill/non-obvious/evidence) in proposed order; owner reaction 'в принципе согласен'."
  - "4 Cards: done — 6 cards elaborated (goal/done_when/why/edge/risk), one-at-a-time then batched 4-6 at owner request ('давай одной пачкой'); >=1 non-obvious card seeded from a rare outlier (protect/learn/trust)."
  - "5 Per-node verdict (owner): done — each card an explicit verdict, no silent batch: card1 'accept', card2 'accept', card3 'принимаю', card5+6 'принимать', card4 'да понятнее принимаю'. g-lr-protect kept whole; portability kept in g-lr-trust (owner accepted as shown)."
  - "6 Order (owner): done — run->protect->integrate->learn->trust->grow approved ('да')."
  - "7 Depth check: done — top level only (G2); deeper splits deferred to shape."
  - "8 Lens sweep: done — all 9 charter lenses covered; spirit-and-transcendence / inner-work-and-disclosure / fatherhood-and-example marked not_needed as NODES with reason (served as terminal values + programs via grow/integrate, guarded by learn's aliveness veto and protect's safety); no new nodes."
  - "9 Close (owner): done — owner approved the whole 6-node tree (G9) 'да'; next = research CALL (route frame->map->research->shape)."

log: "map: owner approved (G9) the 6-node outcome tree on the manager-as-spine root (run/protect/integrate/learn/trust/grow), order run->protect->integrate->learn->trust->grow; narrow map_evidence (5 finders -> 6 non-obvious outcomes all verified + 7 blind spots) folded; one-real-run kept lightweight (anti-paper floor) + research cadence clarified (default hold, research on dissatisfaction; first intake mandatory); 9 lenses covered; next c-life-reset-research-build-manager-001."

next: |
  CALL c-life-reset-research-build-manager-001
  to: session
  direction: life-reset
  play: research
  node: g-life-reset-root
  goal: |
    Produce implementation evidence + 2-3 options each (with a recommendation),
    NOT decisions, for HOW to build the strict self-improving manager across the
    6 outcome nodes — so a following shape can size a tiny first real-run slice.
  context: |
    os/KERNEL.md ; os/plays/research.md ; CHARTER.md (authority) ; TREE.md
    (6 outcome nodes = the WHAT) ; this history file (full cards + the risk lines
    that flagged each deferred mechanism) ; vision capture §7 (open questions).
  boundaries: |
    Evidence + options only; the owner decides. No building. Do NOT re-open the
    tree (6 outcomes owner-approved) — surface, do not silently change. Keep the
    one-real-run gate (no week / no N-cycle) and the guardrails (sealed core +
    inviolable override; recovery protected; no false precision). Source
    boundaries intact (summaries, not raw neighbor data). One entity per chat.
  done_when: |
    A research RESULT with evidence + 2-3 options (each with a recommendation) for:
    week-graph node/edge form + day-derivation; technique-selection (R32); balance
    minimum/routine/wishes/rest (R29); safe self-rewrite gate mechanism (sealed
    core + override-survives-rewrite); collapse-grip detection (inside override);
    earned/demotable autonomy scoring; how a "hollow week" / aliveness is measured;
    ceremony/burden budget; non-punitive re-plan + re-entry; realism bar of the
    one-real-run gate. Next = shape CALL on g-lr-run bundling the v1 thin slice.
  return: |
    RESULT with evidence, options+recommendations, captures, decisions_needed,
    next = shape CALL.
  budget: one research pass (multi-agent per the parallel-verify discipline).

---

## Appendix — full cards (detail: target for each TREE node)

Format per card: Goal · Done_when · Why -> criteria · Edge · Risk. Tags from the
skeleton: owner / gap-fill:<lens> / non-obvious. Evidence findings each card
carries are noted. The one-line `why` in TREE.md is the compression of "Why" here.

### Card 1 — g-lr-run · Ритм реально крутится  [owner]
- Goal: a working weekly+daily rhythm exists — real composed weeks in strict order
  (musts/floors -> routine -> wishes-through-filter -> explicit cuts; all 3 streams;
  <=1 new experiment) and days derived from them with the R11 gate; divergence of
  plan from reality enters the next pulse as input, experienced as correction, not
  a verdict. Default each week = compose from the ACCEPTED process; no mandatory
  per-week process-research.
- Done_when: on real data >=1 real week composed in this order (floors first; >=1
  wish through the filter OR rejected with reason; >=1 explicit cut), >=1 real day
  derived, R11 fired >=1; all durable. This IS "started using it" — a real rhythm,
  not paper.
- Why -> criteria: carries 3 (conscious week, strict order, 3 streams) and supplies
  2 of 4 durable artifacts of 1; substrate the other 5 nodes stand on.
- Edge: Direction OS already gives durable-state + CALL/RESULT + owner-approved
  gates — the rhythm inherits the mechanics; the strict musts-before-wishes order
  with protected floors + loud cuts is owner-required and not what ordinary
  planners do. Incumbents (Todoist/Notion/AI-assistant) won't follow: their
  business is to CAPTURE tasks, not to throttle and cut.
- Risk: (1) the week degrades into a wishlist (filter doesn't bite) -> closed by
  g-lr-protect; (2) the rhythm costs more attention than it saves (bureaucracy) ->
  closed by g-lr-grow (stays light); (3) "started using" degrades to paper-done ->
  held by the anti-paper floor.

### Card 2 — g-lr-protect · Асимметричная строгость реально защищает  [non-obvious]
- Goal: the crash loop is cut at the fixation step — the manager throttles the
  owner's own surge (can say NO to over-investment on a high-energy day), no
  pillar/commitment is displaced SILENTLY (any displacement surfaces as an explicit
  owner-visible cut), recovery is a legitimate STATE the structure can be in (a
  sanctioned reduced-load mode, not a discarded system), and the manager holds a
  grip even when the owner is the unreliable narrator or absent for days.
- Done_when: durable state has (a) >=1 episode where the manager limited/refused
  over-investment OR named a pillar starting to fall BEFORE the crash and held the
  protected class (recovery/floors); (b) >=1 episode of a sanctioned reduced-load
  mode (load down, structure intact) — not "full load until break" and not
  "discarded on a bad day"; the filter passed genuine recovery >=1 when needed
  (matches criterion 1, item 1).
- Why -> criteria: carries 1 (inoculate the documented loop fixation->pillars-fall
  ->binge/smoke/zone-out->crash, its FIRST step) + g3 (the filter MUST pass
  recovery, else the manager becomes the burnout loop). The reason re-frame
  happened: without this node "asymmetric strictness" is a slogan.
- Edge: seeded on rare examples, not a textbook — AA sponsorship (durability =
  accountability OUTSIDE willpower, reaching in exactly when willpower fails) ·
  Rule of St. Benedict ~1500 yrs (longevity = "discretion": a built-in de-load,
  else a strict rule is discarded on a bad day) · Graham Greene's quota (the
  durable CAP inspired days as hard as they floor bad ones — the enemy was over-
  investment, not laziness). Owner edge: he demanded a non-caving manager AND
  protected recovery — that IS the asymmetry. Incumbents won't follow: productivity
  apps optimize CAPTURE and throughput; telling the user "stop, don't invest so
  much" is against their nature.
- Risk: (1) strictness bites recovery too -> the manager becomes a burnout loop
  (kill_by already in pre-mortem: refused needed recovery + coincided with relapse
  -> filter immediately loosened); (2) the collapse-grip drifts into auto-
  surveillance -> must stay inside the owner-override path (the detection MECHANISM
  is the next research leg, not here); (3) bundled tightly — collapse-grip could be
  its own node (owner accepted the card whole).
- Carries: non-obvious outcomes #1 (external grip), #2 (sanctioned reduced-load),
  #3 (throttle the surge); blind spots bad-week/absence-restart, between-things
  trough.

### Card 3 — g-lr-integrate · Три потока без потерь и без захвата scope  [gap-fill:cross-direction-integration]
- Goal: directions (health, game — as agreed SUMMARIES, not raw), programs, and
  backlog feed ONE weekly rhythm; nothing important is lost or silently displaced
  between streams; neighbor boundaries intact (life-reset does NOT store raw
  health/game/Solmax data and does not rewrite their scope); a conflict between
  streams surfaces explicitly and is routed (owner tops up or a routed request to
  the source), not swallowed.
- Done_when: >=1 real case where the manager combined input from >=2 streams into a
  week reading neighbors as summary (not raw) and, when info was thin, topped up via
  owner / a routed request rather than inventing; >=1 case where a potential
  conflict/displacement between streams surfaced EXPLICITLY and was routed; the
  life-reset durable state contains no raw neighbor data.
- Why -> criteria: carries 3 (all 3 streams present) + g4 (source boundaries
  inviolable — no duplicating nutrition/training/game/Solmax) + the hub model
  ("nothing is lost", coordinate at the level of commitments, don't mirror raw).
- Edge: exactly the owner's setup — health and game already exist as separate
  directions with their own source-of-truth, and he required not duplicating them;
  Direction OS already does CALL/route between directions. Most "second brain" /
  life-OS tools instead pull EVERYTHING into one data lake (ingest-all) — a
  coordinator that keeps neighbors behind a boundary and reads only summaries is
  against their model.
- Risk: (1) temptation to ingest raw data "for convenience" -> g4 violation (kill:
  manager mirrors neighbor raw -> revert to summary-only); (2) summary too thin ->
  manager invents for the neighbor -> held by "owner tops up / routed request";
  (3) integration degenerates into another duplicate tracker -> closed by g-lr-grow
  (stays light).

### Card 4 — g-lr-learn · Review->улучшение реально срабатывает, ядро запечатано  [non-obvious]
- Goal: day reviews aggregate into a weekly review (commitments vs evidence) ending
  in >=1 decision {hold|mutate|kill|route|research|simplify}; the self-improvement
  loop has FIRED for real at least once (the manager rewrote part of its own
  operation from a reviewed conclusion about THIS owner, owner-approved, persisted,
  pointable-to); the protected core (asymmetry, rest/safety floors, override/
  rollback) DEMONSTRABLY survives every rewrite; self-rewrite authority is earned
  and demotable; a "complete but hollow" week can be named a FAILURE.
- Done_when: (a) >=1 weekly review with an explicit decision, default visible as
  hold and research/mutate taken only on dissatisfaction (the owner's cadence);
  (b) >=1 real self-rewrite from a conclusion about the owner, owner-approved,
  saved, pointable-to; (c) after it, override/rollback work as before AND floors
  intact — verified; (d) >=1 review where "all done, but empty" is judged not-a-
  success on the aliveness/meaning axis.
- Why -> criteria: carries routed 7 (self-improvement-fires) + g1 (gated rewrite +
  inviolable override) + protects the terminal value (efficiency = method; else the
  optimizer quietly eats recovery "for efficiency" and improves the system INTO the
  crash).
- Edge: provably-corrigible-agents (arXiv 2507.20964 — corrigibility/off-switch
  stay provably binding UNDER self-modification; a flat self-rewriter optimizes away
  its own stop) + the GTD limitation ("tells you what EXISTS, not what MATTERS").
  Owner edge: Direction OS already lives on gated owner-approved changes. Incumbent
  AI assistants self-improve WITHOUT a sealed core or an override invariant — a
  feature for them, a self-drift-into-crash risk for the owner.
- Risk: (1) the loop never fires -> an "adaptive" system = a static one (kill: a
  3rd repetition of one failure-pattern without a system change); (2) review
  degenerates into ritual without mutation -> the decision must be from the set;
  (3) the earned-autonomy scoring rule / how "hollow" is measured = the next
  research leg, NOT here (implementation-leak flag); (4) "sealed" only on paper ->
  done_when requires a REAL override+floors check after a rewrite.
- Carries: non-obvious outcomes #4 (sealed core), #6 leg-B (hollow-week veto);
  blind spots earned/demotable autonomy, witnessed self-improvement event.

### Card 5 — g-lr-trust · Доверие и продолжаемость  [non-obvious]
- Goal: the manager feels like a mentor the owner does NOT resist (R7); trust
  survives a demonstrably WRONG verdict (a tolerated error band, not all-or-
  nothing) and a multi-day RUPTURE (non-punishing re-entry, no fresh full intake);
  a short decided front "what matters now / what is deliberately NOT done" the owner
  acts FROM instead of re-deciding; and the LIVE running state (cycle position,
  current protections, autonomy rung) continues in a new chat/provider — not just
  static facts.
- Done_when: (a) >=1 episode: a wrong verdict, yet the owner still accepted the
  manager's next strict call; (b) >=1 episode in durable: skipped for days (relapse)
  -> non-punishing re-entry with no full intake; (c) >=1 action FROM the decided
  front without re-deciding; (d) a new chat/provider continued the RUNNING state
  (cycle position, current protections, autonomy rung), not just the archive.
- Why -> criteria: carries 2 (acceptance-after-USE — which includes use through a
  wrong verdict and a relapse) + 4 (portability, deepened to live state) + R7 +
  non-caving (the stricter the manager, the costlier one wrong "no" and one
  rupture).
- Edge: algorithm-aversion (Dietvorst — people abandon an algorithm after ONE
  error and hold it to a higher bar than a human; fatal for a manager whose power
  is non-caving verdicts) + alliance rupture-repair (the alliance predicts
  adherence better than technique; a rupture without a non-punishing return =
  premature termination). Owner edge: he demanded a non-caving manager AND "my
  reflection" — trust must survive both its error and his relapse, else the power
  collapses on the first "no". Incumbents are either impersonal (no alliance) or
  caving (no power) — no "strict but trusted through error" middle.
- Risk: (1) one wrong "no" zeroes trust -> the manager is dead (the node's main
  risk); (2) a strict return punishes the relapse -> accelerates abandonment (kill:
  return-after-rupture requires punishment -> remove immediately); (3) bundles two
  beasts — trust-through-error and live-state portability (owner accepted bundled;
  may split portability 4 later).
- Carries: non-obvious outcomes #5 (calibrated trust + rupture-return), #6 leg-A
  (obeyed decided front); blind spot live-running-state portability.

### Card 6 — g-lr-grow · Сам себя определяет, растит и остаётся лёгким  [gap-fill:improvement-and-learning]
- Goal: first run = mandatory self-definition (intake: missing questions,
  understands the situation, picks techniques fitting the owner — tasks first, then
  techniques, R32); backlog = capture-without-obligation (not-to-lose != must-do);
  candidates promote to programs / new repeatable processes / separate directions
  (a process is defined BEFORE it runs: outcome/scope/interface/durable-state/
  cadence/link-to-week/stop); extensions do NOT bloat the week; the manager can
  become SMALLER (shed dead rules), and the meta-layer holds the same anti-
  procrastination bar as the daily loop.
- Done_when: (a) the first run really did intake + picked >=1 technique, saved
  (mandatory once); (b) >=1 idea went to backlog as capture-without-obligation
  without bloating the week, lifecycle visible; (c) an extension/new program starts
  only AFTER a genuinely-used v1 going fine (the owner's "fine -> extend"), via a
  process defined before launch; (d) >=1 case where the manager SHED a dead rule /
  simplified a format, not only added.
- Why -> criteria: carries routed 5 (managed-extensions) + 6 (process-incubation/
  self-definition) + system-quality (anti-bureaucracy) + guards two meta-failures
  (tracking-as-procrastination; formalization-breeds-bureaucracy). Gated on a
  proven v1 — extensions layer AFTER the run (the lightened "further architecture
  blocked until the gate passes").
- Edge: the owner explicitly demanded a "tiny first version" and "no bureaucracy";
  Direction OS already has a parked->active->done lifecycle and G8-intake.
  Incumbents only GROW (more features, more capture) — no productivity system
  counts "becoming smaller" as success. "Can slim down" is the owner's anti-
  incumbent edge.
- Risk: (1) extensions race ahead of use (built a lot, never ran) -> the owner's
  pre-mortem #11, held by the "only after a real v1" gate; (2) backlog = a
  graveyard of guilt -> capture != promise, hard cleanup; (3) "slims" only in words
  -> done_when requires >=1 real shed.

END_OF_FILE: live/life-reset/history/2026-06-21-s-life-reset-map-manager-tree-001.md
