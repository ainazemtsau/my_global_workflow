# CALL c-exec-g-37a1-body-plan-publication-unblock-001

> **RETIRED — DO NOT DISPATCH (2026-07-30).** The owner explicitly asked to repair the Character frontier. The
> publication work described below is already complete; contract v34 exposes the actual remaining conflict as frozen
> authority under one old change id. Current routing is
> `c-control-g-37a1-body-first-person-replace-close-001`. The prior bytes remain below as historical evidence only.

direction: indie-game-development
track: t-body
for: t-2
node: g-37a1
to: executor (engineering same-leg successor, product repo)
kind: engineering
repo: C:\projects\Unity\GasCoopGame_win-u2
issued: 2026-07-30 by s-repair-g-37a1-body-plan-publication-route-001

engineering_contract: 31
stage: PLAN — separate publication-unblock session
slot: **WIN-U2** — existing worktree `C:\projects\Unity\GasCoopGame_win-u2`, branch `slot/win-u2`, Target **Local**
product change_id: **remains `c-exec-g-37a1-body-first-person-001`**
budget: one fresh bounded product-control session; every feature stage remains a separate fresh session

## goal

The frozen owner-approved minimal Character PLAN has a lawful GREEN publication result and an exact initial PLAN
receipt, while the superseded r2 RED pair remains immutable failed evidence and no Character implementation stage has
started.

## context

- The Direction task remains t-2: a first-person body walks, jumps, falls, lands, stands stably and climbs one simple
  cubic ledge by querying the cubic grid directly, with no generated terrain mesh colliders and no first/third-person
  switch.
- WIN-U2 is clean at `420d6f8d05984fbc9f15bc44591e8f9b2b928b69` on permanent branch `slot/win-u2`, Target Local.
  That commit freezes the accepted minimal PLAN-AMEND. Product `validation.config` is contract v31; this is a
  same-lineage successor and preserves pin 31 rather than bundling Re-sync.
- `openspec/changes/c-exec-g-37a1-body-first-person-001/PLAN.md` and
  `docs/body-first-person-decisions.md` record the exact owner verdict `принимаю` and remain publication-blocked.
- The required PLAN-publication route currently runs the ordinary headless suite before its PLAN branch. That suite
  includes 16 deliberately failing BodyMotion tests from the immutable superseded r2 candidate; the reproduced result
  is `1830 passed / 16 failed`. The PLAN-AMEND session therefore returned genuine ESCALATE and issued no new receipt.
- The failed r2 authority stays fixed: carrier `00012b6877c4512dd6d58303805660b8cffdc731`, RED
  `3cfecc766471bf2913b63784fbd2bcb64ef0988a`, process tip `8106170d7bfdd5cad705eeab248285fcd0621339`,
  receipt `docs/measurements/root-receipts/c-exec-g-37a1-body-first-person-001/01-pair-candidate-r2.json`, verdict
  `PAIR-FREEZE FAIL / NOT BUILD ELIGIBLE`.
- The owner selected the narrow route with the exact words `да, вариант A, запускаем U2`: preserve the old RED as
  evidence and make PLAN publication exclude only that explicitly superseded immutable lineage.
- Current Direction authority is
  `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\NOW.md`; the frozen plan packet
  and repo-local `AGENTS.md`, `validation.config` and process checks are product authority.

## boundaries

- This session may change only the minimum repo process/control-plane checks, their existing self-test surface,
  validation/publication configuration, lifecycle evidence and the new PLAN receipt needed for a lawful publication.
- Do not edit the frozen PLAN, proposal, delta spec, tasks, owner decision page or Character ADR at `420d6f8d`.
- Do not edit, delete, retire, relabel or reclassify any r2 carrier, Character test/test-support, manifest, receipt,
  review or refutation byte. Their failed status and `NOT BUILD ELIGIBLE` meaning remain unchanged.
- Do not introduce a broad test skip or make PLAN publication ignore unrelated/current failures. The exemption must be
  bound to the exact superseded r2 lineage and must fail closed when that identity or disposition is absent, stale or
  mismatched.
- Do not edit Character production behavior, gameplay assets, scenes, prefabs, packages or ProjectSettings. Do not
  open Unity Editor or Unity MCP. Do not run PAIR-CANDIDATE, PAIR-FREEZE, BUILD, VALIDATE, Deliver or an executable.
- Do not Re-sync, create/switch/reset/delete branches, create/move/delete worktrees, stash, clean, force or rewrite
  history. Existing unrelated work and evidence are preserved.
- If the narrow exception cannot be made both exact and fail-closed, return one genuine ESCALATE instead of weakening
  the global gate or modifying the failed r2 evidence.

## done_when

1. The repo has one minimal, documented PLAN-publication rule that distinguishes an explicitly superseded immutable
   failed pair from current PLAN obligations without exempting unrelated failures.
2. Existing repo-native control-plane checks include a negative/seeded miss proving that a missing, stale, mismatched
   or over-broad superseded-lineage declaration fails closed; no standalone ad-hoc scanner substitutes for the existing
   check surface.
3. The exact frozen owner-approved packet at commit `420d6f8d05984fbc9f15bc44591e8f9b2b928b69` passes the real
   PLAN-publication route for change id `c-exec-g-37a1-body-first-person-001`.
4. A committed fresh receipt exists at
   `docs/measurements/root-receipts/c-exec-g-37a1-body-first-person-001/minimal-mvp/00-plan.json`; it truthfully records
   stage `PLAN`, state `ACTIVE`, the frozen plan/decision identities and eligibility only for a fresh minimal
   PAIR-CANDIDATE.
5. Git proves all r2 carrier/RED/test/test-support/manifests/receipts/reviews remain byte-identical and retain
   `PAIR-FREEZE FAIL / NOT BUILD ELIGIBLE`; no BUILD authority points to them.
6. The diff contains only the bounded process/control-plane, existing check/self-test, publication config, lifecycle
   and receipt surfaces. Character behavior and the frozen PLAN packet are unchanged; Unity was not opened.
7. The result is committed on `slot/win-u2`, the checkout is clean, and the exact checks/commands and outputs are
   recorded. Success opens only a separate fresh minimal PAIR-CANDIDATE session; it does not start that stage here.

## return

On success, return a committed product-stage handback with basis/head/tree, changed paths, exact publication command
and output, seeded-miss evidence, the new receipt path/blob/hash, proof of unchanged r2 bytes and the one next legal
fresh stage. This is product-local progress under the still-open pin-31 root, not a delivery HOME and not BUILD
authority. If any boundary or done_when item cannot be met, return one genuine `HOME: ESCALATE` with the exact blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-37a1-body-plan-publication-unblock-001-call.md
