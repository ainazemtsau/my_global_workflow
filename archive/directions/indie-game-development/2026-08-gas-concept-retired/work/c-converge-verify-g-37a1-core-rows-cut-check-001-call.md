> **RETIRED 2026-07-31 — DO NOT DISPATCH.** The bet `g-37a1` was closed with verdict `obsolete` (the owner changed the game concept). Every CALL issued under it is dead regardless of the status written below. This file is preserved as evidence of what was decided, never as a frontier. The live frontier is `live/indie-game-development/NOW.md`, which currently has `bet: null` and no open calls. See `history/2026-07-31-s-review-g-37a1-obsolete-concept-change-001.md`.

CALL c-converge-verify-g-37a1-core-rows-cut-check-001
to: session
direction: indie-game-development
play: converge-verify
node: g-37a1
verify_target: build
goal: |
  ONE SHORT INDEPENDENT LOOK AT THE CUT — not another full sweep. The owner deleted the
  acceptance apparatus from `work/converge-g-37a1-core-rows.md` on 2026-07-28
  (`NOW.md` `d-core-acceptance-format-001`), and `s-converge-g-37a1-core-rows-cut-001` executed it.
  You answer ONE question in two halves:
  **(a) did the cut delete something the BUILD actually needs, and (b) does the reduced set still
  cover the fifteen criteria?**
context: |
  THIS IS STILL STEP 3 of the owner's six-step route (`NOW.md` `d-post-verify-route-001`), which he
  amended himself on 2026-07-28: the route stands, the acceptance FORMAT it was verifying does not.
  Step 3 has now consumed two verification legs and one repair round and twenty-four findings, none
  of which ever challenged a requirement line — which is exactly why the owner cut the instrument
  rather than repairing it a third time.

  READ, in this order:
  - `live/indie-game-development/work/converge-g-37a1-core-rows.md` — the target. The cut is
    declared in the §THE CUT banner at the top; §CHECK is what replaced the apparatus;
    §DIED-WITH-THE-CUT names what went.
  - `history/2026-07-28-s-converge-g-37a1-core-rows-cut-001.md` — the cut RESULT: what was deleted,
    what was kept, and the three findings folded in.
  - `history/2026-07-28-s-converge-verify-g-37a1-core-rows-002.md` — the eight findings R1–R8, so
    you can confirm that R1, R6 and R7 are actually resolved and that R2, R3, R4, R5 and R8 really
    do die with the text they were against.
  - `live/indie-game-development/TREE.md` `g-37a1` — the fifteen criteria, his verdict on exact text.
  - `live/indie-game-development/work/core-requirements-g-37a1.md` — the sixteen live lines and the
    settings register.

  The pre-cut version of the artifact is in git if you need to see what a specific deletion removed.
boundaries: |
  SHORT AND NARROW. This is a look, not a sweep. Two things are in scope and nothing else.

  IN SCOPE (a): **did the cut take something the BUILD needs?** A deleted outsider procedure is NOT
  a loss — that is the decision. A loss is a REQUIREMENT, a forced PARAMETER, a named HAZARD, a
  glossary reading, a seam or a routed price that existed before the cut and exists nowhere now.
  Check especially: every row still carries its OBSERVABLE and its FORCES; §KNOBS still carries
  every parameter the deleted tests forced into existence; the hazards that used to live inside
  outsider blocks (ROW 13's dominant-kind render payload, ROW 9's more-versus-faster split, ROW 14's
  presence-without-size failure mode, ROW 12's infinite-source conflict) survive somewhere.

  IN SCOPE (b): **does the reduced set still cover the fifteen criteria?** §WHAT maps each one. Check
  that nothing became silently uncovered, that what IS uncovered is named, and that §WHAT's own
  statement of what the cut costs (advance falsifiability of the legibility criteria) is honest
  rather than decorative.

  OUT OF SCOPE, and a finding against any of these is a FALSE finding:
  - that the file no longer prescribes how the owner tests anything — that IS his decision;
  - that §CHECK's machine list is short, or that a given claim is not machine-assertable — the list
    is deliberately minimal and perception is deliberately not on it;
  - the five findings recorded in §DIED-WITH-THE-CUT (R2, R3, R4, R5, R8);
  - anything already refuted by the two prior verifications, listed by name in their receipts;
  - §OWNER's three answers, and the eleven closed rulings.

  NOT REOPENABLE, on his exact words: passability deleted; no sinks; the way up is the player's; two
  gases with liquid as headroom; breach behaviour is an EVENT whose form is a SETTING; no value
  below; death restarts the level in full; air refills only at the base; the rig carries a level
  button, a start state and an immortality switch, all off by default; no completion condition; the
  core is NOT graded on «весело». A finding against a CRITERION is out of scope — the card carries
  his `да` on exact text.

  THE SETTING RULE STILL GOVERNS: a question he turned into a knob is not an open question, and a
  finding that a row is incomplete because a value is unchosen is a false finding.

  Do not answer `d-air-counter-visibility-001`. Do not manufacture an owner decision. Do not open a
  bet, a task or a track. Do not restore any deleted apparatus, and do not propose a replacement
  ruler — if you find a real gap, NAME it and route it, do not fill it with a procedure.
  Do not read `archive/**` or the product repository.
done_when: |
  1. (a) answered: either nothing the build needs was lost, or the specific losses are named with
     where they used to live.
  2. (b) answered: the fifteen criteria still map, and every uncovered thing is named.
  3. R1, R6 and R7 are confirmed resolved — or named if they are not.
  4. `§SIGNOFF: converge-verify passed @ <date>` written on PASS; on FAIL, the named items returned
     to `converge` in ONE bounded CALL of one-to-three-sentence fixes.
return: |
  One short `converge-verify` RESULT. On PASS: the signoff line and step 3 marked COMPLETE in
  `d-post-verify-route-001`. Either way this does NOT block the step-5 brief
  (`c-map-g-37a1-engine-ladder-brief-001`), which runs in parallel by the owner's own amendment.
budget: one session, short, no owner.
surface: a FRESH chat, separate from the cut leg and from both prior verifications.

END_OF_FILE: live/indie-game-development/work/c-converge-verify-g-37a1-core-rows-cut-check-001-call.md
