RESULT s-visual-004 (continues s-visual-003.next — owner «го»)
direction: indie-game-development   play: work (executor CALL-framing for the g-7e15 parallel track)   node/task: g-7e15 (VISUAL parallel track)

outcome: |
  The build-step-1 executor CALL is AUTHORED + adversarially HARDENED and registered. The stale pre-structure-first
  c-visual-001 placeholder ("grid→GPU pipe over RN1") is RE-SPECCED to the structure-first build-step 1: a WORKING
  gas-visual on the developer's HOME machine on FAKE data — a one-way read-only SEAM (IGasViewSource) filling TWO GPU
  resources (a per-cell GridView volume + a per-type GasParams GraphicsBuffer) + a FakeGasViewSource + ONE shared URP
  RenderGraph raymarch "body" pass styling each sample by GasParams[dominantTypeId] → a fake blob shows WHERE +
  HOW-MUCH + a visible edge. Binding headless evidence = a stride-conformance gate (a declared expected-stride constant
  vs BOTH the C# blittable struct AND the documented HLSL layout) + a PASSING negative control + a headless data-path
  test; the SOLE owner-run axis = owner-EYE. The leg FREEZES the final buffer layout (incl. the warning-granularity
  fork) so steps 4/6 fill it unchanged. READY for a fresh GasCoopGame_dev session (opens with a PLAN, owner present).

evidence: |
  work/c-visual-001-call.md (the full hardened CALL + provenance + residual risks + left-to-PLAN). Hardened by workflow
  wf_5dd9d57a-142 (5 agents: 4 review lenses — Unity-6.3 feasibility / scope-boundary / done_when-verifiability /
  read-only-discipline — → 1 synthesis). NOW.md: c-visual-001 re-specced in open_calls + g-7e15.next updated. Reconciled
  against committed state first-hand (git HEAD aa0462f; no new concurrent commits since s-visual-003).

state_changes: |
  work/: + c-visual-001-call.md (the hardened build-step-1 executor CALL).
  NOW.md open_calls c-visual-001: RE-SPECCED from the pre-structure-first P1 ("grid→GPU pipe over RN1") to the
    structure-first build-step 1 (status queued; note rewritten to the seam + two GPU resources + FakeGasViewSource +
    body + stride-gate + owner-eye + the frozen-layout/warning-granularity decision + the deferred-scope boundaries;
    points to work/c-visual-001-call.md). next = c-visual-002.
  NOW.md parallel_tracks g-7e15 `next`: the "RECOMMENDED next leg = author the build-step-1 executor CALL" sentence
    REPLACED → "BUILD-STEP-1 CALL AUTHORED + hardened → work/c-visual-001-call.md (c-visual-001 re-specced + queued);
    READY: owner opens c-visual-001 in a fresh GasCoopGame_dev session; next after = c-visual-002".
  LOG.md: + the s-visual-004 line.

captures:
  - The hardening caught real scope creep that would have stalled the builder: clipmap/scroll + depth-composited full-res
    front were DEFERRED (fixed bounded region + flat gradient edge this leg); the warning-granularity fork was RE-ADDED
    so this leg freezes the FINAL layout (steps 4/6 fill it unchanged, not re-cut a gated seam).
  - The stride-conformance gate's self-mark loophole (C#-vs-C#) was closed → a declared constant asserted against BOTH
    the C# struct AND the documented HLSL layout + a PASSING negative control. (A reusable pattern for cross-language
    GPU-buffer seams.)

decisions_needed: []

play_check:
  - 1 recite: done — restated the goal (author the build-step-1 executor CALL per s-visual-003.next + doc §7 step 1).
  - 2 do the work: done — drafted the CALL, hardened it via wf_5dd9d57a-142 (4 lenses + synthesis), folded all must-fixes,
    wrote work/c-visual-001-call.md, re-specced c-visual-001 in open_calls, updated g-7e15.next.
  - 3 self-check: done — CALL hygiene held (goal/done_when business+architecture-level; HOW left to the PLAN under
    left_to_plan; no procedure paraphrase); boundaries tight (later steps explicitly deferred); binding headless evidence
    vs the sole owner-eye gate split; reconciled committed state (HEAD aa0462f) before writing; G1 intact (parallel
    track, no active_task created).
  - 4 close (owner): done — owner «го» authorized framing the build-step-1 CALL; it is authored, hardened, registered,
    and ready for him to open.

log: work/CALL-framing (g-7e15, s-visual-004): build-step-1 executor CALL authored + adversarially hardened (wf_5dd9d57a-142, 4 lenses) → work/c-visual-001-call.md; c-visual-001 re-specced + queued; READY for a GasCoopGame_dev session (PLAN); next = open c-visual-001, then c-visual-002.

next: |
  Open c-visual-001 in a fresh GasCoopGame_dev session (read work/c-visual-001-call.md by path; opens with a PLAN, owner
  present; builds dev→main when green). On its GREEN return (the stride gate + negative control + data-path test green +
  the recorded home frame-cost number + the owner-eye confirmation), a fresh OS session applies the RESULT (c-visual-001
  done) and the bet/track rolls to c-visual-002 (build-step 2 — three gases from DATA + the forgot-to-register guard).

END_OF_FILE: live/indie-game-development/history/s-visual-004.md
