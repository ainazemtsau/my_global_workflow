# RESULT — s-work-039 (indie-game-development / g-9c41 / Sc-kernel — work: binding fresh-session G5 close)

Date: 2026-07-03. Play: work (task close via the binding G5 refutation gate). Owner present («да по всем»).
Binding G5 workflow: wf_91e57ec8-b24 (10 adversarial lenses + independent per-finding adjudication + synthesis; 19 agents).

```
RESULT
session: s-work-039 — indie-game-development / g-9c41 / Sc-kernel — play: work (task close via the binding G5 refutation gate)

outcome: |
  Sc-kernel (c-exec-023, active-front sparse tick kernel) is DELIVERED and already MERGED to GasCoopGame main
  (@b7d4226, pushed; owner-authorized «мержим», owner-eye live MCP Play-mode of GasBuoyancyDemoScene). The
  BINDING fresh-session G5 — the first genuinely separate refutation (10 adversarial lenses vs real code +
  independent per-finding adjudication + synthesis, wf_91e57ec8-b24; the builder's own G5 was a same-session
  Sonnet pre-pass, not binding) — returns runtime SOUND: 0 P1, no shipped-wrong behavior. Byte-identity,
  roster-independence (KILL gate genuine + non-vacuous: cell-by-cell Mass equality roster-1 vs 64), ZERO-LEGACY
  (dense round-trip off the authoritative path — independently grep-confirmed), and contiguous-array field
  authority all HOLD. Found 2 P2 completeness/reproducibility gaps + 5 P3 honesty nits — none a kill condition,
  all routed into one small follow-up leg c-exec-024 (the slice is on main: a fix is a new leg, not a silent
  edit). Owner accepted «да по всем» (2026-07-03): Sc-kernel done + cleanup leg; d-hangar-flood-fallback-001
  answered (design + admission ceiling); SPEC §6 п.2 ceiling seated; Unity absolute-ms capture stays deferred.

evidence: |
  - GasCoopGame main @b7d4226 (merges 675a6ff + b7d4226), dev @0886930; diff 5442be0..0886930 = +4186/-483,
    full Sc-kernel test suite + ADR-E-0002 + measurement JSONs. Suite 1275/1275; determinism scans green
    (zero-float + int-overflow incl. Core/Field/Coarse); mutation 80.6% (>=70); check.ps1 -Deliver exit 0.
  - Binding G5 (this session, 19 agents): verdict DEFECTS-FOUND = 2 P2 + 5 P3, 0 P1. P2#1 = scaling-benchmark
    generator git-ignored/uncommitted → measurement numbers (incl. flooded 70.788 ms that auto-opens the flood
    decision) not reproducible/falsifiable by any committed gate (ScKernelScalingMatrixSchemaTests asserts only
    shape/>0). P2#2 = AssertContiguousArrayAuthority (SparseDominantGuardrails.cs:42-56) non-recursive → misses a
    future nested-object / base-class-private authority. P3 = FULL-path SPEC wording vs Step-only timing (Step-only
    is the correct production ceiling); stale store_memory note; buoyancy framed as registry entry vs inlined
    _buoyStamp; stale InternalsVisibleTo comment; single-commit RED->GREEN.
  - Independent ZERO-LEGACY confirm (this session): DenseVoxelStepOracle internal-sealed, used only as the retained
    test oracle (VoxelField.cs:48); authoritative SparseStep body (913-1240) touches no dense-expand symbol.
  - Owner sign-offs on main: merge «мержим» (675a6ff, Anton); owner-eye live Play-mode (b7d4226); Unity absolute-ms
    defer + test-assertion amendments owner-acked (esc-c-exec-023-*).

state_changes:
  - NOW.md: updated -> 2026-07-03 by s-work-039.
  - NOW.md tasks[Sc-kernel].status: framed-awaiting-executor -> done; status_note rewritten.
  - NOW.md bet.current_truth rewritten (Sc-kernel DELIVERED+MERGED; road rolls through the Sc-kernel->Sc-reactions gap).
  - NOW.md open_calls: c-exec-023 -> CLOSED (comment); ADD c-exec-024. c-exec-021 + c-visual-003 unchanged.
  - NOW.md next_slices: Sc-kernel bullet -> the gap bullet (c-exec-024 + W1b per d-w1b-window-001).
  - NOW.md decisions[d-hangar-flood-fallback-001].status -> answered «да» (owner 2026-07-03): design + admission ceiling.
  - NOW.md history_pointers + next block updated.
  - knowledge/g9c41-gas-engine-SPEC.md §6 п.2: ceiling placeholder -> ~35 000 @50 ms / ~70 000 @100 ms (weak-peer x4;
    Step-only production ceiling; c-exec-023 flooded leg2) + caveat. owner_approved 2026-07-03 «да по всем».
  - work/c-exec-024-call.md: NEW cleanup CALL. LOG.md: append. history/2026-07-03-s-work-039-...md: this RESULT.

captures:
  - Routed-HOME follow-on legs from the c-exec-023 RESULT (parked): Z-normal aperture feature leg
    (_scratch/feature-request-z-normal-apertures.md); retire interim coarse far-tier (INTERIM-COARSE-FAR-TIER.md);
    optional c-020 buoyancy-strength tuning (16.6%-up heavy through a wide opening).
  - Process: even an owner-merged, owner-eye'd slice benefited from the truly-separate binding G5 — it surfaced 2 P2
    the same-session Sonnet G5 missed. Keep binding G5 a fresh session, never a same-session subagent pre-pass.

decisions_needed:
  - none new (the 4-item batch answered live «да по всем»). Still pending: d-marketing-wake-001, d-coop-interdependence-repin-001.

play_check:
  - 1 Recite: done — Sc-kernel goal + done_when + bet g-9c41 restated in the opening contract.
  - 2 Owner inputs (owner): engine slice, not owner-content; the CLOSURE was the owner decision — owner «да по всем» (2026-07-03).
  - 3 Do the work: done — binding fresh-session G5 (10 lenses + adjudication + synthesis) + independent ZERO-LEGACY grep-confirm.
  - 4 Self-check vs done_when: done — evidence checked point-by-point; behavioral gates real; gaps = completeness/reproducibility/honesty (kill_by NOT breached).
  - 5 Close: this RESULT.

log: |
  2026-07-03 — g-9c41/Sc-kernel binding fresh-session G5 close (s-work-039): c-exec-023 SOUND (0 P1), 2 P2 + 5 P3
  -> Sc-kernel done + merged main @b7d4226; c-exec-024 cleanup opened; d-hangar-flood-fallback answered
  (design+admission ceiling); SPEC §6 п.2 ceiling seated ~35k@50ms/~70k@100ms.

next:
  CALL c-exec-024 — outcome: the Sc-kernel measurement numbers are reproducible from a clean clone, the
  field-storage guard's guarantee matches its stated purpose, and every c-exec-023 artifact's wording matches the
  code. Fires in a FRESH GasCoopGame_dev session in the Sc-kernel->Sc-reactions gap, BEFORE c-exec-021. Full CALL:
  work/c-exec-024-call.md. Gap sequence after: W1b engine mini-CALL (d-w1b-window-001), then §Re-sync + full
  re-hardening + fire c-exec-021 (Sc-reactions). dev->main merge owner-gated.
```

## Binding-G5 confirmed defects (full, for the record)

**P2 #1 — scaling-matrix non-reproducible.** The generator `_scratch/sc-kernel-bench/ScKernelScalingBenchmark.cs.reference` is git-ignored and absent from the committed tree (`git ls-tree -r 0886930 _scratch` = only `.gitignore` + README; `git grep ScKernelScalingBenchmark @0886930` = empty). Artifacts call it "the committed, reproducible headless scaling benchmark" and cite `dotnet test --filter FullyQualifiedName~ScKernelScalingBenchmark`, which matches ZERO committed tests. The only committed gate (`ScKernelScalingMatrixSchemaTests.cs:31-64`) asserts shape/presence/>0 only. So load-bearing numbers — flooded `ms_per_tick_peak` 70.788 → `over_budget:true` auto-opening d-hangar-flood-fallback-001, checksum floor 26.76 ms, bytes/cell 1745.581 — are hand-maintained data no committed gate can reproduce or falsify.

**P2 #2 — field-storage guard non-recursive.** `AssertContiguousArrayAuthority` (SparseDominantGuardrails.cs:42-56) iterates `GetFields` of ONLY the passed type and trips `IsCollectionReference` on a DIRECTLY-declared field; it never descends into nested reference-type fields (net8 probe: NestedHelperDict tripped=False, DirectDict tripped=True; base-class private Dictionary also tripped=False). A future nested-object / base-class-private authority refactor would silently pass. Nothing wrong ships today: the real authority is `_records` (SparseDominantCellRecord[]), `_activeCells`/`_slotByCell` (int[]), value-type scalars — all direct + array/value, so the guard passes truthfully and every planted control trips.

**P3 (5, none blocking):** (3) buoyancy framed as a "wake-registry entry" but implemented as inlined `_buoyStamp` (VoxelField.cs:1097) — RESULT.md honest, behavior byte-verified 3000 ticks; (4) SPEC SHALL `spec.md:108-110` says gates time "the FULL path (Step + MeaningChecksum)" but they time Step only — and Step-only IS the correct production ceiling (production never checksums per-tick), so the SPEC wording is the defect; (5) stale `store_memory` note (json:238) attributes 343 MB to "still-ctor-allocated dense scratch" — wrong, it's the roster-independent sparse per-face pair arrays (~222 MB), lazy-alloc is real; (6) false "NO InternalsVisibleTo" comment `CoarseGuardTests.cs:1352-1353` (pre-existing, unrelated subsystem); (7) leg2 RED→GREEN + owner-amendment all in one commit e2fb9e5 — not git-verifiable as RED-first, though assertions non-vacuous by static reading.

END_OF_FILE: live/indie-game-development/history/2026-07-03-s-work-039-sckernel-binding-g5-close.md
