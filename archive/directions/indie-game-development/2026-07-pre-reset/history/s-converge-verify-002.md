# s-converge-verify-002 — converge-verify RE-RUN of g-9c41 Wave-2 §CONTRACTS (PASSED CLEAN)

Session: converge-verify · direction indie-game-development · node g-9c41 (Wave-2 §CONTRACTS — repaired set) ·
parent s-converge-003 (converge-arch repair; c-converge-003) · CALL c-converge-verify-002 · 2026-06-16 ·
surface: claude/cli — a FRESH session (G5), distinct from the c-converge-003 repair session AND its in-session
pre-pass (wf_b5e55d56-406).

This is the BINDING independent G5 re-refutation of the REPAIRED §CONTRACTS set (section H CR1/CR2/CR3 closing
F1 + re-scoped XL1/XL2 dissolving F2 + §RESOLVED-4 + GG4 bounds + CS3). Method: an independent adversarial
fan-out (14 agents — one skeptic per axis/adjudication prompted to BREAK the set, each finding cross-examined by
a second independent skeptic prompted to REFUTE it), with the fresh main session as the BINDING reviewer
adjudicating every finding against the VERBATIM LOCK (s-converge-verify-001 §APPENDIX) — not by vote. The full
reasoning is recorded both here and as §VERIFY-2 + §SIGNOFF in work/converge-g-9c41.md.

Headline: PASSED CLEAN — 0 BLOCK-severity holes. A single BLOCK was proposed on the no-leaning axis (CR1
coverage floor leans on the undefined term "entitled-query set") and was ADJUDICATED DOWN to CLEAN: the floor is
a universally-quantified falsifiable observable, and the proposed fix would itself be a firewall leak. 4
non-blocking CAPTUREs carried forward. The contract gate is LIFTED — shape (c-shape-wave2) may CONSUME the
verified contracts.

## RESULT

```RESULT
to: writer
direction: indie-game-development
play: converge-verify
node: g-9c41 (Wave-2 §CONTRACTS — repaired set)
parent: s-converge-003 (converge-arch repair; c-converge-003)

outcome: |
  converge-verify RE-RUN (binding fresh-session G5 refutation, c-converge-verify-002) of the REPAIRED §CONTRACTS
  set (section H CR1/CR2/CR3 closing F1 + re-scoped XL1/XL2 dissolving F2 + §RESOLVED-4 + GG4 bounds + CS3).
  VERDICT = PASSED CLEAN. An independent four-axis refutation (completeness / firewall / no-leaning /
  no-lock-reopen — firewall + no-lock-reopen narrowed to the new/edited entries; no-leaning + consumer-
  independence re-traced across every CR-citing contract) with the three CALL-mandated adjudications found 0
  BLOCK-severity holes. The F1 closure (coarse-tier replication + consistency, per-layer incl. coarse
  temperature) and the F2 dissolution (sink re-scope under the deferred feedback) HOLD with no new hole. Shape
  (c-shape-wave2) MAY now CONSUME the contracts as VERIFIED. 4 non-blocking CAPTUREs carried forward.

evidence: |
  - Independent adversarial fan-out (14 agents: 7 axis/adjudication skeptics prompted to BREAK the set, each
    finding adversarially cross-examined; the fresh main session = binding reviewer, adjudicating every finding
    vs the VERBATIM LOCK, not by vote). Net survivors after cross-examination + binding adjudication: 0 BLOCK,
    4 CAPTURE.
  - COMPLETENESS PASS — ORACLE-NMTL 25/25 independently re-confirmed (not by trusting the §REPAIR re-map): class
    6 (CR1/CR2/CR3) closes all three F1 silent PLAN escapes (host-only / float-hash-breaks-bit-exact / un-named
    coarse divergence bound); class 9 (XL2 coherent under the sink — locked C21 byte-identical holds LITERALLY
    for independent/sink layers); class 10 (XL1 sink + §RESOLVED-4 + locked bus — adjudication 3); class 20 (CR1
    coverage floor + OR2 entitlement-overlap — adjudication 1); class 22 (CS3 explicit named deferral). No missed
    class now that coarse replication is explicit (the tier-transition / newly-entitled-region race is covered by
    COMPOSITION: CR1 coverage floor + GG4/OR4 no-jerk-on-entry + class 13). Atomic split: crit-3 + crit-10 all
    clauses map; crit-9 7/8 map, the keyframe-inclusive bandwidth basis carried by the crit-9 text +
    knowledge/g9c41-wave1-hold-mechanism-lossy-projection + CR1/CR2 (→ CAPTURE V2-1, non-blocking).
  - FIREWALL PASS (new/edited) — observables carry properties/shapes, HOW→PLAN. XL1 MECHANISM-trim is REAL
    (moved to §RESOLVED-4 + PLAN; §RESOLVED-4 keeps delivery sync-pull-vs-subscription →PLAN). Closest borderline
    = CR3 "integer/fixed-point" — CONFIRMED CLEAN: it names the LOCKED I4/C1 integer-only-authoritative discipline
    EXTENDED to the new tier (frozen invariant), the representation magnitude →PLAN.
  - NO-LEANING PASS — the four F1 leans now cite CR1/CR2/CR3 (crit-3→CR1/CR2; crit-9→CR1 "the on-wire coarse
    cell-count crit-9 sizes IS this replicated state"; OR2-on-a-client→CR1 peer-to-peer consumer-independence;
    GG4/OR4→CR1/CR2/C22+OR1). CR1/CR2/CR3 own foundations trace to locked/signed facts (CR2→I7/I8/I10;
    CR3→I1/I4/C1/C19; CR1 host-only-decided-out→owner-signed read-gas mechanic + OR1/OR2). A BLOCK proposed on
    the "entitled-query set" term was ADJUDICATED DOWN: the coverage floor is a universally-quantified
    falsifiable observable (it holds for-all-entitled-queries without a closed-form entitlement set), and the
    proposed fix (pin each consumer's query domain in the contract) would itself be a FIREWALL LEAK — confirming
    the contract is right to route the entitlement-mechanism →PLAN.
  - NO-LOCK-REOPEN PASS (new/edited, vs verbatim LOCK) — CR1 surfaces a new coarse resolutionKey value + the
    barrier-table re-size as locked-wire-enum/table extensions per the LOCK's own SHALL-re-size (the standard
    verify-001 blessed for XL2); CR3 EXTENDS I4/C1 + references (not redefines) CellHash.FoldLayer + the [2,4]
    table; XL1 (feedback deferred) leaves GridEventKind + §T12 + C21 UNTOUCHED (pure sink, no temperature-sourced
    event); §RESOLVED-4 reads only COMMITTED revisions (never live mid-tick state → preserves locked
    single-writer-per-(LayerKey,phase)); CS3 names the future versioned surface without redefining it.
  - The three mandated adjudications resolved GENUINE ✓: (1) interest-set COVERAGE FLOOR escapes both horns (OR2
    is a tier-RESOLUTION floor, not spatial-entitlement-everywhere → no whole-level mandate; entitlement set
    UPSTREAM of the relevance mechanism → not a tautology; the "client far-AI querying an entitled region sees no
    gap" acceptance is the teeth); (2) coarse-temperature PER-LAYER discharge is real (CR1/CR2/CR3 carry it
    INLINE; coarse state = A8.8; host-solves/client-reconstructs = no client re-sim; NOT leaning on §T12; PUSH vs
    PULL distinct and the dependency stated); (3) class-10 ✓ INDEPENDENTLY CONFIRMED — the literal frozen crit-10
    10b "reaction/heat→temperature responds" is the gas→temperature SINK (feedback is the unwritten reverse
    arrow), satisfied NOW by XL1; the contingency is non-circular (clean verify unblocks shape → shape rewords
    crit-10 as G9 downstream).
  - Recorded as §VERIFY-2 + §SIGNOFF: converge-verify passed @ 2026-06-16 in work/converge-g-9c41.md.

state_changes:
  - file: live/indie-game-development/work/converge-g-9c41.md
    edit: |
      Append §VERIFY-2 (converge-verify RE-RUN — c-converge-verify-002): the PASSED-CLEAN verdict, the re-run
      method, the four-axis PASS, the three adjudications (genuine ✓), the 4 CAPTUREs, the play_check; then
      §SIGNOFF: converge-verify passed @ 2026-06-16 (c-converge-verify-002). END_OF_FILE trailer moved to the new
      end.
  - file: live/indie-game-development/NOW.md
    edit: |
      open_calls — c-converge-verify-002: status ready→done; note replaced with the PASSED-CLEAN verdict (0
      BLOCKs, 4 CAPTUREs, 3 adjudications genuine ✓) + pointers to §VERIFY-2 + this history file.
  - file: live/indie-game-development/NOW.md
    edit: |
      open_calls — c-shape-wave2: replace the ⚠ CONTRACT GATE clause with ✅ CONTRACT GATE LIFTED — §CONTRACTS
      VERIFIED (c-converge-verify-002 PASSED); the contracts are NO LONGER DRAFT — shape MAY bind them into
      executor done_when (copy the §CONTRACTS acceptance + §WHAT acceptance rows; carry the →PLAN rows as
      PLAN-agenda). Carry the 4 CAPTUREs (V2-1 keyframe-inclusive bandwidth basis; V2-2 entitlement anchored to
      gameplay-reach; V2-3 exercise the coarse temperature READ; V2-4 = THIS shape's G9 job: reword TREE
      done_when #10 to NAME the temperature→gas feedback deferral — non-gating TREE-honesty, the sink already
      satisfies the literal frozen crit-10).
  - file: live/indie-game-development/LOG.md
    edit: append one line for this session (link to this history file).
  - file: live/indie-game-development/history/s-converge-verify-002.md
    edit: create — this file.

captures:
  - "V2-1 (crit-9 bandwidth basis): the crit-9 PLAN/validation pass must compute coarse-grid bandwidth from the
     ON-WIRE keyframe-inclusive basis (~11k lossy cells — crit-9 refined text + the knowledge file + CR1/CR2
     coarse-keyframe-cadence →PLAN), NOT A8.4's literal 'measured dirty-chunk size' (a delta-only number = the
     ~5.3x optimistic basis the c-review-001 refinement rejects). A8.4 + the §COVERAGE-MAP crit-9 row are STALE
     w.r.t. the post-FORM-pass crit-9 refinement → freshen at the next converge-arch / audit touch (verify
     writes no contracts)."
  - "V2-2 (entitlement anchoring): keep CR1's 'entitled' anchored to gameplay-reach (actor position / render
     view per OR2/OR3), never silently re-bound to the streamed interest window (which would degrade the
     coverage floor to a tautology); the 'a client far-AI/visual querying an entitled region sees no gap'
     acceptance is the teeth."
  - "V2-3 (coarse temperature read): exercise the COARSE per-band temperature PULL (read-model on a committed
     revision) at coarse scale in PLAN tests, mirroring the coarse PUSH CR1/CR2/CR3 mandate — not only the fine
     per-room read the §RESOLVED-4 illustration shows."
  - "V2-4 (crit-10 TREE honesty): c-shape-wave2 G9 rewords TREE done_when #10 to NAME the temperature→gas
     feedback deferral (d-tempfeedback-001) so the done_when stops implying feedback — non-gating; a TREE-honesty
     obligation, not a contract gate. The sink already satisfies the literal frozen crit-10 (confirmed)."

decisions_needed: []   # verify passed clean; no owner G7 fork (the repair raised none; the re-verify confirms)

canon_proposed:
  - "ORACLE-NMTL — the 25-class checklist for a networked multi-TIER multi-LAYER host-authoritative
     field-simulation core. REUSED this re-run (per CALL); still PROPOSED to knowledge/ (serves: g-9c41 +
     future networked-sim nodes; read_by: converge-verify of this node-class). converge-verify proposes;
     review/pulse promote (verify never writes knowledge/)."

play_check:
  - "1 recite: stated the two propositions (repaired set COMPLETE; no answer leans/leaks) — done."
  - "2 attack-completeness (INDEPENDENT oracle): ORACLE-NMTL reused (re-proposed canon); 25/25 independently
     re-confirmed; crit-3/9/10 atomically split, all clauses map (crit-9 keyframe-basis = CAPTURE V2-1,
     non-blocking); no missed class on the repaired surface — PASS."
  - "3 attack-smuggling (firewall + no-leaning + no-lock-reopen): firewall PASS (HOW→PLAN; CR3 integer-discipline
     borderline confirmed clean; XL1 MECHANISM-trim real); no-leaning PASS (four F1 leans now cite CR1/CR2/CR3;
     CR1/CR2/CR3 trace to locked/signed facts; the proposed BLOCK on the 'entitled' term adjudicated DOWN — the
     floor is a falsifiable observable, the proposed fix would itself be a firewall leak); no-lock-reopen PASS
     (CR1/CR3 surface extensions per the LOCK's SHALL-re-size; XL1 leaves enum/§T12/C21 untouched; §RESOLVED-4
     reads committed-only) — checked vs verbatim ADR-0004 §LOCK + ADR-0003 C1–C22."
  - "3b adjudications (CALL-mandated): (1) class-20 interest-set COVERAGE FLOOR GENUINELY ✓ (closed falsifiable
     observable, not a deferral); (2) coarse-temperature PER-LAYER discharge of XL1 GENUINE ✓ (CR1/CR2/CR3
     per-layer inline, no §T12 lean, host-solves/client-reconstructs, PUSH/PULL distinct); (3) class-10 ✓
     INDEPENDENTLY CONFIRMED (XL1 sink satisfies the literal frozen crit-10; the rewording is a forward
     TREE-honesty mandate, non-gating, non-circular)."
  - "4 close: PASSED CLEAN — §SIGNOFF: converge-verify passed written; 0 BLOCKs; 4 non-blocking CAPTUREs
     forwarded; next = c-shape-wave2 CONSUMES the VERIFIED contracts. Not two-strikes (F1/F2 re-closed; this
     re-verify passes clean)."

log: "indie-game-development converge-verify g-9c41 Wave-2 §CONTRACTS (c-converge-verify-002): PASSED CLEAN — independent fresh-session re-refutation of the REPAIRED set (section H CR1/CR2/CR3 + re-scoped XL1/XL2 + §RESOLVED-4 + GG4 bounds + CS3) across 4 axes + 3 mandated adjudications, adversarially cross-examined (14 agents); F1 closure + F2 dissolution HOLD; ORACLE-NMTL 25/25 re-confirmed; 0 BLOCK-severity holes (a proposed BLOCK on the CR1 coverage-floor 'entitled' term adjudicated DOWN — the floor is a falsifiable observable; the proposed fix would be a firewall leak); 3 adjudications genuine ✓ (interest-floor closed; coarse-temperature per-layer discharged; class-10 sink satisfies the literal frozen crit-10, INDEPENDENTLY CONFIRMED); 4 non-blocking CAPTUREs forwarded. §SIGNOFF: converge-verify passed. Shape may CONSUME the contracts. next = c-shape-wave2."

next: |
  CALL c-shape-wave2 — shape Wave 2 of bet g-9c41 (on the LOCKed stream), now CONSUMING the VERIFIED §CONTRACTS.
  to: session   direction: indie-game-development   play: shape   node: g-9c41 (Wave 2)
  parent: s-converge-verify-002 (converge-verify PASSED CLEAN — the §CONTRACTS set is verified)
  goal: |
    g-9c41 Wave 2 exists as a committed, shaped bet (G6/G9): real coarse gas behaviour on real (small) generated
    levels, networked-consistent across host + clients on the LOCKed stream, the temperature layer riding
    alongside, the riskiest NEW assumption (the novel band solver) identified with a task that tests it first,
    and the bet ending in an artifact a human can read WITHOUT a debug overlay.
  context: |
    - The §CONTRACTS set is VERIFIED (converge-verify passed, c-converge-verify-002 — §VERIFY-2 + §SIGNOFF in
      work/converge-g-9c41.md). Shape COPIES the §CONTRACTS acceptance + the §WHAT `acceptance` rows into the
      executor done_when (binding by G5) and carries the `→PLAN` rows (CR1 carrier/interest-mechanism; CR2
      Q_coarse/divergence-bound/coarse-settle/coarse-mass-bound/seam-composition; CR3 coarse representation; the
      band-handoff prep-window; the cross-tier coordinate mapping) as the PLAN-agenda.
    - Carry the 4 converge-verify CAPTUREs: V2-1 (compute coarse-grid bandwidth from the ON-WIRE keyframe-inclusive
      basis ~11k, NOT A8.4's stale delta-only wording); V2-2 (keep CR1 "entitled" anchored to gameplay-reach,
      never the streamed window); V2-3 (exercise the COARSE per-band temperature READ in PLAN tests); V2-4 = THIS
      shape's G9 job: reword TREE done_when #10 to NAME the temperature→gas feedback deferral (d-tempfeedback-001)
      — the sink already satisfies the literal frozen crit-10 (confirmed), so this is TREE-honesty, NOT a gate.
    - The LOCKed Wave-1 foundation is fixed, do NOT re-open it: GasCoopGame ADR-0004 §LOCK + ADR-0003 v2 C1–C22.
    - Wave-1 learnings: knowledge/g9c41-wave1-*. Owner steers: d-generator-001 (DA structure day-one, PGG
      population later via the same adapter), d-bandhandoff-001 + d-tempfeedback-001 (read architecture
      §RESOLVED-4).
    - The re-shaped bet context lives in NOW.md active_bet (approach/wave_plan/rules/cut_list).
  boundaries: |
    Appetite wall stays 2026-07-24 (G3 — never extended; node is multi-wave, move tail waves not the wall). Do
    NOT re-open the Wave-1 HOLD verdict / the LOCK / C1–C22 / t-2 artifacts. Shape only — the build is separate
    executor legs. TREE/CHARTER edits reach state only via owner in-session approval (G9). The structural
    parallel-track tree work is c-map-003/c-map-004, NOT this shape.
  done_when: |
    A committed Wave-2 bet valid under G6 (appetite + kill_by + a cut list ≥1 real cut + a per-lens sweep verdict
    + a task testing the riskiest NEW assumption). The VERIFIED §CONTRACTS acceptance/contract rows are copied
    into the executor done_when (binding by G5). The two owner-mandated bet inputs are baked in: (1) a
    player-facing termination artifact legible without a debug overlay; (2) coarse-grid bandwidth sized from the
    ON-WIRE keyframe-inclusive rate (~11k). crit-10 reworded under G9 to NAME the feedback deferral. The 4
    converge-verify CAPTUREs are addressed in-shape or routed to PLAN.
  return: |
    shape RESULT — committed Wave-2 bet (G6/G9), tasks for the wave, next = executor CALL on the first task.
  budget: one session.
  surface: a session on this OS direction (chat or this repo via CLI); the owner is present for the shape (G9).
```

END_OF_FILE: live/indie-game-development/history/s-converge-verify-002.md
