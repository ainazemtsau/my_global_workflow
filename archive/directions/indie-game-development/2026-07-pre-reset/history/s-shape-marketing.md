# RESULT — s-shape-marketing (shape, g-2f8c marketing track)

play: shape   direction: indie-game-development   node: g-2f8c
parent: CALL c-shape-marketing (parked follow-up from s-map-003); owner present, co-creating (G9)

outcome: |
  Node g-2f8c is set up as a WORKING PARALLEL LOW-CEREMONY PROCESS — a direct analog of the canon track — NOT a
  second active bet (G1; the g-9c41 Wave-2 spine is untouched). Delivered:
  - local play `marketing-forge` (forge ONE marketing card from open→frozen: Frame → Research + owner-inputs →
    Cross-track check → Choose(owner) → Draft → Voice gate(owner) → Freeze(owner); canon-forge-style; 597 words).
  - render-only `marketing-status` (Kahn topo-sort over the graph; "one chat shows the next step").
  - the question-graph ledger in work/marketing/ (board.md + seed cards q-foundation / q-studio-identity /
    q-positioning-hook).
  - design doc work/marketing-track-design-2026-06-16.md (PROCESS only).
  CRITICAL FRAME (owner, repeated + finally locked «скоуп верный»): this session builds ONLY the PROCESS. The
  go-to-market plan, goals, numbers, channels, stages are DERIVED INSIDE the process later, one card at a time,
  in separate chats — NOT baked here. The owner has no marketing expertise; the process supplies it (research +
  his inputs); he judges fit/worth and signs the voice. Marketing CONTENT is deliberately absent from the node.

evidence: |
  - Files created/cleaned this session (procedure/product files, not state):
    plays/marketing-forge.md (597w ≤600), plays/marketing-status.md (301w);
    work/marketing-track-design-2026-06-16.md; work/marketing/board.md;
    work/marketing/questions/{q-foundation,q-studio-identity,q-positioning-hook}.md.
    Removed strategy-laden drafts (q-start-strategy, q-quiet-start, q-first-storefront-artifact, q-channel-plan).
  - EXTENDING budget: this direction now has 4 local plays (canon-forge, canon-status, marketing-forge,
    marketing-status) ≤5; both new plays ≤600 words (wc verified).
  - Owner approvals (G9, his words): scope = build only the process, marketing derived inside — «скоуп верный»;
    commit + merge to main + push — «сначала запушь всё, замержи в main и запушь».
  - State edits applied by the CLI writer (this session): TREE g-2f8c detail pointer (node stays parked);
    NOW open_calls += c-shape-marketing(done); LOG += one line. NOW spine (active_bet Wave 2 / active_tasks /
    next c-exec-005) byte-untouched.

state_changes: |
  TREE.md (G9 — owner-approved): g-2f8c `detail` gains a pointer to the process (the two local plays + the
    work/marketing graph + the design doc) and records that the plan is derived inside, not baked. Node `status`
    stays `parked`; goal/done_when UNCHANGED.
  NOW.md: open_calls += c-shape-marketing (status done, note). active_bet / active_tasks / recurring / next
    (c-exec-005) UNTOUCHED — the parallel track never touches the active bet (G1, canon precedent).
  LOG.md += one line. history/s-shape-marketing.md = this RESULT.
  No knowledge/ write (PROPOSE-only; none needed). No CHARTER change.

captures:
  - 3 deep multi-agent workflows ran DURING shaping (process-hardening wf_b0e30aa1; structure re-research
    wf_c68bbbe6 + wf_0b516278; deep current marketing-strategy wf_6038de20). They informed the PROCESS design,
    but their marketing CONTENT/strategy is deliberately NOT baked into the node (owner: build the node, derive
    the work inside). The strategy output sits only in the workflow transcript — available as a REFERENCE the
    process MAY pull from later, never as the plan.
  - work/marketing-research-2026-06-16.md remains a standing research REFERENCE (not the plan); the process
    derives goals/path per card.

decisions_needed: []   # none — the owner's four embedded judgment calls (hook from canon, EA pricing, recorder
  # build, gate reads) are CONTENT the process surfaces later, per card; not this session's decisions.

play_check:
  - 1 recite: done — g-2f8c node + parent restated; goal already an outcome (a working marketing system).
  - 2 appetite: done — NOT a bet, so no bet-slot appetite; cost = a thin parallel draw on the owner (voice +
    sign), like the canon track. Owner confirmed the parallel-track framing.
  - 3 approaches (owner): done — A (marketing-forge generative PROCESS, canon-forge-style) chosen over B (dated
    checklist) and C (build-byproduct); B folded as the delegable-logistics layer, C as a cross-track input.
    Owner delegated the mechanism choice («приму любой режим … чтобы было под меня») and confirmed «скоуп верный».
  - 4 scope hammer / cut list (≥1 real cut): done — design §6: CUT the baked go-to-market plan/stages/strategy
    (derived inside instead), CUT owner-written/AI-invented numbers (derived per card with basis), CUT the
    weekly cadence (event/stage-driven). All on owner steers.
  - 5 lens sweep: done — design §6: commercial/traction + audience-workflow = SERVED (the process is the machine;
    pre-mortems #6/#7); core-depth / co-op-first / technical / scope = not_needed as process work (owned elsewhere
    / held by process integrity).
  - 6 riskiest assumption: done — design §6: the process produces non-generic, in-the-owner's-voice output he can
    actually run without expertise. TEST = the first run (q-foundation) with a voice sample through the voice gate
    + owner verdict (живой/фейк).
  - 7 tasks: held — this is a parallel track, NOT a NOW bet (G1), so NO NOW tasks are written. The "next work" =
    the first process run (q-foundation), recorded in next.
  - 8 kill criteria: n/a as a bet — not a bet, no kill_by; the process self-corrects per card and is gated on
    "something real to say". Named honestly rather than invented.
  - 9 close (owner): done — owner approved the scope («скоуп верный») and instructed commit + merge-to-main + push;
    the only state change is the owner-approved TREE detail pointer + the NOW open_call close + a LOG line.
  - G1: held — no second active bet; NOW spine (Wave 2 / c-exec-005) untouched.
  - G2: held — TREE stays outcome-level; the marketing question-graph lives in work/marketing/, not in TREE/NOW.
  - G9: held — the TREE detail edit carries the owner's explicit approval; the process voice/plan is co-created,
    never AI-baked.
  - EXTENDING: held — 4 local plays ≤5; both new plays ≤600 words.

log: "shape (g-2f8c marketing → PROCESS set up, owner-approved G9): built the marketing track as a parallel low-ceremony PROCESS (NOT a 2nd active bet, G1), canon-forge-style — local plays marketing-forge + marketing-status + work/marketing/ seed graph (q-foundation / q-studio-identity / q-positioning-hook). Re-scoped 4× under owner steers: the plan/goals/channels/stages/numbers are DERIVED INSIDE the process (one chat = one card), NOT baked here; cadence event/stage-driven not weekly; voice-gate = owner closes (живой/фейк) + AI pre-check + humanize loop; cross-track both ways (pull canon hook, recommend dev — owner relays, never adds scope). 3 deep workflows informed the design; their marketing CONTENT deliberately NOT baked. Design = work/marketing-track-design-2026-06-16.md. NOW spine (Wave 2 / c-exec-005) untouched. next = first run = q-foundation in a fresh marketing-forge chat."

next: |
  First run of the process — owner-triggered, in a FRESH chat (one chat = one card):
  CALL c-mforge-001 — to: session   direction: indie-game-development   play: local/marketing-forge   card: q-foundation
  goal: forge the foundation card — inside the process (research + owner inputs, step by step) determine our real
    situation, realistic goals (derived with basis, not written/invented), and the path under us; spawn the child
    cards; run the voice gate on a sample (the first process-validity test).
  context: the marketing track is set up (plays/marketing-forge.md + plays/marketing-status.md + work/marketing/);
    g-9c41 stays the only active bet (G1); marketing fills parallel capacity, unevenly.
  return: one frozen foundation card + spawned children + a LOG line; owner's voice-verdict recorded.
  budget: one chat, one card.
  (Sibling parked follow-up from s-map-003 still open: c-map-004 — decide co-op-interdependence node (b).)

END_OF_FILE: live/indie-game-development/history/s-shape-marketing.md
