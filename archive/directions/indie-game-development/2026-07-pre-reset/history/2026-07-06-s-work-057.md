# RESULT — s-work-057 (work, g-9c41 / Sc-reactions) — 2026-07-06

Reconcile the c-exec-021 executor checkpoint (execution sessions 1–2) and frame the reaction-engine-completion
continuation CALL. Play: work. OS-worktree session (indie-game-development); no product code touched.

## outcome

The c-exec-021 executor checkpoint is RECONCILED into direction state and a re-planning continuation CALL is framed.
Verified FIRST-HAND in the product repo (`GasCoopGame_dev`) + a 4-agent adversarial verification pass
(wf_f85e9ed7-786) over the real `dev` code, which corrected the checkpoint in two material places and surfaced two
items it missed. The owner directive **"D+A" (2026-07-06)** is definitively grounded (quoted in dev commit `0de6cbe`):
commit the completed groundwork on `dev` (NOT merged, owner-gated); the reaction-ENGINE completion goes to a separate
re-planning session — which this CALL frames. Sc-reactions stays ACTIVE; c-exec-021 stays in-flight (checkpoint, not
a terminal return). G1 intact (1 active task).

## evidence

- **Product git (first-hand):** `main @0dd823b` (docs commit past `26dd062`); `dev @0a95f01` = two c-021 commits
  (`0de6cbe` groundwork + `0a95f01` folder meta) on base `26dd062`, **NOT merged**; `dev_2 @38dd61f` = the parallel
  c-visual-006 track. Checkpoint git claims CONFIRMED exactly.
- **"D+A" grounded:** `0de6cbe` body — "Per owner direction (D+A, 2026-07-06): commit this completed groundwork; the
  reaction ENGINE completion goes to a separate re-planning session." → phase = PLAN continuation, no fabrication.
- **Executor discipline verified:** owner-present PLAN 2026-07-06 (Q1–Q4 ratified) + `ADR-E-0006` + frozen
  `openspec/changes/c-exec-021-sc-reactions/` (proposal/spec/tasks) + the independent RED battery (11 test files).
- **Verification pass wf_f85e9ed7-786 (4 adversarial agents over real `dev` code):**
  - V1 (approach) **CONFIRMED**: generic data-index resolver keyed by canonical `ReactantKey` (branches only on int
    Tier), generic `Dictionary<int,IReactionOutcomeHandler>` outcome registry; **ZERO type-pair dispatch** anywhere;
    both anti-cheat REDs genuine. Approach token honest. *New:* the type-hardcode scan roots on `Core/Field/Types`
    (the RESOLVER) but NOT `Core/Field/Voxel/Reactions` (the ENGINE) → done_when #3 scan-scope gap.
  - V2 (config-K / admission / byte-identity) **PARTIAL**: store K=4 + 5th-type throw CONFIRMED, but the checkpoint
    OVERSTATES — the multi-cell/zone junction tests only demand no-throw+conservation (admission clamp with K=4
    satisfies); only the `SingleCellGrid` `NTypeJunction(5/8)` forces >4/cell (a SEED-time throw) = the genuine
    widen-K-vs-same-cell-policy PLAN fork. finding-3 CONFIRMED + sharpened: the fold is NOT skip-zero across lanes and
    folds `_speciesCount` → a naive widening would REBASELINE the frozen golden (STOP-class).
  - V3 (test-author vs engine bugs) finding-4 CONFIRMED (both tests = `Assert.Throws<System.Exception>` exact-type,
    engine throws correct derived types); **finding-5 RECLASSIFIED — NOT an engine bug** (ran it live): the reacted
    record is byte-identical to a fresh product-only record, mass conserved to the bit; the mismatch is the TEST's
    `freshField` built at `TwoReactantRegistry(threshold:100)` vs the reacted field's `threshold:1` → legitimate
    ContentHash divergence. Fix is test-side; do NOT hunt a phantom mass-leak.
  - V4 (gates/goldens) **CONFIRMED** (static + independent live headless run): 13-entry byte-identity goldens
    UNCHANGED `26dd062`→`0a95f01` (SAFE call-site edits, no rebaseline); non-reaction 1343/1343 GREEN; exactly 10
    reaction RED, all under `Field/Reactions/`.
- **Continuation CALL:** `work/c-exec-021-continuation-call.md` (phase PLAN, base `dev @0a95f01`).

## state_changes (NOW.md)

- `updated:` → `2026-07-06 by s-work-057`.
- `bet.goal` "Latest session" → `s-work-057 (c-exec-021 checkpoint reconciled; ...)`.
- `tasks[Sc-reactions]`: status STAYS `active`; goal gains a leg-1-groundwork-committed note; `unblock_when` → owner
  launches a fresh GasCoopGame_dev re-planning PLAN for the c-021 continuation (`work/c-exec-021-continuation-call.md`,
  base `dev @0a95f01`).
- `open_calls[c-exec-021]` note → CHECKPOINT RETURNED (stays in open_calls — in-flight checkpoint, NOT terminally
  returned/done): PLAN Q1-Q4 + ADR-E-0006 done, no-pair-dispatch engine confirmed, RED battery in place, groundwork
  committed dev@0a95f01 (D+A, not merged), non-reaction 1343/1343 green + goldens intact, 10 reaction RED → continuation.
- `next` → pointer to `work/c-exec-021-continuation-call.md`.
- No TREE.md / CHARTER.md change (G9 n/a). No decisions inbox change.

## captures

- Memory correction: the belief "33 fail on SeedMass/SingleRoom → then green" was WRONG — the reaction engine is
  substantially built; the 10 REDs are engine-completion gaps (admission clamp / config-K / skip-zero fold) + 3
  test-author fixes, not a KIT-then-green sequence.
- finding-5 mislabel risk: a checkpoint that calls a test-baseline mismatch an "engine bug (stale-TypeId/wider-record)"
  can send a builder hunting a non-existent defect — the return-verification (run-it-live) is what caught it.
- Recurring cross-track ADR-number hygiene is clean here (engine ADR-E-0006 vs visual dev_2 track — no collision).

## decisions_needed

None batched to the owner now. The 3 genuine design decisions (single-cell overflow widen-K-vs-same-cell-policy;
skip-zero-keeps-golden STOP-confirm; SeedMass-clamp public-contract nod) + the 3 test-author acks are FRAMED in the
continuation CALL for resolution at the fire-time owner-present PLAN (per owner "D+A" = a separate re-planning session),
each with a recommendation. That is the correct venue (code-context-present), not an OS-side batch.

## play_check (play: work)

1. Recite — DONE: Sc-reactions goal/done_when + bet g-9c41 restated in the opening contract.
2. Owner inputs (owner) — n/a: an engineering executor CALL, not owner-content (owner personally-lived artifact);
   one line, moved on.
3. Do the work — DONE: `call:executor` continuation CALL framed (`work/c-exec-021-continuation-call.md`), grounded
   first-hand + adversarial verification; NO solution designed here (architecture stays the fire-time PLAN's, owner
   present).
4. Self-check — DONE: reconciliation checked against the checkpoint's claims point-by-point (git state, goldens,
   1343/1343, 10 RED, approach) — all verified; two checkpoint mischaracterizations corrected with cited evidence.
5. Close — this RESULT; task stays ACTIVE (checkpoint, not last-task-done → no review CALL required); next = the
   continuation CALL.

## log

s-work-057 (2026-07-06) — work (g-9c41/Sc-reactions): reconciled the c-exec-021 executor checkpoint (verified
first-hand + 4-agent adversarial pass); groundwork committed dev@0a95f01 (D+A, not merged), non-reaction 1343/1343
GREEN + byte-identity goldens INTACT, generic no-pair-dispatch engine CONFIRMED; 10 reaction RED CORRECTED (finding-5
NOT an engine bug; finding-1 narrower; +KRaise test-design + engine-half scan-scope); framed the re-planning
continuation CALL → work/c-exec-021-continuation-call.md.

## next

CALL: `work/c-exec-021-continuation-call.md` — fire the c-exec-021 CONTINUATION (reaction-engine completion) in a fresh
owner-present GasCoopGame_dev re-planning PLAN, base `dev @0a95f01`. Short PLAN (decisions 1–3 + 3 test-author acks) →
EXECUTE leg-1-finish (10 RED → GREEN) → leg 2. dev→main merge/push OWNER-GATED. Parallel visual = c-visual-006.

END_OF_FILE: live/indie-game-development/history/2026-07-06-s-work-057.md
