# s-visual-006 — frame c-visual-002 (gas-lab + cloud-at-rest) on the engine contour

date: 2026-06-29
play: work (framing — author the next executor CALL, same workflow as the engine legs)
node: g-7e15 (VISUAL track — G1 intact, g-9c41 untouched)
owner-present: yes (challenged the process; chose «стартуем 0-1»)

## Trigger

Owner: «почему мы не работаем как по разработке, по нашему workflow (как над симуляцией)? ты какой-то свой рабочий
процесс придумал?» — a correct process-consistency challenge. I had handed a hand-written "paste this prompt" instead
of a proper executor CALL.

## Answer / correction

The visual track is NOT a separate process — it is the SAME OS contour as the simulation: OS authors a CALL → owner
opens it in a fresh dev session with a PLAN → build with gates → RESULT comes home. I shortcut it with a paste-prompt
(the same off-contour move that caused the original drift); corrected by framing a real CALL.

The ONE legitimate sim-vs-look difference is the binding acceptance gate, and it is a property of the task, not an
invented process: the SIMULATION is proven by objective HEADLESS tests (determinism / mass / mutation / fresh-session
G5 refutation) — so it carries a heavy independent-RED-test-author + adversarial-hardening round, because a determinism
bug is catastrophic and objectively testable. The LOOK cannot be unit-tested ("красиво/жемчужина" is the owner's EYE),
so a render-only look leg keeps the identical contour but is RIGHT-SIZED: binding gate = owner-eye «ближе к жемчужине»
plus the render-only regression invariants (Core diff empty, sim tests unchanged, stride/seam green); no heavy
refutation round (nothing correctness-critical to refute).

## RESULT

```
RESULT
session: s-visual-006
direction: indie-game-development
play: work (framing)
node: g-7e15 (VISUAL parallel track — G1 intact, g-9c41 untouched)

outcome: |
  Corrected an off-contour shortcut: the next visual step is framed as a PROPER executor CALL — the SAME workflow as
  the simulation legs (OS authors the CALL → owner opens it in a fresh dev2 session with a PLAN → render-only gates +
  owner-eye → RESULT home), not a hand-written paste-prompt. c-visual-002 = gas-lab (open-space cloud) + cloud-at-rest
  look (step 0+1 of GasCoopGame docs/gas-visual-stage-plan §S6+); render-only; RIGHT-SIZED (binding gate = owner-eye
  «ближе к жемчужине» + render-only invariants; no heavy adversarial hardening). Owner approved starting step 0+1.

evidence: |
  work/c-visual-002-call.md authored (goal/context/scope/boundaries/done_when/return/budget; render-only invariants +
  owner-eye binding). NOW.md open_calls c-visual-002 = framed; the contour matches the engine pattern (cf. c-exec-018).
  No code built this session (framing only). Reconciled with the concurrent read-only Gas-Visual R&D Center (1b3ea58):
  research feeds ideas, c-visual-002 is the build — no conflict.

state_changes: |
  NOW.md: open_calls += c-visual-002 (framed); g-7e15.next folds the owner-approved gas-feature-first step ladder
  (0 lab → 1 rest → 2 spawn → 3 jet → 4 types → 5 behaviour; later particles/Feel + perf) + "start 0+1" + the
  workflow-identical-to-engine note. LOG append. history/s-visual-006.md. NO TREE/CHARTER change (G9 not triggered);
  G1 intact.

play_check:
  - "Frame the next CALL as an outcome (goal carries no method paraphrase): DONE."
  - "(owner) owner asked why the visual isn't on the sim's workflow + chose to start 0+1 — corrected to the proper
    contour; his words «стартуем 0-1»."

log: framed c-visual-002 (gas-lab + cloud-at-rest, step 0+1) as a proper executor CALL on the SAME contour as the
  engine legs; corrected the paste-prompt shortcut; named the one legit sim-vs-look gate difference; owner starts 0+1.

next:
  call: c-visual-002 (work/c-visual-002-call.md) — owner opens it in a FRESH GasCoopGame_dev_2 session, opens with a
  PLAN (owner present).
END_OF_RESULT
```

END_OF_FILE: live/indie-game-development/history/s-visual-006.md
