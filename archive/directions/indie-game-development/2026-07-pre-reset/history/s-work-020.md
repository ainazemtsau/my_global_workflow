# RESULT — s-work-020 (work/reshape · g-9c41/S1 · deep-research → reframe S1 to FORCED-FLOW)

play: work (reshape of the active task, owner-steered)
direction: indie-game-development
node: g-9c41
task: S1 (reframed)
date: 2026-06-26

## outcome

S1 is REFRAMED from "выброс + выдавливание" to the GENERAL FORCED-FLOW primitive, after the owner pushed back that
gas richness must not be a hard compromise (the gas sim is the game's "heart") and that geometry-splitting + an
event-impulse + fixed-point math are all viable. An owner-requested deep-research workflow confirmed it and produced a
3-tier richness architecture; the owner approved («ок») building TIER 1 now. The c-exec-015 CALL is rewritten around
it, with a STEP-0 de-risk spike first.

## evidence

- Deep-research workflow (20 agents, ~2M tokens, web + code, adversarially verified): 7 landscape angles → 3 architect
  candidates → skeptics stress-tested each on determinism / weak-hardware / does-it-deliver → judge synthesized. Full
  digest saved to `work/gas-richness-deep-research-2026-06-26.md`. Verdict ranked "Forced-Flow Hybrid" #1 (ships inside
  the locked integer-lockstep architecture, no lock-crack, lowest cost, == S1 generalized).
- The verification caught + corrected real overclaims (folded as the 4 load-bearing corrections): fork-split and
  persistent-split are the same physics (not two wins); "momentum" cheap = a decaying gust, not coasting inertia; the
  Kp≥2*degree contraction proof is SUSPENDED during forcing (not preserved); the bias decay-tail inflates the active-set.
- Rewritten build-ready CALL: `work/c-exec-015-call.md` (STEP-0 spike gate first; bias FACE-LEVEL-UNIFORM per the §9.5
  trip-wire; NO velocity field; carried H1/H2).

## the 3-tier model (decision d-gas-richness-tiers-001, owner «ок» to tier 1)

- TIER 1 — BUILD NOW (= S1/c-exec-015, no lock-crack): integer IMPULSE-EVENTS write a DECAYING directional-bias register
  on the S0 gradient face-flow → AUTHORITATIVE wind + decaying momentum-hint + transient fork-split + DESIGNED permanent
  split via a face-level one-way VALVE. Owner's IDEA 1 (a deterministic split-impulse EVENT in the grid) = the keystone,
  zero new wire traffic.
- TIER 2 — DEFERRED (ADR-class, post-sparse-solver): seeded-integer lattice-gas (FHP/ILG) in a few bucket-3 rooms for
  true emergent vortices as shared truth; vortices elsewhere = the read-only cosmetic g-7e15 visual layer fed the bias
  field (zero gate risk).
- TIER 3 — RESERVED (owner-gated, cracks the lock): fixed-point (Q-format) velocity field for true stored inertia +
  graceful permanent split. Owner's IDEA 2 («float via 2 ints» = fixed-point) is real + proven (Photon Quantum /
  StarCraft / FixPointCS) but bespoke + heavy + IS the velocity/advection the roadmap names a STOP-escalation → needs an
  owner-signed ADR-0010. Triggered ONLY if owner-eye proves tier 1 isn't alive enough. NOT adopted now.

## state_changes

- NOW.md active_tasks S1: kind/goal/done_when/status REFRAMED to the forced-flow primitive + the STEP-0 spike-first gate.
- NOW.md open_calls c-exec-015: status comment + note rewritten (reframed; CALL rewritten).
- NOW.md decision_inbox: ADD d-gas-richness-tiers-001 (answered, owner-steered «ок»; the 3-tier model + the 4 corrections
  + the library shortlist).
- NOW.md phase: prepend the s-work-020 entry. NOW.md next: rewritten (open the reframed c-exec-015, step-0 spike first;
  reserved tiers + deferrals carried).
- LOG.md: one line. work/c-exec-015-call.md: rewritten. work/gas-richness-deep-research-2026-06-26.md: created (the
  research digest).

## captures

- KNOWLEDGE-PROMOTION CANDIDATE (for review/pulse): the 3-tier gas-richness architecture (impulse-event/directional-bias
  NOW / lattice-gas rooms / fixed-point velocity) + the verified boundary (what is authoritative-cheap vs reserved vs
  cosmetic-only) → durable; basis = work/gas-richness-deep-research-2026-06-26.md.
- The S0-era findings memory was updated this session-arc (S0 delivered + hangar measured); the richness model extends it.

## decisions_needed

None blocking. The owner steered + approved («ок») tier 1; the reserved tiers are named and owner-gated (no decision
needed until owner-eye on tier 1). The maintenance request (os/engineering) remains pending a separate session.

## play_check (play: work / reshape of the active task)

1. Recite — DONE: restated S1; the owner's pushback + the deep-research basis.
2. Owner inputs (owner) — DONE: this WAS owner-steered; the owner contributed two design ideas (split-impulse event,
   fixed-point), both validated and folded; the reframe was approved in-session («ок»). The build mechanism (CALL) is a
   machine artifact, not owner-content.
3. Do the work — DONE: ran the owner-requested deep-research (workflow), synthesized the 3-tier model, rewrote the CALL,
   recorded the decision. No code; planning/framing only.
4. Self-check — DONE: the CALL + decision trace to the verified research; the 4 corrections are folded; honest caps set;
   the STOP-escalate boundary on the velocity-field tier is explicit.
5. Close — DONE: this RESULT; next = the rewritten c-exec-015 (step-0 spike first).

## log

2026-06-26 — work/reshape (g-9c41/S1, s-work-020, owner «ок»): S1 reframed to FORCED-FLOW after an owner-requested
deep-research (20-agent workflow, adversarially verified) → 3-tier richness model (d-gas-richness-tiers-001), tier 1
built now (impulse-events + decaying directional bias = wind + gust + designed split, no lock-crack); tiers 2/3
(lattice-gas rooms / fixed-point velocity) reserved owner-gated. c-exec-015 CALL rewritten, step-0 de-risk spike first.

## next

CALL c-exec-015 (rewritten) — S1 FORCED-FLOW, build-ready at work/c-exec-015-call.md. Owner opens a fresh GasCoopGame_dev
session, opens with a PLAN, runs the STEP-0 de-risk spike FIRST (RED settle oracle + symmetric decay + loopback hash +
owner-eye on a forked corridor + vent); only if green, builds выброс/выдавливание/ветер as the first impulse types.
RESULT applied home by a separate OS writer.

END_OF_FILE: live/indie-game-development/history/s-work-020.md
