# s-shape-wave2 — shape g-9c41 Wave 2 (committed bet, G6/G9)

date: 2026-06-16
play: shape
node: g-9c41 (Wave 2)
parent: s-converge-verify-002 (converge-verify PASSED CLEAN — §CONTRACTS verified)
hardening: wf_d2c5dcb5-0ab (10 agents — 3 approaches generated + judge-ranked on equal footing; 6-dimension
  adversarial G6 stress-test, default-to-refute; 3 blockers + ~10 majors surfaced, ALL folded)

---

## RESULT

```
session: s-shape-wave2
direction: indie-game-development
play: shape
node: g-9c41 (Wave 2)
parent: s-converge-verify-002 (converge-verify PASSED CLEAN — §CONTRACTS verified)

outcome: |
  g-9c41 Wave 2 is a committed, shaped bet (G6/G9, owner «го»). Approach = BUILD the genuinely-novel coarse
  stratified band solver on the ALREADY-LOCKED Wave-1 stream (ranked 8.5 over 2 alternatives on equal footing,
  wf_d2c5dcb5-0ab; the alternatives are its fallback ladder, not rivals). The VERIFIED §CONTRACTS acceptance is
  bound into the executor done_when (binding by G5); the →PLAN rows + 4 CAPTUREs ride the PLAN agenda. Two
  owner-mandated inputs baked in: (1) a player-facing termination artifact legible WITHOUT a debug overlay (t-4);
  (2) coarse-grid bandwidth sized from the ON-WIRE keyframe-inclusive ~11k basis (V2-1). crit-10 reworded under
  G9 to NAME the temperature→gas feedback deferral (V2-4). Owner refinements folded: generator-agnostic topology
  (scene+tags → TopologyDocument, not a per-generator API adapter), DA in room-composer mode with hand-made tagged
  room prefabs, gas-cell size configurable + geometry-aligned, T3/12.5cm dropped to not-planned (finest = 25cm).

evidence: |
  - Approaches generated + judged on equal footing (shape step 3): wf_d2c5dcb5-0ab — BUILD 8.5 / Well-Mixed Scalar
    6.5 / Hand-Authored Fixture 5; winner BUILD scoped to the brief §5 day-one cut.
  - Adversarial G6 stress-test (default-to-refute, 6 dimensions, same workflow) surfaced 3 blockers + ~10 majors;
    ALL folded into the bet (t-1 split + scale-realistic graph; GG4/OR4 split coarse-in / fine-deferred;
    OR1/OR3 two-consumer probe; CR3 float presupposition removed; IN2 + GT deferral named; date-ladder + t-2-owns-
    breach so t-4 parallelizes; single binary kill predicate; honest degrade ladder; co-op/depth lens honesty;
    crit-9 arithmetic task; crit-7 1-player; independent-test-author/G5 kill-gate regime).
  - Bet passes G6: cut list with a real fresh cut (T2-detail → Wave 3) + named-deferrals; per-lens verdict for all
    6 charter lenses; a task (t-1) testing the riskiest NEW assumption first + cheapest. G4: kill_by = single binary
    predicate + threshold + 06-30 checkpoint + 07-24 wall. G1: ≤3 active (t-1 active, rest queued/blocked).
  - Owner approvals (G9): bet + crit-10 rewording «го»; dates «по срокам в принципе окей»; crit-10 wording delegated
    «ты сам смотришь, чтобы соответствовало»; approach + DA/gas-cell/topology refinements iterated to «го».

state_changes:
  NOW.md:
    - active_bet: replaced phase/appetite/approach/done_when/wave_plan/cut_list/lens_verdicts/kill_by/early_finish/
      forecast/against with the Wave-2 bet (BUILD; 8-clause done_when binding the §CONTRACTS; kill_by single binary
      predicate + degrade ladder + evaluator/rollback named). rules unchanged (R12–R15 + vendor + R5/R15).
    - active_tasks: t-1 (active, KILL-GATE), t-2 (blocked_on t-1), t-3 (queued, ∥ t-4), t-4 (queued, ∥, depends t-2).
    - open_calls: c-shape-wave2 → done; + c-exec-005 (t-1 executor CALL) ready.
    - decision_inbox: d-generator-001 note appended with the s-shape-wave2 refinement (DA room-composer + scene+tags).
    - next: CALL c-exec-005 (t-1).
  TREE.md:
    - g-9c41 done_when #10 reworded (G9, owner «го» + delegated wording): names the temperature→gas feedback
      deferral, qualifies "at coarse scale" + per-layer CR1/CR2/CR3, marks the seam EXERCISED not argued, keeps the
      real-breach clause, adds the "proves the SEAM not gameplay depth" caveat. crit-9 untouched (owner-set at review).
  LOG.md: one line appended.
  history/s-shape-wave2.md: full RESULT saved.

captures:
  - "DA strategy refined (owner «го», → d-generator-001 + PLAN): use DA in ROOM-COMPOSER mode (the recently-added
     room-placement layer), not Tiles; rooms = hand-made tagged prefabs now / PGG-generated interiors later behind the
     SAME generator-agnostic seam. Topology read from (scene-geometry + semantic tags/markers) → TopologyDocument, NOT
     DA's stale 2D API — decouples us from the vendor and from DA's vertical-not-in-API limitation."
  - "PGG-vs-DA actuality = an OPTIONAL future research session (owner: «можно отдельно запустить ресёрч») when room
     interiors come up — NOT needed for Wave 2; population stays OUT (d-generator-001)."
  - "Gas detail-tier cell size (Wave 3 / PLAN): MUST be configurable, NOT hardcoded; default 25cm, dial-able to 50cm;
     the gas cell must equal-or-be-a-clean-divisor-of the geometry/occupancy grid so it aligns and can fill sub-cell
     recesses (owner: 25cm notches can't be filled by 50cm gas). Wave-2 forward constraint: don't hardcode resolution."
  - "T3 (12.5cm hero-bubble) DROPPED to not-planned: it was a parked brief hypothesis with no requirement; in 3D it is
     ~8× cells (2³). Finest committed tier = 25cm (T2, Wave 3). T3 stays a free-to-add-later level under the same
     thinnest-open-level rule ONLY on measured need (owner-confirmed)."
  - "V2-1 staleness (→ next converge-arch / audit touch on work/converge-g-9c41.md, outside shape's write scope): A8.4
     'measured dirty-chunk size' + the §COVERAGE-MAP crit-9 row are STALE (delta-only) vs the on-wire keyframe-inclusive
     ~11k basis; freshen them. Bound into t-1(d) done_when meanwhile."
  - "Large/tall spaces (hangars 12–24m, shafts, long corridors): window radius is player-bounded, unaffected by room
     height; the coarse tier carries everything outside the window. Full subdivision of huge volumes (z-slabs/grids) =
     later-scale work; Wave-2's ~3,300-sector arithmetic (t-1) already includes the 4 hangars of the R1 profile."

decisions_needed: []   # all owner forks resolved in-session («го»)

play_check:
  - "1 recite: restated g-9c41 Wave-2 goal + parent g-a7f2; node goal is an outcome, not an activity — OK (done)"
  - "2 appetite (owner): node wall fixed 2026-07-24 (G3, multi-wave); set Wave-2 kill checkpoint 06-30 + terminus
     rides to ~07-21. Owner «по срокам в принципе окей» (done)"
  - "3 approaches (owner): 3 structurally-distinct approaches generated + ranked on equal footing (wf_d2c5dcb5-0ab,
     BUILD 8.5 / Scalar 6.5 / Fixture 5); BUILD picked, losing ideas → captures (degrade fallback, week-1 legibility
     pre-test). Owner approved BUILD «го» after re-explanation (done)"
  - "4 scope hammer: cut list = a real fresh cut (T2-detail → Wave 3, it hurts: t-4 renders coarse-only, GG4/OR4 fine
     handoff unexercised) + inherited deferrals named separately (done)"
  - "5 lens sweep: verdict for all 6 charter lenses — commercial=TASK(t-4), technical=TASK(t-1), core-depth=seam-
     exercised-but-feedback-deferred, co-op=NOT-advanced-named-deferred(c-map-004), scope=cut-list+concurrency,
     audience=t-4. No silent skip (done)"
  - "6 riskiest assumption: novel band solver (brief §7-#2, zero precedent) + CR3 integer-exact reduction; t-1 tests
     both FIRST + cheapest, headless, no net/DA, on a scale-realistic graph (done)"
  - "7 tasks: t-1..t-4, riskiest first, kinds = executor; t-4 parallel work-stream; evaluator (independent test-author
     + fresh-session G5 kill-gate) + rollback (scalar-per-volume degrade) named (done)"
  - "8 kill criteria: single binary metric + threshold + 06-30 checkpoint + 07-24 wall; next_if_true/false ladder (done)"
  - "9 close (owner): full bet shown on one screen, iterated 3 rounds for clarity; owner «го» on the bet + crit-10
     rewording (G9 owner_approved); crit-10 wording delegated «ты сам смотришь, чтобы соответствовало» (done)"

log: "shape (g-9c41 Wave 2 → committed, G6/G9, owner «го»): BUILD real coarse band-sim on the LOCKed stream; 4 tasks (t-1 band-solver KILL-GATE first/headless/scale-validated → t-2 real DA-composed level+breach+coarse replication+single-oracle probe → t-3 temperature layer plugs in → t-4 player-legible render terminus ∥); VERIFIED §CONTRACTS bound into done_when (CR1/CR2/CR3+XL1/XL2+GG3/GG4+IN1/IN2+OR1/OR3), 4 CAPTUREs folded; kill_by single binary predicate (06-30) + scalar-per-volume degrade; crit-10 reworded (G9) to NAME the temperature→gas feedback deferral; owner refinements (generator-agnostic topology via scene+tags, DA room-composer, configurable+aligned gas cell, T3/12.5cm dropped). next = c-exec-005 (t-1)."

next: |
  CALL c-exec-005 — Wave-2 t-1: band-solver KILL-GATE (headless, scale-validated, integer-exact-replicable).
  to: executor (coding agent)   direction: indie-game-development   product repo: GasCoopGame (branch dev → main when green)
  play: work (executor leg) — g-9c41 Wave 2, task t-1   parent: s-shape-wave2
  goal: |
    Prove the novel coarse band solver FIRST + cheapest — numerically stable, in-budget at the ~3,300-sector R1
    scale, and deterministically reducible to the LOCKed integer-exact state so it replicates host==client — with
    ZERO network/DA/Unity dependency. The riskiest NEW assumption (brief §7-#2, zero precedent) dies before any
    real-level/temperature work. + a week-1 owner-acceptance legibility pre-test.
  context: |
    - Build ON the LOCKed foundation, do NOT re-open it: GasCoopGame ADR-0004 §LOCK + ADR-0003 v2 C1–C22.
    - VERIFIED §CONTRACTS (binding by G5): work/converge-g-9c41.md §CONTRACTS — esp. CR1/CR2/CR3 (section H) + the
      §VERIFY-2/§SIGNOFF. Architecture-on-paper rides PLAN as INPUT EVIDENCE: brief §3.1 (the solver), §4 (R1 numbers
      ~3,280 sectors / ~7,000 band cells), §7 (#2 solver tests, #3 throughput anchor); §EMERGENT Q5 in
      work/converge-g-9c41-arch.md (CR1/CR2/CR3 derivation).
    - Bandwidth basis = ON-WIRE keyframe-inclusive ~11k cells (knowledge/g9c41-wave1-hold-mechanism-lossy-projection +
      crit-9 refined text + CAPTURE V2-1), NOT A8.4's delta-only number. CR3 is firewall-clean: do NOT presuppose float.
    - The Wave-2 bet (approach/done_when/wave_plan/cut_list/kill_by) lives in NOW.md active_bet.
  boundaries: |
    Headless / engine-free C# only (R13) — NO Unity, NO DA, NO network harness in t-1. NO re-opening the LOCK /
    C1–C22 / t-2/t-3 artifacts. G0 ledger frozen default-FAIL BEFORE the spike; builder never edits ledger/spec/
    criteria; the §7-#2 battery + CR3 negative test + CR1/CR2 oracles authored by an INDEPENDENT test-author from the
    contract acceptance rows. Open HOW magnitudes (Q_coarse, bit-width, settle predicate, reduction rule) → ratified
    at the interactive PLAN (owner present), not invented.
  done_when: |
    (a) brief §7-#2 battery PASSES on a scale-realistic procedurally-generated synthetic sector graph (~3,300 sectors):
        sealed-room+source; two-room multirate mass-drift <1e-9/hr; cold-rung checkerboard (0 flow-sign-flip >2
        consecutive ticks + total-variation non-increasing); interface-crossing; hot-CO2 inversion to ceiling;
        Steckler monotone interface descent; AND the quasi-steady pressure solve converges in-budget (≤4ms, or
        ≤6–8ms) at ~3,300 nodes WORST-CASE post-breach.
    (b) CR3: the chosen integrator reduces DETERMINISTICALLY to exact integer/fixed-point authoritative state before
        hashing/wiring; a non-deterministically-reduced float path FAILS the host==client lossless oracle (negative
        test on a cell with realistic multi-species flux summands — non-vacuous).
    (c) §7-#3 K-species throughput micro-bench on a min-spec profile (capped/3060 proxy allowed).
    (d) crit-9 scale arithmetic vs the R1 profile, bandwidth from the ON-WIRE keyframe-inclusive ~11k basis (V2-1),
        ≥2 independent recomputes + named assumptions + aggregate-clamp-at-scale check.
    (e) WEEK-1 pure-local (R14, zero net) owner-acceptance legibility pre-test ("does this read as real coarse gas?")
        — a NO converts the wave early (degrade).
    Binding kill-gate verdict at the ~06-30 checkpoint = an independent fresh-session G5 refutation (Codex/GPT-5.5
    pre-pass allowed; binding = fresh session) scored against the frozen default-FAIL G0 ledger.
  return: |
    executor RESULT — evidence = commits/PR + dotnet headless gates green + the committed numerical battery + the CR3
    negative test + the crit-9 arithmetic artifact + the owner legibility-pretest verdict; kill-gate verdict
    (PASS → t-2; FAIL → next_if_false(i) degrade to scalar-per-volume).
  budget: to the 2026-06-30 checkpoint (band-solver kill-gate). Owner present for the interactive PLAN (the ADR ratifies the HOW).
  surface: a fresh executor session (coding agent) in the GasCoopGame product repo.
```

END_OF_FILE: live/indie-game-development/history/s-shape-wave2.md
