# s-research-013 — g-9c41 — research (deepen limits: 40 rooms, optimizations, LOD ladder, vertical-as-authority)

date: 2026-06-22
play: research
direction: indie-game-development
node: g-9c41
input: owner follow-up — clarifies verticality 12-16m = STACKED FLOORS (≈3 rooms on top of each other, each ~4m), NOT a tall open hall. Three questions: (1) what does "40 rooms" mean concretely (with corridors)? (2) with the most-optimized optimizations that don't touch core things, how much can we improve? (3) how much do performance AND scale improve if distant rooms degrade in precision (T0 near / T1 2.5D / T2 1m+2.5D / far super-simplified)? Wants to understand limits deeply to gameplay-constrain.

---

## RESULT

```
play: research
goal: ответить на 3 вопроса владельца (что такое 40 комнат + стопочная вертикальность; выигрыш бескомпромиссных оптимизаций; выигрыш LOD-деградации T0/T1/T2) реальными числами под исправленную модель (этажи стопкой, не залы)

outcome:
  5-агентный веер (wf_a8d20786-f3d) + 2 адверсариальных скептика. Выводы:
  (1) "40 комнат" = ~40 камер (~150м³) + ~48 коридоров ≈ 7440 м³ = между LC medium(6k)/large(12k); плотный нормальный-к-большому уровень, пробежка ~2 мин.
  (2) СТОПКА ОБЪЁМ-НЕЙТРАЛЬНА: 3 комнаты стопкой = 3 в ряд = те же клетки; вертикальность стопкой добавляет 0 клеток; переток между этажами = поток через открытую грань шахты (дёшев на любом размере). Раздувает только открытый зал — теперь редкий.
  (3) 40 комнат @50см = 59.5k клеток → НЕ стена даже в worst-case (60k/4-6 ядер = 10-15k/ядро/тик@20Гц, глубоко в полосе 32-120k). Стена там НЕ CPU.
  (4) "Бесплатные" оптимизации НЕ перемножать наивно: AVERAGE ~×30-100 (доминирует сон), WORST (манипуляция=№1) только ×8-20 и СОН=×1 (frontier §A). Бюджетировать по worst-case.
  (5) LOD-лестница: стоимость ∝ ближним комнатам ≈ константа (на машину = грубое всего уровня + ОДИН локальный пузырь ~46-60k @50см); масштаб ~40→~300-400 комнат (~×8-10); цена = возврат re-flux/collapse/R33/LOD-тегов.
  ★ ДВЕ ПОПРАВКИ СКЕПТИКОВ (несущие):
  (A) Лок УЖЕ выбрал LOD-лестницу — T0/T1/T2 = залоченная модель (S4 coarse-rollup+LOD = ЗАПЛАНИРОВАННЫЙ слайс, не опциональный рычаг). "Равномерно/Alt-5.3 как дефолт" = ПРЕДЛОЖЕНИЕ пересмотра лока, НЕ пересказ канона. Малый масштаб лишь делает далёкий тир почти-лишним → решает ангар-проба → упрощение = re-shape (решение владельца).
  (B) "Присесть под слой" НЕ бесплатна и НЕ косметика: чтобы вертикаль реально меняла что бьёт игрока, разрешение по высоте у тела должно быть АВТОРИТЕТНЫМ (в чексумме). Развязка = авторитетные высотные ПОЛОСЫ у пола (ведро-2, 2-3 полосы, дёшево/детерминированно, НЕ полное 3D). Это РЕШЕНИЕ, не бесплатно → новое d-vertical-bands.

evidence:
  - Workflow wf_a8d20786-f3d (5 агентов + 2 скептика, 433k токенов).
  - АРИФМЕТИКА (скептик-numbers: воспроизведена точно): 7440 м³ = 476k/59.5k/7.4k @25/50/100; стопка объём-нейтральна; сценарии C=14440/D=12240; r³-скейл пузыря.
  - ОПТИМИЗАЦИИ (скептик): worst-case ×8-20 (не ×30), sleep=×1 при манипуляции, multicore ×4-6 суб-линейно, НЕ перемножать (разные члены уравнения C≈Σ(awake·cells·faces·op)·tick).
  - ПОПРАВКИ ЧИСЕЛ: 369k-пузырь = r≈11м (истинный r=12 сфера ~463k, оптимистично); полоса 32-120k scalar-или-Burst неясна (та же whole-CPU ×4-8 неоднозначность); LOD scale per-MACHINE = грубое + 1 пузырь (не сумма по пирам) → LOD ДЕШЕВЛЕ заявленного.
  - ПОПРАВКА ЛОКА (скептик-arch): рекомендация "Alt-5.3 дефолт" конфликтует с knowledge/g9c41-architecture-locked-slices (cell-LOD залочен) + dev-plan-graph (S4=запланирован) → подавать как предложение пересмотра, не канон.
  - Продукт: work/gas-cellsize-levelscale-2026-06-22.md §9 (дополнение).

state_changes:
  - work/gas-cellsize-levelscale-2026-06-22.md: +§9 (углубление: 40 комнат конкретно + стопка-нейтральна + сценарии A-D + оптимизации average/worst + LOD выигрыш/цена + 2 поправки скептиков + 3 оси вертикали + пределы + d-vertical-bands).
  - LOG.md: +1 строка (s-research-013).
  - history/s-research-013.md: этот файл.
  - NOW.md / залоченная архитектура / ставка S0: НЕ ТРОНУТЫ (research информирует).

captures:
  - "Соединённая открытыми шахтами вертикальная стопка = потенциально НЕ-засыпающий регион (буянси-переток по всей высоте = непрерывное движение газа, frontier §A). Объём тот же, но awake-set по такой цепочке может быть полным → бюджетировать как worst-case при шейпе S3 (высота)."
  - "S0/S3-PLAN: решить число и высоту АВТОРИТЕТНЫХ высотных полос (ведро-2) для 'присесть под слой' ДО первой вертикальной механики — иначе вертикаль тихо станет косметикой ИЛИ дорогим ведро-3."
  - "Ангар-проба должна гонять single-core SCALAR И single-core Burst (+ все ядра), чтобы развязать 'полоса 32-120k = scalar или Burst' — иначе SIMD-множитель частично двойной-учёт."
  - "369k-пузырь якорь оптимистичен (r≈11м, не 12м; истинный r=12 ~463k). Для честного worst-case 25см-reach держать ~460k."

decisions_needed:
  - d-vertical-bands — Сколько АВТОРИТЕТНЫХ высотных полос несёт грубое поле для вертикали ('присесть под слой')? Рек.: 2-3 полосы = ведро-2 (дёшево, детерминированно, в чексумме, НЕ полное 3D); решить ДО первой вертикальной механики. Иначе вертикаль = косметика (присесть не меняет урон) ИЛИ дорогое ведро-3.
  - d-global-vs-mix (ПЕРЕФОРМУЛИРОВАН в свете лока) — Держать полную S4-LOD-машинерию (залочено) ИЛИ упростить далёкий тир на малом масштабе (уровень почти влезает равномерно)? Рек.: решать ПОСЛЕ ангар-пробы; упрощение = re-shape залоченного плана (свежая сессия), не research.
  - (несут силу прежние d-cellsize-default / d-level-scale-ceiling / d-sleep-policy / d-hangar-probe-first из s-research-012).

play_check:
  1. Recite — DONE: восстановлены 3 вопроса + уточнение (стопка, не зал).
  2. Investigate — DONE: 5-агентный веер (масштаб-стопкой + оптимизации + LOD + 2 скептика); analysis на верифицированных якорях s-research-012.
  3. Confidence — DONE: точная относительная арифметика отделена от неизмеренного single-core (риск ×4-8); 2 поправки скептиков (лок-конфликт, вертикаль-авторитет) подняты в headline; названо что меняет ответ (ангар-проба scalar+Burst+all-core).
  4. Close — DONE: этот RESULT; ответ в формате владельца (таблицы, 3 оси, пределы); продукт §9; решения в decisions_needed.

log: "2026-06-22 — research (g-9c41, s-research-013): углубление пределов (вертикальность=этажи стопкой). 40 комнат≈7440м³ (между LC medium/large); стопка объём-нейтральна; 40@50см=59.5k не стена даже worst-case; оптимизации worst ×8-20 (сон=×1 при манипуляции); LOD стоимость∝ближним≈константа, масштаб ~40→300-400 (~×8-10) ценой возврата re-flux/collapse. 2 поправки скептиков: лок УЖЕ выбрал LOD-лестницу (Alt-5.3-дефолт=предложение пересмотра); 'присесть под слой' требует АВТОРИТЕТНЫХ высотных полос (ведро-2)→d-vertical-bands. Дополнил work/gas-cellsize-levelscale §9. → history/s-research-013.md"

next: awaiting_decision — новое d-vertical-bands (число авторитетных полос) + переформулированный d-global-vs-mix (полная S4-LOD vs упростить, гейт=ангар-проба) + прежний батч (s-research-012). S0 не ждёт (нейтральна). Если владелец решит упростить далёкий тир / пересмотреть LOD-лок — re-shape/maintenance в свежей сессии. Гейт всего абсолютного = ангар-проба (scalar + Burst, 1 ядро + все ядра, тяжёлый солвер, worst-case).
```

END_OF_FILE: live/indie-game-development/history/s-research-013.md
