# s-repair-consolidate — single-source canon for g-9c41 (2026-06-23, owner «да»)

**Play:** repair (state hygiene / drift-kill). **Owner present.** Agent-CLI self-writer after this RESULT.

## Trigger
Owner hit recurring drift mid-S0-build: a retired «height-bands / 2-band» concept resurfaced (in the live `CoarseBandStep` code, in the "authority" cellsize doc as a fake `d-vertical-bands` decision, and in a `knowledge/` pointer), contradicting the locked «50cm isotropic, height emergent». Root cause (verified by git): ~15 parallel sessions over 2 days wrote ~10 layered research docs into one git; "cleanups" were file-moves + SUPERSEDED banners that got overtaken by more research → a palimpsest, not a single source. Owner authorized a radical fix: ONE spec from scratch + delete-the-rest.

## Outcome
A single owner-signed canon — **`knowledge/g9c41-gas-engine-SPEC.md`** — replaces ~11 layered docs. It is the ONE source every g-9c41 build reads (9 sections: how-to-read / locked architecture / gas model / §9 seams / slice spine S0–S6 / OPEN / BURIED / code-sync / replacement-map).

## How (the guarantee, not "it's clean")
1. **Converge** (wf_ea38c86b, 14 agents): 8-agent source sweep + drift-hunt + code-map → 1 author drafts the spec.
2. **Adversarial verify** (5 skeptics try to BREAK it): caught the far-tier-law contradiction, an internally-impossible level-scale/hangar arithmetic, a lock-breaking «bigger cells» hangar fallback, two lossless misses (R21 front-is-derived; representation_tag enum), and — critically — a SILENT lock re-wording (the spec performed `d-cellsize-ratify-001` without the owner G9). All fixed; the owner's «да» is the G9 ratification.
3. **Completeness audit** (wf_127002ba, 9 agents, source-grounded — owner feared lost detail, flagged destructibility): verdict "faithful distillation, +8 load-bearing additions". Restored 8 invariants that read like implementation detail but a build could silently violate with every gate green: damage/dose reads coarse-not-bubble; detonation reads coarse totals; integer mass clamp [0,cap]; vertical = buoyancy-bias not per-column sort; capacity+back-pressure kept law; committed-revision cross-layer reads; the destructibility seam (back-pressure stores the door-break pressure) + named-open edge/geometry mass-conservation; DA-only S0 cross-check (PLAN R1). Destructibility now fully captured.

## state_changes (applied by this writer)
- `knowledge/g9c41-gas-engine-SPEC.md` CREATED = the canon (owner-signed).
- 11 docs → `work/archive/`: gas-model-architecture-decision, gas-cellsize-levelscale, grid-vs-graph-resolution, detail-authority-cost-model, gas-architecture-full-audit, gas-frontier-verdict, gas-frontier-research, converge-g-9c41, converge-g-9c41-arch, gas-engine-INDEX, dev-plan-graph.
- `knowledge/g9c41-architecture-locked-slices.md` + `g9c41-drift-guard.md` → banner-redirect to canon (kept as history).
- `work/c-exec-004-call.md` → SUPERSEDED banner (pre-ADR-0010 host-single-writer / band-sim).
- `c-exec-013-call.md` + `c-exec-013-PLAN-s0.md` → reading-set repointed to the canon; the S3-«height-bands» phantom corrected (OUT-OF-S0 label only; no S0 deliverable/test/code touched).
- `NOW.md`: phase entry added; `d-cellsize-ratify-001` → **done** (RATIFIED by owner «да»: Fact #2 = 50cm isotropic / height emergent / NO bands).

## Process fix
g-9c41 = **ONE chat at a time** during build; research is CLOSED (the parallel-research marathon was the drift engine). Light rule, no new mechanism.

## next
S0 build resumes on the clean canon + `c-exec-013-PLAN-s0.md` (still paused until the owner kicks off the dev-worktree build session). `CoarseBandStep` 2-band code = kept-as-far-tier, authoritative role retired at S4 (canon §8) — not an S0 action.

END_OF_FILE: history/s-repair-consolidate.md
