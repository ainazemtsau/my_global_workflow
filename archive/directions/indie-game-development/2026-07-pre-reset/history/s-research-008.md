# RESULT — s-research-008 (research child, c-research-gasdepth-001)

play: research
direction: indie-game-development
node: g-9c41 — представление газа + симуляция ВДАЛИ от игрока (грубый/глубинный слой)
parent: owner / решение по представлению грубого газового слоя
date: 2026-06-18

outcome: |
  Ответил на ограниченный вопрос CALL: есть ли способ ХРАНИТЬ+СИМУЛИРОВАТЬ грубый дальний газ лучше «2 полос на
  комнату», дёшево дающий (a) облако-в-месте, (b) реакции-по-пересечению, (c) стопку 3+ газов — детерминированно/
  целочисленно, синхронизируемо, на min-spec, соло. ВЕРДИКТ: лучшее — НЕ другая архитектура, а тот же
  секторно-полосный граф (ARCH-1v2) с покрученными ручками + один НОВЫЙ дешёвый шов, нащупанный поиском
  (граф-фронтов). «2 полосы / 1 сектор» = день-один деградация, честно недостаточная для (a)/горизонт-(b); НИ ОДНО
  структурно-другое семейство не бьёт секторно-полосный граф под жёсткими фильтрами (все — цена∝объём, нужно
  цена∝структура-и-дешевле-вдали; и/или падают на детерминизме-целочисленности или соло). Хотелки лежат на
  ОРТОГОНАЛЬНЫХ осях: (c)=вертикаль→N-полос; (a)/(b)=горизонталь→деление сектора ⊕ граф-фронтов ⊕ seed-источника.
  Findings → work/research-g-9c41-coarse-representation-2026-06-18.md (включая raw deep-research промпт для владельца).

evidence: |
  - strategic_search, 17 агентов (wf_68c339e6-fcf): 8 структурно-разных семейств × (генератор + враждебный
    опровергатель) + критик полноты. Каждый кандидат оценён против (a)/(b)/(c) + двух жёстких фильтров + масштаба
    ~3300 секторов min-spec; одно опровержение + первый пробник на каждого.
  - Опровергательные вердикты: N-полос = augment-only (верт. ручка, c); суб-сектор = augment-only (гориз. ручка,
    a/b-гранул., det УСЛОВНО=compile-time целая геометрия); воксель/октодерево = scale FAIL (full-fine регом
    768k/352–820МБ ИЛИ макро-воксель под-стенная протечка); CA/lattice = oracle-only (det-по-формату, НЕдоступно
    по объёму; Noita-коп не реплицирует пиксель-CA); пуфы = cull (render+oracle, уже s-arch-003); AMR = cull (соло:
    refluxing/regrid = многолетние команды); моменты/спектр = augment-only хинт-ONLY (уже s-arch-003 GH-3; правда =
    Gibbs ложные возгорания); hybrid source-list = oracle/seed-only (b=НЕТ структурно, a узко в окне игрока).
  - Критик полноты (covered_ok=false): ОДИН реальный пробел на оси ХРАНЕНИЯ — горизонтальная-позиция-в-комнате,
    который весь набор систематически опускал. Закрывают дёшево + проходят оба фильтра: ГРАФ-ФРОНТОВ (сильнейший
    augment: целочислен, разрежён, наступающий-фронт + узловая реакция) и HEIGHT-FIELD (пер-архетип, CFL/целый
    caveat). 4 остальных (level-set, precomputed-Green, semi-Lagrangian, нейро) distinct, но oracle-only / hard-fail.
    flow-network и noise-поле = релейблы (не новое).
  - Совпадение с прежними измерениями: все вердикты согласны с s-arch-003 (GH-1 lazy-closed-form 160–350× хуже;
    GH-3 моменты −31% ширины → ложные возгорания; survivors VFX-фронты + оракул) и ARCH-1v2 §3.1/§4/§6 (числа,
    B/voxel cull). Внешние факты датированы (Teardown fixed-point; Noita Entangled Worlds; NanoVDB read-only/float;
    integer lattice-gas спурьёзные скорости; AMR refluxing; Sethian&Smereka level-set; Stam semi-Lagrangian;
    shallow-water dense-gas; QMOM float-realizability). Полный список — work-файл §6.

state_changes: []   # research НЕ пишет state. Findings возвращаются родителю; знание/captures триажит шейп/владелец.

captures:
  - "★ ГРАФ-ФРОНТОВ — НОВЫЙ кандидат Волны 3 для (a)/(b)-горизонт + «наступающего фронта»: хранить 1-D позицию
     газового фронта на рёбрах портал-графа (несколько целых/ребро), кинематическое продвижение, слияние в узлах =
     ЛОКАЛИЗОВАННОЕ событие реакции (комнато-граф так не может); НЕ-авторитетный хинт-слой над полосным складом
     (правило-связка позиция↔масса, иначе 2-й источник правды). Триаж на ШЕЙПЕ Волны 3. Первый пробник = полдня
     integer-harness (коридор-цепь, два фронта в узле, бит-детерминизм)."
  - "Меню пробников (всё полдня, offline, до сети/DA): (1) N-полос A/B стопка/верт-реакция; (2) граф-фронтов
     игрушка; (3) суб-сектор compile-time бит-идентичный партиционер + реакция-через-границу; (4) seed-источника
     позиция-vs-перенос; (5) height-field отложить. Каждый с pre-agreed kill-rule (см. work §2/§5)."
  - "Деление сектора несёт СКРЫТЫЙ детерминизм-риск: cut-cell геометрия во float → разные машины → разный граф с
     тика 0 (ФАТАЛЬНО). Лечение обязательно: разбиение compile-time целочисленно на хосте, в снапшот/replay,
     клиентам как ДАННЫЕ. Зафиксировать в Волна-3 PLAN, если суб-секторы включаются."
  - "Подтверждение для активного бета: текущий t-2/c-exec-006 на 2-полосах/1-секторе КОРРЕКТЕН как день-один; поиск
     валидирует существующую лестницу деферралов (N-полос/суб-секторы — будущие швы, d-roomfull-001). НЕ трогать бет
     (G1)."

decisions_needed: []   # research informs, не решает; владелец/шейп решают, включать ли и когда (рекомендация — Волна-3 шейп)

knowledge_candidate: |
  (предложение, НЕ записано — knowledge пишет review/pulse) «Грубое представление газа: вердикт». Ни одно
  структурно-другое семейство (воксель/CA/пуф/AMR/level-set/semi-Lagrangian/нейро/Green) не бьёт секторно-полосный
  граф под жёсткими фильтрами (детерм.-цел. + соло), т.к. их цена ∝ ОБЪЁМУ, а нужно ∝ СТРУКТУРЕ-и-дешевле-вдали.
  Три хотелки = ортогональные ручки на графе: (c)→N-полос (верт., позитивность по построению); (a)/(b)→деление
  сектора (compile-time целая геометрия!) ⊕ граф-фронтов (новый дешёвый хинт) ⊕ seed-источника. Непрерывные (a)/(b)
  = тонкий тир (Волна 3). «2 полосы/1 сектор» достаточны ТОЛЬКО как день-один-минимум. Кто читает: шейп Волны 3.

play_check:
  - "1 Recite — DONE: вопрос восстановлен дословно (один ограниченный); формат (3–5 выживших + поля + вердикт + raw
     deep-research промпт) и бюджет (один проход + анализ, partial-on-time) приняты. Один вопрос — не дробил."
  - "2 Investigate — DONE: методом CALL (strategic_search распределением структурно-разных + cull weak tails) через
     17-агентный workflow поверх двух прежних брифов; источники датированы; raw deep-research промпт отрендерен как
     самодостаточный (§7 work-файла) для НЕЗАВИСИМОГО владельцева прохода (метод CALL = strategic_search + raw
     deep-research; я отработал strategic_search и снабдил промптом, не форсируя checkpoint — бюджет partial-on-time
     + CALL.next = вернуть findings, не ждать отчёт)."
  - "3 Confidence — DONE: установленное (с источниками) отделено от вывода (work §6/§8); названо, что изменило бы
     ответ (тонкий тир вернут / уровни ×5–10 меньше / коп-детерминизм смягчён — ни одно не верно)."
  - "4 Close — DONE: RESULT в запрошенном формате; findings → work-файл; knowledge_candidate предложен, не записан;
     log; next = return-to-parent (owner)."

log: "research: c-research-gasdepth-001 закрыт — лучше «2 полос на комнату» = тот же секторно-полосный граф с ручками (N-полос→c; деление+ГРАФ-ФРОНТОВ→a/b) + seed; ни одна экзотика (воксель/CA/пуф/AMR/level-set/нейро) не бьёт под жёсткими фильтрами (цена∝объём vs ∝структура); граф-фронтов = новый дешёвый Волна-3 кандидат; меню полдня-пробников; raw deep-research промпт владельцу; бет не тронут (G1)."

next: |
  return-to-parent: владелец / решение по представлению грубого газового слоя. Findings →
  work/research-g-9c41-coarse-representation-2026-06-18.md. РЕКОМЕНДАЦИЯ (не решение): триаж графа-фронтов + меню
  пробников на ШЕЙПЕ Волны 3 (созвучно d-return-reconstruction-001: deep-research отложен до Волна-3 шейпа); активный
  бет (t-2/c-exec-006) НЕ трогать (G1). Опционально: владелец прогоняет raw deep-research промпт (§7) в ChatGPT Pro /
  Claude для независимого кросс-прохода; новый отчёт дайджестит будущая сессия.

END_OF_FILE: live/indie-game-development/history/s-research-008.md
