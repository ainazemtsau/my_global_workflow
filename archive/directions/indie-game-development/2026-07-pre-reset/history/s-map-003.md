# RESULT — s-map-003 (map, g-a7f2 root parallel-track re-check)

play: map   direction: indie-game-development   node: g-a7f2 (root)
parent: CALL c-map-003 (framed at review c-review-001); owner present card-by-card (G9)

outcome: |
  The parallel-track structure was re-checked after the spectacular clip was dropped (s-shape-004). The
  orphaned "first clip" trigger is RESOLVED and the two harvest-proposed nodes are decided — but the session
  also CORRECTED course on the owner's challenge: instead of fixing a specific artifact, it makes the artifact
  an OUTPUT of a new generative marketing process. Net (all owner-approved card-by-card, G9):
  - REJECTED the contested fixed-artifact node "(a) first player-legible artifact" as a CRUTCH — pre-fixing the
    artifact pre-empts the very process that should decide it; the legibility FLOOR is already owned by the
    Wave-2 player-facing-terminus mandate.
  - ADDED g-2f8c "delegable authentic social-marketing system": a GENERATIVE PROCESS (its own local play,
    canon-forge-style) that EMITS the marketing artifacts to make (what/where/when), in the solo-dev's authentic
    voice; logistics delegable to the owner's wife + brother, the voice stays the owner's (AI amplifies).
  - RE-HUNG the orphaned trigger: storefront/audience EXECUTION (g-5b07 page, g-e6f2 cadence) starts when g-2f8c
    publishes its FIRST artifact. The build-side guarantee that showable material exists needs NO new node — it
    already lives in the Wave-2 player-facing-terminus mandate + the fixed 2026-08-31 page date; g-2f8c's first
    emission is timed to 08-31 (teeth vs pre-mortem #2).
  - DEFERRED node "(b) co-op interdependence proof" at the owner's request to close (he will set next steps) →
    routed to follow-up c-map-004. (b) is a real candidate (pre-mortem #5, also feeds the streamer-virality
    channel per the market research), not rejected on merits.
  Tree stayed outcome-level (G2); NOW spine untouched (Wave 2 stays the active spine).

evidence: |
  - Owner card-by-card approvals this session (G9): (c) g-2f8c «принять»; the restructure (drop (a) + re-hang
    the trigger + rely on the already-existing teeth) «принять». (b) NOT verdicted — owner chose to close
    («давай закрываем»), so (b) is recorded DEFERRED, not added (G9: no node enters the tree without explicit
    approval).
  - Two deep-research passes grounded the decisions: market research (workflow wf_c85f223d-c09 — for this genre
    the first material is a real-gameplay systemic-moment GIF + an illustrated capsule, NOT concept/vision;
    channel/positioning capture can start before gameplay; the build is the binding constraint) and a structure
    design-panel (wf_9d9c7ccf-cd5 — 5 candidate structures + adversarial critique; winner = the owner's
    generative-process model with the teeth named where they already exist, no guard node; (a) confirmed a crutch).
  - Applied TREE.md edits: root map_order rewritten (trigger resolution); g-2f8c added; g-e6f2 crit-1 + g-5b07
    crit-1 re-pointed. NOW.md: c-map-003 → done. LOG += one line. history/s-map-003.md = this RESULT.

state_changes: |
  TREE.md (G9 — owner-approved card-by-card):
    - root map_order REWRITTEN: clip-trigger RESOLVED (storefront/audience exec starts on g-2f8c's first
      artifact; teeth = Wave-2 terminus mandate + 08-31 page date; first emission timed to 08-31); (a) REJECTED
      as a crutch (reason recorded); g-2f8c named as the parked generative marketing process; (b) PROPOSED,
      DEFERRED → c-map-004.
    - ADDED child g-2f8c "delegable authentic social-marketing system" (status parked; goal/why/done_when/detail).
    - g-e6f2 done_when crit-1: trigger "first harness clip" → "g-2f8c publishes its first artifact"; g-e6f2
      EXECUTES g-2f8c's plan (logistics delegable to wife/brother; authentic owner voice).
    - g-5b07 done_when crit-1: first gameplay material = g-2f8c's first published artifact off the Wave-2
      terminus, fronted by the illustrated capsule (highest-ROI).
    - NO node (a) added (rejected). (b) NOT added (deferred).
  NOW.md:
    - open_calls: c-map-003 queued → done. NO other NOW change — active_bet / wave_plan / next (c-shape-wave2)
      / active_tasks UNTOUCHED (CALL boundary: NOW not touched; Wave 2 stays the spine).
  LOG.md += one line. history/s-map-003.md created (this RESULT).

captures:
  - Channel CAPTURE layer (mailing list "beta-key-for-email" + one reserved studio handle + a "coming soon"
    page) is pure-upside and can START NOW, independent of the artifact (research wf_c85f223d-c09); active posting
    cadence waits for g-2f8c's first artifact. → input to c-shape-marketing.
  - Illustrated capsule (~$300) is the single highest-ROI marketing dollar (the game in ~90% of impressions);
    needs only the locked art direction (already frozen: low-poly stylized, register B). → c-shape-marketing / g-5b07.
  - Helper resource: owner's wife + brother can run marketing LOGISTICS if the system is clear/rule-based with an
    AI analysis layer; the authentic VOICE is not delegable. (Also saved to auto-memory.)

decisions_needed:
  - id: d-coop-node-001 (DEFERRED → c-map-004)
    note: |
      Decide node (b) "co-op interdependence proof" — the gas world must STRUCTURALLY force cooperation
      (pre-mortem #5), currently unowned before a parked Steam Playtest. Options: (1) add as a separate node
      (peer to g-9c41/g-d3a8); (2) fold into g-d3a8 (co-op roles already live there); (3) drop. Recommendation:
      (1) or (2) — it is real (also feeds the streamer-virality channel per research) and the playtest is too
      late for a co-op-FIRST game. Owner chose to close c-map-003 before verdicting; carried to c-map-004.

play_check:
  - 1 recite: done — mission + current tree state restated in plain owner language (parallel tracks + the orphan).
  - 2 candidates & evidence (owner): done — asked the owner for his candidates first (anti-anchoring); he engaged
    in discussion. map_evidence = the s-review-001 harvest (fresh, same day) + 2 deep-research passes (market +
    structure); search-first taken as a G7 option the owner approved («давай ... сделаем»).
  - 3 skeleton (owner): done — the whole proposed restructure shown on one screen; owner reacted, then CHALLENGED
    the fixed-artifact node as a crutch → skeleton revised to the generative-process model.
  - 4 cards: done — (c), (a), (b) elaborated as cards one at a time, each anchored (why/edge/risk).
  - 5 per-node verdict (owner): (c) «принять» (owner_approved); restructure (drop (a) + re-hang trigger) «принять»
    (owner_approved); (a) REJECTED with reason (crutch); (b) DEFERRED at owner's request (NOT added — G9 respected:
    no silent acceptance, no node added without approval).
  - 6 order (owner): done — Wave 2 stays the spine (NOW.next untouched); g-2f8c + (b) parked; no immediate bet.
  - 7 depth check: held — top level only; no tasks written (G2); g-2f8c's own play is designed later (shape).
  - 8 lens sweep: done — commercial/traction + audience-workflow now OWNED by g-2f8c (pre-mortem #6/#7);
    co-op-first lens flagged unowned → (b) deferred to c-map-004; core/technical untouched (Wave 2 spine).
  - 9 close (owner): done — owner «давай закрываем»; RESULT emitted; tree edits carry owner approval; NOW spine
    untouched.
  - G9: held — every TREE edit carries owner card-by-card approval; (a) rejected with reason; (b) NOT added
    (deferred, no approval) — writer-clean.
  - G2: held — outcomes only, no tasks in the tree.
  - G8: held — new node defaults parked; (b) held as a parked proposal pending decision.
  - G7: held — the deferred (b) decision carries options + a recommendation, batched into decisions_needed.

log: "map (g-a7f2 root, c-map-003 → CLOSED): clip-trigger RESOLVED — storefront/audience re-hung on g-2f8c's first artifact (teeth = Wave-2 terminus + 08-31 page date, no guard node); +g-2f8c generative marketing process (own play, EMITS artifacts; logistics delegable to wife/brother, voice = owner); (a) fixed-artifact node REJECTED as crutch; (b) co-op-interdependence DEFERRED → c-map-004. 2 deep-research passes (market wf_c85f223d-c09 + structure design-panel wf_9d9c7ccf-cd5 → owner's generative model won). NOW spine untouched. next = parked c-shape-marketing + c-map-004; spine = c-shape-wave2."

next: |
  No immediate bet — Wave 2 (c-shape-wave2) remains the active spine (NOW.next untouched). Two PARKED follow-ups,
  owner-triggered when he chooses (both parallel, non-gating to Wave 2):
  - c-shape-marketing — shape g-2f8c into its own local play (canon-forge-style): define the go-to-market plan,
    stand up the pure-upside capture layer (mailing list + reserved handle + coming-soon page), wire the delegable
    logistics split, and emit the first artifact card timed to 2026-08-31. Low-ceremony parallel track.
  - c-map-004 — decide node (b) "co-op interdependence proof" (add separate / fold into g-d3a8 / drop;
    recommendation: add or fold — it is real + feeds the streamer-virality channel).

END_OF_FILE: live/indie-game-development/history/s-map-003.md
