# History — PuppetMaster P2a0 route reconciliation

session: s-repair-player-puppetmaster-p2a0-001 (Codex; play: repair)
direction: indie-game-development / g-9c41 / M1-P2a0

## Owner summary

Вариант А принят: до production controller запускается отдельный ≤полудня PuppetMaster authority suitability spike.
Он проверяет падение/подъём и A/B/C именно под сетевой authority, а не только внешний вид. P2a0 занял открытие уже
существующего M1-5 player-tracking leg; 14-й leg не добавлен, M1-GAS-PROBE только вышел из текущего трёхслотового окна.

```text
RESULT s-repair-player-puppetmaster-p2a0-001 (call: d-player-shell-parallel-001-owner-verdict-2026-07-11)
direction: indie-game-development   play: repair   node/task: g-9c41/M1-P2a0
outcome: |
  Owner chose route A. A self-contained <=half-day PuppetMaster authority suitability spike is READY as a real
  parallel M1 task in the disposable lane-C lab. It must decide root authority and multiplayer suitability before
  any production controller freeze. It is charged to existing M1-5 capacity rather than becoming a 14th product leg;
  M1-GAS-PROBE is deferred from the current three-task window, not cancelled. No product BUILD/import/merge occurred.
evidence: |
  Owner actual words after reading the recommendation: «В принципе, согласен». Owner then asked: «нам надо будет еще
  ресерч сделать, что вообще подходит ли нам... Потому что у нас есть мультиплеер» and explicitly delegated operation:
  «ты должен все вести сам. Ну, как бы нашему workflow все.» This confirms spike-first A and requires a simple launch
  handoff; it does not pre-decide the later A/B/C technical verdict.

  work/puppetmaster-player-authority-2026-07-11.md records primary local/plugin evidence: cached 1.4, researched current
  1.5, no product import, Target+physical Puppet split, Puppet/Unpinned/GetUp and canMoveTarget root motion, no cached
  asmdef, collision-layer assumptions, PhysX non-lockstep limit and old Mirror precedent only. Its narrowed P2a0 survived
  three same-family revalidators 4/4 each with zero refutations; those are non-binding in-session pre-passes. The new
  executor CALL therefore requires a fresh independent G5/refutation before returning its architecture recommendation.

  Fresh state before repair had exactly three rolling tasks: Lv-ingest active, M1-A0 blocked and M1-GAS-PROBE blocked.
  The least-disruptive fold replaces the not-yet-runnable GAS-PROBE window entry with P2a0; GAS-PROBE remains in the bet
  forecast/kill condition and returns after Phase 0 + checksum foundation + successor A0. Existing M1 shape step 5 is
  player tracking, so P2a0 is its risk-first opening. Appetite remains 13 and explicitly forbids silent extension.

  knowledge/g9c41-lanes-venues.md already assigns the disposable prototype venue C:\projects\Unity\GasCoopGame_lab to
  lane C and requires worktree/editor/lock/baseline/MCP preflight. No registered current open_call occupies that lane.
  git ls-remote verified product origin/main = a644e5db538d1102f726630d6a4ac2f3f1bdcf5a at framing time; the CALL requires
  re-sync and reconciliation if it moves. Product contract stamp, lab existence, UnityLockfile, seat and editor identity
  are intentionally not claimed from this Direction worktree; they are mandatory first-step preflight in the CALL.
state_changes: |
  ADD live/indie-game-development/work/c-exec-player-puppetmaster-p2a0-001-call.md as the complete self-contained
  executor CALL: business outcome = verified PuppetMaster/root-authority multiplayer suitability; candidates A/B plus
  bounded C kill-probe; plugin/version/seat/asmdef/layer/lab preflight; transition capture; network authority inventory;
  fresh independent refutation; no FishNet BUILD, damage, Core edit, vendor/settings/product merge; <=half-day STOP.
  ADD live/indie-game-development/work/player-puppetmaster-p2a0-start-2026-07-11.md as the owner quick-start with one
  launch sentence and the workflow/owner touchpoint split.
  PATCH live/indie-game-development/work/poligon-m1-shape-2026-07-10.md with a current-state pointer: historical
  11-leg/rolling-wave text is superseded only by fresh NOW plus the P2a0 route; no approved ladder outcome is rewritten.

  PATCH live/indie-game-development/NOW.md by stable ids: set updated to this session; retain one active bet and the
  13-leg ceiling; append to appetite that P2a0 opens existing M1-5 and is not leg 14; append P2a0 to forecast; replace
  rolling task M1-GAS-PROBE with active M1-P2a0 (GAS-PROBE remains future/uncancelled); add open_call
  c-exec-player-puppetmaster-p2a0-001; remove answered decision d-player-shell-parallel-001. Preserve Lv-ingest,
  M1-A0, all unrelated calls/decisions, recurring and fresh-current primary NOW.next.

  REGENERATE live/indie-game-development/work/board/dashboard.html live sections: P2a0 appears as ready parallel work;
  the answered PuppetMaster decision moves from owner batch to closed; road/P2 copy no longer promises controller-first;
  today's journal includes this route while preserving all fresh-current unrelated concurrent entries.
  APPEND the exact log line below once; SAVE this full RESULT once at this history path. No TREE.md, CHARTER.md,
  knowledge, os/** or product-repo change.
captures:
  - If hybrid B or full-physics C survives, the next engineering proof is a separate two-peer FishNet authority spike;
    production controller remains blocked until that proof and a later owner verdict.
  - Rig/clip selection may continue as separate read-only research, but no Animator/Puppet/controller API is frozen here.
  - If plugin import/compatibility/setup consumes the timebox, checkpoint/kill is the correct outcome; no scope shrink.
decisions_needed: []
play_check:
  - 1 Name the contradiction: done — the chosen parallel P2a0 conflicted with an already full three-task rolling window,
    fixed 13-leg ceiling and a still-pending decision; simply adding it would invent a fourth task/14th leg.
  - 2 Reconstruct: done — newest-first state/history/research/shape/venue evidence showed M1-5 as the existing player leg,
    M1-GAS-PROBE as not runnable yet, lane C as the disposable venue and PhysX/network authority as the new load-bearing risk.
  - 3 Propose corrected state: done — named M1-5 fold, third-slot replacement, lab preflight, exact spike boundary,
    multiplayer matrix and one-line owner launch were shown without changing the primary Phase-0 route.
  - 4 Confirm: done — owner actual words were «В принципе, согласен» and «ты должен все вести сам» after reading the
    recommended A; this repair implements only that route and leaves the later architecture verdict open.
  - 5 Friction: skipped — this divergence came from a new load-bearing PuppetMaster fact plus owner choice, not a recurring
    Direction-OS rule defect; the detected concurrent writer was handled by fresh-state semantic rebase, not an OS edit.
log: 2026-07-11 — repair/route (g-9c41/M1-P2a0, s-repair-player-puppetmaster-p2a0-001): owner выбрал A («в принципе, согласен»); disposable PuppetMaster authority suitability spike открыт параллельно в lane-C lab как risk-first часть существующего M1-5, не 14-й leg; он обязан вернуть root-handoff + multiplayer authority verdict до production controller, без FishNet BUILD/damage/merge. → history/2026-07-11-s-repair-player-puppetmaster-p2a0-001.md
next: |
  CALL c-exec-player-puppetmaster-p2a0-001
  → live/indie-game-development/work/c-exec-player-puppetmaster-p2a0-001-call.md
  This is the ready parallel handoff; primary direction NOW.next remains work/c-exec-lv-ingest-phase0-call.md.
```

END_OF_FILE: live/indie-game-development/history/2026-07-11-s-repair-player-puppetmaster-p2a0-001.md
