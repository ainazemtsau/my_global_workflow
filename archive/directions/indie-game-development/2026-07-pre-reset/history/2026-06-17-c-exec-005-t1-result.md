# RESULT — c-exec-005 t-1 (coarse band gas solver kill-gate) — REBUILT under d-fillmodel-001

Closing report for leg **t-1** of bet **g-9c41 Wave 2** (direction `indie-game-development`, CALL
`c-exec-005` **RE-PLAN / REBUILD**), the **~2026-06-30 band-solver kill-gate**. The first build self-declared
a PASS that the owner OVERTURNED (the vertical fill was a FIXED proportion, `lowerShare = total·eff/(eff+16)` —
~80/20 for cold-heavy, INDEPENDENT of quantity). This leg REBUILDS the fill model under the owner's
**d-fillmodel-001** (capacity-fill + overflow) + fixes per-band temperature, stages all artifacts, and reverts
the `Unsafe.dll.meta` flip. Architecture re-frozen in `docs/adr/ADR-0005-coarse-band-solver.md` (BS4/BS10/BS14
re-frozen; D1–D4 + d-fillmodel-001 owner-ratified). The LOCK (ADR-0004 §LOCK) + ADR-0003 v2 C1–C22 are
**UNTOUCHED**. Status: **Axis A GREEN — REMEDIATED to spec; kill-gate VERDICT = PASS_WITH_NITS → roll to t-2.**
`& .\tools\check.ps1 -Deliver` → "OK: all gates green" (boundary 0/0; **198/198**, 0 skipped; hygiene OK —
incl. a post-G5 Codex review: `SetSource` out-of-range coordinate guards + the `floor`→truncation-toward-zero
spec/ADR wording fix). The prior d-fillmodel PASS was VOIDED pre-merge when an audit found the orifice diverged
from frozen BS3 (raw
summed-mass `pa−pb`, capacity ignored); it was remediated BEFORE merge (owner **P1=B** orifice →
mass-vs-capacity + a deterministic discriminator + re-proven quiescence; **P2=A** artifact-gate split; **#1**
negative-source reject; de-crutching; negative-path coverage). The NEW independent fresh-session G5 =
**PASS_WITH_NITS, `failedHalf: none`**; owner MULTI-QUANTITY legibility = PASS («Да, читается»). Post-G5: the
Codex P1 (SetSource coord-guards) + P2 (floor→truncation wording) fixed; NIT N1 (`Unsafe.dll.meta`) resolved via
`git skip-worktree` (the live Editor's re-flips can no longer be staged); NIT N3 (`.serena/`/`_Recovery`)
removed + gitignored. **NOT merged/committed — held for the owner's dev→main merge-go.**

## outcome

The rejected fixed-proportion fill is replaced by the owner-ratified **capacity-fill + overflow** model, the
per-band temperature defect is fixed, and the build is made committable (all files staged, evidence matched,
the `.meta` flip reverted). Against the re-frozen `done_when`:

- **(FIX 1 — FILL MODEL, d-fillmodel-001).** Each sector has a fixed **free volume** (a per-room structural
  constant, NOT a fraction of the current gas); the lower band's `FloorCapacity = ½·FreeVolume`. The lower band
  fills densest-first (by per-band `eff`) up to `FloorCapacity`; the excess **overflows** up. **Quantity-
  dependent by construction**: little gas → a thin floor layer / empty ceiling; more → the level rises; past
  capacity → overflow. Integer-native (CR3) and mass-exact; the BS1 energy/quiescence certificate is re-derived
  (the per-species target is fixed across a settle phase). The per-sector free volume lives on
  `CoarseSectorGraph` (realistic-spread by seed in t-1; **named seam: t-2 binds it to the real DA geometry**)
  and is **NOT wired/hashed** — replication/CR1/CR2/CR3 are byte-untouched.
- **(FIX 2 — PER-BAND TEMPERATURE, Codex P1#1).** The old `Settle` averaged `(temp[lower]+temp[upper])/2` into
  ONE `eff`. The new solver uses **each band's own temperature**: the lower band's capacity is throttled by its
  OWN temperature (a hot floor holds proportionally less, driving gas up). The NEW **DIFFERENTIAL-BAND** test
  (L25) refutes the averaging bug (equal mean temp, hot floor vs medium floor ⇒ strictly less on the hot floor).
- **(FIX 3 — ARTIFACTS / EVIDENCE, Codex P1#2).** All work is staged: `Core/Field/Coarse/*`,
  `tests/.../Coarse/*`, `docs/adr/ADR-0005*`, the regenerated `docs/measurements/g9c41-t1-*`, and
  `openspec/changes/c-exec-005-*`. This RESULT's evidence matches the diff. The t-1 measurement artifacts were
  REGENERATED from the new solver (the scene-profile hash now folds the per-sector volumes; the bench/telemetry
  reflect the capacity-fill kernel); the t-2/t-3 artifacts are byte-untouched (SHA-pinned by L19).
- **(FIX 4 — Unsafe.dll.meta, Codex P2#3).** The Editor-import flip (`Exclude Editor: 1→0`,
  `Editor enabled: 0→1`) is reverted via `git checkout` — **BUT the live dev Editor (open on this worktree)
  re-imports plugins and RE-FLIPS it after each revert**; the headless gate cannot catch plugin-`.meta`
  content. It is restored to main's committed state + `git update-index --skip-worktree`, so the Editor's
  re-flips can no longer be staged on this clone (NIT N1 RESOLVED — no merge-time manual revert). The headless coarse core
  does not use `System.Runtime.CompilerServices.Unsafe`, so this never affects the engine-free build/gate.
- **(NUMERICAL — §7-#2 battery, REBUILT).** The independent test-author authored the NEW **FILL-LEVEL** (L24)
  and **DIFFERENTIAL-BAND** (L25) regimes from the re-frozen spec (RED-before / GREEN-after) and reconciled
  L1/L7/L8 to the capacity-fill + per-band dynamics; the existing battery (mass-exact, no oscillation +
  TV-non-increasing, hot-inversion, Steckler, synthetic-graph) stays green. The pressure solve is in-budget:
  **worst-case post-breach coarse tick = 1.06 ms @ 3,136 sectors** (≤4 ms target; 8 ms honesty ceiling).
- **(CR3 — integer-exact, non-vacuous control).** The `FloatMutant` reduction now lives in the integer
  relaxation step (`Δm = gap/τ`, double round-half-up vs Canonical truncation); its coarse `CellHash` DIFFERS
  from `Canonical` on a multi-species-flux cell while `Canonical` replicates host==client bit-exact (L15).
- **(CR1/CR2 — host + 2 clients, PER LAYER).** Lossless bit-exact every tick + lossy ≤Q + bit-exact at settle,
  ONE shared codec path, under drop+delay+reorder + the no-stale-chunk oracle (L10–L14) — UNCHANGED and green
  (the fill-model change does not touch the wire).
- **(LEGIBILITY — MULTI-QUANTITY, BS14 re-frozen).** A pure-local headless ASCII render shows the owner
  SEVERAL fill quantities (low → mid → overflow) AND the same gas in a larger room — quantity-dependence + per-
  room at a glance (`docs/measurements/g9c41-t1-legibility.txt`, deterministic self-hash; L20). **Owner
  gamer-eye verdict (2026-06-17): PASS — «Да, читается» (reads as real coarse gas at low → mid → overflow, and
  per-room).**

**Kill-gate routing:** Axis A is GREEN. The binding kill-gate VERDICT is recorded after (1) the owner's
MULTI-QUANTITY legibility verdict and (2) an independent fresh-session G5 refutation + a Codex read-only
pre-pass scored vs the G0 ledger. PASS ⇒ roll to t-2; FAIL after reasonable iteration ⇒ scalar-per-volume
degrade (brief §6).

## evidence

- **Gate:** `& .\tools\check.ps1` → `OK: all gates green` — boundary build (engine-free dependency gate)
  0 warnings/0 errors; headless `dotnet test GasCoopGame.Core.slnx -c Release` **198/198** (103 Wave-1 + 95
  coarse: L1–L25 + the G1–G17 guard tests + the A1–A10 audit/review-coverage tests); hygiene OK (incl. the Unity-`.meta`-tracking guard).
  `-Deliver` green (RESULT field gate). (Env: only Windows PowerShell 5.1 — run as `& .\tools\check.ps1`.)
- **G2 mutation score (test strength):** `tools/mutation.ps1` (Stryker, coarse scope `**/Field/Coarse/**/*.cs`)
  = **76.76%** (393 detected / 512 valid) ≥ the `mutation_kill_floor=70` — REFRESHED after the review-fix test
  sweep (was 75.72% on the pre-sweep suite; the new guard/coverage tests raised it). This leg is
  `strong_check_grandfathered` so the score is informational for `-Deliver`, but recorded honestly in
  `docs/measurements/mutation-c-exec-005-t1-coarse-band-solver.json`.
- **G0 ledger:** `openspec/changes/c-exec-005-t1-coarse-band-solver/tasks.md` — Axis A all-passing with opened
  evidence (named tests); the kill-gate VERDICT line PENDING owner legibility + the fresh-session G5.
- **Independently-authored acceptance tests** (test-author read ONLY the re-frozen spec, never the solver
  formula): `tests/GasCoopGame.Core.Tests/Field/Coarse/` — `CoarseSolverBatteryTests` (NEW L24 FILL-LEVEL +
  L25 DIFFERENTIAL-BAND; reconciled L1/L7/L8 for the capacity-fill + per-band dynamics — robust inequality
  bounds, still RED-against-the-old-model), `CoarseReplicationTests` (L10–L15), `CoarseScaleTests`,
  `CoarseArtifactTests`, `CoarseLegibilityTests` (L20 MULTI-QUANTITY). The builder did NOT edit any acceptance
  test (the BUILDER-side `CoarseArtifactEmitter` — the multi-quantity render + the artifact emission — is not
  an acceptance test).
- **Solver change (the ONLY behaviour changed):** `Assets/GasCoopGame/Core/Field/Coarse/CoarseBandStep.cs`
  (`Settle` = capacity-fill + overflow + per-band-temperature throttle; `Reduce` carries the CR3 arms) +
  `CoarseSectorGraph.cs` (per-sector free volume, structural/not-wired, folded into `ProfileHash`). The
  orifice/pressure transport, the store, the carrier/wire/codec, and the scale graph topology are UNCHANGED.
- **Regenerated artifacts (`docs/measurements/`):** `scene-profile-g9c41-t1-coarse.json` (hash now folds the
  volumes), `repro-constants-g9c41-t1.json` (records the d-fillmodel-001 constants), `g9c41-t1-bench.json`
  (worst-case post-breach **1.06 ms** @ 3,136 sectors, cells/ms ≈ 47,700, inBudget=true),
  `g9c41-t1-scale-arithmetic.json` (memory 125,440 B; on-wire keyframe-inclusive 132,300 B; steady **82.69
  KB/s ≪ 275**, the DERIVED worst-case = one keyframe per settle window 1.6 s — corrects a stale 4.41 from the
  pre-derivation 30 s interval), `g9c41-t1-{lossless,lossy}.json` (`schemaVersion=3`, keyframe-inclusive on-wire),
  `g9c41-t1-legibility.txt` (multi-quantity).
- **Constitution:** `git diff main -- docs/adr/ADR-0003* docs/adr/ADR-0004* docs/measurements/g9c41-t2-*
  docs/measurements/g9c41-t3-*` is EMPTY; `Unsafe.dll.meta` at main's committed state + `git skip-worktree`'d (NIT N1 RESOLVED: the live Editor's
  re-flips can no longer be staged; never affects the engine-free core/gate); core engine-free (boundary
  0/0); coarse state `int32` (L1/L15); `L19_T2AndT3Artifacts_RemainByteUntouched` SHA-match; all Wave-1 goldens
  green.
- **Validator:** an independent fresh-session G5 refutation = **PASS_WITH_NITS, `failedHalf: none`** (recorded in
  ADR-0005 §Verdict + the ledger). Three Codex review rounds were then fixed (SetSource coord-guards; floor→
  truncation wording; settle/orifice/Execute integer-overflow guards; `IsWireEncodable` ≤255-chunk cap; the
  per-cell `MaxCellMass` injection ceiling). Finally an exhaustive in-house **convergence audit** (8 adversarial
  lenses × 3-skeptic refute-verify + a completeness critic) found **0/10 candidates surviving refutation** (no
  reachable corruption); it closed one real input gap (`FreeVolume` now bounded by
  `CoarseSectorGraph.MaxFreeVolume`) and the run-contract coverage gaps are now pinned by the **A1–A6** tests
  (per-species cross-sector conservation, both orifice directions, conflicting-direction multi-portal in one
  sweep, the `E=Σmass²/V` contractivity certificate, the `MaxFreeVolume` guard, synthetic-volume determinism). A follow-on review then made the previously-DEAD
  `IsWireEncodable` predicate LIVE — `CoarseLayerKeys.EnsureWireEncodable` now enforces the byte-`chunkIndex`
  ≤255 cap at the coarse encode sites (the reconstruction harness + emitter, NOT the LOCKed shared encoder),
  pinned by the **A7** tests (direct + an over-cap-graph integration reject).
- **Commits:** NONE yet — the working tree is staged/ready; the merge to `main` is held for the owner's go.

## assumptions

- **t-1 is a de-risking kill-gate on a SYNTHETIC seeded graph, headless** — no DA, no real breach beyond the
  portal-toggle the battery needs, no transport (the host==client oracle is in-memory loopback; R13/R14).
- **The fill model is capacity-fill + overflow (d-fillmodel-001).** Owner-ratified micro-parameters: per-sector
  free volume = a structural room constant (NOT a fraction of current gas), realistic-spread by seed in t-1,
  **t-2 binds it to real DA room geometry**; lower-band capacity = geometric **½** of the free volume; layering
  densest-first by per-band `eff`; NO discrete middle class.
- **The per-band-temperature throttle is the builder's formula** (BS4 leaves the exact integer formula to the
  builder, constrained by the BS10 battery): the lower band's effective capacity falls linearly with its OWN
  temperature, reaching 0 at a span of 1024 (4×256). A **per-species buoyancy RATE** ("very light rises
  faster", `τ ⇒ τ(s)`) is a NAMED future seam — owner-anticipated, OUT of t-1 scope.
- **The integer relaxation residual is ±(τ−1) = ±3** (C# integer division truncates toward zero), so the
  acceptance tests pin EXACT values only for mass conservation and the empty/overflow boundary, and robust
  inequalities (not ±τ windows) for saturated/throttled band levels.
- **The frozen buoyancy LAW** (`MolecularWeight={64,32,16,8}`, `BuoyancyTempShift=2`, higher-eff→lower-band)
  and the integer-native discipline (D1) are UNCHANGED. The throughput bench is a min-spec PROXY
  (reported-not-auto-judged, ADR-0004 T13).

## cuts

- **Per-species buoyancy RATE (lighter rises faster) → a later wave** (named seam; the settle is already a
  per-species loop, so `τ ⇒ τ(s)` drops in without re-architecture). OUT of t-1 per the CALL (only the fill
  model + per-band temperature changed this leg).
- **Real DA-composed level + real controlled breach + the real per-room geometry → t-2** (t-1 uses a synthetic
  seeded graph with a realistic per-sector volume spread).
- **Per-sector room CAPACITY with back-pressure/sink → t-2 (NAMED SEAM, empirically pinned).** In t-1 a sustained
  no-sink source eventually accumulates a room past the int domain and the overflow guard throws. The transactional
  tick makes that failed tick CONSISTENT (mass-exact, no wrap, clock frozen — A8-1/A9/A9c), BUT the overflow is
  TERMINAL: clearing the source does NOT restore liveness (the already-accumulated mass still can't fit; the field
  re-throws — the A10-3 finding). The real fix that PREVENTS the accumulation (a full room exerts back-pressure /
  vents through a sink) is t-2. **t-1 guards CONSISTENCY; t-2 will guard LIVENESS.**
- **Cross-band per-species buoyancy INVERSION → t-2 (NAMED SEAM, owner-ratified disposition this leg).** Codex P1
  flagged that the settle ignores cross-band effective density. Resolved (owner-confirmed): the t-1 per-band-
  temperature throttle + species-id fill is the COHERENT model — a per-species cross-band `eff` sort has NO fixed
  point (period-2 oscillation under per-band-cell temperature, proven arithmetically) and would INTRODUCE a bug —
  and it matches all tests, so there is **NO settle change**. A TRUE cross-band inversion needs **per-species
  temperature** (each gas carrying its own T — a seam DISTINCT from the per-species RATE seam BS4 already names):
  a **t-2 SHAPE input**. BS4's "a hot heavy species inverts past a cold light one" prose over-specifies this
  untested cross-band behaviour; **narrowing BS4 is a t-2 planner action (owner-confirmed — NOT a t-1 builder
  edit; BS4 stays frozen this leg).**
- **The XL2 separate temperature-layer + the inter-layer read seam → t-3; the player-legible render terminus →
  t-4; temperature→gas FEEDBACK → post-g-d3a8; meta-gas effects (poison/damage) → g-d3a8; the fine tier +
  coarse↔fine handoff → Wave 3.** (Unchanged from the original t-1 scope.)
- **Sub-band smoothness (a detailed vertical layer) → Wave 3** (2 coarse bands are accepted as "ok for now"
  per the owner).

## cost

Model: **Claude Opus 4.8** (builder/planner). Sub-agent portions: the independent test-author for the
capacity-fill battery (~144k sub-agent output tokens, model sonnet) + the L1/L7/L8 reconciliation (~64k,
sonnet). The (pending) fresh-session G5 validator + the Codex read-only pre-pass are separately metered. No
external spend (local `dotnet`; the headless leg used no MCP/Editor). The first (overturned) build's cost is
sunk and not re-counted here.

## manual-acceptance

Owner verify steps (the same checks the validator re-runs):

1. `git -C C:\projects\Unity\GasCoopGame_dev status` on `dev`, then `& .\tools\check.ps1` → expect
   `OK: all gates green` (boundary 0/0, `dotnet test` 198/198, hygiene OK). `& .\tools\check.ps1 -Deliver` →
   green.
2. **Eyeball the MULTI-QUANTITY legibility render** `docs/measurements/g9c41-t1-legibility.txt` — confirm it
   reads as real coarse gas at LOW / MID / OVERFLOW fill (thin floor / risen level / overflow to ceiling) and
   that the same gas in a larger room stays a floor layer. **This is your BS14 gamer-eye verdict.**
3. Confirm the fill is QUANTITY-DEPENDENT, not a fixed proportion: read `CoarseBandStep.Settle` (capacity-fill
   densest-first to `FloorCapacity = ½·FreeVolume`, overflow up) and the FILL-LEVEL test
   `CoarseSolverBatteryTests.L24_FillLevel_QuantityDependent_ThinThenRisesThenOverflows`.
4. Confirm per-band temperature: `CoarseSolverBatteryTests.L25_DifferentialBand_HotFloorDrivesGasUp_VsUniformCold_PerBandEff`
   (equal-average temps, hot floor leaves strictly less low).
5. Confirm `git diff origin/main` of the locked ADRs + t-2/t-3 artifacts is EMPTY and `Unsafe.dll.meta` matches
   main (it is `git skip-worktree`'d, so the live Editor's re-flips can't be staged; the LOCK + C1–C22 + Wave-1 goldens untouched).
6. **Record the kill-gate VERDICT** after the independent fresh-session G5 refutation + the Codex read-only
   pre-pass (no FishNet/PlayMode axis this leg — t-1 is headless by design).

## next

Route this RESULT **home to the direction `indie-game-development` (workflow-OS)**. **Kill-gate VERDICT =
PASS_WITH_NITS → t-2, OWNER-CONFIRMED** (198/198, mutation 76.76% ≥ 70, independent fresh-session G5 = PASS,
owner MULTI-QUANTITY legibility = PASS, owner-confirmed Codex P1 disposition: settle is correct, no change).
Delivered on the owner's merge-go: this leg's build commit `a868270` **merged `dev→main` + pushed to origin**
(owner-authorized this session). Follow-on is **t-2** (real DA-composed level + breach + coarse replication) on the LOCKed stream
+ the now-quantity-dependent coarse tier.

**t-2 SHAPE inputs carried home (do NOT lose):**
1. **Per-sector room CAPACITY + back-pressure/sink** — gives the overflow guard LIVENESS (t-1 guards only
   consistency; a no-sink room is terminal-on-overflow today).
2. **Cross-band per-species buoyancy INVERSION** — needs **per-species temperature** (each gas carrying its own
   T), a seam DISTINCT from the per-species RATE seam BS4 already names.
3. **Narrow BS4 at t-2 SHAPE** (with the owner): its "inverts past a cold light one" prose over-specifies the
   untested cross-band behaviour the per-band model can't coherently do — a planner action, NOT a builder edit.
4. **openspec lifecycle (L22):** apply the change spec delta to `openspec/specs/sim-core/spec.md` + archive the
   change folder — left for the direction/planner (it touches the planner-owned spec).

The builder does **NOT** author the next task CALL — handed back to the direction.
