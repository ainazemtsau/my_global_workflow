# CALL c-work-grid-v1-critical-path-plan-001

to: session
direction: indie-game-development
track: grid
play: work
node: g-4b92
task: v1-critical-path-plan
issued: 2026-07-20 (s-work-grid-current-authority-audit-verdict-001)

goal: |
  У владельца есть один понятный, принятый и исполнимый Grid V1 master plan: он ограждает legacy,
  замораживает минимальный общий contract, проводит Structure/NearGas и Actor/Object contact через
  него, доказывает controlled world change и передаёт готовые drops одной permanent Integration Lab.

context: |
  Принятая factual/priority основа: `work/grid-current-authority-audit-2026-07-20.md`. Владелец выбрал
  маршрут A точным ответом `A` после уточнения `грид-модуль сейчас в приоритете` и `у нас горят сроки`.
  Маршрут A означает: сразу запретить новые legacy dependencies; сохранить работающие primitives и
  proofs; заморозить минимальный Grid contract; заменять behavior paths по одному и удалять каждый сразу
  после доказанной замены; permanent Lab собирать последней.

  Planning input по Object ↔ Layer: `work/grid-object-layer-interaction-resolver-brief.md`. Временные
  вопросы других треков уже вынесены в `work/cross-track-recovery-notes-2026-07-20.md`; этот файл не
  является authority и не должен расширять критический путь.

  Program dependency context: `TREE.md` g-4b92, `work/program-v2-integration-lab-plan.md` M3/M5 и
  `knowledge/g9c41-gas-engine-SPEC.md`. Current audit basis был product main/origin/main `45b15623`;
  перед любым exact source disposition перечитать fresh product authority. Неопубликованная Program
  cleanup-линия `72c7c8c6 → c5c21c13 → baf8513c` остаётся внешним evidence/blocker, не Grid authority.

boundaries: |
  PLAN discussion only. Не менять product code/tests/assets/scenes, не запускать Unity/tests/build/
  Deliver, не создавать product branch/worktree или executor BUILD CALL, не удалять legacy физически.

  Не включать в Grid V1 critical path самостоятельный Wind Layer, generic flow/field framework, полный
  Multiplayer integration, damage/dose/balance, presentation, неизвестные будущие layers или permanent
  Lab implementation. Wind/Multiplayer остаются сохранёнными handoff-вопросами.

  Exact aperture остаётся отдельной defect-line со своим acceptance и не объединяется с Object ↔ Layer
  в один implementation leg. Не назначать выдуманный millisecond/min-spec budget. Не превращать Grid в
  владельца Gas physics, destruction policy, Character gameplay, network session или Integration Lab.

done_when: |
  1. Один owner-readable artifact `work/grid-v1-critical-path-plan.md` объясняет цель Grid V1 простыми
     словами, показывает один критический путь и отдельно называет cuts/deferred work.
  2. Legacy matrix называет точные current source families как `KEEP`, `QUARANTINE / NO NEW DEPENDENCIES`,
     `FIX SEPARATELY` или `DELETE AFTER REPLACEMENT PROOF`; у каждой DELETE-линии есть replacement,
     proof и immediate-removal trigger, поэтому две behavior authorities не остаются жить вместе.
  3. Minimal behavioral contract фиксирует только необходимые Grid V1 обязательства: stable address/
     projection; tick + committed revision envelope; committed read boundary; immutable change batch и
     независимое чтение несколькими consumers; canonical ordering; stale/live mid-tick fail-closed.
  4. Для спорных shape-defining мест сравниваются 2–3 конкретных варианта и даётся рекомендация: event
     delivery model, ownership boundary Grid↔Layer↔Actor и Object-contact coordination. HOW низкого риска
     остаётся будущему product PLAN.
  5. План разделён на dependency-ordered cohesive legs размером не больше focused half-day каждый, с
     exact outcome, proof, rollback и deletion trigger: legacy fence; Grid core; Structure/NearGas
     adapters; Actor occupancy/contact; exact aperture/world change; final legacy retirement; Lab handoff.
  6. Proof matrix сначала допускает два маленьких synthetic consumers для быстрого contract refutation,
     но закрывает real V1 только через NearGas и Actor/contact на одной committed revision. Wind/Water
     stubs не используются как ложное доказательство универсальности.
  7. Явно записано, что идёт строго последовательно до contract freeze, что после freeze технически
     независимо, и почему рекомендуемый execution default всё равно остаётся focused/sequential при
     горящих сроках, если владелец отдельно не включает параллельность.
  8. План содержит exact acceptance chain до M3/M5 и одной permanent Integration Lab, risk/stop list и
     первый bounded product planning handoff; он не открывает BUILD сам по себе.
  9. Владелец получает план в разговоре и фактическими словами принимает или исправляет его. Без этих
     слов остаётся same-position planning continuation, а product execution не открывается.

return: |
  Один work RESULT: путь к принятому или ожидающему verdict Grid V1 plan; owner words; legacy matrix;
  minimal contract; critical-path legs, proofs, cuts, risks и deletion triggers; один следующий
  planning-only/product-PLAN handoff или точный blocker. Отдельно перечислить всё, что не запускалось.

budget: one owner-present planning session
surface: owner-present

END_OF_FILE: live/indie-game-development/work/c-work-grid-v1-critical-path-plan-001-call.md
