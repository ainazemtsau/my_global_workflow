CALL c-work-launch-control-demo-release-control-installation-001
to: session
direction: indie-game-development
track: launch-control
play: work
node: g-b847
task: demo-release-control-installation-001
issued: 2026-07-24 (s-review-launch-control-demo-control-room-retirement-001)

goal: |
  Current Direction state exposes one Character-independent READY Demo Release
  Control root governed only by the accepted durable policy, and the legacy
  Demo Control Room cannot be mistaken for current authority.

context: |
  Owner verdicts: `PLEASE IMPLEMENT THIS PLAN`; `разблокируй`; `я хочу, чтобы
  ты всё завершил и в конце сказал: всё готово, всё проверено, можно приступать
  к конкретной работе`.

  Authority and evidence:
  - `knowledge/g-b847-demo-release-control-policy-v1.md` is current policy;
  - `history/2026-07-24-s-review-launch-control-demo-control-room-retirement-001.md`
    gives the binding fresh `PARTIALLY MET` retirement disposition;
  - `TREE.md` owns g-b847 and R0 g-5cf2 through R5 g-5632;
  - `NOW.md` is current routing truth;
  - `work/launch-control/demo-control-room.md` is unchanged historical text,
    already retired from authority but not yet converted to a tombstone;
  - `knowledge/g9c41-lanes-venues.md` declares the owner-panel render rules.

  Current transition facts are not completion evidence: Demo Basis is `NONE`;
  October is selected; February is owner-only; forecast is `NO DATE BASIS`;
  no permanent controller, tombstone, probe, target launch or release progress
  has been installed by the review.

  The installed controller must use one initial ordinary root id
  `c-work-launch-control-demo-release-control-cycle-001`, remain under stable
  track `launch-control` with `outcome_dispatch: true`, and be READY on exit.
  Its CALL must support fresh start-day, material-return, owner-verdict and
  hard-date-checkpoint cycles without storing a daily plan.

  Installation evidence must record PASS/FAIL for exactly these deterministic
  policy scenarios using the specified input, not substituted live status:
  1. NO-BASIS: October selected, basis NONE and durations missing -> R0 gap,
     `UNFORECASTABLE / NO DATE BASIS`, affected work WAIT, no definition launch.
  2. MISSED: an October hard gate passed unmet while inputs are also missing ->
     `MISSED` wins; February remains inactive without owner words.
  3. CREDIBLE: all intervals exist and conservative upper bound plus owner-set
     margin clears every gate with no blocker -> `CREDIBLE`.
  4. AT-RISK: all inputs exist but conservative bound misses a future gate ->
     `AT RISK`, named cut/decision and recompute, no automatic route switch.
  5. BASIS-STALE: consumer pins B1 and Canon makes B2 current -> exclude the B1
     launch and require target-owned RECONCILE; no controller-authored paste.
  6. INDEPENDENT: Character WAITING and Program BLOCKED while collision-free
     Grid/Gas outcomes exist -> both may rank; foreign waits are not global.
  7. RESOURCE-UNKNOWN: one shared Unity/machine/review surface is UNKNOWN ->
     PROTECT/exclude only that collision surface; unrelated candidate survives.
  8. ZERO: every candidate lacks authority, dependency or resource -> lawful
     launch set 0, exact blockers shown, controller remains READY, no busywork.
  9. MANY: two outcomes are independent and resources are known free -> show
     both; owner chooses 0..N; no automatic launch or foreign-root mutation.
  10. RECOVERY: accepted proof becomes unstable and a result proves one missing
      control mechanism -> STABILIZE affected proof, then bounded PROBATION of
      that mechanism; broader route invalidation uses REPLAN, no copied status.

boundaries: |
  Do not invent or approve Demo Basis, cuts, February activation, internal
  dates, private Steam facts, product/player evidence or release progress. Do
  not start target work, BUILD, publication, seven-day probe or binding
  post-install G5.

  Preserve Character, Program, Grid, Gas, Level, Canon, Visual and Marketing
  roots exactly. Do not mutate TREE/CHARTER, product repositories or technical
  HOW. Do not retain DCR snapshots, daily appendices, DAG/envelope hierarchy or
  copied current status as authority.

done_when: |
  1. `work/launch-control/demo-control-room.md` is a short historical tombstone
     that points to the policy, review receipt and Git history and says it is
     not current authority; its old body is not migrated into another file.
  2. One complete controller CALL for
     `c-work-launch-control-demo-release-control-cycle-001` implements the
     policy contract without current status, technical HOW or second hierarchy.
  3. NOW replaces only this installation root with that permanent root READY,
     Character-independent, under existing `launch-control/outcome_dispatch`.
  4. Live launch-control pointers and owner panel no longer route to the review,
     installation or old DCR as authority.
  5. A named installation receipt records all ten scenarios above with exact
     expected/actual output and all ten PASS; DCR authority reads/edits during
     evaluation equal zero.
  6. Demo Basis remains NONE, October selected, February owner-only, forecast
     NO DATE BASIS; no foreign root, product state or release-progress claim
     changes.

return: |
  One work RESULT with tombstone/policy/controller identities, permanent READY
  root, ten-scenario readback and exact unchanged foreign-state boundary. Do
  not claim seven-day probe or binding post-install G5 complete.

budget: one bounded controller/tombstone installation
surface: cli

END_OF_FILE: live/indie-game-development/work/launch-control/c-work-launch-control-demo-release-control-installation-001-call.md
