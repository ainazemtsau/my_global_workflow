# Газовый движок — план разработки по шагам (2026-06-26)

**Статус:** план развития после применения `R1-layered-reshape`. Источник истины по состоянию = `NOW.md`. Канон = `knowledge/g9c41-gas-engine-SPEC.md`. Полная архитектура/анализы = work/gas-engine-layered-reshape-{2026-06-26,R1-2026-06-26}.md + gas-cloud-vs-grid-analysis-2026-06-26.md + gas-variant-b-affordability-2026-06-26.md.

## Зафиксированные решения (сходимость)

- **Архитектура** (triple-vetted, 3 состязательных воркфлоу): слойный координирующий grid (газ 50 / структура 25 / размещение 25-резерв), grid авторитетен, гибрид с кап-сферой визуала. Облака-как-авторитет отвергнуты.
- **Комнаты:** дефолт — ~однородные малые/средние. **Большие залы НЕ спец-кейсим сейчас, отложено-расширяемо** (owner): если упрутся по замеру — (а) авторить зал как сегменты-комнаты с дверьми-во-всю-стену (дальнее грубее даётся даром), либо (б) far-only `mid-2.5D` + sub-partition. Этажи друг над другом = просто стопка комнат (объём-нейтрально).
- **Точность B:** full-3D там, где живёт настоящий-B вопрос; большинство B реклассифицируется в дешёвый монотон-порог по грубому тоталу. State-driven-near-demote + lazy-resolve — отвергнуты.
- **Сеть:** lockstep (только инпуты по проводу) — оставляем; это и есть то, что делает большой детальный газ дешёвым по каналу. State-sync хуже.
- **Применено:** Факт 2 (слойный, ратифицирован) + сплит разрешений + контракт меток + правило двери + кап-сфера (4м@50см старт, ADR) + предикат монотон-B + settled_flag. `c-exec-013` → `c-exec-014`. `mid-2.5D` отложен (резерв).

## Исходная задача — решена

Тред начался с **S0 / ридера уровня Dungeon Architect** + баг с дверью (газ открыл всю стену) + идея «поразносить поверхности по типам». **Решено архитектурой:** ридер = слой СТРУКТУРЫ (читает геометрию DA + метки на модулях: геометрические валидируются против меша, семантические авторишь ты); дверь = настоящая апертура; «типы» = маркерная топология. Это строит `c-exec-014`.

## Порядок сборки — маленькие, играбельные шаги

Каждый шаг: WHAT + зелёные автотесты (RED-first, независимый тест-автор) + **сцена, где владелец сам играется** (не только автотесты).

| Шаг | Что | Автотест | Владелец сам трогает |
|---|---|---|---|
| **0. Применить фундамент** (OS) | `R#`→канон, завести `c-exec-014`, коммит | — | — (канон/состояние) |
| **1. Ридер → сетка** (слой структуры) | уровень DA/фикстура → целочисленная сетка + комнаты (region_id) + апертуры дверей | re-воксел байт-идентичен; фикстура; апертура валидируется-против-меша; типы-метки прочитаны | сцена: грузишь уровень, **видишь сетку + метки комнат + апертуры дверей** (без газа). ← глазами проверяешь «двери/типы прочитаны верно» — исходный вопрос |
| **2. Газ заполняет комнату** (face-flow) | спавн газа → течёт через апертуры, стелется низом (эмерджентная высота) | MONOTONIC, SLIT-SEEPS, WALL-ZERO, MASS-CONSERVATION, EMERGENT-HEIGHT | спавн-в-клетку + орбит-камера, **смотришь как заполняет** и течёт к соседу. Owner-eye «точно + весело» |
| **3. Детерминизм + lockstep** | zero-float скан; loopback 2 процесса байт-идентично | скан как check.ps1 гейт + planted-float RED | видишь «2 копии идут одинаково» |
| **4. Бенчмарк-харнесс в Unity** | сцена с кнопкой → меряет активные клетки + кадр-тайм | — | **жмёшь кнопку, видишь числа**: типовой сценарий + стресс (большой объём / каскад) |
| **5+. Дальше** | пролом (S5), реакции, монотон-B порог, типы-метки структуры | каждый — свой RED + один играбельный шаг | по одной механике |

### Шаг 4 / замер — важная оговорка про железо

Газосим — это **CPU**; RTX 4090 ему не помогает; топовый процессор владельца делает замеры **оптимистичными**. Бенчмарк показывает **клетки + мс**, а вывод делаем по **бюджету слабого железа** (потолок ~200к активных клеток на слабом ядре), с запасом — НЕ «у меня на риге плавно = шипим». **Первая build-задача = «проба зала»** (1 игрок в большом открытом объёме, full-3D, Burst/Jobs) — это #1 неизмеренное число всей архитектуры; оно решает, нужны ли вообще оптимизации больших залов. + RED-оракул монотон-B (full-sim vs порог, с не-монотонным контролем, который ОБЯЗАН провалиться).

## Ритм

- По одному маленькому шагу; каждый кончается играбельной сценой + зелёными автотестами, прежде чем следующий.
- Сборка идёт в `GasCoopGame_dev` (MCP + ветка dev); владелец гоняет играбельные сцены + бенчмарки; OS применяет RESULT-ы домой отдельным writer-ом.
- `mid-2.5D` / sub-partition / position-hints / blob-render / placement-механика — отложенные резервы, включаются по необходимости (замер), не сейчас.

## После c-exec-014 (S0 доставлен) — s-work-019, 2026-06-26

**S0 закрыт.** `c-exec-014` (слойный S0) влит в GasCoopGame `main` @824948d (запушено), `-Deliver` зелёный (mutation 77.6%, fresh-session G5 Sonnet COULD-NOT-REFUTE), проверено первой рукой. **Шаги 1–4 роадмапа доставлены**, включая **пробу зала** — #1 неизмеренное число.

**Проба зала (результат):** 196 608 клеток / **24,5 мс/тик** в среднем (65,5 макс) на СИЛЬНОМ CPU; активных всего **1 562** из 196 608. Плотный солвер считает ВСЕ клетки каждый тик (~125× лишней работы); мс/тик почти не зависит от заполнения.

**Что это значит (стратегически):** на полном объёме 196k плотный зал уже **на грани** на сильном CPU, а слабое ядро ~2–4× медленнее ⇒ жизнеспособность БОЛЬШИХ залов на слабом железе **не доказана**, пока не появится sparse-солвер (обещает ~100× запаса; регистр §9.3 уже заложен, но `VoxelFaceFlow.Step` его пока не потребляет). НО: ближние слайсы S1–S5 идут по обычным комнатам, где плотный солвер замерен нормально (типовой сценарий 3 456 клеток = 0,525 мс/тик).

**Решения планнера по 5 пунктам из RESULT (владелец делегировал):**
- `d-sparse-solver-defer-001` — **ОТЛОЖИТЬ** sparse-солвер + пул буферов (GC) + перезамер на слабом CPU к **гейту больших залов**. Это НЕ следующий leg: большие залы отложены-зарезервированы, S1–S5 их не требуют. Это именованный, готовый optim-leg, который срабатывает ПЕРЕД любой фичей большого зала; вердикт по жизнеспособности залов завязан на него. Veto открыт: если владелец хочет front-load перф-дериск вместо S1 — следующий CALL меняется.
- `d-overflow-guard-001` — **ПОСТРОИТЬ** класс-wide гард на `int*int`-переполнение (build-скан, RED-first, посаженный контроль ОБЯЗАН провалиться) + **авторизовать** дописывание строки spec-silence про экстремальные координаты (`coord·spa`). Оба — в `c-exec-015` (H1/H2), тем же leg.

**Следующий слайс = S1** (`c-exec-015`, work/c-exec-015-call.md): выброс-при-спауне + выдавливание телом — первый ДИНАМИЧЕСКИЙ газ на фундаменте S0, внутри ЗАМКНУТОЙ целочисленной модели (без velocity/advection — нужда в них = STOP-эскалация). Открывается PLAN-ом (владелец рядом), §Re-sync v8→текущий сначала. Дальше — S2 (мультиплеер lockstep loopback).
