# CALL c-work-grid-v1-executor-plan-001

to: session
direction: indie-game-development
track: grid
play: work
node: g-4b92
task: v1-executor-plan
issued: 2026-07-21 (s-work-grid-v1-critical-path-plan-accepted-001)

goal: |
  У владельца есть принятый и согласованный с текущей product authority Grid V1 executor plan:
  точный source/legacy disposition и будущие Grid-owned roots реализуют master plan, не забирают
  consumer behavior у других треков и сохраняют обязательный Multiplayer handoff/gate.

context: |
  Binding Direction authority: `work/grid-v1-critical-path-plan.md`, owner verdict
  `Принять план с обязательным Multiplayer handoff/gate`.

  Factual/priority basis: `work/grid-current-authority-audit-2026-07-20.md`, accepted route A.
  Planning input: `work/grid-object-layer-interaction-resolver-brief.md`. Integration acceptance:
  `work/program-v2-integration-lab-plan.md`, especially M3/M5 and the separate co-op claim boundary.

  The master plan freezes the plain responsibility split and eleven <=focused-half-day Grid legs.
  Grid owns shared spatial addressing/projection, confirmed revision/change transport, generic
  Layer↔Layer read boundary and Object↔Layer Resolver/index/subscriptions. Layers and objects own
  their state, simulation, identity, footprint, adapters and consequences.

  Current product status was observed clean at `main == origin/main @
  f6e4f725543514bd67441d4be9ca181392f48c73`; this is evidence, not a launch pin. Re-read current
  product AGENTS/contract/registry/SPEC/ADR/PLAN/code/tests first-hand before any exact disposition.
  If authority is unavailable or materially contradicts the accepted master plan, return a precise
  same-position checkpoint; do not silently choose.

boundaries: |
  PLAN discussion only. Do not mutate the product repo or create a product branch/worktree; do not
  run Unity, tests, build or Deliver; do not issue BUILD, PAIR-CANDIDATE or another engineering root.

  Do not implement or design Gas, Water, Wind, Character, Loot, damage, dose, balance, presentation,
  multiplayer transport/session or Integration Lab behavior. Do not create real or placeholder
  Water/Wind layers/adapters/fixtures. Do not treat synthetic layers as real-consumer acceptance.
  Do not fold exact aperture into Resolver or silently select a second real consumer layer.

  Preserve the accepted master-plan ownership, both generic paths, eleven-leg half-day cap,
  consumer handoffs, mandatory two-peer Multiplayer gate and existing-dashboard recommendation.
  Exact source detail belongs only in the subordinate executor appendix.

done_when: |
  1. One owner-readable subordinate artifact `work/grid-v1-executor-plan.md` is based on first-hand
     fresh product authority and reconciles current Grid/Layer/Object/tick/event/read surfaces without
     changing the accepted master-plan meaning.
  2. An exact source/legacy matrix classifies current artifacts as `KEEP`, `NO NEW DEPENDENCIES`,
     `FIX SEPARATELY` or `DELETE AFTER REPLACEMENT PROOF`, with the evidence, replacement trigger and
     rollback/removal condition for every named disposition.
  3. The eleven Grid-owned legs become dependency-ordered future product roots no larger than a focused
     half-day, each with one cohesive outcome, proof, conflict surface, rollback and removal trigger;
     no implementation or product root is launched.
  4. Object↔Layer and consumer-driven Layer↔Layer remain separate, synthetic two-layer proof claims no
     real consumer completion, and the unknown second real consumer stays an explicit M3/M5 blocker.
  5. Separate named handoffs cover Gas, Character, Level/Structure, Program/Multiplayer and Integration;
     no consumer behavior, real adapter or gameplay proof is reassigned to Grid.
  6. The mandatory Multiplayer gate names the Grid-owned deterministic/checksum output, the Program-owned
     identity/input/order/two-peer proof, its place before any co-op-ready claim, and the exact trigger for
     a later separate Multiplayer map decision if one bounded Program slice is insufficient.
  7. The dashboard recommendation remains one maintained section in the existing owner panel; no separate
     render is built.
  8. The owner receives the executor plan and accepts or corrects it in actual words. Without those words,
     keep this same planning position and do not open product execution.

return: |
  One work RESULT with the accepted or owner-pending executor plan; exact owner words; current-product
  inventory/conflicts; source/legacy disposition; future half-day roots; both generic paths; synthetic
  and real acceptance split; consumer and Multiplayer handoffs; sequence/parallel/blockers; cuts/risks;
  one next planning handoff or exact blocker; and an explicit not-run list.

budget: one fresh owner-present planning session
surface: owner-present

END_OF_FILE: live/indie-game-development/work/c-work-grid-v1-executor-plan-001-call.md
