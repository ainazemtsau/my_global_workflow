CALL c-core-0-gas-states-001
to: session
direction: indie-game-development
play: local/canon-forge (Кузница v2)
node: g-d3a8
piece: core-0 — газ: состояния и что они значат
mode: МЕХАНИКА (адаптировано: таблица СОСТОЯНИЙ вместо таблицы глаголов)

goal: |
  Владелец имеет однозначный, спарринг-выдержанный ответ (или честное «обсуждается») на ОДИН вопрос: что такое газ
  в игре для игрока — какие у него состояния, что каждое состояние значит (видно? вредит? реагирует? двигается? где
  лежит?), что переключает состояния (симом, не скриптом), и как это ложится на движок. Без этого куска кандидаты
  наполнения пузыря (core-1) повисают — владелец поймал это в первой же core-1 сессии.

context: |
  Новая сессия. Сама читает канон-репо по START.md В ПОРЯДКЕ: CONSTITUTION → CORE → INDEX → QUEUE → SESSION
  (github.com/ainazemtsau/gas_coop_game_canon, клон C:\projects\gas_coop_game_canon). Вход с любого чата: «что дальше»
  → этот кусок (QUEUE core-0 = СЛЕДУЮЩИЙ).
  - Материал от владельца (2026-07-09, слова его — НЕ вердикт, довести обсуждением): «скорее всего у нас будет два
    состояния — спящее и активированное (пока не подтверждено); естественно, и тот и тот должен забирать [в пузырь],
    но активированное — гораздо сложнее».
  - CORE (принято направлением): спящие газы + разведка = вход; сон оставляет следы, «нет следа — нет газа»; «сон» =
    ДИЗАЙН-состояние, не движковая оптимизация (поправка владельца); спящий ≈ безвреден → бодрствующий опасен →
    разбуженный не вовремя смертелен; три фазы этажа (тихо → движение → эскалация) — это ЭТАЖНЫЙ ритм, кусок core-4,
    здесь не решается целиком.
  - Материал из питча (sources/pitch-ono-dyshit-2026-07.md): триггеры пробуждения по типам — тепло/огонь/холод,
    перепад давления (дверь/пролом/струя), скорость движения игрока (медленно можно, бег будит), начало добычи,
    цепь от соседа, разрыв трубы; на wake: появляется фронт, меняет плотность/высоту, становится реактивным/вредным.
    Это кандидатский материал, порядок и состав не предрешены.
  - Известные узлы, которые кусок обязан развязать:
    (а) ВИДИМОСТЬ по состояниям: спящий «почти невидим» + «нет следа — нет газа» — что честно видит игрок в каждом
        состоянии? Кандидат из улова: газ становится ВИДИМЫМ внутри шарика (шарик-проявитель) — обсудить, не предрешать.
    (б) «ОЗЕРО»: термин под тяжёлый осевший газ; где и как «спит» лёгкий газ (потолок? щели?) — что вообще значит
        «лежит» для каждого веса.
    (в) Переключение: бинарно (спит/активен) или есть промежуток («потревожен»)? Переключает СИМ (поток/тепло/контакт),
        не скрипт и не магический aggro.
    (г) Обратимость: активированный засыпает обратно? Когда? (масса сохраняется, «здание помнит» — материал питча).
  - Движок (столп 6/П3 — «ноль сверх запланированной дороги»): байт-сон = оптимизация равновесия (регион без потока
    не считается); дизайн-сон = ОТДЕЛЬНЫЙ дешёвый слой поверх (состояние + триггеры + активация на wake) — совместимы,
    не путать. Sc-sense (доза у актёра) сдан; Sc-damage (последствия) ждёт дизайна.
  - Формат захода (адаптация МЕХАНИКИ): подготовка (движок здесь + референсы Phasmophobia/Lethal/R.E.P.O. — как они
    делают «опасность в состояниях» читаемой + где страх↔жадность) → ТАБЛИЦА СОСТОЯНИЙ (состояние / что видит-слышит
    игрок / что делает газ / что переключает туда-обратно / чем грозит / пример на одном газе) → ≤3 кандидата-МОДЕЛИ
    состояний, каждый прогнан ОДНОЙ сценой посекундно на двоих (игроки входят на тихий этаж, находят газ, тревожат его)
    → рекомендация с причиной. Кандидаты уровня «газ в шарик»: просто, бытовой интуицией, без душноты.
  - Компас CORE обязателен; слово «детская» в формулировках запрещено (правка владельца).

boundaries: |
  Один вопрос по умолчанию; кластер со следами-разведкой (core-5) допустим ТОЛЬКО с названной причиной. Решаем ТОЛЬКО
  семантику состояний газа. НЕ решаем: наполнение пузыря (core-1 ждёт этот кусок), этажный ритм целиком (core-4),
  каталог следов (core-5), ростер/имена, числа порогов/урона, экономику, название. Принятое ядро (CORE) не
  переобсуждать; замена куска ядра — только открыто. Не поддакивать: слабости каждого кандидата, включая владельческий.
  Вердикт = дословные слова владельца; «обсуждается» — валидный исход. Боковое → улов (≤5). Стоп-краны: «рамка», «парковка».

done_when: |
  Сессия закрыта RESULT: карточка core-0 с дословным вердиктом владельца — ИЛИ честный статус «обсуждается»; таблица
  состояний заполнена; сцена на двоих посекундно прогнана для рекомендованной модели; узлы (а)–(г) либо решены, либо
  явно оставлены с пометкой куда; линзы прогнаны где применимы (fun-статус честный: «нужен грейбокс» валиден);
  INDEX/QUEUE обновлены (core-1 разблокируется, если состояния определены); улов разобран владельцем; LOG + history.

return: |
  Русский читаемый заход (подготовка → таблица состояний → ≤3 модели со сценой → рекомендация), затем спарринг,
  сходимость, вердикт владельца; в конце — RESULT.

budget: одна сессия

END_OF_FILE: live/indie-game-development/work/c-core-0-gas-states-call.md
