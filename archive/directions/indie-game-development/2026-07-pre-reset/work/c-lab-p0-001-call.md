# CALL c-lab-p0-001

to: executor
for: g-9c41 / Стенд «Газовая лаборатория» P0 (линия C, прототип-лента)
issued: 2026-07-10 (s-lanes-install-001)

ПЕРВЫЙ ШАГ СЕССИИ (owner-verdict guard): предъявить владельцу решение D1 «прототип-лента» —
отдельная lab-сборка (свой asmdef), общается с ядром ТОЛЬКО через существующий публичный API,
полные ворота контракта НЕ применяются к lab-скриптам (ядро/сеть/чексум остаются под полными
воротами как раньше; лента аддитивна, работающие ворота не ослабляются). Получить его «да»
В ЭТОЙ СЕССИИ до любого кода. Нет «да» — STOP, checkpoint.

goal: |
  Верстак v0 в НОВОМ worktree lab: (1) игровая runtime-палитра, зеркалящая публичный API
  VoxelSandboxDirector (pause/step/run/скорость в пределах существующих клампов; burst/puff;
  vent/jet/erupt; двери/клапаны; FLOOD; ClearGas/Rebuild; выбор уровня по LevelSource);
  (2) свободная камера WASD+мышь (Tab прячет UI); (3) клик-проба клетки: концентрация/тип/потоки/
  телеграф через существующие read-only свойства; (4) HUD v0: тик, ms/тик со спарклайном (внешний
  Stopwatch вокруг шага), ActiveFaceCount, масса по типам; MeaningChecksum — только КНОПКОЙ с
  подписанной ценой; (5) лампа детерминизма: LoopbackOwnerEyeDirector положить в сцену + 1–2
  канированных GasScenario-пресета (Builder API существует) + plant-divergence самопроверка;
  (6) панель «БЛИЖНИЙ/ДАЛЬНИЙ УРОВЕНЬ: НЕ ПОСТРОЕНО» видна с первого запуска. Все кнопки впрыска
  мимо честного пути носят бейдж INTERIM (на main это все).

boundaries: |
  ZERO изменений Core/**; не трогать dev/dev_2/main; больших сцен НЕТ (это P1 — сначала чинка
  рендер-детектора и читальный шов); никакого сериализатора/рекордера; никакого оверлея
  спящих/бодрых клеток (нужен читальный шов линии A). Unity-MCP недоступен → STOP, инструкции
  владельцу по шагам, никаких обходов (правило владельца, контракт v16/v17).

venue-setup (правило 1 карты — ОБЯЗАТЕЛЬНО): создать worktree по
knowledge/g9c41-lanes-venues.md §Создание; проверить назначение/свободу/отсутствие второго
редактора; base = текущий origin/main (§Re-sync); check.ps1 зелёный ДО первого изменения.

done_when: |
  Владелец в Play-режиме управляет всем из сцены без инспектора (owner-run gate — шаги,
  отправленные владельцу, не считаются done до его подтверждения); лампа детерминизма работает
  (AGREE + plant-divergence ловится); INTERIM-бейджи и NOT-BUILT-панель на месте; check.ps1
  зелёный (lab ничего не ломает); RESULT + следом G5-CALL в конвейер.

return: RESULT (продуктовые коммиты на ветке lab; мерж — очередью, слот уточняет борд).
budget: 1-2 лега.

launch:
  lane: C
  venue: GasCoopGame_lab (ветка lab, редактор №3) — СОЗДАТЬ по карте
  machine: ПК
  base: origin/main текущий (§Re-sync)
  conflict_surface: только новая lab-сборка — ничего общего с A/B/D
  depends_on: []
  merge_queue: после текущих слотов; уточнить бордом при закрытии
  gates: G5 = свежая сессия; owner-eye = сам верстак (owner-run)

END_OF_FILE: live/indie-game-development/work/c-lab-p0-001-call.md
