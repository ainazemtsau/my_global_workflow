# RESULT -- s-health-core-kernel-cleanslate-reset-001 (clean-slate reset of health-ai)

direction: health - node: g-health-core - bet: b-health-core-kernel
trigger: owner directive -- "сделай ещё раз ресёрч, давай полностью заново ресетним всё ... чистый лист, никаких
ресёрчей, ни личных данных ... все папки должны быть удалены" -> "го"
date: 2026-06-21

## outcome
health-ai reset to a TRUE blank slate on top of the proven kernel engine, classified + executed + adversarially
verified across two workflows. Committed in the PRODUCT repo (8e981ec, UNPUSHED). Nutrition deliberately NOT
rebuilt -- left absent so the owner can watch the cold-start re-author it from zero.

## what was done (health-ai commit 8e981ec)
- DELETED whole: the entire x_nutrition/ domain (37 files: research, program, cycle, menu, recipes, grocery,
  fallbacks, reviews, mid-run cursor + workflow graph, handoffs, procedures, index), acceptance/kernel/journey-proof.md
  (absent = pending to the kernel-spine check, never blank; regenerated by the next cold-start), and the empty root
  stubs recipes/ + logs/.
- BLANKED to clean templates (shape kept, all personal data removed): core/profile/owner-facts.md +
  derived-anchors.md (empty {}), core/programs/owner-current-program.md + owner-current-program-lifecycle.md
  (uninitialized), core/extensions/registry.md (no active domains), core/index.md + acceptance/core/evidence-summary.md
  (de-personalized).
- REWROTE tools/check_core_evidence.py -- it had the owner's real data (age 35 / 125 kg / smoking context /
  owner_message_2026-06-19) AND "x_nutrition active" baked in as PASS assertions; rewritten to assert the blank slate
  (so "all green" no longer smuggles personal data back in).
- DE-NUTRITIONED AGENTS.md / SYSTEM.md / README.md / CHATGPT_PROJECT.md (removed nutrition-as-live wording; the six
  load-bearing keywords schema_version/rounding/PLAN/LOG/Slugs/irreducible preserved in AGENTS/SYSTEM so two checks
  stay green) + reworded one stale example line in core/governance/thin-domain-contract.md.

## verification (own + adversarial)
- Plan: workflow wf_1bcd4174 (classify whole repo + 2 skeptics: nothing load-bearing deleted, nothing dirty kept).
- Execute + verify: workflow wf_7dce4f4d (1 executor + 3 skeptics): zero-dirt CLEAN, engine-intact CLEAN,
  checks-green flagged only 2 non-blocking notes (one stale example line -> fixed; not-yet-committed -> now committed).
- Own confirmation: ran all 4 gates directly = exit 0 each; git status = only the expected 50-file delta; x_nutrition
  + journey-proof + recipes/ + logs/ confirmed gone. Pre-kernel cold-start drives entirely off core/ (bootstrap.md
  verified) so deleting the whole domain is safe + recoverable via git history.

## state_changes applied (writer = this session, post-RESULT)
- NOW.md: added a PROGRESS note to CALL c-health-core-kernel-incident-fix-001 -- HEALTH-AI CLEANUP escalated to a full
  reset and DONE (8e981ec); NEW next = owner-watched cold-start that re-authors nutrition clean AFTER folding in the
  engine fixes (abstract-first rule + proof-of-load marker + WA-K2/K3 clarifier); remaining scope items stand.
- LOG.md: appended the clean-slate reset line.
- history/: this file.

## next
1. Fold the 3 engine fixes into the instructions BEFORE the cold-start, so the fresh nutrition is born correct:
   (a) the abstract-first / never-pick-concrete-foods rule as a first-class contract + check; (b) the proof-of-load
   marker in the entry instructions (visible header every reply); (c) the WA-K2/K3 ceiling clarifier.
2. Then the owner-watched COLD-START run in a fresh chat re-authors nutrition from zero through the gate (this also
   regenerates acceptance/kernel/journey-proof.md = the WA-K10 proof on the fresh domain).
3. Owner pushes health-ai (8e981ec) per protocol; OS wt/health commits pushed/merged per protocol.

END_OF_FILE: live/health/history/2026-06-21-s-health-core-kernel-cleanslate-reset-001.md
