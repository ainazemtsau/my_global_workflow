# RESULT — s-health-core-kernel-wave0-derisk-001 (work; Wave-0 de-risk)

direction: health · node: g-health-core · bet: b-health-core-kernel
play: work (desk de-risk; session-kind, no executor, no build, no owner gate)
call: c-health-core-kernel-wave0-derisk-001 · date: 2026-06-20

## outcome
Wave-0 de-risk verdict produced and **fresh-read confirmed**. BOTH binding riskiest assumptions **PASS**:
- **t-1 zero-engine subtraction** — every engine clause of the two hardest nutrition files
  (`x_nutrition/procedures/workflow-router.md` + `x_nutrition/workflow/graph.md`) maps to an inherited kernel
  contract; post-subtraction remainder = exactly the WA-K9 thin shape `{namespaced data, one procedure per term,
  one registry line, one state-machine instance}`; `named_engine_residual = null`. The nutrition router FILE
  collapses into the inherited core router (KR10).
- **t-2 cold-start bootstrap-recursion (KQ2/O1)** — realizable cold: non-circular (fixed point = the kernel-owned
  universal bootstrap machine, a TIER-2 engine, file-absence cold-start detection), no hand-seeded cursor (the
  initial cursor is the 2nd governed write, an OUTPUT of the transition), and the first SEED→PROPOSED→ACTIVE walk
  IS the binding WA-K10 journey-proof.

The gated executor CALL `c-health-core-kernel-spine-journey-executor-001` is **RELEASED**. Three named residuals
(all additive, none re-opens or weakens a signed row) folded into the executor CALL's context/PLAN; one is a
non-blocking owner decision (D-kernel-1).

## evidence
- Artifact (the two ledgers + verdicts + the surviving seam): `live/health/work/wave0-derisk-g-health-core-kernel.md`.
- Fresh read (mirrors G5 separate-context discipline): workflow **wf_c9806260-68d** — 2 independent provers + 6
  adversarial refuters (3 lenses per task), each refuter in its own fresh context attempting to PRODUCE the FAIL
  condition. Full 50-row clause ledger + every refuter's reasoning in that transcript.
- t-1: verdict PASS; residual `is_clean=true`, `named_engine_residual=null`; refuters [structural-residual,
  engine-code-residual, mapping-handwave] all PASS_HOLDS, `survives_rebuttal=false` on every candidate.
- t-2: prover PASS (`is_circular=false`, `needs_hand_seeded_cursor=false`, `is_genuine_journey_proof=true`);
  refuters [gate-recursion, degenerate-journey] PASS_HOLDS; refuter [chicken-egg-regress] raised F-C
  (registry-line governance seam, `survives_rebuttal=true`) — adjudicated NOT a t-2 FAIL (no hand-seed, no
  circularity) and non-blocking → PLAN + decision D-kernel-1.

## adjudication (gate authority = this session)
- t-1 FAIL bar = "a named non-empty engine residual the kernel does not absorb." NOT met (3/3 refuters PASS_HOLDS;
  the closest finding — per-domain executable validators — is removable scaffolding absorbed by KL7+W1+KC7). PASS.
- t-2 FAIL bar = "it can only be walked by hand-seeding a cursor first → refutes KQ2/O1." NOT met (bootstrap is
  non-circular and the cursor is the 2nd write). The surviving F-C is a one-level governance-coverage seam on the
  registry-line write, not the FAIL condition and not architecture-threatening (low blast radius; content still
  walks the gate; nutrition already attached so the spine/journey is unaffected). PASS, with F-C → PLAN + decision.
- Disposition mirrors how converge-verify handled KQ-rev / KQ-concurrent (PLAN residuals, not bounces) and respects
  "don't break what works" — additive, no signed row weakened, the servant-box build is not stalled.

## residuals folded into the executor CALL (input/PLAN only; done_when §WA-K1–K12 unchanged)
- **F-A (t-1, executor-input):** the firewall-by-subtraction must DELETE the per-domain executable validators
  (`tools/check_nutrition_*.py`, `acceptance/x_nutrition/*` fixtures, `x_nutrition/contracts/normalizer-contract.md`)
  and replace them with the ONE kernel-universal KC7 LLM-evaluated barrier (the write-barrier de-couple already
  in-scope). Confirms the subtraction target is larger than the two named files.
- **F-B (t-1, executor-input):** the live cursor `x_nutrition/state/current-workflow.md` holds KM1/KC2-forbidden
  content (`current_weight_kg`, clinical `post_smoking_cessation_context`) → attach-time conformance migration on
  the lift (out-of-band task_bccaef53 spawned by the refuter).
- **F-C (t-2, PLAN-residual → D-kernel-1):** the registry-line write that grants a domain run-authority has no
  signed governance home (gated-as-attach-SEED | KM11-style control-file exemption | decide-and-inform). Resolve at
  PLAN before the first COLD new-domain attach (deferred second-domain leg).

## decisions_needed
- **D-kernel-1** (blocking: NO; tagged to the deferred second-domain-attach leg). Question: how is the registry-line
  write governed (attach = 1 folder + 1 line, grants domain run-authority; KG6/KA2 treat it as an ungated mechanical
  append, KV1 would gate it by default, no KM11-style exemption exists)? Options: (1, recommended) infrastructure
  control-file, not under the content gate, decide-and-inform — bad_because: AI could attach an empty domain without
  approval, but an empty domain with no ACTIVE definition produces nothing ACTIVE, blast radius low; (2) registry
  line rides the bootstrap journey as the attach-SEED (gated) — bad_because: heavier attach; (3) explicit KM11-style
  exemption row — bad_because: extra ceremony = option 1 with a term. Recommendation: 1. Default if unanswered: take
  option 1, record in the second-domain-attach leg PLAN.

## captures
- F-A: on the lift, delete the per-domain executable validators; logic is fully absorbed by the one kernel KC7 barrier.
- F-B: live nutrition cursor holds KM1/KC2-forbidden content → attach-time conformance migration (task_bccaef53).

## state_changes applied (writer = this Codex session, post-RESULT)
- NOW.md: tasks.t-1 active→done (+done_note); tasks.t-2 active→done (+done_note); open_calls
  c-health-core-kernel-wave0-derisk-001 in_flight→done; open_calls c-health-core-kernel-spine-journey-executor-001
  ready_gated→ready (gate_cleared 2026-06-20); decisions += D-kernel-1; next replaced with the RELEASED executor
  CALL (de-risk CALL + GATED marker dropped; F-A/F-B added to context, F-C added to boundaries; §WA-K1–K12 done_when
  untouched).
- LOG.md: appended the Wave-0 de-risk line.
- work/wave0-derisk-g-health-core-kernel.md: created (the de-risk evidence artifact).

## play_check
- 1 Recite: done — restated t-1/t-2 goal+done_when and the bet they gate (opening contract).
- 2 Owner inputs: skipped — not owner-content (internal architecture de-risk verdict, not something the owner lives
  by/operates/sends); no owner inputs needed (boundary: no owner gate).
- 3 Do the work: done — ran the de-risk as a workflow (2 provers + 6 adversarial refuters in fresh contexts),
  produced both ledgers + verdicts; no scope expansion (3 findings → captures/PLAN/decision, not acted on inline).
- 4 Self-check: done — compared output to each done_when point: t-1 residual ledger present + thin-shape clean
  (PASS); t-2 written verdict present, cold/non-circular/journey-proof (PASS); both confirmed in a fresh read.
- 5 Close: done — RESULT with the two ledgers (evidence in work/), PASS/PASS verdict, next = released executor CALL.

## log
Wave-0 de-risk: t-1 zero-engine PASS + t-2 cold-start bootstrap PASS, fresh-read confirmed; executor CALL released;
3 residuals folded (F-C → non-blocking D-kernel-1).

## next
Release CALL **c-health-core-kernel-spine-journey-executor-001** (to executor, health-ai; done_when = §WA-K1–K12
VERBATIM frozen; context now carries F-A/F-B as executor-inputs and F-C as a PLAN-residual tied to D-kernel-1).
See `live/health/NOW.md → next`.

END_OF_FILE: live/health/history/2026-06-20-s-health-core-kernel-wave0-derisk-001.md
