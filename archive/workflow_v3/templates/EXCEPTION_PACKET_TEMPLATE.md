# Exception Packet Template

status: template

## Exception Packet

`status`: blocked

`exception_type`:

`blocked_action`:

`missing_or_conflicting_item`:

`why_continuation_is_unsafe`:

`allowed_next_move`:

`return_destination`:

## Boundary

STOP beats guessing. Exception output blocks the current action until source, procedure, role, write, acceptance, legacy, conflict, validation, or path issue is resolved.

END_OF_FILE: workflow_v3/templates/EXCEPTION_PACKET_TEMPLATE.md
