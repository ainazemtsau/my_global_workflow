# Derived Gate Check Template

status: template

## Derived Gate Check

`gate_check_id`:

`gate_type`: strategy | discovery | delivery_flow | verification | interface_dependency | risk_gate | memory_learning

`generated_because`:

`blocked_action`:

`required_evidence`:

`required_decision`:

`gate_result`: pass | fail | defer | replan

`next_move_packet_ref`:

`quality_check_owner`: workflow_v3/interfaces/15_PARENT_INTEGRATION_AND_IMPACT_INTERFACE.md#quality-checks

## Boundary

Derived Gate Check is an internal procedure check record generated by a boundary crossing, evidence gap, risk, or required decision.

It is not a separate runtime entity, accepted state, or route authority.

END_OF_FILE: workflow_v3/templates/DERIVED_GATE_CHECK_TEMPLATE.md
