# Node Closure Summary Template

status: template

## Node Closure Summary

`node_id`:

`closure_status`:

`material_result`:

`evidence_refs`:

`acceptance_decision_refs`:

`accepted_changes`:

`rejected_paths`:

`unresolved_blockers`:

`map_update_candidates`:

`memory_candidates`:

`next_move_candidate`:

`closure_fact`:

`limitations`:

## Boundary

Node closure is not implicit accepted state.

Closure summarizes evidence, decisions, and candidates; accepted state changes require the explicit acceptance/update path.

END_OF_FILE: workflow_v3/templates/NODE_CLOSURE_SUMMARY_TEMPLATE.md
