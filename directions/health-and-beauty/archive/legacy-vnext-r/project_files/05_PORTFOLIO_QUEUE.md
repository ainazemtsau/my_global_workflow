# 05_PORTFOLIO_QUEUE.md

```yaml
project_file_projection: 1
schema: direction_project_file_projection.v1
source_file: "directions/health-and-beauty/project_files/05_PORTFOLIO_QUEUE.md"
canonical_source: GitHub repository file
projection_status: fresh_after_p0_phase_start_patch
activated_at: "2026-05-12"
```

## Canon rule

This file is an active GitHub Direction runtime file. If it conflicts with another current GitHub Direction file, return Context Request; do not invent state.

## Current queue state

Current Phase: `directions/health-and-beauty/phases/first-working-health-body-operator-training-v0`

Current Phase status: `active`

Latest closed Phase: `directions/health-and-beauty/phases/ai-nutrition-operating-layer`

Latest closed Phase status: `closed_complete`

Active Goal: `none`

Next route: `G1_GOAL_SHAPE`

## Current priority

Shape the first Goal for `first-working-health-body-operator-training-v0`.

Goal seed:

`shape-operational-boundary-and-training-process-v0`

The first Goal must create a precise Goal Contract for building the autonomous Health/Body Operator training process v0. It must preserve the boundary that workflow builds/changes systems while the operational Project runs daily nutrition, training, logs, and adjustments.

## Current Phase closure target

The active Phase closes only when the operational Project can demonstrate capability:

```text
minimal training state -> first-week training/activity plan -> log inside Operational Project -> adjustment
```

This validation is capability validation, not daily tracking inside workflow.

## Superseded / deferred items

* Reopening Project `Питание` setup repair remains superseded unless new failure evidence appears.
* Old MacroFactor Goal / Execution Pack / Wave 1 are historical evidence only.
* MacroFactor-centered workflow remains superseded/paused.
* Full food tracker/API automation/perfect recipe database/huge vault remain deferred.
* Mealie recipe sync / meal planner sync is optional/nonblocking unless explicitly reshaped later.
* Heavy tracking remains parked; do not revive MacroFactor/heavy tracking as default.
* Standalone metrics/readiness/documentation packet work is superseded unless it is inside the result-facing operator/correction loop.
* Nutrition polish is deferred until concrete real-use friction appears.
* Metrics / weekly review are parked until training v0 or a concrete need makes them the next bottleneck.
* Supplements / fasting / brain-diet are parked and require evidence/safety-aware research if later activated.
* Menu generation alone is not enough.
* Workflow daily logging is rejected; daily operation belongs in the Operational Project.

## Direction Map note

`08_DIRECTION_MAP.md` is intentionally unchanged by this P0 patch. Active Front remains `n2_first_weekly_body_transformation_correction_loop`. Any old route-binding text in the map is stale-but-nonblocking for `G1_GOAL_SHAPE` only.
