# Converge — g-health-core (assembly surface, removable; not a state file)

> Produced by converge play, session s-health-core-converge-001 (2026-06-14). Heavy node.
> This is the grep-able WHAT-spec assembly surface (per os/docs/converge-design.md). Authority is
> the owner §SIGNOFFs below + the RESULT in history/, never this file alone. Input evidence
> (refuted/confirmed, imported as authority ONLY where an owner approval is cited):
> work/health-core-recon-research.md, work/converge-nutrition-define-draft.md,
> work/converge-health-nutrition-input.md (all UN-RATIFIED except where a knowledge//history id is named).

## §TRIAGE
triage: **heavy** — converge **ON** — because **model-bearing** (a wrong stub is fatal: the data model
defines the whole product; v1's flat model structurally forced asking the owner an expert question)
**AND cross-cutting** (the core OWNS shared concepts — metrics/phase/PLAN-vs-LOG/review — that both
modules consume) **AND decomposes into subsystems** (profile/anchors engine · PLAN-vs-LOG store ·
definition library · parse-vs-values firewall · review/cadence engine · self-extensibility seam ·
module-contract). All three heavy triggers fire.

## §IMPORTS (born-closed — owner-approved decisions; each enters Resolve §WHAT as `status: answered, source: <id>`)
A row that NAMES a mechanism still runs Resolve 3(c) decomposition (closing a mechanism never closes its parameters).

| # | source (id) | decision (born-closed) | owner evidence | maps to done_when | names mechanism? → params still open |
|---|---|---|---|---|---|
| I1 | history/2026-06-14-s-health-reframe-charter-001.md | Provider-independence is a hard constraint: the system IS plain Markdown/YAML in git, managed by any LLM (ChatGPT/Claude/Claude Code/Codex) via AGENTS.md + CLAUDE.md(→AGENTS.md) + portable system-prompt; numbers-in-files, arithmetic-in-documented-formulas; no proprietary memory/runtime. | owner "ok" (R1–R13, F1–F4) | #2 (acceptance property) | NO |
| I2 | history/2026-06-14-s-health-reframe-charter-001.md | Owner interface = text/voice/photo; system parses into an input draft, asks a clarifying question only on material ambiguity. | owner "ok" (R6) | #6 (acceptance property) | YES → material-ambiguity threshold, question-order, confidence/source schema, recorded-with-flag-vs-ask (recon Q6) |
| I3 | history/2026-06-14-s-health-reframe-charter-001.md | Self-extensibility (R13): new global procedures/functions via a defined workflow; new domain = 1 folder + 1 registry line; no core rewrite. | owner "ok" (R13) | #7, #8 | YES → procedure-definition contract; registry-line / x_&lt;domain&gt;_* schema (recon Q10) |
| I4 | history/2026-06-14-s-health-reframe-charter-001.md | Core-first: strong core first; nutrition/training are MODULES on it sharing metrics/phases with cross-module exchange. | owner "ok" | #5, #8 | YES → which concepts core OWNS; day-level coupling depth (recon Q4) |
| I5 | history/2026-06-14-s-health-reframe-map-001.md | Tree re-mapped core-first: g-health-core converges first; g-health-ai-core dropped (artifacts = dirty input); modules reframed. | owner "ok" | node scope/framing | NO |
| I6 | history/2026-06-14-s-health-reframe-map-001.md | PLAN-vs-LOG + anchors-vs-derived is the core SPINE (LLM resolves daily numbers from stored anchors; modeled on plain-text-accounting + flat-text program DSLs). | owner "ok" (part of approved re-map) | #4, #5 ("PLAN-vs-LOG"), #3 (typed boundary) | YES → which fields are owner_facts vs derived_anchors; protein ref weight (Q1); resolve-on-read vs materialize (Q3); EMA/expenditure windows |
| I7 | knowledge/health-ai-core-guidance-governance.md | Direction OS ↔ Health AI System boundary: Direction OS strategic-only; raw daily data lives in Health AI System. Core is a guidance/governance layer, not only a data store. | promoted to knowledge after owner review 2026-06-13 (review-001 approval, owner chose A) | #9 (boundary preserved) | **RECONCILED by G7-3 SIGNOFF (2026-06-14):** the boundary is a ONE-WAY visibility/dependency — Direction OS reads ALL of Health AI on demand; Health AI carries ZERO reference to Direction OS. "Receives only summary/…" is refined to: Direction OS does not DUPLICATE raw data into its own state (not-a-diary), but is NOT limited to a filtered feed for reading. → cross-repo contract for converge-arch |

**NOT imported** (held OPEN — owner approval is absent or explicitly deferred): the **research-and-decide
operational membership test** (flagged in the nutrition Define draft as a CANDIDATE that must hit an owner
G7 gate; charter ratified the *intent* at high altitude but NOT the operational decide-vs-ask test) → **owner Gate G7-1**;
the 16-term nutrition glossary (un-ratified); all recon transferable patterns (input evidence, decides nothing);
the autonomy level (recon Q2) → **owner Gate G7-2**; biofeedback objective-recovery scope, carb-coupling depth,
template-vs-materialize — all system-decided/deferred (see §SYSTEM-LOCKED / §DEFERRED).

---

## §GLOSSARY (disputed terms — read 2+ ways on disk; Define-phase product)
Sign rule (G7): only the **owner-gate** terms below need the owner's signature (the readings ARE the options).
**System-locked** readings are picked by the system under research-and-decide (rationale+evidence cited) and
will be independently refuted by **converge-verify**, not signed by the owner. **Charter-locked** = already
owner-ratified at charter altitude. TERM-PROPERTY RULE applies: a Resolve §WHAT line may cite a term only for
a property in its row.

### A. OWNER-GATE terms — require owner §SIGNOFF (batched into 3 G7 decisions; readings = options)

**G7-1 — research-and-decide membership test + the askable personal-facts set.**
Covers terms: `research-and-decide (principle + membership test)` · `owner facts / факты владельца` ·
`anchor / якорный профиль (owner_facts half)` · `setup-intake (non-blocking, skip≠block)` ·
`tracking (owner logging-capacity/cadence ASK sliver)` · `biofeedback (owner-supplied 0–5 input sliver)`.
- meaning-here (recommended reading **A**): membership test = *researchable-from-evidence+profile ⇒ DECIDE,
  else ⇒ ASK*; carve-out **only by named constraint, never by owner wish**; askable set = irreducible
  un-researchable facts (dislikes/allergies/meds/hard-schedule/budget/real cooking-time/equipment) +
  sustainable logging cadence; goal-weight & protect-strength = system-PROPOSED targets the owner ratifies;
  sex/height = objective owner-supplied; cooking-time = a personal fact. Setup intake is thin, typed,
  **non-blocking** (skip ≠ block; system fills with decided defaults).
- load-bearing properties: defines the entire ASK surface (criterion 3 "спрашивает только … личные факты");
  the structural fix for the v1 defect (flat model forced asking meals/day).
- competing readings (the options): **B** wide askable set (goal-weight/protect-strength + owner wishes about
  expert vars are asked) = re-opens v1 over-asking; **C** narrow set but cooking-time treated as a
  system-decided cadence (tighter decide bias).
- citations: nutrition-draft L19–24 (membership test, "meals/day ⇒ DECIDE (the v1 bug)"), L85–88 (open edge:
  cooking-time = fact or cadence?); nutrition-input L102 (the crux); recon L10 (owner FACTS list) vs
  nutrition-draft L27–28 (narrow personal-fact); CHARTER L125–128 (principle, no test).
- ✅ **SIGNED (2026-06-14): reading A.** Owner: "Совсем остальным согласен." Membership test locked
  (researchable-from-evidence+profile ⇒ DECIDE, else ⇒ ASK; carve-out only by named constraint;
  goal-weight/protect-strength system-proposed/owner-ratified; cooking-time = personal fact;
  non-blocking skippable setup). NOTE: the non-blocking property is reinforced system-wide by G7-2 (R3/R4).

**G7-2 — the autonomy dial ("leader with brakes"): how much the system may auto-apply vs must surface.**
Covers terms: `autonomy / leads (leader with brakes)` · `system-decided variable (auto-apply envelope)` ·
`review-mechanism (autonomy level, recon Q2)` · `parse (per-entry ratify gate?)` ·
`clarifying-question (interruption posture)` · `owner-never-edits (control-posture half: authority stays in GitHub)`.
- meaning-here (recommended reading **A**, with **C**'s dial available): **decide-and-inform** default — system
  drives, logs best-effort silently, parses-and-commits when confident, asks ONLY on a short enumerable
  **brakes-list** (safety items, below calorie floor, raising training/diet intensity, sharp course change);
  the weekly review **PROPOSES** target changes and waits for ratification only on braked items. Optional single
  config dial (Coached/Collaborative/Manual) defaulting to this.
- load-bearing properties: the exact dial between v1-over-asking and forbidden-silent-autopilot; the guarded
  risk_posture bars silent autopilot, the trigger complaint ("behaved as asker, not leader") bars propose-and-wait.
- competing readings (the options): **B** propose-and-wait (collaborative) = essentially the v1 over-asker;
  **C** configurable autonomy level the owner flips over time (default = A).
- citations: nutrition-draft L36–37 (leader-with-brakes), recon L45–48 (three autonomy levels, "config flag for
  how much the review may auto-apply"), recon Q2 (open, owner-signed); CHARTER L199–204 (guarded).
- ✅ **SIGNED (2026-06-14): A's drive/auto-apply posture KEPT; the ASKING posture AMENDED by the owner.**
  Owner verbatim: "система может спрашивать и много … если я вот явно скажу что на это я не буду отвечать,
  то она не должна мне дальше морозить … должна как дефолт или примерный взять. Но я буду стараться всегда
  отвечать … может что-то не записал или не помню … с этим надо голову не дурить." Locked:
  (R2) the system MAY ask freely — minimize-asking is NOT the target;
  (R3) every question is NON-BLOCKING — on decline / "no data" / "don't remember" / "won't answer" → take a
  sensible default or estimate, record it as a revisable default, proceed, and do NOT re-pester that point;
  (R4) best-effort answering is the norm but missing/forgotten/unrecorded data is never made a problem.
  KEPT (accepted under "остальным согласен"): decide-and-inform on expert variables; review PROPOSES changes;
  the safety brakes-list still applies, with the conservative/safe option as the default-on-non-answer.
  ⇒ "non-blocking + graceful-default-on-decline + no-re-pester" is a SYSTEM-WIDE invariant for Resolve
  (attach to parse, clarify-question, setup-intake, review terms).

**G7-3 — the Direction OS boundary: the closed upward set + the raw-vs-summary "never a diary" line.**
Covers term: `Direction OS boundary — summary/decision/problem/CALL vs raw daily data` (done_when #9; reconciles I7).
- meaning-here (recommended reading **B** membership + **A** strictness): adopt the governance-doc **6-item**
  upward set {summary, problem, decision, incident, evidence-pointer, CALL} (incident + evidence-pointer are
  genuinely strategic and already owner-accepted 2026-06-13), with **strict** raw-data rule (raw daily data —
  weight, food, sets, photos, check-ins — NEVER crosses) and **milestone-gated** significance for "summary".
- load-bearing properties: defines HIS diary line; the firewall the whole product (sc-health-ai-system) is justified by.
- competing readings (the options): **A** strict 4-item set {summary, decision, problem, CALL}; **C** 4-item +
  periodic (weekly) summaries allowed up.
- citations: TREE done_when #9 (4-item) vs knowledge/health-ai-core-guidance-governance.md L5 (6-item);
  CHARTER L104–105, L284 ("стратегический review … только по значимым рубежам").
- ✅ **SIGNED (2026-06-14): REFRAMED by the owner** — supersedes the A/B/C upward-filter framing.
  Owner verbatim: "Direction OS … мы разрабатываем этот проект health AI … тут то что нужно для разработки,
  для новых фич … Direction OS знает про health AI, оно может зайти и почитать все данные. Health AI не знает
  про direction OS." Locked (R5): the relationship is a **ONE-WAY visibility/dependency** — Direction OS is
  the development + strategic layer for Health AI and may read **ALL** of Health AI's data on demand (for
  development, features, strategy, review). Health AI does **NOT** know about / depend on / write up to
  Direction OS — it is a self-contained operational system. Raw daily data is **owned and stored in Health
  AI**; Direction OS does not duplicate it into its own state (stays not-a-diary) but reads it directly when
  needed; its own persistent state stays strategic. ⇒ The "raw-vs-summary" line is an OWNERSHIP/STORAGE rule,
  not a flow filter; the binding fact is the visibility DIRECTION. Cross-repo CONTRACT for converge-arch:
  the Health AI product repo carries ZERO reference to / dependency on Direction OS; Direction OS gets full
  read access to Health AI.

### B. SYSTEM-LOCKED terms (system picks reading + evidence; converge-verify refutes; owner NOT asked)
FIREWALL applied: every concrete magnitude/format is `→ PLAN`, never frozen into the WHAT glossary.

| term | locked reading (one line) | `→ PLAN` magnitudes | evidence |
|---|---|---|---|
| anchor / якорный профиль (shape + derived layer) | ONE profile.md, two typed sections: owner_facts (asked once) + derived_anchors (system-decided, updated only at review w/ rationale); "anchor" = the system-derived compute roots; daily numbers = formulas off anchors; revision = edit a few anchors | which formulas/constants | recon L9–14, L123, L138 |
| system-decided variable (membership + values) | every expert var (meals/day, timing, macros, deficit band, tracking precision, food selection) decided autonomously + recorded rationale+evidence; meals/day OUT of the gate (v1 fix) | — | nutrition-draft L19–23, L32–34 |
| provider-independence | best-effort portability with NAMED residual risks (NOT assumed-away byte-determinism): determinism via numbers-in-files + documented formulas + constrained vocab; leak points named (AGENTS.md auto-load differs → convention in file CONTENT; YAML coercion; single-writer) | word budgets (~150–300) | recon L148–157, Q8 |
| conventions (AGENTS.md+CLAUDE.md+system-prompt) | three deliberately-redundant carriers because auto-load differs per tool; convention lives in file CONTENT | file budgets | recon L59–63, L154–156 |
| numbers-in-files / arithmetic-in-formulas | PRINCIPLE locked (store raw, compute via documented public formulas → reproducible) | 7-day EMA, 4/4/9, ~7700 kcal/kg, e1RM formula, %-of-anchor | recon L150–153 |
| no runtime/DB/server | flat files in git + LLM-as-runtime per-chat AND forward exclusion: NO auto-firing scheduler/cron anywhere; recurring = triggered | — | TREE L69; nutrition-draft L52–54; nutrition-input L98 |
| owner-never-edits — derivation invariant & path | edit-the-anchor-not-the-output INVARIANT is locked; write-back PATH (chat→GitHub) is `→ PLAN` (control-posture half → Gate G7-2) | connector/vendor specifics | nutrition-draft L38–41; nutrition-input L85 |
| program (программа) | anchored relative-prescription: multi-phase arc (start_date, ~2–3 phases ~12wk, checkpoints, progression rule) parameterized by stored anchors; days = formulas off anchors (старт trigger = owner, → Gate G7-1) | phase counts/weeks | nutrition-input L28; recon L81–85 |
| plan / PLAN (план) | the PLANNED file family (dateless intent, edited rarely, never overwritten to record a result); menu-cycle sits below the arc | file layout | recon L18–21; nutrition-input L27 |
| menu / menu-cycle | system-generated not owner-picked; program/plan/menu hierarchy + re-solve rule → converge-arch; mostly a nutrition-module noun | — | nutrition-input L29; nutrition-draft L82 |
| phase / фаза (lever + ownership + enum) | CORE-OWNED first-class shared lever (enum: cut/maintain/build/deload/diet-break) carried as plan metadata, gating both modules; modules consume, don't duplicate | phase names, cadence magnitudes | recon L86–88, L126; TREE #5 |
| якоря / derived (производные от якорей) | "anchor" = system-derived compute inputs; daily prescriptions = formulas off them; "захардкоженные" = rejected antonym (owner_facts half + protein-ref ratify → Gate G7-1) | — | recon L9–14; TREE #4; Q1 |
| metrics ownership (core vs module) | core defines each shared metric ONCE; modules consume; "owns" = owns the FORMULA — raw stored, trend/adherence derived-on-read | — | recon L124–125, L20–21, L146 |
| trend (вес+тренд) | derived smoothed series on read from stored RAW weights; never act on a single reading; the ONE metric both modules consume | EMA window/constant | recon L22–25 |
| biofeedback / самочувствие (scope) | subjective 0–5 daily self-reports (sleep/energy/hunger/stress); objective HRV/RHR/readiness EXCLUDED/deferred (marketing-dominated evidence) — see §DEFERRED | scale items | recon L54–58, Q7 |
| adherence | derived-at-read, never-stored consistency-of-logging (days logged, ≥2 eating-occasions, % targets met; logging-consistency primary R²=0.27); fallback/"lazy food" COUNTS, not a slip | — | recon L49–52; nutrition-draft L45–46 |
| PLAN vs LOG | TWO file families: dateless plan/template (intent, rarely edited) + dated append-only LOG (actuals, one file/day, processed by date field); reconciliation/adherence derived across, not stored | YYYY-MM-DD naming, frontmatter shape | recon L18–21, L36–39 |
| review-mechanism (math/triggers/guardrails) | no-mutation reconciliation that PROPOSES (rationale+rule) w/ declarative guardrail/floor pre-checks; two triggers (owner-PULL complaint → must mutate via proposal; system-PUSH date/signal → must emit a decision); firewalled from the strategic Direction OS review (autonomy level → Gate G7-2) | — | recon L140–144; nutrition-draft L47–51 |
| cadence (каденцию задаёт программа) | per-program/per-phase (not global) AND deviation-gated (lever moves only when slope deviates beyond noise AND persists N weeks), minimum-data floor (never re-issue faster than ~2–3wk) | weekly default, N, 2–3wk, 0.5–1%/wk, decision_interval_days | recon L160–166, Q9 |
| daily prescription (resolve-on-read) | "не захардкоженные" SETTLED; resolve-on-read vs materialized-day-files is an OPEN arch fork → converge-arch (recon recommends resolve-on-read) | file layout | recon Q3, L84 |
| tracking — analytic precision | minimum-sufficient detail / what-must-be-logged is a system evidence call (open evidence gap → round-2 research); low-friction photo/voice/text capture (owner logging capacity/cadence → Gate G7-1) | grams vs portion-tiers vs hand-units | nutrition-input L30, #30; nutrition-draft L62–65; recon Q6 |
| parse / структурированная запись (firewall) | LLM parses NL/voice/photo into a PROPOSED draft, maps each item to a definition-library entry; NUMBERS looked up from files, never generated ("не выдумываются") (per-entry ratify gate → Gate G7-2) | confidence cutoff | recon L70–74, L128; TREE #6 |
| clarifying-question — materiality test | what counts as MATERIAL ambiguity (divergence AND changes the day's accounting) is a system call; default log-silently; portion-question FIRST; aim ~0/log (interruption posture → Gate G7-2) | confidence cutoff, wording | recon L76–79; nutrition-input #34; Q6 |
| active program / library / сверка | a closed reference vocabulary the parser matches against: definition LIBRARY (foods/, exercises/) by stable slug + per-100g fields (USDA-FDC provenance); "сверка" = per-item map (write) + plan-vs-actual reconcile (review) | FDC field schema, slug format | recon L31–35, L103–104, Q5 |
| self-extensibility / global procedure | structural SEAM: a typed procedure-definition CONTRACT registering a new global procedure without core rewrite; jobs TRIGGERED (owner/chat/checkpoint), bounded by scope+stop; NOT a self-firing cron | registry format, spec template | nutrition-draft L52–54; nutrition-input L98; recon Q10 |
| new domain vs global procedure | TWO distinct extension classes: domain = 1 folder + 1 namespaced registry line (no core edit); global procedure = a richer authored-via-workflow contract | folder layout, registry-line syntax | TREE #7; recon L33–35 |
| module / attach-without-rewrite | thin additive attachment that CONSUMES core concepts (metric trio, phase, PLAN-vs-LOG, review) + adds only namespaced fields; never edits core; must not duplicate; nutrition-as-reusable-template = arch recursion → converge-arch | attach-point format | TREE #8; recon L124–126; nutrition-input L106 |
| contract / shared-data exchange | low-frequency shared state (phase + metric trio) + the one high-frequency link (coarse day_type → carb target); keep cross-links COARSE; day_type provenance (PLANNED vs LOGGED) → converge-arch | carb-tier magnitudes | recon L86–92, Q4 |

### C. CHARTER-LOCKED term (already owner-ratified at charter altitude — recorded for completeness, no new gate)
| term | locked reading | basis |
|---|---|---|
| safety / medical floor | a **non-overridable HARD layer ABOVE research-and-decide** (the decide-pass cannot relax it): calorie/protein floors + red-flag list as inviolable gates, not soft guardrails. Floor MAGNITUDES are system/evidence `→ PLAN`; the floor's *existence-and-status* is owner-ratified. | CHARTER L90–93 "Медицинская безопасность выше темпа … hard constraint"; medical-safety lens L158–161; guarded posture L199–204. Surfaced by the completeness critic (missed by miners). |

### D. UNDISPUTED-but-glossary-grade sub-clauses (named explicitly so they aren't silently absorbed; not signed)
- criterion **1a** — state in human-readable Markdown/YAML in git as the store: undisputed storage-format requirement (the prose-body-vs-YAML-frontmatter layering is a `→ PLAN` file-shape detail, not a glossary dispute).
- criterion **2d** — value-type grammar + constrained vocabulary (fixed/range/relative/amrap + unit enum + slug convention): single-reading but core-grade; the recon calls a constrained documented vocabulary *mandatory* for determinism. Named here; mechanized at Resolve/PLAN, not owner-signed.

---

## §SIGNOFF — Define
**§SIGNOFF: owner approved Define @ 2026-06-14** (session s-health-core-converge-002). The three batched
G7 readings are locked (✅ SIGNED in §GLOSSARY A above):
- **G7-1 = A** — owner: "Совсем остальным согласен."
- **G7-2 = A drive/auto-apply + AMENDED asking posture** (R2 ask-freely / R3 non-blocking + graceful-default /
  R4 forgiving) — owner: "система может спрашивать и много … если я вот явно скажу что на это я не буду
  отвечать, то она не должна мне дальше морозить … должна как дефолт или примерный взять … может что-то не
  записал или не помню … с этим надо голову не дурить."
- **G7-3 = REFRAMED, one-way visibility** (R5) — owner: "Direction OS знает про health AI, оно может зайти и
  почитать все данные. Health AI не знает про direction OS."

Owner gates spent: **1 of ≤3** across the converge set (Define). Resolve + converge-arch share the rest.
Resolve (§WHAT) may now start (next session, c-health-core-converge-003).

## §WHAT — Resolve
status: **NOT STARTED** — built in the next session (converge Resolve) once §SIGNOFF Define exists, because every
§WHAT line cites a §GLOSSARY reading and the owner-gate readings are not yet locked. Resolve will derive rows from
THREE sources: (a) each done_when criterion; (b) each cross-node edge; (c) mechanism decomposition of every
born-closed import that NAMES a mechanism (I2, I3, I4, I6, I7) + each committed mechanism, recursively.

END_OF_FILE: live/health/work/converge-g-health-core.md
