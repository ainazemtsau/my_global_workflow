# Wave-0 de-risk — g-health-core KERNEL (t-1 zero-engine subtraction + t-2 cold-start bootstrap; gate that releases the executor build)

> Produced by the work play, session s-health-core-kernel-wave0-derisk-001 (2026-06-20), CALL c-health-core-kernel-wave0-derisk-001.
> PAPER ONLY — no build, no journey-walk, no owner gate, no edits to health-ai content. Authority is the §GATE VERDICT + the RESULT in history/, never this file alone.
> Fresh-read confirmation (mirroring G5 separate-context discipline): workflow wf_c9806260-68d — 2 independent provers + 6 adversarial refuters (3 lenses per task), each refuter in its own fresh context trying to PRODUCE the FAIL condition. Full 50-row clause ledger + every refuter's reasoning live in that workflow transcript; this file is the readable, self-contained verdict.

## §GATE VERDICT
- **t-1 (zero-engine subtraction): PASS.** 3/3 refuters → PASS_HOLDS; no surviving engine residual.
- **t-2 (cold-start bootstrap-recursion): PASS on the binding KQ2/O1 core** (non-circular · no hand-seeded cursor · genuine WA-K10 journey-proof); prover + 2/3 refuters confirm. ONE surviving governance-coverage seam (registry-line write authority) — does NOT meet the t-2 FAIL bar and does NOT block the spine+journey → routed to PLAN + a non-blocking owner decision.
- **Decision: RELEASE the gated executor CALL** c-health-core-kernel-spine-journey-executor-001, with three named residuals folded into its context/PLAN (additive; none re-opens or weakens a signed row).

---

## §t-1 — ZERO-ENGINE SUBTRACTION-PROOF
**Method.** Subtract the two hardest nutrition files clause-by-clause against the kernel contracts the kernel already supplies; after subtraction the remainder must be ONLY the WA-K9 thin shape.

**Subtraction target (as scoped by the CALL):** `x_nutrition/procedures/workflow-router.md` + `x_nutrition/workflow/graph.md` (the per-domain router procedure + the per-domain state-machine DEFINITION). Refuter coverage widened the target to the per-domain executable validator surface (see Finding F-A).

**Residual ledger (verdict: PASS, residual clean, named_engine_residual = null).** Every engine clause of both files maps to an inherited kernel contract:
- Router identity / "first operator procedure" / resolve-one-state-before-output / one-chat-one-task → **KC1** (single router, sole entry, KR1/KR6) + **KC10** (one-job, KR9). The nutrition `workflow-router.md` FILE itself collapses into the inherited core router (**KR10**: router+cursor+machine are CORE-owned primitives every domain inherits; "it authors no dispatcher").
- Startup load-order + cursor-vs-graph validation + drift-blocks-to-repair → **KC1** boot (KR3) + **KC3** (KR4). 
- Preflight reject-set (the whole reject list) → **KC1** (KR5 fail-closed) + **KD2** (ask-guard: expert variables preflight-rejected) + **KC9** (render-before-decision) + **KC7** (claim-transition-without-evidence) + **KC12/KL7** (no core rewrite, no cross-domain). The reject *mechanism* is kernel; the nutrition reject *lists* (family enum, expert-variable list, forbidden-infra tokens) are DATA.
- Router output contract + blocked-output shape → **KC10** (KR9 one-state/one-task output, verbatim).
- State-resolution rules + the 7 states + transition path + MUTATION return-by-scope + required-state-fields + per-state bodies → **KC3** (the DEFINITION is a DOMAIN-owned DATA instance riding the gate, KS10/WA-K12; a state authorizes exactly one family, KS3; allowed_next_states, KS5) + **KC4** (each authorized output binds one procedure per term).
- DAY_LOOP 4-stage parse-to-LOG → **KC1/KR7** (the signed W44 parse is ONE dispatched procedure inheriting the router preflight — NOT a second dispatcher) → residual = procedure-per-term, and the parse/library/value-grammar surface is CORE-owned (**CA6/CA7/KL7**).
- PROGRAM pipeline (intake→research→evidence→synthesis), REVIEW decision-class, normalizer reject+mapping → each a single registered procedure per term (**KC4/KG1/KL2**); the reject mechanism is **KC7/KV10/KV12**; the step *sequence* is the domain machine ordering (**KC3**).
- WGA15 writer-handoff validation → **KC7** (writer-applier barrier in a separate context) + **KC4/KG2/KG4** (registry binds writable-family/status; authoring writes only PROPOSED); required-fields = handoff-packet schema (KP9). No domain writer is stood up.

**Post-subtraction remainder = exactly the WA-K9 thin shape:**
1. `{x_nutrition namespaced DATA}` — family enum, expert-variable list, per-state AI-decided variable names, review-trigger values, forbidden-token lists, decision-class enum, the input/load file-name set.
2. `{ONE state-machine INSTANCE}` — the 7-state definition with per-state family/allowed_next/guards/return-edges, authored as an owner-approved ACTIVE DATA artifact in the inherited kernel grammar.
3. `{ONE bounded PROCEDURE per term}` — PROGRAM pipeline steps, the DAY_LOOP parse-to-LOG, the CYCLE/WEEK_PLAN/REVIEW/MUTATION authoring procedures; each a single registered procedure with no dispatcher of its own.
4. `{ONE registry LINE (+ one folder)}` — already present as the single `x_nutrition` entry in `core/extensions/registry.md`.

**Conclusion.** The nutrition router collapses to data + thin-procedure under the kernel; no irreducible domain engine survives (no second dispatcher, no domain lifecycle, no domain cursor-advance mechanism, no domain logic needing code). **WA-K9 is meetable.**

### t-1 non-fatal findings (executor-inputs; they STRENGTHEN firewall-by-subtraction, not threaten WA-K9)
- **F-A (coverage — widen the subtraction target).** The prover walked only the two named files; the per-domain executable validators (`tools/check_nutrition_*.py` ≈ 51KB+14KB+14KB+8KB, `acceptance/x_nutrition/*` fixtures, `x_nutrition/contracts/normalizer-contract.md`) are real nutrition-namespaced engine that re-encodes workflow/governance invariants. They are PRE-kernel scaffolding whose every check decomposes 1:1 onto KL7+W1+KC7+KC3/KC9/KC10/KD2/KR5 → on the lift they must be DELETED and replaced by the ONE kernel-universal LLM-evaluated KC7 barrier (the "write-barrier de-couple from the 4 `check_nutrition_*.py` calls" already named in t-3 scope). Confirms the firewall-by-subtraction is real and larger than two files.
- **F-B (conformance — attach-time migration).** The live cursor `x_nutrition/state/current-workflow.md` (L45-64) embeds KM1/KC2-forbidden content (`current_weight_kg: 125` + a clinical `post_smoking_cessation_context` block). This is a conformance defect in the live instance (it actually proves KM1 has teeth), not an un-absorbed engine. The lift must migrate this out of the cursor (cursor = pure non-content control pointer). Flagged out-of-band by the refuter (task_bccaef53).

---

## §t-2 — COLD-START BOOTSTRAP-RECURSION REALIZABILITY
**The recursion (KQ2).** A new domain has no cursor/machine, yet WA-K12/KS10/B-3 require its state-machine DEFINITION to be an owner-approved ACTIVE artifact authored THROUGH the gate — chicken-and-egg.

**Bootstrap sequence traced cold (verdict: PASS — is_circular=false, needs_hand_seeded_cursor=false, is_genuine_journey_proof=true):**
- **S0/S1 cold state + detection:** new domain has NO cursor file. The kernel engine (KC1/KC2/KC3/KC4/KC5/KC6/KC7/KC14) is already built ACTIVE as TIER-2. Cold-start = **file-absence** of the domain cursor (KD3/KP1) — absence is observable, needs nothing seeded.
- **S2 fixed point breaks the recursion:** with no domain definition, the router resolves the BOOTSTRAP state against the **KERNEL-OWNED universal bootstrap state-machine** (an engine part, ACTIVE-as-kernel, inherited per KL7/KC12/KR10), NOT the domain's not-yet-authored machine. First state resolved against KERNEL data.
- **S3–S7 walk through the gate:** the BOOTSTRAP state authorizes exactly one output — the kernel/global bootstrap procedure "author THIS domain's definition (+ initial cursor) as PROPOSED" (KS3/KG5, writable-status=PROPOSED only, KG4). The definition lands PROPOSED (KV3/KV2) → readable owner present (KC8/KV21) → owner echoed-words gate (KC6/KV4/KV5) → SEPARATE writer context persists ACTIVE with evidence-of-transition commit+diff+re-read refuted outside the producing chat (KC7/KV11/WA-K5). Two-write order (KD4/KP10): definition/status-flip FIRST, the **initial CURSOR is the SECOND write** — an OUTPUT of the transition, never hand-seeded.
- **S8:** domain now has an owner-approved ACTIVE definition (WA-K12) + a valid initial cursor. This first SEED→PROPOSED→ACTIVE walk with evidence **IS the binding WA-K10 journey-proof.**

**Why O1 is forced (not circular):** the kernel bootstrap machine is the hand-built-ONCE TIER-2 base case (not a domain artifact); the first DOMAIN definition is the OUTPUT that walks the per-domain gate. The four alternatives are correctly refuted (O2 ad-hoc=FM3 self-activation · O3 hardcode=KA2/KG6 core-edit · O4 ship-ACTIVE=single-hop KV2 · O5 empty-ACTIVE=FM5 placeholder).

**Caveat (does not change verdict):** the current repo's `graph.md`/`current-workflow.md` were hand-authored PRE-kernel (no gate, no journey, no evidence) — that is the pre-kernel defect that motivated the reframe, NOT evidence the O1 bootstrap is unrealizable. The nutrition engine even demonstrates the realizable mechanics (file-absence preflight, single router over a file-backed cursor, a fixed STARTUP entry state); what it lacks is exactly the gate/lifecycle the kernel adds.

### t-2 surviving governance seam (1 refuter, survives_rebuttal=true) — routed to PLAN + owner decision
- **F-C (registry-line write has no signed governance home).** For the router (KC1/KD1) to select BOOTSTRAP and the registry (KC4/KG7) to dispatch the bootstrap procedure FOR domain X, domain X's **registry line** (1 folder + 1 line) is the TRUE first write. It grants a whole domain *run-authority* (its deletion = remove-domain, KA2/KA6/WA-K8), yet KG6/KA2/KA8 frame it as a "purely additive" UNGATED mechanical append — no status leaf, no SEED→PROPOSED→ACTIVE, no owner_approved marker, no evidence-of-transition. KV1 ("every owner-facing artifact carries a mandatory status leaf; only ACTIVE is authority") would pull it under the gate by default; KM11 had to be EXPLICITLY minted to exempt the CURSOR, but there is NO parallel exemption for the registry line — which is MORE authority-bearing than the cursor. The live `core/extensions/registry.md` confirms the symptom (x_nutrition entry: `status: active` + `activation_source: owner_request`, but no lifecycle leaf / approval marker / evidence on the registry artifact itself).
- **Disposition: NOT a Wave-0 kill, NOT the t-2 FAIL condition.** It is not a hand-seeded cursor and not a circular walk — KQ2/O1 core holds. Blast radius is low: an ungated registry line cannot smuggle owner-facing CONTENT to ACTIVE (content still independently walks the gate); an attached-but-undefined domain just sits in BOOTSTRAP and can produce nothing ACTIVE without the owner. And it does **not block the spine+journey build**: nutrition is already attached, so t-3 walks its definition over an existing line — the seam only bites at the first COLD NEW-domain attach (the deferred second-domain leg).
- **Resolve at PLAN before the first cold new-domain attach** (one of: registry line gated as the attach-SEED of the bootstrap journey | KM11-style control-file exemption | decide-and-inform). Tied to owner decision **D-kernel-1**.

END_OF_FILE: live/health/work/wave0-derisk-g-health-core-kernel.md
