# NOW — health

active_bet:
  id: b-health-core-kernel
  node: g-health-core
  status: active
  appetite: 14 calendar days (kill 2026-07-04), servant-boxed — WIN = the BINDING PRECONDITION green (WA-K10 journey-proof + KC5/KC6/KC7/KC14), NOT the full breadth; honest sizing MULTI-LEG (see TREE g-health-core appetite/kill_by).
  chosen_approach: >
    EXTRACT-AND-CANONIZE (= BUILD for an LLM-as-runtime kernel: author conventions + per-term procedure files +
    registry, no server). ONE indivisible TIER-2 deliverable in health-ai over the born-closed TIER-1 data
    layer: (1) LIFT the proven hand-rolled nutrition trio (x_nutrition/procedures/workflow-router.md,
    workflow/graph.md, state/current-workflow.md) UP into CORE-owned files (KC1 router / KC2 cursor / KC3
    def+cursor split / KC4 typed registry); (2) BUILD the net-new governance spine the trio never had (KC5
    status → KC6 owner-approval gate → KC7 writer-applier barrier in a separate context with
    evidence-of-transition → KC8 confirm-by-layer → KC9 render-gate → KC10 one-job/handoff → KC11 provenance,
    + KC12 thin-attach + KC13 anti-fragility + KC14 cold-start bootstrap & the binding journey-proof); (3)
    CLOSE THE FIREWALL BY SUBTRACTION (no engine code under any x_*). Losing approaches → captures
    (greenfield discards ~40% proven behavior; pure-reuse has no governance spine to reuse).
  one_screen: >
    Build the ONE indivisible TIER-2 kernel (the governance/runtime engine every health domain inherits
    unchanged), walk ONE real artifact SEED→PROPOSED→ACTIVE through the gate with evidence-of-transition refuted
    in a fresh session. Validated the repo-native way (acceptance-matrix rows + check-scripts + negative-fixtures
    + fresh-session journey-evidence refutation), NOT the code-CONTOUR mutation/headless/Assets gates (no
    app/server — they don't map). Executor done_when copies §WA-K1–K12 VERBATIM (G5, frozen). Kernel = ONE node,
    MULTIPLE legs — never split across nodes.
  forecast: >
    The riskiest assumption (a thin domain needs ZERO engine code — WA-K9) is proven on paper in hours before
    any build; the binding precondition (journey-proof + KC5/6/7/14) is the cheapest place "the engine works"
    becomes observable; ~40% of the engine is a lift of already-proven nutrition code; the repo already has the
    right validation form (matrix + check-scripts). So the precondition can land inside the box.
  against: >
    For an LLM-as-runtime with no executable runtime, the §WA-K behavioral criteria are discharged by
    authored-convention + static-lint + ONE worked example, not a behavioral dispatch harness (accepted open
    flank). Honest sizing is multi-leg, not half a day — the weaker predecessor data layer took ~6 days; the box
    risks blowing on SCOPE and tripping the procrastination kill for the wrong reason — mitigated by de-risk-first
    + the named multi-leg appetite.
  deferred (named, not cut into this box):
    - WA-K6 render-before-decision on a real upstream/downstream pair → follow-on breadth leg.
    - WA-K8 anti-fragility STRONGEST form (a real second LIVE domain survives removal) → first training/second-domain attach. The write-barrier de-couple from the 4 check_nutrition_*.py calls + the one-domain removal + disable-one-procedure-leaves-siblings-green ARE in-scope (t-3).
    - F15/F16 hybrid cleanup of dirty x_nutrition seed artifacts → separate later CALL (move to provenance, not delete).
  riskiest_assumption: >
    A thin domain truly needs ZERO engine code — the live hand-rolled nutrition engine collapses to {data, 1
    procedure/term, 1 registry line, 1 state-machine instance} once the kernel exists (WA-K9/KC12/KL1). The
    kernel's whole reason to exist and the exact claim whose failure got the nutrition slice rejected; a LIVE
    counterexample sits on disk; the journey-proof does NOT test it. Tested FIRST, on paper, before any build
    (t-1). Second kill-bearer (KQ2 cold-start bootstrap recursion) also paper-checked first (t-2).
  cut_list:
    - cut: no SECOND domain re-homed (training stays parked). WA-K8 proven by the one-domain removal + write-barrier de-couple (IN-SCOPE t-3) + disable-one-procedure-siblings-green; the STRONGER "a live sibling survives" form → first second-domain attach. what_breaks → strongest anti-fragility form deferred. disposition → later-leg.
    - cut: no executable runtime conformance harness firing resolve-state→dispatch / simulating an LLM turn. Verification = static check-scripts + negative-fixtures + ONE walked journey. what_breaks → "router fires every turn for every provider" stays asserted-by-convention, never machine-proven — the bet's open flank. disposition → parked.
    - cut: no full PROJECT_SETUP §Re-sync to os/engineering v7. Run under the repo's existing writer-gate + acceptance-matrix discipline + a minimal validation.config stamp. what_breaks → code-stack machinery (mutation/openspec/RESULT.md/new md-yaml profile) not imported (doesn't map to a no-app md/yaml kernel); the v7 clauses that bite are kept (G0 frozen §WA-K verbatim, builder-cannot-weaken-oracle, binding fresh-session G5). disposition → parked.
    - cut: no hard-delete/cleanup of dirty x_nutrition seed artifacts; they move to provenance under the gate. what_breaks → workspace not tidied this box (cosmetic); F15/F16 is a signed later CALL. disposition → later-leg.
  lens_sweep:
    - weight-nutrition: not_needed — TIER-3 domain content (parked nutrition); engine hook already signed (KC3 domain state-machine instance, KC9 derived grocery must-not-lie, KG1 one-procedure-per-stage); adding it = KC12 violation + the pre_mortem procrastination trap.
    - strength-body-composition: not_needed — TIER-3 (parked training); training reuses the IDENTICAL kernel (KL8/KR10/KC12) + the one coarse seam CA5 day_type→carb; no strength content decidable at the engine layer.
    - activity-conditioning: not_needed — TIER-3 (parked); attaches as thin {data, 1 proc/term, 1 line, 1 instance} (KC12/WA-K9); activity logging = a decide-and-inform DAY_LOOP state (KC8); no kernel task.
    - adherence-relapse: not_needed — engine hooks first-class but ALL signed (KS6 non-blocking pause + W21/G7-2 no re-pester + KM2/KM7/KV16 loss-free handoff + KC14 cold resume); carried by WA-K7/WA-K11; thresholds are domain content.
    - medical-safety: not_needed (borderline) — KV19 AI-non-deletable owner safety facts + before/after preservation (carried by KC7/WA-K5) + KS6 safety-brake exception honor "медбезопасность выше темпа"/guarded; red-flag thresholds are domain content (KC12).
    - ai-system-data-architecture: TASK — this IS the bet; build the kernel (KC1–KC14 + 4 picks) with §WA-K1–K12 verbatim, KV21/WA-K2 owner pref (result + what will be saved, never raw YAML); realized by t-1/t-2/t-3.

tasks:
  - id: t-1
    kind: session
    status: done
    done_note: >
      DONE 2026-06-20 (s-health-core-kernel-wave0-derisk-001): zero-engine subtraction PASS — every engine clause
      of workflow-router.md + graph.md maps to an inherited kernel contract; post-subtraction residual = the WA-K9
      thin shape {namespaced data, one procedure per term, one registry line, one state-machine instance};
      named_engine_residual = null. Fresh-read confirmed (wf_c9806260-68d; 3/3 refuters PASS_HOLDS). Findings F-A
      (subtract per-domain validators) + F-B (cursor conformance) → executor-inputs. Evidence:
      work/wave0-derisk-g-health-core-kernel.md §t-1.
    goal: >
      Prove on paper that a thin domain needs ZERO engine code (WA-K9/KC12/KL1) BEFORE any build — the kernel's
      riskiest assumption and the exact claim whose failure got the nutrition slice rejected.
    done_when: >
      A written residual ledger over health-ai x_nutrition/procedures/workflow-router.md (preflight reject-set +
      STARTUP/PROGRAM/CYCLE/WEEK_PLAN/DAY_LOOP/REVIEW/MUTATION state rules + provider-continuation) and
      x_nutrition/workflow/graph.md: EITHER (PASS) every engine clause maps to an inherited KC1/KC3/KC4/KC8/KC9/
      KC10 contract and the residual is only {x_nutrition data, per-term procedures, 1 registry line, 1
      state-machine instance} — WA-K9 meetable — OR (FAIL, kills the bet) a named non-empty engine residual the
      kernel does not absorb, handed to the session lead. No build, no journey-walk, no owner gate.
  - id: t-2
    kind: session
    status: done
    done_note: >
      DONE 2026-06-20: KQ2/O1 cold-start bootstrap realizable cold — non-circular (fixed point = kernel-owned
      universal bootstrap machine, file-absence cold-start detection), no hand-seeded cursor (cursor = 2nd governed
      write), and the first SEED→PROPOSED→ACTIVE walk IS the WA-K10 journey-proof — PASS. Fresh-read confirmed (2/3
      refuters PASS_HOLDS; 1 surviving governance seam F-C registry-line authority → PLAN + non-blocking D-kernel-1,
      NOT a t-2 FAIL). Evidence: work/wave0-derisk-g-health-core-kernel.md §t-2.
    goal: >
      Paper-check the second kill-bearing assumption (KQ2 cold-start bootstrap recursion) cheaply, before the
      build — confirm the kernel-owned universal bootstrap machine can author a domain's first definition+cursor
      THROUGH the gate from a genuine no-cursor state with evidence-of-transition.
    done_when: >
      A written verdict over health-ai core/extensions/ + the existing router load-order: EITHER (PASS) the
      bootstrap sequence can author the first definition+cursor cold through the gate (no hand-seeded cursor) and
      that first walk IS the WA-K10 journey-proof — OR (FAIL/bounce) it can only be walked by hand-seeding,
      refuting KQ2/O1. Paired with t-1 as the Wave-0 de-risk; both PASS confirmed in a FRESH read (mirroring the
      G5 separate-context discipline) before t-3 dispatches. No build.
  - id: t-3
    kind: executor
    status: done_with_incident
    repo: health-ai (C:\projects\health-ai)
    done_note: >
      DONE-WITH-INCIDENT 2026-06-21 (s-health-core-kernel-incident-rca-001). The kernel spine (KC1-KC11 + KC14) was
      BUILT in health-ai and the binding journey-proof walked SEED-PROPOSED-ACTIVE with evidence (commit 679dd8d),
      refuted fresh = SURVIVES (9a0124a) -> the bet binding precondition is met. BUT the first nutrition RUN that
      followed exposed real defects: an agent surfaced+persisted a CONCRETE owner-facing menu as PROPOSED without
      owner approval (8504d3f), reverted by owner (adebf3d); stale concrete content left in-repo with active flags;
      owner-facts duplicated across projection files; the "abstract-first / never pick concrete foods" rule was
      never in any contract; no visible proof-of-load marker on chat replies; and the executor returned NO OS
      RESULT -> live/health drifted a full leg (re-synced by this RESULT). Nutrition PAUSED. Full RCA: workflow
      wf_39392526-26c (8 agents, adversarially verified) -- NOT a WA-K2/WA-K3 contradiction, but an
      under-specified freeze seam + a missing requirement. Remediation in NOW.next
      (CALL c-health-core-kernel-incident-fix-001). WA-K6 + WA-K8 remain red_deferred (unwalked).
    gated_on: t-1 PASS AND t-2 PASS (fresh-read confirmed)
    goal: >
      Build the ONE indivisible TIER-2 kernel spine (lift runtime primitives KC1–KC4 + build governance KC5–KC11
      + cold-start KC14) over the born-closed data layer, and walk ONE real artifact SEED→PROPOSED→ACTIVE through
      the gate in a SEPARATE writer context with evidence-of-transition — greening the BINDING PRECONDITION
      (WA-K10 journey-proof + KC5/KC6/KC7/KC14) and the runtime primitives, refuted by a fresh session. Honest
      sizing: multi-leg; the executor plan-gate splits (e.g. 3a lift KC1–KC4 / 3b governance spine + journey) so
      each leg ≤ a focused half-day. Within the spine the construction order is KC1–KC4 → KC5→KC11 → KC12/KC14,
      and the journey is walked only AFTER KC5/KC6/KC7 are green.
    done_when: >
      §WA-K1–K12 frozen VERBATIM (builder cannot edit; full text in open_calls →
      c-health-core-kernel-spine-journey-executor-001). Each WA-K → ≥1 acceptance-matrix row + check_ref;
      WA-K3/K4/K6/K8/K10 each get a RED negative-fixture the check trips on; each engine part resolves to a
      committed core/** path the check Test-Paths to a non-trivial artifact. THIS spine wave greens WA-K1, K2,
      K3, K4, K5, K7, K9 (nutrition re-attached thin), K10 (journey-proof, authored cold, commit+diff+re-read
      evidence), K11, K12; the follow-on breadth leg rides WA-K6 + WA-K8. Every WA-K row carries an explicit
      green / RED-deferred disposition (v7 deliverable-coverage). Closed by a fresh-session refutation.

open_calls:
  - id: c-health-core-kernel-shape-001
    status: done
    note: >
      DONE 2026-06-20 (s-health-core-kernel-shape-001): g-health-core SHAPED into b-health-core-kernel,
      owner-approved (G9, "Подтверждаю"). One indivisible TIER-2 engine bet, EXTRACT-AND-CANONIZE, 14-day
      servant-box (kill 2026-07-04, procrastination-tied), win = binding precondition green. 3 owner decisions
      signed (appetite / no-full-Re-sync / nutrition-first). Riskiest assumption (zero-engine WA-K9) + KQ2
      bootstrap paper-proven FIRST (t-1, t-2) gating the executor spine+journey build (t-3, §WA-K1–K12 verbatim).
      Parallel analysis + adversarial verify (wf_56681b10-608) caught 3 blocking draft defects, corrected.
  - id: c-health-core-kernel-wave0-derisk-001
    status: done
    note: >
      DONE 2026-06-20 (s-health-core-kernel-wave0-derisk-001): t-1 zero-engine subtraction PASS + t-2 KQ2/O1
      cold-start bootstrap PASS, fresh-read confirmed (wf_c9806260-68d, 2 provers + 6 adversarial refuters).
      Executor CALL released. 3 residuals folded: F-A subtract per-domain validators + F-B cursor conformance
      (executor-inputs) · F-C registry-line governance → non-blocking decision D-kernel-1. Evidence:
      work/wave0-derisk-g-health-core-kernel.md.
  - id: c-health-core-kernel-spine-journey-executor-001
    status: done
    closed: 2026-06-21 (done_with_incident -- see NOW.next + LOG + history/2026-06-21-s-health-core-kernel-incident-rca-001.md)
    incident_note: >
      Kernel spine + journey-proof built and proven in health-ai (679dd8d journey ACTIVE; 9a0124a fresh-refutation
      SURVIVES). BUT the executor returned its results ONLY inside the health-ai repo (in-product handoff packets),
      never as an OS RESULT with state_changes -- the workflow-loop gap that left live/health drifted a full leg.
      The follow-on nutrition run overran the thin-attach boundary and hit the menu-without-approval violation
      (8504d3f, owner-reverted adebf3d). Closed; remediation tracked in NOW.next (c-health-core-kernel-incident-fix-001).
    gate_cleared: 2026-06-20 (Wave-0 PASS, fresh-read confirmed)
    note: >
      RELEASED 2026-06-20 — Wave-0 de-risk PASS/PASS cleared the gate. Executor CALL into health-ai for the kernel
      spine + journey-proof; done_when copies §WA-K1–K12 VERBATIM (frozen, Unicode preserved), carried live in
      NOW.next. Context amended with Wave-0 residuals F-A (subtract per-domain validators) + F-B (cursor
      conformance) as executor-inputs and F-C (registry-line governance) as a PLAN-residual tied to D-kernel-1 —
      all input/PLAN only; done_when untouched.

recurring: []

decisions:
  - id: D-kernel-1
    blocking: false
    tag: deferred second-domain-attach leg (does NOT block the spine+journey build; nutrition already attached)
    raised: 2026-06-20 (s-health-core-kernel-wave0-derisk-001, Wave-0 t-2 refutation F-C)
    question: >
      How is the registry-line write governed? Domain attach = 1 folder + 1 registry line, which grants the domain
      run-authority (its deletion = remove-domain). It has no signed governance home: KG6/KA2 treat it as an
      ungated mechanical append, while KV1 (every owner-facing artifact carries a status leaf; only ACTIVE is
      authority) would gate it by default; no KM11-style exemption exists for it.
    options:
      - "1 (recommended): registry line = infrastructure control-file (like the cursor) → NOT under the content gate; mechanical append, decide-and-inform. bad_because: AI could attach an empty domain without owner approval — but an empty domain with no ACTIVE definition produces nothing ACTIVE (content still walks the gate); blast radius low."
      - "2: registry line rides the bootstrap journey as the attach-SEED → {registry line + definition + cursor} walk SEED→PROPOSED→ACTIVE together (gated). bad_because: slightly heavier attach; upside is owner-explicit domain attach."
      - "3: explicit KM11-style exemption row for the registry line. bad_because: another governance term for a small thing; effectively option 1 with ceremony."
    recommendation: 1
    default_if_unanswered: take option 1, record in the second-domain-attach leg PLAN

next: |
  # RELEASED 2026-06-20 — Wave-0 de-risk PASS/PASS, fresh-read confirmed (s-health-core-kernel-wave0-derisk-001,
  # wf_c9806260-68d). t-1 zero-engine subtraction PASS; t-2 KQ2/O1 cold-start bootstrap PASS. This executor CALL is
  # now LIVE. Folded-in Wave-0 residuals (input/PLAN only — done_when §WA-K1–K12 unchanged): F-A subtract per-domain
  # validators · F-B cursor conformance · F-C registry-line governance → non-blocking decision D-kernel-1.
  # ====================================================================================================
  # SUPERSEDED 2026-06-21 (s-health-core-kernel-incident-rca-001). The build below ALREADY HAPPENED and is
  # closed done_with_incident -- DO NOT run it. The frozen WA-K1-K12 verbatim is retained below ONLY as the
  # binding acceptance reference (its permanent home is health-ai acceptance/kernel/wa-k.md). The REAL next
  # action is the incident-fix CALL immediately below. Nutrition is PAUSED.
  # ====================================================================================================
  CALL c-health-core-kernel-incident-fix-001
  to: session lead + executor (health-ai)
  direction: health  node: g-health-core  bet: b-health-core-kernel
  repo: C:\projects\health-ai
  PROGRESS 2026-06-21: HEALTH-AI CLEANUP escalated by owner ("го") to a FULL CLEAN-SLATE RESET and DONE
    (health-ai commit 8e981ec, UNPUSHED): deleted the ENTIRE x_nutrition/ domain + acceptance/kernel/journey-proof.md,
    blanked owner profile/program/anchors to empty templates, emptied the registry (no active domain), rewrote
    tools/check_core_evidence.py (had 125 kg + x_nutrition-active baked in), de-nutritioned AGENTS/SYSTEM/README/
    CHATGPT_PROJECT (6 load-bearing keywords preserved). All 4 gates green; zero personal-data/research/run-output
    remains; engine intact + cold-start possible from core/ alone (adversarially verified: plan wf_1bcd4174, execute
    wf_7dce4f4d). Nutrition is now a TRUE blank slate, NOT rebuilt. NEW next = the owner-watched COLD-START run that
    re-authors nutrition clean -- and that rebuild must FIRST fold in the engine fixes (abstract-first/never-pick-foods
    rule + proof-of-load marker + WA-K2/K3 ceiling clarifier) so the fresh domain is born correct. Remaining scope
    items below. UPDATE 2026-06-21: SEAM RECONCILIATION + PROOF-OF-LOAD MARKER now DONE (health-ai 6b117f0) --
    corrected the save-gate to the owner-confirmed model "specialist composes in full, owner only gates" (system
    composes all owner-facing content incl. concrete menus/recipes; nothing first-time persisted-even-as-PROPOSED or
    activated without explicit echoed owner approval; gate = approve/ask/request-changes conversation; DROPPED the
    abstract-first/never-pick-foods crutch; DEFINED the adjustment-vs-new-authoring boundary so a concrete-menu swap
    under an ACTIVE program is NOT a silent decide-and-inform); WA-K3 "lands PROPOSED = status ceiling, not a
    save-license" clarifier; proof-of-load reply header mandated across router + AGENTS/SYSTEM/CHATGPT_PROJECT.
    Cross-model (Sonnet, 2 reviewers) folded in -- both independently caught the undefined review-driven-adjustment
    re-incident path (now closed). All 4 gates green. REMAINING = WA-K6/K8 live walk + ENCODE the agreed 9-stage
    nutrition process, both at the owner-watched COLD-START.
  why: >
    First nutrition run: an agent surfaced+persisted a CONCRETE owner-facing menu/recipes as PROPOSED without
    owner approval (8504d3f), owner-reverted (adebf3d). Verified root causes (NOT a contradiction): (1) an
    under-specified FREEZE seam -- the frozen WA-K3 line dropped KV3's "(never directly ACTIVE)" status-ceiling
    clarifier + the WA-K2 cross-ref, so a code-only agent read "always lands PROPOSED" as a write-now license;
    (2) a MISSING REQUIREMENT -- "abstract/numeric structure first, never pick concrete foods for the owner" lived
    only in chat/history, never in any contract (WA-K11 violation); (3) STALE CONCRETE DATA left in-repo with
    active flags (recipes/grocery/fallbacks status=active_first_cycle while their source menu=SEED) that agents
    grab; (4) owner-facts (125 kg, smoking-cessation block) duplicated into ~16 projection files instead of one
    source; (5) no visible proof-of-load marker on chat replies; (6) executor returned no OS RESULT (this RESULT
    closed that gap).
  scope (each a separate one-job session):
    - HEALTH-AI CLEANUP (executor; owner-confirm the delete scope FIRST): delete the stale concrete owner-facing
      execution content the owner never approved -- x_nutrition/recipes/first-cycle-base-recipes.md,
      x_nutrition/grocery/current-grocery-needs.md, x_nutrition/fallbacks/fallback-meals.md,
      x_nutrition/reviews/first-cycle-review.md, and the SEED menu shell x_nutrition/menus/current-menu-cycle.md;
      strip duplicated owner-facts out of every projection (single source = core/profile/owner-facts.md); fix
      x_nutrition/index.md so output-heavy families are NOT advertised as "active artifacts". KEEP: kernel
      (core/runtime, core/governance), workflow graph, cursor, program v1 ACTIVE (real owner echo), owner-facts.
    - SEAM RECONCILIATION (os-maintenance; touches signed WA-K rows -> proper play, NOT a hotfix): additive
      clarifier to WA-K3 (status ceiling, not write-now) + restore KV3 cross-ref; record the
      abstract-first / never-pick-foods requirement as a first-class contract rule.
    - PROOF-OF-LOAD MARKER (executor): every chat reply opens with a visible header proving the workflow loaded
      (current state/cursor + which rules loaded), like the Direction-OS opening-contract header -- owner-checkable
      at a glance, so the owner SEES when a chat did not load the rules.
    - VERIFY (executor): WA-K6 (render-before-decision) + WA-K8 (anti-fragility) are red_deferred -- walk them
      live before any nutrition resumes.
  boundaries: nutrition execution stays PAUSED until cleanup + the abstract-first rule + the marker are in place;
    no new build; do not delete program v1 / owner-facts / the kernel; D-kernel-1 default option 1 stands.
  return: a RESULT per sub-session with state_changes for live/health.
  # ----------------------------------------------------------------------------------------------------
  # HISTORICAL (closed done_with_incident, do not run) -- the original build CALL + frozen acceptance follows:
  CALL c-health-core-kernel-spine-journey-executor-001
  to: executor (engineering agent)
  direction: health  node: g-health-core  bet: b-health-core-kernel
  repo: C:\projects\health-ai (branch main; data-layer TIER-1 green; core/runtime + core/governance currently EMPTY — the kernel adds them ON TOP)
  goal: >
    Realize the ONE indivisible TIER-2 kernel spine — the governance/runtime engine every health domain inherits
    unchanged — as md/yaml conventions + per-term procedure-definition files + the registry, over the born-closed
    TIER-1 data layer, by LIFTING the proven hand-rolled nutrition trio (router/graph/cursor) up into core/ and
    BUILDING the net-new governance spine; and walk ONE real artifact SEED→PROPOSED→ACTIVE through the gate in a
    SEPARATE writer context with evidence-of-transition, so the binding journey-proof and the
    lifecycle/gate/barrier/bootstrap precondition are green.
  context: >
    ARCHITECTURE-ON-PAPER INPUT EVIDENCE (carry as input, NEVER into done_when): the 14 contracts KC1–KC14 + 7
    seams S1–S7 + 4 picks (KQ1 writer-applier in a SEPARATE context; KQ2 universal cold-start bootstrap machine;
    KQ3 lifecycle/state-machine ORTHOGONAL, coupling only at render-before-decision; KQ-rev definition-reversion =
    block-and-rebuild orphan cursor) — live/health/work/converge-g-health-core-kernel-arch.md. PLAN AGENDA (HOW
    residuals — set at PLAN, refuted later, NEVER into done_when): KP1 cursor path/token→KC2 · KP2 def+cursor
    shapes + guard-predicate language + per-domain state/edge/family enum→KC3 · KP3 boot-sequence→KC1/KC14 · KP4
    registry-line grammar→KC4 · KP5 approval-marker schema + materiality-check→KC6 · KP6 evidence mechanism
    (commit+diff+re-read)→KC7 · KP7 provenance path→KC11 · KP8 freshness threshold→KC7 · KP9 handoff schema→KC10 ·
    KP10 partial-write atomicity→KC7 · KP11 relax-trigger N→KC8 · KP12 domain-selection signal→KC1; PLAN
    residuals: KQ-rev orphan-detection/repair format → KP2+KP10; KQ-concurrent one-line note (absorbed by KM7
    handoff token + git-merge). DATA-LAYER POINTER (born-closed, never re-open):
    live/health/work/converge-g-health-core.md (CA1–CA9, W1–W79, WA1–WA12; CA5 day_type→carb the one signed
    cross-domain seam; CA8 schema_version; CA9 determinism carriers). REUSE-NOT-REDERIVE: lift
    x_nutrition/procedures/workflow-router.md + workflow/graph.md + state/current-workflow.md into CORE-owned
    files; reuse tools/check_*.py + acceptance/core/matrix.json + negative-fixtures as the conformance harness.
    WAVE-0 F-A (subtraction target is larger than the two files): the firewall-by-subtraction must DELETE the
    per-domain executable validators tools/check_nutrition_*.py + acceptance/x_nutrition/* fixtures +
    x_nutrition/contracts/normalizer-contract.md and replace them with the ONE kernel-universal KC7 LLM-evaluated
    barrier (the write-barrier de-couple already in-scope); each of their checks decomposes 1:1 onto
    KL7+W1+KC7+KC3/KC9/KC10/KD2/KR5. WAVE-0 F-B (attach-time conformance): the live cursor
    x_nutrition/state/current-workflow.md holds KM1/KC2-forbidden content (current_weight_kg + clinical
    post_smoking_cessation_context) — migrate it OUT of the cursor on the lift (cursor = pure non-content control
    pointer). OWNER DURABLE PREF (KV21/WA-K2): NEVER show the owner writer/YAML files — every gate surface presents "result
    + what will be saved"; linter/writer artifacts stay internal. PRECONDITION FRAMING (context, NOT done_when):
    the lifecycle/gate/writer-barrier/cold-start behavior (carried by the verbatim WA-K2/K3/K4/K5/K10 below) must
    be green before any TIER-3 domain reaches ACTIVE. Within the spine the construction order is KC1–KC4 →
    KC5→KC11 → KC12/KC14, and the journey is walked only AFTER the lifecycle+gate+barrier are green.
  boundaries: >
    NO executable runtime/server/daemon/cron/scheduler/background-worker (the LLM is the runtime; trigger
    value-domain is the closed set {owner, chat-turn, checkpoint}). NO second domain re-homed (training stays
    parked; WA-K8 proven by the one-domain removal + write-barrier de-couple from the 4 check_nutrition_*.py
    calls + disable-one-procedure-siblings-green; the two-live-domain sibling-survival proof is a flagged
    follow-on). NO runtime conformance harness firing resolve-state→dispatch / simulating an LLM turn
    (verification = static check-scripts + negative-fixtures + ONE walked journey + fresh-session refutation). NO
    new TREE nodes / no fragmenting the engine (ONE TIER-2 build). NO re-opening any signed
    §KW/§WA-K/§OWNER-DECISION/CA row. NO KP/HOW value minted into a done_when line; NO KC/KP token added to any
    WA-K line. NO owner-facing YAML/writer files shown to the owner. NO hard-delete of dirty x_nutrition seed
    artifacts (move to provenance; F15/F16 is a separate later CALL). NO full PROJECT_SETUP §Re-sync (run under
    the existing writer-gate + acceptance-matrix discipline; stamp a minimal validation.config:
    synced_contract_version + a one-line note "md/yaml kernel: code gates N/A, conformance = matrix+check+journey-evidence").
    NO code-CONTOUR gate force-fit (mutation/headless-build/Assets-existence do not map to a no-app md/yaml
    kernel); the v7 clauses that DO bite are kept: G0 frozen §WA-K verbatim, builder-cannot-weaken-the-oracle,
    binding fresh-session G5.
    WAVE-0 F-C (NEW PLAN residual — input only, NOT into done_when): the registry-line write that grants a domain
    run-authority (attach = 1 folder + 1 line) has no signed governance home; resolve its treatment
    (gated-as-attach-SEED | KM11-style control-file exemption | decide-and-inform) at PLAN BEFORE the first COLD
    new-domain attach (deferred second-domain leg) — it does NOT block this spine+journey (nutrition already
    attached). Tied to owner decision D-kernel-1 in NOW.decisions. Add NO KP/KC/HOW token to any WA-K line.
  done_when: |
    §WA-K1–K12 are the BINDING frozen acceptance, copied VERBATIM (builder cannot edit). Each WA-K → ≥1
    acceptance-matrix row + check_ref; WA-K3/K4/K6/K8/K10 each get a RED negative-fixture the check trips on; each
    engine part resolves to a committed core/** path the check Test-Paths to a non-trivial artifact (the md/yaml
    deliverable-exists analogue). THIS spine wave must turn GREEN: WA-K1, WA-K2, WA-K3, WA-K4, WA-K5, WA-K7,
    WA-K9 (nutrition re-attached thin), WA-K10 (journey-proof, authored cold by the bootstrap machine, with
    commit+diff+re-read evidence), WA-K11, WA-K12. The FOLLOW-ON breadth leg rides WA-K6 (real upstream/downstream
    render pair) and WA-K8 (with one attached domain the "all OTHER domains green" clause is proven STRUCTURALLY +
    disable-one-procedure-siblings-green; two-live-domain proof = binding follow-on). Every WA-K row carries an
    explicit green / RED-deferred disposition (v7 deliverable-coverage — none silently omitted). Closed by a
    FRESH-session refutation of the journey-proof and the lifecycle/gate/barrier/bootstrap.

    VERBATIM §WA-K1–K12 (frozen; no HOW/KP/KC token on any line):
    WA-K1 A turn that reaches output or runs a procedure without first passing the single router (resolve-state→dispatch) is illegal; exactly one router serves all domains; no domain stands up a second dispatcher. (KR1)
    WA-K2 confirm-before-save BY LAYER (axis authoring-vs-adjustment): first-time authored owner-facing content + SEED/PROPOSED→ACTIVE flips present a readable "result + what will be saved" and persist only after the owner's explicit echoed approval; daily logging AND review-driven adjustments of already-ACTIVE anchors stay decide-and-inform. (KV20/KV21/KM11/KR11)
    WA-K3 No role both authors and activates: AI-authored owner-facing content always lands PROPOSED; PROPOSED→ACTIVE only on the owner's echoed words + named artifact + version + date; no timeout-to-active. (KV3/KV4/KV5/KV17)
    WA-K4 Authority is a per-artifact `status` property on SEED→PROPOSED→ACTIVE→SUPERSEDED; only ACTIVE is authority; location never confers it; ≤1 ACTIVE per family; skip-hop forbidden. (KV1/KV2)
    WA-K5 No durable status flip is trusted without evidence-of-transition (commit + diff + re-read), refuted outside the producing chat. (KV11)
    WA-K6 Render-before-decision: a downstream projection (menu/grocery/log) cannot be PROPOSED/ACTIVE until its upstream program+cycle are ACTIVE — enforced at dispatch AND re-validated at write. (KV9/KS8/KL4/KD5)
    WA-K7 One-chat-one-job: each job = one bounded deliverable from a closed job-type enum; >1 content family per non-logging job is writer-rejected; DAY_LOOP the one same-state logging exception; each job closes with a mandatory handoff packet. (KR9/KV15/KV16)
    WA-K8 Anti-fragility (binding): removing any one domain (delete folder + registry line) leaves the kernel + all other domains green (every remaining derived number resolves; no dangling reference; no broken dispatch), and disabling/superseding one procedure leaves its siblings green. (KA6)
    WA-K9 A thin domain layer is EXACTLY {namespaced data, one procedure per term, one registry line, one state-machine instance} — zero engine code; it never edits or duplicates a kernel engine part. (KL1/KL7)
    WA-K10 A durable repo end-to-end JOURNEY proof (one real artifact walked SEED→PROPOSED→ACTIVE through the gate, with evidence-of-transition) is a binding acceptance precondition before any domain runs. (KD3 + F8)
    WA-K11 Provider-portable runtime: working-state/cursor is file-backed (md/yaml + git), never provider memory; any provider cold-resumes the same procedure from the committed cursor. (KM6/KR12)
    WA-K12 A domain's state-machine DEFINITION (stage ordering + allowed transitions) is an owner-approved ACTIVE artifact riding the gate. (KS10 / B-3)
  return: >
    RESULT: per-WA-K matrix-row + check_ref + (where applicable) negative-fixture status; the WA-K10
    journey-proof artifact path + its commit+diff+re-read evidence; the committed core/** path per engine part;
    an explicit green / RED-deferred disposition for every WA-K1..K12 (this wave green-set vs follow-on-deferred,
    each named, none silently omitted); confirmation no signed row re-opened and no KP/HOW/KC token leaked into a
    done_when line; the minimal validation.config stamp; the fresh-session refutation verdict.
  budget: >
    honest multi-leg — the executor plan-gate splits into legs each ≤ a focused half-day (e.g. 3a lift KC1–KC4 +
    registry / 3b governance spine KC5–KC7+KC14 + journey-proof); stop-and-report if any leg runs 2× over.

END_OF_FILE: live/health/NOW.md
