# CALL — c-exec-g-5a7c-thing-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-thing-1
issued: 2026-08-12 by s-work-g-5a7c-thing-1-dispatch-001
status: ready
basis: `f3c40c4010ee2f0f700cdf1bfbcea14a301d488c` — `origin/main` = `origin/dev`, перемерено
       `git ls-remote origin` перед выпуском 2026-08-12.

**ЕСЛИ `origin/main` СДВИНУЛСЯ — НЕ СТОП, А ПЕРЕБАЗИРОВАНИЕ.** Возьми свежую голову, собери на ней,
назови её отдельной строкой возврата. СТОП домой — на всё ОСТАЛЬНОЕ неожиданное.

## slot

**WIN-U3**, каталог `C:\projects\Unity\GasCoopGame_win-u3`, ветка `slot/win-u3`.

Перемерено селектором: все четыре слота `CLEAN / AVAILABLE / lease none`. **WIN-U3 отстаёт сильно —
его дерево на `352f96b0`.** Догон до `f3c40c40` будет длинным. WIN-U1 и WIN-U2 заняты двумя другими
нарядами и брать их нельзя.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U3 -Claim -LeaseId c-exec-g-5a7c-thing-1-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/main`, readback дерева, `git status` пуст.

**UNITY СКОРЕЕ ВСЕГО НЕ НУЖЕН.** Вся работа — в engine-free `Core` и в headless-тестах, а гейт
доказывается СОСТАВОМ КОММИТОВ, а не глазом. Если по ходу окажется, что без редактора никак — это
СТОП домой с объяснением, а не самостоятельное открытие Unity в отстающем слоте.

**ПАРАЛЛЕЛЬНО ИДУТ ДВА НАРЯДА, И ИХ ФАЙЛЫ ТЕБЕ ЗАПРЕЩЕНЫ:**
- `c-exec-g-5a7c-places-1-001` в WIN-U2 — `Assets/TunnelCrew/Profiles/**`, `Core/House/**`,
  `World/HousePlaceMarker.cs`, `Network/NetworkHouseholder.cs`;
- `c-exec-g-5a7c-body-1-001` в WIN-U1 — `Assets/TunnelCrew/Art/**`,
  `Network/NetworkWalkerPresentation.cs`, `World/Householder.prefab`,
  `Settings/PlaySettings.asset`, `Settings/PresentationSettings.asset`.

## goal

**У ПРЕДМЕТА ПОЯВЛЯЕТСЯ СОСТОЯНИЕ, И СМЕНА СОСТОЯНИЯ САМА ПРОИЗВОДИТ СОБЫТИЕ МИРА. БУДИЛЬНИК
ПЕРВЫЙ.**

Слова владельца 2026-08-12, которыми задача куплена: «у нас груз, это должен быть какой-то
интерфейс, который потом либо расширять, добавлять функционал»; «даже может, когда стоять, например,
будильник, а несут карандаш, и карандаш задел будильник, там будильник может зазвонить»; «не только
с тем, что ударился, а с, например, ориентацией в пространстве»; «нам такую надо тоже очень хорошо
основу для этого заложить».

**И ЕГО ЖЕ ГРАНИЦА, дословно:** «Я не говорю, что нам сейчас надо краску делать». Краска, ящики,
плита и любые следующие предметы НЕ делаются. Форма обязана их принять — и это проверяется гейтом на
подставленном втором предмете, а не обещанием.

**НАЗВАННЫЙ ПОДХОД — ОДИН:** предмет получает состояние, смена состояния кладёт обычную ситуацию в
тот же тик-лист, которым уже пользуется груз, и хозяин слышит её обычным путём. **Прямой дороги
«предмет → хозяин» не появляется ни при каком раскладе.** Отход — ESCALATE домой.

**ЭТО НЕ ОБЩАЯ СИСТЕМА ИНТЕРАКТИВНЫХ ПРЕДМЕТОВ.** Устав дословно: «симуляции сред входят по одной,
каждая отдельным решением, общий фреймворк под них не строится». Второй новый слой волну не
расширяет, а режет. Основа доказывается не чертежом, а тем, что второй предмет стоит дёшево.

## context — перемерено чтением байтов на `f3c40c40` 2026-08-12

### ПОДАРОК ПЕРВЫЙ: половина «карандаш задел будильник» УЖЕ РАБОТАЕТ

`Network/CargoBodyContact.cs:117-179` — `OnCollisionEnter` и `OnCollisionStay` вызывают `Record`,
и **`Record` НЕ ФИЛЬТРУЕТ, во что именно врезались.** Любая коллизия даёт силу и точку. Поиска
`IgnoreCollision`, `excludeLayers`, `includeLayers`, `gameObject.layer` и `LayerMask` по всему
`Assets/TunnelCrew` — **ноль совпадений**, то есть тела груза сталкиваются друг с другом обычным
образом.

Докстрока файла говорит «Only the house is physical here, so every press this records is a press
against the house» — это утверждение о МИРЕ на момент написания, а не фильтр в коде.

**Следствие: удар груза о груз уже сегодня производит контакт и уже становится шумом**, помеченным
`ContactedHouse`. Чего нет — у будильника нет СОСТОЯНИЯ и смена состояния не рождает СВОЕЙ ситуации.
Строить надо это, а не механику столкновения.

### ПОДАРОК ВТОРОЙ: дверь для нового производителя уже прорублена

`Core/Multiplayer/CarryWorld.cs:66-68,97-103,149-153` — у мира есть `_situations` (тик-лист) и
`_queuedSituations`, и внешний производитель уже умеет класть туда ситуацию: докстрока на `:97`
говорит, что положенное «enters the tick list as cargo-produced situations and is reduced by the
same». Эту дверь построила закрытая `t-observe-1` под ручные маркеры лаборатории. Ты ей пользуешься,
а не рубишь новую.

### ПОДАРОК ТРЕТИЙ: у экземпляра груза уже есть изменяемые значения

`Core/Cargo/CargoState.cs` несёт `CargoId`, `CargoClassId`, `X/Y/Z`, `Yaw`, кватернион
`RotationX/Y/Z/W`, `HomeX/HomeZ`, **`Integrity`** и **`IsActive`**. То есть форма «у экземпляра есть
своё изменяемое значение, и оно ездит в состоянии» уже существует, и кватернион даёт тебе
ориентацию, которая понадобится для второго способа срабатывания.

**`Integrity` НЕ ТРОГАЙ И НЕ ИСПОЛЬЗУЙ КАК СОСТОЯНИЕ БУДИЛЬНИКА:** повреждение и уничтожение груза
вырезаны подписанной строкой узла.

### ЛОВУШКА, КОТОРУЮ НАДО ЗНАТЬ ДО ПОСТРОЙКИ: за тик хозяин слышит РОВНО ОДНО

`Core/Multiplayer/CarryWorld.cs:167-168`:
`Stimulus = WorldSituationNoise.Loudest(_readOnlySituations, rules.Noise);` и сразу
`Householder.Observe(Stimulus, tickSeconds);`

Весь тик-лист сводится к ОДНОМУ самому громкому стимулу, и только он доходит до хозяина. Порядок
громкости в `Core/Situations/WorldSituationNoise.cs:168-183`: `Landing` 4 > `RepeatedHeavyNoise` 3 >
`BoardCreaked` 2 > `QuietRustle` 1.

**Значит звон, рождённый В ТОТ ЖЕ ТИК, что и удар, который его вызвал, будет ПРОГЛОЧЕН более громким
ударом.** Это не дефект и не повод трогать сведение: правило сведения принадлежит дорожке хозяина, и
трогать его тебе запрещено.

Правильный ответ здесь — тот же, что и требование владельца про конечность звона: **звон живёт
несколько тиков**, поэтому будет услышан на следующем тике, когда удар уже не соревнуется. Это
делает длительность звона осмысленной, а не декоративной. Проверь это прогоном и назови в возврате.

### Числа, которые нельзя двигать

`Settings/GameRulesSettings.asset`: `_cargoRustleForce: 60`, `_cargoKnockForce: 150`,
`_cargoHeavyNoiseForce: 400`. Три класса груза: `0.192 × 0.04 × 0.04` масса `48`;
`0.288 × 0.06 × 0.06` масса `100`; `0.384 × 0.08 × 0.08` масса `240`. Налётчик ростом `0.16` м.
В сцене четыре точки появления груза, у каждой свой класс.

`WorldSituationNoise` дополнительно: порог `<= 0` ВЫКЛЮЧАЕТ свою полосу, а не пропускает всё.

### Что рядом сломано и что чинить запрещено

`Core/Multiplayer/CarryWorld.cs:162` — `Deliveries.Observe(Cargo.GetState(0))`: доставка видит
только первый груз. Это зарегистрированная улика `i-delivery-observes-only-the-first-cargo-001`, и
она **НЕ твоя работа**. Не чини.

### База прогонов

`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **214 passed,
0 failed, 0 skipped**, прогнано 2026-08-12 на `f3c40c40`.

`tools/hygiene.ps1` — зелёный. **`tools/check.ps1 -Deliver` КРАСНЫЙ на семи исторических отчётах —
зарегистрированный дефект `i-deliver-gate-red-when-main-catches-dev-001`, НЕ твоя работа и НЕ повод
для СТОПа.**

## boundaries

- **Задача не расширяется.** Три строки `done_when` — весь объём.
- **НИ ОДНА СЦЕНА НЕ ОТКРЫВАЕТСЯ.** `IntegratedHouse.unity` принадлежит задаче `t-rooms-1`,
  `HouseholdLab.unity` — задаче `t-lab-2`, и обе копируются фикстурами в тестовый проект. Гейт
  доказывается headless на существующих точках появления груза; если без сцены никак — СТОП домой.
- **НЕ ОТКРЫВАЕШЬ файлы параллельных нарядов** — списки в разделе `slot`.
- **ПРАВИЛО СВЕДЕНИЯ «самый громкий за тик» НЕ ТРОГАЕТСЯ.** Оно принадлежит дорожке хозяина. Если
  звон глохнет — лечится длительностью, а не сведением.
- **НОВОЙ ПОЛОСЫ ШУМА НЕТ, ПОРОГИ `60 / 150 / 400` НЕ ДВИГАЮТСЯ**, специального случая под
  звонящий предмет не заводится.
- **`Integrity` не используется как состояние**, повреждение и уничтожение не делаются.
- **НЕ ДЕЛАЕТСЯ:** каталог предметов, ящики, плита, хрупкость, аномальные свойства, ловушки,
  защитные устройства, выключение предмета игроком (слово владельца «наверное, даже может и не надо
  выключать»), рыночная и личная ценность, краска и любые следующие предметы.
- **ПРЯМОЙ ДОРОГИ «ПРЕДМЕТ → ХОЗЯИН» НЕ ПОЯВЛЯЕТСЯ.** Всё идёт через тик-лист ситуаций.
- **Настроечные значения — не код** (контракт 36): длительность звона и любые пороги живут в
  настройках и тестами не покрываются. Тест проверяет ФОРМУ, никогда величину.
- **Тест только там, где владелец не увидит ответ глазом.** Здесь глазом не видно почти ничего,
  поэтому тесты — основное доказательство.
- **Самописный сканер как улика поведения запрещён.**
- **Инструмент недоступен → СТОП домой.** Никогда обходной путь.
- **Решение владельца → ESCALATE домой.**
- **Расписка обязательна:** наряд не существует, пока исполнитель не расписался.

## done_when

Три строки — потолок контракта 36. Ни одна не снимаема.

1. **СОСТОЯНИЕ ЕСТЬ, И ОНО САМО ПРОИЗВОДИТ СОБЫТИЕ.** У экземпляра груза появляется состояние;
   будильник первый и умеет быть заведённым, звонящим и упавшим. **Он начинает звонить оттого, что
   его задел другой груз**, а смена состояния кладёт обычную ситуацию в тик-лист мира тем же путём,
   которым туда попадают ситуации груза — без прямой дороги к хозяину. Звон конечен: длится
   настроечное число и замолкает.

2. **ГЛАВНЫЙ ГЕЙТ — РАСШИРЯЕМОСТЬ, ДОКАЗАННАЯ СОСТАВОМ КОММИТОВ.** Второй предмет с другим
   состоянием **и второй способ срабатывания — не от удара, а от ориентации** (перевернулся) —
   вводятся ДОБАВЛЕНИЕМ: коммит, который их вводит, **не трогает ядро, решатель, перечисления и
   первый предмет**. Доказывается составом коммитов, как уже доказывалась расширяемость
   обработчиков хозяина, а не рассуждением о диффе.

3. **НИЧЕГО НЕ СЛОМАНО, ОТКАТ НАСТОЯЩИЙ.** Пороги `60 / 150 / 400` не двигались, нового вида полосы
   нет, правило сведения не тронуто, пять реакций целы, поведение по умолчанию не изменилось;
   прогон тестов назван числом против базовых 214; и существует откат одним коммитом, исполненный
   НАСТОЯЩИМ `revert`, дерево после которого сходится с деревом базы.

## return

**Первая строка возврата — РАСПИСКА:** сколько строк `done_when` получено (должно быть три) и с
какого базиса собрано.

Дальше:

- что сделано, по каждой из трёх строк отдельно;
- **состав коммитов гейта 2**: какой коммит ввёл второй предмет, какой — второй способ
  срабатывания, и сколько файлов каждый тронул;
- **что произошло с ловушкой «самый громкий за тик»**: проглотился ли звон ударом в тот же тик,
  через сколько тиков он был услышан, и какая длительность звона это обеспечивает — числами;
- полный список путей диффа, и отдельно подтверждение, что среди них НЕТ ни одной сцены и ни одного
  файла параллельных нарядов;
- прогон тестов до и после числами;
- откат: команда, хеши, совпадение деревьев;
- **что НЕ сделано и почему**;
- всё неожиданное, включая всё, что противоречит перемеренному выше контексту.

## budget

Половина сфокусированного дня. Порог смерти 3 этой ставки считает ноги: если на общую задачу и
первые задачи всех трёх дорожек уйдёт больше двенадцати ног, первой на паузу встаёт дорожка `body`,
следом `t-loot-1`.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-thing-1-001-call.md
