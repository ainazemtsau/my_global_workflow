# CALL c-spike-pgg-001

to: executor (owner-present: Unity-часть = руки владельца + MCP)
for: g-9c41 / Полигон М1, шаг 4 (модули DA) — таймбокс-спайк PGG
issued: 2026-07-10 (s-pgg-analysis-001; owner «да»; вердикт анализа = work/pgg-analysis-2026-07-10.md)

goal: |
  Ответить на ОДИН вопрос (старый арх-вердикт ≤2-3 дня, теперь с готовым планом): «На Unity 6.3:
  нарисовал клетки (GridPainter) → Generate → сохранил СТАТИЧНЫЙ префаб-модуль (двери по позициям
  сокетов DA, ноль PGG-компонентов) → DA SnapGridFlow собрал уровень с этим модулем → наш
  BuiltSceneRoomReader вокселизировал его как рукотворный?» PASS = PGG принимается редакторским
  помощником авторинга модулей в PLAN шага M1-4 (+ покупка тайл-кита + батч-скрипт вариантов +
  авто-маркеры через OnSpawnProcessor). FAIL на этапе 6.3/UI = отказ, модули руками (опц. оставить
  только Tile Designer, если выживет он один).

план (из анализа):
  День 1: БРОСОВЫЙ Unity 6.3 проект (НЕ продукт!) → импорт PGG 1.6.6.2.12 из кэша Asset Store →
    Field Designer Window + GridPainter живы, Generate работает. Известный Windows DPI-баг прячет
    кнопку Paint — сначала DPI-совместимость, потом выводы. Гейт: UI жив на 6.3.
  День 1-2: официальный плейлист (5 частей) + рецепт комнаты из мануала (стены→косметика→мебель;
    дверные направляющие). FieldSetup ОДНОГО архетипа комнаты; CellSize согласовать с габаритами
    DA-модуля и шагом газ-сетки (решение записать).
  День 2-3: один модуль → наш save-as-prefab скрипт (~50 строк: снять GridPainter/GridPainterData/
    ObjectStampEmitter, mesh-combine выключить или запечь) → перенести префаб в продукт → регистрация
    в SnapGridFlow → DA собирает → читатель вокселизирует (реальная площадка: Unity via MCP + owner-eye).

boundaries: |
  Таймбокс ЖЁСТКИЙ ≤3 дня — дальше STOP и честный вердикт, не «ещё чуть-чуть». Один архетип комнаты;
  плейсхолдер/демо-префабы разрешены; БЕЗ Build Planner, БЕЗ рантайм-генерации (отклонена вердиктом),
  БЕЗ покупки кита до PASS. Продуктовый репозиторий не трогать до финального шага переноса (перенос =
  один префаб + регистрация, отдельным коммитом). Никаких самописных сканеров как доказательства —
  площадка = редактор/MCP + глаза владельца. Пиновать версию PGG.

done_when: |
  Вердикт PASS/FAIL записан с доказательством (владелец видел: DA собрал уровень с PGG-модулем, газ
  читается; или зафиксирован этап падения). PASS → капчур в PLAN шага M1-4 (кит, батч, маркеры).
  RESULT + LOG; G5 лёгкий (спайк — не продуктовый лег; бросовый проект).

launch:
  lane: D-соседняя (авторинг-разведка) — НЕ занимает ни один worktree линий
  venue: бросовый Unity-проект вне всех репо (например C:\projects\Unity\PGG_Spike) + финальный шаг в продукте
  machine: ПК (редактор бросового проекта; №1/№2/№3 не трогать)
  base: продукт не трогается до шага переноса; перенос — на свежий origin/main (§Re-sync)
  conflict_surface: нет (до переноса); перенос = Assets/**/Modules + регистрация SnapGridFlow
  depends_on: []  # параллелен всему, вкл. Phase 0
  merge_queue: перенос-коммит — в хвост очереди по борду
  gates: owner-eye = сам спайк; полные ворота НЕ требуются (бросовый проект), кроме шага переноса в продукт

END_OF_FILE: live/indie-game-development/work/c-spike-pgg-001-call.md
