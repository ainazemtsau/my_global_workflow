# CALL — c-exec-newgame-folder-and-scene-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 35
basis: 4e95845c (main, проверено первой рукой 2026-08-02)
direction: indie-game-development
node: g-6b13
task: a-1
lane: переноска
issued: 2026-08-02 by s-shape-g-6b13-small-tasks-and-lanes-001
status: ready
slot: Называет владелец в момент запуска. **`WIN-U1` НЕДОСТУПЕН** — на нём висит аренда мёртвого
      корня `c-exec-newgame-folder-and-gates-001`. Свободны `WIN-U2`, `WIN-U3`, `WIN-U4`; их
      worktree уже существуют, новых не создавать. `WIN-CTRL` (`GasCoopGame_dev`) незаконен.
      Идёт ПАРАЛЛЕЛЬНО с `c-exec-gate-surface-cut-001` в другом слоте — файлы не пересекаются.

## goal

У новой игры есть своя папка, и в ней открывается пустая сцена. Жмёшь Play — сцена открывается.

Это всё. Первый шаг ставки, и он намеренно крошечный.

## context

Концепт: кооперативная игра, где команда выносит груз из дома, копает себе тоннель и прячет его, а
хозяин дома это замечает и отбирает. Ближайшая цель — стенд, на котором владелец своими руками
посмотрит, как двое несут ОДИН груз по сети. Эта задача только заводит место; груз, сеть и хозяин —
следующие задачи, не эта.

**Заготовка уже существует и её можно поднять.** Остановленная ветка `slot/win-u1` (коммиты
`8eb0bb0f..c4d34fc4`) завела `Assets/TunnelCrew/Core` с asmdef, `core/TunnelCrew.Core.csproj` и
`TunnelCrew.Core.slnx`, ничего старого не тронув. Взять оттуда каркас можно — назови в возврате, что
именно взял. **`tests/TunnelCrew.Core.Tests/Sources/SourcePresenceGateTests.cs` (247 строк) НЕ
БРАТЬ ни в каком виде:** это тесты выдуманного требования, которое владелец отверг пять раз.
Аренду `WIN-U1` освободить, ветку дальше не развивать.

## boundaries

- **`tools/`, `validation.config` и `check.ps1` НЕ ТРОГАТЬ.** Их владеет параллельный наряд
  `c-exec-gate-surface-cut-001`. Подключение новой игры к проверкам придёт позже, когда у неё
  появится логика, которую есть смысл тестировать. Здесь тестировать нечего.
- **Старый код не трогать вообще.** Ни `Assets/GasCoopGame/**`, ни `tests/GasCoopGame.Core.Tests`.
- **Никаких проверок на то, что папка пуста, что сцена не открылась и тому подобного.** Владелец
  видит это глазами за секунду и возражал против таких проверок пять раз. Правило:
  `C:/my_global_workflow_worktrees/indie-game-development/live/indie-game-development/knowledge/no-checks-the-owner-makes-by-eye.md`.
- Тесты пишутся только на бизнес-логику нового кода. В этой задаче логики нет, значит тестов нет.
- Имя папки, устройство сцены и форма каркаса — твои решения, владельцу не выносятся: его прямое
  слово — «технически обсуждается в репозитории».
- Не выдумывать: чего нет в этом наряде — вопрос домой одной строкой, а не догадка в код.
- Бумаги минимум. Владелец 2026-08-02: «не пиши тысячу строк бумаг ради десяти строк кода».
  Предыдущая попытка этой задачи стоила 1427 строк и дала шесть строк игрового кода.

## done_when

Владелец открывает проект, жмёт Play — сцена новой игры открывается. Больше в ней ничего нет.

## return

Домой, в направление `indie-game-development`, задача `a-1` ставки `g-6b13`. В возврате: коммиты,
что именно поднято из `slot/win-u1`, и одной строкой — что оказалось дороже или невозможнее.

## budget

Одна инженерная нога, и она маленькая.

END_OF_FILE: live/indie-game-development/work/c-exec-newgame-folder-and-scene-001-call.md
