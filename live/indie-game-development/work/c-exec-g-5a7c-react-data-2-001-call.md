# CALL — c-exec-g-5a7c-react-data-2-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: householder
task: t-react-data-2
issued: 2026-08-11 by s-work-g-5a7c-react-data-2-dispatch-001
status: ready
basis: `2c8dc0c79ce2100a9e9dbb3d64f69729f1d4a313`, дерево
       `931b31eb3d724278d54d0aca448357680affc753` — `origin/dev` = `origin/main`, перемерено
       `git ls-remote origin` перед выпуском. Содержит закрытые `t-react-mem` и `t-react-2`.

**ЕСЛИ `origin/dev` СДВИНУЛСЯ — НЕ СТОП, А ПЕРЕБАЗИРОВАНИЕ.** Взять свежую голову, собрать на ней,
назвать её отдельной строкой возврата. Правило введено прошлым нарядом и сработало как задумано.
СТОП домой — на всё ОСТАЛЬНОЕ неожиданное.

Пин: `engineering_contract: 36` против продуктового `synced_contract_version: 36`. Re-sync не нужен.

## slot

**WIN-U2**, каталог `C:\projects\Unity\GasCoopGame_win-u2`, ветка `slot/win-u2`.
Перемерено файлом состояния перед выпуском: **все четыре слота `AVAILABLE / lease none`**.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-react-data-2-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/dev`, readback дерева, `git status` пуст.

Unity не требуется: работа в engine-free `Core`, `Assets/TunnelCrew/Profiles/**`, `StreamingAssets`
и headless-тестах. Мост WIN-U2 существует (`http://localhost:27497/p/41ebd445`); поле `unrecorded` в
файле состояния — устаревшая запись, а не отсутствие моста.

## goal

**Поведение хозяина начинает описываться ФАЙЛОМ, и описанное файлом работает В ИГРЕ.**

Дословное требование владельца 2026-08-11, названное им ГЛАВНЫМ: «мне ещё главное требование, чтобы
я мог вот это поведение хозяина кастомизировать, желательно через чат-бот — я ему говорю, **он JSON
кастомизирует**… **мы программно не задаём никакого хозяина**».

Эта задача — срез, сделанный `t-react-2` по разрешённой линии, и заведена она сразу, чтобы срез не
потерялся.

## context — перемерено чтением байтов на `2c8dc0c7`

### Точка расширения есть, и она хороша

`IHouseholderDecisionHandler` (`HouseholderDecisionPipeline.cs:6-12`) — `Name` плюс один метод
`Evaluate(HouseholderDecisionSnapshot)`.

`HouseholderDecisionPipeline` (`:40-70`) принимает `params IHouseholderDecisionHandler[]`, спрашивает
дефолтную политику только когда не ответил никто, и отвергает пустые имена и дубли имён.

Арбитр — одна названная вещь: `HouseholderDecisionArbitration.RuleName =
"HighestWeightThenHandlerName"` (`:21`).

**Оба существующих обработчика ПАРАМЕТРИЗОВАНЫ, то есть строятся из данных без нового кода.**
`HouseholderFactCombinationDecisionHandler` (`:16-28`) принимает имя, два имени фактов, строку
реакции и необязательное имя ветки. Второй обработчик устроен так же вокруг порога `TimesNoticedAt`.

### ГЛАВНАЯ ДЫРА, ради которой задача и существует

**`HouseholderDecisionPipeline.Default` — ПУСТОЙ конвейер** (`:46-47`,
`new HouseholderDecisionPipeline()` без обработчиков), и именно он стоит политикой в игре
(`AuthoritativeHouseholder.cs:147`, `:157`, `Householder.cs:1706`).

**В продакшене НИКТО не собирает конвейер с обработчиками — перемерено поиском
`new HouseholderDecisionPipeline` по `Assets`: все попадания либо `Default`, либо его использование.
Композиции с обработчиками есть только в тестах.**

То есть сегодня оба построенных обработчика в собранной игре не работают. Точка расширения доведена
до игрового пути, но ВКЛЮЧАТЬ в неё нечего и нечем. **Это и есть строка 2.**

### Форма ветки поведения уже совпадает с формой шага распорядка

`HouseholderBehaviorBranch` (`HouseholderRoutineProfile.cs:76`) — именованная одноразовая лента
шагов; конструктор уже проверяет: имя обязательно, шагов не меньше одного, у каждого шага конечная
положительная длительность, обстоятельство и умение не `None`.

`HouseholderRoutineStep` несёт `DurationSeconds`, `Circumstance`, `Ability`, `PlaceName`,
`RoutineReaction`.

**Файл профиля уже описывает шаги ровно этими словами.**
`Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json` — 20 строк, поля
`durationSeconds`, `when`, `action.ability`. То есть ветка ложится в тот же формат ДОБАВЛЕНИЕМ
раздела, а не новым форматом.

### Что разборщик умеет сегодня

`HouseholderProfileLoader` знает ДВА умения (`:43` `walk-route`, `:46` `wait-at-route-point`) и всё
прочее отвергает. Приём «неизвестное отвергается названно» уже есть — его надо распространить, а не
изобретать.

Заготовка под места по имени тоже есть: намеренно пустой `IsKnownPlace` с комментарием, что его
подключит будущая задача.

### Что уже зелёное

`2c8dc0c7` даёт **184 passed, 0 failed, 0 skipped** — прогнано Направлением на этой самой голове, на
выгруженном дереве.

## boundaries — что НЕ трогать

- **НЕ ЗАКОЛАЧИВАТЬ ДВЕРЬ** (`knowledge/core-is-extended-never-rewritten.md`). По каждому месту,
  которое задача открывает, в возврате называется результат теста **«чтобы добавить сюда новое —
  ДОБАВИТЬ или ПРАВИТЬ?»**.
- **Форма обработчика и арбитр НЕ переделываются.** Интерфейс `IHouseholderDecisionHandler`, правило
  `HighestWeightThenHandlerName` и вычисляемый вес построены прошлой задачей и остаются как есть.
  Задача добавляет ДОРОГУ ИЗ ФАЙЛА к ним, а не новую механику решений.
- **Числовая оценка, жеребьёвка, долговременная память между запусками, зрение, Odin Inspector и
  редакторский UI — вне задачи.** Odin владелец назвал как «потом», и формат данных от него не
  зависит.
- **Пять производственных реакций не меняются по содержанию** — те же поводы, приоритеты, занятия,
  времена жизни.
- **Файлы дорожки `cargo-hands` — чужие:** `Core/Cargo/**`, `Network/CargoBody.cs`,
  `Network/NetworkPlaySettings.{cs,asset}`, тесты `Cargo*`, `TelekineticPickup*`,
  `FirstPersonCarry*`. Упёрся — СТОП домой.
- **Публичные сигнатуры `CarryWorld.Advance` не меняются** (обе перегрузки).
- **Пороги `60 / 150 / 400` не двигаются.**
- `IntegratedHouse.unity` не редактируется. `ProjectSettings/**`, `Assets/GasCoopGame/**` закрыты.
- **ЗАПРЕЩЁННЫЕ ДОВОДЫ, снятые владельцем дословно:** «сочетаний получится 2^N» и «столько-то
  тестов сломается» — не принимаются.
- Самодельный сканер исходников уликой не является и не пишется.

## РАЗРЕШЁННАЯ ЛИНИЯ РАЗРЕЗА

**Строка 2 — «описанное файлом работает В ИГРЕ» — остаётся ВСЕГДА.** Без неё задача повторит уже
случившуюся ошибку: шов есть, а включить в него нечего.

Если не помещается в полдня, режется в таком порядке и это называется в возврате:

1. сначала уезжают ОБРАБОТЧИКИ из файла, а ВЕТКИ из файла остаются — они дешевле, потому что форма
   шага уже совпадает с форматом;
2. схема условий богаче двух готовых форм обработчика (произвольные предикаты из данных) НЕ строится
   в этой задаче ни при каком раскладе — данными собираются те две формы, что уже есть.

Обратный разрез запрещён: ветки из файла без работающей дороги в игру бесполезны.

## owner-visible check

**Владелец эту задачу руками НЕ судит.** Его вердикт по дорожке назначен на `t-react-3`.

Поведение при этом обязано остаться прежним, когда файл ничего не авторит, и измениться там, где
авторит. Писать «поведение не изменилось» целиком — запрещено.

## headless evidence

- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`. База — `184 passed` на `2c8dc0c7`.
- **ГЛАВНЫЙ ГЕЙТ — ОБРАБОТЧИК, ОПИСАННЫЙ ТОЛЬКО ФАЙЛОМ, СРАБАТЫВАЕТ НА ИГРОВОМ ПУТИ.** Тест
  авторит строку в файле профиля, поднимает мир и гоняет `CarryWorld.Advance` — и хозяин ведёт себя
  иначе. Тест на пути правил этого НЕ доказывает: сегодня в игре стоит пустой конвейер.
- **Гейт ветки из файла:** ветка, описанная только файлом, включается обработчиком и по окончании
  возвращает прежнее занятие.
- **Гейт кривой строки:** неизвестное имя факта, неизвестное умение, дубль имени обработчика,
  отрицательная длительность — отвергаются НАЗВАННО, а не глотаются. Показать тестом на каждую
  форму порчи.
- **Гейт пустого авторинга:** файл без веток и обработчиков даёт поведение как на базе, доказано
  существующими зелёными тестами.
- **Зелёные ворота поведением игры не являются**
  (`knowledge/green-gates-do-not-compile-the-game.md`).

## откат

- **Точка возврата — ГОЛОВА ДОРОЖКИ на момент старта** (свежая `origin/dev`), а не константа наряда.
- Откат назвать ОДНОЙ командой и **исполнить настоящим revert**, как в двух предыдущих задачах:
  после него `git rev-parse <ветка>^{tree}` возвращает дерево той головы знак в знак.
- Прямо заявить, что вне репозитория для отката ничего не нужно.

## done_when

1. **Ветка поведения и обработчик описываются ФАЙЛОМ.** Профиль хозяина получает разделы под ветки
   и под обработчики; из авторской строки собираются те же две формы обработчика, что уже построены
   (комбинация двух фактов; порог накопления), и та же `HouseholderBehaviorBranch`. Формат
   расширяется ДОБАВЛЕНИЕМ раздела — форма шага ветки уже совпадает с сегодняшним шагом распорядка.

2. **Описанное файлом работает В ИГРЕ, а не только в тесте.** Сегодня `HouseholderDecisionPipeline.Default`
   пуст и в продакшене никто не собирает конвейер с обработчиками — перемерено поиском. После задачи
   игра собирает конвейер из прочитанного файла, и обработчик, описанный ТОЛЬКО файлом, срабатывает
   через `CarryWorld.Advance`. Это главный гейт.

3. **Кривая строка отвергается названно, пустой авторинг ничего не меняет, границы целы, откат
   проверен.** Неизвестные имена, дубли и невалидные числа отвергаются с названной причиной, а не
   молча. Файл, ничего не авторящий, даёт поведение как на базе; пять производственных реакций целы
   по содержанию. Границы целы — сцена, файлы `cargo-hands`, обе публичные сигнатуры
   `CarryWorld.Advance`, пороги, `ProjectSettings/**` — не тронуты, доказано диффом. Откат назван
   одной командой, исполнен настоящим revert и проверен readback дерева.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / householder / t-react-data-2`.

**ПЕРВОЙ СТРОКОЙ — РАСПИСКА:** число полученных строк `done_when` (их три).

Также обязательно:

- **тест «ДОБАВИТЬ или ПРАВИТЬ» по КАЖДОМУ открытому месту**;
- **как выглядит авторская строка** — показать её кусок файла целиком, чтобы владелец увидел, что
  именно он будет диктовать;
- **где игра читает файл и собирает конвейер** — точное место, и почему именно там;
- **что происходит при кривой строке** — по одному примеру на каждую форму порчи;
- **сколько стоит НОВАЯ форма обработчика** (не новая строка, а новая форма) — числом правок;
- **резал ли по разрешённой линии** — да или нет, что уехало и почему;
- проверенный откат: команда и readback дерева;
- терминальный readback селектора `CLEAN / AVAILABLE / lease none`;
- **отдельной строкой: с какой головы `origin/dev` собиралось и публиковалось**;
- расхождения свежей базы с grounds наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает.**

## budget

Один engineering root. Половина дня. Числовая оценка, жеребьёвка, произвольные предикаты из данных,
персистентность между запусками, зрение, Odin и редакторский UI сюда НЕ входят. Не помещается даже
после разреза — STOP HOME с одним точным blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-react-data-2-001-call.md
