# CALL: реакция выбирается по важности, а не по порядку в списке

CALL c-exec-g-5a7c-priority-1-001

to: executor
direction: indie-game-development
play: work
node: g-5a7c
bet: bet-g-5a7c-wave-8
track: ядро
for: t-priority-1
repo: C:\projects\Unity\GasCoopGame_win-u2
basis: origin/main @ 782349bd — перемерить при старте

## ПРОЧИТАТЬ ДО ПЕРВОЙ СТРОКИ

- `knowledge/how-the-householders-plan-is-built.md` — форма плана выбрана владельцем, **не
  пересматривается**. Таблица разреженных строк остаётся арбитром.
- `knowledge/core-is-extended-never-rewritten.md` — тест «ДОБАВИТЬ или ПРАВИТЬ».
- `knowledge/what-the-householders-memory-actually-does.md`.

## goal

**Строка, дописанная в профиль, может выиграть у встроенной — по названной важности, а не по месту
в списке. И видно, из чего он выбирал.**

## почему это первое во всей волне

Перемерено домом: `Core/Householder/HouseholderDecision.cs:552-562` идёт циклом **ПО ИНДЕКСУ** и
возвращает первое совпадение; слова «приоритет» в этом цикле **нет ни разу**. Профиль при этом
дописывает строки **в конец**.

**Значит любое правило, добавленное до этой починки, проигрывает беззвучно — без ошибки, без строки
в логе.** Владелец собирается проверять гипотезу о хозяине на новых правилах. Без этой починки он
получил бы ложный ответ и не узнал бы об этом.

## done_when

1. **ВЫБОР ПО ВАЖНОСТИ.** Реакция выбирается по названной важности, а не по порядку в списке.
2. **СТРОКА ПРОФИЛЯ МОЖЕТ ВЫИГРАТЬ У ВСТРОЕННОЙ.** Доказать тестом.
3. **УМОЛЧАНИЕ НЕ МЕНЯЕТ СЕГОДНЯШНЕЕ ПОВЕДЕНИЕ, БИТ В БИТ.** Профиль без явной важности ведёт себя
   как раньше. **Это и есть приёмка: в игре ничего не изменилось.**
4. **КАНДИДАТЫ ВЫХОДЯТ НАРУЖУ.** Пайплайн отдаёт наблюдателю, **какие строки соревновались за этот
   тик, какая победила и чем именно** — важность, имя, невыполненное условие у проигравших.
   **Рисовать это НЕ надо** — рисует полоса `полигон`. Твоя часть — открыть, а не показать.
   Причина, по которой это входит сюда: форма плана решена так, что хозяин каждый тик выбирает
   заново, и **без списка кандидатов его поведение — чёрный ящик**; а файл, в котором это
   открывается, твой, и отдавать его второй полосе нельзя.
5. **ТЕСТЫ ЗЕЛЁНЫЕ.** Их **640** на `782349bd`; другое число — сказать.

## что НЕ входит

- Метки о вещах и дело с результатом — следующие задачи этой же полосы, по очереди.
- Никакого нового поведения. Ноль изменений в игре — это условие, а не побочный эффект.

## boundaries

**ВЛАДЕЕТ:** `Core/Householder/**`, `Profiles/HouseholderProfileLoader.cs`,
`StreamingAssets/TunnelCrew/Householder/*.json`.

**НЕ ТРОГАЕТ:** `World/SceneHousePlan.cs`, `Core/House/**`, `World/HouseBuilding/*.house.json` —
полоса `дом` пишет туда прямо сейчас. `Editor/HouseholderObservation*`, `Scenes/HouseholdLab.unity` —
полоса `полигон`.

## return

Домой: коммиты; чем доказано, что умолчание не изменило поведение; **какой формой кандидаты выходят
наружу** — это нужно полосе `полигон` дословно; число тестов.

## budget

Полдня.

END_OF_FILE: live/indie-game-development/work/2026-08-29-call-priority-1.md
