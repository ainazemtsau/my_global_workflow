# CALL — c-work-g-5a7c-sight-1-binding-g5-001

to: session
direction: indie-game-development
track: householder-life
play: work
node: g-5a7c
task: t-sight-1
issued: 2026-08-13 by s-work-g-5a7c-sight-1-checkpoint-001
status: ready

**ЭТО СВЕЖАЯ БИНДИНГОВАЯ ПРОВЕРКА В ДРУГОМ ФИЗИЧЕСКОМ ЧАТЕ (G5).** Светлое закрытие по этой задаче
объявлено НЕДОСТУПНЫМ заранее, в самой карточке: строки 1, 2 и 6 судит глаз владельца. Внутрисессионный
субагент её не заменяет. RESULT прямо называет venue.

**ТВОЯ РАБОТА — ПОПЫТАТЬСЯ ОПРОВЕРГНУТЬ, а не подтвердить.** Отчёт исполнителя, зелёные ворота и слова
владельца, пересказанные исполнителем, — это входные улики, а не закрытие.

## goal

По семи строкам `done_when` карточки `t-sight-1` вынесен биндинговый вердикт на ОПУБЛИКОВАННОЙ голове:
либо все семь выдержали свежую попытку опровержения и задача закрывается, либо назван точный блокер без
завышенного вывода.

## context

**Состояние Направления:**

- `live/indie-game-development/cards/t-sight-1.md` — **семь строк `done_when` и они закон.** Наряд
  исполнителю сжимал их в три; закрывается задача по СЕМИ.
- `live/indie-game-development/cards/bet-g-5a7c-wave-3.md` — §`evaluator` (буква закрытия),
  §`kill_by` порог 1, §`riskiest_assumptions` допущение 1.
- `live/indie-game-development/work/c-exec-g-5a7c-sight-1-001-call.md` — выданный наряд и его границы.
- `live/indie-game-development/history/2026-08-13-s-work-g-5a7c-sight-1-checkpoint-001.md` — приём
  возврата этой ногой и что именно она перемерила сама.

**Продукт читается в `C:/projects/Unity/GasCoopGame`. Перемерено 2026-08-13:**

- `origin/main` = `origin/dev` = `c485b30e704b1706675dd92d15c5223b0d166b92`. **Зрение ОПУБЛИКОВАНО.**
- база наряда: `4fc04b4c93a097e479a1ce33f47ee55bd3915c67`, дерево `2b18de9fd00969ab74b8a3f5fd4eeb2d0e47923d`.
- цепь: `93b08c3a` (F, первая сборка) → `bf9dadb3` (R, настоящий revert) → `c569b97f` (S, reapply) →
  `c4ee0fc1` (правка после прогона владельца) → `86804752` (запись его прогона) → `ec1934de` (отметка
  о доставке на `dev`).
- отчёт: `origin/main:docs/results/c-exec-g-5a7c-sight-1-001.md`.

**Что уже перемерила нога-отправитель — перепроверь, а не переписывай:** `WorldSituationKind` = `None 0,
ContactedHouse 1, Landed 2, SawRat 3` (добавление, прежние члены не тронуты); ранг зрения `2.5` в
`WorldSituationNoise.cs:186-194`; `HouseholderSightSettings.asset` несёт `_range 6.5`,
`_sectorDegrees 100`, `_bodyPointCount 5`, `_seenFactLifetimeSeconds 1.5`, `_eyeHeight 1.65`,
`_ratBodyHeight 0.16`, `_ratBodyRadius 0.06`, `_drawSightGizmos 1`; все файлы зрения присутствуют на
`origin/main`.

**Заявления отчёта, которые НЕ проверены Направлением и являются твоей работой:** прогон `246 → 251 →
252`; честность луча и перехват настоящим коллайдером; правило «видно при `floor(N/2)+1`»; авторитетность
направления взгляда (последний ненулевой пройденный сегмент `HouseWalk`, не Transform и не презентация);
что потребитель не изменился; что координата игрока не приходит после потери цели; что откат и reapply
сошлись деревьями; что ручки читаются живьём во время игры.

## boundaries

- **Продукт только читать.** Не редактировать, не сливать, не публиковать, не двигать refs, не арендовать
  слоты и не менять slot-state. Все четыре слота сейчас `AVAILABLE`, `lease none` — оставь их такими.
- **СЛОВА ВЛАДЕЛЬЦА ПОЛУЧЕНЫ 2026-08-13 И ЛЕЖАТ В ЖУРНАЛЕ `t-sight-1`. Дословно: «Подтверждаю зрение
  своими глазами»**, вместе с его же оговоркой про дрожь. Цитируй ОТТУДА, а не из
  `docs/results/...`: там вердикт записан ИСПОЛНИТЕЛЕМ в пересказе, и пересказ подписью не является.
  Больше ничего из его слов не выводить: сказанное про зрение — это всё, что сказано.
- **НОВОГО ГЕЙТА НА ЕГО ГЛАЗ НЕ ВЫДУМЫВАТЬ.** Он уже трогал. Требовать второй прогон можно только если
  байты противоречат записанному вердикту.
- **ДРОЖЬ В ПОГОНЕ НЕ ЯВЛЯЕТСЯ КРИТЕРИЕМ ЭТОЙ ЗАДАЧИ.** Ею нельзя ни завалить задачу, ни промолчать
  про неё: она зарегистрирована `i-householder-chase-path-jitters-in-doorways-001` вместе с
  дословными словами владельца и его гипотезой. Проверь, что формулировка улики верна байтам, и всё.
- **ПРО ПЛИТУ И НЕПРЕРЫВАЕМОЕ ЗАНЯТИЕ — НЕ ПОДНИМАТЬ ВОВСЕ.** Владелец 2026-08-13 снял сам вопрос:
  «мне не надо, чтобы была такая конкретная реакция… Это я приводил пример, у нас нет плиты, нет»;
  «Сейчас как бы по мне нормально, что когда хозяин видит игрока, мышь, он за ним бежит». Решение
  `d-sight-overrides-the-hot-stove-001` и улика
  `i-safe-interruption-collapses-walking-and-delicate-phase-001` закрыты его словом и лежат в
  `cards/closed/`. Не переоткрывать и в вердикт не тащить.
- **Самописный сканер как улика поведения запрещён.** Инструмент недоступен → СТОП домой.
- Не трогать `t-loot-1`, её замороженный кандидат и ветку `preserve/...` — другая дорожка, своя нога.
- Задачу `t-occupations-1` не переоткрывать: её строка 3 выполнена буквально, неверна была журнальная
  запись про плиту, и она уже исправлена уликой.

## done_when

1. Все семь строк `done_when` карточки `t-sight-1` перемерены на `c485b30e` попыткой опровержения, и по
   каждой названо, ЧЕМ она закрыта: байтами (путь, строка, значение), прогнанной проверкой (команда и
   вывод) или словами владельца (дословно и где они лежат).
2. Прогон Core перемерен своей рукой: команда
   `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release`, число названо и
   сопоставлено с базовыми **246**. Заявленные `252` подтверждены или опровергнуты.
3. Строка 3 карточки — «ядро только расширяется» — проверена как ФОРМА, а не как обещание: назван
   потребитель, который НЕ изменился, и показано, чем это доказано.
4. Строка 5 — «магического знания нет» — проверена попыткой опровержения: найден или не найден путь, по
   которому позиция игрока попадает хозяину после потери цели.
5. Строка 7 — откат — перемерена по хешам и деревьям своей рукой, а не пересказом отчёта.
6. Вердикт по порогу смерти 1 ставки назван прямо: сработал он или нет, и на каком основании. Порог
   звучит «он потрогал и сказал: хозяин либо слепой, либо читер» — если он этого не сказал, порог не
   сработал, и это надо записать, а не подразумевать.
7. RESULT даёт ОДИН вердикт. PASS закрывает `t-sight-1` и очищает этот CALL; FAIL/CHECKPOINT оставляет
   задачу `active`, называет один точный блокер и одно продолжение. Все остальные дорожки, наряды,
   задачи и улики сохраняются нетронутыми.

## return

Полный RESULT по `work`: PASS или CHECKPOINT; **матрица по семи строкам** карточки с точными
SHA / путь:строка / вывод проверки / дословными словами владельца; явное `binding fresh physical
session`; вердикт по порогу смерти 1; state delta и ровно один локальный handoff.

budget: одна свежая физическая сессия

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-sight-1-binding-g5-001-call.md
