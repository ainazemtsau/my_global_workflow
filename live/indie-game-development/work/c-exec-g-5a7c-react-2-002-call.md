# CALL — c-exec-g-5a7c-react-2-002

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: householder
task: t-react-2
issued: 2026-08-11 by s-work-g-5a7c-react-2-dispatch-003
status: ready
basis: `5fe0dadbc7aa54751e7dd267d670a2de3da85746`, дерево
       `0be95abe9bbac925c4e29f9d1f070ee6c17b21bd` — `origin/dev` = `origin/main`, перемерено
       `git ls-remote origin` перед выпуском. Это состояние содержит закрытую `t-react-mem`.

**ЕСЛИ `origin/dev` СДВИНУЛСЯ — НЕ СТОП, А ПЕРЕБАЗИРОВАНИЕ, и это изменение против прошлого наряда.**
Параллельная дорожка `cargo-hands` работает прямо сейчас (WIN-U1, аренда
`c-exec-g-5a7c-settings-1-001:BUILD`) и публикуется часто; базис протухал дважды подряд. Клауза 5
`evaluator` этой волны и так требует собирать вместе с ТЕКУЩЕЙ головой другой дорожки. Поэтому:
взять свежую голову `origin/dev`, собрать на ней, **назвать её в возврате отдельной строкой**.
СТОП домой — на всё ОСТАЛЬНОЕ неожиданное (чужая аренда, не-AVAILABLE, расхождение дерева).

Пин: `engineering_contract: 36` против продуктового `synced_contract_version: 36`. Re-sync не нужен.

## slot

**WIN-U2**, каталог `C:\projects\Unity\GasCoopGame_win-u2`, ветка `slot/win-u2`.
Прочитано файлом состояния перед выпуском: `lifecycle: AVAILABLE`, `lease: none`,
`mcp-endpoint: unrecorded`. WIN-U1 — `CLAIMED` чужой арендой, не выбирать.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-react-2-002:BUILD`
2. `git fetch origin`, встать на свежую `origin/dev`, readback дерева, `git status` пуст.

Unity для этой задачи НЕ нужен: работа ограничена engine-free `Core` и headless-тестами. Мост
WIN-U2 существует и отвечал ранее по `http://localhost:27497/p/41ebd445`; поле `unrecorded` в файле
состояния — устаревшая запись, а не отсутствие моста. Понадобится Unity — записать endpoint и
live-проверить, что он отвечает именно как WIN-U2.

## goal

**У хозяина появляется ТОЧКА РАСШИРЕНИЯ: обработчиков МНОГО, новый добавляется не трогая ядро, и
обработчик умеет переключить ВЕТКУ поведения, а не только выдать одну реакцию.**

Прямые слова владельца, на которых стоит задача: «запускается определённая ветка поведения… он ходит
просто по дому, а при определённых совпадениях он становится более агрессивно ходить, или в одной
комнате сторожить»; «мы будем делать pipeline… отдельный ассистент, который будет днями сидеть
писать эти хендлеры»; «есть дефолтное поведение, но на какие-то мы будем писать… обрабатываем только
те комбинации, которые нам интересны».

## context — перемерено чтением байтов на `5fe0dadb`

### Шов уже есть и он хорош — не хватает МНОЖЕСТВЕННОСТИ

`IHouseholderDecisionPolicy` (`Householder.cs:856-860`) — **один метод**:
`HouseholderDecisionProposal Evaluate(HouseholderDecisionSnapshot snapshot)`.

Снимок уже несёт всё, что нужно обработчику (`HouseholderDecisionSnapshot`, `:668`):
`Intention`, `NoticedFact` (производное = первый факт), **`NoticedFacts` списком**,
`NoticedFactCount`, `HasListeningPoint`, `ListeningPoint`, `CurrentPosition`, `ActiveResponse`,
`Reactions` (каталог), плюс `TimesNoticedAt(...)` — НАКОПЛЕНИЕ, закрытое `t-react-mem`.

Политика ставится на существующий объект: `AuthoritativeHouseholder.DecisionPolicy`
(`AuthoritativeHouseholder.cs:143-154`), по умолчанию singleton, `null` возвращает singleton.

**ЧЕГО НЕТ: обработчик ровно ОДИН.** `DecisionPolicy` — одиночное свойство. Написать второй
обработчик можно, включить одновременно с первым — нельзя. Реализаций сегодня две
(`HouseholderReactionDecisionPolicy` `:862`, `SuspiciousNoiseDecisionPolicy` `:962`) плюс три
тестовых двойника, и все они конкурируют за одно место.

**Это и есть предмет строки 1: композиция многих плюс ДЕФОЛТ плюс названное правило разрешения.**

### Чем обработчик может ответить сегодня — и почему ветку он включить не может

`HouseholderDecisionProposal` (`Householder.cs:774`) несёт: `IsPresent`, `Reaction` (строка
каталога), `Target`, `FactOccurrenceId`, и производные от строки `Occupation`, `Priority`,
`PhysicalActionName`, `Cause`, `MovementPace`, `ResponseKind`.

**То есть предложение — это ОДНА реакция и её цель. Поля «включить такой-то режим» нет.**

Распорядок хозяина — ОДНА циклическая лента: `_routineStepIndex` идёт
`(_routineStepIndex + 1) % _profile.RoutineStepCount` (`AuthoritativeHouseholder.cs:317`),
профиль подставляется один раз при посадке (`:231-233`). Второй ленты не существует.

**НАЗВАНО ЗАРАНЕЕ И ЧЕСТНО: строка 2 — единственное место этой задачи, где ядро ПРАВИТСЯ, а не
дополняется.** Правка стоит трёх точек: поле в `HouseholderDecisionProposal` (у него ТРИ
конструктора, `:776`, `:797`, `:822`), его потребление в `Step`, и переключение ленты в
`AuthoritativeHouseholder`. Платится ОДИН раз, и после неё новая ветка — данные.

### Форма строки реакции — самое дорогое, что осталось

`HouseholderReaction` — **14 публичных полей**, и `new HouseholderReaction(` встречается **7 раз**:
шесть в `HouseholderReaction.cs` (пять производственных строк плюс проекция) и один в
`HouseholderReactionDataTests.cs`.

**Значит новое поле строки стоит СЕМИ правок.** Это вторая по дороговизне форма ядра; первую
(`HouseholderLocalMemory`) `t-react-mem` починила до нуля правок за поле.

**ТРЕБОВАНИЕ ВЛАДЕЛЬЦА К ФОРМЕ, вписанное в карточку и обязательное здесь: ВЕС строки объявляется
ВЫЧИСЛЯЕМЫМ ОТ СОСТОЯНИЯ, а не целочисленной константой.** Обоснование —
`knowledge/householder-architecture-is-a-sparse-rule-table.md`: числовая оценка есть НАДМНОЖЕСТВО
таблицы правил, и разница между ними ровно одна — чем является вес. Константа означает, что переход
к оценке потребует переписать КАЖДУЮ строку, то есть ровно то, что он запретил. Вычисляемый вес
означает, что переход есть ДОБАВЛЕНИЕ.

Его пример — «хозяин уже туда ходил, он знает, поэтому БЕЖИТ, а не идёт» — при вычисляемом весе не
требует второй строки-дубля: та же строка считает свой вес из `TimesNoticedAt`.

**Задача НЕ строит числовую оценку.** Все веса на выходе могут быть постоянными; важно, что форма
это допускает. И если ты добавляешь поле в строку реакции — плати семь правок ОДИН раз и сделай так,
чтобы СЛЕДУЮЩЕЕ поле стоило меньше.

### Что уже умеет файл, и что в нём — заготовка

`Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json` — 20 строк, только
распорядок из двух шагов. Реакций в нём нет. Разборщик знает ДВА умения
(`HouseholderProfileLoader.cs:43,46`), остальное отвергает.

**Две заготовки под требуемое реальны и не выдуманы:** поле `"when": "always"` в каждом шаге
распорядка — место под УСЛОВИЕ; и намеренно пустой `IsKnownPlace` с комментарием, что его подключит
будущая задача, — место под МЕСТА ПО ИМЕНИ.

### Что уже зелёное

`5fe0dadb` даёт **179 passed, 0 failed, 0 skipped** — прогнано Направлением на этой самой голове, на
выгруженном дереве.

## РАЗРЕШЁННАЯ ЛИНИЯ РАЗРЕЗА — резать без возврата за словом

Задача крупная, и линия названа заранее. **Строка 1 (множественность обработчиков, дефолт, арбитр,
вычисляемый вес) остаётся здесь ВСЕГДА.** Если не помещается в полдня:

- **сначала уезжает АВТОРИНГ ДАННЫМИ**: обработчик остаётся написанным классом, а описание строк и
  условий в JSON становится своей задачей. Форма обработчика при этом обязана остаться такой, чтобы
  данные легли на неё ДОБАВЛЕНИЕМ;
- **потом уезжает СТРОКА 2 (ветка поведения)** своей задачей со своим откатом.

Обратный разрез ЗАПРЕЩЁН: строить ветку поведения раньше множественности бессмысленно — включать её
будет некому. **Что срезано — называется в возврате, а не умалчивается.**

## boundaries — что НЕ трогать

- **НЕ ЗАКОЛАЧИВАТЬ ДВЕРЬ** (`knowledge/core-is-extended-never-rewritten.md`). По каждому месту,
  которое задача открывает, в возврате называется результат теста **«чтобы добавить сюда новое —
  ДОБАВИТЬ или ПРАВИТЬ?»**. Не назвал — правило не применено.
- **АРБИТР ОБЪЯВЛЯЕТСЯ ЯВНО И ОДИН.** Правило разрешения между двумя подошедшими обработчиками —
  названная вещь в одном месте, а не порядок файлов, не порядок регистрации и не «первый попавшийся».
  Жеребьёвка и случайность в эту задачу НЕ входят: владелец решения по ним не давал.
- **Каталог пяти производственных реакций не меняется по содержанию** — те же поводы, приоритеты,
  занятия, времена жизни. Меняться может ФОРМА строки, не её значения.
- **Файлы дорожки `cargo-hands` — чужие:** `Core/Cargo/**`, `Network/CargoBody.cs`,
  `Network/NetworkPlaySettings.{cs,asset}`, тесты `Cargo*`, `TelekineticPickup*`,
  `FirstPersonCarry*`. Упёрся — СТОП домой.
- **Публичные сигнатуры `CarryWorld.Advance` не меняются** (обе перегрузки, `CarryWorld.cs:97`,
  `:115`) — их зовут тесты чужой дорожки и `NetworkWalkerCourier.cs`.
- **Пороги `60 / 150 / 400` не двигаются**, спецслучая под волочение нет.
- `IntegratedHouse.unity` не редактируется. `ProjectSettings/**`, `Assets/GasCoopGame/**` закрыты.
- **ЗАПРЕЩЁННЫЕ ДОВОДЫ, снятые владельцем дословно:** «сочетаний получится 2^N» — не принимается,
  он пишет только интересные ему; «столько-то тестов сломается» — не принимается, тесты забота
  исполнителя, а не граница требования.
- Самодельный сканер исходников уликой не является и не пишется.

## owner-visible check

**Владелец эту задачу руками НЕ судит.** Его вердикт по дорожке назначен на `t-react-3`, в самом её
конце, нарочно — чтобы он не судил два недоделанных вечером одного дня.

Поведение при этом ОБЯЗАНО измениться там, где включён новый обработчик, и не измениться нигде
больше. Писать «поведение не изменилось» целиком — запрещено.

## headless evidence

- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`. База — `179 passed` на `5fe0dadb`.
- **ГЛАВНЫЙ ГЕЙТ — ДВА ОБРАБОТЧИКА, НАПИСАННЫЕ КАК ИХ БУДЕТ ПИСАТЬ АССИСТЕНТ.** Первый — на
  СОЧЕТАНИЕ поводов; второй — использующий НАКОПЛЕНИЕ (`TimesNoticedAt`), потому что владелец назвал
  этот случай дословно. Оба въезжают **без единой правки ядра, решателя, перечислений и друг друга**
  — предъявляется ДИФФОМ. Выключение любого не ломает остальные и не ломает дефолт.
- **Гейт конвейера:** два обработчика, добавленные как две независимые пачки, не конфликтуют, и
  порядок их разрешения объясняется НАЗВАННЫМ правилом, а не порядком файлов.
- **Гейт вычисляемого веса:** показать тестом, что вес одной строки МОЖЕТ зависеть от состояния
  (например от `TimesNoticedAt`), даже если в игре все веса постоянны.
- **Гейт дефолта:** когда ни один обработчик не подошёл — поведение как на базе, доказано
  существующими зелёными тестами.
- **Зелёные ворота поведением игры не являются**
  (`knowledge/green-gates-do-not-compile-the-game.md`).

## откат

- **Точка возврата — ГОЛОВА ДОРОЖКИ на момент старта задачи** (свежая `origin/dev`), а не константа
  из этого наряда: базис протухал дважды подряд, а откат, привязанный к голове, выжил.
- Откат назвать ОДНОЙ командой и **исполнить настоящим revert**, как это сделано в `t-react-mem`:
  после него `git rev-parse <ветка>^{tree}` возвращает дерево той головы знак в знак.
- Прямо заявить, что вне репозитория для отката ничего не нужно.

## done_when

1. **Обработчиков МНОГО, новый добавляется не трогая ядро, и арбитр назван.** Вместо одиночного
   `DecisionPolicy` — композиция обработчиков плюс ДЕФОЛТ, отвечающий когда не подошёл никто.
   Правило разрешения между двумя подошедшими — одна названная вещь, и **вес строки объявлен
   ВЫЧИСЛЯЕМЫМ от состояния, а не константой**. Доказано ДВУМЯ обработчиками, написанными как их
   будет писать ассистент — один на сочетание поводов, второй на накопление, — которые въезжают без
   единой правки ядра, решателя, перечислений и друг друга, и выключение любого не ломает остальные.

2. **Обработчик умеет переключить ВЕТКУ поведения.** Ветка — названная вещь; включённая ветка
   меняет то, чем хозяин занят, а не только одну реакцию; возврат из неё к обычной жизни работает и
   доказан тестом. Правка ядра здесь ОЖИДАЕМА и названа заранее — предложение, его потребление и
   переключение ленты распорядка; в возврате называется, во сколько точек она обошлась.

3. **Дефолт по метаданным, границы целы, откат проверен.** Когда обработчик не подошёл — поведение
   как на базе, и на что реагирует дефолт, решают метаданные события (громкость числом, место,
   время). Пять производственных реакций сохранены по СОДЕРЖАНИЮ. Границы целы — сцена, файлы
   `cargo-hands`, обе публичные сигнатуры `CarryWorld.Advance`, пороги, `ProjectSettings/**` — не
   тронуты, доказано диффом. Откат назван одной командой, исполнен настоящим revert и проверен
   readback дерева.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / householder / t-react-2`.

**ПЕРВОЙ СТРОКОЙ — РАСПИСКА:** число полученных строк `done_when` (их три).

Также обязательно:

- **тест «ДОБАВИТЬ или ПРАВИТЬ» по КАЖДОМУ открытому месту** — правило владельца;
- **какой формой описан обработчик** (класс, строка данных, оба) и почему именно так под конвейер
  ассистента; сколько мест занимает ОДИН новый обработчик;
- **как назван арбитр** и что происходит при двух подошедших с равным весом;
- **чем выражен ВЫЧИСЛЯЕМЫЙ вес** и тест, который это показывает;
- **во сколько точек обошлась правка ядра под ветку поведения** — числом;
- **резал ли по разрешённой линии** — да или нет, что именно уехало и почему;
- **сколько правок стоит теперь новое поле строки реакции** — было семь;
- проверенный откат: команда и readback дерева;
- терминальный readback селектора `CLEAN / AVAILABLE / lease none`;
- **отдельной строкой: с какой головы `origin/dev` собиралось и публиковалось**;
- расхождения свежей базы с grounds наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает.**

## budget

Один engineering root. Половина дня на строку 1; строки 2 и 3 внутри неё, если помещаются, иначе
режутся по НАЗВАННОЙ линии. Числовая оценка, жеребьёвка, долговременная память как система, зрение,
Odin-редактор и межзапусковое сохранение сюда НЕ входят. Не помещается даже после разреза — STOP
HOME с одним точным blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-react-2-002-call.md
