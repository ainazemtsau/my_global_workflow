# CALL c-exec-player-puppetmaster-p2a0-002 — trustworthy live-source authority proof (PLAN ONLY)

> **FIRE-READY PLAN-only continuation.** Run in a fresh owner-present `GasCoopGame_lab` product PLAN session.
> The session ends after the owner-readable technical plan is explicitly approved. Independent RED authoring,
> BUILD, Unity asset/runtime mutation and evidence capture belong only to later fresh sessions.

to: executor
direction: indie-game-development
node: g-9c41
task: M1-P2a0
repo: C:\projects\Unity\GasCoopGame_lab
kind: engineering
change_id: c-exec-player-puppetmaster-p2a0-002
runs_in: fresh owner-present product PLAN session only
source_checkpoint: lab@0a54c884; c-exec-player-puppetmaster-p2a0-001 CHECKPOINT / NOT DELIVERED
planning_reference: product origin/main@a644e5db observed 2026-07-11; re-read all refs and Editor identity at start

## goal

An owner-approved implementation contract exists for a trustworthy live-source PuppetMaster authority proof, so a
later fresh BUILD can distinguish A, B, C or asset rejection without accepting self-reported state as evidence.

## context

- Owner explicitly continues the investigation after the failed first harness:
  «то, что сейчас был плохой результат, не значит, что нам не надо дальше проводить исследования».
- Read first:
  - `docs/results/c-exec-player-puppetmaster-p2a0-001.md`;
  - `docs/reviews/review-c-exec-player-puppetmaster-p2a0-001.md`;
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-001/PLAN.md`;
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-001/specs/puppetmaster-authority-spike/spec.md`;
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-001/tasks.md`;
  - `docs/adr/ADR-E-0010-c-exec-player-puppetmaster-p2a0-001-puppet-authority-spike.md`.
- Trusted checkpoint facts: exact PuppetMaster 1.5 SHA-256
  `03E126E93BA44BE178DD102A7A318A0F8207350C9640068B903307D1C0AFBED3`; Unity `6000.3.17f1`;
  default-assembly import compiled; typed PuppetMaster objects instantiated; 65/65 negative controls passed.
  The official nested asmdefs produced 17 missing `Unity.InputSystem` reference errors and were rolled back.
- Untrusted checkpoint facts: no accepted A/B/C payload, transition trace, contact sheet, clip or owner verdict.
  The dirty vendor/harness snapshot is forensic evidence only. Its self-reported fields cannot be reused as a
  behavior oracle.
- The old change folder, ADR, RESULT, G5 and independently authored acceptance suite are immutable history.
  New planning artifacts and future tests use the `c-exec-player-puppetmaster-p2a0-002` identity; do not revise
  history to make the first attempt appear successful.
- Existing owner-approved scope remains: exact imported `Puppet Extended` / `Pilot Root`; input-free synthetic
  driver; matched A/B rock plus point impulse; bounded C kill-probe; PuppetMaster 1.5 in default assembly; runtime
  collision containment; multiplayer inventory only, with a separate two-peer proof mandatory if B or C survives;
  checkpoint research only, no product delivery.
- Candidate A keeps controller/capsule root authoritative. Candidate B performs one explicit
  controller→pelvis→controller ownership cycle. Candidate C is a bounded full-physics controllability kill-probe.
- This is continuation of the existing M1-5/P2a0 allocation, not a new product leg and not permission to extend the
  later production-controller scope.
- Contract v19 applies: PLAN and BUILD are different sessions. PLAN ends at owner approval and commissions no RED
  tests. The later independent test-author and builder are also separate from one another.

## boundaries

- **PLAN ONLY:** no production code, helper, acceptance/RED test, prefab, scene, asset import, package, layer,
  Physics setting, harness cleanup, runtime capture or delivery evidence may be created or edited in this session.
  Planning documents/spec/ADR/tasks for the new change are the only allowed product-repo writes.
- Preserve `openspec/changes/c-exec-player-puppetmaster-p2a0-001/**`, ADR-E-0010, its evidence documents and its five
  independently authored acceptance files byte-for-byte. New tests later are additive under the `002` identity.
- Do not reset, clean, stash, stage or commit the current dirty forensic vendor/harness snapshot. If an
  explicit-path planning-only commit cannot be made safely, STOP with the exact git blocker.
- Do not choose A, B, C or rejection from the old payload. The PLAN designs how a later run can make that verdict.
- No Core, FishNet, product controller, damage, health/incapacitation, gas consequence, final rig tuning, final
  camera/collider feel, two-peer BUILD, main/dev/dev2 mutation, product merge or `-Deliver`.
- Unity MCP is first choice for every capable live-Editor read. Do not ask the owner to operate Unity pre-emptively.
  Ask for computer-use/manual setup only after a concrete MCP/Editor/UI blocker exists; name the unavailable tool
  and give exactly one bounded click-by-click action. Static source scans never substitute for live behavior.
- The Planner may specify the later RED→BUILD→G5 handoff but may not run it or author its BUILD CALL. Direction OS
  decides the downstream route after the approved PLAN returns.
- If the evidence requirements cannot fit the research cap without weakening a restart class, STOP with the exact
  conflict and 2–3 owner-readable options; never revive the rejected self-reporting shortcut.

## done_when

1. Fresh PLAN preflight records lab HEAD/status, current origin/main, Editor/project identity, MCP availability,
   exact imported plugin identity and the disposition of every dirty forensic path. It separates trusted checkpoint
   facts from rejected claims without mutating the lab.
2. One detailed but simple owner-readable PLAN compares credible evidence architectures, selects one and explains
   every technical decision and why: observation source, recorder ownership, immutable run identity, write-once
   payload, source-vs-recorded independence, per-FixedUpdate sampling, reset/stimulus provenance, frame/capture
   binding and failure/cleanup behavior.
3. The frozen spec maps every G5 restart class to explicit acceptance properties, negative controls and later live
   evidence:
   - G5-1 `anti-fabricated-live-evidence`: exactly one immutable run-id-bound payload; recorded values and
     independently read source values cannot originate from the same self-report; later tests cannot overwrite or
     silently select another run.
   - G5-2 `frozen-stand-and-no-input`: package read-back identity and instantiated hierarchy identity are separately
     exact; every enabled device-input reader, including `BallShooter`, fails the run.
   - G5-3 `observed-trajectory-and-authority`: real Unity `FixedUpdate` samples with no fabricated numbering or
     skipped updates; positions, owner flags, epochs, camera/collider consequences and B ownership edges are
     derived from live objects. B has two reset-separated, uniquely keyed subtraces: rock and point impulse.
   - G5-4 `matched-fixture-and-capture`: settled reset fingerprints, stimulus provenance, actual rock contact,
     independent point impulse, matched A/B fixtures and run-id-bound clips/contact sheets.
   - G5-5 `candidate-c-controllability`: gravity-only/drift is a failing negative control; C may survive only on
     measured commanded response and bounded error reduction while physical ownership remains continuous.
   - G5-6 `collision-mutation-atomicity`: validation/constructor failure cannot strand the global collision matrix;
     before/during/after state and restoration on every exit path are real run evidence.
4. The PLAN freezes the later sequence without executing it:
   a fresh independent test-author reads only the frozen `002` spec and authors genuinely failing additive tests
   plus negative controls; a different fresh builder makes those immutable tests pass; real MCP EditMode/PlayMode
   runs produce the payload and captures; normal non-deliver checks run; then a new fresh G5 tries to refute every
   mechanism and multiplayer inference. Only after that does the owner receive the final A/B/C/reject verdict.
5. The plan defines the owner-interaction boundary: MCP/automation first; computer-use or one manual Unity action
   only for a named UI-only/tool-unavailable blocker; after the action, automation resumes. Owner-eye reviews only
   the final two short comparable clips and never substitutes for recorded behavior.
6. The future research BUILD is capped at one focused half-day. A setup/import/rig/evidence-infrastructure blocker
   consuming 90 minutes triggers a checkpoint before any fidelity cut. The PLAN session itself is capped at
   90 minutes.
7. New frozen planning artifacts exist under
   `openspec/changes/c-exec-player-puppetmaster-p2a0-002/` with an owner-readable `PLAN.md`, proposal, frozen spec
   including spec-silence/negative-control coverage, machine ledger and a new change-id-bearing `ADR-E-*`.
   They explicitly preserve the old change as immutable failed evidence.
8. The owner’s exact approval words/token and the planning-only commit are returned. The diff contains only new
   `002` planning artifacts: no code, test, prefab, scene, vendor asset, package, settings, capture, RESULT or BUILD
   evidence. The session then STOPS.

## return

Return the PLAN handoff: fresh repo/Editor/MCP facts; selected and rejected evidence architectures; owner-readable
PLAN path; frozen spec/proposal/tasks/ADR paths and commit; the six-class acceptance/negative-control table; exact
independent test-author → fresh builder → MCP runs → fresh G5 handoff; owner-interaction triggers; timebox/STOP
conditions; exact owner approval words; and proof that no RED/BUILD/runtime/product work occurred. Do not author the
BUILD CALL in the product repo.

## budget

One owner-present PLAN session, maximum 90 minutes. The plan may reserve one later fresh research BUILD of at most
one focused half-day, with a 90-minute setup checkpoint; that BUILD is not authorized by this CALL.

launch:
  lane: C
  venue: C:\projects\Unity\GasCoopGame_lab (branch lab, Unity Editor 3; PLAN artifacts only)
  machine: PC
  base: lab@0a54c884 checkpoint; product origin/main@a644e5db observed; re-read, do not reset dirty forensic state
  conflict_surface: new c-exec-player-puppetmaster-p2a0-002 planning artifacts only
  depends_on: []
  merge_queue: none — disposable research, no product merge
  gates: actual owner PLAN approval; independent RED, MCP behavior runs and fresh G5 belong to later sessions

END_OF_FILE: live/indie-game-development/work/c-exec-player-puppetmaster-p2a0-001-continuation-call.md
