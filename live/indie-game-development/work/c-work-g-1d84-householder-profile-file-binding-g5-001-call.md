# CALL — c-work-g-1d84-householder-profile-file-binding-g5-001

to: session
direction: indie-game-development
track: хозяин
play: work
node: g-1d84
task: t-host-3
issued: 2026-08-06 by s-work-g-1d84-householder-profile-file-route-001
status: ready

## goal

По exact runtime-кандидату профиля хозяина вынесен биндинговый вердикт: все строки `t-host-3.done_when`
либо выдержали свежую попытку опровержения и готовы к публикации без изменения кандидата, либо
оставлен один точный blocker без завышенного вывода.

## context

Живое состояние и точная задача:

- `live/indie-game-development/NOW.md` — `t-host-3` и этот единственный корень полосы «хозяин»;
- `live/indie-game-development/work/c-exec-g-1d84-householder-profile-file-001-call.md` — исходный
  business `done_when` и границы;
- `live/indie-game-development/history/2026-08-06-s-work-g-1d84-householder-profile-file-route-001.md`
  — Direction-приём возврата, слова владельца и carry-forward.

Продукт читается в `C:/projects/Unity/GasCoopGame`:

- база исходного наряда: `d60c2f04228bdfa161d6e122c56a2c5ccfec89f3`;
- exact runtime-кандидат: `b1ac36749f31cad34a9ab77020641f5baf1a474a`;
- handoff с итоговым принятым отчётом:
  `ae9ce7b15349ac3eb1218446b2872412c27c435f`;
- кандидат является предком handoff; локальная ветка `slot/win-u1` держит handoff;
- после свежего fetch продуктовые `origin/main` и `origin/dev` =
  `8ffe5a5d89398b1dd7bcf450157446f7952f3855` и кандидата НЕ содержат.

Несущие артефакты exact кандидата:

- `Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`,
  blob `fba2242a6801ba815dce4a75bf3b24376de269cd`;
- `docs/householder-profile-format.md`,
  blob `a20ec69c3c5c8a14d1fc4f7388bfe499e4d899ce`;
- итоговый отчёт
  `ae9ce7b1:docs/results/c-exec-g-1d84-householder-profile-file-001.md`,
  blob `9caa0b1d9b3feb9db3b61726ea569df186c463c5`.

Отчёт заявляет: обычный repository JSON и документ формата; unknown-member rejection с путём,
строкой и позицией; атомарное чтение и разрешение; неразрешившийся профиль не сажает и не публикует
хозяина; профиль выбирает порядок, длительность, обстоятельство, способность и непрозрачное место;
host-only загрузку без нового RPC/тика; 77 Core-тестов и repo-native gate зелёными; ручную ПРОБУ
владельца после исправления `b1ac3674`. Это входные утверждения, а не готовый вердикт — каждую строку,
которую можно проверить байтами, надо перемерить на exact SHA.

Дословное слово владельца при возврате домой:
**«c-exec-g-1d84-householder-profile-file-001 завершён и принят владельцем в режиме ПРОБА»**.
Он отдельно обязал сохранить carry-forward: нынешняя пауза — только доказательство файлового профиля;
будущие занятия описываются как намерения («готовить в кухне»), исполняются фазами
«путь → действие → завершение», считают длительность после прибытия и имеют явную политику
прерывания/возобновления. Direction записало это входом в `t-host-6.note`, а не расширением `t-host-3`.

## boundaries

- Продукт только читать: не редактировать, не сливать, не публиковать, не пушить, не двигать refs,
  не арендовать `WIN-U1` и не менять slot-state.
- Проверяется exact кандидат `b1ac3674`; более поздний runtime-коммит требует нового verdict.
- Возврат исполнителя, зелёные проверки и слова владельца — evidence inputs, не автоматическое
  Direction-close. Эта нога сама пытается опровергнуть каждую строку.
- Не требовать нового действия владельца: он уже дал итоговый verdict по ПРОБЕ. Не выводить из него
  больше, чем сказано и чем записано в принятом отчёте.
- Carry-forward не превращать в требования текущего кандидата и не строить сейчас. Не менять
  `t-host-4`/`t-host-5`/`t-host-6`, кроме сохранения уже записанного носителя.
- `t-host-3` не закрывать на непубликованном кандидате. PASS открывает один same-lane continuation
  на публикацию exact кандидата; публикация и последующая reconciliation закрываются отдельной ногой.
- Это binding G5 в новом физическом чате. Внутрисессионный subagent/pre-pass её не заменяет; RESULT
  прямо называет venue.

## done_when

1. На `b1ac3674` перемерено существование текстового профиля и документа формата, отсутствие
   scene/binary carrier и соответствие документа реальным принимаемым полям, типам и зарегистрированным
   именам.
2. Попыткой опровержения перемерено, что неизвестное поле и неразрешившаяся ссылка дают понятную
   ошибку с местом, а атомарный failure не запускает и не публикует хозяина; назван фактический host
   путь загрузки и проверено отсутствие второго authority/RPC/tick.
3. Перемерено, что профиль действительно несёт порядок дел, длительность, обстоятельство,
   способность и действие/место, а длительность применяется к activity после перехода к ней; не
   выведено содержание, которого exact кандидат не имеет.
4. Ручная часть закрыта только exact словами владельца и принятым handoff-отчётом: изменение профиля
   дало другое поведение, сломанное поле назвало место и не запустило хозяина. Если эти receipts
   противоречат байтам или не разрешаются — это blocker, а не догадка.
5. Carry-forward классифицирован как будущий input `t-host-6` и не использован для расширения либо
   отклонения текущей принятой ПРОБЫ.
6. RESULT даёт один verdict. PASS оставляет `t-host-3: open`, очищает этот CALL и открывает один
   same-lane publication continuation для exact кандидата. FAIL/CHECKPOINT оставляет задачу open,
   называет один точный blocker и одно продолжение. Все остальные полосы, calls, tasks и issues
   сохраняются.

## return

Полный RESULT по `work`: PASS или CHECKPOINT; матрица по строкам исходного `done_when` с exact
SHA/path/blob/check/owner-receipt; явное `binding fresh physical session`; state delta и ровно один
локальный handoff.

budget: одна свежая физическая сессия

END_OF_FILE: live/indie-game-development/work/c-work-g-1d84-householder-profile-file-binding-g5-001-call.md
