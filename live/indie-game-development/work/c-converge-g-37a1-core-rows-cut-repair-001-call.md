CALL c-converge-g-37a1-core-rows-cut-repair-001
to: session
direction: indie-game-development
play: converge
node: g-37a1
status_note: WAITING on c-map-g-37a1-level-authoring-001. Items 1-3 below rest on his amended text.
goal: |
  BRING `work/converge-g-37a1-core-rows.md` INTO LINE WITH SIX THINGS, and nothing else: three
  owner rulings of 2026-07-28 (`NOW.md` `d-core-level-authoring-001`) and three findings of the
  cut-check (`history/2026-07-28-s-converge-verify-g-37a1-core-rows-cut-check-001.md`). No owner is
  needed once the map leg has run. Every item is small; none touches a criterion, and the three that
  do rest on text he has by then signed.
context: |
  THE CUT ITSELF HELD. The cut-check confirmed the fifteen criteria still map, that every forced
  parameter, hazard, seam and routed price survived, that the stated cost of the cut is honest, and
  that R1, R6 and R7 are closed. What follows is a short list, not a fourth sweep.

  READ, in this order:
  - `NOW.md` `d-core-level-authoring-001` — his three rulings, verbatim, with reasons.
  - `history/2026-07-28-s-converge-verify-g-37a1-core-rows-cut-check-001.md` — the cut-check, its
    three findings, and §AFTER THE RESULT.
  - the map RESULT that this CALL waits on — the amended criterion 12 and the amended requirements
    line 5, which are what items 1-3 must match.
  - `work/converge-g-37a1-core-rows.md` — the target.
boundaries: |
  NO APPARATUS MAY BE RESTORED OR PROPOSED. A fix that reintroduces a deck, a threshold, a sample
  size, a pass bar or a prescribed outsider sitting is a defect of the repair, not a repair —
  `d-core-acceptance-format-001` is his decision and it governs.

  Do not choose a value for any knob. Do not choose a file format, schema, path or tool for the
  layout file — the property is «an AI can change it on his instructions»; the mechanism is
  `PLAN`/step 4. Do not manufacture an owner question. Do not open a bet, a task or a track.

  NOT REOPENABLE: the live requirement lines; the fifteen criteria, including 13, 14 and 15, which
  he settled («с критериями согласен, их не трогаем»); the eleven closed rulings; §OWNER's three
  answers; R1, R6 and R7's fixes, which the cut-check confirms CLOSED; the five findings of
  §DIED-WITH-THE-CUT (R2-R5, R8); the SETTING rule; and the cut itself.
done_when: |
  FROM HIS RULINGS —

  1. **THE LEVEL BUTTON IS GONE, and everything that hung off it goes with it.** ROW 5 loses the
     load-another-level control from its three in-game controls, leaving the start state and the
     immortality mode. §KNOBS' `rig controls` row loses it. **The requirement that the build ship at
     least TWO authored levels is STRUCK** — it was derived solely from «другой» in the button
     clause, and with the clause gone the derivation is gone; one authored section is enough. **M8
     loses its second half** and asserts only that death returns the section to its pre-run state.
     ROW 12's «PER AUTHORED LEVEL … the build ships more than one level» is restated for one section
     with more permitted. **§SEAM 5 loses the level-load question** — «a level load by one player is
     either a session-wide reload or a split world» is no longer forced by this core; the knob-
     ownership and step-rate questions and the one-value-per-session property STAY. §GLOSSARY
     «уровень» is re-signed for one authored section instance described by its layout file, and the
     base is named among its contents, which closes the cut-check's CUT-1 by dissolution rather than
     by repair. Record in each place that the striking is HIS ruling, so no leg re-derives it.

  2. **THE SECTION'S LAYOUT IS AUTHORED CONTENT IN A TEXT FILE.** §KNOBS' LEVEL-AUTHORING table
     keeps its membership and gains the property his ruling adds: it lives in a text file changed
     between runs in the Editor or by editing the file, never from the running game, in a format an
     AI can change on his verbal instructions and can use to write a second file. **The RIG/
     LEVEL-AUTHORING split itself is UNCHANGED and is confirmed by his words** — «спорные числа»
     stay reachable from the built player without a rebuild; the layout does not. The base's
     placement stays a RIG knob per R1 and its authored default belongs to the layout file. §GLOSSARY
     «настройка» and «спорное число» gain no new property and are not re-signed.

  3. **ROW 16 IS REWRITTEN FOR REAL CO-OP.** Its OBSERVABLE currently under-reads criterion 11 by
     naming three examples; his ruling is that the core is a genuine cooperative game — two players
     on two machines see the same world, each other and how the other moves, the cut geometry, the
     substance and its simulation, everything the game contains. Replace the itemised list with the
     property. Record his architectural instruction — the core is built ready for co-op from the
     start — as a constraint routed to `shape`/`PLAN`, NOT as a row and NOT in the card, on his own
     words: «это в карточку не надо, технические детали запихивать».

  FROM THE CUT-CHECK —

  4. **ROW 6: «дойти до низа» is cut, on his ruling and wider than the finding.** Delete «Two
     players given the same section reach the bottom by cutting in different places» from the
     OBSERVABLE outright — it absorbed a deleted outsider test, lost the word that carried it
     (*forced*), and asserts a fact about players that a correct build can fail.

  5. **ROW 12's AUTHORING CONSTRAINT is restated without «низ».** Its purpose survives and its
     wording does not: the level must never FORCE a breach — there is always somewhere to dig that
     opens no pocket — and it must contain at least one place where going through a pocket costs
     visibly less than going around. Magnitudes stay HOW → `PLAN`. Check the rest of the file for
     any other place that presupposes a bottom to reach; criterion 13 already forbids a completion
     condition, so nothing may depend on arriving anywhere.

  6. **§CHECK's totality claim is scoped.** «Everything below is judged one of two ways and there is
     no third» is contradicted by §WHAT's own decision column for criteria 13 (absence), 14 (records)
     and 15 (direction state) and by ROW 17's CHECK. Scope the sentence to what the BUILD must do.
     Price: zero.

  7. **§WHAT, §ORACLE, §BUILD-CLOSES-BETTER and §SIGNOFF are updated wherever items 1-6 change what
     they summarise** — R6 established that a repair scoped by section cannot reach the sections that
     summarise it, so the roll-ups are IN scope here by construction.

  8. **NO §SIGNOFF** — a converge does not sign its own work.
return: |
  One short `converge` RESULT naming what each of the eight now says, and what the striking of the
  second authored level removes from the price a shape leg carries.
budget: one session, short, no owner. FRESH CHAT, separate from the cut-check and from the map leg.

END_OF_FILE: live/indie-game-development/work/c-converge-g-37a1-core-rows-cut-repair-001-call.md
