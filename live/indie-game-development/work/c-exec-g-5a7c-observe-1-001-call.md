# CALL — c-exec-g-5a7c-observe-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: householder
task: t-observe-1
issued: 2026-08-11 by s-work-g-5a7c-observe-1-dispatch-001
status: ready
basis: `87d399f67b0d9ec34ec97463692d753d590a7f6a` — `origin/dev` = `origin/main`, перемерено
       `git ls-remote origin` перед выпуском. Содержит закрытые `t-react-mem`, `t-react-2`,
       `t-react-data-2`.

**ЕСЛИ `origin/dev` СДВИНУЛСЯ — НЕ СТОП, А ПЕРЕБАЗИРОВАНИЕ.** Взять свежую голову, собрать на ней,
назвать её отдельной строкой возврата. СТОП домой — на всё ОСТАЛЬНОЕ неожиданное.

Пин: `engineering_contract: 36` против продуктового `synced_contract_version: 36`. Re-sync не нужен.

## slot

**WIN-U2**, каталог `C:\projects\Unity\GasCoopGame_win-u2`, ветка `slot/win-u2`.
Перемерено файлом состояния: все четыре слота `AVAILABLE / lease none`.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-observe-1-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/dev`, readback дерева, `git status` пуст.

**UNITY ЗДЕСЬ НУЖЕН — в отличие от трёх предыдущих задач дорожки.** Работа создаёт сцену, окно
редактора и накладку на Scene view. Записать endpoint слота в файл состояния и live-проверить, что
мост отвечает именно как WIN-U2; мост существует (`http://localhost:27497/p/41ebd445`), а поле
`unrecorded` в файле состояния — устаревшая запись. Если мост не верифицируется как WIN-U2 —
**СТОП и вопрос владельцу**, без shell/HTTP/headless обхода.

## goal

**Владелец получает удобный способ смотреть, КАК хозяин ходит и ПОЧЕМУ он это делает, и подавать
события руками.**

Его слова 2026-08-11: «для поведения хозяина нужен отдельный стенд… не в текущей сцене явно…
смотреть сверху на хозяина, спавнить, что тут это, тут это, очень удобно, в runtime… я хочу иметь
удобный способ проверять, как он ходит. **Удобный и при том не сделанный на коленке. Он должен
дальше быть расширяемый.**»

Это критерий 8 узла, вырезанный волной и возвращённый его живым словом.

**ИНСТРУМЕНТ ТОЛЬКО СМОТРИТ И ПОДАЁТ СОБЫТИЯ.** Настройку поведения он снял сам: «дублировать,
наверное, и не надо… мне чат будет расписывать… пока не надо». Поведение остаётся в JSON-профиле.

## context — перемерено чтением байтов на `87d399f6`

### СТРОИТЬ ПОЧТИ НЕЧЕГО: цепь уже считается и выбрасывается

За один тик хозяин уже собирает разом намерение, замеченный факт (с местом, временем, громкостью
числом и счётчиком «сколько раз тут уже было»), правдоподобную смену приоритета, физическое действие
с целью, возвращение к жизни и имя ветки поведения. Почти всё это роняется.

**Задача не строит новое знание — она перестаёт его выбрасывать.**

### ТРИ ЗАКРЫТЫЕ ДВЕРИ, и все три открываются ДОБАВЛЕНИЕМ

1. **Мир закрыт у курьера.** `Network/NetworkWalkerCourier.cs:29` — `private CarryWorld _world`;
   наружу у класса торчит `LocalPlayerId` (`:50`), `SetLocalInput` (`:103`) и хуки жизненного цикла.
   Достать состояние хозяина снаружи нечем. **Это единственный настоящий блокер.**
2. **Результат шага не сохраняется.** Хозяин берёт из результата память и имя ветки, остальное
   роняет.
3. **ИМЯ СРАБОТАВШЕГО ОБРАБОТЧИКА ВЫБРАСЫВАЕТСЯ.** В `Core/Householder/HouseholderDecisionPipeline.cs`
   переменная `selectedHandlerName` — ЛОКАЛЬНАЯ: арбитр знает, чей обработчик победил, а наружу
   уходит только предложение. Прочитано в байтах.
   **Без этого поля владелец никогда не узнает, ЧЬЯ строка сработала — а принимать обработчики
   пачками от ассистента он собирается.** Это самая ценная из трёх правок.

### Что уже есть и на что опираться, а не изобретать

- **Образец редакторного инструмента с мышью в сцене:** `Assets/TunnelCrew/Editor/StartConditionsEditor.cs`
  — перетаскивание точек прямо в Scene view. Каркас брать оттуда.
- **Odin Inspector УЖЕ КУПЛЕН И ЛЕЖИТ В ПРОЕКТЕ:** `Assets/Plugins/Sirenix/` — перемерено
  `git ls-tree`. В нашем коде не использован **ни разу** (`git grep Sirenix` по `Assets/TunnelCrew`
  пуст). Платить не надо. **Каркас всё равно строится на голом Unity**; Odin допустим только там,
  где он экономит явно, и его использование называется в возврате.
- **Сцены в сборке — ровно две:** `Lobby.unity` и `IntegratedHouse.unity`
  (`ProjectSettings/EditorBuildSettings.asset`). В репозитории лежат ещё две сцены —
  `HostWalksHisDay.unity` и `SingleWalker.unity`, **обе вне сборки**.
- **`HostWalksHisDay.unity` — мёртвый стенд прошлой эпохи.** Он и есть доказательство риска: стенд
  со своей жизнью протухает молча. Новый полигон не имеет права повторить это.
- **Панель условий заглушена в Play** — `Editor/StartConditionsEditor.cs:63`, `:70`, `:249`
  (`EditorApplication.isPlayingOrWillChangePlaymode`). Отсюда сегодняшний цикл: чтобы переставить
  что-нибудь, надо выйти из Play, переставить, войти. Задача ломает именно этот цикл.
- **Места по ИМЕНИ заблокированы:** `Profiles/HouseholderProfileLoader.cs:30`, `:65`, `:627` —
  проверка имени места всегда отвечает «неизвестно». Значит пульт работает КООРДИНАТАМИ, а
  «поставь в спальню» ждёт отдельной задачи привязки дома.

### Что покажет инструмент в первый же день, и это не дефект

`Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json` несёт
`"behaviorBranches": []` и `"decisionHandlers": []`. **В собранной игре сегодня не работает ни один
обработчик** — возможность построена, но ничего ещё не написано. Окно покажет это сразу, и это его
первая польза, а не поломка.

### Что уже зелёное

`87d399f6` даёт **196 passed, 0 failed, 0 skipped** — прогнано Направлением на этой самой голове.

## boundaries — что НЕ трогать

- **НЕ ЗАКОЛАЧИВАТЬ ДВЕРЬ** (`knowledge/core-is-extended-never-rewritten.md`). По каждому месту,
  которое задача открывает, в возврате называется результат теста **«чтобы добавить сюда новое —
  ДОБАВИТЬ или ПРАВИТЬ?»**.
- **ПОЛИГОН НЕ ЗАВОДИТ СВОЕЙ ЖИЗНИ.** Ни одной строки правил, своей симуляции, своего хозяина,
  своего каталога, своего профиля. Тот же судья, тот же профиль, те же пороги. **Замок механический:
  состав компонентов полигона обязан быть подмножеством состава игровой сцены плюс пульт, и это
  проверяется чтением двух файлов сцен ТЕКСТОМ, без Unity.** Проверку предъявить в возврате.
- **ИСКУССТВЕННОЕ СОБЫТИЕ НЕ ИМЕЕТ СВОЕЙ ДОРОГИ.** Клик мышью порождает ту же `WorldSituation`, что
  и настоящий контакт груза, и проходит те же пороги `60 / 150 / 400`. Обход порогов — дефект, а не
  удобство: иначе владелец будет проверять не ту дорогу.
- **ИНСТРУМЕНТ НЕ ЕДЕТ ИГРОКУ.** Окно и накладка — в `Editor/`. Полигон НЕ вносится в
  `EditorBuildSettings`. Код пульта не компилируется в сборку.
- **`IntegratedHouse.unity` НЕ РЕДАКТИРУЕТСЯ.** Полигон — НОВАЯ сцена.
- **Поведение игры не меняется ни на грамм.** Пять реакций, пороги, профиль, дефолт — как есть.
  Три правки ядра — только ДОБАВЛЕНИЕ доступа, ничего не переписывается.
- **Файлы дорожки `cargo-hands` — чужие:** `Core/Cargo/**`, `Network/CargoBody.cs`,
  `Network/NetworkPlaySettings.{cs,asset}`, тесты `Cargo*`, `TelekineticPickup*`,
  `FirstPersonCarry*`. Упёрся — СТОП домой.
- **Публичные сигнатуры `CarryWorld.Advance` не меняются.**
- **`ProjectSettings/**` закрыт ЦЕЛИКОМ, включая `EditorBuildSettings.asset`** — то есть полигон в
  список сцен сборки не вносится вовсе, и это не забывчивость, а требование.
  `Assets/GasCoopGame/**` закрыта.
- **Настройка поведения в инспекторе НЕ строится** — владелец снял это дословно.
- Самодельный сканер исходников уликой не является и не пишется.

## РАЗРЕШЁННАЯ ЛИНИЯ РАЗРЕЗА — резать без возврата за словом

**Строка 1 — три правки ядра плюс ОКНО — остаётся ВСЕГДА.** Без окна полигон ничего не показывает.

Если не помещается в полдня: **ПОЛИГОН уезжает своей задачей**, окно остаётся и работает поверх
настоящей сцены. Обратный разрез ЗАПРЕЩЁН. Что срезано — называется в возврате.

## owner-visible check

**Эту задачу судит ГЛАЗ ВЛАДЕЛЬЦА, и потому светлое закрытие ей НЕДОСТУПНО по букве волны.**
Работа исполнителя кончается на «построено, опубликовано, откат исполнен». Заявлять, что инструмент
«удобный», в возврате ПРЯМО ЗАПРЕЩЕНО — это его слово, а не твоё.

**И отдельный запрет: ни одна галка «сделано» ни в одной задаче не закрывается тем, что «в полигоне
работает».** Полигон — инструмент наблюдения, не поставка и не доказательство поведения игры.

## headless evidence

- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`. База — `196 passed` на `87d399f6`.
- **Три правки ядра покрыты тестом:** имя сработавшего обработчика доезжает наружу; последний
  результат шага читается; состояние хозяина достижимо снаружи мира. Это engine-free и проверяется
  headless.
- **Замок полигона предъявляется ТЕКСТОМ:** состав компонентов полигона — подмножество игровой
  сцены плюс пульт, показать сравнением.
- **Через прямой MCP точного слота — настоящая Unity import/compile evidence** плюс скриншот окна с
  разобранной цепью на живом тике.
- **Зелёные ворота поведением игры не являются**
  (`knowledge/green-gates-do-not-compile-the-game.md`).

## откат

- **Точка возврата — ГОЛОВА ДОРОЖКИ на момент старта** (свежая `origin/dev`), не константа наряда.
- Откат назвать ОДНОЙ командой и **исполнить настоящим revert**, как в трёх предыдущих задачах:
  после него `git rev-parse <ветка>^{tree}` возвращает дерево той головы знак в знак.
- Прямо заявить, что вне репозитория для отката ничего не нужно.

## done_when

1. **ЦЕПЬ ВИДНА, И ИМЯ ОБРАБОТЧИКА ДОЕЗЖАЕТ.** Окно в Unity показывает по шагам за выбранный тик:
   что случилось → что услышал (вид, МЕСТО, громкость ЧИСЛОМ) → что запомнил, включая «сколько раз
   тут уже было» → **ЧЬЯ строка сработала** → что делает. Три двери открыты ДОБАВЛЕНИЕМ: доступ к
   миру у курьера, последний результат шага, и имя сработавшего обработчика, которое сегодня
   выбрасывается локальной переменной. Окно работает поверх ЛЮБОЙ открытой сцены, включая настоящую.

2. **СОБЫТИЯ ПОДАЮТСЯ РУКАМИ С ВИДА СВЕРХУ, И ПОЛИГОН НЕ ЖИВЁТ СВОЕЙ ЖИЗНЬЮ.** Новая сцена: вид
   сверху на хозяина, клик мышью в точке порождает ТУ ЖЕ `WorldSituation` через ТЕ ЖЕ пороги
   `60 / 150 / 400`. Своих правил, своего судьи, своего профиля у полигона нет ни одного — замок
   предъявляется сравнением состава двух сцен текстом.

3. **ИНСТРУМЕНТ НЕ ЕДЕТ ИГРОКУ, ПОВЕДЕНИЕ НЕ ИЗМЕНИЛОСЬ, ОТКАТ ПРОВЕРЕН, И РАСШИРЯЕМОСТЬ НАЗВАНА.**
   Окно и накладка в `Editor/`; полигон не в списке сцен сборки; код пульта в сборку не
   компилируется. Пять реакций, пороги, профиль и дефолт целы; существующие тесты зелёные.
   В возврате названо ЧИСЛОМ, во что обойдётся добавить в окно новый вид события и новый показатель
   памяти. Границы целы — сцена игры, файлы `cargo-hands`, сигнатуры `CarryWorld.Advance` — доказано
   диффом. Откат назван одной командой, исполнен настоящим revert и проверен readback дерева.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / householder / t-observe-1`.

**ПЕРВОЙ СТРОКОЙ — РАСПИСКА:** число полученных строк `done_when` (их три).

Также обязательно:

- **тест «ДОБАВИТЬ или ПРАВИТЬ» по КАЖДОМУ открытому месту**;
- **как открыть окно и что в нём видно** — по шагам, как это сделает владелец: открыл, нажал, увидел;
- **скриншот окна с разобранной цепью** на живом тике;
- **чем предъявлен замок полигона** — сравнение состава двух сцен;
- **во что обойдётся добавить в окно новый вид события и новый показатель** — числом;
- **использовался ли Odin** и где именно, раз он в проекте уже есть;
- **резал ли по разрешённой линии** — да или нет, что уехало и почему;
- проверенный откат: команда и readback дерева;
- терминальный readback селектора `CLEAN / AVAILABLE / lease none`;
- **отдельной строкой: с какой головы `origin/dev` собиралось и публиковалось**;
- расхождения свежей базы с grounds наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает, и светлое закрытие ей недоступно:** её
судит глаз владельца.

## budget

Один engineering root. Половина дня на строку 1; строка 2 внутри, если помещается, иначе режется по
НАЗВАННОЙ линии. Настройка поведения в инспекторе, пятое звено цепи, сценарии событий, расстановка
по именам мест, перечитка профиля на ходу и показ в собранном exe сюда НЕ входят. Не помещается
даже после разреза — STOP HOME с одним точным blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-observe-1-001-call.md
