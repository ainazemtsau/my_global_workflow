# CALL — c-exec-g-5a7c-player-vertical-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
stage: PLAN
direction: indie-game-development
node: g-5a7c
task: t-scale-3
issued: 2026-08-08 by s-work-g-5a7c-scale-3-call-001
status: ready
slot: **WIN-U1** — рабочее дерево `C:/projects/Unity/GasCoopGame_win-u1`, ветка `slot/win-u1`.
      Перемерено первой рукой 2026-08-08 после отзыва прошлого наряда: дерево ЧИСТО, вершина
      `ae023ba5` — ровно база, слот `AVAILABLE`, лиза `none`. Чужой работы в слоте нет.
      **Unity Editor в этом слоте поднимает владелец.** Нужный MCP недоступен — СТОП и вопрос
      владельцу, а не обходной путь.
base: `ae023ba5f5d168c819d6b7b2874be36a55f37d63` — `main` = `origin/main` = `dev` = `origin/dev`
      на 2026-08-08. Ground для трёхстороннего перемера, а не freshness-lock.
      Пин перемерен: `engineering_contract: 36` против `validation.config`
      `synced_contract_version: 36` — дрейфа нет, Re-sync не должен.

## goal

У налётчика есть высота: её считает создатель игры, она доезжает до всех копий, и её одинаково
видят камера и проверка свободного места.

## context

- **Сегодня высоты нет вовсе, и это перемерено первой рукой на базе:**
  `Core/Multiplayer/PlayerState.cs` — конструктор `PlayerState(float x, float z)`, свойства `X` и
  `Z`, третьего нет; `Network/WalkerSnapshot.cs` — поля `PlayerId`, `X`, `Z`, `Yaw`, `Moved`, `Y`
  отсутствует; `Network/NetworkWalkerPresentation.cs` строит позицию как
  `new Vector3(snapshot.X, 0f, snapshot.Z)` — ноль вбит константой.
- **Камера сегодня вбивает свою высоту, и это точное место:**
  `Network/NetworkWalkerCamera.cs:41` — `_camera.transform.position = new Vector3(standing.x,
  _settings.EyeHeight, standing.z)`. Точка опоры приходит из
  `NetworkWalkerPresentation.TryGetStandingPoint` (`:45`), высота берётся из настроек мимо неё.
- **Шов свободного места уже стоит и он двумерный:** интерфейс `IWalkSpacePort` объявлен в
  `Core/Multiplayer/WalkSpacePort.cs:23` как
  `WalkStep Resolve(int playerId, float fromX, float fromZ, float toX, float toZ)`; реализация —
  `Network/WalkSpaceProbe.cs:20`, вызов — `Core/Multiplayer/AuthoritativeWalkerRoster.cs:184`.
  **Поправка к прежним основаниям:** файл называется `WalkSpacePort.cs`, а не `IWalkSpacePort.cs`
  — прежний разбор называл его вторым именем.
- **Стены сегодня не имеют высоты вовсе** (разбор `W21`): прямоугольник по X/Z, выталкивание круга
  независимо от любой Y. Перемерить это первой рукой — факт снят до перевода масштаба.
- **Порядок тика зафиксирован в одном месте** — `Core/Multiplayer/CarryWorld.cs:36-58`: груз
  замедляет → все идут → груз тянут → факт доставки → хозяин слышит шум → шаг хозяина. Готовая
  точка под новую ось; менять порядок не требуется.
- **Мир идёт в росте налётчика 0.16 м**, глаза 0.136, скорость 0.6 м/с, ближняя плоскость камеры
  0.01 — принято прогоном владельца и его словом «физика держится, размер тот»
  (`docs/results/c-exec-g-5a7c-scale-probe-001.md` на базе). Эти числа не пересматриваются.
- **Общей настройки масштаба в проекте НЕТ и заводить её запрещено.** Прошлая задача, которая её
  строила, снята владельцем 2026-08-08; подписанная строка карточки узла теперь говорит дословно:
  «Общей настройки масштаба нет и не заводится: пересчёт мира был однократным и кончился».

### Происхождение строк задачи — проверено этой ногой, и одну поправку оно дало

Наряд обязан был проверить, откуда взялась каждая несущая строка задачи. Результат:

- **«вдвоём по сети обе копии показывают одну и ту же высоту»** — прямо из подписанного критерия 2
  карточки узла («его одинаково видят сервер, обе копии, камера и столкновения») и строки разбора
  `W13`. Основание твёрдое, строка остаётся гейтом.
- **«камера садится на присланную высоту вместо того, чтобы вбивать свою»** — строка разбора `W14`
  плюс перемеренный факт кода выше. Основание твёрдое.
- **«третье число живёт в состоянии игрока, в шаге и в снимке» и «порт свободного места отвечает в
  трёх координатах»** — происходят из строки `W19`, а `W19` называется **«Цена вертикали у
  игрока»**: это ЗАМЕР СТОИМОСТИ, а не требование. Он затвердел в задаче как приказ об устройстве.
  **Поэтому в `done_when` ниже они стоят как ИСХОД, а не как места в коде:** где именно живёт число
  и какой формы становится шов — решает исполнитель. Замер передаётся как основание: около девяти
  файлов, в восьми по одной строке.

## boundaries

- **Вертикали ГРУЗА здесь нет.** Три заморозки `CargoBody.cs:66-68` не снимать, гравитацию не
  включать, точку приложения тяги не трогать — это `t-scale-4`, названная самой дорогой для
  отката задачей волны.
- **Путь наверх НЕ строится.** Ни лестниц, ни мебели, ни вентиляции, ни прыжка, ни «ключевых
  комнат». Из чего сделан путь снизу вверх — **владелец отложил своими словами** 2026-08-07:
  «когда будет задача, тогда это и сделать». Это не та задача.
- **Двигает ли телекинез самого налётчика вверх — вопрос ВЛАДЕЛЬЦА, а не исполнителя.** Он
  отложен тем же его словом. Здесь он не решается и не предвосхищается ни строкой кода.
- Общей настройки масштаба не заводить и ничего от неё не выводить.
- Дом, хозяина, обстановку, облик, слой шума и пороги `60 / 150 / 400` не трогать.
- **Инструмент недоступен — СТОП.** Обходных путей нет; исключение — только письменное «да»
  владельца.
- **Всё, что этот наряд утверждает про устройство проекта, — основание, а не приказ.** Перемерь
  каждый путь первой рукой на объявленной базе; неверное основание при верной инструкции
  исправляется на месте и называется в возврате.

## done_when

1. Высота налётчика существует и авторитетна: её считает создатель игры на сервере, она доезжает
   до копий вместе с остальным положением, и обе копии показывают одну и ту же высоту одного и
   того же ходока.
2. Высоту видят те, кто обязан её видеть: камера садится на присланную высоту вместо того, чтобы
   вбивать свою, а проверка свободного места отвечает с учётом высоты, а не поперёк неё.
3. Кроме высоты не изменилось ничего: горизонтальное движение, столкновения со стенами, груз,
   хозяин и слой шума ведут себя ровно как до задачи, и откат назван и проверен.

## return

Терминальный HOME по pinned v36: product RESULT, commits/manifest, вывод проверок и честная
диспозиция всех трёх строк `done_when`, плюс отдельной строкой — что при перемере разошлось с
основаниями этого наряда. Прогон вдвоём по сети закрывает строку 1 наблюдением, а не рассуждением;
если поднять две копии нечем — это СТОП и вопрос владельцу, а не замена наблюдения выводом.
Продуктовый handback сам задачу Direction НЕ закрывает.

## budget

Один engineering root, не больше сосредоточенного дня. **Если высота требует тронуть груз, снять
хоть одну заморозку или построить путь наверх — СТОП и HOME с точным разрывом:** это признак, что
границы задачи проведены неверно, и решает это Direction, а не наряд.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-player-vertical-001-call.md
