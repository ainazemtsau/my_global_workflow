# CALL — c-converge-arch-g-1d84-001

to: session
direction: indie-game-development
play: converge-arch
node: g-1d84
issued: 2026-08-04 by s-converge-g-1d84-integrated-house-slice-001
status: ready

## goal

У узла g-1d84 закрыты контракты с соседями и отработаны его высокорисковые архитектурные вопросы:
каждый вопрос несёт опровергнутое сравнение вариантов и либо подпись владельца, либо явную открытую
строку с названным отвечающим. Результат ложится входной уликой в наряд, а не в done_when.

## context

WHAT-поверхность закрыта: `work/converge-g-1d84.md` — 107 строк §WHAT, 21 ряд §GLOSSARY,
28 ввезённых решений §IMPORT, `triage: heavy — converge ON`, `open_rows: 22`. Владельцу не должна
ни одна строка: обе его развилки подписаны 2026-08-04 (W70 «отбирает — самое простое»; W10 «место,
где груз не проходит, есть»), плюс слово на набор — «фиксирую». Полный разбор и точные слова —
`history/2026-08-04-s-converge-g-1d84-integrated-house-slice-001.md`.

Девять строк ждут именно этой ноги, и они перечислены здесь, чтобы их не пришлось выискивать:

- **W09 — где живёт правда о доме:** в данных слоя правил или в объектах и коллайдерах сцены. Названо
  самым дорогим устройственным местом волны; ни один покупной инструмент знание дома слою правил не
  даёт. Сегодня в продукте стены единственной комнаты описаны данными настроек, а маршрут хозяина —
  `Transform[]` в сцене, то есть оба чтения живут одновременно.
- **W52 — форма файла профилей хозяина.** Map-нога назвала это ПЕРВЫМ дорогим для отмены решением
  волны и отправила сюда. **Подпись владельца на этом выборе обязательна**, и форма разговора задана
  им самим: «чтобы мне написали каждый условный риск и ограничение архитектуры, чтобы я согласился
  с ней или нет». Уже закрыто и не переоткрывается: текстовые файлы в репозитории, описание формата
  под code assistant, отвержение неверного файла с причиной и местом ошибки, отдельного редактора нет.
- **W05 — что такое «стабильный идентификатор» корня и «явный интерфейс» между корнями.**
- **W06 — чем проверяемо доказывается «скрытых ссылок между корнями нет».** Это несущая половина
  критерия 8: именно она физически разрешает параллельную работу, ради которой владелец принял рамку
  одной карточки.
- **W40 — имя шва, через который добавляется СПОСОБНОСТЬ хозяина.** В коде уже есть названный шов
  решения (`IHouseholderDecisionPolicy`, две реализации плюс тестовая подмена), но способностей нет
  вовсе и шов для них не назван.
- **W31 — где живёт режим камеры** и является ли направление взгляда авторитетным состоянием.
  Принятое расслоение относит камеру к «только моё», но принудительное переключение самой игрой —
  это правило, а правила живут у судьи.
- **W61 — глубина вложенных отвлечений** (цепочка прерванных дел или одно).
- **W80 — что значит «инструментов» в критерии 10** и входят ли они в пробу наравне с наборами.
- **W105 — сколько в этой волне новых слоёв** и входит ли в них камера. Наряд называет два дорогих
  для отмены решения; третье добавила предыдущая нога, и это вывод, а не ввоз.

Края, которые обязаны получить запись §CONTRACTS: g-c519 (не входит, перепроверяется отдельной ногой
после игры в этот кусок), закрытая ставка g-6b13 (существующий груз и шов хозяина — вход), отдельная
ставка на копание (тоннели здесь только открываются), g-2b7f и g-8a41 (узел ничего им не обязан ни
одним критерием), корень g-0c26, будущий узел «полный заход демо».

Открытые записи, которые эта нога обязана прочитать: `i-architecture-pass-skipped-for-this-bet-001`
(правило: разбор перед первой задачей полосы, пишущей код в новом слое), `i-owner-standing-rule-extend-never-rebuild-001`,
`i-ready-made-tools-scan-2026-08-03-001` (живые ловушки PGG, Dungeon Architect 1.22.0, Unity 6000.5,
переходник FishNet↔Steam), `i-verification-guards-unwired-not-deleted-001`,
`i-direction-to-product-call-contract-001`.

Продуктовые факты берутся first-hand и с ОБЪЯВЛЕННОЙ базой. На 2026-08-04 это
`C:/projects/Unity/GasCoopGame`, `main` = `origin/main` = `origin/dev` = `839df47e`. Две записи NOW
устарели и проверены: слот WIN-U1 фактически `AVAILABLE` без аренды, а правила мёртвого концепта
(«Engine-free core, always», «Deterministic core») из продуктовой конституции уже удалены.

Цена предыдущего опыта названа прямо: тяжёлый разбор на g-8f31 стоил шести ног и нуля строк кода.
Эта нога работает по девяти названным строкам и краям, а не по всему узлу заново.

## boundaries

- Не менять CHARTER, TREE и §WHAT `work/converge-g-1d84.md`. Закрытая строка не переоткрывается;
  если разбор упрётся в противоречие с карточкой — это решение владельца через map.
- Не создавать ставку, задачи, полосы и не выпускать инженерный наряд.
- Архитектура-на-бумаге едет в `context` наряда как входная улика и НИКОГДА в `done_when`.
- Низкорисковое (раскладка буфера, формат байтов, конкретные числа, выбор инструмента моделирования)
  остаётся `→ PLAN` и здесь не решается.
- Не втаскивать свойства тоннеля из замороженного `work/converge-g-8f31.md` — он улика, не власть.
- Не добавлять требований к составу проверки владельца, дат и сроков.
- Технические развилки нести владельцу простым языком с рекомендацией и ценой; коды и идентификаторы
  в разговоре с ним не использовать. Развилку, ответ на которую уже лежит в состоянии его словами,
  ему не выносить — сначала искать ответ, потом спрашивать.

## done_when

1. `work/converge-g-1d84.md` §CONTRACTS существует: каждое взаимодействие с соседним узлом, проверенное
   против дерева, имеет запись; ни один потребляемый контракт не висит без названного производителя.
2. `work/converge-g-1d84-arch.md` существует и по каждому высокорисковому вопросу несёт таблицу
   опровергнутых вариантов и либо подпись владельца его словами, либо `open` с названным отвечающим.
3. Форма файла профилей хозяина (W52) вынесена владельцу с ценой и риском и закрыта его словами либо
   осталась явно открытой на нём.
4. `arch_open: <n>` записано; `arch_in_context_only: PASS` — ни один выбор не попал в done_when.
5. Одна миномётная проверка на пропуск: назван хотя бы один архитектурный вопрос, который эта нога
   НЕ задала.

## return

Полный Direction RESULT со ссылками на §CONTRACTS и `work/converge-g-1d84-arch.md`,
`contract_coverage`, `arch_open`, точными словами владельца по подписанным выборам и одним локальным
следующим маршрутом — свежая `converge-verify`. Разработку не запускать.

budget: one owner-present session

END_OF_FILE: live/indie-game-development/work/c-converge-arch-g-1d84-001-call.md
