# CALL — c-exec-g-5a7c-occupations-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: householder-life
task: t-occupations-1
issued: 2026-08-12 by s-work-g-5a7c-occupations-1-dispatch-001
status: ready
basis: `c1d8d3845f4c5a8c6dca49d30f5d7e8cac09f815` — `origin/main` = `origin/dev`, перемерено
       `git ls-remote origin` перед выпуском 2026-08-12.

**ГОЛОВА СЕГОДНЯ ДВИГАЛАСЬ ТРИЖДЫ, И ДВЕ ДОРОЖКИ НА ЭТОМ УЖЕ ОБОЖГЛИСЬ.** Правило прежнее — не
СТОП, а перебазирование, — но с добавкой, которая стоила нам двух переделок: **сверь
`origin/main` ЕЩЁ РАЗ ПЕРЕД ТЕМ, КАК ОТДАВАТЬ РЕЗУЛЬТАТ, а не только перед сборкой.** Обе
пострадавшие дорожки проверили голову один раз, на старте. СТОП домой — на всё ОСТАЛЬНОЕ
неожиданное.

## slot

**WIN-U3**, каталог `C:\projects\Unity\GasCoopGame_win-u3`, ветка `slot/win-u3`.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U3 -Claim -LeaseId c-exec-g-5a7c-occupations-1-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/main`, readback дерева, `git status` пуст.

**UNITY НЕ НУЖЕН** для основной работы: она в engine-free `Core/Householder/**`, в загрузчике
профиля и в headless-тестах. Если упрёшься — СТОП домой.

**ПАРАЛЛЕЛЬНО ИДУТ ДВА НАРЯДА, ИХ ФАЙЛЫ ЗАПРЕЩЕНЫ:**
- `c-exec-g-5a7c-rooms-1-001` в WIN-U2 — `Assets/TunnelCrew/World/**`, `Assets/TunnelCrew/Scenes/**`,
  `Assets/TunnelCrew/Art/Furnishings/**`, `Assets/Tests/TunnelCrew/ThreeRoomHouse/**`;
- `c-exec-g-5a7c-body-1-002` в WIN-U1 — `Assets/TunnelCrew/Presentation/**`,
  `Assets/TunnelCrew/Art/Bodies/**`, `Network/NetworkWalkerPresentation.cs`,
  `World/Householder.prefab`, `Settings/PresentationSettings.*`.

## goal

**ХОЗЯИН ЗАНЯТ ПОНЯТНЫМ ДЕЛОМ В НАЗВАННОМ МЕСТЕ, У ДЕЛА ЕСТЬ ФАЗЫ, И ОПАСНУЮ ОН СНАЧАЛА
ОБЕЗОПАСИТ.**

**Это цель всей волны его словами:** «сделать уже такую игровую систему… чтобы хозяин имел
комплексное поведение»; «я хочу видеть места, условно, что он там занимается, что-то на кухне,
что-то на кровати где-то долго, где-то нет. Останавливается, вот это всё видит мышь, слышит мышь».

**Правило фаз — из его документа демо, дословно:** «если хозяин режет продукты, его относительно
легко отвлечь. Если на плите что-то горит, он не должен по первому шуму мгновенно бросить всё и
побежать за крысами: сначала он физически обезопасит плиту». И вывод оттуда же: «игроки должны
учиться читать намерение, а не заучивать секундный маршрут».

**НАЗВАННЫЙ ПОДХОД — ОДИН:** занятие описывается ДАННЫМИ в профиле и опирается на уже приехавшие
роли мест; фазы — свойство занятия, а не новая ветка в решателе. Отход — ESCALATE домой.

## context — перемерено чтением байтов на `c1d8d384` 2026-08-12

### Что уже приехало и на что ты опираешься

- **Роли мест работают** (шов `t-places-1`): профиль называет роль, дом её объявляет,
  `HousePlan.TryFindPlaceByRole`, а **неизвестная роль НЕ убивает хозяина — её шаг становится
  пустым**. Следуй этому образцу для всего нового.
- **Место уже течёт до шага распорядка:** `HouseholderPlaceReference RoutinePlace` живёт во входе
  шага (`Core/Householder/Householder.cs:1700,1727,1739,1779,2134,2738`).
- **Замеченный факт уже несёт КОМНАТУ** (`t-place-fact-1`), и накопление умеет считать по комнате.
- **Прерывание уже есть данными:** поле `RequiresSafeInterruption` у реакции и раздел
  `activeResponseRules` в профиле с приоритетом и режимами вытеснения `higher-priority` /
  `higher-or-equal-latest` (`docs/householder-profile-format.md`). **Переиспользуй их, не заменяй.**

### Чего нет и что ты строишь

- **Умений ровно ДВА:** `walk-route` и `wait-at-route-point`, зарегистрированы switch-ом в
  `Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs:53,56`; расширяются через
  `TryResolveAbility` (`:18,47,124`). Оба сегодня используют маршрут из сцены.
- **Занятий в перечислении пять, и все реактивные, кроме одного:** `WalkRoute`,
  `InvestigateSuspiciousNoise`, `ListenForQuietRustle`, `GuardAgainstRepeatedHeavyNoise`,
  `InvestigateLanding`. Обычной жизни у хозяина ровно одна — ходить по маршруту.
- **Фаз нет вовсе.** Дело сегодня — это шаг с длительностью, и прерываемость не зависит от того,
  что он в этот момент делает.

### Что мешает и на что не надо натыкаться

**За тик хозяин слышит РОВНО ОДНО** — `Core/Multiplayer/CarryWorld.cs:167-168` сводит тик-лист к
самому громкому стимулу. Это НЕ твоя задача и трогать сведение запрещено. Знай про него, чтобы не
объяснять им поведение фаз.

### База прогонов

`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **239 passed,
0 failed, 0 skipped**, прогнано Направлением 2026-08-12 на `c1d8d384`.

`tools/hygiene.ps1` зелёный. **`tools/check.ps1 -Deliver` красный на исторических отчётах —
зарегистрированный дефект `i-deliver-gate-red-when-main-catches-dev-001`, НЕ твоя работа.**

### ЗАВИСИМОСТЬ ОТ ДОМА, НАЗВАННАЯ ЧЕСТНО

Строка 3 требует, чтобы **опубликованный** профиль зажил занятиями. Для этого в доме должны быть
объявлены роли — кухня, спальня и так далее. Дом (`t-rooms-1`) на момент выпуска этого наряда
**ещё не опубликован**, его кандидат лежит в WIN-U2.

Поэтому: строки 1 и 2 строй и доказывай **headless на синтетических планах дома**, ровно как это
делали шов и «место как факт». Строку 3 закрывай **последней**, уже на опубликованном доме. **Если
к моменту строки 3 дом не приехал — СТОП домой**, а не выдумывай роли и не правь сцену.

## boundaries

- **Задача не расширяется.** Три строки `done_when` — весь объём.
- **НИ ОДНА СЦЕНА И НИ ОДИН ПРЕФАБ НЕ ОТКРЫВАЮТСЯ.** Они принадлежат `t-rooms-1`. Роли в доме
  объявляет он, не ты.
- **НЕ ОТКРЫВАЕШЬ файлы параллельных нарядов** — списки в разделе `slot`.
- **НЕ ТРОГАЕШЬ** `Core/Cargo/**`, `Core/Situations/**`, `Network/Cargo*`,
  `Settings/GameRulesSettings.*`, `CarryWorld.cs`.
- **ПРАВИЛО «САМЫЙ ГРОМКИЙ ЗА ТИК» НЕ ТРОГАЕТСЯ.**
- **ПОРОГИ `60 / 150 / 400` НЕ ДВИГАЮТСЯ.** Каталог пяти реакций не трогается.
- **НЕ ДЕЛАЕТСЯ:** зрение; расписание по времени суток; шесть бытовых фаз по часам и вечер на
  25–35 минут (вырез узла); душ как маскировка звука (вырез узла); перестановка вещей хозяином;
  уход из дома и возвращение с более жёстким средством; новые виды события; новые реакции.
- **СПИСОК ИЗ ДЕСЯТИ ДЕЛ ЕГО ДОКУМЕНТА — НЕ ТРЕБОВАНИЕ.** Сколько дел войдёт, решает объём полдня;
  минимум — те, которых требует центральная кража его документа: **готовит и моется**.
- **Настроечные значения — не код** (контракт 36): длительности фаз живут в данных и тестами не
  покрываются. Тест проверяет ФОРМУ, никогда величину.
- **Тест только там, где владелец не увидит ответ глазом.** Строки 1 и 2 глазом не видны — их
  доказывают тесты. Строку 3 судит он.
- **Самописный сканер как улика поведения запрещён.**
- **Инструмент недоступен → СТОП домой. Решение владельца → ESCALATE домой.**
- **Расписка обязательна.**

## done_when

Три строки — потолок контракта 36. Ни одна не снимаема.

1. **ЗАНЯТИЕ В НАЗВАННОМ МЕСТЕ, АВТОРИМОЕ ДАННЫМИ.** Шаг распорядка называет роль места и ДЕЛАЕТ
   там дело, а не просто стоит; новое занятие добавляется в профиль строкой, без новой строки кода
   сверх регистрации умения; неизвестное умение отвергается названно и с путём, а неизвестная роль
   по-прежнему просто молчит.
2. **У ДЕЛА ЕСТЬ ФАЗЫ, И ОПАСНАЯ ЗАКРЫВАЕТСЯ ПЕРВОЙ.** Дело состоит из фаз, различающихся
   прерываемостью: реакция, требующая безопасного прерывания, ждёт конца опасной фазы, а реакция,
   которой это не нужно, прерывает сразу. Существующие `RequiresSafeInterruption` и
   `activeResponseRules` **переиспользованы, а не заменены**. Доказывается headless.
3. **ЭТО ВИДНО В ИГРЕ, И НИЧЕГО НЕ СЛОМАНО.** Опубликованный профиль живёт занятиями вместо «иди по
   маршруту / постой», и в собранной игре видно, что хозяин занят делом, а не патрулирует; пять
   реакций целы; прогон тестов назван числом против базовых **239**; откат одним коммитом исполнен
   НАСТОЯЩИМ `revert`, дерево сходится с деревом базы.

## return

**Первая строка — РАСПИСКА:** сколько строк `done_when` получено (три) и с какого базиса собрано.

Дальше:

- что сделано, по каждой из трёх строк отдельно;
- **какие занятия и какие фазы заведены**, и какой строкой профиля добавляется следующее;
- сколько строк КОДА стоило новое занятие сверх регистрации умения — числом;
- как ведёт себя неизвестное умение и неизвестная роль, чем доказано;
- полный список путей диффа и подтверждение, что среди них нет сцен, префабов и файлов
  параллельных нарядов;
- прогон тестов до и после числами;
- откат: команда, хеши, совпадение деревьев;
- **подтверждение, что голова `origin/main` сверена ПЕРЕД тем, как отдавать результат;**
- **что владельцу запустить и на что посмотреть** для строки 3;
- что НЕ сделано и почему; всё неожиданное.

## budget

Половина сфокусированного дня. Порог смерти 3 этой ставки считает ноги.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-occupations-1-001-call.md
