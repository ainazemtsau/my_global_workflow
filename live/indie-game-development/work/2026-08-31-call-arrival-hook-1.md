# CALL: «сделал» наступает, когда он дошёл и закончил, а не когда тронулся

Задача: `t-arrival-hook-1` · волна `bet-g-5a7c-wave-9` · полоса `утро`, ПЕРВАЯ в ней
База: продуктовый `main` = `c7e926cf`. Стартовать С НЕГО, а не с ветки слота.
Слот: **WIN-U2**, словом владельца.

---

## ПРОЧИТАТЬ ДО ПЕРВОЙ СТРОКИ

Владелец описал, каким должно быть утро хозяина, своими словами:

> «Он пошёл там, **поставил кофе, там чайник заваривается, пошёл, что-то там с кухни собрал себе
> на обед**, туда пошёл. То есть не то, что он как робот стоит.»

Направление разобрало его пример по кускам и перемерило каждый. **Почти всё выражается данными и
уже работает:** «поставил» — данные; «идёт без него» — данные, шкала с распадом это настоящий
таймер в JSON; «пора возвращаться» — данные; «отвлёкся и вернулся к недоделанному» — уже работает,
лента замораживается и доигрывается.

**Не работает ровно одно слово: «СДЕЛАЛ».** И это единственный настоящий разрыв во всём его примере.

## ЗАМЕР ДО ПРАВКИ

`Probe/Householder/Householder.cs:310-317`:

```
var isReaction = proposal.IsPresent &&
    proposal.Kind == HouseholderDecisionKind.Reaction;
if (isReaction)
{
    // Only the winner's, and only once it has won.
    standingAfter = HouseholderEffect.ApplyAll(proposal.Effects, standingBefore);
}
```

Единственный сторож здесь — `isReaction`. Условия «дошёл» нет. Значит эффекты выигравшей строки
применяются в тот тик, когда она **выиграла**, то есть когда он только тронулся. Метка «кофе
поставлен» встанет **за десять секунд до того, как он подойдёт к плите**, и дело закроется на старте.

*(Карточка задачи называет строку `:313` — там открывающая скобка, сам вызов на 315-317. Смещение
названо в её журнале, существо замера верно.)*

## ФОРМА, КОТОРУЮ НАДО ПЕРЕИСПОЛЬЗОВАТЬ, А НЕ ИЗОБРЕТАТЬ

**Крючок «по завершении» в этом файле УЖЕ ЕСТЬ.** `Householder.cs:1029-1036`:

```
private static HouseholderDeed DeedOfEnding(
    HouseholderActiveResponse response,
    bool responseCompleted)
{
    return responseCompleted
        ? response.Reaction.Deed
        : HouseholderDeed.Nothing;
}
```

Зовётся на `:1009` и `:1208`. Его собственный комментарий: «A response still running leaves nothing
behind». То есть различение «идёт» / «завершился» построено и работает — просто через него ходит
только **поступок**, а не эффекты.

**Это сильно снижает цену задачи и одновременно её главный риск.** Если ты ловишь себя на том, что
строишь второй механизм завершения рядом с этим — остановись: скорее всего форма понята неверно.

## goal

**У авторской строки появляется способ сказать «мои эффекты применяются по прибытии», и это слово
ДАННЫХ в профиле, а не ветка `if` в коде.**

## done_when

1. **ЗАМЕР ДО ПРАВКИ НАЗВАН В ВОЗВРАТЕ** — своими словами, с номерами строк, которые ты видел сам.
2. **У СТРОКИ ЕСТЬ СЛОВО, ГОВОРЯЩЕЕ «ЭФФЕКТЫ ПО ПРИБЫТИИ».** В профиле, не в коде.
3. **УМОЛЧАНИЕ НЕ МЕНЯЕТ СЕГОДНЯШНЕЕ ПОВЕДЕНИЕ БИТ В БИТ.** Строка без нового слова применяет
   эффекты как раньше. **Это и есть приёмка: в игре ничего не изменилось.**
4. **МЕТКА НЕ ПОЯВЛЯЕТСЯ, ПОКА ОН НЕ ДОШЁЛ.** Доказано тестом на ПОРЯДОК — метка после прибытия, —
   а не на длительность. Величины остаются данными.
5. **ТЕСТ «ДОБАВИТЬ ИЛИ ПРАВИТЬ» НАЗВАН ЧИСЛОМ.** Сколько мест ты ПРАВИЛ, а не добавил.
   **Больше пяти — СТОП и вопрос домой.** Это порог смерти всей волны, а не придирка к задаче:
   аудит ядра обещал, что хозяина «переписывать не надо», и хук прибытия — первое место, где
   обещание проверяется на несущем куске тика.
6. **НЕИЗВЕСТНОЕ СЛОВО ПО-ПРЕЖНЕМУ ОТКАЗЫВАЕТ ГРОМКО**, а не становится молча умолчанием.
   Словари загрузчика закрыты намеренно (`HouseholderProfileLoader.cs:64-83` и соседние) — не
   ослабляй их.
7. **ТЕСТЫ ЗЕЛЁНЫЕ.** Их 716 на `c7e926cf`, прогнано направлением, не заявлено.

## что НЕ входит

- **Наполнение профиля утром.** Это `t-morning-content-1`, следующая задача той же полосы. Ты даёшь
  механизм, содержание пишет она.
- **Занятия с фазами, шкалы, распорядок длиннее одного шага** — там же.
- **Конец захода, часы, срок утра** — исключены прямым словом владельца.
- **Перенос вещи на место** — полоса `руки`, отдельная задача.

## boundaries — ТРИ НОГИ ИДУТ ОДНОВРЕМЕННО, НЕ ЗАЛЕЗАЙ К СОСЕДЯМ

Твоя поверхность: `Probe/Householder/Householder.cs`,
`Probe/Householder/HouseholderRoutineState.cs`, `Profiles/HouseholderProfileLoader.cs`,
`StreamingAssets/TunnelCrew/Householder/default.householder.json`.

**Занято прямо сейчас другими ногами:**
- `Network/**` и `Editor/**` — нога прибора (`c-exec-g-5a7c-instrument-1-001`, WIN-U1);
- `tools/**` и `tests/TunnelCrew.Core.Tests/HouseholderDecisionPipelineTests.cs` — нога гейта
  (`c-exec-g-5a7c-gate-headless-1-001`, WIN-U4);
- `Probe/Householder/HouseholderDecisionPipeline.cs`, `HouseholderDecisionCandidate.cs` и шесть
  `Householder*DecisionHandler.cs` — полоса `почему`, идёт после.

**Свои тесты клади в отдельный файл**, не в `HouseholderDecisionPipelineTests.cs` — его сейчас
правит нога гейта.

## СТОП и вопрос домой

- **Правок больше пяти мест** — стоп и число домой. Не «почти пять».
- **Оказалось, что нужен второй механизм завершения рядом с `DeedOfEnding`** — стоп: это значит,
  что форма понята неверно, и это находка важнее задачи.
- **Умолчание не удаётся сохранить бит в бит** — стоп. Молча изменившееся поведение здесь хуже,
  чем несделанная задача: волна судит содержание на этом механизме.
- **Unity не поднялся или MCP не отвечает** — СТОП и скажи, какой слот поднять. Обходного пути не
  строй.

## return

1. Число правленых мест и разбор: что правил, что добавил.
2. Как называется новое слово в профиле и почему именно так.
3. Улика на пункт 3: чем доказано, что умолчание не изменило поведение.
4. Улика на пункт 4: тест на порядок, его имя и что именно он утверждает.
5. Прогон тестов числом.
6. Всё, что заметил и не чинил, — списком. **Не чини молча.**

## budget

Одна нога. Новой машинерии здесь ровно одна штука, и форма для неё уже существует. Если по ходу
окажется, что это не так — это находка и повод остановиться, а не повод строить больше.


---

# ДОПОЛНЕНИЕ 2026-08-31: ПОВЕРХНОСТЬ РАСШИРЕНА НА ДВА ФАЙЛА-НОСИТЕЛЯ

**Исполнитель WIN-U2 остановился по СТОПу наряда, назвал три запрещённых обхода вслух
(sentinel-эффект, кодирование в имени, глобальная таблица) и не построил ни одного. Это
правильное поведение, и разрешение выдано после собственной проверки направления, а не по его
рассказу.**

## ЧТО ПЕРЕПРОВЕРЕНО НАПРАВЛЕНИЕМ НА `c7e926cf`

Все пять его утверждений сошлись с байтами:

- `Householder.cs:310-317` — эффекты живут только в `proposal.Effects` и применяются сразу;
- `Householder.cs:391` — `activeResponse = new HouseholderActiveResponse(proposal);`;
- `HouseholderActiveResponse.cs:13-25` — конструктор копирует `IsPresent`, `Reaction`, `Target`,
  `FactOccurrenceId`, `ConsumedFactOccurrences`, `Phase`, `HoldRemainingSeconds`. **Поля `Effects`
  среди них нет.** После перехода эффекты потеряны;
- `DeedOfEnding` (`:1029-1036`) — единственный существующий крючок завершения;
- `Effects` во всей папке `Probe/Householder/` упоминаются ровно в двух файлах: `Householder.cs:316`
  и `HouseholderDecision.cs:430-462`, где они и живут полем proposal.

## РАЗРЕШЕНО

Поверхность расширяется на два файла-носителя:

- `Probe/Householder/HouseholderDecision.cs` — типизированное слово timing на proposal;
- `Probe/Householder/HouseholderActiveResponse.cs` — донести timing и партию эффектов до
  существующего `DeedOfEnding`.

**Итого четыре правленых продуктовых файла** (`Householder.cs`, `HouseholderDecision.cs`,
`HouseholderActiveResponse.cs`, загрузчик) — **порог пяти не превышен, наряд продолжается.**

Шесть `Householder*DecisionHandler.cs` остаются занятыми полосой `почему` и не трогаются:
принято его решение добавлять timing внешней обёрткой загрузчика.

## МИНА, КОТОРУЮ ОН НЕ НАЗВАЛ, А ОНА ВЗОРВЁТСЯ МОЛЧА

`HouseholderActiveResponse.WithProgress` (`:54-68`) пересобирает отклик через синтетический
proposal и передаёт **`null` вместо эффектов** — строка `null,` внутри вызова. Значит новое поле,
добавленное в конструктор, **будет обнуляться на каждом тике прогресса**, и это не уронит ни
компиляцию, ни один тест: отклик просто тихо потеряет партию по дороге к завершению.

Это ровно тот класс, о котором предупреждает комментарий в `HouseholderStanding`: пропущенная
строка «компилируется, ничего не бросает, ни один тест не краснеет и молча стирает поле на каждом
тике». **`WithProgress` обязан протащить новое поле, и на это нужен отдельный тест.**

## ЦЕНА РАЗРЕШЕНИЯ, КОТОРУЮ НАДО ЗНАТЬ

`HouseholderDecision.cs` производит **пять** присваиваний исхода
`HouseholderDecisionCandidateOutcome` (строки 651, 661, 662, 665, 761). Это поверхность задачи
`t-refusal-reason-1` полосы `почему`. **Значит `почему` теперь ждёт, пока эта нога доедет в `main`.**
Полоса ещё не стартовала, порядок не ломается — но зафиксировано, чтобы не столкнулись.

## ДОПОЛНЕНИЕ К ВОЗВРАТУ

Сверх названного в наряде:

1. **Чем доказано, что `WithProgress` не теряет партию** — тестом, а не чтением.
2. Число правленых продуктовых файлов итогом — ожидается четыре.
3. Форма слова timing: как оно называется в профиле и как — в типе, и почему это одно и то же
   понятие, а не два.

END_OF_FILE: live/indie-game-development/work/2026-08-31-call-arrival-hook-1.md
