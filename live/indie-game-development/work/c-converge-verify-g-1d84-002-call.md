# CALL — c-converge-verify-g-1d84-002

to: session
direction: indie-game-development
play: converge-verify
node: g-1d84
issued: 2026-08-04 by s-converge-arch-g-1d84-002
status: ready

## goal

Свежая, ОТДЕЛЬНАЯ от работы сессия судит набор g-1d84 заново после того, как вторая `converge-arch`
отработала семь строк, возвращённых первой проверкой. Судится набор целиком —
`work/converge-g-1d84.md` (§WHAT, §GLOSSARY, §COVERAGE, §SIGNOFF, §CONTRACTS, §ARCH-002) и
`work/converge-g-1d84-arch.md` (A1-A11, поправки A2/A4/A8, §MINER, §MINER-002, §SIGNOFF).

Это ВТОРАЯ проверка узла. Первая (`s-converge-verify-g-1d84-001`, 2026-08-04) вернула
`complete=FAIL smuggling=FAIL` и семь строк. **Потолок второго FAIL действует: если эта проверка тоже
вернёт FAIL, решение о том, что делать с узлом дальше, принимает ВЛАДЕЛЕЦ своими словами, а не
следующая нога.** Проверка обязана назвать это в своём RESULT прямо и предложить ему ветки.

## context

**Что изменила нога 002, построчно — сверять надо именно это:**

1. **F1 «сервисы» — закрыто словами владельца.** Он сказал в чате 2026-08-04: «Сервисы, это я обычно,
   когда пишу сообщение, я не пишу, я использую голосовой ввод. Это что-то не так, значит,
   распознало. Сервисы не надо искать. Это ошибка, это что-то не так распозналось» и «нужно почистить
   от этого слова». Заведена строка W106; ряд §GLOSSARY НЕ заводится и значение НЕ изобретается.
   Чистка текста критерия 8 — правка `TREE.md`, маршрут map.
2. **F2 — заведена W107 `open → map`** (сколько комнат и есть ли обстановка). Число не изобретено.
3. **F3 — W06 понижена в `open → PLAN`, заведена W06a с опытом.** Измерено первой рукой на
   `839df47e`: два блока `PrefabInstance` (`Lobby.unity:1512`, `NetworkWalkers.unity:167`), 22 записи
   переопределений, у всех `objectReference: {fileID: 0}`. Диск вопрос не решает; опыт требует Unity
   у владельца. Снятый сторож возвращён в долг (поправка A4).
4. **F4 — W52 возвращена в `open → владелец`, заведена W52a с ценами** ($129 Indie; бесплатный тир
   AGPL непригоден; MIT/BSD-альтернативы существуют; ручная проверялка 1,5-3 дня + 0,5-2 ч на поле).
   Формат нога НЕ выбирала (поправка A2).
5. **F5 — построчное понижение с названным преемником** в §CONTRACTS «Счёт этой ноги»; два счёта файла
   сведены к одному.
6. **F6 — W80 понижена в `open → владелец`** (поправка A8).
7. **F7 — §SIGNOFF поправлен: слово на набор в целом «фиксирую»**, по RESULT converge-ноги.

Плюс §MINER-002: три новых незаданных вопроса (M5 проходимость и курьер, M6 зрение и перекрытие,
M7 адресация профиля), у каждого назван отвечающий, ни один не отвечён выводом ноги.

**Счёт после правок, который надо пересчитать механически:** строк §WHAT 111; `answered` 86;
`acceptance` 26; `улика` 3; `open_rows` 22 = 21 `open` + 1 `deferred`, из них владельцу 2 (W52, W80),
PLAN/ADR 17, map 2 (W94a, W107), `deferred` 1 (W75).

**Объявленная база продуктовых фактов:** `C:/projects/Unity/GasCoopGame`, `main` = `origin/main` =
`origin/dev` = `839df47e`, рабочее дерево чисто. Перепроверить базу первой рукой до любого `факт кода`.

## boundaries

- Проверка НИЧЕГО не отвечает и НИЧЕГО не чинит. Находка возвращается строкой, а не заполняется.
- Владельцу не выносить ни одного нового решения. Два вопроса, ждущие его слова (W52 формат и W80
  «инструменты»), уже стоят в `NOW.md` `decisions` — проверка их не переформулирует и не решает.
- Ставку, задачи и полосы не создавать. Разработку не запускать. `TREE.md` не трогать.
- Свежая физическая сессия, отдельная от ноги 002 и от любого day-чата.

## done_when

1. Вердикт `complete` и `smuggling` вынесен по набору целиком, с построчным обоснованием.
2. Каждая из семи прежних находок проверена на предмет «починено ли ИМЕННО ЭТО» — и отдельно на
   предмет «не завезла ли починка новую невыполненную опору».
3. Счёт файла пересчитан механически и сверен с §SIGNOFF и §CONTRACTS: два счёта обязаны сойтись.
4. Сила каждого токена владельца проверена на «не выше настоящей», включая новые слова про «сервисы».
5. Три новых пробела §MINER-002 проверены на то, что у каждого назван отвечающий и ни один не закрыт
   выводом ноги.
6. При FAIL — потолок второго FAIL назван прямо и владельцу предложены ветки его словами.

## return

Полный Direction RESULT. При PASS — `next` = `shape`. При FAIL — решение владельца о ветке.

budget: одна свежая сессия

END_OF_FILE: live/indie-game-development/work/c-converge-verify-g-1d84-002-call.md
