# CALL — c-work-g-5a7c-telekinesis-call-001

to: session
play: work
direction: indie-game-development
node: g-5a7c
task: t-scale-5
issued: 2026-08-08 by s-repair-g-5a7c-wave-reshape-001
status: ready

## goal

Существует готовый к отправке наряд исполнителю на `t-scale-5` — телекинез лучом.

## context

- **Волна пересобрана владельцем 2026-08-08 и телекинез стоит теперь ПЕРВЫМ.** Причина его же:
  волна покупает три вещи — телекинез, вертикальность, пропорции; всё остальное берётся
  примитивами, дом отложен целиком. Телекинез первый потому, что работает уже сегодня, ничего не
  ждёт, и на нём висит порог смерти ставки (`kill_by` 3): он трогает луч и говорит, так носить
  можно или нельзя. Разбор: `history/2026-08-08-s-repair-g-5a7c-wave-reshape-001.md`.
- **ОБЯЗАННОСТЬ, которую нога исполняет до написания наряда:** проверить происхождение КАЖДОЙ
  несущей строки `done_when` задачи `t-scale-5` — чьи это слова и где они лежат. За один день эта
  проверка поймала три случая, где замер стоимости или наблюдение о коде затвердели как приказ об
  устройстве. Строка, у которой источник не находится, идёт владельцу вопросом, а не в наряд;
  строка, чей источник — замер, формулируется как ИСХОД, а не как место в коде.
- **Что задача несёт по карточке:** длина луча становится переменной на держателя вместо общего
  смещения; предмет выбирается прицелом И близостью — кандидат тот, на кого смотришь И кто в
  пределах дальности (его вердикт, `W121`); владелец берёт груз, не подходя вплотную, и ведёт его.
  **Закрывает задачу его слово о том, управляемо это или нет.**
- **Правило про лут, принятое им 2026-08-08, задевает эту задачу напрямую:**
  `knowledge/loot-is-inactive-until-a-beam-touches-it.md`. Лежащий лут неактивен — не шумит, не
  двигается, сдвинуть его нельзя, но мыши он мешает пройти; единственный способ его тронуть —
  луч. Наряд обязан это учитывать: «взять лучом» становится в том числе переходом лута из
  неактивного в живое. Строить сам переход целиком здесь не обязательно — это делит границу с
  `t-scale-4`; наряд обязан провести границу явно и сказать, что берёт, а что оставляет.
- **Числа связаны, и это записанный закон:** `knowledge/scale-down-raises-mass.md`. Скорость нельзя
  двигать в одиночку — она тянет массу, демпфирование тяги и жёсткость хвата, иначе установившаяся
  тяга пробивает потолок руки и хват рвётся. Любая настройка луча, задевающая скорость переноски,
  задевает и их.
- **Известная кривизна, которую здесь НЕ чинят:** единица силы в контакте занижена примерно в
  1.2 раза — `Network/CargoBodyContact.cs` делит импульс на `Time.fixedDeltaTime` (0.02), а судья
  шагает ≈0.0167. Принадлежит слою шума. Названо, чтобы наряд не принял расхождение за свою ошибку
  и не полез чинить.
- **Что уже стоит в мире:** налётчик 0.16 м, скорость 0.6 м/с, груз 19×4×4 см массой 20, потолок
  числа рук `_cargoMaxHolders: 2` при цели волны в четверых. Продуктовая база — `ae023ba5`
  (`main` = `dev`); перемерить первой рукой и сверить `engineering_contract` с продуктовым стампом.
- **Слот WIN-U1 свободен и чист** — перемерено 2026-08-08: `AVAILABLE`, `lease: none`, вершина
  ровно на базе. Unity в слоте поднимает владелец.

## boundaries

- Наряд не решает за владельца ни одного вопроса продукта и геймплея.
- Вертикали здесь нет — ни у мыши, ни у груза: это `t-vert-1` и `t-scale-4`.
- Четверо за один груз — `t-scale-6`, здесь потолок числа рук не поднимается.
- Дом, обстановка, облик и слой шума не трогаются; пороги `60 / 150 / 400` не двигаются.
- Общей настройки масштаба не заводить и ничего от неё не выводить.

## done_when

Наряд самодостаточен и отправляем: цель, границы, `done_when` и возврат стоят на строках
`t-scale-5`, происхождение каждой несущей строки проверено и названо, граница с `t-scale-4` по
переходу лута из неактивного в живое проведена явно, `engineering_contract` перемерен, лимит v36 в
три пункта соблюдён, и всё, что наряд утверждает про устройство проекта, перемерено первой рукой.

## return

RESULT ноги: наряд в `work/`, зарегистрированный карточкой `call`, плюс честно названное то, что
наряд решил НЕ включать, и любая строка задачи, чьё происхождение не нашлось.

## budget

Одна сессия.

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-telekinesis-call-001.md
