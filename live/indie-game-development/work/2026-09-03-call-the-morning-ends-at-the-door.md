# CALL: утро кончается тем, что он выходит за дверь

Задача: `t-the-morning-ends-at-the-door-1` · волна `bet-g-5a7c-wave-12` (рубеж Р1, 11.09) · полоса `утро`
Слот: **WIN-U3** · репозиторий: `C:\projects\Unity\GasCoopGame_win-u3`
Слот `AVAILABLE`, аренда `none` — проверено в `gascoop-slot-state.v1.json` 2026-09-03.
Основание: **`main` = `dev` = `2d2c27ff`.** База тестов: headless `819/819/0` — **перемерь и назови своё число.**

**ЭТО ГОЛОВА ВОЛНЫ И САМАЯ ТЯЖЁЛАЯ СТРОКА РУБЕЖА. У НЕЁ НЕТ ЗАПАСНОГО ПУТИ: «без этого демо нет».**
Нарезка признаёт честно — **это не полдня.**

## 0. ЧТО УЖЕ ЛЕЖИТ ГОТОВЫМ, ЧТОБЫ НЕ СТРОИТЬ ВТОРОЙ РАЗ

Замерено review 03.09 и перепроверено нарезкой на `2d2c27ff`:

- **Распорядок — данные.** `Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`:
  шесть строк — подъём и **пять одинаковых `walk-route` по 12 секунд**; занятий четыре
  (`get-up-from-the-bed`, `put-the-coffee-on`, `make-his-lunch`, `come-back-to-the-coffee`).
- **Место «дверь» уже есть.** `Assets/TunnelCrew/World/HouseBuilding/first-house.house.json:588` —
  `"name": "Front door"`. Дом — данные, `walls` пустой, стены вычисляются.
- **Глаголы авторятся из профиля.** `Profiles/HouseholderProfileLoader.cs`. Словарь мозга сегодня —
  четыре глагола: `walk-route`, `walk-toward`, `perform-occupation`, `perform-routine-occupation`.
- **Канал поступков построен и расширяем.** `Probe/Householder/HouseholderVocabulary.cs`,
  `HouseholderDeedName` — его собственный комментарий говорит, что второе слово стоит только
  исполнения, потому что канал уже есть.
- **Запас времени и давление работают** — волна 11, принято его глазом 02.09.

**ЧЕГО НЕТ, И ЭТО ГЛАВНОЕ.** `leave-for-work` встречается в `Assets/**` пять раз: дважды как данные
профиля и трижды в комментариях `HouseholderProfileLoader.cs:880, :3613`. **Ни одной строки кода,
приводящей хозяина в это состояние, нет.** Это имя срока, против которого строка взвешивает себя.
И **сигнала «раунд кончился» наружу не существует вовсе** — улика `i-a-run-has-no-end` говорит то же
про заход с июля.

## 1. ФОРМА НАЗЫВАЕТСЯ ДО ПЕРВОЙ СТРОКИ КОДА

Что добавляется новым, что придётся тронуть, почему эта форма, а не соседняя.
**Переделываешь больше, чем добавляешь — остановись и верни фактом.**

## 2. done_when

В карточке `t-the-morning-ends-at-the-door-1`, восемь строк: пятый глагол; дверь как цель;
терминальная строка распорядка вместо хвоста из пяти переходов; сигнал конца раунда наружу; утро
5–8 минут вместо 120 секунд; строка «давление сжимает план и не ускоряет ходьбу»; ноль строк
содержания; числа в настройках и профиле.

## 3. ГЛАВНЫЙ ПОРОГ ЭТОЙ НОГИ

**ЕСЛИ ПОНАДОБИТСЯ ЗАВЕСТИ УПРАВЛЯЮЩИЙ РАУНДОМ, НОВУЮ СЦЕНУ-ОБОЛОЧКУ ИЛИ ВТОРОЙ ИСТОЧНИК ПРАВДЫ О
ТОМ, ЧТО РАУНД ИДЁТ — СТОП И ЧИСЛА ДОМОЙ.** Это жизненный цикл, а не глагол, и решать его не ноге.
Именно это допущение волна назвала своим самым рискованным.

## 4. ОСТАЛЬНЫЕ ПОРОГИ

1. **Тронут `Network/CargoBody.cs` или что-либо в `Network/**`** — прогноз волны был неверен, скажи
   это фактом. Ожидается, что задача живёт в `Probe/Householder`, `Profiles` и профиле.
2. **Заведена хоть одна строка содержания утра** (кофе → кейс → одежда, список на холодильнике,
   привычка, табличка старта) — откат. Квитанции `concept_basis` нет.
3. **Пришлось погасить тест** — запрещено абсолютно.
4. **Появился источник вида «событие отняло N секунд»** — откат. Запас времени **выводится**, а не
   накапливается: жёсткий инвариант утверждённой им спеки давления.

## 5. ЧТО ВЕРНУТЬ

RESULT по схеме: форма пятого глагола и терминальной строки; чем именно наружу сообщается конец
раунда и кто это слышит; новые числа профиля и почему такие; свои числа тестов; и отдельной
строкой — **что осталось до того, чтобы владелец прошёл утро от будильника до двери руками.**

END_OF_FILE: live/indie-game-development/work/2026-09-03-call-the-morning-ends-at-the-door.md
