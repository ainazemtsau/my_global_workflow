# CALL — c-converge-arch-g-5a7c-repair-001

to: session
direction: indie-game-development
play: converge-arch
node: g-5a7c
issued: 2026-08-07 by s-converge-verify-g-5a7c-001
status: waiting — дожидается возврата `c-converge-g-5a7c-repair-001`

## goal

Починить в `work/converge-g-5a7c-arch.md` и в §CONTRACTS файла `work/converge-g-5a7c.md` шесть
именованных мест, которые вернула первая проверка узла. Архитектурный разбор НЕ переоткрывается:
шесть выводов из восьми проверка подтвердила как настоящие выводы по опровержению, полнота контрактов
по дереву подтверждена, `arch_in_context_only: PASS` подтверждён отдельно.

## context

**Вердикт проверки:** `verify_target: build complete=FAIL smuggling=FAIL`. Полный разбор —
`history/2026-08-07-s-converge-verify-g-5a7c-001.md`. Это был ПЕРВЫЙ отказ; следующая проверка будет
ВТОРОЙ, и по плею второй FAIL кончает бумажную фазу узла. Значит эта нога закрывает возвращённые
строки настоящим источником ЛИБО честно оставляет их `open` с названным отвечающим.

**База продуктовых фактов, перепроверена проверкой первой рукой:** `C:/projects/Unity/GasCoopGame`,
`main` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`, дерево чисто, только чтение.

**Что проверка ПОДТВЕРДИЛА и что переделывать запрещено.** `A1`, `A2`, `A3`, `A5`, `A6`, `A7` —
настоящие выводы по опровержению: каждое отпадение варианта перемерено (`noEngineReferences: true`;
потеря высоты между `HouseWall.WorldBounds` и `CarryStand.cs:71-76`; `m_LocalScale`; числа движка
`0.01`/`0.02`; статический `DeliveryPoint.ActivePoints`; одинаковые тройки имён у события и факта).
`A2` названа самой крепкой строкой набора. Полнота §CONTRACTS по дереву (12 краёв + 14 швов),
`contracts_open: 1` (`C05`), `arch_open: 8`, «вердиктов владельца один, подписей ноль», «карточка не
тронута ни знаком» — всё сошлось механически. Маршрут `C07` подтверждён по всем трём источникам
дословно и переписыванию НЕ подлежит. Сила токена «А, обстановка стоит» не повышена ни в одном из
четырёх мест, и оговорка про «несущие задевают» честно записана прочтением, а не его словом.

## boundaries

- `TREE.md` и `CHARTER.md` не трогать. Ставку, задачи и полосы не создавать. `knowledge/` не писать.
- §IMPORT, §GLOSSARY, §WHAT, §DRIFT и оба §SIGNOFF файла `work/converge-g-5a7c.md` не трогать ни
  строкой — их правит `converge`, и предыдущая арх-нога это правило выдержала побайтово. Держать.
- Продуктовый репозиторий только на чтение.
- Владельцу НЕ выносить ни одного нового решения. Понижение строки в `open → OWNER` — это маршрут, а
  не вопрос: отвечающий называется, вопрос формулирует та нога, которая до него дойдёт.
- Ни одна строка арх-файла не становится требованием и не едет в `done_when`. Держать
  `arch_in_context_only: PASS`.

## done_when

1. **`C26` перестаёт объявлять куплённым то, чего карточка не покупает.** Сегодня строка говорит:
   «состояние двери «открыта / закрыта» принадлежит ОПИСАНИЮ ДОМА… и **куплено оно подписанным
   критерием 6** — «стены и закрытые двери ослабляют»: ослаблять нечему, если состояния нет».
   Контрпример лежит внутри самого набора: `W63` пишет — «если двери не открываются, ослабление
   становится **статическим свойством стены**». Дверь, закрытая всегда, исполняет букву критерия 6
   целиком и переменной не требует. Критерий 6 покупает ОСЛАБЛЕНИЕ у двери; открывается ли она
   вообще — это `W63`, и это его ВХОД с двойной оговоркой («должны как-то же закрываться. **Возможно,
   и не будут**»), а не его ответ. Несущее предложение понижается в `open` с названным отвечающим
   (владелец внутри задачи, дошедшей до `W63`). Остальное в строке верно и остаётся: измеренный факт,
   что дверей как вещей сегодня нет (`Doorway` — ярлык назначения места `HousePlace.cs:13`, проход —
   ребро графа с шириной, поиск на `isOpen|isClosed|passable|blocked` даёт ноль состояний), и цена
   физической двери против падающего предмета. То же исправление делается в подразделе `A9`, откуда
   вывод пришёл.
2. **`A4` теряет утверждение, что критерий 3 куплен даром.** Строка говорит: «это НЕ четыре режима к
   постройке, а следствие формулы, которая уже написана и уже работает». Следствием формулы это
   является — но с другим порогом, чем подписано. Арифметика на числах самого набора: `W07b` задаёт
   массу 50 кг, `A4` включает `useGravity`, значит вес 490.5 Н; потолок на руку 200 Н
   (`CargoHaul.HandForce` → `Cap(…, MaxHandForce)`, сумма по рукам в `Reduce`). Одна рука — 200 Н,
   две — 400 Н, ни та ни другая веса не перебивают; три — 600 Н. Его текстура `I09` («**два уже могут
   выше**») и подписанный критерий 3 при этих числах невыполнимы на второй ступени. Утверждение
   «даром» понижается; порог по числу рук называется вопросом с отвечающим и ответом НЕ заполняется.
   Сам выбор варианта (а) в `A4` проверкой не оспорен — оспорено только это утверждение.
3. **Пол получает своё место в разборе.** `A4` включает гравитацию, и с этого момента груз лежит и
   волочится по полу. Измерено проверкой первой рукой: в `IntegratedHouse.unity` есть `Floor`
   (`MeshCollider`) и `Delivery Yard Floor` (`BoxCollider`); ни на одном нет `HouseWall`, значит пола
   нет в описании дома вовсе (`CarryStand.ReadAuthoredWalls` читает только `HouseWall`); при этом
   `CargoBodyContact.Record` срабатывает на ЛЮБОМ столкновении груза, а `CargoBodyPort` объявляет
   «The house is the only physical thing in this world the cargo can meet» — объявление становится
   ложным ровно в этот момент. Разбор обязан сказать две вещи и обе — с опровергнутыми вариантами:
   входит ли пол в описание дома по `A2`, и становится ли контакт с полом источником общего слоя по
   `A5`. Если бумагой это не решается — строка помечается `open` с отвечающим. Заодно проверяется, не
   покупает ли `A4` даром половину критерия 4 (одна рука не поднимает → груз на полу → контакты идут;
   четыре поднимают → контакты кончаются): если покупает — это надо сказать, потому что сегодня этого
   не говорит никто.
4. **Прочтение «несущие задевают» понижается в `open → OWNER`.** `A9` записывает его честно
   прочтением («Прочтение опирается на текст карточки, а не на его ответ»), но оставляет рабочим, а
   `C25` не упоминает вовсе. Оно несущее и перемерено: у ходоков коллайдеров нет нигде
   (`NetworkWalkerPresentation.cs:203-205`), `CargoNoise.cs:57-64` объявляет прямо — «a walker is not
   a physical body in this game and the engine can never report a collision with one». Значит второе
   прочтение (задевает ТЕЛО человека) требует отдельного правила в слое правил, а первое не требует
   ничего: выбор между ними и есть решение, есть ли эта работа. Сужение стоит не только против
   критерия 5, но и против `CHARTER.md:92` — «несущие задевают обстановку и попадают в то, что хозяин
   расставил, — и именно по этому он их находит». Это вопрос геймплея и по G7 он его. Понизить с
   названным отвечающим; вопрос ему здесь НЕ формулировать.
5. **`A9` перестаёт противоречить сам себе про силу его уточнения.** Преамбула подраздела говорит
   верно: «СИЛА ЗАПИСАНА КАК ПРЕДПОЛОЖЕНИЕ, А НЕ КАК ТРЕБОВАНИЕ… он открыл фразу словами «я сейчас
   не могу точно сказать» и закрыл «я думаю, что нет»». Таблица классов через шесть строк говорит
   обратное: уточнение «делает его **СИЛЬНЕЕ** прежнего вердикта». Хеджированное высказывание не
   усиливает нехеджированный вердикт. Практического следствия нет («работы это не добавляет ни
   строчки»), но это единственное место во всём наборе, где сила токена записана выше настоящей.
   Правится одним словом; та же формулировка стоит в `LOG` за `s-converge-arch-g-5a7c-003` и там
   остаётся как историческая запись, а не правится задним числом.
6. **`A8` теряет решённым вопрос управления.** Выживший вариант (б) перечисляет три изменения, и
   первое — «ГДЕ берётся хват (**прицел вместо близости**)». Оно ни с чем не сравнивалось и ничем не
   опровергалось: появляется только внутри текста выжившего варианта. Карточка этого не говорит —
   кр. 3 говорит «берут и несут **на расстоянии**, а не хватом за корпус», то есть про дальность
   НЕСЕНИЯ, а не про способ ВЫБОРА предмета; близость на расстоянии тоже возможна, сегодня
   `_cargoPickupRange: 1.3` — при росте 0.16 м это восемь ростов. Чем берут предмет — это управление,
   то есть его область, и сам набор это признаёт в `M7` («как ощущается управление лучом… не
   архитектура, а его глаз»). Эта треть `A8` понижается в `open` с отвечающим; остальные две трети
   держатся на измеренных швах (`CargoHaul.AtPoint`, `CargoHold` в системе груза, `Reduce` на список
   любой длины) и не оспорены.
7. Счёт арх-файла и §CONTRACTS обновлён под то, что получилось, и **говорит то же самое, что скажет
   §WHAT после ремонтной `converge`-ноги.** Расхождение двух счётов — отдельная находка проверки
   (`W103` против `C07`), и повториться оно не должно.

## return

Полный Direction RESULT. `state_changes` — `work/converge-g-5a7c-arch.md`, §CONTRACTS файла
`work/converge-g-5a7c.md`, `NOW.md` (`open_calls`) и `LOG.md`. В `next` — свежая `converge-verify`
отдельной физической сессией; она будет ВТОРОЙ проверкой узла, и это надо сказать в её наряде прямо.

budget: одна сессия

END_OF_FILE: live/indie-game-development/work/c-converge-arch-g-5a7c-repair-001-call.md
