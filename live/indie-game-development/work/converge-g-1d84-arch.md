# converge-g-1d84-arch — архитектура на бумаге

leg: s-converge-arch-g-1d84-001 (2026-08-04, владелец в чате)
call: `work/c-converge-arch-g-1d84-001-call.md`
reads: `work/converge-g-1d84.md` (§WHAT закрыт), `TREE.md` g-1d84, `knowledge/how-the-game-is-built-layers.md`,
      `knowledge/host-shared-vocabulary.md`, `knowledge/no-checks-the-owner-makes-by-eye.md`, `CHARTER.md`

**СТАТУС ЭТОГО ФАЙЛА.** Это архитектура НА БУМАГЕ. Она едет в `context` инженерного наряда как входная
улика и НИКОГДА в `done_when`. Обязывающий ADR принимает PLAN в продукте; если бумажный лучший вариант
не выдержит в коде, PLAN эскалирует обратно. Ни одна строка ниже не является требованием узла.

**Объявленная база продуктовых фактов.** Всё, помеченное `факт кода`, проверено первой рукой в
`C:/projects/Unity/GasCoopGame`, `main` = `origin/main` = `origin/dev` = `839df47e`, рабочее дерево
чистое, только чтение.

**Чем эта нога НЕ является.** Внутрисессионного веерного опровержения здесь не было: нога прошла
одним контуром по девяти названным строкам и краям, читая состояние и продукт первой рукой. Биндинговая
проверка — отдельная свежая `converge-verify`, и она судит этот файл заново. Записано так, чтобы никто
не прочитал разбор как уже опровергнутый.

---

## §РЕШЕНО ВЛАДЕЛЬЦЕМ 2026-08-04 — три подписи и их точная сила

| id | что подписано | его слова | сила токена |
|---|---|---|---|
| **П1** | Правда о доме — вариант В: дом собирается руками в сцене, при старте с неё снимается описание, дальше правила работают только с описанием | **«вопрос один, я с тобой согласен, да»** | прямое согласие на показанный вариант; варианты были названы и объяснены поимённо |
| **П2** | Швы сцены — вариант Б: каждый крупный кусок живёт своей заготовкой-префабом, в сцене один объект «здесь всё соединяется» | **«насчет вопроса два я, наверное, с тобой согласен»** | согласие с хеджем «наверное»; записано именно так и сильнее цитироваться не может |
| **П3** | Форма файла профилей — вариант А: строгий формат со схемой | **«Мне, в принципе, все равно… Если ты считаешь, что лучший вариант А, я принимаю»** | принятие ПО ДОВЕРИЮ к рекомендации, а не самостоятельный вердикт по содержанию. Он прямо назвал себя безразличным. Никто не вправе цитировать это как его собственный выбор формата |

**Три добавления владельца в тех же словах, каждое меняет работу:**

- **Д1 (к П1).** «Unity нам позволяет и теги разные, да, и компоненты навешивать, которые помогают чтению.» —
  разметка сцены компонентами признана им законным способом авторства.
- **Д2 (к П1), приглашение к спору.** «Для нас важно будет… понимать комната, где, что в комнате находится.
  Может быть даже, да, комната в комнате… в какой части. …я сейчас не могу представить, что нам это надо
  будет, да, ты можешь это оспорить… если это условно недорого стоит, да, если какая-то древовидная
  структура, то как бы почему бы и нет.» — разобрано в A10, спор проведён.
- **Д3 (к П2), новое требование к форме работы.** «Я-то не против разных сцен, чтобы мы работали…
  у него своя сцена… мы можем условно замокать, если что-то не готово, если готово, подтянуть…
  Просто, ну, я хочу, чтобы соблюдался порядок, чтобы сцены не создавались по каждому чиху, и в каждой
  сцене вот такого функционала должно быть, ну, реально удобные инструменты, там, либо через Odin
  Inspector, либо еще как-то, это не обязательно в рантайме… Условно, чтобы я мог разные условия там
  составлять, ну, типа так потестил, так потестил.» — разобрано в A11.

---

## A1 (строка «где живёт правда о доме») — ПОДПИСАНО П1

**Вопрос.** Дом — это комнаты, стены, проёмы, смысловые места, участки тоннелей и их проходимость.
Где живёт описание, по которому судья отвечает «тут пройдёт / тут нельзя / это кухня»?

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Правда в сцене: правила спрашивают у движка (коллайдеры, `Physics`, NavMesh) | **ОТКЛОНЁН.** `how-the-game-is-built-layers.md:14` требует, чтобы каждое правило умело работать ТОЛЬКО у судьи; запрос к движку этого не умеет. Проверить в тесте и в одиночку нельзя. Разбор готовых инструментов п.4: «Без него правда о доме окажется в коллайдерах Unity, то есть судьёй станет физика движка — тот же класс, что NavMeshAgent, только на уровне уровня.» Прецедент отклонённой работы `a-2` — тот же класс |
| (б) Правда в данных, сцена целиком генерируется из них | **ОТКЛОНЁН КАК ЕДИНСТВЕННАЯ ФОРМА.** Работает и уже работает: `CarryStand.cs:70-88` строит стены из чисел `NetworkPlaySettings`, и `WalkSpaceProbe.cs:26-45` отвечает «пройдёт ли мышь» по тому же списку — одно описание, два потребителя, разъехаться не могут (`факт кода`). Но авторство настоящего дома числами проваливает линзу 3 (реально ли одному) и линзу 2: владелец дом глазами не соберёт |
| (в) Сцена — поверхность авторства, описание снимается при старте, дальше правила работают только с описанием | **ВЫЖИЛ И ПОДПИСАН.** Уже работает: `HouseholderRouteController.cs:101-132` при `Awake` превращает `Transform[]` и точку прослушивания в `HouseholderPoint[]`, и дальше `Householder.Step` про Unity не знает (`факт кода`). Дом собирается руками и виден глазами; правила гоняются в тесте и в одиночку |
| (г) То же, плюс описание ЗАПЕКАЕТСЯ в текстовый файл на этапе редактора; рантайм читает файл | **НЕ ОТКЛОНЁН, ОТЛОЖЕН.** Добавляет: дом становится таким же текстом, как профили — виден в дифе, читается ассистентом, тесты без Unity, будущее копание читает тот же файл. Цена: кнопка перезапекания и сторож «сцену поменяли, файл забыли». Это законный класс проверки (`no-checks-the-owner-makes-by-eye`): ошибка выглядит как работающая игра |
| (д) Покупной редактор уровней (Dungeon Architect, PGG) как источник правды | **ОТКЛОНЁН.** Разбор готовых решений п.4: ни один не даёт слою правил знание дома, все делают КАРТИНКУ. Плюс `GenerateOnStart`/`RandomSeed` включены у PGG по умолчанию и пересоберут уровень со случайным зерном отдельно на каждой машине; Dungeon Architect в проекте 1.22.0 при фиксе компиляции под Unity 6 в 1.23.0. Остаётся законным ИНСТРУМЕНТОМ авторства на этапе редактора, никогда правдой |

**НЕСУЩЕЕ СЛЕДСТВИЕ, ради которого вариант выбран именно так.** Решение — не «сцена или файл».
Решение в том, что **описание дома является отдельной названной вещью слоя правил, у которой может быть
больше одного производителя.** Снятие со сцены — производитель №1, запечённый файл — производитель №2.
Тогда переезд (в)→(г) является ДОБАВКОЙ, а не переписыванием, что и есть постоянное правило владельца
`i-owner-standing-rule-extend-never-rebuild-001`. Если описание не назвать отдельно, переезд станет
переделкой всего, что дом читает.

**ВТОРОЕ НЕСУЩЕЕ СЛЕДСТВИЕ, выведенное из критерия 5, а не выбранное.** Профиль — текстовый файл,
который пишет ассистент, никогда не открывающий Unity. Значит места дома обязаны иметь **имена,
написанные текстом**, иначе профиль физически не может сказать «в восемь утра иди на кухню». Это не
предпочтение — это то, что критерий 5 делает с критерием 6.

**Цена, названная честно:** у каждого места дома появляется поле «имя», которое надо заполнять руками,
и загрузка обязана отвергать дубли и пустые имена, называя место ошибки. Это та же машинерия отвержения,
что у профилей, — пишется один раз на двоих.

**Риск:** описание живёт только в памяти запущенной игры; вне Unity его не посмотреть, пока не сделан (г).

---

## A2 (строка «форма файла профилей») — ПОДПИСАНО П3

**Вопрос.** В каком виде лежит текстовый файл профиля, который составляет и правит code assistant и
который игра обязана отвергнуть с понятной причиной, называющей место ошибки.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Строгий формат (JSON) с published-схемой | **ВЫЖИЛ И ПРИНЯТ.** Три проверенных довода. (1) `факт кода`: `com.unity.nuget.newtonsoft-json` версии 3.2.1 уже стоит в `Packages/manifest.json` — читалка есть, ставить нечего, и она сообщает номер строки и позицию, то есть «место ошибки» закрыто чужим готовым кодом. (2) Схема формата — стандарт, который ассистент понимает без объяснений, и тот же файл-схема СРАЗУ работает проверялкой: критерий 5 требует «описание формата» и «отвержение с причиной», и оба закрываются ОДНОЙ вещью. (3) Читалка работает без движка, то есть влезает в слой правил с `noEngineReferences: true` (`факт кода`: `TunnelCrew.Core.asmdef`) |
| (б) Читаемый формат (YAML) | **ОТКЛОНЁН, но не как плохой.** Читается глазами заметно лучше, и это не пустяк. Отклонён по двум механическим причинам: значимые отступы — частый отказ ассистента, дающий файл, который ЧИТАЕТСЯ, но значит не то (то есть неверный результат выглядит как верный — ровно класс, который глазами не ловится); и известная ловушка, где `нет` молча становится ложью. Плюс читалки в проекте нет, нужна сторонняя |
| (в) Свой формат / размеченный текст | **ОТКЛОНЁН.** Описание формата и все сообщения об ошибках пишем сами, каждое новое поле профиля — наша работа вместо чужой готовой. Проваливает линзу 3 и линзу 7 |
| (г) Unity-ассет (ScriptableObject) | **ИСКЛЮЧЁН САМОЙ КАРТОЧКОЙ**, критерий 5 и его слова про code assistant. Записан, чтобы было видно, что вариант рассматривался, а не забыт |
| (д) Код / встроенный DSL | **ОТКЛОНЁН.** Нарушает «данные — да, код — никогда» в части того, КТО эти файлы пишет |

**Честная цена (а), названная владельцу до его слова:** файлы менее приятны глазами. Смягчения,
которые НЕ являются требованиями и решаются на PLAN: поля называются по-русски; в формате есть место
под заметку-комментарий.

**Оговорка по брандмауэру КАК/ЧТО.** Слово «формат» обычно является токеном КАК и уходит в PLAN.
Здесь оно работается на этом слое ПО ПРЯМОМУ ТЕКСТУ НАРЯДА и потому, что map-нога назвала его первым
дорогим для отмены решением волны. Оно живёт в ЭТОМ файле как входная улика и НЕ попадает ни в
`done_when` узла, ни в §CONTRACTS.

---

## A3 (строка «стабильный идентификатор корня») — РЕШЕНО НОГОЙ, развилки нет

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Собственные номера Unity (fileID в сцене, GUID префаба) | **ОТКЛОНЁН.** Меняются при переоформлении сцены и слияниях; человеком и ассистентом не пишутся; вне Unity не читаются; текстовый профиль на них сослаться не может |
| (б) Строка, написанная руками, с проверкой на дубли при загрузке | **ВЫЖИЛ.** Читается, пишется в профиль, видна в дифе. Риск опечатки и дубля закрывается той же проверкой отвержения, что у профилей |
| (в) Перечисление в коде (enum) | **ОТКЛОНЁН.** Добавление комнаты становится правкой кода — против направления «данные, а не код» критериев 4 и 5 |
| (г) Путь по именам объектов в иерархии | **ОТКЛОНЁН.** Переименование и перенос молча ломают ссылку, а зависимость от иерархии — это ровно та скрытая связь, которую запрещает критерий 8 |

**Решено (б).** Развилкой владельцу не выносилось: форма вынуждена критерием 5 (профиль текстовый),
и все альтернативы отпадают механически, а не по вкусу.

---

## A4 (строка «явный интерфейс между корнями» и «скрытых ссылок нет») — ПОДПИСАНО П2

**Вопрос.** Чем ДОКАЗЫВАЕТСЯ отсутствие скрытых ссылок между корнями. Это несущая половина критерия 8:
именно она физически разрешает параллельную работу, ради которой владелец принял рамку одной карточки.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Как сегодня: всё в сцене, куски соединены перетаскиванием в инспекторе | **ОТКЛОНЁН.** Две полосы, тронувшие сцену, дают конфликт в файле сцены, который руками не сливается — то самое узкое горло, которое карточка назвала риском. Доказать «скрытых ссылок нет» нечем |
| (б) Дисциплина плюс вычитка кода | **ОТКЛОНЁН.** Именно это уже отказало: `a-2` молча приняла решение о расслоении не в ту сторону, и это стоило отклонённой работы |
| (в) Сканер исходников на `FindObjectOfType` / `GameObject.Find` / `Resources.Load` / синглтоны | **НЕ ГЛАВНОЕ ДОКАЗАТЕЛЬСТВО, но полезно и уже зелено.** `факт кода`: в `Assets/TunnelCrew/**` таких вызовов НОЛЬ, глобальных синглтонов сцены НОЛЬ; всё соединено через `[SerializeField]`. Не ловит настоящий случай — ссылку между корнями, сохранённую в файле сцены |
| (г) **Каждый корень — префаб; в сцене один объект-сборщик, раздающий куски друг другу при старте** | **ВЫЖИЛ И ПОДПИСАН.** Unity ФИЗИЧЕСКИ не позволяет ассету-префабу сохранить ссылку на объект в сцене. Значит скрытая ссылка между корнями становится невозможной ПО УСТРОЙСТВУ ДВИЖКА, а не по нашей аккуратности: доказательство не проверкой, а конструкцией. Побочный выигрыш — полосы правят разные файлы и не встречаются |
| (д) Шина событий / ассеты-события | **ОТКЛОНЁН.** Прячет, кто с кем говорит, — то есть буквально производит скрытые связи, которые критерий 8 запрещает |

**Решено (г) как главное доказательство, (в) как бесплатный побочный сторож.** Проверка (в) относится
к законному классу: она проверяет ФАКТ о тексте, а не эмулирует суждение, и владелец её глазами не
увидит. Проверка вида «в файле корня нет ссылки внутрь другого корня» пока НЕ заводится: при (г) ей
нечего ловить. Заводится только если появится случай.

**Форма явного интерфейса.** Контракт между корнями объявляется интерфейсом в СЛОЕ ПРАВИЛ, а сборщик
подаёт реализацию. Прецедент существует и работает: `IWalkSpacePort` (`Core/Multiplayer/WalkSpacePort.cs`)
объявлен в правилах, реализован сценой (`WalkSpaceProbe`) и передаётся в `CarryWorld.Advance` параметром
(`факт кода`). Изменение состоит только в том, что сегодня реализацию подаёт `[SerializeField]` курьера,
а станет — сборщик. Это добавка, не переделка.

---

## A5 (строка «шов для СПОСОБНОСТИ хозяина») — РЕШЕНО НОГОЙ

**Что есть сегодня, `факт кода`.** Названный шов РЕШЕНИЯ существует: `IHouseholderDecisionPolicy.Evaluate(snapshot)`
(`Householder.cs:406-410`) с двумя реализациями и подменой в тесте. Но ветвление по видам реакций живёт
в ДВУХ местах, и критерий 4 запрещает оба: `switch (fact.Name)` внутри политики (`:430`) и
`switch (input.PendingEvent.Name)` внутри самого `Householder.Step` (`:861`) — второй за швом не стоит
вообще. Способностей в коде нет ни одной; профилей нет.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Способность = ещё одна реализация существующего шва решения, список опрашивается по приоритету | **ЧАСТИЧНО.** Самое дешёвое, шов уже доказан. Но не закрывает второе место ветвления: способность, вводящая НОВЫЙ вид замеченного факта, всё равно требует правки `Step` |
| (б) **Два названных шва: восприятие (событие → замеченный факт) и решение (факт → предложение).** Способность объявляет обе половины | **ВЫЖИЛ.** Ложится один в один на словарь владельца: слово 2 «замеченный факт» — это восприятие, слова 3-4 «правдоподобная смена приоритета → физическое действие» — это решение. Убирает оба ветвления. Первый шов наполовину существует, второй существует целиком |
| (в) Способность = целый модуль из шести слов, со своей памятью | **ОТКЛОНЁН.** Собственная память у способности убивает единственную коробку локальной памяти — то, на чём держится и тестируемость хозяина, и его будущая передача по сети |
| (г) Способности только данными, без кода | **ОТКЛОНЁН САМОЙ КАРТОЧКОЙ.** Критерий 4 ТРЕБУЕТ, чтобы хотя бы одна способность была добавлена КОДОМ через названный шов. Плюс данные-без-кода означают интерпретатор правил — система, которую режет ограничение простоты |
| (д) Готовый фреймворк поведения (деревья поведения, utility AI, GOAP, Feel) | **ОТКЛОНЁН.** `host-shared-vocabulary.md:37-38` прямо: фраза владельца НЕ требует планировщика, взвешивания полезности, обучения и готового фреймворка. Плюс замена доказанного шва фреймворком — переписывание, а не расширение |

**Решено (б).** Два шва, оба названы по словам владельца. Развилкой не выносилось: критерий 4 плюс
принятый словарь вынуждают форму, а не оставляют выбор. **Названо как самая вероятная мишень
опровержения этой ноги.**

---

## A6 (строка «где живёт режим камеры») — РЕШЕНО НОГОЙ, вопрос оказался тремя

**Что есть сегодня, `факт кода`.** У ходока НЕТ направления взгляда вообще: `PlayerState` несёт только
X и Z, снимок везёт X, Z, Moved. Камера сетевого стенда — одна общая ортографическая сверху
(`NetworkWalkerCamera`), живёт в `LateUpdate` слоя мира и авторитетного состояния не касается.

**(1) Режим камеры (первое/третье лицо).** Живёт в слое мира, «только моё». На то, что произойдёт в
игре, не влияет — граница из `how-the-game-is-built-layers.md:53` проводится именно этим вопросом.

**(2) Принудительное переключение игрой.** Это ПРАВИЛО, и правила живут у судьи. Разрешение: правило
рождает НАМЕРЕНИЕ («сейчас смотри сюда»), слой мира его исполняет; сама камера остаётся клиентской.
В этой волне ничто переключение не вызывает — критерий 3 требует только, чтобы устройство ДОПУСКАЛО, —
поэтому строить нечего, надо лишь не закрыть шов.

**(3) Направление взгляда — авторитетно ли.**

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Взгляд едет судье и становится авторитетным состоянием | **ОТКЛОНЁН на эту волну.** Платится полем в состоянии и в снимке ради того, чем в этой волне никто не пользуется |
| (б) **Клиент переводит намерение движения в мировые координаты ДО отправки** | **ВЫЖИЛ.** «W плюс поворот камеры» превращается в направление мира на клиенте; судья получает то же самое, что и сегодня. Слой правил не меняется НИ НА СТРОКУ (`MovementInput` уже мировой X/Z), курьер не открывается |
| (в) Судья считает движение по осям экрана | **ОТКЛОНЁН.** Судья тогда обязан знать камеру каждого — камера перестаёт быть «только моим» |

**Решено (б). Взгляд в этой волне НЕ авторитетен.** Названный будущий шов: если хват или прицел
когда-нибудь начнут зависеть от того, куда смотришь, взгляд входит в авторитетное состояние одним
полем — добавкой. Сегодня хват берётся по РАССТОЯНИЮ и ближайшей точке поверхности
(`AuthoritativeCargoRoster.cs:257-274`), взгляд ему не нужен (`факт кода`).

---

## A7 (строка «глубина вложенных отвлечений») — РЕШЕНО НОГОЙ

**Что есть сегодня, `факт кода`.** Ровно ОДИН замеченный факт и ОДИН активный ответ в локальной памяти;
одновременные РАЗНЫЕ реакции бросают исключение (`Householder.cs:895-898`) — сознательная граница ПРОБЫ.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Одна ячейка, как сегодня | **ОТКЛОНЁН.** Утверждённый базис п.5 называет вложенные отвлечения, критерий 6 говорит «по дороге его может отвлечь новое событие» |
| (б) Ровно одна вложенность | Закрывает букву критерия 6. Но число «один» оказывается зашитым в код — против правила расширения |
| (в) Стек произвольной глубины | **ОТКЛОНЁН.** Нужна политика протухания забытых дел, и глубина игроку не видна: механика без видимого выхода, которую режет ограничение простоты |
| (г) **Стек, глубина — ручка в данных, по умолчанию единица** | **ВЫЖИЛ.** Устройство стековое с первого дня, поставляемое поведение — одна вложенность; поднять глубину = поменять число. Записи протухают тем же механизмом, что уже есть у замеченного факта |

**Решено (г).** Отдельно и обязательно: исключение на одновременные разные реакции ЗАМЕНЯЕТСЯ настоящим
разрешением по приоритету — поле `HouseholderPriority` для этого уже существует (`факт кода`). Это
добавка, и без неё пять различимых реакций критерия 4 просто уронят игру.

---

## A8 (строка «что значит „инструментов“ в критерии 10») — РЕШЕНО НОГОЙ, помечено как слабое

Карточка называет четыре НАБОРА (Quaternius, Kenney, KayKit, Poly Haven) и ни одного инструмента.

| чтение | чем опровергнут / чем выжил |
|---|---|
| (а) «Инструменты» = то, чем бесплатный материал вносят и собирают в дом; проба — это СБОРКА, а не сравнение | **ВЫЖИЛ.** Опирается на собственные слова критерия «и её результат виден в сцене»: результат, видимый в сцене, — это собранный угол дома, а не таблица сравнения. Плюс утверждённый базис п.8 уже называет Blender для оболочки дома, тоннелей и hero props |
| (б) «Инструменты» = отдельный список инструментов, сравниваемых наравне с наборами | **ОТКЛОНЁН.** Ни одного инструмента карточка не называет, и назвать их пришлось бы ноге — то есть нога сама сочинила бы требование. Даёт бумажную ногу вместо видимого результата |
| (в) Оговорка, считаются только четыре набора | **ОТКЛОНЁН.** Слово стоит в утверждённом им тексте; вычёркивать его нога не вправе |

**Решено (а).** Помечено честно: это ЧТЕНИЕ его утверждённого текста, а не ввоз, и оно самое слабое
в файле. Смягчение реальное: при ЛЮБОМ из трёх чтений первый шаг работы один и тот же — собрать угол
дома из бесплатного и посмотреть глазами, — поэтому расхождение ничего не стоит, пока не начнёт стоить.

---

## A9 (строка «сколько новых слоёв и входит ли камера») — ОТВЕЧЕНО

Перечень выведен из карточки против продукта на `839df47e`, а не переписан из наряда.

| # | новый слой | почему новый |
|---|---|---|
| 1 | **Дом как данные** — описание дома и его производитель | Сегодня дома нет: единственное помещение — комната 14×14 из чисел настроек. Самое дорогое для отмены (A1) |
| 2 | **Профили** — формат, читалка, отвержение с местом ошибки | Сегодня нет ни профиля, ни парсера, ни формата; настройки хозяина — двоичный ассет (`факт кода`) |
| 3 | **Хозяин под судьёй и в снимке** | Найдено этой ногой, см. §MINER M1. Наряд его не называл |

| # | НЕ новый слой | почему |
|---|---|---|
| — | **Камера** | Камера уже живёт в слое мира (`NetworkWalkerCamera`). Первое лицо меняет ей тело, а не заводит слой. Новая ОБЯЗАННОСТЬ в существующем слое одна: клиент переводит движение в мировые координаты (A6) |
| — | **Сборка сцены (сборщик и корни-префабы)** | Новый МЕХАНИЗМ, не слой: соединение уже существует, меняется место, где оно живёт (A4) |
| — | **Тоннели** | При решении A1 участок тоннеля — вид записи в описании дома, а не своё устройство |
| — | **Погоня** | Новый ВХОД в существующее правило (хозяину начинают давать положения игроков), а не слой |

**Ответ: три новых слоя, один новый механизм, одна новая обязанность в существующем слое.
Камера в новые слои НЕ входит.** Наряд называл два дорогих решения; третье (хозяин под судьёй) нашла
эта нога, четвёртое (камера) в перечень не попало.

---

## A10 — вложенность мест: спор по приглашению владельца (Д2)

Он попросил оспорить и сказал, что любой исход нормален. Спор проведён, и ответ распадается надвое.

**Половина первая — ДЕРЕВО МЕСТ, и оно не гипотеза.** Критерий 6 требует, чтобы профиль задавал
«действия ВНУТРИ комнаты». Действие внутри комнаты обязано иметь цель внутри комнаты, иначе оно не
выражается. Значит «что находится в кухне» — не будущая роскошь, а то, что критерий 6 уже просит.
Механически это ОДНО поле: у места есть родитель. Содержимое кухни — это места, чей родитель кухня;
«комната в комнате» и «в какой части комнаты» — тот же родитель, другая глубина. Снятие со сцены даёт
вложенность вообще даром: иерархия объектов в сцене УЖЕ дерево, компонент на дочернем объекте
вкладывается по построению. Цена близка к нулю, а отказ означает переавторство каждого места позже.
**Довод владельца принимается, и он сильнее, чем он сам его подал.**

**Половина вторая — ИНВЕНТАРЬ, и он отклоняется.** «Инвентарь» в его смысле — предметы со свойствами,
которые берут, используют, теряют, — это МЕХАНИКА. `CHARTER.md:91`: механика входит в объём только
явной заменой другого объёма и только его решением, названным по имени; её отсутствие никогда не
считается дефектом. Он сам пометил это как «навряд ли будет». В волну не входит.

**Различение, которое дороже обоих ответов, и его нельзя потерять: ВЛОЖЕННОСТЬ — НЕ ПРОХОДИМОСТЬ.**
«Стол находится в кухне» и «из кухни можно пройти в коридор» — два разных отношения. Дерево отвечает
на первое, граф проёмов — на второе. Слить их в одну структуру — классическая ошибка, после которой
путь хозяина начинает ходить сквозь стены либо не находится вовсе. Оба отношения дёшевы порознь и
дороги вместе.

**Решено:** место имеет родителя (дерево) и отдельно — названные проходы между местами (граф).
Инвентарь не входит.

---

## A11 — рабочие сцены и инструменты в них: требование владельца (Д3)

Его слова заводят требование к ФОРМЕ РАБОТЫ. Оно не меняет карточку и в `done_when` не идёт.

**Что принято дословно.** Рабочие сцены под отдельные куски законны; неготовое замокано, готовое
подтянуто; в каждой такой сцене — реально удобные инструменты (Odin Inspector или свои), необязательно
в рантайме, чтобы владелец сам составлял разные условия; и порядок — сцены не заводятся по каждому чиху.

**Чем это уже обеспечено.** Odin Inspector в проекте стоит (`факт кода`: `Assets/Plugins/Sirenix/…asmdef`).
Прецедент панели существует: `HouseholderRouteControllerEditor` уже даёт кнопки, рождающие события у
хозяина. Принятое знание уже разрешает рабочие сцены прямо (`how-the-game-is-built-layers.md:74`).
Решение A4 делает их ДЕШЁВЫМИ: рабочая сцена — это сборщик плюс те корни-префабы, что нужны, плюс моки
на остальное; собственного кода у неё нет.

**ГРАНИЦА, которую нельзя дать размыть.** Критерий 1 говорит: отдельные стенды результатом больше не
считаются. Рабочие сцены — ИНСТРУМЕНТ, а не поставка; результат волны по-прежнему одна сцена, где груз,
камера, хозяин, дом и тоннели работают одновременно. Записано явно, чтобы позже никто не прочёл слова
владельца о рабочих сценах как смягчение критерия 1.

**СПОР, который здесь обязан прозвучать.** Моки — это то, чем уже один раз заплатили: закрытая ставка
доказала переноску и хозяина ПОРОЗНЬ, и на `839df47e` нет ни одной сцены, где хозяин и двое сетевых
носильщиков присутствуют одновременно (`факт кода`). Если каждая полоса живёт в своей сцене с моками,
интеграционный сюрприз приезжает в конце — а именно этого волна и не должна допустить. Поэтому правило
парное, и вторая половина не менее обязательна первой: **интегрированная сцена собирается РАНО и каждый
корень попадает в неё, как только существует, а не в конце.** Это ровно то, что карточка уже сказала
своей строкой риска — «швы появляются раньше остальных полос».

**Решено:** рабочие сцены разрешены при трёх условиях — у сцены есть названный кусок, за который она
отвечает; у неё есть панель управления условиями на этапе редактора; и её корень попадает в
интегрированную сцену, как только существует. Порядок заведения рабочих сцен назначает нарезка.

---

## §MINER — какие архитектурные вопросы НЕ были заданы

Наряд перечислил девять строк. Четыре вещи в них не входили.

**M1. Хозяин сегодня считается НЕ судьёй, и это не мелочь.** `факт кода`: `HouseholderRouteController.Update`
крутит `Householder.Step` в обычном кадре у того, у кого открыта сцена; сцена `HostWalksHisDay` сети не
содержит вовсе; в снимке (`WalkerSnapshot`, `CargoSnapshot`) хозяина нет. Критерий 6 говорит: все решения
считает создатель игры. Значит хозяин переезжает в тот же тик, где уже считаются игроки и груз
(`CarryWorld.Advance`), а его положение и занятие едут в снимке рядом с ними. **Это третий новый слой
волны, и наряд называл два.** Развилкой не является — следствие критерия.

**M2. Как хозяин ходит по размеченному дому.** `факт кода`: сегодня маршрут — `Transform[]` по кругу, а
на реакцию хозяин идёт по прямой к точке прослушивания. В доме со стенами прямая упрётся в первую же
стену, а критерий 6 требует «строит свежий путь». Готовый ответ движка (NavMesh, `com.unity.ai.navigation`
в проекте стоит) — тот же капкан, что и раньше: движок становится судьёй, ответ не воспроизводится в
тесте и может разойтись на двух машинах. **Ответ ноги: путь считается в слое правил по НАЗВАННЫМ местам
дома и названным проходам между ними** (A10, вторая структура). Дёшево, проверяемо тестом и намертво
привязано к A1: без имён у мест этого не сделать. Это самый крупный вопрос, которого в наряде не было.

**M3. Событий у хозяина не существует ни одного.** `факт кода`: единственные вызовы `EmitBoardCreak`,
`EmitQuietRustle`, `EmitRepeatedHeavyNoise` — кнопки в инспекторе редактора
(`HouseholderRouteControllerEditor.cs:175,181,187`). Чтобы хозяин замечал игроков и гнался (критерий 7),
кто-то в мире обязан рождать замеченные факты. Это НОВЫЙ шов, и именно на нём позже стоит вся
следующая карточка про улики (§CONTRACTS C04).

**M4. Отзывчивость от первого лица.** `факт кода`: предсказания на клиенте нет вообще —
`OnClientTick` шлёт ввод судье, клиент только применяет снимки. На виде сверху это терпимо, от первого
лица заметно сильно, и карточка сама называет риском «переноска от первого лица ощущается вяло».
**Заранее НЕ чинится:** восприятие — настройка, а не гейт, и владелец смотрит глазами. Принятое знание
прямо разрешает лекарство и не требует трогать курьера (`how-the-game-is-built-layers.md:39-42`).
Требование одно: шов оставить таким, чтобы «показывать сразу и тихо поправляться» добавлялось без
переписывания. Владельцу названо вслух, чтобы не стало сюрпризом на первом запуске.

---

## §SIGNOFF

- **Architect подписан владельцем 2026-08-04 в трёх местах** — П1, П2, П3 выше, с точной силой каждого
  токена. Больше ничего у него не запрашивалось.
- **Решено самой ногой, с названной вынуждающей причиной:** A3 (имя корня), A5 (два шва хозяина),
  A6 (взгляд не авторитетен), A7 (стек глубиной-ручкой), A8 (чтение слова «инструментов»),
  A9 (перечень слоёв), A10 (дерево мест без инвентаря), A11 (правило рабочих сцен), M1, M2, M4.
- **Самые вероятные мишени опровержения, названы самой ногой:** A8 (чтение его текста, а не ввоз);
  A5 (форма двух швов); M2 (граф проходов ещё не существует ни в каком виде).

arch_open: 0 — ни одна архитектурная строка не осталась без ответа или подписи
arch_in_context_only: PASS — ни один выбор этого файла не попал в `done_when` узла
owner_signed: 3 (П1, П2, П3)
miner_gaps_named: 4 (M1, M2, M3, M4)

END_OF_FILE: live/indie-game-development/work/converge-g-1d84-arch.md
