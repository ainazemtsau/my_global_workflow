# CALL — c-exec-g-5a7c-body-1-002

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36.
direction: indie-game-development
node: g-5a7c
track: body
task: t-body-1
issued: 2026-08-12 by s-work-g-5a7c-body-1-checkpoint-001
status: ready
basis: `4e5858b4b7cf4a6c8c9cad08e3fde4a6915200f3` — `origin/main` = `origin/dev` =
       `origin/slot/win-u2`, перемерено `git ls-remote origin` 2026-08-12.

**Это продолжение `c-exec-g-5a7c-body-1-001`, а не новая работа.** Кандидат построен, перемерен
Направлением и признан хорошим. Не хватает ровно одного: он собран на протухшей базе и не
опубликован.

## slot

**WIN-U1**, каталог `C:\projects\Unity\GasCoopGame_win-u1`, ветка `slot/win-u1`. Кандидат уже лежит
там: `4bb27ca4` (reapply), `2fb82f54` (revert), `0ea2cb5c` (работа), дерево чистое.

## goal

**Перебазировать кандидата на опубликованную голову, переисполнить откат на НЕЙ и опубликовать.**

## context — перемерено Направлением 2026-08-12

**ГЛАВНОЕ, И ЭТО ОПАСНО.** Родитель твоего рабочего коммита `0ea2cb5c` —
`f3c40c4010ee2f0f700cdf1bfbcea14a301d488c`. За время сборки в `main` опубликовался закрытый шов
`t-places-1`, и голова стала `4e5858b4`.

Поэтому дифф `origin/main..HEAD` сегодня **УДАЛЯЕТ ВЕСЬ ШОВ**: `−23` в `Core/House/HousePlan.cs`,
`−83` в `Core/Householder/Householder.cs`, `−60` в `HouseholderRoutineProfile.cs`, `−84` в
`Profiles/HouseholderProfileLoader.cs`, поле роли в `World/HousePlaceMarker.cs`, **`−402` строки
целиком — весь файл `tests/TunnelCrew.Core.Tests/HouseholderPlaceRoleTests.cs`** — и отчёт шва.
Публикация как есть стёрла бы задачу, закрытую часом ранее.

**ХОРОШАЯ НОВОСТЬ: перебазирование чистое.** Перемерено `comm` по спискам путей: собственный дифф
тела (`f3c40c40..HEAD`, 34 файла) и дифф шва (`f3c40c40..4e5858b4`, 11 файлов) **не пересекаются НИ
ОДНИМ файлом**. Конфликтов быть не должно; если конфликт всё же появился — СТОП домой, значит
что-то не так с моим замером.

**ОТКАТ НАДО ПЕРЕИСПОЛНИТЬ.** Существующий сошёлся, но со СВОЕЙ базой: `tree(2fb82f54)` =
`5ec74916…` = `tree(f3c40c40)`. Для новой базы он ничего не доказывает: `tree(4e5858b4)` =
`b15f5ad4c2147a923dbe3ddec930ba1bf38212f2`.

**ЧИСЛО ТЕСТОВ ИЗМЕНИТСЯ, И ЭТО НОРМАЛЬНО.** Твои `214/214` были верны для старой базы. На
`4e5858b4` база — **219 passed, 0 failed, 0 skipped**, прогнано Направлением. После перебазирования
ждём 219, и любое отклонение объясняется.

## boundaries

- **Ничего нового не строится.** Ни одной новой возможности, ни одной правки поведения. Только
  перебазирование, переисполненный откат и публикация.
- **НИ ОДНА СЦЕНА НЕ ОТКРЫВАЕТСЯ** — в кандидате их ноль, и так и остаётся.
- **ФАЙЛЫ ШВА НЕ ТРОГАЮТСЯ ВООБЩЕ.** После перебазирования их в твоём диффе быть не должно:
  `Core/House/**`, `Core/Householder/**`, `Profiles/**`, `World/HousePlaceMarker.cs`,
  `NetworkHouseholder.cs`, `tests/**`, `docs/results/c-exec-g-5a7c-places-1-001.md`.
- **`Settings/GameRulesSettings.asset` не трогается** — параллельно идёт `t-thing-1` в WIN-U3.
- Инструмент недоступен → СТОП домой. Конфликт при перебазировании → СТОП домой.
- **Расписка обязательна.**

## done_when

1. **ПЕРЕБАЗИРОВАНО НА `4e5858b4`,** и дифф `origin/main..HEAD` содержит ТОЛЬКО собственные файлы
   тела: ни одного файла шва, ни одной сцены, ни одного удаления чужой работы.
2. **ОТКАТ ПЕРЕИСПОЛНЕН НА НОВОЙ БАЗЕ** настоящим `revert`, и дерево после него сходится с
   `b15f5ad4c2147a923dbe3ddec930ba1bf38212f2` знак в знак.
3. **ОПУБЛИКОВАНО** в `dev` и `main`, readback удалённых ссылок приведён, прогон на выгруженном
   дереве публикации назван числом против базовых 219, слот отпущен под тем же lease.

## return

Первая строка — расписка: три строки `done_when`, базис `4e5858b4`.

Дальше: команды и хеши перебазирования; дифф путей после него; хеши и совпадение деревьев отката;
readback `git ls-remote`; число тестов; подтверждение, что файлов шва в диффе НЕТ.

**И отдельной строкой — что владельцу запустить и на что посмотреть**, чтобы принять задачу
глазом: три движения и четыре копии по сети закрываются его прогоном, а не воротами.

## budget

Мало. Это механическая нога.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-body-1-002-call.md
