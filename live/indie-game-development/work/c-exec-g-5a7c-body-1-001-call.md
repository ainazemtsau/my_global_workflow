# CALL — c-exec-g-5a7c-body-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: body
task: t-body-1
issued: 2026-08-12 by s-work-g-5a7c-body-1-dispatch-001
status: ready
basis: `f3c40c4010ee2f0f700cdf1bfbcea14a301d488c` — `origin/main` = `origin/dev`, перемерено
       `git ls-remote origin` перед выпуском 2026-08-12.

**ЕСЛИ `origin/main` СДВИНУЛСЯ — НЕ СТОП, А ПЕРЕБАЗИРОВАНИЕ.** Взять свежую голову, собрать на ней,
назвать её отдельной строкой возврата. СТОП домой — на всё ОСТАЛЬНОЕ неожиданное.

## slot

**WIN-U1**, каталог `C:\projects\Unity\GasCoopGame_win-u1`, ветка `slot/win-u1`.

Перемерено селектором перед выпуском: все четыре слота `CLEAN / AVAILABLE / lease none`.
**WIN-U1 отстаёт: его дерево на `a5a84350`, удалённая `origin/slot/win-u1` на `43648b2d`.** Первым
делом догнать до `f3c40c40`.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U1 -Claim -LeaseId c-exec-g-5a7c-body-1-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/main`, readback дерева, `git status` пуст.

**UNITY ЗДЕСЬ НУЖЕН ПО-НАСТОЯЩЕМУ**, в отличие от параллельного наряда по шву: модели, префабы,
импорт ригов, анимация. Мост WIN-U1 ЖИВ И ПРОВЕРЕН этой ногой: отвечает, Unity `6000.5.5f1`, не в
Play, не компилирует. Поле `mcp-endpoint: unrecorded` в файле состояния — устаревшая запись, а не
отсутствие моста.

**ЖДИ ДЛИННЫЙ РЕИМПОРТ.** Редактор WIN-U1 сейчас открыт на дереве `a5a84350`; переход на `f3c40c40`
приносит около сорока изменённых файлов `Assets`, включая новые сцену и скрипты. Дай ему
доимпортировать до первой правки. Проверка, что мост смотрит именно в WIN-U1: на его дереве
**нет** `Assets/TunnelCrew/Scenes/HouseholdLab.unity` до догона и **есть** после.

**ПАРАЛЛЕЛЬНО В WIN-U2 ИДЁТ ДРУГОЙ НАРЯД** — `c-exec-g-5a7c-places-1-001` по шву «роли мест». Он
трогает `Assets/TunnelCrew/Profiles/**`, `Core/House/**`, `World/HousePlaceMarker.cs` и, возможно,
`Network/NetworkHouseholder.cs`. **Ты не открываешь ни один из этих файлов.**

## goal

**У налётчика и хозяина появляются настоящие тела, и модель меняется на другую без единой правки
игровой логики.**

Смысл задачи — ШОВ ПОД ЗАМЕНУ, а не выбор облика. Облик владелец выбирает параллельно сам и прямо
сказал, что ещё не выбрал: «я не знаю, как это будет сделано. Может вообще нам придётся там
немножко делать каких-то микрочеловечков». Его слово, которым задача куплена, 2026-08-12: «мы
просто можем взять там какую-то дефолтную модель там с анимацией, со всем, чтобы просто мы потом
могли поменять на нашу новую».

**Отсюда прямое следствие, и оно не подлежит толкованию:** любая модель, которую ты поставишь, —
ЗАГЛУШКА. Ты не выбираешь, как выглядит игра. Не подбирай «красиво», не додумывай стиль, не трать
время на облик. Гейт стоит на ЗАМЕНЕ, а не на виде.

**НАЗВАННЫЙ ПОДХОД — ОДИН:** тело становится подменяемой заготовкой, на которую презентация
ссылается ДАННЫМИ. Отход от него — ESCALATE домой, а не решение на месте.

## context — перемерено чтением байтов на `f3c40c40` 2026-08-12

### ГЛАВНАЯ ПОПРАВКА: оплаченный вход — это МЕБЕЛЬ, а не персонажи

Карточка задачи и текст ставки утверждали, что вход по персонажам уже оплачен веткой
`probe/free-kits-2026-08-06`. **Это неверно, и проверено содержимым, а не размером.** В ветке
двадцать один содержательный файл, и все они обстановка: лампа `KayKit/lamp_standing.fbx`, стул и
стол `Kenney/chair.fbx` и `tableRound.fbx`, `Quaternius/Bookshelf.fbx` и `Houseplant_1.fbx`,
материал пола PolyHaven. **Ригнутых тел, скелетов и анимаций там нет.** Заведена улика
`i-free-kits-probe-holds-furniture-not-characters-001`.

**Что в ветке ДЕЙСТВИТЕЛЬНО оплачено и что ты переиспользуешь — ОБРАЗЕЦ АВТОРИНГА:**
`Art/FreeKitsProbe/Sources/<вендор>/` с `License.txt` и `SOURCE.md` рядом с исходником, и
собственная заготовка проекта в `Art/FreeKitsProbe/Prefabs/`, плюс `AUTHORING.md`. Повтори эту
раскладку для тел. Саму мебель НЕ трогай — она вход в задачу `t-rooms-1`, не в эту.

### Состояние тел сегодня

| Кто | Как нарисован | Шов |
|---|---|---|
| Налётчик | `Network/NetworkWalkerPresentation.cs:153,166,201` — `GameObject.CreatePrimitive` даёт `Capsule` и `Cube`, тело собирается КОДОМ | **шва нет вовсе** |
| Хозяин | префаб `World/Householder.prefab`, дочерний `Householder Body` с `MeshFilter` на встроенную капсулу `fileID: 10208` | **шов есть**, внутри префаба |
| Груз | `Network/NetworkCargoPresentation.cs:140` и `Core/Cargo/CargoBody.cs:80` — `CreatePrimitive(Cube)` | не твоё |

**Анимационной обвязки нет ни грамма:** поиск по всему `Assets/TunnelCrew/**` даёт **ноль**
упоминаний `Animator`, `SkinnedMeshRenderer` и `Avatar`. В `Packages/manifest.json` из
анимационного есть только базовый модуль и мост `com.ivanmurzak.unity.mcp.animation`.

В игровом арте `Assets/TunnelCrew/Art/` лежит ровно один файл — материал стен.

### КОЛЛИЗИЯ, ИЗ-ЗА КОТОРОЙ ПОЯВИЛАСЬ ЖЁСТКАЯ ГРАНИЦА ПО СЦЕНАМ

`NetworkWalkerPresentation` (guid `494bc80483aa7e143beafdcf468c4fdd`) висит компонентом на объектах
**обеих** сцен: `IntegratedHouse.unity:1285` и `HouseholdLab.unity:1285`.

Значит привычный путь «добавить сериализованное поле префаба и назначить его в сцене» потребовал бы
править обе сцены. Обе принадлежат чужим дорожкам — `IntegratedHouse` дорожке `house-and-things`,
`HouseholdLab` задаче `t-lab-2`, — и обе копируются в тестовый проект ФИКСТУРАМИ
(`tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj`, блок `None Include`), поэтому правка
сцены ломает scene-fixture тесты.

**Факт, который тебе пригодится, и это факт, а не указание:** у `NetworkWalkerPresentation` уже
есть `[SerializeField] private PlaySettings _settings;`, назначенный в сцене, а в
`Assets/TunnelCrew/Settings/` лежат `PlaySettings.asset` и `PresentationSettings.asset`. Как именно
довести ссылку на заготовку до презентации без правки сцены — твоё решение.

`World/Householder.prefab` инстанцирован в обеих сценах, но правка САМОГО префаба сцены не трогает:
экземпляры наследуют. Тело хозяина поэтому меняется чисто.

### База прогонов

`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **214 passed,
0 failed, 0 skipped**, прогнано этой ногой на `f3c40c40`. Твоя работа — презентация, и тесты Core
она в норме не двигает; если число изменилось, объясни почему.

`tools/hygiene.ps1` — зелёный. **`tools/check.ps1 -Deliver` КРАСНЫЙ на семи исторических отчётах —
зарегистрированный дефект `i-deliver-gate-red-when-main-catches-dev-001`, НЕ твоя работа и НЕ повод
для СТОПа.**

## boundaries

- **Задача не расширяется.** Три строки `done_when` — весь объём.
- **НИ ОДНА СЦЕНА НЕ ОТКРЫВАЕТСЯ.** Ни `IntegratedHouse.unity`, ни `HouseholdLab.unity`, ни любая
  другая. Основание измерено выше и двойное: чужие дорожки и фикстуры тестов. Если шов окажется
  недостижим без правки сцены — **СТОП домой**, а не правка.
- **НЕ ОТКРЫВАЕШЬ файлы параллельного наряда:** `Assets/TunnelCrew/Profiles/**`, `Core/House/**`,
  `World/HousePlaceMarker.cs`, `Network/NetworkHouseholder.cs`.
- **НЕ ТРОГАЕШЬ** `Core/**` вообще, кроме случая, когда без этого шов физически не строится — тогда
  назови место в возврате отдельной строкой. Груз, шум, хозяин как правила, профиль — не твои.
- **НЕ ТРОГАЕШЬ мебель** `Art/FreeKitsProbe/**` — она вход задачи `t-rooms-1`.
- **ТОЛЬКО БЕСПЛАТНЫЕ АССЕТЫ.** Общий денежный предел направления — около $3,000, из них примерно
  $1,500 уже потрачено. **Любая покупка — ESCALATE домой**, никогда решение на месте. У каждого
  источника обязаны быть `License.txt` и `SOURCE.md` рядом с исходником, как в образце пробы.
- **ТЫ НЕ ВЫБИРАЕШЬ ОБЛИК.** Модель — заглушка. Никакой доводки внешнего вида, материалов сверх
  необходимого, поз, лиц, аксессуаров. Устав прямо боится полировки внешнего вида.
- **НЕ ДЕЛАЕТСЯ:** кастомизация головы и аксессуары (слово владельца «сейчас это пока вообще
  неважно»), ragdoll, лицевые эмоции, поставленный режим третьего лица, инверсная кинематика,
  анимация переноски груза, анимация реакций хозяина.
- **ТЕЛО — ПРЕЗЕНТАЦИЯ, НЕ АВТОРИТЕТ.** Ни одна величина, которую считает судья, не начинает
  зависеть от модели.
- **Настроечные значения — не код** (контракт 36): тестами не покрываются; тест проверяет форму,
  никогда величину.
- **Тест только там, где владелец не увидит ответ глазом.** Здесь почти всё видно глазом, поэтому
  тестов должно быть мало или ноль — и это правильно, а не халтура.
- **Самописный сканер как улика поведения запрещён.**
- **Инструмент недоступен → СТОП домой.** Если Unity-мост не отвечает или импорт рига не проходит —
  СТОП, никогда обходной путь.
- **Расписка обязательна:** наряд не существует, пока исполнитель не расписался.

## done_when

Три строки — потолок контракта 36. Ни одна не снимаема.

1. **ШОВ И ЗАМЕНА — ГЛАВНЫЙ ГЕЙТ.** У налётчика и у хозяина тело задаётся подменяемой заготовкой,
   а не строится кодом и не сидит встроенной капсулой; и модель меняется на другую, ЗАМЕТНО
   ОТЛИЧАЮЩУЮСЯ, **без единой правки игровой логики и без открытия единой сцены** — доказано
   фактической подменой, а не обещанием.
2. **ТРИ ДВИЖЕНИЯ И ЧЕСТНЫЙ АРТ.** Оба тела — ригнутые заготовки, у которых работают стойка,
   ходьба и поворот; модели бесплатные, завёрнуты в собственные заготовки проекта, лежат в
   `Assets/TunnelCrew/Art/`, и у каждого источника рядом `License.txt` и `SOURCE.md` по образцу
   ветки пробы.
3. **НИЧЕГО НЕ СЛОМАНО, ОТКАТ НАСТОЯЩИЙ.** Налётчик остаётся ростом `0.16` м, хозяин человеческого
   размера, физика, камера и столкновения не изменены; четыре копии по-прежнему соединяются и
   играют, тело остаётся презентацией; прогон тестов назван числом против базовых 214; и существует
   откат одним коммитом, исполненный НАСТОЯЩИМ `revert`, дерево после которого сходится с деревом
   базы.

## return

**Первая строка возврата — РАСПИСКА:** сколько строк `done_when` получено (должно быть три) и с
какого базиса собрано.

Дальше:

- что сделано, по каждой из трёх строк отдельно;
- **чем именно доказана замена**: какая вторая заготовка подставлена и какой диффа при этом не
  было;
- полный список путей диффа, и отдельно подтверждение, что среди них НЕТ ни одной сцены;
- откуда взята каждая модель, под какой лицензией, и что лицензия позволяет;
- прогон тестов до и после числами;
- откат: команда, хеши, совпадение деревьев;
- **что НЕ сделано и почему**;
- всё неожиданное, включая всё, что противоречит перемеренному выше контексту.

Отдельной строкой: **что владелец должен увидеть глазом**, чтобы принять задачу, и как это
запустить. Эту задачу закрывает его глаз, а не зелёные ворота, и это названо заранее.

## budget

Половина сфокусированного дня. Порог смерти 3 этой ставки считает ноги: если на общую задачу и
первые задачи всех трёх дорожек уйдёт больше двенадцати ног, **эта дорожка встаёт на паузу первой**
— названо владельцу до подписи ставки.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-body-1-001-call.md
