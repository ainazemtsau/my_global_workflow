CALL c-converge-g-37a1-core-rows-001
to: session
direction: indie-game-development
play: converge
node: g-37a1
goal: |
  Turn the seventeen owner-approved core lines into CHECKABLE ROWS — one row per line, each
  decidable by a person who did not write it — and produce the §GLOSSARY and §WHAT that the
  previous converge closed as a CHECKPOINT without ever reaching.
context: |
  THIS IS STEP 2 OF A SIX-STEP ROUTE THE OWNER SET HIMSELF and no leg may reorder it without
  his words. The route lives verbatim in `NOW.md` `d-post-verify-route-001`. Step 1 is DONE:
  the minimal core exists as seventeen lines he approved on exact text on 2026-07-28, and the
  card `g-37a1` now carries fifteen criteria. Step 4 — the bounded read-only engine-fit check —
  is running in PARALLEL with you, by his instruction; do not wait for it, do not read its
  output, and do not let an engine fact enter your rows.

  READ FIRST, in this order:
  - `live/indie-game-development/work/core-requirements-g-37a1.md` — the seventeen lines, their
    origin, the explicit NOT-core list, the two answered owner questions, and the register of
    questions deliberately turned into SETTINGS rather than decided. This is your subject.
  - `live/indie-game-development/TREE.md`, node `g-37a1` — the fifteen approved criteria. These
    carry his verdict on exact text and you may not change a word of them.
  - `history/2026-07-28-s-map-g-37a1-core-requirements-001.md` — the receipt, including which
    of his words produced which line.
  - `live/indie-game-development/work/oracle-first-playable-core-v1.md` — fourteen decision
    classes for this node class. It is a CANDIDATE with no authority, but it is the instrument
    that found the four gaps this direction had, and all four are now answered. Use it as your
    coverage checklist and say so.
  - `history/2026-07-27-s-converge-verify-g-37a1-digging-card-001.md` — the failed verification
    whose findings this route exists to discharge.
  - `live/indie-game-development/work/converge-g-37a1.md` — design MATERIAL ONLY, and it carries
    six retirement banners plus a CONSUMED banner. Read the banners BEFORE the body they
    correct. Several of its sections are known stale; nothing in it outranks the card.

  WHAT CHANGED ON 2026-07-28 AND MUST NOT BE RE-OPENED — all his, on exact text:
  - PASSABILITY IS DELETED. Any pocket may be entered; the two kinds differ by the PRICE IN AIR
    and by nothing else; an emptied pocket is ordinary dug space that costs nothing to enter.
  - THERE ARE NO SINKS. Substance leaving a pocket ends up in the crew's own workings.
  - THE VALUE IS BELOW. The crew digs down and sideways; there are no ready ladders or lifts;
    the way up is made by the player with the same digging plus a jump; being left without one
    is a legitimate death.
  - TWO GASES NOW. Liquid is headroom and may later enter as a REPLACEMENT for gas.
  - DEATH COSTS ZERO with instant return; the price of death is a SETTING built up later.
  - BEHAVIOUR AT THE MOMENT OF BREACH is an EVENT whose concrete form is a SETTING, not a
    hardwired variant. Wider cut = stronger and faster; a narrow notch = a thin trickle; the
    first breach does not kill instantly.
boundaries: |
  Converge only. No owner in this leg. Do not create a bet, tasks, tracks, lanes or an executor
  CALL. Do not touch `CHARTER.md`, `TREE.md` or either card — they carry his verdict on exact
  text and only a `map` leg with his words may change them. Do not write `knowledge/`. Do not
  read `archive/**` or the frozen canon repository. Do not touch the product repository or
  Steam state. No numeric release chance. Do not answer `d-air-counter-visibility-001`.

  AND THE RULE THAT DEFINES THIS LEG: a question the owner deliberately turned into a SETTING is
  NOT an open question you may close. The register is in §«Вопросы, превращённые в настройки» of
  the requirements artifact. Your row for such a line tests that the knob EXISTS, is reachable
  from a built player and changes what it claims to change — never what its value should be.
  Inventing a value is the failure this route was built to avoid.
done_when: |
  1. One row per core line, seventeen rows, each stating: what must be observably true, how a
     person who did not write it decides yes or no, and where the evidence comes from.
  2. §GLOSSARY signed: every term that carries weight across rows has one reading. The previous
     converge mined the term net («вещество», «участок», «карман», «вскрытие», «выработка»,
     «различимо на глаз», «спорное число») and never signed it because the concept changed
     under it; the concept is now settled, so sign it.
  3. §WHAT produced: coverage of the card's fifteen criteria by the rows, with every criterion
     mapped and every uncovered clause named rather than filled.
  4. The fourteen oracle classes swept, with a verdict per class against the ROWS — not against
     the old artifact. Any class still uncovered is named as a gap, not patched.
  5. Rows that cannot be decided without the owner are listed separately as owner content with
     options and a recommendation, and are NOT decided.
  6. The rows a build closes better than a document are marked as such, so the `shape` leg at
     step 6 can turn them into tasks instead of more paper. `g-12fd` died of the paper route.
return: |
  One `converge` RESULT: the seventeen rows, §GLOSSARY, §WHAT, the oracle sweep, the owner-content
  list, and the successor CALL for step 3.
budget: one session, no owner.
surface: a FRESH chat. The owner is not required and should not be pulled in.

after: |
  Step 3 — `converge-verify` in another FRESH session with no owner: try to break the rows.
  Step 4 is already running in parallel and returns to `i-engine-fit-decision-ladder`.
  Step 5 — the owner's ladder verdict. Step 6 — `shape`.

END_OF_FILE: live/indie-game-development/work/c-converge-g-37a1-core-rows-001-call.md
