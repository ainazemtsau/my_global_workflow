# CALL c-prep-net-spike-001

to: session (play: research)
for: g-9c41 / линия E — подготовка спайка «две реальные машины через интернет»
issued: 2026-07-10 (s-lanes-install-001)

goal: |
  Подготовить (НЕ провести) первый честный интернет-спайк лока на РЕАЛЬНОМ газовом ядре:
  (1) выбор машины №2 — MacBook (mac-билд; бонус: кросс-платформенная сверка детерминизма
  целочисленного ядра) vs облачная Windows-VM (чище для первого замера) — рекомендация с ценой;
  (2) чек-лист настройки MacBook (dotnet SDK, pwsh, роль G5/писаря — независимо от выбора);
  (3) план замера: поведение stall-barrier на 80–150 мс, что считаем «зелёным», какая дешёвая
  пер-тик сверка вместо O(domain)-чексума (28 мс) допустима как ВРЕМЕННАЯ для спайка (честно
  пометить, настоящий дешёвый дайджест = S4-пакет линии A);
  (4) каркас будущего executor-CALL спайка с LAUNCH-блоком.
boundaries: не строить, Core/** не трогать; результат = план + чек-листы + каркас CALL.
done_when: план и рекомендация записаны в work/, каркас CALL готов, RESULT сдан.
return: RESULT (капчуры/решения — владельцу батчем).
budget: 1 сессия.

launch:
  lane: E
  venue: OS-чат / headless; worktree и Unity не нужны
  machine: любая (MacBook ок)

END_OF_FILE: live/indie-game-development/work/c-prep-net-spike-001-call.md
