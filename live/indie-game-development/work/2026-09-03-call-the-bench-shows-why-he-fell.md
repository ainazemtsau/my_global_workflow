# CALL: стенд «хозяин» — видно, от чего он потерял равновесие

Задача: `t-the-bench-shows-why-he-fell-1` · волна `bet-g-5a7c-wave-12` (рубеж Р1, 11.09) · полоса `тело`
Слот: **WIN-U4** · репозиторий: `C:\projects\Unity\GasCoopGame_win-u4`
Слот `AVAILABLE`, аренда `none` — проверено в `gascoop-slot-state.v1.json` 2026-09-03.
Основание: **`main` = `dev` = `origin/main` = `origin/dev` = `2d2c27ff`**, все четыре слота влиты и чисты.
База тестов по словам review 03.09: headless `819/819/0`, EditMode `233/233` — **перемерь у своей головы и назови своё число.**

**ЭТО ПЕРВАЯ НОГА ВОЛНЫ И ЕДИНСТВЕННЫЙ ДОЛГ ПРИЁМКИ ВОЛНЫ 11.** И это **не WIN-U2** — его слово
02.09: «пока не стоит, так как он занимался стендом, и лучше в другом слоте это сделать».

## 0. СПЕЦИФИКАЦИЯ УЖЕ НАПИСАНА, И НЕ ЭТОЙ НОГОЙ

`work/2026-09-02-order-the-homeowner-balance-bench.md` — наряд, написанный возвратом самой ноги,
которая упёрлась в отсутствие стенда. **Читается целиком и остаётся спецификацией.** Ниже — только
то, что нарезка добавляет и закрывает.

## 1. ШОВ, КОТОРЫМ СТЕНД СТАВИТ УДАР — НАЗВАН НАРЕЗКОЙ, НЕ ИЗОБРЕТАЙ ВТОРОЙ

Review 03.09 нашёл, что наряд перечисляет, **что показывать**, но не то, **чем бить**. Замерено при
нарезке на `2d2c27ff`:

- **Расстановка — JSON:** `Assets/TunnelCrew/Editor/IntegrationLab.<имя>.json`, схема
  `tunnelcrew-integration-lab-arrangement.v2`. Семь расстановок набора «мышь» уже лежат там же.
- **Кусок удара:** `launched-cargo` — `definitionId`, старт `x/y/z`, скорость `velocityX/Y/Z`.
- **Полка:** окно `IntegrationLabWindow` (`ArrangementFolder = "Assets/TunnelCrew/Editor"`,
  префикс `IntegrationLab.`); кнопки идут в `NetworkWalkerCourier.TryLoadLabArrangement /
  FireLabArrangement / ResetLabArrangement / ReleaseLabArrangement`.
- **Приборы обратно:** `LabShoveObserversRpc` → `IntegrationLabReadout` (IMGUI, потому и виден на
  собранном клиенте и во втором редакторе).

**ЧЕГО НЕ ХВАТАЕТ, И ЭТО ПЕРВАЯ РАБОТА ЗАДАЧИ.** `IntegrationLabArrangement.ParseKind` знает три
вида куска: `frozen-walker`, `launched-cargo`, `hovering-cargo`. **Мишени-хозяина среди них нет.**
Нужен четвёртый вид: поставить тело хозяина в известную точку и заморозить его распорядок, чтобы
удар был воспроизводим. Имя и форма — твои; шов — этот.

## 2. ВОПРОС, КОТОРЫЙ НЕ ПЕРЕОТКРЫВАЕТСЯ

Наряд 02.09 оставил открытым «не свести ли стенд с `ReachLab`». **Он решён:** закрытая задача
`t-one-lab-he-can-set-up-himself-1` строкой 7 ответила **«не сводить»**. Не переспрашивай.

## 3. goal и done_when

В карточке `t-the-bench-shows-why-he-fell-1`, восемь строк. Коротко: набор «хозяин» — **второй набор
существующего `MouseLab`, не третья сцена** (Р14); приборы по наряду; клиентская кукла помечена
выключенной словами на экране; числа порогов читаются из `GameRulesSettings.asset`, не дублируются;
**игровой снапшот не расширяется ни на байт**; приёмка на двух экранах.

## 4. ЧЕГО ЭТА НОГА НЕ ДЕЛАЕТ

Не чинит механику равновесия — она опубликована и доказана физикой, стенд её **показывает**.
Не делает красоту падения, анимации, реплики. Не строит оглушение мыши хозяином. Не трогает
`Network/HouseholderSnapshot.cs`.

## 5. ПОРОГИ

1. **Правка `Network/HouseholderSnapshot.cs`** — откат. Приборные числа едут dev-RPC.
2. **Правка самой механики равновесия** — откат.
3. **Пришлось погасить тест** — запрещено абсолютно (`tools/hygiene.ps1:363-376`).
4. **Набор не помещается в `MouseLab` по устройству** — СТОП и вопрос владельцу: отдельная сцена
   разрешена только его словом.

## 6. ЧТО ВЕРНУТЬ

RESULT по схеме: что построено, чем ставится удар, что видно на host и что на client, свои числа
тестов, и **отдельной строкой — готов ли стенд к приёмке его глазом и что именно ему смотреть.**
Приёмка снимается владельцем, а не ногой.

END_OF_FILE: live/indie-game-development/work/2026-09-03-call-the-bench-shows-why-he-fell.md
