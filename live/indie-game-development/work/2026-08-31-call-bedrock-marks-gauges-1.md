# CALL: метки и шкалы переезжают в закреплённое — первый настоящий перенос

Задача: `t-bedrock-marks-gauges-1` · полоса `закрепление` · **ВНЕ волны девятой**
База: продуктовый `main` = `c7e926cf`. Стартовать С НЕГО, а не с ветки слота.
Слот: **WIN-U3**, словом владельца.

---

## ПРОЧИТАТЬ ДО ПЕРВОЙ СТРОКИ

Граница сборок построена: `TunnelCrew.Core` — закреплённое, список ссылок пуст; `TunnelCrew.Probe`
— пробы, смотрит на Core; обратно не компилируется, доказано `CS0246`. **В закреплённом сегодня
лежат ровно три файла описания комнаты.** Эта нога переносит туда следующую горсть.

**ПЕРВЫЙ ПЕРЕНОС ЦЕНЕН НЕ КОДОМ, А ТЕМ, ЧТО НАХОДИТ ДЕФЕКТЫ КОНВЕЙЕРА** — пока цена ошибки два
файла, а не сорок. Поэтому кусок выбран самый чистый из существующих, и от тебя ждут не только
переноса, но и честного рассказа, обо что споткнулся.

**ПОЛОСА ОБЪЯВЛЕНА ВЛАДЕЛЬЦЕМ 2026-08-31, дословно:** «объявляю пятую полосу «закрепление», идёт
вне волны, слот U3». Задача **не служит цели девятой волны** и не двигает ни одной её строки.
Ты никого не задерживаешь и тебя никто не ждёт.

## ЗАМЕР, СНЯТЫЙ ДО ВЫДАЧИ НАРЯДА

Направление проверило оба файла на `main` `c7e926cf`:

- `Assets/TunnelCrew/Probe/Householder/HouseholderMarks.cs` — **только `using System;`**;
- `Assets/TunnelCrew/Probe/Householder/HouseholderGauges.cs` — **только `using System;`**;
- каждый тип, который они упоминают, объявлен внутри них самих (`HouseholderMarkScope`,
  `HouseholderMark`, `HouseholderMarks`, `HouseholderGaugeDefinition`, `HouseholderGaugeCatalog`,
  `HouseholderGauges`);
- пространство имён у обоих — **уже `TunnelCrew.Core`**;
- потребители: только `Probe/Householder/**` и headless-набор `tests/TunnelCrew.Core.Tests`.
  `TunnelCrew.Probe` уже ссылается на `TunnelCrew.Core`; headless-проект видит обе сборки.

**Отсюда ожидание: перенос — это `git mv` и больше ничего.** Если окажется иначе, это находка.

## goal

**Две горсти закреплены, граница после этого доказана заново, и названо, чем эта нога споткнулась.**

## done_when

1. **ДВА ФАЙЛА С МЕТАМИ ПЕРЕЕХАЛИ ЧЕРЕЗ `git mv`** в `Assets/TunnelCrew/Core/Householder/`.
   Именно `git mv` — чтобы `.meta` уехали вместе и GUID'ы уцелели.

2. **НИ ОДНОЙ ПРАВКИ ИСХОДНИКОВ. НИ ОДНОЙ ПРАВКИ `.asmdef`.**
   Это приёмка, а не пожелание. **Если правка всё-таки нужна — СТОП и вопрос домой**, потому что
   тогда замер выше неверен, и это важнее переноса.

3. **ГЕЙТ ВОЗРАСТА НАЗВАН ЧИСЛОМ.** Правило — `knowledge/compiling-alone-is-not-being-ready.md`:
   компилятор доказывает, что файл МОЖНО закрепить, и никогда — что его СТОИТ. Решает возраст.
   Замер направления: по одной правке за всю историю, не тронуты 15 дней. **Перемерь сам.**
   **Мерить обязательно через `git log --follow -M --numstat`** с отбрасыванием коммитов с нулевым
   numstat: переименование `Core/` → `Probe/` (коммит `3c63dff2`) обнулило наивный возраст для всей
   пробы, и `git log` по новому пути покажет один день на всё.

4. **ГРАНИЦА ПРОВЕРЕНА ПОСЛЕ ПЕРЕНОСА, ОБЕИМИ СИСТЕМАМИ СБОРКИ.** Обращение из закреплённого в
   пробу не компилируется — ПОКАЗАНО текстом ошибки, в Unity и в headless отдельно. Проверка
   временная; в дереве после ноги её не остаётся.

5. **ПОВЕДЕНИЕ НЕ ИЗМЕНИЛОСЬ НИ НА БАЙТ.** Тесты зелёные, число то же: 716 headless на `c7e926cf`
   плюс Unity-набор.

6. **НАЗВАН СЛЕДУЮЩИЙ КАНДИДАТ И ПОЧЕМУ ИМЕННО ОН** — одна строка, по замеру, а не по вкусу.

## что НЕ входит

- **`Probe/Movement` — НЕ брать.** Перемерено: он отрезаем, но НЕ изолирован. Его типы торчат в
  `Network/NetworkHouseholder.cs`, `Network/NetworkWalkerCourier.cs`, `World/StartConditions.cs` и
  в четыре EditMode-сборки тестов, у которых `TunnelCrew.Core` нет. Из 17 сборок, ссылающихся на
  `TunnelCrew.Probe`, `Core` есть только у семи.
- Перенос чего-либо сверх названных двух файлов.
- Уборка, оптимизация, разрезание файлов, вынос чисел в настройки.

## boundaries — ТРИ ДРУГИЕ НОГИ ИДУТ ОДНОВРЕМЕННО

- `Network/**` и `Editor/**` — нога прибора (WIN-U1);
- `tools/**` и `tests/TunnelCrew.Core.Tests/HouseholderDecisionPipelineTests.cs` — нога гейта (WIN-U4);
- `Probe/Householder/Householder.cs`, `HouseholderRoutineState.cs`,
  `Profiles/HouseholderProfileLoader.cs`, поставочный профиль — нога хука прибытия (WIN-U2).

**Ты трогаешь только два файла и папку `Core/Householder/`.** Они не пересекаются ни с одной из
трёх. Файл `Householder.cs` пользуется шкалами — но пользуется ПО ИМЕНИ, в том же пространстве
имён, и правки не требует.

## СТОП и вопрос домой

- **Понадобилась правка исходника или `.asmdef`** — стоп. Замер направления оказался неверен, и это
  новость важнее задачи.
- **Возраст по `--follow` вышел заметно меньше заявленного** — стоп: гейт возраста не пройден, а
  компилятор про него не знает ничего.
- **Unity не поднялся или MCP не отвечает** — СТОП и скажи, какой слот поднять. Обходного пути не
  строй.

## return

1. Диф целиком — он должен быть чистым переносом.
2. Возраст обоих файлов числами, с командой, которой мерил.
3. Текст ошибки компилятора, доказывающий границу, — из Unity и из headless отдельно.
4. Прогон тестов числом.
5. **Обо что споткнулся** — даже если мелочь. Это половина ценности первой ноги конвейера.
6. Следующий кандидат и замер под ним.

## budget

Половина ноги. Если вышло больше — скажи, обо что, и это будет полезнее самого переноса.

END_OF_FILE: live/indie-game-development/work/2026-08-31-call-bedrock-marks-gauges-1.md
