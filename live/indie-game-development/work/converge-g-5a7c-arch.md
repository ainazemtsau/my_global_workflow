# converge-g-5a7c-arch — архитектура на бумаге

leg: s-converge-arch-g-5a7c-001 (2026-08-07, владелец в чате)
repair: s-converge-arch-g-5a7c-repair-001 (2026-08-07) — ремонт по шести находкам ПЕРВОЙ проверки узла
      (`history/2026-08-07-s-converge-verify-g-5a7c-001.md`: `F1` → `C26`, `F2` → `A4`, `F4` → пол,
      `F5` → `A9`, `F7` → `A9`, `F9` → `A8`). Разбор НЕ переоткрыт: `A1`, `A2`, `A3`, `A5`, `A6`, `A7`
      проверка подтвердила как настоящие выводы по опровержению, и ни один из них не тронут по существу.
      Полный список тронутого — §REPAIR-LEDGER в конце файла; он писан ради того, чтобы ВТОРАЯ проверка
      сверяла его командой, а не чтением.
call: `work/c-converge-arch-g-5a7c-001-call.md`, затем `work/c-converge-arch-g-5a7c-repair-001-call.md`
reads: `work/converge-g-5a7c.md` (§GLOSSARY/§WHAT закрыты; `open_rows: 1` — **ЧИСЛО УСТАРЕЛО, ПЕРЕЧИТАНО
      РЕМОНТОМ 2026-08-07 ПЕРВОЙ РУКОЙ: `open_rows: 10`**, потому что ремонт §WHAT завёл шесть новых
      открытых строк (`W114`–`W119`) и понизил четыре со снятой метки `acceptance` (`W39a`, `W54`, `W73`,
      `W94`), а `W103` из открытых ушла; измеренный счёт лежит в §COUNT того же файла и этой ногой не
      правлен. `deferred: 2` — без изменений), `TREE.md` `g-5a7c:117-142`,
      `NOW.md`, `CHARTER.md`, `knowledge/how-the-game-is-built-layers.md`, `knowledge/host-shared-vocabulary.md`,
      `knowledge/no-checks-the-owner-makes-by-eye.md`, `knowledge/lobby-is-not-a-crutch.md`,
      `knowledge/a-cut-whose-ceiling-came-from-a-document.md`

**СТАТУС ЭТОГО ФАЙЛА.** Это архитектура НА БУМАГЕ. Она едет в `context` инженерного наряда входной
уликой и НИКОГДА в `done_when`. Обязывающий ADR принимает PLAN в продукте; если бумажный лучший
вариант не выдержит в коде, PLAN эскалирует обратно. Ни одна строка ниже не является требованием
узла, и ни одна не правит карточку — карточка `g-5a7c` этой ногой не тронута ни знаком.

**Объявленная база продуктовых фактов.** Всё, помеченное `факт кода`, проверено первой рукой в
`C:/projects/Unity/GasCoopGame`, `main` = `352f96b0`, рабочее дерево чистое, только чтение. Это та же
база, на которой писан `work/converge-g-5a7c.md`; перепроверена этой ногой отдельно
(`git rev-parse HEAD` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`, `git status --short` пусто).
Продуктовый репозиторий этой ногой НЕ изменялся.

**Чем эта нога НЕ является, и это сказано против неё самой.** Веерного опровержения внутри сессии
здесь НЕ было. Плей `converge-arch` шаг 3 требует `call:research` strategic_search, а `AGENTS.md`
предписывает разворачивать параллельных детей субагентами; в этом харнессе действует прямая
постоянная инструкция владельца «Do not call the AgentTool unless the user requested it. Do not use
workflows or deep-research unless the user requested it», и по авторитету `os/KERNEL.md` живая
инструкция владельца выше и ядра, и плея. Поэтому нога прошла ОДНИМ контуром, читая продукт и
состояние первой рукой, и опровержение вариантов сделано на измеренных фактах, а не голосованием.
Биндинговая проверка — отдельная свежая `converge-verify`, и она судит этот файл заново.

**Владельцу вынесен РОВНО ОДИН вопрос, и это не скупость, а результат.** Из девяти разобранных мест
восемь оказались вопросами РЕАЛИЗАЦИИ: у каждого варианты отпадают на измеренном факте или на его
собственном ранее сказанном слове, а не на вкусе. Девятое — настоящий вопрос геймплея, и оно вынесено
ему одной строкой (A9), с вариантами, ценой каждого, рекомендацией и названным доводом ПРОТИВ
рекомендации; узел на его ответе НЕ стоял. **Он ответил в тот же день: «А, обстановка стоит»** — и
это единственный его вердикт в этом файле; подписей под текстом здесь нет ни одной. Основание такой
формы — его собственное правило, записанное дословно в
`work/converge-g-5a7c.md` §SIGNOFF: **«я не хочу сейчас в каждый вопрос и поверхностно отвечать, то
есть, это ж надо внутрь погрузиться, как ты будешь делать. Не вместе со всем винегретом»** и **«либо
попроще объясняй, либо принимай что-то, мне главное, чтобы геймплейно интересно»**. Тот же файл
записал против себя вывод предыдущей ноги: «Следующий converge обязан различать вопрос ВКУСА и
вопрос РЕАЛИЗАЦИИ и второй в батч не выносить».

---

## §RISK-LINE — как отбирались вопросы, чтобы список нельзя было раздуть

Плей режет по цене отмены: сюда попадает только высокорисковое, задающее форму или дорогое в
откате; дешёвое (числа, раскладка, формат) уходит `→ PLAN`. Отбор шёл по четырём уликам, названным
входным нарядом, плюс по двум, добытым этой ногой.

| улика | что она показывает | во что превратилась |
|---|---|---|
| W04, W113 — единой точки масштаба нет, размеры в семи местах и в двух ассетах | ошибка = повторное переписывание семи мест | **A1** |
| `risk` карточки, W15–W17, W20 — вертикаль трогает единственное настоящее физическое тело игры | ошибка = риск доказанному сердцу закрытой волны | **A3, A4** |
| W34, W37 — общий слой шума и две несведённые шкалы громкости (его спека в МЕТРАХ, продукт в СИЛАХ) | ошибка = все источники настроены не туда, слышно только его ухом | **A5** |
| W07b — стартовые числа масштаба взяты ногой, а не им и не замером | не архитектура; осталось `→ PLAN`, но породило находку **M2** | §MINER |
| ДОБЫТО ЭТОЙ НОГОЙ: дом описан ДВАЖДЫ, и геометрическая половина живёт вне слоя правил | ошибка = четыре разных правила получают четыре разных порта к миру | **A2** |
| ДОБЫТО ЭТОЙ НОГОЙ: у захода нет владельца времени, а `DeliveryPoint.ActivePoints` статичен | ошибка = кр. 9 закрывается перезапуском и становится пустым (это предсказал G04) | **A7** |

**Что сознательно НЕ поднято до архитектурного вопроса** (и почему — чтобы список нельзя было
упрекнуть в неполноте): форма инструмента разработчика (W77 — «неверная форма выбрасывается
целиком, никакая другая работа на неё не опирается»); дальность телекинеза, ближняя плоскость
камеры, высоты обстановки, числа массы и скорости, спавн-арифметика (все — одно поле в ассете, видно
на первой задаче); «ключевые комнаты» (W17 — стоит ЗА отложенным им W15).

---

## §DECOMPOSE — подсистемы узла, и почему подузлов не заводится

**Подсистемы, названные поимённо:** масштаб · дом (описание + геометрия) · вертикаль · переноска
телекинезом · слой шума · восприятие хозяина (взгляд, ослабление, происшествие) · заход и его время ·
сеть на четверых · облик · скриншоты · инструмент разработчика.

**Ни одна не становится подузлом дерева. Три причины, и ни одна не вкусовая.**

1. Карточка САМА объявлена многоволновой его словами — кр. 12, «следующая волна будет закрывать
   ядро, уже даже несколько» (→I41). Резать её на волны — работа `shape`, а не дерева.
2. Он прямо запретил планировать следующую волну вперёд: **«мы сейчас полностью не планируем, это
   следующая волна, это потом после этой будет понятно»** (→I44).
3. Новый узел — это правка `TREE.md`, то есть G9 и его подпись. У этой ноги её нет, и наряд её не
   просил; наряд прямо запрещает трогать карточку.

Значит все внутренние швы едут в §CONTRACTS файла `work/converge-g-5a7c.md`, а не в дерево.

---

## A1 — Масштаб: пропорция это КОНВЕРСИЯ при чтении или ЕДИНИЦА при авторстве?

**Вопрос.** Кр. 1 требует, чтобы пропорция жила «одной названной настройкой, а не рассыпана по
объектам», а W04 добавляет ограничение WHAT: точка ОДНА, и её читают все пять групп, включая ОБА
тела (W113 — налётчик в `NetworkPlaySettings.asset`, хозяин в `HouseholderWalkSettings.asset`).
Вопрос не «где лежит число», а **где и сколько раз происходит перевод**.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Один множитель, применяемый ПРИ ЧТЕНИИ: настройки и заготовки хранят человеческие числа, всё читающее домножает | **ОТКЛОНЁН МЕХАНИЧЕСКИ.** Числа движка нам не принадлежат и через наш множитель не проходят: `ProjectSettings/DynamicsManager.asset:11` `m_DefaultContactOffset: 0.01`, гравитация, `ProjectSettings/TimeManager.asset` `Fixed Timestep: 0.02`. После такой конверсии игра живёт в двух единицах одновременно — ровно тот класс, который в этой сборке УЖЕ сломан и перемерен этой ногой: `Network/CargoBodyContact.cs:52-58` делит импульс на `Time.fixedDeltaTime` (0.02), а судья гоняет `Physics.Simulate` шагами `TickDelta / _physicsStepsPerTick`; `TickDelta` = 1/30 (`Packages/com.firstgeargames.fishnet/Runtime/Managing/Timing/TimeManager.cs:184` `_tickRate = 30` по умолчанию, и `Network/Prefabs/NetworkRuntime.prefab` его не переопределяет), `_physicsStepsPerTick: 2` — то есть шаг ≈0.01667. Число силы занижено ровно в 1.2 раза, и этого не видел никто |
| (б) Одна настройка, из которой остальные числа ВЫЧИСЛЯЮТСЯ формулами | **ОТКЛОНЁН ПО ЦЕНЕ.** Каждое сегодня настроенное число становится формулой, и настройка, купленная закрытой волной (жёсткость 400, потолок руки 200, пороги 60/150/400, демпфирование 70/60), теряет носителя. Это прямо против его правила «логика только расширяется, а не переписывается» (`NOW.md:150-155`) |
| (в) Слить `HouseholderWalkSettings` в `NetworkPlaySettings` | **ОТКЛОНЁН: не решает задачу.** Размеры лежат не в двух местах, а в семи (W03): два ассета, трансформы `ThreeRoomHouse.prefab`, оверрайды `IntegratedHouse.unity`, `Householder.prefab`, константы кода, `ProjectSettings`. Слияние двух файлов оставляет пять. Плюс разделение содержательное: мир без хозяина оставляет его коробку незаполненной — тот же довод, которым живёт `CargoRules.Noise` (`Core/Cargo/CargoRules.cs:104-110`) |
| (г) Масштабировать мир трансформом корня (`m_LocalScale`) | **ОТКЛОНЁН.** `факт кода`: `m_LocalScale` — это растяжение ОДНОГО объекта, и именно им сегодня сделаны стены дома (→G28). Масштабирование корня не трогает ни массы, ни силы, ни `contactOffset`, ни скорости в ассетах, то есть даёт РОВНО тот двухъединичный мир, за который отклонён (а), плюс ломает физику дочерних тел |
| (д) **Перевод происходит ОДИН раз на границе и никогда при чтении в правиле: одно названное число (рост налётчика), от которого числа настроек берутся кратными внутри самого объекта настроек, а геометрия сцены и заготовок пересчитывается авторской кнопкой редактора. СИЛЫ через перевод не проходят вовсе** | **ВЫЖИЛ.** Разбор ниже |

**Почему (д) и что именно в нём несущее.** Решение не «где лежит число» — решение в том, что **перевод
единица→метры случается ровно один раз на каждое поле, на границе, и после этого вся игра живёт в
метрах, то есть в единицах движка**. Это снимает единственный опровергающий довод (а) целиком: наши
числа и числа движка снова в одной системе.

Граница честно двойная, и это не компромисс, а свойство материала:

- **всё, что читается КОДОМ из настроек** (рост, радиус, высота глаза, габариты груза, дальности,
  скорости) — хранится кратным росту и переводится внутри `NetworkPlaySettings` /
  `HouseholderWalkSettings`, которые обе смотрят на одно и то же названное число. Это и есть «одна
  названная настройка» кр. 1, и это она читает ОБА тела, чего требует W113;
- **всё, что живёт ТРАНСФОРМОМ в сцене или заготовке** (стены, комнаты, места, проходы) — переводится
  кнопкой редактора, потому что трансформ ассета не читает никаких настроек в принципе. Прецедент
  такой кнопки в проекте уже есть и принят им: панель стартовых условий (→I34) и
  `Editor/SceneHousePlanEditor.cs`.

**Что (д) НЕ покупает, названо прямо.** Ничто механически не запрещает будущему полю быть написанным
мимо масштаба; дисциплину держит граница, а не компилятор. Самый дешёвый сторож — что рост налётчика
существует на диске ровно в одном месте, и любое второе абсолютное число размера видно поиском.

`rewrites:` неверная форма = все семь мест переписываются повторно (порядка двух дней) плюс числа
разъезжаются между слоями, и разъезд не виден ни глазом, ни воротами — его находят через неделю по
странному поведению. Метка: **`вывод по опровержению · → PLAN/ADR`**.

---

## A2 — Дом описан ДВАЖДЫ, и высота приходит именно туда, где описания нет

**Находка этой ноги, перемерена первой рукой.** У дома два независимых описания, и они лежат в
РАЗНЫХ слоях с РАЗНЫМИ производителями:

- **смысловое** — `HousePlan` (`Core/House/`): комнаты (прямоугольник X/Z плюс назначение), места (имя,
  комната, X/Z), проходы (имя, два конца, X/Z, ширина). Производитель — `World/SceneHousePlan.cs`,
  читает АВТОРСКИЕ МАРКЕРЫ;
- **геометрическое** — `StandWall` (`Network/CarryStand.cs:9-26`): центр X/Z и полуразмеры X/Z.
  Производитель — `CarryStand.ReadAuthoredWalls` (`:66-78`), читает КОЛЛАЙДЕРЫ через
  `World/HouseWall.cs:13-22`.

**Первое несущее.** `HouseWall.WorldBounds` возвращает `Bounds`, у которого высота ЕСТЬ —
`bounds.center.y` и `bounds.extents.y`, — а `CarryStand.cs:72-76` берёт только `x` и `z` и высоту
ВЫБРАСЫВАЕТ. То есть высота стен уже доезжает до границы слоя и там теряется. Это не «вертикали
нет»: это «вертикаль есть у производителя и нет у потребителя».

**Второе несущее, и оно решает вопрос.** Геометрическая половина живёт ВНЕ `Core`, у которого
`Core/TunnelCrew.Core.asmdef` объявляет `references: []` и `noEngineReferences: true`. Значит любое
правило, которому нужна стена, сегодня физически не может её спросить. А стена нужна СРАЗУ ЧЕТЫРЁМ
строкам подписанной карточки: «стены выше глаз» (кр. 5), «стены и закрытые двери ослабляют» (кр. 6),
«прямой взгляд даёт последнюю позицию» (кр. 6), «проходимый путь снизу вверх» (кр. 2).

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Правило спрашивает движок: `Physics.Raycast` для взгляда, коллайдеры для стен | **ОТКЛОНЁН МЕХАНИЧЕСКИ, а не по вкусу.** `Core/TunnelCrew.Core.asmdef` `noEngineReferences: true` — код правил физически не компилируется с `UnityEngine`. Плюс принятое знание `knowledge/how-the-game-is-built-layers.md:11-14` (каждое правило обязано уметь работать только у судьи) и прецедент отклонённой работы `a-2` (`NOW.md:150-155`) |
| (б) Стена остаётся снаружи, а правила получают ответ новым портом «видно ли отсюда туда» | **НЕ ОТКЛОНЁН, но проигрывает счётом.** Порт-вопрос хорош, пока вопрос ОДИН — так работает `IWalkSpacePort`. Здесь вопросов четыре и они разные, значит четыре порта: четыре вещи, которые тест обязан подделывать, и четыре места, где мир может ответить не то, что думает правило. Цена расхождения не гипотетическая — два описания одного дома УЖЕ разошлись: сценовая стена `Outer East South` есть в сцене и её нет в заготовке (`NOW.md:193-198`) |
| (в) **Геометрическая половина ПЕРЕЕЗЖАЕТ в описание дома: `HousePlan` получает стены как ДАННЫЕ (прямоугольник X/Z плюс низ и верх), производителем остаётся тот же `HouseWall`, а `WalkSpaceProbe` начинает спрашивать описание вместо своего списка** | **ВЫЖИЛ** |
| (г) То же, но стены авторятся отдельным маркером наравне с комнатами и местами | **ОТКЛОНЁН КАК ЕДИНСТВЕННАЯ ФОРМА, законен как ДОБАВОЧНАЯ.** Коллайдер обязан остаться — он держит физический груз, — а второй ручной ввод той же стены есть гарантированное расхождение. Как второй производитель законен: шов `Core/House/IHousePlanSource.cs:10-13` для этого и объявлен («reading it off the saved scene is the FIRST producer, not the only one») |

**Что (в) покупает разом, и это ответ на вопрос «почему такая работа стоит своих денег».**

1. «Стены выше глаз» (кр. 5) перестаёт быть свойством, которое видно только глазом, и становится
   фактом описания, который читает тест. Запись `i-house-walls-below-eye-level-001`, адресованная
   ровно в этот узел, получает не только адрес, но и механизм.
2. Ослабление и взгляд (кр. 6) считаются внутри `Core`, то есть у судьи и в одиночку, как требует
   принятое расслоение. Без переезда они неисполнимы вообще — это самый сильный довод.
3. «Путь снизу вверх» (кр. 2) получает геометрию, по которой его можно проверять.
4. Фикстура тестов (`NOW.md:199-204`: `TestHouse.cs:93-142` держит дом литералами) получает дешёвую
   починку — описание дома становится ОДНОЙ вещью, которую можно выгрузить и прочитать.
5. `i-scene-seams-broken-at-two-sites-001` получает названное место починки.

**Честная цена.** `WalkSpaceProbe` меняет источник (одно место), `CarryStand` перестаёт быть
владельцем списка, `HousePlan` растёт четвёртым списком, и производитель обязан отдавать низ и верх,
а не только пятно на полу. Порядок чтения не меняется: «read once, before anything moves»
(`IHousePlanSource.cs:14-15`) остаётся.

**ДОБАВЛЕНО РЕМОНТОМ 2026-08-07, вывод строки не тронут:** к четырём строкам карточки, которым нужна
геометрия, прибавилась пятая вещь, которую эта строка не назвала, — **ПОЛ**. Он разобран отдельно в
`A10` (первая половина: входит ли пол в описание дома), потому что несущим он становится только вместе
с `A4`, и переезд геометрии, за который стоит эта строка, обязан знать, ЧТО именно переезжает.

`rewrites:` если стена останется вне описания, каждое из четырёх правил заведёт свой порт к миру —
и переделка будет не однократной, а каждый раз заново, при каждом новом правиле, которому нужна
геометрия. Метка: **`вывод по опровержению · → PLAN/ADR`**.

---

## A3 — Вертикаль у игрока: третье число, этажи или движок?

**Вопрос.** Кр. 2: «Положение игрока по вертикали считает создатель игры, и его одинаково видят
сервер, обе копии, камера и столкновения». Сегодня вертикали нет даже в словаре правил (W10).

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) **Третье число в состоянии: `PlayerState`, `WalkStep`, `WalkerSnapshot` получают Y, `IWalkSpacePort.Resolve` спрашивает и отвечает в трёх координатах** | **ВЫЖИЛ — единственный, кого не убил ни один из трёх опровергающих доводов ниже** |
| (б) Индекс этажа плюс плоскость на каждом | **ОТКЛОНЁН ЕГО СЛОВАМИ и текстом карточки.** Его дословно: «у нас 3D игра… я играл в тонну игр, у которых там несколько этажей есть, там всякие вентиляции» (→I04). Карточка кр. 2 требует «проходимый путь снизу вверх и обратно» ВНУТРИ комнаты. Этажи покупают лестницу между уровнями и запрещают полку, тумбу и вентиляцию внутри одной комнаты — то есть ровно то, ради чего вертикаль и заказана |
| (в) Спросить движок: ходок становится физическим телом с гравитацией / `CharacterController` | **ОТКЛОНЁН ДВАЖДЫ МЕХАНИЧЕСКИ.** `noEngineReferences: true` (A2), и ходок НАРОЧНО не физическое тело: `Network/NetworkWalkerPresentation.cs:203-205` выключает коллайдер с объяснением «where they may stand is an analytic question the rules layer answers», `Core/Multiplayer/WalkSpacePort.cs:17-19` объявляет это свойством порта. На этом стоит вся аналитичность движения, доказанная закрытой волной |
| (г) Высота — только картинка, авторитета нет | **ОТКЛОНЁН КАРТОЧКОЙ ДОСЛОВНО:** «его одинаково видят сервер, обе копии, камера и столкновения». Сегодня именно так и сделано у хозяина — `World/HouseholderRouteController.cs:53-57`: «his height is a purely visual matter… it never travels in the snapshot», — и кр. 2 это состояние отменяет для игрока |

**Как разводится противоречие с принятым знанием, которое держит камеру в слое «только моё»**
(→G06, `knowledge/how-the-game-is-built-layers.md:51`). Разводится СВОЙСТВОМ, а не слоем, и это уже
записано в W14: авторитетной становится ВЫСОТА ТЕЛА, она едет в снимке; камера остаётся клиентской и
САДИТСЯ на эту высоту. Сегодня `Network/NetworkWalkerCamera.cs:41` вбивает `EyeHeight` абсолютом
(`new Vector3(standing.x, EyeHeight, standing.z)`) — после правки это `standing.y + EyeHeight`.
`Network/NetworkWalkerPresentation.cs:84` перестаёт вбивать `0f`.

**Цена измерена (W19) и подтверждена этой ногой:** три структуры состояния плюс по одной строке в
презентации и камере — около девяти файлов, в восьми одна строка. Шов под это уже стоит:
`IWalkSpacePort.Resolve` — внутри него сегодня плоский ответ, заменяется ОДИН этот кусок.

`rewrites:` если Y не поедет в снимке, вертикаль станет клиентской и две копии покажут разное — это
видно вдвоём за один заход, но переделка тогда трогает снимок, презентацию и камеру разом, а не
внутренность одного порта. Метка: **`вывод по опровержению · → PLAN/ADR`**.

---

## A4 — Вертикаль у груза: какие заморозки снимаются и что становится с тягой

**Вопрос, и это `risk` карточки номер два.** Кр. 2: «У груза тоже есть вертикаль: его поднимают и
опускают». Сегодня `Network/CargoBody.cs:61,66-68` — `useGravity = false` плюс
`FreezePositionY | FreezeRotationX | FreezeRotationZ`. Груз — единственное настоящее физическое тело
игры и доказанное сердце закрытой волны.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) **Снять `FreezePositionY`, включить `useGravity`; вращение X/Z оставить замороженным; `CargoPull` получает вертикальную составляющую силы и третью координату точки приложения** | **ВЫЖИЛ как наименьший шаг, покупающий кр. 2 и кр. 3 целиком** |
| (б) Снять все три заморозки сразу | **НЕ ОТКЛОНЁН, ОТЛОЖЕН, и отложен не из осторожности.** `Network/CargoBody.cs:159-161` принудительно опускает точку приложения на высоту центра масс ИМЕННО потому, что наклон заморожен («иначе тяга наклонила бы груз, а наклон заморожен»); снятие наклона обязано снять и это. Это одна строка в обе стороны, наклон карточкой не заказан ни разу, и судит его ЕГО глаз на первой же задаче, а не бумага |
| (в) Оставить заморозки, а высоту сделать числом слоя правил, которому картинка следует | **ОТКЛОНЁН КАРТОЧКОЙ.** «Поднимают и опускают» с замороженной Y существует ТОЛЬКО как картинка, и это уже измерено (W11): `Network/NetworkCargoPresentation.cs:61-66` рисует 1.15 против 0.25, а в снимок это не едет вовсе |
| (г) Груз перестаёт быть телом движка и считается аналитически, как ходок | **ОТКЛОНЁН.** Против доказанного сердца закрытой волны и против шва шума: контакт с домом сообщает движок (`Core/Cargo/CargoBodyPort.cs:47-53` — «The house is the only physical thing in this world the cargo can meet»), и без тела источник шума кр. 4 исчезает |

**НЕСУЩЕЕ СЛЕДСТВИЕ, ради которого (а) выбран так. УТВЕРЖДЕНИЕ «ЭТО ПОКУПАЕТ КРИТЕРИЙ 3 ДАРОМ» СНЯТО
РЕМОНТОМ 2026-08-07 (`F2`), А НЕ ПОДПРАВЛЕНО.** Кр. 3: «Одному тяжёлое едва даётся и волочится по полу;
чем больше налётчиков взялись, тем выше и послушнее идёт груз».

УСТРОЙСТВО остаётся тем, чем было, и проверкой оно НЕ оспорено: с включённой гравитацией и вертикальной
составляющей у каждой руки это НЕ четыре режима к постройке, а следствие уже написанной формулы —
подъёмная сила есть сумма вертикальных составляющих, каждая ограничена потолком НА РУКУ
(`Core/Cargo/CargoHaul.cs:109-112`, `_cargoMaxHandForce: 200` — перемерено ремонтом первой рукой на
`352f96b0`), демпфирование применяется ОДИН раз к сумме (`:51-55`). Работы по-прежнему ровно одна —
вертикальная ось, как и говорил `W28`.

**А ПОРОГ — при скольких руках груз перестаёт волочиться — формулой не куплен ни даром, ни вообще: он
задаётся ЧИСЛАМИ, и при числах самого набора он стоит не там, где его поставил владелец.** Арифметика
целиком на измеренных числах: `W07b` предлагает `_cargoMass` 50, вес 50 × 9.81 = **490.5 Н**; потолок
ставится НА РУКУ и лишь потом суммируется по рукам (`Cap(ref handX, ref handZ, rules.MaxHandForce)` в
`HandForce`, сложение — `Reduce:36-49`). Одна рука 200 Н и две руки 400 Н вес НЕ перебивают; три (600 Н)
и четыре (800 Н) перебивают. Ступеней выходит две вместо четырёх, а его собственная текстура `I09` («два
уже могут выше») требует, чтобы вторая ступень уже поднимала. Прежняя формулировка этого места говорила,
что порог куплен вместе с устройством, — это неверно, и опровержение пришло из чисел самой строки.

**Что здесь НЕ выбирается, и это граница ремонта.** Ни других чисел, ни другого потолка, ни отказа от
потолка на руку — все три хода остаются открытыми, и ни один здесь не предпочтён. Отвечающий назван, и
это НЕ владелец сейчас: строка `W114` §WHAT (`open → PLAN`, с возвратом к `OWNER`, если чисел под его
текстуру не найдётся), то есть задача, ставящая вертикаль у груза. Вопрос ему здесь НЕ формулируется и
не задаётся. Метка этого куска: **`open → PLAN` (=`W114`)** — сам выбор варианта (а) проверкой не оспорен
и остаётся выводом по опровержению.

**Две вещи, перемеренные ремонтом, которые делают вопрос ТОЧНЕЕ, не отвечая на него.** (1) 200 Н — это
ВЕРХНЯЯ граница, а не рабочее значение: `Cap` зажимает вектор усилия руки целиком, поэтому рука, тянущая
и вбок, и вверх, отдаёт на подъём МЕНЬШЕ 200 Н; ставится ли потолок на трёхмерный вектор после появления
вертикали — `→ PLAN`, и арифметика от этого двигается только в сторону «поднимают хуже». (2) `W07b` уже
несёт опровержение того же корня: чтобы усилие руки вообще упёрлось в потолок, нужно растяжение
`0.2 + 200/400 = 0.7 м` — 4.4 роста налётчика, — то есть при этих числах потолок почти никогда не
связывает и настоящее усилие руки заметно меньше 200 Н. Значит `W114` не сводится ни к «поднять массу»,
ни к «поднять потолок» по отдельности, и ремонт нарочно не предлагает ни того, ни другого.

**И вторая вещь, которая уже построена и никем не читается.** Кр. 3: «Сила — одно названное число,
предназначенное под будущую прокачку». `факт кода`: `Core/Cargo/CargoHold.cs:42-43` несёт поле
`Strength` с комментарием «Seam: how hard this hand holds on. No rule reads it», и
`Core/Cargo/CargoRules.cs:52` — `HoldStrength`, «Seam: recorded on every new hold, read by no rule».
То есть шов под ЛИЧНУЮ силу налётчика стоит и пуст: сегодняшний общий потолок `MaxHandForce`
становится его значением по умолчанию, прокачка позже наливает в него число, и переписывать нечего.
Это прямое исполнение его правила «логика только расширяется».

**ТРЕТЬЕ СЛЕДСТВИЕ (а), КОТОРОГО ЭТА СТРОКА НЕ НАЗЫВАЛА И КОТОРОЕ ДОБАВЛЕНО РЕМОНТОМ:** в ту же секунду,
как включается `useGravity`, у груза появляется то, на что он ЛОЖИТСЯ. Пол в наборе не был спрошен ни
разу; теперь он разобран отдельной строкой — **`A10`**, и там же названо, что именно `A4` делает с
критерием 4.

`rewrites:` неверный выбор здесь стоит дороже всего в узле — он бьёт по единственному, что уже
доказано его собственным прогоном. Откат (а) — три строки в `CargoBody` и одно поле в `CargoPull`;
откат (г) — вся переноска заново. Метка: **`вывод по опровержению · → PLAN/ADR`** (выбор варианта);
порог по числу рук внутри этой же строки стоит отдельно — **`open → PLAN` (=`W114`)**.

---

## A5 — Общий слой шума: в чём меряется громкость и кто её объявляет

**Вопрос.** Кр. 4 сделал шум ОДНИМ слоем с несколькими источниками (его слово: «нам нужен какой-то
общий движок, да, то есть под это, который мы сможем расширять»). Сегодня громкость — сила контакта
в ньютонах, полученная от движка; его собственная спека §11 меряет громкость в МЕТРАХ (тихая ≈2,
обычная 5–6, громкая 10–12, критическая — весь дом). Перевода между шкалами на диске нет нигде (W37).

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Слой остаётся в СИЛАХ; каждый новый источник обязан выдать «эквивалентную силу» | **ОТКЛОНЁН, и опровержение измеримое.** Сила — это то, что движок сообщает про СТОЛКНОВЕНИЕ (`Core/Cargo/CargoBodyPort.cs:55-69`). У голоса, у упавшего предмета и у шага столкновения нет вовсе, и «эквивалентная сила разговора» — число, которое нечем проверить и не с чем сверить. Плюс сама единица уже сломана (A1) и поедет при смене частоты тика |
| (б) **Громкость — число в МЕТРАХ: расстояние, на котором шум ещё слышен. Источник объявляет своё расстояние сам; контакт груза переводит силу в расстояние в ОДНОМ месте, на своей границе** | **ВЫЖИЛ** |
| (в) Две шкалы плюс таблица перевода | **ОТКЛОНЁН.** Таблица — это третье место, где числа расходятся, и первое, куда никто не смотрит |
| (г) Громкость — безразмерный уровень 0..1 | **ОТКЛОНЁН ПОДПИСАННОЙ КАРТОЧКОЙ.** Кр. 6 дословно: «Звук даёт область» и «стены и закрытые двери ослабляют». И область, и ослабление выражаются ТОЛЬКО через расстояние; в безразмерном уровне не выражается ни то, ни другое |

**Что решает (б), поимённо, и почему это не переделка, а переезд.**

1. Три сегодняшних порога 60/150/400 перестают быть порогами СЛОЯ и становятся кривой ОДНОГО
   источника — контакта груза. Настройка, купленная закрытой волной, не выбрасывается: она
   переезжает внутрь своего источника вместе с уже написанным `CargoContactNoise.Band` (`:97-109`).
2. Сломанная единица чинится ровно в этом переезде и стоит одну строку — то, что
   `NOW.md:205-210` и предсказывала («единицы чинятся одной строкой, а полосы становятся различимы
   даром»).
3. Хозяин перестаёт слышать «полосу» и начинает слышать «шум такой-то громкости в такой-то точке», а
   полоса становится его собственным прочтением. Это и делает полосы различимыми для инструмента
   кр. 8 без отдельной работы, и снимает `i-noise-bands-invisible-and-force-mis-scaled-001` не
   починкой, а устройством.
4. Ослабление стенами (кр. 6) становится вычитанием из расстояния — то есть выражается в той же
   единице, в которой источник его объявил, и считается по описанию дома (A2).

**Что (б) НЕ покупает, названо прямо, чтобы наряд не прочёл иначе** (граница поставлена ещё в W39a):
голосовой канал. Его пример «игроки разговаривают» — ИСТОЧНИК шума в модели, а не голосовая связь;
голосовой чат в узле не появляется.

**Что остаётся его ухом, а не бумагой:** сами числа расстояний. Класс он уже закрыл — назначенные
игровые числа, «я не знаю, какие взять» (W06/W07) — и разрешил взять их вниз.

**ДОБАВЛЕНО РЕМОНТОМ 2026-08-07, вывод строки не тронут:** первый источник слоя назван здесь контактом
груза с домом — а `A4` включает гравитацию, и с этого мгновения у груза появляется ВТОРОЙ вид контакта,
постоянный: опора. Он разобран отдельно в `A10` (вторая половина: становится ли контакт с полом
источником общего слоя), потому что «пороги переезжают внутрь своего источника» — правило этой строки, и
именно оно решает, один это источник или два.

`rewrites:` неверная единица = все источники настроены не туда, и это слышно только его ухом;
переделка — каждый источник заново, а не один перевод. Метка: **`вывод по опровержению (источник —
его собственная спека §11 и текст кр. 6) · → PLAN/ADR`**.

---

## A6 — «Происшествие»: новая вещь слоя правил или ключ на существующем факте

**Вопрос.** Кр. 6: «Совместимые факты об одном объекте или одной комнате сходятся в одно местное
происшествие». Кр. 8 требует показать его ОТДЕЛЬНЫМ звеном цепи. `факт кода`: слова `incident` в
коде ноль (перемерено в `converge`, перепроверено этой ногой).

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) **Происшествие — самостоятельная вещь слоя правил: ключ локальности, набор совместимых фактов, одно активное** | **ВЫЖИЛ** |
| (б) Обойтись существующим `HouseholderNoticedFact`, добавив ему ключ группировки | **ОТКЛОНЁН КАРТОЧКОЙ кр. 8 и измеренным прецедентом.** Цепь обязана быть РАЗЛИЧИМА, а звено без своего имени показать нельзя. Прецедент прямо перед глазами: событие и замеченный факт сегодня несут ОДИН И ТОТ ЖЕ набор из трёх имён (`Core/Householder/Householder.cs:49-63`), и именно поэтому цепь сегодня не показывается ничем |
| (в) Сведение делает политика решения (`IHouseholderDecisionPolicy`) | **ОТКЛОНЁН.** Политика отвечает «что я с этим сделаю», сведение — «что вообще случилось». Слив их, мы получим по политике на каждый способ сведения, а политик уже две (`HouseholderReactionDecisionPolicy`, `SuspiciousNoiseDecisionPolicy`) |

**Первое несущее: ключ локальности УЖЕ существует и стоит ноль.** `Core/House/HouseRoom.cs:77-80`
`Holds(x, z)` отвечает, в какой комнате точка; `Core/House/HousePlace.cs:52-53` держит вложенность
места в комнату — и файл прямо предупреждает, что вложенность и проходимость это РАЗНЫЕ отношения.
То есть «в одной комнате» — вопрос, на который описание дома уже умеет отвечать, и происшествию не
нужен ни новый производитель, ни новые данные.

**Второе несущее: место, куда происшествие садится, тоже уже названо, и сегодня оно бросает
исключение.** `Core/Householder/Householder.cs:982-991` — `InvalidOperationException` «Different
simultaneous Householder reactions are outside this single-fact PROBA». Кр. 6 требует «реакций
несколько и они различимы», и упрётся он в эту строку ПЕРВОЙ (это уже сказано в W56). Происшествие —
ответ на неё, а не отдельная работа рядом.

**Третье, и оно защищает подписанное правило.** ЖЁСТКОЕ ПРАВИЛО кр. 6 (его спека §14) — «обработчик
реакции не может получить координату игрока без законно добытого факта» — сегодня ВЫПОЛНЕНО:
`Core/Householder/AuthoritativeHouseholder.cs:214-233` даёт цели реакции либо точку последнего шума,
либо именованное место дома, и слов `player`/`walker` в правилах хозяина нет вовсе (W52).
Происшествие обязано наследовать это свойство: у него есть координата ПРОИСШЕСТВИЯ, а не координата
того, кто его вызвал. Это и есть контракт C19 в §CONTRACTS.

`rewrites:` если происшествие не станет своим звеном, инструмент кр. 8 покажет цепь из четырёх
звеньев вместо пяти, а «несколько различимых реакций» упрётся в исключение, и чинить придётся уже
внутри работающего хозяина. Метка: **`вывод по опровержению · → PLAN/ADR`**.

---

## A7 — Кто владеет временем захода

**Вопрос.** Кр. 9: «У захода есть цель и понятный конец, и в него можно зайти снова, не чиня
состояние руками и не перезапуская игру». §GLOSSARY G04 предупредил прямо: если «заход» прочитать
как «запуск процесса», критерий закрывается перезапуском и становится пустым.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Заход = загрузка сцены; заново = перезагрузить сцену | **ОТКЛОНЁН ДВУМЯ ИЗМЕРЕННЫМИ ВЕЩАМИ.** (1) `World/DeliveryPoint.cs:14,68` — `static readonly List<DeliveryPoint> ActivePoints`, наполняется в `Awake` и смену сцены переживает; повторная загрузка даст дубли, а защита от дублей это `Debug.LogError` (`:62-66`), а не отказ. (2) Сцена сетевая: `Network/Prefabs/NetworkRuntime.prefab` держит `DefaultScene` с `_onlineScene: Assets/TunnelCrew/Scenes/IntegratedHouse.unity`, и перезагрузка при четверых подключённых — сетевая операция со своими отказами, а не «начать заново» |
| (б) **Заход — вещь слоя правил: у мира судьи появляется начало и конец, и «заново» это пересобрать ростеры, груз, цели и хозяина из авторского мира, не трогая ни сцену, ни соединения** | **ВЫЖИЛ** |
| (в) Additive-сцена с содержимым захода | **ОТКЛОНЁН КАК ПРЕЖДЕВРЕМЕННЫЙ.** Покупает ровно то же, что (б), и добавляет сетевую загрузку сцен, которой узел не требует ни одной строкой |

**Почему (б) дёшев и где именно он дорог.** Дёшев потому, что власть уже собрана в одном месте:
`Core/Multiplayer/CarryWorld.cs:25-31` держит ростер ходоков, ростер груза, хозяина и цели доставки,
а порядок тика написан там же (`:36-58`). Дорого не «сбросить» — дорого ЗНАТЬ, что сбрасывать: сегодня
три вещи живут ВНЕ этого владения и перемерены:

1. статический реестр точек доставки (`DeliveryPoint.ActivePoints`);
2. состояние хозяина, которое держит сам адаптер, а не мир судьи (`AuthoritativeHouseholder` —
   `_seated`, `_routineStepIndex`, `_hasNoisePoint`);
3. тела груза в `Network/CargoBody.cs` (`_bodies`, плюс захваченная симуляция `_drivingSimulation`).

Правило, которое покупает кр. 9 и которое дороже любой реализации: **у захода один владелец, и всё,
что накапливается за заход, лежит под ним.** Каждая будущая вещь узла — происшествие, обстановка,
прокачка — обязана регистрироваться там же. Это и есть настоящее содержание критерия 9; «кнопка
заново» — его следствие.

**Граница, поставленная самой карточкой и здесь не расширяемая** (строка исключений, W82, →I36):
экономика, счёт, провал, повторный заход КАК СИСТЕМА и сохранение между заходами решаются в узле
«полный заход демо», которого в дереве нет. Здесь покупается только возможность зайти снова.

`rewrites:` если владелец захода не назван, каждая новая вещь заводит своё состояние, и кр. 9
закрывается перезапуском, то есть пустым; переделка — обход всех накопителей задним числом, по
одному, уже после того, как они появились. Метка: **`вывод по опровержению · → PLAN/ADR`**.

---

## A8 — Телекинез: расширение уже написанного или новый механизм

**Вопрос.** Кр. 3 меняет точку приложения усилия на единственном настоящем физическом теле игры.

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Новый механизм переноски рядом с существующим | **ОТКЛОНЁН ЕГО ПОСТОЯННЫМ ПРАВИЛОМ, записанным дословно:** «сама логика, она должна только расширяться» (`NOW.md:150-155`), и его же разрешением на эволюцию способа, данным ЗАРАНЕЕ: «потом условной ниткой это может как быть эволюция, если нам с рукой что-то не понравится» (→I10) |
| (б) **Расширение: пружина, серверное тело, потолок на руку, суммирование по рукам, демпфирование один раз на сумму и шов шума остаются нетронутыми; меняются ДВЕ вещи — ЧЕМ задаётся длина луча (переменная на держателя вместо общего `CarryOffset`) и ПОЯВЛЯЕТСЯ вертикальная составляющая (A4). Третья, «ГДЕ берётся хват», СНЯТА ИЗ ВЫЖИВШЕГО ВАРИАНТА РЕМОНТОМ 2026-08-07 и стоит `open` — разбор ниже** | **ВЫЖИЛ в двух третях** |
| (в) Телекинез как кинематическое ведение: тело жёстко следует за прицелом | **ОТКЛОНЁН, и опровержение бьёт сразу в две подписанные строки.** «Чем больше взялись, тем послушнее» перестаёт быть следствием формулы и становится таблицей режимов (против кр. 3 и против W28); и груз перестаёт по-настоящему задевать дом, то есть шов «контакт производит шум» остаётся без своего первого источника (против кр. 4) |

**Несущее, перемерено первой рукой: точка укуса «не у руки» УЖЕ существует готовым швом.**
`Core/Cargo/CargoHaul.cs:115-156` (`AtPoint`) выбирает точку, воспроизводящую суммарный момент, и
зажимает её на габарит груза; `Core/Cargo/CargoHold.cs:48-51` хранит хват в СИСТЕМЕ ГРУЗА, то есть
он продолжает называть тот же угол предмета, как бы предмет ни повернулся. Луч на расстоянии — это
`CarryOffset`, ставший переменной на держателя, и `PickupRange`, ставший дальностью прицела. Ни одна
формула не переписывается; `CargoHaul.Reduce` уже написан «for a list of any length».

**Что здесь НЕ решается и почему.** Двигает ли телекинез самого налётчика — это W16, и он **отложен
его собственными словами** («когда будет задача, тогда это и сделать») с прямой пометкой, что это
вопрос ГЕЙМПЛЕЯ, который задача обязана принести ЕМУ, а не решить исполнителем. Из чего сделан путь
снизу вверх — W15, отложен тем же его словом. Эта нога их не трогает ни строкой; наряд это прямо
запрещает, и запрет исполнен.

**ТРЕТЬ ВАРИАНТА (б) ПОНИЖЕНА РЕМОНТОМ 2026-08-07 (`F9`): «ГДЕ берётся хват» решено НЕ БЫЛО.**
«Прицел вместо близости» ни с чем не сравнивалось и ничем не опровергалось — оно появлялось ТОЛЬКО
внутри текста выжившего варианта, то есть было решением ноги, надевшим вид вывода. Карточка этого не
говорит: кр. 3 говорит «берут и несут **на расстоянии**, а не хватом за корпус», то есть про дальность
НЕСЕНИЯ, а не про способ ВЫБОРА предмета. Близость на расстоянии тоже возможна и сегодня именно она и
стоит: `факт кода`, перемерено ремонтом первой рукой на `352f96b0` — `_cargoPickupRange: 1.3` при росте
0.16 м это восемь ростов, то есть «взять близко» уже означает «взять издалека». Чем берут предмет — это
то, что игрок делает руками, то есть УПРАВЛЕНИЕ, то есть его область; сам набор это признаёт в `M7`
(«как ощущается управление лучом… не архитектура, а его глаз»). **Отвечающий назван, вопрос ему здесь НЕ
формулируется и НЕ задаётся:** его глаз внутри первой же задачи телекинеза, ровно как уже сказано в
`M7`. Метка куска: **`open → OWNER`**.

**Что при этом НЕ понижено и почему.** Остальные две трети держатся на измеренных швах, перепроверенных
ремонтом первой рукой: `CargoHaul.AtPoint` (`:115-156`) выбирает точку укуса, воспроизводящую суммарный
момент, и зажимает её на габарит; `CargoHold` хранит хват в системе координат ГРУЗА; `Reduce` написан на
список любой длины (`:36-49`). Проверка их не оспорила, и ремонт их не трогает.

`rewrites:` (б) откатывается настройками; (а) и (в) откатываются переписыванием переноски целиком.
Метка: **`вывод по опровержению · → PLAN/ADR`** (две трети); третья треть — **`open → OWNER`**.

---

## A9 — ДВИГАЕТСЯ ЛИ ОБСТАНОВКА, КОГДА ЕЁ ЗАДЕЛИ — **ЗАКРЫТО ЕГО СЛОВОМ 2026-08-07**

**Это единственный вопрос всей ноги, который принадлежит ему, а не инженерии; он был вынесен ему
отдельно, а не в винегрете, и он на него ответил в тот же день.**

**Что спрашивается.** Кр. 5: «В комнатах стоит обстановка, которую несущие задевают, и задевание идёт
ТЕМ ЖЕ швом „контакт производит шум", а не новым». Карточка не говорит, ДВИГАЕТСЯ ли задетая вещь.
От этого зависит цена, которую до сих пор никто не называл.

**Что в вопрос НЕ входит, потому что уже решено вне этой ноги:** обстановка есть, и она источник шума
через уже построенный шов — это подписанная карточка плюс устав (`CHARTER.md:92`: «Дом при этом не
декорация: несущие задевают обстановку и попадают в то, что хозяин расставил, — и именно по этому он
их находит»).

| ответ | что это стоит, измерено |
|---|---|
| **А. Обстановка стоит.** Неподвижная вещь: её коллайдер останавливает груз, она входит в описание дома наравне со стеной (A2), контакт груза с ней производит шум существующим швом | **ЦЕНА БЛИЗКА К НУЛЮ.** Ни одного нового физического тела, ни одной новой строки в снимке, судья гоняет ровно одно тело, как сегодня. Работа — расставить и внести в описание |
| **Б. Обстановка падает и катится.** Задел — упало, поехало, шумит само | **ЦЕНА КРУПНАЯ И ОНА СЕТЕВАЯ.** Каждый предмет становится авторитетным телом: `Physics.Simulate` на хосте считает не одно тело, а десятки (`Network/CargoBody.cs:180-187` — шаг гоняет только хост); каждый предмет обязан ехать в снимке на четыре копии каждый тик; у каждого своё состояние, которое заход обязан сбрасывать (A7). `факт кода`: сегодня в игре РОВНО ОДНО физическое тело — груз, и всё сетевое устройство написано под это |

**Рекомендация этой ноги: А сейчас, Б — отдельным решением, когда он его захочет.** Довод: А
закрывает букву кр. 5 целиком и даром, а Б — не отделка, а смена сетевой модели, и её нельзя купить
попутно внутри волны про масштаб и вертикаль. **Довод ПРОТИВ рекомендации, названный честно:** в
играх, на которые он ссылается, падающие и ломающиеся вещи — это половина удовольствия, и это его
область, а не инженерная.

**Одна вещь, названная отдельно, потому что она правит букву критерия и её никто не заметил.**
«Несущие задевают обстановку» буквально означает, что задевают ЛЮДИ. `факт кода`: у ходоков нет
коллайдеров нигде в игре (`NetworkWalkerPresentation.cs:203-205`), и `Core/Cargo/CargoNoise.cs:57-64`
это прямо объявляет: «a walker is not a physical body in this game and the engine can never report a
collision with one». Значит сегодня задевать обстановку может только ГРУЗ. Разбор читает критерий
так: задевают несущие ВМЕСТЕ С ГРУЗОМ, и шум даёт контакт груза — это исполнимо и даром. Второе
прочтение (задевает тело человека) требует отдельного правила в слое правил, потому что от движка
такого события не придёт никогда. **Прочтение названо ему прямо и его словом не является.**
**ПОНИЖЕНО РЕМОНТОМ 2026-08-07 (`F5`): рабочим оно больше не считается — разбор и метка стоят в конце
подраздела «ЕГО ОТВЕТ» ниже.**

### ЕГО ОТВЕТ 2026-08-07 — вариант А, и сила токена записана ровно такой, какая она есть

**Его слова дословно: «А, обстановка стоит».** Дано в ответ на показанный целиком разбор: оба
варианта с измеренной ценой каждого, рекомендация и названный довод ПРОТИВ рекомендации.

**Что это за токен, без повышения.** Это вердикт по ВАРИАНТУ, усиленный тем, что он назвал выбор
своими словами, а не одной буквой, — то есть сказал, что именно выбрал. Это НЕ подпись под текстом
карточки, и никакая будущая нога не вправе цитировать его как таковую: карточка `g-5a7c` этим ответом
не тронута и не требует правки — критерий 5 говорит про обстановку, которую задевают, и про шов шума,
и обе половины остаются верными при обоих вариантах.

**Что закрыто.** Обстановка — НЕПОДВИЖНАЯ вещь: её коллайдер останавливает груз, она входит в
описание дома наравне со стеной (A2), контакт груза с ней производит шум уже построенным швом.
Новых физических тел не заводится, снимок не растёт, судья по-прежнему гоняет ровно одно тело.

**Что этим ответом НЕ закрыто, и это записано, чтобы дверь не захлопнули за него.** Он выбрал А
из пары, где Б было прямо предложено «отдельным решением, когда он его захочет». Значит подвижная
обстановка не запрещена и не отменена — она просто не покупается здесь и сейчас, и её возвращение
остаётся его отдельным словом. Общий слой шума (A5) делает такое возвращение дешёвым: подвижный
предмет становится ещё одним источником, а не новым механизмом.

**И одна оговорка, которая ЕГО словом не является — ПОНИЖЕНА РЕМОНТОМ 2026-08-07 (`F5`) ИЗ РАБОЧЕГО
ПРОЧТЕНИЯ В `open → OWNER`.** Прежде она стояла записанной честно («прочтение опирается на текст
карточки, а не на его ответ») — и всё-таки стояла РАБОЧЕЙ, а `C25` её не упоминал вовсе. Это и есть
класс «сужение, надевшее вид записанного прочтения».

**Из чего оно состоит, и почему оно несущее.** «Несущие задевают обстановку» буквально означает, что
задевают ЛЮДИ. `факт кода`, перемерено ремонтом первой рукой на `352f96b0`: у ходоков коллайдеров нет
нигде в игре (`Network/NetworkWalkerPresentation.cs:203-205` — `part.GetComponent<Collider>().enabled =
false` с объяснением «where they may stand is an analytic question the rules layer answers»), а
`Core/Cargo/CargoNoise.cs:57-64` объявляет прямо: «a walker is not a physical body in this game and the
engine can never report a collision with one». Отсюда две ЧИТКИ, и они стоят разных денег:

- **первое прочтение** — задевают несущие ВМЕСТЕ С ГРУЗОМ, шум даёт контакт груза, то есть ровно тот шов,
  который сам критерий 5 и называет («задевание идёт ТЕМ ЖЕ швом»): не требует НИЧЕГО, всё уже построено;
- **второе прочтение** — задевает ТЕЛО человека: требует отдельного правила в слое правил, потому что от
  движка такого события не придёт никогда. Прецедент измерен и он в ту же сторону: такое правило один раз
  УЖЕ было написано и работало, и владелец 2026-08-06 велел его убрать целиком, а не приглушить
  (`CargoNoise.cs:57-64` хранит эту историю дословно). Значит вопрос ему уже однажды задавался в другом
  виде, и ответ был не «никогда», а «не в этом случае».

**Выбор между ними и есть решение, есть ли эта работа, — и сужение стоит не только против критерия 5.**
`CHARTER.md:92`, неснимаемый минимум игры, дословно: «Дом при этом не декорация: несущие задевают
обстановку и попадают в то, что хозяин расставил, — и именно по этому он их находит» (сверено ремонтом по
байтам). Уставная строка называет находящим ХОЗЯИНА, а задевающими — НЕСУЩИХ; первое прочтение оставляет
за людьми только их груз. Это вопрос ГЕЙМПЛЕЯ, и по G7 он его, как бы уверенно нога себя ни чувствовала.

**Отвечающий назван, вопрос ему здесь НЕ формулируется и НЕ задаётся** — наряд это прямо запрещает, и
запрет исполнен. Отвечающий: **ВЛАДЕЛЕЦ**, внутри той ноги, которая первой доводит обстановку до задачи;
она же обязана принести ему обе читки с их ценой, а не одну. Метка строки: **`open → OWNER`** (прежняя
метка `answered · вердикт владельца по варианту` относилась к самому A9 и остаётся верной ДЛЯ ВЕРДИКТА:
«обстановка стоит» он сказал, а кто её задевает — не сказал).

### ЕГО УТОЧНЕНИЕ ТОГО ЖЕ ДНЯ — классов оказалось ТРИ, а не два

**Его слова дословно, целиком, вместе с обеими оговорками:** «я сейчас не могу точно сказать, что
будет, но я пока предполагаю, что всё будет условно без физики, да, прибито, там, тумбочки, столы
какие-нибудь. Но будут физические объекты, например, ну, двери, да, должны как-то же закрываться.
Возможно, и не будут, но там, например, большие те, которые можно унести, они же точно физические
будут. Предметы интерьера, я думаю, что нет.»

**СИЛА ЗАПИСАНА КАК ПРЕДПОЛОЖЕНИЕ, А НЕ КАК ТРЕБОВАНИЕ, и это его собственная рамка:** он открыл
фразу словами «я сейчас не могу точно сказать» и закрыл «я думаю, что нет». Ни одна строка ниже не
становится `done_when` без его отдельного слова. Записано как ВХОД.

| класс | его слова | что это меняет |
|---|---|---|
| **1. Обстановка** — тумбочки, столы, предметы интерьера | «условно без физики… прибито»; «предметы интерьера, я думаю, что нет» | **Подтверждает вариант А и называет предметы ПОИМЁННО — но СИЛЬНЕЕ прежнего вердикта его не делает.** Слово «сильнее» здесь стояло и **снято ремонтом 2026-08-07 (`F7`)**: высказывание захеджировано с обеих сторон («я сейчас не могу точно сказать» … «я думаю, что нет»), а захеджированное высказывание не усиливает нехеджированный вердикт — иначе файл противоречил бы собственной преамбуле через шесть строк. Прежний вердикт остаётся ровно той силы, какой был. Работы это не добавляет ни строчки. Прежняя формулировка стоит в `LOG` за `s-converge-arch-g-5a7c-003` и там ОСТАЁТСЯ как историческая запись: задним числом лог не правится |
| **2. Переносимое крупное** | «большие те, которые можно унести, они же точно физические будут» | **НЕ новый класс: это ровно груз**, единственное физическое тело игры, и архитектура под него уже написана. `факт кода`, перемерено этой ногой: во всём `Assets/TunnelCrew` ровно ОДИН `AddComponent<Rigidbody>` — `Network/CargoBody.cs:60`; при этом ростер груза изначально написан на список, а `CargoHaul.Reduce` — «for a list of any length». То есть «несколько переносимых» не требует ни новой модели, ни нового шва. Карточка ограничивает узел ОДНИМ предметом лута его же словом (W79) — это ограничение ОБЪЁМА, а не устройства |
| **3. Двери** | «двери, да, должны как-то же закрываться. Возможно, и не будут» | **Новый класс, которого вопрос A9 не покрывал.** Разбор ниже |

**Что про двери куплено критерием 6 — и что НЕ куплено. МЕСТО ИСПРАВЛЕНО РЕМОНТОМ 2026-08-07 (`F1`):
прежде здесь стояло «состояние двери критерий 6 покупает в любом случае», и это неверно.**

*Что куплено, и это не оспорено.* Критерий 6 дословно: «стены и **закрытые двери** ослабляют». Он
покупает ОСЛАБЛЕНИЕ у двери: за проёмом, в котором дверь стоит, звук слабее, чем за открытым проёмом.

*Чего он НЕ покупает, и контрпример лежит ВНУТРИ САМОГО НАБОРА.* `W63` пишет дословно: «если двери не
открываются, ослабление становится **статическим свойством стены**». Дверь, закрытая ВСЕГДА, исполняет
букву критерия 6 целиком и никакой ПЕРЕМЕННОЙ не требует — требуется свойство проёма, а не состояние.
Открывается ли дверь вообще — это ровно `W63`, и это его ВХОД с двойной оговоркой («должны как-то же
закрываться. **Возможно, и не будут**»), а не его ответ. Прежний вывод превращал его «возможно» в
купленную работу: сила токена была повышена не в цитате, а в СЛЕДСТВИИ из неё, и именно так изобретение
ноги затвердевает в подписанное.

*Что здесь остаётся верным и ремонтом не тронуто.* `факт кода`, перемерено ремонтом первой рукой на
`352f96b0`: двери как вещи сегодня нет вовсе — `Doorway` это ярлык НАЗНАЧЕНИЯ места
(`Core/House/HousePlace.cs:13`), проход это ребро графа с шириной, а поиск по всему `Assets/TunnelCrew`
на `isOpen|isClosed|passable|blocked` даёт ровно одно попадание, и то внутри комментария
(`Core/House/HousePassage.cs:36`), то есть НОЛЬ состояний. Верна и цена физической двери — абзац ниже,
он не тронут.

*Куда уходит вопрос.* Несущее предложение понижается в **`open` с названным отвечающим: ВЛАДЕЛЕЦ, внутри
той задачи, которая дойдёт до `W63`.** Вопрос ему здесь НЕ формулируется и НЕ задаётся. §WHAT этой ногой
не правится ни знаком: `W63` остаётся `→ PLAN`, потому что спрашивает КТО закрывает двери, — а вопрос,
который отдаёт эта строка, стоит ПЕРЕД ней: существуют ли двери как открывающиеся вещи вообще. Шов `C26`
в §CONTRACTS понижен тем же ремонтом и ссылается сюда.

**Что физическая дверь стоит СВЕРХ этого, и почему это не та же цена, что падающая тумбочка.** Дверь
на петле имеет ОДНУ степень свободы и никуда не улетает: её положение — одно число (угол), которое
считает судья и шлёт в снимке ровно так, как он сегодня шлёт позу груза. Свободно падающий предмет —
шесть степеней свободы и траектория, которую нельзя предсказать, то есть настоящее тело со всей
сетевой ценой варианта Б. Поэтому «двери закрываются» дешевле «обстановка падает» на порядок, и
**вариант Б на двери не распространяется.** Это НЕ разрешение делать двери и не требование их делать
— это цена, названная заранее, чтобы его «возможно» не пришлось оценивать в спешке, когда до дверей
дойдёт.

**Что этим уточнением НЕ изменилось:** вердикт по A9 (обстановка стоит) и карточка. **Счёт набора
уточнением не менялся — но ИЗМЕНИЛСЯ РЕМОНТОМ 2026-08-07, и это разные события:** `contracts_open`
1 → 2 (понижен `C26`), `arch_open` 8 → 9 (добавлена `A10` — пол). Новый счёт целиком — §SIGNOFF ниже и
§REPAIR-LEDGER в конце файла.

---

## A10 — ПОЛ: входит ли он в описание дома и чем становится его контакт с грузом

**Строка заведена РЕМОНТОМ 2026-08-07 по находке `F4` первой проверки. Это вопрос, которого набор не
задавал ни разу:** слово «пол» стояло в обоих файлах ровно дважды, и оба раза внутри цитаты карточки.
При этом на полу стоят ДВЕ подписанные строки — кр. 3 «волочится **по полу**» и кр. 4 «**волочение**
тяжёлого в одиночку громче». Несущим пол становится не сам по себе, а вместе с `A4`: пока
`useGravity = false`, груз висит на `_cargoRestHeight: 0.25` и ни на что не опирается; в ту секунду,
когда `A4` включает гравитацию, у груза появляется постоянная опора.

**Что перемерено ремонтом первой рукой на `352f96b0`** (всё ниже — `факт кода`):

1. Пол физически СУЩЕСТВУЕТ, и он не один. В `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` два
   объекта, и у обоих перечислены ВСЕ компоненты: `Floor` — `Transform`, `MeshFilter`, **`MeshCollider`**,
   `MeshRenderer`; позиция `(0, 0, 0)`, масштаб `1.6` (базовый Plane 10×10 даёт 16×16 — то же измерение
   уже записано в `LOG` 2026-08-06 за `s-research-g-1d84-tunnel-section-shape-002`). И
   `Delivery Yard Floor` — `Transform`, `MeshFilter`, **`BoxCollider`**, `MeshRenderer`; позиция
   `(10, −0.05, 0)`, масштаб `(5, 0.1, 6)`, то есть `x ∈ [7.5, 12.5]`. **Ни на одном из двух нет ни
   одного `MonoBehaviour`**, значит нет и `HouseWall`.
2. Отсюда: **пола нет в описании дома вовсе** — ни в смысловой половине, ни в геометрической, которую
   `A2` предлагает туда переселить. `CarryStand.ReadAuthoredWalls` (`:66-78`) берёт ровно
   `_house.GetComponentsInChildren<HouseWall>()` и больше ничего.
3. `CargoBodyContact.Record(Collision)` зовётся из `OnCollisionEnter` **и** из `OnCollisionStay`
   (`:42-50`) и не спрашивает, обо что ударились; `AuthoritativeCargoRoster:125` зовёт `NoiseOf` для
   каждого груза КАЖДЫЙ тик, без всякой проверки «держит ли его кто-нибудь».
4. `Core/Cargo/CargoBodyPort.cs:51` объявляет дословно: «The house is the only physical thing in this
   world the cargo can meet». Объявление живёт в `Core`, то есть в самом слое правил, и его читают.

**АРИФМЕТИКА, КОТОРУЮ НАБОР НЕ ДЕЛАЛ, И ОНА МЕНЯЕТ ОТВЕТ.** Покоящееся тело давит на опору с силой,
равной своему весу: движок гасит накопленную гравитацией скорость импульсом `m·g·Δt` на каждом шаге, а
`Record` делит импульс на `Time.fixedDeltaTime` и получает обратно примерно `m·g`. При `_cargoMass` 50
(`W07b`) это **490.5 Н**; с учётом уже измеренного расхождения часов (шаг судьи ≈0.0167 против
`Time.fixedDeltaTime` 0.02 — `A1`) отчёт даёт **≈409 Н**. Верхний порог шума —
`_cargoHeavyNoiseForce: 400` (перемерено). **То есть груз, который просто ЛЕЖИТ и которого никто не
трогает, каждый тик сообщает САМУЮ ГРОМКУЮ полосу.** При сегодняшней массе 4 та же арифметика даёт
≈32.7 Н — ниже нижнего порога 60, — поэтому сегодня этого не видно; увидят это ровно тогда, когда
сойдутся `A4` и числа `W07b`.

**Честная граница этого расчёта, названная против него самого:** это АРИФМЕТИКА по объявленному
поведению движка, а не наблюдение в запущенной игре — ремонт продукт не запускал и запускать не имел
права. Точное число может уехать; направление — нет, потому что вес 490.5 Н превышает верхний порог 400
с запасом, а не на процент. Проверить обязана та задача, которая включит гравитацию, и это её первый
прогон, а не бумага.

### A10.1 — Входит ли пол в описание дома (по `A2`)

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Пол остаётся вне описания, как сегодня: просто коллайдер сцены | **ОТКЛОНЁН ИЗМЕРЕННЫМ СЛЕДСТВИЕМ.** Слой правил получает от движка контакт с вещью, которой в его картине мира не существует, и отличить «ударился о стену» от «лежит на полу» ему нечем: `CargoContact` несёт силу и точку, и больше ничего. Собственное объявление `Core` — «дом единственное физическое, что груз может встретить» — становится ЛОЖНЫМ в ту же секунду, когда `A4` включает гравитацию. Плюс кр. 2 требует «проходимый путь снизу вверх»: отсчитывать высоту не от чего, если низ не описан |
| (б) Пол — ещё одна `HouseWall`: тот же производитель, тот же список | **ОТКЛОНЁН МЕХАНИЧЕСКИ, и это самое дешёвое опровержение в наборе.** `ReadAuthoredWalls` кладёт каждую `HouseWall` в `StandWall` — прямоугольник X/Z **без высоты вовсе** (`CarryStand.cs:9-26`), а `WalkSpaceProbe.PushOut` (`:47-70`) ВЫТАЛКИВАЕТ круг ходока из каждого такого прямоугольника. Пол, поданный как стена, вытолкнет ходока из всего дома разом: `Floor` 16×16 покрывает дом целиком. Отличить стену от пола этой записи нечем — высоты у неё нет |
| (в) **Пол — запись описания дома со СВОИМ назначением («опора»): прямоугольник X/Z плюс верх; производитель — тот же авторский коллайдер, что и у стены, но помеченный иначе; ходьба его не читает, а слой шума и «путь снизу вверх» читают** | **ВЫЖИЛ.** Разбор ниже |
| (г) Пол — свойство КОМНАТЫ (у комнаты появляется высота пола), а не отдельная запись | **ОТКЛОНЁН ИЗМЕРЕННОЙ ГЕОМЕТРИЕЙ.** `Delivery Yard Floor` лежит ЦЕЛИКОМ ВНЕ всех комнат: он занимает `x ∈ [7.5, 12.5]`, самая восточная комната (`East Room`, центр `x = 4.875`, `_size.x = 4.25`) кончается на `x = 7.0`, а наружная стена `Outer East South` стоит в `x = 7.25` — всё перемерено по сцене и заготовке. При этом точка доставки стоит именно там (`Delivery Point`, `x = 10`), то есть ровно в том месте, где кончается «волочение по полу» кр. 3. Пол, привязанный к комнате, двор не покрывает. Согласуется с `M5`: место снаружи — обычный `HousePlace`, а не комната |

**Почему (в), и что в нём несущее.** Пол — это НЕ стена и НЕ комната: это третий вид записи, и разводит
их ровно одно свойство. Стена ЗАПРЕЩАЕТ стоять; пол РАЗРЕШАЕТ и задаёт высоту, на которой стоят. Отсюда
следует главное и дешёвое: **`WalkSpaceProbe` его не читает вовсе**, значит опровержение (б) на него не
распространяется, а `A2` не переписывается — он лишь получает третий вид записи в тот же список, за
который и стоит («геометрия, у которой есть низ и верх»). Производитель остаётся тем же авторским
коллайдером, то есть второго ручного ввода не заводится — ровно тот довод, которым сам `A2` отклонил
свой вариант (г).

**Честная цена.** Одно новое назначение у авторского компонента, один список в `HousePlan`, один
читатель в слое шума (`A10.2`) и один — у будущего «пути снизу вверх». Ноль новых физических тел, ноль
строк в снимке.

`rewrites:` если пол останется вне описания, слой правил будет судить о доме по картине мира, в которой
пола нет, — а физика будет кормить его контактами с полом каждый тик. Переделке подлежит правило
слышимости целиком, а не одно число. Метка: **`вывод по опровержению · → PLAN/ADR`**.

### A10.2 — Становится ли контакт с полом источником общего слоя шума (по `A5`)

| вариант | чем опровергнут / чем выжил |
|---|---|
| (а) Ничего не меняется: пол — просто ещё один контакт того же источника (контакт груза) | **ОТКЛОНЁН АРИФМЕТИКОЙ ВЫШЕ.** Покоящийся груз при числах `W07b` сидит в ВЕРХНЕЙ полосе постоянно (≈409 Н против порога 400), и хозяин будет прибегать на груз, который просто лежит. Кр. 4 при этом не исполняется, а ломается: «волочение громче переноски» превращается в «лежание громче всего» |
| (б) **Опора — ОТДЕЛЬНЫЙ источник слоя со своей кривой, ровно как предписывает `A5` («пороги переезжают внутрь своего источника»): удар о дом остаётся одним источником, опора становится вторым** | **ВЫЖИЛ.** Разбор ниже |
| (в) Контакт с полом ФИЛЬТРУЕТСЯ и до слоя не доходит вовсе | **ОТКЛОНЁН ПОДПИСАННОЙ КАРТОЧКОЙ.** Кр. 3 говорит «волочится **по полу**», кр. 4 — «**волочение**… громче». Отфильтровав пол, мы выбрасываем ровно тот звук, который карточка называет самым громким |
| (г) Оставить один источник, но поднять пороги выше веса покоя | **ОТКЛОНЁН ДВАЖДЫ.** (1) Это маскировка, а не решение: масса — открытое число (`W07b`, `W114`), значит пороги придётся двигать вслед за каждой её правкой, и три настроенных числа снова теряют смысл. (2) Порог выше веса покоя делает неслышимым и НАСТОЯЩИЙ удар той же силы: полосы окажутся настроены под покой, а не под события |

**Почему (б).** Удар и опора — физически разные события: удар случается ОДИН раз и имеет величину; опора
длится и величины в смысле «громко» не имеет вовсе. `A5` уже дал правило, по которому они разводятся:
источник объявляет своё расстояние САМ, а слой не знает, кто источник и не меняется от появления нового.
Значит у опоры своя кривая, и покой на ней — это не «тихо», а «нет события». Ни одна строка `A5` при
этом не переписывается: он ровно это и предусматривал, просто первый источник в нём был назван один.

**ЧЕГО ЭТА СТРОКА НЕ ДЕЛАЕТ, И ЭТО ГРАНИЦА.** Сама кривая опоры — от чего она считается (скорость
скольжения, пройденный путь, что-то ещё) — здесь НЕ выбирается и уходит `→ PLAN`. Ни одного числа ремонт
не предлагает.

**ОТВЕТ НА ПРЯМОЙ ВОПРОС НАРЯДА: НЕТ, `A4` НЕ ПОКУПАЕТ ПОЛОВИНУ КРИТЕРИЯ 4 ДАРОМ.** Наряд просил
проверить именно это, и результат честный. НАПРАВЛЕНИЕ механизм даёт верное: одна рука не поднимает →
груз на полу → контакты идут; четверо поднимают → контакты кончаются. Но даром не выходит ни при одном
из двух устройств. При одном недифференцированном источнике сломана базовая линия (покой = верхняя
полоса). При разведённых источниках (б) громкость волочения задаётся кривой ОПОРЫ, а её ещё никто не
выбирал. Поэтому `W115` §WHAT остаётся ОТКРЫТОЙ ровно в том виде, в каком её завёл ремонт `converge`:
развилка «даром» против «отдельной величины» не решена, отвечающий прежний — задача, строящая общий слой
шума. Что добавилось: **один из двух способов ответить «даром» опровергнут** — тот, где пол не отличается
от стены. Второй (кривая опоры, у которой покой даёт ноль) остаётся возможным и здесь НЕ выбран.

**Что с `W116`.** Ближняя половина, которую строка `W116` явной ссылкой отдала этому наряду, отвечена
здесь: пол входит в описание дома (`A10.1`-в) и контакт с полом становится ОТДЕЛЬНЫМ источником общего
слоя (`A10.2`-б). Дальняя половина — как пол настраивается и меряется — остаётся задаче, поэтому
**`W116` остаётся `open → PLAN`, и `open_rows: 10` в §COUNT этим ответом НЕ меняется.** §WHAT этой ногой
не тронут ни знаком.

`rewrites:` если опора не станет своим источником, три настроенных порога шума придётся настраивать
заново под покоящийся груз, а после первой же правки массы — ещё раз. Метка:
**`вывод по опровержению · → PLAN/ADR`**.

---

## §MINER — какие архитектурные вопросы НЕ были заданы

Заход сделан один раз, после того как A1–A9 были написаны, и он ловил именно незаданное.

**ЗАПИСАНО РЕМОНТОМ 2026-08-07 ПРОТИВ САМОГО ЭТОГО РАЗДЕЛА:** заход НЕ поймал пол — вопрос, который
стоит прямо под двумя подписанными строками карточки и становится несущим от выбора `A4`. Нашла его
первая проверка (`F4`), а не этот заход; теперь он стоит строкой `A10`. Семь строк ниже не тронуты; это
замечание добавлено, чтобы полнота раздела не читалась сильнее, чем она есть.

**M1. Есть ли вертикаль у ХОЗЯИНА, и что будет со взглядом, если её нет.** Кр. 2 называет игрока и
груз; хозяина не называет ни разу. Ответ взят ПО СОСТОЯНИЮ, а не изобретён: `NOW.md:146` дословно —
«ХОЗЯИН — не трогается, пока он не гоняется по тоннелям». Но отсюда следует вещь, которую надо
назвать вслух: у хозяина всё равно обязана появиться **высота глаза** — одно авторское число, — иначе
«прямой взгляд даёт последнюю позицию» (кр. 6) считается в плоскости, и налётчик на полке для него
не существует ни как видимый, ни как невидимый. Высота глаза БЕЗ вертикального движения стоит одно
поле в `HouseholderWalkSettings` (там уже есть `capsuleHeight: 2`). `rewrites:` без неё правило
взгляда пишется плоским и переписывается целиком, когда полка появится. **`→ PLAN`**.

**M2. Абсолютная скорость: хозяин быстрее налётчика, и `W07b` этого не назвала.** Перемерено на
`352f96b0`: `Settings/HouseholderWalkSettings.asset` — `movementSpeed: 1.75`,
`urgentMovementSpeed: 3.25`. Предложенная `W07b` скорость налётчика — 1.0 м/с. Значит в человеческом
доме взрослый ХОДИТ в 1.75 раза быстрее налётчика и БЕЖИТ в 3.25 раза быстрее. Это не ошибка расчёта
— это его следствие, и оно геймплейное: убежать от хозяина в открытой комнате нельзя. Ловля из узла
убрана его словом (→I21), поэтому цена сегодня не смертельная, но «он мешает им причинно» читается
совсем иначе. Строка — **улика** для `shape` и для его глаза на первой же задаче; числом её решает он
или задача по его же разрешению «возьми тогда сам или давай конкретно обсудим в задачах». Оба числа
живут в разных ассетах, что и делает эту пару первым потребителем A1.

**M3. Сколько стоит вертикаль в сети.** Не спрошено ни разу, считаем честно: третье число у каждого
ходока и у груза — 4 байта на сущность на тик; при четверых и одном грузе это 20 байт на тик, при
`_tickRate = 30` — 600 байт/с на копию. Ничтожно. Вопрос закрыт как НЕ высокорисковый и записан,
чтобы к нему не возвращались.

**M4. Кто гоняет физику при четверых.** `Network/CargoBody.cs:180-187`: шаг работает только у того,
кто захватил симуляцию (хост); клиенты рисуют присланное. Четвёртый игрок физику не удваивает.
Вопрос закрыт как не высокорисковый — НО он же единственный настоящий довод против варианта Б в A9:
всё, что становится телом, становится телом НА ХОСТЕ.

**M5. Становится ли «снаружи» новым понятием после W112.** Его решение — точка спавна вне дома плюс
прямой прямоугольный ход внутрь. `факт кода`: `HouseRoom` — прямоугольник, и всё, что вне всех комнат,
сегодня просто «не в комнате»; точка доставки уже стоит именно там и работает. Значит новое понятие
НЕ нужно: место снаружи — обычный `HousePlace`, ход — обычный `HousePassage` между ним и местом дома,
и `HouseWalk` пройдёт по нему без единой правки. Попутно `W67` (четвёртый игрок в стене:
`_spawnOriginX + _spawnSpacing·n` = −1.5, 1.5, 4.5, **7.5** при восточной стене в 7.25) перестаёт быть
арифметикой и становится авторской геометрией, как `DeliveryPoint`. **`→ PLAN`**,
`rewrites:` если спавн останется арифметикой, каждая пересборка дома будет снова ставить кого-то в
стену — видно на первом же заходе вчетвером, которого пока не было ни разу.

**M6. Что произойдёт с двадцатью двумя тестами, когда дом пересоберут.** `NOW.md:199-204`:
`tests/TunnelCrew.Core.Tests/TestHouse.cs:93-142` держит три комнаты, семь мест и два прохода
литералами, и файл сам предупреждает, что при сдвиге маркера тесты продолжат доказывать прежнее. A2
даёт этому дешёвую починку — описание дома становится ОДНОЙ вещью, которую можно выгрузить и
прочитать, — но починка не бесплатна и обязана быть НАЗВАНА в `shape`, а не обнаружена в первый день.
Строка не отложена никуда: она **улика** для `shape`, а не вопрос с отвечающим.

**M7. Чего эта нога честно не знает.** Как ощущается управление лучом от первого лица при росте
0.16 м — не архитектура, а его глаз; никакая бумага этого не покупает, и первая же задача обязана
дать ему это посмотреть.

---

## §NOT-DECIDED — сознательно НЕ решено, и где решается

| что | где решается |
|---|---|
| Из чего сделан путь снизу вверх (мебель, лестница, вентиляция, прыжок) — W15 | **ОТЛОЖЕНО ЕГО СЛОВАМИ**: «когда будет задача, тогда это и сделать». Внутри задачи, не здесь |
| Двигает ли телекинез самого налётчика — W16 | **ОТЛОЖЕНО ЕГО СЛОВАМИ**, и помечено как вопрос ГЕЙМПЛЕЯ, который задача обязана принести ЕМУ |
| Наклоняется ли груз при переноске (снятие `FreezeRotationX/Z`) | Одна строка в обе стороны; его глаз на первой задаче с вертикалью. Бумагой не решается (A4-б) |
| Форма инструмента разработчика | `→ PLAN`, W77: неверная форма выбрасывается целиком, никакая другая работа на неё не опирается |
| Числа: масса, скорости, дальность луча, расстояния громкости, высоты обстановки, ближняя плоскость камеры | `→ PLAN` по его прямому разрешению «возьми тогда сам или давай конкретно обсудим в задачах»; каждое — одно поле в ассете |
| Сколько реакций строится в первой волне | Дефолт взят `converge` (W54) по его разрешению и записан как НЕ подпись; задача вправе взять другое |
| Порядок и состав первой волны | `shape`. В токен «принимаю» первый срез НЕ входит (→I54) |
| ~~Двигается ли обстановка~~ | **ЗАКРЫТО ЕГО СЛОВОМ 2026-08-07** — «А, обстановка стоит», см. A9. Строка оставлена зачёркнутой, а не удалена, чтобы было видно, что вопрос задавался и получил ответ, а не растворился |
| Вернётся ли подвижная обстановка когда-нибудь | Его отдельным словом. Вариант Б был предложен «отдельным решением, когда захочешь», и выбор А эту дверь не закрывает; общий слой шума (A5) делает возвращение дешёвым |
| **ДОБАВЛЕНО РЕМОНТОМ 2026-08-07 — пять строк, понижённых по находкам первой проверки. Ни по одной из них вопрос владельцу здесь НЕ формулируется и НЕ задаётся.** | |
| **Кто именно задевает обстановку** — груз ВМЕСТЕ с несущими или ТЕЛО человека (`A9`, `F5`) | **`open → OWNER`.** Вопрос геймплея, по G7 его. Спрашивает та нога, которая первой доведёт обстановку до задачи, и она обязана принести ОБЕ читки с ценой. Стоит против `CHARTER.md:92`, а не только против кр. 5 |
| **Существуют ли двери как ОТКРЫВАЮЩИЕСЯ вещи вообще** (`C26`, `A9`, `F1`) | **`open → OWNER`,** внутри задачи, дошедшей до `W63`. Критерий 6 покупает ослабление у двери, но не переменную: дверь, закрытая всегда, исполняет его букву целиком. Его собственный вход двойной — «должны как-то же закрываться. Возможно, и не будут» |
| **Чем берут предмет — прицелом или близостью** (`A8`, `F9`) | **`open → OWNER`.** Это управление, то есть его глаз внутри первой задачи телекинеза; сам набор это уже признавал в `M7`. Сегодня стоит близость на расстоянии: `_cargoPickupRange: 1.3` = восемь ростов |
| **При скольких руках груз перестаёт волочиться** (`A4`, `F2`) | **`open → PLAN` (=`W114`)**, с возвратом к `OWNER`, если чисел под его текстуру не найдётся. Ни другие числа, ни другой потолок, ни отказ от потолка на руку здесь не предпочтены |
| **Кривая опоры: от чего считается громкость волочения** (`A10.2`) | **`→ PLAN`.** Устройство решено (опора — свой источник), содержание кривой — нет; связанная развилка `W115` остаётся открытой за задачей общего слоя шума |

---

## §SIGNOFF

**§SIGNOFF: Declare (контракты) @ 2026-08-07 — вердиктов владельца НЕ запрашивалось и НЕ получено, и
вот почему поимённо.** §CONTRACTS (в `work/converge-g-5a7c.md`) содержит двенадцать краёв к узлам
дерева и **шестнадцать** внутренних швов (C26 — состояние двери — добавлен 2026-08-07 по его же
уточнению и ПОНИЖЕН ремонтом того же дня; C27 и C28 — пол как опора и опора как источник шума —
добавлены ремонтом 2026-08-07 по `A10`; прежнее число было четырнадцать). Из краёв **ни один не
является развилкой**: одиннадцать либо
односторонние (узел производит, потребитель припаркован или не существует), либо ограничительные
(«ничто в этом узле не имеет права зашить двойку»), либо пустые по существу. Двенадцатый, C05
(`g-c519`), несёт ДЕФЕКТ СОСТОЯНИЯ, а не вопрос вкуса: `why` того узла до сих пор называет способом
дойти ЗАКРЫТЫЙ `g-1d84`, а его `not_inherited` обещал перепроверку «после того как в интегрированный
кусок дома поиграли» — условие сработало 2026-08-07. Чинит `map`, и строка стоит `open → map`
(=W109). Из внутренних швов один — C25 — является настоящей развилкой и вынесен ему как A9.
**ДОБАВЛЕНО РЕМОНТОМ 2026-08-07:** ждущих строк в §CONTRACTS стало ДВЕ, а не одна — к `C05` (`→ map`)
прибавился `C26`, понижённый в `open → OWNER` по находке `F1`. Развилкой в смысле «вынести владельцу
СЕЙЧАС» он не становится: наряд ремонта прямо запретил выносить новое решение, и вопрос не
формулируется — называется только отвечающий.

**§SIGNOFF: Architect (архитектура) @ 2026-08-07 — вердикт владельца получен РОВНО ОДИН, и он записан
без смягчения и без повышения.** Девять высокорисковых мест разобраны опровергнутыми сравнениями;
восемь получили вывод, где отпадение вариантов держится на измеренном факте кода, на подписанном
тексте карточки или на его собственных ранее сказанных словах — но **ни один из восьми не является
его выбором, и никто не вправе цитировать их как подписанные.** Обязывающий ADR по каждому принимает
PLAN; если бумажный лучший вариант не выдержит в коде, PLAN эскалирует обратно. Девятое (A9) — вопрос
геймплея — вынесено ему одной строкой с двумя ответами, измеренной ценой каждого, рекомендацией и
названным доводом ПРОТИВ рекомендации, и **закрыто его словом в тот же день: «А, обстановка стоит».**
Это вердикт по ВАРИАНТУ, усиленный его собственной формулировкой выбора; подписью под текстом
карточки он не является, и карточка этим ответом не тронута.

**§SIGNOFF: Architect — РЕМОНТ @ 2026-08-07 (`s-converge-arch-g-5a7c-repair-001`). Новых вердиктов
владельца НЕ запрашивалось и НЕ получено; их по-прежнему ровно ОДИН, подписей под текстом карточки —
ноль.** Ремонт не переоткрыл разбор: шесть выводов, подтверждённых первой проверкой (`A1`, `A2`, `A3`,
`A5`, `A6`, `A7`), по существу не тронуты. Тронуто ровно то, что вернула проверка, и лечение ВЕЗДЕ одно
и то же — понижение, а не свежий ответ. **Заведена одна новая строка `A10` (пол)** — не потому, что
ремонту захотелось разбирать дальше, а потому, что этого дословно потребовал `done_when` 3 наряда, и
вопрос стоит прямо под двумя подписанными строками карточки.

**Счёт после ремонта, чтобы его не пришлось выводить** (прежние числа названы рядом; всё меряется по
этому файлу и по §CONTRACTS одной командой):

- `arch_rows: 9 → 10` — добавлена `A10` (пол), по `F4`;
- `arch_open: 8 → 9` — девять строк (`A1`–`A8`, `A10`) с рекомендованным ответом и обязывающим ADR у
  PLAN; `A9` по-прежнему закрыт вердиктом владельца по варианту и в это число не входит;
- **`arch_lines_owed_to_owner: 0 → 3`** — новое поле, и оно и есть результат ремонта. Три КУСКА строк
  понижены в `open → OWNER`, каждый с названным отвечающим и БЕЗ сформулированного вопроса:
  `A9`/оговорка (кто задевает обстановку — `F5`), `A8`/треть (чем берут предмет — `F9`), `C26`
  (существуют ли открывающиеся двери — `F1`). Четвёртый кусок, `A4`/порог (`F2`), понижен в
  `open → PLAN` (=`W114`) с возвратом к владельцу, если чисел не найдётся, и в это число не входит;
- `contracts_open: 1 → 2` — `C05` (`→ map`, =`W109`) плюс понижённый `C26` (`→ OWNER`);
- внутренних швов §CONTRACTS: **14 → 16** (добавлены `C27` — опора в описании дома, `C28` — опора как
  источник слоя шума; оба по `A10`). Краёв к узлам дерева по-прежнему **12**, полнота по дереву не
  изменилась: `A10` не заводит ни одного нового соседа;
- вердиктов владельца получено: **1** (A9), без изменений. Подписей под текстом карточки: **0**;
- отложено его собственными словами и не тронуто: **2** (`W15`, `W16`) — ремонтом не тронуты ни знаком;
- строк §MINER: **7**, без изменений; добавлено одно замечание ПРОТИВ раздела — заход не поймал пол;
- карточка `g-5a7c` не тронута ни знаком; §IMPORT, §GLOSSARY, §WHAT, §DRIFT, §COUNT и оба §SIGNOFF
  файла `work/converge-g-5a7c.md` ремонтом не тронуты ни строкой — он правил только §CONTRACTS;
- `arch_in_context_only: PASS` — ни одна строка этого файла, включая всю `A10`, не является
  требованием узла и не едет в `done_when`.

**Сведение с §COUNT §WHAT, чтобы расхождение двух счётов не повторилось** (`F6` было ровно про это).
Счётов ТРИ, они меряют РАЗНОЕ, и ни один не обязан совпадать с другим числом — обязаны совпадать
УТВЕРЖДЕНИЯ:

| счёт | где живёт | что меряет | сейчас |
|---|---|---|---|
| §WHAT | §COUNT в `work/converge-g-5a7c.md` | строки решений `W…` | `what_rows: 122`, `open_rows: 10`, `deferred: 2` |
| §CONTRACTS | «Счёт этой ноги» там же | края и швы `C…` | 12 краёв + 16 швов, `contracts_open: 2` |
| арх-файл | здесь | строки `A…` и `M…` | `arch_rows: 10`, `arch_open: 9`, `arch_lines_owed_to_owner: 3` |

Точки, где они СМЫКАЮТСЯ, и все три проверены ремонтом:

1. `W103` — §WHAT говорит `answered · улика` со ссылкой на `C07`, `C07` говорит «закрыт названным
   маршрутом». Расхождение, найденное `F6`, снято ремонтом `converge`; ремонт арх-ноги дочистил в `C07`
   устаревшую фразу «единственная открытая строка §WHAT».
2. `W114` ↔ `A4`: обе говорят «порог не решён, отвечает задача, возврат к владельцу, если чисел нет».
   `open_rows` от этого не меняется — строка уже открыта.
3. `W116` ↔ `A10`: ближняя половина отвечена в `A10`, дальняя остаётся за задачей, поэтому `W116`
   остаётся `open → PLAN` и `open_rows: 10` НЕ меняется. Та же логика у `W115`: `A10` опроверг один из
   двух способов ответить «даром», но развилку не закрыл.
4. `W63` ↔ `C26`: §WHAT не правлен, `W63` остаётся `→ PLAN` (КТО закрывает двери); `C26` отдаёт вопрос,
   стоящий ПЕРЕД ней (существуют ли открывающиеся двери вообще), и стоит `open → OWNER`.

**Канон, ПРЕДЛОЖЕННЫЙ, а не записанный** (`converge-arch` в `knowledge/` не пишет; сливает
`review`/`pulse`): «описание дома — одна вещь слоя правил, у которой есть и смысл, и геометрия»;
«громкость — это расстояние»; «у захода один владелец, и всё накопленное лежит под ним»; «перевод
масштаба случается один раз на границе, и силы через него не проходят». **Добавлено ремонтом
2026-08-07:** «стена запрещает стоять, опора разрешает и задаёт высоту — это разные записи одного
описания»; «покой не бывает громким: длящийся контакт не событие».

---

## §REPAIR-LEDGER — что именно тронул ремонт 2026-08-07, построчно

Раздел писан для ВТОРОЙ проверки, чтобы она сверяла список командой, а не чтением. Ремонт исполнял
наряд `work/c-converge-arch-g-5a7c-repair-001-call.md`; каждая строка ниже названа его `done_when` и
находкой первой проверки.

| # `done_when` | находка | где тронуто | что сделано | чего НЕ сделано |
|---|---|---|---|---|
| 1 | `F1` | `A9` («ЕГО УТОЧНЕНИЕ»), `C26` в §CONTRACTS | Утверждение «состояние двери куплено критерием 6» СНЯТО; несущее предложение понижено в `open → OWNER` (владелец внутри задачи, дошедшей до `W63`) | Не тронуты: измеренный факт, что дверей как вещей нет, и цена физической двери. Вопрос владельцу не сформулирован. `W63` в §WHAT не тронута |
| 2 | `F2` | `A4` | Утверждение «критерий 3 куплен даром» СНЯТО; порог по числу рук назван вопросом и понижен в `open → PLAN` (=`W114`) с возвратом к `OWNER` | Выбор варианта (а) не оспорен и не тронут. Ни одного нового числа не предложено — ни массы, ни потолка |
| 3 | `F4` | новая `A10`; по одному добавочному абзацу в `A2` и `A5`; абзац-указатель в `A4`; `C27`, `C28` в §CONTRACTS | Пол разобран двумя опровергнутыми таблицами: входит в описание дома (`A10.1`-в), становится ОТДЕЛЬНЫМ источником слоя (`A10.2`-б). Названо прямо: `A4` половину критерия 4 даром НЕ покупает | Выводы `A2` и `A5` не тронуты — добавлены только указатели. Кривая опоры не выбрана. `W115` и `W116` остаются открытыми, `open_rows: 10` не меняется |
| 4 | `F5` | `A9` (оба места: абзац до его ответа и «одна оговорка» после), `C25` в §CONTRACTS | Прочтение «несущие задевают» понижено из рабочего в `open → OWNER`; названо, что сужение стоит и против `CHARTER.md:92` | Вопрос владельцу не сформулирован и не задан. Вердикт «А, обстановка стоит» не тронут и в силе не изменён |
| 5 | `F7` | `A9`, таблица классов, строка 1 | Слово «сильнее» снято: уточнение подтверждает вариант А поимённо, но захеджированное высказывание нехеджированный вердикт не усиливает | `LOG` за `s-converge-arch-g-5a7c-003` НЕ правлен задним числом — историческая запись остаётся как есть |
| 6 | `F9` | `A8`, строка (б) таблицы и разбор под ней | «ГДЕ берётся хват (прицел вместо близости)» вынуто из выжившего варианта и понижено в `open → OWNER` (его глаз в первой задаче телекинеза, как уже говорил `M7`) | Две другие трети не тронуты: они держатся на перемеренных швах `CargoHaul.AtPoint`, `CargoHold`, `Reduce` |
| 7 | `F6` | `reads:` в шапке; §SIGNOFF этого файла; вводный абзац и «Счёт этой ноги» §CONTRACTS; `C07` | Счета сведены: `open_rows` в шапке 1 → 10, три счёта разведены таблицей с точками смыкания, из `C07` убрана устаревшая фраза «единственная открытая строка §WHAT» | §WHAT и §COUNT не тронуты ни знаком — их правит `converge`, и ремонт `converge` их уже поправил |

**Что ремонт НЕ трогал вообще, и это проверяется диффом:** `TREE.md`, `CHARTER.md`, `knowledge/**`,
§IMPORT, §GLOSSARY, §WHAT, §DRIFT, §COUNT и оба §SIGNOFF файла `work/converge-g-5a7c.md`, строки `W15`
и `W16`, продуктовый репозиторий `C:/projects/Unity/GasCoopGame` (только чтение, `main` =
`352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`, дерево чисто до и после). Ставка не открыта, задач и полос
нет, владельцу не вынесено ни одного нового решения.

**Веер.** Опровержение сделано одним контуром: в этом харнессе действует прямая постоянная инструкция
владельца не разворачивать субагентов и workflow, и по авторитету `os/KERNEL.md` живая инструкция
владельца выше плея. Та же граница названа предыдущей арх-ногой и первой проверкой; названа здесь,
чтобы никто не прочёл ремонт как веерный.

END_OF_FILE: live/indie-game-development/work/converge-g-5a7c-arch.md
