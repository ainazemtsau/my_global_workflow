# CALL — c-exec-g-5a7c-loot-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-loot-1
issued: 2026-08-12 by s-work-g-5a7c-loot-1-dispatch-001
status: ready
basis: `0618184f5e81f6f573bf88bea31782a5c85987a4` — `origin/main` = `origin/dev`, перемерено
       `git ls-remote origin` перед выпуском 2026-08-12.

**ГОЛОВА СЕГОДНЯ ДВИГАЛАСЬ ЧЕТЫРЕЖДЫ.** Правило прежнее — не СТОП, а перебазирование, — и с
добавкой: **сверь `origin/main` ЕЩЁ РАЗ ПЕРЕД ТЕМ, КАК ОТДАВАТЬ РЕЗУЛЬТАТ.** Две дорожки сегодня
на этом обожглись, обе проверили голову один раз, на старте. СТОП домой — на всё ОСТАЛЬНОЕ
неожиданное.

## slot

**WIN-U1**, каталог `C:\projects\Unity\GasCoopGame_win-u1`, ветка `slot/win-u1`. Перемерено:
`head 8c3bb8af`, `CLEAN`, `AVAILABLE`, `lease none` — отстаёт на несколько документных коммитов,
догон короткий.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U1 -Claim -LeaseId c-exec-g-5a7c-loot-1-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/main`, readback дерева, `git status` пуст.

**UNITY НУЖЕН:** расстановка лута в комнатах и проверка твёрдости — это сцена и физика.

**ПАРАЛЛЕЛЬНО ИДЁТ `c-exec-g-5a7c-occupations-1-001` в WIN-U3.** Его файлы тебе запрещены:
`Core/Householder/**`, `Assets/TunnelCrew/Profiles/**`, `Assets/StreamingAssets/**`.

## goal

**В ДОМЕ ЛЕЖИТ НАБОР ЛУТА НА ОДНОГО, НА ДВОИХ И НА ЧЕТВЕРЫХ, И ЛУТ ТВЁРДЫЙ ДЛЯ ЛУТА.**

Слова владельца, которыми задача куплена: «несколько видов лута, ну, их просто выносить надо. То
есть один за одним условно, один лут, ну там, допустим, штук пять лута, которые одному можно
вынести, там три лута, которые нужно два человека, и два лута, например, где нужно четыре. Ну и это
должно настраиваться». И про библиотеку: «поставил, оно работает».

**И ЕГО ОТДЕЛЬНОЕ РЕШЕНИЕ 2026-08-12, ДОСЛОВНО: «лут не должен через друг друга проходить».**

**НАЗВАННЫЙ ПОДХОД — ОДИН:** набор и его состав живут ДАННЫМИ, существующие числа переиспользуются,
а твёрдость возвращается ПОСЛЕ измерения причины, по которой её выключили. Отход — ESCALATE домой.

## context — перемерено чтением байтов на опубликованной голове 2026-08-12

### Дом приехал, и лут теперь есть куда класть

`t-rooms-1` опубликована и принята владельцем: четыре комнаты по назначению — `Bedroom`, `Kitchen`,
`Corridor`, `Living Room`; контур `11.0 × 8.5` м; стены `2.6` м; три внутренних проёма шириной
`1.5`. Девять названных мест с ролями: `sleep`, `watch-window`, `bedroom-door`, `hall-centre`,
`cook`, `food-storage`, `kitchen-door`, `living-door`, `quiet-seat`. Четырнадцать предметов
обстановки — физические тела с `HouseWall`.

**Сцена и префаб дома теперь твои:** дорожка `house-and-things`, и параллельных претендентов на них
нет.

### Половина набора УЖЕ СТОИТ — строить надо меньше, чем кажется

- **Три класса груза заведены** в `Settings/GameRulesSettings.asset`: `0.192 × 0.04 × 0.04` масса
  `48`; `0.288 × 0.06 × 0.06` масса `100`; `0.384 × 0.08 × 0.08` масса `240`.
- **Порог по рукам настоящий и уже работает:** `144 / 300 / 720` Н = одна / две / четыре руки.
  Переизобретать его запрещено — задача даёт ему предметы, на которых он виден.
- В сцене **четыре точки появления** (`Cargo Spawn 0..3` под `StartConditions`), у каждой свой
  класс.
- Налётчик ростом `0.16` м; потолок носильщиков на один груз — четыре.

Не хватает: библиотеки (лут живёт настройками плюс сценой, а не файлом, который «поставил — и
работает») и количества.

### ТВЁРДОСТЬ: причина выключения найдена, но НЕ ОБЪЯСНЕНА

`Assets/TunnelCrew/Network/CargoBody.cs:163,193,195` — `SetBodyActivity` для НЕАКТИВНОГО груза
ставит `rigidbody.detectCollisions = false` И `body.Collider.enabled = false`. Осевший невзятый груз
физически отсутствует, поэтому сталкиваться могут только два одновременно активных груза.

**ПОЧЕМУ так сделано — в коде не написано.** Вероятные причины — устойчивость решателя или цена
симуляции, но это догадки. **Строка 2 требует сначала ИЗМЕРИТЬ, потом включать.** Если измерение
покажет, что включение обратно ломает решатель или роняет частоту — это СТОП домой с числами, а не
самостоятельный компромисс.

Улика: `i-cargo-passes-through-cargo-in-the-running-game-001`.

### База прогонов

`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **239 passed,
0 failed, 0 skipped**, прогнано Направлением на опубликованной голове.

`tools/hygiene.ps1` зелёный, `tools/check.ps1` зелёный. **`-Deliver` красный на исторических
отчётах — зарегистрированный дефект `i-deliver-gate-red-when-main-catches-dev-001`, НЕ твоя
работа.**

Образец Unity-доказательства уже есть и его стоит повторить: `t-rooms-1` сделала EditMode-тест,
который создаёт локальную `PhysicsScene`, инстанцирует настоящий префаб и бьёт производственным
`CargoBodyContact`. Это тот вид доказательства, который здесь нужен.

### Что рядом сломано и чинить запрещено

- `i-delivery-observes-only-the-first-cargo-001` — доставка видит только первый груз
  (`CarryWorld.cs:162`). **НЕ твоё, не чини**, даже если предметов станет десять.
- `i-householder-walks-through-walls-001` — хозяин задевает косяк краем капсулы. Не твоё.

## boundaries

- **Задача не расширяется.** Три строки `done_when` — весь объём.
- **НЕ ОТКРЫВАЕШЬ файлы параллельного наряда:** `Core/Householder/**`,
  `Assets/TunnelCrew/Profiles/**`, `Assets/StreamingAssets/**`.
- **НЕ ТРОГАЕШЬ** `Assets/TunnelCrew/Presentation/**`, `Art/Bodies/**`, `World/Householder.prefab`,
  `Settings/PresentationSettings.*`, `HouseholdLab.unity`.
- **ПОРОГИ ШУМА `60 / 150 / 400` НЕ ДВИГАЮТСЯ.** Порог по рукам `144 / 300 / 720` не
  переизобретается.
- **ДОСТАВКУ НЕ ЧИНИШЬ.**
- **ЧЕТЫРЕ РУКИ — ПОТОЛОК.** Ни один груз не требует больше (критерий 7 узла и его документ демо).
- **НЕ ДЕЛАЕТСЯ:** рыночная и личная ценность вещи, продажа, жертвы, повреждение и уничтожение,
  крупный груз вроде телевизора (вырез узла), новые геометрии сверх нужного, состояния предметов
  сверх уже опубликованных, тоннели.
- **ОБЛИК НЕ ДОВОДИТСЯ.** Лут должен читаться как предметы, а не быть красивым.
- **Настроечные значения — не код** (контракт 36): массы, размеры и состав набора живут в данных и
  тестами не покрываются. Тест проверяет ФОРМУ, никогда величину.
- **Самописный сканер как улика поведения запрещён.**
- **Инструмент недоступен → СТОП домой. Решение владельца → ESCALATE домой.**
- **Расписка обязательна.**

## done_when

Три строки — потолок контракта 36. Ни одна не снимаема.

1. **НАБОР ЕСТЬ, ЛЕЖИТ В КОМНАТАХ И НАСТРАИВАЕТСЯ.** В доме стоят предметы трёх весов — те, что
   выносит один, те, что требуют двоих, и те, что требуют четверых, — разложенные по названным
   комнатам; **лут описывается файлом-библиотекой и ставится в сцену без правки кода**; какой лут и
   сколько его — правится данными. Ориентир владельца «пять / три / два» — ориентир, а не закон:
   точные числа назови в возврате.
2. **ЛУТ ТВЁРДЫЙ ДЛЯ ЛУТА — ЕГО РЕШЕНИЕ, И ПРИЧИНА ВЫКЛЮЧЕНИЯ ИЗМЕРЕНА, А НЕ УГАДАНА.** Покоящийся
   предмет перестаёт быть проходимым: два предмета не проникают друг в друга ни в покое, ни при
   переноске. **Перед включением измерь и назови числом, во что обходилось выключение** — что
   именно ломается или дорожает, если коллизии не выключать. Доказательство — настоящий Unity-прогон
   на живой физической сцене тем же приёмом, что использовала `t-rooms-1`. Если измерение
   показывает, что включение ломает решатель — **СТОП домой с числами.**
3. **НИЧЕГО НЕ СЛОМАНО, ОТКАТ НАСТОЯЩИЙ.** Порог по рукам виден на новых предметах и не изменён;
   пороги шума не двигались; доставка не тронута; прогон тестов назван числом против базовых
   **239**; откат одним коммитом исполнен НАСТОЯЩИМ `revert`, дерево сходится с деревом базы.

## return

**Первая строка — РАСПИСКА:** три строки `done_when`, базис.

Дальше:

- что сделано, по каждой из трёх строк отдельно;
- **состав набора числами**: сколько предметов какого веса и в каких комнатах;
- **как выглядит библиотека**: чем описывается предмет и какой строкой добавляется следующий;
- **измерение по твёрдости**: что именно давало выключение коллизий и что стало после включения —
  числами, а не ощущением;
- полный список путей диффа и подтверждение, что среди них нет файлов параллельного наряда;
- прогон тестов до и после; Unity-прогон отдельно;
- откат: команда, хеши, совпадение деревьев;
- **подтверждение, что голова `origin/main` сверена ПЕРЕД тем, как отдавать результат;**
- **что владельцу запустить и на что посмотреть** — строку 1 и «твёрдость руками» судит он;
- что НЕ сделано и почему; всё неожиданное.

## budget

Половина сфокусированного дня. Порог смерти 3 этой ставки считает ноги.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-loot-1-001-call.md
