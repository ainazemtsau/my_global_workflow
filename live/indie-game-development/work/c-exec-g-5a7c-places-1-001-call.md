# CALL — c-exec-g-5a7c-places-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: — (общая задача-шов, дорожки не имеет)
task: t-places-1
issued: 2026-08-12 by s-work-g-5a7c-places-1-dispatch-001
status: ready
basis: `f3c40c4010ee2f0f700cdf1bfbcea14a301d488c` — `origin/main` = `origin/dev` = `origin/slot/win-u2`,
       перемерено `git ls-remote origin` перед выпуском 2026-08-12.

**ЕСЛИ `origin/main` СДВИНУЛСЯ — НЕ СТОП, А ПЕРЕБАЗИРОВАНИЕ.** Взять свежую голову, собрать на ней,
назвать её отдельной строкой возврата. СТОП домой — на всё ОСТАЛЬНОЕ неожиданное.

Пин: `engineering_contract: 36` против продуктового `synced_contract_version`. Re-sync не нужен.

## slot

**WIN-U2**, каталог `C:\projects\Unity\GasCoopGame_win-u2`, ветка `slot/win-u2`.

Перемерено селектором перед выпуском: **все четыре слота `CLEAN / AVAILABLE / lease none`.**
WIN-U2 выбран потому, что его дерево **уже стоит на `f3c40c40`** — догонять нечего. WIN-U1 стоит на
`a5a84350` и оставлен свободным под параллельную задачу `t-body-1`.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-places-1-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/main`, readback дерева, `git status` пуст.

**Unity для этой работы не требуется:** вся она в engine-free `Core`, в
`Assets/TunnelCrew/Profiles/**` и в headless-тестах. Мост при этом ЖИВ И ПРОВЕРЕН этой ногой:
редактор WIN-U2 отвечает, Unity `6000.5.5f1`, не в Play, не компилирует. Поле `mcp-endpoint:
unrecorded` в файле состояния — устаревшая запись, а не отсутствие моста.

**ЗАКРЫТ СТАРЫЙ ОТКРЫТЫЙ ВОПРОС:** 2026-08-11 нога честно записала, что доказать, КАКОЙ каталог
открыт в редакторе, нельзя, потому что оба слотовых дерева стояли на одном коммите. Сегодня они
разошлись, и это проверено: мост WIN-U2 находит `Assets/TunnelCrew/Scenes/HouseholdLab.unity`
(guid `6d52ee3ef501a5a4c82c11185afedd68`), мост WIN-U1 не находит ничего — а этой сцены на
`a5a84350` и не существует. Мосты действительно смотрят в разные деревья, и WIN-U2 — тот, что нужен.

## goal

**Хозяин начинает говорить о доме РОЛЯМИ, дом их объявляет, а роль, которой в этом доме нет,
МОЛЧИТ вместо того, чтобы убивать хозяина.**

Сегодня всё наоборот: профиль, назвавший ЛЮБОЕ место, не запускает хозяина вовсе — он молча не
появляется в игре.

Это общая задача-шов третьей волны. Пока она идёт, две из трёх дорожек ставки заняты не будут.

**НАЗВАННЫЙ ПОДХОД — ОДИН, и он не подлежит замене на «меньшую форму»:** словарь приходит ИЗ
описания дома через существующий шов `IHousePlanSource`. Загрузчик профиля не заводит собственного
словаря мест, не читает сцену и ничего не угадывает. Любой отход от этого — ESCALATE домой, а не
решение на месте.

**СЛОВО ВЛАДЕЛЬЦА, которым куплен именно этот разрез, 2026-08-12:** «давай, наверное, роли
попробуем». Он же поставил задачу, которую разрез решает: «просто тупо будет, если мы возьмём
хозяина в какой-то другой дом, и он начнёт там боить, потому что у него хендлеров же там нет».

## context — перемерено чтением байтов на `f3c40c40` 2026-08-12

### Где именно сегодня умирает хозяин

`Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs`:

- `:30` — в интерфейсе `IHouseholderProfileReferenceResolver` объявлен `bool IsKnownPlace(string name)`;
- `:65-69` — реализация возвращает `false` с комментарием дословно: `// Deliberately empty. t-host-2
  connects the house dictionary through this method.`;
- `:736-744` — читает необязательное `action.place` и, если `placeName != null &&
  !references.IsKnownPlace(placeName)`, бросает ошибку с путём в JSON.

`docs/householder-profile-format.md` описывает следствие дословно: отказ по месту «is logged once and
the host port remains unreadable. The authoritative Core therefore never seats or publishes that
householder». То есть это не деградация, а **полное отсутствие хозяина в игре**.

### Что про дом УЖЕ построено и переиспользуется, а не изобретается

- `Assets/TunnelCrew/Core/House/HousePlan.cs` — комнаты, места, проходы; строится один раз до
  движения и только читается; копирует переданные массивы; отдаёт `IReadOnlyList`; умеет
  `TryFindRoom`, `TryFindRoomAt(x,z)`, `TryFindPlace`, `TryFindPassage`, `TryFindPassageBetween`;
  есть `HousePlan.Empty`.
- `Assets/TunnelCrew/Core/House/IHousePlanSource.cs` — шов производителя; отсутствие источника даёт
  `HousePlan.Empty`, «so a rule reading the house never has to check first».
- `Assets/TunnelCrew/World/SceneHousePlan.cs` — первый производитель: читает authored-маркеры
  `HouseRoomMarker` / `HousePlaceMarker` / `HousePassageMarker`, НЕ коллайдеры; не кэширует;
  `Complaints()` жалуется на недоделанную разметку для глаза владельца, и от неё ничего не зависит.
- `Assets/TunnelCrew/Network/NetworkHouseholder.cs:42,80,102` — источник ищется
  `GetComponentInChildren<IHousePlanSource>()` и спрашивается ОДИН РАЗ, до того как что-либо
  двинулось. Проводка от дома до хозяина, таким образом, уже существует.
- `Assets/TunnelCrew/World/ThreeRoomHouse.prefab` — сегодняшний дом: три комнаты (`West Room`,
  `Middle Room`, `East Room`), семь мест (`Bedside`, `West Window`, `West Door Side`, `Hall Centre`,
  `Work Table`, `Store Corner`, `East Door Side`), два проёма (`West Doorway`, `East Doorway`,
  `_width: 2`).
- `HousePlace` несёт `Name`, `RoomName`, `X`, `Z` (видно по использованию в `Complaints()`).

### Что умеет профиль сегодня

`Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`: `formatVersion 1`,
`routine` из двух шагов (`walk-route` 12 с и `wait-at-route-point` 3 с, оба `when: always`),
`activeResponseRules` с одной строкой `repeated-heavy-noise / 100 / higher-or-equal-latest`,
`behaviorBranches: []`, `decisionHandlers: []`.

Зарегистрированных умений ровно два — `walk-route` и `wait-at-route-point`, — и **оба используют
маршрут из сцены и потому в рабочем профиле `place` не несут**. Приём «неизвестное отвергается
названно и с путём в JSON» в разборщике уже существует и распространяется, а не изобретается.

### Что компилируется headless, а что нет — это решает, где может жить доказательство

`tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj` компилирует: весь
`Assets/TunnelCrew/Core/**` (через зеркальный `core/TunnelCrew.Core.csproj`, `EnableDefaultCompileItems=false`,
`Compile Include="..\Assets\TunnelCrew\Core\**\*.cs"`), **плюс поимённо
`Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs`** и четыре файла `Editor/HouseholderObservation*`.

Он НЕ компилирует `Assets/TunnelCrew/World/**` и `Assets/TunnelCrew/Network/**` — это MonoBehaviour'ы.

**Отсюда прямое следствие для доказательства, и оно входит в `done_when` 2:** главный гейт обязан
быть доказуемым В HEADLESS-ТЕСТАХ. Значит словарь ролей обязан пересекать границу как engine-free
данные, а не доставаться через MonoBehaviour. Как именно — твоё решение; но «проверено только в
редакторе» гейт не закрывает.

Фикстурами в тестовый проект копируются `default.householder.json`, `IntegratedHouse.unity`,
`HouseholdLab.unity` и `ProjectSettings/EditorBuildSettings.asset` — то есть scene-fixture тесты
читают сцены текстом. Правка сцен ломает их и здесь запрещена (см. границы).

### База прогонов

`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **214 passed,
0 failed, 0 skipped**, прогнано ЭТОЙ НОГОЙ на `f3c40c40` 2026-08-12.

`tools/hygiene.ps1` — зелёный. **`tools/check.ps1 -Deliver` КРАСНЫЙ на семи исторических отчётах —
это зарегистрированный процессный дефект `i-deliver-gate-red-when-main-catches-dev-001`, НЕ твоя
работа и НЕ повод для СТОПа.** Не чини его и не считай своим провалом.

## boundaries

- **Задача не расширяется.** Три строки `done_when` — весь объём. Сэкономленное не тратится на
  новое.
- **СЦЕНЫ И ПРЕФАБЫ НЕ ОТКРЫВАЮТСЯ ВООБЩЕ:** ни `Assets/TunnelCrew/Scenes/IntegratedHouse.unity`, ни
  `HouseholdLab.unity`, ни `Assets/TunnelCrew/World/ThreeRoomHouse.prefab`. Они принадлежат дорожке
  `house-and-things` и задаче `t-rooms-1`, а в `g-1d84` параллельные полосы разошлись на файле сцены
  ДВАЖДЫ, и слитые байты не запускал никто. **Добавить сериализуемое ПОЛЕ роли в
  `HousePlaceMarker.cs` можно — это код. Проставлять значения ролей в префабе нельзя.**
- **Занятия внутри комнаты здесь НЕ делаются** — это `t-occupations-1`. Эта задача даёт словарь, а
  не действия. Новых умений не регистрировать.
- **Не делаются:** зрение, новые виды события, состояние предметов, комната внутри факта, иерархия
  ролей, наследование профилей, несколько хозяев.
- **Пороги `60 / 150 / 400` не двигаются.** `Core/Cargo/**` не трогается. `Core/Householder/**`
  трогается только настолько, насколько требует проводка словаря, и каждое такое место называется в
  возврате.
- **Поведение по умолчанию не меняется ни на грамм:** опубликованный профиль ролей не называет, пять
  реакций целы по содержанию, распорядок тот же.
- **Настроечные значения — не код** (контракт 36): они живут в данных и тестами не покрываются.
  Тест проверяет ФОРМУ, никогда величину.
- **Тест только там, где владелец не увидит ответ глазом.** Здесь весь шов глазом невидим, поэтому
  тесты — основное доказательство, и это не исключение из правила, а его применение.
- **Самописный сканер как улика поведения запрещён.**
- **Инструмент недоступен → СТОП домой.** Никогда обходной путь. Исключение только письменным «да»
  владельца.
- **Решение владельца → ESCALATE домой,** а не догадка на месте. Это относится и к любому отходу от
  названного подхода.
- **Расписка обязательна:** наряд не существует, пока исполнитель не расписался.

## done_when

Три строки — потолок контракта 36 для инженерного наряда. Ни одна не снимаема.

1. **СЛОВАРЬ РАБОТАЕТ И НЕИЗВЕСТНОЕ МОЛЧИТ.** Шаг профиля может назвать РОЛЬ места, и хозяин с
   таким профилем ЗАПУСКАЕТСЯ; словарь ролей приходит из описания дома (`HousePlan` через
   существующий `IHousePlanSource`), а не заводится загрузчиком; а роль, которой в этом доме НЕТ,
   молчит — шаг с ней не срабатывает, хозяин загружается, живёт дальше и не исчезает из игры.
2. **ГЛАВНЫЙ ГЕЙТ — ОДИН ПРОФИЛЬ В ДВУХ ДОМАХ, ДОКАЗАННЫЙ HEADLESS.** Тест строит ДВА разных
   описания дома с разными точками и одинаковыми ролями, и ОДИН И ТОТ ЖЕ файл профиля работает в
   обоих. Доказательство живёт в `tests/TunnelCrew.Core.Tests` и проходит `dotnet test`; «проверено
   в редакторе» эту строку не закрывает.
3. **НИЧЕГО НЕ СЛОМАНО, И ОТКАТ НАСТОЯЩИЙ.** Кривая строка по-прежнему отвергается названно и с
   путём в JSON; опубликованный профиль ролей не называет, поэтому поведение хозяина в игре не
   меняется и пять реакций целы; прогон тестов на выгруженном дереве публикации назван числом
   против базовых 214; и существует откат одним коммитом, исполненный НАСТОЯЩИМ `revert`, дерево
   после которого сходится с деревом базы.

## return

**Первая строка возврата — РАСПИСКА:** сколько строк `done_when` получено (должно быть три) и с
какого базиса собрано.

Дальше:

- что сделано, по каждой из трёх строк отдельно;
- полный список путей диффа;
- прогон тестов ДО и ПОСЛЕ числами, прогнанный тобой на выгруженном дереве публикации;
- каждое место в `Core/Householder/**`, которого пришлось коснуться, поимённо;
- откат: команда, хеши, совпадение деревьев знак в знак;
- **что НЕ сделано и почему** — вырезы называются, а не умалчиваются;
- всё неожиданное, включая всё, что противоречит перемеренному выше контексту.

Если `origin/main` сдвинулся — отдельная строка с новой головой.

## budget

Половина сфокусированного дня. Порог смерти 3 этой ставки считает ноги: на общую задачу и первые
задачи всех трёх дорожек их положено не больше двенадцати, иначе первой на паузу встаёт дорожка
`body`, следом `t-loot-1`.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-places-1-001-call.md
