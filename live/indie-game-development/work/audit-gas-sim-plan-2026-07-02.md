# AUDIT: planned gas-simulation solution vs OWNER requirements — 2026-07-02

Owner-requested (in-chat, 2026-07-02, verbatim priority ladder given): full audit of the PLANNED simulation
solution (not code quality), resolve problems, propose the better solution; owner words OVERRIDE docs, docs
supplement. Method: multi-agent workflow `wf_a299ecef-44e` (46 agents, ~5.1M tokens): 6 requirement-dimension
auditors (local-depth / global-scale / verticality / multiplayer / types-mixing / words-vs-docs) → adversarial
per-finding verification → 3 independent architect packages (richness-first / scale-roadmap-first /
player-visible-robustness-first) → 2 independent judges. **35 findings raised → 34 survived verification
(11 CONFIRMED-as-stated at P2+, 1 P1), 1 refuted.** Builds ON the 2026-07-02 24-finding review
(work/review-gas-sim-visual-2026-07-02.md), does not re-report it.

## Owner requirement ladder (2026-07-02, binding over docs)

- TIER-0 (незыблемые): R1 deep believable+gameplay-interesting gas around the player (explosions, mixing);
  R2 global simulation across the whole space (coarser far = ok); R3 level variety — mines/shafts, tall
  vertical levels (hangars desirable but owner: «второстепенное»).
- MAJOR: R4 vertical gameplay; R5 no hard level-size limits (squeeze licensed: «тут мы можем как-то уместиться»).
- Named: R6 multiplayer such that the player never notices problems; R7 procedural visualization layerable;
  R8 many gas types / behaviors / interactions / wind / ventilation through the grid.
- Standing feedback: open-space-first (gas spawns anywhere, no walls-assumption; directional damaging jets in
  the open); all changes additive; locks reopen only signed.

## Headline verdict

1. **The architecture core is RIGHT for the ladder — no course change.** Both ratified locks hold under attack:
   input-lockstep (ADR-0002) dominates all alternatives on player-visible failure modes (host-auth clouds =
   silent per-peer divergence, the unfixable R6 class + mass-conservation kills per
   work/gas-cloud-vs-grid-analysis-2026-06-26.md; rollback = per-mispredict resim of ~196k cells + constant
   pops). Gas 50cm isotropic + sparse-dominant + the signed road order (Sc-kernel → Sc-reactions → Sc-damage)
   all verified correct and additive-safe. The HELD host-auth-clouds draft stays HELD — verdict rationale now
   recorded (§Package item 7).
2. **The single P1:** the richness escalation ladder (TIER-1 impulse/bias shipped → TIER-2 lattice-gas
   bucket-3 rooms → TIER-3 fixed-point velocity) — the plan's own answer to R1 «очень правдоподобно» — was
   dropped from live state at the 2026-06-29 NOW compaction. The ARMED re-trigger (open-space jet owner-eye
   look at the combat slice; open-space contrast today ~2:1) exists only in history files. Without repair the
   plan silently plateaus at TIER-1.
3. **The systemic pattern across the P2s:** promises/obligations living on NO slice or with UNDATED deferrals —
   TIER-2/3 ladder (above); typing mechanism «зеркало среды» (road ends before delivering it); S4 far-tier
   re-entry unowned; TREE #11 host-resilience carrier retired; gas-on-real-network-bus undated while Oct-5 is
   a networked demo; SPEC §4 шов 7 player-kinematics pinned to no slice; ×4-8 multicore divisor load-bearing
   but unscheduled AND citing a mechanism (Burst/Jobs) that R13 (engine-free core) forbids; wake-trigger set
   undefined exactly when Sc-kernel is about to pin active-set semantics; >4-type overflow fork framed as
   halt-avoidance without the R1 mixing lens; checksum O(domain cells) excluded from the Sc-kernel scaling
   gates; near-vertical multi-room (Z-stacked) gameplay untested; floor/ceiling breach inexpressible in the
   aperture schema; flat-hangar keyed evidence while the TIER-0 vertical archetype (mine/shaft) has zero probe.
4. **What HOLDS is substantial** (Appendix C): near face-flow base law believable and wall-free; open-space IS
   the stress case; explosion grammar (no approach-detonation, telegraph+bang) sound; buoyancy shipped signed;
   R2 semantics genuinely hold today within the ceiling (byte-sleep is an exact fixed point of the tick);
   determinism battery protects the silent-drift half of R6; measure-first de-risk loop fired honestly.

## The adopted solution package (richness-first, judges 9/10 + grafts from the other two)

Nothing reopens a lock; nothing weakens a gate; zero new build slices before the jewel; everything additive.

1. **Keep:** ADR-0002 lockstep; gas 50cm isotropic; sparse-dominant store; the signed road Sc-kernel →
   Sc-reactions → Sc-damage; S3/S4/S5 deferral as a bundle (but dated/owned, see 8); visual ON HOLD; LOUD
   THROW as interim invariant assert; Факт-5 no-late-join for the demo.
2. **Re-pin the richness ladder into live state (the P1 fix):** NOW.decisions entry restored; ARMED re-trigger
   = REQUIRED owner-eye axis on Sc-damage («one honest no-walls open-space jet look; failure = the signed
   evidence to open the TIER escalation decision»); bang-read axis on Sc-reactions («the bang reads as a BANG
   up close incl. one open-space no-walls detonation»); one SPEC §6 line naming the full ladder (TIER-2
   precondition «после sparse-солвера» = MET at Sc-kernel GREEN — armed, not pulled); post-Sc-kernel
   checkpoint records it.
3. **Lock-name separation (SPEC, one paragraph, G9):** ADR-0002 NETCODE lock ≠ the no-stored-velocity
   REPRESENTATION boundary (S1/ADR-0012-s1). TIER-3, if evidence triggers it, reopens the LATTER; integer
   fixed-point velocity is lockstep-compatible (Photon Quantum/StarCraft precedent). Prevents a future R1
   verdict from spuriously relitigating R6's foundation.
4. **c-exec-023 (Sc-kernel) framing additions (owner present, all additive, ≈+1 leg absorbed by the split-trip
   clause):** (a) scaling gates time the FULL authoritative path (Step + MeaningChecksum) or add a named
   checksum-scaling gate — if the checksum must become an active-set fold, take it as a SCHEMA decision now
   (canonical X,Y,Z sorted-active fold preserves the hash); (b) gate 2(c) flooded-hangar becomes
   measure-and-BIND: over-budget weak-peer projection auto-opens owner decision d-hangar-flood-fallback-001
   (levers: segment-authored halls FIRST per «второстепенное», sub-partition, mid-2.5D far-only, GAP-4
   tick_divisor, all-peers-all-bubbles, volume cap, multicore — activation owner-signed); (c) WAKE-TRIGGER
   REGISTRY seam in the PLAN (active: boundary flux, conductivity/occupancy change, impulse footprint;
   RESERVED stubs: env-vector delta, reaction front, temperature delta; honesty note: lazy catch-up ≠
   equivalent) — the single most rework-preventing line (protects «зеркало среды» AND the active-front
   economy); (d) roster-128 row; (e) measure-only all-cores naive-split row (real divisor on record before
   anything cites ×4-8); (f) MINE/SHAFT probe row (tall stacked shaft + side rooms, light gas released at
   bottom, ms/tick + active-set trajectory during the rise) — the TIER-0 vertical archetype gets its first
   number; (g) TwoStackedRooms Z-adjacent fixture + independent REDs (light gas rises through the Z-band
   portal, heavy does not) — closes the untested near-vertical multi-room seam; (h) bytes/cell + store memory
   row; (i) pin a NAMED weak-peer CPU proxy class (documented ×2-4 rule); (j) JEWEL GUARD recorded: Sc-kernel
   = the LAST pre-reactions engine-only insertion absent explicit owner re-affirmation quoting R1; (k) the
   2026-07-24 visible-gas milestone re-affirmed or consciously moved HERE, never via «tail movable».
5. **c-exec-021 (Sc-reactions) shape agenda pins (banner already mandates body rewrite; prep runs PARALLEL to
   the Sc-kernel executor leg):** (a) overflow fork primary branch = LAYERED MASS-CONSERVING policy —
   reaction-priority consumption first («слишком много газов = начинается химия», junctions become
   set-pieces), universal floor = admission clamp at the face extending destinationCapacity (reads as
   saturation/back-pressure), K raisable via config citing the owner's 2026-07-02 R8 statement; RED re-worded
   «the sim survives a 5-type junction MASS-EXACTLY» — NEVER «levels never produce it»; plus an R1 owner-eye
   mixing scenario (3+ gases at a junction reads believable); (b) schema = AXIS rules default + OPTIONAL
   explicit reactant-SET override DATA rows (precedence explicit>axis, both in handshake hash) — per-pair
   signature chemistry stays authorable without pair-dispatch; (c) wave×wind RED oracle: gas advected across
   a marching condition-front converts EXACTLY ONCE; (d) bang-read owner-eye axis (see 2); blast-wave fork 3
   resolved WITH the R1 ladder in view (recommendation: gameplay-load-bearing); (e) typing-mechanism HOMING
   decided here — recommendation: named Sc-typing slice immediately after Sc-reactions, written into
   NOW.next_slices (accumulate-with-hysteresis recommended over instant flip); (f) weak-hardware budget fork
   consumes the flooded number + CPU proxy as required inputs.
6. **Sc-damage road-card rows (recorded now, shaped later):** SPEC §4 шов 7 player-kinematics decision BEFORE
   any dose code reads a position (off-checksum-HP-by-accident = worst outcome); temperature = SINK layer only
   guard (T→buoyancy wiring = d-tempfeedback-001, its own signed decision, named re-entry = this shape);
   crouch-under-layer head-Z/feet-Z dose acceptance; the ARMED open-space jet look (TIER ladder evidence);
   cross-type total-capacity seam decided no later than here.
7. **Multiplayer (R6):** lockstep KEPT with the verdict rationale recorded next to the HELD clouds draft
   (loud-and-rare beats silent-and-constant; fixable with UX, not with paradigm swap). July
   d-demo-road-001 shape = CALENDAR date 2026-07-10..15 (not event-triggered) with MANDATORY rows: halt/desync
   player UX (loud stop → readable error → lobby, honoring the base↔raid boundary); raid-boundary checkpoint
   decision; desync-report artifact on the existing ADR-0017 MeaningMembers mask; peer-drop policy fork
   (evidence anchor: SimInstance.cs:38 — no timeout/drop path, any peer drop stalls everyone indefinitely) +
   host-migration spike 1-2 days timeboxed (TREE #11 carrier — lockstep makes it plausible-cheap, spike it,
   never assert it); CANDIDATE slice **Sc-net** (gas on the real FishNet bus + minimal halt UX + desync report)
   positioned after Sc-damage, sized/confirmed at the July shape, hard backstop = scheduled before the
   2026-08-31 Steam-page copy freeze if the page says «co-op»; Steam-transport certification by the Sep-21
   press cut OR signed direct-IP demo decision; mid-raid REJOIN stays cut for the demo (R6 verbatim wording
   recorded beside Факт-5's «позже — может быть»; buy rejoin when strangers, not friends, start dropping).
8. **Scale/vertical honesty (SPEC batch, ONE G9 signature bundled with the c-exec-023 framing sign-off, ~10
   one-line additive edits):** §6 п.2 LEVEL-DESIGN CONTRACT line = measured fully-fine ceiling (number flows
   from Sc-kernel's flooded capture in the same RESULT); §6 п.3 «через Burst/Jobs» → «детерминированное
   фикс-партиционирование в engine-free ядре (механизм решается на своём слайсе)» + RESERVED slice Sc-par
   named; §6 line: S4 partition source for open/wall-less volumes = AUTOMATIC sub-partition (authored
   segment-rooms = optimization, not premise); §5 S3-entry banner: S3 far vertical strata land ON the S4
   rollup, NEVER on interim CoarseBandStep; §6 п.4 note: strata count = per-region function of room height,
   not a global constant; S5 line: floor/ceiling breach = extending aperture authoring to Z-normal faces,
   wall-only S5 = scope cut needing owner ack; richness ladder line (see 2); lock-name separation (see 3);
   §6 OPEN line: capacity is per-type by construction, cross-type TOTAL-capacity = named flow-law seam on
   reserved GAP-1 fields, decide by Sc-damage PLAN; §6 п.3 fallback ladder reordered (segment hangars FIRST,
   «smaller levels» = last resort with signature; «1 ангар на уровень» desirable-not-required). S4 re-entry
   trigger = EARLIEST of: first designed level brief (EXPLICITLY incl. mines/tall levels) exceeding the
   recorded ceiling / before 2026-08-31 IF Steam copy markets scale / post-Sc-damage road checkpoint backstop.
   OWNER-OF-THE-WATCH for every trigger = the road checkpoint session named in NOW.next.
9. **Decision-queue repairs (writer-applied):** d-biglevels-tree9-001 gains the ladder-consistent third option
   (keep coarse-far-tier as the R2 carrier with dated S4 re-entry keyed to mines-not-only-hangars; re-sign
   ONLY the numeric 200×200×40/≥1000-volume profile down to a TARGET with explicit R5 squeeze license; note
   that option-2-as-worded silently also re-signs the SPEC's ~150-room target). d-finer-grid-fork-001 text
   broadened: option 2 = READ-ONLY detail-bubble slice as view refinement PLUS the ведро-1 host with its
   Fact-3 ADR (radius+cell-size); option 1 gains the honesty line (taking it orphans the bubble's sim-side
   roles — all-peers-all-bubbles hangar fallback + ведро-1 feel — re-record or drop WITH signature).
10. **Traceability:** every additive line in the framing CALL / SPEC batch / agenda pins carries its source
    finding id (per the judge graft) so closure is mechanically auditable at the next sweep.

## Honest limitations the owner must accept (player-facing, plain words)

- Направленная струя в открытом поле сегодня ~2:1 контраст («толчок», не резкая струя); взрыв = телеграф +
  расходящийся толчок в стабильность, НЕ бегущая ударная волна. Улучшение — только через owner-eye вердикты
  (бах на Sc-reactions, струя на Sc-damage) → подписанное включение TIER-2/TIER-3 (платно: новые члены
  чексуммы + перезамер слабого пира). Не бесплатно, не автоматически.
- Максимум 4 типа газа в одной клетке; 5-й на перекрёстке = химия (реакция освобождает слоты) или
  насыщение/обратное давление — по подписанной политике; буквальной 5-газовой смеси в клетке не будет.
- «Нагрел комнату — газ озверел» на этой дороге не приезжает: temp→gas отложено (d-tempfeedback-001,
  re-entry = Sc-damage shape); ранняя смена типа — от плотности/соседей.
- Газы не выталкивают друг друга из клетки (нет меж-типового вытеснения) до отдельного решения по закону
  потока — записано как именованный шов (решить ≤ Sc-damage PLAN).
- Потолок полностью-детального уровня ≈200k клеток (~134 комнаты) на слабом ядре; канонические ~150 комнат
  УЖЕ выше; больше — только S4 (теперь с датированными триггерами, но без даты сборки). Шахты/вертикали В
  ПРЕДЕЛАХ потолка работают полностью уже сейчас.
- Затопленный гигантский открытый ангар может превысить бюджет слабого пира даже после Sc-kernel — тогда
  подписанный фолбэк (сегментировать ангары ПЕРВЫМИ — по владельческому же рангу), не тихое ужатие всей игры.
- Мультиплеер сегодня: рассинхрон/краш/дроп = громкая остановка, рейд кончен для ВСЕХ, повторного входа нет;
  один слабый ПК замедляет газ у всей группы (постоянная цена бит-идентичности); читы ESP-класса возможны
  (принято для friends-coop, записано). Июльский demo-shape делает всё это видимым и подписанным.
- Газ ещё ни разу не ездил по реальной сети (loopback-only); первый реальный линк может вскрыть проблемы
  ощущения (input-delay/stall), которые ни один текущий гейт не покрывает — потому Sc-net и реальный-линк
  eval стоят ДО демо.
- До ~2026-07-24 единственное видимое = телеграф+бах в движковом debug-виде (визуал на паузе по директиве
  владельца — осознанная цена «сначала симуляция»).
- Числа слабой машины — прокси (×2-4 от сильной, однопоточно); реальный min-spec CPU фиксируется на shape
  реакций; до тех пор «влезает/не влезает» = оценки.

## Words-vs-docs divergences (owner asked explicitly; his words win)

- TREE #9 (200×200×40м, ≥1000 объёмов) сильнее его слов («можем ужаться») → re-sign numbers as TARGET with
  squeeze license, keep the R2 carrier (package item 9).
- Hangar: owner demoted to «второстепенное», road centers evidence on the hangar → hangar HOLDS as the honest
  open-space stress-proxy; only the FALLBACK ORDER inverts his ladder (fixed in package item 8) and the
  mine/shaft archetype lacked any probe (fixed in item 4f).
- «Мультиплеер идеально» (R6) vs docs: Факт-5 no-late-join + loud-halt + no recovery — docs promise LESS than
  his words; reconciliation = July shape rows + honest demo-page wording (item 7), not silent.
- Visual ON HOLD is CONSISTENT with R7 (read-only seam preserved); but the road's anti-tunnel justification
  still cited the frozen visual track as counterweight → replaced by the JEWEL GUARD + milestone re-affirmation.
- Open-space-first feedback vs room-centric SPEC framing: near tier genuinely wall-agnostic (HOLDS); the
  room-assumption lives only in S4 rollup partitioning → automatic sub-partition line (item 8).

## Refuted (1)

words-vs-docs-003 («R1 levers have no home») — misread: the TIER-3 re-trigger IS homed (combat slice =
Sc-damage, on the road), Sc-reactions terminates in an owner-eye visible telegraph+bang, and
d-finer-grid-fork-001 is already a tracked pending decision. See Appendix B.

## Judges

Both judges: **richness-first wins (9 / 9)** over scale-roadmap-first (8.5 / 8.5) and
player-visible-robustness-first (7 / 7). Grafts adopted into the package above: one-signature SPEC batch,
finding-id traceability, explicit cost accounting, owner-of-the-watch on every trigger, at-Sc-kernel-GREEN
checkpoint choreography (3 state writes in one session), Sc-par named in SPEC as RESERVED, lockstep verdict
rationale recorded beside the HELD clouds draft, Sc-net candidate slice composition, calendar-dated July
shape, Steam-transport Sep-21 backstop, SimInstance.cs:38 anchor, blunt limitation phrasings, ESP honesty
line, accumulate-with-hysteresis typing recommendation.

## Appendix A — the 34 confirmed findings (full claims, post-verification)

### local-depth-01 [P1 / CONFIRMED] The concrete TIER-2/TIER-3 escalation trigger for near-player believability was dropped from live state at the 2026-06-29 NOW compaction — the richness stack now plateaus at TIER-1 with no scheduled re-check

- **Requirement:** R1 (TIER-0) — very believable, gameplay-interesting gas around the player; standing feedback: directional damaging jets in the open
- **Claim:** The plan's own answer to 'does decaying-bias TIER-1 reach очень правдоподобно' was a named, ARMED re-trigger: one honest no-walls open-space jet owner-eye look at the COMBAT slice (S6-class), because S1's owner-eye «работает» was measured on confined cubes while open-space directional contrast is still only ~2:1 (not a sharp jet). That trigger lived in the pre-compaction NOW (d-gas-richness-tiers-001) with 'NO silent entry'; the compacted live NOW.md decisions list, TREE, and the SPEC no longer carry it anywhere — the SPEC only says TIER-3 is a 'резерв' with no criterion or schedule, TIER-2 (lattice-gas bucket-3 rooms) appears in no live file at all, and the current road (Sc-kernel → Sc-reactions → Sc-damage) names no combat slice or open-space jet look. Additionally, TIER-2's stated precondition ('после sparse-солвера') is about to be satisfied by Sc-kernel, yet nothing marks that checkpoint. Net effect: near-player richness silently tops out at mushy diffusion + a ~2:1 open-space jet unless someone remembers a criterion that now exists only in history files — exactly the 'silent drop of frozen intent' failure the owner has flagged before.
- **Resolution folded into the package:** Additive repair, no lock touched: re-pin d-gas-richness-tiers-001 into live state. (1) Add the ARMED re-trigger as a REQUIRED-to-CHECK owner-eye axis on the first combat-class slice — Sc-damage is the natural S6-class home (fold into its shape agenda now, alongside the already-folded d-coop-interdependence-repin-001 axis): 'one honest no-walls open-space jet look; if it cannot read as a tight jet, that look = the signed evidence to open the TIER-3 ADR' (cite lock as ADR-0002 per SPEC §1 поправка — the old ADR-0010 wording is a known citation error). (2) Add one SPEC §6 line naming the full escalation ladder (TIER-1 shipped / TIER-2 lattice-gas bucket-3 rooms, precondition = Sc-kernel, ADR-class / TIER-3 fixed-point velocity, owner-signed lock reopen) so the reserve tiers stop living only in a dated research file. (3) Record a post-Sc-kernel checkpoint note that TIER-2's 'после sparse-солвера' precondition is now met (armed, not pulled).
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\history\s-work-023.md` — «d-gas-richness-tiers-001 += UPDATE: S1a velocity status — TIER-3 reserved, trigger ARMED (open-space still ~2:1; «работает» was on confined cubes); re-trigger = one honest no-walls open-space jet look at the COMBAT slice = signed evidence for ADR-0010. ... Carry so the corridor 27x can't silently an…»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\now-snapshot-2026-06-29.md` — «VELOCITY / TIER-3 → RESERVED, trigger ARMED (d-gas-richness-tiers-001): «работает» was on confined cubes; open-space is still ~2:1 (not a sharp jet); re-trigger = one honest no-walls open-space jet look at the COMBAT slice; that look (if it fails) is the signed evidence for the ADR-0010 lock-crack. …»

### local-depth-02 [P2 / PARTIAL] Explosion 'bang' up close is structurally capped (bounded decaying bias inside a stability envelope) and its believability has only a non-binding owner-eye, with the blast-wave gameplay-vs-cosmetic fork still open

- **Requirement:** R1 (TIER-0) — explosions near the player must be believable AND gameplay-interesting
- **Claim:** The planned explosion is telegraph+bang whose outward push is pinned to the S1 decaying face-bias impulse bounded inside a settle envelope (c-exec-021 boundaries + model property 3), the same mechanism whose open-space directional contrast measured ~2:1; the only in-CALL feel check is done_when #11 ('confidence, NOT a gate'), whose axes verify the bang OCCURS and its consequence is NON-LOCAL — not that it READS as a bang up close or in the open — and fork 3 (blast wave gameplay-load-bearing vs cosmetic) is open until the c-021 shape. The TIER escalation ladder itself IS armed with a recorded owner-eye re-trigger (d-gas-richness-tiers-001 / s-work-023: one honest no-walls open-space JET look at the COMBAT slice = signed evidence for the lock-crack ADR), so a weak open-space read is not structurally invisible; the actual gap is that this armed trigger is jet-specific and homed at Sc-damage — neither c-exec-021's owner-eye axis nor the ladder names a weak-BANG read as escalation evidence, and the ENGINE-ONLY design defers most of the up-close 'reads as a bang' load to the on-hold visual track without an explicit record of which half of R1 believability the engine slice must carry. Fix at the mandated c-021 body rewrite: add the bang-read owner-eye axis (incl. one open-space no-walls detonation), name a failed bang-read as additional signed ladder evidence alongside the jet look, and resolve fork 3 with the R1 ladder in view.
- **Resolution folded into the package:** At the c-exec-021 re-hardening/shape (its banner already mandates a body rewrite): (1) add an explicit R1 owner-eye axis 'the bang reads as a BANG up close, including one open-space no-walls detonation' (owner-eye stays confidence-not-gate — additive, weakens nothing); (2) record that a failed bang-read is ANOTHER named piece of signed evidence for the TIER-2/TIER-3 escalation ladder (same channel as local-depth-01), so a weak result routes to a decision instead of evaporating; (3) resolve fork-3 (load-bearing vs cosmetic) with the R1 ladder in view rather than as a pure scope call.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-021-call.md` — «the PLAN MUST bound the emitted Strength/Ttl so the COMBINED desiredMove (gradient + buoyancy + shove) still reaches rest ... 11. **OWNER-EYE (confidence, NOT a gate):** owner sees two reactant test gases meet, TELEGRAPH (warn), then BANG»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\gas-reaction-typing-design-2026-06-30.md` — «3. Blast wave: gameplay-load-bearing (pushes gas/bodies, chains detonations → in the sim, S1-impulse) vs cosmetic (a visual whoosh → could live in the visual track).»

### local-depth-03 [P2 / CONFIRMED] The environment-interaction half of R1 (dynamic-typing mechanism + temperature→gas feedback) sits behind an unnamed slice with an undated two-link deferral chain

- **Requirement:** R1 (TIER-0) — believable gas interaction with the environment around the player (heat/fire/conditions changing gas behavior); SPEC §5 owner-signed core requirement 'зеркало среды'
- **Claim:** Env-derived dynamic typing is an owner-signed CORE requirement, but the plan only lays the stamp socket (Sc-rep, done): the typing MECHANISM (exposure accumulator/flip) is deferred to a 'typing slice' that appears on no road list — NOW's road ends at Sc-damage with no typing entry — and c-exec-021's banner treats it as 'its own concern'. Worse, the most believability-loaded conditions (heat: explosion warms a cold gas which turns aggressive) additionally wait on the undated d-tempfeedback-001 temp→gas feedback deferral; Sc-damage builds only the gas→temperature sink, not the reverse read. So the chain is: unnamed typing slice → undated temp→gas feedback → dormant temperature rows. Nothing breaks today, but as written the 'many faces, mirror of environment' behavior near the player has no scheduled delivery point and can quietly plateau at density/neighbour-driven flips only — a large silent haircut on the near-player believability the requirement was signed for.
- **Resolution folded into the package:** At the c-exec-021 shape (owner present): decide whether the typing mechanism lands INSIDE Sc-reactions (design forks 1/4 are already on its agenda) or as a named Sc-typing slice immediately after it, and write the chosen slice into NOW next_slices so the road visibly ends where the signed requirement is delivered, not before it. Simultaneously give d-tempfeedback-001 a named re-entry condition (natural: 'decide at the Sc-damage shape, where the temperature sink-layer lands') instead of undated. Pure naming/scheduling — no new build, no gate weakened.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «Sc-rep кладёт только ШТАМП-сокет; механизм типизации (аккумулятор/флип) — слайс реакций/типизации ... temp-ключевая типизация ждёт отложенной temp→gas обратной связи (`d-tempfeedback-001`) — сокет кладётся, temp-строки СПЯТ; density/соседи могут драйвить раньше.»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-021-call.md` — «the typing MECHANISM (exposure accumulator/flip) is its own concern — if «accumulate» wins (design fork-1), it adds a NEW per-cell replicated field = a NEW cell-keyed SKIP-ZERO MeaningChecksum member ... a §9.5 schema decision to take deliberately at the typing slice.»

### local-depth-04 [P3 / PARTIAL] The detail bubble — canon's designated host for ведро-1 near-player feel, the hangar 'all-peers-all-bubbles' fallback, and the anti-ESP argument — exists on no slice, and d-finer-grid-fork-001 option 1 would silently orphan those sim-side roles (builds on review findings 7/23)

- **Requirement:** R1 (TIER-0) — richness/feel around the player (vortices, transient splits); open-space feedback
- **Claim:** The SPEC Fact-3 detail bubble — canon host for ведро-1 near-player feel (vortex, transient fork-split), presumed object of the §6 п.3 all-peers-all-bubbles hangar fallback, and referenced (non-load-bearing) by §6 п.7 — is pinned to no slice. This gap was already reported by the 2026-06-29 plan audit ("pin the bubble to a slice + record the dependency"): the dependency half was recorded (road-shape kill_by, wave-plan honesty note, c-exec-021 forward-discipline), but the pin half silently fell through the road re-shape. The pending d-finer-grid-fork-001 is the only live decision touching near-player refinement: option 2 names the detail-bubble seam, but option 1 ("accept render-side as the medium; strike the line") is worded as a visual-only cut and does not state that taking it leaves the bubble's sim-side roles unscheduled while the visual track carrying the render-side alternative is ON HOLD. The orphaning would be neither permanent nor unrecorded (SPEC stays canon; per-PLAN bucket classification resurfaces the missing host at the first ведро-1 mechanic's PLAN), but the decision text should additively gain one honesty line on option 1 — and optionally fold "READ-ONLY detail-bubble slice as ведро-1 host with its Fact-3 ADR" into option 2 — so the owner's choice is fully informed and no canon role is waved through without a signature.
- **Resolution folded into the package:** Before the owner takes d-finer-grid-fork-001, additively broaden its text so the choice is honest: option 2 becomes 'a READ-ONLY detail-bubble slice — view refinement PLUS the ведро-1 host, with its Fact-3 ADR (radius + cell size, owner-signed)'; option 1 gains one honesty line: 'this also leaves the detail-bubble mechanism unbuilt — the §6 п.3 all-peers-all-bubbles hangar fallback and ведро-1 feel mechanics must be re-recorded as render-side or dropped WITH a signature'. Either branch stays lock-safe (bubble is off-checksum by construction); the point is that no sim-side canon role gets dropped silently by what looks like a visual-only decision.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «**Ведро-1 — сглаживается:** свободное локальное ощущение (вихрь; раздвоение, которое сливается обратно в комнате) — ВНЕ чексуммы, в отбрасываемом пузыре. ... Пузырь КАПНУТ фиксированным радиусом-сферой ... (старт 4м@50см; 25см-внутри или большой радиус тихо вернут взрыв → лок обязателен)»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\character-road-shape-2026-06-29.md` — ««all-peers-all-bubbles» (§6 п.3) presumes the detail-bubble mechanism not yet built, so it is a future-fallback, not an immediate remedy.»

### local-depth-05 [P3 / CONFIRMED] The >4-co-resident-types overflow fork (c-021 banner item 13) is framed purely as halt-avoidance; the R1 mixing-richness lens is missing from its agenda (builds on review finding 4)

- **Requirement:** R1 (TIER-0) mixing + R8 many types interacting near the player
- **Claim:** The LOUD-THROW halt itself is already a named fork (banner item 13, review finding 4) — not re-reported. The gap on my axis: the fork's listed policy options (canonical merge-to-nearest-axis / raised K / owner-signed declared sink) are evaluated only against 'is the throw unreachable', with an adversarial 5-type-junction RED proving unreachability. None of the agenda asks whether the chosen policy PRESERVES believable mixing near the player: merge-to-nearest-axis silently rewrites what a 5-gas junction looks and behaves like at the exact spot R1 cares most about, and K=3 is itself a per-cell mixing-richness ceiling in a game whose core fantasy is mixing. A policy can pass the unreachability RED while making dense multi-gas encounters read wrong up close.
- **Resolution folded into the package:** One additive line on banner item 13's shape agenda: the chosen policy is ALSO judged against the R1 mixing bar — an owner-eye scenario where 3+ gases meet at a junction must still read believable (what the merged/dropped mass looks like near the player is part of the decision, not an implementation detail). No gate weakened; the unreachability RED stays.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-021-call.md` — «the shape MUST decide, owner present: (a) the representation-level policy for NON-reacting >4-type mixes (canonical merge-to-nearest-axis / raised K / owner-signed declared sink), and (b) an adversarial RED: a 5-type junction scenario proving the throw is unreachable»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «реальный предел — co-resident-активные типы в зоне (горстка) + число активных клеток»

### global-scale-1 [P2 / PARTIAL] S4 room-rollup is SPEC-mandatory already at the owner's TARGET scale (~150 rooms), yet deferred UNDATED with an unowned re-entry trigger

- **Requirement:** R2 (TIER-0 global sim everywhere) + R5 (no hard size limits) + TREE g-9c41 done_when #9
- **Claim:** SPEC §6 п.2 itself declares S4 room-rollup obligatory at the owner-raised target scale (~150 rooms ≈223k cells > ~200k comfortable weak-core ceiling ≈134 rooms), yet the road defers S3/S4/S5 with only the vague, unowned, undated trigger «вернуть, когда уровни вырастут», and the SPEC's own resolution point for the ceiling («уточняется hangar-пробой + на PLAN S4») circularly inherits that undated deferral. The pending decision d-biglevels-tree9-001 covers only the TREE#9 huge-level case (~64× over), not the nearer fact that the SPEC's own ~150-room target already exceeds the interim fully-fine ceiling, and the measured ceiling is not written anywhere as an explicit level-design contract that level/demo design would consult. This endangers R5 (MAJOR — owner accepts squeezing, but only via explicit signature) and TREE g-9c41 done_when #9, not TIER-0: R2's global sim stays intact on any level within the ceiling, and the drop-risk is already surfaced non-silently as a pending owner decision. Fix = when resolving d-biglevels-tree9-001, widen its framing to include the ~150-room target case, write the Sc-kernel flooded-hangar measured ceiling into SPEC §6 п.2 as an explicit level-design contract line, and give S4 re-entry a dated/owned trigger (the signed Sc-kernel slice already schedules the flooded-hangar capture that supplies the number).
- **Resolution folded into the package:** Resolve d-biglevels-tree9-001 with option 1, concretized (additive, no lock touched): (a) write the measured fully-fine interim ceiling into SPEC §6 п.2 as an explicit LEVEL-DESIGN CONTRACT line (number set from Sc-kernel's flooded weak-peer-adjusted capture); (b) S4 re-entry fires at the EARLIEST of: (i) any level/demo design brief exceeding that ceiling, (ii) before the 2026-08-31 Steam-page copy freeze IF page copy markets scale/huge levels (do not market scale with no machinery), (iii) end of the character road (after Sc-damage) as the latest backstop; (c) name the owner of the watch = the road checkpoint session (NOW next). If the owner instead re-signs TREE#9 down (option 2), that is legitimate ONLY as an explicit signature, never as a fallback default.
- evidence: `live/indie-game-development/knowledge/g9c41-gas-engine-SPEC.md` — «§6 п.2: «NB: ~150 комнат (≈223к) уже выше КОМФОРТНОГО потолка ~200к (≈134 комнаты) на слабом ядре — это и делает S4 room-rollup ОБЯЗАТЕЛЬНЫМ»»
- evidence: `live/indie-game-development/knowledge/g9c41-gas-engine-SPEC.md` — «§5: «Far-тир S3/S4/S5 ОТЛОЖЕНЫ (масштаб-водопровод для больших уровней — не срочно на текущем малом масштабе; вернуть, когда уровни вырастут)»»

### global-scale-2 [P2 / PARTIAL] Flooded player-occupied open hangar consumes the whole weak-peer budget and the Sc-kernel gate only MEASURES it — no fallback decision is wired when the measurement shows the problem

- **Requirement:** R3-hangars (owner: «второстепенное» = major-not-critical) + R2 worst case + R5
- **Claim:** The c-exec-023 draft's flooded-hangar gate 2(c) is measure-and-record only: unlike the road's own precedent (character-road-shape-2026-06-29.md Sc-rep kill_by: "hangar blows the plausible ceiling → STOP + re-shape to a lock-safe fallback", which fired 2026-07-02 and produced signed d-sparse-tick-kernel-001), the draft wires no kill_by/next_if_false tying an over-budget flooded number (all ~196k cells active; dense roster-1 class = 18.0 ms strong machine, ×2–4 weak peer, more under co-resident overlay lanes, single-threaded, vs the working 50–100 ms budget) to any decision, and the return rolls to Sc-reactions on GREEN. The measurement is not doomed to die unseen — the owner sees the before/after table at owner-eye (done_when 7), the hangar BUDGET/threshold + target-weak-hardware fork is already scheduled owner-present at the Sc-reactions shape (road cut-list; c-021 banner item 8; SPEC §6 п.3), and NOW.md next schedules kill_by-setting + adversarial hardening at framing — but none of these REQUIRES that an over-budget flooded number opens the owner-gated reserved-fallback decision (sub-partition / mid-2.5D far-only LOD / all-peers-all-bubbles / smaller volumes — all "включаем ТОЛЬКО если замер покажет проблему", activation owner-signed), the reactions-shape fork is chartered to set the budget not to adjudicate flooded fallbacks, and the SPEC's first-line non-reserved multicore lever (active_cells/cores ×4–8, Burst/Jobs) is likewise unscheduled on any slice. Fix stays as proposed: at c-exec-023 framing, additively wire the conditional — IF the flooded weak-peer projection exceeds the recorded budget THEN the RESULT opens d-hangar-flood-fallback-001 enumerating the reserved lock-safe levers (plus the multicore lever) — and/or name the flooded number a required input to the Sc-reactions-shape budget fork.
- **Resolution folded into the package:** Additive, at c-exec-023 framing: extend the return contract with an explicit conditional — IF the flooded-hangar weak-peer projection exceeds the recorded budget THEN the RESULT must open owner decision d-hangar-flood-fallback-001 enumerating the already-reserved, lock-safe levers (activate sub-partition of large volumes; mid-2.5D far-only vertical LOD; GAP-4 per-region tick_divisor as a degraded-tick lever for flooded-but-not-player-adjacent subvolumes; or a signed max-single-open-volume cap). No lever activates silently — the decision node just guarantees the measurement cannot die in a docs/measurements file.
- evidence: `live/indie-game-development/work/c-exec-023-draft-call.md` — «done_when 2(c): «adversarial «hangar flooded» — ALL ~196k cells active: measured and recorded vs the 50–100 ms budget with the weak-peer ×2–4 caveat … no silent pass» — measured, but no conditional action; return: «On GREEN the road rolls to Sc-reactions»»
- evidence: `live/indie-game-development/knowledge/g9c41-gas-engine-SPEC.md` — «Факт 2 ведущий кадр: «Зарезервировано (НЕ активно): авторить зал как сегменты-комнаты с дверьми-во-всю-стену … ИЛИ дальний вертикальный LOD mid-2.5D … + sub-partition. Включаем ТОЛЬКО если замер покажет проблему.»; §6 п.3: «cost ∝ active fronts ЛОЖНО (стена возвращается у игрока в ангаре …)»»

### global-scale-3 [P2 / PARTIAL] Per-tick MeaningChecksum is O(domain cells) and the Sc-kernel scaling gates time SIM-only Step — the «cost ∝ active cells» contract as gated excludes the checksum, which also nullifies the byte-sleep economy at scale

- **Requirement:** R2 (global sim everywhere on weak peer) + R5 (big domains) + Fact 1 per-tick checksum
- **Claim:** The Sc-kernel scaling gates (c-exec-023 done_when 2, 'same harness as 2026-07-02') time VoxelFaceFlow.Step only (VoxelBenchmarkDirector.cs Stopwatch loop lines 206-219; Caveat line 268), while the canonical per-tick lockstep contract (SPEC Fact 1 'по-тиковый чексумм по СМЫСЛУ'; §4 seam 5 cell-keyed per-cell stamp) also includes SparseDominantNearGasField.MeaningChecksum, whose cost is O(domain) walk + O(non-empty) fold (line 267) — independent of the awake/active front. The slice can therefore certify 'cost ∝ active cells' and byte-sleep with every gate green while its own adversarial case (flooded hangar, gate 2c) retains an untimed per-tick checksum residue (~5-10 ms on the owner machine at ~200k saturated cells, 2-4x on a weak peer vs the 50-100 ms budget) — a permanent floor under the 'спящий газ бесплатен' economy once gas joins the network bus (~S4; until then the checksum is loopback-only per SPEC §4 seam 5, so this is a future-contract gap, not a shipping cost today). Likewise the 'sparse' store allocates three domain-sized arrays (~40 B/cell, lines 63-65) and no gate or measurement row anywhere records bytes, despite SPEC §6 p.3's own 'стена = кэш/память, не флопы'. (The original TREE#9 12.8M-cell extrapolation is dropped: huge levels are already routed to S4 rollup under pending d-biglevels-tree9-001, and the empty-cell walk alone at that scale is a few ms, not budget-class.) Additive fix at framing: time the full authoritative per-tick path (Step + MeaningChecksum) in the scaling gates or add a named checksum gate including the saturated case — noting a sorted-active-set fold in canonical X,Y,Z order preserves the identical hash value, so this fits inside done_when 1's existing 'canonical iteration order' PLAN decision without a schema change — and add bytes/cell + total store memory as a measure-only row in the before/after matrix.
- **Resolution folded into the package:** Additive, at c-exec-023 framing: (a) redefine the scaling gates to time the FULL authoritative per-tick path (Step + MeaningChecksum) or add a named checksum-scaling gate; if checksum must become active-set/incremental to pass, that is a checksum-SCHEMA decision to take NOW cell-keyed-canonically (per the SPEC's own «schema-решение СЕЙЧАС, не миграция на живых узлах» principle), not a later migration; (b) add bytes/cell and total store memory as a recorded row in the before/after measurement matrix (measure-only, no threshold needed yet). Neither touches ADR-0002 — an active-set fold in canonical X,Y,Z order preserves meaning-coverage.
- evidence: `C:/projects/Unity/GasCoopGame/Assets/GasCoopGame/Core/Field/Voxel/SparseDominantNearGasField.cs` — «line 257/267: «public MeaningChecksum MeaningChecksum(MeaningMembers members)» → «for (int cell = 0; cell < _cellCount; cell++)»; lines 63-65: «_records = new SparseDominantCellRecord[cellCount]; _activeCells = new int[cellCount]; _slotByCell = new int[cellCount];»»
- evidence: `C:/projects/Unity/GasCoopGame/Assets/GasCoopGame/Adapters/GasView/VoxelBenchmarkDirector.cs` — «line 268: «- SIM-only ms/tick (VoxelFaceFlow.Step), measured by Stopwatch» (checksum NOT in the timed loop)»

### global-scale-4 [P2 / CONFIRMED] Byte-sleep wake-trigger set is undefined at the slice that pins sleep semantics; env-derived dynamic typing already signs a mechanic that mutates SLEEPING cells without mass flux

- **Requirement:** R2 («симуляция проходит везде» in the owner's sense) + R8 (types/interactions) + signed dynamic-typing core requirement
- **Claim:** Sleep-as-identity is only an honest «global sim everywhere» if EVERY event that would change a sleeping cell provably wakes it. c-exec-023 done_when 1 has the PLAN decide «how dormant saturated gas byte-sleeps» but nowhere requires enumerating WAKE triggers, and two already-signed future mechanics write to sleeping cells with zero mass flux: (a) env-derived dynamic typing — the per-cell exposure accumulator «integrates env-vs-порог по тику»: a sleeping saturated cloud whose environment shifts (heat rises, neighbors change) accumulates nothing while asleep, so it never re-types — contradicting the signed «зеркало среды» core requirement — or the typing slice must keep gas awake wherever env varies, destroying the active-front economy Sc-kernel just bought; (b) structure/conductivity flips (S5 breach) and impulse-event footprints must wake regions. If Sc-kernel pins a mass-flux-only wake rule, Sc-reactions/typing forces a kernel rework one slice later.
- **Resolution folded into the package:** Additive, mirrors the OS's own §9-seams pattern (data-form now, behavior later): require the c-exec-023 PLAN to record a WAKE-TRIGGER REGISTRY as a seam — active rows: boundary mass flux, face conductivity/occupancy change (breach), impulse-event footprint; RESERVED rows (stubs, no behavior): env-vector change at cell, reaction front arrival, temperature delta. Sc-reactions/typing then plugs into a named socket instead of reopening the kernel. Also record the honesty note: lazy catch-up accumulation on wake is NOT equivalent when env varies over the sleep interval — the reserved row must be a wake trigger, not a backfill.
- evidence: `live/indie-game-development/work/c-exec-023-draft-call.md` — «done_when 1: «DECIDE+RECORD: active-set/front data structure + canonical iteration order …; how dormant saturated gas byte-sleeps (Факт-4)» — no wake-trigger enumeration required»
- evidence: `live/indie-game-development/knowledge/g9c41-gas-engine-SPEC.md` — «§5 динамическая типизация: «в РАНТАЙМЕ при смене среды НАКАПЛИВАЕТ и может ФЛИПнуть тип … per-cell exposure-аккумулятор integrates env-vs-порог по тику»; §3: «settled_flag … гейтит ТОЛЬКО far-демоут/сон»»

### global-scale-5 [P2 / CONFIRMED] The ×4-8 multicore divisor is load-bearing in the SPEC's weak-peer ceiling math but is scheduled on NO slice, and its stated mechanism (Burst/Jobs) contradicts the R13 zero-Unity-refs core

- **Requirement:** R2 + R5 (weak-peer ceiling math for large levels) + live rule R13
- **Claim:** SPEC §6 п.3 defines the weak-peer limit as active_cells / cores «через Burst/Jobs … → ×4-8; мерить на ВСЕХ ядрах», and the large-level viability math (finding 1, finding 2) only closes with that divisor: ~223k flooded active cells ≈ 22–180 ms weak-peer single-threaded straddles/exceeds the budget, but fits comfortably at /4–8. Yet (a) both real measurements (c-exec-014, 2026-07-02) are single-threaded loops and the c-exec-023 draft contains no parallelism item — no slice anywhere builds or even measures the multicore path; (b) Burst/Jobs are Unity packages, while live rule R13 locks the sim core as «чистая C#-библиотека, ноль Unity-ссылок, headless» — the deterministic-parallel-tick story for an engine-free core (fixed deterministic partitioning over System.Threading, or moving the authoritative tick behind an adapter seam) has never been designed. The plan silently banks a 4-8× that nothing delivers and whose named mechanism its own rules forbid.
- **Resolution folded into the package:** Additive: (a) one-line SPEC §6 п.3 correction — replace «через Burst/Jobs» with «через детерминированное фикс-партиционирование в engine-free ядре (механизм решается на своём слайсе)» so the math stops citing a mechanism R13 forbids; (b) name a reserved slice «Sc-par / deterministic parallel tick» positioned after Sc-kernel and before S4 is relied on (it is the cheap half of the same headroom S4 buys); (c) add to the c-exec-023 measurement matrix one MEASURE-ONLY all-cores naive-split row so the real divisor (not the assumed 4-8) is on record before level-size promises use it. No lock touched — determinism-by-construction (pure function of last tick, double-buffered) already permits this by design.
- evidence: `live/indie-game-development/knowledge/g9c41-gas-engine-SPEC.md` — «§6 п.3: «предел слабого пира = active_cells / cores (грид — чистая функция прошлого тика, double-buffered, детерминирован по ядрам через Burst/Jobs, без atomics) → ×4-8; мерить на ВСЕХ ядрах»; §5: «R13 sim-ядро = чистая C#-библиотека, ноль Unity-ссылок, headless»»
- evidence: `C:/projects/Unity/GasCoopGame/docs/measurements/sc-rep-hangar-real-measurement-2026-07-02.md` — ««Runtime (this capture): .NET 8.0.20 (CoreCLR), Release config, dotnet run» — a single-threaded console loop; no all-cores row exists in any measurement doc»

### global-scale-6 [P3 / CONFIRMED] S4 rollup presumes a region partition that open-space/no-rooms worlds lack; the reserved answer is authoring-time and the automatic sub-partition + far vertical strata are undefined

- **Requirement:** R2/R5 for open-space-first worlds (standing feedback: gas spawns ANYWHERE, drop the walls-assumption) + R3 tall vertical levels far-fidelity + R4
- **Claim:** The plan's only scale mechanism is room-graph rollup over region_id labels; the SPEC's reserved answer for big open volumes — «авторить зал как сегменты-комнаты с дверьми-во-всю-стену» — is an AUTHORING-time device that assumes designed room boundaries, while standing owner feedback makes open, wall-less space first-class; the automatic «sub-partition» that would cover arbitrary open volumes and procedural exteriors (GAP-2 void/exterior is a sink socket, not a partition) is reserved with no algorithm, owner, or slice. Likewise the far-tier vertical strata count (§6 п.4) — what preserves R3/R4 vertical routing in a rolled-up tall shaft — is deferred to the same undated S3/S4 PLAN. P3 not P2 because the near tier is partition-free (open space already works fully-fine within the size ceiling), hangars are owner-declared secondary, and the levers are at least named; but if the S4 shape arrives room-graph-only it under-delivers exactly the level classes the owner ranked TIER-0 (mines/shafts/vertical) and open space.
- **Resolution folded into the package:** Additive, zero build now: when d-biglevels-tree9-001 is resolved, add ONE SPEC §6 line: «S4 partition source for open/wall-less volumes = AUTOMATIC sub-partition (algorithm = S4 PLAN decision; authored segment-rooms are an optimization, not the premise)», and pin §6 п.4 (vertical strata) + open-space partitioning as named mandatory items on the future S4 PLAN agenda so the rollup shape cannot silently assume rooms exist.
- evidence: `live/indie-game-development/knowledge/g9c41-gas-engine-SPEC.md` — «Факт 2 ведущий кадр: «дефолт — ~однородные малые/средние комнаты … Зарезервировано (НЕ активно): авторить зал как сегменты-комнаты с дверьми-во-всю-стену … + sub-partition»; §3: «rollup требует РАЗДЕЛА = граф-как-метка»; §6 п.4: «Вертикальное разрешение FAR-rollup … Когда: PLAN S3/S4»»
- evidence: `C:/projects/Unity/GasCoopGame/docs/INTERIM-COARSE-FAR-TIER.md` — «the only existing far-tier partition source is the room/topology ingestion (RoomSpec/LinkSpec via TopologyDocument) — generator-authored rooms, no automatic partitioner exists»

### verticality-far-reentry-keyed-to-wrong-archetype [P2 / PARTIAL] All scale evidence, probes and re-entry discourse are keyed to the FLAT hangar (owner: «второстепенное»); the TIER-0 vertical archetype (mine/shaft/tall level) has zero probe, zero trigger, zero named place in the pending scale decision

- **Requirement:** R3 (mines/tall levels, TIER-0 variety) + R5
- **Claim:** The TIER-0 vertical archetype (mine/shaft/tall level) has no probe row in any measurement matrix (harness = TYPICAL + HANGAR only; Sc-kernel c-exec-023 re-runs the same two) and is unnamed in the S3/S4 re-entry trigger («вернуть, когда уровни вырастут», undated) and in the pending d-biglevels-tree9-001 options. The real hazard is decision framing, not an unmeasured load class: option 2 (re-sign TREE #9 down to "the fully-fine ceiling the measured hangar number supports") would silently cap big MINES — a TIER-0 archetype riding the same S4 rollup machinery — while being presented to the owner against the hangar archetype he explicitly ranks «второстепенное». Corrections to the original: the hangar fixture is not flat (64×64×48 cells = 24 m tall); the rising-column perf case is bounded by existing gates (flooded-hangar all-active upper bound via Факт-2 volume-neutrality + isotropy, and ADR-0019's designed convergence-to-rest under the persistent bias), so the missing vertical row is a realistic-trajectory gap, not an unbounded blind spot; and vertical-at-scale machinery is named in the deferred plan (S3 routing layers, SPEC §6 п.4). Target-scale (~150-room/~223k-cell) vertical levels fit fully-fine + Sc-kernel; only mid+/tall-beyond-ceiling verticals are unowned. Fix stays as proposed: one additive shaft/stacked-mine RunHangar-style row in the Sc-kernel matrix, and extend the d-biglevels-tree9-001 wording so the S4 re-entry condition and any re-sign-down explicitly name the mine/tall-level archetype.
- **Resolution folded into the package:** Additive, two cheap moves: (1) add a MINE/SHAFT probe scenario to the Sc-kernel c-exec-023 measurement matrix (same harness as 2026-07-02: e.g. a tall stacked-shaft-plus-side-rooms topology of comparable cell count, light gas released at the bottom; record ms/tick + active-set trajectory during the full rise) — one extra RunHangar-style row, no lock touched; (2) when the owner answers d-biglevels-tree9-001, extend the wording so the S4 re-entry condition is keyed to the FIRST designed mine/tall level exceeding the fully-fine comfort ceiling (~200k cells / ~150 rooms), not only to the TREE#9 huge-hangar promise. This makes the TIER-0 archetype the trigger owner instead of a bystander.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «§6 п.3: «Hangar-проба (единственное неизмеренное число всей архитектуры). Что: игрок в открытом зале ~256к клеток @50см»»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-023-draft-call.md` — «done_when 2(c): adversarial «hangar flooded» — ALL ~196k cells active … (this is the SPEC's real worst case)»

### verticality-s3-builds-on-doomed-2band [P3 / PARTIAL] S3 (far vertical strata) as written in the SPEC still targets/extends the interim 2-band code that S4 deletes, ordered BEFORE S4 with Deps: S0 only — the resurrect-the-buried-bands trap is fixed only in a history-not-authority audit

- **Requirement:** R3 + R4 (far vertical routing for mines)
- **Claim:** SPEC §5's S3 entry is stale/ambiguous: it still reads «грубый far-тир получает несколько вертикальных слоёв» with Deps: S0, listed before S4, and the SPEC carries no explicit rule that S3's far vertical strata land on the S4 rollup rather than the interim 2-band CoarseBandStep; the 2026-06-29 audit's «Decide S3-vs-S4 order» PLAN-guard was never folded into the canon and lives in a history-not-authority file. The trap is, however, already guarded elsewhere: the durable product-repo tracker docs/INTERIM-COARSE-FAR-TIER.md forbids building new functionality on the REMOVE-listed code (new dependency = regression), SPEC §6 п.4 frames far vertical strata as a property of the rolled-up (S4) room decided at joint PLAN S3/S4, and S3/S4/S5 are deferred as one bundle to be re-prioritized together at far-tier re-entry. Residual risk = a mis-shaped direction-side PLAN and rework friction at re-entry (the OS-side planner reads only the SPEC), not a resurrection reaching shipped code. Fix stays the same one additive owner-signed banner line at the S3 entry.
- **Resolution folded into the package:** One additive banner line in SPEC §5 at the S3 entry (owner-signed, since the SPEC is G9-signed): «S3 far vertical strata land ON the S4 rollup (or inside the S4 slice), NEVER on the interim CoarseBandStep; at far-tier re-entry S4 shapes first or S3 folds into it». This costs one sentence now, closes the resurrection trap the audit already saw, and weakens no gate.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «§5: «S3 — Высота / расслоение. … Грубый far-тир получает несколько ВЕРТИКАЛЬНЫХ слоёв для маршрутизации по высоте. … Deps: S0.» (S4 lists Deps: S0, S2 — no S3→S4 dependency either way)»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\gas-engine-plan-audit-2026-06-29.md` — «S3 would bolt new height layers onto the 2-band code marked for deletion, and runs BEFORE S4 deletes it [watch, PLAN-guard] — the retired "height-bands" idea quietly grows new responsibilities. Decide S3-vs-S4 order»

### verticality-near-multiroom-unproven [P2 / CONFIRMED] Near-vertical GAMEPLAY is proven only inside a single room: Z-adjacent stacked rooms have zero test fixture (helper is explicitly X-only), the Z-portal opening semantics are exercised nowhere, no vertical level/scene exists, and crouch-under-layer is promised in canon but named on no slice's acceptance

- **Requirement:** R4 (vertical gameplay, MAJOR) + R3
- **Claim:** What is PROVEN: per-type buoyancy COM drift + settle + no-pop inside one room (ScWeightBuoyancy/SettleOracle tests, BuoyancyDemoDirector = three columns in ONE tall sealed 4×4×H room); emergent height at wall sills (RF5 structural). What is NOT proven anywhere: (a) gas rising THROUGH stacked rooms — the canonical fixture states «Only X adjacency is supported in this helper», and StructureVoxelizer's portal Z-band loop (the only path that would open floor/ceiling faces between stacked regions, via wall-door sill semantics) has no test with a Z-adjacent room pair, so multi-room vertical routing rests on an untested seam; (b) crouch-under-layer — SPEC Факт 2 promises it FREE («урон читает Z-клетку головы») but damage does not exist yet and the Sc-damage road card names no vertical/head-Z acceptance; (c) no vertical space exists in any scene (owner-eye: existing levels = «boxy 6×4×6 box / horizontal tube»); TREE done_when #5's fixed scene set names a «vertical shaft pattern» that was never built. If the Z-portal semantics are subtly wrong for stacked pairs, R4 breaks silently while all current gates stay green.
- **Resolution folded into the package:** Additive test/acceptance work, no new slice needed: (1) add a Z-adjacent stacked-room fixture (TwoStackedRooms) + REDs proving a light gas seeded in the lower room rises into the upper room through the Z-band portal and a heavy gas does not — natural home = Sc-kernel's independent-author REDs (it re-touches exactly this tick path) or a small standalone test leg; (2) fold a crouch-under-layer acceptance (head-Z vs feet-Z dose differs in a stratified room) into the Sc-damage PLAN's done_when when it is shaped; (3) when VISUAL resumes, keep c-visual-004's already-agreed «roomy vertical space» demo scene as the owner-eye vertical proof — it is the cheapest path to the TREE #5 'vertical shaft pattern' promise.
- evidence: `C:\projects\Unity\GasCoopGame\tests\GasCoopGame.Core.Tests\Field\Voxel\VoxelTestFixtures.cs` — «Room A: X=[0,3], Room B: X=[4,7]. gapAxis must be 0 (X). (Only X adjacency is supported in this helper.)»
- evidence: `C:\projects\Unity\GasCoopGame\Assets\GasCoopGame\Core\Field\Structure\StructureVoxelizer.cs` — «// 2. Far-tier portals open the FULL sub-face width over their [sill, sill+openingSize) gas-Z band … for (int f = 0; f < VoxelFaces.Count; f++)»

### verticality-strata-count-global-constant-risk [P3 / CONFIRMED] §6 п.4 (vertical strata a rolled-up room keeps) can stay open until S4 PLAN — but nothing records that a FIXED global strata count lies for shafts; parameterize by room height should be pinned as a note now

- **Requirement:** R3 + R4 (far vertical believability in mines)
- **Claim:** §6 п.4 is safely deferred for SCHEMA reasons (collapse is per-layer by шов 4 «НИКОГДА не усреднять колонку в один скаляр», Sc-rep preserved the seam, and the recommendation already says «тюнимый LOD-параметр, НЕ core-константа») — so answering it early is NOT required for mines. The residual gap: the recommendation reads as a single tunable NUMBER. For a mine, one global count (e.g. 2–4 strata) means a 40m shaft rolled up holds 10–20m per stratum — distant gas 'rising a shaft' would jump in building-sized chunks (the same chunky-band lie as the buried 2-band, just N>2). The cheap insight worth recording NOW, while it is fresh, is that the parameter should scale with region height (strata ≈ extentZ / fixed stratum height, clamped), i.e. per-region data, not one global knob.
- **Resolution folded into the package:** Additive one-line amendment to §6 п.4's recommendation (no decision forced, no lock touched): «candidate answer at S4 PLAN: strata count = per-region function of room height (fixed stratum height, clamped), NOT a single global constant — a global count lies for shafts». Decision itself stays at PLAN S4 as scheduled.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «§6 п.4: «сколько вертикальных страт удерживает свёрнутая (rolled-up) комната … Рекомендация: дешёвое в-чексумме число вертикальных слоёв far-тира (тюнимый LOD-параметр, НЕ core-константа). Когда: PLAN S3/S4.»»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «§4 шов 4: «collapse()/expand() … по-слойный (НИКОГДА не усреднять колонку в один скаляр)»»

### verticality-floor-breach-inexpressible [P3 / CONFIRMED] Declared apertures and the S5 breach authoring path are wall-only by schema: ApertureSpec is a (U,Z) wall rectangle and StructureVoxelizer skips Z-normal faces — a floor/ceiling breach (gas pours down a blown floor) is inexpressible without a schema extension nobody has named

- **Requirement:** R4 (vertical gameplay) + destruction canon (root fantasy #4)
- **Claim:** The occupancy DATUM covers all 6 faces (Z boundary faces exist as AllClosed bitmaps), but both authoring paths for openings are wall-shaped: ApertureSpec's coordinates are «inclusive lateral U columns × vertical Z rows» (no in-plane U,V for a horizontal surface), and ApplyAperture hard-skips Z-normal faces («normalAxis == 2 continue — vertical-wall apertures only»). S5 breach = «открыть ЛАТЕНТНЫЕ 25см под-грани (флипнуть occupancy-битмап структуры)» — flipping latent sub-faces that today can only be declared on walls. So the signature vertical destruction beat for a mine (breach a floor, gas cascades down onto players below) cannot be authored. Partial vertical connections (hatches) have a workaround — model the shaft as its own narrow region with a full-boundary Z-band portal — but breach does not. S5 is deferred, so this is a naming gap, not a build defect; left unnamed, the S5 shape would discover it late.
- **Resolution folded into the package:** Name it on the S5/destructibility shape agenda now (one additive line on the S5 entry or the §6 п.8 destructibility node): «S5 shape must decide floor/ceiling breach — requires extending the aperture/occupancy authoring schema to Z-normal faces (data model already carries them); wall-only S5 is a scope cut needing owner ack per the deliverable-exists rule». Until then, level design uses the shaft-as-region workaround for vertical openings.
- evidence: `C:\projects\Unity\GasCoopGame\Assets\GasCoopGame\Core\Field\Structure\StructureVoxelizer.cs` — «if (normalAxis == 2) continue;                 // vertical-wall apertures only (X/Y normal)»
- evidence: `C:\projects\Unity\GasCoopGame\Assets\GasCoopGame\Core\Field\Structure\ApertureSpec.cs` — «SubFaceMinU..SubFaceMaxU = inclusive lateral 25 cm sub-face columns along the boundary's in-plane horizontal axis. SubFaceMinZ..SubFaceMaxZ = inclusive vertical 25 cm sub-face rows»

### verticality-temp-buoyancy-wiring-tension [P3 / CONFIRMED] ADR-0019 promises «Sc-damage wires the temperature term later with no schema change», but temperature→gas feedback is owner-deferred (d-tempfeedback-001) and lock-adjacent — the Sc-damage PLAN could silently advance a deferred owner decision

- **Requirement:** R4 (vertical believability: thermal inversion) + standing feedback (locks reopen only signed)
- **Claim:** Cross-band/thermal inversion status: the near buoyancy law reserved the far-tier temperature term zero-stubbed, and ADR-0019 says Sc-damage «wires it later with no schema change». But wiring T into buoyancy IS the temperature→gas feedback that the owner deferred (d-tempfeedback-001; TREE #10: «REVERSE temperature→gas FEEDBACK is DEFERRED … NAMED here, not silently dropped») and that SPEC §4 шов 7 marks lock-adjacent (committed-revision cross-layer read; «отложенная temp→gas обратная связь вскрывает лок»). Two authoritative texts point opposite ways for the same slice. Vertical believability does NOT need it near-term: weight-typed gases already cover the mine archetypes (heavy blackdamp settles, light firedamp/smoke rises), and the per-species-temperature seam is protected as a TREE #11 KEEP-OPEN invariant — so the right move is a guard, not a schedule change.
- **Resolution folded into the package:** One additive guard line on the Sc-damage road-card entry (character-road-shape §road item 5): «Sc-damage builds temperature as a SINK layer only; wiring T into the ADR-0019 buoyancy term = the deferred temp→gas feedback (d-tempfeedback-001) — needs its own owner-signed decision + committed-revision read design, NOT silently included». Cheap, additive, prevents an accidental soft-reopen at the Sc-damage PLAN.
- evidence: `C:\projects\Unity\GasCoopGame\docs\adr\ADR-0019-sc-weight-per-type-buoyancy.md` — «The temperature term −(T >> shift) is RESERVED / zero-stubbed (T = 0 this slice), keeping the far-tier law shape so Sc-damage wires it later with no schema change.»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «§4 шов 7: «1-тик лаг и есть причина, почему отложенная temp→gas обратная связь вскрывает лок»; §5: «temp-ключевая типизация ждёт отложенной temp→gas обратной связи (d-tempfeedback-001) — сокет кладётся, temp-строки СПЯТ»»

### multiplayer-01-desync-halt-has-no-player-story-or-recovery [P2 / CONFIRMED] Desync/halt = «громкая остановка» has no player-experience design and no recovery path anywhere on the plan; with Факт-5 (no mid-raid join) any halt = whole raid lost for all peers

- **Requirement:** R6 (player never notices problems); interacts with Факт-1 (desync = loud halt) and Факт-5 (no late-join)
- **Claim:** The planned model's ONLY answer to desync is a fail-fast throw (LockstepDivergenceException) — correct as a dev-time correctness tool, but the plan nowhere defines what a PLAYER experiences (freeze? error screen? return to lobby?), has no session-recovery/checkpoint decision, and no desync-report artifact for post-mortem. Because Факт-5 forbids mid-raid join («позже — может быть», undated), any loud halt — a determinism bug, the >4-type LOUD THROW, or a crash — ends the raid for ALL peers with zero re-entry. Grep across live/** and docs/** finds no slice, decision, or backlog item owning halt UX, recovery, or desync telemetry. The MeaningMembers per-member checksum mask (ADR-0017 Decision 2) is exactly the substrate a cheap desync-report needs — it exists but nothing schedules wiring it into a player-facing/ops artifact.
- **Resolution folded into the package:** Additive, no lock touched: add a named «halt/desync player experience + recovery» row to the d-demo-road-001 July shape session (already queued in NOW.decisions): (1) minimal halt UX (loud stop → readable error → return to lobby/base, honoring Факт-5's clean base↔raid session boundary); (2) an owner decision on raid-boundary checkpointing (what, if anything, survives a halted raid); (3) a desync-report artifact built on the existing ADR-0017 MeaningMembers mask (tick + first-divergent member) so field desyncs are debuggable. None of this reopens ADR-0002 — it consumes it.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «line 32: «рассинхрон = громкая остановка» — no further player-side treatment anywhere in the SPEC»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «line 52: «Факт 5. НЕТ позднего входа (late-join)… вход в середине рейда запрещён (позже — может быть)»»

### multiplayer-02-host-resilience-obligation-orphaned [P2 / CONFIRMED] HOST-RESILIENCE is an owner-signed TREE done_when obligation («host can leave/crash without ending the session», #11) whose carrier (Wave-A spike) was retired — no surviving slice owns it, and no peer-drop policy exists at all

- **Requirement:** R6; TREE g-9c41 done_when #11 (owner-approved G9)
- **Claim:** TREE #11 names host-resilience «a named core obligation… (§H — spiked in Wave A)», but the Wave-A/B plan is retired (NOW kill_by: «the old ‘Waves A→B’ wording is retired») and the spike never ran — history shows host-migration as «the top open chunk» (s-review-002) that was to be «a named spike», then vanished from the character road (Sc-types→weight→rep→kernel→reactions→damage) and from the deferred S3/S4/S5. The SPEC carries only the unproven assertion «Миграция хоста почти бесплатна» (Факт-1). Worse, the lockstep input-complete barrier (SimInstance.cs:38 returns null until all inputs arrive) means ANY peer drop — not just the host — stalls every peer indefinitely: no drop-peer/continue-without/pause policy is designed anywhere. Player-visible outcome today-as-planned: one player's crash or quit freezes or kills the session for everyone, violating a signed done_when. This is the same silent-canon-drop class as d-biglevels-tree9-001 (TREE #9).
- **Resolution folded into the package:** Additive: queue a NOW.decision (pattern of d-biglevels-tree9-001) — either (a) re-pin TREE #11 to a named carrier: the host-migration/peer-drop spike becomes a mandatory row of the d-demo-road-001 July shape (lockstep genuinely makes host migration cheap — any peer has full state — but «cheap» must be spiked, and the peer-drop policy [pause-with-UI vs continue-without-inputs vs end-raid] is an owner design call), or (b) explicitly re-sign #11 down for the demo (host quits = raid ends, stated honestly). Never let the obligation keep floating on a retired wave.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\TREE.md` — «done_when #11 (lines 137-138): «HOST-RESILIENCE is now a named core obligation: a host can leave/crash without ending the 4–8 co-op session (§H — spiked in Wave A)»»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\NOW.md` — «kill_by (lines 67-68): «the old «Waves A→B» wording is retired» — the wave that carried the §H spike no longer exists; next_slices list no host/peer-drop item»

### multiplayer-03-no-dated-path-to-real-networked-gas [P2 / CONFIRMED] R6 has no dated path: gas is loopback-proven only, gas-as-ITickInputBus-citizen is «a bigger seam ~S4» (S4 deferred, undated), Steam transport is YELLOW-deferred/uncertified, and real-link latency/loss has never been claimed — while the Oct-5 target is a networked co-op demo

- **Requirement:** R6; TREE g-9c41 done_when #3 (2+ networked clients see consistent gas)
- **Claim:** Every player-noticeable multiplayer property lives exactly in the undated items: (1) the real gas field has never ridden the real FishNet bus (SPEC honestly flags it; the only real-hop test was the 8x8 toy SimCore in ADR-0002); (2) the shipping transport (FishySteamworks — needed for friends-over-internet NAT traversal) is YELLOW-deferred, uncertified against pinned FishNet 4.7.2/Unity 6, last released 2024; (3) input-delay feel, jitter/stall behavior under real links is explicitly not claimed by any artifact; (4) TREE done_when #3 has no carrier slice. The 2026-07-02 review's d-demo-road-001 already names the demo-road hole broadly; the R6-specific delta is that the NETWORK ladder (gas-on-bus → input-delay/stall UX → Steam-transport certification → real-link soak) is not enumerated anywhere, so a July shape session could sketch a «demo road» that still omits it. R6 cannot be evaluated at all — let alone hold — until these land; nothing dates them.
- **Resolution folded into the package:** Additive, folds into the already-queued d-demo-road-001 July shape: make the network-integration ladder MANDATORY named rows of that shape — (i) gas-on-real-bus slice (the ~S4 seam pulled forward as its own netcode slice, independent of the S4 rollup: the seam is bus citizenship, not big-hall LOD — they are separable); (ii) input-delay/stall feel evaluation on a real link (owner-run, two machines, per the owner-run-gate rule); (iii) Steam-transport certification leg (or an explicit demo decision to ship direct-IP/Tugboat); (iv) TREE #3 re-pinned to whichever slice carries it. If the ladder does not fit before Oct-5, that is a conscious re-date — the review's named failure mode otherwise.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «line 126: «⚠ Доказан LOOPBACK; газ ещё НЕ в реальной сетевой шине инпутов (gas-as-ITickInputBus-citizen = бóльший шов ~S4)»; line 132: «Far-тир S3/S4/S5 ОТЛОЖЕНЫ» (undated)»
- evidence: `C:\projects\Unity\GasCoopGame\docs\adr\ADR-0002-net-determinism-lockstep-fishnet.md` — «lines 134-141: «Axis 4 — Steam transport (shipping): YELLOW-deferred (FishySteamworks uncertified vs FishNet 4.7.2/Unity 6 — a later integration leg)» and rubric line 114: «FishySteamworks/FishyFacepunch last released 2024»»

### multiplayer-04-weakest-peer-budget-has-no-target-no-passfail-no-degradation-policy [P2 / PARTIAL] The weakest-peer premise (Факт-1: scale limit = weakest peer's CPU) has no defined weak-CPU target, the flooded-hangar worst case is measure-and-record only (not pass/fail), and there is NO runtime degradation policy — the planned behavior when a peer falls behind is a loud tick-drop failure

- **Requirement:** R6; Факт-1 weakest-peer premise; TREE done_when #1 (min-spec)
- **Claim:** Three related gaps around the weakest-peer premise (Факт-1), each verified but weaker than originally framed: (1) no weak-CPU class is named anywhere (TREE #1 min-spec lists only GPU/RAM; SPEC §6 п.3 defers the weak-hardware budget/threshold fork to the Sc-reactions shape, AFTER Sc-kernel's flooded-hangar capture) — the flooded worst case will be bracketed only by the rough documented ×2-4 proxy against the 50-100 ms budget, not by a pinned target; this is an owner-signed recorded deferral, but its ordering means the SPEC's own named worst case is measured before the threshold that judges it exists. (2) Sc-kernel gate 2c mandates measure-and-surface («no silent pass») but binds no failure branch: an over-budget flooded number can be recorded while the slice closes GREEN, with the owner decision left implicit in the later reactions-shape fork and the adjacent d-biglevels-tree9-001 rather than explicitly queued off gate 2c's outcome. (3) No live artifact owns runtime degradation for a peer that cannot hold budget — GAP-4 per-region tick_divisor is a stub bound to nothing, degradation ladders exist only in frozen archive docs, and the only implemented behavior is ADR-0002's loud tick-count assert (a test false-green guard, not a session policy); under lockstep one over-budget peer slows/fails the session for everyone. Mitigating context: real networked gas sessions are deferred to ~S4, so leg 3 is a plan-naming gap, not an imminent defect. Cheap additive fix at the already-scheduled owner-present c-exec-023 framing: pin a named weak-CPU proxy, bind gate 2c's over-budget outcome to an explicit queued owner fork, and name the runtime-degradation question as a mandatory row of the reactions-shape weak-hardware fork.
- **Resolution folded into the package:** Additive, at c-exec-023 framing (owner present, already scheduled as the very next step): (1) fix a recorded weak-peer CPU proxy NOW (a named CPU class + the documented ×2–4 rule) instead of leaving the denominator to the later reactions shape — the flooded-hangar number is meaningless without it; (2) convert gate 2c's outcome into an explicit decision point: IF flooded-hangar × weak-proxy > budget THEN a queued owner fork (level-design cap on simultaneously-active volume / S4-rollup re-entry / tick-rate policy) — never a silently recorded overrun; (3) name the runtime-degradation question (what a live session does when a peer exceeds budget: slow-all with a «waiting» UX vs loud stop) as a mandatory row of the reactions-shape weak-hardware fork. All sim-side; ADR-0002 untouched.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-023-draft-call.md` — «lines 45-47: «(c) adversarial «hangar flooded» — ALL ~196k cells active: measured and recorded vs the 50–100 ms budget with the weak-peer ×2–4 caveat … no silent pass» — record-and-surface, no pass/fail threshold or failure-branch decision»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «line 152: «целевое слабое железо (форк — ставит бюджет/порог) решается на shape слайса реакций» + «cost ∝ active fronts» ЛОЖНО (стена возвращается у игрока в ангаре…)»; line 32: «Предел масштаба = CPU самого слабого пира»»

### multiplayer-05-player-position-determinism-unowned-before-sc-damage [P2 / CONFIRMED] Sc-damage (dose/HP — «the first real gameplay payoff») requires cross-peer-consistent player position, but the SPEC's own prerequisite («ОТДЕЛЬНЫЙ хеш player-kinematics… решить ДО первой силовой механики», §4 шов 7) is pinned to no slice and absent from the Sc-damage shape line

- **Requirement:** R6; SPEC §4 шов 7; §3 «Урон/контакт/доза читаются… из грубого авторитетного поля на каждом пире»; §9.5 checksum includes «дозу лута (D6)»
- **Claim:** Dose is computed per-peer from the coarse field at the player's occupied cell. Player movement will live in Unity (float character controller) — not in the integer sim — so two peers can disagree about which Z-cell/room a player occupied on the dose tick. Then either (a) dose/HP is folded into the checksum (D6 loot-dose already is) → routine desyncs (loud all-peer halts) from ordinary movement, or (b) HP stays off-checksum → peers silently disagree about who is dying — for R6 the worse outcome, because it is invisible drift the halt machinery cannot catch. The 2026-06-29 plan audit already flagged the skipped decision as «[watch, reserved-danger] … becomes a blocker the moment a mechanic ties a gas-push to a player BODY», but the character road's Sc-damage entry is one sentence («Dose-from-coarse (gas hurts; type-specific); temperature as a sink-layer») with no player-kinematics item, and no NOW.decision or CALL carries шов 7. Rolling-wave shaping means nothing forces it onto the Sc-damage PLAN agenda.
- **Resolution folded into the package:** Additive (same pattern as d-coop-interdependence-repin-001, which was successfully folded into the Sc-reactions/Sc-damage PLANs): pin SPEC шов 7 to the Sc-damage PLAN as a MANDATORY owner-present agenda row — decide the player-position agreement model before any dose code reads a position (candidate designs to put to the owner: quantized position rides the input bus as per-peer-authoritative data — the standard friends-coop lockstep answer, anti-cheat already conceded by §6 п.7; vs deterministic integer movement inside the sim — note the own-input latency cost; vs off-checksum HP with an explicit consistency argument). Record the choice in the Sc-damage CALL so a build session cannot inherit the gap silently.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «line 111: «…+ ОТДЕЛЬНЫЙ хеш player-kinematics (хеш газа не ловит движение игрока) — для будущих силовых механик; решить ДО первой силовой механики»; line 88: «Урон/контакт/доза читаются ТОЛЬКО из грубого авторитетного поля на каждом пире… (доза = время-интегрированное грубое событие из резидентного …»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\character-road-shape-2026-06-29.md` — «line 19: «Sc-damage — DAMAGE / TEMPERATURE. Dose-from-coarse (gas hurts; type-specific); temperature as a sink-layer.» — no player-position/kinematics determinism item»

### multiplayer-06-tier3-lock-conflation-misroutes-a-future-r1-vs-r6-decision [P3 / CONFIRMED] The canon frames TIER-3 (fixed-point velocity / true inertia) as «вскрывает лок ADR-0002», conflating the NETCODE lock with the no-stored-velocity sim boundary — integer fixed-point velocity is lockstep-compatible, so a future R1-richness escalation would needlessly relitigate R6's foundation

- **Requirement:** R6 (e-question: what happens to multiplayer if R1 later demands TIER-3); SPEC §1 lock discipline
- **Claim:** ADR-0002's actual content is input-lockstep + integer-only determinism. A fixed-point (two-integer) velocity field satisfies integer-only by construction — the richness research itself cites Photon Quantum/StarCraft/FixPointCS («реально, проверено») — i.e., deterministic-lockstep engines that DO store velocity. TIER-3's real reopen surface is: the S1 «НЕ хранимая скорость» boundary (ADR-0012-s1/§9.5), new replicated checksum members, and weakest-peer CPU/memory cost — all sim-representation and budget matters, none of them the netcode. As written («это резерв TIER-3, вскрывает лок ADR-0002»), a future owner-eye verdict «затухающий bias недостаточно живой» (the ARMED re-trigger at the combat slice) would be routed into «we must reopen the network lock», either scaring the team off a legitimate richness tier or spuriously putting lockstep itself on the table. R6 is not actually hostage to TIER-3; the canon text says it is.
- **Resolution folded into the package:** Additive one-paragraph SPEC clarification at the next owner-present SPEC touch (G9, since it edits the canon): separate the two locks by name — ADR-0002 (netcode: input-lockstep + integer determinism) vs the no-stored-velocity representation boundary (S1/ADR-0012-s1) — and state that TIER-3, if ever triggered, reopens the LATTER via a signed decision (plus new checksum members + a weakest-peer re-measure), while input-lockstep survives an integer TIER-3 unchanged. Cheap now; prevents a mis-routed future relitigation of R6. (Same citation-drift class the SPEC §1 ADR-0010→ADR-0002 поправка already fixed once.)
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «line 74: «НЕ истинная инерция/coasting (это резерв TIER-3, вскрывает лок ADR-0002 — d-gas-richness-tiers-001)»»
- evidence: `C:\projects\Unity\GasCoopGame\docs\adr\ADR-0002-net-determinism-lockstep-fishnet.md` — «Decision 2 (lines 42-47): «Integer-only deterministic core. No float/double…» — fixed-point velocity (two integers) complies; nothing in the ADR forbids stored state, it forbids nondeterminism»

### types-mixing-1 [P2 / PARTIAL] K=4 co-residency cap is insufficient for the mixing fantasy; the named fork's default resolution path (constrain levels to <=4 types per zone) silently under-delivers R8 — mass-conserving graceful degradation must be the primary branch

- **Requirement:** R1 (believable, gameplay-interesting mixing near the player) + R8 (many types, interactions)
- **Claim:** K=3 overlay (max 4 co-resident types) with >K = LOUD THROW is confirmed reachable on the normal tick path (VoxelFaceFlow clamps per-plane only; CommitStep→ReplaceFromPlaneMassesValidated→SparseDominantCellRecord.AddMass:142; SeedMass likewise) = deterministic all-peer lockstep halt — but this is a known, owner-signed interim policy (2026-06-30 fork: LOUD THROW, mass-conserving) whose resolution is already scheduled owner-present at the c-exec-021 shape (banner item 13), with an interim ship-block and the c-exec-023 draft explicitly deferring K/overflow to that fork. The surviving defects to fold into that shape: (1) banner item 13(b)'s RED is worded "proving the throw is unreachable in the intended level + chemistry design" — satisfiable by constraining levels/chemistry to <=4 types per zone, an R8 scope-down vector analogous to the TREE-#9 "smaller levels" fallback NOW.md warns against; re-word it as "the sim survives a 5-type junction mass-exactly (or degrades by an owner-signed mass-conserving policy)", never "levels never produce it". (2) The shape's candidate list should include the mass-conserving branches the audit derives (reaction-priority consumption; admission clamp extending the existing destinationCapacity mechanism so a full cell presents headroom 0 for an unmatched type; K raised via config) alongside the named merge/raised-K/declared-sink. (3) At the shape, cite the owner's 2026-07-02 R8 statement as the "owner data" ADR-E-0001 explicitly reserves for revisiting K (the ADR does not weld K — it names the revisit path). K=4's practical tightness (3-way junction + reaction product + player-thrown gas; typing flip fronts; fork-2 residue) is real design pressure the shape must weigh against R1/R8.
- **Resolution folded into the package:** At the c-exec-021 shape (owner present, additive — the Sc-rep LOUD THROW stays as the invariant assert for non-flow paths), adopt a LAYERED mass-conserving policy and make it, not level-constraint, the primary branch: (1) REACTION-PRIORITY CONSUMPTION first — before overflow resolution, if any co-resident subset matches a reaction rule, fire it this tick (products may free lanes); believability HIGHEST: 'too many gases meeting = chemistry happens' turns 5-type junctions into designed set-pieces instead of crashes; only partial (inert quintets remain). (2) Universal floor = ADMISSION CLAMP at the face: extend the existing per-cell destinationCapacity clamp (VoxelFaceFlow.cs:202-227) — a destination with no matching lane and no free lane presents headroom 0 for that type; mass stays in the source cell (conserved by the same mechanism that already conserves clamp-denied units), no cascade, no throw, deterministic, integer; same admission rule for SeedMass (grenade blooms to nearest admissible cells in canonical order). Believability cost LOW: reads as saturation/back-pressure — a saturated mixture refuses new gas, the thrown cloud pools around the junction. (3) Optionally raise K via config (record grows 8->12 ints; cite the owner R8 requirement as the ADR-E-0001 revisit data; K becomes config like roster so future raises are data not rework). Rank the other two candidates BELOW these: merge-into-nearest-axis conserves mass but risks phantom reactivity (inert merged into fuel) unless gated by an axis-legality table; smallest-mass eviction to a neighbor reads as displacement but cascades in saturated regions (needs bounded depth + fallback to the clamp). Add the adversarial 5-type junction RED as: 'the sim survives it mass-exactly', not 'levels never produce it'.
- evidence: `C:\projects\Unity\GasCoopGame\Assets\GasCoopGame\Core\Field\Voxel\SparseDominantCellRecord.cs` — «line 142: throw new InvalidOperationException("sparse-dominant cell cannot hold more than four positive co-resident types"); — reached from AddMass on the normal path»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-021-call.md` — «banner item 13: «>K = LOUD THROW is wired into the NORMAL tick path — under lockstep that is a deterministic ALL-PEER session halt reachable by ordinary gameplay mixing (5 types meeting at a junction), in a game whose core fantasy is mixing … (b) an adversarial RED: a 5-type junction scenario provin…»

### types-mixing-2 [P3 / PARTIAL] Banner-mandated AXES-only reaction schema caps per-pair character: types sharing axis coordinates become chemically identical; explicit reactant-set DATA overrides must be explicitly re-permitted at the shape

- **Requirement:** R8 («разное поведение, взаимодействуют между собой»)
- **Claim:** c-exec-021 internally contradicts itself on the reaction schema: banner item 2 («AXES …, NOT type-pairs», with the banner-wins preamble, and schema absent from banner item 8's open-fork list) pre-empts the decision that the body's done_when #1(i) explicitly assigns to the owner-present shape (explicit reactant-set rules vs axes). No signed decision resolved this: the s-reshape owner sign was a bulk «беру рекомендации» in which schema was not a named fork, and the G9-signed canon SPEC (Факт-4) still words chemistry as a type-keyed integer collision table, forbidding only pair-keyed CODE branching — explicit reactant-set DATA rows are canon-legal. If the future shape reads the banner strictly, that canon-legal option is silently dropped and bespoke per-pair chemistry becomes expressible only via indicator/split axes (an authoring-economy erosion, not a hard expressiveness cap — and the banner's own item 5 keeps per-TYPE response-table DATA for waves, so type-keyed data is not blanket-forbidden). R8 (unranked) is not itself endangered: inter-type variety survives via env bands, per-type wave responses, and env-derived dynamic typing. The defect is a drift/ambiguity hazard on the mainline slice's governing CALL that banner item 14's mandated body rewrite does not name specifically; fix = one recorded additive line at the shape (axis rules as default tier + optional explicit reactant-set override DATA rows, precedence explicit-set > axis, both folded into the session handshake hash via the already-specified two-level canonicalization — preserving the no-pair-dispatch crux and the NEW-TYPE-IS-DATA RED unchanged).
- **Resolution folded into the package:** One recorded line at the c-exec-021 shape (additive, no lock touched): schema = AXIS rules as the default tier + OPTIONAL explicit reactant-SET override rows, both pure DATA applied by the same generic engine (precedence: explicit-set > axis; deterministic tiebreak), both folded into the session handshake hash using the already-specified two-level canonicalization (reactant set sorted per rule, rules stable-sorted, count folded). This preserves the crux exactly (a new type still = 0 code branches; the NEW-TYPE-IS-DATA RED still binds) while keeping per-pair signature chemistry authorable. Also consider the cheap synergy: product = 'parent substance re-typed by local env' (rides the env-derived typing socket) as a data-declared product kind — it multiplies outcome variety without more rules.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-021-call.md` — «banner item 2: «Reaction schema → AXES (fuel↔oxidizer, cold↔heat, caustic↔inert, …), NOT type-pairs — a new gas of any tier adds ZERO new rules (O(axes))» vs body §the model #1: «Whether the schema is explicit reactant-set rules OR per-type reagent-class axes combined by a fixed function is the PLAN…»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\gas-reaction-typing-design-2026-06-30.md` — ««A rule = (predicate over reactants' axis coords + optional env band) → (outcome kind, integer consume ratio rA:rB, optional product type C, per-tick rate cap)» — one product per rule»

### types-mixing-3 [P2 / CONFIRMED] Env-derived dynamic typing collides with byte-sleep/active-front: no wake-source seam for 'environment changed under slept gas' is named anywhere, and Sc-kernel is about to pin the active-set design without it

- **Requirement:** R8 (env-derived typing is a signed core requirement, SPEC §5) + R1 believability («зеркало среды»)
- **Claim:** SPEC §5 requires the per-cell exposure accumulator to integrate env-vs-threshold EVERY tick, while SPEC §3 and Sc-kernel make only the ACTIVE front compute ('Спящий насыщенный газ хранится байт-сном … считается только активный ФРОНТ'). Density/neighbour-keyed conditions self-wake (any mass change is activity), but the flagship condition — temperature (deferred, d-tempfeedback-001) — changes the committed env-vector WITHOUT any gas flow: a slept saturated room that heats up has no mechanism to re-enter the active set, so its gas never accumulates exposure and never flips (the lore promise breaks: gas ignores its environment exactly when the player heats a room to convert it), OR the typing slice must sweep all gas-bearing cells per tick — re-introducing the domain-cells cost axis Sc-kernel exists to kill (the 587ms lesson in a new coat). c-exec-023's PLAN item decides 'active-set/front data structure + canonical iteration order + how dormant saturated gas byte-sleeps' with NO mention of a non-flow wake source; if the kernel's wake sources are welded to mass-flux only and locked behind byte-identity goldens, retrofitting an env-delta wake later is exactly the large-rework class P2 names.
- **Resolution folded into the package:** Additive one-liner at c-exec-023 FRAMING (before the kernel shape is pinned): the active-set design MUST name a reserved WAKE-SOURCE seam — beyond mass-flux/impulse wake, an enumerable hook where a committed-revision env-delta event (per region or cell, published by future layers like the temperature sink) enqueues gas cells into the active set. Reserved-not-built (zero behavior now, mirrors the §9 shov discipline: schema decision now, not a migration on live nodes). At the typing slice, temperature rows stay dormant until d-tempfeedback-001 lands, then drive wake through this seam. On the open typing fork: recommend ACCUMULATE-with-hysteresis (matches the owner lean) over instant flip for believability — instant flip strobes at noisy env boundaries (mass oscillates A↔C every tick where the env-vector hovers at threshold → visual flicker + active-set inflation + checksum churn); hysteresis makes conversion read as gradual and telegraphed, and 'identity moves mass / regime is a derived read' stays clean.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «§3 (line 64): «Спящий насыщенный газ хранится байт-сном (lossless), считается только активный ФРОНТ» + §5 (line 142): «per-cell exposure-аккумулятор integrates env-vs-порог по тику … env-вектор … читается на COMMITTED-ревизии (1-тик лаг)»»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-023-draft-call.md` — «done_when #1: «DECIDE+RECORD: active-set/front data structure + canonical iteration order (deterministic, pinned); how dormant saturated gas byte-sleeps (Факт-4)» — no env-delta wake source named»

### types-mixing-4 [P3 / CONFIRMED] Condition-wave × wind composition unspecified: gas advected across a capped marching front can tunnel through unconverted or double-convert on re-entry — the wave fork lacks a moving-gas oracle

- **Requirement:** R8 (interactions/waves) + R1 believability
- **Claim:** The condition-wave design is a marching coarse front with a per-tick cap; S1 wind bias moves gas through faces every tick, independently of the wave's march rate. Nothing in the design doc or the c-exec-021 banner (item 5 covers only the propagation CHANNEL/machinery question) specifies what happens when gas moves relative to the front: gas blown forward can cross ahead of the capped front between wave steps and never receive the tag (unconverted pockets past a 'wave that converts everything'), and gas circulating back into already-passed cells can be converted twice if the tag lingers or not at all if it expired. Every outcome is deterministic under lockstep — the hazard is purely believability: arbitrary striping/holes in the signature 'волна, по которой газ меняется, каждый тип по-своему' moment, discovered only at owner-eye after the machinery is built.
- **Resolution folded into the package:** Fold two additive lines into the wave fork at the c-exec-021 shape: (1) semantics decision — tag-on-REGION-until-expiry (cells evaluate gas-present × tag-present at committed revision; conversion idempotent per (cell, wave-id) with the wave-id in the event payload) vs tag-on-front, decided explicitly; (2) an owner-eye-backed RED oracle: gas advected across a marching front under standing wind (S1 vent) converts EXACTLY ONCE — no tunneling past the front, no double conversion on recirculation. Cheap to state now; expensive to discover as a look bug after the promo-event substrate is built.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\gas-reaction-typing-design-2026-06-30.md` — ««Cost = a marching front with a per-tick cap (spread over ticks, not one frame)» — no relative-motion (gas vs front) semantics»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-021-call.md` — «banner item 5 addresses only the channel: «The reactions shape MUST decide which machinery carries it and verify FIRST-HAND whether a propagating promo-event substrate exists or must be built» — nothing on advected gas crossing the front»

### types-mixing-5 [P3 / PARTIAL] Capacity/back-pressure law is per-TYPE by construction: co-resident types never compete for cell capacity (a 4-type cell holds 4×MaxCellMass), so cross-type displacement/saturation can never emerge without a flow-law change — unrecorded as a seam

- **Requirement:** R1 mixing believability + R8 (interactions through the grid)
- **Claim:** The near-tier flow clamp computes destination headroom per species plane (VoxelFaceFlow.cs:207-209 inside the per-species loop) and the sparse record caps each LANE at MaxCellMass (SparseDominantCellRecord.cs:178-184; TotalMass guarded only against int range) — so by construction co-resident types never compete for cell capacity (a 4-type cell holds up to 4×MaxCellMass), gases interpenetrate with zero mutual resistance, and cross-type displacement/back-pressure/mixture-saturation semantics cannot emerge without a flow-law change that touches every golden. The per-plane-cap FACT is recorded once (c-exec-021-call.md's reaction-overflow hardening: "MaxCellMass is a PER-PLANE per-(species,cell) cap") but only as a reaction-product throw hazard — the canon SPEC's capacity law (§3 d-roomfull-001, §6.1 cap open item, GAP-1 fields) is species-blind and nowhere names the cross-type TOTAL-capacity variant as a seam; the far tier's capacity law is meanwhile total-mass-based (openspec sim-core spec: total sector mass vs SectorCapacity), so near/far capacity semantics silently diverge at the future S4 rollup. NOTE: d-displacement-s1b-cut-001 is NOT evidence here — S1b выдавливание = BODY-dams-gas displacement with a named re-entry trigger and a recorded per-cell-capacity dependency (capacity-pressure-2026-06-27.md), orthogonal to per-type-vs-total. Resolution stands minus the S1b rhetoric: add one additive SPEC §6 OPEN line naming per-type-capacity-by-construction and the cross-type TOTAL-capacity variant (reserved GAP-1 cap fields) as a named flow-law seam, to be decided no later than the Sc-reactions/Sc-damage shape (where mixture-total semantics first get consumers).
- **Resolution folded into the package:** Record it, don't build it: add one line to SPEC §6 (OPEN list, additive) — «capacity/back-pressure is per-type by construction (per-species clamp + per-lane cap); a cross-type TOTAL-capacity variant (enables displacement/выдавливание and mixture-saturation semantics) is a named flow-law seam using the reserved GAP-1 cap fields, to be decided no later than the Sc-damage PLAN». This keeps the d-displacement-s1b-cut-001 cut honest (cut-with-a-door, not cut-and-welded) and gives finding types-mixing-1's admission-clamp option a recorded footing.
- evidence: `C:\projects\Unity\GasCoopGame\Assets\GasCoopGame\Core\Field\Voxel\VoxelFaceFlow.cs` — «lines 207-209 (inside `for (int s = 0; s < speciesCount; s++)`): Array.Copy(oldMass, remaining, cellCount); for (int c = 0; c < cellCount; c++) destinationCapacity[c] = VoxelField.MaxCellMass - oldMass[c]; — oldMass is the per-species plane»
- evidence: `C:\projects\Unity\GasCoopGame\Assets\GasCoopGame\Core\Field\Voxel\SparseDominantCellRecord.cs` — «lines 181-182: AddChecked throws only when a single lane exceeds MaxCellMass — TotalMass across 4 lanes is guarded only against int range (line 38)»

### types-mixing-6 [P3 / CONFIRMED] Sc-kernel's roster-independence gate stops at roster-64 while the advertised config ceiling is 128 — the one number the fork keeps citing stays extrapolated, though post-kernel a 128 row is cheap

- **Requirement:** R8 (many types at the ~128 roster) 
- **Claim:** The 2026-07-02 measurement could not run roster-128 ('64 was the largest that fit comfortably in this harness's process memory') and flags 1.16s as extrapolation; the Sc-kernel draft then binds its roster-independence gate at '1 vs 64' and re-runs the matrix at '1/4/16/64' — the GasRosterConfig default ceiling (128) is never exercised even after the kernel removes the very dense planes that caused the memory constraint. The scaling argument says 64→128 is structurally safe post-kernel, but the plan's own discipline (hangar 'single unmeasured number' lesson, review finding 3: a stand-in number let a vacuous gate stand) argues for measuring the advertised ceiling once it costs nothing.
- **Resolution folded into the package:** At c-exec-023 framing (one word of diff): extend the measurement matrix and the roster-independence gate to include a roster-128 row (1 vs 128), since the sparse kernel removes the roster-sized planes that made 128 unrunnable. Closes the last extrapolated number on the R8 cost story.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\c-exec-023-draft-call.md` — «done_when #2(a): «roster-independence — hangar ms/tick at roster 1 vs 64 differs by ≤ ~10-20% (was 32.5×)»; #3: «TYPICAL + HANGAR × roster 1/4/16/64»»
- evidence: `C:\projects\Unity\GasCoopGame\docs\measurements\sc-rep-hangar-real-measurement-2026-07-02.md` — «STOP: «Roster=128 (the architecture's actual target ceiling) was not run — 64 was the largest that fit comfortably in this harness's process memory … this is an extrapolation, not a measurement»»

### words-vs-docs-001-tree9-conflates-tier0-r2-with-major-r5 [P2 / PARTIAL] TREE #9 + the undated far-tier deferral conflate TIER-0 R2 (global sim, coarser far) with MAJOR R5 (huge sizes, squeeze-licensed); pending decision d-biglevels-tree9-001 offers no ladder-consistent option

- **Requirement:** R2 (TIER-0: global simulation across the WHOLE space, may be coarser far away) + R5 (MAJOR: no hard size limits, owner accepts squeezing «тут мы можем как-то уместиться») + R3 (TIER-0: mines/shafts, tall vertical levels)
- **Claim:** Pending decision d-biglevels-tree9-001 is under-decomposed against the owner ladder: option 2 ("re-sign TREE #9 down to the fully-fine ceiling") as worded would drop BOTH the R5-squeezable numeric profile (200×200×40m, ≥1000 volumes) AND the only done_when line requiring the coarse far tier to be designed and arithmetic-validated — without stating the cross-doc consequence that SPEC §6 п.2's own canonical target (~150 rooms ≈ 223k cells) already exceeds the comfortable weak-core ceiling (~200k) and declares S4 room-rollup обязателен, so a fully-fine-ceiling re-sign implicitly re-signs the SPEC target scale too. A third middle option should be added (keep the coarse-tier/S4 machinery with a dated re-entry condition, incl. the SPEC §6 п.4 vertical-strata question for R3/R4 at scale; re-sign only the numeric profile down as an explicit R5 squeeze). This is a decision-framing completeness gap, not a TIER-0 break: R2 mandates global coverage (coarser-far is a permission, satisfied fully-fine within the ceiling), the collapse/expand seam is preserved in the locked architecture (TREE #12 / SPEC Fact 2 / Sc-rep done_when) independent of #9, the decision is pending and explicit (not a silent drop), and option 1 is already ladder-consistent.
- **Resolution folded into the package:** At d-biglevels-tree9-001, add a THIRD option matching the ladder (additive banner on TREE #9, owner-signed): (i) keep 'a coarse far tier exists and is validated' as the R2 TIER-0 carrier and give S4 (and the S3 vertical-strata question, SPEC §6 п.4 - R3 verticals) a DATED re-entry condition (e.g., the first mines/vertical big-level test map, or before Steam-page copy freezes per the existing option 1); (ii) re-sign the numeric 200x200x40 / >=1000-volume profile down to a TARGET with explicit R5 squeeze license. Reject option 2 as worded - it silently drops R2's carrier along with the numbers. Additive, no lock touched.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\NOW.md` — «The far-tier S3/S4/S5 scale plumbing is parked until levels get big.»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\TREE.md` — «the coarse simulation tier is designed and arithmetic-validated to hold a huge procedurally generated level profile (≈200×200×40 m class, ≥1000 volumes) within measured memory/tick/bandwidth budgets»

### words-vs-docs-002-jewel-now-five-slices-deep-no-insertion-guard [P2 / CONFIRMED] TIER-0 R1 (explosions/mixing) is now FIVE consecutive owner-signed engine-only slices deep with no guard against a sixth insertion, and the reactions CALL needs a full re-shape when reached

- **Requirement:** R1 (TIER-0: deep gas interaction near the player - explosions, mixing, believable AND gameplay-interesting behavior)
- **Claim:** The docs sequence the jewel behind Sc-types, Sc-weight, Sc-rep, and now Sc-kernel (owner-signed 2026-07-02) - the 5th consecutive engine-only slice - while NOW itself recorded the 4th as already pre-mortem-#2 tunnel territory. Each insertion was individually owner-signed (not drift), but no doc encodes his just-stated priority that R1 is the reason the game is worth making: nothing prevents a 6th pre-reactions insertion, c-exec-021 requires a full drift-sweep/body-rewrite re-shape when finally reached (extra latency), and the only player-visible milestone (2026-07-24 telegraph+bang and/or two-type visual) is marked 'tail movable' with its visual half frozen by the owner's own hold.
- **Resolution folded into the package:** Additive guard at the Sc-kernel framing (c-exec-023, owner present): (i) record that Sc-kernel is the LAST pre-reactions engine-only insertion absent an explicit owner re-affirmation quoting R1; (ii) run the c-exec-021 re-shape prep (banner items 11-14 drift-sweep / body rewrite) in PARALLEL with the Sc-kernel executor leg so reactions does not serialize behind it; (iii) re-affirm or consciously move the 2026-07-24 milestone at the same framing (never let it slip silently via 'tail movable'). No lock touched; sequencing already owner-signed stays honored.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\NOW.md` — «(1) accept ~1-slice milestone tail move (honest: 4th consecutive engine-only slice — pre-mortem-#2 tunnel; g-7e15 visual is the parallel player-facing surface)»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\NOW.md` — «2026-07-24 remains the visible-gas milestone (telegraph+bang and/or two-type visual on the real sim; the old «Waves A→B» wording is retired), tail movable.»

### words-vs-docs-004-hangar-fallback-ladder-inverts-owner-priority [P3 / PARTIAL] Hangar as stress-proxy HOLDS, but the pre-named fallback ('smaller fully-fine levels') would spend TIER-0 R3 variety and MAJOR R5 size to preserve the owner-demoted hangar case

- **Requirement:** R3 (TIER-0: mines/shafts, tall verticals; hangars explicitly «второстепенное») + R5 (MAJOR: no hard size limits, squeeze acceptable)
- **Claim:** SPEC §6 п.3's fallback sentence («два лок-БЕЗОПАСНЫХ: all-peers-all-bubbles / меньшие уровни полностью-fine») omits a cross-reference to the ladder-consistent cheapest cut the SPEC itself already reserves and measurement-wires in the §2 leading frame (segment-authored halls, «включаем ТОЛЬКО если замер покажет проблему»), and the standing pending decision d-biglevels-tree9-001 guards only the «smaller levels needs an explicit signature» half — not the «cap/segment hangars first, per владелец's 'второстепенное'» ordering. Practical risk is low: when the de-risk wall actually fired (2026-07-02) the response was owner-signed Sc-kernel, not the pre-named shrink, and the live c-exec-023 path pre-names no fallback (STOP-and-surface). Fix = a small additive cross-reference/ordering note on §6 п.3 (optionally marking §6 п.2's target «1 hangar per level» as desirable-not-required), foldable into the pending d-biglevels-tree9-001 owner decision rather than a new banner.
- **Resolution folded into the package:** Additive owner-signed banner on SPEC §6 п.3 reordering the fallback ladder per the ladder: on a flooded-hangar budget failure, FIRST option = cap/segment hangars specifically (activate the already-reserved segmented-hall authoring - «второстепенное» makes this the cheap cut), preserving level size and mine/vertical variety; 'smaller levels overall' demoted to last resort requiring an explicit signature (it silently spends R3/R5). Mark §6 п.2's '1 hangar per level' as desirable-not-required. Keep the hangar as the measurement proxy and keep all Sc-kernel hangar gates unchanged.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «Фолбэк если не влезает — два лок-БЕЗОПАСНЫХ: all-peers-all-bubbles / меньшие уровни полностью-fine.»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\character-road-shape-2026-06-29.md` — «at Sc-rep time the ACTIONABLE fallback is smaller fully-fine levels (reduce the active-cell ceiling)»

### words-vs-docs-005-mp-never-notices-vs-loud-halt-and-no-rejoin [P3 / PARTIAL] R6 'player never notices problems' vs the plan's only failure answers: loud all-peer halt, no desync recovery, no mid-raid rejoin after a drop - no road item owns making these invisible

- **Requirement:** R6 (multiplayer works so the player never notices problems - «чтобы у игрока не возникали вопросы»)
- **Claim:** The lockstep core genuinely serves R6 (bit-identical shared gas; host-resilience a named TREE #11 obligation; the >4-co-resident-type all-peer halt already has an owned defusal path via the c-exec-021 shape fork, banner item 13). The real residual gap is narrower than claimed: (a) player-facing desync-recovery UX has NO owner-priced answer in any live doc — the loud halt is a detection discipline and the old recovery machinery was deleted with the host-broadcast path (c-exec-017); (b) R6's verbatim 2026-07-02 wording postdates the owner-signed Fact-5 no-late-join cut (which already consciously prices no-mid-raid-rejoin with an explicit «позже — может быть» reservation) and is not recorded next to that fork; (c) weak-peer stall UX is unowned, though the measured 2-4x deficit itself is owned by the signed Sc-kernel slice (d-sparse-tick-kernel-001) and the weakest-peer premise is part of the ratified ADR-0002 lock. Disposal: additively fold a session-resilience axis (desync-recovery UX, the reserved rejoin fork with R6 recorded verbatim, weak-peer stall UX) into the pending d-demo-road-001 July shape agenda; no lock or Fact is touched; rejoin reopens only via signed decision.
- **Resolution folded into the package:** Additive: fold a named 'session-resilience axis' into the d-demo-road-001 July shape agenda (it must be priced before the demo road is fixed anyway): desync-recovery UX (what players see after a loud halt - e.g., halt -> raid checkpoint/relaunch), the mid-raid drop-REJOIN fork that Fact 5 explicitly reserved with «позже — может быть» (reopens ONLY via signed decision, never silently), and weak-peer stall UX. Record R6's verbatim wording next to the fork so the owner prices the gap himself; no lock or Fact is touched by the recording.
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «рассинхрон = громкая остановка. Предел масштаба = CPU самого слабого пира, НЕ канал.»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\knowledge\g9c41-gas-engine-SPEC.md` — «Факт 5. НЕТ позднего входа (late-join). Лобби/база → Start → рейд с теми, кто есть; вход в середине рейда запрещён (позже — может быть).»

### words-vs-docs-006-visual-hold-consistent-but-tunnel-counterweight-vanished [P3 / CONFIRMED] Visual ON HOLD is consistent with R7, but the road's anti-tunnel justification still cites the frozen visual track as the player-facing surface - the guard silently lost its counterweight

- **Requirement:** R7 (procedural visualization can be layered on top) + charter pre-mortem #2 (no private engineering tunnel)
- **Claim:** The owner's hold («сначала разобраться с симуляцией») matches R7's own wording - visualization is a layer on top, and the R13/R14 read-only seam is preserved, so the hold costs no architecture (this half HOLDS). The divergence: the road-shape lens sweep justifies consecutive engine-only slices BY the parallel visual track 'keeping proof flowing', and NOW's fork-1 resolution leans on the same argument - both now stale, since the owner froze that track on 2026-07-02 and the 07-24 milestone's 'two-type visual' branch froze with it. No live doc records that the sole remaining player-facing terminus is the Sc-reactions telegraph+bang owner-eye.
- **Resolution folded into the package:** Additive one-line NOW note at the Sc-kernel framing: with visual on hold, the ONLY player-facing terminus is the Sc-reactions telegraph+bang owner-eye - the lens-sweep justification of engine-only slices no longer holds as written; re-affirm the 07-24 milestone against this (ties into words-vs-docs-002). R7 itself needs no change; do not un-hold visual (owner directive).
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\NOW.md` — «OWNER DIRECTIVE 2026-07-02: VISUAL track ON HOLD — «сначала разобраться с симуляцией»; c-visual-004 NOT framed.»
- evidence: `C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\character-road-shape-2026-06-29.md` — «the parallel g-7e15 visual track is the project-level player-facing surface keeping proof flowing (render-only, does not yet consume the new rep); the chain commits to a dated surface»

## Appendix B — refuted (for honesty)

- **words-vs-docs-003-r1-believability-levers-have-no-home** — Both named levers for R1 near-player believability (finer near grid; TIER-3 fixed-point velocity for jets-in-the-open) are orphaned - one an unresolved fork, the other's trigger can never fire on the current road
  WHY refuted: The finding's central claim — that the TIER-3 fixed-point re-trigger is homed to an unreachable 'owner-eye during look-work' and 'structurally cannot fire' on the current road, leaving no scheduled moment for the owner's TIER-0 believability verdict — misreads the docs by relying on the 2026-06-26 research doc's original wording and missing the later owner-accepted update. history/s-work-023.md (2026-06-28, owner «весь пакет») re-homed the trigger concretely: 'd-gas-richness-tiers-001 += UPDATE: ... re-trigger = one honest no-walls open-space jet look at the COMBAT slice = signed evidence for ADR-0010', with a carried capture 'VELOCITY re-trigger lands at the COMBAT slice... Carry so the corridor 27x can't silently answer the open-space 2:1 question' (full record in work/now-snapshot-2026-06-29.md). The combat slice is Sc-damage (damage = concentration), which IS on the current road per NOW.md next_slices — exactly the re-homing the finding's own proposed_resolution requests. Second, 'every remaining road slice is engine-only (owner-eye = sees the same scenes)' is false: only Sc-kernel is byte-identical engine-only; work/character-road-shape-2026-06-29.md states the re-scoped Sc-reactions 'terminates in the owner-eye visible telegraph+bang', and NOW.md kill_by holds the 2026-07-24 visible-gas milestone on the real sim. Third, the visual-track hold is irrelevant to the trigger: all prior believability owner-eyes (S1 «работает», Sc-types two-gas look) ran on the engine debug view, not g-7e15. The remaining true sliver — d-finer-grid-fork-001 unresolved — is not 'orphaned': it is a tracked NOW.md decision (from review finding 23, same 2026-07-02 cycle), surfaced in next as 'Still pending owner', with its decide-by anchor (before visual WAVE 2) unpassable while visual is on hold. Restating an already-surfaced pending owner decision is not a plan defect; per the rules this is 'already resolved by a signed decision/scheduled slice' plus a docs misread.

## Appendix C — what HOLDS (verified good, per dimension)

### local-depth
- Near-player transport base HOLDS: 50cm isotropic 3D face-flow with integer gather-then-apply, opening-area in the flow law (R6, a slit seeps >0), emergent height (low door drains only lower Z-cells, crouch-under-layer free), and buoyancy as Z-face BIAS with per-column instant sorting explicitly FORBIDDEN near (no vertical pop) — a genuinely believable, wall-free base law around the player (SPEC §3 Факт-2/face-flow/vertical).
- Open-space-first HOLDS at the near tier: the model is a grid + open faces, not rooms — gas can spawn anywhere; the architecture's single worst case is deliberately an OPEN 196k-cell hangar (measured 2026-07-02), and Sc-kernel's done_when adds the adversarial 'hangar flooded' all-active capture, so open space is the stress case, not an afterthought (SPEC §6 п.3; c-exec-023 draft #2c).
- Explosion GRAMMAR (fairness/determinism/readability) HOLDS: trigger reads refinement-invariant coarse totals so an approaching player can never self-detonate a cloud (no gameplay-pop/desync), chains advance one-tick-per-step with a TICK-COUNT falsifier, and telegraph-then-bang is a player-readable warning — a gameplay-interesting explosion design near the player (SPEC §3 несущие инварианты; c-exec-021 done_when #4).
- Mixing/reactions DESIGN quality HOLDS: axes-not-pairs integer data chemistry (new gas = zero new rules), partial combine only at the overlap front with a per-tick cap (instant whole-cloud combine structurally impossible), condition-waves as propagating data-driven events, mass exactness with declared-sink discipline — rich, near-player-relevant interaction design (gas-reaction-typing-design 2026-06-30; c-exec-021 re-scope banner items 2-5).
- Damage/dose near the player HOLDS as planned: dose is time-integrated from the AUTHORITATIVE 50cm field at the player's head Z-cell (0 overlap → 0 dose), cell-granular — not room-mushy — wherever a player is present, since rooms with players stay fine-tier; 'coarse' here means authoritative-vs-bubble, not room-granular (SPEC §3 «Что БЬЁТ и что ТРИГГЕРИТ»; Факт-2 vertical).
- The de-risk loop protecting R1's CPU headroom HOLDS: the hangar wall fired honestly on a real measurement, Sc-kernel (cost ∝ active cells) was inserted BEFORE reactions with owner signature, and the plan is honest that 'cost ∝ active fronts' is FALSE in the player-in-hangar case — the near-player richness budget is being defended by measurement, not hope (NOW d-sparse-tick-kernel-001; c-exec-023 draft; SPEC §6 п.3).
- The S1 TIER-1 impulse layer itself HOLDS as shipped mechanism: выброс/ветер+развилка/односторонний клапан/jet-emitter are authoritative, in-checksum, zero new wire traffic, with symmetric decay and return-to-rest oracles — the cheapest real directionality money can buy inside the lock; the gap is the dropped escalation trigger above it (finding local-depth-01), not the tier itself (SPEC §3 Форсированный поток; s-work-023).

### global-scale
- HOLDS — honest level-size answer to focus (a), post-Sc-kernel on the weak peer (50–100 ms budget, measured basis 2026-07-02): MEDIUM levels (≈40 rooms / ≤~60k cells) fit comfortably even fully flooded (~6–50 ms weak-peer single-thread; quiet play is sub-ms class — TYPICAL 2,934 active = 0.408 ms strong). LARGE levels (~150 rooms / ≈223k cells) fit in NORMAL play (active set ~10^3–10^4 cells ⇒ single-digit ms weak) but the fully-flooded case straddles/exceeds the budget single-threaded (~22–180 ms weak) — large is honest only with localized activity, the unscheduled ×4-8 multicore divisor (finding global-scale-5), or S4. HUGE (TREE#9 12.8M cells) does not fit fully-fine on any lever (~64× ceiling, ~0.5+ GB domain-proportional store) — S4-only, as d-biglevels-tree9-001 already states.
- HOLDS — the plan is honest with itself about my axis's worst case: SPEC §6 п.3 explicitly declares «cost ∝ active fronts ЛОЖНО» and enumerates the exact adversarial cases (player-in-hangar wall, cascades, perpetually-vented rooms), and the c-exec-023 draft carries a real adversarial flooded-hangar gate (all ~196k active) rather than a happy-path benchmark. The flooded gate is also a strict superset of the ventilation/wind standing-active worst case (R8), so that named owner requirement is covered by the same capture. The gap is what happens AFTER the measurement (finding global-scale-2), not the measurement's honesty.
- HOLDS — R2 semantics of byte-sleep: as designed (byte-sleep lossless, saturate-and-hold mass-exact per d-roomfull-001, settled_flag gates ONLY far-demote/sleep never near-transition, mass clamp [0,cap] with loud assert), a sleeping saturated region is a provable fixed point of the tick — not-ticking it is arithmetically identical to ticking it. That IS «симуляция проходит везде» in the owner's sense for mass transport, provided the wake-trigger set is complete (the one caveat = finding global-scale-4).
- HOLDS — the de-risk machinery on this axis worked exactly as intended and the road order is right for R2: the 07-05 wall was keyed to the hangar number, the real measurement fired it, Sc-kernel (cost ∝ active cells) was inserted BEFORE Sc-reactions the same day with an owner signature (d-sparse-tick-kernel-001) — fixing the cost model before reactions/typing multiply per-cell cost is the correct dependency order, and the c-exec-014 readout had already named it «the #1 optimization to bank before big halls».
- HOLDS — the S4 seam survived the representation change: Sc-rep's done_when explicitly required preserving per-region structure + the collapse/expand seam (verified through its gate battery and fresh G5 COULD-NOT-REFUTE), so the undated S4 deferral is a SCHEDULING problem (finding global-scale-1), not an architectural regression — S4 still plugs in without core rework, which is the best possible state for a deferred TIER-0-carrying node.
- HOLDS — R2 «coarser far away but runs everywhere» is architecturally sound as planned: one conservation law at two granularities (grid = base table, graph = materialized rollup), collapse/expand as the sole mass-exact conversion path, far tier in-checksum — global authoritative coverage never has a hole in space, only in resolution; and until S4, the near 50cm tier IS everywhere (the interim 2-band coarse code has no runtime consumer), so today's builds genuinely simulate the whole space at full resolution within the size ceiling.

### verticality
- NEAR vertical law is real and shipped, not promised: Sc-weight delivered a SIGNED per-type integer buoyancy (rises AND sinks AND neutral about absolute 0 — deliberately better than the far tier's unsigned max(1,..) law), as a creeping Z-face bias, NOT a column sort; monotone settle to barometric equilibrium proven by oracle; no-pop, conservation, carry-clean and over-strength failure modes each pinned by independent RED controls; owner-eye saw the heavy/neutral/light COM demo (BuoyancyDemoDirector, three columns in one tall room); the per-type weight is folded into the registry ContentHash so a weight override cannot green-checksum-desync (ADR-0019).
- Emergent height near is proven structural, not asserted: RF5 test pins that no face above a door's sill+openingSize is open (low door leaks only the bottom cells), and face-flow tests cover sill/openingSize selectivity — the SPEC's «высота эмерджентна» claim has direct RED coverage on the near tier.
- The sub-quantum vertical threshold (cells < ~24 mass do not drift) is honestly documented, owner-acked («документируем» 2026-06-29), pinned by a mass-quantum test, and shown irreducible (±1 floor would over-sort, stored carry would break DERIVED) — a rare example of a vertical limitation being surfaced instead of hidden.
- Stacked rooms are volume-neutral and the topology layer is genuinely 3D: conformance checks bounds/overlap on all three axes, vertical stacking costs the same cell count as flat layout (SPEC Факт 2 «стопка комнат объём-нейтральна»), and Z-boundary portals ARE expressible (the portal Z-band opens Z-normal faces between stacked regions; partial hatches modelable as narrow shaft regions) — so R3 mines at the ~150-room/~223k-cell target scale fit the fully-fine near grid even before any far-tier work; the S3/S4 deferral does NOT block small/target-scale vertical level design.
- There is NO far-tier vertical lie today: the interim 2-band coarse sim has zero runtime consumers since c-exec-013 ZERO-LEGACY (render adapter + demo scenes deleted; INTERIM-COARSE-FAR-TIER.md tracks it with an explicit do-not-extend rule) — R2 'simulation everywhere' is currently served by the authoritative near grid everywhere, and the plan of record replaces the 2-band with an area-aware rollup at S4 rather than shipping its Lower/Upper chunking to players.
- The road deliberately rescued the vertical math from the S4 delete-set: Sc-weight was explicitly sequenced to port the proven CoarseSpecies weight/heat law shape into the near tier «до того, как S4 её удалит» (SPEC §5 road order) — the foreseeable vertical-knowledge loss was planned around, and the per-species temperature seam stays a TREE #11 KEEP-OPEN invariant (not welded shut).
- Sc-kernel (owner-signed 2026-07-02, BEFORE reactions) is the correct near-term enabler for the TIER-0 mine archetype: cost ∝ active cells + open faces + bounded co-resident types means a mostly-empty tall mine is cheap even at large cell counts, and its done_when honestly includes the all-cells-active adversarial capture rather than assuming sparse fronts.

### multiplayer
- HOLD (f): Lockstep (ADR-0002) remains the right call for friends-coop; NO confirmed pressure justifies reopening it, signed or otherwise. Evidence: the only measured multiplayer-relevant pressure (tick cost ∝ roster × domain cells, 587 ms roster-64 hangar) is a KERNEL-SHAPE defect fixed netcode-neutrally by the signed Sc-kernel slice (work/c-exec-023-draft-call.md); the host-authoritative-clouds alternative was adversarially killed on exactly the TIER-0 axes (mass conservation, determinism, deep level-spanning interaction = the CENTER) with its one real win (fixed-radius bubble cap) already adopted lock-safely as the off-checksum detail bubble (work/gas-cloud-vs-grid-analysis-2026-06-26.md §5-6: two of three kills LANDED, «Keep the grid; cap the bubble»); host-broadcast would resurrect the keyframe-inclusive bandwidth problem TREE #9 measured as ~5.3x optimistic plus snapshot/late-join obligations lockstep freed us from. Even the worst flooded-hangar outcome routes to level-scope/rollup fallbacks (owner already accepts R5 squeeze), not to the netcode. The clouds draft correctly stays HELD; its conditional trigger should stay recorded, not acted on.
- HOLD: the determinism-by-construction battery genuinely protects the silent-drift half of R6 (the half players notice as rubber-banding/ghost gas in other games): integer-only + zero-float/int-overflow scans + the loud mass-clamp rule (no «checksum GREEN, physics garbage», SPEC line 58) + cell-keyed stamp fold with DISTINCTNESS/cross-peer REDs (Sc-rep, G5 COULD-NOT-REFUTE) + fail-fast loopback halt (ADR-0017) — and it is continuously carried forward (c-exec-023 done_when #5 keeps both scan roots + closes the Coarse/ hole; c-exec-021 banner item 10 preserves it as principle). Given determinism holds, lockstep structurally guarantees all peers see IDENTICAL gas — the strongest possible consistency story for R1-class deep interaction with zero field traffic.
- HOLD (c, building on review finding #4 — not re-reported): the >4-co-resident-types LOUD THROW all-peer halt has a SCHEDULED owner-present defusal — c-exec-021 banner item 13 makes it a named shape fork (representation-level policy for non-reacting >4-type mixes + an adversarial 5-type-junction RED proving the throw unreachable in the shipped level/chemistry design) and honestly marks 5+-type zones ship-blocking until then. Residual watched, not dangling: Sc-kernel explicitly must NOT touch K/overflow policy (c-exec-023 STOP), so the window closes exactly at the reactions shape, which is the right place (chemistry defines which co-residency is legal).
- HOLD (a-partial): Факт-5 (no late-join) is an honest, owner-ratified scope cut, not a hole — it removes the full-snapshot obligation for v1 and gives a clean base↔raid session boundary (SPEC line 52), which is the correct cheap v1 shape for friends-coop; it only becomes an R6 problem in combination with the unplanned halt/drop story (findings 01/02), where the resolutions land.
- HOLD (d-partial): the plan is HONEST about its own network gap — SPEC line 126 explicitly brands S2 as loopback-only with gas-on-real-bus a named bigger seam, and the 2026-06-29 audit independently verified the labeling («already honestly named»). The defect is undatedness (finding 03), not self-deception; the seam inventory needed to schedule it already exists on file.

### types-mixing
- HOLDS — representation substrate for many types: sparse-dominant store (contiguous fixed-size records, no per-cell heap), per-cell dominant STAMP cell-keyed in MeaningChecksum with isolation/distinctness/cross-peer REDs, roster = config (~128 default, not welded) — verified in code (SparseDominantNearGasField.cs, SparseDominantCellRecord.cs) and in ADR-E-0001; this is the right R8 substrate and it exists, merged, gate-green.
- HOLDS — the roster cost cliff did NOT survive into the plan: the real 2026-07-02 measurement caught tick cost ∝ roster × domain cells, the de-risk wall fired, and the owner signed Sc-kernel BEFORE reactions; the draft gate battery (roster-independence, active-scaling, adversarial flooded-hangar, GC-zero, field-level storage guard) directly binds the R8 cost claim. Typed face state is already bounded (SparseDominantFaceFluxCarryState.MaxTypeLanesPerFace=4, side-bound mirrors the cell bound) so the cliff does not migrate to faces; per-tick checksum folds are active-cell-keyed skip-zero, so no per-type checksum-size cliff remains identified.
- HOLDS — wind/ventilation through the grid (R8): S1 forced-flow is SHIPPED (выброс, ветер with fork-split, one-way клапан = permanent designed split, jet-emitter) as in-checksum integer face bias with symmetric decay, ±1 floor, settle guarantees and RED oracles; the honest-limits table (richness research) prevents overpromising (transient open-space split, no true inertia), and escalation tiers (TIER-2 lattice rooms, TIER-3 fixed-point velocity) are named, owner-gated reserves rather than silent lock cracks. Wind is per-face-uniform so it moves a mixed cell as bulk — believable for R1.
- HOLDS — interaction-composition determinism discipline is strong across wind × buoyancy × reactions: combined desiredMove (gradient + bias + buoyancy) with the c-020 settle-envelope RED, gather-then-apply with order-permutation controls, telegraph→bang chained one-step-per-tick with a tick-count oracle, reaction rule-set folded into the session handshake ContentHash with BOTH convergence and divergence REDs, and cross-layer reads pinned to committed revision (1-tick lag). The known composition hazards are named as oracles, not vibes.
- HOLDS — partial combine at the overlap front (reactions fire only where ≥2 reactant types co-reside, per-tick cap marches the front inward) makes gradual believable mixing of two large clouds structural — instant whole-cloud combine is impossible by construction, which serves R1 believability directly.
- HOLDS — env-derived dynamic typing SERVES R8 rather than complicating it, given the seam cuts already made: one substance/many faces multiplies variety without roster bloat, the REGIME (derived, moves nothing) vs IDENTITY (the only mass-mover) split is clean, the stamp socket landed in Sc-rep without the mechanism, and the accumulator (if it wins fork-1) is correctly pre-declared as a new cell-keyed skip-zero checksum member. The two real gaps found are reported as findings 3 (sleep/wake) and 4 (wave×wind), not here.

### words-vs-docs
- R2 (global sim) holds TODAY and the docs are honest about it: the whole domain is authoritative at 50cm everywhere (SPEC Fact 2 «Слой ГАЗ = 50см глобально, авторитетно везде, по ВСЕМ осям, включая вертикаль»), and SPEC §6 п.3 explicitly refuses the optimistic shortcut («'cost ∝ active fronts' ЛОЖНО») - the plan's spine IS the owner's R2; the divergence found (words-vs-docs-001) is only about the undated far-tier path at future scale, not about today's model.
- Open-space-first standing feedback holds at the near tier: near = a wall-agnostic 3D face-flow grid (gas can spawn anywhere; walls are just closed faces, emergent from geometry), and the hangar probe - one open 196k-cell volume - is precisely the open-space worst case, so the hangar-as-gate is the honest stress proxy for ALL big open volumes (answering the prompt's (b): proxy yes, over-investment no; only the fallback ordering diverges, words-vs-docs-004).
- R8 (many types, interactions, wind, ventilation - all through the grid) holds strongly: unlimited type CATALOG owner-signed (TREE g-d3a8 done_when 1 «должно быть очень много типов»), roster size = config not welded (SPEC Fact 4), Sc-kernel's whole point is roster-independence (c-exec-023 gate 2a), wind/valve/fork-split/jet delivered in S1 (SPEC §3 forced flow), and the reactions re-scope is axes-not-pairs O(axes) so new gases add zero rules (c-exec-021 banner item 2).
- R4 (vertical gameplay) holds at the near tier: height emergent from geometry (low door leaks only lower cells, crouch-under-layer free - SPEC Fact 2), per-type weight/buoyancy DELIVERED and merged (Sc-weight, buoyancy-bias not sort), and height-bands are correctly buried (SPEC §7); only the FAR-tier vertical-strata question is open (§6 п.4, folded into words-vs-docs-001).
- R6 core holds where it matters most: input-lockstep gives bit-identical shared gas on every peer (no ghost/interpolation divergence a player could notice in normal play), host-resilience is a NAMED core obligation (TREE #11), and the docs self-flagged the worst R6 violation honestly (c-exec-021 banner 13 marks the >4-type all-peer halt ship-blocking) - the gap is only recovery/rejoin UX (words-vs-docs-005).
- R7 (visualization layered on top) holds and the owner's visual hold is CONSISTENT with it: the visual track is read-only over the authoritative grid, decoupled by the R13/R14 seam, so freezing it costs no architecture and it re-attaches later without engine rework (only the stale anti-tunnel citation diverges, words-vs-docs-006, P3).
- R1 machinery direction holds: sparse-dominant + bounded mix-overlay + per-cell dominant STAMP is exactly the substrate mixing/reactions/dynamic-typing need, the road order (representation -> kernel -> reactions) was independently verified correct by the 2026-07-02 review, and every insertion before the jewel was owner-signed at a fork, not drift - the divergences found are about pacing guards and orphaned levers, not about the plan building the wrong thing.

END_OF_FILE: live/indie-game-development/work/audit-gas-sim-plan-2026-07-02.md
