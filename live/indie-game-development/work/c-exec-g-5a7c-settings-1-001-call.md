# CALL — c-exec-g-5a7c-settings-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА здесь не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
task: t-settings-1
issued: 2026-08-11 by s-work-g-5a7c-settings-1-dispatch-001
status: ready
basis: `6599ae87e5435b709f4b382becb16a018a5ae695` — `origin/dev` = `origin/main`. Перемерено
       `git ls-remote origin` непосредственно перед выпуском. Это состояние содержит закрытую
       дорожку `cargo-hands` целиком (наклон, соло-подъём, две мощности, сила на мыши, три класса).

**ПАРАЛЛЕЛЬНАЯ ПОЛОСА ХОЗЯИНА ЖИВА, И ПЕРЕСЕЧЕНИЕ ПРОВЕРЕНО ПО ФАЙЛАМ, А НЕ НА ГЛАЗ.** Наряд
`c-exec-g-5a7c-react-2-001` прямым текстом запрещает себе `Network/NetworkPlaySettings.cs` и
`.asset` и обязан вернуть `IntegratedHouse.unity` байт в байт. Пересечения нет ни одного.
**ВНИМАНИЕ:** оба наряда полосы хозяина сегодня стоят на базе `2f7f2e87`, то есть на 22 коммита
позади. Их перебазирует их собственная полоса; ЭТА задача их не трогает и работает от свежей головы.

Пин перемерен: `engineering_contract: 36` против `validation.config` `synced_contract_version: 36`.

## slot

**WIN-U1.** Каталог `C:\projects\Unity\GasCoopGame_win-u1`, ветка `slot/win-u1`.

Перемерено настоящим селектором (только чтение) непосредственно перед выпуском: `state: CLEAN`,
`lifecycle: AVAILABLE`, `lease: none`, `branch: slot/win-u1`, `head: 6599ae87…` — знак в знак базис,
синхронизации не требуется. `mcp-endpoint: unrecorded`.

**Первое действие, меняющее состояние:**

`.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U1 -Claim -LeaseId c-exec-g-5a7c-settings-1-001:BUILD`

Селектор fast-forward НЕ делает. Любое расхождение, чужая аренда или не-AVAILABLE — СТОП домой;
WIN-U2 не брать, он принадлежит полосе хозяина. Для Unity-действия live-проверить endpoint именно
этого lease; если прямой Unity MCP недоступен или не верифицируется как WIN-U1 — **СТОП и вопрос
владельцу**, без shell/HTTP/headless обхода.

## goal

**Настройки разъезжаются по своим местам, имя перестаёт врать, а место груза в мире перестаёт быть
числами в файле и становится сценой.**

Это ЧИСТО СТРУКТУРНАЯ работа. Поведение игры не меняется ни на грамм, ни одно число не меняет
значения. Владелец нашёл этот дефект сам, с двух сторон независимо: «я не понимаю, почему в одном
Network Play Settings лежат вообще разные настройки» и «я хочу в определённое место поставить… там
типа на столе, не на полу».

## context — перемерено чтением байтов на `6599ae87`

### Что там лежит: 56 полей в коробке с именем про сеть

`Assets/TunnelCrew/Network/NetworkPlaySettings.asset` несёт **56 сериализованных полей**. Сеть в
них — ДВА: `_defaultAddress`, `_port`. Остальные 54 попали туда потому, что коробка была первой и
единственной.

Устав направления даёт единственный тест принадлежности
(`knowledge/how-the-game-is-built-layers.md`, блок «четыре вида происходящего»): **«влияет ли это на
то, что произойдёт в игре»**. Четыре вида названы там же, плюс пятый — место в мире, которое по
уставу принадлежит слою МИРА, то есть сцене.

### ГЛАВНАЯ ОПАСНОСТЬ ЗАДАЧИ: сортировать по имени НЕЛЬЗЯ, имя врёт

Две ловушки перемерены заранее, обе противоречат тому, как поле называется:

- **`_eyeHeight` звучит как настройка камеры — это ПРАВДА ИГРЫ.** Читается судьёй:
  `NetworkPlaySettings.CreateCargoRules()` кладёт его в `pickupEyeHeight`, оттуда
  `CargoRules.PickupEyeHeight`, оттуда `AuthoritativeCargoRoster.cs:643` — начало луча захвата.
  Разная высота глаз у двух игроков = разные захваты. В личные не отправлять.
- **`_lookPitchLimit` был личным ВЧЕРА и перестал быть сегодня.** `NetworkWalkerInput.cs:141`
  зажимает им наклон прицела, а после `t-cargo-2` наклон прицела ЗАДАЁТ ВЫСОТУ ГРУЗА. Значит разный
  предел у двух игроков даёт им разную досягаемость груза. Это правда игры, хотя лежит в локальном
  вводе.
- **`_lookSensitivity` — действительно личное.** Единственный читатель `NetworkWalkerInput.cs:137`,
  чистая чувствительность мыши, наружу не летит.

**Каждое из 56 полей относится по СВОИМ ЧИТАТЕЛЯМ, найденным поиском, а не по звучанию имени.**
Возврат обязан назвать поимённо каждое поле, чей вид определился читателем ВОПРЕКИ имени.

### Кто вообще читает этот ассет — двенадцать файлов

`Editor/StartConditionsEditor.cs`, `Network/CargoBody.cs`, `Network/NetworkCargoPresentation.cs`,
`Network/NetworkPlaySettings.cs`, `Network/NetworkSessionService.cs`, `Network/NetworkWalkerCamera.cs`,
`Network/NetworkWalkerCourier.cs`, `Network/NetworkWalkerInput.cs`,
`Network/NetworkWalkerPresentation.cs`, `Network/WalkSpaceProbe.cs`, `World/StartConditions.cs`.
Все они — Unity-слой; **слой правил (`Core/**`) этот ассет не видит вовсе**, он получает готовый
`CargoRules`. Эту стену задача НЕ трогает: она разбирает коробку, а не переставляет слои.

### Место груза в мире — четыре поля, которым место в сцене

`_cargoSpawnX: 0`, `_cargoSpawnZ: 2.5`, `_additionalCargoSpawns` (три точки),
`_additionalCargoClassIds: 0, 1, 2`. Сегодня это координаты, вписанные в файл настроек: положить
груз на стол нельзя, потому что координату стола не впишешь.

В сцене уже стоит подходящая опора: `World/StartConditions` — «сиденье», из которого собираются
стартовые условия перед Play, со своим редактором `Editor/StartConditionsEditor.cs`. Его
собственный комментарий говорит прямо: «где начинается груз, остаётся в `NetworkPlaySettings`».
Именно эту строку задача и переписывает.

### Что УЖЕ сделано и переделке не подлежит

В классе **десять блоков `[Header]`**, заведённых `t-cargo-2`, и владельцу уже отдан одностраничный
список ручек. Понятность наполовину куплена. Эта задача её НЕ переписывает — она чинит структурную
половину.

### Почему побайтовый гейт обязателен

Разбор коробки — ровно та операция, где значение молча обнуляется. У нас это уже было: в
`t-situation-1` переименование сериализованного поля едва не обнулило время жизни пятой реакции
(`landingLifetimeSeconds: 8`), и поймал это только заранее поставленный гейт, а не внимательность.

## проверка происхождения

| Строка наряда | Источник |
|---|---|
| пять видов разъехались, имя перестало врать | подписанная карточка `t-settings-1` `done_when` 1; его слова 2026-08-11 |
| каждое поле отнесено по читателям, а не по имени | там же, `done_when` 2; тест устава «влияет ли это на то, что произойдёт в игре» |
| место груза стало сценой | там же, `done_when` 3; его слова «я хочу в определённое место поставить… на столе, не на полу» |
| чувствительность становится личной | там же, `done_when` 4; строка устава «только моё: камера, интерфейс» |
| каждое число до и после байт в байт | там же, `done_when` 5 |
| параллельно полосе хозяина, пересечение проверено по файлам | там же, `done_when` 6 |

**Не импортируется** мебель, столы, расстановка груза по дому, переделка слоёв, новая панель
разработчика, переписывание группировки инспектора, любое изменение поведения игры.

## boundaries — что НЕ трогать

- **НИ ОДНО ЧИСЛО НЕ МЕНЯЕТ ЗНАЧЕНИЯ.** Переезжает поле, не значение. Задача, которой «удобнее»
  подкрутить число по дороге, — **СТОП домой**.
- **ФАЙЛЫ ПОЛОСЫ ХОЗЯИНА — ЧУЖИЕ.** `Core/Householder/**`, `Core/Situations/**`,
  `World/Householder*`, `Settings/HouseholderWalkSettings.{cs,asset}`, все `Householder*Tests.cs` и
  `CargoContactNoiseTests.cs` — не трогаются. Упёрся — **СТОП домой**.
- **ПУБЛИЧНАЯ СИГНАТУРА `CarryWorld.Advance` НЕ МЕНЯЕТСЯ** — её держит полоса хозяина.
- **`Core/**` НЕ ПЕРЕСТРАИВАЕТСЯ.** Слой правил этот ассет не видит и видеть не должен; он получает
  `CargoRules`. Если разбор потребовал править `Core` — это признак, что стену собрались двигать:
  **СТОП и вопрос владельцу**.
- **`IntegratedHouse.unity` ОТКРЫВАЕТСЯ — и это ЕДИНСТВЕННАЯ задача волны, которой можно.**
  Разрешено ровно одно: завести объекты точек появления груза и перенести в них существующие
  координаты. Мебель, стены, свет, дом, `StartConditions.Players`, положение хозяина — не трогать.
- **Пороги шума `60 / 150 / 400`, массы классов, все силы и высоты** переезжают со своими
  значениями и ни одно не крутится.
- **`ProjectSettings/**` не трогать.** `Assets/GasCoopGame/**` закрыта.
- **Новых игровых чисел не заводить.** Если разбор требует нового поля — назвать в возврате
  отдельной строкой с причиной.
- Самодельный сканер исходников уликой не является и не пишется.

## owner-visible check

**Проверка обратная обычной: всё должно быть ровно как вчера.** Тот же подъём, тот же наклон, тот
же хозяин, те же три куска груза. Любое замеченное отличие — разрыв, и его называют, а не объясняют.

Единственное, что законно меняется для владельца, — **где он теперь эти числа находит** и то, что
точки появления груза стали объектами, которые видно и можно двигать мышкой.

## headless evidence

- **Тот же набор тестов зелёный.** База `6599ae87` даёт **173 passed, 0 failed, 0 skipped**. Ни одно
  ожидание не переписывается; доказать диффом по `tests/`.
- **ГЛАВНЫЙ ГЕЙТ — ПОБАЙТОВОЕ СОВПАДЕНИЕ ВСЕХ 56 ЗНАЧЕНИЙ.** Выписать значения ДО (на базе) и ПОСЛЕ
  (на голове), по всем новым местам, и вставить сверку в возврат дословно. Ноль расхождений —
  единственный принимаемый результат. Поле, у которого значение не сохранилось, — разрыв, даже если
  тесты зелёные.
- **Гейт на имя:** в имени ассета/класса, владеющего массами, силами и порогами, слова про сеть
  больше нет.
- **Гейт на место:** `grep` по `_cargoSpawnX\|_cargoSpawnZ\|_additionalCargoSpawns` в
  `Assets/TunnelCrew/Network/` → пусто; координаты живут в сцене.
- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`.
- Через прямой MCP точного слота — настоящая Unity import/compile evidence. Эта задача трогает
  сериализованные ассеты и сцену, поэтому Unity-проверка здесь **несущая, а не формальная**: если
  ссылка порвётся, тесты этого не увидят вовсе.
  **Зелёные ворота поведением игры не являются и так не предъявляются.**

## откат

- **Точка возврата — `6599ae87e5435b709f4b382becb16a018a5ae695`.**
- Откат назвать ОДНОЙ точной командой и **исполнить настоящим revert**: после отката
  `git rev-parse <ветка>^{tree}` возвращает дерево базы знак в знак. Выведенный откат вместо
  исполненного — назвать выведенным.
- Прямо заявить, что для отката не нужно ничего вне репозитория.

## done_when

1. **ПЯТЬ ВИДОВ РАЗЪЕХАЛИСЬ, И КАЖДОЕ ПОЛЕ ОТНЕСЕНО ПО ЧИТАТЕЛЯМ, А НЕ ПО ИМЕНИ.** Связь, правда
   игры, только моё, оформление и место в мире живут отдельно; имя того, что владеет массами,
   силами и порогами, больше не говорит про сеть. Принадлежность каждого из 56 полей доказана
   поиском его читателей; поля, чей вид определился ВОПРЕКИ имени, названы поимённо — как минимум
   `_eyeHeight` и `_lookPitchLimit`, которые звучат лично, но читаются судьёй и потому остаются
   правдой игры. Личным становится только доказанно локальное.

2. **МЕСТО ГРУЗА В МИРЕ СТАЛО СЦЕНОЙ.** Точки появления — объекты в `IntegratedHouse.unity`,
   которые видно в редакторе и можно двигать мышкой; класс груза выбирается на самой точке.
   Координат появления груза в `Assets/TunnelCrew/Network/` не осталось — проверяется поиском.
   Мебели, столов и расстановки НЕ заводится: строится шов, содержимое принадлежит волне дома.

3. **НИ ОДНО ЧИСЛО НЕ ИЗМЕНИЛО ЗНАЧЕНИЯ, ПОВЕДЕНИЕ НЕ ПОЕХАЛО, ОТКАТ ИСПОЛНЕН.** Все 56 значений
   выписаны до и после и совпадают байт в байт — сверка вставлена в возврат дословно. Тесты
   зелёные (`173 passed`), ни одно ожидание не переписано, доказано диффом по `tests/`. Unity
   импортирует и компилирует, ссылки на ассеты и сцену целы. Границы целы — файлы полосы хозяина,
   `Core/**`, `ProjectSettings/**`, дом кроме заведённых точек, не тронуты, доказано диффом. Откат
   исполнен настоящим revert и проверен readback до дерева базы.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / t-settings-1`: точные коммиты и их
продуктовая диспозиция; scoped manifest/diff и точные outputs проверок. Также обязательно:

- **таблица всех 56 полей**: имя → куда переехало → значение до → значение после. Это главное
  доказательство задачи, и оно предъявляется целиком, а не выборкой;
- **список полей, чей вид определился читателем вопреки имени**, с указанием читателя;
- **где теперь живёт место груза** — имя объекта, как владелец его двигает, как выбирает класс;
- **как выглядит для владельца** — куда он теперь идёт за числами, простыми словами, одним абзацем;
- **что стало с `StartConditions`** и его редактором;
- проверенный откат: команда и readback дерева базы;
- терминальный readback селектора слота `CLEAN / AVAILABLE / lease none`;
- **строкой: с какой головы полосы хозяина собиралось перед публикацией** — клауза 5 `evaluator`;
- каждое расхождение свежей базы с grounds этого наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает.** После HOME отдельная нога Направления
читает возврат первой рукой и закрывает `t-settings-1`, называя вслух, каким путём шла. Строки этой
задачи проверяемы по байтам, поэтому светлое закрытие ей доступно — в отличие от задач дорожки
груза.

## budget

Один engineering root. Работа механическая, но объёмная: 56 полей и двенадцать читателей. Полдня.
Мебель, расстановка, панель разработчика, переделка слоёв и переписывание группировки инспектора
сюда не входят. Не помещается — STOP HOME с одним точным blocker и предложением линии разреза,
но резать самому по неназванной линии запрещено.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-settings-1-001-call.md
