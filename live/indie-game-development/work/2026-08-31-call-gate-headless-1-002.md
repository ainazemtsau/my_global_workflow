# CALL: доказать, что красный headless действительно останавливает публикацию

Задача: `t-gate-headless-1` · волна `bet-g-5a7c-wave-9` · полоса `починка` · **доработка**
База: продуктовый `main` = `56a7b88a`.
Слот: **WIN-U2**.

---

## ПРОЧИТАТЬ ДО ПЕРВОЙ СТРОКИ

**Механизм построен и опубликован. Не хватает ровно одного — доказательства, что он работает.**

Первая нога (`c-exec-g-5a7c-gate-headless-1-001`) завела прогон 725 headless-тестов внутрь
закрывальщика и сдала работу в `main`. Её приёмка требовала доказать **посевом мины**, что красный
headless останавливает публикацию, и прямо добавляла: «без этого пункт 1 недоказан».

**Доказательства нет, и это признаёт сам её отчёт**, закоммиченный в `main`:
«Required red-headless evidence is BLOCKED» — и переносит работу в `## next`.

Причина была уважительная и уже устранена: **обе попытки закрытия умирали на ЧУЖОМ красном
EditMode** (у хозяина пропал `Animator`) и до headless не доходили — 408 и 351 секунда Unity
впустую. Тот красный был следствием стёртых настроек импорта модели; направление вернуло байты из
истории git, и **дерево теперь зелёное**. Доказательство стало возможным.

**ТЕБЕ НЕ НАДО НИЧЕГО СТРОИТЬ.** Код на месте. Надо провести опыт и предъявить его вывод.

## goal

**Показано текстом закрывальщика, что красный headless останавливает публикацию, и что `dev` при
этом не уезжает.**

## done_when

1. **ПОСЕВ МИНЫ В HEADLESS-ТЕСТ, ПРОГОН ЗАКРЫВАЛЬЩИКА, ВОЗВРАТ.** Сломай один headless-тест,
   запусти штатное закрытие, покажи, что оно **упало на headless**, верни.
   **Мину сей не в `HouseholderDecisionPipelineTests.cs`** — именно этот файл в прошлый раз
   уехал сломанным в `dev` и держал публикацию трёх ног в заложниках. Возьми любой другой.

2. **ВЫВОД ЗАКРЫВАЛЬЩИКА ВСТАВЛЕН В ВОЗВРАТ ЦЕЛИКОМ** — тот кусок, где видно, что остановка
   произошла именно на headless, а не на EditMode и не на `-Deliver`. Это главный продукт ноги.

3. **`dev` НЕ СДВИНУЛСЯ.** Назови SHA `dev` до опыта и после. Сегодня закрывальщик сливает в `dev`
   ШАГОМ 2/7, до проверки — значит `dev`, скорее всего, СДВИНЕТСЯ, и это тоже результат: назови
   его честно числом, не оправдывай. Починка этого — соседняя задача `t-close-safely-1`, не твоя.

4. **МИНА ВОЗВРАЩЕНА, И ЭТО ПРОВЕРЕНО ИЗ `dev`, А НЕ ИЗ СВОЕЙ ВЕТКИ.**
   `git show dev:<файл>` обязан показать исходное значение. Прошлый раз возврат остался на ветке
   слота, и в `dev` неделю жила бы сломанная защита.

5. **САМОТЕСТ ЗАКРЫВАЛЬЩИКА ПРОГНАН, И ЕГО ВЫВОД ПРЕДЪЯВЛЕН.** `tools/close-leg.selftest.ps1`,
   318 строк, 21 случай. В прошлый раз он числился зелёным только со слов — предъяви вывод.

6. **ВРЕМЯ ПОЛНОГО ЗАКРЫТИЯ НАЗВАНО ЧИСЛОМ.** В прошлый раз оно не было названо ни разу, потому
   что ни одно закрытие не дошло до конца. Теперь дойдёт.

7. **ТЕСТЫ ЗЕЛЁНЫЕ** — 725 headless плюс Unity-набор.

## что НЕ входит

- **Правка порядка шагов закрывальщика** (слияние до проверки) — это `t-close-safely-1`, идёт в
  WIN-U4 параллельно. **Не лезь в `close-leg.ps1`**, кроме как для посева мины в ТЕСТ.
- Замок на `testrun`, сводка с коммитом, путь закрытия для N ног — там же.
- Любые правки в `Assets/**`.

## boundaries

Твоя поверхность: один headless-тест на выбор (для мины) и артефакты прогона.
**`tools/**` прямо сейчас правит нога `t-close-safely-1` в WIN-U4** — не трогай его вообще.

`Network/**` — нога зрения в WIN-U1. `Core/Householder/` и `Probe/Householder/HouseholderMarks.cs`,
`HouseholderGauges.cs` — нога закрепления в WIN-U3.

## СТОП и вопрос домой

- **Закрытие снова умерло на чужом красном** — СТОП, назови тест и ошибку. Не чини: чужая
  поверхность, и это ровно тот случай, который сегодня уже стоил полдня.
- **Мина не смогла уронить закрытие** — это находка важнее задачи: значит гейт не работает.
  СТОП и полный вывод домой.
- **`tools/**` изменился под тобой** — значит соседняя нога доехала; СТОП и перечитай базу.
- **Unity не поднялся** — СТОП, скажи какой слот. Обходного пути не строй.

## ЗНАЙ ПРО СЕГОДНЯШНИЙ ДЕНЬ

- **`testrun` общий и без замка.** Ручной прогон `unity-tests.ps1` переставляет чекаут под чужим
  закрытием, а сводка не пишет коммит — испорченное доказательство выглядит нормальным.
  **Пользуйся закрывальщиком, руками в `testrun` не гоняй.**
- **Склад арта общий на пять чекаутов через junction.** Файл в `iCloudDrive\TunnelCrew-Assets\`
  виден во всех слотах сразу. Сегодня так стёрся риг хозяина и уронил закрытие всем ногам.

## return

1. **Вывод закрывальщика на упавшем прогоне**, целиком, с видимой остановкой на headless.
2. SHA `dev` до и после опыта.
3. `git show dev:<файл>` после возврата — доказательство, что мина снята именно в `dev`.
4. Вывод `close-leg.selftest.ps1`.
5. Время полного закрытия числом.
6. Прогон тестов числом.
7. Всё, что заметил и не чинил, — списком.

## budget

Половина ноги. Это опыт, а не постройка. Если полезла постройка — задача понята неверно, стоп.

END_OF_FILE: live/indie-game-development/work/2026-08-31-call-gate-headless-1-002.md
