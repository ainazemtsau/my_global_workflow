# CALL — c-ctrl-integrate-gate-surface-cut-001

to: executor
kind: engineering (integration)
repo: ainazemtsau/GasCoopGame
engineering_contract: 35
direction: indie-game-development
node: g-6b13
task: c-1 (интеграция уже закрытой работы)
issued: 2026-08-02 by s-work-g-6b13-integrate-gate-cut-issued-001
status: ready
slot: **`WIN-CTRL`** — `C:/projects/Unity/GasCoopGame_dev`, ветка `dev`. Это единственный слот для
      интеграции по правилу продуктового `AGENTS.md:161,165,198`. Слоты `WIN-U*` сюда не годятся.
      Проверено первой рукой 2026-08-02: дерево `GasCoopGame_dev` чистое, `dev` и `main` совпадают
      байт в байт (0/0), `dev` отстаёт от кандидата ровно на два коммита.

## goal

Внести уже закрытую и проверенную работу по урезанию проверок в `dev`, затем в `main`, и освободить
слот `WIN-U3`.

## context

Кандидат: **`97412301bc6b66550c32215b0beefd94e0032c50`**, ветка `slot/win-u3`, два коммита
(`1f016999` вырез + `97412301` документы).

Работа ЗАКРЫТА и перепроверена Направлением первой рукой: `check.ps1` 854 → 224 строки, прогон
7,99 с по часам против 210 с, зелёный; отчёт ноги на месте со всеми семью полями. Здесь ничего
не пересматривается и не доделывается — только интеграция.

**Зачем это срочно:** урезание живёт ТОЛЬКО в своей ветке. На `main` по-прежнему лежит `check.ps1`
на 854 строки, поэтому каждая новая задача получает старый прогон на 210 секунд вместо восьми.
Ускорение уже оплачено работой, но нигде не действует.

Плановая стадия здесь НЕ требуется: это интеграция, а не постройка.

## boundaries

- **Кандидат не менять.** Ни строки кода, ни правок «заодно». Единственное разрешённое изменение
  внутри интеграции — строка статуса в закрывающем отчёте, см. ниже.
- Ничего не резать и не возвращать из вырезанного.
- `Assets/TunnelCrew/**` не трогать: там идёт своя работа в другом слоте.
- Ветку `slot/win-u2` не трогать вообще.
- **Инструмент недоступен — СТОП.** Если контракт интеграции требует прогона PlayMode через MCP, а
  Editor или MCP не поднимаются — не обходить никакими суррогатами: одной строкой домой «нужен
  запуск владельца» и стоять.
- Если что-то дороже или невозможнее, чем описано, — ESCALATE домой одной строкой, а не обход.

## done_when

1. `dev` содержит кандидата `97412301`.
2. Строка статуса в `docs/results/c-exec-gate-surface-cut-001.md` переставлена на
   `**Status: DELIVERED on \`dev\`**` — по правилу, которое сама эта нога и записала: отчёт пишет
   нога, статус переставляет интеграция после слияния.
3. `tools/check.ps1 -Deliver` на состоянии `dev` — ЗЕЛЁНЫЙ. Красный означает, что не доставлено.
4. `dev` слит в `main` и запушен.
5. Слот `WIN-U3` освобождён по процедуре `AGENTS.md:205-210`: сохранение непубликованных байт под
   durable ref, продвижение фиксированной ветки слота только созданием-на-опубликованном-кончике
   или перемоткой, затем
   `tools/select-slot.ps1 -Slot WIN-U3 -Release -LeaseId c-exec-gate-surface-cut-001:BUILD`, затем
   read-only селектор против реального чекаута. Если освобождение упирается в STOP — работа может
   быть опубликована, но блокер называется точно и явно.

## return

Домой, в направление `indie-game-development`, задача `c-1` ставки `g-6b13`. В возврате: на чём
стоят `dev` и `main`, вывод `-Deliver`, состояние слота `WIN-U3` после освобождения, и одной
строкой — что оказалось дороже или невозможнее, чем описано.

## budget

Одна нога интеграции. Бумаги минимум: здесь ничего не строится.

END_OF_FILE: live/indie-game-development/work/c-ctrl-integrate-gate-surface-cut-001-call.md
