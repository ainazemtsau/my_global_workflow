# CALL — c-work-g-5a7c-scale-4-call-001

to: session
play: work
direction: indie-game-development
node: g-5a7c
task: t-scale-4
issued: 2026-08-09 by s-work-g-5a7c-vert-1-close-001
status: ready

## goal

Существует готовый к отправке наряд исполнителю на `t-scale-4` — лут живой только пока его держат
лучом или он падает; лёг и никто не держит — неактивен, но пройти мешает.

## context

- **Вертикаль у игрока закрыта его командой.** `t-vert-1` доставлена; опубликованная вершина
  продукта `102af1b1` (`origin/dev` = `origin/main` = `origin/slot/win-u1`). Прыжок `Space` 2.2 м/с,
  удерживаемый полёт `F` 0.65 м/с, гравитация ходока 3 м/с². Разбор:
  `history/2026-08-09-s-work-g-5a7c-vert-1-close-001.md`.
- **ВНИМАНИЕ, ЭТО НОВОЕ И ПРЯМО КАСАЕТСЯ ЭТОЙ ЗАДАЧИ: у ходока ТЕПЕРЬ ЕСТЬ СВОЯ ГРАВИТАЦИЯ**
  (`_walkerGravity: 3`) как хостовое аналитическое правило, а у груза гравитация по-прежнему
  выключена и ось Y заперта (`CargoBody`). Эта задача включает груза настоящую физику. Значит в
  одной сцене окажутся ДВА разных вертикальных закона — аналитический у мыши и физический у груза.
  Наряд обязан назвать это прямо и сказать, что с этим делает; молча оставлять расхождение нельзя.
- **Строка задачи переписана владельцем 2026-08-08** (`knowledge/loot-is-inactive-until-a-beam-touches-it.md`,
  его слова: «когда игроки не поднимают лут, он как бы и звуков не должен издавать… условно
  неактивный»; «лут мешает пройти»). Прежняя редакция требовала просто включить гравитацию — и
  ломала его же требование `W120`: лежащее под гравитацией тело давит на пол весом ≈165–195 Н против
  порогов шума 60 / 150 / 400, то есть шумело бы вечно.
- **Откат здесь дороже обычного:** трогается единственное настоящее физическое тело игры, ту
  переноску владелец принимал собственным прогоном. Наряд обязан назвать откат и проверить его.
- **Телекинез: дальность взятия 1.28 м (8 ростов), вынос 0.8 м, длина хвата 1.6 м.** Груз, взятый
  вплотную, отлетает к 0.8 м — это известная и записанная граница
  (`i-telekinesis-carry-distance-is-fixed-001`), в этой задаче она НЕ чинится.
- **Проверка происхождения обязательна** и уже трижды окупилась: три последние ноги нашли в задачах
  строки, выросшие из замера стоимости или из его смягчённых слов, а не из требования. Каждая строка
  `done_when` проверяется на источник до отправки; снятая строка называется вслух с основанием.

## boundaries

- Наклон груза НЕ трогать — вырез 1 ставки, судит его глаз.
- Потолок числа рук не трогать (`t-scale-6`); два шва не заводить (`t-scale-7`).
- Пороги шума `60 / 150 / 400` не двигать. Дом, обстановку, хозяина и облик не трогать.
- Способ подъёма мыши не переоткрывать: он закрыт отдельной задачей и висит уликой
  `i-vertical-both-is-a-normalization-not-his-word-001`.
- Инструмент недоступен — СТОП и вопрос владельцу, никогда обходной путь.

## done_when

Наряд написан, лежит в `work/`, зарегистрирован карточкой `call` и годен к отправке: слот и база
перемерены первой рукой на момент написания, `engineering_contract` пин проставлен, у каждой строки
`done_when` проверено происхождение, расхождение двух вертикальных законов названо, откат назван, и
в возврате прямо сказано, что продуктовый handback задачу Направления НЕ закрывает — закрывает его
слово о том, та ли это переноска, что он принимал.

## return

RESULT ноги `work` с зарегистрированным нарядом и продолжением той же волны.

## budget

Одна нога. На 2026-08-09 замерено: WIN-U1 `AVAILABLE`, `lease: none`, `slot/win-u1` = `origin/main`
= `102af1b1`; продуктовый стамп контракта 36 = текущий 36, дрейфа нет. Перемерить заново.

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-scale-4-call-001.md
