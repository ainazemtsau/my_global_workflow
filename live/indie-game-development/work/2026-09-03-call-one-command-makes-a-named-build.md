# CALL: сборка получает имя, и мы впервые узнаём, что в неё едет

Задача: `t-one-command-makes-a-named-build-1` · волна `bet-g-5a7c-wave-12` (рубеж Р1, 11.09) · строка 10
Слот: **WIN-U1** · репозиторий: `C:\projects\Unity\GasCoopGame_win-u1`
Слот `AVAILABLE`, аренда `none` — прочитано в `gascoop-slot-state.v1.json` 2026-09-03.
Основание: **`origin/main` = `origin/dev` = `5fcdc11f`.**
База: headless `882/882/0`, EditMode `246/246/0` на `a3b899db` — **перемерь и назови свои числа.**

**ЭТА НОГА НАМЕРЕННО СТОИТ В СТОРОНЕ ОТ ВСЕХ ОСТАЛЬНЫХ.** Три других слота заняты своими
поверхностями: WIN-U2 — сценарий Multiplayer Play Mode, WIN-U3 — живая проба утра, WIN-U4 — стенд и
равновесие. Здесь не пересекается ни один файл, и одна возможность пересечения **вырезана явно**,
см. §2.

## 1. ЧТО СДЕЛАТЬ — ЧЕТЫРЕ ВЕЩИ, И ВСЕ ЗАМЕРЕНЫ ЗАРАНЕЕ

### 1.1. НОМЕР ВЕРСИИ И ПАПКА ВЫДАЧИ

Замер Направления на `5fcdc11f`, чтобы ты не искал: **сборка одной командой уже есть.**
`tools/unity-build.ps1` зовёт `-executeMethod TunnelCrew.Editor.PlayerBuildCommand.BuildWindows64`
(`Assets/Editor/PlayerBuildCommand.cs`), кладёт `Builds/StandaloneWindows64/TunnelCrew.exe`, пишет
`Logs/unity-gate/player-build.json` из настоящего `BuildReport` и возвращает 0 только на `Succeeded`.
Сцены берутся из Build Settings, не дублируются в коде.

- `DefaultOutputDir = "Builds/StandaloneWindows64"` (`:29`), `ExecutableName = "TunnelCrew.exe"` (`:30`).
- **Аргумент папки выдачи УЖЕ ЕСТЬ:** `-tcOutput` (`:84`). Номера версии нет.

Нужно: **номер версии — дата плюс коммит — и папка выдачи**, поверх существующего.
**ПОРОГ: если появляется НОВЫЙ скрипт сборки — прогноз волны был неверен, скажи это фактом, а не молча.**

### 1.2. ЧТО ФАКТИЧЕСКИ ЕДЕТ В СБОРКУ — ЗАМЕР, А НЕ ПРАВКА

Замер Направления на `5fcdc11f`: в Build Settings ровно три сцены — `Lobby.unity`,
`IntegratedHouse.unity`, `MouseLab.unity`. Сцены `ThreadFeelAudition.unity` и
`TrailerHouse_EveningLab.unity` в сборку **не входят вовсе**. Чужие демо-клипы Feel живут в
`Assets/TunnelCrew/Art/Thread/Feel/**` (три префаба плюс `Audition/ThreadFeelAuditionSet.asset`) и
`Assets/TunnelCrew/Presentation/Feel/**`. **Единственная поставляемая ссылка на них —
`IntegratedHouse.unity`.**

Нужно: по `BuildReport` назвать **путями**, что фактически лежит в собранном exe из чужого, и какой
**ровно одной правкой** в `IntegratedHouse.unity` эта ссылка выключается.

**ПРАВКУ НЕ ДЕЛАТЬ. `IntegratedHouse.unity` НЕ ТРОГАТЬ.** Сцену держит WIN-U3. Это ровно тот конфликт
слияния на общем файле, из-за которого волна девять встала.

**ГРАНИЦА ВЛАДЕЛЬЦА, ПОСТАВЛЕННАЯ 2026-09-03, ДОСЛОВНО:** «в сборке не должно быть там лишней инфы…
но я как бы не хочу, чтобы это удалялось у меня из работы. Ну, что для работы нужно. Может, нам потом
как бы и нужны они будут», и шире — «возможно, кроме fill ещё что-то будет» (`fill` = `Feel`,
распознавание голоса).

**ВЫКЛЮЧИТЬ ИЗ СБОРКИ — ДА. УДАЛИТЬ ИЗ ПРОЕКТА — НЕТ.** Удаление любого файла Feel, поломка сцены
`ThreadFeelAudition.unity` или окна аудита — нарушение границы владельца и откат.

### 1.3. ОДИН АБЗАЦ В ОПУБЛИКОВАННОМ ОТЧЁТЕ

`docs/results/c-exec-g-5a7c-the-magnet-has-two-poles-on-a-thing-001.md:233-240` несёт заголовок
«Найденный дефект, НЕ мой и НЕ тронутый» про `AnnounceNewLabShoves` — а дефект был закрыт коммитом
`e9412d6c` в том же дереве, и подмёл он весь класс, а не одну строку.

**Допиши абзац: найден и закрыт, коммитом таким-то.** Историю не переписывать, старый текст не
стирать. Один абзац, один файл, ничего больше.

### 1.4. ЧЕТЫРЕ ГОЛОВНЫХ ТЕСТА СЛЕЗАЮТ С РАСХОДНОГО ПРОФИЛЯ

Улика `i-mechanism-tests-stand-on-disposable-shipped-content-001`, замерена 2026-08-31: удаление
четырёх дел из `Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json` роняет
**четыре теста**, и все четыре — головные, про механизмы, а не про содержание:

| тест | файл |
|---|---|
| `TheCommittedProfileCarriesTwoChores…` | `tests/TunnelCrew.Core.Tests/HouseholderChoreTests.cs` |
| `TheShippedChoreRowsGiveWayToSomethingHeJustNoticed` | там же |
| `ArrivalEffectWrapperKeepsTheInnerConditionsNotMetReason` | `tests/TunnelCrew.Core.Tests/HouseholderReactionPriorityTests.cs` |
| `TheShippedQuietTickKeeps…SevenReasons` | там же |

Владелец 2026-08-31 объявил содержимое профиля **расходным полигоном**: оно будет меняться часто и
без предупреждения. Тест механизма, стоящий на расходной заготовке, перестаёт различать поломку
механизма и смену содержания.

Нужно: **эти четыре теста берут собственную заготовку, а не поставочный профиль.** Доказательство —
не рассуждение: измени поставочный профиль и покажи, что четыре теста остались зелёными.

**ГРАНИЦЫ ЭТОГО ПУНКТА, И ОНИ УЗКИЕ.**
- Правишь **только** `HouseholderChoreTests.cs` и `HouseholderReactionPriorityTests.cs` плюс новая
  заготовка. **Сам `default.householder.json` НЕ трогаешь** — он расходный и принадлежит владельцу.
- **`HouseholderDoesNotFreezeTests.cs` НЕ трогаешь.** Он слеп по своей фикстуре, это известно, и его
  чинит отдельная нога по вечной заморозке утра. Если увидишь его красным — назови фактом, не чини.
- Домов у тестов два: headless гоняет `tests/TunnelCrew.Core.Tests/**`, Unity-тесты живут в
  `Assets/Tests/**`. Поиск по одному даёт ложный ноль.

## 2. ЧЕГО В ЭТОЙ НОГЕ НЕТ

- **`IntegratedHouse.unity` не трогается вовсе** — см. §1.2. Её держит WIN-U3.
- **Ядро хозяина и вечная заморозка утра** — отдельная нога, ждёт слова владельца. Ты про неё знаешь
  ровно затем, чтобы не залезть в её файл.
- Свои звуки (`t-six-events-make-a-sound-1` — сначала чисто, потом своё), Steam-транспорт, лобби с
  именами, цвета.

## 3. ПОРОГИ

1. **Появился новый скрипт сборки** — прогноз волны был неверен, скажи фактом.
2. **Погашен хоть один тест** — запрещено абсолютно (`tools/hygiene.ps1:363-376`).
3. **Удалён хоть один файл Feel** — нарушена граница владельца, откат.
4. **Тронута `IntegratedHouse.unity`** — откат.
5. **Тронут `default.householder.json` или `HouseholderDoesNotFreezeTests.cs`** — откат: первый
   принадлежит владельцу, второй чинит другая нога.

## 4. МЕХАНИКА СЛОТА — ТРИ ПРАВИЛА, КОТОРЫЕ УЖЕ СТОИЛИ ВРЕМЕНИ

1. **Общий прогонный чекаут `GasCoopGame_testrun` двигает только `tools/close-leg.ps1`, внутри
   замка.** Ручной `git checkout` там вчера чуть не отнял прогон у чужой ноги.
2. **После «подтяни dev» открытые сцены надо перезагрузить, и ни в коем случае не сохранять.**
   Сегодня это стоило владельцу двух часов на лобби без списка мест: Unity не перечитывает уже
   открытую сцену, а сохранение старой поверх новой стирает обновление по-настоящему. Перезагрузку
   делаешь ты и говоришь ему одной строкой, что сделал.
3. **Закрытие — штатным `tools/close-leg.ps1`.** В возврате строка «аренда освобождена» либо прямая
   причина удержания и подо что.

## 5. ЧТО ВЕРНУТЬ

RESULT по схеме, и в нём поимённо:

- **Свои числа** — headless и EditMode на своей голове, не переписанные отсюда.
- **Как выглядит имя сборки** — точная строка папки и exe на живом примере.
- **Замер Feel** — что фактически лежит в собранном exe, путями; и какой ровно одной правкой в
  `IntegratedHouse.unity` это выключается. Правку не делать.
- **Четыре теста** — с чего на что переехали, и ПОКАЗАННОЕ доказательство: правка поставочного
  профиля больше их не роняет. Показать, а не рассудить.
- **Живая проба** — exe запущен на машине, где проекта нет. Если такой машины нет — скажи фактом,
  не подменяй чистую машину похожей.
- Строка «аренда освобождена» либо причина удержания.

END_OF_FILE: live/indie-game-development/work/2026-09-03-call-one-command-makes-a-named-build.md
