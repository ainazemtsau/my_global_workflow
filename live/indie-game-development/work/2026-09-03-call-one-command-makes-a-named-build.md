# CALL: сборка одной командой с именем, и в ней нет чужого

Задача: `t-one-command-makes-a-named-build-1` · волна `bet-g-5a7c-wave-12` (рубеж Р1, 11.09) · строка 10
Слот: **WIN-U1** · репозиторий: `C:\projects\Unity\GasCoopGame_win-u1`
Слот `AVAILABLE`, аренда `none` — прочитано в `gascoop-slot-state.v1.json` 2026-09-03, не вспомнено.
Основание: **`origin/main` = `origin/dev` = `5fcdc11f`.** База: headless `882/882/0`, EditMode
`246/246/0` на `a3b899db` — **перемерь и назови свои числа.**

**ПОЧЕМУ ЭТА ЗАДАЧА В ЭТОМ СЛОТЕ, А НЕ В СВОЁМ.** Полоса `оболочка` — слот WIN-U2, и в ней стояли
пять задач подряд в одной машине. Полоса `мышь` израсходована: её единственная задача сдана сегодня.
**Слово владельца 2026-09-03: «Ну, мы типа можем взять сборку».** До 11.09 восемь дней, поэтому
задача 8 идёт в WIN-U1 параллельно задаче 6 в WIN-U2. Правило «одна нога — одна машина» держится;
правило «одна полоса — одна машина» снято его словом ровно на эту задачу.

## 0. ЧТО УЖЕ ЛЕЖИТ ГОТОВЫМ — ЗАМЕРЫ НАПРАВЛЕНИЯ НА `5fcdc11f`, НЕ ДОГАДКИ

- **`Assets/Editor/PlayerBuildCommand.cs`** — `DefaultOutputDir = "Builds/StandaloneWindows64"` (:29),
  `ExecutableName = "TunnelCrew.exe"` (:30), `SummaryPath = "Logs/unity-gate/player-build.json"` (:31).
  **Аргумент папки выдачи УЖЕ ЕСТЬ:** `-tcOutput` (:84). Номера версии нет.
- **Build Settings — ровно три сцены** (`ProjectSettings/EditorBuildSettings.asset`):
  `Lobby.unity`, `IntegratedHouse.unity`, `MouseLab.unity`. `ThreadFeelAudition.unity` и
  `TrailerHouse_EveningLab.unity` в сборку **не входят вовсе**.
- **Feel живёт в двух местах:** префабы `Assets/TunnelCrew/Art/Thread/Feel/**` (три штуки плюс
  `Audition/ThreadFeelAuditionSet.asset`) и код `Assets/TunnelCrew/Presentation/Feel/**` (часть под
  `Editor/` со своим asmdef). **Единственная поставляемая ссылка — из `IntegratedHouse.unity`.**
- **Сборка плеера была сломана до сегодня и никто этого не видел.** `AnnounceNewLabShoves` объявлен
  внутри `#if UNITY_EDITOR`, вызывался снаружи — файл не компилировался в player-конфигурации.
  Закрыто коммитом `e9412d6c`. **Это и есть настоящая причина задачи.**

## 1. done_when

В карточке `t-one-command-makes-a-named-build-1`, все пять пунктов. Читай её целиком: пункт 3 был
переписан сегодня словом владельца и означает не то, что означал утром.

## 2. ГРАНИЦА ВЛАДЕЛЬЦА ПО ПУНКТУ 3 — ЧИТАЙ ДО ПЕРВОЙ ПРАВКИ

Его слово 2026-09-03, дословно: **«в сборке не должно быть там лишней инфы… но я как бы не хочу,
чтобы это удалялось у меня из работы. Ну, что для работы нужно. Может, нам потом как бы и нужны они
будут»**, и шире — **«возможно, кроме fill ещё что-то будет»** (`fill` в расшифровке голоса = `Feel`).

**ВЫКЛЮЧИТЬ ИЗ ПОСТАВЛЯЕМОЙ СБОРКИ. ИЗ РЕПОЗИТОРИЯ НЕ УДАЛЯТЬ.**
Удаление `Art/Thread/Feel/**` или `Presentation/Feel/**` — нарушение строки, а не её исполнение.
Аудиторская сцена `ThreadFeelAudition.unity` и окно аудита остаются рабочим инструментом.

## 3. ЧЕГО В ЭТОЙ НОГЕ НЕТ

- **`IntegratedHouse.unity` НЕ ТРОГАТЬ.** Сцену прямо сейчас держит WIN-U3 под арендой
  `c-exec-g-5a7c-the-morning-ends-at-the-door-002:proba` — конец утра, выход за дверь, сигнал конца
  раунда. Это ровно тот конфликт слияния на общем файле, из-за которого волна девять встала.
  Значит: ссылку на Feel из этой сцены **не режешь**. Ты её **МЕРИШЬ** и возвращаешь фактом —
  что именно едет в сборку и какой ровно одной правкой это выключается. Резать будет нога, которая
  получит сцену, когда WIN-U3 её отдаст.
- Свои звуки не кладутся: это `t-six-events-make-a-sound-1`, и порядок такой — сначала чисто, потом своё.
- Steam-транспорт, лобби с именами, цвета — не сюда.

## 4. ПОРОГИ

1. **Появился новый скрипт сборки** — прогноз волны был неверен, скажи это фактом, а не молча.
2. **Погашен хоть один тест** — запрещено абсолютно (`tools/hygiene.ps1:363-376`).
3. **Удалён хоть один файл Feel** — нарушена граница владельца, откат.
4. **Тронута `IntegratedHouse.unity`** — откат.
5. **`ThreadFeelAudition.unity` перестала открываться или окно аудита перестало работать** — откат:
   это его рабочий инструмент.

## 5. МЕХАНИКА СЛОТА — ТРИ ПРАВИЛА, КОТОРЫЕ УЖЕ СТОИЛИ НАПРАВЛЕНИЮ ВРЕМЕНИ

1. **Общий прогонный чекаут `GasCoopGame_testrun` двигает только `tools/close-leg.ps1`, внутри
   замка.** Ручной `git checkout` там — то, чем вчерашняя нога чуть не отняла прогон у чужой
   (`i-the-shared-runner-guard-knows-only-some-of-its-users-001`, второй случай).
2. **После «подтяни dev» открытые сцены надо перезагрузить, и ни в коем случае не сохранять.**
   Новое знание `a-pull-under-a-live-editor-shows-a-stale-scene`: сегодня это стоило владельцу двух
   часов на лобби без списка мест. Перезагрузку делаешь ты и говоришь ему одной строкой, что сделал.
3. **Закрытие — штатным `tools/close-leg.ps1`.** В возврате строка «аренда освобождена» либо прямая
   причина удержания и подо что (`a-lease-ends-with-the-work-that-took-it`).

## 6. МАЛЕНЬКИЙ ОТДЕЛЬНЫЙ ПУНКТ, НАЗВАННЫЙ ЯВНО

Опубликованный `docs/results/c-exec-g-5a7c-the-magnet-has-two-poles-on-a-thing-001.md:233-240` несёт
заголовок «Найденный дефект, НЕ мой и НЕ тронутый» про `AnnounceNewLabShoves` — а дефект был закрыт
коммитом `e9412d6c` в том же дереве. **Допиши абзац: найден и закрыт, коммитом таким-то.** Историю
не переписывать, старый текст не стирать — дописать, что случилось после.
Границы: один абзац, один файл, ничего больше (`i-a-published-report-calls-a-defect-untouched-after-it-was-fixed-001`).

## 7. ЧТО ВЕРНУТЬ

RESULT по схеме, и в нём поимённо:

- **Свои числа** — headless и EditMode на своей голове, не переписанные из наряда.
- **Как выглядит имя сборки** — точная строка папки и exe, на живом примере.
- **Четыре головных теста** — с чего на что переехали, и чем доказано, что они больше не падают от
  правки поставочного профиля (то есть: правку сделал и показал, а не рассудил).
- **ЗАМЕР Feel:** что фактически лежит в собранном exe по `BuildReport` — путями, а не словами; и
  какой ровно одной правкой в `IntegratedHouse.unity` это выключается. Правку **не делать**.
- **Живая проба:** exe запущен на машине, где проекта нет. Если такой машины у тебя нет — скажи это
  фактом, не подменяй чистую машину чем-то похожим.
- Строка «аренда освобождена» либо причина удержания.

END_OF_FILE: live/indie-game-development/work/2026-09-03-call-one-command-makes-a-named-build.md
