# CALL — c-exec-g-1d84-host-under-the-judge-002

to: executor (продуктовый репозиторий)
repo: ainazemtsau/GasCoopGame
kind: engineering
engineering_contract: 36
direction: indie-game-development
node: g-1d84
track: хозяин
task: t-host-1
issued: 2026-08-05 by s-work-g-1d84-three-lane-calls-001
status: ready
replaces: c-exec-g-1d84-host-under-the-judge-001 — тот наряд НИ РАЗУ не запускался и снят с фронтира
      навсегда: у него не было площадки (корень хозяина в сцену ещё не въехал) и база `839df47e`
      умерла. Работы потеряно ноль. Разбор сохранён в §ЧЕКПОЙНТ его файла.
slot: НАЗЫВАЕТ ВЛАДЕЛЕЦ В СООБЩЕНИИ (этого требует контракт продукта). Свободный и полностью чистый
      сейчас один — `WIN-U1`; `WIN-U2` и `WIN-U4` требуют по одной уборочной операции, см. ниже.

## goal

Решения хозяина считает создатель игры в том же тике, где считаются игроки и груз. Его положение и
занятие едут в снимке рядом с ними, и на второй копии он идёт так же. Владелец видит его одинаково на
двух экранах.

Это ТРЕТИЙ НОВЫЙ СЛОЙ волны: сегодня хозяина в сети нет вообще.

## база и проверенные пути

**Объявленная база:** `C:/projects/Unity/GasCoopGame`, `main` = `origin/main` = `origin/dev` =
`2eee9719af1a26c770e7d1e16d7793a2da9890cc`. Спрошено у сервера напрямую. Каждый путь ниже проверен на
ЭТОЙ базе командой `git cat-file -e 2eee9719:<путь>`.

**Что есть сегодня — измерено, а не пересказано:**

- `Assets/TunnelCrew/World/Householder.prefab` — заготовка хозяина, корень `Householder` с двумя
  детьми: `Householder Body` (капсула, `CapsuleCollider`, `HouseholderRouteController`) и
  `Householder Route` (`Route Point 01..04` + `Listening Point`). GUID заготовки
  `4b7e1c93a0d8f24ab9c65e0173f8d2a6`. В сцене стоит `PrefabInstance` своим корнем — площадка для
  этой задачи ПОЯВИЛАСЬ задачей `t-scene-2`.
- `Assets/TunnelCrew/World/HouseholderRouteController.cs` — **вот главный факт: хозяин крутится в
  ОБЫЧНОМ КАДРЕ.** `Update()` (строка 142) копит `Time.deltaTime` и в цикле
  `while (_accumulatedSeconds >= _simulationStepSeconds)` зовёт `Householder.Step` (строка 147). То
  есть у того, у кого открыта сцена, и ни у кого больше. Маршрут — `Transform[] routePoints` из
  сцены, настройки — `Assets/TunnelCrew/Settings/HouseholderWalkSettings.asset`.
- `Assets/TunnelCrew/Core/Householder/Householder.cs` — правила хозяина в слое правил: типы
  `HouseholderOccupation`, `HouseholderEvent`, `HouseholderIntention`, `HouseholderPoint`,
  `HouseholderStepInput`, `HouseholderStepResult`, шов решения `IHouseholderDecisionPolicy` и сам
  `Householder.Step`. Слой правил движка не знает: `Core/TunnelCrew.Core.asmdef` —
  `noEngineReferences: true`.
- `Assets/TunnelCrew/Core/Multiplayer/CarryWorld.cs` — один тик судьи в единственно верном порядке:
  `Cargo.ApplyCarryLoad` → `Walkers.Advance` → `Cargo.Advance` (строки 22-31). Хозяина в этом тике
  СЕГОДНЯ НЕТ.
- `Assets/TunnelCrew/Network/NetworkWalkerCourier.cs` — курьер: `OnServerTick` (строка 120) зовёт
  `_world.Advance(...)` (126) и `PublishSnapshots()` (133); рассылка — `ReceiveSnapshotsObserversRpc`
  (194); клиент шлёт только ввод (`SubmitInputServerRpc`, 137).
- `Assets/TunnelCrew/Network/WalkerSnapshot.cs` — `PlayerId / X / Z / Yaw / Moved`.
  `Assets/TunnelCrew/Network/CargoSnapshot.cs` — `CargoId / X / Z / Yaw / HolderCount / Integrity /
  Moved`. **Снимка хозяина не существует ни одного.**
- `Assets/TunnelCrew/Core/Multiplayer/WalkSpacePort.cs` + `Network/WalkSpaceProbe.cs` — существующий
  прецедент явного интерфейса: порт объявлен в слое правил, реализован сценой, передан параметром в
  `CarryWorld.Advance`. **Это образец того, как хозяин должен получить доступ к миру.**
- `Assets/TunnelCrew/Editor/HouseholderRouteControllerEditor.cs` — три кнопки инспектора, сегодня
  единственные производители событий хозяина.
- `core/TunnelCrew.Core.csproj`, `tests/TunnelCrew.Core.Tests/` (в том числе `HouseholderTests.cs`),
  `tools/check.ps1`.

**Слоты — перемерено 2026-08-05 против `2eee9719`, и по реестру, и по каталогам:**

- **`WIN-U1`** — `AVAILABLE`, дерево чисто, HEAD `adae7d40` = `origin/slot/win-u1`, 2 позади, 0 своих.
  Единственный полностью готовый. Первой операцией после аренды — привести к `2eee9719`.
- **`WIN-U2`** — `AVAILABLE`, 15 позади, серверная ссылка `e4df0459` является предком `main` (чужой
  работы НЕТ), но в дереве одна незакоммиченная правка `ProjectSettings/EditorBuildSettings.asset`.
  Селектор чистоту проверяет — правку надо снять ДО аренды, и это уборка, а не работа этой ноги.
- **`WIN-U3`** — ЗАНЯТ (`c-exec-g-1d84-free-kits-probe-001:BUILD`).
- **`WIN-U4`** — дерево чисто, 24 позади, НО `origin/slot/win-u4` = `7e9556a1` несёт шесть коммитов
  ЧУЖОГО узла `g-37a1` (`i-slot-u4-remote-tip-carries-foreign-commits-001`). Первый же push встретит
  чужой tip. Разбирается ДО аренды.

Первая операция ноги:
`.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U<N> -Claim -LeaseId c-exec-g-1d84-host-under-the-judge-002:BUILD`.

## context

- **Расслоение — единственное, что здесь нельзя нарушить.** Решения хозяина считаются в слое правил
  без движка; движок только ПОКАЗЫВАЕТ. Сегодня `HouseholderRouteController` считает и показывает в
  одном месте, и это ровно то, что задача разводит.
- **Готовый поиск пути движка НЕ ПОДКЛЮЧАТЬ** (вырез 2 ставки), хотя `com.unity.ai.navigation`
  в проекте стоит. Иначе судьёй становится движок, ответ не проверяется тестом и может разойтись на
  двух машинах. Хождение по НАЗВАННЫМ местам дома — это `t-host-2`, и она ждёт `t-house-4`.
- **ХОЖДЕНИЕ СКВОЗЬ СТЕНЫ В ЭТОМ НАРЯДЕ НЕ ЧИНИТЬ — НО ЭТО ОТЛОЖЕНО, А НЕ ОТМЕНЕНО.** Хозяин сегодня
  протыкает обе пары перегородок: отрезок РТ01 (−3.5; −2.5) → РТ02 (3.5; −2.5) пересекает южные,
  РТ03 (5.11; 5.58) → РТ04 (−3.5; 2.5) — северные; проёмы (z ∈ (−1; 1)) не используются ни разу.
  Посчитано направлением. Владелец 2026-08-05 выбрал не чинить сейчас («пусть хотя ходит через
  стены») и в тот же день ПОПРАВИЛ ноги, которая прочла это как отмену: «Я имел в виду для
  выполненной задачи… но вообще, естественно, это надо будет чинить». Дом починки — `t-host-2`
  («путь по названным местам через названные проходы; в стены он не идёт»), и она чинит это по
  существу, а не заплаткой. Значит здесь: маршрут не переставлять и заплаток не ставить — но и не
  писать в отчёте, будто вопрос снят. Запись — `i-host-walks-through-walls-001`.
- **ПРЯМОЕ СЛЕДСТВИЕ ДЛЯ ЭТОЙ ЗАДАЧИ, которое стоит держать в голове при выборе шва:** раз путь
  хозяина скоро начнёт считаться по местам дома и проходам, шов «откуда хозяин берёт правду о мире»
  лучше сразу делать таким, чтобы `t-host-2` его РАСШИРЯЛА, а не переписывала. Это не требование и не
  `done_when` — это названный заранее контекст, чтобы не пришлось платить дважды.
- **Маршрут — челнок, а не круг:** `Householder.Step` разворачивается на концах массива точек.
  Это факт базы, а не дефект; менять поведение не заказано.
- **Сглаживание движения по сети и предсказание на клиенте ВЫРЕЗАНЫ** ставкой. Хозяин на второй копии
  поедет тиками, как ходоки и груз. Это ожидаемо и сюрпризом быть не должно.

## boundaries

- **Правила хозяина не переписывать.** `Householder.cs` и его типы в этой задаче не расширяются;
  прежние тесты (`HouseholderTests.cs`, `HouseholderAdditionalReactionTests.cs`) остаются зелёными
  без правки. Если правило ПРИДЁТСЯ тронуть — это один точный blocker домой с названной причиной.
- **СНИМОК ХОЗЯИНА — ЗАКАЗАН, ВТОРОЙ КАНАЛ — НЕТ.** Добавить хозяина в существующую рассылку курьера
  (свой тип снимка рядом с `WalkerSnapshot`/`CargoSnapshot`) — это и есть задача. А вот если
  понадобится ВТОРОЙ канал, второй RPC, второй тик или изменение порядка существующего тика — **СТОП
  И ВОПРОС ВЛАДЕЛЬЦУ**, а не решение исполнителя (`knowledge/how-the-game-is-built-layers.md:86`,
  рискованная догадка 4 ставки, названная заранее).
- **Порядок тика не менять.** `Cargo.ApplyCarryLoad` → `Walkers.Advance` → `Cargo.Advance` — принятый
  порядок. Хозяин встаёт в тот же тик; ГДЕ именно внутри порядка — решение ноги, и оно называется в
  RESULT с доводом.
- **Сцену хозяина `HostWalksHisDay.unity` не удалять и не править.** Её судьба решается отдельно.
- **Новых рабочих сцен не заводить** (вырез 8). **Дом не трогать** — ни `ThreeRoomHouse.prefab`, ни
  стены, ни их высоту (владелец отложил своим словом). **Груз и первое лицо не трогать.**
- **Закрытую папку `Assets/GasCoopGame/**` не трогать, не читать, не чинить**; её тесты не запускать.
  `Assets/CodeRespawn/**` и `Assets/Plugins/**` — тоже.
- **Проверок на то, что владелец видит глазами, не писать.** Тест слоя правил законен и нужен там,
  где неверный результат выглядит как верный — здесь это в первую очередь «хозяин на второй копии
  идёт из того же снимка, а не из своего локального кадра».
- **ЧУЖОЙ ДИФФ ОТ ОТКРЫТИЯ UNITY НЕ ПРИПИСЫВАТЬ СЕБЕ** (список — как в остальных нарядах:
  `.nuget-installed.json`, `McpPlugin*.dll`, `ReflectorNet.dll`, `Packages/manifest.json`,
  `Packages/packages-lock.json`, `ProjectSettings/EditorBuildSettings.asset`). Не stage, не revert,
  не clean, не stash.
- **Закрывающий отчёт пишет САМА нога** в своём слоте, до возврата домой.

## done_when

1. Решения хозяина считает создатель игры (сервер) в том же тике, где считаются игроки и груз.
   `HouseholderRouteController.Update()` перестаёт быть местом, где живёт симуляция хозяина.
2. Положение хозяина и его занятие едут в снимке рядом с ходоками и грузом.
3. На второй копии хозяин идёт так же — из снимка, а не из своего локального кадра.
4. Владелец увидел его одинаково на двух экранах — и сказал это своими словами.

Пункты 1-3 доказываются файлами, диффом, тестами слоя правил и прогоном. Пункт 4 доказывается ЕГО
словами; отчёт исполнителя их не заменяет.

## return

Полный продуктовый RESULT домой: точная база, точный кандидат, дифф по файлам, свой прогон, честный
закрывающий отчёт со статусом, состояние слота и аренды. Отдельно назвать: где именно внутри тика
встал хозяин и почему там; какой формы получился его снимок и почему; каким швом слой правил хозяина
получил доступ к миру (и совпал ли он с образцом `IWalkSpacePort`); понадобилось ли трогать курьера
сверх добавления снимка. Отклонения от `done_when` пишутся В RESULT домой, а не только во внутренний
PLAN.

budget: одна инженерная нога, режим ПРОБА

END_OF_FILE: live/indie-game-development/work/c-exec-g-1d84-host-under-the-judge-002-call.md
