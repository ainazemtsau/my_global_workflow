# CALL: хозяин берёт вещь в играемой сцене, и это видят оба клиента

CALL c-exec-g-5a7c-reach-wire-1-002

to: executor
direction: indie-game-development
play: work
node: g-5a7c
bet: bet-g-5a7c-wave-6
track: провод
for: t-reach-wire-1
repo: C:\projects\Unity\GasCoopGame_win-u2
basis: origin/main @ 42f499d3 — перемерено домом 2026-08-28; перемерить при старте

## goal

**Закрыть два оставшихся условия главной задачи волны.** Три из пяти закрыты 27 августа и
переделке не подлежат. Открыты ровно эти два, дословно из карточки задачи:

> **3.** В играемой сцене `IntegratedHouse` хозяин **по своему собственному решению** подходит,
> приседает, берёт предмет и ставит обратно. Не по нажатию клавиши, не в `ReachLab`.
>
> **4.** Два подключённых клиента видят одно и то же движение и одно и то же положение вещи.
> Расхождение между экранами — провал критерия, а не мелочь.

## что дом перемерил сегодня на `42f499d3` — принимай как вход, но перепроверь

**ХОРОШАЯ ЧАСТЬ: провод собрался целиком, и это новое с 27 августа.**
- `Network/NetworkWalkerCourier.cs:827` заполняет `ReachThingName` из `householderState.Reach.ThingName`;
- `Network/NetworkHouseholder.cs:250-268` разрешает имя и зовёт `ReachFor`, а прежнюю вещь кладёт;
- в боевом профиле появился настоящий поступок: `default.householder.json:74` —
  `"deed": { "do": "put-in-place", "thing": "Mug", "place": "Kitchen counter" }`;
- `Mug` реально описан в доме: `first-house.house.json:776-777`, комната Kitchen, на Dining table.

**ПЛОХАЯ ЧАСТЬ, И ОНА И ЕСТЬ ПРЕДМЕТ ЗАДАЧИ.**
- Носитель `InteractionObject` (GUID `41f425a4a83914b04af3f1642c000f5d`) во всём проекте
  **РОВНО ОДИН** — `Assets/TunnelCrew/Art/Loot/AlarmClockView.prefab`, и он же единственный префаб
  в `Art/Loot/` вообще.
- **У `Mug` модели с хватом нет.** То есть сегодня хозяин потянется за вещью, которую клиенту
  нечем взять.
- `AlarmClockView` не упоминается **ни в одном** `.cs`, `.asset` или `.unity` — проверено поиском.
  Рядом лежит `Settings/AlarmClockVisual.asset`, и что именно он делает, дом НЕ проверял.
- В доме 12 предметов лута, среди них `Alarm clock` — комната Bedroom, на `His bedside table`.

## ПЕРВЫЙ ШАГ — ЗАМЕР, ДО ЛЮБОЙ ПРАВКИ

**Появляется ли в играемой сцене модель с авторским хватом хоть для одного из 12 предметов?**
Зайди в `IntegratedHouse`, запусти, посмотри, чем нарисован `Alarm clock` и чем `Mug`.

- Если модель с хватом уже встаёт — дальше работа маленькая: направить поступок на неё.
- **Если ни один предмет не получает модель с хватом — это и есть настоящая работа задачи**, и её
  размер дом не знает. Назови его ЧИСЛОМ и, если он больше половины дня, **СТОП и вопрос домой**,
  а не тихое расширение. Обходных путей не искать.

## done_when

1. **ВИДНО В `IntegratedHouse`.** Хозяин по собственному решению — не по кнопке — подходит,
   приседает, берёт предмет и ставит обратно. Доказательство: прогон в редакторе, и в возврате
   сказано, за какой вещью он тянулся и куда её поставил.
2. **ДВА КЛИЕНТА ВИДЯТ ОДНО И ТО ЖЕ.** Движение и конечное положение вещи совпадают на обоих
   экранах. Расхождение — провал, а не мелочь; если оно есть, верни это фактом.
3. **РЕШЕНИЕ ОСТАЛОСЬ ЗА ХОЗЯИНОМ.** Ни одной новой кнопки, ни одного форсирования из редактора
   в боевом пути. Форсировать ради ПРОВЕРКИ можно — но тогда в возврате сказано, что прогон был
   форсированным, и отдельно показан прогон, где он решил сам.
4. **ЧИСЛА НЕ ЗАШИТЫ.** Всё, что решает «дотянется ли», «когда начать», «когда отпустить»,
   остаётся настройками. Это условие уже закрыто 27 августа — не сломай его.
5. **ТЕСТЫ ЗЕЛЁНЫЕ.** `dotnet test tests/TunnelCrew.Core.Tests -c Release` целиком зелёный. Их
   **564** на `42f499d3`; если число другое, скажи.

## boundaries

**ВЛАДЕЕТ НА ВРЕМЯ ЗАДАЧИ:** `Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`
(там правка в одну строку — имя вещи и место), `Assets/TunnelCrew/Scenes/IntegratedHouse.unity`,
`Network/**`, `World/HouseholderRouteController.cs`.

**ЭТО ВАЖНО:** файл профиля обычно принадлежит полосе `словарь`, и наряд на раздражение его ждёт.
Поэтому правка в нём — одна строка, и не больше. Всё остальное в этом файле не трогать.

**НЕ ТРОГАЕТ:** `Presentation/BodyReach.cs` — принадлежит полосе `анимация`, эта задача его только
зовёт. `Core/Householder/**` сверх необходимого.

**НЕ ДЕЛАЕТ:**
- **не авторит новый хват** — это отдельная задача `t-grip-second-1` со своим нарядом;
- не строит «хозяин замечает перемещённый лут»;
- не чинит материалы кожи хозяина (известный дефект, отдельная работа);
- не трогает шкалы и раздражение.

## return

Домой: коммиты; **чем нарисован предмет, за которым он тянулся**; что показал прогон в редакторе;
что показали два клиента; ЧИСЛОМ — сколько стоило поставить в сцену вещь, которую можно взять;
и одной строкой — нужна ли игре работа над библиотекой моделей лута, раз носитель хвата в проекте
один.

## budget

Полдня. Если первый шаг покажет, что модели с хватом не встают ни для одного предмета и это
дороже — СТОП и вопрос домой.

END_OF_FILE: live/indie-game-development/work/2026-08-28-call-reach-wire-finish.md
