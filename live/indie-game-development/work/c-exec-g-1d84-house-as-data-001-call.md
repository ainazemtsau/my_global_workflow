# CALL — c-exec-g-1d84-house-as-data-001

to: executor
direction: indie-game-development
node: g-1d84
task: t-house-4
track: дом
issued: 2026-08-06 by s-day-g-1d84-parallel-lane-calls-001
status: ready
slot: **WIN-U4** — назван владельцем; см. §Слот
base: `d60c2f04228bdfa161d6e122c56a2c5ccfec89f3`

## goal

Дом перестаёт быть коллайдерами и становится данными слоя правил: у комнат и мест появляются
смысловые метки, у описания дома — своё имя в слое правил и больше одного возможного производителя,
а проходы между местами существуют отдельно от вложенности. Владелец смотрит на список комнат, мест и
проходов, снятый с настоящей сцены.

## done_when (дословно из карточки, не пересказывать)

У комнат и мест есть смысловые метки; описание дома — отдельная названная вещь слоя правил, у которой
может быть больше одного производителя (снятие со сцены — первый); отдельно от вложенности мест
существуют названные проходы между местами. Правила видят дом списком, а не коллайдерами. Владелец
видит список комнат, мест и проходов, снятый с настоящей сцены.

## context — измерено первой рукой на объявленной базе

База спрошена у сервера: `main` = `origin/main` = `origin/dev` = `d60c2f04` (2026-08-05 17:47 +0300).
Каждый путь ниже проверен командой `git cat-file -e d60c2f04:<путь>` — все шестнадцать существуют.

**Дома в слое правил сегодня НЕТ ВООБЩЕ.** `Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef` имеет
`"references": []` и `noEngineReferences: true` — слой правил не ссылается ни на что. Описание дома
обязано лечь именно туда и НЕ имеет права затащить туда движок. Готовый образец шва, сделанный
соседней полосой: `IHouseholderWorldPort` объявлен в правилах
(`Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs:42`), реализован сценой
(`Assets/TunnelCrew/Network/NetworkHouseholder.cs`). Повторить эту форму, а не изобретать свою.

**Что дом такое сегодня.** `Assets/TunnelCrew/World/ThreeRoomHouse.prefab` — руками собранная коробка;
`Assets/TunnelCrew/World/HouseWall.cs` помечает одну стену, и её коллайдер служит одновременно
физической формой и данными о проходимости (`WorldBounds => _collider.bounds`). Это ровно то
состояние, из которого задача велит выйти: правила должны видеть список, а не коллайдеры.

**Маршрут хозяина сегодня.** Четыре точки лежат в `Assets/TunnelCrew/World/Householder.prefab`,
крутит их `Assets/TunnelCrew/World/HouseholderRouteController.cs`. Он ходит СКВОЗЬ перегородки —
четыре прохода за круг, посчитано по координатам. **Это известно, отложено владельцем и чинится НЕ
здесь**, а в `t-host-2` («путь по названным местам через названные проходы»), которая ждёт как раз
эту задачу.

**Сцена.** `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` — интегрированная; в ней уже стоят двое
сетевых носильщиков, физический груз, хозяин отдельным корнем и панель стартовых условий
(`Assets/TunnelCrew/World/StartConditions.cs`, только редактор, `#if UNITY_EDITOR`). Свойство «нажал
Play — уже в игре, без экранов» ломать нельзя.

**Больше одного производителя — это требование, а не пожелание.** Владелец 2026-08-06 решил, что
настоящий дом строится следующей волной отдельным узлом, а нынешняя коробка остаётся черновиком.
Описание дома переживёт замену коробки только если снятие со сцены будет ПЕРВЫМ производителем, а не
единственно возможным. Приваривать описание к сцене запрещено.

## boundaries

- **Логика ядра только РАСШИРЯЕТСЯ, не переписывается** (постоянное правило владельца). Правила
  хозяина (`Assets/TunnelCrew/Core/Householder/Householder.cs`) этой ногой не трогаются.
- **НЕ чинить хождение сквозь стены** — дом этой починки `t-host-2`.
- **НЕ поднимать стены** (сегодня 1.5 при глазах 1.7) — отложено владельцем до настоящего дома.
- **НЕ пересобирать планировку**, не добавлять комнат: вырез ставки «ровно три комнаты» держится, а
  настоящий дом уехал в следующую волну.
- **НЕ добавлять поиск пути движком** (NavMesh вырезан ставкой) и никаких рантайм-генераторов.
- **НЕ трогать профили хозяина** — параллельно в другом слоте идёт `t-host-3`; см. §Параллель.
- Мультиплеер строится сразу, а не прикручивается потом. Рабочая сцена законна, но только с НАЗВАННЫМ
  куском и панелью условий, и её корень едет в интегрированную сцену, как только существует.
- Продуктовый контракт — `AGENTS.md` на базе; ворота — `.\pwsh.cmd tools/check.ps1`.

## Параллель — читать до начала работы

Одновременно с этим нарядом в другом слоте идёт `c-exec-g-1d84-householder-profile-file-001`
(`t-host-3`, профили хозяина текстом). Раздел границы такой: **словарь дома — имена комнат, мест и
проходов — принадлежит ЭТОЙ ноге.** Соседняя ссылается на места непрозрачными именами и ничего о доме
не решает. Если эта нога обнаружит, что ей нужно менять правила хозяина — **СТОП И ВОПРОС ВЛАДЕЛЬЦУ**,
а не решение исполнителя.

## Хвосты — два решения владельца, которые дважды не поехали

Оба приняты 2026-08-05, оба должны были ехать «пунктом в первом же продуктовом наряде», и два наряда
подряд их не повезли. Едут здесь, и это не расширение объёма:

1. **Откат облачных сервисов Unity.** `ProjectSettings/UnityConnectSettings.asset:7` на базе —
   `m_Enabled: 1`. Его слово дословно: «откатываем». Вернуть `m_Enabled: 0`. Вопрос «оставлять ли»
   закрыт, открыто только исполнение.
2. **Закрытие мёртвого корня.** `c-exec-grid-v1-g01-document-authority-001` числится ACTIVE на стадии
   PLAN с 2026-07-21, реализации нет, при нём только `00-plan.json` и `01-plan-route-fix.json`. Его
   слово: «закрыть». Выдать терминальную квитанцию.

Если какой-то из двух снова не поедет — назвать причину в отчёте прямым текстом, а не умолчать.

## Слот

**`WIN-U4`** — `AVAILABLE`, `lease: none`, дерево чисто, `HEAD` = `d60c2f04` **ровно на базе**: это
единственный слот, которому не нужна подготовка. Каталог: `C:\projects\Unity\GasCoopGame_win-u4`.

```
.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U4 -Claim -LeaseId c-exec-g-1d84-house-as-data-001:BUILD
```

Не занят и не готов: `WIN-U1` (свободен, но 19 позади базы — нужен fast-forward), `WIN-U2` и `WIN-U3`
держат чужие аренды.

## return

RESULT домой, в Направление. Продуктовое слияние и зелёные ворота Направлением за закрытие НЕ
считаются: закрывает отдельная нога по своему тесту, и последнее слово по видимому — глаз владельца.
В отчёт положить ДОСЛОВНО тот список комнат, мест и проходов, который увидит владелец, и назвать, чем
именно он снят со сцены.

## budget

Одна нога. Полдня по замыслу нарезки. Если упрёшься в необходимость менять правила хозяина или
планировку дома — чекпойнт и вопрос, а не расширение.

END_OF_FILE: live/indie-game-development/work/c-exec-g-1d84-house-as-data-001-call.md
