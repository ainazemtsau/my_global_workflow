# CALL — c-converge-verify-g-5a7c-002

to: session
direction: indie-game-development
play: converge-verify
node: g-5a7c
issued: 2026-08-07 by s-converge-arch-g-5a7c-repair-001
status: ready

## goal

Свежая, ОТДЕЛЬНАЯ от работы сессия судит набор `g-5a7c` заново и пытается его СЛОМАТЬ. Судится набор
целиком: `work/converge-g-5a7c.md` (§DRIFT, §IMPORT, §GLOSSARY, §WHAT, оба §SIGNOFF, §CONTRACTS,
§COUNT) плюс `work/converge-g-5a7c-arch.md` (§RISK-LINE, §DECOMPOSE, `A1`–`A10`, §MINER,
§NOT-DECIDED, §SIGNOFF, §REPAIR-LEDGER). Вердикт — `verify_target: build`, `complete` и `smuggling`
по набору целиком.

## context

**ЭТО ВТОРАЯ ПРОВЕРКА УЗЛА, И ЭТО МЕНЯЕТ СТАВКИ. Сказано прямо, как требовал возврат первой.**
Первая — `history/2026-08-07-s-converge-verify-g-5a7c-001.md`, вердикт `complete=FAIL smuggling=FAIL`.
По плею **второй FAIL кончает бумажную фазу узла**: возврата в `converge`/`converge-arch` не будет,
поверхность замораживается уликой, и владельцу выносится ОДНО решение — резать, делить в `map` или
идти на том, что отвечено. Поэтому проверка обязана быть точной в обе стороны: и не пропустить дыру,
и не назвать дырой честно понижённую строку. **Строка, помеченная `open` с названным отвечающим, свою
клаузу ПОКРЫВАЕТ и находкой не является** — это законный исход, предписанный первой проверкой обеим
ремонтным ногам.

**Что произошло между первой проверкой и этим нарядом — две ремонтные ноги, ни одна дыра не заткнута
ответом.**

1. `s-converge-g-5a7c-repair-001` (§WHAT, разбор — `history/2026-08-07-s-converge-g-5a7c-repair-001.md`).
   `open_rows` вырос с 1 до 10, а не упал. Шесть строк заведены ОТКРЫТЫМИ (`W114`–`W119`), четыре
   понижены со снятой метки `acceptance` (`W39a`, `W54`, `W73`, `W94`), `W07b` потеряла обоснование
   целиком, `W103` ушла из открытых с собранным маршрутом. Заведён §COUNT.
2. `s-converge-arch-g-5a7c-repair-001` (арх-файл и §CONTRACTS, разбор —
   `history/2026-08-07-s-converge-arch-g-5a7c-repair-001.md`). Шесть находок отработаны понижением;
   заведена одна новая строка `A10` (пол) и два внутренних шва (`C27`, `C28`); заведён
   §REPAIR-LEDGER — таблица «что тронуто, что не тронуто», написанная РАДИ ЭТОЙ ПРОВЕРКИ, чтобы её
   можно было сверить командой.

**Счёт, который надо пересчитать МЕХАНИЧЕСКИ и сверить по обоим файлам. Счётов ТРИ, они меряют
РАЗНОЕ, и совпадать обязаны УТВЕРЖДЕНИЯ, а не числа — таблица смыкания стоит в §SIGNOFF арх-файла:**

- §WHAT (§COUNT): `what_rows: 122`, `open_rows: 10`, `open_owed_by_owner: 1` (`W94`),
  `open_owed_by_plan: 8`, `open_owed_by_shape: 1` (`W117`), `deferred: 2` (`W15`, `W16`),
  `acceptance_rows: 42`;
- §CONTRACTS: **12 краёв + 16 внутренних швов**, `contracts_open: 2` (`C05` → map, `C26` → OWNER);
- арх-файл: `arch_rows: 10`, `arch_open: 9` (`A1`–`A8`, `A10`), `arch_lines_owed_to_owner: 3`,
  §MINER 7 строк, **вердиктов владельца в наборе по-прежнему РОВНО ОДИН, подписей под текстом
  карточки — НОЛЬ.**

**Что ремонт арх-ноги утверждает про себя, и что надо атаковать в первую очередь.**

1. **Шесть выводов подтверждены первой проверкой и по существу НЕ тронуты** (`A1`, `A2`, `A3`, `A5`,
   `A6`, `A7`). В `A2` и `A5` добавлено по одному абзацу-указателю на `A10`. Проверить диффом, что
   добавлены именно указатели, а не новые выводы, и что ни один прежний вывод не переписан.
2. **`A10` (пол) — единственная НОВАЯ строка разбора, и она несёт арифметику, которой в наборе не
   было.** Утверждается: покоящийся груз при `_cargoMass` 50 отчитывается силой ≈409 Н при верхнем
   пороге `_cargoHeavyNoiseForce: 400`, то есть просто лежащий груз был бы самым громким событием
   игры. **Сам разбор называет это АРИФМЕТИКОЙ по объявленному поведению движка, а не наблюдением в
   запущенной игре.** Атаковать в первую очередь: верна ли арифметика; не стала ли она требованием;
   не выбрал ли разбор кривую опоры или число, объявив, что не выбирает.
3. **Четыре понижения — проверить, что каждое ПОНИЖЕНИЕ, а не переодетый ответ.** `C26` (состояние
   двери), `A4`/порог по числу рук, `A8`/«чем берут предмет», `A9`/«кто задевает обстановку». У
   каждого обязан быть назван отвечающий и НЕ сформулирован вопрос владельцу.
4. **Проверить, что владельцу не вынесено ни одного нового решения ни одной из двух ремонтных ног** —
   ни в `NOW.decisions`, ни текстом внутри набора.
5. **Проверить силу каждого токена владельца заново, включая правку `F7`.** Ремонт утверждает, что
   снял единственное место, где сила была выше настоящей («уточнение делает вердикт СИЛЬНЕЕ»), и что
   `LOG` за `s-converge-arch-g-5a7c-003` сознательно НЕ правлен задним числом.
6. **Проверить, что §WHAT, §COUNT, §IMPORT, §GLOSSARY, §DRIFT и оба §SIGNOFF converge-ноги ремонтом
   арх-ноги не тронуты ни знаком**, а §CONTRACTS не тронут ремонтом `converge`. Оба это утверждают —
   утверждение подлежит побайтовой проверке, а не приёму на слово.
7. **Проверить, что карточка `g-5a7c` не тронута ни знаком** ни одной из ног — диффом по `TREE.md`.
8. **Найти то, о чём набор по-прежнему не спрашивает.** Первая проверка назвала три предмета; два из
   них закрыты строками (`W117` — уставная линза 6, `W118` — «материальный результат»), третий
   (пол) стал `A10` и `W116`. Не назвать ни одного нового — это находка сама по себе.

**Объявленная база продуктовых фактов:** `C:/projects/Unity/GasCoopGame`, `main` =
`352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`, рабочее дерево чисто, только чтение. Перепроверить базу
первой рукой ДО любого `факт кода`. Числа и места, на которых стоит ремонт и которые дешевле всего
опровергнуть: компоненты `Floor` и `Delivery Yard Floor` в `Scenes/IntegratedHouse.unity` (и
отсутствие на них `MonoBehaviour`); `CarryStand.cs:9-26,66-78`; `WalkSpaceProbe.cs:47-70`;
`CargoBodyContact.cs:42-65`; `AuthoritativeCargoRoster.cs:110-135,196-204`;
`CargoNoise.cs:57-64,96-115`; `CargoHaul.cs:36-49,100-112,115-156`; `CargoBodyPort.cs:47-53`;
`HousePlace.cs:13`; `HousePassage.cs:36`; `NetworkPlaySettings.asset` (`_cargoMass: 4`,
`_cargoMaxHandForce: 200`, `_cargoPickupRange: 1.3`, `_cargoRustleForce: 60`, `_cargoKnockForce: 150`,
`_cargoHeavyNoiseForce: 400`); геометрия `East Room` (`_size.x: 4.25`, центр `x: 4.875`) против
`Outer East South` (`x: 7.25`) и `Delivery Yard Floor` (`x: 10`, масштаб `5 × 0.1 × 6`).

**Ограничение обеих ремонтных ног и этой проверки, названное прямо:** веерного опровержения внутри
сессии нет — в этом харнессе действует прямая постоянная инструкция владельца не разворачивать
субагентов и workflow, и по авторитету `os/KERNEL.md` живая инструкция владельца выше плея. Значит
эта проверка — ВТОРОЕ независимое опровержение набора, а не второй веер.

## boundaries

- Проверка НИЧЕГО не отвечает и НИЧЕГО не чинит. Находка возвращается строкой, а не заполняется.
- Владельцу не выносить ни одного НОВОГО решения. Понижённые строки называют его отвечающим — вопрос
  ему формулирует та нога, которая до него дойдёт, а не эта.
- `TREE.md` и `CHARTER.md` не трогать. Ставку, задачи и полосы не создавать. `knowledge/` не писать.
- Продуктовый репозиторий только на чтение.
- Свежая физическая сессия, отдельная от `s-converge-g-5a7c-real-core-001`, от всех
  `s-converge-arch-g-5a7c-*` (включая ремонтную), от `s-converge-g-5a7c-repair-001`, от
  `s-converge-verify-g-5a7c-001` и от любого day-чата.

## done_when

1. Вынесен `verify_target: build complete=<PASS|FAIL> smuggling=<PASS|FAIL>` по набору целиком, с
   построчным обоснованием. В `play_check` стоит `second_verification: PASS` и сказано прямо, что
   потолок второго FAIL применён или не применён.
2. Каждая из двенадцати клауз `done_when` карточки разбита атомарно и сопоставлена со строкой набора.
   Клауза, не дошедшая ни до одной строки, объявляется FAIL. **Клауза, дошедшая до строки `open` с
   названным отвечающим, ПОКРЫТА** — это законный исход, и назвать его дырой нельзя.
3. Проверено, что ни одна из шести находок первой проверки не вернулась в другом месте набора: `F1`,
   `F2`, `F4`, `F5`, `F7`, `F9` — по одной строке на каждую, с указанием, где именно смотрели.
4. §REPAIR-LEDGER арх-файла сверен КОМАНДОЙ, а не чтением: каждая клетка «что сделано» и «чего НЕ
   сделано» подтверждена или опровергнута диффом.
5. Три счёта сведены механически, и проверена именно та точка, на которой первая проверка нашла `F6`:
   не говорит ли какой-нибудь раздел то, что другой раздел того же набора уже называет неверным.
6. Сила каждого токена владельца проверена на «не выше настоящей», отдельно — правка `F7`.
7. Утечка HOW проверена в обе стороны: величина, формат, частота или раскладка, заехавшие в
   `acceptance` §WHAT; и `arch_in_context_only` — ни одна строка арх-файла, включая всю `A10`, не
   оказалась требованием и не поехала в `done_when`.

## return

Полный Direction RESULT. При PASS — `§SIGNOFF: converge-verify passed @ <date>` и `next` = `shape`,
с готовыми к копированию `acceptance`-строками §WHAT, §CONTRACTS и повесткой PLAN; открытые строки
едут в `shape` названными, а не молча. **При FAIL — потолок:** возврата в `converge`/`converge-arch`
НЕ выдавать, поверхность заморозить уликой и вынести владельцу ОДНО решение с тремя ветками (резать /
делить в `map` / идти на том, что отвечено), с рекомендацией и названным доводом ПРОТИВ рекомендации.

budget: одна свежая сессия

END_OF_FILE: live/indie-game-development/work/c-converge-verify-g-5a7c-002-call.md
