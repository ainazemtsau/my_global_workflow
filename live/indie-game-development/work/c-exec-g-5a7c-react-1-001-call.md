# CALL — c-exec-g-5a7c-react-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА здесь не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: householder
task: t-react-1
issued: 2026-08-10 by s-work-g-5a7c-tracks-dispatch-001
status: ready
basis: `4cf2b5dc5b7d6b22d580d57d97e3254fb061f230`, дерево
       `58105a5887d7131db608fd2831396c245882d5e5` — `origin/dev` = `origin/main`. Перемерено
       `git ls-remote origin` непосредственно перед выпуском. Это состояние уже содержит закрытую
       `t-situation-1` (слой ситуаций мира). Ground для свежего перемера, не freshness-lock.

**ПАРАЛЛЕЛЬНАЯ ДОРОЖКА ИДЁТ ОДНОВРЕМЕННО.** `cargo-hands` работает в слоте WIN-U1 по наряду
`c-exec-g-5a7c-cargo-1-001` от той же базы. Разделение файлов — жёсткое, ниже в `boundaries`.
Пин перемерен: `engineering_contract: 36` против продуктового `validation.config`
`synced_contract_version: 36`. Re-sync не нужен.

## slot

**WIN-U2.** Каталог `C:\projects\Unity\GasCoopGame_win-u2`, постоянная ветка `slot/win-u2`.

Перемерено настоящим селектором (`tools/select-slot.ps1 -Slot WIN-U2`, только чтение)
непосредственно перед выпуском наряда:

- `state: CLEAN`, `lifecycle: AVAILABLE`, `lease: none`;
- `branch: slot/win-u2`, **`head: 352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`**;
- `state-authority: C:\projects\Unity\GasCoopGame_slot-state\gascoop-slot-state.v1.json`;
- `mcp-endpoint: unrecorded`.

**СЛОТ ОТСТАЁТ ОТ БАЗЫ НА 49 КОММИТОВ, И ЭТО НАЗВАНО ЗАРАНЕЕ.** Перемерено:
`git rev-list --count 352f96b0..4cf2b5dc` = `49`, а `git merge-base --is-ancestor 352f96b0 4cf2b5dc`
проходит — то есть догоняется чистым fast-forward, без слияния и без force. **Селектор
fast-forward НЕ делает** (`fast-forwards: false` — перемерено в его же выводе), поэтому это
отдельный явный шаг.

**Порядок первых действий, и он не переставляется:**

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-react-1-001:BUILD`
2. `git fetch origin` и затем fast-forward `slot/win-u2` до
   `4cf2b5dc5b7d6b22d580d57d97e3254fb061f230` **только ff-only**. Любой отказ ff-only — **СТОП
   домой**: значит слот не чистый предок, и догонять его силой этому наряду нельзя.
3. Readback: `git rev-parse HEAD` = `4cf2b5dc…` и `git rev-parse HEAD^{tree}` = `58105a58…`,
   `git status` пуст. Только после этого начинается работа.
4. **Первый Unity-запуск в этом слоте переимпортирует проект после 49 коммитов и займёт заметное
   время — это ожидаемо, а не отказ.**

Любое расхождение, чужая аренда или не-AVAILABLE — СТОП домой; другой слот не выбирать, WIN-U1
занят параллельной дорожкой. Для Unity-действия записать и непосредственно перед вызовом
live-проверить endpoint именно этого lease; если прямой Unity MCP недоступен или не верифицируется
как WIN-U2 — **СТОП и вопрос владельцу**, без shell/HTTP/headless обхода.

## goal

**Реакция хозяина становится ОДНОЙ названной вещью, и шестая реакция добавляется одними данными,
без единой правки закона.**

Сегодня одна реакция — это член в ПЯТИ разных перечислениях, три ветки `switch` и одно поле
времени жизни, размазанное по трём файлам. Его требование дословно: «с шумом нужна чёткая
структура, которую мы потом можем легко расширять». Главный гейт задачи — не «структура есть», а
шестая реакция, приезжающая ДАННЫМИ и работающая, доказанная тестом.

Поведение игры при этом не меняется НИ НА ГРАММ: те же пять реакций, те же поводы, те же
приоритеты, те же времена жизни, то же вытеснение, та же одна ситуация за тик.

## context — перемерено чтением байтов на `4cf2b5dc`

### Во что сегодня обходится ОДНА реакция — посчитано по пятой, которую он оставил

`Assets/TunnelCrew/Core/Householder/Householder.cs` — 1674 строки.
`Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs` — 383 строки.

**ПЯТЬ перечислений, каждое получает свой член:**

| Перечисление | Строка объявления | Член пятой реакции |
|---|---|---|
| `HouseholderOccupation` | `:5` | `InvestigateLanding = 5` |
| `HouseholderPriority` | `:15` | `Landing = 5` |
| `HouseholderCauseName` | `:42` | `Landing = 5` |
| `HouseholderNoticedFactName` | `:52` | `Landing = 5` |
| `HouseholderEventName` | `:62` | `Landing = 4` |

**ТРИ ветки `switch`:**

- решатель — `Householder.cs:487-505` (`case HouseholderNoticedFactName.Landing when …`), рядом с
  тремя такими же (`:438`, `:455`, `:471`);
- событие → замеченный факт и время жизни — `Householder.cs:1021-1023`, рядом с тремя такими же
  (`:1004`, `:1010`, `:1015`);
- причина → замеченный факт — `Householder.cs:1667-1668`, рядом с тремя такими же
  (`:1661`, `:1663`, `:1665`).

**ОДНО поле времени жизни в ТРЁХ файлах:** `HouseholderWalkTuning` (`Householder.cs:663`, `:691`),
сериализованное поле и свойство `World/HouseholderWalkSettings.cs`, значение в
`Settings/HouseholderWalkSettings.asset:21` (`landingLifetimeSeconds: 8`).

**ПОПРАВКА К ТЕКСТУ ЗАДАЧИ, названная честно.** Карточка `t-react-1` говорит «размазана по ШЕСТИ
перечислениям плюс ветка решателя». Перемерено: перечислений, получающих член на реакцию, **пять**,
а веток `switch` — **три**, плюс поле времени жизни в трёх файлах. Требование строки от этого не
меняется, но цифра в наряде должна быть верной, а не переписанной с карточки.

### Ещё четыре перечисления, которые реакция ИСПОЛЬЗУЕТ, но не расширяет

`HouseholderPhysicalActionName` (`:33`), `HouseholderActionTargetName` (`:71`),
`HouseholderMovementPace` (`:80`), `HouseholderResponseKind` (`:87`), плюс фазовые
`HouseholderOccupationPhase` (`:25`) и `HouseholderResponsePhase` (`:95`). Их члены общие
(`WalkToward`, `SituationPoint`, `Routine`, `AcknowledgeOnArrival`), и новая реакция их
ПЕРЕИСПОЛЬЗУЕТ. Это и есть готовый словарь физического действия — переизобретать его не нужно.

### Словарь, которым обязана говорить эта работа — его собственные слова

`knowledge/host-shared-vocabulary.md`, владелец дословно 2026-08-02: «Иллюзию ума создаёт
небольшой общий словарь: намерение → замеченный факт → правдоподобная смена приоритета →
физическое действие → локальная память → возвращение к жизни.»

Карточка задачи требует четыре из шести прямо: **повод (какая ситуация) → приоритет → физическое
действие И ЕГО ЦЕЛЬ → возвращение к жизни.** «Схватить» всегда «схватить ЧТО» — цель не
опускается.

### Слой ситуаций, построенный общей задачей — вход этой дорожки

`Assets/TunnelCrew/Core/Situations/WorldSituation.cs`: `WorldSituationKind`
(`None/ContactedHouse/Landed`) и `WorldSituation` с `Kind`, `SourceId`, `Strength`, `PointX`,
`PointZ`. Список — `CarryWorld.Situations` (`IReadOnlyList<WorldSituation>`), живёт ровно один тик.
Проекция в стимул — `Core/Situations/WorldSituationNoise.cs`, `Loudest(...)` оставляет одну самую
громкую за тик. Хозяин берёт стимул в `AuthoritativeHouseholder.Observe(HouseholderStimulus, float)`
(`:172`).

**Записанное про порядок дословно, чтобы не изобретать заново:** «Ordering is report order.
Merging: none yet; every report stays in the list. Displacement: none in the list; the current
householder projection selects the loudest and keeps the earlier report on a tie.»

### Файл профиля — перемерен, и он НЕ то, чем кажется

`Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json` — 21 строка, и в ней
ТОЛЬКО распорядок: `formatVersion`, `profileId` и два дела (`walk-route` 12 с,
`wait-at-route-point` 3 с). Ни одной реакции, ни одного порога, ни одного времени жизни там нет.
Владелец предполагал обратное, поправка ему выдана. **Строка 5 задачи: файл профиля НЕ
расширяется.** Входит ли реакция в файл — эта задача НЕ решает.

### Что уже зелёное

База `4cf2b5dc` даёт **151 passed, 0 failed, 0 skipped** — прогнано Направлением на этой самой
голове. Реакции хозяина покрыты пятью файлами: `HouseholderTests.cs`,
`HouseholderAdditionalReactionTests.cs`, `HouseholderInTheJudgesTickTests.cs`,
`HouseholderWalksTheHouseTests.cs`, `HouseholderProfileTests.cs`, плюс `CargoContactNoiseTests.cs`
на конце «ситуация → реакция».

## проверка происхождения

| Строка наряда | Источник |
|---|---|
| реакция описана ОДНОЙ названной вещью: повод → приоритет → физическое действие И ЕГО ЦЕЛЬ → возвращение к жизни | подписанная карточка `t-react-1` `done_when` 1, дословно; словарь `knowledge/host-shared-vocabulary.md` его словами |
| все ПЯТЬ сегодняшних реакций выражены этой вещью, включая пятую | там же, `done_when` 2; его слово 2026-08-10 «Пятую реакцию оставляем» |
| поведение не изменилось, доказано существующими зелёными тестами БЕЗ правки их ожиданий | там же, `done_when` 3 |
| ШЕСТАЯ реакция добавляется одними ДАННЫМИ и работает, БЕЗ ЕДИНОЙ ПРАВКИ ЗАКОНА, доказано тестом | там же, `done_when` 4; его требование «с шумом нужна чёткая структура, которую мы потом можем легко расширять» |
| файл профиля НЕ расширяется | там же, `done_when` 5 |
| `IntegratedHouse.unity` не открывается; `NetworkPlaySettings.asset` принадлежит другой дорожке | `bet-g-5a7c-wave-2` `## tracks`, границы по файлам |
| приём больше одной ситуации за тик сюда НЕ входит | `bet-g-5a7c-wave-2` `## riskiest_assumptions` 2 — это `t-react-2` со своим откатом |

**Не импортируется** приём больше одной ситуации за тик, реакция на СОЧЕТАНИЕ ситуаций, зрение
хозяина, подставленный второй источник, занятия хозяина как намерение, видимые или слышимые полосы
шума — это `t-react-2`, `t-react-3` и вырезы волны.

## boundaries — что НЕ трогать

- **Задача не расширяется.** Пять строк карточки — весь объём. Поведение игры не меняется ни на
  грамм.
- **`Assets/TunnelCrew/Scenes/IntegratedHouse.unity` НЕ ОТКРЫВАЕТСЯ.** Задача, упёршаяся в файл
  сцены, — **СТОП и вопрос владельцу**, а не правка.
- **ФАЙЛЫ ПАРАЛЛЕЛЬНОЙ ДОРОЖКИ — ЧУЖИЕ, и это механизм, а не пожелание.** Дорожке `cargo-hands`
  принадлежат и этим нарядом НЕ ТРОГАЮТСЯ: `Core/Cargo/**`, `Network/CargoBody.cs`,
  `Network/NetworkPlaySettings.cs`, `Network/NetworkPlaySettings.asset`, и тестовые файлы
  `CargoHaulTests.cs`, `CargoCarryTests.cs`, `CargoLiftLawTests.cs`, `CargoPoseTests.cs`,
  `CargoWalkSpaceTests.cs`, `CargoClassSeamTests.cs`, `TelekineticPickupTests.cs`,
  `FirstPersonCarryTests.cs`. Если работа упёрлась в любой из них — **СТОП домой**.
- **ПУБЛИЧНАЯ СИГНАТУРА `CarryWorld.Advance` НЕ МЕНЯЕТСЯ.** Её вызывают тесты параллельной
  дорожки (`CargoCarryTests.cs`, десятки вызовов) и `Network/NetworkWalkerCourier.cs:160`. Менять
  её — значит сломать чужую полосу на слиянии. Внутренности `CarryWorld` править можно.
- **`Network/CargoBodyContact.cs` не трогается вовсе** — чинён общей задачей, в дорожках ничей.
- **Пороги `60 / 150 / 400` НЕ ДВИГАЮТСЯ и специального случая под волочение не заводится** — это
  запрет самой улики `i-dragged-cargo-rustles-every-tick-001`, и её работа принадлежит `t-react-2`.
- **ДАННЫЕ РЕАКЦИИ НЕ КЛАДУТСЯ В `NetworkPlaySettings.asset`** — этот файл принадлежит дорожке
  `cargo-hands`. Дом для данных реакции выбирает исполнитель (код или собственный ассет хозяина), и
  этот выбор называется в возврате с причиной.
- **Файл профиля `default.householder.json` не расширяется** и загрузчик профиля не открывается.
- **Пятая реакция сохраняется целиком:** тот же повод, тот же приоритет, то же занятие, то же время
  жизни `8`, то же вытеснение. Меняется форма описания, не содержание.
- **`ProjectSettings/**` не трогать.** `Assets/GasCoopGame/**` закрыта.
- Самодельный сканер исходников уликой не является и не пишется.

## owner-visible check

**Проверка у этой задачи обратная обычной: всё должно быть как вчера.** Тот же хозяин, те же пять
реакций, те же пороги, тот же бег и та же караулка. Любое замеченное отличие — это разрыв, и его
называют, а не объясняют.

Владелец эту задачу руками НЕ судит и не должен: его вердикт по этой дорожке назначен на
`t-react-3`, в самом её конце, и это нарочно — чтобы он не судил два недоделанных вечером одного
дня. Наряд не ставит ему условий.

## headless evidence

- **Тот же набор тестов зелёный без правки ожиданий.** База `4cf2b5dc` даёт **151 passed, 0
  failed, 0 skipped**; после этой задачи — столько же проходящих плюс новые. Механическая
  подстановка переименованных символов разрешена и правкой ожидания НЕ является; изменение любого
  ожидаемого значения, любого числа и любого смысла утверждения — является. Доказать диффом по
  `tests/`.
- **ГЛАВНЫЙ ГЕЙТ — ШЕСТАЯ РЕАКЦИЯ ДАННЫМИ, и он доказывается ТЕСТОМ, а не рассуждением.** Новый
  тест добавляет шестую реакцию ОДНИМИ ДАННЫМИ и показывает, что она срабатывает: повод замечен,
  приоритет применён, физическое действие с целью выбрано, возвращение к жизни произошло. Тем же
  приёмом, каким в первой волне класс груза был доказан ВТОРЫМ классом
  (`CargoClassSeamTests.SecondTestOnlyClassChangesEveryReaderWithoutChangingTheRule`): шестая
  реакция живёт ТОЛЬКО в тесте, в игре реакций остаётся пять.
- **Гейт на «без единой правки закона»:** дифф этого теста и его данных не содержит правок
  решателя, перечислений и веток `switch`. Предъявляется диффом, а не обещанием.
- **Пятая реакция цела по ЧИСЛАМ:** занятие `5`, приоритет `5`, причина `5`, замеченный факт `5`,
  событие `4`, время жизни `8` на HEAD — вставить readback.
- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`.
- Через прямой MCP точного слота — настоящая Unity import/compile evidence.
  **Зелёные ворота поведением игры не являются и так не предъявляются**
  (`knowledge/green-gates-do-not-compile-the-game.md`).

## откат

- **Точка возврата — `4cf2b5dc5b7d6b22d580d57d97e3254fb061f230`**, дерево
  `58105a5887d7131db608fd2831396c245882d5e5`.
- Откат назвать ОДНОЙ точной командой и **исполнить проверкой первой рукой**: после отката
  `git rev-parse <ветка>^{tree}` возвращает дерево базы знак в знак. Выведенный из истории откат
  вместо исполненного — назвать выведенным, а не выдавать за проверенный.
- Прямо заявить, что для отката не нужно ничего вне репозитория.

## done_when

1. **Реакция стала одной названной вещью, и все пять сегодняшних выражены ею.** Существует
   названный тип, описывающий реакцию словарём владельца: повод (какая ситуация) → приоритет →
   физическое действие И ЕГО ЦЕЛЬ → возвращение к жизни. Все пять сегодняшних реакций, включая
   пятую («расследовать приземление»), описаны ИМ и берутся оттуда, а не из пяти перечислений и
   трёх веток `switch`. Дом данных реакции назван, и он НЕ `NetworkPlaySettings.asset` и НЕ файл
   профиля.

2. **ШЕСТАЯ реакция добавляется одними данными и работает — доказано тестом.** Тест добавляет
   шестую реакцию, не правя ни решателя, ни перечислений, ни веток; она замечается, получает
   приоритет, вызывает физическое действие с целью и возвращает хозяина к жизни. Дифф теста
   предъявляется как доказательство, что закон не тронут. В игре реакций остаётся ПЯТЬ — шестая
   живёт только в тесте.

3. **Поведение не изменилось ни на грамм, границы целы, откат проверен.** Существующие тесты
   зелёные (`151 passed, 0 failed, 0 skipped`), ни одно ожидание не переписано — только
   механическая подстановка переименованных символов, доказанная диффом по `tests/`. Пятая реакция
   цела по числам (занятие `5`, приоритет `5`, причина `5`, факт `5`, событие `4`, время жизни `8`).
   Правило «одна самая громкая ситуация за тик» НЕ тронуто. Границы целы — сцена, файлы дорожки
   `cargo-hands`, публичная сигнатура `CarryWorld.Advance`, `CargoBodyContact.cs`, пороги
   `60 / 150 / 400`, файл профиля и `ProjectSettings/**` не тронуты, доказано диффом. Откат назван
   одной командой и проверен readback до дерева базы `58105a58`.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / householder / t-react-1`: точные
коммиты и их продуктовая диспозиция; scoped manifest/diff и точные outputs проверок. Также
обязательно:

- **где живёт описание реакции** — точный путь, имя типа, имена полей — и как каждое из четырёх
  слов словаря легло на поле; какое слово легло плохо, если легло;
- **где живут ДАННЫЕ пяти реакций** и почему именно там, раз ни настройки сети, ни файл профиля
  нельзя;
- **дифф теста шестой реакции** с прямым заявлением, что решатель, перечисления и ветки в нём не
  правились;
- **что осталось от пяти перечислений и трёх веток** — что удалено, что осталось и почему;
- **readback чисел пятой реакции** и значения `8` на HEAD;
- **что придётся тронуть в день, когда приедет реакция на СОЧЕТАНИЕ ситуаций и когда хозяин начнёт
  слышать больше одной за тик** — списком мест (это готовит `t-react-2`, но ею не является);
- проверенный откат: команда и readback дерева базы;
- терминальный readback селектора слота `CLEAN / AVAILABLE / lease none`;
- **отдельной строкой: с какой головы параллельной дорожки собиралось перед публикацией** — по
  клаузе 5 `evaluator` этой волны ни одна дорожка не публикует в `main` состояние, которого не
  собирала вместе с текущей головой другой;
- каждое расхождение свежей базы с grounds этого наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает.** После HOME отдельная нога Направления
читает возврат первой рукой и закрывает `t-react-1` по букве `evaluator` этой волны, называя вслух,
каким из двух путей шла.

## budget

Один engineering root. Ноль нового поведения, одна новая форма описания и один тест на шестой
реакции. Половина дня работы. Приём больше одной ситуации за тик, комбинация, зрение и подставленный
источник сюда не входят. Не помещается без расширения границ — STOP HOME с одним точным blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-react-1-001-call.md
