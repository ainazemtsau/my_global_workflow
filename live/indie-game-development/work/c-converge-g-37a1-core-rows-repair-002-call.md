CALL c-converge-g-37a1-core-rows-repair-002
to: session
direction: indie-game-development
play: converge
node: g-37a1
goal: |
  Repair `work/converge-g-37a1-core-rows.md` on the eight named findings R1–R8 of
  `history/2026-07-28-s-converge-verify-g-37a1-core-rows-002.md` — and on NOTHING else. The narrow
  rerun of step 3 found that **all sixteen previous repairs CLOSE their charges** and that **both of
  the previous repair's judgment calls survive**; what fails is text the previous repair wrote, plus
  three roll-up lines its own CALL forbade it to touch. Every one of the eight is a
  one-to-three-sentence fix and none needs the owner. When they hold, step 3 reruns narrowly again.
context: |
  THIS IS STILL STEP 3 OF THE OWNER'S OWN SIX-STEP ROUTE (`NOW.md` `d-post-verify-route-001`), which
  no leg may reorder without his words, and it adds no step: `os/plays/converge-verify.md` step 4
  routes a build FAIL back to `converge`, and this is the second time it has done so on the same
  target. Steps 1, 2 and 4 are COMPLETE. Step 5 — his ladder verdict — still waits on step 3
  passing.

  READ, in this order:
  - `history/2026-07-28-s-converge-verify-g-37a1-core-rows-002.md` — the eight findings R1–R8, each
    with what it hits, where it routes and its two-strikes note; the two judgment calls that
    SURVIVED and why, so neither is re-opened; and the eight raw findings this run REFUTED or
    DOWNGRADED, which must not be re-raised.
  - `live/indie-game-development/work/converge-g-37a1-core-rows.md` — the target.
  - `history/2026-07-28-s-converge-verify-g-37a1-core-rows-001.md` — the sixteen ORIGINAL findings,
    needed only to confirm that a fix does not undo a closed one, plus its seven refutations and
    four confirmations, which also stay closed.
  - `live/indie-game-development/TREE.md` `g-37a1` — the fifteen criteria, his verdict on exact
    text. A finding against a CRITERION is out of scope and none of the eight is one.
  - `live/indie-game-development/work/core-requirements-g-37a1.md` — the sixteen live lines, the
    NOT-core list and, decisively for R1, **the settings register**, whose line «Где стоит база и
    сколько воздуха она возвращает | ручка» settles R1 without the owner. Its «Цена смерти (сейчас
    ноль)» row remains stale and remains outside this leg's authority.

  Do NOT read `history/2026-07-28-s-converge-g-37a1-core-rows-001.md` or
  `history/2026-07-28-s-converge-g-37a1-core-rows-repair-001.md`'s reasoning as authority over the
  findings: the RESULT of the rerun is the charge you answer.
boundaries: |
  REPAIR ONLY WHAT IS NAMED. Sections in scope: §KNOBS (R1), §RULES (R2, R3), ROW 15 (R4), ROW 9
  (R5), §WHAT's criterion-11 routing sentence (R7) and §WHAT's acceptance-labour block (R8), plus
  ROW 12's OUTSIDER FAIL list only so far as R1 requires.

  **SCOPE IS WIDENED BY EXACTLY THREE LINES, and this is deliberate.** The previous repair CALL put
  §ORACLE and §BUILD-CLOSES-BETTER out of scope, and R6 shows a section-scoped repair CALL cannot be
  self-consistent: a fix that STRIKES a claim leaves every roll-up restating it stale, and roll-ups
  live in other sections by construction. **In scope, and nothing else in those two sections:**
  §BUILD-CLOSES-BETTER's listing of «ROW 10's ban» as closed by a document; §ORACLE class 13's
  "four seams, all decidable now"; §ORACLE class 14's "the guard against an eternal laboratory is
  that planning of the next version does not wait". Each is corrected to match the repaired text it
  restates. **§ORACLE's score-line arithmetic is NOT in scope** — it is wrong and it predates both
  repairs, it is recorded in the rerun's captures, and fixing it here would exceed this CALL.

  OUT OF SCOPE and not to be re-opened: §OWNER; §GLOSSARY, §SEAM and §SIGNOFF, whose repairs all
  held; ROWS 1, 2, 3, 4, 6, 8, 10, 11, 13, 14, 16 and 17; ROW 7, retired in place with its number
  kept. **ROW 16's strike of late-join and ROW 5's second-authored-level requirement both SURVIVED
  their attack and are CLOSED** — do not re-raise, re-argue or soften either; the rerun added a
  derivation for the first (criterion 9's full restart plus ROW 16's «both dig in the same section»
  entail a session-wide reload, so nothing joins anything) which may be written into ROW 16's FORCES
  as a citation, and that is the only permitted touch of ROW 16.

  NOT REOPENABLE, on his exact words: passability is deleted; there are no sinks; the way up is made
  by the player; two gases now with liquid as headroom; breach behaviour is an EVENT whose form is a
  SETTING; there is NO value below in the core; death restarts the level in full; air refills ONLY
  at the base; the rig carries a level button, a start state and an immortality switch, all
  defaulting to off; the core has no completion condition; and the core is NOT graded on «весело».

  THE SETTING RULE STILL GOVERNS. No value may be chosen for K1–K5, air capacity, the base's refill
  or the step rate. R1 is a CLASS question — which table a parameter belongs in — not a value
  question, and it is answered by his register, not by a judgment. R2's fix may not weaken a test:
  the choice is between re-classing rule 2 out of BEDROCK and committing a minimum session content,
  and either is a sharpening.

  Do not answer `d-air-counter-visibility-001`; it stays open. Do not manufacture an owner decision —
  R7 exists precisely because one was manufactured. Do not open a bet, a task or a track. Do not
  read `archive/**` or the product repository; every engineering fact is carried by an existing
  first-hand receipt, including R5's solver constants at `i-flow-model-unmeasured`.
done_when: |
  1. Each of R1–R8 is resolved inside its named section, or is returned with a stated reason why it
     cannot be resolved without exceeding this CALL.
  2. R1 is resolved by CITATION to the owner's settings register, not by a preference: base
     placement appears in exactly one §KNOBS table, ROW 15's FORCES agrees with it, and ROW 12's
     OUTSIDER FAIL list is consistent with the result.
  3. R2 and R3 leave §RULES with a pass rule that can FAIL a wrong build and PASS a right one — the
     stimulus is controlled to the same standard as the three decks, and the wrong-rule FAIL is
     scoped so two administrators reach the same verdict.
  4. R4 and R5 leave ROW 15 and ROW 9 with exactly one row-level verdict rule each, covering every
     scored part, with no part carrying a FAIL condition that decides nothing.
  5. R6's three lines match the text they restate.
  6. R7 leaves criterion 11's clause held by a constraint that forbids what he forbade — deferring
     the network basis past `g-37a1` — and does not forbid ordinary sequencing of `g-37a1`'s own
     rows across its own bets. **The `NOW.md` copy is already corrected by the rerun; §WHAT must
     match it, not the reverse.**
  7. R8 leaves the acceptance-labour count correct and says which rows carry a committed instrument.
  8. Nothing else in the file is edited, no `§SIGNOFF` is written — a repair does not sign its own
     work — and every change is marked in place with its finding number and the date, as the
     previous repair did, so the next rerun can find them.
return: |
  One `converge` RESULT, then `c-converge-verify-g-37a1-core-rows-003` — a third narrow rerun of
  step 3 in a fresh chat, on what this repair changes.

  **KERNEL TWO-STRIKES IS LIVE AND ITS BOUNDARY IS NAMED.** The rerun adjudicated it in the open and
  did not fire it: no finding repeated a charge. **It fires next time on either of two points.** A
  THIRD charge that §KNOBS' governance classes are inconsistent — RIG versus LEVEL-AUTHORING — is
  the second failed correction round on that point. A SECOND charge against §RULES' committed pass
  rule is the second failed correction round on that one. If either occurs, the verification
  RESULT **checkpoints and says so** rather than issuing a fourth repair CALL into the same loop,
  and the checkpoint goes to the owner as a readable brief about the route, never as another repair.
budget: one session, no owner.
surface: a FRESH chat, separate from the rerun that produced these findings, from the first repair,
  from the first verification and from the session that wrote the rows.

after: |
  Step 5 — the owner's ladder verdict (`i-engine-fit-decision-ladder`) — still waits on step 3
  passing, and it is an OWNER step: it reaches him as a brief with options and a recommendation and
  is never decided by a leg. Step 6 — `shape` — is the first leg that may create a bet or a task,
  and it is the only place the PRE-BUILD stretch can be guarded, by `appetite` and `kill_by`.

disposition: |
  **CANCELLED 2026-07-28, NEVER RUN, by the owner's own decision on the acceptance format**
  (`NOW.md` `d-core-acceptance-format-001`; quoted in full at the top of
  `work/converge-g-37a1-core-rows.md`). This CALL was eight one-to-three-sentence repairs to the
  acceptance apparatus. **Five of the eight — R2, R3, R4, R5 and R8 — were against text the cut
  deletes outright**, so repairing them would have been work on paper scheduled for deletion. The
  remaining three — R1 (§KNOBS base placement), R6 (three stale roll-ups), R7 (§WHAT criterion 11) —
  are folded into the cut leg `s-converge-g-37a1-core-rows-cut-001` and are resolved there.
  Its receipt: `history/2026-07-28-s-converge-g-37a1-core-rows-cut-001.md`. The successor is
  `c-converge-verify-g-37a1-core-rows-cut-check-001` — ONE short independent look, not a third full
  sweep. **KERNEL two-strikes never fired and its boundary is moot**: the sections it guarded
  (§KNOBS' governance classes, §RULES' committed pass rule) no longer carry a scoring apparatus to
  charge a third time.

END_OF_FILE: live/indie-game-development/work/c-converge-g-37a1-core-rows-repair-002-call.md
