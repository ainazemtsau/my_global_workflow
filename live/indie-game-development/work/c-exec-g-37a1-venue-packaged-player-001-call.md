# CALL c-exec-g-37a1-venue-packaged-player-001

> **RETIRED FROM DISPATCH — REPLACEMENT CLOSE PENDING.** PAIR-FREEZE rejection is recorded at
> `2a66cd10756ae17dff709a85d2e6f499f31e3dd4`; this CALL and its PAIR-CANDIDATE are historical evidence and MUST NOT
> be resumed or repaired. After Re-sync v34, only
> `c-control-g-37a1-venue-packaged-player-replace-close-001` may process-close this root as `REPLACED`.

direction: indie-game-development
track: t-venue
for: t-3
node: g-37a1
to: executor (engineering root CALL, product repo)
issued: 2026-07-28 by s-shape-g-37a1-core-bet-001
amended: 2026-07-28 by s-repair-g-37a1-exec-call-contract-001 (bounced by the product contract; see §Что было исправлено)

engineering_contract: 31
stage: PLAN (root entry; the repo runner carries the leg through its own ladder)
slot: НАЗЫВАЕТ ВЛАДЕЛЕЦ В СООБЩЕНИИ (этого требует контракт продукта). Рекомендация — `WIN-U3`;
      `WIN-U1` занят владельцем 2026-07-28, `WIN-U2` отдан парному наряду по телу
budget: одна инженерная нога по контуру продукта

## goal

Владелец запускает игру ДВОЙНЫМ КЛИКОМ по собранному плееру — без Unity, без консоли, без ручного
ремонта состояния. Повторный запуск работает и приходит в то же стартовое состояние.

Идёт ПАРАЛЛЕЛЬНО остальным полосам: трогает сборку и инструменты, а не симуляцию.

## slot / контур (обязательная часть, проверено 2026-07-28)

- **Слот называет владелец в сообщении с задачей** — так требует контракт продукта, и агент выбирать
  не вправе. Этот файл слот не фиксирует: он даёт рекомендацию, а обязательным является слово владельца.
  **Рекомендация — `WIN-U3`** (`C:\projects\Unity\GasCoopGame_win-u3`, Target **Local**, постоянная
  ветка `slot/win-u3`). Запасной — `WIN-U4`.
- **`WIN-U1` занят владельцем** (сказано им 2026-07-28); `WIN-U2` отдан парному наряду по телу.
- **Селектор не видит открытый Unity Editor.** `tools/select-slot.ps1` читает жизненный цикл и лиз, а
  не то, работает ли владелец в этом каталоге прямо сейчас: 2026-07-28 он показал `AVAILABLE` для всех
  четырёх слотов, включая занятый `WIN-U1`. Поэтому перед работой — и селектор, и слово владельца.
  Не-AVAILABLE, несовпадение ветки или расхождение с названным слотом — STOP.
- **`C:\projects\Unity\GasCoopGame_dev` (WIN-CTRL) — НЕ рабочее место.** Это только интеграция; feature-WIP
  там запрещён. Предыдущая редакция этого CALL направляла работу туда и была справедливо отбита.
- Не создавать, не удалять и не перемещать worktree; не создавать, не переключать, не переименовывать,
  не сбрасывать и не удалять ветки.
- `engineering_contract: 31` — пин корневого CALL; все его продолжения внутри ноги наследуют пин.
- Вход — стадия **PLAN**. Дальше нога идёт по собственной лестнице репозитория продукта
  (PLAN → PAIR-CANDIDATE → PAIR-FREEZE → BUILD → гейты), записывая свои stage receipts там же.
  ДОМОЙ в направление возвращается только терминальный REPORT или настоящий ESCALATE.
  Продуктовые сессии не выпускают Direction-CALL'ов.

## context (CALL самодостаточен)

**Спецификация требований (абсолютный путь):**
`C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\core-requirements-g-37a1.md`
Обслуживаемая строка — **1**: «Игра запускается как игра: двойной клик по сборке, без Unity и без
консоли; повторный запуск работает». Это ПЕРВЫЙ критерий карточки узла и он гейтит доказательства
почти всех остальных: каждая строка называет собранный плеер как своё свидетельство.

Машинная проверка, которую нога обязана сделать зелёной, — **M7** из §CHECK файла
`C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\converge-g-37a1-core-rows.md`:
«собранный плеер запускается, и повторный запуск приходит в то же стартовое состояние при действующей
конфигурации».

**Что установлено про сам продукт:** во всём репозитории нет ни одного скрипта, собирающего плеер
(36 скриптов в `tools/`, ни один не собирает), и ни одного упоминания сборки плеера в 53 ADR. Это тот
пункт, который вылезает в последний момент, поэтому он идёт первой волной.

**Второй потребитель работы, за пределами узла:** карточка `g-2b7f` (страница Steam) ждёт пять честных
геймплейных скриншотов ИЗ СБОРКИ g-37a1. Собранный плеер — то, из чего их снимают.

## boundaries

- Только венью: скрипт сборки, собранный плеер и его повторный запуск.
  **Сцену НЕ создавать и список сцен не менять**, если PLAN не покажет, что иначе плеер не собрать:
  текущая разрешённая стартовая сцена уже единственная в Build Settings, и этой ноге достаточно её.
  Наполнение сцены содержимым принадлежит тем полосам, которые это содержимое делают.
- Ни симуляции, ни тела, ни картинки, ни арта, ни меню, ни экрана загрузки.
- Внутриигровых ручек здесь не строить: стартовое состояние и бессмертие — следующая работа этой же
  полосы.
- Требования направления не меняются. Если PLAN считает, что требование надо изменить — это не решение
  продуктовой сессии: назвать и вернуть.
- Недоступный инструмент, явно требуемый авторитетами репозитория (Unity, лицензия сборки, headless-раннер)
  **ОСТАНАВЛИВАЕТ ногу и спрашивает владельца.** Никаких обходов. Самописный сканер исходного текста
  запрещён как доказательство поведения: «плеер запускается» доказывается запуском.

## done_when

1. В репозитории есть воспроизводимый скрипт сборки плеера, и он отработал.
2. Собранный плеер существует по названному пути, запускается двойным кликом, закрывается и
   запускается повторно, приходя в то же стартовое состояние при действующей конфигурации.
3. Проверка **M7** написана и зелена в собственном гейте продукта.
4. Из этой сборки физически можно снять скриншот (окно рисует сцену).
5. Гейты продукта зелёные по своему регламенту.

## return

Терминальный REPORT домой в направление: точный путь сборки, коммит, результат гейтов. Отдельно
назвать, что владелец должен сделать своими руками — шаг, отправленный владельцу, не считается
выполненным, пока он не подтвердил.

## Что было исправлено 2026-07-28 (нога отбила первую редакцию, и была права)

1. **Работа направлялась в `GasCoopGame_dev` (WIN-CTRL)**, где feature-WIP запрещён. Теперь — слот `WIN-U1`.
2. **Не был выбран обязательный слот `WIN-U1..WIN-U4`.** Теперь выбран, и его доступность проверена.
3. **Не было пина `engineering_contract: 31`** и не была названа стадия входа. Теперь есть оба.
4. **`done_when` требовал «названную сцену в Build Settings»**, не называя её, при том что разрешённая
   стартовая сцена уже единственная в списке. Пункт снят: он был уже выполнен, и нога сузилась до того,
   чего действительно нет, — скрипта сборки и собранного плеера.

Причина дефекта: корневой CALL был написан без чтения `AGENTS.md` продуктового репозитория.
Отбой исполнителя — правильное поведение, а не помеха.

END_OF_FILE: live/indie-game-development/work/c-exec-g-37a1-venue-packaged-player-001-call.md
