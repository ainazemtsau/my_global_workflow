# CALL c-exec-g-37a1-venue-packaged-player-001

direction: indie-game-development
track: t-venue
for: t-3
node: g-37a1
to: executor (engineering CALL, product repo)
issued: 2026-07-28 by s-shape-g-37a1-core-bet-001
budget: одна строительная нога

## goal

Владелец запускает игру ДВОЙНЫМ КЛИКОМ по собранному плееру — без Unity, без консоли, без ручного
ремонта состояния. Повторный запуск работает и приходит в то же стартовое состояние.

Идёт ПАРАЛЛЕЛЬНО всему: трогает настройки проекта и сборку, а не симуляцию.

## context (CALL самодостаточен)

**Продуктовый репозиторий:** рабочее дерево `C:\projects\Unity\GasCoopGame_dev`, ветка `dev`.
Не создавать новых worktree.

**Спецификация требований (абсолютный путь):**
`C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\core-requirements-g-37a1.md`
Обслуживаемая строка — **1**: «Игра запускается как игра: двойной клик по сборке, без Unity и без
консоли; повторный запуск работает». Это ПЕРВЫЙ критерий карточки узла, и он гейтит доказательства
почти всех остальных: каждая строка называет собранный плеер как своё свидетельство.

Машинная проверка, которую эта работа обязана сделать зелёной, — **M7** из §CHECK файла
`live/indie-game-development/work/converge-g-37a1-core-rows.md`: «собранный плеер запускается, и
повторный запуск приходит в то же стартовое состояние при действующей конфигурации».

**Почему это самая незаметная дыра в продукте, установлено первыми руками 2026-07-28:**
`EditorBuildSettings` содержит ровно ОДНУ включённую сцену — пустую заготовку URP-шаблона, — при том
что реальных сцен в проекте девять и ни одна в список не входит; во всём продукте нет ни одного
скрипта, собирающего плеер (36 скриптов в `tools/`, ни один не собирает), и ни одного упоминания
сборки плеера в 53 ADR. Это тот пункт, который вылезает в последний момент, поэтому он идёт первой
волной, а не последней.

**Второй потребитель этой работы, за пределами узла:** карточка `g-2b7f` (страница Steam) ждёт пять
честных геймплейных скриншотов ИЗ СБОРКИ g-37a1. Собранный плеер — это то, из чего их снимают.

## boundaries

- Только венью: список сцен, скрипт сборки, собранный плеер и его повторный запуск. Ни симуляции, ни
  тела, ни картинки.
- Ничего не выдумывать про содержимое сцены: она может быть пустой или содержать то, что уже есть.
  Сцена наполняется другими полосами.
- Внутриигровых ручек здесь не строить, кроме подготовки места под них: стартовое состояние и
  бессмертие — отдельная работа этой же полосы, следующая по очереди.
- Никакого арта, никакого меню, никакого экрана загрузки.
- Недоступный инструмент (Unity не запущен, нет лицензии на сборку, нет headless-раннера)
  **ОСТАНАВЛИВАЕТ ногу и спрашивает владельца.** Никаких обходов. Самописный сканер исходного текста
  запрещён как доказательство поведения: «плеер запускается» доказывается запуском, а не чтением
  конфигурации.

## done_when

1. Названная сцена включена в `EditorBuildSettings` и является той, что запускается.
2. В репозитории есть воспроизводимый скрипт сборки плеера, и он отработал.
3. Собранный плеер существует по названному пути, запускается двойным кликом, закрывается и
   запускается повторно, приходя в то же стартовое состояние при действующей конфигурации.
4. Проверка **M7** написана и зелена в собственном гейте продукта.
5. Из этой сборки физически можно снять скриншот (окно рисует сцену).
6. Гейты продукта зелёные на ветке `dev`.

## return

RESULT домой в направление, с точным путём сборки, коммитом/веткой и результатом гейтов. Отдельно
назвать, что именно владелец должен сделать своими руками, если что-то из проверки требует его машины —
шаг, отправленный владельцу, не считается выполненным, пока он не подтвердил.

END_OF_FILE: live/indie-game-development/work/c-exec-g-37a1-venue-packaged-player-001-call.md
