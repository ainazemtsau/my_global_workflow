# CALL — c-converge-verify-g-1d84-001

to: session
direction: indie-game-development
play: converge-verify
node: g-1d84
issued: 2026-08-04 by s-converge-arch-g-1d84-001
status: ready

## goal

Свежая отдельная сессия ПЫТАЕТСЯ СЛОМАТЬ закрытую поверхность g-1d84 до нарезки: каждый атомарный
кусок двенадцати критериев доходит до строки — отвеченной или явно открытой с названным отвечающим, —
и каждая отвеченная строка стоит на источнике ВНЕ решавших ног. Ответов эта нога не даёт: она либо
опровергает, либо пропускает.

`verify_target: build`. Это ПЕРВАЯ converge-verify по этому узлу: прежних FAIL в `history/` у g-1d84
нет, потолок второго FAIL не действует.

## context

Поверхность закрыта двумя ногами и лежит в двух файлах.

- `work/converge-g-1d84.md` — §IMPORT (28 ввезённых решений), §GLOSSARY (21 ряд), §WHAT (107 строк),
  §COVERAGE, §SIGNOFF от converge-ноги, и добавленный §CONTRACTS от converge-arch-ноги.
- `work/converge-g-1d84-arch.md` — архитектура на бумаге: девять высокорисковых вопросов с таблицами
  опровергнутых вариантов, три подписи владельца, §MINER с четырьмя вопросами, которых наряд не задавал.

Полный разбор обеих ног — `history/2026-08-04-s-converge-g-1d84-integrated-house-slice-001.md` и
`history/2026-08-04-s-converge-arch-g-1d84-001.md`. **Рассуждения решавших ног ты не читаешь** — только
их результат и источники, на которые он ссылается.

Владельцу не должна ни одна строка. Его подписи 2026-08-04, дословно и с точной силой:

- правда о доме — **«вопрос один, я с тобой согласен, да»**;
- швы сцены — **«насчет вопроса два я, наверное, с тобой согласен»** (согласие с хеджем, сильнее
  цитироваться не может);
- форма файла профилей — **«Мне, в принципе, все равно… Если ты считаешь, что лучший вариант А, я
  принимаю»** (принятие ПО ДОВЕРИЮ к рекомендации, а не самостоятельный вердикт по содержанию).

Плюс два его добавления в тех же словах — про вложенность мест и про рабочие сцены с инструментами;
разобраны в A10 и A11 арх-файла.

**Куда бить в первую очередь — сами решавшие ноги назвали свои слабые места, и это не индульгенция:**

- **A8** — чтение слова «инструментов» в критерии 10 является ЧТЕНИЕМ его утверждённого текста, а не
  ввозом. Самая слабая строка файла по собственному признанию.
- **A5** — форма двух швов хозяина (восприятие и решение) выведена ногой из критерия 4 и принятого
  словаря; подписи владельца на ней нет.
- **M2** — путь хозяина по размеченному дому: графа проходов не существует ни в каком виде, а без него
  критерий 6 не собирается.
- **A10** — дерево мест принято, инвентарь отклонён по уставу; спор вела сама нога.
- Пять строк вида «решено ногой с названной вынуждающей причиной» (A3, A5, A6, A7, A8, A9, A10, A11) —
  каждая обязана иметь причину ВНЕ собственного рассуждения ноги, иначе её лекарство ПОНИЖЕНИЕ до
  `open` с названным отвечающим, а не новый ответ.

Продуктовые факты обеих ног объявлены на базе `C:/projects/Unity/GasCoopGame`,
`main` = `origin/main` = `origin/dev` = `839df47e`, дерево чистое. Любой факт, помеченный `факт кода`,
перепроверяется на ЭТОЙ базе; расхождение — находка.

Открытых строк, ждущих не тебя: PLAN — одиннадцать; map — две (`W94a` объём карточки и столкновение
с будущей ставкой на копание); отложено самой карточкой — одна. Открытая строка с названным
отвечающим ПОКРЫВАЕТ свой кусок критерия: неотвеченный вопрос дырой не является.

## boundaries

- Ничего не отвечать и ничего не чинить. Только опровергать или пропускать.
- Не читать рассуждения решавших ног как источник; источник — только то, на что они ссылаются.
- Не заводить свой чек-лист класса узла как стандарт: в `knowledge/` его нет, значит он `capture`,
  а не находка.
- Не менять CHARTER, TREE, §WHAT и §CONTRACTS. Не создавать ставку, задачи и полосы.
- Не выносить владельцу новых решений: ниже потолка verify маршрутизирует находки и не решает ничего.
- Утёкшую величину или форму КАК переносить в `→ PLAN`, а не опровергать по существу.

## done_when

1. Названо `verify_target: build`.
2. Каждый атомарный кусок двенадцати критериев, цели, «зачем», строки риска и строки исключений
   сопоставлен со строкой §WHAT или §CONTRACTS. Кусок, не дошедший ни до одной строки, — FAIL: назвать,
   не заполнять.
3. Для каждой несущей строки и каждой `acceptance` названы поддерживающие факты и потребована
   разрешимая ссылка. Строка, чей единственный источник — рассуждение решавшей ноги, названа такой, и
   её лекарство ПОНИЖЕНИЕ.
4. Назван хотя бы один предмет из критериев или одна линза устава, о которых карточка не спрашивает ни
   разу; не назвать ни одного — само по себе находка.
5. `play_check` несёт `verify_target: build complete=<PASS|FAIL> smuggling=<PASS|FAIL>`.
6. При PASS — `§SIGNOFF: converge-verify passed @ <дата>` и открытый shape с тем, что копируется в
   `done_when` задач, и повесткой для PLAN. При FAIL — названные строки возвращаются в converge либо
   converge-arch, и проверка гоняется заново.

## return

Полный Direction RESULT. Разработку не запускать. Ставку не открывать.

budget: one fresh session, отдельная от обеих решавших ног

END_OF_FILE: live/indie-game-development/work/c-converge-verify-g-1d84-001-call.md
