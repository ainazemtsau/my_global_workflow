# CALL — c-exec-g-1d84-cargo-tight-spot-001

to: executor (продуктовый репозиторий)
repo: ainazemtsau/GasCoopGame
kind: engineering
engineering_contract: 36
direction: indie-game-development
node: g-1d84
track: дом
task: t-house-2
issued: 2026-08-05 by s-work-g-1d84-three-lane-calls-001
status: ready
slot: НАЗЫВАЕТ ВЛАДЕЛЕЦ В СООБЩЕНИИ (этого требует контракт продукта). Рекомендация — `WIN-U1`.

## goal

В доме есть хотя бы одно место, где груз не проходит просто так: игроку надо решить — повернуть груз
или зайти иначе. Владелец упирается балкой в это место и решает.

## база и проверенные пути

**Объявленная база:** `C:/projects/Unity/GasCoopGame`, `main` = `origin/main` = `origin/dev` =
`2eee9719af1a26c770e7d1e16d7793a2da9890cc`. Спрошено у сервера напрямую (`git ls-remote`). Каждый путь
ниже проверен на ЭТОЙ базе командой `git cat-file -e 2eee9719:<путь>` — правило
`i-call-named-path-absent-on-declared-basis-004` после четырёх бунсов этого класса.

**ГЛАВНЫЙ ИЗМЕРЕННЫЙ ФАКТ, И С НЕГО НАЧИНАЕТСЯ РАБОТА: механика, возможно, УЖЕ ЕСТЬ.**

- Балка: `NetworkPlaySettings.asset` — `_cargoLength: 2.4`, `_cargoWidth: 0.5`, `_cargoHeight: 0.5`,
  спавн в `(0; 2.5)`.
- Проёмы дома: перегородки `ThreeRoomHouse.prefab` стоят парами при x = −2.5 и x = +2.5, каждая пара —
  отрезки z ∈ [1; 7] и z ∈ [−7; −1]. Между ними проём **шириной 2.0** (z ∈ (−1; 1)). Проёма ровно два.
- Значит балка 2.4 ПОПЕРЁК в проём 2.0 не проходит, а ВДОЛЬ (профиль 0.5) проходит свободно.

**ПЕРВАЯ РАБОТА НОГИ — НЕ СТРОИТЬ, А ПРОВЕРИТЬ ЭТО В ИГРЕ.** Возможно, задача уже закрыта геометрией,
которая приехала вместе с трёхкомнатным домом, и тогда честный результат — «проверено, работает,
построено ноль строк». Возможно, на практике игроки протаскивают балку не думая (стены — коробки,
хват идёт от рук, груз физический) — тогда нужно **одно** место поуже. Что именно окажется правдой,
наряд не угадывает: это измеряется запуском.

**Пути, проверенные на базе:**

- `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` — интегрированная сцена, восемь корней; среди них
  сохранённый корень `ThreeRoomHouse` и корень `Householder`. Дом лежит в файле сцены, а не рождается
  на Play (это закрыто задачей `t-scene-2`).
- `Assets/TunnelCrew/World/ThreeRoomHouse.prefab` — восемь стен: `Outer North/South/West/East` и
  `Divider West North/South`, `Divider East North/South`. Каждая несёт компонент `HouseWall`.
- `Assets/TunnelCrew/World/HouseWall.cs` — `WorldBounds => _collider.bounds`. Коллайдер стены
  является И физической формой, И данными пространства ходьбы. Одна правда, а не две.
- `Assets/TunnelCrew/Network/CarryStand.cs` — сборщик читает стены через
  `_house.GetComponentsInChildren<HouseWall>()`; ничего не инстанцирует.
- `Assets/TunnelCrew/Network/WalkSpaceProbe.cs` — проверка ходьбы. **Известная и НЕ твоя дыра:**
  проверяется конечная точка шага, а не отрезок. Владелец 2026-08-05 проверил своим глазом и сказал
  дословно «Я доказал, я в сцене, но ничего не проходит» — вопрос ЗАКРЫТ, чинить не надо, поднимать
  вопросом домой не надо.
- `Assets/TunnelCrew/Network/NetworkPlaySettings.asset` / `.cs` — все числа груза и дома.
- `core/TunnelCrew.Core.csproj`, `tests/TunnelCrew.Core.Tests/`, `tools/check.ps1`.

**Слоты — перемерено 2026-08-05 против `2eee9719`, и по реестру, и по рабочим каталогам:**

- **`WIN-U1` — РЕКОМЕНДАЦИЯ.** `AVAILABLE`, `lease: none`, дерево чисто, ветка `slot/win-u1`,
  HEAD `adae7d40` = `origin/slot/win-u1` (локальная и серверная сходятся), 2 позади `main`, 0 своих.
  Первой операцией после аренды — привести к `2eee9719`.
- `WIN-U2` — `AVAILABLE`, но в дереве одна незакоммиченная правка
  `ProjectSettings/EditorBuildSettings.asset` (дрейф редактора). Селектор чистоту проверяет.
- `WIN-U3` — ЗАНЯТ (`c-exec-g-1d84-free-kits-probe-001:BUILD`), 194 незакоммиченных файла.
- `WIN-U4` — дерево чисто, но `origin/slot/win-u4` несёт шесть коммитов ЧУЖОГО узла `g-37a1`.

Первая операция ноги, до любой записи в рабочий каталог:
`.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U<N> -Claim -LeaseId c-exec-g-1d84-cargo-tight-spot-001:BUILD`.
Не-`AVAILABLE`, несовпадение ветки или расхождение с названным слотом — STOP. Слот занимается ОДИН.

## context

Задача карточки — «дом не одна пустая коробка»: в нём есть место, где перенос груза требует РЕШЕНИЯ,
а не просто ходьбы. Это первое место во всей волне, где дом что-то ТРЕБУЕТ от игроков.

- **Расширять проход в этой волне нечем** — механики расширения нет и она не заводится. Решение
  игрока — повернуть груз или зайти иначе, и только.
- **Груз физический и его несут двое.** Хват меряется от рук носильщика (`_cargoCarryOffset: 1.3`),
  срыв — от тела. Поворот груза уже работает: взгляд рулит ношей (задача `t-player-1`, закрыта).
- **Стены дома ниже глаз** (верх 1.5, глаза 1.7). Владелец 2026-08-05 решил дословно: «Не надо стены
  поднимать, похуй, потом поднимем, когда будем делать». НЕ ЧИНИТЬ, вопросом домой не поднимать.
- **Хозяин ходит сквозь перегородки** — известно, посчитано, и владелец выбрал не чинить сейчас
  («пусть хотя ходит через стены»). Это вход в `t-host-2`, а не в эту задачу. Не трогать.
- Смысловые метки комнат, описание дома как данных и проходы как записи — это `t-house-4`. Здесь
  дом остаётся геометрией.

## boundaries

- **Дом не пересобирать.** Восемь стен — принятая владельцем работа. Если нужно место поуже, оно
  делается ДОБАВЛЕНИЕМ (одна стена/выступ/косяк) или сдвигом ОДНОЙ существующей стены, и в RESULT
  прямо называется, что именно изменено и почему.
- **Стены не поднимать** (его слово выше). **Хождение хозяина сквозь стены не чинить** (его слово).
- **Хозяина, курьера, снимки и первое лицо не трогать вовсе.** Ни `Householder.cs`, ни
  `NetworkWalkerCourier.cs`, ни `WalkerSnapshot`/`CargoSnapshot`, ни камеру.
- **`WalkSpaceProbe` не переписывать.** Дыра «шаг по конечной точке» закрыта проверкой владельца.
- **Новых рабочих сцен не заводить** (вырез 8 ставки). Результат — та же одна интегрированная сцена.
- **Числа груза не трогать** без прямой нужды; если пришлось — назвать каждое изменение и его довод.
- **Закрытую папку `Assets/GasCoopGame/**` не трогать, не читать, не чинить**; её тесты не запускать.
  `Assets/CodeRespawn/**` (Dungeon Architect) и `Assets/Plugins/**` — тоже не трогать.
- **Проверок на то, что владелец видит глазами, не писать** (`knowledge/no-checks-the-owner-makes-by-eye.md`).
  Тест слоя правил законен и нужен там, где неверный результат выглядит как верный.
- **ЧУЖОЙ ДИФФ ОТ ОТКРЫТИЯ UNITY НЕ ПРИПИСЫВАТЬ СЕБЕ:** `Assets/Plugins/NuGet/.nuget-installed.json`,
  `McpPlugin*.dll`, `ReflectorNet.dll`, `Packages/manifest.json`, `Packages/packages-lock.json`,
  `ProjectSettings/EditorBuildSettings.asset`. Не stage, не commit, не revert, не clean, не stash.
  Мешает закоммитить своё отдельно — это один точный blocker домой, а не очистка дерева.
- **Закрывающий отчёт пишет САМА нога** в своём слоте, с честным статусом, до возврата домой.

## done_when

1. В доме есть хотя бы одно место, где груз не проходит просто так и игроку надо решить — повернуть
   его или зайти иначе. ЕСЛИ это уже так на объявленной базе без единой правки — это законный
   результат, и он доказывается измерением, а не строительством.
2. Владелец упёрся балкой в это место и решил — своими словами.

Пункт 1 доказывается файлами, числами и прогоном. Пункт 2 доказывается ЕГО словами; отчёт исполнителя
их не заменяет и не пересказывает.

## return

Полный продуктовый RESULT домой: точная база, точный кандидат, дифф по файлам, свой прогон, честный
закрывающий отчёт со статусом, состояние слота и аренды. Отдельно назвать: оказалась ли механика уже
готовой на базе или потребовалась правка; если потребовалась — что именно изменено и почему именно
так; какие числа тронуты. Шаги приёмки для владельца — списком, коротко. Отклонения от `done_when`
пишутся В RESULT домой, а не только во внутренний PLAN (`i-product-plan-deviations-unrouted-001`).

budget: одна инженерная нога, режим ПРОБА

END_OF_FILE: live/indie-game-development/work/c-exec-g-1d84-cargo-tight-spot-001-call.md
