# CALL — c-exec-two-players-networked-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 35
basis: e0000c5b (ветка `slot/win-u2`, проверено первой рукой 2026-08-02) — строится ПОВЕРХ a-1, не на main
direction: indie-game-development
node: g-6b13
task: a-2
lane: переноска
issued: 2026-08-02 by s-work-g-6b13-a1-closed-a2-issued-001
status: ready
slot: **`WIN-U2`** — та же полоса, тот же worktree `C:/projects/Unity/GasCoopGame_win-u2`, та же ветка
      `slot/win-u2`. Новых worktree не создавать. **`WIN-U3` НЕ ТРОГАТЬ** — там прямо сейчас идёт
      `c-exec-gate-surface-cut-001` и он владеет `tools/`. `WIN-U1`, `WIN-U4`, `WIN-CTRL` — мимо.

## goal

Двое игроков по сети в одной сцене. Один создаёт игру, второй подключается; обе фигуры ходят, и на
обоих экранах видно обе.

## context

Концепт: кооперативная игра, где команда выносит груз из дома, копает себе тоннель и прячет его, а
хозяин дома это замечает и отбирает. Ближайшая цель — стенд, на котором владелец своими руками
посмотрит, как ДВОЕ несут ОДИН груз по сети. Это второй шаг к нему: сначала просто двое в одной
сцене, груз приходит на следующих шагах.

**Что уже есть, проверено первой рукой 2026-08-02:**

- `a-1` закрыт словом владельца: `Assets/TunnelCrew/` с asmdef `TunnelCrew.Core`,
  `Assets/TunnelCrew/Scenes/EmptyScene.unity`, `core/TunnelCrew.Core.csproj`. Коммит `e0000c5b`;
- **в сцене НОЛЬ объектов — ни камеры, ни света.** Владелец нажал Play и увидел «No cameras
  rendering»; это было ожидаемо и принято. Камера появляется здесь, вместе с фигурами: без неё он
  их не увидит;
- Unity **6000.5.5f1**; URP 17.5.0; Input System 1.19.0;
- **FishNet лежит как ПАКЕТ** — `Packages/com.firstgeargames.fishnet/`. Это зависимость проекта, а
  НЕ старый игровой код: правило устава про старую папку на него не распространяется, читать и
  использовать можно свободно. Переизобретать сетевой слой не нужно;
- `com.unity.multiplayer.center` 1.0.1 в манифесте есть. `com.unity.multiplayer.playmode` — нет;
  добавить его в манифест, если так удобнее гонять две копии, МОЖНО: это изменение проекта, а не
  установка чего-то на машине владельца.

**Сетевая модель выбрана СЛОВАМИ ВЛАДЕЛЬЦА, не выводи её заново:** считает ведущий (хост), остальные
показывают присланное. Побитового согласия независимых машин не требуется. Кооператив и хост он
обсуждать не хочет — «кооператив 100%, хост, это уже моя проблема, как остальные подсоединятся».

**Как он это смотрит:** «я, когда буду работать, я могу запустить несколько Unity Editor» — и они у
него уже стоят и работают. Значит **на его стороне ставить ничего не нужно**. Собранный exe здесь НЕ
требуется: он приходит позже, задачей `a-5`, когда появится что тестировать с другим человеком.
Никаких требований к тому, как, сколько раз и с кем он проверяет, в наряд не писать — это прямой
запрет владельца.

## boundaries

- **`tools/`, `validation.config` и `check.ps1` НЕ ТРОГАТЬ.** Прямо сейчас их переписывает
  параллельный наряд `c-exec-gate-surface-cut-001` в `WIN-U3`. Любая правка там — гарантированный
  конфликт.
- **Старый код не трогать вообще:** ни `Assets/GasCoopGame/**`, ни `tests/GasCoopGame.Core.Tests`.
  Смотреть туда за примерами тоже не надо — устав это запрещает. `Packages/**` под этот запрет НЕ
  подпадает: пакеты это зависимости.
- **Никаких проверок на то, что видно глазами.** Ни «сцена не открылась», ни «объект отсутствует»,
  ни «папка пуста» — владелец возражал против этого класса пять раз, и один такой файл уже был
  выброшен. Правило:
  `C:/my_global_workflow_worktrees/indie-game-development/live/indie-game-development/knowledge/no-checks-the-owner-makes-by-eye.md`
- **Тесты — только на бизнес-логику нового кода.** Подключение и движение фигур бизнес-логикой не
  являются, это интеграция с движком и сетью: их проверяет глаз, а не тест. Если по ходу появится
  настоящая логика со своим правилом — тест на неё уместен.
- **Простота — жёсткое ограничение устава.** Никаких менеджеров сессий, лобби, экранов подключения,
  списка игр, чата, никнеймов, переподключения. Ведущий создаёт, второй входит — и всё.
- **Не выдумывать.** Чего нет в этом наряде — вопрос домой одной строкой, а не догадка в код.
  Особенно это касается всего, что похоже на игровое решение: как выглядят фигуры, как они
  управляются, какая камера — бери самое тупое, что работает, и не изобретай механик.
- **Бумаги минимум.** Наряд `a-1` дал восемь файлов на 192 строки и закрылся — держи ту же планку.
- **Инструмент недоступен — СТОП.** Если Unity Editor или Unity MCP не поднимаются, не обходи это
  никакими суррогатами: одной строкой домой «нужен запуск владельца» и стой.
- Если что-то дороже или невозможнее, чем описано, — ESCALATE домой одной строкой, а не обход.

## done_when

Владелец запускает две копии, в одной создаёт игру, во второй подключается — и видит **на обоих
экранах обе фигуры**, и обе ходят.

## return

Домой, в направление `indie-game-development`, задача `a-2` ставки `g-6b13`. В возврате: коммиты,
одной строкой — что именно владельцу нажать, чтобы поднять две копии (без требований к тому, как он
проверяет), и одной строкой — что оказалось дороже или невозможнее, чем описано.

## budget

Одна инженерная нога.

END_OF_FILE: live/indie-game-development/work/c-exec-two-players-networked-001-call.md
