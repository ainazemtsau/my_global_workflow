# CALL — c-exec-g-5a7c-react-2-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА здесь не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: householder
task: t-react-2
issued: 2026-08-11 by s-work-g-5a7c-react-2-dispatch-001
status: ready
basis: `2f7f2e87b3d3fa1e3b40e50f5063a87530fc5187`, дерево
       `79fb9710f2d02f9983c94a0704e8ef96a454ee3f` — `origin/dev` = `origin/main`. Перемерено
       `git ls-remote origin` непосредственно перед выпуском. Это состояние уже содержит закрытые
       `t-situation-1` (слой ситуаций мира), `t-react-1` (каталог реакций) и опубликованную работу
       дорожки `cargo-hands`. Ground для свежего перемера, не freshness-lock.

**ЕСЛИ `origin/dev` СДВИНУЛСЯ С ЭТОГО SHA — СТОП ДОМОЙ, не догадываться.** `t-cargo-1` ещё не
закрыта Направлением: её строку про провисание судит ГЛАЗ владельца, и порог смерти (1) волны прямо
допускает откат дорожки `cargo-hands` после его вердикта. Такой откат сдвинет базу под этой работой.

Пин перемерен: `engineering_contract: 36` против продуктового `validation.config`
`synced_contract_version: 36`. Re-sync не нужен.

## slot

**WIN-U2.** Каталог `C:\projects\Unity\GasCoopGame_win-u2`, постоянная ветка `slot/win-u2`.

Перемерено файлом состояния `C:\projects\Unity\GasCoopGame_slot-state\gascoop-slot-state.v1.json`
непосредственно перед выпуском:

- WIN-U2 — `lifecycle: AVAILABLE`, `lease: none`, `mcp-endpoint: unrecorded`;
- WIN-U1 — **`CLAIMED`, аренда `c-exec-g-5a7c-cargo-1-001:BUILD`**: слот параллельной дорожки,
  не выбирать ни при каких обстоятельствах;
- локальная `slot/win-u2` уже стоит РОВНО на базисе `2f7f2e87` — **догонять на 49 коммитов, как в
  прошлый раз, НЕ НАДО.** Проверить readback и идти дальше.

**Порядок первых действий:**

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-react-2-001:BUILD`
2. `git fetch origin`, readback: `git rev-parse HEAD` = `2f7f2e87…`,
   `git rev-parse HEAD^{tree}` = `79fb9710…`, `git status` пуст.
3. Любое расхождение, чужая аренда или не-AVAILABLE — **СТОП домой**.

**Про `origin/slot/win-u2`:** удалённая ссылка осталась на `e4df0459`, локальная догнана до
`2f7f2e87`. Селектор читает локальное, выдача не ломается — названо, чтобы это не приняли за разрыв.

**Unity.** У этого слота endpoint `unrecorded`. Для ЛЮБОГО Unity-действия — записать endpoint и
непосредственно перед вызовом live-проверить, что он отвечает именно как WIN-U2. Если прямой Unity
MCP недоступен или не верифицируется — **СТОП и вопрос владельцу**, без shell/HTTP/headless обхода
(`knowledge/no-checks-the-owner-makes-by-eye.md`, правило владельца про самописные обходы).

## goal

**Хозяин перестаёт слышать одну ситуацию за тик и начинает получать ВСЕ, а порядок, слияние,
вытеснение и прерывание становятся названными правилами.**

Его слова, на которых стоит вся дорожка: «какую-то систему event'ов… чтобы хозяин на шум либо на
**комбинацию шумов** может реагировать вот так и вот так». Сегодня комбинация структурно
НЕВОЗМОЖНА — не «не реализована», а невозможна: до хозяина физически доезжает ровно одна ситуация.

**Это рискованное допущение №2 волны.** Читаемость хозяина сегодня держится ровно на том, что он
слышит одну самую громкую. Открыть приём БЕЗ названной политики прерывания — значит получить
дёргающегося хозяина, и порог смерти (2) волны описывает, что тогда происходит: откат и дорожка
режется до «данные плюс подставленный источник».

## context — перемерено чтением байтов на `2f7f2e87`

### ГЛАВНАЯ ПОПРАВКА К КАРТОЧКЕ, названная вслух, а не молча исправленная

Карточка `t-react-2` строка 1 ссылается на `Householder.cs:1026-1044` и говорит, что вторая
ситуация другого рода **«отвергается»**. Обе части устарели, и обе перемерены здесь:

1. **Строки уже не те.** `t-react-1` переписала `Householder.cs` — три ветки `switch` удалены
   целиком. То же правило живёт теперь на **`Householder.cs:1072-1081`**.
2. **Оно не отвергает — оно БРОСАЕТ ИСКЛЮЧЕНИЕ.**
   `throw new InvalidOperationException("Different simultaneous Householder reactions are outside
   this single-fact PROBA.")`. Перемерено, что это НЕ регресс `t-react-1`: тот же `throw` с тем же
   текстом стоял на базе волны `4cf2b5dc:1040`, и предыдущая задача сохранила его знак в знак.

**Требование строки не меняется, но работа выглядит иначе, чем при нарезке:** открыть приём — это
УБРАТЬ БРОСОК и поставить на его место названное правило, а не ослабить фильтр.

### Почему бросок сегодня не стреляет — ДВА глушителя выше по течению

Именно они и есть предмет строки 1.

**Глушитель первый — проекция.** `Core/Situations/WorldSituationNoise.cs`:
`Loudest(IReadOnlyList<WorldSituation>, …)` (`:110-121`) сворачивает ВЕСЬ список тика в ОДИН
`HouseholderStimulus`. Порядок громкости выписан числами в `Loudness(...)` (`:154-168`):
`Landing = 4` > `RepeatedHeavyNoise = 3` > `BoardCreaked = 2` > `QuietRustle = 1`, при равенстве
остаётся более ранний. Полосы выбираются `Band(...)` (`:135-152`) сверху вниз.

**Глушитель второй — приёмник.** `Core/Householder/AuthoritativeHouseholder.cs`:

- `Observe(HouseholderStimulus, float)` (`:176-186`) — новый стимул ВЫБРАСЫВАЕТСЯ, если уже есть
  `_pendingEvent` или `_offeredStimulus`. Один слот на тик, второй звук просто исчезает.
- `TryHear(...)` (`:333-343`) — второе событие в тике не принимается.
- `CanHear(...)` (`:345-374`) — событие ДРУГОГО рода отвергается, пока жив замеченный факт или
  активный ответ.

**КОД САМ НАЗЫВАЕТ ЭТОТ ШОВ, и это самый полезный факт наряда.** Доксртока над `TryHear`
(`:314-332`) объясняет, что глушитель существует ТОЛЬКО потому, что правило бросает: «That was
survivable while he ran on his own frame… He now runs inside the judge's tick, where the same throw
would cost every player the whole tick». И заканчивается прямо: **«If his rules ever grow a way to
offer an event and be told no, this goes.»** Эта задача и есть тот день. Комментарий обязан уехать
вместе с глушителем, а не остаться врать.

### Список ситуаций УЖЕ лежит на расстоянии одной строки

`Core/Multiplayer/CarryWorld.cs:124-138`: список чистится в начале тика (`_situations.Clear()`),
производители наполняют его (`Cargo.Advance(…, _situations)`), и затем

```csharp
Stimulus = WorldSituationNoise.Loudest(_readOnlySituations, rules.Noise);
Householder.Observe(Stimulus, tickSeconds);
```

**`_readOnlySituations` — весь список тика — уже здесь, строкой выше.** Новой проводки не нужно:
нужно решить, ЧТО именно доезжает до хозяина и по какому названному правилу.

`AuthoritativeHouseholder.Observe` перемерен на предмет вызывающих: их **ровно два** —
`CarryWorld.cs:138` и `HouseholderReactionDataTests.cs:198`. Сигнатура свободна.

### ПОЛОВИНА СТРОКИ 2 УЖЕ СДЕЛАНА ПРЕДЫДУЩЕЙ ЗАДАЧЕЙ — перемерено

Карточка требует, чтобы политика прерывания стала «названным правилом или полем». **Поле уже есть:**
`t-react-1` завела `RequiresSafeInterruption` строкой каталога реакций
(`Core/Householder/HouseholderReaction.cs`), и решатель читает именно его —
`Householder.cs:477-479`:

```csharp
if (!reactions.TryGetByNoticedFact(fact.Name, out var reaction) ||
    (reaction.RequiresSafeInterruption && !snapshot.Intention.IsSafeToInterrupt))
```

Значения по строкам перемерены: `suspicious-noise` — `true`, `quiet-rustle` — `true`,
`repeated-heavy-noise` — `false`, `landing` — `false`, `daily-route` — `false`.

**Чего НЕТ:** это булево «можно ли прервать ЕГО», а не политика «что происходит, когда прерывают».
Возобновление сегодня выражено одним `ReturnToLifeMode.ResumePreviousIntention` и одним активным
ответом `HouseholderLocalMemory.ActiveResponse`. **Задача от этого стала МЕНЬШЕ, чем при нарезке —
это измеренный факт, а не разрешение расшириться на сэкономленное.**

### Почему названный тест не может проверить свой случай — подтверждено байтами

`IsSafeToInterrupt` вычисляется в ЕДИНСТВЕННОМ месте — `Householder.cs:1385-1421`:
`true` ровно тогда, когда шаг дошёл до путевой точки и фаза стала `PausingAtRoutePoint`;
`false` на КАЖДОМ тике фазы `Moving`.

Тест `tests/TunnelCrew.Core.Tests/HouseholderWalksTheHouseTests.cs:289`
`ASecondNoiseDuringTheWayBackDistractsHimAgain` подаёт второй шум под собственной охраной
(`:304-308`):

```csharp
if (noisesMade < 2 && memory.Intention.IsSafeToInterrupt &&
    !memory.ActiveResponse.IsPresent && !memory.NoticedFact.IsPresent)
```

То есть второй шум подаётся ТОЛЬКО когда хозяин стоит на путевой точке — **никогда «во время пути
назад»**, ровно наоборот тому, что обещает имя теста. Строка 3 карточки подтверждена первой рукой:
тест сегодня свой случай проверить не может, и зелёным он остаётся по построению.

### Второй вход событий, который нельзя забыть и нельзя расширять

`IHouseholderWorldPort.TakeHeardEvent()` (объявление `AuthoritativeHouseholder.cs:96`, потребление
`:219`) — это кнопки шума из сцены, второй путь события. Интерфейс реализуют
`Network/NetworkHouseholder.cs:140`, `World/HouseholderRouteController.cs:91` и **ЧЕТЫРЕ
существующих тестовых двойника**: `CargoContactNoiseTests.cs:129`,
`HouseholderInTheJudgesTickTests.cs:62`, `HouseholderProfileTests.cs:37`,
`HouseholderReactionDataTests.cs:314`.

**Расширять `IHouseholderWorldPort` этой задачей ЗАПРЕЩЕНО.** Кнопка — это по смыслу одно событие,
а расширение интерфейса заставило бы править четыре существующих тестовых файла и вылезло бы за
полдня. Множественность приходит через `Observe` и список `CarryWorld`, где он уже есть.

### Шорох волочения — числа улики и то, чем его МОЖНО и НЕЛЬЗЯ мерить

Улика `i-dragged-cargo-rustles-every-tick-001`: расчёт из живых настроек даёт остаточное давление
на пол ~`100.1` Н, пересчёт подшагом докладывает ~`83.4` Н, что попадает в полосу `QuietRustle`
(`60 <= 83.4 < 150`), то есть до ~`60` стимулов в секунду. **Это РАСЧЁТ, а не замер.**

Пороги перемерены на HEAD — `Assets/TunnelCrew/Network/NetworkPlaySettings.asset:72-74`:
`_cargoRustleForce: 60`, `_cargoKnockForce: 150`, `_cargoHeavyNoiseForce: 400`. Файл принадлежит
дорожке `cargo-hands`.

**Замерить это headless НЕЛЬЗЯ, и это перемерено, а не предположено.** Число силы рождается в
`Assets/TunnelCrew/Network/CargoBodyContact.cs` — это `MonoBehaviour` на 112 строк, берущий импульс
ПРЯМО из решателя Unity («One number and one point, taken straight from the engine»). Ни один
engine-free тест этого числа не производит. Значит замер — это Unity-прогон, и другого пути нет.

### Что уже зелёное

База `2f7f2e87` даёт **158 passed, 0 failed, 0 skipped** — прогнано Направлением на этой самой
голове, на выгруженном дереве. Реакции и ход хозяина покрыты `HouseholderTests.cs`,
`HouseholderAdditionalReactionTests.cs`, `HouseholderInTheJudgesTickTests.cs`,
`HouseholderWalksTheHouseTests.cs`, `HouseholderProfileTests.cs`,
`HouseholderReactionDataTests.cs`, плюс `CargoContactNoiseTests.cs` на конце «ситуация → реакция».

## проверка происхождения

| Строка наряда | Источник |
|---|---|
| приём берёт ВСЕ ситуации тика, а не самую громкую | подписанная карточка `t-react-2` `done_when` 1; его слово «на шум либо на комбинацию шумов» |
| порядок, слияние, вытеснение и прерывание — названные правила или поля, а не ветка решателя | там же, `done_when` 2 |
| тест `ASecondNoise…` чинится и начинает проверять свой случай, либо удаляется | там же, `done_when` 3; `i-householder-activities-are-intentions-not-instructions-001`, четвёртое свойство |
| шорох волочения МЕРЯЕТСЯ прогоном, а не считается; пороги `60 / 150 / 400` не двигаются, спецслучая под волочение нет | там же, `done_when` 4; буквальный запрет улики `i-dragged-cargo-rustles-every-tick-001` |
| откат одним коммитом, доказан настоящим revert до дерева базы | там же, `done_when` 5; `evaluator` волны, блок про откаты |
| хозяин обязан остаться читаемым | `bet-g-5a7c-wave-2` `## riskiest_assumptions` 2 и порог смерти (2) |
| `IntegratedHouse.unity` не редактируется ни одной дорожкой | `bet-g-5a7c-wave-2` `## tracks`, границы по файлам |
| занятия хозяина как НАМЕРЕНИЕ с фазами сюда не входят | `bet-g-5a7c-wave-2` `## cut_list` 5 — вырез в волну дома |

**Не импортируется** зрение хозяина и подставленный источник (`t-react-3`), занятия хозяина как
намерение с фазами «путь → действие → завершение», видимые или слышимые полосы шума, общий
фреймворк под источники, очередь событий и история, ситуация, пережившая тик.

## РАЗРЕШЁННЫЙ РАЗРЕЗ — единственный, и он разрешён БЕЗ возврата за словом

Карточка называет его заранее: **«приём берёт все ситуации тика» остаётся здесь, «прерывание и
возобновление становятся названной политикой» уезжает СВОЕЙ восьмой задачей.** Резать по этой линии
разрешено исполнителю самому; всё остальное режется только владельцем.

Перемеренная поправка к этому разрезу: половина политики прерывания уже стоит полем
`RequiresSafeInterruption`, поэтому разрез может и не понадобиться. Решает исполнитель по факту, и
называет в возврате, резал или нет и почему.

## boundaries — что НЕ трогать

- **Задача не расширяется.** Пять строк карточки — весь объём; сэкономленное на уже сделанной
  половине строки 2 не тратится на новое.
- **`Assets/TunnelCrew/Scenes/IntegratedHouse.unity` НЕ РЕДАКТИРУЕТСЯ.** Запустить сцену в
  play-режиме для замера — можно; файл обязан вернуться байт в байт, и это проверяется
  `git status` после выхода из режима. Если редактор его изменил — вернуть и назвать в возврате.
- **ФАЙЛЫ ПАРАЛЛЕЛЬНОЙ ДОРОЖКИ — ЧУЖИЕ.** Дорожке `cargo-hands` принадлежат и этим нарядом НЕ
  ТРОГАЮТСЯ: `Core/Cargo/**`, `Network/CargoBody.cs`, `Network/NetworkPlaySettings.cs`,
  `Network/NetworkPlaySettings.asset`, и тестовые файлы `CargoHaulTests.cs`, `CargoCarryTests.cs`,
  `CargoLiftLawTests.cs`, `CargoPoseTests.cs`, `CargoWalkSpaceTests.cs`, `CargoClassSeamTests.cs`,
  `CargoThreeDimensionalGripTests.cs`, `TelekineticPickupTests.cs`, `FirstPersonCarryTests.cs`.
  Упёрся в любой из них — **СТОП домой**.
- **ПУБЛИЧНАЯ СИГНАТУРА `CarryWorld.Advance` НЕ МЕНЯЕТСЯ** — её зовут тесты чужой дорожки
  (`CargoCarryTests.cs`, десятки вызовов) и `Network/NetworkWalkerCourier.cs:160`. Внутренности
  `CarryWorld` править можно.
- **`IHouseholderWorldPort` НЕ РАСШИРЯЕТСЯ** — причина выше: четыре существующих тестовых двойника.
- **`Network/CargoBodyContact.cs` не редактируется** — он ничей, чинён общей задачей. Читать для
  замера можно; временную отладочную печать в него не класть.
- **ПОРОГИ `60 / 150 / 400` НЕ ДВИГАЮТСЯ и специального случая под волочение не заводится** — это
  буквальный запрет самой улики. Замер их не меняет, он их проверяет.
- **Файл профиля `default.householder.json` не расширяется** и загрузчик профиля не открывается.
- **`ProjectSettings/**` не трогать.** `Assets/GasCoopGame/**` закрыта.
- Самодельный сканер исходников уликой не является и не пишется.

## owner-visible check

**Владелец эту задачу руками НЕ судит, и наряд не ставит ему условий.** Его вердикт по этой дорожке
назначен на `t-react-3`, в самом её конце, и это нарочно — чтобы он не судил два недоделанных
вечером одного дня.

Но проверка «как вчера» здесь НЕ действует, в отличие от прошлой задачи: **поведение обязано
измениться** — вопрос, где именно и остался ли хозяин читаемым. Писать «поведение не изменилось»
прямо запрещено.

## headless evidence

- **Существующие тесты зелёные без правки ожиданий.** База даёт `158 passed`; после задачи —
  столько же проходящих плюс новые, МИНУС `ASecondNoiseDuringTheWayBackDistractsHimAgain`, если он
  удалён по строке 3. Механическая подстановка переименованных символов правкой ожидания НЕ
  является; изменение любого ожидаемого значения, любого числа и любого смысла утверждения —
  является. Доказать диффом по `tests/`.
- **Главный гейт строки 1 доказывается ТЕСТОМ на ДВУХ ситуациях РАЗНОГО рода в ОДНОМ тике**, где
  сегодня либо теряется вторая, либо летит `InvalidOperationException`. Тест обязан показать, какое
  именно названное правило решило исход — порядок, слияние или вытеснение, — а не просто «не
  упало».
- **Гейт на глушители:** дифф показывает, что `Observe`/`TryHear`/`CanHear` больше не выбрасывают
  вторую ситуацию молча, а доксртока `TryHear:314-332` про «If his rules ever grow a way to offer an
  event and be told no, this goes» уехала вместе с ней. Предъявляется диффом.
- **Гейт на читаемость:** тест, показывающий, что хозяин НЕ дёргается — что при двух ситуациях
  подряд он не бросает начатое на каждом тике. Что именно значит «не дёргается», выражается
  названным правилом и проверяется числом (сколько раз сменилось занятие за N тиков), а не глазом.
- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`.
- Через прямой MCP точного слота — настоящая Unity import/compile evidence.
  **Зелёные ворота поведением игры не являются и так не предъявляются**
  (`knowledge/green-gates-do-not-compile-the-game.md`).

## замер шороха волочения — протокол, и он допускает СТОП

Строка 4 карточки требует ЗАМЕРА, а не расчёта, и замер возможен только в Unity — обоснование
перемерено выше (`CargoBodyContact` — `MonoBehaviour`).

1. **Делать этот пункт ПЕРВЫМ, до единой правки правил.** Он ничего не меняет в коде и отвечает на
   вопрос «доступен ли вообще Unity в этом слоте» ДО того, как потрачена половина дня.
2. Живой верифицированный MCP этого lease → запустить сцену, проволочь груз одним носильщиком по
   полу и снять ЧИСЛО: сколько стимулов полосы `QuietRustle` приходит в секунду и какая сила
   докладывается. Число — в возврат, вместе с тем, как оно снято.
3. **MCP недоступен или не верифицируется как WIN-U2 → СТОП, вопрос владельцу, одним точным
   блокером.** Пересчитать вместо замера и назвать это замером — прямо запрещено: улика требует
   прогона именно потому, что расчёт уже есть и ему не верят.
4. Замер НИЧЕГО не чинит. Пороги не двигаются, спецслучай не заводится. Что делать с числом —
   решает владелец после того, как оно появится.

## откат

- **Точка возврата — `2f7f2e87b3d3fa1e3b40e50f5063a87530fc5187`**, дерево
  `79fb9710f2d02f9983c94a0704e8ef96a454ee3f`.
- **Отката два, и оба обязаны быть одним коммитом:** правило «одна самая громкая ситуация за тик»
  возвращается целиком (`done_when` 5 карточки), и вместе с ним — оба глушителя приёмника.
- Откат назвать ОДНОЙ точной командой и **исполнить проверкой первой рукой**: после отката
  `git rev-parse <ветка>^{tree}` возвращает дерево базы знак в знак. Выведенный из истории откат
  вместо исполненного — назвать выведенным, а не выдавать за проверенный.
- Прямо заявить, что для отката не нужно ничего вне репозитория.
- **Этого пункта не было в возврате прошлого наряда вовсе, и закрывающая нога Направления делала
  его сама** (`i-executor-return-omits-named-return-items-001`). Здесь он не факультативен.

## done_when

1. **Приём открыт: ВСЕ ситуации тика доходят до хозяина, а порядок, слияние и вытеснение — названные
   правила.** Бросок `Householder.cs:1072-1081` на две одновременные разнородные реакции убран и
   заменён названным правилом; оба глушителя (`Loudest`-свёртка на пути в `Observe` и
   отбрасывание второго стимула в `Observe`/`TryHear`/`CanHear`) сняты или переписаны так, что
   вторая ситуация больше не теряется молча. Доказано тестом на ДВУХ ситуациях разного рода в ОДНОМ
   тике, который называет, какое правило решило исход. `IHouseholderWorldPort` не расширен.

2. **Хозяин остался читаемым, и имитация кончилась.** Политика прерывания и возобновления выражена
   названным правилом или полем (поле `RequiresSafeInterruption` уже есть — доведено, а не
   переизобретено), и есть тест, показывающий числом, что хозяин не дёргается при двух ситуациях
   подряд. Тест `ASecondNoiseDuringTheWayBackDistractsHimAgain` ЛИБО починен и начал проверять свой
   случай — второй шум приходит во время ДВИЖЕНИЯ назад, — ЛИБО удалён с названной вслух причиной.
   Существующие тесты зелёные, ни одно ожидание не переписано, доказано диффом по `tests/`.

3. **Шорох волочения замерен прогоном, границы целы, откат проверен.** Число стимулов `QuietRustle`
   в секунду при волочении снято Unity-прогоном — либо СТОП домой одним точным блокером, если MCP
   слота не поднялся; расчёт вместо замера замером не называется. Пороги `60 / 150 / 400` не
   двинуты, спецслучая под волочение нет. Границы целы — сцена не отредактирована, файлы дорожки
   `cargo-hands`, публичная сигнатура `CarryWorld.Advance`, `CargoBodyContact.cs`, файл профиля и
   `ProjectSettings/**` не тронуты, доказано диффом. Откат назван одной командой и проверен readback
   до дерева базы `79fb9710`.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / householder / t-react-2`: точные
коммиты и их продуктовая диспозиция; scoped manifest/diff и точные outputs проверок.

**ПЕРВОЙ СТРОКОЙ ВОЗВРАТА — РАСПИСКА:** число полученных строк `done_when` этого наряда.
До неё наряд не считается выданным (`evaluator` волны, клауза 3;
`knowledge/an-order-does-not-exist-until-it-is-delivered.md`). Прошлый возврат этой дорожки её не
содержал.

Также обязательно, и ни один пункт не опускается молча:

- **как теперь называется порядок, слияние и вытеснение** — точные имена типов, полей или правил, и
  что происходит с двумя ситуациями РАВНОЙ громкости;
- **что стало с броском** и с доксрокой `TryHear:314-332`, которая сама себя приговорила;
- **резал ли ты по разрешённой линии** (политика прерывания → восьмая задача) — да или нет и почему;
- **что именно значит «не дёргается»**, выраженное правилом и числом из теста;
- **судьба теста `ASecondNoise…`**: починен или удалён, и если починен — чем именно его охрана
  теперь отличается;
- **ЗАМЕР шороха: число стимулов в секунду и сила**, чем и как снято — или один точный блокер, если
  Unity не поднялся;
- **проверенный откат: одна команда и readback дерева базы `79fb9710`**;
- **терминальный readback селектора слота** `CLEAN / AVAILABLE / lease none`;
- **отдельной строкой: с какой головы параллельной дорожки собиралось перед публикацией** — по
  клаузе 5 `evaluator` этой волны ни одна дорожка не публикует в `main` состояние, которого не
  собирала вместе с текущей головой другой;
- каждое расхождение свежей базы с grounds этого наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает.** После HOME отдельная нога Направления
читает возврат первой рукой и закрывает `t-react-2` по букве `evaluator` этой волны, называя вслух,
каким из двух путей шла.

## budget

Один engineering root. Половина дня. Задача меньше, чем при нарезке: половина политики прерывания
уже стоит полем, а список ситуаций уже лежит строкой выше вызова. Зрение, подставленный источник,
занятия как намерение с фазами, очередь событий и общий фреймворк сюда не входят. Не помещается без
расширения границ — сначала разрезать по РАЗРЕШЁННОЙ линии, и только если не помогло — STOP HOME
с одним точным blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-react-2-001-call.md
