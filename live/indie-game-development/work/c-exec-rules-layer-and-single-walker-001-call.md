# CALL — c-exec-rules-layer-and-single-walker-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 35
basis: c75015a8 (исходная база корня); PLAN/ADR `7c5fc5a6`, tooling `ad42b2d8`, PLAN receipts `f7387751` в `WIN-U3`
direction: indie-game-development
node: g-6b13
task: a-1b
track: переноска
issued: 2026-08-02 by s-work-g-6b13-foundation-call-issued-001
status: waiting
waiting_on: [c-ctrl-publish-root-lifecycle-singleton-fix-001]
slot: `WIN-U3`, чист и освобождён на `f7387751`. Не занимать и не запускать для feature-работы,
      пока tooling-child не вернулся HOME и Direction отдельно не открыл продолжение.

## CHECKPOINT НАПРАВЛЕНИЯ — РЕДАКЦИЯ 3, 2026-08-02

Владелец дословно: **«PLAN и ADR-E-0019 по содержанию принимаю. BUILD пока не запускать: сначала
исправить Direction-маршрут и отделить ad42b2d8 от задачи a-1b».**

Поэтому PLAN и ADR приняты по содержанию, но это **не** разрешение BUILD. Между замороженным
PLAN-коммитом `7c5fc5a6` и квитанциями `f7387751` лежит независимое исправление
`tools/root-lifecycle-check.ps1` (`ad42b2d8`). Корень ждёт отдельный child
`c-ctrl-publish-root-lifecycle-singleton-fix-001`: тот публикует tooling независимо и возвращает
чистый feature-дифф U3 без переписывания принятого PLAN. До его HOME в этом корне ничего не
запускается. После HOME следующий допустимый продуктовый этап — свежий `PAIR-CANDIDATE` по
контракту 35, но и его открывает только новое Direction-продолжение; автоматического BUILD нет.

## ЭТА РАБОТА ИДЁТ В ДВА ЗАХОДА. ПОСЛЕ ПЛАНА — СТОП

**Заход 1: план. Заканчивается возвратом домой, а не постройкой.**
**Заход 2: постройка. Начинается только по отдельному Direction-продолжению после очистки маршрута;
принятие содержания PLAN/ADR само по себе её не запускает.**

Причина названа прямо, чтобы не выглядело бюрократией: предыдущая попытка этой полосы построила
рабочий код за тридцать минут и была ОТКЛОНЕНА владельцем целиком — не за ошибки, а за то, что
правило игры оказалось внутри сетевого куска. План заставил бы написать это ЗАРАНЕЕ одной строкой,
и работа не была бы потрачена. Плановая стадия возвращена именно за этим, и владелец потребовал её
сам.

## goal

Завести каркас новой игры, слой правил, и положить в него первое настоящее правило — движение.
Один игрок ходит. **Сети в этой задаче нет вообще.**

Владелец жмёт играть один — человечек ходит. Всё.

## context

**ОБЯЗАТЕЛЬНОЕ ЧТЕНИЕ ПЕРЕД ПЛАНОМ, это не справка:**
`C:/my_global_workflow_worktrees/indie-game-development/live/indie-game-development/knowledge/how-the-game-is-built-layers.md`
Устройство согласовано с владельцем в разговоре и принято его словом. Оно не обсуждается здесь и не
переизобретается — по нему работают.

Коротко, чтобы не было разночтений: три слоя. **Правила** — обычный код, не знает ни про Unity, ни
про сеть, по состоянию и вводу считает новое состояние. **Мир** — сцена, камера, пол, модели.
**Курьер** — собирает ввод, несёт судье, у судьи вызывает правила, результат рассылает. Курьера в
этой задаче НЕТ, он приходит следующей.

**ПОПРАВКА НАРЯДА 2026-08-02, редакция 2. Первая редакция была ДЕФЕКТНОЙ и исполнитель законно
её остановил.** Она утверждала, что на базе `c75015a8` уже есть `Assets/TunnelCrew/**` и
`core/TunnelCrew.Core.csproj`. **Этого там нет.** Проверено сейчас: каркас построен задачей `a-1`
коммитом `e0000c5b`, но живёт ТОЛЬКО на ветке `slot/win-u2`, под отклонённой работой `955e5e62`,
и в `main` он никогда не сливался — в `main` уехало только урезание проверок. Сессия проверяла
каркас в чужом рабочем каталоге и объявила базой `main`; это её ошибка, не исполнителя.

**Что реально есть на `c75015a8`:** урезанный контур (`check.ps1` 224 строки, 16 скриптов) и
больше ничего из новой игры. `Assets/TunnelCrew/**` НЕТ. Кода новой игры НЕТ.

**Что из этого следует для работы:** каркас создаётся ЗДЕСЬ, в этой же задаче, и это разрешено
явно. Он крошечный — папка `Assets/TunnelCrew/`, её `.meta`, asmdef `TunnelCrew.Core` с
**обязательным** `noEngineReferences: true` (это и есть стена, без неё вся затея бессмысленна),
пустая сцена и проект для сборки слоя правил. Брать это из `slot/win-u2` НЕ ОБЯЗАТЕЛЬНО: там восемь
файлов шаблонного текста, и написать своё чище, чем тянуть через сдвинувшуюся базу. Коммит
`e0000c5b` после этого остаётся заброшенным, и это осознанно.

Unity `6000.5.5f1`, URP 17.5.0, Input System 1.19.0.

**Про отклонённую попытку:** на ветке `slot/win-u2` лежит `955e5e62` с
`Assets/TunnelCrew/Net/NetworkPlayerMotor.cs` и `NetworkButtons.cs`. **Оттуда не берётся ничего.**
Это образец того, чего делать НЕЛЬЗЯ: правило движения внутри `NetworkBehaviour`, вход в игру через
`OnGUI`-накладку с кнопками поверх картинки. Смотреть туда можно только чтобы не повторить.

## Заход 1 — что должно быть в плане

Плановая стадия с ADR по обычному контракту репозитория. **План короткий.** От него нужно ровно
шесть ответов, каждый в несколько строк, а не трактат:

0. **Какой каркас ты заводишь** — папка, asmdef со стеной, сцена, проект. Одной строкой на пункт.
1. **Где живёт правило движения** — какая сборка, какой файл, какой у него вход и выход. Оно обязано
   быть вызываемым без Unity: на вход состояние и ввод, на выход новое состояние.
2. **Что окажется в сцене** — камера, пол, фигура. Самое тупое, что работает.
3. **Как достигается «нажал играть и ты в игре»** — без кнопок поверх картинки.
4. **Что ты НЕ трогаешь.**
5. **ADR** — решение о расслоении в том виде, в каком оно применяется к продукту: игровая логика
   живёт в сборке без движка, сеть будет курьером, почему так (судья один, значит правило обязано
   уметь работать только у судьи; и локальное предсказание возможно только при отдельном слое
   правил). ADR нужен затем, чтобы следующие ноги в продукте видели это решение, а не узнавали
   заново.

**После плана — СТОП.** Возврат домой одним сообщением: где план и ADR лежат, и эти шесть ответов
своими словами. Постройку в этом же заходе НЕ начинать.

## Маршрут после PLAN — сейчас НЕ открыт

После HOME tooling-child новое Direction-продолжение может вернуть этот же корень в repo runner на
следующий обязательный этап `PAIR-CANDIDATE`, не прямо в BUILD. Затем контракт 35 сам ведёт корень
через binding fresh `PAIR-FREEZE`, fresh BUILD, product validation и REPORT отдельными авторами;
домой возвращается только terminal REPORT либо настоящий ESCALATE. Ни один из этих этапов этой
редакцией не запущен.

Когда законный fresh BUILD будет открыт после пары, в нём:

- правило движения в слое правил;
- сцена с камерой, полом и фигурой;
- вход без кнопок поверх картинки;
- **один тест: правило движения даёт результат при вызове ВНЕ Unity.** Это законный тест и он
  обязателен: глазами увидеть, в правильном ли слое лежит правило, НЕВОЗМОЖНО, поэтому запрет
  «не проверять то, что владелец видит сам» на него не распространяется.

## boundaries

- **Сети в этой задаче НЕТ.** Ни FishNet, ни `NetworkBehaviour`, ни подключений. Она приходит
  следующей задачей и не должна потребовать правки правила.
- **Трогать можно:** `Assets/TunnelCrew/**`, `core/TunnelCrew.Core.csproj`, свой тестовый проект,
  свои файлы плана/ADR/квитанций.
- **Трогать нельзя:** `tools/`, `validation.config`, `check.ps1`; `Assets/GasCoopGame/**` и
  `tests/GasCoopGame.Core.Tests`; чужие ветки слотов.
- **УПЁРСЯ В НЕОБХОДИМОСТЬ МЕНЯТЬ КУРЬЕРА ИЛИ УСТРОЙСТВО СЛОЁВ — СТОП И ВОПРОС ДОМОЙ.** Это не
  решение исполнителя ни при каких обстоятельствах. Такой момент означает, что чего-то не
  предусмотрели, и решает это владелец.
- **Никаких проверок на то, что видно глазами:** «сцена не открылась», «объект отсутствует», «папка
  пуста». Владелец возражал против этого класса пять раз, один такой файл уже выброшен. Правило:
  `C:/my_global_workflow_worktrees/indie-game-development/live/indie-game-development/knowledge/no-checks-the-owner-makes-by-eye.md`
- **Простота — жёсткое ограничение устава.** Ни лобби, ни экранов, ни настроек, ни менеджеров.
- **Не выдумывать игровых решений.** Как выглядит фигура, как быстро ходит, какая камера — бери
  самое тупое, что работает. Чего нет в наряде — вопрос домой одной строкой, а не догадка в код.
- **ИНСТРУМЕНТ НЕДОСТУПЕН — СТОП.** Если Unity Editor или Unity MCP не поднимаются — никаких
  суррогатов: одной строкой домой «нужен запуск владельца» и стой.
- Если что-то дороже или невозможнее, чем описано, — ESCALATE домой одной строкой, а не обход.

## done_when

Владелец открывает проект, жмёт играть один — человечек ходит. Правило движения при этом лежит в
слое правил и вызывается вне Unity, что показано тестом.

## return

PLAN уже вернулся домой и принят по содержанию. После нового запуска repo runner возвращает корень
в направление `indie-game-development`, задача `a-1b` ставки `g-6b13`, только на terminal REPORT
либо genuine ESCALATE. В REPORT — коммиты, вывод проверок и одной строкой, что оказалось дороже или
невозможнее, чем описано.

## budget

PLAN закрыт. Остальной бюджет наследует исходный корень; отдельный tooling-child feature-бюджет не
расходует и BUILD не открывает.

END_OF_FILE: live/indie-game-development/work/c-exec-rules-layer-and-single-walker-001-call.md
