# CALL — c-shape-g-1d84-001

to: session
direction: indie-game-development
play: shape
node: g-1d84
issued: 2026-08-04 by s-converge-verify-g-1d84-002 (после вердикта владельца о ветке)
status: ready

## goal

Нарезать `g-1d84` в ставку: мелкие задачи, полосы, вырезы, аппетит и порог смерти — и получить на
это слово владельца. Основание — его собственный выбор ветки на потолке второго FAIL, дословно:
**«В, идём дальше на том, что отвечено»**. Разбор набора закончен и третьего круга бумаги нет.

## context

**Что решил владелец и что его слово НЕ покрывает.** Он выбрал вариант (В) из трёх, показанных ему
на развилке `d-g-1d84-second-fail-branch-001`: вырезать / разрезать картой / идти дальше на том, что
отвечено. Слово прямое и без хеджа. Оно покрывает РОВНО одно: бумажная фаза узла закончена и
открывается нарезка. Оно НЕ является: одобрением списка задач, аппетита, полос, вырезов, ответом на
две висящие развилки и подписью под чем-либо из пяти находок проверки. Ставку по-прежнему открывает
его отдельное слово на показанный ему экран.

**Цена варианта (В), названная ему ДО выбора и потому едущая сюда требованием формы, а не новой
идеей:** нарезка **читает саму карточку `TREE.md` g-1d84**, а не только набор строк с меткой
`acceptance`. Иначе волна соберётся без цели захода и без трёх комнат — обе эти строки в набор
`acceptance` не попали, и это находка V5.

**Что отвечено и на чём стоит нарезка.** `work/converge-g-1d84.md`: 111 строк §WHAT, 88 `answered`
(из них 26 `acceptance`), 3 улики, 19 `open`, 1 `deferred`; §CONTRACTS C01-C21 плюс `contracts_open: 1`
(C05a — карточка копания). `work/converge-g-1d84-arch.md`: A1-A11 с поправками, §MINER и §MINER-002,
`arch_in_context_only: PASS` — **ни одна строка арх-файла не является требованием узла**, она едет в
`context` продуктового наряда уликой и никогда в `done_when`. Счёт перепроверен механически второй
проверкой и сходится точно; объявленная база продуктовых фактов —
`C:/projects/Unity/GasCoopGame`, `main` = `origin/main` = `origin/dev` = `839df47e`, дерево чисто,
проверено первой рукой дважды.

**Пять находок второй проверки — это ПОЧИНКА НОСИТЕЛЕЙ, а не открытые вопросы.** Владелец изменил
карточку своей подписью («1а, 2а, 3а, 4а») ПОСЛЕ того, как оба файла были закрыты; носители его слова
следом не пересобрали. Полный текст — `work/converge-g-1d84.md` §VERIFY-002. Ни одна из пяти не
требует нового решения и ни одна не отвечается выдумкой:

1. **V1.** Реестр `owner_approved` в `TREE.md` и ряд ввоза I01 приписывают ДЕЙСТВУЮЩИЙ текст более
   раннему и более широкому токену «принимаю карточку». Четыре токена «1а, 2а, 3а, 4а» не названы ни
   там, ни там. Починка: строка реестра `TREE.md` о четырёх правках 2026-08-04 с их точной силой
   (одна буква = одна правка, показанный ДО правки точный текст; источник —
   `history/2026-08-04-s-map-g-1d84-card-text-and-time-seam-001.md`) и переcсылка I01 на обе подписи.
2. **V2.** Строка W57 (`answered · acceptance`) несёт вычеркнутое им «Распорядок по времени и
   обстоятельствам». Починка: привести к утверждённому тексту критерия 6 («порядок дел, их
   длительности и обстоятельства») и завести отдельную строку на вопрос «чем выражается длительность
   дела, когда часов игры и цикла суток нет» с названным отвечающим (PLAN). Ответ не изобретать.
   §CONTRACTS C16 несёт то же неполное чтение.
3. **V3.** Единственное условие успеха узла — «Когда груз в ней — заход удался, и игра это
   показывает» — не признано ни одной строкой, а W99 и C09 прямо отказываются решать «чем кончается
   заход». Починка: строка `acceptance` словами самой карточки; отдельная строка `open → PLAN` на то,
   ЧЕМ игра это показывает; сужение W99 и C09 до того, что карточка действительно оставила снаружи
   (счёт, награда, провал, повторный заход, экономика).
4. **V4.** W05 поднята в `answered`, хотя половина строки (стабильный идентификатор, A3) — решение
   ноги в документе, объявившем себя необязывающим. Починка — понижение этой половины в `open` с
   названным преемником (PLAN/ADR), как у четырёх её сестёр.
5. **V5.** «Не меньше трёх комнат, соединённых проходами, и… обстановка» (крит. 6) и «одна названная
   точка доставки за его пределами, поставленная руками» (крит. 2) не помечены `acceptance`. Починка —
   пометить и увезти в `done_when` задач.

**Две развилки владельца висят и нарезкой НЕ решаются:** форма файла профилей (`W52`,
`d-profile-file-format-recommendation-002`) и чтение слова «инструментов» в критерии 10 (`W80`,
`d-toolkit-reading-of-criterion-10-002`). Обе в `NOW.md` `decisions`, обе ждут его слова. Нарезка
обязана назвать, какие именно задачи на них стоят, и не ставить их в работу до его ответа.

**Постоянные правила владельца, действующие на эту нарезку:**
- **Задачи режь МЕЛКО**, единица задачи — видимый глазами результат (`i-bet-tasks-never-sized-001`).
- **Дат и сроков не назначать**, пока он сам о них не заговорит; аппетит и порог смерти — его словами.
- **Логика только расширяется**, расслоение строится сразу правильно, мультиплеер не добавляется потом
  (`i-owner-standing-rule-extend-never-rebuild-001`).
- **Рабочие сцены законны при трёх условиях** — названный кусок, панель условий в редакторе, корень
  попадает в интегрированную сцену как только существует (`i-owner-working-scenes-need-real-tools-001`).
- **Параллельность живёт полосами внутри одной карточки**, один владелец изменений у сцены — это
  условие, под которое он принял рамку одной карточки.
- Переписка с ним — простым языком, без кодов задач и внутренних идентификаторов.

## boundaries

- **Карточку не переоткрывать.** Двенадцать критериев утверждены; четыре правки подписаны построчно.
- **Ни одна открытая строка не отвечается нарезкой.** 19 `open` остаются открытыми со своими
  отвечающими; строка, придуманная ногой, закрытой быть не может.
- **Арх-файл в `done_when` не попадает ни одной строкой** — только в `context` нарядов уликой.
- Продуктовую работу не запускать: нарезка режет и получает его слово, наряды выпускаются отдельно.
- Свежая физическая сессия, владелец в чате (нужны его слова про аппетит, полосы и WIP).

## done_when

1. `g-1d84` получил `appetite` и `kill_by` **его словами**, и стал ставкой по его отдельному слову на
   показанный ему целиком экран ставки. Дат не назначать.
2. Список задач: каждая ≤ половины сфокусированного дня, единица — видимый глазами результат;
   названо, сколько из них идёт одновременно (его WIP).
3. Полосы объявлены ТОЛЬКО его точными словами либо не объявлены вовсе; у каждой полосы назван её
   кусок и момент, когда её корень попадает в интегрированную сцену.
4. Настоящий список вырезов (G6), вердикт по каждой из семи линз устава и задача, проверяющая самую
   рискованную догадку.
5. `done_when` задач собран из строк `acceptance` **и из прочитанной напрямую карточки** — цель захода
   с точкой доставки и три комнаты с обстановкой присутствуют явно.
6. Пять починок носителей исполнены (V1-V5), ни одна не превращена в новый ответ; строка реестра
   `TREE.md` о четырёх токенах записана с их точной силой.
7. Названо построчно, какие задачи ждут его двух висящих развилок, и они не поставлены в работу.

## return

Полный Direction RESULT с экраном ставки, подписанным его словом. `next` — первые наряды.

budget: одна свежая сессия, владелец в чате

END_OF_FILE: live/indie-game-development/work/c-shape-g-1d84-001-call.md
