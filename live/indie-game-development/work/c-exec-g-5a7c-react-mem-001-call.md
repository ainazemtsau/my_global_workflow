# CALL — c-exec-g-5a7c-react-mem-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: householder
task: t-react-mem
issued: 2026-08-11 by s-work-g-5a7c-react-mem-dispatch-001
status: ready
basis: `2f7f2e87b3d3fa1e3b40e50f5063a87530fc5187`, дерево
       `79fb9710f2d02f9983c94a0704e8ef96a454ee3f` — `origin/dev` = `origin/main`, перемерено
       `git ls-remote origin` перед выпуском.

**`origin/dev` сдвинулся — СТОП ДОМОЙ.** `t-cargo-2` идёт параллельно в WIN-U1 и может
опубликоваться раньше.

Пин: `engineering_contract: 36` против продуктового `synced_contract_version: 36`. Re-sync не нужен.

## slot

**WIN-U2**, каталог `C:\projects\Unity\GasCoopGame_win-u2`, ветка `slot/win-u2`, стоит РОВНО на
базисе. Аренда свободна (предыдущий наряд остановлен владельцем до единой строки кода).

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-react-mem-001:BUILD`
2. readback: `git rev-parse HEAD` = `2f7f2e87…`, `HEAD^{tree}` = `79fb9710…`, `git status` пуст.
3. Чужая аренда или не-AVAILABLE — СТОП домой. WIN-U1 занят параллельной дорожкой.

Unity для этой задачи НЕ нужен: изменение ограничено engine-free `Core` и headless-тестами. Мост
WIN-U2 при этом живой (`http://localhost:27497/p/41ebd445`, перемерен), если понадобится.

## goal

**Хозяин получает НОСИТЕЛЬ: событие несёт метаинформацию, он помнит несколько событий сразу,
накопленное переживает истечение, и его логику решений можно подменить прямо в игре.**

Это фундамент под прямое требование владельца, названное им трижды: поведение хозяина должно быть
сложным и расширяемым, обработчики на выбранные комбинации должны въезжать НЕ ТРОГАЯ ядро, и
накопленное должно влиять — его слова: «хозяин уже туда ходил, он знает — он туда бежит, хотя
вначале идёт».

**Поведение игры этой задачей НЕ меняется.** Она делает следующие шаги дешёвыми, а не видимыми.

## context — перемерено чтением байтов на `2f7f2e87`

### Что хозяин помнит сегодня, и чего в этом нет

`HouseholderNoticedFact` (`Householder.cs:155-200`) несёт РОВНО ЧЕТЫРЕ вещи: имя факта, номер
случая, время замечания, время истечения, плюс признак «обработан».

**Места нет.** Оно живёт отдельно и в единственном экземпляре: `_attentionPoint` +
`_hasAttentionPoint` (`AuthoritativeHouseholder.cs:139-140`), перезаписывается КАЖДЫМ принятым
шумом (`:269-270`). Комментарий файла честен: «It is remembered until the next noise replaces it».

**Громкости нет.** `WorldSituation` несёт `Strength` (`Core/Situations/WorldSituation.cs:36`), но
проекция превращает силу в ПОЛОСУ (`WorldSituationNoise.Band`) и число выбрасывает: до хозяина
доезжает `HouseholderStimulus` = имя события + X + Z (`:37-42`). Отличить еле слышный шорох от
почти-стука он физически не может.

**Накопления нет ни одного.** Единственное растущее число в памяти — `LastFactOccurrenceId`
(`Householder.cs:961`), и это бирка сопоставления факта с ответом, а не величина: читается только
как id (`:510`, `:554`, `:1162`). Поиск `++`/`+= 1` по всем 1702 строкам даёт одно попадание — шаг
по маршруту. Истечение стирает факт в ноль (`:1052-1056`), следа «это уже было» не остаётся.

**Фактов ровно один.** Поле `NoticedFact` (`:955`) — не список. На втором разнородном ядро БРОСАЕТ
`InvalidOperationException` (`:1072-1081`); перемерено, что это не регресс — тот же бросок стоял на
базе волны `4cf2b5dc:1040`. В собранной игре второй факт молча не слышится
(`AuthoritativeHouseholder.CanHear:368-373`).

### Форма памяти — и почему она дешевле, чем записано в улике

`HouseholderLocalMemory`: 13 полей, внутренний позиционный конструктор (`Householder.cs:913-941`),
публичный посевной (`:888-911`), и ПЯТЬ мест сборки внутри `Step`
(`:1236, :1289, :1350, :1433, :1603`).

**Тестов среди этих мест НОЛЬ, и это перемерено поимённо.** Все ДВЕНАДЦАТЬ конструкций памяти в
тестах — `HouseholderAdditionalReactionTests.cs:129,476`, `HouseholderInTheJudgesTickTests.cs:168,207`,
`HouseholderProfileTests.cs:74,141`, `HouseholderReactionDataTests.cs:116`,
`HouseholderTests.cs:14,316`, `HouseholderWalksTheHouseTests.cs:202,296,347` — идут через
ОДНОАРГУМЕНТНЫЙ посевной конструктор `new HouseholderLocalMemory(point)` и новым полем не ломаются.

### Шов под свою логику объявлен и недостижим из игры

`IHouseholderDecisionPolicy` (`Householder.cs:451`) и трёхаргументный `Step` (`:1029`) существуют.
Игровой путь их не зовёт: `AuthoritativeHouseholder.cs:234` вызывает двухаргументный `Step`,
который жёстко подставляет единственную политику (`:1024`); трёхаргументный зовут только тесты
(`HouseholderTests.cs:266`).

**Ставить политику придётся НА СУЩЕСТВУЮЩИЙ экземпляр, а не через конструктор.**
`CarryWorld.cs:78` — `public AuthoritativeHouseholder Householder { get; } = new AuthoritativeHouseholder();`
— свойство только на чтение с инлайновой инициализацией; довод конструктора с игрового пути
недостижим.

### Три существующих теста ассертят снимаемое ограничение

Названы поимённо, чтобы их пересмотр не выглядел правкой ожиданий:
`HouseholderAdditionalReactionTests.Step_DifferentReactionWhileFactExists_IsExplicitlyUnsupported`
(`:88`, бросок на `:103`);
`HouseholderAdditionalReactionTests.Step_BoardCreakFactStillExpiresDuringActiveResponse`
(`:115`, бросок на `:151`);
`HouseholderInTheJudgesTickTests.ASecondDifferentNoiseIsRefused_AndEveryoneElseStillGetsThatTick`
(`:337`).

Они описывают ОГРАНИЧЕНИЕ, которое владелец приказал снять. Пересмотр с причиной разрешён и
правкой ожиданий НЕ считается. **Все остальные тестовые файлы остаются нетронутыми.**

### Что уже зелёное

`2f7f2e87` даёт **158 passed, 0 failed, 0 skipped** — прогнано Направлением на этой голове.

## boundaries — что НЕ трогать

- **НЕ ЗАКОЛАЧИВАТЬ ДВЕРЬ.** Правило владельца
  (`knowledge/core-is-extended-never-rewritten.md`): ядро только РАСШИРЯЕТСЯ. По каждому месту,
  которое эта задача открывает, в возврате называется результат теста **«чтобы добавить сюда новое
  — ДОБАВИТЬ или ПРАВИТЬ?»**. Не назвал — правило не применено.
- **Обработчиков на комбинации, веток поведения и авторинга файлом здесь НЕТ** — это `t-react-2`.
  Здесь строится носитель и включается точка, через которую он читается.
- **Файлы дорожки `cargo-hands` — чужие:** `Core/Cargo/**`, `Network/CargoBody.cs`,
  `Network/NetworkPlaySettings.{cs,asset}`, тесты `Cargo*`, `TelekineticPickup*`,
  `FirstPersonCarry*`. Упёрся — СТОП домой.
- **Публичная сигнатура `CarryWorld.Advance` не меняется** — её зовут тесты чужой дорожки и
  `NetworkWalkerCourier.cs:160`. Внутренности править можно.
- **Пороги `60 / 150 / 400` не двигаются**, спецслучая под волочение нет.
- `IntegratedHouse.unity` не редактируется. `ProjectSettings/**`, `Assets/GasCoopGame/**` закрыты.
- **ЗАПРЕЩЁННЫЙ ДОВОД:** число существующих тестов не является основанием сузить задачу — владелец
  снял это дословно. Тесты — забота исполнителя.
- Самодельный сканер исходников уликой не является и не пишется.

## headless evidence

- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`. База — `158 passed`; после задачи столько же плюс новые, минус
  три названных пересмотренных.
- **Гейт дешевизны доказывается ДОБАВЛЕНИЕМ настоящего поля,** а не заявлением: дифф показывает,
  что новое поле памяти не потребовало трогать пять мест сборки.
- **Гейт «политика работает в игре»:** тест ставит СВОЮ политику на `CarryWorld.Householder`,
  гоняет `CarryWorld.Advance` и получает ДРУГОЕ поведение. Тест на пути правил этого не доказывает.
- **Зелёные ворота поведением игры не являются** (`knowledge/green-gates-do-not-compile-the-game.md`).

## откат

- **Точка возврата — ГОЛОВА ДОРОЖКИ на момент старта задачи**, а не дерево базы волны: `t-cargo-2`
  публикуется параллельно и базу перекроет. Ровно это уже случилось на `t-react-1`.
- Откат назвать ОДНОЙ командой и **исполнить проверкой первой рукой**: `git rev-parse <ветка>^{tree}`
  возвращает дерево той головы знак в знак. Выведенный откат назвать выведенным.
- Прямо заявить, что вне репозитория для отката ничего не нужно.

## done_when

1. **Событие несёт метаинформацию, и набор полей расширяем.** Замеченный факт несёт КАК МИНИМУМ:
   что, ГДЕ (место события, а не одна общая точка внимания), КОГДА, НАСКОЛЬКО ГРОМКО — громкость
   доезжает до хозяина числом, а не только полосой. **Хозяин держит НЕСКОЛЬКО таких событий
   одновременно, каждое со своим сроком**; бросок `Householder.cs:1072-1081` и молчаливый отказ
   `CanHear` сняты. Дешевизна доказана диффом: добавление поля не потребовало трогать пять мест
   сборки памяти.

2. **Есть накопление, переживающее истечение события, и своя логика работает В ИГРЕ.** Минимум
   накопления: «сколько раз он уже замечал такое в этом месте» — величина, а не бирка, читаемая
   решателем. Своя политика ставится на существующий `CarryWorld.Householder` и доказывается тестом
   через `CarryWorld.Advance`, а не через путь правил.

3. **Поведение не изменилось, границы целы, откат проверен.** Те же пять реакций и те же числа;
   существующие тесты зелёные, кроме ТРЁХ названных поимённо, пересмотренных с причиной — прочие
   тестовые файлы не тронуты, доказано диффом по `tests/`. Границы целы — сцена, файлы
   `cargo-hands`, публичная сигнатура `CarryWorld.Advance`, пороги, `ProjectSettings/**`. Откат
   назван одной командой и проверен readback дерева.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / householder / t-react-mem`.

**ПЕРВОЙ СТРОКОЙ — РАСПИСКА:** число полученных строк `done_when` (их три). До неё наряд не выдан.

Также обязательно:

- **тест «ДОБАВИТЬ или ПРАВИТЬ» по КАЖДОМУ открытому месту** — это правило владельца;
- **какие поля теперь несёт событие** и как добавляется восьмое — точным числом правок;
- **чем выражено накопление**, кто его читает и что произойдёт с ним при перезапуске мира;
- **как политика ставится в игре** и почему именно так, раз свойство `Householder` только на чтение;
- **что стало с тремя названными тестами** — по одной причине на каждый;
- проверенный откат: команда и readback дерева;
- терминальный readback селектора `CLEAN / AVAILABLE / lease none`;
- **отдельной строкой: с какой головы дорожки `cargo-hands` собиралось перед публикацией**;
- расхождения свежей базы с grounds наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает.**

## budget

Один engineering root. Половина дня. Поверхность перемерена и лежит в одном файле `Householder.cs`
плюс `AuthoritativeHouseholder.cs` и одна строка `CarryWorld.cs`. Обработчики, ветки поведения,
авторинг файлом, долговременная память как система сюда НЕ входят. Не помещается без расширения
границ — STOP HOME с одним точным blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-react-mem-001-call.md
