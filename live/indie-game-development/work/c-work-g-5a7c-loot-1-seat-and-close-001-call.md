# CALL — c-work-g-5a7c-loot-1-seat-and-close-001

to: session
play: work
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-loot-1
issued: 2026-08-14 by s-work-g-5a7c-loot-1-binding-g5-001
status: ready

## goal

**В ДОМЕ ВИДНО ДЕСЯТЬ ПРЕДМЕТОВ, А НЕ ДЕВЯТЬ, И `t-loot-1` ЛИБО ЗАКРЫТА, ЛИБО ЕЁ ОСТАТОК НАЗВАН
ОДНОЙ СТРОКОЙ.**

## context

Биндинговая проверка G5 прошла на опубликованной голове `53afdc2d` и дала **FAIL** —
`history/2026-08-14-s-work-g-5a7c-loot-1-binding-g5-001.md`. Четыре строки `done_when` из шести
прошли попытку опровержения и перемеряны первой рукой; повторять их не надо:

- **строка 2** (состав правится без кода) — массив `_cargoSpawns` + два поля на точке; незнакомый
  id кладёт хост с объяснением до спавна;
- **строка 4** (ступень 144 / 300 / 720 = 1 / 2 / 4 руки) — `_cargoLiftPower: 200`,
  `_cargoGravity: 3`; `LootHeft` не несёт собственных порогов;
- **строка 5** (четыре руки потолок) — 240 → 4 руки, предел 266.67 массы;
- **строка 6** (откат одним коммитом) — `git diff 53afdc2d 1307aa3e -- Assets tests | git apply
  --check` чисто, код выхода 0.

Провалены строки **1** и **3**, и обе одним фактом: **ряд 2 расстановки, «Loot 2 Bedside Radio»,
стоит ЦЕЛИКОМ внутри коллайдера матраса.** Точка плана (x, z) = (−4.400, 2.500) — геометрический
центр кровати; названная опора `Bed Frame` даёт верх y = 0.450; тело предмета ложится
y 0.450…0.490; `Bed Mattress` (BoxCollider `3338339972013066219`, не триггер, включён, слой 0,
Rigidbody нет) занимает x −4.975…−3.825, y 0.450…0.600, z 1.600…3.400. Перекрытие равно всему
габариту предмета. Спавнится он кинематическим, поэтому его даже не выталкивает — он просто внутри
непрозрачного ящика, и в доме видно девять предметов вместо десяти, лёгких — четыре вместо пяти.

**Почему это вышло законно, и почему заплатка «сдвинуть на глаз» лечит симптом.** `LootSupport`
спрашивает `surface.Raycast` — только НАЗВАННЫЙ коллайдер, — а `LootSupport` на матрасе нет вовсе.
Шесть опор префаба: `Kitchen Table`, `Bed Frame`, `TV Stand`, `Kitchen Counter`, `Bedside Table`,
`Coffee Table`. То есть настоящий верх кровати назвать было физически нечем.

Два известных места, куда предмет садится чисто, — на выбор исполнителя, оба перемеряны:
`Bed Mattress` с собственной `LootSupport` (верх y = 0.600), либо открытая кромка рамы при
z ≈ 3.45 (матрас кончается на z = 3.400, рама — на 3.500).

Улика: `i-bedside-radio-spawns-inside-the-mattress-001`.

Ручная приёмка владельца после этой проверки стоит так:

1. отдельный вес одному предмету — **закрыт его словами** «я проверял смену веса»;
2. покрытие пола — **закрыт замером, его рук не ждёт**: четыре половые плиты дома не несут
   коллайдера вовсе, единственный пол — плоскость сцены на y = 0, снятие покрытия не сдвинет ни
   одного предмета;
3. модель по типу — хост **его словами**, куб-запаска **байтами** (`_visual` заполнен только у
   id 1), клиент **ждёт**: 2026-08-14 он ответил дословно «Второе окно не запускал / не помню».

## boundaries

- **Вырез Unity-проверок не переоткрывается.** Владелец ответил «а» 2026-08-14 по
  `d-loot-unity-matrix-cut-001`; улика `i-unity-checks-for-the-loot-foundation-were-cut-001` несёт
  триггер на первую же ногу, трогающую лут, — это её дело, а не строка этой задачи, и требовать
  тестов эта нога не обязана.
- **Не расширять `t-loot-1`.** Четыре улики лута — проваливающийся груз, хозяин на луте,
  лаборатория без расстановки, вырез проверок — закрытия задачи не блокируют.
- **Замороженный кандидат `e0a30194` не открывать.**
- Из OS-сессии продуктовый репозиторий руками не править — правка едет нарядом исполнителю.
- Владельцу ставить только то, что он увидит в игре; несделанный им пункт — `waiting`, не FAIL.

## done_when

1. **НИ ОДИН ИЗ ДЕСЯТИ ПРЕДМЕТОВ НЕ СТОИТ ВНУТРИ ДРУГОГО ТВЁРДОГО ТЕЛА** на опубликованной голове,
   и это показано числами по каждому из десяти, а не по одному пересаженному.
2. **ОСТАТОК ПРИЁМКИ НАЗВАН ИЛИ ВЗЯТ.** Пункт про клиента либо получил его дословные слова, либо
   помечен `waiting` с точной формулировкой.
3. **`t-loot-1` ЗАКРЫТА ИЛИ ЕЙ НАЗВАН ОСТАТОК ОДНОЙ СТРОКОЙ.** Закрытие — только когда строки 1 и 3
   перестали быть проваленными; пересказ возврата уликой по-прежнему не считается.

## return

RESULT игры `work`: состояние строк 1 и 3 с числами по всем десяти предметам; диспозиция улики про
радио; статус `t-loot-1`; при закрытии — `state_changes`, закрывающие задачу. `t-loot-1` НЕ
последняя задача ставки: `t-house-asset-1` открыта в этой же дорожке, `t-handlers-1` и `t-lab-2`
ждут в соседней, — `review` ставки этой ногой не открывается.

## budget

Одна нога.

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-loot-1-seat-and-close-001-call.md
