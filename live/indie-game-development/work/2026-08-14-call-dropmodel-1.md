# НАРЯД — УБРАТЬ ПРИСЛАННУЮ МОДЕЛЬ ХОЗЯИНА

**Слот:** WIN-U2 · **Репозиторий:** `C:\projects\Unity\GasCoopGame_win-u2`
**Базис:** `origin/main` — перебазируйся и назови голову в возврате.

**ПЕРВАЯ СТРОКА ВОЗВРАТА — РАСПИСКА:** сколько строк `done_when` (их четыре), голова и число тестов
до первой правки.

---

## goal

Присланная модель хозяина уходит из репозитория, чтобы в дальнейшем не путаться с той, что делается
сейчас.

## context

**СЛОВО ВЛАДЕЛЬЦА 2026-08-14:** «нужно удалить модели, которые пришли. Они там не оптимальны, мы там
сейчас занимаемся другой моделью хозяина. Эти нужно удалить, дальше мы потом не путались, и в этой
волне не будет внедрения модели хозяина».

**УДАЛЕНИЕ БЕЗОПАСНО, И ЭТО ПРОВЕРЕНО, А НЕ ПРЕДПОЛОЖЕНО.** На роли хозяина стоит заглушка Kenney
(`guid 1affb07ef0c98ac438cd7a1031b30d9f`), не присланная модель. Поиск по `guid` файла
`homeowner.fbx` (`638af4864c3f5994c95bfe266f771531`) вне самой папки даёт **ноль ссылок** —
ни в `Assets`, ни в `Packages`, ни в `ProjectSettings`. Игра её не использует.

**ЧТО УДАЛЯЕТСЯ:** папка `Assets/TunnelCrew/Art/Bodies/Sources/Anton/` целиком — 160 файлов
в дереве (FBX хранятся в LFS).

## boundaries

**ТЫ УДАЛЯЕШЬ РОВНО ОДНУ ПАПКУ** и её `.meta`. Ничего больше.

**ТЫ НЕ ТРОГАЕШЬ:**

- `Assets/TunnelCrew/Art/Bodies/HOW-TO-SEND-A-MODEL.md` — **это общая инструкция для будущих
  моделей, а не документ про эту.** Она нужна, когда приедет следующая.
- `Assets/TunnelCrew/Art/Bodies/Prefabs/**` и `Sources/Quaternius/`, `Sources/Kenney/` — рабочие
  заглушки, на них стоят обе роли.
- `Assets/TunnelCrew/Settings/**` — роли и размеры не меняются.
- `docs/results/**` — отчёты о выполненной работе не переписываются задним числом.
- любые файлы трёх идущих ног: генератор дома, физика груза, план ядра.

## done_when

1. **ПАПКА `Sources/Anton/` УДАЛЕНА ЦЕЛИКОМ** вместе с `.meta`.
2. **ИГРА РАБОТАЕТ КАК РАНЬШЕ** — обе сцены открываются, хозяин и налётчики на месте с прежними
   заглушками. Проверено запуском, не предположением.
3. **ГЕЙТ ЗЕЛЁНЫЙ** — число тестов совпадает с базовым, ноль пропущенных. Расхождение означало бы,
   что на модель кто-то всё-таки ссылался.
4. **ОТКАТ ОДНИМ КОММИТОМ**, доказанный выполненным `git revert`; SHA коммита и дерева в возврате.

## return

Короткий отчёт `docs/results/c-exec-g-5a7c-dropmodel-1-001.md`. Расписка, четыре строки, и
**отдельно — не осталось ли осиротевших ссылок**: если Unity при открытии проекта пожалуется на
пропавшие файлы, это надо назвать, а не проглотить.

## budget

Меньше ноги. Если окажется, что что-то на модель всё-таки ссылалось — **остановись и верни фактом**,
не чини молча.

END_OF_FILE: live/indie-game-development/work/2026-08-14-call-dropmodel-1.md
