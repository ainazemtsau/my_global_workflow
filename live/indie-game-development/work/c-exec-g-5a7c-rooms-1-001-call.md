# CALL — c-exec-g-5a7c-rooms-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-rooms-1
issued: 2026-08-12 by s-work-g-5a7c-rooms-1-dispatch-001
status: ready
basis: `4e5858b4b7cf4a6c8c9cad08e3fde4a6915200f3` — `origin/main` = `origin/dev` =
       `origin/slot/win-u2`, перемерено `git ls-remote origin` 2026-08-12 после публикации шва.

**НАРЯД РАЗБЛОКИРОВАН: шов `t-places-1` опубликован и закрыт.** Поле роли у места существует
(`World/HousePlaceMarker.cs:30` — `public string Role`), поиск по роли живёт в
`Core/House/HousePlan.cs:139` (`TryFindPlaceByRole`), а профиль называет роль через
`TryResolvePlaceRole`. Неизвестная роль теперь НЕ убивает хозяина: шаг с ней просто становится
пустым. Ты этим пользуешься, а не заводишь заново.

**ЕСЛИ `origin/main` СДВИНУЛСЯ — НЕ СТОП, А ПЕРЕБАЗИРОВАНИЕ.** База прогона на `4e5858b4`:
**219 passed, 0 failed, 0 skipped**, прогнано Направлением. Слот WIN-U2 отпущен:
`CLEAN / AVAILABLE / lease none`, дерево уже на `4e5858b4`.

## slot

**WIN-U2**, каталог `C:\projects\Unity\GasCoopGame_win-u2`, ветка `slot/win-u2` — тот же слот, в
котором закрылся шов. Перемерено селектором 2026-08-12 после публикации: `head 4e5858b4`,
`state: CLEAN`, `lifecycle: AVAILABLE`, `lease: none` — дерево уже актуально, догонять нечего.

1. `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U2 -Claim -LeaseId c-exec-g-5a7c-rooms-1-001:BUILD`
2. `git fetch origin`, встать на свежую `origin/main`, readback дерева, `git status` пуст.

**UNITY НУЖЕН ПО-НАСТОЯЩЕМУ:** геометрия, префабы, сцена, физические тела. Мост WIN-U2 проверен
2026-08-12: отвечает, Unity `6000.5.5f1`. Поле `mcp-endpoint: unrecorded` в файле состояния —
устаревшая запись, а не отсутствие моста.

**ПАРАЛЛЕЛЬНО В WIN-U1 ИДЁТ НАРЯД НА ТЕЛА** (`c-exec-g-5a7c-body-1-001`). Он живёт в
`Assets/TunnelCrew/Art/**`, в презентации и в `World/Householder.prefab`. **Ты не открываешь ни один
из них.** `World/Householder.prefab` — тело хозяина, не часть дома, и оно не твоё.

## goal

**Дом перестаёт быть коробкой и становится жильём, в котором понятно, где ты, и есть за что
спрятаться и обо что задеть.**

Это критерий 5 узла, подписанный владельцем, дословно: «Рабочая архитектура из его спеки:
одноэтажный дом около 11 × 8.5 м, потолок 2.6 м, гостиная / кухня / спальня / коридор, соединённые
проходами. Стены выше глаз ходока… В комнатах стоит обстановка, которую несущие задевают, и
задевание идёт ТЕМ ЖЕ швом "контакт производит шум", а не новым».

**НАЗВАННЫЙ ПОДХОД — ОДИН:** дом строится РУКАМИ, обстановка становится обычным физическим телом, и
шум от неё идёт существующим контактным швом. Никакой процедурной генерации, никакого нового вида
события, никакого отдельного «шума мебели». Отход — ESCALATE домой.

**Слово владельца 2026-08-12:** «не делаем расчёт на то, что у нас будет процедурная генерация, то
есть вручную все уровни будем делать… Сейчас исходим из того, что делаем всё руками». И отдельно про
инструмент: расстановка через Unity MCP разрешена как СПОСОБ работы — ставить коробки командами
можно и нужно, — но строить «накидыватель уровней» запрещено, это его отдельная отложенная идея.

## context — перемерено чтением байтов 2026-08-12

Числа дома, стен и хозяина сняты на `f3c40c40` и ПЕРЕПРОВЕРЕНЫ как не затронутые швом: шов тронул
`HousePlaceMarker.cs` и `HousePlan.cs` кодом, но ни одного `.prefab`, `.unity` или `.asset` в его
диффе нет. Значит всё ниже верно и на `4e5858b4`.

### Что за дом стоит сегодня

`Assets/TunnelCrew/World/ThreeRoomHouse.prefab`:

- **три комнаты**, названные по сторонам света, а не по назначению: `West Room`, `Middle Room`,
  `East Room`;
- **семь мест**: `Bedside`, `West Window`, `West Door Side`, `Hall Centre`, `Work Table`,
  `Store Corner`, `East Door Side`;
- **два проёма**: `West Doorway`, `East Doorway`, оба `_width: 2`;
- **восемь стен**, и все восемь имеют `m_LocalScale` с `y: 1.5`. Внешние — длиной `14.5`,
  внутренние перегородки — `6`, толщина `0.5`. То есть сегодняшний след дома примерно **14.5 × 14
  м**, а критерий требует **около 11 × 8.5**: это перестройка со СЖАТИЕМ, а не подгонка. Помни, что
  налётчик ростом `0.16` м, и сжатие дома заметно меняет расстояния, которые он проходит.

### Хозяин, чьи глаза задают высоту стен

`Assets/TunnelCrew/Settings/HouseholderWalkSettings.asset`: `capsuleHeight: 2`, `capsuleRadius: 0.5`,
`movementSpeed: 1.75`, `urgentMovementSpeed: 3.25`, `waypointPauseSeconds: 0.6`. Стены на `1.5` при
двухметровом хозяине — это и есть та дыра, которую закрывает строка 1
(`i-house-walls-below-eye-level-001`).

### Почему мебель шумит БЕСПЛАТНО, и это главный подарок задачи

`Assets/TunnelCrew/Network/CargoBodyContact.cs:9-11`, дословно: «Only the house is physical here, so
every press this records is a press against the house; being caught by a walker is answered in the
rules layer instead». То есть шов не различает, ЧТО именно физическое: любое физическое тело, кроме
ходока, уже считается домом и уже производит контакт.

**Следствие: обстановка, поставленная как обычное физическое тело, начинает шуметь сама, без единой
новой строки в шве.** Это ровно то, чего требует критерий 5 («тем же швом, а не новым»), и это надо
подтвердить прогоном, а не принять на веру.

**Следствие второе, которое надо назвать в возврате, а не замолчать:** шум от мебели сегодня будет
НЕОТЛИЧИМ от шума о стену — тот же вид события, те же пороги. Различать их эта задача не просит и
не должна; но если при прогоне окажется, что дом стал шуметь заметно чаще, чем раньше, это факт для
владельца, а не повод крутить пороги.

### Что про дом уже знают правила

`Core/House/HousePlan.cs` (комнаты, места, проходы; строится один раз, только читается),
`Core/House/IHousePlanSource.cs`, `World/SceneHousePlan.cs` (читает authored-маркеры
`HouseRoomMarker` / `HousePlaceMarker` / `HousePassageMarker`, НЕ коллайдеры; `Complaints()` жалуется
на недоделанную разметку), `Network/NetworkHouseholder.cs:42,80,102` (спрашивает план один раз до
движения).

**Поле роли места УЖЕ ЕСТЬ, НО ОНО ПУСТОЕ** — и это твоя работа. Шов `t-places-1` привёз механизм:
`HousePlaceMarker.Role` (`:30`), `HousePlan.TryFindPlaceByRole` (`:139`) и разрешение роли в
загрузчике профиля. Но **значений ролей в сегодняшнем доме нет ни одного**: шов по своей границе не
имел права трогать префаб. Ты пользуешься механизмом и заполняешь роли — заводить его заново не надо.

Побочно знай: неизвестная роль теперь НЕ убивает хозяина, а делает свой шаг пустым. Значит дом с
недозаполненными ролями запустится МОЛЧА, и `Complaints()` — единственное, что тебе про это скажет.

### Сцены — фикстуры тестов, и это меняет цену правки

`IntegratedHouse.unity` и `HouseholdLab.unity` копируются в тестовый проект как фикстуры
(`tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj`, блок `None Include`), и по ним ходят
scene-fixture тесты. Ты имеешь право править `IntegratedHouse.unity` — она принадлежит твоей
дорожке. **Но каждое изменившееся ожидание теста обязано быть названо в возврате с объяснением,
почему это правильное изменение, а не поломка.** `HouseholdLab.unity` ты НЕ открываешь: она
принадлежит задаче `t-lab-2` в другой дорожке.

### Уже оплаченная обстановка, с которой можно начать

Ветка `origin/probe/free-kits-2026-08-06` (два коммита `f980dce9`, `e9f23cef`) несёт готовую
обстановку с лицензиями и образцом авторинга: `KayKit/lamp_standing.fbx`, `Kenney/chair.fbx`,
`Kenney/tableRound.fbx`, `Quaternius/Bookshelf.fbx`, `Quaternius/Houseplant_1.fbx`, материал пола
PolyHaven; раскладка `Art/FreeKitsProbe/Sources/<вендор>/` с `License.txt` и `SOURCE.md` плюс
собственные заготовки в `Art/FreeKitsProbe/Prefabs/` и `AUTHORING.md`.

**Это ЕДИНСТВЕННОЕ место в проекте, где обстановка уже есть.** Брать её или строить свою — твоё
решение; но если берёшь, лицензии и `SOURCE.md` едут вместе с ней. Новые бесплатные источники
разрешены по тому же образцу. **Любая покупка — ESCALATE домой.**

### База прогонов

`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **219 passed,
0 failed, 0 skipped**, прогнано Направлением 2026-08-12 на опубликованной голове `4e5858b4`. Это
твоя база.

`tools/hygiene.ps1` — зелёный. **`tools/check.ps1 -Deliver` КРАСНЫЙ на семи исторических отчётах —
зарегистрированный дефект `i-deliver-gate-red-when-main-catches-dev-001`, НЕ твоя работа.**

## boundaries

- **Задача не расширяется.** Три строки `done_when` — весь объём.
- **НЕ ОТКРЫВАЕШЬ:** `HouseholdLab.unity`, `World/Householder.prefab`,
  `Assets/TunnelCrew/Art/**` вне обстановки, презентацию, `Core/Householder/**`,
  `Assets/TunnelCrew/Profiles/**`, `StreamingAssets/**`.
- **НЕ ЗАВОДИШЬ поле роли места** — оно приезжает швом. Нет его — СТОП.
- **ПОРОГИ `60 / 150 / 400` НЕ ДВИГАЮТСЯ**, и специального случая под мебель не заводится. Если дом
  стал шуметь чаще — это факт в возврат, а не повод крутить числа.
- **НОВОГО ВИДА СОБЫТИЯ НЕТ.** Мебель шумит существующим контактным швом или не шумит вовсе.
- **НЕ ДЕЛАЕТСЯ:** второй дом и переключение домов (это `t-house-asset-1`), набор лута
  (`t-loot-1`), предметы с состоянием (`t-thing-1`), тоннели и входы в них, окна и двери, которые
  открываются, зрение, занятия хозяина, маршрут хозяина сверх того, что нужно, чтобы он не застрял.
- **НИКАКОЙ ПРОЦЕДУРНОЙ ГЕНЕРАЦИИ и никакого Dungeon Architect** — вычеркнуто владельцем дословно.
- **ОБЛИК НЕ ДОВОДИТСЯ.** Дом должен читаться как жильё, а не быть красивым. Устав прямо боится
  полировки внешнего вида; материалы — минимум, чтобы комнаты различались.
- **Настроечные значения — не код** (контракт 36): тестами не покрываются.
- **Тест только там, где владелец не увидит ответ глазом.** Здесь почти всё видно глазом; машинного
  доказательства требует ровно одно — что мебель шумит тем же швом.
- **Самописный сканер как улика поведения запрещён.**
- **Инструмент недоступен → СТОП домой.** Никогда обходной путь.
- **Решение владельца → ESCALATE домой.** Сюда относится всё, где придётся выбирать, как дом
  выглядит и что в нём стоит, если это меняет игру, а не декорацию.
- **Расписка обязательна:** наряд не существует, пока исполнитель не расписался.

## done_when

Три строки — потолок контракта 36. Ни одна не снимаема.

1. **ДОМ СТАЛ ЖИЛЬЁМ.** Четыре НАЗВАННЫЕ по назначению комнаты — гостиная, кухня, спальня, коридор
   — соединены проходами, по числам критерия 5 (около 11 × 8.5 м, потолок 2.6 м); **стены выше глаз
   хозяина**, а не по пояс, как сегодня; и на глаз это узнаваемое человеческое жильё, а не игровой
   лабиринт.
2. **МЕБЕЛЬ РАБОТАЕТ ДВАЖДЫ.** В комнатах стоит обстановка, она НАСТОЯЩЕЕ физическое препятствие —
   за неё можно спрятаться и об неё можно задеть, — и задевание её грузом производит шум **ТЕМ ЖЕ
   контактным швом, что и стена**: без нового вида события, без нового источника и без движения
   порогов. Это единственная строка, которую доказывает прогон, а не глаз.
3. **ПРАВИЛА ЗНАЮТ ЭТОТ ДОМ, И ОТКАТ НАСТОЯЩИЙ.** Каждая комната, место и проход размечены
   authored-маркерами, `SceneHousePlan.Complaints()` пуст, роли мест объявлены механизмом шва
   `t-places-1`; прогон тестов назван числом против фактической базы, и КАЖДОЕ изменившееся
   scene-fixture ожидание названо с объяснением; откат одним коммитом исполнен НАСТОЯЩИМ `revert`,
   дерево после которого сходится с деревом базы.

## return

**Первая строка возврата — РАСПИСКА:** сколько строк `done_when` получено (должно быть три), с
какого базиса собрано и подтверждение, что шов `t-places-1` действительно опубликован.

Дальше:

- что сделано, по каждой из трёх строк отдельно;
- **план дома**: какие четыре комнаты, какие места в них, какие проходы, какие роли где объявлены;
- **чем доказано, что мебель шумит тем же швом** — прогон, а не рассуждение;
- **стал ли дом шуметь заметно чаще** и на сколько — число, а не ощущение;
- полный список путей диффа; отдельно — каждое изменившееся ожидание scene-fixture теста с
  объяснением;
- откуда взята каждая единица обстановки и под какой лицензией;
- прогон тестов до и после числами;
- откат: команда, хеши, совпадение деревьев;
- **что НЕ сделано и почему**;
- всё неожиданное, включая всё, что противоречит перемеренному выше контексту.

Отдельной строкой: **что владелец должен увидеть глазом**, чтобы принять задачу, и как это
запустить. Строки 1 и 2 в части «спрятаться» судит он, а не ворота.

## budget

Половина сфокусированного дня. Порог смерти 3 этой ставки считает ноги: на общую задачу и первые
задачи всех трёх дорожек их положено не больше двенадцати.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-rooms-1-001-call.md
