CALL c-work-launch-control-demo-release-control-cycle-001
to: session
direction: indie-game-development
track: launch-control
play: work
node: g-b847
task: demo-release-control-cycle-001
issued: 2026-07-24 (s-work-launch-control-demo-release-control-installation-001)

goal: |
  One fresh Demo Release Control cycle gives the owner a disposable, truthful
  control screen for the triggering start-day, material-return, owner-verdict
  or hard-date event, while the Character-independent controller remains READY
  for the next event.

context: |
  Durable authority is
  `knowledge/g-b847-demo-release-control-policy-v1.md`. Outcome authority is
  `TREE.md`; current routing and evidence identities come only from fresh
  `NOW.md`, exact registered CALLs/RESULTs and named product/player/Steam
  receipts. Re-read them at the start of the cycle. This CALL contains no
  current-status snapshot.

  The ordinary root belongs to stable track `launch-control`, whose existing
  owner-approved `outcome_dispatch: true` right is bounded by the policy. The
  triggering owner intent arrives in plain words as start-day, a material
  return/loss, an owner verdict or a hard-date checkpoint. A state question is
  read-only. Each completed cycle returns one same-position READY continuation;
  no daily screen or plan is carried forward as authority.

boundaries: |
  Do not read the retired Demo Control Room as authority. Do not invent or
  approve Demo Basis, evidence, cuts, dates, private Steam facts, forecast
  inputs, release progress or February activation. Do not launch, pause,
  retarget or close a foreign root; owner launch/loss words may alter only the
  matching CALL bookkeeping allowed by policy and packet rules.

  Do not create a backlog, daily plan/archive, copied status, causal DAG,
  percent-complete model, second hierarchy or technical HOW. Do not edit
  TREE/CHARTER or product repositories. Do not run the installation replay,
  seven-day material-return probe or binding G5 inside an operating cycle.

done_when: |
  1. The owner receives one screen of at most 400 words containing only:
     selected route and next hard gate; latest real product/player result or
     NONE; exact accepted Demo Basis and proof horizon; focus; running;
     collision-free `launch now` items with surface, paste-ready CALL text and
     exit; exclusions/collision surfaces; protect/cut-next/cut-trigger;
     forecast class; and the next recompute trigger.
  2. Every displayed claim pins its fresh authority/evidence identity; any
     missing or stale identity fails closed under the policy.
  3. Admission is lawful 0..N: independent outcomes may rank together, zero
     creates no busywork, and the owner alone chooses what to launch.
  4. Forecast uses exact precedence `MISSED > UNFORECASTABLE > CREDIBLE > AT
     RISK`; missing dated inputs render `NO DATE BASIS`, and February stays
     inactive without exact owner words.
  5. No foreign root, plan, product or evidence claim changes except exact
     matching launch/loss bookkeeping supported by owner/runtime evidence.
  6. The RESULT records the fresh identities read and registers exactly one
     same-track READY continuation for the next event without storing this
     cycle's screen as living truth.

return: |
  A readable <=400-word owner screen followed by one work RESULT naming the
  triggering event, exact identities/evidence, lawful 0..N disposition,
  unchanged foreign boundary and one same-position READY continuation. No
  controller-authored target HOW and no copied screen/status artifact.

budget: one bounded event cycle
surface: cli

END_OF_FILE: live/indie-game-development/work/launch-control/c-work-launch-control-demo-release-control-cycle-001-call.md
