# CALL: перемерить весь склад улик, не починив ни одной

Задача: `t-issue-revision-1` · ставка `g-5a7c`, **вне полос девятой волны**
База: продуктовый `main` = `dev` = `origin/main` = `b22c00a2`.
Слот: **WIN-U2**.

---

## ПРОЧИТАТЬ ДО ПЕРВОЙ СТРОКИ

**Эта нога ничего не чинит. Она меряет.** Если по дороге захочется исправить однострочник — не
исправляй. Запиши в список и иди дальше. Нога, которая чинит попутно, уничтожает то единственное,
ради чего затевалась: доверие к результату замера.

**ЗАЧЕМ, ЧИСЛОМ.** Направление сегодня проверило шесть открытых улик перед тем, как выбрать работу
в свободный слот. **Четыре из четырёх проверенных оказались протухшими:**

| Улика говорит | На `b22c00a2` на самом деле |
|---|---|
| план дома собирается без мебели, тело хозяина не объявлено | **половина починена**: `SceneHousePlan.cs:86` передаёт `furnishings` и высоту потолка; `HouseholderRouteController.cs:289` зовёт `WithBody(body)` боевым кодом |
| в `dev` лежит подменённое `Is.EqualTo(3L)` | **исчезла**: в `main` правильное `2L` |
| капсула хозяина радиусом `0.5` задевает косяк | **число протухло вдвое**: `HouseholderWalkSettings.asset:32` даёт `capsuleRadius: 0.25` |
| неизвестный темп роняет сессию **каждый тик** | **переехало**: `AuthoritativeHouseholder.cs:480` зовёт проверку ОДИН раз, под `if (!_seated)`, до первого шага |

Ни одна не осталась такой, какой записана. **Список улик перестал быть очередью работ.** Каждый
выбор задачи стоит получаса на вопрос «а это ещё правда?», и дважды сегодня этот вопрос был задан
слишком поздно.

## goal

**Каждая открытая продуктовая улика получает судьбу по сегодняшним байтам, и склад снова
становится очередью.**

## done_when

1. **КАЖДАЯ ОТКРЫТАЯ ПРОДУКТОВАЯ УЛИКА ПЕРЕМЕРЕНА И ОТНЕСЕНА К ОДНОЙ ИЗ ТРЁХ КУЧ:**
   **УМЕРЛА** · **ЖИВА КАК ЗАПИСАНА** · **ЖИВА, НО ЗАМЕР ДРУГОЙ**.
   Ровно одна куча на улику. «Частично» — это **ЖИВА, НО ЗАМЕР ДРУГОЙ**, и тогда назови, какая
   половина умерла.

2. **НА КАЖДУЮ УЛИКУ — КОМАНДА И ЕЁ ВЫВОД.** Ту, которую запускал сам. «Проверил, не
   воспроизводится» без команды не принимается ни для одной строки. Это главная приёмка ноги: без
   неё ревизия станет ровно тем складом старых впечатлений, который она разбирает.

3. **НИ ОДИН БАЙТ ПРОДУКТА НЕ ИЗМЕНЁН.** Доказывается `git status --porcelain` в возврате: пусто.

4. **ДЛЯ КУЧИ «ЖИВА, НО ЗАМЕР ДРУГОЙ» НАЗВАНО, ЧТО РАЗЪЕХАЛОСЬ** — старое число против нового,
   старый путь файла против нового, старое имя символа против нового.

5. **НАЗВАНЫ ТРИ САМЫЕ ДОРОГИЕ ЖИВЫЕ УЛИКИ, С ПРИЧИНОЙ И С ПОВЕРХНОСТЬЮ ФАЙЛОВ.** Оценка, не
   наряд: что брать — решает владелец. Поверхность нужна, чтобы следующая нарезка знала про
   столкновения.

6. **ЧИСЛО НАЗВАНО:** сколько просмотрено, сколько в каждой куче.

## где лежат улики

Репозиторий направления: `C:\my_global_workflow_worktrees\indie-game-development`,
папка `live\indie-game-development\cards\`, файлы `i-*.md` со `status: open`.

**Читать только те, у кого `route:` НЕ `maintenance`** — маршрут `maintenance` означает улику про
сам Direction-OS, а не про продукт, и в ревизию не входит. Улики про репозиторий направления —
тоже мимо.

Тело улики лежит в блоке `## issue`. Там же обычно стоят пути и номера строк — **они и есть то,
что протухает**, так что проверяй сам факт, а не только строку.

**В репозиторий направления НЕ ПИШИ ничего.** Карточки правит только направление.

## что НЕ входит

- Любая правка продукта, включая однострочную и очевидную.
- Улики маршрута `maintenance` и всё про Direction-OS.
- Заведение новых карточек. Попавшееся по дороге идёт **списком в возврат**, карточку заводит
  направление.
- Проверка того, ПРАВИЛЬНО ли улика описывает желаемое поведение. Ты меряешь, воспроизводится ли
  её утверждение, а не согласен ли ты с ним.

## boundaries — ДВЕ НОГИ ИДУТ ОДНОВРЕМЕННО

Ты **только читаешь**, поэтому столкнуться не можешь ни с кем. Но знай, что занято, чтобы не
принять чужую незаконченную правку за состояние `main`:

- **WIN-U1** — зрение: `Network/NetworkHouseholder.cs`, `Network/HouseholderSightSource.cs`.
- **WIN-U3** — «почему»: `Probe/Householder/HouseholderDecisionPipeline.cs`,
  `HouseholderDecisionCandidate.cs`, шесть `Householder*DecisionHandler.cs`,
  `Network/NetworkWalkerCourier.cs`.

**Меряй по `b22c00a2`, а не по чужим рабочим деревьям.** Если улика бьёт в занятый файл — так и
скажи: «жива на базе, но файл сейчас правится ногой N». Это ценная строка, а не помеха.

## СТОП и вопрос домой

- **Улику нельзя перемерить без запуска игры или Unity** — не запускай. Отнеси её в отдельную
  четвёртую кучу **«НУЖЕН ЖИВОЙ ПРОГОН»** и назови, какой именно. Слот Unity этой ноге не нужен и
  не берётся.
- **Улик оказалось больше сорока** — стоп и число домой: разобьём на две ноги.
- **Улика ссылается на файл, которого больше нет** — это не стоп, это находка: куча «жива, но
  замер другой», и назови, куда файл переехал, если нашёл.

## return

1. **Таблица на каждую улику: id → куча → команда → её вывод одной строкой.** Это тело возврата.
2. Число: просмотрено, и сколько в каждой из четырёх куч.
3. Три самые дорогие живые, с причиной и поверхностью файлов.
4. Для каждой из кучи «замер другой» — что именно разъехалось.
5. `git status --porcelain` — пусто.
6. Всё, что заметил и не чинил, — списком. **Не чини молча.**

## budget

Одна нога, чтение и замеры. Unity не поднимается, тесты не гоняются, слот не занимается ничем
тяжёлым. Если полезла правка — задача понята неверно.

END_OF_FILE: live/indie-game-development/work/2026-08-31-call-issue-revision-1.md
