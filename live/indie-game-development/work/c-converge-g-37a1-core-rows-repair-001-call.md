> **RETIRED 2026-07-31 — DO NOT DISPATCH.** The bet `g-37a1` was closed with verdict `obsolete` (the owner changed the game concept). Every CALL issued under it is dead regardless of the status written below. This file is preserved as evidence of what was decided, never as a frontier. The live frontier is `live/indie-game-development/NOW.md`, which currently has `bet: null` and no open calls. See `history/2026-07-31-s-review-g-37a1-obsolete-concept-change-001.md`.

CALL c-converge-g-37a1-core-rows-repair-001
to: session
direction: indie-game-development
play: converge
node: g-37a1
goal: |
  Repair `work/converge-g-37a1-core-rows.md` on the sixteen named findings of
  `history/2026-07-28-s-converge-verify-g-37a1-core-rows-001.md` — and on NOTHING else. The rows
  FAILED step 3. Not one of the sixteen live requirement lines is challenged; every finding is
  against the acceptance instrument the rows are. When the named sections hold, step 3 reruns
  narrowly on what changed.
context: |
  THIS IS STILL STEP 3 OF THE OWNER'S OWN SIX-STEP ROUTE (`NOW.md` `d-post-verify-route-001`),
  which no leg may reorder without his words. Step 3 has not passed; this leg is what step 3's FAIL
  routes to by `os/plays/converge-verify.md` step 4 («FAIL: build returns named rows to
  converge»). Steps 1, 2 and 4 are complete. Step 5 — his ladder verdict — still waits on step 3.

  READ, in this order:
  - `history/2026-07-28-s-converge-verify-g-37a1-core-rows-001.md` — the sixteen findings F1–F16,
    each with what it hits and where it routes, plus seven findings that were REFUTED and must not
    be re-raised, and four hazards recorded as CONFIRMATIONS because the target already names them.
  - `live/indie-game-development/work/converge-g-37a1-core-rows.md` — the target.
  - `live/indie-game-development/TREE.md` `g-37a1` — the fifteen criteria, his verdict on exact
    text. A finding against a CRITERION is out of scope and none of the sixteen is one.
  - `live/indie-game-development/work/core-requirements-g-37a1.md` — the sixteen live lines, the
    NOT-core list and the settings register. Its «Цена смерти (сейчас ноль)» row is stale against
    the amendment and is named in the verification's captures.
boundaries: |
  REPAIR ONLY WHAT IS NAMED. The sections in scope are §GLOSSARY, §RULES, §KNOBS, §WHAT, §SEAM and
  ROWS 1, 5, 6, 9, 10, 12, 13, 14, 15, 16, 17. Everything else in the file is out of scope,
  including §OWNER, which the verification did not attack at all.

  NOT REOPENABLE, on his exact words: passability is deleted; there are no sinks; the way up is
  made by the player; two gases now with liquid as headroom; breach behaviour is an EVENT whose
  form is a SETTING; there is NO value below in the core and ROW 7 stays retired in place with its
  number kept; death restarts the level in full; air refills ONLY at the base; the rig carries a
  level button, a start state and an immortality switch, all defaulting to off; the core has no
  completion condition; and the core is NOT graded on «весело».

  THE SETTING RULE STILL GOVERNS: a question he turned into a knob is not an open question. Do not
  choose a value for K1–K5, for air capacity, for the base's refill or for the step rate. F1, F10
  and F15 are about which knobs may exist and what they are coupled to — never about their values.

  Do not touch `CHARTER.md`, `TREE.md` or either card. Do not write `knowledge/`. Do not open a
  bet, a task or a track. Do not read `archive/**` or the product repository. Do not answer
  `d-air-counter-visibility-001` — F5 says only that ROW 15's test (b) leans on it, not that it
  must be decided. Do not manufacture an owner decision; F11 is the only finding that may need his
  word, and deciding whether it does is this leg's call, not a licence to open one silently.
done_when: |
  1. F1 — §KNOBS' `section` group versus ROW 12's "set before the player is built" and criterion
     12's no-generator clause — resolved so that all three can hold, and §GLOSSARY's «спорное
     число» either re-signed for its new property or the group narrowed to match the signed one.
  2. F2 — «уровень» signed, and it is stated whether the build must contain a second section for
     ROW 5's in-game load control to be satisfiable. §SIGNOFF's term count corrected.
  3. F3 — §RULES completed against the rows (at minimum: any pocket may be entered; the kinds are
     told apart before entering; nothing restricts where you cut; being sealed in kills you) and
     given a committed pass rule; a rule that is a negative-by-absence is marked as checked by
     absence rather than by an outsider's unprompted statement.
  4. F4 — ROW 17's guard test restated so it is not failed by criterion 15, and «вечная
     лаборатория» either re-signed to what his words actually fix or marked as not signed; the
     pre-build stretch named as belonging to `shape`'s appetite and `kill_by`.
  5. F5, F6 — ROW 15's test (b) pinned to defaults with a committed pass rule and its dependence on
     `d-air-counter-visibility-001` stated honestly; ROWS 1, 3, 13 and 15 carry the default pin
     that ROWS 4 and 9(b) already carry.
  6. F7, F8 — the extended deck states whether its frames are of BREACHED pockets, and the three
     statistical halves (ROWS 9, 13, 14) carry a sample size and a decision rule or revert to a
     mechanical rule.
  7. F9, F10, F11, F12 — ROW 10's closure claim corrected to what its instruments can observe;
     ROW 9's coupling restated over the variables that actually decide it; ROW 16's late-join
     either struck or raised; ROW 6's test (b) stated against a level property some row requires.
  8. F13, F14 — §WHAT's two governance clauses named as governance, as criterion 15 already is;
     ROW 1 and ROW 5 reconciled on what persists across a run.
  9. F15 — the machine budget named as a class, covered or explicitly uncovered; the rig × network
     seam added to §SEAM or §SEAM's completeness claim withdrawn.
  10. F16 — the acceptance labour named and routed to `shape` as a price, alongside the venue and
     the parameter home. It is NOT priced here.
  11. Nothing is filled beyond the named findings, and no requirement line changes.
return: |
  One `converge` RESULT: the repaired sections, what each finding's resolution was, and a
  re-issued narrow `c-converge-verify-g-37a1-core-rows-002` against the changed sections only.
budget: one session, no owner.
surface: a FRESH chat, separate from this verification and from the session that wrote the rows.

disposition: |
  DISCHARGED 2026-07-28 by `s-converge-g-37a1-core-rows-repair-001`. **ALL SIXTEEN FINDINGS
  RESOLVED inside the named sections, and nothing else was filled.** No requirement line, criterion
  or card changed; no knob value was chosen; no owner question was opened, answered or manufactured;
  `d-air-counter-visibility-001` stays open; no `§SIGNOFF` was written, because a repair does not
  sign its own work. Two judgment calls belong to this leg and are declared for attack: F11's
  late-join was **STRUCK rather than raised**, because no owner text contains it and three of his
  own standing rules decide it, so raising it would manufacture a decision; and F2 resolved to **the
  build must ship at least two authored levels**, derived from criterion 12 plus his «другой». Four
  things were deliberately NOT filled and are named instead: the machine budget's price and the
  acceptance labour's price (both `shape`), the pre-build stretch (`shape`'s appetite and
  `kill_by`), and criterion 11's co-op ordering clause, which is held by nothing and is routed to
  `shape` as a constraint on the bet. Receipt:
  `history/2026-07-28-s-converge-g-37a1-core-rows-repair-001.md`. Successor:
  `c-converge-verify-g-37a1-core-rows-002`, a narrow rerun of step 3 on what changed. Step 5 still
  waits on step 3 by the owner's own route.

END_OF_FILE: live/indie-game-development/work/c-converge-g-37a1-core-rows-repair-001-call.md
