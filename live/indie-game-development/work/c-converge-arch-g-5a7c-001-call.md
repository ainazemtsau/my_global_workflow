CALL c-converge-arch-g-5a7c-001
to: session
direction: indie-game-development
play: converge-arch
node: g-5a7c

goal: |
  У узла `g-5a7c` закрыты контракты с каждым соседом, которого он кормит или чей выход читает, а его
  высокорисковые архитектурные вопросы существуют опровергнутыми сравнениями с подписанным владельцем
  выбором либо названной открытой строкой с названным отвечающим. Набор готов к свежей
  `converge-verify`, и ни одна архитектурная бумага не заехала в `done_when` карточки.

context: |
  СОСТОЯНИЕ УЗЛА. Карточка `g-5a7c` — `TREE.md`, статус `parked`, двенадцать критериев. Она правлена
  сегодня в трёх местах и правки подписаны владельцем: критерий 8 больше не запрещает игровой
  интерфейс, критерий 4 переписан под общий расширяемый слой шума, строка исключений несёт половину
  фразы про спавн и прямой ход. Разбор до правки — `history/2026-08-07-s-map-g-5a7c-ui-line-removal-001.md`.
  Читать карточку в её ТЕКУЩЕМ виде, а не по прежним пересказам.

  ГОТОВЫЙ ВХОД. `work/converge-g-5a7c.md` — закрытые §GLOSSARY (29 рядов, у каждого второе чтение с
  путём и строкой) и §WHAT (113 строк решений по всем двенадцати критериям и по краям к шести
  соседям), §SIGNOFF на Define и Resolve. Триаж этого файла — `heavy`, `converge ON`, то есть плей
  идёт полным составом. `open_rows: 1` — `W103` (куда приземляются находки плейтеста `g-8a41`,
  отвечающий PLAN, узла-получателя не названо нигде). `deferred: 2` — `W15` и `W16` (из чего сделан
  путь снизу вверх), отложены СЛОВАМИ владельца «когда будет задача, тогда это и сделать»; его же
  пометка, что двигает ли телекинез самого налётчика — вопрос ГЕЙМПЛЕЯ, который задача обязана
  принести ЕМУ, а не решить за него.

  ЧЕМ ИЗМЕРЕНО «heavy», чтобы нога не переоткрывала обоснование: вертикали нет даже в СЛОВАРЕ слоя
  правил (`grep -rnw "Height|Y"` по engine-free `Core/` = ноль при контрольном `Width` = 8+), понятия
  «происшествие» нет вовсе (`incident` = ноль), нет зрения и ослабления стенами, а единой точки
  масштаба не существует — размеры и скорости лежат в СЕМИ местах и в ДВУХ ассетах сразу
  (`NetworkPlaySettings.asset` для налётчика, `HouseholderWalkSettings.asset` для хозяина, `W113`).

  ЧЕТЫРЕ ВЕЩИ, КОТОРЫЕ РАЗБОР УЖЕ НАЗВАЛ ДОРОГИМИ И НЕОБРАТИМЫМИ — это улика для отбора
  высокорисковых вопросов, а не готовый список: (1) единая точка масштаба, которую обязаны читать оба
  тела, при неверной форме = повторное переписывание семи мест плюс разъезд чисел между слоями
  (`W04`); (2) вертикаль трогает единственное настоящее физическое тело игры — уже доказанное сердце
  закрытой волны (`risk` карточки, `W15`–`W17`); (3) общий слой шума и две несведённые шкалы
  громкости — его спека меряет в МЕТРАХ, продукт в СИЛАХ, перевода на диске нет нигде (`W34`, `W37`),
  а единицы уже сломаны (`CargoBodyContact.cs:58` делит на `Time.fixedDeltaTime` 0.02 при шаге судьи
  ≈0.0167); (4) стартовые числа масштаба `W07b` — это РАСЧЁТ ноги по его разрешению «возьми тогда
  сам», а не его подпись и не замер.

  СОСЕДИ, ЧЬИ КРАЯ УЖЕ НАЗВАНЫ В §M: `g-2b7f` (страница Steam ждёт честные геймплейные скриншоты),
  `g-8a41` (внешний плейтест), `g-7b42` (фестивальное демо), плюс будущая ставка на тоннели, сеть
  доступа и копание, адресат которой существует записью `i-digging-own-bet-authored-fresh-001` и
  узла в дереве НЕ имеет.

  Состояние: `live/indie-game-development/NOW.md`, `TREE.md` (карточка `g-5a7c`),
  `work/converge-g-5a7c.md`, `CHARTER.md`.

boundaries: |
  Карточку `g-5a7c` не трогать: её текст подписан владельцем, и правка карточки — работа `map`, а не
  этой ноги. Ставку не открывать, задач и полос не создавать, продуктовый репозиторий не трогать.
  Не решать за владельца ни один вопрос геймплея — включая отложенные им `W15`/`W16` и вопрос про
  телекинез, двигающий самого налётчика. Не записывать `knowledge/` — этот слой только предлагает.

done_when: |
  1. Каждый узел дерева, чьи выходы этот узел читает или чьи входы он кормит, имеет свою строку
     контракта, и ни один потреблённый контракт не висит без названного производителя — включая тот
     случай, когда производителя в дереве ещё нет.
  2. Каждый высокорисковый архитектурный вопрос узла несёт опровергнутое сравнение вариантов и либо
     подписанный владельцем выбор с его дословными словами, либо открытую строку с названным
     отвечающим. Дешёвые вопросы туда не попадают.
  3. Ни одна архитектурная бумага не оказалась в `done_when` карточки: она едет входной уликой.
  4. `W103` получил либо ответ, либо явного отвечающего; `W15` и `W16` остались отложенными словами
     владельца и в чужое решение не превратились.
  5. Выпущена свежая `converge-verify` по этому узлу.

return: |
  RESULT с §CONTRACTS и (heavy) `work/converge-g-5a7c-arch.md`, числами открытых строк, дословными
  словами владельца под каждым подписанным выбором и подтверждением, что карточка не тронута.

budget: одна нога
