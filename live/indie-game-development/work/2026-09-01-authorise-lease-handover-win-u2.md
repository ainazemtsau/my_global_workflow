# РАЗРЕШЕНИЕ НА ПЕРЕДАЧУ АРЕНДЫ WIN-U2 — С ОДНИМ ОБЯЗАТЕЛЬНЫМ ШАГОМ ПЕРЕД НЕЙ

К наряду `c-g5-g-5a7c-noise-reaches-by-distance-1-002` · слот **WIN-U2**

---

## СНАЧАЛА ПРИЧИНА, ПО КОТОРОЙ ЭТО НЕ ПРОСТО «ДА»

**КАНДИДАТ `b4b010c1` СУЩЕСТВУЕТ В ОДНОМ ЭКЗЕМПЛЯРЕ И НИГДЕ БОЛЬШЕ.** Перепроверено направлением:
дерево чистое, но коммит **не содержится ни в одной удалённой ветке**. Он живёт только как локальная
`slot/win-u2`.

**ТЫ СОБИРАЛСЯ ЗАПУСТИТЬ `preflight -Repair`.** Обычная операция «привести слот к базе» **сотрёт
эту ветку вместе со всей ногой слуха**. У направления это уже случалось: улика
`i-accepted-work-lives-only-in-a-released-slot-001` — принятая владельцем работа однажды осталась в
единственном экземпляре в слоте, помеченном свободным.

**Правило, которого до сих пор не было ни в одном наряде, ставится здесь:
СЛОТ МЕНЯЕТ АРЕНДУ ТОЛЬКО ПОСЛЕ ТОГО, КАК ЕГО РАБОТА ОПУБЛИКОВАНА ЛИБО СОХРАНЕНА ОТДЕЛЬНОЙ
ССЫЛКОЙ.**

## ШАГ 0, ОБЯЗАТЕЛЬНЫЙ И ПЕРВЫЙ

В `C:\projects\Unity\GasCoopGame_win-u2` создай спасательную ссылку на кандидата:

```
git branch keep/hearing-b4b010c1 b4b010c1
```

**Проверь, что она есть, и назови её в возврате.** Это локальная ветка, ничего не публикует и не
пушит. Без неё дальше не идти.

## ШАГ 1 — ЗАПРОШЕННОЕ БУКВАЛЬНОЕ ПОДТВЕРЖДЕНИЕ

Дословно, как ты просил:

> **Разрешаю освободить активную аренду WIN-U2 `t-noise-reaches-by-distance-1:BUILD`, затем занять
> слот арендой `t-noise-reaches-by-distance-1:REVIEW` и выполнить живое ревью.**

**ОСНОВАНИЕ, ЧТОБЫ ЭТО НЕ БЫЛО СНЯТИЕМ ЧУЖОЙ РАБОТЫ.** Аренда принадлежит **той же задаче**,
`t-noise-reaches-by-distance-1`, и меняется только фаза: `BUILD` → `REVIEW`. Сборка **сдана и
принята**: кандидат `b4b010c1`, семь файлов, `747 / 747 / 0` прогнаны направлением лично, дерево
чистое, ничего не идёт. **Живой сессии сборки в этом слоте нет.** Аренда пережила работу, которая
её взяла, — это дефект реестра, а не признак чужой активности.

## ШАГ 2 — ОСТОРОЖНО С `-Repair`

После захвата аренды **убедись, что `preflight -Repair` не сбросил `slot/win-u2`**. Проверь
`git rev-parse slot/win-u2` — должно быть `b4b010c1`. Сбросил — восстанови из
`keep/hearing-b4b010c1` и **скажи об этом в возврате отдельной строкой**: это будет доказанный
дефект штатного инструмента, и он дороже ревью.

## ДАЛЬШЕ — БЕЗ ИЗМЕНЕНИЙ

Всё остальное по наряду `c-g5-g-5a7c-noise-reaches-by-distance-1-002`: **живые замеры обязательны**,
находка в коде записывается строкой и **не заканчивает сессию**, мерить слух кнопками инспектора
бессмысленно — пользуйся стендом наблюдения.

Ничего не чинить, не коммитить, не публиковать.

## ЧТО ДОБАВИТЬ В ВОЗВРАТ

- Создана ли `keep/hearing-b4b010c1`.
- Что стало с `slot/win-u2` после `-Repair` — тот же коммит или сброшен.
- Дальше — пункты возврата основного наряда.

END_OF_FILE: live/indie-game-development/work/2026-09-01-authorise-lease-handover-win-u2.md
