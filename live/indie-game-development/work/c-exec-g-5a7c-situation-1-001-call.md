# CALL — c-exec-g-5a7c-situation-1-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36; ОПОРА здесь не включена и сама не включается.
direction: indie-game-development
node: g-5a7c
task: t-situation-1
issued: 2026-08-10 by s-work-g-5a7c-situation-1-dispatch-001
status: ready
basis: `43648b2dd4fbaaf9570bd56cdf440cc904e1c222`, дерево
       `7a05d905d95915f29286ab666fe6aca853301abf` — `origin/dev` = `origin/main` =
       `origin/slot/win-u1`. Перемерено `git ls-remote origin` непосредственно перед выпуском.
       Это состояние уже содержит закрытую первую волну `g-5a7c` (последней в ней шла `t-scale-7`,
       класс груза). Ground для свежего перемера, не freshness-lock.

**Одно расхождение, названное прямо: локальная `main` в слоте =
`11f8ddbf1f11722c5bfa25e3104c3847e45d7b04` и отстала на 27 коммитов** (`main...origin/main` =
`0 27`). Опубликованное состояние живёт в `origin/*` и в `slot/win-u1`. Локальную `main` этой
работой не двигать и не догонять. `origin/main` уже догнал `origin/dev` — это условие улики
`i-deliver-gate-red-when-main-catches-dev-001`. **Если финальный `-Deliver` встанет красным на
ЧУЖОЙ истории, а не на своей работе — это она: назвать в возврате и НЕ чинить чужие отчёты внутри
этого наряда.**

Пин перемерен: `engineering_contract: 36` против продуктового `validation.config`
`synced_contract_version: 36`. Re-sync не нужен.

## slot

**WIN-U1.** Каталог `C:\projects\Unity\GasCoopGame_win-u1`, постоянная ветка `slot/win-u1`.

Перемерено настоящим селектором (`tools/select-slot.ps1 -Slot WIN-U1`, только чтение)
непосредственно перед выпуском наряда:

- `state: CLEAN`, `lifecycle: AVAILABLE`, `lease: none`;
- `branch: slot/win-u1`, `head: 43648b2dd4fbaaf9570bd56cdf440cc904e1c222` — знак в знак базис
  этого наряда;
- `state-authority: C:\projects\Unity\GasCoopGame_slot-state\gascoop-slot-state.v1.json`;
- `mcp-endpoint: unrecorded`.

**Первое действие, меняющее состояние:**

`.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U1 -Claim -LeaseId c-exec-g-5a7c-situation-1-001:BUILD`

Селектор заново проверяет path/branch/HEAD/state. Любое расхождение, чужая аренда или не-AVAILABLE
— СТОП домой; другой слот не выбирать. Для Unity-действия записать и непосредственно перед вызовом
live-проверить endpoint именно этого lease. Сейчас endpoint не записан: если прямой Unity MCP
недоступен или не верифицируется как WIN-U1 — **СТОП и вопрос владельцу**, без shell/HTTP/headless
обхода. Claim не запускает и не останавливает Editor.

## goal

**Ситуация мира становится названной вещью с видом и местом, живущей у хозяина игры ровно один
тик, — и хозяин перестаёт знать, КТО её произвёл.**

Груз кладёт свои сегодняшние ситуации (`ContactedHouse`, `Landed`) через это место, а не мостом
лично к хозяину. Хозяин берёт оттуда. Поведение игры при этом не меняется — с ОДНИМ названным ниже
исключением про единицы силы, купленным отдельной подписанной строкой.

Покупается ШОВ, а не система. **Главная опасность названа заранее: слой, дошедший только до правил,
выглядит заложенным и является ложным** — ровно так был потерян круг работы на `t-scale-7`. Поэтому
главный гейт этой задачи — не «слой существует», а поиск по имени, который обязан вернуть пусто.

## context — перемерено чтением байтов на `43648b2d` в слоте WIN-U1

### Сегодняшняя дорога от груза к хозяину — три звена, все названы

| Звено | Место | Что делает |
|---|---|---|
| производитель | `Core/Cargo/AuthoritativeCargoRoster.cs:152`, `:468` | `SituationOf(cargo, landed)` строит `CargoSituation` (`ContactedHouse` / `Landed`) |
| буфер на тик | `AuthoritativeCargoRoster.cs:61`, `:69`, `:117`, `:155` | `List<CargoSituation> _situations`, публичный `Situations`, `Clear()` в начале `Advance` |
| перевод | `Core/Cargo/CargoNoise.cs:79-104` | `CargoContactNoise.Of(CargoSituation, CargoNoiseTuning)` → `HouseholderStimulus` |
| свёртка | `CargoNoise.cs:129-134`, роутер `AuthoritativeCargoRoster.cs:125`, `:156-158`, `:178` | `Loudest(...)` оставляет ОДНУ самую громкую за тик, она садится в `Cargo.Stimulus` |
| **мост лично к хозяину** | `Core/Multiplayer/CarryWorld.cs:114` | `Householder.Observe(Cargo.Stimulus, tickSeconds)` |
| потребитель | `Core/Householder/AuthoritativeHouseholder.cs:172` | `Observe(HouseholderStimulus stimulus, float seconds)` |

Зачаток слоя (`CargoSituation`, переименование шума в `HouseholderStimulus`) вырос СЛУЧАЙНО внутри
`t-scale-4`. Он живёт в `Core/Cargo/**` и назван по производителю — поэтому он и есть мост, а не
слой.

### Где хозяин знает груз ПО ИМЕНИ — 23 попадания в двух файлах, перемерено `grep -rnic`

`Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs` — **6**:
строки `128`, `160` (комментарии «in the same tick as … the cargo», «what the cargo just …»),
`359-360`, `376-377` (плечи `switch` на `CargoLanded`).

`Assets/TunnelCrew/Core/Householder/Householder.cs` — **17**:
`HouseholderOccupation.InvestigateLandedCargo = 5` (`:12`), `HouseholderPriority.CargoLanded = 5`
(`:22`), `HouseholderCauseName.CargoLanded = 5` (`:49`), `HouseholderNoticedFactName.CargoLanded = 5`
(`:59`), `HouseholderEventName.CargoLanded = 4` (`:68`), поле настройки
`cargoLandedLifetimeSeconds` / `CargoLandedLifetimeSeconds` (`:663`, `:674`, `:691`) и плечи
решателя (`:487`, `:491-492`, `:498`, `:1021-1023`, `:1667-1668`).

`Assets/TunnelCrew/Core/Householder/HouseholderRoutineProfile.cs` — **0**.

### Снаружи `Core` тот же груз назван ещё в трёх местах — и это НАСТОЯЩИЕ имена, не проза

- `Assets/TunnelCrew/World/HouseholderWalkSettings.cs:16` — сериализованное поле
  `private float cargoLandedLifetimeSeconds;` и его свойство `:36`;
- `Assets/TunnelCrew/Settings/HouseholderWalkSettings.asset:21` — `cargoLandedLifetimeSeconds: 8`.
  **Это сериализованные байты: переименование поля без `[FormerlySerializedAs]` или без правки
  самого ассета молча обнулит восьмёрку**, и хозяин перестанет помнить приземление. Число `8`
  обязано пережить задачу;
- `Assets/TunnelCrew/World/HouseholderRouteController.cs:167`, `:196` — два чтения
  `settings.CargoLandedLifetimeSeconds`.

Чистая проза про мир, где груз просто упомянут в комментарии
(`Network/HouseholderSnapshot.cs:8`, `Editor/HouseholderRouteControllerEditor.cs:49`, `:82`,
`World/HouseholderRouteController.cs:14`, `:33`), гейтом НЕ является и переписывать её не нужно.

### Единицы силы — число перемерено, и оно НЕ равно тому, что можно подумать из улики

Перемерено первой рукой:

- `Network/CargoBody.cs:123` — `Physics.simulationMode = SimulationMode.Script`: физику шагает
  судья, а не Unity;
- `Network/CargoBody.cs:273-274` — `var steps = _settings.PhysicsStepsPerTick; var step = seconds /
  steps;`, дальше `Physics.Simulate(step)` (`:288`);
- `Network/NetworkPlaySettings.asset:59` — `_physicsStepsPerTick: 2`;
- FishNet `TimeManager` не сериализован ни в `Scenes/IntegratedHouse.unity`, ни в
  `Network/Prefabs/NetworkRuntime.prefab` (поиск по GUID `3fdaae44044276a49a52229c1597e33b` пуст) →
  действует умолчание `_tickRate = 30`
  (`Packages/com.firstgeargames.fishnet/Runtime/Managing/Timing/TimeManager.cs:184`), то есть тик
  ≈ `0.03333` с;
- **настоящий шаг симуляции ≈ `0.03333 / 2` = `0.016667` с.** Именно это число ревью назвало
  «судья шагает ≈0.0167»: это ШАГ, а не тик. Делить на тик было бы второй ошибкой;
- `Network/CargoBodyContact.cs:84-89` — `var step = Time.fixedDeltaTime; … var force =
  collision.impulse.magnitude / step;`, а `ProjectSettings/TimeManager.asset` `Fixed Timestep: 0.02`.
  `Physics.Simulate(step)` поле `Time.fixedDeltaTime` НЕ меняет, поэтому делитель сегодня
  действительно `0.02`;
- пороги, на которые это число попадает: `NetworkPlaySettings.asset` `_cargoRustleForce: 60`,
  `_cargoKnockForce: 150`, `_cargoHeavyNoiseForce: 400`.

**СЛЕДСТВИЕ, НАЗВАННОЕ ЧИСЛОМ ЗАРАНЕЕ, А НЕ ОБНАРУЖЕННОЕ ПОТОМ.** `0.02 / 0.016667 = 1.2`. После
починки та же физическая тычка даёт число В 1.2 РАЗА БОЛЬШЕ, а пороги `60 / 150 / 400` по
подписанной строке НЕ двигаются. Значит дом начнёт слышать примерно на 20% раньше. **Это
ЕДИНСТВЕННОЕ разрешённое отличие от вчерашнего поведения, оно куплено подписанной строкой задачи, и
врать про «ни на грамм» здесь запрещено:** возврат обязан назвать измеренный шаг и измеренный
множитель ЧИСЛАМИ, а не словами.

**Честно про происхождение этих чисел.** `0.02` и `_physicsStepsPerTick: 2` прочитаны в байтах.
Частота тика `30` ВЫВЕДЕНА из умолчания FishNet, потому что `TimeManager` нигде не сериализован, —
она не снята с живого редактора. Совпадение с независимо посчитанным ревью «≈0.0167» её
подтверждает, но не заменяет замера. **Замерить настоящий шаг в работающем слоте — часть работы**;
если он окажется другим, множитель называется другой, и наряд этим не нарушается — нарушается он
молчанием.

Смысл починки — не подкрутить громкость, а сделать число независимым от чужой настройки: делитель
обязан ВЫВОДИТЬСЯ из того самого шага, которым идёт `Physics.Simulate`, и переставать быть верным
никогда — ни при смене частоты тика, ни при смене `_physicsStepsPerTick`, ни при смене
`Fixed Timestep`. Захардкоженное `0.016667` — это тот же дефект с другим числом.

### Что на этой поверхности ещё есть, и что с этим НЕ делать

- Хозяин слышит РОВНО ОДНУ самую громкую ситуацию за тик (`Loudest`, `AuthoritativeHouseholder`
  отвергает вторую другого рода во время активной реакции). **Это правило остаётся ровно таким.**
  Открывать приём больше одной ситуации за тик — задача `t-react-2` другой дорожки, и у неё свой
  откат.
- Пятая реакция («расследовать приземлившийся груз») остаётся: его слово 2026-08-10 — «Пятую
  реакцию оставляем». Меняется её ИМЯ, не её существование, не её порог, не её приоритет и не её
  время жизни.
- `Core/Cargo/AuthoritativeCargoRoster.Situations` уже публичен и уже живёт один тик — это зачаток,
  а не готовый слой: он живёт в папке груза и назван по производителю.
- Доставка наблюдает только `Cargo.GetState(0)` (`CarryWorld.cs:110`) — известное следствие
  (`i-delivery-observes-only-the-first-cargo-001`), не дефект этой задачи и не её работа.

## проверка происхождения

| Строка наряда | Источник |
|---|---|
| ситуация — названная вещь с видом и местом, список живёт ровно один тик, ни очереди, ни хранения, ни подписчиков | подписанная карточка `t-situation-1` `done_when` 1, дословно |
| груз кладёт свои ситуации через это место, а не мостом лично к хозяину | там же, `done_when` 2 |
| в коде хозяина не осталось ни одного упоминания груза — поиском по имени | там же, `done_when` 3; там же названо главным гейтом и тестом рискованного допущения №5 |
| порядок, слияние и вытеснение НАЗВАНЫ либо полями, либо явной строкой «пока нет» | там же, `done_when` 4; требование улики `i-world-situations-need-extensible-reaction-architecture-001` |
| поведение не изменилось, доказано существующими зелёными тестами без правки их ожиданий | там же, `done_when` 5 |
| единицы силы починены здесь, пороги `60 / 150 / 400` не двигаются | там же, `done_when` 6; половина улики `i-noise-bands-invisible-and-force-mis-scaled-001` |
| один список типизированных ситуаций на сервере; НЕ входят подписчики, история событий, ситуация, пережившая тик, весовые приоритеты и любой общий фреймворк под источники | `bet-g-5a7c-wave-2` `## approach`, «самая маленькая версия этого подхода» |
| задача стоит ДО расхождения дорожек и блокирует обе | его слова 2026-08-10: «одна задача блочит две полосы, то, ну, естественно, ждем» |
| пятая реакция остаётся | его слово 2026-08-10: «Пятую реакцию оставляем» |
| `IntegratedHouse.unity` не открывается | `bet-g-5a7c-wave-2` `## tracks`; в волне `g-1d84` полосы разошлись на этом файле дважды |

**Не импортируется** зрение хозяина, комбинация ситуаций, ручное управление грузом, видимая или
слышимая полоса шума, занятия хозяина как намерение, доставка, ценность и прокачка — всё это либо
вырезы этой волны, либо задачи других дорожек.

## boundaries — что НЕ трогать

- **Задача не расширяется.** Шесть строк карточки — весь объём. Поведение игры не меняется ни на
  грамм, кроме одного названного множителя `1.2` в единицах силы.
- **`Assets/TunnelCrew/Scenes/IntegratedHouse.unity` НЕ ОТКРЫВАЕТСЯ.** Дом принадлежит владельцу и
  следующей волне. Задача, упёршаяся в файл сцены, — **СТОП и вопрос владельцу**, а не правка.
- **Пороги `60 / 150 / 400` не двигаются**, ни одного тюнингового поля не добавляется и не
  удаляется. Чинятся только единицы, в которых считается сила.
- **Реакции хозяина не трогаются.** Меняется только то, ОТКУДА он берёт ситуацию, — не то, что он с
  ней делает: те же пороги, те же приоритеты, те же времена жизни, то же вытеснение, та же одна
  ситуация за тик.
- **Зрение не строится, комбинация не строится, ручное управление не строится, второй источник в
  игре не заводится.** Подставленный источник — задача `t-react-3`, не эта.
- **Закон подъёма, наклон груза (`FreezeRotationX | FreezeRotationZ`), телекинезные `1.28 / 0.8 /
  1.6`, `_cargoHoldStrength: 200`, `_cargoMaxHolders: 4`, класс груза из `t-scale-7` — не трогать.**
- **`ProjectSettings/**` не трогать**, в том числе `Fixed Timestep`. Починка живёт в коде, а не в
  настройках проекта.
- `Assets/GasCoopGame/**` закрыта. Второго транспорта и второй власти не заводить.
- Самодельный сканер исходников уликой не является и не пишется.

## owner-visible check

Проверка у этой задачи обратная обычной: **всё должно быть как вчера, кроме одного места, названного
заранее.**

Тот же вход Host и второй копией, тот же груз, те же четыре куска, та же переноска суммой рук, та же
доставка, тот же дом, тот же хозяин с теми же пятью реакциями. Единственное, что законно может
отличаться, — хозяин реагирует на тычки примерно на 20% раньше, потому что число силы стало
считаться в правильных единицах.

Любое ДРУГОЕ замеченное отличие — это разрыв, и его называют, а не объясняют. Как именно владелец
это проверяет и проверяет ли — его дело; наряд не ставит ему условий.

## headless evidence

- **Тот же набор тестов зелёный без правки ожиданий.** База `43648b2d` даёт **151 passed, 0 failed,
  0 skipped**; после этой задачи — столько же проходящих. Механическая подстановка переименованных
  символов в тестах разрешена и правкой ожидания НЕ является; изменение любого ожидаемого значения,
  любого числа и любого смысла утверждения — является. Доказать диффом по `tests/`, а не словами.
- **Главный гейт прогнать и вставить вывод дословно:**
  `grep -rni "cargo" Assets/TunnelCrew/Core/Householder/ --include=*.cs` → **пусто**.
  Сегодня эта же команда даёт 23 строки; ноль — единственный принимаемый результат.
- **Второй гейт, на именах снаружи `Core`:**
  `grep -rn "CargoLanded\|cargoLanded\|InvestigateLandedCargo" Assets/TunnelCrew/World/
  Assets/TunnelCrew/Settings/ Assets/TunnelCrew/Network/ Assets/TunnelCrew/Editor/` → пусто, при
  этом значение времени жизни пятой реакции (`8`) на месте и читается.
- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` и
  `.\pwsh.cmd tools/hygiene.ps1`.
- Через прямой MCP точного слота — настоящая Unity import/compile evidence.
  **Зелёные ворота поведением игры не являются и так не предъявляются**
  (`knowledge/green-gates-do-not-compile-the-game.md`): они не компилируют Unity-слой вовсе.

## откат

- **Точка возврата — `43648b2dd4fbaaf9570bd56cdf440cc904e1c222`**, дерево
  `7a05d905d95915f29286ab666fe6aca853301abf`.
- Откат назвать ОДНОЙ точной командой и **исполнить проверкой первой рукой**: после отката
  `git rev-parse <ветка>^{tree}` возвращает дерево базы знак в знак. Выведенный из истории откат
  вместо исполненного — назвать выведенным, а не выдавать за проверенный.
- Прямо заявить, что для отката не нужно ничего вне репозитория.

## done_when

1. **Слой ситуаций мира существует и стал ЕДИНСТВЕННОЙ дорогой.** Есть названный тип ситуации с
   видом и местом и один список этих ситуаций у хозяина игры, живущий РОВНО один тик: очищается в
   начале каждого тика, между тиками ничего не хранит, ни очереди, ни подписчиков не имеет. Дом
   этого типа и списка — вне `Core/Cargo/**` и вне `Core/Householder/**`, и ни тип, ни список не
   названы по грузу. Груз кладёт туда обе сегодняшние ситуации (`ContactedHouse`, `Landed`), хозяин
   берёт оттуда, и прямого моста «значение груза → `Householder.Observe`» в `CarryWorld` больше нет.
   **Порядок, слияние и вытеснение НАЗВАНЫ** — либо полями и функциями, либо одной явной написанной
   строкой «пока нет, и вот что происходит вместо этого»; молча не остаются.

2. **Хозяин перестал знать, кто произвёл ситуацию, — и это доказано поиском, а не обещанием.**
   `grep -rni "cargo" Assets/TunnelCrew/Core/Householder/ --include=*.cs` возвращает ПУСТО (сегодня
   23 строки), вывод команды вставлен в возврат. Ни одного имени с грузом не осталось и в
   householder-файлах снаружи `Core` — `World/HouseholderWalkSettings.cs`,
   `World/HouseholderRouteController.cs`, `Settings/HouseholderWalkSettings.asset`, — при этом
   сериализованное время жизни пятой реакции `8` переживает переименование и читается на HEAD. Сама
   пятая реакция остаётся ровно той же: тот же повод, тот же приоритет, то же время жизни, то же
   занятие — меняется только имя.

3. **Ничего другого не поехало, единицы починены, откат проверен.** Те же тесты зелёные
   (`151 passed, 0 failed, 0 skipped`), ни одно ожидание не переписано — только механическая
   подстановка переименованных символов, доказанная диффом по `tests/`. Сила считается против шага,
   которым `Physics.Simulate` реально идёт (`tickSeconds / PhysicsStepsPerTick`), а не против
   `Time.fixedDeltaTime`; делитель выведен, а не захардкожен; измеренный шаг и получившийся
   множитель названы числами; пороги `60 / 150 / 400` байт в байт на месте и ни одного тюнингового
   поля не прибавилось и не убавилось. Границы целы — сцена, дом, обстановка, класс груза, закон
   подъёма, наклон, телекинезные числа, доставка и `ProjectSettings/**` не тронуты, доказано диффом.
   Откат назван одной командой и проверен readback до дерева базы.

## return

Терминальный HOME/RESULT в `indie-game-development`, `g-5a7c / t-situation-1`: точные коммиты и их
продуктовая диспозиция; scoped manifest/diff и точные outputs проверок. Также обязательно:

- **где живёт слой** — точный путь, имя типа, имя списка — и почему это не папка груза и не папка
  хозяина;
- **как ситуация попадает внутрь и как берётся наружу** — оба конца по именам;
- **что записано про порядок, слияние и вытеснение** — дословно, включая честное «пока нет»;
- **вывод главного гейта дословно** (`grep -rni "cargo" … Core/Householder/`) и вывод второго гейта;
- **измеренный шаг симуляции и измеренный множитель** — числами, вместе с тем, как делитель теперь
  выводится и почему он не поедет при смене частоты тика или `_physicsStepsPerTick`;
- **дифф по `tests/`** с прямым заявлением, что изменены только имена символов;
- **что переживает пятая реакция** — значение `8` на HEAD и как оно пережило переименование поля;
- **что придётся тронуть в день, когда приедет второй источник** — списком мест (это готовит
  `t-react-3`, но ею не является);
- проверенный откат: команда и readback дерева базы;
- терминальный readback селектора слота `CLEAN / AVAILABLE / lease none`;
- каждое расхождение свежей базы с grounds этого наряда, допущения и всё срезанное бюджетом.

**Продуктовый handback задачу Направления НЕ закрывает.** После HOME отдельная нога Направления
читает возврат первой рукой и закрывает `t-situation-1` по букве `evaluator` этой волны, называя
вслух, каким из двух путей шла. Публикация исполнителем в `main` до закрытия — законный порядок его
выбором 2026-08-10.

Пока эта задача идёт, ОБЕ дорожки волны стоят. После её закрытия открываются сразу две: `t-cargo-1`
(`cargo-hands`) и `t-react-1` (`householder`).

## budget

Один engineering root. Один новый слой, ноль нового поведения, одна строка про единицы. Половина дня
работы. Второй источник, комбинация, зрение, ручное управление и видимые полосы шума сюда не входят.
Не помещается без расширения границ — STOP HOME с одним точным blocker.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-situation-1-001-call.md
