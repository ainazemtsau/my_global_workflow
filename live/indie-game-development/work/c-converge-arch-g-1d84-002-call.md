# CALL — c-converge-arch-g-1d84-002

to: session
direction: indie-game-development
play: converge-arch
node: g-1d84
issued: 2026-08-04 by s-converge-verify-g-1d84-001
status: ready

## goal

Свежая converge-arch отрабатывает СЕМЬ строк, возвращённых первой `converge-verify` с вердиктом
`complete=FAIL smuggling=FAIL`. Возвращаются именно строки, а не файл: §WHAT и §CONTRACTS остаются
как есть, поверхность не переписывается, и всё, что проверка прошла, повторно не открывается.

Это ПЕРВЫЙ FAIL узла g-1d84 (проверено первой рукой: в `history/` нет ни одной прежней
`converge-verify` по этому узлу). Потолок второго FAIL не действует. После этой ноги проверка гоняется
заново отдельной свежей сессией.

## context

Что проверка ПРОШЛА и что переоткрывать запрещено:

- Дисциплина ссылок. Двадцать пять источников перепроверены первой рукой — `knowledge/**` строки 14,
  39-42, 51, 53, 56, 74; словарь хозяина 29 и 37-38; лобби 20-21; `CHARTER.md` 91-97 и 99-109; все
  десять пунктов утверждённого базиса; `TREE.md:40-46`. Разошедшихся нет ни одной.
- Все выборочные `факт кода` перепроверены на объявленной базе (`main` = `origin/main` = `origin/dev`
  = `839df47e` подтверждено первой рукой): габариты груза, `_cargoMaxHolders: 2`, `_standHalfExtent: 7`,
  комментарий про двойку, `PlayerState` только X/Z, сигнатура `Householder.Step`, шов решения на 407,
  два ветвления на 430 и 861, исключение на 895-898, `HouseholderPriority`, ноль
  `FindObjectOfType`/`GameObject.Find`/`Resources.Load`, `noEngineReferences: true`, newtonsoft 3.2.1,
  Odin на месте, кнопки `Emit*` только в редакторе, `IWalkSpacePort`, `_enablePrediction: 0`, четыре
  сцены, отсутствие `Assets/TunnelCrew/Art/`, сцена хозяина вне `EditorBuildSettings`. **Расхождений
  ноль.**
- Счёт файла пересчитан механически: 107 строк, 83 `answered`, 26 `acceptance`, 2 `улика` — точно;
  `open_rows: 22` = 21 `open` плюс один `deferred` (W75), разбивка 9+11+1+1 сходится.
- Сила токенов владельца записана везде НЕ ВЫШЕ настоящей: В1 условное, В2 по ссылке, П2 с хеджем,
  П3 по доверию, одно слово на три карточки. Ни один токен не завышен.
- Все тринадцать пунктов строки исключений доходят до W95; у всех двенадцати критериев есть строки
  покрытия.

## boundaries

- Ничего из перечисленного выше не переоткрывать. §WHAT и §CONTRACTS не переписывать — работа идёт
  ПО СЕМИ строкам ниже.
- Владельцу не выносить ни одной находки как готовое решение: если строка требует его слова, он
  получает нормальный разбор с вариантами и рекомендацией, и открывать её вердикт можно только его
  собственными словами.
- Не чинить F3 рассуждением. `knowledge/compute-a-law-before-freezing-it.md` (принято 2026-07-31,
  `status: current`) прямо про этот класс: несущее утверждение, которого никто не выполнял, СНАЧАЛА
  считается, и замораживается только измеренный результат.
- Ставку, задачи и полосы не создавать. Разработку не запускать.

## возвращаемые строки

**F1 · ПОЛНОТА · «сервисы» (критерий 8) не доходит ни до одной строки.**
Критерий 8 дословно: «Дом, тоннели и **сервисы** лежат отдельными корнями и префабами со стабильными
идентификаторами и явными интерфейсами». Слово «сервисы» встречается в `work/converge-g-1d84.md`
НОЛЬ раз и в `work/converge-g-1d84-arch.md` НОЛЬ раз. У него нет строки §WHAT, нет ряда §GLOSSARY
(а читается оно минимум двумя способами: сетевые службы движка, игровые службы, курьер), его нет в
списке «проверено и отброшено» и нет в честном списке §COVERAGE «о чём карточка ни разу не
спрашивает». §COVERAGE относит критерий 8 к W03-W09 и W11 — ни одна из них не перечисляет, ЧТО
именно становится корнем. Это не опечатка: слово ввезено дословно из утверждённого базиса п.6.
ПОЧЕМУ ЭТО СТОИТ ДЕНЕГ: подписанное устройство A4 («каждый корень — префаб, в сцене один сборщик»)
нельзя применить к классу корня, которого никто не определил, а нарезка будет резать полосы именно
по критерию 8. Заполнять проверка не вправе — строка возвращается пустой.

**F2 · ПОЛНОТА · «по комнатам» (цель) и «настоящий дом» («зачем») не доходят ни до одной строки.**
Цель требует нести груз «по комнатам», «зачем» говорит, что узел «первый раз ставит хозяина в
НАСТОЯЩИЙ дом». §COVERAGE относит цель к W01, W12, W15, W28, W56, W59, W68, W73, а «зачем» — к W97,
W98, W99; ни в одном списке нет строки, которая требовала бы от дома больше одной комнаты или хоть
какой-то обстановки. §COVERAGE сам признаёт, что «сколько комнат в доме» не спрашивается. Рядом стоит
устав: `CHARTER.md:92`, неснимаемый минимум — «Дом при этом не декорация: несущие задевают обстановку
и попадают в то, что хозяин расставил, — и именно по этому он их находит»; и §CONTRACTS C17 уже
опирается на «задетую обстановку» как на источник замеченных фактов, при том что существование
обстановки не требует ни одна строка. Единственная строка про физическое содержание дома — W66, и она
описывает сегодняшнюю комнату 14×14. В закрытом виде поверхность выполняется целиком внутри одной
размеченной пустой коробки, и первым местом, где это вылезет, будет критерий 12 — владелец зашёл и
поиграл. Маршрут — тот же, что у W94a и C02: объём утверждённой карточки меняет map, а не converge.
Проверка не заполняет и не изобретает число комнат.

**F3 · СМУГГЛИНГ · W06: «доказательство не проверкой, а конструкцией» не покрывает случай, который
нога назвала сама, а подпись П2 стоит не на нём.**
A4 честно называет настоящий случай: «Не ловит настоящий случай — ссылку между корнями, сохранённую
**в файле сцены**». Затем вариант (г) объявляет его невозможным: «Unity ФИЗИЧЕСКИ не позволяет
ассету-префабу сохранить ссылку на объект в сцене». Утверждение верно для ПРЕФАБА-АССЕТА и не
покрывает ЭКЗЕМПЛЯР префаба, поставленный в сцену: переопределения полей экземпляра Unity хранит в
файле сцены, и сериализованное поле экземпляра вправе указывать на другой объект сцены — ровно
«ссылка между корнями, сохранённая в файле сцены». И формулировка критерия 8 («лежат отдельными
корнями и префабами»), и подпись П1 («дом собирается руками В СЦЕНЕ») ставят корни в сцену
экземплярами, то есть именно в непокрытый случай. Гарантия держится только если корней в сцене на
этапе авторства нет вовсе и их рождает сборщик при старте, — а это сталкивается с П1.
СЛЕДСТВИЕ УЖЕ ПРИНЯТО: `converge-g-1d84-arch.md:134` снимает сторожа на основании этого утверждения —
«Проверка вида „в файле корня нет ссылки внутрь другого корня“ пока НЕ заводится: при (г) ей нечего
ловить».
СИЛА ТОКЕНА: П2 — «насчет вопроса два я, наверное, с тобой согласен» — согласие с хеджем на
УСТРОЙСТВО (вариант Б). Это не вердикт о том, что считать ДОКАЗАТЕЛЬСТВОМ. §CONTRACTS тем не менее
считает «W05+W06 — одна подпись на обе».
ЛЕКАРСТВО: W06 возвращается в `open`, и утверждение СЧИТАЕТСЯ, а не переспоривается — час работы:
завести корень префабом, поставить экземпляр, переопределить на нём поле ссылкой на второй корень,
сохранить, прочитать файл сцены. Замораживается измеренный результат. Проверка исхода не назначает.

**F4 · СМУГГЛИНГ · A2/П3: второй и сильнейший довод под подписью-по-доверию не разрешается на
объявленной базе.**
Довод (2) варианта (а): «тот же файл-схема СРАЗУ работает проверялкой: критерий 5 требует „описание
формата“ и „отвержение с причиной“, и оба закрываются ОДНОЙ вещью». Проверено на `839df47e`:
`com.unity.nuget.newtonsoft-json` 3.2.1 ЕСТЬ (`Newtonsoft.Json.dll`) — довод (1) разрешается,
синтаксическая ошибка действительно несёт строку и позицию. Но проверялки схемы в проекте НЕТ
НИКАКОЙ: ни `Newtonsoft.Json.Schema` (ни dll, ни asmref), ни одной ссылки в `Assets/**`, и код
проекта Newtonsoft сегодня не использует вообще. `Newtonsoft.Json.Schema` — ОТДЕЛЬНАЯ библиотека со
своей коммерческой лицензией, в пакет Unity она не входит. При этом требование критерия 5
СМЫСЛОВОЕ — «неверный файл отвергается с понятной причиной, называющей место ошибки»: неизвестная
способность, недостающее поле, негодное значение, — и строка с позицией парсера этого не даёт.
Значит «одна вещь закрывает оба» не подпёрто ничем: либо проверялка пишется руками — а это ровно тот
довод, которым A2 отклонил вариант (в) («все сообщения об ошибках пишем сами»), — либо в платный
продукт Steam входит коммерчески лицензированная библиотека.
СИЛА ТОКЕНА делает это хуже, а не лучше: П3 — «Мне, в принципе, все равно… Если ты считаешь, что
лучший вариант А, я принимаю» — принятие ПО ДОВЕРИЮ к рекомендации, и сам арх-файл запрещает
цитировать это как его собственный выбор.
ЛЕКАРСТВО: W52 возвращается в `open`; цена лицензии и цена ручной проверялки называются ДО того, как
рекомендация снова кладётся ему на стол. Формат проверка не выбирает.

**F5 · СМУГГЛИНГ · «Девять строк закрыты» противоречит `arch_in_context_only: PASS` и §SIGNOFF в том
же файле.**
§CONTRACTS «Счёт этой ноги» объявляет девять строк ЗАКРЫТЫМИ: три подписью владельца (W09, W05+W06,
W52) и пять решением ноги (W40, W31, W61, W80, W105). При этом (а) §WHAT по-прежнему держит все
девять как `open → converge-arch`, и §SIGNOFF по-прежнему считает `open_rows: 22 — converge-arch 9`,
оставленные «как есть» сознательно; и (б) заголовок арх-файла говорит «Ни одна строка ниже не
является требованием узла» при `arch_in_context_only: PASS`. То есть пять строк записаны закрытыми
документом, который сам себя объявил необязывающим, а их названный отвечающий (converge-arch)
израсходован без названного преемника. По правилу play лекарство — ПОНИЖЕНИЕ до `open` с названным
отвечающим, а не свежий ответ. Два несходящихся счёта в одном файле — это ровно то, что нарезка
прочтёт неверно.
ЛЕКАРСТВО: назвать преемника построчно. W31, W61, W105 → PLAN/ADR (арх-файл сам говорит, что
обязывающий ADR принимает PLAN). W40 → PLAN: критерий 4 требует, чтобы шов был НАЗВАН, но не
говорит, кто его называет. W80 — см. F6. W09, W05, W52 — подписи владельца остаются подписями, но
W05+W06 разделяются по F3.

**F6 · СМУГГЛИНГ · W80/A8: основание, по которому чтение выбрано, опровергается строкой того же
файла.**
A8 отклоняет чтение (б) доводом: «Ни одного инструмента карточка не называет, и назвать их пришлось
бы ноге». Но критерий 10 сжимает утверждённый базис п.8, чей собственный заголовок — «Арт **и
инструменты**», который говорит «До покупки проводится visual/**toolkit** spike» и называет
конкретный инструмент: «Blender прежде всего используется для модульной оболочки дома, тоннелей и
особых/hero props». Строка W86a того же файла это и записывает — «Blender … стоит в утверждённом
базисе (п.8), в критерий 10 не вошёл». То есть источник ВНЕ ноги и называет инструмент, и называет
пробу toolkit-пробой; названное основание для отклонения (б) опровергается внутри собственного файла.
A8 честно пометил себя самым слабым местом — но честность источником не является.
ЛЕКАРСТВО: ПОНИЖЕНИЕ W80 до `open` с названным отвечающим. Это чтение ЕГО утверждённого текста,
значит вопрос идёт к нему нормальным разбором с вариантами — и открывает его только его слово.
Проверка ниже потолка новых решений владельцу не выносит и этого вопроса ему не задавала.

**F7 · СОГЛАСОВАННОСТЬ (мелкое) · §SIGNOFF занижает настоящий токен владельца.**
`work/converge-g-1d84.md:385` говорит «**Слово на набор в целом:** ожидается». Собственный RESULT
converge-ноги говорит обратное: «Слово на набор в целом: „фиксирую“. Спрошено ОТДЕЛЬНО от двух
развилок» (`history/2026-08-04-s-converge-g-1d84-integrated-house-slice-001.md:29`), и то же слово
несут `LOG.md:5` и `work/c-converge-arch-g-1d84-001-call.md:21`. Артефакт не обновили, а арх-нога
сохранила §SIGNOFF «как есть». Направление ошибки безопасное — файл занижает, а не завышает, — но
§SIGNOFF это ровно тот раздел, по которому нарезка узнаёт, что подписано.
ЛЕКАРСТВО: поправить строку по RESULT. Нового слова владельца для этого не нужно.

## done_when

1. Каждая из семи строк отработана явно: F1 и F2 получают либо строку с источником вне ноги, либо
   `open` с названным отвечающим — заполнять их выдумкой запрещено. F3 замораживается только
   измеренным результатом. F4 несёт названную цену лицензии и ручной проверялки. F5 называет
   преемника-отвечающего построчно и приводит два счёта файла к одному. F6 понижен. F7 поправлен.
2. Ни одна строка, которую проверка прошла, не переоткрыта; §WHAT и §CONTRACTS не переписаны.
3. Ни один вердикт владельца не объявлен без его дословных слов, и ни одно чтение его текста не
   записано как ввоз.
4. Открыта одна свежая `converge-verify` по g-1d84 — отдельной сессией, не этой.

## return

Полный Direction RESULT. Ставку не открывать, задачи и полосы не создавать, разработку не запускать.

budget: one fresh session, отдельная от этой проверки

END_OF_FILE: live/indie-game-development/work/c-converge-arch-g-1d84-002-call.md
