# CALL — c-work-g-5a7c-loot-1-binding-g5-001

to: session
play: work
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-loot-1
issued: 2026-08-14 by s-work-g-5a7c-loot-unity-cut-accepted-001
status: ready

**СВЕЖИЙ ОТДЕЛЬНЫЙ ЧАТ.** Это биндинговая проверка G5: она обязана идти в физически другой сессии,
чем те, что делали работу и принимали возврат. Светлое закрытие запрещено собственной строкой
карточки `t-loot-1`.

## goal

**ВЕРДИКТ ПО ЗАКРЫТИЮ `t-loot-1` СТОИТ НА ПОПЫТКЕ ОПРОВЕРГНУТЬ КАЖДУЮ ЕЁ СТРОКУ НА
ОПУБЛИКОВАННОЙ ГОЛОВЕ, А НЕ НА ПЕРЕСКАЗЕ ВОЗВРАТА.**

## context

Наряд `c-exec-g-5a7c-loot-foundation-001` вернулся, опубликован и **закрыт**. Публикация задачу не
закрывает — это написано на самой карточке.

**Что уже перемерено ДВУМЯ независимыми ногами и сошлось до знака** (не перемеряй ради ритуала,
перемеряй только то, что несёт твой вердикт):

- `origin/main` = `origin/dev` = `53afdc2d`; кандидат `8240329c` — его предок.
- 22 коммита от `1307aa3e`; `1307aa3e..8240329c` = 49 файлов, +3764 / −404.
- Десять описаний предметов `_definitionId` 1…10, массы `5×48 / 3×100 / 2×240` — ровно 5/3/2, числа
  `48/100/240` не тронуты.
- Гейт второго вида `f841b52a` — ровно три файла, 305 строк, ни одного из шести названных файлов
  ядра.
- `docs/reviews/review-c-exec-g-5a7c-loot-foundation-001.md`: 22 поднято / 13 опровергнуто /
  9 подтверждено, все девять `fixed 8a247a7f`.
- `preserve/c-exec-g-5a7c-loot-1-001-win-u1-local-20260813` = `e0a30194`, и он **не** предок головы.
- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` = **293 / 0 / 0**,
  прогнано обеими ногами на опубликованной голове.

**Что НЕ доказано и является твоим предметом:**

1. **Ручная приёмка владельца — два пункта из трёх.** Его дословные слова 2026-08-14: «я проверял
   смену веса» и «и что будильник рисуеться своей моделью». Пункт **2** наряда — снять или заменить
   временное покрытие пола и убедиться, что предметы стоят на настоящей опоре при неправленых
   высотах — **не прогонялся вовсе**. Пункт **3** покрыт наполовину: клиент отдельно и куб-запаска
   им не названы. Полная матрица — в блоке `owner-eye matrix` карточки `t-loot-1`.
2. **Поведение в собранной игре.** Мои замеры доказывают публикацию, а не поведение. Открытыми
   остаются и ждут именно тебя две улики: `i-alarm-binds-to-weight-class-not-to-the-thing-001`
   (звонит один, а не два) и `i-loot-candidate-emits-stale-contact-on-pickup-001` (протухший удар
   не стреляет после подъёма, живой шумит один раз, настоящее увиденное не подавляется).
3. **Unity-проверок нет ни одной, и это ПРИНЯТЫЙ вырез,** не дефект и не повод для FAIL: владелец
   2026-08-14 ответил **«а»** на решение `d-loot-unity-matrix-cut-001`. Заведено уликой
   `i-unity-checks-for-the-loot-foundation-were-cut-001` с триггером на первую же ногу, трогающую
   лут. **Не открывай этот вопрос заново и не требуй тестов.**

## boundaries

- **Ты не чинишь.** Твой продукт — вердикт с уликами. Найденный дефект становится уликой или строкой
  FAIL, а не правкой.
- **Не расширяй.** Четыре находки возврата уже заведены отдельными карточками и **не являются
  строками `t-loot-1`**: `i-cargo-sinks-into-the-floor-when-the-householder-arrives-001` (груз
  проваливается в пол после переноски, `y = −198`),
  `i-householder-climbs-loot-and-stuck-on-it-001`,
  `i-observation-lab-no-longer-mirrors-house-loot-001`,
  `i-unity-checks-for-the-loot-foundation-were-cut-001`. Ни одна из них не блокирует закрытие
  задачи; их наличие — не довод за FAIL.
- **Замороженного кандидата `e0a30194` не открывать, не менять, не патчить.**
- **Владельцу ставить ровно то, что он увидит в игре.** Пункт 2 — это его руки: он снимает или
  заменяет временное покрытие пола. Инженерных вопросов ему не задавать. Он не обязан прогонять
  сегодня; несделанный им пункт — законный `waiting`, а не FAIL.
- Правило и запрет не переоткрывать: `knowledge/no-checks-the-owner-makes-by-eye.md`.

## done_when

1. **КАЖДАЯ ИЗ ШЕСТИ СТРОК `done_when` КАРТОЧКИ `t-loot-1` ПОЛУЧИЛА ПОПЫТКУ ОПРОВЕРЖЕНИЯ И
   ВЕРДИКТ.** Для каждой названо, чем она доказана или чем провалена, первой рукой — прочитанные
   байты, прогнанная команда, дословные слова владельца с указанием, где они лежат. Пересказ
   возврата уликой не считается.
2. **ТРИ ПУНКТА РУЧНОЙ ПРИЁМКИ ЗАКРЫТЫ ЕГО СЛОВАМИ ИЛИ ЧЕСТНО ОСТАВЛЕНЫ ЖДАТЬ.** Пункт 2 либо
   прогнан им и записан дословно, либо помечен `waiting` с точной формулировкой того, что ему
   осталось сделать. Пункт 3 доведён до клиента и куб-запаски. Ни один пункт не закрывается
   пересказом.
3. **ДВЕ ОТКРЫТЫЕ УЛИКИ ПОВЕДЕНИЯ ПОЛУЧИЛИ ДИСПОЗИЦИЮ.** Про один будильник и про протухший удар —
   закрыты уликой поведения либо остались открытыми с названной причиной. Задача закрывается
   вердиктом `PASS`, только если обе разрешены и все шесть строк прошли.

## return

RESULT игры `work`: вердикт `PASS | FAIL` со строчной таблицей; что перемерено первой рукой и чем;
дословные слова владельца с указанием их места; диспозиция двух улик; при `PASS` —
`state_changes`, закрывающие `t-loot-1`; при `FAIL` — что именно не сошлось и наименьшая
следующая нога. Проверено при выпуске: `t-loot-1` **не** последняя задача ставки — в той же
дорожке открыта `t-house-asset-1`, в соседней ждут `t-handlers-1` и `t-lab-2`. Значит `review`
ставки этой ногой не открывается ни при каком вердикте.

## budget

Одна нога. Тяжёлое многоагентное веерное опровержение не требуется — но это сокращает разброс, а не
отменяет ни G5, ни проверку писателя.

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-loot-1-binding-g5-001-call.md
