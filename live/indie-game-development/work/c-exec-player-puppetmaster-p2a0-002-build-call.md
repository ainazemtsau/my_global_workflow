# CALL c-exec-player-puppetmaster-p2a0-002-build-001 — trustworthy PuppetMaster live-source proof (BUILD ONLY)

> **FIRE-READY BUILD-only research route.** The owner-approved PLAN is frozen at
> `b90b7205f2251d3d4dee92dddabdd794157fbf4c`. Execution begins with a fresh independent
> spec-only RED author. A different later fresh builder may implement against those immutable tests.
> Terminal status remains `CHECKPOINT / NOT DELIVERED`.

to: executor
direction: indie-game-development
node: g-9c41
task: M1-P2a0
repo: C:\projects\Unity\GasCoopGame_p2a0_002
kind: engineering
change_id: c-exec-player-puppetmaster-p2a0-002
phase: BUILD-only
issued: 2026-07-12 (s-work-player-puppetmaster-p2a0-build-route-001)
frozen_base: b90b7205f2251d3d4dee92dddabdd794157fbf4c
owner_approval: owner-ack:owner-chat-2026-07-12-p2a0-002-live-source-v1-six-properties

goal: |
  PuppetMaster 1.5 has independently witnessed, run-id-bound live Unity evidence sufficient to
  recommend Candidate A, Candidate B, bounded Candidate C, or asset rejection for the future
  networked player root-authority route without treating harness self-report, pixels, source text
  or owner-eye as mechanism proof.

context: |
  Owner approval words:
  `УТВЕРЖДАЮ PLAN c-exec-player-puppetmaster-p2a0-002: live-source v1, шесть свойств.`

  The sole implementation authority is product commit
  `b90b7205f2251d3d4dee92dddabdd794157fbf4c` and its frozen artifacts:
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-002/PLAN.md`;
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-002/proposal.md`;
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-002/specs/puppetmaster-live-authority-proof/spec.md`;
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-002/tasks.md`;
  - `docs/adr/ADR-E-0011-c-exec-player-puppetmaster-p2a0-002-test-owned-live-source-proof.md`.

  Create a new clean disposable worktree
  `C:\projects\Unity\GasCoopGame_p2a0_002` on branch
  `codex/c-exec-player-puppetmaster-p2a0-002-build` directly from frozen commit b90b7205.
  Do not rebase that frozen research history onto a newer main. Record fresh `origin/main`,
  ancestry, worktree list and Editor/MCP identity at execution start. The source commit descends
  from product `origin/main@a644e5db538d1102f726630d6a4ac2f3f1bdcf5a`; it also contains the
  immutable 001 checkpoint/history and old independent suite required by the new regression proof.

  Preserve `C:\projects\Unity\GasCoopGame_lab` byte-for-byte as the dirty forensic snapshot.
  Copy nothing from its RootMotion import, failed harness, Library, generated output or dirty
  settings. The clean worktree re-hashes and re-imports only:
  `C:\Users\Anton\AppData\Roaming\Unity\Asset Store-5.x\RootMotion\ScriptingPhysics\PuppetMaster.unitypackage`
  with required SHA-256
  `03E126E93BA44BE178DD102A7A318A0F8207350C9640068B903307D1C0AFBED3`.
  Read back PuppetMaster 1.5 and use the already-proven default assembly. Do not repeat the failed
  optional nested-asmdef experiment. Vendor content remains uncommitted by default.

  Execution-role order is binding:
  1. after clean read-only venue preflight, a fresh independent test-author reads repository
     process instructions and only the frozen `002` spec as behavior authority; it reads no PLAN,
     ADR, old/new tests, implementation, vendor or failed harness;
  2. that author creates only additive `002` acceptance/NegativeControl files, proves genuine RED
     on the frozen implementation and commits them separately;
  3. only after the RED commit and immutable file hashes exist does a different fresh builder read
     the full frozen PLAN and implement; the builder may not edit the new tests, the old five
     acceptance files, the old helper, or any frozen planning artifact;
  4. oracle compile/meaning defects return exclusively to the independent author; the builder
     never weakens tests to proceed.

  The old 65 NegativeControls are regression evidence only and must remain byte-identical and
  65/65 GREEN. No old payload, clip, transition, fixture or A/B/C verdict is accepted.

boundaries: |
  BUILD only from the frozen `002` contract. Do not reopen PLAN choices, reinterpret an old run,
  repair or promote the dirty `001` harness, or select A/B/C before new live proof and fresh G5.

  Never edit:
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-001/**`;
  - `openspec/changes/c-exec-player-puppetmaster-p2a0-002/{PLAN.md,proposal.md,tasks.md,specs/**}`;
  - ADR-E-0010 or ADR-E-0011;
  - `docs/results/c-exec-player-puppetmaster-p2a0-001.md`;
  - `docs/reviews/review-c-exec-player-puppetmaster-p2a0-001.md`;
  - the five independently authored `001` acceptance files;
  - the additive `002` acceptance files after their independent RED commit.

  No Core, FishNet, production controller, damage, health/incapacitation, gas consequence, final
  camera/collider/rig feel, custom Candidate-C active-ragdoll locomotor, two-peer BUILD,
  TagManager/DynamicsManager/Input ProjectSettings mutation, product branch, PR, merge or
  `tools/check.ps1 -Deliver`.

  Builder harness is actuator/object-reference bridge only. It may not report owner, phase,
  state, pose, source copy, contact, transition, fixture equality, fixed sample, capture success,
  acceptance success or verdict. Builder-written JSON, sparse MCP polling, source scan, video or
  marker values cannot substitute for test-owned live reads.

  MCP/automation is first for every capable Unity action. Ask the owner for computer-use or one
  manual Unity action only after a concrete named UI-only or unavailable-tool blocker exists.
  Name the exact tool/UI blocker and give exactly one bounded click-by-click action; resume
  automation immediately afterward. Owner-eye is limited to the final two short comparable clips
  after machine proof and fresh G5.

  A required Editor/MCP/capture tool outage, package/hash mismatch, wrong endpoint/project,
  need to weaken one of G5-1…G5-6, post-result threshold/token change, second occurrence of the
  same failure class, or scope expansion is a mandatory STOP/checkpoint. No workaround,
  self-granted waiver or reduced-fidelity substitute.

done_when: |
  1. Clean-venue preflight records creation of
     `C:\projects\Unity\GasCoopGame_p2a0_002` /
     `codex/c-exec-player-puppetmaster-p2a0-002-build` from exact b90b7205, fresh remote/main and
     ancestry, contract v19, all worktrees/Editors/lockfiles/RAM, and exact MCP
     endpoint→`Application.dataPath`. It proves no lab/vendor/harness/Library artifact was copied
     and preserves every 001 history/test hash.
  2. A separate fresh spec-only RED author commits only additive 002 assertion,
     NegativeControl, EditMode and PlayMode observer/coordinator tests. The new positives are
     genuinely RED for missing live implementation, every named wrong implementation is rejected,
     the old five files are byte-identical, and the unchanged old category remains exactly 65/65
     GREEN. Author identity/session, changed-file list, commit, hashes and RED transcripts exist
     before any builder implementation.
  3. A different fresh builder starts only after bullet 2. It makes the immutable tests pass
     without editing them or frozen artifacts. It re-hashes and cleanly imports the exact package,
     reads back PuppetMaster 1.5, compiles in default assembly, uses exact `Puppet Extended /
     Pilot Root`, and neither stages vendor content by default nor copies/reuses the old harness.
  4. The only builder evidence bridge is global `P2A0002RigBridge` with exactly these object
     references: `ControllerRoot`, `TargetRoot`, `PhysicalRoot`, `GameplayCapsule`,
     `ProofCamera`, `PuppetMasterObject`, `BehaviourPuppetObject`, `PuppetRigidbodies`,
     `ControllerWriter`, `SyntheticTargetDriver`; and exactly these verbs: `ResetRig`,
     `SelectCandidate`, `ArmScriptedProgram`, `DisarmScriptedProgram`, `Teardown`.
     Test-owned recorder and independently implemented source verifier use the frozen
     `P2A0002Rig/*` paths, exact marker/writer/driver/vendor types, enum discovery and live
     muscle-graph pelvis discovery. Every value-bearing pair equals its independently read source
     at the same run/subtrial/observer-sequence/engine-fixed-time key; collections are deep-copied
     and non-aliased. Lease/root/epoch markers only cross-check owner derived from real writer,
     capsule, Camera-parent and stimulus-response consequences.
  5. Before fixture construction, the test-owned coordinator writes an exclusive immutable
     write-ahead attempt tree: UUID/ordinal/contract hashes and eligibility in `start.json`,
     pass/fail/cancel plus failure class in `terminal.json` from `finally`. The first attempt is
     the only eligible attempt per sealed contract; retry requires a new committed contract and
     immutable old→new invalidation edge. Failed/cancelled attempts never disappear. Exactly one
     accepted envelope publishes by exclusive same-volume atomic directory move to
     `docs/measurements/c-exec-player-puppetmaster-p2a0-002-live-proof/`; no overwrite, append,
     “latest” lookup or second accepted selection is possible. Canonical JSON, complete attempt
     tree, source map, pre-run contract, payload and all captures are SHA-256/byte-length bound.
  6. Exact package/prefab/live-instance identities are separately test-owned readback. The loaded
     proof scene has a closed-world enabled-component inventory: `BallShooter`,
     `RootMotion.Demos`, PlayerInput, UI/legacy/device-input readers and every unknown enabled
     behaviour fail. The immutable scripted driver reads no device. A/B consume identical
     commands. The late-order test observer records every actual Unity `FixedUpdate`; engine
     fixed time advances exactly one current fixedDeltaTime and skipped intervals cannot be
     hidden by ordinal renumbering.
  7. A/B each have reset-separated rock and point-impulse subtraces. Every arm settles for at
     least 25 real fixed steps within the frozen caps before fingerprinting. A/B fixture and
     scripted sources match; real rock contact is proven from test-owned callbacks on rock and
     puppet sides with matching colliders/time/contact/non-zero impulse; point impulse is a
     distinct test-owned `Rigidbody.AddForceAtPosition(..., ForceMode.Impulse)` action with
     recorded point/vector and before/after response. A remains controller-owned. Each B
     subtrace has exactly one causally derived controller→pelvis and one later
     pelvis→controller edge, no gap/overlap/reversal/extra cycle, with marker and epoch changes
     exactly coincident.
  8. Candidate C runs a settled zero-horizontal-command gravity/drift baseline, resets identically
     and runs the frozen horizontal command through the nearest vendor physical mechanism without
     a custom locomotor. It survives only with continuous physical ownership, command-axis
     displacement above matched baseline noise plus at least 0.05 m, error decrease on at least
     75% of eligible intervals and terminal error no greater than 50% of initial. Gravity-only,
     equal-baseline, discontinuous-owner and non-reducing-error controls fail the same assertion;
     otherwise C is Cut/Unproven.
  9. Layer 8↔9 collision mutation is one validated transaction. Real EditMode/live-engine fault
     injection throws after every construction/setup stage, including immediately after global
     mutation, and reads exact before/during/after state. Validation, constructor, behavior,
     capture, cancel, normal, nested and duplicate-disposal paths restore the original matrix
     exactly and idempotently. Nothing publishes until independent restoration readback succeeds;
     Input/TagManager/DynamicsManager remain unchanged.
  10. One accepted MCP PlayMode coordinator proof produces exactly two comparable 1280×720,
      30 fps H.264 clips (`candidate-a.mp4`, `candidate-b.mp4`) and exactly two 4×2 PNG contact
      sheets. Sheet columns are `Puppet`, `Unpinned`, `GetUp`, `Recovered`; rows are rock and
      point impulse. Every tile and contiguous clip range maps to exact run id, sample/time,
      vendor state and derived owner; no reset, hit, handoff, get-up or recovery is hidden.
      Captures are owner navigation only, never mechanism evidence.
  11. All new targeted headless NegativeControls, real MCP EditMode and the one accepted PlayMode
      proof are GREEN; old controls remain 65/65 GREEN. Normal `tools/check.ps1` runs without
      `-Deliver` and its full output is attached. The immutable historical 001
      `full_runtime_cycle_green=false` result is classified honestly rather than edited; any
      unrelated/new RED is a STOP. Multiplayer inventory covers host truth, local prediction,
      remote candidate, correction/teleport/get-up/epoch hazards and hit+authority-change in one
      network interval. Surviving B or C marks a separate one-machine/two-peer FishNet proof
      mandatory and keeps production controller blocked.
  12. After builder evidence is committed, a separate fresh-session G5 attempts to refute
      G5-1…G5-6, complete attempt selection, both source paths, input inventory, fixed-step
      continuity, A/B matching/contact/edges/captures, C baseline, collision atomicity and every
      multiplayer inference. `docs/reviews/review-c-exec-player-puppetmaster-p2a0-002.md` records
      fully disposed findings and fix-class sweeps. Only after accepted fresh G5 does the owner
      review the two clips and return one final A/B/C/reject verdict.
  13. The complete BUILD stops within one focused half-day. At 90 minutes without the first
      settled live smoke, it returns a checkpoint before any fidelity cut. The builder writes
      `docs/results/c-exec-player-puppetmaster-p2a0-002.md` with exact status
      `CHECKPOINT / NOT DELIVERED`, timebox/manual-action log, evidence, assumptions, cuts and
      final verdict or precise blocker. It never runs/claims `-Deliver`, product DoD,
      mergeability or product merge, and routes RESULT home without authoring the next CALL.

return: |
  Return one reconciled research checkpoint covering every numbered done_when bullet: clean
  worktree/base/endpoint preflight; independent RED author session/commit/hashes/transcripts;
  separate builder sessions/commits; exact package/import/default-assembly readback; source map,
  sealed pre-run contract and complete attempt tree; new/old NegativeControl TRX; real MCP
  EditMode and accepted PlayMode run ids; accepted payload/manifest/clips/contact-sheet hashes;
  A/B/C and collision evidence; normal non-deliver check transcript; multiplayer inventory;
  fresh G5 review/dispositions/sweeps; owner clip verdict if reached; time/cost, manual blockers,
  assumptions/cuts and exact checkpoint RESULT path/status. Direction OS decides all later work;
  do not author a next CALL.

budget: |
  One focused half-day for the entire RED→BUILD→MCP evidence chain. Setup/import/rig/observer/
  publisher/capture has a hard 90-minute checkpoint before any fidelity cut. Same failure class
  twice, required-tool outage or any frozen-property weakening = immediate STOP.

launch:
  lane: C
  venue: C:\projects\Unity\GasCoopGame_p2a0_002
    (branch codex/c-exec-player-puppetmaster-p2a0-002-build; new clean disposable worktree;
    new Editor identity must be proven before mutation)
  machine: PC
  base: b90b7205f2251d3d4dee92dddabdd794157fbf4c
    (exact frozen research base; record fresh origin/main but do not rebase the proof)
  conflict_surface: additive 002 tests, test-owned observer/publisher, disposable bridge/fixture,
    docs/measurements and checkpoint RESULT only
  depends_on: []
  merge_queue: none — research checkpoint; no PR/product merge
  gates: independent spec-only RED → different fresh builder → real MCP EditMode/one accepted
    PlayMode proof → normal non-deliver check → separate fresh G5 → owner final verdict
  owner_eye: only the two final comparable A/B clips after machine proof and fresh G5

END_OF_FILE: live/indie-game-development/work/c-exec-player-puppetmaster-p2a0-002-build-call.md
