# CALL — c-exec-newgame-folder-and-gates-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 35
basis: 4e95845c (main, проверено 2026-08-01)
direction: indie-game-development
node: g-6b13
task: t-1
issued: 2026-08-01 by s-shape-g-6b13-two-stands-bet-001
status: blocked — ждёт двух слов владельца (см. §slot и §необратимое ниже)
slot: НЕ НАЗНАЧЕН НАРЯДОМ. Правило продуктового репозитория (`AGENTS.md`, «Worktree workflow»,
      редакция владельца 2026-07-17) требует, чтобы задачу называл ОДИН слот — `WIN-U1`, `WIN-U2`,
      `WIN-U3` или `WIN-U4` — и прямо запрещает исполнителю выбирать слот самому. На 2026-08-01 все
      четыре `AVAILABLE` с пустой арендой (`<git-common-dir>/gascoop-slot-state.v1.json`,
      прочитано первой рукой). `WIN-CTRL` (`GasCoopGame_dev`) для этой работы незаконен — он только
      для интеграции. Наряд выпускается со слотом, который назовёт владелец; до этого он не
      запускается.

## goal

Новая игра получает своё место в репозитории и свои проверки, а старый код уезжает в отдельную
папку вместе со своими тестами и перестаёт участвовать в проверках новой игры. Продуктовая
конституция перестаёт описывать мёртвую игру.

Это первая задача новой ставки и предпосылка ко всему остальному: следующие четыре задачи пишут
код в папку, которой пока нет.

## context

**Что произошло в направлении.** Концепт сменился: строится кооперативная игра, где команда
прокапывает тоннель и выносит грузы из дома, а хозяин дома это замечает и отбирает. Старая газовая
игра закрыта. Владелец 2026-08-01 остановил бумажную проработку и запустил работу; ближайшая цель —
два выбрасываемых стенда, на которых он своими руками посмотрит переноску груза несколькими
игроками и реакцию хозяина. Стенды — следующие задачи, не эта.

**Две записи направления, которые эта задача несёт целиком:**

- `i-old-code-move-to-own-folder-001` — решение владельца о переносе и вся измеренная цена;
- `i-product-constitution-unrouted-001` — конституция написана под лок-степную газовую игру.

Обе — в `C:/my_global_workflow_worktrees/indie-game-development/live/indie-game-development/NOW.md`.

**Уставные факты, которые здесь действуют** (`live/indie-game-development/CHARTER.md`, строки
названы точно):

- `:86` — граница между старым и новым проведена по ПАПКЕ, а не по дате; старый код не удаляется,
  но не читается, не цитируется, его тесты не чинятся и из проверок новой игры не запускаются;
  новая игра пишется в новой папке с нуля;
- `:90` — новые репозитории под эту игру не заводятся;
- `:95` — глобальной симуляции газа в игре нет;
- `:96` — «новая игра стартует быстро, но не грязно»: документация новой игры однозначна и без
  хлама;
- `:97` — правило, написанное под мёртвый концепт, кодом не является и по умолчанию не наследуется.

**Не уставный, но решённый факт — источник назван точно, потому что в уставе его НЕТ:** сетевая
модель выбрана словами владельца 2026-08-01 — **считает ведущий (хост), остальные показывают
присланное**, побитовое согласие независимых машин не требуется. Записано в
`NOW.md` `i-product-constitution-unrouted-001`, а не в `CHARTER.md`. (Первая редакция этого наряда
приписала это уставу; свежая проверка поймала ложную атрибуцию.)

**Измеренная цена переноса, проверенная первой рукой** (не переизмерять, но и не доверять слепо —
перепроверить на текущей базе то, что будешь трогать):

- 188 отслеживаемых файлов вне `Assets/GasCoopGame` содержат этот путь. Разбивка пересчитана на
  текущей базе: docs 83, openspec 58, tests 27, tools 13, `validation.config` 1, `core` 1,
  `characters` 1, `render-contracts` 1, `CLAUDE.md` 1, `AGENTS.md` 1, `.agents` 1 = 188.
  **`.github/workflows/core-ci.yml` в этот список НЕ входит — там ноль вхождений;** первая редакция
  наряда назвала CI вместо `.agents/skills/unity-urp-visual-dev/SKILL.md`, и это исправлено здесь.
  Внутри самой `Assets/GasCoopGame` этот же литеральный путь хардкодят ещё 7 файлов — по уставу они
  не чинятся, но их поломка при переезде это ожидаемая цена, а не твоя ошибка;
- **главная опасность — ложная зелень:** `core/`, `characters/` и `render-contracts/` несут
  `EnableDefaultCompileItems=false` и единственный `<Compile Include="..\Assets\GasCoopGame\…">`,
  поэтому при неразрешимом пути `dotnet build` завершается УСПЕШНО на пустой компиляции;
- 27 тестовых файлов хардкодят игровые пути; сканы детерминизма падают закрыто и громко;
- **НЕОБРАТИМОЕ, И ОНО ЖДЁТ ОТДЕЛЬНОГО СЛОВА ВЛАДЕЛЬЦА:** семь токенов `exists:Assets/GasCoopGame/…`
  в незаархивированных ЗАМОРОЖЕННЫХ спеках станут неразрешимыми навсегда (`validation.config:337`
  запрещает правку задним числом). Ещё ~26 таких токенов лежат под `openspec/changes/archive/` —
  они тоже станут неразрешимыми, но из скана уходят по той же строке конфигурации, поэтому «семь»
  это цена ВНУТРИ скана, а не полная. Запись `i-old-code-move-to-own-folder-001` требует явного
  согласия владельца именно на это. Первая редакция наряда сняла требование собственным
  рассуждением — свежая проверка вернула это как выдумку сессии. **Без его слова эту часть не
  делать;** остальное в задаче от неё не зависит;
- `.github/workflows/core-ci.yml` идёт на `pull_request` И на push в `main` и `dev`; полный реимпорт
  Unity получат ШЕСТЬ чекаутов — пять линкованных worktree плюс основной `GasCoopGame`;
- перенос НЕВИДИМ для пакетных гейтов (`tools/delivering-scope.ps1:71` выводит область только из
  диффов `openspec/docs`) — учти это при выборе способа проверки;
- Unity-сторона дешёвая: один ассет по GUID, все 19 asmdef по имени, `.meta` переезжают внутри
  `Assets/`.

Владелец допустил вариант «либо вообще без тестов» для уносимого кода — выбор способа за тобой.

## boundaries

- **Игровой код здесь не пишется.** Ни стендов, ни механик, ни сцен: только место, проверки и
  конституция.
- Старый код НЕ удаляется и НЕ чинится. Его тесты не приводятся к зелёному — они уезжают как есть.
- Имя и точное место новой папки, способ переноса, форма гейтов — твои решения. Владельцу они не
  выносятся: его прямое слово 2026-08-01 — «технически обсуждается в репозитории».
- Работать в ТОМ слоте, который назвал владелец (см. `slot:` в шапке). **Новых worktree не
  создавать, слот не выбирать самому** — это прямое правило продуктового `AGENTS.md`.
- Конституция — это `## Constitution` в продуктовом `AGENTS.md` (проверено первой рукой: шесть
  правил, из них под мёртвый концепт написаны **#1 «Engine-free core, always»** и **#6
  «Deterministic core — no wall-clock, no unseeded RNG»**). Она переписывается ПОД НОВЫЙ КОНЦЕПТ, а
  не правится точечно: правила мёртвого концепта снимаются (`CHARTER:97` — правило под мёртвый
  концепт кодом не является), взамен приходит «считает ведущий» и настолько мало, насколько
  возможно. Заголовок обещает валидатор («the validator checks changes against these»), но такого
  валидатора в `tools/` и `validation.config` нет — учти, что это проза, а не гейт.
- **Необратимую часть (семь токенов в замороженных спеках) не делать, пока владелец не сказал «да»
  отдельно.** Остальная работа от неё не зависит и делается.
- Если что-то в задаче окажется невозможным или дороже, чем выглядит, — ESCALATE домой одной
  строкой, а не обход.

## done_when

1. Место новой игры заведено и подключено к проверкам; старый код и его тесты лежат в отдельной
   папке, и из проверок новой игры не запускается оттуда ничего.
2. Проверки новой папки дают КРАСНЫЙ, когда проверять нечего, — включая случай пустой компиляции
   при неразрешимом пути.
3. Продуктовая конституция описывает новый концепт и модель «считает ведущий»; правил мёртвой
   газовой игры в ней не осталось.

## return

Домой, в направление `indie-game-development`, задача `t-1` ставки `g-6b13`. В возврате: коммиты,
вывод проверок, что именно краснеет на посеянной ловушке, и одной строкой — что оказалось дороже
или невозможнее, чем описано выше.

## budget

Одна инженерная нога.

END_OF_FILE: live/indie-game-development/work/c-exec-newgame-folder-and-gates-001-call.md
