# РАЗРЕШЕНИЕ: аренда `t-noise-reaches-by-distance-1:REVIEW` на WIN-U2 снимается

К наряду `c-exec-g-5a7c-the-bench-opens-quiet-1-001` · слот **WIN-U2**

---

## ОСТАНОВКА ВЕРНА. АРЕНДА ПЕРЕЖИЛА СВОЮ РАБОТУ В ТРЕТИЙ РАЗ ЗА ДЕНЬ

Сторож прав, и вина не твоя.

**РАБОТА, КОТОРАЯ ВЗЯЛА ЭТУ АРЕНДУ, ЗАКОНЧЕНА И ЗАКРЫТА:**

- ревью слуха вернуло вердикт **«не опровергнуто»** с живыми числами;
- задача `t-noise-reaches-by-distance-1` **закрыта как сделанная**;
- кандидат `b4b010c1` **сохранён дважды**: `slot/win-u2` и `keep/hearing-b4b010c1` — обе ссылки
  проверены направлением, обе на `b4b010c1`, дерево чистое.

**Правило «слот не меняет аренду, пока работа не сохранена» — выполнено.** Освобождать безопасно.

## БУКВАЛЬНОЕ ПОДТВЕРЖДЕНИЕ

> **Разрешаю освободить аренду WIN-U2 `t-noise-reaches-by-distance-1:REVIEW`, затем занять слот
> арендой `t-the-bench-opens-quiet-1:BUILD` и выполнить наряд.**

Основание: задача, которой принадлежит аренда, **закрыта**; её работа сохранена двумя ссылками;
живой сессии в слоте нет.

## ПОСЛЕ ЗАХВАТА — ОДНА ПРОВЕРКА

`preflight -Repair` не должен сбросить `slot/win-u2`. Проверь `git rev-parse slot/win-u2` — должно
остаться **`b4b010c1`**. Сбросил — восстанови из `keep/hearing-b4b010c1` и **скажи отдельной
строкой**: это доказанный дефект штатного инструмента, и он дороже ноги.

## И НОВОЕ ТРЕБОВАНИЕ К ВОЗВРАТУ, ДЕЙСТВУЮЩЕЕ С ЭТОГО МОМЕНТА

**Последним шагом ноги аренда освобождается.** В возврате обязана быть строка: «аренда освобождена»
— либо прямая причина, по которой слот удержан, и **подо что именно**.

Правило заведено в `knowledge/a-lease-ends-with-the-work-that-took-it.md` и теперь читается каждым
нарядом этого направления.

## ДАЛЬШЕ — БЕЗ ИЗМЕНЕНИЙ

Всё остальное по наряду `c-exec-g-5a7c-the-bench-opens-quiet-1-001`: два json-файла, кода не
трогать, живой прогон, и первым делом проверить, что пустой список событий не роняет окно.

END_OF_FILE: live/indie-game-development/work/2026-09-01-authorise-release-win-u2-review-lease.md
