# CALL — c-ctrl-g-1d84-one-scene-publish-001

to: executor
kind: engineering
repo: C:/projects/Unity/GasCoopGame_dev
engineering_contract: legacy:c-exec-g-1d84-one-scene-what-exists-001
direction: indie-game-development
node: g-1d84
task: t-scene-1
track: сцена
issued: 2026-08-05 by s-work-g-1d84-one-scene-binding-g5-pass-001
status: ready
slot: WIN-CTRL
basis: fresh main/dev and remote refs at launch; candidate identity is pinned below

## goal

Exact owner-tested runtime/report chain опубликована в `main`/`dev`, closing report честно имеет
статус `DELIVERED` после merge/publish/readback, а WIN-U1 терминально освобождён без потери семи
foreign working-tree edits либо возвращён один точный blocker.

## context

- Fresh physical binding G5 PASS: Direction RESULT
  `history/2026-08-05-s-work-g-1d84-one-scene-binding-g5-pass-001.md`.
- Exact immutable input chain: basis `839df47e78127fe2ebfba5eabb307bf6bdd61e9b` → runtime
  `9113b24a9a9e753de702101b3dbf1eddcf1e8e0f` → first report
  `82a6a6c4e88a4d216e8fa1db092118acf66b5fff` → owner receipt
  `923f6f7c748b61bd5e85a272b3a5e012414e992f`.
- Runtime manifest is one R100 scene rename with its R100 `.meta` and two path-reference edits:
  `Assets/TunnelCrew/Network/Prefabs/NetworkRuntime.prefab` and
  `ProjectSettings/EditorBuildSettings.asset`. Scene GUID remains
  `31d4e989f2353534182e51e1c86ef3e9`.
- Commit `923f6f7c` has sole parent `82a6a6c4` and changes only
  `docs/results/c-exec-g-1d84-one-scene-what-exists-001.md`; the four runtime paths are identical
  between `9113b24a` and `923f6f7c`.
- Owner words are committed exactly: «вижу старую сцену по которой ношу балку» and «Принимаю базу».
  Their force is only acceptance of the old geometry as the `IntegratedHouse` basis, not a future
  house look or a new Play request.
- Last committed custody receipt records WIN-U1 as `DIRTY-PRESERVED / CLAIMED`, lease
  `c-exec-g-1d84-one-scene-what-exists-001:BUILD`, endpoint `unrecorded`, with seven foreign paths:
  `Assets/Plugins/NuGet/.nuget-installed.json`, `Assets/Plugins/NuGet/McpPlugin.Common.dll`,
  `Assets/Plugins/NuGet/McpPlugin.dll`, `Assets/Plugins/NuGet/ReflectorNet.dll`,
  `Packages/manifest.json`, `Packages/packages-lock.json`, and
  `ProjectSettings/EditorBuildSettings.asset`.
- G5 could not obtain a fresh ordinary status because Git LFS clean-filter failed on shared
  `lfs/tmp` with `Access is denied`; it did not touch LFS or the checkout. Control must establish
  current custody through the product's native process, never infer clean state from this limitation.

## boundaries

- Work only through the product root `AGENTS.md` and the штатный serialized WIN-CTRL/Control process.
  This CALL authorizes integration/publication/control-plane close, not feature changes.
- Reconcile exact commits `9113b24a`, `82a6a6c4`, and `923f6f7c` over fresh current `main`/`dev`,
  preserving compatible parallel changes. Keep the exact runtime candidate bytes immutable.
- Do not stage, merge, attribute, overwrite, clean, revert, or silently stash any of the seven foreign
  WIN-U1 edits as candidate work. Terminal release is legal only after they are preserved losslessly
  with exact recovery/readback evidence; otherwise return one exact blocker and leave custody safe.
- Change the closing report status to `DELIVERED on dev` only after its runtime/report chain is merged
  and published with local and remote ref readback. Do not pre-write delivery status.
- Use only proportional light repo-native checks required by current Control authority. No Unity run,
  new owner Play, new feature tests, old-folder tests, or broad legacy suite is requested by this CALL.
- Do not rewrite, squash, substitute, or manufacture a new runtime candidate. Do not release a slot on
  a claim alone; publication/readback and lossless foreign-byte custody both have to resolve.

## done_when

1. Fresh `main`/`dev` contain the exact ordered input commits `9113b24a`, `82a6a6c4`, `923f6f7c`
   through a preservation-aware Control integration; local and remote ancestry/ref readback is exact,
   runtime blobs match the G5-passed candidate, and no foreign WIN-U1 byte entered the publication.
2. After merge and publication, `docs/results/c-exec-g-1d84-one-scene-what-exists-001.md` alone receives
   its truthful `DELIVERED on dev` status; proportional light repo-native checks are green and the HOME
   names exact commands/results, report commit/blob, published refs, and any preserved parallel changes.
3. WIN-U1 is terminally `AVAILABLE` with lease cleared only after all seven foreign paths are preserved
   recoverably and read back exactly; otherwise HOME returns one precise preservation/release blocker,
   leaves the slot/bytes safe, and claims neither publication close nor release.

## return

Полный product RESULT домой: exact integration/report commits; ancestry and local/remote refs; runtime
blob identity; proportional check outputs; final report status/blob; preservation artifact/readback for
all seven foreign paths; final selector/lease state. On any gap, one exact blocker with no destructive
cleanup and no false `DELIVERED`/`AVAILABLE` claim.

budget: one light serialized WIN-CTRL Control leg

END_OF_FILE: live/indie-game-development/work/c-ctrl-g-1d84-one-scene-publish-001-call.md
