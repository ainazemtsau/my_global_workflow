# CALL — c-map-real-core-node-001

to: session
direction: indie-game-development
play: map
node: g-0c26
issued: 2026-08-07 by s-review-g-1d84-integrated-house-partial-001
status: ready

## goal

В roadmap появляется точная owner-approved карточка узла «настоящее ядро» — того, в который УЖЕ ИГРАЕТСЯ как в игру, — и `g-3f90` получает явную судьбу: либо складывается в неё, либо остаётся отдельным узлом его словом.

## context

`g-1d84` закрыт 2026-08-07 его словом, ставка закрыта вердиктом review `partial`. Что построено и им подтверждено: одна запускаемая игра, где двое на разных копиях от первого лица несут общий физический груз в названную точку доставки, хозяин живёт по размеченному дому и приходит на шум, есть собранный exe. Разбор — `history/2026-08-07-s-review-g-1d84-integrated-house-partial-001.md`.

**ВХОД ПРИНОСИТ ВЛАДЕЛЕЦ, И БЕЗ НЕГО НОГА НЕ НАЧИНАЕТСЯ.** Его слова 2026-08-07: «следующая волна будет закрывать ядро, уже даже несколько… мне главное, чтобы там игралось без основных концепций, была реальная игровая сцена, то есть не то, что сейчас». Примерный контур демо он прорабатывает в параллельном чате; этот наряд его НЕ реконструирует и не выдумывает.

Что уже решено им и не переоткрывается:

1. **Узел многоволновой.** Он сам сказал «уже даже несколько». Карточка пишется так, чтобы её закрывала не одна ставка.
2. **Отдельной ноги на исправления не существует.** Прямое решение 2026-08-07: каждая находка review привязана к волне, которая и так трогает её поверхность, и едет строкой в наряд, а не отдельной задачей. Список привязок — в RESULT ревью, §add_back и §state_changes.
3. **Класс «код не вызывается» им закрыт** («это в принципе похер, будет вызываться потом»): `profileId` парсится и не читается, `PlaceName` парсится и не читается, загрузчик берёт путь при одном жёстко зашитом вызове. Обратно не поднимать. ОДНО исключение он вывел из этого класса сам — `IsKnownPlace` возвращает `false`, и профиль, назвавший место, не деградирует, а убивает хозяина целиком.
4. **Аналитика допустима** — гейт не ставится, откатывать флаг после ручной сборки не надо. Открытым остался только вопрос публичной сборки (`i-analytics-allowed-decide-before-public-build-001`).

**РЕКОМЕНДАЦИЯ REVIEW ПО `g-3f90`, делегированная ему сессии его же словом «давай твою рекомендацию»: сложить `g-3f90` в новый узел.** Довод: «настоящий дом с обстановкой» и «в это играется как в игру» — одна вещь, которую он оценит одним взглядом, а два узла заставят каждую будущую ногу спрашивать «вид — это сюда или туда». Складывание также возвращает ширину дерева к семи. **Что нельзя потерять при складывании:** `g-3f90` несёт долг перед страницей Steam — честные геймплейные скриншоты для `g-2b7f` критерий 2 — и оплаченный вход, серверную ветку `probe/free-kits-2026-08-06` в продуктовом репозитории. Обе строки переезжают в новую карточку дословно. Рекомендация не является его вердиктом: точный текст решает он.

Живые записи, которые эта нога обязана прочитать как ВХОД (не как требования):

- `i-householder-activities-are-intentions-not-instructions-001` — его собственная модель занятия: намерение → фазы «путь → действие → завершение» → длительность после прибытия → явная политика прерывания/возобновления. Четвёртое свойство сегодня отсутствует и подпёрто зелёным тестом, который своего случая проверить не может.
- `i-profile-cannot-name-a-house-place-001` — пока не подключён `IsKnownPlace`, профиль не может назвать комнату, то есть «готовить в кухне» не выражается вообще.
- `i-house-walls-below-eye-level-001` — стены 1.5 при глазах 1.7; дом починки — этот узел.
- `i-scene-seams-broken-at-two-sites-001`, `i-house-fixture-not-bound-to-the-prefab-001`, `i-noise-bands-invisible-and-force-mis-scaled-001` — три находки review, каждая со своим триггером внутри этого узла.
- `i-vertical-is-absent-and-owner-wants-it-001` и `i-digging-own-bet-authored-fresh-001` — вертикаль и тоннели. Порядок, предложенный разбором и ждущий его слова: высота едет с настоящим миром, тоннели идут ПОСЛЕ неё своей ставкой.
- `i-steam-appid` — его рубеж 2026-08-15, он же уставный пересмотр октябрьского маршрута. До него восемь дней.

Авторитетные указатели: `live/indie-game-development/CHARTER.md`, `TREE.md`, `NOW.md`, `history/2026-08-07-s-review-g-1d84-integrated-house-partial-001.md`, `knowledge/`.

## boundaries

- CHARTER не меняется. Противоречие с уставом возвращает маршрут во `frame`, а не чинит устав из `map`.
- Не создавать ставку, задачи, полосы и инженерные наряды; продуктовый репозиторий не трогать.
- Не сочинять контур демо за владельца и не выводить его из этого наряда: без его входа нога останавливается и спрашивает.
- Не наследовать молча `done_when` `g-1d84` и `g-3f90`: новая карточка пишется заново, старые критерии в неё не переливаются.
- Не превращать находки review в `done_when`. Они едут уликами в `context` будущих нарядов — это его прямое решение.
- Не переоткрывать закрытые им решения: аналитику, класс «код не вызывается», отсутствие отдельной ноги на исправления.
- G9: точный текст новой карточки и судьба `g-3f90` требуют его отдельного слова на показанный текст.

## done_when

1. Под `g-0c26` лежит точная owner-approved карточка узла настоящего ядра с `goal`, проверяемым `done_when`, `why`, `risk`, `status: parked`, написанная под его контур демо и под его же условие «чтобы игралось… реальная игровая сцена».
2. `g-3f90` имеет явную owner-approved судьбу; при складывании долг по скриншотам для `g-2b7f` и вход `probe/free-kits-2026-08-06` перенесены дословно. Ширина открытых детей `g-0c26` названа числом в RESULT.
3. RESULT цитирует его точные слова по каждому изменённому тексту, сохраняет `bet: null` и `tasks: []`, полос не создаёт и передаёт ровно один законный следующий маршрут.

## return

Полный Direction RESULT с точной дельтой TREE и NOW, owner-verdict evidence по каждой изменённой карточке и одним следующим маршрутом. Разработку не запускать.

budget: one owner-present session

END_OF_FILE: live/indie-game-development/work/c-map-real-core-node-001-call.md
