# CALL — c-exec-g-1d84-householder-profile-file-002

to: executor
direction: indie-game-development
node: g-1d84
task: t-host-3
track: хозяин
issued: 2026-08-06 by s-work-g-1d84-householder-profile-return-checkpoint-001
status: ready
engineering_contract: 36
repo: C:/projects/Unity/GasCoopGame
slot: **WIN-U1** — уже держит работу, см. §Слот. Ветку НЕ сбрасывать
base: `ae9ce7b15349ac3eb1218446b2872412c27c435f` (tip `slot/win-u1`), сводится с `main` = `1703599ba27b395c65f42cc8391c411693cac697`

Продолжение `c-exec-g-1d84-householder-profile-file-001`. Та нога сделана и принята владельцем
в режиме ПРОБА; она НЕ переделывается и не пересматривается. Здесь доделывается ровно то, чего
у неё не было: работа нигде не опубликована, а с тех пор `main` ушёл вперёд и чисто не сводится.

## goal

Принятая работа профиля живёт в `dev` и `main` вместе с уже приехавшей туда работой шума — то есть
там, где ей и место, а не отдельной веткой.

## done_when

1. Кандидат профиля сведён с нынешним `main` без потери ни одной из двух работ; репозиторные проверки
   на сведённом состоянии проходят; сведённое состояние опубликовано в `dev` и `main`, SHA назван.

**Участия владельца в этой ноге НЕТ и не заказывается.** Он уже прогнал профиль и сказал: «я
протестировал, работает» и «не хочу ещё раз тестировать». Строки `done_when` задачи, требовавшие его
руки, закрыты этими его словами; порча файла НЕ запрашивается. Гейтим сборку, а не владельца
(`knowledge/no-checks-the-owner-makes-by-eye.md`).

## context — измерено Направлением первой рукой 2026-08-06, не пересказ отчёта

**РАБОТА ЛЕЖИТ В ОДНОМ ЭКЗЕМПЛЯРЕ И ЭТО ГЛАВНОЕ.** `git for-each-ref --contains ae9ce7b1` даёт ровно
одну ссылку — локальную `refs/heads/slot/win-u1`. На сервере её нет: `origin/slot/win-u1` = `adae7d40`,
это старый коммит. В `main` её тоже нет: `git merge-base --is-ancestor b1ac3674 1703599b` = NO, для
`ae9ce7b1` = NO. Пять коммитов (`git rev-list --count d60c2f04..ae9ce7b1` = 5) держатся ТОЛЬКО
указателем локальной ветки в слоте, который в `gascoop-slot-state.v1.json` числится `AVAILABLE`,
`lease: none`. Обычная первая операция аренды — «привести слот к базе» — сотрёт их.

**ЧИСТО УЖЕ НЕ СВОДИТСЯ, И КОНФЛИКТ РОВНО ОДИН.** `git merge-tree --write-tree 1703599b slot/win-u1`
выходит с кодом 1: `CONFLICT (content)` в `Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs`,
и это единственный конфликтный путь. Причина названа: базой ноги был `d60c2f04`
(`git merge-base ae9ce7b1 1703599b` подтверждает), а после неё в `main` приехала работа шума
(`c-exec-g-1d84-world-noise-contact-001`, интеграция `9077b26f`, `main` = `1703599b`), и она
правила тот же файл — 54 строки в нём по `git diff --stat 8ffe5a5d 1703599b`.

**ЧТО ИМЕННО СВОДИТСЯ.** Со стороны профиля `git diff --stat d60c2f04 ae9ce7b1` = 16 файлов,
1194 добавления, 6 удалений; трогаются `AuthoritativeHouseholder.cs`, `Householder.cs`,
`NetworkHouseholder.cs`, остальное — новые файлы (`HouseholderProfileLoader.cs`,
`HouseholderRoutineProfile.cs`, профиль, документ формата, тесты). Со стороны шума —
`AuthoritativeHouseholder.cs`, `CargoNoise.cs`, `CargoRules.cs`, `CarryWorld.cs`, `CargoBody*.cs`,
`NetworkPlaySettings.*`, тесты. Обе работы кормят одного и того же хозяина: профиль задаёт его
распорядок, шум — замеченный факт, на который он отвлекается. Сведение обязано оставить рабочими обе.

**ЧЕГО У ПЕРВОЙ НОГИ НЕ БЫЛО, И ЭТО НЕ ЕЁ ВИНА.** Владелец гонял ПРОБУ на `b1ac3674` — то есть на
состоянии БЕЗ работы шума. Сведённое состояние не запускал никто. Это ровно тот класс, который уже
записан в `i-published-to-main-before-any-direction-close-001`: слияние двух полос, которого не видел
ни один живой человек.

## Конфликт измерен Направлением — он ровно один и он тривиальный

Чтобы исполнитель не открывал его вслепую и не тратил на это ногу. Трёхсторонним слиянием файла
`AuthoritativeHouseholder.cs` (база `d60c2f04`, 225 строк → профиль 301, `main` 267) получается
**ровно ОДИН конфликтный блок, пять строк**, и обе стороны там только ДОБАВЛЯЮТ поля рядом:

```
профиль:  _profile, _routineStepIndex, _routineStepRemainingSeconds
main:     _noisePoint, _hasNoisePoint
```

Ответ — оставить оба набора. Это не спор двух решений, это две независимые пристройки в одно место
класса. Если при сборке вылезет что-то помимо этих пяти строк — вот это уже настоящая находка, и её
надо назвать в отчёте прямым текстом, а не сглаживать.

## boundaries

- **Работу первой ноги НЕ переделывать.** Формат, загрузчик, документ и профиль приняты владельцем;
  здесь их не переписывают, не «улучшают» и не расширяют.
- **Слот НЕ приводить к базе.** `slot/win-u1` — носитель работы; на сервере она продублирована веткой
  `keep/householder-profile-2026-08-06` = `ae9ce7b1`.
- **Владельца ни о чём не просить.** Ни прогонов, ни подтверждений, ни скриншотов.
- **Конфликт разрешает исполнитель, а не владелец.** Это внутреннее устройство одного файла; по его
  постоянному правилу архитектурные решения на него не спихиваются.
- Второй сетевой канал, RPC или свой тик — **СТОП И ВОПРОС ВЛАДЕЛЬЦУ**.
- Второй профиль, пять реакций и новая способность — `t-host-4`, `t-host-5`, `t-host-6`, НЕ эта нога.
- Продуктовый контракт — `AGENTS.md` на базе; ворота — `.\pwsh.cmd tools/check.ps1`.

## Слот

**`WIN-U1`**, каталог `C:\projects\Unity\GasCoopGame_win-u1`, ветка `slot/win-u1`, `HEAD` =
`ae9ce7b1`, дерево чисто (`git status --short` пусто). В `gascoop-slot-state.v1.json` он `AVAILABLE`,
`lease: none` — владелец его освободил и подтвердил это словами.

```
.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U1 -Claim -LeaseId c-exec-g-1d84-householder-profile-file-002:BUILD
```

**Сразу после аренды и ДО любой другой операции** сохранить единственный носитель на сервер, как
в своё время была сохранена проба бесплатных наборов веткой `probe/free-kits-2026-08-06`.

## return

RESULT домой, в Направление. Задачу закрывает Направление; из этого отчёта ему нужны SHA
опубликованного состояния и прямой ответ на один вопрос: вылезло ли при слиянии что-нибудь помимо
пяти измеренных строк.

## Слова владельца, снявшие с этой ноги всё, кроме слияния

Три его фразы 2026-08-06, каждая закрыла свою строку:

1. **«шва достаточно»** — строка карточки «действия внутри комнаты» закрыта устройством: поле `place`
   у действия есть, неизвестное имя отвергается при старте, привязка имён комнат живёт в `t-host-2`.
   Записано честно: закрыта швом, а не работающим примером, и это его выбор из двух показанных.
2. **«я протестировал, работает»** и **«не хочу ещё раз тестировать»** — строки, требовавшие его
   руки, закрыты его собственным вердиктом. Прежняя редакция этого наряда требовала повторной приёмки
   на сведённом состоянии; ТРЕБОВАНИЕ СНЯТО как ошибка Направления — это был гейт на поведение
   владельца, прямо запрещённый `knowledge/no-checks-the-owner-makes-by-eye.md`.
3. **«если она не влита ни в dev, ни в main, то напиши, потому что при закрытии слота она,
   естественно, должна была влить»** — он назвал настоящий затык раньше и точнее, чем Направление.
   Публикация возвращена в эту ногу и стала её единственным делом.

**ЧТО ЭТИМ ПРИНЯТО КАК РИСК, А НЕ ЗАМОЛЧАНО.** После слияния хозяин будет одновременно ходить по
распорядку из файла и отвлекаться на шум. Эту пару вместе не запускал никто: владелец гонял профиль на
`b1ac3674`, то есть до прихода шума. Ловят это только репозиторные проверки. Если они зелёные —
работа публикуется, и вопрос о живом запуске остаётся владельцу на момент закрытия задачи, а не
гейтом здесь.

Носитель продублирован на сервер до выпуска наряда: `refs/heads/keep/householder-profile-2026-08-06`
= `ae9ce7b1`, проверено `git ls-remote origin`.

## budget

Одна нога, меньше половины дня по замыслу: сведение одного файла, прогон ворот и два его шага.

END_OF_FILE: live/indie-game-development/work/c-exec-g-1d84-householder-profile-file-002-call.md
