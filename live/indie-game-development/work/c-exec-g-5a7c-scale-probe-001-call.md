# CALL — c-exec-g-5a7c-scale-probe-001

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
stage: PLAN
direction: indie-game-development
node: g-5a7c
task: t-scale-1
issued: 2026-08-07 by s-shape-g-5a7c-001
status: ready
slot: **WIN-U1** — назван владельцем 2026-08-07 его словом «давай слот 1». Рабочее дерево
      `C:/projects/Unity/GasCoopGame_win-u1`, ветка `slot/win-u1`.
      Отдельно: в сессии, писавшей этот наряд, Unity MCP отвалился — если инструмент в этом слоте
      недоступен, это СТОП и вопрос владельцу, а не обходной путь.
base: `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa` (`main`, дерево чисто на 2026-08-07). Это ground для
      трёхстороннего перемера, а не freshness-lock: первая работа наряда — перемерить базу, слот и
      каждый названный путь первой рукой.

## goal

Мир игры запускается в пропорции роста налётчика **0.16 м**, и про физику на этом росте существует
измеренный ответ: держится она или нет.

## context

- **Это ПЕРВАЯ задача ставки и она стоит первой не по порядку, а по риску.** Вся волна и весь узел
  `g-5a7c` строятся против числа 0.16 м; документация движка прямо велит держать тела около единицы,
  а мы уходим в шесть раз ниже. Если физика здесь не держится, ставка режется НА ЭТОЙ задаче, а не в
  конце — и пропорция возвращается владельцу вопросом.
- **Пропорция — его вердикт, а не предложение сессии.** Дословно 2026-08-07: **«берём 0,16»**, дано на
  показанной ему композиции. Число не обсуждается; обсуждается только то, выдержит ли его движок.
- **Эта задача НАРОЧНО не строит единую точку масштаба.** Она самая дешёвая форма ответа на самый
  дорогой вопрос: руками поставить числа, запустить, посмотреть. Точку масштаба ставит СЛЕДУЮЩАЯ
  задача (`t-scale-2`), и делает это уже зная ответ. Строить её здесь — значит платить день за то,
  что может быть выброшено через два часа.
- **Единой точки масштаба сегодня НЕТ, и это перемерено первой рукой на объявленной базе:** размеры,
  скорости и высоты лежат минимум в СЕМИ местах — `Network/NetworkPlaySettings.asset`,
  `Settings/HouseholderWalkSettings.asset`, трансформы `World/ThreeRoomHouse.prefab`, оверрайды
  `Scenes/IntegratedHouse.unity`, `Householder.prefab`, константы в коде (`DeliveryPoint.cs`,
  `StartConditions.cs`, `HouseRoomMarker.cs`), `ProjectSettings/DynamicsManager.asset` и
  `TimeManager.asset`. Значит перевод руками задевает несколько файлов — это ожидаемо и это не повод
  заводить архитектуру раньше времени.
- **Числа, которые придётся тронуть, названы по именам** (все перемерены на базе): `_movementSpeed: 3`,
  `_cargoMass: 4`, `_cargoRestHeight: 0.25`, `_cargoPickupRange: 1.3`, `_cargoCarryOffset: 1.3`,
  `_cargoGripLength: 3.2`, `_cargoHandReach: 0.2`, `_walkerRadius: 0.45`, `_spawnOriginX: -1.5`,
  `_spawnSpacing: 3`; у хозяина — `movementSpeed: 1.75`, `urgentMovementSpeed: 3.25`,
  `capsuleHeight: 2`. Ближняя плоскость камеры сегодня `0.3` в сцене и `NetworkWalkerCamera.cs:33` её
  не трогает — **на росте 0.16 она отсечёт почти всё**, поэтому она входит в задачу.
- **ЧЕГО СТАРТОВЫЕ ЧИСЛА НЕ НЕСУТ, и это записано против набора, а не в его пользу.** Разбор
  предлагал коэффициент 12.5, скорость 1.0 м/с и массу 50 — **обоснование этих чисел было опровергнуто
  арифметикой их же самих** и снято. Числа остаются стартовым предложением без выдержавшего
  обоснования, и перевод масштаба НЕ ПРОВЕРЕН. Отдельно не проверено, что настройка шума переживает
  переход: три порога `60 / 150 / 400` были подобраны в старом масштабе. **Наряд вправе взять другие
  числа**; он обязан либо привести свой перевод, либо принести вопрос.
- **Четыре рычага физики, если её поведёт, названы заранее и все четыре уже существуют:** шаг контакта
  (`DynamicsManager` `m_DefaultContactOffset: 0.01` — это 6% роста налётчика), итерации решателя (6),
  подшаги тика (`_physicsStepsPerTick: 2`), назначенная масса вместо выведенной из плотности.
- **Единица силы в проекте уже сломана и это измерено:** `Network/CargoBodyContact.cs` делит импульс на
  `Time.fixedDeltaTime` (0.02), а судья симулирует шагами ≈0.0167 — число занижено примерно в 1.2 раза.
  **Чинить это здесь НЕ надо** — оно принадлежит слою шума следующей волны; названо, чтобы наряд не
  принял расхождение за собственную ошибку перевода.
- Ставка, задача и вырезанное: `live/indie-game-development/NOW.md`, `g-5a7c` / `t-scale-1`.

- **⚠️ В ЭТОМ СЛОТЕ ЛЕЖИТ НЕЗАКОММИЧЕННАЯ РАБОТА, И ОНА ТА САМАЯ.** Перемерено первой рукой
  2026-08-07 в `C:/projects/Unity/GasCoopGame_win-u1`: `HEAD` = `352f96b0` (= `main`, ветка
  `slot/win-u1`), `git status --short` даёт ровно две строки, обе **untracked** —
  `?? Assets/TunnelCrew/Probe.meta` и `?? Assets/TunnelCrew/Probe/`, внутри
  `Probe/ScaleTableau/ScaleTableau.unity`. **Это композиция масштаба, на которой владелец дал
  вердикт «берём 0,16».** Её нет ни в одной ветке, ни на сервере, ни в `main` — она существует в
  ОДНОМ экземпляре, вот здесь. Обычная первая операция аренды «привести слот к базе» её СОТРЁТ.
  **Первое действие наряда — сохранить её** (коммит в слотовую ветку либо явное `keep/`-сохранение),
  и только потом что-либо приводить к базе. Класс известен и открыт записью
  `i-accepted-work-lives-only-in-a-released-slot-001`.

## boundaries

- **Ничего не переустраивать.** Ни единой точки масштаба, ни новых систем, ни новых слоёв, ни новых
  объектов настроек. Только числа в существующих местах плюс ближняя плоскость камеры.
- **Вертикали в этой задаче нет** — ни у игрока, ни у груза; заморозки груза не снимать, гравитацию не
  включать. Это задачи `t-scale-3` и `t-scale-4`.
- **Слой шума не трогать вовсе.** Пороги `60 / 150 / 400` не подбирать и не двигать под новый масштаб —
  если они после перевода звучат не так, это НАБЛЮДЕНИЕ в возврат, а не работа этой задачи.
- Хозяина, дом, обстановку, заход, облик и сеть на четверых не трогать.
- **Инструмент недоступен — СТОП.** Если Unity Editor в названном слоте не поднят или нужный MCP
  отвалился, наряд останавливается и говорит владельцу, что запустить. Обходных путей нет; исключение
  — только его письменное «да».

## done_when

1. Мир запускается в пропорции 0.16 м: размеры, скорости, масса и ближняя плоскость камеры переведены
   руками в существующих местах, ничего не переустроено, новых систем, слоёв и точек настроек не
   заведено, а взятый перевод приведён числами.
2. Про физику на этом росте существует измеренный ответ: дрожит ли тело, проваливается ли сквозь пол,
   врут ли контакты, — с числами и с названием применённых рычагов, если они понадобились.
3. Откат назван и проверен: возврат прежних чисел возвращает прежнее поведение.

## return

Терминальный HOME по pinned v36: product RESULT, commits/manifest, вывод проверок и честная диспозиция
всех трёх строк `done_when`, плюс отдельной строкой — что после перевода звучит или выглядит не так.
Продуктовый handback сам задачу Direction НЕ закрывает: закрытие `t-scale-1` — слово владельца о том,
держится ли физика и тот ли это размер, и оно берётся отдельной Direction-ногой. **Требований к тому,
как именно он смотрит, нет и быть не может.**

## budget

Один engineering root, не больше сосредоточенного полудня. Если перевод масштаба требует новой общей
системы или новой точки настроек — **STOP и HOME с точным разрывом**: это признак, что задача
`t-scale-2` должна идти раньше, и решает это Direction, а не наряд.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-scale-probe-001-call.md
