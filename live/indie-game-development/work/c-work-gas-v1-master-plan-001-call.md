# CALL c-work-gas-v1-master-plan-001

to: session
direction: indie-game-development
track: gas
play: work
node: g-1a63
task: v1-master-plan
issued: 2026-07-21 (s-work-gas-v1-current-authority-audit-accepted-001)

goal: |
  У владельца есть принятый ступенчатый Gas V1 master plan: одна понятная карта Gas-owned simulation
  outcomes, зависимостей и доказательств ведёт от текущей foundation к full-level и multiplayer-ready
  product handoff, а каждый крупный узел сохраняет отдельное подробное планирование до реализации.

context: |
  Binding audit and owner route: `work/gas-v1-current-authority-audit-2026-07-21.md`.
  Exact owner words: `Полный Gas V1 master plan сейчас, реализация ступенями — рекомендую`; далее
  `в принципе, согласен`. Владелец уточнил, что master plan показывает путь и node-разбиение, но не
  подменяет подробный план каждого узла; оптимизация отдельно обсуждается и принимается до реализации.

  Program boundary: `work/program-v2-integration-lab-plan.md`. Accepted Grid boundary:
  `work/grid-v1-critical-path-plan.md`. Scale authority: `knowledge/g9c41-gas-engine-SPEC.md` and
  `knowledge/g9c41-scale-watchmen.md`. Eight-player requirement:
  `knowledge/g9c41-players-8-owner-requirement.md`. Current terminal foundation:
  `history/2026-07-19-s-work-near-gas-l1b-v29-terminal-home-close-001.md`.

  Current product evidence observed during the audit: clean `main == origin/main @
  f6e4f725543514bd67441d4be9ca181392f48c73`; Core/tests are unchanged from the first-hand Grid/Gas
  inspection base `45b15623`. Re-read fresh product authority before making any current claim.

  Owner target for the future scale node: about 150 rooms, average roughly 8×4×8 m with 20–30%
  variation, long corridors and vertical layouts; giant hangars stay paused. Gas owns simulation and
  its evidence. Program/Multiplayer owns session/identity/input delivery and returns network proof.

boundaries: |
  Owner-present PLAN only. Do not mutate the product repo or create a product branch/worktree. Do not
  run Unity, tests, build, Deliver or benchmarks; do not issue BUILD, PAIR-CANDIDATE, executor CALL or
  any engineering root.

  Produce the route map, not detailed implementation designs for every node. Each future major node
  must name its own later owner-present detailed-plan gate before implementation. In particular, do
  not silently choose the far-room optimization model, min-spec hardware/budget, parallel algorithm,
  exact public API or generated-level storage layout. Present any owner decision needed to freeze the
  master plan in plain language and wait for actual words.

  Do not take ownership of Grid spatial/commit/change infrastructure, generic Resolver, Level topology,
  Character/Actor authority, Multiplayer session/transport, Integration Lab composition, rendering,
  presentation, canon or balance. Do not invent Grid signatures before contract freeze. Preserve the
  current gas-local forced-flow behavior and deferred independent Wind target, but create no Wind node,
  track, API, state, adapter, fixture or stub.

  Do not reopen S0–S6, Sc-damage, Sc-catalog, Lv-ingest or old executor routes. Giant hangars, late join,
  dedicated server and a second permanent Lab remain outside Gas V1 unless the owner explicitly changes
  that boundary. Direction default stays Grid unless the owner explicitly changes it.

done_when: |
  1. One owner-readable artifact `work/gas-v1-master-plan.md` explains the complete route in ordinary
     language without requiring the owner to know internal ids, code names or stale road terminology.
  2. The route is decomposed into a small set of dependency-ordered Gas-owned nodes. Every node names
     its outcome, why it exists, entry dependency, proof/exit condition, external handoff and mandatory
     separate detailed-plan gate; the master plan does not pre-design its implementation.
  3. The map covers at least: live deterministic Gas composition; runtime source lifecycle; committed
     publication/digest; generated-Level intake; full-level scale/optimization; world-change reaction;
     player consequence output; Multiplayer proof handoff; and one permanent Integration intake.
  4. The scale node binds the owner profile of about 150 rooms, average roughly 8×4×8 m with size
     variation, long corridors/verticality and no giant-hangar promise. It defines representative and
     stress evidence, min-spec dependency and an explicit owner verdict before any optimization BUILD,
     without choosing the technical model in this master-plan session.
  5. The Multiplayer boundary states exactly what Gas must return (deterministic reducer, ordinary digest,
     ordered event surface and eight-player load evidence), what Program/Multiplayer owns, where real
     two-machine and final eight-player proofs gate co-op readiness, and when a separate Multiplayer track
     requires a fresh map decision.
  6. Grid, Level/Structure, Character/Actor, Program/Multiplayer, Integration, Wind, Presentation and canon
     handoffs have explicit inputs/outputs and wait conditions; no foreign behavior is reassigned to Gas.
  7. Current delivered foundations are reused, obsolete routes stay closed, and every deferred/cut item
     has a return trigger or explicit non-goal.
  8. Sequence, safe parallel planning, blockers and owner decision points are visible. The first later
     detailed node plan is recommended, but no product execution is opened.
  9. The owner receives the complete master plan and accepts or corrects it in actual words. Without those
     words, keep this same planning position and do not open a subordinate node plan.

return: |
  One work RESULT with the accepted or owner-pending Gas V1 master plan; exact owner words; node map;
  dependencies and parallel front; proof/exit condition per node; scale and min-spec gate; Gas versus
  Multiplayer boundary; all cross-track handoffs; obsolete/deferred/cut disposition; risks; one later
  detailed planning handoff or exact blocker; and an explicit not-run list.

budget: one fresh owner-present planning session
surface: owner-present

END_OF_FILE: live/indie-game-development/work/c-work-gas-v1-master-plan-001-call.md
