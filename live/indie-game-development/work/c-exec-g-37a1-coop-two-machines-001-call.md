# CALL c-exec-g-37a1-coop-two-machines-001

direction: indie-game-development
track: t-coop
for: t-5
node: g-37a1
to: executor (engineering root CALL, product repo)
status: READY (разблокирован 2026-07-28)
issued: 2026-07-28 by s-shape-g-37a1-core-bet-001
amended: 2026-07-28 by s-repair-g-37a1-exec-call-contract-001 (same contract defect as the venue CALL)
amended: 2026-07-28 by s-work-g-37a1-topology-boundary-001 — контракт реза получен и вписан ниже; статус BLOCKED → READY

engineering_contract: 31
stage: PLAN (root entry; the repo runner carries the leg through its own ladder)
slot: НЕ ВЫБРАН — выбирается владельцем в момент разблокировки, из свободных `WIN-U1..WIN-U4`,
      после read-only проверки `tools/select-slot.ps1 -Slot WIN-U<N>`. Работа НИКОГДА не идёт
      в `C:\projects\Unity\GasCoopGame_dev` (WIN-CTRL — только интеграция, feature-WIP запрещён).
budget: одна инженерная нога по контуру продукта

## КОНТРАКТ РЕЗА — получен 2026-07-28, зависимость снята

`c-work-g-37a1-topology-boundary-001` вернул его. Полная страница с обоснованием:
`C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\topology-boundary-g-37a1.md`
— читать §4 (контракт), §2 (идентичность и что складывает контрольная сумма) и §3 (цена реза).

```
DigCommand                 — сериализуемая, целочисленная, без Unity
  tick     : int           — такт, к которому применяется (он же ключ барьера полноты ввода)
  peerId   : int           — кто; авторитетную личность назначает хост (существующее правило шины)
  x, y, z  : int           — АБСОЛЮТНАЯ координата решётки, НЕ плотный индекс клетки
  seq      : int           — собственный номер игрока внутри такта

TopologyCommit             — одна атомарная фиксация, одна на такт
  tick        : int
  revision    : int        — ++revision при фиксации; строго монотонна, единственная ось порядка
  openedCells : (x,y,z)[]  — канонический возрастающий порядок (z,y,x), дедуплицировано
  changedFaces: (cell, face, conductivity)[]   — ВЫВЕДЕНО, не авторское
```

Полный порядок применения — `(tick, peerId, seq)`; он строгий, поэтому обе машины применяют одно и то
же множество в одном порядке. Правила: фиксация ВНУТРИ такта, после упорядочивания команд и ДО шага
вещества (строка 9 — у вскрытия есть начало); либо все открытые за такт клетки легли, либо ни одна;
рез уже открытой клетки — детерминированный no-op без строки в дельте (строка 8 и проверка M3 делают
мир монотонным); дельта `(revision, openedCells)` — это то, что проигрывает отстающая машина, а
ревизия — точка возобновления. **Контракт НАМЕРЕННО не несёт:** отката, предсказания, примирения,
материала клетки и флага «ломаемая поверхность» — заранее объявленная поверхность мёртвого яруса
сюда не возвращается ни в каком виде.

**Ничего из этого не пишется с нуля, и это дешёвая половина новости:** `NearGasTickInput` уже
сериализуемый вход ровно одного такта с отказом при неверном индексе; `NearGasSimulation.Step` уже
одна атомарная фиксация (подготовка в приватном черновике → проверки → `CommitPreparedStep`);
`RevisionFeed` уже выдаёт монотонную ревизию; `FishNetTickInputBus` уже даёт надёжно-упорядоченную
ретрансляцию через хоста с барьером полноты ввода по такту; а `CoarseBreachLayer.Command` даёт
правильную ФОРМУ команды при неправильном ключе — форму берём, ключ нет. **Оговорка, которую нужно
проверить первой:** `NearGasSimulation` НЕ ПОДКЛЮЧЁН — демо-директоры зовут `Voxelizer.Voxelize` и
`VoxelField.CreateZero` напрямую (issue `i-live-tier-dead-islands-001`).

**ОДИН ВОПРОС ИЗ §SEAM 5 ТЕПЕРЬ ВЫНУЖДЕН, А НЕ ОТКРЫТ:** частота такта обязана быть ОДНА на сессию,
иначе поле `tick` в команде означает у двух машин разное. Остальные два вопроса шва открыты:
кто владеет значением ручки в сессии на две машины, и переключатели стенда — на ИГРОКА или на СЕССИЮ.

**ЗАВИСИМОСТЬ, КОТОРУЮ НАДО ЗНАТЬ ДО СТАРТА:** мутация мира (адресация по координате вместо позиции
в сортировке, чанковая адресация, путь открытия клетки на такте) в бете НЕ ИМЕЕТ СВОЕЙ ЗАДАЧИ —
решение владельца `d-sim-build-task-001` в `NOW.md`. Пока оно не принято, эта нога либо ждёт, либо
получает мутацию мира в свой объём — но не молча.

## goal

Владелец стартует один и стартует с напарником на второй физической машине; обе машины видят ОДИН
участок, который обе меняют. Рез, сделанный одним, виден у другого; ничто не присутствует у одного и
не отсутствует или устарело у другого.

## context (CALL самодостаточен)

**Продуктовый репозиторий:** работа идёт в слоте `WIN-U<N>`, выбранном владельцем при разблокировке
(см. §slot выше).

**Спецификация требований (абсолютный путь):**
`C:\my_global_workflow_worktrees\indie-game-development\live\indie-game-development\work\core-requirements-g-37a1.md`
Обслуживаемая строка — **16**: «Сетевая основа с первой строчки; старт одному и старт вдвоём на второй
машине». Карточка узла — критерий 11.

**ПОЧЕМУ ЭТА ПОЛОСА СУЩЕСТВУЕТ С ПЕРВОГО ДНЯ, ХОТЯ ЗАБЛОКИРОВАНА.** Критерий 11 владельца: «Сетевая
основа не откладывается — кооператив идёт сразу за первым играбельным ядром, а не после игры», и его
же «это 100%». Связывающее ограничение: **вынести кооператив ЗА ПРЕДЕЛЫ узла g-37a1 может только
владелец, не планирующая нога.** Распределение работ ВНУТРИ узла — обычная последовательность и
разрешения не требует. Полоса объявлена сразу именно чтобы кооператив не выпал из бета молча.

**Как владелец сам читает строку 16, его слова 2026-07-28:** «Тут имелось в виду синхронизация, да, то
есть по кооперативу». **Это требование СИНХРОНИЗАЦИИ, а не видимости и не камеры.** Игрок от первого
лица себя не видит — это не нарушение строки.

**Что уже забанковано в продукте и НЕ переписывается (ветка 2, его вердикт):** слой детерминизма и
репликации и край FishNet сохраняются; перенос вещества по решётке и проводимость по количеству
открытых подграней сохраняются. Это самое дорогое, что есть, и оно остаётся.

**ВОПРОСЫ §SEAM 5, которые никто не решал и которые всплывут здесь** (источник:
`live/indie-game-development/work/converge-g-37a1-core-rows.md`, §SEAM пункт 5):
- кто владеет значением ручки в сессии на двух машинах;
- должен ли шаг симуляции быть одинаковым на обеих машинах и что происходит при расхождении;
- каждый переключатель стенда — на ИГРОКА или на СЕССИЮ: бессмертие для одного игрока и бессмертие для
  сессии — это разные игры.
**Одно свойство уже вынуждено:** ручка, меняющая симуляцию, обязана иметь ровно ОДНО действующее
значение на сессию, иначе «оба копают в одном участке» ложно по построению.

**ОТДЕЛЬНОЕ ПРЕДУПРЕЖДЕНИЕ, установленное первыми руками 2026-07-28:** идентичность регионов в живом
ярусе сегодня ВЫВЕДЕНА ИЗ ГЕОМЕТРИИ и складывается в контрольную сумму, по которой две машины сверяют
мир (`Core/Field/Voxel/VoxelField.cs:903-923`). При непрерывном копании геометрия меняется каждые
несколько секунд. Ответ на это даёт `t-1`; здесь он ПОТРЕБЛЯЕТСЯ, а не изобретается заново.

## boundaries

- Позднее подключение игрока (late join) **вычеркнуто** и здесь не появляется: ни один текст владельца
  его не содержит, а полный перезапуск уровня по смерти вместе со строкой 16 делает его противоречивым.
- Никакого выделенного сервера, никаких корректирующих снимков по хэшам чанков, никакого предсказания и
  примирения — этого нет в первом ядре.
- Не изобретать ответы на вопросы §SEAM 5, если они окажутся решениями владельца: назвать и остановиться.
- Требования не меняются.
- Недоступный инструмент (Unity не запущен, нет второй машины) **ОСТАНАВЛИВАЕТ ногу и спрашивает
  владельца.** Проверка на двух физических машинах — это шаг владельца, и он не считается выполненным,
  пока владелец его не подтвердил.

## done_when

1. Владелец стартует один — работает.
2. Владелец и напарник стартуют на двух ФИЗИЧЕСКИХ машинах и оказываются в одном участке.
3. Рез, сделанный одним игроком, появляется у другого; вещество ведёт себя одинаково у обоих.
4. Ничто не присутствует у одного и не отсутствует или не устарело у другого.
5. Ручка, меняющая симуляцию, имеет ровно одно действующее значение на сессию.
6. Гейты продукта зелёные на ветке `dev`; подтверждение двух машин — от владельца.

## return

RESULT домой в направление.

END_OF_FILE: live/indie-game-development/work/c-exec-g-37a1-coop-two-machines-001-call.md
