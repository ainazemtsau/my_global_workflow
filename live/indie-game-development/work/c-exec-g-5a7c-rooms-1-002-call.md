# CALL — c-exec-g-5a7c-rooms-1-002

to: executor
kind: engineering
repo: ainazemtsau/GasCoopGame
engineering_contract: 36
mode: ПРОБА — режим по умолчанию контракта 36.
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-rooms-1
issued: 2026-08-12 by s-work-g-5a7c-rooms-1-checkpoint-001
status: ready
basis: `8c3bb8af16e40357055569092fe1f98b2eef18aa` — `origin/main` = `origin/dev`, перемерено
       `git ls-remote origin` 2026-08-12.

**Это продолжение `c-exec-g-5a7c-rooms-1-001`, а не новая работа.** Дом построен, владелец принял
его своим глазом (`5dc30367 docs(result): record owner-eye acceptance`), Направление кандидата
перемерило. Не хватает ровно одного: он собран на базе, устаревшей ДВАЖДЫ, и не опубликован.

## slot

**WIN-U2**, каталог `C:\projects\Unity\GasCoopGame_win-u2`, ветка `slot/win-u2`. Кандидат уже там:
`5dc30367`, `7ba39fab`, `b3db9606` (reapply), `c2c0cc00` (revert), `e592d7c6` (работа). Дерево
чистое. Слот числится `CLAIMED` под lease `c-exec-g-5a7c-rooms-1-001:BUILD` — продолжай под ним же
или перевзведи под `c-exec-g-5a7c-rooms-1-002:BUILD`.

## goal

**Перебазировать принятый дом на текущую опубликованную голову, переисполнить откат на НЕЙ и
опубликовать.**

## context — перемерено Направлением 2026-08-12

**ГЛАВНОЕ, И ЭТО ОПАСНЕЕ, ЧЕМ БЫЛО У ТЕЛА.** Родитель твоего рабочего коммита `e592d7c6` —
`a7d37b3e`. С тех пор в `main` опубликовались ДВЕ задачи: «место как факт» (`c1d8d384`) и тела
(`8c3bb8af`).

Поэтому дифф `origin/main..HEAD` сегодня содержит **23 УДАЛЕНИЯ ЧУЖОЙ РАБОТЫ**, среди них:
- весь арт тел — `Art/Bodies/Sources/Quaternius/Casual_Male.fbx` (1.6 МБ), `License.txt`,
  `SOURCE.md`, `AUTHORING.md`, контроллер `QuaterniusPlaceholderLocomotion.controller`, обе
  заготовки `Quaternius_CasualMale*.prefab`;
- работа «места как факта», включая её тестовый файл.

Публикация как есть **стёрла бы две принятые владельцем задачи**.

**ХОРОШАЯ НОВОСТЬ: перебазирование чистое.** Перемерено `comm` по спискам путей: собственный дифф
дома (12 содержательных файлов — семь материалов обстановки, `World/ThreeRoomHouse.prefab`,
`Scenes/IntegratedHouse.unity`, новые EditMode-тесты с их asmdef и отчёт) и дифф опубликованного
(`a7d37b3e..origin/main`) **не пересекаются НИ ОДНИМ файлом**. Конфликтов быть не должно; если
конфликт появился — СТОП домой, значит мой замер неверен.

**ОТКАТ НАДО ПЕРЕИСПОЛНИТЬ.** Существующий сошёлся со своей базой и на новой ничего не доказывает.
Целевое дерево: **`tree(8c3bb8af)`**, возьми его сам через `git rev-parse '8c3bb8af^{tree}'` и
приведи в возврате.

**ЧИСЛО ТЕСТОВ.** На `8c3bb8af` база — **239 passed, 0 failed, 0 skipped**, прогнано Направлением.
Твои EditMode-тесты дома в этот счёт не входят: их гоняет Unity, а не `dotnet test`. Назови оба
результата отдельно.

**ПРИЁМКА ВЛАДЕЛЬЦА УЖЕ ЕСТЬ И ПОВТОРНО НЕ ЗАПРАШИВАЕТСЯ.** Он принял дом глазом до этого наряда.
Перебазирование не меняет ни одного байта дома — оно только перестаёт удалять чужое.

## boundaries

- **Ничего нового не строится.** Ни одной новой комнаты, ни одного предмета обстановки, ни одной
  правки поведения. Только перебазирование, переисполненный откат и публикация.
- **ФАЙЛЫ ЧУЖИХ ЗАДАЧ НЕ ТРОГАЮТСЯ.** После перебазирования в твоём диффе не должно быть НИ ОДНОГО
  удаления: ни `Art/Bodies/**`, ни `Presentation/**`, ни `Settings/PresentationSettings.*`, ни
  `World/Householder.prefab`, ни `Core/Householder/**`, ни `tests/**`, ни чужих отчётов.
- **`Core/Householder/**` и `Assets/TunnelCrew/Profiles/**` не трогаются** — параллельно идёт
  `t-occupations-1` в WIN-U3.
- Инструмент недоступен → СТОП домой. Конфликт при перебазировании → СТОП домой.
- **Расписка обязательна.**

## done_when

1. **ПЕРЕБАЗИРОВАНО НА `8c3bb8af`,** и дифф `origin/main..HEAD` содержит ТОЛЬКО собственные файлы
   дома: **ноль удалений чужой работы**, проверено выводом `git diff --name-status | grep '^D'`.
2. **ОТКАТ ПЕРЕИСПОЛНЕН НА НОВОЙ БАЗЕ** настоящим `revert`, и дерево после него сходится с
   `tree(8c3bb8af)` знак в знак.
3. **ОПУБЛИКОВАНО** в `dev` и `main`, readback `git ls-remote` приведён, headless-прогон на
   выгруженном дереве публикации назван числом против базовых **239**, EditMode-прогон дома назван
   отдельно, слот отпущен.

## return

Первая строка — расписка: три строки `done_when`, базис `8c3bb8af`.

Дальше: команды и хеши перебазирования; **вывод `git diff --name-status origin/main HEAD | grep '^D'`
после него — он обязан быть пустым**; хеши и совпадение деревьев отката; readback `git ls-remote`;
оба числа прогонов; подтверждение, что голова сверена ПЕРЕД публикацией, а не только перед сборкой.

## budget

Мало. Это механическая нога.

END_OF_FILE: live/indie-game-development/work/c-exec-g-5a7c-rooms-1-002-call.md
