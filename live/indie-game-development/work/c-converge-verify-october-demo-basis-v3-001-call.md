CALL c-converge-verify-october-demo-basis-v3-001
to: session
direction: indie-game-development
play: converge-verify
node: g-12fd
verify_target: specification
goal: |
  A fresh independent session tries to BREAK the exact Demo Basis
  `live/indie-game-development/work/october-demo-basis-v3.md` before any narrow
  review closes the parked specification outcome `g-12fd`.
context: |
  Exact artifact identity: `october-demo-basis-v3`, node `g-12fd`.

  APPROVAL BINDING — read it precisely, it is narrower than v2's.
  The owner's verbatim words of 2026-07-26 stand on: the MUST 5 line, the removal
  of all technique from the Basis, the removal of the «обязательно двое»
  requirement, the four provisional first-slice decisions, and the ordered
  engineering read. All five decisions are reproduced verbatim in the artifact's
  final section «Что изменилось в версии 3» and in the leg receipt
  `history/2026-07-26-s-work-october-demo-basis-v3-revision-001.md`.
  MUST items 1–4 and 6–8 carry over from v2 verbatim under v2's «да».
  The remaining eight findings were delegated to the leg by his exact words:
  «Остальные восемь находок — механические, чини сам, ко мне не возвращайся с
  ними. Время ограничено: 7 августа граница урезания объёма.»
  There is NO separate «да» on the assembled v3 text as a whole, and none was
  requested — his instruction forbade returning. Treat every mechanical repair as
  agent work performed under an explicit owner delegation, not as owner content:
  that is exactly the class this verification exists to attack.

  Predecessor: `october-demo-basis-v2` was owner-approved 2026-07-26 («да» on its
  exact eight-item MUST list) and FAILED independent verification the same day —
  `history/2026-07-26-s-converge-verify-october-demo-basis-v2-001.md`, ten
  findings F1–F10. v2 is now a tombstone; its full text lives in commit
  `c982c79a`. v1's full text lives in commit `3f135b9b`.

  Current authority to read: `CHARTER.md`; `TREE.md` (`g-12fd` done_when);
  `NOW.md`; `knowledge/canon-clean-authority-reset.md`;
  `knowledge/strategy-reset-boundary.md` (note issue `i-frontier-knowledge-stale`
  — that file still names a superseded CALL as the sole lawful frontier);
  `work/concept-frame-v1.md`; `work/pgg-analysis-2026-07-10.md`; the exact
  artifact above; the v2 verification receipt named above; and this CALL.

  The binding done_when set is `g-12fd` in `TREE.md`, plus the done_when of
  `work/c-work-october-demo-basis-v3-revision-001-call.md`, which is a live file
  and needs no quoting.

  HOW EACH v2 FINDING WAS DISPOSITIONED. Verify each disposition against the
  artifact; a disposition that did not actually land in the text is a defect.

  - F1 (MUST 5 misattributed the recorded PGG verdict) — CLOSED BY DELETION on
    the owner's words. MUST 5 is now one line with no tool, no attribution and no
    conditional acceptance, and the «Свидетельство под MUST 5» paragraph is gone
    from «На чём это стоит». No archive read of the PGG spike receipt was
    authorized; the owner ordered a fresh engineering read instead.
  - F2 (determinism-removal rationale unsupported) — CLOSED BY DELETION on the
    owner's words. Host-side assembly, seed and layout replication no longer
    appear in the Basis at all. The question itself now lives only in
    `i-procgen-determinism`.
  - F3 (causal-cooperation supports over-counted and leaning on deferred and
    undecided content) — RESOLVED BY OWNER CONTENT. One support remains (the big
    ball needs two sides at once), the «обязательно двое» requirement is removed,
    and the refutation test plus falsifier 1 are the check. His words:
    «Не доказывать на бумаге: смотрим в игре. Ничего дополнительно не
    придумывать.» Attack whether §2, §3 and §5 still lean on anything the removed
    supports carried.
  - F4 (falsifier 5 vacuous or dead) — MECHANICAL under delegation. The text is
    kept verbatim, explicitly marked «Признак спит», declared not-evaluated and
    not-fired while the mechanic is deferred, and revived only with the mechanic
    and a new Basis version. Attack whether a dormant falsifier is honest or
    still a dead criterion.
  - F5 (done_when atom «исключения явны» had no content) — MECHANICAL under
    delegation, and the subtractive option the finding named was NOT taken: the
    heading keeps «Исключения» and the artifact now states explicitly that it
    declares no case in which it deliberately does not apply, naming the
    provisional decisions and open questions as the only things it deliberately
    does not decide. Attack whether an explicit null satisfies the atom.
  - F6 (invalidation set not updated for a generated level) — MECHANICAL under
    delegation. The evidence tuple gains a fifth binding (the exact generated
    level the attempt ran on) and two invalidators (the module set, the
    generation rules). The doorway-width parameter disappeared with F1's
    deletion; the physical requirement now lives only in MUST 4, and the artifact
    says so. Attack completeness against the current MUST list.
  - F7 (public-claim ceiling permitted claims no MUST can produce) — MECHANICAL
    under delegation, resolved by narrowing, since restoring a MUST would reverse
    the owner's deferral. «реагирует на здание» and «при настоящей опасности»
    moved from «Можно» to «Нельзя, пока не появится в сборке», with the reason
    recorded, including that the provisional first-slice decision has no damage
    and no death. Attack the remaining «Можно» list item by item.
  - F8 (MUST 5 feasibility unbounded inside a fixed calendar) — ANSWERED BY THE
    OWNER with the engineering-verdict option, not with an appetite and not with
    a cut. His words are in `i-procgen-determinism`: PGG and Dungeon Architect
    are to work together, PGG determining structure inside a module and Dungeon
    Architect assembling the level from modules; separately to be checked are
    Unity 6.3 with PGG (kill-risk), zero precedent for the pair, zero art, and
    2–4 days to the first module. No appetite exists, because this node creates
    no bet and no tasks; the standing 2026-08-07 scope-cut boundary is the
    owner's own named limit. The artifact names the dependency risk in one
    non-technical line under the MUST list. THIS IS THE SHARPEST TENSION IN v3
    and must be attacked directly: the owner ordered all technique out of the
    Basis, while an unresolved third-party dependency is exactly the thing a
    boundary specification usually has to name where the MUST item lives. Decide
    whether one pointer line discharges it or whether it is a disclosure gap; do
    not resolve it by rewriting the owner's line.
  - F9 (the promise asserted «двадцать минут» that nothing backs and nothing
    tests; §2 asserted route-blocking and punishment) — MECHANICAL under
    delegation, resolved subtractively, because adding a session-length falsifier
    would have invented content the owner did not order and he said «ничего
    дополнительно не придумывать». The number left §1 and now lives only in open
    question 7; «перекрывает маршрут» and «наказывает за небрежность» left §2.
    Attack whether §1's promise is now complete and whether §2's danger stage
    still has anything MUST-backed to stand on.
  - F10 («связана со свежими release-reality receipts» only transitive) —
    MECHANICAL under delegation. «На чём это стоит» now names the Next Fest
    page-before-registration requirement with its source, the five standing
    dates, the 2026-08-07 boundary in the owner's words, the Steam fee/credit/no-
    AppID state with its screenshot receipt, and the fact that no product build
    is bound to this Basis yet with the 2026-09-03 candidate gate. Attack whether
    the receipts are current and whether the artifact's own evidence tuple is now
    release reality rather than only build reality.
  - D1 (a count stated where the itemization yields another number) — APPLIED
    HERE: this CALL states open questions as a list, never as a count. The
    artifact holds the itemized questions 1, 2, 3, 5, 6, 7, 8, with 4 resolved
    into MUST 5 and the v1 numbering deliberately preserved.

  CAVEATS CARRIED FORWARD.

  - The v1 authoring leg reordered the concept frame's DEMO-BLOCKING questions on
    explicit owner instruction: substance behaviours were derived last, not
    chosen first. Verify that v3 still satisfies every atomic done_when clause
    under that order.
  - During v1 authoring the owner authorized one bounded read of the frozen canon
    repository `C:\projects\gas_coop_game_canon` as evidence only, recorded as
    issue `i-canon-repo-evidence-read`. Its file list is carried forward in v3
    under «На чём это стоит». Every one of those files is historical evidence
    with zero authority; a claim resting only on a legacy file's former accepted
    status is a smuggling FAIL. No new legacy read is authorized, and none was
    performed in the v3 revision.
  - `work/oracle-demo-boundary-specification-v1.md` exists: the node-class oracle
    the v2 verification authored from first principles. It is PROPOSED, not
    promoted, and has zero authority. Reusing it unchanged would inherit the
    previous attacker's blind spots, and three parts of v3 are new surface it
    contains no test for: the «Провизорные решения для первого куска» section
    that declares itself not-law, the dormant falsifier, and the explicit
    null-exceptions statement. Extend or re-derive the oracle before the attack
    and say which; an empty oracle BLOCKS.
  - Recorded by the v2 verification as WEAK but never raised as a finding, and
    deliberately not repaired: §3's «Задели косяк. Хлопок.» — contact-triggered
    bursting is nowhere established in MUST, while the readable moment's payoff
    depends on it. §3 is owner content approved twice and was left verbatim.
    Attack it deliberately now rather than as an oversight.
  - Two-strikes watch: a finding that repeats a v2 finding on the same substance
    is a KERNEL §2 two-strikes signal, not just another finding. Say so if it
    happens.
boundaries: |
  Refute or pass. Never answer an open question, never repair the artifact, never
  choose content, never treat an agent's mechanical repair or recommendation as
  owner approval. Do not read the v3 revision session's reasoning; attack the
  artifact. The dispositions above are the leg's claims about itself and are
  attack surface, not evidence.
  Do not reverse or re-litigate the owner's decisions: procedural generation
  stays IN, technique stays OUT of the Basis, the two hall mechanics stay
  deferred, terminology stays «вещество», the provisional first-slice decisions
  stay explicitly not-law.
  Do not open shape, bet, tasks or tracks. Do not mutate product, Steam state,
  `CHARTER.md`, `TREE.md` or `knowledge/**`. Do not read `archive/**` or the
  frozen canon repository. Do not publish an uncalibrated numeric release chance.
  The artifact's unanswered content questions are a declared legitimate result.
  An unanswered question is a FAIL only where a done_when clause, a MUST item, a
  path stage, the cooperation support or a public claim depends on its answer.
done_when: |
  1. Every atomic clause of `g-12fd` done_when and of
     `c-work-october-demo-basis-v3-revision-001` done_when is split and mapped to
     an exact artifact section; an uncovered clause is named, never filled.
  2. Every weight-bearing claim required by done_when names its supporting
     value/fact with a resolved citation; untraceable or legacy-status-only
     support is FAIL. Every mechanical repair listed above is checked for whether
     it actually landed in the text and whether it silently chose content.
  3. The single cooperation support, the readable moment, the falsifiers
     including the dormant one, the null-exceptions statement, the not-law
     provisional section and the invalidation tuple are attacked for
     circularity, unfalsifiability and hidden dependence on the open questions.
  4. MUST 5's unresolved external dependency inside the standing calendar is
     attacked as a disclosure question, given the owner's order that technique
     leave the Basis. A MUST that cannot be built is named, never silently
     downgraded; declaring it unbuildable remains an engineering verdict, not a
     specification verdict.
  5. Outcome is PASS with `§SIGNOFF: converge-verify passed @ <date>` and
     play_check `verify_target: specification complete=PASS smuggling=PASS`, or
     FAIL with named findings returned to owner-authority `work` on `g-12fd`.
  6. On PASS, exactly one narrow `review` CALL is opened naming the node, the
     exact artifact identity, the owner receipts and this verification receipt.
     No shape is opened either way. `g-12fd` stays parked.
return: |
  One converge-verify RESULT with verify_target, the atomic clause map, the
  smuggling audit, the oracle it used and how it was extended or re-derived,
  PASS/FAIL, and either the signoff plus one narrow review CALL, or named
  findings for the owner-authority work continuation.
budget: one fresh session, no owner presence required
surface: any, but NEVER the v1 authoring, v2 revision or v3 revision chat

END_OF_FILE: live/indie-game-development/work/c-converge-verify-october-demo-basis-v3-001-call.md
