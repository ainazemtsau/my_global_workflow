CALL c-map-g-5a7c-ui-line-removal-001
to: session
direction: indie-game-development
play: map
node: g-5a7c

goal: |
  Карточка `g-5a7c` больше не запрещает игровой интерфейс, и её действующий текст соответствует
  прямому приказу владельца 2026-08-07. Точный заменяющий текст утверждён его отдельным словом
  на показанный целиком фрагмент (G9), и реестр `owner_approved` несёт этот вердикт с записанной
  силой токена.

context: |
  ЕГО СЛОВА 2026-08-07, ДОСЛОВНО И ЦЕЛИКОМ — это и есть основание наряда:
  «смотри, игрового интерфейса нет вовсе, я не понимаю, откуда это пошло. Ну типа, если надо,
  пусть будет. Это надо убрать это требование, игрового интерфейса нет вовсе, что за бред?
  Откуда оно вообще появилось? Это надо убрать. Ну лобби, естественно, да, должно быть. И если
  там надо какой-то интерфейс во время игры, то пожалуйста, можно добавлять.»

  ОТКУДА ОНО ПОШЛО — ответ, который он спросил, установлен и проверен: из фразы «без UI»,
  приписанной ему ногой `s-map-real-core-node-001`. Единственный носитель этой фразы на диске —
  таблица §evidence той ноги, `history/2026-08-07-s-map-real-core-node-001.md:58`, плюс
  производная строка реестра `TREE.md:4`. Независимого следа разговора нет. Разбор:
  `work/converge-g-5a7c.md` строка W111 и §IMPORT I33.

  ЧТО ФИЗИЧЕСКИ МЕНЯЕТСЯ — два места в `TREE.md`, оба внутри `g-5a7c`:
  (1) критерий 8, `TREE.md:128`. Сегодня кончается словами
      «Это ИНСТРУМЕНТ, а не игровой интерфейс: игрового интерфейса здесь нет вовсе.»
      Предложенный заменяющий текст последнего предложения:
      «Это инструмент разработчика, а не часть игры для игрока.»
  (2) строка исключений, `TREE.md:133`. Сегодня начинается словами
      «Здесь НЕ появляются: игровой интерфейс; телевизор и любой крупный груз; …»
      Предложенный заменяющий текст: то же самое без «игровой интерфейс; ».

  ЧТО ЭТО НЕ ТРОГАЕТ И ЧЕГО НЕ ОТКРЫВАЕТ: остальные одиннадцать критериев, цель, `why`, `edge`,
  `risk`, статус `parked`. Оговорка критерия 10 «поставленный игровой режим третьего лица не
  заказывается» стоит отдельно и снятием запрета на интерфейс не затрагивается. Лобби этим
  запретом никогда и не покрывалось (`knowledge/lobby-is-not-a-crutch.md`), и он это подтвердил.

  ВТОРОЙ ВОПРОС, КОТОРЫЙ ЕМУ СТОИТ ЗАДАТЬ В ТОЙ ЖЕ НОГЕ, потому что он привёз материал сам и
  тот трётся о строку исключений. Его слова 2026-08-07: «надо будет просто сделать где-то
  условно вне дома точку спавна, где все спавнятся вот на своих местах… может быть такая
  интерпретация туннеля, типа там быть небольшая точка за домом, туннель, просто прямой, да,
  прямоугольный, ну и вход в дом. То есть это не как реализация туннеля, просто что спавн за
  домом, и вот некий туннель туда можно забегать.» Строка исключений выносит «тоннели, сеть
  доступа и копание» отдельной ставкой ПОСЛЕ узла. Это уже записано решением `converge`
  (`work/converge-g-5a7c.md` W112) как ГЕОМЕТРИЯ, а не механика, и границу он поставил сам.
  Спросить надо ровно одно: нужна ли карточке половина фразы, называющая это прямо, чтобы
  будущая нога не прочла прямой ход как начало тоннельной ставки. Вариант «не нужна, разбора
  converge достаточно» — законный и должен быть показан наравне.

  ТРЕТЬЕ, ЕСЛИ НОГА ВСЁ РАВНО ОТКРЫВАЕТ РЕЕСТР `owner_approved` (`TREE.md:4`): там стоит
  утверждение, что правила на число открытых детей узла в OS нет. Оно ЛОЖНО — правило есть,
  `os/schema/direction-files.md:57`, дословно `Width ≤7 open children per node.` Нарушения нет,
  открытых детей ровно семь. Полная починка записи (она уехала также в `NOW.md` и `LOG.md`) —
  отдельная нога `repair`, и этот наряд её не выполняет; но повторять ложное утверждение в новом
  тексте нельзя.

  Состояние: `live/indie-game-development/NOW.md`, `TREE.md:117-142`, `work/converge-g-5a7c.md`.

boundaries: |
  Не трогать другие критерии, цель, `why`, `edge`, `risk`, статус и других узлов дерева.
  Не открывать ставку, не создавать задач и полос. Не решать, КАКОЙ именно интерфейс появится в
  игре — снятие запрета не является заказом на интерфейс. Продуктовый репозиторий не трогать.
  Не чинить запись о ширине дерева за пределами текста, который эта нога и так пишет.

done_when: |
  1. Текст критерия 8 и строки исключений в `TREE.md` не содержит запрета на игровой интерфейс, и
     заменяющий текст был показан владельцу целиком ДО правки.
  2. Его вердикт на этот точный текст записан в `owner_approved` дословно, с указанной силой
     токена; ни одна строка не приписана ему без его слов.
  3. Вопрос о половине фразы про спавн и прямой ход задан и его ответ записан — включая ответ
     «не нужна».
  4. Остальные одиннадцать критериев, цель, `why`, `edge`, `risk` и `status: parked` не изменены,
     и это сказано в RESULT явно.

return: |
  RESULT с точным до/после по двум местам, дословным вердиктом владельца и подтверждением, что
  ничего сверх названного не тронуто.

budget: одна нога
