# CALL — c-work-g-5a7c-house-asset-1-001

to: session
play: work
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-house-asset-1
issued: 2026-08-14 by s-work-g-5a7c-loot-1-owner-close-001
status: ready

## goal

**ДОМ СТАНОВИТСЯ ПЕРЕНОСИМЫМ АССЕТОМ: В ПРОЕКТЕ ЖИВУТ ДВА РАЗНЫХ ДОМА, ОНИ ПЕРЕКЛЮЧАЮТСЯ БЕЗ
ПРАВКИ КОДА, И ОПИСАНИЕ ЕДЕТ ВНУТРИ АССЕТА, А НЕ РЯДОМ С НИМ.**

Шесть строк `done_when` — на карточке `t-house-asset-1`; её собственная строка `ЗАКРЫТИЕ` говорит,
что светлое закрытие здесь ДОСТУПНО: всё меряется байтами, тестами и составом ассетов.

## context

Соседняя задача дорожки `t-loot-1` закрыта его словом «закрываем» 2026-08-14
(`history/2026-08-14-s-work-g-5a7c-loot-1-owner-close-001.md`). Из её биндинговой проверки эта
задача наследует ОДИН измеренный факт, который бьёт прямо в строку 2 («описание едет вместе с
домом, а не отдельным файлом, который может с ним разъехаться»):

**расстановка лута сегодня разъезжается с домом по построению.** Десять точек `CargoSpawnPoint`
висят детьми объекта `StartConditions` в СЦЕНЕ `IntegratedHouse.unity`, а поле `_support` каждой из
них ссылается на компонент `LootSupport` ВНУТРИ экземпляра префаба `ThreeRoomHouse`. Шесть таких
опор лежат в самом префабе (Kitchen Table, Bed Frame, TV Stand, Kitchen Counter, Bedside Table,
Coffee Table), две — в сцене (Floor, Entry Bench). То есть второй дом не унесёт с собой ни одной
строки расстановки, а ссылки первого дома указывают на конкретный экземпляр. Это не требование к
этой ноге и не расширение задачи — это факт, который она обязана увидеть до того, как решит, что
именно кладётся внутрь ассета.

Ещё из того же замера, по той же причине: `SceneHousePlan` уже читает разметку префаба, а сам дом
уже префаб — механизм существует и не изобретается (сказано на карточке).

Открытые улики дорожки, ни одна из которых эту задачу не блокирует и ни одна из которых не
является её строкой: `i-bedside-radio-spawns-inside-the-mattress-001`,
`i-cargo-sinks-into-the-floor-when-the-householder-arrives-001`,
`i-householder-climbs-loot-and-stuck-on-it-001`,
`i-observation-lab-no-longer-mirrors-house-loot-001`,
`i-unity-checks-for-the-loot-foundation-were-cut-001`,
`i-test-house-draft-describes-a-house-that-is-gone-001`.

Идеи владельца рядом, содержанием этой ноги НЕ являющиеся: `idea-loot-placement-tool-001`,
`idea-level-basis-authoring-tool-001`.

Опубликованная голова продукта на момент выпуска: `53afdc2d`, `origin/main` = `origin/dev`.

## boundaries

- **Не расширять до расстановки лута.** Инструмент расстановки — отдельная идея владельца, не
  строка этой задачи.
- **Процедурной генерации нет** — его собственный вырез. Выбора дома в меню, нескольких хозяев и
  загрузки уровня на ходу здесь тоже нет (вырезы карточки).
- **Замороженный кандидат `e0a30194` не открывать.**
- Из OS-сессии продуктовый репозиторий руками не править — сборка едет нарядом исполнителю.
- Владельцу ставить только то, что он увидит в игре.

## done_when

Шесть строк карточки `t-house-asset-1`, каждая со своей уликой. Закрытие светлое — но только если
КАЖДАЯ строка перемеряна первой рукой по байтам, составу ассетов или прогнанной команде; пересказ
возврата исполнителя уликой не считается.

## return

RESULT игры `work`: состояние шести строк с уликами; при закрытии — `state_changes`, закрывающие
`t-house-asset-1`. **Проверить при возврате свежим чтением, а не по этому наряду:** останутся ли
после неё открытые задачи ставки (сейчас `t-handlers-1` и `t-lab-2` заблокированы в соседней
дорожке) — если не останется ни одной, наряд обязан открыть `review` ставки.

## budget

Одна нога.

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-house-asset-1-001-call.md
