# CALL — c-converge-g-5a7c-repair-001

to: session
direction: indie-game-development
play: converge
node: g-5a7c
issued: 2026-08-07 by s-converge-verify-g-5a7c-001
status: ready

## goal

Починить в `work/converge-g-5a7c.md` шесть именованных мест, которые вернула первая проверка узла.
Разбор `g-5a7c` НЕ переоткрывается: карточка подписана, счёт §IMPORT/§GLOSSARY держится, 110 из 116
строк §WHAT не оспорены ни одной находкой. Работа ремонтная и узкая.

## context

**Вердикт проверки:** `verify_target: build complete=FAIL smuggling=FAIL`. Полный разбор —
`history/2026-08-07-s-converge-verify-g-5a7c-001.md`. Это был ПЕРВЫЙ отказ; следующая проверка будет
ВТОРОЙ, и по плею второй FAIL кончает бумажную фазу узла и выносит владельцу одно решение вместо
третьего круга. Значит эта нога закрывает возвращённые строки настоящим источником ЛИБО честно
оставляет их `open` с названным отвечающим — второе законно и поражением не является.

**База продуктовых фактов, перепроверена проверкой первой рукой:** `C:/projects/Unity/GasCoopGame`,
`main` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`, дерево чисто, только чтение. Все числа ниже
измерены на ней.

**Что проверка ПОДТВЕРДИЛА и что переделывать запрещено:** карточка не тронута (`git diff` по
`TREE.md` за три арх-коммита пуст); §IMPORT, §GLOSSARY, §WHAT, §DRIFT и оба §SIGNOFF не тронуты
арх-ногой (побайтово, строки 1–411, различий ноль); `W15` и `W16` не тронуты и остаются отложенными
его словами; `open_rows: 1`, `deferred: 2`, `contracts_open: 1`, `arch_open: 8` — точны. Сила всех
девяти токенов владельца, включая «А, обстановка стоит», нигде не повышена до подписи под карточкой.

## boundaries

- `TREE.md` и `CHARTER.md` не трогать. Ставку, задачи и полосы не создавать. `knowledge/` не писать.
- `work/converge-g-5a7c-arch.md` не трогать — у него свой наряд, `c-converge-arch-g-5a7c-repair-001`.
- §CONTRACTS (`C01`–`C26`) не трогать: раздел написан арх-ногой, и правит его она же.
- Продуктовый репозиторий только на чтение.
- Владельцу НЕ выносить ни одного нового решения без прямой необходимости. Строка, которую нельзя
  закрыть источником вне ноги, помечается `open` с отвечающим — вопрос ему формулирует та нога,
  которая до него дойдёт.
- §IMPORT и §GLOSSARY не переписывать: находок по ним нет.

## done_when

1. **`W07b` — обоснование опровергнуто арифметикой и приведено в честный вид.** Опровержение целиком
   на числах самой строки: (а) строка задаёт `_movementSpeed` 3 → 1.0, то есть делит скорость на 3,
   а обосновывает сохранение чисел силы тем, что «скорости падают в 12.5 раз» — при её собственных
   числах `m·Δv` растёт в 12.5/3 ≈ 4.2 раза, и типичный контакт уезжает примерно на две полосы вверх
   по шкале 60 / 150 / 400, то есть три настроенных порога смысла НЕ сохраняют; (б) «ускорение падает
   ровно во столько, во сколько уменьшился мир» верно только в насыщении: вне его усилие пружины
   пропорционально растяжению, растяжение — длина и падает в 12.5 раз при сохранённой жёсткости, а
   масса растёт в 12.5 раз, значит ускорение падает в 12.5² ≈ 156 раз; (в) чтобы выбрать потолок
   200 Н, нужно растяжение около полуметра в мире, где налётчик ростом 0.16 м, то есть потолок на
   руку перестаёт связывать вовсе. КЛАСС («назначенные игровые числа») остаётся закрытым его словом и
   не переоткрывается. Новых чисел эта нога НЕ придумывает: строка либо получает верное обоснование,
   либо теряет его и остаётся `→ PLAN` с честной пометкой, что перевод масштаба не проверен.
2. **Порог по числу рук назван вопросом, а не следствием.** При числах строки `W07b` вес груза
   50 кг × 9.81 = 490.5 Н, а потолок на руку 200 Н (`CargoHaul.HandForce` → `Cap(…, MaxHandForce)`,
   сумма по рукам в `Reduce`): одна рука 200 Н, две 400 Н — веса не перебивают обе, три 600 Н
   перебивают. Значит его собственная текстура `I09` («**два уже могут выше**») и подписанный
   критерий 3 («чем больше налётчиков взялись, тем выше») при этих числах невыполнимы на второй
   ступени. В §WHAT появляется строка, которая это называет и помечена `open` с отвечающим; ответом
   она НЕ заполняется.
3. **Критерий 4 получает строку про число носильщиков.** Сегодня «волочение тяжёлого в одиночку
   громче, чем перенос того же груза несколькими» доходит только до `W33` (измерение, что этого нет:
   `NoiseOf` получает лишь силу контакта и координаты, список держащих в расчёт не входит) и до `W34`
   (его слова про общий слой). Как число носильщиков входит в громкость, не решает ни одна строка и
   ни одна A-строка, и `open` нигде не помечено — слово «держащих» встречается в наборе ровно один
   раз. Заводится строка с меткой `open` и названным отвечающим (`→ PLAN`, если это следствие
   устройства; `→ OWNER`, если это его число). Ответом не заполняется.
4. **Пол получает строку.** Две подписанные строки стоят на нём — кр. 3 «волочится **по полу**» и
   кр. 4 «**волочение**… громче», — а в наборе слово «пол» встречается ровно дважды, оба раза внутри
   цитаты карточки. Измерено проверкой первой рукой: в `IntegratedHouse.unity` есть `Floor`
   (`MeshCollider`) и `Delivery Yard Floor` (`BoxCollider`); ни на одном нет `HouseWall`, значит пола
   нет в описании дома вовсе (`CarryStand.ReadAuthoredWalls` читает только `HouseWall`); при этом
   `CargoBodyContact.Record` срабатывает на любом столкновении груза, а `CargoBodyPort` объявляет
   «The house is the only physical thing in this world the cargo can meet» — и это объявление
   становится ложным в тот момент, когда арх-вариант `A4` включает `useGravity`. Строка называет
   вопрос и помечается `open` с отвечающим; вторую половину (входит ли пол в описание дома и
   становится ли контакт с полом источником общего слоя) закрывает арх-наряд, и на неё ссылаются.
5. **`W103` и `C07` перестают говорить разное.** Сегодня §WHAT говорит `open → PLAN` и «узел-
   получатель не назван нигде», а `C07` и «Счёт этой ноги» говорят, что `PLAN` — неверный адресат и
   маршрут собран (`review`/`map`). Маршрут `C07` проверкой ПОДТВЕРЖДЁН по всем трём источникам
   дословно (`TREE.md` `g-8a41` кр. 3; `os/KERNEL.md:63`; `NOW.md:102-107`) и переписыванию не
   подлежит. Правится только строка §WHAT — она получает верный адресат или явный указатель на `C07`,
   и счёт `open_rows` приводится к тому, что получилось.
6. **Пять строк `acceptance` теряют метку, которой не заслуживают.** Легенда §WHAT дословно:
   «`acceptance` — копируется в done_when задачи дословно». Метку носят вопреки собственному тексту:
   `W39` («**Числа НЕ требование**»), `W54` («**не факт, что оно будет**… задача вправе взять
   другое»), `W73` («решено **этой ногой**»), `W94` («решено **этой ногой**»), `W39a` («граница,
   поставленная **этой ногой**»). Каждая получает `open` либо `→ PLAN` с названным отвечающим. Ни
   одна не заполняется свежим ответом — лекарство именно понижение. Заодно уходит утечка HOW:
   величины `W39` и счёт `W54` под `acceptance` стоять не могут.
7. **Три неспрошенных предмета спрошены — по одной строке на каждый, ответом не заполняются.**
   (а) уставная линза 6 «Сохраняет ли оно надёжность работы владельца?» — ноль вхождений её предмета
   в обоих файлах набора, при том что узел объявлен многоволновым его же словом, а `CHARTER.md`
   pre-mortem 8 описывает ровно этот риск; (б) «материальный результат» — пятое звено цепи критерия 8,
   встречается в наборе дважды и оба раза как переписанная строка цепи, тогда как у остальных четырёх
   звеньев есть свой ряд §GLOSSARY или своя A-строка; (в) «ширина проходов согласована с маленьким»
   (кр. 1) — доходит только до `G15` и `W40`, решающей строки нет, при том что `HousePassage.Width`
   сегодня не читает никто.
8. Счёт §WHAT в конце файла приведён к измеренному: строк в таблице **116** (`W01`–`W110` плюс
   `W07a`, `W07b`, `W39a`, `W111`, `W112`, `W113`), и он записан там, где его найдёт следующая
   проверка. Число `113` из прежнего наряда в состоянии не жило нигде и воспроизведению не подлежит.

## return

Полный Direction RESULT. `state_changes` — только `work/converge-g-5a7c.md`, `NOW.md` (`open_calls`)
и `LOG.md`. В `next` — перевод `c-converge-arch-g-5a7c-repair-001` из `waiting` в `ready`.

budget: одна сессия

END_OF_FILE: live/indie-game-development/work/c-converge-g-5a7c-repair-001-call.md
