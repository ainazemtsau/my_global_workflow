# CALL — c-work-g-5a7c-vert-1-call-001

to: session
play: work
direction: indie-game-development
node: g-5a7c
task: t-vert-1
issued: 2026-08-08 by s-work-g-5a7c-telekinesis-close-001
status: ready

## goal

Существует готовый к отправке наряд исполнителю на `t-vert-1` — мышь поднимается над полом, и
владелец после прогона говорит, КАКИМ способом мыши поднимаются в этой игре.

## context

- **Телекинез закрыт, и закрыт его словом.** `t-scale-5` доставлена и принята: дословно 2026-08-08
  «всё в принципе работает», «можем закрывать задачу». Опубликованная вершина продукта —
  `11f8ddbf1f11722c5bfa25e3104c3847e45d7b04` (`dev` = `main` = `origin/dev` = `origin/main`).
  Порог убийства (3) карточки `g-5a7c` не сработал: волна идёт своим порядком, `t-vert-1` — вторая.
  Полный разбор: `history/2026-08-08-s-work-g-5a7c-telekinesis-close-001.md`.
- **ЭТА ЗАДАЧА ИЗМЕНИЛА ТО, ЧТО СЛЕДУЮЩЕЙ НЕ ПРИДЁТСЯ СТРОИТЬ ЗАНОВО.** Взгляд по вертикали теперь
  доходит до создателя игры: клиент шлёт `axisX, axisZ, yaw, pitch, carryToggle` одним уже
  существующим ServerRpc (`Network/NetworkWalkerCourier.cs:126`), сервер хранит `Pitch`
  (`Core/Multiplayer/AuthoritativeWalkerRoster.cs:189`). Прежнее основание наряда `t-scale-3` —
  «наклон живёт только на клиенте» — БОЛЬШЕ НЕВЕРНО, и наряд обязан перемерить это первой рукой, а
  не переписать со старого текста. Новый ввод подъёма едет в тот же существующий путь, а не рядом.
- **Высоты у ходока по-прежнему нет.** Это перемеряется заново: сегодня движение — плоский набор, а
  та высота, что уже фигурирует в игре (0.02 / 0.092 м у показа груза), — это ПОКАЗ, а не физика.
  Наряд обязан назвать, что именно из этого он трогает.
- **Ограничение, названное в самой карточке заранее и НЕ чинимое здесь:** стены в правилах не имеют
  верха вовсе — только центр и полуразмеры по X и Z. Перелететь стену нельзя, и проба этого не
  проверяет. Эта работа не оценена и в волну не входит.
- **Задача закрывает ЕГО СЛОВО о способе** — прыжок, полёт, оба или ни то ни другое. Значит цель
  наряда: дать ему попробовать оба на двух клавишах, а не выбрать за него. `W16` («двигает ли
  телекинез самого налётчика») помечен вопросом ВЛАДЕЛЬЦА и здесь не решается ногой.
- **Проверка происхождения обязательна и уже дважды окупилась.** Две последние ноги нашли в задачах
  строки, выросшие из замера стоимости и из его смягчённого «я думаю, что потом», а не из
  требования. Каждая строка `done_when` наряда проверяется на источник до отправки; снятая строка
  называется вслух вместе с основанием.

## boundaries

- Вертикаль ГРУЗА не трогать — это `t-scale-4`. Потолок числа рук не трогать — это `t-scale-6`.
  Два шва — `t-scale-7`.
- Дом, обстановку, хозяина, облик и пороги шума не трогать. Общей настройки масштаба не заводить.
- Ящики для пробы примитивные и одноразовые: дом здесь не строится.
- Инструмент недоступен — СТОП и вопрос владельцу, никогда обходной путь.

## done_when

Наряд написан, лежит в `work/`, зарегистрирован карточкой `call` и годен к отправке: слот и база
перемерены первой рукой на момент написания, `engineering_contract` пин проставлен, у каждой строки
`done_when` проверено происхождение, и в возврате прямо сказано, что продуктовый handback задачу
Направления не закрывает — закрывает его слово о способе подъёма.

## return

RESULT ноги `work` с зарегистрированным нарядом и продолжением той же волны.

## budget

Одна нога. Слот на момент этого брифа: WIN-U1 `AVAILABLE`, `lease: none`, `slot/win-u1` =
`11f8ddbf` = `main`, ничего чужого впереди. Продуктовый стамп контракта 36 = текущий 36, дрейфа нет.
Всё это перемеряется заново на момент написания наряда.

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-vert-1-call-001.md
