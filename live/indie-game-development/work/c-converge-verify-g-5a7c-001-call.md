# CALL — c-converge-verify-g-5a7c-001

to: session
direction: indie-game-development
play: converge-verify
node: g-5a7c
issued: 2026-08-07 by s-converge-arch-g-5a7c-001
amended: 2026-08-07 by s-converge-arch-g-5a7c-002 — единственная открытая строка набора закрыта
         вердиктом владельца; счёт и список атак приведены к новому состоянию
status: ready

## goal

Свежая, ОТДЕЛЬНАЯ от работы сессия судит набор `g-5a7c` заново и пытается его СЛОМАТЬ. Судится набор
целиком: `work/converge-g-5a7c.md` (§IMPORT, §GLOSSARY, §WHAT, §DRIFT, оба §SIGNOFF и добавленный
§CONTRACTS) плюс `work/converge-g-5a7c-arch.md` (§RISK-LINE, §DECOMPOSE, A1–A9, §MINER, §NOT-DECIDED,
§SIGNOFF). Вердикт — `verify_target: build`, `complete` и `smuggling` по набору целиком.

## context

**Это ПЕРВАЯ проверка узла.** `converge-verify`-RESULT-ов по `g-5a7c` в `history/` нет ни одного;
потолок второго FAIL не действует и заранее не применяется.

**Кто что написал, чтобы проверка не судила чужой счёт как свой.** `work/converge-g-5a7c.md`
§IMPORT/§GLOSSARY/§WHAT/§DRIFT и оба его §SIGNOFF написаны ногой `s-converge-g-5a7c-real-core-001`;
триаж `heavy`, `converge ON`. §CONTRACTS того же файла и весь арх-файл написаны ногой
`s-converge-arch-g-5a7c-001` (эта нога), и она объявила, что §WHAT не тронула ни строкой — **это
утверждение подлежит механической проверке, а не приёму на слово.**

**Карточка правлена ТРИЖДЫ сегодня, и читать её надо в текущем виде.** `TREE.md` `g-5a7c:117-142`,
статус `parked`, двенадцать критериев. Правки подписаны тремя отдельными словами владельца
(`1 принимаю`, `2 принимаю`, `3 А`) — разбор в `history/2026-08-07-s-map-g-5a7c-ui-line-removal-001.md`.
Прежние пересказы карточки в старых файлах устарели.

**Счёт, который надо пересчитать МЕХАНИЧЕСКИ и сверить с обоими файлами:**

- §WHAT: 113 строк решений, `open_rows: 1` (`W103`), `deferred: 2` (`W15`, `W16`);
- §CONTRACTS: 12 краёв к узлам дерева + **14** внутренних швов (`C26` — состояние двери — добавлен
  2026-08-07 по его же уточнению; прежнее число было 13); `contracts_open: 1` (C05 → map). **C25
  закрыт вердиктом владельца 2026-08-07** — прежнее число было 2;
- арх-файл: `arch_open: 8` — A1–A8 с рекомендованным ответом и обязывающим ADR у PLAN. **A9 закрыт
  тем же вердиктом** — прежнее число было 9; §MINER 7 строк; **вердиктов владельца в наборе ровно
  один, подписей под текстом карточки — ноль.**

**Что эта нога утверждает про себя и что надо атаковать в первую очередь:**

1. **`W103` объявлена закрытой не решением ноги, а собранным маршрутом** — три части источника лежат
   в `TREE.md` `g-8a41` кр. 3, `os/KERNEL.md:63` и `NOW.md:102-107`. Ответ живёт в §CONTRACTS C07;
   строка §WHAT намеренно НЕ правлена. Проверить: закрывает ли этот маршрут вопрос на самом деле, и
   не является ли «строка в §WHAT говорит `open → PLAN`, а C07 говорит другое» расхождением двух
   счётов внутри одного набора — ровно тот класс, который уже стоил `g-1d84` находки F5.
2. **Восемь архитектурных выводов (A1–A8) объявлены выводами ПО ОПРОВЕРЖЕНИЮ, а не решениями ноги.**
   Каждый обязан отпадать на измеренном факте кода, на подписанном тексте карточки или на его ранее
   сказанных словах. Проверить построчно: где опровержение на самом деле держится на рассуждении
   ноги, лекарство — ПОНИЖЕНИЕ в `open` с названным отвечающим, а не свежий ответ.
3. **Утверждается, что владельцу вынесен ровно ОДИН вопрос (A9), и что остальные восемь — вопросы
   реализации.** Проверить обратное: нет ли среди восьми ещё одного вопроса ГЕЙМПЛЕЯ, который нога
   решила за него. G7 прямо запрещает изобретать концепт и геймплей, как бы уверенно нога себя ни
   чувствовала.

3a. **Единственный вердикт владельца в наборе — проверить его силу отдельно и придирчиво.** Его слова
   2026-08-07: **«А, обстановка стоит»**, даны на показанный целиком разбор A9 с обоими вариантами,
   ценой каждого, рекомендацией и доводом ПРОТИВ рекомендации. Набор записывает это как вердикт по
   ВАРИАНТУ, усиленный его собственной формулировкой, и прямо отрицает, что это подпись под текстом
   карточки. Проверить: (i) не повышена ли сила токена нигде в трёх местах, где он теперь стоит
   (арх A9, §CONTRACTS C25, §SIGNOFF обоих файлов); (ii) не выведено ли из него больше, чем он
   сказал — в частности, набор утверждает, что подвижная обстановка НЕ запрещена и её возвращение
   остаётся его отдельным словом; (iii) не превратилась ли в его слово оговорка про «несущие
   задевают», которая объявлена ПРОЧТЕНИЕМ ноги по тексту критерия 5, а не его ответом.

3b. **Его уточнение того же дня про три класса — проверить, что оно записано ВХОДОМ, а не
   требованием.** Дословно: «я сейчас не могу точно сказать, что будет, но я пока предполагаю, что
   всё будет условно без физики, да, прибито, там, тумбочки, столы какие-нибудь. Но будут физические
   объекты, например, ну, двери, да, должны как-то же закрываться. Возможно, и не будут, но там,
   например, большие те, которые можно унести, они же точно физические будут. Предметы интерьера, я
   думаю, что нет.» Набор утверждает три вещи, и каждую надо атаковать отдельно: (i) обстановка —
   подтверждение варианта А его же словами, поимённо; (ii) «переносимое крупное» — НЕ новый класс, а
   ровно груз, и довод стоит на измеренном факте (единственный `AddComponent<Rigidbody>` в проекте
   плюс ростер и `CargoHaul.Reduce`, написанные на список любой длины) — проверить этот факт первой
   рукой; (iii) двери — новый шов `C26`, где состояние двери объявлено куплённым критерием 6
   независимо от физики, а физическая дверь названа отдельным и НЕ решённым вопросом. Проверить
   главное: не превратилась ли где-нибудь его двойная оговорка («не могу точно сказать»,
   «возможно, и не будут», «я думаю, что нет») в требование или в `done_when`.
4. **Утверждается, что `W15` и `W16` не тронуты** и остаются отложенными его словами. Проверить
   дословно.
5. **Утверждается, что карточка `g-5a7c` не тронута ни знаком.** Проверить диффом.

**Объявленная база продуктовых фактов:** `C:/projects/Unity/GasCoopGame`, `main` = `352f96b0`,
рабочее дерево чисто, только чтение. Перепроверить базу первой рукой ДО любого `факт кода`. Числа,
на которых стоит разбор и которые дешевле всего опровергнуть: `_tickRate = 30` по умолчанию у FishNet
и отсутствие переопределения в `NetworkRuntime.prefab`; `_physicsStepsPerTick: 2`; `Fixed Timestep:
0.02`; `CargoBodyContact.cs:52-58`; `CarryStand.cs:66-78` против `HouseWall.cs:13-22`;
`CargoHold.cs:42-43`; `CargoHaul.cs:109-156`; `DeliveryPoint.cs:14,68`; `HouseRoom.cs:77-80`;
`Core/TunnelCrew.Core.asmdef`.

**Одно ограничение этой ноги, названное ею самой и подлежащее учёту при чтении:** веерного
опровержения внутри сессии НЕ было — в этом харнессе действует прямая постоянная инструкция владельца
не разворачивать субагентов и workflow, и по авторитету `os/KERNEL.md` живая инструкция владельца выше
плея. Значит эта проверка — ПЕРВОЕ независимое опровержение набора, а не второе.

## boundaries

- Проверка НИЧЕГО не отвечает и НИЧЕГО не чинит. Находка возвращается строкой, а не заполняется.
- Владельцу не выносить ни одного НОВОГО решения. Ждущих его вопросов в наборе больше нет: он ответил
  на единственный 2026-08-07. Прежнее решение `d-furniture-moves-or-stands-001` снято из `NOW.md`
  как отвеченное — не воскрешать и не переформулировать.
- `TREE.md` и `CHARTER.md` не трогать. Ставку, задачи и полосы не создавать. `knowledge/` не писать.
- Продуктовый репозиторий только на чтение.
- Свежая физическая сессия, отдельная от `s-converge-arch-g-5a7c-001` и от любого day-чата.

## done_when

1. Вынесен `verify_target: build complete=<PASS|FAIL> smuggling=<PASS|FAIL>` по набору целиком, с
   построчным обоснованием.
2. Каждая из двенадцати клауз `done_when` карточки разбита атомарно и сопоставлена со строкой набора —
   §WHAT, §CONTRACTS или A-строкой. Клауза, не дошедшая ни до одной строки, объявляется FAIL и НЕ
   заполняется. Названная `open` строка свою клаузу покрывает.
3. Назван хотя бы один предмет `done_when` или уставная линза, о котором набор не спрашивает ни разу.
   Не назвать ни одного — это и есть находка.
4. Каждая несущая строка `acceptance` §WHAT и каждый вывод A1–A8 проверены на источник ВНЕ решающей
   ноги. Строка, чей единственный источник — рассуждение самой ноги, называется такой и понижается в
   `open` с названным отвечающим.
5. Сила каждого токена владельца проверена на «не выше настоящей» — включая три сегодняшних вердикта
   по карточке, «Б Ну да, надо, чтобы осталось», «принимаю» на карточку, «возьми тогда сам» и
   сегодняшнее «А, обстановка стоит».
6. Два счёта набора сведены механически: §WHAT, §SIGNOFF converge-ноги, §CONTRACTS и §SIGNOFF
   арх-файла обязаны говорить одно и то же.
7. Утечка HOW проверена: величина, формат, частота или раскладка, заехавшие в `acceptance`,
   переносятся `→ PLAN`; ни одна строка арх-файла не оказалась требованием.

## return

Полный Direction RESULT. При PASS — `§SIGNOFF: converge-verify passed @ <date>` и `next` = `shape`,
с готовыми к копированию `acceptance`-строками, §CONTRACTS и повесткой PLAN. При FAIL — именованные
строки возвращаются в `converge`/`converge-arch`, ничего не чинится на месте.

budget: одна свежая сессия

END_OF_FILE: live/indie-game-development/work/c-converge-verify-g-5a7c-001-call.md
