# CALL — c-work-g-5a7c-handlers-1-dispatch-001

to: session
direction: indie-game-development
track: householder-life
play: work
node: g-5a7c
task: t-handlers-1
issued: 2026-08-13 by s-work-g-5a7c-sight-1-binding-g5-001
status: ready

## goal

Дорожка `householder-life` продолжена: `t-handlers-1` либо выдана исполнителю готовым нарядом, либо
названа одним точным блокером, который мешает её выдать.

## context

**Основания ниже перемерены 2026-08-13 биндинговой проверкой зрения. Перемерь их сам: они не
авторитет, они отправная точка.**

- `t-sight-1` закрыта этой же датой; `t-place-fact-1` и `t-thing-1` закрыты раньше. Это ВСЕ три
  зависимости, названные в `cards/t-handlers-1.md` §`note`.
- Карточка при этом стоит `status: blocked` и **не несёт `unblock_when`**. Расхождение состояния
  разрешается здесь, а не обходится.
- Продуктовая голова на момент выпуска: `origin/main` = `origin/dev` =
  `c485b30e704b1706675dd92d15c5223b0d166b92`. Все четыре слота `AVAILABLE`, `lease none`.
- Параллельно в `house-and-things` уже лежит готовая `c-exec-g-5a7c-loot-foundation-001` от той же
  головы. Пересечение по файлам обязано остаться нулевым.

**Что закрытая `t-sight-1` привезла и на что эта задача опирается:** пятый вид ситуации `SawRat` с
рангом `2.5`, факт `SeenRat` со своим временем жизни из ассета, шестая реакция `seen-rat` и ответ
`TrackVisibleThenSearch`. Строка 4 карточки требует хотя бы одну комбинацию на комнате и хотя бы одну
на факте зрения — обе опоры теперь существуют.

**Улика, чей `review_when` называет именно эту задачу:**
`i-householder-chase-path-jitters-in-doorways-001` — хозяин дёргается, когда бежит на увиденное.
Она требует СНАЧАЛА измерить, а не чинить по гипотезе, и её гипотеза (перевывод маршрута каждый тик)
принадлежит исполнителю зрения, а не измерению. Входит она райдером или не входит — решается здесь,
и решение называется вслух.

**Райдер, уже назначенный ставкой:** `i-dragged-cargo-rustles-every-tick-001` — величина МЕРЯЕТСЯ
прогоном, пороги `60 / 150 / 400` при этом не двигаются (строка 7 карточки).

**Гейт расширения обязателен и обязан занять одну из трёх строк `done_when` наряда** —
`knowledge/core-is-extended-never-rewritten.md` §ГЕЙТ ВТОРОГО ВИДА. Второй вид называется здесь, а не
отдаётся исполнителю на выдумывание.

## boundaries

- Закрытие `t-handlers-1` объявлено в её карточке: светлое НЕДОСТУПНО, строку 5 судит его глаз в
  собранной игре. Наряд обязан оставлять эту возможность, а не заменять её тестом.
- Файлы `c-exec-g-5a7c-loot-foundation-001` этой дорожке запрещены.
- Пороги шума `60 / 150 / 400` не двигаются. Правило «один самый громкий за тик» не трогается.
- Объём подписанной ставки не расширяется: одиннадцать задач, ноль новых слоёв.
- Плита и непрерываемое занятие не поднимаются: владелец снял вопрос 2026-08-13 своим словом, обе
  карточки закрыты и лежат в `cards/closed/`.
- Улика про дрожь не редактируется этой ногой без её собственного измерения.

## done_when

1. Состояние `t-handlers-1` приведено в соответствие измеренным зависимостям, и основание названо
   поимённо по каждой из трёх.
2. Либо наряд исполнителю выпущен целиком — с гейтом второго вида в одной из трёх строк `done_when`,
   с названным вторым видом и с явным решением про райдеры (дрожь, волочение), — либо назван один
   точный блокер, мешающий его выпустить.
3. Сказано вслух, что делает эта задача с дрожью: измеряет, чинит или не касается, — и почему.
4. Ни одна чужая дорожка, задача, улика и ветка не тронуты.

## return

Полный RESULT по `work`: измеренные зависимости, состояние карточки, выпущенный наряд или названный
блокер, вердикт по дрожи, state delta и ровно один локальный handoff.

budget: одна сессия

END_OF_FILE: live/indie-game-development/work/c-work-g-5a7c-handlers-1-dispatch-001-call.md
