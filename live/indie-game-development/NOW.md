# NOW — indie-game-development

active_bet: none

active_tasks: []

recurring: []

open_calls: []

decision_inbox: []

next: |
  CALL c-ev-001
  to: session
  direction: indie-game-development
  play: research
  node: g-a7f2
  goal: |
    Map-grade external evidence for the indie-game-development direction exists in state: the current-year default path with numbers, how comparable ambitions actually reached this charter's success criteria, and the strongest argument against the default — every item with dated sources, ready to ground the first goal map.
  context: |
    Brief: map_evidence — os/plays/research.md, Notes.
    Read:
    - live/indie-game-development/CHARTER.md (success criteria incl. six-month money gate 2026-12-11, lenses, owner edges, risk posture)
    - live/indie-game-development/TREE.md (root g-a7f2 only, no children yet)
    The deep-research run happens on a raw tool outside OS rules: render the brief as ONE self-contained plain-language prompt; the returned report is evidence — this session digests it and writes the RESULT.
  boundaries: |
    Inform only — no tree nodes, no strategy choice, no tasks; paths are generated later in the map session.
    Archive material is evidence-only.
  done_when: |
    RESULT.captures cover, each item with dated sources:
    - the current-year baseline with numbers;
    - comparable-ambition cases split by branch class (money/audience/distribution vs tech/gameplay), rare verified cases favored over genre hits;
    - the strongest argument against the baseline.
  return: |
    RESULT with captures, sources, confidence and limits, log line, next = map CALL on g-a7f2 carrying the captures.
  budget: one session plus one deep-research run

END_OF_FILE: live/indie-game-development/NOW.md
