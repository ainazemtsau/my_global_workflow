---
id: t-thing-belief-1
_kind: task
_bet: bet-g-5a7c-wave-8
track: ядро
status: done
_pos: 152
goal: Хозяин знает, как в его доме заведено, ошибается на этот счёт, и поправляют
  его только его глаза
---

## done_when
**КАРТОЧКА ЗАВЕДЕНА ЗАДНИМ ЧИСЛОМ 2026-08-30 ПЛЕЕМ `repair`.** Наряда в направлении не было ни
одного: работа приехала в продукт, не имея ни задачи, ни наряда, ни строки в истории. Поэтому
`done_when` ниже — **РЕКОНСТРУКЦИЯ ПО ПОСТАВЛЕННЫМ БАЙТАМ**, а не заказ, подписанный до постройки.
Ни одну строку отсюда нельзя предъявлять как согласованное заранее требование.

Перемерено первой рукой на `main = dev = f6fc0918`.

1. **ВСЕЗНАНИЕ УБИТО.** До ноги мир каждый тик сообщал хозяину комнату КАЖДОГО предмета в доме —
   вещь, спрятанную в комнате, где он ни разу не был, авторская строка спрашивала и получала
   правду. Теперь всё идёт через тот же глаз, что отвечает про крысу.
   Проверено: `Network/NetworkHouseholder.cs:254` — `SeeWhatIsLyingAbout(..., IHouseholderThingSightSource sight)`,
   и при `sight == null` отчёт пуст.
2. **РАССТАНОВКА — ЗАПАСНОЙ ОТВЕТ, ЧИТАЕТСЯ СО СЦЕНЫ.** Не засев и не вторая копия описания дома.
   Проверено: `NetworkHouseholder.cs:389` — `_arrangement = new HouseArrangement(exact)` из
   `ReadTheArrangement(conditions)`.
3. **УБЕЖДЕНИЕ — ЧЕТВЁРТОЕ ПОЛЕ `HouseholderStanding`, ПИШЕТ ТОЛЬКО ТИК.**
   Проверено: `Core/Householder/HouseholderStanding.cs:43-53` — конструктор на 4 аргумента,
   `Beliefs`; единственная запись — `Householder.cs:64-69`, `standingBefore.Noticing(...)` из
   наблюдений на входе. Ни правило, ни эффект туда не дотягиваются.
4. **АВТОРИТСЯ В JSON СТРОКОЙ, НЕ НАЗЫВАЮЩЕЙ ПРЕДМЕТ.**
   Проверено: `Profiles/HouseholderProfileLoader.cs:2325` — `case "thing-belief"`; условия
   `{"thing":…,"is":…}`, `{"thing":…,"believedIn":"room:…"}`, `{"anyThing":"missing","countAtLeast":N}`.
5. **ГРАНИЦА ЯДРА НЕ СЛОМАНА.** `grep -ri cargo Assets/TunnelCrew/Core/Householder/` → **0 строк,
   0 файлов** на `f6fc0918`.
6. **ТЕСТЫ ЗЕЛЁНЫЕ.** `dotnet test tests/TunnelCrew.Core.Tests -c Release` на `f6fc0918` →
   **694 пройдено, 0 не пройдено**. Прогон направления, не заявление исполнителя.

**ЧТО НОГА ВЗЯЛА СВЕРХ ЛЮБОГО МЫСЛИМОГО НАРЯДА** и почему это записано здесь, а не потеряно:
починка всезнания хозяина не заказывалась — предыдущая нога (`t-thing-missing-1`) его и создала,
а эта заметила и закрыла. Плюс структурная правка: пять мест пересобирали `HouseholderStanding`
руками, теперь все идут через один приватный копирующий конструктор.

**ЧТО НЕ ПРОВЕРЕНО И ОСТАЛОСЬ УЛИКОЙ:** видит ли луч ЛЕЖАЩУЮ вещь —
`i-a-ray-may-not-see-a-thing-lying-down-001`.
## журнал
2026-08-30 · закрыта по факту: шесть строк реконструированного done_when перевыверены первой рукой на f6fc0918 — зрение фильтрует отчёт о вещах (NetworkHouseholder.cs:254), расстановка читается со сцены (:389), убеждение четвёртым полем standing пишет только тик (HouseholderStanding.cs:43-53, Householder.cs:64-69), thing-belief авторится в JSON (HouseholderProfileLoader.cs:2325), grep Cargo = 0, тесты 694/694. Непроверенным осталось одно — видит ли луч лежащую вещь: улика i-a-ray-may-not-see-a-thing-lying-down-001 · history/2026-08-30-s-repair-g-5a7c-three-legs-behind-001.md
2026-08-30 · записи догнали продукт: три ноги закрыты по факту с перевыводом done_when из байтов f6fc0918, две карточки заведены задним числом, замер ядра хозяина перемерен, два долга вынесены уликами · history/2026-08-30-s-repair-g-5a7c-three-legs-behind-001.md
END_OF_FILE: live/indie-game-development/cards/closed/t-thing-belief-1.md
