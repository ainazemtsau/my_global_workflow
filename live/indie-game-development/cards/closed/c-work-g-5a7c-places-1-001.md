---
id: c-work-g-5a7c-places-1-001
_kind: call
to: session
play: work
for: t-places-1
node: g-5a7c
status: done
issued: 2026-08-12
description_by: dev
description: 'Довести общую задачу-шов «роли мест» до закрытия: наряд, доставка с распиской, возврат'
_pos: 65
---

## goal
Задача `t-places-1` («роли мест») доведена до конца: наряд написан, ФИЗИЧЕСКИ ДОСТАВЛЕН
исполнителю с распиской, работа принята и задача закрыта по букве `evaluator` этой ставки.

Это ПЕРВАЯ задача третьей волны и единственная общая. Пока она идёт, дорожки `householder-life` и
`house-and-things` не заняты — его правило 2026-08-10: «одна задача блочит две полосы, то, ну,
естественно, ждем». Дорожка `body` ею НЕ блокируется: `t-body-1` не трогает ни правил, ни профиля
и может идти параллельно.

## context
Ставка `bet-g-5a7c-wave-3` открыта 2026-08-12 его словом «Подписываю, открывай ставку».
Читать: `cards/t-places-1.md` (семь строк `done_when` и `note`), `cards/bet-g-5a7c-wave-3.md`
блоки `approach`, `evaluator`, `tracks`, `riskiest_assumptions`.

**ПРОДУКТ ПЕРЕМЕРЕН ЭТОЙ НОГОЙ 2026-08-12 на опубликованной голове `f3c40c40` — брать входом:**
`origin/main` = `origin/dev` = `origin/slot/win-u2` = `f3c40c4010ee2f0f700cdf1bfbcea14a301d488c`;
`origin/slot/win-u1` = `43648b2dd4fbaaf9570bd56cdf440cc904e1c222` (слот отстаёт, назвать в наряде).

- `Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs:65-69` — `IsKnownPlace` возвращает
  `false` с комментарием «Deliberately empty», `:740` отвергает любой шаг с именем места. Следствие
  практическое: профиль, назвавший ЛЮБОЕ место, не запускает хозяина вовсе — порт хоста остаётся
  нечитаемым, и авторитетный Core его не сажает.
- `Core/House/HousePlan.cs` УЖЕ существует и несёт комнаты, места и проходы с поиском по имени и по
  точке. `World/SceneHousePlan.cs` читает их с authored-маркеров и жалуется на недоделанную
  разметку. `Network/NetworkHouseholder.cs:80,102` УЖЕ спрашивает план один раз до движения.
- Сегодняшний дом `World/ThreeRoomHouse.prefab`: три комнаты (West / Middle / East), семь мест
  (Bedside, West Window, West Door Side, Hall Centre, Work Table, Store Corner, East Door Side),
  два проёма шириной `2`.
- Зарегистрированных умений ровно два: `walk-route` и `wait-at-route-point`, оба игнорируют место.
  Формат профиля описан в `docs/householder-profile-format.md`.

**БУКВА `evaluator` ЭТОЙ ВОЛНЫ** перенесена из второй без изменений: публикация исполнителем в
`main` и закрытие ПОСЛЕ — законный порядок; закрытие идёт светлым путём либо свежей биндинговой
проверкой в другом чате; **наряд не существует, пока исполнитель не расписался** — готовая паста с
ПОЛНЫМ текстом и первая строка возврата с числом полученных строк `done_when`; зелёные ворота не
предъявляются как доказательство поведения игры; базис протух — перебазироваться, а не
останавливаться.

## boundaries
- **Задача не расширяется.** Семь строк `done_when` — весь объём.
- **`IntegratedHouse.unity` и префабы дома НЕ ОТКРЫВАЮТСЯ** — они принадлежат дорожке
  `house-and-things` и задаче `t-rooms-1`.
- **Занятия внутри комнаты здесь НЕ делаются** — это `t-occupations-1`. Эта задача даёт словарь, а
  не действия.
- **Зрение, состояние предмета, новые виды события — не здесь.**
- **Поведение по умолчанию не меняется ни на грамм:** опубликованный профиль ролей не называет.
- **НОВОЕ ПРАВИЛО ЭТОЙ ВОЛНЫ:** наряд не содержит ни одного инструмента, продукта или механизма,
  которого нет в словах владельца. Полезное предложение ноги выносится ОТДЕЛЬНОЙ строкой с пометкой
  «это предложение ноги», а не вписывается в объём.
- Наряд исполнителю пишет эта нога и ОБЯЗАНА доставить его полный текст с распиской.

## done_when
1. Наряд на `t-places-1` написан по семи строкам `done_when` карточки, ни одна не потеряна и ни
   одна не дописана.
2. Наряд ФИЗИЧЕСКИ ДОСТАВЛЕН: готовая паста с полным текстом отдана исполнителю, первая строка
   возврата — расписка с числом полученных строк.
3. Работа вернулась, задача закрыта по букве `evaluator`, и нога называет вслух, каким из двух
   путей шла.
4. **ГЛАВНЫЙ ГЕЙТ ПЕРЕМЕРЕН ПЕРВОЙ РУКОЙ:** один и тот же файл профиля работает в ДВУХ разных
   описаниях дома с разными точками и одинаковыми ролями.
5. `t-places-1` в статусе `done`; дорожки `householder-life` и `house-and-things` разблокированы.

## return
RESULT ноги `work` с закрытой `t-places-1` и распиской исполнителя в возврате.

Дальше открываются обе оставшиеся дорожки: `t-sight-1` в `householder-life` (рискованное допущение
№1 волны, несёт свой откат и требует его глаза) и `t-rooms-1` в `house-and-things`. Дорожка `body`
к этому моменту уже может идти или быть закрытой.

## budget
Ноги `work` по обычному счёту: наряд, выдача, возврат. Порог смерти 3 этой ставки считает ноги — на
общую задачу и первые задачи всех трёх дорожек их положено не больше двенадцати, иначе первой на
паузу встаёт `body`, следом `t-loot-1`. Половина дня работы исполнителя.

## журнал
2026-08-12 · закрыта СВЕТЛЫМ путём на опубликованной голове 4e5858b4: все семь строк перемерены этой ногой по байтам и прогону — откат СОШЁЛСЯ ЗНАК В ЗНАК (дерево revert 5ec74916 = дерево базы f3c40c40), 219/219 прогнаны здесь против базовых 214, пять реакций целы диффом в НОЛЬ строк, игровой профиль и все сцены с префабами не тронуты вовсе, а IsKnownPlace заменён на TryResolvePlaceRole, читающий словарь из HousePlan · history/2026-08-12-s-work-g-5a7c-places-1-close-001.md
2026-08-12 · наряд написан и выдан исполнителю в WIN-U2 как c-exec-g-5a7c-places-1-001; ждём возврата с распиской · history/2026-08-12-s-work-g-5a7c-places-1-dispatch-001.md
2026-08-12 · наряд зарегистрирован shape третьей волны и не запущен · history/2026-08-12-s-shape-g-5a7c-wave-3-001.md
END_OF_FILE: live/indie-game-development/cards/closed/c-work-g-5a7c-places-1-001.md
