---
id: t-world-port-1
_kind: task
_bet: g-5a7c
track: провод
status: done
goal: Шов хозяина к миру перестаёт быть односторонним — его правила могут что-то сделать
unblock_when: t-reactions-data-1 закрыта — новое слово словаря обязано родиться в
  новом доме, а не в старом
_pos: 135
---

## done_when
1. **У ШВА ПОЯВЛЯЕТСЯ ВТОРОЕ НАПРАВЛЕНИЕ.** `IHouseholderWorldPort` сегодня умеет ровно `Read()`
   и `TakeHeardEvent()` (`AuthoritativeHouseholder.cs:89-98`) — это чистое чтение. Появляется
   способ его правилам ЧТО-ТО СДЕЛАТЬ, и он один, а не два разных на один вопрос.
2. **ОДНО НАСТОЯЩЕЕ ДЕЙСТВИЕ ПРОХОДИТ НАСКВОЗЬ.** Правило хозяина поднимает вещь и кладёт её в
   другое место, и цепь видна целиком: решение ядра → шов → мир → снимок → обе копии экрана.
3. **ЯДРО ОСТАЁТСЯ БЕЗ ДВИЖКА.** Ноль ссылок на `UnityEngine` в `Assets/TunnelCrew/Core` после
   правки — свойство измеряется, а не предполагается. 526 тестов зелёные.
4. **НОВОЕ СЛОВО РОЖДАЕТСЯ В ДАННЫХ, ГДЕ ЭТО ВОЗМОЖНО.** Реакция, которой действие пользуется,
   заводится строкой профиля, а не строкой C#. В C# добавляется только то, без чего действие
   некому исполнить — и это записывается в возврате явно.
5. **ЦЕЛЬ ДЕЙСТВИЯ — ВЕЩЬ ИЛИ МЕСТО, НАЗВАННЫЕ ПРЯМО.** Никакого поиска ближайшего объекта по
   координате: тот же запрет, что в `t-reach-wire-1`, и по той же причине.

## done_not_when
Задача НЕ строит: «хозяин замечает перемещённый лут и относит на место» как готовое поведение —
это следующая волна; двери и их состояние; ловлю мыши.

## note
**ЭТО САМОЕ ГЛУБОКОЕ ИЗ НАЙДЕННОГО.** Продуктовый обзор §6 формулирует так: это не отсутствующий
член перечисления, это **отсутствующее направление у шва**. Поднять, отнести, открыть, закрыть,
поскользнуться — всему этому сегодня не через что пройти.

**ПОЧЕМУ ЖДЁТ `t-reactions-data-1`.** Действие добавит слово в словарь. Если добавить его до
переезда, слово родится в C# и переедет вторым заходом — двойная работа, и обе полосы упрутся в
`HouseholderReaction.cs` одновременно.

**ЦЕННИК ИЗМЕРЕН:** на `HouseholderPhysicalActionName` ветвятся 7 файлов, 17 мест. Это честная
цена нового телодвижения, и она починкой словаря не снимается.

**ПОРЯДОК СМЕРТИ:** эта задача третья с конца в списке на вылет, если потолок в четыре дня
упрётся.

## журнал
2026-08-30 · перенесена в закрытые закрытием восьмой волны: статус done стоял с ШЕСТОЙ волны, а карточка всё это время лежала среди живых — работа давно в продуктовом main · history/2026-08-30-s-review-g-5a7c-wave-8-close-001.md
2026-08-28 · ПОПРАВКА К ПРЕДЫДУЩЕЙ СТРОКЕ ЖУРНАЛА, И ОШИБКА БЫЛА МОЯ. Писатель потребовал проверки второго экрана, сославшись на самое рискованное допущение волны — мину сериализатора FishNet. Допущение СНЯТО 2026-08-27 первой же задачей волны t-reach-wire-1 и снято посевом самой мины: написан ровно тот ручной сериализатор, которого боялся наряд, он молча привёз null, и на этом фоне доказано, что новое поле снимка доезжает. Строка стоит в журнале ставки с того же дня, и писатель её читал. Сверх того поступок НОВОГО ПОЛЯ СНИМКА НЕ ЗАВОДИТ ВОВСЕ: едет через существующий cargo-реестр и существующие снимки, то есть по пути, которым уже ездит груз игроков. Требование проверки снято как опирающееся на закрытый риск. ВЫРЕЗ ВТОРОГО ЭКРАНА ПРИНЯТ СЛОВАМИ ВЛАДЕЛЬЦА 2026-08-28 дословно: «мне показалось, как больше такая ресёрчная внутренней структуры, и если тесты работают, всё работает, то типа нормально» и «я не совсем понимал, что там проверять, поэтому сказал, чтобы он запушил». Теперь у принятия есть источник, которого не было у формулировки исполнителя
2026-08-28 · ДОСТАВЛЕНО И ОПУБЛИКОВАНО (42f499d3, main == dev), НО ЗАДАЧА НЕ ЗАКРЫТА — ОДНА СТРОКА ИЗ ПЯТИ БЕЗ УЛИКИ. Перемерено писателем первой рукой: UnityEngine в Assets/TunnelCrew/Core — ноль попаданий; dotnet test 564 из 564 зелёными (+13 к прежним 551); HouseholderDeed живёт в восьми файлах продукта с тестами на разрешение неоднозначных имён. Строка 3 закрыта, строки 4 и 5 закрыты (новое слово в данных, цель названа прямо, неоднозначность отказывает). Строка 1 исполнена по существу, но НЕ по букве: IHouseholderWorldPort не изменился вовсе, поступок уезжает полем Deed на HouseholderStepResult и исполняется адаптером — способ один, ядро осталось чистой функцией, расхождение записано намеренно. Строка 2 НЕ ЗАКРЫТА: наряд требовал цепь до ОБЕИХ копий экрана, исполнитель сохранил второй экран вырезом со словами принято при закрытии — кем принято не названо, слова владельца нет. Это класс улики i-executor-paraphrase-became-the-approval, и именно здесь он дорог: самое рискованное допущение волны — сериализатор FishNet, который проглатывает новое поле снимка МОЛЧА, а поступок едет через снимки, и одним экраном этот класс не ловится по построению
2026-08-27 · заведена по замеру, что шов хозяина к миру только на чтение; поставлена в ожидание переезда словаря, чтобы новое слово родилось сразу в данных · history/2026-08-27-s-shape-g-5a7c-wave-6-001.md

END_OF_FILE: live/indie-game-development/cards/closed/t-world-port-1.md
