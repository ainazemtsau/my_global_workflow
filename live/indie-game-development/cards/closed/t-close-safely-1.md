---
id: t-close-safely-1
_kind: task
_bet: g-5a7c
track: починка
status: active
_pos: 163
goal: dev двигается только на зелёном, замок берёт площадка, а сводка называет коммит
---

## done_when
**ЧЕТЫРЕ НАХОДКИ ОДНОГО ДНЯ, И ВСЕ ЧЕТЫРЕ СТОИЛИ ВОЛНЕ ПОЛДНЯ.** Задача взята девятой его словом
2026-08-31: «Девятая задача, окей, можем девятую взять». Ни одна из четырёх не выдумана ногой —
каждая замерена, три из них назвали сами исполнители.

1. **ЗАКРЫВАЛЬЩИК СЛИВАЕТ В `dev` РАНЬШЕ, ЧЕМ ЧТО-ЛИБО ПРОВЕРИТ.** `tools/close-leg.ps1` — шаг
   `2/7` «merge the candidate into dev» стоит ПЕРЕД шагом `3/7` «run the tests». Значит любое
   упавшее закрытие **навсегда оставляет свою работу в `dev`** с непройденными гейтами.
   **Цена, замеренная 2026-08-31:** десять коммитов трёх ног лежали в `dev`, ни один не пропущен
   через гейт; посевная мина чужой ноги держала публикацию другой в заложниках.
   Причина существования такого порядка законна: тесты надо гонять на РЕЗУЛЬТАТЕ слияния. Неверна
   не идея, а МЕСТО: сливают в общую ветку.

2. **ЗАМОК УВАЖАЕТ ТОЛЬКО ЗАКРЫВАЛЬЩИК, А ПЛОЩАДКА ОБЩАЯ.** `gascoop-integration.lock` берётся в
   `close-leg.ps1:228` и честно сериализует закрытия. Но гейт гоняется в общем чекауте `testrun`,
   и **любой ручной прогон `unity-tests.ps1` заходит туда мимо замка**, переставляя чекаут под
   чужим закрытием. Так 2026-08-31 было испорчено доказательство ноги прибора.

3. **СВОДКА ПРОГОНА НЕ ЗАПИСЫВАЕТ, ЧТО ИМЕННО ПРОВЕРЯЛА.** В `tools/unity-tests.ps1` слов `commit`,
   `sha`, `rev-parse` — **ноль**. Поэтому испорченное доказательство выглядит как нормальное, и
   отличить их после факта нечем.

4. **ДЛЯ N НОГ НА N СЛОТАХ ШТАТНОГО ПУТИ ЗАКРЫТИЯ НЕТ.** Назвал исполнитель WIN-U1 2026-08-31:
   `-Deliver` проверяет ВСЕ отчёты, изменившиеся относительно `main`, а `-AlsoDeliver` требует,
   чтобы чужие отчёты лежали НА КАНДИДАТЕ. Когда три слота уже слиты в `dev` и ни один не
   опубликован, закрыться нельзя ни из одного: без `-AlsoDeliver` краснеет `-Deliver`, с ним падает
   префлайт. Он развязал слиянием `dev` в свою ветку — **это обход формы, а не её решение**.


1. **`dev` ДВИГАЕТСЯ ТОЛЬКО НА ЗЕЛЁНОМ.** Слияние кандидата происходит НЕ в `dev`, а во временную
   ссылку; гейты гоняются на ней; `dev` продвигается последним действием и только при зелёном.
   **Доказано посевом мины:** упавшее закрытие обязано оставить `dev` ровно там, где он был.
   Замер до правки назван в возврате: сегодняшний порядок шагов `2/7` и `3/7`.

2. **В ТИПИЧНОМ СЛУЧАЕ НИЧЕГО НЕ СТРОИТСЯ.** Слияния здесь почти всегда fast-forward, и тогда
   «временная ссылка» — это сам коммит-кандидат. Решение обязано это использовать, а не создавать
   лишнюю ветку ради формы. Если оказалось иначе — назвать почему.

3. **ЗАМОК БЕРЁТ ПЛОЩАДКА, А НЕ ПРОЦЕДУРА.** Кто входит в `testrun` — тот берёт
   `gascoop-integration.lock`: и закрывальщик, и ручной прогон. Отдельный `testrun` на слот НЕ
   строить: `Library` весит 7–8 ГБ, четыре копии это тридцать гигабайт ради того, что решается
   замком.

4. **СВОДКА ПРОГОНА НАЗЫВАЕТ КОММИТ И ЧЕКАУТ.** `unity-tests.ps1` записывает в свою сводку SHA
   дерева под тестом и путь чекаута. Самая дешёвая строка задачи и самая ценная как улика: без неё
   любое доказательство, снятое в общем месте, непроверяемо.

5. **У N НОГ НА N СЛОТАХ ЕСТЬ ШТАТНЫЙ ПУТЬ ЗАКРЫТИЯ.** Либо `-AlsoDeliver` начинает находить чужие
   отчёты там, где они реально лежат, либо форма закрытия называется иначе — но **обход слиянием
   `dev` в слот перестаёт быть единственным способом**. Свойство «ничего не уезжает молча, каждая
   нога НАЗВАНА» сохраняется.

6. **ФОКУСНЫЙ САМОТЕСТ ЗАКРЫВАЛЬЩИКА ЗЕЛЁНЫЙ** — `tools/close-leg.selftest.ps1`, обязателен после
   любой правки закрывальщика.

7. **ВРЕМЯ ЗАКРЫТИЯ НАЗВАНО ЧИСЛОМ ДО И ПОСЛЕ.**

8. **ТЕСТЫ ЗЕЛЁНЫЕ.** 725 headless на `56a7b88a` плюс Unity-набор.

**ЧЕГО НЕ ВХОДИТ:** защита общего склада арта — это `i-shared-art-store-has-no-guard-001`, отдельный
предмет и отдельное решение владельца про политику хранения. Любые правки в `Assets/**`.

9. **ЗАКРЫВАЛЬЩИК НЕ ШТАМПУЕТ `DELIVERED` ПОВЕРХ ОТЧЁТА, КОТОРЫЙ САМ ГОВОРИТ «BLOCKED».**
   Замерено 2026-08-31: `git show 56a7b88a -- docs/results/c-exec-g-5a7c-gate-headless-1-001.md`
   показывает единственную содержательную правку — `Status: NOT INTEGRATED` заменён на
   `DELIVERED on dev`, при том что тело того же отчёта несёт «Required red-headless evidence is
   BLOCKED» и переносит работу в `## next`. Гейт пометил доставленным то, что сам отчёт объявил
   недоделанным. Либо закрывальщик читает такое признание и отказывается, либо он не трогает поле
   `Status` вовсе — **выбор назови с причиной**, это решение о форме, а не косметика.

10. **СУФФИКС ЛИЗЫ СРАВНИВАЕТСЯ БЕЗ ОГЛЯДКИ НА РЕГИСТР, ЛИБО ОДНА СТОРОНА ПРИВОДИТСЯ К ДРУГОЙ.**
    Замерено ногой закрепления 2026-08-31: штатный claim записывает лизу с суффиксом `:BUILD`, а
    `close-leg` по умолчанию ждёт `:build`. Первый dry-run отказался «без записей», и исполнителю
    пришлось звать закрывальщик с явным `-LeaseId`. Отказ по регистру буквы — не защита, а шум:
    он тратит попытку и учит обходить проверку флагом вместо того, чтобы ей верить.
## журнал
2026-08-31 · Все десять строк приёмки перевыведены направлением первой рукой на опубликованном main = b22c00a2: reflog dev несёт одну запись 11:44:39 за весь пробег закрытия 11:39-11:44; интеграционный коммит 80de396d имеет двух родителей, то есть предпосылка карточки про fast-forward опровергнута замером и закрыта её же оговоркой; самотест закрывальщика и headless-набор ЗАПУЩЕНЫ САМИМ НАПРАВЛЕНИЕМ (CLOSE-LEG SELFTEST: OK, 725/725); EditMode 220/220 прочитан из машинного артефакта editmode-summary.json, называющего проверенный коммит; Resolve-SlotLease сличает имя с учётом регистра, стадию без и возвращает записанное написание. Под Assets ноль байт, самотест 255 вставок и ноль удалений. Лёгкое закрытие, G5-чат не открывался.
2026-08-31 · девятая задача волны закрыта лёгким закрытием: dev теперь двигается последним и это доказано reflog, а не рассказом; самотест и headless прогнаны самим направлением, EditMode прочитан из артефакта, называющего проверенный коммит; предпосылка карточки про fast-forward опровергнута — ошибка направления; два остатка разнесены уликами · history/2026-08-31-s-work-g-5a7c-close-safely-close-001.md
2026-08-31 · отчёт безопасного закрытия перевыверен по байтам: dev действительно двигается шестым шагом и reflog это подтверждает чужим контролем; но строка 10 про регистр лизы не сделана и не названа, а dev прямо сейчас несёт неотданный отчёт U2 — правка AGENTS.md разрешена этой же ногой · history/2026-08-31-s-work-g-5a7c-close-safely-scope-001.md
2026-08-31 · первый перенос в закреплённое закрыт по байтам — четыре rename и folder-meta, ноль строк C#, граница цела; три из пяти его находок разнесены по адресам · history/2026-08-31-s-repair-g-5a7c-bedrock-close-001.md
2026-08-31 · три ноги перевыверены по байтам: хук прибытия закрыт чисто, прибор с тремя оговорками, а гейт закрывать нельзя — он не доказал собственную мину, и это признаёт его же отчёт в main · history/2026-08-31-s-repair-g-5a7c-three-closes-001.md
2026-08-31 · нога зрения выдана на базе, которой не существовало до сегодня — прибор напечатал 0 из 14; и нарезана девятая задача из четырёх инфраструктурных находок дня · history/2026-08-31-s-work-g-5a7c-sight-dispatch-001.md
END_OF_FILE: live/indie-game-development/cards/closed/t-close-safely-1.md
