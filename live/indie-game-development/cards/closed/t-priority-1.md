---
id: t-priority-1
_kind: task
_bet: bet-g-5a7c-wave-8
track: ядро
status: done
goal: Реакция выбирается по важности, а не по порядку в списке
_pos: 146
---

## done_when
1. **РЕАКЦИЯ ВЫБИРАЕТСЯ ПО ВАЖНОСТИ, А НЕ ПО ПОРЯДКУ В СПИСКЕ.** Сегодня
   `Core/Householder/HouseholderDecision.cs:552-562` идёт циклом ПО ИНДЕКСУ и возвращает первое
   совпадение; слова «приоритет» в этом цикле нет ни разу.
2. **СТРОКА, ДОПИСАННАЯ В ПРОФИЛЬ, МОЖЕТ ВЫИГРАТЬ У ВСТРОЕННОЙ.** Сегодня профиль дописывает
   строки В КОНЕЦ, значит любая новая проигрывает всем существующим — **беззвучно, без ошибки и
   без строки в логе**. Доказать тестом: строка профиля с большей важностью побеждает встроенную.
3. **УМОЛЧАНИЕ НЕ МЕНЯЕТ СЕГОДНЯШНЕЕ ПОВЕДЕНИЕ.** Профиль без явной важности ведёт себя как
   раньше, бит в бит. Это и есть приёмка: **в игре ничего не изменилось.**
4. **КАНДИДАТЫ ВЫХОДЯТ НАРУЖУ.** Пайплайн отдаёт наблюдателю, какие строки соревновались за этот
   тик, какая победила и чем именно. Не рисовать — только открыть. Причина: форма плана решена так,
   что хозяин каждый тик выбирает заново (`knowledge/how-the-householders-plan-is-built.md`), и без
   списка кандидатов его поведение — чёрный ящик; а файл, где это открывается, принадлежит этой
   задаче, и отдавать его полосе `полигон` нельзя.
5. **ТЕСТЫ ЗЕЛЁНЫЕ.** Их 640 на `782349bd`.
## журнал
2026-08-30 · ЗАКРЫТА ЛЁГКИМ ЗАКРЫТИЕМ — потому что каждую строку done_when направление перевывело первой рукой из закоммиченных байтов, а не приняло со слов исполнителя. Проверено 2026-08-30 на slot/win-u2 @ b405178e (работа в 292158ee), база 782349bd. 1. ВЫБОР ПО ВАЖНОСТИ — арбитр назван: HighestWeightThenCatalogOrder. Второй ключ — порядок каталога, а НЕ имя, и это разобрано исполнителем письменно: у строк порядок и есть сегодняшнее поведение, арбитр по имени поменял бы landing и suspicious-noise местами и изменил игру. 2. СТРОКА ПРОФИЛЯ ПОБЕЖДАЕТ ВСТРОЕННУЮ — тест AnAuthoredRowBeatsABuiltInOneByNamingItsImportanceAndNotByItsPlace прочитан целиком. Он утверждает ровно то, что нужно, и защищён от самообмана: дописанная строка обязана быть ПОСЛЕДНЕЙ в списке кандидатов, иначе победа ничего не доказывает; с uniform она проигрывает, с insistent выигрывает; и тик ДЕЙСТВИТЕЛЬНО действует по победителю (Cause = RepeatedHeavyNoise), а не просто называет его. 3. БИТ В БИТ — git diff --stat 782349bd..HEAD показывает, что НИ ОДИН существующий тестовый файл не тронут: единственный тестовый файл в диффе новый. Плюс исполнитель сеял мины и назвал ту, что не покраснела. 4. КАНДИДАТЫ ВЫХОДЯТ НАРУЖУ — HouseholderStepResult.DecisionCandidates, роскошь Count/At, никогда не null; в кандидате Source/Name/Outcome/HasWeight/Weight/Won. Поверхность прочитана в коде. 5. ТЕСТЫ ЗЕЛЁНЫЕ — ПРОГНАНЫ НАПРАВЛЕНИЕМ, а не приняты со слов: dotnet test → 647 пройдено, 0 не пройдено. Было 640, семь новых ровно по числу новых методов. Слово о весах: в профиле целого числа нет намеренно, только два слова uniform/insistent — запрет константы из householder-architecture-is-a-sparse-rule-table соблюдён. long Weight существует только как вычисленное значение для наблюдателя.
END_OF_FILE: live/indie-game-development/cards/closed/t-priority-1.md
