---
id: i-a-seeded-mine-still-lives-in-dev-001
_kind: issue
level: execution
route: work
status: done
_pos: 164
evidence: history/2026-08-31-s-repair-g-5a7c-publication-block-001.md
---

## issue
**В `dev` ЛЕЖИТ НАМЕРЕННО СЛОМАННЫЙ СТОРОЖ, И СЛОМАН ИМЕННО ТОТ, КОТОРЫЙ ОХРАНЯЕТ ГЛУХОТУ ХОЗЯИНА.**

Перемерено направлением 2026-08-31 напрямую, а не по чужому отчёту:
`git show dev:tests/TunnelCrew.Core.Tests/HouseholderDecisionPipelineTests.cs` строка 206 несёт
`Assert.That(result.DecisionWeight, Is.EqualTo(3L))` — подменённое значение. Настоящее `2L`.

**ОТКУДА.** Коммит `bb32f3f8` «Seed failing headless close proof» — посев мины ногой гейта
`c-exec-g-5a7c-gate-headless-1-001`. Наряд ТРЕБОВАЛ посеять мину, показать красное закрытие и
**ВЕРНУТЬ**. Возврат написан — коммит `03cd6c87` «Restore seeded mine and record Unity block» — но
`git branch -a --contains 03cd6c87` даёт **ровно одну ветку: `slot/win-u4`**. В `dev` он не влит.

**ЦЕНА.** Сторож на строке 206 охраняет класс дефекта, из-за которого хозяин на прошлой неделе
оглох и проходил мимо крысы. Публикация `dev` в `main` в нынешнем виде опубликовала бы сломанного
сторожа вместе с волной — то есть выключила бы защиту от ровно того отказа, ради которого нога
гейта и затевалась.

**ПОЧЕМУ ЭТО НЕ ЗАМЕТИЛИ.** Нога гейта влилась в `dev` ПЕРВОЙ, до обеих ног, чьи возвраты пришли
домой. Ни одна из трёх ног не прошла штатное закрытие: закрывальщик упирается в красное от арта
(см. `i-shared-art-store-has-no-guard-001`) и до шага публикации не доходит. Мина осталась в `dev`
незамеченной, потому что смотреть на неё было некому.

**ЧИНИТСЯ ОДНИМ ДЕЙСТВИЕМ:** влить `03cd6c87` из `slot/win-u4` в `dev` до всякой публикации.
## review_when
`work` — снимается влитием `03cd6c87` в `dev`. Проверка: `git show dev:tests/TunnelCrew.Core.Tests/HouseholderDecisionPipelineTests.cs`
строка 206 обязана нести `Is.EqualTo(2L)`. **Пока она несёт `3L`, публикация запрещена.**
## журнал
2026-08-31 · УМЕРЛА. Перемерено на b22c00a2: HouseholderDecisionPipelineTests.cs:206 несёт правильное Is.EqualTo(2L). Возврат мины доехал в main.
2026-08-31 · склад улик перемерен целиком: из 38 продуктовых улик 9 мертвы и 13 несут неверный замер, то есть протухло больше половины; самый ценный замер перевернул вывод направления — проёмы сузились сильнее, чем похудела капсула, и запас упал с 0.25 до 0.15 м; заодно найдены четыре карточки, лежащие в closed со статусом open · history/2026-08-31-s-work-g-5a7c-issue-revision-close-001.md
2026-08-31 · СТОП прибора разобран: красное действительно предсуществует волне, но диагноз исполнителя неверен в трёх местах, а настоящий блокер другой — в dev лежит намеренно сломанный сторож глухоты, чей возврат остался на ветке слота · history/2026-08-31-s-repair-g-5a7c-publication-block-001.md
END_OF_FILE: live/indie-game-development/cards/closed/i-a-seeded-mine-still-lives-in-dev-001.md
