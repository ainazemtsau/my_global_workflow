---
id: c-work-g-5a7c-situation-1-001
_kind: call
to: session
play: work
for: t-situation-1
node: g-5a7c
status: waiting
issued: 2026-08-10
description_by: dev
description: 'Довести общую задачу-шов «ситуация мира» до закрытия: наряд, доставка
  с распиской, возврат'
_pos: 16
waiting_on: c-exec-g-5a7c-situation-1-001
---

## goal
Задача `t-situation-1` («ситуация мира») доведена до конца: наряд написан, ФИЗИЧЕСКИ ДОСТАВЛЕН
исполнителю с распиской, работа принята и задача закрыта по букве `evaluator` этой ставки.

Это ПЕРВАЯ задача второй волны и единственная общая: пока она идёт, ни одна дорожка не занята —
его словом «одна задача блочит две полосы, то, ну, естественно, ждем».
## context
Ставка `bet-g-5a7c-wave-2` открыта 2026-08-10 его словом «Принимаю все твои рекомендации».
Аппетит **Б**: семь задач, ОДИН новый слой — и этот слой ровно тот, который строит эта задача.
Читать: `cards/t-situation-1.md` (шесть строк `done_when` и `note`), `cards/bet-g-5a7c-wave-2.md`
блоки `approach`, `evaluator`, `riskiest_assumptions`, `tracks`.

ПРОДУКТ ПЕРЕМЕРЕН РЕВЬЮ 2026-08-10 на опубликованной голове `43648b2d` — брать входом, не
перепроверять с нуля: `origin/dev` = `origin/main` = `origin/slot/win-u1` = `43648b2d`, 151/151
зелёные. `CargoSituation` (`ContactedHouse`/`Landed`) и переименование шума в
`HouseholderStimulus` — это ЗАЧАТОК слоя, выросший случайно из `t-scale-4`. Хозяин слышит РОВНО
ОДНУ ситуацию за тик. `Network/CargoBodyContact.cs:58` делит импульс на `Time.fixedDeltaTime`
(`0.02`), а судья шагает ≈`0.0167`.

БУКВА `evaluator` ЭТОЙ ВОЛНЫ, его выбор: публикация исполнителем в `main` и закрытие ПОСЛЕ неё —
законный порядок. Закрытие идёт либо светлым (каждая строка перемеряется первой рукой по
байтам), либо свежей биндинговой проверкой в другом чате, если строку судит его глаз. **Наряд не
существует, пока исполнитель не расписался:** готовая паста с ПОЛНЫМ текстом и первая строка
возврата с числом полученных строк `done_when`. Это стоило первой волне круга работы и
опубликованной пустышки (`knowledge/an-order-does-not-exist-until-it-is-delivered.md`,
`i-call-text-never-reached-the-executor-001`).
## boundaries
- **Задача не расширяется.** Шесть строк `done_when` — это весь объём. Поведение игры не меняется
  ни на грамм; всё, что меняет поведение, принадлежит дорожкам и другим задачам.
- **`IntegratedHouse.unity` НЕ ОТКРЫВАЕТСЯ.** Дом принадлежит владельцу и следующей волне; в
  `g-1d84` полосы разошлись на этом файле дважды, и слитые байты не запускал никто.
- **Пороги `60 / 150 / 400` НЕ ДВИГАЮТСЯ.** Чинятся только единицы, в которых считается сила.
- **Реакции хозяина здесь НЕ трогаются** — это `t-react-1`, другая дорожка. Эта задача меняет
  только то, ОТКУДА он берёт ситуацию, а не то, что он с ней делает.
- **Зрение не строится, комбинация не строится, ручное управление не строится.**
- Наряд исполнителю пишет эта нога и ОБЯЗАНА доставить его полный текст с распиской. Без
  расписки наряд не выдан.
## done_when
1. Наряд на `t-situation-1` написан по шести строкам `done_when` карточки задачи, ни одна не
   потеряна и ни одна не дописана.
2. Наряд ФИЗИЧЕСКИ ДОСТАВЛЕН: готовая паста с полным текстом отдана исполнителю, и первая строка
   его возврата — расписка, называющая число полученных строк `done_when`.
3. Работа вернулась, и задача закрыта по букве `evaluator` ставки; нога называет вслух, каким из
   двух путей шла (светлое закрытие или свежая биндинговая проверка).
4. Главный гейт перемерен ПЕРВОЙ РУКОЙ, а не принят с отчёта: в коде хозяина не осталось ни
   одного упоминания груза — поиском по имени.
5. `t-situation-1` в статусе `done`; обе дорожки разблокированы.
## return
RESULT ноги `work` с закрытой `t-situation-1` и распиской исполнителя в возврате.

Дальше открываются ОБЕ дорожки сразу (`NOW.track_wip_limit: 2`): `t-cargo-1` в `cargo-hands` и
`t-react-1` в `householder`. `t-cargo-1` — рискованное допущение №1 волны и несёт свой откат.
## budget
Ноги `work` по обычному счёту: наряд, выдача, возврат. Порог смерти (5) этой ставки считает
ноги — на общую задачу и первые задачи обеих дорожек их положено не больше двенадцати, иначе
вторая дорожка встаёт на паузу. Половина дня работы исполнителя.
## журнал
2026-08-10 · возврат пришёл, t-situation-1 закрыта светлым путём, обе дорожки волны разблокированы
2026-08-10 · наряд на общую задачу-шов выдан в WIN-U1 готовой пастой с распиской первой строкой возврата: шесть строк задачи свёрнуты в три строки контракта 36 без потерь, главный гейт замерен заранее — 23 упоминания груза в коде хозяина против требуемого нуля, найдено имя груза в сериализованном ассете хозяина, а починка единиц силы оказалась не нейтральной и названа числом до работы: шаг судьи 0.016667 против делителя 0.02, то есть дом станет слышать в 1.2 раза громче при неподвижных порогах · history/2026-08-10-s-work-g-5a7c-situation-1-dispatch-001.md
2026-08-10 · вторая волна нарезана и подписана: аппетит Б его словом на три показанных размера, две дорожки его словами и одна общая задача-шов до их расхождения, семь задач и один новый слой; впервые за две ставки написан блок evaluator — его выбором фактический порядок стал законом, а к нему прибиты расписка исполнителя и запрет выдавать зелёные ворота за поведение игры; наклон груза куплен заново своей задачей и своим откатом, а модификатор вырезан его же словом · history/2026-08-10-s-shape-g-5a7c-wave-2-001.md
END_OF_FILE: live/indie-game-development/cards/closed/c-work-g-5a7c-situation-1-001.md
