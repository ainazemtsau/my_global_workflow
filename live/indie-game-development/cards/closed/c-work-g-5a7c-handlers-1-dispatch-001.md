---
id: c-work-g-5a7c-handlers-1-dispatch-001
_kind: call
to: session
for: t-handlers-1
play: work
track: householder-life
status: done
issued: 2026-08-13
call: work/c-work-g-5a7c-handlers-1-dispatch-001-call.md
description: 'Продолжение дорожки householder-life: t-handlers-1 выдаётся нарядом
  либо называется точный блокер'
_pos: 86
---

## журнал
2026-08-13 · наряд c-exec-g-5a7c-handlers-1-001 выпущен целиком, блокеров нет; три зависимости перемерены поимённо и карточка переведена blocked → active · history/2026-08-13-s-work-g-5a7c-handlers-1-dispatch-001.md
2026-08-13 · наряд на t-handlers-1 выпущен целиком и без блокеров, а выдача нашла, что задача не та, какой выглядела — МЕХАНИЗМ УЖЕ ПОСТРОЕН, А ПОВЕДЕНИЯ НЕТ: конвейер обработчиков, два типа обработчиков и ветки поведения существуют в ядре и УЖЕ читаются из JSON загрузчиком с именованным отказом на неизвестную реакцию и лишнее поле, а в опубликованном игровом профиле оба массива пусты, значит наряд покупает написанные строки, а не слой; найдено точное место недостачи, одна функция — существующая комбинация срабатывает, когда два факта просто ОБА ЖИВЫ, и не проверяет ни комнату, ни предмет, поэтому строка 1 карточки сегодня не выполнена, хотя комбинация как таковая есть, а комната в факте уже лежит от закрытой t-place-fact-1; отдельно измерено, за что НЕ надо платить второй раз — строка 6 «чья строка сработала, видно» уже выполнена целиком, имя обработчика штампуется конвейером и доезжает до наблюдения вместе с именем ветки; ВТОРОЙ ВИД НАЗВАН НАПРАВЛЕНИЕМ, как требует правило, и взят из строки 1 карточки и цепи документа владельца, а не выдуман — локальность въезжает ТРЕТЬИМ видом обработчика рядом с двумя существующими, гейт занимает строку done_when, а не границы, и доказывается составом коммита против ПОИМЁННОГО списка шести файлов ядра с заранее названной разрешённой ценой; три зависимости перемерены поимённо и все закрыты, поэтому карточка переведена blocked → active, а стояла она blocked БЕЗ unblock_when — расхождение снято, а не обойдено; дрейфа контракта нет, обе стороны на 36, и наряд несёт ровно три строки потолка, в которые уложены все восемь строк карточки вместе с райдером на замер волочения числом; ДРОЖЬ в наряд НЕ вошла и это решение — другая поверхность, её улика требует сначала измерить, а смешанная нога режется до сборки, поэтому она вынесена владельцу ВОПРОСОМ с честной ценой в одну ногу против порога 3, а не заведена задачей · history/2026-08-13-s-work-g-5a7c-handlers-1-dispatch-001.md
2026-08-13 · биндинговая проверка ЗАКРЫЛА t-sight-1 — семь строк карточки перемерены на опубликованной c485b30e попыткой ОПРОВЕРГНУТЬ каждую, и ни одна не опровергнута; прогон Core сделан своей рукой и дал 252 при нуле пропущенных, а базовые 246 перемерены БЕЗ переключения дерева счётом атрибутов NUnit (228+18 на 4fc04b4c против 234+18 на голове), то есть цепь 246→252 подтверждена обеими точками первой руки; «потребитель не меняется» проверено побайтно и устояло — Observe идентичен базе, оба Loudest идентичны базе, и из шестнадцати файлов, знавших про слой ситуаций, тринадцать не в диффе вовсе, а цена расширения названа вслух как требует карточка правила: девять членов дописано в восемь перечислений без единой перенумерации, две правки ветки в одном файле-редукторе и одна правка сигнатуры каталога реакций; «магического знания нет» атаковано трижды и не пробито, причём решает не чтение, а прогнанный мной тест, который двигает игрока ПОСЛЕ потери видимости и три тика подряд держит цель в старой точке; откат перемерен по деревьям — дерево revert совпадает с деревом базы знак в знак, и отдельно измерено то, чего в отчёте не было: правка после его прогона не добавила ни одного нового пути, все её семь путей входят в 22 пути первой сборки, а всё прочее между базой и головой это только документы, значит «одним коммитом» верно и сегодня; ядро не знает про мебель не по обещанию, а по сборке — TunnelCrew.Core компилируется под netstandard2.1 без UnityEngine; ПОРОГ СМЕРТИ 1 НЕ СРАБОТАЛ и это записано прямо, потому что в двух его предложениях нет ни «слепой», ни «читер», ни «ручками не лечится»; найдено одно расхождение байтов со словами карточки и оно НЕ раздуто в блокер — порог видимости считается в коде как sampleCount/2+1, в ассете названо только число точек, поэтому «2 из 5» задать нельзя, и это ушло уликой; Unity-прогон намеренно НЕ перезапущен по границе наряда, а не по недоступности площадки, и это измерено — дерево win-u3 несёт ровно опубликованные производственные байты; слоты остались AVAILABLE, в продукте не изменено ничего · history/2026-08-13-s-work-g-5a7c-sight-1-binding-g5-001.md
END_OF_FILE: live/indie-game-development/cards/closed/c-work-g-5a7c-handlers-1-dispatch-001.md
