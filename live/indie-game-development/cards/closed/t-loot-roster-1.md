---
id: t-loot-roster-1
_kind: task
_bet: bet-g-5a7c-wave-8
track: ядро
status: done
_pos: 153
goal: Игра получает все четырнадцать вещей, которые дом заявляет, и тест перестаёт
  держать своё мнение о количестве
---

## done_when
**КАРТОЧКА ЗАВЕДЕНА ЗАДНИМ ЧИСЛОМ 2026-08-30 ПЛЕЕМ `repair`.** Наряда в направлении не было.
`done_when` — **РЕКОНСТРУКЦИЯ ПО ПОСТАВЛЕННЫМ БАЙТАМ**, не подписанный заранее заказ.

Перемерено первой рукой на `main = dev = f6fc0918`.

1. **ОБЕ НЕДОСТАЮЩИЕ ВЕЩИ ПОДКЛЮЧЕНЫ.** Дом заявляет четырнадцать предметов, сцена подключала
   двенадцать: `Spare keys` и `Spare mug` лежали и в префабе, и в файле дома, и не доезжали до
   `StartConditions` — игра раскладывала двенадцать, и нигде об этом не говорилось. Это ровно та
   пара, ради которой строился слой убеждений: бросить первое и переключиться на запасное.
   Проверено разрешением ссылок через префаб: `IntegratedHouse.unity:1067-1081` — `_cargoSpawns`
   несёт **14** записей; `fileID 2090291535` → `FirstHouse.prefab` GameObject `6969998205459681442`
   = **`Spare keys`**; `fileID 1843943968` → `7830600372006809175` = **`Spare mug`**.
   `first-house.house.json` заявляет ровно 14 имён, дубликатов нет.
2. **ТЕСТ БОЛЬШЕ НЕ ДЕРЖИТ СОБСТВЕННОГО МНЕНИЯ О КОЛИЧЕСТВЕ.** Он утверждал ДВЕНАДЦАТЬ — и в имени,
   и в теле; дом вырос до четырнадцати, и неправ оказался тест.
   Проверено: `Assets/Tests/TunnelCrew/HouseBuilding/FirstHouseInSceneEditModeTests.cs` — метод
   переименован в `TheGameIsHandedExactlyTheThingsTheHouseAuthorsAndEveryOneOfThemResolves`,
   читает `first-house.house.json` через `HouseDescription.TryRead` и сравнивает **множеством
   ИМЁН**, а не числом: пропущенная вещь и вещь, подключённая дважды, по одному числу неразличимы.
   Порядок намеренно не утверждается — место записи и есть cargo id.
3. **ПРАВКА СЦЕНЫ — ЧИСТОЕ ДОБАВЛЕНИЕ.** 24 строки, ни одного удаления
   (`git show --stat fef06356`).
4. **РЕГРЕССИЯ, ВНЕСЁННАЯ РЕДАКТОРОМ, ОТКАЧЕНА.** Сохранение сцены из редактора WIN-U2 обнулило
   `NetworkHouseholder._carry` — больная база ассетов молча выбросила ссылку, которую не смогла
   разрешить. Проверено на `f6fc0918`: `IntegratedHouse.unity:1589` несёт
   `_carry: {fileID: 11400000, guid: 348cfce6493edaa4394ff3d5f7232b9a, type: 2}` — прежние байты.
5. **ТЕСТЫ ЗЕЛЁНЫЕ.** `dotnet test` на `f6fc0918` → **694 пройдено, 0 не пройдено**, прогон
   направления. Оговорка: сам изменённый тест — EditMode, в эти 694 он НЕ входит; его зелень
   (220/220) — заявление исполнителя из Unity, направлением не перепроверено.
## журнал
2026-08-30 · закрыта по факту: пять строк реконструированного done_when перевыверены первой рукой на f6fc0918 — _cargoSpawns несёт 14 записей, две новые разрешены через префаб в Spare keys и Spare mug, диф сцены чистое добавление 24 строки, _carry восстановлен прежними байтами (:1589), тест читает first-house.house.json и сравнивает множеством имён. Зелень самого изменённого EditMode-теста (220/220) — заявление исполнителя, направлением не перепроверена · history/2026-08-30-s-repair-g-5a7c-three-legs-behind-001.md
2026-08-30 · записи догнали продукт: три ноги закрыты по факту с перевыводом done_when из байтов f6fc0918, две карточки заведены задним числом, замер ядра хозяина перемерен, два долга вынесены уликами · history/2026-08-30-s-repair-g-5a7c-three-legs-behind-001.md
END_OF_FILE: live/indie-game-development/cards/closed/t-loot-roster-1.md
