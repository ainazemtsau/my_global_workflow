---
id: t-assetlib-1
_kind: task
_bet: bet-g-5a7c-wave-4
status: ready
kind: executor
order: 21
goal: 'Дом стоит на своей библиотеке ассетов: купленные модели вместо коробок, замена — одна строка'
_pos: 78
---

## done_when
1. **ОБА КУПЛЕННЫХ ПАКА ЖИВУТ В ПРОЕКТЕ** и закоммичены. Названо, что из них НЕ импортировано:
   мебельный пак везёт 6 сцен и 3 вложенных `.unitypackage`, они в игру не тащатся.
2. **ЕСТЬ БИБЛИОТЕКА, И КАЖДАЯ ЕЁ ЗАПИСЬ — НАШ ПРЕФАБ.** Модель из пака лежит внутри, наше
   снаружи. **Ни один префаб пака не правится** — иначе обновление пака затрёт правку.
3. **ЧЕРТЁЖ ЗОВЁТ БИБЛИОТЕКУ ПО ИМЕНИ.** У предмета в описании дома появляется имя записи, сборщик
   берёт префаб оттуда. Промежуточного справочника «предмет → префаб» НЕ заводится: библиотека
   и есть этот справочник.
4. **ЧЕГО НЕТ — ИМЕНОВАННАЯ КОРОБКА ТОГО ЖЕ ГАБАРИТА**, тоже записью библиотеки. Дом собирается
   целиком при любом числе недостающих моделей.
5. **59 ПРЕДМЕТОВ ОБСТАНОВКИ И 12 ПРЕДМЕТОВ ДОБЫЧИ ПРОЙДЕНЫ ПОИМЁННО** — по каждому сказано
   «модель из пака» или «коробка». Список коробок целиком в возврате: он и есть заказ в лабораторию.
6. **СТЕЛС НЕ СДВИНУЛСЯ** — прогон честного луча зрения хозяина до и после даёт те же числа,
   числа в возврате. Это несущее допущение волны, а не формальность.
7. **ЗАМЕНА ДОКАЗАНА НА ВТОРОМ ЭКЗЕМПЛЯРЕ** — модель одного предмета меняется правкой одной строки,
   без правки кода.
8. **ГЕЙТ ЗЕЛЁНЫЙ НА ОБЪЕДИНЁННОМ ДЕРЕВЕ** с текущими головами других идущих ног, не на своём.
9. **ОТКАТ ОДНИМ КОММИТОМ**, доказанный ВЫПОЛНЕННЫМ `git revert` с приведёнными SHA.

## note
**ЭТО ЕГО ЗАМЫСЕЛ, НЕ ВЫВОД НАПРАВЛЕНИЯ.** Дословно 2026-08-16: «нам нужен какой-то процесс…
мы будем собирать свою библиотеку ассетов»; «мы как-то добавляем его в свой, может, создаем prefab
на нем»; «потом в спеке… можно будет сделать, что вот использовать такой prefab уже из нашей
библиотеки».

**ГРАНИЦА ЗНАНИЯ — ТОЖЕ ЕГО СЛОВО, И ОНА НЕСУЩАЯ.** Про Concept Lab: «им уже пофиг, это уже наши
вопросы, где найти либо заменить именно asset». Библиотеку знает ТОЛЬКО чертёж дома. Ни канон,
ни Concept Lab имён наших префабов не знают и знать не должны — иначе каждая замена модели
протухает в их документах и рождает третью копию правды.

**ПАКИ КУПЛЕНЫ, ОНИ ПЕРВЫЙ ИСТОЧНИК.** Его слово: «мы купили два пака, которые должны, в принципе,
всё покрывать, и мы должны использовать их оттуда, ну только в крайнем случае искать что-то
на стороне». Мебель — `ithappy` Realistic Furniture, 959 префабов, куплен НЕ в Unity Asset Store,
а напрямую у студии (разовая покупка, 5 мест). Внешнее — `NosenseStudio` Suburban Stylized,
148 префабов.

**ЧТО ОТМЕНЯЕТСЯ ЭТИМИ ПАКАМИ.** Реестр лаборатории планировал строить руками семь предметов
гостиной (радиатор, торшер, ковёр, ящик, провода, пульт, наличники). В паке 20 ламп, 19 ковров,
91 «prop». Строить до сверки с паком — работа впустую.

**ГЛАВНОЕ ПРЕПЯТСТВИЕ ИЗМЕРЕНО ЗАРАНЕЕ: ИМЕНА В МЕБЕЛЬНОМ ПАКЕ НИЧЕГО НЕ ЗНАЧАТ** — не
`washing_machine`, а `bathroom_012`, `prop_038`, `kitchen_045`; внутри FBX то же самое. Подбор
скриптом по именам ДАЁТ МУСОР, это проверено: «ящик с инструментом → почтовый ящик», «верстак →
журнальный столик», «нет стиральной машины» при 38 моделях ванной. Выбирать надо глядя.

**ВАРИАНТЫ (красная / серая тумбочка) ВЫРЕЗАНЫ — ЭТО ПРЕДЛОЖЕНИЕ НОГИ, принятое его «да, оформляй
наряд», а не его собственное решение.** Довод: в паке 959 моделей, красная и серая тумбочки там
скорее всего лежат двумя разными моделями — это две записи, а не механизм; и свои материалы ещё
не выбраны, развилка в лаборатории открыта. Место под варианты остаётся бесплатным: раз предметы
зовутся именами библиотеки, добавить `nightstand_red` рядом можно в любой момент.

**ДОМ БУДЕТ МЕНЯТЬСЯ, И ЭТО НЕ ПОВОД ЖДАТЬ.** Его слово 2026-08-16: «дом у нас в любом случае будет
меняться… скорее всего, будет ещё один этаж и подвал… не надо что-то спеха где-то менять…
это здесь не фиксировано». Работа этой карточки от планировки НЕ зависит: библиотека устроена
по видам предметов, а не по комнатам, и переживает перепланировку целиком.

**ОТНОШЕНИЕ К `t-rooms-content-1`.** Та карточка про «по чему играют» — ступени, укрытия, высоты.
Эта про «из чего сделано». Пересечения нет. Но `t-rooms-content-1` стоит `waiting` на содержании
из Concept Lab, а содержание уже приехало и уже расставлено закрытой `t-housebuilder-2` — её
состояние надо перемерить отдельно, здесь это не делается.

**ПОВЕРХНОСТЬ ИЗМЕРЕНА, А НЕ ОБЪЯВЛЕНА** — по правилу входа в очередь этой ставки. Пишет:
`Assets/ithappy/**`, `Assets/NosenseStudio/**`, библиотека под `Assets/TunnelCrew/World/`,
`World/HouseBuilding/first-house.house.json`, `Editor/HouseBuilding/HouseBuilder.cs`.
Идущие ноги: `coreplan-1` не пишет ни строки продуктового кода (сказано в её наряде дословно);
`dropmodel-1` держит `Art/Bodies/**` и `TunnelCrew/Settings/**` — не пересекается;
`unitygate-1` называет `Assets/TunnelCrew/**` как поверхность ЧТЕНИЯ для прогона тестов, пишет
в оснастку гейта. **Названное взаимодействие, не блокер:** импорт полутора гигабайтов меняет
дерево, которое `unitygate-1` меряет, поэтому строка 8 требует гейт на объединённом дереве.

**ЗАКРЫТИЕ:** светлое недоступно — строку 5 в части «похоже ли это на дом мастерового» и общий вид
судит его глаз.

## наряд
`work/2026-08-16-call-assetlib-1.md`

## журнал
2026-08-16 · заведена его словом «да, оформляй наряд» после разбора замысла библиотеки; паки куплены и опись снята заранее — 959 и 148 префабов, имена в мебельном паке ничего не значат · history/2026-08-16-s-work-g-5a7c-assetlib-1-dispatch-001.md
END_OF_FILE: live/indie-game-development/cards/t-assetlib-1.md
