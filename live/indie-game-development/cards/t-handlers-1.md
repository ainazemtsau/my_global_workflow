---
id: t-handlers-1
_kind: task
_bet: bet-g-5a7c-wave-4
status: ready
kind: executor
order: 12
goal: Несколько фактов складываются в один инцидент, и набор обработчиков написан
  под центральный кейс
_pos: 72
---

## done_when
1. **ИНЦИДЕНТ ИЗ НЕСКОЛЬКИХ ФАКТОВ.** Совместимые факты про одну комнату или один предмет сводятся
   в одно местное происшествие, и хозяин отвечает на происшествие, а не на последний факт. Это
   критерий 6 узла дословно.
2. **НАБОР ОБРАБОТЧИКОВ НАПИСАН.** Строки написаны под центральный кейс его документа и лежат в
   игровом профиле, а не только в тестах. Сегодня `behaviorBranches` и `decisionHandlers` в
   опубликованном `default.householder.json` ПУСТЫ.
3. **«НЕ ВИЖУ — НАКЛОНЮСЬ».** Среди строк есть та, что отвечает на «шумело здесь, а никого не
   видно»: он наклоняется и заглядывает. Добавляется одно физическое действие, не больше.
4. **КОМБИНАЦИИ ОПИРАЮТСЯ НА МЕСТО И НА ЗРЕНИЕ.** Хотя бы одна строка использует комнату
   (`t-place-fact-1`) и хотя бы одна — факт зрения (`t-sight-1`).
5. **ВЛАДЕЛЕЦ ПРОИЗВЁЛ ХОТЯ БЫ ОДНУ КОМБИНАЦИЮ САМ В СОБРАННОЙ ИГРЕ.** Не в лаборатории. Без этой
   строки задача кончится вердиктом про код, и это ровно то, на чём `t-react-3` прошлой волны была
   снята.
6. **ЧЬЯ СТРОКА СРАБОТАЛА — ВИДНО.** Имя сработавшего обработчика доходит до наблюдения, а не
   умирает локальной переменной.
7. **РАЙДЕР — ШОРОХ ВОЛОЧЕНИЯ ЗАМЕРЕН, А НЕ ПОСЧИТАН.** Улика
   `i-dragged-cargo-rustles-every-tick-001`: величина меряется прогоном, пороги `60 / 150 / 400`
   при этом НЕ двигаются и специального случая под волочение не заводится. Замер записан числом.
8. **ОТКАТ** одним коммитом до головы дорожки.

## состояние строк на 2026-08-13, перемерено закрывающей ногой
`1` ✓ · `2` ✓ · `3` ✓ · `4` ✓ · `5` **НЕТ** · `6` ✓ (уже была выполнена до наряда) · `7` ✓ ·
`8` ✓. Семь строк из восьми закрыты байтами, прогоном и опубликованным профилем; разбор
построчно — `history/2026-08-13-s-work-g-5a7c-handlers-1-close-001.md`.

**Строка 5 не выполнена и не выполняется НИЧЕМ, что есть в игре сегодня.** Владелец нажимал
кнопку в редакторской лаборатории, а лаборатория этой строки не закрывает по прямому запрету
`t-lab-2` строка 6: «ни одна галка "сделано" не закрывается тем, что "в лаборатории работает"».
**Его слова 2026-08-13, дословно.** На прямой вопрос, где он видел комбинацию — «кнопкой в
лаборатории». И следом: «я не буду в игре тестировать такие условия так как сейчас не удобно мы
можем потом ногу сделать что бы это было тетсить удобнее».

Причина при этом НЕ в его нежелании: обе новые строки профиля просят полосы `quiet-rustle` и
`suspicious-noise`, а в собранной игре их рождает только удар груза, и единственный настоящий
замер такого удара — 1 884 … 54 730 при пороге тяжёлого `400`. То есть «не удобно» на самом деле
«нечем» — улика `i-quiet-noise-is-unreachable-in-the-built-game-001`.

## note
**ЕГО СЛОВО, снявшее конвейер:** «конвейер обработчиков, наверное, всё-таки не надо… мы не знаем,
как это работает… нам нужно просто написать какие-то хендлеры вот для нашего текущего шва». И
отдельно: «кто будет писать её — будем уже именно в задачу самой решим». Значит выбор исполнителя
для этой задачи делается при выдаче наряда, а не заложен ставкой.

**ЦЕПЬ ИЗ ЕГО ДОКУМЕНТА, которую строка 1 обязана воспроизвести:** «шум возле тумбочки → открытый
ящик → будильник звонит из неправильного места → сам будильник движется по полу. Тогда хозяин
вполне обоснованно понимает, что его будильник сейчас кто-то уносит». Ящик в волну не входит;
воспроизводится та часть цепи, для которой есть источники: шум, комната, звонящий предмет, взгляд.

**ЖЁСТКОЕ ПРАВИЛО КРИТЕРИЯ 6 ДЕРЖИТСЯ:** обработчик не получает координату игрока без законно
добытого факта.

**ЗАВИСИМОСТИ:** задача идёт ПОСЛЕ `t-sight-1` и `t-place-fact-1` в своей дорожке и после
`t-thing-1` в чужой. Если `t-thing-1` не доехала, строки про звонящий предмет режутся, остальные
остаются.

**ЧЕГО ЗДЕСЬ НЕТ:** конкурирующих и отложенных происшествий (вырез узла), маскировки звука душем
(вырез узла), долговременной памяти между заходами, автоматической приёмки обработчиков, зрения как
такового.

**ЗАКРЫТИЕ:** светлое НЕДОСТУПНО — строку 5 судит его глаз в собранной игре.
## журнал
2026-08-13 · наряд `c-exec-g-5a7c-handlers-1-001` ЗАКРЫТ, а задача НЕТ, и разводит их ровно строка 5 — его собственная проверка в собранной игре, которую он отказался делать дословно: «кнопкой в лаборатории… я не буду в игре тестировать такие условия так как сейчас не удобно мы можем потом ногу сделать что бы это было тетсить удобнее»; семь строк из восьми перемерены МОЕЙ рукой: третий вид `local-incident` въезжает коммитом `cfb71f57` на СЕМЬ путей и не трогает НИ ОДНОГО из шести названных файлов ядра, опубликованный `default.householder.json` несёт две строки и ветку вместо двух пустых массивов, добавлено РОВНО одно физическое действие `CrouchAndPeek`, обе строки опираются и на комнату и на факт зрения, имя сработавшей строки доходит до наблюдения тем механизмом, что был до наряда, откат `a22f5c98` прочитан лично и его дерево `85bb8d5a` побайтно равно дереву базы, 267 из 267 тестов прогнаны мной при нуле упавших против базовых 254; ЗАЧЕМ СТРОКА 5 НЕ ВЫПОЛНИМА СЕГОДНЯ — найдено здесь, чтением источника шума, а не исполнителем: в собранной игре шорох и стук рождает ТОЛЬКО удар груза, кнопки лаборатории живут в редакторском файле и подставляют ровно пороговое число, а единственный настоящий замер удара груза — его собственный — дал 1 884 … 54 730 против порога тяжёлого 400, то есть настоящий груз в тихие полосы не попадает вовсе и обе новые строки в игре недостижимы; лаборатория эту строку не закрывает по прямому запрету `t-lab-2` строка 6; задача переведена active → blocked с `unblock_when` его словами, а решение «где чинить» уехало уликой `i-quiet-noise-is-unreachable-in-the-built-game-001`, которая объясняет одной причиной и недостижимое происшествие, и его же «хозяин топчется по грузу» · history/2026-08-13-s-work-g-5a7c-handlers-1-close-001.md
2026-08-13 · наряд на t-handlers-1 выпущен целиком и без блокеров, а выдача нашла, что задача не та, какой выглядела — МЕХАНИЗМ УЖЕ ПОСТРОЕН, А ПОВЕДЕНИЯ НЕТ: конвейер обработчиков, два типа обработчиков и ветки поведения существуют в ядре и УЖЕ читаются из JSON загрузчиком с именованным отказом на неизвестную реакцию и лишнее поле, а в опубликованном игровом профиле оба массива пусты, значит наряд покупает написанные строки, а не слой; найдено точное место недостачи, одна функция — существующая комбинация срабатывает, когда два факта просто ОБА ЖИВЫ, и не проверяет ни комнату, ни предмет, поэтому строка 1 карточки сегодня не выполнена, хотя комбинация как таковая есть, а комната в факте уже лежит от закрытой t-place-fact-1; отдельно измерено, за что НЕ надо платить второй раз — строка 6 «чья строка сработала, видно» уже выполнена целиком, имя обработчика штампуется конвейером и доезжает до наблюдения вместе с именем ветки; ВТОРОЙ ВИД НАЗВАН НАПРАВЛЕНИЕМ, как требует правило, и взят из строки 1 карточки и цепи документа владельца, а не выдуман — локальность въезжает ТРЕТЬИМ видом обработчика рядом с двумя существующими, гейт занимает строку done_when, а не границы, и доказывается составом коммита против ПОИМЁННОГО списка шести файлов ядра с заранее названной разрешённой ценой; три зависимости перемерены поимённо и все закрыты, поэтому карточка переведена blocked → active, а стояла она blocked БЕЗ unblock_when — расхождение снято, а не обойдено; дрейфа контракта нет, обе стороны на 36, и наряд несёт ровно три строки потолка, в которые уложены все восемь строк карточки вместе с райдером на замер волочения числом; ДРОЖЬ в наряд НЕ вошла и это решение — другая поверхность, её улика требует сначала измерить, а смешанная нога режется до сборки, поэтому она вынесена владельцу ВОПРОСОМ с честной ценой в одну ногу против порога 3, а не заведена задачей · history/2026-08-13-s-work-g-5a7c-handlers-1-dispatch-001.md
2026-08-14 · ПЕРЕНЕСЕНА В ЧЕТВЁРТУЮ ВОЛНУ ЕГО СЛОВОМ «чиним шум». Задача не переписана и не урезана: семь строк из восьми остаются закрытыми байтами и прогоном, а разводит их по-прежнему строка 5 — его собственная проверка в СОБРАННОЙ игре. Изменилось одно: `unblock_when` перестал ждать его согласия и стал ждать РАБОТУ — возврат `t-noise-1`, которая делает тихий и средний шум достижимыми. До неё строка 5 невыполнима не по его нежеланию, а потому что произвести нужный шум в игре нечем · history/2026-08-14-s-review-g-5a7c-wave-3-close-001.md
2026-08-14 · РАЗБЛОКИРОВАНА возвратом t-noise-1: три полосы шума стали достижимы в собранной игре, работа опубликована коммитом de9ea893 · history/2026-08-14-s-review-g-5a7c-wave-3-close-001.md
END_OF_FILE: live/indie-game-development/cards/t-handlers-1.md
