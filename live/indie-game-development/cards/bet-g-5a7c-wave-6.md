---
id: bet-g-5a7c-wave-6
_kind: bet
node: g-5a7c
opened: 2026-08-27
status: active
_pos: 130
---

## owner_word
**«давай волну A»** — 2026-08-27, на показанные три варианта. И следом довод его же словами:
**«я вот согласен с твоей волной, потому что не хватает вот этого»**.

- **Аппетит дословно:** «аппетит — 4 дня (ничего не срезаем)».
- **Форма дословно:** «важно делать, чтобы у нас как минимум две параллельные работы были.
  В идеале четыре как бы, да, то есть там так брать».
- **Добавил анимацию:** «чтобы анимации были совсем… что он переносит… чтобы мы настроили
  нормальный процесс создания анимации. Сейчас у нас есть базовый, но не факт, что он пойдёт».
- **Добавил шкалы и фазы:** «у хозяина тоже типа шкалы… шкала раздражения и… фазы безумия, да…
  разные модельки наложены. Ну, это нужно будет ресёрч сделать, как нам лучше делать».
- **Про закрытый каталог реакций:** «у нас что максимум может быть 11 реакций если да то это
  точно нужно исправлять».
- **Про сцену:** «да, возвращай IntegratedHouse».

## goal
**Хозяин делает вещи в игре, и это видно.**

Первая волна под новым концептом ONLY VERMIN KNOW. Новая игра целиком построена на наблюдении
за жизнью хозяина и её разгадывании — его собственные слова, записанные в продуктовом обзоре:
«основа игры — ритуалы хозяина и их разгадывание… он вечером полирует нож, а нож нужно украсть»,
и «антагонист один на сцене, поэтому чем шире он умеет взаимодействовать, тем лучше — это самое
главное».

Замер, с которого волна начинается: **вся жизнь хозяина сегодня — два таймера.**
`default.householder.json` содержит занятие `cook` (три фазы) и `wash` (две), у каждой фазы только
`durationSeconds`. Ничего в мире они не трогают, потому что `IHouseholderWorldPort` умеет ровно
`Read()` и `TakeHeardEvent()` — **у его правил нет способа что-либо сделать с миром.** Дотягивание
через Final IK построено и принято владельцем, но живёт только на стенде `ReachLab`.

Из двенадцати критериев узла волна двигает **6** (хозяин причинен) и **9** (заход играется),
и не закрывает ни одного целиком.

## appetite
**ЧЕТЫРЕ ДНЯ, ЕГО СЛОВОМ, И НИЧЕГО НЕ СРЕЗАЕТСЯ.**

Дат нет — его запрет от 2026-08-02 действует; пятая волна была разовой отменой ради внешнего
срока Steam. Здесь потолок в днях, а не дата в календаре.

**ВНЕШНЯЯ СВЕРКА ГОВОРИТ, ЧТО ЭТО КАЛИБРОВАНО, А НЕ ОПТИМИЗМ.** Пятая волна: четыре дня, шесть
задач, вердикт «выполнено, принято». Здесь: четыре дня, семь задач плюс одна починка, и четыре
полосы вместо трёх.

**НО ОДНО ОТЛИЧИЕ НАЗВАНО ДО СТАРТА.** Пятая волна влезла в четыре дня ПОТОМУ, ЧТО резала:
его же слова тогда — «остальные шесть комнат, звук, интерфейс, хват, похуй, выкидываем». Здесь
он сказал обратное. Поэтому вместо резки заранее согласован **порядок смерти**: если потолок
упрётся, работы умирают в этом порядке — сначала `t-irritation-1`, потом `t-carry-place-1`,
потом `t-world-port-1`. Ничего не режется сейчас; решено только, кто уходит первым.

## tracks
Четыре полосы, его словами «в идеале четыре». Лимит одновременности — `NOW.track_wip_limit: 4`,
поставленный им же 2026-08-12 словом «да хоть четыре».

| Полоса | Задачи | Владеет файлами |
|---|---|---|
| `словарь` | `t-reactions-data-1`, `t-irritation-1` | `HouseholderReaction.cs`, `HouseholderProfileLoader.cs`, `default.householder.json` |
| `провод` | `t-reach-wire-1`, `t-world-port-1` | `AuthoritativeHouseholder.cs`, `Network/**`, `World/HouseholderRouteController.cs` |
| `анимация` | `t-grip-second-1`, `t-carry-place-1` | `Presentation/BodyReach.cs`, `Art/**`, `Probe/ReachLab/**` |
| `ресёрч` | `t-madness-research-1` | ничего |

**ГРАНИЦЫ ИЗМЕРЕНЫ, А НЕ ОБЪЯВЛЕНЫ — И ПЕРВЫЙ ЗАМЕР БЫЛ НЕПОЛОН.** Нарезка объявила пересечений
ноль; перемер того же дня, по вопросу владельца «какие наряды я могу запускать параллельно», нашёл
ДВА. Записано так, а не тихо исправлено.

**Разведено владением файла — приёмом, которым в этом узле уже разводили четыре ноги:**

- `Presentation/BodyReach.cs` (325 строк) → полоса `анимация`; полоса `провод` его только зовёт.
- `HouseholderReaction.cs` + `HouseholderProfileLoader.cs` → полоса `словарь`. **Найдено перемером:**
  сегодня `HouseholderActionTargetName` знает четыре цели и ни одна из них не ВЕЩЬ; если проводу
  нужна пятая, она ложится ровно в эти два файла — на это перечисление ветвятся `HouseholderAction.cs`,
  `HouseholderDecision.cs`, `HouseholderReaction.cs`, `HouseholderVocabulary.cs` и
  `HouseholderProfileLoader.cs`.
- `IntegratedHouse.unity` → починка `t-scene-restore-1`. **Найдено перемером:** провод обязан
  показать результат в этой же сцене, а сцену Unity, которую правят двое, никто потом не открывает.

**Три зависимости, а не одна.** `t-world-port-1` ждёт `t-reactions-data-1` целиком.
`t-reach-wire-1` стартует сразу, но два её куска ждут: слово словаря — `t-reactions-data-1`,
демонстрация в сцене — `t-scene-restore-1`. Всё, что она делает со стороны `Network/**`, свободно
от обоих: `HouseholderSnapshot` наполняют ровно три файла, и все три в `Network/`.

**В НУЛЕВОЙ ЧАС СТАРТУЮТ ЧЕТЫРЕ — по одной на полосу:** `t-reactions-data-1` (словарь),
`t-reach-wire-1` (провод), `t-grip-second-1` (анимация), `t-madness-research-1` (ресёрч).
Ровно на лимите. Починка `t-scene-restore-1` полосы не занимает: она вне волны по содержанию,
живёт в списке сборки и своей сцене, и не пересекается ни с одной из четырёх.

Самое рискованное допущение волны проверяется в первый час, а не в конце: мина сериализатора
FishNet — первый шаг `t-reach-wire-1`.

## cut_list
1. **Замах предметом.** Это анимационный клип плюс момент попадания плюс реакция; Final IK
   покрывает дотягивание и взятие, а не удар. Отдельная работа.
2. **Фазы безумия строятся.** В волне только ресёрч; ни одной модели.
3. **Хозяин замечает перемещённый лут.** Его прямая просьба, но без провода строить не на чем.
   Следующая волна.
4. **Двери с состоянием.** Поверхность B продуктового обзора целиком.
5. **Звук.** Назван им фактом, а не задачей: «я просто определил, что нет».
6. **Свет и материалы.** Свет становится данными, которые читают обе сцены, и делается это,
   когда дом станет переносимым ассетом. Смешивать съёмочную сцену с играемой запрещено уликой
   `i-measurement-taken-in-a-scene-that-does-not-ship-001`.
7. **Переписывание карточки узла.** Цель g-5a7c всё ещё описывает отменённую игру. Это работа
   для `map` с его подписью, а не для этой волны.

## riskiest_assumption
**ЧТО ПРОВОД ВООБЩЕ ПРОЙДЁТ.**

У FishNet нет своего сериализатора для наших типов, а ручной **проглотит новое поле снимка
молча** — не упадёт, а тихо приедет пустым. Мина названа заранее самой ногой, которая строила
дотягивание, и записана в `i-the-householder-reach-is-not-wired-to-the-game`.

Проверяется первым шагом `t-reach-wire-1`, ДО добавления поля, и стоит меньше часа.

## kill_by
Порогов четыре, дат ни одной.

1. Провод не проходит из-за сериализатора, а обход стоит дороже одной задачи → волна встаёт,
   вопрос владельцу.
2. Авторинг хвата на ВТОРОМ предмете стоит дороже первого → процесс не работает, полоса
   `анимация` умирает и уходит в ресёрч.
3. Перенос словаря ломает хоть один из 526 тестов, и починка не влезает в задачу → откат,
   реакции остаются в C#.
4. Ресёрч не даёт ни одного варианта дешевле, чем «отдельная модель на каждую фазу» → фазы
   уходят из волны целиком.

## lenses
| Линза | Вердикт |
|---|---|
| 1. Демо и выпуск | Задача — без действующего хозяина демо не о чем |
| 2. Понятность игроку | Задача — сегодня его движений в игре не видно ни секунды |
| 3. Потянет ли один | Задача — 7 против доказанного потолка 11; полоса `ресёрч` кода не трогает |
| 4. Новый канон | Задача — «ритуалы хозяина и их разгадывание» это и есть |
| 5. Платный продукт | `not_needed`: волна внутренняя, внешняя веха взята пятой волной |
| 6. Надёжность работы | Задача — четыре полосы против простоев, названных им самим |
| 7. Проще или сложнее | **Проще.** `t-reactions-data-1` УБИРАЕТ слой: реакции уезжают из кода в текст |

## forecast
**Волна впервые за узел делает игру, а не картинку, и впервые уменьшает количество кода.**

Раннейший сигнал — проверка сериализатора FishNet в первый час `t-reach-wire-1`. Она решает,
есть ли у полосы `провод` предмет вообще.

Второй по раннести — цена авторинга хвата на втором предмете. Она решает, существует ли
«нормальный процесс создания анимации», о котором он спросил, или его придётся изобретать.

## against
**ПЕРВОЕ — волна не даёт ни одного нового кадра.** После пятой волны, которая вся была про
кадры, это выглядит остановкой. Ставлю потому, что дальше кадры снимать не с чего: хозяин в
игре не делает руками ничего, а новая игра вся про то, что он делает.

**ВТОРОЕ — «ничего не срезаем» при потолке в четыре дня.** Пятая волна взяла четыре дня именно
резкой. Ставлю потому, что порядок смерти согласован заранее, и это дешевле, чем резать сейчас
то, что может влезть.

**ТРЕТЬЕ — полоса `словарь` трогает самое обтестированное место проекта.** 526 тестов стоят
вокруг ядра хозяина. Ставлю потому, что перенос реакций — это переезд, а не переписывание:
`HouseholderReactionDefinition` уже 13 именованных полей, из которых 9 перечисления. Порог
смерти 3 написан ровно под это.

**ЧТО ЭТОТ ДОВОД НЕ ОСПАРИВАЕТ:** его собственный вывод, что каталог реакций надо чинить.
Одиннадцать — не потолок, а ценник, и один из двух ценников снимается насовсем.

## журнал
2026-08-27 · САМОЕ РИСКОВАННОЕ ДОПУЩЕНИЕ ВОЛНЫ СНЯТО В ПЕРВЫЙ ЧАС И ОКАЗАЛОСЬ ЛОЖНЫМ: новое поле снимка доезжает, и доказано это ПОСЕВОМ мины, а не рассуждением — написан ровно тот ручной сериализатор, которого боялся наряд, и он молча привёз null. Одно из двух пересечений, найденных перемером границ, НЕ СОСТОЯЛОСЬ: пятое слово словаря не понадобилось, потому что вещь не новый ответ на вопрос «откуда взялась позиция цели». Провод доставлен, поведение не изменилось ни на кадр, и нога сказала это сама · history/2026-08-27-s-work-g-5a7c-reach-wire-return-001.md
2026-08-27 · ПЕРЕМЕР ГРАНИЦ ПО ВОПРОСУ ВЛАДЕЛЬЦА «какие наряды я могу запускать параллельно» НАШЁЛ ДВА ПЕРЕСЕЧЕНИЯ, которых нарезка не увидела и объявила ноль: провод и словарь сходятся на HouseholderReaction.cs и HouseholderProfileLoader.cs, если проводу понадобится пятая цель действия «вещь» — сегодня их четыре и ни одна не вещь; провод и починка сходятся на IntegratedHouse.unity, а сцену Unity, которую правят двое, потом никто не открывает. Оба разведены владением файла, ни одна нога не отложена: провод стартует в нулевой час, потому что вся его работа со стороны Network/** свободна от обоих — HouseholderSnapshot наполняют ровно три файла, и все три там · history/2026-08-27-s-shape-g-5a7c-wave-6-001.md
2026-08-27 · ШЕСТАЯ ВОЛНА ОТКРЫТА ЕГО СЛОВОМ «давай волну A», И ЭТО ПЕРВАЯ ВОЛНА ПОД НОВЫМ КОНЦЕПТОМ. Цель — хозяин делает вещи в игре, а не на стенде. Замер, с которого всё началось: вся его жизнь это два таймера в json, а шов к миру умеет только читать. Его вопрос «у нас что максимум может быть 11 реакций» получил измеренный ответ — не потолок, а два разных ценника, и первый из них поднят из довеска в полосу, идущую первой. Четыре полосы с границами, посчитанными по файлам, а не объявленными; аппетит четыре дня его словом, и вместо резки согласован порядок смерти · history/2026-08-27-s-shape-g-5a7c-wave-6-001.md

END_OF_FILE: live/indie-game-development/cards/bet-g-5a7c-wave-6.md
