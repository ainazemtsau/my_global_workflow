---
id: i-world-marks-have-nobody-to-draw-them-001
_kind: issue
level: objective
route: shape
status: open
_pos: 159
evidence: work/2026-08-30-call-thing-missing-1.md
---

## issue
**МЕТКИ МИРА ОТДАНЫ НАРУЖУ, И РИСОВАТЬ ИХ НЕКОМУ.**

Строка `done_when` задачи `t-thing-missing-1` — «**ВИДНО В ОКНЕ НАБЛЮДЕНИЯ.** Метки мира
показываются там же, где остальная цепочка» — **разрезана самим нарядом по двум полосам**.
Дословно, `work/2026-08-30-call-thing-missing-1.md:53-55`:

> Рисует полосу `полигон`; твоё дело — чтобы метки было ЧТО показать.

Половина полосы `ядро` отдана и проверена: `Core/Householder/HouseholderStepResult.cs:167` несёт
`WorldMarks` рядом с `DecisionCandidates`, тем же швом.

**ВТОРАЯ ПОЛОВИНА НЕ НАЧАТА И НИ В ОДНОЙ КАРТОЧКЕ НЕ ЧИСЛИТСЯ.** Перемерено на `f6fc0918`:
`grep -rn "WorldMarks" --include=*.cs Assets/` даёт совпадения ТОЛЬКО внутри
`Assets/TunnelCrew/Core/Householder/`; в `Assets/TunnelCrew/Editor/**` слова `Marks` нет вообще —
ноль совпадений. У полосы `полигон` в восьмой волне стоит одна задача, `t-bench-place-1`, и она
про другое.

**ПОЧЕМУ ЭТО НЕ МЕЛОЧЬ.** Восьмая волна перерезана ради того, чтобы владелец руками проверил
гипотезу про хозяина: спрятал кружку — посмотрел, что тот сделал. Метка мира — это то, чем
«кружку спрятали» входит к нему в голову. Пока её не рисуют, проверка идёт вслепую: поведение
видно, а вход, который его вызвал, — нет.

**ЗАКРЫТИЕ `t-thing-missing-1` ЭТУ ПОЛОВИНУ НЕ ПОКРЫВАЕТ.** Задача закрыта по факту 2026-08-30
плеем `repair`; долг вынесен сюда, чтобы он нашёлся, а не растворился в закрытой карточке.
## review_when
`shape` — при нарезке следующей волны либо при первом же наряде на полосу `полигон`.
Снимается кодом в `Assets/TunnelCrew/Editor/HouseholderObservation*`, читающим
`HouseholderStepResult.WorldMarks`.
## журнал
2026-08-30 · записи догнали продукт: три ноги закрыты по факту с перевыводом done_when из байтов f6fc0918, две карточки заведены задним числом, замер ядра хозяина перемерен, два долга вынесены уликами · history/2026-08-30-s-repair-g-5a7c-three-legs-behind-001.md
END_OF_FILE: live/indie-game-development/cards/i-world-marks-have-nobody-to-draw-them-001.md
