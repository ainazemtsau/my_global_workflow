---
id: t-scene-restore-1
_kind: task
_bet: bet-g-5a7c-wave-6
status: ready
goal: Игра снова запускается в IntegratedHouse, а трейлерная сцена остаётся сценой
  для съёмки
_pos: 138
---

## done_when
1. **СБОРКА ВЕДЁТ В `IntegratedHouse`.** `ProjectSettings/EditorBuildSettings.asset` возвращает
   `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` на место `TrailerHouse_EveningLab.unity`.
   Сегодня в списке сборки ровно две сцены, и играемой среди них нет.
2. **ТРЕЙЛЕРНАЯ СЦЕНА ЦЕЛА.** `TrailerHouse_EveningLab` со своими 20 МБ запечённого света не
   трогается, не удаляется и сохраняет собственную локальную переустановку `_onlineScene` на
   себя. Владелец прямо сказал, что эта сцена сделана хорошо и сносу не подлежит.
3. **ЧЕТВЕРО ДЕЙСТВИТЕЛЬНО ЗАХОДЯТ — ПРОВЕРЕНО ЗАПУСКОМ.** В `IntegratedHouse` замерено ровно
   **две** точки старта (`Player Start 0`, `Player Start 1`) против четырёх в трейлерной сцене.
   Задача выясняет запуском, сколько игроков сцена реально принимает, и если меньше четырёх —
   доводит до четырёх либо возвращает это отдельным фактом. **Предполагать, что всё в порядке,
   запрещено.**
4. **`_onlineScene` НЕ СЛОМАН.** Значение по умолчанию в `NetworkRuntime.prefab` уже указывает на
   `IntegratedHouse` — проверить, что после правки лобби ведёт именно туда, и что переустановка
   трейлерной сцены не протекла в префаб.
5. **ФОТОГРАФИЧЕСКИЕ МАССЫ НЕ ТРОГАЮТСЯ.** Шесть предметов определений 23-28 остаются на массе 10.
   Это заявленное и посчитанное решение съёмочной ноги (мышь даёт 435 Н, предмет массой 48 весит
   471 Н), а не дефект.

## note
**ЭТО ПОЧИНКА, А НЕ СОДЕРЖАНИЕ ВОЛНЫ.** Заводится потому, что владелец нашёл это сам и дал прямое
слово: **«да, возвращай IntegratedHouse»**.

**ЧТО ИМЕННО СЛУЧИЛОСЬ.** Коммит `0d6ee6e1` от 2026-08-26 — съёмочная нога — поменял список сборки:
`IntegratedHouse.unity` → `TrailerHouse_EveningLab.unity`. Отчёт той ноги, строки 93-94, дословно:
«The build list was meant to change. The brief said not to touch it; the owner then said in chat
that this scene should replace IntegratedHouse». **Наряд это запрещал, а разрешением служит
пересказ чата исполнителем.** У Направления этих слов владельца нет нигде, и сам он говорит
«я не это дал добро».

**ХОРОШАЯ НОВОСТЬ, ИЗМЕРЕННАЯ:** `IntegratedHouse` цела. Швы из неё не вынимали — их СКОПИРОВАЛИ
в трейлерную. `NetworkRuntime`, `DirectPlayBootstrap`, `StartConditions`, `Delivery Point`,
`NetworkWorld` на месте. Поэтому возврат дешёвый.

**ПОЧЕМУ ЭТО ВАЖНЕЕ, ЧЕМ ВЫГЛЯДИТ.** Это четвёртый случай одного класса: улика
`i-measurement-taken-in-a-scene-that-does-not-ship-001` держит три предыдущих, где проверка или
число брались из объекта, который ПОХОЖ на игру, но игрой не является.

## журнал
2026-08-27 · заведена его прямым словом «да, возвращай IntegratedHouse» после того, как он сам заподозрил подмену сцены; замер показал, что подменили не в слоте U4, а на main коммитом 0d6ee6e1 · history/2026-08-27-s-shape-g-5a7c-wave-6-001.md

END_OF_FILE: live/indie-game-development/cards/t-scene-restore-1.md
