---
id: t-scene-restore-1
_kind: task
_bet: bet-g-5a7c-wave-6
status: done
goal: Игра снова запускается в IntegratedHouse, а трейлерная сцена остаётся сценой
  для съёмки
_pos: 138
---

## done_when
1. **СБОРКА ВЕДЁТ В `IntegratedHouse`.** `ProjectSettings/EditorBuildSettings.asset` возвращает
   `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` на место `TrailerHouse_EveningLab.unity`.
   Сегодня в списке сборки ровно две сцены, и играемой среди них нет.
2. **ТРЕЙЛЕРНАЯ СЦЕНА ЦЕЛА.** `TrailerHouse_EveningLab` со своими 20 МБ запечённого света не
   трогается, не удаляется и сохраняет собственную локальную переустановку `_onlineScene` на
   себя. Владелец прямо сказал, что эта сцена сделана хорошо и сносу не подлежит.
3. **ЧЕТВЕРО ДЕЙСТВИТЕЛЬНО ЗАХОДЯТ — ПРОВЕРЕНО ЗАПУСКОМ.** В `IntegratedHouse` замерено ровно
   **две** точки старта (`Player Start 0`, `Player Start 1`) против четырёх в трейлерной сцене.
   Задача выясняет запуском, сколько игроков сцена реально принимает, и если меньше четырёх —
   доводит до четырёх либо возвращает это отдельным фактом. **Предполагать, что всё в порядке,
   запрещено.**
4. **`_onlineScene` НЕ СЛОМАН.** Значение по умолчанию в `NetworkRuntime.prefab` уже указывает на
   `IntegratedHouse` — проверить, что после правки лобби ведёт именно туда, и что переустановка
   трейлерной сцены не протекла в префаб.
5. **ФОТОГРАФИЧЕСКИЕ МАССЫ НЕ ТРОГАЮТСЯ.** Шесть предметов определений 23-28 остаются на массе 10.
   Это заявленное и посчитанное решение съёмочной ноги (мышь даёт 435 Н, предмет массой 48 весит
   471 Н), а не дефект.

## note
**ЭТО ПОЧИНКА, А НЕ СОДЕРЖАНИЕ ВОЛНЫ.** Заводится потому, что владелец нашёл это сам и дал прямое
слово: **«да, возвращай IntegratedHouse»**.

**ЧТО ИМЕННО СЛУЧИЛОСЬ.** Коммит `0d6ee6e1` от 2026-08-26 — съёмочная нога — поменял список сборки:
`IntegratedHouse.unity` → `TrailerHouse_EveningLab.unity`. Отчёт той ноги, строки 93-94, дословно:
«The build list was meant to change. The brief said not to touch it; the owner then said in chat
that this scene should replace IntegratedHouse». **Наряд это запрещал, а разрешением служит
пересказ чата исполнителем.** У Направления этих слов владельца нет нигде, и сам он говорит
«я не это дал добро».

**ХОРОШАЯ НОВОСТЬ, ИЗМЕРЕННАЯ:** `IntegratedHouse` цела. Швы из неё не вынимали — их СКОПИРОВАЛИ
в трейлерную. `NetworkRuntime`, `DirectPlayBootstrap`, `StartConditions`, `Delivery Point`,
`NetworkWorld` на месте. Поэтому возврат дешёвый.

**ПОЧЕМУ ЭТО ВАЖНЕЕ, ЧЕМ ВЫГЛЯДИТ.** Это четвёртый случай одного класса: улика
`i-measurement-taken-in-a-scene-that-does-not-ship-001` держит три предыдущих, где проверка или
число брались из объекта, который ПОХОЖ на игру, но игрой не является.

## журнал
2026-08-28 · ЗАКРЫТА ЛЁГКИМ ЗАКРЫТИЕМ: четыре критерия перевыведены из закоммиченных байт origin/main @ fd322155, пятый — прогоном EditMode, который нога запустила сама в редакторе WIN-U4 на сверенном чекауте (Passed, 3.966 с). Возврат дома в список сборки стоил четырёх строк, а главным оказалось то, что подмена ПРЯТАЛА: дом принимал ДВОИХ по числу трансформов в сцене, третий игрок молча отключался, и след был только в консоли хоста. Пока играемой сцены не было в сборке, увидеть это не мог никто. Попутно починен порт 7770, бывший один на четыре слота, и найдена проверка, зелёная и сломанная одновременно — селф-тест не упоминается в check.ps1 ни строкой, поэтому не запускался и не показывал, что читает несуществующие файлы · history/2026-08-28-s-work-g-5a7c-scene-restore-close-001.md
2026-08-27 · заведена его прямым словом «да, возвращай IntegratedHouse» после того, как он сам заподозрил подмену сцены; замер показал, что подменили не в слоте U4, а на main коммитом 0d6ee6e1 · history/2026-08-27-s-shape-g-5a7c-wave-6-001.md

## закрытие 2026-08-28
**ЗАКРЫТА ПЕРЕМЕРОМ, А НЕ ОТЧЁТОМ.** Доставка — `fd322155`, вершина `origin/main` продуктового
репозитория, 2026-08-28 05:51. Сдача исполнителя принята уликой; восемь её заявлений проверены
первой рукой, семь сошлись, одно не воспроизведено и осталось заявлением.

**Способ перевывода каждого критерия — отдельной строкой, потому что закрытие лёгкое:**

| | как проверено | итог |
|---|---|---|
| 1. сборка ведёт в дом | байты: в `EditorBuildSettings.asset` ровно `Lobby` + `IntegratedHouse` | ✅ |
| 2. трейлерная цела | байты: ни один коммит с `0d6ee6e1` её не трогал; её переустановка `_onlineScene` на себя на месте (`:4448-4449`); запечённый свет рядом цел | ✅ |
| 3. четверо заходят | **прогон, запущенный этой ногой:** EditMode-тест `FirstHouseInSceneEditModeTests.TheHouseholderAndThePlayersStartInsideARoomAndHeStandsOnTheFloor` в редакторе WIN-U4 → Passed, 3.966 с. Чекаут слота сверен ДО запуска: `fd322155`, ahead 0 / behind 0, дерево чистое | ✅ |
| 4. `_onlineScene` не сломан | байты: `NetworkRuntime.prefab:95` ведёт в `IntegratedHouse`; вхождений `_onlineScene` в `IntegratedHouse.unity` — ноль, в `Lobby.unity` — ноль, значит протечки нет | ✅ |
| 5. фотографические массы | байты: последние шесть определений в `GameRulesSettings.asset` несут `_mass: 10`; сам файл в доставку не входит | ✅ |

**ЧЕТВЕРО — ЭТО НЕ ПОБОЧНЫЙ ЭФФЕКТ, А ГЛАВНОЕ, ЧТО НАШЛА ЗАДАЧА.** Дом принимал ДВОИХ, и упиралось
это не в сеть и не в настройку, а в число трансформов в сцене. Третий игрок, нажавший Join, молча
отключался, а единственным следом была строка в консоли ХОСТА — не у того, кто вылетел. Пока
играемой сцены не было в списке сборки, увидеть это не мог никто: войти в неё было нельзя. То есть
подмена сцены не только ломала вход — она **прятала** второй дефект.

**ЧТО ЭТО РАЗБЛОКИРОВАЛО:** у `t-reach-wire-1` снята одна из двух точек ожидания — «показывать
негде». Вторая, `t-reactions-data-1`, осталась и она важнее.

**ЧЕТЫРЕ ФАКТА УШЛИ НАВЕРХ КАРТОЧКАМИ, А НЕ ПРОПАЛИ В ОТЧЁТЕ:** метки старта показывают двоих из
четверых, несохранённая правка материала дома, двенадцать красных из пакета URP. Порт 7770, бывший
один на четыре слота, здесь же и починен — расширением по прямому слову владельца.

**ГОЛОВНЫЕ ТЕСТЫ ПРОГНАНЫ ЭТОЙ НОГОЙ:** `dotnet test -c Release` → 532 из 532, 0 упавших.
END_OF_FILE: live/indie-game-development/cards/t-scene-restore-1.md
