---
id: bet-g-5a7c-wave-12
_kind: bet
node: g-5a7c
opened: 2026-09-03
_pos: 193
---

## owner_word
**«все принимаю»** — 2026-09-03, на показанный целиком экран волны: цель, замах с внешним взглядом,
десять задач в четырёх полосах, порядок смерти из шести строк, шесть вырезов, решение о звуке,
шесть кусков спеки давления, семь линз, самое рискованное допущение, семь порогов смерти, прогноз
и довод против, шов удара для стенда.

Слово принимает и порядок смерти отдельной строкой — он был показан как второй вопрос.

**ЕМУ ПРЕДШЕСТВОВАЛО ЕГО ЖЕ СЛОВО НА ПЛАН, 2026-09-03:** «я вижу смысл, мне нравится, можем по ней
работать» и «я хочу, чтобы автономно уже по нашему плану, что мы составили, чтобы всё работало…
волну 12 будем нарезать в новом чате». Дата 11.09 и список рубежа приходят оттуда, а не из этой
нарезки: волна = рубеж, аппетит кончается в дату.

**ЧЕГО ЕГО СЛОВО НЕ ПОКРЫВАЕТ, И ЭТО НАЗВАНО ЗДЕСЬ.** Три слова раздела 11.1 плана (путь полного
падения по сети; Y Bot как запасное тело; слух хозяина через стены) он в этой сессии не дал; они
были положены ему на стол до первого шага плея. Рубеж Р1 они не держат. И квитанции
`concept_basis` нет — волна техническая, см. блок `concept_basis`.
## goal
**УТРО КОНЧАЕТСЯ ТЕМ, ЧТО ОН ВЫХОДИТ ЗА ДВЕРЬ, И РАУНД ПОКАЗЫВАЕТ, ВО СКОЛЬКО ЭТО ОБОШЛОСЬ.**

Рубеж Р1 «Скелет утра» принятого плана `work/2026-09-03-demo-plan-to-october-5.md`, раздел 5.
Срок — пятница 11.09. Полное утро от будильника до экрана результата: уродливое, но целое, и его
можно дать жене 12.09, а вечером 12.09 четверо на четырёх машинах проходят раунд.

Что владелец увидит сам: запускает, мешает хозяину пять минут, тот выходит за дверь, экран говорит
«вышел в T+…» и даёт звёзды, «ещё раз» перезапускает без лобби. И впервые видно глазом, **почему**
хозяин потерял равновесие.

**ЗАМЕРЫ СНЯТЫ ПРИ САМОЙ НАРЕЗКЕ 2026-09-03, ПЕРВОЙ РУКОЙ, НА `main` = `dev` = `origin/main` =
`origin/dev` = `2d2c27ff`.** Четыре слота продукта чисты и влиты в `main` без остатка
(`slot/win-u1..u4`, ноль коммитов вне `main`); custody-файл `gascoop-slot-state.v1.json` даёт всем
четырём `lifecycle: AVAILABLE`, `lease: none`.

1. **СЦЕНАРИЯ НА ЧЕТВЕРЫХ НЕТ КАК ФАЙЛА.** В `Assets/Settings/PlayMode/` ровно один файл —
   `HostAndOneClient.asset`. Это самая дешёвая строка рубежа: она снимает гейт «четыре экрана» с
   созванной компании на любой день.

2. **СБОРКА ОДНОЙ КОМАНДОЙ УЖЕ ЕСТЬ, И ПЛАН ЭТОГО НЕ ЗНАЛ.** `tools/unity-build.ps1` зовёт
   `-executeMethod TunnelCrew.Editor.PlayerBuildCommand.BuildWindows64`
   (`Assets/Editor/PlayerBuildCommand.cs`), кладёт `Builds/StandaloneWindows64/TunnelCrew.exe` и
   пишет машинный `BuildReport`. Не хватает ровно двух вещей: **номера версии и папки выдачи** —
   это `DefaultOutputDir` / `ExecutableName` плюс аргумент скрипта, а не новый скрипт.

3. **ДАВЛЕНИЕ ТЕМП НЕ ВЫБИРАЕТ.** Все 15 присвоений `MovementPaceName` вне тестов лежат в
   `Probe/Householder/HouseholderReaction.cs` (реакции на шум) и `Profiles/HouseholderProfileLoader.cs`
   (авторинг из профиля). В пути давления — `HouseholderTimeReserve.cs`, `HouseholderStanding.cs` —
   **ноль**. Строка рубежа «давление сжимает план и **не** ускоряет ходьбу» наполовину зелёная по
   чтению; живой пробы не хватает.

4. **ШОВ, КОТОРЫМ СТЕНД СТАВИТ УДАР, СУЩЕСТВУЕТ И НАЗВАН.** Расстановка — JSON
   `Assets/TunnelCrew/Editor/IntegrationLab.<имя>.json`, схема
   `tunnelcrew-integration-lab-arrangement.v2`; кусок `launched-cargo` несёт `definitionId`, старт
   `x/y/z` и скорость `velocityX/Y/Z`. Полка — `IntegrationLabWindow`, кнопки идут в
   `NetworkWalkerCourier.TryLoadLabArrangement / FireLabArrangement / ResetLabArrangement`;
   приборы возвращаются `LabShoveObserversRpc` в `IntegrationLabReadout`. Семь расстановок набора
   «мышь» уже лежат в той же папке. **Чего не хватает: видов куска три —
   `frozen-walker`, `launched-cargo`, `hovering-cargo`; мишени-хозяина среди них нет.**

5. **ЛОКАЛИЗАЦИИ НЕТ ВОВСЕ, И ИМЕННО ПОЭТОМУ ОНА СЕЙЧАС ДЁШЕВА.** В `Packages/manifest.json` нет
   `com.unity.localization`; игрового текста — около пяти строк в `LobbyController.cs` плюс надписи
   сцены лобби.

6. **СВОИХ ЗВУКОВ НОЛЬ.** Поиск `*.wav|*.mp3|*.ogg` по `Assets/TunnelCrew` — ни одного файла;
   упоминания Feel живут в трёх сценах, включая `IntegratedHouse.unity`.
## done_when
**СПИСОК РУБЕЖА Р1 ПОСТРОЧНО. Каждая строка 11.09 либо зелёная по улике, либо красная. Жёлтых нет.**

| # | Строка рубежа | Задача | Вид улики |
|---|---|---|---|
| 1 | Д5 прошло: утро кончается выходом; экран «вышел в T+…» и ★; «ещё раз» без лобби; один человек на одной машине проходит раунд | `t-the-morning-ends-at-the-door-1` + `t-the-round-shows-what-it-cost-1` | живая проба в игре |
| 2 | план утра — кофе → кейс → одежда → выход + одна привычка; список на холодильнике; интерфейс «сейчас/дальше»; табличка старта | — | **ПЕРЕНОСИТСЯ БЕЗ ШТРАФА: строки содержания, квитанции `concept_basis` нет** |
| 3 | все надписи с первого дня идут через таблицу строк по ключам, английский и русский заполнены | `t-every-word-comes-from-a-key-1` | прогон + живая проба (переключение языка) |
| 4 | давление сжимает план и **не** ускоряет ходьбу | `t-the-morning-ends-at-the-door-1` | замер (сделан при нарезке) + живая проба |
| 5 | магнит к лёгкому предмету с двумя полюсами работает в доме | `t-the-magnet-has-two-poles-on-a-thing-1` | живая проба на стенде «мышь» и в доме |
| 6 | хозяин шлёпает увиденную рядом мышь; она отлетает, лежит, роняет мелочь | `t-he-slaps-the-mouse-he-sees-1` | живая проба на стенде «хозяин» |
| 7 | Д1 прошло: стенд «хозяин» построен, равновесие принято глазом — долг волны 11 закрыт | `t-the-bench-shows-why-he-fell-1` | **живая проба его глазом на двух экранах** |
| 8 | стенд «сеть»: создан сценарий Multiplayer Play Mode на четверых и четыре клиента видят одно событие равновесия | `t-four-screens-see-one-fall-1` | живая проба (четыре окна на одной машине) |
| 9 | лобби: имена, старт вчетвером; инструкция подключения через интернет написана и проверена с одним другом | `t-four-people-connect-over-the-internet-1` | живая проба со вторым человеком |
| 10 | скрипт сборки exe одной командой с номером версии в папку выдачи; четыре головных теста переехали с поставочного профиля на тестовую копию | `t-one-command-makes-a-named-build-1` | прогон (сборка зелёная) + живая проба (exe с чистой машины) |
| 11 | временные звуки для шести событий; в сборке нет чужих демо-клипов Feel | `t-six-events-make-a-sound-1` + `t-one-command-makes-a-named-build-1` | живая проба его ухом |

**ПРОГОН ОДИН СТРОКУ НЕ ЗАКРЫВАЕТ.** Урок волны 11, записанный в плане (раздел 10): зелёный прогон
дважды не предсказал работоспособность. Улика строки — живая проба на стенде или в игре; прогон
только сторожит.

**ЛЮДИ (раздел 7 плана — окна готовности сборки, не обязательство владельца).** 12.09 (сб) — жена
соло; вечером 12.09 — четверо на четырёх машинах, первая живая игра вчетвером в истории проекта.
Пакостей ещё нет, и это нормально.

**ЕСЛИ ЭКСПОРТ CONCEPT LAB ПРИДЁТ ДО 11.09** — квитанция вносится в этот блок ногой `work`, и
строка 2 входит в остаток волны без новой нарезки.
## appetite
**ДЕСЯТЬ ЗАДАЧ В ЧЕТЫРЁХ ПОЛОСАХ. АППЕТИТ КОНЧАЕТСЯ 11.09, А НЕ С РАБОТОЙ.**

Дата не изобретена нарезкой: она приходит из принятого им 03.09 плана, где сказано «волна = рубеж…
ставка берёт имя, дату и список рубежа целиком; аппетит кончается в дату». `NOW.track_wip_limit`
= 4, занято четыре.

**ВНЕШНИЙ ВЗГЛЯД, И ОН ПРОТИВ ЭТОЙ ВОЛНЫ.** Наблюдения прежних волн, названные до подписи:
4 задачи → дошла; 11 задач / 0 новых слоёв → дошла; 7 план (11 факт) → `partial`; 23 / 3 →
`partial`; 6 план (**14** факт) → `partial`. **Ни одна волна с планом больше семи задач не доходила
целиком.** Десять — за верхней границей, и это не оценка скорости, а счёт исходов.

Из этого следует не «сделать меньше» — список рубежа менять нельзя, — а **написать порядок смерти
до работы, а не в пятницу.**

**ПОРЯДОК СМЕРТИ.** Все строки Р1 — «Обязательно» (раздел 1.7), поэтому внутри яруса первым умирает
то, у чего есть запасной путь.

1. **`t-six-events-make-a-sound-1`** — тишина законна до 29.09 (раздел 8 плана), ни одно обещание
   рубежа на звуке не стоит.
2. **`t-four-people-connect-over-the-internet-1`** — умирает до «VPN как основной способ, инструкция
   без проверки другом» (риск 8 плана). Вечер 12.09 живёт на VPN или в одной сети.
3. **`t-the-magnet-has-two-poles-on-a-thing-1`** — Д3 целиком доказывается на Р2; Р1 просит только
   первую половину.
4. **`t-he-slaps-the-mouse-he-sees-1`** — удар груза остаётся единственным источником импульса;
   приёмник уже общий, третий вызывающий добавляется позже.
5. **`t-one-command-makes-a-named-build-1`** — сборка одной командой уже существует; умирает только
   номер версии и папка выдачи, exe копируется руками для 12.09.
6. **`t-every-word-comes-from-a-key-1`** — умирает **с названной ценой**: каждая надпись, написанная
   после 11.09, переделывается потом, и цена растёт после HUD и онбординга.

**НЕ УМИРАЮТ:** `t-the-bench-shows-why-he-fell-1` (единственный долг приёмки волны 11 и поверхность
для двух других задач), `t-the-morning-ends-at-the-door-1` и `t-the-round-shows-what-it-cost-1`
(у Д5 нет запасного пути — «без этого демо нет»), `t-four-screens-see-one-fall-1` (стоит один файл
и снимает гейт навсегда).

Порядок написан с учётом урока девятой волны: **первым на вылет не ставится единственное, что
владелец потрогает руками.** Здесь то, что он трогает, — утро и стенд, и они не умирают.
## tracks
**ЧЕТЫРЕ ПОЛОСЫ = ЧЕТЫРЕ СЛОТА РАЗДЕЛА 6 ПЛАНА.** Разведены по папкам продукта намеренно — урок
девятой волны, конфликт слияния на профиле. Одна нога = одна полоса = один слот.
Все четыре слота проверены при нарезке: `lifecycle: AVAILABLE`, `lease: none`.

### полоса `тело` — слот WIN-U4 — две задачи

**1. `t-the-bench-shows-why-he-fell-1` — СТЕНД «ХОЗЯИН»: ВИДНО, ОТ ЧЕГО ОН ПОТЕРЯЛ РАВНОВЕСИЕ.**
Единственный долг приёмки волны 11. Второй набор существующего `MouseLab`, не третья сцена (Р14).
Наряд написан 02.09 возвратом самой ноги; при выдаче к нему добавлен **шов удара** и снят
переоткрытый вопрос про `ReachLab` («не сводить» — ответ закрытой задачи).
**Приёмка — его глаз на двух экранах.** Первая нога волны; **не WIN-U2, его слово 02.09.**

**2. `t-he-slaps-the-mouse-he-sees-1` — ХОЗЯИН ШЛЁПАЕТ УВИДЕННУЮ РЯДОМ МЫШЬ.**
Третий вызывающий общего приёмника `AuthoritativeWalkerRoster.AddImpulse` (`:709`); первые два —
груз (`AuthoritativeCargoRoster.cs:352`) и нить (`AuthoritativeThreadRoster.cs:482`). Приёмник
источника не знает, поэтому это источник, а не новый путь.
Приёмка — на стенде из задачи 1: **поверхность режется в той же волне.**

### полоса `утро` — слот WIN-U3 — две задачи

**3. `t-the-morning-ends-at-the-door-1` — УТРО КОНЧАЕТСЯ ВЫХОДОМ. Голова волны.**
Пятый глагол «уйти»; `Front door` (`first-house.house.json:588`) как терминальная строка распорядка
вместо пяти одинаковых `walk-route` по 12 с; сигнал конца раунда наружу; утро растянуто со 120 с
до 5–8 минут. Плюс закрывается строка «давление не ускоряет ходьбу».
**Честно: это не полдня.** Самая тяжёлая строка рубежа и единственная без запасного пути.

**4. `t-the-round-shows-what-it-cost-1` — ЭКРАН РЕЗУЛЬТАТА И «ЕЩЁ РАЗ».**
«Вышел в T+…», ★ по опозданию, рестарт пересевом на хосте без выхода в лобби (Р9).
Берёт ключи из таблицы строк, поэтому задача 7 идёт первой в WIN-U2.

### полоса `мышь` — слот WIN-U1 — одна задача

**5. `t-the-magnet-has-two-poles-on-a-thing-1` — МАГНИТ К ЛЁГКОМУ ПРЕДМЕТУ, ОБА ПОЛЮСА, В ДОМЕ.**
Не только на стенде: строка рубежа говорит «работает в доме». Д3 частично — полюса к предмету;
полюса к товарищу, payload, толчок тяжёлого и вытягивание лежащей остаются на Р2.

### полоса `оболочка` — слот WIN-U2 — пять задач

**6. `t-four-screens-see-one-fall-1` — ЧЕТЫРЕ ЭКРАНА ВИДЯТ ОДНО СОБЫТИЕ.**
Сценарий Multiplayer Play Mode на четверых — файл, которого нет; затем четыре клиента видят одно
событие равновесия. Файл делается в первый день и независим; проверка четырёх экранов ждёт задачу 1.

**7. `t-every-word-comes-from-a-key-1` — КАЖДАЯ НАДПИСЬ ИЗ КЛЮЧА, АНГЛИЙСКИЙ И РУССКИЙ.**
**Идёт первой в полосе**, до первой новой надписи волны. Исключение Р15, названное вслух:
**приборы стендов не локализуются** — `IntegrationLabReadout` собирает строки в рантайме и адресован
разработчику.

**8. `t-one-command-makes-a-named-build-1` — СБОРКА С ИМЕНЕМ, И В НЕЙ НЕТ ЧУЖОГО.**
Номер версии (дата + коммит) и папка выдачи поверх существующего `tools/unity-build.ps1`; четыре
головных теста с поставочного профиля на тестовую копию; ноль чужих демо-клипов Feel в сборке.

**9. `t-four-people-connect-over-the-internet-1` — ЧЕТВЕРО ПОДКЛЮЧАЮТСЯ.**
Лобби с именами и стартом вчетвером; инструкция подключения через интернет (проброс порта или VPN),
проверенная с одним другом.

**10. `t-six-events-make-a-sound-1` — ШЕСТЬ СОБЫТИЙ ЗВУЧАТ.**
Удар, падение, шлепок, будильник, дверь, результат. Временные свободные звуки (Р13).

### ЧЕГО В ВОЛНЕ НЕТ, И ЭТО НАЗВАНО ЗАРАНЕЕ

Содержание утра без квитанции. Полное падение и вылет по сети. Окклюзия слуха хозяина. Стресс,
пакости, пушка, смена одежды, заметность мелких опасностей — рубежи Р2–Р3. Планировка трёх зон —
Р2. Мышь сквозь мышь — вне демо решением Р1 плана.
## cut_list
**ЧТО ОТРЕЗАНО ОТ ЭТОЙ ВОЛНЫ, ПОИМЁННО. Ни одна строка не говорит «работы не осталось» без замера.**

1. **СОДЕРЖАНИЕ УТРА — ОТЛОЖЕНО БЕЗ ШТРАФА.** План утра «кофе → кейс → одежда → выход», одна
   привычка, список на холодильнике, интерфейс «сейчас/дальше», табличка старта.
   *Причина:* квитанции `concept_basis` нет — неизменяемого экспорта документа демо из Concept Lab
   не существует, его слово 03.09: «сейчас там Concept Lab закрепляют, скоро будет».
   *Вернётся, когда:* экспорт придёт и квитанция ляжет в этот блок. **Конец утра как механизм —
   техническая строка и в волну входит.**

2. **ПОЛНОЕ ПАДЕНИЕ И ВЫЛЕТ ПО СЕТИ (Р2).** *Причина:* ждёт его слова, раздел 11.1 №1 плана; три
   пути названы, рекомендация — (а) корень плюс шесть ключевых костей, пока он не стоит.
   *Что должно случиться раньше:* его слово. Д2 в этой волне снимается **на окне равновесия**, где
   поза корня честна (наклон до 35°), и это не обход, а граница построенного.

3. **ОККЛЮЗИЯ СЛУХА ХОЗЯИНА — ОТЛОЖЕНА, И ЭТО НЕ «РАБОТЫ НЕ ОСТАЛОСЬ».** См. блок `lenses`,
   решение о звуке. Замер приложен там же: три радиуса в `GameRulesSettings.cs:263-265`, ноль
   совпадений `occlu|linecast|raycast` в слуховом пути, у зрения полноценный шов
   `HouseholderSightSource._fixedOccluderRoot`. Работы там на второй шов размером со зрение.
   *Причина отсрочки:* план сам ставит её в «Желательно» (Р16). *Раньше должно случиться:*
   зелёные обязательные строки Р1–Р2 и его слово (11.1 №3).

4. **ПЯТЬ ИЗ ШЕСТИ КУСКОВ СПЕКИ ДАВЛЕНИЯ ВРЕМЕНИ, И ШЕСТОЙ С ОГОВОРКОЙ.** Правило
   `what-the-time-pressure-spec-still-owes` требует слова по каждому:
   - **стресс целиком** — отложен; раньше должны заработать конец утра и видимое давление, а бросок
     и срыв — по новому глаголу каждый, и в этой волне уже есть один новый глагол;
   - **ограниченная случайность** — отложена; раньше должно появиться больше одного правдоподобного
     варианта (в игре сегодня ноль случайности по решению);
   - **полная формула `Score(a)`** — отложена; требует второго хозяина;
   - **пять каналов наблюдаемости** — отложены; требуют анимаций, вход 18–22.09;
   - **debounce и гистерезис** — отложены; нужны только под реплики и анимрежимы, которых нет;
   - **`B_host = β_host · T_baseline`** — отложено, **но с оговоркой этой волны**: утро удлиняется
     со 120 с до 5–8 минут, то есть `T_baseline` меняется в 3–4 раза. Числа `deadlineSeconds` и
     `comfortableBufferSeconds` перенастраиваются руками в профиле; вывод порога из безразмерного
     характера остаётся долгом, потому что на одном хозяине он не проверяется ничем.

5. **МЫШЬ СКВОЗЬ МЫШЬ.** Вне демо решением Р1 плана; Д3 её не требует. Вернуть может только его
   слово (уведомление 11.1, четвёртый пункт).

6. **ПЛАНИРОВКА ТРЁХ ЗОН, ВАННАЯ, ОКНО-ДЕКОРАЦИЯ, ТОЧКИ ЗИПА.** Рубеж Р2. Дом — данные
   (`first-house.house.json`, `walls` пустой, стены вычисляются), поэтому это правка комнат и
   проёмов, а не инструмент; ждёт своей строки, не своего механизма.

7. **ТРИ ПРИПАРКОВАННЫХ УЗЛА ФЕСТИВАЛЯ И СТАРЫЙ ТЕКСТ КАРТОЧКИ `g-5a7c`.** Улика
   `i-three-parked-nodes-and-two-cards-describe-a-closed-route-001`; чинит нога `map`, рекомендована
   на этой неделе, волну не блокирует. Авторитет для этой нарезки — устав, план и его слово.
## riskiest_assumption
**САМОЕ РИСКОВАННОЕ ДОПУЩЕНИЕ: ЧТО «КОНЕЦ УТРА» — ЭТО ОДИН ГЛАГОЛ И ОДНА ТЕРМИНАЛЬНАЯ СТРОКА,
А НЕ НОВЫЙ СЛОЙ.**

*Основание считать так.* Распорядок — данные (`StreamingAssets/TunnelCrew/Householder/default.householder.json`):
подъём и пять одинаковых `walk-route` по 12 секунд, четыре занятия. Место `Front door` в доме уже
есть (`first-house.house.json:588`). Глаголы авторятся из профиля через `HouseholderProfileLoader`,
и канал поступков (`HouseholderDeedName`) построен именно как расширяемый закрытый набор — его
собственный комментарий говорит, что второе слово стоит только исполнения.

*Основание сомневаться, и оно сильнее, чем выглядит.* **Сигнала «раунд кончился» наружу не
существует вообще** — не только у утра: улика `i-a-run-has-no-end` жива с июля и говорит про заход.
`leave-for-work` в `Assets/**` встречается пять раз — дважды как данные профиля и трижды в
комментариях `HouseholderProfileLoader.cs:880, :3613`; **ни одной строки кода, приводящей хозяина в
это состояние, нет.** То есть вопрос не «как назвать пятый глагол», а «кто владеет концом раунда и
кто его слышит» — это жизненный цикл, а не слово в словаре.

*Как узнаем, что допущение неверно.* Первая нога полосы `утро` упрётся в необходимость завести
управляющий раунда, новую сцену-оболочку или второй источник правды о том, что раунд идёт. Тогда
она **обязана остановиться и вернуть числа**, а не строить: порог 1 блока `kill_by`.

**ВТОРОЕ ДОПУЩЕНИЕ.** Что окно равновесия выдержит на четырёх виртуальных экранах при одной лишь
позе корня. Замер review 03.09: клиентская кукла кинематическая, `collisionLayers` и `groundLayers`
обнуляются при отсутствии авторитета (`HouseholderBalanceBody:161-162`); снапшот несёт восемь полей
равновесия и сверен пополю. Для наклона до 35° этого достаточно. Если разойдётся — дорогая половина
Д2 вылезла раньше срока, и это вопрос по 11.1 №1, а не работа волны.

**ТРЕТЬЕ.** Что локализация сейчас дёшева: пакета нет, текста около пяти строк. Если пакет
локализации потянет рантайм-зависимость, ломающую сборку, — откат к простому словарю по ключам
(порог 5).

**ЧЕТВЁРТОЕ.** Что номер версии — правка двух констант в `PlayerBuildCommand.cs` и аргумента в
`unity-build.ps1`, а не новый скрипт сборки.
## kill_by
**СЕМЬ ПОРОГОВ. Каждый — про остановку и вопрос домой, а не про «сделать поменьше».**

1. **КОНЕЦ УТРА ПОТРЕБОВАЛ УПРАВЛЯЮЩЕГО РАУНДОМ ИЛИ НОВОЙ СЦЕНЫ-ОБОЛОЧКИ.** СТОП и числа домой.
   Это жизненный цикл, а не глагол, и решать его не ноге.

2. **ПРИШЛОСЬ ПРЕВРАТИТЬ ПОЛЕ В РЕЕСТР В `Network/CargoBody.cs:36`** (`_householderPuppet` — одно
   поле, не список). Откат. Вторая кукла — это решения Р2 и Р3, не Р1; названо заранее, потому что
   план говорит «не трогать», а первая же вторая кукла заставит тронуть именно это.

3. **НА ЧЕТЫРЁХ ЭКРАНАХ ОКНО РАВНОВЕСИЯ РАЗОШЛОСЬ.** СТОП и вопрос владельцу по разделу 11.1 №1:
   дорогая половина Д2 вылезла раньше срока, и выбор пути (а)/(б)/(в) — его.

4. **ПРИШЛОСЬ ПОГАСИТЬ ХОТЬ ОДИН ТЕСТ.** Запрещено гигиеной абсолютно (`tools/hygiene.ps1:363-376`:
   `[Ignore]` — всегда бан, `Assert.Ignore` — тихий пропуск под зелёными воротами). Тест гасится
   только починкой. Замер до волны: headless `819 / 819 / 0`, снят review 03.09 на `2d2c27ff`.

5. **ЛОКАЛИЗАЦИЯ ПОТРЕБОВАЛА ПАКЕТА С РАНТАЙМ-ЗАВИСИМОСТЬЮ, ЛОМАЮЩЕЙ СБОРКУ.** Откат к простому
   словарю по ключам без пакета. Цель строки — чтобы текста не было в коде и сценах, а не чтобы в
   проекте появился конкретный пакет.

6. **ЛЮБАЯ НОГА НАЧАЛА ЗАВОДИТЬ СТРОКИ СОДЕРЖАНИЯ УТРА БЕЗ КВИТАНЦИИ `concept_basis`.** Откат.
   Граница authority устава: концепт входит только неизменяемым экспортом Concept Lab.

7. **НАСТУПИЛО 11.09.** Волна закрывается тем, что есть, по порядку смерти из блока `appetite`.
   Аппетит кончается в дату — его слово на план. Красная строка «Обязательно» в этот день идёт по
   правилу красной строки раздела 10 плана, а не по молчаливому переносу.
## lenses
**ЧЕРЕЗ ЧТО СМОТРЕЛИ ПРИ НАРЕЗКЕ.**

**ОБЯЗАТЕЛЬНОЕ РЕШЕНИЕ ПРО ЗВУК** — правило `the-sound-decision-is-taken-at-every-cut`

Обе карточки прочитаны, обе `status: open`: `i-a-living-day-breaks-a-promise-that-rested-on-emptiness-001`
и `idea-owner-hearing-is-decided-by-distance-001`.

**РЕШЕНИЕ: ПОЛОВИНА БЕРЁТСЯ, ПОЛОВИНА ОТКЛАДЫВАЕТСЯ, И ОБЕ НАЗВАНЫ.**

- **Берётся:** ухо игрока — временные звуки шести событий, задача `t-six-events-make-a-sound-1`.
  Это оформление (Р13), и оно дёшево: своих аудиофайлов в `Assets/TunnelCrew` сегодня ноль.
- **Откладывается:** окклюзия слуха хозяина. *Почему:* план сам ставит её в «Желательно» (Р16) —
  документ демо требует от хозяина только «слышит по расстоянию», и это уже построено.
  *Что должно случиться раньше:* зелёные обязательные строки Р1–Р2 и его слово (11.1 №3).

**И ЭТО НЕ ТРЕТИЙ, НЕЗАКОННЫЙ ОТВЕТ.** Волна 11 написала «работы не осталось» и была опровергнута.
Здесь замер приложен и говорит обратное: у слуха три радиуса
(`GameRulesSettings.cs:263-265` — `_cargoRustleHearingRadius`, `_cargoKnockHearingRadius`,
`_cargoHeavyNoiseHearingRadius`), поиск `occlu|linecast|raycast` по слуховому пути даёт **ноль**,
а у зрения полноценный шов — `HouseholderSightSource._fixedOccluderRoot` и `SightOutcome.Occluded`.
**Работы там на второй шов размером со зрение. Отложено, а не исчерпано.**

**СЕМЬ ЛИНЗ УСТАВА, ВЕРДИКТ ПО КАЖДОЙ**

1. **Приближает демо 05.10, публичное демо 26.10 и выпуск?** Да. Р1 — скелет, на котором стоят все
   остальные рубежи: без конца раунда нет ни ★, ни вечеров, ни холодных тестов.
   → `t-the-morning-ends-at-the-door-1`, `t-the-round-shows-what-it-cost-1`, `t-four-screens-see-one-fall-1`.
2. **Понятность и качество для игрока?** Да — впервые у раунда есть конец и счёт; и впервые видно,
   отчего хозяин упал. → задачи 3, 4 и 1.
3. **Реально одному закончить и поддерживать?** **На грани, и это названо числом.** Десять задач при
   историческом потолке доходивших в семь. Закрыто не обещанием, а порядком смерти.
4. **Основано на закреплённом неизменяемом входе Concept Lab?** **НЕТ, и это названо.** Квитанции
   `concept_basis` нет; волна техническая, строки содержания перенесены (вырез 1).
5. **Приближает платный продукт и коммерческое знание?** `not_needed` — волна внутренняя; страница
   и транспорт Steam живут вне этого плана (после 05.10).
6. **Сохраняет надёжность работы владельца?** Да: четыре полосы разведены по папкам раздела 6,
   одна нога = один слот. **Но WIN-U2 несёт пять задач из десяти** — названо в блоке `against`.
7. **Проще или сложнее?** Сложнее на пятый глагол и таблицу строк. Взамен вырезано семь пунктов:
   содержание утра, падение по сети, окклюзия слуха, пять кусков спеки, мышь сквозь мышь,
   планировка трёх зон, дерево узлов.
## forecast
**КАЖДАЯ СТРОКА НАЗЫВАЕТ ФАЙЛ, ГРАНИЦУ ИЛИ ЧИСЛО, КОТОРОЕ ПЕРВАЯ ЖЕ НОГА МОЖЕТ ОПРОВЕРГНУТЬ.**
Правило `a-forecast-line-is-a-cost-claim-and-must-be-refutable`, заведённое после того, как три
строки прогноза волны 11 из четырёх оказались неверны — и в обе стороны.

- **Стенд не трогает механику равновесия:** `Network/HouseholderSnapshot.cs` не расширяется **ни на
  байт**. Опровергается диффом одного файла в день сдачи задачи 1.
- **Сценарий на четверых — один новый файл** в `Assets/Settings/PlayMode/` и **ноль строк C#**.
  Опровергается `git show --stat` наряда.
- **Локализация не трогает `Assets/TunnelCrew/Probe/**` ни одной строкой** и умещается примерно в
  пять существующих строк `LobbyController.cs` плюс надписи сцены лобби.
- **Сборка — правка `DefaultOutputDir` / `ExecutableName` в `Assets/Editor/PlayerBuildCommand.cs`
  и аргумента в `tools/unity-build.ps1`; нового скрипта не появляется.** Если появится — прогноз
  неверен, и сборка стоила больше, чем думала нарезка.
- **Шлепок — третий вызывающий `AuthoritativeWalkerRoster.AddImpulse` (`:709`), сигнатура приёмника
  не меняется.** Если её придётся менять — прогноз неверен.
- **Конец утра — самая дорогая задача волны.** Тронет `Probe/Householder/HouseholderVocabulary.cs`,
  `Profiles/HouseholderProfileLoader.cs`, `StreamingAssets/TunnelCrew/Householder/default.householder.json`
  и заведёт один исходящий сигнал. **Если она тронет `Network/CargoBody.cs` или что-либо в
  `Network/**` — допущение о глаголе было неверным**, и это сработавший порог 1.

**СИГНАЛ, КОТОРОГО НЕ БУДЕТ.** Ни одна отдельная задача не покажет «демо». Утро станет играемым
только когда сойдутся задачи 3 и 4, а до того полоса будет выглядеть как работа без картинки.

**СИГНАЛ, КОТОРЫЙ БУДЕТ, И ОН РАННИЙ.** Файл сценария на четверых и таблица строк ложатся в первый
день. Если к концу первого дня их нет — виновата не работа, а слот.
## against
**ДОВОД ПРОТИВ ЭТОЙ ВОЛНЫ, И ОН НЕ СЛАБЫЙ.**

**Пять задач из десяти сидят в одной полосе — оболочке, и ни одну из них владелец не потрогает
руками.** Сценарий, таблица строк, версия сборки, лобби, временные звуки — всё это инфраструктура.
А весь смысл рубежа («дать жене сборку 12.09») висит на одной полосе `утро`, и её первая задача —
самая тяжёлая строка рубежа и единственная без запасного пути. **Если утро встанет на жизненном
цикле раунда, волна выдаст аккуратную оболочку вокруг утра, которое по-прежнему никуда не кончается,
и вечеру 12.09 играть будет не во что.**

Честная альтернатива, которая была рассмотрена и отвергнута: отдать концу утра две полосы — ядро в
`утро`, экран результата в `оболочку`. Отвергнута по двум причинам: на девятой волне две полосы в
одной папке дали конфликт слияния, и таблица строк обязана лечь до первой надписи, а это само по
себе работа `оболочки`.

**ВТОРОЙ ДОВОД, И ОН ПРО НАПРАВЛЕНИЕ, А НЕ ПРО ПРОДУКТ.** Review волны 11 показал, что карта
стоимости была неверна: три строки прогноза из четырёх опровергнуты, а самая дорогая часть волны —
полоса стенда, восемь опубликованных ног — в карту вообще не входила и появилась только после того,
как две полосы независимо упёрлись в её отсутствие. **Десять запланированных задач — ровно та форма,
которая уже подводила: шесть запланированных превратились в четырнадцать карточек.**

Противоядий два, и оба механические, а не словесные: каждая строка блока `forecast` называет файл,
и порядок смерти написан до работы, а не в пятницу. Третьего нет — если волна снова разъедется,
это увидит review 11.09 и скажет числом.

**ТРЕТИЙ ДОВОД, ПОМЕЛЬЧЕ.** Волна берёт таблицу строк ради дешевизны «сейчас», но экран результата
и лобби — единственные её потребители на 11.09. Если задача 6 умрёт по порядку смерти, таблица
останется почти пустой закладкой. Это принято сознательно: закладка стоит несколько строк, а
переделка после HUD и онбординга — в разы больше.
## concept_basis
**ГРАНИЦА AUTHORITY.** Концепт живёт только в Concept Lab. Этот блок хранит квитанции неизменяемых
входов именно волны 12, а не копию концепта.

**OWNER-APPROVED IMMUTABLE EXPORT**

**ЕГО НЕТ.** На 2026-09-03 неизменяемого экспорта документа демо из Concept Lab не существует;
его слово 03.09: «сейчас там Concept Lab закрепляют, скоро будет». Поэтому:

- **ВОЛНА ТЕХНИЧЕСКАЯ, И ЭТО СКАЗАНО ВСЛУХ.** Её объём — механизмы, а не содержание игры: пятый
  глагол и терминальная строка распорядка; сигнал конца раунда; экран результата как поверхность;
  стенд «хозяин» и шов постановки удара; сценарий на четверых; таблица строк по ключам; номер
  версии сборки; лобби и подключение; временные звуки; магнит к предмету; шлепок через уже общий
  приёмник импульса.
- **СТРОКИ СОДЕРЖАНИЯ НЕ ЗАВОДЯТСЯ НИ ОДНОЙ НОГОЙ.** Порог 6 блока `kill_by`.
- **ЧТО ИМЕННО ЖДЁТ ЭКСПОРТА:** план утра «кофе → кейс → одежда → выход», одна привычка, список на
  холодильнике, табличка старта, интерфейс «сейчас/дальше», содержание порогов ★. Это строка 2
  рубежа Р1, и она переносится на следующий рубеж **без штрафа** — так решено принятым планом.
- **ЕСЛИ ЭКСПОРТ ПРИДЁТ ДО 11.09:** квитанция (id, хеш, `source_lab_commit`, `immutable: true`)
  вносится сюда ногой `work` с патчем, и строка 2 входит в остаток волны без новой нарезки.

**ЧТО ЗАМЕНЯЕТ ЭКСПОРТ ДО ЕГО ПРИХОДА**

Ничего. Отсутствующий вход — красная строка с его именем, а не самодельная замена (раздел 13 плана).
## журнал
2026-09-03 · двенадцатая волна нарезана рубежом Р1 «Скелет утра» к 11.09 его словом «все принимаю» — десять задач в четырёх полосах со списком рубежа построчно и видом улики на каждую строку; порядок смерти написан до работы, потому что волн с планом больше семи задач целиком не доходило ни одной; при нарезке перемерено три вещи сверх плана — сборка одной командой уже есть, давление темп не выбирает, а шов удара для стенда существует и ему не хватает ровно вида куска «мишень-хозяин»; решение о звуке и шесть кусков спеки давления произнесены вслух; четыре первых наряда выданы, стенд «хозяин» первой ногой и в WIN-U4 · history/2026-09-03-s-shape-g-5a7c-wave-12-001.md
END_OF_FILE: live/indie-game-development/cards/bet-g-5a7c-wave-12.md
