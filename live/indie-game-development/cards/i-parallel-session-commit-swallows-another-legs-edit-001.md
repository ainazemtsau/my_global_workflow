---
id: i-parallel-session-commit-swallows-another-legs-edit-001
_kind: issue
_pos: 21
level: direction
route: maintenance
---

## issue
Параллельная сессия закоммитила `git add -A` НЕЗАКОНЧЕННУЮ правку `NOW.md` другой сессии, шедшей в тот же момент: правка уехала под чужим сообщением коммита, хотя то же сообщение отдельно утверждает, что чужие правки не тронуты. Содержимое уцелело случайно — при другом порядке правок это была бы наполовину применённая транзакция в общем горячем файле.
## review_when
MAINTENANCE отдельной сессией, когда владелец её запустит; `live/**` она не трогает. До неё каждая нога перед коммитом сверяет, что в индексе только её собственные пути.
## evidence
Коммит `5aac4232` (`s-map-g-1d84-tunnel-out-of-the-wave-001`) содержит поля `ready_because` и `preserve_done`, написанные `s-work-g-1d84-householder-profile-owner-verdict-001`; его же сообщение — «Правки параллельной сессии в work/ не тронуты и не закоммичены». Разбор — history/2026-08-06-s-work-g-1d84-householder-profile-owner-verdict-001.md.
## журнал
2026-09-03 · ТРЕТИЙ СЛУЧАЙ, И ОН ПРОИЗОШЁЛ СЕГОДНЯ ЖИВЬЁМ, ПОКА ЭТА УЛИКА БЫЛА ОТКРЫТА. Нога магнита (полоса «мышь») положила в индекс свои 14 файлов и не успела дойти до git commit: параллельная нога утра (полоса «утро») закоммитила первой, и её коммит 7580c5d5 унёс ВСЕ 14 чужих файлов под своим сообщением про ETA и вечную заморозку. Ничего не потеряно — файлы целы в дереве, — но история направления теперь приписывает состояние одной ноги журнальной строке другой, и по git log эту ногу не найти. КОРЕНЬ НАЗВАН ТОЧНЕЕ ПРЕЖНЕГО: дело не в git add -A, а в том, что ДВЕ ЖИВЫЕ СЕССИИ СИДЯТ В ОДНОМ WORKTREE И ДЕЛЯТ ОДИН ИНДЕКС. Пока это так, никакая дисциплина staging не помогает: между git add и git commit всегда есть окно. Форма починки, не проверенная: коммитить пробелом путей (git commit -- <пути>), который берёт содержимое рабочего дерева мимо индекса, — это защищает от попадания в чужой коммит, но НЕ защищает от того, что чужой git add -A унесёт твоё. Настоящая починка — по worktree на ногу, и это вопрос обслуживания OS, а не этого направления · history/2026-09-03-s-work-g-5a7c-magnet-return-001.md
2026-08-29 · **ВТОРОЙ СЛУЧАЙ, И КЛАСС НАБРАЛ ПОРОГ §7 KERNEL.** Коммит `2ee78932`
(«шов закрыт светлым закрытием») забрал ТРИ незакоммиченных файла чужой ноги:
`knowledge/how-the-householders-plan-is-built.md` (новая страница, 108 строк),
правку `knowledge/householder-architecture-is-a-sparse-rule-table.md` (20 строк) и правку
журнала `cards/d-householder-plan-shape-001.md` (8 строк). Всё это писала параллельная сессия
ресёрча архитектуры, и уехало оно под сообщением про шов. **Данные целы, потерь нет** —
перепроверено побайтово. Цена в другом: запись слова владельца «давай Б» теперь ищется по
коммиту про совсем другое, а история ноги разорвана между двумя чужими коммитами.
Это второе совпадение класса — порог §7 KERNEL для самостоятельного изменения OS взят.
Нога ресёрча НЕ открывала MAINTENANCE REQUEST сама: это отдельная работа и чужой плей
END_OF_FILE: live/indie-game-development/cards/i-parallel-session-commit-swallows-another-legs-edit-001.md
