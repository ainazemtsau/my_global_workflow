---
id: t-reactions-data-1
_kind: task
_bet: bet-g-5a7c-wave-6
track: словарь
status: ready
goal: Реакции хозяина переезжают из C# в профиль, и двенадцатая заводится строкой
  текста
_pos: 131
---

## done_when
1. **ДВЕНАДЦАТАЯ ЗАВОДИТСЯ БЕЗ C#.** В `default.householder.json` появляется секция `reactions[]`,
   и новая реакция, собранная из уже существующих слов словаря, работает в игре **без единой
   правки C#**. Доказательство — не рассуждение, а прогон: реакция, которой не было, названа в
   файле и наблюдаемо сработала.
2. **ОДИННАДЦАТЬ ПЕРЕЕХАЛИ БЕЗ ПОТЕРЬ.** Все реакции, сегодня зашитые в
   `HouseholderReactionCatalog.CreateCurrent` (`HouseholderReaction.cs:383-513`), живут в профиле,
   и `CreateCurrent` либо исчезает, либо остаётся ровно как встроенный запасной набор с одним
   названным основанием. Все 13 полей `HouseholderReactionDefinition` имеют дом в схеме, включая
   `MovementPaceName`, `ReturnToLifeMode` и `RequiresSafeInterruption`.
3. **526 ТЕСТОВ ЗЕЛЁНЫЕ, И ОДИН НОВЫЙ КРАСНЕЕТ ПРАВИЛЬНО.** `dotnet test` целиком зелёный без
   Unity. Добавлен тест, который на неизвестном имени реакции отказывает **по имени**, а не молча
   подставляет пустую — тот же приём, каким уже защищены имена фактов.
4. **ВТОРОЙ ЦЕННИК ОСТАЁТСЯ ЧЕСТНЫМ И НАЗВАН.** Задача НЕ пытается вынести в данные
   `HouseholderPhysicalActionName` и прочие перечисления: новое физическое действие обязано стоить
   кода, потому что кто-то должен его исполнить. В возврате записано одной строкой, что именно
   осталось в C# и почему.

## note
**ЭТО ПЕРЕЕЗД, А НЕ ПЕРЕПИСЫВАНИЕ.** `HouseholderReactionDefinition` — обычный класс с 13
именованными полями, из которых 9 перечисления и 2 строки. Это уже строка данных, живущая не в
том доме. Секции `reactions[]` в схеме нет вообще, поэтому `HouseholderProfileLoader` умеет только
назвать существующую через `TryGetByName`.

**ПОЧЕМУ ЭТА ЗАДАЧА ИДЁТ ПЕРВОЙ.** Продуктовый обзор `docs/engineering/householder-interaction-surfaces.md`
§6: любая следующая работа добавит по слову в закрытый словарь, и потом это придётся выносить тем
же трудом плюс миграцией. Дешевле вынести до, а не после.

**УЛИКА, ЧТО ПУТЬ «ТОЛЬКО ДАННЫЕ» НИКОГДА НЕ ХОДИЛИ:** `git log --follow` по файлу профиля даёт
11 коммитов, и каждый из них правил ещё и C#.

**ЛОВУШКА:** `Weight` в определении — делегат, а не значение. Он в данные не переносится; решить
и записать, как строка профиля выбирает уже существующий вычислитель веса.

## журнал
2026-08-27 · заведена первой задачей шестой волны после его вопроса «у нас что максимум может быть 11 реакций если да то это точно нужно исправлять» · history/2026-08-27-s-shape-g-5a7c-wave-6-001.md

END_OF_FILE: live/indie-game-development/cards/t-reactions-data-1.md
