---
id: t-the-bench-shows-why-he-fell-1
_kind: task
_bet: g-5a7c
track: тело
status: open
goal: 'Стенд «хозяин»: без консоли видно, от какого груза он потерял равновесие —
  долг приёмки волны 11'
_pos: 218
---

## done_when
**ЕДИНСТВЕННЫЙ ДОЛГ ПРИЁМКИ ВОЛНЫ 11. ПРИЁМКА — ЕГО ГЛАЗ, И ПОВЕРХНОСТЬ РЕЖЕТСЯ ЗДЕСЬ ЖЕ.**

Наряд написан 02.09 возвратом самой ноги, которая упёрлась в отсутствие стенда:
`work/2026-09-02-order-the-homeowner-balance-bench.md`. Он остаётся спецификацией целиком; ниже —
только то, что нарезка добавляет и закрывает.

1. **НАБОР «ХОЗЯИН» — ВТОРОЙ НАБОР СУЩЕСТВУЮЩЕГО `MouseLab`, НЕ ТРЕТЬЯ СЦЕНА.** Решение Р14 плана.
   Отдельная сцена разрешена только если набор не помещается по устройству — тогда это его слово
   02.09, а не решение ноги.

2. **ШОВ, КОТОРЫМ СТЕНД СТАВИТ УДАР, НАЗВАН НАРЕЗКОЙ И НЕ ИЗОБРЕТАЕТСЯ ЗАНОВО.**
   Расстановка — JSON `Assets/TunnelCrew/Editor/IntegrationLab.<имя>.json`, схема
   `tunnelcrew-integration-lab-arrangement.v2`; кусок `launched-cargo` несёт `definitionId`, старт
   `x/y/z`, скорость `velocityX/Y/Z`. Полка — `IntegrationLabWindow`; кнопки идут в
   `NetworkWalkerCourier.TryLoadLabArrangement / FireLabArrangement / ResetLabArrangement /
   ReleaseLabArrangement`. Приборы возвращаются `LabShoveObserversRpc` в `IntegrationLabReadout`.
   **Видов куска сегодня три — `frozen-walker`, `launched-cargo`, `hovering-cargo`. Мишени-хозяина
   среди них нет: первая работа задачи — четвёртый вид куска**, который ставит тело хозяина в
   известную точку и замораживает распорядок, чтобы удар был воспроизводим.

3. **ПОКАЗЫВАЕТСЯ ТО, ЧТО ПЕРЕЧИСЛЕНО В НАРЯДЕ.** Груз (имя, масса, скорость, траектория, host tick,
   точка контакта, направление и величина импульса); `TiltDegrees` с неподвижными отметками `8°` и
   `35°`; `HadAcceptedImpact`, `ImpactSource`, `ImpactStrength`, фаза
   `Stable / TryingToStayUp / Fallen / Recovering`, `RemainingSeconds`, `NeedsRearm`,
   `InterruptsRoutine`; прежнее дело как `running → paused → resumed`.

4. **КЛИЕНТСКАЯ КУКЛА ВЫКЛЮЧЕНА, И ЭТО НАПИСАНО НА ЭКРАНЕ СЛОВАМИ.** При отсутствии авторитета
   `HouseholderBalanceBody:161-162` обнуляет `collisionLayers` и `groundLayers`. Без надписи
   наблюдатель решит, что клиент считает и ошибается.

5. **ЧИСЛА ПОРОГОВ ЧИТАЮТСЯ, А НЕ ДУБЛИРУЮТСЯ.** `8 / 1.5 / 35` живут в
   `Assets/TunnelCrew/Settings/GameRulesSettings.asset`.

6. **ИГРОВОЙ СНАПШОТ НЕ РАСШИРЯЕТСЯ НИ НА БАЙТ.** Приборные числа едут dev-RPC той же формы, что у
   набора «мышь». Порог: правка `Network/HouseholderSnapshot.cs` — откат.

7. **ПРИЁМКА СНИМАЕТСЯ НА ДВУХ ЭКРАНАХ.** Без Console наблюдатель связывает **один видимый груз и
   один контакт** с первой сменой фазы; near-miss реакции не вызывает. Прецедент готов:
   `IntegrationLabReadout` рисуется IMGUI и потому виден на собранном клиенте и во втором редакторе.

8. **МЕХАНИКА РАВНОВЕСИЯ НЕ ЧИНИТСЯ.** Она опубликована на `2d2c27ff` и доказана физикой; стенд её
   показывает. Любая правка самой механики — откат.

**ВОПРОС «НЕ СВЕСТИ ЛИ С `ReachLab`» ЗАКРЫТ И НЕ ПЕРЕОТКРЫВАЕТСЯ:** закрытая задача
`t-one-lab-he-can-set-up-himself-1` строкой 7 ответила «не сводить».
## журнал
2026-09-03 · двенадцатая волна нарезана рубежом Р1 «Скелет утра» к 11.09 его словом «все принимаю» — десять задач в четырёх полосах со списком рубежа построчно и видом улики на каждую строку; порядок смерти написан до работы, потому что волн с планом больше семи задач целиком не доходило ни одной; при нарезке перемерено три вещи сверх плана — сборка одной командой уже есть, давление темп не выбирает, а шов удара для стенда существует и ему не хватает ровно вида куска «мишень-хозяин»; решение о звуке и шесть кусков спеки давления произнесены вслух; четыре первых наряда выданы, стенд «хозяин» первой ногой и в WIN-U4 · history/2026-09-03-s-shape-g-5a7c-wave-12-001.md
END_OF_FILE: live/indie-game-development/cards/t-the-bench-shows-why-he-fell-1.md
