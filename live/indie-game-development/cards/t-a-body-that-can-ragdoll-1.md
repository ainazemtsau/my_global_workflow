---
id: t-a-body-that-can-ragdoll-1
_kind: task
_bet: g-5a7c
track: хозяин спотыкается
status: active
goal: В проекте есть humanoid-тело хозяина с PuppetMaster, и кукольная часть живёт
  в контракте замены тела
_pos: 197
---

## done_when
**ЕСТЬ ТЕЛО, НА КОТОРОМ МОЖНО ПОСТРОИТЬ КУКЛУ, И СЛЕДУЮЩИЙ ХОЗЯИН БУДЕТ АССЕТОМ, А НЕ ПРАВКОЙ
КОДА.**

1. **QUATERNIUS `Casual_Male` ПЕРЕИМПОРТИРОВАН КАК HUMANOID.** Замер до, снят направлением при
   нарезке: `Sources/Quaternius/Casual_Male.fbx.meta` несёт `animationType: 2` — это **Generic** —
   и `avatarSetup: 0`. После: `animationType: 3`, аватар собран и валиден.
   **Аватар не собрался — СТОП и вопрос владельцу.** Самодельный риг, правка костей и второй
   плагин запрещены. Порог `kill_by 3` волны.

2. **PUPPETMASTER СТОИТ И КОМПИЛИРУЕТСЯ.** Замер: в `main` его **ноль файлов**. Владелец ставит его
   сам, своим словом. Не стоит — СТОП, не ставить за него.

3. **КУКЛА ПОСТРОЕНА ПО HUMANOID-РИГУ, И ТЕЛО ПО-ПРЕЖНЕМУ ПРОСТО РАБОТАЕТ.** Хозяин стоит, идёт
   маршрутом, тянется за вещью через Final IK — как до этого. **Кукла на этой ноге ничего не
   роняет и никуда не падает:** равновесие — следующая задача.

4. **КУКОЛЬНАЯ ЧАСТЬ ЖИВЁТ В `BodyVisualDefinition` ИЛИ В ЕГО РАСШИРЕНИИ.** Контракт замены тела
   уже существует и объявляет себя так: «One replaceable, presentation-only body». Задача его
   **расширяет**, а не строит второй рядом.

5. **`TunnelCrew.Probe` НЕ ТРОНУТ НИ ОДНОЙ СТРОКОЙ, И ЕГО ASMDEF-ССЫЛКИ НЕ МЕНЯЮТСЯ.** Мозг хозяина
   — 12 100 строк — ссылается только на `TunnelCrew.Core` и тела не видит. Тронул — откат,
   порог `kill_by 4` волны.

6. **ТЕСТЫ НЕ ХУЖЕ.** Было `752 / 752 / 0`, пропущено 0. Ни один не погашен.

7. **ЖИВОЙ ПРОГОН ДО ВОЗВРАТА.** Две минуты: идёт маршрутом, делает дела, берёт вещь. Встал —
   СТОП и число домой.

8. **В ВОЗВРАТЕ НАЗВАНО СДЕЛАННОЕ РУКАМИ.** Из `Art/Bodies/AUTHORING.md`: масштаб (Quaternius
   замерен как `3.287` юнита), выравнивание по полу, разворот, контроллер с тремя float-параметрами
   `LocomotionBlend` / `PlanarSpeed` / `TurnRate`. Что потребовалось и какие числа вышли — это цена
   каждого следующего хозяина, и её надо знать.

## мина, названная заранее
`QuaterniusPlaceholderLocomotion.controller` уже существует и собран под Generic-риг. Перевод модели
в Humanoid может его разорвать. **Если клипы перестанут играть — это ожидаемо, чинить контроллер, а
не риг.** `Animator.SetFloat` по несуществующему параметру молча игнорируется, поэтому тело в
дефолтной позе навсегда — симптом потерянного параметра, а не сломанного аватара.

## что НЕ входит
Равновесие, спотыкание, падения — задача `t-he-loses-his-balance-and-tries-to-stay-up-1`.
Виктор — рабочим телом больше не будет, не трогать. Анимации — приходят из Concept Lab, не
сочинять. Любая починка найденного по дороге: записать и не чинить.

## журнал
2026-09-01 · нарезана одиннадцатой волной; полоса умирает целиком, а не половиной — половина её невидима владельцу · history/2026-09-01-s-shape-g-5a7c-wave-11-001.md
END_OF_FILE: live/indie-game-development/cards/t-a-body-that-can-ragdoll-1.md
