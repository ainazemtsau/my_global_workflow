---
id: t-thing-missing-1
_kind: task
_bet: bet-g-5a7c-wave-8
track: ядро
status: ready
goal: Мир подаёт хозяину метки о вещах на входе тика, и граница ядра не ломается
_pos: 147
---

## done_when
1. **МИР ПОДАЁТ ХОЗЯИНУ МЕТКИ, А НЕ ФАКТ.** Это решено ресёрчем архитектуры и не пересматривается:
   вещи входят в ядро **метками**, а не новым членом перечисления фактов. Замер оттуда: факт стоил
   бы члена перечисления в **одиннадцати файлах**; метка не стоит **ни одного нового слова** —
   условия `MarkPresent` / `MarkAbsent` / `MarkAtLeast` уже их читают.
2. **МЕТКИ ЕДУТ НА ВХОДЕ ТИКА, ТОЛЬКО НА ЧТЕНИЕ.** Не пишутся в его память и не протухают.
   Причина названа замером: у метки нет поля происхождения, поэтому мир и автор профиля делят одно
   плоское пространство имён; до посадки его память есть `default`, а `Marks` там `null`; метода
   слияния у `HouseholderMarks` нет вовсе. **Подача заново каждый тик снимает все три беды сразу.**
3. **ГРАНИЦА ЯДРА НЕ ЛОМАЕТСЯ.** После правки `grep Cargo` по `Core/Householder/` обязан остаться
   **НОЛЬ**. Это проверяется и называется в возврате числом.
4. **ВИДНО В ОКНЕ НАБЛЮДЕНИЯ.** Метки мира показываются там же, где остальная цепочка.
5. **ТЕСТ «ДОБАВИТЬ ИЛИ ПРАВИТЬ» НАЗВАН ЧИСЛОМ.** Сколько мест правки. Больше пяти — СТОП и вопрос
   домой.
6. **ТЕСТЫ ЗЕЛЁНЫЕ.** Их 640 на `782349bd`.
END_OF_FILE: live/indie-game-development/cards/t-thing-missing-1.md
