---
id: t-places-1
_kind: task
_bet: g-5a7c
status: active
kind: executor
order: 1
goal: Хозяин говорит о доме ролями, дом их объявляет, неизвестная роль молчит
_pos: 54
---

## done_when
1. **ПРОФИЛЬ НАЗЫВАЕТ РОЛЬ.** Шаг распорядка может назвать роль места («где спят», «где еда»),
   и хозяин с таким профилем ЗАПУСКАЕТСЯ. Сегодня он не запускается вовсе:
   `Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs:65-69` — `IsKnownPlace` возвращает
   `false` с комментарием «Deliberately empty», а `:740` отвергает любой шаг с именем места,
   после чего порт хоста остаётся нечитаемым и авторитетный Core хозяина не сажает вовсе.
2. **ДОМ ОБЪЯВЛЯЕТ РОЛИ.** Описание дома говорит, какие его точки какие роли играют. Словарь
   приходит из `HousePlan` (он уже строится `SceneHousePlan` и уже доезжает до
   `NetworkHouseholder:80,102`), а не изобретается загрузчиком профиля.
3. **НЕИЗВЕСТНАЯ РОЛЬ МОЛЧИТ, А НЕ УБИВАЕТ.** Профиль, назвавший роль, которой в этом доме нет,
   загружается; шаг с такой ролью не срабатывает; хозяин работает дальше. Это ПРОТИВОПОЛОЖНО
   сегодняшнему поведению и является главным содержанием задачи.
4. **ГЛАВНЫЙ ГЕЙТ — ОДИН ПРОФИЛЬ В ДВУХ ДОМАХ.** Тест строит два разных описания дома с разными
   точками, но одинаковыми ролями, и ОДИН И ТОТ ЖЕ файл профиля работает в обоих. Без этой строки
   разрез «человек / жильё» не доказан.
5. **ЯВНАЯ ОШИБКА ОСТАЁТСЯ ЯВНОЙ.** Кривая строка по-прежнему отвергается названно и с путём в
   JSON — приём уже существует в разборщике и распространяется, а не изобретается.
6. **ПОВЕДЕНИЕ ПО УМОЛЧАНИЮ НЕ МЕНЯЕТСЯ.** Опубликованный `default.householder.json` ролей не
   называет; хозяин ходит ровно как сегодня. Пять реакций целы по содержанию.
7. **ОТКАТ** одним коммитом до головы на момент старта, исполненный настоящим revert.

## note
**ЭТО ОБЩАЯ ЗАДАЧА-ШОВ. ОНА НЕ ПРИНАДЛЕЖИТ НИ ОДНОЙ ДОРОЖКЕ.** Пока она идёт, занято ноль дорожек
из трёх — его правило 2026-08-10: «одна задача блочит две полосы, то, ну, естественно, ждем».
Дорожка `body` ею НЕ блокируется и может идти параллельно: она не трогает ни правил, ни профиля.

**ЕГО СЛОВО:** «давай, наверное, роли попробуем» — 2026-08-12, на разобранное сравнение с его
собственным вариантом жёсткой привязки («получается, что хозяин к дому привязан в любом случае»).

**ПОЧЕМУ РОЛИ, А НЕ ТОЧКИ — асимметрия риска, названная ему до подписи.** Цена сегодня одинаковая:
эти ~10 строк обязаны быть написаны в любом случае. Но если роли окажутся лишними — потеря НОЛЬ,
роль с именем точки работает как привязка; если привязка окажется тесной — переписывать каждый дом,
а домов, по его словам, будет много.

**ЧТО УЖЕ СТОИТ и не строится заново:** `Core/House/HousePlan.cs` с комнатами, местами и проходами;
`SceneHousePlan` читает их с authored-маркеров; сегодняшний дом уже несёт три комнаты, семь мест и
два проёма шириной 2. Не хватает ровно проводки словаря в загрузчик профиля и понятия роли.

**ЧЕГО ЗДЕСЬ НЕТ:** иерархии ролей, наследования профилей, нескольких хозяев, действий внутри
комнаты (это `t-occupations-1`), нового вида события, зрения.

**ЗАКРЫТИЕ:** светлое доступно — все семь строк перемеряются по байтам и прогону тестов.
## журнал
2026-08-12 · наряд выдан в WIN-U2 от опубликованной головы f3c40c40; семь строк свёрнуты в три строки контракта 36 без потерь, главным гейтом поставлена доказуемость HEADLESS · history/2026-08-12-s-work-g-5a7c-places-1-dispatch-001.md
END_OF_FILE: live/indie-game-development/cards/t-places-1.md
