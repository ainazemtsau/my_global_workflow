---
id: t-cargo-delta-1
_kind: task
_bet: bet-g-5a7c-wave-5
status: parked
goal: 'Sparse pose/hold: неизменившийся груз не едет каждый tick, а gameplay-state
  живёт отдельно'
_pos: 91
---

## done_when
1. **B0: РЕЕСТР И БЕЗГОЛОВЫЕ ТЕСТЫ.** Сохраняются опубликованные проверки нового id,
   идентичного повтора, тысячи тактов дрейфа, удаления/возврата id и NaN-safe сравнения.
2. **B0: ЖИВОЙ SHADOW-ЗАМЕР.** `c-exec-g-5a7c-cargo-delta-1-002` записывает raw full /
   would-publish counters, serialized bytes и отдельные 60 секунд покоя без изменения
   получателей и full-array delivery.
3. **B0: НОЛЬ ИЗМЕНЕНИЙ В ПОВЕДЕНИИ.** Registry/shadow не меняет получателей,
   клиентскую full-replacement семантику или gameplay-state.
4. **B1: ТОЛЬКО SPARSE POSE/HOLD И АТОМАРНО.** Подключение registry, pose/hold catch-up,
   removal delivery и снятие правила «нет в pose-снимке = удалить view» входят одним
   коммитом после B0 runtime, интегрированных repair A + beam и закрытой
   `t-cargo-state-lifecycle-1`. Dirty projection содержит только реально читаемые pose/hold
   поля; `Integrity`, `ThingStateId`, `ThingRemainingSeconds` и `Moved` не считаются delta-полями,
   не резервируются и не заменяют отдельный state carrier.
5. **HOST И REMOTE ВИДЯТ ОДИН МИР.** Одиночный хост, удалённый клиент, removal/re-entry и
   late pose/hold catch-up доказаны отдельно; stale catch-up не откатывает более новую позу.
6. **ШЕСТЬДЕСЯТ СЕКУНД ПОКОЯ — НОЛЬ POSE/HOLD BYTES.** Замером, а не рассуждением;
   семантические gameplay-state start/change/end измеряются своей задачей и не маскируются
   под pose traffic.
7. **ОТКАТ B1 ОДНИМ КОММИТОМ.** Дерево отката побайтно равно названной базе.

Полный разбор B0/B1 и repair-граница:
`work/2026-08-17-call-cargo-delta-1.md`.
## note
**НЕ СЛУЖИТ ПЯТИ КАДРАМ И НЕ ГЕЙТИТ ИХ.** B0 опубликована как code/control checkpoint
(`history/2026-08-20-s-work-g-5a7c-cargo-delta-b0-checkpoint-001.md`); живые bytes/counters
остаются в blocked `c-exec-g-5a7c-cargo-delta-1-002` и не запускают Unity до освобождения
слота скриншотной волной.

**B1 = SPARSE POSE/HOLD, НЕ STATE.** Она ждёт B0 runtime evidence, интегрированный
`c-exec-g-5a7c-cargo-sleep-repair-1-002`, интегрированный `c-exec-g-5a7c-beam-1-001` и
закрытую `t-cargo-state-lifecycle-1`. До этого B1 CALL не выдаётся.

**GLOBAL GAMEPLAY-STATE — ОТДЕЛЬНАЯ ЗАДАЧА.**
`t-cargo-state-lifecycle-1` владеет ordered start/change/end, server tick +
sequence/revision, late-join current-state catch-up, клиентским consumer и удалением
мёртвых wire-полей. Future room interest не фильтрует её.

**ПОРЯДОК:** B0 runtime + repair A + beam → item-state lifecycle → B1 → C → D.
## журнал
2026-08-20 · B1 отделена от глобального gameplay-state — pose/hold остаётся sparse, lifecycle вынесен в отдельную blocked-задачу перед B1, мёртвые wire-поля не резервируются · history/2026-08-20-s-repair-g-5a7c-cargo-state-lane-001.md
2026-08-20 · B0 опубликована как code/control checkpoint без изменения доставки; live counter/bytes и 60 секунд покоя остаются, continuation ждёт Unity-слот · history/2026-08-20-s-work-g-5a7c-cargo-delta-b0-checkpoint-001.md
2026-08-20 · B0 запущена в отдельном worktree — registry, headless tests и теневые счётчики без изменения доставки и без Unity-слота · history/2026-08-20-s-work-g-5a7c-cargo-delta-b0-launch-001.md
2026-08-17 · НАРЯД B НАРЕЗАН ЗАНОВО: ПРЕЖНИЙ ПРОЕКТ ДАВАЛ ПУСТОЙ ДОМ У ХОСТА ПРИ ЗЕЛЁНЫХ ТЕСТАХ, А НОГА ЛУЧА УЖЕ ПОМЕНЯЛА ФОРМУ RPC. Атака шестью агентами нашла отказ двумя независимыми путями: тиковое сообщение имеет ДВУХ получателей с разными условиями — гости через ObserversRpc, а собственный экран хоста прямым вызовом под if IsClientInitialized, — и первый серверный такт наступает РАНЬШЕ, чем поднимается клиентская половина хоста, поэтому реестр записывает все предметы как известные ВСЕМ при нуле получателей, и лежащий предмет не изменится больше никогда; основание прежнего проекта «каждый наблюдатель получает каждое сообщение» для хоста ЛОЖНО по построению. Единственная страховка ломается одним атрибутом: SendTargetRpc молча возвращается при excludeServer и локальном клиенте, а у TargetRpc умолчание ExcludeServer=false, то есть ПРАВИЛЬНЫЙ КОД ЭТО ОТСУТСТВИЕ ПАРАМЕТРА, а неправильный выглядит единообразно с соседней строкой. РАЗРЕЗ ПЕРЕСТАВЛЕН: резать по границе хост-клиент нельзя, потому что Apply трактует любой массив как полную перепись мира и пустой массив значит «уничтожить все виды» — успех хостовой половины ЕСТЬ её отказ, пустой дом через 33 миллисекунды; настоящая граница это кто имеет право прислать неполный массив, отсюда B0 с реестром, тестами и теневым замером идёт сейчас без риска, а B1 одним коммитом позже. НАЙДЕНО ТО, ЧЕГО НАПРАВЛЕНИЕ НЕ ЗНАЛО: нога луча на slot/win-u1 уже добавила третий массив и новый файл CargoHoldSnapshot, и два конфликта с ногой B это вставка В ОДНУ И ТУ ЖЕ СТРОКУ, плюс снятие поля Moved красит строку, которую луч уже переписал, плюс бюджет пакета считался без рук, а это 120 байт каждый такт при четырёх руках. Предикат исправлен: Yaw исключается, потому что клиент его не читает вообще и ветка чтения мертва, при этом Yaw единственное поле снимка, не защищённое от нечислового значения. И записана СВОЯ ОШИБКА: параллельной ноге было сказано, что форму RPC никто не меняет — неверно, её меняет наша же нога луча · history/2026-08-17-s-work-g-5a7c-cargo-delta-dispatch-001.md
END_OF_FILE: live/indie-game-development/cards/t-cargo-delta-1.md
