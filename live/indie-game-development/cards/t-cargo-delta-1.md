---
id: t-cargo-delta-1
_kind: task
_bet: bet-g-5a7c-wave-5
status: ready
goal: 'Слать только изменившееся: потолок 26 предметов на дом исчезает'
_pos: 91
---

## done_when
1. **РЕЕСТР ОПУБЛИКОВАННОГО** и безголовые тесты к нему, включая тысячу тактов медленного дрейфа и возврат выбывшего предмета с тем же снимком.
2. **ТЕНЕВОЙ РЕЖИМ:** реестр считает дельту на живой игре, провод несёт полное. Экономия записана числом.
3. **НОЛЬ ИЗМЕНЕНИЙ В ПОВЕДЕНИИ** — часть B0 не трогает ни одного получателя.
4. **ПОДКЛЮЧЕНИЕ, ДОГОН И СНЯТИЕ ПРАВИЛА «нет в снимке = удалить» — ОДНИМ КОММИТОМ** (часть B1, после возврата луча и ноги A).
5. **ОДИНОЧНЫЙ ХОСТ ВИДИТ ДОМ** — отдельной строкой, потому что этот отказ тесты не ловят.
6. **ШЕСТЬДЕСЯТ СЕКУНД ПОКОЯ — НИ ОДНОГО БАЙТА ГРУЗА.** Замером, не рассуждением.
7. **ОТКАТ** одним коммитом.

Полный текст с пятью тихими отказами — `work/2026-08-17-call-cargo-delta-1.md`.
## note
**НЕ СЛУЖИТ ПЯТИ КАДРАМ, ЗАКРЫТИЕ ВОЛНЫ НЕ ГЕЙТИТ.** Вторая из четырёх ног
по физике груза.

**B0 ОПУБЛИКОВАНА КАК СИЛЬНЫЙ CODE/CONTROL CHECKPOINT, НО НЕ ЗАКРЫТА.**
Exact reviewed candidate `a67d1d038f7b40c581177fc560801fb8ff6005f1` опубликован
в `origin/main` и `origin/dev` tip
`97ca2c98485f158d3367103b202000481e1e74d7`. Registry/state machine,
headless 38/38, full Release 442/442 и независимая exact-candidate review зелёные.
Полный `CargoSnapshot[]` всё ещё доезжает тем же получателям; B1/sparse delivery нет.

**B0 ЕЩЁ ТРЕБУЕТ ЖИВОГО ЗАМЕРА.** Нет Unity shadow counter, реальных
serialized/wire bytes и 60 секунд покоя; экономия числом не заявлена.
`c-exec-g-5a7c-cargo-delta-1-002` blocked до освобождения Unity-слота
скриншотной волной. До этого Unity не запускать и слот не резервировать.

**B1 ЖДЁТ ВОЗВРАТА И ИНТЕГРАЦИИ ОБЕИХ ЗАВИСИМОСТЕЙ:**
`c-exec-g-5a7c-cargo-sleep-repair-1-001` и `c-exec-g-5a7c-beam-1-001`.
До этого не начинать sparse/delta RPC, removal delivery, catch-up или смену
клиентской full-replacement семантики.

**STATE OBSERVABILITY НЕ РЕАЛИЗОВАНА B0.** Звон, разлив, след и их окончания
должны оставаться видимы/слышны каждому клиенту как обязательная B1/C acceptance;
технические состояния не нужны, мёртвые будущие поля не сохраняются.
## журнал
2026-08-20 · B0 опубликована как code/control checkpoint без изменения доставки; live counter/bytes и 60 секунд покоя остаются, continuation ждёт Unity-слот · history/2026-08-20-s-work-g-5a7c-cargo-delta-b0-checkpoint-001.md
2026-08-20 · B0 запущена в отдельном worktree — registry, headless tests и теневые счётчики без изменения доставки и без Unity-слота · history/2026-08-20-s-work-g-5a7c-cargo-delta-b0-launch-001.md
2026-08-17 · НАРЯД B НАРЕЗАН ЗАНОВО: ПРЕЖНИЙ ПРОЕКТ ДАВАЛ ПУСТОЙ ДОМ У ХОСТА ПРИ ЗЕЛЁНЫХ ТЕСТАХ, А НОГА ЛУЧА УЖЕ ПОМЕНЯЛА ФОРМУ RPC. Атака шестью агентами нашла отказ двумя независимыми путями: тиковое сообщение имеет ДВУХ получателей с разными условиями — гости через ObserversRpc, а собственный экран хоста прямым вызовом под if IsClientInitialized, — и первый серверный такт наступает РАНЬШЕ, чем поднимается клиентская половина хоста, поэтому реестр записывает все предметы как известные ВСЕМ при нуле получателей, и лежащий предмет не изменится больше никогда; основание прежнего проекта «каждый наблюдатель получает каждое сообщение» для хоста ЛОЖНО по построению. Единственная страховка ломается одним атрибутом: SendTargetRpc молча возвращается при excludeServer и локальном клиенте, а у TargetRpc умолчание ExcludeServer=false, то есть ПРАВИЛЬНЫЙ КОД ЭТО ОТСУТСТВИЕ ПАРАМЕТРА, а неправильный выглядит единообразно с соседней строкой. РАЗРЕЗ ПЕРЕСТАВЛЕН: резать по границе хост-клиент нельзя, потому что Apply трактует любой массив как полную перепись мира и пустой массив значит «уничтожить все виды» — успех хостовой половины ЕСТЬ её отказ, пустой дом через 33 миллисекунды; настоящая граница это кто имеет право прислать неполный массив, отсюда B0 с реестром, тестами и теневым замером идёт сейчас без риска, а B1 одним коммитом позже. НАЙДЕНО ТО, ЧЕГО НАПРАВЛЕНИЕ НЕ ЗНАЛО: нога луча на slot/win-u1 уже добавила третий массив и новый файл CargoHoldSnapshot, и два конфликта с ногой B это вставка В ОДНУ И ТУ ЖЕ СТРОКУ, плюс снятие поля Moved красит строку, которую луч уже переписал, плюс бюджет пакета считался без рук, а это 120 байт каждый такт при четырёх руках. Предикат исправлен: Yaw исключается, потому что клиент его не читает вообще и ветка чтения мертва, при этом Yaw единственное поле снимка, не защищённое от нечислового значения. И записана СВОЯ ОШИБКА: параллельной ноге было сказано, что форму RPC никто не меняет — неверно, её меняет наша же нога луча · history/2026-08-17-s-work-g-5a7c-cargo-delta-dispatch-001.md
END_OF_FILE: live/indie-game-development/cards/t-cargo-delta-1.md
