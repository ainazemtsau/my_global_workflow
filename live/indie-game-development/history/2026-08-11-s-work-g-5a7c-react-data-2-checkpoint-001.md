# RESULT — s-work-g-5a7c-react-data-2-checkpoint-001

play: work · возврат пришёл КАНДИДАТОМ, публикации нет; нога перемеряет его первой рукой сейчас и
НЕ закрывает задачу. Владелец поручил дождаться публикации опросом, закрыть работу и составить
следующий наряд, а при находке проблемы — остановиться и написать ему
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-data-2 ·
call: c-exec-g-5a7c-react-data-2-001
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `c726955c`, дерево чисто. Продукт перемерен
`git ls-remote origin`: `origin/dev` = `origin/main` = `2c8dc0c7` — **публикации кандидата ещё нет**.
Кандидат живёт на локальной `slot/win-u2`, tip `30c05449`. Все четыре слота по файлу состояния —
`AVAILABLE / lease none`.

## outcome

**Задача НЕ закрывается — публикации нет.** Кандидат перемерен целиком; **проблем не найдено, СТОП
владельцу не требуется.**

**Расписка получена и верна:** «Получено строк done_when: 3» — в наряде их ровно три.

**ГЛАВНАЯ ДЫРА, ради которой наряд и писался, закрыта.** Наряд нашёл, что
`HouseholderDecisionPipeline.Default` — пустой конвейер, и композиции с обработчиками жили только в
тестах, то есть в собранной игре не работал ни один. Теперь профиль читается хостом, из него
собирается конвейер, и **обработчик, описанный ТОЛЬКО файлом, проверяется настоящим `CarryWorld`**.

## evidence — прогнано и прочитано ЭТОЙ ногой

**Прогон сделан здесь**, на выгруженном дереве кандидата:

```text
кандидат 30c05449: Пройдено 196, не пройдено 0, пропущено 0, всего 196
```

База `2c8dc0c7` дала `184/184` — прогнано этой ногой на предыдущем шаге. Разница `+12`.

**ГЛАВНЫЙ ГЕЙТ ВЗЯТ.** Тест
`HouseholderProfileAuthoringTests.FileOnlyHandlerRunsInsideCarryWorldAndBranchResumesPriorOccupation`
(`:110`) строит НАСТОЯЩИЙ `CarryWorld` (`:123`) и отдельный базовый мир для сравнения (`:124`).
Прочитано, а не принято со слов: проверка идёт по игровому пути, а не по пути правил.

**Пять производственных реакций целы по СОДЕРЖАНИЮ — доказано диффом.** Единственное изменение в
каталоге — ДОБАВЛЕННЫЙ метод `TryGetByName`; ни одно значение ни одной строки не тронуто.

**Файл профиля изменён минимально и безопасно:** добавлены ровно два ПУСТЫХ раздела
(`"behaviorBranches": []`, `"decisionHandlers": []`). Поведение по умолчанию не меняется ничем.

**Границы целы — доказано пустым выводом.** `git diff --name-only 2c8dc0c7..30c05449` по
`Core/Cargo/**`, `Network/CargoBody.cs`, `Network/NetworkPlaySettings.{cs,asset}`, `Scenes/**`,
`ProjectSettings/**`, `Assets/GasCoopGame/**`, `Core/Multiplayer/CarryWorld.cs` — **пусто**.

**Дифф — одиннадцать путей**, все в хозяине, профиле, загрузчике, документации формата и тестах.

**ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК, третий раз подряд в этой дорожке.**
`git rev-parse 42997a72^{tree}` = `931b31eb3d724278d54d0aca448357680affc753` = дерево головы
`2c8dc0c7`; обратный revert `30c05449^{tree}` = `f480926f…` = дерево исходного `9a43548f`.

**Разрез НЕ делался** — доставлены и ветки из файла, и обе готовые формы обработчика. Наряд разрешал
резать; исполнитель не резал и сказал это прямо.

**Проверена зависимость, которую наряд не называл.** Тестовый проект получил `Newtonsoft.Json 13.0.2`
и линк на загрузчик. Перемерено, что риска нет: загрузчик использовал `Newtonsoft.Json` УЖЕ ДО
задачи (`2c8dc0c7:HouseholderProfileLoader.cs:4-5`), а игра держит `com.unity.nuget.newtonsoft-json
3.2.1` в `Packages/manifest.json`. Тест лишь повторяет то, что у игры уже есть.

## Что НЕ проверено

Unity-редактор код не собирал — наряд прямо ограничил доказательство engine-free ядром и
headless-тестами; загрузчик при этом живёт вне `Core` и теперь компилируется тестовым проектом по
линку, то есть покрытие headless стало ШИРЕ, чем было.

Названные в возврате шесть форм отказа кривой строки прочитаны как ТАБЛИЦА возврата, а не как байты;
их результат покрыт тестами, и тесты зелёные.

## state_changes

1. `c-exec-g-5a7c-react-data-2-001`: `status: ready` → `waiting` — исполнен, ждёт публикации.
2. `t-react-data-2` остаётся `open`.
3. NOW, ставка, узел и дорожка `cargo-hands` не тронуты.

## captures

- **Опрос публикации armed** — каждые пять минут; отдельно ловится расхождение `dev` и `main`.
- **Слот освободился ДО публикации**, в отличие от прошлой задачи. Владелец сказал, что закрытие
  идёт, значит Control-транзакция работает вне слотового файла. Проблемой не считаю, но отмечено.
- **Цена НОВОЙ ФОРМЫ обработчика названа возвратом: два продуктовых места** — новый класс и один
  case загрузчика. Новая СТРОКА готовой формы стоит только JSON. Это то, что нужно знать конвейеру.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.

## decisions_needed

Нет. Проблем не найдено, СТОП не объявляется.

## play_check

- 1 recite: done — пять строк карточки и три строки наряда сведены
- 2 owner inputs (owner): done — он поручил закрыть работу без него
- 3 do the work: done — кандидат перемерен, прогон сделан здесь
- 4 self-check: done — сошлось всё проверяемое; непроверяемое названо; проверена зависимость, о
  которой наряд не спрашивал
- 5 close: **checkpoint** — публикации нет

## Гейты

- **G5.** Ничего не объявлено `done`. Маршрут закрытия — светлый.
- **G7.** Ничего продуктового не решено.
- **G9.** Карточки задачи и ставки не редактировались.
- **evaluator, клауза 1.** Закрытие только ПОСЛЕ публикации.
- **evaluator, клауза 3.** Расписка получена и сверена: три строки.

## log

возврат по авторингу данными пришёл кандидатом без публикации — задача НЕ закрыта, но кандидат перемерен целиком и проблем не найдено: 196/196 прогнаны этой ногой против 184/184 на опубликованной базе; ГЛАВНАЯ ДЫРА, ради которой наряд и писался, закрыта — тест строит НАСТОЯЩИЙ CarryWorld и проверяет обработчика, существующего только в файле, то есть авторская строка работает на игровом пути, а не в пути правил; пять производственных реакций целы по содержанию, единственное изменение каталога — добавленный поиск по имени; файл профиля получил ровно два ПУСТЫХ раздела, поэтому поведение по умолчанию не изменилось ничем; границы пусты по семи запрещённым поверхностям, дифф одиннадцать путей; ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК третий раз подряд; разрез не делался вовсе, доставлены и ветки из файла, и обе формы обработчика; отдельно проверена зависимость, которую наряд не называл — Newtonsoft тестового проекта не нов для игры, загрузчик использовал его и раньше, а Unity держит пакет в манифесте

## next

1. **Ждать публикации** — опрос armed, каждые пять минут.
2. После публикации — закрывающая нога: сверить опубликованное с проверенным `30c05449`, закрыть
   светлым путём.
3. Затем `t-react-3` — последняя задача дорожки и ЕГО вердикт. Наряд на неё потребует его участия,
   в отличие от трёх предыдущих.
