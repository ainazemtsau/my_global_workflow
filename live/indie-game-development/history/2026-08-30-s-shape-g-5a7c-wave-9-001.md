# RESULT — s-shape-g-5a7c-wave-9-001

direction: indie-game-development
play: shape
node/task: g-5a7c / bet-g-5a7c-wave-9
track: —
date: 2026-08-30
outcome_kind: shape — девятая волна открыта: четыре полосы, семь задач, и проверка построенного впервые стоит отдельной задачей, а не подразумевается

owner_approved: |
  Экран показан ему целиком одним сообщением — цель, четыре полосы, семь задач с обоснованным
  порядком, замеры под каждой полосой, три подхода с двумя отклонёнными и причинами, девять
  вырезов плюс одиннадцать бесхозных задач, пять порогов смерти, семь линз с вердиктом на каждой,
  довод против и прогноз. Его слово: **«Открывай как есть, семь задач»**.

  Четыре полосы названы им самим в этом же разговоре, и на четвёртую его слово дословно
  **«ДА БЕРЕМ»**. Лимит полос 4 стоял в указателе с шестой волны его словом и не менялся.

  **ОДНА ЗАДАЧА ПЕРЕПИСАНА ЕГО ОТВЕТОМ, И ЭТО ЛУЧШЕЕ, ЧТО СЛУЧИЛОСЬ В ЭТОЙ НОГЕ.** Нарезка
  спросила, какие именно «четыре мелкие починки полигона» он имел в виду, и предложила четыре
  кандидата из живых улик. Он ответил: **«БЕЗ ПОНЯТИЕ ЕСЛИ УВИДИМ ТО ПО ФАКТУ ПОЧИНИМ»**. Поэтому
  задача перестала быть списком из четырёх выдуманных дефектов и стала разбором его прогона.
  Список, который нарезка была готова написать за него, не написан.

  **Узел `g-5a7c`, `CHARTER.md` и реестр `owner_approved` этой ногой НЕ изменены.** Узел уже
  `active` и остаётся им; девятая волна — не активация, а следующая ставка на том же узле, как
  шестая, седьмая и восьмая.

outcome: |
  **ОТКРЫТА ДЕВЯТАЯ ВОЛНА `bet-g-5a7c-wave-9`: ЧЕТЫРЕ ПОЛОСЫ, СЕМЬ ЗАДАЧ.**

  Её цель — **владелец садится и видит, как хозяин живёт утро, а когда что-то идёт не так, экран
  говорит почему.** Восьмая волна построила машину и оставила проверку; эта проводит проверку и
  наполняет машину.

  **ЧЕТЫРЕ ЗАМЕРА, НА КОТОРЫХ СТОИТ ВОЛНА, СНЯТЫ ЭТОЙ НОГОЙ ПЕРВОЙ РУКОЙ, А НЕ ВЗЯТЫ СО СЛОВ.**
  Владелец назвал четыре полосы по существу; нарезка обязана была перевывести каждую из кода, и
  каждая подтвердилась — но одна с ДРУГИМ виноватым, чем предполагала улика.

  1. **Зрение целится в пол под вещью.** `NetworkHouseholder.cs:305` и `:313` передают в
     `CanSee(observer, x, floorY, z)` третьим аргументом `observer.Y` — высоту ног САМОГО хозяина,
     — при том что `CargoState.Y` существует и не читается ни в одной из двух веток. Улика
     `i-a-ray-may-not-see-a-thing-lying-down-001` подозревала `RayIsClear` (луч упирается в саму
     вещь); замер называет более дешёвого виноватого, и наряд обязан проверить обоих.
  2. **«Сделал» наступает, когда он тронулся.** `Householder.cs:313` применяет эффекты выигравшей
     строки в тот же тик, когда она выиграла. Хука завершения нет ни одного.
  3. **Профиль пуст.** Блока `occupations` НЕТ, блока `gauges` НЕТ, `routine` — ОДИН шаг «стоять
     одну секунду». Есть 5 реакций и 7 обработчиков. Весь его день — один стоящий человек плюс
     ответы на мышей. **Сводка для нарезки называла другие числа (8 намерений / 2 занятия /
     1 реакция / 2 строки распорядка) — они устарели, и нарезка пользуется своим замером.**
  4. **Отказ безымянен ровно там, где его читают.** Восемь причин отказа объявлены, и у строк
     каталога работают все восемь. Шесть обработчиков получают ровно одну — `NoProposal`
     (`HouseholderDecisionPipeline.cs:199`).

  **ПОРЯДОК ЗАДАЧ ВЫНУЖДЕН, И КАЖДЫЙ ШАГ ОБОСНОВАН ЗАМЕРОМ.** Зрение первым: если луч не видит
  лежащую вещь, весь слой убеждений прошлой волны инертен, и три остальные полосы судят слепого.
  Разбор прогона вторым: владельцу нечего смотреть на слепом хозяине. Хук прибытия раньше
  наполнения: занятие с фазами, у которого «сделал» наступает на старте, наполнять бессмысленно.
  Полоса `руки` ждёт зрения — обе трогают `NetworkHouseholder.cs`, и это единственная коллизия по
  файлам во всей волне; она разведена временем, а не надеждой.

  **ЧТО В ВОЛНЕ НОВОГО ПО ФОРМЕ.** Это первая волна узла, где **проверка построенного стоит
  отдельной задачей с собственным `done_when`**, а не подразумевается. Порог смерти 5 восьмой
  волны не сработал только потому, что владелец не садился; здесь его прогон — задача, у которой
  есть приёмка: каждый названный им дефект получает одно из трёх, ноль молча пропущенных.

  **СЛОТ В КАРТОЧКЕ НЕ ЗАПИСАН НИ ДЛЯ ОДНОЙ ПОЛОСЫ.** Столбец «Слот» восьмой волны врал по всем
  трём полосам, потому что живую тройку переставили после написания таблицы. Слот выводится из
  реестра лиз в день выдачи; для первого наряда он выведен сегодня и назван в самом наряде.

evidence: |
  Продуктовый `main` = `dev` = `origin/main` = `c7e926cf`. Направление на `f62f228a` (коммит
  закрытия восьмой волны). Все замеры ниже сняты этой ногой.

  **ПОЛОСА `починка`.**
  - `Assets/TunnelCrew/Network/NetworkHouseholder.cs:305` — `sight.CanSee(observer, arranged.X, observer.Y, arranged.Z)`;
    `:313` — `sight.CanSee(observer, state.X, observer.Y, state.Z)`.
  - `Assets/TunnelCrew/Network/HouseholderSightSource.cs:92-110` — третий параметр называется
    `floorY`, и `TrySee` строит от него мышиное тело вверх (`RatBodyHeight`, `RatBodyRadius`,
    строки ~303-320).
  - `Assets/TunnelCrew/Probe/Cargo/CargoState.cs:130` — `public float Y` существует.
  - Число «8 из 14» — замер ВЛАДЕЛЬЦА своими руками, не перевыверен ногой и помечен в задаче как
    его, а не как наш.
  - Гейт: `tools/close-leg.ps1:257` зовёт `unity-tests.ps1`, `:310` — `check.ps1 -Deliver`; строки
    `dotnet test tests/TunnelCrew.Core.Tests` в закрывальщике нет. Сторожа против глухоты —
    `tests/TunnelCrew.Core.Tests/HouseholderChoreTests.cs:212, :315, :565`.

  **ПОЛОСА `утро`.**
  - `Assets/TunnelCrew/Probe/Householder/Householder.cs:310-317` — `standingAfter =
    HouseholderEffect.ApplyAll(proposal.Effects, standingBefore)` под условием `isReaction`,
    то есть в тик победы.
  - Поставочный профиль `Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`:
    ключей `occupations` — **0**, `gauges` — **0**, `routine` — **1** шаг
    (`wait-at-route-point`, 1.0 c, `when: always`), `reactions` — **5**,
    `activeResponseRules` — **2**, `behaviorBranches` — **1**, `decisionHandlers` — **7**.

  **ПОЛОСА `почему`.**
  - `Assets/TunnelCrew/Probe/Householder/HouseholderDecisionCandidate.cs:24-51` — восемь исходов
    плюс `Won`.
  - Все восемь производятся на стороне каталога: `HouseholderDecision.cs:651, 661, 662, 665, 673,
    687` (через `Furthest`).
  - `HouseholderDecisionPipeline.cs:199` — единственный исход, который получает обработчик:
    `NoProposal`. Обработчиков шесть: `AccumulatedFact`, `Chore`, `FactCombination`,
    `LocalIncident`, `Standing`, `ThingBelief`.

  **ПОЛОСА `руки`.**
  - `NetworkHouseholder.cs:704-718` — дословно «HIS LEGS ARE NOT DRIVEN FROM HERE».
  - `tests/TunnelCrew.Core.Tests/HouseholderChoreTests.cs:~459` —
    `NoChoreInTheShippedFileCarriesADeedItCannotFinish`, тест-растяжка с записанной причиной.

  **СЛОТ ДЛЯ ПЕРВОГО НАРЯДА ВЫВЕДЕН, А НЕ СКОПИРОВАН.** Реестр
  `GasCoopGame_slot-state/gascoop-slot-state.v1.json` на 2026-08-30: все четыре слота
  `AVAILABLE`, лиз нет. Отставание от `main`: u1 — 3 коммита, **u2 — 0**, u3 — 22, u4 — 44.
  Выбран WIN-U2: он стоит ровно на `main`, и его Unity-MCP в этой сессии отвечает.

  **ПРОВЕРКА ПОСЛЕ ПРАВОК.** `osctl check` — 61 замечание, столько же, сколько после закрытия
  восьмой волны; «механических проблем нет». Рабочий набор `osctl context --for g-5a7c` — 25
  файлов, все семь новых задач и все одиннадцать вырезанных видны.

state_changes: |
  Всё через `osctl`; руками — только `NOW.md` и два файла в `work/`.

  СОЗДАНО — ставка (1):
  - `bet-g-5a7c-wave-9`, `status: active`, десять блоков: `owner_word`, `goal`, `appetite`,
    `tracks`, `cut_list`, `riskiest_assumption`, `kill_by`, `lenses`, `forecast`, `against`.

  СОЗДАНО — задачи (7), у каждой `_bet: g-5a7c` и своя полоса:
  - `починка`: `t-sight-thing-1` (`active`), `t-bench-by-fact-1`, `t-gate-headless-1`;
  - `утро`: `t-arrival-hook-1`, `t-morning-content-1`;
  - `почему`: `t-refusal-reason-1`;
  - `руки`: `t-carry-back-1` (`blocked`, ждёт, пока `t-sight-thing-1` доедет в `main` — обе
    трогают `NetworkHouseholder.cs`).

  СОЗДАНО — наряд первой задачи (1):
  - `c-exec-g-5a7c-sight-thing-1-001`, `status: ready`, слот WIN-U2, текст целиком в
    `work/2026-08-30-call-sight-thing-1.md`.

  ЗАКРЫТО (1): `c-shape-g-5a7c-wave-9-001` — вернувшийся наряд этой ноги.

  РУКАМИ (2): `NOW.md` — `bet: g-5a7c`, `track_wip_limit: 4` без изменений;
  `work/2026-08-30-call-sight-thing-1.md` — текст наряда.

  НЕ ТРОНУТО НАМЕРЕННО: узел `g-5a7c`, `CHARTER.md`, реестр `owner_approved`, одиннадцать
  вырезанных задач (остаются живыми и видимыми), карточка решения по нити.

captures:
  - Отклонённый подход Б — «строить недостающее»: запас времени, процесс со своими часами, «защитить». Три из них стоят на часах, которые он снял, и волна про проверку, а не про стройку. Вернётся, когда встанет вопрос «напряжённая ли игра».
  - Отклонённый подход В — «сделать проверку ненужной»: выкинуть полигон и играть мышью. Отклонён его же словом «где я НЕ БЕГАЮ МЫШЬЮ».
  - Ни одна из одиннадцати бесхозных задач НЕ пересекается с четырьмя полосами, которые он назвал. Это факт про backlog, а не про волну: он копился под другой концепт.
  - Плей `shape` шаг 4 велит класть вырезанное в `parked`, а у вида `task` такого статуса нет вовсе (`open | active | blocked | done`). Отсюда, вероятно, и брались все те off-schema `parked`, которые обзор чинил сегодня. Противоречие внутри самой OS, маршрут `maintenance`.
  - Сводка `work/2026-08-30-handoff-next-wave.md` §3 несёт устаревшие числа профиля (8 намерений / 2 занятия / 1 реакция / 2 строки распорядка). Замер этой ноги: занятий 0, шкал 0, распорядок 1 шаг, реакций 5, обработчиков 7.

decisions_needed:
  - Ни одного нового. Один переходит из закрытия волны и нарезку не блокирует: `d-which-thread-document-is-the-contract-001` — какой из двух подписанных им экспортов является контрактом нити. Нить в девятую волну не входит вовсе.

play_check: |
  1. Пересказ — сделано: цель узла, его тринадцать критериев и цель родителя `g-0c26` прочитаны;
     волна двигает 6 и 13 и не закрывает ни одного целиком. Цель узла — исход, а не занятие,
     править не понадобилось.
  2. Аппетит ПЕРВЫМ, до задач — сделано (G3). Его слово «Открывай как есть, семь задач»; часов и
     дат нет, его запрет от 2026-08-02 соблюдён. Внешняя проверка сравнением: восьмая волна —
     3 полосы и 9 задач, шестая — 4 полосы и его «4 дня». Назван порядок смерти.
  3. Подходы — сделано: три структурно разных, два отклонены с причиной, выбран «чинить и
     наполнять» с одной строкой почему; проигравшие ушли в captures.
  4. Молот по объёму — сделано (G6): девять вырезов плюс одиннадцать бесхозных задач списком.
     Резалось до боли: «защитить» вырезано, хотя три из четырёх решений хозяина уже приехали даром.
  5. Обход по линзам — сделано (G6): вердикт по каждой из семи, включая два `not_needed` с
     причиной.
  6. Самое рискованное допущение — сделано (G6): четыре ранжированы, первое получает ПЕРВУЮ задачу
     и самую дешёвую проверку из существующих — шарики `Draw Sight Gizmos` за один прогон.
  7. Задачи — сделано (G7): семь, у каждой проверяемый `done_when`, самая рискованная первой;
     четыре полосы объявлены ЕГО дословными словами, лимит 4 — его же, с шестой волны.
  8. Пороги смерти — сделано (G4): пять, все на событиях, дат нет.
  9. Закрытие (owner) — сделано: экран показан целиком одним сообщением, его слово
     **«Открывай как есть, семь задач»**. Узел не переписан (G9): он уже `active`, и девятая
     волна — не активация. Наряд первой задачи заведён карточкой и уходит в `next`.

  ГОТОВНОСТЬ К АКТИВАЦИИ (converge Definition-of-Ready): не применяется. Узел `g-5a7c` активен с
  2026-08-07 и этой ногой в `active` не переводится — это четвёртая ставка подряд на уже активном
  узле, ровно как шестая, седьмая и восьмая, ни одна из которых converge не запускала.

  ЧЕГО ЭТА НОГА НЕ ДЕЛАЛА: не трогала продуктовый репозиторий ни одним байтом. Все замеры —
  чтение.

next: |
  `c-exec-g-5a7c-sight-thing-1-001` — зрение целится в вещь, а не в пол под ней. Слот WIN-U2,
  текст целиком в `work/2026-08-30-call-sight-thing-1.md`. Это самое рискованное допущение волны:
  его ответ решает, реален ли слой убеждений, доставленный восьмой волной, и стоит ли судить по
  нему три остальные полосы.

  Параллельно, без слота Unity и без ожидания: `t-gate-headless-1` — 716 тестов в гейт слияния.

END_OF_FILE: live/indie-game-development/history/2026-08-30-s-shape-g-5a7c-wave-9-001.md
