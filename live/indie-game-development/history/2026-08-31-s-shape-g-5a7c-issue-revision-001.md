# RESULT — s-shape-g-5a7c-issue-revision-001

direction: indie-game-development
play: shape
node/task: g-5a7c / t-issue-revision-1, вне полос волны
track: —
date: 2026-08-31
outcome_kind: shape — ревизия склада улик нарезана его словом и выдана в свободный слот; заодно проведена ревизия самого направления, и первое, что она нашла, — ошибка этой же сессии

owner_approved: |
  Его слово 2026-08-31, дословно: **«давай ревизию»** — в ответ на предложение пустить в свободный
  слот не починку, а перезамер склада улик. Вторым предложением того же сообщения он спросил про
  само направление: **«и я имел ввиду также наш direction ничего чинить не нужно?»** — и это
  прямой заказ на ревизию репозитория направления, исполненный этой же ногой.

  Замах девятой волны НЕ расширен: задача заведена **вне полос**, как работа ставки в
  освободившуюся мощность. Прецедент — полоса закрепления, объявленная его словом накануне.

outcome: |
  **СКЛАД УЛИК ПЕРЕСТАЛ БЫТЬ ОЧЕРЕДЬЮ РАБОТ, И ЭТО ИЗМЕРЕНО, А НЕ ПОЧУВСТВОВАНО.** Направление
  проверило шесть открытых улик перед тем, как выбрать работу в свободный слот, и **четыре из
  четырёх проверенных оказались протухшими**:

  - «в игре нет мебели, тело хозяина не объявлено» — **половина починена чужой ногой**:
    `SceneHousePlan.cs:86` передаёт `furnishings` и высоту потолка шестиаргументным конструктором,
    а `HouseholderRouteController.cs:289` зовёт `WithBody(body)` боевым кодом, не тестом;
  - «в `dev` живёт подменённое `Is.EqualTo(3L)`» — **исчезла**, в `main` правильное `2L`;
  - «капсула радиусом `0.5` задевает косяк» — **число протухло вдвое**, сегодня
    `capsuleRadius: 0.25`;
  - «неизвестный темп роняет сессию каждый тик» — **переехало**: проверка зовётся ОДИН раз под
    `if (!_seated)`, до первого шага, с объясняющим комментарием.

  Три живых кандидата на починку упёрлись каждый в своё: самый ценный (опечатка темпа убивает
  сессию, а владелец вот-вот сядет писать содержимое профиля) правится в
  `NetworkWalkerCourier.cs`, **который сейчас у ноги «почему»**; второй по ценности задевает
  поверхности **обеих** идущих ног; третий свободен, но его собственное число протухло вдвое.
  **Выбирать работу вслепую больше нельзя, и это причина ноги.**

  **РЕВИЗИЯ САМОГО НАПРАВЛЕНИЯ НАШЛА ЧЕТЫРЕ РАСХОЖДЕНИЯ, И ПЕРВОЕ — ОШИБКА ЭТОЙ ЖЕ СЕССИИ.**
  `t-close-safely-1` и `t-gate-headless-1` были закрыты сегодня БЕЗ `--status done` и остались
  со `status: active` внутри `closed/`. Панель показала бы «ЗАКРЫТО» вместо «сделано» — то есть
  два часа назад направление своими руками создало ровно тот класс расхождения записи и
  реальности, ради которого сегодня утром чинилось всё остальное. **Исправлено обеим.**

  Остальные три: `t-bench-by-fact-1` несла `unblock_when`, но стояла `open` вместо `blocked` —
  условие называет зрение, которое ещё не доехало; три файла в `work/` не имели трейлера
  `END_OF_FILE`. Всё исправлено этой ногой.

  **ЧТО НАПРАВЛЕНИЕ ПРОВЕРИЛО И НАШЛО ЗДОРОВЫМ.** Механических проблем после правок ноль. Живых
  карточек 161, закрытых 235, отчётов в `history/` 365. `NOW.md` — **шесть строк**, то есть улика
  `i-now-hot-file-over-ceiling-001` про потолок в 150 строк **тоже протухла** и уйдёт в ту же
  ревизию. Оставшиеся 61 замечание — устаревшие поля шапки (`kind`, `order`, `found`) на давно
  закрытых карточках, и `check` сам называет их не мешающими работе.

  **ОДИННАДЦАТЬ ЗАДАЧ СТАВКИ ЛЕЖАТ БЕЗ ПОЛОСЫ, И ЭТО НЕ ДЕФЕКТ.** Ставка держит больше задач, чем
  одна волна: подвал, модели лута, обстановка комнат, руки от первого лица и другие. Они не
  протухли — последние касания 30–31 августа. Названо здесь, чтобы следующая нарезка знала, из
  чего выбирает.

evidence: |
  **Продуктовый репозиторий**, перемерено на `main` = `dev` = `origin/main` = **`b22c00a2`**:

  - `Assets/TunnelCrew/World/SceneHousePlan.cs:86` — шесть аргументов, включая `furnishings`;
  - `Assets/TunnelCrew/World/HouseholderRouteController.cs:289` — `.WithBody(body)` в боевом коде;
  - `git show main:tests/.../HouseholderDecisionPipelineTests.cs` строка ~206 — `Is.EqualTo(2L)`;
  - `Assets/TunnelCrew/Settings/HouseholderWalkSettings.asset:32` — `capsuleRadius: 0.25`;
  - `Assets/TunnelCrew/Probe/Householder/AuthoritativeHouseholder.cs:480` — вызов проверки темпа
    под `if (!_seated)`, `:686` — сама проверка, бросает `ArgumentException`;
  - `Assets/TunnelCrew/Probe/House/HouseWalk.cs` — слово `Width` не встречается ни разу
    (улика про косяки жива по механизму, но не по числу).

  **Репозиторий направления:**

  - `osctl check` до правок: два замечания «закрыта со статусом 'active'» на `t-close-safely-1` и
    `t-gate-headless-1`; после правок — `механических проблем нет`;
  - `live/indie-game-development/NOW.md` — 6 строк;
  - три файла `work/` без трейлера, найдены обходом `tail -1`.

state_changes: |
  СОЗДАНО (1 задача + 1 наряд + 1 документ):
  - `t-issue-revision-1` → `active`, **вне полос волны**, шесть строк приёмки;
  - `c-exec-g-5a7c-issue-revision-1-001` → слот **WIN-U2**, база `b22c00a2`;
  - `work/2026-08-31-call-issue-revision-1.md`.

  ИСПРАВЛЕНО ПО РЕВИЗИИ НАПРАВЛЕНИЯ (6):
  - `t-close-safely-1` → `status: done` (было `active` внутри `closed/`);
  - `t-gate-headless-1` → `status: done` (то же);
  - `t-bench-by-fact-1` → `blocked` (несла `unblock_when`, стояла `open`);
  - трейлеры `END_OF_FILE` дописаны трём файлам `work/`.

  БЕЗ ИЗМЕНЕНИЙ: замах девятой волны, её полосы и её восемь задач.

captures: |
  - **ЗАПИСЬ, СДЕЛАННАЯ СЕГОДНЯ, УЖЕ РАЗОШЛАСЬ С НАМЕРЕНИЕМ ЧЕРЕЗ ДВА ЧАСА.** Два лёгких закрытия
    прошли без `--status done`, потому что писатель звал `card close --why` и не знал, что статус —
    отдельный флаг. Инструмент не обязан напоминать, но **ревизия его поймала за один прогон** —
    и это довод в пользу того, чтобы `osctl check` гонялся после каждого закрытия, а не когда
    вспомнят. Кандидат в правило.
  - **ГНИЛЬ ЗАМЕРОВ ИМЕЕТ ФОРМУ, А НЕ ТОЛЬКО ЧАСТОТУ.** Из четырёх протухших: одна умерла целиком,
    одна починена наполовину чужой ногой, у одной уехало ЧИСЛО, у одной уехало МЕСТО вызова.
    Четыре разных способа протухнуть на четыре улики. Значит перезамер не сводится к «ещё
    воспроизводится или нет» — и приёмка ноги написана под эти четыре формы.
  - **ВЛАДЕЛЕЦ САМ СПРОСИЛ ПРО ЗДОРОВЬЕ НАПРАВЛЕНИЯ**, и это первый раз, когда ревизия запущена по
    его вопросу, а не по расписанию. Нашлась ошибка той же сессии — то есть вопрос был не праздный.

decisions_needed: |
  Ни одного. Второй свободный слот (WIN-U4) остаётся пустым намеренно: он ждёт первую починку,
  которую ревизия подтвердит живой.

play_check: |
  1. OPEN — его слово «давай ревизию» плюс прямой вопрос про направление.
  2. SHAPE — задача заведена ВНЕ полос, замах волны не тронут; приёмка написана под четыре
     найденные формы гнили, а не под общее «перепроверить».
  3. (owner) — его слово получено до нарезки, процитировано дословно.
  4. CLOSE — RESULT, применение, коммит; наряд выдаётся в WIN-U2.

log: |
  ревизия склада улик нарезана его словом и выдана в WIN-U2: четыре из четырёх проверенных улик
  протухли четырьмя разными способами, и выбирать работу вслепую стало дороже, чем перемерить всё;
  ревизия самого направления нашла четыре расхождения, и первое — два сегодняшних закрытия без
  статуса done, то есть ошибка этой же сессии

next: |
  Идут три ноги: зрение U1, «почему» U3, ревизия улик U2. **WIN-U4 свободен намеренно** — ждёт
  первую починку, подтверждённую живой.

  Волна: закрыто шесть задач из девяти, три ждут починки зрения.
