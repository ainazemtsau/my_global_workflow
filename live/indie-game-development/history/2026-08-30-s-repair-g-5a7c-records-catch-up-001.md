# RESULT — s-repair-g-5a7c-records-catch-up-001

direction: indie-game-development
play: repair
node/task: g-5a7c / bet-g-5a7c-wave-8
track: —
date: 2026-08-30
outcome_kind: repair — карта, из которой нарезается следующая волна, называла мёртвый путь и врала слотами; девять карточек шестой-седьмой волн стояли незакрытыми при работе в main

owner_approved: |
  Батч показан ему целиком таблицей из семи строк до первой записи (шаг 4 плея `repair`), включая
  строку «чего НЕ трогаю и называю вслух». Его слово: **«да, закрывай восьмую волну»** — в ответ на
  сообщение, где было сказано «если да — пишу ремонт прямо сейчас, а закрытие волны делаю сразу за
  ним». Закрытие самой ставки в эту ногу НЕ входит: его пишет `review`, следующей ногой.

outcome: |
  **ПОЧЕМУ ЭТА НОГА ВООБЩЕ ПОНАДОБИЛАСЬ, ОДНОЙ СТРОКОЙ.** Карточка восьмой волны — это образец, по
  которому нарезается девятая: следующая нарезка списывает с неё раскладку полос, путей и слотов.
  Образец врал в обеих несущих колонках.

  **ГЛАВНОЕ: САМЫЙ ГОРЯЧИЙ ПУТЬ ПРОЕКТА В ТАБЛИЦЕ МЁРТВ.** Полоса `ядро` владела
  `Core/Householder/**`. Коммит `3c63dff2` перенёс все 62 файла хозяина в `Probe/Householder/`, и на
  сегодняшнем `main` по старому пути **ноль файлов**. Любой наряд, написанный по этой таблице,
  назвал бы несуществующий путь — ровно класс `i-call-named-path-absent-on-declared-basis`, который
  в этом направлении уже ловили четырежды.

  **ВТОРОЕ: СТОЛБЕЦ СЛОТОВ ВРАЛ ПО ВСЕМ ТРЁМ ПОЛОСАМ.** Коммит направления `3cb03ed8` переставил
  живую тройку на u1/u2/u3 его словом, а таблицу после этого не обновили. Нарезка разослала бы
  наряды в u3/u4, которые отстают от `main` на 22 и 44 коммита, вместо u1, стоящего ровно на нём.
  Это не теория: сегодня наряд `chore-result` стартовал с базы двухчасовой давности, не увидел
  переименования и написал новый файл прямо в закреплённую сборку.

  **ТРЕТЬЕ: ДЕВЯТЬ КАРТОЧЕК ШЕСТОЙ И СЕДЬМОЙ ВОЛН СТОЯЛИ НЕЗАКРЫТЫМИ.** Шесть нарядов (пять `ready`,
  один `delivered`) и три задачи в статусе `delivered`, которого у задачи не существует. Работа всех
  девяти в `main` с 27-29 августа. По карточкам две волны выглядели идущими.

  Сводка для нарезки называла пять нарядов; их **шесть** — `reach-wire-1-001` она пропустила.

  **ЧЕТВЁРТОЕ: ТРИ ПРАВИЛА ПРО ПРОБУ И ОПОРУ ЖИЛИ ТОЛЬКО В ОДНОРАЗОВОЙ СВОДКЕ** и исчезли бы вместе
  с ней. Самое ценное из них — «закрепление идёт ВНЕ волны» — решает, отбирает ли закрепление слоты
  у игровых ног. Перенесено в знание с указанием читателя и триггера.

  **ПЯТОЕ: СПИСОК, ПО КОТОРОМУ ВЛАДЕЛЕЦ СОБИРАЛСЯ ТЕСТИРОВАТЬ, ВРАЛ В ПЕРВОЙ СТРОКЕ.** Она обещала
  два занятия с фазами (12/5/8 и 10/5), удалённые из профиля тем самым коммитом, на который список
  же и ссылался как на дату замера. Строка 36 внизу это признавала, строка 1 — нет.

evidence: |
  Продуктовый `main` = `c7e926cf`; репозиторий направления на `509f2e98` в момент старта ноги.

  - **Мёртвый путь:** `git ls-tree -r --name-only main -- Assets/TunnelCrew/Core/Householder` →
    **0**; `... -- Assets/TunnelCrew/Probe/Householder` → 32 файла `.cs`. Переименование —
    `3c63dff2`, все строки `R100`.
  - **Слоты:** продуктовый `ae542864` «Integrate c-exec-g-5a7c-bench-place-1-001 **from WIN-U1**»;
    журнал `closed/t-furniture-plan-1.md` — «проверено на `slot/win-u3`»; реестр лиз на момент
    ноги ядра — WIN-U2. Коммит направления `3cb03ed8` — «живая тройка волны 8 переставлена на
    слоты u1/u2/u3 словом владельца».
  - **Девять карточек:** прочитан `status` каждой; пять `ready`, один `delivered`, три задачи
    `delivered`. Слияния в `main`: `bf7a2552` (bench-householder), `378a3fc5` (carry-place),
    `70cf2637` (thread-body), `8f679196` (reach-wire-1-001, 2026-08-27).
  - **Статус задачи:** законный список — `open | active | blocked | done`
    (`os/schema/direction-files.md`, шаблон task). `delivered` в нём отсутствует.
  - **Статус наряда:** законный список — `ready | running | waiting | blocked | paused`
    (`os/schema/direction-files.md:166`). `parked` в нём отсутствует.
  - **Ставки:** `closed/bet-g-5a7c-wave-6.md` и `...wave-7.md` лежали в `closed/` со
    `status: active`; закрытые ранее `wave-3`/`wave-4` несут `closed`. `osctl check` этого не ловит:
    его проверка покрывает виды `task` и `node`, вид `bet` не покрыт.
  - **Занятия удалены:** `git diff 01eff008 c7e926cf -- Assets/StreamingAssets/TunnelCrew/
    Householder/default.householder.json` — блок `occupations` вырезан целиком вместе с обоими
    шагами распорядка, которые его исполняли; распорядок стал одним шагом `wait-at-route-point`
    без места, а такой шаг код переводит в «стоять столбом» (`Householder.cs:526-546`).

  **Проверка после правок:** `osctl check` — замечаний **66**, столько же, сколько до ноги.
  Нового долга гигиены не добавлено.

state_changes: |
  Всё через `osctl`, рукой не правилась ни одна карточка.

  ИСПРАВЛЕНО (1 блок):
  - `bet-g-5a7c-wave-8`, блок `tracks` — путь полосы `ядро` `Core/Householder/**` →
    `Probe/Householder/**`; полоса `дом` получила обе половины (`Core/House/**` + `Probe/House/**`);
    столбец «Слот» приведён к факту (ядро u2, дом u3, полигон u1). Ниже таблицы добавлена поправка,
    называющая, ЧТО было неверно, — чтобы следующая нарезка не переписала её обратно. Весь
    остальной текст блока сохранён байт в байт.

  ЗАКРЫТО ПО ФАКТУ (9 карточек, каждой — причина и указатель на этот отчёт):
  - наряды: `bench-householder-1-001`, `carry-place-1-001`, `thread-body-1-001`,
    `grip-second-1-001`, `reach-wire-1-002`, `reach-wire-1-001`;
  - задачи `delivered` → `done`: `t-thread-body-1`, `t-bench-householder-1`, `t-carry-place-1`.

  ПРИВЕДЕНО К СХЕМЕ (2):
  - `bet-g-5a7c-wave-6` и `bet-g-5a7c-wave-7` — `status: active` → `closed`, правкой на месте в
    `closed/`, без открытия.

  СОЗДАНО (2):
  - `knowledge/bedrock-promotion-runs-beside-the-wave.md` — три правила про пробу и опору, с его
    дословными словами про отдельный слот, с замерами, опровергнувшими сводку в двух местах
    (`Probe/House` целиком не отрезается; начинать с `Marks`/`Gauges`, а не с `Whereabouts`), и с
    ловушкой замера возраста через переименование. Слот НЕ заморожен: он операционный факт.
  - `i-live-cards-point-at-closed-and-empty-bets-001` — остаток, который ремонт чинить не вправе.

  ИСПРАВЛЕНО В РАБОЧЕМ ДОКУМЕНТЕ (1):
  - `work/WHAT-THE-HOUSEHOLDER-CAN-DO.md`, строка 1 — переписана по фактическому профилю, с прямой
    пометкой, что прежняя редакция была неверна на том самом коммите, который называла.

  НЕ ТРОНУТО НАМЕРЕННО, И ЭТО ГЛАВНАЯ ГРАНИЦА ЭТОЙ НОГИ:
  - **Пять нарядов в статусе `parked`.** Законный `paused` требует `paused_by` со ссылкой на слово
    владельца; слова нет. Поставить его значит выдумать вердикт.
  - **Двенадцать живых задач** — семь на закрытых ставках, пять без ставки вовсе. Проставить `_bet`
    значит задним числом записать задачу в состав ставки; членство решает `review`/`shape`.
  - **Сама ставка `bet-g-5a7c-wave-8` не закрыта.** Её девять задач `done`, три `parked`. Закрытие
    ставки — работа `review`, и она следующая.
  - **Три карточки нарядов восьмой волны лежат в `closed/` с нетерминальными статусами**
    (`running`, `ready`, `ready`). Это не дефект этой ноги, а действующий порядок репозитория:
    закрытый наряд сохраняет последний статус (образец — `closed/c-exec-g-5a7c-react-data-2-001.md`,
    `status: waiting`). Менять порядок — вопрос схемы, не ремонта.

captures: |
  - `osctl check` не проверяет вид `bet` вообще: две закрытые ставки простояли со `status: active`
    незамеченными. Проверка покрывает `task` и `node`. Это находка про инструмент, не про состояние.
  - Сводка для нарезки насчитала пять стухших нарядов; их шесть. Если бы ремонт закрыл ровно
    названные пять, шестой всплыл бы следующим аудитом как новая находка.

decisions_needed: |
  Ни одного в этой ноге. Один переходит в следующую: судьба трёх припаркованных задач восьмой волны
  (`t-day-end-1`, `t-look-sound-1`, `t-thread-pull-1`) решается при её закрытии.

  Отдельно и заранее: `t-thread-pull-1` несёт цель «нить тянет НЕПРЕРЫВНО», а подписанный владельцем
  2026-08-30 экспорт `EXP-20260830-001` пишет «это не трос, это не непрерывный луч». Закрытие волны
  обязано это назвать, а не перенести молча.

play_check: |
  1. Назвать противоречие — сделано: карта врёт путём и слотами; девять карточек стоят
     незакрытыми при работе в `main`.
  2. Восстановить новейшим-первым — сделано по продуктовым коммитам и байтам, не по карточкам.
     Отчёты и сводка читались как улики: сводку в двух местах опроверг замер.
  3. Предложить исправленное состояние — сделано, по строке причины на правку, включая строку
     «чего НЕ трогаю».
  4. Подтверждение (owner) — батч показан таблицей из семи строк; его слово **«да, закрывай
     восьмую волну»**. До него не записано ничего.
  5. Дыра в OS → FRICTION — одна строка, ниже.

log: |
  карта нарезки перестала врать: путь ядра переехал в пробу ещё утром, слоты разъехались с
  реальностью, девять карточек шестой-седьмой волн закрыты по факту, три правила про опору спасены
  в знание

next: |
  Домой. Следующая нога — `review`, закрывает восьмую волну: девять задач `done`, три `parked`
  ждут его решения. Она же снимает блокер с наряда на тестовый гейт и с нарезки девятой волны.

FRICTION: |
  2026-08-30 · `osctl check` не покрывает вид `bet`: две ставки простояли в `closed/` со
  `status: active`, и ни одна проверка этого не сказала. Проверка знает про задачу без ставки и про
  второй корень, но про ставку не знает ничего. Рядом второй случай того же дня: пять живых нарядов
  стоят статусом `parked`, которого нет в списке законных статусов наряда, и это тоже не ловится.
  Общее у обоих — проверка сверяет СВЯЗИ карточек и не сверяет ЗНАЧЕНИЕ поля `status` со списком,
  законным для этого вида. Вторая такая запись откроет маршрут на изменение OS.
