# RESULT — s-work-g-5a7c-react-mem-close-001

play: work · close: **light — because** все три строки `done_when` наряда и все шесть строк карточки
`t-react-mem` перемерены ПЕРВОЙ РУКОЙ по закоммиченным байтам опубликованной головы: набор тестов
прогнан здесь на выгруженном дереве публикации, места сборки памяти, границы и состав диффа
проверены командами, прогнанными здесь. Ни одной строки, которую судит его глаз, его ощущение или
чужой прогон, в этой задаче нет — наряд прямо снял с неё Unity и вердикт владельца
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-mem ·
call: c-exec-g-5a7c-react-mem-001
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `33534c53`, дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге ПОСЛЕ его слов «dev закончил»: `origin/dev` = `origin/main` =
`5fe0dadbc7aa54751e7dd267d670a2de3da85746`. Слот WIN-U2 по файлу состояния — `AVAILABLE`,
`lease: none`.

## outcome

`t-react-mem` закрыта. **Носитель существует: событие несёт метаданные, хозяин помнит несколько
событий сразу, накопленное переживает истечение, и его логику решений можно подменить в игре.**

**Главный гейт задачи взят по-настоящему, и он был не про функцию, а про ЦЕНУ.** Задача заводилась
потому, что каждое новое поле памяти стоило семи правок и дорожало с каждой построенной ногой.
Перемерено на опубликованной голове: **ноль из пяти мест сборки `HouseholderLocalMemory` тронуты** —
`git diff 6599ae87..5fe0dadb` по `Householder.cs` не содержит ни одной строки
`new HouseholderLocalMemory(`. Восьмое поле факта въехало, не тронув ни одной сборки.

## evidence — перемерено и прогнано первой рукой на `5fe0dadb`

### Опубликовано ровно то, что было проверено

История `6599ae87..5fe0dadb` — тринадцать коммитов: три продуктовых (`be15e2b4`, `318a780f`,
`f594e86a`), три отката, три реаплая и четыре документных. **Дифф КОДА от проверенного кандидата
`598e6368` до опубликованного пуст:** `git diff --stat 598e6368..5fe0dadb -- Assets tests core
ProjectSettings Packages tools validation.config` не вернул ничего. Весь дифф кандидат→публикация —
**один добавленный документ** `docs/results/c-exec-g-5a7c-react-mem-001.md`. То есть опубликованы
ровно те байты, которые нога проверила на предыдущем шаге.

### Прогон сделан здесь

```text
dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release
Пройдено 179, не пройдено 0, пропущено 0, всего 179
```

На выгруженном `git archive 5fe0dadb` дереве, слот не тронут. Голова дорожки `6599ae87` даёт
`173/173` — тоже прогнано этой ногой. Разница ровно `+6`: три ограничения пересмотрены на месте,
шесть проверок добавлены.

### `done_when` 1 — метаданные и несколько фактов

`HouseholderNoticedFact` (`Householder.cs:232-248`) несёт **восемь** полей, прочитанных поимённо:
`Name`, `OccurrenceId`, `Position`, `NoticedAtSeconds`, `ExpiresAtSeconds`, `Loudness`, `IsHandled`,
`OccurrenceCountAtPlace`. Метаданные едут и выше по течению: `HouseholderEvent` получил `Position` и
`Loudness` (`:128-132`) — **громкость больше не умирает в полосе**, хозяин впервые способен отличить
еле слышный шорох от почти-стука.

Память держит набор вместо одного факта; бросок на втором разнородном снят — тесты, которые его
ассертили, названы поимённо и пересмотрены (ниже).

**Дешевизна доказана диффом, а не обещанием:** ноль из пяти мест сборки.

### `done_when` 2 — накопление и политика в игре

Накопление живёт на самом факте (`OccurrenceCountAtPlace`) и читается решателем через снимок:
`TimesNoticedAt` на снимке решения (`:756`) и на памяти (`:1442`). Это величина, а не бирка.

Политика ставится на СУЩЕСТВУЮЩИЙ объект — `DecisionPolicy` стало свойством
`AuthoritativeHouseholder` (`:143-154`), по умолчанию singleton, `null` возвращает singleton.
**`Core/Multiplayer/CarryWorld.cs` не изменён вовсе**, обе публичные перегрузки `Advance` (`:97`,
`:115`) целы байт в байт — то есть требование наряда исполнено ровно так, как было измерено при
выдаче.

### `done_when` 3 — поведение, границы, откат

**Границы целы, и это доказано ПУСТЫМ выводом на опубликованной голове.** `git diff --name-only
6599ae87..5fe0dadb` по `Core/Cargo/**`, `Network/**`, `Scenes/**`, `ProjectSettings/**`,
`Assets/GasCoopGame/**`, `Core/Multiplayer/CarryWorld.cs`, `StreamingAssets/**`, `Settings/**` и
`Core/Householder/HouseholderReaction.cs` — **пусто**. Отсюда без отдельных проверок: пороги
`60 / 150 / 400` целы, файл профиля цел, сцена цела, **каталог пяти реакций не тронут** — те же пять
реакций и те же числа.

**В `tests/` изменены ровно ДВА файла** — `HouseholderAdditionalReactionTests.cs` и
`HouseholderInTheJudgesTickTests.cs`, то есть ровно те, где живут три названных наряду поимённо
теста. Прочие тестовые файлы не тронуты ни байтом.

**ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ, И ЭТО ПЕРВЫЙ РАЗ В ЭТОЙ ДОРОЖКЕ.** Перемерено:
`git rev-parse b035861f^{tree}` = `5734e0ba59694f35cebead61a9483a742b5b784e` = дерево головы дорожки
`6599ae87` знак в знак; реаплай `598e6368^{tree}` = `f594e86a^{tree}` = `c9f6014e…`. На `t-react-1`
отката не было вовсе, на `t-situation-1` он был взят с возврата.

### Слот вернулся

WIN-U2 — `lifecycle: AVAILABLE`, `lease: none`, прочитано файлом состояния, а не со слов возврата.

## Что НЕ проверено и на закрытие не влияет

Оба утверждения возврата, опирающиеся на его слова, остаются непроверенными и живут карточкой
`q-two-return-claims-rest-on-unverified-owner-words-001`: снятие СТОПа при уехавшем базисе и
owner-eye, которого наряд не просил. **Ни одно не входит в `done_when`**, поэтому светлое закрытие
ими не затронуто. Unity-редактор код не собирал — наряд прямо ограничил доказательство engine-free
ядром и headless-тестами, и это названо, а не замолчано.

## state_changes

1. `t-react-mem`: закрыть, терминальный статус `done`. Закрыта СВЕТЛЫМ путём — названо вслух по
   букве `evaluator` клауза 2.
2. `c-exec-g-5a7c-react-mem-001`: закрыть — наряд исполнен и опубликован в `5fe0dadb`, слот вернулся
   `AVAILABLE / lease none`.
3. `q-two-return-claims-rest-on-unverified-owner-words-001` остаётся открытой: его слово не получено,
   и на закрытие задачи оно не влияло.
4. `NOW.md`, `g-5a7c`, текст ставки и дорожка `cargo-hands` не тронуты. Дорожка `householder`
   освободилась: следующий корень — `t-react-2`.

## captures

- **Базис наряда протух второй раз подряд**, оба раза из-за публикации параллельной дорожки. Откат
  этой волны привязан к голове дорожки и потому выжил, а базис по-прежнему пишется константой.
  Место для ревью волны.
- **`origin/slot/win-u2` остался на `e4df0459`** третий раз подряд; селектор читает локальное.
- **У WIN-U2 снова `mcp_endpoint: unrecorded`**, хотя мост живой и его адрес измерялся
  (`http://localhost:27497/p/41ebd445`). Запись endpoint не переживает освобождение аренды.
- **WIN-U1 держит `c-exec-g-5a7c-settings-1-001:BUILD`** — наряд другой полосы, эта нога его не
  касается.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.

## decisions_needed

Нет. Дорожка свободна, следующий наряд пишется без его участия: требование «вес строки вычисляемый,
а не константа» уже вписано в `t-react-2`.

## play_check

- 1 recite: done — шесть строк карточки и три строки наряда сведены
- 2 owner inputs (owner): done — от него ничего не требовалось; наряд прямо снял с задачи Unity и
  его вердикт
- 3 do the work: done — опубликованное перемерено первой рукой, прогон сделан здесь
- 4 self-check: done — каждая строка закрыта своим измерением; непроверяемое названо непроверяемым
- 5 close: done — светлый путь, назван вслух; продолжение дорожки — `t-react-2`

## Гейты

- **G1.** Дорожка `householder` освободилась; `cargo-hands` держит свой корень; лимит `2` цел.
- **G2.** Задача служила активной ставке.
- **G3.** Аппетит не тронут.
- **G5.** Светлый путь по букве плея и по букве `evaluator` клауза 2: каждая строка перемерена
  первой рукой по закоммиченным байтам. Отдельный свежий чат не открывался, и это названо.
- **G7.** Ничего продуктового или геймплейного этой ногой не решено.
- **G9.** Карточки задачи и ставки не редактировались, только закрыты и снабжены журналом.
- **evaluator, клауза 1.** Закрытие ПОСЛЕ публикации.
- **evaluator, клауза 5.** Кандидат собран и опубликован НА голове дорожки `cargo-hands`
  `6599ae87`, а не на устаревшем базисе — проверено историей `6599ae87..5fe0dadb`.

## log

`t-react-mem` закрыта светлым путём на опубликованной голове 5fe0dadb, и главный гейт был не про функцию, а про ЦЕНУ: ноль из пяти мест сборки памяти тронуты, то есть восьмое поле факта въехало, не потребовав семи правок, ради чего задача и заводилась; событие несёт восемь полей — что, где, когда, насколько ГРОМКО числом, номер случая, обработан ли и сколько раз такое уже было в этом месте, — громкость больше не умирает в полосе, память держит набор вместо одного факта, накопление читаемо решателем через снимок, а политика решений стала свойством на существующем объекте, и CarryWorld не изменён вовсе; 179/179 прогнаны этой ногой на выгруженном дереве публикации против 173/173 на голове дорожки, разница ровно шесть; дифф КОДА от проверенного кандидата до опубликованного ПУСТ — сверху лёг один документ, то есть опубликованы ровно проверенные байты; границы доказаны пустым выводом по девяти запрещённым поверхностям, каталог пяти реакций не тронут, в тестах изменены ровно два разрешённых файла; ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК — впервые в этой дорожке; два утверждения возврата про его слова остаются непроверенными карточкой и в done_when не входили

## next

1. **Дорожка `householder` свободна — следующий корень `t-react-2`**: точка расширения, дефолт по
   метаданным плюс обработчики на выбранные комбинации, добавляемые без правки ядра и умеющие
   переключить ветку поведения. Требование «вес строки вычисляемый, а не константа» уже вписано в
   карточку.
2. Дорожка `cargo-hands` держит свой корень (`c-exec-g-5a7c-settings-1-001` в WIN-U1).
3. Ждут его слова, без вопроса: два утверждения возврата, порог смерти (5), текст `## approach`
   ставки.
