# RESULT — s-review-g-5a7c-first-wave-met-001

play: review · verdict: **met**
direction: indie-game-development · node: g-5a7c · bet: bet-g-5a7c
date: 2026-08-10

base_read: OS — `wt/indie-game-development` = `3f34f034`, рабочее дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` = `origin/slot/win-u1` =
`43648b2dd4fbaaf9570bd56cdf440cc904e1c222`. Опровержение шло по байтам в worktree
`GasCoopGame_dev` на `43648b2d`, дерево чисто, продукт этой ногой НЕ изменялся. Свежий физический
чат: ни одна задача волны здесь не закрывалась.

## outcome

Первая волна узла `g-5a7c` закрыта вердиктом **`met`**. Узел НЕ закрывается — он многоволновой
собственным словом владельца («следующая волна будет закрывать ядро, уже даже несколько»), и его
текст не изменён ни в одном знаке.

Что доказано и пережило опровержение: мир стоит в пропорции роста налётчика `0.16` м; у игрока и у
груза есть высота, которую считает сервер и одинаково видят обе копии и камера; добычу берут и
несут лучом на расстоянии; чем больше рук взялось, тем выше идёт груз; оба шва — личная мощность
хвата и именованный класс груза — заложены так, что старого общего источника чисел в коде НЕ
СУЩЕСТВУЕТ.

Почему `met`, а не `partial`: под опровержением не сломалась ни одна клауза. Всё, что не построено,
СНЯЛ САМ ВЛАДЕЛЕЦ до или во время работы — `t-scale-2` его словом «снимаем», `t-scale-3` заменена
пробой `t-vert-1`, первая строка `t-scale-5` снята проверкой происхождения как его вход, а не
требование, и позже отложена его же «Оба варианта А». Это не `killed`: ни один из трёх порогов
смерти не сработал. Это не `obsolete`: назначение узла не отменено.

## evidence — перемерено и прогнано первой рукой в этом чате

**Тесты прогнаны здесь, а не приняты с отчёта.** `dotnet test tests/TunnelCrew.Core.Tests/
TunnelCrew.Core.Tests.csproj -c Release` на `43648b2d` → **пройдено 151, не пройдено 0, пропущено 0**.
Рабочее дерево после прогона чисто (`bin/`/`obj/` игнорируются, проверено `git check-ignore`).

**Пропорция.** `NetworkPlaySettings.asset` на HEAD: `_walkerHeight: 0.16`, `_walkerRadius: 0.036`,
`_eyeHeight: 0.136`, `_movementSpeed: 0.6`, `_cargoPickupRange: 1.28` (8 ростов), `_cargoGripLength:
1.6`, `_cargoHandReach: 0.016`. Полный след волны — `git diff 352f96b0..43648b2d --
NetworkPlaySettings.asset`: скорость `3 → 0.6`, рост `2 → 0.16`, радиус `0.45 → 0.036`, глаз
`1.7 → 0.136`, груз `2.4×0.5×0.5 → 0.192×0.04×0.04`, масса `4 → 48`, число рук `2 → 4`, потолок
руки `_cargoMaxHandForce: 200` УДАЛЁН и заменён на `_cargoHoldStrength: 200` (личная мощность).

**Вертикаль у игрока — сервер, обе копии, камера.** Высоту считает `AuthoritativeWalkerRoster.
Advance` (прыжок — защёлка, полёт — удержание, гравитация `3`); `WalkerSnapshot` везёт `Y`;
`NetworkWalkerPresentation` рисует `new Vector3(snapshot.X, snapshot.Y, snapshot.Z)` — прежний
вбитый ноль исчез, это единственная содержательная правка файла за волну; `NetworkWalkerCamera`
садится на `standing.y + EyeHeight`. Клиент шлёт только байт намерения (`SubmitInputServerRpc`).

**Вертикаль у груза и структурная тишина.** `CargoState.Y` + `IsActive`; `CargoSnapshot` везёт `Y`
и `CargoClassId`. Посадка: `landed = !held && IsActive && Contact.HasSupport` → тело выходит из
симуляции, `ReadPoses` его пропускает, `ApplyPull` не вызывается. Тишина структурная, а не
настроенная.

**Ступень подъёма пересчитана этой ногой из живых настроек, а не принята с возврата.** Вес
`48 × 3 = 144` Н; каждая рука даёт `(0.092 − y) × 2000` с потолком `200` Н на руку. Равновесие:
**1 рука → 0.020 м, 2 → 0.056, 3 → 0.068, 4 → 0.074**, разброс `0.054` м. Число `0.020` совпадает с
высотой лежащего груза (полувысота класса `0.04/2`) — то есть одному груз ФИЗИЧЕСКИ едет по полу.
Затухание держится на `1` при любом числе рук (`VerticalDampingCoefficient` по корню суммарной
жёсткости), поэтому «лишняя рука делает пружинистее» закрыто устройством.

**Швы.** `grep -rn "CargoLength\|CargoWidth\|CargoHeight\|CargoMass" Assets/ tests/ --include=*.cs`
→ **пусто**, прогнано здесь. `grep -rn "MaxHandForce"` → **пусто**. Потолок читается у каждой руки
(`CargoHaul.EffectiveHandLimit` → `hold.Strength`).

**Дом и хозяин не тронуты по составу.** `IntegratedHouse.unity` держит `ThreeRoomHouse` и
`Householder` отдельными живыми корнями (`m_SourcePrefab` совпадают с `.meta` заготовок). Сборка —
две сцены: `Lobby` и `IntegratedHouse`.

**Перетягивание УЖЕ работает и доказано.** `CargoHaulTests.TwoHandsPullingOppositeEndsOppositeWays_
TurnTheCargoWithoutMovingIt`: сила по X и Z ноль, момент не ноль. Входит в прогнанные 151.

### Три места, где запись расходится сама с собой — названы, не спрятаны

1. Подписанный аппетит ставки обещал «мир переезжает на рост 0.16 м **с единой названной точкой
   масштаба**». Точки нет: владелец снял задачу словом «снимаем», а строку 1 карточки узла
   переписал и подписал «да, оформляй» — «общей настройки масштаба нет и не заводится». Текст
   аппетита при этом остался прежним. Клауза не сломалась, её убрал владелец; карточка узла
   говорит правду, карточка ставки — историю.
2. Первая строка `done_when` задачи `t-scale-5` (переменная дальность луча на держателя) НЕ
   ПОСТРОЕНА СОЗНАТЕЛЬНО, и текст строки нарочно оставлен нетронутым, чтобы `review` это увидел.
   Увидел. Долг живой и он владельцем отложен, а не потерян.
3. Размер аппетита назывался как «СЕМЬ задач»; фактически заведено восемь, две сняты владельцем,
   шесть построены. Объём не превышен ни разу.

### Что нашла эта нога и не назвала ни одна нога волны

**Из четырёх кусков лута доставить можно только один.** `CarryWorld.Advance:108-109` —
`Deliveries.Observe(Cargo.GetState(0))`. Строка унаследована с тех пор, когда груз был один, и
волна её не трогала законно: наряд `t-scale-4` прямо запрещал трогать доставку. Но последствие
своего же добавления (`_additionalCargoSpawns` — три куска) волна не назвала. **Слово владельца
2026-08-10: не чиним** — «это же не то, что будет… разного типа луты будут, поэтому сейчас такая
заглушечная реализация, хрен с ней». Заведена улика.

**Мир не уменьшился — уменьшилась мышь.** Скорость `3 → 0.6` м/с при доме прежних размеров
(`ThreeRoomHouse.prefab`: стены `14.5 × 1.5 × 0.5` и `0.5 × 1.5 × 14`). Комната — 87 ростов мыши,
23 секунды пешком, 39 с грузом (`_carrySpeedScale: 0.6`). Ни один экран ставки этого не считал.

**Вертикаль есть у мыши, но не у мира.** `WalkSpaceProbe` знает РОВНО ОДИН `_verticalProbeBox`
(`0.9 × 0.72 × 0.9`, верх на `0.72` м), у стен верхов нет вовсе — они остались плоскими
преградами на любой высоте. Встать можно только на этот ящик и на крышки кусков лута
(`AuthoritativeCargoRoster.ResolveWalk`, выбор верхов до боков). Прыжок берёт `2.2²/(2×3) = 0.81` м.

**Вырезанный критерий 4 частично сделал себя сам.** Один носильщик держит груз на `0.020` м, то
есть на полу, живым телом → контакт каждый тик; двое и больше поднимают на `0.056`+ → контакта нет.
«Один шумит, несколько молчат» материально работает. Оговорка: это расчёт, инструментального
прогона шума не делалось.

**Одна мёртвая ручка.** `_cargoRestHeight: 0.02` едет в `CargoRules.CargoRestHeight` и не читается
никем (`grep` по `.CargoRestHeight` в `Assets/**` пуст) — подтверждено независимо от заведшей ноги.

## verdict_vs_forecast

Прогноз ставки был `no_basis`, и это было честно. Теперь наблюдений три, и образец виден:
11 задач / 0 новых слоёв → дошла; 23 / 3 → `partial`; 7 (факт 8, из них 6 построено) / 1 → `met`.
Числа вероятности по-прежнему нет и не выводится, но правило для следующего шейпа названо:
**до восьми задач и не больше одного нового слоя.**

СЮРПРИЗ ПРОТИВ `against`. Довод против гласил: волна трогает самое доказанное место игры и «не
испортится это бумага, а не факт». Переключатель — его слово после `t-scale-4` — не сработал:
переноску он принял повторным прогоном. Укусило в другом месте, и это был процесс, а не продукт:
наряд на класс груза ФИЗИЧЕСКИ не дошёл до исполнителя, стоил полного круга работы и опубликованной
в `main` пустышки. Второй укус — 24 ноги за четверо суток на шесть построенных задач.

## lens_harvest

1. **Октябрьское демо и выпуск.** Волна двигала ядро. Календарь напряжён и назван владельцу:
   его собственный пересмотр октябрьского маршрута — `2026-08-15` (через пять дней), регистрация
   на фестиваль — `2026-08-31` (`g-2b7f.by`), ни одна строка Steam-работы не начата.
2. **Понятность и качество для игрока.** Впервые существует ощущаемая механика, принятая его
   руками: луч берёт на 8 ростов, груз поднимается суммой рук. Линза требовала задачу `t-scale-5` —
   она закрыта его словом.
3. **Реально ли одному закончить.** Шесть построенных задач за четверо суток, но 24 ноги — четыре
   ноги разговора на одну задачу постройки. Это цена, которую волна нигде не считала.
4. **Новый одобренный канон.** Волна канона не создавала. Неснимаемый минимум устава: груз есть,
   хозяин есть, тоннеля нет нигде — факт для будущей карты, не дефект волны.
5. **Платный продукт и коммерческое знание.** Не прибавилось ничего; линза так и говорила.
6. **Надёжность работы владельца.** В минус и крупно: наряд не доехал до исполнителя, и OS этого
   не заметила — у неё есть обязательные сверки слота, базы, дерева и версии контракта и ноль
   проверки, что текст приказа доставлен. В плюс: композиция масштаба, которую он судил, сохранена
   отдельной пробной сценой вне сборки (`2904b596`).
7. **Проще или сложнее.** Сложнее в одном месте (высота у двух сущностей, два шва) и по-настоящему
   проще в другом: игра больше не знает «размеров груза» — она знает классы предметов, и второй
   класс приезжает без правки закона, что доказано тестом на подставленном втором классе.

## add_back

**Один вырез не удержался, и это главная находка шага.** Вырез №5 гласит дословно: «ХОЗЯИН
ЦЕЛИКОМ… Хозяин в волне работает ровно как сегодня». На байтах это неверно: хозяин получил ПЯТУЮ
реакцию `InvestigateLandedCargo` — новое занятие, новый приоритет, новый повод, новый замеченный
факт, новое событие, новый вид цели действия (`SituationPoint`), ветка в решателе, настройка
`cargoLandedLifetimeSeconds: 8` и зелёный тест. Пришло из `t-scale-4`: чтобы лежащий груз замолчал,
посадка стала отдельным событием, а вместе с событием заехала реакция на него. Ни одна нога волны
этого не назвала. На прошлом ревью владелец говорил «второй профиль и пять различимых реакций точно
снимаем». Вынесено ему решением, не исправлено здесь.

**Вырез №3 (весь общий слой шума) поехал частично:** построен типизированный `CargoSituation`
(`ContactedHouse`/`Landed`) со списком `Situations`, а шум переименован в `HouseholderStimulus`
(`Hear` → `Observe`). Это начало вырезанного слоя. Оно уже ведётся уликой про архитектуру ситуаций
и становится содержанием дорожки 2 следующей волны.

**Пять вырезов держатся, проверено байтами:** наклон груза заперт (`CargoBody.TiltConstraints =
FreezeRotationX | FreezeRotationZ`); редакторской кнопки пересчёта нет (`StartConditionsEditor`
изменён на чтение проекции класса, шесть строк); дом остался коробкой со стенами `1.5` м;
хозяин-профиль не расширен; заход, инструмент цепи, облик и скриншоты не тронуты.

**Трусливых вырезов нет.** **Пропущенный вырез есть и он дорогой:** нигде не было названо, что
уменьшение мыши в 12.5 раза при неизменном доме делает заход в пять раз длиннее. Это не настройка
скорости, это форма игры, и она не проходила ни через один экран.

## owner_words

Три слова этой сессии, каждое на прямо заданный вопрос, записаны той силы, какой были:

1. **Полёт** — «Ну, полет пусть пока остается, но, естественно, мы будем пересматривать. Но пока
   пускай будет.» То есть полёт ОСТАЁТСЯ, и это решение временное его же словом.
2. **Доставка** — «Ну, нет, сейчас ничего… у нас там это же не то, что будет… будешь приносить
   разного типа луты будут, поэтому сейчас такая заглушечная реализация, хрен с ней.» Не чиним.
3. **Строка критерия 4** — «давай, как ты рекомендуешь, пусть и оставим». Фраза «„позвать ещё
   одного" становится решением про риск» ОСТАЁТСЯ в подписанном тексте: волна её материально
   заработала. Долг `card_line_not_touched` из шейпа этим закрыт, ноги `map` не требуется.

Четвёртое и пятое — про следующую волну:

4. **Форма** — «Давай две дорожки, дом следующей», на показанные три варианта и мой довод против
   дома («Если против, обязательно пиши, почему» — довод дан: файл сцены уже разводил полосы в
   `g-1d84`, дом и хозяин это одна работа, дом это волна кадров).
5. **Референс** — «Да, R.E.P.O.» на прямой вопрос, и отдельно: «я так подразумеваю, что мы сделаем,
   что если приложено к одной точке, то другая будет провисать. Точно как в игре Repo».

## knowledge

Три записи, каждая с реальным `read_by`:

- `knowledge/an-order-does-not-exist-until-it-is-delivered.md`
- `knowledge/a-cut-holds-only-where-bytes-are-measured.md`
- `knowledge/shrinking-the-hero-does-not-shrink-the-world.md`

## state_changes

1. `bet-g-5a7c` закрыта, терминальный статус **`met`**.
2. `g-5a7c`: `status` `active` → `shaped`. **Ни один знак текста карточки не изменён** — ни цель,
   ни двенадцать критериев, ни `why`/`risk`/`edge`, ни `appetite`, ни `kill_by`. `appetite` и
   `kill_by` описывают ЗАКОНЧЕННУЮ первую волну и переписываются следующим `shape`, а не здесь.
3. `NOW.bet` → null. Полос у закрытой ставки не было, распускать нечего.
4. Заведено: `d-householder-fifth-reaction-unbought-001` (ждёт его слова),
   `i-delivery-observes-only-the-first-cargo-001` (его словом не чинится).
5. Переставлены триггеры у шести улик, чей срок наступил закрытием ставки:
   `i-flight-makes-the-way-up-a-mouse-property-001` (его слово записано, полёт остаётся с
   пересмотром), `i-lift-step-accepted-but-small-001` и `i-telekinesis-carry-distance-is-fixed-001`
   (дорожка «груз слушается рук»), `i-bigger-loot-needs-the-cargo-class-seam-001` (шов существует,
   ждёт волну дома), `i-scale-verdict-covers-size-not-chase-001` (погоня и скорость судятся волной
   дома и хозяина), `i-published-to-main-before-any-direction-close-001` (следующий `shape` обязан
   написать `evaluator` — в закрытой ставке его не было вовсе).
6. Журнальная строка в `g-5a7c`, `bet-g-5a7c`, обе идеи следующей волны и остальные затронутые
   улики одним `osctl leg close`.
7. Полный RESULT — в `history/2026-08-10-s-review-g-5a7c-first-wave-met-001.md`.
8. CHARTER, продуктовый репозиторий и все прочие узлы НЕ менялись.

## captures

- Дорожки следующей волны встречаются ровно в одном месте — шов «ситуация мира»: груз (а позже
  обстановка и взгляд) производит ситуации, хозяин их потребляет. Зачаток вырос случайно из
  `t-scale-4`. Шейп обязан поставить его ОБЩЕЙ задачей до расхождения дорожек, иначе обе будут
  изобретать его врозь.
- Наклон груза и поворот груза — ОДНА работа: обе требуют, чтобы захват стал трёхмерной точкой
  (сегодня `GripX`/`GripZ` без высоты) и чтобы подъём перестал прикладываться в центр масс
  (`CargoBody.ApplyPull` — `AddForce(Vector3.up)` мимо точки, нарочно, комментарий на месте).
  Делать порознь — платить дважды.
- Профиль хозяина (`StreamingAssets/TunnelCrew/Householder/default.householder.json`, 20 строк)
  задаёт только РАСПОРЯДОК: два дела и их длительности. Реакции — шесть перечислений и ветка
  решателя. Владелец предполагал обратное; поправка ему выдана.
- Хозяин слышит РОВНО ОДНУ ситуацию за тик (самую громкую), а вторая другого рода во время
  активной реакции отвергается. «Реакция на комбинацию» сегодня структурно невозможна.

## decisions_needed

Одно, заведено карточкой и ждёт его слова: **пятая реакция хозяина уже в игре и куплена не была** —
убрать обратно или оставить. Рекомендация записана: оставить, потому что он всё равно захочет,
чтобы хозяин слышал грохот упавшего ящика, а выдирать работающее ради чистоты счёта — потеря.
Отдельно остаётся отложенным его же решением `d-agents-skills-belong-in-repo-001`, названное ему в
начале ноги.

## play_check

- **1 verify by refutation** — done: вердикт `met`; тесты прогнаны здесь (151/151), пропорция,
  вертикаль, тишина, ступень и оба шва перемерены по байтам на `43648b2d`; `killed` и `obsolete`
  исключены явно; три расхождения записи с собой названы.
- **2 harvest per lens** — done: все семь линз устава получили явную диспозицию, включая «не
  прибавилось ничего».
- **3 tree diff (owner)** — done: три артефакта вынесены по одному, три его слова получены и
  процитированы; текст карточки узла не изменён ни в одном знаке (G9).
- **4 add-back check** — done: вырез №5 сломан и назван, №3 поехал частично, пять держатся,
  пропущенный вырез назван.
- **5 knowledge** — done: три записи с реальным `read_by`.
- **6 forecast & next** — done: числа нет, `no_basis` заменён названным правилом размера; три
  волны показаны с честным размером, довод против дома дан по его прямому требованию, выбор
  сделан им.
- **7 close** — done: ставка закрыта, полос не было, ЗАДАЧ И ПОЛОС НЕ СОЗДАНО, CALL НЕ ВЫПУЩЕН по
  его прямым словам «нам не надо сейчас что-то запускать».
- **G1** — активной ставки не осталось, `NOW.bet` пуст.
- **G7** — ни одно игровое решение не принято ногой: полёт, доставка, строка критерия и форма
  волны закрыты его словами; пятая реакция вынесена ему решением.
- **G9** — текст узла не редактировался вовсе.

## log

g-5a7c: первая волна закрыта вердиктом `met` — пропорция 0.16, вертикаль у игрока и у груза, луч и ступень по рукам доказаны байтами и прогнанными здесь 151/151, ни одна клауза не сломалась, а всё неснятое снял сам владелец; ревью нашло три вещи, которых не назвала ни одна нога волны — доставка видит один кусок лута из четырёх, мир стал впятеро длиннее по времени, и вырез «хозяин работает как сегодня» сломан пятой реакцией; следующая волна выбрана им — две дорожки, дом следующей

## next

`shape` СВЕЖИМ ЧАТОМ режет вторую волну узла `g-5a7c` по его выбору: **одна ставка, две дорожки,
идущие одновременно**, плюс общая задача-шов до их расхождения.

- **Общая задача (до расхождения):** назвать шов «ситуация мира» — вид, источник, место.
- **Дорожка 1 «Груз слушается рук»** (референс назван им: R.E.P.O.): положение груза
  приблизить/отдалить и вверх/вниз с падением силы по выносу; поворот груза И провисание дальнего
  конца — ОДНОЙ задачей со своим откатом (вернуть две заморозки), судит его глаз; личная мощность
  хвата становится числом на игроке. Сюда же едут `i-telekinesis-carry-distance-is-fixed-001`,
  `i-lift-step-accepted-but-small-001`, `i-cargo-rest-height-setting-now-unread-001`.
- **Дорожка 2 «На что и как реагирует хозяин»**: реакции перестают быть шестью перечислениями;
  хозяин держит больше одной ситуации за тик; появляется комбинация. **Зрение НЕ строится**, но
  структура обязана принять «он увидел мышь» новым источником без правки закона — доказывается
  тестом на подставленном источнике, тем же приёмом, каким в этой волне доказан класс груза вторым
  классом. Сюда едут `i-world-situations-need-extensible-reaction-architecture-001`,
  `i-noise-bands-invisible-and-force-mis-scaled-001`, `i-dragged-cargo-rustles-every-tick-001`.
- **Шейп обязан:** написать `evaluator` ставки (в закрытой его не было вовсе), спросить у владельца
  число одновременных дорожек, принести ему решение про пятую реакцию хозяина, и держать размер в
  пределах «до восьми задач, не больше одного нового слоя» — иначе резать.
- **Дом как жильё — СЛЕДУЮЩАЯ волна его словом.** Он же владеет файлом сцены; отдавать его
  параллельной дорожке запрещено этим ревью.

END_OF_FILE: live/indie-game-development/history/2026-08-10-s-review-g-5a7c-first-wave-met-001.md
