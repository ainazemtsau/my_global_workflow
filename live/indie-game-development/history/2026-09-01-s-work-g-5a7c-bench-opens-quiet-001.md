# RESULT — s-work-g-5a7c-bench-opens-quiet-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-the-bench-opens-quiet-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — его догадка проверена и оказалась верна кодом: сценарий стенда не умеет ничего, кроме шумов, поэтому тишина по умолчанию ничего не ломает; задача заведена и выдана

close: light — because всё несущее перевыведено направлением первой рукой на байтах: содержимое сценария, автозагрузка, устройство полки, отсутствие проверки на непустоту, и главное — комментарий самого класса, объявляющий, что сценарий не несёт ни поведения, ни профиля.

owner_approved: |
  Его слово 2026-09-01, дословно: **«если мы уберем этот шум, это не поломает сценарий хозяина.
  То есть он будет заниматься своими делами. Если да, то давай уберем.»**

  **Слово УСЛОВНОЕ, и условие проверено до заведения задачи, а не после.**

outcome: |
  **ОН СПРОСИЛ ПРАВИЛЬНУЮ ВЕЩЬ, И ОТВЕТ НАШЁЛСЯ В КОММЕНТАРИИ САМОГО КЛАССА.**
  `HouseholderObservationScenario.cs:29-31`: «A flat, repeatable list of world situations.
  **It has no conditions, behavior, profile**…». Сценарий умеет ровно одно — подбрасывать шумы в
  заданные такты. День хозяина целиком приходит из `default.householder.json`.
  **Убрать шумы и сломать этим распорядок физически нечем.** Его догадка верна.

  **ОН ЖЕ НАЗВАЛ И РАЗВИЛКУ ПРАВИЛЬНО:** «если нет, то похоже какая-то проблема». Если бы день
  хозяина зависел от шума стенда, это был бы дефект куда крупнее самого шума. Проверка стоила
  двух чтений и закрыла оба исхода.

  **ЕГО ЦЕЛЬ ТРЕБУЕТ БОЛЬШЕГО, ЧЕМ ОН ПОПРОСИЛ, И ЭТО НАЗВАНО ПРЯМО.** Он сказал «уберём этот шум»
  про первый. Но в файле их **три**: такт 1 — шорох 5, такт 40 — скрип 20, такт 100 — удар 60.
  Убрать один — он всё равно не увидит непотревоженного дня, а это и есть то, чего он просит
  словами «он будет заниматься своими делами». **Поэтому задача сформулирована как тишина по
  умолчанию, а не как удаление одной строки** — и это сказано ему прямо, а не сделано молча.

  **ПРАВКА ОКАЗАЛАСЬ БЕЗ КОДА, И ЭТО ЗАМЕР, А НЕ НАДЕЖДА.** Полка сценариев
  (`HouseholderObservationWindow.cs:1039-1067`) читает **всю папку** и делает кнопку на каждый
  файл. Значит три шума переезжают в собственный файл и появляются кнопкой **сами**, а
  автозагружаемый файл становится пустым. Два текстовых файла, ноль строк кода.

  **ЕДИНСТВЕННОЕ МЕСТО РИСКА НАЙДЕНО И НАЗВАНО ЧЕСТНО.** Проверки на непустой список в разборе
  **нет** — единственный «empty» бросается на пустой строке json, а не на пустом массиве. Но
  `Start()` и `TakeNextStep()` на нуле событий **живьём не проверял никто**, и наряд требует
  проверить это первым делом, а не предположить.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** на `slot/win-u2` = `b4b010c1`:

  - `HouseholderObservation.default-scenario.json` — заголовок «три шума разной силы в разных
    углах», три ситуации: `atStep 1` сила `5` в `(0,0)`; `atStep 40` сила `20` в `(4,1)`;
    `atStep 100` сила `60` в `(-4,-2)`;
  - `HouseholderObservationWindow.cs:381-383` — окно **само** грузит этот файл при открытии;
  - `HouseholderObservationScenario.cs:29-31` — комментарий класса: **«no conditions, behavior,
    profile»**; разбор требует только `schema`, `title`, `situations`;
  - `HouseholderObservationScenario.cs:66` — единственный `«The observation scenario is empty»`
    бросается в начале `Parse`, **до** разбора массива: это проверка пустого json-текста, а не
    пустого списка ситуаций. **Проверки на непустой массив нет;**
  - `HouseholderObservationWindow.cs:1039-1067` — `DrawScenarioShelf` перебирает `Scenarios()` и
    даёт кнопку на каждый файл папки; файл с ошибкой показывается с жалобой, а не прячется.

state_changes: |
  СОЗДАНО (1 задача + 1 наряд + 1 документ):
  - `t-the-bench-opens-quiet-1` → `active`, полоса `слух`, семь строк приёмки;
  - `c-exec-g-5a7c-the-bench-opens-quiet-1-001` → слот **WIN-U2**, база `b4b010c1`;
  - `work/2026-09-01-call-the-bench-opens-quiet.md`.

  БЕЗ ИЗМЕНЕНИЙ:
  - `i-a-debug-button-lets-noise-skip-distance-001` остаётся открытой: **уходит только половина
    про шумящий сценарий**, врущие кнопки инспектора остаются в улике;
  - остальные карточки волны без изменений.

captures: |
  - **ЕГО ВОПРОС БЫЛ ПРОВЕРКОЙ ГИПОТЕЗЫ, А НЕ ПРОСЬБОЙ ОБЪЯСНИТЬ.** «Если да — уберём, если нет —
    похоже, проблема» — он сам назвал оба исхода и то, что второй хуже. **Условное слово владельца
    требует проверить условие ДО заведения задачи**, иначе задача заводится на его догадке, а не на
    его решении.
  - **ОТВЕТ ЛЕЖАЛ В КОММЕНТАРИИ КЛАССА, НАПИСАННОМ ЗАРАНЕЕ И ИМЕННО ПРО ЭТО.** «It has no
    conditions, behavior, profile» — ровно та гарантия, которой не хватало. **Второй раз за день
    ответ находится в чужом комментарии**, и оба раза — в комментарии, объясняющем границы вещи.
  - **ЕГО ПРОСЬБА БЫЛА УЖЕ ЕГО ЦЕЛИ, И РАЗНИЦА НАЗВАНА ВСЛУХ.** Он просил убрать один шум; чтобы
    получить непотревоженный день, нужна тишина по умолчанию. **Расширение объявлено ему прямо,
    а не сделано молча** — иначе это было бы раздуванием его просьбы.

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — его условное слово с проверяемым условием.
  2. WORK — условие проверено первой рукой и оказалось выполненным; отсутствие кода в правке
     доказано устройством полки; единственное место риска названо честно и вынесено в приёмку;
     разница между его словами и его целью названа ему прямо.
  3. (owner) — его слово получено и процитировано дословно.
  4. CLOSE — RESULT, применение, коммит; наряд уходит владельцу.

log: |
  его догадка проверена и верна: сценарий стенда по устройству не несёт ни поведения, ни профиля —
  только шумы, поэтому тишина по умолчанию распорядок хозяина сломать не может; правка оказалась без
  кода, полка сама сделает кнопку на новый файл, а единственное непроверенное место — пустой список
  событий в живом окне; ему прямо сказано, что убрать один шум мало для неподтревоженного дня

next: |
  Наряд `c-exec-g-5a7c-the-bench-opens-quiet-1-001` уходит владельцу для **WIN-U2**.

  Волна: голова закрыта, ходьба мертва, замирание в работе в U4, публикация ждёт его, стенд
  делается тихим.
