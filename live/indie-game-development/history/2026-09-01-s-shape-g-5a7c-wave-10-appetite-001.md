# RESULT — s-shape-g-5a7c-wave-10-appetite-001

direction: indie-game-development
play: shape
node/task: g-5a7c / bet-g-5a7c-wave-10, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: shape — замах волны подтверждён его словом «берём четыре»; три числа исходного экрана оказались протухшими за час и пересняты первой рукой; голова волны выдана нарядом

close: light — because каждая несущая строка перевыведена этой ногой на байтах: головы веток прочитаны, слияние проделано в выброшенной копии, тесты прогнаны направлением лично трижды (main, ветка ходьбы, слитое дерево), отказ красного прочитан дословно из вывода прогона. G5-чат не открывался.

owner_approved: |
  Его слово 2026-09-01, дословно: **«берём четыре»** — на показанный целиком экран волны: цель,
  две полосы, четыре задачи, пять порогов смерти, девять вырезов и довод против.

  Замах подтверждён как предложено. Ни одна строка задач не изменена по существу; правки блоков —
  исправление устаревших чисел, а не смена предмета.

outcome: |
  **ЭКРАН ВОЛНЫ, ПОКАЗАННЫЙ ВЛАДЕЛЬЦУ ЧАС НАЗАД, СОДЕРЖАЛ ТРИ НЕВЕРНЫХ ЧИСЛА — И УСТАРЕЛ ОН ЗА
  ВРЕМЯ САМОЙ НАРЕЗКИ.** Нарезка стояла на `main` = `064ab568`; пока она шла, в `main` уехали руки
  (`9a65fc00`) и дела (`0e72d610`). Ошибка ровно того класса, который направление ловит у себя
  вторую неделю: **замер снят, мир поехал, вывод остался.** Первым делом этой ноги было переснять,
  а не начать работу.

  **ЧТО ИСПРАВЛЕНО, ПОИМЁННО:**

  | было на экране | на самом деле |
  |---|---|
  | `main` = `064ab568` | **`0e72d610`** |
  | четыре готовых куска ждут публикации | **один** — ветка `slot/win-u4`; руки и дела уже в `main` |
  | публикацию держит один красный | **пять** на ветке; один — после слияния |

  **И ГЛАВНОЕ: ЦЕНТРАЛЬНОЕ УТВЕРЖДЕНИЕ ВОЛНЫ УСТОЯЛО, НО ТОЛЬКО ПОТОМУ, ЧТО ЕГО ПРОВЕРИЛИ РУКАМИ.**
  Направление проделало слияние в выброшенной рабочей копии `GasCoopGame_testrun`, ничего не
  коммитя. Конфликт **ровно один**, одной областью профиля (строки 278–405), и сторона ходьбы там
  **ПУСТА**: ветка вырезала дела, которые `main` с тех пор починил. Разрешение — взять сторону
  `main`. После него `routine 6 · handlers 7 · occupations 4 · gauges 1` и тесты **744 / 743 / 1**.
  **Обещание «останется один красный, и он про слух» — правда, и теперь она измерена, а не взята
  из чужого возврата.**

  **ЧЕТЫРЕ ЛИШНИХ КРАСНЫХ ОКАЗАЛИСЬ УЛИКОЙ, ПОПАВШЕЙ В ЦЕЛЬ.** Все четыре — тесты на содержимое
  поставочного профиля, красные только потому, что ветка ходьбы поменяла **расходный полигон**
  (`Expected: 7 / But was: 3`). Это ровно
  `i-mechanism-tests-stand-on-disposable-shipped-content-001`, заведённая вчера. Они гаснут
  возвратом содержимого, а не починкой — **улика остаётся открытой и записана в приёмку публикации
  как улика, а не как успех**.

  **КРАСНЫЙ ПРО СЛУХ ОКАЗАЛСЯ ДОСЛОВНОЙ ЖАЛОБОЙ ВЛАДЕЛЬЦА, СНЯТОЙ МАШИНОЙ.**

  ```
  `noise 5` at  1,0s -> who says ListenForQuietRustle    ← он стоит: услышал
  `noise 5` at 10,0s -> who says WalkRoute               ← он идёт:  НЕ услышал
  windows: 40,6s of 52,0s (78% of his day)
  ```

  **78% своего дня хозяин непрерываем, и каждый такой момент — ходьба.** Его слова: «я не хочу так,
  что когда он идет, он глух ко всему». Число теперь есть, и оно ушло в наряд и в цель волны.

  **НАРЯД ПОСТАВЛЕН НЕ НА `main`, И ЭТО РЕШЕНИЕ, А НЕ УДОБСТВО.** На чистом `main` дефекта нет
  физически: распорядок — один шаг «стоять одну секунду», непрерываемых моментов ноль, тест зелёный.
  **Дефект существует только там, где у хозяина есть день.** Поэтому стенд ноги — `main` со слитой
  веткой ходьбы, слияние объявлено стендом, а не поставкой, и публикация ноге прямо запрещена.

  **РАСТЯЖКА, ПОСТАВЛЕННАЯ НОГОЙ ХОДЬБЫ ИМЕННО НА ЭТУ ВОЛНУ, НАЙДЕНА И ПЕРЕДАНА ВПЕРЁД.**
  `HisDayIsOneWordToWhoAndNoWindowIsLeftToCatchHimIn` держит `windowSeconds > 0` и собственным
  комментарием говорит: «When that wave lands this goes red and is deleted, which is the point of
  measuring it here». **Без этой строки в наряде исполнитель упёрся бы в чужой красный и встал.**
  Оба исхода названы заранее: снялась с заменой — хорошо; осталась зелёной — тоже хорошо.

evidence: |
  **ВСЁ СНЯТО ЭТОЙ НОГОЙ ПЕРВОЙ РУКОЙ.**

  - головы: `main` = `dev` = `origin/main` = **`0e72d610`**; `slot/win-u2` = то же;
    `slot/win-u1` = `9a65fc00`; `slot/win-u4` = `687b6b55`;
  - `git log main` — `0e72d610 Close t-chores-can-finish-1`, `9a65fc00 Close t-carry-back-1`;
    `HouseholderHands.cs` присутствует в дереве `main`;
  - **прогон на `main`: не пройдено 0, пройдено 739, всего 739;**
  - **прогон на `slot/win-u4`: не пройдено 5, пройдено 739, всего 744.** Имена всех пяти сняты из
    вывода: `EitherMomentAnswersTheFirstNoiseAndTheSecondStillTakesHim` (`HandlersRecipeTests:236`),
    `TheCommittedProfileCarriesTwoChoresOneOfThemWithASpareAndAWayToDropIt`,
    `TheShippedChoreRowsGiveWayToSomethingHeJustNoticed`,
    `ArrivalEffectWrapperKeepsTheInnerConditionsNotMetReason`,
    `TheShippedQuietTickKeepsItsWinnerTargetAndSevenReasons`;
  - `git merge-tree --write-tree --name-only main slot/win-u4` → **exit 1**, конфликтный путь
    **ровно один**: поставочный профиль;
  - слияние материализовано в `GasCoopGame_testrun` (detached, чистая, после замера
    `merge --abort`, HEAD вернулся на `0e72d610`): маркеры — **одна область**, строки 278–405,
    сторона `slot/win-u4` пуста;
  - после разрешения «взять `main`»: `routine 6 · decisionHandlers 7 · occupations 4 · gauges 1`,
    JSON валиден;
  - **прогон на слитом дереве: не пройдено 1, пройдено 743, всего 744.** Единственный отказ и его
    вывод процитированы дословно, включая `windows: 40,6s of 52,0s (78% of his day)`;
  - реестр слотов `gascoop-slot-state.v1.json` прочитан перед выдачей: **все четыре AVAILABLE**.
    MCP `unity-win-u1` и `unity-win-u3` не отвечают (ConnectionRefused, редакторы закрыты) —
    поэтому слот выбран из живых, WIN-U2.

state_changes: |
  ОБНОВЛЕНО (3 блока карточек — исправление устаревших чисел, предмет не менялся):
  - `bet-g-5a7c-wave-10` блок `appetite` → его слово «берём четыре» записано, замах закрыт;
  - `bet-g-5a7c-wave-10` блок `goal` → пять замеров пересняты на `0e72d610`, три исправлены,
    добавлено число 78% и результат слияния;
  - `t-everything-lands-1` блок `done_when` → кусок один, а не четыре; разрешение конфликта
    измерено; четыре содержательных красных названы уликой.

  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-noise-reaches-by-distance-1-001` → слот **WIN-U2**, база — `main` = `0e72d610`
    со слитой `slot/win-u4`;
  - `work/2026-09-01-call-noise-reaches-by-distance-1.md`.

  СТАТУС:
  - `t-noise-reaches-by-distance-1` → `active` (была `open`).

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-walking-stops-deafening-1` остаётся `blocked` с тем же условием;
  - `t-a-fact-knows-who-made-it-1` остаётся `open`;
  - `t-everything-lands-1` остаётся `blocked`;
  - `bet-g-5a7c-wave-9` остаётся `active`: руки и утро ждут его глаза, не работы.

captures: |
  - **ЭКРАН, ПОКАЗАННЫЙ ВЛАДЕЛЬЦУ, УСТАРЕЛ ЗА ВРЕМЯ СВОЕЙ ЖЕ НАРЕЗКИ.** Три числа из пяти были
    неверны через час после того, как их сняли. **Правило-кандидат: числа про состояние веток и
    публикаций пересматриваются перед КАЖДОЙ выдачей наряда, а не наследуются из карточки — они
    гниют быстрее суток, иногда быстрее часа.** Это ровно та же болезнь, что уже записана
    про живые замеры в `done_when`, но здесь она ударила в блок `goal`.
  - **ПРОВЕРКА ЧУЖОГО ЧИСЛА ОКУПИЛАСЬ, ХОТЯ ЧИСЛО ОКАЗАЛОСЬ ВЕРНЫМ.** «После разрешения останется
    один красный» пришло из возврата исполнителя. Направление проделало слияние само — и получило
    не только подтверждение, но и **четыре имени, дословный отказ и число 78%**, которых в чужом
    возврате не было. **Перепроверка чужого числа стоит дёшево и приносит больше, чем проверяет.**
  - **ЧУЖОЙ КРАСНЫЙ, ПОСТАВЛЕННЫЙ ПРЕДЫДУЩЕЙ НОГОЙ КАК РАСТЯЖКА, ОБЯЗАН БЫТЬ В НАРЯДЕ.** Нога
    ходьбы написала в комментарии, что её утверждение должна снять волна слуха. Не прочитай нарезка
    этот комментарий — исполнитель упёрся бы в красный, которого не ждал, и встал.
    **Правило-кандидат: перед выдачей наряда искать в затрагиваемых тестах комментарии,
    адресованные будущей ноге.**

decisions_needed: |
  Ни одного. Замах закрыт его словом, наряд выдан.

play_check: |
  1. OPEN — его слово «берём четыре» на показанный целиком экран волны.
  2. SHAPE — все несущие числа пересняты перед выдачей, три исправлены; слияние проверено руками в
     выброшенной копии; чужая растяжка найдена и передана в наряд; слот выбран по прочитанному
     реестру и живому MCP, а не по памяти.
  3. (owner) — слово на замах получено и процитировано дословно.
  4. CLOSE — RESULT, применение, коммит; текст наряда уходит владельцу.

log: |
  замах десятой волны закрыт его словом «берём четыре» — и первым же делом три числа показанного
  экрана оказались протухшими за час: руки и дела уже уехали в main, куск остался один, а красных
  на ветке пять; слияние проделано направлением руками — конфликт один, сторона ходьбы пуста,
  после разрешения 744/743/1, и единственный красный оказался дословной жалобой владельца:
  78% своего дня хозяин глух, и каждый такой момент — ходьба

next: |
  Наряд `c-exec-g-5a7c-noise-reaches-by-distance-1-001` уходит владельцу для **WIN-U2**.

  Волна: голова выдана; `ходьба перестаёт глушить` ждёт ответа головы; `свой шум` открыт;
  публикация ждёт погасшего красного. Девятая волна ждёт его глаза.
