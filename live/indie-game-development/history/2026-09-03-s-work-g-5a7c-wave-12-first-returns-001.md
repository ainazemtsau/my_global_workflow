RESULT s-work-g-5a7c-wave-12-first-returns-001 (call: — владелец-инициированная, без родительского CALL)
direction: indie-game-development   track: —   play: work   node/task: bet-g-5a7c-wave-12 (три возврата первого дня взяты в карточки; выдан следующий наряд WIN-U2)

outcome: |
  **ТРИ ИЗ ДЕСЯТИ ЗАДАЧ ВОЛНЫ ДОСТАВЛЕНЫ В ДЕНЬ НАРЕЗКИ.** Продукт перемерен первой рукой:
  `main` = `dev` = `origin/main` = `origin/dev` = `2cea9a94`. Возвраты взяты в карточки в тот же
  день — правило раздела 10 плана, заведённое после того, как половина работы волны 11 оказалась
  без карточек до сверки.

  - **Стенд «хозяин»** — `663cb3a7`, закрыт `7922ecdb`. Шов, названный нарезкой, оказался верным:
    добавлен четвёртый вид куска (`Probe/Bench/IntegrationLabPiece.cs` +24), расстановок стало три —
    `IntegrationLab.householder-knocked-off-balance / -near-miss / -touch-too-slow`;
    `IntegrationLabRig` +319, `IntegrationLabReadout` +180, `NetworkHouseholder` +64.
  - **Конец утра** — `35a17831`, интегрирован `82d0c636`, закрыт `198ab3fe`. 640 строк новых тестов
    `HouseholderMorningEndsAtTheDoorTests`.
  - **Таблица строк** — `521b822c … 2cea9a94`. Сборка `Words`, отказ на неизвестном языке снят
    после слова владельца.
  - **Магнит** — в работе, `slot/win-u1` на `96a05f14`, четыре коммита вне `main`.

  **ГЛАВНОЕ, РАДИ ЧЕГО ЭТА НОГА СУЩЕСТВУЕТ: СТРОКА ПРОГНОЗА ВОЛНЫ БЫЛА НАПИСАНА СЛИШКОМ ТЕСНО, И
  REVIEW 11.09 ПРОЧИТАЛ БЫ ЕЁ НЕВЕРНО.** Нарезка написала: «если задача конца утра тронет
  `Network/CargoBody.cs` или что-либо в `Network/**` — допущение о глаголе было неверным». Задача
  тронула `Network/NetworkWalkerCourier.cs` **+15 строк**. По букве прогноза это сработавший порог 1.
  По байтам — нет: пятнадцать строк лежат внутри `AppendWhatHeChose`, то есть в отчёте «почему»,
  и коммит говорит прямо: «Nobody is told a round is running; there is no round owner, no shell
  scene and no second source of truth about one». **Порог 1 не сработал; неверна была строка
  прогноза — она назвала папку там, где должна была назвать механизм, и при этом противоречила
  собственному `done_when` задачи, который сам требовал сигнала наружу.** Это тот же класс ошибки,
  который правило `a-forecast-line-is-a-cost-claim-and-must-be-refutable` вывело из волны 11, и он
  повторился в первой же волне после правила.

  **ВТОРОЙ ФАКТ, КОТОРЫЙ МЕНЯЕТ СЛЕДУЮЩИЙ НАРЯД.** Конец раунда выходит наружу **защёлкнутым
  фактом, а не событием**: «his morning is over, at T+ by his own clock, and which state that was»,
  и это **чтение**, за которым никто не обязан следить. Значит `t-the-round-shows-what-it-cost-1`
  строит экран, который **читает** этот факт, а не подписывается на сигнал. Ни в ставке, ни в
  карточке задачи этого нет — записано здесь и в журнале задачи.

  Утро удлинено данными, а не кодом: `deadlineSeconds` 120 → **360**, `comfortableBufferSeconds`
  30 → **90**, хвост распорядка из пяти `walk-route` по 12 с заменён тремя по 110 с плюс
  терминальная строка `leave-for-work` с местом `leave`. Шесть минут — внутри требуемых планом 5–8.
  **Строк содержания не заведено ни одной** — порог 6 ставки выдержан.

  **НИ ОДНА ИЗ ТРЁХ ЗАДАЧ НЕ ЗАКРЫТА `done`.** У всех трёх приёмка — живая проба владельца, и ни
  одна не снята: прогон её не заменяет (правило волны 11, раздел 10 плана). Статус `waiting` с
  названным условием разблокировки — та же форма, которой волна 11 держала равновесие.

evidence: |
  - Продукт перемерен 2026-09-03 после возвратов: `main` = `dev` = `origin/main` = `origin/dev` =
    `2cea9a94`; `slot/win-u2` = `2cea9a94`, `slot/win-u3` = `198ab3fe`, `slot/win-u4` = `90fbc3ea`,
    `slot/win-u1` = `96a05f14` (четыре коммита вне `main` — магнит в работе).
  - Тринадцать коммитов между `2d2c27ff` и `2cea9a94`, из них головы механик: `663cb3a7`
    (стенд), `35a17831` (конец утра), `521b822c` (таблица строк).
  - `git show 35a17831 -- Assets/TunnelCrew/Network/NetworkWalkerCourier.cs` — +15 строк, все внутри
    `AppendWhatHeChose`, с комментарием «It is a READING and not a signal somebody has to be
    watching for».
  - `git show 35a17831 -- Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json` —
    `deadlineSeconds: 120 → 360`, `comfortableBufferSeconds: 30 → 90`, пять `walk-route` по 12 с →
    три по 110 с плюс `leave-for-work` с `place: leave`. Ни одной строки содержания.
  - `git show --stat 663cb3a7` — `Probe/Bench/IntegrationLabPiece.cs` +24 (четвёртый вид куска),
    три новых `IntegrationLab.householder-*.json`, `IntegrationLabRig` +319, `IntegrationLabReadout`
    +180, `LabArrangementFilesTests` +119.
  - Числа тестов приходят из возвратов исполнителей и этой ногой НЕ перепрогонялись: headless
    `863/863/0`, EditMode `245/245/0`, hygiene OK. Названо явно, потому что число выросло с 858 до
    863 из-за встречной ноги, и это факт чужого замера, а не мой.

state_changes: |
  1. SET `t-the-bench-shows-why-he-fell-1` → `status: waiting`, `unblock_when: владелец посмотрел
     стенд своим глазом на двух экранах`.
  2. SET `t-the-morning-ends-at-the-door-1` → `status: waiting`, `unblock_when: живая проба
     владельцем на удлинённом утре`.
  3. SET `t-every-word-comes-from-a-key-1` → `status: waiting`, `unblock_when: живая проба
     владельцем — переключение языка меняет надписи`.
  4. SET `status: done` на трёх карточках нарядов: `c-exec-g-5a7c-the-bench-shows-why-he-fell-001`,
     `c-exec-g-5a7c-the-morning-ends-at-the-door-001`, `c-exec-g-5a7c-every-word-comes-from-a-key-001`.
     Наряд исполнен — это не то же самое, что задача принята.
  5. ADD `work/2026-09-03-call-four-screens-see-one-fall.md` и карточку
     `c-exec-g-5a7c-four-screens-see-one-fall-001` (`to: executor`, `status: ready`, слот WIN-U2,
     `basis: 2cea9a94`).
  6. Журнальные строки на ставку и затронутые карточки; поправка к строке прогноза записана в
     журнал ставки, а не правкой блока `forecast` — блок остаётся тем, что был подписан, чтобы
     review 11.09 сверял его построчно и видел поправку рядом.
  7. `NOW.md` не менялся.

captures:
  - Строка прогноза, которая называет папку вместо механизма, ловит свою же задачу за исполнение
    её `done_when`. Кандидат в правило знания; одного случая мало — правило
    `a-forecast-line-is-a-cost-claim-and-must-be-refutable` уже существует, и это второй случай его
    класса, но с другой стороны: не «неопровержимая строка», а «строка, опровергающая верную работу».
    Решает review 11.09, а не эта нога.
  - Конец раунда — защёлкнутое чтение, не событие. Влияет на форму экрана результата и, позже, на
    телеметрию раунда.
  - Три задачи из десяти доставлены за день нарезки. Внешний взгляд ставки говорил, что волны с
    планом больше семи задач целиком не доходили; первый день говорит обратное. Один день — не
    распределение; сверять на 11.09.

decisions_needed:
  - q: Приёмка трёх доставленных задач — его глаз. Стенд «хозяин» ждёт взгляда на двух экранах,
      удлинённое утро — прохода от будильника до двери, язык — переключения.
    options: [посмотреть сегодня; посмотреть на сверке 11.09]
    recommendation: стенд первым — он же поверхность для шлепка и для четырёх экранов.

play_check:
  - 1 recite: done — работа названа его словами: «u2 ответил», возврат передан целиком; направление
    берёт возвраты в карточки и выдаёт следующий наряд.
  - 2 owner inputs (owner): done — его слово о запуске четырёх ног и переданный возврат WIN-U2.
    Ни одно решение о содержании игры не принято; вопрос исполнителя про строку в консоли —
    его, не мой, и в карточки не переносился.
  - 3 do the work: done — продукт перемерен первой рукой, три возврата взяты в карточки, следующий
    наряд написан и выдан.
  - 4 self-check: done — ни одна задача не помечена `done`: приёмка глазом не снята ни у одной,
    и прогон её не заменяет. Числа тестов названы как чужой замер, а не как мой.
  - 5 close: done — `close: light — because` каждая строка выведена первой рукой из коммитов
    `2d2c27ff..2cea9a94` продуктового репозитория и из байтов профиля и курьера; ни одно
    утверждение о поведении игры не сделано без файла и строки. G5-чат не открывается.

log: три из десяти задач волны 12 доставлены в день нарезки и взяты в карточки — стенд 663cb3a7, конец утра 35a17831, таблица строк 2cea9a94; ни одна не закрыта, у всех приёмка глазом не снята; главное — строка прогноза волны была написана слишком тесно и поймала верную работу: конец утра тронул NetworkWalkerCourier на 15 строк отчёта «почему», а не завёл управляющего раундом, и коммит говорит это дословно, так что порог 1 не сработал; конец раунда выходит наружу защёлкнутым чтением, а не событием, и экран результата обязан его читать; утро удлинено данными до 360 секунд без единой строки содержания; следующий наряд WIN-U2 выдан на базе 2cea9a94

next: |
  `c-exec-g-5a7c-four-screens-see-one-fall-001` готов к запуску в WIN-U2 на базе `2cea9a94`.
  Свободных полос нет: U1 в работе (магнит), U3 и U4 ждут следующих задач своих полос —
  `t-the-round-shows-what-it-cost-1` (U3) и `t-he-slaps-the-mouse-he-sees-1` (U4); их наряды
  пишутся отдельными ногами, когда владелец их запросит.
  Приёмка трёх доставленных задач — за владельцем; стенд первым, он же поверхность для двух
  других задач волны.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-work-g-5a7c-wave-12-first-returns-001.md
