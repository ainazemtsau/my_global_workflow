# RESULT — s-work-g-5a7c-body-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-body-1
track: body
date: 2026-08-12
outcome_kind: checkpoint — наряд выдан, задача НЕ закрыта

owner_approved: |
  «напиши наряд на t-body-1 и выдавай в WIN-U1» — 2026-08-12.

  **FRICTION, названная вслух второй раз за день.** Это ТРЕТЬЯ нога в одном физическом чате, и
  часом ранее эта же сессия сама сказала владельцу «третьей тут быть не должно». Основание для
  продолжения — порядок власти KERNEL («live owner instruction > this kernel») и проведённое вслух
  различие: свежий чат жёстко нужен для РАЗБОРА ВОЗВРАТА, где надо пытаться опровергнуть, а не для
  написания наряда. Названный риск — притащить допущения прошлого наряда вместо замеров — снят
  тем, что дорожка тела измерена с нуля, и замеры сразу нашли две ошибки в собственной подписанной
  ставке (ниже). Возврат по обоим нарядам разбирается в отдельных свежих чатах, без исключений.

outcome: |
  Наряд `c-exec-g-5a7c-body-1-001` на `t-body-1` написан и выдан в слот **WIN-U1** от опубликованной
  головы `f3c40c40`. Задача переведена в `active`. Дорожка `body` пошла параллельно шву, как и
  обещано ставкой.

  **ВЫДАЧА НАШЛА ДВЕ ОШИБКИ В СОБСТВЕННОЙ ПОДПИСАННОЙ СТАВКЕ, И ОБЕ ПЕРЕПИСАЛИ НАРЯД ДО ОТПРАВКИ.**

  1. **«ВХОД УЖЕ ОПЛАЧЕН» — НЕВЕРНО ПРО ПЕРСОНАЖЕЙ.** Ветка `probe/free-kits-2026-08-06` открыта
     СОДЕРЖИМЫМ, а не размером: двадцать один содержательный файл, и все — обстановка (лампа
     KayKit, стул и стол Kenney, книжный шкаф и растение Quaternius, ламинат PolyHaven). Ригнутых
     тел, скелетов и анимаций там нет вовсе. Нога `shape` измерила «50 файлов, +1761, ноль
     удалений» и не открыла папку — тот же класс ошибки, что и Dungeon Architect часом ранее:
     утверждение принято по метрике вместо содержания. Источник у ошибки не только мой: критерий 10
     узла ставит фразу про ригнутых персонажей и фразу «вход уже оплачен веткой» рядом, и они
     читаются как одно. Заведена `i-free-kits-probe-holds-furniture-not-characters-001`; карточка
     `t-body-1` поправлена здесь же; текст узла правит `map`, текст ставки — `review`, и нога `work`
     ни того ни другого не трогает.
     **Что в ветке правда оплачено и что наряд переиспользует — ОБРАЗЕЦ АВТОРИНГА:**
     `Sources/<вендор>/` с `License.txt` и `SOURCE.md` рядом с исходником плюс собственная заготовка
     в `Prefabs/`. Сама мебель — вход задачи `t-rooms-1`, и наряд запрещает её трогать.

  2. **«ПЕРЕСЕЧЕНИЕ ДОРОЖКИ `body` С ОБЕИМИ ДРУГИМИ — НОЛЬ» ВЕРНО ТОЛЬКО ПРИ НОВОЙ ГРАНИЦЕ.**
     `NetworkWalkerPresentation` висит компонентом на объектах ОБЕИХ сцен —
     `IntegratedHouse.unity:1285` и `HouseholdLab.unity:1285`. Значит привычный путь «сериализованное
     поле префаба, назначенное в сцене» потребовал бы править обе сцены, а они принадлежат чужим
     дорожкам и вдобавок копируются в тестовый проект фикстурами. Наряд поэтому запрещает открывать
     сцены вообще и оставляет механизм шва исполнителю, дав ему один факт: у презентации уже есть
     сериализованная ссылка на настройки. Если шов недостижим без правки сцены — СТОП домой.
     Тело хозяина этой проблемы не имеет: `World/Householder.prefab` меняется внутри себя, а
     экземпляры в сценах наследуют.

  **ТРЕТЬЯ НАХОДКА, не ошибка, но она меняет размер задачи:** анимационной обвязки в проекте нет ни
  грамма — ноль упоминаний `Animator`, `SkinnedMeshRenderer` и `Avatar` во всём
  `Assets/TunnelCrew/**`. Строка «работают стойка, ходьба и поворот» строится с нуля, а не
  настраивается поверх существующего.

  **АСИММЕТРИЯ ТЕЛ, названная в наряде таблицей:** у хозяина шов ЕСТЬ (префаб со встроенной
  капсулой внутри), у налётчика шва НЕТ ВОВСЕ — тело собирается кодом через `CreatePrimitive`.

  **ГРАНИЦА, КОТОРУЮ Я ПОСТАВИЛА ЖЁСТЧЕ КАРТОЧКИ, и основание.** Владелец выбирает облик
  параллельно в Concept Lab и прямо сказал, что не выбрал. Поэтому наряд объявляет любую
  поставленную модель ЗАГЛУШКОЙ и прямым текстом запрещает исполнителю выбирать облик, доводить
  внешний вид и тратить на это время. Это исполнение G7, а не осторожность: облик — его решение.

evidence: |
  **ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ ПЕРВОЙ РУКОЙ 2026-08-12 на `f3c40c40`.**

  Содержимое оплаченной ветки:
  - `git diff --name-only origin/main...origin/probe/free-kits-2026-08-06` → 21 содержательный файл,
    поимённо перечислены в наряде; два коммита `f980dce9` и `e9f23cef`.

  Как нарисованы тела:
  - `Network/NetworkWalkerPresentation.cs:153,166,196,201` — `GameObject.CreatePrimitive` с
    `PrimitiveType.Capsule` и `PrimitiveType.Cube`;
  - `Network/NetworkCargoPresentation.cs:140` и `Core/Cargo/CargoBody.cs:80` — `CreatePrimitive(Cube)`;
  - `World/Householder.prefab` — дочерний `Householder Body`, `MeshFilter` → встроенный меш
    `fileID: 10208`.

  Анимационная обвязка:
  - поиск `Animator|SkinnedMeshRenderer|Avatar` по всем `.cs` в `Assets/TunnelCrew` → **0 совпадений**;
  - `Packages/manifest.json` — из анимационного только базовый модуль и мост
    `com.ivanmurzak.unity.mcp.animation`.

  Коллизия по сценам:
  - guid `NetworkWalkerPresentation.cs` = `494bc80483aa7e143beafdcf468c4fdd`, встречается в
    `IntegratedHouse.unity:1285` и `HouseholdLab.unity:1285`;
  - guid `Householder.prefab` встречается в обеих сценах тринадцать и тринадцать раз (экземпляры);
  - обе сцены копируются фикстурами в `tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj`.

  Слоты и мост:
  - селектор: все четыре `CLEAN / AVAILABLE / lease none`; WIN-U1 `head a5a84350`,
    `origin/slot/win-u1` `43648b2d` — отставание названо в наряде первым действием;
  - мост WIN-U1 отвечает: Unity `6000.5.5f1`, не в Play, не компилирует; и он доказанно смотрит в
    ДРУГОЕ дерево, чем WIN-U2 (`HouseholdLab` находится только у второго).

  База прогонов:
  - `dotnet test ... -c Release` → **214 passed, 0 failed, 0 skipped**, прогнано этой ногой.

  **G5:** нога ничего не закрывает — она выдаёт наряд. Биндинговая проверка не требуется и не
  выдаётся за существующую. Закрытие `t-body-1` светлым путём недоступно и названо заранее: её судит
  глаз владельца.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-body-1-001-call.md` — полный текст наряда.
  2. Создана `cards/c-exec-g-5a7c-body-1-001.md` — `status: ready`, `slot: WIN-U1`,
     `basis: f3c40c40`, `engineering_contract: 36`.
  3. `t-body-1`: `open → active`; блок `## note` исправлен — неверное «вход уже оплачен» заменено
     измеренным фактом про мебель, добавлены состояние тел, отсутствие анимационной обвязки и
     найденная коллизия по сценам; добавлен блок `## журнал`.
  4. Создана `cards/i-free-kits-probe-holds-furniture-not-characters-001.md` — `route: review`,
     триггер на review третьей волны: поправить критерий 10 узла и текст ставки.
  5. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-body-1-dispatch-001.md`.

  **Текст ставки `bet-g-5a7c-wave-3` и текст узла `g-5a7c` НЕ редактированы** — `Writes:` плея
  `work` их не покрывает. Оба неверных места названы в улике с маршрутом.

captures:
  - |
    ТОТ ЖЕ КЛАСС ОШИБКИ ДВАЖДЫ ЗА ОДИН ДЕНЬ: утверждение о содержимом принято по МЕТРИКЕ вместо
    содержания. Утром — Dungeon Architect, вписанный по старому решению без слова владельца;
    сейчас — «вход оплачен», выведенное из «50 файлов, +1761, ноль удалений» без открытия папки.
    Если поймается третий раз, это MAINTENANCE, а не ошибка ноги.
  - |
    Строка «работают стойка, ходьба и поворот» в критерии 10 узла написана так, будто анимационная
    обвязка существует. Её нет ни грамма. Это не блокер задачи, но размер строки в критерии узла
    больше, чем он выглядит, и это стоит помнить на review.
  - |
    Мебель из `probe/free-kits-2026-08-06` — готовый вход в `t-rooms-1`: пять предметов обстановки с
    лицензиями и образцом авторинга. Задача, которой нужна мебель как препятствие для взгляда и как
    источник шума, начинается не с нуля.

decisions_needed: []

play_check:
  - 1 recite — done. Цель и семь строк `done_when` карточки `t-body-1` пересказаны; обязательство ставки названо: дорожка `body` идёт параллельно шву и умирает первой при перегреве.
  - 2 owner inputs — не требуется, и это обосновано, а не пропущено: единственный вопрос, который принадлежит только владельцу, — облик, и он ЗАКРЫТ его же словом «какую-то дефолтную модель… чтобы потом поменять на нашу новую». Наряд превращает это в границу «модель — заглушка, облик не выбираешь», вместо того чтобы спрашивать заново.
  - 3 do the work — done. `call:executor` выпущен с goal, context, boundaries, done_when, return и budget; направление задало исход и планку, техническое КАК оставлено исполнителю — в том числе механизм шва, который наряд сознательно НЕ предписывает, а только ограничивает запретом на сцены.
  - 4 self-check — done. Три строки контракта покрывают все семь строк карточки, соответствие расписано; бюджет контракта 36 соблюдён без овербюджетного токена.
  - 5 close — checkpoint. Задача НЕ закрыта: наряд выдан, возврата нет.
  - G2 — done. Задача служит активной ставке; найденная мебель отнесена к `t-rooms-1`, а не втянута сюда.
  - G5 — не применяется: нога ничего не закрывает. Недоступность светлого закрытия для этой задачи названа заранее.
  - G7 — done. Облик не выбран ногой и прямо запрещён исполнителю; покупка ассета маршрутизирована в ESCALATE.
  - G10 — done. Шаги видны, RESULT финальный.

log: наряд на тело выдан в WIN-U1 от опубликованной головы f3c40c40, и выдача нашла две ошибки в собственной подписанной ставке — «вход уже оплачен» оказалось неверным про персонажей, потому что ветка probe/free-kits это МЕБЕЛЬ (лампа, стул, стол, шкаф, растение, ламинат) и ни одного ригнутого тела, а нога shape измерила размер ветки вместо содержимого, тот же класс ошибки, что и Dungeon Architect часом ранее; и «пересечение дорожки body с обеими другими — ноль» верно только при новой границе, потому что NetworkWalkerPresentation висит на объектах ОБЕИХ сцен, а они принадлежат чужим дорожкам и копируются фикстурами в тестовый проект, поэтому наряд запрещает открывать сцены вообще и оставляет механизм шва исполнителю со СТОПом, если иначе нельзя; сверх этого измерено, что анимационной обвязки в проекте нет ни грамма (ноль Animator, SkinnedMeshRenderer и Avatar), что у хозяина шов есть префабом, а у налётчика нет вовсе, потому что тело строится кодом примитивами, и что оплачен на самом деле ОБРАЗЕЦ АВТОРИНГА с лицензией рядом с исходником, который наряд и переиспользует; облик объявлен заглушкой и его выбор исполнителю прямо запрещён, покупка ассета уходит в ESCALATE

next: |
  Ждём возврата исполнителя по `c-exec-g-5a7c-body-1-001`. Первая строка возврата — расписка с
  числом полученных строк `done_when` (три) и базисом.

  **Оба возврата — по шву в WIN-U2 и по телу в WIN-U1 — разбираются в ОТДЕЛЬНЫХ свежих чатах.**
  Этот чат исчерпан: три ноги в нём уже есть.

  Из трёх задач ставки, готовых к запуску после шва, наряды пока не написаны ни на одну.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-body-1-dispatch-001.md
