RESULT s-work-g-5a7c-measured-not-acted-001 (call: — владелец-инициированная, без родительского CALL)
direction: indie-game-development   track: —   play: work   node/task: g-5a7c

outcome: |
  **ЧЕТЫРЕ ПРОБЛЕМЫ, КОТОРЫЕ ВЛАДЕЛЕЦ НАЗВАЛ 03.09 КАК НОВЫЕ, УЖЕ ЛЕЖАЛИ ИЗМЕРЕННЫМИ И
  ЗАПИСАННЫМИ. ЗАМЕР РАБОТАЕТ; НЕ РАБОТАЕТ ПУТЬ ОТ ЗАПИСИ ДО РАБОТЫ.**

  Повод: разбор одной проблемы занял 92 агента, и владелец спросил, почему. Его слова: «у нас уже
  стал жёсткий говнокод… надо ещё рефакторить и что-то делать с тестами… они не тестируют то, что
  надо, они долго гоняются… я не понимаю, почему ты мне не сказал про этот реворк… с таким темпом
  ты до одиннадцатого не дойдёшь».

  **ЧЕТЫРЕ ЕГО ПУНКТА — ЧЕТЫРЕ ЛЕЖАЩИЕ КАРТОЧКИ:** переработка ядра
  (`i-householder-core-needs-structural-rework-001`, `route: shape`, открыта с 14.08, перемерена
  30.08); стоимость прогона (`i-unity-test-runs-cost-five-minutes-for-one-second-of-work-001`,
  01.09, Unity 316 с против headless 1 с, причина — 37 циклов `ForceDomainReload` за batch); тесты
  на расходном содержимом (`i-mechanism-tests-stand-on-disposable-shipped-content-001`, 31.08);
  и кто разрешил переработку ядра (`q-core-rework-authorization...`, `asks: владелец`, 17.08).

  **ПРИЗНАНО ПРЯМО: НАРЕЗКА ВОЛНЫ 12 ПРОШЛА МИМО ПЕРЕРАБОТКИ ЯДРА.** Улика лежала в списке
  маршрута `shape`, который нарезка читала; она взяла восемь улик, названных в запросе волны, и эту
  не взяла. Это не пробел механизма доставки — механизм положил её в список. Это решение ноги,
  и оно было неверным.

  **ЗАМЕР МАСШТАБА, СНЯТЫЙ ЭТОЙ НОГОЙ РУКАМИ (без агентов) на `slot/win-u4`:**
  наш код 198 файлов / 59 901 строка; тесты 137 файлов / 52 079 строк; тестов 959
  (747 headless + 212 Unity) — ≈54 строки на тест. Пять файлов о реакциях хозяина — 5 576 строк.
  Крупнейший файл продукта `Profiles/HouseholderProfileLoader.cs` — 3 829 строк на чтение
  JSON-профиля. Времена прогонов, встречающиеся в отчётах: 4, 5, 6, 7, 10, 11 и 35 минут.

  **ГЛАВНОЕ ЧИСЛО: 959 ТЕСТОВ НЕ ПОЙМАЛИ, ЧТО ТЕЛО УХОДИТ В ПОЛ И НЕ ВСТАЁТ.** Теста, проверяющего
  высоту тела после падения, в проекте нет ни одного.

  **ПРО 92 АГЕНТА — ЭТО БЫЛА НЕ СЛОЖНОСТЬ КОДА, А ПЕРЕСТРАХОВКА НОГИ.** Разбор устроен как 5
  исследователей и по три скептика на каждую выдвинутую версию; исследователи вернули по 5–7
  версий, отсюда ~87 скептиков. Множитель выбран направлением, а не продиктован задачей.

evidence: |
  - Его слова в этой сессии, процитированы в улике дословно.
  - Замеры сняты этой ногой в `C:\projects\Unity\GasCoopGame_win-u4` на `slot/win-u4` = `8182c984`:
    `find … -name "*.cs" | wc -l` и `wc -l` по продукту и тестам; `grep -c` по `[Test]`, `[Fact]`,
    `[Theory]`, `[UnityTest]`.
  - Четыре карточки прочитаны первой рукой в `live/indie-game-development/cards/`.
  - Отсутствие теста на высоту тела — из разбора 03.09, перепроверено чтением
    `Assets/Tests/TunnelCrew/Networking/HouseholderBalancePhysicsTests.cs` (единственные `position` —
    постановка груза перед броском, строка 302).
  - Состав разбора 92 агентов — из его собственной структуры: 5 углов × (1 исследователь + версии×3).

state_changes: |
  1. ADD улику `i-what-was-measured-never-reached-the-work-001` (`level: direction`,
     `route: review`) — четыре карточки против четырёх его пунктов, замер масштаба, признание
     промаха нарезки волны 12.
  2. Журнальная строка на узел `g-5a7c`.
  3. Ничего не решено про волну 13: содержание следующей волны — решение владельца о сроках и
     объёме, и устав оставляет его ему. Вопрос вынесен ему в чат с ценой каждого варианта.

captures:
  - Механизм «улика с маршрутом всплывает при нарезке» работает и его недостаточно: он кладёт улику
    в список из восемнадцати, а нарезка берёт те, что названы в запросе волны. Кандидат в правило
    или в MAINTENANCE — но одного случая мало, и решает review.
  - Множитель на опровержение (три скептика на версию) даёт квадратичный рост числа агентов, когда
    исследователь возвращает много версий. Ограничивать надо число версий, а не число скептиков.

decisions_needed:
  - q: Содержание волны 13 (18.09) — рубеж Р2 по плану, или переработка ядра и тестов.
    options: [Р2 как в плане; волна про тесты «которые ловят»; полная переработка ядра]
    recommendation: волна про тесты — мутационный проход по ядру, удаление тех, что никогда не
      падают, расщепление медленных Unity-прогонов. Переработка ядра решается после неё, с
      результатами на руках, и после ответа на вопрос от 17.08.

play_check:
  - 1 recite: работа названа его словами — почему так много агентов, почему не сказал про реворк,
    что делать с тестами.
  - 2 owner inputs (owner): его слова процитированы дословно; решение о содержании волны 13 за него
    не принято и вынесено в `decisions_needed` с ценой каждого варианта.
  - 3 do the work: замеры сняты руками без агентов — прямой ответ на его претензию к темпу;
    четыре карточки найдены и сверены; улика заведена.
  - 4 self-check: промах нарезки волны 12 назван прямо и не смягчён; состав 92 агентов разобран как
    выбор направления, а не как свойство кода.
  - 5 close: `close: light — because` каждая строка выведена из его слов, из четырёх прочитанных
    карточек и из замеров, снятых этой ногой в продуктовом чекауте. G5-чат не открывается.

log: четыре проблемы, названные им как новые, уже лежали измеренными — переработка ядра открыта с 14.08 с маршрутом на нарезку, стоимость прогона измерена 01.09 числом 316 с против 1 с, тесты на расходном содержимом с 31.08, вопрос об авторизации переработки с 17.08; нарезка волны 12 прочитала список маршрута shape и прошла мимо переработки ядра — это признано прямо; замер масштаба руками без агентов: 198 файлов и 59901 строка кода против 137 файлов и 52079 строк тестов, 959 тестов, 3829 строк в загрузчике JSON-профиля, и 959 тестов не поймали, что тело уходит в пол; 92 агента прошлого разбора — перестраховка ноги, а не сложность кода

next: |
  Ждёт его решения содержание волны 13. До него ничего не режется и не переписывается.
  Идущий разбор «почему падение выглядит плохо» — последний такого размера; множитель на
  опровержение ограничивается числом версий, а не числом скептиков.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-work-g-5a7c-measured-not-acted-001.md
