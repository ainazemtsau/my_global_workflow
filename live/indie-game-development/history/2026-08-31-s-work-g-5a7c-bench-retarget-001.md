# RESULT — s-work-g-5a7c-bench-retarget-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-bench-by-fact-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: work — карта полигона перенацелена на ветку, где хозяин ходит; прежний наряд снят как смотревший не в то дерево

owner_approved: |
  Его слово 2026-08-31, дословно: **«давай карту полигона на эту ветку»**.

  Ни одна строка `done_when` не изменена.

outcome: |
  **НАРЯД ПЕРЕПИСАН ЦЕЛИКОМ, А НЕ ДОПОЛНЕН, И ЭТО НАМЕРЕННО.** Прежний
  (`c-exec-g-5a7c-bench-by-fact-1-001`) стоял на `main` и описывал бы **стоящего хозяина** — то
  есть состояние, куда владелец не придёт. Дописывать к нему поправку значило бы оставить
  исполнителю два противоречащих текста. Снят как `dropped` с причиной; текст ему не выдавался ни
  разу, вреда не нанёс.

  **СЛОТ ВЫБРАН ЗАМЕРОМ, А НЕ ПО ПРИВЫЧКЕ.** Реестр прочитан до выдачи: **все четыре слота
  `AVAILABLE`**, и `win-u4` **уже стоит на `slot/win-u4` = `bd76a42c`** и чист. То есть ветку
  переставлять не нужно вовсе, а селектор её и не переставляет.

  **КАРТА ПОЛУЧИЛА ЧЕТВЁРТУЮ ЧАСТЬ, КОТОРОЙ НЕ БЫЛО В ПЕРВОЙ РЕДАКЦИИ: «ЧТО ОН МОЖЕТ РУКАМИ».**
  Он прямо сказал, что мышью бегать не собирается и хочет через полигон шуметь и двигать вещи.
  Команда шума существует и компилируется в игру; **есть ли команды «убрать» и «переместить» —
  неизвестно**, и наряд велит выяснить руками и честно назвать, чего нельзя.

  **И ЧАСТЬ «ЧЕГО ЕЩЁ НЕТ» ЗАЩИЩАЕТ ОТ ЛОЖНОГО ВЫВОДА.** На пяти из шести точек хозяин просто
  стоит — занятий там нет. **Это ровно то, что он просил** («пусть постоял, не занимается»), и
  карта обязана сказать это прямо, иначе он примет исполнение своей же просьбы за недоделку.

  **ПЯТЬ ИЗВЕСТНЫХ ВРАНИЙ ПЕРЕЧИСЛЕНЫ ПОИМЁННО**, чтобы первый живой взгляд не ушёл в починку не
  того: врущая короткая строка консоли, не доходящий тихий шум, баннер отменённой игры, `who` не
  отличает дело от распорядка, пять красных тестов.

evidence: |
  Прочитано направлением напрямую перед выдачей:

  - `gascoop-slot-state.v1.json` — **все четыре слота `AVAILABLE`, `lease: none`**;
  - ветки чекаутов: `win-u4` → `slot/win-u4` = `bd76a42c`, `dirty=0`; остальные три на своих
    слотовых ветках и тоже чисты;
  - живой круг из возврата ноги сценария: шесть мест, времена, повтор;
  - тесты на этой ветке прогнаны направлением лично: **744 всего, 739 зелёных, 5 красных**.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-bench-by-fact-1-002` → слот **WIN-U4**, база **`bd76a42c`**;
  - `work/2026-08-31-call-bench-on-the-walking-branch-1.md`.

  СНЯТО (1 наряд):
  - `c-exec-g-5a7c-bench-by-fact-1-001` → `dropped`, superseded. Смотрел в `main`.

  АКТИВИРОВАНО (1):
  - `t-bench-by-fact-1` → `active`.

captures: |
  - **ЗАНЯТОСТЬ СЛОТОВ ПРОЧИТАНА ДО ВЫДАЧИ, А НЕ ПОСЛЕ.** Полчаса назад направление четырежды
    доложило «идёт в U1» на основании статуса карточки. Теперь порядок обратный: сначала реестр,
    потом слово. **Проверка стоит одной команды и сегодня уже дважды дала ответ, которого не было
    в голове.**
  - **ПОПРАВКА К НАРЯДУ ХУЖЕ НОВОГО НАРЯДА, КОГДА МЕНЯЕТСЯ БАЗА.** Сегодня к одному наряду легло
    четыре дополнения подряд, и последнее отменяло смысл первого. Здесь база сменилась целиком —
    дописывать было нельзя.

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — его прямое слово «давай карту полигона на эту ветку».
  2. WORK — реестр слотов и ветки чекаутов прочитаны до выдачи; известные врания и пробелы собраны
     в наряд заранее; прежний наряд снят, а не дополнен.
  3. (owner) — его слово получено и процитировано.
  4. CLOSE — RESULT, применение, коммит; текст уходит владельцу для пересылки в WIN-U4.

log: |
  карта полигона перенацелена на ветку со сценарием: прежний наряд смотрел в main и описал бы
  стоящего хозяина, снят как superseded; новый стоит на bd76a42c в WIN-U4, чей чекаут уже там,
  и получил четвёртую часть — что владелец может сделать руками без беготни мышью

next: |
  Текст уходит владельцу. Свободны WIN-U1, WIN-U2, WIN-U3.

  **После этого возврата он садится смотреть.**
