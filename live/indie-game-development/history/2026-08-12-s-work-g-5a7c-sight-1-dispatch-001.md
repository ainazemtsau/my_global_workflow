# RESULT — s-work-g-5a7c-sight-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-sight-1
track: householder-life
date: 2026-08-12
outcome_kind: checkpoint — наряд выдан, задача НЕ закрыта

owner_approved: |
  «пиши наряд на зрение» — 2026-08-12.

outcome: |
  Наряд `c-exec-g-5a7c-sight-1-001` написан и выдан в **WIN-U3** от опубликованной головы
  `4fc04b4c`. Задача `open → active`. Это **самое рискованное допущение волны**, и наряд говорит
  исполнителю прямо: отрицательный исход — нормальный результат пробы, а не провал.

  **ВЫДАЧА НАШЛА ТРИ ВЕЩИ, И ОДНА ИЗ НИХ МЕНЯЕТ ФОРМУ ЗАДАЧИ.**

  1. **У ХОЗЯИНА НЕТ НАПРАВЛЕНИЯ ВЗГЛЯДА НИГДЕ В ПРАВИЛАХ.** Поиск `Facing|Heading|facing` по
     `Core/Householder/**` и `Network/HouseholderSnapshot.cs` даёт **ноль**. Направление существует
     только в презентации: закрытая сегодня `t-body-1` выводит его из ненулевого полученного
     перемещения, чтобы тело не ехало боком.
     **Следствие, вписанное в наряд границей:** «прямой взгляд» из подписанного критерия 6 требует
     направления, и оно обязано быть АВТОРИТЕТНЫМ значением. Читать его с презентации запрещено —
     иначе авторитет начнёт зависеть от картинки, а `t-body-1` только что специально это исключила
     («тело — презентация, не авторитет»). Без этой границы исполнитель почти наверняка взял бы
     готовый поворот из презентации, потому что он там уже есть и работает.
  2. **ХОЗЯИН НЕ ЗНАЕТ ПРО ИГРОКОВ ВООБЩЕ** — ноль упоминаний `Walker|player` в его авторитетном
     ядре. Весь его мир это ситуации, значит игроки попадают к нему только новым источником.
  3. **ДВИЖКОВЫХ ЛУЧЕЙ У СУДЬИ НЕТ:** в `Network/**` только `Physics.Simulate` и `simulationMode`,
     оба в `CargoBody.cs`, который занят параллельным нарядом на лут. Поэтому наряд требует ОТДЕЛЬНЫЙ
     НОВЫЙ ФАЙЛ под производителя зрения.

  **ПРОБА ВПЕРВЫЕ ИМЕЕТ СМЫСЛ, И ЭТО ПРИЕХАЛО СЕГОДНЯ.** Опубликованный дом несёт **25 объектов с
  `HouseWall`** — восемь стен, перегородки проёмов и четырнадцать предметов обстановки. Утром
  прятаться было не за чем.

  **ЛОВУШКА НАЗВАНА ДО ПОСТРОЙКИ В ТРЕТИЙ РАЗ, И ТЕПЕРЬ С ПРЕЦЕДЕНТОМ.** Мир сводит тик-лист к
  одному самому громкому стимулу, поэтому увиденная крыса в тик громкого удара будет проглочена.
  Ровно это уже случилось со звонком будильника, и там лечили длительностью, а не правкой сведения.
  Наряд запрещает трогать сведение, требует назвать числом, куда зрение встало в порядке громкости,
  и разрешает СТОП домой, если без правки сведения зрение неработоспособно — потому что это уже
  решение владельца.

evidence: |
  Перемерено этой ногой на `4fc04b4c`:
  - `WorldSituationKind` — по-прежнему ровно два члена, `ContactedHouse` и `Landed`.
  - `grep Facing|Heading|facing` по `Core/Householder/**` и `Network/HouseholderSnapshot.cs` → **0**.
  - `grep Walker|player` по `Core/Householder/AuthoritativeHouseholder.cs` → **0**.
  - `grep Physics.|Raycast|SphereCast|Linecast` по `Network/*.cs` → только `CargoBody.cs:144,153,376`.
  - `origin/main:Assets/TunnelCrew/World/ThreeRoomHouse.prefab` → **25** вхождений `HouseWall`.
  - `Core/Multiplayer/CarryWorld.cs:167-168` — сведение к одному самому громкому.
  - `tools/select-slot.ps1 -Slot WIN-U3` → `head 4fc04b4c`, `CLEAN / AVAILABLE / lease none`.
  - `dotnet test … -c Release` → **246 passed**, прогнано этой ногой.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-sight-1-001-call.md` и карточка `c-exec-g-5a7c-sight-1-001`
     (`ready`, `slot: WIN-U3`, `basis: 4fc04b4c`).
  2. `t-sight-1`: `open → active`, журнальная строка.
  3. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-sight-1-dispatch-001.md`.

captures:
  - |
    ЗАКРЫТАЯ ЗАДАЧА СОЗДАЛА СОБЛАЗН ДЛЯ СЛЕДУЮЩЕЙ. `t-body-1` вывела направление хозяина в
    презентации — правильно для своей цели, — и тем самым положила рядом со зрением готовый, но
    запрещённый источник направления. Такие «удобные соседи» стоит искать в каждой выдаче: что
    закрытая вчера задача положила рядом, чем завтрашняя соблазнится воспользоваться неправильно.
  - |
    ПРАВИЛО «ОДИН САМЫЙ ГРОМКИЙ ЗА ТИК» УПИРАЕТСЯ УЖЕ В ТРЕТИЙ ИСТОЧНИК ПОДРЯД: звонок будильника,
    теперь зрение. Каждый раз оно лечится обходным путём в самой задаче. Это перестало быть
    случайностью и стоит вынести на review ставки отдельным вопросом: правило написано под мир с
    одним источником, а источников уже три.

decisions_needed: []

play_check:
  - 1 recite — done. Семь строк карточки пересказаны; статус самого рискованного допущения назван исполнителю прямо.
  - 2 owner inputs — не требуется сейчас: сектор и дальность подписаны критерием 6 как настройка, а не как вопрос. Его глаз понадобится ПОСЛЕ, и наряд требует отдать ему ручки.
  - 3 do the work — done. `call:executor` выпущен; названный подход «движок меряет, правила решают» зафиксирован одним токеном; отвергнутое владельцем приближение комнатами прямо запрещено.
  - 4 self-check — done. Семь строк карточки свёрнуты в три строки контракта без потерь.
  - 5 close — checkpoint.
  - G7 — done. Отрицательный исход пробы объявлен нормальным заранее, чтобы исполнитель не подгонял результат.
  - G10 — done. RESULT финальный.

log: наряд на зрение выдан в WIN-U3 от 4fc04b4c, и выдача нашла три вещи, одна из которых меняет форму задачи — у хозяина НЕТ направления взгляда нигде в правилах, оно живёт только в презентации, куда его положила закрытая сегодня t-body-1, поэтому наряд границей требует авторитетного направления и прямо запрещает читать его с картинки, иначе авторитет начнёт зависеть от того, что нарисовано; хозяин при этом не знает про игроков вообще, значит они попадают к нему только новым источником; движковых лучей у судьи нет, и производитель зрения обязан быть отдельным новым файлом, потому что единственное место с Physics занято параллельным нарядом на лут; проба впервые имеет смысл, потому что дом привёз 25 физических объектов, за которые можно спрятаться; и ловушка «один самый громкий за тик» названа до постройки в третий раз подряд, теперь с прецедентом будильника — трогать сведение запрещено, СТОП домой разрешён

next: |
  Идут: зрение в WIN-U3, лут в WIN-U1.

  Свободна и без наряда: `t-house-asset-1`. `t-handlers-1` ждёт зрения, `t-lab-2` идёт последней.

  Задачу судит глаз владельца, и наряд обязывает исполнителя вернуть ему ручки — дальность, сектор,
  число точек тела, время жизни факта.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-sight-1-dispatch-001.md
