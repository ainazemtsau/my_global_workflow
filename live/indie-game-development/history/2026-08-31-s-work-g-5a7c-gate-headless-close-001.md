# RESULT — s-work-g-5a7c-gate-headless-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-gate-headless-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: work — задача про гейт закрыта лёгким закрытием со второй попытки: недостающая улика посева перевыведена цепочкой, где каждое звено измерено направлением

close: light — because пять строк приёмки из шести перевыведены направлением первой рукой из закоммиченных байтов, идентификаторов блобов, reflog обеих веток и СОБСТВЕННЫХ прогонов направления. Ключевая строка 2 закрыта не рассказом исполнителя, а детерминированной цепочкой: собственный зелёный прогон направления доказывает, что проверяемое выражение даёт False; диф мины (прочитан направлением) требует True; значит красный неизбежен. Шестая строка НАРУШЕНА, названа, и виновник — сам наряд направления. G5-чат не открывался.

owner_approved: |
  Задача входит в девятую волну, открытую его словом 2026-08-30 «Открывай как есть, семь задач».
  Ни одна строка `done_when` не изменена. Закрытие по перевыведенному факту — работа писателя,
  отдельного слова не требует.

outcome: |
  **ЗАДАЧА, КОТОРУЮ СЕГОДНЯ УТРОМ ПРИШЛОСЬ ОТКРЫВАТЬ ОБРАТНО, ЗАКРЫВАЕТСЯ ВТОРОЙ РАЗ — И НА ЭТОТ
  РАЗ КЛЮЧЕВАЯ СТРОКА ДОКАЗАНА, А НЕ ЗАЯВЛЕНА.** Утренний ремонт снял её с `done` ровно потому, что
  улики красного не было ни одной: обе попытки закрытия умирали на ЧУЖОМ красном EditMode и до
  headless не доходили, и отчёт самого исполнителя это признавал. Нога `-002` строилась исключительно
  ради недостающей улики и ничего больше не трогала — ни `tools/**`, ни `Assets/**`.

  **СТРОКА 2 ЗАКРЫТА ЦЕПОЧКОЙ, ГДЕ КАЖДОЕ ЗВЕНО ИЗМЕРЕНО НАПРАВЛЕНИЕМ, А НЕ ПЕРЕСКАЗОМ ПРОГОНА.**
  Это важно: сам прогон — событие во времени, его нельзя перевывести из байтов никому и никогда.
  Но его следствие можно:

  - направление **само прогнало** headless на опубликованном `main`: 725 из 725 зелёных. Значит
    выражение `objective.Observe(CargoAt(7.9f, -4f))` даёт **False** — это измеренный факт, а не
    чужое слово;
  - мина `1ac70d9c` — **чистая инверсия одного утверждения**: `Is.False` → `Is.True` на том же
    выражении. Диф прочитан направлением;
  - тест детерминированный, чистый C#, без времени и окружения. **Значит красный неизбежен**, и это
    следствие собственного замера направления, а не доверие;
  - `main` **не сдвинулся**: его reflog идёт `a45b4957 @ 11:00:17` → `4e9177af @ 11:30:20`, а
    заминированное закрытие шло около `11:06`. Публикация действительно остановлена;
  - восстановление `a1712bff` **побайтно точное**: блоб вернулся `97d93cfd` → `2ae5bef3` →
    `97d93cfd`. Никакого переписывания истории, только вперёд-коммит.

  **МЕХАНИЗМ СТРОКИ 1 ПРОЧИТАН В КОДЕ, НО ЕЁ ЧИСЛО СГНИЛО, И ЭТО ДЕФЕКТ НАПИСАНИЯ КАРТОЧКИ.** Строка
  требовала «716 headless-тестов»; сегодня их 725. Требование — механизм, а не величина, и механизм
  на месте: `close-leg.ps1:383-390` гоняет `dotnet test` на интеграционном коммите, ненулевой выход
  останавливает закрытие до шага `4/7`. Отдельно ценная деталь на `:382`: перед headless чекаут
  принудительно возвращается на интеграционный коммит, **потому что Unity успевает переписать
  отслеживаемые байты**, — иначе два гейта доказывали бы разные деревья.

  **СТРОКА 3 ПРОВЕРЕНА ЧТЕНИЕМ САМОГО ТЕСТА, А НЕ ЕГО ИМЕНИ.**
  `HouseholderDecisionPipelineTests.CatalogStillCompetesWhenHandlerAnswersEveryTick` строит
  синтетический `EveryTickDecisionHandler`, отвечающий каждый тик, и требует, чтобы более тяжёлое
  каталожное предложение всё равно выиграло. Он опирается на интерфейс обработчика, а не на текущую
  реализацию дел. **Это сторож на класс, как и просила приёмка.**

  **СТРОКА 6 НАРУШЕНА, И ВИНОВАТ НАРЯД НАПРАВЛЕНИЯ, А НЕ ИСПОЛНИТЕЛЬ.** «Слот Unity не занимается»
  было написано на задачу, от которой направление же потребовало прогона закрывальщика — а
  закрывальщик гоняет Unity. Условие было невыполнимо с момента написания. Оно названо здесь, а не
  тихо пропущено: **строка не выполнена, и её невыполнение не влияет на цель задачи.**

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ ПЕРВОЙ РУКОЙ** на опубликованном `main` = `dev` = `origin/main` =
  `b22c00a2`:

  1. **Гейт гоняет headless на интегрированном коммите** — `close-leg.ps1:383-390`: `dotnet test`
     на `tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj`, `-c Release`, с TRX; ненулевой
     выход бросает исключение на `:390`. Перед этим `:382` — принудительный возврат чекаута на
     интеграционный коммит после Unity. **Число в строке приёмки (716) сгнило: сегодня 725.**
  2. **Красный останавливает публикацию** — цепочка выше: собственный прогон направления
     (`не пройдено 0, пройдено 725`), диф мины `1ac70d9c` (`Is.False` → `Is.True`), reflog `main`
     (`a45b4957 @ 11:00:17` → `4e9177af @ 11:30:20`, заминированное закрытие ~`11:06`), блобы
     восстановления (`97d93cfd` → `2ae5bef3` → `97d93cfd`).
     Дополнительно и независимо: нога `t-close-safely-1` посеяла мину ДВАЖДЫ, и оба раза `dev` не
     сдвинулся — reflog `dev` направление читало само.
  3. **Сторож на класс** — `HouseholderDecisionPipelineTests.cs:164` и далее: синтетический
     `EveryTickDecisionHandler`, проверка через `HouseholderDecisionPipeline.Evaluate`, зависимость
     только от интерфейса.
  4. **Самотест закрывальщика ЗАПУЩЕН НАПРАВЛЕНИЕМ** — `CLOSE-LEG SELFTEST: OK`.
  5. **Время названо числом до и после** — отчёт `-001`: EditMode 330.5 с на 220 тестов;
     добавленный headless-проект 5.88 с настенных (0.901 с исполнения), **около 1.8 %** от времени
     EditMode. Отчёт `-002`: EditMode 279 с на интеграционном коммите.
  6. **Слот Unity занят — строка НЕ выполнена.** Названо выше.

  Из отчёта `-002`, как улика, не как доказательство: `724 passed, 1 failed`, падение
  `Expected: True, But was: False`, остановка внутри шага `3/7` с текстом
  `the headless run failed on the integrated commit 25a7286e`.

  Отчёт в `WIN-U2` **побайтно идентичен** опубликованному в `main` — сверено `diff`.

state_changes: |
  ЗАКРЫТО ПО ПЕРЕВЫВЕДЕННОМУ ФАКТУ (2):
  - `t-gate-headless-1` → `done`;
  - `c-exec-g-5a7c-gate-headless-1-002`.

  ОБНОВЛЕНО (1 улика):
  - `i-a-report-in-main-says-delivered-about-unfinished-work-001` — противоречие сузилось и
    остаётся: заголовок `DELIVERED` теперь ПРАВДА (улика доставлена ногой `-002`), но тело того же
    отчёта по-прежнему говорит, что требуемой улики нет. Врёт теперь ТЕЛО, а не заголовок.
    Улика остаётся открытой: файл в `main` продолжает противоречить сам себе.

captures: |
  - **ЧИСЛО В `done_when` СГНИЛО ЗА ДВЕ НЕДЕЛИ — 716 ПРОТИВ 725.** Правило «живые замеры не кладут
    в условие приёмки» направлению известно и было нарушено при нарезке этой карточки. Здесь оно
    обошлось дёшево, потому что требование читается как механизм. Но это **вторая за день
    предпосылка направления, опровергнутая замером** (первая — «слияния почти всегда
    fast-forward» в `t-close-safely-1`). Обе — числа и частоты, написанные из головы.
  - **УСЛОВИЕ, НЕВЫПОЛНИМОЕ С МОМЕНТА НАПИСАНИЯ.** Строка 6 запрещала занимать слот Unity задаче,
    от которой тот же наряд требовал прогона закрывальщика, гоняющего Unity. Кандидат в правило:
    **перед выдачей проверять, не противоречит ли ограничение наряда его же требованию к
    доказательству.** Третий случай за день, когда приёмка и наряд расходятся.
  - **ЗАДАЧА ЗАКРЫВАЛАСЬ ДВАЖДЫ, И ПЕРВЫЙ РАЗ БЫЛ ЛОЖНЫМ.** Механизм лжи — закрывальщик,
    штамповавший `DELIVERED` поверх отчёта, признававшего себя недоделанным. Сегодня же построена
    защита от повторения (`t-close-safely-1`), и она проверена самотестом, прогнанным направлением.
    **Замкнулось: дефект, найденный ремонтом утром, к вечеру закрыт кодом и доказан.**

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — возврат ноги `-002`; отчёт в слоте сверен с опубликованным побайтно.
  2. WORK — шесть строк приёмки пройдены построчно; пять перевыведены первой рукой, шестая названа
     нарушенной с причиной; отчёт исполнителя читался как улика.
  3. (owner) — не требуется: задача в открытой его словом волне, закрытие по факту.
  4. CLOSE — лёгкое закрытие с обоснованием в поле `close`; G5-чат не открывался: событие прогона
     заменено детерминированной цепочкой из собственных замеров направления.

log: |
  задача про гейт закрыта со второй попытки, и ключевая строка на этот раз доказана: собственный
  зелёный прогон направления даёт False там, где мина требует True, значит красный неизбежен, а
  reflog main показывает, что публикация действительно встала; строка про незанятый слот Unity
  нарушена — условие было невыполнимо с момента написания, и написало его направление

next: |
  Волна: закрыто шесть задач из девяти. Идут две ноги — зрение U1 и «почему» U3.
  **Свободны два слота: WIN-U2 и WIN-U4, и занять их нечем:** три оставшиеся задачи ждут починки
  зрения. Зрение — единственный критический путь волны.
