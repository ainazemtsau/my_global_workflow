# RESULT s-repair-g-37a1-grid-and-building-001

call: plain owner message (no CALL) — вопрос о концепте, перешедший в решения
direction: indie-game-development · track: t-sim · play: repair · node/task: g-37a1/t-10
date: 2026-07-29

## outcome

Состояние приведено к двум фактам, которых в нём не было: продукт по t-6 ушёл на целую стадию
вперёд записанного, а t-10 была написана под модель, которую владелец отменил в тот же день.

Наряд t-6 переведён в `running` со ссылкой на замороженный PLAN и идущую стадию PAIR-CANDIDATE.
t-10 переписана под одну сетку: газовая клетка равна базовому блоку при старте 1 м, связные пустоты
внутри блока не моделируются, у блока одно число открытости на грань, принятая неточность названа
явно. t-11 выровнена по тому же метру.

Постройка блоками, которую владелец назвал сильнейшим кандидатом концепта, НЕ внесена в бет: её
текст — дословно критерий 10 условий приёмки в `TREE.md`, разворот идёт маршрутом frame/map, и он
репейру запрещён. Она записана как вход следующей версии вместе с найденной причиной, по которой
без противовеса не выпускается. Направление контракта реза и потолок маски грани записаны как
решения с триггером «перед выпуском CALL по t-7», а не как правки задач: проверка показала, что
первое — размен, а не гигиена.

## evidence

- Слова владельца 2026-07-29, дословно: «строить, естественно, только по блокам, можно, не по
  микровокселям»; «Микровоксели нужны только для разбивки, разрушения»; «логично, чтобы они
  обсыпались»; «нам не важна точность симуляции, нам важен геймплей»; «мы можем попробовать… если мы
  сделаем всё по одному метру»; «возможно, тогда надо максимальную маску взять»; «это в принципе не
  страшно» (про утечку через нерасколотый блок); «ок, запускаю в том чате, а ты оформляй свою ногу».
- Разрешение на запись после показанного одного батч-дифа: «и ты можешь обновить».
- Продукт, прочитан первыми руками, только чтение: PLAN задачи
  `c-exec-g-37a1-gas-rest-and-checksum-001` завершён, все его пункты закрыты, ADR написан, четыре
  owner-решения записаны дословно; собственный коммит-заморозка `ab71037c`, квитанция `e790a1fc`,
  оба содержатся в `origin/main`; незакрытые стадии — PAIR-CANDIDATE, PAIR-FREEZE, BUILD, VALIDATE,
  REPORT. В слоте `slot/win-u4` (стоит ровно на `origin/main`) лежат незакоммиченные
  `G37Fixtures.cs`, `G37RestPointTests.cs`, `G37Assertions.cs` — 888 строк работы прерванной сессии.
- PLAN сам выносит за скобки всё, что меняет эта нога: «No topology work of any kind (that is
  `t-7`). No change to the gas representation. … No change to the default resolution.» Поэтому метр,
  маска и направление контракта его не обесценивают и перезапуск не требуется.
- `Core/Field/Voxel/VoxelResolution.cs:84` — `MaxSubFacesPerFace = 63`: не более 7 делений на
  сторону. Крошка 4×4×4 (16 подграней) проходит, 8³ (64) и 16³ (256) — нет.
- `Core/Field/Structure/StructureGasProjection.cs:18` + `SubFaceOccupancy.cs` — проводимость грани
  есть целочисленный счёт открытых подграней с остатком по грани: щель в одну подгрань сочится, а не
  обрубается в ноль. Пересечения масок с двух сторон в коде нет: стороны одинаковы по построению.
- `Core/Field/Voxel/VoxelField.cs:818-828` — `NotifyConductivityChanged` отвергает закрытую грань:
  пути «открыть грань» не существует ни в одну сторону.
- `grep -riw seq` по `Assets` — ноль совпадений; буфер задержки ввода описан в ADR-0002 и не
  построен. Отсюда «постановка блока может быть отклонена, и сообщить об этом нечем».
- Проверка построения из пяти независимых чтений и трёх встречных разборов, 2026-07-29:
  три разбора из трёх опровергли предварительную рекомендацию ноги. Опровергнуты и записаны как не
  подлежащие возврату: «нельзя строить в газ» как защита; «грубая стена — слабая стена»;
  «симуляция бесплатна, фича независима». Подтверждено: заливка целого блока не разрезает пустоту
  ТОЛЬКО при газовой клетке, равной блоку. Сводка — `work/2026-07-29-core-grid-and-building-decisions.md`.
- Дрейф, найденный попутно и подтверждённый: ряды 8 и 11 в `work/converge-g-37a1-core-rows.md` не
  обновлены поправкой 2026-07-29; `work/topology-boundary-g-37a1.md` §3 обосновывает модель
  стоимости снятой редакцией ряда 8.
- До применения перечитаны свежие `NOW.md`, `TREE.md`, LOG tail, `os/plays/repair.md` и writer
  contract. `TREE.md` и `CHARTER.md` не менялись. Продуктовый репозиторий открывался только на
  чтение.

## state_changes

```text
live/indie-game-development/NOW.md:
  updated -> s-repair-g-37a1-grid-and-building-001
  bet.cut_list: строка микротопологии больше не несёт «0,5/1 м»; добавлена строка одной сетки —
    газовая клетка = базовый блок, старт 1 м, связные пустоты внутри блока не моделируются,
    крошка 4×4×4, ширина маски грани — versioned config, а не константа
  tasks.t-10: goal и done_when переписаны под одну сетку; сняты связные пустоты внутри клетки,
    по-компонентные маски граней, сохранение массы при объединении полостей и замер двух вариантов
    базового блока; добавлены одно число открытости на грань, принятая неточность и осыпание крошки
  tasks.t-11: замер «вариантов базового блока 0,5 и 1 м» -> при базовом блоке 1 м с крошкой 4×4×4
  open_calls.c-exec-g-37a1-gas-rest-and-checksum-001: ready -> running; добавлен started с owner
    launch words и продуктовой ссылкой (PLAN ab71037c, квитанция e790a1fc, стадия PAIR-CANDIDATE,
    слот win-u4, engineering_contract 31); id, track, for, call и смысл сохранены
  issues.i-gas-core-open-questions-001: пункт «0,5/1 м — t-10/t-11» отмечен закрытым решением
    владельца в пользу 1 м; review_when выровнен
  issues: + i-topology-commit-direction-001 (execution, route work, триггер — перед выпуском t-7)
  issues: + i-subface-mask-ceiling-001 (execution, route work, тот же триггер)
  issues: + i-player-building-next-version-001 (roadmap, route map, триггер — TREE критерий 14)
  issues: + i-core-rows-amendment-drift-001 (execution, route review)
  tasks/tracks/forecast/recurring/decisions в остальном не тронуты; ни одна задача не закрыта,
    ни один наряд не удалён, WIP-лимит не менялся

live/indie-game-development/work/:
  + 2026-07-29-core-grid-and-building-decisions.md
  converge-g-37a1-core-rows.md: добавлена шапка-предупреждение о неприменённой поправке; ни один
    байт ниже не изменён
  topology-boundary-g-37a1.md: добавлена шапка-предупреждение о снятом обосновании §3; ни один байт
    ниже не изменён

work/game-concept-discussion-handoff-2026-07-29.md (репозиторный work/):
  закоммичен как есть, evidence-only; документ сам объявляет себя неавторитетным

os/FRICTION.md:
  + одна строка: bare `work/<file>` pointer ambiguous between repo-root and live/<id>/work

live/indie-game-development/LOG.md: одна строка
live/indie-game-development/history/2026-07-29-s-repair-g-37a1-grid-and-building-001.md: этот RESULT
```

## play_check

- 1 name the contradiction: done — (а) наряд t-6 стоял `ready`, продукт на стадии PAIR-CANDIDATE
  с замороженным PLAN; (б) t-10 описывала связные пустоты внутри газовой клетки, отменённые
  решением владельца о метре и о неважности точности.
- 2 reconstruct: done — newest-first по LOG, history 2026-07-29, git обеих репозиториев, PLAN и
  tasks.md в продукте, слоты worktree. Артефакты и коммиты поставлены выше логов.
- 3 propose corrected state: done — каждое изменение с одной причиной; ничего не удалено; четыре
  новых issue со стабильным id, уровнем, маршрутом, триггером и указателем; forecast не трогался,
  калибровки не появилось.
- 4 confirm (owner): done — один батч-диф показан целиком; слова владельца после него: «и ты можешь
  обновить». `TREE.md` и `CHARTER.md` не менялись: разворот критерия 10 передан маршруту map через
  `i-player-building-next-version-001`.
- 5 friction: done — одна строка в `os/FRICTION.md` про неоднозначность префикса `work/`; OS здесь
  не чинился.

## captures

- Владелец 2026-07-29 назвал возможной будущей вставкой давление газа, ломающее блок, и реакции.
  Не в ядре; втыкается в три существующие розетки. Записано в
  `work/2026-07-29-core-grid-and-building-decisions.md` §4 как кандидат в противовес постройке.

## decisions_needed

- нет. Разворот критерия 10 не выносится сейчас: он открывается только после того, как владелец
  зашёл в собранное ядро (`TREE.md` g-37a1 критерий 14), и это записано как issue с триггером.

## next

return-to-owner. Действующий фронт: `c-exec-g-37a1-gas-rest-and-checksum-001` (t-sim) идёт в слоте
win-u4 на стадии PAIR-CANDIDATE; готовы к выдаче `c-exec-g-37a1-body-first-person-001` (t-body) и
`c-exec-g-37a1-venue-packaged-player-001` (t-venue). Перед любым CALL по t-7 сначала решаются
`i-topology-commit-direction-001` и `i-subface-mask-ceiling-001`.

## correction (тот же день, тот же leg, поймано владельцем сразу после apply)

Первая редакция t-10 и §1 рабочего документа несла строку «крошка со временем осыпается, и блок
приходит к полностью сплошному либо полностью пустому». Оба конца были неверны, и владелец поймал
это первым же вопросом («мы же хотим, чтобы клетка по частям откалывалась»):

- «полностью сплошному» закрывало бы выкопанное без вещества — прямое нарушение подписанной
  строки 8 и критерия 10 в `TREE.md`;
- «полностью пустому» означало бы, что один удар киркой со временем бесплатно растворяет кубометр.

Исправлено в том же состоянии: крошка объявлена временным ХРАНИЛИЩЕМ, а не временной геометрией.
По истечении срока подробная маска отбрасывается, шесть чисел открытости граней сохраняются, блок
не возвращается к сплошному и не пустеет сам. Явно записано, что кирка откалывает КУСКИ, а не
открывает блок целиком, и что перестановочность копания держится на кусках. Требование владельца
про память при этом выполняется полностью.

Изменённые поля: `NOW.md` tasks.t-10.done_when, `NOW.md` bet.cut_list (строка одной сетки),
`work/2026-07-29-core-grid-and-building-decisions.md` §1, §2 и §4. Остальные state_changes выше
применены без изменений.

END_OF_FILE: live/indie-game-development/history/2026-07-29-s-repair-g-37a1-grid-and-building-001.md
