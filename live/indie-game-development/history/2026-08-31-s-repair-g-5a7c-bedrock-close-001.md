# RESULT — s-repair-g-5a7c-bedrock-close-001

direction: indie-game-development
play: repair
node/task: g-5a7c / t-bedrock-marks-gauges-1, полоса `закрепление`
track: закрепление
date: 2026-08-31
outcome_kind: repair — первый настоящий перенос в закреплённое закрыт по перевыверенным байтам; его пять «обо что споткнулся» разнесены по адресам

owner_approved: |
  Закрытие по перевыверенному факту — работа ремонта, отдельного слова не требует. Полоса
  объявлена его словом 2026-08-31 («объявляю пятую полосу «закрепление», идёт вне волны, слот U3»),
  и это уже записано в карточке ставки.

  Ни одна строка `done_when` не изменена.

outcome: |
  **ПЕРВЫЙ ПЕРЕНОС ПОСЛЕ ПОСТРОЙКИ ГРАНИЦЫ ПРОШЁЛ РОВНО ПО ФОРМЕ, И ЭТО ПЕРЕВЫВЕРЕНО, А НЕ ПРИНЯТО
  НА СЛОВО.** Диф коммита `df1d70c2`: четыре rename по 100 %, одна новая folder-meta на 8 строк,
  **ноль строк C# добавлено и ноль удалено**. Ожидание наряда — «`git mv` и больше ничего» —
  выполнено буквально.

  **ГРАНИЦА ЦЕЛА, И ЭТО ГЛАВНОЕ.** `TunnelCrew.Core.asmdef` по-прежнему несёт `references: []` и
  `noEngineReferences: true`; второго `.asmdef` в новой подпапке нет; **за всю волну не изменён
  ни один `.asmdef`** — `git diff --stat c7e926cf main -- '*.asmdef'` пуст. Отрицательный контроль
  предъявлен дважды, обеими системами сборки: `CS0246` на `HousePlan` в Unity и в
  `dotnet build core/TunnelCrew.Core.csproj`, и удалён после.

  В закреплённом теперь **пять файлов вместо трёх**: три описания комнаты плюс метки и шкалы.

  **ВОЗРАСТ СОШЁЛСЯ С НЕЗАВИСИМЫМ ЗАМЕРОМ НАПРАВЛЕНИЯ.** Оба файла рождены коммитом `3f499016`
  2026-08-15 (`+207/-0` и `+310/-0`) и с тех пор не тронуты ни разу. Он назвал 15 полных суток,
  направление считало 16 календарных — тот же коммит, та же метка времени, разница в способе счёта.

  **ПЯТЬ «ОБО ЧТО СПОТКНУЛСЯ» — ЭТО ПОЛОВИНА ЦЕННОСТИ НОГИ, КАК И БЫЛО ЗАКАЗАНО.** Три из пяти
  разнесены этой ногой по адресам:

  - **полный набор тестов, запущенный в живом редакторе слота, переписал два отслеживаемых файла**
    (`FirstHouse.prefab`, `ProjectSettings/DynamicsManager.asset`); он восстановил их через Git и
    назвал это главным дефектом собственного выполнения. **Это ТРЕТИЙ за сутки случай одного
    класса** — заведена улика;
  - **`close-leg` ждёт суффикс лизы `:build`, а штатный claim пишет `:BUILD`** — отказ по регистру
    буквы; ушло десятой строкой в `t-close-safely-1`;
  - **готового следующего кандидата нет**, и он доказал это по двум осям — ушло в знание.

  **ЗНАНИЕ ИСПРАВЛЕНО ТАМ, ГДЕ ОНО ВРАЛО.** Запись `bedrock-promotion-runs-beside-the-wave.md`
  называла `Probe/Movement` «единственным сегодня полностью отрезаемым модулем». Отрезаем — да;
  изолирован — нет, и на этой оси подряд провалились ДВА кандидата. Добавлен §4 «кандидат
  проверяется по двум осям, и вторая важнее первой», а §2 приведён с ним в согласие, чтобы файл не
  спорил сам с собой.

evidence: |
  Всё перевыверено направлением на `main` = `dev` = `origin/main` = **`a45b4957`**:

  - `git show --stat --summary df1d70c2` → 5 файлов, 8 вставок; четыре строки `rename … (100%)`,
    одна `create mode … Core/Householder.meta`;
  - `git ls-tree -r --name-only main -- Assets/TunnelCrew/Core/` → пять `.cs`: `House/HousePassage`,
    `House/HousePlace`, `House/HouseRoom`, `Householder/HouseholderGauges`,
    `Householder/HouseholderMarks`, плюс один `TunnelCrew.Core.asmdef`;
  - `git show main:Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef` → `"references": []`,
    `"noEngineReferences": true`;
  - `git diff --stat c7e926cf main -- '*.asmdef'` → пусто;
  - возраст: `git log --follow -M --numstat` с отбрасыванием нулевых → по одной ненулевой записи на
    файл, обе `3f499016`, 2026-08-15 06:12:38 +0300.

  Из отчёта исполнителя, как улики, не как доказательства: `CS0246` в двух системах сборки;
  headless 725/725 и EditMode 220/220 на интегрированном коммите; `check.ps1` зелёный.

state_changes: |
  ЗАКРЫТО ПО ПЕРЕВЫВЕРЕННОМУ ФАКТУ (2):
  - `t-bedrock-marks-gauges-1` → `done`;
  - `c-exec-g-5a7c-bedrock-marks-gauges-1-001`.

  СОЗДАНО (1 улика):
  - `i-a-full-run-in-a-live-editor-rewrites-tracked-bytes-001` — третий за сутки случай класса
    «редактор в живом или общем дереве молча меняет байты, на которые кто-то опирается».
    Маршрут `work`, снимается вместе с `t-close-safely-1`.

  ДОПИСАНО (1 строка приёмки):
  - `t-close-safely-1` получила десятую строку: суффикс лизы сравнивается без оглядки на регистр.

  ИСПРАВЛЕНО ЗНАНИЕ (1):
  - `knowledge/bedrock-promotion-runs-beside-the-wave.md` — добавлен §4 о двух осях выбора
    кандидата, с тремя перемеренными примерами и датой, когда появится следующий
    (`WalkingBody.cs`, 2026-09-10). §2 приведён в согласие с §4: `Probe/Movement` больше не
    называется годным кандидатом.

  ПОЛОСА `закрепление` ОСТАЁТСЯ ОБЪЯВЛЕННОЙ И ПУСТОЙ. Это не простой и не дефект: правило §1 того же
  знания говорит, что она идёт рядом с волной и волну не держит.

captures: |
  - Два места, где нога споткнулась и которые никуда не ушли, потому что мелкие: пути в наряде
    отличались от фактических (`C:\\my_global_workflow_worktrees\\…` против его чтения), и запись в
    Git потребовала повтора вне песочницы после `Permission denied`. Обе — трение окружения, не
    дефекты состояния.

decisions_needed: |
  Ни одного. Следующий кандидат на закрепление появится 2026-09-10; до тех пор полоса пуста
  законно.

play_check: |
  1. Назвать противоречие — нога заявила выполнение, а закрывать по заявлению сегодня уже трижды
     оказывалось ошибкой.
  2. Восстановить новейшим-первым — форма дифа, состав закреплённого, asmdef и возраст прочитаны
     из байтов `a45b4957`; отчёт читался как улика.
  3. Предложить исправленное состояние — закрытие плюс три адреса для находок ноги.
  4. Подтверждение (owner) — не требуется: закрытие по факту в объявленной полосе.
  5. Дыра в OS → нет. Все три находки продуктовые и разнесены.

log: |
  первый перенос в закреплённое закрыт по байтам — четыре rename и folder-meta, ноль строк C#,
  граница цела; три из пяти его находок разнесены: улика про живой редактор, строка про регистр
  лизы, и правило двух осей в знание

next: |
  Домой. Полоса `закрепление` пуста до 2026-09-10 — это законная пауза, а не простой.

  Волна: закрыто четыре задачи, гейт открыт обратно до доказательства. Идёт зрение в WIN-U1;
  ждут выдачи `t-close-safely-1` в WIN-U4 и доработка гейта `-002` в WIN-U2. WIN-U3 свободен.
