# RESULT — s-shape-g-1d84-integrated-house-bet-001

leg: s-shape-g-1d84-integrated-house-bet-001
play: shape
direction: indie-game-development
node: g-1d84
call: work/c-shape-g-1d84-001-call.md
date: 2026-08-04
owner_present: yes

## outcome

Узел «интегрированный кусок дома» стал ставкой направления его отдельным словом на показанный целиком
экран. Двадцать три задачи по полдня, у каждой единица — видимый глазами результат; пять названных
полос при его WIP в три; десять вырезов, два из которых реально больно; вердикт по каждой из семи
линз устава; четыре ранжированные догадки, и самая рискованная стоит ВТОРОЙ задачей всей волны.
Аппетит и порог смерти записаны его словами, дат нет ни одной. Пять починок носителей §VERIFY-002
исполнены, и ни одна не превращена в новый ответ. Выпущен один продуктовый наряд на первую задачу;
остальные пишутся своей ногой.

## evidence

1. **Слово владельца, дословно и в трёх частях** — единственный источник открытия ставки:
   `1 Аппетит: берём Б`; `2 ок с 3х но если будут параллеьные еще я их буду запускать если будет
   время`; `3 да`. Экран был показан ему ЦЕЛИКОМ в одном сообщении до ответа: аппетит с его точкой
   пересмотра, все двадцать три задачи с их видимым результатом, пять полос, десять вырезов, семь
   линз, четыре догадки, порог смерти, две висящие развилки и два флага.
   **Сила токенов записана и не завышена:** аппетит выбран вариантом Б из двух показанных; WIP
   объявлен ТРОЙКОЙ, а оговорка про дополнительные параллельные принадлежит ему и остаётся его —
   ни одна нога не превращает её в более высокий постоянный WIP; `да` открывает ставку и не является
   подписью под текстом ни одной отдельной задачи.
2. **База продукта проверена первой рукой**, а не взята из отчёта и не из чужого рабочего каталога:
   `C:/projects/Unity/GasCoopGame`, `HEAD` = `main` = `839df47e`, `git status --porcelain` пуст.
   Через `git ls-tree -r 839df47e` подтверждено существование каждого пути, названного в наряде на
   первую задачу; подтверждено также, чего НЕТ: папки `Assets/TunnelCrew/Art/`, сцены хозяина в
   `EditorBuildSettings`, и хотя бы одной сцены, где хозяин и двое сетевых носильщиков вместе.
3. **Слоты перепроверены и одна запись состояния закрыта измерением.** Реестр найден по факту, а не
   по памяти: он живёт в `C:/projects/Unity/GasCoopGame_slot-state/gascoop-slot-state.v1.json`, а в
   `.git` самого репозитория осталась только пустая `.lock`, которую нельзя принимать за реестр. Все
   четыре слота `AVAILABLE`, `lease: none`. `slot/win-u1` стоит на `839df47e`;
   `git branch --contains 8eb0bb0f` не возвращает ни одной ветки — семи коммитов остановленного
   наряда нет нигде, поднимать из них нечего.
4. **Двадцать три задачи, у каждой поле `visible`** — то, на что он смотрит глазами. Это жёстче
   правила «≤ половины сфокусированного дня» и исполняет его прямое требование «Задачи режь МЕЛКО…
   Каждая задача — это что-то, на что я могу посмотреть глазами».
5. **Размер назван честно ДО его выбора аппетита:** 23 задачи против 11 задач и нуля новых слоёв в
   закрытой волне; три новых слоя (описание дома как данных, профили текстом, хозяин под судьёй и в
   снимке). Ни одна дата из этого размера не выводилась и ему не приносилась.
6. **`done_when` задач собраны из строк `acceptance` И из карточки, прочитанной напрямую** — это цена
   варианта (В), названная ему до выбора. Проверяемо: цель захода с точкой доставки уехала в
   `t-house-6`, три комнаты — в `t-house-1`, обстановка — в `t-house-3`, и все три пришли из текста
   карточки, а не из набора `acceptance`, куда они не попали (находка V5).
7. **Пять починок носителей исполнены, каждая с записанной границей того, чем она НЕ является** —
   `work/converge-g-1d84.md` §SHAPE-001. Счёт файла пересобран и сведён к одному числу: 114 строк,
   88 `answered`, 29 `acceptance`, 3 улики, 22 `open` + 1 `deferred`; владельцу по-прежнему должны
   ровно две строки.
8. **Ни одна открытая строка не закрыта нарезкой.** 19 прежних открытых плюс три новые едут в наряды
   вопросами к инженерной ноге; арх-файл не дал ни одной строки в `done_when` — только улику в
   `context`.

## state_changes

1. `TREE.md` — g-1d84: `status: parked` → `active`; добавлены `appetite` и `kill_by` его словами;
   `detail` переставлен на этот RESULT. **Текст карточки не тронут ни одной буквой:** цель,
   двенадцать критериев, `why` и `risk` — как были.
2. `TREE.md` `owner_approved` — две новые записи реестра: (а) четыре правки карточки 2026-08-04 с
   точной силой токенов «1а, 2а, 3а, 4а», отклонёнными вариантами и явным запретом читать четыре
   буквы как одну подпись (починка V1 — реестр эту правку пропустил, и пропуск был настоящим
   дефектом); (б) открытие ставки 2026-08-04 с его тремя частями ответа и точной силой каждой.
3. `NOW.md` — `bet` (цель, слово владельца, аппетит, подход с двумя отклонёнными на равных
   основаниях, порог смерти, обе ветки, оценщик и откат, четыре ранжированные догадки, десять
   вырезов, семь линз, диспозиция открытых строк); `tracks` (WIP 3 с точной силой его оговорки,
   правило одной сцены, пять полос с названным куском и названным моментом входа корня в
   интегрированную сцену); `tasks` — двадцать три задачи.
4. `NOW.md` `open_calls` — `c-shape-g-1d84-001` очищен; заведены `c-exec-g-1d84-one-scene-what-exists-001`
   (первая задача, `ready`, текст написан) и `c-work-g-1d84-first-lane-calls-001` (написать наряды на
   три остальные готовые задачи; текста намеренно нет, потому что каждый обязан проверить КАЖДЫЙ
   названный путь на объявленной базе).
5. `NOW.md` `decisions` — `d-g-1d84-second-fail-branch-001` закрыт нарезкой, как и было записано в
   самой записи; обе висящие развилки владельца получили поле `blocks_task`: форма файла профилей
   держит ровно `t-host-3` и полосу не останавливает, чтение слова «инструментов» не держит ничего
   до первой покупки или установки.
6. `NOW.md` `issues` — `i-bet-tasks-never-sized-001` исполнено и оставлено открытым ради правила
   вперёд; `i-newgame-call-oversized-and-stopped-001` закрыто ИЗМЕРЕНИЕМ, а не решением;
   `i-owner-working-scenes-need-real-tools-001` проверено построчно против пяти полос.
7. `work/converge-g-1d84.md` — §SHAPE-001 плюс правки ровно в названных находками местах: I01, W57,
   C16, W05, W94a, W107, W99, C09 и три новые строки W108, W109, W110; в §SIGNOFF добавлено
   четвёртое число счёта, прежние сохранены.
8. `work/c-exec-g-1d84-one-scene-what-exists-001-call.md` — новый наряд на первую задачу.
9. `LOG.md` — одна строка.

## captures

- Вырез 1: сглаживание движения по сети и предсказание на клиенте. Названо владельцу ДО первого
  просмотра первого лица, чтобы тиковое движение на второй копии не стало сюрпризом.
- Вырез 2: готовый поиск пути движка. Пакет навигации в проекте стоит и остаётся неиспользованным.
- Вырез 6: ловля мыши и что происходит с пойманным игроком — его собственная отложенная идея.
- Реестр слотов физически живёт вне репозитория, в `GasCoopGame_slot-state/`, а внутри `.git` лежит
  только пустая `.lock`. Ногу это не блокировало; записано, чтобы следующая не искала его в `.git`.
- Имя игры и base AppID стоят в дереве на 2026-08-05, то есть на завтра, и от сборки не зависят.
  Названо ему флагом; его сознательный порядок «работа первой» не переигрывается.

## decisions_needed

Ни одной новой. Две прежние остаются `waiting_owner` и обе названы построчно вместе с задачами,
которые на них стоят: форма файла профилей (`t-host-3`) и чтение слова «инструментов» (ничего не
держит до первой покупки или установки). Нарезка их не решала и в работу не ставила.

## play_check

1. **Recite** — цель и двенадцать критериев g-1d84 пересказаны, цель родителя g-0c26 названа. Цель
   узла является результатом, а не деятельностью: «существует играемый кусок дома… владелец заходит
   и играет в это сам».
2. **Appetite first** — аппетит поставлен ПЕРВЫМ пунктом экрана и до списка задач, с прямо
   написанным условием «если ты меняешь аппетит — список пересобирается, а не досочиняется».
   Внешний взгляд предъявлен тремя сравнимыми: закрытая волна g-6b13 (11 задач, ноль новых слоёв,
   четыре календарных дня, два критерия из четырёх не сделаны); бумажная петля g-8f31 (шесть ног,
   89 строк, ноль строк игрового кода); и собственный размер этой волны (23 задачи, три новых слоя).
   Дат не назначено ни одной — его запрет 2026-08-02 действует, и размер назван без вывода даты.
3. **Approaches** — три структурно разных способа названы ДО эскиза: «полосы по подсистемам, сборка
   в конце», «взять готовое», «живая сцена с первого дня». У каждого названа догадка, на которую он
   ставит. Выбран третий, с однострочным почему-он-лучше-остальных; два отклонённых ушли в записи
   ставки с причинами, а не выброшены. Названо явно, чего самая маленькая версия НЕ включает.
4. **Scope hammer** — десять вырезов, из них первые два реально больно (сглаживание по сети;
   готовый поиск пути движка), что и требует G6. Входящие открытые строки разбора вырезом НЕ стали
   ни одна: 22 из 23 уехали в наряды строками `→ PLAN`, две остались за владельцем.
5. **Lens sweep** — вердикт по каждой из семи линз устава: три задачи (линзы 2, 3, 6) и четыре
   `not_needed` с причиной (линзы 1, 4, 5, 7). Молчаливого пропуска нет. Линзы 3, 5 и 6, которые
   §COVERAGE честно называл неадресованными, получили ответ здесь.
6. **Riskiest assumption** — четыре догадки ранжированы по силе убить волну, первой стоит догадка
   выбранного подхода и то, что судит владелец глазами. Тест самой рискованной — `t-player-1`,
   ВТОРАЯ задача всей волны, полдня, на уже собранном. Глубина проверки откалибрована по
   осторожному режиму устава: проверяем на самом дешёвом из существующего, а не строим стенд.
7. **Tasks** — двадцать три задачи, каждая с проверяемым `done_when` и полем `visible`. Числа «3-7»
   плей не выдерживает, и это записано честно, а не обойдено: KERNEL §3 требует от задач РАЗМЕРА и
   отдаёт нарезку списка самой shape, а прямое требование владельца («режь МЕЛКО… чтобы посмотреть
   глазами») делает единицу мельче полудня. Внутри полос — от двух до девяти задач. Порядок ставит
   рискованную задачу второй. Все задачи — `executor`. Оценщик и откат названы до задач. Полос две
   и больше, поэтому они предложены владельцу вместе с WIP, и объявлены ТОЛЬКО его словами.
8. **Kill criteria** — порог его словами, дат нет по его прямому запрету; KERNEL ставит живое слово
   владельца выше себя, и прецедент g-6b13 идентичен. Добавлены два наблюдения волны, показанные ему
   и покрытые его «да». Обе ветки написаны: next_if_true и next_if_false.
9. **Close** — один экран показан целиком, ставка открыта его отдельным словом.

**Границы наряда исполнены построчно.** Карточка не переоткрыта (правки только `status`, `appetite`,
`kill_by`). Ни одна открытая строка не отвечена нарезкой. Арх-файл не дал ни строки в `done_when`.
Продуктовая работа не запущена — выпущен один наряд со статусом `ready`, запускает его владелец.
Пять починок носителей исполнены, ни одна не стала новым ответом.

**Названное трение, а не оправдание.** Плей shape говорит «3–7 задач», и волна из двенадцати
критериев в это число не помещается при требовании владельца резать мельче полудня. Нога выбрала
требование владельца и KERNEL §3, а не число плея, и записывает это как расхождение для
maintenance, а не как исполнение. Второй случай того же расхождения — повод для MAINTENANCE REQUEST.

## log

Ставка g-1d84 открыта его словом: 23 мелкие задачи с видимым результатом, 5 полос при WIP 3, 10
вырезов, 7 линз, самая рискованная догадка стоит второй задачей; аппетит и порог смерти его словами
без дат; пять починок носителей исполнены без единого нового ответа; выпущен один первый наряд.

## next

1. **`c-exec-g-1d84-one-scene-what-exists-001`** — `ready`, текст написан, база и все названные пути
   проверены первой рукой на `839df47e`. Первая задача волны: одна сцена, в ней двое сетевых
   ходоков и существующий груз, Play поднимает игру сразу как судью.
2. **`c-work-g-1d84-first-lane-calls-001`** — `ready`. Написать наряды на `t-house-1`, `t-host-1` и
   `t-look-1`. Готовых к старту задач четыре, WIP владельца — три: три из четырёх идут, четвёртую
   запускает он сам, если захочет.
3. **После первых трёх задач — точка пересмотра аппетита.** Она часть его выбора варианта Б и
   принадлежит ему: нога её не проводит и не переносит.
4. Обе висящие развилки — когда он скажет. Ни одна из них ставку сегодня не держит.

END_OF_FILE: live/indie-game-development/history/2026-08-04-s-shape-g-1d84-integrated-house-bet-001.md
