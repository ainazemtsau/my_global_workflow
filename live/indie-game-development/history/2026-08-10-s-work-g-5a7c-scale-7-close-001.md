# RESULT — s-work-g-5a7c-scale-7-close-001

play: work · close: light — because каждая строка `done_when` разрешается либо байтом, деревом,
коммитом и выводом проверки, которые эта нога перечитала и ПРОГНАЛА первой рукой, либо уже
закоммиченными дословными словами владельца об этом же кандидате; ни одного качественного вердикта,
которого никто не выносил, здесь не додумывается
direction: indie-game-development · node: g-5a7c · task: t-scale-7 · call: c-exec-g-5a7c-cargo-class-seam-001
date: 2026-08-10

base_read: OS — `wt/indie-game-development`, рабочее дерево чисто, ветка опережает `origin` на 106
коммитов. Продукт перемерен `git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` =
`origin/slot/win-u1` = `43648b2dd4fbaaf9570bd56cdf440cc904e1c222`. Слот WIN-U1 по
`gascoop-slot-state.v1.json`: `lifecycle: AVAILABLE`, `lease: none`, `mcp_endpoint: unrecorded`.
Рабочее дерево слота чисто, HEAD = опубликованной голове.

## outcome

`t-scale-7` закрыта, и с ней закрыта последняя задача волны `g-5a7c`. Числа груза перестали быть
общей настройкой игры: длина, ширина, высота и масса живут свойствами именованного класса
`standard-cargo`, а конкретный кусок называет свой класс полем `CargoState.CargoClassId`.

**Шов проверен по сильному признаку, а не по слабому.** Слабый признак — «все читают из класса».
Сильный — старого источника БОЛЬШЕ НЕТ: `_cargoLength`, `_cargoWidth`, `_cargoHeight`, `_cargoMass`
удалены из настроек, `HalfLength`, `HalfWidth`, `HalfHeight`, `CargoMass` удалены из `CargoRules`.
Взять общее число неоткуда, потому что его не существует.

**Задача закрывается со второго кандидата, и первый был отбит этой же линией проверки.** Первый
возврат (`5cbe80ca`) нёс только opaque `CargoClassId` без чисел, а его тест
`ClassIdentityAloneDoesNotChangeTheCarryLaw` доказывал ОБРАТНОЕ требуемому и стоял в графе
«доказательства». Корень найден и записан отдельной карточкой: наряд физически не дошёл до
исполнителя (`i-call-text-never-reached-the-executor-001`).

## evidence — перемерено и прогнано первой рукой, а не принято с возврата

**Именованный класс и его числа.** `Assets/TunnelCrew/Core/Cargo/CargoClass.cs` — engine-free
`CargoClass` (`CargoClassId`, `Name`, `Length`, `Width`, `Height`, `Mass`, полуразмеры),
`ICargoClassResolver`, `CargoClassCatalog` и проекции `Body` / `Presentation` / `StartMarker`.
В `NetworkPlaySettings.asset` один класс: `standard-cargo`, id `0`, `0.192 × 0.04 × 0.04`, масса
`48`; `_additionalCargoClassIds: 0, 0, 0` — все четыре куска на нём.

**Старого источника нет.** `grep -rn "CargoLength\|CargoWidth\|CargoHeight\|CargoMass" Assets/
--include=*.cs` — **пусто**. Прогнано этой ногой на `43648b2d`.

**Семь читателей, каждый перечитан по диффу `918600ba..43648b2d`:** зажим хвата
(`CargoGeometry.NearestLocalPoint` → `rules.ResolveCargoClass(cargo).HalfLength/HalfWidth`); луч
выбора (`CargoGeometry.TryRaycast` → полуразмеры класса); преграда
(`AuthoritativeCargoRoster.TryGetBox` — коробка по class id КАЖДОГО предмета, а не одна на всех);
точка приложения (`CargoHaul.MaximumOffset(CargoClass)`); вес в законе подъёма
(`CargoHaul.VerticalDampingCoefficient(_, cargoClass.Mass, _)`); физическое тело (`CargoBody` →
`CargoClassProjections.Body`, и scale, и `Rigidbody.mass`); рисунок (`NetworkCargoPresentation` →
`CargoClassProjections.Presentation`); редакторская метка (`StartConditionsEditor` и
`World/StartConditions` → `CargoClassProjections.StartMarker`).

**Доказательство шва — тест, требуемый нарядом, и он теперь доказывает то, что требовалось.**
`CargoClassSeamTests.SecondTestOnlyClassChangesEveryReaderWithoutChangingTheRule`: ОДИН экземпляр
`CargoRules`, каталог из двух классов (`2×2×2` масса `4` и `6×4×4` масса `16`), шестнадцать
утверждений — луч попадает раньше, зажим шире по длине и по ширине, преграда шире, точка приложения
дальше, вертикальная сила меньше по массе, тело больше по всем трём размерам и тяжелее, рисунок
больше по всем трём, метка выше и больше. Правило при этом не правится. Инвертированный тест
первого кандидата удалён. Второй класс живёт ТОЛЬКО в тесте — в игре класс один.

**Поведение не изменилось — проверено числами, а не заявлением.** Числа переехали знак в знак
(`0.192 / 0.04 / 0.04 / 48`). Высота спавна: база `918600ba` брала `_cargoRestHeight` = `0.02`,
HEAD берёт `cargoClass.CenterYOnFloor` = `0.04 / 2` = `0.02` — то же число, источник правильный
(высокий класс в будущем не окажется в полу).

**Ни один существующий тест не ослаблен — это факт, а не заявление.** `git diff --stat
918600ba..43648b2d -- tests/` = ОДИН файл, `+307 −0`. Изменённых тестов ноль.

**Проверки прогнаны этой ногой на опубликованной голове `43648b2d`:**
`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **пройдено 151,
не пройдено 0, пропущено 0**. `.\pwsh.cmd tools/hygiene.ps1` → `hygiene OK`. Рабочее дерево слота
после прогонов чисто.

**Границы целы.** `git diff --name-only 918600ba..43648b2d -- ProjectSettings/ Assets/GasCoopGame/
Assets/Scenes/ '*.unity'` — пусто. На HEAD не тронуты: `_cargoHoldStrength: 200`,
`_cargoMaxHolders: 4`, `_cargoVerticalDampingRatio: 1`, `_cargoHandForceReachFalloff: 1`,
`_cargoCarryHeight: 0.092`. Первый шов задачи (личная мощность хвата) не переделан:
`CargoHaul.cs:173` — `var limit = hold.Strength;`, `CargoHold.Strength` на месте.

**Публикация и целостность слияния.** Кандидат `eb8babad` интегрирован merge-коммитом `e6170c6b`,
документные головы `3823220c` и `43648b2d`. `git diff --stat 206c0baf 43648b2d -- Assets/ tests/
ProjectSettings/` — **пусто**: принятые байты слиянием не переписаны. Опубликовано:
`origin/dev` = `origin/main` = `origin/slot/win-u1` = `43648b2d`.

**Откат.** `git log --oneline --no-merges 918600ba..43648b2d -- Assets/ tests/` даёт РОВНО два
коммита содержания: `eb8babad` и `5cbe80ca`. Других коммитов, трогающих эти пути, нет, поэтому
`git revert --no-edit eb8babad 5cbe80ca` возвращает `Assets/` и `tests/` к дереву базы по
построению. Названо честно: это ВЫВЕДЕНО из истории, а не исполнено — настоящего revert-коммита с
readback дерева, какой был у `t-scale-6` (`cbf3a360`), здесь нет.

**Слово владельца — уже закоммичено на опубликованной голове**, `docs/results/
c-exec-g-5a7c-cargo-class-seam-001.md`, после свежего прогона на несущей половине `eb8babad`,
дословно: **«отличий не заметел»**. Именно эта строка закрывает задачу по тексту наряда. Прежнее
«работает также» относилось к кандидату-бирке и здесь не используется.

**Что НЕ перемерено этой ногой и названо как есть:** настоящий Unity import/compile. Слот
освобождён, endpoint снят с записи, editor этой ноге недоступен. Улика взята с возврата: прямой MCP
`WIN-U1`, `AssetDatabase.Refresh(ForceSynchronousImport)`, `IsCompiling=false`, свежих `Error` /
`Exception` нет — снята на `eb8babad`, то есть на тех же байтах, что опубликованы (byte-identity
через слияние проверена выше). Плюс сам прогон владельца в собранной игре есть более сильное
свидетельство компиляции, чем импорт.

### `done_when` наряда — построчная диспозиция

**Строка 1 — числа из именованного класса, шов у всех читателей, место для второго класса доказано
тестом. ЗАКРЫТА.** Класс есть, общих полей нет в коде вовсе, семь читателей идут через класс,
требуемый тест написан и делает ровно то, что заказано; в игре класс один.

**Строка 2 — поведение не изменилось ни на грамм. ЗАКРЫТА.** Числа знак в знак, высота спавна то же
`0.02`, 151/151 зелёные и прогнаны здесь, ни один существующий тест не тронут, первый шов
`t-scale-6` не переделан, слова владельца «отличий не заметел».

**Строка 3 — границы сохранены, откат проверен. ЗАКРЫТА С ОДНОЙ НАЗВАННОЙ СЛАБОСТЬЮ.** Границы и
запретные поверхности перемерены и чисты; гигиена зелёная здесь; Unity-компиляция взята с возврата
на байт-идентичном кандидате; откат ВЫВЕДЕН из истории двух коммитов, а не исполнен.

## state_changes

1. `t-scale-7`: закрыть, терминальный статус `done`.
2. `c-exec-g-5a7c-cargo-class-seam-001`: закрыть — наряд исполнен вторым кандидатом.
3. Заведены `i-call-text-never-reached-the-executor-001` (level `direction`, route `maintenance`) и
   `i-cargo-rest-height-setting-now-unread-001` (level `execution`, route `work`).
4. `g-5a7c`, `NOW.bet` и текст ставки НЕ трогать: волну закрывает `review`, не эта нога.
5. Одним `osctl leg close` сохранить этот RESULT в
   `history/2026-08-10-s-work-g-5a7c-scale-7-close-001.md` и добавить log ровно один раз в журналы
   `g-5a7c`, `t-scale-7`, закрытого наряда и
   `i-published-to-main-before-any-direction-close-001`.

## captures

- Половинчатый кандидат `5cbe80ca` был опубликован в `dev`/`main` (merge `60c94c10`, голова
  `35f6c2b7`) ДО того, как Направление нашло дыру. Тот же корень, что уже ведётся карточкой
  `i-published-to-main-before-any-direction-close-001` — строка в её журнал, новой карточки не
  заводится.
- `i-bigger-loot-needs-the-cargo-class-seam-001` теперь РАЗБЛОКИРОВАНА: шов, которого ждал «лут
  побольше», существует. Диспозицию решает `review`, не эта нога.

## decisions_needed

Нет. Ждущий вопрос `d-agents-skills-belong-in-repo-001` назван владельцу в начале ноги и остаётся
отложенным его же решением вести его отдельным чатом.

## play_check

- 1 recite: done — наряд, карточка `t-scale-7` и возврат сведены; задача последняя в ставке
- 2 owner inputs (owner): done — «отличий не заметел», закоммичено на `43648b2d`
- 3 do the work: done — байты, диффы, дерево, история и две проверки прогнаны первой рукой
- 4 self-check: done — каждая строка `done_when` разложена, слабость по откату и Unity-компиляции
  названа, а не спрятана
- 5 close: done — задача закрыта, волна уходит в `review`

## Гейты

- **G5.** `close: light` — законно: несущие строки перемерены или прогнаны здесь, слово владельца
  закоммичено. Первый кандидат этой же линией был ОТБИТ, то есть проверка сработала как проверка.
  Две строки, взятые с возврата (Unity-компиляция) и выведенные (откат), названы прямо.
- **G3.** Аппетит не тронут: доделка выполняла подписанный `done_when`, а не расширяла его.
- **G9.** Карточка узла не редактировалась — только строка в журнал.
- **G2.** Второго предмета, ценности и прокачки не появилось; идеи следующей волны не тронуты.
- **Панель.** Направление панели не объявляет — регенерация пропущена.

## log

класс груза заложен по-настоящему со второго кандидата: числа переехали в именованный класс, общих полей в коде не осталось вовсе, все семь читателей идут через класс и доказаны тестом на подставленном втором классе, а 151/151 и гигиена прогнаны этой ногой на опубликованной голове; первый кандидат нёс одну бирку без чисел, и корнем оказался наряд, физически не дошедший до исполнителя

## next

`review` закрывает ставку `g-5a7c` — СВЕЖИМ ФИЗИЧЕСКИМ ЧАТОМ, не этим. Ему на стол: вердикт волне
против `forecast` и `against`; разбор выреза №4 (дом остался коробкой — его вердикт по лучу был
вердиктом про луч, а не про дом); три улики про структуру
(`i-world-situations-need-extensible-reaction-architecture-001`,
`i-noise-bands-invisible-and-force-mis-scaled-001`, разблокированная
`i-bigger-loot-needs-the-cargo-class-seam-001`); две улики про порядок
(`i-call-text-never-reached-the-executor-001` — маршрут `maintenance`,
`i-published-to-main-before-any-direction-close-001`); его оговорка «очень мало» по ступени
подъёма; две идеи следующей волны (`idea-manual-cargo-control-001`,
`idea-complex-noise-recognition-001`); и выбор следующей волны.
