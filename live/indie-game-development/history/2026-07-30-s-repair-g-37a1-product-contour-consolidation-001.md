# RESULT — s-repair-g-37a1-product-contour-consolidation-001

play: repair
direction: indie-game-development
node: g-37a1
date: 2026-07-30
owner_authority: "«Окей, запускай полную чистку, да, там работай сколько надо… Если что-то типа в конфликтное есть и оно слишком там, условно, бюрократическое, вырежи просто это… посмотри слоты, может можно перезапустить будет команды уже с обновленной версией, чтобы я смотрел, как работает, чтобы эту путаницу убрать.» Затем, о правке Направления: «ну ты жена менять, поменяй, я тебе даю разрешение.»"

## outcome

Продуктовый контур приведён к одному согласованному состоянию, и три разошедшиеся с фактом формулировки в Направлении исправлены. Ни один открытый наряд не удалён, не ретайрен и не сменил статус: общего разрешения на чистку для этого недостаточно (repair §4, removal boundary), и владелец ни один наряд поимённо не называл.

## step 1 — противоречие

Направление описывало продуктовый контур состоянием, которого больше нет: три наряда ссылались на условия, уже выполненные, уже неверные либо изменившиеся под ними. Отдельно: сам продукт нёс три разные версии инженерного контракта в шести рабочих папках одновременно.

## step 2 — реконструкция (артефакты старше логов)

Прочитано первыми руками, продукт `C:/projects/Unity/GasCoopGame` и слоты:

- Расщепление версий: `main`/`dev`/`win-u1` = 33, `win-u3` = 34, `win-u2` = 31.
- Корень расщепления: **два независимых ресинка 31→33, оба 2026-07-30 09:34, на разных ветках** — `6069024b` (линия main) и `498b609d` (`slot/win-u3`). Их `validation.config` совпал побайтово, поэтому переносить требовалось только дельту v34.
- `tools/delivering-scope.ps1` (2026-07-29) объявляет себя в собственной шапке как «ОДНА производная, подключаемая КАЖДЫМ местом обхода, чтобы будущий обход не смог про неё забыть». Подключена была к двум потребителям: `check.ps1`, `coverage-check.ps1`. Пять гейтов обходили все непроархивированные пакеты без развязки: `negative-control-check`, `escape-class-check`, `fix-class-check`, `refuted-register-check`, `review-check`.
- Последствие класса, не гипотеза: `c-exec-v31-venue-authority-binding-fix-001` — единственный корень, дошедший до REPORT — заблокирован дважды на ЧУЖИХ пакетах (`06-report-blocked-r1.json`, `08-report-blocked-r2.json`) при `core_build_green: true`, 1829 пройденных тестах, 117 отрицательных контролях и 44 связывающих случаях. Поле `blocker.forbidden_here` в его же квитанции ЗАПРЕЩАЛО ему чинить то, что его блокировало.
- Шестое место того же класса найдено прогоном, а не чтением: `result-check.ps1` при отсутствии своего отчёта проверял ВСЕ `docs/results/*.md`, требуя DELIVERED в том числе от `c-exec-grid-v1-g01-document-authority-001` — корня ACTIVE на стадии PLAN с 2026-07-21 без единой строки реализации.

## step 3 — что изменено

**Продукт (4 коммита, все локальные, ничего не запушено):**

| коммит | что |
|---|---|
| `7dd09147` | v34 перенесён на main точечным cherry-pick `bdf4a7aa`; работа main (`select-slot`, сканы) не тронута |
| `7245d269` | развязка подключена к пяти оставшимся местам обхода |
| `a79fb27f` | незавершённый корень больше не обязан рапортовать DELIVERED |
| `2b3d5afe` | ветка С изменениями, но без своего отчёта, больше не считается «ветвью без изменений» |

Итог: **контракт 34 во всех шести рабочих папках, несохранённых изменений ноль**. Развязка подключена к восьми местам вместо двух.

**Направление (NOW.md):**

1. Наряд t-body `c-exec-g-37a1-body-plan-publication-unblock-001` — примечание исправлено. Заявленная им работа («узкий законный маршрут публикации, исключающий замороженные красные тесты») **уже сделана контрактом v34**: `-PlanPublication` и `-Deliver` взаимоисключающие (`tools/check.ps1:16-18`), у публикации плана свой stage-local набор (`:81`). Проверено прогоном в WIN-U2 — полный набор тестов не запускается. Настоящий оставшийся блокер другой: X18, замороженное объявление указывает на blob `346f9b41`, в HEAD лежит `bdf46175`, потому что пакет правили НА МЕСТЕ. Законный путь — тот же v34 replacement-close, что у t-venue.
2. Наряд t-venue `c-control-...-replace-close-001` — добавлен результат проверки: слот ушёл вперёд (`9c9a068e`), но **оба процитированных предусловия целы** — blob `f754601e` побайтово тот же на `bdf4a7aa` и на `9c9a068e`, а `bdf4a7aa` остаётся предком HEAD. Перевыпуск не нужен.
3. Наряд t-render — пин продуктового ребёнка исправлен с 31 на **34**: v34 предписывает новым корням пин 34, выданные корни свои пины сохраняют.
4. Заведены два issue: `i-product-contour-consolidated-v34-001` (что именно изменилось в контуре, чтобы никто не переделывал) и `i-grid-v1-g01-dead-root-open-001` (мёртвый корень: теперь безвреден, но остаётся ACTIVE — решение за владельцем).

## step 4 — граница удаления соблюдена

Ни один открытый наряд не удалён, не ретайрен, не переведён в другой статус. Ни одна полоса не распущена. Ни одна подписанная владельцем формулировка не тронута. Владелец разрешил «поменять», но поимённо ни один наряд не называл — а removal boundary требует именно этого.

## evidence

- Продукт: `7dd09147`, `7245d269`, `a79fb27f`, `2b3d5afe`; исходные позиции всех шести веток сохранены в `refs/backup/pre-v34-cleanup/*`.
- Батарея на main: `OK: all gates green`. Все посевные ловушки по-прежнему краснеют (R1–R9 `result-check`, derivation/closing/negative-control). Аварийный режим сохранён: невыводимая область действия проверяет ВСЁ.
- Слоты в своём заявленном состоянии: t-sim 23 красных из 2013 (все `G37*`), t-body 16 из 1863 — оба совпадают с их квитанциями до вмешательства.
- Доказательство, что класс снят: в WIN-U3 гейт отчётов теперь падает на СВОЕЙ причине («ветка меняет репозиторий, но не добавляет своего отчёта — нога не закрыта»), а не на чужом пакете от 21 июля.

## captures

- Претензия «ОДНА производная, подключаемая каждым местом обхода» была записана в шапке файла и не выполнена: подключено 2 из 8. Ничто в контуре не проверяет такие утверждения о самом себе.

## decisions_needed

- Судьба `c-exec-grid-v1-g01-document-authority-001`: закрыть или припарковать. Не срочно — он больше никого не блокирует.
- Пушить ли шесть веток продукта на origin.

## play_check

- Шаг 1 противоречие названо · шаг 2 реконструкция по артефактам и коммитам, не по логам · шаг 3 по одной причине на изменение · шаг 4 removal boundary соблюдена, ничего не удалено · шаг 5 одна строка FRICTION.
- G9 не затронут: CHARTER и TREE не менялись.
- Прогресс не выдуман: ни одна задача не отмечена done, ни один наряд не закрыт.

## next

Полосы свободны. Ближайшее действие по каждой — в её наряде; у t-body и t-venue оно теперь одно и то же: v34 replacement-close.

END_OF_FILE: live/indie-game-development/history/2026-07-30-s-repair-g-37a1-product-contour-consolidation-001.md
