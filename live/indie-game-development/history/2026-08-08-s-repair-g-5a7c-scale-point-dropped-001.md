# RESULT — s-repair-g-5a7c-scale-point-dropped-001

play: repair
direction: indie-game-development · node: g-5a7c · task: t-scale-2
date: 2026-08-08
base_read: OS `6894b097`, дерево `wt/indie-game-development` чисто на входе. Продукт читался
на `ae023ba5`; продуктовые байты, ветки и slot-state этой ногой НЕ изменялись.

## 1. Противоречие, в двух строках

Ставка несёт задачу `t-scale-2`, требующую «единую точку масштаба», и наряд на неё уже ушёл в
слот. Владелец 2026-08-08 разобрал требование и снял его словом **«снимаем»**: пересчёт мира был
однократным, его сделала `t-scale-1`, и он кончился — дальше уровни строятся в настоящих метрах и
смотрятся глазом, а не умножаются на множитель.

## 2. Реконструкция — почему требование вообще появилось

Новейшее-первым по журналам, разбору и байтам:

- **Точка масштаба осмысленна ровно во время ПЕРЕСЧЁТА.** `t-scale-1` взяла мир, где ходок был
  2 м, и умножила его числа на `s = 0.08` (`ae023ba5`, `docs/results/c-exec-g-5a7c-scale-probe-001.md`
  §1). Пока идёт такой перевод, один множитель полезен. Он был одноразовым и завершён.
- **Дальше пересчитывать нечего.** Дом человеческий по замыслу: дверь 0.8 м потому, что двери
  такие. Уровни авторятся в настоящих метрах, налётчик в них 0.16 м. Ни один объект ни на что не
  умножается.
- **Снятый наряд содержал прямо вредное требование:** «оба набора настроек — налётчика и хозяина —
  берут свои числа от него кратными». Это означает «рост хозяина = функция роста налётчика».
  Неверно: хозяин человек, его рост не зависит от роста налётчика, и связывать их — заводить
  провод между несвязанными вещами.
- **Улики, которые предыдущая нога привела ЗА точку, рассыпаются.** Хозяин 2 м против 1.75 м в
  композиции — это вопрос «какого роста человек», отвечаемый глазом, а не дрейф системы. Проход
  2 м — это «сегодняшний дом коробка-заглушка», которую перестраивает критерий 5, а не дрейф.
  Ни то, ни другое точка не чинит. Записано против прошлой ноги, а не сглажено.
- **Корень.** Требование «пропорция живёт одной названной настройкой» пришло из планирующей ноги,
  а не от владельца. Та же строка карточки дословно говорит: «**как это устроено внутри движка,
  карточка не назначает**» — устройство никто не подписывал, оно затвердело по дороге. Класс уже
  известен по памяти направления: изобретение ноги, застывшее как подписанная спека.
- **Проверено первой рукой: `t-scale-2` — лист.** Ни одна из пяти оставшихся задач волны
  (`t-scale-3` … `t-scale-7`) её не упоминает и не зависит от неё: вертикаль у игрока, вертикаль у
  груза, луч, число рук и два шва работают с числами напрямую. Снятие ничего не блокирует.
- **Проверено первой рукой: в слоте нет работы.** `C:/projects/Unity/GasCoopGame_win-u1`,
  `git status --porcelain` пуст, вершина `ae023ba5` — ровно база. Слот числился `CLAIMED`, лиза
  `c-exec-g-5a7c-scale-point-001:PLAN`. Терять при остановке нечего.

## 3. Исправленное состояние

| Изменение | Причина |
|---|---|
| `t-scale-2` уходит из ставки, закрыта | Снята владельцем: «снимаем». Задача-лист, ничего не блокирует |
| `c-exec-g-5a7c-scale-point-001` закрыт как отозванный | Наряд ушёл, но работа в нём не начиналась; владелец назвал остановку, попросив текст для освобождения слота |
| Заведена улика `i-node-criterion-demands-a-scale-setting-we-are-not-building-001` | Строка 1 карточки узла продолжает требовать «одну названную настройку». До правки это живое противоречие, и оно не имеет права раствориться |
| Зарегистрирован `c-map-g-5a7c-scale-criterion-001` (`play: map`) | Правка строки узла — семантическое изменение карточки узла; `repair` его не делает, оно идёт в `map` под G9 |

Ставка `g-5a7c` остаётся `active`. Волна идёт на `t-scale-3` — вертикаль у игрока. `NOW.bet` не
менялся. Карточка узла НЕ редактировалась.

**Граница снятия соблюдена.** Открытый наряд снимается только словами владельца, называющими эту
поверхность: он сказал «снимаем» в прямом ответе на предложение снять `t-scale-2` вместе со
строкой «наряд не отправляй, если уже вставил — останови ту сессию», и отдельно попросил текст,
которым слот освобождается. Обе поверхности названы им, а не выведены.

## 4. Что НЕ сделано этой ногой и почему

- **Строка 1 карточки узла не тронута.** `repair` не правит семантику карточек узла — это `map`
  с подписью владельца (G9). До неё узел честно несёт противоречие, помеченное уликой.
- **Освобождение слота не сделано отсюда.** Лизу снимает та сессия, которая её взяла; текст ей
  передан владельцу в чате. Направление продуктовым состоянием не распоряжается.
- **Ничего не отменено в `t-scale-1`.** Перевод на 0.16 остаётся, он принят его словом.

## captures

- Числа самого налётчика — глаза, радиус, дальность взятия, длина хвата — действительно живут «в
  его ростах». Если рост когда-нибудь изменится, их двигают вместе: 14 чисел в одном файле.
  Системы под это не нужно и заранее её строить не надо.

## decisions_needed

Нет. Решение принято владельцем и процитировано.

## play_check

- 1 назвать противоречие: done — карточка требует устройства, которое владелец разобрал и снял.
- 2 реконструкция: done — по журналам, разбору `W04`/`W113`/`W119`, байтам продукта на `ae023ba5`
  и пяти оставшимся карточкам задач; проверено, что `t-scale-2` — лист, а слот пуст.
- 3 предложенное состояние: done — четыре изменения, по одной причине на каждое.
- 4 подтверждение (owner): done — его слово **«снимаем»**, данное в прямом ответе на предложение
  снять задачу; отдельно **«дай ещё текст для того, чтобы слот освободить»**. Ранее в том же
  разговоре: «я до сих пор не понимаю… разные же будут геометрии», «мы ж будем создавать те самые
  уровни и всё, и смотреть».
- 5 фрикция: done — см. ниже, одна строка, OS здесь не чинится.

## FRICTION

Требование к устройству («пропорция живёт одной названной настройкой») попало в подписанную
карточку узла из планирующей ноги, при том что соседнее предложение той же строки прямо
отказывается назначать устройство. Строка прожила от `map` через `converge`, `converge-arch`, две
проверки и `shape` до наряда исполнителю, и остановил её владелец вопросом «зачем», а не гейт.
Это второй случай того же класса в этом направлении.

## Гейты

- **G7.** Решение продукта принял владелец; нога его не выводила и не смягчала. Свой прошлый
  довод нога сняла как несостоятельный, а не защищала.
- **G9.** Карточка узла не изменена; правка её строки вынесена в отдельную ногу `map` под подпись.

## log

t-scale-2 снята его словом «снимаем» — точка масштаба была изобретением ноги, а не требованием; наряд отозван, волна идёт на вертикаль

## next

`CALL c-map-g-5a7c-scale-criterion-001` — принести владельцу точный текст строки 1 карточки
`g-5a7c` на подпись. Бриф: `work/c-map-g-5a7c-scale-criterion-001-call.md`.

END_OF_FILE: live/indie-game-development/history/2026-08-08-s-repair-g-5a7c-scale-point-dropped-001.md
