# RESULT — s-work-g-5a7c-the-shelf-is-buttons-not-files-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-the-bench-opens-quiet-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — красный тест поймал ошибку в замысле наряда, а не помешал работе: правка теста запрещена, исправлен замысел, и правило про аренду сработало в первый же выход

close: light — because всё перевыведено направлением первой рукой: тест прочитан целиком, зависимости от имени файла найдены полным поиском по `Assets` и `tests`, и способность окна жить без загруженного сценария прочитана в его же коде. Ничего не закрывается как достигнутое.

owner_approved: |
  Задача заведена его словом 2026-09-01 «если да, то давай уберем». **Предмет не изменился** —
  изменилась только форма исполнения после красного теста. Отдельного слова не требуется.

outcome: |
  **КРАСНЫЙ ТЕСТ ПОЙМАЛ ОШИБКУ В ЗАМЫСЛЕ НАРЯДА, И ПРАВКА ТЕСТА ЗАПРЕЩЕНА.**
  `EveryReadySituationParsesAndIsNamedInWords` требует от **каждого файла на полке**: разбирается,
  имеет заголовок, заголовок не равен имени файла, и **что-то делает**.

  **Правило верное, и оно про полку, а не про расходный полигон.** Полка — это кнопки.
  **Кнопка, которая ничего не делает, врёт тому, кто её нажал.**

  **А НАРЯД ВЕЛЕЛ СДЕЛАТЬ РОВНО ТАКУЮ КНОПКУ.** Направление думало про «что загружено при открытии»
  и не заметило, что **тем же файлом определяется «что лежит на полке»**. Две разные вещи, смешанные
  в одну. Ошибка наряда, а не исполнителя и не теста.

  **ПРАВИЛЬНАЯ ФОРМА ОКАЗАЛАСЬ ПРОЩЕ НЕПРАВИЛЬНОЙ: не «загружен пустой сценарий», а НЕ ЗАГРУЖЕН
  НИКАКОЙ.** И обе проверки говорят, что это дёшево:

  - **имя `default-scenario` не ждёт ни один тест.** Полный поиск по `Assets` и `tests` — четыре
    совпадения, все в самом окне, и `:1094` берёт из константы только **папку**;
  - **окно уже умеет жить без сценария.** `DrawScenario()` рисует пустое поле и кнопку `Load`, а
    вывод расписания стоит под `if (_scenario != null && _scenarioRun != null)`. **Состояние
    «ничего не загружено» предусмотрено заранее.**

  **ЗАПРЕТ «КОДА НЕ ТРОГАТЬ» СНЯТ, ПОТОМУ ЧТО ОН СТОЯЛ НА ОШИБОЧНОЙ ПОСЫЛКЕ.** Направление обещало,
  что правка обойдётся текстовыми файлами. Обещание было неверным; две строки загрузки разрешены
  прямо, и порог оставлен: больше — СТОП.

  **ПРАВИЛО ПРО АРЕНДУ, ЗАВЕДЁННОЕ СЕГОДНЯ, СРАБОТАЛО В ПЕРВЫЙ ЖЕ ВЫХОД.** Исполнитель удержал слот
  под незавершённую ногу **и назвал причину и подо что именно** — ровно то, что требует
  `knowledge/a-lease-ends-with-the-work-that-took-it.md`. **За день это третья попытка починить
  аренду и первая, которая не стоила остановки.**

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** на `slot/win-u2` = `b4b010c1`:

  - `tests/…/HouseholderObservationToolTests.cs:357-391` — тест перебирает **все** файлы
    `HouseholderObservation.*.json` в фикстурах и проверяет четыре вещи, последняя —
    `Assert.That(scenario.Events, Is.Not.Empty, $"{slug} would run and do nothing.")`;
  - `grep "default-scenario\|DefaultScenarioAssetPath"` по `Assets` и `tests` → **четыре
    совпадения, все в `HouseholderObservationWindow.cs`** (`:28`, `:29`, `:382`, `:1094`);
    `:1094` — `Path.GetDirectoryName(...)`, то есть только папка;
  - `HouseholderObservationWindow.cs:381-383` — автозагрузка при открытии, **две строки**;
  - `DrawScenario()` — поле объекта, кнопки `Load` и `Restart schedule`, и вывод расписания под
    `if (_scenario != null && _scenarioRun != null)`.

  Из возврата ноги, как улика: пустой `default-scenario.json` и новый `three-noises.json` созданы;
  код не менялся; `746 / 1 / 747`; обычный Play без исключений, хозяин перемещался; полка по
  импортированным файлам должна давать четыре кнопки; аренда удержана с названной причиной.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-the-bench-opens-quiet-1-002` → слот **WIN-U2**;
  - `work/2026-09-01-answer-the-window-opens-with-nothing-loaded.md`.

  ЗАКРЫТО КАК ОТОЗВАННОЕ (1 наряд):
  - `c-exec-g-5a7c-the-bench-opens-quiet-1-001` → `dropped`: замысел был неверен, тест это поймал.

  ОБНОВЛЕНО (1 задача):
  - `t-the-bench-opens-quiet-1` блок `done_when` → форма исправлена: окно открывается, **не
    загрузив никакого** сценария; пустой файл не создаётся; правка теста запрещена; две строки кода
    разрешены. **Предмет задачи не менялся.**

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-a-fact-knows-who-made-it-1` остаётся `active` в WIN-U4;
  - `t-everything-lands-1` остаётся `open`.

captures: |
  - **НАПРАВЛЕНИЕ ПРОВЕРИЛО РАЗБОР И НЕ ПРОВЕРИЛО ТЕСТЫ.** Перед выдачей наряда искали запрет на
    пустой список **в парсере** и не нашли — и на этом остановились. Запрет стоял **в тесте про
    полку**. **Правило-кандидат: прежде чем обещать «это разрешено», искать запрет и в проверках
    тоже, а не только в том коде, который его исполняет.** Это тот же промах, что и утром: одно
    место проверено, вывод обобщён.
  - **ТЕСТ ОКАЗАЛСЯ УМНЕЕ НАРЯДА, И ЭТО РЕДКОСТЬ, СТОЯЩАЯ ЗАПИСИ.** Сегодняшняя ревизия дня уже
    находила тесты, стоящие на расходном содержимом. **Этот — не из них:** он про инструмент, а не
    про полигон, и его правило «кнопка обязана что-то делать» **пережило замысел, который его
    нарушал.**
  - **ПРАВИЛЬНАЯ ФОРМА ОКАЗАЛАСЬ ПРОЩЕ НЕПРАВИЛЬНОЙ.** «Не загружать ничего» дешевле, чем
    «загружать пустое», и не требует лишнего файла. **Когда проверка отвергает замысел, стоит
    искать форму проще, а не обходить проверку.**
  - **ПРАВИЛО ПРО АРЕНДУ ОТРАБОТАЛО С ПЕРВОГО РАЗА.** Заведено в knowledge несколько часов назад
    после трёх остановок за день; в первый же выход исполнитель удержал слот и **назвал причину и
    подо что**. Правило, положенное туда, где его читают, работает сразу.

decisions_needed: |
  Ни одного владельческого.

play_check: |
  1. OPEN — СТОП ноги: красный тест запрещает пустое расписание.
  2. WORK — тест прочитан целиком и признан верным; правка теста запрещена; зависимости от имени
     файла проверены полным поиском; способность окна жить без сценария прочитана в его коде;
     собственный ошибочный запрет «кода не трогать» снят с объяснением.
  3. (owner) — не запрашивался: предмет не менялся, менялась форма исполнения.
  4. CLOSE — RESULT, применение, коммит; переизданный наряд уходит владельцу.

log: |
  красный тест поймал ошибку в замысле наряда, а не помешал работе: он требует, чтобы каждая кнопка
  на полке что-то делала, а наряд велел сделать кнопку-пустышку, смешав «что загружено при открытии»
  с «что лежит на полке»; правка теста запрещена, форма исправлена и оказалась проще — окно
  открывается, не загрузив никакого сценария, что уже предусмотрено его же кодом; правило про аренду,
  заведённое сегодня после трёх остановок, сработало в первый же выход

next: |
  Переизданный наряд `c-exec-g-5a7c-the-bench-opens-quiet-1-002` уходит владельцу для **WIN-U2**.

  Волна: голова закрыта, ходьба мертва, стенд делается тихим, замирание чинится по найденной
  причине в U4, публикация ждёт только замирания.
