# RESULT — s-work-g-5a7c-cargo-sleep-paste-001 (play: work)

## outcome

**Слот вернул корректный STOP, и он поймал ошибку Направления, а не свою.**

Выдача дала исполнителю id наряда и слот. Исполнитель живёт в продуктовом репозитории и карточек
Направления не видит — id для него пустой звук. Он честно остановился ДО первой записи, слот не
захватывал, и запросил ровно то, чего не хватало.

**Нарушено собственное правило пятой волны, записанное в её же evaluator:** «наряд не существует,
пока исполнитель не расписался: готовая паста с ПОЛНЫМ текстом и первая строка возврата — расписка с
числом полученных строк done_when». Правило было под рукой и не применено.

**Вторая половина ошибки — форма.** Контракт продукта ждёт наряд с ПОТОЛКОМ в три строки `done_when`,
в которые укладывается всё остальное. Наряд нёс тринадцать. Тринадцать свёрнуты в три без потери ни
одной: мир и каскад; шум плюс новый носитель; границы и откат.

## evidence

- STOP исполнителя: `WIN-U3`, `slot/win-u3`, `HEAD f45b08d…`, чисто, `AVAILABLE`, слот не захвачен.
  Претензия дословно: «В разрешённой Grid-authority нет G-5A7C, cargo/sleep или указанного change id,
  поэтому нельзя вывести поведение и acceptance из одного slug». Претензия верна.
- Правило волны — блок `## evaluator` карточки `bet-g-5a7c-wave-5`, пункт 3.
- Запрошенное поимённо и теперь выданное: имя стадии лизы (`…:build`), три `done_when`, HOME
  (Direction), нужен ли Unity (да, EditMode; головная часть — `dotnet` без слота), нужен ли
  owner-eye (нет ни на одной строке), режим (ПРОБА).
- Отдельно вписано то, чего он не спрашивал, но что снимает его же возражение: **Grid не трогается
  вовсе**, а авторитет ноги — сам CALL плюс `AGENTS.md`, как и говорит раздел про repo-local
  authority.

## state_changes

- `work/2026-08-17-paste-cargo-sleep-1.md` — самодостаточная паста: зачем нога, что уже измерено и не
  переоткрывать, три строки `done_when` со свёрнутыми деталями, границы, пять мин, форма возврата.

## captures

**Класс ошибки стоит назвать, потому что он повторяемый.** Направление три раза подряд дополняло
наряд, следило за слотами и пересечениями — и отдало исполнителю ссылку вместо текста. Ссылка
работает только внутри того репозитория, где живёт состояние; через границу репозиториев ходит
только текст. Правило волны про «полную пасту» существует ровно из-за этого и было написано раньше,
чем эта ошибка.

**Слот сработал ровно как задумано:** остановился до первой записи, ничего не испортил, назвал
недостающее списком. Это не потеря времени, а сработавшая проверка.

## decisions_needed

Ничего. Владельцу — вставить пасту.

## play_check

Ошибка признана своей и названа классом, а не случайностью. Форма наряда приведена к контракту
продукта, а не к удобству Направления. Ни одна из тринадцати строк приёмки не потеряна при свёртке в
три.

## log

слот вернул корректный STOP и поймал ошибку Направления: исполнителю ушла ссылка вместо текста, а через границу репозиториев ходит только текст

## next

Владелец вставляет пасту в `Gas U3`. Наряд закрыт для дополнений.
