# RESULT — s-shape-g-1d84-tunnel-removal-001

play: shape (последовательная перенарезка активной ставки вслед за правкой карточки; ставка не
открывается заново)
direction: indie-game-development · node: g-1d84
date: 2026-08-06
base_read: OS `2148cb00` — перечитан свежим после ДВУХ коммитов параллельной сессии; правки той
сессии в `work/` и её четыре новые записи не тронуты и не закоммичены этой ногой.

## outcome

`NOW` приведён в соответствие с карточкой, которую владелец подписал словом «да A». Тоннель ушёл из
ставки целиком: задача снята, корень отозван, вырез переписан, полоса опустела. Выход из дома,
ставший подписанной строкой карточки, внесён в задачу и в ТЕКСТ наряда — то, чего `map` сделать не
могла. **Ставка только сократилась: 17 задач → 16, закрыто 7, остаётся 9.** Ни одна строка не
добавлена, аппетит и kill-порог не трогались.

## Что снято, и куда оно уехало

**Задача `t-house-5`** удалена из `NOW.tasks`. Её дословный текст — goal и все четыре предложения
`done_when` — сохранён в `i-digging-own-bet-authored-fresh-001`, чтобы будущая карточка не сочиняла
её заново.

**Корень `c-exec-g-1d84-tunnel-section-001`** удалён из `open_calls` вместе со всеми накопленными за
день полями. **Файл наряда `work/c-exec-g-1d84-tunnel-section-001-call.md` НЕ удаляется** — его
раздел `## seam` (шесть строк формы) есть готовая улика для будущей ставки, и `work/` для того и
существует.

**Развилка `d-tunnel-section-how-it-opens-001`** удалена из `decisions`: вопрос «чем открывается
участок» умер вместе с участком. Все шесть вопросов и семь вариантов открытия с их сетевой ценой
сохранены в той же записи о будущей ставке.

**Полоса `игрок`** НЕ удалена и стоит пустой. Это не недосмотр: KERNEL §3 — «только `review`
распускает полосы закрываемой ставки; ни один другой плей её не создаёт и не убирает». Пустая полоса
WIP не тратит (занято три корня из четырёх при `track_wip_limit: 4`), новых задач в неё класть
нельзя, и она исчезнет при `review` g-1d84. Прежняя метка сохранена в поле `emptied`.

## Что переписано, а не удалено

**Вырез 4** был «больше одного тоннельного участка — строится один участок»; стал «ТОННЕЛЬ ЦЕЛИКОМ»
и вместе с ним любая вертикаль. Это самый большой вырез волны, и основанием записана не экономия, а
его собственный довод: вертикаль будет обязательно, а плоский тоннель он будет переделывать глазами.

**Рискованное допущение 4** называло спусковым крючком открытие тоннельного участка. Участка нет, но
допущение НЕ умерло вместе с ним: правило «новый ВИД сетевой посылки — СТОП и вопрос владельцу»
живо для оставшихся задач. Переписано с измеренным основанием: во всём проекте один сетевой объект и
ровно две посылки, ни одного `SyncVar`/`SyncList`, прецедент расширения — `HouseholderSnapshot`
третьим параметром того же `ObserversRpc`.

**Пересказ цели ставки** приведён к карточке (хвост про тоннели убран). **Линза 3** пересчитана на
16 задач. **Линза 7** («проще или сложнее») усилена честно: ушла не только задача, но и целый новый
слой — состояние у названной вещи, переключаемая стена, бит в снимке, — то есть волна стала и
короче, и мельче по устройству. **Аппетит** получил строку о сокращении с прямой пометкой, что G3
не задет: из ставки только вычли.

## Что добавлено — ровно одно, и оно из подписанной строки

Критерий 2 карточки теперь несёт «в доме для этого руками сделан выход наружу». Отсюда:

- **`t-house-6.done_when`** получила середину про проём и пол; `goal` стала «выход из дома, точка
  доставки и сигнал удачи». Задача перепроверена как по-прежнему не больше полудня: прибавка —
  около двадцати минут ручной работы.
- **Текст наряда `c-exec-g-1d84-delivery-point-001`** переписан в пяти местах: `goal`, `context`
  (измеренная причина плюс СПОСОБ), `boundaries`, `done_when` (три строки → четыре), `return`.

**Способ положен в наряд нарочно, чтобы исполнитель не изобретал:** проём делается так же, как уже
сделаны два дверных — кусок наружной стены режется на ДВА объекта со своими коллайдерами и своими
`HouseWall`, щель между ними остаётся пустой. Тогда он открыт и для ходока (его выпихивает список
прямоугольников `CarryStand`, собираемый один раз в `Awake` из `HouseWall`), и для груза (настоящий
коллайдер) — одним движением, без синхронизации двух списков руками.

**`boundaries` наряда прямо запрещают** дверь, заслонку, переключение, состояние, тоннель, пандус и
любую вертикаль: это прямые исключения карточки, и соблазн «раз уж режем стену» закрыт заранее.

## Измерения, на которых стоит эта нога

Всё ниже перемерено первой рукой на `8ffe5a5d` в этом же разговоре и здесь только приложено:
наружные стены `Outer North/South` (z = ±7.25, 14.5 × 0.5) и `Outer West/East` (x = ±7.25, 0.5 × 14)
без единого проёма; пол — встроенный Plane при `scale 1.6`, то есть 16 × 16 м (−8…8) при наружной
грани стен ±7.5, откуда полметра пола за стеной; `CarryStand.Awake` собирает прямоугольники из
`HouseWall` один раз; груз физический с настоящим коллайдером.

## state_changes

1. `NOW.tasks`: `t-house-5` удалена (17 → 16). Порядковые номера остальных НЕ перенумерованы —
   в ряду остаётся дырка на месте 12, и это дешевле, чем трогать шестнадцать строк.
2. `NOW.tasks` `t-house-6`: `goal` и `done_when` расширены выходом; + `exit_added` с измеренной
   причиной и размером прибавки.
3. `NOW.open_calls`: `c-exec-g-1d84-tunnel-section-001` удалён целиком (3 корня остаётся).
4. `NOW.open_calls` `c-exec-g-1d84-delivery-point-001`: + `exit_written_into_the_call_2026_08_06`.
5. `NOW.decisions`: `d-tunnel-section-how-it-opens-001` удалена.
6. `NOW.tracks` `игрок`: `tasks: []`, метка заменена, + `emptied` с цитатой правила KERNEL §3.
7. `NOW.bet`: `goal` приведена к карточке; `appetite` + строка о сокращении; `cuts` 4 переписан;
   `riskiest_assumptions` 4 переписан; `lens_verdicts` 3 и 7 пересчитаны.
8. `NOW.issues` `i-digging-own-bet-authored-fresh-001`: + `tunnel_left_the_wave_2026_08_06` (снятая
   задача и снятая развилка дословно, указатели на два файла `## seam` и на запись о вертикали);
   `review_when` дополнен — запись держит теперь и тоннель, а не только копание.
9. `work/c-exec-g-1d84-delivery-point-001-call.md`: пять правок выше.
10. `NOW.md` шапка; `LOG.md` — одна строка; `history/` — этот файл.

Не тронуто: `TREE.md` (карточку правил `map`, здесь она только исполняется), `CHARTER.md`,
`knowledge/`, `track_wip_limit`, `kill_by`, `evaluator`, `approach`, `owner_word`, `status` ставки,
файл наряда на тоннель, два чужих корня, правки параллельной сессии в `work/`, продуктовый
репозиторий.

## captures

- В ряду `order` осталась дырка на месте 12. Перенумерация отложена сознательно: она трогает
  шестнадцать строк ради косметики. Если ряд станет дырявым дальше — сводить одним заходом.
- Файл `work/c-exec-g-1d84-tunnel-section-001-call.md` теперь наряд без задачи и без корня. Он
  оставлен уликой; если такие файлы начнут копиться, им нужен признак «улика, не фронтир».

## decisions_needed

Нет. Всё, что решал владелец, решено его «да A».

## play_check

- **Шаг 1 recite** — сделан: карточка перечитана после правки `map`, состояние — свежим после двух
  коммитов параллельной сессии.
- **Шаги 2, 3, 8 (аппетит, подходы, kill-критерии)** — НЕ ПРИМЕНЯЛИСЬ И НЕ ТРОГАЛИСЬ: ставка не
  открывается заново, подход не пересматривается, из неё только вычитают. G3 соблюдён буквально —
  аппетит не расширен ни на строку.
- **Шаг 4 scope hammer** — исполнен в обратную сторону: вырез не искали, вырез ПРИШЁЛ от владельца,
  и он записан как самый большой в волне, с его собственным доводом, а не с придуманным.
- **Шаг 5 lens sweep** — прогнан по всем семи линзам; две пересчитаны (3 и 7), пять не изменились и
  проверены поимённо. Молчаливого пропуска нет.
- **Шаг 6 riskiest assumption** — допущение 4 не удалено вместе с его прежним крючком, а переписано
  с измеренным основанием.
- **Шаг 7 tasks** — 16 задач, каждая по-прежнему ≤ полудня; изменившаяся `t-house-6` перепроверена
  отдельно. Полосы НЕ создавались и НЕ удалялись (KERNEL §3).
- **Шаг 9 close** — один экран изменений показан владельцу в ответе; RESULT записан.
- **G1** — активная ставка одна, занято три корня из четырёх. **G2** — волна только сузилась.
  **G3** — не расширена. **G4** — `kill_by` и `next_if_*` не трогались. **G6** — вырез настоящий,
  вердикт по каждой линзе на месте, задача на риск (`t-player-1`) закрыта и не переписывалась.
  **G7** — ни одного решения за владельца: перенос стоит на «да A», способ проёма — инженерная
  форма, а не его выбор.
- `archive/**` не открывался; продуктовый репозиторий не трогался.

## log

g-1d84: `NOW` приведён в соответствие с карточкой, подписанной его «да A», — ВТОРОЙ ШАГ переноса
тоннеля, исполнительный. СНЯТО: задача `t-house-5` (17 задач → 16, закрыто 7, остаётся 9), корень
`c-exec-g-1d84-tunnel-section-001` из `open_calls` целиком, развилка `d-tunnel-section-how-it-opens-001`
из `decisions`. НИЧЕГО НЕ ПОТЕРЯНО: дословный текст снятой задачи, все шесть вопросов развилки и семь
вариантов открытия с их сетевой ценой сохранены в `i-digging-own-bet-authored-fresh-001`, а файлы
обоих нарядов с разделами `## seam` НЕ удалены и оставлены уликой будущей ставки. ПЕРЕПИСАНО, А НЕ
УДАЛЕНО: вырез 4 стал «ТОННЕЛЬ ЦЕЛИКОМ» и это самый большой вырез волны, основанием записан его
собственный довод (вертикаль будет обязательно, плоский тоннель он переделает глазами); рискованное
допущение 4 не умерло вместе со своим крючком — правило «новый ВИД сетевой посылки — СТОП» живо для
оставшихся задач и переписано с измеренным основанием (один сетевой объект, две посылки, ноль
`SyncVar`); пересказ цели ставки приведён к карточке; линзы 3 и 7 пересчитаны, причём 7 усилена
честно — ушла не только задача, но и целый новый слой, то есть волна стала и короче, и мельче.
ПОЛОСА `игрок` ОСТАВЛЕНА ПУСТОЙ НАРОЧНО: KERNEL §3 разрешает распускать полосы только `review`
закрываемой ставки, пустая полоса WIP не тратит и исчезнет при закрытии — записано, чтобы следующая
нога не «прибралась». ДОБАВЛЕНО РОВНО ОДНО, И ОНО ИЗ ПОДПИСАННОЙ СТРОКИ: выход из дома. `t-house-6`
получила его в `goal` и `done_when` и перепроверена как по-прежнему ≤ полудня (прибавка около
двадцати минут); текст наряда `c-exec-g-1d84-delivery-point-001` переписан в пяти местах, `done_when`
стало четыре строки вместо трёх, а в `context` положен СПОСОБ, чтобы исполнитель не изобретал —
проём режется так же, как два уже существующих дверных, одним разрезом стены на два объекта со своими
коллайдерами и `HouseWall`, и тогда он открыт и для ходока, и для груза сразу; `boundaries` прямо
запрещают дверь, заслонку, переключение, состояние, тоннель, пандус и любую вертикаль. Аппетит,
kill-порог, WIP, подход и evaluator не трогались; G3 соблюдён буквально — из ставки только вычли.
Правки параллельной сессии в `work/` не тронуты и не закоммичены.

## next

Волна готова к работе в урезанном виде. На столе владельца одно: назвать слот (`WIN-U2` или
`WIN-U4`) для двух готовых сборочных корней — доставка с выходом и шум от контакта груза. Тоннель и
вертикаль ждут своей ставки; её карточку пишет `map`, когда он скажет.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-shape-g-1d84-tunnel-removal-001.md
