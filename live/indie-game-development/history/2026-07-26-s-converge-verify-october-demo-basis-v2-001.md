# RESULT s-converge-verify-october-demo-basis-v2-001

direction: indie-game-development
play: converge-verify
node: g-12fd
call: c-converge-verify-october-demo-basis-v2-001
verify_target: specification
date: 2026-07-26

## outcome

FAIL. The exact owner-approved artifact `work/october-demo-basis-v2.md`
(identity `october-demo-basis-v2`, owner words «да» on its exact eight-item MUST
list, 2026-07-26) does not survive the independent attack. Ten findings are
named and none is filled, repaired or decided here. They return to the
owner-authority `work` contour as
`work/c-work-october-demo-basis-v3-revision-001-call.md`. Verification reruns
against v3.

No owner decision was reversed, reinterpreted or re-litigated. Procedural
generation stays IN as MUST 5. The two deferred hall mechanics stay deferred.
The terminology stays «вещество». The eight MUST items are neither reordered nor
reduced by this leg.

Two roots produce eight of the ten findings:

1. **MUST 5 entered with a rationale its own cited evidence does not support.**
   The recorded PGG verdict is misattributed (wrong tool, and a conditional
   acceptance rendered unconditional), and the claim that host-side assembly
   plus a shipped layout removes the determinism requirement has no resolved
   support at all. → F1, F2, F8, and part of F6.
2. **The two deferrals emptied mechanics that three sections still lean on.**
   §2's danger stage, §4's causal-cooperation supports, falsifier 5 and §5's
   public-claim ceiling all still stand on hall behaviour that moved to «потом
   рассмотреть» or on undecided open questions 3, 7 and 8. → F3, F4, F7, F9.

The remaining two are coverage gaps against `g-12fd` done_when: an atom with no
content in the artifact (F5) and an atom covered only transitively (F10).

The leg was NOT blocked: no node-class checklist existed in `knowledge/`, so one
was authored from first principles before the attack —
`work/oracle-demo-boundary-specification-v1.md`, proposed for promotion by
review or pulse. It has no authority until promoted.

## evidence

SESSION INDEPENDENCE. Fresh session, neither the authoring chat
(`s-work-october-demo-basis-authoring-001`) nor the revision chat
(`s-work-october-demo-basis-v2-revision-001`). Neither leg's reasoning was read:
of the revision receipt only its `## evidence` block (the owner's verbatim words
and the approval binding) and its quoted returning CALL were read, because the
CALL names that quote as the second binding done_when set. Its
`## done_when reconciliation`, `## deviations` and `## handed to verification`
sections were deliberately NOT read — inheriting the author's own clause map
would defeat the play.

FRESH STATE. The two Demo Basis legs had been committed on local `main` in the
primary folder (`3f135b9b`, `c982c79a`) while this worktree sat on
`wt/indie-game-development`, four `os/` maintenance commits ahead and two live
commits behind. `main` was merged into this worktree before any write, so the
attack ran against fresh current state, not against the stale worktree copy.
`os/plays/converge-verify.md` is byte-identical on both branches; `os/KERNEL.md`
differs only in the lane wording, which does not touch this play.

SOURCES READ (the CALL's closed authority list, nothing else): `CHARTER.md`;
`TREE.md` (`g-12fd`); `NOW.md`; `knowledge/canon-clean-authority-reset.md`;
`knowledge/strategy-reset-boundary.md`; `work/concept-frame-v1.md`;
`work/pgg-analysis-2026-07-10.md`; `work/october-demo-basis-v2.md`; this CALL;
plus two bounded reads the CALL's own done_when made necessary: the v1 blob in
commit `3f135b9b` (needed to verify "carrying forward all of v1 unchanged EXCEPT
the deltas" — the author's own reconciliation cannot substitute for the diff),
and the withdrawn v1 CALL plus the v1 tombstone now live in `work/`. Both are
live-repo history, not `archive/**`.

NO LEGACY READ. `archive/**` was not opened, and neither was the frozen canon
repository. Where a claim could only be discharged from the archive (F1's PGG
spike close receipt) the claim is recorded as untraceable inside current
authority, which is a FAIL, not a reason to open the archive.

INDEPENDENT DIFF v1 → v2. The carry-forward instruction was checked directly.
Result: every delta the revision done_when ordered is present, and no undeclared
semantic edit was found outside them. Two observations, neither a defect: MUST 5's
parenthetical was rendered from the CALL's English into Russian with the meaning
preserved; and the instruction "CUT loses «процедурная генерация»" was vacuous —
v1's CUT list never contained it, only v1's open question 4 claimed it did, and
that claim disappeared with the question (D3). The falsifier block is byte-for-
byte unchanged from v1, which is what makes F4 possible.

## atomic clause map

Set A — `TREE.md` `g-12fd` done_when, split atomically.

| Atom | Requirement | Artifact section | Verdict |
|---|---|---|---|
| A1.1 | минимальное обещание игроку | §1 | PASS text; F9 on the twenty-minute quantity |
| A1.2 | путь начало → опасность или задача → совместное действие → понятный результат | §2 | PASS via the «или задача» disjunct only; danger content FAILs (F9, F3) |
| A1.3 | один читаемый момент | §3 | PASS |
| A1.4 | проверка причинного сотрудничества | §4 | FAIL (F3) |
| A1.5 | предел публичных обещаний | §5 | FAIL (F7) |
| A2.1 | обязательное явно | §6 MUST | PASS |
| A2.2 | желательное явно | §6 SHOULD | PASS |
| A2.3 | вырезанное явно | §6 CUT | PASS |
| A2.4 | исключения явны | heading «Исключения и признаки опровержения» | FAIL — uncovered (F5) |
| A2.5 | признаки опровержения явны | 6 falsifiers | PASS as a list; FAIL on effectiveness (F4) |
| A2.6 | старый Canon и прежние возможности не получают автоматического места | «На чём это стоит» guard sentence | FAIL — MUST 5 grants Dungeon Architect a role from a misattributed prior approval (F1) |
| A3.1 | Основа связана со свежими release-reality receipts | none | FAIL — transitive only (F10) |
| A3.2 | новая версия отменяет зависимые доказательства до повторной проверки | «Что отменяет зависимые доказательства» | PASS on the rule; FAIL on completeness for MUST 5 (F6) |

Set B — the revision CALL `c-work-october-demo-basis-v2-revision-001` done_when,
quoted in `history/2026-07-26-s-work-october-demo-basis-v2-revision-001.md`.

| Atom | Verdict |
|---|---|
| v2 exists as the single live Basis, identity `october-demo-basis-v2` | PASS |
| carries v1 forward unchanged except the ordered deltas | PASS (independent diff) |
| keeps v1's unverified-status line, falsifiers, invalidation set, «На чём это стоит», open questions | PASS — and the unchanged falsifier block is what F4 lands on |
| MUST becomes exactly the eight listed items | PASS; the English→Russian rendering of MUST 5's parenthetical preserves the meaning, and it is that meaning that FAILs under F1 |
| both removed items land in «потом рассмотреть» with the recorded reason, not in CUT | PASS |
| CUT loses «процедурная генерация», everything else stands | PASS, vacuously (D3) |
| terminology «вещество» throughout | PASS |
| open questions become the itemized set; question 4 drops as RESOLVED | PASS on the itemization; the CALL's own count says "eight" and its itemization yields seven (D1) |
| v1 becomes a short tombstone naming v2, «принимаю» and commit `3f135b9b` | PASS |
| v1 verification CALL withdrawn with a recorded disposition | PASS |
| one fresh v2 verification CALL carrying v1's three caveats | PASS |
| `NOW.md` open_calls holds exactly that CALL; `i-procgen-determinism` and `i-demo-scope-cap` updated | PASS as applied; `i-procgen-determinism` asserted as settled what F1/F2 refute, and is corrected by this leg |
| `bet: null`, `tasks: []`, no track, no numeric chance | PASS |
| one LOG line, full RESULT in `history/`, `END_OF_FILE:` trailers, one commit | PASS (`c982c79a`) |
| `g-12fd` parked, TREE unchanged | PASS |

## smuggling audit

Weight-bearing non-owner assertions, each with its named support and verdict.
Owner content decisions are supported by his words and are not attacked here.

| Claim | Named support | Verdict |
|---|---|---|
| «Dungeon Architect — editor-time module authoring, именно это одобрил записанный вердикт PGG» (MUST 5) | `work/pgg-analysis-2026-07-10.md` | FAIL. That file gives editor-time authoring to PGG, «принять после спайка», and gives DA the runtime-assembler role with owner-hand-authored module shells. The artifact's own evidence section states it correctly, so v2 contradicts itself. |
| «редакторский авторинг модулей подтверждён кодом и **принят**» («На чём это стоит») | same file | FAIL. The verdict says «принять после спайка». The spike close receipt is archive-quarantined, so the condition is untraceable inside current authority. |
| «Сборку делает хост и рассылает готовую раскладку, а не сид, поэтому детерминизм генератора не требуется» (MUST 5) | none named | FAIL. Untraceable, and contradicted by the cited pipeline «DA собирает уровень в рантайме по сиду». `NOW.md` itself lists layout replication as open. |
| «Ширина проёмов ограничена и связана с размерами шара» (MUST 5) | owner's approved list | PASS as owner content; it is a generation parameter, which is why F6 must name it. |
| «Вердикт не проверялся на Unity 6.3 — красный флаг №1» («На чём это стоит») | same file, red flag 1 | PASS. Correctly and honestly carried. |
| «оно мешает нести, перекрывает маршрут и наказывает за небрежность» (§2) | MUST 2 fixes only «тянет вниз / рвётся вверх ... действует и на шар, и на игрока» | FAIL. Route-blocking and punishment need open question 8's answer. |
| «Двадцать минут» (§1) | no MUST item, no falsifier | FAIL. Disclosed, but asserted as a quantity and untestable. |
| «пока несёшь — ... не глушишь источник, не вытаскиваешь упавшего» (§4) | deferred mechanic; open question 3 | FAIL. |
| «лопнувший шар меняет воздух и **маршрут** для всех сразу» (§4) | MUST 3 covers bursting; route change needs question 8 | PARTIAL FAIL, and it is not a two-player requirement at all. |
| «причинная нагрузка держится на трёх оставшихся пунктах» (§4) | the three bullets | FAIL. Only bullet 2 requires two players simultaneously and is fully MUST-backed. |
| «вещество ... которое реагирует на здание» (§5 «Можно») | no MUST item after the deferrals | FAIL. §5's own rule forbids claiming it. |
| «абсурдный мир при настоящей опасности» (§5 «Можно») | open questions 3 and 8 | FAIL, same class. |
| «Задели косяк. Хлопок.» (§3) | MUST 3 «лопнуть»; MUST 4 «отпустил — уронил» | WEAK. Contact-triggered bursting is nowhere established; the moment's payoff depends on it. Recorded inside F9's class, not raised as a separate finding. |
| «Основа считается опровергнутой, если ... 5. Заглушение источника не меняет зал физически» | subject deferred | FAIL. Vacuous or dead (F4). |
| The evidence tuple and invalidation list | artifact text | FAIL for completeness after MUST 5 (F6). |
| «Ни один из этих файлов не получает власти ... Каждое положение выше стоит на собственном доводе, а не на прежнем статусе принятия» | artifact guard sentence | The guard is correct and well written; MUST 5 violates it (F1). |

Legacy-status-only support: none found outside F1. The bounded canon read is
carried as evidence with zero authority, exactly as `i-canon-repo-evidence-read`
requires, and no claim in v2 rests on a legacy file's former accepted status —
`CORE.md`'s «мне нравится» and `c-002`'s «ADMIT TO CANON» are cited as history,
never as authority. That part of the artifact is clean.

## authored node-class oracle

`work/oracle-demo-boundary-specification-v1.md` — twelve tests (O1–O12) for the
node class "owner-approved demo boundary specification", authored from first
principles before the attack, because `knowledge/` holds no checklist for this
class and an empty oracle BLOCKS the leg. Its first principle: a boundary
specification is judged not on whether its content is good — content is the
owner's — but on whether a reader who has only this artifact can build the demo,
tell when it is finished, and tell when the specification itself has been proven
wrong.

O5, O8, O9, O10 and O12 each caught a defect the artifact's own structure did not
surface (F3, F4, F6, F9, F1/F2 respectively). O1, O2's promise text, O3 for the
joint-action stage, O4, O6's forbidden list, O7 and O11's disclosure in «На чём
это стоит» passed.

Honest statement of the guarantee, per the play's note: cited openings are
structurally excluded — every citation in the artifact was resolved against its
source and the mismatches are named above. This oracle attacks uncited
assumptions; it is rigorous, not a closed proof. No competitor precedent was
used: none is available inside current authority, and inventing one would be a
fabricated precedent.

## findings

The ten findings F1–F10, with exact artifact text, named support, the reason each
fails and whose part it is to resolve, are written out in full in
`work/c-work-october-demo-basis-v3-revision-001-call.md`. Summary, most severe
first:

- **F1** MUST 5 misattributes the recorded PGG verdict — wrong tool, and a
  conditional acceptance made unconditional; the artifact contradicts its own
  evidence section, and `g-12fd` done_when atom A2.6 fails with it.
- **F2** MUST 5's determinism-removal rationale (host assembles, ships the
  layout) has no resolved support and contradicts the cited pipeline.
- **F3** §4's causal-cooperation supports lean on the deferred mechanic and on
  open questions 3 and 8, and the stated count of three surviving supports is one.
- **F4** Falsifier 5 is vacuous or dead after the deferral.
- **F5** done_when atom «исключения явны» has no content in the artifact.
- **F6** The invalidation set was not updated for a generated level: generator,
  module set and the attempt's layout identity are missing.
- **F7** §5's «Можно» permits claiming building reaction and real danger that no
  MUST item can produce, against §5's own rule.
- **F8** MUST 5's feasibility is unbounded inside a fixed calendar — unverified
  Unity 6.3, zero precedent for the tool pair, zero art kit, 2–4 days to the
  first module, plus F2's unestablished replication — and it carries no appetite.
  Named, not downgraded: declaring it unbuildable is an engineering verdict, not
  a specification verdict.
- **F9** The promise asserts «двадцать минут» that no MUST backs and no falsifier
  tests; §2's danger stage asserts route-blocking and punishment that only open
  question 8 can supply.
- **F10** «Связана со свежими release-reality receipts» is satisfied only
  transitively: the artifact names no receipt, gate, date, AppID state or product
  build identity.

Also recorded, not findings against the artifact: **D1** the revision CALL's own
open-question count says eight where its itemization yields seven; **D2**
`knowledge/strategy-reset-boundary.md` still names the authoring CALL as the sole
lawful frontier (stale — `converge-verify` never writes `knowledge/`); **D3** v1's
question 4 claimed procedural generation was in CUT while v1's CUT never listed
it, and the claim died with the question.

Nothing in this list is a repeat of an earlier verification finding: the v1
verification CALL was withdrawn before dispatch and produced no findings, so
KERNEL two-strikes is not in play for any of them.

## state_changes

1. `work/oracle-demo-boundary-specification-v1.md` — CREATED. The authored
   node-class oracle, proposed for promotion to `knowledge/` by review or pulse;
   no authority until promoted.
2. `work/c-work-october-demo-basis-v3-revision-001-call.md` — CREATED. The
   owner-present work CALL carrying all ten findings, self-contained.
3. `work/c-converge-verify-october-demo-basis-v2-001-call.md` — a `disposition:`
   block appended: discharged, FAIL, receipt and successor named. Spent, not
   withdrawn.
4. `NOW.md` — `updated:` line to this leg. `direction_forecast.drivers[0]`: the
   frontier moves from the v2 verification to the owner-present v3 revision, with
   the failure and its two roots recorded and no owner decision reversed.
   `issues.i-demo-scope-cap`: the 2026-08-07 cut no longer depends on the v2
   verification but on a Basis version passing verification; evidence retargeted.
   `issues.i-procgen-determinism`: corrected — the owner's decision stands, its
   stated rationale does not, and runtime determinism is unresolved pending an
   engineering verdict rather than settled by the artifact.
   `open_calls`: the spent verification row is replaced by exactly one
   `c-work-october-demo-basis-v3-revision-001`. `bet: null`, `tasks: []`,
   `decisions: []`, `recurring: []` unchanged.
5. `LOG.md` — one line prepended.
6. `history/2026-07-26-s-converge-verify-october-demo-basis-v2-001.md` — this
   RESULT.

Not touched: `CHARTER.md`, `TREE.md`, `knowledge/**`, the artifact
`work/october-demo-basis-v2.md`, the concept frame, the PGG analysis, product,
Steam state, the frozen canon repository, `archive/**`. `g-12fd` stays parked with
no bet, tasks, tracks or shape. No numeric release chance was published. No
signoff line was written, because the artifact did not pass.

DECLARED DEVIATION. The play lists `NOW.md` (decisions, open_calls) and `LOG.md`
as this play's writes. Two issue rows inside `NOW.md` were also corrected, not
because verification decides anything, but because both rows are receipts of the
verification's own outcome: `i-demo-scope-cap`'s trigger is literally "the day
converge-verify passes or fails", which fired today, and `i-procgen-determinism`
asserted as settled the exact claim F1/F2 refute. Leaving either as written would
have left the direction asserting a verified rationale that failed verification.
No new issue was created and no route changed.

## captures

- `knowledge/strategy-reset-boundary.md` still calls the authoring CALL the sole
  lawful frontier; it needs a one-line refresh by repair or by the narrow review
  that eventually closes `g-12fd` (D2).
- The v2 Demo Basis legs were committed on local `main` in the primary folder
  while the direction worktree sat on `wt/indie-game-development`; both branches
  now hold the state only because this leg merged `main` in. The two branches
  drift apart again the moment the next leg runs in the other folder. Worth one
  hygiene decision by the owner, not by a leg.
- `work/oracle-demo-boundary-specification-v1.md` is a candidate cross-node
  checklist for every future `outcome_kind: specification` node in any direction,
  not only this one. Promotion is review's or pulse's call.

## decisions_needed

None from this leg. Verification routes findings and decides nothing; the ten
findings are the returning CALL's business and the owner's words are collected
there, not here.

## play_check

verify_target: specification complete=FAIL smuggling=FAIL

- Step 1 recite: done — both specification attacks stated, `verify_target` named.
- Step 2 completeness: both binding done_when sets split atomically and mapped;
  three atoms fail and one passes only on a disjunct; oracle authored from first
  principles before the attack, so the leg was not BLOCKED; uncovered atoms named,
  never filled.
- Step 3 smuggling: every weight-bearing non-owner claim required by done_when has
  its support named and resolved or marked untraceable; nothing was repaired, no
  content was chosen, no agent recommendation was treated as owner approval. No
  imported counterpart contract existed. No `→ PLAN` move applies — this is a
  specification target, not a build spec.
- Step 4 close: FAIL, so findings return to owner-authority `work` on `g-12fd`; no
  `§SIGNOFF` line written; no narrow review opened; no shape opened either way.
- G1: no bet, no tasks, no lane created; one untracked specification-authoring
  frontier remains legal. G5: this IS the separate fresh refutation session, and it
  is neither the authoring nor the revision chat. G7: no owner decision point was
  created. G9: `CHARTER`/`TREE` untouched. G10: steps exposed, RESULT final, the
  leg wrote nothing before emitting it.

## log

2026-07-26 | s-converge-verify-october-demo-basis-v2-001 | converge-verify | direction | g-12fd/october-demo-basis: independent verification of Demo Basis v2 FAILED with ten named findings — MUST 5 misattributes the recorded PGG verdict and its determinism-removal rationale is unsupported, and the two deferred hall mechanics left the danger stage, the causal-cooperation supports, falsifier 5 and the public-claim ceiling standing on deferred or undecided content; a node-class oracle was authored from first principles so the leg was not blocked; no owner decision reversed, findings returned to the owner-present v3 revision, g-12fd stays parked.

## next

`c-work-october-demo-basis-v3-revision-001` — owner-present specification
continuation on `g-12fd`, ready now, in a fresh chat with the owner. It carries
all ten findings; F1 is a factual correction, F6, F10 and D1 are mechanical and
need only his confirmation, and the rest need his words. Its RESULT produces
`work/october-demo-basis-v3.md` and one fresh `converge-verify` against v3. Only
after that verification passes may the narrow `review` close `g-12fd`, and only
then may `shape` open `g-37a1`.

Standing calendar unchanged and unmoved by this failure: 2026-08-07 is the
scope-cut boundary if no Basis version has passed verification, 2026-08-11 page
submitted, 2026-08-31 page public and registered, 2026-09-03 build candidate,
2026-09-15 demo live.

END_OF_FILE: live/indie-game-development/history/2026-07-26-s-converge-verify-october-demo-basis-v2-001.md
