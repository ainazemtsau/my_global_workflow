# RESULT — s-work-g-6b13-layering-accepted-and-resplit-001

play: work
direction: indie-game-development
node: g-6b13
date: 2026-08-02

## outcome

Владелец принял устройство игры после разговора, в котором сам нашёл дырку в первом объяснении.
Устройство записано отдельной страницей простым языком. Отклонённая сетевая задача разрезана на две:
фундамент без сети и доказательство, что фундамент верный.

## evidence

**Его слово:** «принимаю». До этого он опроверг первую редакцию объяснения собственным вопросом —
что будет, если понадобится детерминированная симуляция, которую не гонят по сети, и которая может
чуть различаться визуально. Первое объяснение сессии («судья решает всё») этого случая не покрывало
и было неполным. Ответ дополнен четырьмя видами происходящего, и это дополнение вошло в запись.

**Проверено первой рукой перед обещанием:** `tools/zero-float-scan.ps1` и
`tools/type-hardcode-scan.ps1` в `C:/projects/Unity/GasCoopGame_win-u3` ЛЕЖАТ ЦЕЛЫМИ — отключён
только вызов из `check.ps1`, где остался лишь `int-overflow-scan`. Значит обещание «дверь во второй
вид открыта, сторож включается на уголок» опирается на факт, а не на память.

## state_changes

`knowledge/how-the-game-is-built-layers.md` — новая страница: три слоя, четыре вида происходящего,
лобби как сменная обёртка, четыре механизма против сползания, и что сознательно не решено.

`NOW.md`: новая задача `a-1b` (один игрок ходит, правило в слое правил, сети нет) ВПЕРЕДИ `a-2`;
`a-2` переоткрыта из `rejected` в `open` с усиленным условием приёмки — правило движения при
подключении второго игрока НЕ МЕНЯЕТСЯ, и это доказательство верности устройства. Задач стало
двенадцать, из них две закрыты.

## captures

Нет.

## decisions_needed

Нет. Открытым сознательно оставлено поведение груза при переноске вдвоём — физическое тело или
счёт по рукам; это решение про ощущение игры, оно владельца и принимается у груза, а не на бумаге.

## play_check

- Устройство не назначено сессией, а согласовано в разговоре: владелец прямо потребовал этой формы
  — «не надо на меня спихивать архитектурные решения, это должно именно в обсуждении быть».
- Обещание про сторожей проверено чтением файлов до того, как было дано.
- Разрез задачи сделан так, что вторая половина ДОКАЗЫВАЕТ первую делом, а не декларацией: тот же
  приём, что и на расширяемости хозяина.
- **ОТСТУПЛЕНИЕ ОТ СХЕМЫ, названное честно:** `KERNEL` §3 отдаёт запись в `knowledge/` плеям
  `review` и `pulse`, а эта нога — `work`. Содержание одобрено владельцем в сессии и записано его
  смыслом; альтернатива — держать устройство в `NOW.md`, который и так раздут, и терять его для
  будущих сессий. Помечено как трение, ратифицируется ближайшим `review`/`pulse`.

## FRICTION

Долговечное решение об устройстве продукта не имеет своего дома: `knowledge/` пишут только `review`
и `pulse`, но узнаётся такое решение в `work`. Второй случай — повод для maintenance.

## log

владелец принял устройство игры; записана страница о слоях, отклонённая задача разрезана на фундамент и доказательство

## next

Наряд на фундамент — один игрок ходит, правило в слое правил, сети нет. После него наряд на второго
игрока, где то же правило не меняется.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-work-g-6b13-layering-accepted-and-resplit-001.md
