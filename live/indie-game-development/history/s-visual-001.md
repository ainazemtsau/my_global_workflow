# RESULT — s-visual-001 (visual-track setup, g-7e15)

session: s-visual-001
date: 2026-06-21
play: research + owner-directed planning (stand up the VISUAL/GASG parallel track on g-7e15)
node: g-7e15 (gas visual identity / GASG)
owner_present: yes (live conversation)

outcome: |
  The VISUAL (GASG) track is stood up as an ACTIVE PARALLEL track on g-7e15 (elevated parked→active),
  with its own appetite, visible in NOW planning — exactly what the owner asked for: «хочу чтобы оно
  участвовало в дереве … со своим аппетитом … чтобы это не было параллельно где-то на задворках …
  начать сейчас». Engine g-9c41 stays the PRIMARY active bet (G1 preserved — the visual track adds NO
  NOW.active_tasks; its work runs as CALLs, like canon/marketing). The track's FIRST step (its research,
  the owner's chosen first move — "figure out HOW, there are many options, write the structure") is
  DELIVERED: work/gas-visual-research-2026-06-21.md. First build step queued as c-visual-001 (P1).

  Key reframes from the owner this session, folded in:
  - Visual is DECOUPLED from the engine — develop the visualizer on FAKE/simulated data now, swap to the
    real Wave-B front later via the R13/R14 seam (the research confirms this is the correct, standard
    architecture: a read-only view over the authoritative grid).
  - Priority = SECONDARY to the engine; cadence = ~40–60 min/day in the engine's build-gaps; started now
    because leaving visuals to the end is a real schedule risk.
  - Earlier over-jargoned/over-engineered framing (a 4th "kuznitsa" + fake-data plumbing as an immediate
    task) was REJECTED by the owner as confusing; corrected to plain "a parallel visual track that begins
    with researching the approach."

evidence:
  - work/gas-visual-research-2026-06-21.md (deep-research wf_e5924329: 95 agents, 24/25 claims verified) —
    layered read-only architecture; VFX-Graph-from-GraphicsBuffer + custom URP raymarch as the two viable
    sim-driven paths; off-the-shelf (Zibra/URP-Fog-Volumes) mismatched; INSIDE low-complexity distinctiveness;
    half/quarter-res + bilateral-upsample min-spec lever; P1→P5 prototype sequence + risk gates.
  - git HEAD reconciled FIRST-HAND before writing (concurrent-session hazard): b274967 = the Живое-Стекло
    cut (s-repair-008) already committed; working tree clean → the FIŠKA cut the owner mentioned is DONE,
    not re-routed.

state_changes:
  - TREE.md g-7e15: status parked→active (as a PARALLEL TRACK — engine g-9c41 stays the primary active bet);
    detail expanded with the track framing + appetite + approach + first step. Node goal/done_when (criteria
    1-5) UNCHANGED. owner_approved (his words above) — G9.
  - NOW.md: added a parallel_tracks: block (g-7e15 visual track — status active, appetite ~40–60 min/day,
    approach, riskiest_unknown, next=c-visual-001); added open_call c-visual-001 (P1, queued). active_bet /
    active_tasks / next (c-exec-012) UNTOUCHED.
  - LOG.md: one line appended.
  - work/gas-visual-research-2026-06-21.md: created (the research artifact).

captures:
  - FRICTION (maintenance candidate): the OS status enum {parked|shaped|active|done|dropped} has no distinct
    value for an "active PARALLEL track" (canon/marketing/mechanics/visual all run alongside the one active
    bet). Recorded g-7e15 as `active` + reconciled in-line (engine = primary bet, no active_tasks added).
    Worth a maintenance look at how the tree/NOW represents sanctioned parallel tracks vs G1's "one active bet".
  - FIŠKA «Живое Стекло» cut — already DONE in the canon track (b274967 / s-repair-008); reconciled, not re-done.

decisions_needed:
  - id: d-visual-start-001
    question: Start P1 (c-visual-001) now, or leave it queued in the plan?
    options:
      - "(A, rec) Queue it — it's ready; you start it in an engine build-gap when you want (parallel, ~40–60 min/day). Nothing blocks it; reads the existing RN1."
      - "(B) Kick off c-visual-001 now as the next session (opens with a PLAN, owner present)."
    recommendation: A — it's queued and ready; you pull it when you have a gap, keeping the engine the primary focus.

play_check:
  - "Research delivered (the owner's chosen first step): done → work/gas-visual-research-2026-06-21.md."
  - "Owner-directed planning change (tree node elevation + appetite): done, owner_approved with his words (G9)."
  - "G1 preserved: engine g-9c41 stays the one active bet; the visual track adds no active_tasks (work via CALLs). (owner) — owner explicitly wanted it active/parallel/not-backwater."
  - "Concurrent-state reconciled before write (git HEAD b274967, tree clean): done — FIŠKA cut already landed, not re-routed."

log: "stood up the VISUAL (GASG) parallel track on g-7e15 (parked→active, owner-directed) + delivered its first-step research; first build step queued = c-visual-001 (P1)."

next: |
  c-visual-001 (P1) is QUEUED (decision d-visual-start-001 — start now vs pull-when-ready, rec = queue).
  Engine spine UNCHANGED: NOW.next stays c-exec-012 (Wave A t-1 sandbox). The visual track and the engine
  run in parallel at the owner's cadence (root map_order). FULL prototype path P1→P5 + risk gates in
  work/gas-visual-research-2026-06-21.md.

END_OF_FILE: live/indie-game-development/history/s-visual-001.md
