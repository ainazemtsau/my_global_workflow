# RESULT — s-work-g-6b13-a1-closed-a2-issued-001

play: work
direction: indie-game-development
node: g-6b13
task: a-1 (закрыта) → a-2 (запущена)
date: 2026-08-02

## outcome

`a-1` закрыта словом владельца после его собственного Play. Первый шаг ставки виден глазами и
принят. Выпущен наряд на `a-2` — двое по сети в одной сцене; камера приходит там же.

## evidence

**Закрытие `a-1` — его слова, дословно:** «ничего не увидел пишет что типа нет камеры (видно что это
именно что мы и ожидали)». `done_when` задачи был: «Владелец открывает проект, жмёт Play — сцена
новой игры открывается. Больше в ней ничего нет». Он открыл, нажал, сцена открылась, и в ней ничего
нет. Пустота была содержанием задачи, а не дефектом, и он подтвердил это сам.

**G5 закрыт его проверкой, а не отчётом сборщика.** Проверка сделана его руками, в его Unity, вне
чата сборки и вне этого чата — то есть независимость выполнена по существу, а не формально.
Возврат исполнителя был принят как улика и отдельно перепроверен по диффу ногой
s-work-g-6b13-a1-scaffold-return-001.

**Предупреждение сработало как надо:** сессия назвала чёрный экран и «No cameras rendering» ДО его
Play, с признанием, что это следствие формулировки наряда, написанной сессией. Поэтому он увидел
ожидаемое, а не поломку. Это ровно та разница, из-за которой он грозил закрыть ставку.

**Проверено первой рукой для наряда `a-2`** в `C:/projects/Unity/GasCoopGame_win-u2`:

- Unity `6000.5.5f1`, URP 17.5.0, Input System 1.19.0;
- **FishNet лежит как ПАКЕТ** — `Packages/com.firstgeargames.fishnet/`. Это зависимость проекта, а
  не старый игровой код: граница устава проведена по ПАПКЕ старой игры, и на `Packages/**` она не
  распространяется. Сетевой слой переизобретать не нужно, и запрет читать старое этому не мешает;
- `com.unity.multiplayer.center` 1.0.1 в манифесте есть, `com.unity.multiplayer.playmode` — нет;
- в сцене `a-1` ноль GameObject-ов, поэтому камера обязана прийти вместе с фигурами.

## state_changes

`NOW.md`: `a-1` → `done` с его дословными словами; наряд `c-exec-newgame-folder-and-scene-001` →
`done`; `a-2` → `active`; новый `open_call` `c-exec-two-players-networked-001` со слотом `WIN-U2` и
базой `e0000c5b`. `work/`: наряд `c-exec-two-players-networked-001-call.md`.

## captures

Нет.

## decisions_needed

Нет.

## play_check

- Закрытие сделано словом владельца, а не возвратом исполнителя — AGENTS.md это прямо требует, и
  возврат был отдельно перепроверен по диффу до его Play.
- Наряд `a-2` строится поверх `e0000c5b`, а не на `main`: полоса продолжается в своей ветке.
- Развязка с параллельной работой сохранена: `a-2` владеет `Assets/TunnelCrew/**` (и при
  необходимости `Packages/manifest.json`), `c-1` владеет `tools/` и `validation.config`. Пересечений
  нет.
- В наряд не написано НИ ОДНОГО требования к тому, как, сколько раз и с кем владелец проверяет —
  его прямой запрет.
- Новых проверок «на то, что видно глазами» не заведено и запрещено поимённо.
- Сетевая модель не выводилась заново: она выбрана его словами и в наряде названа как данность.

## log

a-1 закрыта его Play, выпущен наряд a-2 — двое по сети в одной сцене

## next

`c-exec-two-players-networked-001` — `ready`, слот `WIN-U2`. Параллельно идёт `c-1` в `WIN-U3`.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-work-g-6b13-a1-closed-a2-issued-001.md
