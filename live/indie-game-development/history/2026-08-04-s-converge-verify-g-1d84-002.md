# RESULT — s-converge-verify-g-1d84-002

direction: indie-game-development
play: converge-verify
node: g-1d84
call: c-converge-verify-g-1d84-002
date: 2026-08-04
owner_present: no

---

## outcome

`verify_target: build` · `complete=FAIL` · `smuggling=FAIL`.

**ВТОРОЙ FAIL УЗЛА — потолок сработал.** Проверено первой рукой: в `history/` ровно одна прежняя
`converge-verify` по g-1d84 (`2026-08-04-s-converge-verify-g-1d84-001.md`). По `os/plays/converge-verify.md`
шаг 4 бумажная фаза узла на этом кончается: **строки в `converge-arch` НЕ возвращаются**, поверхность
заморожена уликой (`work/converge-g-1d84.md` §VERIFY-002), и владельцу написана ОДНА развилка — вырезать,
разрезать в map, или идти дальше на том, что отвечено. Третий круг открывают только его слова.

**Сначала честно о том, что набор выдержал, потому что это большая часть.** Объявленная база
подтверждена первой рукой. Счёт файла пересчитан МЕХАНИЧЕСКИ разбором самого текста и сходится точно:
111 строк §WHAT (111 уникальных id, дублей ноль), 88 `answered`, 26 `acceptance`, 3 `улика`, 19 `open`
плюс 1 `deferred` = 20; разбивка 2 + 17 + 0 + 1 = 20; полный счёт 88 + 3 + 19 + 1 = 111. §SIGNOFF,
§CONTRACTS «Счёт этой ноги» и `contracts_open: 1` говорят одно и то же — лекарство F5 в части счёта
держит. Измерение, которым нога 002 закрыла F3, перемерено первой рукой и подтвердилось полностью.
Текст изменённых критериев в `TREE.md` совпадает ДОСЛОВНО с тем, что разбор записал закрытым. Ни один
токен владельца не записан выше настоящего, включая новую подпись «1а, 2а, 3а, 4а»: закрытыми
объявлены ровно те строки, которые каждая буква покрывает. У всех трёх новых пробелов §MINER-002 назван
отвечающий, и ни один не закрыт выводом ноги. Пять из семи прежних находок исполнены чисто.

**Причина всех пяти новых находок одна.** Карточку владелец изменил ПОСЛЕ того, как оба файла набора
были закрыты. Сами правки чистые. Но следом за правкой не пересобрали носители его слова: реестр
подписей `TREE.md`, ряд ввоза I01, одну `acceptance`-строку, два места, объявляющие «чем кончается
заход», и набор `acceptance`, который уезжает в нарезку. Это не спор о содержании — это разъехавшиеся
носители одного и того же его слова, и каждый чинится минутами письма. Чинить их эта нога не вправе:
проверка ничего не отвечает и ничего не чинит, а третий круг открывает только он.

| id | класс | находка |
|---|---|---|
| V1 | источник | I01 закрывает «точный текст двенадцати критериев» токеном «принимаю карточку»; с 2026-08-04 он не покрывает критерии 2, 6, 8 и строку исключений. Реестр `owner_approved` в `TREE.md` четыре новых токена НЕ называет (грепом ноль вхождений) — и `TREE.md` отдельно, и этот файл через I01 приписывают действующий текст более раннему и более широкому слову |
| V2 | источник | W57 (`answered · acceptance`) несёт вычеркнутое «Распорядок по времени и обстоятельствам» со ссылкой на карточку, которая так больше не говорит; `acceptance` копируется в `done_when` дословно. Новое слово «длительности» встречается во всём наборе один раз — в приписке W58 — и ни одна строка его не спрашивает, при том что часы игры и цикл суток теперь в строке исключений |
| V3 | полнота | «Когда груз в ней — заход удался, и игра это показывает» — единственное условие успеха узла — не признано ни одной строкой: W94a его цитирует, а W99 и C09 прямо отказываются решать «чем он кончается» в этой волне. Ни `acceptance`, ни шва C13-C21 на точку доставки, признание и сообщение |
| V4 | источник | Починка F5 подняла W05 из `open` в `answered`, хотя половина строки — решение ноги (A3) в документе, объявившем себя необязывающим. Это ровно диагноз F5, а лекарство плея для него — понижение, а не повышение; четыре сестринских строки на такой же опоре остались `open` |
| V5 | полнота | «не меньше трёх комнат… и обстановка» и «одна названная точка доставки… поставленная руками» помечены `answered · подпись владельца`, а не `acceptance`; §SIGNOFF фиксирует это намеренно. Но плей отдаёт нарезке именно `acceptance`, и +2-4 дня, названные ценой правки, в эту выдачу не едут |

Названо отдельно и вердикта не несёт: W06a единственной венью называет Unity у владельца, а счёт держит
её в «PLAN/ADR» при строке «владельцу должны ДВЕ»; C05a подорожал вместе с тремя комнатами и не
пересобирался.

## evidence

**База, подтверждённая первой рукой.** `C:/projects/Unity/GasCoopGame`: `git rev-parse` даёт
`839df47e78127fe2ebfba5eabb307bf6bdd61e9b` на `HEAD`, `origin/main` и `origin/dev`;
`git status --porcelain` пуст.

**Механический пересчёт.** Строки §WHAT (114-291) разобраны программно по шаблону `| W<id> |` с
извлечением последней колонки: 111 строк, 111 уникальных id, дублей ноль. Метки: `answered` 88
(32 голых + 26 `acceptance` + 10 `факт кода` + 4 «риск назван» + 3 подписи на map + 2 граница +
2 → shape + 1 П2/A3 + 1 → PLAN + 1 evidence + 1 evidence с оговоркой + 1 disposition + 1 «два текста» +
1 ручка + 1 П1 + 1 «его слово»), `acceptance` 26, `улика` 3 (W52a, W65, W84), `open` 19
(W04, W06, W06a, W26, W31, W33, W40, W43, W52, W53, W61, W71, W77, W80, W85, W86a, W90, W91, W105),
`deferred` 1 (W75). Все три числа §SIGNOFF, разбивка 2+17+0+1 и `contracts_open: 1` сходятся.

**Перемеренное измерение F3.** В `Assets/**` ровно два блока `PrefabInstance` — строка 1512
`Lobby.unity` и строка 167 `NetworkWalkers.unity`, обе строки читаются `PrefabInstance:`. Записей
`objectReference` в этих двух файлах 11 + 11 = 22. Непустых `objectReference` во всём `Assets/**`
(27 строк, 5 сцен, 29 префабов) — НОЛЬ. Вывод ноги 002 «диск не отвечает ни за, ни против»
подтверждён.

**Выборочные `факт кода`, перепроверенные на объявленной базе (расхождений ноль).**
`Packages/manifest.json` — `com.unity.nuget.newtonsoft-json` 3.2.1; `Newtonsoft.Json.Schema` в проекте
отсутствует (совпадения по имени только у Dungeon Architect и в `Library/PackageCache`);
`Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef` — `noEngineReferences: true`;
`Assets/TunnelCrew/Scenes/` — четыре сцены (`HostWalksHisDay`, `Lobby`, `NetworkWalkers`, `SingleWalker`);
`Assets/TunnelCrew/Art/` не существует; `NetworkPlaySettings.asset` — `_cargoLength: 2.4`,
`_cargoWidth: 0.5`, `_cargoHeight: 0.5`, `_cargoMaxHolders: 2`.

**Текст карточки против разбора.** `TREE.md` критерий 2 («ГРУЗ И ЦЕЛЬ ЗАХОДА… одну названную точку
доставки за его пределами, поставленную руками. Когда груз в ней — заход удался, и игра это
показывает… Счёт, награда, провал, повторный заход и экономика здесь не решаются»), критерий 6
(«Дом — не одна пустая коробка: не меньше трёх комнат, соединённых проходами, и в комнатах стоит
обстановка, которую несущие задевают… распорядок — порядок дел, их длительности и обстоятельства»),
критерий 8 («Дом и тоннели лежат отдельными корнями…», слова «сервисы» нет) и строка исключений
(«часы игры и цикл времени суток») совпадают дословно с тем, что записали закрытым W94a, W107, W106,
W58 и W95.

**V1, проверено грепом.** «1а, 2а, 3а, 4а» в `TREE.md` — ноль вхождений; `card-text-and-time-seam` в
`TREE.md` — ноль вхождений; последняя запись реестра `owner_approved` по g-1d84 остаётся map-нога
g-0c26 с токеном «принимаю карточку» на «the exact twelve-criterion text… shown in full». Ряд I01
цитирует тот же токен и те же два источника. §IMPORT нога 002 не трогала по собственной записи;
`state_changes` map-ноги §IMPORT и реестр `TREE.md` тоже не называют.

**V2, проверено грепом.** «длительност*» во всём наборе: одно вхождение, `converge-g-1d84.md:209`
(приписка W58); в `converge-g-1d84-arch.md` — ноль. «по времени»: два вхождения — `:208` (W57,
`answered · acceptance`, ссылка `→I01 крит.6`) и `:209` (W58, где цитируется уже новый текст).
§CONTRACTS C16 несёт «Распорядок по порядку дел и обстоятельствам» без длительностей.

**V3, процитировано дословно.** W99 (`answered (граница)`): «длина захода, чем он кончается, что такое
провал, переход между участками и экономика улучшений решаются ТОЛЬКО там и здесь не изобретаются».
C09: «Контракт ЗАПРЕТИТЕЛЬНЫЙ: длина захода, чем он кончается, что такое провал… здесь НЕ решаются».
Строки «заход удался» в §WHAT есть только в W94 и W94a (цитата), в §CONTRACTS — только в C02 и в
«Счёте этой ноги». Перечень подсистем §CONTRACTS цели захода не содержит. Цена правки, названная
самой map-ногой: «примерно полдня (объём-триггер и сообщение)».

**V4, арифметика.** `answered` 83 → 86 при четырёх добавленных строках, из которых `answered` только
W106; значит две существующие строки поднялись в `answered` — W09 (подпись П1, законно) и W05.
Половина W05 подписана П2, половина является решением ноги A3; арх-файл о себе: «Ни одна строка ниже
не является требованием узла», §SIGNOFF: «Ни одно из решений ноги обязывающим не является».

**V5.** §SIGNOFF: «`acceptance`: 26 → 26 → 26 (ни одна новая строка не копируется в done_when)».
`os/plays/converge-verify.md` шаг 4: «Build opens shape with acceptance/contracts to copy».

**Семь прежних находок, построчно.** F1 — исполнено чисто: источник вне ноги (его прямая речь),
значения не изобретено, ряда словаря нет, карточка вычищена, побочный эффект («поимённо остаются две
вещи») назван ему ДО подписи, вариант «третьим назвать хозяина» показан и не выбран; ни одна строка
набора на третий названный корень не опирается. F2 — исполнено в карточке, но принесло V3 и V5.
F3 — исполнено и перемерено; сторож возвращён в долг; наблюдение по венью названо отдельно.
F4 — исполнено: возврат в `open`, цены названы до рекомендации, формат не выбран, П3 не повышен,
средний вариант назван находкой, а не ответом. F5 — исполнено в счёте, но принесло V4.
F6 — исполнено: основание отозвано источником вне ноги, чтение не выбрано, вопрос стоит у владельца.
F7 — исполнено: «фиксирую» подтверждено первой рукой в
`history/2026-08-04-s-converge-g-1d84-integrated-house-slice-001.md:29`, `LOG.md` и
`work/c-converge-arch-g-1d84-001-call.md:21`.

**§MINER-002.** M5 — отвечающий PLAN/ADR, ввезённая половина объявлена ввезённой с ссылкой
`how-the-game-is-built-layers.md:48` и с заранее названным триггером «упёрлись в курьера — стоп и
вопрос владельцу»; M6 — PLAN/ADR; M7 — PLAN/ADR. Ни один не закрыт выводом ноги.

**Граница гарантии, честно.** Процитированные открытия структурно исключены из атаки: проверка била
по неподпёртым допущениям, а не доказывала файл. Цены и лицензии W52a/A2 живут вне диска и в этой
сессии первой рукой не проверялись — они помечены уликой и вердикта не несут. Это строгость, а не
закрытое доказательство.

## state_changes

1. `work/converge-g-1d84.md` — добавлен раздел `§VERIFY-002` перед трейлером: вердикт, счёт потолка,
   таблица пяти находок, два названных наблюдения, полный список пройденного и граница гарантии.
   §WHAT, §GLOSSARY, §IMPORT, §OWNER, §COVERAGE, §CONTRACTS, оба §SIGNOFF, §VERIFY-001 и §ARCH-002 не
   тронуты ни одной строкой.
2. `NOW.md` — `open_calls`: возвращающийся `c-converge-verify-g-1d84-002` снят, список пуст; нового
   наряда НЕ открыто, потому что по потолку следующий ход выбирает владелец. `decisions`: добавлена
   одна — `d-g-1d84-second-fail-branch-001` (вырезать / разрезать картой / идти дальше на отвеченном),
   `status: waiting_owner`. Две прежние решения не тронуты и не переформулированы. `updated` переписан
   на эту ногу. Ставка, задачи, полосы, issues и прогноз не тронуты.
3. `LOG.md` — строка ноги первой в списке.
4. `history/2026-08-04-s-converge-verify-g-1d84-002.md` — этот RESULT.

`TREE.md` и `CHARTER.md` не тронуты. Ни одна строка §WHAT не возвращена и не заполнена.

## captures

- Класс, который стоит увидеть ВПЕРЁД, а не в этом узле: **правка утверждённой карточки после закрытия
  разбора обязана пересобирать носители — реестр подписей, ряд ввоза, `acceptance`-строки и контракты,
  говорящие о том же.** Все пять находок этой проверки — один этот класс. Кандидат в `os` через
  maintenance, если повторится на другом узле; одной ногой правило не заводится.
- Реестр `owner_approved` в `TREE.md` до сих пор вёл КАЖДУЮ поправку, включая однострочные. Разрыв
  цепочки заметен только тому, кто читает `history/`; лечится строкой реестра, а не правилом.
- `acceptance` как единственная выдача нарезке — узкое место формы: строка, закрытая подписью
  владельца, но не помеченная `acceptance`, до задачи не доезжает. Замечено на двух самых дорогих
  строках волны.

## decisions_needed

Одна, и она обязана быть одна — потолок второго FAIL: `d-g-1d84-second-fail-branch-001`.
Ветку выбирает владелец своими словами: (А) вырезать узел; (Б) разрезать его картой на два;
(В) идти дальше на том, что отвечено, с прямо названной оговоркой, что нарезка читает саму карточку,
а не только набор `acceptance`. Рекомендация ноги — (В); выбор не сделан и делаться не будет.
Четвёртый ход — короткий ремонт бумаги перед нарезкой — по правилу плея открывает только его слово.

Две прежние решения висят и этой ногой не тронуты: форма файла профилей (W52) и чтение слова
«инструментов» (W80).

## play_check

- `verify_target: build complete=FAIL smuggling=FAIL`
- Шаг 1 — заявка прочитана: обе атаки названы, цель `build` объявлена. Рассуждения решавших ног
  источником не считались — источником считалось только то, на что они ссылаются.
- Шаг 2 — полнота: двенадцать критериев НОВОЙ редакции, цель, «зачем», риск и строка исключений
  разбиты атомарно и сопоставлены со строками §WHAT и §CONTRACTS. Не дошло до признающей строки одно
  (V3); один `done_when`-носитель противоречит утверждённому тексту (V2). Слова карточки, о которых
  она ни разу не спрашивает, названы: **«длительности»** (крит. 6) и **«показывает»** (крит. 2) —
  оба заведены правкой, ни одно не заполнено этой ногой. Чек-листа класса узла в `knowledge/` нет;
  своего эта нога не заводила.
- Шаг 3 — источник: для несущих строк и всех `acceptance` названы поддерживающие факты и затребована
  разрешимая ссылка. Три строки не выдержали (V1, V2, V4); свежих ответов не дано ни одного, ни одна
  строка не понижена и не заполнена самой проверкой.
- Шаг 4 — закрытие: **второй FAIL, подтверждённый по `history/`.** Строки НЕ возвращены,
  `converge-arch` не открыт, `shape` не открыт, §SIGNOFF о прохождении не написан. Поверхность
  заморожена уликой, владельцу написана ровно ОДНА развилка, и она не выбрана.
- Границы наряда соблюдены: ничего не отвечено и не починено; §WHAT, §CONTRACTS, оба §SIGNOFF,
  `TREE.md` и `CHARTER.md` не тронуты; ставка, задачи и полосы не созданы; два прежних вопроса
  владельцу не переформулированы и не решены; сессия свежая и отдельная от ноги 002, map-ноги и
  любого day-чата.
- G7: единственное новое решение владельца — ветка потолка, и её требует сам плей.
  `owner_verdicts_invented: 0`.

## log

2026-08-04 | s-converge-verify-g-1d84-002 | converge-verify | direction | g-1d84: вторая свежая
проверка вернула complete=FAIL smuggling=FAIL — второй FAIL, потолок сработал: строки не возвращены,
поверхность заморожена, владельцу написана одна развилка. Пять находок одного рода — карточка
изменена его подписью после закрытия обоих файлов, а носители не пересобраны: реестр TREE и ввоз I01
приписывают действующий текст более раннему слову, `acceptance`-строка распорядка копирует в задачу
вычеркнутое «по времени» и «длительности» никто не спрашивает, единственное условие успеха захода не
признано ни одной строкой и дважды прямо исключено, W05 поднята в закрытое на решении ноги, три
комнаты и точка доставки не попали в выдачу нарезке. Прошло: база 839df47e, счёт 111/88/26/3/20
сходится точно, измерение скрытых ссылок перемерено, текст карточки совпадает дословно, токены не
завышены, у трёх новых пробелов назван отвечающий.

## next

Ход владельца, и только его: ветка по `d-g-1d84-second-fail-branch-001`. По его слову открывается
ровно один следующий шаг — `review` на вырез, `map` на разрез, `shape` на «идти дальше», либо
названный им самим короткий ремонт бумаги. Ни один наряд не открыт, и нарезка сама не открывается.

END_OF_FILE: live/indie-game-development/history/2026-08-04-s-converge-verify-g-1d84-002.md
