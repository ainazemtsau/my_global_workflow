# RESULT — s-shape-g-5a7c-wave-5-001 (play: shape)

## outcome

**Пятая волна открыта.** `NOW.bet = g-5a7c`, ставка `bet-g-5a7c-wave-5`, шесть задач заведены,
седьмая — уже идущая `t-assetlib-1` — admitted в волну, потому что она производит ровно то, чем
живут `t-room-look-1` и `t-loot-models-1`.

Цель волны впервые снаружи игры: пять скриншотов настоящего геймплея на странице Steam, которых не
стыдно. Планка его словом ниже обычной — «мы не должны ими гордиться, главное чтобы не стыдно было»
— и порог смерти написан под неё.

**Форма волны отличается от всех предыдущих тремя вещами, и все три — его слова.**

1. **У волны есть ДАТА, и это отмена его собственного запрета дат от 2026-08-02**, дословно
   записанного в аппетите четвёртой волны. Дата не наша: её ставит Steam.
2. **Кадр собирается, а не ловится.** Он разрешил инсценировку прямо. Это снимает ловлю моментов,
   доводку дома целиком и четверых живых игроков — то есть большую часть цены набора.
3. **Ни одна задача не спрашивает владельца по ходу.** Его требование: «Я хочу, чтобы ты создал
   отдельные задачи… Она должна мне расписать, что спросить, что сделать». Все вопросы адресованы
   Concept Lab и задаются ногами; в каждой карточке есть блок «что спросить».

## evidence

Три замера на `548e6340` стали строками `done_when`, а не жалобами:

- все 14 материалов дома — голый `_BaseColor` без единой карты;
- весь дом освещён одним источником плюс скайбокс, ни одной лампы дома нет;
- `LineRenderer` в продуктовом коде — ноль, при том что `HolderCount` доезжает до презентации каждый
  такт, то есть «сколько лучей» и «чьё натяжение» уже на клиенте.

**Поправка владельца принята дословно и изменила задачу:** «будильника нет в модели… там круг, но
это не будильник». Значит единственный назначенный `LootVisualProfile` — примитив, и **настоящих
моделей лута в игре нет ни одной**. `t-loot-models-1` покупает первые, а не «остальные».

**Вторая его поправка развязала две задачи:** перекраска предмета не нужна, достаточно, чтобы луч
присоединялся — «похуй на сам объект». Раньше `t-loot-models-1` была сшита с `t-beam-1`, теперь нет.

**Занятость слотов перемерена, а не предположена:** `WIN-U1`, `WIN-U2`, `WIN-U4` свободны;
`WIN-U3` — `CLAIMED` под `c-exec-g-5a7c-assetlib-1-001:build`, и `slot/win-u3` несёт **десять**
неслитых коммитов. Значит три слота, не четыре, и это записано в аппетит.

## state_changes

- `card new bet-g-5a7c-wave-5` — одиннадцать блоков: его слово, цель, аппетит, порог, вырезы, разрез,
  границы, три рискованных допущения, семь линз, прогноз, довод против.
- `card new t-beam-1`, `t-bodies-intake-1`, `t-room-look-1`, `t-loot-models-1` — `ready`;
  `t-hands-1`, `t-frames-1` — `waiting` с `waiting_on`.
- `card set t-assetlib-1 _bet=bet-g-5a7c-wave-5` — идущая работа admitted в волну.
- `now set bet=g-5a7c`.

## captures

Своя ошибка в этой же ноге: шесть карточек завелись с мусором в `status` и `goal` — интерполяция
подстановки в цикле подставила тип, а не значение. Поймано своим же осмотром карточки, а не гейтом,
и починено на месте. Урок узкий и его стоит держать: **после массового заведения карточек читать
одну целиком, а не доверять выводу команды.**

## decisions_needed

Ничего. Цель, срок, содержание, вырезы и порог даны им прямо; порог подтверждён его словами «в
четверг снимаем тем, что есть… В четверг выкладываем всем, что есть».

## play_check

G6 ✓ — настоящий список вырезов (шесть, четыре с цитатой), вердикт по каждой из семи линз, и задача,
проверяющая самое рискованное допущение (`t-bodies-intake-1`, строка 2: приёмник проверяется на
заглушке ДО прихода настоящих моделей). G4 ✓ — есть `done_when` и `kill_by`. G1 ✓ — четвёртая волна
закрыта обзором до открытия пятой. G3 ✓ — аппетит поставлен до задач. Полос нет, и это сказано вслух.

## log

пятая волна открыта: цель снаружи игры, дата вместо запрета дат, и кадр собирается, а не ловится

## next

Выдача: три свободных слота под `t-beam-1`, `t-bodies-intake-1` и `t-room-look-1` или
`t-loot-models-1`.
