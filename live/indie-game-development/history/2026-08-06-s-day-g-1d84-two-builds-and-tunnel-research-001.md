# RESULT — s-day-g-1d84-two-builds-and-tunnel-research-001

play: day · direction: indie-game-development · node: g-1d84 · дата: 2026-08-06

## outcome

Владелец развёл три корня на два разных обращения: две сборки запускаются как есть, а тоннель он
запускать запретил и попросил вместо этого разбор — «я пока не очень представляю… хочу понять
реализацию». Заведён исследовательский ребёнок тоннельного корня; сам корень переведён в ожидание.

Своя ошибка исправлена в ту же ногу: предыдущий ход сообщил владельцу, что Направление отстало и
наряды стоят на мёртвой базе. Оба утверждения были ложны — читалось состояние ДО коммита параллельной
сессии.

## evidence

### Исправление собственного отчёта

- `t-house-4` закрыта коммитом `be92daaf` («закрыта close: light на 8ffe5a5d, два хвоста доехали,
  t-host-2 и t-house-5 разблокированы»). Предыдущий ход этой день-сессии сказал владельцу «Направление
  снова отстало» — неверно, читалось состояние до коммита. Он поправил: «дом уже должен быть закрыт мы
  его закрывали в параллельной сессии». Поправка признана и снята в чате.
- Второе ложное утверждение — «наряды написаны на мёртвой базе». Перемерено первой рукой: оба CALL уже
  несут `base: 8ffe5a5d…`, перебитую ногой `s-work-g-1d84-house-as-data-close-001`. Дополнительно
  проверено независимо, что все четыре файла, названные этими нарядами, **между `d60c2f04` и
  `8ffe5a5d` не менялись ни байтом**: `HouseholderRouteControllerEditor.cs`,
  `AuthoritativeCargoRoster.cs`, `IntegratedHouse.unity`, `Householder.cs`. Наряды готовы к запуску.

### Перемер продукта

`origin/main` = `origin/dev` = `8ffe5a5d89398b1dd7bcf450157446f7952f3855` (2026-08-06 06:22 +0300),
9 коммитов сверх прежней базы `d60c2f04`. В диапазоне приехали, помимо самой поставки дома, **оба
дважды непоехавших решения владельца** — проверено на дереве `origin/main`:
`ProjectSettings/UnityConnectSettings.asset:7` = `m_Enabled: 0` (было `1`), и у мёртвого корня
`c-exec-grid-v1-g01-document-authority-001` появился третий, терминальный файл квитанций
`02-closed-by-owner.json`. Механизм «едет пунктом в наряде» не сработал дважды и сработал на третий
раз.

Слоты на момент записи: `WIN-U1` `CLAIMED` под идущие профили (3 незакоммиченных файла, 2 своих
коммита — работа идёт); `WIN-U2`, `WIN-U3`, `WIN-U4` `AVAILABLE`, деревья чисты.

### Измерение, ради которого разбор тоннеля вообще осмыслен

Поставка `t-house-4` создала ровно ту вещь, записью которой обязан быть участок:
`Assets/TunnelCrew/Core/House/` — `HousePlan`, `HousePassage`, `HousePlace`, `HouseRoom`,
`IHousePlanSource`; со стороны сцены `SceneHousePlan` и три маркера.

**`HousePassage` несёт имя, два конца по именам мест, координаты и `Width` — и НИ ОДНОГО поля про
проходимость.** Проход сегодня неизменяемый факт «эти два места соединены». Задача требует прохода,
чья проходимость МЕНЯЕТСЯ событием игры. Недостающая часть названа точно: она мала по объёму и
неочевидна по устройству — что и объясняет, почему владелец просит разбор до сборки, а не после.

Соседний факт, положенный в наряд уликой: `Width` уже описывает, что проход бывает у́же груза, и его
собственный комментарий говорит «Nothing reads it yet». Быть ли «закрыто» отдельным состоянием или
частным случаем ширины — вариант для разбора, а не готовый ответ.

### Слова владельца (дословно, 2026-08-06)

> я сейчас хочу, чтобы мы запустили обсуждение по туннелю, да, как мы будем делать, как это будет
> играться, что нам вначале минимально сделать. и запустить от шума от контакта груза. Ну и точку
> доставки, наверное, простая. То есть вот можем три запустить, но при том тоннель не запускаем, а
> именно в обсуждение запускаем. Просто я пока не очень представляю, да. То есть хочу понять
> реализацию, как что-то будет делать.

## state_changes

1. `NOW.open_calls`: заведён `c-research-g-1d84-tunnel-section-shape-001` — `to: research`,
   `parent: c-exec-g-1d84-tunnel-section-001`, полоса `игрок`, `ready`. Это РЕБЁНОК сборочного корня,
   а не второй корень полосы: WIP не тратит, артефактов не создаёт, продукт только читает.
2. `NOW.open_calls` `c-exec-g-1d84-tunnel-section-001`: `blocked` → `waiting`, ждёт возврата ребёнка и
   его слова по развилке «чем открывается участок». Слот не называется — он сам запретил запуск.
3. Создан артефакт наряда `work/c-research-g-1d84-tunnel-section-shape-001-call.md`.
4. `NOW.md` шапка `updated:` — на эту ногу.
5. `LOG.md` — одна строка.

Не тронуто: карточка, задачи, вырезы, полосы, WIP, `CHARTER.md`, `TREE.md`. Два сборочных наряда
(`c-exec-g-1d84-delivery-point-001`, `c-exec-g-1d84-world-noise-contact-001`) НЕ трогались — их
`unblock_when` требует, чтобы слот назвал владелец в сообщении с запуском, и это его ход, не ноги.

## captures

- Полосы `игрок` и `внешний-вид` после перенарезки держат тоннель и шум с погоней — идентификаторы
  разошлись с содержимым. Названо владельцу дважды, вердикта нет. Косметика, но он читает эти строки.
- `WIN-U3` несёт `slot/win-u3` = проба наборов плюс слияние `dev`; при следующей аренде ветку можно
  безопасно сбросить — работа лежит на `probe/free-kits-2026-08-06`.

## decisions_needed

Открыто и ждёт его: чем именно открывается тоннельный участок. Карточка оставила это открытым
СОЗНАТЕЛЬНО (критерий 10: «Чем именно открывается участок, здесь не решается»); разбор раскладывает
варианты и не выбирает.

## play_check

- Шаг 1: продукт, слоты, обе базы и подписанная карточка перечитаны из Git этой ногой; собственные два
  ложных утверждения предыдущего хода перемерены и сняты.
- Шаг 5: разрешение — его слова выше, дословно процитированные. Сохранён ровно объявленный им расклад:
  тоннель НЕ запускается, вместо него разбор; две сборки остаются его ходом.
- Границы `day`: карточка, задачи, вырезы, полосы и WIP не правились; `CHARTER`/`TREE` не тронуты;
  `knowledge/` не писалась.
- G1: занят один сборочный корень из четырёх; исследовательский ребёнок корнем не считается.

## log

g-1d84: владелец развёл три готовых корня — две сборки запускает как есть, а тоннель запускать
ЗАПРЕТИЛ и попросил разбор («тоннель не запускаем, а именно в обсуждение запускаем… хочу понять
реализацию»). Заведён `c-research-g-1d84-tunnel-section-shape-001` ребёнком сборочного корня, сам
корень `blocked` → `waiting`. Разбор осмыслен точным измерением: поставка `t-house-4` дала
`HousePassage` с именем, концами, координатами и `Width`, но БЕЗ единого поля про проходимость, а
задача требует прохода, чья проходимость меняется событием игры; развилка «чем открывается» оставлена
карточкой открытой и закрывается его словом. ИСПРАВЛЕНЫ ДВА СОБСТВЕННЫХ ЛОЖНЫХ УТВЕРЖДЕНИЯ предыдущего
хода: Направление НЕ отставало (`t-house-4` закрыта `be92daaf`, читалось состояние до коммита) и
наряды НЕ на мёртвой базе (обе уже несут `8ffe5a5d`, и все четыре названных ими файла между базами не
менялись ни байтом — перепроверено независимо). Перемерено попутно: оба дважды непоехавших решения
владельца доехали — `m_Enabled: 0` в `main` и терминальная квитанция `02-closed-by-owner.json` у
мёртвого корня.

## next

Владельцу: вставить разбор тоннеля в отдельный чат; запустить две сборки, назвав в сообщении слот
(`WIN-U2` и `WIN-U4` свободны и чисты). После возврата разбора — его слово по развилке, и только потом
сборочный наряд на тоннель получает слот.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-day-g-1d84-two-builds-and-tunnel-research-001.md
