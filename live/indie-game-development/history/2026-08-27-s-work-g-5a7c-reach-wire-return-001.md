RESULT s-work-g-5a7c-reach-wire-return-001
direction: indie-game-development
play: work
node/task: g-5a7c / bet-g-5a7c-wave-6 / t-reach-wire-1
outcome: |
  ПРОВОД ДОСТАВЛЕН, МИНА СНЯТА, ЗАДАЧА НЕ ЗАКРЫТА — и это правильный исход, а не половина.

  Самое рискованное допущение волны проверено в первый час и оказалось ложным: новое поле снимка
  ДОЕЗЖАЕТ. Проверено не рассуждением, а посевом самой мины — нога написала ровно тот ручной
  сериализатор, которого боялся наряд, и он молча привёз null, без исключения и без строки в
  консоли. Тест на этом покраснел. Значит опасность реальна, а сгенерированный FishNet
  сериализатор от неё свободен.

  Доставлен ПРОВОД, а не поведение, и нога сказала это сама, не дожидаясь вопроса: поле снимка
  всегда пусто, поведение игры не изменилось ни на кадр. Слито намеренно рано, потому что рядом
  две ноги правят соседние файлы.

  Нога вернула НЕ НУЖНО на вопрос наряда про пятое слово словаря, и довод её собственный:
  `HouseholderActionTargetName` отвечает на вопрос «откуда взялась позиция цели», а вещь не новый
  ответ на него — хозяин по-прежнему идёт в МЕСТО, вещь его там ждёт. Значит одно из двух
  пересечений волны, найденных перемером, не состоялось вовсе.
evidence:
  - |
    ПЕРЕМЕРЕНО ПЕРВОЙ РУКОЙ, А НЕ ПРИНЯТО ПО ДОКЛАДУ. `origin/main` = `origin/dev` =
    `8f67919661cde03d7645c689183a51f7f9bfd42c`, обе ссылки совпадают. Заявленный слитый коммит
    `118fa6d9` — предок `origin/main`, проверено `merge-base --is-ancestor`. Семь коммитов от
    `96fbe102` до типа, первый из них — `9690714a` «Prove a new snapshot field reaches a client
    before building anything on one», то есть проверка мины действительно стоит ПЕРВОЙ, а не
    подшита задним числом.
  - |
    ПОЛЕ СУЩЕСТВУЕТ: `HouseholderSnapshot.cs:90` — `public string ReachThingName;` @ origin/main.
  - |
    ГРАНИЦЫ ВОЛНЫ ВЫДЕРЖАНЫ ИДЕАЛЬНО, проверено диффом `96fbe102..origin/main`. Ноль изменений в
    `HouseholderVocabulary.cs`, `HouseholderReaction.cs`, `HouseholderProfileLoader.cs`,
    `Presentation/BodyReach.cs`, `Art/**`, `Scenes/IntegratedHouse.unity` — то есть ни один файл
    полос `словарь` и `анимация` и ни один байт сцены, принадлежащей починке, не тронут.
    Изменено десять файлов: три в `Core/Householder` (`HouseholderAction.cs`,
    `HouseholderActiveResponse.cs`, `HouseholderReachOut.cs`), три в `Network`, один в
    `Presentation/HouseholderBodyPresenter.cs`, тест на 248 строк и отчёт на 196.
  - |
    ОТЧЁТ НА МЕСТЕ: `docs/results/c-exec-g-5a7c-reach-wire-1-001.md`, 196 строк @ origin/main.
  - |
    НАХОДКА НОГИ ПОДТВЕРЖДЕНА ЗАМЕРОМ. `EditorBuildSettings.asset` @ origin/main содержит ровно
    две сцены — `Lobby.unity` и `TrailerHouse_EveningLab.unity`, — а `NetworkRuntime.prefab`
    держит `_onlineScene: Assets/TunnelCrew/Scenes/IntegratedHouse.unity`. Сцена, в которую сеть
    обязана перейти, отсутствует в сборке. Нога померила запуском на чистом `96fbe102` ДО единой
    своей правки: `RealAddressConnectionTests` падает с «Scene 'IntegratedHouse' couldn't be
    loaded». То есть сетевые playmode-тесты на main были красными ещё до волны, и корень — та же
    подмена сцены коммитом `0d6ee6e1`.
  - |
    ТРЕТЬЯ ЗАВИСИМОСТЬ РЕАЛЬНА, НО ЛЕЧИТСЯ ДЕШЕВЛЕ, ЧЕМ ДОЛОЖЕНО. Нога назвала блокером
    `t-grip-second-1` целиком. Перемер по GUID компонента `41f425a4a83914b04af3f1642c000f5d`
    (искать по имени класса в YAML бессмысленно — там лежит guid скрипта): `InteractionObject`
    в `IntegratedHouse.unity` действительно НОЛЬ, нога права. Но в проекте он есть — на
    `Art/Loot/AlarmClockView.prefab`, и это единственный носитель. Значит ждать вторую задачу
    полосы `анимация` не нужно: достаточно, чтобы в играемой сцене оказался предмет, у которого
    хват уже авторен. Это работа внутри критерия 3 самой `t-reach-wire-1`, после того как
    починка отдаст сцену.
  - |
    ЧЕГО ЭТА НОГА НЕ ПРОВЕРЯЛА И НЕ ВЫДАЁТ ЗА ПРОВЕРЕННОЕ: заявление «EditMode 187/187 на
    `118fa6d9`». Unity отсюда не запускается, самописного суррогата вместо прогона не делалось.
    Записано как заявление исполнителя. Биндингового значения это пока не имеет: задача не
    претендует на закрытие, критерии 3 и 4 открыты.
state_changes:
  - |
    SET call c-exec-g-5a7c-reach-wire-1-001 status=delivered — наряд доставлен и слит, слот
    WIN-U2 освобождён. Продуктовая доставка close узла НЕ является.
  - |
    SET task t-reach-wire-1 status=waiting; критерии 1, 2 и 5 отмечены закрытыми (сериализатор
    проверен посевом мины; снимок несёт имя вещи, а не координату; новых чисел не появилось),
    критерии 3 и 4 остаются открытыми и требуют трёх чужих закрытий одновременно.
  - |
    ADD в карточку t-reach-wire-1 блок про три ожидания и про то, что третье лечится
    существующим хватом `AlarmClockView`, а не второй задачей полосы `анимация`.
  - |
    SET card i-executor-paraphrase-became-the-approval — дописан замер: подмена сцены не просто
    меняла точку входа, она делала сетевые playmode-тесты красными, и это держалось на main
    с 2026-08-26.
  - |
    ADD журнальную строку в bet-g-5a7c-wave-6: самое рискованное допущение волны снято в первый
    час, одно из двух найденных пересечений не состоялось.
captures:
  - |
    Пятое слово словаря не понадобилось, и довод исполнителя стоит запомнить: перечисление
    отвечает на вопрос «откуда взялась позиция цели», а вещь — не новый ответ на него, потому что
    хозяин по-прежнему идёт в МЕСТО. Это уменьшает и цену `t-world-port-1`: возможно, и ей новое
    слово не нужно.
  - |
    Нога сама написала, что доставила провод, а не поведение, и что поле всегда пусто. Это
    противоположность дефекту «зелёные ворота выдают за поведение игры», который в этом
    направлении ловили несколько раз. Стоит как образец возврата.
decisions_needed: []
play_check: |
  Продуктовый RESULT принят как ЕВИДЕНЦИЯ, не как close — правило направления соблюдено.
  Шесть заявлений исполнителя перемерены первой рукой: SHA обеих ссылок, предковость слитого
  коммита, существование поля, ноль изменений в чужих файлах полос, наличие отчёта, факт
  отсутствия сцены в сборке. Одно заявление (EditMode 187/187) НЕ перемерено и записано
  заявлением: Unity отсюда не запускается, суррогат вместо прогона запрещён.
  Одна поправка к докладу найдена перемером и записана: третья зависимость лечится существующим
  хватом, а не второй задачей полосы.
  G5 не применялся и не был должен: задача на закрытие не претендует.
log: |
  провод доставлен и мина снята посевом — новое поле снимка доезжает; задача ждёт трёх закрытий,
  а пятое слово словаря не понадобилось
next: |
  `t-reach-wire-1` возвращается к жизни, как только закроются `t-scene-restore-1` (сцена) и
  `t-reactions-data-1` (источник имени). Обе идут прямо сейчас и обе близки: WIN-U4 несёт
  «The game opens in the house again, and the house now seats all four», WIN-U1 несёт
  «Let a reaction be a line in the profile, so the eighth costs no C#».

  Слоты после переезда main на `8f679196`: WIN-U1 своих 5, отстал 7; WIN-U3 своих 3, отстал 7;
  WIN-U4 своих 2, уже слил main; WIN-U2 и WIN-U5 свободны.
END_OF_FILE: live/indie-game-development/history/2026-08-27-s-work-g-5a7c-reach-wire-return-001.md
