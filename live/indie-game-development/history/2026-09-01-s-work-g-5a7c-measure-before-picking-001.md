# RESULT — s-work-g-5a7c-measure-before-picking-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-a-fact-knows-who-made-it-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — расширение починки не разрешено вслепую: числа самой ноги его опровергают, и вместо него выдан один дешёвый замер, решающий форму починки

close: light — because несущее перевыведено направлением первой рукой: обе точки отказа в коде найдены и прочитаны, ветка «та же комната — по прямой» и ветка Дейкстры по названным местам различены, и довод против расширения построен на числах самой ноги, а не на мнении.

owner_approved: |
  Не требовалось: выбор формы починки — инженерное решение. Владельцу сообщается фактом.

outcome: |
  **ГИПОТЕЗА НАПРАВЛЕНИЯ ПОДТВЕРЖДЕНА ЖИВЬЁМ: `FurnitureInTheWay = 3`.** Улика про молчаливый отказ
  пути перестала быть чтением кода и стала замером.

  **НО РАСШИРЕНИЕ, КОТОРОЕ ПРОСИТ НОГА, ОПРОВЕРГАЕТСЯ ЕЁ ЖЕ ЧИСЛАМИ.** Она просит разрешения
  «выбирать другую точку маршрута после отказа». Из точки `(2.915, 3.923)` **уже отказаны две
  разные цели**: `(-4.30, 3.80)` в прошлом прогоне и `(-0.50, 1.60)` в этом. Не одна недостижимая
  точка — **две подряд**. Если из этой точки не добраться никуда, перебор переберёт все и
  **замрёт снова**.

  **И ЕЁ СОБСТВЕННЫЙ НЕЗАВИСИМЫЙ РЕВЬЮЕР НАПИСАЛ РОВНО ЭТО** — «повторное зависание при исчерпании
  всех вариантов» — **и сказал «не готово».** Разрешить расширение значило бы разрешить починку,
  которую её же проверка объявила неготовой.

  **ГЛАВНОЕ, ЧТО НЕ СХОДИТСЯ И НА ЧТО НИКТО НЕ СМОТРЕЛ: ОН ТУДА ПРИШЁЛ.** С `(-4.44, 3.29)` на
  26.45 с до `(2.915, 3.923)` на 63.47 с. **Путь внутрь существовал.** Если наружу нет ни одного —
  **дефект не в целях, а в том, где он стоит.**

  **ГИПОТЕЗА НАПРАВЛЕНИЯ, ОБЪЯВЛЕННАЯ ГИПОТЕЗОЙ, А НЕ ДИАГНОЗОМ.** Внутрь его довела ветка «та же
  комната — иди по прямой» (`HouseWalk.cs:278-282`), поставив на сырую координату у тумбы; наружу
  маршрут строится Дейкстрой **по названным местам** (`:360-372`). Из сырой точки внутри следа
  мебели граф мест его не вытаскивает. **Сегодня направление уже дважды подало гипотезу как
  доказанное — здесь этого не повторено.**

  **ВМЕСТО РАЗРЕШЕНИЯ ВЫДАН ЗАМЕР, КОТОРЫЙ РЕШАЕТ ВСЁ И СТОИТ ДЁШЕВО.** Из замороженной точки
  попросить путь к пяти-шести разным местам и **отдельно назад, откуда он пришёл**:
  отказаны все, включая обратный — дефект в его положении, перебор точек бесполезен;
  часть достижима — дефект в выборе точки, и расширение **разрешено сразу**, с двумя условиями.

  **РАЗРЕШЕНИЕ ВЫДАНО УСЛОВНЫМ, А НЕ ОТКАЗАНО.** Нога не теряет ход: она делает замер и, если
  вышла вторая ветка, строит своё расширение в том же заходе. **Обязательной строкой приёмки стало
  то, что нашёл её ревьюер:** исчерпание вариантов не замораживает.

  **И ОТДЕЛЬНО ЗАПРЕЩЕНО ЛЕЧИТЬ СИМПТОМ.** Нога нашла в профиле **пять одинаковых walk-route** после
  единственного занятия — поэтому смена записи распорядка не меняет точку. Находка ценная, но
  сделать маршруты разными, чтобы яма перестала ловить, **запрещено**: полигон расходный, а дыра
  переедет в любой другой дом. Трогать профиль можно только чтобы **воспроизвести** дефект.

  **ПОРОГ МЕСТ ПОДНЯТ С ШЕСТИ ДО ДЕСЯТИ.** Шесть уже занято вместе с TTL, и замер может потребовать
  своего. Прежний порог остановил бы ногу на арифметике, а не на существе.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** на `slot/win-u2` = `b4b010c1` (тот же механизм ходьбы):

  - `HouseWalk.cs:22-35` — `enum HouseWalkRefusal`: `NoSuchPlace = 1`, `FurnitureInTheWay = 3`;
  - `HouseWalk.cs:278-282` — ветка «та же комната»: путь строится **прямой линией к сырой точке**,
    без участия названных мест;
  - `HouseWalk.cs:285-293` — если названных мест в доме нет вовсе, отказ выдаётся только когда цель
    в описанной комнате;
  - `HouseWalk.cs:360-372` — иначе Дейкстра **по названным местам**, и `last < 0` либо
    бесконечная сумма дают `FurnitureInTheWay`. **Отказ выносится про МАРШРУТ, а не про цель**,
    поэтому непроходимость может исходить и от его собственного положения;
  - `Householder.cs:1304` — единственный читатель `IsRefused` во всём продукте.

  **Из возврата ноги, как улика:** `FurnitureInTheWay = 3` живьём; TTL сохранён коммитом
  `4ecc2f82`; WIP в трёх файлах, шесть мест вместе с TTL; живой ход
  `26.45 с (-4.44, 3.29)` → `63.47 с (2.915, 3.923)` → `221.40 с (2.915, 3.923)`, цель
  `(-0.50, 1.60)`; в профиле пять одинаковых walk-route; тесты `745 / 744 / 1` после WIP;
  независимый ревьюер: «не готово», `elapsedSeconds <= 0`, повторное зависание при исчерпании.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-004` → слот **WIN-U4**, база `4ecc2f82` плюс WIP;
  - `work/2026-09-01-answer-measure-before-you-pick-another-point.md`.

  ЗАКРЫТО КАК ОТОЗВАННОЕ (1 наряд):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-003` → `dropped`: причина подтверждена живьём, но
    предложенное расширение опровергается числами самой ноги; переиздан как `-004`.

  ОБНОВЛЕНО (1 улика, НЕ закрыта):
  - `i-a-refused-walk-is-heard-by-nobody-001` — причина подтверждена живьём; таблица доведена до
    **пяти независимых подтверждений одной точки**; добавлено решающее уточнение «две разные цели
    отказаны, а внутрь он пришёл»; гипотеза про две ветки маршрутизации записана **как гипотеза**.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-a-fact-knows-who-made-it-1` остаётся `active`;
  - `t-the-bench-opens-quiet-1` остаётся `active` в WIN-U2;
  - `t-everything-lands-1` остаётся `open` и ждёт конца замирания.

captures: |
  - **НОГА ПРИНЕСЛА ЧИСЛА, КОТОРЫЕ ОПРОВЕРГАЮТ ЕЁ ЖЕ ПРЕДЛОЖЕНИЕ, И НЕ ЗАМЕТИЛА ЭТОГО.** Две
    отказанные цели из одной точки лежали в её собственной таблице. **Правило-кандидат: у просьбы
    расширить починку первым делом искать опровержение В ЧИСЛАХ ТОЙ ЖЕ НОГИ — они уже собраны и
    ничего не стоят.**
  - **ЕЁ НЕЗАВИСИМЫЙ РЕВЬЮЕР СКАЗАЛ «НЕ ГОТОВО», А ПРОСЬБА ПРИШЛА ВСЁ РАВНО.** Замечание про
    повторное зависание было в возврате, но не помешало просить разрешение. **Собственная проверка,
    сказавшая «не готово», должна останавливать просьбу, а не сопровождать её.**
  - **«ОН ТУДА ПРИШЁЛ» — ВОПРОС, КОТОРОГО НИКТО НЕ ЗАДАЛ ЗА ЧЕТЫРЕ ЗАХОДА.** Путь внутрь
    существовал; значит асимметрия. **Правило-кандидат: у застрявшего спрашивать не «куда он не
    может», а «как он сюда попал» — вход доказывает, что связность была.**
  - **ПОРОГ ЧИСЛА МЕСТ ЧУТЬ НЕ ОСТАНОВИЛ НОГУ НА АРИФМЕТИКЕ.** Шесть из шести занято, а работа не
    сделана. **Порог должен считать РАЗМАХ предмета, а не строки; когда он начинает мешать замеру,
    его поднимают вслух.**

decisions_needed: |
  Ни одного владельческого.

play_check: |
  1. OPEN — СТОП ноги с просьбой расширить починку.
  2. WORK — обе точки отказа найдены и прочитаны в коде; различены две ветки маршрутизации;
     довод против построен на числах самой ноги и на её собственном ревью; вместо отказа выдан
     развилочный замер, чтобы нога не теряла ход; гипотеза объявлена гипотезой.
  3. (owner) — не запрашивался: форма починки инженерная.
  4. CLOSE — RESULT, применение, коммит; переизданный наряд уходит владельцу.

log: |
  расширение починки не разрешено вслепую: нога просила «выбирать другую точку», но её же числа это
  опровергают — из замороженной точки отказаны две разные цели, а её независимый ревьюер предсказал
  повторное зависание при исчерпании вариантов; главное, чего не заметил никто за четыре захода: он
  туда ПРИШЁЛ, значит путь внутрь был, и дефект скорее в его положении, чем в целях; вместо отказа
  выдан развилочный замер достижимости, решающий форму починки за один заход

next: |
  Переизданный наряд `c-exec-g-5a7c-a-fact-knows-who-made-it-1-004` уходит владельцу для
  **WIN-U4**: сначала замер достижимости, потом починка по его ветке.

  Волна: голова закрыта, ходьба мертва, стенд делается тихим в U2, замирание меряется в U4,
  публикация ждёт только замирания.
