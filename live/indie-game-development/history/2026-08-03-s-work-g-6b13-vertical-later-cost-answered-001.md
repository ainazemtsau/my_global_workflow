# RESULT — s-work-g-6b13-vertical-later-cost-answered-001

call: none — прямой вопрос владельца по его постоянному правилу «расширяем, а не переписываем»
direction: indie-game-development
track: переноска
play: work
node/task: g-6b13 / a-4b
date: 2026-08-03

## outcome

Владелец спросил дословно: «сейчас может быть вырезано, но мне главное, чтобы мы потом могли
добавить эту вертикальность. Это не будет проблемой?» Ответ дан ПО ПРОЧИТАННОМУ КОДУ на
`origin/main`, а не по памяти и не по отчёту исполнителя, и записан в состояние, чтобы его прочла
та нога, которая когда-нибудь возьмёт вертикаль.

Ответ по существу: переписывать устройство не придётся — вертикаль остаётся внутри папки груза и её
адаптера. Но и «снять заморозку» не выйдет: плоскость выражена ТИПАМИ, а одно место (сведение рук в
одну тягу) придётся переписать, а не дополнить.

Из ответа вышла одна дешёвая правка вперёд, и она внесена: в `a-4b` записано не запирать наклон
камеры. Это единственное место, где плоскость могла впервые вылезти за пределы папки груза.

## evidence

Прочитано первой рукой на `origin/main` = `8da64943`:

- `Core/Cargo/CargoState.cs` — `X`, `Z`, `Yaw`, `HomeX`, `HomeZ`, `Integrity`. Высоты нет.
- `Core/Cargo/CargoBodyPort.cs` — `CargoBodyPose(X, Z, Yaw)`; `ICargoBodyPort` ровно три метода:
  `TryReadPose`, `ApplyPull`, `ReadContact`. Движок не спрашивают о держателях и не рассказывают о них.
- `Core/Cargo/CargoPull.cs` — `ForceX/ForceZ`, `PointX/PointZ`, скалярный `Torque`, и его
  собственный комментарий объясняет двумерный приём: две руки за разные концы в разные стороны дают
  нулевую силу и ненулевой поворот, и «ни одна одиночная сила ни в какой точке этого не скажет».
- `Core/Cargo/CargoHold.cs` — `GripX/GripZ`, `TargetX/TargetZ`, `MaxLength`, `Strength`, `CanEvict`.
- `Core/Cargo/CargoGeometry.cs` — прямой и обратный перенос через `sin/cos` ОДНОГО угла.
- `Core/Cargo/CargoHaul.cs` — `Reduce(cargo, holds, rules, velocityX, velocityZ, yawRate)`:
  скалярная угловая скорость.
- `Network/CargoBody.cs:56-58` — `rigidbody.constraints = FreezePositionY | FreezeRotationX |
  FreezeRotationZ`, с комментарием «Flat by constraint». Убирается одной строкой — и именно поэтому
  это ловушка: `TryReadPose` высоту не вернёт, в снимок она не поедет, на клиентах груз останется
  на полу. Молчаливое расхождение вместо вертикали.
- `Core/Movement/PlayerState.cs` — только `X` и `Z`. То есть при его же чтении вертикали (груз
  поднимается, игрок стоит на полу) ходок и его проверка проходимости не трогаются вообще.

## state_changes

- `NOW.md/decisions/d-first-person-before-the-build-001`: новое поле `vertical_later_cost` —
  полный ответ по прочитанному коду: что не двинется, что тронется механически, единственное место
  под переписывание, ловушка «разморозить одной строкой», почему мыши не трогаются, и решение
  сессии не класть третью координату про запас.
- `NOW.md/tasks/a-4b`: в `constraint` добавлен пункт (5) — наклон камеры не запирать, взгляд умеет
  вверх и вниз; помечено как ИНЖЕНЕРНОЕ решение сессии, а не его слово, со ссылкой на обоснование.
- `updated` на эту сессию; препенд LOG receipt; полный RESULT в history.
- НЕ ТРОНУТО: `open_calls`, порядок задач, состав задач, полоса хозяина, TREE, CHARTER, knowledge и
  продуктовый репозиторий. Вертикаль задачей не назначена и закрытой не объявлена.

## captures

- Плоскость, выраженная ТИПАМИ, а не соглашением, — это довод ЗА текущее устройство: компилятор сам
  выдаёт полный список мест, которые надо тронуть. Стоит запомнить как приём: где решение может
  однажды развернуться, лучше типы, чем флаг.
- Обратная сторона того же приёма: заранее положенное поле, которого никто не касается и ничто не
  проверяет, протухает молча. Поэтому «про запас» здесь отвергнуто, хотя пять швов a-4 положены
  именно про запас — разница в том, что у тех швов есть писатель и снимок, а у пустой высоты не
  было бы ни одного.

## decisions_needed

Нет. Вопрос был про цену, ответ дан; вертикаль остаётся вырезанной по его же слову, и назначать её
задачей эта нога права не имеет.

Остаётся висеть прежний, ему уже вынесенный: вести ли закрытие переноски и камеру параллельно.
Рекомендация не изменилась — закрыть переноску его пятью минутами.

## play_check

- 1 recite: done — вопрос перечитан вместе с его постоянным правилом «мы не переделываем, мы
  расширяем» (`i-owner-standing-rule-extend-never-rebuild-001`), которое он этим вопросом и
  применяет.
- 2 owner inputs (owner): done — вопрос его, ответ технический и по правилу
  «делегированные технические вопросы решает нога» решён ногой; его слова про вырезанное не
  переоткрыты.
- 3 do the work: done — восемь файлов прочитаны на опубликованном tip; ответ построен на них, а не
  на отчёте исполнителя и не на памяти.
- 4 self-check: ответ проверяем — каждое утверждение привязано к файлу и строке; ловушка с
  разморозкой названа явно, чтобы её не повторил тот, кто прочтёт «flat by constraint» и решит, что
  дело в одной строке.
- 5 close: done — из ответа вышла ровно одна дешёвая правка вперёд, и она внесена в задачу, а не
  оставлена в чате.

## log

g-6b13/a-4b: владелец спросил, не будет ли проблемой добавить вертикальность потом. Ответ дан по
прочитанному коду: устройство не переписывается, вертикаль сидит внутри папки груза, но плоскость
выражена ТИПАМИ, одно место (сведение рук в одну тягу) пишется заново, а «разморозить одной строкой»
даёт молчаливое расхождение. Записано в решение; в a-4b внесена дешёвая правка вперёд — не запирать
наклон камеры.

## next

Полоса переноски продолжается прежним корнем: `c-work-a4-close-verification-001` в НОВОМ физическом
чате.

END_OF_FILE: live/indie-game-development/history/2026-08-03-s-work-g-6b13-vertical-later-cost-answered-001.md
