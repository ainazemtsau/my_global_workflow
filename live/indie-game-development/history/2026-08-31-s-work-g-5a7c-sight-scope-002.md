# RESULT — s-work-g-5a7c-sight-scope-002

direction: indie-game-development
play: work
node/task: g-5a7c / t-sight-thing-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: scope — порог правок поднят с пяти до восьми, потому что находка рецензента бьёт в уже одобренную строку приёмки; оставшиеся двенадцать признаны правильным поведением по замеру настроек

owner_approved: |
  Отдельного слова не требуется и не выдумывается. Поднятие порога — операционное решение наряда,
  а не изменение предмета: порог в пять мест был бюджетным сторожем, написанным направлением.
  Работа, которую покупают три лишних места, **уже требуется строкой приёмки 4**, одобренной
  владельцем при нарезке волны 2026-08-30.

  Ни одна строка `done_when` не изменена и ни одна не добавлена.

outcome: |
  **ЧИСЛО СДВИНУЛОСЬ: 0 ИЗ 14 → 2 ИЗ 14, И ОБЕ НАЗВАННЫЕ ПРИЧИНЫ СНЯТЫ.** `Mug` перешёл из
  `HE IS STANDING ON IT` в `SEES IT (3 of 3)`, `Tin can` — из `BLOCKED (0 of 3)` туда же. Порог
  смерти волны не сработал и не должен был: он написан на «всё ещё ноль», а не на «мало».

  **ПОРОГ ПРАВОК СРАБОТАЛ КАК ЗАДУМАН, И ЭТО ТРЕТИЙ ХОРОШИЙ СТОП ЭТОЙ НОГИ.** Пять мест —
  бюджетный сторож: его работа заставить остановиться и назвать число, когда работа оказалась
  больше ожидаемой. Исполнитель остановился на пятом, не тронул `commit`, удержал лизу и принёс
  разбор. Ни одного молчаливого расширения за всю ногу.

  **НАХОДКА РЕЦЕНЗЕНТА ПОДТВЕРЖДЕНА ЧТЕНИЕМ ПРАВКИ И ОКАЗАЛАСЬ СИЛЬНЕЕ, ЧЕМ ОН НАПИСАЛ.** Он
  назвал её «недоказанным риском для зрения на мышей». Направление считает риск **конкретным и
  называемым**. Сегодняшняя проверка терминальности спрашивает «это вообще какая-нибудь вещь, и
  лежит ли она под конвертом пробы?» — и не спрашивает «это ТА САМАЯ вещь?». Поскольку
  `RayIsClear` общий с живой фичей, **мышь, спрятавшаяся за кружкой, станет видна сквозь кружку.**
  Это прямое попадание в строку приёмки 4 — «зрение на мышей не изменилось, доказывается тестом,
  а не словами», — и «неопровергнутый риск» её не закрывает.

  **ПОЭТОМУ ВОСЕМЬ МЕСТ — НЕ УСТУПКА, А ТО, ЧТО УЖЕ ТРЕБУЕТ ЕГО СОБСТВЕННАЯ ПРИЁМКА.** Три лишних
  места покупают исключение **по ЛИЧНОСТИ вместо исключения по ГЕОМЕТРИИ**: разницу между верным и
  неверным, а не между хорошим и лучшим. Находку велено ПОЧИНИТЬ, а не закрыть: это классический
  костыль, верный ровно там, где его мерили, и молча неверный у соседа — класс, пойманный сегодня
  уже дважды.

  **ОСТАВШИЕСЯ ДВЕНАДЦАТЬ — ПРАВИЛЬНОЕ ПОВЕДЕНИЕ, И ЭТО ЗАМЕР, А НЕ УСПОКОЕНИЕ.** Перемерено в
  `HouseholderSightSettings.asset`: дальность **6.5 м**, сектор **100°**. Хозяин стоит в одной
  точке и смотрит в одну сторону; шесть вещей дальше 6.5 м и шесть вне стоградусного конуса — это
  зрение, которое работает. **Хозяин, видящий все четырнадцать вещей дома с одного места, был бы
  дефектом.** Исполнителю прямо велено за ними не гнаться.

  Годится ли дальность 6.5 м именно для этого дома — **вопрос настройки, а не дефекта**, и он
  принадлежит `t-bench-by-fact-1`, где полигон проходит владелец. Правка ассета остаётся
  запрещённой.

evidence: |
  Перемерено направлением в рабочем дереве `C:\projects\Unity\GasCoopGame_win-u1`
  (незакоммиченная правка) и на продуктовом `main` = `b22c00a2`:

  - `git status --porcelain` — три изменённых файла, `commit` не выполнялся;
    `git diff --stat` — 488 вставок, 10 удалений, из них 433 вставки в файле тестов;
  - `HouseholderSightSource.cs`, новый блок в `RayIsClear`: условие терминальности —
    `collider.GetComponentInParent<CargoBodyContact>() != null` плюс сравнение границ коллайдера
    с конвертом пробы по X и Z. **Идентификатор предмета в условии не участвует** — находка
    рецензента подтверждена;
  - `HouseholderSightSource.cs:~172-220` — снятие `OnTopOfIt`: введён `hasPlanarDirection`,
    и при совпадении по X/Z направление берётся из взгляда, а не из нулевого вектора к цели.
    Форма правильная;
  - `Settings/HouseholderSightSettings.asset` — `_range: 6.5`, `_sectorDegrees: 100`,
    `_bodyPointCount: 5`, `_ratBodyRadius: 0.075`.

  Из его возврата, как улика: `HE SEES 2 OF 14 THINGS`, разбор до правки (самоперекрытие 1,
  `OnTopOfIt` 1, `TOO FAR` 6, `BEHIND HIM` 6), четыре targeted Unity-теста зелёные,
  headless 725/725, routine gate зелёный, лиза WIN-U1 удержана.

state_changes: |
  ДОПИСАНО (1 документ наряда):
  - `work/2026-08-31-call-sight-thing-1.md` — раздел «ДОПОЛНЕНИЕ 2: порог поднят до восьми,
    находка рецензента чинится, а не закрывается». Прежний текст и первое дополнение не тронуты.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-sight-thing-1` остаётся `active`, `c-exec-g-5a7c-sight-thing-1-001` остаётся `ready`.
  - Новый порог: **восемь мест**. Новый СТОП: восьми не хватило — остановка, **не ползти к
    двенадцати**; третье расширение подряд означало бы, что задача понята неверно.

captures: |
  - **ПОРОГ, НАПИСАННЫЙ ЧИСЛОМ, ОКАЗАЛСЯ ПОЛЕЗНЕЕ ПОРОГА, НАПИСАННОГО ПРАВИЛОМ.** Утром
    направление заменило такой же порог в полосе «почему» правилом («одно слово на один различимый
    отказ»), потому что число было взято из воздуха. Здесь число из воздуха сработало **лучше**:
    оно ничего не запрещало, оно заставило принести разбор в тот момент, когда разбор ещё дёшев.
    Вывод не «числа лучше правил», а: **порог полезен ровно настолько, насколько дёшево его
    пересмотреть**, и пересмотр обязан быть однократным решением дома, а не переговорами.
  - **РЕЦЕНЗЕНТ НЕДООЦЕНИЛ СОБСТВЕННУЮ НАХОДКУ, И ЭТО СТОИЛО БЫ СТРОКИ ПРИЁМКИ.** «Недоказанный
    риск» звучит как то, что можно закрыть обсуждением; «мышь видна сквозь кружку» — нет. Разница
    только в формулировке, и она решает, чинят находку или закрывают. Приём на будущее:
    **требовать от рецензента назвать риск сценарием, а не оценкой.**

decisions_needed: |
  Ни одного. Владельцу нечего выбирать: поднятие порога служит уже одобренной строке 4, а
  оставшиеся двенадцать вещей — правильное поведение по замеру настроек.

play_check: |
  1. OPEN — возвращённый СТОП по идущей ноге; приёмка перечитана построчно ДО ответа.
  2. WORK — правка прочитана в его дереве, находка рецензента подтверждена в коде, настройки
     зрения перемерены на `main`; отчёт читался как улика.
  3. (owner) — не требуется: порог написан направлением, работа требуется одобренной строкой.
  4. CLOSE — RESULT, применение, коммит; нога зрения продолжается.

log: |
  порог правок зрения поднят с пяти до восьми: находка рецензента подтверждена чтением кода и
  оказалась сильнее его формулировки — исключение идёт по геометрии, а не по личности, значит мышь
  за кружкой станет видна сквозь кружку, и это бьёт в строку приёмки про мышей; оставшиеся
  двенадцать вещей признаны правильным поведением по замеру дальности 6.5 м и сектора 100°

next: |
  Ответ уходит в WIN-U1. Идут три ноги: зрение U1, «почему» U3, ревизия улик U2.
  WIN-U4 свободен намеренно.

  Зрение остаётся критическим путём: его возврат разблокирует `t-morning-content-1`,
  `t-carry-back-1` и `t-bench-by-fact-1`.
