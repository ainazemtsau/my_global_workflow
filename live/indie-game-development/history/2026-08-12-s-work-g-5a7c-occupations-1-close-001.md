# RESULT — s-work-g-5a7c-occupations-1-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-occupations-1
track: householder-life
date: 2026-08-12
close: owner-verdict — светлый путь этой задаче был объявлен недоступным до работы: строку «в
  собранной игре видно, что хозяин занят делом» судит глаз. Свежий чат не открывался и не нужен:
  суждение вынес владелец собственным прогоном и оно лежит в закоммиченном отчёте вместе с его
  оговоркой. Всё механически проверяемое — деревья двух откатов, состав диффа, нулевой дифф
  каталога реакций, содержимое опубликованного профиля, прогон — перевыведено ЭТОЙ ногой из байтов.

owner_approved: |
  «завершена c-exec-g-5a7c-occupations-1-001» — 2026-08-12.

  И его вердикт в закоммиченном отчёте §manual-acceptance: кандидат прогнан в Unity; постоянная
  общая надпись `PerformRoutineOccupation` в инспекторе принята **временно ради закрытия этой
  задачи**, и он направил задачу к закрытию, потребовав, чтобы дальше публиковался понятный
  настоящий статус занятия и фазы — как минимум отличающий готовку от мытья.

outcome: |
  **`t-occupations-1` ЗАКРЫТА `done`** на опубликованной голове `4fc04b4c` (`origin/main` =
  `origin/dev`).

  **У ХОЗЯИНА ПОЯВИЛАСЬ ЖИЗНЬ, И ОНА ЖИВЁТ В ФАЙЛЕ.** Опубликованный
  `default.householder.json` больше не содержит `walk-route` / `wait-at-route-point` в распорядке.
  Вместо них — занятия с фазами, прочитанные мной прямо из опубликованного файла:

  - **`cook`** — `prepare-food` 12 с (прерываемо), **`secure-hot-stove` 5 с (НЕ прерываемо)**,
    `serve-food` 8 с (прерываемо);
  - **`wash`** — `wash-up` 10 с, `dry-off` 5 с.

  **ПРАВИЛО ЕГО ДОКУМЕНТА ИСПОЛНЕНО БУКВАЛЬНО.** Он писал: «если на плите что-то горит, он не должен
  по первому шуму мгновенно бросить всё и побежать за крысами: сначала он физически обезопасит
  плиту». Фаза `secure-hot-stove` — единственная непрерываемая из пяти. Это ровно та строка, ради
  которой задача и заводилась, и она видна в данных, а не в обещании.

  **ЗАНЯТИЯ ПРИВЯЗАНЫ К РОЛЯМ ОПУБЛИКОВАННОГО ДОМА:** `cook` к роли `cook` на кухне, `wash` к
  спальне. То есть цепь «шов дал роли → дом их заполнил → занятия ими пользуются» замкнулась за
  один день, тремя разными задачами в трёх слотах.

  **ОТКАТ ИСПОЛНЕН ДВАЖДЫ И ОБА РАЗА СОШЁЛСЯ — ДЕВЯТЫЙ РАЗ ПОДРЯД В ЭТОМ УЗЛЕ.** Деревья `7f1e5984`
  и `656bacba` оба равны `64a0e883…` = дереву базы `0618184f`.

  **ГРАНИЦЫ ДЕРЖАЛИСЬ:** ноль сцен и префабов в диффе, ноль файлов чужих дорожек (`Core/Cargo/`,
  `Presentation/`, `Art/`, `World/`) — при том, что параллельно в WIN-U1 идёт лут ровно по этим
  путям. Каталог пяти реакций — дифф **ноль строк**.

  ## ЧТО ОН ПРИНЯЛ ВРЕМЕННО, И ЭТО ЕГО УКАЗАНИЕ, А НЕ ДЕФЕКТ

  Прогнав кандидата, он увидел, что наружу занятия выходят **одной общей надписью**: инспектор
  показывает `PerformRoutineOccupation` независимо от того, готовит хозяин или моется. Различие
  живёт в профиле и в ядре, но не в наблюдаемом мире.

  Он принял это **временно, ради закрытия задачи**, и прямо потребовал: дальше должен публиковаться
  понятный настоящий статус занятия и фазы, как минимум отличающий готовку от мытья. Заведена
  `i-occupation-status-is-generic-outside-core-001` с его требованием; отложенную работу закрывающая
  транзакция в себя не включала, и Направление владеет её CALL.

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ.**

  - `git ls-remote origin` → `main` = `dev` = `4fc04b4c`.
  - Девять коммитов сверх `0618184f`: работа `cef6ab91`, revert `7f1e5984`, reapply `f7360aca`,
    второй revert `656bacba` и его снятие `30f1a484`, затем документные, включая
    `faf9e811 Record temporary occupation status acceptance`.
  - Деревья: база `0618184f` → `64a0e883…`; `7f1e5984` → `64a0e883…`; `656bacba` → `64a0e883…`
    (**оба совпали**).
  - `.unity` и `.prefab` в диффе → **0**; файлы чужих дорожек → **0**.
  - `git diff … -- HouseholderReaction.cs | wc -l` → **0**; пять реакций целы.
  - Опубликованный `default.householder.json` прочитан: занятия `cook` и `wash`, пять фаз,
    непрерываемая ровно одна — `secure-hot-stove`.
  - `dotnet test … -c Release`, прогнано ЗДЕСЬ на выгруженном дереве публикации → **246 passed,
    0 failed, 0 skipped** против **239**; новый файл тестов на 682 строки.
  - Слот WIN-U3 отпущен: `head 4fc04b4c`, `AVAILABLE`, `lease none`.
  - Отчёт прочитан целиком, включая §manual-acceptance и абзац про общую надпись.

state_changes: |
  1. `t-occupations-1`: `active → done`, журнальная строка, перенесена в `cards/closed/`.
  2. `c-exec-g-5a7c-occupations-1-001`: `ready → done`, журнальная строка, перенесён в
     `cards/closed/`.
  3. Создана `i-occupation-status-is-generic-outside-core-001` — `route: work`, несёт его
     требование про настоящий видимый статус занятия и фазы.
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-occupations-1-close-001.md`.

  `t-sight-1` дополнительно ничем не блокируется: `Householder.cs` освободился. Статус у неё уже
  `open`, менять нечего.

captures:
  - |
    ЦЕПЬ ИЗ ТРЁХ ЗАДАЧ ЗАМКНУЛАСЬ ЗА ОДИН ДЕНЬ И В ТРЁХ РАЗНЫХ СЛОТАХ: шов дал понятие роли, дом
    заполнил роли, занятия ими воспользовались. Ни одна из трёх не открывала файлы двух других. Это
    первое подтверждение, что форма «дорожки по владению файлами» работает не только на бумаге —
    стоит предъявить это на review ставки против довода про цену параллели.
  - |
    ВСЕ ТРИ ЕГО СЕГОДНЯШНИХ ЗАМЕЧАНИЯ ОДНОГО РОДА: груз проходит сквозь груз, хозяин задевает косяк,
    занятие не видно снаружи. Каждое — про то, что построенное не ПРОЯВЛЯЕТСЯ в наблюдаемом мире,
    хотя в данных и в ядре оно есть. Тесты этого класса не ловят по построению. Для review ставки
    это один вопрос, а не три: чем волна доказывает, что построенное видно в игре.

decisions_needed: []

play_check:
  - 1 recite — done. Семь строк карточки и три строки наряда пересказаны.
  - 2 owner inputs — исполнены: правило фаз он дал документом, вердикт по прогону дал сам, состав дел ограничен минимумом его же центрального кейса и им не оспорен.
  - 3 do the work — приёмка возврата.
  - 4 self-check — done. Каждая строка перевыведена здесь из байтов и прогона; его временная оговорка не засчитана как полное исполнение, а вынесена уликой с его требованием.
  - 5 close — done, маршрутом owner-verdict; основание в поле `close`.
  - G5 — выполнен: суждение вынес владелец, механика перевыведена ногой.
  - G7 — done. Ни одного игрового решения не принято.
  - G10 — done. RESULT финальный.

log: t-occupations-1 закрыта ЕГО ВЕРДИКТОМ на 4fc04b4c — у хозяина появилась жизнь и она живёт в файле: опубликованный профиль больше не ходит по маршруту, а ГОТОВИТ на кухне и МОЕТСЯ, причём правило его документа исполнено буквально, фаза secure-hot-stove единственная из пяти помечена непрерываемой, то есть горящую плиту он сначала обезопасит и только потом побежит; занятия привязаны к ролям опубликованного дома, и цепь «шов дал роли → дом их заполнил → занятия ими воспользовались» замкнулась за один день тремя задачами в трёх слотах, ни одна не открыла файлы двух других; откат исполнен дважды и оба раза сошёлся, девятый раз подряд; 246/246 против 239, ноль сцен и ноль чужих файлов при идущем параллельно луте, каталог реакций дифф в ноль строк; и он принял ВРЕМЕННО то, что наружу занятия выходят одной общей надписью, потребовав дальше настоящий видимый статус занятия и фазы — заведена улика с его требованием, а не засчитано как сделанное

next: |
  **Свободны и без нарядов:** `t-sight-1` (`Householder.cs` освободился) и `t-house-asset-1`.

  Идёт: лут в WIN-U1, несёт райдером его решение про твёрдость.

  Осталось после лута: `t-handlers-1` ждёт зрения, `t-lab-2` идёт последней.

  Для review ставки накопилось три его замечания одного рода — груз сквозь груз, хозяин в косяк,
  занятие не видно снаружи. Это один вопрос: чем волна доказывает, что построенное ВИДНО в игре.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-occupations-1-close-001.md
