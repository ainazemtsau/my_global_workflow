# RESULT — s-work-g-5a7c-body-1-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-body-1
track: body
date: 2026-08-12
close: owner-verdict — НЕ светлый путь и НЕ свежий чат опровержения, и это названо честно.
  Светлое закрытие этой задаче было объявлено недоступным ещё до работы: строку «работают стойка,
  ходьба и поворот» байты не доказывают. Свежий чат тоже не нужен и не открывался: он существует,
  чтобы вынести суждение, которого никто не выносил, а здесь суждение ВЫНЕС ЕДИНСТВЕННЫЙ, КТО МОЖЕТ
  — владелец, собственным прогоном, и его вердикт лежит в закоммиченном отчёте. Всё механически
  проверяемое — деревья двух откатов, состав диффа, целость трёх предыдущих задач, прогон —
  перевыведено ЭТОЙ ногой из байтов.

owner_approved: |
  «завершилась c-exec-g-5a7c-body-1-001» — 2026-08-12.

  И его собственный прогон, записанный в закоммиченный отчёт
  `docs/results/c-exec-g-5a7c-body-1-001.md` §Manual-acceptance: «The owner completed the live PROBA
  loop on 2026-08-12… The owner confirmed that skeletal animation is present, judged the clip/model
  quality poor but acceptable because this body is deliberately a placeholder, and explicitly closed
  the task.»

outcome: |
  **`t-body-1` ЗАКРЫТА `done`** на опубликованной голове
  `8c3bb8af16e40357055569092fe1f98b2eef18aa` (`origin/main` = `origin/dev`).

  **ЕГО ГЛАЗ НАШЁЛ НАСТОЯЩИЙ ДЕФЕКТ, И ЭТО ГЛАВНОЕ СОБЫТИЕ ЗАДАЧИ.** Живой осмотр показал
  инициализированный Animator со всеми тремя параметрами — то есть по приборам всё было хорошо. А
  прогон владельца показал, что **тело едет боком с нулевым поворотом и сбрасывает ходьбу в стойку
  между сетевыми выборками**. Исполнитель починил в той же сессии: поворот выводится из ненулевого
  полученного перемещения, последнее направление сохраняется в покое, измеренная скорость ходьбы
  держится `0.12` с между выборками. Это ровно тот случай, ради которого правило «зелёные ворота не
  компилируют игру» и существует.

  **ПЕРЕБАЗИРОВАНИЕ ПРОШЛО И НИЧЕГО НЕ СТЁРЛО.** Опасность, которую я нашла в прошлый заход, снята
  по факту: на опубликованной голове целы файлы всех трёх ранее закрытых задач — тесты шва
  (`HouseholderPlaceRoleTests`), тесты «места как факта» (`HouseholderRoomFactTests`) и предмет с
  состоянием (`CargoThing`). Ни одного удаления чужой работы.

  **ПОРЯДОК СОБЛЮДЁН ТОТ, КОТОРЫЙ Я ПОСТАВИЛА:** сначала перебазирование, потом его глаз, потом
  публикация. Тело и шов он смотрел уже вместе.

  **ОТКАТ ИСПОЛНЕН ДВАЖДЫ И ОБА РАЗА СОШЁЛСЯ — СЕДЬМОЙ РАЗ ПОДРЯД В ЭТОМ УЗЛЕ.** Деревья `4b3fcbe2`
  и `89e8a16f` оба равны `df6f95fc…` = дереву базы `c1d8d384`.

  **ГРАНИЦЫ ДЕРЖАЛИСЬ:** ноль сцен в диффе, ноль файлов чужих дорожек (`Core/House/`, `Core/Cargo/`,
  `Profiles/`, `ThreeRoomHouse`). Работа целиком в новом модуле `Assets/TunnelCrew/Presentation/**`,
  в `PresentationSettings`, в префабе тела хозяина и в арте.

  **ЧТО ЗАКРЫТО ПО СУЩЕСТВУ.** Примитивы ушли: капсула и куб больше не строятся кодом, встроенная
  капсула из префаба хозяина убрана. Модель настоящая, CC0-1.0, лицензия и источник рядом. Замена —
  сменой одного поля данных, без правки скриптов и сцен. Масштаб держится: налётчик `0.159998` м,
  хозяин `1.999997` м, коллайдер цел, root motion выключен, своё тело на своём экране не рисуется.

  ## ЧТО НЕ ПРОВЕРЕНО, И Я ЭТО НАЗЫВАЮ, А НЕ ЗАМАЛЧИВАЮ

  Строка 6 карточки требовала «четыре копии по-прежнему подключаются и играют». **Прогон был
  локальный** — отчёт говорит «After stopping the other Unity Editor», о четырёх копиях не пишет.

  Почему это не повод не закрывать: во-первых, «тело — презентация, а не авторитет» доказывается
  байтами и доказано (импортированные коллайдеры, тела, камеры и слушатели выключены, root motion
  off, авторитетный слой не тронут); во-вторых, гонять четверых ради регрессии презентации — не тот
  порог, который стоит вешать на владельца, и это согласуется с тем, как он реально проверяет.
  Названо здесь, чтобы `review` волны знало, что этой проверки не было.

  **Качество заглушки он назвал сам:** «poor but acceptable because this body is deliberately a
  placeholder». Никакого «поставленного облика» задача не заявляет.

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ.**

  - `git ls-remote origin` → `main` = `dev` = `8c3bb8af`.
  - Девять коммитов сверх `c1d8d384`: работа `db0fec06`, revert `4b3fcbe2`, reapply `f587f2a3`,
    **починка по глазу владельца `3ccc866b`**, второй revert `89e8a16f` и его снятие `c7b7ec9b`,
    затем три документных.
  - Деревья: база `c1d8d384` → `df6f95fc…`; `4b3fcbe2` → `df6f95fc…`; `89e8a16f` → `df6f95fc…`
    (**оба совпали**).
  - Целость предыдущих задач: `git ls-tree -r origin/main` находит `HouseholderPlaceRoleTests`,
    `HouseholderRoomFactTests` и `CargoThing.cs` — **семь совпадений, ничего не удалено**.
  - `.unity` в диффе → **0**; файлов чужих дорожек → **0**.
  - `git diff --stat c1d8d384 origin/main` → 34 файла, +1616 / −126.
  - `dotnet test … -c Release`, прогнано ЗДЕСЬ на выгруженном дереве публикации → **239 passed,
    0 failed, 0 skipped**, ровно как на базе — презентация тестов Core не двигает, и это ожидаемо.
  - Отчёт `docs/results/c-exec-g-5a7c-body-1-001.md` прочитан, включая §Manual-acceptance и пункт 2
    с описанием найденного его глазом дефекта.

state_changes: |
  1. `t-body-1`: `active → done`, журнальная строка, перенесена в `cards/closed/`.
  2. `c-exec-g-5a7c-body-1-001`: `ready → done`, журнальная строка, перенесён в `cards/closed/`.
  3. `c-exec-g-5a7c-body-1-002`: `ready → done`, журнальная строка, перенесён в `cards/closed/` —
     добивка исполнена, работа опубликована под отчётом `001`.
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-body-1-close-001.md`.

  **Дорожка `body` НЕ распускается этой ногой:** дорожки дissolve только `review` закрываемой
  ставки. Её единственная задача закрыта, и дорожка стоит пустой до review.

captures:
  - |
    ПРИБОРЫ СКАЗАЛИ «ХОРОШО», ГЛАЗ СКАЗАЛ «ЕДЕТ БОКОМ». Живая инспекция показала инициализированный
    Animator со всеми параметрами — и это было правдой и одновременно бесполезно. Дефект был в том,
    ЧТО подавалось в эти параметры между сетевыми выборками. Это третье за волну подтверждение
    правила `green-gates-do-not-compile-the-game`, и первое, где «зелёным» была не сборка, а живой
    осмотр редактора. Осмотр редактора — тоже прибор.
  - |
    ЧЕТЫРЕ КОПИИ НЕ ПРОВЕРЯЛИСЬ НИ РАЗУ ЗА ВОЛНУ. Строка про них есть в карточке `t-body-1`, но
    прогон был локальный. Ни одна задача волны не проверяла сеть. При закрытии ставки это стоит
    назвать прямо: волна трогала презентацию, ядро хозяина и груз, и ни одна не подтвердила, что
    четверо по-прежнему соединяются.

decisions_needed: []

play_check:
  - 1 recite — done. Семь строк карточки и три строки наряда пересказаны.
  - 2 owner inputs — исполнены: облик закрыт его словом про заглушку, а его глаз дан и записан.
  - 3 do the work — приёмка возврата.
  - 4 self-check — done. Механические строки перевыведены здесь из байтов; строка про три движения закрыта его вердиктом; непроверенная строка про четыре копии названа отдельно, а не засчитана молча.
  - 5 close — done, маршрутом owner-verdict; основание записано в поле `close` и объясняет, почему не светлый путь и почему не свежий чат.
  - G5 — выполнен: суждение вынес владелец собственным прогоном, и это зафиксировано в закоммиченном артефакте. Свежий чат не подменяет вердикт, которого он уже дал.
  - G7 — done. Ни одного игрового решения ногой не принято; качество заглушки оценил он.
  - G9 — не применяется: одобрения содержания не требовалось.
  - G10 — done. RESULT финальный.

log: t-body-1 закрыта ЕГО ВЕРДИКТОМ на опубликованной голове 8c3bb8af, и главным событием задачи стал его собственный прогон — живая инспекция показывала инициализированный Animator со всеми параметрами, то есть по приборам всё было хорошо, а его глаз увидел, что тело едет боком с нулевым поворотом и сбрасывает ходьбу в стойку между сетевыми выборками; исполнитель починил в той же сессии, выведя поворот из ненулевого полученного перемещения и удержав измеренную скорость 0.12 с между выборками, и владелец принял, назвав качество плохим, но приемлемым для заглушки; опасность прошлого захода снята по факту — перебазирование прошло и НИЧЕГО не стёрло, файлы всех трёх ранее закрытых задач целы, порядок соблюдён тот, который я поставила: сначала перебазирование, потом его глаз, потом публикация; оба отката сошлись с деревом базы, седьмой раз подряд; ноль сцен и ноль чужих файлов, 239/239; названо не закрытым: четыре копии по сети не проверялись — прогон был локальный, и за всю волну сеть не проверяла ни одна задача

next: |
  **Дорожка `body` пуста: её единственная задача закрыта.** Распускает дорожки `review` ставки, не
  эта нога.

  Идут: занятия в WIN-U3, дом в WIN-U2 (кандидат готов, не опубликован, при приёмке обязан начаться
  с перебазирования — иначе сотрёт «место как факт»).

  Публикация дома развяжет `t-loot-1` вместе с починкой сквозного прохода по его решению,
  `t-house-asset-1` и отложенную приёмку по комнатам.

  `t-sight-1` ждёт освобождения `Householder.cs` от занятий и появления мебели.

  Для `review` волны: четыре копии по сети не проверяла ни одна задача.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-body-1-close-001.md
