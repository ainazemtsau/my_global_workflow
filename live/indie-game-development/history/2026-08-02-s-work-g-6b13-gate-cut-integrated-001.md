# RESULT — s-work-g-6b13-gate-cut-integrated-001

play: work
direction: indie-game-development
node: g-6b13
task: c-1
date: 2026-08-02

## outcome

Урезание проверок интегрировано в `main`. Всё заявленное перепроверено первой рукой и подтвердилось.
Найдены две вещи, которых в возврате не было: ускорение пока не действует ни в одном рабочем слоте,
и красный `-Deliver` на слитом состоянии объяснён механизмом, прочитанным в коде.

## evidence

**Заявленное подтверждено полностью:**

- `main`, `dev`, `slot/win-u3`, `origin/main` — все на `c75015a8`;
- кандидат `97412301` является предком `main`; поверх него РОВНО ОДИН коммит, и это перестановка
  статуса. Ничего лишнего не приехало;
- на `main` теперь `check.ps1` на **224** строки и **16** скриптов вместо 854 и 32;
- статус в `docs/results/c-exec-gate-surface-cut-001.md` — `**Status: DELIVERED on \`dev\`**`;
- `WIN-U3`: `AVAILABLE`, аренда `none`, рабочее дерево чистое;
- обычный `check.ps1` на слитом состоянии — свой замер, `OK: all active gates green`, exit 0.

**Находка 1: ускорение есть на `main`, но ни в одном рабочем слоте его нет.** Рабочие деревья
`WIN-U1` и `WIN-U4` стоят на старой базе `4e95845c`, `WIN-U2` — на `955e5e62` (та же база плюс
отклонённая работа), и у всех трёх в дереве старый `check.ps1` на 854 строки. Практическая проверка:
прогон в `WIN-U4` не уложился в две минуты и был прерван. Следующая задача в свободном слоте
получила бы 210 секунд — ровно то, что владелец считал исправленным. Лечится строкой в наряде:
первым действием обновиться на текущий `main`.

**Находка 2: красный `-Deliver` на слитом состоянии — по замыслу, и механизм прочитан, а не
угадан.** Свой прогон дал КРАСНЫЙ там, где исполнитель отчитался зелёным, и оба верны.
`tools/delivering-scope.ps1:101` дословно: «$null scope (underivable) validates everything».
У `dev` относительно `main` ноль коммитов — область выводить не из чего, поэтому проверяется всё,
включая чужие исторические отчёты. Упали ровно четыре, все чужие и старые (2026-07-06, 07-13, 07-14,
07-29), ни одного из этой работы. Исполнитель мерил, когда `dev` ещё опережал `main`; его зелёный
был настоящим в тот момент.

**Тот же механизм объясняет разброс времени, который иначе выглядел бы враньём:** 8 секунд в слоте
с изменениями (узкая область) против 33 секунд на состоянии без изменений (проверяется всё).
Для реальной работы верно число 8.

## state_changes

`NOW.md`: наряд интеграции → `done` с перепроверенными фактами; две новые записи —
`i-slots-carry-old-contour-until-updated-001` и
`i-deliver-red-on-fully-merged-state-is-by-design-001`.

## captures

Нет.

## decisions_needed

Нет.

## play_check

- Возврат обработан как улика: перемерены ВСЕ четыре ссылки, происхождение лишнего коммита, размер
  контура, статус отчёта, состояние слота и оба прогона.
- Расхождение с отчётом исполнителя не списано и не выдано за его ошибку: механизм найден чтением
  кода, и обе стороны оказались правы.
- Найдено то, чего в возврате не было и что ударило бы по владельцу первым: слоты со старым
  контуром.
- Ни одна цифра не принята на слово. Число «8 секунд» уточнено с условием, при котором оно верно.

## log

урезание проверок в main: 224 строки и 16 скриптов вместо 854 и 32, слот освобождён; найдено, что в рабочих слотах ускорения ещё нет

## next

Следующая строительная задача — фундамент со слоем правил. Её наряд ОБЯЗАН первым действием
обновить слот на текущий `main`, иначе она получит старый контур.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-work-g-6b13-gate-cut-integrated-001.md
