RESULT s-work-g-5a7c-loot-1-binding-g5-001 (call: c-work-g-5a7c-loot-1-binding-g5-001)
direction: indie-game-development   track: house-and-things   play: work   node/task: g-5a7c/t-loot-1

## ВЕРДИКТ: FAIL

Четыре строки из шести прошли попытку опровержения на опубликованной голове `53afdc2d`. Две
провалены ОДНИМ И ТЕМ ЖЕ фактом, который я нашла первой рукой и которого нет ни в возврате
исполнителя, ни в двух прошлых ногах: **один из десяти предметов стоит не на мебели, а ВНУТРИ неё.**

outcome: |
  «Bedside Radio» — второй ряд расстановки, `_definitionId: 2` — стоит ЦЕЛИКОМ внутри коллайдера
  матраса. Его опора названа «Bed Frame», верх рамы кровати y = 0.450, предмет ложится телом
  0.450…0.490, а матрас в этой же точке занимает 0.450…0.600 по высоте и накрывает её по обеим
  горизонталям. Перекрытие 0.192 × 0.040 × 0.040 — это ВЕСЬ объём предмета. Спавнится он
  кинематическим (`isActive: false` → `rigidbody.isKinematic = true`), то есть его даже не
  вытолкнет: он просто лежит внутри непрозрачного ящика. В доме видно девять предметов, а не десять,
  и лёгких — четыре, а не пять, при том что «штук пять лута, которые одному можно вынести» — его
  собственный ориентир строкой 1.

  Остальные девять ЧИСТЫ: я проверила тело каждого против КАЖДОГО коллайдера сцены и префаба дома.

  Пункт 2 ручной приёмки — снять временное покрытие пола — больше НЕ ждёт его рук: он решён
  замером. Четыре половых плиты дома не несут коллайдера вовсе, единственный пол — плоскость сцены
  на y = 0, а опора спрашивает ТОЛЬКО названный коллайдер, поэтому снятие покрытия не сдвинет ни
  одного предмета ни на миллиметр. То, что он видел глазом («покрытие временное, но погружение
  настоящее»), тоже перемерено и оказалось не про лут: три напольных предмета лежат дном на y = 0,
  а видимая поверхность пола на y = 0.030, и ходок стоит на той же плоскости. Это покрытие, а не
  опора.

  Обе улики поведения ЗАКРЫТЫ, каждая своим родом улики: про два будильника — счётом по байтам,
  про протухший удар — поимённым чтением всех пяти мест сброса и условия чтения.

evidence: |
  Всё ниже прочитано и прогнано МОЕЙ рукой на `53afdc2d`. Числа, уже сошедшиеся у двух ног
  (голова, предок-кандидат, 49 файлов, гейт `f841b52a`, ревью 22/13/9, `preserve` `e0a30194`,
  293/0/0), я НЕ перемеряла — они не несут этот вердикт.

  **СТРОКА 1 — НАБОР ЕСТЬ → ПРОВАЛЕНА.**
  Данные дают ровно 5/3/2: `git show 53afdc2d:Assets/TunnelCrew/Settings/GameRulesSettings.asset`
  несёт десять строк `_definitionId` 1…10, каждый id один раз, массы 48 (id 1–5), 100 (id 6–8),
  240 (id 9,10). Расстановка тоже десять: `_cargoSpawns` на `StartConditions` в
  `IntegratedHouse.unity` = 10 записей, и каждая `CargoSpawnPoint` называет свой id, все десять
  различны.
  Но мир даёт 4/3/2. Ряд 2 «Loot 2 Bedside Radio», план (x, z) = (−4.400, 2.500), опора
  «Bed Frame»:
  - `Bed Frame` BoxCollider `3637382884084279954` → мир центр (−4.400, 0.225, 2.500), полуразмеры
    (0.650, 0.225, 1.000) → **верх y = 0.4500**;
  - предмет id 2 = 0.192 × 0.04 × 0.04 → тело x −4.496…−4.304, **y 0.450…0.490**, z 2.480…2.520;
  - `Bed Mattress` BoxCollider `3338339972013066219` (`m_IsTrigger: 0`, `m_Enabled: 1`,
    `m_Layer: 0`, MeshRenderer есть, Rigidbody нет) → мир центр (−4.400, 0.525, 2.500),
    полуразмеры (0.575, 0.075, 0.900) → занимает **x −4.975…−3.825, y 0.450…0.600,
    z 1.600…3.400**;
  - предмет лежит внутри матраса по ВСЕМ трём осям, перекрытие = его собственный габарит.
  Точка спавна стоит в геометрическом центре кровати, то есть это не край и не допуск.
  Так вышло законно: `LootSupport.TryFindTop` спрашивает `surface.Raycast` — ТОЛЬКО названный
  коллайдер, — а `LootSupport` на матрасе нет вовсе (шесть опор префаба: Kitchen Table, Bed Frame,
  TV Stand, Kitchen Counter, Bedside Table, Coffee Table), поэтому назвать настоящий верх кровати
  автор физически не мог.
  И это ровно та дыра, которую исполнитель назвал сам: его таблица говорит «body bottom touches
  without penetration — measured for 5 of 10; the rest not individually measured», и радио — среди
  пяти неизмеренных.

  **СТРОКА 2 — СОСТАВ НАСТРАИВАЕТСЯ → ПРОШЛА.**
  Что стоит и сколько — два сериализованных места и ноль кода: массив `_cargoSpawns` на
  `StartConditions` и два поля на каждой точке (`_definitionId`, `_support`). Выбор предмета в
  инспекторе — список ИМЁН (`Editor/CargoSpawnPointEditor.cs`, `DrawItemChoice`), не номер.
  Незнакомый id больше не подменяется первой строкой: `GameRulesSettings.TryResolve` промахивается,
  и `NetworkWalkerCourier.TrySayWhatIsWrongWithTheLayout` кладёт хост с объяснением ДО того, как
  что-то заспавнится.

  **СТРОКА 3 — ЛУТ ЖИВЁТ БИБЛИОТЕКОЙ, «поставил — оно работает» → ПРОВАЛЕНА тем же фактом.**
  Библиотека есть и она данные: строка предмета = имя, id, три размера, масса, id поведения,
  необязательный визуал; `LootCatalogData.Complaints()` жалуется на всё сразу и до старта хоста
  (`NetworkSessionService.TrySayWhatIsWrongWithTheItemTypes` — до `StartConnection`). Но
  «поставил — оно работает» на ряде 2 не выполняется: поставил — и оно внутри матраса.

  **СТРОКА 4 — ПОРОГ ПО РУКАМ 144 / 300 / 720 → ПРОШЛА.**
  Настройки: `_cargoLiftPower: 200`, `_cargoGravity: 3`, `_cargoMaxHolders: 4`. Вес = масса × 3:
  48 → 144 Н, 100 → 300 Н, 240 → 720 Н — ступень цела до числа. Рук нужно
  `ceil(вес / 200)` = **1 / 2 / 4**. `Core/Cargo/LootHeft.cs` не несёт НИ ОДНОГО собственного
  порога — берёт `liftStrength`, `cargoGravity`, `maxHolders` снаружи; инспектор зовёт
  `rules.CreateCargoRules()`, то есть тот же объект, на котором едет подъём. Второй реализации
  закона нет.

  **СТРОКА 5 — ЧЕТЫРЕ РУКИ ПОТОЛОК → ПРОШЛА.**
  Самый тяжёлый предмет 240 → 4 руки, ровно потолок. Предел четырёх рук = 4 × 200 / 3 = 266.67
  массы (800 Н), то есть 240 под ним с запасом; `HoldReachAllowance` на нём = 800/720 − 1 = 0.111 м.
  Ни одно из десяти описаний не просит пятую руку.

  **СТРОКА 6 — ОТКАТ ОДНИМ КОММИТОМ → ПРОШЛА, и это мой прогон, а не пересказ.**
  Возврат показывал `git reset --hard` на слоте — это не коммит. Я проверила иначе, на
  опубликованной голове и не трогая её:
  `git diff 53afdc2d 1307aa3e -- Assets tests | git apply --check` → чисто, код выхода 0.
  Значит одна обратная заплата ложится на голову целиком и один коммит возвращает все 49 путей к
  голове дорожки.

  **ПУНКТ 2 ПРИЁМКИ — ПОКРЫТИЕ ПОЛА → РЕШЁН ЗАМЕРОМ, ЕГО РУК НЕ ЖДЁТ.**
  `Bedroom Floor`, `Kitchen Floor`, `Corridor Floor`, `Living Room Floor` в `ThreeRoomHouse.prefab`
  — плиты локально y = 0.015 толщиной 0.03, и коллайдера у них **ноль**. Единственный пол с
  коллайдером — корневой `Floor` сцены: MeshCollider на меше Plane, масштаб 1.6 → 16 × 16 м,
  верх y = 0, не триггер, включён. Опора спрашивает только названный коллайдер, поэтому снятие
  покрытия не сдвинет ни один предмет.
  Что при этом ВИДНО и перемерено: Tool Box (h = 0.06), Strongbox и Travel Trunk (h = 0.08) лежат
  дном на y = 0, а видимая поверхность пола на y = 0.030 — то есть 3 см внутри плиты (половина
  ящика с инструментами и 37.5 % каждого тяжёлого). Ходок стоит на той же плоскости y = 0. Это
  покрытие, а не опора, и живёт оно в `t-house-asset-1`.

  **ПУНКТ 3 ПРИЁМКИ — МОДЕЛЬ ПО ТИПУ → ЗАКРЫТ НА ДВЕ ТРЕТИ.**
  Хост — его словами: **«и что будильник рисуеться своей моделью»**.
  Куб-запаска — байтами: `_visual` заполнен ровно у id 1 (`AlarmClockVisual.asset`, guid
  `98cfd74a…` → `AlarmClockView.prefab`, guid `08f7ac2e…`), у остальных девяти поля нет вовсе →
  `TryResolveVisual` промахивается → `NetworkCargoPresentation.Create` строит
  `PrimitiveType.Cube`. Девять кубов и одна модель — это данные, а не суждение.
  Клиент — ЖДЁТ. Сегодня, 2026-08-14, на прямой вопрос он ответил дословно: **«Второе окно не
  запускал / не помню»**. Механически риска почти нет — рисуют оба экрана ОДНИМ компонентом из
  одного снимка (`NetworkCargoPresentation`, «EVERY screen draws it this way, the host's included»),
  а `CargoSnapshot.DefinitionId` — целое поле снимка, — но это ровно то, что он увидит глазом, и
  придумывать вместо него проверку запрещено.

  **УЛИКА ПРО ДВА БУДИЛЬНИКА → ЗАКРЫТА.** Считается, а не наблюдается: звонит столько предметов,
  сколько строк расстановки несут тип с поведением. `_alarmBehaviorId: 1`; `_behaviorId: 1` стоит
  ровно у id 1, у девяти других 0; `LootBehaviorRegistry.TryResolve` промахивается и на нуле, и на
  незнакомом; `CargoClass.cs` удалён, в продакшене `CargoClassId` не осталось (единственные
  вхождения — константа `AlarmCargoId` в `CargoThingTests.cs`); заводит будильник только
  `GameRulesSettings.CreateCargoSpawnState` через `definition.BehaviorId`. Расстановка несёт id 1
  ОДИН раз. **2 → 1.**
  Попутный замер, который стоит знать: `CargoBody.Step` выходит сразу, если ни одно тело груза не
  Active, и ходоки разводятся с грузом счётом в ростере, а не физикой. Значит лежащий заведённый
  будильник можно ударить только другим ЖИВЫМ грузом или неся его самого; ходок, задевший его,
  не звонит никогда.

  **УЛИКА ПРО ПРОТУХШИЙ УДАР → ЗАКРЫТА.** Я искала дыру поимённо и не нашла: окно контакта чистится
  в ПЯТИ местах — (1) `CargoBody.Step`, после КАЖДОГО подшага, у всякого тела, которое ни Active,
  ни ThingListening; (2) `SetBodyActivity(active: false)`; (3) там же на переходе «держали →
  отпустили»; (4) `SetThingListening` при смене, пока тело не Active; (5) `CargoBodyContact.Take`
  чистит окно на каждом чтении. Читает ростер ровно при `IsActive || thingNeedsBody`, а
  `ThingListening` ставится тем же предикатом в том же тике, поэтому окно не бывает старше одного
  тика. Пробуждение намеренно НЕ чистит, и это безопасно ровно потому, что место (1) уже опустошило
  окно на предыдущем подшаге.
  Говорю прямо: это чтение путей кода, а не живой прогон. Живого прогона, который отличил бы
  починку от тишины, не существует — у лежащего обычного предмета удар теперь не слышен ВООБЩЕ,
  поэтому «хозяин не прибежал» одинаково выглядит и при починке, и при поломке.

  **ЧЕМ ПРОВЕРЕНА МОЯ ЖЕ ГЕОМЕТРИЯ.** Все высоты я считала из коллайдеров разбором YAML
  (вращений в цепочке нет ни одного — проверено). Пять высот, которые исполнитель мерил ЖИВЫМ
  прогоном, сошлись с моим расчётом до цифры: Alarm Clock 0.670 на верхе 0.650; Kitchen Timer
  0.920 на 0.900; Hall Key Box 0.620 на 0.600; Strongbox и Travel Trunk 0.040 на 0.

  **МЕЛОЧЬ, НЕ СТРОКА ЗАДАЧИ.** Модель будильника — цилиндр 0.16 × 0.02 × 0.16, а тело предмета
  0.192 × 0.04 × 0.04: нарисованная «таблетка» шире тела вчетверо по одной горизонтали. Владелец
  этот вид уже видел и принял.

state_changes: |
  - `t-loot-1`: остаётся `active`. Блок `owner-eye matrix` переписан: пункт 1 — его словом ДА,
    пункт 2 — РЕШЁН ЗАМЕРОМ (его рук не ждёт), пункт 3 — хост его словом, куб-запаска байтами,
    клиент ЖДЁТ с его дословным «Второе окно не запускал / не помню». Добавлен блок
    `биндинговая проверка G5 2026-08-14` с таблицей шести строк и вердиктом FAIL.
  - Новая улика `i-bedside-radio-spawns-inside-the-mattress-001` (level: product, route: work).
  - `i-alarm-binds-to-weight-class-not-to-the-thing-001` → ЗАКРЫТА (2 → 1 по байтам).
  - `i-loot-candidate-emits-stale-contact-on-pickup-001` → ЗАКРЫТА (пять мест сброса прочитаны).
  - Возвращающий наряд `c-work-g-5a7c-loot-1-binding-g5-001` → закрыт.
  - Зарегистрирован продолжающий наряд `c-work-g-5a7c-loot-1-seat-and-close-001`
    (to: session, play: work, track house-and-things, task t-loot-1, status ready);
    текст — `work/c-work-g-5a7c-loot-1-seat-and-close-001-call.md`.

captures:
  - Матрас — единственная в доме поверхность, на которую очевидно кладут вещи и у которой нет
    `LootSupport`. Кандидат в общее правило: опора вешается на то, что видно как верх, а не на то,
    что несёт.
  - Модель предмета сегодня может быть шире его тела вчетверо и никто этого не заметит; когда
    появится вторая модель, расхождение вида и тела станет системным.

decisions_needed: []

play_check:
  - 1 recite: done — шесть строк `t-loot-1` и три пункта ручной приёмки восстановлены из карточки
    и наряда до первого замера.
  - 2 owner inputs (owner): done — задан ровно один вопрос, тот, ответ на который есть только у
    него; его дословный ответ 2026-08-14: **«Второе окно не запускал / не помню»**. Пункт 2 ему не
    задавался, потому что решился замером до вопроса.
  - 3 do the work: done — разбор `IntegratedHouse.unity`, `ThreeRoomHouse.prefab`,
    `GameRulesSettings.asset` и десяти путей кода на `53afdc2d`; тело каждого из десяти предметов
    проверено против каждого коллайдера сцены и префаба.
  - 4 self-check: done — каждая из шести строк получила попытку опровержения и вердикт; две
    провалены одним фактом, четыре прошли.
  - 5 close: done — FAIL, задача остаётся `active`, продолжающий наряд зарегистрирован. `review`
    ставки не открывается: `t-house-asset-1` открыта в этой же дорожке, `t-handlers-1` и `t-lab-2`
    ждут в соседней.

log: биндинговая G5 лута FAIL — «Bedside Radio» целиком внутри матраса, 4 строки из 6 прошли, обе улики поведения закрыты

next: |
  CALL `c-work-g-5a7c-loot-1-seat-and-close-001` — to: session, play: work, track:
  house-and-things, task: t-loot-1. Пересадить единственный предмет, стоящий внутри другого тела,
  и закрыть `t-loot-1` или назвать точный остаток.

END_OF_FILE: live/indie-game-development/history/2026-08-14-s-work-g-5a7c-loot-1-binding-g5-001.md
