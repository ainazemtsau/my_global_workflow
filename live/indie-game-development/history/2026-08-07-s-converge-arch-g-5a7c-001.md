RESULT s-converge-arch-g-5a7c-001 (call: c-converge-arch-g-5a7c-001)
direction: indie-game-development   play: converge-arch   node/task: g-5a7c

outcome: |
  У узла `g-5a7c` закрыты контракты со всеми соседями и разобрана высокорисковая архитектура.

  **КОНТРАКТЫ.** `work/converge-g-5a7c.md` получил §CONTRACTS: двенадцать краёв — строка на КАЖДЫЙ
  узел `TREE.md` без исключения (пятнадцать узлов плюс корень; сам `g-5a7c` в таблицу не заводится),
  включая те, у которых взаимодействия нет, — плюс два производителя/потребителя, которых в дереве
  нет вовсе: будущая ставка на тоннели/сеть доступа/копание и узел «полный заход демо». Ни один
  потреблённый контракт не висит без названного производителя; у обоих несуществующих записан явный
  порядок постройки, и он взят из его собственных слов, а не выведен ногой. Плюс тринадцать
  внутренних швов узла. §IMPORT, §GLOSSARY, §WHAT, §DRIFT и оба прежних §SIGNOFF не тронуты ни одной
  строкой.

  **ПОДУЗЛОВ НЕ ЗАВЕДЕНО**, и три причины не вкусовые: карточка сама объявлена многоволновой его
  словом (кр. 12, «следующая волна будет закрывать ядро, уже даже несколько»); он прямо запретил
  планировать следующую волну вперёд («мы сейчас полностью не планируем»); новый узел — правка
  `TREE.md`, то есть G9 и его подпись, которой у этой ноги нет и которую наряд не просил. Подсистемы
  стали внутренними контрактами, резать на волны и полосы будет `shape`.

  **АРХИТЕКТУРА.** Создан `work/converge-g-5a7c-arch.md`: девять высокорисковых мест, у каждого
  таблица опровергнутых вариантов. Восемь получили ВЫВОД ПО ОПРОВЕРЖЕНИЮ с обязывающим ADR у PLAN;
  девятое — настоящий вопрос геймплея — вынесено ВЛАДЕЛЬЦУ. Подписей владельца этой ногой получено
  НОЛЬ, и это записано ровно так.

  **ДВЕ НАХОДКИ ДОБЫТЫ ЭТОЙ НОГОЙ И ПОДНЯТЫ ДО АРХИТЕКТУРНЫХ ВОПРОСОВ.**
  (1) **Дом описан ДВАЖДЫ, и высота приходит именно туда, где описания нет.** Смысловое описание
  (`HousePlan`: комнаты, места, проходы) живёт в engine-free `Core/`; геометрическое (`StandWall`:
  прямоугольники X/Z) — вне его, в `Network/CarryStand.cs`. `HouseWall.WorldBounds` отдаёт `Bounds`,
  у которого высота ЕСТЬ, а `CarryStand.cs:72-76` берёт только `x`/`z` и высоту ВЫБРАСЫВАЕТ. При
  этом `Core/TunnelCrew.Core.asmdef` объявляет `noEngineReferences: true`, то есть правило физически
  не может спросить движок — а стена нужна СРАЗУ ЧЕТЫРЁМ строкам подписанной карточки: «стены выше
  глаз» (кр. 5), «стены и закрытые двери ослабляют» и «прямой взгляд даёт последнюю позицию»
  (кр. 6), «проходимый путь снизу вверх» (кр. 2).
  (2) **У захода нет владельца времени.** `DeliveryPoint.ActivePoints` — `static readonly List`,
  наполняется в `Awake` и переживает смену сцены; состояние хозяина держит адаптер
  (`AuthoritativeHouseholder`), тела груза держит `CargoBody`. То есть кр. 9 закрылся бы
  перезапуском и стал бы пустым — ровно то, что предсказал §GLOSSARY G04.

  **ДВЕ ВЕЩИ НАЙДЕНЫ УЖЕ ПОСТРОЕННЫМИ И НЕЧИТАЕМЫМИ, и обе покупают критерии даром.**
  `CargoHold.Strength` (`Core/Cargo/CargoHold.cs:42-43`, «Seam: how hard this hand holds on. No rule
  reads it») — готовый пустой шов под «силу как одно названное число под будущую прокачку» (кр. 3).
  `HouseRoom.Holds(x, z)` плюс `HousePlace.RoomName` — готовый ключ локальности для «происшествия»
  (кр. 6): «в одной комнате» описание дома уже умеет отвечать.

  **ВЛАДЕЛЬЦУ ВЫНЕСЕН РОВНО ОДИН ВОПРОС, а не батч.** Двигается ли обстановка, когда её задели
  (`d-furniture-moves-or-stands-001`): «стоит» стоит почти ноль, «падает и катится» меняет сетевую
  модель, потому что сегодня в игре РОВНО ОДНО физическое тело. Основание такой формы — его
  собственные слова, записанные в §SIGNOFF converge-файла: «я не хочу сейчас в каждый вопрос и
  поверхностно отвечать… Не вместе со всем винегретом». Узел на его ответе НЕ стоит: строка идёт
  `open → OWNER` и по правилу передачи открытых строк обязана дойти до него при `shape`, если он не
  ответит раньше.

  **`W103` ЗАКРЫТА СОБРАННЫМ МАРШРУТОМ, А НЕ РЕШЕНИЕМ НОГИ.** Прежний адресат `PLAN` назван неверным:
  продуктовый PLAN не решает, что из находок внешнего плейтеста становится работой. Настоящий маршрут
  собран из трёх источников ВНЕ этой ноги — фильтр объявляет сам сосед (`TREE.md` `g-8a41` кр. 3),
  форму приземления объявляет ядро (`os/KERNEL.md:63`), адресат выбирается по уровню находки
  (`i-full-run-node-to-be-authored-001`, `NOW.md:102-107`). Отвечающий — `review`/`map`. Ответ живёт в
  §CONTRACTS C07; строка §WHAT намеренно не правлена, потому что `converge-arch` в чужой раздел не
  пишет.

  **`W15` и `W16` не тронуты ни знаком** и остаются отложенными его собственными словами; `W16`
  сохраняет пометку, что это вопрос ГЕЙМПЛЕЯ, который задача обязана принести ЕМУ.

  **Карточка `g-5a7c` не тронута ни знаком. Продуктовый репозиторий не изменялся.**

evidence: |
  **Объявленная база продуктовых фактов и её проверка первой рукой.**
  `C:/projects/Unity/GasCoopGame`, `git rev-parse HEAD` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`,
  `git status --short` пусто, только чтение. Та же база, на которой писан `work/converge-g-5a7c.md`.

  **1. Контракты — покрытие по дереву.** `live/indie-game-development/work/converge-g-5a7c.md`
  §CONTRACTS. Узлы `TREE.md` перечислены командой (`grep -n "      - id: g-"`): g-37a1, g-6b13,
  g-1d84, g-5a7c, g-3f90, g-8f31, g-4d7a, g-c519, g-2b7f, g-5e8c, g-8a41, g-7b42, g-9d16, g-c4af,
  g-12fd плюс корень g-0c26 — пятнадцать плюс корень, и каждый имеет строку C01–C12 (несколько
  закрытых узлов сведены в одну строку с явным перечислением ids). Два несуществующих производителя —
  C11 (ставка на копание, адресат `i-digging-own-bet-authored-fresh-001`) и C12 (узел «полный заход
  демо», адресат `i-full-run-node-to-be-authored-001`) — названы с порядком постройки.

  **2. Высокорисковая архитектура.** `live/indie-game-development/work/converge-g-5a7c-arch.md`,
  разделы A1–A9, у каждого таблица «чем опровергнут / чем выжил». Опровержения, которые держатся на
  ИЗМЕРЕННОМ факте, а не на рассуждении:

  - **единицы уже сломаны, и это перемерено этой ногой:** `Network/CargoBodyContact.cs:52-58` делит
    импульс на `Time.fixedDeltaTime` = `ProjectSettings/TimeManager.asset` `Fixed Timestep: 0.02`, а
    судья гоняет `Physics.Simulate` шагами `TickDelta / _physicsStepsPerTick`
    (`Network/CargoBody.cs:180-187`); `TickDelta` = 1/30, потому что
    `Packages/com.firstgeargames.fishnet/Runtime/Managing/Timing/TimeManager.cs:184` — `_tickRate = 30`
    по умолчанию, и `Assets/TunnelCrew/Network/Prefabs/NetworkRuntime.prefab` его не переопределяет
    (проверено `grep -n "_tickRate\|TickRate"` по префабу — ноль строк); `_physicsStepsPerTick: 2` в
    `NetworkPlaySettings.asset`. Шаг ≈0.01667 против делителя 0.02 — число силы занижено ровно в
    1.2 раза. Это опровергает A1(а) и A5(а);
  - **правило не может спросить движок:** `Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef` —
    `"references": []`, `"noEngineReferences": true`. Опровергает A2(а) и A3(в) механически;
  - **высота стены доезжает до границы и теряется:** `Assets/TunnelCrew/World/HouseWall.cs:13-22`
    возвращает `Bounds`; `Assets/TunnelCrew/Network/CarryStand.cs:9-26,66-78` строит `StandWall` из
    `bounds.center.x/z` и `bounds.extents.x/z`, высота не берётся. Несущий факт A2;
  - **ходок нарочно не физическое тело:** `Network/NetworkWalkerPresentation.cs:203-205` выключает
    коллайдер с объяснением; `Core/Multiplayer/WalkSpacePort.cs:17-19` объявляет это свойством порта.
    Опровергает A3(в) и стоит за оговоркой к кр. 5 в A9;
  - **у груза три заморозки и выключенная гравитация:** `Network/CargoBody.cs:61,66-68`; точка
    приложения принудительно опущена на высоту центра масс `:159-161` именно потому, что наклон
    заморожен. Несущий факт A4;
  - **подъём существует только как картинка:** `Network/NetworkCargoPresentation.cs:61-66` рисует
    1.15 против 0.25, в снимок это не едет. Опровергает A4(в);
  - **формула переноски уже написана «for a list of any length»:** `Core/Cargo/CargoHaul.cs:19-60`
    (сумма по рукам, момент от плеча, демпфирование ОДИН раз на сумму), `:100-112` (пружина с
    потолком на руку), `:115-156` (`AtPoint` — точка укуса не у руки, зажатая на габарит груза).
    Несущий факт A4 и A8;
  - **шов под личную силу стоит и пуст:** `Core/Cargo/CargoHold.cs:42-43` и
    `Core/Cargo/CargoRules.cs:52` — оба помечены «Seam… read by no rule»;
  - **ключ локальности для происшествия уже есть:** `Core/House/HouseRoom.cs:77-80` (`Holds`),
    `Core/House/HousePlace.cs:52-53` (`RoomName`, с прямым предупреждением, что вложенность и
    проходимость — разные отношения). Несущий факт A6;
  - **место, куда садится происшествие, сегодня бросает исключение:**
    `Core/Householder/Householder.cs:982-991` — `InvalidOperationException` «Different simultaneous
    Householder reactions are outside this single-fact PROBA». Несущий факт A6;
  - **жёсткое правило §14 сегодня выполняется:** `Core/Householder/AuthoritativeHouseholder.cs:214-233`
    даёт цели реакции точку последнего шума либо именованное место дома. Основание C19;
  - **статический реестр точек доставки:** `World/DeliveryPoint.cs:14,68`, защита от дублей — это
    `Debug.LogError` (`:62-66`), а не отказ; сцена сетевая — `NetworkRuntime.prefab` `DefaultScene`
    `_onlineScene: Assets/TunnelCrew/Scenes/IntegratedHouse.unity`. Опровергает A7(а);
  - **вся власть уже собрана в одном месте:** `Core/Multiplayer/CarryWorld.cs:25-31,36-58`. Делает
    A7(б) дешёвым;
  - **источник шума — единственный и он про столкновение:** `Core/Cargo/CargoBodyPort.cs:47-69`,
    `Core/Cargo/CargoNoise.cs:50-109`. Опровергает A5(а) и A8(в);
  - **скорости лежат в двух ассетах:** `Network/NetworkPlaySettings.asset` `_movementSpeed: 3`;
    `Settings/HouseholderWalkSettings.asset` `movementSpeed: 1.75`, `urgentMovementSpeed: 3.25`.
    Основание M2 и C22;
  - **спавн-арифметика ставит четвёртого в стену:** `NetworkPlaySettings.cs:131-134`
    `_spawnOriginX + _spawnSpacing * ordinal` при `-1.5` и `3` даёт −1.5, 1.5, 4.5, **7.5**.
    Основание M5.

  **3. Ни одна архитектурная бумага не заехала в `done_when`.** Арх-файл объявляет свой статус первой
  же строкой раздела и повторяет его в §SIGNOFF; ни одна A-строка не помечена `acceptance` — метки
  арх-файла отличны от меток §WHAT намеренно и объявлены. `TREE.md` этой ногой не открывался на
  запись; проверяется диффом коммита.

  **4. `W103`.** Три источника ответа, все вне этой ноги: `TREE.md` `g-8a41` кр. 3 («чинится то, что
  независимо всплыло не меньше чем у двух участников»); `os/KERNEL.md:63` («Work unrelated to the bet
  becomes an issue/capture until the correct review/map/maintenance route admits it»);
  `NOW.md:102-107` (`i-full-run-node-to-be-authored-001`). Записано в §CONTRACTS C07 и в «Счёт этой
  ноги».

  **5. `W15`/`W16` не тронуты.** Обе строки §WHAT остались дословно теми же; §NOT-DECIDED арх-файла
  повторяет их отложенность его словами и не добавляет ни одного вывода поверх.

  **6. Свежая `converge-verify` выпущена.**
  `live/indie-game-development/work/c-converge-verify-g-5a7c-001-call.md`; зарегистрирована в
  `NOW.md` `open_calls` как единственный фронтир, `status: ready`, свежая физическая сессия.

  **ЧЕГО В ЭТОЙ УЛИКЕ НЕТ, И ЭТО НАЗВАНО ЧЕСТНО.** Веерного опровержения внутри сессии не было: плей
  требует `call:research` strategic_search, `AGENTS.md` предписывает разворачивать параллельных детей
  субагентами, а в этом харнессе действует прямая постоянная инструкция владельца «Do not call the
  AgentTool unless the user requested it. Do not use workflows or deep-research unless the user
  requested it» — и по `os/KERNEL.md` живая инструкция владельца выше и ядра, и плея. Нога прошла
  одним контуром, читая продукт и состояние первой рукой. Следствие записано в наряд проверки:
  `converge-verify` будет ПЕРВЫМ независимым опровержением набора, а не вторым.

state_changes: |
  1. `work/converge-g-5a7c-arch.md` — СОЗДАН. Разделы: шапка со статусом и базой, §RISK-LINE,
     §DECOMPOSE, A1–A9, §MINER (M1–M7), §NOT-DECIDED, §SIGNOFF. Заканчивается трейлером
     `END_OF_FILE: live/indie-game-development/work/converge-g-5a7c-arch.md`.

  2. `work/converge-g-5a7c.md` — ДОБАВЛЕН раздел §CONTRACTS между §SIGNOFF и трейлером
     `END_OF_FILE:`. Содержит: правила чтения, таблицу краёв C01–C12, таблицу внутренних швов
     C13–C25, «Счёт этой ноги» с блоком `contract_coverage` / `contracts_open: 2` / `arch_open: 9` /
     `arch_in_context_only: PASS` / `canon_proposed`. **§IMPORT, §DRIFT, §GLOSSARY, §WHAT и оба
     §SIGNOFF файла НЕ ТРОНУТЫ ни одной строкой** — сохранить как есть.

  3. `work/c-converge-verify-g-5a7c-001-call.md` — СОЗДАН, полный CALL, трейлер на месте.

  4. `NOW.md`:
     - шапка `updated:` → `2026-08-07 by s-converge-arch-g-5a7c-001`;
     - `open_calls`: УДАЛИТЬ возвращающуюся запись `c-converge-arch-g-5a7c-001`; ДОБАВИТЬ
       `c-converge-verify-g-5a7c-001` (`status: ready`, `to: session`, `for: g-5a7c`,
       `issued: 2026-08-07`, `call: work/c-converge-verify-g-5a7c-001-call.md`, `note` с указанием,
       что сессия обязана быть свежей и что это первая проверка узла). Единственный фронтир;
     - ДОБАВИТЬ строку-комментарий после `open_calls` о возврате `c-converge-arch-g-5a7c-001`,
       включая названный и НЕ починенный дефект соседа `g-c519` (маршрут `map`) и честную запись про
       отсутствие веера;
     - `decisions`: ДОБАВИТЬ `d-furniture-moves-or-stands-001` (два варианта с измеренной ценой,
       рекомендация «А сейчас, Б отдельным решением», плюс довод против рекомендации). Существующее
       решение `d-agents-skills-belong-in-repo-001` СОХРАНИТЬ без изменений;
     - `bet: null`, `tasks: []`, `recurring: []`, `direction_forecast` и ВСЕ записи `issues`
       сохранить без изменений.

  5. `LOG.md` — ДОБАВИТЬ одну строку сверху, сразу под заголовком, перед строкой
     `s-map-g-5a7c-ui-line-removal-001`.

  6. `history/2026-08-07-s-converge-arch-g-5a7c-001.md` — этот RESULT целиком.

  7. `TREE.md` и `CHARTER.md` — НЕ ТРОГАТЬ. `knowledge/` — НЕ ТРОГАТЬ (`converge-arch` только
     предлагает канон, слияние делают `review`/`pulse`). Продуктовый репозиторий — НЕ ТРОГАТЬ.

captures:
  - "Скорость: при 1.0 м/с у налётчика (`W07b`) взрослый хозяин ХОДИТ в 1.75 раза быстрее и БЕЖИТ в 3.25 раза быстрее — убежать от него в открытой комнате нельзя. Улика для `shape` и для его глаза на первой задаче; числом решает он или задача по его разрешению. Подробно — §MINER M2."
  - "Высота глаза хозяина: без неё «прямой взгляд даёт последнюю позицию» (кр. 6) считается в плоскости, и налётчик на полке не существует для него ни как видимый, ни как невидимый. Одно поле в `HouseholderWalkSettings`; вертикального ДВИЖЕНИЯ хозяину при этом не даётся. §MINER M1."
  - "Фикстура тестов: двадцать два теста ходят по рукописной копии дома (`TestHouse.cs:93-142`). Переезд геометрии в описание дома (A2) даёт дешёвую починку, но её надо НАЗВАТЬ в `shape`, а не обнаружить в первый день. §MINER M6."
  - "Ветка `probe/free-kits-2026-08-06` отстаёт от `main` на 66 коммитов: «вход уже оплачен» (кр. 10) не покрывает работу по переносу. §CONTRACTS C04."

decisions_needed:
  - q: "Обстановка в комнатах — она СТОИТ или ПАДАЕТ И КАТИТСЯ, когда её задели грузом?"
    options: ["А) СТОИТ — неподвижная вещь, коллайдер останавливает груз, контакт производит шум уже построенным швом; цена близка к нулю, ни одного нового физического тела и ни одной новой строки в снимке", "Б) ПАДАЕТ И КАТИТСЯ — задел, упало, поехало, шумит само; каждый предмет становится авторитетным телом, хост считает десятки тел вместо одного, каждый едет в снимке на четыре копии каждый тик, у каждого своё состояние, которое заход обязан сбрасывать"]
    recommendation: "А сейчас, Б — отдельным решением, когда он его захочет. А закрывает букву критерия 5 целиком и даром; Б — не отделка, а смена сетевой модели, и её нельзя купить попутно внутри волны про масштаб и вертикаль: сегодня в игре РОВНО ОДНО физическое тело. Довод ПРОТИВ рекомендации, названный честно: в играх, на которые он ссылается, падающие и ломающиеся вещи — половина удовольствия, и это его область, а не инженерная. Узел на ответе НЕ стоит: строка `open → OWNER` (арх A9, контракт C25) и обязана дойти до него при `shape`, если он не ответит раньше."

play_check:
  - "1 Declare: done — §CONTRACTS в `work/converge-g-5a7c.md`. Полнота проверена ПО ДЕРЕВУ командой, а не по памяти: пятнадцать узлов плюс корень, строка на каждый, включая узлы без взаимодействия. Ни один потреблённый контракт не висит: у двух несуществующих производителей (ставка на копание, узел «полный заход демо») записан явный порядок постройки его собственными словами. HOW-токенов в контрактах нет — push/poll, формат, частота и раскладка стоят `→ PLAN` во всех тринадцати внутренних швах. Двусторонний канон ПРЕДЛОЖЕН, не записан."
  - "1 Declare (owner): skipped — вердиктов владельца по контрактам НЕ запрашивалось, и это результат, а не пропуск: из двенадцати краёв ни один не является развилкой (одиннадцать односторонние, ограничительные или пустые; двенадцатый, C05, несёт дефект состояния с маршрутом `map`, а не вопрос вкуса). Единственная развилка среди внутренних швов — C25 — вынесена как A9 и стоит решением `d-furniture-moves-or-stands-001`."
  - "2 Decompose: done — одиннадцать подсистем названы поимённо в §DECOMPOSE арх-файла; ПОДУЗЛОВ НЕ ЗАВЕДЕНО по трём названным причинам (карточка объявлена многоволновой его словом; его прямой запрет планировать следующую волну вперёд; новый узел = правка `TREE.md` = G9 и подпись, которой нет). Внутренние швы записаны контрактами C13–C25, сторона WHAT; HOW у всех `→ PLAN`."
  - "3 Architect: done — девять высокорисковых мест, у каждого таблица опровергнутых вариантов. Отбор по RISK-LINE объявлен таблицей, и там же названо, что сознательно НЕ поднято до архитектурного вопроса и почему. Мультиплеер и масштаб работали механическими критериями отбраковки (A1, A3, A5, A9-Б). Один заход §MINER дал семь незаданных вопросов. `arch_open: 9`."
  - "3 Architect (owner): skipped с названным основанием — подписей владельца НОЛЬ. Восемь выводов из девяти держатся на измеренном факте кода, на подписанном тексте карточки или на его ранее сказанных словах; ни один не является его выбором и не может цитироваться как подписанный, обязывающий ADR у PLAN. Девятое (A9) — настоящий вопрос геймплея — вынесено ему одной строкой с двумя ответами, измеренной ценой каждого и рекомендацией, и остаётся `open → OWNER`. Форма выбрана по его собственному правилу, записанному дословно в §SIGNOFF converge-файла: «я не хочу сейчас в каждый вопрос и поверхностно отвечать… Не вместе со всем винегретом»."
  - "4 Close & route: done — `contract_coverage: каждый узел TREE.md имеет строку; ни один потреблённый контракт не висит без названного производителя; два производителя ещё не существуют и записаны явным порядком постройки`; `arch_open: 9` (A1–A8 с ADR у PLAN, A9 у владельца); `arch_in_context_only: PASS` — ни одна строка арх-файла не помечена `acceptance`, не является требованием и не едет в `done_when`; карточка не тронута ни знаком. `next` = `c-converge-verify-g-5a7c-001`, свежая сессия, затем `shape`."
  - "ОТКЛОНЕНИЕ ОТ ПЛЕЯ, ОБЪЯВЛЕНО: шаг 3 требует `call:research` strategic_search, а `AGENTS.md` — разворачивать параллельных детей субагентами. В этом харнессе действует прямая постоянная инструкция владельца не вызывать AgentTool и не использовать workflow; по `os/KERNEL.md` живая инструкция владельца выше и ядра, и плея. Нога прошла одним контуром с чтением продукта первой рукой; следствие («converge-verify будет ПЕРВЫМ независимым опровержением, а не вторым») записано и в арх-файл, и в наряд проверки."
  - "`open_rows` наследуемые: `W103` закрыта собранным маршрутом из трёх источников вне ноги (ответ в C07, строка §WHAT намеренно не правлена); `W15` и `W16` не тронуты и остаются отложенными его словами."

log: "**АРХИТЕКТУРНЫЙ СЛОЙ `g-5a7c` ПРОЙДЕН ПОЛНЫМ СОСТАВОМ: контракты плюс девять высокорисковых мест с опровергнутыми сравнениями.** §CONTRACTS добавлен в `work/converge-g-5a7c.md`: строка на КАЖДЫЙ узел дерева плюс два производителя вне его, тринадцать внутренних швов, подузлов не заведено. Две находки добыты этой ногой: дом описан ДВАЖДЫ и геометрическая половина живёт вне engine-free слоя правил, а высота стен теряется на границе; у захода нет владельца времени, и кр. 9 закрылся бы перезапуском. Восемь выводов из девяти — по опровержению, с обязывающим ADR у PLAN; подписей владельца НОЛЬ. Владельцу вынесен ровно ОДИН вопрос: двигается ли обстановка. `W103` закрыта собранным маршрутом, `W15`/`W16` не тронуты, карточка не тронута, `arch_in_context_only: PASS`. Веера внутри сессии не было по прямой инструкции владельца — сказано прямо. Выпущен единственный фронтир `c-converge-verify-g-5a7c-001`."

next: |
  `c-converge-verify-g-5a7c-001` — свежая, ОТДЕЛЬНАЯ физическая сессия судит набор целиком и пытается
  его сломать. При PASS — `shape`; при FAIL — именованные строки обратно в `converge`/`converge-arch`.
  Параллельно и ничего не блокируя: ответ владельца по `d-furniture-moves-or-stands-001` (если он
  захочет ответить раньше, чем строка дойдёт до него при `shape`).

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-001.md
