# RESULT — s-work-g-5a7c-the-freeze-is-the-task-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-a-fact-knows-who-made-it-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — сборка дерева нашла зависание, которого не было ни у одного из слитых кусков; петля, объявленная мёртвой утром, воскресла к вечеру, и задача, стоявшая первой на вылет, стала держателем всего видимого

close: light — because всё несущее перевыведено направлением на байтах: реакция `landing` и её единственный выход по прибытии прочитаны в коде, `SourceId` и его читатели найдены поиском, `AcknowledgeOnArrival` прослежен до места завершения. Живой прогон и координаты читались как улика исполнителя, и её вывод подпёрт независимым чтением механизма. G5-чат не открывался.

owner_approved: |
  Отдельного слова не требовалось и не запрашивалось: разрешение локального коммита и выбор
  следующей работы для занятого слота — механика, а не его предмет.

  **Замах волны при этом НЕ расширен.** Задача `t-a-fact-knows-who-made-it-1` входила в его
  «берём четыре» с самого начала; изменился только её порядок — из первой на вылет она стала
  обязательной. Владельцу это сообщено фактом.

outcome: |
  **СБОРКА ДЕРЕВА СДЕЛАЛА РОВНО ТО, ЗАЧЕМ ЕЁ ЗАВОДИЛИ, И РЕЗУЛЬТАТ ХУЖЕ ОЖИДАЕМОГО — ЗНАЧИТ ОНА
  ОКУПИЛАСЬ.** Нога слила `main` с веткой ходьбы, получила ровно обещанные числа
  (`routine 6 · handlers 7 · occupations 4 · gauges 1`, тесты `744 / 743 / 1`) — **и живой прогон
  показал зависание.** Владельца не позвали. Это прямое исполнение СТОП-условия наряда «хозяин
  где-то встал — не зови на поломку», и оно сберегло его первый взгляд.

  **ПЕТЛЯ, ОБЪЯВЛЕННАЯ МЁРТВОЙ СЕГОДНЯ УТРОМ, ВОСКРЕСЛА СЕГОДНЯ ВЕЧЕРОМ — И ЭТО ГЛАВНЫЙ УРОК ДНЯ.**
  Нога дел замерила её честно: **ноль тиков, дважды.** Замер снят на дереве **без ходьбы**.
  Стоило слить ходьбу — **петля вернулась в худшем виде: не сто секунд, а без конца.**
  Будильник положен на `Kitchen counter`, и хозяин остался в `InvestigateLanding` на
  `(2.915, 0, 3.923)` **с пустыми руками**: за **60.17 с** ни состояние, ни координаты не
  изменились.

  **ДВА КУСКА, КАЖДЫЙ ПРОВЕРЕННЫЙ ПО ОТДЕЛЬНОСТИ, ДАЛИ В СУММЕ ПОЛОМКУ, КОТОРОЙ НЕ БЫЛО НИ У
  ОДНОГО.** Ни один гейт этого не показывал бы: обе ветки зелёные по своим меркам.

  **ПОД ОДНИМ СИМПТОМОМ ОКАЗАЛИСЬ ДВЕ РАЗНЫЕ ДЫРЫ, И ЭТО ВЫЯСНИЛОСЬ ТОЛЬКО ТЕПЕРЬ.**
  Первая известна: **факт не знает, кто его произвёл**, хотя `SourceId` лежит готовым.
  Вторая новая и хуже: **у ответа `AcknowledgeOnArrival` нет иного выхода, кроме прибытия** — ни
  удержания, ни срока, ни отказа. **Недостижимая точка морозит хозяина навсегда**, и упавшая от
  мыши вещь на той же тумбе сделает это ровно так же. Вторая дыра не лечится первой и записана
  отдельной строкой приёмки.

  **ЗАДАЧА, СТОЯВШАЯ ПЕРВОЙ НА ВЫЛЕТ, СТАЛА ДЕРЖАТЕЛЕМ ВСЕГО ВИДИМОГО.** В нарезке волны
  `t-a-fact-knows-who-made-it-1` умирала первой — «сегодня эта дыра работает в нашу пользу».
  **Прогон это опроверг за один день.** Пока хозяин замирает, ни утро, ни руки не принимаются его
  глазом и публикация бессмысленна. Порядок смерти пересмотрен фактом, а не мнением.

  **И ЭТО РОВНО ТА ЗАДАЧА, КОТОРУЮ ВЛАДЕЛЕЦ ВЕЛЕЛ ПУСТИТЬ В U4 ЧАСОМ РАНЬШЕ, А НАПРАВЛЕНИЕ
  ОТГОВОРИЛО.** Довод против был верен по существу — столкновение поверхностей с ногой слуха
  реально. Но **приоритет направление оценило неверно**: задача была названа наименее ценной,
  а оказалась единственной, что стоит между владельцем и его игрой. Довод «подождать часы» стоил
  бы дня, если бы сборка дерева не вскрыла это сама.

  **БАННЕР ОКАЗАЛСЯ НЕ МУСОРОМ, И УЛИКА ПЕРЕПИСАНА, А НЕ ОТЛОЖЕНА.** `DeliveryPoint` рисует
  надпись отменённой игры **и держит живую authoritative-цель доставки**. Нога остановилась по
  прямому СТОП-условию и была права. Предмет сменился: из косметики он стал вопросом о содержании —
  **нужна ли игре про мышей цель доставки вообще**, и это разговор, а не правка.

evidence: |
  Перевыведено направлением на дереве `slot/win-u2` = `a24a3c32` (тот же слитый состав):

  - `HouseholderReaction.cs:658-677` — реакция `landing`: `ResponseKind = AcknowledgeOnArrival`,
    `ActionTargetName = SituationPoint`, `RequiresSafeInterruption = false`;
  - `Householder.cs:914-919` — `AcknowledgeOnArrival` ставит `responseCompleted = true`
    **только внутри `if (reachedTarget)`**; все прочие виды ответа получают `holdDurationSeconds`,
    **этот не получает ничего**;
  - `WorldSituation.cs:73` — `public int SourceId { get; }`; поиск по `SourceId` вне редакторских
    файлов даёт два места: саму структуру и `World/HouseholderObservationMarker.cs`;
  - профиль слитого дерева: строка `something-fell-where-it-rustled` — обработчик происшествия,
    требующий двух фактов.

  Из возврата исполнителя, как улика: один конфликт слияния, только профиль;
  `routine 6 · decisionHandlers 7 · occupations 4 · gauges 1`; тесты `744 / 743 / 1`; будильник
  положен в `06:36:15` и `06:36:21`; `InvestigateLanding` на `(2.915, 0, 3.923)`, **60.17 с** без
  изменения; `DeliveryPoint` рисует баннер и регистрирует authoritative-цель; карта пополнена
  стендом, в ней найдены три устаревшие строки; `HEAD` = `687b6b55`, `MERGE_HEAD` = `0e72d610`,
  ничего не опубликовано.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-001` → слот **WIN-U4**;
  - `work/2026-09-01-answer-commit-blocked-tree-and-fix-the-freeze.md` — разрешение локального
    коммита заблокированного дерева и наряд на зависание.

  СТАТУС:
  - `t-a-fact-knows-who-made-it-1` → `active` (была `open`).

  ОБНОВЛЕНО (2 улики, обе НЕ закрыты):
  - `i-he-investigates-the-noise-he-made-himself-and-stalls-001` — переписана: петля объявлена
    мёртвой и воскресла, под симптомом две разные дыры, вторая (ответ без выхода кроме прибытия)
    названа впервые;
  - `i-a-banner-from-the-cancelled-game-spans-the-screen-001` — переписана: не мусор, а лицо
    работающего механизма; предмет сменился на вопрос о содержании.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-morning-content-1` и `t-carry-back-1` остаются `active`: их приёмка — его глаз, а смотреть
    по-прежнему не на что;
  - `t-walking-stops-deafening-1` остаётся `blocked`;
  - `t-everything-lands-1` остаётся `blocked`.

captures: |
  - **«ЗАМЕРЕНО МЁРТВЫМ» ЖИВЁТ РОВНО ДО СЛЕДУЮЩЕГО СЛИЯНИЯ.** Петлю замерили честно и объявили
    мёртвой утром; к вечеру она вернулась хуже прежнего. **Правило-кандидат: вывод «дефект мёртв»
    привязывать к дереву, на котором он снят, и переснимать после каждого слияния — иначе он
    становится ложной уверенностью с хорошей родословной.**
  - **ДВА КУСКА, КАЖДЫЙ ЗЕЛЁНЫЙ, ДАЛИ СЛОМАННОЕ ЦЕЛОЕ, И НИ ОДИН ГЕЙТ ЭТОГО НЕ ПОКАЗАЛ БЫ.**
    Сборка дерева заводилась ради глаза владельца, а окупилась как интеграционный гейт.
    **Правило-кандидат: перед тем как звать владельца, кто-то обязан прогнать СОБРАННОЕ живьём —
    и это дешевле, чем его разочарование.**
  - **НАПРАВЛЕНИЕ ВЕРНО НАЗВАЛО СТОЛКНОВЕНИЕ И НЕВЕРНО НАЗВАЛО ПРИОРИТЕТ.** Владелец велел пустить
    эту задачу часом раньше; довод против был про поверхности и был верен. Но задача, названная
    наименее ценной, оказалась единственным держателем всего видимого. **Правило-кандидат: довод
    «подождём, задача малоценна» проверять вопросом «что она блокирует, если окажется ценной».**
  - **ИСПОЛНИТЕЛЬ НЕ ПОЗВАЛ ВЛАДЕЛЬЦА НА ПОЛОМКУ, ХОТЯ ЭТО БЫЛА ЦЕЛЬ ЕГО НОГИ.** СТОП-условие
    сработало против собственной приёмки ноги. Это дороже выполненной ноги.

decisions_needed: |
  Ни одного обязательного. Владельцу сообщается фактом: задача, которую он велел пустить в U4,
  пущена — по причине, которой час назад не было.

play_check: |
  1. OPEN — СТОП исполнителя с тремя вопросами: коммит, баннер, зависание.
  2. WORK — заявление о зависании перепроверено чтением механизма первой рукой; вторая дыра
     (ответ без выхода) найдена направлением и в возврате не называлась; граница нового наряда
     построена по живой поверхности WIN-U2, чтобы слоты не столкнулись; обе улики переписаны
     фактом, а не дополнены.
  3. (owner) — не запрашивался: замах не расширен, изменился порядок внутри его же четырёх задач.
  4. CLOSE — RESULT, применение, коммит; ответ уходит владельцу для передачи в WIN-U4.

log: |
  сборка дерева нашла зависание, которого не было ни у одного из слитых кусков: петля самошума,
  честно объявленная мёртвой утром, воскресла к вечеру без конца — хозяин замер на 60 секунд с
  пустыми руками; под симптомом оказались две разные дыры, и вторая названа впервые: у ответа
  «признать по прибытии» нет иного выхода, кроме прибытия, поэтому недостижимая точка морозит
  навсегда; задача, стоявшая первой на вылет, стала держателем всего видимого и выдана в U4

next: |
  Ответ уходит владельцу для передачи в **WIN-U4**: коммит заблокированного дерева, затем
  зависание.

  Волна: голова слуха в работе в U2, свой шум в работе в U4, ходьба ждёт ответа головы,
  публикация ждёт обоих. Девятая волна ждёт дерева, на которое можно смотреть.
