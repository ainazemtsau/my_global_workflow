# RESULT — s-work-g-5a7c-carryworld-boundary-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-noise-reaches-by-distance-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — граница наряда расширена седьмым файлом: исполнитель нашёл настоящий дефект порядка, и ошиблась нарезка, а не он

close: light — because всё, на чём стоит решение, перевыведено направлением первой рукой на дереве исполнителя: родители слияния, сигнатура редукции, единственный боевой вызов, единственная реализация `Reaches` и порядок усадки. Инженерная граница — не предмет владельческого суждения, владельцу ушла одна строка отчёта. G5-чат не открывался.

owner_approved: |
  Не требуется и **намеренно не запрашивалось**. Вопрос «можно ли править седьмой файл» —
  инженерный выбор, а не то, что владелец увидит в игре; его правило прямое: инженерное решай сам.

  Владельцу сообщено фактом, а не вопросом.

outcome: |
  **ИСПОЛНИТЕЛЬ НАШЁЛ НАСТОЯЩИЙ ДЕФЕКТ ПОРЯДКА, И ЭТО НЕ ОТГОВОРКА, ЧТОБЫ РАСШИРИТЬ ПОВЕРХНОСТЬ.**
  Перепроверено направлением на его дереве `a24a3c32`:

  `WorldSituationNoise.Loudest(IReadOnlyList<WorldSituation>, tuning)` **сводит весь тик к одному
  стимулу по одной только полосе громкости и не знает, где стоит хозяин**. Единственный боевой
  вызов — `CarryWorld.cs:254`. Значит любая проверка расстояния ПОСЛЕ редукции видит только
  выжившего: **дальний тяжёлый шум съедает тик и затем отбрасывается по радиусу, а близкий шорох
  того же тика уже потерян.**

  Для игры, где несколько мышей шумят в доме одновременно, это рабочий случай, а не угол. И
  маскировка тут не побочный эффект, а объявленное свойство редукции — в наборе уже стоит тест
  `Loudest_HeavyMasksSightThenSightWinsWithoutTheImpact`.

  **ГРАНИЦУ ОШИБЛА НАРЕЗКА, А НЕ ИСПОЛНИТЕЛЬ.** Наряд назвал поверхностью `Probe/Situations` и
  `Probe/Householder`. Сборка восприятия за тик живёт в `Probe/Multiplayer`, и наряд про это
  промолчал, потому что нарезка меряла место, где шум *описан*, а не место, где он *сводится*.

  **ДОВОД ЗА РАЗРЕШЕНИЕ ОКАЗАЛСЯ СИЛЬНЕЕ, ЧЕМ «ИНАЧЕ НЕ ПОЧИНИТЬ».** Двумя десятками строк выше,
  `CarryWorld.cs:232`, тот же метод уже берёт `Householder.GetState()` и разрешает **зрение**
  относительно наблюдателя. **Слух, разрешаемый там же и так же, — та же форма, а не новая.**
  Это место, где код уже отвечает на «что он воспринимает», и потому разрешение не размывает
  порог смерти волны, а попадает в него.

  **РАЗРЕШЕНО С ЧЕТЫРЬМЯ УСЛОВИЯМИ, И ГЛАВНОЕ ИЗ НИХ — ПРО ЯМУ, КОТОРУЮ ИСПОЛНИТЕЛЬ УЖЕ ОДИН РАЗ
  ЗАКОПАЛ.** Он сам написал в комментарии, что расстоянию нужна настоящая позиция хозяина и потому
  первый тик сначала усаживает его (`_seated = true`, строка 546) и лишь затем впускает шум.
  **Перенос фильтра выше по течению открывает ровно эту яму заново.** Способ закрытия оставлен ему;
  требование — закрыть **тестом**, а не рассуждением. Остальные три: один ответчик на «доходит ли»
  (иначе порог смерти №4), `CarryWorld` седьмое и последнее место, в сетевом шве больше ничего.

  **И ОДНО НАБЛЮДЕНИЕ, КОТОРОЕ ОСВОБОЖДАЕТ ЗАДАЧУ ВОЛНЫ.** В правке уже стоит
  `WithReachedStimulusInterruptible`: если шум дошёл, ходьба не делает хозяина глухим. Похоже, что
  `t-walking-stops-deafening-1` не понадобится — **но это должен назвать возврат числом**, а не
  вывести направление чтением диффа. Запрошено прямо.

evidence: |
  Снято направлением на дереве исполнителя `slot/win-u2` = `a24a3c32`, рабочая копия грязная,
  ничего не коммичено:

  - `git cat-file -p a24a3c32` → родители **`0e72d610`** (main) и **`687b6b55`** (ветка ходьбы) —
    слияние ровно то, что объявлял наряд;
  - `WorldSituationNoise.cs:231-243` — `Loudest(IReadOnlyList<WorldSituation>, tuning)`, редукция
    по `Loudness(Heard)`, **позиции слушателя в сигнатуре нет**;
  - `grep Loudest` по продукту → **единственный боевой вызов `CarryWorld.cs:254`**; остальные три
    в `HouseholderSightTests.cs`;
  - `CarryWorld.cs:232` — `var observer = Householder.GetState();` уже стоит для зрения, двумя
    десятками строк выше редукции; файл 302 строки;
  - `Reaches` — **одна реализация** (`WorldSituationNoise.cs:138`) и **один вызов**
    (`AuthoritativeHouseholder.cs:671`): сегодня ответчик один, и условие его сохраняет;
  - `AuthoritativeHouseholder.cs:546` — `_seated = true`, и допуск стимула перенесён ногой ПОСЛЕ
    него сознательно, с комментарием;
  - `git diff --stat` — 6 файлов, +264 −18; три радиуса по полосам заведены в
    `GameRulesSettings.asset`, то есть `done_when` 3 и 4 выполнены по форме.

  Из возврата исполнителя, как улика: профиль `6 / 7 / 4 / 1`; база `744 / 743 / 1`; после правки
  `745 / 745 / 0`; гигиена и гейты зелёные.

state_changes: |
  СОЗДАНО (1 документ):
  - `work/2026-09-01-answer-carryworld-seventh-place.md` — ответ на СТОП, разрешение седьмого
    файла с четырьмя условиями и тремя дополнительными пунктами возврата.

  ОБНОВЛЕНО (1 наряд):
  - `c-exec-g-5a7c-noise-reaches-by-distance-1-001` — поверхность расширена
    `Assets/TunnelCrew/Probe/Multiplayer/CarryWorld.cs`; расширение записано в журнал карточки.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-noise-reaches-by-distance-1` остаётся `active` — нога продолжается, ничего не закрыто;
  - `t-walking-stops-deafening-1` остаётся `blocked`: снимать его вправе только возврат ноги,
    а не чтение диффа направлением.

captures: |
  - **ИСПОЛНИТЕЛЬ ОСТАНОВИЛСЯ ДО ЖИВОГО ПРОГОНА, ЧТОБЫ НЕ ВЫДАВАТЬ ЕГО ЗА ДОКАЗАТЕЛЬСТВО НЕГОДНОГО
    КАНДИДАТА.** Ровно тот урок, за который позавчера заплатила чужая нога зелёными тестами на
    сломанном варианте. **Урок дошёл через наряд и сработал у другого исполнителя.**
  - **ГРАНИЦА ПОВЕРХНОСТИ БЫЛА НАЗВАНА ПО МЕСТУ, ГДЕ ПРЕДМЕТ ОПИСАН, А НЕ ГДЕ ОН СВОДИТСЯ.** Шум
    описан в `Probe/Situations`, а сводится к одному стимулу в `Probe/Multiplayer`.
    **Правило-кандидат: границу наряда про восприятие называть по цепочке «где родится → где
    сводится → где решается», а не по папке с именем предмета.**
  - **ЧУЖОЙ ДЕФЕКТ, НАЙДЕННЫЙ НОГОЙ ВНУТРИ ЧУЖОЙ ГРАНИЦЫ, СТОИТ ДОРОЖЕ САМОЙ НОГИ.** Дефект
    порядка «редукция раньше слышимости» существовал бы и после идеально выполненного наряда — и
    проявлялся бы как «иногда не слышит рядом», то есть как невоспроизводимая мелочь.
  - **РАЗРЕШЕНИЕ ГРАНИЦЫ ПРОВЕРЕНО НА СИММЕТРИЮ, А НЕ НА НЕОБХОДИМОСТЬ.** «Иначе не починить» —
    слабый довод, им можно оправдать что угодно. Сильный: **в этом же методе двадцатью строками
    выше уже разрешается зрение относительно наблюдателя.** Форма не новая.

decisions_needed: |
  Ни одного владельческого. Инженерная граница решена направлением.

play_check: |
  1. OPEN — СТОП исполнителя с вопросом о седьмом файле.
  2. WORK — заявление перепроверено первой рукой на его дереве: сигнатура редукции, единственный
     боевой вызов, единственный `Reaches`, порядок усадки; разрешение обосновано симметрией с уже
     существующим разрешением зрения; яма первого тика названа как обязательное условие с тестом.
  3. (owner) — не запрашивался намеренно: инженерная граница не его предмет.
  4. CLOSE — RESULT, применение, коммит; ответ уходит владельцу для передачи в WIN-U2.

log: |
  исполнитель слуха остановился на границе и оказался прав: редукция шумов сводит тик к одному
  стимулу до всякой проверки расстояния, поэтому дальний громкий съедает тик и затем отбрасывается
  по радиусу, теряя близкий шорох — граница наряда была названа по папке, где шум описан, а не где
  он сводится; седьмой файл разрешён по симметрии со зрением, которое там же и так же уже
  разрешается, с условием одного ответчика и теста на первый тик

next: |
  Ответ уходит владельцу для передачи в **WIN-U2**. Нога продолжается на том же дереве.

  Возврат обязан назвать: как закрыт первый тик, чем доказано, что маскировка больше не теряет
  близкий шорох, живой прогон, и нужна ли задача про ходьбу.
