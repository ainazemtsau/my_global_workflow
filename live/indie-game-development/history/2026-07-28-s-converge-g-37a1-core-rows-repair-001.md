# RESULT s-converge-g-37a1-core-rows-repair-001

direction: indie-game-development
play: converge
node: g-37a1
call: c-converge-g-37a1-core-rows-repair-001
date: 2026-07-28

## outcome

**ALL SIXTEEN FINDINGS REPAIRED, inside the named sections, and nothing else was filled.** No
requirement line changed, no criterion changed, neither card was touched, no knob's value was
chosen, no owner question was opened or answered, and no `§SIGNOFF` was written — a repair does not
sign its own work. The rows return to step 3 for a narrow rerun
(`c-converge-verify-g-37a1-core-rows-002`). `g-37a1` stays `parked`; no shape, no bet, no task, no
track; `knowledge/` was not written.

**THE TWO HEAVIEST FINDINGS RESOLVED THE SAME WAY, AND THEY ARE ONE PROBLEM.** F1 and F2 both came
from a single missing distinction: **the parameter home holds two different kinds of number, and the
file treated them as one.** RIG KNOBS are the contested numbers he declined to decide and routed to
the rig — they must be reachable from the built player (criterion 12). LEVEL-AUTHORING INPUTS are
the section's own content — dimensions, pocket count, pocket sizes, per-pocket kind, total substance
— and they are fixed before the build (criterion 10), appear in none of his registers, and if the
built player could dial them the game would be placing pockets at load, which is the level generator
criterion 12 forbids. §KNOBS is now two tables, ROW 5 governs only the first, ROW 12 says the same
in the other direction, and all three of the texts that could not hold together now hold.
**And the answer to "then what CAN the built player change about the section" is the load button** —
which is F2: the build must ship **at least two authored levels**, because his own word is «другой»
and a button that reloads the same section is ROW 4's restart, which is separately required.
«уровень» is signed for the first time.

**THE THIRD-HEAVIEST WAS §RULES, and it was the only thing criterion 12's «все правила читаются» is
judged against.** It claimed to be derived from the rows and was not. It is now **fifteen rules**,
the four additions being exactly the four the rows require and the list lacked — any pocket may be
entered (the ruling that REPLACED passability, and the one an outsider is least likely to reach
unprompted, because a wall is the natural assumption about a hazard you can see); the two kinds are
told apart before entering; nothing restricts where you cut; being sealed in kills you. It gains a
**three-part pass rule committed before the sitting**: six BEDROCK rules a session cannot avoid
teaching, which must appear in a free statement recorded before any question; a fixed questionnaire
for the rest, where a rule that had to be ASKED still passes but the asked-list travels to the next
version as legibility debt; and the one negative-by-absence rule checked by ABSENCE rather than by
an unprompted statement, because a player has no occasion to say that nothing came after him. **The
sharpest half is new: the row FAILS if the outsider states a rule the game does not have.** A
confidently wrong rule is a worse legibility defect than a missing one, and nothing in the file
could catch it.

**TWO JUDGMENT CALLS BELONG TO THIS LEG AND ARE DECLARED FOR ATTACK.**

**F11 — late-join is STRUCK, not raised with him, and that was this leg's call to make.** ROW 16 had
required that a player joining after digging began sees the section as it is. No owner text contains
it; snapshot join is a distinct engineering commitment, booked in the previous converge's build
order as item 6 of seven. Three of his own standing rules decide it without him: the set's governing
rule is «минимальный набор с возможностью расширения»; the requirements artifact's authoring rule 3
says nothing said earlier is a requirement until he repeated it there, and this was never said by
him at all; and CHARTER lens 3 asks whether one developer can finish the volume. Striking it costs
nothing his text asks for — two players who both start a session are covered, and the meet-in-a-hole
test is unchanged. **Manufacturing an owner decision for a commitment he never made is the failure
this route exists to avoid**, so it is struck rather than raised, and named in place so a build leg
does not re-add it by habit.

**F2's consequence — "the build must ship at least two levels" — is a derivation, and it is the one
repair that could be accused of smuggling a requirement into a set that may not gain one.** It is
declared as such in the successor CALL and is the second thing the rerun is told to attack.

**WHAT WAS DELIBERATELY NOT FILLED, and each is outside a converge's authority.** The machine
budget's PRICE (F15's price half) and the acceptance labour's PRICE and owner (F16) are routed to
`shape` as the third and fourth items of the venue / parameter-home class. The PRE-BUILD stretch —
the one that actually killed `g-12fd`, and which no row can guard because every row names the built
player as its evidence — is routed to `shape`'s `appetite` and `kill_by` (G3, G4). And criterion 11's
co-op ordering clause is **held by nothing today**, which is F13's concrete consequence: it is named
as governance and routed to `shape` as a binding constraint on the bet, because a `shape` leg could
otherwise defer ROW 16 and nothing in the row set would catch it.

**WHAT THE REPAIR DID NOT TOUCH.** §OWNER, which the verification did not attack at all; §ORACLE and
§BUILD-CLOSES-BETTER, which it did not name; ROWS 2, 4, 8 and 11; ROW 7, retired in place with its
number kept. The seven findings refuted in-leg and the four hazards recorded as CONFIRMATIONS are
not re-raised. §ORACLE class 8 — no row states which single measurement would show the model wrong —
remains the set's one genuine gap and is untouched, because it was not among the sixteen.

## evidence

**SESSION INDEPENDENCE AND SURFACE.** Fresh chat in the direction worktree, no owner present, as the
CALL requires. Not the verification chat, not the chat that wrote the rows, not a day chat.

**WHAT WAS READ.** `work/c-converge-g-37a1-core-rows-repair-001-call.md`;
`history/2026-07-28-s-converge-verify-g-37a1-core-rows-001.md` (the sixteen findings, the seven
refutations, the four confirmations, the six competing classes);
`work/converge-g-37a1-core-rows.md` (the target); `TREE.md` `g-37a1` (the fifteen criteria);
`work/core-requirements-g-37a1.md` (the sixteen live lines, the NOT-core list, the settings
register); `NOW.md` (issues, open_calls, decisions); `os/KERNEL.md`; `os/plays/converge.md`;
`os/plays/converge-verify.md`. **NOT read:** `archive/**`, the product repository, and no engine
fact was re-derived — the two engineering receipts cited in the file (V3 porosity, `PlayerSense`)
are carried from the existing first-hand receipts of 2026-07-27 and 2026-07-28.

**FAN-OUT: none, deliberately.** `converge`'s mandated fan-out is its step-2 miner and its step-3
strategic_search for contested rows. Neither applies: the terms were already mined and signed by the
original converge, no row was reopened, and this leg answers a closed list of sixteen named charges
rather than searching for options. The CALL says one session, no owner. Single-agent is the correct
shape and is recorded rather than assumed.

**STATE WAS RE-READ AT `991ea9e6`** — the verification's own commit — before any edit, and no
concurrent leg moved `live/**` during this one.

## findings — what each of the sixteen resolved to

**F1 — §KNOBS split in two.** RIG KNOBS (body, dig, terrain/K5, breach/K1, air/K3, base, death/K2,
rig controls, ground legibility/K4, step rate) are reachable from the built player; LEVEL-AUTHORING
INPUTS (the section's dimensions, pocket count, pocket sizes, per-pocket kind, total substance, base
placement) are fixed before the build and the built player may not write them. ROW 5's OBSERVABLE
narrowed to "every number in §KNOBS' RIG groups"; ROW 12's OBSERVABLE made explicit per authored
level and its OUTSIDER test gains a FAIL if the parameter home lets an outsider change the section.
**§GLOSSARY's «спорное число» is NOT re-signed and gains no property** — the group was narrowed to
match the reading already signed, and the excluded reading is written into its competing-readings
column, which is what that column is for.

**F2 — «уровень» signed; at least two authored levels required.** New §GLOSSARY row: one loadable
authored section instance, with the inputs fixed for it before the build. Competing readings named:
«уровень» = «сцена» (which makes the load button impossible), and «уровень» as a progression stage
(the core has none). ROW 5 carries the consequence and routes its price to `shape`; ROW 1's FORCES
gains "at least two authored levels"; §WHAT's criterion 12 row records it. **§SIGNOFF's term count
corrected to twenty-nine** — it read twenty-six while the table held twenty-seven, because «база»
was added and the count was not, which is the cheapest symptom of the pattern behind seven findings.

**F3 — §RULES from eleven to fifteen, with a committed pass rule.** Detailed above.

**F4 — ROW 17's guard restated; «вечная лаборатория» PARTLY signed; the pre-build stretch routed.**
The old test ("is anything downstream blocked on him playing") is failed by criterion 15 and also
condemns criterion 14's own safeguard — both owner-approved text in the same card. The restated
test: **has the next version's work started and is it proceeding, with nothing WAITING IDLE on his
play**, with two named exemptions (criterion 15's date estimate, a forecast and not work; criterion
14's safeguard, which needs one entry and not a duration). The distinguishing property is stated
once so no later leg re-derives it: **an eternal laboratory is work sitting idle waiting for play; an
item that needs one datum from play while the work beside it continues is not that.** «вечная
лаборатория» is now PARTLY SIGNED: his words fix one behaviour — work does not stop for play — and
the earlier full definition was this file's inference and is withdrawn. The unsigned half is
explicitly where the pre-build stretch lives, and it is routed to `shape`.

**F5 — ROW 15's test (b) pinned, given a pass rule, and its dependence stated honestly.** Test (b) is
split into (b-i) behaviour, a binary the administrator records, and (b-ii) words, asked in fixed
wording, where any answer connecting the base to air passes. The independence claim is re-derived
per half rather than asserted for the row: (a) and (b-i) are independent of
`d-air-counter-visibility-001`; **(b-ii) DEPENDS on it** and survives all four options on the record,
because the base's EFFECT is perceptible under each; if an option is ever chosen under which it is
not, (b-ii) is VOID and the row is decided by (a) and (b-i) — recorded before the sitting rather
than argued after it. The decision stays open and unanswered.

**F6 — the default pin added to ROWS 1, 3, 13 and 15**, in the form ROW 4 already carried, each
naming what specifically breaks: immortality makes ROW 3(b) false by construction and ROW 15(a)
unrunnable; the start-state knob changes what ROW 1's "same starting condition" means and what ROW
13(b) has to spend.

**F7 — the extended legibility deck now states its frames are of BREACHED pockets**, seen from
outside after the cut and before entering, with unbreached faces explicitly excluded and sent to ROW
14's deck where the answer is size and never kind. This is the criterion-2 / criterion-7 collapse
the file itself warns produces false findings, committed until now by its own newest instrument.

**F8 — sample sizes and decision rules committed for all three statistical halves.** ROW 9(a) goes
MECHANICAL like §14's original: six pairs, all six ordered correctly, any error fails, six being the
size at which coin-flipping cannot pass. ROW 13(d): twelve frames, six per kind, at least ten
correct — not all-correct, because it is a per-frame perceptual judgment under the game's own light
and an all-correct bar would fail on the deck rather than on the build. ROW 14: ten pairs for
presence at ≥9, ten pairs for size at ≥8, one error looser because criterion 2 asks only «примерно»,
with the size ratio committed with the deck and its magnitude routed to `PLAN`.

**F9 — ROW 10's closure claim struck and replaced by what each instrument actually closes**, plus a
fourth test that can observe the realistic failure: **two observers on two machines watching the same
cells at the same moment**, which a per-actor player-dependent term (the shape the product already
ships) makes diverge and which tests (a), (b) and (c) are all blind to. The residue — a symmetric
term acting only while a player is present — is named as closed at build time by the substance step's
input list in the engineering contour, not by this instrument.

**F10 — ROW 9's coupling restated over the four variables that actually decide it.** Time to death at
the face is capacity ÷ (base rate × the per-kind multiplier in force), against the breach severity
K1 — so «первое вскрытие не убивает мгновенно» is a property of a SET of four, not of the K1/K3
pair, and the old remedy left capacity and base rate free. Restated constraint on shape: either the
rig demonstrates the property across the four together at the corners of their ranges, or one of the
four carries a bound. This now agrees with ROW 15, which had already called capacity one of the two
most load-bearing numbers in the rig while F10's original terms left it free.

**F11 — struck.** Detailed above.

**F12 — ROW 6's test (b) split, and ROW 12 now carries the level property it rested on.** (b1) tests
the VERB — reaching the bottom without breaching is possible, and the game refusing a cut is the
authored panel. (b2) tests the SECTION — the route through a pocket visibly costs less than around
it — and **a FAIL there is a verdict on the authored section, not on the dig verb**, routing to level
authoring. ROW 12's FORCES gains the AUTHORING CONSTRAINT both halves need, with magnitudes to
`PLAN`.

**F13 — two governance clauses named as governance.** Criterion 11's «Сетевая основа не
откладывается» and criterion 14's «Участок остаётся частью пути демо» are ordering and roadmap
commitments, and neither ROW 16 nor ROW 17 can hold one. Criterion 14's half is held by `TREE.md`
and enforced at the `review` that closes the node. **Criterion 11's half is held by nothing**, which
is the concrete consequence the verification named, and it is routed to `shape` as a binding
constraint on the bet plus one sentence in `i-core-acceptance-instrument-unpriced` — because a note
in a `work/` file is not read by a shape leg and would not have fixed it.

**F14 — ROWS 1 and 5 reconciled by a term the file lacked: «состояние захода».** Run state is
everything the play produced and none of it survives a death or a relaunch; CONFIGURATION is the
parameter home's values and the rig switches, and it persists across both by construction or ROW 5's
instrument is impossible. **ROW 1's "same starting condition" means the same run state at the
configuration in force**, its two launches happen with the parameter home unchanged, and the flat
claim that "nothing persists across a run at all" is struck as true of run state and false of
configuration. His «между заходами ничего не копится» is about what ACCUMULATES, which is why the
new term is signed by citation and «заход» needed only its boundary clause made precise.

**F15 — the machine budget named as an UNCOVERED CLASS, and §SEAM's completeness claim withdrawn.**
No row states the built core must run at an interactive rate on his machine, and none is invented
here, because that would add a requirement and this leg may not: it is named as a class constraining
the whole set rather than a clause of any criterion, and its price goes to `shape`. §SEAM now says
four seams **of the substance** are decidable now and adds a fifth that is not: **the rig meets the
network** — who owns a knob's value in a two-machine session, whether the step rate must be identical
on both, and whether each rig switch is per-player or per-session. **One property is already forced
and is stated rather than left open: a knob that changes the simulation must have exactly one value
in force for a session, or ROW 16's «both dig in the same section» is false by construction.**

**F16 — the acceptance labour named in §WHAT and routed to `shape` as a price, not priced here.**
Eleven of sixteen rows need a person outside development, four need a deck or a bar committed before
a sitting, and only criterion 7 asks for an outsider at all — so the obligation comes from the row
FORMAT, not from his text. **This repair made it larger, not smaller**, and says so: three decks now
carry named sample sizes and §RULES carries a questionnaire.

## state_changes

1. `work/converge-g-37a1-core-rows.md` — REPAIRED. A dated REPAIRED banner in the header; §GLOSSARY
   gains «уровень» and «состояние захода», re-marks «вечная лаборатория» as PARTLY SIGNED, narrows
   «спорное число» by its competing-readings column, and makes «заход»'s boundary clause precise;
   §RULES rewritten to fifteen rules with a three-part committed pass rule; §KNOBS split into RIG
   KNOBS and LEVEL-AUTHORING INPUTS; §WHAT's criteria 11, 12 and 14 rows rewritten, its coverage
   arithmetic corrected, and two blocks added (the machine budget as an uncovered class, the
   acceptance labour as a routed price); §SEAM's completeness claim withdrawn and seam 5 added;
   §SIGNOFF's term count corrected to twenty-nine; ROWS 1, 3, 5, 6, 9, 10, 12, 13, 14, 15, 16 and 17
   repaired on their named findings. **Every change is marked in place with its finding number and
   the date, so the narrow rerun can find them.** §OWNER, §ORACLE, §BUILD-CLOSES-BETTER and ROWS 2,
   4, 7, 8 and 11 are untouched.
2. `work/c-converge-verify-g-37a1-core-rows-002-call.md` — CREATED. The narrow rerun of step 3,
   scoped to what changed, carrying what may not be re-raised, naming this leg's two judgment calls
   as the things to attack hardest, and recording that KERNEL two-strikes is live for this target.
3. `work/c-converge-g-37a1-core-rows-repair-001-call.md` — `disposition:` block appended.
4. `NOW.md` — `updated:` to this leg. `direction_forecast.drivers[1]` gains a leading paragraph:
   the repair ran, all sixteen are resolved, the two judgment calls, and that the FAIL still stands
   until the rerun passes. `i-engine-fit-decision-ladder.review_when` gains the price consequence of
   F1 and F2 resolving. `i-card-clauses-unverifiable.review_when` records that both instruments are
   now repaired and awaiting the rerun, and that the second authored level is a further unpriced
   item. `i-core-acceptance-instrument-unpriced` gains criterion 11's co-op ordering constraint
   (F13). `open_calls`: the spent repair CALL replaced by exactly one
   `c-converge-verify-g-37a1-core-rows-002`. `d-post-verify-route-001.progress` records the repair
   and the narrow rerun without reordering his route. `bet: null`, `tasks: []`, `recurring: []`
   unchanged.
5. `LOG.md` — one line appended.
6. `history/2026-07-28-s-converge-g-37a1-core-rows-repair-001.md` — this RESULT.

Not touched: `CHARTER.md`; `TREE.md` and both cards, which carry his verdict on exact text;
`knowledge/**`; `work/core-requirements-g-37a1.md`; the product repository; Steam state;
`archive/**`. No `§SIGNOFF` was written. No shape, bet, task, track or executor CALL was created. No
owner decision was answered, reopened or manufactured, and `d-air-counter-visibility-001` stays open.

## captures

- **THE SETTINGS REGISTER IN `work/core-requirements-g-37a1.md` IS STILL STALE ON ITS MOST
  LOAD-BEARING LINE** — «Цена смерти (сейчас ноль)» — while the card now makes death cost the entire
  excavation of the run and ROW 4 uses K2 as "a price beyond the run". The verification named it; it
  is out of this leg's scope (the CALL forbids touching that file) and is carried forward so the
  first leg with authority over it fixes it. **A `shape` leg reads that register.**
- **THE PATTERN BEHIND SEVEN OF THE SIXTEEN FINDINGS HAS A CHEAP COUNTERMEASURE, and this repair is
  its first instance.** The pattern: a claim established against text A carried forward when A is
  amended into A′ without re-deriving it. The countermeasure that worked here: **every repaired
  claim states WHICH text it was derived against and when** — ROW 15's dependence is re-derived per
  half rather than asserted for the row, ROW 1's persistence clause names what it is true of and
  what it is false of, and every change carries its finding number and date. Worth proposing to
  `pulse` as a convention for any amended converged artifact, not as another gate.
- **A SECOND AUTHORED LEVEL IS THE CHEAPEST UNPRICED ITEM IN THE SET and probably the cheapest thing
  on this list to build** — the same authoring path with different numbers, no new mechanism — but
  it is genuinely absent from step 4's matrix, and it is the only one of the three price items that
  a `shape` leg could mistake for free.
- **The competing class list authored by the verification (C1–C6) is still proposed for promotion
  alongside `work/oracle-first-playable-core-v1.md`**; this repair consumed C1, C4, C5 and C6 as
  findings and did not touch the promotion question, which belongs to `review` or `pulse`.

## decisions_needed

**None, and one was actively declined.** F11 was the only finding that might have needed his word,
and this leg decided it does not: the late-join requirement is struck because no owner text contains
it and three of his own standing rules decide it, so raising it would manufacture a decision rather
than resolve one. That call is declared in the successor CALL as one of the two things the rerun
must attack hardest. No knob value was chosen, `d-air-counter-visibility-001` stays open, and the
eleven closed rulings were not re-litigated.

## play_check

- `triage: heavy — converge ON` — unchanged, and this is a RETROFIT-shaped bounded repair rather
  than a fresh converge: the node's settled decisions were imported by the original leg and none is
  reopened.
- **Step 1, triage and import:** no new import. The three owner forks are answered and their
  receipts stand; the eleven closed rulings were carried, not re-opened.
- **Step 2, Define:** no miner ran and none was needed — the mechanical seed from `done_when` words
  was swept by the original converge, and the two terms added here were named by the findings
  themselves («уровень» by F2, «состояние захода» by F14, both signed by CITATION to his existing
  words, which is how this file signs in a no-owner leg). **«вечная лаборатория» moved the other
  way and is now PARTLY signed** — the honest direction for a term the file had signed by inference.
  TERM-PROPERTY RULE honoured: «спорное число» gains no property and was not re-signed.
- **Step 3, Resolve:** no row was reopened and no new question was raised. Two build-deciding
  parameters were born here rather than at task time, both forced by the findings: the LEVEL-
  AUTHORING / RIG split of the parameter home (F1) and the second authored level (F2). Firewall
  held: every magnitude was routed to `PLAN` — how much cheaper the route through a pocket must be
  (F12), the size ratio in ROW 14's deck (F8), the step rate and its cross-machine mechanism (F15),
  the runtime cost ceiling (F15). Acceptance properties stayed here; no HOW leaked into a row.
- **Step 4, Close & route:** no executor CALL emitted. `imported: []` (nothing new).
  `converge_coverage:` all sixteen findings → a named section, with F15's price half and F16 marked
  `covered_by: shape` and the pre-build stretch `covered_by: shape (appetite, kill_by)`.
  `canon_proposed: []` — no glossary row is proposed to `knowledge/` by this leg; the competing class
  list remains the verification's proposal. `next`: `converge-verify`, narrowly.
- **Forward-clean / backward-clean:** no `open` or `deferred` row exists in the file; every repaired
  claim carries its finding number, its date and its citation.
- **Owner-verdict guard:** no owner-readable verdict was asked for by this CALL and none was
  invented. F11 was decided as a scope call under his own standing authoring rules, not as a design
  verdict, and it is declared as this leg's call in both the RESULT and the successor CALL so the
  rerun can overturn it. No row was asked to prove the core is fun; no knob value was attacked or
  chosen; no criterion was touched.
- **G-gates:** G1 untouched (`bet: null`, no lane). G3/G4 — the pre-build guard is named as
  belonging to `shape`'s appetite and `kill_by` and is not improvised here. **G5 — NOT claimed. This
  leg is the WORK, not the verification**; the binding fresh refutation is
  `c-converge-verify-g-37a1-core-rows-002` in a separate chat, and no `§SIGNOFF` was written. G7 —
  no decision opened, and the one that might have been is declined with its reason. G9 — no CHARTER,
  no TREE, no card, no `knowledge/`. G10 — steps exposed, RESULT final, state changed only by
  applying this RESULT.
- **Two-strikes:** not live for this leg — this is the FIRST correction round on these sixteen
  points. It becomes live at the rerun, which is told so in its CALL: a repeated charge against the
  same section checkpoints rather than issuing a third repair into the same loop.

## log

2026-07-28 | s-converge-g-37a1-core-rows-repair-001 | converge | direction |
g-37a1/core-rows: all sixteen findings repaired in the named sections and nothing else filled — the
parameter home holds two kinds of number and the file treated them as one, so §KNOBS is now RIG
knobs versus LEVEL-AUTHORING inputs and the build must ship at least two authored levels; late-join
is struck rather than raised.

## next

`c-converge-verify-g-37a1-core-rows-002` — a narrow rerun of step 3 in a FRESH chat, no owner, on
what the repair changed and on anything it may have broken. It is told to attack this leg's two
judgment calls hardest: the strike of ROW 16's late-join, and the derivation that the build must
ship at least two authored levels. **Step 5 — his ladder verdict — still waits on step 3 by his own
route**, and F1 and F2 resolving means a step-5 verdict quoting 50–90 days must now also say that
the parameter home must refuse writes to the section and that a second authored level is unpriced.

END_OF_FILE: live/indie-game-development/history/2026-07-28-s-converge-g-37a1-core-rows-repair-001.md
