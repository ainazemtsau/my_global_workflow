# RESULT s-work-g-37a1-gas-rest-amend-route-001

call: возврат `c-exec-g-37a1-gas-rest-and-checksum-001` пакетом ESCALATE + живое указание владельца
direction: indie-game-development · track: t-sim · play: work · node/task: g-37a1/t-6
date: 2026-07-30

## outcome

Наряд t-6 вернулся ESCALATE, а не провалом. Стадия PAIR-CANDIDATE закрыла единственное незакрытое
обязательство замороженного плана (§8, спуск под одновременностью) — и доказательство вышло
ОТРИЦАТЕЛЬНЫМ: закон, утверждённый владельцем 29 июля, поле к покою не приводит. Это ровно тот STOP,
который план сам себе назвал. Владелец выбрал починку («Вариант 2»), и применение починки
фальсифицирует замороженный текст трёх требований, а формат квитанций продукта не позволяет вернуться
из стадии проверки назад в стадию плана внутри одной цепочки. Исполнитель отказался переписать
замороженный пакет на месте — хотя проверил на макете, что все автопроверки это пропустили бы — и
вернул задачу домой. Направление выдало свежий корень.

Возврат обработан, наряд-преемник `c-exec-g-37a1-gas-rest-amend-001` выпущен как стадия PLAN на
слоте WIN-U4 с сохранением change_id продукта. t-6 остаётся active; ни одно требование владельца не
снято, ни одна задача не переписана.

Живое указание владельца 2026-07-30 («единственное требование — мультиплеер»; «не паримся насчёт
оптимизации, но если в плане есть, то делаем»; «как можно проще и быстрее»; «самая минимально
правильная, которую потом можно расширять»; впереди вероятны взаимодействующие симуляции — ветер,
вода, огонь, температура) применено к ноге целиком и вписано в CALL отдельным разделом, старшим
плана. Оно же сняло посылку, на которой держался разбор направления до этого: «наша игра — сплошные
тоннели» больше не действует — «у нас сейчас тут два концепта игры. Один про туннели, другой нет».

Ответ на единственный содержательный вопрос ноги от смены посылки не изменился, изменилось
обоснование. Порог остаётся ПАРАМЕТРОМ с полом `2 · degree = 12` и значением по умолчанию 12 — не
ради скорости затухания в тоннелях (аргумент снят), а потому что параметр это одно число в
конфигурации, это самый дешёвый из возможных швов расширения при вероятных будущих симуляциях, и
потому что уже построенный стенд замера кривой Θ без него нечего крутить. Вморозить 12 константой
было бы дороже, а не дешевле: пункты 2 и 5 done_when t-6 умерли бы второй раз.

Проверка «проще и быстрее» дала неожиданный и хороший ответ: **поправка сжимает ногу, а не
расширяет.** Выбранная владельцем починка удаляет механику, а не добавляет — чётностный размен и
компонента `E` становятся мёртвым кодом, требование `G37-PARITY-005` снимается целиком. Стоят как
написаны 14 из 16 поведенческих тестов, 6 из 8 негативных контролей, все 4 класса свойств, стенд
замеров, три геометрии фикстур, узкий ключевой корень, миграция вызовов, legacy-свёртка и
`Carry(cell, face)` как публичный API. Переписываются два оракула, посевы фикстур и текст двух
требований. Резать в этой ноге нечего: дорогие части уже построены, и снять их стоило бы дороже, чем
оставить.

## evidence

**Проверено первыми руками в `C:/projects/Unity/GasCoopGame_win-u4`, а не принято на слово:**

- `slot/win-u4 @ 7e9556a1fc5309eb97cf488c87be522d75faee98`, синхронна с origin, дерево чистое;
  `origin/main` и `origin/dev` оба на `3c392f0c` — не тронуты;
- три квитанции цепочки на месте; последняя — `02-pair-candidate-owner-adjudicated.json`,
  `state: ACTIVE`, стадия `PAIR-CANDIDATE`, вердикт
  `PAIR_CANDIDATE_OWNER_ADJUDICATED_AMENDMENT_UNREPRESENTABLE_IN_CHAIN`;
- `tools/root-lifecycle-check.ps1`: закрытый словарь шести стадий и допуск только «та же стадия или
  следующая» подтверждены; ветка retry требует `earliest_invalid_stage -ceq` собственной стадии
  квитанции; `plan_freeze_commit` и весь объект `frozen` закреплены за преемниками. Заявленный
  маршрут «назад в PLAN внутри цепочки невозможен» — верен;
- **центральное утверждение эскалации проверено отдельно и подтверждено:** в блоке `if($Scan)`
  квитанции связываются по хешу `previous_receipt`, а НЕ по имени файла и не по папке — второй цикл
  валидирует квитанцию с `previous_receipt: null` автономно вызовом `Assert-Receipt $r $null`.
  `Assert-Frozen` сверяет путь регулярным выражением `^openspec/changes/.+/PLAN\.md$` и не связывает
  его с `change_id`. Значит свежий корень действительно может заморозить ту же папку пакета на новом
  коммите, а старая цепочка остаётся зелёной на `ab71037c`. Новая папка и новый change_id НЕ нужны;
- `tools/delivering-scope.ps1`: `Sort-Object -Property Name | Select-Object -Last 1` — «текущей»
  считается алфавитно последняя квитанция, поэтому имена новой цепочки обязаны начинаться с `03-`;
  `Test-RootStillInFlight` возвращает истину только при `ACTIVE` и стадии не `REPORT`, то есть пауза
  действительно СНИМАЕТ поблажку. Указание «не ставить старый корень на паузу» верно;
- `GasParentParams.cs:22` — `MinSpreadKp = 2 * VoxelFaces.Count` (12); `VoxelField.cs:20` —
  `MaxCellMass = 1 << 24` (16 777 216). Масштаб порога подтверждён.

**Проверено против подписанных требований владельца** (`work/core-requirements-g-37a1.md`): порог как
параметр не задевает строки 9, 10, 11 и 16. Масса не исчезает — порог её не двигает, а не удаляет
(строка 11); проводимость по-прежнему масштабирует скорость набора, поэтому открытая грань
разменивает вчетверо быстрее щели при той же разнице (строка 9).

**Пробел в подписи, зафиксированный, а не сглаженный.** По «Варианту 2» (R2) дословный токен
владельца в записях продукта ЕСТЬ. По уточнению R2a дословных слов НЕТ нигде — ни в
`docs/g-37a1-owner-repair-selection.md`, ни в маршрутном документе, ни в квитанции, которая при этом
называет его `owner_ruling_2026_07_30`. R2a записан в CALL как инженерно-вынужденный, а не как
подпись владельца: альтернатива R2b даёт покой 60/216 и 192/1296 на перечисленных тоннелях, а куб
4×4×4 не успокаивается никогда — то есть проваливает пункт 1 done_when t-6. Дословный токен берётся
на owner-approval стадии PLAN, где он и требуется по контракту.

**Слова владельца 2026-07-30, дословно:** «единственное требование, что он должен работать для
мультиплеера»; «Пока не паримся насчёт оптимизации, где как работает. Ну, если в плане есть, то
делаем»; «Нужно как можно проще, быстрее сделать, потому что не факт, что оно вообще нужно будет»;
«самую дефолтную, самую минимально правильную, которую, если что, можно будет потом расширять»;
«Велика вероятность, что у нас будут разные симуляции, которые смогут взаимодействовать друг с
другом. Там, например, ветер, вода, не знаю, огонь, температура»; «у нас сейчас тут два концепта
игры. Один про туннели, другой нет».

**Продуктовые документы возврата:**
`docs/engineering/g-37a1-jacobi-descent-proof.md` (отрицательное доказательство),
`docs/g-37a1-owner-repair-selection.md` (выбор R2 и перечисление),
`docs/engineering/g-37a1-amendment-route-and-owner-ruling.md` (маршрут и находки),
`docs/measurements/root-receipts/c-exec-g-37a1-gas-rest-and-checksum-001/02-pair-candidate-owner-adjudicated.json`.

Полный `check.ps1` на `7e9556a1` остаётся красным на 18 намеренно красных тестах G37 — ожидаемое
состояние стадии PAIR-CANDIDATE, а не регрессия.

## state_changes

1. `NOW.md` — `updated` на 2026-07-30 by `s-work-g-37a1-gas-rest-amend-route-001`.
2. `NOW.md` `open_calls` — запись `c-exec-g-37a1-gas-rest-and-checksum-001` (`running`) снята:
   наряд вернулся. На её место — `c-exec-g-37a1-gas-rest-amend-001`, track `t-sim`, `status: ready`,
   `to: executor`, `for: t-6`, `issued: 2026-07-30`, с примечанием о вынужденном слоте WIN-U4 и о
   том, что change_id продукта не меняется.
3. Новый файл `work/c-exec-g-37a1-gas-rest-amend-001-call.md` — наряд-преемник.
4. `work/2026-07-29-gas-core-engineering-spec.md` — поправка 2026-07-30 в шапке, четыре строки,
   байты остального сохранены: снята «накопитель остатка удаляется» (§4.1, §12); восстановлена
   мёртвая зона Θ как параметр с полом 12 (§4.2); снята посылка о тоннелях как основание для выбора
   значений (§4.2, §4.4); замер 3 стоит как написан и не расширяется (§11).
5. `NOW.md` `issues` — добавлены `i-product-plan-deviations-unrouted-001` (route `work`) и
   `i-two-live-game-concepts-001` (route `map`).
6. `NOW.md` `direction_forecast` — `as_of` на 2026-07-30; драйвер о продуктовом доказательстве
   переписан: первое материальное доказательство пришло и оно отрицательное.
7. `LOG.md` — одна строка. `history/2026-07-30-s-work-g-37a1-gas-rest-amend-route-001.md` — этот RESULT.

t-6 остаётся `active`. Задачи, бет, треки, cut_list, CHARTER и TREE не менялись.

## captures

- **Дыра в инженерном контуре, найденная стадией PAIR-CANDIDATE и не использованная ею.** Правка
  замороженного пакета на месте проходит ВСЕ проверки: `Assert-Frozen` резолвит блобы только на
  историческом коммите заморозки и никогда не читает HEAD; `frozen_artifacts_git_blobs` не имеет ни
  одного потребителя в `tools/`; `delivering-scope.ps1` освобождает любой ACTIVE-корень до REPORT от
  сильной батареи. Это дефект engineering-контракта (`os/engineering`, v31), а не направления, и по
  правилам он идёт отдельной maintenance-сессией. Готовый к вставке `MAINTENANCE REQUEST` отдан
  владельцу в чате.
- Одно слово в реплике владельца 2026-07-30 не разобрано на слух («не факт, что будет
  семеризация»). Ни на одно решение этой ноги не влияет; уточнить при случае.

## decisions_needed

Ни одного блокирующего. Порог как параметр — восстановление уже подписанного done_when t-6, а не
новое решение; дословный токен по R2a берётся на owner-approval стадии PLAN, где контракт его и
требует; слот WIN-U4 вынужден расположением пакета и назван в CALL с этим основанием.

## play_check

1. **Recite** — goal и done_when t-6 восстановлены дословно; обслуживаемый бет g-37a1 назван.
2. **Owner inputs** — задано два вопроса, владелец ответил живым указанием, которое сняло посылку
   первого вопроса и задало рамку всей ноге. Указание применено и процитировано дословно.
3. **Do the work** — `call:executor`: выпущен наряд-преемник с goal, context, boundaries, done_when,
   return и budget. Направление задало исход и доказательства; техническое КАК осталось стадии PLAN
   продукта.
4. **Self-check** — сверено с done_when t-6 по пунктам: все семь остаются в силе, пункты 2 и 5
   восстановлены после отклонения, зафиксированного PLAN §9 и Направлением не обработанного.
5. **Close** — продолжение в той же полосе t-sim; unrelated state сохранён.

G1: один бет, лимит полос 5, в t-sim по-прежнему один обычный корень. G3: appetite не расширен. G5:
`done` никому не поставлено — вернувшийся ESCALATE закрытием не является. G7: решения выданы с
опциями и рекомендацией. G9: CHARTER и TREE не тронуты. G10: шаги показаны, RESULT терминален.

## log

t-6 вернулась ESCALATE с ОТРИЦАТЕЛЬНЫМ доказательством: утверждённый закон к покою не приводит;
починка владельца выбрана, поправка выпущена свежим корнем на том же пакете, порог восстановлен как
параметр с полом 12, посылка о тоннелях снята живым указанием владельца.

## next

`c-exec-g-37a1-gas-rest-amend-001` — `ready`, полоса t-sim, слот WIN-U4, worktree
`C:\projects\Unity\GasCoopGame_win-u4`. Отдельно и вне направления: `MAINTENANCE REQUEST` по дыре в
проверках замороженного пакета.

END_OF_FILE: live/indie-game-development/history/2026-07-30-s-work-g-37a1-gas-rest-amend-route-001.md
