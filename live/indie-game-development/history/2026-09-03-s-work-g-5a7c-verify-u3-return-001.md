RESULT s-work-g-5a7c-verify-u3-return-001 (call: — владелец-инициированная, без родительского CALL)
direction: indie-game-development   track: утро   play: work   node/task: t-the-morning-ends-at-the-door-1

outcome: |
  **ВОЗВРАТ WIN-U3 РАЗОБРАН ТРИНАДЦАТЬЮ АГЕНТАМИ: ШЕСТЬ НЕЗАВИСИМЫХ ЗАМЕРОВ, У КАЖДОГО СВОЙ
  СКЕПТИК, ОБЯЗАННЫЙ ОПРОВЕРГНУТЬ ВЫВОД.** Его слово 03.09: «нужно внимательно проанализировать».

  **1. НАПРАВЛЕНИЕ ОШИБЛОСЬ, И ЭТО ПЕРВОЕ, ЧТО НАДО СКАЗАТЬ.** Наряд утверждал, что ошибка ETA
  «копится втрое дольше» на удлинённом утре. **Механизма накопления в коде нет:**
  `HouseholderTimeReserve` — `readonly struct`, поля пишутся только в конструкторе (`:71-88`); ETA
  собирается с нуля каждый вызов (`:192-196`); прошлая оценка в `Calculate` не передаётся вовсе
  (`:152-159`); `Calculate` зовётся ровно из одного места (`Householder.cs:192`). Исполнитель
  опроверг посылку справедливо.

  **2. НО ОБА СМОТРЕЛИ НЕ ТУДА, И НАСТОЯЩЕЕ ЧИСЛО В ДВАДЦАТЬ РАЗ КРУПНЕЕ.**
  `RemainingMandatoryWorkSeconds` (`:252-260`) **намеренно не добавляет длительности строк
  `walk-route`** — в коде это записано словами. А `HouseholderRoutineState.cs:363-378` отматывает их
  по стенным часам. После `35a17831` профиль — занятие 7 с плюс три `walk-route` по 110 с при
  дедлайне 360. **В оценку не входит 330 из 337 секунд утра.** Первый тик: ETA ≈ 32,8 с, запас
  327,2 с, режим `Comfortable` — при утре, которое нельзя отмотать быстрее 337 с. Занижение
  ≈ 305–312 с. На базе `2d2c27ff` было 60 из 120 (50 % дедлайна); стало 92 %. Геометрический эффект,
  вокруг которого шёл весь спор, лежит в вилке 0…18 с — на два порядка мельче.
  Сегодня это не ломает игру (`:56-58`: «Nothing in the simulation selects behaviour from this
  enum»), но давление `P` уже читается правилами.

  **3. ДЕФЕКТ ВЕЧНОЙ ЗАМОРОЗКИ РЕАЛЕН, ЛЕЖИТ В `main`, НЕ ПОКРЫТ НИ ОДНИМ ТЕСТОМ, И КЛИРЕНС РАВЕН
  НУЛЮ.** `HouseholderRoutineState.cs:277-285` — ранний возврат на терминальной строке стоит **до**
  обработки отказа (`:299-300`); отказанная дорога отчитывается тем же `WalkToward`, что успешная.
  Воспроизведено дважды независимо: 200 000 тиков, ноль движения, `MorningEnd.IsPresent == False`,
  headless при этом зелёный. Сторож существует — `HouseholderDoesNotFreezeTests` написан ровно про
  этот симптом — и **слеп по построению фикстуры**: дом без мебели, тела нет, терминальной строки в
  профиле нет. Узкое место не в прихожей, а в спальне: `Chest of drawers` кончается на `z = 6.100`,
  `Bed` начинается на `z = 6.600` — зазор ровно `0.500` м при диаметре тела `0.500` м. Сдвиг кровати
  на **0.001 м** — отказ. В доме уже 207 из 4902 стояночных точек без пути к двери.
  **Мыши это вызвать не могут** (план кэшируется до движения, `Blockers` читает только
  `Furnishings`). Но рубеж Р2 требует перестроить дом в три зоны — это гарантированно двигает мебель.
  Это не регрессия: на `2d2c27ff` `LeaveForWork` не существует нигде.

  **4. ИЗ ТРЁХ ВАРИАНТОВ ПОЧИНКИ НЕ ГОДИТСЯ НИ ОДИН, И ПЕРВЫЙ ОПАСЕН.** Вариант 1 при исчерпании
  отказов оставляет `Current` на терминальной строке, `StandStill` жёстко отдаёт `WaitAtRoutePoint`,
  и защёлка `:287-294` объявляет **«он вышел за дверь» про человека, стоящего в спальне** — ложь в
  том единственном факте, ради которого писался `35a17831`. Вариант 2 только диагностирует. Вариант 3
  упирается в слово владельца 01.09: «Владелец не может смотреть на игру, где хозяин стоит столбом,
  независимо от того, какое слово печатается рядом».
  **Форма починки — четвёртый вариант:** «прибыл» и «не смог» становятся двумя исходами **одного**
  решения терминальной строки, взаимоисключающими по построению, — ровно форма правила
  `a-rule-that-answers-every-tick-silences-the-fallback`. Плюс жалоба дома при постройке через
  существующий канал `SceneHousePlan.cs:89-99`. **Но исход «не смог» обязан быть поведенческим, а
  не только фактом, и это слово владельца.**

  **5. ЧИСЛА ЗАМЕРА ИСПОЛНИТЕЛЯ ВОСПРОИЗВОДЯТСЯ ТОЧНО.** Дом найден
  (`tests/TunnelCrew.Core.Tests/FurnishedHouse.cs:102-119`), плечи 1.0000 / 6.5765 / 1.0000 / 7.8026
  = **16.379 → 16,38**, лапа в лапу, включая порядок. Две оговорки: **1,75 м/с — скорость стенда,
  отгружаемая 1 м/с**, поэтому его секундные выводы относятся к стенду; и **сам прогон не закоммичен
  нигде** — чисел 263,9 / 338,7 / 74,8 нет ни в одном ref. Упрекать тут надо не его, а нас: наряд не
  потребовал закоммитить стенд замера.

  **6. ЧИСЛО 863 НИКТО НИКОГДА НЕ ГОНЯЛ.** `git diff --stat 198ab3fe origin/main -- tests/` →
  «1 file changed, 23 insertions(+)», ни одной строки удаления; рабочая копия WIN-U3 чиста.
  Настоящая последовательность по первому родителю: 819 → 852 → 857 → 863 → **882**. 863 — арифметика
  слияния двух ног, а не чей-то прогон.

  **7. И БАЗА В НАРЯДАХ УСТАРЕЛА:** `origin/main` = **5fcdc11f**, а не `2cea9a94`, как написано в
  выданных сегодня нарядах. Householder-код между ними не менялся, но ссылку надо поправлять.

  **ПОБОЧНО:** утро в шесть минут набрано лентой, а не дорогой — круг из шести якорей 25,77 м, то
  есть ~26 с ходьбы при отгружаемой 1 м/с, против 330 с строк распорядка. Формально строка рубежа
  «утро 5–8 минут» выполнена; по существу это заменитель содержания, которого ещё нет.

evidence: |
  - Его слово 03.09: «нужно внимательно проанализировать» — на пересланный возврат WIN-U3.
  - Возврат исполнителя целиком (передан владельцем в чат): числа замера, три варианта починки,
    самоназванный дефект.
  - Тринадцать агентов: шесть замеров по одному утверждению каждый, шесть скептиков с заданием
    опровергнуть, один свод. Скептики сняли два вывода собственных замеров как перепрыг
    («геометрия не воспроизводится» — снято; «монотонного убывания нет» — снято).
  - `HouseholderTimeReserve.cs:56-58, 71-88, 152-159, 192-196, 252-260, 317-326`;
    `HouseholderRoutineState.cs:256-263, 277-285, 299-300, 363-378, 422-429`;
    `Householder.cs:117-125, 133, 163-169, 192, 569, 603, 652, 898`;
    `AuthoritativeHouseholder.cs:685`; `HouseWays.Rect.Blocks`; `HouseBuilder.cs:497`;
    `NetworkHouseholder.cs:248-249`; `SceneHousePlan.cs:89-99`;
    `HouseholderWalkSettings.asset:16-17`; `default.householder.json:9-46`;
    `FurnishedHouse.cs:102-119, 108, 139`;
    `HouseholderDoesNotFreezeTests.cs` + `HouseholderChaseJitterTests.cs:532-565`.
  - Воспроизведение заморозки: два независимых прогона на чистых выгрузках `origin/main`,
    200 000 тиков, позиция неизменна, `MorningEnd.IsPresent == False`.
  - `git diff --stat 198ab3fe origin/main -- tests/`; `git -C <u3> status --porcelain` — пусто.
  - Блоб `HouseholderRoutineState.cs` = `842c1e04…` одинаков на `35a17831`, `2cea9a94`, `5fcdc11f`
    и в рабочей копии WIN-U3 — то есть дефект отгружен, а не локален.

state_changes: |
  1. ADD issue `i-the-morning-freezes-forever-when-the-door-path-is-refused-001`
     (`level: objective`, `route: work`, `blocks: t-the-morning-ends-at-the-door-1`).
  2. ADD issue `i-the-eta-omits-330-of-337-seconds-of-the-morning-001` (`route: work`) — включая
     дословную запись ошибки направления.
  3. ADD issue `i-the-six-minute-morning-is-tape-not-road-001` (`route: review`).
  4. ADD question `q-owner-what-he-does-when-he-cannot-leave-001` (`asks: владелец`, без `about`) —
     пять вопросов: поведение при отказе, экран для «не вышел», законный ли это исход, чинить ли до
     11.09, разрешена ли жалоба дома при постройке.
  5. Журнальные строки на ставку, задачу и наряд-продолжение WIN-U3.
  6. Наряд-продолжение WIN-U3 **не выдан**: форма починки требует его слова о поведении, а
     выдавать наряд под невыбранное поведение запрещено G7.
  7. План не тронут. Строка 111 плана («конца утра и выхода нет») устарела — это находка для
     `review` 11.09, а не правка этой ноги.

captures:
  - Строка прогноза волны про `Network/**` и посылка наряда про накопление ETA — **два случая одного
    класса за один день**: направление называло папку и механизм там, где надо было называть замер.
    Первый поймал review-скептик, второй — исполнитель. Третий случай сделает это правилом.
  - Наряд не потребовал закоммитить стенд замера, и теперь числа исполнителя нечем перепроверить.
    Кандидат в правило: замер, названный в `done_when`, обязан приезжать закоммиченным.
  - `HasExhaustedRefusedWalks` — второй мёртвый запасной путь того же класса, в `cuts` отчёта
    `35a17831` не назван.
  - EditMode-набор Unity (245 тестов) не прогонялся ни одним из шести замеров: MCP-серверы
    `unity-win-u1/u2/u3` отказывали `ConnectionRefused` во всех сессиях. Чтением подтверждено лишь,
    что `LeaveForWork` в `Assets/Tests` не упоминается ни разу.
  - Ветка активного отклика (шум, погоня) при отказе пути не разобрана — второй путь к тому же
    симптому, конечна ли там заморозка, неизвестно.

decisions_needed:
  - q: Пять вопросов карточки `q-owner-what-he-does-when-he-cannot-leave-001` — что делает хозяин,
      когда выйти не может; что показывает экран, если он не вышел; законный ли это исход раунда;
      чинить до 11.09 или после; разрешена ли жалоба дома при постройке.
    options: [ответить сейчас — наряд уйдёт сегодня; отложить до сверки 11.09 — дефект едет в демо]
    recommendation: ответить сейчас; у Д5 нет запасного пути, а Р2 гарантированно двигает мебель.
  - q: Живая проба конца утра его глазом до сих пор НЕ проведена (отчёт ноги,
      `docs/results/c-exec-g-5a7c-the-morning-ends-at-the-door-001.md:189`). По плану именно она
      закрывает строку рубежа, прогон только сторожит.
    options: [посмотреть сегодня; на сверке 11.09]
    recommendation: сегодня — это главный незакрытый пункт Д5, и он не про код.

play_check:
  - 1 recite: done — работа названа его словами: внимательно проанализировать возврат WIN-U3.
  - 2 owner inputs (owner): done — ни один из пяти вопросов о поведении не решён за него; наряд под
    невыбранное поведение не выдан. Его слово 01.09 про «стоит столбом» использовано как основание
    отказа от варианта 3, а не переистолковано.
  - 3 do the work: done — шесть замеров, шесть скептиков, свод; три улики и вопрос заведены.
  - 4 self-check: done — ошибка направления названа первой строкой вывода и записана в улику, а не
    в примечание. Два вывода собственных замеров сняты скептиками и не пошли в карточки. Числа
    исполнителя перепроверены и подтверждены, а не приняты на слово.
  - 5 close: done — `close: light — because` каждая строка выведена из байтов продуктового
    репозитория, из воспроизведённых прогонов и из его собственных слов. Поведения, которого не
    измеряли, эта нога не утверждает: механизм роста ошибки при chore и ветка активного отклика
    помечены непроверенными. G5-чат не открывается.

log: возврат WIN-U3 разобран тринадцатью агентами со скептиком на каждый вывод — направление ошиблось: накопления ошибки ETA в коде нет, исполнитель опроверг посылку наряда справедливо; но оба смотрели не туда, и настоящее число в двадцать раз крупнее — 330 из 337 секунд утра не входят в оценку по построению, потому что строки walk-route намеренно не добавляются в остаток, а отматываются по стенным часам; дефект вечной заморозки реален, лежит в main, не покрыт ни одним тестом (сторож слеп по фикстуре) и стоит на нулевом клиренсе — зазор между кроватью и комодом ровно равен диаметру тела, сдвиг на миллиметр даёт отказ; мыши вызвать не могут, но Р2 требует перестроить дом; из трёх вариантов починки не годится ни один, первый рождает ложную защёлку «он вышел» про стоящего в спальне; числа исполнителя воспроизводятся точно, но 1,75 м/с это стенд, а отгружаемая скорость 1 м/с; число 863 никто никогда не гонял

next: |
  Наряд-продолжение WIN-U3 держится до его слова: форма починки известна, поведение — нет.
  Слот WIN-U3 занят исполнителем, дерево чисто, ничего не потеряно.
  Ждёт его: пять вопросов про отказ выхода, и живая проба конца утра его глазом.
  На сверку 11.09: строка «утро 5–8 минут» набрана лентой; строка 111 плана устарела; база нарядов
  указывала 2cea9a94 вместо 5fcdc11f.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-work-g-5a7c-verify-u3-return-001.md
