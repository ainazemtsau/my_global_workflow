# RESULT — s-work-g-5a7c-close-safely-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-close-safely-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: work — девятая задача волны закрыта лёгким закрытием: все десять строк перевыведены первой рукой, две из них прогонами направления

close: light — because каждая из десяти строк приёмки перевыведена направлением первой рукой из закоммиченных байтов, reflog, артефактов прогона или СОБСТВЕННЫХ прогонов направления. Самотест закрывальщика и headless-набор запущены направлением лично; EditMode прочитан из машинного артефакта, называющего проверенный коммит. Ни одна строка не закрыта по заявлению исполнителя. G5-чат не открывался.

owner_approved: |
  Задача взята его словом 2026-08-31: «Девятая задача, окей, можем девятую взять». Расширение
  поверхности на `AGENTS.md` — решение направления, записанное в дополнение наряда 11:33:16 и
  прочитанное исполнителем из файла; в чат владельца оно не выносилось и его слова не требовало.

  Ни одна строка `done_when` не изменена.

outcome: |
  **ЗАКРЫВАЛЬЩИК БОЛЬШЕ НЕ ДВИГАЕТ ОБЩУЮ ВЕТКУ РАНЬШЕ, ЧЕМ ЧТО-ЛИБО ПРОВЕРИТ, И ЭТО ДОКАЗАНО НЕ
  РАССКАЗОМ, А REFLOG.** Закрытие шло с 11:39:22 по 11:44:57. В reflog `dev` за весь этот отрезок
  **ровно одна запись — `11:44:39`**, и она последняя. Раньше на этом месте стоял `merge` вторым
  шагом из семи, до единого запущенного теста.

  **ЛУЧШИЙ КОНТРОЛЬ ЭТОГО ДНЯ НИКТО НЕ ПЛАНИРОВАЛ.** В тот же час WIN-U2 гоняла СТАРЫЙ
  закрывальщик с такой же намеренной миной, и её первым действием стал коммит
  `1ac70d9c Seed headless close-gate failure`, положенный в `dev` **до тестов**. Два порядка, одно
  утро, один и тот же намеренный красный, два исхода — оба видны в одном reflog. Посевом такого не
  строят.

  **ПРЕДПОСЫЛКА КАРТОЧКИ ОКАЗАЛАСЬ НЕВЕРНА, И ЭТО ОШИБКА НАПРАВЛЕНИЯ, А НЕ НОГИ.** Строка приёмки 2
  писала: «слияния здесь почти всегда fast-forward, и тогда временная ссылка — это сам
  коммит-кандидат». Перемерено: `git log -1 --format=%p 80de396d` даёт **двух родителей**
  (`4e9177af 274d6057`) — настоящее слияние. Все три вызова закрывальщика за утро были настоящими
  слияниями, потому что между ними публиковались другие ноги. Путь «ничего не строится» существует
  и не сработал **ни разу на четырёхслотовом утре**. Строка приёмки несла оговорку «если оказалось
  иначе — назвать почему», он назвал, и потому линия закрыта её же escape-веткой, а не натяжкой.

  **ДЕСЯТАЯ СТРОКА СДЕЛАНА ГЛУБЖЕ, ЧЕМ ПРОСИЛО НАПРАВЛЕНИЕ, И ЭТО ЕГО ИНЖЕНЕРНОЕ РЕШЕНИЕ.**
  Дополнение просило сравнивать суффикс без учёта регистра. Он заметил то, чего не заметило
  направление: `select-slot.ps1` сравнивает лизу с учётом регистра **и при освобождении**, поэтому
  принять написание вызывающего значило бы перенести тот же отказ с шага `1/7` на `7/7` — с
  захваченным слотом. Решение: `Resolve-SlotLease` сличает идентификатор через `-cne` (имя — это
  личность), стадию через `-ine`, и возвращает **написание из общего состояния**, которое
  закрывальщик принимает как своё.

  **ДЕВЯТЬ ЗАМЕЧЕННОГО И НЕ ПОЧИНЕННОГО ВЫЛОЖЕНЫ СПИСКОМ, НИЧЕГО НЕ СПРЯТАНО.** Три из девяти
  разнесены этой ногой по адресам; остальные шесть — названные ограничения и объявленные решения,
  а не долги: асимметрия диапазонов объявлена решением, `checkout --force` назван единственным
  местом, способным уничтожить байты, узость проверки признания названа прямо.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ ПЕРВОЙ РУКОЙ** на опубликованном `main` = `dev` = `origin/main` =
  `slot/win-u4` = **`b22c00a2`**, слот `WIN-U4` = `AVAILABLE`, замок отпущен `11:44:57`:

  1. **`dev` двигается последним** — `git reflog show dev`: единственная запись отрезка,
     `b22c00a2 @ 11:44:39 close-leg: integrate c-exec-g-5a7c-close-safely-1-001 from WIN-U4`;
     баннеры шагов в опубликованном коде: `2/7 build the integration commit -- dev does NOT move
     here`, `3/7 run the tests`, `6/7 move dev, fast-forward main, publish`.
  2. **Настоящее слияние, не fast-forward** — `80de396d` имеет двух родителей.
  3. **Замок** — файл читался направлением, пока держался:
     `c-exec-g-5a7c-close-safely-1-001 WIN-U4 …`, затем строка отпускания. Случаи замка покрыты
     самотестом, запущенным направлением.
  4. **Сводка называет коммит** — артефакт `Logs/unity-gate/editmode-summary.json`, прочитан
     направлением: `commitAtEnd = 80de396d…`, тот самый интеграционный коммит. Направление
     **независимо наблюдало** `testrun` на `80de396d` и на отцепленной голове в 11:42, пока
     закрытие шло.
  5. **Диапазон публикации от `main`** — `close-leg.ps1:225-243`; `-AlsoDeliver` ищет отчёты в
     слитом дереве.
  6. **Самотест закрывальщика ЗАПУЩЕН НАПРАВЛЕНИЕМ:** `CLOSE-LEG SELFTEST: OK`.
  7. **Время названо числом до и после** — префлайт 3341 → 3585 мс; полное закрытие 339 с, из них
     301 с Unity.
  8. **Тесты.** Headless **ЗАПУЩЕН НАПРАВЛЕНИЕМ** на опубликованном `main`:
     `не пройдено 0, пройдено 725, всего 725`. EditMode прочитан из машинного артефакта:
     `passed 220, failed 0, total 220, result Passed`, Unity `6000.5.5f1`.
  9. **Отказ штамповать `DELIVERED` поверх признавшегося отчёта** — `close-leg.ps1:248-262`;
     случаи в самотесте, прогнанном направлением: «a report declaring itself blocked is caught»,
     «ordinary lower-case prose is deliberately not a declaration».
  10. **Регистр лизы** — `Resolve-SlotLease` в `close-leg-lib.ps1`: `id` через `-cne`, `stage`
      через `-ine`, возвращается записанное написание; `close-leg.ps1:159-166` его принимает.
      Случаи в самотесте: «a different change id is refused», «a malformed or absent lease is
      refused, never guessed at».

  **Поверхность ноги** — `git diff --stat 4e9177af b22c00a2`: восемь файлов, 1142 вставки,
  91 удаление. **Под `Assets/**` ноль байт.** `close-leg.selftest.ps1`: **255 вставок, ноль
  удалений** — строго аддитивно.

state_changes: |
  ЗАКРЫТО ПО ПЕРЕВЫВЕДЕННОМУ ФАКТУ (2):
  - `t-close-safely-1` → `done`;
  - `c-exec-g-5a7c-close-safely-1-001`.

  СОЗДАНО (2 улики):
  - `i-a-report-in-main-says-delivered-about-unfinished-work-001` — ложное «доставлено» в
    `gate-headless-1-001`, живое в `main`. Маршрут `work`, снимается вместе с `t-gate-headless-1`.
  - `i-the-shared-runner-guard-knows-only-some-of-its-users-001` — три остатка одной формы: замок
    защищает только владельцев нового скрипта, площадка узнаётся по имени папки, `select-slot.ps1`
    остался чувствительным к регистру. Маршрут `work`.

  ИСПРАВЛЕНО В КАРТОЧКЕ (0 строк приёмки; поправка в журнал):
  - предпосылка «почти всегда fast-forward» опровергнута замером; поправка записана журналом
    карточки, сама строка приёмки не менялась — она несла оговорку и закрылась по ней.

captures: |
  - **ЭТО БЫЛА МОЯ ОШИБКА ПРЕДПОСЫЛКИ, И ОНА НАШЛАСЬ ТОЛЬКО ПОТОМУ, ЧТО НОГА ЕЁ ПРОВЕРИЛА.**
    Направление написало «почти всегда fast-forward» из головы, не замерив. На четырёхслотовом
    утре путь не сработал ни разу. Правило на будущее: **любая предпосылка о ЧАСТОТЕ («обычно»,
    «почти всегда», «в типичном случае») в `done_when` должна нести замер или оговорку.** Здесь
    оговорка была, и она спасла строку от натяжки — но не по замыслу, а по удаче.
  - **РАЗРЕШЕНИЕ, ПОЛОЖЕННОЕ В ФАЙЛ, ДОШЛО ДО ИСПОЛНИТЕЛЯ БЕЗ ЧЕЛОВЕКА ПОСЕРЕДИНЕ.** Направление
    записало дополнение в `work/2026-08-31-call-close-safely-1.md` в **11:33:16**; исполнитель
    закоммитил обе правки в **11:38:43**, процитировав запрет направления почти дословно. Владелец
    ничего не пересылал и считал, что нога стоит. **Это первый за день случай, когда правило
    «разрешение живёт в файле, а не в чате» сработало наблюдаемо.** Стоит правила.
  - **Владелец едва не остановил идущее закрытие.** Он спросил, стопать ли ногу, в 11:42 — когда
    она держала замок интеграции и стояла на отцепленной голове в общей площадке. Остановка
    оставила бы замок висеть. Дешёвый приём, сработавший здесь: **перед ответом на «стопать?»
    читать файл замка и состояние площадки, а не рассуждать.**

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — возврат ноги по идущей задаче; закрытие уже опубликовано на момент чтения.
  2. WORK — десять строк приёмки перевыведены первой рукой; две проверены СОБСТВЕННЫМИ прогонами
     направления, одна — машинным артефактом; отчёт исполнителя читался как улика.
  3. (owner) — не требуется: задача взята его словом, расширение поверхности документное.
  4. CLOSE — лёгкое закрытие с обоснованием в поле `close`; G5-чат не открывался и не требовался.

log: |
  девятая задача волны закрыта лёгким закрытием: dev теперь двигается последним и это доказано
  reflog, а не рассказом; самотест и headless прогнаны самим направлением, EditMode прочитан из
  артефакта, называющего проверенный коммит; предпосылка карточки про fast-forward опровергнута —
  ошибка направления; два остатка разнесены уликами

next: |
  Волна: закрыто пять задач из девяти. Идут две ноги — зрение U1 и «почему» U3.
  **Свободны два слота: WIN-U2 и WIN-U4.**

  Ждут починки зрения три задачи: `t-morning-content-1`, `t-carry-back-1`, `t-bench-by-fact-1`.
  `t-gate-headless-1` остаётся открытой до красной улики.
