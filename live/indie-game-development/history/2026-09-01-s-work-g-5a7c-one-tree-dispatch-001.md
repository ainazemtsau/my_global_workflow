# RESULT — s-work-g-5a7c-one-tree-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-morning-content-1, полоса `утро`
track: утро
date: 2026-09-01
outcome_kind: work — свободный слот отдан не второй задаче волны, а глазу владельца: собирается одно дерево, где видно всё сразу, и в него сложены две улики, которые сами просили попутную ногу

close: light — because ни одна строка не закрывается этой ногой; всё, что она утверждает как факт — состав двух веток, единственность конфликта, числа профиля и счёт тестов — перевыведено направлением на байтах в предыдущей ноге того же дня и перепроверено здесь чтением карточек улик. Нога выдаёт наряд, а не принимает работу.

owner_approved: |
  Его слово 2026-09-01, дословно: **«слияние на посмотреть»** — выбор из двух названных вариантов
  для свободного слота WIN-U4.

  Ему предшествовало его же **«да, запускай свой шум в u4»**, от которого он отказался, услышав
  довод: обе задачи правят одну функцию и одну структуру.

outcome: |
  **НАПРАВЛЕНИЕ ВОЗРАЗИЛО ПРОТИВ ЕГО ЖЕ РАСПОРЯЖЕНИЯ, И ЭТО ОКАЗАЛОСЬ ВЕРНО.** Он велел пустить в
  свободный слот `t-a-fact-knows-who-made-it-1`. Замер, снятый до ответа, показал столкновение:
  задача про свой шум читает `SourceId`, задача про слух читает `PointX/PointZ/Loudness`, **и оба
  поля проходят через одну функцию `WorldSituationNoise.Of()` в одну структуру
  `HouseholderStimulus`**. Текстовый конфликт мелкий; опасность в другом — нога слуха вправе решить,
  что «слышит ли он» живёт в новом месте, и тогда вторая нога полдня строит на исчезнувшей форме.
  Это порог смерти №4 самой волны: два механизма на один вопрос. **Он согласился и выбрал другое.**

  **И ТОТ ЖЕ ЗАМЕР ДАЛ ТРЕТЬЮ НАХОДКУ ТОГО ЖЕ РОДА, ЧТО ДЕРЖИТ ВСЮ ВОЛНУ.** `WorldSituation`
  **уже несёт `SourceId`**, а в решающем слое его не читает никто: единственное упоминание вне
  самой структуры — `HouseholderObservationMarker.cs`. То есть «факт знает, кто его произвёл» —
  тоже не стройка, а незаданный вопрос. **Третий случай подряд: ингредиент лежит, вопрос не задан.**

  **ГЛАВНОЕ, ЧТО ВСКРЫЛОСЬ ПРИ ВЫБОРЕ РАБОТЫ ДЛЯ СЛОТА: ВЛАДЕЛЬЦУ ФИЗИЧЕСКИ НЕ НА ЧТО СМОТРЕТЬ.**

  | где | что есть | чего нет |
  |---|---|---|
  | `main` = `0e72d610` | дела с концом, руки, зрение | хозяин **стоит**: распорядок в один шаг |
  | `slot/win-u4` = `687b6b55` | ходьба по шести местам, утро | нет рук, нет дел |

  Приёмка **обеих** живых задач девятой волны — его глаз (`t-morning-content-1` строка 5,
  `t-carry-back-1` строка 6). **Целое существовало ровно один раз: в выброшенной рабочей копии,
  которую направление собрало часом раньше ради замера.** Две задачи стояли неделю в ожидании
  взгляда на то, чего не существовало ни в одном дереве.

  **ДВЕ УЛИКИ ВЛОЖЕНЫ В НОГУ ПО ИХ СОБСТВЕННОМУ УСЛОВИЮ, А НЕ РАЗДУТЫ В ЗАДАЧИ.** Обе прямым
  текстом писали, что отдельной ноги не стоят: баннер — «отдельной ноги на один баннер не нужно»,
  карта — «снимается первой же ногой, трогающей карту». **Эта нога — первая, кто оказался рядом**,
  и попадание точное: владелец через минуты сядет смотреть на полигон, где поперёк экрана висит
  **«DELIVERY POINT — BRING THE CARGO HERE»** от отменённой игры, а карта, писанная чтобы он знал
  чем смотреть, молчит про окно наблюдения, которым он и пользуется.

  **В НАРЯДЕ НЕТ НИ ОДНОГО ТРЕБОВАНИЯ К ТОМУ, КАК ОН БУДЕТ СМОТРЕТЬ.** Записка ограничена
  пятнадцатью строками и названа перечнем того, что лежит, а не протоколом проверки; карте
  запрещено предписывать поведение. Его правило про ноль требований к его игре соблюдено буквой.

evidence: |
  Снято этой ногой:

  - `WorldSituation.cs:73` — `public int SourceId { get; }`; поиск по `SourceId` вне редакторских
    файлов даёт **два места**: саму структуру и `World/HouseholderObservationMarker.cs`.
    **В решающем слое не читает никто;**
  - `WorldSituationNoise.cs:42-70` — `HouseholderStimulus` с `PointX`, `PointZ`, `Loudness`;
    обе задачи волны проходят через `WorldSituationNoise.Of()` — **поверхности пересекаются**;
  - реестр `gascoop-slot-state.v1.json`: **WIN-U2 CLAIMED**, аренда
    `t-noise-reaches-by-distance-1:BUILD`, MCP `http://localhost:27497/p/41ebd445`;
    **WIN-U4 AVAILABLE**; U1 и U3 свободны, но их MCP не отвечает (ConnectionRefused).

  Перевыведено предыдущей ногой того же дня и здесь используется как основание наряда:
  `main` = `0e72d610`; `slot/win-u4` = `687b6b55`; конфликт при слиянии **ровно один**, одной
  областью профиля; после разрешения `routine 6 · handlers 7 · occupations 4 · gauges 1` и тесты
  **744 / 743 / 1**; единственный красный — про слух, и он уже в работе в WIN-U2.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-one-tree-to-look-at-001` → слот **WIN-U4**, ветка `slot/win-u4`, база
    `687b6b55` + `main` `0e72d610`;
  - `work/2026-09-01-call-one-tree-to-look-at.md`.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-morning-content-1` и `t-carry-back-1` остаются `active` — наряд готовит их приёмку, но не
    закрывает: закрывает его глаз;
  - `t-a-fact-knows-who-made-it-1` остаётся `open` и невыданной — ждёт ответа головы волны;
  - обе улики остаются открытыми до возврата.

captures: |
  - **ДВЕ ЗАДАЧИ НЕДЕЛЮ ЖДАЛИ ВЗГЛЯДА НА ТО, ЧЕГО НЕ БЫЛО НИ В ОДНОМ ДЕРЕВЕ.** Приёмка обеих —
    его глаз, а собранного целого не существовало: куски лежали на разных ветках. Направление
    заметило это только когда искало работу для свободного слота.
    **Правило-кандидат: если приёмка задачи — глаз владельца, кто-то обязан отвечать за
    существование дерева, на которое он посмотрит. Иначе задача висит бессрочно, и никакой гейт
    этого не покажет.**
  - **ВОЗРАЖЕНИЕ НА ЕГО РАСПОРЯЖЕНИЕ ОКУПИЛОСЬ, ПОТОМУ ЧТО БЫЛО ЗАМЕРОМ, А НЕ МНЕНИЕМ.** Довод
    состоял из двух имён файлов и одной функции. **Он согласился сразу.**
  - **ТРЕТИЙ СЛУЧАЙ ПОДРЯД: ИНГРЕДИЕНТ ЛЕЖИТ, ВОПРОС НЕ ЗАДАН.** Точка шума, громкость, теперь
    `SourceId`. **Правило-кандидат, уже записанное при нарезке волны, подтверждается третий раз:
    прежде чем объявлять слой отсутствующим, искать его ингредиенты в структурах — они могут уже
    лежать и просто не читаться.**
  - **УЛИКА, НАПИСАВШАЯ СЕБЕ УСЛОВИЕ СНЯТИЯ, СНИМАЕТСЯ САМА.** Обе улики заранее сказали, чьей
    ногой их брать, и обе попали в первую же подходящую. **Это дешевле любой очереди приоритетов.**

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — его слово «слияние на посмотреть» из двух названных вариантов.
  2. WORK — столкновение поверхностей замерено ДО ответа и предъявлено именами файлов; реестр
     слотов прочитан, а не вспомнен; две улики вложены по их собственному условию, а не раздуты в
     задачи; наряду запрещено публиковать и запрещено трогать чужой красный.
  3. (owner) — его слово получено и процитировано дословно; возражение снято его же выбором.
  4. CLOSE — RESULT, применение, коммит; текст наряда уходит владельцу.

log: |
  свободный слот отдан не второй задаче волны, а глазу владельца — и по дороге вскрылось, что две
  задачи девятой волны неделю ждали взгляда на дерево, которого не существовало ни на одной ветке;
  запуск «своего шума» параллельно отклонён замером: обе задачи проходят через одну функцию и одну
  структуру, и SourceId оказался третьим подряд ингредиентом, который лежит и не читается; в ногу
  вложены баннер отменённой игры и карта, молчащая про стенд — обе улики сами просили попутную ногу

next: |
  Наряд `c-exec-g-5a7c-one-tree-to-look-at-001` уходит владельцу для **WIN-U4**.

  После него у владельца впервые есть одно дерево на посмотреть, и его глаз закрывает сразу две
  задачи девятой волны. `t-a-fact-knows-who-made-it-1` ждёт ответа головы волны из WIN-U2.
