# RESULT — s-work-g-5a7c-vert-1-close-001

play: work · close: light — because механические строки закрываются байтами и прогоном, которые эта
нога сделала своей рукой, а закрывающее слово владельца существует дословно и приведено как есть;
единственное, чего в его словах НЕТ, — это выбор одного из четырёх, и он не выдаётся за сделанный
direction: indie-game-development · node: g-5a7c · task: t-vert-1 · call: c-exec-g-5a7c-player-vertical-probe-001
date: 2026-08-09
base_read: OS — ветка `wt/indie-game-development` разошлась с `main`: нога, писавшая наряд,
коммитила в `main` (`2779b648`), а в рабочем дереве лежал панельный коммит `b94d8f40`. Сведено
слиянием `203b8a9e` ДО единой записи; незакоммиченная работа по панели сохранена нетронутой.
Продукт `C:/projects/Unity/GasCoopGame_dev`: `HEAD` = `dev` = `origin/dev` = `origin/main` =
`origin/slot/win-u1` = `slot/win-u1` = `102af1b1`. Продуктовые байты, ветки, слоты и slot-state этой
ногой НЕ изменялись.

## outcome

У мыши появилась высота, и её считает хозяин игры. Прыжок (`Space`) и полёт (удерживаемый `F`)
работают одновременно, мышь встаёт на одноразовый ящик высотой 4.5 роста и возвращается на пол;
обе копии рисуют одну высоту одной мыши, камера едет за своей.

Владелец прогнал Host/Join сам 2026-08-09, отверг НОЛЬ способов и разрешил закрыть задачу его
словами. Порог «ни то ни другое» не сработал.

**`t-vert-1` закрыта. Но записанный исход `оба` — это НОРМАЛИЗАЦИЯ ИСПОЛНИТЕЛЯ, а не буква
владельца, и здесь это названо прямо.** Наряд `c-exec-g-5a7c-player-vertical-probe-001` возвращён и
закрыт.

## evidence

**Слово владельца, дословно** (`docs/results/c-exec-g-5a7c-player-vertical-probe-001.md` на
`102af1b1`, прогон 2026-08-09):

> Проверил, всё работает, движки работают, полет работает, ну, пока значение, ну, наверное,
> нормальное, в принципе нормальное. В мультиплеере проверил, работает. Можно закрывать задачу.

плюс его прямая команда этой ноге 2026-08-09: «РАБОТА ЗАВЕРШЕНА ЗАКРЫВАЙ ЕЕ».

**Перемерено первой рукой этой ногой, а не принято с отчёта:**

| Что | Чем перемерено |
|---|---|
| Работа доставлена | `102af1b1` = `HEAD` = `dev` = `origin/dev` = `origin/main` = `origin/slot/win-u1` = `slot/win-u1`; merge `7695e4a6` имеет ровно двух родителей — прежнюю вершину `11f8ddbf` и принятый tip `edc4b92a` |
| **Байты владельца не переписаны слиянием** | дерево merge `436456a5` = дерево принятого tip `436456a5`, знак в знак; сверху только `102af1b1`, трогающий один файл отчёта |
| Кандидат стоит на объявленной базе | `8c3c48f4` имеет ровно одного родителя `11f8ddbf` — базу наряда |
| Два способа и их числа | `NetworkPlaySettings.asset`: добавлены ровно три ключа — `_walkerJumpSpeed: 2.2`, `_walkerFlightSpeed: 0.65`, `_walkerGravity: 3`. Ни один существующий ключ не изменён и не удалён |
| Высота у хозяина игры, а не у клиента | клиент шлёт ОДИН байт намерения (`Jump`/`Flight`) тем же `SubmitInputServerRpc` (`NetworkWalkerCourier.cs:139-145`) — ни высоты, ни скорости. Второго RPC, второго курьера и client authority нет |
| Обе копии видят одну высоту | `WalkerSnapshot` получил поле `Y`; `NetworkWalkerPresentation` рисует `snapshot.Y` вместо прежнего жёсткого нуля |
| Камера едет за своей мышью | `NetworkWalkerCamera.cs:41` — `standing.y + EyeHeight` вместо прежней абсолютной `EyeHeight` |
| Ящик существует в сцене | `IntegratedHouse.unity:948` — объект `Vertical Probe Box`; верх 0.72 м = 4.5 роста при росте 0.16 |
| **Стены верхов НЕ получили** | в `WalkSpaceProbe` бока есть ТОЛЬКО у одного ящика и только пока ноги ниже его верха; домовые стены остались плоскими блокираторами, независимыми от высоты |
| Груз не тронут | `_cargoRestHeight: 0.02`, `_cargoCarryHeight: 0.092`, `_cargoMaxHolders: 2`, телекинезные `1.28`/`0.8`, пороги шума — на `HEAD` те же. `CarryWorld.cs` изменён ТОЛЬКО добавленной перегрузкой; порядок вызовов груза сохранён, поведение груза не тронуто |
| Ворота зелёные | ПРОГНАНО ЭТОЙ НОГОЙ на `102af1b1`: `dotnet test … -c Release` → **127 пройдено, 0 не пройдено, 0 пропущено** |
| Слот освобождён | `gascoop-slot-state.v1.json`: WIN-U1 `lifecycle: AVAILABLE`, `lease: none` |

**Чего эта нога своей рукой НЕ повторяла:** Unity import/compile и сам прогон владельца — Editor
поднимает он, и подменять его глаз нечем. Прочитанное расхождение, не влияющее на публикацию:
локальная `main` в рабочем дереве `GasCoopGame_dev` отстала на четыре коммита от `origin/main`;
опубликованное состояние — в `origin/*`, и оно сошлось.

review: n/a — light change. Возвращающийся наряд не объявлял ни замороженной спеки, ни
`openspec/changes/<id>`; `docs/reviews/review-c-exec-g-5a7c-player-vertical-probe-001.md` в продукте
не существует — перемерено.

## Класс и подметание по закрытой находке ревью

- **Находка.** Гонка одноразового нажатия прыжка: край нажатия мог потеряться или повториться в
  зависимости от порядка тиков.
- **Класс (invariant).** Одноразовый край ввода, переходящий по сети, защёлкивается НА ХОСТЕ и
  потребляется ровно один раз за авторитетный шаг.
- **Исправление.** `AuthoritativeWalkerRoster`: `walker.JumpRequested` ставится при приёме намерения
  (`:226-227`) и гасится ровно в шаге (`:314-316`); покрыто двумя сфокусированными тестами.
- **sweep — родственные края, проверенные этой ногой первой рукой:**
  - переключатель хвата (`carryToggle`) — **closed**: та же форма. Клиент гасит свой край сразу
    после отправки (`NetworkWalkerCourier.cs:146-147`), хост копит в `_pendingToggles` и очищает
    ровно один раз за тик (`AuthoritativeCargoRoster.cs:299`). Это единственный второй одноразовый
    край в этом пути, и он уже был таким до этой ноги;
  - удерживаемый полёт краем не является — **n/a** по классу.
- **Названный остаток, не спрятанный:** два нажатия прыжка между двумя клиентскими тиками
  схлопываются в одно намерение. Это сжатие, а не потеря, и оно обычное для такого пути.

## Диспозиция по `done_when` наряда — все три строки

| Строка наряда | Диспозиция |
|---|---|
| 1. Прыжок и полёт на двух разных названных клавишах; мышь поднимается с пола, встаёт на ящик 4–5 ростов и возвращается вниз; владелец ДОСЛОВНО выбирает `прыжок`/`полёт`/`оба`/`ни то ни другое` | **ЗАКРЫТА ЧАСТИЧНО, И РАЗРЫВ НАЗВАН.** Механика: клавиши `Space` и `F` перемерены по коду и настройкам, ящик в сцене, верх 0.72 м = 4.5 роста. Прогон владельца состоялся. **Но дословного выбора одного из четырёх В ЕГО СЛОВАХ НЕТ.** Он сказал «всё работает, движки работают, полет работает… Можно закрывать задачу». `оба` получено ИСПОЛНИТЕЛЕМ через его же допущение №2 — прочтение «движки» как подтверждения прыжка. Допущение правдоподобно (скорее всего это распознанное «прыжки»), но это ВЫВОД, а не его буква |
| 2. Высоту считает хост; намерение идёт тем же `SubmitInputServerRpc` без параллельной власти; Host и Join показывают одну высоту, камера следует ей; невидимая форма правила покрыта engine-free тестами без тестов на tuning-величины | **ЗАКРЫТА.** Всё перемерено по коду этой ногой (см. таблицу). Клиент шлёт байт намерения, не высоту. 127/127 прогнаны этой ногой; величины 2.2 / 0.65 / 3 лежат в настройках и тестами на точное значение не заморожены |
| 3. Тесты, hygiene, реальная Unity import/compile и прогон владельца зелёные; diff доказывает неприкосновенность груза, числа рук, двух швов, W16, стен без верхов, дома/хозяина/облика/шума и общей настройки масштаба | **ЗАКРЫТА, и раздельно честно.** Тесты и неприкосновенность перемерены этой ногой построчно по диффу и по значениям на `HEAD`. Unity import/compile и owner-run — записи исполнителя и владельца, этой ногой не повторённые: Editor поднимает он |

## Диспозиция по `done_when` карточки задачи

**«Мышь может подняться над полом ДВУМЯ способами на двух клавишах… встать на примитивный ящик
высотой примерно в четыре-пять её ростов и слезть с него обратно; высоту считает создатель игры,
обе копии по сети показывают одну и ту же высоту одной и той же мыши, и камера едет за своей»** —
ЗАКРЫТА байтами и его прогоном.

**«Задачу закрывает СЛОВО ВЛАДЕЛЬЦА о том, каким способом мыши поднимаются — прыжок, полёт, оба,
или ни то ни другое»** — ЗАКРЫТА ЕГО ПРЯМОЙ КОМАНДОЙ, А НЕ ВЫБОРОМ. Его «Можно закрывать задачу» и
«РАБОТА ЗАВЕРШЕНА ЗАКРЫВАЙ ЕЕ» — живое указание владельца, которое по порядку власти старше
карточки, поэтому задача закрывается. Но выбор способа из четырёх он не произносил, ни один способ
не отверг, и `оба` в записи стоит с пометкой «нормализация исполнителя».

## Что закрытие НЕ покрывает — записано против себя

- **`оба` не подписано.** Заведена улика `i-vertical-both-is-a-normalization-not-his-word-001`. Пока
  она открыта, ни одна нога не вправе ссылаться на `оба` как на подписанный владельцем выбор
  геймплея и строить на нём вырезы или архитектуру.
- **Полёт меняет смысл подписанного критерия 2 узла.** Критерий требует «в ключевых комнатах есть
  проходимый путь снизу вверх и обратно». Если полёт остаётся, путь наверх перестаёт быть свойством
  ДОМА и становится свойством МЫШИ — то есть критерий закрывается не тем, чем задумывался. Это
  вопрос геймплея, и он его, а не ноги. Заведена улика
  `i-flight-makes-the-way-up-a-mouse-property-001`.
- **Числа приняты «пока»:** его же слова — «пока значение, ну, наверное, нормальное». Это не
  вердикт о том, что 2.2 / 0.65 / 3 остаются.
- **Перелёт стен не построен и не проверен** — стены по-прежнему без верхов, доказана только
  дорога пол ↔ верх ящика ↔ пол.

## state_changes

- `t-vert-1` → `done`, закрыта. Карточка `done_when` НЕ редактировалась.
- `c-exec-g-5a7c-player-vertical-probe-001` → закрыт как возвращённый и принятый.
- Заведена улика `i-vertical-both-is-a-normalization-not-his-word-001` (level `objective`, route
  `review`).
- Заведена улика `i-flight-makes-the-way-up-a-mouse-property-001` (level `objective`, route
  `review`).
- Зарегистрирован `c-work-g-5a7c-scale-4-call-001` (`to: session`, `play: work`, `for: t-scale-4`,
  `ready`), бриф — `work/c-work-g-5a7c-scale-4-call-001.md`.
- `g-5a7c` остаётся `active`; `NOW.bet` не менялся; карточка узла не редактировалась.

## captures

- У ходока теперь есть собственная гравитация `3 м/с²` как хостовое правило. Следующая задача
  включает НАСТОЯЩУЮ гравитацию живому грузу — два разных вертикальных закона в одной сцене стоит
  свести глазами при её наряде, чтобы падающий груз и падающая мышь не расходились на ощупь.
- Локальная `main` в `GasCoopGame_dev` отстала от `origin/main` на четыре коммита. Не мешает, но
  это ровно та поверхность, на которой уже жила улика про красный `-Deliver`.

## decisions_needed

Одно, и оно не блокирует закрытие: подтвердить одним словом, что мыши поднимаются ОБОИМИ способами
(прыжок и полёт остаются), либо назвать другой из четырёх. Его команда закрыть задачу исполнена
независимо от ответа; ответ снимает улику `i-vertical-both-is-a-normalization-not-his-word-001` и
решает судьбу критерия 2 узла.

## play_check

- 1 recite: done — цель и `done_when` `t-vert-1` и наряда пересказаны против возврата построчно.
- 2 owner inputs (owner): done — его слова существуют дословно и приведены: «Проверил, всё работает,
  движки работают, полет работает… Можно закрывать задачу», плюс «РАБОТА ЗАВЕРШЕНА ЗАКРЫВАЙ ЕЕ».
  Чего в них НЕТ — названо, а не додумано.
- 3 do the work: done — работа сделана нарядом в слоте WIN-U1 и вернулась; эта нога свела возврат с
  `done_when` наряда и карточки и перемерила механику первой рукой.
- 4 self-check: done — построчная диспозиция трёх строк наряда и двух строк карточки выше; ворота
  прогнаны этой ногой на `102af1b1` (127/127).
- 5 close: done — задача и наряд закрыты, названа продолжающая нога той же волны (`t-scale-4`).

## Гейты

- **G5.** `close: light` заявлен построчно: механика закрыта байтами и прогоном этой ноги, слово
  владельца существует дословно в закоммиченном документе. Что НЕ повторено своей рукой (Unity
  import/compile, его прогон) — названо явно.
- **G7.** ГЛАВНОЕ ЭТОЙ НОГИ: `оба` не превращено в его вердикт. Геймплей всегда его, поэтому
  нормализация исполнителя записана как вывод с указанием, из чего он сделан, и ему вынесен ровно
  один короткий вопрос, не блокирующий его же команду закрыть.
- **G3.** Аппетит не растянут: обе находки ушли в улики, не в работу.
- **G9.** Карточка узла не редактировалась — только строка в её журнал.
- **Порядок власти.** Живое указание владельца «ЗАКРЫВАЙ ЕЕ» старше строки карточки, требующей
  выбора из четырёх; задача закрыта по указанию, а расхождение записано, а не спрятано.

## log

вертикаль есть: прыжок и полёт на двух клавишах, высоту считает хозяин игры — задача закрыта его командой, но исход «оба» это нормализация исполнителя, а не его слово

## next

`CALL c-work-g-5a7c-scale-4-call-001` — нога направления пишет наряд исполнителю на `t-scale-4`
(лут живой только пока его держат лучом или он падает; лёг — неактивен, но пройти мешает).
Бриф: `work/c-work-g-5a7c-scale-4-call-001.md`.

END_OF_FILE: live/indie-game-development/history/2026-08-09-s-work-g-5a7c-vert-1-close-001.md
