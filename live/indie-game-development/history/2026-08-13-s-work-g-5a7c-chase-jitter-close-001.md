# RESULT — s-work-g-5a7c-chase-jitter-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-handlers-1
track: householder-life
date: 2026-08-13
outcome_kind: close — дрожь починена и опубликована, улика закрыта его глазом; наряд на обработчики разблокирован
close: light — because все три строки `done_when` наряда перемерены этой ногой первой рукой по закоммиченным байтам и прогону, кроме одного КРАСНОГО прогона до починки, который резолвится записанным выводом в опубликованном отчёте, и кроме глаза владельца, чьи слова уже закоммичены и сказаны мне прямо. Ни одна строка не требует суждения, которого никто не выносил.

owner_approved: |
  **Мне, дословно, 2026-08-13:** «c-exec-g-5a7c-chase-jitter-001 выполнена».

  **Его вердикт по игре, закоммичен в продукте на `27c98d14`
  (`docs/results/c-exec-g-5a7c-chase-jitter-001.md` §Manual-acceptance), дословно:**
  «проверил хозяин не дергается можно закрывать».

  Дефект нашёл его глаз, и его же глаз его закрыл.

outcome: |
  **ДРОЖЬ ПОЧИНЕНА, ОПУБЛИКОВАНА И ЗАКРЫТА ЕГО СЛОВОМ.** Улика
  `i-householder-chase-path-jitters-in-doorways-001` уезжает в закрытые.

  **ПРИЧИНА ОКАЗАЛАСЬ НЕ ТОЙ, ЧТО НАЗЫВАЛ НАРЯД, И ЭТО ПОКАЗАЛ ЗАМЕР — ровно как наряд и требовал.**
  Наряд ставил ведущей причиной перестроение маршрута каждый тик. Замер подтвердил перестроение
  (30 раз в секунду, 81 % из них дают другой маршрут) — **и тут же его опроверг как причину дрожи:**
  в открытой комнате перестроение идёт ровно так же часто, 98,9 % дают другой маршрут, а разворотов
  ноль и метров назад ноль.

  Настоящая причина: перестроение **сбрасывает прогресс по маршруту в ноль**. Пока маршрут — одна
  точка, сбрасывать нечего. Как только маршрут идёт через проём, он начинается с именованного места,
  и хозяин каждый тик получает первой точкой то, что уже позади него. 5,3 метра ходьбы ради 34 см,
  61 % из них назад, заперт в коробке `0,34 × 1,0` м **в метре не доходя до проёма**.

  **ЭТО ЗАОДНО ОБЪЯСНИЛО ЕГО СОБСТВЕННУЮ ОГОВОРКУ, КОТОРОЙ НИКТО НЕ ПОНИМАЛ.** Он сказал «вроде бы
  было даже не в проёме такое» — и был прав буквально: дёргался хозяин НЕ в проёме, а внутри спальни
  около места `West Door Side`. Улика хранила это как загадку; замер её снял.

  **ШИРИНА ПРОХОДА К ЭТОМУ ДЕФЕКТУ НЕ ПРИЧАСТНА, и это измерено, а не отброшено.** До починки хозяин
  до проёма вообще не доходил — «пересёк линию стены: не пересёк ни разу». После починки проходит в
  `0,033` м от центра при запасе `0,25`. Итог разведения: **100 % дёрганья — сброс прогресса, 0 % —
  косяк двери.** `i-householder-walks-through-walls-001` не закрывается: она остаётся про
  НЕцентральные случаи (несомый груз, двое в проёме) и получает этот замер в журнал.

  **ЧТО ОСТАЛОСЬ И НАЗВАНО ВСЛУХ:** 3 % ходьбы назад и один разворот в секунду на настоящей смене
  комнаты. Исполнитель их не убирал, объяснил почему (это законная перерисовка, и её первая точка
  иногда позади него, потому что маршрутизатор соединяет позицию только с именованными местами), и
  вынес владельцу прямым вопросом. Владелец прогнал и не увидел. Вклад упал с 61 % до 3 %.

  **НАРЯД НА ОБРАБОТЧИКИ РАЗБЛОКИРОВАН:** `c-exec-g-5a7c-handlers-1-001` возвращается `waiting →
  ready`, поле ожидания снято. Он ждал ровно эту ногу и больше ничего.

evidence: |
  **ВСЁ НИЖЕ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ. Слово владельца «выполнена» принято как улика, а не как факт —
  по нашему же правилу.**

  ## Голова

  `git ls-remote origin` → `main` = `dev` = **`27c98d14918568543d92c6a199953777be8a5cad`**
  (было `c485b30e`). Работа ОПУБЛИКОВАНА.

  Цепь `c485b30e..27c98d14` — четыре коммита, из них производственный РОВНО ОДИН:
  `0700582f` «Hold the chase walk instead of redrawing it every tick»; остальные три
  (`e0929a4f`, `ea00a874`, `27c98d14`) трогают только `docs/results/c-exec-g-5a7c-chase-jitter-001.md`.

  ## Строка 1 наряда — причина измерена числом

  Числа лежат в опубликованном отчёте таблицей и печатаются самим прогоном. **Я прогнала набор сама**,
  и вывод несёт измеренную строку живьём:
  `crossed the wall line  Bedroom Doorway: 0.033m off centre (clearance 0.25m) — clears the frame`.
  Замер сделан и в проёме, и в открытой комнате, как требовал наряд, и именно сравнение двух этих
  колонок опровергло гипотезу наряда.

  ## Строка 2 — починено измеренное, красный до, зелёный после

  - **Зелёный — мой прогон:** `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release`
    → **`не пройдено 0, пройдено 254, пропущено 0, всего 254`**. Против базовых **252**, дельта **+2**.
    Пропущенных ноль — числа не набраны отключёнными тестами.
  - **Красный до починки** — единственная строка, которую я НЕ перевыводила своей рукой: она
    резолвится записанным выводом прогона в закоммиченном отчёте с точными ожиданиями и фактами
    (`Expected: less than 2.0f But was: 18.666666f`; `Expected: less than 0.1f But was: 0.611284912f`).
    Перевыводить её значило бы откатывать опубликованный продукт.
  - **Правка мала и бьёт ровно в названное место** — `Householder.cs` `+19/-6`: связка
    «`EndsAt` плюс перерисовка» заменена на `HouseWalk.Toward(...)` с выходом `startAfresh`, и в коде
    записан сломанный инвариант дословно. Настроечного числа не появилось — контракт 36 требует
    данных для допуска, а допуска здесь нет вовсе.

  ## Строка 3 — зрение цело, откат настоящий

  - **Зрение:** все **252** прежних теста зелёные в моём прогоне без единой правки; в производственном
    диффе `c485b30e..27c98d14` **ноль файлов зрения и ноль файлов настроек** — проверено
    `git diff --name-only`. Ассет зрения, ранг `2.5`, правило «самый громкий за тик» и пороги
    `60/150/400` не тронуты.
  - **ОТКАТ ПЕРЕМЕРЕН МОЕЙ РУКОЙ, а не взят из отчёта.** Отчёт говорит, что демонстрационный коммит
    отката снят и не публикуется, — но объект жив, и я его прочитала:
    `d2701686` = `Revert "Hold the chase walk instead of redrawing it every tick"`,
    родитель `0700582f`, **дерево `ecab5bc02b4ccdf40065f8f01ca67b0dfd0ff21f` — побайтно равно дереву
    базы `c485b30e`**; `git diff --stat c485b30e d2701686` пуст. Настоящий `revert`, деревья сошлись.
  - **Дифф ровно три файла** (`HouseWalk.cs`, `Householder.cs`, новый
    `tests/TunnelCrew.Core.Tests/HouseholderChaseJitterTests.cs`) плюс один документ. Ни одного
    запрещённого файла: ни груза, ни сцен, ни префабов, ни поверхности обработчиков.

  ## Тревога про слот U1 из отчёта — ПРОВЕРЕНА, ничего не потеряно

  Исполнитель предупредил, что `slot/win-u1` несёт четыре неопубликованных коммита лута и отстаёт от
  `main`. Перемерено сейчас:

  - все четыре объекта (`f5df5e54`, `dbf95a9d`, `43d40286`, `e0a30194`) **живы и держатся тремя
    ссылками сразу**: `refs/heads/preserve/c-exec-g-5a7c-loot-1-001-win-u1-local-20260813`,
    та же ветка на `origin`, и `refs/backup/pre-slot-u1-realign-20260813/slot-win-u1`;
  - это не четыре разных работы, а одна цепь кандидата лута: сборка → откат → повтор → запись улик,
    с вершиной `e0a30194`, которая и есть замороженный кандидат;
  - `slot/win-u1` уже выровнен на `main` (`27c98d14`) с созданной резервной ссылкой.

  **Ничего не потеряно и терять было нечего.** Записано, потому что предупреждение было верным по
  форме: селектор слотов показывает `CLEAN / AVAILABLE` и не проверяет, отстаёт ли ветка.

state_changes: |
  1. `c-exec-g-5a7c-chase-jitter-001`: `ready → done`, уезжает в `closed/`. Основание — светлое
     закрытие, разобранное построчно выше.
  2. `i-householder-chase-path-jitters-in-doorways-001`: `open → done`, уезжает в `closed/`. Закрыта
     починкой и его глазом.
  3. `c-exec-g-5a7c-handlers-1-001`: `waiting → ready`, поле `waiting_on` снято. Наряд не тронут ни
     одной буквой сверх этого.
  4. `work/c-exec-g-5a7c-handlers-1-001-call.md`: в `context` добавлен один абзац-предупреждение про
     устаревший тестовый дом. Это объявленная правка файла `work/`, а не переписывание наряда: ни
     `goal`, ни `boundaries`, ни `done_when`, ни `return` не изменены.
  5. `i-householder-walks-through-walls-001`: журнальная строка с замером — маршрут ставит хозяина в
     `0,033` м от центра проёма при запасе `0,25`, то есть по центру он проходит. Улика НЕ
     закрывается: остаётся про нецентральные случаи.
  6. Заведена улика `i-test-house-draft-describes-a-house-that-is-gone-001`.
  7. Полный RESULT сохранён в `history/2026-08-13-s-work-g-5a7c-chase-jitter-close-001.md`.

  **ЧЕГО ЭТА НОГА НЕ СДЕЛАЛА.** Не тронута карточка ставки. Не тронуты `t-loot-1`, наряды по луту,
  ветка `preserve/...` и резервная ссылка. `t-handlers-1` остаётся `active` — она не закрывается этой
  ногой и её собственное закрытие судит его глаз в собранной игре. В продукте не изменено ничего.

captures:
  - |
    **НАРЯД НАЗВАЛ ПРИЧИНУ НЕВЕРНО, И ИМЕННО ПОЭТОМУ ЗАМЕР СТОЯЛ ПЕРВОЙ СТРОКОЙ.** Выдача нашла в
    байтах красивый механизм — сломанный инвариант точного сравнения — и он оказался ВЕРНЫМ фактом и
    НЕВЕРНОЙ причиной: перестроение идёт, но дрожи само по себе не даёт. Дрожь давал сброс прогресса
    по маршруту, которого выдача не заметила вовсе. Правило «сначала измерить, потом чинить»
    сработало ровно так, как задумано, и стоило одной строки наряда. **Класс на будущее: красивое
    объяснение, найденное чтением, — это гипотеза той же силы, что и чужая гипотеза, даже когда оно
    подтверждается байтами.**
  - |
    **ЕГО ОГОВОРКА ОКАЗАЛАСЬ ТОЧНЫМ ПОКАЗАНИЕМ, А НЕ ШУМОМ.** «Вроде бы было даже не в проёме такое»
    мы держали как неудобную деталь, ослабляющую главную гипотезу. Замер показал, что он описал
    место буквально верно: дёрганье шло внутри спальни, у именованного места, в метре до двери.
    Дважды подряд теперь его неточная на слух формулировка оказывалась точнее нашей уверенной.

decisions_needed: []

play_check:
  - 1 recite — done. Пересказаны три строки наряда `c-exec-g-5a7c-chase-jitter-001`, обязательство улики «сначала измерить, а не чинить по гипотезе», её же законный исход «это дубль» и разведение с уликой про ширину прохода.
  - 2 owner inputs (owner) — done. Его слова получены дословно дважды: мне прямо — «c-exec-g-5a7c-chase-jitter-001 выполнена», и закоммичено в продукте — «проверил хозяин не дергается можно закрывать». Нового прогона не запрашивалось.
  - 3 do the work — приём возврата с перемером по байтам: голова, цепь коммитов, состав диффа, свой прогон 254, чтение коммита отката и сверка деревьев, проверка тревоги про слот U1.
  - 4 self-check — done против трёх строк наряда построчно; названа единственная строка, не перевыведенная своей рукой (красный прогон до починки), и названо, чем она резолвится.
  - 5 close — светлое, и основание записано в поле `close`. Закрыт наряд и улика; задача `t-handlers-1` НЕ закрывается.
  - G5 — не открывался: светлое закрытие законно, потому что каждая строка резолвится сама — байтами, моим прогоном или его уже закоммиченными словами.
  - G7 — done. Ни одного игрового решения: остаточные 3 % ходьбы назад оценил его глаз, а не нога.
  - G3 — done. Аппетит не расширен: ноль новых задач; одна улика закрыта, одна заведена указателем, одна дополнена журнальной строкой.
  - G10 — done. RESULT финальный.

log: дрожь ПОЧИНЕНА, ОПУБЛИКОВАНА и закрыта ЕГО ГЛАЗОМ — «проверил хозяин не дергается можно закрывать» в продукте и «выполнена» мне прямо, и это перемерено, а не принято на слово: origin/main = origin/dev уехали с c485b30e на 27c98d14, производственный коммит РОВНО ОДИН на три файла плюс три документных, ноль файлов зрения и настроек в диффе, 254 из 254 прогнаны моей рукой при нуле пропущенных против базовых 252, и коммит отката d2701686 ПРОЧИТАН лично — его дерево ecab5bc0 побайтно равно дереву базы, git diff пуст, хотя отчёт называл этот коммит неопубликованным; ГЛАВНОЕ — ПРИЧИНА ОКАЗАЛАСЬ НЕ ТОЙ, ЧТО НАЗЫВАЛ НАРЯД, и это показал замер, стоявший первой строкой ровно для такого случая: перестроение маршрута каждый тик реально идёт 30 раз в секунду и в 81% даёт другой маршрут, но дрожи само по себе НЕ даёт — в открытой комнате оно идёт так же часто при нуле разворотов и нуле метров назад; настоящая причина в том, что перестроение СБРАСЫВАЕТ ПРОГРЕСС по маршруту в ноль, и на маршруте через проём хозяин каждый тик получал первой точкой то, что уже позади него — 5,3 м ходьбы ради 34 см, 61% из них назад, заперт в коробке 0,34×1,0 м в метре НЕ ДОХОДЯ до проёма; этим же снята его оговорка «вроде бы было даже не в проёме такое» — он был буквально прав, дёргался хозяин внутри спальни у именованного места, а не в двери; ширина прохода разведена измерением и к дефекту не причастна вовсе — до починки он до проёма не доходил ни разу, после проходит в 0,033 м от центра при запасе 0,25, поэтому i-householder-walks-through-walls-001 НЕ закрывается и получает этот замер в журнал как уточнение про нецентральные случаи; остаточные 3% ходьбы назад на настоящей смене комнат исполнитель не убирал, объяснил и вынес владельцу прямым вопросом, а владелец прогнал и не увидел; тревога отчёта про слот U1 проверена и ничего не потеряно — четыре коммита это одна цепь кандидата лута с вершиной e0a30194, держится тремя ссылками сразу включая резервную, а сам слот уже выровнен на main; наряд на обработчики РАЗБЛОКИРОВАН waiting → ready и в его context добавлено одно предупреждение про устаревший тестовый дом ThreeRoomDraft, который описывает три комнаты и два проёма вместо нынешних четырёх и трёх — заведено уликой, потому что его зелёные тесты доказывают что-то не про тот дом, на который владелец нажимает Play, а следующий наряд как раз обязан опереться на комнату

next: |
  `c-exec-g-5a7c-handlers-1-001` — наряд на обработчики, теперь `ready` и ничем не заблокирован.
  Слот выбирается селектором.

  Параллельно в `house-and-things` идёт `c-exec-g-5a7c-loot-foundation-001`.

END_OF_FILE: live/indie-game-development/history/2026-08-13-s-work-g-5a7c-chase-jitter-close-001.md
