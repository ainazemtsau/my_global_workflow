# RESULT — s-repair-g-5a7c-wave-reshape-001

play: repair
direction: indie-game-development · node: g-5a7c
date: 2026-08-08
base_read: OS `d1561d8f`, дерево `wt/indie-game-development` чисто на входе. Продукт читался на
`ae023ba5`; продуктовые байты, ветки и slot-state этой ногой НЕ изменялись.

## 1. Противоречие, в двух строках

Ставка несла задачу `t-scale-3`, которая добавляла игроку высоту и **ни одного способа её
изменить**: в мире не на что встать, стены в правилах не имеют верха, поэтому высота осталась бы
вечным нулём, а задача закрылась бы, не сдвинув игру. Владелец, разбирая волну, остановил и её, и
весь порядок: «я не думаю, что нам тут дом нужен… нам нужен телекинез, вертикальность и пропорции.
Вот это нам нужно сделать».

Вторым противоречием была задача `t-scale-4`: она требовала включить гравитацию, а это ломало его
же требование `W120` — лежащее под гравитацией тело шумит вечно.

## 2. Реконструкция

- **`t-scale-3` непроверяема по устройству.** Перемерено первой рукой на `ae023ba5`: в сцене нет
  ни одного предмета, кроме пола и восьми стен; стена в слое правил несёт только `CenterX`,
  `CenterZ`, `HalfX`, `HalfZ` — высоты у неё нет вообще, выталкивание идёт плоским кругом против
  плоского квадрата (`Network/WalkSpaceProbe.cs`). Встать не на что, перелететь нельзя.
- **Мышь — не физическое тело, и это записано в коде прямым текстом** (`WalkSpaceProbe.cs`:
  «мышь НЕ является физическим телом и никогда не симулируется»). Значит её вертикаль — арифметика
  в слое правил, а не настройка физики: дёшево и предсказуемо.
- **Груз — единственное настоящее физическое тело**, и его вертикаль стоила дорого по причине,
  которую первый разбор не называл. Перемерено: шум записывается не только при ударе, но и каждый
  тик, пока есть касание — `Network/CargoBodyContact.cs`, обработчики `OnCollisionEnter` и
  `OnCollisionStay`, сила = импульс / шаг. Лежащее под гравитацией тело даёт импульс опоры,
  равный весу: при массе 20 это **165–195 Н** против порогов шума **60 / 150 / 400**. То есть
  просто лежащий груз стучал бы выше среднего порога вечно.
- **Владелец нашёл решение сам, и оно лучше того, что предлагала нога.** Нога предлагала обойти
  проблему управляемой высотой вместо гравитации. Он предложил перерезать провод в правильном
  месте: лут неактивен, пока его не взяли лучом. Тогда гравитация настоящая, но живёт только там,
  где нужна, а лежащий лут молчит по устройству. Предложение ноги снято в пользу его решения.
- **Дом отложен его словом**, и это возврат к его же подписанному аппетиту, где вариант «Б плюс
  дом» был отклонён именно за то, что растягивается в постройку дома до первого касания телекинеза.

## 3. Исправленное состояние

| Изменение | Причина |
|---|---|
| `t-scale-3` снята и закрыта | Труба без содержимого: высота без способа её изменить и без того, на что встать |
| Заведена `t-vert-1` — проба вертикали мыши | Заменяет снятую. Прыжок И полёт на двух клавишах, ящик высотой в 4–5 ростов, закрытие — его слово о том, каким способом мыши поднимаются |
| `t-scale-4` переписана целиком | По его решению про лут: гравитация настоящая, но только на живом луте; лежащий неактивен, не шумит, не двигается, но мешает пройти |
| Новый порядок: телекинез 1, проба вертикали 2, груз 3, вчетвером 4, швы 5 | Телекинез первым, потому что он работает уже сегодня и на нём висит порог смерти ставки. «Вчетвером» обязана быть после груза: «чем больше взялись, тем ВЫШЕ идёт груз» без высоты груза невыразимо |
| Заведена запись `knowledge/loot-is-inactive-until-a-beam-touches-it.md` | Его решение по игре записано ЕГО словами как решение, а не протащено как деталь реализации |

Дом в волне не появляется. Ставка `g-5a7c` остаётся `active`, карточка узла НЕ редактировалась,
`NOW.bet` не менялся.

**Граница снятия соблюдена.** Задача `t-scale-3` снимается его прямыми словами про эту волну и это
решение; открытых нарядов на неё не было — наряд `c-exec-g-5a7c-player-vertical-001` был написан,
но не отправлен, и закрывается вместе с задачей.

## 4. Что НЕ сделано и почему

- **Критерий 4 карточки узла не тронут.** Правило про лут его сужает, а не опровергает: «шум это
  выбор команды» становится буквально верным (один волочит и шумит, четверо несут и тихо, лежит и
  молчит). Правка карточки не требуется, и потому не делается.
- **Касание предмета как активация** («мышь задела музыкальную шкатулку, та включилась») — не
  строится и заранее не закладывается. Перемерено, что это будет дёшево: проверка свободного места
  уже сейчас вычисляет факт упора и выбрасывает его, возвращая только исправленную позицию, — то
  есть нужно вернуть ещё и «во что упёрлась». Место этому названо его же карточкой, критерий 5:
  «обстановка, которую несущие задевают, и задевание идёт ТЕМ ЖЕ швом». Это волна дома.
- **Стены без верха** — работа не оценена и в волну не входит; названа в карточке `t-vert-1` как
  известное ограничение пробы.

## captures

- Музыкальная шкатулка / будильник — предмет, который включается от касания. Его слова: «влетела,
  чётко коснулась — активируется». Отличить чёткое касание от «прошла вдоль и задела» — вопрос
  ощущения и вопрос ВЛАДЕЛЬЦА, когда дойдёт. Будильник уже существует во владельческой композиции
  масштаба как `ALARM CLOCK 12cm`.
- Мышь больше не сможет задеть предмет и что-то опрокинуть. Он назвал это приемлемым; обратимо
  поштучно — отдельный предмет можно сделать всегда живым, не меняя правило.
- Второй этаж и крыша: он назвал их вероятными («дом может быть двухэтажным… на другие этажи или
  крышу идти»). Стены без верха делают это отдельной неоценённой работой. Решается после пробы.

## decisions_needed

Нет. Все решения приняты владельцем в разговоре и процитированы.

## play_check

- 1 назвать противоречие: done — задача, закрываемая без изменения игры, и задача, ломающая его
  собственное требование про тишину лежащего груза.
- 2 реконструкция: done — по разбору (`W10`, `W11`, `W16`, `W20`, `W21`, `W120`), по аппетиту
  карточки и по шести перемеренным первой рукой фактам продуктового кода.
- 3 предложенное состояние: done — пять изменений, по причине на каждое.
- 4 подтверждение (owner): done. Его слова: «нам нужен телекинез, вертикальность и пропорции, вот
  это нам нужно сделать… дом будет прорабатываться отдельно, его не надо»; «я согласен с твоим
  решением» (порядок и проба с двумя клавишами); «когда игроки не поднимают лут, он как бы и
  звуков не должен издавать… условно неактивный»; «я думаю, что лут мешает пройти»; «продолжаем
  дальше вот именно по этому решению».
- 5 фрикция: done — см. ниже.

## FRICTION

Третий случай одного класса за один день: строка задачи, требующая устройства («гравитация
включена»), происходит из строки разбора, которая называется «Цена вертикали у груза» — то есть из
ЗАМЕРА, а не из требования. Первые два случая — точка масштаба и «третье число живёт в состоянии
игрока». Общее у всех трёх: замер стоимости или наблюдение о коде превращается в приказ об
устройстве, проходит все ворота и останавливается только вопросом владельца «а зачем это вообще».
Обязанность проверять происхождение несущих строк, введённая сегодня в бриф, поймала третий случай
— но она живёт в тексте одного брифа, а не в правиле. Это заявка на изменение OS.

## Гейты

- **G7.** Все решения продукта приняты владельцем и процитированы. Предложение ноги (управляемая
  высота вместо гравитации) снято в пользу его решения, а не защищалось.
- **G9.** Карточка узла не изменена.

## log

волна пересобрана его словами: телекинез первым, проба вертикали вместо трубы в никуда, лут неактивен пока его не взяли лучом

## next

`CALL c-work-g-5a7c-telekinesis-call-001` — нога направления пишет наряд исполнителю на
`t-scale-5` (телекинез лучом), с обязанностью проверить происхождение каждой несущей строки задачи.

END_OF_FILE: live/indie-game-development/history/2026-08-08-s-repair-g-5a7c-wave-reshape-001.md
