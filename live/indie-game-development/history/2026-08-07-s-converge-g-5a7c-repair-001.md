# RESULT — s-converge-g-5a7c-repair-001

direction: indie-game-development
play: converge
node: g-5a7c
call: work/c-converge-g-5a7c-repair-001-call.md
date: 2026-08-07
session: свежая физическая сессия, отдельная от `s-converge-g-5a7c-real-core-001`, от всех трёх
      `s-converge-arch-g-5a7c-00*`, от `s-converge-verify-g-5a7c-001` и от любого day-чата
mode: РЕМОНТ по именованным находкам первой проверки. Разбор узла НЕ переоткрывается.

**Веер.** Опровержение и перемер сделаны одним контуром: в этом харнессе действует прямая постоянная
инструкция владельца не разворачивать субагентов и workflow, и по `os/KERNEL.md` живая инструкция
владельца выше плея. Названо здесь же, чтобы следующая проверка не прочла эту ногу как веерную. Шаг 2
плея (`call:research` — миner по словарю) не запускался и по этой же причине, и потому что §GLOSSARY
находок не имеет и наряд его трогать запретил.

**База продуктовых фактов перепроверена ДО правок:** `C:/projects/Unity/GasCoopGame`,
`git rev-parse HEAD` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`, `git rev-parse main` — тот же
объект, `git status --short` пусто. Репозиторий этой ногой не изменялся.

---

## outcome

**Шесть возвращённых мест закрыты, и ни одно не закрыто выдуманным ответом.** Из восьми `done_when`
наряда выполнены все восемь. Правлено ровно 13 строк §WHAT из 122 (семь переписанных, шесть новых)
плюс легенда раздела; остальные 109 строк из прежних 116 не тронуты ни знаком, §IMPORT, §GLOSSARY,
§DRIFT, оба §SIGNOFF и весь §CONTRACTS (`C01`–`C26`) — тоже.

**Главное, и это ответ на вопрос, который наряд ставил прямо: ни одна дыра не заткнута ответом.**
Шесть новых строк заведены ОТКРЫТЫМИ, четыре понижены из `acceptance` в открытые, и `open_rows`
вырос с 1 до 10. Это и есть предписанный плеем и нарядом исход: строка, которую нельзя закрыть
источником вне ноги, помечается `open` с названным отвечающим. Владельцу не вынесено ни одного
нового решения; единственная строка, которая должна ему, — `W94` — названа `open → OWNER` без
формулировки вопроса.

**Одно место закрылось НЕ ремонтом, а собранным маршрутом, и это единственная строка, ушедшая из
открытых:** `W103` перестала говорить «узел-получатель не назван нигде» и получила адресат
`review`/`map` со ссылкой на `C07`, потому что все три источника маршрута лежат вне решающей ноги и
перепроверены проверкой дословно.

**Чего эта нога сознательно НЕ сделала, чтобы следующая не искала:** не тронула `work/converge-g-5a7c-arch.md`
(его `reads:` в строке 5 до сих пор несёт `open_rows: 1` — это теперь неверно, и правит это арх-наряд,
у которого в `done_when` 7 стоит требование свести счета), не тронула §CONTRACTS, не завела ни
одного нового числа, не выбрала между «даром» и «отдельной величиной» в `W115`, не назначила порог
по числу рук в `W114` и не определила пол в `W116` — ближнюю половину последнего закрывает арх-наряд.

---

## evidence

### 1. `W07b` — обоснование опровергнуто арифметикой и снято (наряд, `done_when` 1)

Строка сохранила числа (`масштаб 12.5`, `_movementSpeed` 3 → 1.0, `_cargoMass` 4 → 50, числа силы не
тронуты) и **потеряла обоснование целиком**. Вместо него в строке стоят три опровержения, все
перемерены этой ногой первой рукой на `352f96b0`:

- **(а)** строка делит скорость на 3, а сохранение чисел силы обосновывала тем, что «скорости падают
  в 12.5 раз». При её собственных числах `m·Δv` растёт в 12.5 / 3 ≈ **4.2 раза**. Шаг между полосами
  перемерен по ассету: `_cargoRustleForce: 60`, `_cargoKnockForce: 150`, `_cargoHeavyNoiseForce: 400`
  — то есть 2.5 и 2.67 раза. Множитель 4.2 больше любого из двух шагов, значит контакт уезжает как
  минимум на полосу вверх, типичный — почти на две, и **три настроенных порога смысла не
  сохраняют**. (Проверка назвала это «примерно на две полосы»; точное отношение записано числом,
  чтобы следующая проверка не пересчитывала.)
- **(б)** «ускорение падает ровно во столько, во сколько уменьшился мир» верно только в насыщении.
  `факт кода` перемерен: `Core/Cargo/CargoHaul.cs:109` — `effort = Math.Max(0f, stretch -
  rules.HandReach) * rules.HandStiffness`. Растяжение — длина, падает в 12.5 раз; жёсткость сохранена
  нарочно; масса растёт в 12.5 раз; значит ускорение падает в 12.5² ≈ **156 раз**, а не в 12.5.
- **(в)** чтобы усилие руки вообще упёрлось в потолок, нужно растяжение `0.2 + 200/400 = 0.7 м`
  (`_cargoHandReach: 0.2`, `_cargoHandStiffness: 400`, `_cargoMaxHandForce: 200` — все три перемерены
  в `Network/NetworkPlaySettings.asset:50-52`). Это 4.4 роста налётчика 0.16 м: потолок на руку, на
  котором держится вся текстура «одному тяжело, вчетвером легко», при этих числах не связывает почти
  никогда.

**Добавлено сверх наряда одним предложением и помечено как незакрытое, а не как решение:** судьба
вылета `_cargoHandReach: 0.2` этой строкой не решалась НИКОГДА — он длина, а не сила, и в список
«числа силы остаются как есть» не входил. Это факт о самой строке, не новое число.

**Что осталось закрытым его словом:** КЛАСС (числа назначенные, игровые) и его разрешение ноге взять
стартовые — **«возьми тогда сам или давай конкретно обсудим в задачах»**. Метка строки поэтому
`answered` (только КЛАСС — его слово) `· → PLAN`, а не `answered` целиком. Новых чисел нога не
придумала ни одного.

### 2. `W114` — порог по числу рук назван вопросом (наряд, `done_when` 2)

Новая строка в разделе C. Арифметика целиком на числах набора, перемерена первой рукой:

| рук | потолок суммы | вес 490.5 Н (`_cargoMass` 50 × 9.81) | что выходит |
|---|---|---|---|
| 1 | 200 Н | не перебивает | волочится — совпадает с его словом |
| 2 | 400 Н | **не перебивает** | **тоже волочится — «два уже могут выше» невыполнимо** |
| 3 | 600 Н | перебивает | поднимают |
| 4 | 800 Н | перебивает | поднимают |

Потолок ставится НА РУКУ и лишь потом суммируется — перемерено в коде: `CargoHaul.HandForce`
кончается `Cap(ref handX, ref handZ, rules.MaxHandForce)`, сложение по рукам идёт в `Reduce:36-49`,
демпфирование вычитается один раз с целого тела. Строка помечена `open → PLAN` с явным возвратом к
владельцу, если чисел под его текстуру не найдётся. **Ответом не заполнена:** ни новых чисел, ни
другого потолка, ни отказа от потолка. Утверждение «критерий 3 куплен даром» стоит в `A4`, то есть в
арх-файле, и понижает его арх-наряд — эта нога туда не писала.

### 3. `W115` — критерий 4 получил строку про число носильщиков (наряд, `done_when` 3)

Новая строка в разделе D. Перемерено этой ногой, что до неё вопрос не решала ни одна строка:
`AuthoritativeCargoRoster.NoiseOf` (`Core/Cargo/AuthoritativeCargoRoster.cs:196-204`) передаёт в
`CargoContactNoise.Of` ровно `contact.Force`, `contact.PointX`, `contact.PointZ` и `rules.Noise` —
списка держащих там нет вовсе.

Строка называет развилку и не выбирает: «даром» (одна рука не поднимает → груз лежит и волочится →
контакт кормит шов; четверо поднимают → контакты кончаются) против «отдельной величины» (громкость
зависит от числа держащих явно — тогда это его число и его ухо). Первое проверить нечем, пока открыт
`W116`: перемерено, что `Network/CargoBodyContact.cs` зовёт `Record` и из `OnCollisionEnter`, и из
`OnCollisionStay`, то есть лежащий груз кормит шов каждым шагом физики — но только если пол вообще
считается тем, обо что груз может удариться. Метка `open → PLAN` с возвратом к `OWNER`.

### 4. `W116` — пол получил строку (наряд, `done_when` 4)

Новая строка в разделе E. Всё перемерено этой ногой первой рукой на `352f96b0`, а не принято от
проверки на слово:

- в `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` есть `Floor` (GameObject `&1231669774`,
  компоненты `!u!4` Transform, `!u!33` MeshFilter, **`!u!64` MeshCollider**, `!u!23` MeshRenderer) и
  `Delivery Yard Floor` (`&368509401`, `!u!4`, `!u!33`, `!u!23`, **`!u!65` BoxCollider**). Пол
  физически существует;
- **ни на одном из двух нет ни одного `MonoBehaviour`**, значит нет и `HouseWall`; во всей сцене
  `HouseWall` встречается один раз (`m_EditorClassIdentifier: Assembly-CSharp::TunnelCrew.World.HouseWall`);
- `Network/CarryStand.cs:66-78` `ReadAuthoredWalls` берёт `_house.GetComponentsInChildren<HouseWall>()`
  и складывает `StandWall` из центра и полуразмеров по X и Z — высоты не сохраняет вовсе. Значит пола
  нет в описании дома ни в смысловой половине, ни в геометрической;
- `Core/Cargo/CargoBodyPort.cs:51` объявляет дословно: «The house is the only physical thing in this
  world the cargo can meet»; то же повторено в `AuthoritativeCargoRoster.cs:185`.

Следствие записано и не решено: как только `A4` включает `useGravity`, это объявление становится
ложным. Метка `open → PLAN`; ближняя половина явной ссылкой отдана наряду
`c-converge-arch-g-5a7c-repair-001` (`A2` — входит ли пол в описание дома; `A5` — становится ли
контакт с полом источником общего слоя).

### 5. `W103` и `C07` перестали говорить разное (наряд, `done_when` 5)

Правлена ТОЛЬКО строка §WHAT, как велел наряд. Она получила верный адресат `review`/`map` и явный
указатель на `C07`, где маршрут стоит целиком. Три источника маршрута названы в самой строке и лежат
вне решающей ноги: `TREE.md` `g-8a41` кр. 3 (фильтр «не меньше чем у двух участников»),
`os/KERNEL.md:63` (форма приземления), `NOW.md:102-107` (выбор адресата по уровню). Проверка сверила
все три дословно и признала маршрут держащимся — поэтому строка законно стала `answered · улика`, а
не осталась открытой. `C07` не тронут ни знаком.

### 6. Пять строк `acceptance` потеряли метку (наряд, `done_when` 6)

Ни одна не заполнена свежим ответом — лекарством было именно понижение:

| строка | было | стало | чем обосновано понижение |
|---|---|---|---|
| `W39` (11 × 8.5, потолок 2.6) | `answered · acceptance` | `answered` (только КЛАСС — его слово) `· → PLAN` | собственный текст: «Числа НЕ требование». Класс «рабочий ориентир» остаётся его словом; величины ушли вниз — заодно закрыта утечка HOW |
| `W54` (сколько реакций) | `answered` (дефолт) `· acceptance` | `open → PLAN` | собственный текст: «не факт, что оно будет… задача вправе взять другое» |
| `W73` (что форсируется) | `answered` (решение ноги) `· acceptance` | `open → PLAN` | собственный текст: «решено этой ногой»; его «либо принимай что-то» даёт право решать, но содержанием строки не является |
| `W94` (честный кадр) | `answered` (решение ноги) `· acceptance` | `open → OWNER` | то же плюс названный отвечающий: кр. 11 закрывается его словом «не стыдно» (`CHARTER.md:26`, →I40), отдельной его реплики про кадр на диске нет (`W93`) |
| `W39a` (голосовой канал) | `answered` (граница ноги) `· acceptance` | `open → PLAN` | границу поставила нога; карточка про голосовой канал молчит и в критериях, и в строке исключений |

Утечка HOW закрыта обоими названными местами: величины `W39` и счёт `W54` под `acceptance` больше не
стоят. Перемерено, что ни одна строка файла не несёт одновременно `acceptance` и `open`: ноль.

Заодно в легенду §WHAT добавлено правило самой метки — `acceptance` стоит только там, где ответ
пришёл из источника ВНЕ ноги, и строка, чей текст говорит «это не требование» или «решено этой
ногой», её носить не может. Это запись механизма, а не новое требование.

### 7. Три неспрошенных предмета спрошены (наряд, `done_when` 7)

По одной строке на каждый, ни одна не заполнена ответом:

- **`W117` — уставная линза 6** («Сохраняет ли оно надёжность работы владельца?»). Перемерено по
  обоим файлам набора: «надёжност», «здоров», «нагрузк», «выгоран» — ноль вхождений. Узел объявлен
  многоволновым его же словом, а `CHARTER.md:208-212` (pre-mortem 8) описывает ровно этот риск и
  ставит мерой сокращение объёма и смену ритма. Отвечающий назван по устройству OS, а не выбран
  ногой: вердикт по каждой линзе даёт `shape` (G6), и он же ставит `appetite`. Метка `open → shape`.
- **`W118` — «материальный результат»**, пятое звено цепи критерия 8. Перемерено: в этом файле слово
  стоит ровно дважды (`W76`, `C24`), оба раза как переписанная строка цепи, и ноль раз в арх-файле; у
  остальных четырёх звеньев определение есть (`G10`, `G09`, `G11`+`A6`, `G12`). Метка `open → PLAN`, с
  названной ценой: если звено окажется вещью модели, а не подписью на экране, правится разбор
  подсистем, а не форма инструмента.
- **`W119` — «ширина проходов согласована с маленьким»** (кр. 1). Перемерено первой рукой:
  `Core/House/HousePassage.cs` сам пишет «Nothing reads it yet»; сегодня `Width` читают ровно три
  места, и ни одно не правило игры — `Editor/SceneHousePlanEditor.cs:126` и `:204` (показ) и
  `World/SceneHousePlan.cs:151` (авторская жалоба при ширине ≤ 0). Метка `open → PLAN`, отвечающий —
  задача, ставящая единую точку масштаба (`W04`).

**Уточнение к тексту наряда, сделанное честно:** наряд пишет «`HousePassage.Width` сегодня не читает
никто». Точнее: ни одно ПРАВИЛО игры его не читает, но три места читают — два редакторских показа и
одна проверка при сборке описания. Вывод наряда от этого не меняется, и в строку записано измеренное,
а не переписанное.

### 8. Счёт §WHAT приведён к измеренному и записан в файле (наряд, `done_when` 8)

Заведён новый раздел `§COUNT` в конце файла — потому что счёта §WHAT в состоянии не стояло вообще, он
жил только в текстах нарядов, и первая проверка поймала там третье число (`113`). Раздел добавлен
ПОСЛЕ §CONTRACTS и его «Счёта этой ноги»; ни одна строка арх-ноги не тронута.

Всё в разделе перемерено по самому файлу:

```
what_rows: 116 → 122          open_rows: 1 → 10          deferred: 2 (W15, W16, не тронуты)
acceptance_rows: 47 → 42      answered_rows: 88          улика: 48       строк с → PLAN: 23
open_owed_by_owner: 1 (W94)   open_owed_by_plan: 8       open_owed_by_shape: 1 (W117)
rows_touched_by_this_leg: 13 из 122; остальные 109 из прежних 116 не тронуты ни знаком
```

Число `113` названо в разделе прямо как не воспроизводимое: в состоянии оно не жило нигде.

---

## state_changes

1. `live/indie-game-development/work/converge-g-5a7c.md` — правки выше: легенда §WHAT (три строки),
   семь переписанных строк (`W07b`, `W39`, `W39a`, `W54`, `W73`, `W94`, `W103`), шесть новых строк
   (`W114`–`W119`), новый раздел `§COUNT` в конце файла. Удалённых строк во всём диффе — только эти
   десять физических строк, каждая заменена своей же новой редакцией.
2. `live/indie-game-development/NOW.md` — `open_calls`: снять `c-converge-g-5a7c-repair-001`
   (исполнен); `c-converge-arch-g-5a7c-repair-001` перевести из `waiting` в `ready`. Добавить
   строку-комментарий о том, что ремонт §WHAT сделан, `open_rows` вырос до 10 и следующая проверка
   будет ВТОРОЙ. Ставка не открывается, задачи и полосы не создаются, `decisions` не трогаются.
3. `live/indie-game-development/LOG.md` — одна строка вверх.
4. `live/indie-game-development/history/2026-08-07-s-converge-g-5a7c-repair-001.md` — этот RESULT.

**Не трогается:** `TREE.md`, `CHARTER.md`, `knowledge/`, `work/converge-g-5a7c-arch.md`, §CONTRACTS
(`C01`–`C26`), §IMPORT, §GLOSSARY, §DRIFT, оба §SIGNOFF, продуктовый репозиторий.

---

## captures

- **CAPTURE (не действовать в этой ноге).** `_cargoHandReach: 0.2` — длина, которую `W07b` не
  переводила, потому что её список назывался «числа силы». Класс шире одной строки: при переводе
  масштаба длины и силы надо разделять явно, иначе «числа силы не трогаем» тихо оставляет в старом
  масштабе и длины. Место для разбора — задача, ставящая единую точку масштаба (`W04`, `C22`).
- **CAPTURE.** Легенда §WHAT теперь несёт правило собственной метки. Если у закрытых наборов
  `g-1d84` и `g-8f31` тот же дефект (проверка предположила это отдельным capture), правило придётся
  писать не в файле узла, а в плее — маршрут `MAINTENANCE`, а не эта нога.

---

## decisions_needed

**Ни одного.** Наряд прямо запретил выносить владельцу новое решение, и запрет исполнен. Единственная
строка, которая ему должна, — `W94` — помечена `open → OWNER`, отвечающий назван, вопрос НЕ
сформулирован и НЕ задан. Строки `W114` и `W115` несут возврат к нему условием, а не вопросом.

---

## play_check

```
mode: repair — разбор g-5a7c не переоткрывался; триаж heavy, §IMPORT и §GLOSSARY не тронуты
imported: без изменений — I01–I54, ни один импорт не переоткрыт и не дописан
fresh_session: PASS — отдельная от решающей, всех трёх арх-ног, проверки и любого day-чата
fan_out: NONE — прямая постоянная инструкция владельца против субагентов и workflow; названо прямо
base_reverified_first: PASS — 352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa, дерево чисто, только чтение,
      перепроверено ДО первой правки
call_done_when: 8/8 — W07b, порог по рукам (W114), носильщики в громкость (W115), пол (W116),
      W103↔C07, пять acceptance-строк, три неспрошенных предмета (W117, W118, W119), счёт §COUNT
converge_coverage: возвращённые клаузы закрыты названной строкой каждая — кр. 1 «ширина проходов» →
      W119 (open → PLAN); кр. 3 порог по рукам → W114 (open → PLAN); кр. 4 число носильщиков →
      W115 (open → PLAN); кр. 3/4 пол → W116 (open → PLAN, ближняя половина у converge-arch);
      кр. 8 «материальный результат» → W118 (open → PLAN); уставная линза 6 → W117 (open → shape).
      Прочие 12/12 клауз покрытия не теряли — проверка это подтвердила и здесь не пересчитывалось
open_rows: 1 → 10 (W39a, W54, W73, W94, W114, W115, W116, W117, W118, W119); W103 закрыта маршрутом
      из трёх источников вне ноги
owner_owed_rows: 1 — W94, вопрос не сформулирован
deferred: 2 — W15, W16, не тронуты ни знаком (перемерено)
acceptance_downgraded: 5 — W39, W54, W73, W94, W39a; ни одна не заполнена свежим ответом
acceptance_open_conflict: 0 строк несут обе метки (перемерено)
how_leak: закрыта — величины W39 и счёт W54 из-под acceptance убраны
no_new_numbers: PASS — ни одного нового числа не назначено; все числа в правках перемерены на
      объявленной базе и принадлежат существующим ассетам
nothing_invented_as_owner_word: PASS — сила девяти токенов владельца не тронута; ни один новый
      токен не приписан
card_untouched: PASS — TREE.md не тронут
arch_untouched: PASS — work/converge-g-5a7c-arch.md и §CONTRACTS (C01–C26) не тронуты
sections_untouched: PASS — §DRIFT, §IMPORT, §GLOSSARY, оба §SIGNOFF без единого знака
no_new_owner_decision: PASS
canon_proposed: без изменений — новых кандидатов эта нога не предлагает
```

---

## log

2026-08-07 | s-converge-g-5a7c-repair-001 | converge | direction | Ремонт §WHAT по шести находкам
первой проверки: все восемь `done_when` закрыты, ни одна дыра не заткнута ответом — `open_rows` 1 → 10.

---

## next

`c-converge-arch-g-5a7c-repair-001` — из `waiting` в `ready`. Его наряд знает про шесть находок
арх-стороны (`C26`, `A4`, пол в `A2`/`A5`, `A9`, одно слово `F7`, `A8`); к ним прибавляются две
вещи, которых он ждать не мог: (1) `reads:` арх-файла в строке 5 несёт `open_rows: 1` — теперь их 10,
и его `done_when` 7 требует свести счета; (2) `W116` явной ссылкой отдала ему ближнюю половину
вопроса про пол, а `W114` и `W115` ссылаются на `A4` и `A5` как на источник ответа.

Затем — свежая `converge-verify`. **Она будет ВТОРОЙ**, и по плею второй FAIL кончает бумажную фазу
узла: возврата не будет, поверхность замораживается уликой, и владельцу выносится ОДНО решение —
резать, делить в `map` или идти на том, что отвечено. Что эта нога оставила проверке для честного
суда: десять открытых строк, каждая с названным отвечающим, и ни одной, закрытой рассуждением ноги.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-g-5a7c-repair-001.md
