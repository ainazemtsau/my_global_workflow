# RESULT — s-work-g-1d84-householder-profile-scope-cut-001

play: work
direction: indie-game-development
node: g-1d84
task: t-host-3
track: хозяин
date: 2026-08-06

**Протокольная оговорка.** Третья нога в одном физическом чате; норма `AGENTS.md` — одна. Нарушено
сознательно: поправка владельца прозвучала здесь, и незаписанная память состоянием не является.
Гейты не обойдены — задача не помечена `done`, G5 не заявляется.

## outcome

Владелец поправил Направление, и поправка верна. Наряд `c-exec-g-1d84-householder-profile-file-002`
сокращён до одного дела — свести и опубликовать. Требование повторной приёмки снято как ошибка
Направления. Задача `t-host-3` остаётся `open` до публикации.

## evidence

**1. Он назвал затык точнее, чем Направление.** Его слова: «если она не влита ни в dev, ни в main, то
напиши, потому что это, ну при закрытии слота она, естественно, должна была влить». Это ровно тот
факт, который прошлая нога измерила (`merge-base --is-ancestor` = NO) и утопила в четырёх других
находках. Настоящий затык один: работа не влита. Всё остальное было следствием.

**2. Ошибка Направления, названная как ошибка.** Прежняя редакция наряда требовала от владельца
проделать ОБА шага приёмки заново на сведённом состоянии. Это гейт на его поведение, запрещённый
прямо: `knowledge/no-checks-the-owner-makes-by-eye.md`. Его вердикт «я протестировал, работает» и
«не хочу ещё раз тестировать» закрывает строки `done_when`, требовавшие его руки. Требование снято,
и снято как дефект ноги, а не как уступка.

**3. Публикация возвращена в наряд.** Прежняя редакция её убирала ради буквы `NOW.bet.evaluator`
(проверка ДО продуктового `main`). Буква держалась на том, что приёмка ещё впереди; приёмки больше
нет, значит держать нечего, и публикация становится единственным делом ноги. Запись
`i-published-to-main-before-any-direction-close-001` этим не закрывается и не смягчается.

**4. Конфликт измерен, чтобы исполнитель не открывал его вслепую.** Трёхстороннее слияние
`Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs`: база `d60c2f04` 225 строк, профиль
301, `main` 267. `git merge-file --diff3` даёт **ровно ОДИН конфликтный блок, пять строк**; обе
стороны только добавляют поля рядом — `_profile`, `_routineStepIndex`, `_routineStepRemainingSeconds`
против `_noisePoint`, `_hasNoisePoint`. Ответ — оставить оба набора. Это не спор решений, а две
независимые пристройки в одно место класса. Наряд требует назвать прямым текстом всё, что вылезет
помимо этих пяти строк.

**5. Принятый риск, записанный без смягчения.** Пару «распорядок из файла + отвлечение на шум» вместе
не запускал никто: владелец гонял профиль на `b1ac3674`, до прихода работы шума. После слияния это
ловят только репозиторные проверки. Живого запуска эта нога не заказывает; вопрос остаётся владельцу
на момент закрытия задачи.

## state_changes

1. `NOW.md` заголовок `updated:` → эта сессия.
2. `NOW.tasks[t-host-3].owner_verdict_2026_08_06`: хвост переписан — осталось одно дело, требование
   повторной приёмки снято его словами, приведён его диагноз затыка дословно.
3. `NOW.open_calls[c-exec-g-1d84-householder-profile-file-002]`: `ready_because` уточнён его новыми
   словами; добавлено `scope_cut_2026_08_06` (сокращение, снятая ошибка, возвращённая публикация,
   измеренный конфликт, принятый риск). Статус остаётся `ready`.
4. `work/c-exec-g-1d84-householder-profile-file-002-call.md`: `goal` → публикация; `done_when` с двух
   пунктов до одного; добавлен раздел с измеренным конфликтом; раздел про два ответа владельца
   переписан под три его фразы; `boundaries` — снят запрет публикации, добавлен «владельца ни о чём
   не просить»; `return` сокращён.
5. `LOG.md` += одна строка; этот RESULT сохранён в `history/`.

## captures

- Класс ошибки, стоивший владельцу раздражения: Направление превратило ЕГО приёмку в гейт и
  предъявило ему пять находок там, где затык был один. Дешёвая форма вперёд — сначала назвать один
  затык, остальное вторым абзацем.

## decisions_needed

Нет.

## play_check

1. **Recite** — цель и `done_when` `t-host-3` те же; ставка `g-1d84`, полоса `хозяин`. ✔
2. **Owner inputs (owner)** — его дословные слова этой ноги: «я протестировал, работает»; «не хочу
   ещё раз тестировать»; «если она не влита ни в dev, ни в main, то напиши, потому что при закрытии
   слота она, естественно, должна была влить». Ими снято требование и возвращена публикация. ✔
3. **Do the work** — наряд сокращён; конфликт измерен первой рукой и положен в наряд числами. ✔
4. **Self-check** — по `done_when` задачи: строки, требовавшие его руки, закрыты его вердиктом;
   остаётся публикация. Задача НЕ помечена `done`. ✔
5. **Close** — корень остаётся `ready` в полосе `хозяин`; `review` не открывается. ✔

close: НЕТ.

## log

t-host-3: владелец поправил Направление — затык один, работа не влита; требование повторной приёмки
снято как гейт на его поведение, публикация возвращена в наряд, конфликт измерен и оказался в пять
строк.

## next

CALL `c-exec-g-1d84-householder-profile-file-002` — `ready`, одно дело: свести и опубликовать.
Участия владельца ноль.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-householder-profile-scope-cut-001.md
