# RESULT — s-work-g-5a7c-thing-1-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-thing-1
track: house-and-things
date: 2026-08-12
close: light — because все три строки `done_when` наряда суть факты, перевыведенные ЭТОЙ ногой из
  закоммиченных байтов и её собственного прогона: равенство деревьев отката, состав коммитов гейта
  расширяемости, отсутствие сцен и чужих файлов в диффе, неизменность правила сведения и каталога
  реакций, 229/229. Ни одна строка наряда не требовала суждения, которого никто не выносил: он
  прямо говорил, что гейт доказывается СОСТАВОМ КОММИТОВ, а не глазом. Свежий чат опровержения не
  открывался. Отдельно: владелец ПРОГНАЛ кандидата сам, и его наблюдение не отменяет ни одной из
  трёх строк, а заводит новую улику.

owner_approved: |
  «закончилась работа по c-exec-g-5a7c-thing-1-001» — 2026-08-12.

  Плюс его собственный прогон, записанный исполнителем в отчёт: он «chose to close this BUILD with
  the observation recorded for Direction/Control rather than expand the three-line CALL into a
  cargo-collision repair».

outcome: |
  **`t-thing-1` ЗАКРЫТА `done` СВЕТЛЫМ ПУТЁМ** на опубликованной голове
  `a7d37b3ecc28151126f950e271e2ec9ddcaed36c` (`origin/main` = `origin/dev`).

  **ГЛАВНЫЙ ГЕЙТ ВЗЯТ СИЛЬНЕЙШЕЙ ИЗ ВОЗМОЖНЫХ ФОРМ.** Наряд требовал, чтобы второй предмет и второй
  способ срабатывания въезжали ДОБАВЛЕНИЕМ, доказанным составом коммитов. Факт:
  - второй предмет — `2f68312a`, **ОДИН файл, `SubstituteCargoThing.cs`, 66 строк, только
    добавление**;
  - второе срабатывание, от ПЕРЕВОРОТА, а не от удара — `7e5041d4`, **ОДИН файл,
    `OverturnedCargoTrigger.cs`, 26 строк, только добавление**.

  Ни один из двух не трогает ядро, решатель, перечисления и первый предмет — для сравнения, сам
  первый предмет стоил девяти файлов и 293 строк. Это ровно тот приём, каким дорожка хозяина
  доказывала расширяемость обработчиков во второй волне.

  **ЛОВУШКА, НАЗВАННАЯ МНОЙ ДО ПОСТРОЙКИ, СРАБОТАЛА РОВНО КАК ПРЕДСКАЗАНО — И ЭТО ХОРОШО.** На тике
  срабатывания присутствуют обе ситуации, удар сводится к `RepeatedHeavyNoise` и **маскирует** звон
  будильника; через один тик удар исчезает и звон слышен как `BoardCreaked` в своей точке. Лечение —
  длительность звона, а не правка сведения: production-длительность `1` секунда лежит настройкой и
  намеренно не прибита тестами. **Правило «самый громкий за тик» и пять реакций не тронуты.** Без
  предупреждения в наряде исполнитель почти наверняка полез бы чинить сведение, которое принадлежит
  чужой дорожке.

  **ГРАНИЦЫ ДЕРЖАЛИСЬ ПОЛНОСТЬЮ:** в диффе `4e5858b4..a7d37b3e` **ноль** файлов `.unity` и **ноль**
  файлов чужих дорожек (`Presentation/`, `Art/`, `Householder.prefab`, `Profiles/`, `Core/House/`).
  Работа целиком в `Core/Cargo/**`, `Network/Cargo*`, `Settings/GameRulesSettings*` и тестах.

  **ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК — ПЯТЫЙ РАЗ ПОДРЯД В ЭТОМ УЗЛЕ.** Более того,
  исполнитель сделал его ДВАЖДЫ: сначала на первом кандидате, потом заново после перебазирования,
  потому что первый откат перестал быть авторитетным. Дерево `c803b8ff` (revert перебазированного)
  = `b15f5ad4…` = дерево базы `4e5858b4`; дерево reapply = дерево работы. **Он сделал ровно то, чего
  не сделала дорожка тела.**

  ## ЧТО НАШЁЛ ПРОГОН ВЛАДЕЛЬЦА — И ЭТО ВАЖНЕЕ САМОЙ ЗАДАЧИ

  Он запустил кандидата в Unity и увидел две вещи:

  1. Хозяин пришёл, но **различить, отреагировал он на будильник или на удар груза об пол, было
     нельзя.** То есть ручной прогон не подтверждает срабатывание «груз о груз», хотя headless
     зелёный.
  2. **ГРУЗ ПРОХОДИТ СКВОЗЬ ГРУЗ.**

  **Причина найдена и перепроверена мной на базисе:** `Network/CargoBody.cs:163,193,195` —
  `SetBodyActivity` для неактивного груза ставит `rigidbody.detectCollisions = false` И
  `body.Collider.enabled = false`. Осевший невзятый груз физически отсутствует. Это лежало на
  базисе и внесено НЕ этой задачей.

  **И это моя ошибка в наряде, названная прямо.** Наряд утверждал: «поиск `IgnoreCollision`,
  `excludeLayers`, `includeLayers`, `gameObject.layer`, `LayerMask` — ноль совпадений, значит тела
  груза сталкиваются обычным образом… удар груза о груз уже сегодня рождает шум». Я искала не тот
  механизм. Утверждение верно для двух АКТИВНЫХ грузов и неверно для покоящегося предмета — а
  покоящийся будильник и есть кейс владельца.

  **Почему это не мелочь:** центральная кража его документа демо — «карандаш задел будильник,
  будильник зазвонил» — сегодня в игре не воспроизводится. Заведена
  `i-cargo-passes-through-cargo-in-the-running-game-001`.

  **ПОЧЕМУ ЗАДАЧА ВСЁ РАВНО ЗАКРЫВАЕТСЯ `done`, а не `partial`.** Три строки наряда — состояние у
  предмета, событие от смены состояния, расширяемость составом коммитов, неизменность соседей и
  откат — выполнены полностью и перевыведены здесь. Сквозной проход пришёл с базиса, а не отсюда, и
  чинить его внутри трёхстрочного наряда означало бы ровно то расширение объёма, которое контракт
  запрещает и от которого владелец сам отказался. Дефект не спрятан: он живёт уликой на критическом
  пути, а не строкой «почти сделано».

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ.**

  - `git ls-remote origin` → `main` = `dev` = `a7d37b3e`.
  - Двенадцать коммитов сверх `4e5858b4`; лестница отката читается: работа `5d35df11`, доказательства
    `2f68312a` / `7e5041d4` / `f3f5653d`, две поправки `1cf939b9` / `be6a3f1f`, затем
    `6b1bb376` revert → `88859f91` reapply, затем перебазирование и снова `c803b8ff` revert →
    `ffe6e073` reapply.
  - Деревья: `4e5858b4` → `b15f5ad4…`; revert `c803b8ff` → `b15f5ad4…` (**совпало**); работа
    `88859f91` и reapply `ffe6e073` → `00d7c19c…` (**совпали**).
  - `git show --stat 2f68312a` → 1 файл, +66. `git show --stat 7e5041d4` → 1 файл, +26.
    `git show --stat 5d35df11` → 9 файлов, +293.
  - `.unity` в диффе → **0**. Файлов чужих дорожек → **0**.
  - `dotnet test … -c Release`, прогнано ЗДЕСЬ на выгруженном дереве публикации → **229 passed,
    0 failed, 0 skipped** против **219** после шва.
  - `4e5858b4:Assets/TunnelCrew/Network/CargoBody.cs` → строки `193` `rigidbody.detectCollisions =
    false` и `195` `body.Collider.enabled = false` внутри `SetBodyActivity` — **на базисе**.
  - Отчёт `docs/results/c-exec-g-5a7c-thing-1-001.md` прочитан, включая §«Loudest-per-tick trap» с
    числами фикстуры и §«Owner runtime observation — follow-up required».

state_changes: |
  1. `t-thing-1`: `active → done`, журнальная строка, перенесена в `cards/closed/`.
  2. `c-exec-g-5a7c-thing-1-001`: `ready → done`, журнальная строка, перенесён в `cards/closed/`.
  3. Создана `i-cargo-passes-through-cargo-in-the-running-game-001` — `route: work`, с найденной
     причиной на базисе, с названной ошибкой моего наряда и с триггером «не раньше публикации
     `t-rooms-1`, потому что решение про мебель и груз принимается один раз».
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-thing-1-close-001.md`.

captures:
  - |
    ИСКАЛА НЕ ТОТ МЕХАНИЗМ. Вывод «тела сталкиваются» был сделан из отсутствия `LayerMask` и
    `IgnoreCollision`, а выключение оказалось через `detectCollisions` и `Collider.enabled`.
    Отрицательный поиск по списку имён — слабая улика: он доказывает отсутствие ИМЕНОВАННОГО
    механизма, а не наличие поведения. Проверять поведение, а не отсутствие ключевых слов.
  - |
    ДВА ИСПОЛНИТЕЛЯ В ОДИН ДЕНЬ ПОЛУЧИЛИ ОДИНАКОВОЕ ПРАВИЛО ПРО ПЕРЕБАЗИРОВАНИЕ И ПОСТУПИЛИ
    ПО-РАЗНОМУ: `t-thing-1` перебазировался и переисполнил откат, `t-body-1` остался на протухшей
    базе. Разница не в правиле, а в том, что один сверил голову перед публикацией, а другой только
    перед сборкой. Формулировку наряда стоит поправить на «сверить голову ПЕРЕД публикацией».
  - |
    Предупреждение о ловушке «самый громкий за тик» окупилось: исполнитель не полез чинить сведение,
    а поставил длительность и доказал поведение фикстурой. Приём — называть чужие ограничения ДО
    постройки — стоит повторять.

decisions_needed: []

play_check:
  - 1 recite — done. Семь строк карточки и три строки наряда пересказаны.
  - 2 owner inputs — не требуется: единственное, что принадлежало ему (нужно ли выключать будильник), было сказано заранее и исполнено конечной длительностью.
  - 3 do the work — приёмка возврата.
  - 4 self-check — done. Каждая строка перевыведена здесь из байтов и прогона; наблюдение владельца проверено отдельно и превращено в улику, а не в снижение вердикта.
  - 5 close — done, светлым путём; основание записано в поле `close` по тесту плея.
  - G5 — выполнен светлым закрытием; свежий чат не требовался, потому что гейт наряда по построению доказывается составом коммитов.
  - G7 — done. Решение про то, какие предметы обязаны сталкиваться в покое, названо игровым и оставлено владельцу в улике.
  - G10 — done. RESULT финальный.

log: t-thing-1 закрыта СВЕТЛЫМ путём на a7d37b3e, и главный гейт взят сильнейшей формой — второй предмет это ОДИН добавленный файл на 66 строк, второе срабатывание от переворота ОДИН файл на 26 строк, и ни один не трогает ядро, решатель, перечисления и первый предмет против девяти файлов и 293 строк у самого первого предмета; откат исполнен ДВАЖДЫ, второй раз после перебазирования, и сошёлся знак в знак пятый раз подряд — то есть эта дорожка сделала ровно то, чего не сделала дорожка тела; 229/229 прогнаны здесь против 219, ноль сцен и ноль чужих файлов; ловушка «самый громкий за тик» сработала ровно как предсказано нарядом и вылечена длительностью звона в одну секунду, а не правкой сведения, которое принадлежит чужой дорожке; но прогон ВЛАДЕЛЬЦА нашёл главное — груз проходит сквозь груз, поэтому центральная кража его документа демо в игре не воспроизводится, причина найдена на базисе в CargoBody.SetBodyActivity, где неактивному грузу выключают detectCollisions и коллайдер, и там же названа моя ошибка: наряд утверждал обратное, потому что искал слои вместо detectCollisions; задача всё равно закрыта done, а не partial, потому что все три строки выполнены, дефект пришёл с базиса и заведён уликой на критическом пути

next: |
  Улика `i-cargo-passes-through-cargo-in-the-running-game-001` лежит на критическом пути демо и
  ждёт первой задачи, которой законно трогать физику груза — не раньше публикации `t-rooms-1`,
  потому что мебель тоже станет физическим телом и решение принимается один раз.

  Слот WIN-U3 отпущен: `CLEAN / AVAILABLE / lease none`, дерево уже на `a7d37b3e`.

  Идут: `t-rooms-1` в WIN-U2, тело ждёт добивки `c-exec-g-5a7c-body-1-002` в WIN-U1.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-thing-1-close-001.md
