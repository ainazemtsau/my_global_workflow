# RESULT — s-work-g-5a7c-loot-mass-and-collider-008

direction: indie-game-development
play: work
node/task: g-5a7c / t-loot-1
track: house-and-things
date: 2026-08-13
outcome_kind: owner-word — вес переехал на вещь его словом; наряд закрыт по всем поправкам

owner_approved: |
  **ВЫБОР ПО ВЕСУ, дословно: «А»** — на показанные два варианта с ценой каждого. Формулировка, которую
  он принимал: «вес пишу прямо на вещи, заготовки нужны только чтобы копировать» против «вес пишу в
  общей карточке, а чтобы дать вещи свой вес, завожу ей отдельную карточку». Названная цена варианта А
  принята вместе с ним: правка числа в заготовке больше не меняет уже созданные предметы.

  **ЕГО ВОПРОСЫ, вскрывшие обе находки этой ноги:**
  «я не понимаю, почему у меня коллайдер для каждой модели не настраивается самому… я ж могу выставить
  вот как надо, там несколько коллайдеров, там какой-то сложный, кругом, цилиндром… почему именно
  коробка, нахуй?»
  «откуда нахуй цифры такие взяли? Ты что, дом подымать будешь?.. я вот создаю будильник, и шо мне
  ставить будильник, у меня весит там 40 килограмм или что?»
  «я не хочу, чтобы у меня только три вида веса было. Я хочу… поставить там 180 килограмм. Для
  какой-то 12 килограмм.»

outcome: |
  **ДВЕ ДЫРЫ ЗА ВЕЧЕР НАШЁЛ ОН, А НЕ ПРОВЕРКА. Это стоит записать прямо, потому что обе были в
  выпущенном наряде и обе доехали бы до сборки.**

  **1. НЕСКОЛЬКО КОЛЛАЙДЕРОВ ЛОМАЮТ КУПЛЕННОЕ ИМ ЖЕ.** Перемерено Направлением лично:
  `Network/CargoBody.cs:113` берёт `GetComponent<Collider>()` — ЕДИНСТВЕННОЕ число, — а строки
  `181, 217, 224` включают/выключают именно его. На префабе из нескольких коллайдеров остальные
  останутся включёнными всегда → предмет наполовину проходим. Это ломает ровно его «лут не должен
  через друг друга проходить». Починка вписана в наряд.

  **Попутно разделены два столкновения, о которых я ему не сказала и от этого весь его вопрос:**
  физическое — Unity, ЛЮБАЯ форма, код берёт что положили на префаб; судейское — ядро
  (`Core/Cargo/CargoGeometry.cs:190-206`, три `IntersectSlab`) знает ТОЛЬКО коробку, и это четыре
  ответа: луч подбора, блокировка ходока, на что встать, где держат руки. Причина коробки —
  детерминизм: этот код обязан дать одинаковый ответ на всех машинах.

  **2. «КГ» — НАША ВЫДУМКА.** `git grep -ci "kg|килограмм"` по `Assets/TunnelCrew` → **ноль**.
  Килограммы приписали мы в своих документах и в наряде; он возмутился нашей подписью, а не игрой.
  Цепочка числа `48` из сообщений коммитов: ящик `4` → мир сжат в 12.5 раз, масса поднята `4 → 50`
  **чтобы не перенастраивать пороги шума** → ускорили переноску, `50 → 20` по той же причине → ввели
  силу руки `200`, поставили `48` без объяснения; `100` и `240` подобраны под лестницу `1 / 2 / 4`
  руки. **Число никогда не было массой предмета — это ручка «сколько рук нужно».**

  **ЧТО ИЗ ЭТОГО СЛЕДУЕТ И ЧЕГО НИКТО НЕ ЗАМЕЧАЛ:** вес решает ещё и ГРОМКОСТЬ. Пороги
  `_cargoRustleForce 60`, `_cargoKnockForce 150`, `_cargoHeavyNoiseForce 400` — это СИЛЫ, перемерено.
  По замеру проверки всё тяжелее ≈`18.5` при обычной переноске всегда попадает в самый громкий ранг,
  то есть **все три нынешних класса всегда орут на максимум, а средние полосы шума построены,
  оплачены и ни разу не использованы.** Будильнику рекомендовано `15` — первое число в проекте,
  попадающее в средний ранг и выше границы дрожи.

  **ВЕС ПЕРЕЕХАЛ НА ВЕЩЬ ЕГО СЛОВОМ, и это отменяет часть его же handoff — его собственным словом.**
  Отменено поимённо: §2 «единый источник физики» в части массы; «отдельная масса одного типа означает
  отдельный профиль»; binding-решение 2 наряда; §9 в части «общий профиль влияет на все использующие
  его definitions». **Осталось в силе:** запрет массы в РАСКЛАДКЕ — то есть довод handoff про «разные
  комнаты» исполнен полностью, — и профиль как носитель формы тела.

  **СЛЕДСТВИЕ, которое иначе стало бы невыполнимой строкой:** идентичностей теперь ДВЕ, а не три.
  Строка 2 `done_when` требовала независимости трёх; третья (физический профиль) в игре после этой
  правки не существует. Подгонять под прежнюю редакцию запрещено, декоративный номер профиля ради
  строки приёмки запрещён отдельно.

  **НАРЯД ЗАКРЫТ ПО ВСЕМ ПЯТНАДЦАТИ ПОПРАВКАМ И РАЗБЛОКИРОВАН.**

evidence: |
  **ПЕРЕМЕРЕНО МНОЙ ЛИЧНО на `origin/main` = `c485b30e`:**

  - `Network/CargoBody.cs:97,113,181,217,224` — `AddComponent<Rigidbody>()`, затем
    `GetComponent<Collider>()` в единственном числе, и переключение именно его.
  - `Core/Cargo/CargoGeometry.cs:190-206` — `HalfLength/HalfHeight/HalfWidth`, три `IntersectSlab`.
    Коробка и только коробка.
  - `git grep -ci "kg\|килограмм" -- Assets/TunnelCrew` → **пусто**.
  - `Settings/GameRulesSettings.asset` — `_cargoGravity: 3`, массы `48/100/240`,
    `_cargoLiftPower: 200`, `_cargoHandReach: 0.016`, `_cargoRustleForce: 60`,
    `_cargoKnockForce: 150`, `_cargoHeavyNoiseForce: 400`. Арифметика лестницы:
    `48×3=144`, `100×3=300`, `240×3=720` против `200 / 400 / 800`.

  **ИЗ ПРОВЕРКИ, не перепроверено мной поштучно:** граница ≈`18.5` для самого громкого ранга; границы
  подъёма `12 / 200 / 266`; происхождение чисел по сообщениям коммитов; «закона числа носильщиков в
  игре нет вовсе». Материалы — `subagents/workflows/wf_843dfea6-aba/journal.jsonl`.

state_changes: |
  1. `work/c-exec-g-5a7c-loot-foundation-001-call.md`: добавлены поправки 11–16 — форма коллайдера и
     починка нескольких коллайдеров; запрет слова «кг»; инспектор показывает И носильщиков И ранг
     шума; блокирующая про отсутствующий закон носильщиков с границами `12/200/266`; любой вес
     разрешён; и поправка 16 — вес на вещи, с поимённым списком отменяемых строк handoff и следствием
     «идентичностей две, а не три».
  2. `c-exec-g-5a7c-loot-foundation-001`: две журнальные строки. Карточка `ready`, **разблокирована**.
  3. Полный RESULT сохранён в `history/2026-08-13-s-work-g-5a7c-loot-mass-and-collider-008.md`.

  Ставка и задачи не тронуты. Его handoff как документ не переписан — отменённые строки названы
  здесь и в наряде, сам файл остаётся исторической записью того, что он присылал.

captures:
  - |
    **ЧИСЛО, ПОДОБРАННОЕ РАДИ ТОГО, ЧТОБЫ НЕ ТРОГАТЬ СОСЕДНЮЮ НАСТРОЙКУ, ДВАЖДЫ ПЕРЕЖИЛО СВОЮ
    ПРИЧИНУ.** Массу поднимали дважды со словами «чтобы не перенастраивать пороги шума» — и в
    результате пороги шума стали недостижимы снизу: всё тяжелее ≈18.5 всегда максимально громкое.
    То есть настройку сохраняли ценой того, ради чего она существует. Общая форма: **число, названное
    производным от другого числа, обязано нести в карточке имя того числа** — иначе оно переживает
    свою причину и его происхождение приходится раскапывать по коммитам.
  - |
    **ДВЕ НАХОДКИ ВЕЧЕРА — ЕГО, И ОБЕ ИЗ ВОПРОСА «А ПОЧЕМУ ТАК?».** Он не проверял код; он отказался
    принять объяснение, которое не сходилось с его опытом («почему именно коробка», «будильник 40
    килограмм?»). Практический вывод для меня: **его «я не понимаю» — это не пробел в его знаниях, а
    сигнал, что объяснение неверно или неполно.** Сегодня оба раза за ним стоял настоящий дефект.

decisions_needed: []

play_check:
  - 1 recite — done. Открытый выбор по весу и два его вопроса пересказаны.
  - 2 owner inputs — done. Выбор «А» получен дословно, цена варианта названа ДО выбора.
  - 3 do the work — шесть поправок в наряд; отменяемые строки handoff перечислены поимённо.
  - 4 self-check — done. Обе находки перемерены мной лично по байтам; заимствованное из проверки помечено как неперепроверенное.
  - 5 close — owner-word. Наряд разблокирован, задача остаётся active.
  - G7 — done. Выбор формы хранения веса сделан ИМ, потому что меняет то, как он авторит; инженерные части решены ногой с записанным основанием.
  - G9 — done. Строки его handoff отменены только его же словом и названы поимённо.
  - G10 — done. RESULT финальный.

log: две дыры за вечер нашёл ОН, а не проверка, и обе доехали бы до сборки — спросив «почему именно коробка, я же могу цилиндр», он вскрыл, что CargoBody берёт GetComponent<Collider> в единственном числе и переключает только его, значит префаб из нескольких коллайдеров даст наполовину проходимый предмет и сломает купленное им «лут не должен через друг друга проходить»; спросив «откуда цифры, будильник у меня 40 килограмм?», он вскрыл, что слова kg в продукте нет ни одного и килограммы приписали МЫ, а 48 никогда не было массой — цепочка из коммитов даёт ящик 4, подъём до 50 при сжатии мира ЧТОБЫ НЕ ПЕРЕНАСТРАИВАТЬ ПОРОГИ ШУМА, пересчёт 50→20 по той же причине и 48 после ввода силы руки без объяснения, а 100 и 240 подобраны под лестницу рук; попутно найдено, что настройку шума сохраняли ценой того, ради чего она существует — пороги 60/150/400 это силы, и всё тяжелее ≈18.5 всегда в самом громком ранге, то есть средние полосы построены и ни разу не использованы, поэтому будильнику рекомендовано 15; ВЕС ПЕРЕЕХАЛ НА САМУ ВЕЩЬ его словом «А» с названной ему заранее ценой, и это отменяет часть его же handoff ЕГО ЖЕ СЛОВОМ — поимённо §2 в части массы, «отдельная масса = отдельный профиль», binding-решение 2 и §9; в силе остались запрет массы в раскладке, которым довод про «разные комнаты» исполнен полностью, и профиль как носитель формы; следствием строка 2 done_when читается как ДВЕ идентичности вместо трёх, потому что третья в игре больше не существует, а декоративный номер профиля ради строки приёмки запрещён отдельно; наряд закрыт по всем пятнадцати поправкам и разблокирован

next: |
  Наряд на лут готов к запуску. `c-work-g-5a7c-sight-1-binding-g5-001` тоже.

END_OF_FILE: live/indie-game-development/history/2026-08-13-s-work-g-5a7c-loot-mass-and-collider-008.md
