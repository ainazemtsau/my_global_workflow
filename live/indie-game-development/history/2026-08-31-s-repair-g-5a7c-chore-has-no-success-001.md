# RESULT — s-repair-g-5a7c-chore-has-no-success-001

direction: indie-game-development
play: repair
node/task: g-5a7c / t-carry-back-1 и улика про дело без успеха
track: руки
date: 2026-08-31
outcome_kind: repair — рекомендация направления опровергнута его же вопросом: переноска ничего не лечит, а дело про чай не имеет ни одного способа завершиться успехом

owner_approved: |
  Повод — его вопрос дословно: **«а почему он вообще стоит без зрения? Типа зачем оно ему, чтобы
  ходить, выполнять свои какие-то обязанности… У меня просто складывается впечатление, что он
  стартует и стоит сразу.»**

  Его модель, названная им же: **«у него в памяти все предметы есть, он изначально знает, что дома
  всё расположено; он может прийти и не увидеть на месте, и начинает потом искать.»**

  Наряд на переноску **отозван в черновик до его слова** — рекомендация, на которой он стоял,
  оказалась неверной. Ни одна строка `done_when` не изменена.

outcome: |
  **ЕГО МОДЕЛЬ РЕАЛИЗОВАНА В ТОЧНОСТИ, И ЭТО НАДО БЫЛО ПРОВЕРИТЬ РАНЬШЕ, ЧЕМ ОБЪЯСНЯТЬ.**
  `HouseholderThingBeliefs.Of(arrangement)` описан в коде дословно: «**A man who knows how his house
  is arranged and has not yet looked at any of it.**» `TryWhereHeThinksItIs` сначала смотрит, видел
  ли он вещь, и **если не видел — падает на расстановку дома**, то есть на то, где вещь ЖИВЁТ.
  Ложь возвращается только если никто не сказал, где она живёт, И он её никогда не видел.

  И поиск при пропаже тоже построен: увиденное «не там, где должно быть» даёт убеждение `Lost`,
  строка перестаёт предлагать, и **выигрывает строка про запасную кружку** — комментарий в
  обработчике называет это подстановкой прямо.

  **ЗНАЧИТ ОТВЕТ НА ЕГО ВОПРОС: ОН НЕ СТОИТ БЕЗ ДЕЛА. ОН СТОИТ В ДЕЛЕ, У КОТОРОГО НЕТ КОНЦА.**
  Он верит, что кружка на обеденном столе, идёт туда, **приходит** — и `who` печатает
  `WalkToward toward SituationPoint "Mug" at (2.70, 2.50)`, стоя ровно в этой точке.

  **НАСТОЯЩИЙ ДЕФЕКТ НАЙДЕН, И ОН НЕ ТАМ, КУДА УКАЗЫВАЛО НАПРАВЛЕНИЕ.** Все три чайных дела несут
  один набор завершений — «кружка в мастерской», «запасная в мастерской», «марка сдался». **Успеха
  среди них нет ни одного.** У `chore-1a` нет ни `when`, ни `effects` вовсе.

  **И ЕДИНСТВЕННЫЙ ПУТЬ К МАРКЕ «СДАЛСЯ» ЗАКРЫЛА ПОЧИНКА ЗРЕНИЯ.** Марку ставит только `chore-1c`,
  а его `when` требует обе кружки `missing`. Пока хозяин был слеп — они считались потерянными.
  Прозрел — выход исчез.

  **ПЕРЕНОСКА ЭТО НЕ ЛЕЧИТ, И РЕКОМЕНДАЦИЯ НАПРАВЛЕНИЯ БЫЛА НЕВЕРНА.** Единственное «носительное»
  завершение — отнести кружку **в мастерскую**, то есть туда, куда её таскают мыши. Хозяин,
  несущий свою кружку в мастерскую, чтобы отказаться от чая, — абсурд, а не поведение.
  **Наряд отозван в черновик, не отправлен.** Его спас только тот факт, что владелец не успел его
  переслать.

  **МЕХАНИЗМ ДЛЯ НАСТОЯЩЕЙ ПОЧИНКИ ПОСТРОЕН СЕГОДНЯ УТРОМ.** Хук прибытия
  (`Householder.cs:1045`, `EffectTiming.Arrival`) применяет эффекты в момент, когда он ДОШЁЛ.
  Успешное завершение дела выражается им напрямую и данными, а не кодом.

evidence: |
  Перемерено направлением на `main` = `dev` = `origin/main` = **`064ab568`**:

  - `HouseholderThingBeliefs.cs:198` — комментарий «A man who knows how his house is arranged and
    has not yet looked at any of it»; `:199-206` — `Of(arrangement)`;
  - `:218-241` — `TryWhereHeThinksItIs`: сначала `_sightings`, затем падение на
    `_arrangement.TryFind(...)` и `arranged.Whereabouts`;
  - `HouseholderChoreDecisionHandler.cs:~283` — комментарий «WHERE HE THINKS IT IS, never where it
    actually is… it is exactly this method returning false that makes substitution work»;
  - профиль, `chore-1a-tea-with-the-kitchen-mug` целиком: `closedBy` из трёх условий, все три —
    сдача; полей `when` и `effects` нет вовсе;
  - профиль, `chore-1c-tea-is-not-happening-today`: `when: [{Mug is missing}, {Spare mug is
    missing}]`, `effects: [{mark gave-up-on-tea of him set 0}]` — **единственное место во всём
    файле, где эта марка ставится**;
  - `chore-2-the-clock-belongs-in-the-kitchen`: `closedBy: [{Alarm clock believedIn room:Kitchen}]`
    — у этого дела завершение осмысленное, но тоже требует переноски.

  **НЕ ВЫЯСНЕНО И НЕ УТВЕРЖДАЕТСЯ:** совместимы ли `set: 0` в эффекте и `present: true` в условии.
  Смотреть надо в исполнение марок, а не в загрузчик. Если несовместимы — второй независимый тупик.

state_changes: |
  СОЗДАНО (1 улика):
  - `i-the-tea-chore-has-no-way-to-succeed-only-to-be-abandoned-001` — у дела про чай нет ни одного
    успешного завершения; путь к марке «сдался» закрыт починкой зрения; переноска не лечит;
    механизм починки (хук прибытия) уже построен. Маршрут `work`.

  ОТОЗВАНО (1 наряд):
  - `c-exec-g-5a7c-carry-back-1-001` → `draft`. Наряд написан на неверной посылке, что переноска
    размыкает круг. Не отправлен, вреда не нанёс.

  ВОЗВРАЩЕНО (1 задача):
  - `t-carry-back-1` → `open`. Задача остаётся законной и полезной, но **критическим путём не
    является**.

captures: |
  - **ВОПРОС ВЛАДЕЛЬЦА ОКАЗАЛСЯ СИЛЬНЕЕ ТРЁХ ОТЧЁТОВ ИСПОЛНИТЕЛЕЙ.** Он спросил «а почему он вообще
    стоит?» — и это единственный вопрос, который никто за день не задал. Три ноги и направление
    разбирали, ПОЧЕМУ не срабатывает механизм, и ни одна не спросила, **есть ли у дела конец**.
    **Правило-кандидат: у зависшего поведения первым делом спрашивать не «что мешает», а
    «а как оно вообще должно кончиться».**
  - **НАПРАВЛЕНИЕ ВЫДАЛО НАРЯД НА ГИПОТЕЗЕ, НЕ ПРОВЕРИВ ЕЁ ДО КОНЦА.** Оно прочитало `closedBy` и
    увидело «нужно отнести» — но не спросило, КУДА. Ответ «в мастерскую» опровергает всю
    рекомендацию одним словом. **Наряд спасло только то, что владелец не успел его переслать.**
  - **ПОЧИНКА ВОСПРИЯТИЯ ЗАКРЫЛА ЗАПАСНОЙ ВЫХОД ВТОРОЙ РАЗ ЗА ДЕНЬ.** Сначала это назвали как
    наблюдение, теперь оно стало причиной тупика. Правило из прошлой ноги подтверждено:
    **у починки восприятия спрашивать, какое условие держалось на том, что он НЕ видел.**

decisions_needed: |
  **ОДИН, И ОН ЕГО.** Что считать успешным завершением дела про чай — содержание. Возможно, эти
  дела вообще устарели: они писались, когда у хозяина не было утра, а теперь утро есть и в нём
  своя строка про кофе.

play_check: |
  1. Назвать противоречие — его вопрос «почему он стоит», на который у направления не было ответа.
  2. Восстановить новейшим-первым — модель убеждений, подстановка при пропаже и все три чайных дела
     прочитаны в байтах; собственная рекомендация проверена и опровергнута.
  3. Предложить исправленное состояние — улика заведена, наряд отозван в черновик, задача возвращена
     в `open`.
  4. Подтверждение (owner) — требуется на содержание: что такое успех для этого дела.
  5. Дыра в OS → нет; два кандидата в правила записаны.

log: |
  вопрос владельца «почему он вообще стоит» оказался сильнее трёх отчётов: его модель убеждений
  реализована в точности, а дело про чай не имеет НИ ОДНОГО успешного завершения — только три
  способа сдаться, и единственный путь к марке сдачи закрыла починка зрения; переноска этого не
  лечит, и наряд на неё отозван до отправки

next: |
  Идёт одна нога: подготовка полигона в WIN-U1. Свободны WIN-U2, WIN-U3, WIN-U4.

  Волна упирается в содержание: чем кончается дело про чай. Механизм для ответа построен сегодня
  утром — хук прибытия.
