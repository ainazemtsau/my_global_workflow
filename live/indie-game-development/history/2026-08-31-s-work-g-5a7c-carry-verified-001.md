# RESULT — s-work-g-5a7c-carry-verified-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-carry-back-1, полоса `руки`
track: руки
date: 2026-08-31
outcome_kind: work — руки построены и порог смерти волны выдержан: механизм остался один; публикация разрешена, задача ждёт глаза владельца

owner_approved: |
  Задача входит в девятую волну, открытую его словом 2026-08-30, и переизданный наряд отвечал на
  его же наблюдение с полигона: **«он пока ничего не берёт, не относит»**.

  Строка приёмки 6 — его глаз — **не закрывается этой ногой и не может быть закрыта никем другим.**

  Ни одна строка `done_when` не изменена.

outcome: |
  **ПОРОГ СМЕРТИ ВОЛНЫ ВЫДЕРЖАН, И ЭТО ГЛАВНОЕ.** Наряд грозил откатом, если появится второй
  механизм, ведущий ноги. Его не появилось, и решение объяснено в коде дословно:

  > «**THIS IS STILL ONE MECHANISM AND NOT TWO**… Nothing new steers him: the same row that owned
  > his legs on the way to the thing owns them on the way back, and the only difference is the one
  > place it names. Where the thing is going is **CARRIED IN**, resolved once out in the world by his
  > own deed, and **never worked out again in here** — deriving it a second time… would be a second
  > answer to “where is this going”, and he would walk to one spot while the thing was set down at
  > another.»

  **ДИАГНОЗ НОГИ ОБЪЯСНИЛ СУЩЕСТВОВАНИЕ РАСТЯЖКИ ЗАДНИМ ЧИСЛОМ, И ЭТО ЛУЧШЕЕ В ВОЗВРАТЕ.** Строка
  дела посылала хозяина туда, где он ВЕРИТ, что вещь лежит. **Вещь в руке — это вещь, которую он
  видит**, значит верит, что она там, где он стоит. Строка посылала его к самому себе; он приходил
  в тот же тик и стоял до конца сессии. Отсюда и запрет: «that was never a bug in this method; it
  was the honest answer to the wrong question, and it is why the shipped file used to be FORBIDDEN
  a chore that picks anything up».

  **РАСТЯЖКА ЗАМЕНЕНА, А НЕ УДАЛЕНА — ПРОВЕРЕНО СЧЁТОМ.** `HouseholderChoreTests.cs`:
  **20 тестовых методов до и 20 после**. Ушла `NoChoreInTheShippedFileCarriesADeedItCannotFinish`,
  пришла `TheShippedChoreWalksTheThingItPickedUpToWhereItIsGoing`, и новая гоняет настоящие тики
  через закоммиченный профиль в обе стороны, **проверяя имя обработчика на совпадение** — то есть
  сама сторожит «один механизм, а не два».

  **ВЫСОТА ВЗЯТА ОДНИМ МЕСТОМ ДЛЯ ОБЕИХ ВЕТОК.** Вспомогательный `Towards(...)` написан однажды и
  спрашивается и туда, и обратно, «because a height picked separately at two sites is two heights
  the day somebody changes one». Это прямой ответ уроку сегодняшнего дня про зрение, где высота
  бралась не оттуда.

  **ТРИ НАХОДКИ НАЗВАНЫ И НЕ ПОЧИНЕНЫ, И ВСЕ ТРИ ЧЕСТНЫЕ.**
  - `chore-1a` (чай) затеняет остальные дела **всегда**: ничья решается алфавитом, а чайное дело
    закрывается только кружкой в мастерской. Живой прогон без вмешательства — 3999 тиков, часы ни
    разу не подняты. **Живой тест уносит обе кружки сам**, и нога прямо называет это единственным
    способом дать очередь второй строке, а не подпоркой механизма;
  - красный PlayMode-тест `HePicksItUpInOneRoomAndPutsItDownInAnother` — **старый**. Нога
    доказала прямым замером: вернула продуктовые файлы на `064ab568` и получила ту же ошибку и те
    же числа. Направление подтвердило: тест существует на `main` до этой ноги;
  - поступок стреляет ещё раз при завершении отклика — лишний подъём-и-кладка; правка была бы
    девятым местом, за порогом.

evidence: |
  **ПЕРЕВЫВЕРЕНО НАПРАВЛЕНИЕМ** на ветке `slot/win-u1`, дерево `293e4d94`:

  - **прогон направления:** `dotnet test tests/TunnelCrew.Core.Tests -c Release` →
    **не пройдено 0, пройдено 739, всего 739.** Столько же, сколько на `main`;
  - `HouseholderChoreTests.cs` — **20 методов `[Test]` на `main` и 20 на ветке**;
    старое имя на ветке отсутствует, новое присутствует;
  - `HouseholderChoreDecisionHandler.cs` — единственная правка поведения: при
    `snapshot.Hands.IsCarrying(_thingName)` цель берётся из `snapshot.Hands.TakingItTo`,
    иначе из `belief.Where`; обе ветки идут через один `Towards(...)`;
  - `HeCarriesAThingAcrossTheHouseTests.cs:104` — красный PlayMode-тест **есть на `main`**,
    то есть предшествует ноге;
  - правка профиля минимальна: пять строк, `deed` у реакции `go-to-the-clock` —
    `put-in-place / Alarm clock / Kitchen counter`. Без неё нести нечего;
  - диф ноги: восемь файлов продукта плюс профиль, два файла тестов и отчёт. **Ничего вне
    объявленной поверхности.**

  Из её возврата, как улика: `tick 1040` — вещь у руки, `The householder put 'Alarm clock' down on
  'Kitchen counter'`, `tick 1545` — лежит на месте 30 тиков; **442 из 1545 тиков с вещью в руке,
  7.76 м пути**. Свежесть сборки доказана наличием `HouseholderHands.IsCarrying(String)` в
  загруженной сборке.

state_changes: |
  РАЗРЕШЕНО (1 действие исполнителю):
  - **публикация штатным `close-leg`.** Тесты зелёные, поверхность выдержана, порог смерти волны
    не сработал.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-carry-back-1` остаётся `active`: **строка 6 — глаз владельца**, и публикация её не
    закрывает. Задача закроется его словом, как закрылась задача полигона.

captures: |
  - **ЗАПРЕТ ОБЪЯСНИЛСЯ ЧЕРЕЗ ПОЛГОДА ПОСЛЕ ТОГО, КАК БЫЛ ПОСТАВЛЕН.** Растяжка запрещала делу
    нести поступок, и никто не знал почему — знали только, что иначе хозяин замирает. Нога назвала
    причину: строка посылала его туда, где он верит, что вещь, а вещь в руке он видит, значит верит,
    что она под ним. **Правило-кандидат: у запрета, чью причину никто не помнит, причина всё-таки
    есть — искать её до того, как снимать.**
  - **ЧЕТВЁРТЫЙ СЛУЧАЙ ЗА ДЕНЬ, КОГДА ИСПОЛНИТЕЛЬ ДОКАЗАЛ ЧУЖУЮ ПОЛОМКУ ЗАМЕРОМ, А НЕ СЛОВОМ.**
    Красный PlayMode-тест он не объявил чужим — он вернул файлы на базу и получил ту же ошибку с
    теми же числами. Это дороже утверждения и снимает вопрос целиком.

decisions_needed: |
  **ОДИН, И ОН ЕГО ГЛАЗ.** Строка 6: он унёс вещь на виду — хозяин поднял и отнёс обратно.
  Проверяется им на полигоне; до тех пор задача не закрывается.

play_check: |
  1. OPEN — возврат ноги с живым замером и тремя названными находками.
  2. WORK — порог смерти проверен чтением кода; счёт тестовых методов сверен до и после; **тесты
     прогнаны направлением лично**; чужой красный подтверждён на `main`; поверхность сверена
     пофайлово.
  3. (owner) — строка 6 остаётся за ним и не подделывается.
  4. CLOSE — RESULT, применение, коммит; разрешение на публикацию уходит исполнителю.

log: |
  руки построены и порог смерти волны выдержан: механизм остался один — та же строка дела ведёт его
  и туда, и обратно, а место назначения приносится извне и не выводится заново; растяжка заменена
  при том же числе методов, 739 зелёных прогнаны направлением, чужой красный доказан возвратом на
  базу; публикация разрешена, задача ждёт глаза владельца

next: |
  После публикации ветку со сценарием сольют с `main`, и владелец увидит ходящего хозяина, который
  ещё и относит вещи.

  **Волна: живых задач две, обе ждут не работы, а его.**
