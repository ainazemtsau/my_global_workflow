RESULT s-work-g-5a7c-the-key-line-is-green-001 (call: — владелец-инициированная, без родительского CALL)
direction: indie-game-development   track: оболочка   play: work   node/task: t-every-word-comes-from-a-key-1

outcome: |
  **ПЕРВАЯ СТРОКА РУБЕЖА Р1 ЗЕЛЁНАЯ.** Задача `t-every-word-comes-from-a-key-1` закрыта `done`:
  приёмка снята его собственной живой пробой, а не прогоном.

  **Его слова 2026-09-03:** «Не, я видел разные, переключал файлы. В файле код языка — всё
  менялось.» Это ровно та приёмка, которую называла карточка: «живая проба — переключение языка
  меняет надписи». Прогон её не заменял и не заменяет (раздел 10 плана, урок волны 11).

  **ТРИ ОСТАЛЬНЫЕ СТРОКИ `done_when` ПЕРЕМЕРЕНЫ ЭТОЙ НОГОЙ ПЕРВОЙ РУКОЙ НА `2cea9a94`, А НЕ ВЗЯТЫ
  ИЗ ВОЗВРАТА ИСПОЛНИТЕЛЯ.**

  1. **Текст живёт по ключам вне кода и сцен.** `Assets/StreamingAssets/TunnelCrew/Text/en.json` и
     `ru.json`; в `Assets/TunnelCrew/Network/LobbyController.cs` не осталось **ни одного** литерала,
     адресованного игроку (поиск строк длиной от шести символов — пусто).
  2. **Два языка заполнены, непереведённых ноль.** По 41 ключу в каждом файле, наборы ключей
     совпадают полностью (`diff` отсортированных списков — пусто).
  3. **Приборы стендов не локализуются, как и требовало исключение Р15.**
     `Network/IntegrationLabReadout.cs` и `Editor/IntegrationLabWindow.cs` — ноль упоминаний
     `Phrases` / `PhraseKey` / `Phrasebook`. На сборку `TunnelCrew.Words` ссылаются четыре asmdef:
     `Networking`, `World`, сама `Words` и её тесты.

  Строка 3 рубежа Р1 — «все надписи с первого дня идут через таблицу строк по ключам, английский и
  русский заполнены» — закрыта уликой. Из одиннадцати строк рубежа это первая зелёная.

  Две другие доставленные задачи остаются `waiting`: стенд ждёт его взгляда на двух экранах,
  удлинённое утро — прохода от будильника до двери. Обе приёмки — его глаз, и ни одна не снята.

evidence: |
  - Его слова в этой сессии, дословно: «Не, я видел разные, переключал файлы. В файле код языка —
    всё менялось. То, что ты пишешь, я это видел.»
  - `git show 2cea9a94:Assets/StreamingAssets/TunnelCrew/Text/en.json` и `ru.json` — 41 ключ в
    каждом; `diff` отсортированных наборов ключей пуст.
  - `git grep '"[A-Za-zА-Яа-я][A-Za-zА-Яа-я ,.!?—-]\{6,\}"' 2cea9a94 -- Assets/TunnelCrew/Network/LobbyController.cs`
    — ноль совпадений.
  - `git grep -c 'Phrases\.\|PhraseKey\|Phrasebook' 2cea9a94 -- Network/IntegrationLabReadout.cs
    Editor/IntegrationLabWindow.cs` — ноль.
  - `Assets/TunnelCrew/Words/GameLanguage.cs` — пин живёт в `UserSettings/TunnelCrew.language.txt`,
    той же идиомой, что и пин порта; `Fallback = "en"`; `All = { en, ru }`.
  - Числа тестов исполнителя (headless `863/863/0`, EditMode `245/245/0`) названы как его замер;
    эта нога их не перепрогоняла и на них не опирается — все четыре строки закрыты байтами и его
    словом.

state_changes: |
  1. CLOSE `t-every-word-comes-from-a-key-1` со статусом `done` — приёмка снята его живой пробой,
     три остальные строки перемерены первой рукой.
  2. Журнальные строки на ставку `bet-g-5a7c-wave-12` и на закрытую задачу: строка 3 рубежа Р1
     зелёная.
  3. `NOW.md` не менялся. Полоса `оболочка` продолжает жить — её следующий наряд
     `c-exec-g-5a7c-four-screens-see-one-fall-001` выдан и стоит `ready`.

captures:
  - Две ноги идут в WIN-U3 и WIN-U4 без карточек направления
    (`c-exec-g-5a7c-the-morning-ends-at-the-door-002:proba`,
    `c-exec-g-5a7c-bench-polygon-proba-1:build`). Владельцу предложено завести их задним числом;
    его слова на это пока нет. Это ровно тот учётный провал, которым волна 11 потеряла восемь ног.

decisions_needed:
  - q: Завести карточки двум продолжениям, идущим в WIN-U3 и WIN-U4 без нарядов направления?
    options: [завести сейчас; оставить до сверки 11.09]
    recommendation: завести сейчас — в волне 11 отсутствие карточек стоило нарезке слепоты.

play_check:
  - 1 recite: done — работа названа его словами: он подтвердил, что переключал язык файлом и
    надписи менялись; вопрос был «что с задачей U2».
  - 2 owner inputs (owner): done — его слова процитированы дословно и являются единственным
    основанием для снятия приёмки. Ни одно решение о содержании игры не принято.
  - 3 do the work: done — три машинные строки `done_when` перемерены первой рукой на `2cea9a94`,
    задача закрыта.
  - 4 self-check: done — числа тестов исполнителя не использованы как основание закрытия; ни одна
    строка не закрыта прогоном. Две другие доставленные задачи намеренно оставлены `waiting`.
  - 5 close: done — `close: light — because` каждая строка `done_when` выведена этой ногой либо из
    байтов коммита `2cea9a94`, либо из его собственных слов, сказанных в этой сессии. G5-чат не
    открывается.

log: строка 3 рубежа Р1 зелёная — задача про таблицу строк закрыта его живой пробой «переключал файлы, всё менялось», а не прогоном; три машинные строки перемерены первой рукой на 2cea9a94: 41 ключ в обоих языках при совпадающих наборах, ноль литералов игроку в LobbyController, ноль упоминаний Phrases в приборах стендов; стенд и удлинённое утро остаются waiting — их приёмка его глазом не снята

next: |
  Полоса `оболочка` идёт дальше: `c-exec-g-5a7c-four-screens-see-one-fall-001` готов к запуску в
  WIN-U2 на базе `2cea9a94`. Приёмка стенда и утра — за владельцем, стенд первым.
  Ждёт его слова: заводить ли карточки двум продолжениям, идущим в WIN-U3 и WIN-U4 без нарядов.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-work-g-5a7c-the-key-line-is-green-001.md
