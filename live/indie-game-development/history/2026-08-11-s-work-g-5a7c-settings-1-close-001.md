# RESULT — s-work-g-5a7c-settings-1-close-001

play: work · close: **light — because** все три строки `done_when` наряда и все шесть строк карточки
`t-settings-1` перемерены ПЕРВОЙ РУКОЙ по закоммиченным байтам опубликованной головы: значения
прочитаны из файлов до и после этой ногой, прогон тестов сделан здесь на выгруженном дереве
публикации, границы и состав диффа проверены командами, прогнанными здесь, а два места, которые
возврат честно назвал недоказанными — живой ответ Unity после последней правки сцены и терминальный
readback селектора, — добраны ЗДЕСЬ прямым чтением. Строки, которую судит его глаз или чужой
пересказ, в этой задаче нет: наряд доказывал неизменность поведения тестами и байтами, а не вердиктом
владельца
direction: indie-game-development · node: g-5a7c · task: t-settings-1 (вне дорожек) ·
call: c-exec-g-5a7c-settings-1-001
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `275e0679`, дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` =
`a5a84350916a19ca49822e5a516589cd78698927`. Селектор слота прочитан read-only командой
`tools/select-slot.ps1 -Slot WIN-U1` в этой ноге.

## outcome

`t-settings-1` закрыта. **Коробка разобрана, имя больше не врёт, место груза стало сценой, и ни одно
из 56 значений не сдвинулось.**

Владельцу теперь: числа лежат в `Assets/TunnelCrew/Settings/` четырьмя ящиками с честными именами —
**Network Connection** (адрес, порт), **Game Rules** (всё, что меняет происходящее: массы, силы,
скорости, пороги, классы груза), **Local Player** (обзор, чувствительность мыши, прицел),
**Presentation** (размеры рисования и цвета). Место груза больше не число в файле: в
`IntegratedHouse.unity` под `StartConditions` стоят четыре объекта `Cargo Spawn 0..3` — их видно,
их двигают мышкой, и класс груза выбирается на самой точке. Точка означает ОПОРУ, а не центр:
поднял точку на стол — груз встал на стол, и высота под класс подбирается сама.

**Возврат пришёл уже опубликованным, и это перемерено, а не принято на слово:** проверенный кандидат
`54fa5f15` дошёл до `origin/main` без единой изменённой строки кода.

## evidence — перемерено и прогнано первой рукой

### Опубликовано ровно то, что было проверено

`54fa5f15` имеет родителем ровно объявленный базис `6599ae87`. Дифф по всем поверхностям задачи от
кандидата до опубликованной головы ПУСТ:

```text
git diff --stat 54fa5f15 a5a84350 -- Assets/TunnelCrew/Settings Assets/TunnelCrew/Network \
  Assets/TunnelCrew/Editor Assets/TunnelCrew/World Assets/TunnelCrew/Scenes
(пусто)
```

Между кандидатом и публикацией лежат откат `9d1bdae5`, реаплай `1e36fe0a`, документ `2c5fa670`,
слияние `fd7583ea` с головой полосы хозяина и документ `a5a84350`. Всё, что пришло сверх кандидата, —
это работа полосы `householder` (`Core/Householder/**`, `Core/Situations/**`, два файла тестов
хозяина) и два документа. Пересечения с задачей — ноль.

### ГЛАВНЫЙ ГЕЙТ: 56 значений сверены ЗДЕСЬ, расхождений НОЛЬ

Прочитаны `git show 6599ae87:…/NetworkPlaySettings.asset` и пять новых ассетов на `a5a84350`.
Объявление сходится: **50 полей верхнего уровня + 6 полей внутри `CargoClassDefinition` = 56**.

- **связь → `NetworkConnectionSettings.asset` (2):** `_defaultAddress: 127.0.0.1`, `_port: 7770`.
- **правда игры → `GameRulesSettings.asset` (37 + 6 вложенных = 43):** `_movementSpeed: 0.6`,
  `_spawnOriginX: -0.12`, `_spawnSpacing: 0.24`, `_walkerRadius: 0.036`, `_walkerJumpSpeed: 2.2`,
  `_walkerFlightSpeed: 0.65`, `_walkerGravity: 3`, `_cargoGravity: 3`, `_eyeHeight: 0.136`,
  `_lookPitchLimit: 80`, `_cargoLinearDamping: 4`, `_cargoAngularDamping: 6`,
  `_cargoMaxAngularSpeed: 2`, `_cargoSolverIterations: 12`, `_cargoSolverVelocityIterations: 4`,
  `_physicsStepsPerTick: 2`, `_cargoSettleLinearSpeed: 0.01`, `_cargoSettleAngularSpeed: 0.05`,
  `_cargoSettleTicks: 2`, `_cargoCarryHeight: 0.152`, `_cargoCarryPower: 200`,
  `_cargoLiftPower: 200`, `_cargoPickupRange: 1.28`, `_cargoCarryOffset: 0.8`,
  `_cargoMaxHolders: 4`, `_cargoGripLength: 1.6`, `_cargoHandReach: 0.016`,
  `_cargoHandStiffness: 2000`, `_cargoVerticalDampingRatio: 1`,
  `_cargoHandForceReachFalloff: 1`, `_cargoHaulLinearDamping: 350`,
  `_cargoHaulAngularDamping: 1.92`, `_carrySpeedScale: 0.6`, `_cargoRustleForce: 60`,
  `_cargoKnockForce: 150`, `_cargoHeavyNoiseForce: 400`, плюс `_cargoClasses` тремя записями
  `small/0/0.192/0.04/0.04/48`, `medium/1/0.288/0.06/0.06/100`, `large/2/0.384/0.08/0.08/240`.
- **только моё → `LocalPlayerSettings.asset` (3):** `_cameraFieldOfView: 70`,
  `_lookSensitivity: 0.12`, `_crosshairSize: 4`.
- **оформление → `PresentationSettings.asset` (4):** `_walkerHeight: 0.16`, `_playerColors` четырьмя
  цветами `(0.95,0.27,0.2,1) (0.16,0.55,0.95,1) (0.2,0.8,0.38,1) (0.95,0.75,0.16,1)`,
  `_cargoColor (0.65,0.35,0.85,1)`, `_cargoHeldColor (0.87,0.63,1,1)`.
- **место в мире → сцена (4):** `_cargoSpawnX/_cargoSpawnZ` и `_additionalCargoSpawns` легли в
  Transform четырёх точек, `_additionalCargoClassIds` — в поле `_cargoClassId` на самих точках.

Контрольная сумма по видам: 2 + 43 + 3 + 4 + 4 = **56**. Пороги шума `60 / 150 / 400` на месте,
массы классов `48 / 100 / 240` на месте, все цвета знак в знак. **Ни одного расхождения.**

### Место груза: равенство доказано кодом, а не обещанием

| CargoId / объект | Transform опоры | класс | центр при спавне |
|---|---|---:|---|
| `0 / Cargo Spawn 0` | `(0, 0, 2.5)` | `0` | `(0, 0.02, 2.5)` |
| `1 / Cargo Spawn 1` | `(-2.5, 0, -0.35)` | `0` | `(-2.5, 0.02, -0.35)` |
| `2 / Cargo Spawn 2` | `(-2.5, 0, 0)` | `1` | `(-2.5, 0.03, 0)` |
| `3 / Cargo Spawn 3` | `(-2.5, 0, 0.35)` | `2` | `(-2.5, 0.04, 0.35)` |

Старый конструктор: `new CargoState(cargoId, x, cargoClass.CenterYOnFloor, z, 0f, x, z, 1f, false,
cargoClassId)`. Новый: то же самое, где Y стал `supportPosition.y + cargoClass.CenterYOnFloor`. У всех
четырёх точек `y = 0`, значит выражения совпадают тождественно. Класс точки 0 читался старым кодом как
`PrimaryCargoClass.CargoClassId`, а `PrimaryCargoClass` — это `_cargoClasses[0]`, то есть `small` с
`CargoClassId = 0`; в сцене явно записан `_cargoClassId: 0`. Порядок сохранён: `_cargoSpawns` в
`StartConditions` перечислен `[Spawn0, Spawn1, Spawn2, Spawn3]`, а курьер крутит тот же цикл
`for (cargoId = 0; cargoId < CargoSpawnCount; cargoId++)`, только источник сменился с ассета на
`StartConditions`.

Дифф сцены — 191 строка вставки и одна замена: четыре GameObject с Transform и `CargoSpawnPoint`,
их четыре ссылки в `_cargoSpawns`, четыре ссылки в `m_Children` у `StartConditions`, и одна ссылка
`_startConditions` у курьера. `_play`, `_householder`, `_players: 2`, дом, мебель, стены, свет и все
семь прочих `_settings` — не тронуты. `NetworkRuntime.prefab` байт в байт.

### Гейты имени и места — пустой вывод

```text
grep -rn "_cargoSpawnX\|_cargoSpawnZ\|_additionalCargoSpawns" Assets/TunnelCrew/Network   → пусто
grep -rln "NetworkPlaySettings" Assets/                                                  → пусто
```

Единственное упоминание старого имени во всём дереве — комментарий
`tests/TunnelCrew.Core.Tests/TelekineticPickupTests.cs:10`, называющий несуществующий уже
`NetworkPlaySettings.asset`. Наряд требовал пустой дифф по `tests/`, поэтому оставлено сознательно и
названо вслух. Ассет, владеющий массами, силами и порогами, зовётся `GameRulesSettings` — про сеть в
имени ни слова.

### Отнесение по читателям, а не по имени — перепроверено поиском

- `_eyeHeight` → **правда игры**, хотя звучит как камера: помимо `NetworkWalkerCamera.cs:44` он едет
  `GameRulesSettings.cs:225 pickupEyeHeight` → `CargoRules.PickupEyeHeight` →
  `AuthoritativeCargoRoster.cs:643`, то есть в начало луча захвата у судьи. Ловушка наряда
  подтверждена.
- `_lookPitchLimit` → **правда игры**: `NetworkWalkerInput.cs:142`. Ловушка подтверждена по букве
  наряда, но с оговоркой — см. находку ниже.
- `_lookSensitivity` → **только моё**: единственный читатель `NetworkWalkerInput.cs:138`.
- `_walkerHeight` → **оформление**: только `NetworkWalkerPresentation.cs` и гизмо
  `StartConditions.cs:120`; судья его не видит.
- `_walkerRadius` → **правда игры** вопреки тому, что его тоже рисуют: `WalkSpaceProbe.cs:35` и
  `AuthoritativeCargoRoster.cs:247` через `CargoRules`.
- `_cameraFieldOfView`, `_crosshairSize` → **только моё**: единственный читатель
  `NetworkWalkerCamera.cs:34,54`.
- `_spawnOriginX`, `_spawnSpacing` → **правда игры**, а не место в мире: их читает `CreateSpawnState`
  при раздаче мест подключившимся.

### Ссылки целы — доказано и байтами, и живым редактором

Старые GUID сохранены и переиспользованы: ассет `6e9b9132205a407d8bd6df51e702bf12` стал
`PlaySettings.asset`, скрипт `5b973f60ef2290747944eadb35eeb8c4` — `PlaySettings.cs`. Поэтому **восемь
ссылок в сцене и одна в `NetworkRuntime.prefab` — девять — не переписывались вовсе**. Все
одиннадцать GUID нового и переиспользованного (четыре ассета, пять скриптов, `CargoSpawnPoint`,
фасад) разрешены в реальные `.meta` командой поиска — ни одной висячей ссылки.

**Живой ответ Unity добран ЭТОЙ ногой** — ровно то, чего возврату не хватило:

```text
editor-application-get-state → IsCompiling: false, IsUpdating: false, IsPlaying: false
                               Unity 6000.5.5f1
StartConditions/Cargo Spawn 2 → компонент TunnelCrew.World.CargoSpawnPoint, isEnabled True
StartConditions._cargoSpawns  → TunnelCrew.World.CargoSpawnPoint[4], все четыре разрешены
```

Элемент `[2]` массива — тот самый компонент, что читается на `Cargo Spawn 2` (instanceID совпал), то
есть и порядок, и разрешение ссылок подтверждены живым редактором, а не только YAML. Журнал ошибок
Unity за весь день (30 записей, 04:39–10:12) прочитан целиком: **ни одной ошибки компилятора
`error CSxxxx` и ни одного `Missing Script`** — все тридцать это отказы самих MCP-вызовов
(валидация параметров, `Console / Clear Logs`).

### Прогон сделан здесь

```text
dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release
Пройден!: не пройдено 0, пройдено 179, пропущено 0, всего 179
```

На выгруженном `git archive a5a84350` дереве в отдельной папке, слот не тронут. Заявленные возвратом
`173/173` на кандидате перепроверять прогоном не нужно и это не поблажка: `core/TunnelCrew.Core.csproj`
компилирует РОВНО `Assets/TunnelCrew/Core/**`, а дифф кандидата по этой папке ПУСТ — компилируемая
поверхность тестов между `6599ae87` и `54fa5f15` побайтово одна и та же, значит результат тождественен
по построению. Дифф по `tests/` тоже пуст: **ни одно ожидание не переписано.**

### Границы — пустой вывод по каждой запрещённой поверхности

```text
git diff --stat 6599ae87 54fa5f15 -- tests                                  → пусто
git diff --stat 6599ae87 54fa5f15 -- Assets/TunnelCrew/Core                 → пусто
git diff --stat 6599ae87 54fa5f15 -- ProjectSettings                        → пусто
git diff --stat 6599ae87 54fa5f15 -- Assets/GasCoopGame                     → пусто
git diff --stat 6599ae87 54fa5f15 -- …/Prefabs/NetworkRuntime.prefab        → пусто
git diff --name-only 6599ae87 54fa5f15 | grep -i householder\|situation\|CarryWorld → пусто
```

Дифф — ровно 33 пути, все внутри `Settings/`, `Network/`, `Editor/`, `World/`, `Scenes/`. Строка 6
карточки (право идти параллельно полосе хозяина) доказана постфактум тем же измерением: полоса
хозяина принесла `Core/Householder/**`, `Core/Situations/**` и два своих файла тестов — **пересечение
ноль**, и слияние `fd7583ea` не тронуло ни одного байта сцены и настроек.

### Откат исполнен и сошёлся знак в знак

```text
git revert --no-edit 54fa5f15  →  9d1bdae5
git rev-parse 9d1bdae5^{tree}  →  5734e0ba59694f35cebead61a9483a742b5b784e
git rev-parse 6599ae87^{tree}  →  5734e0ba59694f35cebead61a9483a742b5b784e
```

Деревья совпали. Реаплай `1e36fe0a` вернул дерево кандидата `521d0556` знак в знак. Ничего вне
репозитория откату не нужно.

### Терминальный readback селектора — добран ЗДЕСЬ

Возврат честно сказал, что отдать `CLEAN / AVAILABLE / lease none` не может, потому что это дело
Control. Прочитано в этой ноге read-only:

```text
slot: WIN-U1 · branch: slot/win-u1 · head: a5a84350916a19ca49822e5a516589cd78698927
state: CLEAN · lifecycle: AVAILABLE · lease: none · availability: AVAILABLE
```

Control свою часть выполнил: слот перемотан на опубликованную голову, аренда снята.
`mcp-endpoint: unrecorded` — снова, третий раз подряд по разным слотам.

### Что осталось непроверенным и почему это не мешает

- **Прогоны `hygiene.ps1` и `check.ps1` этой ногой не повторялись** — они стоят на записанном выводе
  возврата. Их существенное содержание перемерено иначе: каждый новый `.cs` имеет отслеживаемый
  `.meta` в том же коммите (видно в манифесте из 33 путей), и все GUID разрешаются. Гейт
  `check.ps1 -Deliver` — дело Control, и его вывод в моих байтах не лежит.
- **Владелец руками ничего не проверял, и возврат этого не заявляет.** По подписанной карточке
  неизменность поведения доказывается тестами и байтами, а не его глазом, поэтому строка закрыта.

## state_changes

1. `t-settings-1`: закрыть, терминальный статус `done`. Закрыта СВЕТЛЫМ путём — назван вслух.
2. `c-exec-g-5a7c-settings-1-001`: закрыть — наряд исполнен, опубликован в `a5a84350`, слот вернулся
   `CLEAN / AVAILABLE / lease none`.
3. Завести `i-host-does-not-enforce-the-settings-pitch-limit-001` (`level: execution`,
   `route: review`) — судья зажимает наклон четвертью оборота константой, а не настройкой `80`.
4. `i-call-named-path-absent-on-declared-basis-004`: получает журнальный указатель на эту ногу —
   СЕДЬМОЙ случай того же класса. Новый ключ в карточку НЕ добавляется: по букве §Issues карточка
   это указатель, а перечень случаев живёт в `history/`.
5. `NOW.md`, `g-5a7c`, текст ставки, обе дорожки и обе открытые карточки вопросов — не тронуты.

## captures

- **СЕДЬМОЙ случай `i-call-named-path-absent-on-declared-basis-004`, и он мой.** Наряд сослался на
  `knowledge/how-the-game-is-built-layers.md` как на источник правила слоёв, не сказав, что файл
  лежит в репозитории Направления: на объявленном базисе продуктового репозитория такого пути нет, и
  исполнитель это назвал. Работу не остановило только потому, что наряд процитировал сам тест
  дословно — «влияет ли это на то, что произойдёт в игре». Ровно класс записи: наряд утверждает о
  венью факт, не сверенный с венью.
- **Наряд соврал ещё в двух мелочах, и обе поймал исполнитель, а не я.** «Двенадцать читателей» —
  файлов одиннадцать (перепроверено: ровно 11 файлов ссылаются на тип, из них 10 потребителей плюс
  сам источник; `LobbyController.cs:38` читает адрес транзитно через
  `NetworkSessionService.DefaultAddress`). «Те же три куска груза» в owner-visible абзаце — кусков
  четыре, и по байтам базиса их всегда было четыре.
- **Задача шла без дорожки при включённом режиме дорожек.** У `c-exec-g-5a7c-settings-1-001` нет поля
  `track`, как у `t-situation-1`, — по замыслу ставки это законно для общей задачи, но карточка
  дорожки `cargo-hands` прямым текстом пишет «НЕ открывает `IntegratedHouse.unity`», а эта задача
  сцену открывала. Место для `audit`, не для правки задним числом.
- **`mcp-endpoint: unrecorded` у WIN-U1** — при том что мост живой и отвечает прямо сейчас. Запись
  endpoint не переживает освобождение аренды; это третье наблюдение подряд.
- **Устаревший комментарий** `TelekineticPickupTests.cs:10` называет несуществующий файл. Уедет с
  первой ногой, которой законно открыть `tests/`.
- **Локальная ветка `main` продуктового репозитория стоит на `11f8ddbf`**, тогда как `origin/main` —
  `a5a84350`. Рабочая копия на `main` протухла на несколько публикаций.

## decisions_needed

Нет. Ни одна строка закрытия не потребовала его слова.

## play_check

- 1 recite: done — шесть строк карточки и три строки наряда сведены и закрыты поимённо
- 2 owner inputs (owner): done — от него не требовалось ничего; наряд доказывал неизменность
  поведения тестами и байтами, вердикт владельца в `done_when` не входил. Открытая карточка
  `q-house-hears-20-percent-earlier-001` вынесена ему этой ногой отдельной репликой и остаётся ждать
- 3 do the work: done — опубликованное перемерено первой рукой, прогон и живое чтение Unity сделаны
  здесь
- 4 self-check: done — каждая строка закрыта своим измерением; непроверенное названо непроверенным
- 5 close: done — светлый путь назван вслух; задача не последняя в ставке, `review` не открывается

## Гейты

- **G1.** Дорожка `householder` держит свой корень `c-exec-g-5a7c-react-2-002`; `cargo-hands`
  свободна; лимит `2` цел. Эта задача дорожки не занимала.
- **G2.** Задача служила активной ставке `g-5a7c`.
- **G3.** Аппетит не тронут.
- **G5.** Светлый путь по букве плея: каждая строка перемерена первой рукой по закоммиченным байтам,
  прогон и живое чтение редактора сделаны в этой ноге. Отдельный свежий чат не открывался, и это
  названо.
- **G7.** Ничего продуктового или геймплейного этой ногой не решено.
- **G9.** Карточки задачи и ставки не редактировались, только закрыты и снабжены журналом.
- **task-play lifecycle.** Открытыми остаются `t-react-2` и `t-react-3` — задача не последняя,
  `review` ставки не открывается.

## log

`t-settings-1` закрыта светлым путём на опубликованной голове a5a84350, и ГЛАВНЫЙ ГЕЙТ взят
по-настоящему: все 56 значений прочитаны этой ногой из файлов до и после и сошлись без единого
расхождения — 2 связи, 43 правды игры, 3 личных, 4 оформления и 4 уехавших в сцену, контрольная сумма
сходится; пороги шума 60/150/400, массы 48/100/240 и все цвета знак в знак; место груза стало четырьмя
объектами `Cargo Spawn 0..3` под `StartConditions`, и равенство доказано не обещанием, а кодом —
старый конструктор состояния отличается от нового ровно слагаемым `supportPosition.y`, которое у всех
четырёх точек ноль, а класс точки 0 совпал с прежним неявным `PrimaryCargoClass`; старые GUID ассета и
скрипта переиспользованы, поэтому восемь ссылок сцены и девятая в префабе не переписывались вовсе, и
все одиннадцать GUID разрешены в реальные `.meta`; 179/179 прогнаны этой ногой на выгруженном дереве
публикации, а заявленные 173 на кандидате доказаны сильнее прогона — компилируемая тестами поверхность
`Assets/TunnelCrew/Core/**` побайтово не тронута, дифф по `tests/` пуст, ни одно ожидание не
переписано; границы пусты по шести запрещённым поверхностям, дифф ровно 33 пути, пересечение с полосой
хозяина ноль; ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК, деревья совпали; два места, которые
возврат честно назвал недоказанными, добраны ЗДЕСЬ — живой редактор отвечает, не компилирует, держит
`CargoSpawnPoint[4]` разрешёнными и не имеет ни одной ошибки компилятора за весь день, а селектор
WIN-U1 читается `CLEAN / AVAILABLE / lease none`; заведена находка про то, что судья зажимает наклон
константой в четверть оборота, а не настройкой 80, то есть поле отнесено к правде игры правильно по
намерению, но сама правда судьёй не охраняется

## next

1. **Полоса хозяина занята**: `c-exec-g-5a7c-react-2-002` в WIN-U2 от головы `5fe0dadb`. Её базис
   уже отстал от опубликованной `a5a84350` — по её же правилу это перебазирование, а не СТОП.
2. **Свободна одна дорожка из двух.** Следующий корень по волне — `t-react-3` (проверка на
   подставленном источнике), но он стоит ПОСЛЕ `t-react-2` по порядку задач дорожки, поэтому пока
   ставится не он, а решение владельца о судьбе волны (ниже).
3. **Ждут его слова, без вопроса от меня:** `q-house-hears-20-percent-earlier-001` (дом начнёт слышать
   на 20% раньше — оставляем или один раз перекручиваем пороги),
   `q-two-return-claims-rest-on-unverified-owner-words-001`, порог смерти (5) волны про число ног.
4. **Новая находка** `i-host-does-not-enforce-the-settings-pitch-limit-001` — маршрут `review`, в эту
   волну не лезет.

END_OF_FILE: live/indie-game-development/history/2026-08-11-s-work-g-5a7c-settings-1-close-001.md
