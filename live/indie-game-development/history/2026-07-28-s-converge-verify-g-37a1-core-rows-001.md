# RESULT s-converge-verify-g-37a1-core-rows-001

direction: indie-game-development
play: converge-verify
node: g-37a1
call: c-converge-verify-g-37a1-core-rows-001
verify_target: build
date: 2026-07-28

## outcome

**FAIL.** `work/converge-g-37a1-core-rows.md` does not survive the independent attack against the
fifteen owner-approved criteria of `g-37a1`. Both build attacks fail, on named rows, and no
`§SIGNOFF` line was written. Sixteen findings F1–F16 are named and **none is filled, repaired or
decided here.** `g-37a1` stays `parked`; no shape, no bet, no task, no track; `knowledge/` was not
written; neither card was touched; no owner question was answered and none was manufactured.

**WHAT THE FAIL IS NOT.** Not one of the sixteen live requirement lines is challenged. Every
finding is against the ROWS — the acceptance instrument the rows are — and three of them (F1, F2,
F15) are the only ones that could move what the build must contain. Nothing here blocks the first
build tasks: a man digs a hole and walks into it, the body, and co-op immediately after are
untouched, exactly as the previous verification found. The owner's rulings were not re-litigated,
no row was asked to prove the core is fun, no knob's value was attacked, and no row was scored
incomplete because a value is unchosen.

**Attack 1 — «the row set is complete» — FAILS.** The fifteen criteria were split atomically
(ninety-one atoms) and mapped to §WHAT rows independently of §WHAT's own arithmetic, then swept
against the author's fourteen-class oracle AND against a competing class list authored from first
principles before the attack (below, and in `captures`). Four holes: §RULES is not the list its own
derivation produces and is the only thing criterion 12's «все правила читаются» is judged against
(F3); §WHAT declares two governance clauses covered by build rows, so the clause whose whole job is
to stop co-op being deferred is unmapped (F13); criterion 12's load-button atom is mapped to a row
whose test no compliant build can pass, because nothing requires a second level to exist (F2); and
a class neither oracle carries — the machine budget, and the seam where the rig meets the network —
is uncovered and unnamed (F15).

**Attack 2 — «no row leans on an unresolved question» — FAILS, and the leaning row is the newest
text in the file.** ROW 15's leash test requires the outsider to say what the base is for; the only
evidence a base did anything is the counter moving, and `d-air-counter-visibility-001` is OPEN. The
row's claim that the visibility decision does not block it was established against test (a) — which
is true of test (a), and `NOW.md` quotes test (a) verbatim as its ground — and was carried across
the amendment unchecked when test (b) was added (F5). Two further leans: ROW 5 leans on a reading
of §KNOBS that ROW 12 contradicts verbatim (F1), and criterion 6's «первое вскрытие не убивает
мгновенно» leans on a knob the coupling note does not name (F10).

**THE TWO PLACES THE CALL SAID TO PUSH HARDEST BOTH BROKE, and they broke differently.** The
BASE: its leash test is the one new outsider test in the amended set that pins no defaults and
commits no pass rule, and it reopens an open owner decision the row declares irrelevant (F5, F6).
ROW 17: the answer to the CALL's question is neither — it does not move the laboratory into the
next version, it leaves the pre-build stretch unguarded, which is the stretch that actually killed
`g-12fd`; and its own operational test is failed by criterion 15, which is owner-approved text in
the same card (F4).

**WHAT SURVIVED THE ATTACK.** Stated first, because most of what was attacked held.

- **The signed distinction «the mark tells SIZE, not KIND» is the strongest single thing in the
  file and it holds.** It was attacked directly — as a collapse of criteria 2 and 7 into one
  legibility clause, and as an over-reading of criterion 2's «примерно насколько большой» — and
  both attacks failed on the row's own ground: it is precisely because size is known before the cut
  and kind is not that a breach is a priced gamble rather than one trade repeated. The file's
  warning that a verification collapsing them will produce a false finding is correct, and the one
  place the file itself commits that collapse is F7.
- **ROW 4 is the best-formed row in the set.** It names its own rig hazard before a verifier can
  ("This row's tests run at DEFAULTS, with every switch off"), records the two derived claims that
  died with the amendment instead of quietly deleting them, and its outsider test can fail in five
  distinct, observable ways. That it names the hazard for itself is exactly why its absence in
  ROWS 1, 3, 13 and 15 is a defect and not a reading (F6).
- **ROW 11's closure of the direction's oldest unfixed problem holds.** With no sinks there is no
  state to wait FOR, the check is one line, and no new mechanic is required. The derived
  acceptance property (rows 3 + 11: a light kind released at the bottom rises into the steps you
  cut) is a real consequence of two rows and is testable as claimed.
- **The SETTING rule was held cleanly throughout the file.** No value is chosen for any of K1–K5,
  and ROW 5's test that catches the fake — "a knob whose change produces no describable difference
  FAILS as a knob even though the file accepted the value" — is a genuinely adversarial instrument
  written against the author's own interest.
- **§SEAM's four seams are correct as far as they go**, and seam 2's removal of the whole
  firing-threshold apparatus from the first core is a real simplification. The gap is a fifth seam
  it does not have (F15).
- **The retirement of ROW 7 is handled correctly.** The number is kept, the reason travels with the
  answer, and the predicted sideways-digging playtest effect is recorded as an accepted property.
  Attacking it would have been a false finding and it was not attacked.
- **Four hazards are already NAMED by the target and are CONFIRMATIONS, not findings:** the
  mixed-region legibility hole at ROW 13, the «sources are finite» conflict at ROW 12, the venue's
  missing price at ROW 1, and the body being new work rather than a check at ROW 2. Each is
  reported to the target's credit and none is re-reported below.

## evidence

**SESSION INDEPENDENCE.** Fresh session in the direction worktree, no owner present, as the CALL
and the play require. Not the converge chat, not the map chat, not a day chat.
`history/2026-07-28-s-converge-g-37a1-core-rows-001.md` was **not read at all** — the play forbids
the verifier reading the deciding session's reasoning; its existence was confirmed by a directory
listing and nothing more. The amendment receipt
`history/2026-07-28-s-map-g-37a1-owner-answers-001.md` IS a `map` receipt and WAS read, as the CALL
permits. `history/2026-07-28-s-research-g-37a1-engine-fit-check-001.md` (step 4) was **not read**,
per the CALL, and no engine fact decided a row. `archive/**` was not read and the product
repository was not opened: every engineering fact cited below is carried by an existing
first-hand receipt of 2026-07-27, never re-derived here.

**STATE MOVED MID-LEG AND WAS RECONCILED.** `NOW.md` was read at `01e75ca8`; the concurrent
research leg `s-research-g-37a1-engine-ecosystem-001` committed `6fba9a55` while the attack was
running, changing `updated:` and `i-engine-fit-decision-ladder`'s `review_when` and `evidence`. The
moved block was re-read at `6fba9a55` before any edit, and this leg's state changes are stated
against it and preserve that leg's text.

**FAN-OUT.** None, deliberately. `converge-verify` is not a play that mandates in-leg fan-out, and
unlike `c-converge-verify-g-37a1-digging-card-001` this CALL carries no instruction to distrust a
single sweep. The leg ran single-agent, with an internal self-refutation pass instead: twenty-three
raw findings entered, **seven were refuted or downgraded** and are named in
`dropped as refuted or already named` below so nobody re-raises them cheaply. This is the binding
fresh G5 pass for the rows, not a pre-pass.

**THE INDEPENDENT ORACLE, and how the play's «do not rely only on the author's instrument» was
met.** `work/oracle-first-playable-core-v1.md` is the author's own checklist and is a CANDIDATE
with no authority; the target sweeps itself against it, so it cannot be the only instrument. Six
competing classes were authored from first principles and shipped precedent BEFORE the attack, on a
different axis — not what the simulation must decide, but what must be true for a solo developer to
USE the artifact daily. They are recorded in `captures` and proposed for promotion. Two of the six
produced findings (C1 → F1, C4 → F15); one produced F16; three found nothing and are recorded as
covered.

**ATOMIZATION.** The fifteen criteria were split into ninety-one atoms independently of §WHAT, and
each was mapped to a row, to a named-uncovered clause, or to a hole. §WHAT's own arithmetic —
thirteen of fifteen carrying a row, one uncovered clause, two glosses — is CORRECT as far as it
goes; the disagreements are F2, F3, F13 and F15, all of which are atoms §WHAT records as covered.

## findings

**F1 — §KNOBS' `section` row contradicts ROW 12 verbatim, and turns the rig into the level
generator criterion 12 forbids.**
ROW 5's OBSERVABLE: "Every number in §KNOBS is readable and writable from the BUILT player, without
Unity, without a code edit and without a rebuild." §KNOBS' `section` group lists *dimensions ·
pocket count · pocket sizes · per-pocket kind · total substance*. ROW 12's OBSERVABLE says those
are "set before the player is built"; criterion 10 says «зафиксированы до сборки»; and ROW 12's own
FORCES says the same four are "level-authoring inputs, explicitly NOT generation (criterion 12: an
experiment instrument, not a level generator)". The three cannot all hold: if pocket count, sizes
and kinds are dialled from the built player and a restart produces a different section, the game is
placing them at load — which is generation — and they are not fixed before the build. This is not
bookkeeping: it decides whether the parameter home is a settings file or a level generator, and
§KNOBS is one of the two items the file itself calls most likely to be discovered late.
*Hits:* §KNOBS, ROW 5, ROW 12. *Routes to:* `converge`.

**F2 — «уровень» is unsigned, and ROW 5's own in-game test cannot be passed by a build that
satisfies every other row.**
§GLOSSARY signs «участок», and signs «сцена» as "One buildable **first** scene used as the
experiment rig". It never signs «уровень» — the unit that criterion 9's full restart and criterion
12's load-button both operate on, and the word the owner used himself («просто кнопкой, чтобы
загрузить другой уровень»). ROW 5's OUTSIDER requires "from inside a running game, load another
level"; no row anywhere requires the build to contain a second section, ROW 12 fixes one section
before the build, and §KNOBS' `section` group is singular. So either «уровень» = «участок» and the
build must ship at least two, which nothing states and nothing prices, or it means a restart of the
same one, in which case the control duplicates ROW 4 and the criterion's own «другой» is not
satisfied. *Corroboration that the amendment did not re-run the term sweep:* §SIGNOFF counts
"Twenty-six terms"; the table now holds twenty-seven, because «база» was added and the count was
not.
*Hits:* §GLOSSARY, ROW 5, ROW 12. *Routes to:* `converge`.

**F3 — §RULES is not the list its own derivation produces, and its pass bar cannot be met.**
The list claims to be "derived from the rows" and to be what «все правила читаются» is judged
against — and nothing else judges that clause. Four rules the rows require a player to read are
absent. (a) **Any pocket may be entered** (ROW 13a, criterion 7) — the ruling that replaced
passability, and the one an outsider is least likely to reach unprompted, because a wall is the
natural assumption about a hazard you can see. (b) **The two kinds are told apart before entering**
(ROW 13d, criterion 7). (c) **Nothing restricts where you cut** (ROW 6's OBSERVABLE, criterion 2) —
the rule that distinguishes this from an authored-panel game. (d) **Being sealed in kills you**
(ROW 3b, criterion 3's «законная смерть»). Separately, the acceptance test — "an outsider, after
one session with no explanation, states these eleven in his own words" — commits **no threshold**,
while every comparable instrument in the file commits its pass rule before the sitting; and rule 10
(«ничего из того, что делает вещество, не направлено на тебя») is a negative-by-absence that an
uninstructed player has no occasion to state at all, so the list is all-or-nothing on an item that
cannot be got.
*Hits:* §RULES. *Routes to:* `converge`.

**F4 — ROW 17's operational guard is failed by the card, and «вечная лаборатория» is signed to a
reading his words do not fix.**
The test is "whether anything downstream is blocked on him playing". Criterion 15 — owner-approved
text in the same card — says «Новая оценка сроков делается после того, как играбельное ядро
существует и **в него играли**», and `NOW.md` `direction_forecast.update_when` says the same. A
live downstream item is blocked on him playing, so ROW 17's test returns FAIL against the card
itself on the day the core is built. Second half: his quoted words («Нет такого, что я сколько-то
буду сидеть в него играть, а мы не будем работать») rule out ONE behaviour; they do not define the
failure mode, and §GLOSSARY converts them into the definition the test then uses — the one term the
file boasts of finally signing is signed by an inference. Third half, and the answer to the CALL's
own question: the guard does NOT move the laboratory into the next version; it leaves the
**pre-build** stretch unguarded, and that is the stretch that killed `g-12fd`, which burned three
legs and eighteen findings and produced no build at all. Every one of the sixteen rows names the
built player as its EVIDENCE, so the entire instrument is inert until a build exists. The guard for
that stretch is the bet's appetite and `kill_by` (G3, G4), which only step 6 creates.
*Hits:* ROW 17, §GLOSSARY. *Routes to:* `converge`; the pre-build guard belongs to `shape`.

**F5 — ROW 15's leash test reopens an open owner decision that the row declares irrelevant.**
Test (b): "PASS if, without being told, he returns to the base at least once before running out,
**and can say what the base is for**." The only evidence that the base did anything is the counter
moving, and `d-air-counter-visibility-001` is OPEN — its four options range from an invisible
counter carried by breathing to a bar on screen, and they do not all support the test. The row
asserts "THIS TEST DOES NOT NEED THE COUNTER TO BE VISIBLE — so `d-air-counter-visibility-001` does
NOT block this row", and that claim is TRUE of test (a), whose ground is death timing;
`NOW.md`'s own note on the decision quotes test (a) verbatim as the reason. Test (b) was added by
the amendment and the claim was extended to the whole row without re-derivation.
*Hits:* ROW 15. *Routes to:* `converge`.

**F6 — the rig switches the amendment requires invalidate the outsider tests of three other rows,
and only ROWS 4 and 9(b) pin their tests to defaults.**
Immortality — air still drains, zero does not kill, and it is a requirement, not a convenience —
makes ROW 15(a) ("play until he dies twice") unrunnable, makes ROW 15(b)'s "before running out"
meaningless, and makes ROW 3(b) ("being stuck resolves as a death rather than leaving him alive and
stuck") false by construction. The start-state knob (how much air, where you start) changes what
ROW 1's "the same starting condition" means on the second launch. ROW 4 names this hazard for
itself and ROW 9(b) pins its test to "the DEFAULT knob values, pass rule committed before the
sitting" — which is precisely why the absence in ROWS 1, 3, 13 and 15 is a defect and not a
generous reading.
*Hits:* ROWS 1, 3, 13, 15. *Routes to:* `converge`.

**F7 — the extended legibility deck can fail a build that is exactly right, by committing the
collapse the same row warns about.**
The new half is "shuffled stills and 3–4 s clips taken from OUTSIDE a pocket; the outsider names
the kind for each frame … FAIL if the outsider cannot beat chance on frames shot from outside." It
never says the pocket is **breached**. An unbreached pocket seen from outside must, by this file's
own signed distinction, be unnameable — before the cut the mark tells SIZE, not KIND — so a deck
assembled from such frames fails a build for having exactly the property the file calls
load-bearing. ROW 13(d)'s OBSERVABLE says "before entering it", and a pocket you have not breached
you have also not entered, so the wrong reading is the literal one. This is the criterion-2 /
criterion-7 collapse that ROW 14 warns "will produce a false finding" — committed by the file's own
newest instrument.
*Hits:* ROW 13(d). *Routes to:* `converge`. (The mixed-region hazard in the same deck is a
CONFIRMATION — ROW 13 already names it.)

**F8 — three discrimination tests are stated as statistics with no sample size and no decision
rule, while claiming their pass rule is committed before the sitting.**
ROW 9(a): "PASS if he orders them correctly and consistently". ROW 13(d): "FAIL if the outsider
cannot beat chance". ROW 14: "PASS if he beats chance on BOTH questions". With two kinds the
baseline is one half, and none of the three says over how many frames or at what margin — so seven
of ten reads as a pass to one administrator and a fail to another. §14's original rule was
deliberately MECHANICAL (any inconsistency within the sitting = FAIL) and needed no statistics at
all; every statistical half added since carries none. A pass rule that is not stated cannot be
committed before the sitting, which is the property that made §14 the strongest instrument in the
record.
*Hits:* ROWS 9, 13, 14. *Routes to:* `converge`.

**F9 — ROW 10's instruments cannot observe the ban the row says they close.**
The row states "A document plus the knob list closes the ban." Neither can see what the simulation
reads. Test (a) checks whether a person can predict from a **stated** rule — it measures the rule's
legibility, not the code's inputs. Test (c) checks a knob list for the player's name — a list is
not an implementation. Test (b) (breach, leave the area, return) cannot detect a player-dependent
term that is symmetric or that acts while he is present. The one re-import construct that survives
every criterion — a runtime mutation of POROSITY, established first-hand 2026-07-27 (V3) — is
invisible to all three, and a per-actor, player-position consumer already ships (`PlayerSense`).
Criterion 4 is the flattest ban in the card («Ни одно движение вещества не вычисляется из положения
игрока») and it has the weakest instrument in the set.
*Hits:* ROW 10. *Routes to:* `converge`.

**F10 — ROW 9's K1/K3 coupling names the wrong second variable, so its remedy secures nothing.**
The coupling finding is real and was new; its terms are wrong. Time to death is capacity ÷ (base
rate × per-kind multiplier). What decides whether the first breach kills is air **capacity** —
which ROW 15 has just called one of the two most load-bearing numbers in the whole rig — together
with the base rate, not the per-kind GAP that K3 names. The stated remedy, "either K3 carries an
upper bound, or the rig must demonstrate the property across K3's range", leaves capacity free, so
demonstrating across K3's range at one capacity proves nothing. «Первое вскрытие не убивает
мгновенно» remains a property of a pair, and it is not the pair the row names.
*Hits:* ROW 9. *Routes to:* `converge`.

**F11 — ROW 16 adds late-join acceptance that no owner text contains.**
Criterion 11 and core line 16 require exactly two things: the owner starts alone, and he starts
with a partner on a second machine. ROW 16's OBSERVABLE adds "A player who joins after digging has
begun sees the section as it is, not as it was", and its FAIL list makes "a join that shows an
un-dug section" a failure. That is snapshot join — a distinct engineering commitment, booked in the
previous converge's build order under "Lockstep for real", item 6 of seven — inside a set whose
governing rule is «минимальный набор с возможностью расширения». It may be right; it is not his.
*Hits:* ROW 16. *Routes to:* `converge`, which either strikes it or raises it as an owner question.
This leg does not decide it and does not open it.

**F12 — ROW 6's test (b) rests on a level property no row requires.**
Its pass condition is "PASS if it is possible … **and if it visibly costs more time than a route
through one**". That is true only if pockets are placed so that routing around them is dearer — a
property of the authored section, which ROW 12's FORCES leaves entirely to level authoring and
which §KNOBS does not constrain. A build satisfying ROW 6 exactly can fail test (b) on the section
it happens to ship with, and the row treats the result as a verdict on the verb.
*Hits:* ROW 6. *Routes to:* `converge`. (Test (a) — two outsiders cutting different routes — is a
CONFIRMATION, not a finding: the row itself says test (b) is the only instrument that decides
criterion 2's negative half. It is noted only because (a) is written with a PASS/FAIL rule and will
be cited as evidence for something it does not measure.)

**F13 — §WHAT declares two governance clauses covered by build rows.**
§WHAT handles criterion 15 correctly: no row by design, "a governance clause about the direction,
not a property of the build", held by `NOW.md`. Two clauses of the same class are recorded as
fully covered with "—": criterion 11's «Сетевая основа не откладывается — кооператив идёт сразу за
первым играбельным ядром, а не после игры», mapped to ROW 16; and criterion 14's «Участок остаётся
частью пути демо, а не отдельной игрой», mapped to ROW 17. Both rows' observables are properties of
a built artifact and neither can hold an ordering commitment. The consequence is concrete: the
clause whose entire job is to stop co-op being deferred is unmapped, so a `shape` leg could defer
ROW 16 to a later bet and nothing in the row set would catch it.
*Hits:* §WHAT. *Routes to:* `converge`.

**F14 — ROW 1's relaunch clause and ROW 5's start-state knob contradict each other, and
configuration is nowhere separated from run state.**
ROW 1 requires a relaunch "in the same starting condition", and its FORCES answers the previously
open clause with "**nothing persists across a run at all**". ROW 5 requires that changing a knob
and restarting produces a run in which the change is visible — so knob values must persist across
exactly the boundary ROW 1 says nothing crosses — and the start state (how much air, where you
start) is one of those knobs, so two launches need not start in the same condition at all. Both
sentences are amended text of 2026-07-28. §GLOSSARY signs neither the boundary nor «уровень» (F2),
so nothing scopes ROW 1's clause to fixed knob values.
*Hits:* ROWS 1, 5, §GLOSSARY. *Routes to:* `converge`.

**F15 — a class neither oracle carries: the machine budget, and the seam where the rig meets the
network.** (competing class C4)
The owner's own third authoring constraint is «не требует ядерного реактора» — one of exactly three
things allowed to shape a requirement. No row states that the built core must run at an interactive
rate on his machine, and §WHAT does not name it as uncovered, so the one runtime-cost constraint he
imposed himself is absent from the instrument. §KNOBS then makes **the simulation step rate** a
knob reachable from the built player, and ROW 16 requires two machines to agree about a world they
both mutate — yet §SEAM enumerates four seams, calls them all decidable now, and does not include
the fifth: who owns a knob's value in a two-machine session, and whether the step rate must be
identical on both. Every other seam in §SEAM is a seam of the substance; the rig is the one system
the amendment made load-bearing and it has no seam at all.
*Hits:* §WHAT, §KNOBS, §SEAM. *Routes to:* `converge` (naming), `shape` (price).

**F16 — the acceptance labour the rows create is unpriced and unassigned.** (competing class C6)
Eleven of the sixteen live rows require a person outside development, and four of those require a
deck or a pass rule committed to the repository before a sitting (ROWS 9, 13, 14, 15). Only
criterion 7 asks for an outsider at all («Проверяется на человеке, который не участвовал в
разработке»). The developer is one person; CHARTER lens 3 asks exactly this question of every large
decision; and §BUILD-CLOSES-BETTER lists which rows a build closes without ever saying who runs the
sittings, how many sittings there are, or when the decks are built. This is the same class of item
as the venue and the parameter home, which the file itself routes as needing a price rather than a
verdict — and it is the third of them.
*Hits:* the row set as an instrument. *Routes to:* `shape`.

## dropped as refuted or already named

Seven raw findings did not survive the self-refutation pass and are recorded so they are not
re-raised cheaply.

1. *«ROW 6 test (a) cannot fail».* REFUTED as stated — it fails for a level that forces one
   corridor. What survives is the narrower F12 note.
2. *«ROW 13's deck cannot handle mixed regions».* ALREADY NAMED by ROW 13 ("KNOWN PLACE WHERE (b)
   AND (d) CAN DISAGREE, named and not fixed"), with the first-hand receipt. CONFIRMATION.
3. *«ROW 12 fails by construction because sources are infinite».* ALREADY NAMED by ROW 12 and
   routed to step 4. CONFIRMATION.
4. *«ROW 1's venue has no price».* ALREADY NAMED by ROW 1 and by `i-card-clauses-unverifiable`.
   CONFIRMATION.
5. *«ROW 11's OBSERVABLE leans on the parked cell-versus-column question».* Refuted as a finding:
   the row names the dependency in FORCES and routes it, and all three of its tests hold under
   either answer.
6. *«The base is a sink and breaks ROW 11».* REFUTED: «вещество» is signed as the two gases, the
   base restores the player's counter, and the row's own note already reserves the question for
   the day air is modelled as a species.
7. *«ROW 17 lets the node close before any row's test is run».* REFUTED: criterion 14 states when
   the RESULT is achieved; it does not waive the other fourteen criteria, which `review` still
   needs evidence for. Criterion-level attack, and out of scope besides.

Also checked and NOT found defective: §SEAM items 1–3; the retirement of ROW 7 and its accepted
playtest effect; ROW 8; the §OWNER section, which was not attacked at all; §BUILD-CLOSES-BETTER's
split; and the K5 / K2 / K4 registrations against the owner's settings register.

## state_changes

1. `NOW.md` — `updated:` to this leg. `direction_forecast.drivers[1]` (the verification-verdict
   slot, previously the 2026-07-27 verdict, whose four numbered items are all closed) replaced with
   this verdict and its route; driver count stays four and `drivers[0]` gains one leading sentence
   pointing at it. `i-engine-fit-decision-ladder.review_when` gains one sentence: step 3 FAILED, so
   step 5 still waits, and F1, F2 and F15 are the only findings that could move step 4's price;
   the concurrent ecosystem-research text committed at `6fba9a55` is preserved verbatim.
   `i-card-clauses-unverifiable` extended: the rig's price depends on which reading of «спорное
   число» binds (F1), the build must contain a second level or the criterion-12 control is not
   satisfiable (F2), and the acceptance labour is a third unpriced item of the same class (F16).
   One issue added, `i-core-acceptance-instrument-unpriced`, for what outlives the repair.
   `open_calls`: the spent verification row replaced by exactly one
   `c-converge-g-37a1-core-rows-repair-001`. `d-post-verify-route-001.progress` records the FAIL
   and the narrow rerun without reordering his route. `bet: null`, `tasks: []`, `recurring: []`
   unchanged. No new decision was opened.
2. `work/c-converge-g-37a1-core-rows-repair-001-call.md` — CREATED. The bounded `converge` CALL
   carrying the sixteen findings, what may not be reopened, and the instruction to repair the
   named sections only.
3. `work/c-converge-verify-g-37a1-core-rows-001-call.md` — `disposition:` block appended:
   discharged, FAIL, receipt, successor.
4. `LOG.md` — one line appended.
5. `history/2026-07-28-s-converge-verify-g-37a1-core-rows-001.md` — this RESULT.

Not touched: `CHARTER.md`; `TREE.md` and both cards, which carry his verdict on exact text;
`knowledge/**`; the target `work/converge-g-37a1-core-rows.md`, which a verifier never repairs;
`work/core-requirements-g-37a1.md`; the product repository, which was not opened; Steam state;
`archive/**`. No `§SIGNOFF` line was written. No shape, bet, task, track or executor CALL was
created. No numeric release chance was published. No owner decision was answered, reopened or
manufactured, and `d-air-counter-visibility-001` stays open and unanswered.

## captures

- **THE COMPETING CLASS LIST, authored from first principles before the attack, proposed for
  promotion alongside `work/oracle-first-playable-core-v1.md` when `review` or `pulse` takes it
  up.** Its axis is deliberately different from the author's fourteen: not what the simulation must
  decide, but what must be true for one person to USE the artifact daily.
  **C1 — the authored content and its authoring loop.** Who authors the section, in which tool, at
  what cost, and how a change to it is made without a rebuild. *Breaks:* the parameters and the
  content get confused, and the rig quietly becomes a generator. *Precedent:* PEAK bakes fourteen
  maps in the Editor because they could not generate on the player's machine; Deep Rock Galactic
  pulled authorial control back from free-form generation. → produced **F1**.
  **C2 — the acceptance instrument's own falsifiability.** For each acceptance test: what sample,
  what threshold, committed when. *Breaks:* the test is argued after the sitting. → sharpened
  **F8**.
  **C3 — the default configuration under which acceptance is judged.** Which switches are off, and
  which tests are void if they are not. *Breaks:* a debug affordance silently passes a test. →
  sharpened **F6**.
  **C4 — the machine budget and the runtime cost ceiling.** What the core may cost per frame, on
  whose machine, and which knobs can move that. *Breaks:* the only cost constraint the owner
  imposed himself is never checked. → produced **F15**.
  **C5 — configuration versus run state.** What survives a death, a restart, and a relaunch, and
  where it lives. *Breaks:* "repeat launch works" and "tuning survives" are stated as absolutes
  that contradict. → sharpened **F14**.
  **C6 — who runs acceptance, and what it costs a solo developer.** How many people outside
  development, how many sittings, prepared by whom. *Breaks:* the acceptance plan is unexecutable
  by the team that exists. → produced **F16**.
  Three of the six found nothing: authority-and-replication, the non-model's seam and the verb's
  footprint are all covered by the rows.
- **The settings register in `work/core-requirements-g-37a1.md` still reads «Цена смерти (сейчас
  ноль)»** while the card now makes death cost the entire excavation of the run and ROW 4 uses K2
  as "a price beyond the run". Not a finding against the rows — ROW 4 uses it correctly — but the
  register is what a `shape` leg reads, and it is stale on the one line the amendment made most
  load-bearing.
- **The pattern behind seven of the sixteen findings is the same one, and it is worth naming for
  the next amendment of any converged artifact:** a claim established against text A is carried
  forward when text A is amended into A′, without re-deriving it. F5 (the invisible-counter claim),
  F6 (the default pin), F7 (the deck's frames), F10 (the coupling's terms) and F14 (persistence)
  are all instances. The glossary's own term count going stale by one in the same edit is the
  cheapest possible symptom of it.

## decisions_needed

None. `converge-verify` routes findings and decides nothing, and no finding here requires an owner
verdict to be repaired. F11 is the only one that may need his word, and the decision about whether
it does belongs to the `converge` that receives it, not to this leg.

## play_check

- `verify_target: build` — complete=FAIL smuggling=FAIL.
- **Step 1, recite the claim:** done. Both build attacks were named before the attack ran: (1) the
  question set is complete; (2) no answer leans on an unresolved question.
- **Step 2, attack completeness with an INDEPENDENT oracle:** done. The author's own oracle is a
  CANDIDATE the target sweeps itself against, so it could not be the only instrument; six competing
  classes were authored from first principles and shipped precedent BEFORE the attack, are recorded
  in `captures`, and are proposed for promotion. The empty-oracle block did not fire. All fifteen
  criteria were split atomically — ninety-one atoms — and mapped independently of §WHAT. Uncovered
  clauses and classes are NAMED and NOT filled; four hazards the target already names are reported
  as confirmations rather than findings.
- **Step 3, attack smuggling:** done. Every weight-bearing acceptance line was traced to owner text,
  to a first-hand receipt, or to nothing: ROW 16's late-join traces to nothing (F11), §KNOBS'
  «спорное число» traces to a property the file's own §GLOSSARY does not sign and its own
  TERM-PROPERTY RULE requires re-signing (F1), and ROW 10's closure instrument traces to a document
  that cannot observe the thing banned (F9). No engineering fact was re-derived: the product
  repository was not opened and every engine claim is carried by its existing 2026-07-27 receipt.
  No leaked HOW magnitude needed moving to `→ PLAN`; the rows already route their HOWs.
- **Step 4, close:** FAIL. No `§SIGNOFF` was written. Named rows return to `converge` by one
  bounded CALL; F16 and F15's price half are marked for `shape`. Verification reruns narrowly on
  what the repair changes.
- **Owner-verdict guard:** no owner verdict was invented, inferred or needed; the three answered
  forks were not attacked; the eleven closed rulings the CALL lists were not re-litigated; no row
  was asked to prove the core is fun; no knob's value was attacked and no row was scored incomplete
  because a value is unchosen.
- **Two-strikes:** no finding here repeats a finding of `s-converge-verify-g-37a1-digging-card-001`
  against the same target — that leg attacked `work/converge-g-37a1.md` and the twelve-criterion
  card, and its nine findings are all either closed or carried by live issues. `KERNEL` §2
  two-strikes is not live.
- **G-gates:** G1 untouched (`bet: null`, no lane created). G5 honoured — this is the binding fresh
  refutation session for the rows, single-agent, in a chat separate from the one that wrote them.
  G7 — no decision opened. G9 — no card, no CHARTER, no `knowledge/` written. G10 — steps exposed,
  RESULT final, state changed only by applying this RESULT.

## log

2026-07-28 | s-converge-verify-g-37a1-core-rows-001 | converge-verify | direction |
g-37a1/core-rows: the rows FAILED independent verification against the fifteen criteria with
sixteen findings and no signoff — not one requirement line is challenged, and the two places the
CALL said to push hardest both broke.

## next

`c-converge-g-37a1-core-rows-repair-001` — a bounded `converge`, no owner, that repairs only the
named sections (§KNOBS, §RULES, §GLOSSARY, §WHAT, §SEAM and ROWS 1, 5, 6, 9, 10, 12, 13, 14, 15,
16, 17) and fills nothing beyond them. Then this verification reruns narrowly on what changed.
Step 5 — his ladder verdict — still waits on step 3 by his own route, and F1, F2 and F15 are the
only findings that could move step 4's price.

END_OF_FILE: live/indie-game-development/history/2026-07-28-s-converge-verify-g-37a1-core-rows-001.md
