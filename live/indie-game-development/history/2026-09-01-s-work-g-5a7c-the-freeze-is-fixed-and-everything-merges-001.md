# RESULT — s-work-g-5a7c-the-freeze-is-fixed-and-everything-merges-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-he-never-stands-where-he-cannot-leave-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — замирание починено в найденном месте, и обе половины дня впервые сливаются начисто: 752 из 752, ноль красных; публикация выдана

close: light — because всё перевыведено направлением первой рукой на байтах: коммит и его родитель, единственность механизма и удаление старого, отсутствие телепорта прочитано в коде, удаление подпорок доказано поиском, прогон тестов сделан направлением дважды — на ветке и на полном слиянии в выброшенной копии. Живой прогон читался как улика исполнителя и подпёрт независимым чтением механизма.

owner_approved: |
  Задача заведена взамен `t-a-fact-knows-who-made-it-1` внутри его же «берём четыре» от 2026-09-01.
  **Замах не менялся: четыре задачи, одна вместо другой.** Отдельного слова на замену предмета
  внутри волны не требуется — приёмка задачи машинная.

  **Публикация его слова ТРЕБУЕТ:** строка приёмки `t-everything-lands-1` прямо говорит, что он
  видит всё сразу и снимается это его глазом. Наряд это повторяет.

outcome: |
  **ЗАМИРАНИЕ ПОЧИНЕНО, И ПОЧИНЕНО ИМЕННО ТАМ, ГДЕ БЫЛА ПРИЧИНА.**

  Перевыверено направлением на `c060052e` (родитель — TTL-коммит `4ecc2f82`, не опубликован):

  - **ОДИН ОТВЕТЧИК, И СТАРЫЙ УДАЛЁН.** `HouseWays.TryStandingAt` отвечает за цель (`:268`),
    за **фактический старт** (`:267`) и за промежуточные названные места (`HouseWays:61`).
    **Поиск по `Standable` во всём продукте — ноль совпадений: старая функция не осталась рядом,
    а заменена.** Порог смерти волны выдержан буквой;
  - **ТЕЛЕПОРТА НЕТ, И ЭТО ПРОЧИТАНО, А НЕ ПРИНЯТО НА СЛОВО.** `var startsWithEscape =
    !SameFloorPoint(start, from);` и затем `if (startsWithEscape) Append(direct, start);` —
    безопасная точка становится **первым waypoint**, положение хозяина не переписывается.
    **Из ямы он выходит шагом.** Сырая цель при этом сохраняется на пути;
  - **ПОДПОРКИ ПЕРВОЙ ПОПЫТКИ УДАЛЕНЫ.** Поиск по `HouseholderRoutineState.cs` на постоянный
    чёрный список и счётчик в двести тиков — **ноль совпадений**. Нога сама выбросила то, что
    написала, когда нашлась настоящая причина;
  - **ПРОГОН НАПРАВЛЕНИЯ: `749 / 748 / 1`**, единственный красный — чужой про слух, живущий на
    другой половине; **пропусков и заглушек в обоих тронутых тестовых файлах — ноль**;
  - слот **освобождён**, дерево чистое, ничего не опубликовано.

  **И ГЛАВНОЕ ЧИСЛО ДНЯ: ОБЕ ПОЛОВИНЫ СЛИВАЮТСЯ НАЧИСТО.** Направление собрало полное слияние
  `slot/win-u2` + `slot/win-u4` в отдельной выброшенной копии, ничего не коммитя:

  ```
  merge-tree               exit 0 — конфликтов нет
  профиль                  routine 6 · handlers 7 · occupations 4 · gauges 1
  ТЕСТЫ                    752 / 752 / 0, пропущено 0
  ```

  **НОЛЬ КРАСНЫХ.** Тот единственный тест, что весь день держал публикацию, гаснет ровно этим
  слиянием. **Дерево, на которое владелец может посмотреть, существует впервые.**

  **ЗАДАЧА, ПОД КОТОРОЙ ЭТО ДЕЛАЛОСЬ, ЗАКРЫТА КАК СНЯТАЯ, А НЕ КАК СДЕЛАННАЯ — И ЭТО ВАЖНО.**
  `t-a-fact-knows-who-made-it-1` требовала, чтобы факт знал своего производителя. **Этого не
  построено и построено не будет этой ногой:** `SourceId` называет предмет, а не действующее лицо,
  и честная починка проходит через границу другого слота. Закрыть её «сделанной» значило бы
  подписать то, чего нет. **Вместо неё в волне стоит задача про то, что действительно построено**,
  со своими строками приёмки. Замах остался четырьмя задачами.

  **ПУБЛИКАЦИЯ ВЫДАНА, И ЕГО СЛОВО В НЕЙ — УСЛОВИЕ, А НЕ ФОРМАЛЬНОСТЬ.** Наряд прямо запрещает
  публиковать до того, как он посмотрел, и требует передать его слова дословно. **Его взгляд на
  этом дереве закрывает сразу три задачи двух волн** — утро, руки и публикацию, — и все три ждали
  не работы, а дерева.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** на `slot/win-u4` = `c060052e`:

  - `git rev-parse c060052e^` → **`4ecc2f82`**: TTL сохранён родителем;
    `git branch -r --contains c060052e` → **пусто**;
  - `git show --stat` → **шесть файлов**, четыре исходных и два тестовых, `+864 −104`;
  - `grep "TryStandingAt"` → одна реализация в `HouseWays` и вызовы для `from`, цели и
    промежуточных мест; `grep "Standable"` по `Assets` → **ноль**;
  - `HouseWalk.cs:272-288` — `startsWithEscape` и `Append(direct, start)`: **безопасная точка
    прибавляется первым waypoint**, а не подменяет положение;
  - `grep "200\|blacklist\|banned\|forbidden"` по `HouseholderRoutineState.cs` → **ноль**;
  - **`dotnet test` направлением на ветке: не пройдено 1, пройдено 748, всего 749**, единственный
    отказ — `EitherMomentAnswersTheFirstNoiseAndTheSecondStillTakesHim`;
  - `grep -c "Ignore]\|Explicit]"` по обоим тронутым тестовым файлам → **0 и 0**;
  - реестр слотов: **все четыре AVAILABLE**, аренда снята.

  **ПОЛНОЕ СЛИЯНИЕ, собрано направлением в `GasCoopGame_testrun` и после замера отброшено:**
  `git merge-tree --write-tree slot/win-u2 slot/win-u4` → **exit 0**; материализованное слияние
  даёт профиль `6 / 7 / 4 / 1` и **`dotnet test` направлением: не пройдено 0, пройдено 752,
  всего 752, пропущено 0**. Копия возвращена на `0e72d610`, дерево чистое.

  Из возврата ноги, как улика: живые координаты за две контрольные минуты
  `(3.517,3.294)` → `(2.967,0.238)` → `(4.100,2.410)` → `(3.423,0.219)` → `(3.000,0.833)`,
  везде `Refusal=None`; `check.ps1` зелёный; независимое ревью без критических и важных.

state_changes: |
  СОЗДАНО И ЗАКРЫТО КАК СДЕЛАННОЕ (1 задача):
  - `t-he-never-stands-where-he-cannot-leave-1` → `done`: дом для того, что действительно
    построено, со своими семью строками приёмки.

  ЗАКРЫТО КАК СНЯТАЯ (1 задача):
  - `t-a-fact-knows-who-made-it-1` → `dropped`: предмет оказался не тем, что держало всё видимое;
    собственный шум живёт уликой `i-sourceid-names-the-thing-not-the-doer-001`.

  ЗАКРЫТО КАК СДЕЛАННЫЙ (1 наряд):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-005` → `done`.

  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-everything-lands-1-001` → слот **WIN-U4**;
  - `work/2026-09-01-call-everything-lands.md`.

  СТАТУС:
  - `t-everything-lands-1` → `active` (была `open`).

captures: |
  - **НОГА ВЫБРОСИЛА СОБСТВЕННЫЕ ПОДПОРКИ, КОГДА НАШЛАСЬ НАСТОЯЩАЯ ПРИЧИНА.** Постоянный чёрный
    список точек и счётчик в двести тиков были написаны ею же в первой попытке. **Проверено
    поиском: их нет.** Это дороже самой починки: обычно написанное защищают.
  - **ЗАДАЧА ЗАКРЫТА СНЯТОЙ, ХОТЯ РАБОТА СДЕЛАНА, — И ЭТО ЕДИНСТВЕННЫЙ ЧЕСТНЫЙ ВАРИАНТ.** Её
    строки требовали, чтобы факт знал производителя; этого нет. **Правило-кандидат: когда нога
    находит и чинит не то, что записано в задаче, работа получает СВОЮ карточку, а старая
    закрывается снятой — иначе в истории остаётся подписанным то, чего не строили.**
  - **ПРИЧИНУ ИСКАЛИ ПЯТЬ ЗАХОДОВ, А ПОЧИНИЛИ ЗА ОДИН.** Слепота, руки, владение фактом, срок
    ответа — четыре неверных диагноза; пятым стал замер достижимости с **обратным путём** в списке,
    и он решил всё за один заход. **Стоимость лежала в постановке замера, а не в починке.**
  - **ДЕРЕВО, НА КОТОРОЕ ВЛАДЕЛЕЦ МОЖЕТ ПОСМОТРЕТЬ, НЕ СУЩЕСТВОВАЛО ДО СЕГОДНЯ НИ НА ОДНОЙ ВЕТКЕ.**
    Три задачи двух волн ждали не работы, а его. **И это заметили только когда искали работу для
    свободного слота.**

decisions_needed: |
  **ОДНО, И ЭТО ЕГО ГЛАЗ.** Публикация не идёт без его слова: он открывает дерево и говорит, видит
  ли он то, что должен. Его взгляд закрывает три задачи сразу.

play_check: |
  1. OPEN — возврат ноги с починкой и освобождённой арендой.
  2. WORK — единственность механизма, удаление старого, отсутствие телепорта и удаление подпорок
     перевыведены первой рукой; тесты прогнаны направлением дважды — на ветке и на полном слиянии;
     задача закрыта снятой, а построенное получило свою карточку.
  3. (owner) — не запрашивался на замену предмета внутри волны; **его слово требуется на
     публикацию и запрошено нарядом.**
  4. CLOSE — RESULT, применение, коммит; наряд на публикацию уходит владельцу.

log: |
  замирание починено в найденном месте: один ответчик на «может ли он тут стоять» отвечает за оба
  конца пути, старая функция удалена, телепорта нет — безопасная точка становится первым waypoint,
  и из ямы он выходит шагом; нога выбросила собственные подпорки, когда нашлась настоящая причина;
  и главное число дня — обе половины сливаются начисто в 752 из 752 при нуле красных, то есть
  дерево, на которое владелец может посмотреть, существует впервые; задача закрыта СНЯТОЙ, потому
  что её предмет так и не построен, а построенное получило свою карточку

next: |
  Наряд `c-exec-g-5a7c-everything-lands-1-001` уходит владельцу для **WIN-U4**: собрать слияние,
  прогнать живьём, позвать его — и **опубликовать только после его слова**.

  **Его взгляд закрывает сразу три задачи двух волн:** утро, руки и публикацию.
