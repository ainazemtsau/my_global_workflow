# RESULT — s-review-g-5a7c-wave-8-close-001

direction: indie-game-development
play: review
node/task: g-5a7c / bet-g-5a7c-wave-8
track: —
date: 2026-08-30
outcome_kind: review — восьмая волна закрыта вердиктом `partial`, полосы распущены, и найдена механическая причина, по которой ни одна живая задача не доезжала до нарезки

owner_approved: |
  Закрытие волны разрешено им заранее и дословно: **«да, закрывай восьмую волну»** (2026-08-30, в
  ответ на батч ремонта, где было сказано «закрытие волны делаю сразу за ним» —
  `history/2026-08-30-s-repair-g-5a7c-records-catch-up-001.md`).

  Три распоряжения этой ноги показаны ему целиком, простым текстом, ДО первой записи: (1) закрыть
  три припаркованные задачи; (2) починить поле `_bet` и закрыть четырнадцать уже сделанных задач;
  (3) записать противоречие по нити решением и не трогать узел. Его слово: **«Всё остальное
  принимаю твои рекомендации»**.

  По нити отдельно, и это его же поправка к моему вопросу: **«понятие там это вот условно, да, как
  полярное сейчас то, что сделано, там надо будет доделывать, но я сейчас пока ещё в Concept Lab
  это проговариваю. То есть условно потом, когда мы к ней придём, я скину уже детальную спеку»**.

  **Узел `g-5a7c`, `CHARTER.md` и реестр `owner_approved` этой ногой НЕ изменены.** Расхождение
  критерия 3 узла с более поздним экспортом названо и оставлено ему — это G9 и отдельная нога.

outcome: |
  **ВОСЬМАЯ ВОЛНА ЗАКРЫТА ВЕРДИКТОМ `partial`, И ОБЕ ЕГО ПОЛОВИНЫ НАЗВАНЫ ЧЕСТНО.**

  Выжило много и оно проверено: девять задач `done`, каждая строка `done_when` перевыведена первой
  рукой из закоммиченных байтов, 716 headless-тестов зелёные (было 694, ни один тест не удалён),
  220/220 EditMode на слитом коммите. Хозяин выбирает реакцию по важности, а не по порядку в
  списке; мир подаёт ему метки на входе тика; в его убеждения пишут ТОЛЬКО его глаза; дело
  кончается меткой и переоткрывается миром; мебель попала в план, и он перестал ходить сквозь неё;
  полигон позволяет прятать и двигать вещи, не входя за мышь; 27 коммитов из трёх слотов сведены в
  одно компилирующееся дерево.

  **Не выжил `done_when` самой ставки, и это не придирка к формулировке.** Её `goal` обещает «утро
  кончается словами УСПЕЛ или ОПОЗДАЛ, нить тянет, а владелец может пробовать И ТО И ДРУГОЕ
  руками». Конца утра нет: `grep round|leave|allChores` по ядру даёт **0**, день по-прежнему
  зациклен арифметически. Нить не тронута — её полоса выведена из волны 29 августа. И третье,
  которое важнее первых двух: **гипотеза, ради которой волна была перерезана, не проверена его
  руками ни минуты.**

  **ВОЛНА ВЫДАЛА МАШИНУ И ОСТАВИЛА ПРОВЕРКУ.** Порог смерти 5 этой же ставки написан ровно про это
  — «владелец сел в свежую сборку и не увидел разницы → волна не закрыта, что бы ни было
  зелёным». Он не сработал не потому, что разница есть, а потому что он не садился. Его слова
  2026-08-30, уже после сдачи: «я хочу увидеть полигон сначала, что он просто делает» и «я сейчас
  в твоих полосах не вижу, чтобы мы остальные вещи протестировали». Девятая волна его же словом
  перекроена под наполнение и проверку — то есть недостающая половина восьмой волны стала
  заданием девятой, а не потерялась.

  **ГЛАВНАЯ НАХОДКА НОГИ — НЕ ПРО ВОЛНУ, А ПРО ТО, ЧЕМ ЕЁ НАРЕЗАЮТ. НИ ОДНА ЖИВАЯ ЗАДАЧА НЕ
  ПОПАДАЛА В РАБОЧИЙ НАБОР — ВСЕ 28, ВКЛЮЧАЯ ДЕВЯТЬ ЗАДАЧ ИДУЩЕЙ ВОЛНЫ.** Улика
  `i-live-cards-point-at-closed-and-empty-bets-001` описала следствие и назвала его «двенадцать
  задач», а причиной сочла членство в ставке. Замер первой рукой показал другое: `osctl context`
  берёт задачу условием `_bet == id ЦЕЛИ`, схема пишет `_bet: g-xxxx`, подсказка самого `check`
  пишет «`--value <id цели>`» — а 50 карточек несли `_bet: bet-g-5a7c-wave-N`, то есть id ВОЛНЫ.
  Совпадения нет ни у одной. Ремонт 2026-08-30 не стал это чинить, потому что счёл `_bet` записью
  в состав ставки; схема прямо говорит обратное — это МЕСТО карточки, «карточка без своего места
  выпадает из рабочего набора и просто теряется».

  Исправлено: всем живым задачам проставлено `_bet: g-5a7c`. Рабочий набор `osctl context --for
  g-5a7c` вырос с 7 файлов до 17, и одиннадцать оставшихся задач нарезка впервые ВИДИТ.

  **ЧЕГО ЭТА НОГА НЕ СДЕЛАЛА НАМЕРЕННО.** Не выбросила ни одной из одиннадцати: они несут замеры и
  его дословные слова, у которых нет второго дома — код подпола, находку про десять точек лута,
  стоящих в СЦЕНЕ, а не в префабе дома, строку 5 обработчиков, и его слово 2026-08-14 про
  присланные модели, которые всё ещё лежат в `main` 169 файлами. Состав девятой волны решает
  `shape` вместе с ним; обзор снял слепоту, а не принял за него решение.

evidence: |
  Продуктовый `main` = `dev` = `origin/main` = `c7e926cf`. Направление на `e95b390d` в момент
  старта ноги. Всё ниже перемерено этой ногой, а не взято из карточек.

  **ВЕРДИКТ ВОЛНЫ — ОПРОВЕРЖЕНИЕ ПО СТРОКАМ.**
  - Конца утра нет: `grep round|leave|allChores` → **0**;
    `Probe/Householder/HouseholderRoutineState.cs:272` — `nextStepIndex %= profile.RoutineStepCount`.
  - Нить не тронута: полоса выведена из волны 29 августа его словом, задача не запускалась.
  - Его руки: ни одного прогона владельцем не записано ни в одном отчёте волны; шаг «видно на
    стенде» задачи `t-chore-result-1` закрыт ПОШАГОВОЙ ПРИЁМКОЙ, написанной ДЛЯ него
    (`docs/results/c-exec-g-5a7c-chore-result-1-001.md:237-263`), а не его прогоном.
  - Что выжило: 716 headless (`dotnet test`, прогнан направлением на `c7e926cf`), 220/220 EditMode
    на слитом коммите, девять карточек задач со строчным перевыводом в журналах.

  **ПОЛОМКА `_bet` — ЧЕТЫРЕ НЕЗАВИСИМЫХ ИСТОЧНИКА, СОГЛАСНЫЕ ДРУГ С ДРУГОМ.**
  - `osctl.py:1035` — `if h.get("_bet") == node and h["_kind"] == "task"`, где `node` это id цели.
  - `os/schema/direction-files.md`, шаблон task — `_bet: g-xxxx`.
  - `osctl.py:1157` — подсказка `check`: «card set --field _bet --value <id цели>».
  - `osctl.py:361-367` — комментарий самого инструмента про этот класс: «заведённая по правилам
    задача не попадала в рабочий набор следующей ноги и просто терялась».
  - Счёт по всем карточкам задач: **28** несут `g-5a7c`, **50** несут `bet-g-5a7c-wave-N`,
    **5** не несли ничего. `osctl context --for g-5a7c` до правки: рабочий набор 7 файлов,
    строка «НЕ ВКЛЮЧЕНО … task 28». После правки: 17 файлов, одиннадцать задач внутри.
  - `check` до ноги — 66 замечаний, после — **61**, «механических проблем нет».

  **ПОЧЕМУ УЛИКА НАСЧИТАЛА ДВЕНАДЦАТЬ, А ИХ ОДИННАДЦАТЬ.** Пересчитано поимённо: шесть незакрытых
  задач на закрытых ставках (`t-cargo-delta-1`, `t-cargo-state-lifecycle-1`,
  `t-cargo-tuning-safety-1`, `t-hands-1`, `t-loot-models-1` на пятой волне; `t-irritation-1` на
  шестой) плюс пять без ставки вовсе. «Семь» в улике не воспроизводится ни при каком прочтении
  слова «живая»; это ошибка счёта, а не изменившееся состояние. Настоящее число всё равно больше:
  выпадали все 28.

  **ПЯТЬ НАРЯДОВ СО СТАТУСОМ `parked`** — статуса нет в схеме (`ready|running|waiting|blocked|
  paused`, `direction-files.md:166`); законный `paused` требует `paused_by` со ссылкой на его
  слово, которого нет. Все пять стояли открытыми на закрытых ставках, то есть дрейф по G2.

  **ЗАДАЧИ ТОЖЕ НЕСЛИ СТАТУСЫ ВНЕ СХЕМЫ, И ЭТОГО УЛИКА НЕ НАЗЫВАЛА.** Законный список —
  `open | active | blocked | done`; в живых стояли `parked` (пять) и `waiting` (шесть).
  Приведены к схеме.

  **НИТЬ: ДВА ПОДПИСАННЫХ ДОКУМЕНТА, ПРОЧИТАННЫЕ ЦЕЛИКОМ.**
  - `concept-lab/exports/EXP-20260829-001-…-thread-mechanic.md` — «Нить можно **непрерывно
    стягивать**»; его утверждение «их можно брать как утверждённые так как я их передал dev team».
  - `concept-lab/exports/EXP-20260830-001-…-charged-polar-impulse.md` — «Это не трос. Это не
    лебёдка. Это **не непрерывный луч**»; его утверждение «я хочу, чтобы ты именно этот вариант
    сохранил, я его подтверждаю».
  - Оба несут в шапке `"Экспорт инертен: он не применяется к карточке автоматически"`, и оба
    отсылают разрешение расхождений к его отдельному слову, а не к дате файла.
  - `concept-lab/STATE.md` на 2026-08-30: у `CAND-0010` следующий шаг — «принести владельцу на
    вычёркивание один конкретный компактный контракт». Лаборатория их ещё не свела.

  **ДВЕ РАБОТЫ, ЖДУЩИЕ ЗАПУСКА, ПЕРЕПРОВЕРЕНЫ ПЕРВОЙ РУКОЙ, А НЕ ПРИНЯТЫ СО СЛОВ.**
  - `tools/close-leg.ps1:257` зовёт `unity-tests.ps1` (EditMode), затем `check.ps1 -Deliver`
    (строка 310). Строки `dotnet test tests/TunnelCrew.Core.Tests` в закрывальщике **нет**, и
    продуктовый `AGENTS.md` прямо пишет, что headless-тесты не входят в `-Deliver`. Гейт видит
    220 и не видит 716.
  - Сторожа против глухоты живут в `tests/TunnelCrew.Core.Tests/HouseholderChoreTests.cs`,
    строки **212, 315, 565** — то есть ровно в тех 716.

  **ЛИНЗЫ УСТАВА, ПО ОДНОЙ СТРОКЕ НА КАЖДУЮ.**
  1. *Демо и выпуск* — `not_needed`: фест исключён его прямым словом, волна внутренняя. Не
     сдвинулось ничего, и это было решено заранее.
  2. *Понятность игроку* — **сдвинулось**: намерение хозяина впервые читается наружу (кандидаты и
     победитель выходят из пайплайна), и «спрятал кружку → пошёл за второй» существует. Но заход
     по-прежнему ничем не кончается: проиграть или выиграть нельзя.
  3. *Потянет ли один* — волна прошла в три полосы вместо пяти и свела 27 неслитых коммитов.
     Самая дорогая её часть оказалась не стройкой, а КРУГОМ ПРОВЕРКИ: первая версия починки была
     неверна, и это нашли не тесты.
  4. *Новый канон* — MAKE HIM LATE выдержал везде, где его трогали. Пришёл новый вход содержания
     от владельца (три `idea`-карточки и одно знание) и **отменил уже написанный наряд** — это
     первый случай в узле, когда канон опроверг задачу до её запуска.
  5. *Платный продукт* — `not_needed`, отложено его словом.
  6. *Надёжность работы* — слияние закрыло реальный риск, но **восемь закрытий из девяти не
     оставили собственного RESULT в `history/`**: свой отчёт написал только ресёрч архитектуры,
     остальное восстановлено двумя ремонтами по продуктовым байтам. Класс
     `i-thirteen-closes-left-no-result-in-history-001` отработал ещё раз, на всю волну.
  7. *Проще или сложнее* — **проще**: третий механизм не написан, заменить-бросить-дожать вышли из
     одной формы; девять пунктов выреза удержаны.

  **СЮРПРИЗ ПРОТИВ СОБСТВЕННОГО ПРОГНОЗА ВОЛНЫ.** Прогноз назвал самым ранним сигналом компиляцию
  слитого дерева и отдал ей судьбу волны целиком. Сигнал пришёл зелёным и оказался не тем риском.
  Настоящий риск нашла независимая состязательная проверка: открытое дело отвечает КАЖДЫЙ тик, и
  каталог реакций перестал спрашиваться вовсе — `carry-tea` побеждал `seen-rat`, хозяин проходил
  мимо крысы в открытую при 716 зелёных тестах. Слово веса, добавленное задачей приоритета ровно
  против такого класса, помочь не могло: обработчик и строка каталога не встречались в одном споре.

  **ДОБАВКА НАЗАД (шаг 4 плея).** Вырез 3 — «четыре решения хозяина: дожать, заменить, бросить,
  защитить» — оказался **робким на три четверти**: три из четырёх приехали ДАРОМ из формы в этой
  же волне, и вырезать их было не нужно. Осталось одно — «защитить», и аудит называет его РЕЖИМОМ,
  а не эпизодом. Пропущенного выреза не найдено, но найдена не бесплатная парковка: три выведенные
  полосы оставили написанные наряды, у двух из трёх цель за сутки протухла, и карточки этих трёх
  нарядов несут в поле `repo` неподставленную переменную.

state_changes: |
  Всё через `osctl`, рукой не правились ни одна карточка. Руками — только `NOW.md`, `work/` и
  `knowledge/`, как и предписано адаптером.

  ЗАКРЫТО — ставка (2):
  - `bet-g-5a7c-wave-8` → `status: partial`, вердикт в журнале;
  - `bet-g-5a7c-wave-5` → перенесена в `closed/` (закрыта вердиктом 2026-08-27, а карточка лежала
    среди живых).

  ЗАКРЫТО — задачи (17):
  - три припаркованные задачи волны: `t-thread-pull-1`, `t-day-end-1`, `t-look-sound-1` →
    `dropped`, каждой своя причина и указатель на то, что несёт её содержание дальше;
  - четырнадцать задач со статусом `done`, лежавших среди живых, → `closed/`: шесть с пятой волны,
    шесть с шестой, две с восьмой (`t-arch-householder-1`, `t-merge-1`).

  ЗАКРЫТО — наряды (5): `cargo-delta-1-002`, `cargo-state-lifecycle-1-001`,
  `cargo-tuning-safety-1-001`, `irritation-1-001`, `interactive-density-001`. Все стояли статусом
  `parked`, которого у наряда нет, на закрытых ставках.

  ЗАКРЫТО — улики (2):
  - `i-live-cards-point-at-closed-and-empty-bets-001` — исполнена, диагноз при этом исправлен;
  - `i-osctl-cannot-link-a-new-task-to-its-bet-001` — протухла: `_bet` входит в `PLACEMENT`, и
    `card set --field _bet` отработал 28 раз подряд.

  ИСПРАВЛЕНО — место карточек (28): всем живым задачам `_bet: g-5a7c` вместо id волны/`null`.

  ИСПРАВЛЕНО — статусы и блокеры одиннадцати оставшихся задач: `parked`/`waiting` → `open`
  (девять) и `blocked` (две). Сняты блокеры, чьё условие выполнено или указывает в пустоту —
  в том числе `waiting_on: t-house-2 — карточки не существует` у `t-house-asset-1` и ожидания
  пятой волны у `t-basement-1` и `t-dropmodel-1`. Двум живым блокерам написано настоящее условие:
  `t-handlers-1` ждёт достижимого тихого шума, `t-hands-1` — решённой формы нити. У
  `t-irritation-1` снята полоса `словарь`: полосы шестой волны распущены.

  СОЗДАНО (4):
  - `d-which-thread-document-is-the-contract-001` — решение: какой из двух подписанных экспортов
    контракт нити; его слова внутри; рекомендация — ждать его детальную спеку;
  - `i-bet-placement-names-the-wave-not-the-node-001` — улика маршрута `maintenance`: `check`
    проверяет только НАЛИЧИЕ `_bet` и никогда не проверяет, что значение разрешается;
  - `knowledge/a-rule-that-answers-every-tick-silences-the-fallback.md` — класс отказа этой волны;
  - `c-shape-g-5a7c-wave-9-001` + `work/2026-08-30-call-shape-wave-9.md` — единственный наряд,
    который выдаёт этот обзор.

  ПЕРЕСЧИТАНО (1): `direction_forecast` — `as_of: 2026-08-30`, статус остаётся `no_basis`
  (эмпирического знаменателя нет), цель не тронута, пять драйверов переписаны от материальных
  доказательств. Темпом этой волны пользоваться нельзя, и причина названа: дыра в учёте не
  сузилась, а расширилась.

  РУКАМИ (1): `NOW.md` — `bet:` очищен, как это делает каждый обзор; `track_wip_limit: 4`
  сохранён.

  НЕ ТРОНУТО НАМЕРЕННО: узел `g-5a7c`, `CHARTER.md`, реестр `owner_approved`, оба экспорта
  Concept Lab, `_bet` в 50 закрытых карточках (для рабочего набора безразлично; следствие названо
  в улике).

captures:
  - Три карточки нарядов восьмой волны, лежащие в `closed/`, несут в поле `repo` буквальное `C:\projects\Unity` плюс неподставленную переменную — наряд, скопированный с них, назовёт несуществующий путь.
  - Столбец «Слот» в карточке волны врал по всем трём полосам и уже был исправлен ремонтом; вывод шире исправления — слот выводится из реестра лиз в день выдачи, а не копируется из прежней таблицы.
  - `osctl check` не проверяет вид `bet` вообще (находка ремонта) И не проверяет разрешимость `_bet` (находка этой ноги) — два разных пробела одного сторожа.
  - Одиннадцать живых задач впервые видны нарезке; из них `t-dropmodel-1` держит его прямое слово 2026-08-14, а работа не сделана: 169 файлов присланных моделей всё ещё в `main`.

decisions_needed:
  - q: Какой из двух подписанных владельцем экспортов является контрактом нити — EXP-20260829-001 («непрерывно стягивать») или EXP-20260830-001 («не непрерывный луч»)?  options: [ждать его детальную спеку, направление выбирает более поздний файл, переписать критерий 3 узла сейчас]  recommendation: ждать его детальную спеку — он сам сказал 2026-08-30, что доводит вариант в Concept Lab и скинет спеку; сами экспорты запрещают разрешать расхождение приоритетом более позднего файла. Карточка d-which-thread-document-is-the-contract-001. Ничего, кроме себя, этот вопрос не блокирует.

play_check: |
  1. Опровержение — сделано. `done_when` ставки не выжил в трёх местах из трёх, названная
     ценность выжила и перемерена. Вердикт `partial`. Прогноз и довод против сравнены с
     реальностью, сюрприз назван: самый ранний сигнал волны пришёл зелёным и оказался не тем
     риском.
  2. Урожай по линзам — сделано, вердикт по каждой из семи, включая «ничего не изменилось» там,
     где это правда.
  3. Диф дерева (owner) — сделано БЕЗ правки дерева. Узел, устав и реестр одобрений не тронуты;
     единственное расхождение (критерий 3 против экспорта) названо и оставлено ему. Его слова
     на три показанных распоряжения: **«Всё остальное принимаю твои рекомендации»**; на нить —
     **«я сейчас пока ещё в Concept Lab это проговариваю… потом скину уже детальную спеку»**;
     на само закрытие — **«да, закрывай восьмую волну»**.
  4. Добавка назад — сделано: вырез 3 был робким на три четверти, три решения приехали даром из
     формы. Пропущенного выреза не найдено; названа цена парковки трёх полос.
  5. Знание — одна запись, `a-rule-that-answers-every-tick-silences-the-fallback.md`, с реальным
     `read_by` и указателем на продуктовый отчёт. Остальное оставлено историей.
  6. Прогноз и следующее — прогноз пересчитан, остаётся `no_basis` (числа нет и не выводится).
     Узел один и без развилки: `g-5a7c` многоволновой его словом и остаётся `active`.
  7. Закрытие — сделано: ставка закрыта, ВСЕ полосы распущены (`ядро`, `дом`, `полигон` и
     выведенные `утро`, `нить`, `вид-звук`, `архитектура`; у последней живой карточки полоса
     снята), задач не заведено ни одной, наряд выдан ровно один.

next: |
  `c-shape-g-5a7c-wave-9-001` — нарезать девятую волну по четырём согласованным полосам
  (`починка`, `утро`, `почему`, `руки`), и первой её обязанностью стоит то, чего восьмая волна не
  сделала: владелец садится и видит, работает ли хозяин. Наряд целиком —
  `work/2026-08-30-call-shape-wave-9.md`.

  Плюс одна карточка ждёт его слова и не блокирует нарезку:
  `d-which-thread-document-is-the-contract-001`.

END_OF_FILE: live/indie-game-development/history/2026-08-30-s-review-g-5a7c-wave-8-close-001.md
