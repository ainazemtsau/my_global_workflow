# RESULT — s-repair-g-5a7c-lab-policy-unstop-001

play: repair · СТОП, объявленный часом ранее, был ошибкой ноги: она прочла документ мёртвой
концепции как действующую власть, хотя правило, запрещающее так делать, лежало в `knowledge/` и
называло `work` своим читателем. Блокировка снята, наряд дополнен настоящим требованием владельца
direction: indie-game-development · node: g-5a7c · track: householder · task: t-observe-1 ·
call: c-exec-g-5a7c-observe-1-001
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `95b596d8`, дерево чисто. Продукт `origin/dev` =
`origin/main` = `87d399f6`, не двигался. Наряд исполнителю по-прежнему НЕ отдан — расписки нет.

## outcome

**Ошибка ноги названа и исправлена в тот же день.** Нога остановила наряд, сославшись на
`docs/legacy-lab-surface-policy.md` («Status: current owner authority, 2026-07-20») как на живую
власть. Владелец дословно: «это мы пытались вообще ДРУГУЮ игру делать, по другой концепции… чё ты
это дёргаешь, почему это в docs вообще лежит».

**Правило существовало и не было прочитано.** `knowledge/code-survives-concept-resets.md`: «бумага,
написанная ради концепта, умирает ВМЕСТЕ с ним, целиком». `knowledge/canon-clean-authority-reset.md`:
«их собственные ярлыки `active`, `accepted`, `ratified`, **`current`**, `canon` не несут сегодняшней
власти», и её `read_by` прямо называет `play: work — before importing legacy meaning into current
product work`. Карточка лежала ровно там, где её читают. Нога её не открыла.

**Это ВТОРОЙ за день отказ такого рода.** Первый — правило про расширяемость ядра, лежавшее в улике
вместо `knowledge/`; тогда механизм был неверен. Сегодня механизм верен, а нога не сработала.

## Что из находки осталось верным

Два факта измерены и от концепции не зависят:

- `Assets/TunnelCrew/Scenes/HostWalksHisDay.unity` лежит мёртвым вне списка сцен сборки;
- песочница реакций `ReactionSandboxScene.unity` плюс её директор были построены и удалены как
  неиспользуемые (манифест `c-exec-program-v2-legacy-lab-purge-001`, коммит `72c7c8c6`).

Оба — примеры сцены под одну проверку, протухшей молча. Они вписаны в НОВОЕ правило как ПРИЧИНА, а
не как запрет.

## ЧТО ОН ПОПРОСИЛ НА САМОМ ДЕЛЕ, и это записано его словами

> «Нам нужна какая-то **дисциплина со сценами**, чтобы их **не начало плодиться хер знает сколько**,
> чтобы мы сцену **изначально делали так, чтобы можно расширять**, чтобы я мог **разные эксперименты
> ставить**… не хочу, чтобы **на каждый чик сцена создавалась**, или сцена была сделана так, что **я
> не могу в ней ничего тестировать**… **весь функционал я должен уметь протестировать**.»

Заведено `knowledge/one-experiment-scene-built-extensible.md`: ОДНА сцена экспериментов на всю игру,
строится расширяемой с первого дня; новая заводится только если в существующей эксперимент поставить
НЕЛЬЗЯ по устройству; сцена под одну проверку запрещена; и три вопроса, на которые обязан ответить
любой наряд, создающий сцену.

**Правило написано ЗАНОВО, его словами, а не унаследовано от мёртвой политики** — это записано в
самой карточке, чтобы следующая нога не приняла совпадение за наследование.

## ЧТО ВЗЯТО СВЕРХ ЭТОГО — и наряд от этого стал лучше

`docs/adr/ADR-0010-test-sandbox-scenario-as-data.md` описывает приём, которого наряду не хватало
вовсе, и он вписан:

1. **сценарий как ДАННЫЕ** — набор ситуаций описывается файлом с читаемыми ключами, значит проверку
   можно прогнать ДВАЖДЫ и сравнить; клик мышью повторить точно нельзя. Клик — разведка, файл —
   повторяемость; одно не заменяет другое;
2. **пауза, шаг на один тик, темп тиков** — чистой машинкой, headless-проверяемой;
3. **структурированный снимок, который читает ЧАТ** — прямой ответ на его собственные слова «мне чат
   будет расписывать, какое там поведение». Окно смотрит глазом, снимок читает агент, и разбор
   поведения перестаёт требовать его присутствия у экрана.

Взят ЗАМЫСЕЛ, не власть: кода нет (`Core/Sandbox`, `TickPacer` не находятся, от линии осталось три
файла), а бумага написана под мёртвый концепт. В наряде это сказано прямо.

## state_changes

1. `c-exec-g-5a7c-observe-1-001`: `blocked` → **`ready`**. Блокировка была ошибкой.
2. Заведена `knowledge/one-experiment-scene-built-extensible.md` — его слова, правило, две измеренные
   причины, три проверочных вопроса для наряда.
3. Улика `i-second-lab-surface-blocked-by-owner-policy-001` переписана: блокировка снята как ошибка,
   выжившие факты сохранены, и открытыми остались ДВА вопроса — гигиена документов продукта и
   «почему правило не сработало».
4. Текст наряда дополнен: правило про единственную сцену с тремя вопросами в границах; приём
   `ADR-0010` тремя пунктами; снимок для чата в `done_when` 1; файл сценария и пауза-шаг в
   `done_when` 2; четыре новых обязательных пункта возврата.
5. `NOW.md`, ставка, узел и дорожка `cargo-hands` не тронуты.

## captures

- **ЖИВАЯ ПРОБЛЕМА, названная им и НЕ решённая здесь:** документы мёртвых концепций лежат в
  продуктовом `docs/` вперемешку с живыми и читаются как действующие. Его слова: «это должно в
  архиве лежать… если это не в архиве, то это эскалация». Маршрут — отдельная нога по продуктовому
  репозиторию; Направление своей властью чужой репозиторий не чистит.
- **«Concept Lab» — отдельный репозиторий владельца**, где он обсуждает концепты игр. В этих двух
  репозиториях его нет и искать было нечего. Он сам снял вопрос: «хрен с ним, контентом не
  наполняем».
- **Гипотеза о причине отказа правила:** `osctl context` собирает рабочий набор по ссылкам карточек,
  а `knowledge/` в него не попадает вовсе — то есть нога видит правила, только если откроет их
  руками. Если случится третий такой отказ, чинить надо это, а не текст правил.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.

## decisions_needed

Нет. Наряд разблокирован и готов к выдаче; вопросов ему не задаётся.

## play_check

- 1 назвать противоречие: done — не политика против наряда, а нога против собственного правила
- 2 реконструкция: done — обе knowledge-карточки прочитаны, выжившие факты отделены от мёртвой бумаги
- 3 предложить исправленное состояние: done — снятие блокировки, новое правило его словами, четыре
  дополнения наряда
- 4 подтверждение (владелец): его слова этого дня, процитированные выше; подписанное не тронуто
- 5 friction: не требуется — правило верно и лежит верно; отказала нога, и это записано уликой

## Гейты

- **G7.** Дисциплина сцен записана ЕГО словами; форма файла сценария и снимка оставлена исполнителю.
- **G9.** CHARTER, узел и текст ставки не редактировались.

## log

СТОП, объявленный часом ранее, снят как ОШИБКА НОГИ: она прочла docs/legacy-lab-surface-policy.md от 20 июля как действующую власть, хотя это бумага мёртвой концепции — владелец дословно «мы пытались вообще другую игру делать, по другой концепции»; правило, запрещающее так делать, у нас было и лежало верно: code-survives-concept-resets говорит «бумага, написанная ради концепта, умирает вместе с ним целиком», а canon-clean-authority-reset прямо перечисляет ярлык CURRENT как не несущий власти и называет play work своим читателем — нога карточку не открыла, и это второй за день отказ правила, но впервые механизм был верен, а не сработала нога; из находки осталось верным ровно два измеренных факта — мёртвый HostWalksHisDay и снесённая как неиспользуемая песочница реакций, — и они вписаны как ПРИЧИНА в новое правило его словами: ОДНА сцена экспериментов на всю игру, расширяемая с первого дня, сцена под одну проверку запрещена, и три вопроса любому наряду, создающему сцену; сверх этого из ADR-0010 взят приём, которого наряду не хватало вовсе — сценарий как ДАННЫЕ ради повторяемости, пауза и шаг на один тик, и структурированный снимок, который читает ЧАТ, то есть прямой ответ на его же «мне чат будет расписывать, какое там поведение»; наряд разблокирован и дополнен, а гигиена документов мёртвых концепций в продуктовом docs вынесена как живая проблема с его словом «если это не в архиве, то это эскалация»

## next

1. **Наряд готов к выдаче** — полный текст для пасты по его слову.
2. Гигиена документов продукта — отдельная нога по продуктовому репозиторию, его словом
   «эскалация».
3. `t-react-3` (order 11) ждёт инструмента.
