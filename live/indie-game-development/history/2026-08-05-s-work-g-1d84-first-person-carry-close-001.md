# RESULT — s-work-g-1d84-first-person-carry-close-001

call: c-exec-g-1d84-first-person-carry-001
direction: indie-game-development
track: игрок
play: work
node/task: g-1d84 / t-player-1
date: 2026-08-05
close: light — because три из четырёх строк `done_when` эта нога перемеряла первой рукой по байтам
опубликованного `b7ae1fa4` (сцена, настройка взгляда, прогон ворот), а четвёртую закрывает вердикт
владельца его собственными словами в этом чате, а не пересказ исполнителя. Ни одна строка не осталась
за суждением, которого никто не выносил, поэтому отдельный свежий чат G5 по `os/plays/work.md`
§Light close не открывается.

## outcome

`t-player-1` закрыта. Самая рискованная догадка волны — «переноска от первого лица окажется вялой или
мутной» — проверена и не сработала: владелец запустил две Unity через Multiplayer Play Mode, поносил
балку вдвоём и сказал, что запланированный минимум сделан. Полоса «игрок» свободна.

Вход этой ноги — не Direction-RESULT, а handback исполнителя плюс отчёт интегратора WIN-CTRL. Он
принят как УЛИКА и перемерен целиком; закрытие стоит на измерении и на слове владельца, а не на
чужом «Status: DELIVERED».

## evidence

Всё измерено этой ногой на опубликованном `main` = `b7ae1fa4`, командами, а не чтением отчёта.

**Цепочка и публикация.** `git ls-remote origin` — `refs/heads/main` и `refs/heads/dev` оба
`b7ae1fa4ecd98c2c4d04376febb5504ff4d5c4ba`, то есть сверено с сервером, а не по локальным ссылкам.
Слияние `1967fac2` имеет родителей `094d8f48` и `066fcba4`, общий предок `839df47e`; кандидат
`066fcba4` — предок `main` (`merge-base --is-ancestor` = YES). `main..dev` и `dev..main` = 0/0.

**Строка 1 — «основной режим камеры в интегрированной сцене — первое лицо».** В
`Assets/TunnelCrew/Scenes/IntegratedHouse.unity` на `b7ae1fa4` ровно ОДНА камера: `--- !u!20 &`
встречается один раз, идентификатор `&2060976673`, `orthographic: 0`. На ней висит
`Assembly-CSharp::TunnelCrew.Networking.NetworkWalkerCamera` с пятью непустыми ссылками —
`_camera: {fileID: 2060976673}`, `_presentation`, `_settings`, `_look`, `_courier`. Прежней общей
ортографической камеры сверху в сцене нет ни одной.

**Строка 2 — «двое на разных копиях берут груз и несут его».** Закрыта словами владельца, см. ниже.

**Строка 3 — «наклон искусственно не заперт».** `Assets/TunnelCrew/Network/NetworkPlaySettings.asset`
на `b7ae1fa4` — `_lookPitchLimit: 80` (плюс `_eyeHeight: 1.7`, `_lookSensitivity: 0.12`). Это предел
взгляда, а не замок.

**Строка 4 — owner-eye verdict.** Слова владельца, см. ниже.

**Ворота перепрогнаны этой ногой на `b7ae1fa4`, а не приняты с чужих слов.**
`dotnet build core/TunnelCrew.Core.csproj -c Release` — Предупреждений 0, Ошибок 0.
`dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` — не пройдено 0,
пройдено 63, пропущено 0. `./pwsh.cmd tools/check.ps1` — `hygiene OK`, `OK: all active gates green`.

**Проверенные утверждения отчёта интегратора — все подтвердились.** `_standWallHeight` на `b7ae1fa4`
не существует ни в `.cs`, ни в `.asset` (встречается только в тексте самого отчёта), поэтому правка
`1.5 → 2.6` отброшена законно. `NetworkWalkers.unity` в дереве `b7ae1fa4` отсутствует — сцена одна.
Слот `WIN-U4` в реестре `C:/projects/Unity/GasCoopGame_slot-state/gascoop-slot-state.v1.json` —
`AVAILABLE`, `lease: none`.

**Что в доказательствах отсутствует, и это названо, а не спрятано.** Unity MCP в работе не
подключался (`mcp_endpoint: unrecorded`), поэтому импорта, компиляции Unity-слоя и PlayMode со
стороны агента нет ни одного — `dotnet` собирает только engine-free ядро. Вся Unity-сторона держится
на запусках владельца. Это ровно класс записи `i-unity-evidence-rests-on-owner-launch-001`, и он этой
ногой не изменён.

## owner words

Дословно 2026-08-05, в этом чате:

- «Я проверил переноску вдвоем от первого лица.»
- «в мультиплеере я протестировал, ну, на одном компе, но с двух через Multiplayer Play или
  Multiplayer Center, да, я запустил две Unity и вдвоём поперемещал эту [балку]»
- «Я посмотрел в U4, потом в D ветке. То есть всё работает.»
- «то, что мы планировали, вот этот минимум сделан. То есть всё, что задачей там должно было
  сделано, сделано.»
- «Не надо стены поднимать, похуй, потом поднимем, когда будем делать»; «На стены как бы пока похуй.»
- «Мне надо, чтобы ты закрыл, потому что задачу блочат другие.»
- «Ну, естественно, ты должен проверять, но с моей стороны я всё тестировал и всё посмотрел, всё это
  нормально.» — поэтому нога перемеряла байты и прогоняла ворота сама, а не закрывала на доверии.

**Границы этих слов, названные им самим:** «по высоте не поднимается… там работы ещё много».
Вертикаль и подъём груза в `t-player-1` не входили (вырез 9 ставки), в закрытие не засчитаны и
остаются снаружи.

**Поправка, которую нельзя потерять.** Отчёт исполнителя честно писал, что переноска вдвоём в первом
лице не упоминалась ни в одном из пяти кругов приёмки. Про ТЕ пять кругов это верно. Владелец
проверил её отдельно, двумя редакторами, и закрывает эту строку именно его сегодняшнее слово, а не
отчёт.

**Дефект самой ноги, записанный без смягчения.** Первый ход сессии предложил владельцу сходить
посмотреть ещё раз и поднять стены перед просмотром. Он это уже смотрел, и предложение стоило ему
круга раздражения. Правило, которое нарушено, в состоянии уже есть:
`knowledge/no-checks-the-owner-makes-by-eye.md`. Никакая нога не назначает владельцу повторный
просмотр того, по чему он уже высказался.

## state_changes

- `NOW.md`: `tasks[t-player-1].status` → `done`, `launch_receipt` заменён на `checkpoint` с полным
  разбором четырёх строк и перемеренной цепочкой; `tasks[t-player-2].status` →
  `ready` + `unblocked` (наряда нет, задача двадцать вторая по порядку); `updated` → эта сессия.
- `NOW.md/open_calls`: удалить только вернувшийся `c-exec-g-1d84-first-person-carry-001`. Наряды
  `c-exec-g-1d84-host-into-one-scene-001`, `c-exec-g-1d84-three-rooms-by-hand-001`,
  `c-exec-g-1d84-host-under-the-judge-001`, `c-exec-g-1d84-free-kits-probe-001` сохранить как есть.
- `NOW.md/issues`: завести `i-scene-2-call-basis-stale-after-publication-001`,
  `i-house-walls-below-eye-level-001`, `i-unity-cloud-services-enabled-in-main-001`,
  `i-slot-u4-remote-tip-carries-foreign-commits-001`; дописать `remeasured` в
  `i-house-work-finished-in-slot-not-returned-001`. Остальные записи не трогать.
- Препендить ровно один LOG receipt и сохранить этот полный RESULT в `history/`.
- CHARTER/TREE/knowledge, продуктовый репозиторий, реестр слотов и ветки не менять.

## captures

- Предсказание клиентского передвижения вырезано сознательно (вырез 1 ставки), и гладкость картинки
  добавила к отставанию ещё один тик. Владелец жаловался на дрожь, а не на задержку. Если задержка
  когда-нибудь станет жалобой — лекарство названо и разрешено концептом: предсказывается только своё
  передвижение, груз — никогда.
- `_physicsStepsPerTick: 2` — ручка, названная исполнителем на случай, если балка начнёт проваливаться
  сквозь стены (поднять до 3–4). Числом решения не является.

## decisions_needed

Нет. Единственный висящий вопрос по владельцу — форма файла профилей (`d-profile-file-format-recommendation-002`), он к этой задаче не относится и блокирует ровно `t-host-3`.

## play_check

- Шаг 1 «сверить наряд» — исполнен: `t-player-1`, полоса «игрок», активная ставка `g-1d84`.
- Шаг 2 «вход от владельца» — исполнен: вердикт получен его словами и процитирован дословно.
  Owner-verdict guard соблюдён — нога остановилась и дождалась его слов, ничего не решив за него.
- Шаг 3 «работа» — работа сделана исполнителем в слоте и интегратором WIN-CTRL; эта нога её приняла,
  перемерила и закрыла.
- Шаг 4 «сверка с done_when» — построчная, четыре строки из четырёх, каждая с названным источником.
- Шаг 5 «закрытие» — light close, обоснование в заголовке. Продуктового review-маркера не добавляет,
  ставку не закрывает: `review` по `g-1d84` не наступил, впереди `t-scene-2` и точка пересмотра
  аппетита.
- G5: венью — light close по правилу плея, а не пропуск. G1/G2: полос по-прежнему пять, WIP-предел 3
  не поднят, ни одна новая полоса не заведена. G7: ни одно продуктовое или геймплейное решение за
  владельца не принято; вопрос про стены закрыт ЕГО словом и заново не открывается.

## next

Полоса «игрок» свободна; следующая по порядку задача волны — `t-scene-2` (переезд хозяина в
интегрированную сцену), её наряд `c-exec-g-1d84-host-into-one-scene-001` уже написан и стоит `ready`.
ВЫДАВАТЬ ЕГО КАК ЕСТЬ НЕЛЬЗЯ: наряд объявляет базой `main` = `02a53bbb`, а `main` ушёл на `b7ae1fa4`,
и мир под нарядом изменился по существу — см. `i-scene-2-call-basis-stale-after-publication-001`.
Ближайшая нога: короткий `work`, который перемеряет базу и названные пути на `b7ae1fa4`, называет
слот с фактическим HEAD и отдаёт владельцу готовую строку запуска. Чужого наряда эта нога не пишет и
не выбирает.

END_OF_FILE: live/indie-game-development/history/2026-08-05-s-work-g-1d84-first-person-carry-close-001.md
