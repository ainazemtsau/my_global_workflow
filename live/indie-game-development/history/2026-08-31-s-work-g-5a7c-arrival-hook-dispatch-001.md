# RESULT — s-work-g-5a7c-arrival-hook-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / bet-g-5a7c-wave-9, полоса `утро`
track: утро
date: 2026-08-31
outcome_kind: dispatch — третья нога волны выдана в WIN-U2; найдено, что форма «по завершении» в ядре уже существует и её надо переиспользовать, а не строить рядом

owner_approved: |
  Его слово на слот и на выдачу дословно: **«u2 давай третью ногу»**.
  Ни одна строка `done_when` не изобретена этой ногой: задача `t-arrival-hook-1` стояла в волне с
  нарезки, наряд её не расширяет и не сужает.

outcome: |
  **ВЫДАН ТРЕТИЙ НАРЯД, И ТЕПЕРЬ ИДУТ ТРИ НОГИ ОДНОВРЕМЕННО** при лимите полос 4:
  прибор (WIN-U1), гейт (WIN-U4), хук прибытия (WIN-U2).

  **ГЛАВНАЯ НАХОДКА ЭТОЙ НОГИ — НЕ ДИСПЕТЧЕРСКАЯ, А ИНЖЕНЕРНАЯ, И ОНА МЕНЯЕТ ЦЕНУ ЗАДАЧИ.**
  Крючок «по завершении» в ядре УЖЕ ПОСТРОЕН: `Householder.cs:1029-1036`, `DeedOfEnding(response,
  responseCompleted)`, зовётся на `:1009` и `:1208`. Его собственный комментарий: «A response still
  running leaves nothing behind». То есть различение «отклик идёт» / «отклик завершился» существует
  и работает — просто через него ходит только ПОСТУПОК, а не эффекты.

  Наряд прямо велит переиспользовать эту форму и объявляет постройку второго механизма завершения
  рядом с ней **условием СТОПа**: если исполнитель ловит себя на этом, значит форма понята неверно,
  и это находка важнее задачи. Без этой строки самый вероятный сценарий — новая ветка `if` в тике,
  то есть ровно то, чего порог «больше пяти правок» и написан не допустить.

  **ЧЕСТНО ПРО ПРЕДЫДУЩУЮ НОГУ.** Диспетчерская нога `s-work-g-5a7c-instrument-dispatch-001` не
  выдала этот наряд, сославшись на то, что не станет решать за владельца, какой слот поднимать.
  Довод слабый: написание наряда слота не занимает. Владелец спросил об этом прямо, и причина
  названа ему как недоработка, а не как осторожность. Стоило выдать все три сразу и дать ему
  выбрать слот.

evidence: |
  **БАЙТЫ, ВНЕСЁННЫЕ В НАРЯД** — продуктовый `main` `c7e926cf`, прочитаны этой ногой:
  - применение эффектов: `Probe/Householder/Householder.cs:310-317` — единственный сторож
    `isReaction`, условия «дошёл» нет; комментарий на месте: «Only the winner's, and only once it
    has won»;
  - существующий крючок завершения: `Householder.cs:1029-1036` `DeedOfEnding`, вызовы `:1009`,
    `:1208`;
  - закрытый словарь способностей: `Profiles/HouseholderProfileLoader.cs:64-83` — три слова
    (`walk-route`, `wait-at-route-point`, `perform-occupation`), `default` возвращает `false`.

  **НЕПЕРЕСЕЧЕНИЕ С ДВУМЯ ИДУЩИМИ НОГАМИ** — пофайлово:
  - хук прибытия: `Probe/Householder/Householder.cs`, `HouseholderRoutineState.cs`,
    `Profiles/HouseholderProfileLoader.cs`, поставочный профиль;
  - прибор (WIN-U1): `Network/**`, `Editor/**`;
  - гейт (WIN-U4): `tools/**`, `tests/TunnelCrew.Core.Tests/HouseholderDecisionPipelineTests.cs`.
  Общих файлов нет. Наряд отдельно велит класть свои тесты В ДРУГОЙ ФАЙЛ, потому что
  `HouseholderDecisionPipelineTests.cs` прямо сейчас правит нога гейта.

  **СОСТОЯНИЕ СЛОТОВ на момент выдачи:** все четыре чекаута на `c7e926cf`, чисты, отставание 0.
  Живой Unity подтверждён только у WIN-U1; U2 требует поднятого редактора, и это названо владельцу
  как его действие.

state_changes: |
  СОЗДАНО (1 карточка + 1 документ):
  - `c-exec-g-5a7c-arrival-hook-1-001` → `t-arrival-hook-1`, слот WIN-U2, база `c7e926cf`,
    документ `work/2026-08-31-call-arrival-hook-1.md`.

  ПЕРЕВЕДЕНО В РАБОТУ (1):
  - `t-arrival-hook-1` → `active`.

  Полос с активной работой стало три из четырёх объявленных; лимит 4 не превышен.

captures: |
  - Наряд несёт раздел «занято прямо сейчас другими ногами» с поимённым перечислением чужих
    поверхностей. Это первый наряд в направлении, который says это явно; до сих пор разведение
    полос жило только в таблице ставки, которую исполнитель не читает. Если сработает — стоит
    сделать привычкой.

decisions_needed: |
  Ни одного. Все три наряда выданы, слоты названы, тексты для вставки отданы владельцу.

play_check: |
  1. OPEN — задача уже стояла в волне; состояние слотов и байты замера перечитаны заново.
  2. WORK — наряд написан; найдена и внесена существующая форма `DeedOfEnding`; поверхности
     соседних ног перечислены поимённо как границы.
  3. (owner) — его слово: «u2 давай третью ногу и напиши почему ты сразу ее не дал?».
     Причина названа ему прямо и записана здесь как недоработка предыдущей ноги.
  4. CLOSE — RESULT финальным сообщением, применение и коммит после него.

log: |
  третья нога волны выдана в WIN-U2 — и найдено, что крючок «по завершении» в ядре уже построен:
  DeedOfEnding различает идущий и завершившийся отклик, через него просто не ходят эффекты

next: |
  Домой. Три ноги идут параллельно, возвраты приходят по одному.

  После прибора открывается `t-sight-thing-1` — её приёмка стоит на числах прибора.
  После хука прибытия открывается `t-morning-content-1` — наполнение утра данными.
  Полоса `почему` и полоса `руки` ждут своей очереди по объявленному порядку.
