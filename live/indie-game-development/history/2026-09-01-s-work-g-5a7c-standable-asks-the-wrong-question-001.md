# RESULT — s-work-g-5a7c-standable-asks-the-wrong-question-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-a-fact-knows-who-made-it-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — развилка закрыта замером начисто, и за ней направление дочитало код до настоящей причины: пригодность для стояния спрашивают про комнату вместо мебели, и спрашивают только про цель, никогда про старт

close: light — because причина перевыведена направлением первой рукой на байтах: тело `Standable` прочитано целиком, его единственный вызов найден полным поиском по файлу, и асимметрия «цель спасают, старт нет» доказана строками, а не рассуждением. Ничего не закрывается как достигнутое, кроме наряда на замер.

owner_approved: |
  Не требовалось: диагностика и форма починки — инженерные. Владельцу сообщается фактом.

outcome: |
  **ЗАМЕР ЗАКРЫЛ РАЗВИЛКУ НАЧИСТО, И РЕЗУЛЬТАТ НЕ ОСТАВЛЯЕТ МЕСТА ТОЛКОВАНИЮ.** Семь запросов из
  семи отказаны — шесть названных мест в разных комнатах **и обратный путь туда, откуда он пришёл**.
  **Ноль точек пути в каждом.** Перебор целей мёртв; развилка «А» — дефект в его положении.

  **НОГА ПРАВИЛЬНО ОСТАНОВИЛАСЬ И НЕ СТРОИЛА.** Её WIP под ветку «Б» остался незакоммиченным, и
  часть его теперь не понадобится. **Это выигрыш замера, а не потеря ноги.**

  **И ЗА ЗАМЕРОМ НАПРАВЛЕНИЕ ДОЧИТАЛО КОД ДО ПРИЧИНЫ. ДВЕ НАХОДКИ, ОБЕ СТРОКАМИ.**

  **ПЕРВАЯ: `Standable` ОТВЕЧАЕТ НЕ НА ТОТ ВОПРОС.** `HouseWalk.cs:832-835` — если точка внутри
  любой комнаты, она **возвращается как есть**. Функция спрашивает «точка внутри комнаты?», а не
  «может ли он тут стоять». **Мебель не участвует.** Точка вплотную к кухонной тумбе лежит внутри
  кухни — принимается без правки, хотя стоять там нельзя. **Имя обещает больше, чем делает код**,
  и это второй раз за день, когда добросовестное имя или комментарий обещали механизм, которого нет.

  **ВТОРАЯ, И ОНА ОБЪЯСНЯЕТ АСИММЕТРИЮ: СПАСАЮТ ТОЛЬКО ЦЕЛЬ.** `HouseWalk.cs:267` —
  `var goal = Standable(house, to);` — **единственный вызов во всём файле**, найден полным поиском.
  Точка `from` уходит в маршрутизацию **сырой**.

  **ОТСЮДА РОВНО ТО, ЧТО НАМЕРЕНО: ПУТЬ ВНУТРЬ БЫЛ, ПУТИ НАРУЖУ НЕТ НИ ОДНОГО.** Цель приняли, он
  дошёл — и оказался там, откуда стартовать нельзя, а стартовую точку не чинит никто.
  **Вопрос «как он сюда попал», заданный прошлой ногой, получил ответ.**

  **ЧТО ЭТО ЗНАЧИТ ДЛЯ ИГРЫ, А НЕ ДЛЯ ПОЛИГОНА.** Вещи лежат на мебели — все. Значит **любая вещь
  может завести хозяина в точку, из которой он не выйдет никогда**, в любом доме, который получит
  игрок. Пять симптомов за два дня выглядели по-разному, место было одно.

  **ГРАНИЦА НАРЯДА ИЗМЕНЕНА ОСОЗНАННО: `HouseWalk.cs` ОТКРЫТ.** Запрет ставился, когда причина была
  неизвестна и правка туда была бы расширением наугад. **Причина найдена и лежит именно там** —
  держать запрет теперь значило бы запрещать чинить найденное.

  **И ДВА ПОРОГА ПОСТАВЛЕНЫ ПРОТИВ ОЧЕВИДНЫХ ПЛОХИХ ПОЧИНОК.** Телепортация в ближайшую хорошую
  точку — откат: это тот самый обман, против которого написан комментарий про человека, идущего
  сквозь стену. Второй ответчик на «можно ли тут стоять» — порог смерти волны.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** на `slot/win-u2` = `b4b010c1` (тот же механизм ходьбы):

  - `HouseWalk.cs:832-835` — тело `Standable`: `if (house.TryFindRoomAt(point.X, point.Z, out _))
    return point;`, дальше — прижатие к коробке ближайшей комнаты. **Обращения к мебели нет ни
    одного;**
  - `grep "Standable"` по файлу → **две строки: объявление `:832` и единственный вызов `:267`**,
    и вызов этот — `Standable(house, to)`, то есть **только цель**;
  - `HouseWalk.cs:255-258` — `from` используется сырым, только для высоты;
  - `HouseWalk.cs:267-282` — ветка «та же комната» строит прямую к уже принятой цели;
  - `HouseWalk.cs:360-372` — иначе Дейкстра по названным местам, и невозможность даёт
    `FurnitureInTheWay`; отказ выносится **про маршрут**, а не про цель.

  **Из возврата ноги, как улика:** таблица семи отказов с нулём точек в каждом, включая обратный
  путь; неподвижность с 63.47 до 221.4 с; тесты `745 / 744 / 1`; `check.ps1` зелёный; TTL сохранён
  в `4ecc2f82`; WIP трёх файлов не продолжен и не закоммичен.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-005` → слот **WIN-U4**, `HouseWalk.cs` открыт,
    порог мест двенадцать;
  - `work/2026-09-01-call-he-must-never-stand-where-he-cannot-leave.md`.

  ЗАКРЫТО КАК СДЕЛАННОЕ (1 наряд):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-004` → `done`: замер сделан и закрыл развилку.

  ОБНОВЛЕНО (1 улика, НЕ закрыта):
  - `i-a-refused-walk-is-heard-by-nobody-001` — переписана под доказанную причину: таблица семи
    отказов, обе находки в `HouseWalk.cs` со строками, следствие для игры, а не для полигона.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-a-fact-knows-who-made-it-1` остаётся `active`;
  - `t-the-bench-opens-quiet-1` остаётся `active`, ждёт освобождения WIN-U2;
  - `t-everything-lands-1` остаётся `open` и ждёт конца замирания.

captures: |
  - **ВОПРОС «КАК ОН СЮДА ПОПАЛ» ПОЛУЧИЛ ОТВЕТ ЗА ОДИН ЗАМЕР ПОСЛЕ ЧЕТЫРЁХ ЗАХОДОВ ВСЛЕПУЮ.**
    Обратный путь в списке запросов оказался решающим: без него таблица говорила бы «цели плохие»,
    с ним — «плохо там, где он стоит». **Правило-кандидат: в замер достижимости всегда включать
    путь НАЗАД, откуда пришёл — он один отличает беду цели от беды положения.**
  - **ВТОРОЙ РАЗ ЗА ДЕНЬ ДОБРОСОВЕСТНОЕ ИМЯ ОБЕЩАЛО МЕХАНИЗМ, КОТОРОГО НЕТ.** Утром это был
    комментарий «причина лежит для того, кто спросит» — и никто не спрашивал. Сейчас — функция
    `Standable`, которая проверяет комнату, а не пригодность. **Правило-кандидат: имя и комментарий
    читать как заявку, а не как гарантию, и проверять поиском, что заявленное выполняется.**
  - **ЗАПРЕТ, ПОСТАВЛЕННЫЙ ПРОТИВ РАСШИРЕНИЯ НАУГАД, СНИМАЕТСЯ, КОГДА ПРИЧИНА НАЙДЕНА ИМЕННО ТАМ.**
    Держать его дальше значило бы запрещать чинить найденное. **Граница наряда — инструмент против
    блужданий, а не догма.**
  - **ЗАМЕР СДЕЛАЛ ЧАСТЬ ЧУЖОЙ РАБОТЫ НЕНУЖНОЙ, И ЭТО ЗАПИСАНО КАК ВЫИГРЫШ.** WIP под ветку «Б»
    может уйти в мусор. Наряд прямо говорит: выбросить лишнее — хороший исход. Иначе нога будет
    защищать написанное.

decisions_needed: |
  Ни одного владельческого.

play_check: |
  1. OPEN — возврат ноги с результатом развилочного замера.
  2. WORK — причина дочитана в коде первой рукой, обе находки доказаны строками и полным поиском
     вызовов; граница наряда изменена осознанно и объяснено почему; поставлены пороги против
     телепортации и второго ответчика; ненужность части чужого WIP объявлена выигрышем.
  3. (owner) — не запрашивался: диагностика и форма починки инженерные.
  4. CLOSE — RESULT, применение, коммит; наряд уходит владельцу.

log: |
  развилка закрыта замером начисто — семь отказов из семи, включая обратный путь, ноль точек в
  каждом, — и за ней направление дочитало код до причины: функция «пригодно для стояния» спрашивает
  только «внутри ли комнаты», мебель её не интересует, а вызывают её единственный раз и только для
  цели, тогда как точка, где он стоит, не проверяется вообще; отсюда и асимметрия «путь внутрь был,
  пути наружу нет»; HouseWalk открыт наряду, телепортация и второй ответчик объявлены откатом

next: |
  Наряд `c-exec-g-5a7c-a-fact-knows-who-made-it-1-005` уходит владельцу для **WIN-U4**.

  Волна: голова закрыта, ходьба мертва, стенд ждёт освобождения WIN-U2, замирание чинится по
  найденной причине, публикация ждёт только его.
