# RESULT — s-research-g-5a7c-arch-householder-1-001

```yaml
outcome: |
  Возвращён ВЫБОР из четырёх структурно разных способов построить план хозяина, с ценой в числах по
  трём случаям, с одним рекомендованным и с названным местом, где рекомендация может быть неверна.
  Развилка «расширением той же таблицы или отдельным слоем» вынесена владельцу — её решает он, и
  страница knowledge заранее записала, что решает не проходящая нога.

  ТРИ ОТВЕТА НАРЯДА:
  (1) закон ещё спасаемого плана вешается на АРИФМЕТИКУ ОБСТАНОВКИ — HouseGap.TryBeside/.Under/
      .AtFloor плюс TryFindFurnishingAt и Top, — а НЕ на отказ ходьбы, как предложили все пятеро
      архитекторов. Нового слоя не нужно: нужны один порт, одно поле на входе тика и одно
      ослабление сторожа.
  (2) результат дела — это МЕТКА; дело кончается не таймером, а появлением метки; «заменить» — это
      другая строка, пишущая ТУ ЖЕ метку. Закрывает дело мир, а не он сам.
  (3) вещи входят в ядро МЕТКАМИ через вход тика — тем же швом, который уже работает наружу
      (HouseholderDeed, ActionTarget.ThingName, ReachThingName), пущенным обратно. grep Cargo по
      Core/Householder/ остаётся 0.

  КОДА НЕ ТРОНУТО: git status --porcelain в продуктовом репозитории пуст, HEAD не двигался ногой.

evidence:
  - work/2026-08-29-research-householder-plan-architecture.md — документ возврата, 6 разделов
  - базис 68c4e933, дерево чистое; ПЕРЕПРОВЕРЕНО на 782349bd после того, как параллельная сессия
    слила шов: git diff --stat 68c4e933..782349bd по Core/Householder/, Core/House/, Profiles/,
    StreamingAssets/.../Householder/ и World/SceneHousePlan.cs = ОДИН файл, ОДНА строка, и это
    правка комментария в HouseWays.cs. Все замеры действительны на свежей голове.
  - первой рукой перемерено: 26 файлов / 9155 строк ядра; 34 файла / 15383 строки тестов;
    загрузчик 2867 строк / 70 меток case; профиль 125 строк; дом 7 комнат / 59 предметов /
    12 кусков лута / 2 места с ролью
  - ФАН-АУТ ПО ПЛЕЮ: пять независимых архитекторов (номинальная группа, пять разных операторов,
    ни один не видел ответов других) + три копателя (довод против пути по умолчанию; редкие
    случаи и дальняя аналогия; что убивает форму при сборке) + шесть скептиков, каждому дано
    задание ОПРОВЕРГНУТЬ. Три скептика собрали и ЗАПУСТИЛИ настоящее ядро в изолированных
    песочницах вне репозитория.
  - опровержение подтвердило Б запуском: трёхшаговая ветка с двумя занятиями в разных местах
    прошла целиком — дошёл, отработал фазы, взял третий шаг, вернулся в тот же шаг дня с тем же
    остатком. «Ветка-дело не работает» опровергнуто.
  - опровержение ОТБИЛО общую ставку всех пятерых: FurnitureInTheWay — ступенька на 0,25 м от
    любой грани с дырой посередине; замерено запуском: 0,02–0,24 м → FurnitureInTheWay,
    0,26 м → None, ровно под столиком → None; и HouseWalk.cs:253 затирает высоту цели, поэтому
    кружка НА столе и кружка ПОД столом — один и тот же запрос.

state_changes:
  - t-arch-householder-1 → status: done, добавлена строка журнала
  - c-research-g-5a7c-arch-householder-1-001 → status: done, перенесена в cards/closed/
  - НОВАЯ улика i-house-plan-in-the-game-has-no-furniture-001 (route: work)
  - НОВАЯ улика i-standing-layer-never-ran-on-shipped-data-001 (route: work)
  - НОВАЯ улика i-a-deed-that-cannot-be-carried-out-is-lost-001 (route: work)
  - НОВОЕ решение d-householder-plan-shape-001 (asks: владелец, status: open)
  - i-householder-core-has-no-memory-carrier-001 → строка журнала: протухла на четыре седьмых
  - work/2026-08-29-research-householder-plan-architecture.md — документ возврата

captures:
  - ЖИВОЙ ДЕФЕКТ, ПЕРЕКРЫВАЮЩИЙ ВОСЬМУЮ ВОЛНУ: SceneHousePlan.cs:63 строит дом четырёхаргументным
    конструктором (furnishings: null), а WithBody(...) имеет одного вызывающего во всём репозитории,
    и это тест. Дефекты маскируют друг друга через HouseWalk.cs:259. В тот день, когда дом начнёт
    отдавать обстановку, HouseWalk начнёт отказывать КАЖДОМУ пути с BodyUnstated и хозяин встанет
    намертво. → i-house-plan-in-the-game-has-no-furniture-001
  - HouseGap — построенный, точный, замкнутой формы слой ответов «что можно достать сзади» и «в какую
    щель пролезают» — имеет НОЛЬ боевых потребителей. Это и есть носитель §6, и его не надо изобретать.
  - Core/Solids/ThreadSight.IsClear(...) через HouseFurnishing.AsSolid() отвечает дословно на «край
    виден» — настоящая линия взгляда в трёх измерениях, уже построена.
  - Правка сторожа эффектов Householder.cs:262-269 несёт ЛОВУШКУ: 262-263 — переменная isReaction,
    переиспользуемая как разветвитель на :271 и :293. Ослабить переменную вместо if = активный отклик
    с пустой реакцией на каждом тихом тике, полный развал поведения, при зелёном существующем тесте.
  - Метки мира НЕЛЬЗЯ писать в его память одной строкой в Advance: тихая перезапись (у метки нет поля
    происхождения), NullReferenceException на первом кадре (до посадки Marks равен null), и метода
    слияния у HouseholderMarks нет. Правильно — хвостовое поле на HouseholderStepInput, только на чтение.
  - Шкала на минимуме неотличима от шкалы, которую ни разу не двигали, и зажим стирает глубину: три
    траты и тридцать читаются одинаково. Начальное упрямство обязано быть шкалой, счёт попыток — меткой.
  - У шага дня одно «только если», и это литерал "always"; Circumstance имеет ноль поведенческих
    читателей (два вхождения, оба — проверки, бросающие на None).
  - Слова «срок» нет во всём дереве исходников: grep по deadline|timelimit|end.?of.?day = 0.
  - Кандидат в knowledge, НЕ записан (ждёт его выбора формы): «§6 меряется обстановкой, а не отказом
    ходьбы» — довод не зависит от выбора А/Б/В и переживёт его, но записывать до его слова рано.

decisions_needed:
  - d-householder-plan-shape-001 — какой формой строится план хозяина. Сводится к одному вопросу,
    который видно в игре: утро идёт по написанному им порядку (А) или пересобирается само под
    давлением (Б). Рекомендация Б, и она МЕНЯЕТСЯ НА А, если он ответит, что хочет писать порядок
    утра сам. В отвергнут доводом его же автора; Г отвергнут по выразимости, а не по цене.

play_check:
  - research шаг 1 Recite: вопрос, формат возврата и бюджет пересказаны; вопрос не расщеплялся
  - research шаг 2 Investigate: метод — чтение байтов первой рукой плюс фан-аут, которого требует
    сам плей (несколько генераторов на один вопрос идут независимо, номинальной группой, слияние и
    дедуп после; плюс брифы копателей). Пять генераторов, три копателя, шесть скептиков.
  - research шаг 3 Confidence: установленное отделено от вывода в §5.4 документа поимённо
  - research шаг 4 Close: возврат в заказанном формате, next = возврат родителю
  - ГРАНИЦЫ НАРЯДА СОБЛЮДЕНЫ: сборка не строилась и не проектировалась — только проверено, выдержит
    ли форма; чисел уровня (длина утра, число дел, число комнат) не выбиралось; принятое (тяга,
    телекинез, две механики) не оспаривалось; кода не тронуто ни одного файла
  - ВЛАДЕЛЬЧЕСКИЙ СТОРОЖ: наряд просит ВЫБОР, решает владелец. Downstream-наряд НЕ открыт, решение
    вынесено карточкой и ждёт его слов
  - G5 НЕ ПРИМЕНЯЛСЯ КАК БИНДИНГОВЫЙ: это research, его продукт — findings, а не построенная вещь.
    Опровержение шло ВНУТРИ сессии шестью скептиками — это pre-pass, а не свежая сессия. Названо честно.
  - ЧЕГО НЕ БЫЛО: Unity не собирался, тесты не гонялись. Всё выше — чтение байтов плюс три запуска
    ядра в песочницах вне репозитория.

log: |
  g-5a7c/архитектура/t-arch-householder-1: РЕСЁРЧ ВЕРНУЛСЯ ВЫБОРОМ ИЗ ЧЕТЫРЁХ, И ГЛАВНОЕ В НЁМ —
  ЧТО ОПРОВЕРЖЕНИЕ ОТБИЛО ОБЩУЮ СТАВКУ ВСЕХ ПЯТИ АРХИТЕКТОРОВ. Диагноз ядра: хозяину не хватает не
  «плана» — у ДЕЛА НЕТ РЕЗУЛЬТАТА, а у ФАКТА НЕТ ПРЕДМЕТА, и это одна дыра с двух концов одного шва.
  Путь по умолчанию («просто больше строк») отвергнут не по цене, а ПО ВЫРАЗИМОСТИ: у авторской
  строки нет колонки ни под дело, ни под предмет, ни под «насколько сломано», и всякий вид
  обработчика требует ФАКТА, что проверяется до развилки по виду (loader:1805 перед :1839). Пять
  независимых архитекторов сошлись на том, что вещи входят МЕТКАМИ, ноль новых слов, ноль правок в
  15 383 строках тестов; трое пришли к одной форме с трёх разных сторон. Шесть скептиков, из них три
  СОБРАЛИ И ЗАПУСТИЛИ ядро: подтвердили ветку-дело запуском (трёхшаговая ветка с занятиями прошла
  целиком и вернулась в тот же шаг с тем же остатком) и ОТБИЛИ §6-прибор — FurnitureInTheWay это
  ступенька на 0,25 м с дырой посередине, а высота цели затирается на HouseWalk.cs:253, поэтому
  кружка НА столе и ПОД столом это один запрос. Верный прибор уже построен и не подключён ни разу:
  HouseGap. И найден живой дефект, перекрывающий всю волну: в собранной игре у дома НОЛЬ предметов
  обстановки (SceneHousePlan.cs:63, четырёхаргументный конструктор) при 59 авторских, а тело хозяина
  не объявлено — два дефекта маскируют друг друга, и первый же, кто отдаст дому обстановку, получит
  вставшего намертво хозяина. Рекомендован способ Б, и он МЕНЯЕТСЯ НА А одним его ответом: пишет ли
  он порядок утра сам.

next: |
  ВОЗВРАТ РОДИТЕЛЮ. t-arch-householder-1 закрыта, наряд c-research-g-5a7c-arch-householder-1-001
  закрыт и перенесён в closed/. Полоса «архитектура» свою задачу отработала.

  ЖДЁТ ЕГО СЛОВА: d-householder-plan-shape-001. Пока он не ответил, ни одна нога не вправе начать
  строить план — форму выбирает он.

  ЧТО МОЖНО ДЕЛАТЬ БЕЗ ЕГО ОТВЕТА, и что должно идти первым в любом случае:
  i-house-plan-in-the-game-has-no-furniture-001 — две строки, одна нога, до любой работы по §6.
  Без неё каждый следующий способ строится вслепую, а первый же, кто отдаст дому обстановку,
  остановит хозяина насмерть.
```

END_OF_FILE: live/indie-game-development/history/2026-08-29-s-research-g-5a7c-arch-householder-1-001.md
