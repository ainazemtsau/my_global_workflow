# RESULT — s-shape-g-6b13-small-tasks-and-lanes-001

play: shape
direction: indie-game-development
node: g-6b13
date: 2026-08-02

## outcome

Ставка g-6b13 перенарезана мелко и запущена. Карточка принята его поправкой, копание вынесено в
свою ставку, второй способ переноски снят, exe вошёл на его место в очереди. Одиннадцать задач,
две полосы, два наряда выпущены.

## evidence

Все решения — его слова 2026-08-02, приведены дословно в `NOW.decisions` и в ledger `TREE.md`.

## state_changes

**TREE.md — карточка g-6b13.** `done_when` с пяти пунктов до четырёх: копание/тоннель вырезаны в
свою ставку, второй способ переноски удалён из критериев и стал запасным ходом в `risk`, exe стал
пунктом 2 его формулировкой, хозяин получил требование расширяемости. Строка исключений: `запускаемая
игра` ушла, `подключение с чужих машин` сузилось до `раздача посторонним`. `appetite` — без
временного потолка по его запрету. `kill_by` — его наблюдение вместо даты. Ledger: PENDING-вердикт
закрыт с записанной ТОЧНОЙ силой токена.

**NOW.md.** `cut_list` — одиннадцать пунктов. `lens_verdicts` — семь, все переписаны на новые
задачи. `tasks` — `t-1..t-6` заменены на `a-1..a-6`, `b-1..b-3`, `c-1..c-2`. `tracks` — две полосы
объявлены его точными словами. `open_calls` — два новых наряда `ready`, старый помечен `dead`.
`decisions` — все пять закрыты.

**work/** — два новых наряда: `c-exec-gate-surface-cut-001-call.md`,
`c-exec-newgame-folder-and-scene-001-call.md`.

## ПОПРАВКА ВЫПУСКА — слоты, до запуска любой работы

Владелец спросил про `WIN-U1`: он его сам запускал под ту же по смыслу работу, остановил, и хотел бы
там же её и доделать, чтобы слот освободился. Мысль верна, но `continue` там незаконен, и проверено
почему:

- ветка `slot/win-u1` несёт `docs/measurements/root-receipts/c-exec-newgame-folder-and-gates-001/`
  и `openspec/changes/c-exec-newgame-folder-and-gates-001/` — корень `ACTIVE`/`PLAN`, заморозивший
  спеку с вырезанным требованием, плюс квитанцию с `owner_verdict_exact`, который словами владельца
  не является. Продолжить ветку = унаследовать обе вещи;
- незакрывающийся корень — уже случившийся класс: `c-exec-grid-v1-g01-document-authority-001` висит
  `ACTIVE` на PLAN с 2026-07-21 и дважды ронял чужие ноги (`i-grid-v1-g01-dead-root-open-001`);
- **проверено, что корня на `main` НЕТ** — он существует только на этой ветке. Значит сброс ветки
  убивает его целиком, а не оставляет висеть;
- **процедура в репозитории уже есть:** `refs/backup/` содержит следы прошлых сбросов слотов,
  включая `refs/backup/pre-slot-reset/slot-win-u1`. Ничего не теряется.

**ЕГО ОКОНЧАТЕЛЬНОЕ РЕШЕНИЕ, и оно проще:** «давай тогда работать в U2, я отдельно… U1 слот сброшу,
и чтобы не терять времени, тогда в U2 будем работать». То есть разбор `WIN-U1` он берёт на себя, а
работа стартует в чистом слоте немедленно. Это лучше того, что предлагала нога: исчезает
зависимость выпущенного наряда от чужой уборки, и ждать не надо ничего.

Итог выпуска: `WIN-U2` → `c-exec-newgame-folder-and-scene-001` (проверено 2026-08-02: ветка
`slot/win-u2` на `main`, рабочее дерево чистое, аренда пустая). `WIN-U3` или `WIN-U4` →
`c-exec-gate-surface-cut-001`. `WIN-U1` — владелец, вне нарядов; **оба наряда получили прямой
запрет туда заходить**, и наряд на папку пишет каркас С НУЛЯ вместо подъёма восьми файлов с той
ветки: это несколько файлов шаблонного текста, дешевле написать, чем связывать две работы.

**Что владельцу сказано про его собственный сброс, как факт а не как указание:** чтобы мёртвый
корень исчез, нужны ОБА действия — освободить аренду в `gascoop-slot-state.v1.json` И сбросить
ветку `slot/win-u1` на `main`. Квитанции и замороженная спека лежат в ВЕТКЕ, а не в аренде: снятие
одной аренды корень не убирает.

Найдено попутно: на брошенной ветке правился `tools/root-lifecycle-check.ps1` (+45/-3) под учёт
мёртвого корня. Наряду по проверкам сказано, что эта правка сюда НЕ переносится и её отсутствие
дефектом не считается — она была сделана ради корня, которого больше нет.

## captures

Узел под копание/тоннель в дереве ещё не существует — его создаёт отдельная map-нога после того,
как владелец посмотрит переноску. До неё копание не работа, а вырез.

## decisions_needed

Нет. Все пять закрыты.

## play_check

- 1 recite: done — цель узла исход, не деятельность.
- 2 appetite: **временного потолка НЕТ, и это не пропуск шага.** Его прямой запрет: «Дат не
  назначай никаких и не приноси мне ни сроков, ни дат смерти ставки, пока я сам про них не
  заговорю». Границу держит `kill_by` его наблюдением. Приоритет он задал сам: качество, потом
  скорость.
- 3 approaches: взято из ноги s-shape-g-6b13-two-stands-bet-001 (три структурно разных подхода,
  выбраны стенды) — заново не генерировалось, потому что подход он не менял, менялась нарезка.
- 4 scope hammer: done — одиннадцать вырезов, из них два новых и крупных (копание, второй способ),
  оба его словом.
- 5 lens sweep: done — семь вердиктов. Линза 6 (надёжность работы владельца) сменила задачу: теперь
  её несёт `c-1`, потому что угрозу он назвал сам — закрывает ставку, если упрётся в возню.
- 6 riskiest assumption: `a-4` — двое держат одно тело по сети. Стоит четвёртой из одиннадцати и
  раньше exe.
- 7 tasks: одиннадцать, у каждой видимый глазами результат. **КОНФЛИКТ С ПЛЕЕМ, разрешён и назван:**
  `os/plays/shape.md` шаг 7 просит 3–7 задач, его прямое указание — «Задачи режь МЕЛКО… Каждая
  задача — это что-то, на что я могу посмотреть глазами». KERNEL строка 3 ставит живое слово
  владельца выше плея, поэтому одиннадцать. Записано во FRICTION.
- 8 kill criteria: его словами, без даты. `next_if_true`: стенды сыграны → map переписывает g-8f31
  и g-4d7a их результатами и создаёт узел копания. `next_if_false`: review.
- 9 close: карточка показана ему изменёнными строками в предыдущем сообщении; принята поправкой.
- G1: две полосы его словами, третья линия (`c-1`/`c-2`) вне полос и игрового кода не касается.
- G9: закрыт принятием-через-поправку; сила токена записана без повышения.

## FRICTION

`os/plays/shape.md` шаг 7 фиксирует 3–7 задач как число. Владелец требует резать по ВИДИМОМУ
результату, и при таком правиле число задач определяется размером узла, а не диапазоном в плее.
Второй случай этого класса — тогда maintenance; первый записан здесь.

## log

ставка перенарезана мелко и запущена: одиннадцать задач, две полосы, два наряда

## next

`c-exec-newgame-folder-and-scene-001` — `ready`, слот назван: `WIN-U2`.
`c-exec-gate-surface-cut-001` — `ready`, `WIN-U3` или `WIN-U4` по его слову при запуске.
`WIN-U1` разбирает владелец сам, вне нарядов.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-shape-g-6b13-small-tasks-and-lanes-001.md
