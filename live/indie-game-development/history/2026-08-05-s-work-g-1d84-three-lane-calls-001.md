# RESULT — s-work-g-1d84-three-lane-calls-001

call: —  (фронтир нарядов после закрытия `t-scene-2`)
direction: indie-game-development
track: direction (три полосы)
play: work
node: g-1d84
date: 2026-08-05
owner_word: «а, дальше» — вариант (а) точки пересмотра аппетита: волна идёт дальше в этом виде.

## outcome

Точка пересмотра аппетита пройдена его словом, и написаны три наряда — по одному на каждую из трёх
полос, которые он держит одновременно: **дом** (`t-house-2`), **хозяин** (`t-host-1`), **сцена**
(`t-scene-3`). До этой ноги диспатчеризуемых нарядов у направления было НОЛЬ.

Слот ни в одном наряде не выбран: его называет владелец, контракт продукта запрещает агенту выбирать.

## решение владельца — точка пересмотра аппетита

Событие, на которое она была поставлена, наступило: закрыты `t-scene-1`, `t-player-1`, `t-scene-2`.
Ему показаны три варианта (идти как есть / резать карточку на два куска / поменять порядок) с фактами
и рекомендацией (а). Ответ дословно: **«а, дальше»**.

Факты, на которых он решал, и они записаны как есть: 4 задачи из 23 закрыты за два дня; обе
рискованные догадки, ради которых точка и ставилась, ответили ДА — переноска от первого лица его
устроила, и одна сцена узким горлом НЕ стала (хозяин и дом въехали отдельными корнями, ссылок между
корнями ноль, полосы шли параллельно в разных слотах без единого столкновения за сцену). ДАТА ИЗ
ЭТОГО НЕ ВЫВОДИЛАСЬ и не будет: его запрет на даты 2026-08-02 действует, `direction_forecast`
остаётся `no_basis`.

## база и её проверка

**`main` = `origin/main` = `origin/dev` = `2eee9719af1a26c770e7d1e16d7793a2da9890cc`**, спрошено у
сервера напрямую. **Все 22 пути, которые называют три наряда, проверены на этой базе поштучно**
командой `git cat-file -e 2eee9719:<путь>` — существуют все 22, ни одного `MISS`. Правило
`i-call-named-path-absent-on-declared-basis-004` исполнено механически, а не декларативно.

**Измерения, попавшие в наряды фактами, а не пересказом:**

- Груз `_cargoLength: 2.4`, `_cargoWidth: 0.5`; проёмы дома шириной ровно **2.0** (перегородки при
  x = ±2.5 отрезками z ∈ [1; 7] и z ∈ [−7; −1]). Балка поперёк не проходит, вдоль проходит. Значит
  механика `t-house-2`, возможно, УЖЕ существует, и наряд начинается с проверки, а не со стройки.
  Это может сэкономить целую ногу.
- `HouseholderRouteController.Update()` (строка 142) копит `Time.deltaTime` и зовёт `Householder.Step`
  (147) в ОБЫЧНОМ КАДРЕ у того, у кого открыта сцена. Хозяина в снимке нет: `WalkerSnapshot` —
  `PlayerId/X/Z/Yaw/Moved`, `CargoSnapshot` — свои поля, типа снимка хозяина не существует ни одного.
- `NetworkWalkerCourier`: `OnServerTick` (120) → `_world.Advance` (126) → `PublishSnapshots` (133);
  рассылка `ReceiveSnapshotsObserversRpc` (194); клиент шлёт только ввод (137).
- `CarryWorld.Advance` (22-31): `Cargo.ApplyCarryLoad` → `Walkers.Advance` → `Cargo.Advance`.
- Odin Inspector в проекте стоит (`Assets/Plugins/Sirenix/Assemblies/**`); прецедент редакторской
  панели — `Assets/TunnelCrew/Editor/HouseholderRouteControllerEditor.cs`; своего `asmdef` у папки
  `Editor` нет.

**Слоты — перемерено против `2eee9719`, и по реестру, и по рабочим каталогам, потому что селектор
смотрит на каталог:**

| слот | реестр | дерево | HEAD | против `main` | серверная ссылка |
|---|---|---|---|---|---|
| `WIN-U1` | AVAILABLE | чисто | `adae7d40` | 2 позади / 0 | сходится |
| `WIN-U2` | AVAILABLE | 1 правка `EditorBuildSettings.asset` | `094d8f48` | 15 / 0 | `e4df0459` — предок `main`, чужого НЕТ |
| `WIN-U3` | CLAIMED | 194 файла | `094d8f48` | 15 / 0 | 22 чужих коммита |
| `WIN-U4` | AVAILABLE | чисто | `066fcba4` | 24 / 0 | 6 чужих коммитов `g-37a1` |

**Полностью готов ровно один — `WIN-U1`.** `WIN-U2` и `WIN-U4` требуют по одной уборочной операции
ДО аренды, и это названо в каждом наряде. Своей несохранённой истории нет ни у одного: все `ahead 0`.

## наряды

1. **`c-exec-g-1d84-cargo-tight-spot-001`** — полоса «дом», `t-house-2`. Место, где груз не проходит
   просто так. Начинается с проверки уже существующей геометрии; строит только если проверка покажет,
   что решения не возникает.
2. **`c-exec-g-1d84-host-under-the-judge-002`** — полоса «хозяин», `t-host-1`. Хозяина считает судья,
   он едет в снимке, на второй копии идёт так же. ЗАМЕНЯЕТ снятый `...-001`. Снимок хозяина заказан;
   второй канал/RPC/тик — СТОП И ВОПРОС ВЛАДЕЛЬЦУ (рискованная догадка 4 ставки, названная заранее).
3. **`c-exec-g-1d84-conditions-panel-001`** — полоса «сцена», `t-scene-3`. Четыре условия на этапе
   редактора, ровно четыре. Свойство «нажал Play — уже в игре без экранов» ломать запрещено.

**Что во ВСЕХ трёх стоит запретом его словом, чтобы никто не переоткрыл:** стены дома не поднимать
(«потом поднимем, когда будем делать»); хождение хозяина сквозь стены не чинить («пусть хотя ходит
через стены»); дыру `WalkSpaceProbe` не трогать («Я доказал, я в сцене, но ничего не проходит»).

## state_changes

- `NOW.md`: в `bet.appetite` дописан пройденный пересмотр с его дословным «а, дальше», фактами и
  явной пометкой, что дата не выводится; в `open_calls` зарегистрированы три новых `ready` наряда.
- Созданы три файла нарядов в `live/indie-game-development/work/`.
- Препендить ровно один LOG receipt и сохранить этот полный RESULT в `history/`.
- CHARTER/TREE/knowledge, продуктовый репозиторий, реестр слотов и ветки не менять.
- **ПАРАЛЛЕЛЬНАЯ СЕССИЯ:** на момент этой ноги `NOW.md` нёс НЕЗАКОММИЧЕННЫЕ правки чужой ноги
  `s-day-g-1d84-issues-cleanup-001` (чистка `issues`, перемер слотов, его слово «откатываем» по
  облачным сервисам, новая запись `i-slot-u3-probe-work-unbanked-001`). Они СОХРАНЕНЫ ПОЛНОСТЬЮ и не
  тронуты: своя дельта положена поверх по стабильным ключам. Единственная потеря, зафиксированная
  честно: та нога удалила заведённую этой сессией строку решения
  `d-g-1d84-appetite-review-after-three-001`; ответ владельца сохранён в `bet.appetite`, поэтому
  вердикт не потерян, но восстанавливать удалённую строку эта нога не стала.

## captures

- `WIN-U3` держит 49 незакоммиченных файлов реальной пробы бесплатных наборов (~15 МБ) — найдено
  чужой параллельной ногой и заведено ею записью `i-slot-u3-probe-work-unbanked-001`. Здесь не
  дублируется, но касается запуска четвёртой полосы.

## play_check

- Вход — его дословное «а, дальше» плюс обещание этой сессии написать три наряда по трём полосам.
- G1: полос по-прежнему пять, WIP-предел 3 не поднят; выпущено ровно три наряда — по одному
  продуктовому корню на полосу, четвёртая полоса («внешний-вид») остаётся его собственным запуском.
- G2: все три задачи служат активной ставке и стоят в её нарезке (order 5, 7, 8).
- G7: ни одно продуктовое или геймплейное решение за владельца не принято. Слот в каждом наряде
  называет он. Три его закрытых вопроса перенесены в наряды ЗАПРЕТАМИ, а не переоткрыты.
- Границы нарезки не расширены: ни одна задача не переписана, ни одного `done_when` не изобретено —
  все три взяты из `NOW.tasks` дословно и разложены на проверяемые пункты.

## next

Владелец называет слот и запускает — по одному наряду за раз или все три, как решит. Полностью готов
`WIN-U1`; `WIN-U2` и `WIN-U4` берутся после одной уборочной операции каждый, названной в нарядах.
Возврат каждого — ДОМОЙ, отдельной ногой.

END_OF_FILE: live/indie-game-development/history/2026-08-05-s-work-g-1d84-three-lane-calls-001.md
