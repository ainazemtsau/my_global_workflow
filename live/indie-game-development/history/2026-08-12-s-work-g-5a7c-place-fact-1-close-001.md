# RESULT — s-work-g-5a7c-place-fact-1-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-place-fact-1
track: householder-life
date: 2026-08-12
close: light — because все три строки `done_when` наряда суть факты, перевыведенные ЭТОЙ ногой из
  закоммиченных байтов и её собственного прогона: равенство деревьев двух откатов, состав диффа,
  нулевой дифф каталога реакций и игрового профиля, имена девяти новых headless-тестов и их зелёный
  прогон, и предъявленное исполнителем число тронутых мест сборки памяти. Ни одна строка НАРЯДА не
  требовала суждения, которого никто не выносил — наряд прямо говорил, что глазом здесь не видно
  ничего и тесты являются основным доказательством. Свежий чат опровержения не открывался.

owner_approved: |
  «закрыта задача c-exec-g-5a7c-place-fact-1-001» — 2026-08-12.

outcome: |
  **`t-place-fact-1` ЗАКРЫТА `done` СВЕТЛЫМ ПУТЁМ** на опубликованной голове
  `c1d8d3845f4c5a8c6dca49d30f5d7e8cac09f815` (`origin/main` = `origin/dev`).

  **ГЛАВНЫЙ РИСК ЗАДАЧИ СНЯТ СИЛЬНЕЙШЕЙ ФОРМОЙ.** Наряд ставил риск не на добычу знания, а на ЦЕНУ
  ПОЛЯ: `t-react-mem` заводилась ровно потому, что новое поле факта стоило семи мест правки. Отчёт
  предъявляет **ноль из пяти мест пересборки `HouseholderLocalMemory`**, а копии факта проносят
  комнату через ОДИН общий помощник. Урок прошлой волны исполнен буквально.

  **Комната приходит от плана дома, а не изобретается** — и тест закрепляет именно то правило,
  которое `HousePlan.TryFindRoomAt` документирует про пограничный случай:
  `FactRoomUsesThePlansFirstMatchAtTheEffectiveEventPoint`.

  **Неизвестная комната ведёт себя как неизвестная роль после шва** — молчит, а не роняет хозяина:
  `MissingHandlerRoomLoadsAndFallsBackWithoutThrowing`. Совместимость назад тоже закреплена:
  `OmittedHandlerRoomKeepsExactPointAccumulation` — профиль без комнаты считает по точке, как
  считал.

  **Накопление по комнате не течёт между комнатами:**
  `RoomAccumulationDoesNotBorrowOccurrencesFromAnotherRoom` и
  `RoomAccumulationCountsDistinctPointsAcrossFactExpiry`.

  **Кривое имя отвергается названно и с путём — тремя способами:** нестроковое значение, пустая
  строка и попытка положить комнату в форму комбинации, которой она не принадлежит.

  **ОТКАТ ИСПОЛНЕН ДВАЖДЫ И ОБА РАЗА СОШЁЛСЯ — ШЕСТОЙ РАЗ ПОДРЯД В ЭТОМ УЗЛЕ.** Деревья `885498e8` и
  `99cdd6e5` оба равны `b6e5d50c…` = дереву базы `a7d37b3e`; работа и reapply равны `0e70c86b…`.

  **ГРАНИЦЫ ДЕРЖАЛИСЬ ПОЛНОСТЬЮ:** ноль сцен и префабов, ноль файлов чужих дорожек
  (`Presentation/`, `Art/`, `Core/Cargo/`, `Core/Situations/`, `World/`), дифф каталога реакций
  **ноль строк**, игровой профиль не тронут вовсе. Работа целиком в трёх файлах продукта плюс тесты
  и документы.

  ## ОДНА ВЕЩЬ ОТЛОЖЕНА, И ЭТО НЕ ДЕФЕКТ

  Исполнитель отдельным коммитом записал **отложенную приёмку владельца**: игровой прогон
  `NOT RUN at BUILD close`, потому что играбельного дома с расставленными комнатами ещё нет, и
  владелец сам отложил проверку до его появления. Отчёт прямо пишет, что не выдаёт это за
  пройденное.

  **На закрытие это не влияет:** наряд не содержал строки, требующей его глаза, и прямо говорил
  обратное — «здесь глазом не видно ничего». Отложенная проверка записана как то, что делается
  ВМЕСТЕ с приёмкой дома, а не как долг этой задачи.

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ.**

  - `git ls-remote origin` → `main` = `dev` = `c1d8d384`.
  - Восемь коммитов сверх `a7d37b3e`; лестница: работа `650dc124`, revert `885498e8`, reapply
    `1b5f54e1`, запись отложенной приёмки `47f7c163`, второй revert `99cdd6e5` и его снятие
    `f7ed795b`, затем два документных.
  - Деревья: база `a7d37b3e` → `b6e5d50c…`; `885498e8` → `b6e5d50c…`; `99cdd6e5` → `b6e5d50c…`;
    работа `650dc124` и reapply `1b5f54e1` → `0e70c86b…`.
  - `.unity` и `.prefab` в диффе → **0**. Файлы чужих дорожек → **0**.
  - `git diff … -- HouseholderReaction.cs | wc -l` → **0**.
  - `git diff --stat … -- Assets/StreamingAssets/` → пусто.
  - Девять новых тестов прочитаны по именам в
    `tests/TunnelCrew.Core.Tests/HouseholderRoomFactTests.cs`.
  - `dotnet test … -c Release`, прогнано ЗДЕСЬ на выгруженном дереве публикации → **239 passed,
    0 failed, 0 skipped** против **229**.
  - Отчёт `docs/results/c-exec-g-5a7c-place-fact-1-001.md`: «The fact field changed zero of the five
    `HouseholderLocalMemory` reconstruction sites» и §Manual-acceptance с явным `NOT RUN`.

state_changes: |
  1. `t-place-fact-1`: `active → done`, журнальная строка, перенесена в `cards/closed/`.
  2. `c-exec-g-5a7c-place-fact-1-001`: `ready → done`, журнальная строка, перенесён в
     `cards/closed/`.
  3. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-place-fact-1-close-001.md`.

captures:
  - |
    ЗАПРОШЕННОГО ПОДТВЕРЖДЕНИЯ «ГОЛОВА СВЕРЕНА ПЕРЕД ПУБЛИКАЦИЕЙ» В ОТЧЁТЕ НЕТ. Строку я добавила в
    `return` именно этого наряда после сегодняшнего провала дорожки тела, и она не исполнена. По
    факту вреда нет — работа легла ровно поверх `a7d37b3e`, — но строка возврата, которую никто не
    заполняет, хуже отсутствующей. Либо требовать её явной строкой `done_when`, либо не просить.
  - |
    В WIN-U2 ЛЕЖИТ ГОТОВЫЙ НЕОПУБЛИКОВАННЫЙ КАНДИДАТ ПО ДОМУ: `e592d7c6` работа, `c2c0cc00` revert,
    `b3db9606` reapply, `7ba39fab` «record furnished house proof», дерево чистое, ветка ahead 19.
    Владелец о завершении `t-rooms-1` не сообщал, и нога его не принимает — но он лежит и ждёт, и
    именно он разблокирует отложенную приёмку по комнатам.
  - |
    ОТЛОЖЕННАЯ ПРИЁМКА СВЯЗЫВАЕТ ТРИ ЗАДАЧИ В ОДИН ПРОГОН ВЛАДЕЛЬЦА: дом даёт комнаты, «место как
    факт» даёт хозяину знание о них, тела дают, на что смотреть. Смотреть их по отдельности дороже,
    чем один раз вместе, и это стоит предложить ему при приёмке дома.

decisions_needed: []

play_check:
  - 1 recite — done. Шесть строк карточки и три строки наряда пересказаны.
  - 2 owner inputs — не требуется: наряд не содержал строки, судимой глазом, и прямо это говорил.
  - 3 do the work — приёмка возврата.
  - 4 self-check — done. Каждая строка перевыведена здесь; отложенная приёмка проверена как НЕ входящая в объём наряда, а не проигнорирована.
  - 5 close — done, светлым путём; основание записано в поле `close` по тесту плея.
  - G5 — выполнен светлым закрытием.
  - G7 — done. Ни одного игрового решения не принято.
  - G10 — done. RESULT финальный.

log: t-place-fact-1 закрыта СВЕТЛЫМ путём на c1d8d384, и главный риск задачи — цена поля — снят сильнейшей формой: НОЛЬ из пяти мест пересборки памяти тронуто, копии факта проносят комнату через ОДИН общий помощник, то есть буквальный урок t-react-mem исполнен; комната приходит от плана дома и следует его же правилу про точку на общей стене, неизвестная комната ведёт себя как неизвестная роль после шва — молчит, а не роняет хозяина, — совместимость назад закреплена отдельным тестом, накопление по комнате не течёт между комнатами, а кривое имя отвергается названно тремя способами; откат исполнен ДВАЖДЫ и оба раза сошёлся с деревом базы, шестой раз подряд; 239/239 прогнаны здесь против 229; ноль сцен, префабов и чужих файлов, каталог реакций и игровой профиль не тронуты вовсе; отложенная приёмка владельца записана исполнителем честно как NOT RUN и на закрытие не влияет, потому что наряд строки под его глаз не содержал; сверх этого замечено, что запрошенное подтверждение про сверку головы перед публикацией в отчёте отсутствует, и что в WIN-U2 лежит готовый неопубликованный кандидат по дому

next: |
  **В WIN-U2 лежит готовый неопубликованный кандидат по дому** (`t-rooms-1`): работа, откат,
  повторное применение и «record furnished house proof», дерево чистое. Владелец о завершении не
  сообщал; когда сообщит — принимать обычным порядком.

  Его публикация разблокирует сразу три вещи: отложенную приёмку по комнатам, приёмку тел на
  собранном доме и улику про сквозной проход груза, которая ждала именно дома.

  Идут: тело добивается в WIN-U1. Свободны и без нарядов: `t-sight-1`, `t-occupations-1`.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-place-fact-1-close-001.md
