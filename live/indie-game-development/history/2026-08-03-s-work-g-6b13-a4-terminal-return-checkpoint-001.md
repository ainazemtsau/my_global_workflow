# RESULT — s-work-g-6b13-a4-terminal-return-checkpoint-001

call: c-exec-two-carry-one-physical-cargo-proba-001
direction: indie-game-development
track: переноска
play: work
node/task: g-6b13 / a-4
date: 2026-08-03

## outcome

Терминальный продуктовый возврат a-4 принят в Direction OS как evidence checkpoint. Двое держат один
физический груз, продуктовая exact-history опубликована, WIN-U3 освобождён. Задача a-4 намеренно
остаётся открытой, и на этот раз пробел ШИРЕ, чем у a-3: отсутствует не только binding fresh G5, но и
половина owner-eye — владелец не сказал ни слова про пункт 2 `done_when` (балка в проёме; мышь не
входит в стену), а перепроверка второго хвата после тюнинга `b7b8eaa0` им не делалась. Нога вернула
это открытым текстом вместо того, чтобы вывести из «работает», и это правильное поведение.

Инженерный CALL снят с горячего frontier и заменён одним fresh-session closing CALL той же полосы,
который несёт обе половины: получить недостающие пункты ЕГО словами, затем пытаться опровергнуть все
три пункта по exact-коду.

Отдельно записан его вопрос про вид от первого лица — как он и просил, заметкой без блокера, но с
названной вслух ценой, которой в его вопросе не было: дёшево меняется КОД, а не ВЕРДИКТ по стенду.

## evidence

- Дословная приёмка владельца в опубликованном отчёте: «два взялись, притом второй как-то не сразу
  взялся, но потом взялся»; «когда разъединяется, ну связь, видно, что разъединяется»; «два взяли,
  один отходит, у него разрывается. Это в принципе тема работает». Это пункт 1 `done_when`.
- Его же слова про заметку: «Не надо там прям блокеры ставить, но заметку оставь»; и про камеру:
  «меня уже начинает немного смущать камера сверху… мы же типа вверх подымать должны, вниз».
- Fresh product readback, проверено первой рукой: `origin/main` = `origin/dev` = `4efbd70c`.
- Exact-history: basis `37519f52`; кандидат `49893ea7` (продукт), `65540566` (отчёт), `b7b8eaa0`
  (тюнинг после первого Play), `dc5d48b0` (приёмка); `git merge-base --is-ancestor dc5d48b0
  origin/dev` = 0; `git log origin/dev` показывает все четыре SHA подряд без merge-коммита —
  интеграция была чистым fast-forward `37519f52..dc5d48b0`, история не переписывалась.
- `slot/win-u3` = `dc5d48b0`, предок `origin/dev`: непубликованных байтов не осталось.
- Слот-реестр `C:/projects/Unity/GasCoopGame_slot-state/gascoop-slot-state.v1.json`: WIN-U3
  `AVAILABLE / lease none`. WIN-U1 `CLAIMED` под `c-exec-host-rustle-and-urgent-guard-proba-001` —
  это параллельная нога полосы хозяина, ожидаемо.
- СВОЙ ПРОГОН на exact-кандидате в `GasCoopGame_win-u3@dc5d48b0`:
  `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` →
  «не пройдено 0, пройдено 38, пропущено 0, всего 38». Дерево слота было и осталось чистым.
- Четыре блоба движения на кандидате идентичны basis: `MovementInput.cs 6427b809`,
  `PlayerMovement.cs 2346fe65`, `PlayerMovement.Rule.cs bfe9aa1d`, `PlayerState.cs df4a754f`.
- `git diff --stat 37519f52 dc5d48b0`: 36 файлов, +2398/−66, все пути внутри разрешённой нарядом
  поверхности (`Core/Cargo/**`, `Core/Multiplayer/**`, `Network/**`, `Scenes/NetworkWalkers.unity`,
  `tests/TunnelCrew.Core.Tests/Cargo*.cs`, собственный `docs/results/**`).
  `Packages/**`, `ProjectSettings/**`, `tools/**`, `validation.config`, `AGENTS.md` и полоса
  хозяина не тронуты ни строкой.
- Опубликованный `docs/results/c-exec-two-carry-one-physical-cargo-proba-001.md` несёт статус
  `DELIVERED on dev`, рабочий масштаб одним числом (1 юнит = 1 метр), шесть классов исправлений от
  двух независимых read-only вычиток, все семь полей и — важнее всего — САМ называет, чего в
  приёмке владельца нет.
- ЧЕГО НЕТ: отдельной свежей физической сессии, которая пыталась бы опровергнуть все три пункта; и
  слов владельца по пункту 2. Поэтому builder-return guard не даёт закрыть задачу.
- НЕ ПЕРЕПРОВЕРЯЛ ГЕЙТЫ СВОЕЙ РУКОЙ, и вот честная причина: локальная `dev` в `GasCoopGame_dev` уже
  ушла на два коммита вперёд `origin/dev` — там merge параллельной ноги полосы хозяина, — а чтобы
  прогнать `check.ps1` ровно на опубликованном `4efbd70c`, пришлось бы трогать живой рабочий
  каталог занятой полосы. Прогон гейтов остаётся заявлением ноги; независимо проверены git-факты,
  блобы, поверхность диффа и headless-тесты на exact-кандидате.
- Локальная ветка `main` в основном чекауте на 64 коммита позади `origin/main` и является его
  предком — расхождения нет, но следующая Control-транзакция обязана сначала её подтянуть.

## state_changes

- `NOW.md`: оставить a-4 `open`; добавить ей поле `product_return` с exact-history, опубликованным
  tip, своим прогоном тестов, неизменными блобами, дословной приёмкой владельца, названным пробелом
  (пункт 2 не смотрелся, второй хват не перепроверялся после тюнинга) и известным ограничением
  скорости груза; обновить `updated` на эту сессию.
- `NOW.md/open_calls`: удалить вернувшийся engineering CALL
  `c-exec-two-carry-one-physical-cargo-proba-001`; зарегистрировать ready same-lane continuation
  `c-work-a4-close-verification-001`. Открытый наряд полосы хозяина
  `c-exec-host-rustle-and-urgent-guard-proba-001` сохранить без изменений.
- `NOW.md/issues`: добавить `i-first-person-view-never-chosen-001` (level execution, route shape) и
  `i-unity-evidence-rests-on-owner-launch-001` (level execution, route work); обновить
  `review_when` у `i-verification-guards-unwired-not-deleted-001` решением «не возвращать сейчас»
  с точным триггером возврата.
- Создать полный CALL-файл
  `live/indie-game-development/work/c-work-a4-close-verification-001-call.md`.
- Препендить LOG receipt и сохранить этот полный RESULT в history. CHARTER, TREE, knowledge,
  остальные задачи, forecast и продуктовый репозиторий не менять.

## captures

- Отчёт называет `RequestedState` («что просят руки») серединой между местами, где СТОЯТ носильщики,
  а не тем, где окажется центр груза. Шов без потребителя, названный, чтобы следующая нога не
  приняла одно за другое. Не действовать, помнить.
- Груз перестал подниматься над головой (в a-3 взлетал на 2.4) — прямое следствие настоящего
  физического тела на плоском стенде. Замена сделана: держимый груз светлеет. Владелец на это не
  реагировал ни в одну, ни в другую сторону.
- Камера кадрирует только ходоков: брошенный далеко груз может уехать за край экрана. Связано с
  вопросом про вид от первого лица, но отдельно от него.

## decisions_needed

**Вид от первого лица — где он встаёт в очередь.** Владелец прямо попросил разъяснить, когда
появится первое лицо, и запретил ставить блокер. Разъяснение дано; решение за ним.
Варианты, с ценой и риском:

1. **Поставить отдельной мелкой задачей полосы переноски ДО вердикта по стенду** (рекомендация).
   Цена — один добавочный корень: направление взгляда в авторитетном состоянии и снимке, камера на
   каждого клиента вместо одной общей, мышь-взгляд, оси поворачиваются до `PlayerMovement.Step`.
   Правило движения не ломается, курьер не трогается. Зачем: вердикт «годится / не годится» по
   стенду переноски, выданный на виде сверху, может не перенестись на игру от первого лица —
   сверху видно обоих носильщиков и оба конца груза, а вся механика построена на том, что концы
   тянут в разные стороны.
2. **После a-5 и a-6, до закрытия ставки.** Дешевле по вниманию сейчас, но вердикт по стенду он
   будет выносить на виде сверху.
3. **Не в этой ставке вообще.** Тогда в записи прямо стоит, что стенды судятся сверху сознательно.

Заводит задачу `shape`, а не эта нога; до его слов ни одна строка отсюда не становится `done_when`.

## play_check

- 1 recite: done — цель a-4, её `done_when` и подписанная архитектура перечитаны из свежего NOW и
  исходного CALL.
- 2 owner inputs (owner): done — его дословная приёмка и его прямое указание про заметку без
  блокера взяты из опубликованного продуктового отчёта; недостающие owner-eye пункты не выведены, а
  названы как недостающие и вынесены в continuation CALL.
- 3 do the work: done — продуктовый возврат сопоставлен с исходным CALL; git-факты, exact-history,
  блобы движения, поверхность диффа и headless-тесты на exact-кандидате перепроверены первой рукой;
  сработавший триггер `i-verification-guards-unwired-not-deleted-001` решён и записан.
- 4 self-check: пункт 1 `done_when` покрыт словами владельца; пункт 3 покрыт своими измерениями,
  кроме прогона гейтов, который остаётся заявлением ноги по названной причине; пункт 2 не покрыт
  ничем. Поэтому checkpoint, а не ложный Direction-close.
- 5 close: done — вернувшийся engineering root заменён одним same-lane fresh closing CALL; новых
  задач не заведено, очередь полосы не переставлена, вопрос про первое лицо ушёл в
  `decisions_needed` и в issue с маршрутом shape.

## log

g-6b13/a-4: продуктовая ПРОБА «двое несут один физический груз» опубликована на 4efbd70c и
освободила WIN-U3; владелец подтвердил глазами только пункт 1, пункт 2 (проём и стена) не смотрел
вовсе; возврат применён как checkpoint, задача оставлена открытой, выпущен один closing CALL, а его
вопрос про вид от первого лица записан заметкой без блокера с названной ценой.

## next

CALL `c-work-a4-close-verification-001` — открыть в НОВОЙ физической задаче и вставить содержимое
`live/indie-game-development/work/c-work-a4-close-verification-001-call.md`. Его PASS закрывает a-4.
До PASS следующую задачу полосы переноски не запускать.

END_OF_FILE: live/indie-game-development/history/2026-08-03-s-work-g-6b13-a4-terminal-return-checkpoint-001.md
