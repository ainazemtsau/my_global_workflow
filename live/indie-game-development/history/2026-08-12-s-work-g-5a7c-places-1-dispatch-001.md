# RESULT — s-work-g-5a7c-places-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-places-1
date: 2026-08-12
call: c-work-g-5a7c-places-1-001
outcome_kind: checkpoint — наряд выдан, задача НЕ закрыта

owner_approved: |
  «поехали, пиши наряд на шов и выдавай» — 2026-08-12, сразу после подписи ставки
  («Подписываю, открывай ставку»).

  **FRICTION, названная вслух:** AGENTS.md говорит «одна атомарная нога = одна работа, обычно один
  физический чат содержит одну ногу и кончается коммитом». Эта нога `work` идёт в том же чате, что
  и закрытая нога `shape`. Основание — порядок власти KERNEL: «live owner instruction > this kernel»,
  и его инструкция была прямой. Цена и выгода названы: свежих замеров нога не потеряла (наоборот,
  весь продуктовый контекст перемерен сегодня первой рукой), но следующая нога — возврат исполнителя —
  обязана быть отдельным чатом, и биндинговая проверка тем более.

outcome: |
  Наряд `c-exec-g-5a7c-places-1-001` на общую задачу-шов `t-places-1` написан и выдан в слот
  **WIN-U2** от опубликованной головы `f3c40c40`. Задача переведена в `active`, CALL `work`
  переведён в `waiting_on` наряда.

  **ЧТО ДАЛА ВЫДАЧА СВЕРХ ПЕРЕПИСЫВАНИЯ КАРТОЧКИ — три измеренных факта, изменивших наряд:**

  1. **ГЛАВНЫЙ ГЕЙТ ПЕРЕПИСАН ПОД ТО, ЧТО ВООБЩЕ КОМПИЛИРУЕТСЯ.** Тестовый проект компилирует весь
     `Assets/TunnelCrew/Core/**` (зеркальный `core/TunnelCrew.Core.csproj` с
     `EnableDefaultCompileItems=false`) плюс ПОИМЁННО
     `Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs` и четыре файла
     `Editor/HouseholderObservation*`. Он НЕ компилирует `World/**` и `Network/**` — это
     MonoBehaviour'ы. Значит гейт «один профиль в двух домах» доказуем headless ТОЛЬКО если словарь
     ролей пересекает границу как engine-free данные, а не достаётся через MonoBehaviour. Это
     записано в `done_when` 2 как требование к ДОКАЗАТЕЛЬСТВУ, а не как архитектурное предписание:
     «проверено в редакторе» строку не закрывает.
  2. **ЗАКРЫТ ОТКРЫТЫЙ ВОПРОС 2026-08-11 ПРО ТО, КУДА СМОТРЯТ МОСТЫ.** Тогда нога честно записала,
     что доказать каталог редактора нельзя, потому что оба слотовых дерева стояли на одном коммите.
     Сегодня они разошлись, и разница проверена дёрганьем инструмента: мост WIN-U2 находит
     `Assets/TunnelCrew/Scenes/HouseholdLab.unity` (guid `6d52ee3ef501a5a4c82c11185afedd68`), мост
     WIN-U1 не находит ничего, а этой сцены на `a5a84350` и не существует. Мосты смотрят в разные
     деревья. Оба редактора живы, Unity `6000.5.5f1`, не в Play, не компилируют.
  3. **ПРЕФАБ И СЦЕНЫ ЗАПРЕЩЕНЫ НАРЯДОМ НЕ ИЗ ОСТОРОЖНОСТИ, А ПО ДВУМ ПРИЧИНАМ СРАЗУ:** они
     принадлежат дорожке `house-and-things` (в `g-1d84` полосы разошлись на файле сцены дважды), и
     `IntegratedHouse.unity` с `HouseholdLab.unity` копируются в тестовый проект ФИКСТУРАМИ, то есть
     правка сцены ломает scene-fixture тесты. При этом добавить сериализуемое ПОЛЕ роли в
     `HousePlaceMarker.cs` разрешено — это код, а не сцена; гейт от значений в префабе не зависит.

  **СЕМЬ СТРОК КАРТОЧКИ СВЁРНУТЫ В ТРИ СТРОКИ БЕЗ ПОТЕРЬ** — потолок контракта 36 для инженерного
  наряда: (1) словарь работает и неизвестная роль молчит = строки 1+2+3; (2) главный гейт headless =
  строка 4; (3) ничего не сломано и откат настоящий = строки 5+6+7.

  **РЕЖИМ — ПРОБА.** Контракт 36: ПРОБА по умолчанию, и ни один агент не переводит ногу в ОПОРУ сам,
  только слово владельца. Названо ему вслух, потому что этот шов — именно то, на чём будут строить
  три другие задачи, и соблазн взять ОПОРУ есть; правило контракта прямое и ноге его не обходить.

evidence: |
  **ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ ПЕРВОЙ РУКОЙ 2026-08-12.**

  Базис и слоты:
  - `git ls-remote origin` → `origin/main` = `origin/dev` = `origin/slot/win-u2` =
    `f3c40c4010ee2f0f700cdf1bfbcea14a301d488c`; `origin/slot/win-u1` = `43648b2d`.
  - `tools/select-slot.ps1` по всем четырём слотам: **все `CLEAN / AVAILABLE / lease none`.**
    WIN-U1 `head a5a84350`, WIN-U2 `head f3c40c40`, WIN-U3 и WIN-U4 `head 352f96b0`.
    Выбран WIN-U2 — его дерево уже на опубликованной голове, догонять нечего; WIN-U1 оставлен
    свободным под параллельную `t-body-1`.
  - `mcp-endpoint: unrecorded` во всех четырёх записях файла состояния — устаревшее поле, не факт о
    мире: оба моста ответили.

  Прогон:
  - `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` →
    **214 passed, 0 failed, 0 skipped**, прогнано здесь на дереве `f3c40c40`. Это базовое число
    наряда.

  Место смерти хозяина:
  - `Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs:30` — `bool IsKnownPlace(string name)` в
    интерфейсе; `:65-69` — реализация `return false` с комментарием `// Deliberately empty. t-host-2
    connects the house dictionary through this method.`; `:736-744` — бросок при `placeName != null
    && !references.IsKnownPlace(placeName)`.
  - `docs/householder-profile-format.md` — следствие дословно: «the host port remains unreadable.
    The authoritative Core therefore never seats or publishes that householder».

  Что про дом уже построено:
  - `Core/House/HousePlan.cs`, `Core/House/IHousePlanSource.cs`, `World/SceneHousePlan.cs`,
    `Network/NetworkHouseholder.cs:42,80,102`.
  - `World/ThreeRoomHouse.prefab`: три комнаты, семь мест, два проёма `_width: 2`.

  Компилируемая поверхность:
  - `core/TunnelCrew.Core.csproj` и `tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj`
    прочитаны целиком; список `Compile Include` и список фикстур `None Include` перечислены в наряде.

  Гейты:
  - `tools/hygiene.ps1` зелёный по последнему замеру review; `tools/check.ps1 -Deliver` красный на
    семи исторических отчётах — зарегистрированный `i-deliver-gate-red-when-main-catches-dev-001`,
    и наряд прямо запрещает исполнителю считать это своим провалом.

  **G5:** эта нога ничего не закрывает — она выдаёт наряд. Биндинговая проверка не требуется и не
  выдаётся за существующую.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-places-1-001-call.md` — полный текст наряда.
  2. Создана `cards/c-exec-g-5a7c-places-1-001.md` — `status: ready`, `slot: WIN-U2`,
     `basis: f3c40c40`, `engineering_contract: 36`.
  3. `c-work-g-5a7c-places-1-001`: `ready → waiting`, `waiting_on: c-exec-g-5a7c-places-1-001`,
     журнальная строка.
  4. `t-places-1`: `ready → active`, добавлен блок `## журнал` с журнальной строкой.
  5. `t-body-1`: `ready → open` — приведение к словарю статусов задачи из
     `os/schema/direction-files.md` (`open | active | blocked | done`); `ready` принадлежит CALL, а
     не задаче. Это исправление собственной ошибки предыдущей ноги, а не смена смысла: задача
     по-прежнему не заблокирована швом и может идти параллельно.
  6. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-places-1-dispatch-001.md`.

captures:
  - |
    ПОЛЕ `mcp-endpoint: unrecorded` В ФАЙЛЕ СОСТОЯНИЯ СЛОТОВ ВРЁТ ПРО МИР по всем четырём слотам:
    мосты WIN-U1 и WIN-U2 отвечают. Это второй раз, когда нога ловит это поле как факт о ФАЙЛЕ,
    прочитанный как факт о МИРЕ (первый — 2026-08-11). Класс уже известен; если поймается третий
    раз, это MAINTENANCE на файл состояния, а не на ногу.
  - |
    Все одиннадцать новых карточек задач третьей волны несут поля шапки `kind` и `order`, которые
    `osctl check` называет «вне известных». Это НЕ новая ошибка: ровно те же два поля несут все
    двадцать задач первой и второй волн. Класс общий и лечится одной репаркой на все задачи
    направления, а не правкой новых карточек в разнобой.
  - |
    Тестовый проект копирует `IntegratedHouse.unity` и `HouseholdLab.unity` фикстурами. Значит любая
    будущая задача, правящая сцену, автоматически становится задачей, правящей вход scene-fixture
    тестов. Это стоит помнить при выдаче `t-rooms-1` и `t-house-asset-1`.

decisions_needed: []

play_check:
  - 1 recite — done. Цель и семь строк `done_when` карточки `t-places-1` пересказаны; обязательство ставки названо: это общая задача-шов, пока она идёт, две дорожки из трёх не заняты.
  - 2 owner inputs — не требуется, и вот почему: владелец по этому шву уже сказал всё, что только он мог знать («давай, наверное, роли попробуем» и постановка про «хозяин начнёт боить в другом доме»). Ни одна строка наряда не требует факта, которого нет в состоянии или в байтах.
  - 3 do the work — done. `call:executor` выпущен с goal, context, boundaries, done_when, return и budget; направление задало ИСХОД и планку, техническое КАК оставлено исполнителю; названный подход зафиксирован ОДНИМ токеном без дизъюнкции, отход от него маршрутизирован в ESCALATE.
  - 4 self-check — done. Три строки контракта покрывают все семь строк карточки, соответствие расписано поимённо; бюджет контракта 36 (не больше трёх строк) соблюдён без овербюджетного токена владельца.
  - 5 close — checkpoint. Задача НЕ закрыта: наряд выдан, возврата нет. CALL `work` остаётся открытым в статусе `waiting`.
  - G5 — не применяется: нога ничего не закрывает.
  - G7 — done. Ни одно игровое ожидание ногой не придумано; режим ПРОБА не выбран ногой, а взят как умолчание контракта и назван владельцу.
  - G10 — done. Шаги видны, RESULT финальный.

log: наряд на общую задачу-шов «роли мест» выдан в WIN-U2 от опубликованной головы f3c40c40, и выдача дала три вещи сверх переписывания карточки: главный гейт переписан под то, что вообще компилируется — тестовый проект собирает Core и загрузчик профиля, но НЕ собирает World и Network, поэтому доказательство «один профиль в двух домах» обязано быть headless, а значит словарь ролей обязан пересекать границу engine-free данными, и «проверено в редакторе» строку не закрывает; закрыт открытый с 11 августа вопрос про то, куда смотрят мосты — деревья слотов наконец разошлись, и мост WIN-U2 находит HouseholdLab, которого на дереве WIN-U1 не существует, то есть мосты действительно разные и выбран нужный; и запрет на сцены обоснован вторым основанием сверх дорожечного — обе сцены копируются в тестовый проект фикстурами, поэтому правка сцены ломает scene-fixture тесты, при этом поле роли в маркере разрешено, потому что это код, а не сцена; семь строк карточки свёрнуты в три строки контракта 36 без потерь, база прогона 214/214 замерена здесь, а красный Deliver на семи исторических отчётах назван заранее как чужой процессный дефект, чтобы исполнитель его не чинил

next: |
  Ждём возврата исполнителя по `c-exec-g-5a7c-places-1-001`. Первая строка возврата обязана быть
  распиской с числом полученных строк `done_when` (три) и назвать базис.

  **Возврат разбирается в ОТДЕЛЬНОМ свежем чате** — эта нога уже вторая в своём физическом чате, и
  третьей там быть не должно.

  Параллельно и не дожидаясь шва может идти `t-body-1` в слоте WIN-U1: дорожка `body` не трогает ни
  правил, ни профиля, ни сцен, и швом не блокируется. Её наряд ещё не написан.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-places-1-dispatch-001.md
