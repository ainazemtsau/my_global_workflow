# RESULT — s-research-g-5a7c-cargo-audit-v2-001 (play: research)

## outcome

**Аудит проверен состязательно тринадцатью агентами и исправлен. Одно утверждение подтверждено
целиком, пять поправлены — и все пять поправок в ту сторону, что было ХУЖЕ, чем писало Направление.**

**Главная поправка: потолок в пять раз ниже.** Не 100-150 предметов на дом, а **26**. Узкое место —
не канал, а размер ОДНОГО сообщения: рабочий предел Tugboat 1280 байт, снимок 45 байт, постоянная
часть ~105. На 27-м предмете сообщение принудительно расщепляется и уходит по НАДЁЖНОМУ каналу с
окном 64 пакета; на 500 предметах это 19 датаграмм каждому наблюдателю каждый такт, окно
исчерпывается за 112 мс, начинается блокировка головы очереди. **Игра не падает — она тонет.**

**Вторая поправка отменяет собственное предложение Направления.** Фильтровать по готовому флагу
`Moved` НЕЛЬЗЯ: он дельта ПОЗЫ, а не состояния. Предмет, схваченный телекинезом и ещё не
стронувшийся, под фильтром не уедет — и цвет захвата, единственная видимая обратная связь
телекинеза, молча отвалится ровно в тот момент, ради которого существует. Он же пороговый, поэтому
оседающий предмет даст цикл создания-уничтожения вида несколько раз в секунду.

**Третья поправка нашла то, чего Направление не искало вовсе, и это самое дешёвое место аудита.**
Отсечения по стенам в игре нет: игрок в гостиной платит за все 139 объектов дома вместо 13. При
десяти комнатах — множитель 10. А дом УЖЕ сгруппирован по комнатам в префабе, значит гашение
закрытой комнаты это `SetActive` на поддереве.

**Четвёртая: потолок спящих ставит не физика, а линейный поиск** — квадрат от числа предметов на
каждом такте, в двух местах сразу. Словарь вместо списка, двадцать строк, уводит потолок с 1500 к
5000-10000.

## evidence

Тринадцать агентов, 0 ошибок, 648 инструментальных вызовов, только чтение. Journal:
`subagents/workflows/wf_a021fbf0-5b9/journal.jsonl`.

Ключевые числа, каждое с цитатой в отчётах агентов: снимок 45 байт (`Writer.cs:399` пакует целые,
`:484` не пакует float — 40 из 45 байт это float); предел сообщения 1280 (`Tugboat.cs:583`);
расщеплённое уходит `Channel.Reliable` (`TransportManager.cs:601,197`); дельта-сериализатор у FishNet
есть в пакете и не подключён (`Runtime/Serializing/Writer.Delta.cs`); `Moved` пишется и не читается
нигде; отсечения нет (`m_OcclusionCullingData: {fileID: 0}`, GPU Resident Drawer выключен, инстансинг
выключен на всех 14 материалах); треугольников во всей сцене ≈36 000.

После починки: 28 байт на предмет, потолок сообщения 41 движущийся, канала 235, **а число предметов
в доме сетью не ограничено вовсе**.

## state_changes

- `work/2026-08-17-audit-cargo-for-hundreds-v2.md` — исправленный аудит целиком, с планом на
  ~4.25 ноги и с разделом «чего не проверяли прогоном».
- `card block i-cargo-transport-caps-the-game-at-150-things-001` — потолок исправлен со 150 на 26.
- `card new i-no-occlusion-culling-house-pays-tenfold-001` — находка про отсечение.

## captures

**Порядок работ изменился по итогам проверки.** Первой идёт не «настоящий сон», а замена списка на
словарь: ~0.25 ноги, ноль изменений поведения, снимает квадрат сразу в двух местах. Дальше сон,
дельты, сжатие, интерес по комнатам.

**Метод сработал ровно там, где должен.** Направление ошиблось оптимистично в пяти утверждениях из
шести; ни одну из этих ошибок не поймал бы чат, который проверяет сам себя.

## decisions_needed

Его решение о запуске четырёх с половиной ног параллельно волне скриншотов. Пересечений с волной нет.

## play_check

Ресёрч в продуктовый репозиторий не писал, слота не занимал, волну не двигал. Все поправки к
собственному аудиту названы поправками, а не «уточнениями».

## log

аудит проверен состязательно и исправлен: потолок не 150 предметов, а 26, и фильтр по Moved был бы багом

## next

Его решение. Первой ногой — словарь вместо списка.
