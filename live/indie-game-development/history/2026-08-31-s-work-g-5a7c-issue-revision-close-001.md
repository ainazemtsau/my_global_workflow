# RESULT — s-work-g-5a7c-issue-revision-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-issue-revision-1, вне полос волны
track: —
date: 2026-08-31
outcome_kind: work — склад улик перемерен целиком: 38 улик, 9 мертвы, 13 несут устаревший замер, и гниль оказалась вдвое обширнее, чем показала выборка направления

close: light — because каждая строка приёмки перевыведена направлением первой рукой. Три замера исполнителя выбраны наугад и повторены командами направления на том же объекте `b22c00a2` — все три сошлись до значения. Чистота обоих рабочих деревьев и состояние слота прочитаны напрямую. Число куч сложено и сошлось с заявленным. G5-чат не открывался.

owner_approved: |
  Задача заведена его словом 2026-08-31 «давай ревизию». Закрытие по перевыведенному факту —
  работа писателя, отдельного слова не требует.

  Ни одна строка `done_when` не изменена.

outcome: |
  **ГНИЛЬ ОКАЗАЛАСЬ ВДВОЕ ОБШИРНЕЕ, ЧЕМ ПОКАЗАЛА ВЫБОРКА.** Направление проверило перед нарезкой
  шесть улик и нашло четыре протухшие. Полный проход даёт **22 протухших из 38** — девять мертвы
  целиком, тринадцать несут неверный замер. **Больше половины склада.** Решение потратить слот на
  перезамер вместо починки было верным, и теперь это не догадка.

  ```
  УМЕРЛА                    9
  ЖИВА КАК ЗАПИСАНА        10
  ЖИВА, НО ЗАМЕР ДРУГОЙ    13
  НУЖЕН ЖИВОЙ ПРОГОН        6
                          ---
                           38
  ```

  **САМЫЙ ЦЕННЫЙ ЕДИНИЧНЫЙ ЗАМЕР ПЕРЕВЕРНУЛ ВЫВОД НАПРАВЛЕНИЯ.** Утром направление прочитало
  улику про косяки, увидело `capsuleRadius: 0.25` вместо записанных `0.5` и заключило, что дефект
  подобрел. **Неверно.** Исполнитель померил вторую половину: проёмов теперь семь, шириной
  0.8…1.4 вместо трёх по 1.5. Запас на сторону упал с **0.25 м до 0.15 м** — стало ТЕСНЕЕ, а не
  свободнее. Направление посмотрело на одну переменную из двух и сделало вывод по половине. Это
  ровно та ошибка, ради которой ревизия и затевалась, и её поймали на самом заказчике.

  **ГНИЛЬ ИМЕЕТ ФОРМЫ, И ИХ БОЛЬШЕ ЧЕТЫРЁХ.** Утреннее наблюдение назвало четыре способа
  протухнуть; полный проход добавил пятый и шестой: **улика частично умерла** (одна её половина
  больше не воспроизводится, вторая жива — «standing-layer», «occupation-status»,
  «executor-paraphrase») и **предмет переехал из закреплённого в пробу** («core-structural»,
  «world-marks»: `Core/` → `Probe/`, то есть путь в улике указывает в пустоту).

  **ИСПОЛНИТЕЛЬ НАШЁЛ ДЕФЕКТ В РЕПОЗИТОРИИ НАПРАВЛЕНИЯ, ХОТЯ ЕМУ БЫЛО ЗАПРЕЩЕНО ТУДА ПИСАТЬ.**
  Две карточки лежат в `closed/`, но несут `status: open`. Направление проверило и нашло
  **четыре**, а не две: он не увидел `d-what-kill-threshold-4-measures-001` и намеренно исключил
  `i-knowledge-still-says-telekinesis-is-an-ability-001` как относящуюся к направлению. Все четыре
  исправлены этой ногой. **Второй раз за день ревизия ловит расхождение записи и реальности
  внутри самого направления.**

  **ТРИ САМЫЕ ДОРОГИЕ ЖИВЫЕ НАЗВАНЫ, И ДВЕ ИЗ ТРЁХ НЕЛЬЗЯ БРАТЬ ПАРАЛЛЕЛЬНО** — их поверхности
  почти совпадают (`Householder.cs`, конвейер решений, загрузчик, снимок, сеть). Третья —
  защита общего склада арта — стоит особняком и **не пересекается ни с одной идущей ногой**.

evidence: |
  **ПЕРЕВЫВЕРЕНО НАПРАВЛЕНИЕМ ПЕРВОЙ РУКОЙ** на том же Git-объекте `b22c00a2`, тремя замерами,
  выбранными наугад из тридцати восьми:

  - **проёмы и радиус** — `HouseholderWalkSettings.asset:32` → `capsuleRadius: 0.25`;
    `FirstHouse.prefab` → семь `_width`: `0.8, 0.8, 0.9, 1, 0.8, 1.4, 0.8`. Сходится до значения,
    и минимальный запас `(0.8 − 0.5) / 2 = 0.15` подтверждён арифметикой;
  - **случайности нет** — поиск по `System.Random`, `UnityEngine.Random`, `Random.Range` в
    `Assets/TunnelCrew/**/*.cs` даёт пусто;
  - **ключей профиля семь, `gauges` отсутствует** — перечислены поимённо: `formatVersion`,
    `profileId`, `routine`, `reactions`, `activeResponseRules`, `behaviorBranches`,
    `decisionHandlers`.

  **Чистота и слот, прочитаны напрямую:** `git status --porcelain` пуст в
  `GasCoopGame_win-u2`; пуст в репозитории направления; `WIN-U2` → `AVAILABLE`, `lease: none`.
  Ни одного продуктового байта не изменено — приёмка 3 выполнена.

  **Арифметика куч:** 9 + 10 + 13 + 6 = 38, сходится с заявленным.

  **Дефект направления:** обходом `closed/` найдено ЧЕТЫРЕ карточки со `status: open` —
  `i-alarm-binds-to-weight-class-not-to-the-thing-001`,
  `i-loot-candidate-emits-stale-contact-on-pickup-001`,
  `i-knowledge-still-says-telekinesis-is-an-ability-001`,
  `d-what-kill-threshold-4-measures-001`.

state_changes: |
  ЗАКРЫТО ПО ПЕРЕВЫВЕДЕННОМУ ФАКТУ (2):
  - `t-issue-revision-1` → `done`;
  - `c-exec-g-5a7c-issue-revision-1-001`.

  ЗАКРЫТО КАК УМЕРШИЕ (7 живых улик):
  - `i-a-seeded-mine-still-lives-in-dev-001` — в `main` правильное `2L`;
  - `i-bedside-radio-spawns-inside-the-mattress-001` — строки расстановки с тем определением нет;
  - `i-house-floors-have-no-colliders-001` — полы строятся примитивом с коллайдером;
  - `i-house-plan-in-the-game-has-no-furniture-001` — план несёт мебель, тело объявляется;
  - `i-householder-tests-load-profile-without-a-house-001` — тесты грузят профиль с планом;
  - `i-measurement-taken-in-a-scene-that-does-not-ship-001` — сцена в списке сборки;
  - `i-quiet-noise-is-unreachable-in-the-built-game-001` — контакт и пороги на месте.

  ПОМЕЧЕНО «ЗАМЕР УСТАРЕЛ» (13 улик) — журнальной строкой, указывающей на этот отчёт; тела улик
  НЕ переписаны, потому что улика есть запись о своём дне:
  `deed-cannot-be-carried-out`, `executor-paraphrase`, `core-needs-structural-rework`,
  `form-transmitted-by-act-seam`, `gauges-cannot-be-authored`, `walks-through-walls`,
  `observation-lab-mirrors`, `occupation-status-generic`, `standing-layer-never-ran`,
  `test-house-draft`, `unity-checks-were-cut`, `unknown-pace-word`, `world-marks-nobody-draws`.

  ИСПРАВЛЕНО В НАПРАВЛЕНИИ (4 карточки с `status: open` внутри `closed/`):
  - три → `done`, `d-what-kill-threshold-4-measures-001` → `dropped` (снят его же словом).

  БЕЗ ИЗМЕНЕНИЙ: 10 улик «жива как записана» и 6 «нужен живой прогон» остаются открытыми.

captures: |
  - **НАПРАВЛЕНИЕ СДЕЛАЛО ВЫВОД ПО ПОЛОВИНЕ ПЕРЕМЕННЫХ, И ЭТО ПОЙМАЛА ЕГО ЖЕ НОГА.** Радиус
    капсулы упал вдвое — направление заключило, что дефект косяков подобрел. Ширина проёмов упала
    почти вдвое ТОЖЕ, и запас стал теснее. Правило-кандидат: **улика о зазоре между двумя вещами
    перемеряется по ОБЕИМ, и вывод «подобрело» без второй половины не делается.**
  - **ШЕСТЬ УЛИК ТРЕБУЮТ ЖИВОГО ПРОГОНА, И ЭТО САМАЯ ЧЕСТНАЯ КУЧА.** Исполнитель не стал
    угадывать по коду там, где ответ даёт только запуск, и для каждой назвал, КАКОЙ именно прогон
    нужен. Это ровно то поведение, которого требует запрет на самописные проверки: инструмента нет
    — скажи, а не подставляй суррогат.
  - **`i-a-ray-may-not-see-a-thing-lying-down-001` попала в «нужен живой прогон», и её как раз
    чинит идущая нога зрения.** Куча не пустая работа: она называет, что именно проверять, когда
    зрение доедет.

decisions_needed: |
  Ни одного. Свободны два слота (WIN-U2 и WIN-U4), и теперь есть чем их занять: очередь
  перемерена и достоверна.

play_check: |
  1. OPEN — возврат ноги ревизии; отчёт читался как улика, не как факт.
  2. WORK — три замера из тридцати восьми повторены командами направления и сошлись; арифметика
     куч сложена; чистота деревьев и состояние слота прочитаны напрямую; найденный им дефект
     направления перепроверен и оказался шире на две карточки.
  3. (owner) — не требуется: задача заведена его словом, закрытие по факту.
  4. CLOSE — лёгкое закрытие с обоснованием; G5-чат не открывался.

log: |
  склад улик перемерен целиком: из 38 продуктовых улик 9 мертвы и 13 несут неверный замер, то есть
  протухло больше половины; самый ценный замер перевернул вывод направления — проёмы сузились
  сильнее, чем похудела капсула, и запас упал с 0.25 до 0.15 м; заодно найдены четыре карточки,
  лежащие в closed со статусом open

next: |
  Идут две ноги: зрение U1, «почему» U3. **Свободны WIN-U2 и WIN-U4, и теперь очередь достоверна.**

  Три самые дорогие живые названы. Две из них — перестройка центра поведения — брать параллельно
  нельзя, их поверхности почти совпадают и соседствуют с занятой U3. Третья, защита общего склада
  арта, **не пересекается ни с одной идущей ногой** и берётся хоть сейчас.

  Шесть улик ждут живого прогона; для каждой названо, какого именно.
