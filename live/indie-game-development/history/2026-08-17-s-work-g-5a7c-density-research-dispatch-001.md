# RESULT — s-work-g-5a7c-density-research-dispatch-001 (play: work)

## outcome

Его вопрос «50 предметов в комнате, 10 комнат, все интерактивны — возможно или нет» получил половину
ответа замером прямо здесь, а вторую половину — наряд на ресёрч, который меряет то, что чтением не
берётся.

**Половина, снятая замером: потолок — сеть, а не физика, и это потолок неоптимизированного кода.**

`NetworkWalkerCourier.PublishSnapshots` строит `new CargoSnapshot[cargoRoster.Count]` и шлёт **весь
массив каждый такт каждому наблюдателю**. Фильтра по `Moved` или по активности нет, хотя поле `Moved`
в снимке есть и заполняется. То есть лежащий на полу камень стоит ровно столько же, сколько летящий.

**И его собственная догадка про фон оказалась не только про понятность, но и про потолок.**
Обстановка — не груз: она не попадает в снимок вовсе и по сети не едет. Значит «стеллаж один объект,
а не сто книг» и «шкаф прибит, открывается только дверца» — это не приём подачи, а **единственный
способ вообще получить его числа**.

## evidence

Замер на `548e6340`, первой рукой:

- `Assets/TunnelCrew/Network/NetworkWalkerCourier.cs`, `PublishSnapshots`: массив груза строится по
  `cargoRoster.Count` целиком, безусловно, и уходит в `ReceiveSnapshotsObserversRpc`.
- `CargoSnapshot` — 15 полей, ≈57-60 байт: id, класс, три координаты, кватернион, число держащих,
  целостность, состояние вещи, остаток времени, флаг движения.
- Массив выделяется заново каждый такт — отдельная нагрузка на сборщик мусора, независимая от сети.
- В доме сегодня **12 предметов груза** и **23 предмета обстановки с моделью** — то есть цена ещё не
  видна и станет потолком ровно тогда, когда предметов станет много.
- Частота тактов в проекте чтением не нашлась: она в настройках FishNet, и её меряет ресёрч.

## state_changes

- `card new idea-interactive-house-density-001` — его содержание целиком: порядки предметов, две
  породы вещей (дорогие хозяину против продаваемых), сознательная скупость на мелкий лут, и его
  приёмы отличать фон от интерактивного.
- `card new i-all-cargo-travels-every-tick-001` — замер как улика с маршрутом.
- `card new c-research-g-5a7c-interactive-density-001` + наряд
  `work/2026-08-17-call-research-interactive-density.md` — восемь вопросов по порядку важности,
  каждый с требованием числа и источника числа.

## captures

Ресёрчу прямо запрещено решать дизайн: что дорого хозяину, что продаётся, сколько чего в комнате —
его содержание, и оно лежит отдельной карточкой. Ресёрч называет потолки и цену их снятия, не более.

## decisions_needed

Ничего. Наряд `ready`, слота не занимает, срок пятой волны не двигает.

## play_check

Шаг 1 ✓. Шаг 2 ✓ — его слова записаны дословно, ни одно число не выведено за него (он сам пометил
свои порядки как «из головы»). Шаг 3 ✓ — `call:research`, бесплатный для слотов. Шаг 4 ✓. Шаг 5 ✓.
G2 соблюдён: работа вне ставки не стала задачей ставки, а ушла ресёрчем и идеей.

## log

его вопрос про плотность получил половину ответа замером: потолок — сеть, и его же приём про фон её и снимает

## next

`research` по `c-research-g-5a7c-interactive-density-001`; пятая волна идёт своим ходом.
