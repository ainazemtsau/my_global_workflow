# RESULT — review g-5a7c: третья волна закрыта, четвёртая открыта

**play:** `review` (закрытие ставки) + `shape` (нарезка следующей) + `work` ×4 (выдача нарядов)
**direction:** indie-game-development · **node:** g-5a7c
**дата:** 2026-08-14

## outcome
`accepted` — ставка `bet-g-5a7c-wave-3` закрыта исходом **partial**, открыта `bet-g-5a7c-wave-4`
с динамической формой, выданы четыре наряда исполнителям.

## evidence

**ЕГО ВЕРДИКТЫ, ДОСЛОВНО, И ВСЕ ЧЕТЫРЕ ЗАПРОШЕНЫ ОТДЕЛЬНО, А НЕ ВЫВЕДЕНЫ.**

- «да, дом-ассет откладываем» → `t-house-asset-1` перенесена, не убита.
- «чиним шум» → `t-handlers-1` перенесена, разблокируется возвратом `t-noise-1`.
- «лабораторию снимаем» → `t-lab-2` снята насовсем.
- «лут проходит какую то провреку сама задача завершена»; «про лут я не понимаю там же закрыли
  уже задачу» → `t-loot-1` закрыта, решение `d-loot-unity-matrix-cut-001` закрыто вариантом (а).
- «закрывай волну и открывай новую» → команда на этот RESULT.
- «давай начнем с а» → присутствие хозяина берётся косметикой.

**ИСХОД ТРЕТЬЕЙ ВОЛНЫ — ВОСЕМЬ ЗАКРЫТЫХ ЗАДАЧ ИЗ ОДИННАДЦАТИ, И ОДНА ИЗ ВОСЬМИ ЗАКРЫТА СЛОВОМ,
А НЕ ВЫПОЛНЕНИЕМ.** `t-loot-1` получила FAIL на биндинговой проверке: «Loot 2 Bedside Radio»
лежит целиком внутри коллайдера матраса (перекрытие `0.192 × 0.040 × 0.040` равно всему габариту),
потому что `LootSupport` на матрасе нет вовсе. Строки 1 и 3 остаются проваленными и названы на
карточке; улика `i-bedside-radio-spawns-inside-the-mattress-001` открыта. Это записано именно так,
чтобы между вердиктом FAIL и статусом `done` не осталось дыры.

**ГРАНИЦЫ ЧЕТЫРЁХ НОВЫХ НОГ ИЗМЕРЕНЫ, А НЕ ОБЪЯВЛЕНЫ.** Проведён замер четырёх поверхностей по коду
(5 агентов, 952 тыс. токенов) и отдельный арбитраж пересечений: 84 пути из заявок проверены
поштучно и существуют, три заявки отклонены как «папка вместо файла», вердикт `RESOLVABLE` при трёх
условиях, и все три вписаны в наряды:

1. консоль подключается через `Network/Prefabs/NetworkRuntime.prefab`, а не через сцену;
2. `Assets/Tests/TunnelCrew/ThreeRoomHouse/**` принадлежит ноге шума — `ThreeRoomHouseEditModeTests.cs:125–149`
   зовёт `CargoBodyContact.Take` рефлексией массивом из четырнадцати аргументов и читает ответ по
   жёстким индексам `[11] [12] [13]`;
3. `World/ThreeRoomHouse.prefab` и `World/Householder.prefab` заморожены для всех, кроме одной ноги
   с эксклюзивом.

**РАЗВЕДКА, НА КОТОРОЙ СТОИТ ОЧЕРЕДЬ:** десять направлений с адверсариальной проверкой каждого
(21 агент, 2,4 млн токенов) плюс восемь направлений второй волны, запущенных после его референса.

**СВЕРКА С ПАРАЛЛЕЛЬНЫМИ СЕССИЯМИ ПРОВЕДЕНА ПЕРЕД ЗАПИСЬЮ И ОКАЗАЛАСЬ НЕ ФОРМАЛЬНОСТЬЮ.** Пока шла
эта нога, параллельные сессии закоммитили `3c50958a`, `2c19a2a6`, `f3621e51`, `edaba963`: добыли
дословные слова владельца по ручной приёмке, закрыли решение по вырезу Unity-проверок, провели
биндинговую G5 с вердиктом FAIL и выпустили наряд `c-work-g-5a7c-house-asset-1-001` на дом-ассет.
Последний выпущен **до** того, как владелец сказал мне «дом-ассет откладываем», и его выпуск был
верен на момент чтения карточек. Живое слово владельца выше состояния карточек, поэтому наряд снят
`superseded`, **но ни один его факт не потерян** — главный перенесён в карточку задачи целиком.

## state_changes

**Закрыто:**
- `bet-g-5a7c-wave-3` → `closed`, исход partial, разбор построчно в журнале → `cards/closed/`
- `t-lab-2` → `dropped` его словом → `cards/closed/`
- `t-loot-1` → `done` его словом при двух проваленных строках → `cards/closed/`
- `d-loot-unity-matrix-cut-001` → `done`, вариант (а) → `cards/closed/`
- `c-work-g-5a7c-house-asset-1-001` → `superseded` словом владельца, факт перенесён → `cards/closed/`
- дорожки `householder-life`, `house-and-things`, `body` → распущены вместе со своей ставкой

**Заведено:**
- `bet-g-5a7c-wave-4` — четвёртая волна: четыре слота + пополняемая очередь из двенадцати позиций
- `t-noise-1`, `t-bodies-1`, `t-furniture-1`, `t-console-1` — `active`, наряды выданы
- `c-exec-g-5a7c-noise-1-001`, `c-exec-g-5a7c-bodies-1-001`, `c-exec-g-5a7c-furniture-1-001`,
  `c-exec-g-5a7c-console-1-001` — `ready`, базис `53afdc2d`
- тексты нарядов: `work/2026-08-14-call-{noise,bodies,furniture,console}-1.md`

**Изменено:**
- `t-house-asset-1` → волна 4, `waiting`, `waiting_on: t-house-2`, факт про `CargoSpawnPoint` в сцене
- `t-handlers-1` → волна 4, `unblock_when` перестал ждать его согласия и стал ждать возврат `t-noise-1`
- `NOW.bet` → `bet-g-5a7c-wave-4`

## captures
- **Референс первого дома** — «условно какая-то американская провинция» с изображением кухни.
  Первое его решение о том, как выглядит мир; разделено в наряде на исполняемое и на настроение.
- **Тела персонажей уехали в Concept Lab** — он лепит их вне репозитория и присылает готовыми;
  наша сторона строит приёмник.
- **Steam Next Fest — один раз на всю жизнь игры.** Календарь на октябрь формально проходим (взнос
  уплачен, налоги закрыты), но участие необратимо. Ложится в его пересмотр маршрута 2026-08-15.
- **Восемь занятостей хозяина не видит никто в игре** — только редакторский инспектор.
- **Пост-обработка выключена галкой на камере**, хотя профиль подключён в `PC_RPAsset`.

## decisions_needed
- **Потолок дома.** На его референсе потолок есть, есть окно и лампы внутри. Прочитано как
  «интерьер со своим светом», а не «кукольный домик сверху». Ждёт подтверждения — оно разворачивает
  всю работу со светом.
- **Просвет под мебелью.** Модели будут на ножках, коллайдер останется коробкой от пола. Поднимать
  коллайдеры (меняет стелс) или закрывать просвет видом.

## play_check
- **G1** — одна активная ставка, потолок одновременности 4, его словом.
- **G3** — аппетит назван тремя границами; вход в очередь стоит за доказанной непересекаемостью файлов.
- **G4** — `done_when` и `kill_by` на месте, пороги на событиях.
- **G5** — `t-loot-1` закрыта после биндинговой проверки в свежем чате (вердикт FAIL) и его прямого
  слова; закрытие волны опирается на его вердикты, а не на вывод ноги.
- **G6** — cut list девять позиций с разделением его цитат и вывода Направления, вердикт по семи
  линзам, четыре ранжированных допущения, тест допущения №1 стоит строками 3–5 первого же наряда.
- **G7** — четыре вердикта запрошены отдельно и получены дословно; ни один не выведен.
- **G9** — ставка открыта его прямым «закрывай волну и открывай новую».

## log
2026-08-14 · третья волна закрыта `partial` четырьмя его вердиктами, четвёртая открыта первой
динамической формой — четыре слота и пополняемая очередь вместо фиксированного списка; четыре
наряда выданы после ИЗМЕРЕНИЯ границ по файлам, а не после их объявления.

## next
Ждём возвраты четырёх ног. По мере освобождения слотов очередь берёт `t-frame-1` (свет и рендер),
затем `t-sound-1`, `t-presence-1`, `t-tell-1`. Вторая волна разведки (восемь направлений) на
возврате — её результат уточняет содержание очереди, но не право её пополнять.

END_OF_FILE: live/indie-game-development/history/2026-08-14-s-review-g-5a7c-wave-3-close-001.md
