# RESULT — s-shape-g-5a7c-001

leg: s-shape-g-5a7c-001 (2026-08-07, владелец в чате)
call: файла наряда НЕТ. Маршрут открыт его прямой репликой **«вариант а идём на том, что отвечено —
      открывай shape»**, что по `os/KERNEL.md` авторитетнее и наряда, и плея.
play: `os/plays/shape.md`
node: `TREE.md` `g-5a7c:117-142` — двенадцать критериев, строка исключений, `why`, `edge`, `risk`.
owner_approved: **ДА** — правка `TREE.md` (`status`, `appetite`, `kill_by`, `detail`) сделана только
      после его слова на показанный целиком экран. Три отдельных токена, ни один не выведен.

---

## outcome

**Узел `g-5a7c` стал активной ставкой. Первая волна: пропорция 0.16 м, вертикаль у игрока и у груза,
телекинез. Семь задач, один новый слой, дат нет, полос нет.**

Definition-of-Ready пройдена **третьей законной распиской**, а не пройденной проверкой: у узла её нет и
не будет — обе `converge-verify` вернули FAIL, и потолок плея это предусмотрел. Расписка — его вердикт
на решение `d-g-5a7c-paper-ceiling-001`, дословно ниже.

---

## evidence

### Три его токена, каждый отдельным словом, ни один не выведен

1. **Ветка потолка** — «вариант а идём на том, что отвечено — открывай shape». Закрывает
   `d-g-5a7c-paper-ceiling-001` и является распиской Definition-of-Ready.
2. **Аппетит** — «Б», дано на показанные рядом А, Б и В с честно названным размером КАЖДОГО до
   выбора (А — 4–5 задач, Б — 7–9, В — 11–13) и с названным доводом против рекомендации.
3. **Экран ставки целиком** — «принимаю», дано на показанные аппетит, подходы, семь задач, семь
   вырезов, семь линз, три порога смерти, прогноз `no_basis` и довод против.

**Плюс его вердикт по содержанию, записанный как вердикт по ОБЪЁМУ, а не как правка карточки:**
«позвать еще одного это решение про риск скорей всего не верная для нас фраза на данном этапе она
вообще смысла не имеет» и «остальные вопросы они же должны быть подняты во время реализации».
Первое снимает находку `V1` второй проверки с ЭТОЙ ВОЛНЫ; второе — общий маршрут для остальных.
**Ни одно из двух не тронуло текст карточки, и это сказано ему прямо до подписи.**

### Шаг 1 — узел восстановлен

Родитель `g-0c26` требует публичного демо и платного выпуска. `g-5a7c` — первый узел, после которого
существует ИГРА. Узел **многоволновой его же словом** («следующая волна будет закрывать ядро, уже даже
несколько»), поэтому `shape` режет ПЕРВУЮ ВОЛНУ, а не весь узел. Цель узла — исход, а не занятие;
починка не потребовалась.

### Шаг 2 — аппетит, и он взят до любого разговора о решении

Внешняя проверка — три сопоставимых, все собственные: закрытая волна `g-1d84` вариант Б (23 задачи,
3 новых слоя → `partial`); волна до неё (11 задач, 0 новых слоёв → дошла); тяжёлый разбор `g-8f31`
(6 ног бумаги → 0 строк игрового кода). Вывод против больших пакетов записан и показан ему.

Дат не приносилось: его запрет 2026-08-02 действует. Точка пересмотра поставлена НА СОБЫТИЕ — после
первой задачи он смотрит, тот ли это размер.

### Шаг 3 — три структурно разных способа, потом минимальное решение

| способ | на что ставит | исход |
|---|---|---|
| **Переехать по месту** — 0.16 в живой сцене, вертикаль третьим числом, телекинез из уже написанной пружины | сегодняшняя переноска, принятая его прогоном, переживёт смену масштаба и появление высоты | **ВЫЖИЛ** |
| Отдельный полигон в масштабе 0.16, потом перенести числа | что работает на полигоне — перенесётся без сюрпризов | ОТКЛОНЁН: у нас уже два описания одного дома и они разошлись (`NOW` несёт запись); третий мир добавит третье расхождение. В заметки |
| Мышь маленькая только на вид, физика в старом масштабе | пропорция — это про то, как выглядит, а не как считается | **ОТКЛОНЁН ЕГО ПОДПИСАННЫМ ТЕКСТОМ:** критерий 1 дословно требует, чтобы «камера, **столкновения, скорость и ширина проходов**» были согласованы с маленьким. В заметки |

Самая маленькая версия выжившего названа вместе с тем, чего в ней НЕТ: дом остаётся коробкой,
обстановки нет, слоя шума нет, хозяин не трогается, захода/инструмента/облика/скриншотов нет.

### Шаг 4 — молот по объёму: семь вырезов, четвёртый настоящий

Полный список — `NOW.bet.cut_list`. Настоящий и болезненный один: **дом как жильё**, и цена названа ему
ДО подписи — луч он будет судить в коробке с перегородками по пояс, значит его вердикт по `t-scale-5`
будет вердиктом про ЛУЧ, а не про ДОМ.

**Один вырез не состоялся, и это находка самого шага.** Слой шума вырезан ЦЕЛИКОМ, кроме одной вещи:
его требование «груз без взаимодействия не шумит» остаётся ВНУТРИ волны, задачей `t-scale-4`. Основание
измеренное, а не вкусовое: `t-scale-4` включает гравитацию, а `CargoBodyContact.Record` зовётся и из
`OnCollisionEnter`, и из `OnCollisionStay`, и `AuthoritativeCargoRoster` зовёт `NoiseOf` каждый тик без
проверки «держит ли кто-нибудь» — значит без этого куска лежащий ящик станет самым громким событием
игры ровно в тот момент, когда включится гравитация. Вырезать его нельзя, не сломав его же требование.

**Девять открытых строк разбора разведены поимённо** (`NOW.bet.open_rows_disposition`): три в волне
(`W114`, `W116`, `W119`), одна отвечена здесь (`W117`), пять вырезаны со своей половиной узла (`W39a`,
`W54`, `W73`, `W94`, `W118`). `W15` и `W16` не тронуты ни знаком и остаются отложенными его словами.

**`W94` — единственная строка, чей отвечающий ВЛАДЕЛЕЦ.** По плею такую строку нельзя вырезать без его
слова. Основание выреза: его слова «остальные вопросы они же должны быть подняты во время реализации»
плюс его подписанный критерий 10 («облик живёт здесь, но не в начале»). **В экране ему было названо
прямо, что это ОБЩИЕ слова, а не отдельный ответ про кадр, и предложено оставить строку открытой, если
он хочет.** Он подписал экран как есть.

### Шаг 5 — семь уставных линз, ни одной молча

Полные вердикты — `NOW.bet.lens_verdicts`. Две линзы, которых набор не спрашивал НИ РАЗУ (находка `V4`
второй проверки), отвечены здесь: линза 5 (`not_needed`) и линза 7 (сложнее в одном месте — высота у
двух сущностей и два шва; взамен вырезано восемь вещей; и одно место становится ПРОЩЕ — семь мест, где
живут размеры, схлопываются в одно). Линза 6 — это была открытая строка `W117`, чей отвечающий назван
`shape` по G6; **отвечена, и строка уходит из открытых.**

### Шаг 6 — самое рискованное допущение и его тест

Ранжировано по силе убить, режим устава — осторожный. Первое: **пропорция 0.16 держится в движке** —
документация движка велит держать тела около единицы, мы уходим в шесть раз ниже, и если поедет,
умирает ВСЯ волна. Тест — `t-scale-1`, первая задача, полдня, руками и без единой новой системы.
Остальные три — вертикаль не портит переноску, луч ощущается управляемым, числа дают ступень там, где
он её поставил.

### Шаг 7 — семь задач

Полные тексты — `NOW.tasks`. Порядок: `t-scale-1` проба масштаба (риск №1) → `t-scale-2` единая точка
масштаба → `t-scale-3` вертикаль у игрока → `t-scale-4` вертикаль у груза и молчащий покой →
`t-scale-5` телекинез → `t-scale-6` лестница по числу держащих → `t-scale-7` два шва.

**Полос НЕ предложено, и это названо ему прямо:** работа одна и последовательная, каждая задача стоит
на предыдущей; полосы здесь были бы бумагой ради бумаги. Судит — кто сводит сцену, тот её один раз и
запускает. Откаты названы по задачам; самый дорогой у `t-scale-4`, поэтому она четвёртая, а не первая.

### Шаг 8 — три порога, все на событиях

Полный текст — `TREE.md` `g-5a7c.kill_by`. Дат нет ни одной. Прогноз честно `no_basis`: сравнимых волн
две, для числа мало; довод против прогноза назван — единственный новый слой трогает самое доказанное
место игры, а не пустое.

### Шаг 9 — закрытие на его слове

Экран показан целиком одним куском; названо, что именно закрывает его слово (аппетит, порог смерти,
активация узла, вырезанное включая `W94`) и что оно НЕ закрывает (текст критериев карточки не меняется
ни в одном знаке). Он сказал «принимаю».

### Продуктовая расписка для первого наряда

`os/engineering/CONTRACT_VERSION` `current: 36`; `C:/projects/Unity/GasCoopGame/validation.config`
`synced_contract_version: 36` — **дрейфа нет, Re-sync не должен**, наряд пинит `engineering_contract: 36`.
База `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`, `main`, дерево чисто. Наряд несёт три строки
`done_when` — под потолком v35/v36, без токена превышения.

---

## state_changes

1. `TREE.md` `g-5a7c`: `status: parked` → **`active`**; добавлены `appetite` (его «Б» с полным текстом
   отклонённых вариантов и точкой пересмотра на событии), `kill_by` (три порога, дат нет,
   `next_if_true`/`next_if_false`), `detail`. **Текст цели, двенадцати критериев, строки исключений,
   `why`, `edge` и `risk` не тронут ни в одном знаке.** Другие узлы дерева не тронуты.
2. `NOW.md`: `bet: null` → полный блок ставки (`node`, `owner_word`, `description`, `goal`, `appetite`,
   `approach`, `kill_by`, `forecast`, `against`, `cut_list`, `lens_verdicts`, `riskiest_assumptions`,
   `open_rows_disposition`, `card_line_not_touched`); `tasks: []` → семь задач; `open_calls` получает
   `c-exec-g-5a7c-scale-probe-001` (`blocked`, `unblock_when` — владелец называет слот Unity);
   `decisions` теряет `d-g-5a7c-paper-ceiling-001` как ОТВЕЧЕННОЕ (его слова записаны в `bet.owner_word`,
   в строке-записи и здесь); добавлена строка-запись об открытии ставки. Полос НЕ заведено,
   `track_wip_limit` не появляется.
3. `work/c-exec-g-5a7c-scale-probe-001-call.md` — новый наряд первой задачи.
4. `LOG.md` — одна строка сверху.
5. `history/2026-08-07-s-shape-g-5a7c-001.md` — этот RESULT.

Не тронуто: `CHARTER.md`, `knowledge/**`, `work/converge-g-5a7c.md`, `work/converge-g-5a7c-arch.md`
(поверхность заморожена потолком второй проверки и правкам не подлежит), продуктовый репозиторий.

---

## captures

- **Отклонённый подход «отдельный полигон в масштабе 0.16»** — не мусор: если после `t-scale-1`
  окажется, что живая сцена мешает мерить физику, он вернётся с уже названной ценой.
- **Отклонённый подход «маленькая мышь только на вид»** — отпал на подписанном тексте; записан, чтобы
  никакая будущая нога не предложила его как экономию.
- **Наклон груза** (снятие заморозок вращения X/Z) — одна строка в обе стороны, судит его глаз; ждёт
  первой задачи с вертикалью, где он на это посмотрит.
- **Кнопка редактора для пересчёта геометрии под масштаб** — окупится в волне, которая пересобирает дом.
- **Фраза критерия 4 про «позвать ещё одного»** — он назвал её лишённой смысла на этом этапе, но она
  стоит в подписанном тексте, и `review` будет судить ставку по карточке. Одна короткая нога `map`
  с точным текстом ему на подпись, когда он захочет.

---

## decisions_needed

**Ни одного.** Все три его слова получены; новых решений ему не выносится. Единственное, что от него
нужно для запуска первой задачи, — **назвать слот Unity**, и это не решение, а оснастка.

---

## play_check

```
1 recite:      узел восстановлен по TREE.md g-5a7c:117-142; цель — исход, починки не требовала;
               узел многоволновой его словом, поэтому режется ПЕРВАЯ волна
2 appetite:    (owner) «Б» — его отдельное слово на показанные рядом А/Б/В с честным размером
               КАЖДОГО до выбора. Внешняя проверка: три сопоставимых собственных волны названы.
               Дат нет (его запрет 2026-08-02); точка пересмотра поставлена на СОБЫТИЕ
3 approaches:  три структурно разных способа названы ДО эскиза; выжил «переехать по месту»;
               «мышь только на вид» отклонён его же подписанным текстом кр. 1, «отдельный полигон»
               — измеренным расхождением двух описаний дома. Оба проигравших ушли в captures.
               Минимальная версия и то, чего в ней НЕТ, названы явно
4 scope:       семь вырезов, четвёртый (дом как жильё) настоящий и его цена названа ему ДО подписи.
               Девять открытых строк разбора разведены ПОИМЁННО: 3 в волне, 1 отвечена, 5 вырезаны.
               W94 — единственная owner-owed — вырезана на его словах, и в экране прямо сказано,
               что слова ОБЩИЕ, с предложением оставить строку открытой
5 lenses:      семь вердиктов, ни одного молча. Линза 6 = открытая строка W117, ОТВЕЧЕНА здесь.
               Линзы 5 и 7 набором не спрашивались ни разу (находка V4) — отвечены здесь
6 riskiest:    четыре допущения ранжированы; первое (0.16 держится в движке) получило ПЕРВУЮ и
               самую дешёвую задачу. Глубина теста калибрована по осторожному режиму устава
7 tasks:       семь задач, каждая ≤ полдня, каждая с проверяемым done_when, kind у всех executor
               кроме закрытия по его слову; риск-задача первая. Полос НЕ предложено — работа одна
               и последовательная, и это названо ему прямо
8 kill:        три порога на СОБЫТИЯХ, дат нет (его запрет); next_if_true / next_if_false названы;
               прогноз честно no_basis с названной опорной группой и доводом против
9 close:       (owner) «принимаю» на показанный целиком экран. Узел active, ставка записана,
               наряд первой задачи зарегистрирован, handoff в next

definition_of_ready: третья законная расписка — его вердикт потолка второго FAIL, дословно
               «вариант а идём на том, что отвечено — открывай shape», записан в решении
               d-g-5a7c-paper-ceiling-001 (снято этим RESULT как отвеченное) и в bet.owner_word.
               Пройденной converge-verify у узла нет и не будет: обе вернули FAIL
G1 wip:        ровно один активный узел в дереве — g-5a7c (перемерено по TREE.md). Полос нет
G3 appetite:   зафиксирован ДО разговора о решении, его отдельным словом, и не продлевается
G4 validity:   done_when у ставки — двенадцать критериев подписанной карточки; kill_by есть
G6 shape:      реальный список вырезов (7, один болезненный), вердикт по КАЖДОЙ из семи линз,
               задача на самое рискованное допущение стоит первой
G7 decisions:  новых решений владельцу не вынесено ни одного
G9 co-creation: TREE.md правлен ТОЛЬКО после его слова; owner_approved проставлен; текст цели,
               критериев, строки исключений, why/edge/risk не тронут ни в одном знаке
engineering:   os/engineering current: 36 · продуктовый стамп 36 — дрейфа нет, Re-sync не должен;
               наряд пинит engineering_contract: 36, три строки done_when (под потолком v35/v36)
owner_present: YES — три его токена в этом чате, каждый отдельным словом
```

---

## log

2026-08-07 | s-shape-g-5a7c-001 | shape | direction | СТАВКА ОТКРЫТА: `g-5a7c` стал активным, первая
волна — пропорция 0.16 плюс вертикаль плюс телекинез, семь задач, один новый слой, дат нет, полос нет.

---

## next

`c-exec-g-5a7c-scale-probe-001` — первая задача ставки, `blocked` до одной вещи: **владелец называет
слот Unity**. Всё остальное в наряде готово (база перемерена, контракт пинится 36, дрейфа нет).

**Почему именно она первая:** это самый дешёвый способ узнать самое дорогое — держится ли физика на
росте мыши. Если нет, ставка режется здесь, а не в конце, и пропорция возвращается к нему вопросом.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-shape-g-5a7c-001.md
