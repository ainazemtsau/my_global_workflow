# s-research-015 — g-9c41 — research (scale 150 rooms, rollup mandatory, full-fill via rollup, C1-C6 confirmations)

date: 2026-06-22
play: research
direction: indie-game-development
node: g-9c41
input: owner voice — scale correction (NOT 40 rooms — target ~150 rooms / 140-200k cells, start ~70k, ceiling ~200k, 1 big hall/level) + earlier corrections C1-C6 (C1 keep 25cm resolvability with 50cm gas; C2 don't bake a cap, make it configurable; C3 full-fill must be possible+handled; C4 walk through gas + displacement/sloshing physics; C5 destructibility 25cm gradation; C6 confirm → design simple→complex with seams).

> NB: 1 of 4 fan-out agents (res25-destruct) failed on a usage-policy false positive (the word "разрушаемость"/"destruct"); the destructibility/partial-conductivity question was covered by the verifier agent (point c). No information lost.

---

## RESULT

```
play: research
goal: ответить на поправку масштаба (150 комнат / 140-200k клеток) + подтвердить C1-C6 реальными числами; влезает ли full-fill, нужен ли rollup

outcome:
  4-агентный веер (wf_6f9b6b48-c7d) + скептик. ГЛАВНОЕ — РАЗВОРОТ §9-вывода:
  (1) ДВА ЧИСЛА: ПАМЯТЬ/размер мира = 140-223k клеток @50см (150 комнат=223k > потолка 200k≈134 комнаты); TICK/тик = near-окно ~12-40k. CPU платит за TICK, не за память.
  (2) ROLLUP ОБЯЗАТЕЛЕН при 150 комнатах (не опционален как казалось при 40): равномерно-мелкое full-active переживает ~40-80 комнат; 223k не влезает в пол 100-180k. На 40 комнатах влезало = артефакт масштаба. Согласовано с локом (S4 запланирован, швы в S0). Меняется только фрейминг.
  (3) FULL-FILL ТЯНЕТСЯ через rollup при ЛЮБОМ размере (подтверждает C3): мелко только near-окно вокруг игроков (ограничено ЧИСЛОМ ИГРОКОВ, не размером уровня: 4p×2-3=12-18k, 8p=24-36k); far=140+ комнат по 1 целому (дёшево+КОНСТАНТНО залиты или пусты) → full-fill 150 комнат ≈ 12-36k/тик, влезает; растёт только память. Full-fill 80-100% = first-class фейл (экспедиция гибнет), ВЛЕЗАЕТ.
  (4) КАП = опциональный (C2/C3 верны, дефолт OFF, заглушка в чексумме). Расчёт НЕ предполагает X% заполнения; сон = бонус не премисса.
  (5) AT-RISK = один большой ОТКРЫТЫЙ зал (near=ОБЪЁМ: 3000м³=24k ok, 10000+м³=80k тесно под whole-CPU) — единственная точка риска, не уровень; дизайн-рычаг владельца.
  (6) C1 25см = локальный пузырь + высотные полосы (не 25см-авторитет). C5 разрушаемость 25см = через ЦЕЛУЮ ЧАСТИЧНУЮ ПРОВОДИМОСТЬ грани (не 25см-сетка, R7 цел; leak-safe). C4 проход бесплатен; раскачивание = 3 уровня (косметика ведро-1 float-ok / импульс-через-дверь ведро-2 / стойко-меняет-исход ведро-3) — ПОПРАВКА «не всё выдавливание авторитетно». C6 простое→сложное = форма залоченного хребта.

evidence:
  - Workflow wf_6f9b6b48-c7d (4 агента + скептик, 249k токенов; 1 агент упал на фильтре, покрыт верификатором).
  - АРИФМЕТИКА (скептик: верна бит-в-бит): 150 комнат = 223.2k @50см (двумя методами: 150×1488/комнату И 150×186м³×8); uniform full-fill 223k не влезает в пол; near-fine ограничен игроками 12-36k; far = ~150 операций.
  - ROLLUP геометрический бонд (near=f(players) vs far=const) робастен к неизмеренному числу → HIGH на структуре.
  - C5 conductivity: per-face state «open/closed + целая проводимость + односторонняя маска» = day-one seam (grid-vs-graph:80, decision:244); leak-safe by construction (одна грань общая A/B).
  - C4 классификация: cost-model §73 (выдавливание внутри=ведро-1 / через-дверь=ведро-2), dev-plan §58-62; drift-guard #4 (деталь в одну сторону).
  - whole-CPU ×4-8 риск: деградация грациозна (меньше near-комнат, 10Гц), не редизайн. Ангар-проба гейтит.
  - Продукт: work/gas-cellsize-levelscale-2026-06-22.md §11.

state_changes:
  - work/gas-cellsize-levelscale-2026-06-22.md: +§11 (масштаб 150, rollup обязателен, full-fill через rollup, C1-C6, поправки скептика).
  - LOG.md: +1 строка (s-research-015).
  - history/s-research-015.md: этот файл.
  - NOW.md / залоченная архитектура / ставка S0: НЕ ТРОНУТЫ (research; входы = captures для S0-PLAN).

captures:
  - "МАСШТАБ-ЦЕЛЬ (для S0-PLAN/shape): ~150 комнат / 140-223k клеток @50см ПАМЯТЬ; tick = near-окно ~12-40k. При 150 комнатах room-rollup (S4) ОБЯЗАТЕЛЕН (не опционален). Равномерно-мелкое ядро (S0-S3) тянет ~40-80 комнат = стартовый полигон."
  - "Full-fill (80-100%) = поддерживаемый first-class фейл; цена/тик ограничена near-окном вокруг игроков (≤8), НЕ размером уровня и НЕ долей заполнения. Кап НЕ закладывать как границу (опциональный конфиг, дефолт OFF, заглушка в чексумме = GAP-1)."
  - "Единственная at-risk точка = один большой ОТКРЫТЫЙ зал (near=объём зала): средний 3000-7000м³ ok; гигант 10000+м³ тесно под whole-CPU. Дизайн-рычаг: не строить гигант если движок не держит; иначе зал на 50см / 10Гц внутри / кап объёма зала."
  - "C5 разрушаемость 25см-градация = на ЦЕЛОЙ ЧАСТИЧНОЙ ПРОВОДИМОСТИ 50см-грани (per-face seam), НЕ на 25см-газосетке (R7: геометрия не мельче газа). Breach = face-flips, пере-разбивает ПОТОК не геометрию. Визуал пролома может быть мельче."
  - "C4 раскачивание/выдавливание классифицировать per-mechanic в PLAN слайса S1: косметика-внутри-комнаты = ведро-1 (вне чексуммы, float ок); импульс-через-дверь = ведро-2 (целый edge-flux в чексумме); стойко-меняет-исход = ведро-3 (≤2-3 комнаты). НЕ закладывать всё выдавливание в чексумму."
  - "Ангар-проба (i5-7400 пол, 1 ядро + все ядра, scalar+Burst, тяжёлый солвер, worst-case игрок-в-зале) — гейт абсолютов: near-радиус, число near-комнат/игрока, мега-зал-потолок. Добыть ДО фиксации этих бюджетов."

decisions_needed:
  - (подтверждены/уточнены, не новые блокеры): газ 50см глобально + room-rollup S4 обязателен при целевом масштабе 150 комнат; кап опциональный; 25см = разрешимость не авторитет; разрушаемость на conductivity. Все = входы S0-PLAN, ратификация в done_when S0 = работа PLAN'а.
  - d-next-step (владелец): собрать план «от простого к сложному» = по сути PLAN слайса S0 (GasCoopGame), ингестящий §1-§11 этого дока — открыть отдельной сессией когда владелец готов.

play_check:
  1. Recite — DONE: поправка масштаба (150 не 40) + C1-C6 восстановлены.
  2. Investigate — DONE: 4-агентный веер (full-fill-бюджет + 25см/разрушаемость + физика-прохода + скептик); 1 агент упал на фильтре, вопрос покрыт верификатором.
  3. Confidence — DONE: структура HIGH (геометрический бонд near-vs-far робастен); абсолюты на неизмеренном single-core (ангар-проба гейтит); 4 поправки скептика подняты (два числа / выдавливание-не-всё-авторитетно / conductivity-не-25см-сетка / мега-зал-единственный-риск).
  4. Close — DONE: этот RESULT; ответ по масштабу+C1-C6 в формате владельца; продукт §11; captures = вход S0-PLAN; тее-ап простое→сложное.

log: "2026-06-22 — research (g-9c41, s-research-015): поправка масштаба (150 комнат/140-223k клеток). РАЗВОРОТ §9: rollup S4 ОБЯЗАТЕЛЕН при 150 (равномерно тянет ~40-80; full-fill через rollup = near-окно ограничено ИГРОКАМИ ~12-36k + far константно → влезает при любом заполнении). Кап опциональный (C2/C3). At-risk = один большой зал. C1 25см=пузырь; C5 разрушаемость=conductivity грани не 25см-сетка; C4 раскачивание=3 уровня (не всё авторитетно). C6 простое→сложное=хребет S0-S6. Дополнил work §11. → history/s-research-015.md"

next: awaiting_owner — подтверждение → следующий шаг = собрать план «от простого к сложному» = PLAN слайса S0 (GasCoopGame, ингестит §1-§11). Все находки = captures для S0-PLAN (масштаб-цель, rollup-обязателен, кап-опционален, 25см-разрешимость, разрушаемость-на-conductivity, C4-классификация, ангар-проба-гейт). S0 открывается PLAN'ом (owner present). Ратификация в done_when S0 / 4 GAP-розетки = работа S0-PLAN, не research.
```

END_OF_FILE: live/indie-game-development/history/s-research-015.md
