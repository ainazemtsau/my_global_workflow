# RESULT — s-work-g-5a7c-impact-direction-001 (play: work)

## outcome

**Четвёртая просьба параллельной ноги подтверждена построчно и взята в наряд A, вместе с миной,
которой в просьбе не было.**

Вектор удара схлопывается в число **дважды**, и оба раза на виду:
`CargoBodyContact.cs:222` берёт `collision.impulse.magnitude`, выбрасывая направление той же строкой;
`:190-191` берёт `|dot(relativeVelocity, contact.normal)|`, теряя нормаль, вдоль которой мерилось.
Без направления скользящий удар отбросил бы существо так же, как прямой.

**Встречная находка, которой в просьбе не было: у импульса есть сторона.** `Collision.impulse` — это
импульс, приложенный **к этому** коллайдеру; оба тела получают свою запись с противоположными
направлениями. Потребителю, который хочет толкнуть того, в кого попали, нужен **обратный** вектор.
Это скомпилируется при любом знаке и молча оттолкнёт мышь не туда. В наряд вписано требование: **имя
поля обязано говорить, чей это импульс** — не комментарий, а имя.

## evidence

- `CargoBodyContact.cs:185-228` прочитан целиком. Обе точки схлопывания подтверждены дословно.
- `WIN-U3` на момент правки — `AVAILABLE`: дополнение попадает в наряд ДО начала работы, третий раз
  подряд.
- Приёмка выросла до тринадцати строк; новая двенадцатая доказывается тестом на скользящий удар —
  касательное попадание обязано дать направление, заметно отличное от прямого в ту же точку.

## state_changes

- `work/2026-08-17-call-cargo-sleep-1.md` — раздел Н4 (направление удара с миной про сторону) и новая
  строка приёмки 12.
- `card new idea-flying-thing-knocks-the-mouse-down-001` — слова владельца о том, что летящий предмет
  сбивает мышь, а хозяина физически не двигает никто.

## captures

**Провенанс слов владельца помечен явно, и это не формальность.** Параллельная нога признала, что в
прошлый раз передала пересказ там, где нужны были слова, и прислала дословные цитаты. Они записаны
как **дословные, но полученные вторыми руками**: в этом чате владелец их не повторял. Носитель,
который берёт нога A, на них НЕ опирается — он взят по структурному доводу. Поведение опирается, и
пишет его та нога, которая слова получила.

**Разделение подтверждено с обеих сторон и держится третий круг:** мы делаем факт выразимым, они
делают поведение. За три круга переписки ни одна из сторон не залезла в чужой файл.

## decisions_needed

Ничего. Наряд A готов и ждёт слота `WIN-U3`.

## play_check

Чужая просьба проверена по коду, а не принята на слово. К принятой просьбе добавлено то, чего в ней
не было и что тише всего сломалось бы. Слова владельца записаны с провенансом, а не как прямая речь
этого чата.

## log

направление удара взято в ногу A, и к нему добавлена мина про сторону импульса, которой в просьбе не было

## next

Владелец открывает `Gas U3`. Наряд A закрыт для дополнений: четыре круга правок — потолок.
