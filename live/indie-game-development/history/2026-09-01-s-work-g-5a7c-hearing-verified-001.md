# RESULT — s-work-g-5a7c-hearing-verified-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-noise-reaches-by-distance-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — слух построен и перевыверен направлением на байтах: семь строк приёмки из восьми закрыты первой рукой, вторая задача полосы умерла как ненужная по замеру, и открыт G5 ровно на ту единственную строку, которую направление перевывести не могло

close: —  задача НЕ закрывается. Семь строк перевыведены первой рукой, восьмая (живой прогон) стоит на улике исполнителя, и правило прямое: light close возможен, только когда КАЖДАЯ строка перевыведена. Открыт G5 в свежей сессии. Закрыта отдельная задача `t-walking-stops-deafening-1` — её ответ перевыведен направлением полностью, close: light.

owner_approved: |
  Задача входит в его «берём четыре» от 2026-09-01. Отдельного слова на промежуточное состояние не
  требуется и не запрашивалось.

  **Смерть второй задачи полосы — не сокращение замаха.** Она нарезана с явным правом оказаться
  ненужной, и это записано в карточке волны его словом при открытии.

outcome: |
  **СЛУХ ПОСТРОЕН, И ЭТО ЛУЧШАЯ НОГА ЭТОЙ ВОЛНЫ ПО КАЧЕСТВУ ИСПОЛНЕНИЯ.** Перевыверено направлением
  первой рукой на `slot/win-u2` = `b4b010c1`:

  - **ровно семь файлов**, +408 −20; в `CarryWorld.cs` изменена **одна инструкция** — к вызову
    добавлен третий аргумент, и больше в сетевом шве ничего;
  - **условие «один ответчик» выполнено буквально:** единственная реализация `Reaches`
    (`WorldSituationNoise.cs:138`) и **единственный боевой вызов** (`:267`). Прежний вызов в
    `AuthoritativeHouseholder` **исчез** — проверка переехала, а не размножилась;
  - **дефект порядка, который нога сама нашла и ради которого останавливалась, закрыт в нужном
    месте:** фильтр стоит ВНУТРИ редукции, до выбора самого громкого;
  - радиусы **`6 / 12 / 18`** лежат в настроечном ассете рядом с силами `3 / 15 / 35` —
    строки приёмки 3 и 4 закрыты формой, а не обещанием;
  - **прогон направления: 747 / 747 / 0, пропущено 0**, `Ignore`/`Explicit` в тронутом файле нет;
  - коммит **не содержится ни в одной удалённой ветке** — не опубликовано.

  **РАСТЯЖКА ПРЕДЫДУЩЕЙ НОГИ ОБРАБОТАНА ЛУЧШЕ, ЧЕМ ТРЕБОВАЛ НАРЯД.** Наряд разрешал оба исхода:
  снять с заменой или оставить зелёной. Нога **оставила утверждение `Is.GreaterThan(0f)` живым и
  переписала его смысл честно**: «his walking phases remain genuinely un-interruptible for work;
  hearing no longer consults that flag, because distance is its one gate». **Ходьба осталась
  непрерываемой для РАБОТЫ и перестала быть воротами СЛУХА** — различение, которого не было ни в
  наряде, ни в нарезке.

  **ВТОРАЯ ЗАДАЧА ПОЛОСЫ УМЕРЛА КАК НЕНУЖНАЯ, И ЭТО ПЕРЕВЫВЕДЕНО, А НЕ ПРИНЯТО НА СЛОВО.**
  `t-walking-stops-deafening-1` нарезалась с явным правом не понадобиться. Направление проверило
  само: красный на моменте ходьбы погас в прогоне направления, а растяжка `windowSeconds > 0`
  **осталась зелёной** — то есть непрерываемые окна на месте, но слух через них проходит.
  **Ненадобность доказана двумя независимыми показаниями машины.** Нога сэкономлена целиком.

  **И ГЛАВНОЕ, ЧЕГО НАПРАВЛЕНИЕ СДЕЛАТЬ НЕ МОГЛО.** Строка приёмки 2 требует живого прогона, а не
  теста. Направление редактор владельца не водит. Таблица трёх замеров и прогон в `HouseholdLab`
  пришли из возврата и остаются **уликой исполнителя**. **Этот день уже дважды заплатил ровно за
  этот разрыв** — один раз более зелёный вариант оказался сломанным живьём, один раз замер сняли на
  отставшей ветке. **Поэтому задача не закрывается, а открыт G5.**

  **И ПОБОЧНАЯ НАХОДКА НОГИ СЛУХА ПОДТВЕРДИЛА ГИПОТЕЗУ ДРУГОГО СЛОТА.** В разделе «замечено, но не
  исправлено» она записала: `InvestigateLanding`, **и путь сообщал `FurnitureInTheWay`**. Это ровно
  то, что наряд WIN-U4 назвал гипотезой: точка приземления недостижима, а у ответа
  `AcknowledgeOnArrival` нет иного выхода, кроме прибытия. **Подтверждение пришло из другого слота,
  другой ногой, по другому поводу — то есть это не самоподтверждение.** И оно меняет порядок работы
  U4: **сначала ответ перестаёт быть вечным, потом факт узнаёт производителя** — потому что
  `FurnitureInTheWay` заморозит хозяина и от чужой вещи тоже.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** на `slot/win-u2` = `b4b010c1`, рабочая копия чистая:

  - `git show --stat b4b010c1` → **7 файлов, +408 −20**;
  - `git branch -r --contains b4b010c1` → **пусто**: не опубликовано;
  - `git show b4b010c1 -- CarryWorld.cs` → изменена **одна инструкция**, добавлен третий аргумент
    `Householder.GetStateForPerception(householderWorld)`;
  - `grep "\.Reaches(\|bool Reaches"` по продукту → реализация `WorldSituationNoise.cs:138`,
    **единственный боевой вызов `WorldSituationNoise.cs:267`**, внутри редукции;
  - `GameRulesSettings.asset:269-274` → `_cargoRustleForce: 3`, `_cargoKnockForce: 15`,
    `_cargoHeavyNoiseForce: 35`, `_cargoRustleHearingRadius: 6`, `_cargoKnockHearingRadius: 12`,
    `_cargoHeavyNoiseHearingRadius: 18`;
  - **`dotnet test` направлением: не пройдено 0, пройдено 747, пропущено 0, всего 747;**
  - `grep Ignore\|Explicit` по `HandlersRecipeTests.cs` → **пусто**;
  - `HandlersRecipeTests.cs:200-203` — растяжка жива, `Is.GreaterThan(0f)`, сообщение переписано;
  - `HandlersRecipeTests.cs:346-379` — `FarHeavyNoiseCannotMaskANearQuietRustleInTheSameTick`
    прочитан целиком: тяжёлый 40 на 20 м (радиус 18) и тихий 5 на 1 м (радиус 6) в одном тике →
    побеждает тихий, факт тяжёлого **не жив**;
  - `HandlersRecipeTests.cs:306` — `FirstTickSeatsAtTheAuthoredStartBeforeNoiseDistanceIsDecided`
    на месте: яма первого тика закрыта тестом, как и требовало условие.

  **ОСТАЁТСЯ УЛИКОЙ ИСПОЛНИТЕЛЯ и подлежит G5:** таблица трёх живых замеров на замороженной
  позиции `(2.9154, 3.9231)`; прогон в `HouseholdLab`, где идущий хозяин услышал шорох на 5 м;
  доказательство, что редактор гоняет новую сборку, через поле `MaximumHearingDistance`;
  зелёность `tools/check.ps1`.

state_changes: |
  ЗАКРЫТО ПО ПЕРЕВЫВЕДЕННОМУ ФАКТУ (1 задача):
  - `t-walking-stops-deafening-1` → `dropped`, «не понадобилась»: слышимость решается расстоянием,
    и «безопасный момент» перестал быть воротами слуха сам собой. Оба показания сняты направлением.

  СОЗДАНО (1 наряд + 2 документа):
  - `c-g5-g-5a7c-noise-reaches-by-distance-1-001` → свежая сессия, слот **WIN-U2** удержан;
  - `work/2026-09-01-g5-noise-reaches-by-distance.md`;
  - `work/2026-09-01-note-furniture-in-the-way-for-u4.md` — записка в соседний слот.

  СТАТУС БЕЗ ИЗМЕНЕНИЙ:
  - `t-noise-reaches-by-distance-1` остаётся `active` — **не закрывается до G5**;
  - `c-exec-g-5a7c-noise-reaches-by-distance-1-001` остаётся открытым: закрывает его close-нога;
  - `t-everything-lands-1` остаётся `blocked`: красный погас, но зависание держит смысл публикации.

captures: |
  - **ЗАДАЧА, НАРЕЗАННАЯ С ПРАВОМ НЕ ПОНАДОБИТЬСЯ, ДЕЙСТВИТЕЛЬНО НЕ ПОНАДОБИЛАСЬ — И ЭТО ПЕРВЫЙ
    ТАКОЙ СЛУЧАЙ.** Обычно нога доказывает свою нужность работой. Здесь ненадобность была объявлена
    нормальным исходом заранее, и предыдущая нога её просто назвала. **Сэкономлена нога целиком, и
    приём подтверждён на практике.**
  - **ИСПОЛНИТЕЛЬ РАЗЛИЧИЛ ТО, ЧЕГО НЕ РАЗЛИЧИЛА НАРЕЗКА.** Наряд знал два исхода для растяжки:
    снять или оставить. Нога нашла третий и лучший: **непрерываемость остаётся для работы и
    перестаёт быть воротами слуха.** Утверждение живо, смысл честен.
  - **ПОБОЧНОЕ НАБЛЮДЕНИЕ ОДНОГО СЛОТА ЗАКРЫЛО ГИПОТЕЗУ ДРУГОГО.** `FurnitureInTheWay` записан «в
    замеченном» ногой, которая занималась совсем другим. **Правило-кандидат: раздел «заметил и не
    чинил» читать как улику для ДРУГИХ живых нарядов, а не только как склад.**
  - **СЕМЬ СТРОК ИЗ ВОСЬМИ — НЕ ПОВОД ДЛЯ ЛЁГКОГО ЗАКРЫТИЯ.** Соблазн был: качество ноги высокое,
    числа сходятся, дефект она нашла сама. **Но восьмая строка — ровно та, на которой этот день
    уже дважды сгорел.** Правило про light close исполнено буквой, а не духом удобства.

decisions_needed: |
  Ни одного владельческого. G5 — свежая сессия, её открывает владелец передачей текста.

play_check: |
  1. OPEN — возврат исполнителя с кандидатом `b4b010c1`.
  2. WORK — семь строк приёмки перевыведены направлением на байтах, включая собственный прогон
     тестов и чтение обоих новых тестов целиком; ненадобность второй задачи проверена двумя
     независимыми показаниями и задача закрыта; восьмая строка честно оставлена уликой исполнителя.
  3. (owner) — не требуется: замах не менялся, ненадобность задачи предусмотрена карточкой волны.
  4. CLOSE — задача НЕ закрыта, открыт G5; закрыта только `t-walking-stops-deafening-1` с
     обоснованием.

log: |
  слух построен и перевыверен направлением на байтах: один ответчик на «доходит ли», фильтр внутри
  редукции до выбора громкого, радиусы 6/12/18 рядом с силами в ассете, 747 зелёных без пропусков —
  и вторая задача полосы умерла как ненужная, доказанно, впервые сработав правом не понадобиться;
  задача НЕ закрыта: восьмая строка приёмки требует живого прогона, а на этой самой строке день уже
  дважды сгорел, поэтому открыт G5

next: |
  Владельцу два текста: **G5 в свежую сессию на WIN-U2** и **записка в WIN-U4** о том, что чужой
  слот подтвердил гипотезу зависания словом `FurnitureInTheWay`.

  Волна: голова ждёт опровержения, вторая задача мертва, свой шум в работе, публикация ждёт
  зависания. Девятая волна ждёт дерева, на которое можно смотреть.
