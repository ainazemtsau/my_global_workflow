# s-research-012 — g-9c41 — research (owner reframe: cell-size × level-scale × full-sim)

date: 2026-06-22
play: research
direction: indie-game-development
node: g-9c41
input: owner plain message reframing the gas-engine — prioritize reactions/variety/interaction (#1); sacrifice level scale first (Lethal-Company-sized); topology matters; 25cm maybe non-critical; gas-position drift on return ok; squeeze multi-core. Four explicit questions (Q1–Q4 below).

> NB: session id = s-research-012 because s-research-011 was already used (2026-06-21, gas-model A+).
> The in-session RESULT block referenced s-research-011 (caught at writer-apply); the binding artifacts use 012.

---

## RESULT

```
play: research
goal: ответить на 4 вопроса владельца (25/50/100см × масштаб уровня × full-sim) реальными числами + разобрать рефрейм (реакции/разнообразие/взаимодействие №1; масштаб — первая жертва; дрейф позиции ок; многоядерность-рычаг)

outcome:
  Разбор завершён и адверсариально проверен (9-агентный веер: бюджет-модель + веб-якоря + восприятие + коллапс-сложности + компромиссы→фишки + 4 скептика). Ключевые выводы:
  (1) «Считать все клетки везде» = равномерная сетка → УБИВАЕТ множитель ×игроков (раньше 369k/игрок ×N; теперь пузырь = кусок единой сетки, 1 раз на всех). Это и есть, почему рефрейм владельца работает.
  (2) Q1: «эффект после входа в газ» развязан на 2 оси — ГОРИЗОНТ (50/100см честны, владелец прав) vs ВЕРТИКАЛЬ (размер клетки её и хоронит, владелец не прав). 25см≈7 слоёв, 50см≈3-4, 100см≈1-2. → не одно число: 50см дефолт, 25см точечно (вертикальные витрины), 100см большие горизонтальные залы.
  (3) Q2: клеток/м³=64/8/1; LC-уровень small/medium/large≈2.5k/6k/12k м³. full-sim-везде: 50см КОМФОРТНО даже на 1 слабом ядре; 25см ТЕСНО (нужен мультиядро ×4-8); 100см тривиально (потолок=восприятие).
  (4) Q3: малый уровень + full-sim-везде удаляет ~40% тяжёлой машинерии (collapse/expand, re-flux на движущейся границе=#1 недоказанный kill-тест, LOD-теги, A/B-дисциплина, R33-скачок, detail-как-слой+tripwire); детерминизм СТРОГО проще; интерактив+разрушаемость ЛЕГЧЕ (ограничение №8 не нарушено); многоядерность=идеал. Уступка по дрейфу (п.6) НЕ нужна в режиме сон-ВЫКЛ (позиция идеальна).
  (5) Q4: 8 компромиссов→фишек (карманы / присядь-под-него / арены-события / газ-живой / плотность-сигнал / малый-плотный-уровень / каскад-волной / контакт==картинка-дозой).

evidence:
  - Workflow wf_5a8b2de1-7f1 (9 агентов, 851k токенов, 4 адверсариальных вердикта).
  - АРИФМЕТИКА (верификатор-budget: воспроизведена точно): cells/m³=64/8/1; cell-counts 2.5k→160k/20k/2.5k, 6k→384k/48k/6k, 12k→768k/96k/12k; ×8/×64 по размеру клетки.
  - ВЕБ-ЯКОРЯ (датированные источники 2026-06-22): ONI полно-сеточный газ ~98-147k клеток/1 поток (straddl-ит наш якорь 75-160k); Factorio input-lockstep + ОТКАЗ от по-тайлового потока (Fluids 2.0); Teardown целые+команды-не-грид в шиппнутом кооп; Phasmophobia 50-70 комнат = жанровый потолок.
  - ПОДТВЕРЖДЁННЫЕ ОГОВОРКИ (скептики): (а) якорь 75-160k мог быть whole-CPU не single-core → мультиядерные вердикты оптимистичны ×4-8; (б) sleep НЕ опционален в худшем случае (зал+манипуляция) → ~40% коллапс держится, но без sleep-машинерии; (в) покомнатный микс ВОЗВРАЩАЕТ re-flux-шов (латентный конфликт двух находок); (г) масштаб ⟂ приоритету №1 (HOLDS), но не путать с огрублением клетки.
  - Единственное неизмеренное число = одно-ядровая пропускная на слабейшем ноуте (ангар-проба) — гейт перед выбором размера/масштаба.
  - Продукт: work/gas-cellsize-levelscale-2026-06-22.md (полный разбор + таблицы + решения).

state_changes:
  - work/gas-cellsize-levelscale-2026-06-22.md: СОЗДАН (продукт).
  - work/gas-engine-INDEX.md: обновлён (новый док в КАРТУ; секция ОТКРЫТО переписана — ограничения×компромиссы РАЗОБРАНЫ; батч 5 решений + ангар-проба).
  - LOG.md: +1 строка (s-research-012).
  - history/s-research-012.md: этот файл.
  - NOW.md / залоченная архитектура / активная ставка S0: НЕ ТРОНУТЫ (research информирует, не решает; Variant-A↔Alt-5.3 = решение владельца, re-shape/converge, не эта сессия).

captures:
  - "РЕШЕНИЕ-РАЗВИЛКА для S2/S4 shape: один глобальный размер клетки (удаляет re-flux-шов + ~40% сложности = Alt 5.3) vs покомнатный микс (25см-витрины, но шов возвращается). Триаж на shape S2/S4."
  - "Ангар-проба должна мерить ДВА числа (1 ядро + все ядра) с ТЯЖЁЛЫМ солвером (K≥2 типа + слои высоты + integer-релаксация), не лёгким ONI-подобным шагом."
  - "S0-PLAN: классифицировать каждую комнату слайса 'вертикаль-несущая→25см / горизонтальная→50-100см' — размер клетки = дизайн-решение, не глобальная перф-крутилка."
  - "Тик-рейт (предполож. 20 Гц) — второй по силе CPU-рычаг после размера клетки; не предложен владельцу как осознанный выбор. Поднять при шейпе бюджета."
  - "forced-coop (R45) на малом уровне имеет МЕНЬШЕ substrate (близость ≠ зависимость, аудит NEEDS-DECISION-4); прогонять каждую кооп-механику через mechanic-lenses СТРОЖЕ (g-d3a8)."

decisions_needed:
  - d-cellsize-default — Размер клетки по умолчанию? A(рек.) 50см+25см точечно(вертик.витрины)+100см большие горизонт.залы | B глобальный 50см | C глобальный 100см (УБИВАЕТ вертикаль). Рек.: A (но требует решить d-global-vs-mix).
  - d-global-vs-mix — Один глобальный размер vs покомнатный микс? Глобальный (удаляет re-flux-шов + ~40% сложности = чистый Alt 5.3) vs микс (25см-витрины, но шов/re-flux возвращается). Рек.: решать на shape S2/S4, не сейчас (S0 нейтрален).
  - d-level-scale-ceiling — Масштаб = LC-размер ~40 комнат как сознательный потолок? Да(рек., жанрово подтверждено; R39/R40/R45 переспроектировать/отложить) | Нет (возвращает LOD-машинерию).
  - d-sleep-policy — Сон обязателен или опциональная оптимизация? Опциональная(рек.), решать ПОСЛЕ ангар-пробы.
  - d-hangar-probe-first — Ангар-проба ПЕРВОЙ (1 ядро + все ядра, тяжёлый солвер)? Да(рек.) — единственное неизмеренное число, гейтит выбор размера/масштаба.

play_check:
  1. Recite — DONE: восстановлены 4 вопроса + рефрейм (8 пунктов).
  2. Investigate — DONE: 9-агентный веер + 4 адверсариальных верификатора; analysis + web, источники датированы.
  3. Confidence — DONE: точная относительная арифметика отделена от НЕизмеренного одно-ядрового якоря; названо, что меняет ответ (ангар-проба single+all-core, тяжёлый солвер); 3 оговорки переданы громко.
  4. Close — DONE: этот RESULT; findings в формате владельца (таблицы по 4 вопросам); продукт в work/; решения в decisions_needed.

log: "2026-06-22 — research (g-9c41, s-research-012): разобран рефрейм владельца «размер клетки × масштаб уровня × full-sim» — 9-агентный веер + 4 скептика. Выводы: «все клетки везде»=равномерная сетка убивает множитель ×игроков; 50см=sweet-spot; малый уровень+full-sim удаляет ~40% машинерии; масштаб ⟂ приоритету №1. 3 проверенные оговорки. Продукт work/gas-cellsize-levelscale-2026-06-22.md + INDEX обновлён. NOW/архитектура НЕ тронуты. → history/s-research-012.md"

next: awaiting_decision — 5 решений на подпись. S0 НЕ ждёт (нейтральна к развилке Variant-A↔Alt-5.3). При подписи: размер-клетки → классифицировать на S0-PLAN; глобальный-vs-микс → shape S2/S4; ангар-проба → отдельный замер-лег (рек. первым). Alt-5.3-как-база → re-shape/converge-re-open в свежей сессии.
```

END_OF_FILE: live/indie-game-development/history/s-research-012.md
