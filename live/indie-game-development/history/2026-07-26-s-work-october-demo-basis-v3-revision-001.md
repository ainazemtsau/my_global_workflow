# RESULT s-work-october-demo-basis-v3-revision-001

direction: indie-game-development
play: work
node/task: g-12fd/october-demo-basis
call: c-work-october-demo-basis-v3-revision-001
date: 2026-07-26

## outcome

The single live Demo Basis is now
`live/indie-game-development/work/october-demo-basis-v3.md`, identity
`october-demo-basis-v3`, written from the owner's five recorded decisions on the
ten findings of the FAILED independent verification of v2.

Version 2 is a tombstone pointing at v3 and at commit `c982c79a`, where its full
text lives; v1's tombstone now points at v3 as well. Exactly one fresh
verification CALL `c-converge-verify-october-demo-basis-v3-001` is open against
the v3 identity.

No owner decision was reversed. Procedural generation stays IN and mandatory, the
two hall mechanics stay deferred, terminology stays «вещество», the eight-item
MUST list stays eight items in the same order. What changed is what the text
stands on: all technique left the Basis for the engineering read, the causal
support collapsed to the one the owner named, and the quantities and public
claims that nothing backs were withdrawn.

`g-12fd` stays parked. No bet, task, track or shape was created. `TREE.md`,
`CHARTER.md` and `knowledge/**` are byte-unchanged.

## evidence

OWNER WORDS FOR THIS REVISION, verbatim as he gave them in this session
(2026-07-26). These are the five decisions; they are reproduced in the artifact's
final section so a later reader does not have to open this receipt.

1. «Технику убрать из Основы совсем. MUST 5 остаётся одной строкой:
   „Процедурная генерация уровня из модулей“. Никаких названий плагинов, сида,
   хоста и раскладки — это технический HOW, ему место в инженерной читке, а не в
   документе о содержании. Находки F1 и F2 закрываются вычёркиванием, моего
   решения там не требуется. Само решение стоит: генерация обязательна.»
2. «Для инженерной читки, не в Основу: я хочу, чтобы PGG и Dungeon Architect
   работали вместе. PGG определяет структуру внутри модуля, Dungeon Architect
   собирает из модулей сам уровень. Это совпадает с записанным пайплайном в
   work/pgg-analysis-2026-07-10.md. Отдельно проверить: Unity 6.3 с PGG не
   подтверждена (kill-риск), ноль прецедентов связки, арта ноль, до первого
   модуля 2–4 дня. Живая строчка i-procgen-determinism описывала вердикт
   неверно — там отклонён рантайм PGG, а не Dungeon Architect.»
3. «Опора „зачем нужны двое“ — одна, и этого более чем достаточно. Большой шар
   несут вдвоём, они координируются — это уже двое. Требование „обязательно
   двое“ из Основы убрать. Не доказывать на бумаге: смотрим в игре. Признак
   опровержения „один игрок последовательно проходит не хуже команды“ остаётся и
   есть та самая проверка. Ничего дополнительно не придумывать.»
4. «Провизорные решения для первого куска, не закон Основы: набор — игрок
   подбегает с шаром, жмёт E, вещество всасывается в радиусе перед ним; у шара
   есть ёмкость и её видно, держишь дольше — набирается больше. Действие
   вещества на игрока — подбрасывает или прижимает; урона и смерти нет; вопрос
   про смерть и спасение не трогаем. Процедурная анимация — позже, при
   расширении.»
5. «Остальные восемь находок — механические, чини сам, ко мне не возвращайся с
   ними. Время ограничено: 7 августа граница урезания объёма.»

APPROVAL BINDING, stated exactly. His words above are the approval for: the MUST 5
line (his own wording, transcribed character for character), the removal of all
technique from the Basis, the removal of the «обязательно двое» requirement, the
four provisional first-slice decisions, and the ordered engineering read. MUST
items 1–4 and 6–8 are carried from v2 verbatim and stand under v2's «да»
(2026-07-26, `history/2026-07-26-s-work-october-demo-basis-v2-revision-001.md`).

There is NO separate «да» on the assembled v3 text as a whole, and none was
requested: decision 5 explicitly forbids returning with the remaining findings.
Every mechanical repair made under that delegation is named in the artifact's
final section and again in the verification CALL, so the next fresh session
attacks it deliberately rather than discovering it. This leg claims no owner
approval it does not hold.

NO CONTENT WAS CHOSEN BY THE AGENT. Where a finding's options were all
content-level, the option taken is either the owner's own words (F1, F2, F3, F8)
or a subtraction of an unsupported claim (F7, F9) — never an invention. Two
repairs are additions and both are non-content: an explicit statement that no
exceptions are declared (F5) and three evidence dimensions plus the release
receipts (F6, F10).

NO LEGACY READ. Neither `archive/**` nor the frozen canon repository was opened.
The owner did not authorize reading the archived PGG spike close receipt that
finding F1 offered as an option; he ordered a fresh engineering read instead, and
that is recorded in `i-procgen-determinism` and `i-canon-repo-evidence-read`.

NO PRODUCT OR STEAM ACTION. Nothing was written outside `live/indie-game-development/`.

FRESH WRITER BASE: branch `wt/indie-game-development`, HEAD `89b1292b`
(`indie-game-development converge-verify g-12fd/october-demo-basis: Demo Basis v2
failed independent verification with ten named findings`); tracked state clean
before apply. The v2 blob is present in `c982c79a` (verified with
`git cat-file -e`), so the tombstone's pointer resolves.

## disposition of every finding — CALL done_when 2

| # | Class | Disposition | Where it landed |
|---|---|---|---|
| F1 | MUST 5 misattributed the recorded PGG verdict | CLOSED BY DELETION on owner decision 1. The tool name, the attribution and the conditional-made-unconditional acceptance are gone; the whole «Свидетельство под MUST 5» paragraph left «На чём это стоит». The archive-read option was NOT taken — the owner ordered an engineering read instead. | artifact §6 MUST 5; «На чём это стоит» → «Техника вынесена из Основы»; `i-procgen-determinism` |
| F2 | Determinism-removal rationale unsupported | CLOSED BY DELETION on owner decision 1. Host-side assembly, seed and layout replication appear nowhere in the Basis. The engineering question survives where it belongs. | artifact §6 MUST 5; `i-procgen-determinism` |
| F3 | Cooperation supports over-counted and leaning on deferred/undecided content | RESOLVED BY OWNER CONTENT, decision 3. One support (the big ball needs two sides at once, in his own words), the «обязательно двое» requirement removed, the refutation test and falsifier 1 named as the only check, the two removed supports recorded with the reason they were removed. Nothing added. | artifact §4 |
| F4 | Falsifier 5 vacuous or dead | MECHANICAL under decision 5. Text kept verbatim per his earlier instruction, marked «Признак спит», declared not-evaluated and not-fired while the mechanic is deferred, revived only with the mechanic and a new Basis version. Restoring the mechanic would have reversed his deferral; deleting it would have overridden his keep-verbatim instruction. | artifact «Исключения и признаки опровержения», falsifier 5 |
| F5 | done_when atom «исключения явны» uncovered | MECHANICAL under decision 5, and the subtractive option the finding offered was deliberately NOT taken: instead of striking the word from the heading, the artifact now states explicitly that it declares no case in which it does not apply, and names the provisional decisions and open questions as the only things it deliberately does not decide. That covers the atom rather than dodging it. | artifact «Исключения и признаки опровержения», first paragraph |
| F6 | Invalidation set not updated for a generated level | MECHANICAL under decision 5, and it follows from his own MUST 5. The evidence binding gains a fifth element (the exact generated level the attempt ran on) and two invalidators (module set, generation rules), all in plain content language with no technique. The doorway-width dimension the finding named vanished with F1's deletion, and the artifact records that the physical requirement now lives only in MUST 4. | artifact «Что отменяет зависимые доказательства»; note under §6 MUST list |
| F7 | Public-claim ceiling permitted claims no MUST can produce | MECHANICAL under decision 5, resolved by narrowing, because restoring a MUST would have reversed his deferral. «реагирует на здание» and «при настоящей опасности» moved from «Можно» to «Нельзя, пока не появится в сборке», with the reason recorded — including that his provisional first-slice decision has no damage and no death, so real danger is not showable. | artifact §5 |
| F8 | MUST 5 feasibility unbounded inside a fixed calendar | ANSWERED BY THE OWNER, decision 2: the engineering-verdict option. Not an appetite (this node creates no bet or tasks, so it cannot carry one) and not a cut. His four named checks and his correction of the issue's own text are recorded in the issue; the artifact names the dependency risk in one non-technical line under the MUST list, and the standing 2026-08-07 boundary is his own quoted limit. | `i-procgen-determinism`; `i-demo-scope-cap`; artifact note under §6 MUST list |
| F9 | «Двадцать минут» asserted and untestable; §2 asserted route-blocking and punishment | MECHANICAL under decision 5, resolved subtractively. The number left §1 and now lives only in open question 7; «перекрывает маршрут» and «наказывает за небрежность» left §2. Adding a session-length falsifier was rejected as inventing content he did not order («ничего дополнительно не придумывать»). | artifact §1; §2; open question 7 |
| F10 | Release-reality receipts only transitive | MECHANICAL under decision 5. «На чём это стоит» now names the Next Fest page-before-registration requirement with its source, the five standing dates, the 2026-08-07 boundary in his words, the Steam fee/credit/no-AppID state with its screenshot receipt, and the fact that no product build is bound to this Basis yet with the 2026-09-03 candidate gate. | artifact «На чём это стоит» → «Свежие receipts календаря и Steam» |
| D1 | A count stated where the itemization yields another number | APPLIED. The new verification CALL states open questions as a list and never as a count; the artifact keeps the itemized 1, 2, 3, 5, 6, 7, 8 with 4 resolved into MUST 5 and the v1 numbering deliberately preserved. | `work/c-converge-verify-october-demo-basis-v3-001-call.md` |
| D2 | `knowledge/strategy-reset-boundary.md` names a superseded CALL as the sole lawful frontier | REGISTERED, not fixed. `work` may not write `knowledge/`, so it became issue `i-frontier-knowledge-stale`, route review, with repair named as the earlier fallback if a planning leg is misled first. | `NOW.md` issues |
| D3 | v1's question 4 claimed procgen was in CUT | No action, as recorded. v2 carried no trace and v3 carries none. | — |

Nothing was silently omitted: every finding has a row, and every row says which
of the three dispositions it is.

## state_changes

Applied against fresh state at `89b1292b`:

1. `work/october-demo-basis-v3.md` — CREATED. The single live Basis, identity
   `october-demo-basis-v3`.
2. `work/october-demo-basis-v2.md` — replaced by a tombstone naming v3, the
   owner's «да» on v2, commit `c982c79a` where its full text lives, and the v2
   verification receipt.
3. `work/october-demo-basis-v1.md` — one line: `current:` retargeted from v2 to
   v3, with a note that v2 is itself a tombstone now. Nothing else touched.
4. `work/c-converge-verify-october-demo-basis-v3-001-call.md` — CREATED. One
   fresh verification CALL against the exact v3 identity, carrying the
   disposition of all ten findings, the narrower approval binding, the four
   caveats and the sharpest tension to attack.
5. `NOW.md` — `updated:` to this leg. `direction_forecast.basis` reworded for the
   unchanged fact (eight MUST items, no appetites, no G5 evidence) and stays
   `no_basis`; `drivers[0]` retargeted to v3 with what stands on owner words,
   what stands on his delegation, and the absence of a whole-text «да».
   `issues.i-demo-scope-cap`: v3 exists ahead of the boundary, the cut now hangs
   on a version passing verification, MUST 5's dependency is unbounded outside
   the Basis, his 2026-08-07 quote added. `issues.i-procgen-determinism`:
   rewritten from his decision 2 — the PGG + Dungeon Architect pipeline as he
   wants it, his four separate checks, and his correction that the recorded
   rejection is of PGG at runtime and never of Dungeon Architect; the remaining
   engineering-open items kept. `issues.i-concept-frame-admission` and
   `issues.i-canon-repo-evidence-read`: pointers moved to v3, and the second
   records that he declined the archived spike receipt. `issues.i-frontier-
   knowledge-stale`: ADDED (D2). `open_calls`: the returning
   `c-work-october-demo-basis-v3-revision-001` is cleared and replaced by exactly
   `c-converge-verify-october-demo-basis-v3-001`. `bet: null`, `tasks: []`,
   `recurring: []`, `decisions: []` unchanged; no track, no numeric chance.
   `i-steam-appid` and `i-steam-sequence-tree` untouched.
6. `LOG.md` — one line prepended.
7. `history/2026-07-26-s-work-october-demo-basis-v3-revision-001.md` — this file.

Not changed: `CHARTER.md`; `TREE.md` (`g-12fd` stays `parked`, no children);
`knowledge/**`; `os/**`; `work/concept-frame-v1.md`;
`work/pgg-analysis-2026-07-10.md`; `work/oracle-demo-boundary-specification-v1.md`;
`work/c-converge-verify-october-demo-basis-v1-001-call.md`;
`work/c-converge-verify-october-demo-basis-v2-001-call.md` (already spent with its
disposition); `work/c-work-october-demo-basis-authoring-001-call.md`;
`work/marketing/**`; the archive segments; the frozen canon repository; product
and Steam state. No owner panel is declared by the two retained knowledge
entries, so none was regenerated.

## done_when reconciliation

1. **v3 exists as the single live Basis, identity `october-demo-basis-v3`,
   carrying v2 forward unchanged except the owner's deltas; v2 becomes a
   tombstone naming v3 and the commit holding its full text.** PASS. Header
   carries identity, node, supersedes, the authoring CALL and this receipt. v2 is
   a tombstone naming v3 and `c982c79a`; v1's tombstone was retargeted so no
   pointer chain ends at a superseded file. Carried forward verbatim: the
   unverified-status line, the owner-is-sole-author line, the terminology note,
   «Для кого это демо», §3, MUST 1–4 and 6–8, SHOULD, «ПОТОМ РАССМОТРЕТЬ», CUT,
   falsifiers 1–4 and 6, the «Свидетельство, а не закон» file list, the authority
   guard sentence, and open questions 2, 5, 6, 7's candidate list.
2. **Every finding F1–F10 dispositioned explicitly; silent omission is not a
   disposition.** PASS. The table above gives all ten plus D1–D3, each labelled
   as owner words, confirmed mechanical repair, or registered elsewhere.
3. **No finding resolved by an agent choosing content; no owner decision from v1
   or v2 reversed without his words.** PASS with the boundary stated openly. Four
   findings were resolved by his words. Two were resolved subtractively (F7, F9) —
   withdrawing an unsupported claim rather than authoring a new one. Two were
   resolved additively but non-substantively (F5's explicit null, F6/F10's
   evidence dimensions and receipts). F4 changed status wording only and kept his
   verbatim text. No deferral was reversed, no MUST was added or removed, no open
   question was answered as Basis law, and the provisional decisions are marked
   not-law in the artifact itself.
4. **`NOW.md`: exactly one fresh `c-converge-verify-october-demo-basis-v3-001`
   against the exact v3 identity carrying this CALL's caveats plus the ten
   dispositions; `i-procgen-determinism` and `i-demo-scope-cap` updated from what
   actually changed; `bet: null`, `tasks: []`, no lane, no numeric chance.** PASS.
   The CALL carries the four caveats (reordered concept-frame questions; the
   bounded canon read with no new authorization; the proposed-not-promoted oracle
   with the three pieces of new v3 surface it has no test for; the WEAK
   contact-burst note left unrepaired) and the two-strikes watch.
5. **`LOG.md` one line; full RESULT in `history/`; `END_OF_FILE:` trailers; one
   commit.** PASS. Every written state file ends with its own trailer.
6. **`g-12fd` stays parked; no shape opened; TREE unchanged.** PASS. `TREE.md` is
   byte-unchanged; no bet, task, track or shape exists.

## deviations — edits beyond the literal instruction, and why

1. **`work/october-demo-basis-v1.md` `current:` retargeted to v3.** The CALL only
   ordered a v2 tombstone. Leaving v1 pointing at v2 would have left a two-hop
   pointer chain ending at a superseded file, which is the palimpsest the
   tombstone exists to prevent. One line, no content.
2. **One non-technical risk line under the MUST list.** Decision 1 removes
   technique from the Basis; the standing October calendar still makes MUST 5 the
   only item with an unresolved external dependency. The line names no tool, no
   seed, no host and no layout — only that the dependency exists and that the
   engineering read owns it. This is the one place where his order and the
   node-class expectation that a MUST names its own risk pull in opposite
   directions, and the verification CALL points at it explicitly rather than
   letting it pass as a smuggle. If he wants it out, it is one line.
3. **Issue `i-frontier-knowledge-stale` added.** D2 was a capture with no owner;
   `work` cannot write `knowledge/`, and the stale line is read by every day,
   frame and map leg before planning. Registered with a route and a trigger
   instead of left in a receipt.
4. **§4's «Запрещено» sentence made conditional** («Если двое и нужны, то из
   физической ситуации…»). Decision 3 removes the requirement that two are
   mandatory; the prohibition on an N-players admission rule is not that
   requirement and he did not ask for it to go, so it stands with the assertion
   removed from its reason clause.

## handed to verification, not resolved here

1. Whether one cooperation support carries §4 at all is exactly what the
   refutation test decides in play, per his instruction. Not arguable on paper
   either way, including by the verification.
2. MUST 5's unresolved external dependency versus his order that technique leave
   the Basis — named as the sharpest tension in the new CALL.
3. §3's contact-triggered bursting («Задели косяк. Хлопок.»), recorded WEAK by
   the v2 verification and never raised as a finding. §3 is owner content
   approved twice; left verbatim and handed over as deliberate attack surface.
4. Whether an explicit null satisfies the «исключения» atom (F5) and whether a
   dormant falsifier is honest (F4). Both are repairs made under delegation, so
   both belong in front of a fresh independent session.

## captures

- The provisional first-slice decisions (pickup on E with a visible ball
  capacity, toss-or-press with no damage and no death, procedural animation
  later) are the first concrete input the future `g-37a1` build has. They are not
  registered anywhere as work and open no node; they live in the Basis marked
  not-law, and the shape that eventually opens `g-37a1` is where they become
  tasks or die.
- `work/oracle-demo-boundary-specification-v1.md` is still only PROPOSED. Two
  verifications of the same node class will now have used it; promotion to
  `knowledge/` by review or pulse would stop each one re-deriving it.
- The owner's time pressure is now explicit and dated in his own words («7
  августа граница урезания объёма»), while the standing chain is still verify →
  narrow review → shape of `g-37a1`. No gate was weakened here; if the chain does
  not clear by 2026-08-07, `i-demo-scope-cap` fires and scope is cut, not the
  date.

## decisions_needed

None from this leg. Decision 5 explicitly forbade returning with the remaining
findings, and every one of them was dispositioned. The open content questions
stay the owner's to answer or leave open; none blocks verification.

## play_check

- Step 1 Recite — done. One untracked `to: session` CALL served the parked
  `outcome_kind: specification` outcome `g-12fd`; the no-bet exception applies.
- Step 2 Owner inputs (owner) — done. His five verbatim decisions of 2026-07-26
  are quoted in full under §evidence, including the MUST 5 line he dictated
  («Процедурная генерация уровня из модулей»), the removal instruction
  («Технику убрать из Основы совсем»), the cooperation ruling («Требование
  „обязательно двое“ из Основы убрать. Не доказывать на бумаге: смотрим в
  игре»), the provisional decisions («Провизорные решения для первого куска, не
  закон Основы»), and the delegation («Остальные восемь находок — механические,
  чини сам, ко мне не возвращайся с ними»). Nothing was re-asked, per that
  delegation.
- Step 3 Do the work — done in specification mode: the owner-authority contour
  wrote the exact versioned artifact from his words and repaired the delegated
  findings without choosing content. No executor was involved; no owner-content
  verdict was delegated.
- Step 4 Self-check — done point by point above, with the four deviations named.
- Step 5 Close — done. The node stays parked with bet/tasks/tracks absent, v2 is
  tombstoned, and exactly one fresh `converge-verify` is opened against the exact
  v3 artifact. Unrelated state preserved.
- G1–G4: pass. `bet: null`, `tasks: []`, no track, exactly one lawful planning
  frontier.
- G5: NOT claimed. Nothing in v3 is verified. This leg ran no in-session
  refutation pre-pass; the binding refutation is the fresh separate session this
  RESULT opens, and it must not be this chat.
- G7: no owner decision point was created; his decisions were already given.
- G9: pass. `CHARTER.md` and `TREE.md` byte-unchanged. The artifact records
  exactly which parts stand on his verbatim words, which stand on v2's «да», and
  that no whole-text «да» exists.
- G10: pass. This RESULT declares the complete delta; the leg wrote nothing
  outside it and became its own writer only after emitting it.

## log

2026-07-26 | s-work-october-demo-basis-v3-revision-001 | work | direction |
g-12fd/october-demo-basis: Demo Basis v3 written from the owner's five recorded
decisions — technique out of the Basis with MUST 5 as his one line, «обязательно
двое» dropped to one physical support, provisional first-slice decisions recorded
as not-law, engineering read ordered; the other eight findings repaired
mechanically under his delegation; v2 tombstoned, one fresh converge-verify
opened, g-12fd stays parked.

## next

`c-converge-verify-october-demo-basis-v3-001` — the sole ready frontier, in a
fresh session that is neither this chat, the v2 revision chat nor the v1
authoring chat. It carries the disposition of all ten findings, the narrower
approval binding, and the sharpest tension to attack. Only after it PASSES may
the narrow `review` close `g-12fd`, and only then may `shape` open `g-37a1`.

Standing calendar unchanged and unmoved by this revision: 2026-08-07 scope-cut
boundary if no Basis version has passed verification, 2026-08-11 page submitted,
2026-08-31 page public and registered, 2026-09-03 build candidate, 2026-09-15
demo live.

END_OF_FILE: live/indie-game-development/history/2026-07-26-s-work-october-demo-basis-v3-revision-001.md
