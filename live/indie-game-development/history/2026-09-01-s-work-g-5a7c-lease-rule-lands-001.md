# RESULT — s-work-g-5a7c-lease-rule-lands-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-the-bench-opens-quiet-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — аренда остановила работу третий раз за день, поэтому вместе с разрешением заведено правило в knowledge, читаемое каждым нарядом: вторая половина условия закрытия старой улики выполнена

close: light — because всё перевыведено направлением первой рукой: обе ссылки на кандидата, чистота дерева, состояние реестра, закрытость задачи-владельца аренды. Ничего не закрывается как достигнутое, кроме самой улики про аренду — и она НЕ закрыта, а доведена до выполнения обоих условий.

owner_approved: |
  Не требовалось: механика слотов. Владельцу сообщается фактом — и это **третий раз за день**,
  когда реестр слотов останавливает работу, о чём он должен знать.

outcome: |
  **АРЕНДА ОСТАНОВИЛА РАБОТУ ТРЕТИЙ РАЗ ЗА ОДИН ДЕНЬ, И КАЖДЫЙ РАЗ СТОРОЖ БЫЛ ПРАВ.**

  | заход | что встало | почему |
  |---|---|---|
  | G5 первый | ревью вернулось **без единого живого замера** | слот держала `…:BUILD` от сдавшей сборки |
  | G5 второй | ревью не смогло освободить аренду само | `-Release` дал `Access denied`, сторож отклонил повтор |
  | стенд | нога не смогла занять слот | `…:REVIEW` осталась после **закрытой** задачи |

  **ТРИ РАЗА — ЭТО НЕ НЕВЕЗЕНИЕ, А ОТСУТСТВУЮЩЕЕ ПРАВИЛО.** До сегодня оно жило в одном наряде;
  теперь заведено в `knowledge/a-lease-ends-with-the-work-that-took-it.md` с `read_by: work,
  review` — **читается каждым нарядом направления.**

  **ЭТО ВЫПОЛНЯЕТ ВТОРУЮ ПОЛОВИНУ УСЛОВИЯ ЗАКРЫТИЯ УЛИКИ, ОТКРЫТОЙ С 6 АВГУСТА.**
  `i-accepted-work-lives-only-in-a-released-slot-001` прямо писала: «Закрывается, когда оно
  появится в тексте, который читают все ноги». **Первая половина выполнена сегодня утром**
  (правило впервые попало в наряд), **вторая — сейчас.** Улика тем не менее **не закрывается**:
  она требовала также, чтобы аренда освобождалась сдачей работы, а не памятью исполнителя — этого
  инструмент пока не делает, правило лишь обязывает ногу сделать это последним шагом.

  **ПРАВИЛО НАПИСАНО ОБЕИМИ ПОЛОВИНАМИ, ПОТОМУ ЧТО ДЕНЬ ПОКАЗАЛ ОБЕ ОШИБКИ.** Не освобождать, пока
  работа не сохранена (иначе `preflight -Repair` её сотрёт — так уже терялась принятая работа), и
  не оставлять после того, как работа сдана (иначе следующая нога встанет). **Зеркальные ошибки
  одного корня.**

  **РАЗРЕШЕНИЕ ВЫДАНО, И НА ЭТОТ РАЗ БЕЗ УСЛОВИЙ:** работа аренды закончена, задача закрыта,
  кандидат сохранён **двумя** ссылками — `slot/win-u2` и `keep/hearing-b4b010c1`, обе на
  `b4b010c1`, дерево чистое. Проверка после `-Repair` оставлена как единственная предосторожность.

  **И В ВОЗВРАТ КАЖДОЙ БУДУЩЕЙ НОГИ ДОБАВЛЕНА СТРОКА:** «аренда освобождена» либо прямая причина
  удержания и **подо что именно**.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** в `C:\projects\Unity\GasCoopGame_win-u2`:

  - `git rev-parse slot/win-u2` → **`b4b010c1`**; `git rev-parse keep/hearing-b4b010c1` →
    **`b4b010c1`**; `git for-each-ref refs/heads/keep` → ровно одна ссылка;
  - `git status --porcelain` → **0 строк**;
  - реестр: `WIN-U2` = `CLAIMED`, аренда **`t-noise-reaches-by-distance-1:REVIEW`**;
    `WIN-U4` = `CLAIMED`, `t-morning-content-1:BUILD` — **вторая аренда тоже названа по задаче,
    которая давно сменилась**, слот работает над зависанием;
  - карточка `t-noise-reaches-by-distance-1` — в `closed/`, статус `done`.

  Из возврата ноги, как улика: preflight подтвердил ветку, HEAD, чистоту и зарегистрированный
  endpoint; claim запрещён из-за `CLAIMED`; файлы не изменялись, Unity не трогали.

state_changes: |
  СОЗДАНО (1 правило знания + 1 документ):
  - `knowledge/a-lease-ends-with-the-work-that-took-it.md` → `read_by: work, review`,
    `status: current` — обе половины правила, форма спасательной ссылки, требование строки в
    `return`;
  - `work/2026-09-01-authorise-release-win-u2-review-lease.md` — буквальное разрешение.

  БЕЗ ИЗМЕНЕНИЙ:
  - `c-exec-g-5a7c-the-bench-opens-quiet-1-001` остаётся `ready`: наряд не переиздаётся, к нему
    выдано разрешение;
  - `i-accepted-work-lives-only-in-a-released-slot-001` остаётся `open`: обе половины условия
    выполнены, но третья часть — чтобы аренда освобождалась инструментом, а не ногой — нет;
  - `t-the-bench-opens-quiet-1` остаётся `active`.

captures: |
  - **ТРИ ОДИНАКОВЫЕ ОСТАНОВКИ ЗА ДЕНЬ, И ТОЛЬКО ТРЕТЬЯ ЗАСТАВИЛА ЗАВЕСТИ ПРАВИЛО.** Первые две
    были починены разрешением «на этот раз». **Правило-кандидат: вторая одинаковая остановка — уже
    повод писать правило, а не выдавать второе разрешение.**
  - **ПРАВИЛО, ЖИВУЩЕЕ В ОДНОМ НАРЯДЕ, НЕ СУЩЕСТВУЕТ.** Утром оно было записано в текст наряда и
    к вечеру не помогло следующей ноге — она читала другой наряд. **Место правила определяет,
    работает оно или нет.**
  - **ВТОРАЯ ЗАНЯТАЯ АРЕНДА НАЗВАНА ПО ЗАДАЧЕ, КОТОРОЙ БОЛЬШЕ НЕ ЗАНИМАЕТСЯ.** `WIN-U4` держит
    `t-morning-content-1:BUILD`, а работает над зависанием. **Реестр читать как «кто взял», а не
    как «кто работает» — до тех пор, пока освобождение не станет частью сдачи.**

decisions_needed: |
  Ни одного владельческого.

play_check: |
  1. OPEN — СТОП ноги: слот занят чужой арендой.
  2. WORK — сохранность кандидата и закрытость задачи-владельца аренды проверены первой рукой;
     разрешение выдано буквальной формой, которой требует сторож; правило заведено в knowledge с
     `read_by`, чтобы третьего повтора не было; названо, что вторая занятая аренда тоже устарела.
  3. (owner) — не запрашивался: механика слотов; тройная остановка сообщена фактом.
  4. CLOSE — RESULT, применение, коммит; разрешение уходит владельцу.

log: |
  аренда слота остановила работу третий раз за день, и на третий раз вместо очередного разрешения
  «на этот раз» заведено правило в knowledge с read_by work и review: аренда не меняется, пока
  работа не сохранена ссылкой, и освобождается последним шагом ноги, а не памятью исполнителя —
  этим выполнена вторая половина условия закрытия улики, открытой с 6 августа; попутно замечено,
  что вторая занятая аренда тоже названа по задаче, которой слот больше не занимается

next: |
  Разрешение уходит владельцу для передачи в **WIN-U2**: снять аренду ревью, занять под стенд,
  выполнить наряд.

  Волна: голова закрыта, ходьба мертва, стенд ждёт слота, замирание меряется в U4, публикация ждёт
  только замирания.
