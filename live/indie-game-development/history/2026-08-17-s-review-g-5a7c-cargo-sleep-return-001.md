# RESULT — s-review-g-5a7c-cargo-sleep-return-001 (play: review)

## outcome

**Нога A доставлена и хороша, но задача НЕ закрыта: состязательная проверка нашла три вещи, и все
три перемерены Направлением первой рукой по байтам, а не приняты от агентов на слово.**

Вклад ноги не оспаривается и он настоящий: мир живёт, сон настоящий, каскад доказан, откат честен,
границы соблюдены, `WorldSituationKind` не тронут, а врущий близнец в тестах починен единым
источником — продукт и тест теперь берут `LootSupport.MinimumUpwardness` из одного места. Её
собственное ревью в три раунда нашло два разрыва и заставило их закрыть.

**Что оно пропустило:**

1. **Громкость приземления упала с пика окна до первого касания — и это доезжает до слуха хозяина.**
2. **Два теста на тишину проходят на базе** и не доказывают ничего.
3. **Две живые пробные ручки умеют заморозить мир навсегда.**

## evidence

Всё перемерено на опубликованном `main` = `75b770df`.

**Находка 1.** База брала силу бегущим максимумом по окну (`if (force <= _force) return;`). Теперь
`_supportForce` пишется только при захвате, а для продолжающейся опоры захват возможен лишь на первом
под-шаге (`captureSupport = !_hasSupport`). При `_physicsStepsPerTick: 2` вес, пришедший на втором
под-шаге, отчитывается лёгким первым касанием. Доезжает до игры: `SituationOf` кладёт в ситуацию
приземления ровно `contact.SupportForce`, пороги `_cargoRustleForce: 3`, `_cargoKnockForce: 15`,
`_cargoHeavyNoiseForce: 35`. **Уронённый предмет может перестать быть услышанным.**

**Находка 2.** На базе `AuthoritativeCargoRoster` читал контакт только при
`(cargo.State.IsActive || thingNeedsBody)`, `ReadContact` возвращал `default` для
`!Active && !ThingListening`, а `Step` досрочно выходил без активных тел — значит `Physics.Simulate`
не вызывался и коллбэков не существовало. Сто «тихих» тактов на базе тихи по построению, и оба теста
там зелёные.

**Находка 3.** `CargoSleepThreshold` и `CargoContactOffset` — единственные соседи без клампа
(`GameRulesSettings.cs:285,287` против `Mathf.Max` у `CargoGravity` и `CargoSolverIterations`), при
этом на них стоит `throw` в горячем пути (`CargoBody.cs:388,390`). Сегодня не стреляет — ассет
заполнен `0.00005` и `0.001`. Но ручки объявлены живыми пробными, и ноль в поле даёт исключение
внутри такта при `simulationMode == Script` **без `finally`**.

## state_changes

- `card close c-exec-g-5a7c-cargo-sleep-1-001` — наряд отработан и доставлен, с разбором в причине.
- `card new` три улики по находкам.
- `card new c-exec-g-5a7c-cargo-sleep-repair-1-001` — ремонт в `WIN-U3`, `ready`.
- `work/2026-08-17-paste-cargo-sleep-repair-1.md` — готовая паста с тремя строками и тремя минами.
- `t-cargo-sleep-1` остаётся открытой: строка «шум не сломан» не просто не доказана, она
  **опровергнута**.

## captures

**Метод сработал третий раз за день, и снова на том же классе.** Утром чужая рука нашла, что физику
всего проекта шагает груз. Днём тринадцать агентов нашли, что потолок в пять раз ниже. Сейчас четыре
агента нашли два теста, которые проходят на старом коде. **Один и тот же класс: проверка, которая
зелена, ничего не проверив.** Он ловится только чужими глазами и ловится каждый раз.

**И обратное тоже стоит записать:** два из четырёх агентов преувеличили тяжесть. Находка про
заморозку мира подана блокером, хотя сегодня она не стреляет: ассет заполнен. Направление понизило её
до настоящей — дефект есть, спусковой крючок в руках владельца, но мир сейчас не падает. Агентам
верят по фактам, не по громкости.

## decisions_needed

Ничего. Ремонт готов к выдаче, слот `WIN-U3` свободен.

## play_check

Каждая находка агентов проверена по коду Направлением, а не переслана владельцу как есть. Одна
понижена в тяжести. Задача оставлена открытой, потому что одна строка приёмки опровергнута, а не
недоказана — закрывать её было бы ложью.

## log

нога A доставлена и хороша, но задача не закрыта: три находки, и одна из них молча лишает хозяина слуха

## next

Владелец вставляет пасту ремонта в `Gas U3`.
