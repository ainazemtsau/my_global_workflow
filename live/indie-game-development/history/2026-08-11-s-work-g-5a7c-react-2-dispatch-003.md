# RESULT — s-work-g-5a7c-react-2-dispatch-003

play: work · выдача наряда на точку расширения — второй корень дорожки `householder`, освободившейся
закрытием `t-react-mem`
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-2
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `a41a9b09`, дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` = `5fe0dadb`, дерево `0be95abe`.
Слоты прочитаны файлом состояния: WIN-U2 `AVAILABLE / lease none`, WIN-U1 `CLAIMED` арендой
`c-exec-g-5a7c-settings-1-001:BUILD`. Пин контракта `36` сверен с продуктовым.

## outcome

Написан и выпущен наряд `c-exec-g-5a7c-react-2-002` в WIN-U2 от базы `5fe0dadb`. Прежний наряд
`c-exec-g-5a7c-react-2-001` закрыт `superseded`: он был остановлен владельцем до единой строки кода,
а карточка задачи с тех пор переписана трижды по его прямым требованиям.

Наряд не существует до расписки — `t-react-2` остаётся `open`, карточка наряда `ready`.

## evidence — перемерено первой рукой перед выпуском

**Шов уже есть и он хорош; не хватает МНОЖЕСТВЕННОСТИ.** `IHouseholderDecisionPolicy`
(`Householder.cs:856-860`) — один метод, а снимок `HouseholderDecisionSnapshot` (`:668`) уже несёт
всё нужное обработчику: `NoticedFacts` списком, `NoticedFactCount`, `ActiveResponse`, каталог,
`CurrentPosition`, и `TimesNoticedAt(...)` — накопление, закрытое `t-react-mem`. Политика ставится
свойством на существующий объект (`AuthoritativeHouseholder.cs:143-154`).

**Чего нет: обработчик ровно ОДИН.** `DecisionPolicy` — одиночное свойство; реализаций уже две
(`:862`, `:962`) плюс три тестовых двойника, и все конкурируют за одно место. Это и есть предмет
строки 1.

**Ветку поведения обработчик включить не может, и место правки измерено.**
`HouseholderDecisionProposal` (`:774`) несёт одну реакцию и цель — поля «включить режим» нет.
Распорядок — ОДНА циклическая лента (`AuthoritativeHouseholder.cs:317`,
`(_routineStepIndex + 1) % _profile.RoutineStepCount`), второй не существует. Правка стоит трёх
точек: поле в предложении (у него ТРИ конструктора `:776`, `:797`, `:822`), его потребление в
`Step`, переключение ленты. **Названо в наряде заранее как единственное место, где ядро правится, а
не дополняется.**

**Форма строки реакции — самое дорогое, что осталось:** `HouseholderReaction` — 14 публичных полей,
`new HouseholderReaction(` встречается СЕМЬ раз (шесть в `HouseholderReaction.cs`, один в тесте).
Новое поле строки стоит семи правок. Требование владельца про ВЫЧИСЛЯЕМЫЙ вес вписано в наряд с
обоснованием и с прямым указанием: платить семь правок ОДИН раз и сделать следующее поле дешевле.

**Заготовки в файле профиля перепроверены:** `"when": "always"` в каждом шаге распорядка и
намеренно пустой `IsKnownPlace` — места под условие и под места по имени.

**База зелёная:** `5fe0dadb` даёт `179 passed, 0 failed, 0 skipped` — прогнано Направлением на этой
голове.

## ИЗМЕНЕНИЕ ПРОТИВ ПРОШЛОГО НАРЯДА, названное вслух

Прежние наряды несли «`origin/dev` сдвинулся — СТОП ДОМОЙ». Базис протух ДВАЖДЫ подряд, оба раза
из-за публикации параллельной дорожки, и во второй раз исполнитель СТОП не исполнил, объяснив это
указанием владельца, которого в байтах нет.

**Правило заменено на перебазирование:** взять свежую голову `origin/dev`, собрать на ней, назвать
её в возврате отдельной строкой. Основание не в удобстве: клауза 5 `evaluator` этой волны и так
требует собирать вместе с ТЕКУЩЕЙ головой другой дорожки, поэтому перебазирование ей соответствует
ЛУЧШЕ, чем блокирующий СТОП. СТОП сохранён на всё остальное неожиданное.

Откат при этом привязан к голове дорожки, а не к константе наряда — приём, который на `t-react-mem`
единственный и выжил.

## Форма наряда

Контракт 36, ПРОБА, `done_when` свёрнут в три строки. Задача крупная, поэтому **линия разреза
названа заранее и резать по ней разрешено без возврата за словом**: строка 1 остаётся всегда,
первым уезжает авторинг данными, вторым — ветка поведения; обратный разрез запрещён, потому что
ветку без множественности включать некому. Срезанное называется в возврате.

Оба запрещённых довода владельца («сочетаний 2^N», «столько-то тестов сломается») вписаны в
границы как снятые дословно.

## state_changes

1. `c-exec-g-5a7c-react-2-001` закрыт `superseded` → `c-exec-g-5a7c-react-2-002`.
2. Заведена карточка наряда `c-exec-g-5a7c-react-2-002` (`slot: WIN-U2`, `basis: 5fe0dadb`,
   `status: ready`).
3. Написан текст `work/c-exec-g-5a7c-react-2-002-call.md`.
4. `t-react-2` остаётся `open` — расписки нет.
5. NOW, ставка, узел и дорожка `cargo-hands` не тронуты.

## captures

- **Форма строки реакции остаётся семь правок за поле.** `t-react-mem` починила первую дорогую форму
  до нуля; вторая ещё не чинена, и наряд просит сделать это заодно с добавлением веса, а не отдельно.
- **Три тестовых двойника уже реализуют `IHouseholderDecisionPolicy`** — при переходе к композиции
  они переедут; наряд это не запрещает, потому что владелец снял довод про тесты.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.
- Карточка вопроса про два непроверенных утверждения прошлого возврата остаётся открытой.

## decisions_needed

Нет. Жеребьёвка и случайность в наряд не входят — по ним его решения нет, и наряд это прямо говорит.

## play_check

- 1 recite: done — шесть строк карточки и все его дословные требования сведены в три строки
- 2 owner inputs (owner): done — выдачу он назвал сам; новых фактов от него не требуется
- 3 do the work: done — продукт перемерен в байтах по всей поверхности задачи, слоты прочитаны
- 4 self-check: done — каждая строка разложена; единственное место правки ядра названо заранее с
  числом точек; изменение правила про базис названо вслух с основанием
- 5 close: checkpoint — расписка вне этой ноги

## Гейты

- **G1.** Дорожка `householder` держит один корень; `cargo-hands` свой; лимит `2` цел.
- **G2.** Задача служит активной ставке.
- **G3.** Аппетит: потолок задач снят его словом; линия разреза названа, чтобы задача не раздулась.
- **G7.** Форма обработчика оставлена исполнителю; жеребьёвка вынесена из наряда, потому что это
  его решение.
- **G9.** Карточки задачи и ставки не редактировались.
- **evaluator, клауза 3.** Расписка первой строкой возврата.
- **evaluator, клауза 5.** Исполнена перебазированием на текущую голову другой дорожки.

## log

наряд на точку расширения выдан в WIN-U2 от свежей головы 5fe0dadb, а прежний остановленный наряд закрыт superseded; перемерено, что шов уже есть и он хорош — интерфейс политики один метод, снимок несёт список фактов, активный ответ, каталог и накопление TimesNoticedAt, политика ставится свойством на существующий объект, — и не хватает ровно МНОЖЕСТВЕННОСТИ: DecisionPolicy одиночное свойство, а реализаций уже две плюс три тестовых двойника, и все конкурируют за одно место; ветку поведения обработчик включить не может, и цена правки измерена заранее — поле в предложении с ТРЕМЯ конструкторами, его потребление в Step и переключение единственной циклической ленты распорядка, это названо единственным местом задачи, где ядро правится, а не дополняется; форма строки реакции остаётся самой дорогой — 14 полей и семь мест сборки, поэтому требование владельца про ВЫЧИСЛЯЕМЫЙ вес вписано вместе с указанием заплатить семь правок один раз и сделать следующее поле дешевле; правило про базис ЗАМЕНЕНО с блокирующего СТОПа на перебазирование, потому что базис протухал дважды подряд из-за публикаций параллельной дорожки, а клауза 5 evaluator и так требует собирать с текущей головой другой дорожки; линия разреза названа заранее — первым уезжает авторинг данными, вторым ветка поведения, обратный разрез запрещён

## next

1. Владелец отдаёт наряд исполнителю; первая строка возврата — расписка.
2. Возврат приходит HOME; `t-react-2` закрывается отдельной ногой после публикации.
3. `t-react-3` — его вердикт по дорожке — после `t-react-2`.
