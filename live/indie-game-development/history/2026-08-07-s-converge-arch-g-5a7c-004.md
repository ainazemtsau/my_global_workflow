# RESULT — s-converge-arch-g-5a7c-004

direction: indie-game-development
play: converge-arch
node: g-5a7c
call: work/c-converge-arch-g-5a7c-repair-001-call.md (возврат ноги) — эта нога записывает ответы
      владельца, полученные ПОСЛЕ возврата, ровно как это делали `s-converge-arch-g-5a7c-002` и `-003`
date: 2026-08-07
session: владелец в чате; та же физическая сессия, что и ремонт, но отдельная нога — свой RESULT,
      свой apply, свой коммит, и Git перечитан заново перед началом

**База продуктовых фактов перепроверена первой рукой ДО любого `факт кода`:**
`C:/projects/Unity/GasCoopGame`, `git rev-parse HEAD` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`,
`git status --short` пусто. Только чтение; репозиторий не изменялся.

**Состояние OS перечитано перед началом:** `git log -1` = `2d2404b4` (коммит ремонтной ноги),
`git status --short` пусто. Ремонт применён и закоммичен, эта нога работает на нём.

---

## outcome

**Владелец, увидев три понижённые ремонтом строки одним списком, закрыл своим словом все три и добавил
четвёртый токен — требование по ИСХОДУ, которого в наборе не было.** Записано четыре его токена; два
соседних высказывания записаны ВХОДОМ, а не требованием, потому что он смягчил их сам.

Счёт: `arch_lines_owed_to_owner` 3 → 0, `contracts_open` 2 → 1, внутренних швов 16 → 17, вердиктов
владельца 1 → 4. **Подписей под текстом карточки по-прежнему НОЛЬ**, карточка `g-5a7c` не тронута ни
знаком.

**Владельцу НЕ выносилось ни одного нового решения.** Он ответил на понижения, которые называли
отвечающего и не задавали вопроса — ровно тот механизм, который наряд ремонта и предписывал.

**Один долг нога знала и не имела права закрыть сама:** `converge-arch` в §WHAT не пишет никогда, а
`shape` копирует в `done_when` именно §WHAT. Долг назван в §SIGNOFF арх-файла и передан наряду
`c-converge-g-5a7c-owner-answers-001`; `c-converge-verify-g-5a7c-002` переведён в `waiting`.

---

## evidence

### 1. ДВЕРИ — «пока не нужно» (закрывает `C26`)

Его слова дословно: **«насчет дверей я думаю пока не нужно потом нужно будет прорабоать»**.

**Что закрыто.** Двери в узел не входят. Понижённое ремонтом утверждение (будто состояние двери куплено
критерием 6) не воскресает и не подтверждается — оно перестаёт быть вопросом ЗДЕСЬ. Следствие для
критерия 6 — дословно то, что уже стояло в `W63` как её `rewrites:`: ослабление у проёма становится
СТАТИЧЕСКИМ свойством, а переменной «открыта / закрыта» в узле не заводится. Работы это не добавляет, а
СНИМАЕТ ту, которую прежний вывод объявлял купленной.

**Сила токена.** Вердикт по МАРШРУТУ, захеджированный одним словом («я думаю»), но однозначный в
главном: не сейчас. Вторая половина фразы сохранена целиком — «потом нужно будет прорабоать», — значит
двери отложены его словом наравне с `W15`/`W16`, а не отменены, и вернутся его же отдельным словом.
Названная заранее цена физической двери (одно число в снимке против шести степеней свободы) остаётся
записанной ровно для этого возврата.

**Что НЕ правится.** §WHAT не тронут: `W63` остаётся `→ PLAN` и остаётся ВЕРНОЙ — её собственный текст и
описывает выбранный им исход. `contracts_open` 2 → 1.

### 2. КТО ЗАДЕВАЕТ ОБСТАНОВКУ — «и груз и человек» (закрывает оговорку `A9`)

Его слова дословно: **«и груз и человек (НО обстановка так то статичная просто некотоыре элементы
интерактивные будут)»**.

**Что закрыто, и это ДОРОЖЕ прочтения, которое держала нога.** Из двух читок выбраны ОБЕ. Первая
(задевают несущие вместе с грузом) остаётся и не стоит ничего. Вторая — задевает ТЕЛО человека —
принята, и она требует своего правила в слое правил: от движка такого события не придёт никогда
(`NetworkWalkerPresentation.cs:203-205` выключает коллайдер ходока; `CargoNoise.cs:57-64` объявляет это
прямо). Уставная строка `CHARTER.md:92` теперь исполняется буквально, а не в сужении.

**Цена измерена первой рукой, и она мала, потому что машинка уже написана.** `WalkSpaceProbe.PushOut`
(`:47-79`) считает наезд круга ходока (`_walkerRadius: 0.45`, перемерено) на прямоугольник X/Z и
**возвращает `true` ровно в тот шаг, когда наехал**. Обстановка по `C25` входит в описание дома наравне
со стеной, то есть становится таким же прямоугольником. Значит «человек задел обстановку» — тот же
расчёт, что уже гоняется каждый шаг для стен. Новое ровно одно: этот `true` должен доехать до слоя шума
— второй источник, шов `C29`.

**Столкновение с уже принятым проверено дословно, и его нет.** `CargoNoise.cs:57-64` хранит: правило
«ГРУЗ встретил ЧЕЛОВЕКА» было написано, работало, и владелец 2026-08-06 велел убрать его ЦЕЛИКОМ, а не
приглушить. Сегодняшний ответ — про другую пару: ЧЕЛОВЕК ↔ ОБСТАНОВКА. Убитое правило не воскресает, и
никакая будущая нога не вправе сослаться на «и груз и человек», чтобы вернуть шум от луча, встретившего
игрока. Это записано и в `A9`, и в `C25`, и в `C29`.

**Его скобка — ВХОД, а не требование.** «Обстановка **так то** статичная просто некоторые элементы
интерактивные **будут**» — оба смягчения его собственные, второе ещё и в будущем времени. Что она
закрывает: вердикт `A9` (обстановка не движется) в силе третий раз, подвижных тел не заводится, снимок
не растёт. Чего нога НЕ додумывает: что такое «интерактивный элемент». Не расшифровано ни во что;
заведена строка **`open → OWNER`**, вопрос ему НЕ задан. Ближайший родственник этого слова в наборе —
двери, и их он той же репликой отложил.

**Что ответ ОТКРЫЛ, и это названо, а не решено.** Если обстановка — прямоугольник, из которого ходока
выталкивают, сквозь стол не пройти; если только источник шума — проходят, и он шумит. Разница видна
глазом за один заход и меняет, как дом ощущается при ходьбе. **`→ PLAN`** с прямой пометкой: если задача
увидит изменение в ощущении дома, она обязана принести это ЕМУ. Отдельного архитектурного вопроса не
заводится — обе формы стоят одну и ту же уже написанную машинку.

**Честно против самой ноги: цена ему при ответе НЕ показывалась.** Ему были названы две читки, но не
было сказано ни что вторая требует своего правила, ни что похожее правило он уже убирал. Вердикт по
ЧИТКЕ записан настоящим — он однозначен, — но задача, которая до этого дойдёт, **обязана показать ему
цену прежде, чем строить**, и запись существует ради этой обязанности.

### 3. ЧЕМ БЕРУТ ПРЕДМЕТ — «Прицелом и близостью» (закрывает третью треть `A8`)

Его слова дословно: **«Прицелом и близостью НО я думаю что потом можно будет дальность прокачивать»**.

**Что закрыто.** Первая половина сказана без оговорок и опровергает прежнюю формулировку арх-файла:
хват берётся не «прицел ВМЕСТО близости», а обоими сразу — кандидат тот, на который смотришь, И он в
пределах дальности. Это одно правило с двумя условиями, а не выбор между двумя правилами.

**Вторая половина — ВХОД.** «Я думаю, что потом можно будет дальность прокачивать» — смягчено дважды.
Стоит ноль: выжившие две трети `A8` уже делают длину луча переменной на держателя, а дальность прицела —
тот же `_cargoPickupRange` (перемерено: `1.3`, восемь ростов при 0.16 м), ставший полем держателя.
Прокачка позже наливает число, переписывать нечего — как `CargoHold.Strength` стоит пустым швом под
личную силу. Его правило «логика только расширяется» исполняется даром.

**Чего ответ не покупает:** как ощущается управление лучом от первого лица — по-прежнему его глаз
(`M7`), и первая же задача обязана дать ему это посмотреть. Цена ему при ответе тоже не показывалась;
здесь обе половины стоят почти ноль, и это записано.

### 4. ВЕС И ПОКОЙ — требование по ИСХОДУ, которого в наборе не было

Его слова дословно: **«насчет веса нужно продумывать систему что бы такого не было что бы груз без
взаимоджействия привлекал хозяина и издавал шум»**.

Сказано без единой оговорки и в повелительном наклонении. Это **не выбор варианта, а требование к
ИСХОДУ**, поставленное на находку `A10`: покоящийся груз при `_cargoMass` 50 отчитывался бы ≈409 Н при
верхнем пороге `_cargoHeavyNoiseForce: 400`. Он прочитал следствие и закрыл его сам.

**Что закрыто.** Варианты (а) `A10.2` («пол — тот же контакт») и (г) («поднять пороги») отклонены теперь
дважды — измерением и его словом. Граница проведена там, где нужна критерию 4: без взаимодействия —
тишина; волочение взаимодействием ЯВЛЯЕТСЯ и обязано быть громче переноски. Выживший (б) — опора как
отдельный источник со своей кривой — требование исполняет.

**Чего оно не выбирает.** Механизм: «нужно продумывать систему» — заказ на устройство, а не его
описание. Кривая опоры остаётся `→ PLAN`, чисел не названо ни одного. И `W115` (как число носильщиков
входит в громкость) его словом НЕ закрыта — он говорил про покой, а не про количество рук.

### 5. Долг перед §WHAT — назван, не закрыт, передан наряду

`converge-arch` в §WHAT не пишет НИКОГДА, а `shape` копирует в `done_when` именно §WHAT. Три места:

1. **`W45`** («каким швом обстановку задевают → ТЕМ ЖЕ, что построен: контакт производит шум») стала
   НЕПОЛНОЙ: контакт человека с обстановкой через шов контакта груза прийти не может физически.
2. **Требование по исходу** — кандидат в свою строку §WHAT: без неё оно потеряется на `shape`. Отдельно
   передано решение, несёт ли строка метку `acceptance`, с доводами в обе стороны.
3. **Вердикт по дверям** надо записать строкой; сам ответ `W63` при этом остаётся верным, поэтому это
   уточнение, а не исправление.

Выдан `c-converge-g-5a7c-owner-answers-001`. `c-converge-verify-g-5a7c-002` переведён в `waiting` и
дополнен разделом про четыре токена — судить набор с расхождением двух счётов значило бы подарить второй
FAIL на дефекте, который уже назван и уже расписан наряду.

---

## state_changes

1. `live/indie-game-development/work/converge-g-5a7c-arch.md` — шапка (`owner_answers:`); `A8` — новый
   подраздел с его вердиктом по третьей трети; `A9` — новый подраздел «ЕГО ОТВЕТЫ НА ТРИ ПОНИЖЕННЫЕ
   СТРОКИ» (три пункта); `A10.2` — его требование по исходу; §NOT-DECIDED — три строки помечены
   закрытыми его словом, две новые заведены; §SIGNOFF — новый абзац, обновлённый счёт, долг перед §WHAT;
   §REPAIR-LEDGER — второй блок.
2. `live/indie-game-development/work/converge-g-5a7c.md` — **только §CONTRACTS**: вводный абзац (второй
   список тронутого), «Правила чтения», `C25` (его вердикт по оговорке и скобка входом), `C26` (закрыт
   его вердиктом), **новый `C29`**, «Счёт этой ноги» и фенсированный блок. §DRIFT, §IMPORT, §GLOSSARY,
   §WHAT, §COUNT и оба §SIGNOFF — ни знака.
3. `live/indie-game-development/NOW.md` — `open_calls`: завести
   `c-converge-g-5a7c-owner-answers-001` (`ready`), перевести `c-converge-verify-g-5a7c-002` в
   `waiting`; добавить строку-комментарий. Ставка не открывается, задачи и полосы не создаются, `issues`
   и `decisions` не тронуты.
4. `live/indie-game-development/LOG.md` — одна строка вверх.
5. `live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-004.md` — этот RESULT целиком.
6. `live/indie-game-development/work/c-converge-g-5a7c-owner-answers-001-call.md` — новый наряд.
7. `live/indie-game-development/work/c-converge-verify-g-5a7c-002-call.md` — дополнен разделом про
   четыре токена и переведён в `waiting`; НЕ переиздан.

**Не трогается:** `TREE.md`, `CHARTER.md`, `knowledge/**`, §WHAT и §COUNT файла
`work/converge-g-5a7c.md`, `W15`, `W16`, продуктовый репозиторий.

---

## captures

- **CAPTURE (не действовать в этой ноге).** «Интерактивные элементы» обстановки и «двери» — это, похоже,
  одно и то же слово с двух сторон: у обоих речь про предмет обстановки, у которого есть состояние,
  меняемое игрой. Он отложил двери и назвал интерактивные элементы в одной реплике. Если это подтвердится
  его словом, узел получает не два вопроса, а один. Читать вместе с
  `i-digging-own-bet-authored-fresh-001`, где точно та же форма («у названной вещи есть состояние,
  которое меняет игра») уже была признана единственным по-настоящему новым в его картине тоннелей.
- **CAPTURE.** Второй источник шума (`C29`) впервые делает слой шума тем, чем его заказывал критерий 4 —
  слоем с НЕСКОЛЬКИМИ источниками, а не одним. То есть проверить «слой расширяем» станет можно уже на
  первой волне, а не на второй. Улика для `shape`.

---

## decisions_needed

**Ни одного нового.** Ему не задан ни один вопрос. Открытые строки называют отвечающего: «что такое
интерактивный элемент обстановки» — он, спрашивает та нога, которая первой доведёт обстановку до задачи;
`A4`/порог по числу рук (=`W114`) — задача, с возвратом к нему; `W115` — задача общего слоя шума; кривая
опоры и «выталкивает ли обстановка ходока» — `→ PLAN`.

---

## play_check

```
owner_tokens_recorded: 4 — двери («пока не нужно»), кто задевает («и груз и человек»), чем берут
      («Прицелом и близостью»), требование по исходу («груз без взаимодействия» не шумит). Плюс 2
      токена записаны ВХОДОМ, потому что он смягчил их сам: прокачка дальности, «интерактивные элементы»
owner_tokens_not_inflated: PASS — каждое смягчение сохранено дословно; ни один токен не назван
      подписью под текстом карточки; подписей по-прежнему 0
owner_decisions_raised: 0 — ни одного вопроса ему не задано; он ответил на понижения, которые называли
      отвечающего и вопроса не формулировали
collision_with_prior_owner_word: PROVEN ABSENT — убранное им 2026-08-06 правило про пару ГРУЗ ↔ ЧЕЛОВЕК
      сверено дословно по CargoNoise.cs:57-64; сегодняшний ответ про пару ЧЕЛОВЕК ↔ ОБСТАНОВКА, и это
      записано в трёх местах, чтобы никто не сослался сюда за возвращением убранного
cost_not_shown_disclosed: PASS — записано против самой ноги, что по двум токенам (кто задевает; чем
      берут) цена ему при ответе не показывалась, и что задача обязана показать её прежде, чем строить
contracts_open: 2 → 1 — C26 закрыт его вердиктом; остался C05 (→ map)
contract_rows: 12 краёв + 17 внутренних швов (добавлен C29 по его ответу)
arch_lines_owed_to_owner: 3 → 0
arch_lines_newly_owed_to_owner: 1 — «что такое интерактивный элемент обстановки», вопрос НЕ задан
arch_rows: 10, arch_open: 9 — без изменений; A4/порог им НЕ отвечен и остаётся open → PLAN (=W114)
arch_in_context_only: PASS — ни одна строка арх-файла не является требованием и не едет в done_when;
      именно поэтому требование по исходу передано в §WHAT отдельным нарядом, а не оставлено здесь
what_untouched_by_this_leg: PASS — §DRIFT, §IMPORT, §GLOSSARY, §WHAT, §COUNT и оба §SIGNOFF
      converge-ноги не тронуты ни знаком; open_rows остаётся 10, deferred остаётся 2
what_debt_named_not_hidden: 3 места (W45, требование по исходу, вердикт по дверям) — переданы наряду
      c-converge-g-5a7c-owner-answers-001, а не оставлены молча
W15_W16_untouched: PASS
card_untouched: PASS — TREE.md не открывался на запись
base_reverified_first: PASS — 352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa, дерево чисто, только чтение
fan_out: одиночный контур по прямой постоянной инструкции владельца
next: converge (owner-answers), затем ВТОРАЯ converge-verify свежей сессией
```

---

## log

2026-08-07 | s-converge-arch-g-5a7c-004 | converge-arch | direction | Владелец закрыл своим словом все
три понижённые ремонтом строки и добавил требование по исходу; `arch_lines_owed_to_owner` 3 → 0,
`contracts_open` 2 → 1, швов 17. Долг перед §WHAT назван и передан наряду.

---

## next

`c-converge-g-5a7c-owner-answers-001` (`ready`) — занести четыре его токена в §WHAT и привести §COUNT.
§CONTRACTS и арх-файл трогать запрещено: они уже приведены, и побайтовая проверка этого правила в обе
стороны — единственный механизм, которым два счёта перестали расходиться.

Затем `c-converge-verify-g-5a7c-002` (`waiting` → `ready`) — **ВТОРАЯ проверка узла, свежей физической
сессией.** По плею второй FAIL кончает бумажную фазу: возврата не будет, поверхность замораживается
уликой, и владельцу выносится ОДНО решение с тремя ветками. Наряд уже дополнен атакой на четыре токена;
самая дорогая из них — «и груз и человек», потому что это единственный ответ, принявший работу, цена
которой ему при ответе не показывалась.

Дальше, при PASS, — `shape`.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-004.md
