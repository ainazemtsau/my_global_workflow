# RESULT — s-shape-g-5a7c-wave-2-001

play: shape (call: `c-shape-g-5a7c-wave-2-001`)
direction: indie-game-development · node: g-5a7c · bet: bet-g-5a7c-wave-2
date: 2026-08-10

base_read: OS — `wt/indie-game-development` = `1d35ceb3`, рабочее дерево чисто на входе.
`NOW.bet` был пуст, `g-5a7c` стоял `shaped`. **Продукт этой ногой не читался и не менялся ни
байтом:** факты продукта взяты из `context` наряда, перемеренные ревью на опубликованной голове
`43648b2d` — наряд это прямо разрешил, а `shape` только режет.

## outcome

Вторая волна узла `g-5a7c` нарезана и подписана владельцем. Ставка `bet-g-5a7c-wave-2` открыта:
**одна ставка, ДВЕ дорожки, идущие одновременно, плюс одна общая задача-шов до их расхождения** —
его форма, не изобретение ноги.

**Его слова, полученные этой ногой, все три на прямо заданные вопросы с показанными вариантами:**

1. **Аппетит — «Принимаю все твои рекомендации»** на показанный целиком экран с ТРЕМЯ размерами и
   честно названным размером КАЖДОГО до выбора. Рекомендацией был вариант **Б: семь задач по
   полдня и ОДИН новый слой.** Отклонённые показаны рядом: А — пять задач, один слой; В — десять
   задач, ДВА слоя.
2. **Число одновременных дорожек — ДВА**, тем же словом (рекомендация была два), плюс его
   отдельное правило: «по возможности мы, естественно, должны как можно больше параллелить, но не
   в ущерб качеству. Если там какие-то задачи будут блочить, ну, типа, не знаю, одна задача
   блочит две полосы, то, ну, естественно, ждем. Но как бы я, мне кажется, тут такого у нас не
   должно быть.» Это его основание под общую задачу-шов: пока `t-situation-1` идёт, занято НОЛЬ
   дорожек из двух.
3. **`evaluator` — вариант 1** (фактический порядок как закон), из двух показанных с честной
   ценой каждого. **Блок написан.** В закрытой ставке `bet-g-5a7c` его не было ВОВСЕ, и триггер
   ревью 2026-08-07 не исполнялся дважды; на третий раз исполнен.

**Карточка узла:** переписаны РОВНО два блока, `appetite` и `kill_by`, оба описывали законченную
первую волну. Двенадцать критериев `done_when`, `goal`, `why`, `risk`, `edge` — **не изменены ни
в одном знаке** (G9); проверено диффом: три хунка, все внутри `status`, `appetite`, `kill_by`.

**Наряды исполнителям НЕ выпущены** — это прямая граница наряда. Зарегистрирован один локальный
фронтир `c-work-g-5a7c-situation-1-001` (`to: session`, `play: work`), и он несёт требование
доставить исполнителю ПОЛНЫЙ текст с распиской.

## evidence

**Форма и её основание.** Две дорожки объявлены его словами и живут карточками `cargo-hands`
(«Груз слушается рук», референс его же: «Да, R.E.P.O.») и `householder` («На что и как реагирует
хозяин»). `NOW`: `bet: g-5a7c`, `track_wip_limit: 2`.

**Размер держится измеренным правилом, а не вкусом.** Правило выведено ревью из трёх волн:
11 задач / 0 новых слоёв → дошла; 23 / 3 → `partial`; 7 (факт 8, построено 6) / 1 → `met`. Эта
волна — **7 задач / 1 новый слой**, то есть форма той, что вернула `met`. Заведено ровно семь
карточек задач.

**Единственный новый слой назван и куплен один раз:** `t-situation-1`, слой ситуаций мира. Его
зачаток вырос СЛУЧАЙНО из `t-scale-4` и был частью выреза №3 первой волны — то есть слой уже
начал протекать в игру без аппетита, и эта ставка его покупает явно.

**Порядок задач против рискованных допущений** (устав в осторожном режиме): `t-cargo-1`
(провисание, снятие двух заморозок вращения) стоит ПЕРВОЙ в своей дорожке, потому что это
допущение №1 и его тест — самый дешёвый из возможных: провисание либо работает, либо нет.
`t-react-2` (приём больше одной ситуации) — допущение №2, со своим откатом. `t-react-3`
(подставленный источник) — допущение №3 и ставка выбранного подхода.

**Оба отката названы ДО работы и обязаны быть доказаны настоящим revert до дерева базы** — тем
приёмом, каким закрывалась `t-scale-6` (`revert cbf3a360`), а не описаны словами.

**Границы по файлам — механизм, а не пожелание.** Ни одна дорожка не открывает
`IntegratedHouse.unity` (в `g-1d84` полосы разошлись на нём дважды, и слитые байты не запускал
никто); `NetworkPlaySettings.asset` принадлежит `cargo-hands`; `Network/CargoBodyContact.cs`
трогается ТОЛЬКО в общей задаче, до расхождения, поэтому в дорожках он ничей. Пороги
`60 / 150 / 400` не двигает никто.

**Шесть улик наряда разложены поимённо** — блок `evidence_disposition` ставки. Три едут в
`cargo-hands` (`t-cargo-2` закрывает две и снимает мёртвую ручку третьей), одна становится
содержанием общей задачи, одна РАСЩЕПЛЕНА надвое с обеими половинами названными, одна едет
райдером в `t-react-2` со своим собственным запретом («мерить прогоном, пороги не двигать»).

**Вырезов восемь, первый настоящий:** личная мощность хвата как число на игроке вырезана, и это
вырезано ЕГО ЖЕ слово. Основание перемерено ревью: шов уже стоит (потолок читается у КАЖДОЙ руки,
общего числа в коде нет вовсе), а прокачка запрещена подписанной строкой узла — значит вырез
ничего ощутимого не отнимает. Названо ему ДО подписи, чтобы он мог вернуть; не вернул.

**Механическая проверка состояния прогнана этой ногой:** `osctl check` → «механических проблем
нет» (100 карточек, живых 73). `osctl context --for g-5a7c` собирает ставку, обе дорожки и все
семь задач — то есть следующая нога увидит свой рабочий набор, а не пустоту.

## state_changes

1. `g-5a7c`: блок `appetite` переписан целиком под вторую волну (27 строк), блок `kill_by`
   переписан целиком (24 строки, ПЯТЬ порогов, все на событиях, дат нет). `status`
   `shaped` → `active`. **Остальной текст карточки не изменён ни в одном знаке.**
2. Заведена `bet-g-5a7c-wave-2` (`_kind: bet`, `node: g-5a7c`, `opened: 2026-08-10`,
   `status: open`), 14 блоков: `owner_word`, `description`, `goal`, `appetite`, `approach`,
   `tracks`, **`evaluator`**, `kill_by`, `forecast`, `against`, `cut_list`, `lens_verdicts`,
   `riskiest_assumptions`, `evidence_disposition`.
3. Заведены две дорожки его словами: `cargo-hands` («Груз слушается рук») и `householder`
   («На что и как реагирует хозяин»), обе `for: g-5a7c`.
4. Заведены СЕМЬ задач: `t-situation-1` (общая, без дорожки — блокирует обе), `t-cargo-1`,
   `t-cargo-2`, `t-cargo-3` (`cargo-hands`), `t-react-1`, `t-react-2`, `t-react-3`
   (`householder`). Все `status: open`, `kind: executor`, с `order` 1–7.
5. `NOW`: `bet: g-5a7c`, `track_wip_limit: 2`.
6. Заведён `c-work-g-5a7c-situation-1-001` — локальный фронтир на первую задачу,
   `to: session`, `play: work`, `status: ready`. **Исполнительского наряда нет и по наряду быть
   не должно.**
7. `c-shape-g-5a7c-wave-2-001` закрыт.
8. **НЕ ТРОНУТО:** `CHARTER.md`, все прочие узлы, все карточки улик и идей (их тела эта нога не
   правит — см. `play_check`), `knowledge/`, продуктовый репозиторий, `os/**`.

## captures

- **ДЕФЕКТ OS, НАЙДЕННЫЙ ЭТОЙ НОГОЙ И НЕ ИСПРАВЛЕННЫЙ ЗДЕСЬ: `osctl` не умеет писать `_bet`.**
  `card new`, `card set` и `card unset` отказывают любому полю на `_` как «опоре карточки», а
  `card new` сам ставит только `_pos`. Значит задача, заведённая командой, НЕ связана со своей
  ставкой, и `osctl context` её не видит: `context --for g-5a7c` вернул бы ставку и дорожки, но
  ноль задач — каждая будущая нога открывалась бы на ставке, не видя её собственных задач. Та же
  дыра у `_parent` для дочернего узла. Семь `_bet: g-5a7c` дописаны в шапки прямо, потому что
  выразить их командой нечем; формат после этого перепроверен (`osctl check` — «механических
  проблем нет», `context` собирает все семь). **Это MAINTENANCE REQUEST отдельной сессией, а не
  работа этой ноги, и до неё каждый `shape` с задачами несёт ту же дыру.**
- `osctl check` называет `kind` и `order` незнакомыми полями шапки на всех семи новых задачах.
  Это не дефект ноги: те же два поля стоят на всех восьми задачах первой волны, и сам `osctl`
  объявляет это фактом, а не запретом. Новые задачи держат ту же форму, что и старые.
- Побочный эффект, названный, чтобы его потом не превратили в требование: провисающий и
  поворачивающийся груз — первое в этой игре, из чего выйдет читаемый геймплейный кадр
  (критерий 11 узла, `g-2b7f` критерий 2). Это НЕ строка `done_when` и НЕ задача этой волны.
- `idea-manual-cargo-control-001` поглощена дорожкой `cargo-hands` целиком;
  `idea-complex-noise-recognition-001` — ЧАСТИЧНО (слой ситуаций и комбинация), её «комплексная
  система распознавания шума» целиком в волну не входит и остаётся его же словом «Потом».
  Закрыть обе статусом `taken` может `review` этой ставки.

## decisions_needed

Нет новых. Отложенным его же решением остаётся `d-agents-skills-belong-in-repo-001`, названное
ему в начале ноги.

**Названо ему в начале ноги и НЕ является решением этой ставки** (уставная линза 1): его
собственный пересмотр октябрьского маршрута назначен на **2026-08-15**, регистрация на фестиваль
— **2026-08-31** (`g-2b7f.by`), и ни одна строка Steam-работы не начата. Эта ставка их не
двигает и по G2 не должна.

## play_check

- **1 Recite** — done: цель и двенадцать `done_when` узла пересказаны, цель родителя `g-0c26`
  прочитана. Цель узла — исход, а не деятельность; чинить нечего.
- **2 Appetite first** — done: аппетит зафиксирован ДО задач, три размера показаны рядом с
  честным размером КАЖДОГО, выбор его. Внешний взгляд — три собственные волны как сравнимые
  случаи; на конфликте доверено им, и правило «до восьми задач, один слой» соблюдено.
- **3 Approaches** — done: три структурно разных способа построить слой ситуаций (типизированный
  поток / домен-мосты / событийная шина), у каждого названо своё допущение; выбран первый с
  однострочным «почему», два отклонённых записаны в `approach`. Самая маленькая версия и список
  того, чего в ней НЕТ, записаны там же.
- **4 Scope hammer** — done: восемь вырезов, первый настоящий и болезненный (модификатор —
  вырезано его же слово), названный ему ДО подписи. Вырезы уехали в волну дома и в улики, не в
  ставку.
- **5 Lens sweep** — done: семь линз устава, ни одной молча. Задачи получили линзы 2 и 6; линза 1
  получила названный флаг с датами вместо тишины.
- **6 Riskiest assumption** — done: пять допущений ранжированы по силе убить, у каждого назван
  тест; №1 стоит ПЕРВЫМ в своей дорожке и стоит один коммит в обе стороны. Глубина теста
  откалибрована по осторожному режиму устава.
- **7 Tasks** — done: семь задач, каждая ≤ полудня, у каждой проверяемые `done_when`, `kind`
  проставлен. Порядок ставит тесты допущений рано. **`evaluator` и откаты названы ПЕРВЫМИ**, как
  требует шаг для исполнительских ставок. Две независимые линии работы → дорожки и их лимит
  вынесены владельцу и получены его словом (G7).
- **8 Kill criteria** — done: пять порогов, все на СОБЫТИЯХ (его запрет на даты 2026-08-02
  действует), `next_if_true`/`next_if_false` записаны, и дорожки умирают ПОРОЗНЬ.
- **9 Close** — done: экран показан целиком, ставка заведена, дорожки объявлены его дословными
  словами, фронтир зарегистрирован карточкой и стоит в `next`.
- **G1** — одна ставка на направление; `track_wip_limit: 2` — его число. Общая задача не
  принадлежит ни одной дорожке, поэтому пока она идёт, занято ноль из двух.
- **G3** — аппетит зафиксирован до задач; порог (4) прямо запрещает расширение под второй слой.
- **G4** — `done_when` и `kill_by` у ставки есть.
- **G6** — реальный список вырезов, вердикт по каждой из семи линз, задача на самое рискованное
  допущение.
- **G7** — ни одно игровое решение не принято ногой. Прочтение «поворот клавишей или только шов»
  названо ему ПРЯМО как прочтение ноги и разрешено РАЗМЕРОМ (в Б клавиша есть, в А её нет), а не
  догадкой; минимальная дистанция груза оставлена ЕМУ внутри `t-cargo-2`.
- **G9** — текст узла вне `appetite`/`kill_by`/`status` не редактировался; изменение проверено
  диффом.
- **Улики и идеи не редактировались.** `Writes:` плея `shape` покрывает `node`/`bet`/`task`/
  `track`/`call` и НЕ покрывает `issue`/`idea`. Их диспозиция записана в блоке
  `evidence_disposition` ставки и в `done_when` задач; закрывают их ноги их собственных
  маршрутов.
- **G5 не применялся и не должен:** `shape` ничего не объявляет `done`.

## log

вторая волна нарезана и подписана: аппетит Б его словом на три показанных размера, две дорожки его словами и одна общая задача-шов до их расхождения, семь задач и один новый слой; впервые за две ставки написан блок evaluator — его выбором фактический порядок стал законом, а к нему прибиты расписка исполнителя и запрет выдавать зелёные ворота за поведение игры; наклон груза куплен заново своей задачей и своим откатом, а модификатор вырезан его же словом

## next

`work` СВЕЖИМ ЧАТОМ по `c-work-g-5a7c-situation-1-001` — общая задача-шов `t-situation-1`.
Пока она идёт, обе дорожки ждут: это его правило, а не простой. После её закрытия открываются
ОБЕ сразу — `t-cargo-1` (рискованное допущение №1, свой откат) и `t-react-1`.

END_OF_FILE: live/indie-game-development/history/2026-08-10-s-shape-g-5a7c-wave-2-001.md
