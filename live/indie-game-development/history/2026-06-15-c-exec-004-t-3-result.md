# RESULT — c-exec-004 t-3 (sustained-load verdict + cross-layer proof + Wave-2 LOCK)

Closing report for leg **t-3** of bet **g-9c41** (direction `indie-game-development`, CALL `c-exec-004`),
the **2026-06-30 kill-gate**. Architecture frozen in `docs/adr/ADR-0004-sustained-load-verdict-lock.md`
(T1–T14), hardened by a 5-skeptic adversarial-verify pass + 3 Codex/GPT-5.5 validator rounds. ADR-0003 v2
**C1–C22 untouched**. Status: **GREEN / GREEN — kill-gate VERDICT = HOLD; stream LOCKED; DELIVERED.**
Axis A (dotnet) GREEN (103/103, `-Deliver` green); Axis B (FishNet PlayMode via MCP) GREEN. Verdict
owner-confirmed (s-decide-001); **merged `dev`→`main` (`6619299`) and pushed to origin.**

## outcome

t-3 pronounced the sustained-load verdict t-2 deferred, against its frozen `done_when`:

- **p1 — sustained-load consistency HOLDS (both clamps engaged).** Over the frozen **M=3 repeated
  pulse→settle wave** (900 ticks; breach@local-120, seal@local-260, source re-armed per cycle), host + 2
  clients: **lossless bit-exact EVERY tick** including every breach; **lossy `|client−host| ≤ Q` every
  tick**; **bit-exact at every converged settle** with **≥6 non-vacuous settles**; the three flow controls
  proven engageable (degenerate-budget negatives) with the real 150/275 budgets recorded-not-judged.
- **p2 — the steady-rate VERDICT = HOLD** (the bet-deciding judgement). A single binary machine-evaluable
  **recovery-mechanism predicate** over the committed series: in LOSSLESS mode under a **binding stress
  budget** (184/184 ⇒ admit 4 vs offered peak 8, so a backlog genuinely forms) **WITH the production fault
  model active** (unreliable-sequenced drop+delay+reorder, `faultSeedT3`) and the on-quiesce reliable
  resync keyframe, **every steady-window settle is bit-exact AND the deferred-chunk backlog drains within
  D=4 ticks** of the post-seal anchor ⇒ **HOLD**. The negative control (resync keyframe disabled) trips
  **BLOWN** (recovery is load-bearing), and an independent host-accepted-revision oracle confirms **no
  stale chunk is ever applied**. The chunked-delta gas stream **holds under sustained load**.
- **p3 — cross-layer interaction proven on the locked stream, under load.** A publish-only `ReactionLayer`
  drives the temperature layer (LayerKey 1) by a frozen `ReactionHeat=32` via one P2 `Reaction` event per
  cycle (P0→P1→P2 coupling), reconstructed host-vs-clients bit-exact at every settle. A **suppressed-event
  negative oracle** (differential at the firing tick == 32; absent when suppressed) proves the coupling is
  real, not vacuous. The real controlled breach runs UNDER the sustained load.
- **p4 — the Wave-2 LOCK** (wire format + revision barrier + channel semantics + cross-layer contract +
  cell-hash domain) is declared LOCKED in ADR-0004 §LOCK — gated behind p1 ∧ p2 (both passed) — and pinned
  by a drift-guard test.
- **Honesty boundary (p2/p5).** The toy scene (8 chunks) runs ~75× under the real 275 KB/s clamp, so the
  verdict proves the scene-size-independent **recovery mechanism**, not a literal 275-blow. The throughput
  half is the honest **projection**: at the measured steady rate, 275 KB/s supports ~**4.9k cells**
  (lossless) / ~**59k cells** (lossy) — the Wave-2 grid budget. Environment-independent facts (bytes/chunk,
  dirty-chunk count, backlog dynamics) are what t-3 stands behind; real-link latency/loss is not claimed.

**Kill-gate routing:** verdict HOLD ⇒ stream LOCKED ⇒ roll to Wave 2 (band sim). (A BLOWN would have routed
p1-fail→P6 model/vendor spike, p2-fail→Barotrauma-hybrid; neither tripped.) The owner confirms the verdict
(s-decide-001).

## evidence

- **Commits (`main` @ `6619299`, fast-forwarded from `dev`):** `36257b4` (freeze ADR-0004 + default-FAIL G0
  ledger, 5-skeptic hardened) → `19bb3ad` (B1 backlog+verdict, L6) → `101b910` (B2 sustained wave,
  L1–L5/L13) → `4eed54b`+`6cadf3d` (validator P0/P2 doc+anchor fixes) → `9ae470c` (B4 VERDICT=HOLD +
  validator P1, L7–L9) → `27a3422` (B3 cross-layer, L11–L12) → `0aaa8e6` (B5 telemetry+projection,
  L10/L15/L16) → `9cfacab` (B6 LOCK drift-guard + RESULT, L14/L17/L20) → `3abcf61` (B7 PlayMode + close,
  L18/L19) → `147f701`+`0115b35`+`8880c98`+`c142940` (Codex round-2/3: P1/P2/P3 fixes + regression guards +
  spec/artifact field alignment) → `6619299` (Wave-2 cleanup card). 15 commits, clean fast-forward to `main`.
- **Gate:** `& .\tools\check.ps1 -Deliver` → `OK: all gates green` — boundary build 0 warnings/0 errors,
  headless `dotnet test GasCoopGame.Core.slnx -c Release` **103/103**, hygiene OK (incl. a new
  Unity-`.meta`-tracking guard), RESULT.md field gate. (Env note: only Windows PowerShell 5.1 is present —
  no `pwsh` — so the gate runs as `& .\tools\check.ps1`; same script, green.)
- **G0 ledger (FINAL state, all 20 entries ☑ with opened evidence):**
  `openspec/changes/archive/c-exec-004-t3-sustained-verdict/tasks.md` — L1–L20 ☑ (Axis A dotnet + Axis B
  MCP PlayMode). The change is archived; the spec delta applied to `openspec/specs/sim-core/spec.md`.
- **Axis B (FishNet PlayMode, MCP, Unity 6.3.17):** the new
  `SustainedBreachWave_RepeatedCycles_BitExact_OverTugboatLoopback` GREEN + R3 suite green with the t-3
  core co-present (one reconnect-test loopback timing flake re-passed isolated — not a regression).
- **Telemetry (NEW, committed):** `docs/measurements/g9c41-t3-{lossless,lossy}.json` (schemaVersion 2,
  `clampVerdict:"HOLD"`, per-tick + per-settle series + steadyWindow + projection + environment) +
  `repro-constants-g9c41-t3.json`. The verdict + projection are independently re-derivable from the
  committed series (not the builder's flag). The t-2 artifacts remain `DEFERRED-T3` (untouched).
- **Constitution:** `git diff main -- docs/adr/ADR-0003-gas-field-state-stream.md docs/measurements/g9c41-t2-*`
  is EMPTY (C1–C22 + t-2 artifacts byte-untouched); core stays engine-free (boundary build);
  `BandwidthClamp.Verdict="DEFERRED-T3"` unedited (drift-guard). t-1 goldens (`9594…`/`10342…`) + t-2
  cell-hash goldens (`17841…`/`16144…`) stay green.
- **ADR:** `docs/adr/ADR-0004-sustained-load-verdict-lock.md` (Status/Context/Decision T1–T14/owner-echo
  R9–R13/alternatives/consequences/adversarial-provenance/§LOCK/§Verdict).
- **Validator (Codex/GPT-5.5):** independently reproduced the dotnet gate across 3 rounds; flagged P1
  (admitted-then-dropped chunk lost), P2 (drain-anchor off-by-one), a doc-staleness, a spec/artifact
  field mismatch, and round-3 latent/altitude/cleanup items — all fixed or owner-accepted-deferred (P1
  improved the design: the resync keyframe + a corrected keyframe-off negative control).

## assumptions

- **t-3 is a de-risking kill-gate leg on a toy scene, not shippable gameplay.** The verdict proves the
  recovery MECHANISM (scene-size-independent); the real-grid throughput is the projection (Wave-2 input).
- **Player-hosted, host-authoritative** authority model; networking edge-only (R12/R14); deterministic
  integer core (no float/wall-clock, seeded RNG; R13).
- **Measurement environment = loopback + software clamps** (the honest boundary above). The dotnet/in-memory
  sustained-load path is the primary autonomous evidence per the budget; the FishNet axis confirms
  consistency over real loopback (Axis B).
- **Frozen ADR-0004 design values taken as given** (T1–T14, owner-approved + adversarially hardened); the
  builder did not change them.

## cuts

Consciously deferred (recorded in ADR-0004 / the change "Out of scope"); raw material for Wave-2 shaping,
owned by the direction:

- **Band/sector solver + the T2 per-cell grid** — Wave 2 (the projection gives its 275 KB/s cell budget).
- **A bigger scene to make the real 275 clamp bind** — would be Wave-2-scale (~2.4k+ hot cells) and a
  toy-diffusion fake; the two-budget model + projection answer this honestly without it.
- **Real-uplink bandwidth** — loopback only (honesty boundary stated).
- **Mid-run late-join as a gate** — stays bet-2-deferred (the verdict's honesty did not require a
  join-baseline).
- **Multiple gas types / real reaction CONTENT** — the cross-layer rule is a single deterministic integer
  constant, NOT modelled heat.

**Wave-2 cleanup card (owner-accepted; from the Codex round-3 review — none ship a bug on a current path):**
the band-sim rewrite of this rig should fold in: (altitude) move the test-only cyclic `sourceCyclePeriod`
off the engine-free core; make `ReactionLayer`'s after-`BreachLayer` ordering an explicit contract (not a
magic key); lift command-conflict precedence to the input plane. (test-dup) extract one shared fault
channel (`WireFaultChannel` ↔ t-2 `FaultChannel`), one `TryAdmitInOrder` budget helper (`BacklogClamp` ↔
`BandwidthClamp`), one `FieldScalars`/kill-gate-rig helper (the ×9 `CellHashOf`/`GasScalars` + the cloned
`Run` bring-up). (latent, schedule-coupled) decouple the `SustainedVerdict` settle attribution from
source-rearm timing, and bound `WireFaultChannel` delays within the run. These are deferred because the
leg is delivered-green and Wave-2 reworks this code anyway — fixing now would churn 100+ green tests for
zero behaviour change.

## cost

Not instrumented end-to-end. Model: **Claude Opus 4.8**. Measured workflow portions: the 8-agent substrate
map ~692k sub-agent output tokens; the 5-skeptic adversarial-verify ~538k. The builder loop + the validator
(external Codex) were not separately metered. No external spend (local dotnet + the owner's Editor via MCP).

## manual-acceptance

Owner verify steps (the same checks the builder/validator ran):

1. `git -C C:\projects\Unity\GasCoopGame pull` (main) → `& .\tools\check.ps1` (or `pwsh tools/check.ps1`
   if installed) → expect `OK: all gates green` (dotnet 103/103, boundary 0/0, hygiene OK).
2. `& .\tools\check.ps1 -Deliver` → expect green (RESULT.md present + all required fields).
3. **Re-derive the verdict yourself** (no trust in the flag): read `docs/measurements/g9c41-t3-lossless.json`
   `series` + `settles`, confirm every steady-window (`[301,900)`) settle `bitExact:true` and `backlogDepth`
   returns to 0 within 4 ticks of each cycle's post-seal new-dirty-0 anchor ⇒ HOLD; check
   `projection.projectedCellsAt275KBs`.
4. **Axis B — PlayMode (MCP, Editor on `dev`):** run the FishNet PlayMode suite — R3
   (`FishNetConvergenceTests` + `FishNetFieldStreamTests` + `FishNetChunkStreamBreachTests`) green with the
   t-3 core co-present, plus the new `SustainedBreachWave_…` test — per-tick host-vs-follower cell-hash equal.
5. Confirm `git diff <t-2-baseline> -- docs/adr/ADR-0003-gas-field-state-stream.md docs/measurements/g9c41-t2-*`
   is EMPTY (C1–C22 untouched) and the t-2 artifacts still read `clampVerdict:"DEFERRED-T3"`.

## next

Route this RESULT **home to the direction `indie-game-development` (workflow-OS)**. The OS writer records
**t-3 = done** (GREEN/GREEN, VERDICT=HOLD owner-confirmed, merged to `main` `6619299` + pushed) and frames
the follow-on: with the verdict HOLD + the LOCK declared, the OS routes to **Wave-2 shaping** (band sim +
grid from DA + the temperature layer) building on the LOCKed contract — folding in the **Wave-2 cleanup
card** above — against the 2026-07-24 G3 wall. The builder does **not** author the next task CALL — handed
back to the direction.

END_OF_FILE: live/indie-game-development/history/2026-06-15-c-exec-004-t-3-result.md
