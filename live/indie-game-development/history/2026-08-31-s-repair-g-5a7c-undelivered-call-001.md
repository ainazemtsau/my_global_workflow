# RESULT — s-repair-g-5a7c-undelivered-call-001

direction: indie-game-development
play: repair
node/task: g-5a7c / t-bench-by-fact-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: repair — направление четырежды доложило владельцу, что нога идёт, при том что текст наряда ему не выдавался ни разу; слот всё это время стоял пустым

owner_approved: |
  Повод — его слова 2026-08-31: **«у меня в U1 ничего не запущено… я не запускал, я не видел,
  чтобы ты мне что-то давал»**. Проверено и подтверждено полностью.

  Ни одна строка `done_when` не изменена.

outcome: |
  **НАПРЯД БЫЛ НАПИСАН, КАРТОЧКА ЗАВЕДЕНА, СЛОТ НАЗНАЧЕН — И ТЕКСТ ВЛАДЕЛЬЦУ НЕ ВЫДАН НИ РАЗУ.**
  Направление в этом направлении не отправляет ничего само: оно даёт владельцу текст, владелец
  пересылает. Текста не было, значит **наряда не существовало.**

  **И ЧЕТЫРЕЖДЫ БЫЛО ДОЛОЖЕНО ОБРАТНОЕ.** В четырёх сообщениях подряд стояло «подготовка полигона
  идёт в WIN-U1», «идёт параллельно и уже пишет карту». Проверено сейчас: **`WIN-U1` —
  `AVAILABLE`, `lease: none`**, и был им всё это время. Владелец строил на этом свои решения о
  том, что запускать дальше.

  **ЭТО ТОЧНО ТОТ КЛАСС, КОТОРЫЙ В ЭТОМ ЖЕ НАПРАВЛЕНИИ УЖЕ ЗАПИСАН.**
  `i-call-text-never-reached-the-executor-001` описывает его дословно: «наряд был написан полностью
  … и ФИЗИЧЕСКИ НЕ ДОШЁЛ до исполнителя… У OS есть обязательные сверки слота, базы, дерева и
  версии контракта — и **ноль проверки, что сам текст приказа доехал**». Тогда цена была
  опубликованный кандидат-пустышка. Сегодня — четыре ложных доклада и потерянные полдня слота.

  **ЧЕГО ОН НЕ ПУТАЛ.** Он помнит, что ногу в U1 сегодня утром закрывал сам, и он прав: это была
  **починка зрения** `t-sight-thing-1` — выдана, отработана, опубликована, слот отпущен. Другая
  задача. Подготовка полигона появилась позже и до него не дошла.

  **И НАРЯД ТЕПЕРЬ ВДОБАВОК СМОТРИТ НЕ В ТО ДЕРЕВО.** Он написан на `main` = `064ab568`, а сценарий,
  который владелец собирается смотреть, строится на ветке `slot/win-u4`. Карта по `main` описала бы
  хозяина, который стоит, — то есть не то состояние, куда он придёт. **Отозван в черновик до
  возврата ноги сценария**, после чего будет перенацелен на ту ветку, где сценарий реально идёт.

evidence: |
  Прочитано направлением напрямую:

  - `gascoop-slot-state.v1.json` — `WIN-U1: AVAILABLE, lease none`; `WIN-U2: AVAILABLE`;
    `WIN-U3: AVAILABLE`; **единственный занятый — `WIN-U4`** под `t-morning-content-1:BUILD`,
    то есть ровно та нога, текст которой владелец переслал;
  - карточка `c-exec-g-5a7c-bench-by-fact-1-001` стояла `ready` со слотом `WIN-U1` с момента
    заведения; документ `work/2026-08-31-call-bench-by-fact-1.md` существует и полон;
  - в четырёх последних сообщениях владельцу утверждалось, что нога идёт.

state_changes: |
  ОТОЗВАНО (1 наряд):
  - `c-exec-g-5a7c-bench-by-fact-1-001` → `draft`. Будет перенацелен на ветку со сценарием после
    возврата ноги утра.

  ВОЗВРАЩЕНО (1 задача):
  - `t-bench-by-fact-1` → `open`.

captures: |
  - **У НАПРАВЛЕНИЯ НЕТ НИ ОДНОЙ ПРОВЕРКИ, ЧТО ТЕКСТ ДОЕХАЛ, И ЭТО ЗАПИСАНО УЛИКОЙ ДАВНО.** Заведение
    карточки со статусом `ready` выглядит как отправка и ею не является. **Правило-кандидат:
    состояние `ready` у наряда означает «готов к выдаче», а не «выдан»; докладывать «идёт» можно
    только прочитав ЗАНЯТОСТЬ СЛОТА, а не статус карточки.** Проверка стоит одну команду и сегодня
    сразу дала ответ.
  - **ЛОЖНЫЙ ДОКЛАД ХУЖЕ ПРОПУЩЕННОЙ РАБОТЫ.** Слот простоял бы и так, но владелец на основании
    «идёт в U1» решал, чем занимать остальные, и дважды спрашивал про свободные слоты, имея на один
    больше, чем думал.

decisions_needed: |
  Ни одного.

play_check: |
  1. Назвать противоречие — его слова против четырёх докладов направления.
  2. Восстановить новейшим-первым — реестр слотов прочитан напрямую, он подтверждает его.
  3. Предложить исправленное состояние — наряд в черновик, задача в `open`, перенацеливание после
     возврата сценария.
  4. Подтверждение (owner) — не требуется: исправление собственной ошибки.
  5. Дыра в OS → класс уже описан уликой `i-call-text-never-reached-the-executor-001`; отдельной
    заявки не открывается, кандидат в правило записан выше.

log: |
  наряд на подготовку полигона был написан и заведён, но текст владельцу не выдавался ни разу —
  а направление четырежды доложило, что нога идёт; реестр слотов подтверждает его: U1 всё это время
  AVAILABLE; тот же класс уже описан уликой про недоехавший наряд

next: |
  Идёт **одна** нога: сценарий в WIN-U4. Свободны U1, U2, U3.

  Подготовка полигона перенацелится на ветку со сценарием, когда та вернётся.
