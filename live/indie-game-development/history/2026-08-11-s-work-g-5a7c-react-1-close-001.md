# RESULT — s-work-g-5a7c-react-1-close-001

play: work · close: **light — because** все три строки `done_when` наряда и все пять строк карточки
`t-react-1` эта нога перемерила ПЕРВОЙ РУКОЙ по закоммиченным байтам: набор тестов прогнан здесь на
выгруженном дереве опубликованной головы, диффы всех пяти путей и обоих коммитов кандидата прочитаны
командами, прогнанными здесь, числа пятой реакции и время жизни `8` прочитаны на HEAD, а откат —
которого в возврате НЕТ ВОВСЕ — выведен и проверен деревом здесь. Ни одной строки, которую судит его
глаз, его ощущение или прогон, которого нога не делала, в этой задаче нет: наряд прямо сказал
«владелец эту задачу руками НЕ судит и не должен», его вердикт по дорожке назначен на `t-react-3`
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-1 ·
call: c-exec-g-5a7c-react-1-001
date: 2026-08-11

base_read: OS — `wt/indie-game-development`, рабочее дерево чисто, голова `c04129fe`. Продукт
перемерен `git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` =
`2f7f2e87b3d3fa1e3b40e50f5063a87530fc5187`. Слот WIN-U2 по `gascoop-slot-state.v1.json`:
`lifecycle: AVAILABLE`, `lease: none` — прочитано файлом, а не со слов возврата. Возврат исполнителя
владелец словами не приносил; он живёт в байтах продукта как `docs/results/c-exec-g-5a7c-react-1-001.md`
на `2f7f2e87` и прочитан отсюда.

## outcome

`t-react-1` закрыта. Реакция хозяина стала ОДНОЙ названной вещью, и шестая приезжает одними данными.

**Главный гейт задачи взят, и взят сильнее, чем требовалось.** Требовалось: шестая реакция
добавляется данными без правки закона, доказано тестом. Факт: коммит теста `c94c1e7c` трогает
РОВНО ОДИН файл — новый тест. Ни перечислений, ни решателя, ни веток он не касается физически, а не
по обещанию. Шестая реакция въезжает приведёнными значениями `(HouseholderEventName)5`,
`(HouseholderNoticedFactName)6`, `(HouseholderPriority)6`, `(HouseholderOccupation)6`,
`(HouseholderCauseName)6` и одной строкой каталога через `.With(...)`.

**Три ветки `switch` не «сократились» — их не осталось ни одной.** На базе `4cf2b5dc` в
`Householder.cs` было три `switch (` и двенадцать `case Householder…`; на опубликованной голове —
**ноль и ноль**. Решатель, таблица событие→факт и таблица причина→факт исчезли целиком; на их месте
поиск строки в каталоге.

**Поведение доказано не «зелёными воротами», а тем, что доказывать было почти нечего:** react-кандидат
не тронул НИ ОДНОГО существующего тестового файла. Механической подстановки переименованных символов,
которую наряд разрешал, тоже не понадобилось — правок в `tests/` нет вообще, кроме одного НОВОГО файла.

## evidence — всё перемерено и прогнано первой рукой этой ногой

### Что именно опубликовано и как дорожки встретились

- Кандидат: продуктовый `b4d7d585` (родитель — ровно базис `4cf2b5dc`) и тестовый `c94c1e7c`.
- Слияние с дорожкой `cargo-hands`: `0ccc0fd7`, родители `6aa478bb` (голова cargo) и `c94c1e7c`.
- Сверху три RESULT-only коммита; опубликованная голова `2f7f2e87` = `origin/dev` = `origin/main`.

**Клауза 5 `evaluator` этой волны исполнена и проверена здесь:** дорожка публиковала в `main` не своё
одинокое состояние, а собранное вместе с текущей головой другой дорожки. `git diff --name-status
6aa478bb..0ccc0fd7` вернул **ровно пять react-путей и ничего больше**;
`git diff --stat c94c1e7c..0ccc0fd7` по этим пяти путям — **пусто**, то есть кандидат прошёл слияние
байт в байт; `git diff --name-only 6aa478bb..0ccc0fd7 -- Core/Cargo/** Network/** Scenes/**
ProjectSettings/** StreamingAssets/**` — **пусто**.

### `done_when` 1 — одна названная вещь, все пять выражены ею, дом данных назван

`Assets/TunnelCrew/Core/Householder/HouseholderReaction.cs` прочитан целиком на `2f7f2e87`.
Названный тип — `readonly struct HouseholderReaction`; его хранилище — `HouseholderReactionCatalog`.
Словарь владельца лёг на поля так:

| Слово владельца | Поле |
|---|---|
| повод — какая ситуация | `EventName` + `NoticedFactName` + `NoticedFactLifetimeSeconds` |
| приоритет | `Priority` (плюс `Occupation` — во что превращается намерение) |
| физическое действие И ЕГО ЦЕЛЬ | `PhysicalActionName` + `ActionTargetName` — цель отдельным полем, не опущена |
| возвращение к жизни | `ReturnToLifeMode` (`ResumePreviousIntention`) + `RequiresSafeInterruption` |

**Плохо не легло ни одно слово.** Единственное, что стоит назвать: `Occupation` — пятое поле,
которого в словаре его словами нет; это сегодняшнее «чем он занят», а не изобретение ноги.

`CreateCurrent(...)` содержит **ровно пять строк**, прочитанных здесь поимённо: `daily-route`,
`suspicious-noise`, `quiet-rustle`, `repeated-heavy-noise`, `landing`. Пятёрка карточки сходится
знак в знак: обычный распорядок — первая строка, «расследовать приземление» — пятая.

**Дом данных: код** — `HouseholderReactionCatalog.CreateCurrent` в
`Core/Householder/HouseholderReaction.cs`, времена жизни втекают параметрами из
`HouseholderWalkTuning`, то есть из уже существующего `Settings/HouseholderWalkSettings.asset`.
Ни `NetworkPlaySettings.asset` (перемерено: react-диффом не тронут), ни файл профиля
(перемерено: не тронут) — обе подписанные границы целы. Причина выбора в возврате прямо не написана;
она выводится из границ наряда, и это названо здесь как вывод, а не как его слова.

### `done_when` 2 — шестая реакция данными, доказано тестом

`git diff --name-status b4d7d585..c94c1e7c` → **одна строка: `A tests/…/HouseholderReactionDataTests.cs`.**
Сильнее этого доказательства «закон не тронут» не бывает: коммит теста физически не содержит других
файлов.

Тест прочитан. Он проводит шестую строку по всей цепочке: событие принято
(`PendingEventConsumed`), замеченный факт — `TestFact`, приоритет и занятие — `TestPriority` /
`TestOccupation`, правдоподобная смена приоритета несёт `TestCause` и id того же появления факта,
физическое действие — `WalkToward` в `SituationPoint` с координатами точки ситуации, активный ответ
помнит строку по имени `test-only-sixth`, а `ReturnToLife` возвращает прерванное занятие и тот же
шаг маршрута. Затем факт становится `IsHandled`, активный ответ гаснет, и хозяин возвращается к
`DailyRoute`. Третий тест доказывает, что уже ПЕРВЫЙ авторитетный тик читает каталог конкретного
мира, а не статическую таблицу.

**В игре реакций осталось пять** — тест сам это утверждает
(`productionReactions.TryGetByEvent(TestEvent)` = `false`, `reactions.Count` = `5`), и то же самое
прочитано в байтах `CreateCurrent`.

### `done_when` 3 — поведение, границы, откат

**Набор тестов прогнан ЭТОЙ ногой** на дереве опубликованной головы, выгруженном `git archive
2f7f2e87` в отдельную папку (слот не трогался, он `AVAILABLE`):

```text
dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release
Пройден!   : не пройдено 0, пройдено 158, пропущено 0, всего 158
```

Число `158`, а не `151` из наряда, разложено: база `4cf2b5dc` = 151, дорожка `cargo-hands` добавила
4 (`CargoThreeDimensionalGripTests`), эта задача — 3. Свои 3 я прочитал поимённо в новом файле.

**Ни одно ожидание не переписано.** `git diff --name-status 4cf2b5dc..c94c1e7c` = ровно пять путей:
`AuthoritativeHouseholder.cs` (M), `Householder.cs` (M), `HouseholderReaction.cs` (A), его `.meta` (A),
`HouseholderReactionDataTests.cs` (A). **Существующих тестовых файлов среди них нет ни одного.**

**Пятая реакция цела по числам — прочитано на HEAD:** `HouseholderOccupation.InvestigateLanding = 5`,
`HouseholderPriority.Landing = 5`, `HouseholderCauseName.Landing = 5`,
`HouseholderNoticedFactName.Landing = 5`, `HouseholderEventName.Landing = 4`,
`Settings/HouseholderWalkSettings.asset:21` `landingLifetimeSeconds: 8`. Тест дополнительно
прибивает эти же пять чисел утверждениями.

**Границы целы, и это следует из тех же пяти путей:** сцена `IntegratedHouse.unity`, все файлы
дорожки `cargo-hands`, `Core/Multiplayer/CarryWorld.cs` (значит и публичная сигнатура `Advance`),
`Network/CargoBodyContact.cs`, `NetworkPlaySettings.asset` с порогами `60 / 150 / 400`,
`StreamingAssets/…/default.householder.json`, `ProjectSettings/**` — не тронуты ни одним байтом.
Правило «одна самая громкая ситуация за тик» живёт в `Core/Situations/WorldSituationNoise.cs`,
который в диффе отсутствует.

**ОТКАТ — в возврате его НЕТ ВОВСЕ. Выведен и проверен ЭТОЙ ногой.** Одна команда:
`git revert -m 1 0ccc0fd7`. Проверка сделана здесь без рабочего дерева, временным индексом: снять
три добавленных пути и вернуть два изменённых к их виду на `6aa478bb` даёт дерево
`3e53fbee20af906d83979bf3bdbb39082c160953` — знак в знак дерево головы `6aa478bb`, на которой
дорожка стояла перед слиянием. Ничего вне репозитория для отката не нужно.
Точка `4cf2b5dc` / дерево `58105a58…` (прочитано здесь, совпадает с нарядом) как точка отката этой
дорожки больше не годится: с тех пор в `main` опубликована дорожка `cargo-hands`, и откат к базе
снёс бы ЕЁ. Честная точка возврата этой работы — `6aa478bb`.

### Unity: компиляции не было, и вот чем это закрыто, а чем — нет

Возврат объявил Unity/MCP ненужным. Наряд просил настоящую import/compile evidence, а
`knowledge/green-gates-do-not-compile-the-game.md` прямо запрещает считать зелёные ворота
компиляцией. Поэтому перемерено здесь:

- `core/TunnelCrew.Core.csproj` компилирует `..\Assets\TunnelCrew\Core\**\*.cs` — ТОТ ЖЕ набор
  файлов, что и Unity-сборка `Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef`, — и с той же мишенью:
  `netstandard2.1`, `LangVersion 9.0`. Мой Release-прогон эти файлы собрал.
- Что мой прогон НЕ покрывает — потребителей вне `Core`. Их три:
  `Network/NetworkHouseholder.cs`, `Network/NetworkWalkerCourier.cs`,
  `World/HouseholderRouteController.cs`. Первый и третий **не изменены с базы вовсе**; второй изменён
  дорожкой `cargo-hands`, не этой. Все символы, которые они зовут, прочитаны на HEAD и на месте:
  `HouseholderEventName.*`, `HouseholderPoint`, `HouseholderProfileLoader`, `HouseholderSurroundings`
  и все три публичных конструктора `HouseholderWalkTuning` (сравнены построчно с базой — идентичны).
  Публичная поверхность `AuthoritativeHouseholder` сравнена с базой целиком — **идентична**.

**Итог честно:** редактор Unity этот код не собирал, и я этого не утверждаю. Поверхность риска для
ИМЕННО этого изменения закрыта построением, а не прогоном, и названа так.

### Чего в возврате не оказалось — перечислено, а не сглажено

Наряд просил девять пунктов в `return`. Четырёх нет: **проверенного отката** (сделан здесь),
**readback чисел пятой реакции и `8`** (сделан здесь), **списка мест на день комбинации** (собран
здесь, ниже), **разбора «какое слово словаря легло плохо»**. Ещё два — раскладка словаря по полям и
причина выбора дома данных — есть частично. Расписки исполнителя (клауза 3 `evaluator`) в байтах нет:
первая строка возврата — `Status: DELIVERED on dev`, а не число полученных строк `done_when`.
Что наряд ДОЕХАЛ, видно косвенно и сильно: работа исполнена приёмом, названным в наряде поимённо
(`CargoClassSeamTests`-техника), с соблюдением поимённого списка чужих файлов и с явным исполнением
клаузы 5 про голову другой дорожки. Прямой расписки у меня нет — она у владельца или её нет.

### Что придётся тронуть в день комбинации — собрано этой ногой, потому что возврат этого не дал

Готовит `t-react-2`, но ею не является. Прочитано на HEAD:

1. `Core/Situations/WorldSituationNoise.Loudest` — сводит список ситуаций тика в ОДИН
   `HouseholderStimulus`; у стимула одно поле `Heard`.
2. `AuthoritativeHouseholder.Observe` (`:176-186`) — новый стимул ОТБРАСЫВАЕТСЯ, если уже есть
   `_pendingEvent` или `_offeredStimulus`. Один слот на тик.
3. `AuthoritativeHouseholder` (`:335`) — `if (heard == None || _pendingEvent.IsPresent) return;`
   второе событие в тике не принимается.
4. `HouseholderReactionCatalog.TryGetByEvent` / `TryGetByNoticedFact` — первая совпавшая строка;
   на комбинацию нужен другой отбор, и это отбор, а не новая ветка.
5. `HouseholderLocalMemory.ActiveResponse` — ОДИН активный ответ; политика прерывания сегодня
   выражена одним булевым полем строки `RequiresSafeInterruption`.

## state_changes

1. `t-react-1`: закрыть, терминальный статус `done`. Закрыта СВЕТЛЫМ путём — названо вслух, как
   требует `evaluator` клауза 2.
2. `c-exec-g-5a7c-react-1-001`: закрыть — наряд исполнен, слит с головой другой дорожки и опубликован
   в `2f7f2e87`; слот WIN-U2 возвращён `AVAILABLE / lease none`.
3. Завести улику `i-executor-return-omits-named-return-items-001` (`level: execution`,
   `route: review`): возврат исполнителя опускает поимённо заказанные пункты `return`, и дважды подряд
   среди них ОТКАТ — тот самый, который `evaluator` этой волны требует доказывать настоящим revert.
   На `t-situation-1` откат и расписка были «взяты с возврата, а не своей рукой»; здесь отката нет
   вовсе, и его пришлось выводить закрывающей ноге. Карточку закрывает `review` этой ставки.
4. `t-react-2`: строка в журнал — список мест, которые тронет приём больше одной ситуации за тик,
   собран в этой ноге и лежит в её RESULT; наряду `t-react-2` его не надо собирать заново.
5. `NOW.md`, `g-5a7c`, текст ставки, `t-cargo-1` и `c-exec-g-5a7c-cargo-1-001` не трогать: дорожка
   `cargo-hands` закрывается своей ногой и своим путём (её строку 5 судит его глаз).
6. Одним `osctl leg close` сохранить этот RESULT в
   `history/2026-08-11-s-work-g-5a7c-react-1-close-001.md` и добавить log ровно один раз в журналы
   `g-5a7c`, `t-react-1`, `c-exec-g-5a7c-react-1-001`, `t-react-2` и новой улики.

## captures

- **Перечисления остались, и это предел доказанного.** Веток `switch` ноль, но пространство значений
  по-прежнему живёт в пяти перечислениях `0…5`. Шестая реакция В ИГРЕ (не в тесте) потребует
  дописать по одному члену в пять перечислений — правка механическая и только добавляющая, ветки и
  решатель при этом не трогаются. Тест это обходит приведением чисел, что наряд прямо и заказывал.
  Это надо знать `t-react-2` и особенно `t-react-3`, где источник подставляют.
- **`origin/slot/win-u2` остался на `e4df0459`**, локальная `slot/win-u2` догнана до `2f7f2e87`.
  Ровно то же дорожка отметила про WIN-U1 в ноге выдачи. Селектор читает локальное, выдача не ломается.
- **WIN-U1 по файлу состояния до сих пор `CLAIMED`, аренда `c-exec-g-5a7c-cargo-1-001:BUILD`**, хотя
  cargo-возврат в байтах называет себя доставленным на `dev`. Дорожку `cargo-hands` закрывает своя
  нога — там это и проверять.
- **Порог смерти (5) считает ноги:** потрачено 4 из двенадцати (общая задача, выдача двух нарядов,
  закрытие `t-situation-1`, закрытие `t-react-1`). Дорожка `cargo-hands` свою закрывающую ногу ещё не
  потратила.

## decisions_needed

Нет. Наряд прямо снял владельца с этой задачи: «Владелец эту задачу руками НЕ судит и не должен: его
вердикт по этой дорожке назначен на `t-react-3`».

## play_check

- 1 recite: done — пять строк карточки `t-react-1` и три строки `done_when` наряда сведены и
  разложены поимённо
- 2 owner inputs (owner): done — новых фактов от него не требуется; наряд снял его с этой задачи
  своим текстом, два ждущих его вопроса вынесены ему отдельно и остаются ждать
- 3 do the work: done — возврат прочитан первой рукой в байтах продукта; все строки перемерены
  здесь, откат выведен и проверен здесь, пропуски возврата названы
- 4 self-check: done — каждая строка `done_when` закрыта своим измерением; то, чего нога не делала
  (Unity-компиляция, расписка), названо как не сделанное
- 5 close: done — светлый путь, назван вслух; продолжение дорожки — `t-react-2`

## Гейты

- **G1.** Дорожка `householder` освободилась: её единственный корень закрыт. `cargo-hands` держит
  свой; лимит `2` не превышен.
- **G2.** Задача служит активной ставке; ничего вне неё не втянуто.
- **G3.** Аппетит не тронут: закрыты ровно подписанные строки, ни одна не расширена.
- **G5.** Светлый путь по букве плея и по букве `evaluator` клауза 2: каждая строка перемерена первой
  рукой по закоммиченным байтам. Ни одной строки, требующей его глаза, его ощущения или чужого
  прогона, в задаче нет. Отдельный свежий чат не открывался, и это названо, а не умолчано.
- **G7.** Ничего продуктового или геймплейного этой ногой не решено.
- **G9.** Карточки задачи и ставки не редактировались, только закрыты и снабжены журналом.
- **evaluator, клауза 5.** Проверена командами здесь: дорожка публиковала состояние, собранное с
  текущей головой `cargo-hands`, и не тронула ни байта чужой полосы.

## log

`t-react-1` закрыта светлым путём на опубликованной голове `2f7f2e87`: реакция стала одной названной вещью `HouseholderReaction` с каталогом на пять строк, четыре слова его словаря легли на поля без потерь (цель действия — отдельным полем), а трёх веток `switch` не осталось ни одной — на базе было три `switch` и двенадцать `case`, на голове ноль и ноль; главный гейт взят сильнее требуемого: коммит теста шестой реакции `c94c1e7c` трогает РОВНО ОДИН файл, шестая въезжает приведёнными числами и одной строкой каталога и проходит цепочку до возвращения к прежнему занятию, а в игре реакций осталось пять; react-кандидат не тронул ни одного существующего тестового файла, поэтому «ожидания не переписаны» доказано отсутствием правок, а не их разбором; 158/158 прогнаны этой ногой на выгруженном дереве публикации, числа пятой реакции и время жизни 8 прочитаны на HEAD, слияние с головой другой дорожки проверено пустыми диффами в обе стороны; отката в возврате не было вовсе — выведен и проверен здесь временным индексом до дерева 3e53fbee головы cargo, а не до базы, которую cargo уже перекрыла; Unity-редактор код не собирал, и это названо: закрыто тем, что зеркальный csproj собирает тот же набор файлов с той же мишенью, а три потребителя вне Core не изменены и все их символы на месте

## next

1. **Дорожка `householder` свободна — следующий корень `t-react-2`** (приём больше одной ситуации за
   тик и реакция на СОЧЕТАНИЕ, со своим откатом «вернуть одну самую громкую»). Список мест, которые
   она тронет, уже собран в этой ноге — новому наряду его собирать не надо.
2. **Дорожка `cargo-hands` ждёт своей закрывающей ноги**, и она НЕ светлая: строку 5 `t-cargo-1`
   судит его глаз. До его слов задача не закрывается, сколько бы зелёного ни было в возврате.
3. Улика про пропуски в возвратах исполнителя ждёт `review` этой ставки.
4. Порог смерти (5): потрачено 4 ноги из двенадцати.
