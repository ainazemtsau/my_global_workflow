# RESULT — s-work-g-5a7c-rooms-1-checkpoint-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-rooms-1
track: house-and-things
date: 2026-08-12
outcome_kind: checkpoint — кандидат принят владельцем, но НЕ опубликован; задача НЕ закрыта

owner_approved: |
  «завершилась c-exec-g-5a7c-places-1-001» — 2026-08-12.

  **Идентификатор назван неверно:** `c-exec-g-5a7c-places-1-001` — это шов, закрытый несколькими
  часами ранее. Перемеренная реальность: завершился **дом**, `c-exec-g-5a7c-rooms-1-001`. Нога
  не гадала: проверены удалённые ссылки, головы всех четырёх слотов и содержимое обоих занятых.

  **Его приёмка дома при этом РЕАЛЬНА и зафиксирована в байтах:** коммит
  `5dc30367 docs(result): record owner-eye acceptance` в WIN-U2.

outcome: |
  **`t-rooms-1` НЕ ЗАКРЫТА. Дом построен, принят его глазом, перемерен Направлением — и не может
  быть опубликован как есть, потому что стёр бы две принятые задачи.**

  **БАЗА УСТАРЕЛА ДВАЖДЫ.** Родитель рабочего коммита `e592d7c6` — `a7d37b3e`. С тех пор
  опубликовались «место как факт» (`c1d8d384`) и тела (`8c3bb8af`).

  Дифф `origin/main..HEAD` содержит **23 УДАЛЕНИЯ ЧУЖОЙ РАБОТЫ**, среди них весь арт тел: `.fbx` на
  1.6 МБ, `License.txt`, `SOURCE.md`, `AUTHORING.md`, контроллер локомоции, обе заготовки — и работа
  «места как факта». Публикация как есть уничтожила бы обе задачи, каждую из которых владелец принял
  сегодня.

  **ЭТО ТРЕТИЙ СЛУЧАЙ ЗА ДЕНЬ И ОН ХУЖЕ ПРЕДЫДУЩИХ:** у тела база отставала на одну публикацию, у
  дома — на две. Механизм тот же: голова сверяется на старте, работа идёт часы, соседняя дорожка
  публикуется, ветка начинает удалять чужое. При трёх-четырёх параллельных фронтах это не
  случайность, а свойство формы.

  **ПЕРЕБАЗИРОВАНИЕ ЧИСТОЕ.** Перемерено `comm`: собственный дифф дома — 12 содержательных файлов
  (семь материалов обстановки, `World/ThreeRoomHouse.prefab`, `Scenes/IntegratedHouse.unity`, новые
  EditMode-тесты с asmdef, отчёт) — **не пересекается с опубликованным ни одним файлом**.

  **ПОВТОРНАЯ ПРИЁМКА НЕ ТРЕБУЕТСЯ.** Перебазирование не меняет ни байта дома: оно только перестаёт
  удалять чужое. Его вердикт по дому остаётся в силе, и новый гейт нога не выдумывает.

  Выпущено механическое продолжение `c-exec-g-5a7c-rooms-1-002`: перебазировать, переисполнить
  откат на новой базе, опубликовать. Главным гейтом поставлено **«ноль удалений чужой работы»**,
  проверяемое выводом `git diff --name-status | grep '^D'`.

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ.**

  - `git ls-remote origin` → `main` = `dev` = `8c3bb8af`; сверх неё **ноль** коммитов.
  - Слоты: WIN-U1 `8c3bb8af` / AVAILABLE; **WIN-U2 `5dc30367` / CLAIMED / lease
    `c-exec-g-5a7c-rooms-1-001:BUILD`**; **WIN-U3 `889c83af` / CLAIMED / lease
    `c-exec-g-5a7c-occupations-1-001:BUILD`**; WIN-U4 `f3c40c40` / AVAILABLE.
  - WIN-U2: пять коммитов сверх `origin/main`, дерево чистое, верхний —
    `5dc30367 docs(result): record owner-eye acceptance`.
  - `git rev-parse e592d7c6^` → `a7d37b3e`.
  - `git diff --name-status origin/main HEAD | grep -c '^D'` → **23**.
  - `comm -12` по спискам путей собственного диффа дома и диффа `a7d37b3e..origin/main` → **пусто**.
  - WIN-U3: сверх `origin/main` **ноль** коммитов — занятия ещё не дали кандидата.

state_changes: |
  1. `t-rooms-1`: `open → active`, журнальная строка.
  2. `c-exec-g-5a7c-rooms-1-001`: остаётся открытым до публикации; журнальная строка.
  3. Создан `work/c-exec-g-5a7c-rooms-1-002-call.md` и карточка `c-exec-g-5a7c-rooms-1-002`
     (`ready`, `slot: WIN-U2`, `basis: 8c3bb8af`) — механическое продолжение с гейтом «ноль
     удалений чужой работы».
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-rooms-1-checkpoint-001.md`.

captures:
  - |
    **ТРЕТИЙ ПРОТУХШИЙ БАЗИС ЗА ДЕНЬ — ПОРОГ ПЕРЕЙДЕН, ЭТО MAINTENANCE.** Я записывала после второго
    случая: «если случится третий, это чиниться должно формой наряда, а не ногой». Случился. Форма
    лечения названа тогда же и остаётся верной: **сверка `origin/main` перед выдачей результата
    обязана быть строкой `done_when`, а не пожеланием в разделе `return`** — пожелание я добавляла
    дважды, и оба раза его не заполнили. Это дефект НАШЕЙ формы наряда при параллельных дорожках, и
    он принадлежит `os/**`, а не этой ноге.
  - |
    Цена ошибки растёт с числом фронтов: тело отставало на одну публикацию, дом на две. При четырёх
    дорожках следующая будет отставать на три. Порог смерти 3 ставки считает ноги разговора — а эта
    поломка добавляет по одной механической ноге на каждый случай, и они в счёт тоже идут.

decisions_needed: []

play_check:
  - 1 recite — done. Семь строк карточки пересказаны.
  - 2 owner inputs — исполнены: его глаз по дому дан и лежит в байтах; повторно не запрашивается, потому что перебазирование не меняет дом.
  - 3 do the work — приёмка возврата; выпущено механическое продолжение вместо закрытия.
  - 4 self-check — done. Неверно названный идентификатор проверен, а не принят: измерены удалённые ссылки, все четыре слота и содержимое обоих занятых.
  - 5 close — checkpoint. Основание: не опубликовано, и публикация как есть уничтожила бы две принятые задачи.
  - G5 — не применяется; закрытия нет.
  - G7 — done. Ни одного игрового решения не принято; повторный гейт на его глаз не выдуман.
  - G10 — done. RESULT финальный.

log: t-rooms-1 НЕ закрыта — дом построен и ПРИНЯТ ЕГО ГЛАЗОМ (коммит record owner-eye acceptance лежит в байтах), но не опубликован и собран на базе, устаревшей ДВАЖДЫ: с тех пор вышли «место как факт» и тела, поэтому дифф содержит 23 удаления чужой работы, включая весь арт тел с FBX на 1.6 МБ, лицензией и контроллером, и работу «места как факта»; перебазирование чистое — пересечение путей пусто, — и выпущено механическое продолжение с главным гейтом «ноль удалений чужой работы»; повторная приёмка не запрашивается, потому что перебазирование не меняет ни байта дома; отдельно: он назвал идентификатор неверно (places-1-001 закрыт часами ранее), и нога это проверила измерением, а не догадкой; и отдельно — это ТРЕТИЙ протухший базис за день, порог перейден, лечение принадлежит форме наряда и уходит в MAINTENANCE

next: |
  `c-exec-g-5a7c-rooms-1-002` — перебазировать, переисполнить откат, опубликовать.

  **Запускать сейчас нечего, и это честный ответ, а не отговорка.** Занятия идут в WIN-U3, дом ждёт
  механической ноги в WIN-U2. Две задачи, готовые к запуску, — `t-loot-1` (вместе с починкой
  сквозного прохода по его решению) и `t-house-asset-1` — обе требуют ОПУБЛИКОВАННОГО дома: первая
  ставит лут в комнаты и правит ту же сцену, второй нечего переносить. `t-sight-1` ждёт, пока
  занятия отпустят `Householder.cs`. `t-handlers-1` и `t-lab-2` ждут своих предшественников.

  То есть публикация дома — единственное, что сейчас что-либо разблокирует, и она в одной короткой
  ноге отсюда.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-rooms-1-checkpoint-001.md
