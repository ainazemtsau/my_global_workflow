# RESULT — s-converge-verify-g-8f31-002

leg: s-converge-verify-g-8f31-002
play: converge-verify
direction: indie-game-development
node: g-8f31
call: `work/c-converge-verify-g-8f31-002-call.md`
date: 2026-08-01
session: свежая физическая сессия, отдельная и от починки-2, и от поправки сети, и от первой
         проверки (G5)

## outcome

**FAIL.** Оба удара плея снова нашли дыру: `complete=FAIL`, `smuggling=FAIL`. Семь именованных
строк возвращаются в `converge` (RETROFIT) нарядом `c-converge-g-8f31-retrofit-003`: четыре меняют
игру, три — формулировку. `§WHAT` не тронут ни одним символом; карточка и устав не тронуты;
`§SIGNOFF: converge-verify passed` НЕ записан.

**Главный факт этой проверки: три из четырёх игровых находок — следствия ПОЧИНОК, а не остатки
старых дыр.** Инвариант, которым закрыли Г3, породил Д1. Односторонний запрет, которым закрыли
Г5, породил Д2. Проверяемое свойство, которым подписали Г4, породило Д4. Класс узла («строку
назвала сама нога, и она стала несущей») сработал в шестой, седьмой и восьмой раз — в новой форме:
**починка родила новую несущую строку**. Четвёртая находка (Д3) — единственная старого типа:
вопрос, который не родился ни в одной из трёх ног и которого нет и в `converge-arch`.

**Пятнадцать строк первой проверки: двенадцать закрыты полностью, Г3 и Г4 наполовину, Г5 закрыта
с новой дырой на месте закрытия.** Постатейно — в наряде и ниже.

## §ORACLE — независимый оракул второй проверки

Первая проверка авторизовала с нуля чек-лист класса «первый ИГРАЕМЫЙ узел кооперативной игры»:
десять измерений, `history/2026-08-01-s-converge-verify-g-8f31-001.md` §ORACLE. Он предложен
каноном, но в `knowledge/` не промоутен (там по-прежнему три записи, ни одна не чек-лист класса;
`converge-verify` `knowledge/` не пишет). Он взят здесь как ПРЕЦЕДЕНТ — и этого мало: набор чинили
ровно под эти десять измерений, поэтому повтор того же оракула проверил бы только починки. К
десяти добавлены три измерения, авторизованные здесь и предлагаемые каноном вместе с ними.

| # | измерение | g-8f31 после починки-2 |
|---|---|---|
| 1 | Вход | покрыто |
| 2 | Глаголы: что чему мешает | **закрыто починкой** (W32: держащий не копает) |
| 3 | Тела: что чем останавливается и выход из запрещённого состояния | **новая дыра — Д2**: выход из груза открыли так, что исчезло «груз разделяет группу» |
| 4 | Конец единицы | закрыто починкой (W90); остаток по числу ходок — Ф-b |
| 5 | Причина трудности: что в СОБРАННОЙ сцене гарантирует, что предмет проверки случится | **дыра осталась — Д1**: «зачем копать» закрыто его словом, а «где будет трудно» держится на инварианте, который спорит с W44 |
| 6 | Отказы | закрыто (W09, W47) |
| 7 | Авторитет и копии | покрыто |
| 8 | Данные против кода | покрыто и сильно |
| 9 | Излучение соседям | покрыто |
| 10 | Язык | **дыра — Ф-a**: новое несущее слово починки-2 по словарю не прогнано |
| **11** | **Вертикаль и тяжесть**: есть ли у мира верх и низ для игрока и для предмета, и что бывает, когда мир получил дыру ВНИЗ | **дыра — Д3**: ни строки ни здесь, ни в `converge-arch` |
| **12** | **Мерка тела**: какой величиной меряется всё, что «проходит / не проходит», и живёт она в узле или уехала в `→ PLAN` | **дыра — Д3 (вторая половина)**: W88 объявляет ширину настоящим ограничением, мерка — в W86 `→ PLAN` |
| **13** | **Следы починок**: каждая строка, вошедшая в файл как ОТВЕТ на находку, читается как новая и проверяется против соседей | **три из трёх крупных ответов дали новую находку** — Д1, Д2, Д4 |

Измерения 11 и 12 — то, чего не спросил никто: узел проектирует копание в объёме и переноску
тяжёлого предмета, ни разу не назвав, есть ли у мира вертикаль и чем меряется тело, которое по
этому миру ходит.

## evidence

- `work/converge-g-8f31.md` — прочитан целиком, 554 строки: все 89 `W`-строк, 35 `I`-строк,
  §GLOSSARY (33 слова), §CONTRACTS (K01–K06), §KNOBS, §SEAM, §OWNER, §COVERAGE, §SIGNOFF.
- `TREE.md` `g-8f31` — десять критериев, `goal`, `risk` и строка исключений разбиты на атомарные
  пункты и отображены на строки §WHAT.
- `work/c-converge-g-8f31-retrofit-002-call.md` — полный текст пятнадцати строк первой проверки;
  по каждой вынесен статус.
- `history/2026-08-01-s-converge-verify-g-8f31-001.md` — §ORACLE как прецедент.
- `NOW.md` — `i-card-prescribes-how-owner-tests-001`, `i-tunnel-role-description-owner-text-001`,
  драйверы прогноза; `knowledge/` — все три записи.
- `work/converge-g-8f31-arch.md` — НЕ использован как условие закрытия ни в одном месте; открыт
  единственной адресной проверкой на слова вертикали, уклона, тяжести и габарита тела: их там
  **ноль**. Это подтверждает, что Д3 — не «решено в другом файле», а не спрошено нигде.
- Мёртвые значения проверены построчно (`дверь|щел|посредник|грыз|укус|хост|число проходимости`):
  все вхождения — мёртвые строки словаря, его собственные слова в §IMPORT, шов будущих узлов
  (§SEAM 6) и история в §OWNER. Ни одно не вернулось живым.
- Расхождение §KNOBS против W17 («рыскание» против «раскачки») найдено первой рукой сверкой
  двух разделов.

## findings — МЕНЯЮТ ИГРУ

Полные формулировки со столбцом «как видно в игре» — в наряде
`work/c-converge-g-8f31-retrofit-003-call.md`. Кратко:

| # | строки | суть | источник конфликта |
|---|---|---|---|
| Д1 | W43 против W44 (W22 третьим стандартом) | Где узкое место гарантировано структурой, оно сделано из НЕкопаемого — и «копать здесь» там невозможно; где «копать здесь» работает, место сделала команда и уровень его не гарантирует. W44 требует все три ответа, W22 — хотя бы один, карточка — выбор | критерий 8 карточки против двух `acceptance`-строк авторства ноги |
| Д2 | W21 (новая половина) против W23 | Груз идёт сквозь игроков (его слово I24), а изнутри груза выход свободен В ЛЮБУЮ СТОРОНУ — значит отрезанный проходит сквозь застрявший груз штатным «вытолкнуть назад» | критерий 4 карточки «разделить группу» + W23 (`acceptance`) |
| Д3 | W65 против I20 и цели карточки; W88 против W86 | Вертикали нет ни у игрока, ни у груза, а маршрут «под землёй → наружу у базы» обязан подниматься. Плюс мерка: «ширина хода — настоящее ограничение», а габарит тела уехал в `→ PLAN` | его слово I20 и `goal` карточки против строки исключений; `acceptance` на величине из `→ PLAN` |
| Д4 | W17 (новая формулировка) против W15 | «Вчетвером быстрее и меньше раскачки» ложно ровно тогда, когда толкающий тормозит, — а тормозить ему разрешает W15. Если же свойство обязано держаться всегда, у третьей и четвёртой руки нет решения | две `acceptance`-строки; механизм назван ногой (`converge-arch` Q3) |

## findings — ФОРМУЛИРОВКА

| # | строки | суть |
|---|---|---|
| Ф-a | §GLOSSARY против W17 и §KNOBS | «Раскачка» несёт вес в `acceptance` и своей строки не имеет; §KNOBS зовёт ту же ручку «рыскание». Класс Ф3/Ф4, слово новое |
| Ф-b | W37 + W77 | «Ходок минимум две» выведено из «грузов два» и неверно вчетвером: коробок несут двое, отвёртку тащит один — оба груза уходят за одну ходку. W77 требует от уровня «комнату с грузом» в единственном числе |
| Ф-c | W10 | Крен снимается «в момент хвата первого», когда оси ещё нет: она появляется только со вторым хватом, и W10 сама это говорит |
| Ф-d | W81 + W90 + §KNOBS | Пробег по поверхности стал предметом проверки его словом I34 и не имеет ни одной величины — ни строки, ни ручки |

## что атаку ПЕРЕЖИЛО

- Ни одно мёртвое значение не вернулось: `дверь`, `щель` и `посредник` держатся мёртвыми ровно так,
  как задумано, — строка есть, значение в ней «слова здесь нет».
- Все строки, чей источник — его слово (I19–I35), прослеживаются до цитаты.
- Правки починки-2, пережившие атаку целиком: W82 (как попадают в дом), W83 (зачем копать — теперь
  свойство собранного уровня), W90 (база и конец ходки), W52 (копаемая обстановка), W32 (держащий
  не копает), W19 (минимум между хватами), W09 (без «пока напарник не вернулся»), обе новые строки
  словаря.
- Контракты K01–K06, §KNOBS, §SEAM, W89, W87, ступени грузов, разведение столкновений по
  направлению — держатся.
- Честность файла снова подтверждена первой рукой: §SIGNOFF сам объявляет полноту недоказанной и
  сам называет класс ошибки пятью случаями. Это и позволило бить точно.

## state_changes

1. `NOW.md` — `open_calls`: `c-converge-verify-g-8f31-002` снят (исполнен, вердикт FAIL); добавлен
   `c-converge-g-8f31-retrofit-003` (play `converge`, RETROFIT, `status: ready`).
2. `NOW.md` — `direction_forecast.drivers`: добавлена запись о втором FAIL, о семи строках и о том,
   что три из четырёх игровых находок — следствия починок; `update_when` не менялся.
3. `NOW.md` — `updated:` на эту ногу.
4. `LOG.md` — одна строка.
5. `history/2026-08-01-s-converge-verify-g-8f31-002.md` — этот RESULT.
6. `work/c-converge-g-8f31-retrofit-003-call.md` — наряд с семью именованными строками и постатейным
   статусом пятнадцати.

`work/converge-g-8f31.md` НЕ изменён ни одним символом. `TREE.md` и `CHARTER.md` не тронуты. Ставка,
полосы, задачи и исполнительные наряды не создавались. `knowledge/` не тронут.

## captures

- Оракул класса «первый играемый узел» вырос до ТРИНАДЦАТИ измерений: к десяти первой проверки
  добавлены «вертикаль и тяжесть», «мерка тела» и «следы починок». Предлагается каноном в
  `knowledge/` целиком; промоутит `review` или `pulse`.
- Наблюдение для `os/MAINTENANCE.md`, НЕ действие этой ноги — ВТОРОЙ случай того же трения:
  `converge` объявляет покрытие механическим, и оба его теста зелёные на файле, где три `acceptance`
  -строки требуют противоположного. Первый случай записан первой проверкой. Двух случаев хватает
  на FRICTION: механическое покрытие не отличает «на критерий есть строка» от «строки между собой
  не спорят».
- Класс, названный этой ногой и не бывший в списке: **починка сама порождает несущую строку**.
  Правило файла ловит изобретение и ловит дыру после снятия, но не ловит третью форму — ответ на
  находку, который становится несущим, не будучи ничьим словом. Все три крупных ответа починки-2
  сработали именно так.

## decisions_needed

Нет. Плей `converge-verify` не заводит новых точек G7: он маршрутизирует находки и ничего не решает.
По этому списку к владельцу, судя по всему, ноль вопросов — Д1 чинится приведением строки ноги к
тексту его карточки, остальные три технические (I30).

## play_check

- 1 recite: done — `verify_target: build`; удар 1 «набор полон», удар 2 «ни один ответ не опирается
  на нерешённое».
- 2 completeness: done — десять измерений первой проверки взяты прецедентом, три добавлены здесь ДО
  атаки и предложены каноном; пустым оракул не остался. Все десять критериев карточки, `goal`,
  `risk` и строка исключений разбиты атомарно и отображены на §WHAT. `complete=FAIL` (Д3, Ф-b, Ф-d).
- 3 smuggling: done — каждая несущая `acceptance`-строка проверена на источник: его слово,
  утверждённый текст карточки, вынужденный вывод — или нога. `smuggling=FAIL` (Д1, Д2, Д4).
- 4 close: done — FAIL возвращает именованные строки в `converge`; §SIGNOFF не подписан.
- `verify_target: build complete=FAIL smuggling=FAIL`
- Свежая физическая сессия, отдельная от всех, кто делал работу (G5) — да; фан-аут не применялся,
  плей его для `converge-verify` не требует.
- Рассуждение решавших сессий не читалось. Из `history` прочитан RESULT первой ПРОВЕРКИ (не ноги
  починки) — ради оракула-прецедента, как предписывает шаг 2.
- `§RULES` арх-файла условиями закрытия не стали; арх-файл открыт одной адресной проверкой на
  отсутствие вертикали и мерки тела.
- Ни одной новой строки в §WHAT, ни одной правки §WHAT: verify никогда не чинит.
- Технические развилки владельцу не вынесены (I30). Дверь и посредник не переоткрывались ни в одной
  находке: все четыре — о собранном уровне, о телах, о вертикали и о переноске.
- Карточка и устав не тронуты; расхождения по карточке ведёт прежняя запись `NOW`.
- G5 не ослаблен; `open_call` закрыт исполнением.

## log

2026-08-01 | s-converge-verify-g-8f31-002 | converge-verify | direction | g-8f31/cargo-through-tunnel

## next

`converge` (RETROFIT) — `c-converge-g-8f31-retrofit-003`. После починки — свежий `converge-verify`,
и только потом `shape`.

END_OF_FILE: live/indie-game-development/history/2026-08-01-s-converge-verify-g-8f31-002.md
