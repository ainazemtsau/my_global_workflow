# RESULT — s-work-g-5a7c-body-1-checkpoint-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-body-1
track: body
date: 2026-08-12
outcome_kind: checkpoint — кандидат перемерен целиком, задача НЕ закрыта

owner_approved: |
  «завершена c-exec-g-5a7c-body-1-001» — 2026-08-12.

  Слово запустило приёмку, но закрытием не является: по правилу направления handback исполнителя —
  улика, а не Direction-OS close. Сам отчёт исполнителя тоже не заявляет закрытия: его первая
  строка — «CANDIDATE on `slot/win-u1`; not integrated into `dev`».

outcome: |
  **`t-body-1` НЕ ЗАКРЫТА. Кандидат построен, перемерен целиком и признан хорошим; не хватает
  ровно публикации, и на пути к ней стоит одна опасная вещь.**

  ## ТРИ ПРИЧИНЫ НЕ ЗАКРЫВАТЬ, по возрастанию серьёзности

  **1. Не опубликовано.** `origin/main` = `origin/dev` = `4e5858b4` — не сдвинулись;
  `origin/slot/win-u1` = `43648b2d`, то есть слотовая ветка не запушена вовсе. Буква `evaluator`
  этой ставки, клауза 1: Направление закрывает задачу ПОСЛЕ публикации.

  **2. БАЗИС ПРОТУХ, И ПУБЛИКАЦИЯ КАК ЕСТЬ СТЁРЛА БЫ ШОВ.** Родитель рабочего коммита `0ea2cb5c` —
  `f3c40c40`. Пока тело строилось, шов `t-places-1` опубликовался и голова стала `4e5858b4`.
  Поэтому дифф `origin/main..HEAD` сегодня УДАЛЯЕТ работу шва: `−23` `HousePlan.cs`, `−83`
  `Householder.cs`, `−60` `HouseholderRoutineProfile.cs`, `−84` `HouseholderProfileLoader.cs`, поле
  роли в `HousePlaceMarker.cs`, **`−402` — весь файл `HouseholderPlaceRoleTests.cs`** и отчёт шва.
  Это ровно тот класс, ради которого в ставке стоит клауза 5 `evaluator` («ни одна дорожка не
  публикует состояние, которого не собирала вместе с текущими головами других дорожек») и ровно то,
  что убило волну `g-1d84`.

  **Вины исполнителя тут меньше, чем кажется:** наряд разрешал перебазирование, но на его старте
  голова ещё не двигалась, и он проверил её один раз — в начале. Расписка это фиксирует честно:
  «Перед работой… после `git fetch` голова `origin/main` не сдвинулась». Сдвинулась позже.

  **И чинится это тривиально.** Перемерено `comm` по спискам путей: собственный дифф тела
  (`f3c40c40..HEAD`, 34 файла) и дифф шва (`f3c40c40..4e5858b4`, 11 файлов) **не пересекаются НИ
  ОДНИМ файлом**. Перебазирование должно пройти без конфликтов.

  **3. Откат «сошёлся», но со СТАРОЙ базой.** `tree(2fb82f54)` = `5ec74916…` = `tree(f3c40c40)`.
  Для своей базы это корректный откат; для текущей `tree(4e5858b4)` = `b15f5ad4…` он не доказывает
  ничего. После перебазирования откат надо переисполнить.

  ## ЧТО СДЕЛАНО ХОРОШО — перемерено мной, а не взято с отчёта

  - **ГЛАВНАЯ ГРАНИЦА ДЕРЖАЛАСЬ: ноль сцен в диффе.** Шов сделан через `PresentationSettings` —
    ровно ту дверь, которую наряд назвал ФАКТОМ, не указанием.
  - **Примитивы ушли:** `NetworkWalkerPresentation.cs` `−80` (капсула и куб больше не строятся
    кодом), `World/Householder.prefab` `−73` (встроенная капсула убрана).
  - **Появился отдельный модуль презентации** `Assets/TunnelCrew/Presentation/**`:
    `BodyVisualDefinition`, `BodyVisualInstance`, `HouseholderBodyPresenter`.
  - **МОДЕЛЬ НАСТОЯЩАЯ, НЕ УКАЗАТЕЛЬ:** `Casual_Male.fbx` — 1 612 396 байт, заголовок
    `Kaydara FBX Binary`. Риг Generic, 23 кости, 11 встроенных клипов.
  - **ЛИЦЕНЗИЯ ЧИСТАЯ И ПРОВЕРЕНА ЧТЕНИЕМ:** Quaternius, **CC0-1.0**, коммерческое использование без
    атрибуции; `License.txt` и `SOURCE.md` лежат рядом с исходником по образцу ветки пробы.
  - **ЗАМЕНА ДОКАЗАНА ИЗМЕРЕНИЕМ В ЖИВОМ РЕДАКТОРЕ, а не обещанием:** хозяин переключён между двумя
    заготовками, ширина `1.819770` → `1.137356` м при неизменной высоте `1.999997`, и при этом
    менялось **только поле `_householderBody._prefab` в ассете** — ни C#, ни контроллера, ни
    игрового префаба.
  - **МАСШТАБ ДЕРЖИТСЯ:** налётчик `0.159998` м, хозяин `1.999997` м; коллайдер хозяина цел
    (радиус `0.5`, высота `2`); импортированные коллайдеры, тела, камеры и аудио выключены, root
    motion выключен; своё тело на своём экране по-прежнему не рисуется.

  ## ЧТО НЕ ЗАКРЫТО — и две из трёх вещей отчёт назвал сам

  - **Поворота как анимации нет.** Поворот берётся от курьера, визуал добавляет ограниченный
    косметический доворот. Отчёт пишет честно: «There is no falsely claimed dedicated turn clip».
    Строка 2 просила «работают стойка, ходьба и поворот»; стойка (`4.166667` с) и ходьба
    (`1.25` с) — настоящие клипы, поворот — нет.
  - **Четверо по сети не проверены** — отчёт сам называет это «owner-eye four-copy acceptance».
  - **ОБЕ ЗАГОТОВКИ — ОДИН И ТОТ ЖЕ ИСХОДНИК, и это моё замечание, не его.** Замена доказана
    переключением `Quaternius_CasualMale` ↔ `Quaternius_CasualMale_Wide`, то есть одна модель в
    двух обёртках. Строка 1 просила «ЗАМЕТНО ОТЛИЧАЮЩУЮСЯ». Механизм при этом доказан по существу:
    сменилось одно поле данных и ничего больше.

  **Светлое закрытие недоступно, и это было названо ДО работы:** строки 1 и 3 требуют живого
  редактора и его глаза. Значит после публикации задачу закрывает либо его слово, либо свежий
  биндинговый чат.

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ.**

  - `git ls-remote origin` → `main` = `dev` = `slot/win-u2` = `4e5858b4`; `slot/win-u1` = `43648b2d`.
    Коммитов сверх `4e5858b4` в `origin/main` — ноль.
  - В `GasCoopGame_win-u1`: ветка `slot/win-u1`, ahead 94, рабочее дерево ЧИСТОЕ (0 изменений);
    `origin/main..HEAD` = `4bb27ca4` reapply, `2fb82f54` revert, `0ea2cb5c` работа.
  - `git rev-parse 0ea2cb5c^` → `f3c40c40` — родитель работы, то есть старая база.
  - Деревья: `4e5858b4` → `b15f5ad4…`; `f3c40c40` → `5ec74916…`; revert `2fb82f54` → `5ec74916…`
    (совпало со СТАРОЙ базой); работа и reapply → `7c68e894…` (совпали между собой).
  - `git diff --name-only origin/main HEAD | grep -Ei '\.unity$' | wc -l` → **0**.
  - Файлы чужой территории в диффе против `origin/main` → пять, все из шва, все с минусами.
  - `comm -12` по спискам путей собственного диффа тела и диффа шва → **пусто**.
  - `ls -l Casual_Male.fbx` → `1612396` байт; `head -c` → `Kaydara FBX Binary`.
  - `License.txt` → `Creative Commons Zero v1.0 Universal (CC0-1.0)`, прочитано.
  - `Casual_Male.fbx.meta` → `animationType: 2` (Generic).
  - Отчёт исполнителя `docs/results/c-exec-g-5a7c-body-1-001.md` прочитан целиком; его собственные
    числа (`1.819770` / `1.137356` / `1.999997` / `0.159998`, клипы `4.166667` и `1.25`) взяты как
    его наблюдения в живом редакторе, чего Направление повторить не может, и названы как ЕГО, а не
    как мои.

  **G5:** ничего не закрывается, биндинговая проверка не требуется и не выдаётся за существующую.

state_changes: |
  1. `t-body-1`: остаётся `active`. Добавлена журнальная строка.
  2. `c-exec-g-5a7c-body-1-001`: остаётся открытым до публикации; добавлена журнальная строка о
     возврате кандидатом.
  3. Создан `work/c-exec-g-5a7c-body-1-002-call.md` и карточка `c-exec-g-5a7c-body-1-002`
     (`status: ready`, `slot: WIN-U1`, `basis: 4e5858b4`) — механическое продолжение:
     перебазировать, переисполнить откат, опубликовать. Ничего нового не строится.
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-body-1-checkpoint-001.md`.

captures:
  - |
    **ПРАВИЛО «ПЕРЕБАЗИРОВАНИЕ ВМЕСТО СТОПА» ПРОВЕРЯЕТСЯ ОДИН РАЗ — В НАЧАЛЕ, А ОПАСНОСТЬ ВОЗНИКАЕТ
    В КОНЦЕ.** Наряд говорит «если `origin/main` сдвинулся — перебазируйся», и исполнитель честно
    проверил голову на старте, когда она ещё не двигалась. Формулировку стоит усилить в следующем
    наряде: сверять голову ПЕРЕД публикацией, а не только перед сборкой. Это первый случай в
    направлении, когда две дорожки реально разошлись во времени, и он обошёлся дёшево только
    потому, что их файлы не пересеклись.
  - |
    Замена модели доказана переключением между двумя обёртками ОДНОГО исходника. Механизм это
    доказывает, «заметно другая модель» — нет. Стоит решить на review волны, достаточно ли этого для
    критерия 10 узла, или второе тело обязано быть другим исходником.
  - |
    Риг у выбранной заготовки **Generic**, не Humanoid. Значит анимации привязаны к этой модели и не
    переносятся на другую автоматически. Для заглушки это неважно, но когда приедет его собственная
    модель из Concept Lab, вопрос «переносятся ли клипы» встанет заново.

decisions_needed: []

play_check:
  - 1 recite — done. Семь строк карточки и обязательство дорожки пересказаны.
  - 2 owner inputs — не требуется сейчас: облик закрыт его словом про заглушку. Его глаз понадобится ПОСЛЕ публикации, и это названо.
  - 3 do the work — приёмка возврата; выпущено механическое продолжение вместо закрытия.
  - 4 self-check — done. Каждая строка сверена; расхождений с отчётом не найдено, две его собственные оговорки подтверждены, одно замечание добавлено сверх отчёта (один исходник на две заготовки).
  - 5 close — checkpoint, не закрытие. Основание названо тремя пунктами, главный из которых — что публикация как есть удалила бы закрытую час назад работу.
  - G5 — не применяется; светлое закрытие для этой задачи недоступно и это было объявлено до работы.
  - G7 — done. Ни одного игрового решения не принято.
  - G10 — done. RESULT финальный.

log: t-body-1 НЕ закрыта, кандидат перемерен целиком и признан хорошим — ноль сцен в диффе, примитивы ушли из презентации ходока и капсула из префаба хозяина, появился отдельный модуль презентации, модель настоящая FBX 1.6 МБ под CC0-1.0 с лицензией рядом, замена доказана измерением в живом редакторе сменой ОДНОГО поля данных при ширине 1.8198 → 1.1374 и неизменной высоте, масштаб 0.159998 и 1.999997 держится; но закрывать нельзя по трём причинам, и вторая опасна: работа не опубликована вовсе, а собрана она на протухшей базе f3c40c40, поэтому публикация как есть УДАЛИЛА БЫ ВЕСЬ ШОВ, закрытый часом ранее, включая его 402-строчный тестовый файл целиком; чинится это тривиально, потому что пересечение путей собственного диффа тела и диффа шва ПУСТО, и выпущено механическое продолжение на перебазирование, переисполнение отката и публикацию; откат при этом сошёлся со СВОЕЙ старой базой и на новой ничего не доказывает; сверх отчёта названо, что обе заготовки это один и тот же исходник в двух обёртках, то есть механизм замены доказан, а «заметно другая модель» нет, и что поворота как анимации нет вовсе — это отчёт признал сам

next: |
  `c-exec-g-5a7c-body-1-002` — механическая нога: перебазировать на `4e5858b4`, переисполнить откат
  на новой базе, опубликовать. Конфликтов быть не должно, пересечение путей пусто.

  **ПОСЛЕ публикации задачу закрывает глаз владельца или свежий биндинговый чат** — светлый путь ей
  недоступен и это было названо до работы. Ему смотреть: три движения и четыре копии по сети.

  Идут параллельно: `t-rooms-1` в WIN-U2, `t-thing-1` в WIN-U3.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-body-1-checkpoint-001.md
