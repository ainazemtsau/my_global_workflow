# RESULT — s-work-g-5a7c-scale-6-close-001

play: work · close: light — because каждая строка `done_when` наряда разрешается либо коммитом,
деревом, значением байта или записанным выводом проверки, которые эта нога перечитала первой рукой,
либо уже закоммиченными дословными словами владельца об этом же артефакте; ни одного качественного
вердикта, которого никто не выносил, здесь не додумывается
direction: indie-game-development · node: g-5a7c · task: t-scale-6 · call: c-exec-g-5a7c-more-hands-lift-higher-001
date: 2026-08-10

base_read: OS — `wt/indie-game-development` = `e9eaaa52`, рабочее дерево чисто. Продукт перемерен
`git ls-remote origin` в начале этой ноги: `origin/dev` = `origin/main` = `origin/slot/win-u1` =
`918600ba355c693068035894ef86d258b46ccb0f`. Слот перемерен настоящим селектором (только чтение):
WIN-U1 `state: CLEAN`, `lifecycle: AVAILABLE`, `lease: none`, `head: 918600ba`. Продуктовые байты,
ветки, слоты и slot-state этой ногой НЕ изменялись.

## outcome

`t-scale-6` закрыта. Груз больше не получает высоту назначением: у живого тела всегда есть вес,
каждая рука тянет к запрошенной точке со своим потолком, и высота — равновесие этих сил. Одна рука
едва отрывает груз от пола, каждая следующая поднимает выше; ветвления по числу носильщиков в законе
нет ни одного.

Ступень создана одним числом — масса `20 → 48`. Высоты по 1/2/3/4 рукам: `0.020 / 0.056 / 0.068 /
0.074`, разброс `0.054` м. Коэффициент вертикального затухания удержан на `1` при любом числе рук,
поэтому находка расчёта «лишняя рука делает груз пружинистее» закрыта устройством, а не обойдена.

Оба шва заложены и работают без функции: потолок силы читается у каждой руки
(`CargoHaul.cs:169` → `hold.Strength`, прежний общий `_cargoMaxHandForce` удалён из всего кода), а
запрошенная точка стала настоящей 3D-точкой со спадом силы по выносу. Первый из двух швов
`t-scale-7` этим выполнен.

**Две вещи закрытием НЕ проглочены.** Владелец принял ступень с прямой оговоркой «очень мало» —
записана уликой `i-lift-step-accepted-but-small-001`. Расчётная находка о том, что волочение груза
одной рукой даёт шорох каждый тик, — уликой `i-dragged-cargo-rustles-every-tick-001`. Ни одна из них
не превращена в требование и не растягивает волну.

Волна не закрыта: остаётся `t-scale-7`, чей наряд уже написан и стоит `blocked` в ожидании слова о
слоте.

## evidence — перемерено первой рукой, а не принято с возврата

**Интеграция и публикация.** Принятый tip `c8861472` интегрирован в `dev` merge-коммитом
`3d24731a` (родители `4699647d` и `c8861472`), закрывающий документный коммит — `918600ba`.
**Проверено этой ногой:** `git diff --stat c8861472 3d24731a -- Assets/ tests/ ProjectSettings/`
пуст — принятые product/test bytes через merge не переписаны. Второй родитель принёс только чужой
инструмент (`tools/task-preflight*`), к этой задаче отношения не имеющий. Опубликовано: `origin/dev`
= `origin/main` = `origin/slot/win-u1` = `918600ba`.

**Откат доказан настоящим revert, а не обещанием.** Коммит `cbf3a360` — `Revert "feat: lift cargo by
summed hand force"`, родитель `9f4eb6cf`, **дерево `a4d326b5a144fddc7f081f41dc525655c7d3342b`, то
есть знак в знак дерево базы наряда.** Перечитано этой ногой из объектной базы.

**Слот освобождён.** Read-only селектор WIN-U1 прямо в этой ноге: `CLEAN / AVAILABLE / lease none`,
`head: 918600ba`.

### `done_when` наряда — построчная диспозиция

**Строка 1 — высота как равновесие, никаких ветвлений, потолок числа рук числом, два шва, ноль новых
величин в `Core`. ЗАКРЫТА.**

Механическая половина перечитана байтами:

| Проверка этой ноги | Результат |
|---|---|
| `grep -n "Count ==\|holds.Count" CargoHaul.cs` | только пустой guard `holds.Count == 0` и граница цикла — **ветвления по числу рук нет** |
| `grep -rn "MaxHandForce" Assets/ tests/` | пусто — общий потолок удалён |
| `CargoHaul.cs:169` | `var limit = hold.Strength;` — правило смотрит в руку |
| `NetworkPlaySettings.asset` | `_cargoMaxHolders: 4`, `_cargoHoldStrength: 200`, `_cargoMass: 48`, `_cargoVerticalDampingRatio: 1`, `_cargoHandForceReachFalloff: 1` |

Поведенческая половина — «одна рука едва отрывает, каждая следующая строго выше, подъём движением, а
не скачком, и груз не начинает раскачиваться от добавления рук» — стоит на его уже закоммиченных
словах в `docs/results/c-exec-g-5a7c-more-hands-lift-higher-001.md@918600ba`:

> «первый, да, было по полу, второй игрок чуть повыше, третий еще чуть повыше»

и, после отдельного списка проверок, включавшего четырёх игроков, **отсутствие прыжков**,
падение/тишину, одинаковую Y на Host/Join и сохранённый занос с догонянием:

> «работает по всем пунктам»

**Строка 2 — ступень видна, обе копии, и в возврате названы числа. ЗАКРЫТА.** Возврат называет
фактическое число ступени (масса `20 → 48` при неизменных `gravity 3`, жёсткости `2000` и
запрошенной высоте `0.092`), разброс высот `0.054` м и — отдельным разделом — что происходит с шумом
при волочении: `~83.4` Н доложенной силы, полоса `QuietRustle`, до `~60` стимулов в секунду, честно
названные расчётом, а не замером. Одинаковая Y на обеих копиях входит в его «работает по всем
пунктам». **Его оговорка «сейчас видно, что поднимается, но вообще она очень мало» записана уликой,
а не сглажена.**

**Строка 3 — принятое не сломано, откат проверен. ЗАКРЫТА.** На интегрированном состоянии записаны
**144 passed, 0 failed, 0 skipped** и `hygiene OK`; Unity `6000.5.5f1`, `IsCompiling=false`,
`IsUpdating=false`, Error-лог за прогон пуст. Границы держатся значениями на HEAD: наклон X/Z
заморожен, `1.28 / 0.8 / 1.6` на месте, пороги `60 / 150 / 400` на месте, сцена, четыре куска,
доставка, курьер, presentation, хозяин и `ProjectSettings/**` в манифест не входят. Откат — коммит
`cbf3a360` с деревом базы, перечитан выше.

### Наблюдение, названное и не превращённое в блокер

Закрывающий Control-handback перечисляет merge, независимый review, headless, гигиену, откат и
публикацию, но **не называет прогон `-Deliver`**. `done_when` этого наряда его по имени и не
требовал, поэтому строка 3 закрыта. Но `origin/main` снова догнал `origin/dev` — ровно условие
улики `i-deliver-gate-red-when-main-catches-dev-001`. Названо здесь, чтобы следующая продуктовая
нога не удивлялась красному экрану на чужой истории.

## state_changes

1. Закрыть task card `t-scale-6`: терминальный статус `done`, история —
   `history/2026-08-10-s-work-g-5a7c-scale-6-close-001.md`.
2. Закрыть возвращённую call card `c-exec-g-5a7c-more-hands-lift-higher-001`: `status: closed`, та же
   история.
3. Завести issue `i-lift-step-accepted-but-small-001`: `level: objective`, `route: review` — его
   оговорка о величине ступени с дословными словами.
4. Завести issue `i-dragged-cargo-rustles-every-tick-001`: `level: execution`, `route: work` —
   расчётный шорох каждый тик при волочении.
5. Обновить `unblock_when` у `c-exec-g-5a7c-cargo-class-seam-001`: публикация и свободный слот
   выполнены, осталось слово владельца о слоте. Статус остаётся `blocked`; ни `for`, ни `call`, ни
   `repo`, ни пин не трогаются.
6. `t-scale-7`, `g-5a7c`, `NOW.bet` не трогать: волна не закрыта, `review` не открывается — открытая
   задача в ставке ещё есть.
7. Одним `osctl leg close` сохранить этот RESULT в
   `history/2026-08-10-s-work-g-5a7c-scale-6-close-001.md` и добавить log ровно один раз в журналы
   `g-5a7c`, `t-scale-6`, возвращённого call, обеих новых улик и ожидающего exec call.

## captures

- Инструмент `tools/task-preflight*` приехал в `dev` чужим коммитом `4699647d` внутри того же окна.
  К этой задаче отношения не имеет, в её манифест не входит, здесь только зафиксирован, чтобы
  будущая нога не приписала его этой работе.

## decisions_needed

Нет.

## play_check

- 1 recite: done — три строки `done_when` наряда сведены построчно; задача служит активной ставке
  `g-5a7c`
- 2 owner inputs (owner): done — его дословные слова уже закоммичены в продуктовом RESULT:
  «первый, да, было по полу, второй игрок чуть повыше, третий еще чуть повыше», «можем закрывать, да,
  с такой пометкой, что не сильно высоко», «работает по всем пунктам»
- 3 do the work: done — merge, дерево, откатный коммит, значения настроек, отсутствие ветвления и
  состояние слота перечитаны первой рукой; две находки заведены уликами
- 4 self-check: done — каждая строка `done_when` имеет свой evidence-токен; отсутствие `-Deliver` в
  Control-handback названо, а не пропущено
- 5 close: done — задача и возвращённый call закрываются, две улики заведены, следующая локальная
  работа уже зарегистрирована и ждёт слова о слоте

## Гейты

- **G5.** `close: light` заявлен построчно. Механическая часть — коммиты, деревья, вывод `grep`,
  значения в ассете, состояние селектора: всё перечитано здесь. Качественная часть — только его уже
  закоммиченные слова, процитированные там, где они лежат. Ни одна строка не требует суждения,
  которого никто не выносил; поэтому отдельный refutation-чат не открывается. Что НЕ повторено своей
  рукой, названо явно: headless-набор и Unity-прогон приняты как записанные выводы проверок, заново
  не запускались.
- **G7.** Геймплей не решён ногой: величина ступени не переоценена, «очень мало» записано как его
  слово с маршрутом на `review`, шум не подкручен.
- **G3.** Аппетит не растянут: обе находки ушли уликами, ни одна не стала работой. Волна остаётся
  семизадачной, `review` не открывается досрочно.
- **G9.** Карточка узла не редактировалась — только строка в её журнал.
- **builder-return is not close.** Закрытие стоит не на продуктовом handback, а на построчной
  диспозиции `done_when` наряда, перемеренной здесь, плюс его собственных словах.
- **Панель.** Направление панели не объявляет — регенерация пропущена.

## log

лут поднимается суммой рук: ступень принята его словами «второй чуть повыше, третий еще чуть повыше… работает по всем пунктам», откат доказан настоящим revert до дерева базы — задача закрыта, а его «очень мало» и шорох при волочении ушли уликами

## next

`CALL c-exec-g-5a7c-cargo-class-seam-001` — последняя задача волны, наряд написан и ждёт одного
слова: в каком слоте её запускать. Публикация и свободный WIN-U1 уже есть. После её возврата и
закрытия волну закрывает `review` ставки `g-5a7c`, который и разбирает обе новые улики.

END_OF_FILE: live/indie-game-development/history/2026-08-10-s-work-g-5a7c-scale-6-close-001.md
