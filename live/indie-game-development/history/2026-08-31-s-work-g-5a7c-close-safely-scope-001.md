# RESULT — s-work-g-5a7c-close-safely-scope-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-close-safely-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: scope — отчёт перевыверен по байтам: девять строк подтвердились, десятая не сделана и не названа, поверхность расширена на AGENTS.md

owner_approved: |
  Отдельного слова не требуется и не выдумывается. Расширение — **один документный файл в
  продуктовом репозитории**, и оно служит уже одобренной строке приёмки 1: порядок шагов
  закрывальщика описан в `AGENTS.md` вторым источником истины, и правка ноги сделала это описание
  ложным. Кода расширение не касается.

  Ни одна строка `done_when` не изменена и ни одна не добавлена.

outcome: |
  **ДЕВЯТЬ СТРОК ИЗ ДЕСЯТИ ПОДТВЕРЖДЕНЫ НЕЗАВИСИМО, ПО БАЙТАМ ЕГО ВЕТКИ, А НЕ ПО ТРАНСКРИПТУ.**
  Это важно: сегодня трижды закрытие по заявлению исполнителя оказывалось ошибкой.

  Главное подтверждено в самом коде, а не в его выводе консоли: `close-leg.ps1` несёт
  `2/7 build the integration commit -- dev does NOT move here`, `3/7 run the tests on the
  integration commit` и `6/7 move dev, fast-forward main, publish`. **`dev` двигается шестым
  шагом, после гейтов и после `-Deliver`.** Порядок, стоивший волне полдня, перевёрнут.

  **ЕСТЕСТВЕННЫЙ КОНТРОЛЬ, КОТОРОГО НИКТО НЕ ПЛАНИРОВАЛ, И ОН СИЛЬНЕЕ ЛЮБОГО ПОСЕВА.** В reflog
  `dev` между `10:55:07` и `11:06:00` пусто, а его закрытие с миной шло `11:00–11:06` — не
  оставило следа. Запись `11:06:00` принадлежит WIN-U2, гонявшей СТАРЫЙ закрывальщик со своей
  намеренной миной: её первым действием стал коммит `1ac70d9c Seed headless close-gate failure`,
  положенный в `dev` **до единого теста**. Одно утро, один и тот же намеренный красный, два
  порядка, два исхода — и оба видны в одном reflog.

  **ДЕСЯТАЯ СТРОКА НЕ СДЕЛАНА, И ЕЁ НЕТ В ОТЧЁТЕ.** Приёмка 10 требует сравнивать суффикс лизы
  без оглядки на регистр. `close-leg.ps1:152` сравнивает через `-cne` — оператор С УЧЁТОМ
  регистра, — а умолчание на `:80` строит `:build` в нижнем. `select-slot.ps1` он не трогал,
  значит штатный claim по-прежнему пишет `:BUILD`. **Тот самый отказ, который стоил ноге
  закрепления попытки и научил её звать закрывальщик с явным `-LeaseId`.** Поиском по отчёту слова
  об этом нет ни одного. Нога остаётся `active`.

  **НАЙДЕНА ЖИВАЯ ОПАСНОСТЬ, КОТОРОЙ В ОТЧЁТЕ НЕТ.** `dev` = `0a4d06f5` ушёл на четыре коммита
  вперёд `main` = `a45b4957`. Содержательная разница — **один документ**: отчёт WIN-U2, чей
  собственный заголовок говорит `Status: CANDIDATE on slot/win-u2 (NOT delivered)`. Кода в разнице
  нет, мина посеяна и восстановлена вперёд-коммитом, `dev` зелёный. Но **любое закрытие,
  публикующее из `dev`, увезёт в `main` неотданную ногу**. Это ровно то, что поймала его Находка 4,
  и состояние сейчас заряжено. Ему делать ничего не надо; развязка за U2.

  **РЕШЕНИЕ ПО `AGENTS.md:390` — ПРАВИТЬ, И В ЭТОЙ ЖЕ НОГЕ.** Абзац описывает мёртвый порядок
  «preflight → merge into `dev` → EditMode на ИНТЕГРИРОВАННОМ коммите». Его читают все исполнители
  во всех слотах на каждой ноге, то есть следующий наряд спланируется по несуществующему порядку.
  Нога, сломавшая документ, чинит документ; отдельная задача положила бы его в очередь за восемью
  другими. Столкновений нет: **ни один из трёх идущих нарядов не называет `AGENTS.md` своей
  поверхностью** — проверено по всем живым CALL-документам. Второй носитель того же старого
  порядка, `docs/results/c-exec-g-5a7c-close-leg-1-001.md:18`, править ЗАПРЕЩЕНО: это отчёт
  закрытой ноги, улика о том, что было верно на её день.

evidence: |
  Перемерено направлением в продуктовом репозитории `C:\projects\Unity\GasCoopGame`, ветка
  `slot/win-u4` = `7ad473d1`:

  - `close-leg.ps1` строки 131/306/338/396/427/445/476 — семь баннеров шагов, порядок как выше;
  - `git reflog show dev --date=iso` — `a45b4957 @ 10:55:07`, затем `25a7286e @ 11:06:00`; между
    ними пусто;
  - `git show --stat 1ac70d9c` — `Seed headless close-gate failure`, `10:58:00`, правит
    `DeliveryObjectiveTests.cs`; слит в `dev` на `11:06:00`;
  - `close-leg.ps1:80` строит лизу с суффиксом `:build` в нижнем регистре; `:152` сравнивает её
    оператором `-cne`, то есть С УЧЁТОМ регистра; `select-slot.ps1` в диффе ноги отсутствует;
  - `git diff --stat main dev` → один файл, `docs/results/c-exec-g-5a7c-gate-headless-1-002.md`,
    +59; его заголовок — `Status: CANDIDATE on slot/win-u2 (NOT delivered)`;
  - `integration-lock.ps1` (новый, 154 строки) — токен пишется в файл, стирается ДО отпускания
    ручки, повторный вход по токену процесса;
  - `unity-tests.ps1` — коммит чекаута читается ПОСЛЕ взятия замка, коммит на выходе тоже, и
    прогон отказывается при расхождении;
  - `close-leg.ps1:248-262` — отказ штамповать `DELIVERED` поверх отчёта, чьё тело признаёт
    блокировку, с прямой ссылкой на случай `gate-headless-1-001`;
  - `close-leg.ps1:225-243` — диапазон публикации начинается с `main`, а не с `dev`;
  - `git diff --stat main slot/win-u4 -- tools/close-leg.selftest.ps1` → `198 insertions(+)`,
    **ноль удалений**;
  - `git merge-base main slot/win-u4` = `56a7b88a`; `git merge-tree --write-tree main slot/win-u4`
    → exit 0, и в полученном дереве присутствуют `Core/Householder/HouseholderGauges.cs`,
    `HouseholderMarks.cs`, `Core/Householder.meta` и оба отчёта закрепления. **«Отстал», а не
    «удаляет»; откатывать нечего.**

  Не перепроверяемое направлением и оставленное уликой: EditMode 220/220, headless 725/725 и
  725 при одном упавшем под миной, самотест закрывальщика зелёный с 21 новым случаем, гигиена
  зелёная, преflight 3341 → 3585 мс.

state_changes: |
  ДОПИСАНО (1 документ наряда):
  - `work/2026-08-31-call-close-safely-1.md` — раздел «ДОПОЛНЕНИЕ 2026-08-31: решение по
    `AGENTS.md`, недоделанная строка 10, и живая опасность в `dev`». Прежний текст не тронут.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-close-safely-1` остаётся `active`, `c-exec-g-5a7c-close-safely-1-001` остаётся `ready`.
    Строка приёмки 10 не выполнена, закрывать нечего.

captures: |
  - **Второй раз за день СТОП исполнителя оказался наполовину не тем вопросом.** Утром зрение
    спросило «расширять ли наряд?», когда половина спрошенного стояла в его `done_when`. Сейчас
    закрытие спросило про `AGENTS.md` — законно, — но **не заметило, что одна его собственная
    строка приёмки не сделана**. Вывод для следующих нарядов: перед ответом на СТОП направление
    обязано пройти приёмку построчно, а не только разобрать заданный вопрос.
  - **Дефект окружения, ставший естественным контролем.** Старый и новый закрывальщик отработали
    один и тот же намеренный красный в одно утро с разницей в шесть минут. Такого доказательства
    не даёт ни один посев, и оно попало в улики только потому, что reflog читался целиком, а не
    вокруг ожидаемой строки.

decisions_needed: |
  Ни одного. Владельцу тут нечего выбирать: расширение документное, внутри продукта, служит уже
  одобренной строке.

play_check: |
  1. OPEN — возвращённый вопрос по идущей ноге; приёмка перечитана построчно ДО ответа.
  2. WORK — десять строк приёмки перемерены первой рукой в его ветке; отчёт читался как улика;
     проверено непересечение `AGENTS.md` со всеми живыми CALL-документами.
  3. (owner) — не требуется: документное расширение, служит одобренной строке 1.
  4. CLOSE — RESULT, применение, коммит; нога закрытия продолжается.

log: |
  отчёт безопасного закрытия перевыверен по байтам: dev действительно двигается шестым шагом и
  reflog это подтверждает чужим контролем; но строка 10 про регистр лизы не сделана и не названа,
  а dev прямо сейчас несёт неотданный отчёт U2 — правка AGENTS.md разрешена этой же ногой

next: |
  Ответ уходит в WIN-U4. Идут четыре ноги: зрение U1, доработка гейта U2, «почему» U3,
  безопасное закрытие U4.

  Развязка `dev` впереди `main` — за ногой U2.
