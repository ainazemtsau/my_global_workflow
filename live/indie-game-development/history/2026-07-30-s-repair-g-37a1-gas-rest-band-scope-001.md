# RESULT s-repair-g-37a1-gas-rest-band-scope-001

call: HOME ESCALATE from c-exec-g-37a1-gas-rest-amend-001
direction: indie-game-development · track: t-sim · play: repair · node/task: g-37a1/t-6
date: 2026-07-30

## outcome

ESCALATE подтверждён первыми руками и потреблён как blocker. Дефект в НАШЕМ замороженном пакете
настоящий: `G37-BAND-004` объявляет правило выброса накопленного остатка полным по трём ветками, а
вторая ветка — `cond == 0` при ранее накопленном ненулевом остатке — не имеет ни атомарного
поведенческого ряда, ни негативного контроля ни в одном из 28 рядов.

Направление добавило к возврату находку, которой в нём не было: состояние этой ветки в t-6 недостижимо
ПО ПОСТРОЕНИЮ, а не по недосмотру. Поэтому вариант «дописать ряд и разрешить минимальный тестовый
носитель» невозможен без построения ядра t-7, и предложенный возвратом пункт 1 отпадает по факту, а не
по предпочтению.

Владелец выбрал вариант **«B»** (дословный токен): ветка передана `t-7`/D1 с его подписью на снятии.

Выпущен один ready свежий PLAN-корень `c-exec-g-37a1-gas-rest-band-scope-001` на тот же product
change_id, слот WIN-U4, пин 31 сохранён. Терминальный `c-exec-g-37a1-gas-rest-amend-001` снят из hot
frontier; его CALL, квитанции и объекты сохранены как улика. PAIR-CANDIDATE, PAIR-FREEZE и BUILD не
открыты. t-6 остаётся active, продуктового кода по t-6 по-прежнему ноль. Bet, tasks, tracks и WIP не
меняются.

## evidence

Всё ниже прочитано напрямую в `C:\projects\Unity\GasCoopGame_win-u4`, а не принято со слов возврата.

- **Венью совпадает с заявленным:** ветка `slot/win-u4`, HEAD
  `6d5879b368ac96ea31cc6bbee841f537ecac8b80`, рабочая копия чистая. Ни commit-носителя, ни RED, ни
  квитанции 06 — сессия PAIR-CANDIDATE действительно ничего не записала.
- **Квитанция и её хеш:** `docs/measurements/root-receipts/c-exec-g-37a1-gas-rest-and-checksum-001/05-plan.json`,
  sha256 пересчитан Направлением: `d0286ac60029acc7c1bf755f26bb307dc2206696a45ef802cbf01cbd7793b7f7` —
  совпадает побайтово с заявленным. `verdict: PLAN_FROZEN_OWNER_APPROVED`,
  `eligibility: ELIGIBLE_FOR_FRESH_PAIR_CANDIDATE`.
- **Счётчики сверены с реестром построчно, а не приняты из квитанции:** 28 поведенческих рядов
  (`T6-01`…`T6-08N`, `T6-14`, `T6-14N`), 7 evidence-only, 14 негативных контролей. Выписаны все 28 —
  ветку `cond == 0` не покрывает ни один.
- **Покрытие двух других ветк подтверждено:** `|Δφ| < Θ` — `tasks.md:38-39` (`T6-04G`/`T6-04GN`);
  отклонение маской направления — `tasks.md:58-59` (`T6-14`/`T6-14N`, решение владельца «вариант 1»,
  записанное в квитанции 05 как `direction_mask: option 1`).
- **Нормативный текст:** `specs/sim-core/spec.md:212` открывает `G37-BAND-004`; `:251` объявляет
  правило выброса ПОЛНЫМ и перечисляет три ветки; `:264` несёт вторую, отдельную клаузу `cond == 0`
  («грань с проводимостью `0` переносит ровно `0`»). Спецификация САМА признаёт на `:256-258`, что
  переход «накопила остаток, затем потеряла проводимость» лежит вне области этой ноги («topology
  belongs to `t-7`/D1») — и тем же абзацем требует его поведение.
- **Недостижимость доказана по коду:** `Assets/GasCoopGame/Core/Field/Voxel/VoxelField.cs:47` —
  `_faceConductivity` объявлен `readonly`; `:320` — выделяется по `_openFaceCount`; `:333` —
  заполняется снимком `fs.Conductivity` в конструкторе. Других записей в файле нет, остальные
  вхождения — чтения `:1239`, `:1669`, `:1782`. `:818-827` — `NotifyConductivityChanged` только будит
  грань («changes NO mass/bias/carry»), значения не меняет и закрытую грань отклоняет исключением.
  Накопление идёт как `acc = |Δφ|·cond + carry`, поэтому при `cond == 0` накопитель равен `carry` и
  накопить с нуля нечем. Вывод: у живого поля проводимость открытой грани неизменна до конца сессии.
- **Долг при этом настоящий:** `:838` — `bool active = _bias[i] != 0 || _faceFluxCarry[i].IsActive` —
  ненулевой остаток держит грань активной, то есть застрявший остаток означает недостижимое
  `ActiveFaces == 0` и вечную сетевую дельту. Правило нужно; оно нужно в `t-7`.
- **Существующая «проверка» ветки поддельная, сильнее чем сообщил возврат:**
  `tests/GasCoopGame.Core.Tests/Field/Voxel/G37QuantizationBandTests.cs:92-108` берёт заваренную
  комнату из одной клетки, проверяет `OpenFaceCount == 0` (грани нет вовсе), а затем ОТ РУКИ дописывает
  строку показаний `new G37BandReading(conductivity: 0, drivingDifference: BaseMass,
  accumulatedTransfer: 0, carryAtRest: 0)`. Значение `carryAtRest: 0` не измерено и измерить его там
  нечем. Комментарий рядом утверждает, что «оба факта измерены на настоящем поле». Это запрещённый
  стоячим законом владельца класс: показание, скормленное оракулу вручную, вместо поведения.
- **Вторая клауза при этом спасаема, и это отдельная находка:**
  `Assets/GasCoopGame/Core/Field/Voxel/CellGrid.cs:70` — членство в наборе открытых граней решает флаг
  `Open`, а НЕ проводимость. Значит открытая грань с `Conductivity == 0` конструируема, попадает в цикл
  течения и даёт непустую фикстуру для `:264`. Та же фикстура показывает, почему отложенная ветка
  недостижима: накопитель равен `carry`, копиться нечему.
- **Побочная находка, опасная именно для мультиплеера:** `MeaningChecksum` складывает ЖИВУЮ
  проводимость с сетки (`VoxelField.cs:1052`), а течение считает по СНИМКУ (`:1669`). Сегодня
  безвредно; `t-7` ломает ровно этот шов.
- **Граница версий:** `validation.config: synced_contract_version: 33` в WIN-U4, записка гласит «no
  reinterpretation of lower-pinned roots», надстройки v33 адресованы пинам `>= 33`.
  `os/engineering/CONTRACT_VERSION current: 34`, но `re-sync:34` установлен в WIN-U3, не в WIN-U4.
  Квитанция 05 уже несёт `lower_pin_preserved: true`. Пин 31 сохраняется законно.
- **Форма жизненного цикла:** `os/engineering/CONTOUR.md:322` — правка ЗАМОРОЖЕННОГО PLAN идёт
  заменой, не правкой на месте. Свежий корень квитанции 05 (`previous_receipt: null`) — уже
  подтверждённый на этом венью законный образец.
- **Контрактное основание дефекта:** `os/engineering/CONTOUR.md` §(d) — смешанные ряды расщепляются на
  атомарные обязательства, каждый поведенческий ряд несёт источник и негатив; и там же: рез обещанного
  требует ЗАПИСАННОГО решения владельца, а самоавторский рез — провал покрытия, не диспозиция.
- **Решение владельца:** дословный токен «B» на выбор из двух показанных вариантов, 2026-07-30, в этом
  чате.
- **Параллельная нога:** во время этой ноги приземлился `s-work-g-37a1-venue-v34-home-consume-001`
  (`a9d5ee61`). Его изменения по t-venue сохранены целиком; дельта этой ноги — только t-sim, два issue,
  строка прогноза и `updated:`.

## state_changes

1. `NOW.md open_calls`: терминальный `c-exec-g-37a1-gas-rest-amend-001` (status `running`) снят и
   заменён на `c-exec-g-37a1-gas-rest-band-scope-001`, track t-sim, for t-6, `status: ready`,
   `to: executor`, с note о подтверждённом ESCALATE, выборе владельца «B», сохранении пина 31 и запрете
   открывать PAIR-CANDIDATE до нового `ELIGIBLE_FOR_FRESH_PAIR_CANDIDATE`.
2. Новый файл `work/c-exec-g-37a1-gas-rest-band-scope-001-call.md` — самодостаточный engineering root
   CALL с абсолютными путями, пином 31, слотом WIN-U4 и девятью пунктами `done_when`.
3. `NOW.md issues`: добавлен `i-g37-cond-zero-release-deferred-to-t7-001` — отложенное обязательство
   выброса остатка при `cond == 0`, route work, `review_when` «Перед выпуском CALL по t-7».
4. `NOW.md issues`: добавлен `i-conductivity-snapshot-vs-live-checksum-001` — расхождение живой
   проводимости в контрольной сумме и снимка в течении, route work, `review_when` «Перед выпуском CALL
   по t-7».
5. `NOW.md direction_forecast.drivers`: строка t-6 переписана — снята устаревшая «14 из 16 тестов»,
   записан второй перезапуск со стадии PLAN и то, что продуктового кода по t-6 ноль.
6. `NOW.md updated:` → `s-repair-g-37a1-gas-rest-band-scope-001`.
7. `os/FRICTION.md`: одна строка о классе «ветки нормативного правила перечисляются по одной без
   проверки покрытия всей области».
8. `LOG.md`: одна строка. Эта запись сохранена в `history/`.

Не менялось: `CHARTER.md`, `TREE.md`, bet, appetite, kill_by, cut_list, lens_verdicts, все 11 задач и
их статусы, пять полос, `track_wip_limit: 5`, `recurring`, `decisions`, четыре остальных open_call
(включая t-venue дельту параллельной ноги), продуктовый репозиторий и любые замороженные объекты.

## captures

- Правило выброса остатка получило вторую ветку (маска направления) при переморозке 05 и третью
  обнаружило только сейчас: квитанция 04 несла 16 поведенческих рядов, квитанция 05 — 28. Ветки
  нормативного правила перечислялись по одной, вместо однократной проверки, что они накрывают всю
  область. Записано в FRICTION как класс; OS в этой ноге не чинился.

## decisions_needed

Нет. Единственное решение этой ноги — выбор «B» — уже получено дословно.

## play_check

- Шаг 1 «назвать противоречие»: `NOW.md` держал t-sim call как `running` с owner-approved PLAN и
  `ELIGIBLE_FOR_FRESH_PAIR_CANDIDATE`, тогда как этот PLAN невалиден на своей же стадии.
- Шаг 2 «реконструировать»: артефакты и коммиты выше перевесили и возврат, и логи; проверены
  первыми руками, включая пересчёт sha256 и построчную сверку 28 рядов.
- Шаг 3 «предложить исправленное состояние»: один причинный ряд на изменение, каждое обязательство
  сохранено — снятая ветка стала issue с триггером на t-7, а не исчезла.
- Шаг 4 «подтверждение владельцем»: два варианта с рекомендацией показаны, получен дословный токен
  «B». Removal boundary соблюдён: снятие требования с подписанного им плана держится на его словах,
  названных именно про эту ветку, а не на общем согласии.
- Шаг 5 «friction»: одна строка, OS не правился.
- G5 не применяется: нога ничего не закрывает как done. G7 соблюдён: решение подано двумя вариантами с
  рекомендацией. G9 не задет: `CHARTER`/`TREE` не менялись. G1/G2/G3 не задеты: bet, appetite и полосы
  те же.
- Владельцу показана честная поправка: обещанный в брифе «ряд-растяжка» на неизменность проводимости
  как красный тест невозможен (он был бы зелёным с самого начала и сломал бы стадию RED), поэтому
  растяжка реализована двумя issue с триггером на t-7 плюс громким отказом из `done_when` 4.

## log

2026-07-30 | s-repair-g-37a1-gas-rest-band-scope-001 | repair | t-sim | g-37a1/t-6: ESCALATE подтверждён первыми руками — правило выброса остатка объявлено полным по трём веткам, а ветка cond == 0 не покрыта ни одним из 28 рядов и её состояние недостижимо ПО ПОСТРОЕНИЮ (проводимость открытой грани readonly-снимок конструктора), поэтому «дописать тест» невозможно без ядра t-7; владелец выбрал «B», ветка передана t-7 двумя issue с триггером перед его CALL, поддельное вписанное от руки показание cond == 0 назначено к удалению и заменено измерением на открытой грани с проводимостью 0, выпущен один ready свежий PLAN-корень с сохранённым пином 31

## next

Диспетчеризовать `c-exec-g-37a1-gas-rest-band-scope-001` отдельной свежей сессией в
`C:\projects\Unity\GasCoopGame_win-u4`. Её терминальный RESULT возвращается ДОМОЙ; PAIR-CANDIDATE
открывается только после того, как квитанция исправленного PLAN снова вернёт
`ELIGIBLE_FOR_FRESH_PAIR_CANDIDATE`. Направление — единственный, кто выдаёт наряд-преемник.

END_OF_FILE: live/indie-game-development/history/2026-07-30-s-repair-g-37a1-gas-rest-band-scope-001.md
