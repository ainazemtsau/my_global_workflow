# RESULT — s-repair-g-5a7c-householder-core-extensibility-001

play: repair · противоречие названо владельцем вслух: подписанный текст ставки прямо запрещает то,
что он теперь требует как жёсткое требование, и правило, которое должно было это поймать, лежало
там, где его не читают
direction: indie-game-development · node: g-5a7c · track: householder
date: 2026-08-11

base_read: OS — `wt/indie-game-development`, рабочее дерево чисто, голова `e094488c`. Продукт
перемерен `git ls-remote origin`: `origin/dev` = `origin/main` = `2f7f2e87`, не двигался с прошлой
ноги. Наряд `c-exec-g-5a7c-react-2-001` выпущен, но НЕ доставлен — расписки нет, значит текст ещё
правится законно.

## 1. Противоречие — двумя строками

**Первое.** `bet-g-5a7c-wave-2` `## approach` дословно: «НЕ входит: подписчики, история событий,
ситуация, пережившая тик, весовое взвешивание приоритетов, **любой общий фреймворк под источники**».
Владелец 2026-08-11 дословно: «нужно закладывать сразу хорошее ядро… долговременная память,
кратковременная… хендлер на любые… никакого хардкода… только расширяя». Живое слово владельца
старше подписанного текста по авторитету (KERNEL, порядок авторитета), но текст ставки этого не
знает, и примирить их молча не имеет права ни одна нога.

**Второе, и оно объясняет первое.** Правило «Логика ядра только РАСШИРЯЕТСЯ, не переписывается»
существовало с **2026-08-02** — в улике `i-owner-standing-rules-on-how-work-is-shaped-001`. Та
карточка САМА называла свой дефект: «его настоящее место `knowledge/` (`read_by`: написание
наряда), и переезд туда делает отдельная нога». Переезд не сделали девять дней. Нога, пишущая
наряд, открывает `knowledge/`, а не улики — поэтому оба наряда 2026-08-10 написаны без правила, и
владелец обнаружил это сам.

## 2. Реконструкция — что перемерено

**Правило было и не действовало.** `i-owner-standing-rules-on-how-work-is-shaped-001` заведена
после первого нарушения, стоившего отклонённой работы
(`history/2026-08-02-s-repair-g-6b13-a2-layering-defect-001.md`). Её `route: work`,
`review_when` — «КАЖДЫЙ раз при написании наряда». Ни один наряд второй волны на неё не ссылается.

**Ядро хозяина проверено чтением байтов, а не рассуждением.** Прогнано 14 агентов на опубликованной
голове `2f7f2e87`, пять линз (память, ключ хендлера, новый источник, авторинг, хардкод), только
чтение. 30 сырых находок; восемь отобраны по силе и отданы ОТДЕЛЬНЫМ агентам с заданием
**опровергнуть** — **ни одна не отбита**. Остальные 22 попытку опровержения не проходили и уликой
не считаются; это названо честно, а не замолчано.

**Диагноз — частично.** Фундамент заложен верно и переделывать его не придётся: ядро правил не
знает про движок (поиск `UnityEngine` по `Core/Householder/` и `Core/Situations/` — ноль
попаданий), реакция это данные (`HouseholderReaction.cs:16`, `.With(...)` на `:134`), каталог
подменяем снаружи (`Householder.cs:727`), числа в ассете, распорядок в файле, шов под свою политику
объявлен (`Householder.cs:451`).

**Носителя памяти нет вовсе, и это ровно то, что владелец назвал требованием номер один.** Самое
дорогое место — `HouseholderLocalMemory`: 13 полей, позиционный конструктор `Householder.cs:913-941`
и ПЯТЬ мест сборки внутри `Step` плюс публичный конструктор. Каждое будущее его желание — шкала
безумия, счётчик мест, история, два факта сразу — проходит через те же семь правок, и цена растёт
с каждой построенной ногой. Второе по цене: политика решений физически не запускается в игре —
`AuthoritativeHouseholder.cs:234` зовёт двухаргументный `Step`, трёхаргументный вызывается только из
тестов. Полный разбор с файлами и строками — в заведённой улике.

**Объём назван честно:** долговременная память — «не задача, это отдельная волна»: у неё нет ни
структуры, ни входа наблюдений (позиции игроков в хозяина не подаются вообще), ни выхода наружу, ни
сохранения между сессиями (`NetworkWalkerCourier.cs:50` — мир создаётся заново на каждом старте).

## 3. Исправленное состояние

**Разрешение владельца на запись — его дословные слова этого дня:** «запиши где-то, нахуй, что
ядро, блядь, сразу залаживаем, чтобы оно расширялось», «выбей», «Я сказал, это жёсткое требование».
Он же прямо запретил переспрашивать: «чтобы меня больше не переспрашивал». Поэтому шаг 4 плея
исполнен его уже сказанными словами, а не новым вопросом; ничего из подписанного им — CHARTER,
карточка узла, текст ставки — этой ногой НЕ тронуто.

1. **Заведена `knowledge/core-is-extended-never-rewritten.md`** — правило 2026-08-02 дословно плюс
   его слова 2026-08-11 целиком, проверяемый ТЕСТ («чтобы добавить новое — ДОБАВИТЬ или ПРАВИТЬ?»),
   таблица из семи вещей, которые обязаны расширяться добавлением, и прямое снятие ложного
   прочтения его же словами («я не говорю, что это мы должны в этой задаче сделать»).
   `reads:` — КАЖДАЯ нога, пишущая наряд, и КАЖДАЯ нарезка, ДО первой строки.
2. **`i-owner-standing-rules-on-how-work-is-shaped-001`: блоки переписаны.** Первое правило
   переехало в knowledge и здесь больше не живёт; ВТОРОЕ (рабочая сцена — инструмент, а не
   поставка) остаётся, потому что его переезд требует ноги, у которой рабочая сцена в объёме.
   Карточка НЕ закрывается.
3. **Заведена `i-householder-core-has-no-memory-carrier-001`** (`level: objective`, `route: review`)
   — семь мест с файлами и строками, что заложено верно, оценка объёма и прямое следствие для
   подписанного текста ставки. `review_when` — НЕМЕДЛЕННО, а не на ревью волны: срок назвал сам
   владелец («в конце волны я хочу как минимум ядро вот этого хозяина»).
4. **Текст наряда `c-exec-g-5a7c-react-2-001` дополнен ДО выдачи** — расписки нет, наряд не выдан,
   правка законна. Добавлена первая граница «НЕ ЗАКОЛАЧИВАТЬ ДВЕРЬ»: правило, встающее на место
   броска, не имеет права быть сформулировано как «фактов ровно один»; строить память на несколько
   фактов задача НЕ обязана — обязана не сделать её невозможной. Плюс в `return` добавлен тест
   «ДОБАВИТЬ или ПРАВИТЬ» по каждому открытому месту и прямой вопрос, ухудшил ли исполнитель форму
   памяти. **Объём задачи НЕ расширен ни на строку** — добавлено ограничение, а не работа.
5. `NOW.md`, `CHARTER.md`, `g-5a7c`, текст ставки, `t-react-2`, `t-cargo-1` и наряд дорожки
   `cargo-hands` не тронуты.
6. Одним `osctl leg close` сохранить этот RESULT и добавить log ровно один раз в журналы `g-5a7c`,
   `t-react-2`, `c-exec-g-5a7c-react-2-001` и обеих улик.

## captures

- **Волна как подписана НЕ доставляет то, что он назвал.** Ставка — семь задач при потолке восемь,
  и её `approach` запрещает часть требуемого. Долговременная память по замеру — отдельная волна.
  Это факт для пересборки дорожки, а не повод расширить `t-react-2` молча.
- **`t-react-2` уже вскрывает те самые пять мест сборки памяти.** Решение «чинить форму памяти в
  ней или отдельной задачей» дешевле принять ДО расписки исполнителя: иначе те же семь мест будут
  правиться дважды.
- **22 находки из 30 не проходили опровержения.** Они существуют в отчёте прогона и могут быть
  подняты пересборкой; уликой этой ноги они не являются.
- Порог смерти (5) считает ноги на общую задачу и первые задачи обеих дорожек: потрачено 4,
  закрытие `t-cargo-1` станет пятым. Эта нога вне счёта.

## decisions_needed

Нет, и это его прямое требование: «чтобы меня больше не переспрашивал». Что делать с волной, он уже
сказал — «в конце волны я хочу как минимум ядро вот этого хозяина»; исполнение этого требует
пересборки дорожки `householder`, и она открывается следующей ногой, а не вопросом.

## play_check

- 1 назвать противоречие: done — два противоречия, оба в двух строках
- 2 реконструкция: done — от карточки правила 2026-08-02 до байтов на `2f7f2e87`; артефакты и
  коммиты поставлены выше журналов, 22 непроверенные находки названы как непроверенные
- 3 предложить исправленное состояние: done — четыре записи, по одной строке причины у каждой
- 4 подтверждение (владелец): исполнено ЕГО УЖЕ СКАЗАННЫМИ словами этого дня, процитированными
  выше; подписанное им не тронуто, границы удаления не пересечены
- 5 friction: done — одна строка в `os/FRICTION.md`, OS этой ногой не чинится

## Гейты

- **G1.** Дорожки не тронуты, корней не заведено.
- **G2.** Всё записанное служит активной ставке.
- **G3.** Аппетит не тронут: объём `t-react-2` не расширен, добавлена только граница.
- **G7.** Продуктовое и геймплейное решение принято ВЛАДЕЛЬЦЕМ и записано его словами; нога не
  выбрала за него ни одной механики и не спроектировала архитектуру — только измерила состояние.
- **G9.** CHARTER, карточка узла и текст ставки не редактировались. Противоречие с текстом ставки
  НАЗВАНО и отдано пересборке, а не примирено молча.
- **Граница удаления `repair`.** Ничего из перечисленного в ней не удалено и не отставлено: улика
  правила осталась открытой со своим вторым правилом, ни одна запись `knowledge/` не тронута, ни
  один открытый наряд не закрыт.

## log

жёсткое требование владельца записано туда, где его читают, и ядро хозяина перемерено против него: правило «логика ядра только РАСШИРЯЕТСЯ» существовало с 2 августа, но лежало в улике, которая сама называла свой дефект — «настоящее место knowledge, переезд делает отдельная нога», — и переезд не делали девять дней, поэтому оба наряда 10 августа написаны без правила и владелец нашёл это сам; переезд сделан, к правилу добавлены его слова 11 августа целиком и проверяемый тест «чтобы добавить новое — ДОБАВИТЬ или ПРАВИТЬ»; ядро проверено 14 агентами в пять линз только чтением байтов на 2f7f2e87, 30 сырых находок, восемь отданы отдельным агентам на ОПРОВЕРЖЕНИЕ и ни одна не отбита, 22 остались непроверенными и названы такими; диагноз частичный и честный — фундамент заложен верно и переделке не подлежит (ядро правил не знает про движок, реакция это данные, каталог подменяем, числа в ассете, распорядок в файле), а носителя памяти нет вовсе: самое дорогое место HouseholderLocalMemory на 13 полей с семью местами правки на каждое новое поле, политика решений физически не запускается в игре и зовётся только из тестов, хендлер на сочетание невыразим одним скалярным ключом, а долговременная память по замеру не задача, а отдельная волна; текст ставки прямо запрещает часть требуемого и это названо, а не примирено молча; наряд t-react-2 дополнен ДО выдачи одной границей «не заколачивать дверь» без единой новой строки работы

## next

1. **Пересборка дорожки `householder` против записанного требования** — ближайшая нога. Она решает,
   что из ядра волна берёт, что не помещается, и чинится ли форма памяти внутри `t-react-2` или
   отдельной задачей. Это `shape`/`review`, потому что трогает подписанный текст ставки.
2. **Решение по форме памяти принимается ДО расписки исполнителя** на `t-react-2` — иначе те же
   семь мест правятся дважды.
3. Наряд `t-react-2` лежит готовым и с новой границей; выдача не блокирована.
4. Дорожка `cargo-hands` по-прежнему ждёт его рук на `t-cargo-1`.
