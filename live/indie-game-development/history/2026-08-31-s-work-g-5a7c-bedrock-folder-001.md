# RESULT — s-work-g-5a7c-bedrock-folder-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-bedrock-marks-gauges-1, полоса `закрепление`
track: закрепление
date: 2026-08-31
outcome_kind: boundary clarification — целевой каталог и его folder-meta признаны законной частью чистого переноса по прецеденту, а не по разрешению

owner_approved: |
  Владельца не спрашивали: создание каталога внутри уже закреплённой сборки — механическое
  следствие переноса, а не выбор, который он увидит в игре. Ни одна строка `done_when` не изменена.

outcome: |
  **ИСПОЛНИТЕЛЬ WIN-U3 ОСТАНОВИЛСЯ ДО ПЕРВОЙ ПРОДУКТОВОЙ МУТАЦИИ.** Он заметил, что на `c7e926cf`
  нет ни `Assets/TunnelCrew/Core/Householder/`, ни его меты, и что буквальное «`git mv` и больше
  ничего» тогда невыполнимо: диф будет четыре rename плюс один новый folder-meta. Это второй за
  сутки случай, когда СТОП сработал как задуман, и оба раза исполнитель пришёл с точным вопросом,
  а не с обходом.

  **ВОПРОС ЗАКРЫТ НЕ РАЗРЕШЕНИЕМ, А ПРЕЦЕДЕНТОМ, И ЭТО ВАЖНЕЕ.** В закреплённой сборке уже лежит
  `Assets/TunnelCrew/Core/House.meta`: предыдущая нога границы
  (`c-exec-g-5a7c-bedrock-boundary-1-001`) создала точно такую же папку с точно такой же метой,
  перенося три файла описания комнаты. То есть форма «новая подпапка + её meta» — не отступление
  от чистого переноса, а уже принятая форма этого самого конвейера. Наряд её просто не назвал.

  **НАЙДЕНА ЛОВУШКА, О КОТОРОЙ ОН НЕ СПРАШИВАЛ.** `TunnelCrew.Core.asmdef` лежит в КОРНЕ
  `Assets/TunnelCrew/Core/` и покрывает подпапки. Второй `.asmdef` в новой папке завёл бы третью
  сборку и тихо разрезал закреплённое надвое. Запрет добавлен в журнал наряда явно.

  **ВОЗРАСТ ПЕРЕМЕРЕН НАПРАВЛЕНИЕМ НЕЗАВИСИМО, И ОН СИЛЬНЕЕ ЗАЯВЛЕННОГО.** Он написал «одна
  ненулевая правка, 15 полных суток». Замер направления: оба файла **СОЗДАНЫ** коммитом `3f499016`
  от 2026-08-15 06:12:38 +0300 (`+207/-0` и `+310/-0`) и **с тех пор не тронуты ни разу** — 16 суток
  по счёту дней. Это не «одна правка», а рождены и забыты: сильнейший возможный сигнал возраста.
  Расхождение 15/16 — округление полных суток против календарных, коммит и метка времени одни и те
  же.

evidence: |
  Продуктовый `main` `c7e926cf`:
  - `git ls-tree -r --name-only main -- Assets/TunnelCrew/Core/` → восемь путей: `House.meta`,
    три `.cs` в `House/` с метами, `TunnelCrew.Core.asmdef` с метой. Каталога `Householder` нет —
    его утверждение верно.
  - **`Assets/TunnelCrew/Core/House.meta` существует** — прецедент folder-meta в этой же сборке,
    созданный предыдущей ногой границы.
  - `TunnelCrew.Core.asmdef` — в корне `Core/`, не в подпапке; подпапки покрыты им автоматически.
  - Возраст, `git log --follow -M --numstat` с отбрасыванием нулевых: по одной ненулевой записи на
    файл, обе — коммит `3f499016`, 2026-08-15 06:12:38 +0300, `+207/-0` и `+310/-0`.

state_changes: |
  ДОПИСАНО В ЖУРНАЛ (1):
  - `c-exec-g-5a7c-bedrock-marks-gauges-1-001` — разрешение на каталог и его мету со ссылкой на
    прецедент `Core/House.meta`; ЯВНЫЙ ЗАПРЕТ заводить второй `.asmdef` в новой папке;
    независимый замер возраста с коммитом и датой.

  НЕ ИЗМЕНЕНО: ни одна строка `done_when`, ни границы соседних ног, ни текст наряда.

captures: |
  - Наряд назвал приёмку «`git mv` и больше ничего», не проверив, существует ли целевой каталог.
    Это дефект наряда, а не исполнителя: направление писало его от замера содержимого файлов и не
    посмотрело на дерево назначения. Следующий наряд переноса обязан называть, существует ли
    целевая папка, и считать её мету заранее.
  - Его строка про следующего кандидата (`Probe/House/WalkingBody.cs`, 4 суток) меряет только
    возраст. У кандидата две оси, и вторая — изолированность: замер направления от 2026-08-31
    показал, что `Probe/House` целиком не отрезается, восемь её файлов тянут ещё четыре. Возврат
    обязан назвать обе оси, иначе следующий выбор повторит ошибку с `Movement`.

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — вход: СТОП исполнителя WIN-U3 до первой мутации.
  2. WORK — дерево назначения прочитано; найден прецедент `Core/House.meta`, который закрывает
     вопрос без всякого разрешения; найдена ловушка второго `.asmdef`; возраст перемерен независимо.
  3. (owner) — не требуется, механическое следствие переноса.
  4. CLOSE — RESULT финальным сообщением, применение и коммит после него.

log: |
  каталог закреплённого и его folder-meta признаны законной частью чистого переноса по прецеденту
  Core/House.meta, а не по разрешению; отдельно запрещён второй asmdef в новой папке; возраст
  перемерен независимо — файлы созданы 15 августа и не тронуты ни разу

next: |
  Домой. WIN-U3 продолжает: четыре `git mv`, обе проверки `CS0246`, полные тесты.
  Возврат обязан назвать следующего кандидата по ДВУМ осям — возраст и изолированность.
