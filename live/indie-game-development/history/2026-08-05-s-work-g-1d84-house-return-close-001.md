# RESULT — s-work-g-1d84-house-return-close-001

call: c-exec-g-1d84-three-rooms-by-hand-001
direction: indie-game-development
track: дом
play: work
node/task: g-1d84 / t-house-1
date: 2026-08-05
owner_launch: «Так закрой ее и давай посмотрим что разблокируется» — 2026-08-05.
close: light — because три из четырёх строк `done_when` эта нога вывела первой рукой из байтов
`b7ae1fa4` (геометрия комнат и проёмов посчитана из координат стен, а не принята на слово; заготовка
и её guid; сборщик и число корней), а четвёртую закрывает УЖЕ ЗАКОММИЧЕННОЕ дословное слово владельца
об этом самом артефакте, лежащее в `docs/results/c-exec-g-1d84-three-rooms-by-hand-001.md`
§Manual-acceptance на том же коммите. Суждения, которого никто не выносил, не осталось ни одного.

## outcome

`t-house-1` закрыта. Работа физически была в `main` и принята владельцем ещё раньше — не хватало
ровно домашнего шага: прочитать отчёт, сверить его с условием задачи построчно, записать хвосты и
снять блокировки. Полоса «дом» открылась: четыре задачи перешли в `ready`.

Вход — не Direction-RESULT, а продуктовый отчёт исполнителя плюс Control-addendum. Принят как УЛИКА
и перемерен целиком.

## evidence

**Публикация.** `9ffad2a6` (`add handmade three-room house`) → `3c1e6f38` (слияние `5f94728c` в
`dev`) → `819684fc` (`mark three-room handback delivered on dev`); все три — предки `b7ae1fa4`.
`WIN-U2` в реестре `AVAILABLE`, `lease: none`.

**Строка 1 — «не меньше трёх комнат, соединённых проходами». ПОСЧИТАНО, А НЕ ПРИНЯТО НА СЛОВО.**
В `Assets/TunnelCrew/World/ThreeRoomHouse.prefab` ровно восемь стен, и имена говорящие: `Outer North`,
`Outer South`, `Outer West`, `Outer East`, `Divider West North`, `Divider West South`,
`Divider East North`, `Divider East South`. Координаты:

| стена | центр | размер |
|---|---|---|
| Outer North / South | z = ±7.25 | 14.5 × 1.5 × 0.5 |
| Outer West / East | x = ∓7.25 | 0.5 × 1.5 × 14 |
| Divider West North / South | x = −2.5, z = ±4 | 0.5 × 1.5 × 6 |
| Divider East North / South | x = +2.5, z = ±4 | 0.5 × 1.5 × 6 |

Перегородка при x = −2.5 длиной 6 с центром z = 4 занимает z ∈ [1; 7]; парная с центром z = −4
занимает z ∈ [−7; −1]. Между ними ПРОЁМ шириной 2 (z ∈ [−1; 1]). При x = +2.5 зеркально то же.
Отсюда ТРИ комнаты — запад x ∈ [−7; −2.5], середина [−2.5; 2.5], восток [2.5; 7] — и ДВА прохода.
Условие «не меньше трёх» выполнено точно, и это следует из чисел, а не из формулировки отчёта.

**Строка 2 — «собран руками в своей заготовке».** Заготовка существует, guid
`3ddac42b85f64bb688681e93d398e573`; восемь стен заданы явными числами, генератора нет ни одного.
Каждая стена несёт `World/HouseWall.cs`, чей `WorldBounds => _collider.bounds` объявлен И физической
формой, И данными пространства ходьбы — то есть правда о стене одна, а не две.

**Строка 3 — «попадает в интегрированную сцену одним объектом».** `Network/CarryStand.cs`, `Awake()`,
строки 53-54: `Instantiate(_settings.HousePrefab, transform, false)`, корню даётся имя заготовки,
стены читаются через `GetComponentsInChildren<HouseWall>()`; `OnDestroy()` убирает за собой. Ссылка
сходится поимённо: `NetworkPlaySettings.asset` — `_housePrefab: {fileID: 100000, guid:
3ddac42b85f64bb688681e93d398e573, type: 3}`, тот же guid, что в `.prefab.meta`. ОДИН объект.

**Строка 4 — «владелец прошёл по трём комнатам».** Его дословное слово, уже закоммиченное:
«Ну тогда все окей. Можно считать, что задача закрыта.» ЧЕСТНАЯ ОГОВОРКА, которую ставит сам отчёт:
отдельного рассказа о полном проходе по комнатам он не давал, и отчёт прямо отказался подменять это
выдуманной квитанцией. Закрытие стоит на его вердикте О ЗАДАЧЕ, и это записано как есть.

**Ворота.** Перепрогнаны этой же датой на том же `b7ae1fa4` ногой
`s-work-g-1d84-first-person-carry-close-001`: build 0/0, tests 63/63, `tools/check.ps1` green.
Повторно не гонялись — коммит тот же самый.

**Чего в доказательствах НЕТ.** Unity-стороны от агента нет и здесь: MCP при работе над домом
подключался (`mcp_endpoint` был записан), но эта нога Unity не открывала. Класс прежний —
`i-unity-evidence-rests-on-owner-launch-001`.

## captures

- Отчёт по дому отмечает, что обновление MCP/NuGet/UPM владельца было сохранено вне кандидата,
  потому что его несла полоса `WIN-U1`. Проверять нечего: `WIN-U1` свободен и чист, а публикация
  первой сцены прошла. Записью не становится.

## state_changes

- `NOW.md`: `tasks[t-house-1].status` → `done`, `launch_receipt` заменён на `checkpoint` с построчным
  разбором четырёх строк; `t-house-2`, `t-house-3`, `t-house-4`, `t-house-6` → `ready` + `unblocked`
  (нарядов нет ни на одну); `t-house-5` остаётся за `t-house-4`; `updated` → эта сессия.
- `NOW.md/open_calls`: удалить только вернувшийся `c-exec-g-1d84-three-rooms-by-hand-001`. Наряды
  `c-exec-g-1d84-host-into-one-scene-001`, `c-exec-g-1d84-host-under-the-judge-001`,
  `c-exec-g-1d84-free-kits-probe-001` сохранить как есть.
- `NOW.md/issues`: `i-house-work-finished-in-slot-not-returned-001` → `closed_2026-08-05` +
  `closed_by`; завести `i-house-root-runtime-only-not-serialized-001` и
  `i-walkspace-probe-tunneling-on-large-step-001`.
- Препендить ровно один LOG receipt и сохранить этот полный RESULT в `history/`.
- CHARTER/TREE/knowledge, продуктовый репозиторий, реестр слотов и ветки не менять.

## decisions_needed

Нет.

## play_check

- Шаг 1 «сверить наряд» — исполнен: `t-house-1`, полоса «дом», активная ставка `g-1d84`.
- Шаг 2 «вход от владельца» — нового входа не требовалось: его вердикт по этому артефакту уже
  закоммичен, а сегодняшнее «Так закрой ее» — запуск закрытия, а не новое суждение.
- Шаг 3 «работа» — работа сделана исполнителем и Control-ногой; эта приняла, перемерила и закрыла.
- Шаг 4 «сверка с done_when» — построчная, четыре из четырёх, каждая с названным источником.
- Шаг 5 «закрытие» — light close, обоснование в заголовке. Ставку не закрывает: `review` по `g-1d84`
  не наступил, впереди `t-scene-2` и точка пересмотра аппетита.
- G5: венью — light close по правилу плея. G1: полос по-прежнему пять, WIP-предел 3 не поднят; четыре
  разблокированные задачи `ready` дispatch-ем не являются — нарядов на них нет.
- G7: ни одного продуктового или геймплейного решения за владельца не принято. Оба хвоста отчёта
  оставлены ВОПРОСАМИ к будущим нарядам, а не закрыты решением ноги.

## next

Полоса «дом» открыта: следующая по порядку — `t-house-2` (место, где груз не проходит просто так,
order 5), затем `t-house-3` (обстановка, order 6) и `t-house-4` (дом как данные, order 9). Нарядов
нет ни на одну; их пишет отдельная нога, и первым же вопросом в любом из них стоит
`i-house-root-runtime-only-not-serialized-001` — хватает ли рантайм-формы дома для авторинга.
Готовый к выдаче наряд у направления сейчас ровно один: `c-exec-g-1d84-host-into-one-scene-001`.

END_OF_FILE: live/indie-game-development/history/2026-08-05-s-work-g-1d84-house-return-close-001.md
