# RESULT — s-work-g-5a7c-bench-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-bench-by-fact-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: work — подготовка полигона выдана в WIN-U1 его словом; нога отдаёт владельцу карту, а не протокол, и обязана прочитать отчёт прибора живым запуском

owner_approved: |
  Его слово 2026-08-31, дословно: **«я хочу, ну, прям чёткую инструкцию: что тестировать, как…
  именно прогон в полигоне — это надо в отдельном слоте сделать, чтобы, ну, типа я получал прям
  инструкции, что как мне делать, что смотреть, что не смотреть»**.

  Ни одна строка `done_when` не изменена.

outcome: |
  **ЗАДАЧА, РАДИ КОТОРОЙ ВОЛНА И ОТКРЫВАЛАСЬ, НАКОНЕЦ ВЫДАНА.** Её приёмка целиком его глаз, и
  разблокировалась она час назад починкой зрения. За весь день он не коснулся ничего, и это
  измеримая цена того, как волна была нарезана.

  **НОГА ОТДАЁТ КАРТУ, А НЕ ПРОТОКОЛ, И ЭТО НАПИСАНО ЗАПРЕТОМ.** Его правило действует:
  **ноль требований к его поведению.** Гейтить можно сборку, но не его. Поэтому наряд запрещает
  «пройдите шаги 1–7» и «убедитесь, что», и требует три части его языком: что здесь живое, что
  заведомо врёт, где лежит отчёт.

  **ГЛАВНАЯ СТРОКА ПРИЁМКИ — ЖИВОЙ ОТЧЁТ, А НЕ СОБРАННЫЙ.** Сегодня нога «почему» честно
  призналась, что её фрагмент `why` реконструирован форматтером из тестового ростера, потому что
  MCP не смог вызвать закрытый писатель. Признание — образцовое; но **живого экрана за весь день
  не видел никто**, и первый живой запуск обязан дать настоящий файл. Наряд прямо запрещает
  суррогат и делает его СТОПом.

  **ВЛАДЕЛЕЦ ЗАРАНЕЕ ПРЕДУПРЕЖДЁН О ЛГУЩЕЙ СТРОКЕ.** Прибор печатает в консоль `why: HE SEES ---`,
  а в файле лежит `HE SEES 2 OF 14 THINGS`. Без этого предупреждения первый же взгляд дал бы
  «прибор мёртв», и следующая нога чинила бы не то.

  **НАЗВАНО, ЧЕГО ОН НЕ УВИДИТ.** Утро (идёт в WIN-U4), перенос вещи на место, слышимость по
  расстоянию. Иначе отсутствие будет принято за поломку — класс, на котором волна уже спотыкалась.

evidence: |
  Перемерено направлением на `main` = `dev` = `origin/main` = **`064ab568`**:
  зрение даёт `2 of 14` при дальности 6.5 м и секторе 100°; причины отказа семи обработчиков в
  `main`; headless 739/739 прогнан направлением лично.

  Слоты: `WIN-U1`, `WIN-U2`, `WIN-U3` свободны; `WIN-U4` держит ногу утра. Выбран `WIN-U1` —
  он держит контекст прибора и зрения с сегодняшних ног.

  Непересечение: нога читает продукт и запускает полигон, продуктового кода не пишет вовсе;
  WIN-U4 работает в профиле, тестах утра и, возможно, одной строке `Householder.cs`.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-bench-by-fact-1-001` → слот **WIN-U1**, база `064ab568`;
  - `work/2026-08-31-call-bench-by-fact-1.md`.

  АКТИВИРОВАНО (1):
  - `t-bench-by-fact-1` → `active`.

captures: |
  - **ЕГО ПРАВИЛО «НОЛЬ ТРЕБОВАНИЙ К ТОМУ, КАК Я ИГРАЮ» ВПЕРВЫЕ ПРИМЕНЕНО К НАРЯДУ, А НЕ К
    ПРИЁМКЕ.** Он попросил «инструкцию, что мне делать», и соблазн был написать протокол. Правило
    говорит обратное: инструкция для него — это карта местности, а не маршрут. Разница
    существенная: карта переживёт любой его способ играть, протокол сломается на первом же шаге в
    сторону.

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — его прямая просьба про отдельный слот и инструкцию.
  2. WORK — состояние `main` перемерено; известные враньё и пробелы собраны в наряд заранее;
     непересечение с идущей ногой проверено по файлам.
  3. (owner) — его слово получено до выдачи и процитировано дословно.
  4. CLOSE — RESULT, применение, коммит; наряд уходит в WIN-U1.

log: |
  подготовка полигона выдана в WIN-U1 его словом: нога открывает полигон и отдаёт владельцу карту
  местности, а не протокол — его правило «ноль требований к тому, как я играю» применено к самому
  наряду; главная строка приёмки требует живого отчёта прибора, суррогат сделан СТОПом

next: |
  Идут две ноги: утро в WIN-U4, подготовка полигона в WIN-U1. Свободны WIN-U2 и WIN-U3.

  В волне остаётся `t-carry-back-1` — перенос вещи на место, разблокирован и не выдан.
