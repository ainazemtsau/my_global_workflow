# RESULT — s-converge-verify-g-1d84-001

direction: indie-game-development
play: converge-verify
node: g-1d84
call: c-converge-verify-g-1d84-001
date: 2026-08-04
owner_present: no

---

## outcome

`verify_target: build` · `complete=FAIL` · `smuggling=FAIL`.

Поверхность g-1d84 НЕ прошла первую свежую проверку. Семь строк возвращены в converge-arch; проверка
гоняется заново отдельной сессией после неё. Это ПЕРВЫЙ FAIL узла — проверено первой рукой, что в
`history/` нет ни одной прежней `converge-verify` по g-1d84, значит потолок второго FAIL не действует
и третий круг его словом не открывается.

Проверка не ответила ни на один вопрос, не починила ни одной строки, не тронула §WHAT, §CONTRACTS и
оба §SIGNOFF и не вынесла владельцу ни одного нового решения.

**Сначала честно о том, что файл выдержал, потому что это не формальность.** Двадцать пять
цитированных источников перепроверены первой рукой — строки `knowledge/**` 14, 39-42, 51, 53, 56, 74;
словарь хозяина 29 и 37-38; лобби 20-21; `CHARTER.md` 91-97 и 99-109; все десять пунктов утверждённого
базиса; `TREE.md:40-46`. Разошедшихся нет ни одной. Все выборочные `факт кода` перепроверены на
объявленной базе, и сама база подтверждена первой рукой (`main` = `origin/main` = `origin/dev` =
`839df47e`, дерево чистое) — расхождений ноль. Счёт файла пересчитан механически и точен: 107 строк,
83 `answered`, 26 `acceptance`, 2 `улика`; `open_rows: 22` = 21 `open` плюс один `deferred` (W75),
разбивка 9+11+1+1 сходится. Сила каждого прослеженного токена владельца записана НЕ ВЫШЕ настоящей:
В1 условное, В2 по ссылке, П2 с хеджем, П3 по доверию, одно слово на три карточки. Завышенных нет —
единственное найденное расхождение (F7) ошибается в безопасную сторону.

**Две дыры полноты.** F1 — «сервисы» из критерия 8 не доходит ни до одной строки: слово встречается
в обоих файлах НОЛЬ раз, ряда словаря у него нет, и его нет даже в честном списке §COVERAGE «о чём
карточка ни разу не спрашивает». F2 — «по комнатам» из цели и «настоящий дом» из «зачем» не доходят
ни до одной строки: ни одна строка не требует от дома больше одной комнаты или какой-либо обстановки,
при том что неснимаемый минимум устава прямо говорит «дом не декорация» и что хозяин находит команду
по задетой обстановке, а §CONTRACTS C17 уже на это опирается.

**Четыре строки стоят на рассуждении решавшей ноги.** F3 — «невозможно по устройству движка» верно
для префаба-АССЕТА и не покрывает экземпляр в сцене, то есть не покрывает случай, который нога назвала
сама; сторож при этом уже снят на этом основании, а подпись П2 стоит на устройстве, а не на
доказательстве. F4 — довод «схема сразу работает проверялкой» не разрешается на объявленной базе:
проверялки схемы в проекте нет никакой, и подпись П3 дана ПО ДОВЕРИЮ к рекомендации. F5 — «девять
строк закрыты» стоит против `open_rows: 22` и против `arch_in_context_only: PASS` в том же файле:
пять строк объявлены закрытыми документом, который сам себя назвал необязывающим, а их отвечающий
израсходован без преемника. F6 — основание, по которому выбрано чтение слова «инструментов»,
опровергается собственной строкой W86a того же файла.

## evidence

**База и её подтверждение.** `C:/projects/Unity/GasCoopGame`: `git rev-parse` даёт `839df47e` на
`main`, `origin/main` и `origin/dev`, `git status --porcelain` пуст. Объявленная база верна.

**Перепроверенные `факт кода` (все сошлись, расхождений ноль).**
`NetworkPlaySettings.asset` — `_cargoLength: 2.4`, `_cargoWidth: 0.5`, `_cargoHeight: 0.5`,
`_cargoMaxHolders: 2`, `_standHalfExtent: 7` (W16, W20, W66);
`AuthoritativeCargoRoster.cs:246` — «A cargo takes as many hands as the settings allow. Nothing here
knows the number two» (W20);
`PlayerState.cs:3-14` — только `X` и `Z` (W32);
`Householder.Step(localMemory, input, decisionPolicy)` на 827 и 837 — ни ходоков, ни груза, ни
положений игроков (W72);
`Householder.cs:407` — `IHouseholderDecisionPolicy.Evaluate`; `:430` — `switch (fact.Name)`;
`:861` — `switch (input.PendingEvent.Name)`; `:895-898` — исключение на одновременные разные реакции;
`:14` — `enum HouseholderPriority` (A5, A7, W40, W42);
`Assets/TunnelCrew/**` — ноль `FindObjectOfType` / `FindObjectsOfType` / `GameObject.Find` /
`Resources.Load` / `FindAnyObjectByType` / `FindFirstObjectByType` (A4 вариант в);
`TunnelCrew.Core.asmdef` — `noEngineReferences: true` (A2 довод 3);
`Packages/manifest.json` — `com.unity.nuget.newtonsoft-json` 3.2.1, `com.unity.ai.navigation` 2.0.12
(A2 довод 1, M2);
`Assets/Plugins/Sirenix/` — Odin на месте (A11);
`EmitBoardCreak` / `EmitQuietRustle` / `EmitRepeatedHeavyNoise` вызываются ТОЛЬКО из
`HouseholderRouteControllerEditor.cs:175,181,187` (M3);
`HouseholderRouteController.cs:101-132` — `Awake` превращает `Transform[]` в `HouseholderPoint[]`
(A1 вариант в);
`IWalkSpacePort` объявлен в `Core/Multiplayer/WalkSpacePort.cs:21`, реализован `WalkSpaceProbe`,
подан параметром в `CarryWorld` (A4 форма интерфейса);
`NetworkWalkers.unity:549` — `_enablePrediction: 0`, предсказания на клиенте нет (M4);
`Assets/TunnelCrew/Scenes/` — ровно четыре сцены, папки `Assets/TunnelCrew/Art/` не существует
(W12, W83); `EditorBuildSettings.asset` — `Lobby.unity` входит (W02), `HostWalksHisDay.unity` не
входит (W91).

**Перепроверенные цитаты (все сошлись).**
`how-the-game-is-built-layers.md:14` «каждое правило игры обязано уметь работать ТОЛЬКО у судьи»;
`:39-42` отзывчивость через локальный показ и тихую поправку; `:51` «Только моё | камера, интерфейс»;
`:53` «Граница проводится одним вопросом: влияет ли это на то, что произойдёт в игре»; `:56` «кто это
считает — настройка, а не свойство кода»; `:74` рабочие сцены как работа владельца.
`host-shared-vocabulary.md:29` «возвращение к жизни: занятие И шаг внутри него, плюс „сейчас
прерывать безопасно“»; `:37-38` «ЧЕГО ЭТА ФРАЗА НЕ ТРЕБУЕТ: планировщика, взвешивания полезности,
обучения, готового фреймворка поведения».
`lobby-is-not-a-crutch.md:20-21` «Сетевая задача — НУЖНО ЛОББИ».
`TREE.md:40-41` утверждённый `dropped_when` — «двое на разных копиях несут один общий физический
груз, рассчитанный создателем игры; хозяин показал три наблюдённые реакции через один расширяемый
шов» (I16, I17 подпёрты).
Утверждённый базис — п.4 дословно несёт «достаточно двух примерных профилей и пяти различимых
реакций» (W44); п.6 несёт триггер additive «размера, времени загрузки или независимого жизненного
цикла» (W07) и слово «сервисы»; п.8 озаглавлен «Арт и инструменты», говорит «visual/toolkit spike» и
называет Blender (F6).

**Механический пересчёт файла.** §WHAT: 107 строк, 83 с меткой `answered`, 26 с `acceptance`,
2 `улика`, 21 `open` плюс W75 `deferred` = заявленные 22. Все три числа §SIGNOFF и разбивка
9 + 11 + 1 + 1 верны.

**Полнота по атомам.** Двенадцать критериев, цель, «зачем», строка риска и строка исключений разбиты
атомарно и сопоставлены со строками §WHAT и §CONTRACTS. Все тринадцать пунктов строки исключений
доходят до W95. Клаузулы, доходящие только через §CONTRACTS, названы явно: «замечает» из цели и
«хозяин видит игрока» из критерия 7 доходят через C17; «строит свежий путь» — через C13. Не дошло
ДВУХ: «сервисы» (крит. 8) и «по комнатам» / «настоящий дом» (цель и «зачем»).

**Предметы, о которых карточка не спрашивает ни разу — названо этой ногой, не решавшими.** «Сервисы»
(критерий 8, слово стоит в утверждённом тексте и не разобрано нигде) и «обстановка» — G14 сам
определяет дом как пространство «с комнатами, обстановкой и метками», C17 требует «задетую
обстановку» источником замеченных фактов, а требования на её существование нет ни одного, при том
что `CHARTER.md:92` держит её в неснимаемом минимуме. Ни то, ни другое в честный список §COVERAGE не
попало.

**Чек-лист класса узла.** В `knowledge/` чек-листа решений для этого класса нет — стандарта эта нога
не заводила. Применён один существующий канон, `compute-a-law-before-freezing-it.md` (принято
2026-07-31, `status: current`), и только в F3, где он подходит дословно.

**Граница гарантии, честно.** Процитированные открытия структурно исключены из атаки; проверка била
по неподпёртым допущениям. Это строгость, а не закрытое доказательство.

## state_changes

1. `work/c-converge-arch-g-1d84-002-call.md` — создан: наряд с семью возвращёнными строками, списком
   того, что переоткрывать запрещено, и границей «владельцу решений не выносить без его слов».
2. `work/converge-g-1d84.md` — добавлен раздел `§VERIFY-001` перед трейлером: вердикт, таблица семи
   находок, список пройденного и честная граница гарантии. §WHAT, §CONTRACTS и оба §SIGNOFF не
   тронуты ни одной строкой.
3. `NOW.md` — `open_calls`: возвращающийся `c-converge-verify-g-1d84-001` снят, открыт
   `c-converge-arch-g-1d84-002` со статусом `ready`; `updated` переписан на эту ногу. Ставка, задачи,
   полосы и решения не тронуты — их не было и не появилось.
4. `LOG.md` — добавлена строка ноги первой в списке.
5. `history/2026-08-04-s-converge-verify-g-1d84-001.md` — этот RESULT.

## captures

- Ни один из 21 ряда §GLOSSARY не закрывает слово «сервисы», хотя оно читается минимум тремя
  способами. Кандидат в канон после того, как converge-arch его определит, — не раньше.
- «Вложенность мест ≠ проходимость» (A10) и «описание дома — одна названная вещь с несколькими
  производителями» (A1) выглядят кросс-узловым знанием, а не свойством этой волны. Промотирует
  review или pulse, не проверка.
- Чек-листа решений для класса «интегрированный кусок игры» в `knowledge/` нет. Если он появится,
  писать его должна review или pulse: чек-лист, написанный проверяющей ногой, стандартом не является.

## decisions_needed

Нет. Ниже потолка второго FAIL проверка маршрутизирует находки и не решает ничего; новых вопросов
владельцу эта нога не открывает. Две находки (F4 цена лицензии, F6 чтение слова «инструментов») в
итоге могут потребовать его слова — их приносит converge-arch нормальным разбором с вариантами и
рекомендацией, и открывает только его собственный ответ.

## play_check

- `verify_target: build complete=FAIL smuggling=FAIL`
- Шаг 1 — заявка прочитана: атака на полноту и атака на источник обе названы, цель `build`
  объявлена. Рассуждения решавших ног как источник не читались: источником считалось только то, на
  что они ссылаются.
- Шаг 2 — полнота: двенадцать критериев, цель, «зачем», риск и исключения разбиты атомарно;
  не дошли до строки два куска (F1, F2), названы и НЕ заполнены. Предмет, о котором карточка не
  спрашивает ни разу, назван — «сервисы», плюс «обстановка».
- Шаг 3 — источник: для несущих строк и всех `acceptance` названы поддерживающие факты и
  затребована разрешимая ссылка. Четыре строки (F3, F4, F5, F6) стоят на рассуждении решавшей ноги;
  для каждой лекарство — ПОНИЖЕНИЕ до `open` с названным отвечающим, свежих ответов не дано ни
  одного. Утёкшая величина по существу не опровергалась.
- Шаг 4 — закрытие: FAIL, названные строки возвращены в converge-arch, проверка гоняется заново.
  Первый FAIL узла подтверждён по `history/`; потолок не действует. §SIGNOFF о прохождении НЕ
  написан. Shape не открыт.
- Границы наряда соблюдены: ничего не отвечено и не починено; §WHAT, §CONTRACTS, CHARTER и TREE не
  тронуты; свой чек-лист класса узла стандартом не объявлен; ставка, задачи и полосы не созданы;
  владельцу не вынесено ни одного нового решения.

## log

2026-08-04 | s-converge-verify-g-1d84-001 | converge-verify | direction | g-1d84: первая свежая
проверка вернула complete=FAIL smuggling=FAIL — две дыры полноты («сервисы» критерия 8 и содержание
дома за целью и «зачем») и четыре строки, чей единственный источник рассуждение решавшей ноги,
включая доказательство отсутствия скрытых ссылок и довод про схему как проверялку; двадцать пять
источников и все выборочные факты кода на 839df47e сошлись без расхождений, счёт файла точен, токены
владельца не завышены; семь строк возвращены в converge-arch, ставка, задачи и полосы не созданы.

## next

Свежая сессия по `c-converge-arch-g-1d84-002`. После неё — новая `converge-verify` по g-1d84,
отдельной сессией от неё. Нарезка не открывается, пока проверка не пройдена.

END_OF_FILE: live/indie-game-development/history/2026-08-04-s-converge-verify-g-1d84-001.md
