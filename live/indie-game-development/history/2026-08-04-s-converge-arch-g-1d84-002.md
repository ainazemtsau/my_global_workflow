# RESULT — s-converge-arch-g-1d84-002

direction: indie-game-development
play: converge-arch
node: g-1d84
call: work/c-converge-arch-g-1d84-002-call.md
date: 2026-08-04
owner_present: да (поправка про «сервисы» и разрешение продолжать даны им в чате этой ноги)

## outcome

Семь строк, возвращённых первой `converge-verify` с вердиктом `complete=FAIL smuggling=FAIL`,
отработаны все семь. Ни одна строка, которую проверка прошла, не переоткрыта; §GLOSSARY, §IMPORT и
ряды §CONTRACTS C01-C21 не тронуты ни одной правкой; ни один вердикт владельца не выдуман.

Главная находка ноги пришла от самого владельца и сняла первую дыру целиком: **слово «сервисы»
оказалось артефактом голосового ввода, а не термином.** Его слова: «Сервисы, это я обычно, когда пишу
сообщение, я не пишу, я использую голосовой ввод. Это что-то не так, значит, распознало. Сервисы не
надо искать. Это ошибка, это что-то не так распозналось», и отдельно — «нужно почистить от этого
слова». Это источник ВНЕ ноги, и он закрывает F1 полностью: значение не изобретается, ряд словаря не
заводится, канон не предлагается. Чистка самого текста — правка `TREE.md`, и её делает map.

Вторая честная цена ноги: **пять строк, которые прежняя арх-нога объявила закрытыми своим решением,
понижены до `open` с названными преемниками, а «arch_open: 0» оказался неверным и стал шестёркой.**
Одно несущее утверждение (доказательство отсутствия скрытых ссылок конструкцией) вынесено в измеримый
опыт вместо спора, и измерить его с диска НЕ удалось — венью Unity, и она у владельца.

## evidence

**Измерено первой рукой** (`C:/projects/Unity/GasCoopGame`, `HEAD` = `origin/main` = `origin/dev` =
`839df47e`, `git status --porcelain` пуст, только чтение):

- В `Assets/**` пять сцен. Настоящих блоков `PrefabInstance` ровно ДВА: `Assets/TunnelCrew/Scenes/Lobby.unity:1512`
  и `Assets/TunnelCrew/Scenes/NetworkWalkers.unity:167`; оба — экземпляры одного
  `Network/Prefabs/NetworkRuntime.prefab` (`guid: 969c59a50d21a71468e57ffc3cc2647b`), оба стоят
  корнями сцены.
- В этих двух блоках 22 записи `m_Modifications` (имя плюс трансформ). У ВСЕХ 22 стоит
  `objectReference: {fileID: 0}`. Непустых `objectReference` во всём `Assets/**` — ноль, включая 29
  файлов `.prefab`. `stripped` не встречается ни разу.
- Методическая оговорка, чтобы счёт не соврал: наивный греп по `PrefabInstance` даёт десятки
  совпадений, но почти все — поле `m_PrefabInstance: {fileID: 0}` у каждого GameObject, а не заголовок
  документа.
- **Вывод: слот под ссылку в файле сцены СУЩЕСТВУЕТ и отделён от поля `value`; заполненного случая в
  проекте НЕТ. Диск не подтверждает и не опровергает утверждение — вопрос требует опыта в Unity.**

**Цены, добытые первой рукой с сайтов поставщиков и из `.nuspec` пакетов 2026-08-04:**

- Json.NET Schema: Indie **$129**, Business **$199** (обязателен при >10 сотрудников), Site **$1299**;
  бессрочно, без роялти; коммерческая лицензия прямо разрешает везти runtime-компонент внутри своего
  продукта. Бесплатный тир — **AGPL-3.0** плюс рантайм-лимит 1000 проверок в час; для закрытой платной
  Steam-игры непригоден ЛИЦЕНЗИЕЙ, а не лимитом. Заявленный драфт — 6.
- Бесплатные существуют и не один: **NJsonSchema 10.9.0** (MIT, работает на уже стоящем Newtonsoft,
  System.Text.Json не тянет; линия без релизов с мая 2023, собрана против Newtonsoft 9.0.1 при наших
  13.0.2); **LateApexEarlySpeed 3.1.0** (BSD-3, ровно `netstandard2.1`); **JsonSchema.Net 8.0.5**
  (последняя MIT — с 9.0.0 бинарник переведён на платный OSMF EULA при доходе от $10 000).
- Отдельно: устаревший `Newtonsoft.Json.Schema` физически лежит ВНУТРИ уже установленной
  `Newtonsoft.Json.dll` — ноль установки, но `[Obsolete]` и старый драфт.
- Ручная проверялка поверх обычного Newtonsoft: **1,5-3 дня** на первую версию, дальше **0,5-2 часа**
  на каждое новое поле. Со схемой: 1-2 дня и 15-30 минут на поле.
- **Смысловой разрыв, не чинящийся выбором библиотеки:** «неизвестная способность» статической схемой
  не выражается вовсе — набор способностей известен только в рантайме, потому что критерий 4 требует,
  чтобы новые способности приходили КОДОМ через названный шов.

**Проверено в состоянии первой рукой:**

- Слово «сервисы»/«services» в игровом смысле не определено и не перечислено НИГДЕ: ни в `knowledge/**`,
  ни в `CHARTER.md`, ни в `os/**`, ни в продуктовом репозитории. Цепочка прослежена до конца:
  `TREE.md:64` ← `work/c-map-next-integrated-house-wave-001-call.md:25` ←
  `history/2026-08-04-s-day-next-wave-basis-map-launch-001.md:25` ← незаписанный ход живого чата.
  Ровно это и подтвердил владелец своими словами.
- F7: `history/2026-08-04-s-converge-g-1d84-integrated-house-slice-001.md:29` говорит «Слово на набор
  в целом: „фиксирую“»; то же слово несут `LOG.md:5` и `work/c-converge-arch-g-1d84-001-call.md:21`.
  §SIGNOFF говорил «ожидается» и занижал токен — поправлено, нового слова владельца не требовалось.
- F6: утверждённый базис п.8 озаглавлен «Арт **и инструменты**», говорит «проводится visual/**toolkit**
  spike» и называет Blender для модульной оболочки дома, тоннелей и hero props. Основание, которым A8
  отклонила чтение (б), опровергается строкой W86a того же файла.
- F2: `CHARTER.md:92` — «Дом при этом не декорация: несущие задевают обстановку и попадают в то, что
  хозяин расставил, — и именно по этому он их находит». §CONTRACTS C17 уже опирается на «задетую
  обстановку», существование которой не требует ни один критерий.
- M5: `knowledge/how-the-game-is-built-layers.md:48` кладёт «прокопанное» в ряд «влияет на игру, копии
  обязаны совпадать | только судья | по сети летит результат»; `:86` делает необходимость менять
  курьера СТОПОМ и вопросом владельцу.

**Веерная работа этой ноги.** Четыре независимых читателя параллельно: источник слова «сервисы» по
всему состоянию и продукту; измерение файлов сцен и префабов; strategic_search по проверялкам схем с
опровергнутой таблицей; miner-заход «какой архитектурный вопрос не задан». Это внутрисессионный
предпроход, а не биндинговая проверка: G5 идёт отдельной свежей сессией по наряду
`c-converge-verify-g-1d84-002`.

## state_changes

1. `work/converge-g-1d84.md` — §WHAT: W05 и W09 переведены в `answered` подписями владельца (П2+A3 и
   П1); W06 понижена в `open → PLAN`; W31, W40, W61, W105 понижены в `open → PLAN/ADR`; W52 возвращена
   в `open → владелец`; W80 понижена в `open → владелец`. Добавлены W06a (опыт), W52a (цены, `улика`),
   W106 («сервисы» — снято его словом), W107 (содержание дома → map). §COVERAGE обновлён построчно.
   §SIGNOFF: слово на набор — «фиксирую» (F7), счёт пересчитан. §CONTRACTS «Счёт этой ноги» заменён
   построчной таблицей понижений. Добавлен §ARCH-002.
2. `work/converge-g-1d84-arch.md` — поправки A2 (цены и опровержение довода 2), A4 (измерение и снятие
   строки о ненужном стороже), A8 (опровергнутое основание, понижение). Добавлен §MINER-002 (M5, M6,
   M7). §SIGNOFF обновлён: `arch_open: 0 → 6`, `miner_gaps_named: 4 → 7`.
3. `NOW.md` — `open_call` `c-converge-arch-g-1d84-002` закрыт, открыт `c-converge-verify-g-1d84-002`;
   заведены две записи `decisions` (форма файла профилей, чтение слова «инструментов») со статусом
   `waiting_owner`; заведена запись `i-card-text-owes-a-map-leg-001`; уточнена
   `i-architecture-pass-skipped-for-this-bet-001`.
4. `work/c-converge-verify-g-1d84-002-call.md` — новый наряд.
5. `LOG.md`, `history/2026-08-04-s-converge-arch-g-1d84-002.md`.

## captures

- Средний вариант формы профиля — JSON, published-схема как ДОКУМЕНТ для ассистента, проверки руками
  без библиотеки-валидатора — на прежнюю развилку не выносился вообще. Записан как третий вариант в
  решении владельцу, но сам факт «развилка была неполной» стоит помнить отдельно.
- `NetworkSessionService` — единственная вещь в продукте, названная «Service», и она сидит своим
  корнем сцены. Совпадение со снятым словом случайное, значением его считать запрещено его же словом.

## decisions_needed

1. **W52 — форма файла профилей.** Варианты, цены и разрыв описаны в `NOW.md` `decisions`
   `d-profile-file-format-recommendation-002`.
2. **W80 — что значит «инструментов» в критерии 10.** `d-toolkit-reading-of-criterion-10-002`.

Обе строки стоят `open` с ним как отвечающим; ни одна не решена ногой.

## play_check

- `contract_coverage`: не пересчитывался и не мог измениться — ряды §CONTRACTS C01-C21 не тронуты;
  добавлена одна строка о том, что опора C17 на «задетую обстановку» теперь имеет названную строку W107.
- `arch_open`: **6** (W06, W52, W80, M5, M6, M7) — у каждой названный отвечающий. Прежний `0` был
  неверен и поправлен.
- `arch_in_context_only`: **PASS** — ни один выбор арх-файла не попал в `done_when` узла; проверено
  заново после правок.
- `owner_verdicts_invented`: **0**. Единственные новые слова владельца в этой ноге — про «сервисы», и
  они процитированы дословно.
- `reopened_passed_rows`: **0**.
- Ставка не открыта, задачи и полосы не созданы, разработка не запущена, `TREE.md` не тронут.

## log

g-1d84: семь возвращённых строк отработаны — «сервисы» снято его собственными словами как ошибка
голосового ввода и уходит чисткой карточки в map, содержание дома открыто строкой к map, доказательство
отсутствия скрытых ссылок понижено и вынесено в измеримый опыт с венью Unity у владельца, довод «схема
сразу работает проверялкой» опровергнут и цены названы, пять строк понижены построчно с названными
преемниками и два счёта файла сведены к одному, чтение слова «инструментов» понижено к владельцу,
§SIGNOFF поправлен на «фиксирую»; найдено три новых незаданных архитектурных вопроса.

## next

`converge-verify` по наряду `work/c-converge-verify-g-1d84-002-call.md` — отдельной свежей сессией.
Потолок второго FAIL действует: при повторном FAIL ветку выбирает владелец своими словами.
Параллельно, независимо от проверки, задолжали одну `map`-ногу по `i-card-text-owes-a-map-leg-001`.

END_OF_FILE: live/indie-game-development/history/2026-08-04-s-converge-arch-g-1d84-002.md
