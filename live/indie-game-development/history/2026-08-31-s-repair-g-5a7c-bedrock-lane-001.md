# RESULT — s-repair-g-5a7c-bedrock-lane-001

direction: indie-game-development
play: repair
node/task: g-5a7c / bet-g-5a7c-wave-9 — объявление полосы `закрепление` и её первая нога
track: закрепление
date: 2026-08-31
outcome_kind: repair — полоса объявлена его словом и записана туда, где её увидит следующая нога; первый кандидат на закрепление заменён по замеру

owner_approved: |
  Полоса объявлена его словом, дословно и целиком:

  > **«объявляю пятую полосу «закрепление», идёт вне волны, слот U3»**

  Больше в его словах ничего нет, и приписывать нельзя. Правило «по одному переносу за раз» —
  НАПРАВЛЕНИЯ, живёт в `knowledge/bedrock-promotion-runs-beside-the-wave.md`; в карточке ставки это
  отмечено отдельной строкой, чтобы через неделю его не прочли как его требование.

outcome: |
  **ПЕРВЫЙ КАНДИДАТ НА ЗАКРЕПЛЕНИЕ ЗАМЕНЁН, И ЭТО ГЛАВНОЕ В НОГЕ.**

  И сводка для нарезки, и знание направления называли `Probe/Movement` «единственным сегодня
  полностью отрезаемым модулем» — пять файлов, 186 строк, замыкание равно ему самому. Отрезаемый —
  верно. **Изолированный — нет.**

  Перемерено: его типы торчат наружу в `Network/NetworkHouseholder.cs` и
  `Network/NetworkWalkerCourier.cs` — **файлы ноги прибора, которая идёт прямо сейчас**, — плюс
  `World/StartConditions.cs` и четыре EditMode-сборки тестов. Из семнадцати сборок, ссылающихся на
  `TunnelCrew.Probe`, `TunnelCrew.Core` есть только у семи. Значит перенос Movement потребовал бы
  дописать `Core` минимум трём тестовым сборкам — то есть тронуть граф сборок под тремя ногами в
  полёте, ровно в тот момент, когда гейт, который поймал бы сломанный граф, сам перестраивается
  третьей из них.

  **ВЗЯТЫ `HouseholderMarks` И `HouseholderGauges`, И ОНИ ЧИЩЕ, ЧЕМ ОЖИДАЛОСЬ.** Проверено чтением
  байтов: у обоих **только `using System;`**, каждый упомянутый тип объявлен внутри них самих, а
  пространство имён — **уже `TunnelCrew.Core`**. Потребители лежат внутри `Probe/Householder/**` и в
  headless-наборе; `TunnelCrew.Probe` уже ссылается на `TunnelCrew.Core`, headless-проект видит обе
  сборки. **Перенос — это `git mv` и больше ничего: ноль правок исходников, ноль правок asmdef.**

  Наряд делает из этого не пожелание, а строку приёмки: если правка всё-таки понадобится — СТОП и
  вопрос домой, потому что тогда неверен замер, и это важнее переноса.

  **ЗАЧЕМ ПОЛОСА ЗАПИСАНА В КАРТОЧКУ ВОЛНЫ, ХОТЯ ОН СКАЗАЛ «ВНЕ ВОЛНЫ».** Не чтобы приписать её
  ставке — прямо оговорено, что она цели волны не служит. А потому, что полоса, которой нет в
  карточке, невидима рабочему набору следующей ноги: ровно так `t-bedrock-boundary-1` выпала из
  него и простояла незамеченной, пока ремонт не полез разбираться. Это записано в карточке причиной,
  а не подразумевается.

evidence: |
  **ЗАМЕРЫ, СНЯТЫЕ ЭТОЙ НОГОЙ НА `main` `c7e926cf`:**

  - `git show main:Assets/TunnelCrew/Probe/Householder/HouseholderMarks.cs` и
    `HouseholderGauges.cs` — в обоих ровно одна строка `using`: `using System;`;
    `namespace TunnelCrew.Core`; объявляемые типы — `HouseholderMarkScope`, `HouseholderMark`,
    `HouseholderMarks`, `HouseholderGaugeDefinition`, `HouseholderGaugeCatalog`, `HouseholderGauges`;
  - потребители вне `Probe/Householder/`: только шесть файлов
    `tests/TunnelCrew.Core.Tests/Householder*Tests.cs`;
  - **Movement:** объявляет `MovementInput`, `PlayerState`, `PlanarYaw`, `PlayerMovement`;
    `PlanarYaw` используется в `Network/HouseholderSnapshot.cs`,
    `Network/NetworkHouseholder.cs`, `Network/NetworkWalkerCourier.cs`, `Probe/Cargo/CargoGeometry.cs`,
    `Probe/Multiplayer/AuthoritativeWalkerRoster.cs` и в EditMode-тестах; `PlayerState` —
    в `World/StartConditions.cs` и четырёх EditMode-сборках;
  - **граф сборок:** 17 `.asmdef` ссылаются на `TunnelCrew.Probe`; `TunnelCrew.Core` есть у семи
    (`HouseBuilding.EditorTests`, `Editor`, `Networking`, `Probe`, `Profiles`, `HouseBuilding`,
    `World`). У остальных десяти — нет.

  **СОСТОЯНИЕ СЛОТОВ:** WIN-U3 свободен, чекаут чист, HEAD `c7e926cf`, отставание 0.
  Занято: U1 (прибор), U2 (хук прибытия), U4 (гейт).

state_changes: |
  СОЗДАНО (2 карточки + 1 документ):
  - `t-bedrock-marks-gauges-1` — задача, `track: закрепление`, `_bet: g-5a7c`, `status: active`;
  - `c-exec-g-5a7c-bedrock-marks-gauges-1-001` — наряд, слот WIN-U3, база `c7e926cf`;
  - `work/2026-08-31-call-bedrock-marks-gauges-1.md`.

  ПЕРЕПИСАНО (1 блок):
  - `bet-g-5a7c-wave-9.tracks` — добавлена пятая полоса `закрепление` его дословным словом, с
    прямой оговоркой, что она цели волны не служит, с причиной, почему всё-таки записана здесь, и
    с отдельной строкой о том, что правило «по одному переносу за раз» принадлежит направлению,
    а не ему.

  ПОЛЕ `_bet` У ЗАДАЧИ ПРОСТАВЛЕНО НАМЕРЕННО, ХОТЯ ОНА «ВНЕ ВОЛНЫ»: без него карточка выпадает из
  рабочего набора `osctl context`, и это ровно тот дефект, который сегодня утром был записан уликой
  `i-live-cards-point-at-closed-and-empty-bets-001`.

captures: |
  - Утверждение «`Probe/Movement` — единственный полностью отрезаемый модуль» живёт в
    `knowledge/bedrock-promotion-runs-beside-the-wave.md`, и оно верно по букве, но вводит в
    заблуждение по существу: отрезаемость и изолированность — разные свойства, и для выбора
    кандидата решает второе. Запись не правил: она не ложна, а неполна, и правка знания — работа
    `review`, а не ремонта. Следующая нога закрепления должна это увидеть в своём же возврате.

decisions_needed: |
  Ни одного. Слот, полоса и предмет названы им; кандидат выбран по замеру.

play_check: |
  1. Назвать противоречие — полоса объявлена его словом, но не существовала ни в одной карточке;
     а названный кандидат на закрепление не выдерживал собственного критерия изолированности.
  2. Восстановить новейшим-первым — байты обоих кандидатов и граф сборок прочитаны заново,
     не взяты из знания и не из сводки.
  3. Предложить исправленное состояние — показано ему с числами до записи: 17 сборок против семи,
     и почему Movement задел бы идущую ногу прибора.
  4. Подтверждение (owner) — его дословное «объявляю пятую полосу «закрепление», идёт вне волны,
     слот U3». До этих слов не записано ничего.
  5. Дыра в OS → FRICTION, ниже. Это ВТОРАЯ запись того же класса за сутки.

log: |
  пятая полоса закрепления объявлена его словом и записана в карточку волны, чтобы не выпасть из
  рабочего набора; первый кандидат заменён по замеру — Movement не изолирован и задел бы идущую
  ногу прибора, взяты метки и шкалы, которым нужен только git mv

next: |
  Домой. Четыре ноги идут параллельно: прибор U1, хук прибытия U2, закрепление U3, гейт U4.
  Возвраты приходят по одному.

  Первый перенос обязан вернуть не только диф, но и то, обо что споткнулся: это половина его
  ценности, и следующий кандидат выбирается по его замеру, а не по сегодняшнему знанию.

FRICTION: |
  2026-08-31 · **ВТОРАЯ ЗАПИСЬ ТОГО ЖЕ КЛАССА ЗА СУТКИ: правку открытой ставки не пишет ни один
  плей.** Первая — `s-repair-g-5a7c-wave-9-amend-001`, где под `repair` заводилась задача. Здесь
  под `repair` объявляется ПОЛОСА, хотя ядро §3 говорит прямо: «Only `shape` declares a lane… No
  other play creates or removes one», а предусловие `shape` — «no active bet», то есть он
  недоступен ровно тогда, когда полоса нужна. Владелец объявил полосу своим словом, как и требует
  правило; исполнить его законным маршрутом невозможно.

  **Порог KERNEL §7 пройден: две совпадающие записи FRICTION открывают маршрут на изменение OS.**
  Предмет для MAINTENANCE — не «разрешить repair всё», а назвать плей или ход, которым живая ставка
  принимает поправку владельца: и задачу, и полосу, и аппетит.
