# RESULT — s-work-g-5a7c-vert-1-call-001

play: work
direction: indie-game-development · node: g-5a7c · task: t-vert-1 · call: c-work-g-5a7c-vert-1-call-001
date: 2026-08-09
base_read: Direction `183abd15ea43e8d6890eac05042c8141d35ee908` = `origin/main`, дерево чисто. Продукт `C:/projects/Unity/GasCoopGame_win-u1`: `HEAD` = `slot/win-u1` = `main` = `dev` = `origin/main` = `origin/dev` = `11f8ddbf1f11722c5bfa25e3104c3847e45d7b04`; рабочее дерево чисто, `main...slot/win-u1` = `0 0`, `git log slot/win-u1 ^main` пуст. Продуктовые байты, ветки и slot-state этой ногой НЕ изменялись.

## outcome

Готов к отправке новый engineering-наряд `c-exec-g-5a7c-player-vertical-probe-001`: в режиме
ПРОБЫ он даёт владельцу одновременно попробовать прыжок и полёт на двух клавишах, подняться на
одноразовый примитивный ящик и вернуться на пол. Наряд не выбирает способ за владельца: терминальный
продуктовый возврат обязан принести его точные слова — `прыжок`, `полёт`, `оба` или `ни то ни
другое`.

Наряд пинит `engineering_contract: 36` и прямо выбранный владельцем `WIN-U1`. Новый ввод подъёма
обязан расширить существующий вход ходока через тот же `SubmitInputServerRpc`, но хранение высоты,
форма правила и форма ответа остаются HOW исполнителя. Высоту считает хост; обе копии, камера и
опора на ящик следуют полученному авторитетному результату.

## evidence

- **Свежая власть Direction.** Перед работой прочитаны `os/KERNEL.md`, `os/plays/work.md`,
  `live/indie-game-development/NOW.md`, все открытые карточки направления и полный входящий бриф.
  После появления параллельного upstream-коммита выполнен повторный `git pull --ff-only`; свежая
  вершина `183abd15` прочитана, текущие целевые карточки перечитаны непосредственно перед записью.
- **Слот и база.** Штатный `.\pwsh.cmd tools/select-slot.ps1 -Slot WIN-U1` вернул
  `CLEAN / AVAILABLE / lease none`, ветку `slot/win-u1`, HEAD `11f8ddbf…`, endpoint
  `unrecorded`. Все пять продуктовых refs (`main`, `dev`, `origin/main`, `origin/dev`, локальная
  slot-ветка) равны `11f8ddbf…`; чужих коммитов поверх `main` нет. Строка статуса `ahead 69`
  относится только к отставшему `origin/slot/win-u1`, а не к работе впереди опубликованной базы.
- **Пин.** `os/engineering/CONTRACT_VERSION` = 36; `validation.config` продукта несёт
  `synced_contract_version: 36`. Поэтому новый root законно идёт как ПРОБА; слова владельца,
  включающего ОПОРУ, нет.
- **Фактический вход.** На `11f8ddbf` `NetworkWalkerCourier` уже собирает
  `axisX, axisZ, yaw, pitch, carryToggle` в одном `SubmitInputServerRpc`, а
  `AuthoritativeWalkerRoster` хранит `Pitch`. Старое основание «pitch только на клиенте» ложное и
  в новый наряд не перенесено.
- **Фактическая высота.** `PlayerState` и `WalkerSnapshot` по-прежнему несут только плоское
  положение; presentation ставит Y = 0, камера использует абсолютный `EyeHeight`.
  `NetworkPlaySettings.asset` несёт `walkerHeight: 0.16`, а `cargoRestHeight: 0.02` и
  `cargoCarryHeight: 0.092` относятся только к ПОКАЗУ груза. `CargoBody` сохраняет Y-lock и
  выключенную гравитацию. `CarryStand`/`WalkSpaceProbe` описывают стены только по X/Z и не имеют
  верха.

### Проверка происхождения трёх строк `done_when`

1. Два способа на двух клавишах, примитивный ящик 4–5 ростов, возврат на пол и финальное слово
   владельца происходят из действующей карточки `t-vert-1`; `history/2026-08-08-s-repair-g-5a7c-wave-reshape-001.md`
   хранит его слова «я согласен с твоим решением» про порядок и пробу с двумя клавишами; текущий
   CALL повторяет четыре допустимых ответа владельца.
2. Хостовая высота, одинаковый показ двух копий и камера происходят из `t-vert-1` и критерия 2
   `g-5a7c`. Обязательное расширение того же ServerRpc — прямое указание владельца в текущем CALL,
   подтверждённое свежими байтами продукта.
3. Зелёные build/hygiene/Unity evidence и сохранение границ происходят из repo-contract v36 и
   прямых границ текущего CALL: груз (`t-scale-4`), число рук (`t-scale-6`), два шва (`t-scale-7`),
   дом/облик/шум/W16 и стены без верха сюда не входят.

Из действующей `t-vert-1` не снята ни одна строка. **Сняты и вслух названы две старые строки формы:**
«третье число живёт в состоянии игрока, в шаге и в снимке» и «порт свободного места отвечает в
трёх координатах». `history/2026-08-08-s-work-g-5a7c-scale-3-call-001.md` выводит обе из `W19/W21`
— замера ЦЕНЫ вертикали, а не требования. Новый наряд оставляет исполнителю форму state/step/snapshot
и порта и требует только проверяемый исход. Не импортированы также общий путь между этажами и
перелёт стен: действующая задача ограничена одноразовым ящиком, а у стен нет верха.

## state_changes

- Создать полный артефакт `work/c-exec-g-5a7c-player-vertical-probe-001-call.md`.
- Зарегистрировать `c-exec-g-5a7c-player-vertical-probe-001` как `call`, `ready`, `to: executor`,
  `for: t-vert-1`, `repo: ainazemtsau/GasCoopGame`, `engineering_contract: 36`, с указателем на
  новый work-артефакт.
- Закрыть возвращающийся `c-work-g-5a7c-vert-1-call-001` как исполненный этим RESULT.
- `t-vert-1` остаётся `open`; `g-5a7c` остаётся `active`; `NOW.bet` не меняется; node/task-критерии
  не редактируются. Продуктовый репозиторий и машинное состояние WIN-U1 не менять.

## captures

Нет. Стены без верха, вертикаль груза, потолок рук, два шва и W16 уже имеют владельцев/границы и не
становятся новой работой этой ноги.

## decisions_needed

Нет. Следующее решение намеренно принимает владелец только после живого прогона двух способов.

## play_check

- 1 recite: done — цель сверена с `t-vert-1` и второй позицией активной ставки `g-5a7c`.
- 2 owner inputs (owner): done — его текущие точные слова: «работа запускается в слоте WIN-U1» и
  «дать ему попробовать оба на двух клавишах, а не выбрать за него». Нового вопроса до артефакта
  не требовалось.
- 3 do the work: done — наряд ПРОБЫ подготовлен на свежей базе, с первым atomic claim, точной
  границей Unity/MCP STOP и тремя строками `done_when`.
- 4 self-check: done — слот, refs, contract stamp, ключевые product blobs и происхождение каждой
  строки перемерены; две старые строки формы сняты по именам и основанию.
- 5 close: done — входящий CALL закрывается, новый executor CALL регистрируется ready; задача
  остаётся открытой до слова владельца после прогона.

## log

наряд ПРОБЫ вертикали написан — WIN-U1 и база 11f8ddbf перемерены, три строки стоят на источниках, старые требования к форме state/step/snapshot сняты как замер стоимости

## next

`CALL c-exec-g-5a7c-player-vertical-probe-001` — ready для executor в WIN-U1. Продуктовый handback,
коммит, merge или push задачу Direction НЕ закрывает: отдельная Direction-нога читает точные слова
владельца после прогона и только ими закрывает или продолжает `t-vert-1`.

END_OF_FILE: live/indie-game-development/history/2026-08-09-s-work-g-5a7c-vert-1-call-001.md
