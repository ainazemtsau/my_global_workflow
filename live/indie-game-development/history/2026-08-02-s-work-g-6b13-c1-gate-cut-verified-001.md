# RESULT — s-work-g-6b13-c1-gate-cut-verified-001

play: work
direction: indie-game-development
node: g-6b13
task: c-1
date: 2026-08-02

## outcome

Возврат по `c-1` перепроверен первой рукой в свежем чате, отдельном от сборки. Цифры подтвердились,
границы соблюдены, ослаблений втихую не найдено. Задача НЕ закрыта: не хватает одного файла —
собственного закрывающего отчёта ноги, без которого `-Deliver` красный и ветка не сливается.
Отдельно найден долг, которого в возврате не было.

## evidence

Коммит `1f016999`, ветка `slot/win-u3`, проверен в `C:/projects/Unity/GasCoopGame_win-u3`.
Семь файлов, 233 вставки / 858 удалений.

**Каждая заявленная цифра проверена самостоятельно:**

| Заявлено | Проверено | Итог |
|---|---|---|
| `check.ps1` 854 → 224 строки | `git show main:tools/check.ps1` = 854 строки; текущий = 224 | подтверждено |
| 32 скрипта / 74 вызова → 16 / 22 | поиск даёт 16 скриптов, 23 вызова | подтверждено; расхождение на один вызов значения не имеет |
| 210 с → 6,414 с медиана | СВОЙ замер: 9,19 с по часам вместе с холодным стартом pwsh, `OK: all active gates green`, exit 0 | подтверждено; их 6,4 с — время внутри процесса, мои 9,2 с — по часам снаружи. Порядок ускорения реален |
| `Assets/TunnelCrew/**` не затронут | в диффе нет ни одного файла оттуда | подтверждено |
| Constitution #1/#6 сняты | было шесть правил, стало четыре; сетевое правило переписано на host-authoritative | подтверждено |

**Качество выреза — сильное, и это стоит назвать:**

- `int-overflow-scan` ОСТАВЛЕН, и его шапка честно переписана: он больше не объявляет себя гарантией
  межпроцессорного счёта, а обоснован тремя реальными P1 с тихой порчей данных. Это ровно правило
  наряда — «что ловит и случалось ли это»;
- старая спека `openspec/specs/sim-core/spec.md` НЕ стёрта, а переобластена: требования помечены как
  удержанные легаси и историческое значение контракта, а не текущая проводка. Запись сохранена
  вместо переписывания истории;
- вырезанные скрипты НЕ УДАЛЕНЫ — все файлы на месте, отключена только проводка. Обратное включение
  стоит одну строку.

**ЧЕГО В ВОЗВРАТЕ НЕ БЫЛО, найдено проверкой:**

1. **Пятеро сторожей, делающих зелёное значащим, отключены** — `negative-control-check`,
   `derivation-marking-check`, `closing-control-check`, `evidence-input-check`,
   `implementation-root-check`. Ни в обычном прогоне, ни под `-Deliver`. Главный из них по
   собственной шапке требует, чтобы у каждого acceptance-свойства был тест, ДОКАЗАННО краснеющий на
   неверной реализации, — то есть ровно тот класс, который наряд запрещал резать. **Сегодня это не
   дефект:** все пятеро работают по замороженным спекам, а у новой игры нет ни одной, им нечего
   проверять. **Но это долг с триггером,** и ничто в репозитории о нём не напоминает. Заведена
   запись `i-verification-guards-unwired-not-deleted-001` с точным моментом возврата.
2. **`-Deliver` красный, и причина не та, что названа в возврате.** Проверено запуском: гейт
   сообщает, что ветка меняет репозиторий, но не добавляет `docs/results/<changeId>.md`, то есть
   нога не закрыта. Исполнитель отнёс это к сериализованному WIN-CTRL-закрытию. Это неверно:
   WIN-CTRL про порядок слияния, а свой закрывающий отчёт пишет сама нога, и требование стоит в
   `## Definition of done` продуктового `AGENTS.md` — который эта же нога правила и требование
   сохранила. Пока файла нет, ветка не сливается.

## state_changes

`NOW.md`: `c-1` → `returned` с перепроверенными цифрами (не `done`); наряд
`c-exec-gate-surface-cut-001` → `returned` с продолжением в тот же слот; новая запись
`i-verification-guards-unwired-not-deleted-001`.

## captures

Нет.

## decisions_needed

Нет. Продолжение на один файл выдано владельцу готовым текстом.

## play_check

- Возврат обработан как улика: КАЖДАЯ заявленная цифра перемерена самостоятельно, включая запуск
  `check.ps1` и `check.ps1 -Deliver` своими руками.
- G5: проверка сделана в ЧАТЕ, ОТДЕЛЬНОМ от сборки, и была попыткой опровергнуть — искалось именно
  то, что наряд запрещал резать. Попытка дала результат: найдены два факта, которых в возврате не
  было. Заявленный исполнителем независимый review без P1/P2 засчитан как предпроход, не как
  связывающая проверка.
- Задача НЕ отмечена `done` при красном `-Deliver`: иначе копятся ветки, которые нельзя слить.
- Ни одного нового сторожа не предложено; отключённые записаны как долг с триггером, а не возвращены
  немедленно.

## log

вырез контура перепроверен первой рукой и подтверждён; найден долг по отключённым сторожам и красный Deliver

## next

Продолжение в `WIN-U3` на один файл: `docs/results/c-exec-gate-surface-cut-001.md`, затем
`-Deliver` зелёный. Параллельно идёт `a-2` в `WIN-U2`.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-work-g-6b13-c1-gate-cut-verified-001.md
