# RESULT — s-work-g-5a7c-place-fact-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-place-fact-1
track: householder-life
date: 2026-08-12
outcome_kind: checkpoint — наряд выдан, задача НЕ закрыта

owner_approved: |
  «пиши наряд на t-place-fact-1 и присылай добивку 002» — 2026-08-12.

outcome: |
  Наряд `c-exec-g-5a7c-place-fact-1-001` написан и выдан в **WIN-U3** от опубликованной головы
  `a7d37b3e`. Задача `blocked → open → active`. Четвёртый фронт.

  **ВЫДАЧА НАШЛА, ЧТО ЗАДАЧА ДЕШЕВЛЕ И БЕЗОПАСНЕЕ, ЧЕМ Я СЧИТАЛА ЧАС НАЗАД.**

  1. **Ядро хозяина УЖЕ держит план дома** — это привёз закрытый шов:
     `AuthoritativeHouseholder.cs:10,20,40` даёт `public HousePlan House { get; }`, а
     `Householder.cs` принимает `HousePlan house` в восьми местах. Тянуть дом заново неоткуда и
     незачем.
  2. **Поиск комнаты по точке существовал ещё ДО шва** — `HousePlan.cs:101` `TryFindRoomAt`, и его
     докстрока уже решает пограничный случай: точка ровно на общей стене отвечает комнатой,
     записанной раньше, потому что альтернатива поставила бы ходока в проёме нигде.
  3. **Значит задача живёт целиком в `Core/Householder/**`** и НЕ трогает ни `CarryWorld`, ни слой
     ситуаций. Я час назад предполагала обратное и на этом основании считала её конфликтующей с
     `t-thing-1`. Проверка это сняла.

  **ГЛАВНЫЙ РИСК ПЕРЕЕХАЛ** с добычи знания на ЦЕНУ ПОЛЯ: `t-react-mem` заводилась ровно потому,
  что новое поле факта стоило семи мест правки. Наряд требует предъявить число тронутых мест, а не
  заплатить молча.

  **В НАРЯД ВПИСАН СЕГОДНЯШНИЙ УРОК:** сверить голову `origin/main` ПЕРЕД публикацией, а не только
  перед сборкой. Дорожка тела на этом обожглась именно сегодня.

  **ОДНОВРЕМЕННО ПОПРАВЛЕНА ДОБИВКА ПО ТЕЛУ `c-exec-g-5a7c-body-1-002`, и поправок две.**
  - **Публикация из неё убрана.** Первая редакция велела опубликовать. Это неверно: задачу судит
    глаз владельца, а тело и шов ВМЕСТЕ не запускал никто — шов поменял, откуда хозяин берёт место
    распорядка, тело поменяло, как он рисуется, и в рантайме это один и тот же хозяин. Смотреть надо
    на слитое состояние, а не на то, что потом изменится. После его слова публикует Control.
  - **Базис переставлен с `4e5858b4` на `a7d37b3e`:** пока наряд лежал, голова сдвинулась ещё раз —
    опубликовалась `t-thing-1`. Ирония названа в самом наряде: это ровно та ошибка, из-за которой он
    и существует.

evidence: |
  Перемерено этой ногой на `a7d37b3e`:
  - `grep HousePlan` по `Core/Householder/` и `Core/Multiplayer/` → `AuthoritativeHouseholder.cs:10,20,40`
    и восемь мест в `Householder.cs`;
  - `Core/House/HousePlan.cs:101` — `TryFindRoomAt`;
  - `tools/select-slot.ps1 -Slot WIN-U3` → `head a7d37b3e`, `CLEAN / AVAILABLE / lease none`;
  - `dotnet test … -c Release` → **229 passed**, прогнано этой ногой;
  - `tree(a7d37b3e)` = `b6e5d50cd3fa8b9d9481743ac514d684761d8ceb`.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-place-fact-1-001-call.md` и карточка
     `c-exec-g-5a7c-place-fact-1-001` (`ready`, `slot: WIN-U3`, `basis: a7d37b3e`).
  2. `t-place-fact-1`: `open → active`, добавлен блок поправки и журнал.
  3. `work/c-exec-g-5a7c-body-1-002-call.md`: публикация убрана из `done_when` 3, базис переставлен
     на `a7d37b3e`, добавлены границы под опубликованную `t-thing-1` и идущую `t-place-fact-1`;
     карточка `c-exec-g-5a7c-body-1-002` получила тот же базис.
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-place-fact-1-dispatch-001.md`.

captures:
  - |
    ЗА ОДИН ДЕНЬ ГОЛОВА СДВИНУЛАСЬ ДВАЖДЫ, И ОБА РАЗА ЛЕЖАЩИЙ НАРЯД УСТАРЕЛ. Наряд с базисом —
    скоропортящийся продукт. Стоит принять правило: базис в лежащем наряде перепроверяется в момент
    выдачи, а не в момент написания; и наряд, пролежавший через чужую публикацию, обязан быть
    перечитан целиком, а не только по строке `basis`.

decisions_needed: []

play_check:
  - 1 recite — done. Шесть строк карточки пересказаны, обязательство дорожки названо.
  - 2 owner inputs — не требуется: задача невидима глазом, вердикта владельца не существует.
  - 3 do the work — done. `call:executor` выпущен; названный подход зафиксирован одним токеном.
  - 4 self-check — done. Шесть строк карточки свёрнуты в три строки контракта без потерь.
  - 5 close — checkpoint.
  - G1 — done. Четвёртый фронт внутри лимита четыре, объявленного его словом.
  - G7 — done. Ни одного игрового решения не принято.
  - G10 — done. RESULT финальный.

log: наряд на «место как факт» выдан в WIN-U3 от опубликованной головы a7d37b3e, и выдача сняла моё же вчерашнее допущение — ядро хозяина УЖЕ держит план дома, это привёз закрытый шов, а поиск комнаты по точке существовал ещё до него, поэтому задача живёт целиком в Core/Householder, не трогает ни CarryWorld, ни слой ситуаций и не конфликтует с предметом, как я считала часом ранее; главный риск переехал с добычи знания на ЦЕНУ ПОЛЯ, потому что t-react-mem заводилась ровно из-за того, что новое поле факта стоило семи мест правки, и наряд требует предъявить число; в наряд вписан сегодняшний урок про сверку головы ПЕРЕД публикацией; одновременно поправлена добивка по телу — из неё убрана публикация, потому что тело и шов вместе не запускал никто и смотреть владельцу надо на слитое состояние, и её базис переставлен на a7d37b3e, потому что пока она лежала, опубликовалась t-thing-1

next: |
  Четыре фронта: дом в WIN-U2, место-как-факт в WIN-U3, тело ждёт добивки в WIN-U1, и улика про
  сквозной проход груза лежит на критическом пути демо до первой задачи, которой законно трогать
  физику груза.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-place-fact-1-dispatch-001.md
