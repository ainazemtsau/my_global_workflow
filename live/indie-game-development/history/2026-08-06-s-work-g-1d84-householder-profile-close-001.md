# RESULT — s-work-g-1d84-householder-profile-close-001

play: work
direction: indie-game-development
node: g-1d84
task: t-host-3
track: хозяин
returning_call: c-work-g-1d84-householder-profile-close-001
date: 2026-08-06

**Протокольная оговорка.** Пятая нога в одном физическом чате; норма `AGENTS.md` — одна. Каждый раз
отступление делалось потому, что слово владельца звучало здесь, а незаписанная память состоянием не
является. Ни один гейт не обойдён.

## outcome

`t-host-3` ЗАКРЫТА, `close: light`, на опубликованном `main` = `dev` = `15ee4151`. Четыре строки
`done_when` из пяти разрешаются сами; пятая закрыта НЕ уликой, а явным решением владельца, и это
записано в состоянии как дырка, а не как выполнение. Разблокированы три задачи полосы `хозяин`.

## evidence

**1. Построчно, без округления.**

| Строка `done_when` | Чем закрыта |
|---|---|
| Профили — текстовые файлы в репозитории, не в сцене и не в двоичных ассетах | `15ee4151:Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`, прочитан целиком: 20 строк JSON, две строки распорядка |
| У формата есть описание, по которому ассистент правит без догадок | `15ee4151:docs/householder-profile-format.md`, прочитано целиком: 61 строка, обе таблицы, точные типы, полный список зарегистрированных имён, «No unlisted field is allowed at any level», три разобранных примера ошибки |
| Профиль задаёт распорядок и действия внутри комнаты | Его слово «шва достаточно». Закрыто УСТРОЙСТВОМ (поле `place`, отказ на неизвестном имени, привязка комнат в `t-host-2`), а не работающим примером — так и записано |
| Владелец правит профиль и видит другое поведение | Его слово «работает», сказанное про СВЕДЁННУЮ опубликованную сборку, а не про ту, что он гонял в ПРОБЕ |
| **Неверный файл отвергается с причиной, называющей место; хозяин не запускается** | **НИЧЕМ. Закрыто его решением, см. §2** |

**2. Дырка, названная ему и оставленная открытой записью.** Перемерено этой ногой на `15ee4151`:
`git grep -l HouseholderProfileLoader` даёт только сам файл и `NetworkHouseholder.cs` — тестов у
загрузчика НОЛЬ; в `HouseholderProfileTests.cs` четыре теста (порядок шагов, ожидание не ломает
реакцию, переход walk→wait в настоящей точке, ожидание съедает обычную паузу) и ни один не про
сломанный файл; поиск по `tests/` на `MissingMember|cannot start|unknown ability|unknown place` пуст.
Живого наблюдения тоже нет: владелец сказал «файл я не ломал». За строкой стоит ТОЛЬКО заявление
исполнителя. Сам продуктовый отчёт это подтверждает: он прямо пишет, что тестовый сборник для
загрузчика не заводится осознанно и диагностика оставлена ручной приёмке.

**ИСПРАВЛЕНИЕ ПРОТИВ СЕБЯ, СДЕЛАННОЕ ДО ЕГО ВЫБОРА.** Направление ранее сказало владельцу, что эту
строку покрывают тесты внутри зелёных 98. Это было НЕВЕРНО. Ошибка отозвана прямо, ему показаны три
варианта — сломать файл за тридцать секунд / завести наряд на тест загрузчика / закрыть как есть с
записанной дыркой — с рекомендацией первого. Он выбрал третий словом «в». То есть выбор сделан на
верных сведениях, а не на ошибке ноги. Дырка живёт записью
`i-broken-profile-rejection-never-observed-001` со спусковым крючком «первый кривой профиль либо
наряд `t-host-4`/`t-host-6`».

**3. Чем подкреплена сама доставка** (перемерено первой рукой этой же парой ног):
`dotnet test tests/TunnelCrew.Core.Tests -c Release` = 98/98 на сведённом состоянии;
`.\pwsh.cmd tools/check.ps1 -Deliver` = `OK: all active gates green`; `git ls-remote origin` даёт
`15ee4151` и для `refs/heads/main`, и для `refs/heads/dev`; слот `WIN-U1` — `CLEAN`, `AVAILABLE`,
`lease: none` на опубликованном tip.

**4. Маршрут закрытия.** `close: light` законен: четыре строки — байты и его дословные слова, пятая —
его дословное решение, процитированное там, где оно живёт. Свежая биндинговая проверка в отдельном
чате нашла бы ровно то же, что нашла эта нога (теста нет, глаза нет), и закрыть строку не смогла бы
тоже — вопрос был не в проверке, а в том, принимает ли владелец дырку. Он принял, зная её цену.

**5. `t-host-3` не последняя задача ставки** — открыты `t-house-6`, `t-host-2`, `t-host-7` и три
разблокированные. `review` не открывается.

## state_changes

1. `NOW.md` заголовок `updated:` → эта сессия.
2. `NOW.tasks[t-host-3]`: `status` `open` → `done`; += `closed` с построчным разбором, названной
   дыркой, его словами и исправлением против себя.
3. `NOW.tasks[t-host-4|t-host-5|t-host-6]`: `blocked` → `open`, `unblock_when` заменён на
   `unblocked`; у каждой назван обязательный вход
   `i-householder-activities-are-intentions-not-instructions-001`, у `t-host-6` дополнительно назван
   стоящий шов `IHouseholderProfileReferenceResolver.TryResolveAbility`.
4. `NOW.open_calls` − `c-work-g-1d84-householder-profile-close-001` (вернулся). Полоса `хозяин`
   остаётся законной через решение ниже.
5. `NOW.issues` += `i-broken-profile-rejection-never-observed-001` (level execution, route work).
6. `NOW.decisions` += `d-householder-next-three-tasks-shape-or-run-001` (track хозяин): идут ли три
   разблокированные задачи как написаны или сперва переписываются под его перенос; рекомендация — `shape`.
7. `LOG.md` += одна строка; этот RESULT сохранён в `history/`.

## captures

- Цена ошибки ноги измерена на живом примере: неверное «это покрыто тестами» едва не закрыло строку
  на пустоте. Дешёвая форма вперёд — прежде чем ссылаться на покрытие, назвать конкретный тест по
  имени файла и метода.

## decisions_needed

`d-householder-next-three-tasks-shape-or-run-001` — три задачи полосы `хозяин` разблокированы разом;
переписывать их под «занятие как намерение» до нарядов или нет. Рекомендация — переписать одной
короткой `shape`-ногой.

## play_check

1. **Recite** — цель и `done_when` `t-host-3` восстановлены дословно; ставка `g-1d84`, полоса
   `хозяин`; возвращающийся корень `c-work-g-1d84-householder-profile-close-001`. ✔
2. **Owner inputs (owner)** — его дословные слова этой ноги: «работает» (про сведённую опубликованную
   сборку) и «в» (закрыть, записав дырку) — сказано в ответ на три названных варианта с указанной
   ценой каждого. Ошибка Направления про покрытие тестами отозвана ДО его выбора. ✔
3. **Do the work** — перемерено покрытие загрузчика; задача закрыта; три задачи разблокированы;
   дырка заведена записью со спусковым крючком; вопрос о форме следующих трёх задач вынесен ему. ✔
4. **Self-check** — таблица §1: четыре строки закрыты уликами и его словами, пятая закрыта его
   решением и помечена как дырка. ✔
5. **Close** — `close: light`; `t-host-3` не последняя задача ставки, `review` не открывается;
   полоса `хозяин` держится решением. ✔

close: light — because четыре строки `done_when` разрешаются сами (две байтами `15ee4151`, две его
дословными словами), а пятая закрыта его явным решением «в», процитированным вместе с полной ценой,
которую ему назвали; ни одна строка не закрыта выводом ноги.

## log

t-host-3 ЗАКРЫТА светло на `main` = `15ee4151`. Четыре строки закрыты байтами и его словами; пятая —
отказ сломанного файла — не покрыта ни тестом, ни глазом и закрыта его решением, дырка оставлена
записью. Разблокированы три задачи полосы `хозяин`.

## next

Решение `d-householder-next-three-tasks-shape-or-run-001` — форма трёх разблокированных задач.
Рекомендация: одна короткая `shape`-нога под «занятие как намерение», потом наряды.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-householder-profile-close-001.md
