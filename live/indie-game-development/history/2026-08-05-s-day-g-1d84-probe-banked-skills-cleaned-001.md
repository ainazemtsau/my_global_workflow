# RESULT s-day-g-1d84-probe-banked-skills-cleaned-001 (call: owner-day-2026-08-05-save-the-probe)

direction: indie-game-development
track: внешний-вид
play: day
node/task: g-1d84 / t-look-1

## outcome

Владелец сказал «спасаем, закоммить пробу отдельно и почисти skills». Исполнено ровно это, ничего
сверх.

Проба бесплатных наборов забанкована отдельным коммитом `e9f23cef` на ветке слота `slot/win-u3` — 50
файлов, до этого не защищённых ничем. Перегенерация `.agents/skills/**` отброшена: 191 файл вернулся
к зафиксированному состоянию.

Проверка перед коммитом показала, что «почисти skills» — не косметика. Перегенератор заменил в
каждом файле прямой вызов инструмента на рецепт через `unity-mcp-cli`, то есть ровно тот сторонний
транспорт, который продуктовый `AGENTS.md` запрещает («Do not regenerate `.agents/skills/**` while
the generator emits non-direct invocation recipes»). Отброс восстановил запрет.

`t-look-1` НЕ закрыта: её `done_when` требует, чтобы владелец посмотрел на угол дома и сказал, стыдно
это или нет. Он не смотрел. Lease слота остаётся за задачей.

## evidence

- Слова владельца дословно: «спасаем, закоммить пробу отдельно и почисти skills».
- Коммит `e9f23cef` на `slot/win-u3`, родитель `094d8f48`: 50 файлов, 1575 вставок, ноль удалений.
- Состав пробы, проверенный поштучно: 4 набора в `Sources/` (Kenney Furniture Kit, KayKit Furniture
  Bits, Quaternius Ultimate House Interior, Poly Haven Laminate Floor 02), 5 собственных
  префабов-обёрток в `Prefabs/`, 1 URP-материал в `Materials/`, `AUTHORING.md` с ручным контуром
  сборки по сетке 0.5 м и шагу поворота 15°.
- Права: все четыре набора CC0 1.0. Каждый `SOURCE.md` несёт создателя, официальную страницу, дату
  получения, SHA-256 архива, взятое подмножество и то, что намеренно опущено. Три набора несут
  `License.txt` дословно; Poly Haven его не поставляет и сослан на официальную страницу лицензии.
- Сайдкары: 49 файлов, 49 пар — ни одного ассета без `.meta`.
- `.\pwsh.cmd tools/hygiene.ps1` → `hygiene OK`, код возврата 0.
- Отброс skills: `git status --porcelain -- .agents/` → 0 строк. Проверка содержания:
  `unity-mcp-cli` в `.agents/skills/ping/SKILL.md` больше не встречается (0), прежняя формулировка
  «Call the connected Unity MCP tool ... directly» на месте.
- Замер до работы: 191 изменённый `SKILL.md` и ноль изменений вне `.agents/skills/`.

## state_changes

Применено к `live/indie-game-development/NOW.md`:

1. `issues[i-slot-u3-probe-work-unbanked-001]` СНЯТА — обе её половины разрешены его словом и
   исполнением: байты закоммичены, перегенерация отброшена. Условие снятия было записано в самой
   строке («закрывается, когда байты либо закоммичены, либо по его слову выброшены»).
2. `issues[i-slot-u4-remote-tip-carries-foreign-commits-001]` перемерена: у `WIN-U3` появился СВОЙ
   неопубликованный коммит `e9f23cef`; прежнее «все четыре ahead 0» больше не верно. Добавлено
   предупреждение не потерять его — он единственный носитель пробы.
3. `decisions[d-agents-skills-belong-in-repo-001]` получила измеренный довод для его отдельного
   разбора: перегенерация ломала запрет на сторонний транспорт, а не просто шумела.

Продуктовый репозиторий, `GasCoopGame_win-u3`, ветка `slot/win-u3`: коммит `e9f23cef`.

## assumptions

«Почисти skills» прочитано как ОТБРОСИТЬ перегенерацию, а не как удалить `.agents/skills/**` из
репозитория. Основание: удаление он сам вынес в отдельный чат вопросом
`d-agents-skills-belong-in-repo-001`, и 191 файл удалять по реплике, сказанной про уборку слота,
было бы решением за него. Если он имел в виду удаление — это одна команда и отдельное слово.

## cuts

- `ProjectSettings/EditorBuildSettings.asset` в `WIN-U3` остаётся изменённым: одна строка
  (`m_UseUCBPForAssetBundles: 0` убрана) — это шум редактора Unity, к пробе отношения не имеет, и он
  его не называл. Слот из-за неё формально не «чистый».
- Ветка `slot/win-u3` не выравнивалась по `main` (отстаёт на 15) и не публиковалась: это отдельное
  решение из `i-slot-u4-remote-tip-carries-foreign-commits-001`, а перемещать ветки ординарной ноге
  запрещено.
- Откат `UnityConnectSettings` здесь не делался: он живёт на `main`, а `WIN-U3` от него отстаёт.
  Едет первым продуктовым нарядом, как записано.

## manual-acceptance

1. `cd C:\projects\Unity\GasCoopGame_win-u3 && git show --stat e9f23cef` — 50 файлов пробы.
2. `git status --porcelain` — одна строка, `EditorBuildSettings.asset`; skills чистые.
3. Открыть `Assets/TunnelCrew/Art/FreeKitsProbe/AUTHORING.md` — там ручной контур сборки.
4. Посмотреть глазами на префабы в `Prefabs/` — это и есть незакрытая половина `t-look-1`.

## next

Домой. `t-look-1` ждёт его глаза, слот остаётся claimed. Ждут отдельных чатов: судьба
`.agents/skills/**` и форма профилей хозяина.

END_OF_FILE
