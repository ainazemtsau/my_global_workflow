# RESULT — s-work-g-5a7c-no-rig-for-a-window-he-opens-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-the-bench-opens-quiet-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — обходной путь отклонён не по букве запрета, а потому что предложенный замер обходил свой же предмет; остаток ноги отдан глазу владельца, чьё это окно

close: light — because отклонение построено на разборе самого предложения: чтение приватных полей и вызов в обход кнопки перевыверены по тексту предложения и по прочитанному ранее коду окна. Машинная часть ноги засчитана по числам исполнителя, подпёртым тем, что тест про полку остался нетронутым.

owner_approved: |
  **ВОПРОС ПЕРЕДАН ВЛАДЕЛЬЦУ И ЖДЁТ ЕГО СЛОВА.** Запрет на обходной путь снимается только его
  письменным «да» — это его правило, и направление его не снимает за него.

  Предмет задачи при этом не менялся.

outcome: |
  **ИСПОЛНИТЕЛЬ СПРОСИЛ ПРАВИЛЬНО, И ОТВЕТ — НЕТ. НО ГЛАВНАЯ ПРИЧИНА НЕ В ЗАПРЕТЕ.**

  Запрет на динамический C# стоит в каждом наряде направления, и снять его может только владелец —
  вопрос ему передан. **Однако разбор самого предложения показал, что оно не измерило бы то, ради
  чего заводилось:**

  - чтение приватных полей `_scenarioAsset / _scenario / _scenarioRun == null` проверяет
    **внутренности**, а не то, что увидит человек, открывший окно;
  - вызов `RunScenario(путь)` **обходит кнопку** — а единственное, чего мы не знаем, это работает
    ли кнопка на полке. **Замер прошёл бы мимо своего предмета** и вернул зелёный отчёт о том,
    чего не проверял.

  **ЭТО ДОРОЖЕ, ЧЕМ НЕПРОВЕРЕННАЯ НОГА**, и потому отклонение опирается на существо, а не на букву.

  **КТО ПРОВЕРЯЕТ ВМЕСТО ЭТОГО — ВЛАДЕЛЕЦ, И ЭТО НЕ ОТГОВОРКА.** Это **его** окно, он открывает его
  сам: «у нас есть окно, да, которое там открывается, Householder. Там можно в коридоре шум, я
  сделал, он побежал на него». То, что исполнитель хотел построить рефлексией, у владельца занимает
  один взгляд. **Строить стенд, чтобы сымитировать человека, открывающего окно, — стенд ради
  стенда.**

  **МАШИННАЯ ЧАСТЬ НОГИ СДЕЛАНА ЦЕЛИКОМ И ЗАСЧИТАНА.** Пустой файл и его `.meta` удалены,
  `three-noises.json` на месте, изменены только операторы автозагрузки в `OnEnable`,
  **тест про полку не тронут** — это было главным условием переиздания, — `747 / 747` зелёные,
  независимое ревью без дефектов.

  **ЗАДАЧА НЕ ЗАКРЫВАЕТСЯ.** Строка приёмки, отданная его глазу, не выполнена, пока он не сказал —
  это его же правило про шаги, отданные владельцу. **Требований к тому, как он смотрит, не
  поставлено ни одного.**

  **И ОТДЕЛЬНО: ЕСЛИ ВЛАДЕЛЕЦ ВСЁ-ТАКИ СКАЖЕТ «ДА»**, замер обязан мерить **кнопку**, а не обходить
  её, и не выдавать приватные поля за доказательство. Это условие записано заранее, чтобы разрешение
  не превратилось в тот же негодный замер.

evidence: |
  Из возврата исполнителя, как улика и основание засчитанной машинной части: `default` JSON и его
  `.meta` удалены; `three-noises.json` сохранён; в `OnEnable` изменены только операторы
  автозагрузки; **тест не менялся**; `747 / 747` зелёные; независимое ревью дефектов не нашло.

  **Подпёрто ранее перевыведенным направлением:** `EveryReadySituationParsesAndIsNamedInWords`
  перебирает все файлы полки и требует непустого расписания — **раз он зелёный и не тронут, пустого
  файла на полке действительно нет**; `DrawScenario()` рисует расписание под
  `if (_scenario != null && _scenarioRun != null)`, то есть состояние «ничего не загружено»
  предусмотрено кодом окна.

  **Не проверено никем и честно оставлено открытым:** как окно выглядит владельцу при открытии и
  играют ли три шума по нажатию кнопки.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-the-bench-opens-quiet-1-003` → слот **WIN-U2**: коммит, освобождение аренды,
    три строки владельцу о том, что посмотреть;
  - `work/2026-09-01-answer-no-script-execute-the-owner-opens-this-window.md`.

  ЗАКРЫТО КАК СДЕЛАННОЕ (1 наряд):
  - `c-exec-g-5a7c-the-bench-opens-quiet-1-002` → `done`: машинная часть выполнена целиком.

  ОБНОВЛЕНО (1 задача):
  - `t-the-bench-opens-quiet-1` блок `done_when` → разделён на «сделано машиной и засчитано» и
    «осталось за его глазом»; записано, почему рефлексия отклонена. **Предмет не менялся.**

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-a-fact-knows-who-made-it-1` остаётся `active` в WIN-U4;
  - `t-everything-lands-1` остаётся `open`.

captures: |
  - **ОБХОДНОЙ ПУТЬ ОТКЛОНЁН ПО СУЩЕСТВУ, А НЕ ПО БУКВЕ, И ЭТО ВАЖНО ДЛЯ БУДУЩИХ ПРОСЬБ.**
    Разбирая предложение, направление нашло, что оно **обходит собственный предмет**: читает
    приватные поля вместо видимого и вызывает функцию вместо кнопки. **Правило-кандидат: на просьбу
    об обходном пути первым делом спрашивать не «можно ли», а «измерит ли он то, ради чего
    заводится» — часто ответ снимает вопрос без всякого запрета.**
  - **СТЕНД, ИМИТИРУЮЩИЙ ЧЕЛОВЕКА, ОТКРЫВАЮЩЕГО ОКНО, — СТЕНД РАДИ СТЕНДА.** Владелец открывает это
    окно сам и постоянно. **Правило-кандидат: прежде чем строить проверку интерфейса, спросить, не
    пользуется ли им владелец каждый день — тогда проверка стоит один взгляд, а не сессию.**
  - **ЗАПРЕТ, КОТОРЫЙ НАПРАВЛЕНИЕ НЕ ВПРАВЕ СНИМАТЬ, ПЕРЕДАЁТСЯ ВЛАДЕЛЬЦУ, А НЕ ОБХОДИТСЯ
    МОЛЧАНИЕМ.** Правило про недоступный инструмент прямо говорит: СТОП и вопрос владельцу.
    Вопрос задан, и одновременно предложен путь, при котором разрешение не понадобится вовсе.
  - **ЗА ОДНУ НОГУ ФОРМА ПРАВИЛАСЬ ДВАЖДЫ, И ОБА РАЗА ПРОВЕРКОЙ, А НЕ МНЕНИЕМ.** Сначала красный
    тест отверг пустой файл, потом разбор отверг рефлексию. **Обе правки удешевили работу**, а не
    удорожили.

decisions_needed: |
  **ОДИН, ВЛАДЕЛЬЦУ, И ОН НЕ СРОЧНЫЙ.** Разрешить ли обходной путь (динамический C#) для проверки
  редакторского окна. **Рекомендация направления — не разрешать:** он открывает это окно сам, и
  проверка стоит ему один взгляд, а построенный замер всё равно мерил бы не кнопку.

play_check: |
  1. OPEN — просьба исполнителя разрешить `script-execute`.
  2. WORK — предложение разобрано и отклонено по существу; машинная часть засчитана и подпёрта тем,
     что нетронутый тест про полку зелён; остаток честно назван непроверенным и отдан его глазу без
     единого требования к тому, как он смотрит; условие на случай его «да» записано заранее.
  3. (owner) — вопрос передан, не решён за него.
  4. CLOSE — задача не закрыта; наряд на коммит и освобождение аренды уходит владельцу.

log: |
  обходной путь отклонён не по букве запрета, а потому что предложенный замер обходил собственный
  предмет: читал приватные поля вместо видимого и вызывал функцию вместо кнопки, то есть вернул бы
  зелёный отчёт о непроверенном; машинная часть ноги засчитана целиком — 747 зелёных при нетронутом
  тесте про полку, — а остаток отдан глазу владельца, чьё это окно и который открывает его сам;
  вопрос о разрешении передан ему, но при рекомендованном пути разрешение не понадобится

next: |
  Наряд `c-exec-g-5a7c-the-bench-opens-quiet-1-003` уходит владельцу для **WIN-U2**: локальный
  коммит, освобождение аренды, три строки о том, что посмотреть.

  Волна: голова закрыта, ходьба мертва, стенд ждёт его взгляда, замирание чинится в U4, публикация
  ждёт только замирания.
