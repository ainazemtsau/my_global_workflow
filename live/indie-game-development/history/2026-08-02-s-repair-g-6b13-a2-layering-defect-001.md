# RESULT — s-repair-g-6b13-a2-layering-defect-001

play: repair
direction: indie-game-development
node: g-6b13
task: a-2
date: 2026-08-02

## outcome

`a-2` отклонена владельцем. Дефект подтверждён чтением кода и он в РАССЛОЕНИИ. Причина — дефект
наряда, написанного этой стороной, а не работа исполнителя. Заодно исправлено место архитектурной
блокировки: она стояла на одну задачу позже, чем нужно.

## evidence

Коммит `955e5e62`, ветка `slot/win-u2`, 11 файлов, 825 вставок. Прочитан первой рукой.

**Что построено:**

- `Assets/TunnelCrew/Net/NetworkPlayerMotor.cs` — `NetworkBehaviour`: читает клавиатуру, шлёт ввод
  через `[ServerRpc]`, двигает `transform.position` на сервере;
- `Assets/TunnelCrew/Net/NetworkButtons.cs` — `OnGUI` с кнопками `Host` / `Join`, нарисованными
  поверх картинки в координатах `Rect(16,16,...)`. Это образец из примеров FishNet;
- `TunnelCrew.Net.asmdef` ссылается на `FishNet.Runtime` и `Unity.InputSystem`;
- **`Assets/TunnelCrew/Core/` содержит ТОЛЬКО asmdef и ноль `.cs`.** Движко-независимый слой,
  заведённый задачей `a-1`, остался пустым.

**Дефект, названный точно:** сеть внизу, игровая логика внутри неё. Движение — правило игры
(скорость, направление, интегрирование позиции) — вшито в `NetworkBehaviour` и не существует
отдельно от сети. Ни запустить в одиночку, ни расширить, ни проверить без поднятой сети нельзя.
Владелец просит обратного расслоения: «мультиплеер был бизнес-слой отдельно, мультиплеер как
обвязка».

**ПРИЧИНА — НАРЯД, И ЭТО ДОКАЗУЕМО ЕГО СОБСТВЕННЫМ ТЕКСТОМ.** `c-exec-two-players-networked-001`
содержит три строки, которые вместе и дали такой результат:

1. «Тесты только на бизнес-логику нового кода. Подключение и **движение фигур** — это интеграция с
   движком и сетью, её проверяет глаз, а не тест». Этой строкой сессия САМА объявила движение
   не-бизнес-логикой. После неё у исполнителя не было ни одной причины класть что-либо в `Core`;
2. «бери самое тупое, что работает» — при отсутствующем требовании расширяемости это честно ведёт
   к `OnGUI`-накладке;
3. «Никаких лобби, экранов подключения» — исполнитель и не сделал экран; он сделал накладку.

**Требования расширяемости в наряде НЕ БЫЛО ВООБЩЕ.** Сессия поставила его на `b-3` (хозяин) и не
поставила на полосу переноски, хотя владелец повторял его про всё ядро: «делаем всё нормально, то
есть чтобы можно было расширять, а не потом переделывать».

**Второй, более глубокий вывод: архитектурная блокировка стояла не там.** Сессия заблокировала
`a-4`, считая, что схема решается там. Решение о РАССЛОЕНИИ принимается в первой задаче, которая
пишет сетевой игровой код, — то есть в `a-2`, и она его приняла молча. Блокировка опоздала на одну
задачу.

## state_changes

`NOW.md`: `a-2` → `rejected` с дословными словами владельца и разбором дефекта; наряд
`c-exec-two-players-networked-001` → `dead`, повторно не выдаётся; `WIN-U2` помечен к освобождению;
`i-architecture-pass-skipped-for-this-bet-001` исправлена — разбор идёт ПЕРЕД первой задачей полосы,
которая пишет код в новом слое, а не перед самой рискованной.

## captures

Коммит `e0000c5b` (каркас `a-1`) остаётся годным: папка, asmdef `TunnelCrew.Core` с
`noEngineReferences: true`, пустая сцена. Скелет был заведён ПРАВИЛЬНЫЙ — `a-2` его просто обошла.

## decisions_needed

Нет. Владелец уже потребовал архитектуру и простой запуск; разбор идёт следующей сессией.

## play_check

- Дефект подтверждён чтением кода, а не пересказом чата.
- Вина назначена туда, где она есть: наряд процитирован построчно. Исполнитель следовал ему точно.
- Исправлено не только следствие (`a-2`), но и правило размещения блокировки, из-за которого дефект
  прошёл.
- Ничего не переписано «на будущее»: преемник выпускается только после подписи владельца.

## log

a-2 отклонена: игровая логика вшита в сеть, Core пуст; причина — три строки наряда, требования расширяемости в нём не было

## next

Архитектурный разбор расслоения — отдельной сессией, ПЕРЕД повторной `a-2`. После подписи владельца
выпускается новый наряд.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-repair-g-6b13-a2-layering-defect-001.md
