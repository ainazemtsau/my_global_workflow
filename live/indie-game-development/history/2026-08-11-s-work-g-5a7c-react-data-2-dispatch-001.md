# RESULT — s-work-g-5a7c-react-data-2-dispatch-001

play: work · выдача наряда на авторинг данными — срез, сделанный `t-react-2` по разрешённой линии
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-data-2
date: 2026-08-11

base_read: OS голова `bc0fe3d2`, дерево чисто. Продукт перемерен `git ls-remote origin`:
`origin/dev` = `origin/main` = `2c8dc0c7`, дерево `931b31eb`. Все четыре слота `AVAILABLE / lease
none`. Пин контракта `36` сверен.

## outcome

Написан и выпущен наряд `c-exec-g-5a7c-react-data-2-001` в WIN-U2 от базы `2c8dc0c7`.
Наряд не существует до расписки — задача остаётся `open`, карточка наряда `ready`.

## evidence — перемерено первой рукой перед выпуском

**НАЙДЕНА ГЛАВНАЯ ДЫРА, и она стала строкой 2 наряда.** `HouseholderDecisionPipeline.Default` — это
`new HouseholderDecisionPipeline()` БЕЗ обработчиков (`HouseholderDecisionPipeline.cs:46-47`), и
именно он стоит политикой в игре (`AuthoritativeHouseholder.cs:147`, `:157`,
`Householder.cs:1706`). Поиск `new HouseholderDecisionPipeline` по `Assets` даёт только `Default` и
его использование — **композиции с обработчиками существуют ТОЛЬКО в тестах**.

Значит оба обработчика, построенные `t-react-2`, в собранной игре сегодня не работают. Точка
расширения доведена до игрового пути, но включать в неё нечего и нечем. Это ровно тот класс ошибки,
который дорожка уже проходила на политике решений, и наряд ставит его главным гейтом: обработчик,
описанный ТОЛЬКО файлом, обязан сработать через `CarryWorld.Advance`.

**Форма ветки уже совпадает с форматом файла.** `HouseholderBehaviorBranch`
(`HouseholderRoutineProfile.cs:76`) проверяет имя, непустоту шагов и полноту каждого шага;
`HouseholderRoutineStep` несёт длительность, обстоятельство, умение, место и строку реакции — а файл
профиля уже описывает шаги словами `durationSeconds`, `when`, `action.ability`. Ветка ложится
ДОБАВЛЕНИЕМ раздела, а не новым форматом. Это записано в наряд, чтобы форматов не изобретали.

**Оба обработчика параметризованы и строятся из данных без нового кода:**
`HouseholderFactCombinationDecisionHandler(name, firstFactName, secondFactName, reaction,
behaviorBranchName)` (`:16-28`); второй устроен так же вокруг порога накопления.

**Приём отказа уже есть:** разборщик знает два умения и всё прочее отвергает
(`HouseholderProfileLoader.cs:43,46`). Наряд требует распространить приём, а не изобретать.

**База зелёная:** `2c8dc0c7` даёт `184 passed` — прогнано Направлением на этой голове.

## Форма наряда

Контракт 36, ПРОБА, три строки `done_when`. Линия разреза названа заранее: строка 2 неснимаема
никогда; первыми уезжают обработчики из файла, ветки остаются; произвольные предикаты из данных не
строятся ни при каком раскладе. Обратный разрез запрещён. Правило про перебазирование сохранено —
оно сработало на прошлой задаче.

## state_changes

1. Заведена карточка наряда `c-exec-g-5a7c-react-data-2-001` (`slot: WIN-U2`, `basis: 2c8dc0c7`,
   `status: ready`).
2. Написан текст `work/c-exec-g-5a7c-react-data-2-001-call.md`.
3. `t-react-data-2` остаётся `open` — расписки нет.
4. NOW, ставка, узел и дорожка `cargo-hands` не тронуты.

## captures

- **Все четыре слота свободны** — дорожка `cargo-hands` между задачами.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.

## decisions_needed

Нет. Владелец отошёл, поручив закрыть работу и составить следующий наряд без него.

## play_check

- 1 recite: done — пять строк карточки сведены в три строки контракта
- 2 owner inputs (owner): done — он поручил выдачу заранее
- 3 do the work: done — продукт перемерен по всей поверхности задачи
- 4 self-check: done — найденная дыра стала главным гейтом, а не примечанием
- 5 close: checkpoint — расписка вне этой ноги

## Гейты

- **G1.** Дорожка `householder` держит один корень.
- **G3.** Линия разреза названа, чтобы задача не раздулась.
- **G7.** Ничего продуктового не решено; Odin и предикаты из данных вынесены за границу.
- **G9.** Карточки задачи и ставки не редактировались.
- **evaluator, клауза 3.** Расписка первой строкой возврата.

## next

1. Владелец отдаёт наряд исполнителю; первая строка возврата — расписка.
2. Возврат приходит HOME; задача закрывается отдельной ногой после публикации.
3. `t-react-3` — его вердикт по дорожке — последняя.
