# RESULT — s-map-g-1d84-tunnel-out-of-the-wave-001

play: map (ревизия одной существующей карточки; новых узлов не заводилось)
direction: indie-game-development · node: g-1d84
date: 2026-08-06
base_read: OS `231b5a53` (перечитан свежим после коммита параллельной сессии; `TREE.md` ею не
трогался, карточка совпала байт в байт с показанной владельцу)

## outcome

Тоннель выведен из подписанной карточки `g-1d84` шестью правками, показанными владельцу ЦЕЛИКОМ и
дословно до единого изменения, и применёнными по его вердикту **«да A»**. Дыра, которую перенос
открыл в критерии 2 (дом закрыт наглухо, а груз надо вынести наружу), закрыта в том же тексте его
выбором варианта A. Новых узлов не создано и следующая волна не планировалась — его прямые слова.
Задачи, наряды и полосы НЕ тронуты: их сводит `shape`.

## owner_approved

**Токен:** `да A` (2026-08-06, в ответ на показанный целиком экран из шести точных правок «было →
станет» плюс три названных варианта по критерию 2 с рекомендацией A).

**Что именно покрыто этим токеном, и ничего сверх:**

1. `goal` — уходит хвост «, — а часть маршрутов открывается заранее нарисованными тоннелями».
2. Критерий 1 — из перечня уходит слово «тоннели».
3. Критерий 9 — «Дом и тоннели лежат отдельными корнями и префабами» → «Дом лежит отдельным корнем
   и префабом».
4. Критерий 10 «ТОННЕЛИ ОТКРЫВАЮТСЯ, А НЕ КОПАЮТСЯ» — удалён целиком; 11 и 12 перенумерованы в
   10 и 11. Больше ничего не сдвигается.
5. Исключения — «валюта открытия тоннелей» → «тоннели целиком и любая вертикаль».
6. `next_if_true` — «отдельная ставка на копание» → «отдельная ставка на копание и тоннели».
7. **Вариант A по критерию 2** — добавлено полфразы: «…поставленную руками; **в доме для этого
   руками сделан выход наружу**».

**Основание переноса — его собственные слова того же дня:** «наверное, хорошая идея тоннели принести
следующую волну со всем вот этим, с вертикальностью… Мне кажется, что лучше в следующую. Ты что
думаешь?» и, после ответа разбора, «давай делать, что [тоннель] переезжает».

**Что он прямо запретил делать этой ногой:** «мы сейчас полностью не планируем, это следующая волна,
это потом после этой будет понятно». Поэтому новый узел под тоннели и вертикаль НЕ заведён, `g-3f90`
не тронут, порядок следующей волны не назначен.

## Почему это был `map`, и что он НЕ делает

Критерий 10 живёт в подписанной карточке в `TREE.md`, а карточку правит только `map` под G9 — с
показом точного артефакта и вердиктом владельца по нему. Ни `research`, ни `shape` этого не могут, и
разбор, который вёл разговор весь день, сознательно остановился и передал сюда.

Задачи, вырезы, полосы, аппетит, `kill_by` и `status` НЕ тронуты и теперь расходятся с карточкой
ОСОЗНАННО — ровно как это уже делала `s-map-g-1d84-core-recut-and-look-node-001`. Их сводит `shape`.

## Что осталось расходиться и ждёт `shape`

1. **Задача `t-house-5`** живёт в `NOW.tasks`, а её основания в карточке больше нет.
2. **Корень `c-exec-g-1d84-tunnel-section-001`** стоит `waiting`; отзывать его — работа `shape`.
   Файл наряда и его раздел `## seam` **не удаляются**: это готовая улика для будущей ставки про
   копание и тоннели.
3. **Полоса `игрок`** после снятия `t-house-5` останется ПУСТОЙ, и это формальный факт, а не
   недосмотр: KERNEL §3 говорит, что полосу распускает только `review` закрываемой ставки, и никакой
   другой плей её не убирает. Пустая полоса WIP не тратит; она исчезнет при `review` g-1d84.
4. **Пункт про выход из дома** должен попасть в текст наряда `c-exec-g-1d84-delivery-point-001` —
   `map` наряды не пишет.

## Что НЕ потеряно вместе с тоннелем

Материал целого дня разбора лежит записанным и переживает перенос: семь пунктов его картины тоннелей
плюс четыре пункта второй порции — дословно в `i-digging-own-bet-authored-fresh-001`; форма ядра
(имя-значение, книжечка у судьи по имени любой названной вещи, вопрос «пройдёт ли это здесь» вместо
чтения флага, авторское начальное состояние) — в разделах `## seam` двух файлов нарядов; измерения
вертикали и цена по кускам — в `i-vertical-is-absent-and-owner-wants-it-001`.

## state_changes

1. `TREE.md`: шесть правок карточки `g-1d84` выше; в `owner_approved` дописан токен `да A` с
   перечнем ровно того, что им покрыто, и с его запретом планировать следующую волну.
2. `NOW.open_calls` `c-exec-g-1d84-tunnel-section-001`: + `card_removed_2026_08_06`.
3. `NOW.open_calls` `c-exec-g-1d84-delivery-point-001`: + `exit_decided_2026_08_06`.
4. `NOW.md` шапка `updated:`; `LOG.md` — одна строка; `history/` — этот файл.

Не тронуто: `CHARTER.md`, `knowledge/`, задачи, вырезы, полосы, WIP, аппетит, `kill_by`, `status`,
файлы обоих нарядов, `g-3f90` и любой другой узел, продуктовый репозиторий.

## decisions_needed

Нет. Все вопросы этого разговора закрыты его вердиктом.

## play_check

- **Шаг 1 recite** — сделан: карточка и состояние дерева перечитаны свежими после коммита
  параллельной сессии; текст карточки совпал с показанным.
- **Шаги 2, 3, 8 (кандидаты, evidence, скелет, прогон по линзам)** — ПРОПУЩЕНЫ С ПРИЧИНОЙ, и
  причина одна: ни одного нового узла не предлагается. Это ревизия одной существующей карточки —
  «Rerunnable anytime — revise, never recreate». Прогон по линзам не запускался, потому что удаление
  критерия не создаёт нового покрытия; вырезанное едет в уже существующую запись о будущей ставке.
- **Шаг 4 cards** — одна карточка, показанная правками «было → станет», а не переписанная целиком:
  так его глазу видно РОВНО то, что меняется.
- **Шаг 5 вердикт** — получен: `да A`, один вердикт на одну карточку, без молчаливого пакета.
- **Шаг 6 порядок** — не менялся: узлы не добавлялись и не снимались.
- **Шаг 7 depth check** — верхний уровень, глубже не резалось.
- **Шаг 9 close** — TREE сохранён, LOG записан.
- **G9 соблюдён:** точный артефакт показан целиком ДО правки, вердикт дословный, `owner_approved`
  отмечен с перечнем покрытого.
- **G2 соблюдён:** волна только сузилась; ничего не добавлено.
- **G7 соблюдён:** выбор по критерию 2 сделан ИМ из трёх показанных вариантов, а не выведен ногой.
- **G8 не задействован:** новых направлений и узлов верхнего уровня не заводилось — его прямой
  запрет планировать следующую волну исполнен буквально.
- `archive/**` не открывался; продукт не трогался.

## log

g-1d84: ТОННЕЛЬ ВЫВЕДЕН ИЗ ПОДПИСАННОЙ КАРТОЧКИ его дословным вердиктом «да A» на показанный
целиком экран из шести точных правок «было → станет». Критерий 10 «ТОННЕЛИ ОТКРЫВАЮТСЯ, А НЕ
КОПАЮТСЯ» удалён целиком, 11-12 перенумерованы в 10-11; хвост про тоннели убран из цели, слово из
критерия 1, «и тоннели» из критерия 9; в исключениях «валюта открытия тоннелей» заменена на «тоннели
целиком и любая вертикаль»; в `next_if_true` добавлено «и тоннели». ДЫРА, КОТОРУЮ ПЕРЕНОС ОТКРЫЛ,
ЗАКРЫТА В ТОМ ЖЕ ТЕКСТЕ И ЕГО ВЫБОРОМ: критерий 2 требует вынести груз ИЗ дома, а дом закрыт наглухо
— ему были показаны три варианта, он выбрал `A`, и критерий 2 получил полфразы «в доме для этого
руками сделан выход наружу», то есть выход стал подписанной строкой волны, а не пожеланием в наряде.
НОВЫХ УЗЛОВ НЕ ЗАВЕДЕНО И СЛЕДУЮЩАЯ ВОЛНА НЕ ПЛАНИРОВАЛАСЬ — его прямой запрет «мы сейчас полностью
не планируем» исполнен буквально; `g-3f90` не тронут. ЗАДАЧИ, ВЫРЕЗЫ, ПОЛОСЫ, АППЕТИТ И СТАТУС НЕ
ТРОНУТЫ и расходятся с карточкой ОСОЗНАННО: снятие `t-house-5`, отзыв корня
`c-exec-g-1d84-tunnel-section-001` и внесение пункта про выход в наряд доставки — работа `shape`, и
названа прямо. ЗАПИСАН ФОРМАЛЬНЫЙ ФАКТ, чтобы `shape` не пыталась сделать лишнего: полоса `игрок`
останется ПУСТОЙ и распустить её может только `review` закрываемой ставки (KERNEL §3), а пустая
полоса WIP не тратит. МАТЕРИАЛ ДНЯ ПЕРЕЖИВАЕТ ПЕРЕНОС и назван поимённо: одиннадцать пунктов его
картины тоннелей дословно в `i-digging-own-bet-authored-fresh-001`, форма ядра в разделах `## seam`
двух файлов нарядов (файлы не удаляются — это улика будущей ставки), измерения и цена вертикали в
`i-vertical-is-absent-and-owner-wants-it-001`. Шаги `map` про кандидатов, скелет и прогон по линзам
пропущены с причиной: ни одного нового узла не предлагалось.

## next

`shape` по `g-1d84`: снять `t-house-5`, отозвать `c-exec-g-1d84-tunnel-section-001`, внести пункт
«проём в наружной стене плюс пол за ним» в текст `c-exec-g-1d84-delivery-point-001`, и записать
пустую полосу `игрок` как ожидаемое до `review`. Отдельно на столе владельца остаётся прежнее: назвать
слот для двух готовых сборочных корней.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-map-g-1d84-tunnel-out-of-the-wave-001.md
