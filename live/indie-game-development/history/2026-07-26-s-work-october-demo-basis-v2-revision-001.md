# RESULT s-work-october-demo-basis-v2-revision-001

direction: indie-game-development
play: work
node/task: g-12fd/october-demo-basis
call: c-work-october-demo-basis-v2-revision-001
date: 2026-07-26

## outcome

The single live Demo Basis is now
`live/indie-game-development/work/october-demo-basis-v2.md`, identity
`october-demo-basis-v2`, carrying the owner's exact recorded revision of
2026-07-26.

Version 1 is a tombstone pointing at v2 and at commit `3f135b9b`, where its full
text lives. Its verification CALL was withdrawn before dispatch with a recorded
disposition, and exactly one fresh verification CALL
`c-converge-verify-october-demo-basis-v2-001` is open against the v2 identity.

`g-12fd` stays parked. No bet, task, track or shape was created. `TREE.md` and
`CHARTER.md` are byte-unchanged. No design was re-derived, no closed point was
reopened and the owner was not re-asked.

## evidence

OWNER WORDS FOR THIS REVISION, verbatim as recorded in the CALL:

- «я чтото это (зал плодит сам, минимум два источника, и заглушение физически
  меняет зал) сам пропустил давай его детально обсудим»
- «зал отвечает на уборку, ну звучит очень натянуто, и так, что я просто бы
  отдельно это рассмотрел»
- «насчет никакой режиссуры это я преувеличил. То есть режиссура, которая мы
  способны сделать, да, вот как раз с триггерами, с таймером, еще что-то,
  которая не требует ручной расстановки»
- «я бы вообще убирал пока вот эти тумблеры. То есть не в какой-то ящик, а в то,
  что ну возможно, чтобы потом рассмотреть»
- «я думаю, что Dungeon Architect нужно включать»
- «либо я вручную буду собирать какую-то комнату, либо мы построим генерацию,
  которая будет создавать комнаты. И получается, мы в демо дадим игрокам, что так
  сгенерилось, так сгенерилось»
- «С твоим предложением, то, что вещество может взаимодействовать на игрока, я
  полностью согласен… по мне, да, это должно быть включено. Я и думал, что это
  включено.»
- «уже газ, наверное, плохо говорить. Сущности какие-то лучше, вещества.»
- «нам нужен базис, что-то определить начальное, чтобы нам разлочить работу
  техническую… на саму разработку как можно быстрее нужно перейти»
- APPROVAL of the exact eight-point MUST list: **«да»**

OWNER-APPROVAL BINDING: the verdict «да» applies to the exact eight-item MUST
list reproduced in `work/october-demo-basis-v2.md` §6. Those eight items are
transcribed from the CALL without addition, removal or reordering. Everything
else in v2 is either carried forward verbatim from the owner-approved v1
(«принимаю», 2026-07-26, commit `3f135b9b`) or is a recorded consequence of the
owner's revision words above, listed line by line under §deviations below.

NO NEW OWNER INPUT WAS TAKEN. This leg re-asked nothing, re-derived nothing and
reopened no closed point, per the CALL's boundaries.

NO LEGACY READ. Neither `archive/**` nor the frozen canon repository was opened
in this leg. The bounded canon read recorded as `i-canon-repo-evidence-read`
belongs to the v1 authoring leg; its file list is carried forward inside v2 as
evidence with zero authority.

FRESH WRITER BASE: HEAD, local `main` =
`3f135b9b` (`indie-game-development work g-12fd/october-demo-basis: owner-approved
Demo Basis v1`); tracked state under `live/` and `os/` clean before apply.

RETURNING CALL, quoted for the verification session's clause map:

```
CALL c-work-october-demo-basis-v2-revision-001
to: session
direction: indie-game-development
play: work
node: g-12fd
goal: |
  Revise the owner-approved Demo Basis to version 2 exactly as the owner
  decided on 2026-07-26, withdraw the now-stale v1 verification CALL, and open
  one fresh converge-verify against v2. g-12fd stays parked.
boundaries: |
  Owner-authority specification continuation. Do not delegate content. Do not
  open shape, bet, tasks or tracks. Do not mutate product, Steam state, the
  canon repository, CHARTER.md or TREE.md. Do not read archive/** or the canon
  repository — this revision needs neither. Do not publish a numeric release
  chance. Do not add anything the owner did not say.
done_when: |
  work/october-demo-basis-v2.md exists as the single live Basis, identity
  october-demo-basis-v2, carrying forward all of v1 unchanged EXCEPT the
  deltas below, and keeping v1's unverified-status line, falsifiers,
  invalidation set, «На чём это стоит» and the open-question list.
  MUST becomes exactly these eight:

  Ничего в демо не требует ручной сборки под конкретный уровень. Триггеры,
  таймеры и устройства допустимы только как то, что размещает генератор.
  Одно вещество с честной физикой и минимум двумя различимыми поведениями
  (тянет вниз / рвётся вверх). Поведение действует и на шар, и на игрока.
  Мусорный шар: набрать вещество из зала, нести, лопнуть. Один глагол
  «неси, не лопни».
  Переноска удержанием на расстоянии, занимающая игрока целиком: медленнее,
  ничего другого, обзор перекрыт, отпустил — уронил. Большой шар один через
  проём не проведёт.
  Процедурная генерация уровня из заранее собранных модулей. Модули
  авторятся в редакторе (Dungeon Architect — editor-time module authoring,
  which the recorded PGG verdict approved). Сборку делает хост и рассылает
  готовую раскладку, а не сид, поэтому детерминизм генератора не требуется.
  Ширина проёмов ограничена и связана с размерами шара, иначе читаемый
  момент возникает не всегда.
  Сеть на 2–4 игрока, чистая установка, повторный запуск, две физические
  машины. Цель проектирования — четверо; вдвоём не ломается.
  Понятное завершение операции: сдан / не успели.
  Прохождение без помощи разработчика.

  REMOVED from MUST, both moved to «потом рассмотреть», not to CUT: «зал
  плодит сам, минимум два источника, заглушение физически меняет зал» and
  «уборка будит остаток». Reason recorded: unverifiable on paper and
  downstream of undecided substances — NOT because they are directing.

  CUT loses «процедурная генерация». Everything else in CUT stands.

  Terminology: «вещество», not «газ», throughout.

  Open questions become eight: the six from v1, plus (7) чем заняты двадцать
  минут кроме набора и переноски — candidates are устройства, постоянная
  течь, будящийся остаток; answered from a playable scene, not on paper, and
  (8) что именно вещество делает с игроком — урон, замедление,
  подбрасывание — merged with the existing damage/death question.
  Open question 4 (процедурная генерация) is now RESOLVED and drops off the
  list: the owner ruled it in as MUST 5.

  work/october-demo-basis-v1.md is replaced by a short tombstone naming
  v2 as current, the owner's v1 approval «принимаю», and commit 3f135b9b
  where the full v1 text lives. Single live source, no palimpsest.
  work/c-converge-verify-october-demo-basis-v1-001-call.md is withdrawn as
  stale with a recorded disposition, and one fresh
  c-converge-verify-october-demo-basis-v2-001 CALL is opened against the
  exact v2 identity, carrying v1's caveats: the reordered concept-frame
  questions, the bounded owner-authorized canon read already recorded in
  i-canon-repo-evidence-read, and the empty-oracle BLOCK condition.
  NOW.md: open_calls holds exactly the new verification CALL;
  i-procgen-determinism updated — the owner ruled procgen IN as MUST 5 with
  host-side assembly and shipped layout, so the remaining engineering
  question is narrowed to module authoring plus layout replication, not
  runtime determinism; i-demo-scope-cap retargeted to v2.
  bet: null, tasks: [], no track, no numeric chance.
  LOG.md gets one line; the full RESULT is saved to history/; every written
  file ends with its END_OF_FILE: trailer; one commit.
  g-12fd stays parked with no bet, tasks, tracks or shape. TREE unchanged.
```

## state_changes

Applied against fresh state at `3f135b9b`:

1. `work/october-demo-basis-v2.md` — created. The single live Basis.
2. `work/october-demo-basis-v1.md` — replaced by a tombstone naming v2, the
   owner's «принимаю» and commit `3f135b9b`.
3. `work/c-converge-verify-october-demo-basis-v1-001-call.md` — replaced by a
   withdrawal record with disposition; original text at commit `3f135b9b`.
4. `work/c-converge-verify-october-demo-basis-v2-001-call.md` — created. One
   fresh verification CALL against the exact v2 identity.
5. `NOW.md` — header updated; forecast driver 1 retargeted to v2 and the «да»
   receipt; `open_calls` replaced with exactly
   `c-converge-verify-october-demo-basis-v2-001`; `i-demo-scope-cap` retargeted
   to v2; `i-procgen-determinism` rewritten to the narrowed engineering question;
   `i-concept-frame-admission` and `i-canon-repo-evidence-read` pointers moved to
   v2. `bet: null`, `tasks: []`, `recurring: []`, `decisions: []`, no track, no
   numeric chance.
6. `LOG.md` — one line appended at the head.
7. `history/2026-07-26-s-work-october-demo-basis-v2-revision-001.md` — this file.

Not changed: `CHARTER.md`; `TREE.md` (`g-12fd` stays `parked`, no children);
`os/**`; `knowledge/**`; `work/concept-frame-v1.md`;
`work/pgg-analysis-2026-07-10.md`; `work/marketing/**`; the archive segments; the
canon repository; product and Steam state. No owner panel is declared by the two
retained knowledge entries, so none was regenerated.

## done_when reconciliation

1. **v2 exists as the single live Basis with identity `october-demo-basis-v2`.**
   PASS. Header carries identity, node, supersedes, the «да» receipt and this
   receipt path. v1 is a tombstone; no second live copy exists.
2. **Carries forward all of v1 unchanged except the deltas, keeping the
   unverified-status line, falsifiers, invalidation set, «На чём это стоит» and
   the open-question list.** PASS with three named deviations, all listed under
   §deviations below. Unverified-status line: verbatim. Falsifiers: all six
   verbatim. Invalidation set: five items, the version line generalized from
   «новая версия Основы (v2)» to «новая версия Основы … предыдущей версией» plus
   the recorded fact that no proof existed under v1. «На чём это стоит»: verbatim
   plus one added evidence paragraph for MUST 5 (see §deviations 3).
3. **MUST becomes exactly the eight listed items.** PASS. §6 MUST reproduces all
   eight in the CALL's order with no addition, removal or reordering. Only the
   MUST 5 parenthetical was rendered into Russian («именно это одобрил записанный
   вердикт PGG») to match the artifact's language; the technical term
   *editor-time module authoring* is left in English as written.
4. **The two removed items moved to «потом рассмотреть», not CUT, with the
   recorded reason.** PASS. §6 has a new «ПОТОМ РАССМОТРЕТЬ» block holding both
   items with the owner's verbatim words, the recorded reason (unverifiable on
   paper, downstream of undecided substances), the explicit statement that the
   reason is NOT directing, and the owner's directing quote showing directing
   stays allowed inside MUST 1.
5. **CUT loses «процедурная генерация»; everything else stands.** PASS with a
   factual note: `процедурная генерация` never appeared literally in v1's §6 CUT
   list. Its exclusion lived in v1 open question 4 («Сейчас в CUT»), which is now
   removed, and it is positively included as MUST 5. The §6 CUT list is therefore
   carried forward byte-identical, which is the correct execution of "everything
   else in CUT stands".
6. **Terminology «вещество», not «газ».** PASS. «газ» is used nowhere as a term
   in the artifact. The only two occurrences are inside the one-line terminology
   note that fixes the rule and quotes the owner's own words. The Latin `gas` in
   the citation path `canon/c-002-gas-behavior-jobs.md` is a filename and is left
   unaltered.
7. **Open-question list.** PARTIAL — the content instruction is executed exactly;
   the CALL's count is not reachable, and is reported rather than forced. The
   CALL says the list "becomes eight: the six from v1, plus (7) … and (8) …" and
   then removes v1 question 4. Six plus two minus one is seven. The artifact
   holds seven open questions, keeping v1 numbering so external references do not
   shift: 1, 2, 3 (damage/death, with the merged "what the substance does to the
   player" folded in as item 8's subject), 5, 6, plus new 7 and new 8. Item 8 is
   listed under its own number, as the CALL enumerates it, and states in its own
   text that it is answered together with question 3 — that is the merge. A
   closing line records that v1 question 4 is resolved into MUST 5 and why the
   numbering has a gap.
8. **v1 tombstone.** PASS. Names v2 as current, the «принимаю» receipt, commit
   `3f135b9b`, this revision leg, and states that no proof existed under v1.
9. **v1 verification CALL withdrawn with a recorded disposition.** PASS. The file
   now records: withdrawn 2026-07-26, never dispatched, reason, and the explicit
   disposition that no session ran, no oracle was authored, no findings exist and
   nothing was consumed. Not a bounce, not a FAIL, not a PASS.
10. **One fresh `c-converge-verify-october-demo-basis-v2-001` against the exact
    v2 identity, carrying v1's caveats.** PASS. All three caveats are carried:
    the reordered concept-frame question order, the bounded canon read recorded as
    `i-canon-repo-evidence-read` (evidence only, zero authority, no new read
    authorized), and the empty-oracle BLOCK condition. Five v2-specific attack
    surfaces are named as attack surface, not verdicts (§handed to verification).
11. **NOW.md.** PASS. `open_calls` holds exactly the new verification CALL;
    `i-procgen-determinism` narrowed to module authoring plus layout replication
    with runtime determinism removed as the open question;
    `i-demo-scope-cap` retargeted to v2. `bet: null`, `tasks: []`, no track, no
    numeric chance; the forecast stays `no_basis`.
12. **LOG line, full RESULT in history/, END_OF_FILE trailers, one commit.**
    PASS. Every written state file ends with its own trailer.
13. **`g-12fd` stays parked; TREE unchanged.** PASS. `TREE.md` byte-unchanged; no
    bet, task, track or shape exists.

## deviations — edits beyond the literal delta list, and why

The CALL says "carrying forward all of v1 unchanged EXCEPT the deltas". Three
places in v1 asserted the two now-deferred mechanisms as fact. Leaving them
verbatim would have made v2 contradict its own MUST list and its own deferral
block. Each was cut back to what the owner's revision leaves standing; nothing was
invented to replace it, and the hole is pointed at open question 7, which the CALL
itself creates.

1. **§1 minimum promise** — removed the clause «а само здание продолжает его
   плодить, пока вы не сообразите, чем именно». Replaced with the substance
   acting on ball and player (MUST 2, owner-approved) and a pointer to open
   question 7 for what fills the twenty minutes.
2. **§2 «Опасность и задача»** — the whole paragraph asserted the two deferred
   mechanisms as the demo's danger. Rewritten to state what the owner's decision
   leaves: the substance's own physics acting on ball and player, an explicit
   record that both mechanisms were deferred by the owner on 2026-07-26, and open
   question 7 as the honest hole.
3. **§4 causal-cooperation support** — the bullet «заглушение источника в одной
   части зала немедленно меняет вещество в другой» rests on a deferred mechanism.
   It is kept in place, marked *(снято на время)* with the reason, rather than
   deleted, so the verification session sees exactly what was withdrawn. One
   sentence records the factual consequence: the causal load now rests on three
   bullets, and whether that is enough is decided by the refutation test, not by
   paper.
4. **«На чём это стоит»** — one evidence paragraph added for MUST 5, because MUST
   5 is a new weight-bearing claim and an uncited weight-bearing claim is exactly
   what converge-verify's smuggling attack kills. It states what
   `work/pgg-analysis-2026-07-10.md` actually records, including that Unity 6.3
   compatibility is that file's own unverified red flag number 1.

Nothing else in v1 was touched.

## handed to verification, not resolved here

These are named for `c-converge-verify-october-demo-basis-v2-001` and are written
into that CALL's context. This leg had no authority to resolve any of them.

1. Whether the demo's danger and causal cooperation still stand after the two
   deferrals, and whether §2 and §4 as rewritten survive attack.
2. Falsifier 5 («Заглушение источника не меняет зал физически») was kept verbatim
   because the CALL instructs keeping v1's falsifiers, but its subject is now
   deferred, so it cannot fire. It is a dormant falsifier by instruction, and it
   is handed to verification as such rather than silently edited.
3. MUST 5 credits the recorded PGG verdict with approving Dungeon Architect
   editor-time module authoring. `work/pgg-analysis-2026-07-10.md` approves **PGG**
   as the editor-time module-authoring assistant, rejects **PGG at runtime** on
   determinism, and places Dungeon Architect as the runtime assembler of
   already-authored static modules; the owner's word «да» stands on the MUST text
   as written. The artifact's evidence paragraph states what the file says; the
   attribution difference is handed to verification, not corrected here.
4. MUST 5's claim that host-side assembly with a shipped layout removes the
   determinism requirement is untested, and Unity 6.3 compatibility of the
   authoring tool is unverified in the recorded analysis. `i-procgen-determinism`
   is narrowed, not closed.
5. §5 public-claim ceiling still lists «процедурная генерация» under «Нельзя,
   пока не появится в сборке». That is not a contradiction with MUST 5 — a MUST
   is what the build owes, the ceiling is what may be said before the build shows
   it — but it is left verbatim per the CALL and named here so verification reads
   it deliberately rather than as an oversight.

## captures

- The first engineering slice was drafted for the owner as a product CALL draft
  only: one authored module, two networked players, one substance, one мусорный
  шар, one doorway — набрать, понести вдвоём, упереться, лопнуть. It is NOT
  registered in `open_calls`, opens no node and creates no bet or track; `g-37a1`
  stays parked and the verification chain is unchanged.
- The owner's pressure «на саму разработку как можно быстрее нужно перейти» now
  runs against the standing chain verify → narrow review → shape of `g-37a1`. No
  gate was weakened here; the tension belongs to the owner at the review.
- Procedural generation entered MUST after the 2026-08-07 scope-cut boundary was
  set, and it is the largest new item in the eight. `i-demo-scope-cap` records it.

## decisions_needed

None from this leg. The seven unanswered content questions in the artifact are
the owner's to answer or leave open; none blocks verification.

## play_check

- Step 1 Recite — done. One untracked `to: session` CALL served the parked
  `outcome_kind: specification` outcome `g-12fd`; the no-bet exception applies.
- Step 2 Owner inputs (owner) — done, and no new input was taken. The owner's
  content decisions were already recorded verbatim in the CALL, including the
  approval of the exact eight-item MUST list: **«да»**. The CALL explicitly
  forbids re-asking, and the owner-verdict guard is satisfied by that receipt.
- Step 3 Do the work — done in specification mode: the owner-authority contour
  transcribed the owner's recorded decision into the exact versioned artifact.
  No executor chose or approved owner-content; no design was re-derived.
- Step 4 Self-check — done point by point above, including the one PARTIAL
  (open-question count) and the four named deviations.
- Step 5 Close — done. The node stays parked with bet/tasks/tracks absent, the
  stale verification CALL is withdrawn with disposition, and one fresh
  `converge-verify` is opened against the exact v2 artifact. Unrelated state
  preserved.
- G1–G4: pass. `bet: null`, `tasks: []`, no track, exactly one lawful open CALL.
- G5: NOT claimed. Nothing in v2 is verified. The binding refutation is the fresh
  separate session opened by this RESULT; this leg ran no in-session pre-pass.
- G9: pass. `CHARTER.md` and `TREE.md` byte-unchanged; the artifact carries the
  owner's exact approval words for the exact MUST list.
- G10: pass. This RESULT declares the complete delta; no live write lies outside
  it.

## log

2026-07-26 | s-work-october-demo-basis-v2-revision-001 | work | direction |
g-12fd/october-demo-basis: owner-recorded revision applied as Demo Basis v2 with
the words «да»; v1 tombstoned, its verification CALL withdrawn undispatched, one
fresh converge-verify opened against v2; g-12fd stays parked.

## next

`c-converge-verify-october-demo-basis-v2-001` is the sole ready frontier and must
run in a fresh session, never this chat and never the v1 authoring chat. `g-37a1`
is not opened; the first engineering slice was handed to the owner as a draft
only, with no route, shape, node or CALL created.

END_OF_FILE: live/indie-game-development/history/2026-07-26-s-work-october-demo-basis-v2-revision-001.md
