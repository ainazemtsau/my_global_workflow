# RESULT — s-shape-g-1d84-core-recut-001

call: owner-direct-shape-g-1d84-core-recut-001
direction: indie-game-development
track: сцена
play: shape
node: g-1d84
owner_approved: "2026-08-06 — «Поимку убираем. Пять полос принимаю, WIP 4. Перенарезку целиком принимаю, записывай.»"

## outcome

Активная ставка сведена с подписанной карточкой без правки `CHARTER.md` и `TREE.md`: вместо 23 задач
осталось 17 — 6 закрытых и 11 живых. Поимка удалена, пять строк внешнего вида/обстановки/третьего
лица/тесного места вышли из ядра, `t-host-7` починена под мировой контакт груза, а строка о
восстановлении намерения переехала из погони в маршрут хозяина. Десять прежних вырезов сведены в
семь настоящих, все семь линз перечитаны. Владелец объявил пять новых полос и WIP 4: четыре корня
могут занимать WIP, тоннель приостановлен до данных дома.

Два текущих наряда `c-exec-g-1d84-house-as-data-001` и
`c-exec-g-1d84-householder-profile-file-001` сохранены без изменения id, статуса, слота, содержания
и CALL-файла. Наряд тесного места закрыт как obsolete без потерь; старый заблокированный наряд
пробы наборов тоже снят как obsolete, а его сохранённая серверная ветка остаётся входом g-3f90.

## evidence

1. **Точные слова владельца после полного экрана:** «Поимку убираем. Пять полос принимаю, WIP 4.
   Перенарезку целиком принимаю, записывай.» Экран до ответа содержал все 17 задач с результатом,
   статусом и отдельной проверкой размера, полный состав пяти полос, три WIP-варианта с рекомендацией
   4, семь вырезов и семь линз.
2. **Карточка-основание:** `TREE.md` g-1d84, двенадцать критериев и список исключений, подписанные
   2026-08-06 словами «1 принимаю»; разбор —
   `history/2026-08-06-s-map-g-1d84-core-recut-and-look-node-001.md`.
3. **Шов t-host-7 перемерен повторно, а не спроектирован заново.** В `WIN-U4` три относящихся файла
   имеют нулевой diff против `d60c2f04`. Единственные вызовы `EmitBoardCreak`, `EmitQuietRustle`,
   `EmitRepeatedHeavyNoise` находятся в `HouseholderRouteControllerEditor.cs`; их другие совпадения —
   объявления методов. `AuthoritativeCargoRoster.cs` каждый тик делает
   `cargo.Contact = body.ReadContact(...)`. Поиск `GetContact(` во всём `Assets/TunnelCrew` даёт
   только объявление `AuthoritativeCargoRoster.GetContact` и Unity-вызов `collision.GetContact(0)`
   внутри `CargoBodyContact`: потребителей геттера списка — ноль.
4. **Приостановленный tight-spot действительно obsolete без потерь:** до этой ноги `WIN-U2` уже
   `AVAILABLE`, lease `none`; в его записи зафиксировано, что задача ничего не построила. Владелец
   подписал удаление поимки и всю перенарезку; тесное место отдельно исключено подписанной карточкой.
5. **Слоты на момент записи:** shared selector — `WIN-U1` CLAIMED профилем, `WIN-U4` CLAIMED домом
   как данными, `WIN-U2` и `WIN-U3` AVAILABLE. Владелец не назначал свободные слоты новым корням,
   поэтому Direction их не выбирал: два новых корня `blocked`, тоннель `paused`.

### Явная проверка размера каждой задачи — вторая нарезка подряд

Правило проверено как объём одной feature-ноги, а не календарный прогноз. Каждая строка ниже —
`≤ полудня`; при выходе за названную единицу executor обязан checkpoint/STOP, а не расширение.

1. `t-scene-1` — одна общая сцена из готовых ходоков и груза; закрыта одной ногой.
2. `t-player-1` — один режим камеры и одна проверка переноски; закрыта одной ногой.
3. `t-scene-2` — перенос одного готового корня хозяина и проверка общей сцены; закрыта одной ногой.
4. `t-house-1` — одна ручная заготовка трёх комнат; закрыта одной ногой.
5. `t-host-1` — один перенос готового хозяина под судью и в снимок; закрыта одной ногой.
6. `t-scene-3` — одна редакторская панель стартовых условий; закрыта одной ногой.
7. `t-house-4` — один производитель описания дома и один список; действующий CALL уже говорит
   «Одна нога. Полдня по замыслу нарезки».
8. `t-host-3` — один дешёвый JSON-формат и один негативный путь; действующий CALL уже говорит
   «Одна нога. Полдня по замыслу нарезки».
9. `t-house-6` — одна точка доставки, одно состояние успеха и один видимый сигнал.
10. `t-host-7` — проводка двух готовых концов и три проверки: нет контакта / дом / игрок.
11. `t-host-2` — один переход на данные дома и одна проверка восстановления намерения; новой
    подсистемы нет.
12. `t-house-5` — один тоннельный участок и одно переключение проходимости.
13. `t-host-4` — один второй профиль и сравнительная проверка двух профилей.
14. `t-host-5` — пять минимальных записей одного формата и структурная проверка отсутствия ветвления.
15. `t-host-6` — ровно одна новая способность и один названный шов.
16. `t-host-8` — одна цепочка погони, потери из виду и ограниченного поиска; восстановление прежнего
    намерения здесь второй раз не закрывается.
17. `t-scene-4` — одна сборка и одно подключение; игровые системы в задачу не входят.

`t-host-9` размера не получила: без строки карточки механика не определена, поэтому по точному слову
владельца «Поимку убираем» она удалена до нарезки, а не объявлена маленькой без основания. Это вторая
последовательная shape-нарезка с явной проверкой каждой задачи; условие
`i-bet-tasks-never-sized-001` выполнено.

## state_changes

1. `CHARTER.md` и `TREE.md` не изменены.
2. `NOW.md -> bet.appetite/approach`: сохранены отсутствие дат/потолка, приоритет, пройденная точка
   пересмотра и «живая сцена»; добавлен принятый текущий размер 17 = 6 done + 11 remaining, а
   устаревшее присвоение бесплатных моделей ядру снято в пользу parked g-3f90.
3. `NOW.md -> bet.cuts`: десять старых строк заменены на семь живых вырезов. Прежние №3, №9 и №10
   не расширены: первый стал фактом карточки, второй — её прямыми исключениями, третий — работой
   g-3f90/g-2b7f.
4. `NOW.md -> bet.lens_verdicts`: записаны семь показанных владельцу вердиктов; линза 2 теперь прямо
   указывает на карточный `t-house-6`.
5. `NOW.md -> tasks`: удалены `t-house-2`, `t-house-3`, `t-look-1`, `t-look-2`, `t-player-2`,
   `t-host-9`; сохранены шесть done; перенумерованы 17 строк; `t-host-7` переписана и разблокирована;
   строка критерия 6 перенесена в `t-host-2`; `t-host-8` оставлена только погоней/потерей из виду и
   ждёт `t-host-2` + `t-host-7`.
6. `NOW.md -> tracks`: WIP 3 → 4; объявлены ровно пять owner-facing полос `сцена`, `дом`,
   `тоннель`, `хозяин`, `внимание` с полным показанным составом. Поскольку schema разрешает retire
   только review при закрытии ставки, пять стабильных route-id не удалены: прежние пустые id `игрок`
   и `внешний-вид` получили новые принятые labels/состав «тоннель» и «внимание». Два текущих CALL
   остаются в прежних `дом`/`хозяин` без правки.
7. `NOW.md -> open_calls`: удалены как obsolete
   `c-exec-g-1d84-cargo-tight-spot-001` и `c-exec-g-1d84-free-kits-probe-001`; добавлены blocked
   `c-exec-g-1d84-delivery-point-001`, blocked `c-exec-g-1d84-world-noise-contact-001` и paused
   `c-exec-g-1d84-tunnel-section-001`. Четыре non-paused корня ровно заполняют WIP 4; агент не
   назначил ни одного слота.
8. `work/`: добавлены три полных pinned-v36 CALL-файла, каждый максимум с тремя done_when bullets и
   явным owner-slot guard. Они не являются launch receipt.
9. `NOW.md -> issues`: закрыты с этой историей `i-wave-recut-to-core-owner-decided-001` и
   `i-bet-tasks-never-sized-001`; текст `i-now-hot-file-over-ceiling-001` механически приведён к
   текущим 17 задачам, сама запись остаётся открытой.
10. `LOG.md`: одна строка; этот полный RESULT сохранён по текущему пути.

## captures

- Нет новых продуктовых требований. Неназначенные слоты — операционная граница запуска, уже
  выраженная статусами CALL, а не новая задача или решение.

## decisions_needed

Нет решения по содержанию. Для запуска каждого из двух новых blocked-корней владелец позже называет
ровно один свободный `WIN-U2` или `WIN-U3`; Direction не распределяет их сама.

## play_check

1. **Recite:** done — цель/12 критериев перечитаны из подписанной карточки; родитель и TREE не менялись.
2. **Appetite first:** done — без дат и потолка; владелец принял новый честный размер целиком, прежний
   приоритет и kill-порог сохранены.
3. **Approaches/minimal solution:** done by preservation — выбранная «живая сцена с первого дня» уже
   доказала две первые догадки; перенарезка меняет объём, не изобретает новый подход.
4. **Scope hammer:** done — семь настоящих вырезов; три старых получили явную диспозицию; ни один не
   расширен молча.
5. **Lens sweep:** done — все семь линз имеют задачу или `not_needed` с причиной.
6. **Riskiest assumption:** done — самый дешёвый оставшийся межсистемный риск проверяет ранний
   `t-host-7`; перемер доказал, что это соединение готовых концов.
7. **Tasks:** done — все 17 перечислены и каждая словами проверена как `≤ полудня`; это вторая
   нарезка подряд. Поимка не получила выдуманного размера и удалена его словом.
8. **Kill criteria:** done by preservation — kill-порог и обе ветви не менялись; дат не добавлено.
9. **Close (owner):** done — «Поимку убираем. Пять полос принимаю, WIP 4. Перенарезку целиком
   принимаю, записывай.» Полный список задач, полос, WIP, вырезов и линз был показан до этих слов.

## log

g-1d84 перенарезана под подписанное ядро: 17 задач, 7 вырезов, 7 линз и 5 полос при WIP 4; поимка
удалена, мировой шум починен, два текущих наряда сохранены, три следующих корня зарегистрированы без
самовольного выбора слотов.

## next

return-to-owner: перенарезка записана. Два текущих наряда продолжаются без изменений; два новых
корня ждут названных владельцем WIN-U2/WIN-U3, тоннель ждёт закрытия t-house-4. Ни один иностранный
CALL не выбран и не запущен этой ногой.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-shape-g-1d84-core-recut-001.md
