# RESULT — s-work-g-5a7c-g5-verdict-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-noise-reaches-by-distance-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — опровержение G5 разобрано первой рукой: находка настоящая и ценнее своего критерия, но задачу не опровергает; сам G5 не сделал того, ради чего открывался, и виноват в этом наряд

close: light — because весь разбор перевыведен направлением на байтах: сборка, в которой живёт обходной путь, все его вызовы, возраст пути, и отдельно — чем ставит шум стенд владельца. Ни одна строка приёмки задачи не закрывается этой ногой. G5-чат не открывался этой ногой, он переиздаётся.

owner_approved: |
  Не требовалось: разбор вердикта ревью и переиздание наряда — механика.

  Владельцу сообщается фактом, включая **признание собственной ошибки наряда**: аренда слота не
  была передана, и живая часть ревью не состоялась по вине направления.

outcome: |
  **ВЕРДИКТ «ОПРОВЕРГНУТО» НЕ ПРИНЯТ КАК ОПРОВЕРЖЕНИЕ ЗАДАЧИ, НО НАХОДКА ПРИНЯТА И ОНА ЦЕННЕЕ ТОГО
  КРИТЕРИЯ, ПОД КОТОРЫЙ ПОДАНА.**

  Перевыверено направлением первой рукой:

  - **факт ревью верен**: `HouseholderRouteController` живёт в **боевой** сборке
    `TunnelCrew.World`, и его `_heardEvent` доходит до `TryHear` мимо `WorldSituationNoise` и мимо
    `Reaches`;
  - **но `EmitEvent` приватен, а два его открытых обёртчика зовутся ровно из двух мест — обоих в
    `Assets/TunnelCrew/Editor/`.** Полный поиск по продукту и тестам: **больше ни одного вызова.**
    В собранной игре путь недостижим;
  - **путь старше ноги слуха**: строка `TryHear(new HouseholderEvent(world.TakeHeardEvent()))` и её
    комментарий «Legacy enum-only events» были до неё. Нога его не создавала.

  **ПОЧЕМУ ЭТО НЕ ПОРОГ СМЕРТИ ВОЛНЫ.** Порог запрещал второй механизм, **ОТВЕЧАЮЩИЙ** на «слышит
  ли он». Легаси-путь на этот вопрос не отвечает — он его **пропускает**, вписывая ответ прямо в
  память, как отладочная команда. Он не конкурирует с расстоянием и старше всего слоя шума.
  **Ревью само это назвало** — «Editor-only инициатор, а не shipping gameplay caller» — и честно
  ограничило свой вывод буквой критерия. Буква нарушена, существо — нет.

  **НО НАХОДКА ВАЖНЕЕ, ЧЕМ ЕЁ ПОДАЛИ, И ВОТ ПОЧЕМУ.** Кнопка `Emit Quiet Rustle` **лжёт о фиче,
  которую ею же и проверяют**: нажатая через весь дом, она заставит хозяина услышать. Инструмент,
  которым меряют слух, слух не меряет.

  **И ПРОВЕРКА, КОТОРОЙ РЕВЬЮ НЕ СДЕЛАЛО, СНИМАЕТ ОСТРОТУ:** стенд наблюдения владельца строит
  настоящие `WorldSituation` с координатами и потому идёт через `Reaches` **честно**. Владелец
  своими словами пользуется именно окном, а не кнопками инспектора. **Лжёт не его инструмент.**
  Без этого замера улика выглядела бы блокирующей.

  **САМ G5 НЕ СДЕЛАЛ ТОГО, РАДИ ЧЕГО ОТКРЫВАЛСЯ, И ЭТО ВИНА НАРЯДА.** Из шести пунктов проверки
  живыми были пять — **ни один не выполнен**. Причина двойная и обе половины наши:
  **аренда `WIN-U2` осталась за завершившейся сборкой** (`…:BUILD`), и свежая сессия справедливо не
  полезла в чужой endpoint; **плюс наряд велел останавливаться на найденном опровержении.**
  Два указания сложились в разбор кода без единого живого замера — **ровно того, ради чего G5 и
  открывался.**

  **ОБЕ ПОЛОВИНЫ ИСПРАВЛЕНЫ В ПЕРЕИЗДАНИИ:** аренда прямо передаётся ревью
  (`…:REVIEW`), а код-находка больше не заканчивает сессию — записывается строкой и не
  останавливает живую часть.

  **ЛАТЕНТНЫЙ РИСК ПРИНЯТ КАК УЛИКА, И ОСТОРОЖНОСТЬ РЕВЬЮ ЗАСЧИТАНА.** Редукция намеренно
  пропускает `Reaches`, когда слушателя нет; ревью отметило риск и **не стало строить на нём
  вердикт, не доказав жизненного цикла**. Это правильная работа.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** на `slot/win-u2` = `b4b010c1`:

  - `Assets/TunnelCrew/World/TunnelCrew.World.asmdef` — **боевая** сборка, без
    `includePlatforms: Editor`; `HouseholderRouteController.cs` лежит в ней;
  - `HouseholderRouteController.cs:117-120` — `private void EmitEvent(...)` пишет `_heardEvent`;
  - `grep "EmitQuietRustle\|EmitRepeatedHeavyNoise\|EmitEvent\|EmitBoardCreaked"` по `Assets` и
    `tests` за вычетом самого контроллера → **ровно две строки**,
    `Editor/HouseholderRouteControllerEditor.cs:68` и `:74`. **Больше нигде;**
  - `AuthoritativeHouseholder.cs:598` — `TryHear(new HouseholderEvent(world.TakeHeardEvent()))`,
    и комментарий «Legacy enum-only events…» рядом — **обе строки старше ноги слуха**;
  - `Editor/HouseholderObservationMarkerCommand.cs:29-85` — стенд владельца строит
    `WorldSituation` c `pointX/pointZ` через `CreatePreset`/`CreateCustom`, то есть **идёт через
    `Reaches`**;
  - `WorldSituationNoise.cs:258-261` — ветка «нет слушателя» возвращает нефильтрованный громчайший,
    с комментарием про cargo diagnostics;
  - реестр `gascoop-slot-state.v1.json` — `WIN-U2` = `CLAIMED`, аренда
    `t-noise-reaches-by-distance-1:BUILD`: **подтверждает названную ревью причину отказа.**

state_changes: |
  СОЗДАНО (1 улика + 1 наряд + 1 документ):
  - `i-a-debug-button-lets-noise-skip-distance-001` → `open`: отладочная кнопка идёт мимо
    расстояния и лжёт о фиче; стенд владельца честен; вторая, производственная дыра про
    отсутствующего слушателя названа отдельно;
  - `c-g5-g-5a7c-noise-reaches-by-distance-1-002` → свежая сессия, **аренда передаётся ревью**;
  - `work/2026-09-01-g5-noise-reaches-by-distance-2.md`.

  ЗАКРЫТО КАК ОТОЗВАННОЕ (1 наряд):
  - `c-g5-g-5a7c-noise-reaches-by-distance-1-001` → `dropped`: вернулся без живых замеров по вине
    наряда; переиздан как `-002`.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-noise-reaches-by-distance-1` остаётся `active`: **опровержения нет, но и подтверждения нет**;
  - остальные карточки волны без изменений.

captures: |
  - **ДВА РАЗУМНЫХ УКАЗАНИЯ НАРЯДА СЛОЖИЛИСЬ В ОДНО НЕРАЗУМНОЕ.** «Останавливайся на найденном
    опровержении» бережёт время; «слот удержан» бережёт работу. Вместе они дали ревью, которое
    не сделало ничего из того, ради чего открывалось. **Правило-кандидат: у наряда на ревью
    проверять, что доступ к предмету проверки ФАКТИЧЕСКИ передан — реестром, а не словом в тексте.**
  - **АРЕНДА СЛОТА ЖИВЁТ ДОЛЬШЕ РАБОТЫ, КОТОРАЯ ЕЁ ВЗЯЛА.** Сборка сдана, аренда осталась
    `…:BUILD`. **Это третий раз за две недели, когда состояние слотов расходится с
    действительностью**, и первые два стоили направлению ложных докладов владельцу.
  - **РЕВЬЮ ЧЕСТНО ОГРАНИЧИЛО СВОЙ ВЫВОД, И ЭТО СДЕЛАЛО ВЕРДИКТ ПОЛЕЗНЫМ, А НЕ БЕСПОЛЕЗНЫМ.**
    Оно само сказало «Editor-only, а не shipping caller» и само отказалось строить вердикт на
    недоказанном жизненном цикле. **Опровержение, знающее свои границы, стоит дороже уверенного.**
  - **НАХОДКА, ПОДАННАЯ ПОД НЕВЕРНЫМ КРИТЕРИЕМ, ЧУТЬ НЕ ПОТЕРЯЛАСЬ.** Как «нарушение буквы» она
    отвергается; как «инструмент лжёт о фиче, которую им проверяют» — важна. **Правило-кандидат:
    у отвергнутого опровержения спрашивать, чем оно ценно ПОМИМО того критерия, под который подано.**

decisions_needed: |
  Ни одного владельческого.

play_check: |
  1. OPEN — вердикт свежей сессии «ОПРОВЕРГНУТО».
  2. WORK — факт ревью перевыверен первой рукой и признан; область его действия измерена полным
     поиском вызовов и возрастом строк; отдельно измерено, чем ставит шум стенд владельца — без
     этого улика выглядела бы блокирующей; собственная вина наряда названа и исправлена обеими
     половинами.
  3. (owner) — не запрашивался: механика ревью; ошибка наряда сообщена фактом.
  4. CLOSE — задача по-прежнему не закрыта; G5 переиздан.

log: |
  опровержение G5 разобрано первой рукой и не принято как опровержение: обходной путь настоящий, но
  зовётся ровно из двух мест, обоих редакторских, и старше самой ноги — он не отвечает на «слышит
  ли он», а пропускает вопрос; зато находка ценнее своего критерия, потому что кнопка лжёт о фиче,
  которой её проверяют, — и отдельно измерено, что стенд владельца честен; сам G5 не сделал ни
  одного живого замера, и виноват наряд: аренда слота осталась за сдавшейся сборкой

next: |
  Переизданный G5 `c-g5-g-5a7c-noise-reaches-by-distance-1-002` уходит владельцу для **WIN-U2**,
  **с передачей аренды ревью**.

  Волна: голова по-прежнему без вердикта, ходьба мертва, граница ответа в работе в U4, публикация
  ждёт обоих. Девятая волна ждёт дерева, на которое можно смотреть.
