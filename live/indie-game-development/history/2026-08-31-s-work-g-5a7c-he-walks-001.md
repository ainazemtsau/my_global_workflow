# RESULT — s-work-g-5a7c-he-walks-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-morning-content-1, полоса `утро`
track: утро
date: 2026-08-31
outcome_kind: work — хозяин ходит: шесть мест по порядку с повтором круга, живьём и на верной базе; и живой прогон забраковал вариант, у которого тесты были ЗЕЛЕНЕЕ

owner_approved: |
  Его слова 2026-08-31: «хочу как можно быстрее видеть… четыре каких-то сделать, этот сценарий…
  он туда, туда, туда, туда зашёл, пусть постоял, не занимается, но делает свой сценарий», и
  «всё, что есть — это тест, всё можно менять».

  Получено шесть мест при требуемых четырёх. Ни одна строка `done_when` не изменена.

outcome: |
  **ХОЗЯИН ХОДИТ. ЭТО ГЛАВНОЕ, И ЭТО ПЕРВОЕ ЗА ВОЛНУ, ЧТО ВЛАДЕЛЕЦ УВИДИТ ГЛАЗАМИ.** Шесть мест по
  порядку, круг замыкается и повторяется:

  ```
  4.5 с   Bedside, спальня   — настоящее занятие с именованной фазой, не отметка
  17.4 с  Ванная
  27.4 с  Кухня
  36.7 с  Гостиная
  43.3 с  Мастерская
  57.1 с  Прихожая
  и обратно: 71.4 Мастерская → Гостиная → 85.7 Спальня → 93.0 Ванная
  ```

  **КОДА НЕ ТРОНУТО НИ СТРОКИ — ПРОВЕРЕНО НАПРАВЛЕНИЕМ.** Диф от базы слияния до кандидата:
  профиль, два файла тестов, и всё. `.cs` вне `tests/` — пусто. Граница наряда выдержана буквально.

  **НОГА НАШЛА ТО, ЧЕГО В НАРЯДЕ НЕ БЫЛО, И ЭТО РЕШИЛО ЗАДАЧУ.** `walk-route` берёт следующую точку
  маршрута **по ИМЕНИ, а не по роли**. Ролей в доме две, а имён в маршруте — шесть. Отсюда шесть
  мест вместо двух. Прогулки поставлены ПОСЛЕ занятия намеренно: `perform-occupation` держит одну
  точку и сбрасывает индекс маршрута.

  **САМОЕ ЦЕННОЕ В ЭТОЙ НОГЕ — ТО, ЧТО ОНА ВЫБРОСИЛА.** Сначала она сделала вариант с хуком
  прибытия: все четыре дела на месте, успешный конец по прибытии, тесты **744 / 743 / 1** — ни
  одного нового красного. **Живой прогон его забраковал.**

  `chore-2` подводит хозяина вплотную к будильнику на тумбочке. Будильник падает. Падение даёт
  `Landing` → `SuspiciousNoise` → `RepeatedHeavyNoise`, и он уходит расследовать шум, **источником
  которого является сам**. Ответ не кончается: он стоит в трёх сантиметрах от точки прослушивания,
  которая уезжает вместе с ним. Воспроизведено дважды, один раз держало больше ста секунд.

  **ТО ЕСТЬ БОЛЕЕ ЗЕЛЁНЫЙ ВАРИАНТ БЫЛ СЛОМАННЫМ, И НОГА ВЫБРАЛА ХУДШИЙ ПО ЧИСЛАМ.** Она назвала
  это уроком сама: «зелёные тесты не значат, что сценарий идёт». **Это подтверждение всей
  сегодняшней дисциплины живого замера, оплаченное собственной работой.**

  **ЦЕНА ПРИНЯТОГО ВАРИАНТА — ЧЕТЫРЕ НОВЫХ КРАСНЫХ, И ВСЕ ЧЕТЫРЕ ПРО УДАЛЁННЫЕ ДЕЛА.** Наряд это
  предупреждал. Но их имена — `TheCommittedProfileCarriesTwoChores…`, `TheShipped…` — вскрывают
  отдельный дефект: **головные тесты механизмов стоят на поставочном содержимом**, которое владелец
  объявил расходным. Заведено уликой.

evidence: |
  Перевыверено направлением в `slot/win-u4`, кандидат **`bd76a42c`**:

  - `git diff --name-only <merge-base> slot/win-u4` → `default.householder.json`,
    `HandlersRecipeTests.cs`, `HouseholderMorningTests.cs`. **Ни одного `.cs` вне `tests/`;**
  - профиль: `routine` — шесть шагов, `perform-occupation` плюс пять `walk-route`;
    `decisionHandlers` — только три инцидентных (`caught-rat-where-the-noise-was`,
    `noise-here-and-nobody-in-sight`, `something-fell-where-it-rustled`); **все четыре дела
    удалены**; `occupations` — четыре, объявлены, в маршруте используется первое;
  - **прогон направления:** `dotnet test tests/TunnelCrew.Core.Tests -c Release` →
    `не пройдено 5, пройдено 739, всего 744`. **Сходится с его отчётом до числа.**

  Из его возврата, как улика: коммит дерева перед замером `d8884907`; проверка пересборки —
  `HouseholderDecisionCandidateOutcomeWords` в загруженной сборке `True`; времена и места круга.

state_changes: |
  СОЗДАНО (2 улики):
  - `i-he-investigates-the-noise-he-made-himself-and-stalls-001` — петля самоподдержки: он роняет
    вещь, слышит собственный шум, уходит расследовать и застревает; **вариант с этой петлёй имел
    тесты лучше принятого**;
  - `i-mechanism-tests-stand-on-disposable-shipped-content-001` — головные тесты механизмов
    опираются на поставочный профиль, который объявлен расходным, и потому перестают отличать
    поломку механизма от смены содержания.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-morning-content-1` остаётся `active`; публикация по-прежнему снята — красных теперь пять.

captures: |
  - **ЗЕЛЁНЫЕ ТЕСТЫ УКАЗАЛИ НА СЛОМАННЫЙ ВАРИАНТ, И ЭТО ЗАМЕРЕНО, А НЕ ПРЕДПОЛОЖЕНО.**
    744/743/1 против 744/739/5, и правильный — второй. **Правило-кандидат: когда два варианта
    отличаются числом красных, живой прогон решает раньше, чем счёт.** Сегодня это спасло ногу от
    публикации петли.
  - **НОГА НАШЛА КЛЮЧ, КОТОРОГО НЕ БЫЛО В НАРЯДЕ, И НАЗВАЛА ЕГО ОТДЕЛЬНО.** «`walk-route` берёт
    точку по имени, а не по роли» — направление писало наряд, зная только про две роли места, и
    считало это потолком. **Потолок был выдуман направлением из неполного замера.**
  - **ГРАНИЦА «КОД НЕ ТРОГАТЬ» ОКАЗАЛАСЬ ВЫПОЛНИМОЙ.** Направление ставило её как СТОП-условие,
    ожидая, что нога упрётся. Не упёрлась: сценарий пошёл на одних данных.

decisions_needed: |
  **ОДИН, И ОН ЕГО.** Смотреть сейчас на ветке — или сначала разобраться с публикацией. Направление
  рекомендует смотреть: работа готова, а публикация не даёт ничего, чего он не увидит на ветке.

play_check: |
  1. OPEN — возврат ноги с живым замером на названной базе.
  2. WORK — диф проверен на отсутствие правок кода; профиль прочитан; **тесты прогнаны
     направлением лично** и сошлись до числа; забракованный вариант разобран и заведён уликой.
  3. (owner) — не требуется на закрытие ноги; требуется его слово о показе.
  4. CLOSE — RESULT, применение, коммит.

log: |
  хозяин ходит: шесть мест по порядку с повтором круга, живьём, и кода не тронуто ни строки —
  сценарий пошёл на одних данных; но главное в ноге то, что она ВЫБРОСИЛА: вариант с хуком имел
  тесты лучше, а живой прогон показал петлю, где хозяин расследует шум, который создал сам

next: |
  **Владелец может смотреть.** Работа лежит кандидатом `bd76a42c` на `slot/win-u4`, слот отпущен.

  Свободны все четыре слота. Подготовка полигона перенацеливается на эту ветку.
