# RESULT — s-work-g-1d84-householder-profile-return-checkpoint-001

play: work
direction: indie-game-development
node: g-1d84
task: t-host-3
track: хозяин
returning_call: c-exec-g-1d84-householder-profile-file-001
date: 2026-08-06

## outcome

**ЧЕКПОЙНТ, НЕ ЗАКРЫТИЕ.** Наряд профиля вернулся сделанным и принятым владельцем в режиме ПРОБА.
Направление перемерило `done_when` первой рукой: две строки из пяти держатся на байтах, три не
разрешаются сами, и по правилу светлого закрытия одной такой строки достаточно, чтобы задача целиком
пошла в биндинговый маршрут. Задача остаётся `open`, возвращающийся корень снят с фронтира, на его
место встал `c-exec-g-1d84-householder-profile-file-002`, ждущий двух слов владельца.

Сверх заказанного нашлось третье, и оно срочнее самой задачи: **принятая работа существует в одном
экземпляре, в локальной ветке освобождённого слота, и обычная первая операция следующей аренды её
сотрёт.** Заодно перемер показал, что два корня из четырёх записаны в NOW не тем статусом, каким они
живут на самом деле.

## evidence

Всё ниже перемерено Направлением первой рукой 2026-08-06; где источник — отчёт исполнителя, это
сказано прямо.

**1. Что построено — держится.**
- Профиль: `ae9ce7b1:Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`, прочитан
  целиком — 20 строк JSON, `formatVersion: 1`, `profileId: default-householder`, две строки распорядка
  (`durationSeconds: 12.0` → `walk-route`; `durationSeconds: 3.0` → `wait-at-route-point`, обе
  `when: always`). Обычный текст репозитория; ни сцены, ни двоичного ассета редактора.
- Описание формата: `ae9ce7b1:docs/householder-profile-format.md`, прочитано целиком — 61 строка.
  Таблица корня (`formatVersion`/`profileId`/`routine`), таблица шага
  (`durationSeconds`/`when`/`action`), `action` = обязательный `ability` плюс необязательный `place`,
  полный список зарегистрированных имён (способности ровно `walk-route` и `wait-at-route-point`,
  обстоятельство ровно `always`), правило «No unlisted field is allowed at any level», отказ на
  числах в кавычках, три разобранных примера ошибки с путём/строкой/позицией.
- Размер доставки: `git diff --stat d60c2f04 ae9ce7b1` = 16 файлов, 1194 добавления, 6 удалений.
  Изменяются три файла (`AuthoritativeHouseholder.cs`, `Householder.cs`, `NetworkHouseholder.cs`),
  остальное — новые (`HouseholderProfileLoader.cs`, `HouseholderRoutineProfile.cs`, профиль,
  документ формата, тесты).
- База ноги объявлена верно: `git merge-base ae9ce7b1 1703599b` = `d60c2f04` — ровно то, что написано
  в наряде.

**2. Строка за строкой по `done_when` — тест светлого закрытия.**

| Строка карточки | Разрешается сама? | Чем |
|---|---|---|
| Профили — текстовые файлы в репозитории, не в сцене и не в двоичных ассетах | **ДА** | байты профиля выше |
| У формата есть описание, по которому ассистент правит без догадок | **ДА** | документ формата выше, полный и самодостаточный |
| Неверный файл отвергается с причиной, называющей место; хозяин не запускается | **НЕТ** | это поведение при запуске; дословный текст ошибки существует только в отчёте исполнителя, Направление игру не запускало |
| Профиль задаёт распорядок — порядок, длительности, обстоятельства — **и действия внутри комнаты** | **ЧАСТИЧНО** | порядок/длительности/обстоятельства — да, байтами; «действия внутри комнаты» — измеренный разрыв, см. ниже |
| Владелец правит через ассистента и видит другое поведение; ломает файл и видит, где именно | **НЕТ** | его акт; до Направления дошёл пересказом в отчёте, а про порчу файла свидетельства нет вовсе |

**Измеренный разрыв по «действиям внутри комнаты».** Поле `place` у действия существует, но документ
формата пишет прямо: «every non-null `place` is intentionally unknown» до привязки дома, а обе
зарегистрированные способности ходят по маршруту сцены и `place` не используют. Шов стоит,
содержимого за ним нет. Это НЕ придирка к исполнителю: наряд, выпущенный самим Направлением, заранее
сказал «эта нога ссылается на места непрозрачными именами и не решает, как они называются», а
привязку отдал `t-host-2`. Поэтому строка вынесена владельцу решением
`d-householder-profile-close-route-001`, а не закрыта и не провалена ногой.

**Вывод по маршруту.** `os/plays/work.md` §Light close: «One line still needing a judgment nobody has
made… sends the whole task to the fresh binding chat; there is no partial light close». Строк таких
три. Светлое закрытие не применялось, `close: light` не выписывался.

**3. РАБОТА ЛЕЖИТ В ОДНОМ ЭКЗЕМПЛЯРЕ — главное, чего не было в заказе.**
- `git for-each-ref --contains ae9ce7b1` → ровно одна ссылка, `refs/heads/slot/win-u1`. (Во время
  замера Направление само создало временную ссылку `refs/tmp/profile-probe` для проверки слияния и
  удалило её после; на момент записи её нет.)
- `origin/slot/win-u1` = `adae7d40` — старый коммит, работы там нет.
- `git merge-base --is-ancestor b1ac3674 1703599b` = NO; то же для `ae9ce7b1` = NO. В `main` работы нет.
- `git rev-list --count d60c2f04..ae9ce7b1` = 5 коммитов.
- `gascoop-slot-state.v1.json`: `WIN-U1` `AVAILABLE`, `lease: none` — владелец слот освободил и
  подтвердил это.
- Значит пять коммитов держатся ОДНИМ локальным указателем в слоте, который считается свободным, а
  обычная первая операция аренды — «привести слот к базе» — этот указатель двигает.

**4. Чисто уже не сводится, и конфликт ровно один.**
- `git rev-parse origin/main origin/dev main` (после `git fetch`) = `1703599b` во всех трёх.
- `git merge-tree --write-tree 1703599b slot/win-u1` → код выхода 1, `CONFLICT (content)` в
  `Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs`, и это единственный конфликтный путь.
- Причина: после базы `d60c2f04` в `main` приехала работа шума (интеграция `9077b26f`), правившая тот
  же файл — 54 строки по `git diff --stat 8ffe5a5d 1703599b`.
- Следствие, которое нельзя терять: владелец гонял ПРОБУ на `b1ac3674`, то есть на состоянии БЕЗ
  работы шума. Сведённое состояние не запускал никто — ровно класс
  `i-published-to-main-before-any-direction-close-001`.

**5. Два корня записаны не тем статусом.**
- `c-exec-g-1d84-world-noise-contact-001` (`t-host-7`) в NOW = `blocked`, «НЕ запущен и слот ему не
  назначен». На деле: `1703599b:docs/results/c-exec-g-1d84-world-noise-contact-001.md` существует со
  `Status: DELIVERED on dev`, кандидат `25cdf4af` со `slot/win-u4` влит в `dev` коммитом `9077b26f`,
  и в отчёте лежит дословный прогон владельца. Больше того: коммит `25cdf4af` называется «удар груза о
  человека удалён целиком по требованию владельца» — то есть половина строки `done_when` `t-host-7`
  снята его словом, и Направление этого не регистрировало.
- `c-exec-g-1d84-delivery-point-001` (`t-house-6`) в NOW = `blocked`, «слот не назначен». На деле:
  `gascoop-slot-state.v1.json` — `WIN-U2` `CLAIMED`, `lease: c-exec-g-1d84-delivery-point-001:PLAN`;
  дерево `GasCoopGame_win-u2` грязное, в нём `DeliveryObjective.cs`, `DeliveryPoint.cs`,
  `DeliveryObjectiveTests.cs` и три изменённых файла.
- Это не закрывает и не открывает ни одну задачу: продуктовая доставка Направлением не считается
  закрытием. Это заведено записью и уходит в `repair`.

**6. Перенос владельца — его слова, не пересказ отчёта.** Сказаны Направлению в этом чате 2026-08-06:
«нынешняя пауза — только доказательство файлового профиля. Будущие занятия должны описываться как
намерения (готовить в кухне), исполняться фазами „путь → действие → завершение“, считать длительность
после прибытия и иметь явную политику прерывания/возобновления». Согласуется с §«Owner carry-forward —
richer activities» продуктового отчёта, но записано как ЕГО слово, потому что оно сказано напрямую.

## state_changes

1. `NOW.md` заголовок `updated:` → эта сессия.
2. `NOW.tasks[t-host-3]`: `status` остаётся `open`; `note` обновлён (наряд вернулся); добавлено поле
   `checkpoint` с построчным разбором `done_when`, измеренным разрывом по «действиям внутри комнаты» и
   дословным переносом владельца.
3. `NOW.open_calls`: удалён `c-exec-g-1d84-householder-profile-file-001`; добавлен
   `c-exec-g-1d84-householder-profile-file-002` — track `хозяин`, `to: executor`, `for: t-host-3`,
   `status: waiting` с `waiting_on`, `engineering_contract: 36`, поля `predecessor`, `preserve_first`,
   `note`. Остальные три корня не тронуты.
4. `NOW.issues` += `i-accepted-work-lives-only-in-a-released-slot-001` (level execution, route work).
5. `NOW.issues` += `i-two-calls-recorded-blocked-are-actually-running-001` (level direction, route repair).
6. `NOW.issues` += `i-householder-activities-are-intentions-not-instructions-001` (level execution,
   route work) — дом переноса владельца.
7. `NOW.decisions` += `d-householder-profile-close-route-001` (track хозяин, два вопроса, четыре
   варианта, рекомендация по первому и её отсутствие по второму).
8. Создан `work/c-exec-g-1d84-householder-profile-file-002-call.md`.
9. `LOG.md` += одна строка; этот RESULT сохранён в `history/`.

Ничего вне перечисленного не менялось. Одновременная правка `s-research-g-1d84-vertical-and-tunnel-placement-004`
(`ae5b19e0`) прочитана и сохранена целиком.

## captures

- Механизм, который сработал третий раз подряд и заслуживает того, чтобы стать формой наряда: явный
  раздел в теле наряда («§Хвосты» у `t-house-4`, `## seam` у доставки и тоннеля, `preserve_first`
  здесь) едет, а память пишущего — нет.
- Впервые с `t-scene-1` есть возможность выдержать букву `NOW.bet.evaluator` (проверка ДО продуктового
  `main`), потому что работа не опубликована. Наряд-продолжение эту возможность сохраняет.

## decisions_needed

`d-householder-profile-close-route-001` — два вопроса владельцу: достаточно ли шва `place` для строки
«действия внутри комнаты», и ломал ли он файл своими руками. Рекомендация есть по первому (1а),
по второму её быть не может — это факт о том, что он делал.

## play_check

1. **Recite** — цель и `done_when` `t-host-3` восстановлены дословно из `NOW.tasks`; обязательство —
   активная ставка `g-1d84`, полоса `хозяин`. ✔
2. **Owner inputs (owner)** — владелец операторствует по этой задаче лично (правка профиля и порча
   файла — его акт), поэтому спрошено ровно то, что знает только он. Его слова, полученные этой ногой
   дословно: «завершён и принят владельцем в режиме ПРОБА»; «WIN-U1 освобождён и подтверждён
   AVAILABLE»; и перенос — «нынешняя пауза — только доказательство файлового профиля. Будущие занятия
   должны описываться как намерения (готовить в кухне), исполняться фазами „путь → действие →
   завершение“, считать длительность после прибытия и иметь явную политику
   прерывания/возобновления». Два оставшихся вопроса вынесены решением, а не додуманы. ✔
3. **Do the work** — приём возврата как улики: перемер `done_when` по байтам, перемер носителя,
   перемер сводимости. Найденные не относящиеся к задаче проблемы (носитель в одном экземпляре; два
   корня с неверным статусом) заведены записями со своими маршрутами и ногу не расширяли. ✔
4. **Self-check** — сверка построчная, таблица выше. Две строки держатся, три нет. ✔
5. **Close** — чекпойнт: возвращающийся id снят, продолжение той же полосы зарегистрировано,
   `t-host-3` остаётся `open`. Последней задачей ставки она не является, `review` не открывается. ✔

close: НЕТ. Светлое закрытие не применялось — три строки `done_when` требуют суждения, которого никто
не выносил. Задача не помечена `done`.

## log

t-host-3: наряд профиля вернулся принятым в ПРОБЕ — построчный перемер держит две строки `done_when`
из пяти, три требуют суждения, задача остаётся open. Найдено сверх заказа: принятая работа лежит в
одном экземпляре в освобождённом слоте и с `main` уже не сводится чисто.

## next

CALL `c-exec-g-1d84-householder-profile-file-002` зарегистрирован в полосе `хозяин` со статусом
`waiting`. Снимается двумя словами владельца по `d-householder-profile-close-route-001`. До этого —
одно действие, не требующее решения: сохранить `slot/win-u1` на сервер именованной ссылкой.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-householder-profile-return-checkpoint-001.md
