# s-shape-004 — shape (re-shape g-9c41 bet under option A): focus the core, drop the clip, deliver in waves

- **date:** 2026-06-14
- **play:** shape (re-shape of an existing bet)
- **node:** g-9c41
- **owner approval (G9):** «фокус на ядро, клип не паримся», «да, A», R1–R13 confirmed (voice, this session) + «давай сделаем что нужно с shape»
- **role:** session → became its own writer after RESULT (agent-CLI on this repo)

## Why this re-shape

The owner's plan stress-test (wf_454d6859-4e4) found the bet was misaligned with his evolved vision and
stale in state: the clip was mis-sourced (promised "spectacular" from coarse band state, which the brief
itself rates as an open risk), and temperature/destruction were cut to exactly what the owner now requires.
Owner's decision: **focus 100% on the real core, drop the spectacular clip, keep network-first, deliver in
waves, 2026-07-24 as a soft target (move tail waves if needed).** This is option A (network-first, raised
floor) minus the clip.

## What changed (applied to state)

- **NOW.md active_bet:** approach + done_when re-shaped → "minimal-but-REAL layered core over the net" (gas
  layer + thin dynamic temperature layer + real controlled destruction + extensible seams; network-first;
  NO clip). Added `wave_plan` (Wave 1 network → Wave 2 coarse sim+grid+levels → Wave 3 detail-near-player →
  Wave 4+ destruction/reactions/scale). cut_list temperature ("static field" → thin DYNAMIC layer) and
  destruction ("scripted breach only" → real CONTROLLED local) SUPERSEDED. kill_by metric breach → real
  controlled. lens commercial/traction → DEFERRED (clip dropped). t-2 RE-SCOPED (toy field carries ≥2
  trivial layers; real controlled breach). t-3 RE-SCOPED (cross-layer interaction + real breach under load).
- **NOW.md open_calls:** c-exec-003 HOLD LIFTED → ready (re-scoped; PLAN re-derives t-2 scope from the
  re-shaped bet + converge §RESOLVED-1/2). leg_opens_with gained a RE-SCOPE banner. c-shape-004 → done.
- **TREE.md g-9c41:** goal gained the layered-architecture language; criterion 8 (spectacular clip)
  DEFERRED; criterion 10 ADDED (layered architecture proven: ≥2 layers on one seam, cross-layer interaction,
  extensible, real controlled breach).

## play_check (shape)

- **appetite:** unchanged 6w/2026-07-24, now explicitly SOFT (owner accepts moving tail waves); G3 honored (re-SHAPE, not extend).
- **approach:** network-first preserved (the lethal risk dies first); the core is now a REAL layered architecture, not a toy-field-plus-clip.
- **scope hammer / cut list:** clip DROPPED; sims stay TRIVIAL; structural collapse OUT; detailed tier + big levels + real gas content → later waves (cut list intact, G6).
- **riskiest assumption:** unchanged — networked chunked-delta consistency, killed first (kill-gate "A", checkpoint 2026-06-30), now across a REAL controlled breach.
- **tasks:** t-1 done; t-2/t-3 re-scoped (waves); ≤3 active (G1).
- **owner close (G9):** owner approved the direction in his words (above); tree edits carry the s-shape-004 stamp.

## Follow-ups (flagged, NOT done here)

1. **Map-level re-check:** the parallel tracks (g-7e15 / g-5b07 page-by-08-31 / g-e6f2 / Oct-5 gate) were
   gated on the ~07-10 clip, now dropped. A map/frame session must re-time that gate.
2. **TREE criteria detail:** criteria 1–7/9 were left as-is (still valid); criterion 10 consolidates the new
   layered requirements. A finer realignment can happen if a wave needs it.
3. **converge continues per-wave:** the remaining open converge questions + converge-arch (c-converge-002,
   grid↔gas contracts) are done JUST-IN-TIME before the wave that needs them (Wave 2), not all upfront.

## Next

**Build Wave 1.** c-exec-003 is ready (HOLD lifted) — the t-2 executor PLAN runs in the product repo
(C:\projects\Unity\GasCoopGame_dev), owner present, consuming work/converge-g-9c41.md, ratifying the
§WHAT-C set into ADR-0003 (incl. the re-scope: ≥2 layers + real controlled breach). Then t-3, then Wave 2.

END_OF_FILE: live/indie-game-development/history/s-shape-004.md
