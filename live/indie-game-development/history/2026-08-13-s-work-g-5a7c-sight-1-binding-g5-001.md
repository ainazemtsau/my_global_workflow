# RESULT — s-work-g-5a7c-sight-1-binding-g5-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-sight-1
track: householder-life
date: 2026-08-13
outcome_kind: PASS — семь строк выдержали попытку опровержения; `t-sight-1` закрыта
venue: **binding fresh physical session.** Отдельный физический чат, не тот, что строил, и не те два,
что вернули чекпойнт и слово владельца. Внутрисессионных субагентов не было ни одного. `close: light`
НЕ применялось и недоступно: карточка объявила это заранее.

owner_approved: |
  **ЗРЕНИЕ, дословно и из журнала `t-sight-1` (строка 69), а не из `docs/results/`:**
  «Подтверждаю зрение своими глазами» — записано
  `history/2026-08-13-s-work-g-5a7c-sight-1-owner-word-001.md`.

  Вторые его слова того же дня, тоже дословно, про дрожь: «Хозяин бежит, единственное, что он дёргано
  бежит, там бывает как будто в проёме застревает, вот он туда-сюда дёргается. Но потом как бы
  вылазит. Вроде бы было даже не в проёме такое».

  **Больше ничего из его слов не выведено.** Ни второго прогона не запрошено, ни нового гейта на его
  глаз не выдумано.

outcome: |
  **ЗАДАЧА `t-sight-1` ЗАКРЫТА. Семь строк её карточки перемерены на опубликованной голове
  `c485b30e704b1706675dd92d15c5223b0d166b92` попыткой ОПРОВЕРГНУТЬ каждую, и ни одна не опровергнута.**

  Сильнее всего били по трём местам, и все три устояли:

  - **«магического знания нет»** — три независимые попытки найти путь, по которому координата игрока
    доходит до хозяина без законно добытого факта. Ни одного не нашлось, и отсутствие доказано не
    чтением, а прогоном: тест двигает игрока ПОСЛЕ потери видимости и три тика подряд проверяет, что
    цель хозяина осталась в старой точке. Тест прогнан этой ногой.
  - **«потребитель не меняется»** — потребитель назван поимённо и сверен побайтно. `Observe`
    идентичен базе, оба `Loudest` идентичны базе, и из шестнадцати файлов, знавших про слой ситуаций
    до зрения, **тринадцать не изменились ни на байт**.
  - **откат** — перемерен по деревьям своей рукой, а не пересказан: дерево отката совпадает с деревом
    базы знак в знак. Отдельно перемерено то, чего в отчёте не было: правка после прогона владельца
    не добавила ни одного нового пути, поэтому «одним коммитом» верно и СЕГОДНЯ, а не только на
    момент демонстрации.

  **ПОРОГ СМЕРТИ 1 НЕ СРАБОТАЛ, и это записано прямо.** Порог звучит «он потрогал и сказал: хозяин
  либо слепой, либо читер, и живыми ручками это не лечится». Его слов в улике ровно два предложения,
  и ни в одном нет ни «слепой», ни «читер», ни «ручками не лечится». Откат зрения не исполняется,
  дорожка `householder-life` вокруг слуха не пересобирается, карточка ставки не тронута.

  **ОДНО МЕСТО, ГДЕ БАЙТЫ УЖЕ СЛОВ КАРТОЧКИ, названо вслух и не раздуто в блокер.** Строка 2 просит
  «названное число, а не константу внутри кода». В ассете названо число ТОЧЕК ТЕЛА (`_bodyPointCount
  5`); сам порог считается в коде — `sampleCount / 2 + 1`. Владелец может сделать зрение строже или
  мягче только сменой числа точек; «видно при 2 из 5» задать нельзя. Правило при этом названо, число,
  которое им управляет, лежит в ассете и правится живьём, а прогон, который он принял, был настроен
  именно так. Поэтому это улика для будущей ноги, а не провал строки.

evidence: |
  **ВСЁ НИЖЕ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ. Где стоит «прогнано» — команда и её вывод мои.**

  ## Голова и чистота

  - `git ls-remote origin` → `refs/heads/main` = `refs/heads/dev` =
    `c485b30e704b1706675dd92d15c5223b0d166b92`.
  - `C:/projects/Unity/GasCoopGame`: `git rev-parse HEAD` = `c485b30e…`, `git status --porcelain`
    пуст ДО и ПОСЛЕ работы. Единственное, что нога написала в продукте, — `bin/`/`obj/` от прогона
    тестов, и они в `.gitignore:138-139`.
  - Ни один ref не двигался. `gascoop-slot-state.v1.json` по-прежнему все четыре слота `AVAILABLE`,
    `lease none`.

  ## ПРОГОН CORE — своей рукой

  Команда (в `C:/projects/Unity/GasCoopGame` на `c485b30e`):
  `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release`

  Вывод: `Пройден! : не пройдено 0, пройдено 252, пропущено 0, всего 252, длительность 264 ms`.

  **252 ПОДТВЕРЖДЕНЫ. Пропущенных ноль** — то есть числа не набраны отключёнными тестами.

  **Базовые 246 перемерены БЕЗ переключения дерева**, счётом атрибутов NUnit по
  `tests/TunnelCrew.Core.Tests/**.cs`:
  `4fc04b4c` → 228 `[Test]` + 18 `[TestCase(` = **246**;
  `c485b30e` → 234 `[Test]` + 18 `[TestCase(` = **252**; `[TestCaseSource]` = 0 на обоих.
  Цепь `246 → 252`, дельта **+6**, сходится с отчётом. Промежуточные `251` перепроверить нечем —
  тот кандидат не лежит ни на одном опубликованном ref, — и они не нужны: карточка просит число
  против 246, и обе точки теперь первой руки.

  ## МАТРИЦА ПО СЕМИ СТРОКАМ `t-sight-1`

  ### 1. ЛУЧ ЧЕСТНЫЙ — ВЫДЕРЖАЛА

  - **Луч настоящий, перехватывает настоящий коллайдер:**
    `Assets/TunnelCrew/Network/HouseholderSightSource.cs:240-246` — `_physicsScene.Raycast(origin,
    dir, _hits, maxDistance, Physics.DefaultRaycastLayers, QueryTriggerInteraction.Ignore)`; ЛЮБОЙ
    попавшийся коллайдер закрывает луч (`:249-250`). Никакого списка слоёв «только мебель», никакого
    исключения по имени. Глаз — от пола плюс `_eyeHeight` (`:97-100`).
  - **Слой правил не узнаёт, что такое мебель — это ДОКАЗАНО МАШИНОЙ, а не заявлено:**
    `core/TunnelCrew.Core.csproj` собирает `Assets/TunnelCrew/Core/**/*.cs` под `netstandard2.1` и
    **не ссылается на UnityEngine вообще**; `grep -rn "UnityEngine" Assets/TunnelCrew/Core/` →
    **ноль совпадений**. Сборка прошла в моём же прогоне
    (`TunnelCrew.Core -> core/bin/Release/netstandard2.1/TunnelCrew.Core.dll`). Ядро физически не
    может назвать коллайдер — иначе оно не скомпилируется.
  - **«Тем же приёмом, каким приходит контакт груза»:** зрение кладёт ситуацию в ТОТ ЖЕ тик-лист
    `_situations`, который наполняет `Cargo.Advance(..., _situations)` (`CarryWorld.cs:180`,
    `:185-202`), и дальше идёт ТОТ ЖЕ `Loudest` и ТА ЖЕ единственная `Householder.Observe`. Второго
    транспорта нет — в методе ровно одно присваивание `Stimulus` и ровно один `Observe`.
  - **Три названных случая** — утверждения лежат в байтах опубликованной головы,
    `Assets/Tests/TunnelCrew/ThreeRoomHouse/HouseholderSightEditModeTests.cs`: открытый пол в
    секторе виден (`:51-54`); крыса за физической ножкой мебели не видна (`:173-176`); крыса на полу
    под физической столешницей не видна с высоты глаз (`:196-199`); настоящий коллайдер
    опубликованного `Coffee Table` — включённый, не-триггер, с `HouseWall` — перехватывает
    производственный запрос (`:204-242`, `:443-447`).
  - **Направление взгляда авторитетное:** `AuthoritativeHouseholder.UpdateFacing` (`:372-380`) берёт
    `result.MovementFacingX/Z` — последний ненулевой ПРОЙДЕННЫЙ сегмент `HouseWalk`, выданный
    `AdvanceAlong`; `HouseholderSightSource.TrySee` (`:59-69`) читает только
    `observer.HasFacing/FacingX/FacingZ`. Ни Transform, ни презентации: `Presentation/**` вообще
    отсутствует в диффе `4fc04b4c..c485b30e`. **Прогнано мной:**
    `AuthoritativeFacingComesFromMovementAndPersistsWhileStill` и
    `AuthoritativeFacingUsesTheLastSegmentAcrossSeveralCorners` — зелёные в моих 252.
  - **Его слово:** «Подтверждаю зрение своими глазами».

  ### 2. ЧАСТИЧНОЕ УКРЫТИЕ НЕ МИГАЕТ — ВЫДЕРЖАЛА, с названной ценой

  - Решение НЕ по одному лучу: `HouseholderSightSource.cs:138-174` перебирает
    `sampleCount = _settings.BodyPointCount` = **5**; точки — центр, верх, низ и две боковые
    (`BodySample`, `:486-523`).
  - Правило: `:139` `var requiredVisible = sampleCount / 2 + 1` → **3 из 5**.
  - Не мигает: `HouseholderSightEditModeTests.cs:114-117` — «the exact-threshold answer must not
    flicker between identical queries»; `:102-105` — 2 из 5 скрыто; `:110-113` — ровно 3 из 5 видно;
    `:122-125` — перекрытый только центральный луч не прячет четыре чистых.
  - Его слово: «Подтверждаю зрение своими глазами».
  - **НАЗВАНО ВСЛУХ:** названное число в ассете — это `_bodyPointCount`, а не сам порог; отношение
    «строгая половина» стоит константой в коде. Строгость двигается только через N (N=1 → 1 из 1;
    N=5 → 3 из 5), «2 из 5» недостижимо без правки C#. Заведена улика
    `i-sight-visibility-ratio-is-code-not-a-knob-001`. **Не блокер:** правило названо, управляющее им
    число лежит в ассете и правится живьём, и принятый им прогон был настроен именно этим числом.

  ### 3. ЗРЕНИЕ — НОВЫЙ ИСТОЧНИК, А НЕ НОВЫЙ МОСТ — ВЫДЕРЖАЛА (это же ответ на строку 3 наряда)

  Проверено как ФОРМА тестом из `knowledge/core-is-extended-never-rewritten.md` §ТЕСТ — «добавить
  или править», — и с обязанностью той же карточки называть цену вслух.

  **Новый член:** `WorldSituation.cs:4-10` → `None 0, ContactedHouse 1, Landed 2, SawRat 3`.
  **Новый производитель:** `Core/Situations/IHouseholderSightSource.cs` (новый, 18 строк) +
  `Network/HouseholderSightSource.cs` (новый, 535 строк).

  **ПОТРЕБИТЕЛЬ, КОТОРЫЙ НЕ ИЗМЕНИЛСЯ — НАЗВАН ПОИМЁННО И ЧЕМ ДОКАЗАН:**

  1. `AuthoritativeHouseholder.Observe(HouseholderStimulus, float)` — **побайтно идентичен**. Тело
     извлечено на обеих головах (`4fc04b4c:…:214-224` и `c485b30e:…:252-262`) и сведено `diff` —
     пусто.
  2. `WorldSituationNoise.Loudest` — **оба перегруза побайтно идентичны**, тем же способом. Правило
     «один самый громкий за тик», которое наряд запрещал трогать, не тронуто.
  3. Файлов, упоминающих `WorldSituation*` в `Assets/TunnelCrew`: **16 на базе, 18 на голове**; оба
     новых — это два новых файла зрения. Из шестнадцати прежних **тринадцать не в диффе вовсе**: все
     пять `Core/Cargo/*`, `CarryWorldObservation.cs`, четыре `Editor/HouseholderObservation*`,
     `Settings/GameRulesSettings.cs`, `World/HouseholderObservationMarker.cs`. Доказательство —
     `git diff --name-only 4fc04b4c c485b30e`.

  **ЦЕНА, НАЗВАННАЯ ВСЛУХ (обязанность карточки правила):**

  - **ДОБАВЛЕНИЕМ:** новый перегруз `CarryWorld.Advance` и новый перегруз
    `AuthoritativeHouseholder.Advance` — **прежние сигнатуры сохранены и делегируют**
    (`CarryWorld.cs:122-158`, `AuthoritativeHouseholder.cs:265-268`), поэтому ни один существующий
    вызывающий не правился; новый конструктор `AuthoritativeHouseholderState` со старым,
    делегирующим в него (`:101-115`).
  - **ЧЛЕН ПЕРЕЧИСЛЕНИЯ — мягкая категория, называю:** дописано **девять членов в восьми
    перечислениях** (`WorldSituationKind`, `HouseholderOccupation`, `HouseholderPriority`,
    `HouseholderPhysicalActionName` ×2, `HouseholderCauseName`, `HouseholderNoticedFactName`,
    `HouseholderEventName`, `HouseholderResponseKind`, `HouseholderResponsePhase`). **Ни одного
    переименования и ни одной перенумерации** — каждая «удалённая» строка в этих кусках диффа это
    запятая, дописанная к прежнему последнему члену.
  - **ПРАВКА — два места, оба в одном файле:** `WorldSituationNoise.Of` получил ветку
    `if (Kind == SawRat)` (`:94-102`), а `Loudness/Rank` — `case SawRat: return 2.5f` с расширением
    `int → float` (`:177-201`). Значит новый вид ситуации стоит РОВНО двух правок, и одну из них
    наряд заказал сам («Где зрение встало в порядке громкости — названо числом»).
  - **ПРАВКА СИГНАТУРЫ — одна:** `HouseholderReactionCatalog.CreateCurrent` получил пятый параметр
    (`HouseholderReaction.cs:331-335`).
  - Пять существующих реакций целы по содержанию: дифф каталога только дописывает `SeenRat(...)`, а
    `HouseholderReactionDataTests` по-прежнему проверяет строки 0–4 и пять идентификаторов `landing`
    числами — зелёный в моём прогоне.

  ### 4. ФАКТ И РЕАКЦИЯ — ВЫДЕРЖАЛА

  - Факт со своим временем жизни: `HouseholderNoticedFactName.SeenRat = 6` дописан к четырём прежним
    (`SuspiciousNoise 2, QuietRustle 3, RepeatedHeavyNoise 4, Landing 5`).
  - **Носитель времени жизни у зрения ДРУГОЙ, и это вынуждено, а не небрежность:** четыре прежних
    приходят параметрами `HouseholderWalkTuning` (`Householder.cs:1604-1655`), а у зрения оно едет
    каждый тик из ассета — `Advance(..., SeenRatFactLifetimeSeconds)` → `WithSeenRatFactLifetime`
    (`:1694-1700`) → `WithSeenRatLifetime` (`HouseholderReaction.cs:451-471`). Строка 6 требует
    ровно этого (живая ручка в ассете), а `Settings/GameRulesSettings.*` наряду был запрещён
    параллельным нарядом на лут — другого носителя не было.
  - Реакция «идти на видимого»: `seen-rat` — `WalkToward`, цель `SituationPoint`, темп `Urgent`,
    `ResponseKind.TrackVisibleThenSearch` (`HouseholderReaction.cs:474-501`).
  - Потерял → ищет у последней виденной точки → возвращается к прежнему занятию:
    `Householder.cs:3149-3179` (`WatchingVisibleTarget → HoldingAtTarget` на `UrgentGuardSeconds`,
    затем `responseCompleted`), `ReturnToLifeMode.ResumePreviousIntention`.
  - **ПОИМКИ НЕТ — проверено двумя способами.** `grep -rniE "despawn|remove.*walker|catch|kill"` по
    `Core/Householder/**`, `Core/Situations/**` и по источнику зрения даёт РОВНО одно совпадение, и
    это комментарий «there is deliberately no catch». И **прогнанный мной** тест утверждает
    `world.Walkers.Count == 1` — «sight never catches or removes the rat» — и что хозяин не оказался
    в точке спрятавшегося игрока.
  - **Прогнано мной:** `SightTracksOnlyLawfulPositions_SearchesAfterLoss_ThenResumes`,
    `ContinuousMovingSightRemainsOneBoundedOccurrence` — зелёные в 252.

  ### 5. МАГИЧЕСКОГО ЗНАНИЯ НЕТ — ВЫДЕРЖАЛА, три попытки опровержения

  1. **Искал утечку поиском.** `grep -rniE "walker|player"` по `Core/Householder/**` и
     `Core/Situations/**` даёт РОВНО одно производственное совпадение — параметр
     `AuthoritativeWalkerState target` в `IHouseholderSightSource.cs:15`, то есть интерфейс, который
     отдаётся ДВИЖКУ. В `Householder.cs` и `AuthoritativeHouseholder.cs` — ноль.
  2. **Прочитал единственный провод.** Цель записывается только в `Householder.cs:2421-2434`, под
     условием `pendingEventConsumed && incomingReaction.ResponseKind == TrackVisibleThenSearch &&
     input.PendingEvent.Position.HasValue` — то есть только из законно произведённой ситуации ЭТОГО
     тика. Движение читает `activeResponse.Target.Position` и ничего больше (`:3115`, `:2969`).
  3. **Проверил, что при невидимости не приходит ничего.** Каждая неуспешная ветка `TrySee` ставит
     `situation = default` и возвращает false (`HouseholderSightSource.cs:84`, `:111-136`,
     `:188-196`); `CarryWorld` добавляет только при `true && sighting.IsPresent` (`:190-201`).
  4. **ПРОГНАНО МНОЙ, и это сильнее чтения:**
     `SightTracksOnlyLawfulPositions_SearchesAfterLoss_ThenResumes` двигает игрока ПОСЛЕ потери
     видимости (`hiddenWalkerX > lastSeenX`) и три тика подряд утверждает, что цель ответа осталась
     ровно в `lastSeenX`: «the hidden walker's new coordinate must not enter the response».

  **Названо вслух и НЕ является нарушением:** при `_drawSightGizmos 1` отладочная проба запоминает
  точку крысы даже когда та скрыта (`:293-347`), и `CompletePicture` достреливает пропущенные лучи —
  но строго ПОСЛЕ решения (`:176-186`), изменить `visibleCount` она не может, и ничто в `Core` её не
  читает. Это картинка для хозяина экрана, а не знание для правил. Цена: до пяти лучей на пару за
  тик вместо раннего выхода; `_bodyPointCount` зажат `1..31` (`HouseholderSightSettings.cs:15,39-43`).

  ### 6. ЖИВЫЕ РУЧКИ — ВЫДЕРЖАЛА

  - Все четыре названные ручки лежат в `Assets/TunnelCrew/Settings/HouseholderSightSettings.asset`:
    `_range 6.5`, `_sectorDegrees 100`, `_bodyPointCount 5`, `_seenFactLifetimeSeconds 1.5` (и рядом
    `_eyeHeight 1.65`, `_ratBodyHeight 0.16`, `_ratBodyRadius 0.06`, `_drawSightGizmos 1`).
  - **Не `ProjectSettings` — измерено:** `ProjectSettings/**` не встречается в
    `git diff --name-only 4fc04b4c c485b30e` вообще.
  - **Читаются живьём:** источник держит ссылку на ассет и перечитывает `_settings.Range`,
    `SectorDegrees`, `BodyPointCount`, `SeenFactLifetimeSeconds` внутри КАЖДОГО вызова `TrySee`
    (`:89-91`, `:109`, `:130`, `:138`); кэшированной копии в файле нет ни одной. Время жизни факта
    едет в правила каждый тик (`CarryWorld.cs:210-215`).
  - Подключено, не тронув запрещённый ассет: `PlaySettings.cs:16,27`, `PlaySettings.asset:19`.
  - Утверждения на опубликованной голове, что правка действует внутри ОДНОГО Play-сеанса:
    `HouseholderSightEditModeTests.cs:60-64` (дальность), `:66-75` (сектор), `:127-136` (число
    точек), `:139-141` (время жизни), `:77-83` (кривое число точек зажимается).
  - Его слово: «Подтверждаю зрение своими глазами».

  ### 7. ОТКАТ — ВЫДЕРЖАЛА, перемерена по хешам и деревьям своей рукой

  `git rev-parse <commit>^{tree}` на каждом:

  ```
  B  4fc04b4c93a097e479a1ce33f47ee55bd3915c67   tree 2b18de9fd00969ab74b8a3f5fd4eeb2d0e47923d
  F  93b08c3a1cbde05fcc23de7bd9525b6c3c58e72c   tree 96509c8261e5749980fda4fa737e01cb364d7def
  R  bf9dadb38c638fdc49ef9b2f69db4afe5c65a349   tree 2b18de9fd00969ab74b8a3f5fd4eeb2d0e47923d  = tree(B)
  S  c569b97f24a205b3e2deb8b0ac17ec1775c772b4   tree 96509c8261e5749980fda4fa737e01cb364d7def  = tree(F)
  ```

  R — настоящий `revert` коммита F, и его дерево совпадает с деревом базы знак в знак; S
  восстанавливает дерево F точно. Это прочитано мной, а не переписано из отчёта.

  **ЧЕГО ОТЧЁТ НЕ ПОКАЗЫВАЛ И ЧТО ПЕРЕМЕРЕНО ОТДЕЛЬНО.** Показанный откат снимает только F, а
  опубликованное зрение — это F плюс правка `c4ee0fc1` после его прогона. Измерено: `c4ee0fc1`
  трогает **7 путей, и все семь входят в 22 пути F**; всё остальное между базой и головой —
  ТОЛЬКО документы (`3d366f63`, `86804752`, `ec1934de`, `af345209`, `c485b30e` — каждый по одному
  `docs/results/*.md`). Поэтому `git diff --name-only 4fc04b4c c485b30e` = 24 пути = 22 пути зрения
  плюс два документа: ни один чужой коммит не трогал файлы зрения. **Снять зрение сегодня —
  по-прежнему ровно один коммит по тем же 22 путям, и он приводит дерево к базовому плюс два
  документа.**

  ## ВЕРДИКТ ПО ПОРОГУ СМЕРТИ 1 СТАВКИ — НЕ СРАБОТАЛ

  Порог дословно: «он потрогал и сказал: хозяин либо слепой, либо читер, и живыми ручками это не
  лечится». Он потрогал — это факт. Сказал он два предложения: «Подтверждаю зрение своими глазами» и
  описание дрожи в беге. **Ни «слепой», ни «читер», ни «ручками не лечится» в них нет.** Порог не
  сработал. Откат зрения не исполняется, `householder-life` не пересобирается вокруг слуха, карточка
  `bet-g-5a7c-wave-3` этой ногой не тронута.

  ## ДРОЖЬ — проверено, что формулировка улики верна байтам, и только

  Обе механические половины `i-householder-chase-path-jitters-in-doorways-001` верны на `c485b30e`:
  цель двигается каждый тик, пока крыса видна (`Householder.cs:2421-2434`), и путь перестраивается
  всякий раз, когда он больше не кончается в цели (`:3116-3122`). «Приехала вместе с этой ногой»
  тоже верно: `TrackVisibleThenSearch` и `HouseholderResponsePhase.WatchingVisibleTarget` — новые
  члены этой ноги. «Ни один гейт не ловит» верно: ни один из шести тестов зрения не гоняет
  устойчивость пути под целью, движущейся каждый тик. **Улика не тронута, задачу ею не заваливали.**

  ## ЧТО Я НЕ СМОГЛА ПРОГНАТЬ, И ПОЧЕМУ ЭТО НЕ ДЫРА

  Единственный Unity-прогон
  (`HouseholderSightEditModeTests.HonestRaysUseMajorityAuthoritativeFacingAndLiveSettings`,
  `[UnityTest]`, входит в Play Mode) я НЕ перезапускала. Для него нужен редактор, работающий в слоте,
  а этот наряд запрещает арендовать слот и двигать refs; все четыре слота `AVAILABLE / lease none` и
  такими оставлены. Заодно измерено, чтобы не выдавать границу за отсутствие площадки: дерево
  `GasCoopGame_win-u3` стоит на `86804752`, и оно отличается от `c485b30e` **только двумя файлами
  `docs/results/*.md`** — то есть несёт ровно опубликованные производственные байты, и прогон был
  физически возможен. Он не сделан по границе наряда, а не по недоступности инструмента.

  Взамен: каждое утверждение того теста прочитано на опубликованной голове и расписано по строкам
  выше, а поведенческие половины строк 1, 2 и 6 стоят на слове владельца — как карточка и назначила.

  `tools/check.ps1` и `-Deliver` не запускались: наряд называет одну команду, зелёные ворота
  поведения не доказывают (`knowledge/green-gates-do-not-compile-the-game.md`), а красный `-Deliver`
  — зарегистрированный чужой дефект `i-deliver-gate-red-when-main-catches-dev-001`.

state_changes: |
  1. `t-sight-1`: `active → done`, уезжает в `closed/`, журнальная строка. Закрытие законно вторым
     путём §`evaluator` ставки — свежая биндинговая проверка в другом физическом чате плюс его
     дословное слово.
  2. `c-work-g-5a7c-sight-1-binding-g5-001`: возвращающийся CALL закрыт, карточка в `closed/`.
  3. Заведена улика `i-sight-visibility-ratio-is-code-not-a-knob-001` (`open`, `level: execution`,
     `route: work`).
  4. Выпущен один локальный преемник в той же дорожке:
     `c-work-g-5a7c-handlers-1-dispatch-001` (`ready`, `to: session`, `play: work`,
     `track: householder-life`, `for: t-handlers-1`) плюс его файл
     `work/c-work-g-5a7c-handlers-1-dispatch-001-call.md`.
  5. Полный RESULT сохранён в `history/2026-08-13-s-work-g-5a7c-sight-1-binding-g5-001.md`.

  **ЧЕГО ЭТА НОГА НАМЕРЕННО НЕ СДЕЛАЛА.** Не тронута карточка ставки (порог 1 не сработал, а
  несработавший порог записи не требует). Не тронут `t-handlers-1` — его `blocked → open` принадлежит
  ноге выдачи, которая обязана перемерить зависимости сама. Не тронуты `t-loot-1`, оба наряда по луту
  и ветка `preserve/...`. Не переоткрыта `t-occupations-1`. Не тронуты закрытые
  `d-sight-overrides-the-hot-stove-001` и
  `i-safe-interruption-collapses-walking-and-delicate-phase-001`, и плита в вердикт не потащена.
  Улика про дрожь не отредактирована. В продукте не изменено ничего.

captures:
  - |
    **ЭТОТ ЗАКРЫТИЕМ ВПЕРВЫЕ ВЫПОЛНЯЕТСЯ ПЕРВОЕ УСЛОВИЕ ПОРОГА СМЕРТИ 3, и у порога не определён
    знаменатель.** Порог 3 срабатывает, когда «общая задача и первая задача КАЖДОЙ дорожки закрыты»
    И расход дороже четырёх ног на закрытую задачу. С закрытием `t-sight-1` первое условие выполнено:
    `t-places-1`, `t-sight-1`, `t-rooms-1`, `t-body-1` закрыты. Второе зависит от того, что считать
    ногой, а карточка этого не говорит. Тем счётом, которым мерил сам ремонт порога (16 ног на 6
    задач 2026-08-12, то есть рабочие ноги волны без ноги нарезки), сегодня выходит **27 ног на 7
    задач = 3.86** — лучше базовых 4.0, порог НЕ срабатывает. Если считать всё подряд с 2026-08-12,
    включая ревью прошлой волны, ногу нарезки и шесть ремонтных ног, выходит **29/7 = 4.14** — и
    порог срабатывал бы. Арбитраж принадлежит `review`/`day`, а не биндинговой проверке; здесь только
    названо число и названа неоднозначность.
  - |
    **ЛУЧШИЙ ЗАМЕР ЭТОЙ НОГИ, ГОДНЫЙ ДЛЯ БУДУЩИХ НАРЯДОВ.** «Ядро не знает про мебель» здесь не
    обещание и не ревью — оно держится СБОРКОЙ: `TunnelCrew.Core` компилируется под `netstandard2.1`
    без ссылки на UnityEngine, поэтому строка, которая назовёт коллайдер, просто не соберётся. Это
    самый дешёвый из виденных в направлении способов сделать шов непробиваемым, и он не стоил ни
    одного теста.

decisions_needed: []

play_check:
  - 1 recite — done. Пересказаны семь строк `t-sight-1` (закон закрытия) против трёх строк наряда исполнителя, буква закрытия §`evaluator` п.2, порог смерти 1 и допущение 1 ставки.
  - 2 owner inputs (owner) — исполнено БЕЗ нового обращения, как требует наряд. Его слова уже получены и процитированы дословно из журнала `t-sight-1`: **«Подтверждаю зрение своими глазами»**, и вторые — про дрожь: «Хозяин бежит, единственное, что он дёргано бежит, там бывает как будто в проёме застревает, вот он туда-сюда дёргается. Но потом как бы вылазит. Вроде бы было даже не в проёме такое». Нового прогона не запрошено, нового гейта на его глаз не выдумано, ничего сверх сказанного не выведено.
  - 3 do the work — попытка опровержения по семи строкам на `c485b30e`: свой прогон Core, чтение байтов, счёт атрибутов для базовых 246, сверка деревьев отката, три независимые атаки на «магическое знание», побайтная сверка потребителя.
  - 4 self-check — done. Каждая из семи строк закрыта названным видом улики: байты (путь:строка:значение), прогон (команда и вывод) или его дословные слова с указанием, где они лежат. Одно расхождение байтов со словами карточки найдено, названо и заведено уликой, а не спрятано и не раздуто в блокер.
  - 5 close — PASS. Основание: свежая биндинговая проверка в ОТДЕЛЬНОМ физическом чате плюс его дословное слово — второй законный путь §`evaluator` п.2. `close: light` не применялось и недоступно по объявлению самой карточки.
  - G5 — исполнен ЭТОЙ ногой. Venue назван прямо: `binding fresh physical session`, не тот чат, что строил, и не те два, что вернули чекпойнт и слово владельца; внутрисессионного пре-пасса не было ни одного.
  - G7 — done. Ни одного игрового решения не принято: отношение видимости названо уликой для будущей ноги, а не выбрано за него; дрожь не чинилась; плита не поднималась.
  - G3 — done. Аппетит не расширен: ноль новых задач, одна улика-указатель и один преемник той же дорожки, объявленный подписанной ставкой.
  - G10 — done. RESULT финальный, поля полные, шаги показаны.

log: биндинговая проверка ЗАКРЫЛА t-sight-1 — семь строк карточки перемерены на опубликованной c485b30e попыткой ОПРОВЕРГНУТЬ каждую, и ни одна не опровергнута; прогон Core сделан своей рукой и дал 252 при нуле пропущенных, а базовые 246 перемерены БЕЗ переключения дерева счётом атрибутов NUnit (228+18 на 4fc04b4c против 234+18 на голове), то есть цепь 246→252 подтверждена обеими точками первой руки; «потребитель не меняется» проверено побайтно и устояло — Observe идентичен базе, оба Loudest идентичны базе, и из шестнадцати файлов, знавших про слой ситуаций, тринадцать не в диффе вовсе, а цена расширения названа вслух как требует карточка правила: девять членов дописано в восемь перечислений без единой перенумерации, две правки ветки в одном файле-редукторе и одна правка сигнатуры каталога реакций; «магического знания нет» атаковано трижды и не пробито, причём решает не чтение, а прогнанный мной тест, который двигает игрока ПОСЛЕ потери видимости и три тика подряд держит цель в старой точке; откат перемерен по деревьям — дерево revert совпадает с деревом базы знак в знак, и отдельно измерено то, чего в отчёте не было: правка после его прогона не добавила ни одного нового пути, все её семь путей входят в 22 пути первой сборки, а всё прочее между базой и головой это только документы, значит «одним коммитом» верно и сегодня; ядро не знает про мебель не по обещанию, а по сборке — TunnelCrew.Core компилируется под netstandard2.1 без UnityEngine; ПОРОГ СМЕРТИ 1 НЕ СРАБОТАЛ и это записано прямо, потому что в двух его предложениях нет ни «слепой», ни «читер», ни «ручками не лечится»; найдено одно расхождение байтов со словами карточки и оно НЕ раздуто в блокер — порог видимости считается в коде как sampleCount/2+1, в ассете названо только число точек, поэтому «2 из 5» задать нельзя, и это ушло уликой; Unity-прогон намеренно НЕ перезапущен по границе наряда, а не по недоступности площадки, и это измерено — дерево win-u3 несёт ровно опубликованные производственные байты; слоты остались AVAILABLE, в продукте не изменено ничего

next: |
  `c-work-g-5a7c-handlers-1-dispatch-001` — единственный преемник, та же дорожка
  `householder-life`, порядок объявлен подписанной ставкой (`t-sight-1` → … → `t-handlers-1`).
  Все его зависимости теперь закрыты: `t-sight-1` этой ногой, `t-place-fact-1` и `t-thing-1` раньше.

  Дорожки свободны: все четыре слота `AVAILABLE`. В `house-and-things` уже лежит своя готовая нога
  `c-exec-g-5a7c-loot-foundation-001` — она чужая этой ноге и здесь не трогалась.

END_OF_FILE: live/indie-game-development/history/2026-08-13-s-work-g-5a7c-sight-1-binding-g5-001.md
