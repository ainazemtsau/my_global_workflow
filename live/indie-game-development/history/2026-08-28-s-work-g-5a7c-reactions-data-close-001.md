# RESULT — work · g-5a7c/словарь · t-reactions-data-1 · закрытие

outcome: **done**. Реакция хозяина заводится строкой текстового файла. Опубликовано на
`origin/main` == `origin/dev` == `daeb3b4d`.

## evidence

**Строки 2–5 — перемерены ПИСАТЕЛЕМ первой рукой на опубликованной голове, а не приняты пересказом:**
- секция `reactions[]` стоит в боевом `default.householder.json` на `main`;
- реакция `stand-over-the-spot` названа только там — в продуктовом C# её имени НЕТ ни разу
  (три вхождения всего, все в `tests/.../HouseholderReactionsFromProfileTests.cs`);
- `dotnet test tests/TunnelCrew.Core.Tests -c Release` на этой же голове: **551 из 551 зелёными,
  0 упавших**. Цифра наряда (526) устарела, и это записано.

**Строка 1 — закрыта СВЕЖЕЙ БИНДИНГОВОЙ СЕССИЕЙ (G5), не пересказом исполнителя.** Ей было дано
одно задание: опровергнуть. Опровергнуть не удалось. Её замеры из живого PlayMode
(`IntegratedHouse`, хост-судья, тик 1/30, через наблюдательный API курьера):
- шорох 5 Н·с в кухне `(1.2, 4.0)`, через 6 тиков падение туда же, хозяин в это время шёл по делам
  в коридоре → `handler=something-fell-where-it-rustled`, реакция `stand-over-the-spot`, темп
  `urgent`, `GuardAtListeningPoint`, факты `[QuietRustle@Kitchen; Landing@Kitchen]`. Дошёл ровно до
  точки, встал, шаг 0 м/тик. `CrouchAndPeekAtTarget` не появлялся НИ РАЗУ; факта `seen-rat` не было
  ни в одном прогоне;
- правка ОДНОГО слова `"urgent"` → `"ordinary"` (`git diff` = 1 строка): шаг за тик
  **0.1083 → 0.03333 м**, отношение **3.25** — ровно отношение двух строк таблицы темпов
  (3.25 и 1.0 м/с). Внутри каждого прогона свой контроль: на рутине 0.0333, в момент реакции
  переключение, после — возврат; в «ordinary» переключения нет вовсе. Обработчик, реакция, вид
  отклика и точка — одинаковы; изменился ТОЛЬКО темп;
- пересборки кода не было: отпечаток размера+времени всех **182** сборок
  `Library/ScriptAssemblies` до правки, после и в конце — один и тот же (`159d7632…`),
  `IsCompiling: false`. **Та же сессия назвала прежнее доказательство (время папки
  `ScriptAssemblies`) слабым** — на Windows перезапись файла внутри время папки не меняет;
- попытка сломать: `"physicalAction": "moonwalk"` → отказ загрузчика с файлом, словом и путём
  `reactions[1].physicalAction`. Молча ничего не делает — нет.

## captures

- `i-unknown-pace-word-kills-the-whole-session-001` (`route: work`) — найдено при опровержении и
  НЕ чинилось, наряд этого не давал: неизвестное слово ТЕМПА отказывает вслух, но
  `ArgumentException` летит из тика (`AuthoritativeHouseholder.cs:556`), а не из загрузчика, и
  `PublishSnapshots()` стоит за ним — снимков нет, `localPlayerId = -1`, **мертва вся сессия, а не
  один хозяин**. Цена опечатки в темпе — «игра не запустилась».
- Оговорка той же сессии: профиль читается ОДИН РАЗ за сессию Play. «Меняется строкой текста» —
  верно; «на лету, не выходя из Play» — не проверялось и утверждением не покрывалось.
- Не сделано и названо: снимка экрана нет — Scene View через мост отдаёт устаревший кадр, Game View
  смотрит глазами мыши из другой комнаты.

## play_check

- G5 — **пройден полным путём, без светлого закрытия.** Одна строка требовала прогона, которого
  писатель сам не делал → отдельный свежий физический чат, задание на опровержение, вердикт
  «опровергнуть не удалось». Остальные четыре перемерены писателем по закоммиченным байтам.
- Продуктовая публикация уликой закрытия не считалась: `main == dev` названо фактом, а решение
  принято по строкам `done_when`.

## next

`t-world-port-1` разблокирована. `t-irritation-1` остаётся `waiting` — не по зависимости наряда, а
по границе файлов: обе задачи пишут в `default.householder.json`, а ставка разводила полосы
владением файлом.

END_OF_FILE: live/indie-game-development/history/2026-08-28-s-work-g-5a7c-reactions-data-close-001.md
