# RESULT — s-work-g-5a7c-react-2-dispatch-001

play: work · выдача одного наряда на второй корень дорожки `householder`, освободившейся закрытием
`t-react-1` в предыдущей ноге
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-2
date: 2026-08-11

base_read: OS — `wt/indie-game-development`, рабочее дерево чисто, голова `6041c36c`. Продукт
перемерен `git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` =
`2f7f2e87b3d3fa1e3b40e50f5063a87530fc5187`, дерево `79fb9710f2d02f9983c94a0704e8ef96a454ee3f` — то
же, что закрывала предыдущая нога, дорожка `cargo-hands` с тех пор ничего не публиковала. Слоты
прочитаны файлом `gascoop-slot-state.v1.json`: WIN-U2 `AVAILABLE / lease none / endpoint
unrecorded`, WIN-U1 `CLAIMED` арендой `c-exec-g-5a7c-cargo-1-001:BUILD`.

## outcome

Написан и выпущен наряд `c-exec-g-5a7c-react-2-001` в слот WIN-U2 от базы `2f7f2e87`.

**Наряд НЕ СУЩЕСТВУЕТ, пока исполнитель не расписался** (`evaluator` клауза 3). Расписка по
построению приходит извне этой ноги, поэтому `t-react-2` остаётся `open`, карточка наряда — `ready`.
В прошлый раз расписки в байтах не оказалось вовсе — теперь требование стоит первой строкой блока
`return`, а не в его середине.

**Главное, что дала эта нога сверх выдачи: карточка задачи ссылается на код, которого больше нет.**
`t-react-2` `done_when` 1 указывает на `Householder.cs:1026-1044` и говорит, что вторая ситуация
другого рода «отвергается». Обе части устарели после `t-react-1`, и обе перемерены здесь заново.

## evidence — перемерено первой рукой перед выпуском

### ПОПРАВКА К КАРТОЧКЕ, названная вслух: это не отказ, это БРОСОК

Правило живёт теперь на `Householder.cs:1072-1081` и не отвергает вторую ситуацию, а бросает
`InvalidOperationException("Different simultaneous Householder reactions are outside this
single-fact PROBA.")`.

**Перемерено, что это НЕ регресс предыдущей задачи:** тот же `throw` с тем же текстом и тем же
условием стоял на базе волны `4cf2b5dc:1040`; `t-react-1` перенесла его знак в знак, заменив лишь
`GetFactName(ActiveResponse.Cause)` на `ActiveResponse.Reaction.NoticedFactName`. Светлое закрытие
`t-react-1` этим не подорвано — проверено прямым чтением базы, а не рассуждением.

Требование строки карточки не меняется, но РАБОТА выглядит иначе: открыть приём — это убрать бросок
и поставить на его место названное правило, а не ослабить фильтр. Карточка не правится (её текст
подписан), поправка живёт в наряде и здесь.

### Почему бросок сегодня не стреляет — два глушителя, и они и есть предмет задачи

- `Core/Situations/WorldSituationNoise.cs:110-121` — `Loudest(...)` сворачивает весь список тика в
  ОДИН стимул; порядок громкости выписан числами `:154-168` (`Landing 4` > `RepeatedHeavyNoise 3` >
  `BoardCreaked 2` > `QuietRustle 1`, при равенстве остаётся ранний).
- `Core/Householder/AuthoritativeHouseholder.cs` — `Observe` (`:176-186`) выбрасывает стимул, если
  слот занят; `TryHear` (`:333-343`) не берёт второе событие; `CanHear` (`:345-374`) отвергает
  событие другого рода при живом факте или активном ответе.

**Самый полезный факт наряда: код сам назвал этот шов.** Доксртока над `TryHear` (`:314-332`)
объясняет, что глушитель существует только потому, что правило бросает, и заканчивается словами
«If his rules ever grow a way to offer an event and be told no, this goes». Это буквально день
`t-react-2`, и наряд требует, чтобы комментарий уехал вместе с глушителем, а не остался врать.

### Задача МЕНЬШЕ, чем при нарезке, и это измерено, а не почувствовано

- **Половина строки 2 уже сделана `t-react-1`:** `RequiresSafeInterruption` — поле строки каталога,
  читается решателем `Householder.cs:477-479`. Значения по пяти строкам прочитаны поимённо
  (`suspicious-noise` `true`, `quiet-rustle` `true`, остальные `false`). Не хватает не поля, а
  политики «что происходит, когда прерывают».
- **Проводки строить не надо:** `CarryWorld.cs:124-138` уже держит весь список тика
  (`_readOnlySituations`) СТРОКОЙ ВЫШЕ вызова `Householder.Observe(Stimulus, tickSeconds)`.
- **Сигнатура `Observe` свободна:** вызывающих ровно два — `CarryWorld.cs:138` и
  `HouseholderReactionDataTests.cs:198`.

Наряд прямо говорит, что сэкономленное на этом НЕ тратится на новое.

### Строка 3 карточки подтверждена байтами

`IsSafeToInterrupt` вычисляется в единственном месте `Householder.cs:1385-1421`: `true` ровно при
приходе на путевую точку (`PausingAtRoutePoint`), `false` на каждом тике фазы `Moving`. Тест
`HouseholderWalksTheHouseTests.cs:289` подаёт второй шум под охраной `:304-308`, которая требует
`IsSafeToInterrupt`, — то есть только когда хозяин СТОИТ, и никогда «во время пути назад», ровно
наоборот собственному имени. Проверить свой случай он сегодня не может.

### Найденная мина: второй вход событий, который нельзя расширять

`IHouseholderWorldPort.TakeHeardEvent()` (объявление `AuthoritativeHouseholder.cs:96`, потребление
`:219`) — кнопки шума из сцены. Интерфейс реализуют `NetworkHouseholder.cs:140`,
`HouseholderRouteController.cs:91` и **четыре существующих тестовых двойника**
(`CargoContactNoiseTests.cs:129`, `HouseholderInTheJudgesTickTests.cs:62`,
`HouseholderProfileTests.cs:37`, `HouseholderReactionDataTests.cs:314`). Расширение интерфейса
заставило бы править четыре существующих тестовых файла и вылезло бы за полдня — наряд это прямо
запрещает, множественность идёт через `Observe` и список `CarryWorld`.

### Строку 4 headless закрыть НЕЛЬЗЯ, и это перемерено

Число силы рождается в `Assets/TunnelCrew/Network/CargoBodyContact.cs` — `MonoBehaviour` на 112
строк, берущий импульс прямо из решателя Unity. Ни один engine-free тест этого числа не производит,
значит «замерить прогоном» = Unity-прогон, другого пути нет. У WIN-U2 `mcp-endpoint: unrecorded`,
а единственный записанный endpoint принадлежит WIN-U1 — слоту параллельной дорожки.

Поэтому наряд ставит замер ПЕРВЫМ действием, до единой правки правил (он ничего не меняет в коде и
отвечает на вопрос о доступности Unity до траты половины дня), и прямо разрешает **СТОП домой одним
блокером**, если MCP не поднимается. Пересчитать вместо замера и назвать это замером — запрещено
явно: улика требует прогона именно потому, что расчёт уже есть и ему не верят.

Пороги перемерены на HEAD и не двигаются: `NetworkPlaySettings.asset:72-74` —
`_cargoRustleForce: 60`, `_cargoKnockForce: 150`, `_cargoHeavyNoiseForce: 400`.

### Слот и параллельная дорожка

WIN-U2 `AVAILABLE`, локальная `slot/win-u2` уже стоит РОВНО на базисе — в отличие от прошлого раза,
догонять 49 коммитов не надо. `origin/slot/win-u2` остался на `e4df0459`; селектор читает локальное,
названо, чтобы это не приняли за разрыв.

**Названный риск базы:** `t-cargo-1` ещё не закрыта Направлением, её строку судит глаз владельца, и
порог смерти (1) волны прямо допускает откат дорожки `cargo-hands`. Такой откат сдвинет базу под
этой работой, поэтому наряд несёт «`origin/dev` сдвинулся — СТОП домой».

### Форма наряда

Контракт 36, режим ПРОБА, `done_when` свёрнут в **три** строки (потолок контракта), пять строк
карточки разложены по ним таблицей `проверка происхождения` без потерь. Разрешённый разрез
(«прерывание и возобновление → своя восьмая задача») перенесён в наряд дословно вместе с правом
исполнителя резать по нему без возврата за словом. Расписка вынесена первой строкой `return`; откат
назван неотменяемым пунктом со ссылкой на то, что в прошлый раз его не было.

## state_changes

1. Заведена карточка наряда `c-exec-g-5a7c-react-2-001` (`to: executor`, `for: t-react-2`,
   `track: householder`, `slot: WIN-U2`, `basis: 2f7f2e87`, `status: ready`).
2. Написан текст наряда `work/c-exec-g-5a7c-react-2-001-call.md`.
3. `t-react-2` остаётся `open`: наряд не выдан, пока нет расписки.
4. `NOW.md`, `g-5a7c`, текст ставки, `t-cargo-1` и `c-exec-g-5a7c-cargo-1-001` не трогать.
5. Одним `osctl leg close` сохранить этот RESULT и добавить log ровно один раз в журналы `g-5a7c`,
   `t-react-2` и новой карточки наряда.

## captures

- **Карточка `t-react-2` называет строки `Householder.cs:1026-1044` и слово «отвергается».** После
  `t-react-1` это `:1072-1081` и БРОСОК. Карточка не правится (текст подписан), поправка живёт в
  наряде и здесь. Если `review` этой волны будет чинить формулировки — вот место.
- **Ссылки на строки в карточках задач протухают от чужой задачи в той же дорожке.** Это второй
  случай в волне (первый — имя `InvestigateLandedCargo` в `t-react-1`, снятое `t-situation-1`).
  Дешёвый вывод, если `review` захочет: в карточку задачи класть ИМЕНА и правила, номера строк — в
  наряд, который пишется в тот же день, что и работа.
- **Порог смерти (5) считает ноги на общую задачу и ПЕРВЫЕ задачи обеих дорожек:** потрачено 4
  (выдача и закрытие `t-situation-1`, общая выдача двух нарядов, закрытие `t-react-1`); закрытие
  `t-cargo-1` ещё не потрачено и станет пятой. Эта нога — про ВТОРУЮ задачу дорожки и в счёт порога
  не входит.
- **У WIN-U2 нет записанного Unity MCP endpoint вовсе.** Если строка 4 вернётся блокером, чинить
  надо не наряд, а то, как слот получает endpoint.

## decisions_needed

Нет. Владельцу эта задача руками не показывается: его вердикт по дорожке назначен на `t-react-3` в
самом её конце, и это сделано нарочно формой волны. Два вопроса, ждущих его слова
(`q-house-hears-20-percent-earlier-001`, `d-agents-skills-belong-in-repo-001`), вынесены ему в
предыдущей ноге и остаются ждать — ни один не блокирует эту выдачу.

## play_check

- 1 recite: done — пять строк `t-react-2`, границы по файлам из `## tracks`, разрешённый разрез из
  `## note` и рискованное допущение №2 сведены
- 2 owner inputs (owner): done — новых фактов от него не требуется; выдачу он назвал сам («выдай
  наряд на t-react-2»)
- 3 do the work: done — наряд написан по контракту 36; продукт перемерен в байтах по всей
  поверхности задачи, слоты прочитаны файлом состояния, устаревшие ссылки карточки исправлены в
  наряде и названы вслух
- 4 self-check: done — каждая строка карточки разложена таблицей происхождения; расхождение карточки
  с байтами названо; невозможность headless-замера обоснована чтением, а не мнением
- 5 close: checkpoint — расписка исполнителя по построению вне этой ноги; задача остаётся `open`,
  карточка наряда `ready`

## Гейты

- **G1.** Дорожка `householder` освободилась закрытием `t-react-1`; её новый корень — единственный.
  `cargo-hands` держит свой. Лимит `NOW.track_wip_limit: 2` не превышен.
- **G2.** Задача служит активной ставке; ничего вне неё не втянуто.
- **G3.** Аппетит не тронут: наряд закрывает ровно подписанные строки. Обнаруженное уменьшение
  объёма (половина строки 2 уже сделана) прямо запрещено тратить на новое.
- **G5.** Здесь ничего не закрывается; закрытие — отдельная нога после публикации.
- **G7.** Ничего продуктового или геймплейного этой ногой не решено: что именно значит «не
  дёргается», оставлено правилу и числу продуктового плана, а не выбрано ногой.
- **G9.** Карточки задачи и ставки не редактировались.
- **evaluator, клауза 3.** Исполнена формой выдачи: полный текст отдаётся пастой/вложением, и первая
  строка возврата обязана быть распиской с числом строк `done_when`.

## log

наряд на `t-react-2` выпущен в WIN-U2 от базы 2f7f2e87, и главное, что дала выдача, — карточка задачи ссылается на код, которого больше нет: `t-react-1` переписала Householder.cs, и правило про две одновременные разнородные реакции живёт теперь на :1072-1081 и не отвергает вторую ситуацию, а БРОСАЕТ исключение; перемерено, что это не регресс прошлой задачи — тот же throw с тем же текстом стоял на базе волны 4cf2b5dc:1040; открыть приём значит убрать бросок, а не ослабить фильтр, и два глушителя выше по течению названы поимённо, причём доксртока над TryHear сама себя приговорила словами «если у его правил появится способ предложить событие и получить отказ, это уходит»; задача оказалась МЕНЬШЕ, чем при нарезке: половина политики прерывания уже стоит полем RequiresSafeInterruption с t-react-1, а весь список ситуаций лежит строкой выше вызова Observe — и наряд прямо запрещает тратить сэкономленное на новое; найдена мина второго входа событий (TakeHeardEvent реализуют четыре существующих тестовых двойника, интерфейс расширять запрещено); строку про замер шороха волочения headless закрыть нельзя, потому что число силы рождается в MonoBehaviour прямо из решателя Unity, поэтому замер поставлен ПЕРВЫМ действием и ему разрешён СТОП домой одним блокером, а пересчитать вместо замера запрещено явно

## next

1. **Владелец отдаёт наряд исполнителю** — полный текст, и первая строка возврата обязана быть
   распиской с числом строк `done_when`. Без расписки наряд не выдан.
2. Возврат приходит HOME; `t-react-2` закрывается отдельной ногой Направления после публикации.
3. **Светлое закрытие `t-react-2` НЕ гарантировано заранее:** строки 1–3 и 5 перемеряются по байтам,
   но строка про замер шороха — это ЧИСЛО, снятое чужим прогоном, которого закрывающая нога сама не
   делает. Если замер состоится, закрытие идёт через свежую биндинговую проверку в другом чате; если
   вернётся блокером — маршрут решает та нога по факту.
4. **Дорожка `cargo-hands` по-прежнему ждёт его рук:** `t-cargo-1` не закрывается без его слов про
   провисание, и её откат сдвинул бы базу под этой работой.
