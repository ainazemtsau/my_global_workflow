# RESULT — s-work-g-6b13-foundation-call-issued-001

play: work
direction: indie-game-development
node: g-6b13
task: a-1b
date: 2026-08-02

## outcome

Выпущен наряд на фундамент: слой правил и первое правило в нём, один игрок ходит, сети нет.
Наряд построен так, чтобы оба провала этой полосы не повторились.

## evidence

**Состояние слотов проверено первой рукой перед выпуском, а не заявлено:**

| Слот | База | `check.ps1` | Дерево |
|---|---|---|---|
| `WIN-U1` | `4e95845c` (старая) | 854 строки | чистое |
| `WIN-U3` | `c75015a8` (= `main`) | **224 строки** | чистое |
| `WIN-U4` | `4e95845c` (старая) | 854 строки | чистое |

Отсюда рекомендация `WIN-U3`: он единственный не требует обновления и стартует на быстром контуре.
Для `U1`/`U4` в наряд внесено обновление первым действием.

**Три вещи, которых у прошлых нарядов не было, и каждая закрывает конкретный случившийся провал:**

1. **Два захода: план с ADR, СТОП, возврат домой — и только потом постройка.** Причина в наряде
   названа прямо, а не как процедура: прошлая попытка построила рабочий код за тридцать минут и
   была отклонена ЦЕЛИКОМ за расслоение. План заставил бы написать «движение кладу в сетевой
   компонент» одной строкой ЗАРАНЕЕ.
2. **Обязательное чтение страницы об устройстве перед планом**, и правило «упёрся в необходимость
   менять курьера — СТОП и вопрос владельцу, ни при каких обстоятельствах не решение исполнителя».
3. **Требование расширяемости и границы слоёв записаны явно.** Их отсутствие в прошлом наряде было
   прямой причиной отклонения.

**Тест на вызов правила вне Unity объявлен обязательным и обоснован:** глазами невозможно увидеть,
в правильном ли слое лежит правило, поэтому запрет «не проверять то, что владелец видит сам» на него
не распространяется. Граница между законным и запрещённым тестом проведена явно, чтобы исполнитель
не угадывал.

**Отклонённая попытка названа в наряде образцом того, чего делать нельзя, с указанием файлов** —
и брать оттуда запрещено.

## state_changes

`NOW.md`: новый `open_call` `c-exec-rules-layer-and-single-walker-001` для `a-1b`.
`work/`: наряд `c-exec-rules-layer-and-single-walker-001-call.md`.

## captures

Нет.

## decisions_needed

Нет.

## play_check

- Слот не назначен сессией: правило продуктового репозитория оставляет выбор владельцу, но
  состояние всех кандидатов измерено и приложено, чтобы выбор был осознанным.
- Плановая стадия возвращена по прямому требованию владельца и ограничена строительными задачами;
  для уборочных остаётся выключенной, как он и сказал.
- В наряде нет ни одного требования к тому, как, сколько раз и с кем владелец проверяет.
- Ни одной новой проверки «на то, что видно глазами»; единственный требуемый тест обоснован тем,
  что глазами он не проверяется в принципе.
- Игровые решения исполнителю не делегированы и не выдуманы сессией: «самое тупое, что работает»
  плюс прямой маршрут вопроса домой.

## log

выпущен наряд на фундамент: слой правил, план с ADR и стоп до постройки, слот с проверенным состоянием

## next

Возврат плана домой. После его принятия — постройка. Затем задача про второго игрока, где то же
правило не меняется ни строкой.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-work-g-6b13-foundation-call-issued-001.md
