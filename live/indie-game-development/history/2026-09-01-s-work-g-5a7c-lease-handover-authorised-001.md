# RESULT — s-work-g-5a7c-lease-handover-authorised-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-noise-reaches-by-distance-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — передача аренды разрешена буквально, но только после спасательной ссылки: кандидат слуха существует в одном экземпляре, и штатный ремонт слота стёр бы его целиком

close: light — because всё несущее перевыведено направлением первой рукой: коммит кандидата, чистота дерева, отсутствие его в любых удалённых ветках, состояние реестра. Ничего не закрывается как сделанное; выдано разрешение и одно обязательное предварительное действие.

owner_approved: |
  Не требовалось: передача фазы аренды внутри ОДНОЙ И ТОЙ ЖЕ задачи — механика слотов.

  Владельцу сообщается фактом, потому что это **третий раз за две недели**, когда реестр слотов
  расходится с действительностью, и первые два стоили ему ложных докладов.

outcome: |
  **СТОРОЖ ОТРАБОТАЛ ПРАВИЛЬНО, И ОТКАЗ ИСПОЛНИТЕЛЯ ВЕРЕН.** Штатный `-Release` дал `Access
  denied`, повтор был отклонён как снятие активной аренды другой задачи, ручная правка JSON после
  отказа запрещена. **Требование буквального подтверждения — не бюрократия, а ровно то, ради чего
  сторож стоит.**

  **НО ГЛАВНОЕ В ЭТОЙ НОГЕ — РИСК, КОТОРОГО ИСПОЛНИТЕЛЬ НЕ НАЗВАЛ, А НАПРАВЛЕНИЕ НАШЛО.**
  Он собирался после захвата запустить `preflight -Repair`. Перепроверено первой рукой:
  **кандидат `b4b010c1` не содержится ни в одной удалённой ветке и живёт только как локальная
  `slot/win-u2`.** Обычная операция «привести слот к базе» **стёрла бы всю ногу слуха целиком** —
  семь файлов, `747 / 747 / 0`, дефект порядка, найденный самой ногой.

  **ЭТО РОВНО ТОТ КЛАСС, КОТОРЫЙ У НАПРАВЛЕНИЯ УЖЕ СЛУЧАЛСЯ**
  (`i-accepted-work-lives-only-in-a-released-slot-001`), и **сегодня он подтвердился ЗЕРКАЛЬНО**:
  тогда аренда исчезла раньше работы, теперь аренда пережила работу. **Общий корень один: аренда —
  заявление о действительности, которое никто с действительностью не сверяет.** Она не
  освобождается сама, когда работа сдана, и не удерживается сама, когда работа не сохранена.

  **ПОЭТОМУ РАЗРЕШЕНИЕ ВЫДАНО УСЛОВНЫМ, И УСЛОВИЕ ИДЁТ ПЕРВЫМ ШАГОМ:** спасательная ссылка
  `keep/hearing-b4b010c1` создаётся ДО передачи аренды. Локальная ветка, ничего не публикует.
  Плюс отдельная проверка после `-Repair`: если он сбросил `slot/win-u2` — восстановить и
  **доложить отдельной строкой**, потому что это будет доказанный дефект штатного инструмента и он
  дороже самого ревью.

  **И ПРАВИЛО, КОТОРОГО НЕ БЫЛО НИ В ОДНОМ ТЕКСТЕ НАРЯДА, НАКОНЕЦ ЗАПИСАНО:** «слот меняет аренду
  только после того, как его работа опубликована либо сохранена отдельной ссылкой». Улика открыта с
  2026-08-06 именно с этим условием закрытия. **Половина условия выполнена сегодня**; вторая —
  правило должно стоять в тексте, который читают все ноги, а не в одном наряде.

  **ЖИВАЯ ЧАСТЬ G5 НЕ СДЕЛАНА ВТОРОЙ РАЗ ПОДРЯД.** Первый раз виноват был наряд, второй раз —
  сторож, отработавший по назначению. Ни одного живого замера слуха по-прежнему нет, и задача
  по-прежнему без вердикта. **Это надо называть прямо, а не растворять в отчёте о процессе.**

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** в `C:\projects\Unity\GasCoopGame_win-u2`:

  - `git rev-parse --short HEAD` → **`b4b010c1`**; `git status --porcelain` → **0 строк**, дерево
    чистое;
  - `git branch -r --contains b4b010c1` → **пусто**: кандидат не существует нигде, кроме локальной
    ветки. Это и есть основание требовать спасательную ссылку;
  - реестр `gascoop-slot-state.v1.json`: `WIN-U2` = `CLAIMED`, аренда
    `t-noise-reaches-by-distance-1:BUILD`; `WIN-U4` = `CLAIMED`, `t-morning-content-1:BUILD`;
    U1 и U3 свободны. **Обе аренды названы по фазе BUILD, обе сборки уже сдали работу** — реестр
    отстаёт от действительности в двух слотах из двух занятых;
  - ранее этой сессией на том же дереве: семь файлов кандидата, `747 / 747 / 0`, ноль пропусков.

  Из возврата ревью, как улика: `select-slot -Release` → `Access denied`; elevated retry отклонён
  сторожем; Unity не вызывался, файлы и реестр не менялись.

state_changes: |
  СОЗДАНО (1 документ):
  - `work/2026-09-01-authorise-lease-handover-win-u2.md` — буквальное разрешение дословно в той
    форме, которую запросил сторож, плюс обязательный шаг 0 (спасательная ссылка) и проверка
    после `-Repair`.

  ОБНОВЛЕНО (1 улика, НЕ закрыта):
  - `i-accepted-work-lives-only-in-a-released-slot-001` — класс подтверждён вторым, зеркальным
    случаем; назван общий корень; в `review_when` записано, что половина условия закрытия
    выполнена, и чего не хватает до второй.

  БЕЗ ИЗМЕНЕНИЙ:
  - `c-g5-g-5a7c-noise-reaches-by-distance-1-002` остаётся `ready`: наряд не переиздаётся, к нему
    выдано разрешение;
  - `t-noise-reaches-by-distance-1` остаётся `active`, **по-прежнему без вердикта**.

captures: |
  - **АРЕНДА СЛОТА — ЗАЯВЛЕНИЕ О ДЕЙСТВИТЕЛЬНОСТИ, КОТОРОЕ НИКТО С НЕЙ НЕ СВЕРЯЕТ.** Оба занятых
    слота помечены фазой `BUILD`, и **обе сборки уже сдали работу**. **Правило-кандидат: аренда
    должна освобождаться сдачей работы, а не памятью исполнителя; пока этого нет, реестр читать
    как «кто взял», а не как «кто работает».**
  - **РАЗРЕШЕНИЕ, ВЫДАННОЕ БЕЗ ПРОВЕРКИ СОХРАННОСТИ, УНИЧТОЖИЛО БЫ НОГУ.** Исполнитель просил
    только подтверждения и собирался сразу за ним запустить штатный ремонт. **Правило-кандидат:
    на любую просьбу освободить слот сначала спрашивать, где ещё существует его работа — и если
    нигде, требовать ссылку ДО, а не после.**
  - **СТОРОЖ, ОТКЛОНИВШИЙ УКАЗАНИЕ ИЗ ТЕКСТА НАРЯДА, БЫЛ ПРАВ.** Наряд писал «перепиши реестр»;
    сторож потребовал отдельного буквального подтверждения. **Текст наряда не должен быть
    достаточным основанием для снятия чужой аренды** — иначе сторож бесполезен.
  - **ДВА ЗАХОДА G5 ПОДРЯД БЕЗ ЕДИНОГО ЖИВОГО ЗАМЕРА.** Первый — вина наряда, второй — работа
    сторожа. **Это надо называть прямо: слух построен, перевыверен на байтах и до сих пор не
    проверен живьём никем, кроме самой сборки.**

decisions_needed: |
  Ни одного владельческого.

play_check: |
  1. OPEN — возврат ревью с запросом буквального подтверждения.
  2. WORK — сохранность кандидата проверена первой рукой ДО выдачи разрешения, и найден риск, не
     названный исполнителем; разрешение выдано условным, с обязательным первым шагом; класс улики
     подтверждён вторым, зеркальным случаем и получил общий корень.
  3. (owner) — не запрашивался: механика слотов; расхождение реестра сообщено фактом.
  4. CLOSE — задача по-прежнему не закрыта; разрешение уходит владельцу.

log: |
  передача аренды WIN-U2 разрешена буквально — но только после спасательной ссылки: кандидат слуха
  не существует нигде, кроме локальной ветки, и штатный preflight -Repair стёр бы всю ногу целиком;
  сторож, отклонивший указание наряда, был прав; класс улики про аренду подтвердился зеркально —
  тогда аренда исчезла раньше работы, теперь пережила её, и оба занятых слота помечены BUILD при
  уже сданных сборках; живого замера слуха по-прежнему нет ни одного

next: |
  Разрешение уходит владельцу для передачи в **WIN-U2**: сначала `keep/hearing-b4b010c1`, потом
  передача аренды, потом живые замеры по наряду `-002`.

  Волна: голова без вердикта третий заход, ходьба мертва, граница ответа в работе в U4, публикация
  ждёт обоих.
