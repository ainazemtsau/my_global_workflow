# RESULT — s-work-g-6b13-a1-scaffold-return-001

play: work
direction: indie-game-development
node: g-6b13
task: a-1
date: 2026-08-02

## outcome

Возврат по a-1 принят как УЛИКА и перепроверен первой рукой. Задача НЕ закрыта: её `done_when` —
глаз владельца, а его Play ещё не было. Границы наряда соблюдены полностью, отклонений нет.
Два честных факта переданы владельцу до того, как он нажмёт Play.

## evidence

Коммит `e0000c5b` («feat: add TunnelCrew empty scene scaffold»), ветка `slot/win-u2`, проверен в
`C:/projects/Unity/GasCoopGame_win-u2` только чтением.

**Что реально легло поверх main — восемь файлов, 192 строки:** `Assets/TunnelCrew.meta`,
`Assets/TunnelCrew/Core.meta`, `Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef` (+`.meta`),
`Assets/TunnelCrew/Scenes.meta`, `Assets/TunnelCrew/Scenes/EmptyScene.unity` (+`.meta`),
`core/TunnelCrew.Core.csproj`.

**Границы наряда — соблюдены, проверено по диффу:** `tools/`, `validation.config`, `check.ps1`,
`Assets/GasCoopGame/**` и `tests/GasCoopGame.Core.Tests` не тронуты ни одной строкой. Ни одного
файла с брошенной ветки `slot/win-u1`. `SourcePresenceGateTests` отсутствует. Каркас написан с нуля,
как и требовал наряд. Заявление исполнителя «старый код, тесты и гейты не тронуты» подтвердилось.

**Сцена настоящая:** валидный Unity-YAML, пять блоков настроек (occlusion, render, lightmap,
navmesh, roots).

**ДВА ФАКТА, которых в возврате не было, и оба названы владельцу:**

1. **В сцене НОЛЬ GameObject-ов** — ни камеры, ни света. При Play в Game-окне будет чёрный экран и
   «No cameras rendering». Это НЕ ошибка исполнителя: `done_when` наряда сказал «больше в ней ничего
   нет», и это формулировка сессии. Камера приходит с `a-2` вместе с игроками.
2. **`.cs`-файлов в новой папке ноль**, а `core/TunnelCrew.Core.csproj` несёт
   `EnableDefaultCompileItems=false` с единственным glob-ом `..\Assets\TunnelCrew\Core\**\*.cs`.
   Значит «focused build — 0 ошибок» это сборка пустоты, и доказательством она не является.
   Сторожа на это НЕ заводить — `knowledge/no-checks-the-owner-makes-by-eye.md`. Факт перестаёт
   что-либо значить с первой строкой настоящего кода в `a-2`.

**Проверено попутно, обе вещи подтвердились:** владелец сбросил `WIN-U1` полностью — слот
`AVAILABLE` с пустой арендой И ветка `slot/win-u1` пуста относительно `main`, квитанций мёртвого
корня в ней нет. Второй `i-grid-v1-g01` не завёлся. `WIN-U3` держит аренду
`c-exec-gate-surface-cut-001:BUILD` — работа по проверкам идёт.

## state_changes

`NOW.md`: `a-1` → `returned` с перепроверенными фактами (не `done`); наряд
`c-exec-newgame-folder-and-scene-001` → `returned`; `c-exec-gate-surface-cut-001` → `running` с
подтверждённой арендой; мёртвый наряд — запись про `WIN-U1` закрыта фактом уборки.

## captures

Ветка `a-1` не несёт ни openspec-изменения, ни квитанций корня — наряд просил минимум бумаги, и для
каркаса без единой строки логики это верно. НО `check.ps1 -Deliver` требует закрывающий отчёт по
`changeId`, поэтому слияние `slot/win-u2` в `main` в нынешнем контуре упрётся в гейт. Решать это
после возврата `c-1`, который прямо сейчас этот контур и режет: мерить сейчас — мерить движущуюся
цель.

## decisions_needed

Нет. Владельцу передан один необязательный выбор: добавлять камеру сейчас или ждать `a-2`.

## play_check

- Возврат исполнителя обработан как улика, не как закрытие — AGENTS.md прямо это требует.
- `a-1` НЕ отмечена `done`: её `done_when` это Play владельца, и самому себе его не засчитать
  (`feedback-owner-run-unity-steps-real-gate`).
- Каждое утверждение возврата перепроверено по диффу; ни одно не принято на слово.
- Найдены два факта, которых в возврате не было, и оба переданы до его Play, а не после.
- Новых проверок не предложено ни одной; факт про пустую сборку записан как факт, а не как повод
  завести сторожа.

## log

возврат a-1 перепроверен первой рукой: границы соблюдены, задача ждёт Play владельца

## next

Владелец жмёт Play. Его слово закрывает `a-1`. Параллельно идёт `c-1` в `WIN-U3`.
Следующий наряд — `a-2` (двое по сети в одной сцене), выпускается после его Play.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-work-g-6b13-a1-scaffold-return-001.md
