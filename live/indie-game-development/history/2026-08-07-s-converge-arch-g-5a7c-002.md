RESULT s-converge-arch-g-5a7c-002 (call: c-converge-arch-g-5a7c-001, продолжение)
direction: indie-game-development   play: converge-arch   node/task: g-5a7c

outcome: |
  **Единственная открытая строка архитектурного разбора закрыта вердиктом владельца в тот же день, в
  который была ему вынесена.**

  Нога 001 разобрала девять высокорисковых мест, восемь закрыла выводом по опровержению и одно —
  вопрос геймплея, «двигается ли обстановка, когда её задели» — вынесла владельцу с обоими
  вариантами, измеренной ценой каждого, рекомендацией и названным доводом ПРОТИВ рекомендации.
  **Он ответил: «А, обстановка стоит».**

  Эта нога записала его ответ в набор, привела счёт к новому состоянию и дополнила уже выпущенный
  наряд проверки. Ничего больше она не делает: ни одной новой строки разбора не заведено, ни один
  вывод 001 не пересмотрен.

  **ЧТО КУПЛЕНО ЕГО ОТВЕТОМ.** Обстановка — неподвижная вещь: её коллайдер останавливает груз, она
  входит в описание дома наравне со стеной (арх A2), а шум даёт контакт груза с ней через уже
  построенный шов «контакт производит шум». Новых физических тел не заводится, снимок не растёт,
  судья по-прежнему гоняет ровно одно физическое тело.

  **ЧТО СОЗНАТЕЛЬНО НЕ ЗАКРЫТО, и это записано, чтобы дверь не захлопнули за него.** Подвижная
  обстановка не запрещена и не отменена. Он выбирал из пары, где второй вариант был прямо предложен
  «отдельным решением, когда он его захочет»; значит она просто не покупается здесь и сейчас, а её
  возвращение остаётся его отдельным словом. Общий слой шума (арх A5) делает это возвращение
  дешёвым: подвижный предмет становится ещё одним источником, а не новым механизмом.

  **СИЛА ТОКЕНА ЗАПИСАНА БЕЗ ПОВЫШЕНИЯ, одинаково во всех местах, куда он попал.** Это вердикт по
  ВАРИАНТУ, усиленный тем, что он назвал выбор своими словами, а не одной буквой, — то есть сказал,
  что именно выбрал. Это НЕ подпись под текстом карточки. Карточка `g-5a7c` этим ответом не тронута и
  правки не требует: критерий 5 говорит про обстановку, которую задевают, и про шов шума, и обе
  половины остаются верными при обоих вариантах.

  **ОДНА ОГОВОРКА ОСТАВЛЕНА ПРОЧТЕНИЕМ НОГИ И ЕГО СЛОВОМ НЕ СТАЛА.** «Несущие задевают обстановку»
  буквально означает, что задевают ЛЮДИ; у ходоков нет коллайдеров нигде в игре, и движок не может
  сообщить о столкновении с ходоком никогда. Разбор читает критерий так, что задевают несущие ВМЕСТЕ
  С ГРУЗОМ, и опирается это прочтение на текст самого критерия 5 («задевание идёт ТЕМ ЖЕ швом»), а не
  на его ответ. Прочтение помечено прочтением и в разборе, и в наряде проверки.

evidence: |
  **Его слова, дословно и целиком:** «А, обстановка стоит».

  **Контекст, в котором они даны, и это важно для силы токена.** Ответ дан на показанный ему целиком
  разбор A9: оба варианта названы («стоит» / «падает и катится»), цена каждого измерена и показана
  (у первого — ни одного нового физического тела и ни одной новой строки в снимке; у второго — хост
  считает десятки тел вместо одного и каждый предмет едет в снимке на четыре копии каждый тик),
  рекомендация дана («А сейчас, Б — отдельным решением, когда захочешь») и рядом с ней назван довод
  ПРОТИВ неё («в играх, на которые ты ссылаешься, падающие и ломающиеся вещи — половина
  удовольствия, и это твоя область, не моя»). То есть он выбирал, видя обе стороны и цену обеих.

  **Куда его ответ записан, три места, и во всех трёх — одной и той же силы:**

  1. `work/converge-g-5a7c-arch.md`, раздел A9 — заголовок сменил `open → ВЛАДЕЛЕЦ` на «ЗАКРЫТО ЕГО
     СЛОВОМ 2026-08-07»; добавлен подраздел с его дословными словами, разбором силы токена, тем, что
     закрыто, тем, что НЕ закрыто, и оговоркой про прочтение. Метка строки:
     `answered · вердикт владельца по варианту` (была `open → OWNER`).
  2. `work/converge-g-5a7c.md` §CONTRACTS, ряд C25 — статус `open → OWNER` сменён на «закрыт
     вердиктом владельца», содержание контракта дописано его ответом. **Ряды C01–C24 не тронуты ни
     одной ячейкой.**
  3. §SIGNOFF обоих файлов — «подписей владельца НОЛЬ» приведено к честному «вердиктов владельца
     ровно один, подписей под текстом карточки — ноль».

  **Счёт набора после ответа, с прежними числами рядом, чтобы было видно, что изменилось:**

  - `contracts_open: 2 → 1` — остался `C05` (дефект `why`/`not_inherited` у `g-c519`, маршрут `map`);
  - `arch_open: 9 → 8` — A1–A8 с рекомендованным ответом и обязывающим ADR у PLAN;
  - вердиктов владельца в наборе: `0 → 1`; подписей под текстом карточки: `0` (не изменилось);
  - `open_rows` §WHAT: `1` (`W103` закрыта маршрутом ещё ногой 001), `deferred: 2` (`W15`, `W16`) —
    не изменилось.

  **Наряд проверки НЕ переоткрывался и НЕ переиздавался.** `c-converge-verify-g-5a7c-001` остаётся
  тем же id и тем же `ready`-фронтиром; он дополнен пометкой `amended`, приведённым счётом и НОВОЙ
  атакой `3a`, которая требует проверить именно этот токен: не повышена ли его сила ни в одном из
  трёх мест; не выведено ли из него больше, чем он сказал (в частности, утверждение набора, что
  подвижная обстановка не запрещена); и не превратилась ли оговорка про «несущие задевают» в его
  слово.

  **`NOW.md`:** решение `d-furniture-moves-or-stands-001` СНЯТО как отвеченное — по гигиене отвеченное
  решение в `decisions` не живёт, а его содержание переехало в набор и в строку-комментарий.

  **Карточка `g-5a7c` не тронута ни знаком. `TREE.md` и `CHARTER.md` не открывались на запись.
  Продуктовый репозиторий не изменялся. `knowledge/` не тронуто.**

state_changes: |
  1. `work/converge-g-5a7c-arch.md`:
     - шапка: абзац «Подписей владельца этой ногой не получено НИ ОДНОЙ» заменён на честную формулу
       «вынесен ровно один вопрос, он ответил в тот же день»;
     - A9: заголовок и закрывающий абзац заменены; добавлен подраздел «ЕГО ОТВЕТ 2026-08-07 — вариант
       А» с дословными словами, силой токена, купленным, НЕ закрытым и оговоркой-прочтением;
     - §NOT-DECIDED: строка про обстановку зачёркнута с пометкой «закрыто его словом» (не удалена —
       чтобы было видно, что вопрос задавался и получил ответ); добавлена строка «вернётся ли
       подвижная обстановка когда-нибудь — его отдельным словом»;
     - §SIGNOFF Architect: переписан под один полученный вердикт; счёт `arch_open: 9 → 8`,
       вердиктов `0 → 1`.
     - Разделы §RISK-LINE, §DECOMPOSE, A1–A8 и §MINER **не тронуты ни строкой**.

  2. `work/converge-g-5a7c.md`:
     - §CONTRACTS, правила чтения: последняя фраза про две ждущие строки приведена к одной (`C05`);
     - §CONTRACTS, ряд `C25`: статус и содержание заменены на закрытие вердиктом владельца.
       **Ряды `C01`–`C24` сохранить без изменений**;
     - §CONTRACTS, «Счёт этой ноги»: первый абзац переписан под один полученный вердикт; блок счёта —
       `contracts_open: 2 → 1`, `arch_open: 9 → 8`; строки `contract_coverage`,
       `arch_in_context_only` и `canon_proposed` сохранить без изменений;
     - **§IMPORT, §DRIFT, §GLOSSARY, §WHAT и оба прежних §SIGNOFF файла НЕ ТРОГАТЬ.**

  3. `work/c-converge-verify-g-5a7c-001-call.md` — тот же id, тот же `status: ready`:
     - добавлено поле `amended:` с автором и причиной;
     - блок счёта приведён к `contracts_open: 1` и `arch_open: 8` с названными прежними числами;
     - добавлена атака `3a` на силу единственного вердикта владельца;
     - `boundaries`: строка про ждущее решение заменена на «ждущих его вопросов в наборе больше нет,
       снятое решение не воскрешать»;
     - `done_when` 5: в перечень токенов добавлено «А, обстановка стоит».

  4. `NOW.md`:
     - шапка `updated:` → `2026-08-07 by s-converge-arch-g-5a7c-002`;
     - `decisions`: УДАЛИТЬ `d-furniture-moves-or-stands-001` как отвеченное.
       `d-agents-skills-belong-in-repo-001` СОХРАНИТЬ без изменений;
     - ДОБАВИТЬ строку-комментарий о его вердикте после существующего комментария ноги 001;
     - `open_calls` не трогать: `c-converge-verify-g-5a7c-001` остаётся единственным фронтиром,
       `status: ready`, id и файл прежние;
     - `bet: null`, `tasks: []`, `recurring: []`, `direction_forecast` и все записи `issues` —
       без изменений.

  5. `LOG.md` — ДОБАВИТЬ одну строку сверху, перед строкой `s-converge-arch-g-5a7c-001`.

  6. `history/2026-08-07-s-converge-arch-g-5a7c-002.md` — этот RESULT целиком.

  7. `TREE.md`, `CHARTER.md`, `knowledge/`, продуктовый репозиторий — НЕ ТРОГАТЬ.

captures:
  - "Возвращение подвижной обстановки, если он его когда-нибудь захочет, стоит дёшево ровно потому, что общий слой шума принимает новый источник без переписывания слоя. Записано как свойство выбранной архитектуры, а не как план."

decisions_needed: []

play_check:
  - "1 Declare: n/a — контракты закрыты ногой 001; эта нога изменила ровно одну ячейку статуса и одну ячейку содержания у ряда C25 и не трогала ни один другой ряд."
  - "3 Architect (owner): done — вердикт владельца получен и записан. Его слова дословно: «А, обстановка стоит», даны на показанный целиком разбор с обоими вариантами, измеренной ценой каждого, рекомендацией и названным доводом против рекомендации. Сила токена записана как вердикт по варианту, усиленный его собственной формулировкой выбора, и прямо объявлена НЕ подписью под текстом карточки; карточка не тронута."
  - "4 Close & route: done — `contract_coverage` не изменилось (ряды C01-C24 не тронуты); `contracts_open: 2 → 1`; `arch_open: 9 → 8`; `arch_in_context_only: PASS` держится — ни одна строка арх-файла не стала требованием и в `done_when` не поехала. Фронтир прежний: `c-converge-verify-g-5a7c-001`, дополнен под новый счёт и не переиздан."
  - "G7: соблюдён в обе стороны — вопрос был вынесен ему с опциями и рекомендацией, ответ получен его словами и не додуман. То, чего он не сказал (запрет на подвижную обстановку в будущем; прочтение «кто именно задевает»), названо не сказанным."
  - "G10: RESULT финальный, состояние написано только через `state_changes`, `(owner)`-шаг несёт его настоящие слова."

log: "**ЕДИНСТВЕННАЯ ОТКРЫТАЯ СТРОКА АРХИТЕКТУРНОГО РАЗБОРА `g-5a7c` ЗАКРЫТА ЕГО СЛОВОМ В ТОТ ЖЕ ДЕНЬ:** «А, обстановка стоит». Сила токена — вердикт по варианту, усиленный его собственной формулировкой; подписью под текстом карточки не является, карточка не тронута. Куплено: обстановка неподвижна, входит в описание дома наравне со стеной, шум даёт контакт груза уже построенным швом — ни одного нового физического тела. НЕ закрыто и записано: подвижная обстановка не запрещена, её возвращение остаётся его отдельным словом. Оговорка про то, кто именно задевает обстановку, оставлена прочтением ноги. Счёт: `contracts_open` 2 → 1, `arch_open` 9 → 8, вердиктов владельца 0 → 1. Решение снято как отвеченное; наряд проверки дополнен атакой на силу этого токена и не переиздан."

next: |
  `c-converge-verify-g-5a7c-001` — прежний и единственный фронтир, свежая ОТДЕЛЬНАЯ физическая сессия.
  Ждущих вопросов у владельца в наборе больше нет.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-002.md
