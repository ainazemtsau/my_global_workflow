# RESULT — s-work-g-5a7c-situation-1-dispatch-001

play: work · close: checkpoint — наряд написан, заполнен измеренными значениями и выдан готовой
пастой; расписка исполнителя и его возврат по построению не могут существовать внутри этой ноги,
поэтому `t-situation-1` остаётся открытой, а фронтир — `waiting`
direction: indie-game-development · node: g-5a7c · task: t-situation-1 · call: c-work-g-5a7c-situation-1-001
date: 2026-08-10

base_read: OS — `wt/indie-game-development` = `8213e905`, рабочее дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` = `origin/slot/win-u1` =
`43648b2dd4fbaaf9570bd56cdf440cc904e1c222`, дерево `7a05d905d95915f29286ab666fe6aca853301abf`.
Слот WIN-U1 перемерен настоящим селектором (только чтение): `CLEAN / AVAILABLE / lease none`,
`branch: slot/win-u1`, `head: 43648b2d`, `mcp-endpoint: unrecorded`. Продуктовые байты, ветки,
слоты и slot-state этой ногой НЕ изменялись.

## outcome

Наряд `c-exec-g-5a7c-situation-1-001` на общую задачу-шов `t-situation-1` написан целиком, заполнен
измеренными значениями (базис, дерево, слот, пин), зарегистрирован карточкой в статусе `ready` и
**выдан готовой пастой с полным текстом** — той единственной формой выдачи, отсутствие которой
стоило первой волне круга работы (`knowledge/an-order-does-not-exist-until-it-is-delivered.md`).

Шесть строк `done_when` карточки задачи свёрнуты в ТРИ строки наряда — потолок инженерного CALL по
контракту 36 (`CONTOUR.md`, «CALL budget (v35)»). Ни одна строка задачи не потеряна и ни одна не
дописана; соответствие показано таблицей ниже.

**Нога нашла один факт, которого не назвала ни карточка задачи, ни улика, ни шейп: починка единиц
силы — не нейтральная правка.** Число силы вырастет ровно в `1.2` раза при неподвижных порогах
`60 / 150 / 400`, то есть дом станет слышать примерно на 20% раньше. Это записано в наряде как
ЕДИНСТВЕННОЕ разрешённое отличие от вчерашнего поведения, а не спрятано под строку «поведение не
изменилось ни на грамм», и вынесено владельцу отдельной карточкой вопроса.

**Расписки в этой ноге нет и быть не может.** Наряд исполняет отдельная продуктовая сессия; первая
строка её возврата и есть расписка. Поэтому `t-situation-1` остаётся `open`, обе дорожки остаются
закрытыми, а фронтир `c-work-g-5a7c-situation-1-001` переведён в `waiting` на этот наряд.

## evidence — перемерено первой рукой на `43648b2d`, а не принято с отчёта

**База и пин.** `git ls-remote origin refs/heads/dev refs/heads/main refs/heads/slot/win-u1` — одно
и то же `43648b2dd4fbaaf9570bd56cdf440cc904e1c222`; дерево `7a05d905d9…`. `os/engineering/
CONTRACT_VERSION` `current: 36` против продуктового `validation.config` `synced_contract_version:
36` — Re-sync не нужен. Локальная `main` в слоте = `11f8ddbf`, `main...origin/main` = `0 27`:
отстала на 27 коммитов, названо в наряде вместе со взведённой уликой
`i-deliver-gate-red-when-main-catches-dev-001` (`origin/main` догнал `origin/dev`).

**Слот.** `tools/select-slot.ps1 -Slot WIN-U1` (только чтение), прогнан в этой ноге: `state: CLEAN`,
`lifecycle: AVAILABLE`, `lease: none`, `head: 43648b2d…` — знак в знак базис наряда. Владелец слот
этой ногой не назывался: все четыре слота `AVAILABLE / lease none` по
`gascoop-slot-state.v1.json`, и WIN-U1 — единственный, чья голова уже равна базису. Выбор
механический и обратимый; его слово ничего здесь не решает и потому не спрашивалось.

**Дорога от груза к хозяину — все шесть звеньев прочитаны в байтах** и вписаны в наряд по именам и
номерам строк: производитель `AuthoritativeCargoRoster.cs:152`/`:468`, буфер на тик `:61`/`:69`/
`:117`/`:155`, перевод `CargoNoise.cs:79-104`, свёртка «одна самая громкая» `CargoNoise.cs:129-134`
+ `:125`/`:156-158`/`:178`, **мост лично к хозяину `Core/Multiplayer/CarryWorld.cs:114`**
(`Householder.Observe(Cargo.Stimulus, tickSeconds)`), потребитель
`AuthoritativeHouseholder.cs:172`.

**Главный гейт замерен ДО работы, чтобы «пусто» было проверяемым, а не декларативным.**
`grep -rnic "cargo" Assets/TunnelCrew/Core/Householder/*.cs` на `43648b2d`:
`AuthoritativeHouseholder.cs` — **6**, `Householder.cs` — **17**, `HouseholderRoutineProfile.cs` —
**0**. Итого **23**. Наряд требует ровно ноль и требует вставить вывод команды дословно.

**Найдено сверх карточки: имя груза живёт и СНАРУЖИ `Core`, в сериализованных байтах.**
`World/HouseholderWalkSettings.cs:16` — поле `cargoLandedLifetimeSeconds`, `:36` — его свойство;
`Settings/HouseholderWalkSettings.asset:21` — `cargoLandedLifetimeSeconds: 8`;
`World/HouseholderRouteController.cs:167`, `:196` — два чтения. Переименование поля без правки
ассета или без `[FormerlySerializedAs]` молча обнулило бы восьмёрку, и хозяин перестал бы помнить
приземление. Опасность названа в наряде, а сохранение числа `8` — во второй строке `done_when`.

**Единицы силы — цепочка перемерена целиком, и число оказалось НЕ тем, что можно было предположить
из улики.** `Network/CargoBody.cs:123` — `Physics.simulationMode = SimulationMode.Script`;
`:273-274` — `step = seconds / _settings.PhysicsStepsPerTick`, `:288` — `Physics.Simulate(step)`;
`NetworkPlaySettings.asset:59` — `_physicsStepsPerTick: 2`; FishNet `TimeManager` не сериализован ни
в `Scenes/IntegratedHouse.unity`, ни в `Network/Prefabs/NetworkRuntime.prefab` (поиск по GUID
`3fdaae44044276a49a52229c1597e33b` пуст) → действует умолчание `_tickRate = 30`
(`Packages/com.firstgeargames.fishnet/Runtime/Managing/Timing/TimeManager.cs:184`), тик ≈ `0.03333`.
**Настоящий шаг симуляции ≈ `0.03333 / 2` = `0.016667` — то есть «≈0.0167» из улики это ШАГ, а не
тик, и делить на тик было бы второй ошибкой.** `CargoBodyContact.cs:84-89` делит на
`Time.fixedDeltaTime`, а `ProjectSettings/TimeManager.asset` даёт `Fixed Timestep: 0.02`;
`Physics.Simulate` это поле не меняет. `0.02 / 0.016667 = 1.2`. Частота тика при этом ВЫВЕДЕНА из
умолчания, а не снята с живого редактора — так и написано в наряде, и замер настоящего шага сделан
частью работы исполнителя.

**Наряд физически выдан.** Полный текст лежит в
`work/c-exec-g-5a7c-situation-1-001-call.md` И отдан владельцу в чате одной готовой пастой. Первая
строка возврата исполнителя обязана быть распиской, называющей число полученных строк `done_when`
(три) и последние пять слов третьей строки — то есть распиской, которую невозможно выдать, не имея
текста.

### Шесть строк задачи → три строки наряда, ни одна не потеряна

| Строка карточки `t-situation-1` | Куда легла |
|---|---|
| 1 — ситуация названная вещь с видом и местом, список ровно один тик, ни очереди, ни хранения, ни подписчиков | наряд `done_when` 1 |
| 2 — груз кладёт свои ситуации через это место, а не мостом лично к хозяину | наряд `done_when` 1 |
| 3 — в коде хозяина ни одного упоминания груза, поиском по имени | наряд `done_when` 2 (плюс замер `23 → 0` и второй гейт снаружи `Core`) |
| 4 — порядок, слияние и вытеснение названы либо полями, либо явной строкой «пока нет» | наряд `done_when` 1 |
| 5 — поведение не изменилось, доказано существующими зелёными тестами без правки ожиданий | наряд `done_when` 3 (плюс `151/151` как базовое число и дифф по `tests/` как доказательство) |
| 6 — единицы силы починены, пороги `60 / 150 / 400` не двигаются | наряд `done_when` 3 (плюс требование ВЫВОДИТЬ делитель, а не хардкодить) |

## state_changes

1. Новая карточка `c-exec-g-5a7c-situation-1-001` (`_kind: call`, `to: executor`,
   `for: t-situation-1`, `status: ready`, `repo: ainazemtsau/GasCoopGame`,
   `engineering_contract: 36`, `slot: WIN-U1`, `basis: 43648b2d…`). Поля `track` карточка не несёт
   намеренно: общая задача-шов не принадлежит ни одной дорожке.
2. Новый файл наряда `work/c-exec-g-5a7c-situation-1-001-call.md` — полный текст, три строки
   `done_when`.
3. `c-work-g-5a7c-situation-1-001`: `status: ready -> waiting`, добавлено
   `waiting_on: c-exec-g-5a7c-situation-1-001`. Фронтир остаётся открытым: его строки про возврат,
   закрытие и разблокировку дорожек не выполнены.
4. Новая карточка `q-house-hears-20-percent-earlier-001` (`_kind: question`, `about: g-5a7c`,
   `asks: владелец`) — измеренный множитель `1.2` и вопрос, оставляем ли слух дома на 20% раньше.
5. `t-situation-1`, `g-5a7c`, `bet-g-5a7c-wave-2`, `NOW` и обе дорожки НЕ трогать: задача не
   закрыта, лимит дорожек не расходуется, ставка не правится.
6. Одним `osctl leg close` сохранить этот RESULT в
   `history/2026-08-10-s-work-g-5a7c-situation-1-dispatch-001.md` и добавить log ровно один раз в
   журналы `g-5a7c`, `t-situation-1`, `c-work-g-5a7c-situation-1-001`,
   `c-exec-g-5a7c-situation-1-001`, `i-world-situations-need-extensible-reaction-architecture-001`
   и `i-noise-bands-invisible-and-force-mis-scaled-001`.

## captures

- **Имя груза живёт в сериализованном ассете хозяина** (`Settings/HouseholderWalkSettings.asset:21`,
  `cargoLandedLifetimeSeconds: 8`). Не отдельная улика: целиком поглощено второй строкой `done_when`
  этого наряда вместе с требованием сохранить восьмёрку.
- **«≈0.0167» из улики `i-noise-bands-invisible-and-force-mis-scaled-001` — это ШАГ симуляции, а не
  тик.** Тик ≈ `0.0333`, `_physicsStepsPerTick: 2`. Улика не правится этой ногой (`Writes:` плея её
  текст не покрывает); уточнение живёт в наряде и здесь, закрывать её будет своя нога.
- `Editor/HouseholderRouteControllerEditor.cs`, `Network/HouseholderSnapshot.cs` и два комментария в
  `World/HouseholderRouteController.cs` упоминают груз чистой прозой про мир. Гейтом это не
  объявлено намеренно — иначе задача превратилась бы в переписывание комментариев.

## decisions_needed

Один, и он не блокирует работу: `q-house-hears-20-percent-earlier-001`. Починка единиц при
неподвижных порогах делает дом слышащим примерно на 20% раньше; судит это только его ухо.
Наряд идёт без ответа, потому что обе несущие строки (`починить единицы`, `пороги не двигать`)
подписаны им самим, а перекрутка порогов принадлежит `t-react-2`, не этой задаче.

Ждущий вопрос `d-agents-skills-belong-in-repo-001` назван владельцу в начале ноги и остаётся
отложенным его же решением вести его отдельным чатом.

## play_check

- 1 recite: done — шесть строк `done_when` карточки и пять строк фронтира сведены; задача служит
  активной ставке `g-5a7c` и стоит ДО расхождения обеих дорожек
- 2 owner inputs (owner): done — фактов, которые знает только он, у этой ноги нет: слот
  механический и перемерен селектором, содержание наряда целиком выводится из подписанных карточек
  и его уже сказанных слов. Единственное, что принадлежит его уху, вынесено карточкой вопроса, а не
  придумано ногой
- 3 do the work: done — базис, дерево, пин, слот, шесть звеньев дороги, 23 упоминания груза,
  сериализованный ассет и вся цепочка единиц перемерены первой рукой; наряд написан и выдан пастой
- 4 self-check: done — шесть строк задачи разложены по трём строкам наряда таблицей; потолок в три
  строки контракта 36 соблюдён; расписки нет и причина названа
- 5 close: checkpoint — задача остаётся открытой, фронтир `waiting`, дорожки не открываются

## Гейты

- **G5.** Ничего не объявлено `done`: `t-situation-1` остаётся `open`. Всё в `evidence` либо
  прогнано, либо прочитано в байтах здесь; единственное выведенное число (частота тика `30`)
  названо выведенным и отправлено на замер исполнителю.
- **G7.** Геймплей ногой не решён: имена внутри кода — HOW исполнителя, а единственное следствие,
  которое судит его ухо, ушло вопросом с вариантами, а не стало решением ноги.
- **G3.** Аппетит не тронут: наряд не добавил задаче ни одной строки. Тест на подставленном
  источнике сюда НЕ втянут — он принадлежит `t-react-3`.
- **G1/G2.** Ни одна дорожка не открыта; `NOW.track_wip_limit: 2` не расходуется, пока идёт общая
  задача — это его собственное правило «одна задача блочит две полосы… ждем».
- **G9.** Карточки узла и ставки не редактировались — только строка в журнал.
- **evaluator этой волны.** Пункт 3 (наряд не существует без расписки) исполнен формой выдачи:
  полная паста плюс обязательная расписка первой строкой возврата. Пункт 4 (зелёные ворота не
  доказывают поведение игры) вписан в наряд дословной ссылкой на
  `knowledge/green-gates-do-not-compile-the-game.md`. Пункт 1 (публикация до закрытия — законный
  порядок) вписан в раздел `return`.
- **Панель.** Направление панели не объявляет — регенерация пропущена.

## log

наряд на общую задачу-шов выдан в WIN-U1 готовой пастой с распиской первой строкой возврата: шесть строк задачи свёрнуты в три строки контракта 36 без потерь, главный гейт замерен заранее — 23 упоминания груза в коде хозяина против требуемого нуля, найдено имя груза в сериализованном ассете хозяина, а починка единиц силы оказалась не нейтральной и названа числом до работы: шаг судьи 0.016667 против делителя 0.02, то есть дом станет слышать в 1.2 раза громче при неподвижных порогах

## next

`CALL c-exec-g-5a7c-situation-1-001` — отправить наряд исполнителю в продуктовый репозиторий, слот
`WIN-U1`, база `43648b2d`. Первая строка его возврата обязана быть распиской; без неё наряд не
выдан и работа не начата.

После HOME отдельная нога Направления читает возврат первой рукой и закрывает `t-situation-1` по
букве `evaluator`, называя вслух, каким из двух путей шла. Она же обязана проверить главный гейт
СВОЕЙ рукой (`grep -rni "cargo" … Core/Householder/` = пусто), а не принять его с отчёта, и назвать
измеренный множитель единиц.

Сразу после закрытия открываются ОБЕ дорожки: `t-cargo-1` в `cargo-hands` (рискованное допущение
№1 волны, со своим откатом) и `t-react-1` в `householder`.

END_OF_FILE: live/indie-game-development/history/2026-08-10-s-work-g-5a7c-situation-1-dispatch-001.md
