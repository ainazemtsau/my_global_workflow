# RESULT — s-work-g-5a7c-scale-3-call-001

play: work
direction: indie-game-development · node: g-5a7c · task: t-scale-3 · call: c-work-g-5a7c-scale-3-call-001
date: 2026-08-08
base_read: OS `b390e04f`, дерево `wt/indie-game-development` чисто на входе. Продукт читался на
`ae023ba5`. Ни один продуктовый байт этой ногой не изменён.

## outcome

Существует отправляемый наряд исполнителю на вертикаль у игрока:
`c-exec-g-5a7c-player-vertical-001`, бриф — `work/c-exec-g-5a7c-player-vertical-001-call.md`.
Новая обязанность — проверять происхождение каждой несущей строки задачи — исполнена и **сразу
дала находку**, изменившую форму двух строк `done_when` из четырёх.

## evidence

### Проверка происхождения — то, ради чего обязанность заводилась

| Строка задачи `t-scale-3` | Откуда пришла | Что с ней сделано |
|---|---|---|
| «вдвоём по сети обе копии показывают одну и ту же высоту» | Подписанный критерий 2 карточки узла: «его одинаково видят сервер, обе копии, камера и столкновения»; строка разбора `W13` | Оставлена гейтом дословно по смыслу |
| «камера САДИТСЯ на присланную высоту вместо того, чтобы вбивать свою» | Строка разбора `W14` плюс факт кода, перемеренный этой ногой | Оставлена, с точным местом дефекта |
| «третье число живёт в состоянии игрока, в шаге и в снимке» | Строка `W19` — и `W19` называется **«Цена вертикали у игрока»** | **Переписана в ИСХОД.** Замер стоимости затвердел как приказ об устройстве |
| «порт свободного места отвечает в трёх координатах» | Та же `W19` плюс `W21` | **Переписана в ИСХОД.** Шов реален, но его форма — работа исполнителя |

**Находка по существу: две из четырёх несущих строк задачи происходят из ЗАМЕРА СТОИМОСТИ, а не
из требования.** Это тот же класс, что стоил сегодня снятого наряда на точку масштаба, только
мягче: содержание строк следует из подписанного критерия 2 (высота авторитетна и её видят все), а
вот ФОРМА — «живёт в состоянии игрока, в шаге и в снимке» — называет места в коде, которые по
разделению полномочий принадлежат исполнителю, а не Направлению. Поэтому наряд формулирует исход и
передаёт замер `W19` (около девяти файлов, в восьми по одной строке) как ОСНОВАНИЕ.

Карточка задачи при этом НЕ правилась: наряд обязан служить её `done_when`, и все четыре её
клаузы покрыты — просто исходом, а не адресом. Если владелец захочет привести и текст карточки,
это отдельная нога.

### Перемерено первой рукой на `ae023ba5`

| Факт | Чем подтверждён |
|---|---|
| Высоты у игрока нет | `Core/Multiplayer/PlayerState.cs` — `PlayerState(float x, float z)`, свойства только `X` и `Z` |
| Высота не едет по сети | `Network/WalkerSnapshot.cs` — поля `PlayerId`, `X`, `Z`, `Yaw`, `Moved`; `Y` отсутствует |
| Показ вбивает ноль | `Network/NetworkWalkerPresentation.cs` — `new Vector3(snapshot.X, 0f, snapshot.Z)` |
| **Камера вбивает свою высоту** | `Network/NetworkWalkerCamera.cs:41` — `new Vector3(standing.x, _settings.EyeHeight, standing.z)`; точка опоры приходит из `TryGetStandingPoint` (`:45`), высота берётся мимо неё |
| Шов свободного места двумерен | `Core/Multiplayer/WalkSpacePort.cs:23` — `WalkStep Resolve(int playerId, float fromX, float fromZ, float toX, float toZ)`; реализация `Network/WalkSpaceProbe.cs:20`, вызов `Core/Multiplayer/AuthoritativeWalkerRoster.cs:184` |
| Порядок тика готов под новую ось | `Core/Multiplayer/CarryWorld.cs:36-58` |

**Поправка к прежним основаниям:** разбор называл шов `IWalkSpacePort.Resolve` и подразумевал файл
того же имени. Тип назван верно, файл — `Core/Multiplayer/WalkSpacePort.cs`. Исправлено в наряде.

**Пин контракта:** `os/engineering/CONTRACT_VERSION` `current: 36` против продуктового
`synced_contract_version: 36` — дрейфа нет. Лимит v36 в три пункта `done_when` соблюдён: четыре
клаузы задачи уложены в три исхода без просьбы о токене.

**Слот:** WIN-U1 освобождён после отзыва прошлого наряда — перемерено: `AVAILABLE`, `lease: none`,
чекаут чист, вершина `ae023ba5` = база. Прошлая сессия отработала остановку полностью.

## Что наряд НЕ включает и почему

- **Путь наверх не строится.** Из чего он сделан — мебель, лестница, вентиляция, прыжок — владелец
  отложил своими словами 2026-08-07: «когда будет задача, тогда это и сделать» (`W15`). Это не та
  задача: `t-scale-3` даёт координату, а не способ по ней двигаться.
- **Двигает ли телекинез налётчика вверх** (`W16`) — помечено разбором как вопрос ВЛАДЕЛЬЦА, не
  инженерии. В наряде стоит прямой запрет предвосхищать его кодом.
- **Вертикаль груза** — `t-scale-4`, отдельно и намеренно.

## captures

- **Критерий 2 карточки узла не закрывается этой волной целиком.** Его вторая половина — «в
  ключевых комнатах есть проходимый путь снизу вверх и обратно» — не куплена ни одной из семи
  задач, и это соответствует аппетиту, где «Б» определён как появление высоты, а не пути. Строки
  `W15`, `W16`, `W17` ждут задачи, которой в этой волне нет. Не дефект, но и не должно раствориться.
- Стены сегодня не имеют высоты вовсе (`W21`). Когда путь наверх появится, это станет работой, и
  сейчас она никем не оценена.

## decisions_needed

Нет.

## play_check

- 1 recite: done — цель и `done_when` `t-scale-3` пересказаны, обязательство ставки названо.
- 2 owner inputs (owner): skipped — фактов, которые знает только он, наряд не требует. Оба
  отложенных им вопроса (`W15` из чего путь, `W16` тянет ли телекинез вверх) в эту задачу не
  входят и в наряде стоят прямым запретом. Слот он назвал для этой ставки ранее словом «давай
  слот 1»; операционная предпосылка — поднятый Unity — передаётся ему в чате.
- 3 do the work: done — наряд написан; проверка происхождения исполнена и дала поправку формы
  двух строк; шесть фактов кода перемерены первой рукой.
- 4 self-check: done — все четыре клаузы `done_when` задачи покрыты тремя исходами; лимит v36
  соблюдён; границы `t-scale-4` и отложенных вопросов проведены явно.
- 5 close: done — наряд зарегистрирован карточкой, возвращающийся CALL закрыт.

## Гейты

- **G7.** Ни одного решения продукта или геймплея наряд не принимает и ни одно не делегирует
  исполнителю. Оба отложенных владельцем вопроса названы отложенными, а не решены молча.
- **G9.** Карточка узла не трогалась.
- **G10.** Наряд не пересказывает процедуру плея и не диктует устройство; после сегодняшней
  находки он сознательно НЕ называет места в коде там, где раньше называл.

## log

наряд на вертикаль у игрока написан; проверка происхождения дала находку — две строки задачи росли из замера стоимости, а не из требования, и переписаны в исход

## next

`CALL c-exec-g-5a7c-player-vertical-001` — исполнителю в `GasCoopGame`, слот WIN-U1, стадия PLAN,
контракт 36. Отправляется, когда владелец поднимет Unity в этом слоте.

END_OF_FILE: live/indie-game-development/history/2026-08-08-s-work-g-5a7c-scale-3-call-001.md
