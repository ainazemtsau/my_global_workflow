# RESULT — s-work-g-5a7c-scale-1-close-001

play: work · close: биндинговая свежая нога направления, как её назвал сам наряд
direction: indie-game-development · node: g-5a7c · task: t-scale-1 · call: c-exec-g-5a7c-scale-probe-001
date: 2026-08-08
base_read: OS `75682445`, дерево `wt/indie-game-development` чисто на входе. Продукт
`C:/projects/Unity/GasCoopGame_dev`: `main` = `origin/main` = `dev` = `origin/dev` = `ae023ba5`.
Продуктовые байты, ветки, слоты и slot-state этой ногой НЕ изменялись — она читала, прогоняла
ворота и брала слово владельца.

## outcome

Самое рискованное допущение ставки проверено и выстояло: мир игры идёт в пропорции роста
налётчика 0.16 м, физика на этом росте держится, и это сказал владелец, а не измеритель.
Порог убийства (1) карточки `g-5a7c` — «мир на 0.16 не держится физикой, и четыре рычага не
помогают» — НЕ сработал: ни один из четырёх рычагов не понадобился вовсе. Ставка не режется,
пропорция в `map` не возвращается, и остальные шесть задач волны строятся против числа 0.16.

`t-scale-1` закрыта. Наряд `c-exec-g-5a7c-scale-probe-001` возвращён и закрыт.

## evidence

**Слово владельца, дословно, в этой ноге 2026-08-08:**

> физика держится, размер тот — закрывай

и, о границах собственной проверки:

> я как бы ошибок вообще не тестировал, потому что там, ну, ещё неинтересно тестировать.
> Поэтому просто я смотрю размер, остальной функционал работает, размеры, ну, вроде норм.
> Ну, насколько мы сейчас пока можем оценить, это значит, что мы это можем закрывать.
> По скорости, ну, стало как бы получше, но, естественно, мы это всё будем дорабатывать.

**Перемерено первой рукой этой ногой, а не принято с отчёта:**

| Что | Чем перемерено |
|---|---|
| Работа доставлена, а не только заявлена | `ae023ba5` = `main` = `origin/main` = `dev` = `origin/dev`; кандидат `09247f2a` втянут merge-коммитом `4550bfe4`, у него ровно два родителя (`352f96b0`, `09247f2a`) |
| Числа стоят в проекте | `Assets/TunnelCrew/Network/NetworkPlaySettings.asset` на `ae023ba5`: `_walkerHeight: 0.16`, `_walkerRadius: 0.036`, `_eyeHeight: 0.136`, `_movementSpeed: 0.6`, `_cargoMass: 20`, `_cargoHandStiffness: 2000`, `_cargoHaulLinearDamping: 350`, `_cargoHaulAngularDamping: 1.92`, `_spawnOriginX: -0.12` |
| Ближняя плоскость переведена | `Assets/TunnelCrew/Scenes/IntegratedHouse.unity:1206` — `near clip plane: 0.01` |
| Ничего не переустроено | `git show --stat 40478a67` = ровно два файла; `ce297f06` = один файл. Новых ассетов, скриптов, слоёв и объектов настроек в диффе нет |
| Ворота зелёные | ПРОГНАНЫ ЭТОЙ НОГОЙ на `ae023ba5`: `./pwsh.cmd tools/check.ps1` → `hygiene OK` / `OK: all active gates green` |
| Владельческая композиция масштаба спасена, а не потеряна | `Assets/TunnelCrew/Probe/ScaleTableau/ScaleTableau.unity` существует на `ae023ba5`; durable-ref `refs/keep/c-exec-g-5a7c-scale-probe-001/scale-tableau` = `2904b596` |
| Мина «слот впереди main» обезврежена | `git log slot/win-u1 ^main` пуст: вершина слота `09247f2a` целиком внутри `main`. Следующая задача в WIN-U1 больше ничего чужого не утащит |
| Отменённое чужое слияние не просочилось | `git merge-base --is-ancestor 5752c0da dev` → НЕТ. Отменённая проба арт-паков `t-look-1` предком `dev` не стала и осталась достижима по `refs/gascoop-preserved/roots/c-exec-g-1d84-free-kits-probe-001/banked-probe` |
| Слот освобождён | `gascoop-slot-state.v1.json`: WIN-U1 `lifecycle: AVAILABLE`, `lease: none` |
| Пин контракта | Наряд `engineering_contract: 36` против продуктового стампа 36 — дрейфа нет, Re-sync не должен |

**Измерения физики, сделанные исполнителем в живом прогоне** (`docs/results/c-exec-g-5a7c-scale-probe-001.md` §3 на `ae023ba5`): два независимых чтения `Rigidbody` дали побитово равные позиции и ТОЧНЫЕ нули скоростей — дрожи на покое нет; вертикаль груза заморожена конструктивно (`CargoBody.cs:66-68`, гравитация выключена), ходоки нефизичны — провалиться сквозь пол нечему ни на каком масштабе; за прогон ни одной ошибки игры. `DynamicsManager` и `TimeManager` — байт в байт база: ни один из четырёх рычагов не тронут.

review: n/a — light change. Возвращающийся наряд не объявлял ни замороженной спеки, ни
`openspec/changes/<id>`: это перевод чисел в существующих местах.

## Диспозиция по `done_when` наряда — все три строки

| Строка наряда | Диспозиция |
|---|---|
| 1. Мир запускается в пропорции 0.16 м: размеры, скорости, масса и ближняя плоскость переведены руками в существующих местах, ничего не переустроено, новых систем/слоёв/точек настроек нет, перевод приведён числами | **ЗАКРЫТА.** Числа перемерены этой ногой в `NetworkPlaySettings.asset` и `IntegratedHouse.unity:1206` на `ae023ba5`; дифф `40478a67` + `ce297f06` = два файла, новых сущностей ноль. Перевод выведен одним множителем `s = 0.08` при сохранённом времени мира (отчёт §1–§2), а не подобран |
| 2. Про физику существует измеренный ответ: дрожит ли, проваливается ли, врут ли контакты — с числами и с названием применённых рычагов | **ЗАКРЫТА, и раздельно честно.** Инструментом: не дрожит, не проваливается, движок молчит, рычагов применено НОЛЬ (отчёт §3). Остаток, недостижимый для MCP (врут ли контакты при ударе, ведёт ли пружину при переноске — нужен игрок за управлением), закрыт руками владельца и его словом «физика держится» |
| 3. Откат назван и проверен: возврат прежних чисел возвращает прежнее поведение | **ЗАКРЫТА.** Откат — `git checkout 352f96b0 -- Assets/TunnelCrew/Network/NetworkPlaySettings.asset Assets/TunnelCrew/Scenes/IntegratedHouse.unity`; обе тропы на `352f96b0` существуют, возврат блобов детерминирован. Проверен прогоном туда и обратно с измерением в живом мире (отчёт §4) |

Сверх наряда исполнитель сделал две вещи, обе — спасение чужого от молчаливой потери: сохранил
владельческую композицию масштаба, существовавшую в одном untracked-экземпляре, и отменил
залипшее чужое слияние, которое незаметно сделало бы отменённую пробу `t-look-1` предком `dev`.
Обе перемерены этой ногой и обе подтвердились.

## Что приёмка НЕ покрывает — записано против себя, а не в свою пользу

Владелец сам назвал границу своей проверки: он смотрел РАЗМЕР. Не судимы:

- **перевёрнутая погоня** — хозяин теперь быстрее налётчика в 2.9 раза шагом и в 5.4 бегом,
  раньше налётчик убегал. Это следствие масштаба, а не чьё-то решение;
- **скорость 0.6 м/с** — «стало получше, но естественно мы это всё будем дорабатывать»;
- **дом как диорама** — стены 1.5 м при хозяине 2 м, он видит поверх каждой стены (уже открытая
  улика `i-house-walls-below-eye-level-001`);
- **слой шума** — импульс контакта сохранён арифметикой, но настоящего удара никто не слышал;
- ближняя плоскость упёрлась в пол движка 0.01: передние ~2 мм несомого груза срезаются.

Заведена улика `i-scale-verdict-covers-size-not-chase-001`, чтобы ни одна следующая нога не
сослалась на это «размер тот» как на суждение о погоне или скорости.

## state_changes

- `t-scale-1` → `done`, закрыта.
- `c-exec-g-5a7c-scale-probe-001` → закрыт как возвращённый и принятый.
- Заведена улика `i-scale-verdict-covers-size-not-chase-001` (level `objective`, route `review`).
- Заведена запись `knowledge/scale-down-raises-mass.md` — уменьшение мира ПОДНИМАЕТ массу, и
  скорость нельзя двигать в одиночку; источники резолвятся в `ae023ba5`.
- Зарегистрирован `c-work-g-5a7c-scale-2-call-001` (`to: session`, `play: work`, `for: t-scale-2`,
  `ready`), бриф — `work/c-work-g-5a7c-scale-2-call-001.md`.
- `g-5a7c` остаётся `active`; `NOW.bet` не менялся; карточка узла не редактировалась.

## captures

- Хозяин в игре ростом 2 м, а во владельческой композиции — 1.75 м. Расхождение названо
  исполнителем, дом трогать было запрещено; кандидат в `t-scale-2` или в дом следующей волны.
- Unity не переимпортирует ассеты во время PlayMode — числа вступают в силу только после выхода
  из игры и повторного входа. Стоило одного ложного чтения на прогоне.

## decisions_needed

Нет. Единственный вопрос ноги — его вердикт — задан и получен.

## play_check

- 1 recite: done — цель и `done_when` `t-scale-1` пересказаны владельцу до вопроса, вместе с
  обязательством ставки (порог убийства 1).
- 2 owner inputs (owner): done — вопрос задан ровно один и ровно тот, который может ответить
  только он. Его слова: «физика держится, размер тот — закрывай»; про границу проверки —
  «я как бы ошибок вообще не тестировал… просто я смотрю размер».
- 3 do the work: done — работа сделана нарядом в слоте WIN-U1 и вернулась; эта нога свела
  возврат с `done_when` и перемерила механику первой рукой.
- 4 self-check: done — построчная диспозиция всех трёх строк наряда выше; ворота прогнаны этой
  ногой на `ae023ba5`.
- 5 close: done — задача и наряд закрыты, названа продолжающая нога той же волны.

## Гейты

- **G5.** Венью выдержано ровно так, как назвал сам наряд: «закрытие `t-scale-1` — слово
  владельца … и оно берётся отдельной Direction-ногой». Эта нога — отдельная и свежая по
  отношению к той, что делала работу (та шла в продуктовом слоте WIN-U1); работу она не делала,
  а пыталась опровергнуть возврат: перемерила доставку, дифф, числа, ancestry, slot-state и сама
  прогнала ворота. `close: light` НЕ заявляется: измерения физики и прогон отката — наблюдения
  исполнителя, которых эта нога своей рукой не повторяла, и это сказано прямо, а не сглажено.
  Продуктовая доставка и пуш в `main` сами по себе задачу не закрывали и не закрыли — закрыло
  слово владельца.
- **G7.** Вердикт не выведен и не растянут из «перезапустил, теперь норм», сказанного на прогоне
  скорости: он спрошен заново и получен словами. Границу своей проверки владелец назвал сам, и
  она записана уликой, а не сглажена.
- **G9.** Карточка узла не редактировалась — только строка в её журнал.

## log

work t-scale-1: физика на 0.16 держится его словом «физика держится, размер тот» — задача закрыта, порог убийства (1) не сработал

## next

`CALL c-work-g-5a7c-scale-2-call-001` — нога направления пишет наряд исполнителю на `t-scale-2`
(единая точка масштаба), уже зная измеренный ответ про физику. Бриф:
`work/c-work-g-5a7c-scale-2-call-001.md`.

END_OF_FILE: live/indie-game-development/history/2026-08-08-s-work-g-5a7c-scale-1-close-001.md
