# RESULT — s-work-g-5a7c-instrument-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / bet-g-5a7c-wave-9, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: dispatch — два наряда девятой волны выданы одновременно, потому что их поверхности не пересекаются ни одним файлом

owner_approved: |
  Его слово на эту ногу дословно: **«пиши наряд на прибор и что мы можем запустить параллельно»**.
  Слово на саму восьмую задачу — **«A»**, записано ремонтом `s-repair-g-5a7c-wave-9-amend-001`.
  Ни одна строка `done_when` в выданных нарядах не изобретена этой ногой: обе задачи уже стояли
  в волне, наряды их не расширяют.

outcome: |
  **ВЫДАНО ДВА НАРЯДА, И ОНИ ДЕЙСТВИТЕЛЬНО ПАРАЛЛЕЛЬНЫ — ПРОВЕРЕНО ПО ФАЙЛАМ, А НЕ ПО НАМЕРЕНИЮ.**

  `c-exec-g-5a7c-instrument-1-001` — прибор. Расширяет уже существующий шов
  `NetworkWalkerCourier.TryReadObservation(out CarryWorldObservation)` в области `#if UNITY_EDITOR`,
  нумерует пробы зрения в `HouseholderSightSource.cs`, печатает текстовый отчёт в
  `Editor/HouseholderObservation*`, чинит пункт меню в `Editor/HouseholdLabMenu.cs` и ставит сторож
  «не стоит N тиков с открытым делом». Слот WIN-U1.

  `c-exec-g-5a7c-gate-headless-1-001` — гейт. Трогает только `tools/**` и один тестовый файл.
  **Слот Unity не занимает вовсе.**

  **ПЕРЕСЕЧЕНИЙ НОЛЬ**, и это не оценка: у прибора вся поверхность в `Network/` и `Editor/`, у
  гейта — в `tools/` и `tests/`. Общих файлов нет ни одного.

  **ТРЕТЬЯ НОГА ГОТОВА К ВЫДАЧЕ И НЕ ВЫДАНА ОСОЗНАННО.** `t-arrival-hook-1` (полоса `утро`) тоже не
  пересекается с прибором: её поверхность — `Probe/Householder/Householder.cs`,
  `HouseholderRoutineState.cs`, загрузчик профиля и сам профиль. Не выдана потому, что её слот
  требует живого редактора Unity, а сегодня в сессии отвечал только WIN-U1, и он ушёл под прибор.
  Поднимать редактор — действие владельца, и решать за него, какой слот открывать, эта нога не
  стала.

  **ПОЧЕМУ ПРИБОР ИДЁТ ПЕРВЫМ, ОДНОЙ СТРОКОЙ.** Без него у `t-sight-thing-1` нет ни одной
  проверяемой строки результата, а на ней стоят три остальные полосы.

evidence: |
  **СОСТОЯНИЕ СЛОТОВ, ПЕРЕМЕРЕНО ПЕРЕД ВЫДАЧЕЙ** (а не взято из реестра, который сегодня уже врал):
  - реестр `gascoop-slot-state.v1.json`: все четыре `AVAILABLE`, лиз нет;
  - реальность чекаутов: `GasCoopGame_win-u1..u4` — все четыре на своих ветках `slot/win-uN`,
    HEAD = `c7e926cf` у всех четырёх, `git status --porcelain` пуст у всех четырёх,
    отставание от `main` = **0** у всех четырёх.
  - Такого чистого состояния за эти сутки не было ни разу: утром u3 и u4 отставали на 22 и 44
    коммита, а u2 держал незакоммиченную чужую работу при `AVAILABLE` в реестре.

  **ЖИВОСТЬ РЕДАКТОРОВ:** подтверждена только у WIN-U1 — владелец в нём играл, направление читало
  его сессию. У U2, U3, U4 MCP в этой сессии отвечал отказом соединения. Это улика ОКРУЖЕНИЯ
  сессии, а не байты репозитория: у другой сессии может быть иначе.

  **НЕПЕРЕСЕЧЕНИЕ ПОВЕРХНОСТЕЙ** — по файлам, названным в обоих нарядах:
  - прибор: `Network/HouseholderSightSource.cs`, `Network/NetworkWalkerCourier.cs` (editor-область),
    `Editor/HouseholderObservation*`, `Editor/HouseholdLabMenu.cs`;
  - гейт: `tools/close-leg.ps1`, `tools/close-leg.selftest.ps1`,
    `tests/TunnelCrew.Core.Tests/HouseholderDecisionPipelineTests.cs`;
  - `t-arrival-hook-1` (не выдана): `Probe/Householder/Householder.cs`,
    `Probe/Householder/HouseholderRoutineState.cs`, `Profiles/HouseholderProfileLoader.cs`,
    `StreamingAssets/TunnelCrew/Householder/default.householder.json`.
  Ни один файл не встречается в двух списках.

  **ЗАМЕРЫ, ВНЕСЁННЫЕ В НАРЯДЫ, ПЕРЕПРОВЕРЕНЫ НА `c7e926cf`:**
  - общий номер пробы — `HouseholderSightSource.cs:355-367` + `targetId = 0` в `CanSee`;
  - шов наблюдения — `NetworkWalkerCourier.TryReadObservation`, `ObservedThing` в
    `#if UNITY_EDITOR`;
  - пустая сцена — `Editor/HouseholdLabMenu.cs:11-12`;
  - гейт — `tools/close-leg.ps1:257` без `-Platform`, `:310` `check.ps1 -Deliver`; слово `dotnet`
    ноль раз в четырёх скриптах гейта;
  - живой замер, ради которого написан сторож — хозяин `(2.70000029, 0, 2.50)` = `Loot/Mug`
    `(2.70000029, 0, 2.50)` в прогоне владельца 2026-08-31.

state_changes: |
  Всё через `osctl`; два документа наряда написаны рукой в `work/`, как и положено.

  СОЗДАНО (2 карточки наряда + 2 документа):
  - `c-exec-g-5a7c-instrument-1-001` → `t-instrument-1`, слот WIN-U1, база `c7e926cf`,
    документ `work/2026-08-31-call-instrument-1.md`;
  - `c-exec-g-5a7c-gate-headless-1-001` → `t-gate-headless-1`, слот Unity не занимается,
    база `c7e926cf`, документ `work/2026-08-31-call-gate-headless-1.md`.

  ПЕРЕВЕДЕНО В РАБОТУ (2):
  - `t-instrument-1` и `t-gate-headless-1` → `active`.

  НЕ ВЫДАНО НАМЕРЕННО:
  - `t-arrival-hook-1` — готова, не пересекается, ждёт живого редактора Unity и слова владельца
    о том, какой слот поднимать.

captures: |
  - Оба наряда несут строку `basis: c7e926cf` и прямое требование стартовать с `main`, а не с ветки
    слота. Причина не абстрактная: сегодня наряд `chore-result` стартовал с базы двухчасовой
    давности, не увидел переименования сборок и написал новый файл прямо в закреплённую сборку.
  - Реестр слотов и реальность чекаутов сегодня совпали впервые. Это состояние хрупкое и не
    сторожится ничем: у реестра нет истории, он вне контроля версий.

decisions_needed: |
  Один, и он маленький: какой слот поднимать под `t-arrival-hook-1`, если владелец хочет третью ногу
  параллельно. U2, U3 и U4 сегодня одинаково чисты и одинаково стоят на `main`; разница только в
  том, что редактор в них не запущен.

play_check: |
  1. OPEN — рабочий набор собран по волне; состояние слотов перемерено по чекаутам, а не по реестру.
  2. WORK — два наряда написаны, ни одна строка `done_when` не изобретена: обе задачи стояли в
     волне до этой ноги. Поверхности сверены пофайлово.
  3. (owner) — его слово на ногу: «пиши наряд на прибор и что мы можем запустить параллельно».
  4. CLOSE — RESULT финальным сообщением, применение и коммит после него.

log: |
  два наряда девятой волны выданы разом — прибор в WIN-U1 и гейт без слота Unity; поверхности
  сверены пофайлово, пересечений ноль, третья нога готова и ждёт поднятого редактора

next: |
  Домой. Возвраты приходят по одному, каждый закрывает свой наряд.

  Прибор возвращает три числа, на которых стоит остаток волны: сколько вещей из четырнадцати хозяин
  видит сегодня, сколько мест правки, и на каком числе тиков сторожит новый тест.

  `t-sight-thing-1` открывается сразу после прибора — её приёмка теперь целиком на его числах.
