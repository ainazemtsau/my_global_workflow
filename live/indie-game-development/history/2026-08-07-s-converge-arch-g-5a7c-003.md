RESULT s-converge-arch-g-5a7c-003 (call: c-converge-arch-g-5a7c-001, продолжение)
direction: indie-game-development   play: converge-arch   node/task: g-5a7c

outcome: |
  **Владелец уточнил вчерашний вердикт в тот же день, и физических классов оказалось ТРИ, а не два.
  Уточнение записано ВХОДОМ, а не требованием, и это его собственная рамка, а не осторожность ноги.**

  Он открыл фразу словами «я сейчас не могу точно сказать, что будет, но я пока предполагаю» и закрыл
  «Предметы интерьера, я думаю, что нет». Ни одна строка, записанная этой ногой, не становится
  `done_when` без его отдельного слова.

  **КЛАСС 1 — ОБСТАНОВКА.** Его слова: «условно без физики, да, прибито, там, тумбочки, столы
  какие-нибудь»; «предметы интерьера, я думаю, что нет». Это подтверждение вчерашнего варианта А, и
  оно СИЛЬНЕЕ прежнего токена: вчерашний был выбором буквы, этот называет предметы поимённо. Работы
  не добавляет ни строчки.

  **КЛАСС 2 — ПЕРЕНОСИМОЕ КРУПНОЕ.** Его слова: «большие те, которые можно унести, они же точно
  физические будут». **Это НЕ новый класс, а ровно груз** — единственное физическое тело игры, под
  которое архитектура уже написана. Значит его фраза не покупает работы: она подтверждает уже
  существующее устройство.

  **КЛАСС 3 — ДВЕРИ.** Его слова: «двери, да, должны как-то же закрываться. Возможно, и не будут».
  Класс, которого вопрос про обстановку не покрывал вовсе.

  **ГЛАВНОЕ ПРО ДВЕРИ, и оно снимает спешку с его «возможно».** Состояние двери «открыта / закрыта»
  УЖЕ куплено подписанным критерием 6 — «стены и **закрытые двери** ослабляют», — и физики оно не
  требует: ослаблять просто нечему, если состояния нет. Заведён внутренний шов `C26`: **состояние
  двери принадлежит ОПИСАНИЮ ДОМА, а не физическому объекту.** Физическая, крутящаяся дверь — вопрос
  отдельный и НЕ решённый, и цена ему названа ЗАРАНЕЕ: дверь на петле имеет одну степень свободы, её
  положение это ОДНО число (угол), которое считает судья и шлёт в снимке ровно как позу груза; падающий
  предмет — шесть степеней свободы и непредсказуемая траектория, то есть настоящее тело со всей сетевой
  ценой отклонённого варианта. **Поэтому отклонённый «обстановка падает» на двери НЕ распространяется**,
  и когда до дверей дойдёт, его «возможно» не придётся оценивать в спешке.

  Половина строки `W63` («кто закрывает двери, если закрытые двери ослабляют») получила его вход и
  осталась `→ PLAN`.

  **ЧТО НЕ ИЗМЕНИЛОСЬ:** вердикт по обстановке (вариант А), `contracts_open: 1`, `arch_open: 8`,
  карточка `g-5a7c` не тронута ни знаком, продукт не изменялся. Внутренних швов стало четырнадцать
  вместо тринадцати.

evidence: |
  **Его слова, дословно и целиком, с обеими оговорками:** «я сейчас не могу точно сказать, что будет,
  но я пока предполагаю, что всё будет условно без физики, да, прибито, там, тумбочки, столы
  какие-нибудь. Но будут физические объекты, например, ну, двери, да, должны как-то же закрываться.
  Возможно, и не будут, но там, например, большие те, которые можно унести, они же точно физические
  будут. Предметы интерьера, я думаю, что нет.»

  **Измерения первой рукой на объявленной базе** (`C:/projects/Unity/GasCoopGame`, `main` =
  `352f96b0`, дерево чистое, только чтение):

  1. **«Переносимое крупное» — это уже существующий класс, а не новый.** `grep -rn
     "AddComponent<Rigidbody>" Assets/TunnelCrew --include=*.cs` даёт РОВНО ОДНО совпадение:
     `Network/CargoBody.cs:60`. То есть утверждение набора «сегодня в игре ровно одно физическое
     тело» верно буквально, на уровне кода, а не как оборот речи. При этом множественность в
     устройство уже заложена: `Core/Cargo/AuthoritativeCargoRoster.cs` — ростер со счётчиком, а
     `Core/Cargo/CargoHaul.cs:12-13` прямо объявляет, что формула «written for a list of any length».
     Значит «несколько переносимых» не требует ни новой модели, ни нового шва; ограничение карточки
     на ОДИН предмет лута (W79, его собственные слова) — это ограничение ОБЪЁМА, а не устройства.

  2. **Двери как вещи сегодня не существует.** `grep -rniE "\bdoor" Assets/TunnelCrew --include=*.cs`
     даёт только `Core/House/HousePlace.cs:13` — `Doorway = 1`, ярлык НАЗНАЧЕНИЯ места, — плюс
     комментарии и `World/HousePassageMarker.cs`. Ни одного объекта-двери. Поиск состояния
     `grep -rniE "isopen|isclosed|passable|blocked" Assets/TunnelCrew --include=*.cs` даёт ОДНУ
     строку, и та — комментарий в `Core/House/HousePassage.cs:36`. То есть состояния «открыта /
     закрыта» нет нигде, при том что подписанный критерий 6 требует, чтобы закрытые двери ослабляли
     звук.

  3. **Проход сегодня — ребро графа с шириной и без состояния.** `Core/House/HousePassage.cs:19-56`;
     файл сам пишет про ширину «Nothing reads it yet».

  **Куда уточнение записано, три места:**

  1. `work/converge-g-5a7c-arch.md`, A9 — добавлен подраздел «ЕГО УТОЧНЕНИЕ ТОГО ЖЕ ДНЯ» с его
     дословными словами, таблицей трёх классов, разбором дверей и явной пометкой, что рамка
     предположения — его собственная. Прежний подраздел с вердиктом А не тронут.
  2. `work/converge-g-5a7c.md` §CONTRACTS — ряд `C25` дополнен тремя классами; **добавлен новый
     внутренний шов `C26`** (состояние двери принадлежит описанию дома) с токеном `rewrites:`.
     Ряды `C01`–`C24` не тронуты.
  3. `work/c-converge-verify-g-5a7c-001-call.md` — добавлена атака `3b`, требующая проверить, что
     уточнение записано входом, а не требованием, и что его двойная оговорка нигде не превратилась в
     `done_when`; счёт внутренних швов приведён к 14 с названным прежним числом.

  **Карточка `g-5a7c` не тронута ни знаком. `TREE.md` и `CHARTER.md` не открывались на запись.
  `knowledge/` не тронуто. Продуктовый репозиторий не изменялся.**

state_changes: |
  1. `work/converge-g-5a7c-arch.md`:
     - A9: ПОСЛЕ подраздела «ЕГО ОТВЕТ 2026-08-07 — вариант А» добавлен подраздел «ЕГО УТОЧНЕНИЕ ТОГО
       ЖЕ ДНЯ — классов оказалось ТРИ, а не два»: дословные слова, рамка предположения, таблица трёх
       классов, разбор дверей (что куплено критерием 6, что стоит физическая дверь и почему она
       дешевле подвижной обстановки на порядок), и явная строка «что этим уточнением НЕ изменилось»;
     - §SIGNOFF Declare: счёт внутренних швов 13 → 14 с названной причиной;
     - разделы §RISK-LINE, §DECOMPOSE, A1–A8, §MINER, §NOT-DECIDED и §SIGNOFF Architect —
       **не тронуты**.

  2. `work/converge-g-5a7c.md` §CONTRACTS:
     - ряд `C25` — дописано уточнение тремя классами; вердикт не изменён;
     - ДОБАВЛЕН ряд `C26` (дверь ← описание дома) с `rewrites:`;
     - «Счёт этой ноги»: «из тринадцати внутренних швов» → «из четырнадцати», с названной причиной.
       Блок счёта (`contracts_open: 1`, `arch_open: 8`, `contract_coverage`, `arch_in_context_only`,
       `canon_proposed`) НЕ изменяется;
     - **§IMPORT, §DRIFT, §GLOSSARY, §WHAT, оба прежних §SIGNOFF и ряды `C01`–`C24` НЕ ТРОГАТЬ.**

  3. `work/c-converge-verify-g-5a7c-001-call.md` — тот же id, тот же `status: ready`:
     - счёт внутренних швов 13 → 14 с названным прежним числом;
     - ДОБАВЛЕНА атака `3b` про уточнение и его оговорки.

  4. `NOW.md`:
     - шапка `updated:` → `2026-08-07 by s-converge-arch-g-5a7c-003`;
     - ДОБАВИТЬ строку-комментарий об уточнении после комментария ноги 002;
     - `open_calls`, `decisions`, `bet`, `tasks`, `recurring`, `direction_forecast` и все записи
       `issues` — **без изменений**. Новых решений не заводится: он ничего не спрашивал и ответа не
       ждёт.

  5. `LOG.md` — ДОБАВИТЬ одну строку сверху, перед строкой `s-converge-arch-g-5a7c-002`.

  6. `history/2026-08-07-s-converge-arch-g-5a7c-003.md` — этот RESULT целиком.

  7. `TREE.md`, `CHARTER.md`, `knowledge/`, продуктовый репозиторий — НЕ ТРОГАТЬ.

captures:
  - "Если физическая дверь когда-нибудь понадобится, она стоит дешевле, чем кажется, ровно потому, что дверь на петле — одна степень свободы: судья считает угол и шлёт его в снимке тем же способом, что позу груза. Записано как свойство выбранной архитектуры, а не как план."
  - "«Несколько переносимых предметов» устройство уже держит (ростер плюс формула на список любой длины); ограничение узла на один предмет лута — объём, который снимает `shape` или его слово, а не переписывание."

decisions_needed: []

play_check:
  - "1 Declare: done — добавлен один внутренний шов `C26` (состояние двери ← описание дома) с `rewrites:`; ряд `C25` дополнен без изменения вердикта. Ряды `C01`–`C24` не тронуты. HOW у `C26` стоит `→ PLAN`, как у всех швов."
  - "3 Architect (owner): done — его уточнение получено и записано ВХОДОМ. Рамка предположения сохранена дословно с обеих сторон фразы; ни одна строка не помечена `acceptance` и в `done_when` не поехала. То, чего он не сказал (будет ли дверь физической), названо не сказанным, и цена обоих ответов названа заранее, чтобы решение потом не принималось в спешке."
  - "4 Close & route: done — `contracts_open: 1` и `arch_open: 8` не изменились, `arch_in_context_only: PASS` держится, карточка не тронута. Фронтир прежний: `c-converge-verify-g-5a7c-001`, дополнен атакой `3b` и не переиздан."
  - "G7: соблюдён — вопрос ему не задавался, потому что он его не задавал и ответа не ждёт; его предположение не превращено ни в требование, ни в новое решение `NOW`."
  - "G10: RESULT финальный, состояние написано только через `state_changes`, `(owner)`-шаг несёт его настоящие слова целиком, включая обе оговорки."

log: "**ЕГО УТОЧНЕНИЕ ТОГО ЖЕ ДНЯ: ФИЗИЧЕСКИХ КЛАССОВ ТРИ, А НЕ ДВА, и записано это ВХОДОМ, а не требованием** — рамка его собственная («не могу точно сказать… я думаю, что нет») и сохранена дословно. Обстановка прибита — подтверждение вчерашнего А, названное поимённо. «Переносимое крупное» — не новый класс, а ровно груз: перемерено, что во всём проекте ровно один `AddComponent<Rigidbody>`, а ростер и формула тяги изначально написаны на список любой длины. Двери — новый класс с его двойной оговоркой; главное про них в том, что состояние «открыта/закрыта» УЖЕ куплено критерием 6 и физики не требует, а физическая дверь на порядок дешевле падающей обстановки, потому что это одно число в снимке против шести степеней свободы. Заведён шов `C26`; `W63` получила его вход. Вердикт, счёт открытых строк и карточка не изменились."

next: |
  `c-converge-verify-g-5a7c-001` — прежний и единственный фронтир, свежая ОТДЕЛЬНАЯ физическая сессия.
  Ждущих вопросов у владельца нет.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-003.md
