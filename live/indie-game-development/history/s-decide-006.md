# s-decide-006 — off-screen return-fidelity bar (g-9c41, d-returnfidelity-001)

2026-06-18. Play: decide (owner present, chat-driven). Parent context: the gas-representation thread
(finding-1 / OR1 escalation → gas vision → c-research-gasdepth-001 / s-research-008 + an owner-run ChatGPT
deep-research cross-pass). Owner «ОК» to the formulation below. Informs the Wave-3 shape; active bet untouched (G1).

## Decision (owner-approved)

HARD REQUIREMENT — FINAL implementation, NOT current wave:

> On return at ANY moment — settled OR still in motion — the gas is seen approximately right (place, spread
> RATE, evolving shape, + the effect of external events like blast/wind), with NO DISSONANCE vs what a
> continuous per-cell sim would give. Math precision NOT needed (~10 cells off is fine); the bar is plausible
> CONTINUITY / no jarring surprise. The coarse layer must carry the TRANSIENT (not only the settled state);
> "hot" fine detail by proximity/return.

## What this corrects (the case the prior framing missed)

Prior framing was binary: near → fine sees everything; far → it settles, then you return to a settled state.
MISSED: the player can return BEFORE it settles (far, mid-spread). So the always-on coarse/far layer must
produce a believable TRANSIENT picture, not just the final settled one.

Resolved as 4 cases:
1. near + active → fine sim live.
2. far + UNSETTLED → coarse carries the transient cheaply (amount-over-time at a believable RATE — it already
   fills gradually, so a mid-spread return is an honest %, not instant-full — + WHERE the front is via the
   s-research-008 knobs + external events applied to the coarse state WHEN they occur); on return the room is
   "heated" to fine and continues from that front.
3. far + settled → coarse holds it; fine reconstructs the cloud.
4. return at ANY moment → plausible, approximate, no dissonance.

Owner floated "hot room" LOD (promote room → detailed compute, demote → coarse when settled, and back) —
accepted as a mechanism. Cheap path: coarse carries the transient always; promote to fine by proximity/return;
running rooms hot while far+unwatched is bounded by simultaneously-active-room count (a Wave-3 probe question).

## Relation / scope

- CLARIFIES + EXTENDS the COVERAGE of GG4/OR4 + d-bandhandoff-001 + d-return-reconstruction-001. NOT a precision
  raise — precision stays loose (consistent with d-bandhandoff-001 "approximately, no shimmer"). NEW scope:
  believability on the MID-TRANSIENT return + external-event reshaping, not only the settled cloud. (Assistant
  over-stated it as a precision raise earlier this session → corrected; owner: precision loose.)
- One-source-of-truth holds (d-return-reconstruction-001): coarse = truth, fine = reconstructed-consistent.
  Determinism is what makes "what would have happened" a well-defined reconstruction target.
- FORWARD-CONSTRAINT (cheap, not code now): do NOT architect the coarse layer to represent only the settled
  state / only room-totals; it must carry the transient + front position + applied external events at enough
  fidelity for an indistinguishable-to-the-eye reconstruction. Subordinates the s-research-008 knobs
  (graph-of-fronts / sub-sectors / checkerboard / source-seed): chosen by whether they let the TRANSIENT
  reconstruction avoid dissonance, not for precision.
- Independent ChatGPT deep-research cross-pass (owner-run, §7 prompt of s-research-008) CONFIRMED the
  s-research-008 verdict (no exotic family beats the sector-band graph under the hard filters; add horizontal
  subdivision + N layers); it adds a "sparse regular checkerboard" variant (determinism-simpler than conformal
  cut-cells) to fold into the Wave-3 probe menu. Both passes agree the decisive evidence is the half-day
  integer probe, not more literature.

## State

- d-returnfidelity-001 → answered (NOW.md decision_inbox).
- Active bet t-2/c-exec-006 (2-band day-one) UNCHANGED + not blocked (G1). Wave-3 shape inherits this bar +
  the s-research-008 probe menu (+ checkerboard variant).
- finding-1 (OR1 surface rises with quantity) = separate in-leg fix; the developer-response packet was handed
  to the owner this session (findings 2/3/4 dispositioned; the two codex-execution-smoke findings out of scope).

END_OF_FILE: live/indie-game-development/history/s-decide-006.md
