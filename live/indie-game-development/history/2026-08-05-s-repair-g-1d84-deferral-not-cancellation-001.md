# RESULT — s-repair-g-1d84-deferral-not-cancellation-001

direction: indie-game-development
play: repair
node: g-1d84
date: 2026-08-05
trigger: владелец сказал «это неправильно записано» — прямая поправка записанного смысла его слов.

## 1. противоречие

**Состояние утверждало, что владелец ЗАКРЫЛ два вопроса. Он их только ОТЛОЖИЛ.**

Его поправка дословно 2026-08-05: «Прежде я не говорил, что вообще не, стены не нужно будет подымать,
что сквозь стены хождение не чинить. Я имел в виду для выполненной задачи, которая тогда выполнилась,
это было не так важно, но вообще, естественно, это надо будет чинить».

Ошибка ноги названа без смягчения: она взяла две ОТСРОЧКИ, привязанные к одной уже выполненной
задаче, и записала их как отмену вопроса вообще, а затем разослала эту отмену запретами в три
свежих наряда. Это ровно тот класс, за который направление уже платило в обратную сторону — отказ
владельца превращали в требование; здесь его отсрочку превратили в постоянный запрет.

## 2. что реконструировано

Обе отсрочки были сказаны про `t-scene-2` и её приёмку:

- «Не надо стены поднимать, похуй, потом поднимем, когда будем делать» — сказано, когда нога
  предложила поднять стены ПЕРЕД его просмотром переезда хозяина. Слово «потом поднимем» само по себе
  прямо говорит, что починка будет.
- «Я выбрал, что пока ничего не менять, пусть хотя ходит через стены» — вариант Б из показанных ему
  двух, про то, чинить ли маршрут ДО его просмотра. Слово «пока» — тоже про срок, а не про отмену.

**Третий вопрос той же группы поправкой НЕ затронут и остаётся закрытым:** «сквозь стены не проходит,
я доказал, я в сцене, но ничего не проходит» — это его собственная ПРОВЕРКА проходимости стен
носильщиками на нынешних скоростях, а не отсрочка. Запись о дыре `WalkSpaceProbe` уже закрыта им и
уже содержит оговорку, что вопрос возвращается, если появится скорость на порядок выше.

## 3. исправленное состояние

**`NOW.md/issues` — две записи:**

- `i-host-walks-through-walls-001`: поле `owner_ruling` переписано с «Вопрос ЗАКРЫТ» на «ОТЛОЖЕНО,
  А НЕ ЗАКРЫТО» с обеими его репликами — исходной и поправкой; сила токена названа явно: не чинить в
  `t-scene-2` и не чинить заплаткой, чинить ОБЯЗАТЕЛЬНО в `t-host-2`. `review_when` дополнен словами
  «то есть ПОЧИНКОЙ, а не забвением».
- `i-house-walls-below-eye-level-001`: добавлено поле `owner_ruling` с обеими его репликами;
  `issue` очищен от «решил не поднимать» и держит теперь измеренное число и известную починку
  (восемь пар: `scale.y` 1.5 → 2.6, `position.y` 0.75 → 1.3); `review_when` переписан — закрывается
  ПОДНЯТЫМИ стенами, а «и так устраивает» остаётся возможным, но не является умолчанием; `evidence`
  переведена с `b7ae1fa4` на актуальный `2eee9719`.

**Три наряда, ВСЕ ТРИ УЖЕ В РАБОТЕ, — формулировки исправлены:**

- `c-exec-g-1d84-cargo-tight-spot-001` (`WIN-U2`): §context и §boundaries переписаны с «НЕ ЧИНИТЬ,
  вопросом домой не поднимать» на «отложено, а не отменено; в объём ЭТОЙ задачи не входит; у каждой
  починки есть свой дом». Добавлен честный ход: если узкое место без поднятых стен не проверяется
  глазом — это вопрос ДОМОЙ одной строкой, а не самостоятельное расширение.
- `c-exec-g-1d84-host-under-the-judge-002` (`WIN-U1`): §context переписан так же, плюс добавлен
  НЕ-требовательный контекст — раз `t-host-2` скоро начнёт считать путь по местам дома, шов «откуда
  хозяин берёт правду о мире» лучше сразу делать расширяемым, чтобы не платить дважды.
- `c-exec-g-1d84-conditions-panel-001` (`WIN-U4`): §boundaries переписан; добавлено прямое требование
  не писать в отчёте, будто вопросы закрыты.

**ОБЪЁМ НИ ОДНОГО ИЗ ТРЁХ НАРЯДОВ НЕ ИЗМЕНЁН.** Ни одна из трёх задач не должна была чинить ни стены,
ни маршрут ни до поправки, ни после. Изменена только ФОРМУЛИРОВКА причины, поэтому идущим ногам
перечитывать наряд не обязательно и ни одна строка уже сделанной работы не становится неверной.

## 4. что осталось владельцу

Один вопрос, и он поставлен ему в чате, а не решён здесь: наряд `c-exec-g-1d84-cargo-tight-spot-001`
ИДЁТ ПРЯМО СЕЙЧАС в `WIN-U2` и трогает тот самый `ThreeRoomHouse.prefab`. Его слова «потом поднимем,
когда будем делать» на эту задачу формально уже ложатся. Поднять стены заодно там или отдельной
задачей — его выбор; нога его не делает, потому что это расширение идущего наряда.

## state_changes

- `NOW.md/issues`: переписаны `i-host-walks-through-walls-001` и `i-house-walls-below-eye-level-001`
  (см. §3). Больше в `NOW.md` не тронуто ничего.
- `work/c-exec-g-1d84-cargo-tight-spot-001-call.md`,
  `work/c-exec-g-1d84-host-under-the-judge-002-call.md`,
  `work/c-exec-g-1d84-conditions-panel-001-call.md`: исправлены формулировки отсрочек; `goal`,
  `done_when`, `база`, `return`, `budget` и объём НЕ тронуты.
- Препендить ровно один LOG receipt и сохранить этот полный RESULT в `history/`.
- CHARTER/TREE/knowledge, продуктовый репозиторий, реестр слотов и ветки не менять.

## friction

Класс называется прямо, потому что он повторяемый: **нога превращает временную отсрочку владельца в
постоянное правило и рассылает его дальше.** Зеркальный класс (его отказ превращают в требование) в
памяти направления уже есть. Общий корень один — сила токена: отсрочка покрывает СРОК и НАЗВАННУЮ
задачу, а не сам вопрос. Одной строки FRICTION достаточно; правило OS этой ногой не меняется.

## play_check

- Шаг 1 «назвать противоречие» — исполнен, в двух строках, с его дословной поправкой.
- Шаг 2 «реконструировать» — исполнен: обе исходные реплики найдены и перечитаны в их контексте;
  третий похожий вопрос проверен отдельно и оставлен закрытым, потому что он был ПРОВЕРКОЙ, а не
  отсрочкой.
- Шаг 3 «исправленное состояние» — по одной причине на изменение, все прежние факты сохранены.
- Шаг 4 «подтверждение владельца» — сама поправка И ЕСТЬ его указание; ничего не удалено, ни одна
  полоса, наряд, запись знания или подписанный артефакт не тронуты, поэтому граница удаления
  (`os/plays/repair.md` шаг 4) не задета. Открытый вопрос вынесен ему, а не решён.
- Шаг 5 «friction» — одна строка выше.

## next

Идущие три наряда продолжают работу без изменений. Владельцу — один вопрос про стены в идущем
наряде по дому.

END_OF_FILE: live/indie-game-development/history/2026-08-05-s-repair-g-1d84-deferral-not-cancellation-001.md
