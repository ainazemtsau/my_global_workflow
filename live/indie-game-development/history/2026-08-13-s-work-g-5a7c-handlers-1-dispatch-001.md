# RESULT — s-work-g-5a7c-handlers-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-handlers-1
track: householder-life
date: 2026-08-13
outcome_kind: dispatch — наряд на `t-handlers-1` выпущен целиком; блокеров нет

owner_approved: |
  Слова владельца этой ногой НЕ запрашивались и не нужны: она выпускает наряд исполнителю, а его
  суждение стоит на ЗАКРЫТИИ задачи (строка 5 карточки — «владелец произвёл хотя бы одну комбинацию
  сам в собранной игре»), и требовать его сейчас значило бы поставить гейт на его поведение.

  Использованы только уже сказанные им слова, дословно и с местом:

  - **Конвейер обработчиков он вырезал, а сами обработчики заказал:** «конвейер обработчиков,
    наверное, всё-таки не надо… мы не знаем, как это работает… нам нужно просто написать какие-то
    хендлеры вот для нашего текущего шва» (`cards/t-handlers-1.md` §`note`).
  - **Расширяемость — жёсткое требование на этапе планирования:** «мне нужно, чтобы ВСЕГДА мы делали
    расширяемый функционал… Я хочу, чтобы это на этапе планирования было»
    (`knowledge/core-is-extended-never-rewritten.md`).
  - **Плита снята им самим:** «мне не надо, чтобы была такая конкретная реакция… у нас нет плиты,
    нет»; «Сейчас как бы по мне нормально, что когда хозяин видит игрока, мышь, он за ним бежит»
    (`history/2026-08-13-s-work-g-5a7c-sight-1-owner-word-001.md`).

outcome: |
  **НАРЯД `c-exec-g-5a7c-handlers-1-001` ВЫПУЩЕН. Блокера нет ни одного.**

  **ЗАВИСИМОСТИ ЗАКРЫТЫ ВСЕ ТРИ, перемерено поимённо:** `t-sight-1` — `done` сегодня же биндинговой
  проверкой; `t-place-fact-1` — `done`; `t-thing-1` — `done`. Это ровно те три, что называет
  §`note` карточки. Поэтому `t-handlers-1` переведена `blocked → active`: у задачи появился живой
  наряд.

  **ГЛАВНОЕ ИЗ ВЫДАЧИ, И ОНО МЕНЯЕТ ФОРМУ ЗАДАЧИ: МЕХАНИЗМ УЖЕ ПОСТРОЕН, А ПОВЕДЕНИЯ НЕТ.**
  Конвейер обработчиков, два типа обработчиков и ветки поведения существуют в ядре И читаются из
  JSON — загрузчик собирает `type: "fact-combination"` и `type: "accumulated-fact"` с именованным
  отказом на неизвестную реакцию, ветку и лишнее поле. А в опубликованном игровом профиле оба
  массива ПУСТЫ. Значит наряд покупает не слой, а написанные строки и то единственное, чего в
  механизме недостаёт.

  **ЧЕГО НЕДОСТАЁТ — НАЙДЕНО ТОЧНОЕ МЕСТО, ОДНА ФУНКЦИЯ.** Существующая комбинация срабатывает,
  когда два факта просто ОБА ЖИВЫ, беря первое непогашенное совпадение каждого. Она **не проверяет,
  что факты про одно место или про один предмет** — то есть строка 1 карточки («совместимые факты
  про одну комнату или один предмет») сегодня не выполнена, хотя комбинация как таковая есть. При
  этом комната в факте УЖЕ лежит: её привезла закрытая `t-place-fact-1`. Не хватает ровно условия
  локальности при срабатывании.

  **ВТОРОЙ ВИД НАЗВАН НАПРАВЛЕНИЕМ, КАК ТРЕБУЕТ ПРАВИЛО, И НЕ ВЫДУМАН МНОЙ.** Локальность въезжает
  ТРЕТЬИМ ВИДОМ обработчика — новым способом срабатывания рядом с двумя существующими, а не вторым
  значением поля у существующего. Взят он из строки 1 самой карточки и из цепи документа владельца
  («шум возле тумбочки → … → будильник звонит из неправильного места»), а не из моей головы, как и
  требует оговорка 1 гейта. Гейт занимает строку `done_when` наряда, а не границы — оговорка 3.
  Доказательство — состав коммита против ПОИМЁННОГО списка из шести файлов ядра, и разрешённая цена
  названа вслух заранее: один новый файл вида, одна ветка `case` в загрузчике и строки в профиле.

  **ЗА ЧТО НЕ НАДО ПЛАТИТЬ ВТОРОЙ РАЗ, И ЭТО НАЙДЕНО ТОЛЬКО ИЗМЕРЕНИЕМ:** строка 6 карточки — «чья
  строка сработала, видно» — на опубликованной голове УЖЕ ВЫПОЛНЕНА. Имя выбранного обработчика
  штампуется конвейером, едет через результат шага и попадает в наблюдение вместе с именем ветки.
  Наряд требует не сломать это и предъявить вывод, а не строить заново.

  **ДРОЖЬ В ЭТОТ НАРЯД НЕ ВОШЛА, и это решение, а не забывчивость.** Основание в трёх словах: другая
  поверхность (движение и построение пути против слоя решений), собственное требование улики сначала
  ИЗМЕРИТЬ, и правило размера — смешанная нога режется до сборки. Улика не тронута, её `review_when`
  цел, а наряд прямо запрещает исполнителю чинить дрожь по гипотезе и требует записать наблюдение,
  если наткнётся. Отдельной ногой она вынесена владельцу вопросом, а не заведена мной как задача.

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ ЧТЕНИЕМ БАЙТОВ НА `c485b30e704b1706675dd92d15c5223b0d166b92`**
  (`origin/main` = `origin/dev`, `git ls-remote origin`).

  ## Зависимости — поимённо

  `osctl card show` по каждой: `t-sight-1` → `status: done`; `t-place-fact-1` → `status: done`;
  `t-thing-1` → `status: done`. Три из трёх, названных §`note` карточки задачи. Карточка при этом
  стояла `blocked` и **без `unblock_when`** — расхождение состояния, снятое здесь переводом в
  `active`, а не обойдённое.

  ## Механизм существует — пути и строки

  - `Assets/TunnelCrew/Core/Householder/HouseholderDecisionPipeline.cs:63` — интерфейс
    `IHouseholderDecisionHandler`; `:100-120` — сам конвейер.
  - `Assets/TunnelCrew/Core/Householder/HouseholderFactCombinationDecisionHandler.cs` — тип «два
    факта живы».
  - `Assets/TunnelCrew/Core/Householder/HouseholderAccumulatedFactDecisionHandler.cs` — тип «факт N
    раз», уже умеющий сужаться комнатой.
  - `Assets/TunnelCrew/Profiles/HouseholderProfileLoader.cs:634-840` — оба типа собираются из JSON;
    `:561-632` — ветки поведения; неизвестная реакция, неизвестная ветка и лишнее поле отвергаются
    названно (`:695-760`).
  - `Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs:450-460` — ветка активируется.

  ## Поведения нет — байты профиля

  `Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json` на `c485b30e`:
  `"behaviorBranches": []`, `"decisionHandlers": []`. Единственная живая строка данных про решения —
  один `activeResponseRules` на `repeated-heavy-noise`.

  ## Точное место недостачи

  `HouseholderFactCombinationDecisionHandler.cs:63-88` — цикл ищет первое непогашенное совпадение
  `_firstFactName` и первое непогашенное совпадение `_secondFactName` и на этом останавливается.
  Ни комната, ни предмет в условии не участвуют. Комбинация «шум на кухне + взгляд в спальне»
  сегодня дала бы одно происшествие.

  ## Строка 6 уже выполнена — цепь целиком

  `HouseholderDecisionPipeline.cs:176` (`WithDecisionHandlerName`) →
  `Householder.cs:2215`, `:2257`, `:2284` (`HouseholderStepResult.DecisionHandlerName`) →
  `Editor/HouseholderObservationSnapshotJson.cs:42-47` (`selectedHandler`, `behaviorBranch`).

  ## Контракт и пересечение

  - `os/engineering/CONTRACT_VERSION` → `current: 36`; `validation.config:5` продукта →
    `"synced_contract_version": 36`. **Дрейфа нет, Re-sync не должен.** Наряд запинен на `36`.
  - Потолок контракта 36 — три строки `done_when` без письменного токена владельца на перебор:
    наряд несёт ровно три, и все восемь строк карточки уложены в них.
  - Пересечение с `c-exec-g-5a7c-loot-foundation-001` — ноль по объявлению обеих сторон: он сам себе
    запретил «расширять behavior seam в generic handler pipeline/combinations» (его §`boundaries`),
    а этот наряд не открывает `Core/Cargo/**`, `Network/Cargo*`, `Settings/GameRulesSettings.*`,
    `Assets/TunnelCrew/World/**`, сцены и префабы.
  - Слот не присвоен намеренно: оба наряда выбирают селектором, и наряд требует перечитать состояние
    перед захватом. На момент выпуска `gascoop-slot-state.v1.json` — все четыре `AVAILABLE`,
    `lease none`.

  ## Базис прогонов

  `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **252 / 0 / 0**,
  прогнано сегодня биндинговой проверкой на этой же голове. Наряд требует число против 252.

state_changes: |
  1. `t-handlers-1`: `blocked → active`. Основание — все три названные зависимости закрыты, и это
     перемерено поимённо. `unblock_when` на карточке не было; поле не заводится.
  2. Выпущен наряд `c-exec-g-5a7c-handlers-1-001` (`ready`, `to: executor`, `kind: engineering`,
     `repo: ainazemtsau/GasCoopGame`, `engineering_contract: 36`, `track: householder-life`,
     `for: t-handlers-1`, `basis: c485b30e…`) плюс его файл
     `work/c-exec-g-5a7c-handlers-1-001-call.md`.
  3. `c-work-g-5a7c-handlers-1-dispatch-001`: возвращающийся CALL закрыт.
  4. Полный RESULT сохранён в `history/2026-08-13-s-work-g-5a7c-handlers-1-dispatch-001.md`.

  **ЧЕГО ЭТА НОГА НЕ СДЕЛАЛА.** Не тронута улика про дрожь и её `review_when`. Не заведено ни одной
  новой задачи. Не тронуты `t-loot-1`, оба наряда по луту и ветка `preserve/...`. Не тронута карточка
  ставки. Не тронут `t-lab-2`: он ждёт эту задачу, и его разблокировка принадлежит её закрытию. В
  продукте не изменено ничего.

captures:
  - |
    **ЦЕНА ГЕЙТА ВТОРОГО ВИДА ИЗМЕРЕНА ВПЕРВЫЕ НА ЖИВОМ НАРЯДЕ, И ОНА ОКАЗАЛАСЬ НУЛЕВОЙ.** Гейт
    поставлен вчера и требует называть второй вид при нарезке. Здесь называть было ИЗ ЧЕГО, не
    придумывая: строка 1 карточки уже требовала локальности, а измерение показало, что именно её и
    не хватает механизму. То есть гейт не добавил объёма — он заставил назвать вслух то, что и так
    было куплено, и превратил это в проверяемый состав коммита. Второй случай проверки — лут; там
    вид пришлось брать из уже доказанной формы `t-thing-1`.
  - |
    **ПОЛЕЗНЫЙ КЛАСС НАХОДКИ ДЛЯ БУДУЩИХ ВЫДАЧ: «механизм построен, данных нет».** Две строки из
    восьми в этой карточке оказались либо уже выполненными (имя обработчика в наблюдении), либо
    дешевле ожидаемого (комбинация есть, локальности нет). Нашлось это чтением ЗАГРУЗЧИКА, а не
    ядра: ядро говорит, что механизм есть, а загрузчик говорит, доедет ли он до игрового файла. Для
    дорожки хозяина это самое дешёвое место замера перед нарядом.

decisions_needed:
  - q: |
      Дрожь в погоне — отдельной ногой сейчас или ждёт? Ты видел её сам: хозяин бежит на крысу и
      дёргается, чаще в проёмах. В наряд на обработчики она не вошла — другая поверхность, и её
      улика требует сначала измерить. Слоты свободны, дорожки не пересекаются, так что нога может
      идти параллельно уже сегодня.
    options: [отдельная нога сейчас параллельно, ждёт до конца волны]
    recommendation: |
      Отдельная нога сейчас. Дрожь портит ровно то, что ты вчера принял, и видна тебе в каждом
      прогоне; поверхности не пересекаются с обработчиками и с лутом, слоты свободны. Честная цена:
      это плюс одна нога к порогу смерти 3, который считает расход ног на закрытую задачу и сегодня
      стоит около 3.86 против базовых 4.0 — то есть запас маленький. Если решишь ждать, улика
      никуда не денется, но чинить придётся в волне, где движение и так будет тронуто.

play_check:
  - 1 recite — done. Пересказаны восемь строк `t-handlers-1`, три её зависимости, обязательство ставки (критерий 6 узла, райдер волочения, гейт второго вида) и потолок контракта 36 в три строки.
  - 2 owner inputs (owner) — НЕ запрашивались, и основание названо: нога выпускает наряд исполнителю, а суждение владельца стоит на ЗАКРЫТИИ (строка 5 карточки — его комбинация в собранной игре), требовать его сейчас значило бы поставить гейт на его поведение. Использованы только уже сказанные им слова, процитированные дословно с местом в `owner_approved`. Один вопрос вынесен в `decisions_needed` — про дрожь, которую он сам и наблюдал.
  - 3 do the work — измерение продукта на `c485b30e`, обнаружение «механизм есть, данных нет», нахождение точного места недостачи, именование второго вида, написание наряда с поимённым списком неприкасаемых файлов.
  - 4 self-check — done против четырёх строк возвращающегося CALL: (1) состояние приведено, три зависимости названы поимённо; (2) наряд выпущен целиком, гейт второго вида стоит строкой `done_when`, вид назван, райдеры решены; (3) про дрожь сказано вслух — не касается, и почему; (4) ни одна чужая дорожка, задача, улика и ветка не тронуты.
  - 5 close — dispatch. Не последняя задача ставки (`t-loot-1` активна, `t-house-asset-1` открыта, `t-lab-2` ждёт), поэтому продолжение — сам выпущенный наряд, а не `review`.
  - G5 — не применяется: ничего не закрыто.
  - G7 — done с оговоркой вслух. Игровых решений не принято: второй вид назван Направлением потому, что правило прямо это требует и запрещает отдавать исполнителю на выдумывание, и взят он из строки 1 самой карточки и цепи документа владельца. Техническое КАК остаётся у исполнителя: как назвать вид, чем выразить локальность — комнатой, предметом или обоими, — и какое одно физическое действие добавить.
  - G3 — done. Аппетит не расширен: ноль новых задач, ноль новых улик, дрожь вынесена вопросом, а не работой; объём наряда — восемь строк карточки, уложенных в три строки потолка контракта.
  - G10 — done. RESULT финальный, поля полные.

log: наряд на t-handlers-1 выпущен целиком и без блокеров, а выдача нашла, что задача не та, какой выглядела — МЕХАНИЗМ УЖЕ ПОСТРОЕН, А ПОВЕДЕНИЯ НЕТ: конвейер обработчиков, два типа обработчиков и ветки поведения существуют в ядре и УЖЕ читаются из JSON загрузчиком с именованным отказом на неизвестную реакцию и лишнее поле, а в опубликованном игровом профиле оба массива пусты, значит наряд покупает написанные строки, а не слой; найдено точное место недостачи, одна функция — существующая комбинация срабатывает, когда два факта просто ОБА ЖИВЫ, и не проверяет ни комнату, ни предмет, поэтому строка 1 карточки сегодня не выполнена, хотя комбинация как таковая есть, а комната в факте уже лежит от закрытой t-place-fact-1; отдельно измерено, за что НЕ надо платить второй раз — строка 6 «чья строка сработала, видно» уже выполнена целиком, имя обработчика штампуется конвейером и доезжает до наблюдения вместе с именем ветки; ВТОРОЙ ВИД НАЗВАН НАПРАВЛЕНИЕМ, как требует правило, и взят из строки 1 карточки и цепи документа владельца, а не выдуман — локальность въезжает ТРЕТЬИМ видом обработчика рядом с двумя существующими, гейт занимает строку done_when, а не границы, и доказывается составом коммита против ПОИМЁННОГО списка шести файлов ядра с заранее названной разрешённой ценой; три зависимости перемерены поимённо и все закрыты, поэтому карточка переведена blocked → active, а стояла она blocked БЕЗ unblock_when — расхождение снято, а не обойдено; дрейфа контракта нет, обе стороны на 36, и наряд несёт ровно три строки потолка, в которые уложены все восемь строк карточки вместе с райдером на замер волочения числом; ДРОЖЬ в наряд НЕ вошла и это решение — другая поверхность, её улика требует сначала измерить, а смешанная нога режется до сборки, поэтому она вынесена владельцу ВОПРОСОМ с честной ценой в одну ногу против порога 3, а не заведена задачей

next: |
  `c-exec-g-5a7c-handlers-1-001` — наряд исполнителю, слот выбирается селектором. Он либо возвращает
  построенные обработчики со сценарием для владельца, либо один точный блокер.

  Параллельно в `house-and-things` идёт `c-exec-g-5a7c-loot-foundation-001` от той же головы;
  пересечение по файлам объявлено нулевым обеими сторонами.

  Ждёт ответа владельца: дрожь в погоне — отдельной ногой сейчас или позже.

END_OF_FILE: live/indie-game-development/history/2026-08-13-s-work-g-5a7c-handlers-1-dispatch-001.md
