# RESULT — s-work-g-5a7c-cargo-2-dispatch-001

play: work · выдача наряда на разблокирующую задачу дорожки груза
direction: indie-game-development · node: g-5a7c · track: cargo-hands · task: t-cargo-2
date: 2026-08-11

base_read: OS — `wt/indie-game-development`, дерево чисто. Продукт перемерен `git ls-remote origin`
в этой ноге: `origin/dev` = `origin/main` = `2f7f2e87b3d3fa1e3b40e50f5063a87530fc5187`, дерево
`79fb9710f2d02f9983c94a0704e8ef96a454ee3f`. Слот WIN-U1 перемерен настоящим селектором:
`CLEAN / AVAILABLE / lease none`, HEAD знак в знак базис. Параллельная сессия за время разговора
закрыла `t-react-1` и выпустила наряд на `t-react-2` — перечитано с диска, не с памяти.

## outcome

Выпущен наряд `c-exec-g-5a7c-cargo-2-001` в WIN-U1 на задачу, которая разблокирует владельца:
сегодня он не может проверить НИ ОДНУ строку дорожки груза, потому что соло груз не отрывается от
пола вообще.

**Главная работа этой ноги — не написать наряд, а не дать построить не то.** Исполнитель и
собственная логика «разделим мощность → станет выше» ведут в тупик, и это опровергнуто арифметикой
в самом наряде: потолок `200` Н СТОЯ не срабатывает (держать надо `144`), поэтому разделение
мощности само по себе не поднимает соло ни на миллиметр. Высота стоя задаётся запрошенной высотой и
жёсткостью; потолок включается только НА ХОДУ, где горизонтальная тяга ужимает весь трёхмерный
вектор. Наряд разводит это в два разных дефекта с разными лекарствами.

## evidence — перемерено первой рукой на `2f7f2e87`

- `CargoHaul.HandForce`: `handY = max(0, TargetY − cargoY) × stiffness`; `Cap(...)` (`:344`) режет
  трёхмерную величину одним числом; `EffectiveHandLimit` (`:223`) берёт `hold.Strength` и делит на
  `1 + falloff × вынос`.
- `AuthoritativeCargoRoster.cs:729` — `var strength = rules.HoldStrength;`: источник мощности ОДИН
  на весь мир, хотя `CargoHold.Strength` поштучное. Это и есть шов, который остаётся достроить.
- Арифметика соло: вес `48 × 3 = 144`; `0.092 − 144/2000 = 0.020` — знак в знак
  `_cargoRestHeight: 0.02`. Лестница `0.020 / 0.056 / 0.068 / 0.074`, разброс `0.054` и от
  запрошенной высоты не зависит.
- Свободный горизонт на ходу: `sqrt(200² − 144²) ≈ 138.8` Н — за ним `Cap` ужимает и вертикаль.
- `AuthoritativeCargoRoster.cs:153`, `:161-164`: `landed = !held && IsActive && Contact.HasSupport`
  → `WithActivity(false)`. Тело гасится на ПЕРВОМ касании после отпускания — прямое нарушение
  подписанного правила `loot-is-inactive-until-a-beam-touches-it`, которое говорит «пока он падает,
  он живёт по-настоящему».
- `NetworkWalkerCourier.OnServerTick` вызывает `CreateCargoRules()` каждый тик — живая настройка
  чисел во время Play УЖЕ работает, строить её не надо. Это снимает половину его запроса про
  удобство и вписано в наряд как факт, а не как задача.

**Числа принесены на пробу и названы таковыми.** Соло `0.08` даёт лестницу
`0.080 / 0.116 / 0.128 / 0.134`, вчетвером ровно на уровне глаз `0.136`. Вариант `0.10` рассмотрен и
отклонён в тексте наряда с причиной: вчетвером он даёт `0.154` при росте мыши `0.16`, груз уезжает
выше глаз. Три класса на пробу: `144 / 300 / 720` Н, то есть одна / две / четыре руки при мощности
`200`.

## state_changes

1. Заведена карточка наряда `c-exec-g-5a7c-cargo-2-001` (`to: executor`, `for: t-cargo-2`,
   `track: cargo-hands`, `slot: WIN-U1`, `basis: 2f7f2e87`, `status: ready`).
2. Написан текст наряда `work/c-exec-g-5a7c-cargo-2-001-call.md`.
3. `t-cargo-2` остаётся `open`: наряд не выдан, пока нет расписки.
4. `NOW.md`, `g-5a7c` и текст ставки не трогать.

## captures

- Волна стоит на потолке размера: восемь задач при правиле «до восьми». Линия разреза в этом наряде
  названа, но её взятие делает девять — решение владельца, не исполнителя.
- `t-cargo-1` опубликована и ждёт его глаза ВМЕСТЕ с этой задачей: судить наклон на грузе, лежащем
  на полу, нечем.

## decisions_needed

Нет. Требование «один должен поднимать» — его слово 2026-08-11; величины принесены на пробу.

## play_check

- 1 recite: done — десять строк `t-cargo-2`, линия разреза и пять пунктов `evaluator` сведены
- 2 owner inputs (owner): done — требование дано его словами, число он оставил своему глазу
- 3 do the work: done — наряд написан по контракту 36, вся арифметика перемерена в байтах
- 4 self-check: done — ловушка «разделение поднимет высоту» опровергнута числами ДО работы
- 5 close: checkpoint — расписка исполнителя вне этой ноги

## Гейты

- **G1.** Один корень на дорожку: `cargo-hands` — этот, `householder` — `t-react-2`. Лимит два.
- **G3.** Аппетит не тронут этим нарядом; линия разреза названа, её взятие вынесено владельцу.
- **G5.** Задача не закрывается; её строку «судит его глаз» светлым путём закрыть нельзя, и это
  написано в наряде.
- **G7.** Числа принесены на пробу и прямо названы непринятыми; выбор за владельцем.

## log

наряд на разблокирующую задачу выдан в WIN-U1 от опубликованной головы 2f7f2e87, и главной работой было опровергнуть удобную неправду до постройки: потолок силы 200 стоя не срабатывает, потому что держать надо 144, поэтому разделение мощности само по себе не поднимает соло ни на миллиметр — высота стоя задаётся запрошенной высотой и жёсткостью, а потолок включается только на ходу, где горизонтальная тяга ужимает весь трёхмерный вектор и свободного горизонта остаётся 138.8 Н; два разных дефекта разведены с разными лекарствами, гашение тела на первом касании названо нарушением подписанного правила, а живая настройка чисел во время Play перемерена как УЖЕ работающая и снята с объёма

## next

1. Владелец отдаёт наряд исполнителю; первая строка возврата — расписка.
2. Возврат приходит HOME, публикуется, и только потом его глаз — один, на оба: подъём и наклон.
3. После публикации обеих дорожек встаёт задача-шов `t-settings-1` (разбор коробки настроек).
