# RESULT — s-work-g-5a7c-morning-remeasure-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-morning-content-1, полоса `утро`
track: утро
date: 2026-08-31
outcome_kind: work — первый живой замер дня снят на ветке БЕЗ починки зрения; вывод «утро не получает хода» скорее всего артефакт, и это ошибка направления, а не ноги

owner_approved: |
  Ни одна строка `done_when` не изменена. Решение о публикации не пересматривается: утро
  по-прежнему остаётся кандидатом.

outcome: |
  **ЖИВОЙ ЗАМЕР СНЯТ НА СТАРОМ КОДЕ, И ВИНОВАТО НАПРАВЛЕНИЕ.** Ноге велели снять замер «в своём
  слоте», и никто не проверил, на чём стоит её ветка. Перемерено:
  `git merge-base main slot/win-u4` = **`b22c00a2`**, а починка зрения опубликована позже,
  в `064ab568`. `git merge-base --is-ancestor 064ab568 slot/win-u4` → **нет**. В ветке
  `NetworkHouseholder.cs` по-прежнему передаёт `observer.Y`.

  **ЭТО ОБЪЯСНЯЕТ ГЛАВНЫЙ ВЫВОД НОГИ ЦЕЛИКОМ.** Её симптомы дословно совпадают с базой ДО починки:
  `HE SEES 0 OF 14 THINGS` (на `main` сегодня — `2 OF 14`) и
  `Mug — HE IS STANDING ON IT — no direction from his eye to it at all` — ровно та строка, ради
  которой чинили зрение. Замкнутый круг, который она описала — слеп → на кружку не смотрел →
  убеждение не обновляется → дело не закрывается → распорядок не выбирается никогда — **замыкается
  именно слепотой**, то есть тем, что в `main` уже снято.

  **Значит «утро не получает хода вообще» — скорее всего артефакт базы, а не факт продукта.** На
  `main` `Mug` даёт `SEES IT (3 of 3 body points clear)`, измерено ногой зрения.

  **СЛИЯНИЕ ПРОВЕРЕНО ВХОЛОСТУЮ И ЧИСТОЕ.** `git merge-tree --write-tree slot/win-u4 main` →
  `exit 0`; в полученном дереве **и** починка зрения (`state.Y` в обоих вызовах), **и** утро
  (`occupations: 4`, `gauges: 1`, `routine: 4`). Терять нечего, конфликтов нет.

  **НОГА ПОЙМАЛА СОБСТВЕННЫЙ СУРРОГАТ И ВЫБРОСИЛА ЕГО — ЭТО ОБРАЗЕЦ.** Первый прогон показал `who`
  и шестьдесят секунд неподвижности. Прежде чем записывать, она спросила у ЗАГРУЖЕННОЙ сборки метод
  `TryReportHouseholder` — его в сборке не оказалось, хотя в исходнике он есть и не под условной
  компиляцией. **Редактор держал старые скомпилированные скрипты.** Она запросила пересборку,
  дождалась, перемерила, и первый прогон в замер не включила. И отдельно назвала, что совпадение
  `dataPath` этого не гарантирует: редактор был её, а код в нём — чужой.

  **ЧТО УСТОЯЛО НЕЗАВИСИМО ОТ БАЗЫ.** Чтение исходника от базы не зависит:
  `WorldSituationNoise.cs` — 218 строк, **расстояния не знает вовсе**, только сила → полоса.
  Значит перед глухотой на ходу стоит **ещё одно условие**, и тихое не становится фактом даже у
  стоящего. Это уточняет предмет будущей волны слышимости: там не одно место, а минимум два.

evidence: |
  Перемерено направлением на продуктовом репозитории:

  - `git merge-base main slot/win-u4` → `b22c00a277e71382067fda9afced70cf809f3f57`;
  - `git merge-base --is-ancestor 064ab568 slot/win-u4` → ложь;
  - `git show slot/win-u4:Assets/TunnelCrew/Network/NetworkHouseholder.cs` — в вызове `CanSee`
    третьей координатой стоит `observer.Y`;
  - `git merge-tree --write-tree slot/win-u4 main` → `exit 0`, дерево `fb293c56`;
    в нём `state.Y` в вызове зрения и профиль с `occupations: 4`, `gauges: 1`, `routine: 4`.

  Из возврата ноги, как улика: путь к живому файлу
  `householder-why-20260831-151221.txt`; `noise 5 ×1` и `×2` → `noticed: 0 fact(s)`;
  контроль `noise 40 ×2` → `noticed: 2 fact(s)`, `InvestigateLanding`; 730 / 729 зелёных /
  1 красный; дерево чистое, лиза отпущена, `slot/win-u4` = `abfbccfe`.

state_changes: |
  ДОПИСАНО (1 документ наряда):
  - `work/2026-08-31-call-morning-content-1.md` — четвёртое дополнение: влей `main`, перемерь
    всё заново тем же протоколом, прежний замер выбрось; проверь пересборку своим же приёмом;
    если утро и после слияния не получает хода — это настоящая находка и СТОП.

  СОЗДАНО (1 улика):
  - `i-a-banner-from-the-cancelled-game-spans-the-screen-001` — через весь экран полигона висит
    «DELIVERY POINT — BRING THE CARGO HERE» от отменённой игры про груз.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-morning-content-1` остаётся `active`; публикация по-прежнему снята.

captures: |
  - **НАПРАВЛЕНИЕ ОТПРАВИЛО МЕРИТЬ, НЕ ПРОВЕРИВ БАЗУ, И ЭТО ТРЕТЬЯ ОШИБКА ЭТОГО КЛАССА ЗА ДЕНЬ.**
    Утром оно ошиблось в предпосылке про fast-forward, днём разрешило пометку, не заглянув во все
    гейты, сейчас послало на живой замер ветку, отставшую на одну публикацию.
    **Правило-кандидат: перед любым «померь вживую» первым делом назвать, на каком коммите стоит
    дерево, и сверить с `main`.** Замер на неизвестной базе — не замер.
  - **ИСПОЛНИТЕЛЬ СПРОСИЛ У СБОРКИ, А НЕ У ИСХОДНИКА, И ЭТО ПОЙМАЛО ЛОЖНЫЙ ПРОГОН.** Приём стоит
    правила: **чтобы доказать, что редактор гоняет твой код, спроси у ЗАГРУЖЕННОЙ сборки то, чего
    в старой быть не могло.** Совпадение путей и профиля этого не доказывает — он это назвал сам.
  - **ЧАСТЬ НАХОДКИ ВЫЖИВАЕТ ОШИБКУ БАЗЫ, ЧАСТЬ НЕТ, И ЭТО НАДО РАЗДЕЛЯТЬ ЯВНО.** Чтение
    исходника (`WorldSituationNoise.cs` не знает расстояния) верно при любой базе; живые числа —
    нет. При разборе испорченного замера первым делом делить улики на «прочитано» и «наблюдено».

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — возврат ноги с живым замером и тяжёлым выводом.
  2. WORK — база ветки проверена ПЕРВЫМ делом и оказалась старой; слияние проверено вхолостую;
     улики разделены на выживающие ошибку базы и не выживающие.
  3. (owner) — не требуется: исправление собственной ошибки направления.
  4. CLOSE — RESULT, применение, коммит; ответ уходит в WIN-U4.

log: |
  первый живой замер дня снят на ветке БЕЗ починки зрения — ошибка направления, отправившего мерить
  и не проверившего базу; симптомы дословно совпали с базой до починки, значит вывод «утро не
  получает хода» скорее всего артефакт; слияние main в ветку проверено вхолостую и чистое,
  сохраняет и зрение, и утро

next: |
  Нога вливает `main` и меряет заново. Подготовка полигона идёт в WIN-U1 — **её базу направление
  проверило: она стоит ровно на `064ab568`.**

  Свободны WIN-U2, WIN-U3 и WIN-U4.
