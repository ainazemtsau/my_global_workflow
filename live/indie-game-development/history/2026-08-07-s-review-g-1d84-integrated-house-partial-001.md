# RESULT — s-review-g-1d84-integrated-house-partial-001

direction: indie-game-development
play: review
node/task: g-1d84 / review
date: 2026-08-07
verdict: partial
owner_approved: |
  Сессия показала владельцу вердикт `partial`, полный разбор по десяти критериям,
  таблицу привязок находок к будущим волнам и ТОЧНЫЙ текст `closed_when` целиком.
  Его слова, каждое на прямо заданный вопрос, записаны ровно той силы, какой были:

  1. «насчет первого пункта да» — узел `g-1d84` → `done`, ставка закрывается
     вердиктом `partial`.
  2. «Второму пункту да» — таблица привязок принята как есть: отдельной ноги на
     исправления не заводится, каждая находка едет с волной, которая и так трогает
     её поверхность.
  3. «пускай аналитика допустима, чтобы она нам не мешала, это отдельно проверка,
     потом решим» — гейт на `UnityConnectSettings.m_Enabled` НЕ ставится, откат
     после ручной сборки прекращается; публичная сборка остаётся отдельным
     вопросом.
  4. «Ну давай твою рекомендацию, потому что для меня код ничего не говорит» —
     судьбу `g-3f90` он делегировал сессии. Рекомендация (сложить в узел
     настоящего ядра) записана в наряд как рекомендация и НЕ исполнена здесь.
  5. «принимаю» — на показанный целиком точный текст `closed_when`. Это
     единственное, что подписано в TREE.

  ОТДЕЛЬНО И РАНЬШЕ В ТОЙ ЖЕ СЕССИИ он ПОПРАВИЛ Направление по границе своих
  прежних слов: «когда говорю „все работает", блядь, значит, все работает. То есть
  там хозяин, какая-то точка доставки, переноска работает и так далее». Это прямое
  слово Направлению, а не пересказ отчёта.

  ЕГО ПЕРВЫЙ ХОД БЫЛ ОБРАТНЫМ и записан вместе со вторым, потому что второй его
  отменяет, а не прячет: «узел точно не закрываем» → после разбора «лучше закрыть
  его, то, что мы сделали, то, для чего он планировался, и просто создать новый,
  который типа настоящее ядро».

## outcome

Активная ставка `g-1d84` закрыта вердиктом `partial`. Узел `g-1d84` закрыт
`done` — не проверкой, а его словом, ровно по механизму, который карточка сама и
описывает критерием 10 («Узел закрывает его слово, а не проверка»). Эти две вещи
верны одновременно и это не описка: узел судится его словом, ставка судится
текстом `done_when`.

ПРОВЕРЕННАЯ ЦЕННОСТЬ, ВЫЖИВШАЯ ПОД ОПРОВЕРЖЕНИЕМ: одна запускаемая сцена, в
которой одновременно живут дом из примитивов (три комнаты, семь названных мест,
два названных прохода), хозяин под судьёй со своим текстовым распорядком, двое на
разных копиях от первого лица с одним общим физическим грузом, шум от контакта
груза с домом, названная точка доставки за домом и собранный exe, который он
запустил двумя копиями.

`done_when` целиком опровержение не пережил: четыре клаузы сломались (ниже).
Поэтому не `met`. Это не `killed`: порог смерти не сработал, временного потолка не
было. Это не `obsolete`: назначение узла не отменено более высокой стратегией — он
сам сказал, что ядро продолжается следующими волнами.

Review не открыл новую ставку, не создал задач и полос, не сочинил карточку
преемника и не решил судьбу `g-3f90` за владельца.

## evidence

Опровержение шло по байтам на продуктовом `main` = `origin/main` = `352f96b0`
(чистое дерево), десятью параллельными read-only проходами; продукт не менялся.
Ни один вывод не принят со слов продуктового отчёта.

**Критерий 1 (ОДНА ИГРА) — выжил.** `Scenes/IntegratedHouse.unity` — 11 корней;
дом (`ThreeRoomHouse` PrefabInstance) и хозяин (`Householder` PrefabInstance)
отдельными корнями, оба активны без сценовых переопределений `m_IsActive`; камера
первого лица корнем; груз создаётся судьёй безусловно
(`NetworkWalkerCourier.cs:51-53` → `CargoBody.cs:49`). Все четверо считаются в
ОДНОМ тике (`CarryWorld.cs:55-56`) и едут в ОДНОМ снимке
(`NetworkWalkerCourier.cs:162-217`). Четыре попытки доказать, что что-то выключено
по умолчанию — хозяин, маршрут, профиль, груз — провалились на байтах. Наблюдение
целиком закрыто его прямым словом (см. `owner_approved`).

**Критерий 2 (ГРУЗ И ЦЕЛЬ ЗАХОДА) — выжил.** Главная угроза («игра показывает» —
редакторская кнопка, которой в билде нет) НЕ подтвердилась: `World/DeliveryPoint.cs`
рисует настоящую `MeshRenderer`-геометрию и `OnGUI`, единственный `#if UNITY_EDITOR`
в файле — гизмо. Проём посчитан числами: 3.00 м, худший поворот балки 2.4×0.5 =
2.4515 м — проходит под любым углом; ходок радиуса 0.45 тоже. Оболочка дома
проверена по всем четырём углам, восточный проём — единственная дыра. Счёта,
награды, провала, повторного захода и экономики в `Core/` нет.

**Критерий 3 (КАМЕРА) — выжил.** Прежней камеры сверху нет ни следа: все её
символы отсутствуют и в коде, и в `NetworkPlaySettings.asset`. Третье лицо
структурно не заперто; стоимость будущего переключения — правка одного
`LateUpdate` и одно поле настроек. Перенесено вперёд честно: `NetworkWalkerCamera.cs`
и `NetworkWalkerInput.cs` побайтово те же, что на `b7ae1fa4`, где он давал вердикт.
Найдено сверх заказа: комментарий `NetworkWalkerInput.cs:104` обосновывает предел
наклона тем, чего в этом коде нет (наклон на движение не влияет вообще — в RPC
нет параметра тангажа). Ограничение верное, обоснование ложное.

**Критерий 4 (ОДИН РАБОЧИЙ ПРОФИЛЬ) — выжил буквой, заголовок преувеличен.** Обе
машинно-проверяемые подклаузы верны буквально: `profileId` проверяется только на
непустоту, `LoadFromFile` принимает путь. Обе при этом полы́е: `ProfileId` не
читает никто, а единственный вызов жёстко зашит на `default.householder.json`
(`NetworkHouseholder.cs:126`). Заголовок «нынешнее поведение хозяина задаётся одним
текстовым профилем» преувеличен: JSON из 20 строк даёт ровно два числа (12.0 / 3.0)
и порядок двух способностей; маршрут и место прислушивания живут в
`Householder.prefab`, десять числовых настроек — в `HouseholderWalkSettings.asset`.
Критерий 6 той же карточки формулирует это правильно («порядок дел и их
длительности»). Само по себе расположение чисел в файле настроек — прямая политика
продуктового контракта v36, а не дефект; дефект — формулировка критерия 4.

**Критерий 5 (ПРОФИЛЬ — ТЕКСТ) — выжил.** Профиль — 20 строк JSON в
`Assets/StreamingAssets/`, описание формата — 61 строка, обе таблицы, точные типы,
полный список зарегистрированных имён, правило «No unlisted field is allowed» и
три разобранных примера ошибки. Одна неточность в описании: `profileId` объявлен
«used in diagnostics», а не читается нигде. Известный пробел (отказ на сломанном
файле) карточка сама вывела из ворот, и это подтверждено: тестов у загрузчика ноль,
и они СТРУКТУРНО невозможны — `core/TunnelCrew.Core.csproj` компилирует только
`Assets/TunnelCrew/Core/**`, а загрузчик лежит в `Profiles/`.

**Критерий 6 (ДОМ РАЗМЕЧЕН) — одна клауза из двенадцати опровергнута.** Три
комнаты, семь мест, два прохода подтверждены байтами заготовки; обстановки в
комнатах нет; путь идёт через названные проходы; телепорта после шума нет
(перемерено: снап `position = activeResponse.Target.Position` отсутствует),
прежнее намерение восстанавливается, путь строится свежий — всё это подпёрто
живыми тестами. **ОПРОВЕРГНУТА клауза «и новое событие может снова его
отвлечь»:** во время активной реакции второй шум ДРУГОГО рода отвергается
(`Householder.cs:1026-1044`), и это утверждает зелёный тест
`ASecondDifferentNoiseIsRefused_AndEveryoneElseStillGetsThatTick`. Хуже: тест,
названный этой клаузой — `ASecondNoiseDuringTheWayBackDistractsHimAgain` — свой
случай проверить НЕ МОЖЕТ, потому что его собственная охрана требует
`IsSafeToInterrupt`, а он `false` на каждом тике движения; путь назад это ровно
движение. Тест зелёный и доказывает правду, но не ту, что в его имени.
Мягче: «намерение разрешается в названное место дома» верно для распорядка и
неверно для реакции — после первого шума цель это сырая координата удара.
Мягче: «в стены не идёт» — свойство сданной топологии, а не охраняемый инвариант
(в `HouseWalk.cs` три задокументированных прямых аварийных выхода, сегодня
недостижимых; тестовый предикат покрывает только две внутренние перегородки).

**Критерий 7 (ШУМ РОЖДАЕТ МИР) — одна клауза сломана, одно утверждение
Направления опровергнуто.** Шов `CargoContactNoise` — единственная дорога из
контакта в факт; контакт с игроком удалён целиком и это утверждает тест
`ACargoDrivenIntoPeopleDoesNothingAtAll`; место шума липко подменяет точку
прислушивания, так что приход именно на место удара реален. **Сломано:** «хозяин
идёт проверять место шума» неверно для тихой полосы (60–150) — там он остаётся
стоять и слушать на месте. **Опровергнуто утверждение самого Направления**
(прежний `NOW`, «единственная дорога в факт»): вторая дорога существует —
`TakeHeardEvent`, редакторская кнопка, которая в билде отсутствует лишь потому,
что сборка вырезает мост инструментов, а не потому, что её удалили.

**Критерий 8 (ОДНА СЦЕНА, НО СО ШВАМИ) — опровергнут в двух местах.**
(1) `World/DeliveryPoint.cs:14` держит `static readonly List<DeliveryPoint>
ActivePoints`, заполняет его в `Awake`, и судья читает его в
`NetworkWalkerCourier.cs:48-49` — глобальный изменяемый реестр по порядку `Awake`,
невидимый в файле сцены и в инспекторе, подберёт любую точку из любой загруженной
сцены. Это ровно тот класс, который клауза называет по имени, и единственная
дорога, по которой судья узнаёт о цели вообще. (2) Полоса доставки вписала
продуктовую геометрию прямо в сцену — правка экземпляра восточной стены плюс
сценовый ребёнок `Outer East South`, — поэтому `ThreeRoomHouse.prefab` в одиночку
больше НЕ воспроизводит сданный дом. Третья клауза, «у сцены один владелец
изменений», не подпёрта ничем: в файл сцены коммитили шесть разных ног, гейта нет.
Отдельно: исполнитель этого критерия САМ отказался объявить его закрытым
(«их венью — Unity у владельца»), и это венью так и не запустили.

**Критерий 9 (СБОРКА) — выжил, включая самую опасную проверку.** Ожидалось, что
exe старше сданных байтов; вместо доверия времени файла прочитана таблица сцен
внутри плеера: `Lobby.unity` и `IntegratedHouse.unity`, ноль вхождений
`SampleScene`, ровно два level-файла. Reflog `WIN-U1` подтверждает: HEAD на момент
сборки `9493470b`, а от него до `352f96b0` изменён один файл — отчёт. Единственная
клауза, которая ФАКТИЧЕСКИ не держится, в тексте критерия не написана: exe
невоспроизводим никем, кроме владельца (сборки в репозитории нет и её однажды уже
отвергли).

**Критерий 10 (ЗАКРЫТИЕ) — сработал.** Он зашёл, поиграл и сказал, что дальше.
Сначала «не закрываем», затем сам пересмотрел на «закрыть и создать новый».

**Что мерилось про сами ворота, потому что на них ссылались все двенадцать
отчётов.** `tools/check.ps1` не содержит ни одного вызова `dotnet`; зелёное
«OK: all active gates green» совместимо с деревом, которое не собирается.
Headless-набор компилирует только `Assets/TunnelCrew/Core/**` — 28 файлов; вне
всякой автоматической компиляции остаются 33 файла (`Network/`, `World/`,
`Editor/`, `Profiles/`), примерно 52.5% нынешнего продуктового C#. Единственная
компиляция Unity-слоя за всю волну — ручная сборка плеера владельцем. Владелец
2026-08-07 вывел этот класс из счёта дефектов прямо: «это в принципе похер, будет
вызываться потом».

**Улики отчётов.** Двенадцать `docs/results/c-exec-g-1d84-*.md` существуют, все со
статусом `DELIVERED on dev`. Шесть из двенадцати несут тело, прямо противоречащее
собственной строке статуса (тело говорит «публикация не выполнялась» / «слот не
освобождён»), потому что статус переворачивает поздний коммит, а абзац о
публикации — нет. Один отчёт цитирует тест, которого на сданном коммите нет
(`HeStartsHisDayAtTheFirstPlaceOnHisRoute`, переименован соседней ногой);
`tools/result-check.ps1` смотрит только цитаты `docs/reviews/*` и `docs/adr/*`,
поэтому висячая цитата теста вне всякого гейта. Один отчёт не содержит ни одной
дословной цитаты владельца вовсе. Самый честный из двенадцати —
`world-noise-contact`: он дважды отказывается объявить половину собственного
`done_when` выполненной.

**Публикация раньше закрытия.** Буква `evaluator` («в `main` только после зелёного
прогона И отдельной свежей биндинговой проверки в другом физическом чате») выдержана
ОДИН раз из ДВЕНАДЦАТИ — `t-scene-1`. Проверено по каждой ноге отдельно
(`merge-base --is-ancestor` от её интеграционного слияния к SHA, который Направление
называет в своём закрытии). Сам реестр `i-published-to-main-...` недосчитал четыре
случая: `t-scene-3`, `t-house-6`, `t-host-7`, `t-scene-4`.

**Ревью-артефактов на волну ноль** (`docs/reviews/` не содержит ни одного файла
`g-1d84`). Под контрактом v36 ПРОБА это законно и владелец эту бюрократию резал
сознательно; следствие называется прямо: единственной перекрёстной проверкой
двенадцати отчётов был собственный перемер Направления.

## forecast_against_reality

Прогноз остаётся `no_basis`: эмпирического знаменателя нет, и волна его не создала.

СЮРПРИЗ ПРОТИВ СТАВКИ. Самая рискованная догадка волны — «переноска от первого
лица окажется вялой или мутной», поставленная второй задачей именно ради раннего
ответа, — НЕ сбылась: её закрыл его собственный прогон («Я проверил переноску
вдвоем от первого лица», «то, что мы планировали, вот этот минимум сделан»).
Укусило трижды, и каждый раз это был процесс, а не продукт:

1. текст выреза №1 был фактически неверен (утверждал, что груз и ходоки на второй
   копии едут тиками, тогда как они скользят через `DrawnPose`), из-за чего хозяина
   нарисовали без сглаживания и владелец увидел дрожь;
2. сцена дважды разошлась при слиянии полос, и опубликованные байты
   `IntegratedHouse.unity` не запускал никто;
3. передача от `map` к `shape` по демо-срезу сорвалась, и четыре мёртвые задачи
   сутки блокировали волну.

Довод против, который волна не сняла и который владелец сам сделал условием
следующих волн: игра остаётся коробкой из примитивов, показать её нельзя.

## lens_harvest

1. **Октябрьское демо и выпуск.** Впервые существует собранный exe нового концепта
   и рантаймовый путь, по которому вторая копия входит по адресу — предусловие
   уставного pre-mortem 5 (чистая установка, две машины). Даты не сдвинуты. Рубеж
   владельца по имени и AppID — 2026-08-15, он же уставный пересмотр октябрьского
   маршрута; до него восемь дней и ни одна строка Steam-работы не начата.
2. **Понятность и качество для игрока.** Появился первый читаемый игровой момент
   за всё направление: груз в точке → надпись → зелёное. Устав требует «хотя бы
   один хорошо читаемый игровой момент» — он есть. Никто, кроме владельца, его не
   видел, и по его же решению состав проверки в карточку не входил.
3. **Реально ли одному закончить.** Двенадцать задач за четверо суток при
   запланированных двадцати трёх и двух owner-approved срезах по ходу. Срок из
   этого не выводится: объём резался дважды, поэтому темп несопоставим с планом.
4. **Новый одобренный канон.** Неснимаемый минимум устава — три части: груз из
   дома / тоннель, который копают и прячут / хозяин, который замечает и отбирает.
   Части 1 и 3 теперь существуют в коде и наблюдались владельцем. Части 2 нет
   нигде: тоннель вышел из волны целиком его вердиктом. Это факт для следующей
   карты, а не дефект волны.
5. **Платный продукт и коммерческое знание.** Не прибавилось ничего, и линза так и
   говорила: `not_needed`. Внешней аудитории, страницы и выручки нет.
6. **Надёжность работы владельца.** В плюс: Play сразу поднимает игру, стартовые
   условия задаются одной редакторской панелью, exe собирается его рукой за минуты.
   В минус: ручная сборка каждый раз пачкает шесть отслеживаемых файлов (он вывел
   это из счёта своим решением по аналитике), слот на старте последней ноги стоял
   на восемь коммитов позади `main`, а `NOW.md` всё ещё выше потолка.
7. **Проще или сложнее.** ПРОЩЕ: 23 → 17 → 16 → 12 задач, три ревизии подряд
   только вычитали, ни один новый слой после перенарезки не добавлен. Оставшаяся
   сложность оплачена семью вырезами и прямыми исключениями подписанной карточки.
   Цена, которую ставка нигде не назвала: около 52.5% продуктового C# не
   компилируется ни одной проверкой, и «зелёные ворота» с «игра работает» —
   непересекающиеся утверждения.

## tree_diff

Одно одобренное изменение: `g-1d84.status` `active` → `done` плюс точный
`closed_when`, показанный владельцу целиком до правки и принятый словом
`принимаю`. Цель, все десять критериев, `why`, `risk`, `appetite`, `kill_by`,
`detail` и `children` карточки сохранены без изменений. `g-3f90` НЕ ТРОНУТ: его
судьба делегирована наряду `c-map-real-core-node-001` с записанной рекомендацией
сложить.

Побочный измеренный эффект: открытых детей у `g-0c26` было восемь при правиле
«не больше семи»; после закрытия `g-1d84` их семь. Вопрос ширины, отложенный
2026-08-06 дословно «до закрытия `g-1d84`», закрылся сам. Добавление узла-преемника
вернёт восьмёрку, если `g-3f90` не сложится в него — это и есть довод рекомендации.

## add_back

Ни один вырез не вернулся. Движение всей волны строго субтрактивное: вырезы 4, 5 и
6 только расширялись, и каждое расширение было его словом, а не решением сессии.

**Настоящие вырезы.** NavMesh — самый дорогой и оплаченный до конца: 460 строк
собственного счёта пути в engine-free `TunnelCrew.Core`, который держался всю волну.
Тоннель целиком — его «да A», самое большое сокращение волны, и единственное
решение, которое честно оплатило дыру, которую само открыло (выход из дома добавлен
полфразой в подписанный критерий 2 и построен руками за двадцать минут); лучше всего
задокументированное решение волны — три варианта показаны, два отклонённых названы и
процитированы. Второй профиль и пять реакций — его собственные слова «второй профиль
и пять различимых реакций точно снимаем», и это самый ценный вырез по смыслу: он убил
работу «доказать, что шов расширяется» в пользу «конкретное демо само скажет, что
добавлять».

**Трусливые вырезы.** Вырез 1 (сглаживание по сети) — его текст был фактически
неверен, ошибка стоила владельцу видимого дефекта, и вырез пришлось сузить,
разрешив то, что он никогда не запрещал; не храбрый вырез, а плохо
сформулированный. Вырез 4 в исходной редакции («не больше двух профилей и пяти
реакций») — самый ясный: его потолок пришёл из строки разбора, цитирующей документ
концепта (`work/converge-g-1d84.md:184`, «→I02 п.4 дословно»), а не из слов
владельца, и он РАЗРЕШАЛ ровно тот объём, который владелец сам убрал через три дня.
Вырез 3 («ровно три комнаты») — записи дважды сами отмечают его расхождение с
ожиданием владельца; он и есть названная причина, почему задачи «собрать дом
руками» в волне не было, и из этого расхождения вырос узел `g-3f90`. Вырезы 9 и 10
вырезами не были вовсе — бухгалтерия: одни уже были исключениями карточки, другие
принадлежат чужим узлам.

**Место, где запись не может ответить.** Из четырёх вещей, снятых демо-срезом
2026-08-06, его слова покрывают половину, ещё четверть — пересказ. Погоня, потеря
из виду и поиск не имеют ни одной его фразы: они уехали на одной букве `А`,
выданной срезу, который сессия сама сочинила и сама порекомендовала, и ни одного
отклонённого варианта в записях нет — в отличие от удаления тоннеля в тот же день,
где отклонённые Б и В названы. Решение почти наверняка верное (погоня не ядро), но
на вопрос «не срезали ли слишком легко» репозиторий здесь ответить не может.

**Пропущенный вырез.** Ни на одном экране ставки не было названо, что около
половины продуктового кода не покрыто ни одной автоматической проверкой и что
ворота не собирают игру. Под ПРОБОЙ это законно, гейт — глаз владельца; но это
цена, которую волна заплатила и ни разу не посчитала.

## knowledge

Три записи, каждая с реальным `read_by`:

- `knowledge/owner-word-boundary-is-asked-the-same-day.md`
- `knowledge/green-gates-do-not-compile-the-game.md`
- `knowledge/a-cut-whose-ceiling-came-from-a-document.md`

## state_changes

- **TREE.md**: `g-1d84.status` `active` → `done`; добавлен точный owner-approved
  `closed_when`. В `owner_approved` добавлена запись этой сессии с пятью его
  токенами, их точной силой и обоими его ходами по закрытию узла. `g-3f90` и все
  прочие узлы не тронуты.
- **NOW.md**: `updated` → эта сессия. `bet` → `null`; `tasks` → `[]`; удалены
  `track_wip_limit` и все пять полос (полосы распускает только `review`).
  `direction_forecast`: `status: no_basis` сохранён, `as_of` → 2026-08-07,
  `basis`/`drivers`/`update_when` переоснованы на факты этой волны.
  `open_calls` → единственный планировочный фронтир `c-map-real-core-node-001`.
  `recurring` и `decisions` сохранены без изменений.
- **NOW.md/issues, закрыты и удалены из горячего состояния (5):**
  `i-delivery-owner-run-predates-final-bytes-001` — закрыта его словом, что вынос
  груза работает на собранной игре (её собственный триггер);
  `i-manual-build-dirties-tree-and-turns-analytics-on-001` — закрыта его решением
  «пускай аналитика допустима» (её собственное условие закрытия);
  `i-host-walks-through-walls-001`, `i-unity-cloud-services-enabled-in-main-001`,
  `i-grid-v1-g01-dead-root-open-001` — были закрыты ранее и висели мёртвым грузом.
- **NOW.md/issues, добавлены (4):** `i-scene-seams-broken-at-two-sites-001`;
  `i-house-fixture-not-bound-to-the-prefab-001`;
  `i-noise-bands-invisible-and-force-mis-scaled-001`;
  `i-analytics-allowed-decide-before-public-build-001` (его «потом решим»,
  повешенное на триггер «перед первой раздачей сборки не ему самому»).
- **NOW.md/issues, обновлены (5):** `i-published-to-main-before-any-direction-close-001`
  (счёт 12 из 12, реестр недосчитал четыре, текст `evaluator` умер вместе со
  ставкой, новый триггер — `shape` следующей ставки);
  `i-house-walls-below-eye-level-001` (дом починки — узел настоящего ядра; числа
  перепроверены, девятая сценовая стена тоже 1.5);
  `i-householder-activities-are-intentions-not-instructions-001` (четвёртое
  свойство подкреплено измерением: политики прерывания нет, а зелёный тест её
  имитирует); `i-profile-cannot-name-a-house-place-001` (перепроверено на
  `352f96b0`; владелец вывел её из класса «код не вызывается»);
  `i-now-hot-file-over-ceiling-001` (417 → ~235 строк, рост держат только записи).
- **work/**: создан `c-map-real-core-node-001-call.md`.
- **knowledge/**: три новые записи (см. выше).
- **LOG.md**: одна строка сверху; этот полный RESULT — в `history/`.
- **CHARTER.md, продуктовый репозиторий и все прочие узлы TREE не менялись.**

## captures

- Контур демо владельца из параллельного чата — вход наряда
  `c-map-real-core-node-001`, не активная работа.
- Рекомендация сложить `g-3f90` в узел настоящего ядра, вместе с обязательным
  переносом долга по скриншотам для `g-2b7f` и оплаченного входа
  `probe/free-kits-2026-08-06`.
- Комментарий `NetworkWalkerInput.cs:104` обосновывает предел наклона тем, чего в
  этом коде нет; починка — одна строка документации, отдельной работы не стоит.
- Две устаревшие бумаги в продукте: `CLAUDE.md` обещает несуществующий
  `GasCoopGame.Core.slnx`; `tools/hygiene.ps1` описывает PlayMode-тесты в
  `Assets/Tests/**`, каковой папки нет, как и Unity-тестовых сборок вообще.
- Шесть отчётов из двенадцати противоречат своей строке статуса. История не
  правится; вперёд — одна строка в процедуре закрытия.

## decisions_needed

Нет. Все четыре вопроса закрыты его словами в этой сессии.

## play_check

- **1 verify by refutation** — done: десять критериев проверены на опровержение по
  байтам `352f96b0` десятью параллельными read-only проходами; четыре клаузы
  сломались, поэтому `met` недоступен; `killed` и `obsolete` исключены явно.
- **2 harvest per lens** — done: все семь линз устава получили явную диспозицию,
  включая «коммерческого знания не прибавилось».
- **3 tree diff (owner)** — done: одно изменение, точный текст показан целиком до
  правки и принят словом `принимаю`; судьба `g-3f90` не решена здесь и
  маршрутизирована в `map`.
- **4 add-back check** — done: ни один вырез не вернулся; настоящие и трусливые
  названы поимённо; названо и то место, где запись ответить не может (погоня).
- **5 knowledge** — done: три записи, каждая с реальным `read_by`.
- **6 forecast & next** — done: `no_basis` сохранён и переоснован; сюрприз назван;
  следующий узел не выбран за владельца, его карточку пишет `map`.
- **7 close** — done: ставка, задачи и все пять полос распущены; новых задач и
  полос не создано; выпущен ровно один законный планировочный CALL.
- **G9** — done: TREE изменён только после его слова на точный артефакт.

## log

g-1d84: вердикт `partial`, узел закрыт `done` его словом по критерию 10. Доказаны
одна запускаемая сетевая игра с домом, хозяином, переноской вдвоём от первого лица,
шумом и точкой доставки плюс собранный exe; не пережили опровержение две клаузы
критерия 8, одна критерия 6 и одна критерия 7. Ставка, задачи и пять полос
распущены, пять записей закрыты, четыре заведены, отдельной ноги на исправления
нет по его решению; выпущен `c-map-real-core-node-001` на карточку настоящего ядра.

## next

return-to-owner. Единственный фронтир — `c-map-real-core-node-001`, и он не
запускается без его контура демо. Новая ставка не открыта, задач и полос нет.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-review-g-1d84-integrated-house-partial-001.md
