# RESULT s-repair-g-37a1-character-replacement-release-001

call: HOME ESCALATE from c-control-g-37a1-body-first-person-replace-close-001
direction: indie-game-development · track: t-body · play: repair · node/task: g-37a1/t-2
date: 2026-07-30

## outcome

Character replacement-close reconstructed as a committed successful repository replacement followed by a failed
physical lease release. The old root is already `RELEASED / REPLACED`; its replacement receipt, stage proof, clean
basis, preserved ref and exact stale inventory remain authoritative and must not be recreated.

After the owner closed Unity, the one remaining blocker disappeared. The returned CALL is consumed and visibly
retired. One ready same-lineage successor,
`c-control-g-37a1-body-first-person-replace-release-002`, now owns only the selector release/readback. It neither
creates the new Character root nor runs any Character product stage. Task t-2 remains active; all unrelated state is
preserved.

## evidence

- Owner-forwarded terminal handback begins `HOME: ESCALATE` and states that the repository close succeeded at
  `slot/win-u2@c936211913907da8d45b4062ef81e000017d80e4`, while only Unity PID 31516 and its lock evidence blocked
  physical release.
- Fresh Git read: WIN-U2 is clean on `slot/win-u2`, HEAD
  `c936211913907da8d45b4062ef81e000017d80e4`.
- Committed `02-replaced.json` directly verifies `state: RELEASED`, terminal verdict `REPLACED`, replacement schema
  v34, `replaced_by: c-exec-g-37a1-body-first-person-minimal-002`, `resume_from: PLAN`, feature pin 31, replacement
  pin 34, basis/salvage `993cb3494e8dae01785f601da331a170fbdd4ab8`, rejection
  `98e44b1fa71c0b9c0ac3f5cd332b956bedd9c6e0`, preserved ref, empty carry and exact 17-path stale inventory.
- Owner then said exactly `закрыл что дальше по character?`. Fresh process read found PID 31516 absent; direct path
  checks found both `Temp/UnityLockfile` and `Library/EditorInstance.json` absent.
- Fresh selector still reports CLEAN, `CLAIMED`, exact lease
  `c-exec-g-37a1-body-first-person-001:PAIR-CANDIDATE`, and `availability: STOP`. This is the sole remaining live
  state transition.
- Product authority says a failed final release check does not repeat product, review or Deliver work; the exact
  selector release and immediate read-only `AVAILABLE` proof complete physical custody.

The Direction worktree contains unrelated untracked `.claude/settings.local.json`; it is preserved and excluded. No
product write, selector release, test, Unity/MCP action, branch/worktree mutation or remote update was performed by
this Direction leg.

## state_changes

1. `NOW.md.updated` — set to this repair session.
2. `NOW.md.open_calls` — consume returned
   `c-control-g-37a1-body-first-person-replace-close-001`; register exactly one ready t-body successor
   `c-control-g-37a1-body-first-person-replace-release-002` for t-2. Preserve every unrelated call, lane, task,
   issue, forecast and decision.
3. Add complete successor CALL
   `work/c-control-g-37a1-body-first-person-replace-release-002-call.md` limited to exact selector Release/readback.
4. Add a visible `RETURNED ESCALATE — DO NOT REDISPATCH` banner to the old CALL; preserve its full evidence.
5. Prepend the declared LOG line and save this full RESULT once in history.

No CHARTER, TREE, bet, task, track, issue or WIP-limit change.

## captures

None.

## decisions_needed

None. The owner already closed Unity and the release-only successor is dispatchable.

## play_check

1. **Name the contradiction** — done: repository receipt says the replacement is terminally `RELEASED / REPLACED`,
   while machine-local slot custody still says `CLAIMED`; the terminal HOME correctly escalated only that physical
   mismatch.
2. **Reconstruct** — done newest-first from the returned HOME, committed receipt, current Git, process/lock checks,
   selector output, NOW/CALL and product slot authority.
3. **Propose corrected state** — done: preserve completed replacement evidence, consume the returned CALL and issue
   one successor containing only exact release/readback.
4. **Confirm (owner)** — done: the owner forwarded the exact terminal HOME, then removed its only named blocker and
   confirmed it with `закрыл что дальше по character?`; no product or strategic verdict is inferred.
5. **Friction** — skipped: the selector failed closed on real Unity evidence and the HOME named the exact owner action;
   this is an external tool-state blocker, not an OS rule hole.

G1: t-body retains exactly one root. G3/G4 unchanged. G5: t-2 is not marked done. G9: CHARTER/TREE untouched. G10:
the returned HOME, completed repository replacement and remaining physical custody are recorded separately.

## log

Character replacement-close принят как частично успешный — committed receipt уже RELEASED / REPLACED на clean
WIN-U2@c9362119, но terminal HOME остался ESCALATE из-за живого Unity и неосвобождённой физической lease; после слов
владельца `закрыл что дальше по character?` подтверждены отсутствие PID 31516 и обоих Unity lock evidence, старый CALL
снят с dispatch, выпущен один ready successor только на selector Release/readback без повторения close, feature,
Unity, BUILD или Deliver.

## next

Dispatch the sole t-body root
`live/indie-game-development/work/c-control-g-37a1-body-first-person-replace-release-002-call.md` in the existing
`C:\projects\Unity\GasCoopGame_win-u2` checkout. After `HOME: REPLACED`, a separate Direction repair may atomically
issue the fresh contract-34 Character PLAN root from the committed replacement basis.

END_OF_FILE: live/indie-game-development/history/2026-07-30-s-repair-g-37a1-character-replacement-release-001.md
