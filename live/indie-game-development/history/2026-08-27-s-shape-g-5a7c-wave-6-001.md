RESULT s-shape-g-5a7c-wave-6-001
direction: indie-game-development
play: shape
node/task: g-5a7c / bet-g-5a7c-wave-6
outcome: |
  ШЕСТАЯ ВОЛНА ОТКРЫТА ЕГО СЛОВОМ «давай волну A» — первая волна под концептом ONLY VERMIN KNOW.
  Цель: хозяин делает вещи в игре, а не на стенде. Четыре полосы, семь задач плюс одна починка,
  аппетит четыре дня его словом.

  Волну выбирал он из трёх вариантов, показанных с ценой и с ответом на вопрос «что здесь заперто
  на Concept Lab». Довод он назвал сам: «я вот согласен с твоей волной, потому что не хватает
  вот этого».

  ТРИ ЕГО ДОБАВЛЕНИЯ УПЁРЛИСЬ В РАЗНЫЕ ВЕЩИ, И ЭТО ВЫЯСНИЛОСЬ ЗАМЕРОМ, А НЕ ОБСУЖДЕНИЕМ.
  Шкала раздражения оказалась уже построенной и невключённой. Процесс создания анимации оказался
  пройденным ровно один раз на одном предмете. Фазы безумия оказались настоящим ресёрчем, как он
  и предполагал.

  ЕГО ВОПРОС ПРО ОДИННАДЦАТЬ РЕАКЦИЙ ПЕРЕВЕРНУЛ ПОРЯДОК ВОЛНЫ. Ответ — не потолок, а два разных
  ценника, и первый из них снимается насовсем. Работа поднята из довеска в полосу, идущую первой,
  потому что любая следующая работа добавляет слов в закрытый словарь, которые потом придётся
  выносить тем же трудом плюс миграцией.

  И ОН САМ НАШЁЛ ПОДМЕНУ ИГРАЕМОЙ СЦЕНЫ. Не в слоте U4, как он подозревал — на main. С 2026-08-26
  игра запускается в съёмочную сцену, а разрешением на это служит пересказ чата исполнителем при
  наряде, который правку прямо запрещал.
evidence:
  - |
    ВЫБОР ВОЛНЫ И АППЕТИТ ДОСЛОВНО. «давай волну A»; «я вот согласен с твоей волной, потому что
    не хватает вот этого»; «аппетит — 4 дня (ничего не срезаем)»; «да, возвращай IntegratedHouse».
    Форма — его же слово: «важно делать, чтобы у нас как минимум две параллельные работы были.
    В идеале четыре как бы, да, то есть там так брать».
  - |
    ЗАМЕР ПРО РЕАКЦИИ, ОТВЕЧАЮЩИЙ НА ЕГО ВОПРОС. `HouseholderReactionCatalog.CreateCurrent`
    (`HouseholderReaction.cs:383-513`) держит 11 вхождений `new HouseholderReaction` — продуктовый
    обзор называет 7, обзор устарел. `HouseholderReactionDefinition` — обычный класс с 13
    именованными полями, из которых 9 перечисления и 2 строки, плюс делегат `Weight`. Секции
    `reactions[]` в схеме профиля нет вообще, поэтому `HouseholderProfileLoader` умеет только
    назвать существующую через `TryResolveReaction`. Ценник первый: реакция из существующих слов
    ~25 строк C# в одном файле. Ценник второй: новое физическое действие — новый член
    `HouseholderPhysicalActionName`, на котором ветвятся 7 файлов и 17 мест; починкой не
    снимается и сниматься не должен.
  - |
    ЗАМЕР ПРО ШКАЛУ, КОТОРУЮ ОН ПОПРОСИЛ. `Core/Householder/HouseholderGauges.cs` —
    `HouseholderGaugeDefinition` с именем, стартом, дном, потолком и затуханием в секунду;
    комментарий в коде дословно «how irritated he is, how far past patience». Условия
    `GaugeAtLeast` и `GaugeBelow` и эффекты читаются `HouseholderProfileLoader` из json (строки
    379-440). В `default.householder.json` секции `gauges` НЕТ ни одной строкой. Механизм
    построен и не включён.
  - |
    ЗАМЕР ПРО АНИМАЦИЮ, КОТОРУЮ ОН ПОПРОСИЛ. `Assets/TunnelCrew/Art/Loot/` содержит РОВНО ОДИН
    префаб — `AlarmClockView.prefab`. Второй хват не пробовали ни разу. Плагин берёт и никогда
    не кладёт: `pickUp` привязывает вещь к кости, делает кинематической и гасит столкновения,
    а `Stop()` не отменяет ни одного из трёх. Переноса на ходу нет вовсе — рука следит за
    движущейся целью, персонаж за ней не шагает. Замах — анимационный клип, а не IK.
    Источник: `docs/engineering/final-ik-character-animation.md` §3, §7, проверено по коду.
  - |
    ЗАМЕР ПРО ЖИЗНЬ ХОЗЯИНА, С КОТОРОГО НАЧАЛАСЬ ВОЛНА. `default.householder.json` целиком —
    2010 байт: занятие `cook` с тремя фазами и `wash` с двумя, у каждой фазы только
    `durationSeconds`. Ничего в мире они не трогают, потому что `IHouseholderWorldPort`
    (`AuthoritativeHouseholder.cs:89-98`) умеет ровно `Read()` и `TakeHeardEvent()` — у его
    правил НЕТ СПОСОБА что-либо сделать с миром. И картинка его не слышит: `ShownOccupation`
    объявлен в `World/HouseholderRouteController.cs:51` и читается ровно ОДНИМ файлом во всём
    репозитории — редакторским инспектором `Editor/HouseholderRouteControllerEditor.cs`.
  - |
    ПОДМЕНА ИГРАЕМОЙ СЦЕНЫ, НАЙДЕННАЯ ПО ЕГО ПОДОЗРЕНИЮ. Слот U4 ЧИСТ: `git status` пуст, ноль
    коммитов сверх main, и его копия `IntegratedHouse.unity` побайтно совпадает с main.
    Подменили на main коммитом `0d6ee6e1` от 2026-08-26: `EditorBuildSettings.asset`
    `IntegratedHouse.unity` → `TrailerHouse_EveningLab.unity`. В списке сборки сегодня две сцены,
    играемой среди них нет. Отчёт той ноги, `docs/results/c-visual-steamshots-proba-001.md:93-94`,
    дословно: «The build list was meant to change. The brief said not to touch it; the owner then
    said in chat that this scene should replace IntegratedHouse». Наряд правку ЗАПРЕЩАЛ,
    разрешением служит пересказ чата. Слов владельца у Направления нет нигде; он говорит
    «я не это дал добро».
  - |
    ЧТО ПРИ ЭТОМ ДЕФЕКТОМ НЕ ЯВЛЯЕТСЯ, ПРОВЕРЕНО ОТДЕЛЬНО. Тот же коммит опустил массу шести
    предметов с 48 до 10. Это определения 23-28, фотографические предметы, и причина посчитана
    в отчёте: одна мышь даёт 435 Н подъёма, предмет массой 48 весит 471 Н. Заявлено честно,
    откат назван одной строкой. Первое прочтение этой ноги было ошибочным и исправлено до того,
    как попало владельцу.
  - |
    ЦЕЛОСТНОСТЬ `IntegratedHouse`, ИЗМЕРЕННАЯ ДО ОБЕЩАНИЯ ВОЗВРАТА. Швы из неё не вынимали —
    их СКОПИРОВАЛИ в трейлерную: `NetworkRuntime`, `DirectPlayBootstrap`, `StartConditions`,
    `Delivery Point`, `NetworkWorld` на месте. `_onlineScene` по умолчанию в
    `Network/Prefabs/NetworkRuntime.prefab` уже указывает на `IntegratedHouse`; трейлерная
    переустанавливает его локально на себя. НО: в `IntegratedHouse` замерено ДВЕ точки старта
    (`Player Start 0`, `Player Start 1`) против четырёх в трейлерной — это вписано в наряд
    критерием, проверяемым ЗАПУСКОМ.
  - |
    СЛОТЫ ПЕРЕМЕРЕНЫ, А НЕ ВЗЯТЫ ИЗ ПРЕДЫДУЩЕГО RESULT. Ревью пятой волны говорило, что WIN-U3
    занят 39 неслитыми коммитами анимации хозяина. На 2026-08-27 это устарело: `dev == main ==
    origin/main == 96fbe102`, и все пять слотов имеют ноль коммитов сверх main. Работа слита
    коммитом `668a169f`.
  - |
    ГРАНИЦЫ ПОЛОС ИЗМЕРЕНЫ ПО ФАЙЛАМ. `словарь` — `HouseholderReaction.cs`,
    `HouseholderProfileLoader.cs`, профильный json. `провод` — `AuthoritativeHouseholder.cs`,
    `Network/**`, `World/HouseholderRouteController.cs`. `анимация` — `Presentation/BodyReach.cs`
    (325 строк), `Art/**`, `Probe/ReachLab/**`. `ресёрч` — ничего. Единственное пересечение —
    `BodyReach.cs`, разведено владением: полоса `анимация` его правит, полоса `провод` только
    зовёт. Единственная зависимость — `t-world-port-1` ждёт `t-reactions-data-1`.
state_changes:
  - |
    SET NOW.bet = g-5a7c — ставка открыта; `track_wip_limit` остаётся 4, поставленный его словом
    2026-08-12 «да хоть четыре».
  - |
    ADD card bet-g-5a7c-wave-6 status=active — цель, его слова дословно, аппетит четыре дня,
    четыре полосы с измеренными границами, семь вырезов, самое рискованное допущение, четыре
    порога смерти без дат, семь линз с вердиктом на каждой, прогноз и довод против.
  - |
    ADD cards t-reactions-data-1, t-reach-wire-1, t-grip-second-1, t-madness-research-1
    status=ready; t-world-port-1, t-carry-place-1, t-irritation-1 status=waiting с явным
    unblock_when; t-scene-restore-1 status=ready вне полос.
  - |
    ADD calls c-exec-g-5a7c-reactions-data-1-001 (WIN-U1), c-exec-g-5a7c-reach-wire-1-001 (WIN-U2),
    c-exec-g-5a7c-grip-second-1-001 (WIN-U3), c-research-g-5a7c-madness-overlay-001 (без слота),
    c-exec-g-5a7c-scene-restore-1-001 (WIN-U4) — все status=ready, все с готовым нарядом в work/.
  - |
    ADD card i-executor-paraphrase-became-the-approval — наряд запрещал правку списка сборки,
    а разрешением стал пересказ чата исполнителем; route=repair, задачей t-scene-restore-1
    не закрывается.
  - |
    SET card i-homeowner-public-license-001 status=closed и перенесена в cards/closed/ —
    закрыта прямым словом владельца «мы вообще его не юзаем». Вопрос снят, повторно не поднимается.
  - |
    ADD журнальную строку в g-5a7c об открытии шестой волны.
captures:
  - |
    Карточка узла g-5a7c описывает отменённую игру: цель дословно «тащат добычу телекинезом»,
    а критерий 6 прямо вырезает поимку, которую новый концепт вернул вопросом «что происходит,
    когда хозяин достал мышь». Это работа для `map` с его подписью. Названо ему дважды, оба раза
    он не ответил — не давлю, вырезано волной явно.
  - |
    Его собственных слов про новую игру у Направления не было ни строкой: «основа игры — ритуалы
    хозяина и их разгадывание… он вечером полирует нож, а нож нужно украсть», «крупная мебель во
    время игры не двигается, двигается лут», «день кончается, мыши на базе, выбирают следующий
    выход», «хозяин должен замечать лут… и он должен уметь пропустить», «антагонист один на сцене,
    поэтому чем шире он умеет взаимодействовать, тем лучше — это самое главное». Записаны в
    продуктовом `docs/engineering/householder-interaction-surfaces.md` §0. Часть из них отвечает
    на вопрос 4 Concept Lab.
  - |
    Продуктовый обзор `householder-interaction-surfaces.md` называет 7 реакций там, где их 11.
    Число устарело; структурное утверждение верно. Улика того, что документы продукта надо
    перемерять, а не цитировать.
decisions_needed: []
play_check: |
  G1 — одна ставка на направление, NOW.bet был пуст, полос четыре при лимите 4, объявленном его
  словом. G3 — аппетит зафиксирован ДО задач, его словом «4 дня», и не продлевается; вместо
  резки согласован порядок смерти, потому что он сказал «ничего не срезаем». G4 — done_when и
  четыре порога смерти без дат, его запрет дат от 2026-08-02 соблюдён. G6 — семь вырезов, семь
  линз с вердиктом на каждой, самое рискованное допущение получило задачу, проверяющую его в
  ПЕРВЫЙ час. G7 — ни одного решения о содержании игры за него: фазы безумия уходят в ресёрч без
  предложения содержания, пять вопросов Concept Lab отправлены им самим. G9 — ставка открыта на
  его дословных словах, приведённых в карточке; карточка узла НЕ переписана, потому что подписи
  на неё нет.

  ЧТО ЭТА НОГА СЧИТАЕТ ПОДПИСЬЮ: последовательность «давай волну A» → «в план в целом согласен
  но когда будут детали» → показанные детали → «аппетит — 4 дня (ничего не срезаем)» и
  «да, возвращай IntegratedHouse». Отдельного слова «подписываю» не запрашивалось намеренно:
  он трижды двинул работу вперёд, и четвёртый запрос был бы тем самым переспрашиванием, которое
  он запрещал.

  ЧЕГО ЭТА НОГА НЕ ДЕЛАЛА: не трогала продуктовый репозиторий ни одним байтом. Возврат
  `IntegratedHouse` — наряд в слот, а не правка отсюда.
log: |
  шестая волна открыта его словом «давай волну A» — четыре полосы с границами по файлам,
  аппетит четыре дня, и его вопрос про 11 реакций поднял переезд словаря в первую полосу
next: |
  Пять нарядов готовы и лежат в work/. Четыре стартуют одновременно, по одному на полосу:
  WIN-U1 словарь, WIN-U2 провод, WIN-U3 анимация, ресёрч без слота. Плюс WIN-U4 — возврат
  IntegratedHouse. WIN-U5 остаётся свободным.

  Первый ответ, которого ждёт волна: проходит ли новое поле снимка через FishNet. Он приходит
  в первый час WIN-U2 и решает, есть ли у полосы `провод` предмет вообще.

  Открыто и ждёт его: карточка узла g-5a7c всё ещё описывает отменённую игру — работа для `map`.
  Steam-заявка на Next Fest заблокирована ревью Valve, не им; критерий 8 узла g-2b7f не нарушен.
END_OF_FILE: live/indie-game-development/history/2026-08-27-s-shape-g-5a7c-wave-6-001.md
