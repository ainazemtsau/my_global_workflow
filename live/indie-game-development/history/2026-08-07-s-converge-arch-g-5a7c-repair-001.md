# RESULT — s-converge-arch-g-5a7c-repair-001

direction: indie-game-development
play: converge-arch
node: g-5a7c
call: work/c-converge-arch-g-5a7c-repair-001-call.md
date: 2026-08-07
session: отдельная физическая сессия; ремонт по шести находкам первой проверки узла

**База продуктовых фактов перепроверена ПЕРВОЙ РУКОЙ ДО любого `факт кода`,** как требовал наряд:
`C:/projects/Unity/GasCoopGame`, `git rev-parse HEAD` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`,
ветка `main`, `git status --short` пусто. Репозиторий этой ногой НЕ изменялся; всё чтение.

**Веер.** Опровержение сделано одним контуром: в этом харнессе действует прямая постоянная инструкция
владельца не разворачивать субагентов и workflow, и по авторитету `os/KERNEL.md` живая инструкция
владельца выше плея. Это та же граница, которую назвали против себя предыдущая арх-нога и первая
проверка; названа здесь, чтобы никто не прочёл ремонт как веерный. Биндинговое опровержение —
свежая `converge-verify`, и она будет ВТОРОЙ.

---

## outcome

**Все семь `done_when` наряда закрыты, и лечение везде одно и то же — ПОНИЖЕНИЕ, а не свежий ответ.**
Ни одна возвращённая дыра не заткнута ответом. Счёт открытых мест вырос, а не упал, и это предписанный
исход: `arch_lines_owed_to_owner` 0 → 3, `contracts_open` 1 → 2, `arch_open` 8 → 9, `arch_rows` 9 → 10.

Владельцу НЕ вынесено ни одного нового решения: по каждому понижению назван ОТВЕЧАЮЩИЙ и НЕ
сформулирован вопрос. `decisions` в `NOW.md` не тронуты.

Архитектурный разбор не переоткрыт. Шесть выводов, подтверждённых первой проверкой (`A1`, `A2`, `A3`,
`A5`, `A6`, `A7`), по существу не тронуты; в `A2` и `A5` добавлено по одному абзацу-указателю на новую
строку `A10`, и это названо прямо, чтобы вторая проверка не искала.

---

## evidence

### `done_when` 1 — `C26` перестаёт объявлять куплённым то, чего карточка не покупает (`F1`)

Прежний текст `C26`: «состояние двери „открыта / закрыта" принадлежит ОПИСАНИЮ ДОМА… и **куплено оно
подписанным критерием 6**». Утверждение о покупке СНЯТО, а не подправлено.

Опровержение лежит внутри самого набора и взято дословно: `W63` пишет — «если двери не открываются,
ослабление становится **статическим свойством стены**». Дверь, закрытая ВСЕГДА, исполняет букву
критерия 6 целиком и переменной не требует: требуется свойство проёма. Критерий 6 покупает ОСЛАБЛЕНИЕ у
двери; открывается ли она вообще — это ровно `W63`, и это его ВХОД с двойной оговоркой («должны как-то
же закрываться. **Возможно, и не будут**»), а не его ответ.

**Что осталось верным и не тронуто** (перемерено ремонтом первой рукой на `352f96b0`):

- дверей как вещей сегодня нет вовсе: `Doorway` — ярлык НАЗНАЧЕНИЯ места
  (`Core/House/HousePlace.cs:13`), проход — ребро графа с шириной; поиск по всему `Assets/TunnelCrew` на
  `isOpen|isClosed|passable|blocked` даёт **ровно одно попадание, и то внутри комментария**
  (`Core/House/HousePassage.cs:36`), то есть НОЛЬ состояний;
- цена физической двери: одно число (угол) в снимке против шести степеней свободы у падающего предмета;
- условная часть вывода: ЕСЛИ состояние двери понадобится, оно принадлежит описанию дома, а не физике —
  она держится сама, потому что `Core/TunnelCrew.Core.asmdef` объявляет `noEngineReferences: true`.

Понижено в **`open` → OWNER**, отвечающий — владелец внутри той задачи, которая дойдёт до `W63`. Вопрос
не сформулирован. §WHAT не тронут: `W63` остаётся `→ PLAN` (она спрашивает КТО закрывает двери), а
вопрос, который отдаёт `C26`, стоит ПЕРЕД ней — существуют ли открывающиеся двери вообще. Исправлено в
двух местах: подраздел «ЕГО УТОЧНЕНИЕ ТОГО ЖЕ ДНЯ» в `A9` и строка `C26` §CONTRACTS.

### `done_when` 2 — `A4` теряет утверждение, что критерий 3 куплен даром (`F2`)

УСТРОЙСТВО не оспорено и оставлено как было: подъём есть следствие уже написанной формулы — сумма
вертикальных составляющих, каждая под потолком НА РУКУ, демпфирование один раз к сумме
(`CargoHaul.cs:109-112` и `:51-55`, перемерено).

ПОРОГ формулой не куплен. Арифметика на числах самого набора: `W07b` предлагает `_cargoMass` 50, вес
50 × 9.81 = **490.5 Н**; потолок ставится НА РУКУ и лишь потом суммируется — перемерено первой рукой,
что `HandForce` кончается `Cap(ref handX, ref handZ, rules.MaxHandForce)`, а сложение по рукам идёт в
`Reduce:36-49`; `_cargoMaxHandForce: 200`. Одна рука 200 Н и две 400 Н вес не перебивают; три (600 Н) и
четыре (800 Н) перебивают. Ступеней две вместо четырёх, и его `I09` — «два уже могут выше» —
невыполнимо.

**Ни одного числа не предложено.** Все три хода (другие числа, другой потолок, отказ от потолка на
руку) остаются открытыми и ни один не предпочтён. Понижено в **`open → PLAN` (=`W114`)** с возвратом к
`OWNER`, если чисел под его текстуру не найдётся; отвечающий — задача, ставящая вертикаль у груза.

Записаны две вещи, делающие вопрос точнее и НЕ отвечающие на него: 200 Н — верхняя граница, а не
рабочее значение (`Cap` зажимает вектор целиком, значит рука, тянущая и вбок и вверх, отдаёт на подъём
меньше); и `W07b` уже несёт третье опровержение того же корня — чтобы упереться в потолок, нужно
растяжение `0.2 + 200/400 = 0.7 м`, то есть 4.4 роста налётчика.

Выбор варианта (а) в `A4` проверкой не оспорен и ремонтом не тронут.

### `done_when` 3 — пол получает своё место в разборе (`F4`)

Заведена новая строка **`A10`** с двумя опровергнутыми таблицами. Всё измерено первой рукой на
`352f96b0`.

**Измерения.**

1. В `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` два пола, и у обоих перечислены ВСЕ компоненты:
   `Floor` — `Transform`, `MeshFilter`, `MeshCollider`, `MeshRenderer`, позиция `(0, 0, 0)`, масштаб
   `1.6`; `Delivery Yard Floor` — `Transform`, `MeshFilter`, `BoxCollider`, `MeshRenderer`, позиция
   `(10, −0.05, 0)`, масштаб `(5, 0.1, 6)`. **Ни на одном нет ни одного `MonoBehaviour`**, значит нет и
   `HouseWall`, значит пола нет в описании дома вовсе (`CarryStand.ReadAuthoredWalls:66-78` берёт ровно
   `GetComponentsInChildren<HouseWall>()`).
2. `CargoBodyContact.Record` зовётся из `OnCollisionEnter` И из `OnCollisionStay` (`:42-50`);
   `AuthoritativeCargoRoster:125` зовёт `NoiseOf` для каждого груза КАЖДЫЙ тик без проверки «держит ли
   кто-нибудь». `CargoBodyPort.cs:51` объявляет «The house is the only physical thing in this world the
   cargo can meet» — и объявление живёт в `Core`.
3. `StandWall` (`CarryStand.cs:9-26`) — прямоугольник X/Z **без высоты вовсе**;
   `WalkSpaceProbe.PushOut:47-70` выталкивает круг ходока из каждого такого прямоугольника.
4. Геометрия двора: `Delivery Yard Floor` занимает `x ∈ [7.5, 12.5]`; самая восточная комната
   (`East Room`, центр `x = 4.875`, `_size.x = 4.25`) кончается на `x = 7.0`; наружная стена
   `Outer East South` стоит в `x = 7.25`; `Delivery Point` — в `x = 10`.

**Арифметика, которой в наборе не было.** Покоящееся тело давит на опору с силой, равной весу: движок
гасит накопленную гравитацией скорость импульсом `m·g·Δt` каждый шаг, а `Record` делит импульс на
`Time.fixedDeltaTime`. При `_cargoMass` 50 это 490.5 Н; с учётом уже измеренного расхождения часов (шаг
судьи ≈0.0167 против `Time.fixedDeltaTime` 0.02, `A1`) отчёт даёт **≈409 Н** при верхнем пороге
`_cargoHeavyNoiseForce: 400`. **Груз, который просто лежит, каждый тик сообщал бы самую громкую
полосу.** При сегодняшней массе 4 это ≈32.7 Н — ниже нижнего порога 60, поэтому сегодня не видно.
**Записано честно против себя:** это расчёт по объявленному поведению движка, а не наблюдение в
запущенной игре; продукт не запускался и запускать его нога права не имела. Направление вывода при этом
не зависит от точности числа — 490.5 Н превышает 400 с запасом, а не на процент.

**`A10.1` — входит ли пол в описание дома (по `A2`).** Отклонены: (а) «остаётся вне описания» —
объявление `Core` становится ложным, и отличить удар о стену от лежания на полу нечем; (б) «пол — ещё
одна `HouseWall`» — вытолкнет ходока из всего дома; (г) «пол — свойство комнаты» — двор доставки лежит
вне всех комнат. **Выжил (в):** пол — ТРЕТИЙ вид записи описания дома («опора»), производитель тот же
авторский коллайдер, ходьба его не читает, читают слой шума и будущий «путь снизу вверх».

**`A10.2` — становится ли контакт с полом источником общего слоя (по `A5`).** Отклонены: (а) «тот же
источник» — арифметикой выше; (в) «фильтровать пол» — подписанной карточкой (кр. 3 «волочится по полу»,
кр. 4 «волочение… громче»); (г) «поднять пороги выше веса покоя» — маскировка, привязанная к открытому
числу массы, плюс делает неслышимым настоящий удар той же силы. **Выжил (б):** опора — ОТДЕЛЬНЫЙ
источник слоя со своей кривой, ровно как предписывает `A5`. Сама кривая — `→ PLAN`, ни одного числа не
предложено.

**Прямой ответ на вопрос наряда: НЕТ, `A4` не покупает половину критерия 4 даром.** Направление
механизма верное (одна рука не поднимает → контакты идут; четверо поднимают → кончаются), но даром не
выходит ни при одном устройстве: при одном источнике сломана базовая линия, при разведённых громкость
волочения задаётся кривой опоры, которой ещё нет. **`W115` остаётся открытой** в прежнем виде;
добавилось только то, что ОДИН из двух способов ответить «даром» опровергнут. **`W116` остаётся
`open → PLAN`** (ближняя половина отвечена здесь, дальняя за задачей), и `open_rows: 10` не меняется.

Два внутренних шва заведены в §CONTRACTS: `C27` (описание дома ← опора) и `C28` (слой шума ← контакт с
опорой). В `A2` и `A5` добавлено по одному абзацу-указателю; выводы обеих строк не тронуты.

### `done_when` 4 — прочтение «несущие задевают» понижено в `open → OWNER` (`F5`)

Перемерено первой рукой: у ходоков коллайдеров нет нигде
(`NetworkWalkerPresentation.cs:203-205` — `part.GetComponent<Collider>().enabled = false`), а
`CargoNoise.cs:57-64` объявляет прямо: «a walker is not a physical body in this game and the engine can
never report a collision with one». Читок две: задевают несущие ВМЕСТЕ С ГРУЗОМ (не стоит ничего) либо
задевает ТЕЛО человека (отдельное правило в слое правил). Выбор между ними и есть решение, есть ли эта
работа.

`CHARTER.md:92` сверен по байтам: «Дом при этом не декорация: несущие задевают обстановку и попадают в
то, что хозяин расставил, — и именно по этому он их находит». Сужение стоит против уставной строки, а
не только против критерия 5. По G7 это его вопрос.

Понижено в **`open → OWNER`** в обоих местах `A9` (абзац до его ответа и «одна оговорка» после) и
названо в `C25`, который прежде не упоминал его вовсе. Отвечающий — владелец, внутри той ноги, которая
первой доведёт обстановку до задачи, и она обязана принести ему ОБЕ читки с ценой. Вопрос здесь не
сформулирован. Вердикт «А, обстановка стоит» этим не ослаблен: он про то, ДВИЖЕТСЯ ли обстановка, и
остаётся в силе.

Записан относящийся сюда измеренный прецедент: правило «груз встретил человека» один раз уже было
написано и работало, и владелец 2026-08-06 велел убрать его целиком, а не приглушить
(`CargoNoise.cs:57-64` хранит эту историю дословно).

### `done_when` 5 — `A9` перестаёт противоречить сам себе про силу его уточнения (`F7`)

Слово «сильнее» снято одной правкой: строка 1 таблицы классов больше не говорит «делает его СИЛЬНЕЕ
прежнего вердикта», а говорит «называет предметы поимённо, но сильнее вердикта не делает» — потому что
высказывание захеджировано с обеих сторон («я сейчас не могу точно сказать» … «я думаю, что нет»), и
преамбула подраздела шестью строками выше говорит ровно это.

`LOG` за `s-converge-arch-g-5a7c-003` несёт ту же формулировку и **намеренно НЕ правлен** — задним
числом лог не переписывается; это сказано прямо и в самой правленой клетке, и здесь.

### `done_when` 6 — `A8` теряет решённым вопрос управления (`F9`)

«ГДЕ берётся хват (прицел вместо близости)» вынуто из выжившего варианта (б): оно ни с чем не
сравнивалось и появлялось только внутри текста самого варианта. Карточка этого не говорит — кр. 3
говорит «берут и несут на расстоянии, а не хватом за корпус», то есть про дальность НЕСЕНИЯ. Перемерено
первой рукой: `_cargoPickupRange: 1.3` при росте 0.16 м — восемь ростов, то есть близость на расстоянии
уже стоит и работает.

Понижено в **`open → OWNER`**; отвечающий — его глаз внутри первой задачи телекинеза, ровно как уже
сказано в `M7`. Вопрос не сформулирован. Две другие трети не тронуты: они держатся на перемеренных швах
`CargoHaul.AtPoint` (`:115-156`), `CargoHold` в системе координат груза и `Reduce` на список любой длины
(`:36-49`).

### `done_when` 7 — счета сведены и говорят то же, что §WHAT (`F6`)

- `reads:` в шапке арх-файла: `open_rows: 1` → прямо помечено устаревшим и перечитано: `open_rows: 10`,
  со ссылкой на §COUNT и с перечислением, откуда взялись десять.
- В §SIGNOFF арх-файла заведена таблица трёх счётов (§WHAT / §CONTRACTS / арх-файл) с явным правилом:
  они меряют РАЗНОЕ, совпадать обязаны утверждения, а не числа. Названы четыре точки смыкания:
  `W103`↔`C07`, `W114`↔`A4`, `W116`/`W115`↔`A10`, `W63`↔`C26` — и по каждой сказано, почему счёт §WHAT
  от этого не двигается.
- Из `C07` убрана устаревшая фраза «`W103`, единственную открытую строку §WHAT»: после ремонта §WHAT
  открытых строк десять, а `W103` из открытых ушла.
- Заведён **§REPAIR-LEDGER** в конце арх-файла — таблица «какой `done_when`, какая находка, где
  тронуто, что сделано, чего НЕ сделано» плюс список нетронутого. Он писан ради того, чтобы вторая
  проверка сверяла его КОМАНДОЙ, а не чтением.

### Что ремонт нашёл против самого набора и записал, не чиня

- §MINER получил замечание ПРОТИВ себя: заход «какой вопрос мы не задали» не поймал пол — вопрос,
  стоящий прямо под двумя подписанными строками карточки. Нашла его первая проверка, а не заход.
- Комментарий `CargoBodyPort` («дом — единственное физическое, что груз может встретить») становится
  ложным в момент включения гравитации. Это уже стоит CAPTURE-ом первой проверки; ремонт подтвердил
  первой рукой и не чинил.

---

## state_changes

1. `live/indie-game-development/work/converge-g-5a7c-arch.md` — шапка (`repair:`, `call:`, `reads:`);
   `A2` и `A5` — по одному абзацу-указателю; `A4` — снято «даром», понижён порог, добавлен указатель на
   `A10`; `A8` — понижена треть варианта (б); `A9` — понижено прочтение (два места), снято «сильнее»,
   снято «куплено критерием 6», обновлён абзац «что не изменилось»; **новая секция `A10`**; §MINER —
   замечание против себя; §NOT-DECIDED — пять новых строк; §SIGNOFF — новый абзац ремонта, новый счёт,
   таблица трёх счётов; §REPAIR-LEDGER — новая секция; канон дополнен двумя предложениями.
2. `live/indie-game-development/work/converge-g-5a7c.md` — **только §CONTRACTS**: вводный абзац
   (список тронутого), «Правила чтения» (ждущих строк две), `C07` (снята устаревшая фраза), `C25`
   (названо понижённое прочтение), `C26` (понижен), **новые `C27` и `C28`**, «Счёт этой ноги» и
   фенсированный блок. §DRIFT, §IMPORT, §GLOSSARY, §WHAT, §COUNT и оба §SIGNOFF — ни знака.
3. `live/indie-game-development/NOW.md` — `open_calls`: снять `c-converge-arch-g-5a7c-repair-001`
   (исполнен), завести `c-converge-verify-g-5a7c-002` со статусом `ready`; добавить строку-комментарий
   о ремонте. Ставка не открывается, задачи и полосы не создаются, `issues` и `decisions` не тронуты.
4. `live/indie-game-development/LOG.md` — одна строка вверх.
5. `live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-repair-001.md` — этот RESULT
   целиком.
6. `live/indie-game-development/work/c-converge-verify-g-5a7c-002-call.md` — новый наряд ВТОРОЙ
   проверки.

**Не трогается:** `TREE.md`, `CHARTER.md`, `knowledge/**`, §WHAT и §COUNT файла
`work/converge-g-5a7c.md`, строки `W15` и `W16`, продуктовый репозиторий
`C:/projects/Unity/GasCoopGame`.

---

## captures

- **CAPTURE (не действовать в этой ноге).** `NoiseOf` вызывается для каждого груза каждый тик БЕЗ
  проверки, держит ли его кто-нибудь (`AuthoritativeCargoRoster:125`). Сегодня это безвредно, потому
  что гравитация выключена и груз висит на высоте покоя. Место для правки — та же задача, что включает
  гравитацию; читать вместе с `A10` и `i-noise-bands-invisible-and-force-mis-scaled-001`.
- **CAPTURE.** `Floor` покрывает 16×16 при доме шириной 14.5, то есть пол выходит за наружные стены со
  всех сторон; `Delivery Yard Floor` — отдельный объект, к дому не примыкающий вплотную (`7.25` против
  `7.5`). Когда пол станет записью описания дома (`A10.1`-в), эта щель и это перекрытие станут
  авторской геометрией, которую кто-то должен нарисовать нарочно.

---

## decisions_needed

**Ни одного.** Наряд прямо запретил выносить владельцу новое решение, и запрет исполнен. Четыре
понижения называют отвечающего и НЕ формулируют вопрос: `C26` (владелец внутри задачи, дошедшей до
`W63`), `A9`/оговорка (владелец), `A8`/треть (его глаз в первой задаче телекинеза), `A4`/порог (задача
с возвратом к владельцу). `d-furniture-moves-or-stands-001` снят как отвеченный и не воскрешается.

---

## play_check

```
contract_coverage: PASS — каждый узел TREE.md имеет строку (пятнадцать плюс корень, сам g-5a7c не
      считается); полнота по дереву ремонтом НЕ менялась: A10 не заводит ни одного нового соседа,
      оба новых шва (C27, C28) внутренние
contract_rows: 12 краёв + 16 внутренних швов (было 12 + 14)
contracts_open: 1 → 2 — C05 (→ map, =W109) и C26 (→ OWNER, понижён этим ремонтом по F1)
arch_rows: 9 → 10 — добавлена A10 (пол), по F4
arch_open: 8 → 9 — A1–A8 плюс A10; A9 по-прежнему закрыт вердиктом владельца по варианту
arch_lines_owed_to_owner: 0 → 3 — A9/оговорка (F5), A8/треть (F9), C26 (F1). Четвёртое понижение,
      A4/порог (F2), стоит open → PLAN (=W114) и сюда не считается
arch_in_context_only: PASS — ни одна строка арх-файла, включая всю новую A10, не является
      требованием узла и не едет в done_when
owner_decisions_raised: 0 — по каждому понижению назван отвечающий, вопрос не сформулирован
owner_tokens: вердикт владельца в наборе по-прежнему ОДИН («А, обстановка стоит»), подписей под
      текстом карточки ноль. Единственное место, где сила была выше настоящей, исправлено (F7);
      LOG за s-converge-arch-g-5a7c-003 намеренно не правлен задним числом
confirmed_conclusions_untouched: PASS — A1, A2, A3, A5, A6, A7 по существу не тронуты; в A2 и A5
      добавлено по одному абзацу-указателю на A10, и это названо прямо
what_untouched_by_this_leg: PASS, ПРОВЕРЕНО ПОБАЙТОВО САМОЙ НОГОЙ, а не заявлено: строки 1–427
      файла work/converge-g-5a7c.md (всё до §CONTRACTS — §DRIFT, §IMPORT, §GLOSSARY, §WHAT и оба
      §SIGNOFF) и весь §COUNT сверены `diff` против `HEAD:` — различий НОЛЬ в обоих случаях.
      open_rows остаётся 10, deferred остаётся 2
W15_W16_untouched: PASS
card_untouched: PASS — TREE.md не открывался на запись
two_ledgers: PASS — три счёта разведены таблицей в §SIGNOFF арх-файла с четырьмя точками смыкания;
      устаревшая фраза в C07 снята; open_rows в шапке арх-файла перечитан 1 → 10
base_reverified_first: PASS — 352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa, дерево чисто, только чтение,
      перепроверено ДО первого `факт кода`
fan_out: одиночный контур по прямой постоянной инструкции владельца; названо прямо в файле и здесь
next: converge-verify — ВТОРАЯ проверка узла, свежая физическая сессия
```

---

## log

2026-08-07 | s-converge-arch-g-5a7c-repair-001 | converge-arch | direction | Ремонт арх-файла и
§CONTRACTS по шести находкам первой проверки: все семь `done_when` закрыты понижением, ни одна дыра не
заткнута ответом; заведена `A10` (пол), `arch_lines_owed_to_owner` 0 → 3.

---

## next

`c-converge-verify-g-5a7c-002` (`ready`) — **ВТОРАЯ проверка узла, отдельной свежей физической
сессией.** В наряде сказано прямо: по плею второй FAIL кончает бумажную фазу узла — возврата в
`converge`/`converge-arch` не будет, поверхность замораживается уликой, и владельцу выносится ОДНО
решение с тремя ветками (резать / делить в `map` / идти на том, что отвечено), с рекомендацией и
названным доводом ПРОТИВ рекомендации.

Первая атака наряда — новая `A10` и её арифметика (покоящийся груз ≈409 Н при пороге 400), которую сам
разбор объявляет расчётом, а не наблюдением. Вторая — §REPAIR-LEDGER, написанный ради механической
сверки. Третья — четыре понижения: каждое обязано быть понижением, а не переодетым ответом.

Дальше, при PASS, — `shape`.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-repair-001.md
