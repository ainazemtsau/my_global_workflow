# RESULT — s-work-g-5a7c-refusal-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-refusal-reason-1, полоса `почему`
track: почему
date: 2026-08-31
outcome_kind: dispatch — свободный слот занят полосой «почему»; заодно закрыта зависимость, которую нарезка пропустила

owner_approved: |
  Его вопрос: **«в свободный слот ничего послать не можем?»** — прямая просьба занять WIN-U3.
  Ни одна строка `done_when` не изменена: задача `t-refusal-reason-1` стояла в волне с нарезки.

outcome: |
  **ПОЛОСА `почему` РАЗБЛОКИРОВАЛАСЬ САМА, И ЭТО НАДО БЫЛО ЗАМЕТИТЬ.** Её держал общий файл
  `HouseholderDecision.cs` с ногой хука прибытия — я записал это ограничение утром, когда разрешал
  той ноге расширить поверхность. Хук закрыт и опубликован, файл свободен, столкнуться больше не с
  чем.

  **ПОЛОВИНА ЭТОЙ ЗАДАЧИ УЖЕ СДЕЛАНА ЧУЖОЙ НОГОЙ.** Прибор печатает `OutcomeWord` — девять
  человеческих формулировок вместо номеров. То есть **экран готов показывать причину, а причины
  нет**: обработчики решений её не производят. Наряд это прямо называет и запрещает переписывать
  готовое: «если для показа хватит уже написанного, так и скажи».

  **ЧИСЛА В КАРТОЧКЕ БЫЛИ НЕВЕРНЫ, И НАРЯД НЕСЁТ ИСПРАВЛЕННЫЕ.** Перемерено на `a45b4957`:
  исходов **девять** (восемь отказных плюс `Won = 8`), а не восемь; обработчиков в поставочном
  профиле **семь** (три `local-incident` + четыре `chore`), а не шесть — шесть это число
  файлов-классов, и поставка использует из них только два вида. Единственное место, где ставится
  `NoProposal`, — `HouseholderDecisionPipeline.cs:199`, и туда попадают все семь.

  **ЗАОДНО ЗАКРЫТА ЗАВИСИМОСТЬ, КОТОРУЮ НАРЕЗКА ПРОПУСТИЛА.** `t-morning-content-1` стояла `open`
  без единого условия: порядок внутри полосы `утро` ставил её просто «после хука прибытия», и она
  была законным кандидатом на свободный слот. Но **дело ведёт хозяина туда, где он ВЕРИТ, что
  вещь, и закрывается меткой, которую ставит увиденное**, а прибор напечатал базу — он видит
  **0 из 14**. Утро, написанное сегодня, дало бы ровно ту картину, которую владелец уже видел:
  человек дошёл до кружки и замер. Содержание судилось бы на сломанном механизме.
  Поставлена `blocked` с условием и причиной в журнале.

evidence: |
  Перемерено направлением на `main` = `dev` = `origin/main` = **`a45b4957`**:

  - `HouseholderDecisionCandidate.cs:26-50` — девять членов перечисления, `Won = 8` девятый;
  - `HouseholderDecisionPipeline.cs:199` — единственное вхождение `NoProposal` во всём файле;
  - `default.householder.json` — `decisionHandlers` семь, типы ровно два: `chore`, `local-incident`;
  - `HouseholderDecision.cs` свободен: правившая его нога `t-arrival-hook-1` закрыта и в `main`.

  **НЕПЕРЕСЕЧЕНИЕ С ТРЕМЯ ИДУЩИМИ НОГАМИ** — пофайлово:
  - `почему` (U3): `HouseholderDecisionPipeline.cs`, `HouseholderDecisionCandidate.cs`, шесть
    `Householder*DecisionHandler.cs`;
  - зрение (U1): `Network/**`;
  - безопасное закрытие (U4): `tools/**`;
  - доработка гейта (U2): один headless-тест по выбору, и ему прямо запрещён
    `HouseholderDecisionPipelineTests.cs`.
  Общих файлов нет.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-refusal-reason-1-001` → `t-refusal-reason-1`, слот WIN-U3, база `a45b4957`,
    документ `work/2026-08-31-call-refusal-reason-1.md`;
  - `t-refusal-reason-1` → `active`.

  ЗАБЛОКИРОВАНО С ПРИЧИНОЙ (1):
  - `t-morning-content-1` → `blocked`, `unblock_when: t-sight-thing-1 доехала в main`.
    Полная причина — в журнале карточки.

  Четыре слота заняты, все четыре полосы волны имеют живую работу.

captures: |
  - Наряд впервые предупреждает соседа о ЧУЖОЙ мине: нога гейта в WIN-U2 сеет её в headless-тест,
    и исполнитель `почему` может увидеть временно красный тест, которого не ломал. Раньше такое
    приводило к тому, что нога чинила чужое или останавливалась зря.

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — свободный слот; перебраны все незакрытые задачи волны и их зависимости.
  2. WORK — наряд написан на перемеренных числах, а не на числах карточки; проверено
     непересечение с тремя идущими ногами; найдена и закрыта пропущенная зависимость.
  3. (owner) — его прямая просьба занять свободный слот.
  4. CLOSE — RESULT финальным сообщением, применение и коммит после него.

log: |
  свободный слот занят полосой «почему» — семь обработчиков перестанут молчать одним безымянным
  словом; заодно утро заблокировано до починки зрения: на слепом хозяине содержание судить нельзя

next: |
  Домой. Идут четыре ноги: зрение U1, доработка гейта U2, «почему» U3, безопасное закрытие U4.

  Ждут: `t-morning-content-1` — зрения; `t-carry-back-1` — зрения; `t-bench-by-fact-1` — зрения
  (прибор уже доехал). То есть **следующий возврат зрения разблокирует сразу три задачи**.
