# RESULT — s-repair-g-6b13-records-vs-reality-001

play: repair
direction: indie-game-development
node: g-6b13
date: 2026-08-02

## outcome

Владелец перечислил одиннадцать утверждений предыдущей сессии и потребовал перепроверить каждое
своими руками. **Все одиннадцать проверены первой рукой; подтвердились все.** Состояние приведено к
проверенной реальности; карточка (TREE) НЕ тронута и уходит ему на слово; перенарезка задач и наряды
ждут его ответов — порядок его собственный: сначала правда в записях, потом карточка, потом нарезка.

## evidence

Продуктовый репозиторий `C:/projects/Unity/GasCoopGame`, main @`4e95845c` (проверено, база наряда
совпадает). Только чтение, ни один файл продукта не изменён.

| № | Утверждение владельца | Вердикт | Чем проверено |
|---|---|---|---|
| 1 | WIN-U1 занят, коммиты 8eb0bb0f..c4d34fc4, NOW числит наряд `ready` | **ПОДТВЕРЖДЕНО** | `.git/gascoop-slot-state.v1.json`: `WIN-U1 = CLAIMED`, аренда `c-exec-newgame-folder-and-gates-001:PLAN`. `git log main..HEAD` в `GasCoopGame_win-u1`: ровно семь коммитов, первый 8eb0bb0f, последний c4d34fc4. NOW.md строка `status: ready` |
| 2 | 1427 добавленных строк, кода игры шесть строк в `BuildMarker.cs`, 247 строк тестов на красный | **ПОДТВЕРЖДЕНО** | `git diff --shortstat main..HEAD` = 25 файлов, 1427 insertions, 3 deletions. `Assets/TunnelCrew/Core/BuildMarker.cs` = 6 строк, пустой `static class`. `tests/TunnelCrew.Core.Tests/Sources/SourcePresenceGateTests.cs` = 247 строк |
| 3 | Сканы показывают на остающиеся пути, без переноса зелёные и правок не требуют; ломает их перенос | **ПОДТВЕРЖДЕНО** | Запуск на main: `zero-float-scan` exit 0 (82 файла), `int-overflow-scan` exit 0, `type-hardcode-scan` exit 0. Их корни — `Assets/GasCoopGame/Core/Field/*`, `Core/Sim`, `Core/Hashing` — при отсутствии переноса не двигаются. Добавочность гейтов новой папки доказана самой остановленной работой: `Assets/TunnelCrew/**` + `core/TunnelCrew.Core.csproj` + свой тестовый проект заведены, старый код не тронут |
| 4 | «Первая инженерная нога, до кода» — не его слово; его слово решает ПЕРЕНОС, а не порядок | **ПОДТВЕРЖДЕНО** | `history/2026-08-01-s-frame-burrow-concept-charter-deltas-001.md` §Правки владельца: его слова — «Я бы перенёс в архив, либо в какую-то папку, и тесты в ту папку, и с них ничего не запускать», «назвать другую папку, другой путь, и всё, он актуален». О порядке ни слова. Фраза «ПЕРВАЯ ИНЖЕНЕРНАЯ НОГА, до кода… одним движением» стоит только в §next того же RESULT — это маршрут ноги |
| 5 | `owner_verdict_exact` содержит английский пересказ, одобрения не было | **ПОДТВЕРЖДЕНО** | `docs/measurements/root-receipts/c-exec-newgame-folder-and-gates-001/00-plan.json`: `"owner_verdict_exact": "folder name, relocation form, and gate shape are repository technical decisions; old links may not interfere"`, рядом `"verdict": "PLAN_CONTRACT_FROZEN_OWNER_DELEGATED"` |
| 6 | Перенос выключает единственный тестовый пакет; 27 файлов зашивают путь строковой константой | **ПОДТВЕРЖДЕНО** | Под `tests/` ровно один `.csproj` — `GasCoopGame.Core.Tests`. 27 его `.cs`-файлов содержат `Assets/GasCoopGame`; в рабочем коде, а не в комментариях: `private const string CoarseRootRel = "…"`, девять `[TestCase("tools/…-scan.ps1", "Assets/GasCoopGame/…")]`, вызовы `RequiredFileExists("Assets/GasCoopGame/…")` |
| 7 | «Никаких дробных», «никаких переполнений», «ядро без движка» жили ради побитового счёта; теперь считает ведущий | **ПОДТВЕРЖДЕНО, и сказано это самой проверкой** | `tools/zero-float-scan.ps1:15-17`: «This scan ON ONE MACHINE *is* the cross-CPU guarantee (integer cross-CPU equality is a GIVEN; the only real desync risk is a float on the authoritative path)». `AGENTS.md:720-727` — правила #1 и #6 конституции. При «считает ведущий» независимого счёта нет; CHARTER:97 требует явно подтвердить или снять — не сделано ни то, ни другое |
| 8 | Правила исправлены сегодня коммитом 5ab48d6e, разбор узлу не нужен | **ПОДТВЕРЖДЕНО** | `git show 5ab48d6e`: converge закрывает строку только источником вне решающей ноги, G7 требует спрашивать при сомнении. Разбор по узлу g-6b13 не открывается: стенды идут прямо в ставку |
| 9 | Проверку размера задач при открытии ставки не сделали | **ПОДТВЕРЖДЕНО** | `history/2026-08-01-s-shape-g-6b13-two-stands-bet-001.md` §play_check пункт 7 — «шесть задач, у каждой проверяемое done_when»; о размере ни слова. KERNEL §3 и shape шаг 7 требуют ≤ полудня |
| 10 | Карточка ставки его словом не утверждена, записано как ожидающее | **ПОДТВЕРЖДЕНО** | `TREE.md` ledger: «`owner_approved` FOR THIS CARD IS PENDING AND IS RECORDED AS PENDING, not claimed». Та же нога записала это в `play_check` как незакрытый G9 |
| 11 | Нарезка предыдущей сессии — предложение, а не приказ | **ПРИНЯТО КАК ПРЕДЛОЖЕНИЕ** | Оценено; собственная нарезка показана владельцу в чате, в состояние не записана до его слова |

## state_changes

Только `NOW.md` и `LOG.md`. `TREE.md`, `CHARTER.md` и продуктовый репозиторий не тронуты.

1. `bet.appetite` и `bet.kill_by` — назначенные сессией даты сняты как выдумка; оба поля пусты и ждут
   его слова. Разрыв по G4 записан прямо, а не закрыт.
2. `bet.forecast`, `bet.against` — сроки убраны; довод про второй способ переноски снят вместе с
   задачей.
3. `tasks` — заголовочная пометка о том, что весь список ждёт перенарезки. `t-1`: `active` → `open`
   (наряд остановлен, ничего не идёт). `t-3`: `open` → `dropped` его прямым словом.
4. `open_calls[c-exec-newgame-folder-and-gates-001]`: `ready` → `paused` + поле `stopped` с его
   словами; `slot` исправлен с «все четыре свободны» на фактическое `WIN-U1 CLAIMED`; `note`
   почищена от мёртвой посылки.
5. `i-old-code-move-to-own-folder-001`: `review_when` переписан — обе половины прежней записи
   опровергнуты; добавлен измеренный факт про единственный тестовый пакет.
6. `i-product-constitution-unrouted-001`: переписан — названо, зачем существовали проверки
   детерминизма, и что перенос от них не зависит; убрана фраза про «первую ногу до кода».
7. Три новые записи: `i-newgame-call-oversized-and-stopped-001`,
   `i-bet-tasks-never-sized-001`, `i-plan-receipt-fabricated-owner-verdict-001`.
8. `direction_forecast` — `as_of`, `basis`, первый драйвер и `update_when` приведены к реальности.
9. `decisions` — пять открытых вопросов владельцу записаны как открытые.

## captures

- Собранный exe перестал быть вырезом и стал требованием («чтобы играть с женой»); строка исключений
  карточки говорит обратное. Правится картой/шейпом после его вердикта, не этой ногой.
- Стенд хозяина получил требование расширяемости («на нём же дальше растим ИИ») — это ближе к
  критерию 7 карточки g-4d7a, чем нынешняя формулировка t-5.

## decisions_needed

Пять, все вынесены владельцу в чате этой ноги и записаны в `NOW.decisions`: условие провала ставки
без дат; срок переноса старого кода; сколько работ гонять одновременно; копание в этой ставке или
отдельной; вердикт на изменённые строки карточки.

## play_check

- 1 назвать противоречие: сделано — одиннадцать пунктов, все с источником.
- 2 реконструкция: сделано — новейшее первым, артефакты и коммиты выше логов; продуктовый репозиторий
  прочитан первой рукой, ничего не принято на веру.
- 3 предложенное состояние: сделано — по одной причине на изменение, ни одна улика не потеряна,
  выдуманные строки помечены выдуманными, а не удалены молча.
- 4 подтверждение владельца: содержание правок он задал сам, назвав каждый пункт и потребовав
  «то, что подтвердится, запиши в состояние»; остановку наряда назвал своими словами, что и есть
  разрешение по границе снятия. **TREE не тронут ни строкой** — карточка идёт ему на слово отдельно,
  как он и потребовал.
- 5 трение: новой строки FRICTION нет. Класс «нога назвала сама, и строка стала несущей» уже ведёт
  `i-paper-loop-outran-the-build-001`; сегодняшние два случая (порядок работ и «сканы переписываются
  в любом случае») — тот же класс, и они записаны в свои issue, а не в новое правило.
- G4: НЕ ЗАКРЫТ и записан как разрыв — kill_by пуст, потому что даты запрещены владельцем, а порог
  без даты решает он. Сессия его не назначает.
- G7: пять вопросов вынесены с вариантами и рекомендацией, батчем.
- G9: карточка не менялась; вердикт на её изменённые строки — открытый вопрос.

## log

одиннадцать утверждений перепроверены первой рукой и подтверждены; состояние приведено к реальности,
карточка ушла владельцу на слово

## next

Ждём его ответов на пять вопросов. Затем — `shape` свежей сессией: изменённые строки карточки в
TREE, мелкая нарезка задач по правилу «каждая задача — то, на что можно посмотреть глазами», и
наряды. Наряд `c-exec-newgame-folder-and-gates-001` повторно не выдаётся ни в каком виде.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-repair-g-6b13-records-vs-reality-001.md
