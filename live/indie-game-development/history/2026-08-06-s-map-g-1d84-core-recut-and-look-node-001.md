# RESULT — s-map-g-1d84-core-recut-and-look-node-001

play: map · direction: indie-game-development · node: g-1d84 (+ новый g-3f90) · дата: 2026-08-06

## outcome

Граница волны переписана под его решение от 2026-08-06: волна покупает ЛОГИКУ И СЕТЬ, а картинку
отдаёт отдельному узлу. Обе карточки показаны ему целиком до единой правки и получили ДВА отдельных
вердикта — `1 принимаю` на текст урезанной g-1d84 и `2 принимаю` на текст нового g-3f90.

Задачи, вырезы, полосы, аппетит и линзы НЕ тронуты. Они теперь расходятся с карточкой осознанно: это
работа `shape`, и нога её намеренно не сделала.

## evidence

### Его слова, на которых стоит правка (дословно, 2026-08-06)

Источники: history/2026-08-06-s-day-g-1d84-profile-format-and-core-recut-001.md и
history/2026-08-06-s-day-g-1d84-slots-released-and-noise-ruling-001.md.

> пусть с примитивами плохой дом, все равно, первое, что я хотел получить, чтобы по сети работает вот
> это

> вот этой волной хотим закончить, что хозяин четко ходит, все работает по сети, при переносе лут можно
> мышь ударить, он привлекает хозяина, а ну хозяин, когда видит этих игроков, он к ним бежит

> Тесное место, где груз не проходит — убираем

> Лут место обстановки, да. Тонн[ель] остается.

> Хочу, чтобы это было, во-первых, в фоне, во-вторых, чтобы это не [у]длиняло волну.

> возможно, это для следующей волны будет […] уже там мы продумаем больше вариант, что мы хотим видеть
> для демо, для скриншотов, и уже там под это устанавливать волну.

> Не знаю, надо, не надо, наверное, не надо, да? […] это отдельно может больше к визуалу.  (масштаб)

> удар по игроку — частный случай шума (давай пока так сделаем)

Его вердикты на показанные тексты, этой ногой:

> 1 принимаю

> 2 принимаю

### Перемер продукта первой рукой (все команды выполнены этой ногой, база `d60c2f04`)

Наряд просил перепроверить и не строить заново. Перепроверено, и одно место оказалось СИЛЬНЕЕ, чем
он говорил.

1. **Цепочка реакции у хозяина стоит целиком.** `Assets/TunnelCrew/Core/Householder/Householder.cs:58`
   — `enum HouseholderEventName` (`BoardCreaked`, `QuietRustle`, `RepeatedHeavyNoise`);
   `AuthoritativeHouseholder.cs:195` `FactOf(...)` переводит событие в замеченный факт;
   `Householder.cs:437` ставит занятие `HouseholderOccupation.InvestigateSuspiciousNoise`. Вход в
   цепочку — `AuthoritativeHouseholder.Hear(...)` через `TakeHeardEvent()`.
2. **В мире эти события не производит НИЧТО.** Единственные вызовы `EmitBoardCreak` /
   `EmitQuietRustle` / `EmitRepeatedHeavyNoise` во всём `Assets/**` — три кнопки инспектора редактора
   (`Assets/TunnelCrew/Editor/HouseholderRouteControllerEditor.cs:175,181,189`). Это ровно то, что
   чинит новый критерий 7.
3. **Со стороны груза шов стоит ДАЛЬШЕ, чем говорил наряд.** `Network/CargoBodyContact.cs` меряет силу
   удара и точку на каждом шаге физики; `Network/CargoBody.cs:190 ReadContact` переводит их в
   `CargoContact` слоя правил; `Core/Cargo/AuthoritativeCargoRoster.cs:113` кладёт значение на каждый
   груз КАЖДЫЙ тик и отдаёт геттером `GetContact` (строка 132). **У `GetContact` ноль вызовов** —
   единственный другой хит `GetContact` во всём дереве это `collision.GetContact(0)` внутри самого
   `CargoBodyContact`. То есть значение уже доезжает в тот же слой, где живёт хозяин, и там
   останавливается. Комментарий файла («Nothing listens to it») верен по существу и занижает, как
   далеко проложен шов.
4. **Вывод наряда о размере подтверждён:** «хозяин замечает» — соединение двух готовых концов, а не
   постройка.
5. **Обстановки в продукте нет вообще.** `Assets/TunnelCrew/World/` содержит стены, дом, хозяина,
   контроллер маршрута, настройки, локального ходока и панель условий — и больше ничего. Его довод
   «обстановку мы еще-то и не делали» верен буквально.
6. **Слова «лут» в продукте нет ни в одном файле** (`git grep -i loot` по `Assets/TunnelCrew/**` —
   пусто). Поэтому в карточке лут назван тем, что несут, а не новым классом предметов.
7. **Стены и глаза:** все восемь стен `ThreeRoomHouse.prefab` — `m_LocalScale.y: 1.5` при
   `m_LocalPosition.y: 0.75`; `NetworkPlaySettings.asset:27` `_eyeHeight: 1.7`. Верх каждой стены на
   20 см ниже глаз. Это критерий 2 нового узла.
8. **Вход нового узла существует:** `origin/probe/free-kits-2026-08-06` = `f980dce9`,
   `git rev-list --count origin/main..` = 2, `git diff --stat origin/main...` = 50 файлов,
   1761 вставка, 0 удалений, всё внутри `Assets/TunnelCrew/Art/FreeKitsProbe/`. Продуктовый
   `main` = `origin/main` = `d60c2f04`.

### Что именно изменилось в карточке g-1d84

| Критерий | Было | Стало |
|---|---|---|
| 3 КАМЕРА | первое лицо + переключение в третье | только первое лицо; третье уезжает в `g-3f90`; добавлена одна строка-сторож, что камера не строится так, чтобы третье лицо стало невозможным |
| 6 ДОМ | «не одна пустая коробка… и в комнатах стоит обстановка… задетая обстановка — один из источников» | дом ОСТАЁТСЯ уже собранной коробкой из трёх комнат; обстановки в волне нет |
| 7 (новый) | — | ШУМ РОЖДАЕТ МИР, А НЕ КНОПКИ РЕДАКТОРА: источник — контакт несомого груза (лута), удар по игроку тот же шов |
| 10 ВНЕШНИЙ ВИД | проба наборов, арт в сцене | критерий УДАЛЁН целиком, переехал в `g-3f90` |
| 9 ТОННЕЛИ (был 9, стал 10) | открываются, не копаются | НЕ ТРОНУТ — его «Тоннель остаётся» |
| исключения | 14 позиций | +6: внешний вид и покупка ассетов, настоящий дом и обстановка, поднятие стен, третье лицо, масштаб, тесное место |
| why / risk | скриншоты как довод, риск бесплатных наборов | переписаны: волна покупает логику и сеть; новый третий риск — волна может кончиться без единого показуемого кадра |

Критерии 1, 2, 4, 5, 8 (погоня), 9 (швы), 11 (сборка), 12 (закрытие) — байт в байт прежние.

### Где нога прочитала, а не процитировала

**Что «лут» — это то, что НЕСУТ, а не новые предметы, стоящие по комнатам.** Прочтение показано ему
ОТДЕЛЬНЫМ абзацем, помеченным как прочтение, с названной альтернативой («лут это новые предметы в
комнатах, которые надо подбирать — это меняет критерий 7 и добавляет работу») и прямой просьбой
поправить. Он не поправил и принял карточку, содержащую это прочтение. Сила токена ровно такая:
альтернатива была показана и НЕ выбрана; отдельного «да» на само прочтение он не давал.

### Ширина дерева — что НЕ было подписано

Правило дерева — не больше семи открытых детей у узла (`os/schema/direction-files.md`). У `g-0c26`
их было ровно семь; `g-3f90` делает восьмой: `g-1d84`, `g-3f90`, `g-c519`, `g-2b7f`, `g-8a41`,
`g-7b42`, `g-9d16`, `g-c4af`. Быстрый путь плея требует при заполненности сперва получить одобрение
на снятие или слияние. **Ни снятия, ни слияния он не одобрял, и ни одного не сделано.** Ему была
показана одна строка с рекомендацией принять восьмую ветку и вернуться к счёту при закрытии `g-1d84`
(закрытый узел схлопывается в строку истории, и веток снова станет семь) со словами «Скажи, если
хочешь иначе». Он не сказал иначе и принял узел. Записано именно так: `2 принимаю` — вердикт на текст
карточки, и лишь следствием на ширину. Известно наперёд и не скрывается: узел «полный заход демо»
(`i-full-run-node-to-be-authored-001`) сделает девятую.

### Шаги плея, пропущенные быстрым путём, и почему

`map` §Roadmap-node fast path: один ограниченный ребёнок под утверждённым родителем — улики
переиспользуются, скелет / поиск / свод по линзам пропускаются с причинами.

- **Шаг 2 (кандидаты владельца + свежий `map_evidence`)** — не спавнился. Содержание правки ЦЕЛИКОМ
  его собственные слова того же дня; анкорить нечем, а исследовательская нога добавила бы чужой
  голос туда, где решение уже принято им. Переиспользованные улики: две сегодняшние `history/`, восемь
  измерений продукта выше, `i-wave-recut-to-core-owner-decided-001`, `i-house-walls-below-eye-level-001`.
- **Шаг 3 (скелет)** — пропущен: узел один, показывать «всю карту на одном экране» нечего.
- **Шаг 8 (свод по линзам)** — пропущен быстрым путём. Линзы волны переписывает `shape` вместе с
  нарезкой, и делать это здесь значило бы решать за неё.
- **Шаг 4, требование «нешаблонного пути»** — честно: отдельного нешаблонного маршрута к критериям
  успеха эта нога не авторила, потому что содержание узла продиктовано владельцем и альтернативного
  маршрута он не заказывал. Нешаблонное в карточке одно и настоящее: узел начинается с УЖЕ
  оплаченной ветки пробы, а не с новой look-dev волны.
- **Шаг 6 (порядок)** — не спрашивался: новый узел `parked`, порядок между ним и волной уже задан его
  же словом «возможно, это для следующей волны будет».

## state_changes

1. `TREE.md` `g-1d84`: `goal`, `done_when` (двенадцать критериев + строка исключений), `why`, `risk`
   заменены на утверждённый им текст. `status: active`, `appetite`, `kill_by`, `detail` НЕ тронуты.
2. `TREE.md`: добавлен узел `g-3f90` (шесть критериев + открытый вопрос про масштаб + граница «облик
   здесь не назначается» + исключения), `status: parked`, `detail` — на этот файл. Место — сразу за
   `g-1d84`, потому что он его прямое продолжение.
3. `TREE.md` `owner_approved`: добавлена запись о двух отдельных вердиктах с точной силой каждого,
   полным перечнем правок, зафиксированным прочтением про «лут» и явной записью, что ширину дерева он
   отдельно не подписывал.
4. `NOW.bet.goal`: пересказ карточки приведён в соответствие с TREE (KERNEL §3 — пересказ обязан
   совпадать). Рядом поставлен комментарий, что остальные поля ставки не тронуты и сводятся `shape`.
5. `NOW.open_calls` `c-exec-g-1d84-free-kits-probe-001`: `unblock_when` теперь называет узел по id
   `g-3f90` вместо обещания «узел будет назван при `map`»; строка остаётся `blocked` и предложением
   не является.
6. `NOW.md` шапка `updated:` — на эту ногу.
7. `LOG.md` — одна строка.

**Чего нога НЕ трогала и почему.** Все двадцать три задачи, десять вырезов, семь линзовых вердиктов,
пять полос, `appetite`, `kill_by`, `evaluator`, `riskiest_assumptions` — работа `shape`; наряд прямо
это запретил. Идущие наряды `c-exec-g-1d84-house-as-data-001` и
`c-exec-g-1d84-householder-profile-file-001` не тронуты. `NOW.issues` не тронуты вовсе: `Writes:`
плея `map` их не покрывает, поэтому `i-wave-recut-to-core-owner-decided-001` остаётся ОТКРЫТОЙ, и
закрыть её сможет `shape` (её `route` — `map`, но её вторая половина ещё не сделана) либо `day`/`pulse`
после перенарезки. `CHARTER.md` не тронут.

## captures

- Задача `t-host-9` «Догнал — отбирает» существует в нарезке, а строки в карточке под неё нет и не
  было. Нога её НЕ добавляла: это расширило бы карточку сверх того, что он подписал. Свести — работа
  `shape`.
- Вырез №3 ставки («ровно три комнаты») и линза 2 («ЗАДАЧА t-house-6») теперь читаются против новой
  карточки иначе, чем писались. Это ожидаемое расхождение, а не дефект.
- Его слово «Хочу, чтобы это было, во-первых, в фоне» про дом и вид сохранено дословно и в требование
  не превращено: карточка `g-3f90` времени и способа запуска не назначает.

## decisions_needed

Нет. Единственное открытое решение направления — `d-agents-skills-belong-in-repo-001`, этой ногой не
трогалось.

## play_check

- G9 выполнен буквой: оба текста показаны ЦЕЛИКОМ в чате ДО единой правки файлов, вердикты запрошены
  раздельно и получены раздельно (`1 принимаю`, `2 принимаю`). Ни одного слова не выведено.
- G7: продуктовое содержание не изобретено. Единственное место, где нога прочитала его слова, названо
  вслух ему и записано выше как прочтение с показанной и не выбранной альтернативой.
- G2 не нарушен: `g-3f90` `parked`, задач и полос не получил, исполнительных строк не создал.
- G1 не тронут: ставка одна, полос по-прежнему пять, WIP не менялся.
- `map` §Writes соблюдён: `TREE.md`, `NOW.md` (`open_calls` + пересказ ставки по KERNEL §3), `LOG.md`.
  `NOW.issues` не писались — грант плея их не покрывает.
- Быстрый путь и три пропущенных шага объявлены с причинами (см. evidence).
- Ширина ≤7 нарушена сознательно и не замаскирована; снятия/слияния он не одобрял.
- Все восемь продуктовых утверждений перемерены первой рукой на объявленной базе `d60c2f04` в
  `C:/projects/Unity/GasCoopGame`, а не унаследованы из наряда — класс
  `i-call-named-path-absent-on-declared-basis-004`.

## log

g-1d84: карточка волны УРЕЗАНА ДО ЯДРА и заведён узел `g-3f90` «настоящий дом и вид» — оба текста
показаны целиком и получили ДВА отдельных вердикта, `1 принимаю` и `2 принимаю`. Волна теперь покупает
логику и сеть: критерий внешнего вида удалён целиком, третье лицо и обстановка ушли, дом остаётся уже
собранной коробкой из примитивов, тоннель НЕ тронут его словом, а новый критерий 7 требует, чтобы шум
рождал мир, а не кнопки редактора — источник этой волны контакт несомого груза, удар по игроку тот же
шов. Перемерено первой рукой на `d60c2f04`: цепочка реакции у хозяина стоит целиком, а производят её
ТОЛЬКО три кнопки инспектора; со стороны груза шов проложен дальше, чем считалось — сила и точка уже
доезжают в слой правил и лежат на каждом грузе каждый тик, у геттера `GetContact` ноль вызовов; слова
«лут» и обстановки в продукте нет вовсе. Узел `g-3f90` начинается с уже сохранённой ветки
`probe/free-kits-2026-08-06` (2 коммита, 50 файлов, 0 удалений) и везёт стены 1.5 при глазах 1.7,
обстановку, третье лицо и скриншоты; масштаб записан открытым вопросом, а не требованием. Записано
честно: прочтение «лут — это то, что несут» показано ему как прочтение и не поправлено, а ширину дерева
(восьмая живая ветка при потолке семь) он отдельно не подписывал — снятия и слияния не одобрял и не
делалось. Задачи, вырезы, полосы и линзы НЕ тронуты — они расходятся с карточкой осознанно и сводятся
`shape`.

## next

`shape` на перенарезку g-1d84 по подписанной карточке — отдельной свежей ногой. Что она обязана свести:
двадцать три задачи против двенадцати критериев (наружу уходят `t-house-2` тесное место, `t-house-3`
обстановка, `t-look-1`, `t-look-2`, `t-player-2`; внутрь приходит связка «мир производит шум»),
десять вырезов, семь линзовых вердиктов, пять полос до нужного числа и диспозиция приостановленного
наряда `c-exec-g-1d84-cargo-tight-spot-001` под вырезанную задачу. Она же может закрыть
`i-wave-recut-to-core-owner-decided-001`, чей маршрут `map` эта нога исполнила наполовину.

Владельцу решать нечего до `shape`; идущие два наряда работают дальше без изменений.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-map-g-1d84-core-recut-and-look-node-001.md
