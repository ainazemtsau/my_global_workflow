# RESULT — s-repair-g-5a7c-bench-tooling-stop-001

direction: indie-game-development
play: repair
node/task: g-5a7c / t-bench-by-fact-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: repair — СТОП по недоступному инструменту принят как образцовый; диагностика оставлена исполнителю, наблюдение направления передано как вход, а не как вердикт

owner_approved: |
  Его слово 2026-08-31: **«продолжай»** — после того как он **отклонил** попытку направления самому
  запросить состояние его редактора. Граница принята и записана: **направление чужой редактор не
  трогает.** Первое доказательство работоспособности — за исполнителем.

  Ни одна строка `done_when` не изменена.

outcome: |
  **НОГА ОСТАНОВИЛАСЬ ПРАВИЛЬНО, И ЭТО НЕ ФОРМАЛЬНОСТЬ.** Инструментов
  (`editor-application-get-state`, `editor-application-set-state`, `screenshot-game-view`) в её
  распоряжении не оказалось — она **не запускала полигон, не доказывала пересборку, не создавала
  отчёт-суррогат, не писала карту, файлов не меняла и отпустила слот**, прямо отказавшись от обхода
  через динамический C#.

  **ТРЕТИЙ СЛУЧАЙ ЗА ДЕНЬ, КОГДА ИСПОЛНИТЕЛЬ ПРЕДПОЧЁЛ ПУСТОЙ РЕЗУЛЬТАТ ЛОЖНОМУ.** Нога «почему»
  честно назвала свой фрагмент реконструированным; нога утра выбросила собственный первый прогон,
  поймав старую сборку; эта не стала изображать замер вовсе.

  **И ОНА ПОШЛА РАЗБИРАТЬ ПРИЧИНУ, А НЕ ЗАЯВЛЯТЬ СЛОТ ЗАНОВО.** Её план — сравнить локальную
  конфигурацию U4 с тем, какие Unity-инструменты реально загрузились в задачу, и проверить
  allowlist, профиль и **статический снимок набора на старте задачи**. Это правильный шаг, и
  направление его не перехватывает.

  **НАПРАВЛЕНИЕ ПЕРЕДАЛО СВОИ ДАННЫЕ КАК ВХОД, А НЕ КАК ОТВЕТ, И ЭТО ВАЖНО.** Оно наблюдало, что в
  ЕГО СОБСТВЕННОМ списке инструменты `unity-win-u4` сначала недосчитывались трёх названных, а затем
  **вернулся целый пласт сразу** — больше сорока имён одним куском. Соблазн был объявить это
  причиной («MCP отвалился и переподключился») и закрыть вопрос.

  **Это было бы тем же дефектом, который направление сегодня совершало трижды: вывод по неполному
  замеру.** Оно видело СВОЙ список, а не список задачи; совпадение симптома не есть совпадение
  причины. Гипотеза исполнителя про снимок на старте объясняет наблюдение не хуже **и проверяется
  изнутри**, тогда как наблюдение направления не проверяется никак.

  **ДИАГНОСТИКА ОБЪЯВЛЕНА ВАЖНЕЕ САМОЙ КАРТЫ.** Если набор инструментов снимается один раз на
  старте задачи, на этом месте однажды встанет любая нога. Поэтому в возврат затребованы три вещи:
  что режет набор, повторяемо ли это, и что делать следующей ноге.

evidence: |
  Наблюдение направления, приведённое как вход диагностики:

  - в его списке инструментов `unity-win-u4` в начале отрезка отсутствовали
    `editor-application-get-state`, `editor-application-set-state`, `screenshot-game-view`;
  - затем появились они и ещё около сорока имён одним куском — `console-clear-logs`,
    `script-read`, `script-update-or-create`, `reflection-*`, `profiler-*`, `package-*`,
    `probuilder-*`, `assets-copy/move/delete`, `editor-selection-*`, `type-get-json-schema`;
  - у `unity-win-u1` те же три инструмента присутствовали всё время.

  Состояние, оставленное ногой, проверено и не нарушено: `slot/win-u4` = `bd76a42c`, дерево
  чистое, слот `AVAILABLE`.

  **Запасной путь проверен вхолостую и не задействован:** `git merge-tree --write-tree
  slot/win-u1 slot/win-u4` → `exit 0`; в полученном дереве профиль со сценарием сохраняется
  (`routine` шесть шагов, три инцидентных обработчика).

state_changes: |
  ДОПИСАНО (1 документ наряда):
  - `work/2026-08-31-call-bench-on-the-walking-branch-1.md` — дополнение: СТОП принят, диагностику
    ведёт исполнитель, наблюдение направления передано как вход; затребованы три ответа о причине;
    приёмка не тронута; запрет на обход подтверждён.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-bench-by-fact-1` — `active`; `c-exec-g-5a7c-bench-by-fact-1-002` — `ready`.

captures: |
  - **ОТКАЗ ИНСТРУМЕНТА ВЫГЛЯДИТ ИЗНУТРИ КАК ОГРАНИЧЕНИЕ ПРАВ, И ОТЛИЧИТЬ ЭТО ОТ СОСТОЯНИЯ СВЯЗИ
    НЕЧЕМ.** Нога описала симптом честно и сделала верный вывод для своей позиции. **Правило-кандидат:
    на СТОП по недоступному инструменту дом даёт свои наблюдения, но диагноз ставит тот, кто
    находится внутри задачи** — только он может прочитать её конфигурацию.
  - **НАПРАВЛЕНИЕ ЕДВА НЕ ЗАКРЫЛО ВОПРОС СВОИМ НАБЛЮДЕНИЕМ.** Оно уже написало «причина найдена и
    она временная» — и остановилось, заметив, что смотрит на свой список, а не на его. Это
    четвёртый за день случай того же класса, и первый, пойманный ДО отправки.
  - **ЗАПАСНОЙ ПУТЬ ГОТОВИТСЯ, НО НЕ НАВЯЗЫВАЕТСЯ.** Слияние в слот с рабочими инструментами
    проверено вхолостую и названо исполнителю как решение ДОМА, а не как его работа. Иначе он
    начал бы его строить вместо диагностики.

decisions_needed: |
  Ни одного. Решение о запасном пути принимается домом, если второй отказ подтвердится.

play_check: |
  1. Назвать противоречие — СТОП по отсутствию инструмента при том, что у соседнего слота он есть.
  2. Восстановить новейшим-первым — списки инструментов, состояние ветки и слота, слияние
     вхолостую; **и отделить наблюдаемое направлением от наблюдаемого задачей.**
  3. Предложить исправленное состояние — дополнение к наряду, приёмка не тронута, диагностика за
     исполнителем.
  4. Подтверждение (owner) — его «продолжай» и его отказ пускать направление в редактор.
  5. Дыра в OS → кандидат в правило записан.

log: |
  СТОП ноги по недоступному инструменту принят как образцовый — обхода не строила, файлов не
  меняла, слот отпустила; диагностику ведёт она сама, а наблюдение направления передано как вход,
  а не как вердикт: оно смотрело на свой список инструментов, а не на её

next: |
  Текст уходит владельцу. Свободны все четыре слота. Запасной путь через `slot/win-u1` проверен и
  ждёт, если второй отказ подтвердится.
