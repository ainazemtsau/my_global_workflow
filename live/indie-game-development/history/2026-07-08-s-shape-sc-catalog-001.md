# RESULT — s-shape-sc-catalog-001

play: shape (re-scope g-9c41 road slice Sc-typing → Sc-catalog) + executor CALL assembly
direction: indie-game-development
node/task: g-9c41 / Sc-catalog (re-scoped from Sc-typing)
date: 2026-07-08
input: |
  owner reframe (in-session, 2 fork rounds) — the next g-9c41 slice is NOT env-derived typing but a solid,
  convenient, extensible KIT to author genuinely-different gas types AS DATA (common + per-type custom laws),
  reactions between them, working in the sim; engine-only; no visual/mod/DLC/procedural/env-mechanism
  (design-aware only). + owner confirmation that the parallel c-exec-033 env-typing framing was an accidental
  wrong-chat and is to be treated as an error («случайно запустил не тот чат … остальные считаем за ошибку»).

## outcome
The g-9c41 character-road slice after Sc-reactions is RE-SCOPED from Sc-typing (env-derived dynamic typing) to
**Sc-catalog** — a data-defined gas-type authoring kit (EXTENSIBLE per-type laws + reactions between types + the
handler-into-production-tick fix; ENGINE-ONLY). Env-derived dynamic typing is PARKED as a later slice
(socket-aware, not built). The accidental parallel framing c-exec-033 (s-work-sc-typing-call-001) is SUPERSEDED
per owner. A hardened executor CALL **c-exec-034** is ready. Sc-catalog is the active slice; next = c-exec-034.

## evidence
- Owner reframe recorded in-session (2026-07-08): build only data-defined distinct types + extensible per-type
  params + reactions + the handler-fix; NO visual/mod/DLC/procedural/env-mechanism/new-physics (design-aware).
- Owner adjudication of the two-writer collision: «идём именно по плану который мы определили; остальные считаем
  за ошибку» → Sc-catalog wins, c-exec-033 superseded.
- Code grounding wf_2e0ee72b-3fc (3 readers over GasCoopGame main + dev2): confirmed the R15 3-layer param
  substrate (GasParentParams → GasMetaType → GasType, `?? ` inherit), the reserved-EMPTY GasInstanceReserved +
  GasLookReserved structs, per-type params today = only 2 int overrides (SpreadKp, RatioToAir), the
  handler-into-production-tick gap (VoxelFaceFlow.Step hardcodes CreateDefault; custom-registry overloads public
  but unused), GasTypeRegistry.ComputeContentHash fold, and the dev2 render read-model seam (IGasViewSource +
  GpuGasParams 128-byte per-type block; shader branch-free).
- CALL hardening wf_e9971bbd-6a8 (5 adversarial reviewers): 3 blockers folded — (1) contract false-alarm: my draft
  said synced 11; verified first-hand GasCoopGame validation.config = **18** (mechanical gates v5–v18 wired), only
  behavioral v19 owed → NOT a blocker; (2) byte-identity restated at the MeaningChecksum layer (not the ContentHash
  seed literal, which may legitimately change); (3) NEW-PARAM-IS-DATA given mechanical teeth (must compile+pass with
  ZERO Core/** edits, else it is the rejected rigid struct). Plus ~12 findings (resolved-value/uniform/length-guarded
  fold, inheritance-path + partition-aliasing REDs, handler-off-fold-only-if-compiled-identical, mass-clamp footgun,
  look-lane half-build guard, "genuinely different" = ≥2 law axes + reactions, v14/v15/multi-actor-property/v16 gates,
  parked-canon annotation). All folded into c-exec-034.

## state_changes (applied by this session as writer)
- NOW.md `updated:` → s-shape-sc-catalog-001 (2026-07-08).
- NOW.md bet.goal: road slot after Sc-reactions re-scoped Sc-typing → Sc-catalog; c-exec-034 framed+hardened; env-typing
  PARKED (socket-aware); accidental c-exec-033 SUPERSEDED; Latest session updated.
- NOW.md tasks: the `Sc-typing` (env-derived) task → replaced by `Sc-catalog` (data-defined distinct types + extensible
  per-type laws + reactions + handler-fix; ENGINE-ONLY), status active.
- NOW.md open_calls: `c-exec-033` entry REMOVED (superseded, accidental) → NEW `c-exec-034` (Sc-catalog) READY. c-visual-007
  + the g-d3a8 design-lab track + decision d-ono-dyshit-core-frame-fork-001 PRESERVED untouched.
- NOW.md next → CALL c-exec-034 (Sc-catalog); c-exec-033 superseded.
- work/: c-exec-034-sc-catalog-call.md created (hardened, id updated, supersede note); c-exec-033-sc-typing-call.md bannered
  SUPERSEDED (env-typing design preserved for the parked later slice); c-exec-032 draft removed (renumbered → 034).
- LOG.md: session line appended.
- knowledge/: new g9c41-sc-catalog-reframe.md (drift-guard).

## captures
- Two-writer-session collision recurred: s-work-sc-typing-call-001 framed c-exec-033 (env-typing) while THIS session was
  shaping Sc-catalog; proximate cause = owner's accidental wrong-chat launch. Serialize g-9c41 writer sessions.

## decisions_needed
none (owner adjudicated the collision + the reframe in-session).

## play_check
- OPEN / orient — done: opening-contract header + shape step plan + restate; job = shape the next g-9c41 slice + assemble CALL.
- shape 1–6 (recite / appetite / approaches / scope-hammer / lens / riskiest) — done: recited; appetite under the g-9c41 de-risk
  wall; approaches grounded in real code; cut list (env-mechanism / visual / mod / DLC / procedural / new-physics parked, ≥1 real
  cut); lens sweep; riskiest = the extensible-param ContentHash fold (the leg-1 falsifier battery).
- owner co-creation (G9) — done: two fork rounds; owner reframed to Sc-catalog and pruned scope in his own words.
- code grounding + CALL hardening — done: wf_2e0ee72b-3fc + wf_e9971bbd-6a8; findings folded; contract state verified first-hand.
- reconcile vs committed HEAD (drift) — done: found the accidental c-exec-033 framing at HEAD (committed by a parallel session);
  surfaced the conflict to the owner; owner adjudicated (error → superseded); applied a surgical revert preserving the g-d3a8 track.
- close → RESULT — done: Sc-catalog active, c-exec-034 ready, next = c-exec-034.

## log
2026-07-08 — shape/re-scope (g-9c41/Sc-catalog, s-shape-sc-catalog-001): owner reframed the post-Sc-reactions slice from
env-derived typing to a data-defined type-authoring KIT (extensible per-type laws + reactions between types + handler-into-tick
fix; engine-only; design-aware on env/visual/mod/DLC/procedural); env-typing parked; accidental parallel c-exec-033 superseded
per owner; executor CALL c-exec-034 code-grounded + adversarially hardened (3 blockers folded; contract verified synced-v18) and
ready; Sc-catalog active, next = c-exec-034.

## next
CALL c-exec-034 → executor (GasCoopGame_dev): work/c-exec-034-sc-catalog-call.md. Fresh owner-present PLAN (contract v19
PLAN/BUILD split), independent RED test-author first. Return RESULT HOME; dev→main merge + push owner-gated. c-exec-033
(env-derived typing) parked/superseded — revive as a later slice if/when env-typing returns. Parallel tracks unchanged
(c-visual-007 Stage 3; g-d3a8 ono-dyshit fork awaiting d-ono-dyshit-core-frame-fork-001).

END_OF_FILE: live/indie-game-development/history/2026-07-08-s-shape-sc-catalog-001.md
