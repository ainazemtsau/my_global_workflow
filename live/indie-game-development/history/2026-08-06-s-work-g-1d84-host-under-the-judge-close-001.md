# RESULT — s-work-g-1d84-host-under-the-judge-close-001

call: c-work-g-1d84-host-under-the-judge-binding-g5-001
direction: indie-game-development
track: хозяин
play: work
node/task: g-1d84 / t-host-1
date: 2026-08-06
owner_word: «Я его видел всё одинаково на двух экранах и тестировал на двух копиях» · «Так, ну
закрывай задачу, то есть то, что ты написал, из-за чего не закрыта, я это тестил, это всё работает».
close: light — because все четыре строки `done_when` теперь имеют доказательство своего рода. Строки
1-2 эта нога и её предшественница перемеряли первой рукой по байтам опубликованного `d60c2f04`
(файл, место, отсутствие второго места симуляции, форма снимка и один канал). Строки 3-4 закрыты
ДОСЛОВНЫМИ словами владельца об этом самом артефакте, сказанными в ответ на прямо заданный вопрос и
закоммиченными этой ногой. Суждения, которого никто не выносил, не осталось.
downgrade: выпущенная накануне свежая биндинговая проверка
`c-work-g-1d84-host-under-the-judge-binding-g5-001` снята с фронтира и заменена светлым закрытием.
Основание — `os/plays/work.md` §Light close: «An already-issued fresh-G5 CALL may be replaced by a
light close when the same test holds and the RESULT records the downgrade». Тест теперь держит по
всем четырём строкам, и понижение записано здесь. Вопрос, ради которого проверка выпускалась —
смотрел ли владелец на двух копиях, — он закрыл сам, а перемеривать байты во второй раз незачем.

## outcome

**`t-host-1` закрыта. Хозяин перестал быть выдумкой каждого отдельного экрана**: его день считает
создатель игры в том же тике, где идут игроки и груз, его положение и занятие едут в том же снимке
по тому же каналу, и обе копии рисуют его из этого снимка, ничего не вычисляя сами.

Разрыв, найденный предыдущей ногой, разрешён так, как и должен был: у владельца **спросили**, он
ответил, ответ записан дословно. Не выведен из шагов приёмки.

**Полоса «хозяин» встала, и это надо знать при выборе следующей работы.** Все восемь оставшихся задач
полосы заблокированы по цепочке: `t-host-2` ждёт `t-house-4` (полоса «дом», порядок 9, наряда нет),
`t-host-8` ждёт `t-host-2`, а `t-host-3` ждёт решения владельца по форме файла профилей
(`d-profile-file-format-recommendation-002`) и держит за собой `t-host-4`, `t-host-5`, `t-host-6`;
`t-host-7` ждёт `t-house-3`, `t-host-9` — `t-host-8`. Свободный слот полосы эта нога не занимает и
чужую полосу не выбирает.

## evidence

**Строки 3 и 4 — дословные слова владельца, 2026-08-06, в ответ на прямо заданный вопрос.**

> Я его видел всё одинаково на двух экранах и тестировал на двух копиях. Так, ну закрывай задачу, то
> есть то, что ты написал, из-за чего не закрыта, я это тестил, это всё работает.

Сила ровно такая, какая нужна, и не больше: он назвал ОБА факта, которых не хватало, — «на двух
экранах» (строка 4) и «тестировал на двух копиях» + «это всё работает» (строка 3), и прямо привязал
это к тому, что было названо причиной незакрытия. Пересказом это не заменялось и не выводилось.

**Строки 1 и 2 — байты опубликованного `d60c2f04`, перемерены первой рукой.**

- `Assets/TunnelCrew/Core/Multiplayer/CarryWorld.cs`: `Householder.Advance(tickSeconds,
  householderWorld)` стоит внутри `Advance(...)` между `Cargo.Advance(...)` и `body?.Step(...)` —
  тот же вызов, где идут ходоки и груз; порядок трёх шагов перевозки не тронут.
- `Assets/TunnelCrew/Network/NetworkWalkerCourier.cs`: `OnServerTick` (122) → `_world.Advance(...)`
  (128), шестым аргументом `_householder` (134). Считает сервер.
- `Assets/TunnelCrew/World/HouseholderRouteController.cs`: слов `Update` и `Step` нет нигде, кроме
  комментария о том, как было раньше — второго места симуляции не осталось.
- `Assets/TunnelCrew/Network/HouseholderSnapshot.cs`: `IsPresent`, `X`, `Z`, `Occupation`; едет
  третьим параметром существующего `ReceiveSnapshotsObserversRpc` (206-211) → `ApplySnapshots`
  (214-217). Второго RPC, второго канала и второго тика нет.

**Публикация.** `main` = `origin/main` = `origin/dev` = `d60c2f04`. Цепочка задачи: `2eee9719` →
`53430228` → `0b90a99f` → `4a514602` → `c50ca549` → `fdfb7e59` → слияние `c9b35650` → `032ebb17`
(`DELIVERED on dev`). Слот `WIN-U1` `AVAILABLE`, `lease: none`.

**Что НЕ входит в это закрытие и осталось открытым — названо, а не спрятано.** Владелец тестировал
байты `4a514602`; опубликованный `IntegratedHouse.unity` на `d60c2f04` другой (`175682d5` →
`9ada2ac9`). Сцену изменило слияние ПАНЕЛИ УСЛОВИЙ (`69c47a08`, общий stripped-трансформ
`&1607884504`), а не работа хозяина: семь путей кода хозяина побайтово те же, что он запускал. Поэтому
риск слитой сцены — не дефект `t-host-1` и не повод её не закрывать; он живёт в
`i-published-to-main-before-any-direction-close-001` и в полосе, которой эта сцена принадлежит.

## state_changes

Применено этой же ногой.

1. `NOW.md` заголовок: `updated: 2026-08-06 by s-work-g-1d84-host-under-the-judge-close-001`.
2. `NOW.tasks` `t-host-1`: `status: open` → `done`; `note` снята и заменена полем `closed` с разбором
   маршрута, словами владельца, записью понижения и явной оговоркой про слитую сцену.
3. `NOW.open_calls`: удалён `c-work-g-1d84-host-under-the-judge-binding-g5-001` (заменён светлым
   закрытием; файл наряда в `work/` сохранён как есть, не удалён и не переписан).
4. `NOW.issues` `i-published-to-main-before-any-direction-close-001`: исправлена одна фраза, ставшая
   неверной («биндинговой проверки у неё нет ни одной»), — теперь запись говорит точно, что `t-host-1`
   прошла тем же путём четвёртой и сутки лежала в `main` без вердикта. Суть записи и её маршрут
   (`review`, решает владелец) не изменены.
5. `LOG.md` — одна строка сверху.
6. `history/2026-08-06-s-work-g-1d84-host-under-the-judge-close-001.md` — этот RESULT целиком.

Не тронуто: `CHARTER.md`, `TREE.md`, `knowledge/**`, все остальные задачи, наряды, полосы и записи;
продуктовый репозиторий (ни одного write). Ничто не разблокировалось: ни одна задача не ждала
`t-host-1`.

## captures

- Полоса «хозяин» свободна, но её следующая задача (`t-host-2`, путь по названным местам через
  названные проходы — там же чинится хождение сквозь стены) стоит за `t-house-4` из чужой полосы.
  Кто и когда займёт освободившийся слот WIP — выбор владельца, не ноги.

## decisions_needed

Нет.

## play_check

- 1 Recite: done — `t-host-1` полосы «хозяин» под ставкой `g-1d84`; возвращается
  `c-work-g-1d84-host-under-the-judge-binding-g5-001`, выпущенный ради строк 3-4.
- 2 Owner inputs (owner): done — вопрос был ровно один и задан прямо; его слова дословно: «Я его видел
  всё одинаково на двух экранах и тестировал на двух копиях» и «то, что ты написал, из-за чего не
  закрыта, я это тестил, это всё работает» + «закрывай задачу».
- 3 Do the work: done — тест светлого закрытия перепрогнан по всем четырём строкам, задача закрыта,
  выпущенная проверка понижена с записью основания.
- 4 Self-check: done — четыре строки `done_when` задачи сверены поштучно (две байтами, две его
  словами); пятое условие закрытия — что не осталось суждения, которого никто не выносил — проверено
  отдельно и держится.
- 5 Close: done — `t-host-1` `done`, наряд снят с фронтира, продолжения полоса не получает, потому что
  все её задачи заблокированы; соседние полосы, наряды и записи сохранены.

log: g-1d84/t-host-1: хозяин под судьёй ЗАКРЫТ светлым закрытием на `d60c2f04` — две недостающие
строки закрыты дословными словами владельца про два экрана и две копии, выданная биндинговая проверка
понижена с записью основания, полоса «хозяин» встала за `t-house-4`.

next: return-to-owner — полоса «хозяин» продолжения не имеет (все восемь оставшихся задач
заблокированы), свободный слот WIP занимает владелец.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-host-under-the-judge-close-001.md
