# RESULT — s-map-g-5a7c-ui-line-removal-001

direction: indie-game-development
play: map
node: g-5a7c
call: c-map-g-5a7c-ui-line-removal-001
date: 2026-08-07
base: TREE/NOW/LOG и `work/converge-g-5a7c.md` прочитаны на `1fb3a9aa` (рабочее дерево чистое); правки записаны поверх него.

## outcome

Карточка `g-5a7c` больше не запрещает игровой интерфейс, а её критерий 4 говорит про шум ровно то,
что сказал владелец. Три места `TREE.md` изменены и каждое подписано отдельным его словом на текст,
показанный ЦЕЛИКОМ до правки:

1. **Критерий 8** потерял последнее предложение и получил новое.
2. **Строка исключений** потеряла «игровой интерфейс; » и получила половину фразы про спавн и
   прямой ход.
3. **Критерий 4** переписан целиком.

Ничего сверх этих трёх мест не тронуто. Ставка не открыта, задач и полос нет, продуктовый
репозиторий не тронут.

## Точное до/после

### Место 1 — критерий 8 «ЦЕПЬ НАБЛЮДАЕМА», последнее предложение

БЫЛО: `Это ИНСТРУМЕНТ, а не игровой интерфейс: игрового интерфейса здесь нет вовсе.`

СТАЛО: `Это инструмент разработчика, а не часть игры для игрока.`

Остальная часть критерия 8 (первое предложение про инструмент, цепь и команды форсирования со
ссылкой на его спеку §15, §16) не изменена ни одним знаком.

### Место 2 — строка исключений

БЫЛО: `Здесь НЕ появляются: игровой интерфейс; телевизор и любой крупный груз; кабель, повреждение
и уничтожение груза; тоннели, сеть доступа и копание — своя отдельная ставка ПОСЛЕ этого узла;
прокачка, магазин, бросок; …`

СТАЛО: `Здесь НЕ появляются: телевизор и любой крупный груз; кабель, повреждение и уничтожение
груза; тоннели, сеть доступа и копание — своя отдельная ставка ПОСЛЕ этого узла (точка спавна
снаружи и прямой ход в дом — геометрия, а не тоннель, и этой ставки не открывают); прокачка,
магазин, бросок; …`

Остальные восемнадцать позиций строки исключений не тронуты.

### Место 3 — критерий 4, целиком

БЫЛО: `4. ТИШИНА — ЭТО ВЫБОР КОМАНДЫ. Волочение тяжёлого в одиночку шумит, чистая переноска
несколькими — нет, и хозяин отвечает на это по-разному. «Позвать ещё одного» становится решением
про риск, а не только про скорость.`

СТАЛО: `4. ШУМ — ЭТО ВЫБОР КОМАНДЫ. Шум рождается одним общим слоем, у которого несколько
источников: контакт груза с домом — первый, дальше добавляются другие, не переписывая слой.
Бесшумно не бывает: волочение тяжёлого в одиночку громче, чем перенос того же груза несколькими, и
хозяин отвечает на разную громкость по-разному. «Позвать ещё одного» становится решением про риск,
а не только про скорость.`

## owner_approved

**ТРИ ОТДЕЛЬНЫХ ТОКЕНА НА ТРИ ТЕКСТА. ЭТО НЕ ОДНА ПОДПИСЬ НА ТРИ И НИКОГДА ТАК НЕ ЦИТИРУЕТСЯ.**
Все три текста были показаны ему в чате целиком ДО любой правки, в одном сообщении, разделённые на
три пронумерованных места, и он ответил тремя пронумерованными словами:

- **`1 принимаю`** — на текст места 1 и места 2 (снятие запрета на игровой интерфейс). Один токен
  на два места, потому что они были показаны как одна правка «снять запрет» и пронумерованы одной
  цифрой; выше этой силы он не цитируется.
- **`2 принимаю`** — на полный текст критерия 4, приведённый выше знак в знак.
- **`3 А`** — выбор варианта А (добавить половину фразы про спавн и прямой ход в строку исключений)
  против показанного наравне варианта Б (не добавлять, разбора `converge` достаточно).

**ЧТО ЯВЛЯЕТСЯ ЕГО СЛОВОМ, А ЧТО — ПРОЧТЕНИЕМ СЕССИИ.** Ни одна строка нового текста не приписана
ему без основания, и вот поимённо, чем каждая обязана его словам:

| Строка нового текста | Его слова |
|---|---|
| Снятие запрета на игровой интерфейс | «Это надо убрать это требование, игрового интерфейса нет вовсе, что за бред? Откуда оно вообще появилось? Это надо убрать… И если там надо какой-то интерфейс во время игры, то пожалуйста, можно добавлять» (2026-08-07) |
| Шум как ОБЩИЙ расширяемый слой с несколькими источниками | «звук, он же не только от лута, от переноски… нам нужен какой-то общий движок, да, то есть под это, который мы сможем расширять» (2026-08-07) |
| «Бесшумно не бывает» | «бесшумно нельзя» |
| Требование про число носильщиков СОХРАНЕНО | «Б Ну да, надо, чтобы осталось, но с моим комментарием» (2026-08-07) |
| Спавн снаружи и прямой ход — геометрия, а не тоннель | «надо будет просто сделать где-то условно вне дома точку спавна, где все спавнятся вот на своих местах… это не как реализация туннеля, просто что спавн за домом, и вот некий туннель туда можно забегать» (2026-08-07) |

**ПРОЧТЕНИЯ СЕССИИ, НАЗВАННЫЕ ЕМУ ПРЯМО ДО ВЕРДИКТА И ПОТОМУ ЗАКОННЫЕ, НО НЕ ЕГО СЛОВА:**
(а) заголовок критерия сменён с «ТИШИНА» на «ШУМ», и ему было сказано, что сохранить прежнее слово —
его выбор; (б) «контакт груза с домом — первый источник» названо в тексте критерия явно;
(в) «чистая переноска несколькими — нет» заменено на сравнение «громче/тише», потому что прежний
оборот прямо спорил с его «бесшумно нельзя».

**ЧТО ПОДПИСЬЮ НЕ ЯВЛЯЕТСЯ И ЗАПИСАНО ИМЕННО ТАК.** Его прежнее «Б Ну да, надо, чтобы осталось, но
с моим комментарием» — вердикт по ВАРИАНТУ, а не подпись под текстом критерия 4: точный текст ему
тогда не показывался. Именно поэтому текст был показан целиком заново и получил отдельное слово
`2 принимаю`.

## evidence

1. **Правка на месте.** `TREE.md`, карточка `g-5a7c`: критерий 4 (текст выше), критерий 8 (текст
   выше), строка исключений (текст выше). Реестр `owner_approved` в шапке `TREE.md` получил новую
   запись с тремя токенами и их точной силой.
2. **Запрета на интерфейс в карточке не осталось нигде.** Перепроверено первой рукой по всему
   диапазону карточки (`TREE.md`, узел `g-5a7c` целиком) поиском на «интерфейс | UI | экран»: два
   носителя было, оба сняты. Оговорка критерия 10 «поставленный игровой режим третьего лица не
   заказывается» — другое требование, снятием запрета не затронута и не тронута.
3. **Откуда взялся снятый запрет — ответ на его собственный вопрос, проверенный первой рукой.**
   Из фразы «без UI», приписанной ему ногой `s-map-real-core-node-001`. Единственный носитель этой
   фразы на диске — `history/2026-08-07-s-map-real-core-node-001.md:58` (таблица §evidence, строка
   «Отсутствие интерфейса (кр. 8, исключения)»), плюс производная строка реестра `TREE.md:4`.
   Независимого следа разговора нет. Согласуется с разбором `work/converge-g-5a7c.md` строки W111 и
   §IMPORT I33 — обе прочитаны этой ногой, а не приняты на веру.
4. **Лобби этим запретом никогда не покрывалось.** `knowledge/lobby-is-not-a-crutch.md:20-40`:
   «Дефектом была ВРЕМЯНКА, а не наличие интерфейса подключения»; его собственное подтверждение в
   том же ответе — «Ну лобби, естественно, да, должно быть».
5. **Основание для критерия 4 — его слова, а не вывод ноги.** `work/converge-g-5a7c.md` W34 (его
   ответ про общий движок и снятие неоднозначности буквы), W33 (измерение: механики «переноска
   несколькими тише» сегодня не существует вообще — `AuthoritativeCargoRoster.NoiseOf` получает
   только силу контакта и координаты, список держащих в расчёт шума не входит), §IMPORT I14
   («бесшумно нельзя»).
6. **Ничего сверх названного не тронуто.** Цель узла, критерии 1, 2, 3, 5, 6, 7, 9, 10, 11, 12,
   `why`, `edge`, `risk`, `status: parked`, `detail` — без изменений; другие узлы дерева —
   без изменений. `bet: null`, `tasks: []`, полос нет — без изменений.
7. **Поправка к ложному утверждению реестра, сделанная только внутри собственного текста.**
   Реестр `TREE.md:4` утверждает, что правила на число открытых детей узла в OS нет. Это ЛОЖНО:
   правило есть — `os/schema/direction-files.md:57`, дословно `Width ≤7 open children per node`.
   Перепроверено первой рукой. Нарушения нет — открытых детей `g-0c26` ровно семь. Новый текст
   реестра ложное утверждение не повторяет и называет его ложным; полная починка записи (она уехала
   также в `NOW.md` и `LOG.md`) принадлежит отдельной ноге `repair` и здесь НЕ сделана — прямая
   граница наряда.

## Заметка, названная ему до вердикта и правкой не ставшая

В цели узла стоит «одному тяжело и шумно, вчетвером легко и тихо». После правки критерия 4 «тихо»
там читается как «тише», а не «беззвучно». Противоречия нет, но расхождение названо ему прямо
до вердикта, и правку под него сессия НЕ предлагала: цель он менять не просил, а границы наряда её
прямо запрещают трогать. Если он захочет подровнять цель — это его слово и отдельный ход.

## state_changes

1. **`TREE.md`, узел `g-5a7c`, `done_when`, критерий 4** — заменён целиком на текст «Место 3 →
   СТАЛО» выше.
2. **`TREE.md`, узел `g-5a7c`, `done_when`, критерий 8** — последнее предложение заменено на
   «Это инструмент разработчика, а не часть игры для игрока.»; остальная часть критерия сохранена
   без изменений.
3. **`TREE.md`, узел `g-5a7c`, строка исключений** — удалено «игровой интерфейс; »; после
   «тоннели, сеть доступа и копание — своя отдельная ставка ПОСЛЕ этого узла» добавлено
   «(точка спавна снаружи и прямой ход в дом — геометрия, а не тоннель, и этой ставки не
   открывают)»; остальные позиции сохранены без изменений.
4. **`TREE.md`, `owner_approved`** — в конец реестра добавлена запись о трёх токенах этой ноги с их
   точной силой, происхождением снятого запрета и поправкой к утверждению про ширину дерева.
5. **`NOW.md`** — `updated:` переведён на `s-map-g-5a7c-ui-line-removal-001`; возвращающийся
   `open_calls` id `c-map-g-5a7c-ui-line-removal-001` снят; добавлен
   `c-converge-arch-g-5a7c-001` (`status: ready`, `to: session`, `for: g-5a7c`, `issued: 2026-08-07`,
   `call: work/c-converge-arch-g-5a7c-001-call.md`); под `open_calls` добавлен комментарий о
   выполненном наряде и о нечиненой записи про ширину дерева. `issues`, `decisions`, `bet`, `tasks`,
   `direction_forecast`, `recurring` сохранены без изменений.
6. **`work/c-converge-arch-g-5a7c-001-call.md`** — создан целиком.
7. **`LOG.md`** — одна строка добавлена сверху (журнал ведётся новыми записями вверх).
8. **`history/2026-08-07-s-map-g-5a7c-ui-line-removal-001.md`** — этот файл.

## captures

- Ложное утверждение «правила на число открытых детей узла в OS нет» живёт в трёх местах —
  `TREE.md` (внутри текста этой ноги поправлено), `NOW.md` и `LOG.md`. Правило есть:
  `os/schema/direction-files.md:57`. Нарушения нет, детей ровно семь; владелец потратил слово
  «разрешаю обойти правило на ограничение узлов» на обход правила, которое ему объявили
  несуществующим. Маршрут — `repair`, отдельной ногой; маршрут узла это не блокирует.
- Цель узла `g-5a7c` содержит «вчетвером легко и тихо». После правки критерия 4 это читается как
  «тише». Не дефект и не требование — вход для будущей `map`-ноги, если владелец захочет.

## decisions_needed

Нет. Все три вопроса этой ноги закрыты его словами в этой же сессии.

## play_check

- 1 Recite: done — состояние карточки и обе правки пересказаны владельцу простыми словами до любого
  текста.
- 2 Candidates & evidence (owner): skipped — карта не пересобирается; правка идёт по его прямому
  приказу 2026-08-07 («Это надо убрать это требование»), а свежая улика уже существует и прочитана
  первой рукой: `work/converge-g-5a7c.md` (2026-08-07, шесть независимых добытчиков плюс критик
  полноты) плюс `history/2026-08-07-s-map-real-core-node-001.md:58` как единственный носитель
  снимаемой фразы. Новый `map_evidence` не заводился, потому что вопрос ноги — текст одной уже
  принятой карточки, а не состав карты.
- 3 Skeleton (owner): skipped — карта не меняется: ни узла не добавлено, ни снято, ни переставлено.
  Показывать нечего.
- 4 Cards: done — три точных заменяющих текста подготовлены и показаны целиком ДО правки.
- 5 Per-node verdict (owner): done — **`1 принимаю`**, **`2 принимаю`**, **`3 А`**. Три слова на
  три текста; заново открывать закрытое им «Б Ну да, надо, чтобы осталось» сессия не стала и
  записала его силу как вердикт по варианту, а не как подпись.
- 6 Order (owner): skipped — порядок узлов не затронут: узел был и остался `parked`, соседи не
  тронуты.
- 7 Depth check: done — глубина не изменена, детей у `g-5a7c` по-прежнему нет.
- 8 Lens sweep: skipped — линз устава касается состав карты, а он не менялся; разбор линз по этому
  узлу уже сделан `converge` (`work/converge-g-5a7c.md` W105, его слово «пункт 8, да, согласно
  твоей рекомендации»).
- 9 Close (owner): done — G9 пройден тремя его словами; TREE и LOG сохранены; следующий узел не
  предлагается, потому что маршрут этого узла уже определён триажем `converge` (`heavy`) и продолжен
  единственным фронтиром `c-converge-arch-g-5a7c-001`.

## log

**КАРТОЧКА `g-5a7c` ПРАВЛЕНА В ТРЁХ МЕСТАХ, ТРИ ОТДЕЛЬНЫХ ВЕРДИКТА ВЛАДЕЛЬЦА НА ТРИ ПОКАЗАННЫХ
ЦЕЛИКОМ ТЕКСТА.** `1 принимаю` — запрет на игровой интерфейс снят из критерия 8 и из строки
исключений. `2 принимаю` — критерий 4 переписан под общий расширяемый слой шума с сохранённым
требованием про число носильщиков. `3 А` — строка исключений получила половину фразы про спавн
снаружи и прямой ход как ГЕОМЕТРИЮ, а не тоннель. Источник снятого запрета найден и назван честно:
фраза «без UI», приписанная ему прошлой map-ногой, независимого следа на диске нет. Выпущен
единственный фронтир `c-converge-arch-g-5a7c-001`.

## next

Один локальный фронтир, зарегистрированный этими же `state_changes`: **`c-converge-arch-g-5a7c-001`**
(`to: session`, `play: converge-arch`, `node: g-5a7c`, `status: ready`). Дальше по маршруту — свежая
`converge-verify`, и только потом `shape`.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-map-g-5a7c-ui-line-removal-001.md
