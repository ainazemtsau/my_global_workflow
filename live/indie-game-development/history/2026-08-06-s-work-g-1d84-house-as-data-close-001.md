# RESULT — s-work-g-1d84-house-as-data-close-001

call: c-exec-g-1d84-house-as-data-001 (возврат исполнителя, HANDBACK)
direction: indie-game-development
track: дом
play: work
node: g-1d84
task: t-house-4
close: light — because четыре из пяти строк `done_when` Направление перемерило первой рукой по
  закоммиченным байтам на `8ffe5a5d`, не опираясь на отчёт исполнителя, а пятая закрыта дословным
  вердиктом владельца «закрываем», полученным в этом чате после того, как ему были показаны оба
  возможных прочтения спорной строки и измеренная цена каждого.

## outcome

Задача `t-house-4` закрыта. Дом перестал быть коллайдерами и стал данными слоя правил: у комнат и мест
есть смысловые метки, описание дома — отдельная названная вещь слоя правил с двумя живыми
производителями, проходы существуют отдельно от вложенности, и владелец видел список, снятый с
настоящей сцены. Вместе с ней доехали и закрылись два решения владельца, дважды не поехавшие прежде.

Полоса `дом` идёт дальше: `t-host-2` разблокирована. Полоса `тоннель` снята с паузы. Три ждущих корня
перебиты на новую базу.

## evidence

**Все измерения сделаны на опубликованном `main` = `origin/main` = `origin/dev` = `8ffe5a5d`; тройное
равенство перемерено первой рукой. Объявленная база наряда `d60c2f04` подтверждена как верная.**

1. **Строка 1 — смысловые метки.** `HouseRoomPurpose` (Unknown/Hall/Living/Sleeping/Working/Storage) и
   `HousePlacePurpose` (Unknown/Doorway/Rest/Work/Storage/Watch) объявлены в слое правил. Числа
   `_purpose` вынуты из закоммиченного `Assets/TunnelCrew/World/ThreeRoomHouse.prefab` — комнаты в
   строках 1123/1170/1219 (3/1/2), места в строках 1265/1310/1355/1400/1445/1490/1535 (2/5/1/5/3/4/1) —
   и разложены обратно в имена. **Совпало со списком владельца знак в знак:** West Room = спальня,
   Middle Room = прихожая, East Room = жилая; Bedside = отдых, West Window = наблюдение,
   West Door Side = проём, Hall Centre = наблюдение, Work Table = работа, Store Corner = хранение,
   East Door Side = проём. Это самый сильный факт ноги: список с экрана владельца независимо
   восстановлен Направлением из байтов, а не принят со слов исполнителя.

2. **Строка 2 — отдельная названная вещь слоя правил, производителей может быть больше одного.**
   `Assets/TunnelCrew/Core/House/` содержит пять файлов: `HousePlan.cs`, `HouseRoom.cs`,
   `HousePlace.cs`, `HousePassage.cs`, `IHousePlanSource.cs`. `Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef`
   прочитан целиком: `"references": []` и `"noEngineReferences": true` — движка в слое правил нет **по
   построению, а не по заявлению**; коллайдер туда не компилируется. Производителей ДВА ЖИВЫХ:
   `Assets/TunnelCrew/World/SceneHousePlan.cs:27` (`MonoBehaviour, IHousePlanSource` — снятие со сцены,
   первый) и `tests/TunnelCrew.Core.Tests/HouseAsDataTests.cs:36` `HandDrawnHouse` — рукописный дом
   вообще без сцены. Требование «описание переживёт замену коробки» выполнено вторым РАБОТАЮЩИМ
   производителем, а не обещанием шва.

3. **Строка 3 — проходы отдельно от вложенности.** Вложенность — поле `HousePlace.RoomName`.
   Проходимость — отдельный тип `HousePassage` с собственным именем, двумя концами-местами, точкой и
   шириной, плюс `Joins`/`OtherEnd` (проход ненаправленный: дверь на восток и она же на запад). В
   заготовке два прохода, оба `_width: 2` (строки 1582, 1629). Две РАЗНЫЕ вещи, а не два поля одной.

4. **Строка 4 — «правила видят дом списком, а не коллайдерами» — ЗДЕСЬ РЕШИЛ ВЛАДЕЛЕЦ, НЕ НОГА.**
   Измеренный факт: у `Read()` во всём проекте три вызова и **ни одного в работающей игре** —
   `Assets/TunnelCrew/Editor/SceneHousePlanEditor.cs:29` (кнопка редактора),
   `tests/TunnelCrew.Core.Tests/HouseAsDataTests.cs:226` (тест) и
   `Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs:112` — последний это `world.Read()`
   ДРУГОГО шва `IHouseholderWorldPort`, к дому отношения не имеющий. Отдельно проверено, что ни
   `IWalkSpacePort.Resolve`, ни `IHouseholderWorldPort.Read` никогда не передавали правилам
   коллайдерное описание дома: первый отвечает «можно ли туда» по одному шагу, второй отдаёт точки
   маршрута. То есть до этой ноги у правил не было НИКАКОГО описания дома, а не коллайдерное.
   Строка допускает два прочтения — «у правил ЕСТЬ описание дома, и оно список» против «в запущенной
   игре правила его СПРАШИВАЮТ». Оба были показаны владельцу целиком, с ценой каждого и с прямым
   указанием, что исполнитель сознательно не написал ~10 строк проводки в `NetworkWalkerCourier`, чтобы
   не класть мёртвый код в файл соседней полосы. **Владелец выбрал первое словом «закрываем»**
   (2026-08-06, в ответ на прямо заданный вопрос). Довод, названный ему до ответа: карточка сама
   назвала, чем эта строка проверяется — списком, снятым с настоящей сцены, а не игрой; потребитель
   куплен строкой `t-host-2` дословно «путь хозяина считается в слое правил по названным местам дома и
   названным проходам между ними». **До его слова строка была открытой, и нога её сама не закрывала.**

5. **Строка 5 — глаз владельца.** Его слова исполнителю 2026-08-06 закоммичены в
   `8ffe5a5d:docs/results/c-exec-g-1d84-house-as-data-001.md:25,27`: «скрипты скомпилировались, ошибок
   нет. Сцена Integrated House запускается» и «вижу список, 3 комнаты 7 мест 2 прохода, гизмо тоже
   вижу»; вторым сообщением он прислал список, снятый кнопкой «Copy this list» со своего экрана.
   **Записано честно: эти слова пришли Направлению ЧЕРЕЗ ОТЧЁТ ИСПОЛНИТЕЛЯ, а не от него напрямую** —
   поэтому у владельца был отдельно спрошен собственный вердикт, и закрытие стоит на его «закрываем», а
   не на пересказе. Это ровно тот случай, который правило «derivable ≠ signed» и требует различать.

6. **Размер и чистота доставки.** `d60c2f04..8ffe5a5d` = 26 файлов, из них **ровно два изменения**
   (`ThreeRoomHouse.prefab` — маркеры; `ProjectSettings/UnityConnectSettings.asset` — откат), остальные
   24 — добавления. Байт-идентичны прежней базе: `IntegratedHouse.unity` (`9ada2ac9`),
   `Householder.prefab` (`df5c93b0`), `HouseholderRouteControllerEditor.cs` (`832c6aa8`),
   `AuthoritativeCargoRoster.cs` (`9711121b`). Соседние полосы не задеты ни одним байтом.

7. **Хвост 1 — облачные сервисы Unity.** `8ffe5a5d:ProjectSettings/UnityConnectSettings.asset:7` =
   `m_Enabled: 0`. Прочитано первой рукой. Решение владельца от 2026-08-05 («откатываем») исполнено.

8. **Хвост 2 — мёртвый корень.** `8ffe5a5d:docs/measurements/root-receipts/c-exec-grid-v1-g01-document-authority-001/02-closed-by-owner.json`
   прочитан целиком: `state: RELEASED`, `verdict: CLOSED_BY_OWNER_WITHOUT_IMPLEMENTATION`,
   `eligibility: NONE_ROOT_IS_TERMINAL`, все четыре разрешения жизненного цикла `false`,
   `next_lawful_action: "none: RELEASED is terminal"`. Поле `not_a_replacement` записано прямо:
   преемника нет, переносить нечего, замороженные PLAN/proposal/tasks/spec и ADR-P-0015 оставлены
   нетронутыми — корень лишён права работать, но ничего не удалено.

9. **Вход `t-host-2` перепроверен по координатам.** Пять названных мест стоят РОВНО на нынешних точках
   хозяина: Bedside (−3.5;−2.5) = РТ01, Work Table (3.5;−2.5) = РТ02, Store Corner (5.11;5.58) = РТ03,
   West Window (−3.5;2.5) = РТ04, Hall Centre (0;0) = `Listening Point`
   (`m_LocalPosition {x: 0, y: 1, z: 0}`, `Householder.prefab` строка 340). Сверено против собственного
   пересчёта Направления в `i-host-walks-through-walls-001`, не против отчёта. Значит починка хождения
   сквозь стены не требует двигать ни одной точки — только водить через `West Doorway` (−2.5;0) и
   `East Doorway` (2.5;0).

10. **Слоты и ветки перемерены полностью.** Аренды: `WIN-U1` `CLAIMED`
    (`c-exec-g-1d84-householder-profile-file-001:BUILD`), U2/U3/U4 `AVAILABLE`, `lease: none` — слот
    U4 освобождён исполнителем корректно. Локальные деревья всех трёх свободных слотов ЧИСТЫ.
    `WIN-U2` HEAD `d60c2f04` — предок `main`, 9 позади. `WIN-U4` HEAD `0fd8ea96` — предок `main`, 2
    позади. **`WIN-U3` HEAD `5752c0da` — РАЗОШЁЛСЯ, 3 коммита впереди `main`**, несёт `f980dce9` (проба
    бесплатных наборов). Ключевое: `f980dce9` — это РОВНО tip серверной ветки
    `origin/probe/free-kits-2026-08-06` (`merge-base --is-ancestor` = да), то есть проба сохранена
    именованной ссылкой и U3 можно ровнять по `main` ничего не теряя. Серверные ссылки слотов: u1
    `adae7d40`, u2 `e4df0459`, u3 `e4eba767`, u4 `7e9556a1` — последняя не двигалась, и это ВЕРНОЕ
    решение исполнителя: push не был бы fast-forward и затёр бы 6 опубликованных коммитов чужого узла.

11. **Ворота исполнителя записаны как ЕГО результат, Направлением не перепрогонялись:** `-Deliver`
    зелёный на интегрированном dev, headless 87/87 (было 73), `root-lifecycle-check.ps1 -Scan` OK.

## state_changes

1. `NOW.tasks[t-house-4].status`: `open` -> `done`; добавлено поле `closed` с построчным разбором всех
   пяти строк, источником каждой и явной пометкой, какая закрыта байтами, а какая словом владельца.
2. `NOW.tasks[t-host-2].status`: `blocked` -> `open`; `unblock_when` заменён на `unblocked`; добавлен
   `note` с тремя входами (проводки нет и это работа задачи; пять совпавших точек; словарь дома
   закреплён за `t-house-4`).
3. `NOW.tasks[t-house-5].status`: `blocked` -> `open`; `unblock_when` заменён на `unblocked` с
   указанием, что условие снято по существу.
4. `NOW.open_calls`: `c-exec-g-1d84-house-as-data-001` снят (закрыт).
5. `NOW.open_calls[c-exec-g-1d84-tunnel-section-001]`: `paused` -> `blocked`; `paused_by` заменён на
   `unpaused` + `unblock_when` (ждёт только имени слота); база и улики обновлены.
6. `NOW.open_calls[c-exec-g-1d84-delivery-point-001]` и `[c-exec-g-1d84-world-noise-contact-001]`:
   база `d60c2f04` -> `8ffe5a5d`, слот-совет `WIN-U2 или WIN-U3` -> `WIN-U2 или WIN-U4`, добавлены
   измерения байт-идентичности их улик.
7. Файлы трёх нарядов в `work/` обновлены теми же правками: база, слот, §context с перемером.
8. `NOW.issues[i-unity-cloud-services-enabled-in-main-001]`: ЗАКРЫТА; `mechanism_failed` заменён на
   `closed` с найденной рабочей формой (явный раздел §Хвосты в теле наряда).
9. `NOW.issues[i-grid-v1-g01-dead-root-open-001]`: ЗАКРЫТА чтением самой квитанции.
10. `NOW.issues[i-slot-u4-remote-tip-carries-foreign-commits-001]`: добавлен полный вечерний перемер;
    прежняя строка «проба — единственный носитель» помечена устаревшей.
11. `NOW.issues[i-host-walks-through-walls-001].review_when`: отмечено, что дом починки открыт.
12. `NOW.issues[i-published-to-main-before-any-direction-close-001]`: добавлен `sixth_case_2026_08_06`
    против собственной ноги — счёт стал шесть из шести.
13. `NOW.issues`: заведена `i-deliver-gate-red-when-main-catches-dev-001` с честной пометкой
    `honest_provenance`, что причинный механизм — заявление исполнителя, не измерение Направления.
14. `LOG.md`: строка ноги. `history/`: этот файл.

Отдельным коммитом `99cfda18` перед работой закоммичено применение предыдущей ноги
`s-shape-g-1d84-core-recut-001`, оставленное её сессией без коммита. Содержимое не менялось.

## captures

- Механизм «решение владельца едет пунктом в первом же наряде» провалился дважды и сработал с третьего
  раза только когда получил СОБСТВЕННЫЙ раздел `§Хвосты` в теле наряда плюс требование «если не
  поедет — назови причину в отчёте прямым текстом». Настоящее место этого правила — `knowledge/` с
  `read_by: написание наряда`; переезд делает отдельная нога вместе с уже ждущим переезда
  `i-owner-standing-rules-on-how-work-is-shaped-001`.
- `HousePassage` знает имя, концы, точку и ширину, но не знает состояния «закрыт/открыт». Ширина при
  этом уже записана и никем не читается — заготовленный шов под «груз шире прохода».

## decisions_needed

Нет. Единственный вердикт этой ноги («закрываем») получен и процитирован.

## play_check

- Шаг 1 «Recite» — выполнен: пять строк `done_when` и обязательство ставки восстановлены дословно из
  `NOW`, не из наряда.
- Шаг 2 «Owner inputs» — выполнен: у владельца спрошено ровно то, что знает только он (его глаз на
  списке) и ровно то, что решает только он (прочтение спорной строки). Один экран, один вопрос.
- Шаг 3 «Do the work» — выполнен как приёмка возврата исполнителя, а не как повтор его работы.
- Шаг 4 «Self-check» — выполнен построчно; каждая строка сверена с названным источником.
- Шаг 5 «Close» — `close: light` по §Light close: четыре строки re-derived первой рукой из
  закоммиченных байтов, пятая — дословные слова владельца, процитированные там, где они живут.
  Отдельная свежая биндинговая сессия НЕ открывалась и это записано открыто, включая то, что буква
  `NOW.bet.evaluator` при этом снова не выполнена (см. `sixth_case_2026_08_06`).
- G5 — соблюдён светлым маршрутом; частичного светлого закрытия не было: спорная строка держала всю
  задачу открытой до слова владельца.
- G7 — соблюдён: нога не решала за владельца ни прочтение строки, ни подпись под его глазом.
- Продуктовый handback сам по себе задачу не закрывал; он входил уликой.

## log

2026-08-06 | s-work-g-1d84-house-as-data-close-001 | work | дом | t-house-4 ЗАКРЫТА `close: light` на
`8ffe5a5d`: четыре строки перемерены Направлением по байтам независимо от отчёта, пятую закрыл личный
вердикт владельца «закрываем». Оба дважды непоехавших решения доехали и закрыты измерением. `t-host-2`
и `t-house-5` разблокированы, базы трёх корней перебиты, слот-совет исправлен на U2/U4.

## next

Ставка идёт дальше. Занят один корень из четырёх (`WIN-U1`, профили хозяина). Свободны `WIN-U2` и
`WIN-U4` — оба чистый fast-forward; `WIN-U3` требует отдельного решения о выравнивании.

Готовы к запуску по одному его слову с именем слота, все три с обновлённой базой: точка доставки
(`t-house-6`), мировой шум от контакта груза (`t-host-7`), тоннельный участок (`t-house-5`).

Без наряда пока `t-host-2` — маршрут хозяина по названным местам. Она же дом починки хождения сквозь
стены, у неё готовый вход (пять совпавших точек) и она же покупает проводку плана в работающую игру.
Наряд под неё пишет Направление отдельной ногой.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-house-as-data-close-001.md
