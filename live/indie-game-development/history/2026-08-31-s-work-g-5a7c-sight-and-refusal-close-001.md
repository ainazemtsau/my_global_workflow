# RESULT — s-work-g-5a7c-sight-and-refusal-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-sight-thing-1 и t-refusal-reason-1, полосы `починка` и `почему`
track: починка
date: 2026-08-31
outcome_kind: work — две ноги закрыты лёгким закрытием по перевыведенным байтам; зрение перестало быть нулём и разблокировало полигон, ради которого волна и открывалась

close: light — because обе задачи закрыты по байтам опубликованного `main` = `064ab568`, прочитанным направлением первой рукой: оба вызова зрения, исключение по личности в луче, состав перечисления исходов, размер и состав дифов. Headless-набор **запущен направлением лично**: 739 из 739. Отчёты исполнителей читались как улики, ни одна строка не закрыта по их заявлению. G5-чат не открывался.

owner_approved: |
  Обе задачи входят в девятую волну, открытую его словом 2026-08-30. Поднятие порога зрения до
  восьми мест и расширение набора слов — операционные решения направления, записанные в
  дополнения нарядов и служащие уже одобренным строкам приёмки.

  Ни одна строка `done_when` не изменена.

outcome: |
  **ЗРЕНИЕ ПЕРЕСТАЛО БЫТЬ НУЛЁМ, И ЭТО ГЛАВНОЕ СОБЫТИЕ ДНЯ.** Прибор печатает
  `HE SEES 2 OF 14 THINGS` против базы `HE SEES 0 OF 14 THINGS`. `Mug` и `Tin can` дают
  `SEES IT (3 of 3 body points clear)` — обе причины, названные направлением, сняты.

  **ОСТАВШИЕСЯ ДВЕНАДЦАТЬ — ПРАВИЛЬНОЕ ПОВЕДЕНИЕ**, и это перемерено, а не принято: дальность
  6.5 м, сектор 100°, хозяин стоит в одной точке. Шесть `TOO FAR`, шесть `BEHIND HIM`. Цель наряда
  звучала «число перестаёт быть нулём», и оно перестало.

  **НАХОДКА РЕЦЕНЗЕНТА СНЯТА ИМЕННО ТАК, КАК ТРЕБОВАЛОСЬ — ПО ЛИЧНОСТИ, А НЕ ПО ГЕОМЕТРИИ.**
  `HouseholderSightSource.cs:346` сравнивает `cargo.CargoId == subjectId`; `subjectId` протянут
  через `TrySee` и `RayIsClear`. Регрессия на мышах закрыта тестом с говорящим именем —
  `RatBehindCargoRemainsOccluded`: мышь за вещью **остаётся невидимой**. Строка приёмки 4
  требовала теста, а не отсутствия опровержения, и тест есть.

  **СЕМЬ ОБРАБОТЧИКОВ ПЕРЕСТАЛИ МОЛЧАТЬ ОДНИМ СЛОВОМ.** Было: единственное безымянное
  `NoProposal` на всех. Стало: девять мест возврата дают достоверную причину, и на них
  понадобилось **ровно четыре новых слова** — `ChoreAlreadyClosed`, `ThingBelievedLost`,
  `ConditionsNotMet`, `ForbiddenFactPresent`. Каждое отвечает правилу «одно слово на один
  различимый отказ» и проходит второй фильтр: прочитав его, владелец знает, что поменять в
  профиле.

  **НАБОР РАСШИРЕН СТРОГО АДДИТИВНО.** Старые значения `0..8` не перенумерованы ни одно; новые
  дописаны `9..12`. Это прямое соблюдение правила «ядро только расширяется» — перенумерация
  сломала бы каждый сериализованный ассет, ссылающийся на исход числом.

  **ДВА ОТКЛОНЕНИЯ ОТ НАРЯДА, ОБА РАЗОБРАНЫ И ОБА ПРИЕМЛЕМЫ.**
  - Нога «почему» тронула `HouseholderProfileLoader.cs` (21 строка), который ей не выдавали.
    Прочитано: это **сквозной проброс через декоратор** `EffectTimingDecisionHandler`. Без него
    каждый обработчик с назначенным временем эффекта продолжал бы докладывать `NoProposal`
    навсегда — то есть цель ноги не достигалась бы вовсе. Правка минимальна и не трогает язык
    профиля. **Уложился в задник ровно: девять файлов и тринадцать членов набора.**
  - Тесты положены в существующий `HouseholderReactionPriorityTests.cs`, хотя наряд просил
    отдельный файл. Столкновения не случилось: соседняя нога работала в
    `HouseholderSightEditModeTests.cs`. Отклонение названо, ущерба нет.

  **СТРОКА 3 ЗАКРЫТА ФАЙЛОМ ОТЧЁТА, А НЕ КОНСОЛЬНОЙ СТРОКОЙ, И ЭТО НАЗВАНО ЧЕСТНО.** Исполнитель
  сам сказал, что фрагмент `why` в его возврате собран боевым форматтером из точного тестового
  ростера, а не прочитан из живого файла, — прямой MCP не смог вызвать закрытый писатель.
  Независимо от этого **нога зрения нашла настоящий дефект показа**: короткая строка консоли
  печатает `why: HE SEES ---`, тогда как полный файл содержит `HE SEES 2 OF 14 THINGS`. Заведена
  улика: **владелец читает отчёт-файл, и он верен; консольная строка врёт.**

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ ПЕРВОЙ РУКОЙ** на `main` = `dev` = `origin/main` = **`064ab568`**:

  Зрение:
  - `NetworkHouseholder.cs` — оба вызова `CanSee` берут высоту у ЦЕЛИ: `:310` и `:325` передают
    `state.Y`. `observer.Y` не участвует ни в одном;
  - `HouseholderSightSource.cs:346` — `cargo.CargoId == subjectId`: исключение **по личности**;
    `subjectId` протянут через `:109`, `:144`, `:169`, `:240`, `:307`, `:450`;
  - `CargoBodyContact.cs` — +3 строки, открытый `CargoId`, ровно то, что требовалось для личности;
  - диф ноги: **четыре файла** (`CargoBodyContact.cs`, `HouseholderSightSource.cs`,
    `NetworkHouseholder.cs`, `HouseholderSightEditModeTests.cs`), восемь логических мест —
    в поднятый порог уложился;
  - из его отчёта, как улика: `HE SEES 0 OF 14` → `HE SEES 2 OF 14`;
    `RatBehindCargoRemainsOccluded` — PASSED.

  Причины отказа:
  - `HouseholderDecisionCandidate.cs` — тринадцать членов; `NotTriggered = 0` … `Won = 8`
    **не изменены**, дописаны `ChoreAlreadyClosed = 9`, `ThingBelievedLost = 10`,
    `ConditionsNotMet = 11`, `ForbiddenFactPresent = 12`;
  - `HouseholderProfileLoader.cs` — `EffectTimingDecisionHandler` реализует
    `IHouseholderDecisionHandlerOutcome` и пробрасывает исход внутреннего обработчика;
  - слова живут в engine-free форматтере, `NetworkWalkerCourier.OutcomeWord` делегирует ему —
    поэтому они покрываются headless-тестами, а не только Unity;
  - диф ноги: девять файлов, 1323 вставки, 46 удалений.

  **Прогон направления:** `dotnet test tests/TunnelCrew.Core.Tests -c Release` на опубликованном
  `main` → **не пройдено 0, пройдено 739, всего 739**. База дня была 725.

  **Склад арта проверен отдельно, потому что нога «почему» пожаловалась на риг:**
  `Victor_Older_BootFix_AutoHide_03.Fbx.meta` — 48 758 Б, **54 кости, 28 переназначений
  материалов**, новых файлов в складе после 08:03 нет. **Склад цел; ошибка была в приватном
  кэше его редактора, а не в общей папке.**

state_changes: |
  ЗАКРЫТО ПО ПЕРЕВЫВЕДЕННОМУ ФАКТУ (4):
  - `t-sight-thing-1` → `done`; `c-exec-g-5a7c-sight-thing-1-001`;
  - `t-refusal-reason-1` → `done`; `c-exec-g-5a7c-refusal-reason-1-001`.

  РАЗБЛОКИРОВАНО (2) — обе ждали ровно зрения, и оно в `main`:
  - `t-carry-back-1` → `open`, полоса `руки`;
  - `t-bench-by-fact-1` → `open`, полоса `починка`. **Это задача, где полигон проходит владелец.**

  СОЗДАНО (1 улика):
  - `i-the-instrument-console-line-lies-while-its-file-tells-the-truth-001` — короткая строка
    консоли печатает `why: HE SEES ---`, полный файл отчёта верен. Маршрут `work`.

  ЗАКРЫТО КАК УМЕРШАЯ (1 улика):
  - `i-a-ray-may-not-see-a-thing-lying-down-001` — её предмет снят этой же ногой зрения, и
    ревизия склада улик отправляла её именно в живой прогон, который теперь состоялся.

captures: |
  - **ТРИ ОСТАНОВКИ ОДНОЙ НОГИ ОКАЗАЛИСЬ ДЕШЕВЛЕ ОДНОГО МОЛЧАЛИВОГО ПРОХОДА.** Нога зрения
    останавливалась трижды: после нулевого замера, на пятом месте правки, и по находке
    рецензента. Каждый раз она приносила числа и не трогала коммит. Итог — восемь мест, четыре
    файла, работающая фича и тест на регрессию. Порог, который «мешает», на деле удержал ногу от
    костыля по геометрии, который прошёл бы замер и сломал мышей.
  - **ИСПОЛНИТЕЛЬ САМ НАЗВАЛ, ЧТО ЕГО УЛИКА НЕ ЖИВАЯ.** Нога «почему» могла молча выдать
    реконструированный фрагмент за прочитанный из игры — отличить было бы нечем. Она сказала.
    Это ровно то поведение, которого требует запрет на суррогаты, и его стоит помнить как
    образец, а не как недостачу.
  - **ЖАЛОБА НА РИГ ОКАЗАЛАСЬ ЛОЖНОЙ ТРЕВОГОЙ, И ПРОВЕРИТЬ ЕЁ СТОИЛО ОДНОЙ КОМАНДЫ.** После
    утренней истории соблазн был поверить сразу. Склад цел; беда была в приватном кэше редактора.
    **Приём: жалоба на общий ресурс проверяется на самом ресурсе, а не по симптому в слоте.**

decisions_needed: |
  Ни одного.

play_check: |
  1. OPEN — два возврата по идущим ногам; обе опубликованы на момент чтения.
  2. WORK — приёмки пройдены построчно; байты опубликованного `main` прочитаны первой рукой;
     headless прогнан направлением; жалоба на склад проверена на самом складе; два отклонения от
     нарядов разобраны поимённо.
  3. (owner) — не требуется: обе задачи в открытой его словом волне, закрытие по факту.
  4. CLOSE — лёгкое закрытие с обоснованием; G5-чат не открывался.

log: |
  зрение и причины отказа закрыты по байтам опубликованного main: хозяин видит 2 из 14 вместо
  нуля, мышь за вещью остаётся невидимой по тесту, семь обработчиков перестали молчать одним
  словом, набор расширен строго аддитивно 9..12; headless 739 прогнан направлением; разблокирован
  полигон, ради которого волна открывалась

next: |
  **ВОЛНА: ВОСЕМЬ ЗАДАЧ ИЗ ДЕВЯТИ ЗАКРЫТО.** Идёт одна нога — утро в WIN-U4.

  **Свободны три слота: U1, U2, U3.** И впервые за волну есть чем их занять по существу:
  `t-carry-back-1` (полоса `руки`) и `t-bench-by-fact-1` — **полигон, который проходит владелец
  своими руками**. Обе разблокированы этой ногой.
