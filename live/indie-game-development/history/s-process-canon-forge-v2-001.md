RESULT s-process-canon-forge-v2-001 (owner commission: rework the canon process; no CALL id)
direction: indie-game-development   play: research   node/task: g-d3a8 / canon-process-v2

outcome: |
  Canon process redesigned and installed as «Кузница v2» after the ChatGPT-run process
  broke (owner: «процесс поломался, не в ту степь»). Diagnosis (state-evidenced): 5
  accreted plays = labyrinth; 40-question graph written for the OLD concept steered
  sessions wrong by construction; premature-freeze incident showed the verdict gate was
  prose not mechanism; no discussion protocol; no amendment path. Research (2 miners:
  studio pillars/ADR/tabletop-worldbuilding practices + documented LLM-process failure
  rails) converged: short human-owned files reloaded every session; single-purpose
  scripted sessions; model proposes into a structured record; only the owner's words
  change status. Installed in canon repo gas_coop_game_canon @715727c + reconciliation
  @7541be3: CONSTITUTION.md (DRAFT: 7 pillar-filters; palette split per owner correction —
  FIXED foundations = gas-sim/reactions/destructible-space, bubble/sleep/bestiary/vertical
  city = DRAFT CANDIDATES; НЕТ-list = owner's negative prompt), SESSION.md (script: frame →
  ≤3 one-paragraph candidates → sparring with mandatory weakness-naming → read-back with
  minuses → owner-verdict-verbatim-or-invalid → record → question-harvest ≤5 each naming
  its gap; MECHANIC mode absorbs mechanic-forge verb-first/worked-loop/lenses; stop-words
  «рамка»/«парковка»; restart-not-recap on drift), QUEUE.md (explicitly DRAFT INPUT for
  the pending cartography call — owner-signed map replaces it; candidates list + parking
  keeps everything), templates/card.md (ADR-shaped: context/decision/consequences-incl-
  negatives/rejected/unresolved-sorted-by-stage + MANDATORY verbatim вердикт_владельца
  field — empty = invalid), INDEX.md (per-card one-liners = the only citable memory),
  AMENDMENTS.md (cards immutable; supersede-by), sources/pitch-ono-dyshit-2026-07.md
  (concept as MATERIAL not canon, incl. negative-prompt list), visuals/PROMPTS.md
  (style-anchor + slot template), LEGACY.md (old questions/maps = evidence not law;
  transfer only through a v2 session), README.md (v2 quick-start; first step =
  cartography). Kept alive from v1: mechanic-lenses as BINDING gate, verb-first rules,
  «бумага ≠ fun», co-creation, rejected_alternatives.

evidence: |
  Canon repo commits 715727c (12 files) + 7541be3 (reconciliation). Research grounding:
  pillars-as-filter (DOOM push-forward; Pears 3–8), ADR shape + immutability/supersede
  (Nygard, AWS), Microscope Palette (ban-list before content) + additive-canon,
  Dawn-of-Worlds out-of-phase-costs-more (→ «предварительная» status instead of hard
  blocking), Rust-RFC unresolved-stage sorting, Lostgarden design-log JIT queue; rails:
  state-files-not-memory (Cline Memory Bank, story bibles), sycophancy is structural →
  verdict never belongs to the model (Sharma 2023; SycEval), answer-bloat → ≤3
  one-paragraph candidates (Lost-in-Multi-Turn 2025), restart-not-recap on drift,
  question-fatigue budgets, prompt-library style-anchor practice. Full miner reports in
  this session's transcript.

state_changes: |
  Applied by the self-writer WITH reconciliation (see WRITER-NOTE). Canon repo already
  committed (@715727c, @7541be3) — product repo, not live/**.
  1. Replace live/indie-game-development/plays/canon-forge.md with the v2 play (thin
     OS-wrapper; one session = one cluster 1–3 questions; owner-verdict mandatory —
     writer rejects cards with empty вердикт_владельца; canon parallel to build bet, G1;
     mechanic cards gate on knowledge/mechanic-lenses.md; superseded map at bottom).
  2. Replace live/indie-game-development/plays/canon-status.md with a superseded stub
     (→ QUEUE.md + INDEX.md).
  3. Replace live/indie-game-development/plays/design-lab.md with a superseded stub
     (folded into v2: candidates/sparring + «проверка: нужен-грейбокс» card field).
  4. Replace live/indie-game-development/plays/mechanic-forge.md with a superseded stub
     (folded into v2 SESSION.md MECHANIC mode; mechanic-lenses.md remains BINDING).
  5. canon-cartography.md: KEPT at v1 (restored after an initial stub — see WRITER-NOTE
     conflict 1): the live route c-cartography-core-concept-rebuild-001 runs through it.
  6. live/indie-game-development/NOW.md:
     6.1 header → updated: 2026-07-09 by s-process-canon-forge-v2-001
     6.2 parallel_tracks/g-d3a8.state: APPEND (not replace) the PROCESS SHELL sentence —
         Кузница v2 installed @7541be3; fixed foundations vs draft-candidates split;
         QUEUE = draft input for the pending cartography; old plays superseded;
         canon-cartography kept v1 for the pending call.
     6.3 decisions untouched (d-ono-dyshit-core-frame-fork-001 already absent — resolved
         by a parallel session; no-op).
     6.4 bet/tasks/open_calls (c-exec-033, c-visual-008, c-cartography-core-concept-
         rebuild-001)/recurring/next untouched — NOW.next remains the cartography CALL.
  7. Append LOG.md line (as written).
  8. Create this history file.
  9. TREE.md, CHARTER.md, knowledge/ untouched.

WRITER-NOTE (reconciliation on apply, stale-state re-verify rule): |
  Mid-apply, committed state showed parallel sessions had moved the direction:
  (a) OWNER CORRECTION recorded in c-cartography-core-concept-rebuild-001 + g-d3a8 note:
  fixed foundations = gas simulation / reactions / destructible space ONLY; bubble, floor
  loop, carry, tools, economy, ЛЁГКИЕ, roster, roles, title, old canon = DRAFT until
  re-approved; next step = cartography map session choosing the smallest first blocking
  question WITHOUT assuming bubble as center; NOW.next routes through local/
  canon-cartography. Conflicts resolved: (1) my initial cartography-play stub RESTORED to
  v1 (the live route needs it); (2) canon repo reconciled @7541be3 — CONSTITUTION palette
  split into FIXED foundations vs DRAFT candidates (bubble items demoted), pillar 3/4
  de-bubbled, QUEUE re-labeled DRAFT INPUT for cartography, README points first step at
  the cartography call; (3) planned g-d3a8 note REPLACE downgraded to APPEND (the owner's
  cartography correction stays authoritative); (4) planned removal of d-ono-dyshit-core-
  frame-fork-001 = no-op (already resolved in parallel); (5) this RESULT's original
  "next = ratify + q-obeshanie" is SUPERSEDED — actual next = the pending cartography
  call, which SHOULD RUN as/with the v2 shell (its owner-signed map becomes the v2
  QUEUE; constitution ratification can open that same session). NOW header applied over
  s-work-065-c-visual-008-frame (a later parallel visual session) — normal last-writer
  semantics.

captures:
  - The pending cartography session should open with CONSTITUTION ratification (one-time
    v2 step 0) and close by writing its owner-signed map INTO QUEUE.md — merging the two
    owner instructions (process rework + concept rebuild) into one route.
  - StyleBible/plates (legacy) need a review pass when q-vizual-gaza comes up.
  - Superseded plays' full v1 texts live in git history — restorable if v2 misses
    something in practice.
  - Watch v2 for 2-3 sessions: if the owner says «рамка» more than once per session, the
    script needs tightening (process-first reflex, not model swapping).
  - Friend-feedback on the concept-share doc, when it returns, enters as QUEUE material
    (not verdicts).

decisions_needed: []  (constitution ratification + map sign-off happen inside the
  cartography/v2 session — G7-batched there)

play_check:
  - 1 Recite: done — job = diagnose the broken canon process and install a reworked one
    (owner commission verbatim: «я бы хотел изменить наш Canon Forge… ожидаю получить
    рабочий процесс»); one session, one job.
  - 2 Investigate: done — first-hand reads (5 plays, canon repo README/template/
    q-north-star/40-question statuses) + 2 research miners (practices; LLM-failure
    rails), sources dated.
  - 3 Confidence: done — diagnosis grounded in state evidence; process choices traced to
    named practices; CONSTITUTION explicitly DRAFT pending ratification; v2 effectiveness
    unproven until run (watch-capture set); mid-apply owner correction honored over my
    earlier draft (bubble de-centered).
  - 4 Close: done — canon repo installed+reconciled+committed, OS plays/state applied
    with documented deviations, log line, next = the pending cartography call under the
    v2 shell.

log: 2026-07-09 — research canon-process-v2: «Кузница v2» installed (constitution-draft with fixed-foundations palette, session script with mandatory owner-verdict, immutable cards+amendments, QUEUE as draft input for cartography); 4 old plays superseded, cartography kept; reconciled mid-apply with owner's parallel concept-rebuild correction.

next: run c-cartography-core-concept-rebuild-001 (fresh chat, local/canon-cartography under the v2 shell: ratify CONSTITUTION, build the question map from fixed foundations, owner-signed map replaces draft QUEUE). Direction primary NOW.next unchanged (per NOW).

END_OF_FILE: live/indie-game-development/history/s-process-canon-forge-v2-001.md
