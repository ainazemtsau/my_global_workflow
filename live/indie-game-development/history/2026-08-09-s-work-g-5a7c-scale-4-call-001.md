# RESULT — s-work-g-5a7c-scale-4-call-001

play: work · close: light — because каждая строка `done_when` входящего CALL это факт, который эта
нога перемерила своей рукой из закоммиченных байтов и селектора: файл наряда и карточка существуют,
слот и база прочитаны настоящим селектором и `git`, пин сверен с `validation.config`, происхождение
каждой строки сверено с закоммиченными словами владельца, а расчёт посчитан здесь, а не заимствован
direction: indie-game-development · node: g-5a7c · task: t-scale-4 · call: c-work-g-5a7c-scale-4-call-001
date: 2026-08-09

base_read: OS — `wt/indie-game-development` = `be6b4558`, рабочее дерево чисто, `origin/main`
подтянут. Продукт `C:/projects/Unity/GasCoopGame_win-u1`: `HEAD` = `slot/win-u1` =
`origin/slot/win-u1` = `dev` = `origin/dev` = `origin/main` =
`102af1b1c076148ff6a7af7591f92b6880cdc70d`, рабочее дерево чисто (`git status --porcelain` пуст),
`git log slot/win-u1 ^origin/main` пуст. **Расхождение, названное прямо: локальная `main` в слоте
отстала на четыре коммита** (`11f8ddbf`, `main...slot/win-u1` = `0 4`). Продуктовые байты, ветки,
слоты и slot-state этой ногой НЕ изменялись; единственное действие в продукте — `git fetch` и
чтение.

## outcome

Готов к отправке engineering-наряд **`c-exec-g-5a7c-loot-inactive-until-held-001`**
(`work/c-exec-g-5a7c-loot-inactive-until-held-001-call.md`): лут — настоящее тело только пока его
держат лучом или пока он падает; лёг и никто не держит — неактивен, ноль тиков шума, сдвинуть нечем,
но сквозь него не пройти. Наклон не трогается, переноска остаётся той, которую владелец принял
прогоном.

Наряд пинит `engineering_contract: 36` и слот `WIN-U1` его прямым словом, перемеренный настоящим
селектором на момент написания.

**Три вещи, ради которых эта нога стоила своего дня:**

1. **Проверка происхождения нашла четвёртую подряд выдумку исполнителя.** Строка «мыши он мешает
   пройти» в `knowledge/loot-is-inactive-until-a-beam-touches-it.md` продолжена глоссой писавшей
   ноги: «она об него **спотыкается и обходит**». Его слова — только «я думаю, что лут мешает
   пройти». Спотыкание, замедление и обход в наряд НЕ пошли; заказан ровно один смысл — сквозь
   лежащий лут не пройти.
2. **Расчёт снял утверждённое число и закрыл дешёвый неправильный путь.** «≈165–195 Н» никем не
   считалось. Посчитано здесь: доложенная сила лежащего тела = `20 · g · 0.01667 / 0.02 = 16.67 · g`
   → **163 Н при 9.81** (выше порога стука 150) и **50 Н при 3** (ниже порога шороха 60). Значит
   исполнитель мог поставить грузу гравитацию `3`, увидеть тишину и объявить требование выполненным,
   не построив неактивности вообще, — а тишина держалась бы на запасе в 10 Н. Наряд это закрывает
   прямо: тишина обязана быть устройством, не порогом и не засыпанием тела движком.
3. **Расхождение двух вертикальных законов названо и сведено к одному числу.** Мышь падает по
   аналитическому правилу с `3` м/с², проектная гравитация Unity — `9.81`: включить `useGravity`
   значит уронить ящик в 3.27 раза быстрее мыши. Наряд требует одну обратимую настройку рядом с
   `_walkerGravity`, по умолчанию равную тому же `3`, ничем не замороженную, и запрещает трогать
   проектную гравитацию.

## evidence — перемерено первой рукой, а не принято с брифа

| Что | Чем перемерено |
|---|---|
| Слот годен | `tools/select-slot.ps1 -Slot WIN-U1` (только чтение): `state: CLEAN`, `lifecycle: AVAILABLE`, `lease: none`, `availability: AVAILABLE`, `branch: slot/win-u1`, `head: 102af1b1…`, `mcp-endpoint: unrecorded` |
| База и отсутствие чужого впереди | `git rev-parse`: `slot/win-u1` = `origin/slot/win-u1` = `dev` = `origin/dev` = `origin/main` = `102af1b1`; `git log slot/win-u1 ^origin/main` пуст; `git status --porcelain` пуст |
| Локальная `main` отстала | `git rev-parse main` = `11f8ddbf`; `git rev-list --left-right --count main...slot/win-u1` = `0 4` |
| Пин контракта | `validation.config` (blob `5a716599405a`): `synced_contract_version: 36` = `engineering_contract: 36` наряда. Дрейфа нет |
| Груз — единственное физическое тело, гравитация выключена, Y заперт | `Network/CargoBody.cs` на `102af1b1`: `useGravity = false`, `FreezePositionY \| FreezeRotationX \| FreezeRotationZ`, `mass = _settings.CargoMass`, `Physics.simulationMode = Script`, `Physics.Simulate` из `Step` |
| Вертикали нет по ВСЕЙ цепи, а не только в теле | `CargoBodyPose`, `CargoState`, `CargoPull`, `CargoSnapshot` — все без Y; высоту выбирает клиент в `NetworkCargoPresentation.Apply` из `HolderCount` |
| Мышь сквозь лут проходит сегодня | `Network/WalkSpaceProbe.cs` знает только стены `CarryStand` и один `_verticalProbeBox`; ссылки на груз нет ни одной |
| Порядок тика | `Core/Multiplayer/CarryWorld.cs`: ходоки спрашивают `IWalkSpacePort` ДО `Cargo.Advance` |
| Груз в сцене один | `Network/NetworkWalkerCourier.cs:54-55` — `CreateCargoSpawnState(0)`, единственный `Cargo.Add` |
| Числа расчёта | `NetworkPlaySettings.asset`: `_cargoMass: 20`, `_walkerGravity: 3`, `_physicsStepsPerTick: 2`, пороги `60 / 150 / 400`, телекинез `1.28 / 0.8 / 1.6`, высоты `0.02 / 0.092`. `ProjectSettings/DynamicsManager.asset`: `m_Gravity y: -9.81`. `ProjectSettings/TimeManager.asset`: `Fixed Timestep: 0.02` |
| Тик = 1/30, и это не догадка | FishNet `TimeManager.cs:184` `_tickRate = 30`; компонент не сериализован ни в `IntegratedHouse.unity`, ни в `NetworkRuntime.prefab` — `NetworkManager.cs:325` `GetOrCreateComponent<TimeManager>()` создаёт его с умолчаниями |
| `Time.fixedDeltaTime` действительно 0.02, а не тик | FishNet переписывает его только в `PhysicsMode.TimeManager` (`TimeManager.cs:544-553`); по умолчанию `_physicsMode = PhysicsMode.Unity` (`:207`) |
| Точка отката | `git rev-parse 102af1b1^{tree}` = `900bc02baf933ab9e46ffa59e657abb1284af505` |

**Чего эта нога своей рукой НЕ делала:** ничего в продукте не строила и не запускала — ни Unity, ни
тестов, ни claim слота. Расчёт силы лежащего тела — установившаяся оценка по формуле, а не замер
прогоном; в наряде он так и назван, и ничего на нём не заморожено.

## Диспозиция по `done_when` входящего CALL — все шесть требований

| Требование CALL | Диспозиция |
|---|---|
| Наряд написан, лежит в `work/`, зарегистрирован карточкой `call` | **ЗАКРЫТО.** `work/c-exec-g-5a7c-loot-inactive-until-held-001-call.md` + `cards/c-exec-g-5a7c-loot-inactive-until-held-001.md` (`to: executor`, `for: t-scale-4`, `ready`) |
| Слот WIN-U1 и база перемерены первой рукой на момент написания | **ЗАКРЫТО.** Настоящий селектор и `git` выше; названо и расхождение — отставшая локальная `main` |
| `engineering_contract` пин проставлен | **ЗАКРЫТО.** `36` против `validation.config` `synced_contract_version: 36` |
| У каждой строки `done_when` проверено происхождение | **ЗАКРЫТО, и с находкой.** Таблица источников в наряде; снятое названо вслух: глосса «спотыкается и обходит», описание механизма как формы, число «165–195 Н», старая редакция «просто включить гравитацию», строка узла про путь наверх |
| Расхождение двух вертикальных законов названо | **ЗАКРЫТО.** Отдельный раздел наряда с числами `3` против `9.81` и следствием «в 3.27 раза быстрее»; сведение к одной названной обратимой настройке; запрет на проектную гравитацию |
| Откат назван и проверен | **ЗАКРЫТО.** Названа точка `102af1b1` и дерево `900bc02b`; наряд требует проверить откат первой рукой readback'ом дерева и прямо заявить, что вне репозитория для отката ничего не нужно |
| В возврате сказано, что продуктовый handback задачу Направления не закрывает | **ЗАКРЫТО.** Раздел `return` наряда: закрывает его слово о том, та ли это переноска, что он принимал |

## Что этот наряд НЕ покрывает — записано против себя

- **Высота препятствия не выбрана, и это сознательно.** Лежащий лут 0.04 м при росте мыши 0.16 м,
  а мышь теперь прыгает и летает. Плоский блокиратор без верха (принятая сегодня семантика всего,
  что преграждает путь) даст над лутом невидимый столб; верх, на который можно встать, — это уже
  платформа, которой аппетит ставки не покупал. Наряд ставит умолчанием первое как «не заводить
  нового механизма», обязывает исполнителя назвать сделанное прямо и оставляет вердикт его глазу.
- **Расхождение единиц силы контакта не чинится.** `Time.fixedDeltaTime` (0.02) против настоящего
  подшага (0.01667) — это `i-noise-bands-invisible-and-force-mis-scaled-001` со своим маршрутом;
  пороги не двигаются.
- **Фиксированная дальность несения не чинится** (`i-telekinesis-carry-distance-is-fixed-001`).
- **«Живой груз ударил неактивный»** не заказано и не заложено: груз в сцене один, вопрос сегодня не
  возникает.
- **Наряд ещё не отправлен и не вернулся.** Его правильность судится возвратом и прогоном владельца,
  а не этой ногой.

## state_changes

- Зарегистрирован `c-exec-g-5a7c-loot-inactive-until-held-001` (`to: executor`, `kind: engineering`,
  `repo: ainazemtsau/GasCoopGame`, `engineering_contract: 36`, `for: t-scale-4`, `status: ready`);
  наряд — `work/c-exec-g-5a7c-loot-inactive-until-held-001-call.md`.
- `c-work-g-5a7c-scale-4-call-001` → закрыт как исполненный; карточка уходит в `cards/closed/`.
- `t-scale-4` остаётся `open` до слова владельца после прогона; `done_when` карточки НЕ
  редактировался.
- `g-5a7c` остаётся `active`; `NOW.bet` не менялся; карточка узла не редактировалась, кроме строки
  журнала.

## captures

- Локальная `main` отстала теперь и в слоте `WIN-U1` (`11f8ddbf` против `102af1b1`), не только в
  `GasCoopGame_dev`. Это ровно та поверхность, на которой висит
  `i-deliver-gate-red-when-main-catches-dev-001`; наряд запрещает её догонять этой работой.
- Высота рисунка груза сегодня выбирается КЛИЕНТОМ из `HolderCount`
  (`NetworkCargoPresentation.Apply`). Когда у груза появится настоящая высота, эта клиентская
  подмена перестанет быть верной — стоит посмотреть на неё глазами в следующей ноге про показ.

## decisions_needed

Одно, и оно НЕ блокирует отправку наряда: **должен ли лежащий лут иметь верх, на который можно
встать, или он преграждает путь как стена — без верха и на любой высоте.** Мышь теперь летает, так
что разница видна глазом. Наряд идёт с умолчанием «как стена» (не заводит нового механизма) и
обязывает исполнителя назвать сделанное; вопрос закрывается словом владельца после прогона, а не
строкой наряда.

## play_check

- 1 recite: done — цель и `done_when` входящего CALL сверены с живой карточкой `t-scale-4`, третьей
  позицией активной ставки `g-5a7c`, и с `knowledge/loot-is-inactive-until-a-beam-touches-it.md`.
- 2 owner inputs (owner): done — его действующие слова уже существуют закоммиченными и приведены
  дословно: «работа запускается в слоте WIN-U1»; «когда, в общем, игроки не поднимают лут, он как бы
  и звуков не должен издавать. То есть он условно неактивный»; «главное сделать так, чтобы игроки не
  могли его двигать»; «если он упадёт на пол, то он должен издать звук»; «я думаю, что лут мешает
  пройти». Нового вопроса ДО артефакта не требовалось: единственная развилка (верх у препятствия)
  решается его глазом на прогоне и вынесена в `decisions_needed`, а не задержала наряд.
- 3 do the work: done — наряд написан на свежей базе `102af1b1`, с первым atomic claim, точной
  границей Unity/MCP STOP, посчитанным расчётом и тремя строками `done_when`.
- 4 self-check: done — построчная диспозиция шести требований CALL выше; слот, refs, пин, ключевые
  product blobs, тик, делитель силы и точка отката перемерены первой рукой.
- 5 close: done — входящий CALL закрывается, executor CALL зарегистрирован `ready`; задача остаётся
  открытой; продолжение той же волны названо.

## Гейты

- **G5.** `close: light` заявлен построчно: все шесть требований входящего CALL — механические
  факты, перемеренные здесь из закоммиченных байтов, селектора и уже сказанных владельцем слов. Ни
  одна строка не требует суждения, которого никто не сделал. Что НЕ повторено своей рукой (ничего в
  продукте не строилось и не запускалось) — названо явно.
- **G7.** Геймплей не решён ногой: способ подъёма не переоткрыт, высота препятствия не выбрана за
  владельца, скорость падения объявлена HOW-умолчанием с причиной и обратимостью, а не подписанной
  строкой, и вынесена его глазу.
- **G3.** Аппетит не растянут: расхождение единиц шума, фиксированная дальность несения и вопрос про
  верх препятствия ушли в улики/решение, а не в работу.
- **G9.** Карточка узла не редактировалась — только строка в её журнал. `done_when` задачи не тронут.
- **Устройство игры.** `knowledge/how-the-game-is-built-layers.md` соблюдено: правило остаётся у
  судьи, курьер правил не получает, и наряд прямо велит СТОП с вопросом владельцу, если работа
  упрётся в необходимость менять курьера.

## log

наряд на неактивный лут написан — расчёт снял утверждённые «165–195 Н» и закрыл дешёвый путь «гравитация 3 и оно тихо», два вертикальных закона сведены к одному числу, «спотыкается и обходит» снято как выдумка ноги

## next

`CALL c-exec-g-5a7c-loot-inactive-until-held-001` — отправить наряд исполнителю в продуктовый
репозиторий, слот `WIN-U1`. После HOME отдельная нога Направления читает возврат первой рукой;
`t-scale-4` закрывает слово владельца о том, та ли это переноска, что он принимал.

END_OF_FILE: live/indie-game-development/history/2026-08-09-s-work-g-5a7c-scale-4-call-001.md
