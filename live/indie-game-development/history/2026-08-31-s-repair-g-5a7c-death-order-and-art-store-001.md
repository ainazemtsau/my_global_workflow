# RESULT — s-repair-g-5a7c-death-order-and-art-store-001

direction: indie-game-development
play: repair
node/task: g-5a7c / порядок смерти волны + улика `i-shared-art-store-has-no-guard-001`
track: —
date: 2026-08-31
outcome_kind: repair — порядок смерти перевёрнут его словом; причина порчи склада арта установлена замером и оказалась не iCloud и не удалением

owner_approved: |
  Его слово 2026-08-31, дословно: **«да, переверни порядок»** — в ответ на прямое предложение
  поднять содержание утра выше водопровода в порядке смерти волны.

  Разбор склада — ответ на его вопрос: **«насчёт склада как бы так и неизвестно, из-за чего это
  произошло… может там это в каком-то iCloud дело. Ну и что это вообще может быть такое, типа
  удалилось, и типа непонятно из-за чего?»** Ответ дан замером; ни одна строка `done_when` не
  изменена, улика не закрыта.

outcome: |
  **ПОРЯДОК СМЕРТИ ПЕРЕВЁРНУТ, И ПРЕЖНИЙ БЫЛ ПРЯМОЙ ОШИБКОЙ НАРЕЗКИ.** Он ставил
  `содержание утра данными` первым на вылет — то есть под нож шла ЕДИНСТВЕННАЯ задача волны,
  которую владелец потрогал бы руками, а водопровод оставался. Цена измерима: за целый день,
  шесть закрытых задач и семь опубликованных ног он не коснулся ничего. Новый порядок: первым
  умирает `разбор прогона`, содержание утра не умирает раньше водопровода.

  **ПРИЧИНА ПОРЧИ СКЛАДА УСТАНОВЛЕНА, И ЭТО НЕ iCloud И НЕ УДАЛЕНИЕ.** Файл никуда не девался:
  сам FBX лежит на месте, **84 092 848 байт, дата 25 августа — не тронут вообще**. Пострадал
  только маленький файл настроек рядом с ним. И его **переписала сама Unity, заводскими
  умолчаниями**.

  **ЧЕТЫРЕ УЛИКИ, КАЖДАЯ ЗАКРЫВАЕТ ОДНУ ВЕРСИЮ:**

  - **Идентификатор в обоих файлах ОДИН И ТОТ ЖЕ** — `32a45d5203b1f7c49a15a6e86e1da92d`.
    Удаление с последующим восстановлением дало бы НОВЫЙ случайный идентификатор. Значит файл не
    удаляли: его переписали, сохранив имя.
  - **Испорченный файл — не обрывок и не мусор.** Это структурно полный, валидный
    `ModelImporter` с той же версией формата (`serializedVersion: 24501`), у которого просто ВСЕ
    настройки заводские: `internalIDToNameTable: []`, `externalObjects: {}`, ноль костей. Так
    выглядит файл, который Unity пишет, когда импортирует модель, ничего о ней не зная. Сбой диска
    или синхронизации даёт обрывок, а не аккуратные умолчания.
  - **Следов iCloud нет ни одного:** ни файлов-заглушек `.icloud`, ни «конфликтных копий» во всём
    складе.
  - **И решающая, случайная:** резервную копию, которую направление создало в 07:53,
    **Unity проиндексировала в 08:03** — рядом лежит сгенерированный ею `DefaultImporter`-файл с
    новым идентификатором. То есть редактор в тот момент **активно сканировал эту физическую
    папку**. Это не гипотеза о том, что editor мог туда смотреть, — это его подпись.

  **МЕХАНИЗМ, СЛОЖЕННЫЙ ИЗ ЭТОГО.** Проверено: **все ПЯТЬ чекаутов — junction на ОДНУ физическую
  папку**. У каждого Unity при этом свой собственный приватный индекс импортированного
  (`Library`), и он в общую папку не входит. Сегодня утром одновременно работали четыре редактора.
  Когда второй редактор смотрит на тот же физический файл со своим индексом, в котором настроек
  этой модели нет, он честно пишет рядом свои — заводские, — поверх чужих. Идентификатор при этом
  сохраняется, потому что он уже лежит в файле. **Ничего не ломается: каждый редактор поступает
  правильно, а результат — стёртый риг у всех пятерых сразу.**

  **ЧТО ЭТО ЗНАЧИТ ДЛЯ УЛИКИ.** `i-shared-art-store-has-no-guard-001` подтверждена сегодняшней
  ревизией как живая, и теперь у неё есть не только следствие, но и механизм. **Сторож должен
  защищать от ОДНОВРЕМЕННОГО ДОСТУПА ДВУХ РЕДАКТОРОВ, а не от синхронизации.** Направление
  собиралось предлагать защиту от порчи файла; замер говорит, что предмет другой.

evidence: |
  Прочитано направлением напрямую в
  `C:\Users\Anton\iCloudDrive\TunnelCrew-Assets\TunnelCrew\Art\Bodies\Sources\Victor\`:

  - `Victor_Older_BootFix_AutoHide_03.Fbx` — **84 092 848 Б, mtime 25 августа 13:17**, не тронут;
  - `…Fbx.meta` (восстановленный) — 48 758 Б, `guid: 32a45d5203b1f7c49a15a6e86e1da92d`,
    `externalObjects` содержит поимённые переназначения материалов;
  - `…Fbx.meta.broken-2026-08-31` (испорченный) — 2 993 Б, **тот же guid**,
    `fileFormatVersion: 2`, `serializedVersion: 24501`, `internalIDToNameTable: []`,
    `externalObjects: {}`;
  - `…Fbx.meta.broken-2026-08-31.meta` — 155 Б, mtime **31 августа 08:03**, содержит
    `DefaultImporter` и НОВЫЙ `guid: 13620fefd0cd7a7428e1181c61fb4c7c`. Создан Unity через десять
    минут после того, как направление положило резервную копию;
  - поиск `*.icloud` и `*conflict*` по всему складу — **ноль совпадений**.

  Junction подтверждены по всем пяти чекаутам (`LinkType: Junction`), цель у всех одна:
  `…\iCloudDrive\TunnelCrew-Assets\TunnelCrew\Art\Bodies\Sources\Victor`. Проверены
  `win-u1`, `win-u2`, `win-u3`, `win-u4`, `testrun`.

state_changes: |
  ИСПРАВЛЕНО ЕГО СЛОВОМ (1 блок карточки волны):
  - `bet-g-5a7c-wave-9`, блок `appetite`: порядок смерти перевёрнут. Первым умирает
    `разбор прогона`; `содержание утра данными` не умирает раньше водопровода. Прежний порядок и
    причина замены записаны в том же блоке, чтобы через неделю не казалось, что так было всегда.

  ДОПОЛНЕНО ЗАМЕРОМ (1 улика, НЕ закрыта):
  - `i-shared-art-store-has-no-guard-001` — получила механизм: порчу вызывает не синхронизация и
    не удаление, а два редактора на одной физической папке с раздельными приватными индексами.
    Предмет будущего сторожа этим меняется.

captures: |
  - **ВЛАДЕЛЕЦ СПРОСИЛ «А ЧТО ЭТО ВООБЩЕ МОЖЕТ БЫТЬ», И ВОПРОС ОКАЗАЛСЯ ДОРОЖЕ ОТВЕТА.** Направление
    весь день считало виновником общий склад как таковой и собиралось предлагать сторожа против
    порчи файлов. Замер показал другое: файл цел, виновата одновременность. **Сторож против не
    того противника стоил бы ноги и не помог бы.**
  - **СЛУЧАЙНАЯ УЛИКА ОКАЗАЛАСЬ РЕШАЮЩЕЙ.** Резервная копия, сделанная утром «на всякий случай»,
    через десять минут получила от Unity собственный `.meta` — и это единственное прямое
    доказательство, что редактор活но работал в общей папке. Приём на будущее: **след, оставленный
    инструментом на твоём собственном временном файле, — это подпись инструмента.**

decisions_needed: |
  Ни одного. Улика остаётся открытой и теперь несёт механизм; когда её возьмут в работу, предмет
  сторожа известен.

play_check: |
  1. Назвать противоречие — порядок смерти резал единственную осязаемую задачу; причина порчи
     склада считалась неизвестной и приписывалась iCloud.
  2. Восстановить новейшим-первым — байты обоих файлов настроек, размер и дата самого FBX,
     junction по всем пяти чекаутам, поиск следов iCloud.
  3. Предложить исправленное состояние — переворот порядка его словом; механизм дописан в улику.
  4. Подтверждение (owner) — его слово «да, переверни порядок» получено до правки.
  5. Дыра в OS → нет. Находка продуктовая и операционная.

log: |
  порядок смерти волны перевёрнут его словом — содержание утра больше не режется первым, и прежний
  порядок стоил ему дня без единого касания; причина порчи склада арта установлена замером: файл
  цел и не удалялся, настройки переписала сама Unity заводскими умолчаниями, потому что пять
  чекаутов смотрят в одну физическую папку своими раздельными индексами

next: |
  Идут три ноги: зрение U1, «почему» U3, утро U4. Свободен WIN-U2.

  Улика про склад теперь несёт механизм: сторож нужен против одновременного доступа двух
  редакторов, а не против синхронизации.
