# RESULT — s-work-g-1d84-built-exe-call-001

play: work (выпуск корня на последнюю задачу волны) + гигиена журнала
direction: indie-game-development · node: g-1d84 · track: сцена
date: 2026-08-07
base_read: OS `be1d8fd5`; `TREE.md` критерий 9, `NOW.md` `t-scene-4`, `os/plays/work.md`,
`os/adapters/coding-agent.md`. Продукт `C:/projects/Unity/GasCoopGame` только читался,
опубликованный `main` = `f03b1468`; байты, ветки, слоты и slot-state НЕ изменялись.

## Выпущен корень

`c-exec-g-1d84-built-exe-and-second-copy-001` → `t-scene-4`, полоса `сцена`, contract 36, stage PLAN,
база `f03b1468`, `status: ready`, слот НЕ назначен. Это единственный открытый корень волны: одиннадцать
из двенадцати задач закрыты.

## Что в наряде измерено, а не предположено

Три входа сняты первой рукой на `f03b1468` и положены в `context` фактами:

1. **Подключение по адресу уже построено.** `Network/NetworkSessionService.cs:67` — сервер, `:74` —
   клиент на адрес по умолчанию, `:93` — клиент на введённый адрес; плюс `Network/LobbyController.cs`,
   `Scenes/Lobby.unity`, `Network/NetworkPlaySettings.asset`. Наряд покупает сборку и проверку, а не лобби.
2. **В списке сборки три сцены, одна лишняя.** `ProjectSettings/EditorBuildSettings.asset`:
   `Lobby.unity`, `IntegratedHouse.unity`, `Assets/Scenes/SampleScene.unity`. Последняя — дефолтная
   пустая сцена Unity.
3. **Автосборки в проекте нет вообще.** `BuildPipeline` и `BuildPlayerOptions` не встречаются ни в
   `Assets/`, ни в `tools/`.

## Два решения, принятые этой ногой, и почему они не отданы исполнителю

- **`SampleScene` убирается из списка сборки.** Одна обратимая строка, сам файл сцены не удаляется.
  Основание: в exe владельца дефолтной пустой сцене делать нечего. Это рутинный выбор, и он назван
  прямо, потому что оставить его исполнителю значило бы отдать решение, которого в наряде нет.
- **Неизвестность про способ сборки закрыта STOP-ом, а не догадкой.** Продуктовый `AGENTS.md:227`
  разрешает агенту работать в редакторе через MCP на лицензии Personal, но сборка ПЛЕЕРА так здесь не
  делалась ни разу. Наряд велит: попробовать штатный Build в слоте; если путь не работает — STOP и HOME
  с точным разрывом, без своего build-скрипта, без установки пакетов, без обходного транспорта. Тогда
  exe собирает владелец, а наряд возвращается проверкой подключения. Обход STOP объявлен провалом
  наряда, даже если exe в итоге появится.

Ограничение слота снято против прежних двух нарядов: они допускали только `WIN-U2`/`WIN-U4`, а сейчас
все четыре слота `AVAILABLE`, `lease: none` — перемерено в `gascoop-slot-state.v1.json`. Выбор
по-прежнему за владельцем, агент слот не выбирает.

## Гигиена журнала — по прямому слову владельца

`LOG.md` вёлся новыми записями сверху, но пять записей оказались дописаны снизу
(`s-map-g-1d84-demo-focused-wave-cut-001`, `...named-house-routes-launch-001`,
`...named-house-routes-close-001`, `s-repair-g-1d84-demo-focused-task-cut-001` и моя предыдущая
`s-work-g-1d84-delivered-pair-close-001`). Они переставлены в верную позицию по времени коммитов
(15:32 / 16:15 / 19:11 / 08-07 06:50 / 08-07 07:0x).

**НИ ОДНА БУКВА ЗАПИСЕЙ НЕ ИЗМЕНЕНА, И ЭТО ПРОВЕРЕНО МАШИНОЙ, А НЕ ГЛАЗОМ:** перестановка выполнена
скриптом с проверкой `set(new) == set(entries)` и `len(new) == len(entries)` — 61 запись до, 61 после,
множества совпали. Заодно убрана одна лишняя пустая строка и восстановлен разделитель перед нижним
указателем `archived:`.

Одна пара осталась в прежнем взаимном порядке НАМЕРЕННО: `frontier-left-to-owner-001` и
`map-demo-focused-wave-cut-001` лежат в ОДНОМ коммите `4481cab1`, и по времени их различить нечем;
взят порядок, который файл уже утверждал.

## Гейты

- **G9** — карточка не трогалась; наряд выпущен против критерия 9 в текущей утверждённой редакции.
- Корень выпущен `ready`, но не запущен: имя слота — слово владельца, и без него наряд не стартует.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-work-g-1d84-built-exe-call-001.md
