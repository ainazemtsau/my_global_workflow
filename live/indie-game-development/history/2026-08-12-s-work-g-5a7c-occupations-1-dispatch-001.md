# RESULT — s-work-g-5a7c-occupations-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-occupations-1
track: householder-life
date: 2026-08-12
outcome_kind: checkpoint — наряд выдан, задача НЕ закрыта

owner_approved: |
  «давай следующий наряд который мы можем запустить если все заблочено то так и пиши» — 2026-08-12.

  И отдельно, как решение: **«лут не должен через друг друга проходить»**, с оговоркой «есть ещё
  проблема… я что-то не видел, чтобы поднимался этот вопрос».

outcome: |
  **ЗАПУСТИТЬ МОЖНО РОВНО ОДНО, И ЭТО `t-occupations-1`.** Наряд `c-exec-g-5a7c-occupations-1-001`
  написан и выдан в **WIN-U3** от опубликованной головы `c1d8d384`.

  ## Его вопрос про сквозной проход: он поднимался, и вот где

  Вопрос был записан ДВАЖДЫ: в продуктовом отчёте `docs/results/c-exec-g-5a7c-thing-1-001.md`
  разделом «Owner runtime observation — follow-up required» и в направлении уликой
  `i-cargo-passes-through-cargo-in-the-running-game-001`, о которой я ему сообщила при закрытии
  `t-thing-1`. Но записан он был **как вопрос К НЕМУ**: улика дословно говорила «решение про то,
  какие предметы обязаны сталкиваться в покое, — игровое, владелец решает».

  **Теперь он решил, и решение записано его словами.** Игровая сторона закрыта: лут обязан быть
  твёрдым для лута. Осталась техническая — почему `SetBodyActivity` вообще выключает
  `detectCollisions` осевшему грузу; причина в коде не названа, и её надо померить, а не
  предположить.

  **ГДЕ ЭТО ЧИНИТСЯ — `t-loot-1`, И ЭТО НЕ НОВАЯ ЗАДАЧА И НЕ ОБМЕН.** Задача, которая ставит в дом
  набор лута, и есть та, которая обязана сделать его твёрдым: без этого её собственный результат —
  десять предметов, проходящих сквозь друг друга. Аппетит волны не расширяется, объявленный обмен
  «что-то входит — `t-loot-1` выходит» не задействуется. `t-loot-1` разблокирована публикацией
  `t-thing-1`, но ждёт публикации дома, потому что ставит лут в комнаты и правит ту же сцену.

  ## Честный ответ на «что можно запустить»

  | Задача | Состояние |
  |---|---|
  | `t-occupations-1` | **запускается сейчас** — пересечение с обеими идущими дорожками ноль |
  | `t-sight-1` | нельзя: сталкивается с занятиями на `Householder.cs`, и её проба бессмысленна без мебели, которая лежит в неопубликованном кандидате дома |
  | `t-loot-1` | нельзя: ставит лут в комнаты и правит ту же сцену, что кандидат дома |
  | `t-handlers-1` | нельзя: ждёт зрения и занятий |
  | `t-lab-2` | нельзя: показывает то, чего ещё нет |
  | `t-house-asset-1` | нельзя: нечего переносить, пока дом не опубликован |

  ## НАХОДКА, КОТОРУЮ НАДО ЗНАТЬ ДО ПРИЁМКИ ДОМА

  **Кандидат дома в WIN-U2 собран на протухшей базе — та же болезнь, что у тела.** Родитель его
  рабочего коммита `e592d7c6` — `a7d37b3e`, а `place-fact` опубликовался позже как `c1d8d384`.
  Поэтому дифф `origin/main..HEAD` в WIN-U2 сегодня **удаляет работу `place-fact`**: `−536`
  `HouseholderRoomFactTests.cs`, `−75` её отчёт и три производственных файла хозяина.

  **Перебазирование чистое** — проверено `comm`: собственный дифф дома (12 файлов: материалы
  обстановки, `ThreeRoomHouse.prefab`, `IntegratedHouse.unity`, новые EditMode-тесты и отчёт) и
  дифф `place-fact` **не пересекаются ни одним файлом**.

  Владелец о завершении дома не сообщал, и эта нога его не принимает. Но когда сообщит — приёмка
  обязана начаться с перебазирования, иначе публикация сотрёт закрытую задачу. Это **второй случай
  за день**; правило про сверку головы перед выдачей результата вписано в новый наряд отдельной
  строкой возврата.

evidence: |
  Перемерено этой ногой на `c1d8d384`:
  - `git ls-remote origin` → `main` = `dev` = `c1d8d384`.
  - WIN-U2: `git rev-parse e592d7c6^` → `a7d37b3e`; `git diff --name-only origin/main HEAD` содержит
    удаления `HouseholderRoomFactTests.cs` и отчёта `place-fact`; `comm -12` по спискам путей
    собственного диффа дома и диффа `place-fact` → **пусто**.
  - Умения: `Profiles/HouseholderProfileLoader.cs:18,47,53,56,124` — ровно `walk-route` и
    `wait-at-route-point`, расширяются через `TryResolveAbility`.
  - Занятия: `enum HouseholderOccupation` — пять членов, из них обычной жизни один (`WalkRoute`).
  - Место уже течёт до шага: `HouseholderPlaceReference RoutinePlace` в
    `Core/Householder/Householder.cs:1700,1727,1739,1779,2134,2738`.
  - `4e5858b4:Network/CargoBody.cs:163,193,195` — `detectCollisions = false` и `Collider.enabled =
    false` для неактивного груза, перечитано.
  - `dotnet test … -c Release` → **239 passed**, прогнано этой ногой ранее сегодня на этом дереве.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-occupations-1-001-call.md` и карточка
     `c-exec-g-5a7c-occupations-1-001` (`ready`, `slot: WIN-U3`, `basis: c1d8d384`).
  2. `t-occupations-1`: `open → active`, журнальная строка.
  3. `i-cargo-passes-through-cargo-in-the-running-game-001`: добавлен блок «решение владельца
     2026-08-12» с его дословными словами; `review_when` переписан с «первая задача, трогающая
     физику груза, не раньше дома» на **`t-loot-1` после публикации дома**, потому что игровая
     сторона вопроса теперь закрыта и осталась только техническая.
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-occupations-1-dispatch-001.md`.

captures:
  - |
    ПРОТУХШИЙ БАЗИС — ВТОРОЙ РАЗ ЗА ДЕНЬ, И ОБА РАЗА ОДИНАКОВО: исполнитель сверяет голову на
    старте, работает несколько часов, за это время публикуется соседняя дорожка, и его ветка
    начинает удалять чужое. При трёх-четырёх параллельных фронтах это не случайность, а свойство
    формы. Одна строка возврата это не лечит: строку «подтверди сверку головы» я уже добавляла в
    наряд `place-fact`, и её просто не заполнили. Если случится третий раз — это MAINTENANCE на
    форму наряда, а не на ногу: сверка головы обязана быть строкой `done_when`, а не пожеланием в
    возврате.
  - |
    Улика про сквозной проход была записана дважды и всё равно прошла мимо владельца. Причина
    вероятна и поправима: она уехала абзацем внутри длинного отчёта о закрытии, где выше стояли
    хорошие новости. Плохую новость, которая ломает его собственный кейс демо, стоит выносить
    ОТДЕЛЬНЫМ первым блоком, а не пунктом в списке находок.

decisions_needed: []

play_check:
  - 1 recite — done. Семь строк карточки и обязательство дорожки пересказаны.
  - 2 owner inputs — не требуется: правило фаз он уже дал документом демо, состав дел ограничен объёмом и минимумом его же центрального кейса.
  - 3 do the work — done. `call:executor` выпущен; зависимость от неопубликованного дома названа честно и снабжена СТОПом вместо выдумывания ролей.
  - 4 self-check — done. Семь строк карточки свёрнуты в три строки контракта без потерь; бюджет соблюдён.
  - 5 close — checkpoint.
  - G2 — done. Починка сквозного прохода отнесена к существующей задаче райдером, а не заведена новой: аппетит не расширяется.
  - G3 — done. Объявленный обмен «что-то входит — `t-loot-1` выходит» не задействован, потому что входить ничего не пришлось.
  - G7 — done. Игровое решение принял владелец, нога записала его слова; техническая часть оставлена измерению.
  - G10 — done. RESULT финальный.

log: запустить можно ровно одно — t-occupations-1, наряд выдан в WIN-U3 от c1d8d384, остальные пять задач заблокированы по названным причинам; решение владельца «лут не должен через друг друга проходить» записано в улику его словами, и вопрос переведён из открытого в решённый: игровая сторона закрыта, осталась техническая — померить, зачем осевшему грузу выключают detectCollisions; починка отнесена к t-loot-1 РАЙДЕРОМ, а не новой задачей, потому что задача, ставящая десять предметов в дом, и обязана сделать их твёрдыми, и объявленный обмен аппетита не понадобился; сверх этого найдено, что кандидат дома в WIN-U2 собран на протухшей базе и его публикация как есть удалила бы работу place-fact вместе с её 536-строчным тестом — перебазирование чистое, пересечение путей пусто, но это ВТОРОЙ случай за день, и записано, что при третьем сверка головы обязана стать строкой done_when, а не пожеланием в возврате

next: |
  Идут три: занятия в WIN-U3, дом в WIN-U2 (кандидат готов, не опубликован, требует перебазирования
  при приёмке), тело в WIN-U1 (добивка на перебазирование).

  Публикация дома развяжет сразу четыре вещи: `t-loot-1` вместе с починкой сквозного прохода,
  `t-house-asset-1`, отложенную приёмку по комнатам и приёмку тел на собранном доме.

  `t-sight-1` ждёт освобождения `Householder.cs` от занятий и появления мебели.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-occupations-1-dispatch-001.md
