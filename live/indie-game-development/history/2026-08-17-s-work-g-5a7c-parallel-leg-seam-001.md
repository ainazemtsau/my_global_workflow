# RESULT — s-work-g-5a7c-parallel-leg-seam-001 (play: work)

## outcome

**Ответ параллельной ноги разобран, обе её поправки подтверждены прогонкой по коду, наряд ноги A
дополнен, и один шов разведён так, чтобы мы не встретились в одной строке.**

**Направление было неправо про буфер лучей, и это подтверждено.** `RayIsClear` возвращает `false` на
первом же ненулевом коллайдере (`HouseholderSightSource.cs:247-250`). Обрезание на 64-м теряет только
лишние попадания; чтобы луч решил, что путь чист, буфер должен быть нулевого размера. Предупреждение
было верным по инстинкту и **неверным по следствию**: это расточительство, а не дефект. Записано как
своя ошибка.

**Их находка в нашем файле подтверждена.** `Integrity`, `ThingStateId`, `ThingRemainingSeconds` во
всём `Assets/TunnelCrew` вне тестов встречаются **ровно в одном месте — в объявлении**. Вместе с
мёртвым `Moved` это около пятой части пакета.

**Но удалять их — не решение ноги.** Поле мёртвое ровно потому, что клиент не показывает звонящий
будильник вовсе. Значит выбор не «удалить или оставить», а «клиент это показывает или нет», и это
содержание игры. Заведено вопросом владельцу, а не решено за него.

## evidence

- `HouseholderSightSource.cs:240-252` прочитан целиком: `_physicsScene.Raycast` в буфер на 64, затем
  цикл, который выходит `return false` на первом ненулевом коллайдере. Их поправка верна дословно.
- `git grep` по `ThingStateId|ThingRemainingSeconds` в `Assets/TunnelCrew` без тестов даёт один файл:
  `Network/CargoSnapshot.cs`. Их находка верна.
- `WIN-U3` на момент правки — `AVAILABLE`, наряд ноги A ещё не взят, поэтому дополнение попадает в
  него до начала работы, а не догоняет её.

## state_changes

- `work/2026-08-17-call-cargo-sleep-1.md` — новый раздел «добавлено по запросу параллельной ноги» и
  две новые строки приёмки (высота у контакта; контакт называет, во что попали, и его не слышит ни
  одно существующее правило хозяина).
- `card new i-three-snapshot-fields-ride-and-nobody-reads-them-001`.
- `card new q-does-the-client-show-thing-state-001` — вопрос владельцу.

## captures

**Решение по шву принято и оно про порядок, а не про компромисс.** Высоту контакта и идентичность
задетого мы кладём сами в ноге A — это механика без выбора. **`WorldSituationKind` мы не трогаем
вовсе:** их тип, их расширяемость, их дизайн; открывают сами ПОСЛЕ возврата ноги A, когда наш файл
стабилен. Так вместо переговоров о слиянии получается последовательность.

**Разделение носителя и поведения.** Способность «летящий предмет сбивает существо с ног» владелец
однажды велел убрать, а его отмена дошла пересказом, не дословно. Нога A делает факт ВЫРАЗИМЫМ —
второй контакт, идентичность задетого — и не делает поведения. Поведение ждёт его собственных слов и
пишется на их стороне. Это тот же стандарт, которым Направление отказалось засчитать за слово
собственный пересказ про хват.

**Учтён их рост снимка хозяина.** Плюс 12-16 байт в постоянной части сообщения двигает потолок
одного пакета с 26 предметов на 25. Мелочь, но названа числом, а не «незаметно».

## decisions_needed

Один вопрос владельцу: показывает ли клиент состояние вещи — звонящий будильник, разлитое молоко,
след. От него зависит, снимает ли нога C пятую часть пакета или оставляет.

## play_check

Обе чужие поправки проверены по коду, а не приняты на слово и не отвергнуты из самолюбия. Своя
ошибка названа ошибкой. Гейт G7 соблюдён: пересказанное слово владельца не стало основанием для
изменения поведения.

## log

обе поправки параллельной ноги подтверждены, наряд A дополнен носителем без поведения, а шов разведён порядком вместо переговоров

## next

Владелец открывает `Gas U3`. Вопрос про состояние вещи ждёт его к ноге C.
