# RESULT — s-work-g-5a7c-cargo-sleep-dispatch-001 (play: work)

## outcome

**Наряд на ногу A выдан в `WIN-U3`.** Это первая из четырёх ног по физике груза и единственная,
результат которой видно глазом: после неё каскад «упало со стола, покатилось, сбило соседа»
становится возможным — сегодня он невозможен не «плохо», а в принципе.

**Наряд собран так, что исполнитель не переоткрывает ничего.** В него вписаны: три механизма из
аудита с номерами строк, все границы «что нельзя трогать» с обоснованием каждой, десять строк
приёмки, и пять названных заранее мин.

**Главная находка выдачи — число не выдумывается.** Порог угла опоры в проекте **уже отвечен**:
`LootSupport.cs:36`, `MinimumUpwardness = 0.5f`, с комментарием «столешница 1, стена 0». Тот же
вопрос, решённый в другом месте проекта. Наряд велит брать оттуда, а не назначать своё.

**Вторая находка — врущий близнец.** `CargoNoiseMeasurementTests.cs:475` содержит дословную копию
`if (contact.normal.y > 0f)`, объявленную в комментарии как «faithful re-implementation of what
CargoBodyContact does TODAY». Поменять продукт и забыть копию — оставить проверку, которая врёт.
Вписано в наряд отдельной строкой.

## evidence

- Слоты перепроверены непосредственно перед выдачей: `WIN-U1` — `CLAIMED` под лучом; `WIN-U2`,
  `WIN-U3`, `WIN-U4` — `AVAILABLE`, но на U2 и U4 лежат невзятые пасты (приёмка тел, вид гостиной).
  **Свободен и не занят пастой ровно `WIN-U3`.**
- Поверхность разведена по коду: нога A целиком хостовая — `CargoBody.cs`, `CargoBodyContact.cs`,
  `Core/Cargo/**`, `CargoBodyPort.cs`. Она **не трогает `NetworkCargoPresentation.cs`**, который
  принадлежит идущей ноге луча, и не трогает форму сетевого снимка.
- **Найдено единственное настоящее пересечение и названо в карточке наряда:**
  `GameRulesSettings.asset` — нога A снимает три мёртвые крутилки улёживания и добавляет три живые
  (порог сна, отступ контакта, порог опоры), а `t-loot-models-1` пишет туда же профили визуала.
  **Их нельзя выдавать одновременно.**
- Дизайн ноги получен состязательной проверкой (13 агентов) и содержит поимённый список мест, где
  смена порта обязана сломать сборку.

## state_changes

- `card new t-cargo-sleep-1` — задача, `ready`.
- `card new c-exec-g-5a7c-cargo-sleep-1-001` — наряд в `WIN-U3`, `ready`, с записанным столкновением
  по `GameRulesSettings.asset`.
- `work/2026-08-17-call-cargo-sleep-1.md` — полный текст наряда.

## captures

**Задача привязана к `bet-g-5a7c-wave-5`, и это НЕ значит, что она служит пяти кадрам.** Она им не
служит, и это написано в её карточке первой строкой заметки. Привязка сделана ровно для того, чтобы
карточка не выпала из рабочего набора: карточка без ставки невидима следующей ноге. Закрытие волны
она не гейтит.

Честно: это отступление от правила «задачи служат только активной ставке», сделанное по прямому
слову владельца, и оно помечено, а не спрятано.

## decisions_needed

Ничего. Владельцу остаётся открыть `Gas U3` и вставить пасту.

## play_check

Шаг 1 ✓. Шаг 2 ✓ — владелец не спрашивается по ходу. Шаг 3 ✓ — `call:executor`. Шаг 4 ✓ — правило
входа соблюдено: поверхности измерены по коду, пересечение НАЙДЕНО и названо, а не объявлено
отсутствующим. Шаг 5 ✓.

## log

наряд на ногу A выдан в WIN-U3, и выдача нашла, что порог опоры в проекте уже отвечен

## next

`t-loot-models-1` — только ПОСЛЕ возврата ноги A либо в другой файл настроек. Ноги B, C, D — после A.
