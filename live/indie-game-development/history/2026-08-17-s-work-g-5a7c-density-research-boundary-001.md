# RESULT — s-work-g-5a7c-density-research-boundary-001 (play: work)

## outcome

Его граница вписана в наряд ресёрча жёстко: **ресёрч не входит в пятую волну, не задерживает её
закрытие, не пишет в продуктовый репозиторий и в первой части не занимает слот.**

Последнее — не вежливость, а арифметика: **волна уже перезаписана.** Четыре задачи `ready`
(`t-beam-1`, `t-bodies-intake-1`, `t-room-look-1`, `t-loot-models-1`) на три свободных слота
(`WIN-U1`, `WIN-U2`, `WIN-U4`; `WIN-U3` держит `assetlib`). Слот, отданный ресёрчу, был бы прямым
вычетом из волны — то есть ровно тем, что он запретил.

Поэтому ресёрч разрезан на две части по признаку «нужен ли прогон»:

- **Часть 1 — без слота, сразу.** Чтение кода, документация FishNet и Unity, разбор игр жанра,
  арифметика по уже измеренным байтам, и цена решений А и Б в ногах с прямым ответом, хватает ли А.
  Это ровно то, что он назвал «знать на будущее».
- **Часть 2 — замеры прогоном, требует слота, ждёт волну.** Байты в секунду на 50 / 200 / 500
  предметов, потолок активных тел, потолок рендера. После четверга или раньше, если волна слот не
  займёт.

И записано, что **часть 2 может не понадобиться вовсе**, если часть 1 даёт однозначный ответ — это
законный исход, а не недоделка.

## evidence

- Волна: четыре задачи в `status: ready`, перечитаны из карточек после нарезки.
- Слоты: `gascoop-slot-state.v1.json` — `WIN-U1`, `WIN-U2`, `WIN-U4` = `AVAILABLE`; `WIN-U3` =
  `CLAIMED` под `c-exec-g-5a7c-assetlib-1-001:build`.
- Что часть 1 берёт без прогона, а часть 2 не может — разделено по одному признаку: нужен ли
  запущенный Unity. Частота тактов лежит в настройках и читается; байты в секунду при 500 предметах
  читанием не берутся.

## state_changes

- `card set c-research-g-5a7c-interactive-density-001 note` — граница на карточке.
- `work/2026-08-17-call-research-interactive-density.md` — новый раздел «ГРАНИЦА, ПОСТАВЛЕННАЯ ИМ»
  с его дословными словами и разрезом на две части.

## captures

Наряд теперь несёт и **мину для решения А**, найденную замером: клиент уничтожает вид любого груза,
которого нет в снимке (`NetworkCargoPresentation.Apply` зовёт `DestroyView` для всех не встреченных).
Значит «перестать слать лежащее» требует двух правок, а не одной, и ресёрч не имеет права оценить А
дешевле, чем оно есть.

## decisions_needed

Ничего. Наряд `ready`, часть 1 запускается без слота и без его участия.

## play_check

Шаг 1 ✓. Шаг 2 ✓ — его граница взята дословно и не смягчена. Шаг 3 ✓. Шаг 4 ✓ — проверено, что
разрез на части опирается на признак «нужен ли прогон», а не на удобство. Шаг 5 ✓. G2 соблюдён:
ресёрч остался вне ставки и вне её задач.

## log

граница ресёрча вписана его словами, и разрез на две части — арифметика слотов, а не вежливость

## next

`research` часть 1 по `c-research-g-5a7c-interactive-density-001`, без слота; выдача трёх нарядов
волны в `WIN-U1`, `WIN-U2`, `WIN-U4`.
