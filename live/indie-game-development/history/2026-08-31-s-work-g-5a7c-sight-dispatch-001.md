# RESULT — s-work-g-5a7c-sight-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / bet-g-5a7c-wave-9, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: dispatch — выдана нога зрения на базе, которой до сегодня не существовало, и нарезана девятая задача из четырёх инфраструктурных находок дня

owner_approved: |
  Его слово на девятую задачу дословно: **«Девятая задача, окей, можем девятую взять»**.
  Его слово на эту ногу: **«ok делай»** — в ответ на показанный список из трёх шагов
  (закрыть три карточки, выдать зрение, нарезать девятую).

  Ни одна строка `done_when` задачи `t-sight-thing-1` не изменена этой ногой: она была переписана
  ремонтом `s-repair-g-5a7c-wave-9-amend-001` и с тех пор та же.

outcome: |
  **ВЫДАНА НОГА ЗРЕНИЯ, И ВПЕРВЫЕ У НЕЁ ЕСТЬ БАЗА.** Прибор предыдущей ноги напечатал число, за
  которым волна охотилась двое суток: **хозяин видит `0 из 14` вещей**. Не 8, не 12 — ноль. Слой
  убеждений восьмой волны инертен полностью.

  Напомню, чего это стоило: число «8 из 14» направление посчитало само, вложило в блок для вставки
  без указания авторства, оно ушло владельцу, вернулось от него нарезке как «его замер» — и
  нарезка на этом основании ОТКАЗАЛАСЬ его перепроверять. Теперь число печатает машина, и в
  приёмке прямо написано, что владелец в подсчёте не участвует.

  **ДОКУМЕНТ НАРЯДА ЗАМЕНЁН ЦЕЛИКОМ, А НЕ ПОДПРАВЛЕН.** Прежний (`2026-08-30-call-sight-thing-1.md`)
  написан до прибора: его приёмка просила владельца посчитать вещи и посмотреть на цвет гизмо —
  обе строки неисполнимы (все вещи писали пробу под номером 0; гизмо не рисовались).

  **И НОМЕРА СТРОК ПЕРЕМЕРЕНЫ, ПОТОМУ ЧТО ОНИ СЪЕХАЛИ.** Нога прибора поменяла сигнатуру `CanSee` —
  добавила `HouseholderSightSubject` и `state.CargoId` (это и есть починка нумерации проб). Оба
  места живы, `observer.Y` по-прежнему уходит координатой высоты: `NetworkHouseholder.cs:310` и
  `:324`. Наряд, написанный по старым номерам, назвал бы не те строки — известный класс дефекта
  этого направления.

  **ДЕВЯТАЯ ЗАДАЧА СОБРАНА ИЗ ЧЕТЫРЁХ НАХОДОК ОДНОГО ДНЯ, И ТРИ ИЗ НИХ НАЗВАЛИ САМИ ИСПОЛНИТЕЛИ.**
  Закрывальщик сливает в `dev` раньше проверки; замок уважает только закрывальщик, а площадка
  общая; сводка прогона не записывает, что проверяла; для нескольких ног на нескольких слотах
  штатного пути закрытия нет. Все четыре — не гипотезы: каждая замерена, и каждая сегодня стоила
  времени.

evidence: |
  Продуктовый `main` = `dev` = `origin/main` = **`56a7b88a`**, перечитано направлением.

  - `sight.CanSee(...)` на `56a7b88a`: `NetworkHouseholder.cs:305-311` (ветка «вещи нет на месте») и
    `:319-325` (ветка «вижу вещь»); координата высоты — `observer.Y` на `:310` и `:324`;
  - `CargoState.Y` существует (`Probe/Cargo/CargoState.cs:130`) и не читается ни в одном из двух;
  - команда `why` построена: `Assets/TunnelCrew/DevConsole/DevConsoleCommands.cs:48`;
  - порядок шагов закрывальщика: `close-leg.ps1` — `2/7` merge, `3/7` run the tests;
  - замок: `close-leg.ps1:228` `Enter-IntegrationLock`, объект `gascoop-integration.lock`;
  - `tools/unity-tests.ps1` — вхождений `commit`/`sha`/`rev-parse` ноль.

state_changes: |
  СОЗДАНО (1 задача + 1 наряд + 2 документа):
  - `t-close-safely-1` — девятая задача, полоса `починка`, восемь строк приёмки;
  - `c-exec-g-5a7c-close-safely-1-001` — наряд, слот WIN-U4, база `56a7b88a`,
    документ `work/2026-08-31-call-close-safely-1.md`;
  - `work/2026-08-31-call-sight-thing-1.md` — новый документ наряда на зрение.

  ПЕРЕНАПРАВЛЕНО (1 карточка):
  - `c-exec-g-5a7c-sight-thing-1-001` — `call` на новый документ, `basis` `56a7b88a`,
    `issued` 2026-08-31, слот WIN-U1. Причина замены записана в её журнал.

  НЕ СДЕЛАНО В ЭТОЙ НОГЕ:
  - три карточки опубликованных ног (`instrument`, `gate-headless`, `arrival-hook`) НЕ закрыты:
    их `done_when` перевыверяется отдельной проверкой по байтам. Закрывать по отчётам исполнителей
    сегодня уже трижды оказывалось ошибкой.

captures: |
  - Наряд на зрение несёт раздел «ЗНАЙ ПРО СЕГОДНЯШНИЙ ДЕНЬ» с тремя ловушками: общий склад арта
    через junction, `testrun` без замка, слияние раньше проверки. Это первый наряд направления,
    который передаёт исполнителю не только предмет, но и грабли соседей.

decisions_needed: |
  Ни одного. Слоты названы, база названа, аппетит поднят его словом.

play_check: |
  1. OPEN — состояние `main`/`dev`/слотов перечитано напрямую; база `0 из 14` взята из прибора.
  2. WORK — два наряда написаны, номера строк перемерены на новом `main`, документ старого наряда
     заменён с записанной причиной.
  3. (owner) — «ok делай» на показанный план; «окей, можем девятую взять» на аппетит.
  4. CLOSE — RESULT финальным сообщением, применение и коммит после него.

log: |
  нога зрения выдана на базе, которой не существовало до сегодня — прибор напечатал 0 из 14; и
  нарезана девятая задача из четырёх инфраструктурных находок дня

next: |
  Домой. Идут две ноги: зрение в WIN-U1, закрепление в WIN-U3. Наряд на девятую готов к выдаче в
  WIN-U4.

  Следующим ходом направления — закрытие трёх опубликованных карточек по перевыверенным
  `done_when`, отдельной ногой.
