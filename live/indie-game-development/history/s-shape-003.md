# RESULT — s-shape-003 (shape, 2026-06-13) — g-9c41 shaped into the direction's first bet

```yaml
RESULT s-shape-003 (call: c-shape-003)
direction: indie-game-development   play: shape   node/task: g-9c41
outcome: |
  The direction's FIRST owner-approved bet is committed; g-9c41 -> active. Approach
  chosen by owner: hybrid in ARCH-1v2 edition ("network first, coarse truth = sector
  bands, clip from band state, T2 deferred") — owner picked Variant 1 over old-A+C /
  A-as-was / C-as-was / B(port). Six inherited decisions surfaced explicitly (nothing
  silent); owner reopened the one that was only batch-approved — network vendor — and
  set FishNet as default (his prior research) with a t-1 viability check and P6 fallback
  intact. Owner reframed R5: special gases are FIELD-TRANSPORTED like all gases (same
  simulation), NOT agents/enemies, NOT self-directed motion — distinguished by params +
  visual + effects; AGENT_SUBSTRATE and velocity-intent REMOVED from the concept (this
  simplifies the bet and deletes the most network-novel risk). Owner added R15 (layered
  gas params: shared parent params for all gases + per-meta-gas params + per-meta-gas
  visual procedurally generated from params; specific gas = pure config) and confirmed
  R12-R14 (player-host only; engine-free core / Unity = render; networking as an edge
  wrapper with 3 build modes). The 5 batched arch clarifications (d-arch-001) are all
  answered: (a) cold-rung 1Hz = never-frozen yes; (b) far-consequence latency
  sim<=100ms/perceived 100-300ms yes; (c) 2-3 bands + derived interface = stratification
  yes; (d) SUPERSEDED by R5-reframe — scalar source/sink driver day-one, velocity/agent
  removed not just deferred; (e) perception ceiling answered in the render slice.
  Bet passes G1-G6: appetite 6w/2026-07-24 (G3), done_when+kill_by (G4), cut list with
  10 real cuts (G6), 6 lens verdicts (G6), riskiest-assumption task first (G6), 3 tasks
  <=half-day (G1). Node reworded under G9 (drop "small", +scale criterion 9, vendor ->
  FishNet). Owner's "fast agents / optimistic-then-trim" concern resolved into the bet
  as the early-finish mechanic + pull-forward ladder + rolling wave (not into a padded
  scope, which G3 forbids).
evidence: |
  Owner approvals this session (his words): approach «1 да» (Variant 1 hybrid ARCH-1v2);
  bet «2 да»; node tree-edit Card 1 «3 да»; R5/R15 echo «1 да». Decisions 2/3/4/7/8/9
  «я беру все рекомендации кроме [5,6]»; Decision 6 «(a)». R12 dictated: «никакого
  выделенного сервера, один хост, к нему подключаются игроки... это даже можно не
  исследовать, это 100% так». Vendor reopen: «раньше мы собирались использовать
  fishnet... можем переделать этот ресёрч». R13 source: «Unity — это просто рендер».
  R14 source: «мультиплеер был как wrapper, и я определял... что в тестовой сцене в
  бизнес-логику использовать». R5 reframe: «не стоит их превращать в врагов... особые
  газы, они будут так же распространяться, как газ, но иметь свои прям интересные
  эффекты». R15: «симуляция должна быть параметризированная... какие-то родительские
  будут уже для всех одинаковые... какие-то уникальные для своего метагаза... визуал
  свой... чтобы он мог потом процедурно генерироваться на основе параметров».
  Gate checks: G1 (1 bet, 3 active tasks), G3 (appetite fixed, never extended), G4
  (done_when + kill_by present), G6 (cut list>=1, lens verdict per lens, riskiest-first
  task), G9 (tree edit carries owner_approved). Brief v2 on the table informed, never
  decided.
owner_approved:
  - bet_g9c41: yes («2 да», 2026-06-13)
  - tree_edit_g9c41: yes («3 да» — goal reworded, criterion 9 added, criterion 2 vendor -> FishNet)
  - r5_refined_and_r15: yes («1 да»)
state_changes: |
  live/indie-game-development/TREE.md: g-9c41 status parked -> active; goal reworded
  (drop "small"; "player-hosted listen-server" -> "player-hosted session (one player
  hosts; no dedicated server)"; +"on a core architected for huge procedural levels";
  topology -> "sector-band grid/room"); criterion 2 vendor parenthetical "(Netcode for
  Entities = default vendor; model locked, vendor swappable)" -> "(default vendor
  FishNet — Steam-only, free, GameObject-native; model locked, vendor swappable)";
  +criterion 9 (scale architecture R1, designed+arithmetic-validated, not full content).
  No other node touched.
  live/indie-game-development/NOW.md: active_bet none -> the g-9c41 bet (approach, rules
  R12-R15 + FishNet, cut_list, lens_verdicts, kill_by, early_finish ladder, forecast,
  against); active_tasks += t-1,t-2,t-3 (t-1 ready, t-2/t-3 blocked-chain); decision_inbox
  d-arch-001 cleared (answered); open_calls keeps c-frame-002; next none -> CALL c-work-001
  (work session on t-1).
  live/indie-game-development/LOG.md: append shape line -> history/s-shape-003.md.
  live/indie-game-development/history/: + s-shape-003.md (this RESULT).
  No CHARTER.md change (c-frame-002 still queued separately).
captures:
  - "R5 (refined, owner 2026-06-13): special/anomalous gases are field-transported by the SAME simulation, NOT agents/enemies, NOT self-directed; distinguished by params + visual + effects. AGENT_SUBSTRATE + velocity-intent REMOVED from the concept (not parked) — return only as a new g-d3a8 design decision. Simplifies g-9c41 (deletes the most network-novel risk)."
  - "R15 (owner 2026-06-13): layered gas params — shared PARENT params for ALL gases (density, per-cell packing, spread speed) so no two behave alike; meta-gas group adds OWN params + OWN visual; specific gas = pure config; per-meta-gas visual defined once but PROCEDURALLY GENERATED from params -> feeds g-7e15 (visual identity) and refines g-9c41 done_when criterion 4."
  - "owner idea parked to g-d3a8 (not committed): special gases may trigger physical EFFECTS/phenomena where they spread (e.g. 'fog with hands') — these are consequences in-place via the reaction/event registry, never a separate agent simulation; 'из головы', may or may not happen."
  - "vendor decision: FishNet is the default network vendor (Steam-only, free, GameObject-native, free Steam transport/relay), supersedes the tree's NfE-default; the gas itself rides OUR custom chunked-delta channel regardless of vendor, so swap cost is low; verified empirically in t-1, P6 fallback NGO/NfE without tree change."
  - "owner workflow principle (record for future shapes): AI writes all code fast, so the binding risk is mis-specified/wrong-direction tasks, not typing speed; resolved into rolling wave + early-finish=success + pull-forward ladder, NOT into padded scope (G3). Honest unknown: real agent throughput on THIS project is unaudited — first week of t-1..t-3 gives the fact."
  - "maintenance still pending (3rd occurrence): standing owner requirements R1-R15 have no home in live state; work/maintenance-request-2026-06-12-standing-requirements.md awaits owner launch — bet rules now duplicate them into NOW.md as a stopgap."
  - "Fable 5 window closes 2026-06-22 — t-1..t-3 (hardest net work) are scheduled into it by design; t-3 falls back to Opus 4.8 if it slips past 06-22."
decisions_needed: []
play_check:
  - "1 recite (owner): done — g-9c41 goal/done_when + parent restated across s-shape-001/002, carried; activity-vs-outcome already clean"
  - "2 appetite (owner): done — FIXED 6 weeks to 2026-07-24 (owner s-shape-001 «6 недель, до 24.07»), untouched here (G3); outside-view comparables carried from brief"
  - "3 approaches + pick (owner): done — 4 approaches re-ranked on equal footing post-v2; owner picked Variant 1 hybrid ARCH-1v2 «1 да»; losing approaches (old-A+C, A-as-was, C-as-was, B-port) -> captures/file; owner reopened vendor «fishnet» and reframed R5 in his words (quoted in evidence)"
  - "4 scope hammer: done — 10-item cut list, each with what-breaks; T2 / sector-subdivision / freq-ladder / DA-voxelization+gate / PGG / directed-gases / dynamic-thermics / physical-destruction / matchmaking / save-load (G6 satisfied, >=1 real cut)"
  - "5 lens sweep: done — 6 verdicts: commercial=TASK(clip), depth=not_needed(g-d3a8; substrate proven), co-op-first=TASK(multi-client consistency), feasibility=TASK(riskiest), scope=not_needed(cut list+G3), audience=not_needed(clip feeds parked g-e6f2) (G6)"
  - "6 riskiest assumption: done — ranked; #1 networked chunked-delta consistency under breach load (honest chunk size unknown) -> t-1 first; #2 band solver numerics; #3 FishNet fit; #4 agent throughput; test depth calibrated to 'guarded' (early/cheap/empirical) (G6)"
  - "7 tasks: done — 3 executor tasks <=half-day, riskiest first (t-1 scaffold+net handshake, t-2 stream+breach+measure, t-3 breach-load+stream-lock kill-gate); wave caps at 3 active (G1)"
  - "8 kill criteria: done — metric (host+2-client hash consistency through breach with clamps + dirty-chunk within aggregate clamp), checkpoint 2026-06-30, hard 2026-07-24, next_if_true/false stated (G4)"
  - "9 close (owner): done — whole bet shown one-screen, approved «2 да»; tree-edit Card 1 approved «3 да» (G9); R5/R15 echo «1 да»; this RESULT is the session's final message; agent-CLI becomes its own writer after emitting it"
log: "2026-06-13 — shape (g-9c41 -> active): direction's FIRST bet committed — hybrid ARCH-1v2 (network-first, sector-band coarse truth, clip from bands, T2 deferred); owner picked Variant 1, reopened vendor -> FishNet, reframed R5 (special gases field-transported, not agents) + added R15 (layered meta-gas params); R12-R15 + Unity-as-render / network-as-wrapper locked as bet rules; 5 arch clarifications answered; 3 net-spike tasks (riskiest first: chunked-delta consistency + measure dirty-chunk), kill-gate 2026-06-30, hard end 2026-07-24, early-finish -> bet-2 ladder; node reworded (-'small' +scale criterion 9 +FishNet) under G9. next = c-work-001 (t-1). -> history/s-shape-003.md"
next: |
  CALL c-work-001
  to: session
  direction: indie-game-development
  play: work
  node: g-9c41
  task: t-1
  goal: |
    Task t-1 is met in GasCoopGame: an engine-free C# sim-core scaffold with a 3-mode
    composition root (pure-local / local host-loop / networked host+clients — R13/R14)
    and a networked harness handshake where a FishNet player-host + 2 headless clients
    run a deterministic fixed-tick loop and agree on a per-tick state hash of a trivial
    field over a fixed run, with a recorded FishNet viability verdict.
  context: |
    live/indie-game-development/NOW.md (active bet: rules R12-R15, vendor=FishNet,
    kill_by, t-1 done_when);
    work/research-g-9c41-core-architecture-2026-06-12-v2.md (§3.9 six components, §3.4
    tick phases, §5 day-one stub-not-skip contracts);
    product repo GasCoopGame@236bc30e (foundation accepted 2026-05-23: harness,
    composition seams, validation entry, topology interface);
    os/engineering/CONTOUR.md + os/engineering/PROJECT_SETUP.md (engineering run contract).
  boundaries: |
    First action: confirm GasCoopGame carries a run contract (root AGENTS.md). If not,
    this CALL is repo setup (interactive stack interview) BEFORE any t-1 build — do not
    improvise on an uninitialized repo. Net model locked (P6). Vendor default FishNet,
    but record the viability verdict honestly: a red verdict triggers the P6 fallback,
    not a silent lock-in. Trivial field only — no real gas sim yet (that is
    post-stream-lock, t-2+). Stay inside t-1 done_when; t-2/t-3 are separate tasks.
  done_when: |
    t-1 done_when (NOW.md) verified by a runnable check: headless 3-instance run with
    per-tick hash equality on the trivial field; the core library builds with ZERO Unity
    references; the 3 composition modes selectable at the root; FishNet verdict recorded.
  return: |
    RESULT (executor evidence: commits/PR + check output) advancing t-1 to done, or a
    fast "blocked, because"; next = c-work-002 (t-2) or repair.
  budget: one focused half-day of human-equivalent work (executor sizing rule)
  parent: s-shape-003
  surface: cli (Fable 5 window until 2026-06-22 — t-1 is the hardest net task; run it here)
```

END_OF_FILE: live/indie-game-development/history/s-shape-003.md
