# RESULT — s-work-g-5a7c-scale-4-owner-word-003

play: work · close: light — because всё, что эта нога записала, стоит либо на его словах, сказанных
ей самой и приведённых дословно, либо на закоммиченных байтах, прочитанных её собственной рукой
direction: indie-game-development · node: g-5a7c · task: t-scale-4 ·
call: c-exec-g-5a7c-loot-inactive-until-held-001 (правится до отправки, остаётся `ready`)
date: 2026-08-09
base_read: OS `29c7d3e6`, ветка `wt/indie-game-development`. Продукт
`C:/projects/Unity/GasCoopGame_win-u1` — только чтение байтов `102af1b1`; ничего не менялось, слот не
захватывался, Editor не поднимался.

## outcome

Три его высказывания разведены по трём разным маршрутам, и ни одно не превращено в то, чем оно не
является.

1. **«Про тоннели я знаю, что не будет, это про будущее»** — принято как есть. Тоннельная часть
   объяснения прошлой ноги была лишней; он это и сказал. Ничего не меняется.
2. **«Груз на 0.8 — это не каноничный вариант… явно не 0.8… должно быть относительно реально
   близко»** — это его ВЕРДИКТ по существу, но не приказ делать это здесь: он сам оставил срок
   открытым («не знаю, сейчас или потом»). Ушёл в улику
   `i-telekinesis-carry-distance-is-fixed-001`, а в наряде **изменил устройство** правила преграды.
3. **«У нас есть там проходы… мы можем просто лупить несколько лутов… Это можно в эту»** — прямое
   указание на ЭТУ задачу. Заказано: несколько кусков лута в сцене, все одного размера.

**Главная техническая находка ноги.** Его слово про близкую дистанцию меняет не число, а МЕСТО
условия в правиле преграды. Прошлая нога сняла условие целиком («сплошной всегда»). Это верно
сегодня и станет неверным ровно тогда, когда он подвинет дистанцию: груз в `0.1` м впереди
перекрывается с телом мыши (радиус `0.036` + полудлина груза `0.096` = `0.132`), и сплошной груз
начнёт выталкивать собственного носильщика, пока пружина тянет груз обратно за ним. Правильное
условие стоит не на состоянии лута, а **на мыши: свои держатели проходят сквозь свою ношу, все
остальные — нет.** Сегодня оно ненаблюдаемо, завтра оно и есть разница между «работает» и «качели».
Место готово: `IWalkSpacePort.Resolve` первым аргументом принимает `playerId`.

## evidence

**Слова владельца, дословно, в этой ноге 2026-08-09:**

> Так, ну я как бы знаю, что не наступит в момент, потому что нет тоннеля. Это я как бы на будущее.
> Ещё один момент, то что груз на 0.8 открыт, это не каноничный, так сказать, вариант. Я не знаю,
> сейчас или потом будем делать так, что груз можно будет приблизить, отдалить. Мы можем сделать
> какое-то минимальное расстояние, на которое можно к себе приблизить груз игрок. Но это явно не 0.8.
> То есть это будет, ну, там, это должно быть относительно реально близко. Не 0.8. Так, ну, тоннели.
> Про тоннели, я знаю, что не будет, это про будущее. Но у нас есть там проходы, и ну мы можем просто
> лупить несколько лутов, пускай ещё добавят какой-нибудь побольше лут. Это можно в эту.

**Перемерено первой рукой на `102af1b1`, а не принято на слово:**

| Что | Чем перемерено |
|---|---|
| Несколько кусков лута ничего не строят | `AuthoritativeCargoRoster` (список `_cargo`), `CargoBody.Spawn`/`Find` (список `_bodies`), `CargoSnapshot[]` в `NetworkWalkerCourier`, `NetworkCargoPresentation.Apply` (цикл по снимкам), `CargoContactNoise.Loudest` — всё написано на список любой длины |
| Жёстко задан только спавн | `NetworkWalkerCourier.cs:54-55` — единственный `Cargo.Add`, `cargoId 0` |
| Разные размеры = шов «класс груза» | `CargoRules` строится ОДИН на всю игру и раздаётся всем кускам; `CargoBody.Spawn` берёт `_settings.CargoLength/Width/Height` для каждого тела; `CargoGeometry` считает по `rules.HalfLength/HalfWidth`. Поштучные размеры — это `t-scale-7`, вырезанный из волны |
| Доставка смотрит только на первый кусок | `CarryWorld.Advance`: `Deliveries.Observe(Cargo.GetState(0))` |
| Место для условия «свой держатель» уже есть | `IWalkSpacePort.Resolve(int playerId, …)` — спрашивающая мышь известна ответу по построению |
| Перекрытие при близкой дистанции | `_walkerRadius: 0.036` + `_cargoLength/2 = 0.096` = `0.132` м против названной им «реально близкой» дистанции |
| Проёмы дома | `ThreeRoomHouse.prefab` + сцена: перегородки `x = ±2.5` дают проём `z ∈ [−1, 1]` = **2 м**, восточная стена — проём `z ∈ [−1.5, 1.5]` = **3 м** |

## Что вынесено владельцу, а не решено ногой

**«Пускай ещё добавят какой-нибудь побольше лут»** НЕ выполнено и вынесено отдельным решением. Два
основания, оба конкретные:

1. Размеры и масса сегодня ОБЩИЕ на всю игру. Кусок другого размера требует поштучных размеров —
   это шов «класс груза», прямо вырезанный из этой волны (`t-scale-7`) и прямо запрещённый границей
   этого наряда. Он тянет за собой `CargoRules`, `CargoGeometry`, спавн тела, выбор при наведении и
   показ.
2. Подписанная строка узла `g-5a7c` исключает «телевизор и любой крупный груз». Насколько его
   «побольше» — это та самая строка, знает только он.

Несколько кусков ОДНОГО размера ни того ни другого не трогают и сделаны сразу.

## Правка наряда, сделанная этой ногой своей рукой

| Место | Что стало |
|---|---|
| раздел «преграда есть У ЛЮБОГО ЛУТА» | условие на состояние лута снято окончательно; введено единственное исключение на МЫШИ — свои держатели проходят; основание — его слово про близкую дистанцию, с арифметикой перекрытия |
| новый подраздел «НЕСКОЛЬКО КУСКОВ ЛУТА В СЦЕНЕ» | его слова целиком; заказ; измеренные проёмы `2 м` / `3 м` против лута `0.192` м; доказательство, что путь уже готов на список; два ограничения — один размер и доставку не трогать |
| `context` | «груз в сцене ровно один» заменено на факт базы плюс указание, что эта задача заводит несколько |
| проверка происхождения | три новые строки с источниками: преграда у любого лута, исключение на держателя, несколько кусков |
| `owner-visible check` | п.4 дополнен («носильщик свою ношу не цепляет»), добавлен п.5 про несколько кусков и отсутствие сплошной стены, остальные перенумерованы |
| `headless evidence` | доказуемая форма уточнена: преграда не зависит от состояния лута, но свои держатели проходят |
| `boundaries` | телекинезная граница усилена его же словами (0.8 неверно, но здесь не чинится); добавлены «все куски одного размера» и «доставку не трогать»; «живой ударил неактивный» переписано под то, что случай впервые стал возможен |
| `done_when` строка 2 | исключение на мыши + несколько кусков одного размера + запрет шва «класс груза» |
| `return` | требует назвать место исключения, число и места кусков, и что класс груза не заведён |
| `budget` | добавлены несколько кусков одного размера |

## state_changes

- `c-exec-g-5a7c-loot-inactive-until-held-001` остаётся `ready` и НЕ отправлен; исправлен на месте
  под три его высказывания. Второй наряд не выпускается.
- `i-telekinesis-carry-distance-is-fixed-001` обновлена: его вердикт 2026-08-09 приведён дословно;
  запрет ссылаться на «всё в принципе работает» как на одобрение `0.8` сделан прямым — он сказал
  обратное; названо связанное последствие про место исключения преграды. Улика остаётся ОТКРЫТОЙ,
  маршрут `review` не менялся.
- `t-scale-4` остаётся `open`; `done_when` карточки не редактировался.
- `g-5a7c` остаётся `active`; `NOW.bet` не менялся; карточка узла не редактировалась, кроме строки
  журнала.

## captures

- Доставка засчитывается только по первому куску (`Cargo.GetState(0)`). С несколькими кусками это
  впервые становится заметно. Не дефект этой задачи, но первая же нога про заход это встретит.
- «Живой груз ударил неактивный» впервые становится физически возможным. Наряд запрещает строить под
  это механику и требует назвать, если проявится.

## decisions_needed

Два, оба вынесены ему прозой с вариантами и рекомендацией, оба НЕ блокируют отправку наряда:

1. **Кусок побольше** — сейчас (открывает шов «класс груза», вырезанный из волны, и упирается в
   подписанную строку «любой крупный груз») или следующей волной. Рекомендация: следующей волной.
2. **Число `0.8`** — подвинуть сейчас как чистую настройку под его «реально близко» (тогда исключение
   на держателя из ненаблюдаемого становится несущим на этом же прогоне, и связка чисел переноски
   идёт под его глаз) или оставить до работы про приближение/отдаление. Рекомендация: оставить.

## play_check

- 1 recite: done — три его высказывания разобраны по отдельности против живой карточки `t-scale-4`,
  границ ставки и улики про дальность.
- 2 owner inputs (owner): done — его слова получены здесь дословно и приведены целиком.
- 3 do the work: done — наряд исправлен в девяти местах, улика обновлена, два решения подготовлены
  к выносу.
- 4 self-check: done — каждое утверждение о том, что «несколько кусков ничего не строят», проверено
  по коду; коллизия «кусок побольше» проверена по `CargoRules`/`CargoBody`/`CargoGeometry` и по
  подписанной строке узла, а не заявлена.
- 5 close: done — наряд остаётся `ready`; задача открыта; два решения у владельца.

## Гейты

- **G7.** Ни одно его высказывание не расширено и не сужено: приказ («несколько лутов… можно в эту»)
  выполнен, вердикт («0.8 не каноничен») записан уликой без работы, оговорка про срок («не знаю,
  сейчас или потом») сохранена как открытая, а спорная часть («кусок побольше») вынесена ему, а не
  решена.
- **G3.** Аппетит не растянут: несколько кусков одного размера — настройка сцены; шов «класс груза»
  и переменная дальность прямо запрещены в наряде.
- **G9.** Карточки узла и задачи не редактировались — только строки журналов. Подписанная строка
  узла про «любой крупный груз» не тронута и названа как основание вопроса.

## log

три его слова разведены: несколько кусков лута заказаны в эту задачу, «0.8 не каноничен» ушло уликой без работы, а кусок побольше вынесен решением — плюс исключение преграды переставлено с состояния лута на мышь, чтобы будущая близкая дистанция не выталкивала носильщика

## next

`CALL c-exec-g-5a7c-loot-inactive-until-held-001` — отправить исправленный наряд исполнителю,
слот `WIN-U1`; его ответ на два решения меняет наряд до отправки, если он его даст. После HOME
отдельная нога Направления читает возврат первой рукой.

END_OF_FILE: live/indie-game-development/history/2026-08-09-s-work-g-5a7c-scale-4-owner-word-003.md
