# RESULT s-converge-verify-october-demo-basis-v3-001

direction: indie-game-development
play: converge-verify
node: g-12fd
call: c-converge-verify-october-demo-basis-v3-001
verify_target: specification
date: 2026-07-26

## outcome

FAIL. The exact artifact `work/october-demo-basis-v3.md` (identity
`october-demo-basis-v3`) does not survive the independent attack. Eight findings
V1–V8 are named and none is filled, repaired or decided here. They return to the
owner-authority `work` contour as
`work/c-work-october-demo-basis-v4-revision-001-call.md`. Verification reruns
against v4. `g-12fd` stays parked, no shape is opened, no `§SIGNOFF` line was
written.

No owner decision was reversed, reinterpreted or re-litigated. Procedural
generation stays IN and mandatory. Technique stays OUT of the Basis. The two hall
mechanics stay deferred. Terminology stays «вещество». The provisional first-slice
decisions stay explicitly not-law. The engineering read stays in
`i-procgen-determinism`.

WHAT PASSED, including the CALL's designated sharpest tension. **F8/v2's disclosure
question is judged DISCHARGED.** One non-technical risk line under the MUST list
(«пункт 5 — единственный, который зависит от внешнего инструмента и
неподтверждённой совместимости движка. Инженерная читка ... идёт отдельно и до
первой сборки уровня») plus a live engineering read that carries the owner's full
risk list in his own words is an adequate home for technical HOW; demanding the
technique back inside the Basis would reverse his order, and declaring MUST 5
unbuildable remains an engineering verdict, not a specification verdict. The
disclosure line has two defects of its own (an unsupported superlative, and two of
his four risks dropped) which are named inside V3, but the *place* is right and the
question does not reopen. Also surviving the attack: the audience filter; §4's
refutation test and its single support, which is genuinely MUST-backed and
genuinely requires two players at once because MUST 4's «отпустил — уронил» makes
two simultaneous holds impossible for a sequential controller; the CUT list; the
«ПОТОМ РАССМОТРЕТЬ» category; the dormancy device itself (honest, and it answers
F4 correctly); F6's fifth binding and two new invalidators; F10's fresh
release-reality receipts, all five of which reconcile against `NOW.md`, `TREE.md`
and `CHARTER.md`; F1/F2's deletion, which removed the misattribution completely and
leaves `g-12fd` done_when atom A2.6 PASSING for the first time; and the whole
legacy-authority half of the artifact, re-checked and clean.

Three roots produce the eight findings.

1. **The readable moment was never mechanically required, and v3 newly asserts
   that it is.** §3 is owner content carried verbatim since v1; none of its three
   physical dependencies is in MUST. → V1, the one large finding.
2. **The mechanically delegated repair round landed item-scoped, and its repairs
   were not checked against each other.** → V2, V6, V7, V8. Two of those are the
   *same point* failing a second time, so KERNEL §2 two-strikes is live on each.
3. **The F1/F2 deletion removed more than technique, and the not-law section it
   introduced is not inert.** → V3, V4. V5 is an independent gap in MUST 7.

## evidence

SESSION INDEPENDENCE. Fresh session. Neither the v1 authoring chat, the v2
revision chat, the v2 verification chat nor the v3 revision chat. The v3 revision
receipt `history/2026-07-26-s-work-october-demo-basis-v3-revision-001.md` was
**not read at all** — its existence was confirmed by a directory listing and
nothing more. The CALL forbids reading that leg's reasoning, and it was not
needed: the owner's five verbatim decisions are reproduced in the artifact's own
final section, and the revision CALL's `disposition:` block is a live `work/` file.
The v2 verification receipt was read; the CALL names it as authority. This leg was
run single-agent: `converge-verify` is not one of the plays that mandate in-leg
fan-out, and the binding refutation is this session itself.

FRESH STATE. Worktree HEAD `b9c7ad25` equals `origin/main` exactly — zero ahead,
zero behind — verified before any read. No merge or rebase was needed and the
attack ran against current committed state.

SOURCES READ. The CALL's closed authority list: `CHARTER.md`; `TREE.md`
(`g-12fd`); `NOW.md`; `knowledge/canon-clean-authority-reset.md`;
`knowledge/strategy-reset-boundary.md`; `work/concept-frame-v1.md`;
`work/pgg-analysis-2026-07-10.md`; `work/october-demo-basis-v3.md`;
`history/2026-07-26-s-converge-verify-october-demo-basis-v2-001.md`; this CALL.
Plus `os/KERNEL.md` and `os/plays/converge-verify.md` as procedure, and five
bounded reads the binding done_when made necessary, each live-repo state or
history and none of them `archive/**`:
`work/c-work-october-demo-basis-v3-revision-001-call.md` (the CALL declares its
done_when a live file needing no quoting, so it had to be read; its `disposition:`
block is the only place the ten v2 dispositions are attested outside the receipt);
`work/oracle-demo-boundary-specification-v1.md`; the v2 blob
`git show c982c79a:...october-demo-basis-v2.md` (the carry-forward atom cannot be
verified without the diff, and the author's own reconciliation cannot substitute
for it — same bounded read the v2 verification made of v1); the v1 and v2
tombstones; and existence checks on `LOG.md`, `history/`, the commit and the Steam
screenshot receipt.

NO LEGACY READ. `archive/**` was not opened and neither was the frozen canon
repository `C:\projects\gas_coop_game_canon`. The archived PGG spike close receipt
was not read and no authorization for it exists — and after F1's deletion nothing
in v3 needs it.

INDEPENDENT DIFF v2 → v3. Run directly against the v2 blob, not against the
changelog. Every delta the owner's five decisions produce is present. MUST 1, 2, 3,
4, 6, 7, 8 are **byte-verbatim** from v2, so they stand under v2's «да» exactly as
the CALL claims. §3, the SHOULD list, «ПОТОМ РАССМОТРЕТЬ», CUT, falsifiers 1–4 and
6, the audience section, the terminology note, the unverified-status blockquote and
the canon-evidence list are byte-verbatim from v2. §4's «Запрещено» was reworded
from v2's assertive «Двое нужны из-за физической ситуации» to v3's conditional
«Если двое и нужны, то из физической ситуации» — a real semantic change, and the
correct one under his words «Требование „обязательно двое“ из Основы убрать».
**One undeclared residue was found by the diff and by nothing else:** §1's and §2's
«двадцать минут» survive verbatim from v2 while the artifact twice claims the
number now lives only in open question 7. That is V2. No other undeclared semantic
edit exists.

DISPOSITION CHECK. Every disposition the CALL claims for F1–F10 was checked against
the artifact text rather than accepted. F1, F2, F5, F6, F9 (partially), F10 landed;
F3's reduction to one support landed and the support holds; F4's dormancy landed;
F7's two named items moved. F9 landed in one sentence and not in the section (V2).
F5's repair collides with F4's (V6). F7's repair was item-scoped (V7). D1 is applied
correctly: the CALL states no count its own itemization contradicts, and the
artifact's counts all reconcile — «опора … одна» (one), «пять вещей» (five
bindings), «два новых отменителя» (two), «Пять решений владельца» (five), open
questions itemized 1, 2, 3, 5, 6, 7, 8 with 4 explained. The one count that does
not reconcile is «Один риск назван прямо здесь» against the owner's four-item risk
list, which is inside V3.

## atomic clause map

Set A — `TREE.md` `g-12fd` done_when, split atomically.

| Atom | Requirement | Artifact section | Verdict |
|---|---|---|---|
| A1.1 | минимальное обещание игроку | §1 | FAIL — the promise section still asserts «двадцать минут», unbacked and untested (V2) |
| A1.2 | путь начало → опасность или задача → совместное действие → понятный результат | §2 | PASS on the «или задача» disjunct and on the joint-action stage, which is now fully MUST-backed; FAIL on the result stage — «не успели» has no mechanism (V5) |
| A1.3 | один читаемый момент | §3 | PASS as stated; FAIL on smuggling — trigger, precondition and payoff are all outside MUST and the artifact asserts the opposite (V1) |
| A1.4 | проверка причинного сотрудничества | §4 | PASS — one support, MUST-backed, requires simultaneity, test not circular. F3 resolved. |
| A1.5 | предел публичных обещаний | §5 | FAIL — «Можно» still permits «абсурдный мир» (V7); the danger prohibition rests on the not-law section (V4) |
| A2.1 | обязательное явно | §6 MUST | FAIL — art is content MUST 5 requires and it is nowhere in the partition (V3) |
| A2.2 | желательное явно | §6 SHOULD | PASS as a list; the readable moment's dependence on SHOULD is carried under V1 |
| A2.3 | вырезанное явно | §6 CUT | PASS as a list; CUT's «таймер снаружи» against MUST 7 is carried under V5 |
| A2.4 | исключения явны | «Исключения» null paragraph | FAIL — the null is contradicted by the dormant falsifier (V6). F5's atom fails a different way, not the same way. |
| A2.5 | признаки опровержения явны | 6 falsifiers, one dormant | PASS as a list and PASS on the dormancy device; falsifier 4's subject is unbuildable, which is V1's consequence, not a defect of the falsifier |
| A2.6 | старый Canon и прежние возможности не получают автоматического места | MUST 5 + «На чём это стоит» guard | **PASS — first time.** F1's deletion is complete; no claim rests on a legacy file's former accepted status; the guard sentence is no longer violated by its own MUST list. |
| A3.1 | Основа связана со свежими release-reality receipts | «Свежие receipts календаря и Steam» | PASS — five receipt classes named with sources, all reconciled against `NOW.md`/`TREE.md`/`CHARTER.md`, screenshot receipt exists on disk, and the artifact states its own empty build binding against the 2026-09-03 gate. F10 resolved. |
| A3.2 | новая версия отменяет зависимые доказательства до повторной проверки | «Что отменяет зависимые доказательства» | PASS on the rule and on MUST 5's three new dimensions; FAIL narrowly — a non-law decision can change the ceiling and the collection mechanic with no invalidator (V4, second half) |

Set B — `work/c-work-october-demo-basis-v3-revision-001-call.md` done_when.

| Atom | Verdict |
|---|---|
| v3 exists as the single live Basis, identity `october-demo-basis-v3` | PASS |
| carries v2 forward unchanged except the deltas the owner's words produce | PASS by independent diff, with one exception: the «двадцать минут» residue is a delta his words ordered and the text did not deliver (V2) |
| v2 becomes a tombstone naming v3 and the commit where its full text lives | PASS — names v3, `c982c79a`, the verification receipt and the revision leg; v1's tombstone was also retargeted |
| every finding F1–F10 dispositioned explicitly in the RESULT | PASS as attested by the CALL's own `disposition:` block; the RESULT-side check is not verifiable in this leg because the receipt may not be read, and it is left to the narrow review. Artifact-side landing of each disposition was checked and is reported above. |
| no finding resolved by an agent choosing content | PASS with one qualification: no *content* was chosen, but two repairs chose *scope* — F7's was narrowed to the two named items and F9's to one sentence (V7, V2) |
| no owner decision from v1 or v2 reversed without his words | PASS |
| `NOW.md` holds exactly one fresh `c-converge-verify-october-demo-basis-v3-001` against the exact v3 identity, carrying the caveats and the ten dispositions | PASS — and the CALL is unusually good: it named its own attack surface, ordered the §3 WEAK attacked, and warned against reusing the v1 oracle. All three instructions produced findings. |
| `i-procgen-determinism` and `i-demo-scope-cap` updated from what actually changed | PASS; `i-procgen-determinism` is accurate and was left untouched by this leg |
| `bet: null`, `tasks: []`, no lane, no numeric chance | PASS |
| one LOG line, full RESULT in `history/`, `END_OF_FILE:` trailers, one commit | PASS — LOG line present, receipt present, trailers verified on every touched file, single commit `b9c7ad25` |
| `g-12fd` stays parked, no shape, TREE unchanged | PASS |

## smuggling audit

Weight-bearing non-owner assertions required by done_when, each with its named
support and verdict. Owner content decisions are supported by his words and are not
attacked as content.

| Claim | Named support | Verdict |
|---|---|---|
| «физическое требование, от которого зависит читаемый момент, целиком живёт в MUST 4 („Большой шар один через проём не проведёт“)» | MUST 4 | **FAIL.** MUST 4 says one person cannot manage what two can. It does not say a ball can be too big for a doorway, does not give the container deformation, does not make contact burst it and does not say a burst releases anything. «Целиком» is false, and it is the stated support for deleting the doorway-width parameter (V1). |
| §3 «Шар набран и слишком велик» | no MUST states size grows with fill; the only volume text is the not-law provisional «ёмкость … набирается больше» | FAIL (V1 trigger). |
| §3 «Протискивают» | SHOULD «настоящая мятая физика тары» | FAIL — a MUST-only build cannot produce it (V1 precondition). |
| §3 «Задели косяк. Хлопок. Вещество выходит здесь» | MUST 3 «лопнуть»; MUST 4 «отпустил — уронил» | FAIL — neither establishes contact-triggered bursting or release-on-burst. Recorded WEAK by the v2 verification and ordered attacked by this CALL (V1 payoff). |
| §1 «Длительность захода в обещании не называется» | the same section, two sentences later, «Чем заполнены двадцать минут…»; §2 likewise | FAIL — self-contradiction, plus two false self-claims that the number lives only in question 7. No falsifier tests session length (V2). |
| «пункт 5 — единственный, который зависит от внешнего инструмента и неподтверждённой совместимости движка» | none named | FAIL as a universal claim — MUST 6's networking is nowhere examined in current authority — and it names two of the four risks the owner listed, dropping «ноль прецедентов связки» and «арта ноль» (V3). |
| MUST 5 «Процедурная генерация уровня из модулей» — the modules themselves | nothing in MUST/SHOULD/deferred/CUT mentions art or assets | FAIL for partition completeness. Art is content, not technique, so his technique-out order does not cover it; `pgg-analysis` «Арта НОЛЬ» and his own «арта ноль» are the facts, and the F1/F2 deletion removed the artifact's only trace of them (V3). |
| §5 «„настоящая опасность“ не показуема» because «урона и смерти в первом куске нет» | the provisional, explicitly not-law first-slice decision | FAIL — a public prohibition inside a done_when deliverable grounded in something the artifact says changes without a new Basis version (V4). |
| «ни один MUST, ни один этап пути, ни одна опора раздела 4 и ни одно публичное обещание на них не опираются» | artifact's own inertness claim | FAIL on the fourth conjunct only; the first three hold up (V4). |
| MUST 7 «сдан / **не успели**» | no MUST supplies a clock or an escalating hall; «таймер снаружи» is CUT, both hall mechanics are deferred, session length is question 7 | FAIL (V5). |
| «Основа не объявляет ни одного случая, в котором она сознательно не действует» | contradicted by falsifier 5's «не проверяется и сработавшим не считается» | FAIL (V6). |
| §5 «Можно: … абсурдный мир» and «аварийная уборка» | no MUST touches setting, art or fiction; object and biome are open question 5 | FAIL, and it is the same class §5 itself gates correctly for tone under «Условно» (V7). |
| «Техника вынесена из Основы … `work/pgg-analysis-2026-07-10.md` здесь больше не цитируется» | contradicted three paragraphs later by decision 2's verbatim quote, which names the plugins, the pipeline, the same file and the kill-risk list, prefixed «не в Основу» | FAIL (V8). |
| §4's single support: «Большой шар физически не проходит там, где пройдёт один человек: его несут вдвоём с двух сторон» | MUST 4, both sentences | **PASS.** MUST-backed and genuinely simultaneous: «отпустил — уронил» makes two concurrent holds unreachable for a sequential solo controller, which is exactly what the refutation test measures. |
| §2 «оно мешает нести» | MUST 2 «тянет вниз / рвётся вверх … действует и на шар», MUST 4 | PASS. The route-blocking and punishment claims that failed in v2 are gone. |
| The five receipts under «Свежие receipts» | `NOW.md`, `TREE.md`, `CHARTER.md`, the screenshot file, the Valve URL | PASS. Dates, fee/credit/AppID state and the Next Fest page-before-registration requirement all reconcile; the screenshot exists on disk. |
| The evidence tuple's five bindings and seven invalidators | artifact text vs the current MUST list | PASS for MUST-driven dimensions; the non-law gap is V4's second half. |
| «Ни один из этих файлов не получает власти … Каждое положение выше стоит на собственном доводе, а не на прежнем статусе принятия» | artifact guard | PASS — and unlike v2, nothing in v3 violates it. |

Legacy-status-only support: none. The bounded canon read stays evidence with zero
authority exactly as `i-canon-repo-evidence-read` requires; `CORE.md`'s «мне
нравится» and `c-002`'s «ADMIT TO CANON» are cited as history and no claim rests on
them. That half of the artifact is clean and improved over v2.

## oracle used, and how it was extended

`work/oracle-demo-boundary-specification-v2.md` — CREATED this leg. The play forbids
relying only on the author's sources and the CALL forbade reusing v1 unchanged. What
was done, stated exactly: the **first principle was re-derived independently** before
the artifact was attacked; **O1–O12 were kept as a test set but each was re-applied
from the artifact text rather than from v1's recorded verdicts**; and **O13–O17 are
new**, for the four surfaces v1 cannot test — a section that declares itself not
law (O13), a dormant falsifier (O14), an explicit null (O15), text repaired by an
agent under owner delegation (O16), and self-description versus body (O17). O16 and
O17 were not named by the CALL and are this leg's own addition; between them they
carry four of the eight findings.

Re-applying rather than trusting produced a correction to v1's own record, which is
the concrete proof the CALL's warning about inherited blind spots was right: v1
recorded that **O4 and O7 passed**, and both in fact failed, in v2 as well as v3.
§3's three dependencies were already outside MUST in v2 verbatim, and v2's SHOULD
list already offered as optional a second substance behaviour that its own MUST 2
mandated. v1 is left in place with one `superseded_by:` line so a future review
promotes v2 rather than the weaker file; neither has authority until promoted.

Honest statement of the guarantee, per the play's note: every citation in the
artifact was resolved against its source and the mismatches are named above, so
cited openings are structurally excluded. This oracle attacks uncited assumptions.
It is rigorous, not a closed proof. No competitor precedent was used: none exists
inside current authority for this node class, and inventing one would be a
fabricated precedent.

## findings

Full text, named support, reason and whose part, for each of V1–V8, is written out
in `work/c-work-october-demo-basis-v4-revision-001-call.md`. Summary, most severe
first:

- **V1** The readable moment of §3 — required by `g-12fd` done_when 1 and by
  `CHARTER.md` §2 — has none of its three physical dependencies in MUST: the
  trigger needs ball size to grow with fill (nowhere), the precondition needs
  deformation (SHOULD only), the payoff needs contact-triggered bursting and
  release-on-burst (nowhere). MUST 5's deletion note asserts the opposite in a
  named, resolvable, false claim. Falsifier 4 fires by construction on any
  MUST-only build.
- **V2** §1 and §2 still assert «двадцать минут» while §1 says the duration is not
  named and the artifact twice claims the number lives only in open question 7. No
  falsifier tests session length. **Second round on F9's point — KERNEL §2
  two-strikes.**
- **V3** Art does not exist and appears nowhere in MUST/SHOULD/deferred/CUT, while
  MUST 5 requires a level assembled from authored modules. The F1/F2 deletion
  removed the artifact's only trace of «арта ноль», and the replacement disclosure
  line claims MUST 5 is the *only* externally dependent item while naming two of
  the owner's four risks. Scope, not feasibility; content, not technique.
- **V4** The not-law provisional section is not inert: §5's prohibition of
  «настоящая опасность» rests solely on the provisional «урона и смерти нет», so
  the public-claim ceiling changes without a new Basis version — and the
  provisional collection mechanic changes with no invalidator.
- **V5** MUST 7's «не успели» branch, and with it the path's result stage, has no
  mechanism: an imposed clock is CUT («таймер снаружи»), both escalating-hall
  mechanics are deferred, and session length is open question 7.
- **V6** The explicit null-exceptions statement is contradicted by the artifact's
  own dormant falsifier — two mechanical repairs from the same round, each
  defensible alone, colliding because neither was checked against the other.
- **V7** §5's «Можно» still permits «абсурдный мир» (and «аварийная») that no MUST
  produces, while the structurally identical tone claim is correctly gated under
  «Условно» one paragraph below. **Second round on F7's point — KERNEL §2
  two-strikes**; the repair moved the two named items instead of applying §5's own
  rule to the list.
- **V8** The artifact reproduces the technique it declares removed — plugin names,
  the pipeline and the kill-risk list — inside a verbatim quote whose first three
  words are «не в Основу», and cites `work/pgg-analysis-2026-07-10.md` three
  paragraphs after stating it is no longer cited there.

TWO-STRIKES, stated as the CALL requires. V2 and V7 are not new findings; they are
F9 and F7 failing a second time on the same substance. In both cases the repair
addressed the instances the finding named rather than the rule the finding
implied. This session is already the fresh chat KERNEL §2 asks for; the operative
consequence is for the next round: fix the rule, state the rule in the artifact,
and let the next verification check the rule rather than hunt for a third
instance. A third round on either point is a KERNEL §2 stop, not another finding.

PROCESS OBSERVATION, not a finding against the owner or the artifact. Four of the
eight findings (V2, V6, V7, V8) come from the mechanically delegated repair class,
and none of them is a content mistake — they are scope-of-repair and
cross-repair-consistency failures. The owner's delegation was explicit, correct and
time-bounded; what the round lacked was a pass that read the assembled artifact
against its own self-claims and against the other repairs. Oracle O16 and O17 exist
to catch exactly that, and the returning CALL asks for it. This is recorded as a
capture, not acted on.

## state_changes

1. `work/oracle-demo-boundary-specification-v2.md` — CREATED. Extended node-class
   oracle: first principle re-derived, O1–O12 re-applied from the artifact text,
   O13–O17 new for a not-law section, a dormant falsifier, an explicit null,
   agent-repaired text and self-description. PROPOSED for promotion by review or
   pulse; no authority until promoted.
2. `work/oracle-demo-boundary-specification-v1.md` — one `superseded_by:` line
   added, so a future review promotes v2 and does not inherit v1's incorrect
   O4/O7 verdicts. Nothing else in the file was touched.
3. `work/c-work-october-demo-basis-v4-revision-001-call.md` — CREATED. The
   owner-present `work` CALL carrying all eight findings with exact artifact text,
   named support, the two live two-strikes points, the carried-forward caveats and
   an honest effort split. Self-contained.
4. `work/c-converge-verify-october-demo-basis-v3-001-call.md` — a `disposition:`
   block appended: discharged, FAIL, receipt, successor, and which claimed
   dispositions landed. Spent, not withdrawn.
5. `NOW.md` — `updated:` to this leg. `direction_forecast.basis`: one clause added
   recording that two Basis versions have now failed verification; the numeric
   chance stays illegal and unpublished. `direction_forecast.drivers[0]`: rewritten
   to the current frontier — v3 failed with eight findings, F8's disclosure
   discharged, the three roots named, the effort split stated, and the route now
   v4 revision → fresh verification → narrow review → shape of `g-37a1`.
   `issues.i-demo-scope-cap`: updated to two failed verifications, twelve days to
   2026-08-07, the boundary now two legs away rather than one, and V3's art gap
   named as the finding most likely to matter for a cut; evidence retargeted.
   `open_calls`: the spent verification row replaced by exactly one
   `c-work-october-demo-basis-v4-revision-001`. `bet: null`, `tasks: []`,
   `decisions: []`, `recurring: []` unchanged.
6. `LOG.md` — one line prepended.
7. `history/2026-07-26-s-converge-verify-october-demo-basis-v3-001.md` — this
   RESULT.

Not touched: `CHARTER.md`, `TREE.md`, `knowledge/**`, the artifact
`work/october-demo-basis-v3.md`, the v1/v2 tombstones, `work/concept-frame-v1.md`,
`work/pgg-analysis-2026-07-10.md`, `issues.i-procgen-determinism` (accurate as
written), `i-frontier-knowledge-stale`, `i-concept-frame-admission`,
`i-canon-repo-evidence-read`, `i-steam-appid`, `i-steam-sequence-tree`, product,
Steam state, the frozen canon repository, `archive/**`. `g-12fd` stays parked with
no bet, tasks, tracks or shape. No numeric release chance was published. No
`§SIGNOFF` line was written, because the artifact did not pass.

DECLARED DEVIATION, same class and same reason as the v2 verification's. The play
lists `NOW.md` (decisions, open_calls) and `LOG.md` as this play's writes. Two
further `NOW.md` fields were also updated: `direction_forecast` and
`issues.i-demo-scope-cap`. Neither is a decision by this leg — both are receipts of
this verification's own outcome. `i-demo-scope-cap`'s `review_when` is literally
"earlier on the day a Demo Basis version passes converge-verify", which resolved
today in the negative, and its body asserted that v3's verification was still open.
The forecast driver named the v3 verification as the current frontier, which it no
longer is. Leaving either as written would have left the direction asserting a live
frontier that is spent. No new issue was created, no route was changed and no
issue's `review_when` was moved.

## captures

- Four of eight findings are defects of the mechanically delegated repair round,
  and none is a content error. The reusable lesson is a cheap one — after a
  delegated repair round, read the assembled artifact against its own self-claims
  and against the other repairs in the same round before closing it. Worth one
  FRICTION entry if it recurs; `os/MAINTENANCE.md` needs two matching entries and
  this is the first. Not acted on here.
- `work/oracle-demo-boundary-specification-v2.md` is a candidate cross-node
  checklist for every future `outcome_kind: specification` node in any direction.
  O16 (delegated repair is bounded) and O17 (self-description matches the body) are
  the two tests with the widest reach beyond this node class. Promotion is review's
  or pulse's call, and v1 must not be promoted in its place.
- `knowledge/strategy-reset-boundary.md` still names
  `c-work-october-demo-basis-authoring-001` the sole lawful frontier. Recorded as
  D2 by the v2 verification, still stale, now three frontiers out of date.
  `converge-verify` never writes `knowledge/`; it stays `i-frontier-knowledge-stale`
  for repair or the narrow review.
- §1's «Всё, что делает один, физически меняет пол и воздух под ногами остальных» is
  a universal quantifier where §5's calibrated version says «действия каждого
  физически меняют ситуацию остальных». Attacked, not raised: it is twice-approved
  owner content in a promise register, and §5's version is the one that has to be
  showable. Noted so a future round does not treat it as an oversight.
- MUST 1 is unsatisfiable without MUST 5 — «Триггеры, таймеры и устройства допустимы
  только как то, что размещает генератор» silently becomes "no triggers, timers or
  devices at all" if generation is ever cut. Attacked, not raised: a Basis need not
  pre-plan its own cuts. Relevant only if the 2026-08-07 boundary fires.
- §2's «Начало» carries «Он загажен и всё ещё работает»; nothing in MUST makes the
  object functional, though MUST 1's generator-placed devices could. Attacked, not
  raised: it is scene-setting, not a mechanism the stage rests on.

## decisions_needed

None from this leg. Verification routes findings and decides nothing. The eight
findings are the returning CALL's business and the owner's words are collected
there, not here.

## play_check

verify_target: specification complete=FAIL smuggling=FAIL

- Step 1 recite: done. Both specification attacks stated — every atomic done_when
  clause satisfied by the exact artifact, and no required claim resting on an
  unresolved assumption. `verify_target: specification` named.
- Step 2 completeness: both binding done_when sets split atomically and mapped to
  exact artifact sections. Set A has six failing atoms and one passing only on a
  disjunct; A2.6 passes for the first time. The oracle was extended, not reused —
  first principle re-derived, O1–O12 re-applied from the text, O13–O17 authored —
  so the leg was not BLOCKED, and re-application corrected two of v1's own
  verdicts. Uncovered atoms are named, never filled.
- Step 3 smuggling: every weight-bearing non-owner claim required by done_when has
  its support named and resolved, or is marked untraceable. Nothing was repaired,
  no content was chosen, no agent recommendation and no mechanical repair was
  treated as owner approval. No imported counterpart contract existed. No `→ PLAN`
  move applies — this is a specification target, not a build spec. The owner's
  decisions were not reversed or re-litigated, and F8/v2's answered question was
  closed as discharged rather than reopened.
- Step 4 close: FAIL, so the findings return to owner-authority `work` on `g-12fd`.
  No `§SIGNOFF` line. No narrow review opened. No shape opened either way.
- Two-strikes: reported explicitly on V2 and V7 per the CALL's watch instruction.
- G1: no bet, no tasks, no lane created; one untracked specification-authoring
  frontier remains legal. G5: this IS the separate fresh refutation session, and it
  is none of the authoring, revision or prior verification chats; the v3 revision
  receipt was not read. G7: no owner decision point was created by this leg. G9:
  `CHARTER.md` and `TREE.md` untouched. G10: steps exposed, RESULT final, the leg
  wrote nothing before the RESULT existed.

## log

2026-07-26 | s-converge-verify-october-demo-basis-v3-001 | converge-verify | direction | g-12fd/october-demo-basis: independent verification of Demo Basis v3 FAILED with eight named findings — the §3 readable moment required by done_when and by CHARTER quality has none of its three physical dependencies in MUST while the artifact asserts «целиком живёт в MUST 4» (V1), the technique deletion also removed the artifact's only trace that art is at zero and art appears nowhere in the scope partition (V3), the not-law provisional section carries §5's danger prohibition (V4), MUST 7's «не успели» has no mechanism that is not CUT or deferred (V5), and four defects came from the mechanically delegated repair round — «двадцать минут» still in §1 and §2 against two false self-claims (V2) and «абсурдный мир» still in «Можно» (V7) are each the SAME point failing twice, so KERNEL two-strikes is live on both, plus the null-exceptions statement colliding with the dormant falsifier (V6) and the technique quoted back into the Basis (V8); F8/v2's disclosure question judged discharged; oracle extended to v2 with O13-O17 and v1's O4/O7 verdicts corrected; no owner decision reversed, findings returned to the owner-present v4 revision, g-12fd stays parked.

## next

`c-work-october-demo-basis-v4-revision-001` — owner-present specification
continuation on `g-12fd`, ready now, in a fresh chat with the owner. It carries all
eight findings. One is a real content decision (V1 — what the readable moment
needs in MUST, or a moment the MUST list can produce). Four are small content
choices (V3 art, V4 the danger prohibition's ground, V5 what «не успели» means,
V7 where «абсурдный мир» belongs). Three need only his confirmation of a text
correction (V2, V6, V8). Two points carry live KERNEL two-strikes and must be
fixed at rule level. Its RESULT produces `work/october-demo-basis-v4.md` and one
fresh `converge-verify` against v4. Only after that verification passes may the
narrow `review` close `g-12fd`, and only then may `shape` open `g-37a1`.

Standing calendar unchanged and unmoved by this failure: 2026-08-07 is the
scope-cut boundary if no Basis version has passed verification — twelve days from
today, and now two legs away rather than one; 2026-08-11 page submitted; 2026-08-31
page public and registered; 2026-09-03 build candidate; 2026-09-15 demo live;
festival 19–26 October; 2026-10-05 is the failure boundary, not the plan.

END_OF_FILE: live/indie-game-development/history/2026-07-26-s-converge-verify-october-demo-basis-v3-001.md
