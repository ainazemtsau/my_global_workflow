# RESULT — s-work-g-5a7c-morning-hold-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-morning-content-1, полоса `утро`
track: утро
date: 2026-08-31
outcome_kind: work — публикация утра снята, а не пробита: гейт гигиены запрещает любой пропуск намеренно, и утро смотрится кандидатом в слоте, где оно уже работает

owner_approved: |
  Его слова 2026-08-31, дословно: **«нам не надо там какие-нибудь проверки делать»**,
  **«надо найти какое-то простое решение… потому что на следующей волне мы всё равно будем это
  исправлять»**, **«я не хочу сейчас зависнуть»**.

  Из них прямо следует отказ от отмычки в гейте и отказ чинить дефект сейчас. Третий вариант ноги
  — положить с красным — невозможен механически, что перемерено направлением.

  Его же предыдущее слово «клади» **исполнить нельзя**, и это названо ему прямо, а не обойдено.

  Ни одна строка `done_when` не изменена.

outcome: |
  **НОГА УПЁРЛАСЬ В СТОРОЖ, КОТОРЫЙ ПОСТРОЕН ИМЕННО ПРОТИВ ТОГО, ЧТО ЕЙ ВЕЛЕЛИ СДЕЛАТЬ, И
  ОСТАНОВИЛАСЬ. ЭТО ПРАВИЛЬНО.** Направление велело пометить красный тест сроком сноса; нога
  проверила все механизмы пометки и нашла, что **каждый закрыт намеренно**:

  - `[Ignore]` — `hygiene.ps1:363-376` дословно: «a hard, unconditional skip → **ALWAYS banned**»;
  - `[Explicit]` — только при `Category("Benchmark")`, и там же прямо: «does NOT let a real test
    hide behind `[Explicit]`». Объявить этот тест бенчмарком было бы **ложью о том, что он такое**;
  - `Assert.Ignore` / `Assume` — отдельный сторож для `tests/**`: «a SILENT skip hidden by the
    green gate»;
  - категория плюс фильтр — меняет авторитетную команду прогона, то есть тот же обход в другой
    одежде;
  - соглашения о карантине в репозитории нет вовсе — нога проверила `tools/**` и `docs/adr/**`.

  **НАПРАВЛЕНИЕ ОШИБЛОСЬ, И ОШИБКА БЫЛА В ПРЕДПОСЫЛКЕ, А НЕ В ДЕТАЛИ.** Правило
  `the-sound-decision-is-taken-at-every-cut.md` написано словами «тест помечен сроком сноса, чтобы
  утро могло лечь в `main`» — то есть в предположении, что красный **можно** погасить. Погасить его
  нечем. Репозиторий с этим разменом не согласен, и у него для этого стоит гейт.

  **ЧЕТВЁРТЫЙ ПУТЬ НАЙДЕН, И ОН НЕ ТРЕБУЕТ НИ ОДНОГО РАЗМЕНА.** Публикация нужна другим ногам,
  чтобы строить поверх. **Поверх утра сейчас никто не строит.** Значит утро может остаться
  кандидатом на `slot/win-u4`, где оно уже работает, а опубликуется волной слышимости — там красный
  позеленеет **от починки, а не от пометки**.

  Перемерено направлением в его дереве `46a2ebb8`: профиль несёт `occupations: 4`, `gauges: 1`,
  `routine: 4 шага`; верхние ключи включают оба ранее отсутствовавших блока. **Хозяин проживает
  утро прямо сейчас — просто не в `main`.**

  **И ЭТО РАЗБЛОКИРУЕТ ГЛАВНОЕ: ЖИВОЙ ЗАМЕР.** За весь день **никто не видел живого экрана** — все
  числа читались из файлов и тестов. Ноге велено снять его в собственном слоте: полный отчёт `why`
  из живого файла, что говорит `who` про день, и работает ли `noise 5` в двух состояниях — когда
  хозяин стоит и когда идёт. Последнее — **живое подтверждение самого дефекта**, ради которого
  волна и открывалась.

evidence: |
  Перемерено направлением:

  - `hygiene.ps1:363-376` на `main` = `064ab568` — запрет прочитан целиком, формулировки выше
    приведены дословно из байтов. **Утверждения ноги верны все пять;**
  - `close-leg.ps1:40-72` — полный список параметров: `LeaseId`, `AlsoDeliver`, `DryRun`,
    `AllowTestRemoval`, `AllowBlockedReport`, `LockWaitSeconds`, `SlotRoot`. **Флага «положить с
    красным тестом» НЕТ**, то есть третий вариант ноги невозможен механически, а не по запрету;
  - `slot/win-u4` = `46a2ebb8`, дерево чистое, история несёт три коммита: пометка `858742fc`,
    её откат `be87f283`, запись причины `46a2ebb8`. **Утверждения не тронуты ни одним символом** —
    подтверждено тем, что пометка откачена целиком;
  - профиль на `46a2ebb8`: `occupations: 4`, `gauges: 1`, `routine: 4`, верхние ключи содержат
    `occupations` и `gauges`.

  Из возврата ноги, как улика: 730 всего, 729 зелёных, 0 пропущенных, 1 красный; гигиена зелёная;
  правок поведенческого кода за всю ногу ноль.

state_changes: |
  ДОПИСАНО (1 документ наряда):
  - `work/2026-08-31-call-morning-content-1.md` — третье дополнение: не публикуем; работа остаётся
    кандидатом; ноге велено снять ЖИВОЙ замер в своём слоте и отпустить лизу. Потолок — ни строки
    правок.

  ИСПРАВЛЕНО ЗНАНИЕ (1):
  - `knowledge/the-sound-decision-is-taken-at-every-cut.md`, §2 — предпосылка «тест помечен сроком
    сноса, чтобы утро могло лечь в main» была неверна: погасить красный нечем. Заменена фактом:
    красный ЖИВ и сам служит сторожем, а правило страхует на случай, если тест когда-нибудь
    погасят. **Правило от этого становится сильнее, а не слабее.**

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-morning-content-1` остаётся `active`. **Задача не закрывается сегодня и не бросается:**
    работа сделана и доказана, но не опубликована, и это записано причиной.

captures: |
  - **НАПРАВЛЕНИЕ ВЫДАЛО РАЗРЕШЕНИЕ НА ДЕЙСТВИЕ, КОТОРОГО РЕПОЗИТОРИЙ НЕ ДОПУСКАЕТ, И НЕ ПРОВЕРИЛО
    ЭТОГО.** Механику пометки направление проверило только со стороны закрывальщика
    (`Compare-TestInventory` — счёт не упадёт) и не заглянуло в гигиену, где стоит абсолютный
    запрет. **Правило-кандидат: прежде чем разрешать механизм, проверить его во ВСЕХ гейтах, а не
    в том, который вспомнился.** Замер «инвентарь не пострадает» был верен и бесполезен: отказал не
    тот гейт.
  - **ЗАПРЕТ, КОТОРЫЙ ВЫГЛЯДИТ ПОМЕХОЙ, ОКАЗАЛСЯ ПРАВ.** Он не дал спрятать красный ради удобства
    дня — и в итоге нашёлся путь, где прятать ничего не нужно. **Сторож, который нельзя обойти,
    заставил искать лучший ход, а не разрешил худший.**
  - **«КЛАДИ» ВЛАДЕЛЬЦА ИСПОЛНИТЬ НЕЛЬЗЯ, И ЭТО СКАЗАНО ЕМУ ПРЯМО.** Соблазн был найти
    полулегальный способ выполнить букву его слова. Его слово было решением о размене, а не о
    механизме; механизм оказался недоступен, и честнее вернуть это ему, чем изобразить исполнение.

decisions_needed: |
  Ни одного. Публикация утра переносится в волну слышимости, где красный зеленеет от починки.

play_check: |
  1. OPEN — СТОП ноги с разбором пяти закрытых путей.
  2. WORK — все пять запретов перечитаны в байтах гигиены; список параметров закрывальщика
     прочитан целиком; состояние кандидата и содержимое профиля перемерены первой рукой.
  3. (owner) — его слова о простом решении и о нежелании зависнуть исполнены; невозможность
     исполнить его прежнее «клади» названа прямо.
  4. CLOSE — RESULT, применение, коммит; ответ уходит в WIN-U4.

log: |
  публикация утра снята, а не пробита: гигиена запрещает любой пропуск теста намеренно, и все пять
  путей закрыты — нога проверила и остановилась правильно; утро остаётся рабочим кандидатом в
  слоте, где владелец его и увидит, а опубликуется волной слышимости, где красный позеленеет от
  починки; направление разрешило механизм, не проверив его во всех гейтах

next: |
  Нога снимает живой замер в WIN-U4 и отпускает лизу. Подготовка полигона идёт в WIN-U1.

  **Впервые за день кто-то посмотрит на живой экран.**
