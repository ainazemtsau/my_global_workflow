# RESULT s-repair-g-37a1-simple-gas-call-base-fix-001

call: HOME ESCALATE from c-exec-g-37a1-simple-gas-rest-001 (v1, pre-freeze)
direction: indie-game-development · track: t-sim · play: repair · node/task: g-37a1/t-6
date: 2026-07-30

## outcome

ESCALATE подтверждён и он был ПРАВИЛЬНЫМ. Дефект — наш: наряд `c-exec-g-37a1-simple-gas-rest-001` v1
отправил работу в WIN-U1 и там же велел переиспользовать четыре геометрии фикстур из файла, которого
на базе назначения не существует. Исполнитель остановился до заморозки, ничего не создал, не полез в
запрещённый пакет, не стал восстанавливать файл с чужой ветки и не выдумал замену.

Наряд исправлен на месте до версии 2 и снова ready. Существо не изменилось: путь 2, новый простой
закон без накопителя остатка. Изменился только вход.

Починено ДВА места того же класса, а не одно упавшее: (1) геометрии заданы определениями, и
PAIR-CANDIDATE явно авторизован создать их с нуля как test-support нового пакета; (2) убрана ссылка
на документ в чужом worktree WIN-U4 — негативный контроль описан прямо в наряде. Остальные шесть
путей наряда сверены с базой поштучно и существуют.

Класс переведён в maintenance: это второй бунс продуктового CALL, и `i-direction-to-product-call-contract-001`
по собственному правилу требует именно этого.

Ни одна задача, полоса, лиза или замороженный объект не тронуты. t-6 остаётся active, продуктового
кода по-прежнему ноль.

## evidence

Всё проверено прямым чтением в `C:\projects\Unity\GasCoopGame_win-u1`, а не со слов возврата.

- **База и её чистота:** ветка `slot/win-u1`, HEAD `2b3d5afe90c0750bce843627e25ab62584ca1367` —
  совпадает с объявленным в наряде и не сдвинулся; `validation.config` даёт
  `synced_contract_version: 34`, то есть пин 34 в наряде верен.
- **Поштучная сверка ВСЕХ путей наряда через `git cat-file -e HEAD:<path>`** — именно та проверка,
  которой не было при выпуске:
  - `Assets/GasCoopGame/Core/Field/Voxel/CellGrid.cs` — OK;
  - `Assets/GasCoopGame/Core/Field/Voxel/VoxelField.cs` — OK;
  - `Assets/GasCoopGame/Core/Field/Voxel/VoxelFaceFlow.cs` — OK;
  - `Assets/GasCoopGame/Core/Field/Voxel/DenseVoxelStepOracle.cs` — OK;
  - `tools/select-slot.ps1` — OK;
  - `validation.config` — OK;
  - `tests/GasCoopGame.Core.Tests/Field/Voxel/G37Fixtures.cs` — **MISSING**. Ровно один битый путь,
    и это тот, на котором нога встала.
- **Почему файла нет:** G37-фикстуры написаны на ветке `slot/win-u4` в ходе легов старого газа и в
  `main` не переносились. Листинг `tests/GasCoopGame.Core.Tests/Field/Voxel/` на базе не содержит ни
  одного файла `G37*`.
- **Проверено, что законной замены на базе НЕТ:** поиск построителей сеток на базе даёт
  `ReactionTestFixtures.SingleCellGrid`, `ReactionTestFixtures.SmallRoomGrid`,
  `LvSourceAssertions.SingleRoomGrid`, `SparseDominantGuardrailKillTests.SingleRoomGrid` — комната и
  одиночная клетка, но НИ столба, ни тоннеля, ни каверны. Поэтому вариант «дать законный committed
  path», предложенный возвратом первым, недоступен по факту, и остаётся второй — определения плюс
  авторизация создать test-support.
- **Это и лучше по существу:** переиспользование фикстур старого закона было остатком того самого
  наследования старой машинерии, от которого ушли утром. Свежие построители геометрии не тянут за
  собой ни посевы, ни оракулы старого закона.
- **Второе место того же класса, найденное сверкой, а не падением:** наряд, объявленный
  самодостаточным, ссылался на `docs/engineering/g-37a1-jacobi-descent-proof.md` в ЧУЖОМ worktree
  WIN-U4. Убрано; определение негативного контроля («грань ниже порога хранит остаток, а не обнуляет
  его») перенесено в текст наряда.
- **Состояние продукта не тронуто, подтверждено возвратом и не опровергнуто чтением:** PLAN не
  замораживался, receipts не создавались, `PlanPublication` не запускался, пакет
  `c-exec-g-37a1-gas-rest-and-checksum-001` не открывался, старый закон не менялся, лиза
  `c-exec-g-37a1-simple-gas-rest-001:PLAN` остаётся CLAIMED на WIN-U1.
- **Возврат прав и в процедурном пункте:** v34 действительно не даёт честной pre-freeze квитанции —
  она потребовала бы уже существующих замороженных PLAN/spec и ложного `plan-proof`. Поэтому ESCALATE
  без квитанции здесь единственный законный выход, а не пропуск шага.

## state_changes

1. `work/c-exec-g-37a1-simple-gas-rest-001-call.md` → версия 2: баннер-исправление сверху с точным
   диагнозом и указанием, что лиза уже взята и переигрывать нечего; блок «что переиспользовать»
   переписан на определения четырёх геометрий с явной авторизацией PAIR-CANDIDATE создать их с нуля и
   с запретом переносить что-либо с ветки старого газа; кросс-венью ссылка на документ WIN-U4 удалена
   и заменена самодостаточным определением негативного контроля.
2. `NOW.md` open_calls `c-exec-g-37a1-simple-gas-rest-001`: note переписан — зафиксированы бунс v1,
   его причина, что именно исправлено, сохранённая лиза и неизменность продукта. Статус остаётся
   `ready`.
3. `NOW.md` issues `i-direction-to-product-call-contract-001`: записан бунс №2 и назван общий корень
   обоих — CALL утверждает факт о венью назначения, не прочитанный в этом венью; `route` переведён
   `work` → `maintenance` по собственному правилу issue; `review_when` переписан на обязательную
   отдельную maintenance-сессию, а до неё — ручную поштучную сверку путей.
4. `NOW.md` `updated:` → `s-repair-g-37a1-simple-gas-call-base-fix-001`.
5. `os/FRICTION.md`: одна строка — второй бунс, общий корень, усугубляющая кросс-венью ссылка,
   кандидат правила для maintenance-сессии. Правило здесь НЕ пишется.
6. `LOG.md`: одна строка. Эта запись — в `history/`.

Не менялось: `CHARTER.md`, `TREE.md`, bet и все его поля, все одиннадцать задач и их статусы, пять
полос, WIP, четыре остальных open_call, все прочие issues, продуктовые репозитории, лизы и замороженные
объекты.

## captures

- Асимметрия, которую стоит помнить: при выпуске обоих провалившихся CALL Направление проверило ровно
  то, что проверяется одной командой (слот, лиза, HEAD, штамп контракта), и не проверило то, что
  требует обхода по списку (существование каждого пути). Дешёвая проверка вытеснила нужную.

## decisions_needed

Нет. Наряд исправлен и готов к отправке; решение владельца по пути 2 и по экрану не переоткрывается.
Отдельно остаётся ранее записанный невлияющий выбор по старому корню
(`i-gas-rest-and-checksum-root-open-001`).

## play_check

- Шаг 1: противоречие названо — наряд утверждал существование файла на базе, где его нет.
- Шаг 2: реконструкция чтением базы назначения; артефакты перевесили и наряд, и возврат; проверено
  также, что предложенный возвратом вариант «дать законный path» недоступен по факту.
- Шаг 3: исправление class-wide, а не точечное — найдено и починено второе место того же класса,
  которое ещё не падало.
- Шаг 4: владельцу показан результат; сам маршрут и утверждённый им экран не менялись, поэтому нового
  owner-вердикта эта нога не требует и не выдумывает. Removal boundary не задет: ни один open_call,
  play, knowledge или подписанный артефакт не снят.
- Шаг 5: одна строка FRICTION; OS в этой ноге не правился, правило отдано maintenance.
- G5 не применяется: нога ничего не закрывает как done. G9 не задет.

## log

2026-07-30 | s-repair-g-37a1-simple-gas-call-base-fix-001 | repair | t-sim | g-37a1/t-6: ESCALATE до заморозки подтверждён и признан правильным — наряд назвал G37Fixtures.cs как источник четырёх геометрий, а на базе назначения 2b3d5afe этого файла нет и законной замены тоже нет (на базе только комната и одиночная клетка); поштучная сверка ВСЕХ семи путей наряда через git cat-file дала ровно один битый и обнаружила второе место того же класса — кросс-венью ссылку на документ в WIN-U4; наряд исправлен до версии 2 с определениями геометрий и явной авторизацией PAIR-CANDIDATE создать test-support с нуля, лиза и продукт не тронуты, класс CALL-дефектов переведён в maintenance как второй бунс

## next

Перезапустить `c-exec-g-37a1-simple-gas-rest-001` версии 2 свежей сессией в
`C:\projects\Unity\GasCoopGame_win-u1`. Лизу брать не надо — она уже CLAIMED за этим change_id.
Стадия прежняя: PLAN, короткий человеческий план на утверждение владельцем. Направление — единственный,
кто выдаёт наряд-преемник.

END_OF_FILE: live/indie-game-development/history/2026-07-30-s-repair-g-37a1-simple-gas-call-base-fix-001.md
