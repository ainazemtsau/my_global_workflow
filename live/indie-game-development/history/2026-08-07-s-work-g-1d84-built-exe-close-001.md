# RESULT — s-work-g-1d84-built-exe-close-001

play: work · close: light
direction: indie-game-development · node: g-1d84 · track: сцена · task: t-scene-4
date: 2026-08-07
base_read: OS `7c7add23`; продукт `C:/projects/Unity/GasCoopGame`, опубликованный
`main` = `origin/dev` = `352f96b0`, чекаут приведён к нему и чист. Продуктовые байты, ветки, слоты и
slot-state НЕ изменялись — нога только читала и прогоняла ворота.

## Задача закрыта — и это ПОСЛЕДНЯЯ задача волны

`t-scene-4` → `done`, `close: light` на `352f96b0`. **12 из 12 задач `g-1d84` закрыты. Открытых корней нет.**

### Размер доставки перемерен первой рукой — он минимальный

`f03b1468..352f96b0` = **ровно два файла**: `ProjectSettings/EditorBuildSettings.asset` (−3 строки) и
отчёт исполнителя. Игровая логика не тронута ничем. Границы наряда выдержаны буквально.

### Построчно

| Строка `done_when` наряда | Чем закрыта |
|---|---|
| 1. Собранный exe; названы цель, папка, версия Unity, сцены | ПРОВЕРЕНО НАПРЯМУЮ НА ДИСКЕ: `GasCoopGame_win-u1/Builds/GasCoopGame.exe`, 667 648 байт, 08:25, рядом `GasCoopGame_Data/`, `MonoBleedingEdge/`, `D3D12/` — standalone Windows x64, Unity `6000.5.5f1`, сцены `Lobby` (0) и `IntegratedHouse` (1). `Builds/` под `.gitignore:18` |
| 2. Вторая копия подключается ПО АДРЕСУ | Его слово «все работает» после прогона ДВУХ копий + перемеренный путь: `LobbyController.cs:9-12` рантаймовый UGUI, `:59` → `_session.Join(_addressInput.text)` → `NetworkSessionService.cs:93`. Главная угроза строке (owner-панели тут обычно редакторские Odin-кнопки, которых в билде нет) проверена и НЕ подтвердилась |
| 3. `SampleScene` вне списка сборки, файл на месте | БАЙТЫ: в `m_Scenes` на `352f96b0` ровно две записи, `SampleScene` там нет; `git cat-file -e` на сам файл прошёл |
| 4. Логика не изменена | Тот же дифф из двух файлов |
| 5. Ворота зелёные | ПРОГНАНЫ ЭТОЙ НОГОЙ на чистом `352f96b0`: `dotnet test ...Core.Tests.csproj -c Release` = **110/110, ноль упавших**; `./pwsh.cmd tools/check.ps1` = `hygiene OK` / `OK: all active gates green` |

## STOP наряда сработал — и это лучший результат ноги

Сборки плеера через Unity-MCP **не существует**: исполнитель перебрал 176 зарегистрированных
инструментов и не нашёл ни одного, оборачивающего `BuildPipeline`. Два возможных обхода —
`script-execute` со своим C# и `reflection-method-call` — запрещены и текстом наряда, и продуктовым
стражем; он их НЕ вызывал, а вернулся домой с точным разрывом. exe собрал **владелец своей рукой**,
после чего нога вернулась проверкой подключения.

Отдельно найдено в истории и это подтверждает решение постфактум: сборочный стенд здесь уже пробовали
завести и ОТВЕРГЛИ (`c-exec-g-37a1-venue-packaged-player-001`, закрыт REPLACED, carrier вычищен), а
`git log --all -S'BuildPipeline.BuildPlayer'` даёт два коммита за всю историю — документация и удаление.
**Ни один коммит репозитория никогда не содержал исполнимого вызова сборки плеера.**

## Найдено сверх заказа

**Ручная сборка сама пачкает дерево, и в этой грязи едет включение аналитики.** После Build шесть
отслеживаемых файлов стали изменёнными; почти всё — доматериализация полей URP/Graphics, но
`UnityConnectSettings` переключила `m_Enabled: 0 → 1`. Исполнитель вернул все шесть к закоммиченному
состоянию и сохранил дифф в некоммитимый `_scratch/`. Направление перепроверило на `352f96b0`:
`m_Enabled: 0`. Заведена запись `i-manual-build-dirties-tree-and-turns-analytics-on-001` — это ВТОРОЙ
случай того же флага в том же файле (первый — `i-unity-cloud-services-enabled-in-main-001`).

## Что закрыто его словом, а не наблюдением

Обе копии в одной игре, двух ходоков и общий груз видел ОН. Исполнитель и Направление наблюдали exe
на диске, но не экран. Режим ПРОБА этого прямо и требует, и записано без смягчения.

## Ставка НЕ закрыта, и это не оговорка

Полнота списка задач не закрывает узел. Критерий 10 подписанной карточки дословно: «ЗАКРЫТИЕ. Владелец
зашёл, поиграл и сказал, что с этим дальше. **Узел закрывает его слово, а не проверка**». Его «все
работает» — приёмка ЗАДАЧИ `t-scene-4`, а не диспозиция всей волны. Поэтому:

- `g-1d84` остаётся `active`;
- полосы НЕ распускаются — по `os/KERNEL.md` §3 их распускает только `review`;
- следующий и последний шаг — нога `review` по `g-1d84`, и ей нужен его вердикт «что с этим дальше».

Брифинг для этого вердикта передан владельцу в чате; нога `review` здесь не запускалась и вердикт не
предполагался.

## Гейты

- **G5** — `close: light`. Четыре строки из пяти перемерены этой ногой первой рукой (диск, байты,
  дифф, собственный прогон ворот), пятая закрыта уже сказанным словом владельца. Отдельный свежий чат
  не открывался — это ровно то исключение, которое `os/plays/work.md` §Light close допускает. Венью:
  улика собрана в этой сессии, ворота прогнаны в ней же.
- **G9** — карточка не трогалась.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-work-g-1d84-built-exe-close-001.md
