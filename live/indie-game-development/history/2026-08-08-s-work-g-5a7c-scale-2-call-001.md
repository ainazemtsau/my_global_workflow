# RESULT — s-work-g-5a7c-scale-2-call-001

play: work
direction: indie-game-development · node: g-5a7c · task: t-scale-2 · call: c-work-g-5a7c-scale-2-call-001
date: 2026-08-08
base_read: OS `a0ff7ffc`, дерево `wt/indie-game-development` чисто на входе. Продукт читался
на `ae023ba5` (`main` = `origin/main` = `dev` = `origin/dev`). Ни один продуктовый байт этой ногой
не изменён — нога только читала и мерила.

## outcome

Существует отправляемый наряд исполнителю на единую точку масштаба: `c-exec-g-5a7c-scale-point-001`,
бриф — `work/c-exec-g-5a7c-scale-point-001-call.md`. Он написан уже зная измеренный ответ про
физику, и он самодостаточен: всё, что он утверждает про устройство проекта, перемерено этой ногой
первой рукой на объявленной базе, а не переписано из прошлого наряда.

## evidence

**Перемерено на `ae023ba5` — где сегодня реально живут абсолютные метровые числа.** Прошлый наряд
называл «минимум семь мест», снятых до перевода; после `t-scale-1` картина сдвинулась, и наряд
несёт новую, а не старую:

| Место | Что там перемерено |
|---|---|
| `Network/NetworkPlaySettings.asset` | 14 длин налётчика и груза (`_walkerHeight 0.16` … `_cargoHandReach 0.016`), скорость `0.6`, плюс четыре силовых числа. Найдена ловушка: `_cargoSpawnX/Z` — координаты ДОМА, лежат в файле налётчика |
| `Settings/HouseholderWalkSettings.asset` | Хозяин НЕ переведён: `movementSpeed 1.75`, `urgentMovementSpeed 3.25`, `capsuleRadius 0.5`, `capsuleHeight 2` |
| `World/ThreeRoomHouse.prefab` | Дом 14.5 × 14 м, стены `m_LocalScale {14.5, 1.5, 0.5}` на ±7.25, четыре перегородки `{0.5, 1.5, 6}` на ±2.5, места на −4.875 / +4.875 |
| Там же | **`W119` найдена в байтах:** проходы `West Doorway` и `East Doorway`, у обоих `_width: 2`, при значении по умолчанию `1f` в `World/HousePassageMarker.cs:32`. При росте 0.16 это 12.5 роста налётчика |
| `World/Householder.prefab` | `m_Radius 0.5`, `m_Height 2` |
| `Scenes/IntegratedHouse.unity` | Оверрайды над заготовкой, `near clip plane: 0.01`, `Delivery Point` на `x = 10`, `_size {4, 4}` |
| Константы в коде | Шесть мест — `DeliveryPoint.cs:21-22`, `HousePassageMarker.cs:65`, `HousePlaceMarker.cs:47,54`, `HouseRoomMarker.cs:51`, `StartConditions.cs:54-55`. **Все до одной гизмо и разметка; ни одна не влияет на игру** — это уточнение против прошлого наряда, который перечислял их как «масштабные константы» без этой оговорки |
| Единственное заявление масштаба | КОММЕНТАРИЙ, а не число: `Network/NetworkPlaySettings.cs:10` — «Working scale: one Unity unit is one metre» |

**Пин контракта перемерен:** `os/engineering/CONTRACT_VERSION` `current: 36` против продуктового
`validation.config` `synced_contract_version: 36` — дрейфа нет, Re-sync не должен, наряд пинится 36.
Под v36 `done_when` нового инженерного наряда ограничен тремя пунктами без письменного токена
владельца — наряд написан ровно в трёх и токена не просит.

**Слот перемерен, и он не повторяет прошлую мину:** `C:/projects/Unity/GasCoopGame_win-u1`,
`git status --porcelain` пуст, вершина `09247f2a`, два коммита позади базы, slot-state
`AVAILABLE / lease: none`. Незакоммиченной чужой работы нет — приводить слот к базе безопасно.
Слот тот же, который владелец назвал для этой ставки словом «давай слот 1»; нового его слова это
не требует.

## Что наряд решает сам, а что нарочно НЕ решает

**Форму точки наряд не назначает.** Разбор оставил `W04` строкой `→ PLAN` — «точка ОДНА, и её
читают все пять групп, включая ОБА тела». Это ограничение по существу, и оно в наряде стоит;
устройство — работа исполнителя, и наряд в него не лезет.

**Расхождение по хозяину закрыто картой, а не догадкой.** Хозяин в игре 2 м, а во владельческой
композиции 1.75 м. Приведение к композиции ИЗМЕНИЛО БЫ мир, а строка карточки `t-scale-2` говорит
дословно: «владелец заходит и мир тот же, что после `t-scale-1`». Значит хозяин остаётся 2 м, точка
его просто выражает, а расхождение уходит наблюдением в возврат. Владельцу это НЕ приносится —
его карточка уже ответила.

**Связанность чисел внесена как обязанность СКАЗАТЬ, а не как приказ.** Закон
(`knowledge/scale-down-raises-mass.md`: масса при уменьшении растёт, скорость нельзя двигать в
одиночку) стоит в наряде с резолвящимися ссылками на `ae023ba5`. Но требовать, чтобы точка тянула
массу и демпфирование, значит назначать устройство — поэтому строка 2 `done_when` требует ответа,
а не конкретного решения.

**Главная строка наряда — «мир не меняется».** Она превращает задачу из настройки в перестановку
и даёт механическую проверку вместо владельческого глаза: числа, которые выдаёт точка, обязаны
совпасть с сегодняшними до последнего знака. Бюджетный STOP поставлен ровно на неё: изменилось
хоть одно видимое игроком число — это возврат домой, а не улучшение на месте.

## state_changes

- Заведён `c-exec-g-5a7c-scale-point-001` (`to: executor`, `kind: engineering`, `for: t-scale-2`,
  `status: ready`, `engineering_contract: 36`), бриф —
  `work/c-exec-g-5a7c-scale-point-001-call.md`.
- Закрыт возвращающийся `c-work-g-5a7c-scale-2-call-001`.
- `t-scale-2` остаётся `open`; `g-5a7c` остаётся `active`; `NOW.bet` не менялся; карточка узла
  не редактировалась.

## captures

- `_cargoSpawnX/Z` — координаты дома, живущие в файле настроек налётчика. Кандидат в разделение
  при постановке точки; названо в наряде как ловушка, но не как требование.
- Поле `Width` у прохода сегодня не читает ни один механизм игры — только два редакторских показа
  и одна авторская жалоба. Если после точки оно так и останется никем не читаемым, это вопрос
  следующей волны, а не этой задачи.

## decisions_needed

Нет. Единственная развилка ноги — рост хозяина — закрыта строкой карточки задачи, а не догадкой.

## play_check

- 1 recite: done — цель и `done_when` `t-scale-2` пересказаны, обязательство ставки названо.
- 2 owner inputs (owner): skipped — фактов, которые знает только он, наряд не требует. Пропорцию
  он уже дал («берём 0,16», «размер тот»), слот назвал для этой ставки («давай слот 1»), а
  единственная развилка внутри задачи закрыта её собственной строкой «мир тот же». Операционная
  предпосылка — поднятый Unity в WIN-U1 — передана ему в чате как предпосылка, а не как решение.
- 3 do the work: done — наряд написан, все его основания перемерены первой рукой на `ae023ba5`.
- 4 self-check: done — сверено с `done_when` карточки задачи попунктно; `W119` несёт строку;
  оба набора настроек названы; лимит v36 в три пункта соблюдён.
- 5 close: done — наряд зарегистрирован карточкой, возвращающийся CALL закрыт.

## Гейты

- **G7.** Ни одно решение продукта или геймплея наряд не принимает и ни одно не делегирует
  исполнителю: скорости не подбираются, дом не строится, ширина прохода возвращается ВЫБОРОМ с
  объяснением, а не молча. Развилка по хозяину не выдумана и не принесена ему лишним вопросом —
  она разрешена его же подписанной строкой.
- **G9.** Карточка узла не трогалась.
- **G10.** Наряд не пересказывает процедуру плея и не диктует устройство; основания в нём помечены
  как основания, подлежащие перемеру, а не как приказ.

## log

наряд на единую точку масштаба написан и отправляем: семь мест перемерены заново, W119 несёт строку, главный гейт — мир не меняется

## next

`CALL c-exec-g-5a7c-scale-point-001` — исполнителю в `GasCoopGame`, слот WIN-U1, стадия PLAN,
контракт 36. Отправляется, когда владелец поднимет Unity в этом слоте.

END_OF_FILE: live/indie-game-development/history/2026-08-08-s-work-g-5a7c-scale-2-call-001.md
