# RESULT s-converge-verify-g-37a1-digging-card-001

direction: indie-game-development
play: converge-verify
node: g-37a1
call: c-converge-verify-g-37a1-digging-card-001
verify_target: build
date: 2026-07-27

## outcome

**FAIL.** `work/converge-g-37a1.md` does not survive the independent attack against the
rewritten twelve-criterion card. Both of the play's build attacks fail, on named rows,
and no `§SIGNOFF` line was written. Nine findings V1–V9 are named and none is filled,
repaired or decided here. `g-37a1` stays parked; no shape is opened, no bet, no task, no
track; `knowledge/` was not written; the card was not touched.

**Attack 1 — «the question set is complete» — FAILS.** The artifact is a self-declared
CHECKPOINT with §GLOSSARY unsigned and §WHAT never produced, so coverage was judged
against an independent oracle authored for this leg
(`work/oracle-first-playable-core-v1.md`, fourteen decision classes from first
principles and external precedent) and against an independent atomization of the card
into sixty atoms. Four of the fourteen classes are NEVER RAISED, and they are all on one
side of the artifact — everything between the simulation and the man who is supposed to
play it daily: embodiment and traversal of a self-dug void, the VENUE of the first
runnable thing, the experiment instrument, and run lifecycle. On the card side, criterion
1 has no coverage row for any of its six atoms, criterion 9 has none for five of its
eight, and criterion 5 has none for its two load-bearing clauses.

**Attack 2 — «no answer leans on an unresolved question» — FAILS, and the leaning answer
is now closed against the artifact.** §7's «decisive engineering fact» and §13 step 3
both rest on the claim that an overfill numerator buys a legible seconds-scale
burst-and-subside. That claim is not merely unmeasured (as `i-flow-model-unmeasured`
records): it is CLOSED BY DERIVATION from constants frozen in shipped code, and the
answer is negative. See V2.

**MUST-ATTACK 1 — CONFIRMED, and it retires the measurement rather than scheduling it.**
The numerator cannot move the timescale. See V2.

**MUST-ATTACK 2 — CONFIRMED that Z4 clause 4 fails as written, with one correction to
the recorded worry: the card holds against the crude forms of the re-import, and the
construct that survives all twelve criteria is topology, not injection.** See V3.

**WHAT SURVIVED THE ATTACK.** Stated first, because most of what was attacked held.

- **§6-BIS's reason survives.** The sharpest attack of the leg tried to kill it on
  arithmetic — both branches of the trade are linear in pocket diameter *d*, so *d*
  cancels and there is no break-even size — and the attack FAILED on three independent
  grounds. A single global ratio deciding identically at every size IS what «одно число
  решает» means; the sign it computes (crossing wins) is exactly what «one is a road»
  means, and §6's original horn was the OPPOSITE fear (route around forever); criterion
  5's impassable kind is the forced second branch; and pre-breach the KIND is unknown, so
  a size-graded mark prices a road-versus-wall gamble whose both tails scale with *d*.
  The owner's own recorded doubt about staticness is routed by his own words to a
  playtest, and this leg may not convert it into a spec defect.
- **All four of §7's first-hand corrections are CONFIRMED**, and correction 1 is if
  anything understated: `CoarseBreachLayer` validates every breach at enqueue against a
  pre-declared surface and `CoarseSectorGraph` builds breachable surfaces as pre-declared
  latent portals, so the coarse solver is structurally the authored breach panel. Cell
  size IS a parameterized pair with (25,100) documented. Graded conductivity DOES ship —
  `StructureGasProjection.Conductivity(occ) => occ.OpenCount`, multiplied into the flow —
  with only runtime mutation missing, and `VoxelField.NotifyConductivityChanged` throwing
  on a non-OPEN face is precisely the seam a dig verb must break. The air counter IS a
  consumer on an accepted socket (`PlayerSense` per-actor per-kind exposure and dose).
- **«Dilutes rather than displaces» is CONFIRMED** first-hand: the kernel loops per
  present species with gather, clamp and apply independent per species and no
  cross-species coupling, and the only cap is per-lane. No capacity, no pressure — so on
  the reading the artifact actually commits to («spurts UNDER PRESSURE»), §7's sentence is
  TRUE, and an attack claiming otherwise was refuted.
- **§14 is the strongest verification instrument in either document** for what it does
  test: deck and pass rule committed before the sitting, developer silent, a mechanical
  FAIL rule inverted against the author's interest, correct names explicitly irrelevant,
  the game's own light condition, and the stills weakness named with clips added.
- **`i-demo-scope-cap`'s parenthetical about the banked look verdict is CORRECT.** An
  attack claiming it inverted was refuted: the banked verdict is single-kind
  (`typeCount | 1` in every probe of `c-visual-007`), so moving to two kinds is what puts
  it at risk and cutting to one removes the risk. What is absent — and asserted nowhere —
  is a recorded legibility verdict on two kinds.
- **Criterion 10's «арт сверх самого дешёвого» IS verifiable by an outsider**, because
  `g-2b7f` done_when 2 supplies the enumeration («свет, цвет, туман, камера на
  примитивных формах») under the same owner approval.
- The coarse band solver's recorded numbers all reproduce (10 Hz base tick, τ=4, kP=4,
  1.06 ms at 3,136 sectors, sealed-sector accumulation released on portal open with
  destination back-pressure), and `main` and `dev` are byte-identical at `1a6373b8`.

**NOTHING ON THE FINDING LIST BLOCKS THE FIRST TWO TASKS.** Every refuter converged on
this independently: «a man digs a hole and walks into it» and co-op immediately after are
untouched by V1–V9 as decisions. What the rows block is the SUBSTANCE step, two criteria
whose meaning an outsider cannot read, and the integrity of the approval record.

## evidence

**SESSION INDEPENDENCE.** Fresh session, in the direction worktree, no owner present, as
the CALL specifies. Not the converge chat, not the map chat, not a day chat.
`history/2026-07-27-s-converge-g-37a1-substance-passage-001.md` was **not read at all** —
the play forbids the verifier reading the deciding session's reasoning, and its existence
was confirmed by a directory listing and nothing more. The map RESULT
`history/2026-07-27-s-map-g-37a1-digging-concept-001.md` WAS read: it produced the
yardstick, and the CALL names it as authority. `archive/**` was not read.

**STATE MOVED MID-LEG AND WAS RECONCILED.** At session open, `TREE.md` was modified and
uncommitted, its `detail:` pointed at a history file that did not exist, and `NOW.md`
still named the map CALL as the frontier. The map writer committed `5cc7f7f5` while the
first attack wave was running, which added the CONSUMED banner, §6-BIS, R23–R27, §7's
four corrections and §9's corrected census, updated `NOW.md`, and created this leg's own
CALL file. Every finding below is stated against `5cc7f7f5`, and the first wave's
findings were re-diffed against the issues that commit booked; three were dropped as
already-booked and are named as such rather than re-reported.

**FAN-OUT.** `converge-verify` is not a play that mandates in-leg fan-out, but the CALL's
own instruction — «do not take any sweep at face value» — was applied to this leg too.
Two waves ran: six attack dimensions plus two oracle authors, then six further dimensions
the amended artifact and the CALL name, each dimension followed by an independent
adversary instructed to REFUTE its findings, to default to REFUTED where evidence could
not be reproduced, and to downgrade any finding a `shape` leg could cut around. Twenty-six
raw findings entered; **fifteen were refuted or downgraded to noise by the adversarial
pass, and one was refuted as a paraphrase of a claim the artifact does not make.** The
nine below are what survived, with the corrections the adversaries imposed folded in.
This is an in-session refutation structure inside a leg that is itself the binding fresh
G5 pass for the artifact; it is not a substitute for one.

**FIRST-HAND PRODUCT READ** at `C:\projects\Unity\GasCoopGame`, commit `1a6373b8`,
read-only. Every engineering claim below carries a file and line, and each was reproduced
by a second reader.

## findings

**V1 — the question set is incomplete, and the gap has a shape.**
Four of the oracle's fourteen classes are never raised: embodiment and traversal of a
self-dug void; the VENUE of the first runnable thing; the experiment instrument; run
lifecycle and irreversibility. On the card side the atomization found no coverage row
anywhere in the artifact for all six atoms of criterion 1, for criterion 5's «Один
проходим, второй нет» and «это видно до того, как в него вошли», for five of criterion
9's eight atoms, and for criterion 4's «порода стоит времени». Sixty atoms were mapped;
fifteen of them are RULES, permissions, predictions or glosses rather than checkable
outcomes — including «Взято в самом простом виде, который потом расширяется, а не
переделывается» (a claim about future rework, unverifiable in principle), «это и есть
игра», «механика входит, если даёт понятный профит» (a decision procedure for future
mechanics), and the whole of criterion 12. That is not automatically a defect — criterion
11's closure clause is a deliberate anti-`g-12fd` guard and criterion 5's stranger clause
is a legitimate verification instrument — but it means the card's twelve criteria contain
roughly forty-five decidable atoms, and a `shape` leg must know which.

**V2 — MUST-ATTACK 1, CLOSED: the numerator cannot move the timescale, and a real
capacity clamps the flow DOWN.**
Per-face per-tick the kernel moves `α · driver` with `α = cond/(Kp·spf)`
(`VoxelField.cs:1545,1559-1566`). `cond` is the open 25 cm sub-face COUNT, bounded by
`spf` (`StructureGasProjection.cs:14-19`, `FaceState.cs:6-9`), so the sub-face machinery
cancels and **α ≤ 1/12 is a hard ceiling at any cell size**: `MinSpreadKp = 2 ·
VoxelFaces.Count = 12` (`GasParentParams.cs:17-22`), both public factories pass
`enforceFloor: true` and a sub-floor type THROWS — «a sub-floor type reintroduces period-2
sloshing» (`GasTypeRegistry.cs:105,110,171-174`) — a floor that exists because `Kp = 4`
was a period-2 checkerboard the owner caught with his own eye (ADR-0011:46-58). One flow
sweep per tick, no sub-stepping (`NearGasSimulation.cs:140-155`). Therefore an N-cell
pocket venting through an aperture of open fraction φ has **τ = N·Kp/φ ticks**, the
numerator cancels out of τ, an overfill driver `(m − C)` gives the SAME τ while moving
LESS mass (it vents only the excess and stops at capacity), and the destination-headroom
clamp that a real capacity would tighten already ships at `MaxCellMass = 1<<24`
(`VoxelField.cs:1606,1621-1622`) — it can only lower the transfer. The only absolute-mass
numerator in the kernel, buoyancy, is deliberately `BuoyancyDenominator/forceMag` times
SLOWER (`:1368-1369`). **No tick-rate constant exists anywhere in `Core` or `Net`** — the
only live values are director intervals of 0.10–0.16 s, i.e. 6–10 Hz — so every
seconds-valued claim in §7, §8 and §3 rests on an unowned parameter.
*Consequence:* the levers that buy a burst are the gas cell size (τ ∝ N, cubic), the tick
rate, the aperture AREA (bounded by a factor of `spf`, and it is the lever the player
holds) and the pocket size in cells. The numerator is not among them. A narrow notch —
which is §13 step 3's own scenario and §3's own «aperture buys time» law — is minutes at
the shipped default; a wide breach on a coarse grid reaches seconds.
*What dies:* §7:316-324's fix as written, §13 step 3's «this is the footage» as written,
and §3's timescale-bearing laws («wait two seconds», the growing seep, «stops if you thin
it before the countdown ends»). *What does not die:* no card criterion — none requires a
burst — and `g-2b7f`'s five stills.
*Correction owed to live state:* `i-flow-model-unmeasured` prices a one-day headless
measurement of the wrong question. The timescale half is closed; what is genuinely open
and worth a day is the affordable envelope — which (cell size, tick rate, aperture,
pocket size) the game can pay for.

**V3 — MUST-ATTACK 2, CLOSED with a correction: Z4 clause 4 fails as written, but the
card holds against the crude re-import and the surviving construct is topology.**
Criterion 2 bans «заранее расставленной автором панели» — placement IN ADVANCE — so a
runtime rule reads PASS on criterion 2's letter, and the good-faith refutation of the
recorded worry is textually correct. It then fails on the conjunction: the
mass-injection form is rejected flatly by criterion 7 («по ходу не растут»), and the
relocation form is rejected by criterion 3 in the owner's own narrow B4 form, because its
destination is computed from the player's position. **The construct that survives all
twelve is a runtime mutation of POROSITY — moving the topology so the cut face meets a
pre-placed pocket** — and the engine cannot express that today either
(`VoxelField.NotifyConductivityChanged` throws on a face that is not already OPEN,
`VoxelField.cs:818-824`). So the owner's question narrows to one sentence, stated in V3's
row of the map CALL, and it is his: may a rule decide WHERE substance can appear while
the crew is already digging, or must every place it can appear be fixed before the build?
**Independently, and booked nowhere: Z4's own neighbouring clause «sources are finite» has
no implementation anywhere in the repository, and the only shipped authored source is
INFINITE by owner-signed default** — so criterion 7's «по ходу не растут» fails by
construction on today's primitives, and the substance step inherits that as work, not as
a law.

**V4 — criterion 5 carries a substance property that nothing in the direction defines,
and criterion 4 says the opposite in letter.**
«Один проходим, второй нет» introduces PASSABILITY. No Z-law, no §3 law, no requirement
R1–R27 and no line of engine code expresses it (grep for
`impassable|passable|blocksMovement|traversab` over the product returns zero), while
criterion 4 states that a pocket IS «уже открытое пространство, в котором нечем дышать» —
enterable but unbreathable. §6-BIS supplies only an ECONOMIC reading — «one is a road, the
other a wall», conditioned on a per-kind air-rate gap of about an order of magnitude.
Two independent adversaries converged on this from different dimensions. An outsider
cannot verify criterion 5 without knowing which reading binds, and it decides the shape of
the air rate table, the legibility target and whether a wall exists at all. Owner content;
one question, his words.

**V5 — visible identity and air cost are read from different fields and can disagree.**
The GridView texel is a FROZEN four bytes — R = the MIXTURE's total density, G = the
DOMINANT type id, B = front strength, A = warning — asserted on both the C# constant and
the HLSL decode, with the more-bits alternative explicitly rejected (ADR-0012 D4:75,81-82,
108-109,119; `GridViewLayout.cs:12-27`; `RealGasViewSource.cs:270-288`). A trace below
4 per-mille renders as nothing (`GridViewLayout.cs:42-46`). Meanwhile the accepted air
socket charges per-species across the whole mixture
(`PlayerSense.cs:694-726`, `:482-499`). So a minority kind is unrepresentable while it is
still charging air, in the two situations the design makes central: the first trace
escaping a fresh notch, and any region where the two kinds have met. §3's note is
backwards as a consequence: the chosen polarity «what you can see is what kills you» is
the one the renderer CANNOT guarantee, and the opposite polarity errs only in the
harmless direction. This is NOT a contract-widening finding — the repo already names the
cheap resolution in code, single-type-per-region seeding
(`FakeGasViewSource.cs:113-116`), which is a build-time choice — but a `shape` leg must
know it before sizing legibility, and §3's note should stop claiming arithmetic necessity.

**V6 — the venue criterion 1 mandates does not exist, is unpriced, and is in no plan.**
`ProjectSettings/EditorBuildSettings.asset:7-9` holds exactly ONE enabled scene, the
empty URP template `Assets/Scenes/SampleScene.unity`; nine real scenes exist and not one
is in the list, so a player built today ships an empty scene. There is zero build
automation anywhere in the product (grep for `BuildPipeline|BuildPlayerOptions|BuildTarget`
over `*.cs/*.ps1/*.yml/*.sh` outside `Library|Packages`: no hits; 36 scripts in `tools/`,
none builds a player), zero player-build mention across 53 ADRs and every measurement
receipt, and no `Build/` directory. Decisively, a gate receipt records the empty default
as a deliberately preserved invariant
(`c-exec-program-v2-legacy-lab-purge-001-build-receipt.md:67-69`). §13's seven steps
contain no packaging step. Criterion 1 is the card's FIRST clause and it gates the
evidence for criteria 5, 8, 9 and 11. This does not need the owner — criterion 1 already
IS his approved answer — it needs to be a named task with a price.

**V7 — the body that exists cannot go down, and the artifact's census never asks.**
`InputBodyStateSource.cs:65-66` integrates a strictly planar walk —
`new Vector3(move.x, 0f, move.y) * (moveSpeed * dt)` at 4 m/s, with Y written once at
seed time and never again. Grep for `gravity|Raycast|CharacterController|Physics.` over
`Characters` and `Adapters/Characters` returns zero hits; the capsule prefab's
`CapsuleCollider` is read by no movement code. The artifact's complete statement of the
embodiment gap is §7:401-403, «nothing a player does moves the substance, and no scene has
ever contained a controlled player and the substance together» — true, and it never asks
whether the body can descend into a hole or climb back out. Task 1 is «a man digs a hole
and **walks into it**», and R23 has just made coming back out required. Gravity plus
ground contact is one standard component, so this is a step-2 scope item, not a blocker —
but it must be in the task, not discovered inside it.

**V8 — `g-5e8c` carries three clauses under no owner verdict, and one of them re-bans what
he had just unbanned.**
New `g-5e8c` done_when 8 ends «Инвентарь, крафт и лут сверх этого не появляются». It was
written by the same leg that deleted R18's ban on «an inventory/crafting/economy layer» on
his own «лут экономика не запрещена … R18 надо удалить, оно невалидно», it sits one node
away from criterion 10's «Лут и экономика не запрещены и входят по общему правилу», and it
lands exactly where R18's SURVIVING half places the subject (upgrades between sections).
The new done_when 9 (latency and loss) and the removal of «второй объект» from done_when 5
are in the same unsigned set. `TREE.md:3` attaches «g-5e8c edited 2026-07-27» to an
approval whose quoted verdicts can only be `g-37a1`'s — the twelve-criterion card text and
«да, сохраняй» on lines 1, 8, 9 and 12, while `g-5e8c` has nine criteria — and the map
RESULT's own `play_check` says «One card, one verdict, on exact text» while its
`state_changes` edited two cards. The same leg also books «how rest stops work» as STILL
OPEN owner content while writing a rest-stop criterion into `g-5e8c`. This is the failure
mode this direction has already paid for once: derivable is not signed. Three clauses,
one missing verdict.

**V9 — four sections a `shape` leg will read are stale, and the file's own reading
protocol does not warn about two of them.**
(a) **§11 was never amended** and is not in the CONSUMED banner's list of four. §11.2's
fix is relocation, which the owner refused; §11.3's «barred by Z3's single-counter rule»
rests on a rule he softened, so a legitimate downed-state softener would be rejected on a
dead citation; and with relocation gone, §11.2's only surviving forcing function is the
growing seep, which V2 has just retired. (b) **§6's premise is stale on a second horn.**
§6-BIS repaired «space you need» and left «ore behind it», although criterion 10 legalises
loot and R18's ban half is deleted — and §6-BIS's own repair list cites that deleted ban as
live. (c) **§9's census is five of nine, not nine.** Item 1's counterparty was the
relocation candidate the owner then refused; item 6 is an owner revision with no
counterparty in state; item 8's stated ground is false, because the latency clause's
«vanished subject» was promoted into criterion 8 of the same card in the same edit; and
the PHANTOM verdict on item 4 is itself false, because old done_when 2 did restrict tools
(«средство для этого, а не отдельный слой игры»). The census is a partial change list and
must never be cited as a completeness claim. (d) **Z1's engineering ground cites a file
that does not exist.** `ScKernelAtomicityAndConservationTests.cs` is not in the product;
the real files are `RW3ConservationAtomicityTests.cs`,
`BreachDiffusionConservationTests.cs` and `CoarseFieldCreateAtomicityTests.cs`, and the
substance of Z1's policy claim is separately challenged in
`i-substance-laws-canon-candidate`. (e) §7's accepted in-leg reconciliation — «the
difference between cheap and expensive is mostly cell size, not two codebases» — is false
once correction 1 stands. (f) §14 tests naming consistency only, so it cannot test
criterion 5's third clause and should say so.

## dropped as already booked, not re-reported

Three first-wave findings were real and are already carried by live state, so they are
recorded here as CONFIRMATIONS rather than findings: Z1's engineering ground being false
first-hand (`i-substance-laws-canon-candidate`), the whole unmeasured-flow-claim frame
(`i-flow-model-unmeasured`, now corrected by V2), and the R1–R27 clause-by-clause pass
(`i-requirements-validity-pass`). Fifteen further raw findings were refuted or downgraded
to noise by the adversarial pass and are deliberately not listed: among them an attack on
§6-BIS's arithmetic that inverted the design intent it quoted, an attack claiming
`i-demo-scope-cap`'s banked-verdict sentence was inverted, a double count of the criterion
5 legibility hole, and a claim that the repo already expresses spurt-and-subside, which
was a paraphrase of a sentence the artifact does not make.

## state_changes

1. `work/oracle-first-playable-core-v1.md` — CREATED. The node-class oracle this leg
   required and `knowledge/` did not have: fourteen decision classes from first
   principles and external precedent, a verdict per class against the artifact, and a
   sourcing ledger that marks five of the artifact's own precedents as unsourced.
   CANDIDATE, proposed for promotion by `review` or `pulse`; no authority until promoted.
2. `work/c-map-g-37a1-verify-rows-001-call.md` — CREATED. The owner-present `map` CALL
   carrying only the rows that need his words (V4, V8, and V3's one-sentence canon
   question routed onward to `review`), plus the routing decision below. Self-contained.
3. `work/c-converge-verify-g-37a1-digging-card-001-call.md` — a `disposition:` block
   appended: discharged, FAIL, receipt, successor, and which of its questions closed.
4. `NOW.md` — `updated:` to this leg. `direction_forecast.drivers[0]` rewritten to this
   verdict and the route. `i-flow-model-unmeasured` corrected: the timescale half is
   closed by derivation, the one-day measurement is retargeted to the affordable
   envelope, and its route changes from a measurement before any build to an envelope
   decision at the substance step. `i-substance-laws-canon-candidate` extended with V3's
   closure and with «sources are finite» having no implementation. Three issues added:
   `i-card-clauses-unverifiable` (V1, V4, V6), `i-g5e8c-unsigned-clauses` (V8) and
   `i-artifact-sections-stale` (V9). `open_calls`: the spent verification row replaced by
   exactly one `c-map-g-37a1-verify-rows-001`. One decision added,
   `d-post-verify-route-001`. `bet: null`, `tasks: []` unchanged.
5. `LOG.md` — one line prepended.
6. `history/2026-07-27-s-converge-verify-g-37a1-digging-card-001.md` — this RESULT.

Not touched: `CHARTER.md`; `TREE.md` and both cards, which carry the owner's verdict on
exact text and may change only by a `map` leg with his words; `knowledge/**`; the artifact
`work/converge-g-37a1.md`, which a verifier does not repair — V9 routes that; the product
repository, which was read-only throughout; Steam state; `archive/**`. No `§SIGNOFF` line
was written. No shape, bet, task, track or executor CALL was created. No numeric release
chance was published. No owner decision was reopened: the objective, the demoted dates,
the softened air rule, the restated cargo ban and two-versus-four players all stand as he
left them.

## captures

- The four never-raised oracle classes cluster on one side of the artifact. That pattern
  itself may be the reusable lesson for the next model-bearing node: a converge fans out
  into the simulation and never turns around to face the player.
- `NotifyConductivityChanged` throwing on a non-OPEN face is named three times across two
  legs as «the seam a dig verb must break». It is the smallest concrete engineering fact
  in the whole set and it belongs in the first build task's description.

## decisions_needed

`d-post-verify-route-001` — after this FAIL, does `shape` open on the owner's live
instruction, or does the artifact get finished first?

- **Option A (recommended).** One short owner-present `map` leg for V4 and V8, then
  `shape`, with V1's coverage rows discharged as TASKS — a named build the owner launches
  twice; the legibility target; the experiment rig — rather than as more paper. This needs
  his live instruction, because `KERNEL` §2's readiness router requires a passing
  `converge-verify` before `shape`, and a live owner instruction is the one thing that
  outranks the kernel by the kernel's own authority order. Ground: nothing on V1–V9 blocks
  «a man digs a hole and walks into it» or co-op; `g-12fd` died of exactly the paper route
  after three legs and eighteen findings; and his own «сначала стабилизируем работу».
- **Option B (by the book).** `map` for V4 and V8, then a bounded `converge` that produces
  §GLOSSARY and §WHAT against the twelve criteria and re-prices step 3 on V2, then a
  narrow `converge-verify` rerun, then `shape`. Two more legs before the first build task,
  and the rows it would close are rows a build closes better.

## play_check

- `verify_target: build` — complete=FAIL smuggling=FAIL.
- Step 1, recite the claim: done. Both build attacks named before the attack ran.
- Step 2, attack completeness with an INDEPENDENT oracle: done. No node-class checklist
  existed in `knowledge/`, so one was authored from first principles and external
  precedent BEFORE the attack (`work/oracle-first-playable-core-v1.md`) and is proposed as
  canon; the empty oracle did not block. Compound done_when clauses were split atomically
  — sixty atoms — and mapped to substantive treatment in the artifact, since §WHAT does not
  exist. Uncovered clauses and never-raised classes are named and NOT filled.
- Step 3, attack smuggling: done. Every weight-bearing engineering value in §7, §8 and §10
  was traced first-hand at `1a6373b8`; untraceable and misattributed items are named in
  V2, V5, V6, V7 and V9(d). No leaked HOW magnitude was moved into a PLAN agenda, because
  no §WHAT exists to move it out of; the rows are routed instead.
- Step 4, close: FAIL. No `§SIGNOFF`. Named rows return by their owner — card content to
  `map` (V4, V8), the canon clause to `review` (V3), artifact hygiene to `repair` (V9),
  and build-shaped rows to whichever of the two routing options the owner chooses.
  Verification reruns later on whatever the map leg changes.
- Owner-verdict guard: no owner verdict was invented, inferred or needed. The routing
  question is stated as a decision with a recommendation and left open.
- Two-strikes check: no finding of this leg repeats a finding the previous verification
  already made against the same target, so `KERNEL` §2 two-strikes is not live.

## log

2026-07-27 | s-converge-verify-g-37a1-digging-card-001 | converge-verify | direction |
g-37a1/digging-card: the artifact FAILED independent verification against the rewritten
card with nine findings and no signoff.

## next

`c-map-g-37a1-verify-rows-001` — one short owner-present `map` leg: what «проходим /
второй нет» means mechanically, whether `g-5e8c`'s three unsigned clauses stand or revert,
and his ruling on `d-post-verify-route-001`. Then `shape` under Option A, or a bounded
`converge` under Option B.

END_OF_FILE: live/indie-game-development/history/2026-07-27-s-converge-verify-g-37a1-digging-card-001.md
