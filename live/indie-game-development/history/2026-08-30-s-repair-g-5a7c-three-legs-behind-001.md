# RESULT — s-repair-g-5a7c-three-legs-behind-001

direction: indie-game-development
play: repair
node/task: g-5a7c / bet-g-5a7c-wave-8 (полосы `ядро`, `закрепление`)
track: —
date: 2026-08-30
outcome_kind: repair — записи направления отстали от продукта на три поставленные ноги; две из них не имели в направлении ни одной карточки

owner_approved: |
  Нога запущена его наряд-сообщением REPAIR от 2026-08-30, перечислявшим пять расхождений
  поимённо. Батч правок показан ему целиком до записи (шаг 4 плея `repair`) и одобрен
  дословным «да». Отдельно в том же ответе он снял вопрос про потолок ноги — цитата
  приведена в блоке `decisions_needed`.

outcome: |
  ПЯТЬ РАСХОЖДЕНИЙ ПРОВЕРЕНЫ, ЧЕТЫРЕ ПОЧИНЕНЫ, ПЯТОЕ ОКАЗАЛОСЬ УЖЕ СДЕЛАННЫМ ЧУЖОЙ СЕССИЕЙ.
  Ничего не закрыто лёгким закрытием: каждая строка `done_when` перевыведена первой рукой из
  закоммиченных байтов на `main = dev = f6fc0918`, тесты прогнаны направлением, а не приняты
  со слов исполнителей.

  ЧТО НАШЛОСЬ СВЕРХ НАРЯДА, И ЭТО ГЛАВНОЕ В НОГЕ.

  1. Строка `done_when` `t-thing-missing-1` про окно наблюдения на `main` НЕ ВЫПОЛНЕНА —
     и виноват не исполнитель. Наряд сам разрезал её по полосам: «Рисует полосу `полигон`;
     твоё дело — чтобы метки было ЧТО показать» (`work/2026-08-30-call-thing-missing-1.md:53-55`).
     Половина `ядра` отдана (`HouseholderStepResult.cs:167`), половина `полигона` не начата и
     не числилась НИГДЕ. Задача закрыта по факту, долг вынесен уликой, а не растворён в закрытой
     карточке.
  2. Потолок наряда «больше пяти — СТОП» пробит (11 мест), а разрешение существовало в одном
     месте на два репозитория — в отчёте самого исполнителя. Владелец спрошен прямо и ответил:
     согласие БЫЛО (он выбирал пункт Б), но приписанной ему фразы он не говорил — её испортил
     голосовой ввод. Это меняет диагноз существующего класса, а не заводит новый (см. ниже).
  3. Его число «1315 строк» точное, но на ногу назад: `Householder.cs` = 1315 ровно на
     `61bc1971`; на `f6fc0918` — 1338.
  4. «Самый длинный метод 56 строк» не воспроизводится ни на одном из двух деревьев.
     Перемерено разбором границ методов по балансу скобок: `StepThroughTheDay` = 346 строк
     (`Householder.cs:115-460`), `StepRoutine` = 264, `StepActiveResponse` = 213 (в улике 214),
     `StepTrackedVisibleResponse` = 175 (в улике 175, байт в байт). `Householder.Step`
     действительно сжался 576 → 79. Простыня не исчезла — она переименовалась, и два из трёх
     методов, названных уликой поимённо, не тронуты с 14 августа. Улика перемерена по ФОРМЕ и
     оставлена ОТКРЫТОЙ; три её дефекта не проверялись и не сняты — это маршрут `shape`.
  5. Пункт 5 наряда выполнять было нечего. Параллельная сессия коммитом `4781a55d` уже
     перевела `t-chore-result-1` в `ready` и вдобавок выдала наряд
     `c-exec-g-5a7c-chore-result-1-001`.
  6. Коммит ноги границы в наряде устарел за время его написания: `slot/win-u3` уже на
     `aa470c9a`, а не на `a21d51c4`.

evidence: |
  ПРОДУКТ. `C:\projects\Unity\GasCoopGame`, `main = dev = f6fc09185af095ce…` (обе ссылки
  разрешены в один SHA командой `git rev-parse main dev`).

  ПРОГОН НАПРАВЛЕНИЯ, НЕ ЗАЯВЛЕНИЕ ИСПОЛНИТЕЛЯ.
  `dotnet test tests/TunnelCrew.Core.Tests -c Release` на `f6fc0918` →
  не пройдено 0, пройдено 694, пропущено 0, всего 694.

  thing-missing (`4aab1415` код → `397703d0` отчёт → `a90ca3b8` влитие → `61bc1971` закрытие):
  - метка, а не факт: `git show 4aab1415 -- HouseholderStanding.cs` не добавляет ни члена в
    `HouseholderConditionKind`; `MarkPresent/MarkAbsent/MarkAtLeast` (3/4/5) были там до ноги;
  - вход тика и только чтение: `Householder.cs:49-53` — `HouseholderStanding.Wearing(…,
    input.WorldMarks)`; `Householder.cs:109` — `.WithStanding(standingAfter.WithoutWorldMarks())`
    перед возвратом в память;
  - граница: `grep -ri cargo Assets/TunnelCrew/Core/Householder/` → 0 строк, 0 файлов (31 файл);
  - окно наблюдения: `grep -rn WorldMarks --include=*.cs Assets/` — совпадения ТОЛЬКО в
    `Core/Householder/`; `grep -rn Marks --include=*.cs Assets/TunnelCrew/Editor/` → пусто.

  thing-belief (`d420fd64` → находки обзора `dd5722c8`/`5ba678d3` → `ffb22448` → влитие `9571aa90`):
  - всезнание убито: `Network/NetworkHouseholder.cs:254` —
    `SeeWhatIsLyingAbout(…, IHouseholderThingSightSource sight)`;
  - расстановка со сцены: `NetworkHouseholder.cs:389` — `_arrangement = new HouseArrangement(exact)`;
  - убеждение — четвёртое поле: `HouseholderStanding.cs:43-53`; единственная запись —
    `Householder.cs:64-69`, `standingBefore.Noticing(…)` из наблюдений на входе;
  - авторится в JSON: `Profiles/HouseholderProfileLoader.cs:2325` — `case "thing-belief"`.

  loot-roster (`fef06356` → `11974ac6` → закрытие `f6fc0918`):
  - `IntegratedHouse.unity:1067-1081` — `_cargoSpawns` несёт 14 записей;
  - имена разрешены ЧЕРЕЗ ПРЕФАБ, а не по отчёту: `fileID 2090291535` → `FirstHouse.prefab`
    GameObject `6969998205459681442` = `Spare keys`; `fileID 1843943968` →
    `7830600372006809175` = `Spare mug`;
  - `first-house.house.json` заявляет ровно 14 имён, дубликатов нет;
  - `git show --stat fef06356` — сцена +24, −0, чистое добавление;
  - `IntegratedHouse.unity:1589` — `_carry: {fileID: 11400000, guid: 348cfce6493edaa4394ff3d5f7232b9a}`:
    обнулённая редактором ссылка возвращена прежними байтами.

  bedrock-boundary — идёт, не готова к запуску.
  `C:\projects\Unity\GasCoopGame_slot-state\gascoop-slot-state.v1.json`: `WIN-U3` =
  `"lifecycle": "CLAIMED"`, `"lease": "c-exec-g-5a7c-bedrock-boundary-1-001:BUILD"`.
  Ветка `slot/win-u3` на `aa470c9a`; `git merge-base --is-ancestor aa470c9a main` → не предок.

  ЗАМЕР ЯДРА ХОЗЯИНА. `wc -l Assets/TunnelCrew/Core/Householder/Householder.cs` = 1338 на
  `f6fc0918`, 1315 на `61bc1971`, 3381 на `bf59c57d`. Обвал 3381 → 1083 — коммит `76b2ab46`.
  Границы методов разобраны по балансу скобок и сверены глазами на `Householder.cs:113` и `:460-472`.

  ЧЕГО В УЛИКАХ НЕТ И БЫТЬ НЕ МОЖЕТ БЕЗ UNITY. Зелень EditMode (220/220), на которую ссылаются
  оба закрывающих отчёта, направлением НЕ перепроверена: изменённый ногой loot-roster тест —
  EditMode и в 694 не входит.

state_changes: |
  Все применены командами `osctl`, ни одна карточка не правилась рукой.

  ЗАКРЫТО ПО ФАКТУ (6):
  - `c-exec-g-5a7c-thing-missing-1-001` — числился `running`, работа в `main`;
  - `t-thing-missing-1` — `ready` → `done`;
  - `c-exec-g-5a7c-thing-belief-1-001` — ЗАВЕДЕН ЗАДНИМ ЧИСЛОМ и закрыт;
  - `t-thing-belief-1` — ЗАВЕДЕНА ЗАДНИМ ЧИСЛОМ (`_bet: bet-g-5a7c-wave-8`, `track: ядро`) и закрыта;
  - `c-exec-g-5a7c-loot-roster-1-001` — ЗАВЕДЕН ЗАДНИМ ЧИСЛОМ и закрыт;
  - `t-loot-roster-1` — ЗАВЕДЕНА ЗАДНИМ ЧИСЛОМ и закрыта.

  Обе заведённые задним числом задачи несут в `done_when` первой строкой прямую пометку
  «РЕКОНСТРУКЦИЯ ПО ПОСТАВЛЕННЫМ БАЙТАМ, а не заказ, подписанный до постройки». Это
  обязательное условие: без него реконструкция через месяц читалась бы как согласованное
  требование, а класс «нога сама сочинила спеку, и та затвердела» в этом направлении уже ловился.

  ПЕРЕВЕДЕНО В РАБОТУ (1):
  - `c-exec-g-5a7c-bedrock-boundary-1-001` — `ready` → `running`,
    `started: 2026-08-30 · WIN-U3 CLAIMED, лиза …:BUILD, slot/win-u3 aa470c9a`.

  УЛИКИ (4):
  - `i-householder-core-needs-structural-rework-001` — блок `issue` переписан целиком: замер
    заменён на сегодняшний, три дефекта и раздел «что уже сделано правильно» сохранены байт в байт,
    улика ОСТАЁТСЯ ОТКРЫТОЙ;
  - `i-executor-paraphrase-became-the-approval` — дописан второй случай и изменённый диагноз класса;
  - `i-world-marks-have-nobody-to-draw-them-001` — НОВАЯ, маршрут `shape`;
  - `i-a-ray-may-not-see-a-thing-lying-down-001` — НОВАЯ, маршрут `work`.

  ГИГИЕНА (1, разрешения не требует по границе плея):
  - `c-exec-g-5a7c-bedrock-boundary-1-001.description` — было длиннее 120 знаков и несло
    задвоенный обрывок «не компилируется … не компилируется». Пока это стояло, `osctl card set`
    отказывал на ЛЮБОЕ поле этой карточки, то есть перевод в `running` был физически невозможен.

  НЕ ТРОНУТО НАМЕРЕННО:
  - `t-chore-result-1` — уже `ready` чужой сессией (`4781a55d`), наряд выдан;
  - `t-bedrock-boundary-1` — `osctl check` держит на ней замечание «задача без ставки»: нет `_bet`,
    значит она выпадает из рабочего набора. Не чиню: проставить `_bet` значит записать её в состав
    ставки, а полосы `закрепление` в таблице полос `bet-g-5a7c-wave-8` нет. Членство в ставке и
    объявление полосы — `shape`, не `repair`. Вынесено во FRICTION.

captures: |
  - Отчёт thing-belief называет ТРЕТЬЕ непроверенное, кроме луча: опечатка в имени вещи
    (`{"thing":"Muug"}`) грузится молча, и строка просто никогда не срабатывает — резолвер имён
    против расстановки не построен, потому что расстановка живёт в сцене, а профиль грузится до неё.
    Тот же класс проект однажды уже закрывал для ролей мест. Уликой НЕ заводил: это содержание
    авторинга, и место ему в наряде на следующий слой профиля, а не в общем списке.
  - Отчёт loot-roster называет побочный ущерб отладке: пробы зрения пишутся под одним id 0 на все
    вещи, поэтому отрисовка показывает одну пробу вместо четырнадцати и затирает картинку игрока
    с id 0. На поведение не влияет, но ровно этот прибор — единственный способ снять улику про луч.

decisions_needed: |
  Ни одного открытого. Заданный вопрос закрыт его словами того же дня, дословно:

  «я помню, что я выбирал что-то пункт B, но вот именно говорить плачу десятку, строю целиком —
  я такого не говорил, ну не помню… я использую голосовой ввод, он не всегда точно распознает
  речь… это какая-то бессмысленная фраза, поэтому, скорее всего, распознавание прошло неверно».

  Из этого следует, что девятая улика, предложенная в батче, НЕ ЗАВЕДЕНА: класс уже существует
  (`i-executor-paraphrase-became-the-approval`, маршрут `repair`, чей `review_when` прямо называет
  улики о том, как чужой пересказ становится основанием), и заводить второй экземпляр значило бы
  расщепить один класс на две карточки. Вместо этого класс ДОПИСАН, и его диагноз изменён:
  чинить надо не доверие к исполнителю — он ничего не выдумал, — а то, что несущее слово владельца
  живёт пересказом в чужом отчёте вместо дома в направлении в день, когда произнесено.

play_check: |
  1. Назвать противоречие — сделано: пять расхождений наряда плюс два, найденных сверх него
     (нерисуемые метки, непроверенный луч).
  2. Восстановить новейшим-первым — сделано по коммитам и байтам продукта; отчёты исполнителей
     читались как улики, а не как истина: каждая несущая строка перепроверена в файле.
  3. Предложить исправленное состояние — сделано, по одной строке причины на каждую правку.
  4. Подтверждение (owner) — его дословное «да» на показанный целиком батч, плюс его ответ
     про голосовой ввод, приведённый выше. До этого слова не записано ничего.
  5. Дыра в OS → FRICTION — одна строка, ниже.

log: |
  три ноги приехали в продукт впереди записей, две вовсе без карточек — закрыты по факту с
  перевыводом done_when из байтов, замер ядра хозяина перемерен, два долга вынесены уликами

next: |
  Домой. Ремонт закрыт, новых нарядов не выдаёт.

  Владельцу — одно действие, и оно уже описано чужим отчётом: включить `Draw Sight Gizmos` в
  `Assets/TunnelCrew/Settings/HouseholderSightSettings.asset`, нажать Play, посмотреть цвет
  шариков на вещах. Это снимает `i-a-ray-may-not-see-a-thing-lying-down-001` — единственное, от
  чего зависит, работает ли вообще слой убеждений, или он инертен навсегда.

  Направлению — `i-world-marks-have-nobody-to-draw-them-001` при первом же касании полосы
  `полигон`, и `t-bedrock-boundary-1` без ставки при следующем `shape`.

FRICTION: |
  2026-08-30 · Плей `repair` обязан привести состояние в соответствие реальности, но чинить
  членство карточки в ставке и объявлять полосу ему запрещено (ядро §3: полосу объявляет только
  `shape`). `t-bedrock-boundary-1` живёт на полосе `закрепление`, которой нет в таблице полос
  её ставки, и потому не имеет `_bet` и выпадает из рабочего набора `osctl context`. Ремонт видит
  дефект, называет его и НЕ МОЖЕТ его устранить, а `shape` открывается не ремонтом. Задача,
  выпавшая из рабочего набора, невидима следующей ноге — то есть дефект самовоспроизводится.
  Вторая такая же запись откроет маршрут на изменение OS.
