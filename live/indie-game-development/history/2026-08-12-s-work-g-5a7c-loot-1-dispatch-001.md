# RESULT — s-work-g-5a7c-loot-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-loot-1
track: house-and-things
date: 2026-08-12
outcome_kind: checkpoint — наряд выдан, задача НЕ закрыта

owner_approved: |
  «пиши наряд на лут» — 2026-08-12.

  И уточнение по стенам, которое переписало чужую улику: **«он не то, что прям сквозь стену пошёл.
  Он проходит именно через проход, но часть капсулы… проходит через стену. Но только часть»**, с
  его же вилкой причин «либо может проблема с алгоритмом, может проблема с моделями».

outcome: |
  Наряд `c-exec-g-5a7c-loot-1-001` написан и выдан в **WIN-U1** от опубликованной головы
  `0618184f`. Задача `open → active`.

  **ЗАДАЧА НЕСЁТ РАЙДЕРОМ ЕГО РЕШЕНИЕ ПРО ТВЁРДОСТЬ ЛУТА** — без новой задачи и без расширения
  аппетита: та, что ставит набор предметов в дом, и обязана сделать их твёрдыми, иначе её
  собственный результат — набор, проходящий сквозь себя. Объявленный обмен «что-то входит —
  `t-loot-1` выходит» не задействован: входить ничего не пришлось.

  **НО РАЙДЕР ВЫДАН С ЗАМКОМ.** Причина, по которой коллизии выключали, в коде НЕ НАПИСАНА:
  `Network/CargoBody.cs:163,193,195` ставит `detectCollisions = false` и `Collider.enabled = false`
  неактивному грузу без объяснения. Поэтому строка 2 требует СНАЧАЛА измерить, во что обходилось
  выключение, и явно разрешает **СТОП домой с числами**, если включение ломает решатель или роняет
  частоту. Догадываться о причине наряд запрещает.

  **ЕГО УТОЧНЕНИЕ ПРО СТЕНЫ ПЕРЕПИСАЛО УЛИКУ, И МОЯ ПЕРВАЯ ГИПОТЕЗА БЫЛА НЕВЕРНА.** Я предположила
  расхождение прямоугольников комнат и сегментов стен. Он уточнил: хозяин идёт именно в проход, а
  клипует край капсулы. Измерение нашло точную причину, и это алгоритм:

  - `HousePassage` НЕСЁТ ширину (`Core/House/HousePassage.cs:55`), плюс собственные `X` и `Z`;
  - **`Core/House/HouseWalk.cs` не читает `Width` ни разу** — ноль совпадений по файлу; планировщик
    ведёт хозяина в ТОЧКУ прохода и не спрашивает, пролезает ли он;
  - капсула хозяина радиусом `0.5`, то есть диаметром `1.0`; проёмы дома `1.5`. Запас по `0.25` м
    на сторону, и только если он идёт ровно по центру.

  Лечится данными, которые уже лежат и не используются. Улика переписана целиком, гипотеза снята
  как неверная.

evidence: |
  Перемерено этой ногой на `0618184f`:
  - `git ls-remote origin` → `main` = `dev` = `0618184f`; слоты WIN-U1 и WIN-U4 `CLEAN / AVAILABLE /
    lease none`.
  - `Settings/GameRulesSettings.asset` — три класса груза `48 / 100 / 240`, пороги шума
    `60 / 150 / 400`.
  - `Network/CargoBody.cs:163,193,195` — выключение коллизий неактивному грузу.
  - `Core/House/HousePassage.cs:50,52,55` — проход несёт `X`, `Z`, `Width`.
  - `Core/House/HouseWalk.cs` — поиск `Width` даёт **0** совпадений.
  - `Settings/HouseholderWalkSettings.asset:24` — `capsuleRadius: 0.5`.
  - Дом на `origin/main`: четыре комнаты по назначению и девять ролей — прочитаны из префаба.
  - `dotnet test … -c Release` → **239 passed**, прогнано этой ногой.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-loot-1-001-call.md` и карточка `c-exec-g-5a7c-loot-1-001`
     (`ready`, `slot: WIN-U1`, `basis: 0618184f`).
  2. `t-loot-1`: `open → active`, журнальная строка про райдер.
  3. `i-householder-walks-through-walls-001`: блок `## issue` переписан целиком по его уточнению;
     первая гипотеза снята как неверная; причина найдена измерением; `review_when` сокращён —
     измерять больше нечего, остался выбор между «путь держит зазор по ширине» и «проёмы шире».
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-loot-1-dispatch-001.md`.

captures:
  - |
    ЕГО ГЛАЗ ВТОРОЙ РАЗ ЗА ДЕНЬ ОКАЗАЛСЯ ТОЧНЕЕ МОЕЙ ГИПОТЕЗЫ. Я записала «ходит сквозь стены» и
    тут же придумала механизм; он уточнил «краем капсулы в проёме», и настоящая причина оказалась
    другой и проще. Правило на будущее: наблюдение владельца записывать ЕГО СЛОВАМИ, а гипотезу
    ставить отдельным помеченным блоком — иначе она читается как факт и уводит следующую ногу.
  - |
    ТРЕТЬЯ УЛИКА ОДНОГО КЛАССА ЗА ДЕНЬ: груз сквозь груз, хозяин краем в косяк. Обе про то, что
    физику собранной сцены не проверяет ничто, кроме его глаза — ни тесты, ни зелёные ворота, ни
    живой осмотр редактора. Для review ставки это уже не случайности, а отсутствующий вид
    доказательства.

decisions_needed: []

play_check:
  - 1 recite — done. Шесть строк карточки пересказаны, райдер назван и обоснован.
  - 2 owner inputs — исполнены: его решение про твёрдость получено дословно; числа набора оставлены ориентиром, а не законом, и это сказано в наряде.
  - 3 do the work — done. `call:executor` выпущен; неизвестная причина выключения коллизий закрыта требованием измерить и правом СТОПа, а не догадкой.
  - 4 self-check — done. Шесть строк карточки плюс райдер свёрнуты в три строки контракта без потерь.
  - 5 close — checkpoint.
  - G2 — done. Райдер вместо новой задачи: аппетит не расширен.
  - G3 — done. Объявленный обмен не задействован.
  - G7 — done. Игровое решение принял он; техническая часть отдана измерению.
  - G10 — done. RESULT финальный.

log: наряд на лут выдан в WIN-U1 от 0618184f и несёт РАЙДЕРОМ его решение «лут не должен через друг друга проходить» — без новой задачи и без расширения аппетита, потому что задача, ставящая набор в дом, и обязана сделать его твёрдым; райдер выдан с замком: причина выключения коллизий в коде не написана, поэтому наряд требует СНАЧАЛА измерить, во что обходилось выключение, и прямо разрешает СТОП домой с числами, если включение ломает решатель; отдельно его уточнение переписало улику про стены и сняло мою гипотезу как неверную — он идёт именно в проход, а клипует край капсулы, и измерение нашло точную причину: HousePassage несёт Width, а HouseWalk не читает её ни разу, при диаметре капсулы 1.0 и проёме 1.5 запас всего по 0.25 на сторону, и лечится это данными, которые уже лежат и не используются

next: |
  Идут: лут в WIN-U1, занятия в WIN-U3.

  Свободна и без наряда: `t-house-asset-1` (дом как переносимый ассет). `t-sight-1` открыта, но
  `Householder.cs` держат занятия.

  Улика про косяк ждёт первой задачи, которой законно трогать движение хозяина; измерять там больше
  нечего, остался выбор между двумя дешёвыми лечениями.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-loot-1-dispatch-001.md
