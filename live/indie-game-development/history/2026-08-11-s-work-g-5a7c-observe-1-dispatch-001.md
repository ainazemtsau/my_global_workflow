# RESULT — s-work-g-5a7c-observe-1-dispatch-001

play: work · выдача наряда на инструмент наблюдения — критерий 8 узла, вырезанный этой волной и
возвращённый живым словом владельца
direction: indie-game-development · node: g-5a7c · track: householder · task: t-observe-1
date: 2026-08-11

base_read: OS голова `5ea56e0a`, дерево чисто. Продукт `origin/dev` = `origin/main` = `87d399f6`,
перемерено `git ls-remote origin`. Все четыре слота `AVAILABLE / lease none`. Пин `36` сверен.

## outcome

Заведена задача `t-observe-1` (order 9) и выпущен наряд `c-exec-g-5a7c-observe-1-001` в WIN-U2.
`t-react-3` сдвинута на order 11 — вердикт владельца по дорожке идёт ПОСЛЕ инструмента, которым он
будет смотреть.

Наряд не существует до расписки — задача остаётся `open`, карточка наряда `ready`.

## ЧТО РЕШИЛО ЗАДАЧУ: ЭТО ЕГО ЖЕ КРИТЕРИЙ, А НЕ НОВОЕ ТРЕБОВАНИЕ

Критерий 8 узла `g-5a7c` дословно требует инструмент разработчика, показывающий цепь и дающий
команды форсирования состояния. Вырез 4 этой волны убрал его. Владелец 2026-08-11 попросил стенд
дважды — то есть снял собственный вырез. Текст выреза в подписанной ставке этой ногой НЕ правится:
его чинит `review`.

**Прежняя строка «ОДНА ИГРА, А НЕ СТЕНДЫ» НЕ нарушается, и это перемерено раскопкой.** Строка
закрытого узла `g-1d84` целиком: «Отдельные стенды РЕЗУЛЬТАТОМ больше не считаются», перечень —
груз, камера, хозяин, дом, то есть перечень ИГРЫ; инструмента разработчика в той карточке нет вовсе.
**Более того: стенды предложил САМ владелец 2026-08-01** («это условно могут быть сцены отдельные,
стенды»), а фраза «результатом больше не считаются» — формулировка ноги, не его слова.

## evidence — прогнано 8 агентами и перепроверено мной первой рукой

Прогон: раскопка истории, три адвоката устройств, три судьи, сведение. Баллы: окно редактора **78**,
отдельная сцена **78/66**, песочница-близнец **54–61**. Победил ГИБРИД: окно плюс полигон, потому что
окно нужно в обоих, а полигон — то, что владелец просил дословно.

**Перепроверено мной, а не принято со слов:**

- **Мир закрыт у курьера:** `NetworkWalkerCourier.cs:29` — `private CarryWorld _world`; наружу
  `LocalPlayerId` (`:50`), `SetLocalInput` (`:103`). Это единственный настоящий блокер.
- **ИМЯ СРАБОТАВШЕГО ОБРАБОТЧИКА ВЫБРАСЫВАЕТСЯ:** в `HouseholderDecisionPipeline` переменная
  `selectedHandlerName` — локальная, наружу уходит только предложение. Прочитано в байтах. Без него
  владелец никогда не узнает, ЧЬЯ строка сработала — а обработчики он собирается принимать пачками.
  Самая ценная из трёх правок.
- **Odin УЖЕ КУПЛЕН и лежит в проекте:** `Assets/Plugins/Sirenix/Assemblies/...`, и в нашем коде
  **не использован ни разу** (`git grep Sirenix` по `Assets/TunnelCrew` пуст). Владелец просил
  «желательно бесплатные», не зная, что платный у него уже есть. Наряд всё равно требует каркас на
  голом Unity, а использование Odin — называть.
- **Сцены в сборке ровно две:** `Lobby.unity`, `IntegratedHouse.unity`. В репозитории лежат ещё две
  вне сборки, включая **мёртвый стенд `HostWalksHisDay.unity`** — он и есть доказательство риска
  «стенд со своей жизнью протухает молча».
- **Панель условий заглушена в Play** (`StartConditionsEditor.cs:63`, `:70`, `:249`) — отсюда
  сегодняшний цикл «выйди из Play, переставь, войди», который задача и ломает.
- **Места по имени заблокированы** (`HouseholderProfileLoader.cs:30`, `:65`, `:627`) — пульт
  работает координатами.

## Форма наряда

Контракт 36, ПРОБА, три строки. Названо заранее:

- **замок против мёртвого стенда механический** — состав компонентов полигона обязан быть
  подмножеством игровой сцены плюс пульт, проверяется чтением двух файлов сцен ТЕКСТОМ;
- **искусственное событие не имеет своей дороги** — тот же `WorldSituation`, те же пороги;
- **инструмент не едет игроку** — `Editor/`, вне списка сцен, `ProjectSettings/**` закрыт целиком;
- **ни одна галка «сделано» не закрывается тем, что «в полигоне работает»**;
- **светлое закрытие задаче НЕДОСТУПНО** — её судит глаз владельца, и заявлять «удобный» в возврате
  прямо запрещено;
- линия разреза: окно остаётся всегда, полигон уезжает своей задачей, обратный разрез запрещён.

**Unity здесь НУЖЕН**, в отличие от трёх предыдущих задач дорожки, — с требованием записать endpoint
и live-проверить мост именно как WIN-U2.

## state_changes

1. Заведена задача `t-observe-1` (order 9, `track: householder`).
2. `t-react-3` сдвинута на order 11.
3. Заведена карточка наряда `c-exec-g-5a7c-observe-1-001`; написан текст наряда.
4. Текст выреза 4 в подписанной ставке НЕ трогается — его чинит `review`.
5. NOW, ставка, узел и дорожка `cargo-hands` не тронуты.

## captures

- **Спека владельца `BURROW-RAIDERS_Demo_Core_Spec_v0.1.md`, на §15 и §16 которой ссылается критерий
  8, В РЕПОЗИТОРИИ ОТСУТСТВУЕТ** — упомянута в пяти документах истории, не лежит ни разу. Значит
  содержание критерия 8 у нас есть только пересказом, и сверить его не с чем. Вопрос вынесен ему.
- **«Команды форсирования состояния» в критерии 8 решены НОГОЙ, а не им** — по разбору
  `work/converge-g-5a7c.md` это осталось предложением, а не его требованием.
- **Пятого звена цепи «материальный результат» не существует ни в одном файле** — окно покажет
  четыре звена и пустое место на пятом. Инструмент дыру не чинит, он делает её видимой.
- **Честная альтернатива втрое дешевле названа и НЕ выбрана:** снять блокировку Play в панели
  условий и добавить кнопку «шум здесь». Не выбрана, потому что он просил стенд дважды и назвал вид
  сверху отдельно.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.

## decisions_needed

Нет вопроса, требующего немедленного ответа. Отсутствие его спеки §15/§16 вынесено ему одной
строкой и работу не блокирует.

## play_check

- 1 recite: done — семь строк карточки и его дословные слова сведены в три строки контракта
- 2 owner inputs (owner): done — он попросил стенд дважды и отдельно снял настройку в инспекторе
- 3 do the work: done — прогнано 8 агентов, ключевые утверждения перепроверены мной в байтах
- 4 self-check: done — риск мёртвого стенда закрыт механическим замком, а не обещанием; недоступность
  светлого закрытия названа заранее
- 5 close: checkpoint — расписка вне этой ноги

## Гейты

- **G1.** Дорожка `householder` держит один корень.
- **G3.** Потолок задач снят его словом; линия разреза названа.
- **G7.** Устройство инструмента выбрано инженерно; «удобно ли» остаётся его словом и в `done_when`
  не превращается.
- **G9.** Карточка узла и текст ставки не редактировались; вырез 4 не тронут.
- **evaluator, клауза 2.** Светлое закрытие задаче недоступно — названо в наряде заранее.
- **evaluator, клауза 3.** Расписка первой строкой возврата.

## next

1. Владелец отдаёт наряд исполнителю; первая строка возврата — расписка.
2. Возврат приходит HOME; задача закрывается отдельной ногой ПОСЛЕ публикации И его взгляда.
3. `t-react-3` (order 11) — его вердикт по дорожке, уже с инструментом в руках.
