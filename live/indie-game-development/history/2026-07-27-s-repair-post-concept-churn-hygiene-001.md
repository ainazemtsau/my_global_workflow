RESULT s-repair-post-concept-churn-hygiene-001 (call: c-repair-post-concept-churn-hygiene-001)
direction: indie-game-development   track: —   play: repair   node/task: g-0c26/post-concept-churn-hygiene

outcome: |
  The tails two concept changes left in hot state are gone, and the next planning leg now
  reads a file that means what it says.

  THE CONTRADICTION, in two lines. `NOW.md` described a direction whose concept had turned
  over twice in about thirty-six hours: eighteen issues, several of them narrating a
  concept that no longer exists or a question a later leg had already answered; four
  `review_when` triggers pointing at a leg that will never run; six forecast drivers where
  the schema allows four; one `open_calls` row carrying a status the schema does not
  define; and `work/converge-g-37a1.md` still routing a reader at answers the owner has
  since refused.

  WHAT IS NOW TRUE.
  1. Eighteen issues are fifteen. Nothing was declared done, nothing was invented, and
     every removal carries its disposition below. The surviving fifteen each keep a stable
     id, a route owner, a review trigger and a resolving pointer, and are on average about
     a third of their previous length — what left is retelling, not findings.
  2. Four dead review triggers now point at the leg that actually carries their rows.
  3. The forecast is four drivers, folded pairwise, with no claim and no receipt dropped.
  4. Six retirement banners make `work/converge-g-37a1.md` say where it lies. No line of it
     was rewritten and no byte was removed.
  5. The superseded `c-map-g-37a1-verify-rows-001` row is removed on the owner's explicit
     word naming that surface; its CALL file stays in `work/` and in Git.
  6. ONE FINDING OF THE 2026-07-27 VERIFICATION IS REFUTED rather than acted on, first-hand
     in the product repository. It claimed that Z1's ground cites
     `ScKernelAtomicityAndConservationTests.cs`, "a file that does not exist". The file
     EXISTS — `tests/GasCoopGame.Core.Tests/Field/Voxel/ScKernelAtomicityAndConservationTests.cs`
     at commit `1a6373b8`, added 2026-07-06, a real mass-conservation, non-negativity and
     atomic-commit-on-throw test for the Sc kernel, authored by an independent test-author
     under the engineering contract. No banner was placed there. `i-substance-laws-canon-candidate`
     now records the true version: the CITATION is sound and only the INFERENCE from it was
     wrong, because the engine ships bounded sinks as a gated, checksum-declared feature, so
     Z1 survives as policy and not as an engine invariant. This is the repair play's own
     precedence rule doing its job — artifacts and commits outrank logs and receipts.

  WHAT WAS DELIBERATELY NOT DONE. CHARTER, TREE, both cards, every owner decision and the
  core-requirements CALL are untouched. `knowledge/strategy-reset-boundary.md` received
  only a banner refresh, not a rewrite — that entry belongs to `review`. LOG was measured
  (47 lines against a ~150-line flag) and NOT rotated. `d-air-counter-visibility-001` and
  `d-october-route-charter` remain open and unanswered. No bet, task, lane or executor CALL
  exists. `archive/**` was not read.

evidence: |
  APPROVAL. The owner was shown one batched diff covering every change below, asked one
  question that repair may not decide for itself, and answered: «применяй + А» — the «А»
  being the removal of the superseded `c-map-g-37a1-verify-rows-001` row from `open_calls`.
  He had first asked «а к чему это строка вообще относиться это что то старое?» and was
  answered before choosing. That is the owner's word naming THAT surface, which is what the
  repair removal boundary requires.

  FIRST-HAND VERIFICATION, product repository `C:\projects\Unity\GasCoopGame` @ `1a6373b8`
  (branch main):
  - `tests/GasCoopGame.Core.Tests/Field/Voxel/ScKernelAtomicityAndConservationTests.cs`
    EXISTS; `git log` gives `0de6cbe1` 2026-07-06; its header names CRITERION 9 old-regime
    invariants — atomic commit on a thrown tick, exact mass conservation, non-negativity —
    and states the builder may never edit it. Therefore the verification's finding
    "a file that does not exist" is FALSE.
  - The other conservation/atomicity files the verification offered as "the real ones"
    also exist (`RW3ConservationAtomicityTests.cs`, `BreachDiffusionConservationTests.cs`,
    `CoarseFieldCreateAtomicityTests.cs`); they are additional, not replacements.

  POINTER SWEEP. Every path cited by `NOW.md` was resolved before the rewrite: 18 of 18
  exist, including the archived `concept-frame-v1.md` (existence checked without reading
  it), `work/marketing/assets/checkpoint-2026-07-12/steamworks-no-app-one-credit.png`,
  `.claude/settings.json` and all five named history receipts. No dangling pointer existed
  and none was introduced.

  IN-FILE VERIFICATION of every banner placed, read directly rather than taken from the
  receipt: §11 lines 550-557 do cite the relocation objective and Z3's single-counter rule;
  §6 lines 208-209 do still list "ore behind it"; §6-BIS lines 232-235 do cite R18's
  deleted half and the pre-rewrite `done_when` numbering; §7 lines 361-363 do carry the
  reconciliation sentence that §7's own correction 1 falsifies; §9's census items 1, 4, 6
  and 8 are as the verification describes, and the phantom verdict on item 4 is refuted by
  the recorded clarification text «средство для этого, а не отдельный слой игры»; §3's note
  and §14's scope are as quoted.

  SCHEMA MEASUREMENTS. `direction_forecast.drivers` allows max 4 and held 6. `open_calls`
  status enum is `ready | running | waiting | blocked | paused` and held an invented
  `superseded`. An issue's fields are id/issue/level/route/review_when/evidence, and
  `i-engine-fit-decision-ladder` carried a seventh, `owner_corrections_2026_07_27`, whose
  content is now inside the issue body and, in its operative form, already inside
  `work/c-map-g-37a1-core-requirements-001-call.md` rules 1-4. LOG: 47 lines.

  FRONTIER INVARIANT. With `bet: null` the schema permits one untracked planning frontier.
  Two ready CALLs existed while this repair ran in parallel by the owner's own arrangement;
  clearing this leg's own call restores exactly one — `c-map-g-37a1-core-requirements-001`.

state_changes: |
  live/indie-game-development/NOW.md
    - updated: 2026-07-27 by s-repair-post-concept-churn-hygiene-001.
    - bet, tasks, recurring: unchanged (`null`, `[]`, `[]`).
    - direction_forecast: `status`, `target`, `as_of`, `update_when` unchanged. `basis`
      unchanged in substance, tightened in wording, keeping the owner's «мы не можем сейчас
      прогнозировать, это лишняя трата времени», the missing denominator, the February 2026
      Next Fest reception reference class and the >=3-outcome condition. `drivers` reduced
      6 -> 4 by folding, per schema max:
        D1 "WHERE THE DIRECTION STANDS" = old driver 2 (card rewritten and approved) + old
           driver 4 (second concept change), with every owner quote preserved.
        D2 "THE ARTIFACT FAILED INDEPENDENT VERIFICATION" = old driver 1, shortened; keeps
           what held, the four failures, and both receipts.
        D3 "NO DATE IS A CONDITION ANY MORE" = old driver 3 + old driver 6 (Valve facts),
           since the Valve dates are exactly what his ruling does NOT move.
        D4 "THE ENGINE QUESTION IS OPEN" = old driver 5 (four engineering facts) + the
           ladder's headline + the gas-versus-liquid pointer.
    - issues: 18 -> 15, reordered into four labelled groups (what the next leg needs;
      design and engineering open; dates/Steam/roadmap; records). Dispositions:
        REMOVED `i-engine-numbers-belong-to-the-old-concept` — FOLDED WHOLE into
          `i-engine-fit-decision-ladder`. Reason: both are the owner's rulings of
          2026-07-27 on the same subject with the same review trigger, and the fold keeps
          his correction «Мы газ, мы отсюда запроектировали под другой концепт», the
          solver-fact versus old-concept-parameter split in full, and the one-metre-cell
          derivation. Nothing referenced it outside NOW.md and the superseded CALL file.
        REMOVED `i-artifact-sections-stale` — DISCHARGED by this leg. Its items (a) §11,
          (b) §6, (c) §9, (e) §7, (f) §14 and the §3 note now carry banners; its item (d)
          is REFUTED first-hand (see evidence) and the correction is recorded inside
          `i-substance-laws-canon-candidate`.
        REMOVED `i-canon-repo-evidence-read` — CLOSED as a record, not an unresolved
          problem. The binding default-closed guard lives in
          `knowledge/canon-clean-authority-reset.md` (read_by every frame/map/canon/research/
          review/work session) and in `.claude/settings.json`; the one authorized bounded
          read and its file list live in `work/october-demo-basis-v3.md`. The fact that
          durable Canon remains NONE is restated inside `i-concept-frame-admission`.
      RETARGETED review triggers from the never-dispatched `c-map-g-37a1-verify-rows-001`
      to `c-map-g-37a1-core-requirements-001`: `i-card-clauses-unverifiable` (passability
      row), `i-substance-states-unpriced`, `i-g5e8c-unsigned-clauses`, and the folded
      ladder. `i-card-clauses-unverifiable` rows 2 and 3 keep their shape-time trigger.
      REWRITTEN SHORTER, no finding dropped: all fifteen survivors.
      `i-substance-passage-open-questions` additionally lost its ANSWERED paragraph, which
      duplicated the card and the digging receipt; only the open owner-content and
      technical halves remain, plus the dropped item and his standing rule.
      `i-frontier-knowledge-stale` updated to "seven frontiers out of date" and records
      that repair has now bannered twice and may do no more.
    - open_calls: `c-map-g-37a1-core-requirements-001` UNCHANGED, byte for byte.
      `c-repair-post-concept-churn-hygiene-001` CLEARED (this leg returns).
      `c-map-g-37a1-verify-rows-001` REMOVED on the owner's explicit «применяй + А» naming
      that row; its CALL file `work/c-map-g-37a1-verify-rows-001-call.md` is untouched on
      disk and in Git, and remains cited as evidence by `i-engine-fit-decision-ladder`.
    - decisions: `d-air-counter-visibility-001` UNCHANGED and still open.
      `d-october-route-charter` UNCHANGED and still open.
      `d-post-verify-route-001` KEPT in hot state although answered — deliberately, because
      its `answer` is the standing six-step route governing the next five legs and no other
      file carries it whole — with `why`/`note` shortened to say exactly that, the three
      option paragraphs dropped to a pointer, and the owner's «порядок такой» and the
      six steps preserved verbatim.
    - END_OF_FILE trailer maintained.

  live/indie-game-development/work/converge-g-37a1.md — SIX RETIREMENT BANNERS, no rewrite,
  no deletion:
    - top CONSUMED block: extended to list the six newly bannered places, including §11,
      which it had never listed.
    - §3, note on the sixth player-facing law: RETIRED — the polarity claim is backwards;
      the renderer forbids rather than forces; single-type-per-region seeding is the cheap
      resolution already named in product code; the table's timescale-bearing laws are
      closed negative by `i-flow-model-unmeasured`.
    - §6: STALE ON A SECOND HORN — the loot horn survives §6-BIS's repair although criterion
      10 legalises loot and economy and R18's ban half is deleted, and §6-BIS itself cites
      that deleted ban and the pre-rewrite clause numbering. Read the ban list from TREE.md.
    - §7, the accepted in-leg reconciliation: RETIRED — falsified by §7's own correction 1;
      the axis is authorship of WHERE, not price. The owner's recorded position stands.
    - §9, the corrected census: NOT A COMPLETENESS CLAIM — five of nine verified; items 1,
      6, 8 and the phantom verdict on 4 named with their defects.
    - §11: NEVER AMENDED — problems 2 and 3 cite a refused answer and a softened rule;
      problem 1 stands.
    - §14: SCOPE — naming consistency only; cannot test criterion 5's third clause or
      passability; still the strongest instrument in either document.
    - END_OF_FILE trailer unchanged and still last.

  live/indie-game-development/knowledge/strategy-reset-boundary.md — BANNER REFRESH ONLY,
  the entry body untouched: the banner no longer names any current frontier (its previous
  one, `c-converge-g-37a1-substance-passage-001`, had itself gone stale within hours) and
  now says seven CALLs out of date, directing every reader to `NOW.md` `open_calls`. The
  rewrite of the entry itself remains reserved to the narrow `review` that owns it.

  live/indie-game-development/LOG.md — one new newest-first line for this leg. Not rotated:
  47 lines against the ~150-line flag.

  live/indie-game-development/history/2026-07-27-s-repair-post-concept-churn-hygiene-001.md
  — this full RESULT.

  os/FRICTION.md — one appended entry (repair step 5), and its misplaced `END_OF_FILE:`
  trailer, which had four entries sitting after it, moved back to the true end of file.
  No OS rule, play, schema or contract version was changed.

captures:
  - The verification's own findings deserve the same adversarial pass it demanded of the
    drift sweep: one of nine ("a file that does not exist") was false on a thirty-second
    first-hand check, and it had already been copied into a live issue.
  - `os/FRICTION.md` had its `END_OF_FILE:` trailer mid-file with four later entries after
    it; fixed in place here, but it suggests appenders do not read to the end.
  - `d-air-counter-visibility-001` has now survived four legs unanswered. It is two minutes
    of owner time and it gates what a player can read on a teammate.

decisions_needed: []

play_check:
  - 1 Name the contradiction: done — hot state described a concept that had turned over
    twice, in eighteen issues, four dead triggers, six drivers, one illegal call status and
    four falsely routing artifact sections.
  - 2 Reconstruct: done — newest-first through NOW/TREE/CHARTER/LOG/history and the named
    artifacts, plus a first-hand read of the product repository at 1a6373b8. Artifacts
    outranked the receipt and refuted one of its findings. Inventory confirmed: no bet, no
    tasks, no lanes, no recurring work, three decisions of which two are open, and one
    planning frontier once this leg's own call clears.
  - 3 Propose corrected state: done — one reason per change, every issue keeping id, route,
    trigger and pointer, every removal carrying a disposition, the forecast basis left
    honest and still `no_basis`, prior bytes left in Git.
  - 4 Confirm (owner): done — ONE batched diff was shown before any file was touched. He
    asked «а к чему это строка вообще относиться это что то старое?» about the superseded
    row, was answered, and then approved with «применяй + А». That is the owner's word
    naming the one surface the removal boundary protects. Nothing outside the shown diff
    was applied.
  - 5 Friction: done — one entry appended to os/FRICTION.md as `logged, watching`; the OS
    was not fixed here.

log: g-0c26/post-concept-churn-hygiene: eighteen issues became fifteen and one verification finding was refuted first-hand.

next: |
  return-to-owner. No continuation CALL is issued: this leg was parallel to the frontier
  and blocks nothing. The direction's single ready frontier is now
  `c-map-g-37a1-core-requirements-001` — step 1 of the six-step route he approved with
  «порядок такой» — and it must run in a FRESH chat with him present.

END_OF_FILE: live/indie-game-development/history/2026-07-27-s-repair-post-concept-churn-hygiene-001.md
