# RESULT — s-work-g-1d84-first-lane-calls-001

leg: s-work-g-1d84-first-lane-calls-001
play: work
direction: indie-game-development
node: g-1d84
call: c-work-g-1d84-first-lane-calls-001 (`open_calls`, `call: null` — текста наряда не было намеренно)
date: 2026-08-04
owner_present: no

## outcome

Написаны три продуктовых наряда на остальные готовые к старту задачи волны — `t-house-1` (три
комнаты, собранные руками), `t-host-1` (хозяина считает судья и он едет в снимке), `t-look-1` (проба
бесплатных наборов). Направление выпускает ТРИ при WIP владельца в три полосы: сцена уже идёт, к ней
встают дом и хозяин. Четвёртый наряд — `t-look-1` — написан целиком, но не выпускается: полосу сверх
трёх запускает сам владелец своим словом, и это записано в самом наряде.

Наряды писались не с нуля: в рабочем дереве лежала незакоммиченная выдача прежней попытки этой же
ноги — три файла нарядов и правка `NOW.md` без строки в `LOG.md` и без RESULT в `history/`, то есть
нога до конца не дошла. Работа этой ноги — перепроверить каждый названный в них факт первой рукой на
объявленной базе и довести до закрытия. **Проверка нашла четыре расхождения, и одно из них
материальное.** Все четыре исправлены в тексте нарядов до выпуска.

## evidence

1. **База подтверждена первой рукой В САМОМ репозитории**, а не в чужом рабочем каталоге и не из
   отчёта: `C:/projects/Unity/GasCoopGame`, `git rev-parse HEAD` = `git rev-parse main` =
   `839df47e78127fe2ebfba5eabb307bf6bdd61e9b`, `git status --porcelain` пуст, ветка `main`.

2. **Существование КАЖДОГО пути, названного в трёх нарядах, проверено `git cat-file -e 839df47e:<путь>`
   и `git ls-tree -r 839df47e`.** Двадцать четыре файла — все `OK`, ни одного `MISS`. Это прямое
   исполнение правила `i-call-named-path-absent-on-declared-basis-004` после четырёх бунсов класса.
   Каталоги посчитаны, а не заявлены: `Assets/CodeRespawn/DungeonArchitect` = 1967 файлов,
   `Assets/Plugins/Sirenix` = 95, `Assets/Plugins/NuGet` = 85, `tests/TunnelCrew.Core.Tests` = ровно 8
   файлов (включая `HouseholderTests.cs` и `HouseholderAdditionalReactionTests.cs`),
   `Assets/TunnelCrew/Art` = 0.

3. **Проверено и то, чего НЕТ** — половина смысла нарядов: папки `Assets/TunnelCrew/Art/` не
   существует; во всей `Assets/TunnelCrew/**` ноль файлов моделей (`.fbx|.obj|.blend|.gltf|.glb|.dae`);
   ноль путей с именами `Quaternius|Kenney|KayKit|PolyHaven|Synty`; ноль путей `FImpossible` (PGG);
   в закрытой `Assets/GasCoopGame/**` осталось ровно ТРИ отслеживаемых файла (`Core.meta`,
   `Core/AGENTS.md`, `Core/AGENTS.md.meta`).

4. **Значения и версии сверены дословно, а не пересказаны.** `ProjectVersion.txt` = `6000.5.5f1`;
   `NetworkPlaySettings.asset` — `_standHalfExtent: 7`, `_standWallHeight: 1.5`,
   `_standWallThickness: 0.5`, `_standDoorwayGap: 2`, `_standDividerZ: 5`,
   `_standWallColor: 0.45/0.45/0.5`, плюс `_playerColors`, `_cargoColor`, `_cargoHeldColor`;
   `TunnelCrew.Core.asmdef` — `"noEngineReferences": true` (строка 13); FishNet `4.7.2`;
   `EditorBuildSettings` — ровно три включённые сцены (`Lobby.unity`, `NetworkWalkers.unity`,
   `Assets/Scenes/SampleScene.unity`); `packages-lock.json` — `com.unity.probuilder 6.1.2`, `depth: 1`,
   притянут `com.ivanmurzak.unity.mcp.probuilder 1.2.23`, прямой строки в `manifest.json` нет;
   `com.unity.ai.navigation 2.0.12` стоит и **измеренно не используется**: `git grep -l
   'UnityEngine.AI|NavMesh'` по `Assets/TunnelCrew/*.cs` не дал ни одного файла, а попадания в сценах
   — пустая заготовка Unity (`m_NavMeshData: {fileID: 0}`).

5. **Номера строк в фактах кода перепроверены поимённо.** `HouseholderRouteController.Update()` — 142,
   `Time.deltaTime` — 144, `Householder.Step` — 147; три кнопки инспектора — 175/181/187;
   `NetworkWalkerCourier.OnServerTick` — 101, `_world.Advance` — 107, `PublishSnapshots` — 114,
   `[ObserversRpc]` + сигнатура `ReceiveSnapshotsObserversRpc` — 167-168; `CarryWorld` держит порядок
   `Cargo.ApplyCarryLoad` (24) → `Walkers.Advance` (25) → `Cargo.Advance` (26), и комментарий файла
   дословно говорит, что порядок живёт здесь, а не в курьере. Поля снимков совпали дословно.
   `WalkSpaceProbe` — `private const int Passes = 4`, читает `_stand.Walls`. `IWalkSpacePort.Resolve` и
   `WalkStep` — в слое правил, без движка. **`Householder.Step` действительно не принимает ни ходоков,
   ни груз, ни позиции игроков:** обе перегрузки берут только `HouseholderLocalMemory`,
   `HouseholderStepInput` (`ElapsedSeconds`, `CurrentFeeling`, `PendingEvent`, `HasListeningPoint`,
   `ListeningPoint`, `RoutePoints`, `Tuning`) и политику решения.

6. **Счёт «12 ссылок» перемерен по GUID, а не по имени.** Литерального `NetworkRuntime` в
   `NetworkWalkers.unity` одно вхождение, но GUID `969c59a50d21a71468e57ffc3cc2647b` встречается
   ровно 12 раз; в `HostWalksHisDay.unity` — 0, и в ней ровно два `MonoBehaviour`. Утверждение наряда
   верно, метод счёта в нём назван неточно и уточнён источником проверки.

7. **Указатель в историю разрешается, и образец происхождения прочитан целиком.**
   `d8d3d67f` = `chore: retire legacy GasCoopGame code and tests`;
   `git show d8d3d67f^:Assets/GasCoopGame/Adapters/Characters/Art/Quaternius/SOURCE.md` отдаёт готовую
   запись (автор, пак, страница, зеркало, CC0-1.0 со ссылкой на текст, SHA-256 архива);
   `git ls-tree -r d8d3d67f^` по той папке = ровно 11 файлов, как и говорит `knowledge/where-art-lives.md`.

8. **Ссылки OS-стороны проверены поимённо:** восемь файлов `knowledge/`, включая строку о курьере
   («СТОП И ВОПРОС ВЛАДЕЛЬЦУ») в `how-the-game-is-built-layers.md` и «нажал играть — уже в игре»;
   все C-контракты `converge-g-1d84.md` от `C01` до `C21` существуют, включая названные C13/C14/C18/C20/C21;
   все W-строки, на которые ссылаются наряды, найдены; разделы `A4/A5/A7/A9` арх-файла на месте;
   все восемь id записей и обе развилки (`d-profile-file-format-recommendation-002`,
   `d-toolkit-reading-of-criterion-10-002`) существуют в `NOW.md`.

9. **`engineering_contract: 36` подтверждён с обеих сторон:** `os/engineering/CONTRACT_VERSION` —
   `current: 36`; продуктовый `validation.config` — `"synced_contract_version": 36`, и продуктовый
   `AGENTS.md` несёт раздел «ДВА РЕЖИМА, ПО УМОЛЧАНИЮ — ПРОБА (контракт v36)». Поэтому `budget: режим
   ПРОБА` законен, а потолок в три `done_when` к этим нарядам не относится: он принадлежит ОПОРЕ.

## findings — четыре расхождения, все исправлены до выпуска

**F1. МАТЕРИАЛЬНОЕ: состояние слотов, записанное в трёх нарядах, устарело.** Наряды утверждали «все
четыре слота `AVAILABLE`, `lease: none`, все четыре каталога чисты и стоят ровно на `839df47e`».
Перемерено первой рукой: **`WIN-U1` уже `CLAIMED` под `lease: c-exec-g-1d84-one-scene-what-exists-001:BUILD`**,
ветка `slot/win-u1`, HEAD `82a6a6c4`, семь изменённых файлов, два коммита впереди базы. U2/U3/U4 —
`AVAILABLE`, чисты, ровно на `839df47e`. Это ровно тот класс, ради которого заведена запись
`i-slots-carry-old-contour-until-updated-001`: измерение устарело за время написания. Во всех трёх
нарядах поимённый блок слотов переписан с прямым запретом брать `WIN-U1`; в наряд `t-look-1` добавлено,
что при трёх идущих полосах свободного слота может не остаться вовсе — тогда СТОП и вопрос домой, а не
работа в чужом слоте.

**F2. МАТЕРИАЛЬНОЕ: первая задача волны уже идёт, а `open_calls` держал её `ready`.** Два коммита в
`WIN-U1` названы её id: `9113b24a` (establish integrated scene) и `82a6a6c4` (record candidate report).
Её собственный отчёт `docs/results/c-exec-g-1d84-one-scene-what-exists-001.md` называет статус дословно
`CANDIDATE on slot/win-u1; owner Play and fresh binding G5 pending`. Продуктовый кандидат: побайтовое
переименование `NetworkWalkers.unity` → `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` с сохранением
GUID `31d4e989f2353534182e51e1c86ef3e9` плюс две переведённые ссылки; свой прогон 43/43 и `check.ps1`
зелёные. `origin/main` при этом остаётся `839df47e` — в продукт это НЕ село. Статус наряда переведён в
`running` с записанной квитанцией запуска: по правилу ядра `running` не предлагается повторно, а
`ready` заставил бы `collect next` выдать идущую работу второй раз. **Закрытием это не является и в
`done` не превращается:** ни отчёт исполнителя, ни зелёный прогон — закрытие остаётся за словом
владельца после Play плюс отдельной свежей биндинговой проверкой в другом физическом чате. Факт
кандидата уехал во все три наряда: целевая сцена сменит имя, а файл сцены и
`ProjectSettings/EditorBuildSettings.asset` объявлены ЧУЖОЙ поверхностью параллельной полосы.

**F3. Неверное имя символа в блоке проверенных путей.** Наряд на дом писал, что стены строит
`BuildWalls`. Такого символа на базе нет вовсе (`git grep BuildWalls` по `Assets` пуст). Фактически —
приватный `Build()` (строка 69) через `Add(...)` (92), стены 78-81, перегородка 87-88, наружу список
отдаётся как `IReadOnlyList<StandWall> Walls` (41-49). Исправлено точными строками. Ровно тот класс
ошибки, который наряд у себя же и запрещает.

**F4. Наряд сузил подписанный текст задачи.** `done_when` задачи `t-look-1` говорит «результат виден
**в сцене**»; наряд написал «результат виден» и слово потерял. Возвращено дословно, а открытый вопрос
«в КАКОЙ именно сцене» оставлен строкой к PLAN, как и был.

## state_changes

1. `live/indie-game-development/work/c-exec-g-1d84-three-rooms-by-hand-001-call.md` — новый файл наряда
   (`t-house-1`), с исправлениями F1, F2, F3.
2. `live/indie-game-development/work/c-exec-g-1d84-host-under-the-judge-001-call.md` — новый файл наряда
   (`t-host-1`), с исправлениями F1, F2.
3. `live/indie-game-development/work/c-exec-g-1d84-free-kits-probe-001-call.md` — новый файл наряда
   (`t-look-1`), с исправлениями F1, F2, F4.
4. `NOW.md` `open_calls` — запись `c-work-g-1d84-first-lane-calls-001` заменена тремя записями нарядов;
   у `c-exec-g-1d84-free-kits-probe-001` стоит поле `dispatch` с его собственными словами о запуске
   четвёртой полосы.
5. `NOW.md` `open_calls` — `c-exec-g-1d84-one-scene-what-exists-001`: `status: ready` → `running` плюс
   поле `launch_receipt` с измеренной квитанцией (F2).
6. `NOW.md` `issues` — `i-slots-carry-old-contour-until-updated-001` закрыт измерением; текст закрытия
   и `evidence` несут ОБА измерения этой ноги, включая разошедшееся второе (F1), и прямо говорят, что
   запись никогда не утверждает свободу слотов.
7. `LOG.md` — строка ноги. `history/2026-08-04-s-work-g-1d84-first-lane-calls-001.md` — этот RESULT.

Задачи `t-scene-1`, `t-house-1`, `t-host-1`, `t-look-1` остаются `ready` в списке задач: право выдачи
живёт в `open_calls.status` (ядро §4), и вводить в список задач второй словарь состояний эта нога не
стала.

## captures

- Ни одной. Ничего нового по дороге не придумано: наряды собраны из карточки, ставки, разбора и
  измеренных фактов кода.

## decisions_needed

- Ни одного НОВОГО. Две развилки владельца висят и держатся своими записями: форма файла профилей
  (`d-profile-file-format-recommendation-002`, держит РОВНО `t-host-3`) и чтение слова «инструментов»
  (`d-toolkit-reading-of-criterion-10-002`, не держит ни одной задачи). Эта нога ни одну не трогала.
- Владельцу принадлежит запуск ЧЕТВЁРТОЙ полосы. Наряд `t-look-1` написан и готов; направление его не
  выпускает, потому что WIP объявлен тройкой, а оговорка про дополнительные параллельные — его
  собственная.

## play_check

- Шаг 1 «Recite» — исполнен: CALL служит открытой ставке `g-1d84`, задачи `t-house-1`/`t-host-1`/`t-look-1`
  принадлежат её списку.
- Шаг 2 «Owner inputs» — владельцу вопросов не задавалось и не требовалось: наряды пишутся из уже
  подписанной карточки, открытой ставки и измеренных фактов кода. Ни одно поле с его словами не
  заполнено пересказом; во всех трёх нарядах прямо написано, что подписи под текстом самих задач он
  не давал.
- Шаг 3 «Do the work» — три `call:executor` в продуктовый репозиторий; направление задаёт исход и
  доказательство, техническое КАК остаётся продуктовому PLAN. Ни одна открытая строка разбора не
  закрыта нарядом: девять строк уехали ВОПРОСАМИ.
- Шаг 4 «Self-check» — исполнен пофактно, результат в §evidence и §findings; четыре расхождения найдены
  и исправлены ДО выпуска, а не замолчаны.
- Шаг 5 «Close» — этот RESULT. Продолжение полос — возврат трёх нарядов домой.
- G1: WIP владельца — три полосы; направление выпускает ровно три наряда, четвёртый ждёт его слова.
  ≤1 продуктовый корень на полосу соблюдён: у каждого наряда свой слот и своя полоса.
- G2: все три задачи служат активной ставке, ничего постороннего не заведено.
- G5: ни одна задача не помечена `done`; кандидат первой задачи прямо назван НЕ закрытием.
- G7: ни одно решение владельца не изобретено; обе висящие развилки оставлены висеть.

## log

Три наряда на первые полосы волны выпущены с перепроверенной базой; найдены и исправлены четыре
расхождения, из них два материальных: слот `WIN-U1` уже занят идущей первой задачей, и сама она стоит
`running` с квитанцией вместо `ready`.

## next

- Три выпущенных наряда идут в продуктовый репозиторий (`t-house-1`, `t-host-1` — свободные слоты
  U2/U3/U4; сцена уже в U1). Возврат каждого — домой, отдельной ногой.
- Четвёртый наряд (`t-look-1`) лежит готовым и ждёт слова владельца.
- `t-scene-1` вернётся кандидатом: его закрытие требует его Play и отдельной свежей биндинговой
  проверки в другом физическом чате.
- Точка пересмотра аппетита стоит после `t-scene-2`, а не сейчас.

## поправка того же дня — F5, найдена при вопросе владельца «как запустить»

**F5. Наряды присваивали себе выбор слота, которого у них нет.** Все три писали «слот выбирает
исполняющая нога». Контракт продукта требует обратного и дословно: `AGENTS.md`, «Worktree workflow» —
владелец называет ровно один из четырёх слотов В СООБЩЕНИИ С ЗАДАЧЕЙ, и «If the message does not name
one of the four slots, ask the owner which slot to use and do not choose one automatically». Прецедент
в этом же направлении уже стоит и найден: коммит `3391d55a` — «slot is named by the owner, not by the
selector». Исправлено во всех трёх нарядах: добавлено поле `slot:` в шапку, блок слотов переписан на
«называет владелец», даны рекомендации (`WIN-U2` — дом, `WIN-U3` — хозяин, `WIN-U4` — проба) с прямым
указанием, что рекомендация обязательной не является, и записана первая операция ноги —
`tools/select-slot.ps1 -Slot WIN-U<N> -Claim -LeaseId <id>:BUILD`, а не-`AVAILABLE` или несовпадение
ветки — STOP. Проверено первой рукой: `pwsh.cmd` и `tools/select-slot.ps1` существуют,
`GasCoopGame_win-u2` стоит на `slot/win-u2`, `GasCoopGame_win-u3` — на `slot/win-u3`.

**Как это пропустили в первый раз:** проверка первой руки шла по путям и фактам КОДА, а процедуру
запуска ноги никто не сверял с продуктовым `AGENTS.md`. Класс тот же, что F3: наряд утверждал про
продукт то, чего в продукте нет.

END_OF_FILE: live/indie-game-development/history/2026-08-04-s-work-g-1d84-first-lane-calls-001.md
