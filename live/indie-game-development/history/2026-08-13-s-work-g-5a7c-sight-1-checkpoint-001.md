# RESULT — s-work-g-5a7c-sight-1-checkpoint-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-sight-1
track: householder-life
date: 2026-08-13
outcome_kind: checkpoint — зрение опубликовано и принято его глазом, но задача НЕ закрыта; лут НЕ приехал

owner_approved: |
  «c-exec-g-5a7c-loot-1-001 и t-sight-1 завершены» — 2026-08-13.

  **ПОЛОВИНА ЭТОГО НЕВЕРНА, и проверено это измерением, а не спором.** Зрение действительно
  завершено и опубликовано. **Лут НЕ завершён:** его нога вернулась `PRESERVED-PAUSED` и ничего
  не опубликовала. `git ls-tree -r origin/main` даёт **ноль** файлов лута.

  Его собственный вердикт по зрению при этом РЕАЛЕН и лежит в байтах продукта
  (`86804752 Record the owner sight trial`), но записан ИСПОЛНИТЕЛЕМ в пересказе, а не его
  словами. Нога это различает и не выдаёт пересказ за подпись.

outcome: |
  **ЗРЕНИЕ ЕСТЬ, ОНО ОПУБЛИКОВАНО, И САМОЕ РИСКОВАННОЕ ДОПУЩЕНИЕ ВОЛНЫ НЕ СБЫЛОСЬ.**

  Допущение 1 ставки звучало так: крыса ростом `0.16` м в человеческом доме находится ниже почти
  всего, и при честном перекрытии мебелью «увидел крысу» станет редкостью, а не механикой. Владелец
  прогнал сам и подтвердил, что хозяин его видит и бежит на него. **Порог смерти 1 не сработал.**
  Откат зрения не исполняется, дорожка `householder-life` не пересобирается.

  **ЗАДАЧА `t-sight-1` ПРИ ЭТОМ НЕ ЗАКРЫТА, и это объявленный заранее порядок, а не осторожность.**
  Её карточка говорит дословно: «светлое НЕДОСТУПНО и это названо заранее — строки 1, 2 и 6 судит
  его глаз. Нужна свежая биндинговая проверка в другом физическом чате плюс его слово». Выпущена
  `c-work-g-5a7c-sight-1-binding-g5-001`.

  **ГЛАВНОЕ ИЗ ЕГО ПРОГОНА — ОН НАШЁЛ ГЛАЗОМ ДЕФЕКТ, КОТОРОГО НЕ ПОЙМАЛ НИ ОДИН ТЕСТ.** Первый
  прогон провалился целиком: хозяин проходил мимо крысы, стоящей в открытую, в коридоре, в проёме и
  в комнате, и в одиночку, и втроём. Причина — реакция `seen-rat` требовала безопасного момента, а
  ходьба безопасной не бывает НИКОГДА (`Householder.cs:2831`), поэтому политика решения отбрасывала
  реакцию, ни разу не заглянув в факт. Зрение существует ровно пока он идёт — значит за всю пробу
  оно не сработало ни разу, а то, что читалось как зрение (разворот на прыжок на кровать), было
  прежним слухом. Обе построенные пробы промахнулись по одной причине: ставили две точки маршрута
  в `0.1` м друг от друга, где хозяин всегда уже стоит. Правка `c4ee0fc1`, регрессия на `20` м.

  **ЛУТ: НИЧЕГО НЕ ПОТЕРЯНО, НО НИЧЕГО И НЕ ПРИЕХАЛО.** Кандидат целиком заморожен на `origin` в
  `preserve/c-exec-g-5a7c-loot-1-001-win-u1-local-20260813` = `e0a30194`. Построено: десять предметов
  данными в четырёх комнатах ровно по его ориентиру 5 / 3 / 2; библиотека, где следующий предмет
  добавляется одной строкой из четырёх полей без правки кода; твёрдость доказана настоящим
  Unity-прогоном на живой `PhysicsScene`. **Замок наряда исполнен буквально:** цена включения
  коллизий сначала ИЗМЕРЕНА — `66.548` мс на `10 240` шагов, ≈ `0.00650` мс на шаг, решатель не
  сломался, — и только потом включена. Единственный блокер: покоящийся предмет стал твёрдым, но его
  окно контакта не читается пока он лежит и не сбрасывается при подъёме, поэтому накопленный удар
  может выстрелить протухшим шумом позже. С приехавшим зрением это стало хуже: выдуманный шум груза
  ранга `3` побьёт настоящее увиденное ранга `2.5` в сведении «один самый громкий за тик».

  **ТРЕТИЙ ДЕФЕКТ ПОДРЯД НАЙДЕН ЕГО ГЛАЗОМ, А НЕ ВОРОТАМИ.** До этого — сквозной проход груза и край
  капсулы в косяке. Сегодня — слепое зрение и дрожь в погоне. Ни один не поймали ни тесты, ни зелёные
  ворота, ни живой осмотр редактора.

evidence: |
  **ВСЁ НИЖЕ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ ЧТЕНИЕМ БАЙТОВ, а не пересказано из отчётов.**

  - `git ls-remote origin` → `main` = `dev` = `c485b30e704b1706675dd92d15c5223b0d166b92`;
    `refs/heads/preserve/c-exec-g-5a7c-loot-1-001-win-u1-local-20260813` = `e0a30194`.
  - `git ls-tree -r --name-only origin/main | grep -i sight` → **12 путей**, зрение на месте.
  - `git ls-tree -r --name-only origin/main | grep -iE "LootLibrary|LootSolidity"` → **пусто**;
    те же пути присутствуют на `e0a30194`.
  - `WorldSituation.cs:4-10` → `None 0, ContactedHouse 1, Landed 2, **SawRat 3**` — прежние члены не
    тронуты, источник въехал ДОБАВЛЕНИЕМ. Это первая проверка формы «ядро только расширяется» на
    живом источнике.
  - `WorldSituationNoise.cs:186-194` → ранг зрения **2.5**, между `RepeatedHeavyNoise 3` и
    `BoardCreaked 2`.
  - `HouseholderSightSettings.asset` → `_range 6.5`, `_sectorDegrees 100`, `_bodyPointCount 5`,
    `_seenFactLifetimeSeconds 1.5`, `_eyeHeight 1.65`, `_ratBodyHeight 0.16`, `_ratBodyRadius 0.06`,
    `_drawSightGizmos 1`.
  - `Householder.cs:1321-1322` (политика), `:2817-2818` (фаза), `:2831` (ходьба) — «занят» это ОДИН
    булев флаг на два смысла.
  - `HouseholderReaction.cs` на `origin/main` → шесть реакций: `daily-route` false,
    `suspicious-noise` **true**, `quiet-rustle` **true**, `repeated-heavy-noise` false,
    `landing` false, `seen-rat` false.
  - **`4fc04b4c:HouseholderReaction.cs` → те же флаги у `landing` и `repeated-heavy-noise` ДО
    зрения.** Это снимает мою прежнюю запись и разбирается ниже.
  - `default.householder.json` → `secure-hot-stove` с `safeToInterrupt: false`, единственная
    непрерываемая из пяти фаз.
  - Слот-состояние `gascoop-slot-state.v1.json` → все четыре слота `AVAILABLE`, `lease none`. Обе
    ноги отпустили аренду честно.

  **ЧТО НОГА НЕ ПРОВЕРЯЛА и отдала биндинговой:** прогон `246 → 251 → 252`, честность луча настоящим
  коллайдером, правило `floor(N/2)+1`, авторитетность направления, отсутствие магического знания и
  совпадение деревьев отката. Это ровно те строки, которые G5 обязан пытаться опровергнуть.

state_changes: |
  1. `c-exec-g-5a7c-sight-1-001`: `ready → done`, уехала в `closed/`, журнальная строка.
  2. `t-sight-1`: остаётся `active`, журнальная строка. Задача НЕ закрыта.
  3. Создан `work/c-work-g-5a7c-sight-1-binding-g5-001-call.md` и карточка
     `c-work-g-5a7c-sight-1-binding-g5-001` (`ready`, `to: session`, `play: work`,
     `track: householder-life`).
  4. `c-exec-g-5a7c-loot-1-001`: `ready → paused` с `unblock_when`, остаётся в `cards/`, журнальная
     строка. НЕ закрыт: кандидат существует и ждёт ре-допуска.
  5. `t-loot-1`: остаётся `active`, журнальная строка с полным разбором.
  6. Заведена улика `i-householder-chase-path-jitters-in-doorways-001`.
  7. Заведена улика `i-safe-interruption-collapses-walking-and-delicate-phase-001`.
  8. Заведена улика `i-loot-candidate-emits-stale-contact-on-pickup-001`.
  9. Заведено решение `d-sight-overrides-the-hot-stove-001` (`open`, три варианта, рекомендация).
  10. Полный RESULT сохранён в `history/2026-08-13-s-work-g-5a7c-sight-1-checkpoint-001.md`.

  **ЧЕГО ЭТА НОГА НАМЕРЕННО НЕ СДЕЛАЛА:** не выпустила наряд-преемник по луту. Это корень другой
  дорожки и отдельная нога. Здесь только исправлена запись о том, что с ним на самом деле произошло.
  Карточка ставки не тронута: порог смерти 1 НЕ сработал, а несработавший порог не требует записи.

captures:
  - |
    **МОЯ ОШИБКА ПРИ ЗАКРЫТИИ `t-occupations-1`, ЗАПИСАННАЯ, А НЕ СПРЯТАННАЯ.** В журнале и в
    сообщении коммита `f9ed5d70` я написала: «фаза secure-hot-stove единственная из пяти помечена
    непрерываемой, ТО ЕСТЬ горящую плиту он сначала обезопасит и только потом побежит». Байтовая
    половина верна. Вывод после «то есть» НЕВЕРЕН и был неверен уже тогда: на той же голове
    `4fc04b4c` реакции `landing` и `repeated-heavy-noise` несли `RequiresSafeInterruption = false`
    и срывали его с плиты. Я вывела поведение из разметки данных, не прочитав политику решения,
    которая эту разметку читает. **Строка 3 самой задачи при этом НЕ нарушена** — она требует ровно
    того механизма, который построен; не выполнено НАМЕРЕНИЕ его документа. Поэтому задача не
    переоткрывается, а вопрос уходит ему решением.
  - |
    **КЛАСС ОШИБКИ ПОВТОРЯЕТСЯ И СТОИТ НАЗВАТЬ ЕГО ВСЛУХ:** и тут, и в прошлой ноге («Control
    вливает без слияния») я выводила ПОВЕДЕНИЕ из одного факта, не прочитав то, что этот факт
    потребляет. Оба раза факт был верен, вывод — нет. Дешёвое лечение: когда закрытие опирается на
    значение поля, читать потребителя поля, а не только само поле.
  - |
    **ЕГО ГЛАЗ ЛОВИТ ТО, ЧЕГО НЕ ЛОВЯТ ВОРОТА, ЧЕТВЁРТЫЙ РАЗ ПОДРЯД.** Сквозной проход груза, край
    капсулы в косяке, слепое зрение, дрожь в погоне. Общая форма всех четырёх: правила думают о
    мире одно, физика делает другое, а тесты проверяют правила. Это не претензия к исполнителям —
    это свойство того, где мы ставим пробы.

decisions_needed:
  - q: |
      Хозяин бросает горящую плиту и бежит на любую громкую вещь. Защищать плиту или пусть бежит?
      Три реакции из шести проходят мимо гейта фазы, и это было верно ДО зрения — зрение стало
      третьей, а не создало дыру. Полный разбор в `d-sight-overrides-the-hot-stove-001`.
    options: [пусть бежит, защитить отдельной задачей сейчас, райдером в t-handlers-1]
    recommendation: |
      Райдером в `t-handlers-1`. Фазы покупались как ОКНО для налётчиков, а сейчас окно закрыто от
      шороха и открыто для всего громкого — чинить стоит. Но новая задача по правилу самой ставки
      выбивает лут, у которого уже почти всё построено, а `t-handlers-1` и так открывает решение
      хозяина, и флаг расщепляется там почти даром.
  - q: |
      Подтверди своими словами вердикт по зрению. Сейчас он лежит в продукте ПЕРЕСКАЗОМ исполнителя
      («после правки он подтвердил, что хозяин видит его и бежит на него»), а строки 1, 2 и 6
      карточки закрываются только твоим словом. Без него биндинговая проверка обязана назвать это
      блокером, а не догадаться.
    options: [подтверждаю, есть оговорки]
    recommendation: |
      Если оговорок нет — достаточно одной фразы. Она нужна не для формы: правило «выведенное не
      равно подписанному» стоит в состоянии как раз потому, что нога однажды выдала вывод за подпись.

play_check:
  - 1 recite — done. Семь строк `t-sight-1` и обязательство ставки пересказаны; проверено, что наряд сжимал их в три, а закрытие судит по семи.
  - 2 owner inputs — исполнены частично и это названо. Его вердикт по зрению и по твёрдости лута существует в байтах продукта, но пересказом исполнителя; его собственные слова запрошены и НЕ выведены. Два вопроса вынесены в `decisions_needed`.
  - 3 do the work — приём двух возвратов, перемеренный по байтам; выпущена биндинговая проверка; заведены три улики и одно решение.
  - 4 self-check — done. Каждая строка отчётов, которую можно проверить байтами, либо перемерена, либо явно отдана биндинговой ноге списком.
  - 5 close — checkpoint. Основание: светлое закрытие `t-sight-1` объявлено недоступным в самой карточке; лут не опубликован вовсе.
  - G5 — не применяется: закрытия нет. Venue следующей проверки назван прямо — свежий физический чат.
  - G7 — done. Ни одного игрового решения не принято ногой. Размен с плитой отдан владельцу с тремя вариантами и рекомендацией.
  - G3 — done. Аппетит не расширен: новых задач не заведено, починка плиты предложена райдером, а не двенадцатой задачей.
  - G10 — done. RESULT финальный, шаги показаны.

log: зрение опубликовано и принято ЕГО ГЛАЗОМ — самое рискованное допущение волны НЕ сбылось, крыса 0.16 среди мебели видна достаточно часто, порог смерти 1 не сработал и откат зрения не исполняется; задача НЕ закрыта, потому что светлое закрытие объявлено недоступным заранее (строки 1, 2 и 6 судит его глаз) — выпущена биндинговая проверка в свежем чате; главное из его прогона: он нашёл ГЛАЗОМ дефект, которого не поймал ни один тест — seen-rat требовала безопасного момента, а ходьба безопасной не бывает никогда, поэтому за всю первую пробу зрение не сработало НИ РАЗУ и то, что читалось как зрение, было прежним слухом; обе построенные пробы промахнулись, ставя точки маршрута в 0.1 м друг от друга; ОТДЕЛЬНО ИСПРАВЛЕНЫ ЕГО СОБСТВЕННЫЕ СЛОВА «завершены»: лут НЕ приехал — вернулся PRESERVED-PAUSED, ноль его файлов на origin/main, кандидат целиком заморожен веткой preserve на e0a30194, при этом райдер «лут не должен через друг друга проходить» исполнен и цена включения ИЗМЕРЕНА числом (66.5 мс на 10240 шагов), а блокер один — окно контакта не сбрасывается при подъёме и выстрелит протухшим шумом, который с приехавшим зрением побьёт настоящее увиденное; и ЗАПИСАНА МОЯ ОШИБКА при закрытии занятий — «плиту он сначала обезопасит» был вывод из разметки данных без чтения политики, а две громкие реакции срывали его с плиты уже тогда, поэтому размен с плитой уходит ЕМУ решением с тремя вариантами, а не чинится ногой

next: |
  `c-work-g-5a7c-sight-1-binding-g5-001` — биндинговая проверка зрения в свежем физическом чате.
  Она либо закрывает `t-sight-1`, либо называет один точный блокер.

  **Вторая нога, готовая к запуску прямо сейчас, и она не эта:** новый наряд по луту от сохранённого
  `e0a30194`. Он владеет починкой окна контакта и публикацией; ремонт покрытия пола и ручной Y-сдвиг
  в него не входят его же запретом. Выпускать его — отдельная нога дорожки `house-and-things`, и
  здесь она намеренно не выпущена.

  Дорожки при этом свободны обе: все четыре слота `AVAILABLE`. `t-house-asset-1` тоже открыта и ни
  от чего не ждёт.

END_OF_FILE: live/indie-game-development/history/2026-08-13-s-work-g-5a7c-sight-1-checkpoint-001.md
