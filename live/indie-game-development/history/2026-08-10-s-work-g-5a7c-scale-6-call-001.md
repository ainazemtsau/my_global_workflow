# RESULT — s-work-g-5a7c-scale-6-call-001

play: work · close: light — because каждая строка `done_when` входящего CALL это механический факт,
перемеренный этой ногой первой рукой: файл наряда и карточка существуют, слот прочитан настоящим
селектором, база и пин прочитаны `git` и `validation.config`, происхождение каждой несущей строки
сверено с закоммиченными словами владельца, а весь расчёт посчитан здесь и перепроверен против
параллельной ноги, а не заимствован
direction: indie-game-development · node: g-5a7c · task: t-scale-6 · call: c-work-g-5a7c-scale-6-call-001
date: 2026-08-10

base_read: OS — `wt/indie-game-development` = `17f83e4b`. В рабочем дереве лежат посторонние правки
другой сессии (`.gitignore`, `README.md`, `os/BOOTSTRAP.md`, `os/FRICTION.md`, `os/KERNEL.md`,
`os2/CONCEPT.md`, `osctl.py`, `panel/cards.py`, плюс неотслеживаемые `.python-version`,
`pyproject.toml`, `uv.lock`) — **этой ногой они не тронуты и в её коммит не входят**. Продукт: три
чтения `git ls-remote origin` в течение сессии, база двигалась дважды. Терминальное состояние —
`origin/dev` = `origin/main` = `origin/slot/win-u1` = `9786da4fe1e6c52f0ee8838cf093db1701b3b8da`,
дерево `a4d326b5a144fddc7f081f41dc525655c7d3342b`. Продуктовые байты, ветки, слоты и slot-state этой
ногой НЕ изменялись; единственные действия в продукте — чтение и read-only селектор.

## outcome

Наряд исполнителю на `t-scale-6` написан, слот назван владельцем и перемерен, карточка
зарегистрирована: `c-exec-g-5a7c-more-hands-lift-higher-001`, WIN-U1, база `9786da4f`, контракт 36,
режим ПРОБА.

**Задача переписана по существу тремя его словами этого дня, и все три записаны дословно в наряде.**
Он спросил сам, до всякой работы: «а нам тут не нужна будет вертикальность? как мы поймем что 4ом
могут например больший груз поднять или выше?» — и был прав. Подписанная строка критерия 3 узла
говорит «чем больше налётчиков взялись, тем ВЫШЕ и послушнее идёт груз», `done_when` карточки
говорит «ПОДЪЁМНАЯ сила суммируется по рукам». **Входящий CALL это слово потерял** и формулировал
задачу как «усилие складывается по рукам»; наряд, написанный по CALL, построил бы горизонтальную
сумму — то есть, по расчёту ниже, ничего наблюдаемого.

Заказан один закон вместо назначения высоты: у живого груза есть вес, руки тянут к запрошенной
точке, каждая со своим потолком, и груз висит там, где это уравновесилось. Ни таблицы по числу рук,
ни отсечки «одному нельзя», ни зашитой в правила величины. Его прямой запрет: «не должно быть, что
один… поднялся на определенную высоту, три еще на какую-то определенную»; «никаких… искусственных
ограничений».

**Два шва заложены без функции, по его же требованию «мы можем ПОТОМ это сделать, я переживаю чтобы
это БЫЛО заложено сразу, чтобы мы не захардкодили поведение»:** потолок силы читается у каждой руки
(место будущей прокачки телекинеза), а запрошенная точка груза становится входом закона с выносом и
высотой, причём доступная сила падает с выносом (место будущего управления «вперёд-назад-вверх-вниз»).
Оба стоят на сегодняшних значениях, поведение игры не меняют, и оба обязаны быть доказаны тестом,
а не заявлены. Первый из двух швов `t-scale-7` этим выполняется здесь — названо прямо, `t-scale-7`
остаётся с классом груза.

Расчёт закрыл дешёвый неправильный путь и поймал одну ошибку в собственном наряде: **«послушнее» не
приходит от числа рук.** Лишняя рука добавляет жёсткости при неизменной вязкости, то есть делает
груз ПРУЖИНИСТЕЕ; наивная вертикаль на одном лишь собственном демпфировании тела дала бы перелёт
36 / 49 / 61% на одной / двух / четырёх руках — прямо наоборот от подписанной строки. Требование
«спокойнее, когда рук больше» переписано как построенное и отдельно доказываемое.

`t-scale-6` остаётся ОТКРЫТОЙ. Её закрывает его слово после прогона.

## evidence — перемерено первой рукой, а не принято с брифа

**Слот.** `tools/select-slot.ps1 -Slot WIN-U1` (только чтение), прогнан непосредственно перед
выпуском: `state: CLEAN`, `lifecycle: AVAILABLE`, `lease: none`, `availability: AVAILABLE`,
`branch: slot/win-u1`, `head: 9786da4fe1e6c52f0ee8838cf093db1701b3b8da`, `mcp-endpoint: unrecorded`,
`state-authority: C:\projects\Unity\GasCoopGame_slot-state\gascoop-slot-state.v1.json`. Слот назван
владельцем дословно: «WIN-U1».

**База и её движение в течение сессии.** `git ls-remote origin` давал последовательно `2986921d`,
затем `9786da4f`. `9786da4f` имеет единственного родителя `2986921d` и меняет только
`docs/results/**`. Дерево `a4d326b5a144fddc7f081f41dc525655c7d3342b`. Локальная `main` = `11f8ddbf`
и отстала — названо в наряде и запрещено догонять.

**Пин.** `os/engineering/CONTRACT_VERSION` `current: 36` против продуктового `validation.config`
`synced_contract_version: 36`. Re-sync не нужен.

**Закон прочитан по байтам, не по документам:** `CargoHaul.cs` (сила по X/Z и поворот, потолок
внутри `HandForce`, демпфирование один раз на тело), `AuthoritativeCargoRoster.cs:136` (высота
назначается `rules.CargoCarryHeight` в тике первой руки), `CargoBody.cs:178` (`FreezePositionY` у
держимого) и `:279-284` (гравитация только `Active && !Held`), `CargoPose.cs` (просьба рук
двумерна), `CargoHold.cs:42` (`Strength` — мёртвый шов), `CargoSnapshot.cs` (`Y` и `HolderCount` уже
едут по сети), `NetworkCargoPresentation.cs:60` (клиент рисует полученный `Y`),
`AuthoritativeCargoRoster.ResolveWalk` (верх препятствия = `State.Y + halfHeight`, поедет сам),
`NetworkPlaySettings.asset` (все величины).

**Расчёт посчитан здесь.** Скорость с грузом `0.6 × 0.6 = 0.36` м/с; установившаяся тяга
`0.36 × (350 + 20·4) ≈ 155` Н против потолка ОДНОЙ руки `200` Н — одна рука уже сегодня держит
полную скорость с запасом ≈29%, а вся разница между двумя и четырьмя руками ≈2 см отставания. Вес
`20 × 3 = 60` Н против тех же `200` Н — вертикальной ступени тоже нет; разброс высот по всем четырём
рукам ≈2.2 см, то есть полтолщины груза `0.04`. HOW-умолчание ступени (вес ≈`144` Н, масса ≈`48`)
названо умолчанием с причиной, объявлено обратимым и не замораживается ни одним тестом.

**Параллельная read-only нога и что с ней сошлось.** `work/research-cargo-haul-law-2026-08-10.md`
(leg `s-work-g-5a7c-scale-6-law-001`, база `9786da4f`) вывела закон независимо. Перепроверено этой
ногой поштучно:

| Утверждение | Их число | Мой пересчёт | Итог |
|---|---|---|---|
| затухание вертикали на 1 / 2 / 4 руках | 0.32 / 0.23 / 0.16 | 0.31 / 0.22 / 0.16; перелёт 36 / 49 / 61% | сошлось |
| затухание горизонтали сегодня | 1.08 / 0.77 / 0.54 | 1.08 / 0.76 / 0.54; перелёт 2.5% и 13.5% | сошлось |
| предел поворота на 1 / 2 / 4 руках | 33 / 67 / 133 °/с | 33.3 / 66.6 / 133.2 °/с | сошлось |
| `126 Н` и `0.875` в опубликованном отчёте неверны | — | строки 315-316 `docs/results/c-exec-g-5a7c-scale-probe-001.md`; обе без `rigidbody.linearDamping: 4`; верные ≈`156 Н` и ≈`1.08` | сошлось |
| порог массы, где одна рука перестаёт держать | 49.7 / 184 | **51.4 / 190** | расхождение ≈3%, вывод тот же |

Ни одно из этих чисел в наряде не заморожено: исполнитель мерит сам
(`knowledge/compute-a-law-before-freezing-it.md`).

**Слова владельца, дословно и в наряде целиком:** про отказ от таблицы высот и «никаких…
искусственных ограничений»; про «один там не сильно подымает… другой выше»; про прокачку силы
телекинеза («сначала… только самые мелкие предметы… при полной прокачке… большие. НО это пока не
проработано но нужно заложить возможность прокачки»); про управление положением («вперёд назад верх
вниз… должно быть ограничено силой… чем дальше по горизонтали тем сила тоже слабее») с его же
уточнением «мы можем ПОТОМ это сделать»; и слово о слоте «WIN-U1».

## Диспозиция по `done_when` входящего CALL — все шесть требований

1. **Наряд лежит в `work/` и зарегистрирован executor call card.**
   `work/c-exec-g-5a7c-more-hands-lift-higher-001-call.md` создан; карточка заведена этой
   транзакцией.
2. **Слот, lawful basis и pin перемерены.** Селектор, `git ls-remote`, `validation.config` — выводы
   приведены выше дословно.
3. **Происхождение строк проверено.** Таблица источников в наряде; находка о потерянном слове
   «подъёмная» записана против входящего CALL, а не сглажена.
4. **«Четверо / сумма / потолок на руку / сеть» сведено с текущими байтами.** `MaxHolders` уже
   число, потолок уже стоит внутри `HandForce` до суммы, снимок уже несёт `Y` и `HolderCount` —
   сетевой работы задача не требует, и это записано в наряде.
5. **Границы `t-scale-7` и architecture issue сохранены.** Класс груза остаётся за `t-scale-7`;
   общая шина ситуаций (`i-world-situations-need-extensible-reaction-architecture-001`) запрещена
   границей. Первый шов `t-scale-7` (личная мощность) выполняется здесь — расхождение с CALL названо
   прямо и в наряде, и здесь.
6. **Маршрут вопроса при отсутствии чисел записан прямо.** Он не понадобился в виде «чисел не
   нашлось»: числа посчитаны, и вопрос об устройстве ступени был вынесен владельцу ДО наряда
   вариантами с рекомендацией. Его ответ и стал заказом.

## Что этот наряд НЕ покрывает — записано против себя

- **Клавиши управления положением груза не делаются** — только форма закона. Улика
  `i-telekinesis-carry-distance-is-fixed-001` не закрывается: `0.8` остаётся на месте.
- **Прокачка не делается** — только место, куда она приедет.
- **Разные по весу предметы не появляются.** Текстура «лёгкий один, средний двое» в этой волне
  недостижима: `t-scale-7` покупает шов на один класс. Владельцу это сказано прямо, и он ответил,
  что показ ступени высотой «тоже ничего не плохо».
- **Опубликованные неверные числа в `docs/results/c-exec-g-5a7c-scale-probe-001.md` не чинятся** —
  заведены уликой, наряд лишь запрещает их наследовать.
- **Волочение груза по полу как источник постоянного шума не чинится** — названо опасностью с
  порогом в наряде и обязано вернуться числом.

## state_changes

1. Зарегистрировать call card `c-exec-g-5a7c-more-hands-lift-higher-001`: `_kind: call`,
   `status: ready`, `to: executor`, `for: t-scale-6`, `issued: 2026-08-10`,
   `call: work/c-exec-g-5a7c-more-hands-lift-higher-001-call.md`, `repo: ainazemtsau/GasCoopGame`,
   `engineering_contract: 36`.
2. Закрыть возвращённую call card `c-work-g-5a7c-scale-6-call-001`: `status: closed`, история —
   `history/2026-08-10-s-work-g-5a7c-scale-6-call-001.md`.
3. Завести issue `i-published-haul-numbers-omit-body-damping-001`: `level: execution`,
   `route: work`, evidence — этот отчёт. Улика не авторизует работу и не становится задачей.
4. `t-scale-6`, `g-5a7c`, `NOW.bet` и любые другие карточки НЕ трогать: задачу закрывает его слово
   после прогона.
5. Одним `osctl leg close` сохранить этот полный RESULT в
   `history/2026-08-10-s-work-g-5a7c-scale-6-call-001.md` и добавить log ровно один раз в журналы
   `g-5a7c`, `t-scale-6`, возвращённого call, нового exec call и новой улики.

Артефакты, входящие в тот же коммит: файл наряда и
`work/research-cargo-haul-law-2026-08-10.md` — выход параллельной read-only ноги внутри этого же
CALL, на который наряд ссылается по пути. Посторонние правки другой сессии в коммит не включаются.

## captures

- Занос груза на повороте существует уже сегодня и измерим без единой правки (33 / 67 / 133 °/с).
  Взят в наряд как контрольный прогон «до», отдельной работы не требует.

## decisions_needed

Нет. Оба решения этой ноги закрыты его прямыми словами того же дня: устройство ступени — «не
контролировать по высоте… никаких искусственных ограничений»; слот — «WIN-U1».

## play_check

- 1 recite: done — `t-scale-6` и входящий CALL сведены попунктно, задача служит активной ставке
  `g-5a7c`
- 2 owner inputs (owner): done — он сам открыл вопрос вертикали, отверг таблицу высот словами «не
  должно быть, что один… поднялся на определенную высоту», потребовал «закладывать структуру,
  которая расширяется», назвал прокачку и управление положением как ЗАЛОЖИТЬ-а-не-сделать, и назвал
  слот «WIN-U1»
- 3 do the work: done — наряд написан, слот/база/пин перемерены, закон посчитан и перепроверен
  против параллельной ноги, три находки записаны
- 4 self-check: done — шесть требований входящего CALL имеют отдельные evidence-строки; не покрытое
  названо против себя
- 5 close: done — возвращённый call закрывается, новый exec call регистрируется, улика заводится,
  задача остаётся открытой до его слова

## Гейты

- **G5.** `close: light` заявлен построчно: все шесть требований входящего CALL — механические
  факты, перемеренные здесь из байтов, селектора и уже сказанных владельцем слов. Ни одна строка не
  требует суждения, которого никто не сделал. В продукте ничего не строилось и не запускалось —
  названо явно.
- **G7.** Геймплей не решён ногой: устройство ступени вынесено владельцу вариантами с рекомендацией
  ДО наряда, величина объявлена HOW-умолчанием с причиной и обратимостью, «послушнее» не выдано за
  бесплатное следствие, а названо построенным требованием.
- **G3.** Аппетит не растянут: клавиши, прокачка, класс груза, шумовой слой и неверный
  опубликованный отчёт ушли в границы и улику, а не в работу. Единственное расширение — первый шов
  `t-scale-7`, и оно названо вслух с причиной.
- **G9.** Карточка узла не редактировалась — только строка в её журнал. `done_when` задачи не
  тронут.
- **Устройство игры.** `knowledge/how-the-game-is-built-layers.md` соблюдено: правило остаётся у
  судьи, курьер правил не получает, наряд велит СТОП с вопросом владельцу, если работа упрётся в
  курьер. `knowledge/compute-a-law-before-freezing-it.md` соблюдено: закон посчитан до заказа, ни
  одно число не заморожено.
- **Панель.** Направление панели не объявляет — регенерация пропущена.

## log

наряд на подъём написан: высота стала равновесием веса и рук, потерянное CALL слово «подъёмная» восстановлено из подписанной карточки, расчёт поймал обратное — лишняя рука делает груз пружинистее, а не послушнее — и два шва, прокачка и положение груза, заложены без управления

## next

`CALL c-exec-g-5a7c-more-hands-lift-higher-001` — отправить наряд исполнителю в продуктовый
репозиторий, слот `WIN-U1`, база `9786da4f`. После HOME отдельная нога Направления читает возврат
первой рукой; `t-scale-6` закрывает слово владельца о том, видна ли ступень.

END_OF_FILE: live/indie-game-development/history/2026-08-10-s-work-g-5a7c-scale-6-call-001.md
