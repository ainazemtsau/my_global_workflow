# RESULT — s-map-g-5a7c-scale-criterion-001

play: map · owner_approved
direction: indie-game-development · node: g-5a7c · call: c-map-g-5a7c-scale-criterion-001
date: 2026-08-08
base_read: OS `2464316a`, дерево `wt/indie-game-development` чисто на входе. Продуктовый репозиторий
этой ногой не читался и не изменялся — правка касается только карточки узла.

## outcome

Строка 1 карточки узла `g-5a7c` говорит то, что направление действительно строит, и владелец
подписал её точный текст. Требование «пропорция живёт одной названной настройкой» больше не стоит
в узле, и его возвращение закрыто последним предложением строки.

## owner_approved

**Точный текст показан владельцу целиком до подписи, и подписан дословно: «да, оформляй»**
(2026-08-08, в ответ на полностью процитированную новую редакцию строки и на разбор того, что в
ней изменено и почему).

Новая строка 1:

> 1. МАСШТАБ. Мир строится в настоящих метрах: дверь, обстановка и хозяин имеют человеческие
> размеры, потому что дом человеческий. Налётчик в нём ростом 0.16 м — его вердикт 2026-08-07
> «берём 0,16», данный на показанной ему композиции, и подтверждённый прогоном 2026-08-08 «физика
> держится, размер тот». Собственные числа налётчика — глаза, радиус, дальность взятия, длина
> хвата — согласованы с его ростом; камера, столкновения и скорость работают на этом росте. Ширина
> проходов и размеры комнат — числа описания дома, а не следствие настройки. Общей настройки
> масштаба нет и не заводится: пересчёт мира был однократным и кончился.

## evidence

**Что сохранено из прежней редакции, дословно:** число 0.16 и его вердикт «берём 0,16» с датой и
обстоятельством; требование, чтобы камера, столкновения и скорость работали на этом росте.
**Что добавлено:** его второй вердикт «физика держится, размер тот», которого в строке не было,
потому что на момент её написания прогона ещё не было.
**Что убрано:** «Пропорция живёт одной названной настройкой, а не рассыпана по объектам» и
«Как это устроено внутри движка, карточка не назначает» — первое было устройством, которого он не
подписывал, второе стало лишним, когда устройство перестало назначаться.
**Что дописано против возврата:** «Общей настройки масштаба нет и не заводится: пересчёт мира был
однократным и кончился». Без этой фразы требование вернулось бы само — оно уже один раз выросло
из ничего.

**Перемерено после правки:** в блоке `done_when` ровно 12 критериев, все на месте; дифф карточки
узла — одна заменённая строка и один убранный пустой хвост блока; критерии 2–12, `goal`, `why`,
`edge`, `risk`, `appetite`, `kill_by` и строка исключений байт в байт прежние.
Запись в реестр `owner_approved` перемерена после записи: хвост строки несёт новую клаузу с
указателем на этот отчёт, прежние 32 488 символов сохранены целиком.

## state_changes

- `g-5a7c` `## done_when`: строка 1 заменена подписанным текстом. Остальные одиннадцать критериев
  и строка исключений не тронуты. Статус узла `active` не менялся.
- `owner_approved`: добавлена клауза `g-5a7c criterion 1 rewritten 2026-08-08` с его словами и
  указателем на этот отчёт.
- Закрыта улика `i-node-criterion-demands-a-scale-setting-we-are-not-building-001` — диспозиция:
  противоречие устранено правкой, ради которой улика и заводилась.
- Закрыт возвращающийся `c-map-g-5a7c-scale-criterion-001`.
- Зарегистрирован `c-work-g-5a7c-scale-3-call-001` (`to: session`, `play: work`, `for: t-scale-3`,
  `ready`), бриф — `work/c-work-g-5a7c-scale-3-call-001.md`.

## captures

- Следующий наряд получил обязанность, которой раньше не было: проверять происхождение каждой
  несущей строки `done_when` до того, как она уедет исполнителю. Родилась из этого случая и
  стоит в брифе `c-work-g-5a7c-scale-3-call-001` прямым текстом.

## decisions_needed

Нет.

## play_check

- 1 recite: done — миссия и текущее состояние дерева пересказаны; правится один критерий одного
  активного узла, остальное дерево не трогается.
- 2 candidates & evidence (owner): skipped — это не пересборка карты, а узкая правка одной строки
  по уже состоявшемуся разбору. Его кандидат и есть предмет правки: «разные же будут геометрии…
  мы ж будем создавать те самые уровни и всё, и смотреть». Свежего `map_evidence` такая правка не
  требует, и вопросом это ему не приносилось.
- 3 skeleton (owner): skipped — новых узлов нет, порядок дерева не меняется, показывать нечего.
- 4 cards: done — переписана одна строка одного критерия; `goal`, `why`, `edge`, `risk` не тронуты.
- 5 per-node verdict (owner): done — его слово **«да, оформляй»** на показанный целиком точный
  текст. Один узел — один вердикт, молчаливой пакетной приёмки нет.
- 6 order (owner): skipped — порядок узлов не менялся.
- 7 depth check: done — глубина не менялась, детей не заводилось.
- 8 lens sweep: skipped — покрытие линз правкой одной строки не меняется.
- 9 close (owner): done — карточка сохранена после подписи, следующая нога названа и
  зарегистрирована.

## Гейты

- **G9.** Точный артефакт показан целиком ДО подписи, подпись процитирована дословно, запись
  внесена в реестр `owner_approved`. Ничего сверх показанного текста не изменено.
- **G7.** Нога не решала за него: она принесла один текст и остановилась. Формулировка «общей
  настройки масштаба нет» — не изобретение ноги, а запись его же довода, которым он снял задачу.

## log

строка 1 карточки узла переписана и подписана — «да, оформляй»; общей настройки масштаба нет и не заводится

## next

`CALL c-work-g-5a7c-scale-3-call-001` — нога направления пишет наряд исполнителю на `t-scale-3`
(вертикаль у игрока), с обязанностью проверить происхождение каждой несущей строки задачи.

END_OF_FILE: live/indie-game-development/history/2026-08-08-s-map-g-5a7c-scale-criterion-001.md
