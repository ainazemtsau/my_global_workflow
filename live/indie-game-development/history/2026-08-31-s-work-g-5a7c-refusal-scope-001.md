# RESULT — s-work-g-5a7c-refusal-scope-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-refusal-reason-1, полоса `почему`
track: почему
date: 2026-08-31
outcome_kind: scope — замер СТОПа перевыверен и держится; слова уже разрешены приёмкой, потолок заменён правилом, ожидание чужой ноги снято замером

owner_approved: |
  Отдельного слова не требуется и не выдумывается. Добавление новых исходов **уже разрешено
  строкой приёмки 2**, одобренной владельцем при нарезке волны 2026-08-30, — она разрешает их
  «ТОЛЬКО после замера, что старого не хватило». Замер предъявлен и перевыверен.

  Поднятие потолка правок и выдача одного файла — операционные решения нарядa, не изменение
  предмета. Ни одна строка `done_when` не изменена и ни одна не добавлена.

outcome: |
  **ТРЕТИЙ ХОРОШИЙ СТОП ЗА ДЕНЬ, И ЕДИНСТВЕННЫЙ, ГДЕ ИСПОЛНИТЕЛЬ НЕ ТРОНУЛ НИ ОДНОЙ СТРОКИ.**
  Ноль правок, ноль прогонов, `HEAD` на `a45b4957`, чекаут чист, лиза удержана. Наряд просил
  ровно этого: «не изобретай слово молча» — и он не изобрёл.

  **ЗАМЕР ДЕРЖИТСЯ, ПЕРЕВЫВЕРЕН ПО БАЙТАМ.** Набор исходов действительно восемь отказных плюс
  `Won`. Мест возврата пустого решения в трёх поставочных обработчиках **девять** — четыре в
  `Chore`, четыре в `LocalIncident`, одно в `Standing`, — и все девять сегодня схлопываются в одно
  безымянное слово. Его пять контрпримеров опираются на реальные ветки: закрытая chore, ложное
  авторское `when`, запрещённый факт в собранном инциденте, потерянная вещь при известном месте,
  кандидат, не способный прервать текущий ответ.

  **ОН СПРОСИЛ РАЗРЕШЕНИЯ НА ТО, ЧТО УЖЕ БЫЛО РАЗРЕШЕНО. ЭТО ТРЕТИЙ СЛУЧАЙ ЗА ДЕНЬ.** Строка
  приёмки 2 разрешает новые слова при условии замера; замер сделан, значит слова разрешены картой,
  а не направлением. Утром то же случилось со зрением (самоперекрытие стояло в его `done_when` 3) и
  с закрытием (`AGENTS.md` он спросил верно, но не заметил невыполненную строку 10). **Три ноги
  подряд спросили дом о том, что было написано в их собственной приёмке.** Это уже не совпадение
  исполнителей, а свойство нарядов: приёмка читается один раз на старте и не перечитывается в
  момент СТОПа.

  **ПОТОЛОК ЗАМЕНЁН ПРАВИЛОМ, А НЕ БОЛЬШИМ ЧИСЛОМ.** «Больше пяти мест» отменено в пользу «одно
  слово на один различимый отказ, до которого поставочный обработчик реально доходит», с жёстким
  задником: девять файлов или тринадцать членов набора. Число, взятое из воздуха, ограничивало бы
  не то — работа тут измеряется различимыми отказами, и их девять, а не пять.

  **ДОБАВЛЕНЫ ДВА ФИЛЬТРА НА КАЖДОЕ НОВОЕ СЛОВО, И ВТОРОЙ ЗАЩИЩАЕТ ТРЕБОВАНИЕ ВЛАДЕЛЬЦА.** Первый:
  слово живёт, только если есть тест, доходящий ровно до того места возврата. Второй, важнее:
  слово обязано быть фразой, прочитав которую владелец знает, **что поменять в профиле**. Его
  требование дословно — «достоверную информацию, вот почему что не так пошло», — и «внутренняя
  проверка не прошла» этому не отвечает: это номер в человеческой одежде.

  **ОЖИДАНИЕ ЧУЖОЙ НОГИ СНЯТО ЗАМЕРОМ, А НЕ РАЗРЕШЕНИЕМ.** Он спросил, ждать ли WIN-U1, потому что
  `OutcomeWord` лежит в `Network/**`. Направление прочитало рабочее дерево зрения: там изменены
  `NetworkHouseholder.cs` и файл тестов — **`NetworkWalkerCourier.cs` не тронут**. Столкновение
  было папочным, а не файловым. Выдан ровно один файл; простой слота на неизвестный срок отменён.

evidence: |
  Перемерено направлением на продуктовом `main` = `dev` = `4e9177af`:

  - `HouseholderDecisionCandidate.cs` — набор `NotTriggered..LostArbitration` (восемь отказных)
    плюс `Won = 8`;
  - число мест `return default;`: `HouseholderChoreDecisionHandler.cs` — 4,
    `HouseholderLocalIncidentDecisionHandler.cs` — 4, `HouseholderStandingDecisionHandler.cs` — 1;
  - `HouseholderChoreDecisionHandler.cs:285-289` — отказ по `belief.IsLost`,
    `!belief.Where.HasPoint`, `!belief.Where.IsInRoom(...)`: три разные причины, один выход;
  - `OutcomeWord` существует ровно в одном файле продукта —
    `Assets/TunnelCrew/Network/NetworkWalkerCourier.cs:562`, вызов на `:538`;
  - рабочее дерево `C:\projects\Unity\GasCoopGame_win-u1`, `git status --porcelain`:
    изменены `Assets/Tests/TunnelCrew/ThreeRoomHouse/HouseholderSightEditModeTests.cs` и
    `Assets/TunnelCrew/Network/NetworkHouseholder.cs`. Больше ничего;
  - `slot/win-u3` = `a45b4957`, чекаут чист — правок он действительно не делал.

  Из его отчёта, как улика: две строки живого отчёта `why`, показывающие, что экран готов
  (`was a real candidate, another one was preferred [wanted it by -1]`).

state_changes: |
  ДОПИСАНО (1 документ наряда):
  - `work/2026-08-31-call-refusal-reason-1.md` — раздел «ДОПОЛНЕНИЕ 2026-08-31: слова разрешены
    замером, потолок поднят, ждать WIN-U1 не надо». Прежний текст не тронут.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-refusal-reason-1` остаётся `active`, `c-exec-g-5a7c-refusal-reason-1-001` остаётся `ready`.
    Работа не начата, закрывать нечего.

captures: |
  - **ТРИ НОГИ ПОДРЯД СПРОСИЛИ ДОМ О ТОМ, ЧТО СТОЯЛО В ИХ СОБСТВЕННОЙ ПРИЁМКЕ.** Зрение —
    самоперекрытие луча (`done_when` 3); закрытие — не заметило невыполненную строку 10; «почему» —
    новые слова (`done_when` 2). Кандидат в правило для `work`: **СТОП обязан начинаться с
    перечитывания собственной приёмки построчно, и в возврате называть, какие строки уже покрывают
    заданный вопрос.** Направление со своей стороны уже дважды за день ловило это ответом, но
    дешевле ловить на стороне исполнителя. Готово к внесению как FRICTION, если случай повторится
    четвёртый раз.
  - **Столкновение поверхностей оказалось папочным, а не файловым, и стоило бы слоту простоя.**
    Наряды называют границы папками (`Network/**`), а работа идёт файлами. Дешёвый приём:
    при СТОПе о столкновении читать `git status` соседнего дерева, а не рассуждать о папке.

decisions_needed: |
  Ни одного. Владельцу тут нечего выбирать: слова разрешены его же строкой приёмки, потолок и
  выдача файла — операционные решения наряда.

play_check: |
  1. OPEN — возвращённый СТОП по идущей ноге; приёмка перечитана построчно ДО ответа.
  2. WORK — замер исполнителя перевыверен первой рукой на `4e9177af`; рабочее дерево соседней ноги
     прочитано напрямую; отчёт читался как улика.
  3. (owner) — не требуется: строка приёмки 2 уже несёт разрешение под условием, условие выполнено.
  4. CLOSE — RESULT, применение, коммит; нога «почему» продолжается.

log: |
  СТОП полосы «почему» разобран: слова уже разрешены строкой приёмки 2, потолок пяти правок
  заменён правилом «одно слово на один различимый отказ» с задником, а ожидание ноги зрения снято
  замером её дерева — она курьера не трогает; третий за день случай, когда нога спросила дом о
  том, что стояло в её собственной приёмке

next: |
  Ответ уходит в WIN-U3. Идут три ноги: зрение U1, «почему» U3, безопасное закрытие U4 —
  последняя внутри закрытия, замок интеграции за ней. WIN-U2 освободился в 11:30.
