# RESULT — s-work-g-5a7c-arrival-hook-surface-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-arrival-hook-1, полоса `утро`
track: утро
date: 2026-08-31
outcome_kind: boundary amendment — поверхность наряда расширена на два файла-носителя после проверки байтов; порог пяти правок не превышен

owner_approved: |
  Владельца не спрашивали, и это правильно: расширение внутри объявленного порога — инженерное
  решение направления, а не выбор, который он увидит в игре. Владельцу оно передано отдельно,
  вместе со следствием для порядка полос.

  Ни одна строка `done_when` не изменена: наряд продолжается тот же.

outcome: |
  **ИСПОЛНИТЕЛЬ WIN-U2 ОСТАНОВИЛСЯ РОВНО ТАМ, ГДЕ НАРЯД ВЕЛЕЛ, И ЭТО ПЕРВЫЙ ЗА СУТКИ СЛУЧАЙ, КОГДА
  СТОП СРАБОТАЛ КАК ЗАДУМАНО.** Он назвал три возможных обхода вслух — sentinel-эффект,
  кодирование в имени, глобальная таблица — и **не построил ни одного**, а пришёл домой с точным
  запросом.

  **РАЗРЕШЕНИЕ ВЫДАНО ПОСЛЕ СОБСТВЕННОЙ ПРОВЕРКИ, А НЕ ПО ЕГО РАССКАЗУ.** Сегодня рассуждение
  исполнителя дважды опровергалось данными (обоснование тест-растяжки про будильник; «восемь из
  четырнадцати»), поэтому все пять его утверждений перечитаны в байтах на `c7e926cf`. **Все пять
  сошлись.** Ключевое: `HouseholderActiveResponse` конструктором копирует семь полей proposal, и
  `Effects` среди них нет — после перехода `Householder.cs:391` эффекты действительно потеряны, и
  донести их до `DeedOfEnding` внутри объявленной поверхности нечем.

  **НАЙДЕНО СВЕРХ ЕГО РАЗБОРА — МИНА, КОТОРАЯ ВЗОРВАЛАСЬ БЫ МОЛЧА.**
  `HouseholderActiveResponse.WithProgress` (`:54-68`) пересобирает отклик через синтетический
  proposal и передаёт **`null` вместо эффектов**. Значит поле, добавленное в конструктор, будет
  обнуляться на каждом тике прогресса, **не уронив ни компиляцию, ни один тест**. Это ровно тот
  класс, о котором предупреждает комментарий в `HouseholderStanding`: пропущенная строка
  «компилируется, ничего не бросает, ни один тест не краснеет и молча стирает поле на каждом тике».
  Требование протащить поле через `WithProgress` и доказать это тестом добавлено в возврат.

  **ЦЕНА РАЗРЕШЕНИЯ НАЗВАНА, А НЕ ЗАМОЛЧАНА.** `HouseholderDecision.cs` производит пять присваиваний
  `HouseholderDecisionCandidateOutcome` (строки 651, 661, 662, 665, 761) — это поверхность задачи
  `t-refusal-reason-1`. **Полоса `почему` теперь ждёт, пока эта нога доедет в `main`.** Полоса ещё
  не стартовала, объявленный порядок не ломается.

evidence: |
  Перечитано направлением на продуктовом `main` `c7e926cf`:

  - `Householder.cs:310-317` — `HouseholderEffect.ApplyAll(proposal.Effects, standingBefore)` под
    единственным сторожем `isReaction`;
  - `Householder.cs:391` — `activeResponse = new HouseholderActiveResponse(proposal);`;
  - `HouseholderActiveResponse.cs:13-25` — конструктор присваивает `IsPresent`, `Reaction`,
    `Target`, `FactOccurrenceId`, `ConsumedFactOccurrences`, `Phase`, `HoldRemainingSeconds`;
    поля `Effects` нет;
  - `HouseholderActiveResponse.cs:54-68` — `WithProgress` собирает
    `new HouseholderDecisionProposal(IsPresent, Reaction, Target, FactOccurrenceId, 0L, null,
    ConsumedFactOccurrences)`; седьмой аргумент — `null`;
  - `grep -rn "\.Effects\b" Probe/Householder/` → ровно два файла: `Householder.cs:316` и
    `HouseholderDecision.cs:430-462`;
  - `grep -n "HouseholderDecisionCandidateOutcome\." HouseholderDecision.cs` → 5 попаданий
    (651, 661, 662, 665, 761).

  **НЕПЕРЕСЕЧЕНИЕ С ТРЕМЯ ИДУЩИМИ НОГАМИ ПОСЛЕ РАСШИРЕНИЯ:**
  U1 прибор — `Network/**`, `Editor/**`; U3 закрепление — `HouseholderMarks.cs`,
  `HouseholderGauges.cs`; U4 гейт — `tools/**`, `HouseholderDecisionPipelineTests.cs`.
  Ни один из двух новых файлов там не встречается.

  **СЧЁТ ПРАВОК:** `Householder.cs`, `HouseholderDecision.cs`, `HouseholderActiveResponse.cs`,
  `HouseholderProfileLoader.cs` — четыре продуктовых файла при пороге пять.

state_changes: |
  ДОПИСАНО (1 документ):
  - `work/2026-08-31-call-arrival-hook-1.md` — раздел «ДОПОЛНЕНИЕ 2026-08-31: ПОВЕРХНОСТЬ
    РАСШИРЕНА НА ДВА ФАЙЛА-НОСИТЕЛЯ»: что перепроверено, что разрешено, мина `WithProgress`, цена
    для полосы `почему`, три добавленных пункта возврата. Тело наряда выше не тронуто ни строкой.

  ДОПИСАНО В ЖУРНАЛ (1):
  - `c-exec-g-5a7c-arrival-hook-1-001` — разрешение, счёт файлов и следствие для полосы `почему`.

  НЕ ИЗМЕНЕНО: ни одна строка `done_when` задачи, ни порог пяти правок, ни границы трёх соседних ног.

captures: |
  - Разрешение положено в ФАЙЛ, а не осталось в чате, намеренно. Возврат исполнителя будет
    ссылаться на «направление разрешило», и сегодня уже дважды такая ссылка оказывалась
    непроверяемой. Теперь она резолвится: документ наряда плюс строка журнала карточки.

decisions_needed: |
  Ни одного. Расширение внутри объявленного порога — инженерное решение направления.

play_check: |
  1. OPEN — вход: СТОП исполнителя WIN-U2 с точным запросом на расширение поверхности.
  2. WORK — все пять его утверждений перечитаны в байтах; найдена шестая, им не названная
     (`WithProgress` обнуляет); посчитана цена для соседней полосы; проверено непересечение с
     тремя идущими ногами.
  3. (owner) — не требуется: владелец этого не увидит в игре. Передано ему отдельно как факт.
  4. CLOSE — RESULT финальным сообщением, применение и коммит после него.

log: |
  поверхность наряда хука прибытия расширена на два файла-носителя после проверки байтов —
  четыре правки при пороге пять; найдена мина, которую исполнитель не назвал: WithProgress
  обнуляет партию эффектов молча

next: |
  Домой. WIN-U2 продолжает наряд. Возврат обязан дополнительно доказать тестом, что `WithProgress`
  не теряет партию.

  Полоса `почему` ждёт эту ногу: обеим нужен `HouseholderDecision.cs`.
