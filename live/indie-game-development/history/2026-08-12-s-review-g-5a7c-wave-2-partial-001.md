# RESULT — s-review-g-5a7c-wave-2-partial-001

direction: indie-game-development
play: review
node/task: g-5a7c / review второй волны
date: 2026-08-12
verdict: partial

owner_approved: |
  Владелец после разъяснения фактического остатка сказал:
  «Тогда можно закрывать. Вот эти комбинации мы будем уже на каких-то реальных
  кейсах делать. То есть у нас там есть демо-версия, ну, планы на демо-версию,
  и мы в соответствии с ней, наверное, будем смотреть. То есть да, это уже
  в следующую волну переносим, вот, ну какие комбинации, хендлеры там
  дополнительные и так далее. То есть поэтому текущую закрываем».

  Сила этого слова ровно такая:
  - текущую вторую волну и её ставку закрыть;
  - конкретные комбинации и дополнительные обработчики не считать сделанными;
  - вернуть их только новой волной из реальных кейсов плана демо;
  - сам многоволновой узел не закрывать.

outcome: |
  Вторая волна `g-5a7c` закрывается вердиктом `partial`.

  Проверенная ценность, пережившая опровержение:
  - груз слушается рук, провисает и поворачивается физически, а число рук
    действительно меняет возможность подъёма;
  - хозяин принимает типизированные ситуации мира, хранит несколько фактов,
    выбирает реакции через расширяемый конвейер и читает правила из данных;
  - опубликован единый Household Lab, где владелец руками подаёт события и видит
    цепь «случилось → услышал → запомнил → обработчик → действие».

  `met` недоступен: задача `t-react-3` не выполнена целиком — конкретная комбинация
  не стоит в игровом профиле, подставленный новый источник «увидел мышь» не
  доказан отдельным тестом, владелец не производил комбинацию в собранной игре,
  а откат этой задачи не существовал. Владелец не требует это доделывать
  абстрактно и переносит содержание в новую, основанную на демо-кейсах волну.

  Это не `killed`: пороги смерти не остановили волну. Это не `obsolete`: груз,
  хозяин и причинная реакция остаются частью выбранной игры.

evidence: |
  Свежая binding-G5 проверка выполнена в этом отдельном review-чате, не в чате
  продуктовой работы.

  Продуктовый опубликованный результат:
  - `origin/main` = `origin/dev` = `origin/slot/win-u2` =
    `f3c40c4010ee2f0f700cdf1bfbcea14a301d488c`, повторно прочитано через
    `git ls-remote` 2026-08-12;
  - `docs/results/c-exec-g-5a7c-observe-1-001.md`;
  - здесь независимо прогнано:
    `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release`
    → 214 passed, 0 failed, 0 skipped;
  - `tools/hygiene.ps1` → green;
  - `tools/check.ps1 -Deliver` остаётся RED на семи старых отчётах — это уже
    зарегистрированный процессный дефект
    `i-deliver-gate-red-when-main-catches-dev-001`, а не опровержение поведения
    этой поставки.

  `t-observe-1` пережила проверку:
  1. Цепь видна в Household Lab и сохранена структурированным снимком; владелец
     проверил стенд руками, а опубликованный отчёт фиксирует его принятие.
  2. Ручные маркеры порождают обычную `WorldSituation`; живые пороги и путь
     preset→world закреплены тестом с нестандартными `3.5 / 7.25 / 11.75`.
  3. Lab использует настоящий `NetworkWalkerCourier → CarryWorld →
     NetworkHouseholder`, тот же профиль и правила; scene-fixture тест сравнивает
     состав с игровой сценой.
  4. Маркер и окно editor-only, Household Lab отсутствует в player build settings.
  5. Новый вид события и новый показатель добавляются через названные точки
     расширения; сценарий и снимок уже существуют.
  6. Отчёт прямо отделяет лабораторию от доказательства поведения игры, а
     владелец не назвал неожиданные реакции техническим дефектом без fixture.
  7. Настоящий откат выполнен:
     `060e01f` → revert `7b9fed3` → reapply `d7d863b`;
     tree `7b9fed3` = tree базы `87d399f`
     (`43285b7b77115b04216a36500e5d008c058b2291`).

  `t-react-3` пережила лишь частично:
  - тестовый `CombinationProfileJson` и
    `FileBuildsTheExistingFactCombinationHandlerForm` доказывают, что
    комбинацию можно описать данными;
  - `IndependentHandlersComposeAndEitherCanBeDisabled` доказывает независимость
    выбора от порядка регистрации;
  - но опубликованный
    `Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`
    всё ещё несёт пустые `behaviorBranches` и `decisionHandlers`;
  - отдельного доказательства подставленного источника «увидел мышь» нет;
  - owner-eye прогон проверял одиночные события и активное прерывание, а не
    комбинацию;
  - отката `t-react-3` нет, потому что задача не запускалась.

  Deliverable reconciliation для `c-exec-g-5a7c-observe-1-001`: функциональные
  байты и итоговый отчёт опубликованы на всех трёх удалённых ветках в `f3c40c40`;
  статус отчёта и его `Next` устарели относительно опубликованных refs. Отдельный
  terminal selector readback в отчёте отсутствует и не выдаётся за существующий.

verdict_vs_forecast: |
  Прогноз был `no_basis` и таким остаётся: эмпирического reference class для
  вероятности демо нет.

  Планировалось 7 задач и 1 новый слой. После двух пересборок дорожки, настройки
  и возвращённого владельцем инструмента появились 11 карточек задач второй
  волны. Сюрприз не в отказе архитектуры: она работает. Сюрприз в том, что
  финальный критерий комбинаций оказался бессмысленно абстрактным без конкретной
  сцены демо.

  Довод против параллели подтвердился частично: общий файл сцены дорожки не
  столкнул, но цена умножилась в разговорах и пересборке критериев. Следующая
  волна не получает комбинации как долг сама по себе — только как следствие
  выбранного демо-кейса.

lens_harvest: |
  1. Октябрьское демо и выпуск: ядро стало сильнее, но публичная сборка и Steam
     этой волной не сдвинуты; следующий объём теперь прямо привязан к плану демо.
  2. Понятность и качество: управление грузом ощущается руками; причинность
     хозяина наблюдаема, но ещё не оценена в реальном игровом эпизоде.
  3. Реально одному: абстрактная задача реакций разрослась; владелец вернул
     правильный разрез — сначала конкретный кейс, затем только нужные правила.
  4. Канон: нового канона review не создаёт; границу следующей волны дал владелец.
  5. Коммерческое знание: не прибавилось; внешнего игрока и публичного демо нет.
  6. Надёжность работы: Household Lab остаётся повторно используемым инструментом;
     бесконечная настройка без ожидаемого поведения остановлена.
  7. Простота: один typed-situation слой, данные реакций и один lab сохранены;
     ненужные сейчас комбинации и дополнительные обработчики вырезаны.

tree_diff: |
  Точный owner-approved diff:
  - ставка второй волны закрывается `partial`;
  - `g-5a7c` остаётся тем же многоволновым узлом: `active → shaped`, текст цели и
    двенадцати критериев не меняется;
  - `t-observe-1` закрывается `done`;
  - `t-react-3` закрывается `dropped`, а не `done`;
  - обе дорожки и оставшийся наряд распускаются;
  - следующая shape-нога зарегистрирована, но не запущена; её вход — реальные
    кейсы плана демо и точные слова владельца выше.

add_back: |
  Возвращённый ранее вырез «инструмент наблюдения» удержался: его вернул сам
  владелец, и он стал одним расширяемым editor-only lab.

  Прежняя граница «поведение не меняется» была расширена owner feedback:
  `activeResponseRules` теперь задаёт приоритет прерывания из данных. Это названо
  в опубликованном отчёте, а не спрятано.

  Настоящий пропущенный вырез — `t-react-3`: комбинацию пытались принимать до
  появления игрового случая, который объясняет, зачем она нужна. Владелец теперь
  вырезал её из этой волны. Зрение, дом как жильё, доставка, прокачка и прочие
  прежние вырезы не вернулись.

knowledge: |
  Новых записей нет. Решение специфично для следующей волны и сохраняется в
  истории, карточке идеи и явном контексте следующего CALL; обобщать его в
  универсальное правило без новых наблюдений не нужно.

forecast: |
  `direction_forecast.status: no_basis` сохраняется; `as_of` обновляется на
  2026-08-12. Положительный сигнал — опубликованные груз, архитектура хозяина и
  принятый lab. Отрицательный — всё ещё нет конкретного репрезентативного
  демо-прохождения и внешнего плейтеста. Следующее обновление — когда план демо
  даст точный игровой fixture, случится внешний прогон либо наступит уставный
  пересмотр 2026-08-15.

state_changes: |
  1. `bet-g-5a7c-wave-2`: закрыть со статусом `partial`.
  2. `g-5a7c.status`: `active → shaped`; остальные поля сохранить.
  3. `NOW.md`: `bet: null`; удалить `track_wip_limit`.
  4. `t-observe-1`: закрыть со статусом `done`.
  5. `c-exec-g-5a7c-observe-1-001`: закрыть по опубликованному `f3c40c40`,
     owner-eye и текущей binding-G5 сверке.
  6. `t-react-3`: закрыть со статусом `dropped` по словам владельца; никакой
     выполненности комбинаций не записывать.
  7. `cargo-hands` и `householder`: закрыть — дорожки распущены review.
  8. `q-house-hears-20-percent-earlier-001`: закрыть без решения о новых
     порогах; эта волна их не крутит, а будущая настройка требует точного fixture.
  9. `idea-manual-cargo-control-001`: закрыть `taken`, поглощена этой волной.
  10. `idea-complex-noise-recognition-001`: оставить открытой и добавить текущие
      дословные слова о реальных кейсах демо.
  11. Закрыть как разрешённые этой волной:
      `i-telekinesis-carry-distance-is-fixed-001`,
      `i-lift-step-accepted-but-small-001`,
      `i-world-situations-need-extensible-reaction-architecture-001`,
      `i-published-to-main-before-any-direction-close-001`.
      Остальные issues сохранить.
  12. Обновить `direction_forecast` без численной вероятности.
  13. Создать `work/c-shape-g-5a7c-wave-3-001-call.md` и ready-карточку
      `c-shape-g-5a7c-wave-3-001`: следующая волна режется из конкретных кейсов
      плана демо; `t-react-3` автоматически не возрождается.
  14. Сохранить полный RESULT в
      `history/2026-08-12-s-review-g-5a7c-wave-2-partial-001.md` и одной
      журнальной строкой отметить все затронутые карточки.

captures:
  - |
    `tools/check.ps1 -Deliver` на опубликованном `f3c40c40` красный из-за семи
    исторических отчётов; класс уже зарегистрирован
    `i-deliver-gate-red-when-main-catches-dev-001`, новой карточки не создавать.
  - |
    В продуктовом отчёте после публикации остались устаревшие Status/Next и нет
    terminal selector readback; это подтверждает существующую
    `i-executor-return-omits-named-return-items-001`, но не превращается в
    продуктовую задачу этой волны.
  - |
    Открытые `i-dragged-cargo-rustles-every-tick-001`,
    `i-noise-bands-invisible-and-force-mis-scaled-001` и
    `i-host-does-not-enforce-the-settings-pitch-limit-001` не чинить абстрактно;
    следующий shape обязан сравнить их с конкретным демо-fixture.

decisions_needed: []

play_check:
  - 1 verify by refutation: done — опубликованный `f3c40c40` перемерен в свежем review-чате; `t-observe-1` выжила, четыре обязательства `t-react-3` не выжили, поэтому verdict `partial`.
  - 2 harvest per lens: done — все семь линз получили явную диспозицию.
  - 3 tree diff (owner): done — владелец сказал «текущую закрываем» и дословно перенёс комбинации/дополнительные обработчики в следующую волну по реальным кейсам демо; текст узла не меняется.
  - 4 add-back check: done — возвращённый lab удержался; преждевременная абстрактная комбинация названа пропущенным вырезом и снята.
  - 5 knowledge: done — новых записей нет; текущая история и идея достаточно узкие и имеют реального будущего читателя.
  - 6 forecast & next: done — `no_basis` сохранён; выбран следующий контур того же узла, но только от demo fixture.
  - 7 close: done — ставка, две задачи, один наряд и обе дорожки закрываются; создаётся один законный planning CALL без запуска.
  - G5: done — это отдельный свежий review-чат; продуктовый handback сам по себе закрытием не считался.
  - G7: done — игровые комбинации, новые обработчики и пороги не придуманы ногой.
  - G9: done — `owner_approved` содержит точные слова для `active → shaped`; содержимое node-карточки не редактируется.

log: Вторая волна g-5a7c закрыта verdict partial: груз и наблюдаемая архитектура хозяина приняты; абстрактные комбинации сняты до конкретного кейса демо. Ставка, задачи и дорожки распущены; следующий shape подготовлен, но не запущен.

next: |
  CALL `c-shape-g-5a7c-wave-3-001` зарегистрирован `ready`, но не запущен.
  Свежая shape-сессия начинает не с восстановления `t-react-3`, а с конкретного
  репрезентативного кейса из плана демо и только затем решает, какие комбинации,
  обработчики и настройки действительно входят.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-review-g-5a7c-wave-2-partial-001.md
