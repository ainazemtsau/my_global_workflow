# RESULT — s-work-g-5a7c-react-mem-checkpoint-001

play: work · возврат пришёл КАНДИДАТОМ, публикации нет; нога перемеряет его первой рукой сейчас,
чтобы закрывающая нога после публикации не делала это заново, и НЕ закрывает задачу
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-mem ·
call: c-exec-g-5a7c-react-mem-001
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `ae5d7a81`, дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` = `6599ae87e5435b709f4b382becb16a018a5ae695`
— это опубликованная голова `t-cargo-2`, а НЕ базис наряда `2f7f2e87`. Кандидат живёт только на
локальной `slot/win-u2` (голова `aa7aedc1`, продуктовый tip `598e6368`); `origin/slot/win-u2`
остался на `e4df0459`.

## outcome

**Задача НЕ закрывается: возврат сам заявляет `NOT DELIVERED on dev`,** а буква `evaluator` этой
волны закрывает задачу ПОСЛЕ публикации. Нога перемерила кандидата целиком и записала результат,
чтобы закрывающая нога не тратила круг заново.

**Всё, что можно проверить байтами, проверено и СОШЛОСЬ. Ни одного расхождения с возвратом.**

## evidence — прогнано и прочитано ЭТОЙ ногой

**Прогоны сделаны здесь, на выгруженных деревьях, слот не тронут:**

```text
кандидат 598e6368: Пройдено 179, не пройдено 0, пропущено 0, всего 179
голова дорожки 6599ae87: Пройдено 173, не пройдено 0, пропущено 0, всего 173
```

Разница ровно `+6`, как и заявлено (три ограничения пересмотрены на месте, шесть проверок новых).

**ГЛАВНЫЙ ГЕЙТ ВЗЯТ, и это лучшее в возврате.** Пять мест сборки `HouseholderLocalMemory` внутри
`Step` — **ноль из пяти тронуты**: `git diff 6599ae87..598e6368` по `Householder.cs` не содержит ни
одной строки `new HouseholderLocalMemory(`. То есть новое поле памяти действительно обошлось без
семи правок, ради чего задача и заводилась.

**Восемь полей факта прочитаны на кандидате поимённо** (`Householder.cs:232-248`): `Name`,
`OccurrenceId`, `Position`, `NoticedAtSeconds`, `ExpiresAtSeconds`, `Loudness`, `IsHandled`,
`OccurrenceCountAtPlace`. Метаданные едут и выше по течению: `HouseholderEvent` получил `Position` и
`Loudness` (`:128-132`), то есть громкость больше не умирает в полосе.

**Накопление читаемо решателем:** `OccurrenceCountAtPlace` на самом факте плюс `TimesNoticedAt` на
снимке решения (`:756`) и на памяти (`:1442`).

**Политика ставится на существующий объект, как требовал наряд:** `DecisionPolicy` — свойство
`AuthoritativeHouseholder` (`:143-154`), по умолчанию singleton, `null` возвращает singleton.
**`CarryWorld.cs` не изменён вовсе**, обе публичные перегрузки `Advance` (`:97`, `:115`) целы.

**Границы целы — проверено пустым выводом.** `git diff --name-only 6599ae87..598e6368` по
`Core/Cargo/**`, `Network/**`, `Scenes/**`, `ProjectSettings/**`, `Assets/GasCoopGame/**`,
`Core/Multiplayer/CarryWorld.cs`, `StreamingAssets/**`, `Settings/**` — **пусто**. Отсюда следует
без отдельной проверки: пороги `60/150/400` целы, файл профиля цел, сцена цела.

**Каталог пяти реакций НЕ тронут** — `HouseholderReaction.cs` отсутствует в диффе, значит те же пять
реакций и те же числа.

**Дифф ровно пять путей:** три продуктовых (`AuthoritativeHouseholder.cs`, `Householder.cs`,
`WorldSituationNoise.cs`) и два разрешённых тестовых (`HouseholderAdditionalReactionTests.cs`,
`HouseholderInTheJudgesTickTests.cs`) — ровно те, где живут три названных поимённо теста. Прочие
тестовые файлы не тронуты.

**ОТКАТ ИСПОЛНЕН ПО-НАСТОЯЩЕМУ, и это ПЕРВЫЙ раз в дорожке.** Перемерено здесь:
`git rev-parse b035861f^{tree}` = `5734e0ba59694f35cebead61a9483a742b5b784e` = дерево головы дорожки
`6599ae87` знак в знак. Реаплай тоже сходится: `598e6368^{tree}` = `f594e86a^{tree}` =
`c9f6014ed80c6e5dc40ae8e92b2b189ffec126d1`. Два предыдущих наряда откат либо не приносили, либо
приносили выведенным.

## ДВА УТВЕРЖДЕНИЯ ВОЗВРАТА, КОТОРЫЕ ИЗ БАЙТОВ НЕ ПРОВЕРЯЮТСЯ

Названы, а не приняты и не отвергнуты.

1. **Базис уехал, а наряд велел СТОП.** Наряд стоял на `2f7f2e87` и прямым текстом говорил:
   «`origin/dev` сдвинулся — СТОП ДОМОЙ». Он сдвинулся: `t-cargo-2` опубликовалась в `6599ae87`.
   Возврат объясняет продолжение словами «after the owner's continue instruction». Такого указания в
   байтах нет. Работа при этом сделана НА НОВОЙ голове и с ней собрана, то есть по существу правее
   буквы наряда — но факт остаётся: единственная защита от гонки дорожек была снята не Направлением.
2. **Owner-eye, которого наряд не просил.** Возврат приводит слова владельца «Сработало так, как ты
   написал». Наряд прямо говорил, что Unity не нужен и поведение не меняется, — вердикт владельца
   этой задаче не требовался и в `done_when` не входит. Утверждение не несущее, но принимать чужой
   пересказ его слов за его слово нельзя.

## МАРШРУТ ЗАКРЫТИЯ ОПРЕДЕЛЁН ЗАРАНЕЕ

**Светлый путь доступен.** Все три строки `done_when` перемерены этой ногой первой рукой по
закоммиченным байтам; ни одна не требует его глаза, его ощущения или чужого прогона. Свежий
биндинговый чат не нужен. Закрывающей ноге останется перемерить ТОЛЬКО опубликованное состояние и
сверить его с проверенным здесь `598e6368`.

## state_changes

1. `c-exec-g-5a7c-react-mem-001`: `status: ready` → `waiting` — наряд исполнен, ждёт публикации в
   `dev`/`main`. Карточка НЕ закрывается.
2. `t-react-mem` остаётся `open`: публикации нет, буква волны закрывает после неё.
3. Заведена карточка вопроса `q-two-return-claims-rest-on-unverified-owner-words-001` — про оба
   утверждения выше; всплывёт перед владельцем, когда откроется нога на этой дорожке.
4. NOW, ставка, узел и дорожка `cargo-hands` не тронуты.

## captures

- **`origin/slot/win-u2` остался на `e4df0459`**, локальная ветка ушла вперёд. Третий раз подряд по
  этой дорожке; селектор читает локальное, выдача не ломается.
- Возврат называет следующим шагом отдельную Control-задачу в `WIN-CTRL` для handback/публикации.
  Это продуктовый процесс, Направление в него не вмешивается.
- **Базис наряда протух второй раз подряд** — оба раза из-за публикации параллельной дорожки. Наряды
  этой волны привязывают откат к голове дорожки, а базис по-прежнему пишут константой. Место для
  ревью волны.

## decisions_needed

Нет вопроса, требующего его немедленного ответа. Два утверждения возврата ждут его слова карточкой и
не блокируют публикацию.

## play_check

- 1 recite: done — три строки `done_when` наряда и шесть строк карточки сведены
- 2 owner inputs (owner): done — от него ничего не требуется; он сам сказал «проверь result и сделай
  что пока можешь»
- 3 do the work: done — кандидат перемерен целиком, два прогона сделаны этой ногой
- 4 self-check: done — сошлось всё, что проверяется байтами; непроверяемое названо непроверяемым
- 5 close: **checkpoint** — задача не закрывается, публикации нет

## Гейты

- **G5.** Ничего не объявлено `done`. Маршрут закрытия назван заранее: светлый, потому что все
  строки перемерены первой рукой.
- **G7.** Ничего продуктового этой ногой не решено.
- **G9.** Карточки задачи и ставки не редактировались.
- **evaluator, клауза 1.** Соблюдена: закрытие только ПОСЛЕ публикации.

## log

возврат по носителю пришёл кандидатом без публикации, поэтому задача НЕ закрыта, но кандидат перемерен целиком этой ногой: 179/179 прогнаны здесь на выгруженном дереве кандидата против 173/173 на голове дорожки — разница ровно шесть, как заявлено; ГЛАВНЫЙ ГЕЙТ взят по-настоящему — ноль из пяти мест сборки памяти тронуты, дифф не содержит ни одной такой строки, то есть новое поле обошлось без семи правок, ради чего задача и заводилась; восемь полей факта прочитаны поимённо, громкость больше не умирает в полосе, накопление читаемо решателем через снимок, политика ставится свойством на существующий объект и CarryWorld не изменён вовсе; дифф ровно пять путей, границы пусты по всем запрещённым поверхностям, каталог пяти реакций не тронут; ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК — впервые в этой дорожке; названы два утверждения, которые из байтов не проверяются: базис уехал на голову cargo и наряд велел СТОП, а продолжение объяснено «указанием владельца», которого в байтах нет, и приведён owner-eye, которого наряд не просил; маршрут закрытия определён заранее — светлый, свежий чат не нужен

## next

1. **Публикация.** После неё — закрывающая нога Направления: перемерить опубликованное состояние и
   сверить с проверенным здесь `598e6368`, закрыть светлым путём.
2. `t-react-2` (точка расширения) пишется после закрытия носителя, с уже вписанным требованием
   «вес строки вычисляемый, а не константа».
3. Два утверждения возврата ждут его слова карточкой, без вопроса.
