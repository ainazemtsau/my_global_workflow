# RESULT — s-repair-g-5a7c-lab-policy-stop-001

play: repair · владелец указал на документ, чтение документа нашло его же подписанную политику,
которой выданный час назад наряд противоречит; наряд остановлен, решение вынесено ему
direction: indie-game-development · node: g-5a7c · track: householder · task: t-observe-1 ·
call: c-exec-g-5a7c-observe-1-001
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `4d45ccff`, дерево чисто. Продукт `origin/dev` =
`origin/main` = `87d399f6`, не двигался. Наряд `c-exec-g-5a7c-observe-1-001` выпущен, но исполнителю
НЕ отдан — расписки нет.

## outcome

**СТОП по его собственному правилу**: «если увидишь проблему, то стопайся, пиши мне сразу».

Он сказал «посмотри документ в проекте concept lab». Такого документа нет ни в OS-репозитории, ни в
продуктовом — все деревья чисты, новых файлов не появлялось, поиск Trilium отвечает ошибкой
`-32602`. Поиск по СОДЕРЖИМОМУ нашёл настоящий предмет: `docs/adr/ADR-0010-test-sandbox-scenario-as-data.md`
— «the owner needs a data-driven gas **LAB**». А рядом с ним — `docs/legacy-lab-surface-policy.md`,
которая наряд и блокирует.

## 1. Противоречие — двумя строками

`docs/legacy-lab-surface-policy.md`, дословно: «Status: **current owner authority, 2026-07-20**…
The product will have exactly **one future permanent Integration Lab**… it remains **one
scene/product route**… **Its construction requires a separately approved plan and green replacement
evidence.**»

Наряд `c-exec-g-5a7c-observe-1-001` строкой 2 требует НОВУЮ отдельную сцену-полигон. По букве
политики это второй постоянный лабораторный вход, а единственный разрешённый строится отдельно
одобренным планом, а не нарядом исполнителю.

## 2. Реконструкция — что перемерено

**ПРЕЦЕДЕНТ СИЛЬНЕЕ ЛЮБОГО ДОВОДА, И ОН ТОЧНО ПРО НАШ СЛУЧАЙ.** Манифест удаления
`docs/measurements/c-exec-program-v2-legacy-lab-purge-001-deletion-manifest.md` перечисляет поимённо
снесённые `Assets/GasCoopGame/ReactionSandboxScene.unity` и
`Assets/GasCoopGame/Adapters/GasView/ReactionSandbox.cs` — коммит `72c7c8c6` «BUILD purge unused
legacy lab surfaces», 2026-07-20. **Песочница реакций этого же класса уже прожила цикл «построили —
не пользовались — снесли».**

**Второй мёртвый стенд лежит прямо сейчас:** `Assets/TunnelCrew/Scenes/HostWalksHisDay.unity` — вне
списка сцен сборки. Наряд его уже называл как доказательство риска; теперь известно, что это не
единичный случай, а второй.

**Линия газа фактически исчезла:** в `Assets/GasCoopGame/**` осталось ТРИ отслеживаемых файла;
`GasLabScene.unity` отсутствует; `Core/Sandbox`, `GasScenario`, `TickPacer` не находятся ни одним
`git ls-files`, хотя политика называет `Core/Sandbox` KEEP-поверхностью. Значит от газовой
лаборатории переиспользуется ЗАМЫСЕЛ, а не код.

**ЧТО РАСКОПКА ДАЛА СВЕРХ БЛОКЕРА, и это ценно.** `ADR-0010` (Accepted, owner-approved) описывает
приём, которого моему наряду не хватало:
- **сценарий как ДАННЫЕ** в engine-free ядре, адресуемый читаемыми ключами, а не индексами;
- **чистый `TickPacer`** — пауза, шаг, темп тиков, без часов, headless-проверяемый;
- **структурированный JSON-снимок, который читает чат.**

Последнее — прямой ответ на его же «мне чат будет расписывать, какое там поведение». И повторяемый
сценарий сильнее ручного тыканья мышью: его можно прогнать дважды и сравнить, а клики — нельзя.
**Мой наряд этого не содержал вовсе** — он покупал только окно и мышь.

**Что НЕ заблокировано:** окно наблюдения и три правки ядра. Окно новой сцены не создаёт, в сборку
не едет, работает поверх настоящей `IntegratedHouse`, ни одного валидатора не трогает. У судей
прогона оно и получило высшую оценку.

## 3. Исправленное состояние

1. `c-exec-g-5a7c-observe-1-001`: `status: ready` → **`blocked`**. Исполнителю не отдан.
2. Заведена улика `i-second-lab-surface-blocked-by-owner-policy-001` (`level: objective`,
   `route: review`) — политика, прецедент снесённой песочницы реакций, что не заблокировано, и
   находка про `ADR-0010`.
3. Текст наряда НЕ переписан: сузить его до окна — значит принять решение за владельца, а он этого
   не поручал и прямо велел останавливаться.
4. Карточка `t-observe-1` не тронута. NOW, ставка, узел и дорожка `cargo-hands` не тронуты.

## decisions_needed

**Одно слово, три варианта, ни один не выбирается ногой.** Его живое слово старше подписанной
политики по порядку авторитета, но политика — его же, и молча примирить их нельзя.

1. **Только ОКНО сейчас, полигон не строим.** Политика не тронута, работа идёт сегодня. Он получает
   «почему хозяин так сделал», но не получает «тыкнуть мышью сверху».
2. **Полигон становится тем самым единственным Integration Lab** — отдельно одобренным планом,
   служит и газу, и грузу, и хозяину. Дороже и не сегодня.
3. **Он снимает политику своим словом** — она его. Тогда наряд идёт как написан, а политику чинит
   отдельная нога, а не молчание.

**Рекомендация: 1 сейчас, 2 отдельным планом.** Основание измерено, а не вкусовое: песочница реакций
этого класса уже снесена как неиспользуемая, а окно такого риска не несёт вовсе — оно работает
поверх настоящей сцены и не может протухнуть отдельно от неё.

**Плюс к любому варианту:** приём из `ADR-0010` — сценарий данными, `TickPacer`, JSON-снимок для
чата — стоит вписать в задачу, какой бы вариант он ни выбрал.

## captures

- **Документа «concept lab» не существует ни под каким именем** — искал по именам файлов и по
  содержимому в обоих репозиториях; Trilium отвечает ошибкой `-32602`. Предмет опознан по смыслу.
- **Политика противоречит сама себе в одном месте:** называет `Core/Sandbox` KEEP-поверхностью, а
  в репозитории его нет. Для нашей задачи это не критично, но при следующем чтении политики это
  надо знать.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.

## play_check

- 1 назвать противоречие: done — политика против строки 2 наряда, двумя строками
- 2 реконструкция: done — манифест удаления, состав линии газа, оба мёртвых стенда прочитаны
- 3 предложить исправленное состояние: done — наряд заблокирован, улика заведена, текст наряда не
  тронут
- 4 подтверждение (владелец): **НЕ исполнено намеренно** — он велел останавливаться и писать; три
  варианта вынесены ему
- 5 friction: не требуется — дефект не в OS

## Гейты

- **G7.** Ни одно решение за него не принято: сузить наряд до окна — его выбор, не ноги.
- **G9.** Подписанное не редактировалось.
- **evaluator, клауза 1.** Ничего не публикуется и не закрывается.

## log

СТОП по его же правилу: он указал на документ, а чтение документа нашло его собственную подписанную политику 2026-07-20 «продукт получит РОВНО ОДНУ постоянную Integration Lab, одна сцена, и её постройка требует отдельно одобренного плана» — которой строка 2 выданного часом ранее наряда противоречит, потому что требует новую отдельную сцену-полигон; наряд переведён в blocked и исполнителю не отдан; прецедент сильнее любого довода и он точно про наш случай — песочница РЕАКЦИЙ этого же класса уже была построена и снесена манифестом удаления как неиспользуемая, а второй мёртвый стенд HostWalksHisDay лежит прямо сейчас; от линии газа осталось три файла, Core/Sandbox и TickPacer не существуют, хотя политика называет их KEEP, значит переиспользуется замысел, а не код; и раскопка дала сверх блокера то, чего моему наряду не хватало вовсе — ADR-0010 описывает сценарий как ДАННЫЕ, чистый TickPacer с паузой и шагом, и структурированный JSON-снимок, который читает чат, то есть прямой ответ на его же «мне чат будет расписывать, какое там поведение», и повторяемый сценарий сильнее ручного тыканья мышью; окно наблюдения при этом не заблокировано ничем; три варианта вынесены ему, ни один не выбран ногой

## next

1. **Его слово по трём вариантам.** До него наряд не выдаётся.
2. Что бы он ни выбрал — вписать в задачу приём `ADR-0010`: сценарий данными, `TickPacer`,
   JSON-снимок для чата.
3. `t-react-3` (order 11) ждёт инструмента.
