# RESULT — s-repair-g-5a7c-three-closes-001

direction: indie-game-development
play: repair
node/task: g-5a7c / bet-g-5a7c-wave-9, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: repair — три опубликованные ноги перевыверены по байтам; две закрыты, третья закрывать НЕЛЬЗЯ, и это поймал её собственный гейт

owner_approved: |
  Его слово на ход: **«ok делай»** — на показанный список из трёх шагов, где первым стояло
  «закрыть три карточки по факту, перевыверив done_when из байтов».

  Ни одна строка `done_when` не изменена. Единственное добавление — девятая строка приёмки в
  `t-close-safely-1`, и это не расширение объёма: тот же предмет, тот же файл, найденный при этой
  же проверке дефект того же закрывальщика.

outcome: |
  **ЗАКРЫВАТЬ ПО ОТЧЁТАМ БЫЛО БЫ ОШИБКОЙ, И ЭТО ДОКАЗАНО НА ТРЕТЬЕЙ НОГЕ.**

  **`t-arrival-hook-1` — ЗАКРЫТА ЧИСТО.** Все семь строк перевыведены из байтов, ни одна не
  натянута. Слово `effectsAt` — настоящее слово ДАННЫХ: читается загрузчиком вне `switch` по типу
  обработчика, ни одного захардкоженного имени. Умолчание защищено пятью независимыми способами,
  включая нулевой enum и НОЛЬ тронутых существующих тестовых файлов. Мина, о которой направление
  предупреждало, снята: `WithProgress` протаскивает поле, на это есть тест, и исполнитель сам
  починил тот же класс потери в `WithTrackedTarget`, о чём его не просили. Правок четыре при
  пороге пять.

  **`t-instrument-1` — ЗАКРЫТА С ТРЕМЯ ОГОВОРКАМИ.** Строки 1, 2, 3, 5 держатся кодом:
  ключ пробы стал парой `(вид, номер)`, каждая из 14 вещей пишет свою; отчёт печатает все четыре
  запрошенных куска СЛОВАМИ и живёт вне `#if UNITY_EDITOR`, то есть работает в `.exe`; счётчик
  «сколько из 14» разобран построчно и считает **тем же предикатом, которым решают правила**, не
  подмешивает `ThingHome` и не наследует протухший вердикт.
  Оговорки: сторож выполнен ИНАЧЕ — ловит «выбрал идти и не сдвинулся 60 тиков», а не «стоит с
  открытым делом»; правок ШЕСТЬ при пороге пять со ссылкой на решение владельца, из байтов не
  проверяемой; у новой сигнатуры `CanSee` ноль тестов.

  **`t-gate-headless-1` — ЗАКРЫВАТЬ НЕЛЬЗЯ, И РЕШАЕТ ОДНА СТРОКА.** Приёмка 2 требовала доказать
  **посевом мины**, что красный headless останавливает публикацию, и прямо добавляла «без этого
  пункт 1 недоказан». Механизм в коде есть, посев и возврат — оба предки `main`, в `main` сейчас
  `2L`. **Но улики КРАСНОГО нет ни одной:** обе попытки закрытия умерли на ЧУЖОМ красном EditMode
  до headless — 408 и 351 секунда Unity впустую.

  **И это признаёт сам отчёт исполнителя, закоммиченный в `main`:** «Required red-headless evidence
  is BLOCKED», работа перенесена в `## next`. Скептик опровергнуть не смог.

  **САМОЕ НЕПРИЯТНОЕ НАЙДЕНО ПОПУТНО: ЗАКРЫВАЛЬЩИК ПОСТАВИЛ `DELIVERED` ПОВЕРХ ОТЧЁТА, КОТОРЫЙ САМ
  ГОВОРИТ «BLOCKED».** Единственная содержательная правка отчёта при публикации — `Status: NOT
  INTEGRATED` → `DELIVERED on dev`. Гейт пометил доставленным то, что автор объявил недоделанным.
  Это уже не про одну ногу: так любая незавершённая работа получает штамп готовности.

  **ЧТО ИЗМЕНИЛОСЬ И ПОЧЕМУ ДОРАБОТКА ВОЗМОЖНА СЕЙЧАС.** Чужой красный — это стёртые настройки
  импорта модели хозяина; направление вернуло байты из истории git, дерево зелёное. Опыт, который
  дважды не удался, теперь проводится.

evidence: |
  Всё перевыверено на `main` = `dev` = `origin/main` = **`56a7b88a`**, отчёты исполнителей читались
  как улики и перепроверялись.

  - `effectsAt` — слово данных: читается загрузчиком для каждой строки вне `switch` по типу
    обработчика; поставочный JSON не изменён; ноль тронутых существующих тестовых файлов;
  - `WithProgress` протаскивает поле, тест на это есть; `WithTrackedTarget` починен тем же классом;
  - проба: `IHouseholderThingSightSource.cs:59-66` — `CanSee(observer, subject, subjectId, x, floorY, z)`;
    `HouseholderSightSource.cs:505-535` — `BeginProbe` сравнивает ПАРУ; сцена держит 14 записей
    `_cargoSpawns`;
  - счётчик: `NetworkWalkerCourier.cs:273-325`, инкремент ровно в одном месте `:316-317` после двух
    честных отсечек, итог строкой `:322`;
  - отчёт вне редактора: `TryReportHouseholder` на `:223`, вне блоков `#if UNITY_EDITOR`
    (65-67, 589-847, 1022-1024, 1206-1209); `TunnelCrew.DevConsole.asmdef` — `includePlatforms: []`;
  - сторож: `tests/.../HouseholderDoesNotFreezeTests.cs:63`, `LongestStallWhileWalking = 60`,
    окно 1800 тиков;
  - гейт: посев `bb32f3f8`, возврат `03cd6c87`, оба предки `main`; в `main` строка 206 несёт `2L`;
    улики красного headless в байтах нет; отчёт в `main` говорит «BLOCKED»;
  - штамп: `git show 56a7b88a -- docs/results/c-exec-g-5a7c-gate-headless-1-001.md` — единственная
    содержательная правка `NOT INTEGRATED` → `DELIVERED on dev`.

  **ЧЕГО ПОДТВЕРДИТЬ НЕЛЬЗЯ И ЭТО НАЗВАНО:** значение «0 из 14» — живой замер, из байтов не
  выводится; прогон самотеста закрывальщика — только со слов; шестое место правки прибора со
  ссылкой на решение владельца — из байтов не проверяется.

state_changes: |
  ЗАКРЫТО ПО ПЕРЕВЫВЕРЕННОМУ ФАКТУ (4):
  - `t-arrival-hook-1` → `done`, чисто;
  - `c-exec-g-5a7c-arrival-hook-1-001`;
  - `t-instrument-1` → `done` с тремя оговорками, названными в журнале;
  - `c-exec-g-5a7c-instrument-1-001`.

  ОСТАВЛЕНО ОТКРЫТЫМ (1) и это главное решение ноги:
  - `t-gate-headless-1` — приёмка 2 не выполнена. В журнал записано, что именно не доказано, чем
    это признаёт сам отчёт, и что чужой красный с тех пор устранён.
  - её наряд `-001` закрыт: работа опубликована, но задача не сделана.

  СОЗДАНО (1 наряд + 1 документ):
  - `c-exec-g-5a7c-gate-headless-1-002` — доработка, слот WIN-U2, база `56a7b88a`. Опыт, а не
    постройка: посеять мину В ДРУГОЙ файл, уронить закрытие, предъявить вывод, вернуть и проверить
    возврат ИЗ `dev`, а не из своей ветки.

  ДОПИСАНО (1 строка приёмки):
  - `t-close-safely-1` получила девятую строку: закрывальщик не штампует `DELIVERED` поверх отчёта,
    который сам говорит «BLOCKED». Это тот же предмет и тот же файл — не расширение объёма.

captures: |
  - Долг, не названный ногой хука прибытия в её же списке «замечено и не чинил»:
    `docs/householder-profile-format.md` не знает слова `effectsAt`. Автор утра будет читать
    документ, в котором нового слова нет.
  - Два непокрытых края того же механизма: реакция, чей отклик завершается на ТОМ ЖЕ тике, и
    отклик, который не завершится никогда — партия молча не применится, ничего не покраснеет.
  - Прибор отдал наверх девять находок, которые не чинил. Ошибка «Body prefab has no Animator»
    печаталась в консоли на КАЖДОМ старте и существовала до ноги — то есть машина кричала о
    сломанном риге задолго до того, как он уронил закрытие, и никто не слушал.

decisions_needed: |
  Ни одного. Доработка выдана, оговорки записаны, объём не менялся.

play_check: |
  1. Назвать противоречие — три ноги опубликованы, но закрывать их по отчётам нельзя: сегодня
     трижды рассуждение исполнителя опровергалось данными.
  2. Восстановить новейшим-первым — каждая строка `done_when` перевыведена из байтов `56a7b88a`;
     спорные отданы скептикам; отчёты читались как улики.
  3. Предложить исправленное состояние — две закрыты, одна оставлена открытой с названной причиной.
  4. Подтверждение (owner) — «ok делай» на показанный план; о том, что гейт скорее всего не
     закроется, владелец был предупреждён заранее.
  5. Дыра в OS → нет. Дыры продуктовые, обе ушли в `t-close-safely-1`.

log: |
  три ноги перевыверены по байтам: хук прибытия закрыт чисто, прибор с тремя оговорками, а гейт
  закрывать нельзя — он не доказал собственную мину, и это признаёт его же отчёт в main

next: |
  Домой. Идут: зрение WIN-U1, закрепление WIN-U3. Ждут выдачи: `t-close-safely-1` в WIN-U4 и
  доработка гейта `-002` в WIN-U2.

  Волна: закрыто три задачи из девяти, четвёртая открыта обратно.
