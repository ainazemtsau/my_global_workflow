# RESULT — s-work-g-5a7c-rooms-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-rooms-1
track: house-and-things
date: 2026-08-12
outcome_kind: checkpoint — наряд написан, держится blocked, исполнителю НЕ отдан

owner_approved: |
  «напиши наряд на t-rooms-1» — 2026-08-12.

  И тем же сообщением — **корректирующее указание про способ доставки**, которое важнее самого
  наряда: «Присылай нормальный кол, не команду. Нахуй ты мне команду присылаешь?.. Пришли текст
  call, либо кратко опиши, что именно должно измениться и как принять результат… Получается, для
  первого, для второго, для третьего в нормальном формате».

outcome: |
  **ГЛАВНОЕ СОДЕРЖАНИЕ ЭТОЙ НОГИ — ИСПРАВЛЕНИЕ СОБСТВЕННОГО ОТКАЗА ДОСТАВКИ, а не третий наряд.**

  Два наряда, выпущенные часом ранее (`c-exec-g-5a7c-places-1-001` и `c-exec-g-5a7c-body-1-001`),
  были «выданы» ссылкой на файл в репозитории направления плюс командой захвата слота. Исполнитель
  в WIN-U2 занял слот и остановился, потому что текста наряда у него нет: сессия исполнителя живёт
  в ПРОДУКТОВОМ репозитории и файл `live/indie-game-development/work/...` не читает вовсе. Его
  дословный ответ владельцу: «Пришли текст call, либо кратко опиши, что именно должно измениться и
  как принять результат. Слот пока удерживаю».

  Это прямое нарушение правила, которое в этом же направлении уже записано знанием и уже стоит
  клаузой 3 в блоке `evaluator` подписанной ставки:
  `knowledge/an-order-does-not-exist-until-it-is-delivered.md` — «готовая паста с ПОЛНЫМ текстом
  наряда, отданная исполнителю». Правило было написано после того, как первой волне это стоило
  полного круга работы и опубликованной в `main` пустышки
  (`i-call-text-never-reached-the-executor-001`). Нога знала правило, процитировала его в обоих
  нарядах — и сама же его не исполнила.

  **Исправлено доставкой всех трёх текстов владельцу целиком, готовыми к вставке.** Ни один из трёх
  нарядов не считается выданным до расписки исполнителя.

  **ТРЕТИЙ НАРЯД НАПИСАН И ДЕРЖИТСЯ `blocked`.** Причина измерена, а не процедурна: параллельный
  наряд по шву добавляет поле роли в `Assets/TunnelCrew/World/HousePlaceMarker.cs`, а этот наряд
  обязан этим полем ПОЛЬЗОВАТЬСЯ, расставляя роли по местам нового дома. Оба работают в `World/**` и
  оба идут к дому. Это ровно случай, описанный владельцем словом «одна задача блочит две полосы, то,
  ну, естественно, ждем». Слот назначен WIN-U2 — тот же, в котором закроется шов, поэтому дерево
  будет уже актуальным.

  **ЧТО ДАЛА ВЫДАЧА СВЕРХ ПЕРЕПИСЫВАНИЯ КАРТОЧКИ:**

  1. **МЕБЕЛЬ БУДЕТ ШУМЕТЬ БЕСПЛАТНО, и это подарок, а не работа.**
     `Network/CargoBodyContact.cs:9-11` дословно: «Only the house is physical here, so every press
     this records is a press against the house». Шов не различает, ЧТО физическое: любое физическое
     тело кроме ходока уже считается домом. Значит обстановка, поставленная обычным телом, начинает
     шуметь сама, без единой новой строки — ровно то, чего требует критерий 5 словами «тем же швом,
     а не новым». Наряд требует подтвердить это прогоном, а не принять на веру.
  2. **И у этого есть обратная сторона, названная в наряде заранее:** шум мебели будет НЕОТЛИЧИМ от
     шума о стену — тот же вид, те же пороги. Различать их задача не просит; но если дом станет
     шуметь заметно чаще, это факт владельцу, а не повод крутить `60 / 150 / 400`.
  3. **ЭТО ПЕРЕСТРОЙКА СО СЖАТИЕМ, а не подгонка.** Восемь стен сегодняшнего дома имеют
     `m_LocalScale.y = 1.5`, внешние длиной `14.5`, внутренние `6`; след дома примерно **14.5 × 14
     м** против требуемых критерием **11 × 8.5**. Хозяин при этом капсула высотой `2`. Сжатие
     заметно меняет расстояния, которые проходит налётчик ростом `0.16` м, и это названо в наряде.
  4. **СЦЕНА ЭТОЙ ДОРОЖКИ — ФИКСТУРА ТЕСТОВ.** `IntegratedHouse.unity` копируется в тестовый проект
     и по ней ходят scene-fixture тесты. Наряд разрешает её править (она принадлежит дорожке), но
     требует назвать КАЖДОЕ изменившееся ожидание с объяснением, почему это правильное изменение, а
     не поломка.
  5. **МЕБЕЛЬ ДЛЯ ЭТОЙ ЗАДАЧИ УЖЕ СУЩЕСТВУЕТ.** Ветка `probe/free-kits-2026-08-06` — лампа, стул,
     стол, книжный шкаф, растение, материал пола, с лицензиями и образцом авторинга. Час назад эта
     же ветка оказалась НЕ тем, чем её считала ставка (не персонажи), а здесь она оказывается ровно
     тем, что нужно. Наряд не обязывает её брать, но называет как единственное место в проекте, где
     обстановка есть.

evidence: |
  **ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ ПЕРВОЙ РУКОЙ 2026-08-12 на `f3c40c40`.**

  - `Assets/TunnelCrew/World/ThreeRoomHouse.prefab`: восемь стен, все с `m_LocalScale.y: 1.5`;
    внешние `x/z = 14.5`, внутренние `6`, толщина `0.5`; три комнаты, семь мест, два проёма
    `_width: 2`.
  - `Assets/TunnelCrew/Settings/HouseholderWalkSettings.asset`: `capsuleHeight: 2`,
    `capsuleRadius: 0.5`, `movementSpeed: 1.75`, `urgentMovementSpeed: 3.25`,
    `waypointPauseSeconds: 0.6`, времена жизни фактов `8 / 6 / 12 / 8`, `quietListenSeconds: 1.25`,
    `urgentGuardSeconds: 1.5`.
  - `Assets/TunnelCrew/Network/CargoBodyContact.cs:9-11` — цитата про «only the house is physical
    here».
  - `tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj` — `IntegratedHouse.unity` и
    `HouseholdLab.unity` в блоке `None Include` как фикстуры.
  - `git diff --name-only origin/main...origin/probe/free-kits-2026-08-06` — состав обстановки.
  - `dotnet test ... -c Release` → 214 passed, прогнано этой ногой ранее сегодня на том же дереве.

  **G5:** нога ничего не закрывает. Наряд исполнителю не отдан и держится `blocked`.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-rooms-1-001-call.md` — полный текст наряда.
  2. Создана `cards/c-exec-g-5a7c-rooms-1-001.md` — `status: blocked`,
     `unblock_when: опубликован шов t-places-1`, `slot: WIN-U2`.
  3. `t-rooms-1` остаётся `blocked` — статус не меняется, наряд лежит готовым.
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-rooms-1-dispatch-001.md`.

captures:
  - |
    ОТКАЗ ДОСТАВКИ ПОВТОРИЛСЯ ПРИ СУЩЕСТВУЮЩЕМ ПРАВИЛЕ. `an-order-does-not-exist-until-it-is-delivered`
    существует, стоит клаузой 3 в `evaluator` подписанной ставки и был процитирован в обоих
    нарядах — и всё равно наряды ушли ссылкой на файл. Значит правило лежит верно, а отказала
    нога: она считала «наряд выдан» = «файл записан и закоммичен». Это второй раз за день, когда
    механизм был верен, а сработала нога. Если поймается третий — MAINTENANCE на форму доставки,
    например обязательная строка RESULT «текст наряда доставлен целиком в чат».
  - |
    Сессия исполнителя живёт в ПРОДУКТОВОМ репозитории и физически не читает
    `live/indie-game-development/work/**`. Это очевидно и всё равно было забыто; стоит держать в
    голове при любом будущем «см. файл».
  - |
    Ветка `probe/free-kits-2026-08-06` за один день сменила роль дважды: не персонажи для `t-body-1`
    (улика `i-free-kits-probe-holds-furniture-not-characters-001`) и ровно нужная обстановка для
    `t-rooms-1`. Один и тот же артефакт бесполезен для одной задачи и ценен для другой — это довод
    за то, чтобы открывать содержимое, а не судить по названию ветки.

decisions_needed: []

play_check:
  - 1 recite — done. Цель и семь строк `done_when` карточки `t-rooms-1` пересказаны; обязательство ставки названо.
  - 2 owner inputs — не требуется: облик дома ограничен подписанным критерием 5 с числами, и наряд прямо запрещает доводку внешнего вида. Всё, что выходит за декорацию, маршрутизировано в ESCALATE.
  - 3 do the work — done. Наряд написан целиком; названный подход зафиксирован одним токеном; механизм не предписан, ограничен границами.
  - 4 self-check — done. Три строки контракта покрывают все семь строк карточки; бюджет контракта 36 соблюдён.
  - 5 close — checkpoint. Наряд НЕ отдан: держится `blocked` до публикации шва по измеренной причине.
  - G2 — done. Задача служит активной ставке.
  - G5 — не применяется.
  - G7 — done. Ни одно игровое решение ногой не принято.
  - G10 — done. RESULT финальный.

log: наряд на дом написан и держится blocked до публикации шва по измеренной причине — шов добавляет поле роли в HousePlaceMarker.cs, а этот наряд обязан им пользоваться, и оба работают в World/**; но главным содержанием ноги стало исправление собственного отказа доставки: два часом ранее «выданных» наряда ушли ссылкой на файл в репозитории направления плюс командой захвата слота, исполнитель занял слот и встал, потому что сессия исполнителя живёт в продуктовом репозитории и файла не видит, и это прямое нарушение правила an-order-does-not-exist-until-it-is-delivered, которое нога сама же процитировала в обоих нарядах — второй раз за день механизм был верен, а отказала нога; сверх этого выдача измерила, что мебель начнёт шуметь БЕСПЛАТНО существующим швом, потому что CargoBodyContact считает домом любое физическое тело кроме ходока, что обратная сторона этого — неотличимость шума мебели от шума стены и вероятный рост частоты, что дом надо не подгонять, а перестраивать со сжатием с 14.5×14 до 11×8.5 при стенах 1.5 и хозяине высотой 2, что сцена дорожки является фикстурой тестов и каждое изменившееся ожидание придётся назвать, и что обстановка для этой задачи уже лежит в той самой ветке пробы, которая часом ранее оказалась бесполезной для тел

next: |
  Три текста наряда доставлены владельцу целиком, готовыми к вставке в сессию исполнителя. **Ни один
  наряд не считается выданным до расписки исполнителя первой строкой возврата.**

  Порядок: `c-exec-g-5a7c-places-1-001` (WIN-U2) и `c-exec-g-5a7c-body-1-001` (WIN-U1) идут
  параллельно сейчас; `c-exec-g-5a7c-rooms-1-001` ждёт публикации шва и берёт WIN-U2 после него.

  Возвраты разбираются в отдельных свежих чатах.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-rooms-1-dispatch-001.md
