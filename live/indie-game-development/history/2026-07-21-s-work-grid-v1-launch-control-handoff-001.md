RESULT s-work-grid-v1-launch-control-handoff-001 (call: c-work-grid-v1-launch-control-handoff-001)
direction: indie-game-development
track: grid
play: work
node/task: g-4b92/v1-launch-control-handoff

outcome: |
  The owner accepted the safe sequence «documentation before code» but rejected the accepted plan's
  minimal G01 document boundary. Formal launch disposition is `CORRECTION FIRST`: before any product
  root, the Grid plan must guarantee one current documentation authority. Every old document capable
  of influencing a Grid executor must be mechanically fenced from authority with enforceable controls
  or deleted from the current working tree while remaining in Git history.

  `work/grid-v1-g01-launch-control-handoff.md` records the rule in owner-readable language, the owner's
  exact words, the accepted plan and binding review identities, the read-only current product observation,
  one exact correction position, the full later admission checklist, proof/rollback and the archive-or-delete
  fallback. Current G01 is not admitted. Grid remains parallel/unretired at product progress 0/11.

  One same-position, owner-present Direction correction is READY/default. It may amend only the accepted
  plan's documentation-authority boundary after the owner's exact approval and then route to fresh review.
  No product mutation, product root, engineering CALL, slot, test or implementation leg was opened.

evidence: |
  Done_when reconciliation
  1. Owner-readable launch-control artifact exists at `work/grid-v1-g01-launch-control-handoff.md`.
     It explains the future first step as documentation-only, what it cannot authorize, and pins accepted
     executor plan blob `dfc8caa77a914ae08100d7c3ff3b174bce2be0ff` plus binding review receipt blob
     `53dc02bcd6ef164c51a74110e90a2b0a234da423`.
  2. The artifact records the exact later admission checks: fresh refs/root+Core instructions/v31 contract/
     registry; accepted-plan and correction-review identities; predecessor receipts; old-CALL non-relaunch;
     one explicit owner-selected clean venue; no competing Core/product-wide-docs mutator; literal allowed
     surfaces; complete documentation inventory; proof and rollback. Uncertainty is STOP.
  3. The artifact preserves the future first product step as documentation-only and keeps G02/code closed
     until an owner-accepted, published and freshly reviewed authority cleanup exists. Historical plans,
     old CALLs and dashboard rows are expressly evidence, never launch authority.
  4. Owner actual words:
     `Окей, я согласен. Только единственное, нам не нужно делать минимальную задачу. По документации нужно
     вычистить, привести её полностью в порядок, убедиться, что нам никаким способом старая документация
     не сможет повлиять на работу. Нужно это прям гарантировать. Если мы не можем гарантировать, удалить
     всю старую документацию. Потому что у меня уже были проблемы, когда старая документация, ну, мы
     запускали задачу, и executor, натыкаясь на старую документацию, что-то менял. Я это не сразу замечал,
     и приводило к проблемам. Поэтому нужно строго гарантировать, что актуальна только новая документация.
     Что старая либо в архиве, притом гарантировать, что её никакой executor не начнёт считать правдой.
     Если не можем гарантировать, ещё раз говорю, удалить её тогда полностью. Но имею в виду физически,
     не из Git.`
     These words accept docs-before-code but materially condition launch, so the binary disposition is
     `CORRECTION FIRST`, not `ADMIT LATER G01 PLAN ROOT`.
  5. The exact correction position is `work/grid-v1-executor-plan.md`, section
     `G01 — Legacy fence / authority supersession`, its Outcome/Proof/Rollback, with only consistent
     propagation to F1 and no-drift control. The technical Grid meaning is unchanged.
  6. Required guarantee is concrete: exhaustive tracked-document inventory; one exact current allowlist;
     one disposition per old file; repo-level non-authority rule; stale-link/citation and output checks;
     seeded negative control; fresh refutation; mandatory tracked deletion when archive safety cannot be
     proved; no Git-history rewrite.
  7. Original admission done_when 5 closes as blocked rather than being falsely claimed: there is no product
     mutation/root/CALL/test or implementation Grid leg. The sole new item is the same-position Direction
     planning correction required by the owner's material condition. Grid remains parallel/unretired at 0/11.

  Read-only current product observation
  8. `C:\projects\Unity\GasCoopGame` remained clean with locally observed
     `HEAD = main = origin/main = f6e4f725543514bd67441d4be9ca181392f48c73`; network fetch was not run,
     so this is evidence rather than a launch pin.
  9. Product authority identities were pinned: root instructions `4fa4b036a98f8ec1cdebcd6ac79c72e670190557`;
     Core instructions `f1e8449797ddf1514185b2fe798f3d71a6d9c39e`; v31 contract
     `502d21a69404f7ec0c998333a2616fe9df1494ef`; registry
     `85451b0770dc66fb137deec2396be5bccfcface6`; living sim-core SPEC
     `2c275c49d0b1adcfd4064e8b4a3e6d4b8740c698`; historical ADR-0016
     `996246183afc8d5bec3fac391d363cb8ceb93249`. Registry showed all four permanent owner-selectable
     slots AVAILABLE/lease-none, but none was selected, allocated or inspected as a launch venue.
  10. Explicitly not run or created in the product repo: write, deletion, archive move, branch, worktree,
      root, lease, slot allocation, Unity, test, build, benchmark, Deliver, fetch/pull/push, product PLAN,
      G01, G02+, PAIR-CANDIDATE, BUILD, engineering CALL, Wind/Water placeholder or other Grid leg.

state_changes: |
  1. Add complete owner-readable `work/grid-v1-g01-launch-control-handoff.md` with disposition
     `CORRECTION FIRST`, owner words, exact correction position, authority pins, archive-or-delete guarantee,
     later admission checklist, proof/rollback and explicit no-product boundary.
  2. In `NOW.md`, set `updated: 2026-07-21 by s-work-grid-v1-launch-control-handoff-001`; remove returning
     `open_calls[id=c-work-grid-v1-launch-control-handoff-001]`; add same-position Grid root
     `c-work-grid-v1-document-authority-correction-001` as READY/default with this history receipt and the
     binding review receipt; set `next.call` to that id. Preserve all unrelated tracks, calls, decisions,
     `bet: null`, tasks and WIP limit exactly as fresh state.
  3. Add complete planning-only CALL file
     `work/c-work-grid-v1-document-authority-correction-001-call.md`. It may co-create and owner-approve one
     narrow Direction plan amendment and route it to fresh review, but cannot mutate the product or issue any
     product root/engineering continuation.
  4. Preserve `TREE.md`, CHARTER, knowledge, accepted Grid artifacts, track mode `parallel`, node status,
     product progress 0/11 and all technical Grid meaning unchanged. No accepted-plan amendment occurs here.
  5. Regenerate the declared owner panel `work/board/dashboard.html` from fresh NOW+LOG: keep counts
     `3 ready / 2 waiting / 2 blocked / 1 paused / 1 decision`; replace launch admission with the simple
     CORRECTION-FIRST documentation-authority card/default; update the Grid track card and 21 July journal;
     re-read `work/review/findings.md` and keep the five still-open Problems rows; create no second render.
  6. Append the declared log line once; save this full RESULT once at
     `history/2026-07-21-s-work-grid-v1-launch-control-handoff-001.md`; preserve unrelated current state.

captures: []

decisions_needed: []

play_check:
  - 1 Recite: done — the CALL was reconciled with bounded parallel Grid scope, accepted plan/review authority, planning-only boundaries and product progress 0/11.
  - 2 Owner inputs (owner): done — owner said `Окей, я согласен` and required `нужно строго гарантировать, что актуальна только новая документация`; old documentation must be safely archived so no executor treats it as truth, or `удалить её тогда полностью ... физически, не из Git`.
  - 3 Do the work: done — one owner-readable handoff maps those words to CORRECTION FIRST, names exactly the current G01 Outcome/Proof/Rollback as the correction position and defines the full archive-or-delete guarantee without amending the accepted plan or product.
  - 4 Self-check: done — artifact sections and evidence lines 1–10 reconcile every original done_when item; the admission branch is truthfully blocked on the new owner condition, while product/node completion remains 0/11 and no execution action exists.
  - 5 Close: done — the returning launch-control root is cleared, one same-position Direction planning correction is READY/default, unrelated state is preserved, and no engineering continuation opens.

log: owner accepted docs-before-code but required one mechanically enforced current documentation authority with archive-or-delete fallback; disposition is CORRECTION FIRST, one owner-present plan-amendment continuation is READY/default, Grid remains parallel at 0/11 and product execution stays closed.

next: |
  CALL c-work-grid-v1-document-authority-correction-001

  to: session
  direction: indie-game-development
  track: grid
  play: work
  node: g-4b92
  task: v1-document-authority-correction
  issued: 2026-07-21 (s-work-grid-v1-launch-control-handoff-001)

  goal: |
    Принятый Grid V1 executor plan имеет owner-approved document-authority boundary, при которой только
    один явно названный current corpus может направлять product executors, а старая документация либо
    доказанно не может стать authority, либо отсутствует из рабочего дерева при сохранённой Git history.

  context: |
    `work/grid-v1-g01-launch-control-handoff.md` records disposition `CORRECTION FIRST`, the owner's exact
    archive-or-delete requirement and one correction position: `work/grid-v1-executor-plan.md`, section
    `G01 — Legacy fence / authority supersession`, with consistent propagation to F1 and no-drift control.

    Binding baseline remains the accepted executor plan and
    `history/2026-07-21-s-review-grid-v1-executor-plan-001.md`. The review could not refute the technical
    Grid route, but the current G01 boundary does not prove that discoverable old documentation cannot
    influence an executor. Product v31/refs/registry observations are evidence only and must be fresh at
    any later launch.

  boundaries: |
    Owner-present Direction planning and read-only product inspection only. Do not mutate the product repo,
    allocate/select a slot, create or move a branch/worktree/root/lease, run Unity/tests/build/benchmark/
    Deliver, publish a product PLAN, or issue G01, PAIR-CANDIDATE, BUILD or any engineering CALL.

    Do not change the accepted Grid mechanics, eleven-leg dependency ladder, consumer ownership, Wind cut,
    Multiplayer gate or product progress. Do not call an archive safe without enforceable repo authority,
    validator coverage, a seeded negative control and fresh refutation. Do not weaken the owner's fallback:
    when non-authority cannot be proved, old files leave the current working tree while Git history remains.
    The accepted executor plan changes only after the owner's exact in-session approval.

  done_when: |
    1. One owner-readable amendment corrects the G01 outcome/proof/rollback so the planned product work owns
       complete documentation-authority cleanup, not a minimal clause edit, while preserving Grid's technical meaning.
    2. It defines the exhaustive inventory boundary and one exact current-authority allowlist; every old document
       that can influence Grid work receives exactly one archive-with-proof or tracked-deletion disposition.
    3. It defines mechanically checkable enforcement for executor inputs and outputs, including repo-level authority,
       stale-link/citation detection, seeded negative control, deletion fallback and binding fresh refutation.
    4. The owner receives the amendment in plain language and accepts or corrects it with actual words; the exact
       accepted diff is recorded without mutating the product repository.
    5. Grid remains parallel/unretired at 0/11. Only a fresh review of an accepted amendment may follow; no product
       root, engineering CALL, slot, test or implementation leg is opened.

  return: |
    One accepted-or-corrected plan amendment, the owner's exact words, the complete archive-or-delete guarantee,
    surviving blocker if any, and a Direction RESULT that opens at most one fresh planning-artifact review.

  budget: one owner-present planning session
  surface: any

END_OF_FILE: live/indie-game-development/history/2026-07-21-s-work-grid-v1-launch-control-handoff-001.md
