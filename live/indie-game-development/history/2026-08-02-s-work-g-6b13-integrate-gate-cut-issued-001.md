# RESULT — s-work-g-6b13-integrate-gate-cut-issued-001

play: work
direction: indie-game-development
node: g-6b13
task: c-1
date: 2026-08-02

## outcome

Выпущен наряд на интеграцию урезания проверок. Процедура взята из продуктового `AGENTS.md` первой
рукой, а не по памяти; все предпосылки слияния проверены до выпуска.

## evidence

**Проверено первой рукой 2026-08-02 в продуктовом репозитории:**

- `GasCoopGame_dev` на ветке `dev`, рабочее дерево ЧИСТОЕ;
- `dev` и `main` совпадают: `git rev-list --left-right --count dev...main` = `0 0`;
- `dev` отстаёт от кандидата ровно на два коммита: `dev...slot/win-u3` = `0 2`, то есть слияние
  будет чистой перемоткой, без переписывания истории;
- на `main` по-прежнему `check.ps1` на **854** строки с 32 скриптами, урезанный до 224 в `main`
  НЕ слит (`git merge-base --is-ancestor slot/win-u3 main` — ложь).

**Процедура интеграции, прочитанная в `AGENTS.md`, а не вспомненная:** `:161,165` — `WIN-CTRL`
(`GasCoopGame_dev`, `dev`) используется только для интеграции; `:198-204` — кандидат замораживается
в своём слоте и передаётся по точному SHA свежей задаче `WIN-CTRL`, интеграция в `dev` идёт по
одной, `-Deliver` зелёный на интегрированном `dev` плюс требуемый PlayMode → слияние `dev` в `main`
→ push; `:205-210` — последним обязательным шагом та же транзакция сохраняет непубликованные байты
под durable ref, продвигает фиксированную ветку слота только созданием-на-кончике или перемоткой,
запускает `select-slot.ps1 -Release` и затем read-only селектор против реального чекаута.

**Строка статуса** переставляется интеграцией — по правилу, которое сама эта нога записала в
`AGENTS.md` в своём же документном коммите.

## state_changes

`NOW.md`: новый `open_call` `c-ctrl-integrate-gate-surface-cut-001` со слотом `WIN-CTRL` и
кандидатом `97412301`. `work/`: наряд `c-ctrl-integrate-gate-surface-cut-001-call.md`.

## captures

После слияния `main` получает урезанный контур, и ветка `slot/win-u2` (принятый каркас плюс
отклонённая сетевая работа) окажется на устаревшей базе. Следующая строительная задача начинается с
перебазирования на новый `main`, и там же решается судьба отклонённого коммита.

## decisions_needed

Нет.

## play_check

- Предпосылки слияния проверены ДО выпуска наряда, а не заявлены: чистота дерева, совпадение
  `dev`/`main`, перемоточность, факт неслитости.
- Процедура процитирована по строкам продуктового правила — прошлые бунсы этого контура происходили
  ровно от утверждений о венью, не сверенных с реальностью.
- Плановая стадия сознательно НЕ требуется и это сказано в наряде: интеграция ничего не строит, а
  владелец подтвердил, что для неcтроительных задач она не нужна.
- Запрет обхода недоступного инструмента внесён в наряд явно.

## log

выпущен наряд на интеграцию урезания проверок в WIN-CTRL; предпосылки слияния проверены первой рукой

## next

После возврата — `main` с быстрым контуром, освобождённый `WIN-U3`, и следующая строительная задача:
фундамент со слоем правил, уже с плановой стадией и ADR.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-work-g-6b13-integrate-gate-cut-issued-001.md
