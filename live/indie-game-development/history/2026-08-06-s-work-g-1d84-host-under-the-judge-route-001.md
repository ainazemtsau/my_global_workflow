# RESULT — s-work-g-1d84-host-under-the-judge-route-001

call: c-exec-g-1d84-host-under-the-judge-002
direction: indie-game-development
track: хозяин
play: work
node/task: g-1d84 / t-host-1
date: 2026-08-06
close: НЕТ — светлое закрытие ОТКАЗАНО тестом `os/plays/work.md` §Light close, задача целиком ушла в
свежую биндинговую проверку. Разбор построчно ниже.

## outcome

Наряд `c-exec-g-1d84-host-under-the-judge-002` вернулся домой и снят с фронтира. Работа существует,
влита и опубликована: продуктовый `main` = `origin/main` = `origin/dev` = `d60c2f04`.

**Задача `t-host-1` НЕ закрыта, и это результат теста, а не осторожности.** Тест светлого закрытия
применён построчно ко всем четырём строкам `done_when` и отказал на двух. Частичного светлого
закрытия не бывает, поэтому вся задача уехала в свежий физический чат
`c-work-g-1d84-host-under-the-judge-binding-g5-001` — с точным списком того, что уже перемерено
байтами, и того, что осталось.

Попутно перемерено сверх заказа и передано в проверку: **опубликовано не то, что запускал владелец** —
файл сцены между запущенным им коммитом и опубликованным `main` разный.

## evidence

### Перемер продукта первой рукой (тексту наряда не поверено ни в одном месте)

`C:/projects/Unity/GasCoopGame`: `git rev-parse main origin/main origin/dev` — все три дают
`d60c2f04228bdfa161d6e122c56a2c5ccfec89f3`, дата коммита 2026-08-05 17:47 +0300.

Цепочка работы хозяина от базы наряда, все коммиты разрешаются:
`2eee9719` → `53430228` (сборка) → `0b90a99f` (отчёт) → **`4a514602`** (правка дрожи — то, что
запускал владелец) → `c50ca549` (запись прогона) → `fdfb7e59` (запись приёмки) → **`c9b35650`**
(слияние в `dev`; `git log -1 --format=%P` даёт ровно `2eee9719` + `fdfb7e59`) → `032ebb17`
(`DELIVERED on dev`). Отчёт — `docs/results/c-exec-g-1d84-host-under-the-judge-002.md`.

Слот: `gascoop-slot-state.v1.json` — `WIN-U1` `AVAILABLE`, `lease: none`, `mcp_endpoint:
unrecorded`. Совпадает с тем, что сказано в CALL этой ноги.

### ТЕСТ СВЕТЛОГО ЗАКРЫТИЯ, ПОСТРОЧНО — обоснование маршрута (`done_when` 1 этой ноги)

Строка задачи в NOW: «Решения хозяина считает создатель игры в том же тике, где считаются игроки и
груз; его положение и занятие едут в снимке рядом с ними. На второй копии хозяин идёт так же.
Владелец увидел его одинаково на двух экранах.» — четыре строки.

**Строка 1 — судья считает хозяина в общем тике. ПРОХОДИТ (факт байтов).**
`d60c2f04:Assets/TunnelCrew/Core/Multiplayer/CarryWorld.cs`: внутри `Advance(...)` стоит
`Householder.Advance(tickSeconds, householderWorld)` между `Cargo.Advance(...)` и `body?.Step(...)` —
тот же вызов, где идут ходоки и груз, порядок трёх шагов перевозки не тронут.
`d60c2f04:Assets/TunnelCrew/Network/NetworkWalkerCourier.cs`: `OnServerTick` (строка 122) зовёт
`_world.Advance(...)` (128), шестым аргументом `_householder` (134).
`d60c2f04:Assets/TunnelCrew/World/HouseholderRouteController.cs`: слова `Update` и `Step` не
встречаются НИГДЕ, кроме одного комментария о том, как было раньше — прежнее место симуляции
действительно исчезло, а не осталось вторым.

**Строка 2 — положение и занятие едут в снимке рядом с ними. ПРОХОДИТ (факт байтов).**
`d60c2f04:Assets/TunnelCrew/Network/HouseholderSnapshot.cs` существует: `IsPresent`, `X`, `Z`,
`Occupation`. В курьере снимок собирается в `PublishSnapshots()` (190-196) и едет ТРЕТЬИМ параметром
существующего `ReceiveSnapshotsObserversRpc` (206-211) → `ApplySnapshots` (214-217). Второго RPC,
второго канала и второго тика в файле нет — рискованная догадка 4 ставки не сработала, останавливаться
и спрашивать владельца было не о чем.

**Строка 3 — «на второй копии хозяин идёт так же». НЕ ПРОХОДИТ.**
Устройство кода это обещает: клиент получает `ApplySnapshots` и зовёт `_householder.Show(...)` (230),
своего шага у него не осталось. Но обещание кода — не факт запуска, а всё, что решает, байтами не
проверяется вообще: компиляция engine-side C# в Unity, кодогенерация FishNet для перечисления
`HouseholderOccupation` внутри RPC, ссылки в файле сцены. Агентской проверки со стороны Unity нет НИ
ОДНОЙ — у `WIN-U1` `mcp_endpoint: unrecorded`, и отчёт исполнителя признаёт это сам: «Unity-сборка
engine-side C# исполнена только владельцем, гейтами репозитория — ни разу». `dotnet test` 73/73 —
это слой правил без движка, он второй машины не касается. Значит единственный венью строки 3 — тот же
запуск владельца, что и у строки 4, и отдельным доказательством она не располагает.

**Строка 4 — «владелец увидел его одинаково НА ДВУХ ЭКРАНАХ». НЕ ПРОХОДИТ, и это главная причина
маршрута.**
Его дословные слова, оба закоммичены (`c50ca549`, `fdfb7e59`):

> Так, ну работает, только единственное, что хозяин как-то перемещается, что ли, ну, слегка дергано.
> Вот он идет, перемещение, но оно как-то дрожит или, я не знаю, как объяснить. Я не знаю, в этом
> задача или нет, ну, входит ли в нашу задачу это или нет. Остальное всё работает.

> Проверил, теперь плавно идет

**Ни в одной из двух реплик нет второй копии, второго экрана и вообще ничего про две машины.** Про два
экрана говорит только исполнитель — и его собственная история показывает, как это появилось:

- в `c50ca549` его текст стоял дословно «**пункт 4 этими словами НЕ закрыт** — дрожь исправлена
  коммитом `4a514602`, и нужен повторный взгляд»;
- в `fdfb7e59` та же строка заменена на «**пункт 4 закрыт**», и рядом дописана НОВАЯ фраза: «Первый
  закрыл „увидел одинаково на двух экранах“ **по шагам приёмки**»; там же появилось «два захода на
  `WIN-U1`, оба со сборкой и **обе копии в сети**».

Между этими двумя коммитами владелец сказал ровно одно: «Проверил, теперь плавно идет». Это про дрожь,
а дрожь видна на одном экране. То есть «два экрана» не пришло от него ни в первом заходе, ни во
втором — оно выведено исполнителем из того, что тот сам написал в своих шагах приёмки. **Это
выводимость, а не подпись**, и строка 4 требует именно суждения владельца, которого никто не выносил.

**Вывод по тесту.** Две строки из четырёх требуют суждения, которого нет ни в байтах, ни в
закоммиченных словах владельца об этом самом артефакте. `os/plays/work.md` §Light close: «One line
still needing a judgment nobody has made — behavior, look, quality, completeness, „works“, an owner
verdict not yet given — sends the whole task to the fresh binding chat; there is no partial light
close.» Маршрут выбран этим, а не удобством: строки 1-2 закрыть светло было можно, и это ничего не
меняет.

### ПЕРЕМЕР СВЕРХ ЗАКАЗА: опубликовано не то, что он запускал

Сравнение блобов между `4a514602` (что запускал владелец) и `d60c2f04` (что опубликовано), восемь
путей работы:

- `Assets/TunnelCrew/Scenes/IntegratedHouse.unity` — **РАЗНЫЙ**: `175682d5` → `9ada2ac9`.
- `NetworkHouseholder.cs`, `NetworkWalkerCourier.cs`, `HouseholderSnapshot.cs`, `CarryWorld.cs`,
  `AuthoritativeHouseholder.cs`, `HouseholderRouteController.cs`, `DrawnPose.cs` — **побайтово те же**.

Причина названа в отчёте соседней полосы: при слиянии панели условий (`69c47a08`) сцена
конфликтовала — обе ноги добавили один и тот же stripped-трансформ `&1607884504`, конфликт разрешён
сведением к одной копии. Проводка хозяина на `d60c2f04` на месте и осмотрена адресно:
`NetworkHouseholder._householder: {fileID: 1607884504}` (корень хозяина) и
`NetworkWalkerCourier._householder: {fileID: 2053887854}` (сам компонент); третье вхождение
`1607884504` — ссылка панели.

**Поломки НЕ утверждается, и это важно.** Утверждается один холодный факт: сцену в её нынешнем, слитом
виде не запускал никто — хозяина владелец смотрел до слияния, панель он смотрел на своей ветке тоже до
слияния. Для строк 3 и 4 это значит, что даже его прошлый запуск относится к другим байтам сцены.
Факт уехал в биндинговую проверку отдельным `done_when`, вердикт по панели условий не выносится.

### Поправка к тексту выреза №1 (`done_when` 4 этой ноги)

Вырез говорил: «Груз и ходоки на второй копии едут тиками». Перемерено на `d60c2f04`:
`Assets/TunnelCrew/Network/DrawnPose.cs` существует и им пользуются ОБЕ существующие презентации —
`NetworkWalkerPresentation.cs` и `NetworkCargoPresentation.cs`. То есть поза приходит тиками, а
рисуется скольжением до следующего слова судьи; тиками груз и ходоки не едут. Комментарий самого
`DrawnPose` описывает ровно тот симптом, который потом увидел владелец: шаг в пару сантиметров у
глаза читается как дрожь ближнего предмета на фоне спокойных стен.

Цена ошибки уже уплачена и она не бумажная: наряд повторил формулировку дословно, исполнитель прочитал
её буквально, написал хозяина прямо в трансформ на тике, и владелец увидел дрожь и сказал о ней.
Починено правкой `4a514602` тем же `DrawnPose`. Поправка ЗАПИСАНА в `NOW.bet.cuts` рядом с прежним
текстом (прежний оставлен нетронутым, чтобы поправку было видно), а не внесена молча. Сам вырез в силе:
вырезано предсказание на клиенте — клиент не досчитывает движение, не экстраполирует и не забегает
вперёд последнего слова судьи.

### Два непоехавших решения владельца (`done_when` 5 этой ноги)

Оба перемерены на опубликованном `d60c2f04` и оба ОСТАВЛЕНЫ ОТКРЫТЫМИ:

- `i-unity-cloud-services-enabled-in-main-001`: `ProjectSettings/UnityConnectSettings.asset:7` —
  по-прежнему `m_Enabled: 1`. Откат не поехал.
- `i-grid-v1-g01-dead-root-open-001`: в каталоге квитанций корня по-прежнему ровно два файла, оба
  стадии PLAN. Терминальной квитанции нет.

В обе записана строка `mechanism_failed`: с момента решений владельца (2026-08-05) домой вернулись ДВА
продуктовых наряда — `c-exec-g-1d84-host-under-the-judge-002` (`c9b35650`) и
`c-exec-g-1d84-conditions-panel-001` (`69c47a08`) — и ни один не нёс ни одного из двух пунктов.
Причина класса, а не случая: решение живёт только в записи, а наряд пишется из задачи и её `done_when`,
куда запись не смотрит. Два подряд — это уже класс.

### Прочее состояние, приведённое к измеренному

`c-exec-g-1d84-cargo-tight-spot-001` стоял `ready`, тогда как реестр показывает `WIN-U2` `CLAIMED` с
арендой `c-exec-g-1d84-cargo-tight-spot-001:BUILD` и записанным endpoint — то есть наряд идёт.
Переведён в `running` с `started`-квитанцией на реестр (`os/adapters/coding-agent.md` Role 1, launch
bookkeeping: runtime claim receipt). Строка `ready` при взятой аренде — это риск запустить наряд
дважды.

Базы: `cargo-tight-spot` и `conditions-panel` объявляют `2eee9719`, `free-kits-probe` — `02a53bbb`;
все три мертвы, живая база `d60c2f04` записана в каждую. Полоса панели условий НЕ ТРОНУТА: её статус
не менялся, вердикта по ней нет, записано только наблюдение, чтобы полоса не переписала уже сделанное.

## state_changes

Применено этой же ногой (агентский CLI на этом репозитории: сессия сначала выпускает RESULT, затем
становится своим писателем).

1. `NOW.md` заголовок: `updated: 2026-08-06 by s-work-g-1d84-host-under-the-judge-route-001`.
2. `NOW.bet.cuts` вырез 1 — добавлена **ПОПРАВКА 2026-08-06** отдельным абзацем; прежний текст выреза
   сохранён дословно и не изменён.
3. `NOW.tasks` `t-host-1` — `status: open` (без изменения), `note` заменена полным разбором маршрута:
   цепочка коммитов, отказ теста светлого закрытия построчно, разный блоб сцены.
4. `NOW.open_calls` — удалён `c-exec-g-1d84-host-under-the-judge-002` (вернулся домой, слияние
   `c9b35650` → `032ebb17`).
5. `NOW.open_calls` — добавлен `c-work-g-1d84-host-under-the-judge-binding-g5-001`
   (track `хозяин`, `to: session`, `for: t-host-1`, `status: ready`).
6. `NOW.open_calls` `c-exec-g-1d84-cargo-tight-spot-001` — `ready` → `running` + `started`-квитанция;
   в `note` записана мёртвая база и живая `d60c2f04`.
7. `NOW.open_calls` `c-exec-g-1d84-conditions-panel-001` — статус НЕ менялся; в `note` добавлено
   наблюдение о мёртвой базе, слиянии `69c47a08` и конфликте сцены, с явной оговоркой, что полосу
   закрывает своя нога.
8. `NOW.open_calls` `c-exec-g-1d84-free-kits-probe-001` — в `note` перемерена живая база `d60c2f04`.
9. `NOW.issues` `i-unity-cloud-services-enabled-in-main-001` — остаётся открытой; добавлено
   `mechanism_failed`, `evidence` перемерено на `d60c2f04`.
10. `NOW.issues` `i-grid-v1-g01-dead-root-open-001` — остаётся открытой; добавлено `mechanism_failed`,
    `evidence` перемерено на `d60c2f04`.
11. `NOW.issues` — заведена `i-published-to-main-before-any-direction-close-001`
    (level `direction`, route `review`).
12. Создан `work/c-work-g-1d84-host-under-the-judge-binding-g5-001-call.md`.
13. `LOG.md` — одна строка сверху.
14. `history/2026-08-06-s-work-g-1d84-host-under-the-judge-route-001.md` — этот RESULT целиком.

Не тронуто: `CHARTER.md`, `TREE.md`, `knowledge/**`, полосы кроме «хозяин», решения, задачи кроме
`t-host-1`, продуктовый репозиторий (ни одного write — только чтение коммитов и блобов).

## captures

- Сторож «вторая другая реакция отказывается» в `AuthoritativeHouseholder.CanHear` — единственное
  место, где предусловие правила продублировано снаружи `Householder.cs`; уходит, когда у правил
  появится способ предложить событие и получить отказ. Отчёт исполнителя, §Assumptions.
- Отладочная панель хозяина усохла до того, что едет в снимке; хозяин в `HostWalksHisDay.unity` встал —
  прямое следствие переноса симуляции, сцену править было запрещено, её судьба решается отдельно.

## decisions_needed

Нет. Маршрут закрытия выбирается тестом плея, а не владельцем, и CALL этой ноги прямо допускал оба
исхода. Один вопрос владельцу существует — смотрел ли он хозяина на двух копиях — но он принадлежит
биндинговой ноге и записан её `done_when` 3, а не решается здесь.

## play_check

- 1 Recite: done — `t-host-1` полосы «хозяин» под ставкой `g-1d84`; четыре строки `done_when`
  восстановлены из NOW дословно и разобраны построчно.
- 2 Owner inputs (owner): skipped — владелец по этой задаче уже высказался дважды, и его слова
  закоммичены (`c50ca549`, `fdfb7e59`); нового ввода нога не просит. Единственный оставшийся вопрос
  (две копии) принадлежит биндинговой ноге и там записан явно. Слова, на которых стоит маршрут, — его
  собственные: «Остальное всё работает» и «Проверил, теперь плавно идет».
- 3 Do the work: done — возврат наряда принят, всё перемерено первой рукой на `d60c2f04`, тест
  светлого закрытия применён построчно, выпущен один биндинговый CALL.
- 4 Self-check: done — пять строк `done_when` CALL этой ноги сверены поштучно, см. ниже.
- 5 Close: done — задача не закрыта, названа причина, полоса получила одно продолжение; соседние
  полосы и записи сохранены.

Сверка с `done_when` CALL этой ноги:
1. Маршрут выбран и обоснован построчно — §ТЕСТ СВЕТЛОГО ЗАКРЫТИЯ; задача явно отправлена в свежую
   биндинговую проверку. ✓
2. Наряд снят с фронтира с точной ссылкой на слияние `c9b35650` (родители `2eee9719` + `fdfb7e59`) и
   маркером `032ebb17`. ✓
3. Живая база `d60c2f04` записана; у всех трёх остальных нарядов объявленная база названа мёртвой. ✓
4. Поправка к тексту выреза №1 записана рядом с прежним текстом. ✓
5. Оба непоехавших решения перемерены, оставлены открытыми, получили `mechanism_failed`. ✓

log: g-1d84/t-host-1: хозяин под судьёй сделан и опубликован, но тест светлого закрытия отказал на
двух строках из четырёх — задача ушла в свежую биндинговую проверку; наряд снят с фронтира,
перемерено, что опубликована не та сцена, которую он запускал.

next: `c-work-g-1d84-host-under-the-judge-binding-g5-001` — свежий физический чат, биндинговая G5 по
`t-host-1`, с одним вопросом владельцу по строке 4.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-host-under-the-judge-route-001.md
