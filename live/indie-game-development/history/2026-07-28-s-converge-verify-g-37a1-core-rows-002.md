# RESULT s-converge-verify-g-37a1-core-rows-002

direction: indie-game-development
play: converge-verify
node: g-37a1
call: c-converge-verify-g-37a1-core-rows-002
verify_target: build
date: 2026-07-28

## outcome

**FAIL — but a different FAIL from the first one, and the difference is the whole report.**
`work/converge-g-37a1-core-rows.md` still does not carry `§SIGNOFF`. **ALL SIXTEEN REPAIRS CLOSE
THEIR CHARGES.** Not one of F1–F16 was merely restated, and the two judgment calls the CALL told
this leg to attack hardest **both survive** — one of them on ground stronger than the repair's own.
Eight findings R1–R8 are named and **none is filled, repaired or decided here.** **NOT ONE OF THE
SIXTEEN LIVE REQUIREMENT LINES IS CHALLENGED**, no criterion is touched, no knob value is attacked
or chosen, no owner question is opened or answered, `d-air-counter-visibility-001` stays open,
`g-37a1` stays `parked`, and no shape, bet, task or track was created.

**WHAT THE EIGHT FINDINGS ARE AGAINST, stated once so the shape of this FAIL is not misread.** Six
of the eight are against **text the repair itself wrote** — the newly committed pass rules, the new
§KNOBS split, the new routing sentences. Two are against **roll-up sections the repair's own CALL
forbade it to touch**, and those two are a scope defect of the bounded CALL rather than a failure of
the repair. **Every one of the eight is a one-to-three-sentence fix**, none needs the owner, and
none reopens a decision. The artifact is converging: sixteen findings became eight, the eight are
smaller, and the two hardest calls held.

**KERNEL TWO-STRIKES IS NOT FIRED, and the closest calls are adjudicated in the open below.** No
finding here repeats a charge of `s-converge-verify-g-37a1-core-rows-001`. Three are close enough
that they are argued rather than asserted: R1 against §KNOBS (F1's charge was five named numbers,
all five correctly relocated; the sixth is created by the split that did the relocating), R2/R3/R5
against §RULES and ROW 9 (F3 and F8 charged ABSENCE of a rule; these charge the CONTENT of the rule
that was supplied, which is a first round on new text), and R6's §BUILD-CLOSES-BETTER half (the same
claim F9 struck, standing in a section F9 never charged and the repair CALL forbade). A third round
against §KNOBS' governance classes WOULD be two-strikes territory, and the successor CALL says so.

## the two judgment calls — both SURVIVE

**F11, the strike of ROW 16's late-join — SURVIVES, and this leg raises the ground it stands on.**

- **No owner text contains it, verified first-hand and not taken on the repair's word.** `CHARTER.md`,
  `TREE.md` (all fifteen criteria of `g-37a1` and all of `g-5e8c`) and
  `work/core-requirements-g-37a1.md` were searched for any late-join, rejoin or mid-session-join
  clause. **Zero hits.** Criterion 11 and core line 16 require exactly the two things the repair
  says they require.
- **The three standing rules it leans on are real.** «минимальный набор с возможностью расширения» —
  his, in `i-engine-fit-decision-ladder` and in the requirements artifact. Authoring rule 3
  («НИЧТО СКАЗАННОЕ РАНЬШЕ НЕ ТРЕБОВАНИЕ, пока он не повторил это здесь») — verified verbatim at
  `work/core-requirements-g-37a1.md`. CHARTER lens 3 («Реально ли одному разработчику закончить и
  поддерживать этот объём?») — verified verbatim at `CHARTER.md` §Линзы решений.
- **The strike is inside a converge's authority, and calling it a manufactured decision would be
  backwards.** The late-join clause was added by a leg, not by him. Removing a leg's own unsourced
  addition is a smuggling repair, which is exactly what step 3 of the play asks for.
- **THE STRONGEST AVAILABLE ATTACK ON IT WAS RUN AND FAILED, and the derivation is recorded because
  the file does not contain it.** ROW 16's FORCES says late-join "is NOT forced by this core" and,
  two sentences later, names ROW 4's full reset with a second player mid-session as "the one
  genuinely open mechanism this row inherits". Those two sentences sit adjacent without being
  connected, and one resolution of the second re-imports the first: if a dead player rejoins a
  living player's in-progress section, that IS snapshot join. **The independence claim is
  nonetheless TRUE, and it is entailed rather than asserted:** criterion 9 restarts «уровень»
  ПОЛНОСТЬЮ, and ROW 16 requires both players to be digging **in the same section** — a per-player
  reset would put them in different sections and fail ROW 16 by construction. So the only reading
  consistent with both is a session-wide reload, under which nothing joins anything. The strike is
  safe, and it is safe for a reason the file should carry.

**F2, "the build must ship at least two authored levels" — SURVIVES both halves of the attack.**

- **The derivation holds.** Criterion 12 lists «загрузка другого уровня кнопкой» among the controls
  reachable from inside the game; his own word is «другой». Reading «другой» as «тот же» empties his
  word and makes the control a duplicate of criterion 9's full restart, which is separately
  required. Nothing may be placed at load time (criterion 12 forbids the generator; ROW 12 fixes the
  inputs before the build). A build therefore satisfies the clause only by containing a second
  pre-authored section. There is no fourth option.
- **It does not smuggle a requirement, and the test that decides this is the repair's own.** F11 was
  struck because no owner text contains it. F2 is kept because owner text contains the clause and is
  otherwise unsatisfiable. That is the correct discriminator — *does the set gain an obligation, or
  does an approved clause gain the content that makes it checkable* — and the repair applied it
  consistently in both directions on the same day.
- **§SIGNOFF's corrected term count is right.** The §GLOSSARY table was counted row by row: **twenty-
  nine.** The arithmetic that went stale in the amendment is now correct.

## findings

**R1 — the §KNOBS split puts the base's placement in BOTH tables, and the owner's own register
decides which one is wrong.**
RIG KNOBS lists `base | where it stands · how much air it gives back · at once or over time`
(row 15). LEVEL-AUTHORING INPUTS lists `section | dimensions · pocket count · pocket sizes ·
per-pocket kind · total substance · **where the base stands**` (rows 11, 12, 15). The same parameter
is therefore simultaneously governed by ROW 5 (readable and writable from the built player) and by
ROW 12 (fixed before the build, and the built player must be unable to write it). ROW 15's FORCES
cites all three base numbers as "knobs, not decisions", so **a row now cites a knob that has moved
out from under it** — which is precisely the check `done_when` 3 asks for. **The class boundary is
decided without the owner and against the LEVEL-AUTHORING entry, by the repair's own stated test:**
that table's note says its inputs "are in none of his registers", and
`work/core-requirements-g-37a1.md`'s settings register contains the line «Где стоит база и сколько
воздуха она возвращает | ручка (следствие базы)». Base placement IS in his register, so by
§GLOSSARY's signed reading of «спорное число» it is a RIG knob and belongs in the first table only.
This is not bookkeeping: ROW 15 calls air capacity and the base's refill the most load-bearing
numbers in the rig now that ROW 7 is retired and there is no completion condition, and whether the
leash's anchor can be moved from the built player or only by re-authoring and rebuilding is a
different rig at a different price. ROW 12's OUTSIDER FAIL list — pocket count, sizes, kinds,
dimensions, total — does not include base placement either, so no test currently decides it.
*Hits:* §KNOBS, ROW 12, ROW 15. *Routes to:* `converge`.
*Two-strikes:* NOT the same charge as F1, which enumerated five numbers, all five correctly moved.
The base contradiction is created by the split itself — before it there was one table and no second
place for a parameter to appear. First round on this item.

**R2 — §RULES' new Part 1 is all-or-nothing on six rules whose stimulus nothing controls, and its
justification is false for at least one of them.**
Part 1 requires all six BEDROCK rules in a free statement, and "A BEDROCK rule missing from Part 1
is a legibility defect in the build, and the row FAILS." The justification is that they are "the
ones a session cannot avoid teaching: he cut rock, he read a face, he opened a pocket, he spent air,
he went back to the base, he died." **That justification is asserted, and it is false for rule 2.**
Rule 2 («Before you cut, the rock shows there is a pocket behind it and roughly how big») is a
perceptual affordance the player need never consult: he can cut, hit a pocket, be surprised, and cut
again, for a whole session, on a build whose mark is perfect. **And the same file grades the same
perception far more loosely elsewhere:** ROW 14 tests exactly this property with a committed deck
and tolerates one error in ten on presence and two in ten on size, because "roughly how big" is a
graded perceptual judgment. §RULES then demands unprompted articulation of it with zero tolerance
and no deck. **Two instruments in one file grade one property at incompatible strictness, and the
stricter one has no stimulus control, no sample and no second chance.** Rules 8, 10 and 11 carry a
weaker form of the same defect: each requires the sitting to have reached the end of the air economy
(both kinds entered, the base used, a death), and **the test commits no minimum session content at
all** — "One session, no explanation", with no duration, no required experiences and no instruction
to the administrator about what must have happened before Part 1 is recorded. Every other committed
instrument in this file controls its stimulus (six pairs, twelve frames, ten pairs); this one
controls only the scoring, and it can fail a build that is exactly right.
*Hits:* §RULES. *Routes to:* `converge`.
*Two-strikes:* NOT the same charge as F3, which charged that four rules were ABSENT and that no
threshold was committed. Both are closed — the four rules are rules 3, 6, 7 and 14, and the
threshold exists. This is a first round on the threshold that was supplied.

**R3 — §RULES' sharpest new clause has no scope, and by the file's own standard that makes it
uncommittable.**
"FAIL if the outsider states, in Part 1 or Part 2, a rule the game does not have." The three
examples are all negations of real rules, and against those the clause is right and is the best
thing added to §RULES. **As written it is not scoped to them.** «the deeper you go the bigger the
pockets» is a rule the game does not have; so is «the wide cut throws it further». Neither
contradicts any of the fifteen — the first is a guess about authored content, which §RULES is
explicitly not about ("The in-game rules a player reads by playing"), and the second is an
embellishment. Under the literal clause both FAIL the row. F8's own principle governs here: a rule
that resolves differently in two administrators' hands cannot be committed before the sitting, and
that is the property the file itself calls the reason §14 was the strongest instrument in the
record. The fix is one word of scope — a statement that CONTRADICTS one of the fifteen — and it
preserves everything the clause was added to catch.
*Hits:* §RULES. *Routes to:* `converge`.

**R4 — ROW 15's newly committed row-level pass rule omits two of the row's three halves.**
The row now carries three scored parts: (a) which kind was more dangerous, "PASS if he answers
correctly, unprompted"; (b-i) behaviour, "PASS on yes"; (b-ii) words, "FAIL only if he cannot
connect the base to air at all". Then: "**The row FAILS if (b-i) fails, or if he can refill anywhere
else.**" That sentence is exhaustive in form and names neither (a) nor (b-ii). **So (b-ii) is given
a FAIL condition and no consequence**, while the void-clause two paragraphs later says that if the
visibility decision ever makes the base's effect imperceptible then "(b-ii) becomes VOID and the row
is decided by (a) and (b-i)" — which only means something if (b-ii) normally decides part of the
row. The two sentences cannot both be right. An administrator facing an outsider who walks back to
the base (b-i pass) but cannot say what it is for (b-ii fail) has no rule, which is the "argued
after the sitting" failure that committing a rule in advance exists to prevent. This is new text
written specifically to close F5, and F5's repair is otherwise the best-argued in the set: the
per-half re-derivation of the dependence on `d-air-counter-visibility-001`, and the survival check
across all four of its options, both hold.
*Hits:* ROW 15. *Routes to:* `converge`.

**R5 — ROW 9(a)'s new decision rule scores a two-part judgment as one binary, and this direction's
own record establishes that the two parts come apart.**
The deck is six pairs; "The outsider says which of the two released more, and faster"; "PASS
requires all six pairs ordered correctly. Any single error FAILS." **Two properties, one verdict,
and no rule for the case where they disagree.** This is not a hypothetical: `i-flow-model-unmeasured`
records, first-hand at product `1a6373b8` and reproduced by two independent readers, that in this
solver the per-face move is `alpha * driver` with `alpha` hard-capped, that **changing the flow
numerator moves the ENDPOINT and never the TIMESCALE**, and that an overfill driver gives the same
time constant while moving less mass. A build in which a wider cut releases visibly MORE while the
timescale barely moves is therefore a live outcome of the engine the rows will be built on — and
criterion 6's own text is «чем шире рез, тем сильнее **и быстрее** пошло», both halves. Under the
current rule such a build is scored by whichever half the outsider happened to answer on. Score the
two halves separately, or state which one decides. The three probability claims attached to the F8
repairs were checked and are all correct: six pairs all-correct is 1 in 64; ten of twelve is 79/4096
≈ 2 in 100; nine of ten is 11/1024 ≈ 1 in 100.
*Hits:* ROW 9. *Routes to:* `converge`.
*Two-strikes:* NOT the same charge as F8, which charged that no sample size and no decision rule
existed. Both now exist for all three rows. This is a first round on the rule that was supplied, and
it is against ROW 9 only — ROWS 13 and 14 carry single-judgment decks and are clean.

**R6 — three roll-up claims in the two sections the repair was FORBIDDEN to touch now restate
exactly what its repairs struck, and this is a defect of the bounded CALL's scope rather than of the
repair.**
Three instances, one cause:
(i) **§BUILD-CLOSES-BETTER** still lists «**ROW 10**'s ban» among the rows "Closed better by a
document plus a one-minute confirmation in the build". Repaired ROW 10 strikes that claim in those
words — "The earlier claim — 'a document plus the knob list closes the ban' — is FALSE, and it is
struck" — and its new test (d) needs two machines and a working co-op session, while its residue is
named as closed only by the substance step's input-list review in the engineering contour.
(ii) **§ORACLE class 13** still reads "**COVERED** — §SEAM, four seams, all decidable now", which is
verbatim the completeness claim §SEAM withdrew under F15.
(iii) **§ORACLE class 14** still reads "**COVERED** … the guard against an eternal laboratory is
that planning of the next version does not wait", while repaired ROW 17 establishes that the guard
covers the POST-build stretch only and that the PRE-build stretch — the one that killed `g-12fd` —
is guarded by nothing in the file.
**Why this is the CALL's defect and not the repair's.** `c-converge-g-37a1-core-rows-repair-001`
scoped the repair to the sections the FINDINGS hit. A fix that strikes a claim invalidates every
roll-up that restates it, and roll-ups live in other sections by construction — §ORACLE and
§BUILD-CLOSES-BETTER exist to summarise the rest of the file. A section-scoped repair CALL therefore
cannot be self-consistent, and the successor CALL must name these three lines as in scope. **It is
also the repair's own named pattern** — a claim carried forward when the text it was derived against
is amended — committed in the one place it was not allowed to look. §BUILD-CLOSES-BETTER is
explicitly written "for the shape leg at step 6", so the live consequence is concrete: a shape leg
reading it will not budget ROW 10's two-machine sitting or its input-list review.
*Hits:* §BUILD-CLOSES-BETTER, §ORACLE 13, §ORACLE 14. *Routes to:* `converge`, with the scope
explicitly widened to these three lines and to nothing else in those sections.
*Two-strikes:* NOT fired. F9 charged ROW 10 and F15 charged §SEAM and §WHAT; all three were
repaired there. Neither §ORACLE nor §BUILD-CLOSES-BETTER has ever been charged.

**R7 — §WHAT's routing of criterion 11 strengthens the owner's clause into a rule his words do not
contain, and manufactures at `shape` the owner decision F11 refused to manufacture.**
His text: «Сетевая основа не откладывается — кооператив идёт сразу за первым играбельным ядром, а
не после игры.» The contrast he draws is *right after the core* versus *after the game*. §WHAT
converts it into: "**a shape leg may not defer ROW 16 to a later bet, and if it proposes to, that is
an owner decision and not a shaping choice.**" Bet granularity inside the node is nowhere in his
words. If `shape` opens bet 1 as the venue, the body and the dig verb — a man digs a hole and walks
into it — and bet 2 as co-op plus the substance, that is ordinary sequencing entirely inside
`g-37a1`, and this sentence turns it into an owner interruption. **The first verification read the
same clause the other way** and said so: "Nothing here blocks the first build tasks: a man digs a
hole and walks into it, the body, **and co-op right after** are untouched." The clause needs a home
— F13's finding is correct and its diagnosis is correct — but the home must forbid what he forbade:
deferring the network basis past `g-37a1`, not distributing `g-37a1`'s own rows across its own bets.
The same sentence also sits inside `i-core-acceptance-instrument-unpriced`, which a shape leg reads.
*Hits:* §WHAT criterion 11, and the copy in `NOW.md`. *Routes to:* `converge` for §WHAT; the
`NOW.md` copy is corrected by this leg's own state change, because a verifier may write `NOW.md`.

**R8 — the acceptance-labour count understates itself by the row that carries the largest sitting in
the set.**
§WHAT: "Eleven of the sixteen live rows require a person outside development (ROWS 1, 2, 3, 6, 8, 9,
11, 13, 14, 15, 16), and four of those require a deck or a pass rule committed to the repository
BEFORE a sitting (ROWS 9, 13, 14, 15)." **ROW 5 is in neither list and belongs in both.** Its own
OUTSIDER test begins "Give an outsider the parameter home and one instruction", then adds three
in-game control tests; and §RULES — the instrument that decides criterion 12's «все правила
читаются», which is ROW 5's criterion — is a full sitting with an outsider, a verbatim free
statement recorded before any question, a fixed questionnaire committed to the repository, and an
asked-list carried forward as legibility debt. **That is the most labour-intensive single sitting in
the file**, and the repair names it as making the labour larger without moving ROW 5 into either
count. Twelve of sixteen, and five with a committed instrument. (ROW 10's test (a) — "a person who
did not write it" — is arguably a thirteenth and is named rather than claimed, because "did not
write it" admits a second developer.) The number matters because it is the number a `shape` leg
prices against, and F16's whole point was that the obligation comes from the row FORMAT rather than
from his text.
*Hits:* §WHAT. *Routes to:* `converge`; the price still belongs to `shape`.

## what held

Stated at length because most of what was attacked held, and because a second FAIL read without this
list would misdescribe the artifact.

- **All sixteen repairs CLOSE their charges.** Checked one by one against the charge as originally
  stated. None is a restatement. F1's five numbers are correctly relocated and ROW 5, ROW 12 and
  criterion 12 now hold together. F4's restated ROW 17 guard survives criterion 15 and criterion
  14's own safeguard, and both exemptions are genuinely dependencies rather than waits — the
  forecast is `no_basis` by his own instruction, and the safeguard needs one entry he must make to
  close the node anyway. F6's default pin is present in all four rows and each names what
  specifically breaks. F7 closes cleanly and **honours the signed size-not-kind distinction rather
  than violating it**: the deck is now explicitly of BREACHED pockets seen from outside, unbreached
  faces are excluded by name and routed to ROW 14, and the criterion-2 / criterion-7 collapse is
  gone. F10's four-variable coupling is correct — time to death is capacity ÷ (base rate × per-kind
  multiplier), against K1 — and it now agrees with ROW 15 instead of contradicting it. F12's split
  is right and ROW 12 now carries the authoring constraint both halves need. F13's criterion-14 half
  is correctly held by `TREE.md`, verified: `g-37a1` is a child of the October demo root and
  `g-5e8c`'s own `why` depends on it. F14 reconciles ROWS 1 and 5 and the new term does the work.
  F15's forced property — one value in force per session for any knob that changes the simulation —
  is correct and correctly scoped to simulation knobs, leaving per-player switches open by name.
- **ROW 4 and ROW 11, untouched and previously called the best-formed things in the file, are not
  damaged by their changed neighbours.** ROW 4's second half raises K2 "from the parameter home",
  and K2 is a RIG knob under the new split — consistent. ROW 11's total-mass readout lives in "the
  parameter home / debug channel" while ROW 12's new FAIL forbids an outsider *changing* the total —
  a read is not a write, and the debug channel covers it either way.
- **The §KNOBS split leaves exactly one row citing a moved knob, and it is R1.** Every other
  citation was traced: K1 → breach, K2 → death, K3 → air, K4 → ground legibility, K5 → terrain, ROW
  2's body sizes, ROW 6's dig granularity, ROW 5's three OUTSIDER instructions — all in RIG groups.
  ROW 12's FORCES correctly names its four as level-authoring.
- **The arithmetic is right.** §GLOSSARY holds twenty-nine rows, matching the corrected §SIGNOFF.
  §WHAT's coverage restatement is right: thirteen criteria carry a row, two (13, 15) are row-less by
  design, three governance clauses exist rather than one, sixteen rows are live. §RULES is fifteen
  rules — six BEDROCK, eight asked, one by absence.
- **The SETTING rule was held cleanly by the repair.** No value is chosen anywhere for K1–K5, air
  capacity, the base's refill or the step rate. ROW 14's size ratio is correctly routed to `PLAN`
  and committed with the deck rather than picked here.

## dropped as refuted or already named

Eight raw findings did not survive the self-refutation pass. Recorded so nobody re-raises them
cheaply, and because two of them are the strongest attacks available on repairs that held.

1. *«ROW 15(a)'s «a factor he calls "a lot"» is an uncommitted bar, F8's defect in a fourth row».*
   **REFUTED.** The operative pass rule is stated and binary — "PASS if he answers correctly,
   unprompted" — and the magnitude is K3's value, which the SETTING rule forbids this file from
   choosing. The clause describes the build property, not the scoring.
2. *«F11's strike is unsafe, because ROW 4's open multiplayer reset can re-import snapshot join».*
   **REFUTED by derivation** — criterion 9's full restart plus ROW 16's "both dig in the same
   section" entail a session-wide reload. Recorded because it is the strongest attack on the strike
   and because the derivation is not in the file.
3. *«ROW 6 needs the defaults pin too — two outsiders at different start states pass test (a)
   trivially».* **DROPPED.** F6 did not name ROW 6, the first run explicitly refuted the ROW-6(a)
   attack, and the failure form is an administrator error rather than a build defect.
4. *«ROW 14's size ratio is an uncommitted bar».* **REFUTED.** The ratio is a magnitude correctly
   routed to `PLAN` and committed with the deck before the sitting; choosing it here would break the
   firewall.
5. *«ROW 10's test (d) needs frame-synchronised capture across two machines».* **REFUTED.** The term
   it exists to catch — per-actor, the `PlayerSense` shape the product already ships — produces a
   gross difference that needs no synchronisation.
6. *«§SEAM 5's one-value-per-session property is wrong, because immortality is per-player».*
   **REFUTED.** The property is scoped to "a knob that changes the simulation", and the per-player /
   per-session question is named as open in the same seam.
7. *«состояние захода» over-claims: it asserts configuration persists across a RELAUNCH while ROW
   5's instrument needs persistence across a run restart only».* **DOWNGRADED to an observation.**
   The justification is broader than the need, but no row fails under either reading, and ROW 1's
   pin ("the parameter home is UNCHANGED between the two starts") makes both starts identical
   whichever way a build stores its configuration.
8. *«ROW 1's FORCES conflates «сцена» and «уровень» — "Which scenes the player contains — at least
   two authored levels"».* **DOWNGRADED to an observation.** §GLOSSARY names «уровень» = «сцена» as
   the reading to avoid, and this sentence puts them in apposition; but FORCES routes HOWs to `PLAN`
   and no test turns on whether two levels live in one scene or two.

Also checked and NOT found defective: the mixed-region hazard at ROW 13 and the sources-are-finite
conflict at ROW 12, both already-named CONFIRMATIONS; ROW 8; §SEAM items 1–4; the retirement of ROW
7; and the K1–K5 registrations against the owner's settings register — which is also where R1's
decisive evidence came from.

## evidence

**SESSION INDEPENDENCE AND SURFACE.** Fresh chat in the direction worktree, no owner present, as the
CALL and the play require. Not the repair chat, not the first verification, not the chat that wrote
the rows, not a day chat. This is the binding fresh G5 pass for the repaired rows.

**WHAT WAS READ, in the CALL's order.**
`work/c-converge-verify-g-37a1-core-rows-002-call.md`;
`history/2026-07-28-s-converge-g-37a1-core-rows-repair-001.md` (the claim under attack);
`work/converge-g-37a1-core-rows.md` in full (the target);
`history/2026-07-28-s-converge-verify-g-37a1-core-rows-001.md` (the sixteen charges as originally
stated, the seven refutations, the four confirmations, the six competing classes);
`TREE.md` `g-37a1` (the fifteen criteria) and the rest of the roadmap for F13's TREE claim;
`work/core-requirements-g-37a1.md` (the sixteen live lines, the NOT-core list, the settings
register); `NOW.md` in full; `CHARTER.md` (lenses, pre-mortem, risk posture); `os/KERNEL.md`;
`os/plays/converge-verify.md`; `.agents/skills/direction-os/SKILL.md`.

**NOT READ:** `history/2026-07-28-s-converge-g-37a1-core-rows-001.md` — the deciding session's
reasoning, which the play forbids the verifier and the CALL names explicitly. Its existence was
confirmed by a directory listing and nothing more. `archive/**` was not read. **The product
repository was not opened**; every engineering fact used here is carried by an existing first-hand
receipt — R5's solver constants from `i-flow-model-unmeasured` (product `1a6373b8`, receipt
`history/2026-07-27-s-converge-verify-g-37a1-digging-card-001.md` V2) and R6's `PlayerSense` shape
from the same receipt's V3. `history/2026-07-28-s-research-g-37a1-engine-fit-check-001.md` was not
read and no engine fact decided a row.

**FAN-OUT: none, deliberately.** `converge-verify` is not a play that mandates in-leg fan-out, this
CALL carries no instruction to distrust a single sweep, and it is a narrow rerun on a closed list.
Single-agent, with an internal self-refutation pass instead: sixteen raw findings entered, **eight
were refuted or downgraded** and are named above.

**HOW THE PLAY'S «do not rely only on the author's instrument» WAS MET on a narrow rerun.** The
target's own §ORACLE is out of scope and could not be the instrument. Three independent instruments
were used instead: (1) **each charge as originally stated** in the first verification's receipt,
read before the repaired text, so every repair was scored against the words it answers rather than
against its own summary; (2) **the owner-approved artifacts read directly** — the fifteen criteria,
the sixteen live lines and the settings register — which is what produced R1's decisive evidence and
R7's, and which is what let F11's strike be verified rather than accepted; (3) **the competing class
list C1–C6** authored by the first run, of which C1 (the authored content and its authoring loop)
produced R1 and C2 (the acceptance instrument's own falsifiability, committed when) produced R2, R3,
R4 and R5.

**STATE.** Read at `9892a091`, the repair's own commit, which is `HEAD` and clean. No concurrent leg
moved `live/**` during this one.

## state_changes

1. `NOW.md` — `updated:` to this leg. `direction_forecast.drivers[1]` gains a leading paragraph:
   the narrow rerun ran, all sixteen repairs CLOSE their charges, both judgment calls survive, eight
   new findings stand against new text and against two roll-up sections, step 3 has still not
   passed, and the FAIL below remains the standing verification state. `i-card-clauses-unverifiable`
   records that base placement is contested between the two §KNOBS tables and that his own register
   decides it (R1), and that the acceptance count is twelve rows rather than eleven (R8).
   `i-core-acceptance-instrument-unpriced` — **the criterion-11 sentence is corrected in place per
   R7**: the constraint is that the network basis may not be deferred past `g-37a1`, not that ROW 16
   may not sit in a later bet inside it; and the labour count is corrected to twelve rows and five
   committed instruments. `open_calls`: the spent verification row replaced by exactly one
   `c-converge-g-37a1-core-rows-repair-002`. `d-post-verify-route-001.progress` records the second
   FAIL and the second repair without reordering his route and without adding a seventh step.
   `bet: null`, `tasks: []`, `recurring: []` unchanged. No decision opened, none answered.
2. `work/c-converge-g-37a1-core-rows-repair-002-call.md` — CREATED. A bounded `converge`, no owner,
   carrying the eight findings, the two-strikes boundary for §KNOBS, and **an explicitly widened
   scope of three lines in §ORACLE and §BUILD-CLOSES-BETTER** which the previous repair CALL
   forbade and which R6 shows a section-scoped CALL cannot avoid.
3. `work/c-converge-verify-g-37a1-core-rows-002-call.md` — `disposition:` block appended.
4. `LOG.md` — one line appended.
5. `history/2026-07-28-s-converge-verify-g-37a1-core-rows-002.md` — this RESULT.

Not touched: `CHARTER.md`; `TREE.md` and both cards, which carry his verdict on exact text;
`knowledge/**`; the target `work/converge-g-37a1-core-rows.md`, which a verifier never repairs;
`work/core-requirements-g-37a1.md`; the product repository, which was not opened; Steam state;
`archive/**`. No `§SIGNOFF` line was written. No shape, bet, task, track or executor CALL was
created. No numeric release chance was published. `d-air-counter-visibility-001` stays open and
unanswered.

## captures

- **A BOUNDED REPAIR CALL SCOPED BY SECTION CANNOT BE SELF-CONSISTENT, and R6 is the proof.** When a
  repair STRIKES a claim, every roll-up that restates it goes stale, and roll-ups live in other
  sections by construction. The cheap countermeasure, worth proposing to `pulse` as a convention
  rather than as a gate: **a repair CALL's scope list carries the sections the findings hit PLUS
  every section that summarises them**, and a repair that strikes a claim greps the artifact for it
  before closing. Both this leg's §ORACLE findings would have been caught by one search.
- **§ORACLE's score line is arithmetically wrong and predates the repair.** "Thirteen of fourteen
  covered, one partial by design, one genuine gap, one routed" sums to sixteen for fourteen classes;
  covered is ELEVEN (2, 3, 4, 5, 6, 7, 10, 11, 12, 13, 14). Out of this leg's scope and not a
  finding — recorded so the leg that next has authority over §ORACLE fixes it in the same pass as
  R6's two lines.
- **The settings register in `work/core-requirements-g-37a1.md` is still stale on «Цена смерти
  (сейчас ноль)»**, which the previous two legs both named and neither could touch. This leg used
  that same register as decisive evidence for R1, which raises the stakes: **a `shape` leg reads it,
  it is now load-bearing for a governance question, and it has been known-stale across three legs.**
  It is the one artifact in this chain with an owner-approved status and no leg with authority over
  it.
- **THE PRE-BUILD STRETCH IS NOW MEASURABLE AND SHOULD BE SAID PLAINLY AT `shape`.** Step 3 has
  consumed two verification legs and two repair rounds, twenty-four findings, and has produced no
  product evidence, while every one of the sixteen rows names the built player as its evidence. The
  artifact IS converging — sixteen findings to eight, all smaller, both hard calls held — and none of
  the eight needs the owner. But this is the exact shape `g-12fd` had, and the only instruments that
  can end it are the bet's `appetite` and `kill_by`, which step 6 creates. Not a finding, because it
  is outside a verifier's authority; recorded because the shape leg must see the count.

## decisions_needed

**None, and none was manufactured.** `converge-verify` routes findings and decides nothing. R1 looks
like a decision and is not: the owner's own settings register already assigns base placement to the
knob class, so the contradiction is resolved by citation rather than by asking him. R7 is the
opposite case — a leg's sentence currently manufactures a `shape`-time owner decision his words do
not require, and removing it is a correction, not a verdict. `d-air-counter-visibility-001` stays
open; the fifteen criteria and the eleven closed rulings were not re-litigated; no row was asked to
prove the core is fun; no knob's value was attacked or chosen; no row was scored incomplete because
a value is unchosen.

## play_check

- `verify_target: build` — complete=FAIL smuggling=FAIL.
- **Step 1, recite the claim:** done. Both build attacks named before the attack ran, narrowed to
  the rerun's subject: (1) each repair closes the charge it answers and the set remains complete;
  (2) nothing the repair wrote leans on an unresolved question and nothing previously sound broke.
- **Step 2, attack completeness with an INDEPENDENT oracle:** done. The target's own §ORACLE is out
  of scope and could not be the instrument; the three independent instruments used are named under
  `evidence`. The empty-oracle block did not fire. Uncovered items are NAMED and NOT filled, and the
  four the CALL lists as deliberately unfilled were each confirmed still named and still unfilled —
  §ORACLE class 8, the machine budget's price, the acceptance labour's price and owner, and the
  pre-build stretch — and are reported as CONFIRMATIONS, not defects, exactly as `done_when` 4
  requires.
- **Step 3, attack smuggling:** done. Every weight-bearing claim the repair wrote was traced to
  owner text, to a first-hand receipt, or to nothing. Three traced to nothing or to less than they
  claim: base placement's LEVEL-AUTHORING entry contradicts his own register (R1), criterion 11's
  routing sentence contains a bet-granularity rule his words do not (R7), and §RULES Part 1's "a
  session cannot avoid teaching" is an assertion (R2). **The two repairs the CALL named as most
  likely to be smuggling were traced hardest and both are clean** — F11's strike removes leg-written
  text rather than adding owner-written text, and F2's second level makes an approved clause
  satisfiable rather than adding an obligation. No engineering fact was re-derived. No leaked HOW
  magnitude needed moving to `→ PLAN`.
- **Step 4, close:** FAIL. No `§SIGNOFF` was written. Named sections return to `converge` by one
  bounded CALL with a scope explicitly widened by three lines. Verification reruns narrowly again.
- **Owner-verdict guard:** no owner verdict was invented, inferred or needed. The CALL asked for no
  owner-readable verdict and none is offered. Step 5 remains his and is not opened here — step 3 has
  not passed, and his own route requires it before the ladder.
- **Two-strikes:** **NOT FIRED, and adjudicated rather than assumed.** No finding repeats a charge
  of `s-converge-verify-g-37a1-core-rows-001`; the three closest calls are argued in `outcome` and
  in each finding's own note. **The boundary is recorded for the next run: a third charge against
  §KNOBS' governance classes, or a second against §RULES' committed pass rule, IS the second failed
  correction round on that point, and the run that finds it checkpoints instead of issuing a fourth
  repair CALL.** The successor CALL carries this.
- **G-gates:** G1 untouched (`bet: null`, no lane). G5 honoured — binding fresh refutation session,
  separate chat from the repair, the first verification and the authoring leg. G7 — no decision
  opened. G9 — no CHARTER, no TREE, no card, no `knowledge/`. G10 — steps exposed, RESULT final,
  state changed only by applying this RESULT.

## log

2026-07-28 | s-converge-verify-g-37a1-core-rows-002 | converge-verify | direction |
g-37a1/core-rows: all sixteen repairs CLOSE their charges and both judgment calls survive — but
eight new findings stand, six against the text the repair wrote and two against roll-ups its own
CALL forbade it to touch, so step 3 still has no signoff.

## next

`c-converge-g-37a1-core-rows-repair-002` — a second bounded `converge`, no owner, on the eight
findings and nothing else, with §ORACLE 13, §ORACLE 14 and §BUILD-CLOSES-BETTER's ROW 10 line
explicitly IN scope because R6 shows a section-scoped repair cannot reach them otherwise. Every one
of the eight is a one-to-three-sentence fix and none needs the owner. Then step 3 reruns narrowly a
second time, with the two-strikes boundary above live. **Step 5 — his ladder verdict — still waits
on step 3 by his own route**, and the four items a step-5 verdict quoting 50–90 days must name as
outside it are unchanged: the amendment items, the parameter home's write refusal, the second
authored level, and the machine budget.

END_OF_FILE: live/indie-game-development/history/2026-07-28-s-converge-verify-g-37a1-core-rows-002.md
