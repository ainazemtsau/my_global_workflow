# RESULT — s-converge-arch-g-5a7c-005

direction: indie-game-development
play: converge-arch
node: g-5a7c
call: work/c-converge-arch-g-5a7c-repair-001-call.md (возврат ноги) — эта нога записывает ответ
      владельца, полученный ПОСЛЕ возврата, тем же порядком, что `-002`, `-003` и `-004`
date: 2026-08-07
session: владелец в чате; отдельная нога — свой RESULT, свой apply, свой коммит, Git перечитан заново

**База продуктовых фактов перепроверена первой рукой ДО любого `факт кода`:**
`C:/projects/Unity/GasCoopGame`, `git rev-parse HEAD` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`,
`git status --short` пусто. Только чтение.

**Состояние OS перечитано перед началом:** `git log -1` = `466bcba5`, дерево чисто.

---

## outcome

**Его ответ про вес ПЕРЕФОРМУЛИРОВАЛ вопрос, а не заполнил его, и это главное в ноге.** Дословно: «тут
сразу не ответишь нужен какой то параметр, но легкий один ожет тащить, средний например 2е и так далее».
Вопрос `W114` («при скольких руках груз перестаёт волочиться») ответа одним числом НЕ ИМЕЕТ: лестница
принадлежит КЛАССУ груза, а не игре. Заведена строка `A11` с четырьмя опровергнутыми вариантами и шов
`C30`. **Ни одного числа и ни одного имени класса не назначено; `W114` остаётся открытой.**

**Вторая находка — про наш собственный язык.** На `W115` он ответил «не понимаю в чем тут вопрос». Это
не отказ отвечать, а измеренный дефект ФОРМУЛИРОВКИ: строка написана языком набора. Маршрут и содержание
развилки верны и не меняются; переписывает `converge`, которая строку и писала.

Счёт: `arch_rows` 10 → 11, `arch_open` 9 → 10, внутренних швов 17 → 18. `contracts_open` без изменений
(1). Владельцу не задан ни один вопрос.

---

## evidence

### Что перемерено первой рукой, и почему это делает его ответ АРХИТЕКТУРНЫМ, а не числовым

1. **Массы у предмета нет — она глобальная и живёт ровно в одном месте.** `grep "CargoMass\|\.mass"` по
   всему `Assets/TunnelCrew` даёт ДВА попадания: объявление `NetworkPlaySettings.cs:118` и единственное
   чтение `Network/CargoBody.cs:61` — `rigidbody.mass = _settings.CargoMass`.
2. **Слой правил про массу не знает ВООБЩЕ.** `grep "Mass"` по `Assets/TunnelCrew/Core/` — ноль строк. В
   конструкторе `CargoRules` четырнадцать полей (`pickupRange`, `holdStrength`, `maxHolders`,
   `gripLength`, `handReach`, `handStiffness`, `maxHandForce`, `linearDamping`, `angularDamping`,
   `carrySpeedScale`, `halfLength`, `halfWidth`, `carryOffset`, `noise`) — массы среди них нет.
3. **Правила ОДНИ на все грузы, предмет своих чисел не несёт.** `AuthoritativeCargoRoster.Add` принимает
   только `CargoState` (поза); `ApplyCarryLoad`, `Reduce` и `NoiseOf` получают один `CargoRules` на весь
   ростер.
4. **Габариты и потолок числа рук тоже общие:** `halfLength`/`halfWidth` в тех же общих правилах,
   `_cargoMaxHolders: 2` в общем ассете.

Следствие: при одних общих числах ступень в игре ровно ОДНА, какой бы предмет ни взяли. Его «лёгкий /
средний» выразить НЕЧЕМ — это не настройка, а отсутствующий шов.

### `A11` — четыре варианта, три отпали

- **(а) оставить общие числа** — отклонён ЕГО словом: «лёгкий один может тащить, средний 2е» при общих
  числах невыполнимо в принципе. Второй независимый довод: ремонт уже показал, что подбором ОДНОЙ массы
  под его текстуру `I09` порог всё равно встанет не там.
- **(в) свой полный набор чисел на каждый предмет** — отклонён: он назвал КЛАССЫ, а не предметы; полный
  набор на вещь есть гарантированный разъезд и бьёт по `A1`, где дисциплина держится тем, что абсолютное
  число размера существует в одном месте.
- **(г) везти массу в слой правил, чтобы лестницу считали правила** — отклонён как НЕнужный, а не как
  неверный: лестница уже следствие физики (`A4`-а), правилам масса для этого не нужна, а `Mass` без
  потребителя — число, которое никто не читает. **Дверь оставлена открытой честно:** если кривая опоры
  (`C28`) захочет массу, это отдельный шов внутри `C28`, а не общее решение здесь.
- **(б) ВЫЖИЛ:** у предмета ССЫЛКА на именованный КЛАСС; класс несёт массу, габариты и потолок числа рук.
  **Формулы не меняются ни одна**, потому что шов уже параметризован: `rules` едет ПАРАМЕТРОМ через
  `ApplyCarryLoad`, `Reduce`, `NoiseOf` — меняется место, откуда его берут. `Reduce` остаётся «for a list
  of any length»; `MaxHolders` остаётся ЧИСЛОМ, а не константой, значит ограничительный контракт `C08`
  («ничто не имеет права зашить двойку») держится; `A1` цел — класс хранит числа кратными росту, точка
  перевода одна.

### Граница, которая не переступлена

Узел ограничен ОДНИМ предметом лута его же словом (`W79`). Поэтому покупается **ШОВ, а не каталог
классов**: один класс, один предмет и место, куда второй класс приедет, не меняя ни формулы, ни ростера.
Ни числа, ни имена классов здесь не назначаются: «лёгкий / средний» записаны ВХОДОМ по его же
«например».

### Что осталось открытым и почему это не минус

`W114` остаётся `open → PLAN` с возвратом к `OWNER`. Меняется её ПОСТАНОВКА, и это надо записать в §WHAT:
спрашивать «какие числа у класса, чтобы его ступень встала там, где её поставил владелец», а не «при
скольких руках». Иначе задача пойдёт искать одну общую массу — то есть ровно то, что опровергнуто
дважды. §WHAT правит `converge`; наряд дополнен.

### Вторая находка: `W115` нечитаема для владельца

Его слова: **«не понимаю в чем тут вопрос»**. Строка `W115` описывает развилку через собственные термины
набора («как ЧИСЛО НОСИЛЬЩИКОВ входит в громкость», «даром через пол» против «отдельной величины») и
ссылается на четыре других идентификатора. Содержание верно, маршрут верен, читаемости нет.

Это дефект того же класса, что уже стоил этому направлению отдельных ног: строка состояния, написанная
для набора, а не для человека, который по ней принимает решение. Правит `converge` (она эту строку и
писала); наряд получил отдельный `done_when` с проверкой вслух — прочитать строку и спросить себя,
ответил бы на неё человек, не читавший набор.

**Развилка ему в этой ноге НЕ переформулирована в состоянии** — это делает `converge`. В чате она ему
объяснена простыми словами, и его ответа нога не ждёт: маршрут `W115` (`open → PLAN`, задача общего слоя
шума, возврат к владельцу, если это его величина) узел не блокирует.

---

## state_changes

1. `live/indie-game-development/work/converge-g-5a7c-arch.md` — новая секция `A11`; §NOT-DECIDED (строка
   про порог переформулирована его словом); §SIGNOFF (счёт: `arch_rows` 11, `arch_open` 10, швов 18,
   вердиктов + вердикт по форме, входов 3; таблица трёх счётов; долг перед §WHAT вырос с 3 до 5 пунктов);
   §REPAIR-LEDGER — третий блок.
2. `live/indie-game-development/work/converge-g-5a7c.md` — **только §CONTRACTS**: вводный абзац (третий
   список тронутого), **новый `C30`**, «Счёт этой ноги» и фенсированный блок. §DRIFT, §IMPORT, §GLOSSARY,
   §WHAT, §COUNT и оба §SIGNOFF — ни знака. Ряды `C01`–`C29` не тронуты.
3. `live/indie-game-development/work/c-converge-g-5a7c-owner-answers-001-call.md` — дополнен: `goal`,
   `context`, список токенов (шесть высказываний), счёт, и два новых `done_when` (5 — переформулировать
   `W114`; 6 — переписать `W115` читаемо). Прежние пункты перенумерованы, ни один не удалён.
4. `live/indie-game-development/work/c-converge-verify-g-5a7c-002-call.md` — дополнен разделом про
   шестой и седьмой токены и обновлённым списком открытого. Статус `waiting` не менялся.
5. `live/indie-game-development/NOW.md` — `open_calls`: note наряда `converge` дополнен; добавлена
   строка-комментарий. Ставка не открывается, задачи и полосы не создаются, `issues` и `decisions` не
   тронуты.
6. `live/indie-game-development/LOG.md` — одна строка вверх.
7. `live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-005.md` — этот RESULT целиком.

**Не трогается:** `TREE.md`, `CHARTER.md`, `knowledge/**`, §WHAT и §COUNT файла
`work/converge-g-5a7c.md`, `W15`, `W16`, продуктовый репозиторий.

---

## captures

- **CAPTURE.** `W115` оказалась нечитаемой для владельца, и это, похоже, КЛАСС, а не случай: строки
  §WHAT пишутся плотным языком набора со ссылками на другие идентификаторы, а решение по ним в итоге
  принимает он. Стоит проверить, сколько ещё строк с меткой `open → OWNER` в закрытых наборах
  сформулированы так же. Маршрут при подтверждении — `MAINTENANCE`: это устройство формата строки, а не
  содержание узла. Читать вместе с находкой `F8` первой проверки (метка `acceptance` как механизм
  затвердевания) — обе про то, что формат строки решает судьбу содержания.
- **CAPTURE (не действовать здесь).** `_cargoMaxHolders: 2` живёт в общем ассете и по `A11` переезжает в
  класс груза. Это тот самый номер, который ограничительный контракт `C08` запрещает зашивать; при
  переезде надо перепроверить, что он остался числом. Место для проверки — задача, делающая класс груза.

---

## decisions_needed

**Ни одного.** Ему не задан ни один вопрос. Открытые строки называют отвечающего: числа класса груза
(=`W114`) — задача, ставящая вертикаль у груза, с возвратом к нему; `W115` — задача общего слоя шума, с
возвратом к нему, если это его величина; кривая опоры — `→ PLAN`; «что такое интерактивный элемент
обстановки» — он, спрашивает та нога, которая первой доведёт обстановку до задачи.

---

## play_check

```
owner_tokens_recorded: 2 — вердикт по ФОРМЕ ответа про порог («сразу не ответишь, нужен какой-то
      параметр») и прямое «не понимаю в чем тут вопрос» про W115. Первый записан вердиктом, второй —
      измеренным дефектом формулировки, а НЕ отказом отвечать
owner_tokens_not_inflated: PASS — «лёгкий / средний НАПРИМЕР» записаны ВХОДОМ, его смягчение сохранено
      дословно; ни числа, ни имена классов не назначены; подписей под текстом карточки по-прежнему 0
owner_decisions_raised: 0 — вопросов ему не задано
no_new_numbers: PASS — ни массы, ни порогов, ни габаритов; A11 решает, ГДЕ живут числа, и не выбирает
      ни одного
scope_not_widened: PASS — покупается ШОВ, а не каталог классов; граница взята из подписанной карточки
      (W79, один предмет лута), и это сказано в A11, в C30 и здесь
arch_rows: 10 → 11 (A11); arch_open: 9 → 10
contract_rows: 12 краёв + 18 внутренних швов (добавлен C30); contracts_open: 1 без изменений
arch_in_context_only: PASS — ни одна строка A11 не является требованием и не едет в done_when
what_untouched_by_this_leg: PASS — §DRIFT, §IMPORT, §GLOSSARY, §WHAT, §COUNT и оба §SIGNOFF
      converge-ноги не тронуты ни знаком; open_rows остаётся 10, deferred остаётся 2
contracts_rows_untouched: PASS — C01–C29 не тронуты; добавлен только C30
what_debt_named_not_hidden: 3 → 5 — добавлены переформулировка W114 и читаемость W115; оба переданы
      наряду c-converge-g-5a7c-owner-answers-001, а не оставлены молча
W15_W16_untouched: PASS
card_untouched: PASS — TREE.md не открывался на запись
base_reverified_first: PASS — 352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa, дерево чисто, только чтение
fan_out: одиночный контур по прямой постоянной инструкции владельца
next: converge (owner-answers, дополнен), затем ВТОРАЯ converge-verify свежей сессией
```

---

## log

2026-08-07 | s-converge-arch-g-5a7c-005 | converge-arch | direction | Его ответ про вес переформулировал
`W114`, а не заполнил её: числа груза — свойство КЛАССА, а не общей настройки. Новая `A11`, шов `C30`.
Отдельно измерено, что `W115` нечитаема для владельца — дефект формулировки, не маршрута.

---

## next

`c-converge-g-5a7c-owner-answers-001` (`ready`, дополнен) — девять `done_when`: занести пять его токенов
в §WHAT, переформулировать `W114`, переписать `W115` читаемо, привести §COUNT и побайтово подтвердить,
что §CONTRACTS и арх-файл не тронуты.

Затем `c-converge-verify-g-5a7c-002` (`waiting` → `ready`) — **ВТОРАЯ проверка узла, свежей физической
сессией**, с потолком второго FAIL. Наряд дополнен атаками на все семь токенов; самая дорогая по-прежнему
«и груз и человек», а самая тонкая — `A11`: проверить, что там не куплен каталог классов вместо шва и не
назначено ни одного числа.

Дальше, при PASS, — `shape`.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-arch-g-5a7c-005.md
