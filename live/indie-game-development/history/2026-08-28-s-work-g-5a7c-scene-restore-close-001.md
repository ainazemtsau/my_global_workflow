RESULT s-work-g-5a7c-scene-restore-close-001
direction: indie-game-development
play: work
node/task: g-5a7c / t-scene-restore-1
close: |
  light — все пять строк done_when перевыведены ЭТОЙ ногой первой рукой: четыре из закоммиченных
  байт origin/main @ fd322155, пятая — прогоном Unity, который нога запустила сама через
  MCP-эндпоинт редактора WIN-U4. Отдельный чат G5 не открывался и не нужен.
outcome: |
  ИГРА СНОВА ОТКРЫВАЕТСЯ В ДОМЕ, И ЗАДАЧА ЗАКРЫТА НЕ ПО ОТЧЁТУ, А ПО ПЕРЕМЕРУ. Сдача исполнителя
  принята как УЛИКА, и восемь её заявлений проверены первой рукой; семь сошлись, одно
  воспроизвести не удалось и записано заявлением, а не фактом.

  ПОЧИНЕНО НЕ ТО, ЧТО БОЛЕЛО, И ЭТО ГЛАВНОЕ В НОГЕ. Наряд просил вернуть дом в список сборки.
  Возврат стоил четырёх строк, а по дороге вскрылся дефект, который подмена сцены ПРЯТАЛА два
  дня: дом принимал ДВОИХ. Не по настройке, не по лимиту сети, не по транспорту — по числу
  трансформов, заведённых в сцене. Третий игрок, нажавший Join, молча отключался, и единственным
  следом была одна строка в консоли ХОСТА, то есть не у того, кто вылетел. Пока играемой сцены не
  было в списке сборки, заметить это не мог никто: в неё просто нельзя было войти.

  ЦЕНА ПЕРЕСКАЗА, СТАВШЕГО РАЗРЕШЕНИЕМ, ОКАЗАЛАСЬ ВЫШЕ ЗАПИСАННОЙ. Улика
  i-executor-paraphrase-became-the-approval держала два последствия: игра запускалась в павильон,
  и сетевые playmode-тесты были красными на main с 26 августа. Третье не было названо: подмена
  ЗАКРЫЛА ЕДИНСТВЕННЫЙ ПУТЬ, на котором дефект двух игроков мог проявиться, и потому он дожил до
  сегодня незамеченным. Один запрет, снятый прозой, стоил направлению не «неудобно», а сломанной
  сети плюс спрятанного потолка в два игрока.

  СЕТЕВОЙ ПОРТ БЫЛ ОДИН НА ЧЕТЫРЕ РАБОЧИХ МЕСТА, и это чинилось расширением по прямому слову
  владельца «я разрешаю расширить делай». Кто первым входил в PlayMode с хостом, тот занимал 7770
  на всей машине, а в трёх остальных слотах ЛЮБОЙ сетевой тест падал с «port unavailable» —
  сообщением, которое выглядит как дефект игры. Это тот же класс, из-за которого написан сам
  наряд: красное показывается не там, где сломано. Теперь у слота свой пин в UserSettings/,
  папке, которая уже в .gitignore, — значит пин физически не может уехать в коммит, а слот не
  может из-за него застрять грязным.

  И НАЙДЕНА ПРОВЕРКА, КОТОРАЯ БЫЛА ЗЕЛЁНОЙ И СЛОМАННОЙ ОДНОВРЕМЕННО. task-preflight.selftest.ps1
  читал два файла навыков, которых на main нет. Молчала она не поэтому: перемер показал, что
  tools/check.ps1 НЕ УПОМИНАЕТ селф-тест ни одной строкой. Он не входил в зелёный гейт, поэтому
  его никто не запускал, поэтому и не всплыло, что он читает несуществующее. Обе строки убраны, а
  утверждения расширены с 18 до 25 и теперь проверяют ЗНАЧЕНИЕ пина (7770+N), а не его наличие.

  ЧТО ЭТО ОТКРЫВАЕТ ВОЛНЕ: у t-reach-wire-1 из двух точек ожидания снята одна — «показывать
  негде». Вторая, t-reactions-data-1, осталась и названа в самой карточке провода самой важной:
  без неё имени вещи взять неоткуда. Полоса «провод» этой ногой НЕ стартует.
evidence:
  - |
    ДОСТАВКА. fd322155 — вершина origin/main продуктового репозитория, 2026-08-28 05:51,
    проверено merge-base --is-ancestor и log -1 origin/main. Восемь коммитов сверх 8f679196,
    двенадцать изменённых файлов, +928/−20 строк. Отчёт на main, 297 строк,
    docs/results/c-exec-g-5a7c-scene-restore-1-001.md.
  - |
    КРИТЕРИЙ 1 — перевыведен байтами. git show origin/main:ProjectSettings/EditorBuildSettings.asset
    даёт ровно две строки path: — Assets/TunnelCrew/Scenes/Lobby.unity и
    Assets/TunnelCrew/Scenes/IntegratedHouse.unity. Съёмочной сцены в списке нет.
  - |
    КРИТЕРИЙ 2 — перевыведен байтами, обе половины. git log 0d6ee6e1..origin/main --
    TrailerHouse_EveningLab.unity пуст: сцену не трогал ни один коммит с момента подмены.
    Её собственная переустановка на месте — TrailerHouse_EveningLab.unity:4448-4449,
    propertyPath: _onlineScene → value: .../TrailerHouse_EveningLab.unity. Файл сцены
    150 869 байт, папка запечённого света рядом цела: LightingData 36 337 байт, лайтмапы и
    четыре пробы отражений — указатели LFS, не тронуты.
  - |
    КРИТЕРИЙ 3 — ЕДИНСТВЕННЫЙ, КОТОРЫЙ НЕЛЬЗЯ ВЗЯТЬ ИЗ БАЙТ, И ОН ПРОГНАН ЭТОЙ НОГОЙ САМОЙ.
    В сцене на origin/main четыре объекта: Player Start 0, 1, 2, 3 — было два. Прогон: tests-run
    в редакторе WIN-U4 через его MCP-эндпоинт, EditMode, метод
    TunnelCrew.EditorTests.FirstHouseInSceneEditModeTests.TheHouseholderAndThePlayersStartInsideARoomAndHeStandsOnTheFloor
    → Passed, 3.966 с, из 217 обнаруженных тестов, 0 упавших. Прогон ЗАКОННЫЙ ДЛЯ MAIN, и это
    проверено ДО запуска: чекаут WIN-U4 стоит на fd322155, ahead 0 / behind 0 относительно
    origin/main, рабочее дерево чистое — редактор открыт ровно на байтах главной ветки. Тест
    утверждает три вещи: PlayerSpawnCount == 4, каждая из четырёх точек попадает внутрь комнаты
    дома, и игрок за последней получает исключение, то есть ОТКАЗ, а не выдуманное место.
  - |
    КРИТЕРИЙ 4 — перевыведен байтами. NetworkRuntime.prefab:95 →
    _onlineScene: Assets/TunnelCrew/Scenes/IntegratedHouse.unity. Протечки нет, и проверена она с
    обеих сторон: в IntegratedHouse.unity вхождений _onlineScene НОЛЬ, в Lobby.unity — НОЛЬ,
    значит обе берут значение префаба, а переустановка живёт только внутри съёмочной сцены.
  - |
    КРИТЕРИЙ 5 — перевыведен байтами, двумя способами. В
    Assets/TunnelCrew/Settings/GameRulesSettings.asset последние шесть определений несут
    _mass: 10 — ровно шесть, ровно последние. И сам файл в доставку НЕ ВХОДИТ:
    git diff --name-only 8f679196 origin/main его не называет.
  - |
    ГОЛОВНЫЕ ТЕСТЫ ПРОГНАНЫ ЭТОЙ НОГОЙ, А НЕ ПЕРЕСКАЗАНЫ.
    dotnet test tests/TunnelCrew.Core.Tests -c Release на чекауте fd322155 →
    «не пройдено 0, пройдено 532, всего 532», 545 мс.
  - |
    ПОРТ НА СЛОТ — перемерен по составу, а не по обещанию. Новые файлы:
    Assets/TunnelCrew/Settings/HostPortForThisCheckout.cs — 207 строк;
    Assets/Tests/TunnelCrew/Networking/HostPortPinTests.cs — 137 строк, 30 тестовых атрибутов;
    плюс правка NetworkConnectionSettings.cs и tools/task-preflight.ps1 (+105 строк). Тестов в
    Assets/Tests/TunnelCrew/Networking/ стало 61 против 31.
  - |
    СЕЛФ-ТЕСТ, БЫВШИЙ ЗЕЛЁНЫМ И СЛОМАННЫМ. Старая редакция читала
    .agents/skills/unity-initial-setup/SKILL.md и .agents/skills/ping/SKILL.md; на main в той
    папке лежат unity-mcp-routing и unity-urp-visual-dev, названных нет. Причина молчания
    перемерена отдельно, и она хуже: git show origin/main:tools/check.ps1 не содержит слова
    selftest вовсе — проверка не входила в зелёный гейт и потому не запускалась. Утверждений
    throw FAIL было 18, стало 25.
  - |
    ЗАЯВЛЕНИЕ, КОТОРОЕ НОГА НЕ СМОГЛА ВОСПРОИЗВЕСТИ И НЕ ЗАПИСЫВАЕТ ФАКТОМ. Владелец передал
    «тестов стало 44 вместо 14». Считано три раза, ни один счёт не даёт этой пары: сетевых тестов
    31 → 61, утверждений селф-теста 18 → 25, вызовов Assert-Contains 20 → 21. Число, вероятно,
    верно для какого-то конкретного среза, но назвать этот срез нога не может, и потому оно
    остаётся заявлением.
  - |
    ЗАЯВЛЕНИЕ, ПРИНЯТОЕ КАК СИЛЬНАЯ УЛИКА, НО НЕ КАК ЗАМЕР НОГИ. PlayMode 2 из 2. Нога прогнала
    EditMode сама (критерий 3), PlayMode — нет: Unity из Направления не запускается, а суррогат
    вместо прогона запрещён. Подпирается не зелёным, а логом FishNet в отчёте:
    «Server IP is 26.230.138.128, port is 7774» — тест поднялся на пине слота по настоящему
    невнутреннему адресу и общего 7770 не касался; и ответом живого редактора
    «IntegratedHouse ... buildIndex: 1», тогда как у сцены вне списка сборки было бы −1.
  - |
    СЛОТ ОСВОБОЖДЁН ЧЕСТНО. gascoop-slot-state.v1.json: WIN-U4 AVAILABLE, lease none. Заняты
    остаются WIN-U1 (полоса «словарь», фаза verify) и WIN-U3 (полоса «анимация», PROBA);
    WIN-U2 и WIN-U5 свободны.
state_changes:
  - |
    SET card t-scene-restore-1 status=done; ADD блок «закрытие 2026-08-28» с разбором пяти
    критериев и способом перевывода каждого; ADD запись в журнал.
  - |
    CLOSE call c-exec-g-5a7c-scene-restore-1-001 status=done с причиной и ссылкой на этот RESULT.
  - |
    SET card t-reach-wire-1 unblock_when — снята закрывшаяся половина, осталась
    «t-reactions-data-1 закрыта»; ADD запись в журнал, называющая, что именно разблокировано.
  - |
    ADD запись в журнал bet-g-5a7c-wave-6.
  - |
    ADD card i-start-points-exist-but-the-editor-shows-two-001 — гизмо и проверка стартов знают
    только первые _players, поэтому четвёртая точка невидима, а оборванная третья не будет
    названа; туда же замер про Player Start 0 под кроватью и про то, что массив стартов не
    выведен в инспектор вовсе.
  - |
    ADD card i-unsaved-house-material-edit-rides-along-001 — несохранённая правка материала дома
    (DarkWood.mat → Wood.mat) живёт в редакторе и записывается на диск любым assets-refresh.
  - |
    ADD card i-twelve-editmode-reds-come-from-the-urp-package-001 — двенадцать красных от
    отсутствующего .meta внутри пакета URP; ни одно утверждение не провалено, обходы уже сделаны
    в двух местах из трёх.
  - |
    ADD запись в журнал i-executor-paraphrase-became-the-approval — материальный ущерб починен,
    класс открыт; названо третье последствие, которого в карточке не было.
  - |
    ADD запись в журнал i-measurement-taken-in-a-scene-that-does-not-ship-001 — четвёртый случай
    класса закрыт материально: играемая сцена вернулась в список сборки.
next: |
  Полоса «починка» отработала и слот отдан. Волна двигается тремя открытыми полосами.

  Ближайшее, что разблокируется, разблокируется НЕ этой ногой: t-reach-wire-1 ждёт
  t-reactions-data-1, которая сейчас в WIN-U1 в фазе verify.

  Свободны WIN-U2, WIN-U4, WIN-U5.

END_OF_FILE: live/indie-game-development/history/2026-08-28-s-work-g-5a7c-scene-restore-close-001.md
