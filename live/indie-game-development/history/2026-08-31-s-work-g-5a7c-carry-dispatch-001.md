# RESULT — s-work-g-5a7c-carry-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-carry-back-1, полоса `руки`
track: руки
date: 2026-08-31
outcome_kind: work — живой замер на верной базе нашёл настоящую причину: распорядок не идёт, потому что дело нельзя закрыть без переноски; лекарство лежало в этой же волне невыданным и выдано

owner_approved: |
  Задача входит в девятую волну, открытую его словом 2026-08-30. Выдача задачи волны в свободный
  слот — работа писателя, отдельного слова не требует; он же и предлагал её пустить.

  Ни одна строка `done_when` не изменена; строка о живом размыкании круга **добавлена в наряд как
  проверка, а не как условие приёмки**.

outcome: |
  **ПЕРЕМЕР НА ВЕРНОЙ БАЗЕ ПОДТВЕРДИЛ ОДНО И ОПРОВЕРГ ДРУГОЕ.** Зрение действительно чинено:
  `HE SEES 2 OF 14 THINGS`, `Mug` и `Tin can` дают `SEES IT (3 of 3)`, и — что важнее числа —
  `he has actually LOOKED at 7 thing(s)`, `Mug thinks on Dining table in Kitchen (HE SAW IT
  THERE, at 63.1s)`. Прежний вывод «видит 0 из 14» мёртв.

  **НО РАСПОРЯДОК ПО-ПРЕЖНЕМУ НЕ ВЫБИРАЕТСЯ НИ РАЗУ ЗА 125 СЕКУНД**, и причина другая, чем
  думали и нога, и направление.

  **КРУГ ВИДЕН ЦЕЛИКОМ, И ЕГО ЗАМКНУЛА САМА ПОЧИНКА ЗРЕНИЯ.** Все три чайных дела закрываются
  одним набором условий — перемерено в профиле:
  `closedBy: [{Mug believedIn room:Workshop}, {Spare mug believedIn room:Workshop},
  {mark gave-up-on-tea present}]`. Хозяин увидел кружку и **верно** считает, что она на кухне.
  Взглядом условие не выполнить — **нужно отнести, а носить он не умеет.** Запасной выход
  `chore-1c` требует `when: [Mug is missing, Spare mug is missing]` — и **закрылся ровно тем, что
  он прозрел**. Пока был слеп, вещи считались потерянными и выход однажды мог сработать.

  **ПОЧИНКА ЗРЕНИЯ НЕ ОТКРЫЛА ДЕЛО, А ДОДЕЛАЛА ЕГО НЕВОЗМОЖНЫМ.** Формулировка ноги, и она точна.

  **ЛЕКАРСТВО ЛЕЖАЛО В ЭТОЙ ЖЕ ВОЛНЕ НЕВЫДАННЫМ.** `t-carry-back-1`, строка приёмки 2 дословно:
  «хозяин, взявший вещь, доходит до места, где она живёт, и кладёт её туда». Задача стояла `open`
  при трёх свободных слотах. **Выдана в WIN-U2.**

  **И ТА ЖЕ ЛОВУШКА УЖЕ БЫЛА ИЗВЕСТНА — ЧЕРЕЗ ДРУГУЮ ДВЕРЬ.** Тест-растяжка
  `NoChoreInTheShippedFileCarriesADeedItCannotFinish` (`HouseholderChoreTests.cs:482`) запрещает
  поставочному делу нести **поступок**, который оно не может закончить, — ставилась затем, чтобы
  дело не заморозило хозяина у предмета. Живой замер показал, что ловушка нашла обход: не через
  поступок, а через **условие закрытия**. Растяжка сторожила одну дверь из двух.

  **ВТОРАЯ НАХОДКА ГЛУБЖЕ И ЗАВЕДЕНА ОТДЕЛЬНО.** `HouseholderDecisionPipeline.cs:317-334`:
  распорядок приходит как `fallback` и берёт ход только если никто не предложил или его вес строго
  больше. Все дела хотят одинаково (`-1`), значит **распорядок в кандидаты не попадает вовсе**.
  Даже с закрываемыми делами хозяин проживёт утро только в промежутках, когда дел нет. Это вопрос
  конструкции, и он вынесен уликой, а не втащен в ногу.

  **НОГА ОТРАБОТАЛА ОБРАЗЦОВО ТРЕТИЙ РАЗ ПОДРЯД.** Она сама проверила базу до слияния, сама
  проверила пересборку своим приёмом (спросила у ЗАГРУЖЕННОЙ сборки два типа из слитых ног,
  получила `True/True`, `isCompiling=False`) и только потом мерила. Правок сверх слияния — ноль.

evidence: |
  Перемерено направлением в `slot/win-u4` (после слияния) и на `main` = `064ab568`:

  - профиль: все три чайных дела несут `closedBy` с `believedIn: room:Workshop` по обеим кружкам
    плюс марку `gave-up-on-tea`; `chore-1c` дополнительно несёт
    `when: [{Mug is missing}, {Spare mug is missing}]`;
  - `chore-2-the-clock-belongs-in-the-kitchen` — `closedBy: [{Alarm clock believedIn room:Kitchen}]`,
    **та же форма**, то есть тоже требует переноски;
  - `NetworkHouseholder.cs:722-728` — «HIS LEGS ARE NOT DRIVEN FROM HERE… it is not solved here at
    all… a carry that drove his feet would be a second answer to where he is going»;
  - `HouseholderChoreTests.cs:482` — растяжка существует и сторожит путь через поступок;
  - `HouseholderDecisionPipeline.cs:317-334` — распорядок как `fallback`, условие
    `fallback.DecisionWeight > selected.DecisionWeight`.

  Из возврата ноги, как улика: `HE SEES 2 OF 14`, `LOOKED at 7 thing(s)`,
  `chore-1a WON [wanted it by -1]` каждый тик `t=13.9…125.6`; тесты 744 всего, 743 зелёных,
  1 красный — тот же; `noise 5 ×2` → `noticed: 0 fact(s)`, контроль `noise 40 ×2` → 2 факта.

state_changes: |
  СОЗДАНО (1 наряд + 1 документ + 1 улика):
  - `c-exec-g-5a7c-carry-back-1-001` → слот **WIN-U2**, база `064ab568`;
  - `work/2026-08-31-call-carry-back-1.md`;
  - `i-the-routine-is-only-a-fallback-and-someone-always-wins-001` — распорядок лишь запасной
    выход, а выигрывает всегда кто-то. Маршрут `work`.

  АКТИВИРОВАНО (1):
  - `t-carry-back-1` → `active`.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-morning-content-1` остаётся `blocked` кандидатом; его карточка уже самоописывается.

captures: |
  - **ПОЧИНКА ЗАКРЫЛА ЗАПАСНОЙ ВЫХОД, И НИКТО ЭТОГО НЕ ПРЕДВИДЕЛ.** `chore-1c` работал ровно
    потому, что хозяин был слеп и считал вещи потерянными. Прозрел — выход исчез.
    **Правило-кандидат: у починки восприятия спрашивать не только «что он теперь видит», но и
    «какое условие держалось на том, что он не видел».**
  - **СТОРОЖ ОХРАНЯЛ ОДНУ ДВЕРЬ ИЗ ДВУХ.** Растяжка запрещала делу нести невыполнимый ПОСТУПОК, а
    ловушка пришла через невыполнимое УСЛОВИЕ ЗАКРЫТИЯ. Форма запрета была уже, чем класс.
  - **ЛЕКАРСТВО ЛЕЖАЛО В ВОЛНЕ НЕВЫДАННЫМ ПРИ ТРЁХ СВОБОДНЫХ СЛОТАХ.** Задача была разблокирована
    час назад и не выдана, потому что никто не связал её с симптомом. **Кандидат в правило: при
    живой находке первым делом перебрать НЕВЫДАННЫЕ задачи волны — лекарство может быть уже
    нарезано.**

decisions_needed: |
  Ни одного. Вопрос о соотношении слоёв ждёт первого живого прогона после переноски и потому не
  выносится владельцу преждевременно.

play_check: |
  1. OPEN — возврат ноги с живым замером на верной базе.
  2. WORK — база ноги и её вывод перепроверены; условия закрытия дел прочитаны в профиле;
     механизм распорядка прочитан в конвейере; невыданные задачи волны перебраны и найдено
     лекарство.
  3. (owner) — не требуется: задача его волны, выдача в свободный слот.
  4. CLOSE — RESULT, применение, коммит; наряд уходит в WIN-U2.

log: |
  живой замер на верной базе нашёл настоящую причину: зрение чинено и он видит семь вещей, но
  распорядок не идёт, потому что дело закрывается только переноской, а запасной выход закрылся
  ровно тем, что хозяин прозрел; лекарство лежало в этой же волне невыданным и выдано в WIN-U2

next: |
  Идут две ноги: подготовка полигона в WIN-U1, переноска в WIN-U2. Свободны WIN-U3 и WIN-U4.

  **Волна впервые упирается в одну задачу, после которой владелец увидит связное поведение.**
