# RESULT — s-work-g-5a7c-wave-5-dispatch-001 (play: work)

## outcome

**Три наряда выданы в три свободных слота**, и выдача нашла настоящее пересечение до того, как оно
стало конфликтом.

`t-beam-1` (WIN-U1), `t-bodies-intake-1` (WIN-U2), `t-room-look-1` (WIN-U4). `t-loot-models-1`
осознанно НЕ выдана: она берёт модели из паков, которые ещё не слиты из WIN-U3.

**Найденное пересечение и его развод.** Обе первые ноги метили в один файл —
`Assets/TunnelCrew/Settings/PresentationSettings.asset`: приёмка тел пишет туда `_raiderBody` и
`_householderBody`, а луч читал бы оттуда цвета груза. Файл отдан целиком приёмке тел, а **данные
луча уезжают в свой новый файл настроек** — этого всё равно требует его собственная строка
«расширять данными». Развод не компромисс: он делает форму лучше, чем была задумана.

## evidence

Поверхности измерены по коду на `548e6340`, а не объявлены:

- `PlaySettings.asset` — агрегат, реальные данные лежат в отдельных ассетах; `NetworkCargoPresentation`
  читает `_settings.Presentation.CargoHeldColor`, то есть `PresentationSettings.asset`, и он же несёт
  `_raiderBody` / `_householderBody`. Отсюда пересечение.
- `t-room-look-1` работает в `Art/HouseMaterials/**` и в живой сцене — с двумя другими не
  пересекается ни одним путём.
- `t-loot-models-1` работает в `GameRulesSettings.asset` и профилях визуала — не пересекается ни с
  чем выданным, но зависит от паков, которые держит `WIN-U3` с десятью неслитыми коммитами.
- Слоты перепроверены непосредственно перед выдачей: `WIN-U1`, `WIN-U2`, `WIN-U4` — `AVAILABLE`,
  `WIN-U3` — `CLAIMED` под `c-exec-g-5a7c-assetlib-1-001:build`.

## state_changes

- `card new c-exec-g-5a7c-beam-1-001` (WIN-U1), `c-exec-g-5a7c-bodies-intake-1-001` (WIN-U2),
  `c-exec-g-5a7c-room-look-1-001` (WIN-U4) — все `ready`.
- `work/2026-08-17-call-beam-1.md`, `-bodies-intake-1.md`, `-room-look-1.md` — полные тексты нарядов.
- `card set` статус `active` на трёх задачах.

## captures

Каждый наряд несёт свой блок «что спросить в Concept Lab» и правило «пока ответа нет — работать на
заглушке, ответ подменяет данные, а не код». Ни один вопрос не адресован владельцу — его прямое
требование.

Порядок приоритетов внутри нарядов расставлен так, что самое рискованное проверяется первым:
`t-bodies-intake-1` строка 2 — приёмник на заглушке ДО прихода моделей — объявлена
неоткладываемой, и наряд прямо разрешает закрыться без строки 3, если модели не успеют.

## decisions_needed

Ничего. Владельцу остаётся открыть три проекта и вставить три пасты.

## play_check

Шаг 1 ✓. Шаг 2 ✓ — владелец не спрашивается, это его требование. Шаг 3 ✓ — три `call:executor`.
Шаг 4 ✓ — правило входа ставки соблюдено: поверхности измерены и пересечение НАЙДЕНО и разведено,
а не объявлено отсутствующим. Шаг 5 ✓.

## log

три наряда выданы, и выдача нашла пересечение до того, как оно стало конфликтом

## next

`t-loot-models-1` выдаётся после слияния `assetlib`. `t-hands-1` — после возврата луча.
`t-frames-1` — последняя.
