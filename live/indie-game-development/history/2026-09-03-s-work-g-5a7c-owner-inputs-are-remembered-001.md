RESULT s-work-g-5a7c-owner-inputs-are-remembered-001 (call: — владелец-инициированная, без родительского CALL)
direction: indie-game-development   track: —   play: work   node/task: g-5a7c

outcome: |
  **ПРОВЕРЕНО ПО КОДУ, А НЕ ОБЕЩАНО: МЕХАНИЗМ НАПОМИНАНИЯ ЕСТЬ, НО ДО СЕГОДНЯ ОН БЫЛ ПУСТ.**

  Его слово 2026-09-03: «мы всё равно это должны контролировать… я хочу убедиться, что мне это
  будет предупреждать… это не значит, что мы через workflow управляем полной разработкой, но
  условно вот эти модели выполняются в другом, а мы должны контролировать всё… просто это не наша
  задача разрабатывать, но наша задача — это всё контролировать. Проверь, что это действительно мне
  будет напоминаться, дальше чтобы я не забыл.»

  **1. МЕХАНИЗМ СУЩЕСТВУЕТ И РАБОТАЕТ ТОЧНО ОДНИМ СПОСОБОМ.** `osctl.py:1080-1092`: в блок «ЖДЁТ
  СЛОВА ВЛАДЕЛЬЦА» попадают карточки вида `question` или `decision`, у которых `asks` — владелец.
  Ядро (§2 OPEN) обязывает: «Whatever context lists as waiting for the owner goes to him in plain
  words BEFORE the first play step». Плей `day`, шаг 2: «His first — before any brief: what waits
  for HIS word, and which dates are near with days left. He may defer; a deferred item stays and
  returns tomorrow.» То есть напоминание встроено и в каждую ногу, и в день.

  **2. ДО СЕГОДНЯ В НЁМ НЕ ЛЕЖАЛО НИЧЕГО — ЗАМЕР, А НЕ ПОДОЗРЕНИЕ.** `osctl context` на
  `indie-game-development` печатал «ждёт слова владельца: **ничего**». Сроки раздела 8 (клипы,
  модель, арт, звук) жили **только в документе `work/2026-09-03-demo-plan-to-october-5.md`**, а
  `work/` — это выход и улика, не состояние; ни `context`, ни `check` его не читают. Три слова
  раздела 11.1 тоже не имели карточки. **Значит на вопрос «напомнит ли» честный ответ был: нет.**

  **3. ТЕПЕРЬ НАПОМИНАЕТ, И ЭТО ПРОВЕРЕНО ЗАПУСКОМ, А НЕ РАССУЖДЕНИЕМ.** Заведены пять карточек
  `question` с `asks: владелец` и **без поля `about`** (см. п. 4, почему это важно). Повторный
  `osctl context` печатает «ЖДЁТ СЛОВА ВЛАДЕЛЬЦА (**5**)», каждая строкой «про направление».

  **4. И ЗАМЕР НАШЁЛ ДЕФЕКТ, КОТОРОГО НИКТО НЕ ИСКАЛ.** В том же коде: `if about and about not in
  chosen: continue` — вопрос с названным `about` **молча выбрасывается**, если та карточка не в
  рабочем наборе. Набор строится вокруг живой ставки. Значит **вопрос к владельцу замолкает ровно
  тогда, когда закрывается задача, к которой он привязан.**
  Живой случай: `q-core-rework-authorization-lives-only-in-the-product-001`, `asks: владелец`,
  заведён review четвёртой волны 17.08, `about: t-coreplan-1` — задача закрыта, и **вопрос не
  показывался владельцу семнадцать дней.** Его предмет не мелкий: план переработки ядра хозяина
  исполнен на десять шагов из тринадцати ногами без карточек, а три шага помечены «ворота: не
  начинать без ответа владельца».
  Заведена улика `i-an-owner-question-goes-silent-when-its-anchor-closes-001`, `route: review`,
  `level: direction`; три варианта починки названы без выбора, один из них — кандидат в
  `MAINTENANCE REQUEST`, потому что это поведение `osctl`, а не состояние направления.

  **ГРАНИЦА, КОТОРУЮ ЭТА НОГА ДЕРЖИТ.** Ни один вход не заказан и не решён за него: карточки
  говорят, что нужно, к какому дню, что встанет без него и какой запасной путь называет план.
  Разработка ассетов остаётся вне workflow — контроль остаётся здесь, ровно как он и сказал.

evidence: |
  - `osctl.py:1080-1092` — отбор в «ждёт слова владельца»: `_kind` из (`question`, `decision`),
    `asks` = владелец, и `if about and about not in chosen: continue`.
  - `os/KERNEL.md` §2 OPEN — «Whatever context lists as waiting for the owner goes to him in plain
    words BEFORE the first play step; he may defer, and a deferred item stays».
  - `os/plays/day.md` шаг 2 — «His first… what waits for HIS word, and which dates are near with
    days left».
  - `uv run --locked python osctl.py context` ДО ноги: «ждёт слова владельца: ничего».
    ПОСЛЕ: «ЖДЁТ СЛОВА ВЛАДЕЛЬЦА (5)», все пять «про направление».
  - Обход живых карточек: `question`/`decision` в направлении было ровно две, и обе с `about` на
    закрытые задачи — `q-core-rework-authorization-lives-only-in-the-product-001`
    (`about: t-coreplan-1`) и `d-where-thing-belief-files-land-001` (`about: t-bedrock-boundary-1`,
    уже решена направлением 30.08).
  - Сроки взяты из принятого плана, раздел 8 (строка 318 и соседние) и раздел 11.1; риск 2 и риск 9
    процитированы как запасные пути.

state_changes: |
  1. ADD пять карточек `question` (`asks: владелец`, `opened: 2026-09-03`, `source` → план,
     **без `about`**):
     `q-owner-input-clips-slap-and-getting-up-by-18-09-001`,
     `q-owner-input-magician-and-throw-clips-by-22-09-001`,
     `q-owner-input-second-costume-and-russian-strings-by-25-09-001`,
     `q-owner-input-art-sound-and-props-by-29-09-001`,
     `q-owner-three-words-from-plan-section-11-1-001`.
     Каждая несёт: что нужно, требования, что встанет без входа, запасной путь плана.
  2. ADD улику `i-an-owner-question-goes-silent-when-its-anchor-closes-001`
     (`level: direction`, `route: review`).
  3. Журнальная строка на узел `g-5a7c`.
  4. `q-core-rework-authorization-lives-only-in-the-product-001` НЕ трогается: перевесить её —
     значит решить за владельца, жив ли ещё её предмет спустя семнадцать дней. Она названа ему в
     ответе словами и вынесена в `decisions_needed`.
  5. План, `NOW.md`, ставка и её задачи не менялись.

captures:
  - Пять карточек — это ровно та форма, которой не хватало: `work/`-документ читают ноги, но не
    читает ни одна команда. Всё, что должно возвращаться к владельцу само, обязано быть карточкой
    `question` с `asks: владелец` и **без `about`**, иначе оно живёт ровно до закрытия якоря.
  - Ближайший срок — 18.09, до него пятнадцать дней. Заказа направление не видело ни по клипам, ни
    по модели фокусника; строка Р0 «источник модели назван и заказан» красная с 03.09.

decisions_needed:
  - q: Вопрос `q-core-rework-authorization-lives-only-in-the-product-001` молчал семнадцать дней —
      авторизация переработки ядра хозяина существует только в продуктовом отчёте, а не в твоих
      словах. Жив ли ещё его предмет?
    options: [ответить сейчас; отдать review 11.09 вместе с уликой про молчащий фильтр]
    recommendation: review 11.09 — предмет старый, а фильтр всё равно чинится там же.
  - q: Заказ клипов шлепка и подъёма — ближайший срок 18.09, пятнадцать дней.
    options: [заказать на этой неделе; решить на сверке 11.09]
    recommendation: заказать на этой неделе — вход не наш, а запасной путь стоит денег.

play_check:
  - 1 recite: done — работа названа его словами: убедиться, что входы вне workflow будут ему
    напоминаться, и что контроль остаётся в направлении, хотя разработка идёт вне его.
  - 2 owner inputs (owner): done — его слова процитированы дословно. Ни один вход не заказан и не
    решён за него; три слова раздела 11.1 внесены как вопрос, а не как ответ.
  - 3 do the work: done — механизм прочитан по коду, пустота замерена запуском, пять карточек
    заведены, работа доказана повторным запуском той же команды.
  - 4 self-check: done — проверка сделана командой, а не рассуждением: «ничего» до, «(5)» после.
    Найденный дефект фильтра назван уликой, а не починен молча; чужая карточка не перевешена.
  - 5 close: done — `close: light — because` каждая строка выведена из строк `osctl.py`, из текста
    ядра и плея `day`, из вывода `osctl context` до и после, и из его собственных слов.
    G5-чат не открывается.

log: проверено по коду и запуском: механизм напоминания о входах вне workflow существует — карточки question с asks владелец идут в «ждёт слова владельца» и по ядру выносятся ему ДО первого шага плея, а плей day ставит их и близкие даты первым шагом; но до сегодня блок был пуст, сроки раздела 8 жили только в work-документе, который не читает ни одна команда, и на вопрос «напомнит ли» честный ответ был «нет»; заведены пять карточек (18.09 клипы, 22.09 модель и бросок, 25.09 костюм и русские строки, 29.09 арт-звук-реквизит, три слова 11.1), повторный context печатает пять вместо «ничего»; тем же замером найден дефект — вопрос владельцу молча замолкает, когда закрывается карточка его about, и один настоящий вопрос так молчал семнадцать дней

next: |
  Напоминание работает: пять карточек стоят в «ждёт слова владельца» и будут первым, что видит
  любая нога и любой день, пока он их не закроет или не отложит.
  Ближайший срок — 18.09 (клипы шлепка и подъёма), пятнадцать дней.
  Дефект молчащего фильтра и вопрос про авторизацию ядра — на сверку 11.09.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-work-g-5a7c-owner-inputs-are-remembered-001.md
