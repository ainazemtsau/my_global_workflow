# RESULT s-repair-g-37a1-simple-gas-recut-001

call: owner decision session (continuation of the confirmed ESCALATE consumed by s-repair-g-37a1-gas-rest-band-scope-001)
direction: indie-game-development · track: t-sim · play: repair · node/task: g-37a1/t-6
date: 2026-07-30

## outcome

t-6 сменила ПРЕДМЕТ работы, а не размер. Ремонт существующего закона газа снят как маршрут; рядом
пишется новый простой закон без накопителя остатка.

Владелец назвал причину сам, и она оказалась вернее моего предыдущего предложения: существующий газ
написан под прошлый концепт, и мы весь день ремонтировали не тот предмет. Мой разрез, принесённый
часом раньше, оставлял работу ремонтом старого закона — он был не лучшей из доступных идей, и это
записано здесь прямо.

Диагноз сведён к ОДНОЙ причине: накопитель остатка. Он делал закон немонотонным; из него выросли мера
`Φ`, потолочные границы, предпосылки, документ доказательства на 345 строк и оба сегодняшних блокера.
Новый закон его не имеет, поэтому остановка следует из строго убывающей суммы квадратов и доказывается
одним абзацем.

Выпущен один ready корень `c-exec-g-37a1-simple-gas-rest-001`: НОВЫЙ change_id, слот WIN-U1, пин 34,
стадия PLAN, реестр около двенадцати рядов вместо сорока девяти. Устаревший
`c-exec-g-37a1-gas-rest-band-scope-001` снят с видимым баннером и сохранён как улика. Пакет
`c-exec-g-37a1-gas-rest-and-checksum-001` и его лиза в WIN-U4 не тронуты.

Bet, appetite, kill_by, cut_list, lens_verdicts, остальные десять задач, пять полос и WIP не менялись.
Карточка g-37a1 и её пятнадцать подписанных критериев не тронуты.

## evidence

- **Слова владельца, дословно, в этом чате 2026-07-30:** «у нас, ну типа условно, так сказать,
  Лего-симуляция газа. То есть мы делали под один концепт, сейчас другой»; «она там ещё хочет починить
  текущий газ. То есть такого быть не может»; выбор пути — «2»; утверждение точного экрана
  проектного решения — «Да».
- **Проверка, что резать можно:** `TREE.md` g-37a1 несёт пятнадцать критериев, подписанных дословно
  многократно. Сверка `done_when` прежней t-6 с ними показала, что к подписанным критериям тянутся
  только сохранение массы (критерий 10) и работа по сети (критерий 11). Покой, теорема сходимости,
  `Φ`, инкрементальная сверка, кривая мёртвой зоны и «половина разницы» в критериях владельца
  ОТСУТСТВУЮТ — это инженерное разбиение ноги планирования. Поэтому перенос и резы не трогают
  подписанное содержание и не требуют маршрута map/frame.
- **Количественное подтверждение слов владельца:** из 49 обязательств замороженного плана примерно 15
  существовали только ради охраны старой машинерии — односторонние клапаны (`T6-14`/`T6-14N`), старая
  контрольная сумма (`T6-08`/`T6-08N`), переезд трёх живых вызовов и переизвод оракула l1b (`T6-12`),
  переиздание 70 литералов в 9 файлах (`T6-10`), доказательство отмены старых строк (`T6-11`),
  половинный зажим (`T6-03`/`T6-03N`). Ни одно не приближало «газ остановился».
- **Рез половинного зажима обоснован самой замороженной спецификацией**, а не моим мнением: она пишет,
  что требование «INERT at every registry the type registry admits», «Under `Θ` it is doubly
  redundant», и что держится оно «because `done_when` 2 names it».
- **Обоснование остановки нового закона проверено арифметически:** при переносе `q` из большей клетки
  в меньшую с разницей `d` изменение суммы квадратов равно `2q(q − d)`; при `q ≤ d/2` и `q ≥ 1` оно
  строго отрицательно. Сумма квадратов — целое неотрицательное, поэтому убывать бесконечно не может.
  Последовательный обход снимает одновременность, из-за которой старому доказательству нужна была
  отдельная гипотеза.
- **Слот выбран фактом, а не агентом.** `tools/select-slot.ps1` прогнан read-only по всем четырём
  слотам 2026-07-30: WIN-U1 — `CLEAN / AVAILABLE / lease none`; WIN-U2 — CLAIMED
  `c-exec-g-37a1-body-first-person-001:PAIR-CANDIDATE`; WIN-U3 — CLAIMED
  `c-exec-g-37a1-venue-packaged-player-001:PAIR-CANDIDATE`; WIN-U4 — CLAIMED
  `c-exec-g-37a1-gas-rest-and-checksum-001:PAIR-CANDIDATE`. Свободен ровно один, поэтому это
  вынужденный слот того же класса, что прежний вынужденный WIN-U4, а не выбор среди равных.
- **Пин 34 обоснован:** параллельная нога `b300f209` консолидировала контур, и WIN-U4
  `validation.config` показывает `synced_contract_version: 34`; `CONTRACT_VERSION` предписывает новым
  корням текущий пин.
- **Почему снятый наряд мёртв дважды:** по существу — он был последним кругом ремонта старого закона;
  механически — выпущен на HEAD `6d5879b3` и штампе 33, а WIN-U4 в течение часа переехал на
  `c8d1e45f` и 34, из-за чего его абзац о границе версий и выбранная форма жизненного цикла стали
  неверны. Никогда не отправлялся.
- **Параллельные ноги этого дня учтены:** `a9d5ee61` (t-venue) и `b300f209` (product contour)
  приземлились во время работы; их дельты по t-venue, issues и `updated:` сохранены, дельта этой ноги —
  только t-6, t-7.unblock_when, forecast, t-sim call и два новых issue.

## state_changes

1. `NOW.md` t-6: `goal` и `done_when` переписаны под новый простой закон — состояние, такт из четырёх
   шагов с последовательным обходом, покой, `Θ = 12` одним числом, отсутствие накопителя, сохранение
   массы, побитовая одинаковость, обязательный провал закона с накопителем, двойник на каждое
   утверждение, запрет открывать старую машинерию, принятая неточность, запрет писать математический
   документ. `status: active` сохранён.
2. `NOW.md` t-7 `unblock_when`: инкрементальная сверка снята из условия разблокировки с указанием, куда
   она уехала.
3. `NOW.md` open_calls: `c-exec-g-37a1-gas-rest-band-scope-001` снят, вместо него ready
   `c-exec-g-37a1-simple-gas-rest-001` (t-sim, for t-6, executor).
4. Новый файл `work/c-exec-g-37a1-simple-gas-rest-001-call.md` — самодостаточный root CALL: пин 34,
   слот WIN-U1 с обоснованием вынужденности, новый change_id, утверждённый экран как задание, шесть
   пунктов `done_when`, список того, что переиспользуется, и список того, что нельзя открывать.
5. `work/c-exec-g-37a1-gas-rest-band-scope-001-call.md`: видимый баннер RETIRED сверху, байты
   исходного текста сохранены.
6. `NOW.md` issues: добавлен `i-gas-rest-deferrals-after-simple-law-001` — три переноса и три реза с
   их триггерами и с записью, что ни один не назван в подписанных критериях.
7. `NOW.md` issues: добавлен `i-gas-rest-and-checksum-root-open-001` — старый продуктовый корень
   остаётся ACTIVE и держит лизу WIN-U4; закрытие или парковку выбирает владелец, агент не закрывает.
8. `NOW.md` issues: `i-g37-cond-zero-release-deferred-to-t7-001` и
   `i-conductivity-snapshot-vs-live-checksum-001` перенаправлены — оба долга относятся к СТАРОМУ
   закону, для нового закона они не возникают (нет накопителя — нечего терять), и это проверяется, а
   не наследуется по инерции.
9. `NOW.md` forecast: `bet.forecast` и строка драйвера t-6 переписаны на смену предмета; прямо
   записано, что продуктового кода по t-6 ноль.
10. `NOW.md` `updated:` → `s-repair-g-37a1-simple-gas-recut-001`. `LOG.md`: одна строка. Эта запись —
    в `history/`.

Не менялось: `CHARTER.md`, `TREE.md` и карточка g-37a1 с пятнадцатью критериями, bet/appetite/kill_by/
cut_list/lens_verdicts, задачи t-1…t-5 и t-7…t-11, пять полос, `track_wip_limit: 5`, `recurring`,
`decisions`, четыре остальных open_call, продуктовые репозитории и любые замороженные объекты и лизы.

## captures

- Проигравший подход этой ноги: разрез t-6 с сохранением ремонта старого закона (принесён владельцу
  часом раньше и отклонён в пользу пути 2). Сохранён как проигравший вариант, а не как план.
- Наблюдение для будущих ног: три из трёх сегодняшних провалов t-6 упираются в одну структуру данных
  (накопитель остатка). Когда ремонт трижды спотыкается о один и тот же элемент, дешевле заменить
  элемент, чем доказывать его корректность.

## decisions_needed

Один, не блокирующий эту ногу: старый корень `c-exec-g-37a1-gas-rest-and-checksum-001` остаётся ACTIVE
и держит лизу WIN-U4. Закрыть его или припарковать — выбор владельца, он записан как
`i-gas-rest-and-checksum-root-open-001` и показывается на следующей ноге t-sim.

## play_check

- Шаг 1: противоречие названо — t-6 направляла работу на ремонт предмета, который владелец объявил
  неверным, и её `done_when` несла десять обязательств против правила ядра «задача ≤ полдня».
- Шаг 2: реконструкция по артефактам и коду; критерии карточки прочитаны, чтобы отделить подписанное
  владельцем от инженерного разбиения; слоты и контракт прочитаны инструментом, а не предположены.
- Шаг 3: каждое изменение с одной причиной; ни одно обязательство не исчезло — три переноса и три реза
  записаны issue с триггерами.
- Шаг 4: владелец утвердил дословно — «2» на путь и «Да» на точный экран. Removal boundary соблюдён:
  снятие open_call названо ему по id в предыдущем сообщении и по существу в утверждённом списке
  действий; карточку и подписанные критерии нога не трогала.
- Шаг 5: FRICTION в этой ноге не добавлялся — класс уже записан строкой этого же дня.
- G3 соблюдён: appetite не продлевался. G6 не применяется: это не shape, бет уже активен. G9 не
  задет: `CHARTER`/`TREE` не менялись. G5 не применяется: нога ничего не закрывает как done.
- Честно зафиксировано: обещанный владельцу «ряд-растяжка» на неизменность проводимости был неверной
  идеей (зелёный с самого начала ряд сломал бы стадию красных тестов), и в новом маршруте он не нужен
  вовсе, потому что охранять старую машинерию больше не требуется.

## log

2026-07-30 | s-repair-g-37a1-simple-gas-recut-001 | repair | t-sim | g-37a1/t-6: владелец назвал предмет неверным («делали под один концепт, сейчас другой»; «починить текущий газ — такого быть не может») и выбрал путь 2, поэтому t-6 переписана с РЕМОНТА старого закона на новый простой закон БЕЗ накопителя остатка, где остановка следует из строго убывающей суммы квадратов и доказывается абзацем вместо документа на 345 строк; сверка с пятнадцатью подписанными критериями показала, что покой, теорема, Φ и инкрементальная сверка владельцем не подписаны, а примерно 15 из 49 обязательств охраняли только старую машинерию; выпущен ready корень на вынужденно свободный WIN-U1 с пином 34 и реестром около двенадцати рядов, устаревший наряд снят баннером, старый пакет и его лиза не тронуты

## next

Диспетчеризовать `c-exec-g-37a1-simple-gas-rest-001` отдельной свежей сессией в
`C:\projects\Unity\GasCoopGame_win-u1`. Это стадия PLAN: короткий человеческий план на утверждение
владельцем, затем отдельными сессиями носитель и красные тесты, обязательное свежее опровержение,
затем BUILD. Направление — единственный, кто выдаёт наряд-преемник.

END_OF_FILE: live/indie-game-development/history/2026-07-30-s-repair-g-37a1-simple-gas-recut-001.md
