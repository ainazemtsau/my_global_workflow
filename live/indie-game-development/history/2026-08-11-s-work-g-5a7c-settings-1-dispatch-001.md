# RESULT — s-work-g-5a7c-settings-1-dispatch-001

play: work · выдача наряда на разбор коробки настроек в освободившуюся полосу
direction: indie-game-development · node: g-5a7c · task: t-settings-1
date: 2026-08-11

base_read: OS — `wt/indie-game-development`, дерево чисто. Продукт перемерен `git ls-remote origin`:
`origin/dev` = `origin/main` = `6599ae87e5435b709f4b382becb16a018a5ae695`. Слот WIN-U1 перемерен
настоящим селектором: `CLEAN / AVAILABLE / lease none`, HEAD знак в знак базис. Все четыре слота
свободны — ни один наряд сейчас не в работе.

## outcome

Выпущен наряд `c-exec-g-5a7c-settings-1-001` в WIN-U1 на разбор коробки настроек. Задача пошла в
полосу, освободившуюся после закрытия `cargo-hands`, и идёт ПАРАЛЛЕЛЬНО живой полосе хозяина.

**Право на параллель не выдано на глаз, а измерено.** Прежняя редакция карточки объявляла задачу
шовной и требовала пустых полос — это правило написала предыдущая нога, когда работали ОБЕ полосы.
Полоса груза кончилась, основание отпало, и вместо ритуала поставлена проверка пересечения по
файлам: наряд `c-exec-g-5a7c-react-2-001` прямым текстом запрещает себе
`Network/NetworkPlaySettings.cs` и `.asset` и обязан вернуть `IntegratedHouse.unity` байт в байт.
Пересечения нет ни одного. Правило заменено, а не отменено: если полоса хозяина когда-нибудь
потребует эти файлы, пересечение возвращается и задача снова становится шовной.

## evidence — перемерено первой рукой на `6599ae87`

- **56 сериализованных полей** в `NetworkPlaySettings` (было 50 до `t-cargo-2`). Сеть среди них —
  ДВА поля: `_defaultAddress`, `_port`.
- **Двенадцать читателей ассета**, все в Unity-слое; `Core/**` его не видит вовсе и получает готовый
  `CargoRules`. Стена, держащая мультиплеер, не при чём — сломана раскладка, а не устройство.
- **ДВЕ ЛОВУШКИ НАЙДЕНЫ ЗАРАНЕЕ, и обе противоречат имени поля.** Это главная ценность наряда:
  сортировка по звучанию имени дала бы неверный результат минимум дважды.
  - `_eyeHeight` звучит как камера, но `CreateCargoRules()` кладёт его в `pickupEyeHeight`, оттуда
    `AuthoritativeCargoRoster.cs:643` — начало луча захвата. Правда игры.
  - `_lookPitchLimit` был личным ВЧЕРА и перестал быть сегодня: `NetworkWalkerInput.cs:141` зажимает
    им наклон прицела, а после `t-cargo-2` наклон прицела задаёт высоту груза. Разный предел = разная
    досягаемость. Правда игры.
  - `_lookSensitivity` — единственный читатель `NetworkWalkerInput.cs:137`, действительно личное.
- **`StartConditions` уже стоит в сцене** со своим редактором, и его собственный комментарий прямо
  говорит «где начинается груз, остаётся в `NetworkPlaySettings`». Это ровно та строка, которую
  задача переписывает.
- **Группировка инспектора УЖЕ есть** — десять блоков `[Header]`, заведённых `t-cargo-2`, плюс
  отданный владельцу одностраничный список ручек. Понятность наполовину куплена, и наряд прямо
  запрещает её переписывать: остаётся структурная половина.
- **Побайтовый гейт обоснован прецедентом, а не осторожностью:** в `t-situation-1` переименование
  сериализованного поля едва не обнулило `landingLifetimeSeconds: 8`, и поймал это только заранее
  поставленный гейт.

**Названо в наряде как чужая забота:** оба наряда полосы хозяина стоят на базе `2f7f2e87`, то есть
на 22 коммита позади опубликованной головы, и WIN-U2 тоже. Перебазирует их их собственная полоса;
эта задача их не трогает.

## state_changes

1. `t-settings-1`: блоки `done_when` и `note` переписаны — свежие числа (56 полей вместо 50), две
   найденные ловушки вписаны поимённо, а правило «только при пустых полосах» заменено проверкой
   пересечения по файлам с названным основанием отмены.
2. Заведена карточка наряда `c-exec-g-5a7c-settings-1-001` (`to: executor`, `for: t-settings-1`,
   `slot: WIN-U1`, `basis: 6599ae87`, `status: ready`).
3. Написан текст наряда `work/c-exec-g-5a7c-settings-1-001-call.md`.
4. `t-settings-1` остаётся `open`: наряд не выдан, пока нет расписки.
5. `NOW.md`, `g-5a7c` и текст ставки не трогать.

## captures

- Оба наряда полосы хозяина (`react-2`, `react-mem`) стоят на устаревшей базе `2f7f2e87` при
  опубликованной `6599ae87`, и оба в статусе `ready` в ОДНОЙ полосе. Второе — вопрос к G1 (≤1
  обычный корень на полосу) и к их собственной сессии, не к этой ноге. Названо владельцу.
- Полоса `cargo-hands` пуста; её роспуск принадлежит `review` ставки.

## decisions_needed

Нет. Запуск параллельно — его слово 2026-08-11 после показанного измерения пересечений.

## play_check

- 1 recite: done — шесть строк `t-settings-1` и правило слоёв сведены
- 2 owner inputs (owner): done — задача заведена его словами, её содержание им же и найдено
- 3 do the work: done — наряд по контракту 36; 56 полей, двенадцать читателей и две ловушки
  перемерены в байтах
- 4 self-check: done — собственное прежнее правило про пустые полосы отменено с названным
  основанием, а не проигнорировано
- 5 close: checkpoint — расписка исполнителя вне этой ноги

## Гейты

- **G1.** Один корень в свободной полосе; полоса хозяина ведётся своей сессией. Лимит два не
  превышен.
- **G2.** Задача служит активной ставке.
- **G3.** Аппетит не тронут: задача уже была в волне, новая не заводилась.
- **G5.** Задача не закрывается. Её строки проверяемы по байтам, поэтому светлое закрытие ей
  доступно — это названо в наряде заранее.
- **G7.** Ничего продуктового не решено: разбор структурный, значения неприкосновенны.

## log

наряд на разбор коробки настроек выдан в WIN-U1 от свежей головы 6599ae87 и пошёл параллельно живой полосе хозяина — право на параллель не выдано на глаз, а измерено: наряд хозяина прямым текстом запрещает себе NetworkPlaySettings и обязан вернуть сцену байт в байт, поэтому прежнее правило «только при пустых полосах» отменено с названным основанием и заменено проверкой пересечения по файлам; главной ценностью наряда стали две найденные заранее ловушки, где имя поля врёт про его вид — _eyeHeight звучит как камера, но читается судьёй в луче захвата, а _lookPitchLimit был личным вчера и перестал быть сегодня, потому что после подъёма наклон прицела задаёт высоту груза; побайтовая сверка всех 56 значений поставлена главным гейтом по прецеденту, а не из осторожности

## next

1. Владелец отдаёт наряд исполнителю; первая строка возврата — расписка.
2. Полоса хозяина продолжается своей сессией, и ей нужно перебазировать оба наряда на `6599ae87`.
3. После обоих возвратов остаются `t-react-3` и закрытие ставки через `review` свежим чатом.
