# RESULT — s-work-g-1d84-host-into-scene-close-001

call: c-exec-g-1d84-host-into-one-scene-001
direction: indie-game-development
track: сцена
play: work
node/task: g-1d84 / t-scene-2
date: 2026-08-05
owner_launch: «Б» + «Я выбрал, что пока ничего не менять, пусть хотя ходит через стены» + «сначала, да, закрывать задачу».
close: light — because три строки `done_when` из четырёх эта нога перемеряла первой рукой по байтам
опубликованного `2eee9719` (включая независимый пересчёт геометрии маршрута и перепрогон ворот), а
четвёртую закрывает УЖЕ ЗАКОММИЧЕННОЕ дословное слово владельца об этом самом артефакте — коммит
`adae7d40`, §«Слово владельца» отчёта. Суждения, которого никто не выносил, не осталось.
downgrade: наряд требовал отдельную свежую биндинговую проверку по пункту 4, и она заменена светлым
закрытием. Основание — `os/plays/work.md` §Light close: «An already-issued fresh-G5 CALL may be
replaced by a light close when the same test holds and the RESULT records the downgrade». Тест держит,
и понижение записано здесь.

## outcome

`t-scene-2` закрыта. **Впервые в проекте хозяин, двое сетевых носильщиков и физический груз
существуют в одной запускаемой сцене одновременно.** Заодно снят рантайм-дом: дом лежит в файле сцены
сохранённым корнем и виден в редакторе без нажатия Play.

ТРЕТЬЯ ЗАДАЧА ВОЛНЫ ЗАКРЫТА, то есть наступила точка пересмотра аппетита — она поставлена на событие,
и событие произошло. Заведена решением `d-g-1d84-appetite-review-after-three-001`; нога её не решает.

## evidence

**Публикация и её целостность.** `git ls-remote` — `origin/main` = `origin/dev` = `2eee9719`. Цепочка:
`b7ae1fa4` → `55c6825f` (сама работа) → `7318a2dc` (отчёт) → `adae7d40` (слово владельца) →
`09859a7a` (слияние) → `2eee9719` (`DELIVERED on dev`). `WIN-U1` в реестре `AVAILABLE`, `lease: none`.

**ГЛАВНАЯ ПРОВЕРКА ЭТОЙ НОГИ: опубликовано РОВНО то, что он запускал.** Шесть runtime-файлов сверены
по хешу между `adae7d40` (состояние на момент его слов) и `2eee9719` (опубликованное):
`IntegratedHouse.unity`, `Householder.prefab`, `CarryStand.cs`, `NetworkPlaySettings.cs`,
`NetworkPlaySettings.asset`, `ThreeRoomHouse.prefab` — **SAME все шесть**. Полный дифф между этими
коммитами — одна строка в тексте отчёта (переставленный статус). Слияние ничего не изменило.

**Строка 1 — хозяин и двое носильщиков с грузом одновременно, хозяин ходит своим кругом.**
В `IntegratedHouse.unity` восемь корней сцены; среди них `PrefabInstance` заготовки `Householder`
(guid `4b7e1c93a0d8f24ab9c65e0173f8d2a6`) и `PrefabInstance` `NetworkRuntime`, который и несёт
носильщиков с грузом. Плюс его слово после прохода по шагам приёмки.

**Строка 2 — отдельные корни, стабильные идентификаторы, скрытых ссылок нет.** Все 33 записи
переопределений во всех трёх `PrefabInstance` имеют `objectReference: {fileID: 0}` — непустых ссылок
на объекты сцены нет ни одной. Единственная межкорневая ссылка — `_house` на `CarryStand`, и она
явное поле инспектора, одностороннее: указывает на `{fileID: 1441002392}`, а это stripped Transform
инстанса `&1441002391`, то есть корень `ThreeRoomHouse` (guid `3ddac42b85f64bb688681e93d398e573`).
Проверено адресно, а не по описанию. Тоннелей в проекте нет вовсе — эта часть строки пуста, а не
нарушена. ЧЕМ ИМЕННО доказывается «скрытых ссылок нет» — строки W06/W06a, и наряд их закрывать
ЗАПРЕЩАЕТ; они остаются открытыми к PLAN, и это не невыполненная строка, а названное исключение.

**Строка 3 — корень дома в файле сцены.** `CarryStand` больше не инстанцирует ничего:
`Instantiate`/`OnDestroy` удалены, поле `_settings` заменено на `[SerializeField] Transform _house`,
добавлена явная ошибка в лог при незаданной ссылке. Мёртвое `_housePrefab` вынуто и из `.cs`, и из
`.asset`: `git grep HousePrefab` по всему `Assets/` не даёт НИ ОДНОГО попадания — второго ответа на
вопрос «откуда берётся дом» больше не существует. Проверено отдельно, что переименование поля не
сломало ничего снаружи: прогон по всем `.unity` и `.prefab` проекта показал, что `CarryStand` живёт
ровно в одной сцене — этой.

**Строка 4 — его слова.** Дословно, коммит `adae7d40` и §«Слово владельца» отчёта на `2eee9719`:
**«провреил все работает как и ожидаеться»** (орфография его, записана как есть). Сказано после
прохода по шагам `Manual-acceptance`, шаг 4 которых — «одновременно в кадре капсула хозяина, идущая
своим маршрутом, и двое сетевых носильщиков с грузом». Сегодня подтверждено ещё раз: «слот с этой
задачей закрыт… эту задачу закрыли».

**Ворота перепрогнаны этой ногой на опубликованном `2eee9719`:** `dotnet test` — 63/63, пропущено 0;
`tools/check.ps1` — `hygiene OK`, `OK: all active gates green`.

**Чего в доказательствах НЕТ, и это названо.** Unity-сборку C# ни один агент не проверял: правки лежат
в `Network/**`, а `dotnet` собирает только engine-free ядро. Исполнитель ОТКАЗАЛСЯ пользоваться
подключённым MCP-эндпоинтом, потому что тот не принадлежал `WIN-U1` (его сцена отдавала 5 корней
против 6 в коммите слота) — это правильное поведение по правилу «инструмент недоступен → стоп, а не
обход», а не халтура, и это стоит записать в его пользу. Венью компиляции — запуск владельца, и он
состоялся. Класс прежний: `i-unity-evidence-rests-on-owner-launch-001`.

## хождение сквозь стены — пересчитано, и это решение владельца

Дом: перегородки при x = ∓2.5 (толщина 0.5), парами по z ∈ [1; 7] и z ∈ [−7; −1]; проёмы — z ∈ (−1; 1).
Маршрут хозяина: РТ01 (−3.5; −2.5), РТ02 (3.5; −2.5), РТ03 (5.11; 5.58), РТ04 (−3.5; 2.5).

- РТ01 → РТ02 идёт по прямой z = −2.5 и пересекает x = −2.5 и x = +2.5 внутри южных перегородок → две стены.
- РТ03 → РТ04: пересекает x = +2.5 при z ≈ 4.65 и x = −2.5 при z ≈ 2.86, обе внутри северных → ещё две.
- Проёмы не используются ни разу. Итого четыре прохода сквозь стену за круг. Маршрут — челнок, не круг.

Отчёт исполнителя это назвал сам и точки НЕ переставлял сознательно, вернув решение домой — правильно.
Владельцу показаны оба варианта с ценой; он выбрал **Б**: «пусть хотя ходит через стены». Заведено
`i-host-walks-through-walls-001` входом в `t-host-2`; вопрос «чинить ли сейчас» закрыт его словом.

## state_changes

- `NOW.md`: `t-scene-2` → `done` + `closed`; `t-scene-3` → `open` (снят `unblock_when`); `t-host-1` →
  `open` + `note` (наряда нет, прежний снят с фронтира навсегда); `updated` → эта сессия.
- `NOW.md/open_calls`: удалить вернувшийся `c-exec-g-1d84-host-into-one-scene-001` И мёртвый
  `c-exec-g-1d84-host-under-the-judge-001` (никогда не запускался, база `839df47e` мертва, по
  собственному тексту перевыпускается свежим; файл наряда с разбором остаётся на диске).
  `c-exec-g-1d84-free-kits-probe-001` сохранить как есть.
- `NOW.md/issues`: удалить `i-house-root-runtime-only-not-serialized-001` — закрыта по существу,
  дом лежит в файле сцены, проверено байтами; завести `i-host-walks-through-walls-001` с его вердиктом.
- `NOW.md/decisions`: завести `d-g-1d84-appetite-review-after-three-001` — точка пересмотра аппетита
  наступила по событию.
- Препендить ровно один LOG receipt и сохранить этот полный RESULT в `history/`.
- CHARTER/TREE/knowledge, продуктовый репозиторий, реестр слотов и ветки не менять.

## captures

- Временный дубль описания хозяина: `HostWalksHisDay.unity` и новая заготовка `Householder.prefab`
  описывают одного и того же хозяина. Сцену наряд удалять запрещал. Не запись — станет ею, если кто-то
  начнёт править хозяина и разъедутся две правды.

## decisions_needed

`d-g-1d84-appetite-review-after-three-001` — точка пересмотра аппетита. Его слово, не ноги.

## play_check

- Шаги 1-5 плея `work` исполнены; шаг 2 нового входа не требовал — вердикт владельца уже закоммичен.
- Понижение свежей биндинговой проверки до light close записано явно (см. `downgrade` в шапке).
- G7: ни одного продуктового или геймплейного решения за владельца. Выбор Б принят как решение и в
  наряды поедет запретом трогать маршрут, а не смягчённым «надо бы всё-таки». Точка пересмотра
  аппетита вынесена ЕМУ с фактами и рекомендацией, а не решена ногой.
- G1/G2: полос пять, WIP-предел 3 не поднят; разблокированные задачи нарядов не имеют и диспатчем не
  являются. Ставка не закрыта — `review` по `g-1d84` не наступил.
- Прогноз направления остаётся `no_basis`: четыре закрытые задачи эмпирическим классом не являются,
  дата не выводится (его запрет на даты 2026-08-02 действует).

## next

Разблокированы `t-scene-3` (панель условий, order 8) и `t-host-1` (хозяин под судьёй, order 7);
нарядов нет ни на одну, у `t-host-1` он пишется ЗАНОВО на базе `2eee9719`. Полоса «дом» открыта
четырьмя задачами с order 5, 6, 9, 13 — тоже без нарядов. Ближайший по порядку — `t-house-2` (order 5).
Диспатчеризуемых нарядов у направления сейчас НОЛЬ. Перед любым следующим нарядом — точка пересмотра
аппетита, потому что её ответ решает, ту ли нарезку мы вообще продолжаем.

END_OF_FILE: live/indie-game-development/history/2026-08-05-s-work-g-1d84-host-into-scene-close-001.md
