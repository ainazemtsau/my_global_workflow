# RESULT — s-work-g-5a7c-telekinesis-call-001

play: work
direction: indie-game-development · node: g-5a7c · task: t-scale-5 · call: c-work-g-5a7c-telekinesis-call-001
date: 2026-08-08
base_read: OS `9051b580`, дерево `wt/indie-game-development` чисто на входе. Продукт читался на
`ae023ba5`. Ни один продуктовый байт этой ногой не изменён.

## outcome

Существует отправляемый наряд исполнителю на телекинез лучом:
`c-exec-g-5a7c-telekinesis-001`, бриф — `work/c-exec-g-5a7c-telekinesis-001-call.md`. Проверка
происхождения снова дала находку: **одна из четырёх несущих строк задачи снята**, потому что она не
требование владельца. И перемер дал факт, которого не называл никто: **создатель игры не знает,
куда игрок смотрит по вертикали**, а без этого строка про прицел невыполнима.

## evidence

### Проверка происхождения — четыре строки, одна снята

| Строка задачи `t-scale-5` | Откуда | Что сделано |
|---|---|---|
| «предмет выбирается прицелом И близостью» | `W121`, его вердикт дословно: **«Прицелом и близостью НО я думаю что потом можно будет дальность прокачивать»**. Первая половина без оговорок | Оставлена, как одно правило с двумя условиями |
| «берёт груз, не подходя вплотную, и ведёт его» | Подписанный критерий 3 карточки узла | Оставлена |
| «закрывает его слово, управляемо это или нет» | Порог смерти ставки (`kill_by` 3), его | Оставлена |
| «длина луча стала переменной на держателя вместо общего смещения» | `W26` — и та же строка помечена дословно **«ЕГО ВХОД… требованием НЕ является»**; его слова смягчены с обеих сторон: «**я думаю** что **потом** можно будет дальность прокачивать» | **СНЯТА** |

**Почему снята, двумя независимыми основаниями.** Первое: разбор сам пометил её как ВХОД, а не
требование, и обе смягчающие оговорки принадлежат владельцу. Второе: подписанный аппетит покупает
**два** шва — класс груза и личная мощность хвата, — и оба уже стоят в `t-scale-7`. Переменная
дальность на держателя была бы ТРЕТЬИМ швом, которого он не заказывал. Строка ушла в captures,
не в наряд.

### Перемерено первой рукой на `ae023ba5`

| Факт | Чем подтверждён |
|---|---|
| **Сервер не знает вертикального прицела** | У ходока есть только `Yaw`; вся работа с направлением идёт через `PlanarYaw` — имя честное, оно плоское. Наклон живёт на клиенте: `Network/NetworkWalkerInput.cs:38` `PitchDegrees` кормит камеру. Клиент шлёт `Core/Movement/MovementInput`: `AxisX`, `AxisZ`, `DeltaSeconds` — и всё |
| Выбор предмета сегодня — близость | `Core/Cargo/AuthoritativeCargoRoster.TryPickUp`: точка руки выносится вперёд по курсу (`HandPoint(rules.CarryOffset)`), затем ищется ближайший груз с расстоянием до ПОВЕРХНОСТИ ≤ `PickupRange` |
| **«На расстоянии» сегодня = почти коснуться** | В ростах налётчика: взятие `0.104` = **0.65 роста**, вынос руки `0.104` = 0.65, длина хвата `0.256` = 1.6, вылет `0.016` = 0.1. Груз 1.2 × 0.25 × 0.25 роста |
| Пружина и потолок на руку остаются | `Core/Cargo/CargoHaul.cs:109` усилие = max(0, растяжение − вылет) × жёсткость; потолок НА РУКУ `:112`; сумма по рукам `:36-49`; демпфирование один раз на сумму `:51-55` |
| Шов «точка укуса не у руки» уже стоит | `CargoHaul.AtPoint` (`:115-156`); `Core/Cargo/CargoHold.cs:48-51` хранит хват в системе груза |

**Поправка к прежним основаниям:** разбор `W26` утверждал, что дальности «от восьми до двадцати
ростов». Это было измерено на `352f96b0`, ДО перевода масштаба. После `t-scale-1` все они меньше
одного роста. Исправлено в наряде; старое основание названо устаревшим, а не тихо переписано.

**Находка, которую стоит унести отдельно.** Строка «выбирается прицелом» выглядела как настройка
правила выбора, а на деле требует, чтобы направление взгляда доехало до создателя игры. При росте
0.16 это не мелочь: коробка 4 см на полу при глазах 13.6 см и расстоянии 10 см — это взгляд вниз
примерно под 50°, то есть почти вся информация о прицеле сидит именно в наклоне, которого сервер
не видит. Наряд называет это как ИСХОД («направление взгляда известно создателю игры»), устройство
оставляет исполнителю.

**Пин контракта:** `current: 36` против продуктового стампа 36 — дрейфа нет. Лимит v36 в три
пункта соблюдён: три оставшиеся несущие строки задачи уложены в три исхода.

**Слот:** WIN-U1 `AVAILABLE`, `lease: none`, чекаут чист, вершина = база.

## Границы, проведённые явно

- **С `t-scale-4`:** здесь лут ведёт себя ровно как сегодня. Правило «лут неактивен, пока его не
  взяли лучом» принято владельцем, но строит его `t-scale-4`. Зависимости нет: сегодня лут всегда
  тело с выключенной гравитацией и запертой высотой, и луч работает с ним как есть.
- **С `t-scale-6`:** потолок числа рук (`_cargoMaxHolders: 2`) не поднимается.
- **С `t-vert-1`:** высоты игроку не заводить.
- **С `t-scale-7`:** третьего шва не заводить.

## captures

- Переменная дальность луча на держателя — его ВХОД («я думаю что потом можно будет дальность
  прокачивать»), не требование. Если он когда-нибудь захочет прокачку дальности, шов ставится тем
  же ходом, что и личная мощность хвата в `t-scale-7`. Сегодня стоит ноль.
- Числа дальности выбирает наряд и приводит их; судит их владелец заходом. Если он скажет, что
  дальность не та, лечится это тремя числами в ассете, а не переписыванием формулы.

## decisions_needed

Нет.

## play_check

- 1 recite: done — цель и `done_when` `t-scale-5` пересказаны, обязательство ставки и порог смерти
  названы.
- 2 owner inputs (owner): skipped — фактов, которые знает только он, наряд не требует. Правило
  выбора он уже дал дословно («Прицелом и близостью»); числа дальности выбирает наряд, а судит их
  он заходом, и это записано в возврат. Операционная предпосылка — поднятый Unity в WIN-U1 —
  передаётся ему в чате.
- 3 do the work: done — наряд написан; проверка происхождения сняла одну строку из четырёх; пять
  фактов кода перемерены первой рукой; одно устаревшее основание исправлено.
- 4 self-check: done — три оставшиеся строки задачи покрыты тремя исходами; лимит v36 соблюдён;
  границы с `t-scale-4`, `t-scale-6`, `t-vert-1` и `t-scale-7` проведены явно.
- 5 close: done — наряд зарегистрирован карточкой, возвращающийся CALL закрыт.

## Гейты

- **G7.** Ни одного решения продукта наряд не принимает. Снятая строка не выброшена молча — она
  названа как его ВХОД и записана в captures. Числа дальности отданы на его суд, а не на суд
  исполнителя.
- **G9.** Карточка узла не трогалась.
- **G10.** Наряд не диктует устройство: «направление взгляда известно создателю игры» — исход,
  способ оставлен исполнителю.

## log

наряд на телекинез написан; проверка происхождения сняла строку про переменную дальность — это его ВХОД, а не требование, и третий шов аппетитом не куплен

## next

`CALL c-exec-g-5a7c-telekinesis-001` — исполнителю в `GasCoopGame`, слот WIN-U1, стадия PLAN,
контракт 36. Отправляется, когда владелец поднимет Unity в этом слоте.

END_OF_FILE: live/indie-game-development/history/2026-08-08-s-work-g-5a7c-telekinesis-call-001.md
