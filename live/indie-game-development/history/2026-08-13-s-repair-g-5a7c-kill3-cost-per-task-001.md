RESULT s-repair-g-5a7c-kill3-cost-per-task-001 (call: none — плановая правка по прямому слову владельца)
direction: indie-game-development   track: none   play: repair   node/task: g-5a7c / bet-g-5a7c-wave-3
outcome: |
  Порог смерти 3 третьей волны («цена разговора») больше не абсолютный счётчик ног. Он меряет
  РАСХОД — «дороже четырёх ног на закрытую задачу», — и это тот же темп первой волны, из которого
  прежнее число и выводилось (24 ноги на шесть построенных задач = 4 на задачу).

  Прежняя редакция была дефектна ДВАЖДЫ, и обе ошибки записаны в карточку, а не спрятаны:
  1. ЧИСЛО. «Больше двенадцати» верно для ДВУХ дорожек: три задачи до контрольной точки × 4 ноги.
     У третьей волны дорожек три, задач до контрольной точки четыре, и то же правило обязано было
     дать шестнадцать. Число перенесли из второй волны без пересчёта на добавленную дорожку.
  2. ФОРМА, и она хуже числа. Абсолютный счётчик растёт от самой параллельности, которую владелец
     же и заказал («нам следующую волну точно нужно запускать, чтобы параллельно шло»). На
     измеренном расходе он сработал бы при 16 ногах на ШЕСТЬ закрытых задач = 2.7 ноги на задачу,
     то есть в полтора раза ЛУЧШЕ базы, из которой сам же и выведен. Кран ловил пробег, а надо
     было расход.

  Порядок выбывания (`body` → `t-loot-1`) НЕ тронут. Остальные четыре порога НЕ тронуты. Ни одна
  задача не снята, не выброшена и не переставлена: `t-loot-1` остаётся в волне.
evidence: |
  Слова владельца, на которых стоит правка (2026-08-13, этот чат):
  — «Звучит как какое-то бредовое правило. Ну типа мы достаточно много, но кстати, не сделали, мне
    кажется, что мы достаточно продуктивно поработали. Зачем нам что-то закрывать?»
  — на показанный целиком текст правки: «Я не понимаю, что за, ну ничего выкидывать не надо, то
    есть, ну, ну я говорю тебе, да, то есть там дальше продолжай, но выше с этим, ничего выкидывать
    не надо». Это и G9-одобрение точного артефакта, и его собственная граница результата.

  Числа перемерены первой рукой в этой ноге, не взяты из прозы:
  — база: `bet-g-5a7c-wave-3.md` §kill_by прежней редакции и `g-5a7c.md` §kill_by второй волны —
    «Двенадцать — это ровно темп первой волны (24 ноги на шесть построенных задач)»;
  — задачи до контрольной точки второй волны: общая `t-situation-1` + первая задача каждой из ДВУХ
    дорожек = 3; третьей волны: общая `t-places-1` + первые задачи `t-sight-1`, `t-rooms-1`,
    `t-body-1` = 4;
  — ноги волны: `ls live/indie-game-development/history/ | grep '^2026-08-12'` = 18 записей, из
    них `s-review-g-5a7c-wave-2-partial-001` и `s-shape-g-5a7c-wave-3-001` — закрытие прошлой
    волны и нарезка этой, то есть 16 рабочих ног волны; на 2026-08-13 записей ноль;
  — закрыто волной за те же ноги: `t-places-1`, `t-rooms-1`, `t-body-1`, `t-thing-1`,
    `t-place-fact-1`, `t-occupations-1` = 6 (карточки в `cards/closed/`, все со `status: done`);
  — 16 / 6 = 2.7 ноги на закрытую задачу против базовых 4.0.

  Применение: четыре вызова `osctl card block` (kill_by, lens_verdicts, riskiest_assumptions на
  ставке; kill_by на узле). `uv run --locked python osctl.py check` → «механических проблем нет».
  Замечания про `kind, order` в шапках задач существовали до этой ноги на всех 28 карточках задач
  и этой ногой не трогаются (граница удаления плея repair).
state_changes: |
  cards/bet-g-5a7c-wave-3.md — блок `kill_by`: пункт 3 переписан целиком с абсолютного счётчика
    «больше двенадцати ног» на расход «дороже четырёх ног на закрытую задачу», с записанной
    причиной обеих ошибок и обеими цитатами владельца. Пункты 1, 2, 4, 5, next_if_true и
    next_if_false — preserve unchanged.
  cards/bet-g-5a7c-wave-3.md — блок `lens_verdicts`: линза 3 «реально ли одному» приведена к новой
    формулировке порога и получила явное «4 ноги на задачу». Линзы 1, 2, 4-7 — preserve unchanged.
  cards/bet-g-5a7c-wave-3.md — блок `riskiest_assumptions`: допущение 4 «три дорожки не утонут в
    разговорах» приведено к новой формулировке теста. Допущения 1-3 — preserve unchanged.
  cards/g-5a7c.md — блок `kill_by`: порог (3) ТРЕТЬЕЙ волны приведён к тому же смыслу (узел несёт
    зеркало порогов ставки, второго источника правды не остаётся). Пороги (1), (2), (4), (5)
    третьей волны и ВЕСЬ текст ПРЕЖНЕЙ ВТОРОЙ ВОЛНЫ, помеченный «для истории», — preserve
    unchanged: история не переписывается.
  НЕ тронуто и названо нарочно: `NOW.md`, `body.md`, `t-loot-1.md` (обе карточки ссылаются на порог
    по номеру и порядку выбывания, их смысл правкой не изменился), ни одна карточка улики, ни одна
    задача, ни один статус, ни одна открытая CALL-карточка.
captures:
  - Ошибка-класс: параметр порога, выведенный из числа дорожек, перенесён в следующую волну как константа. Класс уже дал один случай; при следующей волне с изменённым числом дорожек все выведенные из него числа обязаны пересчитываться, а не копироваться.
  - Абсолютный счётчик как форма порога противоречит заказанной владельцем параллельности: любой порог, растущий от числа одновременных дорожек, наказывает за то, что он же и просил. Форму «расход на результат» стоит проверить на остальных порогах при следующей нарезке.
decisions_needed: []
play_check:
  - 1 Назвать противоречие: done — карточка обещает резать волну при «больше двенадцати ног», а измеренный темп волны лучше базы, из которой двенадцать выведено; и само двенадцать посчитано для двух дорожек, а стоит в волне на трёх.
  - 2 Реконструировать: done — новейшее-первым по журналам карточек, `history/` за 2026-08-10..13 и `cards/closed/`; числа второй волны прочитаны в разделе «ПРЕЖНЯЯ ВТОРАЯ ВОЛНА, для истории» карточки узла. Инвентарь волны: 6 закрыто, 2 в работе (`t-sight-1` доставлена в продукт и ждёт закрытия Направлением, `t-loot-1` строится в WIN-U1), 3 осталось.
  - 3 Предложить исправленное состояние: done — одна причина на изменение, перечислены в state_changes; выброшено ничего, история второй волны сохранена дословно.
  - 4 (owner) Подтвердить одним общим дифом: done — полный текст правки показан владельцу до применения; его слова «ну я говорю тебе, да, то есть там дальше продолжай» и «ничего выкидывать не надо». Это же G9-одобрение точного артефакта для карточки узла `g-5a7c`.
  - 5 Трение: skipped — дыры в OS эта нога не нашла. Дефект был в содержании карточки, а не в правиле; механизм «пороги показываются владельцу целиком до подписи» сработал, ошибку поймал он сам.
log: порог смерти 3 третьей волны исправлен ЕГО словом «ничего выкидывать не надо»: абсолютный счётчик «больше двенадцати ног» заменён расходом «дороже четырёх ног на закрытую задачу», и в карточку записаны обе ошибки прежней редакции — двенадцать было верно для ДВУХ дорожек (три задачи до контрольной точки × 4 ноги темпа первой волны) и перенесено на три без пересчёта, где то же правило даёт шестнадцать, а сверх числа неверна была сама форма: абсолютный счётчик растёт от заказанной им же параллельности и сработал бы на измеренных 16 ногах при ШЕСТИ закрытых задачах, то есть 2.7 на задачу против базовых 4.0, в полтора раза ЛУЧШЕ базы, из которой выведен; порядок выбывания, остальные четыре порога, обе зависимые карточки и вся история второй волны не тронуты, ни одна задача не снята
next: |
  return-to-owner.
  Фронтир не эта нога: `c-exec-g-5a7c-sight-1-001` вернулась доставленной в продукт
  (`origin/main` = `origin/dev` = `af345209`, WIN-U3 освобождён, вердикт владельца по зрению
  положительный), но builder-return закрытием Направления не является. Закрытие `t-sight-1`
  требует свежей биндинговой проверки в ОТДЕЛЬНОМ физическом чате — по клаузе 2 evaluator самой
  ставки, потому что строку судит его глаз и прогон, которого нога не делала сама. Туда же уезжают
  два хвоста, отправленные исполнителем домой: дёрганье пути в проёмах и сделка с плитой (зрение
  перебивает фазу, которую профиль пометил непрерываемой). Порог 3 после этой правки на закрытии
  не сработает: измеренный расход 2.7 против 4.0.
