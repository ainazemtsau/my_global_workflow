# RESULT — s-work-g-1d84-conditions-panel-route-001

RESULT s-work-g-1d84-conditions-panel-route-001 (call: work indie-game-development/сцена g-1d84/t-scene-3)
direction: indie-game-development   track: сцена   play: work   node/task: g-1d84/t-scene-3

## outcome

Панель условий сделана, влита и опубликована, но **задача `t-scene-3` НЕ ЗАКРЫТА**: тест светлого
закрытия применён построчно и **отказал на двух строках из трёх**, поэтому вся задача целиком ушла в
свежую биндинговую проверку `c-work-g-1d84-conditions-panel-binding-g5-001`. Частичного светлого
закрытия не бывает — так говорит `os/plays/work.md` §Light close, и здесь это правило сработало
ровно так, как задумано.

Наряд `c-exec-g-1d84-conditions-panel-001` снят с фронтира: работа вернулась домой, слияние в `dev` —
`69c47a08` (родители `032ebb17` + `54296e8e`), кандидаты `172aebaf` + `c5d707c0`, опубликованное
состояние — `main` = `origin/main` = `origin/dev` = `d60c2f04`. Оба кандидата проверены как предки
`d60c2f04` командой, а не отчётом.

Главное, что нашла эта нога сверх заказа: **переключатель «один игрок или двое» не задаёт ничего** —
у него нет ни одного читателя при запуске, — и наряд такой ширины выреза не разрешал.

## evidence

Всё перемерено первой рукой в `C:/projects/Unity/GasCoopGame` на `d60c2f04`; продукт только на чтение,
ни одного write.

**Цепочка и опубликованное состояние.**

- `git rev-parse HEAD` = `origin/main` = `origin/dev` = `d60c2f04228bdfa161d6e122c56a2c5ccfec89f3`.
- `git rev-list --parents -n 1 69c47a08` → родители `032ebb17` (dev после слияния хозяина) и
  `54296e8e` (вершина полосы панели).
- `git merge-base --is-ancestor 172aebaf d60c2f04` → YES; то же для `c5d707c0` → YES.
- Порядок коммитов панели в `2eee9719..d60c2f04`: `172aebaf` (сборка) → `79d6ffc7` (отчёт) →
  `c5d707c0` (перерисовка меток) → `a17f9422`, `68f2a9df`, `b7fc819f`, `3b3a1534`, `54296e8e` (записи
  в отчёт) → `69c47a08` (слияние) → `d60c2f04` (`DELIVERED on dev`).
- Слот: `C:/projects/Unity/GasCoopGame_slot-state/gascoop-slot-state.v1.json` — `WIN-U4`
  `lifecycle: AVAILABLE`, `lease: none`. Контекст наряда подтверждён; аренда снята после отчёта,
  который писался при ещё держащейся аренде.

**СТРОКА 1 — «На интегрированной сцене в редакторе задаются стартовые условия: где стоит груз, где
хозяин, включён он или выключен, один игрок или двое». ОТКАЗ.**

Держится байтами:

- `d60c2f04:Assets/TunnelCrew/Scenes/IntegratedHouse.unity` — `SceneRoots` содержит девять корней,
  девятый `1874233012`; объект `m_Name: StartConditions` (строка 598), скрипт
  `Assembly-CSharp::TunnelCrew.World.StartConditions` (строка 615).
- Существуют `Assets/TunnelCrew/World/StartConditions.cs`,
  `Assets/TunnelCrew/Editor/StartConditionsEditor.cs` и обе `.cs.meta`.
- Четыре раздела инспектора: `StartConditionsEditor.cs:95` «Where the cargo stands», `:119` «Where the
  householder stands», `:155` «Is the householder in», `:177` «One player or two».
- Три условия пишутся в свои настоящие дома, а не в копию: груз — `_playSettings.Update()` /
  `ApplyModifiedProperties()` вокруг полей `_cargoSpawnX`/`_cargoSpawnZ` в `NetworkPlaySettings.asset`
  (:106-109); место хозяина — `householder.position = position` + `SetDirty` + `MarkOwningSceneDirty`;
  включён/выключен — `householder.SetActive(active)` (:298) под `Undo.RecordObject`.

НЕ держится — четвёртое условие:

- `git grep -n "StartConditions\|StartPlayerCount\|\.Players" d60c2f04 -- Assets/TunnelCrew` за
  вычетом самого редактора даёт у `_players` **ровно одного читателя во всём проекте**:
  `StartConditions.cs:68`, внутри `OnDrawGizmos`, внутри `#if UNITY_EDITOR`. У публичного геттера
  `Players` (`:44`) **ноль мест вызова**.
- Число ходоков при запуске решает `NetworkWalkerCourier.OnClientLoadedStartScenes:104` —
  `_world.Walkers.Add(connection.ClientId, _settings.CreateSpawnState(_world.Walkers.Count))` на
  каждое соединение; панель там не спрашивается.
- Панель честна в собственной подсказке (`StartConditionsEditor.cs` `DrawPlayers`): «This is a record
  of what you are setting up, not a lock: the judge hands a walker to whoever connects, so it neither
  forces the second copy nor turns one away».
- **Наряд разрешал не это.** Его дословный диапазон: «от „сколько ходоков поднимать локально“ до „эта
  галочка просто ничего не делает ВО ВТОРОЙ КОПИИ, и так и написано в подсказке“». Нижняя граница
  привязана ко второй копии. Поставлен переключатель, не делающий ничего НИГДЕ — это шире, чем наряд
  назвал допустимым. Отчёт объявляет это отклонением сам («Счёт игроков — ЗАПИСЬ намерения, а не
  рычаг»), с двумя честными причинами: курьер трогать запрещено, `Assets/Plugins/**` читать запрещено.
- Удовлетворяет ли запись-без-рычага строку «задаются стартовые условия: … один игрок или двое» —
  **суждение, которого никто не выносил**. Ни байт, ни слово владельца его не заменяют, поэтому
  строка 1 светлым закрытием не закрывается. Решать это за него нога отказывается (G7).

Сюда же — и это отдельная причина отказа:

- **Компиляция и работа панели в редакторе агентом не проверены ни разу.** Отчёт закрывает компиляцию
  цепочкой вывода из владельцева взгляда: «он видит метки ⇒ `Assembly-CSharp` собрался»; «он описал
  разделы ⇒ `Assembly-CSharp-Editor` собрался». Цепочка правдоподобна, но это рассуждение исполнителя
  о том, что следует из чужого взгляда, а не агентская проверка и не слова владельца о компиляции.
  Прямого MCP к Unity в той сессии выставлено не было; в реестре у `WIN-U4` `mcp_endpoint: unrecorded`.
  Класс — `i-unity-evidence-rests-on-owner-launch-001`, и наряд этой ноги велел проверить именно это.

**СТРОКА 2 — «Рантайм не требуется». ДЕРЖИТСЯ.**

Прочитан весь `d60c2f04:Assets/TunnelCrew/World/StartConditions.cs`: в классе НЕТ `Awake`, `Start`,
`Update`, `OnEnable` и `OnDisable` вовсе. Единственный метод — `OnDrawGizmos` вместе с тремя
приватными помощниками отрисовки, и все они внутри `#if UNITY_EDITOR`. Комментарий класса это же и
заявляет («Nothing on this component runs»), но вердикт стоит на отсутствии методов, а не на
комментарии. Применять при запуске нечего по построению: три условия уже лежат в тех самых байтах,
которые Play читает (`NetworkPlaySettings.asset`, трансформ корня хозяина, `m_IsActive`), а четвёртое
не читается никем. Это факт байтов, а не суждение.

**СТРОКА 3 — «Владелец сам собрал РАЗНЫЕ условия и запустил». ОТКАЗ, и это решающая строка.**

Его дословные слова, лежащие в коммите `54296e8e`
(`docs/results/c-exec-g-1d84-conditions-panel-001.md`, раздел «Слово владельца»):

> я вижу, ну вот, что вот эта панель работает. Всё, ну, плей работает. Гизмос я вижу

Сказано после того, как он открыл проект, нашёл панель, включил `Gizmos` и нажал Play — так пишет сам
отчёт. Разбор:

- «панель работает» — панель на месте и работает. Это про строку 1, и то частью.
- «плей работает» — Play поднимается. Не сказано, что он поднимается **в собранных им условиях**.
- «Гизмос я вижу» — метки видны. Это про отрисовку.

Ни в одной из трёх частей нет того, что он **собрал** хотя бы один набор условий, тем более **разные**
наборы, и запустил каждый. Строка требует именно этого — множественного числа и его собственной руки.

**Отчёт противоречит сам себе трижды, и это перемерено по его же тексту:**

1. Шапка раздела «Слово владельца»: «**Это закрывает пункт 3 `done_when`**».
2. Собственный раздел о компиляции: «Что этим НЕ доказано и остаётся за владельцем: сам прогон Play в
   собранных условиях, то есть пункт 3 `done_when`. Компиляция — да, поведение — ещё нет».
3. `Manual-acceptance` пункт 9: «~~Пункт 3 `done_when` — ваши слова.~~ **ПРОЙДЕНО 2026-08-05**».

Первое и третье — вывод; второе — факт. Отчёт при этом сам же и пишет, что формальное закрытие отдаёт
свежей биндинговой проверке и себя ею не считает. То есть его собственный текст, прочитанный целиком,
на закрытие не претендует — претендует только шапка. **Это выводимость, а не подпись** — тот же
разрыв, что стоил лишней ноги на `t-host-1` накануне.

**ОПУБЛИКОВАНО НЕ ТО, ЧТО ОН ЗАПУСКАЛ — перемерено блобами.**

`Assets/TunnelCrew/Scenes/IntegratedHouse.unity`: на `c5d707c0` (что он запускал) блоб `627ec609`, на
`d60c2f04` (что опубликовано) — `9ada2ac9`. `git diff` между ними даёт ровно одно изменение, и оно из
соседней полосы: на корень `NetworkWorld` добавлен компонент `&2053887854`
(`TunnelCrew.Networking.NetworkHouseholder`, `_householder: {fileID: 1607884504}`) и строка
`_householder: {fileID: 2053887854}` в курьера. Стрипнутый трансформ `&1607884504` встречается в
слитой сцене 3 раза против 2 в кандидате — след того самого конфликта, разрешённого сведением к одной
копии. Проводка панели на слитой сцене на месте (девятый корень в `SceneRoots` цел). Поломку
утверждать не надо; холодный факт один: **сцену в её нынешнем, слитом виде не запускал никто.** Живёт
это в `i-published-to-main-before-any-direction-close-001`, заведённой накануне.

**Что НЕ проверялось и почему.** Продуктовый репозиторий открывался только на чтение (`git show`,
`git grep`, `git diff`, `git rev-parse`, `git ls-remote`). Ни одной записи, ни слияния, ни push, ни
касания реестра слотов. Unity не запускался — и именно поэтому компиляция названа выше незакрытой
агентски, а не объявлена закрытой.

## state_changes

Применено этой же ногой (агентский CLI на этом репозитории: сессия сначала выпускает RESULT, затем
становится своим писателем).

1. `NOW.md` заголовок: `updated: 2026-08-06 by s-work-g-1d84-conditions-panel-route-001`.
2. `NOW.tasks` `t-scene-3` — `status: open` (без изменения), добавлена `note` с построчным разбором
   маршрута: цепочка коммитов, отказ теста светлого закрытия по каждой из трёх строк, мёртвый
   четвёртый переключатель, разный блоб сцены.
3. `NOW.open_calls` — удалён `c-exec-g-1d84-conditions-panel-001` (вернулся домой, слияние
   `69c47a08`, родители `032ebb17` + `54296e8e`, кандидаты `172aebaf` + `c5d707c0`, опубликовано
   `d60c2f04`).
4. `NOW.open_calls` — добавлен `c-work-g-1d84-conditions-panel-binding-g5-001` (track `сцена`,
   `to: session`, `play: work`, `for: t-scene-3`, `status: ready`).
5. `NOW.issues` `i-slot-u4-remote-tip-carries-foreign-commits-001` — остаётся открытой, диспозиция не
   менялась; в `evidence` добавлен перемер 2026-08-06: `origin/slot/win-u4` по-прежнему `7e9556a1`,
   прежняя фраза «встали ровно на `main` = `2eee9719`» больше не читается буквально, `WIN-U4`
   `AVAILABLE` / `lease: none`.
6. Создан `work/c-work-g-1d84-conditions-panel-binding-g5-001-call.md`.
7. `LOG.md` — одна строка сверху.
8. `history/2026-08-06-s-work-g-1d84-conditions-panel-route-001.md` — этот RESULT целиком.

Не тронуто: `CHARTER.md`, `TREE.md`, `knowledge/**`, полосы кроме «сцена», решения, задачи кроме
`t-scene-3`, остальные записи, продуктовый репозиторий (ни одного write — только чтение коммитов и
блобов).

## captures

- **Пятое условие «где стоят игроки» ждёт своей карточки, и владелец про него уже спрошен.** Отчёт
  измерил: `NetworkPlaySettings.CreateSpawnState(ordinal)` считает позицию как
  `_spawnOriginX + _spawnSpacing * ordinal`, а Z прибит к нулю константой — двоих нельзя развести по
  разным комнатам вовсе. Курьер при этом трогать не надо (сигнатура сохраняется). Владелец 2026-08-05
  выбрал увезти это домой, а не делать в той ноге. Задачей волны не является и без его слова не станет.
- **MPPM 2.0.2 уже стоит** в `Packages/manifest.json`: `Unity.Multiplayer.Playmode.CurrentPlayer.IsMainEditor`
  превратил бы «двое» в один щелчок примерно восемью строками в `DirectNetworkPlayBootstrap`. Это
  готовый путь на случай, если владелец скажет, что запись-без-рычага его не устраивает. Работой не
  является — сборка того пакета в той сессии была непроверяема.
- **Два решения владельца по-прежнему не поехали** (откат Unity Cloud, закрытие мёртвого корня
  `c-exec-grid-v1-g01-document-authority-001`). Эта нога продуктового наряда не писала — писать их
  было некуда, поэтому пункты снова не поехали и обе записи остаются открытыми со своими
  `mechanism_failed`. Ближайший носитель — первый же следующий продуктовый наряд полосы «сцена».

## decisions_needed

Отдельным решением в `NOW` не заводится: оба вопроса адресованы владельцу внутри выпущенной
биндинговой проверки, потому что оба решаются одним взглядом на одну и ту же панель и разводить их по
разным чатам значило бы спросить его дважды об одном.

1. Собирал ли он РАЗНЫЕ условия и запускал ли каждое — или посмотрел на панель и нажал Play один раз.
2. Устраивает ли его, что переключатель «один игрок или двое» ничего не меняет при запуске.

## play_check

- 1 recite: done — задача `t-scene-3`, три строки `done_when`, полоса «сцена», ставка `g-1d84`;
  наряд `c-exec-g-1d84-conditions-panel-001` вернулся домой и подлежит снятию.
- 2 owner inputs (owner): нового ввода владельца эта нога не запрашивала и не могла — она судит
  маршрут по уже закоммиченным уликам. Его уже сказанные слова процитированы дословно там, где лежат
  (`54296e8e`): «я вижу, ну вот, что вот эта панель работает. Всё, ну, плей работает. Гизмос я вижу».
  Два новых вопроса к нему сформулированы и переданы в биндинговый наряд, где их и надо задать; ни
  один из них здесь не отвечен и не выведен.
- 3 do the work: done — тест светлого закрытия применён построчно к трём строкам на опубликованных
  байтах `d60c2f04`, всё перемерено первой рукой.
- 4 self-check: done — строка 2 держится байтами; строка 1 держится частью и падает на четвёртом
  условии плюс на неподтверждённой агентски компиляции; строка 3 не закрыта его словами. Две строки из
  трёх → частичного светлого закрытия не бывает → вся задача в свежий биндинговый чат.
- 5 close: done — `t-scene-3` остаётся `open`; выпущен один same-lane корень
  `c-work-g-1d84-conditions-panel-binding-g5-001`; возвращённый наряд снят с фронтира со ссылкой на
  слияние.

## log

см. строку в `LOG.md`.

## next

CALL `c-work-g-1d84-conditions-panel-binding-g5-001` — свежая биндинговая проверка `t-scene-3` в
ОТДЕЛЬНОМ физическом чате: перемерить строки 1-2 на `d60c2f04` попыткой опровержения, задать владельцу
два коротких вопроса про уже сделанное и вынести один вердикт. Файл наряда —
`live/indie-game-development/work/c-work-g-1d84-conditions-panel-binding-g5-001-call.md`.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-conditions-panel-route-001.md
