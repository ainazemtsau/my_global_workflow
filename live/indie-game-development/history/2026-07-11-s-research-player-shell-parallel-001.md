# RESULT — s-research-player-shell-parallel-001 (2026-07-11)

session: s-research-player-shell-parallel-001 (Codex; play: research; checkpoint awaiting owner route)
direction: indie-game-development / g-9c41 / player-shell-parallel

## Owner summary

Безопасная параллельная часть найдена, но продуктовый BUILD не открыт. Базовый контроллер уже относится к M1
player-tracking / историческому P2, поэтому новый верхнеуровневый трек не нужен. Рекомендован короткий
owner-present reconciliation: явно вписать P2a Player Shell в текущие 13 легов, выбрать тонкий pose-shell либо
детерминированный motor и разрешить venue. До этого Phase 0 остаётся главным следующим шагом.

```text
RESULT s-research-player-shell-parallel-001 (call: owner-plain-player-shell-parallel-2026-07-11)
direction: indie-game-development   play: research   node/task: g-9c41/player-shell-parallel
outcome: |
  CHECKPOINT / RECOMMENDATION READY, PRODUCT BUILD NOT AUTHORIZED. The basic controller belongs inside the current
  M1 player-tracking intent (the historical Gas Lab P2 names the WASD capsule + live dose), not in a new top-level
  track. A likely parallel split exists: P2a Player Shell on a disjoint presentation/input surface, then P2b for live
  SimInstance/Sc-sense/spawn/FishNet/two-machine integration. Current state has no Player Shell task or ready CALL,
  the 13-leg NOW road evolved beyond the older 11-leg M1 shape, and product venue policy does not automatically allow
  worktree-per-feature. Therefore the next safe move is a fresh owner-present shape/repair-style reconciliation that
  charges P2a to the current appetite, chooses pose-publisher versus deterministic motor, proves the exact diff is
  disjoint, and records the venue exception or reuse rule. Phase 0 remains the primary direction next.
evidence: |
  Direction authority: NOW.md (13-leg appetite; current tasks Lv-ingest/M1-A0/M1-GAS-PROBE; no Player Shell CALL),
  work/poligon-m1-shape-2026-07-10.md (older M1-5 player tracking and three-task rolling wave), and the live-status
  warning plus historical P2 capsule-controller section in work/board/dashboard.html. Sc-sense evidence:
  knowledge/g9c41-sc-sense-delivered-unwired.md states that actor pose/dose is proven in harness/FishNet witness but
  not wired into SimInstance; product TickInput/FishNet convergence artifacts carry integer actor pose + a separate
  player digest, while GasSenseCapsuleDebug is frame-time debug presentation and no production controller/entity was
  found. Product policy evidence: AGENTS.md defaults to one persistent dev worktree, while current M1 created named
  venue exceptions and serial merge slots; current dev/dev2/core surfaces are occupied, so reuse or a new lab venue
  must be decided explicitly.
  External primary sources: Git worktrees https://git-scm.com/docs/git-worktree.html; Unity per-project Library cache
  https://docs.unity3d.com/kr/current/Manual/AssetDatabase.html and Smart Merge
  https://docs.unity3d.com/kr/current/Manual/SmartMerge.html; FishNet basic client-authoritative movement
  https://fish-networking.gitbook.io/docs/tutorials/getting-started/moving-your-player-around (an easy transport path,
  not automatically compatible with this project's input-lockstep authority).
  Parallel research: 3 independent explorers converged on the same no-controller / existing-pose-seam / unwired-sense
  boundary. The first same-family refutation round exposed overclaims about immediate BUILD and automatic worktree
  permission. Revised round: governance survived 3/3; venue routing survived 2/3 and one refutation correctly narrowed
  the rule to "reuse persistent dev when explicitly free, otherwise record an exception"; exact P2a scope remained
  3/3 INCONCLUSIVE until PLAN. These are non-binding in-session pre-passes, not fresh-session G5.
  Writer rebase evidence: origin/main advanced from 2b0f7db to 19c25ea during apply. That concurrent board-repair commit
  included the byte-identical LOG line for this session but not its NOW decision, history or work brief; the writer
  treated the append as an idempotent already-applied operation and applied the remaining explicit delta on fresh state.
state_changes: |
  ADD live/indie-game-development/work/player-shell-parallelization-2026-07-11.md as the non-canonical research brief.
  live/indie-game-development/NOW.md: set updated to
  `2026-07-11 by s-research-player-shell-parallel-001`; UPSERT pending decision
  `d-player-shell-parallel-001` with the three options/recommendation below; preserve the bet, appetite, tasks,
  open_calls, all other decisions, recurring and primary next exactly as current.
  live/indie-game-development/LOG.md: append exactly the `log:` line below once.
  Save this full RESULT once at
  live/indie-game-development/history/2026-07-11-s-research-player-shell-parallel-001.md.
  Regenerate the live owner-panel sections in live/indie-game-development/work/board/dashboard.html from current
  NOW+LOG: add the detailed d-player-shell-parallel-001 decision card and today's Player Shell journal line; preserve
  the panel's current freshness cleanup and all unrelated live cards.
  No CHARTER.md, TREE.md, task status, open_call, NOW.next or product-repo change.
captures:
  - Candidate split for shaping: P2a = multiplayer-shaped local Player Shell; P2b = authoritative/live integration.
  - P2a must not claim multiplayer done: command/pose authority, collision/tick order, spawn ownership and two-machine
    proof remain explicit later gates.
  - Venue conflict to resolve: product default persistent dev versus a named project-level lab/player exception;
    never reuse an occupied dev2/Core venue and never assume a third Unity/MCP instance is independently addressable.
  - The owner panel is a render only; NOW remains authority for d-player-shell-parallel-001 and its eventual verdict.
  - Same-direction writer serialization was violated during this apply; the semantic rebase recovered consistent final
    state without reverting the concurrent board repair or duplicating the LOG line.
decisions_needed:
  - q: d-player-shell-parallel-001 — choose how the base controller enters the current M1 road.
    options: ["A: fold a P2a Player Shell into current M1 after a fresh owner-present reconciliation; no BUILD before its ready CALL", "B: create a separate player track with its own appetite/governance", "C: wait for the sequential M1 player-tracking step"]
    recommendation: "A; it captures safe parallelism without duplicating M1 or silently extending appetite. The reconciliation must choose pose-shell vs deterministic motor, prove disjointness, and resolve the venue policy before dispatch."
play_check:
  - 1 Recite: done — one bounded question, return = route/scope/worktree recommendation, budget = one read-only research session.
  - 2 Investigate: done — read current direction state/road/Sc-sense evidence, product code and venue policies; checked Git/Unity/FishNet primary docs; ran 3 independent explorers and two refutation rounds.
  - 3 Confidence: done — established seams and occupied surfaces were separated from the deliberately unproven P2a manifest; every successful refutation was folded into the narrower recommendation.
  - 4 Close: done as checkpoint — decision is owner-routed, no BUILD/task/CALL opened, and Phase 0 remains primary next.
log: 2026-07-11 — research/checkpoint (g-9c41/player-shell-parallel, s-research-player-shell-parallel-001): базовый контроллер привязан к существующему M1 player-tracking/P2 намерению, но BUILD не открыт — сначала owner-present reconciliation должен вписать P2a в текущий appetite, выбрать pose-shell против deterministic motor и явно разрешить venue; Phase 0 остаётся primary next. → history/2026-07-11-s-research-player-shell-parallel-001.md
next: |
  awaiting_decision d-player-shell-parallel-001; primary direction next remains
  work/c-exec-lv-ingest-phase0-call.md. On owner option A, a fresh owner-present shape/repair-style session reconciles
  the current M1 road and authors a fire-ready Player Shell executor CALL; no product BUILD starts from this checkpoint.
```

END_OF_FILE: live/indie-game-development/history/2026-07-11-s-research-player-shell-parallel-001.md
