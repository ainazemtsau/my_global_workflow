# RESULT — s-converge-verify-g-5a7c-002

leg: s-converge-verify-g-5a7c-002 (2026-08-07, свежая физическая сессия, владельца в чате нет)
call: `work/c-converge-verify-g-5a7c-002-call.md` (`ready`)
play: `os/plays/converge-verify.md`
verify_target: build
judged: `work/converge-g-5a7c.md` (§DRIFT, §IMPORT, §GLOSSARY, §WHAT, три §SIGNOFF, §CONTRACTS,
        §COUNT-2, §COUNT) плюс `work/converge-g-5a7c-arch.md` (§RISK-LINE, §DECOMPOSE, `A1`–`A12`,
        §MINER, §NOT-DECIDED, §SIGNOFF, §REPAIR-LEDGER), против карточки `TREE.md` `g-5a7c:117-142`.

**Веер.** Опровержения субагентами не было: в этом харнессе действует прямая постоянная инструкция
владельца не разворачивать субагентов и workflow, и по авторитету `os/KERNEL.md` живая инструкция
владельца выше плея. Это ВТОРОЕ независимое опровержение набора, а не второй веер, и наряд говорит
это прямо.

---

## outcome

**`verify_target: build complete=FAIL smuggling=FAIL`. Это ВТОРОЙ FAIL узла, и потолок плея ПРИМЕНЁН:
возврата в `converge`/`converge-arch` не выдано, поверхность заморожена уликой, владельцу вынесено
ОДНО решение с тремя ветками.**

Счёт отказов по `history/`: `2026-08-07-s-converge-verify-g-5a7c-001.md` — `complete=FAIL
smuggling=FAIL` (первый); эта нога — второй. Других `converge-verify` по `g-5a7c` в `history/` нет.

Найдено семь мест: четыре по полноте (`V1`–`V4`), три по протаскиванию (`V5`–`V7`). Ни одно не
починено здесь, ни одно не заполнено ответом, владельцу не вынесено ни одного НОВОГО решения кроме
предписанного потолком.

**И это надо сказать прямо, потому что иначе вердикт читается сильнее, чем он есть: набор выдержал
почти всё, и выдержал МЕХАНИЧЕСКИ, а не на слово.** Все три счёта пересчитаны командой и сошлись до
строки; все заявления о нетронутости проверены диффом, а не чтением; вся продуктовая база перемерена
первой рукой; главная проверка наряда — прочтение «заложить ≠ сделать» против строки исключений
подписанной карточки — ПОДТВЕРЖДЕНА. Четыре из семи находок — это «предложение подписанной карточки
не дошло ни до одной строки» и «устаревшая таблица счёта», а не изобретённая работа.

---

## evidence

### Шаг 0 — база перепроверена ПЕРВОЙ РУКОЙ ДО чтения набора

`C:/projects/Unity/GasCoopGame`: `git rev-parse HEAD` = `352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa`,
ветка `main`, `git status --short` пусто. Совпадает с объявленной базой обоих файлов. Репозиторий
только читался.

### Шаг 1 — механический пересчёт (наряд, `done_when` 5)

Пересчитано скриптом по текущему `work/converge-g-5a7c.md`, а не переписано из наряда:

| измерено | §COUNT-2 утверждает | сошлось |
|---|---|---|
| `what_rows: 126`, дублей id ноль | 126 | ✓ |
| `open_rows: 9` — `W39a`, `W54`, `W73`, `W94`, `W114`, `W116`, `W117`, `W118`, `W119` | 9, те же id | ✓ |
| `open_owed_by_owner: 1` (`W94`) | 1 | ✓ |
| `open_owed_by_plan: 7` | 7 | ✓ |
| `open_owed_by_shape: 1` (`W117`) | 1 | ✓ |
| `deferred: 2` (`W15`, `W16`) | 2 | ✓ |
| `acceptance_rows: 47` | 47 | ✓ |
| строк с одновременными `acceptance` и `open`: **0** | 0 | ✓ |
| `answered: 93` · `улика: 48` · метка содержит `→ PLAN`: 23 | 93 / 48 / 23 | ✓ |
| §CONTRACTS: 31 ряд = `C01`–`C12` края + `C13`–`C31` швы | 12 + 19 | ✓ |
| арх-файл: `## A1`…`## A12` = 12 строк; §MINER 7 строк | 12 / 7 | ✓ |

### Шаг 2 — заявления о нетронутости проверены ДИФФОМ (наряд, `done_when` 4)

Шесть коммитов набора: `4456f67a` (`converge` ремонт), `2d2404b4` (арх ремонт), `466bcba5` (004),
`4f9a2040` (005), `7f0e0214` (006), `b4ed7f26` (`converge` его ответы).

- **`TREE.md`, `CHARTER.md`, `knowledge/**` не входят ни в один из шести `--stat`.** Карточка не
  тронута ни знаком — измерено, а не принято на слово.
- **Арх-ноги не трогали §WHAT:** `git show <c> -- converge-g-5a7c.md | grep -cE "^[-+]\| W"` = **0**
  для всех четырёх арх-коммитов. Их хунки лежат целиком ниже строки 427 при §CONTRACTS на 428 —
  §WHAT (186–389), §SIGNOFF (390–427) и §COUNT не задеты.
- **`converge`-ноги не трогали §CONTRACTS:** тот же счёт по `^[-+]\| C` для `4456f67a` = **0**;
  §COUNT добавлен чистой вставкой в конец файла (в хунке `@@ -510,4` нет ни одной `-`-строки).
- **`b4ed7f26` тронул ровно 9 строк §WHAT** — правлены `W26`, `W45`, `W63`, `W114`, `W115`; новые
  `W120`, `W121`, `W122`, `W123`, — плюс абзац легенды, плюс блок §SIGNOFF, плюс §COUNT-2. §COUNT-2
  вставлен ПЕРЕД §COUNT чистой вставкой: в хунке `@@ -629,6` ни одной `-`-строки, то есть прежний
  §COUNT не правлен ни знаком, ровно как заявлено. Арх-файла в этом коммите нет вовсе.
- **§REPAIR-LEDGER сверен посекционным диффом арх-файла.** `A1`, `A3`, `A6`, `A7` — байт в байт не
  тронуты ремонтом. `A2` +5 строк и `A5` +6 строк — оба абзаца прочитаны: это указатели на `A10`
  («вывод строки не тронут»), а не новые выводы. `A4` +33 (`F2`), `A8` +17 (`F9`), `A9` +45 (`F1`,
  `F5`, `F7`), `A10` создана, §MINER +5, §NOT-DECIDED +6, §SIGNOFF +48, §REPAIR-LEDGER создан. Все
  семь строк таблицы подтверждены.
- **Одна неточность самой таблицы, названная ради точности и НЕ возвращаемая находкой:** §MINER и
  §NOT-DECIDED ремонт тронул, а ни одна строка §REPAIR-LEDGER их не называет. Обе правки объявлены
  внутри самих разделов и в §SIGNOFF, обе — против набора (самокритика §MINER, понижения
  §NOT-DECIDED). Это недоучёт таблицы, а не необъявленная правка.
- **`LOG` за `s-converge-arch-g-5a7c-003` задним числом НЕ правлен** — `LOG.md:17` на месте, как и
  обещала строка 5 леджера.

### Шаг 3 — продуктовые факты, на которых стоит набор, перемерены первой рукой

Всё ниже прочитано в `352f96b0`. Совпало ВСЁ, без единого расхождения:

- `Network/NetworkPlaySettings.asset`: `_movementSpeed: 3`, `_cargoRestHeight: 0.25`, `_cargoMass: 4`,
  `_physicsStepsPerTick: 2`, `_cargoPickupRange: 1.3`, `_cargoCarryOffset: 1.3`,
  `_cargoHoldStrength: 1`, `_cargoMaxHolders: 2`, `_cargoGripLength: 3.2`, `_cargoHandReach: 0.2`,
  `_cargoHandStiffness: 400`, `_cargoMaxHandForce: 200`, `_carrySpeedScale: 0.6`,
  `_cargoRustleForce: 60`, `_cargoKnockForce: 150`, `_cargoHeavyNoiseForce: 400`,
  `_walkerRadius: 0.45`, `_spawnOriginX: -1.5`, `_spawnSpacing: 3`.
- `IntegratedHouse.unity`, компоненты перечислены полностью: `Floor` — Transform, MeshFilter,
  **MeshCollider (64)**, MeshRenderer, позиция `(0,0,0)`, масштаб `1.6` → 16×16;
  `Delivery Yard Floor` — Transform, MeshFilter, **BoxCollider (65)**, MeshRenderer, позиция
  `(10, −0.05, 0)`, масштаб `(5, 0.1, 6)` → `x ∈ [7.5, 12.5]`. **Ни на одном из двух нет ни одного
  MonoBehaviour (114)** — значит `HouseWall` нет. Контроль: у `Outer East South` MonoBehaviour ЕСТЬ,
  позиция `x: 7.25`, масштаб `x: 0.5`. `East Room` живёт в `World/ThreeRoomHouse.prefab`
  (центр `x: 4.875`, `_size: {x: 4.25}` → восточный край `7.0`). Вся геометрия `A10.1`-(г) верна.
- Спавн: `−1.5 + 3n` = −1.5, 1.5, 4.5, **7.5** при восточной стене `[7.0, 7.5]` — `W67` верна.
- `Core/Cargo/CargoHold.cs:42-43` — «Seam: how hard this hand holds on. **No rule reads it.**»;
  `:45-46` — второй такой же. `AuthoritativeCargoRoster` кладёт в новый хват `rules.HoldStrength`.
  Шов `C31`/`A12` действительно стоит ПУСТЫМ.
- `grep "Mass"` по `Assets/TunnelCrew/Core/` = **0**; `grep "CargoMass|\.mass"` по `Assets/TunnelCrew`
  = ровно **2** (`NetworkPlaySettings.cs:118`, `Network/CargoBody.cs:61`); в конструкторе `CargoRules`
  ровно **14** полей и массы среди них нет. `A11` верна.
- `CargoHaul.cs:112` — `Cap(ref handX, ref handZ, rules.MaxHandForce)`; `:13` — «written for a list of
  any length». `CargoBodyContact` зовёт `Record` и из `OnCollisionEnter`, и из `OnCollisionStay`, и
  делит импульс на `Time.fixedDeltaTime`. `AuthoritativeCargoRoster:125` зовёт `NoiseOf` каждый тик
  без проверки «держит ли кто-нибудь».
- `CargoBodyPort` — «The house is the only physical thing in this world the cargo can meet».
  `CargoNoise.cs:57-64` хранит историю удалённого правила ДОСЛОВНО, и пара там **груз ↔ человек**.
  `WalkSpaceProbe.PushOut` возвращает `true` ровно в шаг наезда. `CarryStand.ReadAuthoredWalls:66-78`
  берёт только `HouseWall` и высоту выбрасывает. `HouseRoom.cs:31-32` — «Nothing here is vertical».
  `HousePassage.cs:17` — «Nothing reads it yet»; `:36` — единственное попадание `passable`, в
  комментарии. `HousePlace.cs:13` — `Doorway`.
- **Арифметика `A10` пересчитана и верна:** 50 × 9.81 = 490.5 Н; с расхождением часов
  (0.0167 / 0.02) отчёт даёт ≈**409 Н** против верхнего порога 400; при сегодняшней массе 4 — ≈32.7 Н,
  ниже нижнего порога 60. **Арифметика `W114` верна:** одна рука 200, две 400 < 490.5, три 600 >
  490.5. Обе честно объявлены расчётом по объявленному поведению движка, а не наблюдением, и это
  ограничение записано в самом наборе.

### Шаг 4 — атака на полноту: двенадцать клауз, разбитых атомарно

| кр. | атомов | куда дошли | итог |
|---|---|---|---|
| 1 МАСШТАБ | 8 | `W01`–`W09`, `W113`, `W119`(`open`), `A1`, `C22` | закрыты; `W119` покрывает свою клаузу как `open` с отвечающим — это законный исход, дырой не является |
| 2 ВЕРТИКАЛЬ | 6 | `W10`–`W22`, `A3`, `A4`, `C20`, `C21`, `M1` | закрыты; `W15`/`W16` законно `deferred` его словами |
| 3 ТЕЛЕКИНЕЗ | 5 | `W23`–`W31`, `W114`(`open`), `W121`, `W123`, `A4`, `A8`, `A11`, `A12` | закрыты |
| 4 ШУМ | 7 | `W33`–`W39a`, `W115`, `W120`, `W122`, `A5`, `A10.2`, `C16`, `C28`, `C29` | **`V1` — два последних атома не дошли НИ ДО ОДНОЙ строки; `V2` — «Бесшумно не бывает»** |
| 5 ДОМ | 7 | `W39`–`W49`, `W116`(`open`), `A2`, `A9`, `A10`, `C25`, `C27`, `C29` | **`V3` — атом «а не новым»** |
| 6 ХОЗЯИН | 9 | `W50`–`W63`, `A2`, `A5`, `A6`, `C15`, `C18`, `C19`, `C26`, `M1` | закрыты |
| 7 ЧЕТВЕРО | 3 | `W64`–`W70`, `C08`, `M4`, `M5` | закрыты |
| 8 ЦЕПЬ | 3 | `W71`–`W77`, `W111`, `W118`(`open`), `A6`, `C24` | закрыты |
| 9 ЗАХОД | 4 | `W78`–`W84`, `W112`, `A7`, `C23` | закрыты |
| 10 ОБЛИК | 8 | `W85`–`W92`, `G18` | закрыты |
| 11 СКРИНШОТЫ | 2 | `W93`, `W94`(`open → OWNER`), `W95`, `C06` | закрыты |
| 12 ЗАКРЫТИЕ | 3 | `W84`, `W96`–`W99` | закрыты |
| исключения | 14 | `W31`, `W79`, `W104`–`W108`, `C11`, `C12`, `C30`, `C31` | закрыты |

#### `V1 — критерий 4 потерял ХВОСТ, ровно как первая проверка нашла его СЕРЕДИНУ · complete=FAIL`

Подписанный текст, две последние вещи: «…и **хозяин отвечает на разную громкость по-разному**.
**«Позвать ещё одного» становится решением про риск, а не только про скорость**».

Перемерено по обоим файлам набора: «разную громкость» — **0**; «по-разному» — **0**; «про риск» — **0**;
«ещё одного» — **0**; «выбор команды» — **0**, то есть даже собственное НАЗВАНИЕ критерия набор не
цитирует ни разу. Слово «громкость» встречается семь раз и всегда об ЕДИНИЦЕ (`W37`, `A5`, `C28`,
`G29`) — в чём мерить, а не что от неё меняется.

Ни одна строка не отвечает и ни одна не помечена `open` с отвечающим. Это ровно тот класс, от которого
плей защищает словами «a named `open` row covers its clause»: защита работает только при метке `open`.

**Почему это не мелочь и почему это ХУЖЕ, чем было до его ответов.** Оба его новых токена бьют именно
сюда, и оба стоят под `acceptance`: `W115` записывает, что поднятый несколькими груз «не будет биться об
пол и не будет создавать звук», `W122` — что шаги игрока в узел не входят. Значит после обоих ответов
позвать четвёртого — строго тише и не стоит ничего: чистый выигрыш, а не решение про риск. Ремонт закрыл
середину критерия (`F3`) и не заметил, что его же ответы вынесли хвост.

**Не заполняется здесь.** Это вопрос ГЕЙМПЛЕЯ и по G7 он его.

#### `V2 — «Бесшумно не бывает» держится ОДНИМ импортом, и его же новое слово это отменяет · complete=FAIL`

Клауза 4 открывается словами «**Бесшумно не бывает**». Перемерено: корень «бесшумн» встречается во всём
наборе **ровно один раз** — в `I14` §IMPORT, где стоит его слово 2026-08-03 «бесшумно нельзя». В
арх-файле — **ноль**. Своей строки §WHAT у атома нет.

При этом `W115` (`acceptance`, его слова 2026-08-07) записывает: «он не будет биться об пол и **не будет
создавать звук**», и озаглавлена «тишина выходит из физики». Два его собственных слова о тишине, три дня
врозь, направлены в разные стороны. Набор записывает оба и не называет ни одно против другого — при том
что в каждом другом месте он взвешивает силу каждого токена поимённо.

Это не утверждение, что его новое слово неверно: оно позднее и оно старшее. Дефект в том, что столкновение
не названо, а `I14` продолжает стоять born-closed так, будто ничего не случилось.

#### `V3 — критерий 5 говорит «а не новым», `W45` говорит «ПОЧЕМУ ЭТО НЕ ТОТ ЖЕ ШОВ» · complete=FAIL`

Подписанный текст клаузы 5: «задевание идёт **ТЕМ ЖЕ швом «контакт производит шум», а не новым**».

Перемерено: «а не новым» в `work/converge-g-5a7c.md` — **0 вхождений**; в арх-файле — 2, и оба мимо (одно
цитирует карточку внутри ВОПРОСА `A9`, второе — про подвижную обстановку). То есть §WHAT — раздел, который
копирует `shape`, — вторую половину подписанного предложения не цитирует ни разу.

А `W45` (`answered · acceptance`) пишет прямо: «**ПОЧЕМУ ЭТО НЕ ТОТ ЖЕ ШОВ**… нужен ВТОРОЙ источник того
же слоя, считаемый АНАЛИТИЧЕСКИ внутри правил, — **шов `C29`**». `C29` заведён и назван швом в трёх местах.

Его слово «и груз и человек» старше карточки и не оспаривается. Дефект ровно один и он процедурный:
подписанная строка перестала быть верной буквально, и **набор этого не сказал и никуда не отправил**. У
него в том же файле есть образец, как это делается правильно: `W111` — его приказ снял требование из
карточки, и правка была отправлена в `map` под G9 отдельным нарядом. Здесь ни строки, ни маршрута; вместо
этого метка `acceptance`, то есть строка копируется в `done_when` задачи дословно и покупает ровно ту
работу, которую подписанное предложение исключало.

#### `V4 — из семи уставных линз спрошена одна, и не спрошена та, что бьёт прямее всех · complete=FAIL`

Плей требует назвать хотя бы одно, о чём карточка не спрашивает; не назвать ничего — само находка.
Первая проверка назвала три предмета, все три стали строками (`W116`–`W119`). Названо новое.

Перемерено по обоим файлам: «сложнее» — **0**, «вырезается» — **0**, «коммерческ» — **0**, «канон» — **0**.
Слово «линза» встречается только вокруг `W117` (линза 6).

**Линза 7 — «Делает ли это игру проще или сложнее? Если сложнее — что взамен вырезается?»** — не спрошена
нигде, и она здесь несущая, а не формальная. Собственный триаж набора объявляет ТРИ новых слоя; ремонт и
его ответы добавили сверх этого третий вид записи описания дома (`C27`), второй источник шума (`C28`),
третий (`C29`), шов класса груза (`C30`) и шов личной мощности (`C31`). `CHARTER.md:91-95` держит простоту
жёстким ограничением, и `I53` это ввозит. Ни одна строка набора не спрашивает, что взамен вырезается —
и `W99` («что защищает от бесконечности») отвечает про ВОЛНЫ, а не про сложность.

Заодно не спрошены линза 5 (платный продукт и коммерческое знание) и линза 4 (одобренный канон); их
называю, но отдельными находками не считаю — линза 7 здесь достаточна и она измерима.

### Шаг 5 — атака на протаскивание

#### `V5 — `F6` ВЕРНУЛАСЬ: таблица смыкания, писанная РАДИ того, чтобы счета не разъехались, разъехалась · smuggling=FAIL`

Арх-файл, §SIGNOFF, таблица «Сведение с §COUNT §WHAT, чтобы расхождение двух счётов не повторилось
(`F6` было ровно про это)». Три ряда. Два обновлены ногами 005/006 (§CONTRACTS → 12 + 19,
`contracts_open: 1`; арх-файл → `arch_rows: 12`, `arch_open: 11`). Третий — ряд §WHAT — стоит как стоял:

> `what_rows: 122`, `open_rows: 10`, `deferred: 2`

Измерено командой по текущему файлу: **126 / 9 / 2**. §COUNT-2 того же набора говорит то же самое.
То есть раздел набора продолжает говорить то, что другой раздел ТОГО ЖЕ набора уже называет неверным —
дословная формулировка `F6`, и на этот раз внутри таблицы, заведённой специально против неё.

Не одно место, а три в том же файле:
- шапка `reads:` — «`open_rows: 1` — ЧИСЛО УСТАРЕЛО… `open_rows: 10`». Сейчас 9;
- §SIGNOFF Declare — «двенадцать краёв… и **шестнадцать** внутренних швов». Сейчас 19, и это говорит
  §SIGNOFF того же файла двадцатью строками ниже;
- там же — «ДОБАВЛЕНО РЕМОНТОМ: ждущих строк в §CONTRACTS стало **ДВЕ**». Владелец закрыл `C26` в тот же
  день; ждущая одна, и это говорит тот же файл.

**Почему это не придирка.** §COUNT-2 умеет объявлять раздел замороженным: он дословно пишет, что прежний
§COUNT «оставлен целиком и не правлен», и поэтому §COUNT находкой НЕ является. В арх-файле такого
объявления нет ни одного. Причина расхождения при этом структурная и честная: `converge` в арх-файл не
пишет, а `converge-arch` закончила до возврата ноги его ответов. Это объяснение, а не починка: `shape`
читает эти числа, чтобы мерить волну.

#### `V6 — `F2` осталась в §WHAT, в строке, до которой ремонт не дошёл · smuggling=FAIL`

`W28` (`answered`, диффом подтверждено: не тронута НИ ОДНОЙ ногой): «Четыре поведения по числу
носильщиков — четыре режима к постройке | **НЕТ, это СЛЕДСТВИЕ уже написанной формулы. Работа ровно
одна: вертикальная ось**».

`W114` в том же §WHAT: «ступень «один / двое / трое / четверо» есть **ПРОИЗВЕДЕНИЕ двух свойств** — веса
КЛАССА груза (`A11`/`C30`) и ЛИЧНОЙ МОЩНОСТИ руки (`A12`/`C31`), — и **ни одно из двух не задаёт порог
само**». `C30` и `C31` — два шва, которых в узле не было.

Узкий ответ `W28` («четыре режима строить не надо») выжил и не оспаривается. Умерла её вторая половина —
«работа ровно одна», — и это ДОСЛОВНО то утверждение, которое `F2` опровергла в `A4` («критерий 3 куплен
даром»). В `A4` оно снято; в §WHAT — стоит. §WHAT копирует `shape`, арх-файл не копирует никто.

#### `V7 — новый вопрос ВЛАДЕЛЬЦУ живёт только в арх-файле и ВНУТРИ `acceptance`-строки · smuggling=FAIL`

Его собственная скобка родила вопрос, которого не было: что такое «интерактивный элемент» обстановки.
Набор записывает его честно и никуда не расшифровывает — это проверено и это правильно.

Но живёт он в трёх местах, и ни одно не является `open`-строкой §WHAT: §NOT-DECIDED арх-файла (арх-файл
в `done_when` не едет НИКОГДА — так объявляет сам набор), текст `C25`, и текст `W45`, у которой метка
`answered · acceptance`. Счёт это фиксирует прямо: `open_owed_by_owner: 1` (только `W94`),
`contracts_open: 1` (только `C05`), и «краем или швом она не становится, поэтому `contracts_open` не
трогает».

**Набор сам написал правило, которое это ловит.** Про требование «покоящийся груз не шумит» он пишет
дословно: «арх-файл в `done_when` не едет НИКОГДА… и если оставить его только здесь, **оно потеряется на
`shape`**» — и поэтому завёл строку `W120`. К вопросу владельца то же правило не применено. Итог: в наборе
владельцу должны ДВА вопроса, а счёт §WHAT показывает один, и второй едет к `shape` спрятанным внутри
строки, помеченной как отвеченная.

### Шаг 6 — шесть находок первой проверки: не вернулись ли (наряд, `done_when` 3)

| находка | где смотрел | вернулась? |
|---|---|---|
| `F1` (`C26` объявляет состояние двери куплённым кр. 6) | `C26` целиком, `A9` §«Что про двери куплено», `A9` §«ЕГО ОТВЕТЫ» п.1, `W63`, все 15 вхождений корня «куплен» в обоих файлах | **НЕТ.** Утверждение снято и прямо помечено «не воскресает и не подтверждается» |
| `F2` (критерий 3 куплен даром) | `A4` целиком, все вхождения «даром», `W28`, `W114` | **В `A4` — нет.** В §WHAT — **ДА**, строкой `W28`: см. `V6` |
| `F4` (пол не спрошен) | `A10`, `A10.1`, `A10.2`, `W116`, `C27`, `C28`, §MINER | **НЕТ.** Разобран двумя опровергнутыми таблицами; `W116` честно осталась `open → PLAN` |
| `F5` (прочтение «несущие задевают» не понижено) | `A9` оба места, `C25`, `W45`, `C29` | **НЕТ.** Понижено, потом закрыто его словом; обе читки приняты, дороже́ прочтения ноги |
| `F7` (хеджированный токен усиливает твёрдый вердикт) | `A9` таблица классов строка 1, `LOG.md:17` | **НЕТ.** «Сильнее» снято и заменено отрицанием; `LOG` за `s-converge-arch-g-5a7c-003` задним числом не правлен — проверено, строка на месте |
| `F9` («прицел вместо близости» решён внутри выжившего варианта) | `A8` строка (б) и разбор, `W121`, §NOT-DECIDED | **НЕТ.** Понижено, потом опровергнуто ЕГО ЖЕ ответом, и набор говорит это прямо: «его словом ОПРОВЕРГНУТА, а не подтверждена» — ровно как требовал наряд |

### Шаг 7 — пять НОВЫХ строк `acceptance` проверены особо (наряд, `done_when` 4a)

| строка | источник ВНЕ ноги | проверяемая форма | поехало ли лишнее | вердикт |
|---|---|---|---|---|
| `W115` | его слова 2026-08-07 дословно, на переформулированную развилку, которую он раньше не понимал | «тишина выходит из физики; слой НИКОГО НЕ СЧИТАЕТ; числа кривой `→ PLAN`» | форма кривой («шумит УДАРАМИ, а не давлением») ВЫВЕДЕНА и помечена именно выводом, а не его словами | **держится**, с оговоркой в шаге 8 |
| `W120` | его требование дословно, без единой оговорки, в повелительном наклонении | **есть, и это лучшая строка набора:** «никто не держит и он НЕ ДВИЖЕТСЯ» вместо непроверяемого «без взаимодействия»; граница покой/движение названа явно | нет; механизм остаётся `→ PLAN` | **держится** |
| `W121` | «Прицелом и близостью» — без оговорок | «кандидат тот, на который смотришь, И он в пределах дальности» — одно правило с двумя условиями | нет; вторая половина фразы («я думаю… потом») уехала ВХОДОМ в `W26`, и строка прямо говорит, что ОЩУЩЕНИЕ управления она не покупает | **держится** |
| `W122` | «этого пока в ядро не надо делать» | «задел предмет = СОБЫТИЕ и шумит; идёт = состояние и не шумит» | нет; и строка сама запрещает читать себя как отмену `C29` | **держится**, см. оговорку ниже |
| `W123` | «это надо заложить… но заложить такое надо» — повелительное, без оговорок | «потолок берётся из личной мощности ЭТОГО хвата, сегодняшнее общее число — значение по умолчанию, наблюдаемое поведение не меняется ни на грамм» | нет; шов перемерен мной первой рукой и действительно пуст | **держится** |

**Оговорка по `W122`, названная ради точности и находкой НЕ являющаяся.** Его «наверное… пока» смягчено
ровно так же, как «я думаю пока не нужно» про двери, — а двери своей `acceptance`-строки не получили
(`W63` осталась `→ PLAN`). Асимметрия есть. Но она законна: дверной токен ответил на вопрос МАРШРУТА, чей
ответ `→ PLAN` уже был верен, а токен про шаги ставит ГРАНИЦУ ОБЪЁМА, у которой своего носителя не было
вовсе. Записано как есть; понижения не требую.

### Шаг 8 — сила каждого токена владельца (наряд, `done_when` 6)

Девять токенов, все проверены на «не выше настоящей». **Подписей под текстом карточки — НОЛЬ, и ни один
токен подписью не назван. Повышения до подписи нет ни в одном месте.** Правка `F7` проверена отдельно и
держится (шаг 6).

Одно место, где форма записи на ступень выше слов, — **и оно названо как оговорка, а не как находка**:
`W115` и `A10.2` пишут «**ПРАВИЛО СИЛЬНЕЕ САМОГО ОТВЕТА, И ОНО СКАЗАНО БЕЗ ОГОВОРКИ: слой шума НИКОГО НЕ
СЧИТАЕТ**». Этого предложения он не произносил — он сказал «это должно САМО ВЫТЕКАТЬ» на прямо
поставленную развилку «само из физики или отдельной ручкой, которая считает руки». СОДЕРЖАНИЕ держится:
развилку он закрыл сам, и его же «если вас больше… но это должно само вытекать» покрывает и число людей,
а не только число держащих. Выше настоящей оказалась АТРИБУЦИЯ формулировки, не выбор. Понижения это не
требует — источник вне ноги настоящий, — но следующая нога не вправе цитировать эту фразу как его слова.

### Шаг 9 — утечка HOW в обе стороны (наряд, `done_when` 7)

- **В `acceptance`**: величины, формат, частота, раскладка — не найдено ни одной, заехавшей ЦЕЛЬЮ.
  Числа в `W41` (1.5 / 1.7), `W123` (200, 1) и `W115` (50, 409, 400) стоят как `факт кода` — измерение
  сегодняшнего состояния и основание для «значения по умолчанию», а не назначенная величина. Понижения
  `F8` держатся: `W39`, `W54`, `W73`, `W94`, `W39a` метку не носят, и `how_leak` прежнего §COUNT верен.
- **`arch_in_context_only`: держится.** Ни одна строка `A1`–`A12`, включая всю `A10`, не помечена
  требованием и в `done_when` не едет. То, что доехало до `W115` и `W123`, — это КОНТРАКТНОЕ поведение
  (`C28`, `C31`), а §CONTRACTS живёт в файле `converge`, не в арх-файле. Граница соблюдена.
- **Карточка не тронута ни знаком** — диффом, а не чтением (шаг 2).

### Шаг 10 — главная проверка наряда: «заложить ≠ сделать» против подписанной строки исключений

Строка исключений перечитана мной ПЕРВОЙ РУКОЙ по `TREE.md:133`. Она называет дословно «прокачка,
магазин, бросок» и «несколько типов лута и их ценность».

**Прочтение ПОДТВЕРЖДАЮ, и не на удобстве, а на тесте, который набор сам себе поставил.** После `C31`:
ничто в игре не улучшается со временем, экрана нет, ступеней нет, стоимости нет, сохранения между
заходами нет, и наблюдаемое поведение не меняется ВООБЩЕ, потому что значение по умолчанию равно
сегодняшнему общему числу — я перемерил, что это число существует (`_cargoMaxHandForce: 200`) и что поле
хвата сегодня никем не читается. После `C30`: класс один, предмет один, второго типа лута нет, ценности
нет вовсе, и `W79` (его слово: один предмет лута) держит границу. `C08` («ничто не имеет права зашить
двойку») выполняется — `MaxHolders` остаётся ЧИСЛОМ, переехавшим в класс.

Третий довод набора — самый сильный и он проверен отдельно: `W31` стоит `answered · acceptance` с
формулировкой «НЕ здесь. Шов объявлен, адресата в состоянии НЕТ» ЕЩЁ ДО его реплики. Различение шва и
системы существовало до токена, а не сочинено под него.

**Узел исключённого его подписью НЕ съел.** Это самая дорогая проверка наряда, и она PASS.

### Шаг 11 — `C29` не воскрешает убитое им правило

Проверено первой рукой: `CargoNoise.cs:57-64` хранит историю дословно, и пара там **груз ↔ человек** («he
does not want a beam meeting a person to fetch the householder»). `C29` — пара **человек ↔ обстановка**.
Разные пары; воскрешения нет, и набор запрещает такую ссылку в трёх местах. **PASS.**

---

## state_changes

**Потолок применён: ни одного ремонтного наряда не выдано.**

1. `live/indie-game-development/NOW.md` → `open_calls`: удалить запись `c-converge-verify-g-5a7c-002`
   целиком (наряд вернулся). Добавить `#`-строку записи о втором FAIL, применённом потолке,
   замороженной поверхности и ИЗМЕРЕННОМ счёте для следующей ноги.
2. `live/indie-game-development/NOW.md` → `decisions`: добавить `d-g-5a7c-paper-ceiling-001` —
   ОДНО решение владельца с тремя ветками, рекомендацией и названным доводом ПРОТИВ рекомендации.
3. `live/indie-game-development/LOG.md`: одна строка сверху.
4. `live/indie-game-development/history/2026-08-07-s-converge-verify-g-5a7c-002.md`: этот RESULT целиком.

Не тронуто: `TREE.md`, `CHARTER.md`, `knowledge/**`, `work/converge-g-5a7c.md`,
`work/converge-g-5a7c-arch.md` (поверхность заморожена уликой — правки в неё запрещены потолком),
продуктовый репозиторий. Ставка не открыта, задач и полос нет.

---

## captures

- **Класс, увиденный дважды подряд на одном узле: ремонт закрывает НАЗВАННУЮ дыру и не перечитывает
  соседнее предложение той же клаузы.** `F3` закрыл середину критерия 4 — `V1` нашла его хвост в
  следующем предложении. Возможное правило для `converge`: клауза чинится ЦЕЛИКОМ и перечитывается
  атомарно после починки, а не в объёме находки.
- **Раздел, писанный ради будущей проверки, стареет быстрее остальных.** §REPAIR-LEDGER и таблица
  смыкания были написаны специально против `F6` — и разъехались первыми, потому что их обновляет нога,
  которой уже нет. Возможное правило: у такой таблицы обязан быть либо владелец, либо объявление
  «заморожено», как это сделал §COUNT-2.
- **`W28` — второй случай подряд, когда несущее утверждение живёт в строке, которую наряд ремонта не
  назвал.** Ремонт правит по списку находок; строка вне списка стареет молча.

---

## decisions_needed

**ОДНО, и оно предписано потолком плея. `d-g-5a7c-paper-ceiling-001`.**

Вопрос: бумажная фаза `g-5a7c` кончилась вторым отказом. Куда узел идёт дальше.

- **а) ИДТИ НА ТОМ, ЧТО ОТВЕЧЕНО** — открыть `shape` прямо сейчас; девять открытых строк едут в волну
  НАЗВАННЫМИ (это механически защищено: `shape` не вправе их потерять), а четыре неотвеченных атома
  критериев 4 и 5 остаются в карточке нерешёнными и решаются его глазом на первой же сборке.
- **б) ДЕЛИТЬ В `map`** — разрезать карточку на две: буквально строящаяся половина (масштаб, вертикаль,
  телекинез, дом, сеть, заход, облик, скриншоты) идёт в `shape` сразу, а «шум как выбор команды»
  становится своим узлом со своим `converge` позже.
- **в) РЕЗАТЬ** — правкой карточки в `map` снять из критерия 4 хвост про риск и различимый ответ на
  громкость, а из критерия 5 слова «а не новым», приведя подписанный текст к тому, что набор реально
  отвечает.

**Рекомендация: (а), идти на том, что отвечено.**
Ни одна находка не является структурной неизвестностью, которая сделает постройку неверной: четыре из
семи — «предложение карточки не дошло ни до одной строки», две — устаревший счёт и устаревшая строка.
Против узла работает измеренная запись самого направления `i-paper-loop-outran-the-build-001`: тяжёлый
`converge` на `g-8f31` дал шесть ног и ноль строк игрового кода — здесь уже двенадцать ног и ноль строк.
А два настоящих геймплейных атома набор САМ отправляет к его глазу, а не к бумаге (`M7`, и прямая
пометка в `C29`, что задача обязана показать ему цену прежде, чем строить).

**Довод ПРОТИВ рекомендации, названный честно и он не слабый.** «ШУМ — ЭТО ВЫБОР КОМАНДЫ» — это
НАЗВАНИЕ критерия 4, а «позвать ещё одного = решение про риск» — единственная строка всей карточки, из-за
которой кооператив является игрой, а не симулятором переноски. Идти в `shape` сейчас значит построить
слой шума, механически правильный и геймплейно пустой; он в него сыграет, скажет «не интересно», и узел
откроется снова — только на одну сборку позже и уже с кодом вокруг неверного допущения. Если окажется,
что риск обязан приходить из источника, который должен существовать в слое с первого дня, то `C16`, `C28`
и `C29` спроектированы без самого важного потребителя, и это уже структурно. Второй довод меньше, но
конкретен: `shape` мерит волну по счёту, а таблица смыкания арх-файла показывает 122/10 вместо
измеренных 126/9.

**Только его слова открывают третий круг бумаги.**

---

## play_check

```
1 recite:        verify_target: build. Претензия восстановлена по карточке TREE.md g-5a7c:117-142 и по
                 обоим файлам набора; рассуждение решавших ног не читалось — ни один history-файл
                 converge/converge-arch этого узла не открыт, кроме RESULT ПЕРВОЙ ПРОВЕРКИ (он нужен
                 done_when 3 и решающей ногой не является)
2 completeness:  двенадцать клауз разбиты атомарно (69 атомов), каждый сопоставлен со строкой §WHAT,
                 §CONTRACTS или A-строкой. Чек-листа класса узла в knowledge/ нет — не применялся и
                 не сочинялся. Четыре атома НЕ дошли ни до одной строки: V1 (два атома кр. 4),
                 V2 (кр. 4 «Бесшумно не бывает»), V3 (кр. 5 «а не новым»). Названо неспрошенное:
                 уставная линза 7 (V4), плюс линзы 4 и 5. Строки open с названным отвечающим
                 (W39a, W54, W73, W94, W114, W116, W117, W118, W119) свои клаузы ПОКРЫВАЮТ и дырами
                 не названы
3 smuggling:     каждая несущая строка проверена на источник вне решающей ноги. Пять новых acceptance
                 держатся на его прямой речи (шаг 7). Понижение не потребовалось ни для одной строки.
                 Найдено три расхождения: V5 (три места арх-файла говорят то, что §COUNT-2 называет
                 неверным — F6 вернулась), V6 (W28 против W114 внутри §WHAT — F2 вернулась),
                 V7 (owner-owed вопрос без строки §WHAT). Утечка HOW в acceptance не найдена;
                 arch_in_context_only держится
4 close:         complete=FAIL smuggling=FAIL. ВТОРОЙ FAIL узла (первый —
                 history/2026-08-07-s-converge-verify-g-5a7c-001.md). ПОТОЛОК ПРИМЕНЁН: возврата в
                 converge/converge-arch НЕТ, ремонтных нарядов не выдано, поверхность заморожена
                 уликой, владельцу вынесено ОДНО решение d-g-5a7c-paper-ceiling-001 с тремя ветками,
                 рекомендацией и доводом против неё. §SIGNOFF не пишется — он пишется только при PASS

verify_target: build complete=FAIL smuggling=FAIL
second_verification: PASS
ceiling_applied: YES — второй FAIL; репарационных CALL не выдано ни одного, решение владельцу заведено
fan_out: NONE — прямая постоянная инструкция владельца в этом харнессе не разворачивать субагентов и
                workflow; по os/KERNEL.md живая инструкция владельца выше плея. Второе независимое
                опровержение, а не второй веер
base_reverified: C:/projects/Unity/GasCoopGame main = 352f96b0911f3dc43f1dc5512e3ac7b20c4de4fa,
                дерево чисто, только чтение — перепроверено ДО чтения набора
counts_remeasured: what_rows 126 · open_rows 9 · open_owed_by_owner 1 · open_owed_by_plan 7 ·
                open_owed_by_shape 1 · deferred 2 · acceptance 47 · acceptance+open 0 · answered 93 ·
                улика 48 · →PLAN 23 · контрактов 12+19 · arch_rows 12 · MINER 7 — §COUNT-2 верен
                до строки; арх-файл в трёх местах устарел (V5)
untouched_verified_by_diff: TREE.md, CHARTER.md, knowledge/** — ни в одном из шести коммитов набора;
                арх-ноги тронули 0 строк §WHAT; converge-ноги тронули 0 рядов §CONTRACTS; §COUNT-2
                вставлен без единой удалённой строки; A1/A3/A6/A7 байт в байт не тронуты ремонтом
first_check_findings: F1 нет · F2 ДА в §WHAT (V6) · F4 нет · F5 нет · F7 нет (LOG:17 не правлен) ·
                F9 нет
exclusion_line_reread: PASS — прочтение «заложить ≠ сделать» подтверждено первой рукой по TREE.md:133
owner_tokens: девять; повышения до подписи нет ни одного, подписей под текстом карточки 0. Одна
                оговорка по форме атрибуции в W115/A10.2, понижения не требует
owner_present: NO — владельца в этой сессии нет, ни одного его слова здесь не добыто и не приписано
```

---

## log

2026-08-07 | s-converge-verify-g-5a7c-002 | converge-verify | direction | ВТОРАЯ ПРОВЕРКА `g-5a7c`
вернула `complete=FAIL smuggling=FAIL` — ПОТОЛОК ПРИМЕНЁН, бумажная фаза узла закрыта, ремонтных
нарядов не выдано ни одного, владельцу вынесено ОДНО решение с тремя ветками.

---

## next

**Ничего не открыто и не выдано — так предписывает потолок.** Единственный законный ход —
`d-g-5a7c-paper-ceiling-001`: его слово выбирает одну из трёх веток, и только его слово откроет третий
круг бумаги.

Куда ветки ведут, когда он ответит:
- (а) → `shape` по `g-5a7c`. Механическая защита уже действует: `shape` не вправе потерять ни одну из
  девяти открытых строк — каждая едет в срез по id или в повестку `→ PLAN`, а строка, чей отвечающий
  ВЛАДЕЛЕЦ (`W94`), обязана нести его слова;
- (б) → `map` по `g-5a7c` (правка карточки, G9, его подпись), затем `shape` по строящейся половине;
- (в) → `map` по `g-5a7c` (правка карточки, G9, его подпись), затем `shape`.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-converge-verify-g-5a7c-002.md
