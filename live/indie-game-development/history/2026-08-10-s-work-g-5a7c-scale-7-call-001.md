# RESULT — s-work-g-5a7c-scale-7-call-001

play: work · close: light — because каждая строка этой ноги это механический факт, перемеренный
первой рукой: продуктовый возврат прочитан целиком по своему пути, его заявление о выполненном шве
проверено `grep` по байтам кандидата, семь читателей размеров груза найдены поиском, а решение
владельца о следующей задаче и о следующей волне процитировано дословно
direction: indie-game-development · node: g-5a7c · task: t-scale-7 · call: — (нога открыта прямым
словом владельца 2026-08-10 «давай вот класс груза, эту t-scale-7 задачу»; зарегистрированного
session-CALL у неё нет, и это названо, а не сглажено)
date: 2026-08-10

base_read: OS — `wt/indie-game-development` = `f28c2300`; посторонние правки параллельной сессии за
время предыдущей ноги были закоммичены ею самой (`cd4c09e4` и три panel-коммита), рабочее дерево
чистое до начала этой ноги. Продукт: `origin/dev` = `origin/main` =
`9786da4fe1e6c52f0ee8838cf093db1701b3b8da`; `origin/slot/win-u1` =
`c8861472110457ee1337e121cce83ef070e4dac2`. Локальный `dev` = `4699647d` (`tools: add autonomous
task preflight`) — чужая работа `WIN-CTRL`, этой ногой не тронута. Продуктовые байты, ветки, слоты и
slot-state НЕ изменялись: только чтение.

## outcome

Наряд на `t-scale-7` написан и зарегистрирован: `c-exec-g-5a7c-cargo-class-seam-001`,
**`status: blocked`** — он трогает ровно те файлы, которые `WIN-CTRL` сейчас публикует, поэтому
`basis` и `slot` в нём намеренно пусты и вписываются при отправке.

**Задача усохла вдвое, и это проверено байтами, а не принято с отчёта.** Карточка `t-scale-7` несёт
два шва. Первый — «потолок усилия берётся из личной мощности хвата, сегодняшнее общее число стало
умолчанием» — **уже выполнен внутри `t-scale-6`**: `_cargoMaxHandForce` отсутствует во всём коде,
`_cargoHoldStrength: 200` копируется в `CargoHold.Strength`, и `CargoHaul.cs:169` читает потолок из
руки. Наряд несёт только второй шов — класс груза — и прямо запрещает переделывать первый.

**Главная находка наряда: шов, дошедший только до `CargoRules`, был бы ложным.** Поиском найдены
семь читателей размеров и массы груза, и три из них ходят в общую настройку МИМО правил —
`CargoBody.Spawn` (коллайдер и масса тела), `NetworkCargoPresentation` (рисуемая коробка),
`StartConditionsEditor` (редакторский инструмент). Если шов остановится на правилах, в день второго
класса правило ответит по-новому, а тело, рисунок и инструмент останутся одного глобального размера
— и это вскроется тогда, а не сейчас. Наряд требует, чтобы шов дошёл до всех семи, и доказывался
тестом со вторым классом, а не заявлением.

**Две его амбиции 2026-08-10 записаны идеями, а не требованиями.** Ручное управление положением И
ПОВОРОТОМ груза (`idea-manual-cargo-control-001`) и комплексная система распознавания шума
(`idea-complex-noise-recognition-001`) — обе с его дословными словами и обе отнесены им же в
СЛЕДУЮЩУЮ волну: «Я не говорю, что сейчас прямо оно должно быть… сейчас можем закрыть волну».
Важное различие, записанное в первой идее: шов под ПОЛОЖЕНИЕ уже заложен в `t-scale-6`, а под
ПОВОРОТ — нет, потому что наклон груза X/Z заморожен вырезом 1 ставки.

`t-scale-6` этой ногой НЕ закрывается: `WIN-CTRL` ещё должен интегрировать, доказать откат, пройти
Deliver, опубликовать и освободить слот, а закрытие Направления — отдельная нога, читающая возврат и
его слова первой рукой.

## evidence — перемерено первой рукой

**Продуктовый возврат прочитан целиком** по пути
`C:/projects/Unity/GasCoopGame_win-u1/docs/results/c-exec-g-5a7c-more-hands-lift-higher-001.md` на
`slot/win-u1` = `c8861472`. Кандидат `9f4eb6cf897e83f6cdda5dcf672ae62110c7ccf1`, дерево кандидата
`164a79865df4bda18341a32f088eb8e0b77aa4b5`, база `9786da4f` — совпадает с базой выданного наряда.
Статус в отчёте: «OWNER-ACCEPTED CANDIDATE on `slot/win-u1`; awaiting `WIN-CTRL` handback».

**Заявление отчёта о первом шве проверено независимо, а не принято:**

| Проверка | Результат |
|---|---|
| `grep -rn "MaxHandForce" Assets/ tests/` | пусто — величина удалена из всего кода |
| `NetworkPlaySettings.asset` | `_cargoHoldStrength: 200`, `_cargoMass: 48`, `_cargoMaxHolders: 4`, `_cargoVerticalDampingRatio: 1`, `_cargoHandForceReachFalloff: 1` |
| `CargoHaul.cs:169` | `var limit = hold.Strength;` — правило смотрит в руку |

**Семь читателей размеров/массы найдены поиском** по `Assets/TunnelCrew/**`: `CargoGeometry.cs:38-39`
и `:98-100`; `AuthoritativeCargoRoster.cs:247-249`; `CargoHaul.cs:225` и `:282-283`;
`CargoBody.cs:51-53` и `:67`; `NetworkCargoPresentation.cs:118-120`; `StartConditionsEditor.cs:227`.
Первые четыре идут через `CargoRules`, последние три — мимо.

**Слова владельца при приёмке `t-scale-6`, взятые из отчёта дословно:** «первый, да, было по полу,
второй игрок чуть повыше, третий еще чуть повыше»; «можем закрывать, да, с такой пометкой, что не
сильно высоко, но мы оставили, потому что все равно будет управляться вертикальность»; «работает по
всем пунктам». Ступень сделана одним числом — масса `20 → 48`; высоты `0.020 / 0.056 / 0.068 /
0.074`; коэффициент затухания удержан на `1` при любом числе рук, то есть находка о пружинистости
закрыта, а не обойдена. Тесты **144 passed**, гигиена `OK`, Unity `6000.5.5f1` чист.

**Слово владельца о следующей задаче и о следующей волне, дословно:** «ручное управление в следующей
волне будем делать… я хочу, чтобы понимали, то есть управление там, вертикально, поворот вверх,
вниз, вбок… к чему мы стремимся… чтобы там была заложена под это структура»; «У нас должна быть
комплексная система распознавания шума в игре. Потом. Я не говорю, что сейчас прямо оно должно
быть»; «сейчас можем закрыть волну… давай вот класс груза, эту t-scale-7 задачу».

## Что эта нога НЕ сделала — записано против себя

- **`t-scale-6` не закрыта и не выдаётся за закрытую.** Продуктовый handback это улика. Отчёт сам
  оставляет `WIN-CTRL` откат-proof, Deliver, публикацию и освобождение слота; третья строка
  `done_when` того наряда до Control proof не закрыта.
- **Наряд не отправлен и отправлен быть не может:** `basis` и `slot` пусты по построению, карточка
  стоит `blocked` со своим `unblock_when`.
- **Расчётная находка о шуме из возврата не разобрана здесь.** Отчёт считает: одиночка на полной
  скорости тратит `195.12` Н из `200` на горизонталь, остаётся `~43.9` Н вертикали, остаточное
  давление на пол `~100.1` Н даёт доложенные `~83.4` Н — это полоса `QuietRustle` (`60 ≤ 83.4 <
  150`), то есть до `~60` стимулов в секунду при непрерывном волочении. Это расчёт из живых настроек,
  а не замер. **Место этой находки — закрытие `t-scale-6`**, где её надлежит завести уликой; здесь
  она названа, чтобы не потеряться, и в идею о шуме внесена как смежная запись.
- **Пометка владельца «очень мало» про величину ступени** живёт пока только в продуктовом отчёте и в
  идее `idea-manual-cargo-control-001`. Закрепить её уликой — тоже работа закрывающей ноги.

## state_changes

1. Зарегистрировать call card `c-exec-g-5a7c-cargo-class-seam-001`: `_kind: call`,
   `status: blocked`, `unblock_when` про публикацию `t-scale-6` и слово владельца о слоте,
   `to: executor`, `for: t-scale-7`, `issued: 2026-08-10`,
   `call: work/c-exec-g-5a7c-cargo-class-seam-001-call.md`, `repo: ainazemtsau/GasCoopGame`,
   `engineering_contract: 36`.
2. Завести idea card `idea-manual-cargo-control-001`: `about: g-5a7c`, `from: владелец`,
   `opened: 2026-08-10`, source — этот отчёт; блоки `idea` и `his_words` с его дословными словами.
3. Завести idea card `idea-complex-noise-recognition-001`: те же поля; блоки `idea` и `his_words`.
4. `t-scale-6`, `t-scale-7`, `g-5a7c`, `NOW.bet` и открытый exec call
   `c-exec-g-5a7c-more-hands-lift-higher-001` НЕ трогать: их разбирает закрывающая нога.
5. Одним `osctl leg close` сохранить этот RESULT в
   `history/2026-08-10-s-work-g-5a7c-scale-7-call-001.md` и добавить log ровно один раз в журналы
   `g-5a7c`, `t-scale-7`, нового exec call и обеих идей.

## captures

- Расчётная находка о шуме при волочении и пометка владельца о величине ступени — обе принадлежат
  закрывающей ноге `t-scale-6` и названы выше поимённо, чтобы она их не пропустила.

## decisions_needed

Нет. Развилка «класс груза или ручное управление» закрыта его прямым словом того же дня.

## play_check

- 1 recite: done — `t-scale-7` и её два шва сведены с текущими байтами; задача служит активной
  ставке `g-5a7c` и является последней карточной задачей волны
- 2 owner inputs (owner): done — он выбрал задачу дословно («давай вот класс груза, эту t-scale-7
  задачу»), отнёс управление и шум в следующую волну («Потом. Я не говорю, что сейчас прямо оно
  должно быть») и потребовал, чтобы структура была заложена
- 3 do the work: done — возврат прочитан, его заявление проверено байтами, семь читателей найдены,
  наряд написан и зарегистрирован закрытым, две идеи заведены
- 4 self-check: done — не сделанное названо против себя, включая незакрытую `t-scale-6` и
  неразобранную находку о шуме
- 5 close: done — новый exec call зарегистрирован `blocked`, идеи заведены, задача остаётся открытой

## Гейты

- **G5.** `close: light` заявлен построчно: всё, на чём стоит эта нога, — прочитанные байты, вывод
  `grep`, путь файла и уже произнесённые владельцем слова. Ни одна строка не требует суждения,
  которого никто не сделал. В продукте ничего не строилось и не запускалось.
- **G7.** Геймплей не решён ногой: величина ступени, ручное управление и система шума не выбраны
  здесь; его пометка «очень мало» записана как его слово, а не как требование.
- **G3.** Аппетит не растянут: обе амбиции ушли идеями в следующую волну, а не в наряд. Волна
  остаётся семизадачной.
- **G9.** Карточка узла не редактировалась — только строка в её журнал. `done_when` задач не тронуты.
- **Идея никогда не требование** (`os/schema/direction-files.md` §idea): у обеих проставлен
  `from: владелец`, обе несут его дословные слова, ни одна не превращена в строку `done_when`.
- **Панель.** Направление панели не объявляет — регенерация пропущена.

## log

наряд на класс груза написан и держится закрытым до публикации t-scale-6: половина задачи оказалась уже сделанной внутри прошлой ноги, шов обязан дойти до семи читателей, а его ручное управление и система шума записаны идеями следующей волны

## next

`CALL c-exec-g-5a7c-cargo-class-seam-001` — разблокировать и отправить, когда `WIN-CTRL` опубликует
интегрированный `t-scale-6` и освободит слот, а владелец назовёт слот. **Раньше этого идёт отдельная
нога закрытия `t-scale-6`:** она читает продуктовый возврат и его слова первой рукой, заводит уликой
находку о шуме при волочении и его пометку о величине ступени. После `t-scale-7` волна закрывается
через `review`.

END_OF_FILE: live/indie-game-development/history/2026-08-10-s-work-g-5a7c-scale-7-call-001.md
