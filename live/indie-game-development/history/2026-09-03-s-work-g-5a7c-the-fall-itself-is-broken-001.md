RESULT s-work-g-5a7c-the-fall-itself-is-broken-001 (call: — владелец-инициированная, без родительского CALL)
direction: indie-game-development   track: тело   play: work   node/task: t-the-bench-shows-why-he-fell-1

outcome: |
  **СТЕНД СДЕЛАЛ РОВНО ТО, РАДИ ЧЕГО СТРОИЛСЯ, И ПЕРВЫМ ЖЕ ПОКАЗАЛ, ЧТО ЗАПИСЬ НАПРАВЛЕНИЯ О
  РАВНОВЕСИИ ЛОЖНА.** Разобрано 92 агентами по коду продукта с проверкой каждой гипотезы тремя
  независимыми скептиками; всё несущее направление перечитало в файлах своими глазами.

  **ГЛАВНОЕ, И ЭТО НЕ ТО, ЧТО ДУМАЛИ ОБЕ СТОРОНЫ.** Формулировка «механика равновесия опубликована
  и доказана физикой» жила в четырёх местах. Нога стенда поправила её так: «доказывалась в доме,
  хозяин усажен, пол — толстые коробки; на голом полигоне разваливается; режим был у́же, чем
  считали». **Эта поправка тоже неверна.** Проверено в
  `Assets/Tests/TunnelCrew/Networking/HouseholderBalancePhysicsTests.cs`: докблок — «No scene is
  saved»; пол — `GameObject.CreatePrimitive(PrimitiveType.Plane)` на слое 0 (строки 60-62); хозяин —
  голый `new GameObject` (строка 64); `NetworkHouseholder` создан на выключенном объекте
  (`logOwner.SetActive(false)`, строка 113) и **обратно не включается нигде** (все четыре
  `SetActive` в файле — 65, 70, 113, 373).
  **Режим доказательства и БЫЛ полигоном.** Полигон 03.09 ничего не сузил — он показал, что
  доказательство никогда не покрывало то, за что его читали. «Усаженность» к равновесию отношения
  не имеет, и это написано словами: `Network/NetworkHouseholder.cs:215-218` — «AdvanceBalance runs
  whether or not he is seated».

  **ДВА СЛЕДСТВИЯ, КАЖДОЕ ПРОВЕРЕНО ОТДЕЛЬНО И КАЖДОЕ ХУЖЕ ИСХОДНОГО СИМПТОМА.**
  (1) **Подъёма нет ни в каком виде, и это решение, объяснённое в коде:**
  `Presentation/HouseholderBalanceBody.cs:104-106` — «The authored controller has no get-up states.
  Recovery is therefore the bounded host phase below…» и `_behaviour.canGetUp = false`.
  (2) **«Он встал» — утверждение таймера, а не тела:**
  `Core/Householder/HouseholderBalance.cs:109-128` — когда `Recovering` истекает, **обе ветки
  возвращают `Stable`**; единственное последствие всё ещё наклонённого тела — флаг `NeedsRearm`.
  И порог падения `35°` физикой не проверялся ни разу: падение в доказательстве пришло от
  вендорского `Unpinned` на `10,7°`.

  **ЧЕГО НАПРАВЛЕНИЕ СКАЗАТЬ НЕ МОЖЕТ, И ЭТО НАЗВАНО ЧЕСТНО.** Причина просадки. Семь механизмов
  были выдвинуты и **все семь опровергнуты** по коду тремя скептиками каждый: кинематический
  телепорт на замёрзшую цель; слепота прибора как замкнутая петля; капсула как причина; отсутствие
  `Continuous` у мышц; назначение скоростей на смене режима; остаточные скорости после сброса;
  корутина смены режима вне шагов ручной симуляции. Чтением это не закрывается — нужен живой замер.

  **И ПОСТАНОВКА ВОПРОСА ПОПРАВЛЕНА ЧИСЛОМ.** Таз `y = −0.324` лежит **внутри** толстой коробки
  (`Ground Solid`: `m_LocalPosition.y = −0.52`, `m_LocalScale.y = 1` → объём −1.02…−0.02;
  `Scenes/HouseholdLab.unity:826-838`). Это просадка примерно на **30 см в пол, а не пробой
  насквозь** — вопрос про депенетрацию и решатель, а не про туннелирование.

  **ОТВЕТЫ НА ПЯТЬ ВОПРОСОВ ВОЗВРАТА.** Р3 — решение, объяснено в коде, закрыт. Р5 — запись ложна,
  и поправка ноги тоже; закрыт уликой. Р4 — да, приёмка обязана знать про высоту: смещение уже
  считается рядом со слепым наклоном (`HouseholderBalanceBody.cs:203`), прибор на него не смотрел.
  Р2 — гипотеза про капсулу как причину опровергнута, но конфигурация дефектна: матрица выключает
  пару `Ragdoll(9) × CharacterController(8)`, а капсула-носитель лежит на `Default(0)`
  (`World/Householder.prefab:47`), и вендорский сторож смотрит на `targetRoot` со слоем 8
  (`BehaviourPuppet.cs:513-519`) — защита есть и не покрывает то, ради чего заведена.
  Р1 — остаётся открытым и уходит в живой замер.

  **СЛИЯНИЕ РАЗРЕШЕНО.** Раздел СТОП в отчёте — находка, а не недоделка наряда; флаг
  `-AllowBlockedReport` разрешён явно. Пробное слияние `slot/win-u4` = `8182c984` в текущий
  `main` = `332488de` даёт **ноль маркеров конфликта**; три файла изменены с обеих сторон, среди них
  `Scenes/HouseholdLab.unity` — её после слияния надо открыть глазами, `merge=unityyamlmerge`
  зелёным слиянием сцены ничего не доказывает.

evidence: |
  - Его слова в этой сессии: «в u4 какая то проблема» плюс полный возврат ноги, сохранённый как
    `work/2026-09-03-return-the-fall-itself-is-broken.md`.
  - Разбор: workflow `bench-fall-broken-research`, 92 агента, 0 ошибок; пять углов, каждая гипотеза
    проверена тремя скептиками по линзам «код / история / альтернатива».
  - Перечитано направлением ГЛАЗАМИ, не со слов агентов:
    `HouseholderBalancePhysicsTests.cs` строки 18-45 (докблок «No scene is saved»), 58-72 (пол-Plane
    и голый carrier), 110-120 (`logOwner.SetActive(false)`), `grep -n SetActive` → 65, 70, 113, 373;
    `HouseholderBalance.cs:100-132` (обе ветки → `Stable`);
    `HouseholderBalanceBody.cs:100-110` (комментарий про отсутствие get-up и `canGetUp = false`).
  - Продукт перемерен при закрытии ноги: `origin/main` = `332488de`; `slot/win-u1` = `5fcdc11f`,
    `slot/win-u2` = `332488de`, `slot/win-u3` = `198ab3fe` — все влиты; `slot/win-u4` = `8182c984`,
    четыре коммита вне `main`.
  - Аренды на момент закрытия: WIN-U1 `CLAIMED` (`t-one-command-makes-a-named-build-1:proba`),
    WIN-U2 `AVAILABLE`, WIN-U3 `CLAIMED` (`...-the-door-002:proba`), WIN-U4 `CLAIMED`
    (`...-throw-repeats-001:build`).
  - Пробное слияние: `merge-tree` от базы `7922ecdb` между `origin/main` и `slot/win-u4` — ноль
    маркеров конфликта, три файла «changed in both».

state_changes: |
  1. ADD issue `i-the-balance-proof-was-a-bare-plane-and-a-timer-001` (`level: objective`,
     `route: review`) — запись «доказано физикой» ложна; поправка ноги стенда тоже ложна; подъёма
     нет; `Stable` объявляется таймером; порог `35°` физикой не проверялся.
  2. ADD issue `i-the-carrier-capsule-sits-outside-its-own-ragdoll-guard-001` (`level: execution`,
     `route: work`) — капсула-носитель на `Default` вне выключенной пары `9×8`; заведена как факт
     о конфигурации, её причинный вывод опровергнут и это сказано в самой карточке.
  3. ADD `work/2026-09-03-call-measure-why-he-sinks.md` и карточку
     `c-exec-g-5a7c-measure-why-he-sinks-001` (`to: executor`, `status: ready`, WIN-U4,
     `parent: c-exec-g-5a7c-the-bench-throw-repeats-001`): сначала слить, затем ОДИН живой замер
     просадки по шагам, починку не строить.
  4. ADD `work/2026-09-03-return-the-fall-itself-is-broken.md` — возврат ноги как улика.
  5. SET `c-exec-g-5a7c-the-bench-throw-repeats-001` → `status: done` (наряд исполнен и вернулся;
     карточка оставалась `ready`).
  6. Ни одно подписанное место с формулировкой «доказано физикой» не переписано: наряд 02.09,
     улика долга приёмки, раздел 2 плана и блок `goal` ставки остаются как есть, поправка лежит
     уликой рядом. Правку решает `review` 11.09.
  7. `t-the-bench-shows-why-he-fell-1` остаётся `waiting`. `NOW.md`, план и ставка не менялись.

captures:
  - Строка рубежа Р1 «Д1 прошло: равновесие принято глазом» не может стать зелёной ремонтом стенда:
    механика не делает того, что строка предполагает. Это красная строка «Обязательно», найденная
    на восьмой день до срока, а не в день сверки.
  - Пока шла эта нога, в направление легли семь коммитов других сессий, и один из них — третий
    зафиксированный случай `i-parallel-session-commit-swallows-another-legs-edit-001`. Состояние
    перечитано заново перед записью; наряд нёс замер слияния в `5fcdc11f` и был поправлен на
    `332488de` до выдачи.
  - Тест доказательства утверждает равновесие на Викторе (`HouseholderBalancePhysicsTests.cs:79` —
    «the shipped Victor puppet has no physical hip»), то есть на теле, источника которого нет в
    репозитории. При замене тела на Y Bot это доказательство придётся снимать заново.

decisions_needed:
  - q: Строка рубежа Р1 про приёмку равновесия глазом. Механика не держит тело на полу и объявляет
      «встал» таймером; починка объёма не имеет и в список Р1 не входила. Что делаем к 11.09?
    options: [чинить внутри волны 12 — тогда назвать, что из Р1 уступает место;
      снять приёмку равновесия с Р1 и перенести её вместе с починкой в Р2 (18.09), оставив в волне
      12 только стенд и замер]
    recommendation: второе — стенд свою работу сделал, а починка падения это не строка списка Р1,
      и втискивать её в неделю значит уронить что-то, что в списке есть.

play_check:
  - 1 recite: работа названа его словами «в u4 какая то проблема» плюс переданный возврат с пятью
    вопросами и открытым вопросом про слияние.
  - 2 owner inputs (owner): его слов о содержании игры в этой ноге не требовалось и не изобреталось.
    Единственный вопрос, который остаётся за ним, вынесен в `decisions_needed` и не решён здесь.
  - 3 do the work: разбор проведён, всё несущее перечитано первой рукой, две улики заведены,
    наряд написан и выдан, устаревший замер в наряде поправлен перед выдачей.
  - 4 self-check: собственная прежняя формулировка направления («доказано физикой», «режим был у́же»)
    названа ложной прямо, а не смягчена. Причина просадки НЕ названа, хотя семь гипотез было —
    все опровергнуты, и это записано как «не знаем», а не как самая правдоподобная версия.
  - 5 close: `close: light — because` каждая несущая строка выведена из байтов продукта, перечитанных
    этой ногой в файлах по номерам строк, и из его собственных слов. Ни одно утверждение о поведении
    игры не сделано без файла и строки; причина просадки намеренно оставлена незакрытой и отправлена
    живым замером. G5-чат не открывается.

log: стенд показал, что сломана сама механика падения, и разбор 92 агентами перевернул запись направления — «равновесие доказано физикой» ложно, но и поправка ноги («доказывалось в доме, усаженный, пол из коробок») тоже ложна: доказательство снималось вне какой-либо сцены, пол был примитивом Plane, хозяин голым GameObject, а NetworkHouseholder создан на выключенном объекте и никогда не включён — режим доказательства и БЫЛ полигоном; подъёма нет вовсе по объяснённому в коде решению, «встал» объявляет таймер, а не тело, и порог 35° физикой не проверялся ни разу; причину просадки направление назвать не может — семь гипотез выдвинуты и все семь опровергнуты, нужен живой замер, и сама постановка поправлена числом: тело просело на 30 см В пол, а не пробило его; слияние U4 разрешено с AllowBlockedReport, замер конфликта перемерен на main=332488de

next: |
  `c-exec-g-5a7c-measure-why-he-sinks-001` готов к запуску в WIN-U4: сначала слияние с
  `-AllowBlockedReport` и взгляд на сцену глазами, затем один живой замер просадки по шагам.
  Ждёт его слова: остаётся ли приёмка равновесия строкой рубежа Р1 или уезжает в Р2 вместе с
  починкой падения.
  На сверку 11.09: поправка формулировки «доказано физикой» в четырёх местах и капсула-носитель
  вне собственного сторожа.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-work-g-5a7c-the-fall-itself-is-broken-001.md
