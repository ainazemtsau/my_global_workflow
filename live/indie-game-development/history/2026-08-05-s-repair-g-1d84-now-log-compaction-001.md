# RESULT — s-repair-g-1d84-now-log-compaction-001

direction: indie-game-development
play: repair
date: 2026-08-05
branch: wt/repair-now-cleanup (изолированная копия), база 1a8c624a
scope: live/indie-game-development/** — только форма горячего состояния. os/** не тронут.

## 1. Названное противоречие (repair шаг 1)

- `NOW.md` — 687 строк / 229 479 байт при шаблонном потолке ~150 строк; 66% файла (150 607 байт)
  занимали `issues`, в которых длинные закрытые разборы соседствовали с рабочим списком и топили его.
  Это ЗАКАЗ, записанный самим направлением: `i-now-hot-file-over-ceiling-001`, route `repair`.
- `LOG.md` — 311 строк / 317 640 байт, 153 записи; по схеме LOG обязан быть индексом «одна строка /
  ≤2 коротких предложения плюс указатель», фактически был вторым пересказом каждой ноги. Ротация в
  этой редакции не выполнялась: нижний указатель вёл в `archive/**`, файла
  `history/LOG-archive-indie-game-development.md` в live не существовало.

## 2. Реконструкция (repair шаг 2) — что найдено в живом состоянии

Активная ставка одна — g-1d84. Пять полос при WIP 3. 23 задачи: done 3, ready 6, blocked 13,
ждёт решения владельца 1. Открытых нарядов 3: один `ready` (`c-exec-g-1d84-host-into-one-scene-001`),
два `blocked`. Решений владельца ждёт одно (`d-profile-file-format-recommendation-002`) плюс одно
закрытое. `issues` 40, из них у восьми терминальный маркер уже был записан в их собственных полях.
`recurring` пуст. `direction_forecast` — `no_basis`, и это правильный статус: reference class как не
было, так и нет.

Проверено первой рукой: висячих указателей в `NOW.md` не было ни одного, трейлеры `END_OF_FILE`
стояли у всех четырёх файлов состояния и у всех трёх файлов нарядов.

## 3. Что изменено и ПОЧЕМУ — по одной строке причины на изменение

### NOW.md

1. **Заголовок сведён в одну строку** — форма шаблона (было разнесено на строки 1 и 3).
2. **`bet:` не тронут ни на байт** — сам заказ говорит, что ~330 строк `bet`/`tracks`/задач это
   законный рост горячих полей; текст ставки подписан владельцем и репайру не принадлежит.
3. **`direction_forecast:` не тронут** — пересчёт прогноза это не гигиена; `no_basis` остаётся верным.
4. **`tracks` переведён в форму схемы** (`track_wip_limit: 3` + пять записей `id`/`label`/`for`/`tasks`)
   — прежний объект `{wip, wip_owner_word, rule, lanes:[{name,piece,root_enters_scene,tasks}]}` схемой
   не предусмотрен; НИ ОДНА полоса не удалена и не переименована, состав задач каждой сохранён строкой
   `tasks:`, а дословное слово владельца о WIP и правило полос уехали в work/ дословно.
5. **Статусы задач приведены к `open|active|blocked|done`** — `ready`, `blocked_by_<id>`,
   `waiting_owner_decision`, `blocked_by_u3_fast_forward_permission` схемой не предусмотрены; каждое
   прежнее условие сохранено дословно в `unblock_when` соответствующей задачи, ни одна блокировка не
   ослаблена и ни одна задача не переведена в `done`.
6. **Внесхемные поля задач сняты со строк** (`track`, `kind`, `visible`, `from`, `note`, `checkpoint`,
   `unblocked`, `blocked_by_owner_decision`) — форма схемы; весь текст уехал ДОСЛОВНО в
   `work/now-compaction-2026-08-05.md §2`, указатель стоит комментарием прямо над `tasks:`.
   `order` оставлен: это живой порядок выдачи волны, а не описание.
7. **Каждая запись `issues` сведена к шести ключам схемы** (`id/issue/level/route/review_when/evidence`),
   `issue` и `review_when` — по ≤2 предложения, `evidence` — только указатели; внесхемные ключи
   (`status`, `closed_by`, `routed_by`, `owner_ruling`, `correction`, `binding_constraint`,
   `owner_design_input_2026_08_03`, `owner_corrections_2026_08_03_evening`, `owner_delivery_point_idea`,
   `owner_situational_digging_idea`, `digging_binding_rules`, `ai_question_closed`, `owner_question_open`,
   `remeasured`) сняты, их содержимое уехало ДОСЛОВНО в `work/now-compaction-2026-08-05.md §3`.
   Ни один id не изменён, ни один маршрут (`route`) не переназначен.
8. **`open_calls` приведены к схеме**: `for:` теперь указывает на задачу (было `for: g-1d84` плюс
   отдельный `task:`), блокировки выражены `unblock_when`, всё остальное — одной строкой `note:`;
   ни один наряд не снят, не переоткрыт и не переведён в другой статус.
9. **`decisions` содержат только нерешённое** — правило NOW-гигиены; форма приведена к `q`/`options`/
   `recommendation` с честной записью, что живой рекомендации нет.
10. **Ни одна строка `bet`, `TREE.md`, `CHARTER.md`, `knowledge/**` и ни один файл наряда не изменены.**

### Снято с горячего состояния — шесть строк, у каждой терминальный маркер был записан ДО этой ноги

Правило: снимается только строка, чьё терминальное состояние уже записано в её собственных полях;
где маркера нет — строка остаётся и только сжимается. Полный текст всех шести — в
`work/now-compaction-2026-08-05.md` (§3 и §5), disposition:

| id | маркер, который был в файле | куда ушло |
|---|---|---|
| `i-walkspace-probe-tunneling-on-large-step-001` | `status: closed_2026-08-05` + `closed_by` со словами владельца «Всё, вот эту хуйню выпили» | work/now-compaction §3; правило вперёд (не заводить тот же вопрос по стенам) живёт в его словах там же |
| `i-scene-2-call-basis-stale-after-publication-001` | `status: closed_2026-08-05` + `closed_by` («пятого бунса этого класса не случилось») | work/now-compaction §3; живой класс продолжает вести `i-call-named-path-absent-on-declared-basis-004` |
| `i-house-work-finished-in-slot-not-returned-001` | `status: closed_2026-08-05` + `closed_by` («Обе половины записи сняты») | work/now-compaction §3; два её хвоста живут отдельными открытыми записями |
| `i-slots-carry-old-contour-until-updated-001` | `review_when:` «**ЗАКРЫТО 2026-08-04 ИЗМЕРЕНИЕМ**» | work/now-compaction §3; правило вперёд живёт в тексте каждого наряда, как сама запись и говорила |
| `i-newgame-call-oversized-and-stopped-001` | `review_when:` «**ЗАКРЫТО 2026-08-04 НАРЕЗКОЙ ИЗМЕРЕНИЕМ**» | work/now-compaction §3; живой урок исполнен нарезкой и ведётся `i-bet-tasks-never-sized-001` |
| `d-g-1d84-second-fail-branch-001` | `status: closed_2026-08-04` + `closed_by` («Запись держалась только до закрытия нарезкой») | work/now-compaction §5; слово владельца «В, идём дальше на том, что отвечено» сохранено дословно там же и в LOG-записи нарезки |

**Что НЕ снято, хотя маркер формально есть.** `i-tunnel-role-description-owner-text-001`
(«ПЕРЕАДРЕСОВАНО 2026-08-04») и `i-three-spaces-own-dangers-001` («ТРИГГЕР ОТРАБОТАН 2026-08-04»)
оставлены открытыми и только сжаты: у обеих тут же назван ЖИВОЙ следующий адресат (отдельная ставка
на копание либо узел «полный заход демо»), то есть материал переадресован, а не исчерпан. Снимать их
значило бы объявить закрытым то, что не закрыто.

### LOG.md

- Оставлены 25 записей — 2026-08-04 и 2026-08-05, то есть вся жизнь действующей ставки g-1d84, плюс
  запись этой ноги; каждая сведена к форме схемы «≤2 коротких предложения плюс указатель».
- 128 записей до 2026-08-04 уехали ДОСЛОВНО, байт в байт, тем же порядком в
  `history/LOG-archive-indie-game-development.md`; прежний нижний указатель на архив в `archive/**`
  сохранён внутри нового архива, а в `LOG.md` остался один нижний указатель на него.

## 4. Числа — фактические, до и после

| файл | было | стало |
|---|---|---|
| `NOW.md` | 687 строк / 229 479 байт | 492 строки / 77 043 байта |
| `LOG.md` | 311 строк / 317 640 байт (153 записи) | 57 строк / ~12,7 KB (26 записей) |
| `history/LOG-archive-indie-game-development.md` | не существовал | 274 строки / 290 155 байт (128 записей дословно) |
| `work/now-compaction-2026-08-05.md` | не существовал | 649 строк / 216 233 байта (68 id дословно) |

Состав горячего состояния: задач 23 (done 3, open 6, blocked 13, из них одна ждёт решения владельца),
полос 5 при WIP 3, открытых нарядов 3 (ready 1, blocked 2), нерешённых записей `issues` 35,
`decisions` 1, `recurring` 0. Все эти числа не изменились этой ногой, кроме `issues` 40 → 35 и
`decisions` 2 → 1 по таблице выше.

**NOW.md всё ещё выше потолка ~150 строк, и это названо вслух, а не спрятано.** В файле 23 живые
задачи и 35 нерешённых записей; дальнейшее сжатие означало бы удаление маршрутов, а не форму.
Запись `i-now-hot-file-over-ceiling-001` поэтому остаётся ОТКРЫТОЙ с обновлёнными числами.

## 5. Чего эта нога НЕ делала

- Не закрыла ни одной задачи, ни одного наряда и ни одной записи, у которой не было терминального
  маркера ДО неё. Прогресса не выдумано нигде.
- Не удаляла и не ретировала ни одной полосы, ни одного открытого наряда, ни одной записи
  `knowledge/`, ни одного local play и не трогала одобренный владельцем статус ни одного артефакта.
- Не трогала `os/**`, `TREE.md`, `CHARTER.md`, `bet:`, `direction_forecast:` и файлы нарядов.
- Не сливала и не переоткрывала `i-call-named-path-absent-on-declared-basis-004` и
  `i-direction-to-product-call-contract-001` — один класс, обе открыты, слияние запрещено.

## 6. Дрейф, найденный при реконструкции и НЕ исправленный этой ногой

Репайр его называет, а не чинит: чинить эти вещи значит решать за владельца или менять семантику.

1. `bet.open_rows_disposition` говорит «2 [строки] — владельцу (см. decisions)», а в `decisions` ждёт
   ОДНА: развилка о слове «инструменты» закрыта владельцем 2026-08-05. Диспозиция второй сохранена в
   `note` наряда `c-exec-g-1d84-free-kits-probe-001` и в
   `history/2026-08-05-s-day-g-1d84-free-kits-manual-authoring-launch-blocked-001.md`.
   `bet` — подписанный текст, репайру не принадлежит.
2. `TREE.md` §owner_approved по-прежнему числит ОБЕ standing decisions как `waiting_owner`. TREE —
   семантика, маршрут `map`/`review`, а не repair.
3. База наряда `c-exec-g-1d84-free-kits-probe-001` объявлена на `main` = `02a53bbb`, продуктовый
   `main` = `b7ae1fa4`. Наряд `blocked`, немедленного вреда нет; требование перемера ДО разблокировки
   записано в его `note` — это тот самый класс, которым уже оплачено четыре бунса.
4. `direction_forecast.as_of` = 2026-08-04 при трёх задачах, закрытых 2026-08-05. Статус `no_basis`
   при этом остаётся верным; пересчёт — дело day/pulse, а не repair.
5. `i-old-code-move-to-own-folder-001` несёт противоречие внутри одного поля («срок больше не вопрос»
   и «открыт только СРОК»). Противоречие НАЗВАНО прямо в тексте сжатой записи и оставлено её маршруту.

## 7. FRICTION

Дыры OS эта нога не нашла: правила repair, схемы и границы удаления оказались достаточны для работы.
Единственное трение — арифметическое и уже описано схемой: направление с одной активной ставкой из
23 задач физически не помещается в потолок ~150 строк, если ни одна строка не выдумана и ни одна не
выброшена. Это не дефект правила, а честная цена размера волны; чинится закрытием работ, а не текстом.

END_OF_FILE: live/indie-game-development/history/2026-08-05-s-repair-g-1d84-now-log-compaction-001.md
