# RESULT — s-work-g-5a7c-handlers-1-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-handlers-1
track: householder-life
date: 2026-08-13
outcome_kind: close — наряд `c-exec-g-5a7c-handlers-1-001` закрыт; задача `t-handlers-1` НЕ закрыта и переведена в `blocked`
close: fresh — because это отдельная физическая сессия от той, что строила (исполнитель работал в продуктовом репозитории, слот WIN-U1), и она РЕФУТИРОВАЛА, а не приняла. Светлый путь недоступен по двум причинам сразу: строка 5 карточки требует суждения, которого никто не вынес в требуемом месте, а две улики возврата — замер волочения и прогон Unity EditMode — сняты в живой сессии владельца и мной первой рукой не перевыводились (редактора у меня нет; выдумывать обход запрещено).

owner_approved: |
  **Его слова мне, дословно, 2026-08-13, три сообщения подряд:**

  «задача c-exec-g-5a7c-handlers-1-001 выполнена»

  На прямой вопрос, где он видел комбинацию — в собранной игре или в окне лаборатории:
  «кнопкой в лаборатории»

  «я не буду в игре тестировать такие условия так как сейчас не удобно мы можем потом ногу сделать
  что бы это было тетсить удобнее»

  **Его вердикт по прогону, закоммичен в продукте на `1307aa3e`
  (`docs/results/c-exec-g-5a7c-handlers-1-001.md` §Manual-acceptance), дословно:**
  «когда в движении нажал то он действительно подошел и остановился».

  **Ни одно из этих слов не закрывает строку 5 задачи, и это не толкование:** слово «нажал» он сам
  же и расшифровал следующим сообщением — лаборатория. Закрытие строки 5 не запрашивалось и не
  получено; вместо него получен его отказ проверять в игре сейчас и его же предложение отдельной
  ноги.

outcome: |
  **НАРЯД ЗАКРЫТ. ЗАДАЧА — НЕТ. РАЗВОДИТ ИХ РОВНО ОДНА СТРОКА.**

  Наряд нёс три строки контракта 36 — все три выполнены, и я перемерила их своей рукой по байтам и
  прогоном. Задача `t-handlers-1` несёт восемь. Семь закрыты. Восьмая — строка 5, «владелец
  произвёл хотя бы одну комбинацию САМ В СОБРАННОЙ ИГРЕ, не в лаборатории» — не закрыта ничем.

  Лаборатория её не закрывает не по моему мнению, а по прямому запрету, записанному в этой же
  дорожке: `t-lab-2` строка 6 — «ни одна галка "сделано" не закрывается тем, что "в лаборатории
  работает"». Сама строка 5 стоит в карточке потому, что на ровно этом месте прошлой волны была
  снята `t-react-3`.

  **ГЛАВНОЕ НАЙДЕНО ЗДЕСЬ, А НЕ В ВОЗВРАТЕ, И ЭТО МЕНЯЕТ ПРИРОДУ ПРОБЛЕМЫ.** Владелец назвал
  причину отказа «не удобно». Чтение источника шума показало, что дело не в удобстве: **в собранной
  игре тихого шума не существует вовсе.**

  - Единственный источник шума в игре — удар груза (`Network/CargoBodyContact.cs:157`,
    `force = collision.impulse.magnitude / step`), а полосы берутся из
    `Settings/GameRulesSettings.asset`: `_cargoRustleForce 60`, `_cargoKnockForce 150`,
    `_cargoHeavyNoiseForce 400`.
  - Кнопки «Quiet rustle» и «Knock» живут в `Assets/TunnelCrew/Editor/**` и подставляют РОВНО
    пороговое число. В собранную игру они не едут — это лаборатория, а не игра.
  - Единственный настоящий замер удара груза — его собственный прогон: **1 884 … 54 730**, каждое
    значение в **4,7 … 136,8 раза выше самого громкого порога**.
  - Обе новые строки профиля просят `quiet-rustle` и `suspicious-noise`. Тяжёлый удар не участвует
    ни в одной.

  Значит рецепт возврата «тихо задеть на 60, потом стукнуть на 150» просит две полосы, которых
  настоящий груз ни разу не показал. **Его инстинкт был верен, а формулировка мягче правды.**

  **ЭТО ЖЕ ОБЪЯСНЯЕТ ЕГО СОБСТВЕННУЮ ЖАЛОБУ ОДНОЙ ПРИЧИНОЙ.** «Хозяин топчется по грузу туда-сюда»
  — следствие того же: тяжёлый факт рождается 30 раз в секунду в чуть другой точке, а правило
  активного ответа `repeated-heavy-noise` несёт `preemption: higher-or-equal-latest` и обязано
  перенацеливать его каждый тик. Это НЕ дрожь маршрута — та измерена, починена и закрыта отдельной
  ногой.

  **ЧТО НАРЯД ВСЁ-ТАКИ КУПИЛ, И ЭТО НЕ МАЛО.** Третий вид обработчика `local-incident` существует,
  въехал одним коммитом, не тронув ни одной строки ядра, две авторские строки лежат в
  ОПУБЛИКОВАННОМ игровом профиле вместо двух пустых массивов, добавлено ровно одно физическое
  действие, имя сработавшей строки видно в наблюдении, и всё это гоняется 267 зелёными тестами.
  Механизм построен и доказан. Не куплено одно — возможность владельцу это увидеть своими руками
  в игре.

  **ПОПУТНО ЗАКРЫТА УЛИКА, КОТОРАЯ ОПРОВЕРГЛА САМА СЕБЯ.**
  `i-dragged-cargo-rustles-every-tick-001` стояла на расчёте «волочение даёт ШОРОХ, ~83,4 Н, полоса
  60…150». Замер дал минимум 1 884 — в 22 раза больше, и это самое тихое из восьми. Её обязательство
  («мерить, а не считать») исполнено, поэтому она закрыта; её имя при этом неверно, и решение «где
  чинить» уехало целиком в новую улику.

evidence: |
  **ВСЁ НИЖЕ, КРОМЕ ЯВНО НАЗВАННЫХ ДВУХ ПУНКТОВ, ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ. Слово владельца «выполнена»
  принято как улика, а не как факт — по нашему же правилу.**

  ## Голова

  `git ls-remote origin` → `main` = `dev` = **`1307aa3e020f54eb42581b98d090c3571cbab53b`**
  (базис наряда был `27c98d14`). Работа ОПУБЛИКОВАНА.

  Цепь `27c98d14..1307aa3e` — десять коммитов. Производственных **ДВА**:
  `bf59c57d` (седьмая реакция и новое физическое действие) и `cfb71f57` (третий вид + строки
  профиля). Ещё один трогает только `Assets/TunnelCrew/Editor/**` (`b587b8ab`, пресет маркера
  «Saw rat» и остаток жизни фактов в окне). Остальные семь — только
  `docs/results/c-exec-g-5a7c-handlers-1-001.md`.

  ## Строка 1 наряда — третий вид, доказанный СОСТАВОМ КОММИТА

  `git show --stat --name-only cfb71f57` — **ровно 7 путей**:
  `HouseholderLocalIncidentDecisionHandler.cs` + его `.meta`, `HouseholderProfileLoader.cs`,
  `default.householder.json`, `HouseholderLocalIncidentTests.cs`,
  `HouseholderActiveResponseRuleTests.cs`, `HouseholderProfileAuthoringTests.cs`.

  **Ни одного из шести названных нарядом файлов ядра в этом коммите НЕТ** —
  `HouseholderDecisionPipeline.cs`, `HouseholderFactCombinationDecisionHandler.cs`,
  `HouseholderAccumulatedFactDecisionHandler.cs`, `Householder.cs`, `HouseholderReaction.cs`,
  `AuthoritativeHouseholder.cs`. Проверено списком путей, а не словами возврата.

  `Householder.cs` и `HouseholderReaction.cs` тронуты СОСЕДНИМ коммитом `bf59c57d`, и это законно:
  запрет наряда адресован коммиту, вводящему третий вид. Прочитала его дифф целиком — правки
  аддитивные: четыре enum'а получают по одному значению в конец, каталог получает седьмую строку
  рядом с шестью, а единственная правка логики (`StepActiveResponse`) — превращение тернарника в
  `switch`, чей `default` сохраняет прежние `GuardListeningPoint` / `UrgentGuardSeconds`.

  Перерасход названной цены исполнитель предъявил сам, диффом, и он обоснован: `case "seen-rat"` в
  словаре фактов профиля (без него строку про зрение НЕЛЬЗЯ НАЗВАТЬ — загрузка падала бы дословным
  `unknown fact 'seen-rat'`), поле `withoutFacts` (без него «никого не видно» невыразимо) и
  `RejectHandlerField` в двух существующих ветках (без него лишнее поле принималось бы молча).

  ## Строка 2 — строки лежат в ОПУБЛИКОВАННОМ профиле

  Прочитала `origin/main:Assets/StreamingAssets/TunnelCrew/Householder/default.householder.json`
  байтами. Было `"behaviorBranches": []`, `"decisionHandlers": []`. Стало: ветка
  `linger-after-peeking` и две строки — `caught-rat-where-the-noise-was`
  (`facts: [quiet-rustle, seen-rat]`, реакция `seen-rat`) и `noise-here-and-nobody-in-sight`
  (`facts: [quiet-rustle, suspicious-noise]`, `withoutFacts: [seen-rat]`, реакция
  `nobody-in-sight`, ветка `linger-after-peeking`).

  - **На комнату опираются обе** — вид `local-incident` комнатный по устройству.
  - **На факт зрения опираются обе** — первая требует `seen-rat`, вторая требует его отсутствия.
  - **Физическое действие добавлено РОВНО ОДНО**: `HouseholderPhysicalActionName.CrouchAndPeek = 8`.
    Проверено диффом enum'а, а не заявлением.
  - Именованный отказ на неизвестное имя жив и расширен: четыре теста, включая
    `field 'room' is not valid for decision handler type 'local-incident'` и
    `field 'facts' is not valid for decision handler type 'fact-combination'`.
  - Имя сработавшей строки доходит до наблюдения — тест
    `TheObservationSnapshotNamesTheIncidentRowThatAnswered` существует и зелёный.

  ## Строка 3 — ничего не сломано, откат настоящий

  - **Мой прогон:** `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release`
    → **`не пройдено 0, пройдено 267, пропущено 0, всего 267`**. Против базовых **254** (перемерены
    предыдущей ногой её рукой), дельта **+13**. Пропущенных ноль.
    **Возврат называет 264 — число устарело:** после того прогона доехали ещё тесты
    (`93a98f23`, `b587b8ab`).
  - **Пять прежних реакций и `seen-rat` целы по содержанию.** Дифф трёх чужих тестовых файлов —
    ровно три числа-инвентаря (`reactions.Count` 6→7, `tuning.Reactions.Count` 7→8,
    `DecisionHandlerCount` 0→2 в двух местах) и одно имя теста. **Ни одной проверки ПОВЕДЕНИЯ не
    тронуто** — прочитала дифф целиком.
  - **Новая реакция не может перехватить прежние по устройству:** у `nobody-in-sight`
    `EventName = None`, значит `IsTriggered = false`, значит ни однофактовая политика, ни правила
    активного ответа выбрать её не могут. Достижима только строкой профиля.
  - **ОТКАТ ПРОЧИТАН МОЕЙ РУКОЙ, а не взят из отчёта.** Отчёт называл его снятым и неопубликованным.
    Объект жив: `a22f5c98e5c1e1aeae9038acf50f6625919ce22c`, «REVERT PROOF (demo, not published)»,
    родитель `cfb71f57`, дерево **`85bb8d5a01863547df951b0dd8922338db62ef5a`** — побайтно равно
    дереву базы `27c98d14`. `git diff --stat 27c98d14 a22f5c98` пуст. Один коммит, настоящий
    `revert`, деревья сходятся. Оговорка: коммит не лежит ни на одной ссылке — `for-each-ref
    --contains` пуст, то есть переживёт он только до сборки мусора.
  - **Ни одной сцены, ни одного префаба, ни одного файла параллельного наряда** в диапазоне
    `27c98d14..1307aa3e`: фильтр по `Scenes/`, `*.prefab`, `Core/Cargo/`, `Network/Cargo`,
    `GameRulesSettings`, `TunnelCrew/World/` даёт **ноль совпадений**.

  ## Закон достижимости — РАСПЕЧАТАН МОИМ ПРОГОНОМ, а не пересказан

  Вывод теста `TheReachableWindowForAnIncidentIsMeasuredNotAssumed` в моём прогоне:

  ```
  --- while he is busy with: WalkRoute ---
    gap  0 ticks (0,0s): INCIDENT      gap 20 ticks (2,0s): INCIDENT
    gap  1 ticks (0,1s): INCIDENT      gap 40 ticks (4,0s): INCIDENT
    gap  8 ticks (0,8s): INCIDENT      gap 70 ticks (7,0s): no incident
  --- while he is busy with: WaitAtRoutePoint ---
    gap  0 ticks (0,0s): INCIDENT      всё остальное: no incident
  ```

  Происшествие складывается, ТОЛЬКО пока хозяин идёт, и окно ~4 секунды. Пока он стоит — первый же
  шум съедается своей реакцией и гасится. Механизм: ходьба объявляет себя небезопасной, одиночные
  реакции пропускаются, факты копятся непогашенными.

  ## Источник шума — прочитан здесь, в возврате этого нет

  - `Network/CargoBodyContact.cs:157` — `var force = collision.impulse.magnitude / step`.
    Единственный производитель шума в игре.
  - `Core/Situations/WorldSituationNoise.Band` — три порога, вниз от громкого.
  - `Settings/GameRulesSettings.asset:67-69` — `_cargoRustleForce: 60`, `_cargoKnockForce: 150`,
    `_cargoHeavyNoiseForce: 400`. Имена полей начинаются с `cargo` — других источников нет.
  - `EmitQuietRustle` / `EmitBoardCreak` вызываются РОВНО из одного места:
    `Assets/TunnelCrew/Editor/HouseholderRouteControllerEditor.cs:62,68`. Редактор, не игра.
  - `Assets/TunnelCrew/Editor/HouseholderObservationMarkerCommand.cs` — пресеты подставляют
    `noise.RustleForce` / `noise.KnockForce` / `noise.HeavyNoiseForce` буквально.
  - `suspicious-noise` рождается из `BoardCreaked` (`HouseholderReaction.cs:362`), `quiet-rustle` —
    из `QuietRustle` (`:386`). Обе новые строки просят только их.

  ## Улика про черновик дома — названная ею нога прошла, опасность не реализовалась

  `HouseholderLocalIncidentTests.cs` не упоминает ни `ThreeRoomDraft`, ни `TestHouse` вовсе:
  доказательство про комнату построено на собственном плане из двух комнат внутри теста. Черновик
  жив в чужих проверках (23 упоминания в тестах ходьбы, 9 в тестах дрожи, 3 в тестах зрения),
  поэтому улика не закрывается, а её `review_when` переведён на следующую ногу по геометрии дома.

  ## ДВЕ ВЕЩИ, КОТОРЫЕ Я НЕ ПЕРЕВЫВОДИЛА СВОЕЙ РУКОЙ, И ЭТО НАЗВАНО

  1. **Замер волочения** (восемь контактов, снимок `observationIndex 1251`) — снят в живой сессии
     ВЛАДЕЛЬЦА и прочитан исполнителем с сырого снимка. Unity у меня нет; самописный сканер как
     улика запрещён, обходить нечем.
  2. **Прогон Unity EditMode** и импорт рукописной `.cs.meta` настоящим импортёром — там же.

  Обе резолвятся закоммиченным отчётом с точными числами и номером снимка. Обе — про измерение, а
  не про суждение.

state_changes: |
  1. `c-exec-g-5a7c-handlers-1-001`: `ready → done`, уезжает в `closed/`. Добавлены `closed` и
     `head: 1307aa3e…`. Основание — три строки наряда, разобранные построчно выше.
  2. `t-handlers-1`: `active → blocked`, заведён `unblock_when` его словами и измеренной причиной.
     Добавлен раздел «состояние строк» — семь из восьми закрыты, строка 5 нет. Ни одна строка
     `done_when` не переписана и не ослаблена.
  3. Заведена улика `i-quiet-noise-is-unreachable-in-the-built-game-001` — `level: product`,
     `route: review`. Она несёт замер, чтение источника шума и оба следствия (недостижимое
     происшествие и топтание вокруг груза) как ОДНУ причину.
  4. `i-dragged-cargo-rustles-every-tick-001`: `open → done`, уезжает в `closed/` с `disposition`.
     Обязательство «мерить, а не считать» исполнено; её расчёт и её имя опровергнуты; выбор места
     починки переехал в улику из пункта 3.
  5. Заведена улика `i-cargo-sinks-into-the-floor-when-the-householder-arrives-001` с его прогона —
     `route: work`, дорожка `house-and-things`.
  6. Заведена идея `idea-make-the-game-hand-testable-001` его словами. **Это идея, а не задача:**
     нарезка — дело `shape`/`review`, не `work`.
  7. `i-noise-bands-invisible-and-force-mis-scaled-001`: журнальная строка — её утверждение
     «верхняя полоса не наблюдалась никем» опровергнуто; невидимость полос НЕ закрыта.
  8. `i-test-house-draft-describes-a-house-that-is-gone-001`: `review_when` переведён на следующую
     ногу по геометрии дома, журнальная строка с перемером. Улика остаётся открытой.
  9. `i-executor-return-omits-named-return-items-001`: журнальная строка с новым подвидом —
     «отчёт-палимпсест».
  10. Полный RESULT сохранён в `history/2026-08-13-s-work-g-5a7c-handlers-1-close-001.md`.

  **ЧЕГО ЭТА НОГА НЕ СДЕЛАЛА.** Не тронута карточка ставки, `NOW.md`, дорожки, `t-lab-2` и всё,
  что относится к `house-and-things` и параллельному наряду по луту. Не заведено ни одной задачи.
  В продуктовом репозитории не изменено НИЧЕГО — только чтение и один прогон тестов.

captures:
  - |
    **ЛАБОРАТОРИЯ СЪЕЛА СТРОКУ, КОТОРУЮ ОБЯЗАНА БЫЛА НЕ ТРОГАТЬ, И СДЕЛАЛА ЭТО ТИХО.** Запрет
    `t-lab-2` строка 6 существует ровно против этого, но он записан в карточке ПОСЛЕДНЕЙ задачи
    дорожки — то есть там, куда никто не смотрит, закрывая девятую. Наряд честно сказал «в
    СОБРАННОЙ игре, а не в лаборатории», исполнитель честно назвал маркер «удобнее всего», владелец
    честно нажал кнопку, и все трое двигались к вердикту про код, который эта дорожка уже один раз
    похоронила (`t-react-3`). **Класс: запрет, живущий не там, где принимается решение, не
    работает.** Проверять при `review` ставки: не пора ли этому запрету жить в самой дорожке.
  - |
    **«НЕ УДОБНО» ОКАЗАЛОСЬ «НЕВОЗМОЖНО», И РАЗНИЦУ НАШЛА ПРОВЕРКА, А НЕ ВОПРОС.** Он объяснил
    отказ удобством — обычная человеческая формулировка, которую легко принять и записать как
    лень или как каприз. Чтение источника шума показало, что в собранной игре тихого шума нет
    вовсе. **Класс: когда владелец отказывается проверять, первым делом надо мерить, ЧЕМ он
    вообще мог бы это проверить, а не уговаривать и не подстраивать рецепт.** Дважды подряд теперь
    его неточная на слух формулировка оказывалась точнее нашей уверенной.
  - |
    **ОТЧЁТ-ПАЛИМПСЕСТ.** Возврат правился семью коммитами поверх себя, и устарели в нём ровно
    решающие строки: `Cuts`/`Next` до сих пор объявляют СТОП по невыполненному замеру, который в
    теле того же документа выполнен восемью числами, а прогон назван 264 при 267 на голове.
    Ничего не пропущено — всё заказанное на месте. **Класс отличается от улики про пропуски: там
    не написали, здесь написали и не догнали.** Ловится только перемером чисел своей рукой.

decisions_needed:
  - |
    **Куда девать `t-handlers-1` — единственный настоящий вопрос, и он его.**
    Механизм построен и доказан, а увидеть его в игре сегодня нечем: тихого шума в собранной игре
    не существует. Три честных исхода, и все три законны:
    (а) починить источник шума отдельной ногой и вернуться к строке 5 — тогда та же нога чинит и
    «хозяин топчется по грузу», это одна причина;
    (б) закрыть строку 5 его прямым словом-исключением, признав, что механизм принят по лаборатории
    — но это ровно то, на чём умерла `t-react-3`, и цена решения записана;
    (в) снять задачу с волны как `partial` при `review` ставки и унести и её, и шум в следующую.
    **Рекомендация: (в) на `review`, потому что решение про источник шума задевает пороги, правило
    сведения и восприятие хозяина сразу, а это размер волны, а не размер ноги.** Вопрос НЕ задаётся
    сейчас отдельным заходом: он естественно встаёт на `review` ставки, и там же лежат его
    альтернативы.

play_check:
  - 1 recite — done. Пересказаны три строки наряда и восемь строк карточки `t-handlers-1`, а также обязательство карточки «светлое закрытие НЕДОСТУПНО — строку 5 судит его глаз в собранной игре».
  - 2 owner inputs (owner) — done. Спрошено ровно одно, что знает только он: где он видел комбинацию. Ответ получен дословно и приведён целиком, вместе с его отказом и его предложением. Ни одно его слово не расширено до требования.
  - 3 do the work — приём возврата с перемером по байтам: голова, состав двух производственных коммитов против шести запретных файлов, опубликованный профиль, дифф чужих тестов, свой прогон 267, чтение коммита отката и сверка деревьев, чтение источника шума, проверка улики про черновик дома.
  - 4 self-check — done построчно против трёх строк наряда и восьми строк карточки; названы две улики, не перевыведенные своей рукой, и названо, чем они резолвятся.
  - 5 close — наряд закрыт; задача НЕ закрыта и переведена в `blocked` с `unblock_when`. Продолжение дорожки не выдаётся этой ногой — см. `next`.
  - G5 — done в этой сессии как в СВЕЖЕЙ по отношению к строившей: исполнитель работал в продуктовом репозитории, слот WIN-U1, отдельным физическим чатом. Пасс рефутирующий: одна строка возврата (рецепт проверки в собранной игре) опровергнута чтением источника шума, два числа возврата (264, СТОП по замеру) опровергнуты как устаревшие. Светлый путь отклонён явно, основание в поле `close`.
  - G7 — done. Ни одного игрового решения не принято ногой. Отказ владельца проверять в игре принят как его решение и НЕ смягчён до строки спеки; его предложение про ногу записано ИДЕЕЙ, а не задачей. Выбор дальнейшей судьбы задачи вынесен ему в `decisions_needed` с рекомендацией.
  - G2/G3 — done. Аппетит не расширен: ноль новых задач, ноль новых дорожек. Заведены две улики и одна идея; одна улика закрыта.
  - G10 — done. RESULT финальный, состояние написано только его `state_changes`.

log: наряд на обработчики ЗАКРЫТ, а задача НЕТ, и разводит их ровно строка 5 — его собственная проверка в СОБРАННОЙ игре, на прямой вопрос он ответил «кнопкой в лаборатории» и «я не буду в игре тестировать такие условия так как сейчас не удобно мы можем потом ногу сделать что бы это было тетсить удобнее»; три строки наряда перемерены МОЕЙ рукой: третий вид local-incident въезжает коммитом cfb71f57 на СЕМЬ путей и не трогает НИ ОДНОГО из шести названных файлов ядра, а Householder.cs и HouseholderReaction.cs правит соседний bf59c57d чисто аддитивно, опубликованный default.householder.json несёт две строки и ветку вместо двух пустых массивов, добавлено РОВНО одно физическое действие CrouchAndPeek, в чужих тестах изменены только счётчики 6→7, 7→8, 0→2 и одно имя теста при НУЛЕ тронутых проверок поведения, откат a22f5c98 ПРОЧИТАН лично — родитель cfb71f57, дерево 85bb8d5a побайтно равно дереву базы, git diff пуст, хотя на ссылках коммит не лежит вовсе, и 267 из 267 тестов прогнаны мной при нуле упавших и нуле пропущенных против базовых 254, тогда как возврат называет 264 и в разделах Cuts/Next всё ещё объявляет СТОП по замеру, который в теле того же документа выполнен восемью числами — заведён класс «отчёт-палимпсест»; ГЛАВНОЕ НАЙДЕНО ЗДЕСЬ, А НЕ В ВОЗВРАТЕ, И ОНО МЕНЯЕТ ПРИРОДУ ОТКАЗА ВЛАДЕЛЬЦА: в собранной игре тихого шума НЕ СУЩЕСТВУЕТ — единственный источник шума это удар груза (CargoBodyContact, force = impulse/step) против порогов _cargoRustleForce 60 / _cargoKnockForce 150 / _cargoHeavyNoiseForce 400, кнопки «Quiet rustle» и «Knock» вызываются РОВНО из редакторского HouseholderRouteControllerEditor и подставляют пороговое число, а единственный настоящий замер удара груза — его собственный прогон — дал 1 884…54 730, в 4,7…136,8 раза выше САМОГО ГРОМКОГО порога, при том что обе новые строки просят quiet-rustle и suspicious-noise и тяжёлый удар не участвует ни в одной; значит его «не удобно» на самом деле «нечем», рецепт возврата «тихо задеть на 60, потом стукнуть на 150» просит полосы, которых настоящий груз ни разу не показал, и та же причина одним махом объясняет его «хозяин топчется по грузу», потому что тяжёлый факт рождается 30 раз в секунду в новой точке, а правило higher-or-equal-latest обязано перенацеливать ответ каждый тик — всё это заведено ОДНОЙ уликой i-quiet-noise-is-unreachable-in-the-built-game-001 с маршрутом на review ставки; попутно ЗАКРЫТА i-dragged-cargo-rustles-every-tick-001, опровергнувшая сама себя — её расчёт обещал шорох 83,4 Н, замер дал минимум 1 884, в 22 раза больше, и её имя неверно; закон достижимости распечатан моим прогоном (происшествие складывается только пока хозяин ИДЁТ, окно ~4 с, у стоящего НИКОГДА); улика про устаревший тестовый дом не реализовалась — тесты происшествия не упоминают ThreeRoomDraft вовсе; заведены улика про проваливающийся в пол груз с его прогона и идея о ноге, делающей игру тестируемой руками, а задача переведена active → blocked с unblock_when его словами

next: |
  **Продолжение дорожки `householder-life` этой ногой НЕ выдаётся, и это решение, а не пропуск.**
  Единственная оставшаяся задача дорожки — `t-lab-2`, лаборатория. Выдавать её сейчас значит
  улучшать ровно тот инструмент, который только что съел строку про собранную игру, и делать это
  до того, как владелец решил судьбу `t-handlers-1`.

  Естественный следующий шаг — **`review` ставки `bet-g-5a7c-wave-3`**, где сходятся все три
  вопроса сразу: судьба `t-handlers-1`, улика про недостижимый тихий шум и объём `t-lab-2`,
  который по своей же карточке зависит от того, сколько реально доехало.

  Параллельно в `house-and-things` продолжает идти `c-exec-g-5a7c-loot-foundation-001` — эта нога
  его не касалась ни одним файлом.

END_OF_FILE: live/indie-game-development/history/2026-08-13-s-work-g-5a7c-handlers-1-close-001.md
