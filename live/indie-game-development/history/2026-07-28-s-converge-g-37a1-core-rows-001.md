# RESULT — s-converge-g-37a1-core-rows-001

leg: s-converge-g-37a1-core-rows-001
direction: indie-game-development
play: converge
node: g-37a1
call: c-converge-g-37a1-core-rows-001
date: 2026-07-28
owner_present: false
owner_approved: false

## outcome

**The seventeen owner-approved core lines are now seventeen CHECKABLE ROWS, each with an outsider
test that can fail, and the §GLOSSARY and §WHAT the previous converge closed as a checkpoint without
ever reaching now exist.** Step 2 of his own six-step route (`d-post-verify-route-001`) is complete
and step 3 is issued. The artifact is `work/converge-g-37a1-core-rows.md`; it supersedes
`work/converge-g-37a1.md` as the assembly surface without copying a line of it.

**COVERAGE IS ARITHMETIC, NOT A CLAIM.** Fifteen criteria: thirteen carry at least one row; two are
deliberately row-less and are named rather than filled — criterion 13 is a NEGATIVE clause checked
by absence (its four seams are decided, see below) and criterion 15 is a governance clause about the
direction, held by the forecast and by `i-october-route-not-a-condition`. Four clauses inside
covered criteria are named as uncovered and NOT filled, and two more are marked as glosses rather
than outcomes.

**THREE THINGS THE ROWS ADD THAT NO PRIOR LEG HAD, and each closes a hole someone else had named.**

1. **§RULES — the ten readable rules.** Criterion 12 says «визуала может не быть, но все правила
   читаются» and nothing in state said WHICH rules, so the clause was unjudgeable and
   `i-card-clauses-unverifiable` recorded exactly that. Ten rules are derived from the rows and the
   acceptance test is stated: an outsider, after one session with no explanation, states the ten in
   his own words. The list is committed before the sitting; a rule he cannot state is a legibility
   defect, not a tester defect.
2. **§KNOBS — the parameter home's minimum contents, and it is four times the register.** Criterion
   12 says «все спорные числа». His register names five. The rows force at least twenty-odd, and
   one of them is named explicitly because the record proves it is unowned: **the simulation step
   rate**, for which no constant exists anywhere in the product, only director intervals — and
   without it as a knob, rows 9, 13 and 15 cannot be compared between runs at all. Like the venue,
   this needs no verdict from him: criterion 12 already IS his answer. It needs a price.
3. **The legibility deck is EXTENDED.** The existing instrument was called the strongest
   verification tool in either document, and its own banner says it measures naming consistency only
   and therefore cannot test «это видно до того, как в них вошли». The deck gains a second half —
   frames shot from OUTSIDE a pocket, beat chance on kind — and row 14 gains a size-ordering test,
   both with pass rules committed before the sitting.

**THE SIGNED DISTINCTION THAT MATTERS MOST, and any verifier who misses it will produce a false
finding.** The rock mark tells **SIZE, NOT KIND**. Before the cut: that there is a pocket and roughly
how big (criterion 2). After the breach and before entering: which kind (criterion 7). The two
legibility clauses sit at DIFFERENT MOMENTS, and collapsing them destroys the design — it is
precisely because size is known and kind is not that a breach is a priced gamble with two outcomes
rather than one trade repeated, which is the standing answer to his own recorded doubt that the
reason is «слишком скучная, нединамичная … один трейд повторяется».

**ONE ROW OF SEVENTEEN HAS NO OBSERVABLE, and it is not a defect of the writing.** Criterion 3 says
«Ценность внизу», and nothing in the seventeen lines gives a player anything he wants down there.
Under the rest of the set going down is strictly WORSE than digging sideways near the surface: the
way back is longer and what you release rises into the shaft you cut. Row 7 is therefore routed to
him with four options and a recommendation and is NOT decided. An uninstructed playtester, as the
set stands, digs sideways.

**THE ORACLE'S CLASS 12 BROKE THE ROWS EXACTLY WHERE IT PREDICTED IT WOULD.** Rows 8 and 11 make the
section monotone — holes never close, substance never leaves — so play CONSUMES a section. Row 4
returns the player at once and row 1 promises a repeat launch. **Nothing anywhere says where he
returns, what survives a death, or what a relaunch starts into.** That is one owner question with
three options, and it is load-bearing in four rows.

**AND A CONSEQUENCE NOBODY HAD DERIVED: at the default, death is the cheapest air refill in the
game.** Nothing in the seventeen lines refills air, and death costs zero with instant return. This
is not an argument against zero — it is why the price of death is a knob, and it is why the return
point in the question above decides whether zero is honest or exploitable.

**ONE COUPLING FINDING, booked nowhere before.** K1 (the breach event's form) and K3 (the per-kind
air-rate gap) are **not independent**. «Первое вскрытие не убивает мгновенно» is a property of the
PAIR: a violent breach form plus a large air-rate gap can kill instantly with both knobs inside
their own ranges. Either K3 carries an upper bound or the rig must demonstrate the property across
K3's range. That is a constraint for `shape`, not a value for this leg to pick.

**WHAT THE ROWS SETTLE THAT WAS OPEN.** Row 11 closes the direction's oldest unfixed problem —
«waiting is always optimal» — with a one-line check rather than a mechanic: after a breach there is
no state to wait FOR, because the substance does not leave. And §SEAM decides all four boundaries
where an excluded thing touches the modelled thing (a held object in the substance: nothing
happens; two kinds meeting: they mix and nothing else, which removes the entire firing-threshold
apparatus of the old §8 from the first core; a shaft too steep to leave: that is the legitimate
death, not a soft-lock; liquid: no seam in this outcome and its price is owed the day it is
admitted).

## evidence

- **ARTIFACT**: `live/indie-game-development/work/converge-g-37a1-core-rows.md` — seventeen rows,
  §GLOSSARY (26 terms), §RULES, §KNOBS, §WHAT, §SEAM, §ORACLE, §OWNER, §BUILD-CLOSES-BETTER,
  §SIGNOFF.
- **EVERY DONE_WHEN OF THE CALL IS MET.** (1) Seventeen rows, one per line, each stating what must
  be observably true, how a person who did not write it decides yes or no, and where the evidence
  comes from. (2) §GLOSSARY signed — twenty-five terms with one reading each, one deliberately NOT
  signed and routed. (3) §WHAT produced: all fifteen criteria mapped, every uncovered clause named
  rather than filled. (4) The fourteen oracle classes swept against the ROWS, verdict per class.
  (5) Rows that cannot be decided without him listed separately with options and a recommendation
  and NOT decided. (6) Rows a build closes better than a document marked for the shape leg.
- **THE SETTING RULE WAS HELD, and it is the rule this route was built around.** Five registered
  knobs K1–K5 appear in rows 3, 4, 9, 13, 14 and 15. **No value was chosen for any of them.** Each
  row tests three things and no more: the knob exists, it is reachable from a BUILT player, and
  changing it changes what it claims to change. One test was written specifically to catch the
  failure mode: a knob whose change produces no describable difference FAILS as a knob even though
  the file accepted the value.
- **THE MINER RAN, as `converge` step 2 mandates.** One read-only child swept every noun of the
  fifteen criteria, the seventeen lines and CHARTER's six lenses against all of
  `live/indie-game-development/**` for second readings on disk. It returned about seventy terms with
  locators. **Its yield that changed the artifact:** «вещество» carries a dead reading in which it
  is carried OUT of the section (the dropped `g-12fd`); «счётчик» carries the retired
  air-is-the-ONLY-counter reading; «сток» is read two ways in the SAME file (no sinks now, one
  expensive sink accepted for a later iteration); «пересборка» means recompile in criterion 12 and
  concept-rebuild in CHARTER; «правила», «условия», «результат», «объём», «заход», «сцена», «закон»
  and «вердикт» each carry two live readings; and **«вечная лаборатория» is defined nowhere on
  disk**, which is what turned criterion 14's anti-`g-12fd` guard into a named gap.
- **FIRST-HAND SOURCES READ THIS LEG**, all in the direction repository at `6987a48f`:
  `os/KERNEL.md`; `os/plays/converge.md`; `os/plays/converge-verify.md`; `os/schema/packets.md`;
  `.agents/skills/direction-os/SKILL.md`; `AGENTS.md`; `CHARTER.md`; `TREE.md`; `NOW.md`; `LOG.md`;
  `knowledge/strategy-reset-boundary.md`; `work/c-converge-g-37a1-core-rows-001-call.md`;
  `work/core-requirements-g-37a1.md`; `work/oracle-first-playable-core-v1.md`;
  `work/converge-g-37a1.md` (all fourteen sections and every banner);
  `history/2026-07-28-s-map-g-37a1-core-requirements-001.md`;
  `history/2026-07-27-s-converge-verify-g-37a1-digging-card-001.md`.
- **NO ENGINE FACT DECIDED A ROW.** Engineering facts appear in exactly three places and all three
  are cited as RECORDED EVIDENCE about risk, never as a reason a row says what it says: row 1's
  venue (one enabled scene, zero build automation), row 2 (the shipped body is a planar walk with no
  gravity and no ground contact), and row 13's known disagreement between what renders and what
  charges air. The step-4 check was neither read nor waited for, per his parallelism instruction.
- **STATE MOVED UNDER THIS LEG AND WAS RECONCILED BEFORE ANY WRITE, and this must be read exactly
  as written.** The leg opened at `6987a48f`. **Step 4 — `s-research-g-37a1-engine-fit-check-001` —
  LANDED at `117da6a0` while the rows were being written**, closing its own `open_calls` row,
  rewriting `direction_forecast.drivers[3]` and amending `i-engine-fit-decision-ladder`. That was
  discovered at apply time, `NOW.md` was re-read fresh, and every edit below is written against the
  current file with its concurrent changes preserved. **NO ENGINE FACT FROM IT ENTERED A ROW**, and
  the rows were complete before it was seen. Its receipt
  `history/2026-07-28-s-research-g-37a1-engine-fit-check-001.md` was **not opened**; what was seen,
  and only during the rebase, is the summary it wrote into `NOW.md` itself. **One convergence is
  declared so that no reader mistakes it for a copy:** that check independently prices row 9
  (breach behaviour) as the largest row at 30–54 days, and it finds the determinism-and-replication
  layer NOT a rewrite target — the same two things this artifact says about row 9's cost and row
  16's cheap-to-get-wrong warning, reached from the requirements side with no engine fact in hand.
- **`work/converge-g-37a1.md` WAS READ AS MATERIAL AND NOT AS AUTHORITY.** Every banner was read
  before the body it corrects. Nothing was copied; what survives is cited by section. The one thing
  taken forward whole is §14's legibility instrument, and it is taken forward EXTENDED, with its own
  banner's scope limit honoured rather than ignored.

## state_changes

`live/indie-game-development/work/converge-g-37a1-core-rows.md` — CREATED. The leg's artifact and
the new assembly surface for `g-37a1`. Input to step 3 and to step 6.

`live/indie-game-development/work/c-converge-verify-g-37a1-core-rows-001-call.md` — CREATED. Step 3
of the agreed route: a fresh session with no owner tries to break the rows. Self-contained; names
the six closed rulings that may not be re-litigated and the SETTING rule that makes a value attack a
false finding.

`live/indie-game-development/work/converge-g-37a1.md` — a single SUPERSEDED pointer line added to
the head banner block. No line of the body rewritten, no evidence removed.

`live/indie-game-development/work/c-converge-g-37a1-core-rows-001-call.md` — a `disposition:` block
appended: discharged, what was produced, what was routed, the successor.

`live/indie-game-development/NOW.md`

- `updated:` to this leg.
- `direction_forecast.drivers[0]` — a leading sentence recording that step 2 is closed, with the
  receipt paths. Nothing else in the driver altered.
- `i-card-clauses-unverifiable` — the experiment-rig row annotated: the two things it named as
  missing now exist (§RULES supplies what «все правила читаются» is judged against; §KNOBS supplies
  the parameter home's minimum contents and shows criterion 12 forces four times the registered
  five). The VENUE row is unchanged and still owed a price; the passability row stays closed.
- `i-substance-passage-open-questions` — annotated with one clause: «why the crew goes in» is now
  sharpened by row 7 into the specific fork `d-core-value-below-001`.
- `open_calls`: `c-converge-g-37a1-core-rows-001` OUT (spent);
  `c-converge-verify-g-37a1-core-rows-001` IN (ready); `c-research-g-37a1-engine-fit-check-001`
  preserved verbatim, still ready and still running in parallel.
- `decisions`: three ADDED, all with options, a downside each and a firm recommendation, none
  answered — `d-core-value-below-001`, `d-core-run-lifecycle-001`, `d-core-outcome-exit-test-001`.
  `d-air-counter-visibility-001`, `d-october-route-charter` and `d-post-verify-route-001` preserved
  verbatim; the last has its progress note updated to step 2 complete.

`live/indie-game-development/LOG.md` — this leg's line prepended once.

`live/indie-game-development/history/2026-07-28-s-converge-g-37a1-core-rows-001.md` — this RESULT.

Not touched: `CHARTER.md`; `TREE.md` and either card, which carry his verdict on exact text;
`knowledge/**`; `core-requirements-g-37a1.md`, which is his approved artifact; the product
repository; Steam state; `archive/**`. No bet, task, track, lane or executor CALL created. No
numeric release chance. `d-air-counter-visibility-001` not answered, as the CALL forbids.

## captures

- **The two legibility clauses of `g-37a1` are at different moments, and nothing in state said so
  until now.** That single sentence is what separates a priced gamble from one repeated trade, and
  it was implicit in a forecast driver and in no card, artifact or issue. Candidate for
  `knowledge/` at the review that closes `g-37a1`; `converge` does not write `knowledge/`.
- **A converge that produces an outsider test per row also produces the shape leg's task list for
  free.** Nine of the seventeen rows are marked build-closes-better because their outsider test
  cannot be run without a build. That mapping came out of the row form itself and cost nothing;
  `g-12fd` died for lack of exactly it.
- The oracle earned its keep a second time: class 12 (run lifecycle) broke the rows precisely where
  its own «what breaks if unanswered» text predicted. Its promotion to `knowledge/` is now supported
  by two independent legs rather than one.
- «Спорное число становится настройкой» has a second half nobody wrote down: **a knob is only a knob
  if changing it produces a describable difference.** A parameter file that accepts a value and
  changes nothing is the cheapest possible fake, and it would pass a naive reading of criterion 12.

## decisions_needed

- id: d-core-value-below-001
  q: What is below, that makes «ценность внизу» true?
  options: [one authored destination at the bottom; deeper pockets are bigger and denser; something
  to collect placed below; leave it as level-authoring direction and not a mechanic]
  recommendation: one authored destination at the bottom — cheapest, authors not one cut, turns
  depth into the price of a run, and it is the only option that makes rows 3, 11 and 15 bite
  together.

- id: d-core-run-lifecycle-001
  q: What is a run, where does the player return after death, and what does a relaunch start into?
  options: [a run is one descent and every launch is a fresh section; the section persists across
  deaths inside one session, resets on relaunch, and he returns at the entrance; the section
  persists across launches]
  recommendation: persists across deaths inside a session, resets on relaunch, return at the
  entrance — it keeps the comparability the rig needs, makes the walk back the real price of a
  death so that zero is honest rather than exploitable, and is the smallest of the three.

- id: d-core-outcome-exit-test-001
  q: What operationally ends `g-37a1`, so that «не вечной лабораторией» is enforceable?
  options: [a number of distinct play-days after which review runs regardless; closure triggered by
  the first day work starts on the next node; leave it as prose]
  recommendation: a small number of distinct play-days that he sets himself — it is a PROCESS
  number, not a game number, so it cannot be deferred to the rig.

- id: d-air-counter-visibility-001
  status: OPEN, untouched, explicitly out of this CALL's scope. Newly established and worth
  recording: it does NOT block row 15, whose outsider test works with an invisible counter. It
  blocks only reading a TEAMMATE's air, and handing air over is outside the core. Off the critical
  path.

## play_check

- **Step 1 Triage & import** — done. `triage: heavy — converge ON — because model-bearing and
  cross-cutting`, written into the artifact header and repeated here. Imported owner-approved
  decisions listed and born-closed: the fifteen criteria, the seventeen lines, the settings
  register, the six rulings of 2026-07-28 and the six-step route. **Every imported answer that NAMES
  a mechanism was run through Resolve 3(c)** — closing a mechanism never closes its parameters —
  which is where §KNOBS comes from and why it is four times the register.
- **Step 2 Define** — done. §GLOSSARY, terms seeded MECHANICALLY from the criteria's and the lines'
  own words, and **one miner `call:research` was run as the play mandates** (read-only child, scope
  `live/indie-game-development/**`, forbidden `archive/**`, the product repo and the deciding
  session's own reasoning). Terms read two or more ways were signed; the yield that changed the
  artifact is itemised under evidence. **DEVIATION, declared:** the play signs disputed terms under
  G7 with the owner's words, and this CALL specifies NO OWNER. So each term is signed by CITATION to
  text he has already approved on exact text or to his recorded words, and a term whose reading his
  words do not fix is NOT signed but routed. The CALL's own done_when defines signing as «every term
  that carries weight across rows has one reading», which is what was produced.
- **Step 3 Resolve** — done. Flat row list, one question per line, derived from all three sources of
  the no-white-spots net: (a) every criterion, via §WHAT; (b) every cross-node edge the boundaries
  name, which for this node is `g-5e8c` (the section chain and its network venue) and `g-2b7f` (the
  five honest screenshots, which depend on rows 13 and 14 producing something legible) — both named,
  neither reopened; (c) MECHANISM DECOMPOSITION, which is where every `FORCES` line comes from and
  which produced §KNOBS. **FIREWALL held:** vendor, algorithm, layout and magnitude were routed
  `→ PLAN` / step 4 and are never a row's content — the substrate unit, the tick rate's value, the
  flow rule's inputs, the replication mechanism. Acceptance PROPERTIES stayed in the rows. Rows
  carrying both were split, never routed whole.
- **Step 4 Close & route** — done. No executor CALL emitted. `next` is `converge-verify`, per the
  play's routing and per step 3 of his own route.
- **G7** — three owner forks surfaced, batched into one §OWNER section, each with options, a
  downside per option and a firm recommendation, and **none decided**. None of the three is a
  contested NUMBER; contested numbers became knobs and are in §KNOBS.
- **Owner-verdict guard** — no owner verdict was invented, inferred, or stretched. This leg had no
  owner and claims none. The artifact's §SIGNOFF says so in as many words.
- **G5** — not claimed. This leg is the work, not its verification; the binding refutation is step 3
  in a separate fresh physical chat and its CALL is issued. No in-session pre-pass is claimed either:
  the one child spawned was a read-only miner mandated by the play, not a refuter.
- **Boundaries honoured** — converge only; no bet, task, track, lane or executor CALL; `CHARTER.md`,
  `TREE.md` and both cards untouched; `knowledge/` not written; `archive/**` not read; the product
  repository and Steam state not touched; no numeric release chance; `d-air-counter-visibility-001`
  not answered; the six rulings of 2026-07-28 not reopened; step 4 not waited for, its receipt not
  opened, and its findings not folded into any row — see the reconciliation note under evidence for
  exactly what was seen and when.
- **ONE BOUNDARY JUDGMENT, declared for a reviewer to overturn.** The CALL forbids touching either
  CARD; it says nothing about `work/converge-g-37a1.md`, which the `converge` play lists as the
  play's own write target. A **single pointer line** was added to that file's existing head banner
  block saying it is superseded as the assembly surface by this leg's artifact. No line of its body
  was rewritten and no evidence was removed. The reason is the failure this direction has already
  paid for twice: a superseded file with no pointer is read as current by the next leg. If a
  reviewer reads the boundary as covering it, the repair is to revert that one line; nothing else
  depends on it.
- **Fan-out** — ONE child, the miner the play mandates in step 2. No refuters, no strategic_search
  (no row was contested enough to need three-to-five refuted options; the three that were became
  §OWNER forks instead, which is the play's own escalation), no workflow. Read-only.
- **Two-strikes** — not live. No point in this leg needed two correction rounds, and no finding of
  this leg repeats a finding the previous verification made against the same target.
- **Errors of this leg, reported unprompted.** (1) The first pass wrote a pass rule for row 7
  («ценность внизу») before noticing that nothing in the seventeen lines gives a player a reason to
  go down; the rule was withdrawn and replaced by a MEASUREMENT plus an owner fork, because a pass
  rule there would have been exactly the invented value this route exists to avoid. (2) Row 15 was
  first written as blocked on `d-air-counter-visibility-001`; that was wrong — its outsider test
  works with an invisible counter — and the correction is recorded because it takes a four-times-
  surfaced decision off the critical path.
- **canon_proposed** — none written, `converge` does not write `knowledge/`. Two candidates named
  for the review that closes `g-37a1`: the size-versus-kind timing distinction, and
  `work/oracle-first-playable-core-v1.md`, whose promotion now has two independent legs behind it.
- **converge_coverage** — every one of the fifteen criteria maps to a row or to a declared
  `covered_by` in §WHAT; every cross-node edge is named; every committed mechanism is decomposed
  into §KNOBS. Coverage is MECHANICAL — completeness rests on step 3 and correctness on his gate,
  and this leg claims neither.

## log

g-37a1/core-rows: the seventeen owner-approved core lines are now seventeen checkable rows with an outsider test each that can actually fail, and the §GLOSSARY and §WHAT the previous converge closed as a checkpoint without ever reaching now exist, closing step 2 of his own six-step route — coverage is arithmetic and not a claim: thirteen of fifteen criteria carry a row, criterion 13 is a NEGATIVE clause checked by absence with all four of its seams decided (a held object in the substance does nothing; two kinds meeting mix and nothing else, which removes the whole firing-threshold apparatus from the first core; a shaft too steep to leave IS the legitimate death; liquid has no seam here and owes its price the day it is admitted) and criterion 15 is a governance clause held by the forecast, with four further clauses named as uncovered and NOT filled and two marked as glosses; three things the rows add that no prior leg had — §RULES, the ten readable rules that «все правила читаются» is finally judged against, §KNOBS, the parameter home's minimum contents which is four times his register of five and which names the simulation step rate explicitly because no constant for it exists anywhere in the product and without it nothing can be compared between runs, and an EXTENSION of the legibility deck to frames shot from outside a pocket, because the existing instrument measures naming consistency only and its own banner says it cannot test «видно до того, как вошли»; the distinction that matters most is signed — the rock mark tells SIZE, not KIND, so the two legibility clauses sit at different moments and collapsing them destroys the design, since it is precisely because size is known and kind is not that a breach is a priced gamble with two outcomes rather than one trade repeated; one row of seventeen has no observable and it is not a writing defect — nothing in the set gives a player anything he wants below, and under no-sinks going down is strictly worse than digging sideways, so it goes to him with four options and a recommendation and is not decided; the oracle's run-lifecycle class broke the rows exactly where it predicted, because rows 8 and 11 make a section monotone and therefore consumed by play while nothing anywhere says where the player returns, what survives a death or what a relaunch starts into; a consequence nobody had derived is that at the default nothing refills air except death, so death is the cheapest refill in the game, which is why its price is a knob and why the return point decides whether zero is honest or exploitable; one coupling booked nowhere before is that the breach form and the air-rate gap are not independent, so «первое вскрытие не убивает мгновенно» is a property of the pair and either the gap carries a bound or the rig must show the property across its range; row 11 closes the direction's oldest unfixed problem, that waiting is always optimal, with a one-line check and no new mechanic — after a breach there is no state to wait for, because the substance does not leave; the setting rule was held with no value chosen for any of the five knobs and one test written specifically to catch the fake, since a knob whose change produces no describable difference fails as a knob even though the file accepted the value; and the leg issues step 3, a fresh no-owner session that tries to break the rows, while step 4 keeps running in parallel. -> history/2026-07-28-s-converge-g-37a1-core-rows-001.md

## next

CALL `c-converge-verify-g-37a1-core-rows-001` — step 3 of the owner's recorded route.

to: session · direction: indie-game-development · play: converge-verify · node: g-37a1 ·
verify_target: build · surface: a FRESH chat, separate from this one, no owner · call file:
`live/indie-game-development/work/c-converge-verify-g-37a1-core-rows-001-call.md`

goal: try to BREAK the seventeen rows against the fifteen criteria — either the set survives with a
signoff, or named rows come back unfilled.

Still running in PARALLEL, untouched by this leg:
`c-research-g-37a1-engine-fit-check-001` — step 4, the bounded read-only engine-fit read, returning
to `i-engine-fit-decision-ladder`.

Then step 5 (his ladder verdict, which needs him and needs both step 3 and step 4 back) and step 6
(`shape`, the first leg that may create a bet or a task). The three §OWNER rows reach him at step 5,
or at step 6 if the shape leg needs them sooner.

END_OF_FILE: live/indie-game-development/history/2026-07-28-s-converge-g-37a1-core-rows-001.md
