# RESULT — s-work-g-5a7c-chase-jitter-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-handlers-1
track: householder-life
date: 2026-08-13
outcome_kind: dispatch — отдельная нога на дрожь выпущена его словом; наряд на обработчики встал за ней

owner_approved: |
  **Дословно, 2026-08-13, в ответ на вынесенное решение:** «давай отдельную ногу на дрожь сейчас».

  Выбран вариант «отдельная нога сейчас» из двух показанных ему вместе с рекомендацией и с честной
  ценой в одну ногу против порога смерти 3.

  **Его же наблюдение, ради которого нога и существует, дословно:** «Хозяин бежит, единственное, что
  он дёргано бежит, там бывает как будто в проёме застревает, вот он туда-сюда дёргается. Но потом
  как бы вылазит. Вроде бы было даже не в проёме такое».

outcome: |
  **НАРЯД `c-exec-g-5a7c-chase-jitter-001` ВЫПУЩЕН ЕГО СЛОВОМ.**

  **ПРИЧИНА НАЙДЕНА В БАЙТАХ ПРИ ВЫДАЧЕ, И ЭТО НЕ ТА ФОРМУЛИРОВКА, ЧТО ЛЕЖАЛА В УЛИКЕ.** Улика
  несла гипотезу исполнителя зрения — «маршрут перевыводится каждый тик». Выдача прочитала код и
  нашла точный механизм, которого в улике не было: `HouseWalkPath.EndsAt` сравнивает цель **ТОЧНО,
  без допуска**, и в том же файле лежит объяснение почему — «оба числа суть копии одного и того же
  авторского или однажды измеренного значения». **Зрение впервые в проекте дало цель, которая
  двигается каждый тик, и этим молча сломало записанный инвариант:** `EndsAt` теперь возвращает
  false на КАЖДОМ тике погони, и путь через дом строится заново много раз в секунду.

  Это делает наряд честным ремонтом: сломанное место названо, а не угадано.

  **НО ЗАМЕР ВСЁ РАВНО ОБЯЗАТЕЛЕН, И НАРЯД ЭТОГО ТРЕБУЕТ ПЕРВОЙ СТРОКОЙ.** Перестройка пути каждый
  тик сама по себе дрожи не доказывает: перестроенный путь может оказаться тем же самым. Видимое
  дёрганье появляется, только если перестройка даёт ДРУГОЙ маршрут. Это гипотеза, и её меряют, а не
  принимают.

  **СОПЕРНИЧАЮЩЕЕ ОБЪЯСНЕНИЕ РАЗВЕДЕНО ЗАРАНЕЕ, А НЕ ОСТАВЛЕНО НА ПОТОМ.** В проёме работает вторая,
  уже измеренная причина: капсула хозяина диаметром `1.0` в проёме шириной `1.5` даёт по `0.25` м
  запаса и только по центру (`i-householder-walks-through-walls-001`). Наряд требует померить обе
  доли отдельно и мерить И в проёме, И в открытой комнате — потому что владелец сказал «вроде бы
  было даже не в проёме такое». Если дрожь целиком объясняется шириной прохода, наряду предписан
  СТОП домой, а улика про дрожь тогда закрывается как дубль — это законный исход, названный заранее.

  **НАРЯД НА ОБРАБОТЧИКИ ВСТАЛ ЗА ЭТОЙ НОГОЙ, И ЭТО ВЫНУЖДЕНО ДВАЖДЫ.** Первое — правило дорожки:
  у полосы законен ровно один корневой наряд, а второй входит только ребёнком, и родитель тогда
  ждёт. Второе — файл: обе ноги правили бы `Core/Householder/Householder.cs`, а сглатывание чужой
  правки при параллельной работе у нас уже зарегистрировано уликой. Поэтому дрожь идёт первой,
  обработчики — сразу за ней. Он просил дрожь СЕЙЧАС, и «первой» это и есть.

evidence: |
  **ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ ЧТЕНИЕМ БАЙТОВ НА `c485b30e704b1706675dd92d15c5223b0d166b92`.**

  ## Механизм дрожи — три строки, которые его составляют

  - `Core/Householder/Householder.cs:2421-2434` — пока крыса видна, цель ответа переписывается
    каждый тик живой позицией из свежей ситуации (`WithTrackedTarget(input.PendingEvent.Position.Value, …)`).
  - `Core/Householder/Householder.cs:3116-3122` — `if (path == null || !path.IsPresent ||
    !path.EndsAt(responseTarget))` → `HouseWalk.To(house, position, responseTarget)`.
  - `Core/House/HouseWalk.cs:78-84` — `EndsAt` возвращает
    `Goal.X.Equals(point.X) && Goal.Z.Equals(point.Z)`, точное сравнение без допуска, **и рядом
    записана причина этого выбора**: «both numbers are copies of the same authored or measured value,
    so „near enough“ would only be a way of keeping a walk that was drawn for somewhere else».

  Инвариант, на котором стояло точное сравнение, зрением нарушен: цель погони копией авторского
  значения не является. Ни один тест этого не ловит — среди шести тестов зрения нет ни одного про
  устойчивость пути.

  ## Соперничающее объяснение — уже измеренные числа чужой улики

  `i-householder-walks-through-walls-001`: `HousePassage` несёт `Width`, `Core/House/HouseWalk.cs`
  не читает его ни разу; капсула диаметром `1.0` (`Settings/HouseholderWalkSettings.asset:24`),
  проёмы `1.5`, запас `0.25` на сторону и только по центру. В проёме обе причины дают один и тот же
  видимый симптом.

  ## Почему наряды сериализованы, а не идут параллельно

  - Правило полос: у полосы не больше одного бесхозного корня; ребёнок обязан быть в `waiting_on`
    родителя. `householder-life` уже имеет корень `c-exec-g-5a7c-handlers-1-001`, поэтому дрожь
    входит его ребёнком, а родитель уходит в `waiting`. Ровно этот же рисунок сейчас живёт в
    `house-and-things`: `c-exec-g-5a7c-loot-1-001` (paused) и его ребёнок
    `c-exec-g-5a7c-loot-foundation-001` (ready).
  - Файл: наряд на обработчики добавляет физическое действие в `Householder.cs`, а починка дрожи
    правит `StepTrackedVisibleResponse` в том же файле. Класс аварии зарегистрирован —
    `i-parallel-session-commit-swallows-another-legs-edit-001`.
  - Пересечения с другой дорожкой нет: `c-exec-g-5a7c-loot-foundation-001` живёт в грузе, сценах и
    префабах, и обе стороны это друг другу запретили.

  ## Контракт и слот

  `os/engineering/CONTRACT_VERSION` → `current: 36`; `validation.config:5` продукта →
  `"synced_contract_version": 36`. Дрейфа нет. Наряд запинен на `36` и несёт три строки потолка.
  Слот не присвоен: выбирается селектором, все четыре читались `AVAILABLE / lease none` при выпуске.

  ## Гейт второго вида здесь не стоит, и это цитата, а не поблажка

  `knowledge/core-is-extended-never-rewritten.md`, оговорка 4: **«Починки и чистые пробы второго вида
  не имеют»**. Наряд говорит это исполнителю прямо, чтобы он не выдумал второй вид ради гейта.

state_changes: |
  1. Выпущен наряд `c-exec-g-5a7c-chase-jitter-001` (`ready`, `to: executor`, `kind: engineering`,
     `repo: ainazemtsau/GasCoopGame`, `engineering_contract: 36`, `track: householder-life`,
     `for: t-handlers-1`, `parent: c-exec-g-5a7c-handlers-1-001`, `basis: c485b30e…`) плюс его файл
     `work/c-exec-g-5a7c-chase-jitter-001-call.md`.
  2. `c-exec-g-5a7c-handlers-1-001`: `ready → waiting`, `waiting_on: c-exec-g-5a7c-chase-jitter-001`.
     Наряд не тронут ни одной буквой: меняются только статус и поле ожидания.
  3. `i-householder-chase-path-jitters-in-doorways-001`: журнальная строка — нога выпущена его
     словом, и записан механизм, найденный выдачей в байтах. Тело улики НЕ переписано: её гипотеза
     остаётся гипотезой до замера, как она сама и требует.
  4. Полный RESULT сохранён в `history/2026-08-13-s-work-g-5a7c-chase-jitter-dispatch-001.md`.

  **ЧЕГО ЭТА НОГА НЕ СДЕЛАЛА.** Не заведено ни одной задачи: дрожь остаётся уликой, а не двенадцатой
  задачей волны — аппетит подписанной ставки не тронут. Не тронута `i-householder-walks-through-walls-001`
  и её `review_when`. Не тронуты `t-loot-1`, оба наряда по луту и ветка `preserve/...`. Не тронута
  карточка ставки. В продукте не изменено ничего.

captures:
  - |
    **ЗРЕНИЕ СЛОМАЛО ЗАПИСАННЫЙ ИНВАРИАНТ, И ИНВАРИАНТ БЫЛ ЗАПИСАН ПРЯМО В ТОМ ЖЕ ФАЙЛЕ.**
    `HouseWalk.cs` объяснял своё точное сравнение цели тем, что цель всегда копия авторского или
    однажды измеренного значения. Зрение дало первую в проекте цель, движущуюся каждый тик, — и
    сломало это молча, потому что инвариант жил в комментарии, а не в проверке. Класс дефекта
    полезный и не про зрение: **новый источник может опровергнуть допущение, записанное чужим слоем
    прозой.** Дешёвая привычка на будущее — когда наряд заводит источник нового ТЕМПА (каждый тик
    вместо разового), пройтись по потребителям и поискать в них слова «copy», «authored», «once»,
    «exact».
  - |
    **ЦЕНА ПАРАЛЛЕЛЬНОСТИ ВПЕРВЫЕ УПЁРЛАСЬ НЕ В ФАЙЛЫ РАЗНЫХ ДОРОЖЕК, А В ОДНУ ПОЛОСУ.** Три
    дорожки волны разведены по владению файлами и не сталкиваются. А две ноги ВНУТРИ одной полосы
    столкнулись сразу: один корень на полосу по правилу, и один и тот же `Householder.cs` по факту.
    То есть внутри дорожки параллельности нет вовсе, и это стоит помнить при нарезке следующей
    волны, а не открывать заново.

decisions_needed: []

play_check:
  - 1 recite — done. Пересказаны улика дрожи с его дословными словами, её требование «сначала измерить, а не чинить по гипотезе», соперничающая улика про ширину прохода и правило, снимающее гейт второго вида с починок.
  - 2 owner inputs (owner) — done. Его решение получено дословно: **«давай отдельную ногу на дрожь сейчас»**, в ответ на два варианта с рекомендацией и с названной ценой в одну ногу против порога смерти 3. Само наблюдение тоже его и процитировано дословно.
  - 3 do the work — измерение поверхности на `c485b30e`, нахождение сломанного инварианта в трёх строках, разведение соперничающей причины числами чужой улики, написание наряда с обязательным замером перед починкой.
  - 4 self-check — done. Наряд несёт три строки потолка контракта 36, требует замер ПЕРЕД правкой, называет законный исход «это дубль, СТОП домой», запрещает поверхность обработчиков и файлы параллельного наряда, и защищает семь строк только что закрытой `t-sight-1`.
  - 5 close — dispatch. Не последняя задача ставки, поэтому продолжение — сам выпущенный наряд, а не `review`.
  - G5 — не применяется: ничего не закрыто.
  - G7 — done. Игровое решение не принято: что чинить, решил ЗАМЕР, а не нога; какой допуск или удержание выбрать — техническое КАК исполнителя; выбор «отдельная нога сейчас» сделан владельцем его словом.
  - G3 — done. Аппетит не расширен: ноль новых задач, дрожь остаётся уликой, объём наряда — одна улика, три строки.
  - G10 — done. RESULT финальный, поля полные.

log: отдельная нога на дрожь выпущена ЕГО СЛОВОМ «давай отдельную ногу на дрожь сейчас», и выдача нашла в байтах причину, которой в улике не было — улика несла гипотезу исполнителя зрения «маршрут перевыводится каждый тик», а чтение показало точный механизм: HouseWalkPath.EndsAt сравнивает цель ТОЧНО, без допуска, и в том же файле записано, почему так можно — «оба числа суть копии одного и того же авторского или однажды измеренного значения»; зрение впервые в проекте дало цель, которая двигается КАЖДЫЙ ТИК, и этим молча опровергло записанный инвариант, поэтому EndsAt возвращает false на каждом тике погони и путь через дом строится заново много раз в секунду, чего не ловит ни один из шести тестов зрения; при этом наряд ВСЁ РАВНО требует замер первой строкой, потому что перестройка пути сама по себе дрожи не доказывает — видимое дёрганье появляется только если перестройка даёт ДРУГОЙ маршрут, и это гипотеза; соперничающее объяснение разведено заранее числами чужой улики — капсула 1.0 в проёме 1.5 даёт запас 0.25 на сторону, поэтому мерить велено И в проёме, И в открытой комнате, раз он сказал «вроде бы было даже не в проёме такое», а если ширина объясняет всё, предписан СТОП домой и улика закрывается как дубль, что названо законным исходом заранее; гейт второго вида с наряда СНЯТ цитатой правила «починки и чистые пробы второго вида не имеют», чтобы исполнитель не выдумал вид ради гейта; наряд на обработчики встал ЗА этой ногой и это вынуждено дважды — у полосы законен один корень, а ребёнок обязан быть в waiting_on родителя, и обе ноги правили бы один и тот же Householder.cs, чей класс аварии уже зарегистрирован уликой; записано следствие для нарезки: три дорожки разведены по файлам и не сталкиваются, а две ноги ВНУТРИ одной полосы столкнулись сразу, то есть внутри дорожки параллельности нет вовсе

next: |
  `c-exec-g-5a7c-chase-jitter-001` — наряд исполнителю, слот выбирается селектором. Он либо
  возвращает замер плюс починку со сценарием для владельца, либо СТОП с измерением, что причина —
  ширина прохода.

  За ним сразу `c-exec-g-5a7c-handlers-1-001`: он стоит `waiting` и станет `ready` сам, как только
  дрожь вернётся.

  Параллельно в `house-and-things` идёт `c-exec-g-5a7c-loot-foundation-001` от той же головы.

END_OF_FILE: live/indie-game-development/history/2026-08-13-s-work-g-5a7c-chase-jitter-dispatch-001.md
