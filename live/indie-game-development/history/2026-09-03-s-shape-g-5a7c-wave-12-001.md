RESULT s-shape-g-5a7c-wave-12-001 (call: c-shape-g-5a7c-wave-12-001)
direction: indie-game-development   track: —   play: shape   node/task: g-5a7c → bet-g-5a7c-wave-12

outcome: |
  Двенадцатая волна нарезана как рубеж Р1 «Скелет утра» принятого плана. Ставка `bet-g-5a7c-wave-12`
  под узлом `g-5a7c`: цель — имя рубежа, срок 11.09, `done_when` — одиннадцать строк рубежа
  построчно с видом улики на каждую и с пометкой переносимой. Десять задач в четырёх полосах
  (= слоты раздела 6), четыре первых наряда выданы, стенд «хозяин» — первой ногой и в WIN-U4.
  Его слово на весь экран волны: **«все принимаю»**, и оно же принимает порядок смерти.

  **Четыре замера сняты при самой нарезке, первой рукой, и три из них плану не были известны.**
  (1) Сборка одной командой уже существует — `tools/unity-build.ps1` → `PlayerBuildCommand.cs` →
  `Builds/StandaloneWindows64/TunnelCrew.exe` с машинным `BuildReport`; строка рубежа стоит не
  скрипт, а номер версии и папку выдачи. (2) Давление темп не выбирает: все 15 присвоений
  `MovementPaceName` вне тестов лежат в реакциях и в загрузчике профиля, в пути давления — ноль;
  строка «не ускоряет ходьбу» наполовину зелёная по чтению. (3) Шов, которым стенд ставит удар,
  существует и назван: расстановка `IntegrationLab.<имя>.json` схемы
  `tunnelcrew-integration-lab-arrangement.v2`, кусок `launched-cargo`, полка `IntegrationLabWindow`,
  приборы обратно через `LabShoveObserversRpc`; **чего нет — вида куска «мишень-хозяин»: их три,
  все про мышь и груз.** Это и есть пробел, который review назвал первым, обо что нога споткнётся.
  (4) Локализации нет вовсе, игрового текста около пяти строк — закладка сейчас действительно
  дешёвая.

  **Замах назван честно и против себя.** Десять задач при историческом потолке: ни одна волна с
  планом больше семи задач не доходила целиком (4 → дошла; 11/0 → дошла; 7 план/11 факт → partial;
  23/3 → partial; 6 план/**14** факт → partial). Список рубежа менять нельзя, поэтому лечение —
  порядок смерти, написанный до работы: первым умирает звук, вторым инструкция интернета, третьим
  магнит, четвёртым шлепок, пятым номер версии, шестым таблица строк с названной ценой. Не умирают
  стенд, конец утра, экран результата и сценарий на четверых.

  **Решение о звуке принято вслух, и это не третий, незаконный ответ.** Ухо игрока берётся
  (временные звуки шести событий); окклюзия слуха хозяина откладывается — с замером, который
  доказывает, что работа там есть: три радиуса в `GameRulesSettings.cs:263-265`, ноль совпадений
  `occlu|linecast|raycast` в слуховом пути, полноценный шов у зрения. По шести кускам спеки давления
  сказано по слову; шестой — `B_host = β·T_baseline` — отложен **с оговоркой этой волны**: утро
  удлиняется в 3–4 раза, то есть `T_baseline` меняется, и числа профиля перенастраиваются руками.

  Волна техническая: квитанции `concept_basis` нет, строки содержания утра перенесены без штрафа.
  Три слова раздела 11.1 положены ему на стол до первого шага плея; он их в этой сессии не дал, и
  рубеж Р1 они не держат.

evidence: |
  - Его слова в этой сессии: «все принимаю» — на показанный целиком экран волны (цель, замах с
    внешним взглядом, десять задач в четырёх полосах, порядок смерти, шесть вырезов, решение о
    звуке, шесть кусков спеки, семь линз, самое рискованное допущение, семь порогов, прогноз и
    довод против, шов удара). До него — «все правильно» на сверку состояния направления.
  - Продукт перемерен первой рукой 2026-09-03: `main` = `dev` = `origin/main` = `origin/dev` =
    `2d2c27ff`; `slot/win-u1..u4` влиты в `main` без остатка, все четыре рабочих дерева чисты;
    `gascoop-slot-state.v1.json` — всем четырём `lifecycle: AVAILABLE`, `lease: none`.
  - `Assets/Settings/PlayMode/` — ровно один файл `HostAndOneClient.asset` (сценария на четверых нет).
  - `tools/unity-build.ps1` + `Assets/Editor/PlayerBuildCommand.cs` — сборка одной командой есть;
    `DefaultOutputDir = "Builds/StandaloneWindows64"`, `ExecutableName = "TunnelCrew.exe"`.
  - `MovementPaceName` вне тестов: 15 присвоений, все в `Probe/Householder/HouseholderReaction.cs`
    и `Profiles/HouseholderProfileLoader.cs`; в `HouseholderTimeReserve.cs` и `HouseholderStanding.cs`
    — ноль.
  - `Assets/TunnelCrew/Editor/IntegrationLabArrangement.cs` — `ParseKind` знает `frozen-walker`,
    `launched-cargo`, `hovering-cargo`; семь расстановок `IntegrationLab.*.json` лежат рядом;
    `IntegrationLabWindow` — полка, `NetworkWalkerCourier.TryLoadLabArrangement` /
    `FireLabArrangement` / `ResetLabArrangement` / `ReleaseLabArrangement` — кнопки.
  - `Packages/manifest.json` — `com.unity.localization` отсутствует; `com.unity.multiplayer.playmode`
    2.0.2 стоит.
  - `first-house.house.json:588` — `"name": "Front door"`.
  - Поиск `*.wav|*.mp3|*.ogg` по `Assets/TunnelCrew` — ноль файлов; Feel упоминается в трёх сценах,
    включая `IntegratedHouse.unity`.
  - Прочитано по CALL: план (13, 5 Р1, 6, 4, 9, 11.1, 3.1, 3.2, 1.7, 10, 12), правило
    `the-demo-plan-is-the-map-until-october-5`, восемь улик review с `route: shape`, шесть правил
    знания с `read_by: shape`, наряд стенда 02.09, записка волны 11, устав (линзы, ограничения,
    отношение к риску, репозитории).

state_changes: |
  1. ADD card `bet-g-5a7c-wave-12` (`bet`, `node: g-5a7c`, `opened: 2026-09-03`) с двенадцатью
     блоками: `owner_word`, `goal`, `done_when`, `appetite`, `tracks`, `cut_list`,
     `riskiest_assumption`, `kill_by`, `lenses`, `forecast`, `against`, `concept_basis`.
  2. ADD десять карточек задач (`task`, `_bet: g-5a7c`, `status: open`), у каждой блок `done_when`:
     `t-the-bench-shows-why-he-fell-1` и `t-he-slaps-the-mouse-he-sees-1` (полоса `тело`);
     `t-the-morning-ends-at-the-door-1` и `t-the-round-shows-what-it-cost-1` (`утро`);
     `t-the-magnet-has-two-poles-on-a-thing-1` (`мышь`);
     `t-four-screens-see-one-fall-1`, `t-every-word-comes-from-a-key-1`,
     `t-one-command-makes-a-named-build-1`, `t-four-people-connect-over-the-internet-1`,
     `t-six-events-make-a-sound-1` (`оболочка`).
  3. ADD четыре наряда в `work/` и четыре карточки `call` (`to: executor`, `status: ready`,
     `basis: 2d2c27ff`): стенд → WIN-U4, конец утра → WIN-U3, таблица строк → WIN-U2, магнит → WIN-U1.
  4. CLOSE `c-shape-g-5a7c-wave-12-001` (`done`) — нарезка исполнена.
  5. `NOW.md` не меняется: `bet: g-5a7c`, `track_wip_limit: 4` — четыре полосы, WIP занят целиком.
  6. Восемь улик review с `route: shape` остаются открытыми: их закрывает review 11.09 по факту, а
     не нарезка по прочтению.
  7. План не тронут: расхождений, требующих патча раздела 14, эта нога не нашла — три замера сверх
     плана уточняют цену строк, а не меняют решения.

captures:
  - Сборка одной командой оказалась построенной, и план об этом не знал: строка рубежа Р1 дешевле,
    чем считалось. Замечено, не превращено в правку плана — цену строк меняет review, а не нарезка.
  - Вида куска «мишень-хозяин» нет в схеме расстановок: `frozen-walker`, `launched-cargo`,
    `hovering-cargo` — все три про мышь и груз. Это первая работа стенда и точный ответ на пробел,
    который review назвал.
  - Сигнала «раунд кончился» наружу нет не только у утра: улика `i-a-run-has-no-end` говорит то же
    про заход с июля. Если конец утра и конец захода — один механизм, это станет видно первой ногой
    полосы `утро`, и тогда это вопрос формы, а не объёма.
  - `T_baseline` меняется в 3–4 раза этой волной, и это ровно тот случай, ради которого спека
    выводила порог из безразмерного характера. Кусок остаётся отложенным (второго хозяина нет), но
    цена ручной перенастройки названа.
  - Проигравшие подходы к концу утра, названные при выборе: (Б) кончать утро таймером — убивает ★ и
    цену времени, оплаченную волной 11; (В) экран по кнопке «сдаться» — не закрывает ни одной
    строки рубежа. Оба остаются запасными формами, если порог 1 сработает.

decisions_needed:
  - q: Три слова раздела 11.1 плана — путь полного падения по сети, Y Bot как запасное тело, слух
      хозяина через стены. Волну 12 не держат; нужны до Р2 (Y Bot) и до Р3 (падение).
    options: [ответить сейчас; ответить на сверке 11.09]
    recommendation: любое; Р1 они не держат.
  - q: Экспорт документа демо из Concept Lab.
    options: [придёт до 11.09 — квитанция вносится ногой work и строки содержания входят в остаток
      волны; не придёт — строка 2 рубежа переносится на Р2 без штрафа]
    recommendation: не блокирует; порядок уже записан в ставке.

play_check:
  - 1 recite: done — цель узла `g-5a7c` и родителя `g-0c26` пересказаны; цель волны — имя рубежа Р1,
    не деятельность.
  - 2 appetite: done — замах назван до разговора о решениях: десять задач, аппетит кончается 11.09
    его словом на план. Внешний взгляд назван и говорит против: потолок доходивших целиком — семь
    задач. На конфликте доверено ему, и лечение — порядок смерти, а не обещание.
  - 3 approaches: done — три структурно разных пути к концу утра названы до эскиза: (А) построить
    пятый глагол и конец раунда; (Б) подделать конец таймером; (В) сделать работу ненужной — экран
    по кнопке «сдаться». Выбран (А): (Б) убивает ★ и цену времени, оплаченную волной 11 — опоздать
    нельзя, если утро всегда кончается по таймеру; (В) не закрывает ни одной строки рубежа.
    Проигравшие подходы — в `captures`.
  - 4 scope hammer: done — семь вырезов, каждый с причиной и условием возврата; ни один не говорит
    «работы не осталось» без команды и результата (правило
    `a-cut-that-says-no-work-left-must-name-the-measurement`).
  - 5 lens sweep: done — семь линз устава, вердикт по каждой; линза 5 — `not_needed` с причиной;
    линза 4 отвечена «нет, квитанции нет» вслух.
  - 6 riskiest assumption: done — «конец утра это глагол, а не слой»; его задача идёт первой в своей
    полосе, и порог 1 требует остановиться, а не строить.
  - 7 tasks: done с названным отступлением — десять задач вместо 3–7 плея, потому что принятый им
    план делает волну равной рубежу и берёт его список целиком. Отступление названо здесь, а не
    молчаливо: сокращать список нарезка не вправе, поэтому размер лечится порядком смерти.
  - 8 kill criteria: done — семь порогов, каждый про остановку и вопрос домой; дата 11.09 — его
    слово на план, не изобретение ноги.
  - 9 close: done — экран показан целиком, слово получено дословно, ставка и задачи заведены,
    четыре первых наряда выданы, `next` их называет.
  - (owner) Owner-verdict guard: экран был показан и сессия остановилась до его слова; ставка
    заведена только после «все принимаю». Три слова 11.1 за него не решены и помечены ждущими.
    Ни одно решение о содержании игры этой ногой не принято.

log: двенадцатая волна нарезана рубежом Р1 «Скелет утра» к 11.09 его словом «все принимаю» — десять задач в четырёх полосах со списком рубежа построчно и видом улики на каждую строку; порядок смерти написан до работы, потому что волн с планом больше семи задач целиком не доходило ни одной; при нарезке перемерено три вещи сверх плана — сборка одной командой уже есть, давление темп не выбирает, а шов удара для стенда существует и ему не хватает ровно вида куска «мишень-хозяин»; решение о звуке и шесть кусков спеки давления произнесены вслух; четыре первых наряда выданы, стенд «хозяин» первой ногой и в WIN-U4

next: |
  Четыре наряда готовы к запуску, полосы разведены, WIP занят целиком:
  `c-exec-g-5a7c-the-bench-shows-why-he-fell-001` (WIN-U4, первый),
  `c-exec-g-5a7c-the-morning-ends-at-the-door-001` (WIN-U3),
  `c-exec-g-5a7c-every-word-comes-from-a-key-001` (WIN-U2),
  `c-exec-g-5a7c-the-magnet-has-two-poles-on-a-thing-001` (WIN-U1).
  Волна закрывается свежим чатом `review` в дату рубежа 11.09 — это и есть пятничная сверка; он же
  сразу открывает нарезку рубежа Р2, без отдельного вопроса владельцу.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-shape-g-5a7c-wave-12-001.md
