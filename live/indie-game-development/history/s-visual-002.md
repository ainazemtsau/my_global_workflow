RESULT s-visual-002 (call: none — plain owner message, KERNEL §2 OPEN)
direction: indie-game-development   play: research   node/task: g-7e15 (VISUAL parallel track)

outcome: |
  The procedural gas-visual ARCHITECTURE (structure-first) is researched + documented. The owner overrode the
  frozen 2026-06-21 "design-feel pass first" next → STRUCTURE-FIRST: build the parameter→picture ENGINE before the
  visual-design grammar (crude visuals OK; new gas type = DATA not code; many types; performant). THE STRUCTURE =
  a read-only, data-driven engine in 4 layers (engine → one-way seam[FAKE now / real later] → ONE shared body
  shader styled by a typeId-indexed param buffer → a separate event-driven warning telegraph) + a reserved bare
  special-gas hook. The owner's explicit shader question is answered: ONE shared shader configured by DATA (a new
  ordinary gas = author one GasTypeDefinition asset + register it, editor-validated; NO per-type shader, NO code).
  A de-risked 7-step build sequence (steps 0-6, each owner-visible on FAKE data) with step 0 = a min-spec perf
  spike FIRST (the #1 unknown). 3 owner decisions ratified.

evidence: |
  work/gas-visual-architecture-2026-06-26.md (the structure doc). Built by workflow wf_579651ba-7f9 (13 agents:
  5 architecture dimensions — data-driven authoring / many-types-perf / two-channels / special-gas-seam /
  reference-architectures — each adversarially verified by an independent skeptic; a draft synthesis; a solo-dev
  over-engineering critic; a final incorporating the critique; 5/5 dimensions verified). Builds on
  work/gas-visual-research-2026-06-21.md (deep-research wf_e5924329: the layered read-only architecture + the two
  render paths), carried forward not relitigated. Owner ratified 3 scope decisions in-session («1 да / 2 да /
  3 заложить место сейчас, строить пока только вид»).

state_changes: |
  NOW.md parallel_tracks g-7e15: the `next:` field REPLACED (was "DESIGN/FEEL pass FIRST … rec design first")
    → the structure-first outcome: architecture documented (work/gas-visual-architecture-2026-06-26.md), the
    4-layer + reserved-hook structure, the one-shared-shader-by-data shader answer, the 3 ratified decisions, the
    two-channel design, the de-risked build sequence (step 0 = min-spec perf spike first), the recommended next leg
    (build-step 0 as a GasCoopGame_dev executor leg, owner-gated), decision d-visual-buildstep0-001 open, the
    fake-data-now/real-integer-grid-later decoupling. Status stays `active` (parallel track). Root NOW.next
    (engine c-exec-014) and the g-9c41 bet UNTOUCHED (G1 — visual is a parallel track, adds no active_tasks).
  work/: + gas-visual-architecture-2026-06-26.md
  LOG.md: + the s-visual-002 line.

captures:
  - Engine-grounding drift caught + corrected: research agents borrowed a "verdict E / float clouds-far" framing for
    Layer 0; our authoritative engine is the integer LAYERED grid (g-9c41). Corrected in the doc; visual conclusions
    unaffected (the visual only needs a read-only host-authoritative snapshot). Watch for this in future visual legs.
  - The reserved special-gas seam may need WIDENING beyond pure read-only-derived motion if the flagship floor-creep /
    ghostly-hands gas must read AHEAD of the coarse sim front — resolved only by the post-v1 one-gas spike (named so
    it isn't a surprise).
  - The visual-design grammar the OTHER chat raised (Legend-3 neutral/toxic/reactive, baseline concept, proximity
    warning layer) is deferred to a later DESIGN pass; the useful kernels were folded structurally (param contract /
    test fixtures / two-channel) and the rest parked.

decisions_needed:
  - q: |
      d-visual-buildstep0-001 — proceed to build-step 0 (the min-spec perf spike, on FAKE data, in a fresh
      GasCoopGame_dev session) as the first visual build leg, or react to / adjust the structure first?
    options: [proceed to build-step 0, adjust the structure first]
    recommendation: |
      proceed to build-step 0, because min-spec perf (does a grid-fed raymarch fit frame budget + does the iGPU
      support the optional compute/VFX path) is the single load-bearing unknown and must be measured BEFORE any
      authoring — but it is OWNER-GATED (he reviews the structure; the real min-spec target / quality-tier knob is
      confirmed when the CALL is authored, since his dev rig is optimistic). If he wants to spar on the structure
      first, that is a fresh session.

play_check:
  - 1 recite: done — restated the question (the procedural-viz ENGINE architecture, structure-first), the return
    format (structure + de-risked build sequence), and confirmed scope via the owner's 3 ratified decisions before
    spending.
  - 2 investigate: done — workflow wf_579651ba-7f9 (web/expertise-grounded, 5 dimensions, adversarial verify per
    dimension), within budget (one workflow, ~13 agents).
  - 3 confidence: done — established (read-only layered structure + one-uber-shader-by-data + two-channel +
    reserved-hook, solo-dev-sized) separated from the load-bearing UNVERIFIED item (min-spec perf), with what would
    change it (build-step 0 measurement on real hardware). Doc §9.
  - 4 close: done (owner present) — readable Russian brief + this RESULT; findings returned; next = awaiting_decision.
    Owner steered structure-first («1 да / 2 да / 3 заложить место сейчас, строить пока только вид»).

log: research (g-7e15, s-visual-002): procedural gas-visual ARCHITECTURE documented (structure-first, owner override) — 4-layer read-only data-driven engine + one-shared-shader-by-data + two channels + reserved special-gas hook + a de-risked build sequence (step 0 = min-spec perf spike first); next = awaiting d-visual-buildstep0-001.

next: |
  awaiting_decision (d-visual-buildstep0-001). On «proceed» → a fresh session authors the build-step-0 executor CALL
  (min-spec perf spike, FAKE data, GasCoopGame_dev, PLAN; confirm the min-spec target / quality-tier knob) per
  work/gas-visual-architecture-2026-06-26.md §7 step 0. On «adjust» → a fresh session reworks the structure with the
  owner before any build.

END_OF_FILE: live/indie-game-development/history/s-visual-002.md
