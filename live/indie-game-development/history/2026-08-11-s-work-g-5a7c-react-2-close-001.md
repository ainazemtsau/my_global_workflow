# RESULT — s-work-g-5a7c-react-2-close-001

play: work · close: **light — because** все три строки `done_when` наряда и все шесть строк карточки
`t-react-2` перемерены ПЕРВОЙ РУКОЙ по закоммиченным байтам опубликованной головы: набор тестов
прогнан здесь на выгруженном дереве публикации, состав коммитов, границы, арбитр, вес и пять строк
каталога прочитаны командами, прогнанными здесь. Ни одной строки, которую судит его глаз, его
ощущение или чужой прогон, в этой задаче нет — его вердикт по дорожке назначен на `t-react-3`
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-2 ·
call: c-exec-g-5a7c-react-2-002
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `daf2fdf1`, дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге после срабатывания опроса: `origin/dev` = `origin/main` =
`2c8dc0c79ce2100a9e9dbb3d64f69729f1d4a313`. Слоты по файлу состояния: **все четыре
`AVAILABLE / lease none`** — WIN-U2 вернулся, WIN-U1 тоже освободился.

## outcome

`t-react-2` закрыта. **У хозяина есть ТОЧКА РАСШИРЕНИЯ: обработчиков много, новый добавляется не
трогая ядро, арбитр назван одной вещью, вес строки вычисляемый, и обработчик умеет переключить
ветку поведения.**

**Главный гейт взят самым сильным из возможных способов — не диффом, а СОСТАВОМ КОММИТОВ.** Наряд
требовал, чтобы два обработчика въехали без правки ядра и друг друга. Коммит первого обработчика
трогает РОВНО ДВА файла (`HouseholderFactCombinationDecisionHandler.cs` + `.meta`), коммит второго —
тоже ровно два (`HouseholderAccumulatedFactDecisionHandler.cs` + `.meta`). Ни один не касается ядра,
решателя, перечислений или другого обработчика. Второй физически не мог тронуть первый.

## evidence — перемерено и прогнано первой рукой на `2c8dc0c7`

### Опубликовано ровно то, что было проверено

История `a5a84350..2c8dc0c7` — четырнадцать коммитов: четыре продуктовых/тестовых (`69bf03ac`,
`1561188d`, `17c563ee`, `f3314056`), четыре отката, четыре реаплая и два документных. **Дифф КОДА от
проверенного кандидата `1bbddf67` до опубликованного пуст** (`git diff --stat 1bbddf67..2c8dc0c7 --
Assets tests core ProjectSettings Packages tools validation.config` — ничего). Весь дифф
кандидат→публикация — **один добавленный документ**. Опубликованы ровно проверенные байты.

### Прогон сделан здесь

```text
dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release
Пройдено 184, не пройдено 0, пропущено 0, всего 184
```

На выгруженном `git archive 2c8dc0c7` дереве. Свежая база `a5a84350` даёт `179/179` — тоже прогнано
этой ногой. Разница ровно `+5`.

### `done_when` 1 — множественность, дефолт, арбитр, вычисляемый вес

`HouseholderDecisionPipeline` композитит любое число независимо написанных обработчиков; когда не
ответил никто — зовётся прежняя политика, то есть дефолт остаётся единственным запасным вариантом и
не конкурирует.

**Арбитр — одна названная вещь в одном месте:** `HouseholderDecisionArbitration.RuleName =
"HighestWeightThenHandlerName"` (`HouseholderDecisionPipeline.cs:21`), единственная точка сравнения
`PrefersCandidate` (`:107`). Порядок файлов и порядок регистрации поведения не задают.

**ВЕС ВЫЧИСЛЯЕМЫЙ, а не константа** — требование владельца исполнено буквально:
`HouseholderReactionWeightEvaluator` есть делегат (`HouseholderReaction.cs:27`), а `Weight` —
свойство определения строки (`:64`), прочитано на опубликованной голове. Дверь к числовой оценке
осталась открытой ДОБАВЛЕНИЕМ, а не переписыванием каждой строки.

**Цена нового поля строки упала.** Пять производственных строк переведены на
`HouseholderReactionDefinition` с ИМЕНОВАННЫМИ инициализаторами; было семь позиционных мест сборки.
Имена всех пяти реакций целы — `Name = "` встречается в каталоге ровно ПЯТЬ раз.

### `done_when` 2 — ветка поведения

Предложение может назвать ветку; принятая ветка — именованная одноразовая лента шагов, меняющая
продолжающееся занятие, а не только мгновенный ответ, с возвратом в прежнюю жизнь. Правка ядра
обошлась в **три точки** — ровно те, что наряд назвал заранее: имя ветки в предложении, его
потребление в `Step`, переключение ленты в `AuthoritativeHouseholder`. Следующая ветка четвёртой
точки не добавляет.

### `done_when` 3 — дефолт, границы, откат

**Границы целы, доказано ПУСТЫМ выводом на опубликованной голове.** `git diff --name-only
a5a84350..2c8dc0c7` по `Core/Cargo/**`, `Network/**`, `Scenes/**`, `ProjectSettings/**`,
`Assets/GasCoopGame/**`, `Core/Multiplayer/CarryWorld.cs`, `StreamingAssets/**`, `Settings/**` —
**пусто**. Отсюда: пороги `60 / 150 / 400` целы, обе публичные сигнатуры `CarryWorld.Advance` целы,
20-строчный файл профиля цел, сцена цела.

**ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК, второй раз подряд.**
`git rev-parse 689646ae^{tree}` = `626b04880517f1054bc12716597d299064a52f37` = дерево свежей базы
`a5a84350`; восстановленный кандидат `1bbddf67^{tree}` = `97bdd674…`.

### Перебазирование — новое правило наряда сработало

Наряд впервые заменил блокирующий СТОП на перебазирование, потому что базис протухал дважды подряд.
Голова сдвинулась с `5fe0dadb` на `a5a84350` до старта; исполнитель встал на свежую голову ДО первой
продуктовой правки, получил на ней `179/179` и назвал её отдельной строкой возврата. Клауза 5
`evaluator` исполнена по существу, а не формально.

### Разрез — по разрешённой линии и назван

Срезан авторинг обработчиков и веток ДАННЫМИ. В задаче осталось всё, что наряд объявил неснимаемым.
Обратный разрез не сделан. Срезанное заведено задачей `t-react-data-2` ещё на checkpoint-ноге, чтобы
не потеряться.

## Что НЕ проверено

Unity-редактор код не собирал — наряд прямо ограничил доказательство engine-free ядром и
headless-тестами. Названо, а не замолчано. Утверждения о независимом ревью и двух найденных им краях
как ПРОЦЕСС из байтов не проверяются; их РЕЗУЛЬТАТ проверяется тестами, и тесты зелёные.

## state_changes

1. `t-react-2`: закрыть, терминальный статус `done`. Светлым путём — названо вслух по букве
   `evaluator` клауза 2.
2. `c-exec-g-5a7c-react-2-002`: закрыть — исполнен и опубликован в `2c8dc0c7`, слот вернулся.
3. `NOW.md`, `g-5a7c`, текст ставки и дорожка `cargo-hands` не тронуты. Дорожка `householder`
   освободилась: следующий корень — `t-react-data-2` (order 8).

## captures

- **Все четыре слота свободны** — WIN-U1 тоже освободился, значит дорожка `cargo-hands` между
  задачами.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.
- Карточка вопроса про два непроверенных утверждения возврата `t-react-mem` остаётся открытой.

## decisions_needed

Нет. Владелец отошёл, поручив закрыть работу и составить следующий наряд без него.

## play_check

- 1 recite: done — шесть строк карточки и три строки наряда сведены
- 2 owner inputs (owner): done — его вердикт по дорожке назначен на `t-react-3`
- 3 do the work: done — опубликованное перемерено первой рукой, прогон сделан здесь
- 4 self-check: done — каждая строка закрыта своим измерением; непроверяемое названо
- 5 close: done — светлый путь; продолжение дорожки — `t-react-data-2`

## Гейты

- **G1.** Дорожка `householder` освободилась; лимит дорожек цел.
- **G3.** Аппетит не тронут: срез сделан по заранее названной линии, а не молча.
- **G5.** Светлый путь: каждая строка перемерена первой рукой. Свежий чат не открывался, названо.
- **G7.** Ничего продуктового не решено.
- **G9.** Карточки задачи и ставки не редактировались.
- **evaluator, клауза 1.** Закрытие ПОСЛЕ публикации.
- **evaluator, клауза 5.** Собрано и опубликовано с текущей головы другой дорожки `a5a84350`.

## log

`t-react-2` закрыта светлым путём на опубликованной голове 2c8dc0c7: у хозяина появилась ТОЧКА РАСШИРЕНИЯ, и главный гейт взят самым сильным способом — не диффом, а составом коммитов: коммит первого обработчика трогает РОВНО ДВА файла, второго тоже ровно два, ни один не касается ядра, решателя, перечислений или другого обработчика; арбитр стал одной названной вещью HighestWeightThenHandlerName в одном месте, вес строки стал ДЕЛЕГАТОМ, а не константой, то есть дверь к числовой оценке осталась открытой добавлением, а пять производственных строк переехали на именованные инициализаторы, отчего новое поле перестало стоить семи позиционных правок при целых именах всех пяти реакций; ветка поведения обошлась ровно в три точки правки ядра, названные нарядом заранее; 184/184 прогнаны этой ногой на выгруженном дереве публикации против 179/179 на свежей базе; дифф КОДА от проверенного кандидата до опубликованного ПУСТ, сверху лёг один документ; границы пусты по восьми запрещённым поверхностям; ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК второй раз подряд; новое правило про перебазирование вместо блокирующего СТОПа сработало как задумано — голова сдвинулась до старта, исполнитель встал на свежую и назвал её; срез сделан по разрешённой линии и заведён задачей заранее

## next

1. **Следующий корень дорожки — `t-react-data-2`** (order 8): обработчики и ветки описываются файлом
   данных, а не только классом. Это его главное требование, срезанное по разрешённой линии.
2. `t-react-3` (order 9) — его вердикт по дорожке, в самом конце.
3. Ждут его слова, без вопроса: порог смерти (5), текст `## approach` ставки, два утверждения
   возврата `t-react-mem`.
