# RESULT — s-work-g-1d84-named-house-routes-close-001

direction: indie-game-development
track: дом
play: work
node/task: g-1d84/t-host-2
date: 2026-08-06
close: light — because строки 1 и 2 `done_when` перемерены этой ногой по байтам опубликованного
`main` и подтверждены собственным прогоном обоих продуктовых ворот, а строка 3 стоит на уже
закоммиченных дословных словах владельца плюс его сегодняшнем прямом слове Направлению.
base_read: Direction `fa6fe21c`, ветка `wt/indie-game-development`, дерево чистое; продуктовый
`main` = `dev` = `origin/main` = `origin/dev` = `f03b1468`, дерево чистое (равенство прочитано
ответом сервера `git ls-remote`).

## outcome

`t-host-2` ЗАКРЫТА. Хозяин перестал ходить сквозь перегородки: его день теперь назван местами дома,
путь между названными местами считается в слое правил по названным проходам того же дома, а после
реакции он не прыгает назад и не доигрывает старый отрезок, а строит свежий путь от того места, где
его отпустила реакция.

Вместе с задачей ПОЧИНКОЙ закрыта запись `i-host-walks-through-walls-001` — ровно так, как её
`review_when` и требовал: не забвением, а исправлением.

ЧТО НЕ ЗАКРЫЛОСЬ И ЗАПИСАНО ОТДЕЛЬНО: привязка словаря дома к текстовому профилю, которую `t-host-3`
отложила именно сюда, НЕ сделана — маршрут настраивается компонентом в инспекторе. Заведена запись
`i-profile-cannot-name-a-house-place-001`. Смысл рычага «где хозяин» в панели стартовых условий
изменился — заведена `i-start-conditions-householder-lever-changed-meaning-001`.

## evidence

Всё ниже прочитано/прогнано ЭТОЙ ногой первой рукой на опубликованном `main` = `f03b1468`, а не
принято со слов продуктового отчёта.

**Строка 1 — путь считается в слое правил по названным местам и проходам; в стены не идёт.**

- `Assets/TunnelCrew/Core/House/HouseWalk.cs` (новый, 460 строк) лежит в `TunnelCrew.Core`, чей
  `TunnelCrew.Core.asmdef` на `f03b1468` по-прежнему `"references": []` и
  `"noEngineReferences": true` — движка в счёте пути нет по построению, а не по заявлению.
- Проводка, которую `t-house-4` СОЗНАТЕЛЬНО оставила ненаписанной, написана:
  `Network/NetworkHouseholder.cs:90-106` — `Read()` объявлен «host simulation edge» и один раз зовёт
  `_housePlanSource.Read()` перед тем, как что-либо двинется. Это тот самый потребитель
  `IHousePlanSource`, которого в работающей игре не было.
- День хозяина стал ИМЕНАМИ: `World/HouseholderRouteController.cs:30,34` — `string[] routePlaces` и
  `string listeningPlace`; в `World/Householder.prefab` записаны `Bedside`, `Work Table`,
  `Store Corner`, `West Window` и `listeningPlace: Hall Centre`.
- Второй источник карты УДАЛЁН, а не оставлен рядом: в `Householder.prefab` на базе `d647a58b` было
  4 вхождения `Route Point`, на `f03b1468` вхождений `Route Point`/`Listening Point` — 0.
- Проходы реально участвуют в счёте: `HouseWalk.cs:291-314` (ближайший проход для цели вне комнат) и
  `:374` `house.TryFindPassageBetween(from.Name, to.Name, out var passage)`.

**Строка 2 — не телепортируется, восстанавливает намерение свежим путём, новое событие снова
отвлекает.**

- Снап, который двигал его рывком: на базе `d647a58b:Core/Householder/Householder.cs:1233` стоит
  `position = activeResponse.Target.Position;`. На `f03b1468` этого присваивания НЕТ — осталось
  только чтение `var responseTarget = activeResponse.Target.Position;` (:1425).

**Чем держатся обе строки — прогоны, сделанные МНОЙ на опубликованном `main`, а не пересказанные:**

- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` →
  `не пройдено 0, пройдено 110, пропущено 0, всего 110`.
- `.\pwsh.cmd tools/check.ps1` → `hygiene OK` / `OK: all active gates green`.

**САМОЕ ВАЖНОЕ В ЭТИХ ТЕСТАХ — ЧТО ОНИ ХОДЯТ ПО НАСТОЯЩЕМУ ДОМУ.** Фикстура `ThreeRoomDraft`
(`tests/TunnelCrew.Core.Tests/TestHouse.cs`) написана руками и сама честно об этом предупреждает,
поэтому её числа сверены с закоммиченным `Assets/TunnelCrew/World/ThreeRoomHouse.prefab`:

- комнаты — центры `−4.875` / `0` / `4.875` при `_size` `4.25×14` / `4.5×14` / `4.25×14`, то есть
  x `−7…−2.75`, `−2.25…2.25`, `2.75…7` и z `−7…7`: совпало с фикстурой знак в знак;
- семь мест в мировых координатах (позиции в префабе локальные, пересчитаны через центры комнат):
  `Bedside` (−3.5; −2.5), `West Window` (−3.5; 2.5), `West Door Side` (−3.5; 0), `Hall Centre`
  (0; 0), `Work Table` (3.5; −2.5), `Store Corner` (5.11; 5.58), `East Door Side` (3.5; 0) — совпало;
- два прохода `West Doorway` (−2.5; 0) и `East Doorway` (2.5; 0), оба `_width: 2` — совпало.

Сами тесты (`HouseholderWalksTheHouseTests.cs`, 8 новых) утверждают ровно те две строки:
`WalkingHisWholeDayNeverCrossesAPartition` меряет пересечение НА ШАГЕ, а не на точке прибытия;
`WalkingHisWholeDayGoesThroughTheOpenings` не даёт первому тесту позеленеть у хозяина, запертого в
одной комнате; `AfterAnsweringANoiseHeTakesHisDayBackUpByAFreshWayFromWhereHeStands` разом проверяет
три отдельно ломающиеся вещи — прыжка нет ни на одном тике, ни одна половина не идёт сквозь
перегородку, и путь назад построен от места, где его отпустила реакция;
`ASecondNoiseDuringTheWayBackDistractsHimAgain` не даёт возврату стать состоянием без выхода.

**Строка 3 — владелец видит переход через проём и возврат к намерению.**

Закрыта ТОЛЬКО его словами, и это записано без смягчения: Направление игру не запускало и запустить
не могло.

- Закоммичено: `f03b1468:docs/results/c-exec-g-1d84-named-house-routes-001.md:12,14` — «да ходит» и
  «давай закрывать задачу я проверил все работает».
- Сегодня сказано Направлению прямо: «задача c-exec-g-1d84-named-house-routes-001 закрыта, закрывай
  у нас».
- ГРАНИЦА ЭТИХ СЛОВ, честно: «да ходит» — прямо про хождение; возврат к намерению, вторая копия и
  панель стартовых условий входят в общее «я проверил все работает» по списку приёмки отчёта
  (пункты 4–6), перечислением он их не подтверждал. Требовать от него отчёта по пунктам запрещено
  прямо (`knowledge/no-checks-the-owner-makes-by-eye.md`), поэтому строка закрыта его вердиктом, а
  не его повторным прогоном.

**Границы наряда соблюдены — проверено, а не предположено.**

- `ThreeRoomHouse.prefab` этой доставкой НЕ ТРОНУТ: `git diff d647a58b f03b1468 -- <префаб>` пуст.
  Ни одной комнаты, места или прохода не добавлено и не переименовано.
- Второго сетевого канала и нового вида посылки нет: `Network/WalkerSnapshot.cs`,
  `Network/HouseholderSnapshot.cs` и `Network/NetworkWalkerCourier.cs` в диффе доставки отсутствуют
  вовсе.
- Вырезанный объём не вернулся: поиск по всему диффу доставки на `chase|pursu|vision|sight|search|
  combat|погоня` — ноль вхождений.
- Слот освобождён по-настоящему: `gascoop-slot-state.v1.json` — все четыре слота `AVAILABLE`,
  `lease: none`.

**Два дефекта в тексте самого наряда, оба измерены и оба безвредны по исходу:**

1. `context` называл `West Doorway` и `East Doorway` «именованными дверными местами». По байтам
   префаба это ПРОХОДЫ (`HousePassageMarker`), а дверные МЕСТА — `West Door Side` и `East Door Side`.
   Исполнитель взял имена из данных дома и своих не выдумал.
2. `base` наряда объявлен как `d647a58b8b64b7d516b583cfde38691109909139`; такого объекта в
   репозитории нет (`git cat-file -t` → `fatal: could not get object info`, exit 128). Настоящий
   коммит — `d647a58b5f39c242e197f6b4d923a348752131fe`: короткий sha был дополнен до сорока знаков.

## state_changes

1. `NOW.md` header `updated:` → `2026-08-06 by s-work-g-1d84-named-house-routes-close-001`.
2. `NOW.tasks/t-host-2`: `status: open` → `done`; добавить поле `closed:` с построчным разбором выше.
   `unblocked` и `note` сохранить.
3. `NOW.tracks/дом`: добавить `completed:` — все три задачи полосы закрыты, корня в полосе нет;
   полоса НЕ распускается (KERNEL §3: полосы распускает только `review`).
4. `NOW.tasks/t-host-8.unblock_when`: записать, что половина условия выполнена — `t-host-2` закрыта
   2026-08-06, задача ждёт только `t-host-7`.
5. `NOW.open_calls`: удалить запись `c-exec-g-1d84-named-house-routes-001` (корень закрыт). Две
   оставшиеся записи не трогать.
6. `NOW.issues/i-host-walks-through-walls-001`: добавить `closed:` — закрыта ПОЧИНКОЙ вместе с
   `t-host-2`, с уликами.
7. `NOW.issues`: добавить `i-profile-cannot-name-a-house-place-001` (крючок `IsKnownPlace` всё ещё
   `return false`; любой профиль, называющий место, отвергается при старте).
8. `NOW.issues`: добавить `i-start-conditions-householder-lever-changed-meaning-001` (рычаг «где
   хозяин» задаёт теперь только точку старта, маршрут принадлежит дому).
9. `NOW.issues/i-call-named-path-absent-on-declared-basis-004`: добавить два измеренных случая из
   текста этого наряда.
10. `NOW.issues/i-published-to-main-before-any-direction-close-001`: записать восьмой случай.
11. `NOW.issues/i-unity-evidence-rests-on-owner-launch-001`: записать, что эта нога впервые за три
    ноги дала агентское доказательство со стороны Unity, и две измеренные ловушки инструмента.
12. `NOW.issues/i-two-calls-recorded-blocked-are-actually-running-001`: одна измеренная строка — оба
    корня доставлены в опубликованный `main`.
13. `LOG.md`: одна строка перед указателем `archived:`; `history/`: этот файл.

Не менять: TREE/CHARTER, ставку, аппетит, полосы и WIP, `decisions`, прогноз, продуктовый
репозиторий, статусы `t-house-6` и `t-host-7` и их `open_calls`.

## captures

- `Assets/TunnelCrew/Scenes/HostWalksHisDay.unity` — мёртвый стенд закрытой волны — теперь выключит
  себя с сообщением в консоль, если его открыть и запустить: его `HouseholderRouteController` остался
  без имён. Удаление сцены — отдельное решение.
- Ширина прохода (`HousePassage.Width`) по-прежнему никем не читается: путь идёт через точку проёма,
  проверки «пролезет ли несомое» нет и наряд её не покупал.
- Две ловушки Unity MCP, измеренные исполнителем: `assets-refresh` с `ForceSynchronousImport` вешает
  плагин целиком (импорт при этом доходит), и сцена, загруженная ДО компиляции нового поля, читает
  это поле как null, хотя на диске оно записано — лечится перезагрузкой сцены после компиляции.

## decisions_needed

Нет.

## play_check

- **1 recite** — done: три строки `done_when` `t-host-2` и обязательство активной ставки `g-1d84`
  сверены с `NOW.md` до любой работы.
- **2 owner inputs (owner)** — done: нужен был ровно один вход, который может дать только он —
  видел ли он это своими глазами. Он дал его дважды: закоммиченными словами в продуктовом отчёте и
  сегодняшним прямым «закрыта, закрывай у нас». Новых вопросов к нему нет.
- **3 do the work** — done: продуктовая нога уже вернулась; работа этой ноги — проверка возврата,
  а не новая реализация.
- **4 self-check** — done: каждая строка `done_when` разобрана отдельно, с указанием, чем именно она
  закрыта и чем НЕ закрыта.
- **5 close** — done: `close: light` по тесту `os/plays/work.md` §Light close. Полоса `дом`
  продолжения не получает — задач в ней больше нет; ставка не последняя, `review` не открывается.
- **G5** — pass по светлому маршруту: строки 1–2 перемерены первой рукой по байтам и двумя
  собственными прогонами ворот, строка 3 — уже закоммиченные дословные слова владельца об этом самом
  артефакте, процитированные там, где они лежат. Отдельный биндинговый чат не открывался, и это
  записано как выбранный маршрут, а не как экономия.
- **G2/G3** — pass: только задача активной ставки; аппетит не трогался, вырезанный объём не вернулся
  (проверено поиском по диффу доставки).
- **G7** — pass: ни одного игрового решения нога не принимала; всё, что требовало суждения владельца,
  либо процитировано, либо оставлено открытым записью.
- **G10** — pass: TREE/CHARTER не тронуты; `live/**` меняется только через `state_changes`.

## log

g-1d84/t-host-2: ЗАКРЫТА, `close: light`, на опубликованном `main` = `dev` = `f03b1468`. Хозяин
больше не ходит сквозь перегородки. Строки 1–2 перемерены первой рукой: `HouseWalk.cs` лежит в
engine-free `TunnelCrew.Core`, `NetworkHouseholder.Read()` наконец зовёт `IHousePlanSource.Read()` —
проводка, которую `t-house-4` оставила ненаписанной; день стал именами мест, четыре трансформа
`Route Point` из префаба исчезли (второго источника карты нет); снап `position =
activeResponse.Target.Position` с базы удалён. Прогоны сделаны Направлением, а не пересказаны:
`dotnet test` 110/110, `check.ps1` `OK: all active gates green`; фикстура тестов сверена с
закоммиченным `ThreeRoomHouse.prefab` — три комнаты, семь мест и два прохода совпали знак в знак,
то есть тесты ходят по настоящему дому. Границы соблюдены: префаб дома не тронут, `HouseholderSnapshot`
и курьер в диффе отсутствуют, вырезанный объём не вернулся. Строка 3 закрыта ТОЛЬКО его словами
(«да ходит», «давай закрывать задачу я проверил все работает» в `f03b1468:docs/results/...:12,14`
плюс сегодняшнее «закрыта, закрывай у нас»), и записана граница этих слов. ПОЧИНКОЙ закрыта
`i-host-walks-through-walls-001`. НЕ закрылось вместе с задачей и заведено записями: крючок профиля
`IsKnownPlace` по-прежнему `return false`, то есть привязка словаря дома к тексту профиля, отложенная
сюда `t-host-3`, не сделана; и рычаг «где хозяин» панели стартовых условий сменил смысл. Полоса `дом`
осталась без задач и не распущена. -> history/2026-08-06-s-work-g-1d84-named-house-routes-close-001.md

## next

return-to-owner. Полоса `дом` пуста, новых корней эта нога не выпускает — управление волной владелец
ведёт в другом чате его словами «менеджментом волны я занимаюсь в другом чате».

Фронтир, названный без присвоения: два продуктовых RESULT доставлены в `main` и ждут КАЖДЫЙ своей
отдельной свежей Direction-ноги — `c-exec-g-1d84-delivery-point-001` за `t-house-6` и
`c-exec-g-1d84-world-noise-contact-001` за `t-host-7`. Пока они не закрыты, `t-host-8` остаётся
`blocked` (её условие теперь выполнено наполовину), а `t-scene-4` — финальная.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-named-house-routes-close-001.md
