# RESULT — s-repair-g-5a7c-householder-recut-001

play: repair · его слова 2026-08-11 сняли потолок задач, сняли «хендлер/event» как требование к
реализации и добавили ГЛАВНОЕ требование — поведение задаётся данными, а не кодом; подписанная
дорожка этого не знала, и примирить молча нельзя
direction: indie-game-development · node: g-5a7c · track: householder
date: 2026-08-11

base_read: OS — `wt/indie-game-development`, дерево чисто, голова `82faebd8`. Продукт: `origin/dev`
= `origin/main` = `2f7f2e87`, не двигался. Слот WIN-U2 остановлен владельцем до единой строки кода,
аренда освобождена. Замер шороха снят ЕГО рукой в живой игре: **7 стимулов `QuietRustle`/с, сила
0–125.714874 Н**.

## 1. Противоречия — по одной строке каждое

1. **Потолок восемь задач.** Ставка резала им; он: «это ограничение искусственное, дальше мы можем
   убрать вообще его». Единственный вырез, сделанный РАДИ потолка, — политика прерывания.
2. **«Хендлер/event» как механизм.** Записано мной как проверяемое требование; он: «это не значит,
   что должна быть такая реализация, я не знаю, что лучше… я привожу примеры, как делал в
   enterprise на Java».
3. **Поведение задаётся кодом.** Его главное требование: «мы программно не задаём никакого
   хозяина», поведение и комбинации — в JSON, который правит агент по его диктовке.
4. **Факт не несёт метаданных.** Он: «не просто сохраняем, что услышал, ещё метаинформацию — с
   какой комнаты, во сколько времени». Места у факта нет вовсе.

## 2. Реконструкция — что перемерено, включая две МОИ ошибки

Прогнано 6 агентов в три угла (интересность, механизм, нарезка без потолка), каждый вывод отдан
скептику на опровержение. Ключевые числа я перепроверил своей рукой.

**ОШИБКА ПЕРВАЯ, моя, в моей же улике.** `i-householder-core-has-no-memory-carrier-001` писала про
форму памяти «семь мест плюс тесты» и НА ЭТОМ ОСНОВАНИИ форма памяти забрала слот у политики
прерывания. Перемерено `git grep`: все **двенадцать** конструкций памяти в тестах идут через
одноаргументный посевной конструктор `new HouseholderLocalMemory(point)` — **тестов среди семи мест
НОЛЬ**, все семь в одном файле. Основание выреза было завышено. Улика исправлена.

**ОШИБКА ВТОРАЯ, тоже моя.** Дорого не то, что я назвал: `.NoticedFact` как свойство читается в
**75 строках тестов** против **12 строк продакшена** (перемерено обеими командами). Замена одного
факта на набор переписала бы 75 ожиданий — класс дефекта, который эта же волна запретила гейтом
`t-react-1`. Добавление набора РЯДОМ с производным свойством ломает ноль. Записано в карточку
буквально.

**МЕХАНИЗМ ОТКЛОНЁН АРИФМЕТИКОЙ.** «Перечисленные строки-сочетания отдельным видом» имеют цену
добавления `2^(N−1)`: видов события сегодня ЧЕТЫРЕ (`Householder.cs:62-69`) → 11 нетривиальных
подмножеств; с обещанными им зрением, ожогом, доставкой и чайником `N=8` → **247**. Постоянная цена
добавления и есть смысл правила «ядро только расширяется». Замена — **набор ИМЕНОВАННЫХ УСЛОВИЙ** в
строке реакции, где сочетание частный случай; и это же единственная форма, которую можно авторить
ДАННЫМИ, чего он потребовал отдельно.

**ЕГО ДОПУЩЕНИЕ ПРО JSON ОПРОВЕРГНУТО ЧТЕНИЕМ.** Он думал, «вроде уже сделано». Профиль — **20
строк и только распорядок из двух шагов**; реакций нет, комбинаций нет; разборщик знает ДВА умения.
Но две заготовки под требуемое реальны: поле `"when": "always"` в каждом шаге и намеренно пустой
`IsKnownPlace` с комментарием, что его подключит будущая задача.

**ЧТО НАРЕЗКА НЕ ПОКУПАЕТ, и это названо, а не спрятано.** Непредсказуемости случайностью не
появляется нигде: `Random` в непроверочном коде отсутствует, поле приоритета нигде не СРАВНИВАЕТСЯ,
выбор — первое совпадение линейным циклом. Покупается только зависимость от состояния. **При этом
случайность у хозяина ДЁШЕВА и это перемерено:** шаг мира крутится только на судье
(`NetworkWalkerCourier.cs:154-169`), по сети уезжают четыре поля (`HouseholderSnapshot.cs:24-30`) —
общего зерна не требуется. Он запретил машинное обучение; случайность не запрещал. Факт для его
решения.

**ГЕЙТ ЧИТАЕМОСТИ БЫЛ НЕВЕРЕН.** «Поменьше смен занятия» берётся идеально самым скучным исходом —
залипшим хозяином. Метрика тянула ровно против его «интересного». Заменена: правило, решившее
исход, называется тестом; разные наборы поводов дают разные исходы; число смен — факт, не ворота.

**НАЗВАНО ДО РАБОТЫ:** при 7 шорохах/с и сроке 6 с факт шороха присутствует почти всё время
волочения, поэтому после открытия приёма ЛЮБОЙ второй повод автоматически станет сочетанием.
Это новая предсказуемость, создаваемая самой работой; лечится решением про однородные факты, а
пороги трогать запрещено уликой и границей дорожек. Решение владельца, вынесено ему.

**НАЗВАНО ДО РАБОТЫ:** игрок сегодня достаёт РОВНО ДВА вида ситуаций — шорох и посадку груза;
«скрип доски» и «повторный грохот» порождаются только кнопкой редактора. Значит «комбинацию он
производит сам» выполнима одной парой, в коробке без обстановки.

## 3. Исправленное состояние

1. **`knowledge/core-is-extended-never-rewritten.md` дополнена** его словами 2026-08-11 целиком:
   механизм НЕ назначен (хендлер/event — его примеры из Java-опыта); требование — свойство
   результата, включая «интересный и НЕПРЕДСКАЗУЕМЫЙ»; поведение задаётся ДАННЫМИ; факт несёт
   метаданные и поля расширяемы; сочетание — частный случай условий, не перечисление; потолок снят.
   Плюс перемеренное состояние профиля и две найденные заготовки.
2. **`i-householder-core-has-no-memory-carrier-001` исправлена**: завышенная цена формы памяти,
   настоящее дорогое место (75 против 12), отклонение механизма арифметикой, и три названных факта
   (десять однослотовых мест трубы; палитра из двух видов ситуаций; дешёвая случайность).
3. **Заведена `t-react-mem`** (order 5): форма памяти принимает поля дёшево, факт несёт МЕСТО рядом
   с уже имеющимся временем, своя политика впервые запускается в игре. Поведение не меняется ни на
   кадр. **Нейтральна к спору о механизме — поэтому выдаётся, не дожидаясь его подтверждения.**
4. **Заведена `t-react-data`** (order 6): все пять реакций и условия их срабатывания переезжают в
   JSON; шестая реакция и новое условие добавляются правкой файла без строки C#. **Это его главное
   требование, а не вырез** — предыдущая нарезка вырезала его, вырез отменён. Названа разрешённая
   линия разреза и запрещён обратный.
5. **`t-react-2` переписана** (order 5→7): несколько фактов, набор РЯДОМ с производным `NoticedFact`,
   вся труба из десяти мест, правила выбора и прерывания — данными, гейт читаемости заменён. Три
   существующих теста, ассертящих снимаемую механику, названы поимённо. Строка про замер шороха
   снята — замер исполнен владельцем.
6. **`t-react-3` переписана** (order 7→9): сочетание условиями, «сочетание бьёт одиночку» правилом,
   палитра из двух видов названа до работы.
7. **`t-react-2` и `t-react-3` ЖДУТ ЕГО ПОДТВЕРЖДЕНИЯ**: обе несут переписанные подписанные строки.
   Ни один наряд по ним не выдаётся до его слова. Это записано в самих карточках.
8. Текст ставки, карточка узла, `CHARTER.md`, `NOW.md` и дорожка `cargo-hands` НЕ тронуты.

## captures

- **Порог смерти (5) волны пробит**: ног потрачено больше двенадцати. Его лекарство — «вторая
  дорожка на паузу», то есть смерть ровно той дорожки, ядро которой он потребовал к концу волны.
  Снятый потолок ЗАДАЧ этого порога не снимает — это разные пороги. Требует его слова.
- **Текст ставки `## approach` по-прежнему запрещает часть требуемого** («любой общий фреймворк под
  источники»). Его живое слово старше, но подписанный текст этого не знает. Чинит `review`/`shape`
  с его подписью, не эта нога.
- **Odin Inspector и редакторский плагин** — его «потом»; формат данных от них не зависит.
- **Волочение никогда не переходит порог стука** (125.7 против 150): одиночный носильщик остаётся
  тихим что бы ни тащил. Улика владельцу, не работа волны.

## decisions_needed

Ему НЕ задаётся ни одного вопроса — он это прямо запретил и он болен. Три вещи ждут его слова и
названы как ждущие, без вопроса: подтверждение двух переписанных карточек; порог смерти (5);
решение про однородные факты («сочетание станет нормой»).

## play_check

- 1 назвать противоречие: done — четыре, по строке каждое
- 2 реконструкция: done — 6 агентов в три угла с опровержением, ключевые числа перепроверены моей
  рукой; две МОИ ошибки найдены и названы первыми
- 3 предложить исправленное состояние: done — восемь записей
- 4 подтверждение (владелец): его слова «исправляй их», «можешь продолжать», «дай либо задачу»;
  подписанное им не тронуто, две переписанные карточки помечены как ждущие и заморожены для выдачи
- 5 friction: не требуется — дефект был в моей улике, не в OS

## Гейты

- **G1.** Дорожка `householder` держит один корень; `cargo-hands` не тронута.
- **G3.** Аппетит: потолок задач снят ЕГО словом. Волна становится 9 задачами при ОДНОМ слое —
  замер направления: 11 задач / 0 слоёв дошла, 23 / 3 partial, 7 / 1 met. Убивали слои, не задачи.
- **G7.** Ни одно геймплейное решение не принято ногой: механизм выбран инженерно и обоснован
  арифметикой; «опасно ли волочение», «случайность», «однородные факты» вынесены ему как факты.
- **G9.** CHARTER, узел и текст ставки не редактировались. Две переписанные карточки помечены и не
  выдаются.

## log

его слова сняли потолок задач, сняли «хендлер/event» как требование к реализации и добавили главное — поведение хозяина задаётся ДАННЫМИ, а не кодом; дорожка пересобрана в четыре задачи, и проверка нашла ДВЕ МОИ ошибки: цена формы памяти была завышена моей же уликой (все двенадцать тестовых конструкций памяти идут через посевной конструктор, тестов среди семи мест ноль), а дорого оказалось другое — NoticedFact читается в 75 строках тестов против 12 продакшена, поэтому набор добавляется РЯДОМ с производным свойством и ломает ноль; механизм «перечисленные сочетания отдельным видом» отклонён арифметикой 2^(N−1) — четыре вида события дают 11 подмножеств, обещанные им восемь дают 247, — и заменён набором именованных условий, который и есть единственная форма, авторимая данными; его допущение «JSON вроде уже сделано» опровергнуто чтением: профиль 20 строк и только распорядок, реакций нет, но две заготовки под требуемое реальны — поле when и намеренно пустой IsKnownPlace; гейт читаемости был неверен и тянул против его же «интересного» — заменён; названо до работы, что после открытия приёма любой второй повод при волочении станет сочетанием и что игрок сегодня достаёт ровно два вида ситуаций; t-react-mem нейтральна к спору о механизме и потому выдаётся сразу, t-react-2 и t-react-3 переписаны и заморожены до его подтверждения

## next

1. **Наряд на `t-react-mem`** — следующим шагом, слот WIN-U2 свободен. Она нейтральна к спору о
   механизме, поэтому его подтверждения не ждёт.
2. `t-react-data`, `t-react-2`, `t-react-3` — после его слова по двум переписанным карточкам.
3. Ждут его слова, без вопроса: порог смерти (5); решение про однородные факты; текст `## approach`
   ставки.
