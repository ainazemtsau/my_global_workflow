# RESULT — s-work-g-5a7c-photoset-spec-intake-001 (play: work)

## outcome

**Спека площадки принята и она переложила волну — обоснованно, а не по вкусу.**

Три вещи изменились, и каждая по своей причине:

1. **Снимают не игровую гостиную, а построенную под кадр площадку** отдельной сценой. Поэтому
   `t-room-look-1` закрыта поглощённой, вместе со своим нарядом, который не начинался. Материалы и
   свет не потеряны — они стали строками нового наряда, и там они точнее: спека называет семейства
   поимённо.
2. **Порядок работ перевёрнут спекой, и её довод сильнее моего:** камеры → свет → кнопка съёмки →
   мебель. «Комната без света с сорока предметами выглядит хуже, чем комната со светом и десятью».
   Мой черновик ставил мебель и материалы первыми.
3. **Появилась работа, которой в волне не было вовсе, и она самая выгодная за час:** кнопка съёмки.
   Пересъёмка после каждой правки света должна стоить одно нажатие, иначе неделя уходит на ручную
   постановку.

## evidence

**Главный замер, которого спека знать не могла, и он решает порядок.** Все названные ею модели лежат
в `slot/win-u3` и **отсутствуют в `main`**: `Assets/ithappy/Furniture_Realistic/**` везёт
`c-exec-g-5a7c-assetlib-1-001`, 5946 файлов, ещё не слито. Проверено поимённо на двенадцати —
`chair_011`, `shelf_007`, `curtain_027`, `coffee_table_013`, `dresser_002`, `lamp_010`, `carpet_008`,
`bathroom_021`, `prop_041`, `prop_012`, `floor_1`, `wall_1` — все на месте, все в слоте.

**Отсюда вывод, который спасает день:** первые пять строк наряда (сцена, камеры, свет, кнопка съёмки,
пост-обработка) **паков не требуют вовсе**. Слот работает сразу, а не ждёт слияния. Это ровно тот
порядок, который спека и предписала, — совпадение не случайное: она строила от камеры.

**Расхождение в самой спеке, снято запросом, а не догадкой:** заголовок называет
`target_project: C:/projects/Unity/ConceptLab`, а путь сцены — `Assets/TunnelCrew/Scenes/…`, то есть
игровой проект. Наряд велит строить в игровом, потому что скриншот обязан быть настоящим геймплеем
настоящей игры, и отправляет расхождение в Concept Lab.

**Опасность, названная заранее:** GPU Resident Drawer, Adaptive Probe Volumes и Volume — настройки
уровня проекта, а рядом идут три ноги. Каждое включение обязано называться в возврате отдельной
строкой.

## state_changes

- `card new t-photoset-1` (`active`) и `c-exec-g-5a7c-photoset-1-001` (WIN-U4, `ready`).
- `work/2026-08-17-call-photoset-1.md` — наряд: десять строк, порядок из спеки, три вопроса в
  Concept Lab, граница честности («геометрия под кадр — можно; масштаб, материалы и механики —
  настоящие»).
- `card close t-room-look-1` (`dropped`, поглощена) и `card close c-exec-g-5a7c-room-look-1-001`
  (заменён до начала работ).
- `card set t-frames-1 waiting_on` — теперь ждёт площадку, луч и тела.

## captures

Спека сама перечисляет, что в ней может быть неверно (§11), и это стоит держать: какая цифра
габарита — высота, неизвестно; телевизор не выбран поимённо; числа камер стартовые; 60–80 предметов
— оценка; окна расширены с 0,70 до 1,00 ради света и **в игровой дом это отступление не переносится
само**.

## decisions_needed

Ничего. Слияние `assetlib` — теперь ворота для строк 6 и 7 наряда, но не для его начала.

## play_check

Шаг 1 ✓. Шаг 2 ✓ — владелец прислал спеку, дополнительных вопросов ему не задано. Шаг 3 ✓. Шаг 4 ✓ —
проверено, что замена `t-room-look-1` бесплатна: наряд не начинался, слот не был занят. Шаг 5 ✓.

## log

спека площадки принята и переложила волну: снимают площадку, а не игровую гостиную, и порядок теперь камеры → свет → кнопка → мебель

## next

Владелец вставляет три пасты: WIN-U1 луч, WIN-U2 приёмка тел, WIN-U4 площадка.
