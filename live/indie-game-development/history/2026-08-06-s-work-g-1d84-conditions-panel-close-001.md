# RESULT — s-work-g-1d84-conditions-panel-close-001

RESULT s-work-g-1d84-conditions-panel-close-001 (call: work indie-game-development/сцена g-1d84/t-scene-3)
direction: indie-game-development   track: сцена   play: work   node/task: g-1d84/t-scene-3

close: light — because каждая из трёх строк `done_when` закрыта либо фактом, перемеренным этой ногой
первой рукой на опубликованных байтах `d60c2f04`, либо дословными словами владельца об этом самом
артефакте, процитированными там, где они лежат. Ни одна строка не оставлена на суждение, которого
никто не выносил.

## outcome

**`t-scene-3` ЗАКРЫТА светлым закрытием на опубликованном `main` = `d60c2f04`.** Владелец собирает
стартовые условия сам, в редакторе, своей рукой — и это его слово, а не вывод из отчёта.

Две строки, на которых тест светлого закрытия отказал утром той же ноги
(`s-work-g-1d84-conditions-panel-route-001`), закрыты его дословными ответами на два прямо заданных
вопроса. Выпущенная тем же днём свежая биндинговая проверка
`c-work-g-1d84-conditions-panel-binding-g5-001` снята с фронтира и заменена светлым закрытием —
`os/plays/work.md` §Light close прямо это допускает, когда тест держит.

Разрыв «выводимость вместо подписи» сработал второй раз за сутки ровно как надо: утром отказали, днём
спросили, получили слово, закрыли — а не вывели.

## evidence

**Дословные слова владельца 2026-08-06 (в ответ на два вопроса, заданных этой же ногой):**

> Ну, я уже не помню, именно в этом слоте я, ну, посмотрел панель, передвигал хозяина, видел, что в
> панели меняются координаты его. Ну, переключатель устраивает. Ну, мы это будем потом всё равно как
> бы дополнять, поэтому, то есть сейчас, который функционал есть, он нормально, да, можно хозяина
> перенести, это всё отлично. И, в общем, на данный момент устраивает, как работает.

**СТРОКА 1 — «На интегрированной сцене в редакторе задаются стартовые условия: где стоит груз, где
хозяин, включён он или выключен, один игрок или двое». ЗАКРЫТА.**

Байтовая часть перемерена первой рукой на `d60c2f04` и держится:

- `SceneRoots` содержит девять корней, девятый `1874233012`; объект `m_Name: StartConditions`
  (строка 598), скрипт `Assembly-CSharp::TunnelCrew.World.StartConditions` (строка 615).
- Четыре раздела инспектора: `StartConditionsEditor.cs:95` «Where the cargo stands», `:119` «Where
  the householder stands», `:155` «Is the householder in», `:177` «One player or two».
- Три условия пишутся в свои настоящие дома, а не в копию: `_playSettings.Update()` /
  `ApplyModifiedProperties()` вокруг `_cargoSpawnX`/`_cargoSpawnZ` в `NetworkPlaySettings.asset`
  (:106-109); `householder.position = position` + `SetDirty` + `MarkOwningSceneDirty`;
  `householder.SetActive(active)` (:298) под `Undo.RecordObject`.

Единственный судейский разрыв — «годится ли четвёртое условие, если переключатель ничего не меняет
при запуске» — закрыт ЕГО словами, а не решением ноги: **«Ну, переключатель устраивает. Ну, мы это
будем потом всё равно как бы дополнять, поэтому, то есть сейчас, который функционал есть, он
нормально».** Вопрос был задан прямо и с названной ценой: ему сказали, что переключатель пишет
намерение и рисует метку, но ходока судья выдаёт любому подключившемуся, и что готовый путь сделать
его настоящим рычагом есть и дёшев. Он выбрал принять как есть и назвал доработку будущей.

Второй разрыв — компиляция и работа панели в редакторе, которые утром были закрыты лишь цепочкой
вывода исполнителя из владельцева взгляда, — теперь закрыт его собственным описанием того, что он
делал и видел: **«посмотрел панель, передвигал хозяина, видел, что в панели меняются координаты
его»**. Панель рисуется, отзывается на его руку и обновляет числа. Это уже не «он видит метки,
значит собралось» — это его утверждение о том, как он ею пользовался.

**СТРОКА 2 — «Рантайм не требуется». ЗАКРЫТА, факт байтов.**

Прочитан весь `d60c2f04:Assets/TunnelCrew/World/StartConditions.cs`: в классе нет `Awake`, `Start`,
`Update`, `OnEnable` и `OnDisable` вовсе; единственный метод — `OnDrawGizmos` с тремя приватными
помощниками отрисовки, все внутри `#if UNITY_EDITOR`. Применять при запуске нечего по построению:
три условия уже лежат в тех самых байтах, которые читает Play. Вердикт стоит на отсутствии методов,
а не на комментарии класса, который заявляет то же самое.

**СТРОКА 3 — «Владелец сам собрал разные условия и запустил». ЗАКРЫТА его словами, и ЧТЕНИЕ ЗАПИСАНО
ЧЕСТНО.**

Что дают его слова прямо:

- **собрал своей рукой** — «передвигал хозяина, видел, что в панели меняются координаты его»: он
  менял стартовое условие сам и видел, как панель его регистрирует;
- **это работает** — «можно хозяина перенести, это всё отлично»;
- **запускал** — «плей работает», закоммичено в `54296e8e`
  (`docs/results/c-exec-g-1d84-conditions-panel-001.md`, раздел «Слово владельца»), сказано после
  того, как он открыл проект, нашёл панель, включил `Gizmos` и нажал Play;
- **вердикт по сделанному целиком** — «И, в общем, на данный момент устраивает, как работает».

**Где здесь чтение, а не буква.** Слово «РАЗНЫЕ» во множественном числе он перечислением не
подтверждал: он назвал одно условие, изменённое своей рукой, и вынес вердикт по панели целиком.
Строка писалась ради его вердикта — вердикт получен. Требовать от него отчёта о том, сколько наборов
условий он собрал и запускал ли каждый, значило бы гейтить его поведение, а не сборку; это запрещено
прямо (`knowledge/no-checks-the-owner-makes-by-eye.md`, и его собственное правило о том, что
предписаний, как ему проверять, не существует). Записано так, чтобы аудит видел границу, а не
догадывался о ней.

**Оговорка «я уже не помню, именно в этом слоте» разобрана, а не проигнорирована.** Она относится к
тому, в какой сессии это было, а не к тому, делал ли он это: изменение координат хозяина ВНУТРИ
ПАНЕЛИ до коммита `172aebaf` было физически невозможно — панели не существовало. Артефакт сам себя и
опознаёт.

**ПРОВОДКА НА СЛИТОЙ СЦЕНЕ ПРОВЕРЕНА ОТДЕЛЬНО И ЦЕЛА** (утренняя нога этого не делала, и это был
открытый хвост):

- `_play: {fileID: 11400000, guid: 6e9b9132205a407d8bd6df51e702bf12, type: 2}` — GUID совпадает байт
  в байт с `d60c2f04:Assets/TunnelCrew/Network/NetworkPlaySettings.asset.meta`. Ссылка разрешается,
  то есть `OnDrawGizmos` не выходит первой строкой и панель не показывает ошибку «нет ассета».
- `_householder: {fileID: 1607884504}` — разрешается в единственное определение стрипнутого
  трансформа (строка 582, `m_PrefabInstance: {fileID: 1607884503}`), а `1607884503` стоит в
  `SceneRoots`. Обе полосы — панель (:617) и `NetworkHouseholder` хозяина (:829) — пользуются ОДНОЙ
  копией: конфликт слияния `69c47a08` сведён верно.
- `_players: 2`.

Риск «слитую сцену не запускал никто» к этой задаче больше не относится и остаётся жить в
`i-published-to-main-before-any-direction-close-001` — той же дорогой, что и вчерашний `t-host-1`.

**Что НЕ проверялось.** Продуктовый репозиторий открывался только на чтение; ни записи, ни слияния,
ни push, ни касания реестра слотов. Unity не запускался — и именно поэтому компиляция закрыта его
словами о собственной работе с панелью, а не объявлена проверенной агентски.

## state_changes

Применено этой же ногой (агентский CLI на этом репозитории: сессия сначала выпускает RESULT, затем
становится своим писателем). Состояние перечитано свежим: между утренней ногой и этой в репозиторий
легла `s-day-g-1d84-profile-format-and-core-recut-001` (`62468f28`); её дельта сохранена целиком.

1. `NOW.md` заголовок: `updated: 2026-08-06 by s-work-g-1d84-conditions-panel-close-001`.
2. `NOW.tasks` `t-scene-3` — `status: open` → `done`; утренняя `note` заменена полем `closed:` с
   разбором всех трёх строк, дословными словами владельца, записанным чтением по слову «разные»,
   записанным понижением биндинговой проверки и проверкой проводки на слитой сцене. Измерение про
   мёртвый четвёртый переключатель сохранено как факт, с пометкой, что владелец принял его осознанно.
3. `NOW.open_calls` — удалён `c-work-g-1d84-conditions-panel-binding-g5-001` (заменён светлым
   закрытием; файл наряда сохранён в `work/` нетронутым).
4. `LOG.md` — одна строка сверху.
5. `history/2026-08-06-s-work-g-1d84-conditions-panel-close-001.md` — этот RESULT целиком.

Не тронуто: `CHARTER.md`, `TREE.md`, `knowledge/**`, полосы кроме «сцена», решения, задачи кроме
`t-scene-3`, все записи включая заведённые дневной ногой
(`i-wave-recut-to-core-owner-decided-001`, `i-probe-commit-exists-only-locally-001`), наряды
`c-exec-g-1d84-cargo-tight-spot-001` и `c-exec-g-1d84-free-kits-probe-001`, продуктовый репозиторий.

Продолжения полосы «сцена» НЕ выпущено и выпускать нечего: единственная оставшаяся задача полосы
`t-scene-4` (собранный exe, порядок 23) стоит `blocked` до того, как внутри волны появится что
смотреть вдвоём. Полоса встаёт; свободный слот никакая нога сама не занимает. Ставка не закрывается —
открытых задач в ней много, поэтому `review` не выпускается.

## captures

- **Пятое условие «где стоят игроки» остаётся живым капчуром.** Z спавна прибит к нулю константой в
  `NetworkPlaySettings.CreateSpawnState`, двоих не развести по разным комнатам; курьер трогать не
  надо. Владелец 2026-08-05 выбрал увезти домой. Его сегодняшнее «мы это будем потом всё равно как бы
  дополнять» — про переключатель, и требованием на пятое условие не становится.
- **MPPM 2.0.2 уже стоит** в `Packages/manifest.json`: `IsMainEditor` сделал бы «двое» одним щелчком
  примерно восемью строками в `DirectNetworkPlayBootstrap`. Хранить до разговора о доработке панели.
- **Два решения владельца по-прежнему не поехали** (откат Unity Cloud, закрытие мёртвого корня
  `c-exec-grid-v1-g01-document-authority-001`): эта нога продуктового наряда не писала, нести их было
  нечем. Обе записи открыты со своими `mechanism_failed`.

## decisions_needed

Нет. Оба вопроса, ради которых утром выпускалась биндинговая проверка, владелец закрыл сам в этом же
чате.

## play_check

- 1 recite: done — `t-scene-3`, три строки `done_when`, полоса «сцена», ставка `g-1d84`; вход —
  утренний отказ теста светлого закрытия и два вопроса, переданных владельцу.
- 2 owner inputs (owner): done — оба вопроса заданы прямо и оба отвечены дословно: «Ну, переключатель
  устраивает. Ну, мы это будем потом всё равно как бы дополнять, поэтому, то есть сейчас, который
  функционал есть, он нормально» и «посмотрел панель, передвигал хозяина, видел, что в панели меняются
  координаты его… можно хозяина перенести, это всё отлично. И, в общем, на данный момент устраивает,
  как работает». Ни одно слово не выведено и не пересказано.
- 3 do the work: done — тест светлого закрытия применён построчно заново, теперь с его словами;
  дополнительно перемерена проводка панели на слитой сцене, чего утренняя нога не делала.
- 4 self-check: done — строка 1 закрыта байтами плюс его вердиктом по четвёртому условию и его
  описанием работы с панелью; строка 2 — факт байтов; строка 3 — его слова, с явно записанной
  границей чтения по слову «разные». Все три строки имеют разрешающийся токен, поэтому светлый
  маршрут законен.
- 5 close: done — `t-scene-3` `done`; биндинговая проверка снята с фронтира с записанным понижением;
  продолжения полосы нет, потому что `t-scene-4` заблокирована порядком; `review` не выпускается,
  ставка не закрыта.

## log

см. строку в `LOG.md`.

## next

return-to-owner. Полоса «сцена» встала: `t-scene-4` (exe) ждёт своего двадцать третьего места, а
диспозицию урезанных задач решает перенарезка по `i-wave-recut-to-core-owner-decided-001`. Ни одного
нового наряда эта нога не открывает.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-work-g-1d84-conditions-panel-close-001.md
