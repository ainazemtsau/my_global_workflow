# RESULT — s-work-g-1d84-delivered-pair-close-001

play: work (два светлых закрытия на одной сводке улик)
direction: indie-game-development · node: g-1d84 · tracks: сцена, внешний-вид
date: 2026-08-07
base_read: OS `37eba1f0` — свежий committed state после ноги `s-repair-g-1d84-demo-focused-task-cut-001`;
`TREE.md`, `NOW.md`, `LOG.md`, `os/plays/work.md` §Light close, `os/adapters/coding-agent.md` перечитаны
перед применением. Продукт `C:/projects/Unity/GasCoopGame`, опубликованный `main` = `origin/main` =
`f03b1468`, чекаут чист; продуктовые байты, ветки, слоты и slot-state НЕ изменялись.

## Отклонение от «одна нога = одна работа», объявлено, а не спрятано

Эта нога закрывает ДВЕ задачи на ДВУХ полосах. Основание: обе доставки лежат на ОДНОМ опубликованном
SHA, проверяются ОДНОЙ сводкой улик и подпёрты ОДНИМ прогоном ворот, который нога сделала сама. Разделять
это на две ноги значило бы дважды мерить одно и то же состояние. Владелец прямо попросил один
проход очистки. Строчная диспозиция у каждой задачи своя и ниже не смешана.

## Что закрыто и чем

### t-house-6 — точка доставки и выход из дома · `close: light` на `f03b1468`

Доставка в `main` с 2026-08-06 15:19 (`6b1622fc` → merge `29e8505d` → `d647a58b`).

| Строка `done_when` | Чем закрыта |
|---|---|
| 1. Одна названная точка снаружи | БАЙТЫ: `house-exit-delivery` в `World/DeliveryPoint.cs:17` и `Scenes/IntegratedHouse.unity:730`; объект `Delivery Point` один. Судья держит `DeliveryObjectives` списком и отвечает по имени |
| 2. Проём в наружной стене, проходимый ходоком и грузом; пол дотянут | ЧИСЛА: восточная стена урезана правкой экземпляра до `scale.z 5.5` при `pos.z +4.25` (z 1.5…7.0); новый сегмент `Outer East South` при x 7.25, z −4.25, scale.z 5.5 (z −7.0…−1.5). **Проём 3 м, z −1.5…+1.5.** У сегмента `HouseWall` (guid `b3735121d0354c5aa02fd9f1a6cd0d7c`) и `BoxCollider` (`!u!65`) — держит и ходока, и груз. `_collider: {fileID: 0}` не связан, поэтому `HouseWall.cs` получил ленивый `GetComponent<Collider>()`. Снаружи `Delivery Yard Floor`. `ThreeRoomHouse.prefab` в диффе отсутствует — данные дома не тронуты |
| 3. Груз в точке → заход удался, игра показывает | Его слова в `f03b1468:docs/results/c-exec-g-1d84-delivery-point-001.md`: «Всё работает, что ты написал. Вижу надпись, зелёный этот становится». Устройство — `DeliveryObjectiveTests.cs` |
| 4. Нет счёта, награды, провала, повторного захода, экономики | БАЙТЫ: поиск по `Core/` на `score\|reward\|economy\|payout\|retry` пуст |
| 5. Владелец вынес груз и увидел результат | Те же его слова |

### t-host-7 — контакт груза рождает шум · `close: light` на `f03b1468`

Доставка в `main` с 2026-08-06 13:16 (`16e87988` → merge `9077b26f` → `efff234f`). Закрывается редакция
`done_when`, приведённая к критерию 7 карточки ногой `s-repair-g-1d84-demo-focused-task-cut-001`.

| Строка `done_when` | Чем закрыта |
|---|---|
| 1. Контакт с домом через названный шов в замеченный факт | БАЙТЫ: `CargoContactNoise` — `Core/Cargo/CargoNoise.cs:68`; единственная дорога `AuthoritativeCargoRoster.cs:199` и `:125`; `CargoBodyPort.cs:53` — «CargoContactNoise is what reads it; damage still nobody». Тест `TheNoiseIsDecidedByTheOneNamedSeam` |
| 2. Контакт с другим игроком ничего не делает | БАЙТЫ: `PlayerContact\|WalkerContact\|HitWalker\|StruckWalker` — НОЛЬ вхождений в `Assets/` и `tests/`. Тест `ACargoDrivenIntoPeopleDoesNothingAtAll`. **Это его снятие, не выполнение:** построено, испытано им, удалено — `b7e30f2d` числом, `25cdf4af` целиком, по его словам «давай сейчас вообще уберем логику» |
| 3. Кнопки инспектора не нужны | Его прогон: «Со стеной работает отлично. То есть, он прибегает» |
| 4. Хозяин идёт проверять место | Тот же прогон + `ContactWithTheHouse_IsNoticedAndSendsHimToWhereItHappened` (приход именно НА МЕСТО удара) |
| 5. Проверка различает отсутствие контакта и контакт с домом | `NoContact_MakesNoNoiseAndLeavesHimOnHisRoute` + предыдущий. Улик БОЛЬШЕ требуемого: покрыт и третий случай, и соседние пороги |

## Ворота — прогнаны этой ногой, не пересказаны

На чистом чекауте `f03b1468`:

- `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` → **110/110, ноль упавших**
- `./pwsh.cmd tools/check.ps1` → `hygiene OK` / `OK: all active gates green`

## Что НЕ закрыто и записано дырками

- `i-delivery-owner-run-predates-final-bytes-001` — НОВАЯ. Видимый прогон владельца по доставке был ДО
  внутренней перестройки на имя/список; повторного прогона на финальных байтах не было. Этот SHA на
  закрытие отправил он сам. Смотреть при наряде `t-scene-4`.
- `i-broken-profile-rejection-never-observed-001` — жива и не сдвинулась. Перемерено на `f03b1468`:
  у `HouseholderProfileLoader` ноль тестов, в `tests/` ни одного про сломанный файл,
  в `HouseholderProfileTests.cs` ровно 4 теста.
- Верхняя полоса реакции хозяина («пришёл и стоит полторы секунды») владельцем не наблюдалась —
  разбега в игре нет. Отчёт называет это лечимым одним числом, не ногой.

## Снято

- `open_calls` → `[]`. Оба корня (`c-exec-g-1d84-delivery-point-001`, `c-exec-g-1d84-world-noise-contact-001`)
  доставлены и закрыты; их запись `blocked` / «НЕ запущен» была неверна почти сутки.
- `i-two-calls-recorded-blocked-are-actually-running-001` — ЗАКРЫТА этой ногой по её собственному
  условию: «статусы обоих корней и судьба удалённой строки `t-host-7` записаны из настоящих улик».
- `c-work-g-1d84-householder-profile-file-binding-g5-001` → `status: superseded`. Сирота, приехавшая
  слиянием `8441787d`: в `NOW.md` её нет нигде. Все четыре несущие предпосылки мертвы (кандидат и
  handoff опубликованы, задача закрыта, поехало слияние, а не exact SHA). Содержание при слиянии не
  потерялось — оба несущих blob совпали: `fba2242a…` и `a20ec69c…`.

## Разблокировано

`t-scene-4` — `blocked` → `open`. Одиннадцать из двенадцати задач волны закрыты, осталась она одна.
Наряда НЕТ. Три входа измерены и положены в задачу: подключение по адресу уже построено
(`NetworkSessionService.cs:67/:74/:93`, `LobbyController.cs`, `Lobby.unity`); в списке сборки лишняя
дефолтная `Assets/Scenes/SampleScene.unity`; автосборки в проекте нет вообще (`BuildPipeline` /
`BuildPlayerOptions` не встречаются). Открытый вопрос ФОРМЫ, решает владелец: собирает exe исполнитель
в слоте через MCP (`AGENTS.md:227` это на Personal разрешает, но сборка плеера так не делалась ни разу)
или жмёт Build владелец, а исполнитель проверяет подключение второй копии.

## Гейты

- **G5** — `close: light` для обеих задач. Каждая строка `done_when` перемерена этой ногой первой рукой
  из закоммиченных байтов, либо закрыта уже закоммиченными дословными словами владельца. Отдельный
  свежий чат не открывался, и это ровно то исключение, которое `os/plays/work.md` §Light close допускает.
  Венью названо: улика собрана в этой сессии, ворота прогнаны в ней же.
- **G9** — карточка не трогалась. Обе задачи закрыты против текста, уже приведённого в соответствие
  утверждённой владельцем правкой критерия 7.

END_OF_FILE: live/indie-game-development/history/2026-08-07-s-work-g-1d84-delivered-pair-close-001.md
