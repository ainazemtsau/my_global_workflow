# RESULT — s-repair-card-journal-dedup-001

direction: indie-game-development
play: repair
node/task: t-scale-20-1 (закрытая задача волны 4 узла g-5a7c)
track: —
date: 2026-08-28
outcome_kind: repair — карточка несла три блока `## журнал` вместо одного, из-за чего два читателя одного формата расходились и направление стояло красным

owner_approved: |
  Гигиена по границе плея `repair` («Hygiene needs none: dangling pointers, EOF trailers,
  **dedup, schema-shape fixes**»), поэтому именной санкции на поверхность не требуется.
  Его слово на запуск ноги, дословно: **«indie game development давай исправим»** —
  в ответ на диагноз maintenance-сессии, записанный строкой FRICTION от 2026-08-28
  с маршрутом «одна нога `repair` на indie-game-development».

outcome: |
  **ДИАГНОЗ БЫЛ ВЕРЕН, А ПРЕДЛОЖЕННОЕ ЛЕЧЕНИЕ — НЕТ.** Строка FRICTION называла фикс так:
  «чинится удалением лишнего блока за минуту». Лишнего блока не было. Три блока `## журнал`
  держали СЕМЬ РАЗНЫХ записей — 2 + 3 + 2, — и удаление любых двух уничтожило бы пять записей
  журнала задачи, которая на этот момент уже закрыта и лежит доказательством. Ремонт сделан
  слиянием, а не удалением: все семь на месте, ни один байт записи не переписан.

  **ЧТО ИМЕННО КРАСНЕЛО.** `panel/cards.py:78` останавливается на повторе имени блока
  (`блок журнал повторяется`), поэтому карточка не читалась совсем: `test_readers` грузил
  279 из 280, `test_carrier` считал одно расхождение между читателями.

  **ПОЧЕМУ ЭТО БЫЛА НЕ КОСМЕТИКА, А ЗАРЯЖЕННАЯ МИНА.** Второй читатель, `osctl.py`, на том же
  файле НЕ останавливается: `read_card` (строки 439–448) на каждом `## журнал` делает
  `blocks[cur] = []`, то есть молча оставляет только ПОСЛЕДНИЙ блок, и при этом трижды кладёт
  имя `журнал` в `order`. Дальше `write_card` (строки 473–476) собирает тело по `order` —
  то есть выписал бы содержимое последнего блока ТРИ РАЗА и стёр два первых. Первая же запись
  в эту карточку любым штатным способом уничтожила бы пять записей журнала молча и необратимо
  в рабочем дереве. Красный `test_carrier` был единственным, что стояло между этим файлом и
  такой записью — и он сработал ровно за тем, за чем заведён.

  **ОТКУДА ВЗЯЛОСЬ.** Карточку писали рукой, а не командой. Задвоение пришло двумя ногами:
  `f1caef31` дописала блок «замер цены» и следом свой `## журнал`, `c71a20fb` дописала блок
  «райдер» и снова свой `## журнал`; на `2aa145ea` карточка уехала в `closed/` уже с тремя.
  Признак ручного письма прямой: записи внутри блоков лежат СТАРЫМ СВЕРХУ, а команда
  `log add` кладёт новое сверху (`journal_put`, «Порядок записи знает команда, а не тот,
  кто её зовёт»). Схема это прямо запрещает — «A card is never hand-edited».

  **ПОБОЧНОЕ, НАЗЫВАЮ ЧЕСТНО.** В шапке изменилась одна строка сверх ремонта:
  `goal: 'Налётчик…'` стал `goal: Налётчик…` без кавычек. Это не правка, а следствие того,
  что файл переписан ПЕРОМ САМОГО ИНСТРУМЕНТА: `write_card` пересобирает шапку через
  `yaml.safe_dump`, и кавычки там не рождаются. Значение и тип не изменились, оба читателя
  разбирают строку одинаково. Я выбрал канонический байт инструмента вместо сохранения
  прежнего: иначе первая же штатная запись в эту карточку дала бы этот диф отдельно.

evidence: |
  Проверено этой ногой 2026-08-28 командами на рабочем дереве, а не по памяти:

  - **До ремонта.** `python panel/test_readers.py` → `FAIL загружаются обе папки: живых 130,
    закрытых 150, загружено 279`. `python panel/test_carrier.py` → `не читается: t-scale-20-1.md:
    ОШИБКА: …: блок журнал повторяется` и `FAIL osctl и панель читают их одинаково (расхождений 1)`.
  - **Семь записей, а не «лишний блок».** Разбор файла по `## ` дал блоки
    `done_when`(8) · `note`(16) · **журнал(2)** · `замер цены…`(23) · **журнал(3)** ·
    `райдер: модель хозяина`(7) · **журнал(2)** — 2+3+2 = 7 непустых строк журнала.
  - **Хронология подтверждена git, а не догадкой.** `git log --oneline -- <оба пути карточки>`
    даёт семь коммитов в порядке, совпадающем с порядком записей в файле сверху вниз:
    `bef6e4e6` (заведена) → `ac095faf` (разблокирована) → `f1caef31` (замер цены) →
    `73e8b7fd` (выдана в U2) → `a50d9651` (учёт исправлен на фактический) → `c71a20fb` (райдер) →
    `2aa145ea` (закрыта, уезд в `closed/`). Поэтому «новое сверху» — это ровно обратный порядок.
  - **Когда задвоилось.** Счёт `^## журнал` по версиям файла: `bef6e4e6` = 1, `f1caef31` = 2,
    `c71a20fb` = 3, `2aa145ea` = 3 (уже в `closed/`).
  - **Мина в `osctl` проверена чтением кода**, не запуском на живом файле:
    `osctl.py:439-448` (`blocks[cur] = []` + `order.append(cur)` на каждом повторе) и
    `osctl.py:473-476` (тело собирается циклом по `order`).
  - **Одиночный ли случай.** Просмотрены ВСЕ 339 файлов под любым `cards/` в `live/**` на
    повтор имени блока: задвоение ровно одно — эта карточка. Других нет.
  - **После ремонта.** Все семь приёмок панели зелёные: `test_readers`, `test_carrier`,
    `test_docs`, `test_osctl`, `test_wave2`, `test_cards_cmd`, `test_context` — каждая печатает
    `ПРИНЯТО`. `test_readers` → `загружаются обе папки: живых 130, закрытых 150, загружено 280`.
    `test_carrier` → `osctl и панель читают их одинаково (расхождений 0)`.
    `python osctl.py check --direction indie-game-development` → `механических проблем нет`.
  - **Размер правки.** `git diff --stat` → один файл, 8 вставок / 11 удалений.

state_changes: |
  1. `live/indie-game-development/cards/closed/t-scale-20-1.md`: три блока `## журнал`
     слиты в ОДИН, стоящий последним — как у 142 из 149 закрытых карточек направления.
     Все семь записей сохранены дословно и переставлены «новое сверху» по схеме
     (`os/schema/direction-files.md`: «its own history, newest first») и по поведению
     `journal_put`. Содержательные блоки «замер цены…» и «райдер: модель хозяина»
     не тронуты и остались на своих местах.
  2. Тем же действием в журнал карточки добавлена восьмая запись — причина ремонта
     с указателем на этот отчёт. Журнал остался append-only: ни одна прежняя строка
     не переписана и не удалена.
  3. Файл переписан ПЕРОМ ИНСТРУМЕНТА: `read_card`/`journal_put`/`write_card` вызваны
     из `osctl.py` напрямую, потому что через CLI это невыразимо — `card block` запрещает
     переписывать `журнал` (append-only), а `log add` не знает флага `--closed`.
     Побочно шапка пересобрана `yaml.safe_dump`: `goal` потерял кавычки, значение то же.
  4. Полный RESULT сохранён в `history/2026-08-28-s-repair-card-journal-dedup-001.md`.
  5. Две строки в `os/FRICTION.md` (шаг 5 плея): о самом ремонте и об OS-дыре,
     которую он вскрыл. OS здесь не чинится.

  Больше в `live/**` не тронуто ничего: ни `NOW.md`, ни одна другая карточка,
  ни `CHARTER.md`, ни `knowledge/`. Ставка, задачи и полосы направления не менялись —
  ремонт касается формы одной закрытой карточки, а не состояния работы.

captures:
  - |
    **ЧИТАТЕЛЬ, КОТОРЫЙ МОЛЧА ТЕРЯЕТ, ОПАСНЕЕ ЧИТАТЕЛЯ, КОТОРЫЙ ПАДАЕТ.** Панель отказалась
    читать файл и этим спасла его. `osctl` прочитал «успешно» — и был в одной команде от
    уничтожения пяти записей. Дешёвое лечение известно и стоит четыре строки: `read_card`
    должен отказываться на повторе имени блока ровно так же, как это делает `panel/cards.py:78`.
    Здесь не чинится по границе плея; заведено строкой трения.
  - |
    **`log add` НЕ ЗНАЕТ `--closed`, ХОТЯ ИМЕННО ЭТОТ СЛУЧАЙ ОПИСАН В КОДЕ.** Комментарий
    `live_only` дословно: «Закрытую карточку молча не правят. Но и чинить её иногда надо —
    например когда порчу в неё записал сам инструмент. Тогда правка идёт с ЯВНЫМ `--closed`».
    Флаг есть у `card set` и `card block`, а у `log add` его нет — то есть записать ПРИЧИНУ
    ремонта в журнал закрытой карточки штатной командой нечем. Заведено той же строкой трения.
  - |
    **КАРТОЧКУ ПИСАЛИ РУКОЙ, И ЭТО ВИДНО ПО ПОРЯДКУ ЗАПИСЕЙ.** Внутри каждого блока записи
    лежали старым сверху, тогда как `log add` кладёт новое сверху. Три ноги подряд дописали
    в карточку блок и следом свой заголовок `## журнал`, вместо `card block` + `log add`.
    Запрет в схеме есть («A card is never hand-edited»), проверки под ним не было ни одной:
    задвоение поймала приёмка панели через две недели, а не запись.

decisions_needed: []

play_check:
  - 1 назвать противоречие — done. Файл против схемы («Every card carries a `## журнал` block»)
    и против собственного читателя: панель падает, `osctl` молча теряет два блока из трёх.
  - 2 восстановить — done. Порядок семи записей восстановлен не по виду файла, а по семи
    коммитам git, каждый из которых добавил ровно одну строку; блоки пересчитаны прямым разбором.
  - 3 предложить исправленное состояние — done. Одно изменение: слияние блоков с сохранением
    всех записей, плюс восьмая строка о причине ремонта.
  - 4 подтверждение владельца — не требуется по границе плея: это dedup и schema-shape fix,
    прямо названные гигиеной. Граница удаления соблюдена: НИЧЕГО не удалено — ни записи,
    ни содержательного блока, ни карточки; только сняты два повторных заголовка.
  - 5 трение — done. Две строки в `os/FRICTION.md`; OS не чинится этой ногой.

log: закрытая карточка t-scale-20-1 несла ТРИ блока «журнал» вместо одного, из-за чего panel/cards.py отказывался её читать и две приёмки из девяти стояли красными — 279 карточек из 280; предложенный диагнозом фикс «удалить лишний блок» уничтожил бы ПЯТЬ записей из семи, потому что лишнего блока не было: три блока держали 2+3+2 разных записи, восстановленных по семи коммитам git; ремонт сделан слиянием в один блок с порядком «новое сверху» по схеме, все семь записей целы дословно, добавлена восьмая о причине; ГЛАВНОЕ НЕ КРАСНЫЙ, А МИНА ПОД НИМ — второй читатель osctl на этом файле НЕ падает, а молча оставляет только последний блок и трижды кладёт имя в order, поэтому write_card при первой же штатной записи выписал бы последний блок трижды и стёр два первых, то есть красная приёмка была единственным, что стояло между карточкой и молчаливой потерей пяти записей; карточка переписана пером самого инструмента, потому что через CLI это невыразимо — card block запрещает переписывать журнал, а log add не знает --closed, и оба этих факта заведены строкой трения вместе с предложением заставить read_card отказываться на повторе имени блока так же, как это делает панель; происхождение названо: карточку писали рукой вопреки схеме, признак прямой — записи внутри блоков лежали старым сверху, а команда кладёт новое сверху; проверено, что случай одиночный — просмотрены все 339 файлов под cards/ в live/**, задвоение ровно одно; после ремонта зелены все семь приёмок панели, загружено 280 из 280, расхождений между читателями ноль, osctl check по направлению чист

next: |
  От него по этому ремонту ничего не нужно. Направление снова читается целиком, приёмки зелёные.

  Осталось снаружи и ждёт отдельного маршрута: правка `osctl` (отказ `read_card` на повторе
  имени блока + флаг `--closed` у `log add`) — это `MAINTENANCE REQUEST`, не эта нога.
  Обе точки лежат строкой в `os/FRICTION.md` с ключом.

END_OF_FILE: live/indie-game-development/history/2026-08-28-s-repair-card-journal-dedup-001.md
