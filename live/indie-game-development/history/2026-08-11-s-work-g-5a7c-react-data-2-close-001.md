# RESULT — s-work-g-5a7c-react-data-2-close-001

play: work · close: **light — because** все три строки `done_when` наряда и все пять строк карточки
`t-react-data-2` перемерены ПЕРВОЙ РУКОЙ по закоммиченным байтам опубликованной головы: набор тестов
прогнан здесь на выгруженном дереве публикации, дифф, границы, каталог реакций, профиль и главный
тест прочитаны командами, прогнанными здесь. Ни одной строки, которую судит его глаз, его ощущение
или чужой прогон, в этой задаче нет — его вердикт по дорожке назначен на `t-react-3`
direction: indie-game-development · node: g-5a7c · track: householder · task: t-react-data-2 ·
call: c-exec-g-5a7c-react-data-2-001
date: 2026-08-11

base_read: OS `wt/indie-game-development`, голова `26fc6283`, дерево чисто. Продукт перемерен
`git ls-remote origin` после срабатывания опроса: `origin/dev` = `origin/main` =
`87d399f67b0d9ec34ec97463692d753d590a7f6a`. Все четыре слота — `AVAILABLE / lease none`.

## outcome

`t-react-data-2` закрыта. **Поведение хозяина описывается ФАЙЛОМ, и описанное файлом работает В
ИГРЕ.** Это главное требование владельца, названное им дословно: «мы программно не задаём никакого
хозяина».

**Главный гейт был именно про игру, и он взят.** Наряд ставился ради дыры, найденной при выдаче:
`HouseholderDecisionPipeline.Default` был ПУСТЫМ конвейером, композиции с обработчиками жили только
в тестах, и оба обработчика, построенные `t-react-2`, в собранной игре не работали. Теперь хост
читает профиль, собирает из него конвейер, и обработчик, существующий ТОЛЬКО в файле, проверяется
настоящим `CarryWorld`.

## evidence — перемерено и прогнано первой рукой на `87d399f6`

### Опубликовано ровно то, что было проверено

История `2c8dc0c7..87d399f6` — шесть коммитов: один продуктовый (`9a43548f`), его откат
(`42997a72`), реаплай (`30c05449`) и три документных. **Дифф КОДА от проверенного кандидата
`30c05449` до опубликованного пуст** (`git diff --stat 30c05449..87d399f6 -- Assets tests core
ProjectSettings Packages tools validation.config` — ничего). Весь дифф кандидат→публикация — один
изменённый документ.

### Прогон сделан здесь

```text
dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release
Пройдено 196, не пройдено 0, пропущено 0, всего 196
```

На выгруженном `git archive 87d399f6` дереве. База `2c8dc0c7` давала `184/184` — прогнано этой ногой
раньше. Разница `+12`.

### `done_when` 1 — описание файлом

Профиль получил разделы `behaviorBranches` и `decisionHandlers`; из авторской строки собираются обе
готовые формы обработчика и та же `HouseholderBehaviorBranch`. Форма шага ветки общая с формой шага
распорядка — то, что наряд перемерил заранее и потребовал не изобретать новый формат.

### `done_when` 2 — ГЛАВНЫЙ ГЕЙТ: работает в игре

Тест `HouseholderProfileAuthoringTests.FileOnlyHandlerRunsInsideCarryWorldAndBranchResumesPriorOccupation`
присутствует на опубликованной голове и строит НАСТОЯЩИЙ `CarryWorld` плюс отдельный базовый мир для
сравнения. Прочитано, а не принято со слов: проверка идёт по игровому пути, а не по пути правил.
Хост читает файл в `NetworkHouseholder.LoadProfileForHost`; политика выбирается только если файл
действительно что-то авторил.

### `done_when` 3 — отказы, пустой авторинг, границы, откат

Кривая строка отвергается НАЗВАННО и с путём в JSON — шесть форм порчи перечислены возвратом
(неизвестный факт, неизвестное умение, дубль имени, отрицательная длительность, инертная реакция,
поле чужой формы), каждая со своей диагностикой и позицией в файле.

**Пустой авторинг ничего не меняет:** профиль на опубликованной голове несёт ровно два ПУСТЫХ
раздела `"behaviorBranches": []`, `"decisionHandlers": []` — прочитано на HEAD.

**Пять производственных реакций целы по СОДЕРЖАНИЮ:** `Name = "` встречается в каталоге ровно ПЯТЬ
раз, а единственное изменение каталога — ДОБАВЛЕННЫЙ метод поиска по имени; ни одно значение ни
одной строки не тронуто.

**Границы целы — доказано пустым выводом на опубликованной голове** по `Core/Cargo/**`,
`Network/CargoBody.cs`, `Network/NetworkPlaySettings.{cs,asset}`, `Scenes/**`, `ProjectSettings/**`,
`Assets/GasCoopGame/**`, `Core/Multiplayer/CarryWorld.cs`.

**ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК, третий раз подряд.**
`git rev-parse 42997a72^{tree}` = `931b31eb…` = дерево головы `2c8dc0c7`; обратный revert
`30c05449^{tree}` = `f480926f…` = дерево исходного `9a43548f`.

### Разрез не делался

Наряд разрешал резать; исполнитель не резал и сказал это прямо. Доставлены и ветки из файла, и обе
готовые формы обработчика.

### Проверено сверх наряда

Тестовый проект получил `Newtonsoft.Json` и линк на загрузчик. Перемерено, что расхождения между
тестами и игрой нет: загрузчик использовал `Newtonsoft.Json` уже ДО задачи, а игра держит
`com.unity.nuget.newtonsoft-json 3.2.1` в манифесте. Побочный эффект полезный: загрузчик живёт вне
`Core` и теперь покрыт headless-тестами, чего раньше не было.

## Что НЕ проверено

Unity-редактор код не собирал — наряд прямо ограничил доказательство engine-free ядром и
headless-тестами. Названо, а не замолчано.

## state_changes

1. `t-react-data-2`: закрыть, терминальный статус `done`. Светлым путём — названо вслух по букве
   `evaluator` клауза 2.
2. `c-exec-g-5a7c-react-data-2-001`: закрыть — исполнен и опубликован в `87d399f6`.
3. `NOW.md`, `g-5a7c`, текст ставки и дорожка `cargo-hands` не тронуты. Дорожка `householder`
   освободилась: остался ПОСЛЕДНИЙ корень — `t-react-3`.

## captures

- **Цена НОВОЙ ФОРМЫ обработчика — два продуктовых места** (класс плюс один case загрузчика); новая
  СТРОКА готовой формы стоит только JSON. Это число нужно знать перед наймом конвейера ассистентов.
- **`t-react-3` устроена иначе трёх предыдущих:** её закрытие требует ЕГО вердикта в собранной игре,
  значит светлое закрытие ей недоступно по букве волны, и наряд обязан это сказать заранее.
- Порог смерти (5) волны про число ног остаётся пробитым и ждёт его слова.
- Карточка вопроса про два непроверенных утверждения возврата `t-react-mem` остаётся открытой.

## decisions_needed

Нет. Владелец поручил закрыть работу и составить следующий наряд без него.

## play_check

- 1 recite: done — пять строк карточки и три строки наряда сведены
- 2 owner inputs (owner): done — его вердикт назначен на `t-react-3`
- 3 do the work: done — опубликованное перемерено первой рукой, прогон сделан здесь
- 4 self-check: done — каждая строка закрыта своим измерением; непроверяемое названо
- 5 close: done — светлый путь; продолжение дорожки — `t-react-3`

## Гейты

- **G1.** Дорожка `householder` освободилась; остался последний корень.
- **G3.** Аппетит не тронут.
- **G5.** Светлый путь: каждая строка перемерена первой рукой. Свежий чат не открывался, названо.
- **G7.** Ничего продуктового не решено.
- **G9.** Карточки задачи и ставки не редактировались.
- **evaluator, клауза 1.** Закрытие ПОСЛЕ публикации.

## log

`t-react-data-2` закрыта светлым путём на опубликованной голове 87d399f6: поведение хозяина теперь описывается ФАЙЛОМ, и описанное файлом работает В ИГРЕ — главное требование владельца «мы программно не задаём никакого хозяина» исполнено; главный гейт был именно про игру и взят: наряд писался ради найденной при выдаче дыры — конвейер в игре был ПУСТЫМ и оба обработчика t-react-2 в собранной игре не работали, — а теперь хост читает профиль, собирает из него конвейер, и тест строит НАСТОЯЩИЙ CarryWorld для обработчика, существующего только в файле; 196/196 прогнаны этой ногой на выгруженном дереве публикации против 184/184 на прежней голове; дифф КОДА кандидат-публикация ПУСТ; кривая строка отвергается названно и с путём в JSON по шести формам порчи; профиль несёт ровно два ПУСТЫХ раздела, поэтому поведение по умолчанию не изменилось, пять реакций целы по содержанию и единственное изменение каталога — добавленный поиск по имени; границы пусты по семи запрещённым поверхностям; ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК третий раз подряд; разрез не делался вовсе; сверх наряда проверена зависимость Newtonsoft — расхождения между тестами и игрой нет, а загрузчик впервые покрыт headless-тестами

## next

1. **Последний корень дорожки — `t-react-3`.** Она устроена иначе трёх предыдущих: её закрывает
   ВЕРДИКТ ВЛАДЕЛЬЦА в собранной игре, значит светлое закрытие ей недоступно и наряд обязан сказать
   это заранее, а работа исполнителя кончается на «построено, опубликовано, откат исполнен».
2. Ждут его слова, без вопроса: порог смерти (5), текст `## approach` ставки, два утверждения
   возврата `t-react-mem`.
