# RESULT — s-shape-g-5a7c-wave-10-001

direction: indie-game-development
play: shape
node/task: g-5a7c / bet-g-5a7c-wave-10
track: —
date: 2026-09-01
outcome_kind: shape — десятая волна открыта его словом: слух решается расстоянием, и это же публикует четыре готовых куска девятой волны; замах ждёт его подтверждения

owner_approved: |
  Его слово 2026-09-01, дословно: **«открывай волну про слух»**. Ему предшествовало его же
  **«да, согласен»** на предложение сделать следующую волну не про новое поведение, а про
  недостроенный слой.

  **ЗАМАХ ЕГО СЛОВА НЕ ИМЕЕТ.** Число задач он не называл; предложено четыре, и блок `appetite`
  прямо говорит, что замах подлежит подтверждению. Девятая волна открывалась так же — сначала
  экран целиком, потом его «Открывай как есть, семь задач».

outcome: |
  **ПРАВИЛО, ЗАВЕДЁННОЕ ЕГО ЖЕ СЛОВОМ ВЧЕРА, СРАБОТАЛО СЕГОДНЯ ПРИ ПЕРВОЙ ЖЕ НАРЕЗКЕ.**
  `knowledge/the-sound-decision-is-taken-at-every-cut.md` требовал прочитать две карточки и принять
  решение вслух — взять или отложить с записанной причиной. **Решение принято: ВЗЯТЬ.** Правило
  исполнено, а не обойдено, и это первый случай, когда сторож, поставленный против забывания,
  отработал по назначению.

  **ГЛАВНЫЙ ЗАМЕР НАРЕЗКИ ПЕРЕВОРАЧИВАЕТ ПРЕДСТАВЛЕНИЕ О РАЗМЕРЕ РАБОТЫ.**
  `HouseholderStimulus` (`Probe/Situations/WorldSituationNoise.cs:42-70`) **уже несёт `PointX`,
  `PointZ` и `Loudness`**. Поиск по `PointX` во всём продукте даёт совпадения **только в
  редакторских файлах** — окне наблюдения и снимке JSON. **В боевом коде точку шума не сравнивает
  с местом хозяина никто.**

  То есть это не «построить слой слышимости». **Ингредиенты лежат готовыми много месяцев, и вопрос
  просто ни разу не был задан.**

  **ВОЛНА ИМЕЕТ ВТОРУЮ ПОЛОВИНУ, И ОНА ВАЖНЕЕ ПЕРВОЙ ДЛЯ ВЛАДЕЛЬЦА.** Девятая волна оставила
  **четыре готовых куска, не уехавших в `main`**: ходьба по шести местам, дела с концом, утро,
  руки. Их держит **ровно один красный тест** — тот самый про тихий шум. Починка слуха публикует
  всё разом, и владелец впервые видит в одном запуске полную картину.

  **ПОРЯДОК СМЕРТИ НАПИСАН С УЧЁТОМ ГЛАВНОГО УРОКА ДЕВЯТОЙ ВОЛНЫ.** Там первым на вылет стояло
  единственное, что владелец потрогал бы руками, и это стоило ему целого дня без касаний.
  **Здесь публикация не умирает никогда**, а первой умирает задача про свой шум — та, чья дыра
  сегодня работает в нашу пользу.

  **ОДНА ЗАДАЧА НАРЕЗАНА С ПРАВОМ НЕ ПОНАДОБИТЬСЯ.** `t-walking-stops-deafening-1` стоит
  `blocked` с условием «первая задача назвала, нужна ли эта». Если слышимость решает расстояние,
  «безопасный момент» перестаёт быть воротами доступа сам собой. **Ненадобность объявлена
  нормальным исходом заранее**, чтобы нога не доказывала свою нужность работой.

  **ДОВОД ПРОТИВ ЗАПИСАН И ОН НЕ СЛАБЫЙ.** Волна почти невидима его глазу: тихий шум и сейчас
  работает рядом, изменится только поведение издалека. Честная альтернатива — «сначала опубликовать
  готовое» — **невозможна**, публикацию держит ровно тот красный. Отсутствие альтернативы и есть
  довод за.

evidence: |
  **ВСЕ ПЯТЬ ЗАМЕРОВ ЦЕЛИ СНЯТЫ ЭТОЙ НАРЕЗКОЙ ПЕРВОЙ РУКОЙ** на `main` = `064ab568`, ни одно число
  не взято из улик — вчерашняя ревизия показала, что 22 улики из 38 протухли:

  - `WorldSituationNoise.cs` — **218 строк**, `HouseholderStimulus` с `PointX`, `PointZ`,
    `Loudness`; слово «расстояние» в файле не встречается ни разу (единственное совпадение —
    `magnitude` в комментарии про настроечные числа);
  - `git grep PointX` по продукту — совпадения только в `Editor/HouseholderObservationWindow.cs`
    и `Editor/HouseholderObservationSnapshotJson.cs`;
  - `GameRulesSettings.asset` — полосы `_cargoRustleForce: 3`, `_cargoKnockForce: 15`,
    `_cargoHeavyNoiseForce: 35`;
  - `HouseholderReaction.cs` — **ровно две** реакции с `RequiresSafeInterruption = true`;
  - `HandlersRecipeTests.cs:214` — красный `EitherMomentAnswersTheFirstNoiseAndTheSecondStillTakesHim`;
  - `git merge-tree --write-tree slot/win-u4 slot/win-u2` → **`exit 1`**, конфликт на профиле;
    состав трёх веток сверен числами.

state_changes: |
  СОЗДАНО (1 ставка + 4 задачи):
  - `bet-g-5a7c-wave-10` → `active`, десять блоков: слово владельца, цель с пятью замерами, замах
    с порядком смерти, две полосы, девять вырезов поимённо, рискованное допущение, пять порогов
    смерти, шесть линз, прогноз, довод против;
  - `t-noise-reaches-by-distance-1` → `open`, полоса `слух`, голова волны;
  - `t-walking-stops-deafening-1` → `blocked`, ждёт ответа первой задачи;
  - `t-a-fact-knows-who-made-it-1` → `open`, первой умирает при нехватке аппетита;
  - `t-everything-lands-1` → `blocked`, полоса `публикация`, ждёт погасшего красного.

  БЕЗ ИЗМЕНЕНИЙ:
  - `bet-g-5a7c-wave-9` остаётся `active` с двумя живыми задачами: руки ждут его глаза, утро —
    кандидат. **Волны идут внахлёст намеренно:** девятая ждёт не работы, а его.

captures: |
  - **СТОРОЖ, ПОСТАВЛЕННЫЙ ВЧЕРА ПРОТИВ ЗАБЫВАНИЯ, ОТРАБОТАЛ СЕГОДНЯ.** Он сам попросил сделать
    улику обязательной к прочтению при нарезке; правило это потребовало, и решение принято вслух.
    **Первый случай за направление, когда процессное правило поймало то, ради чего писалось.**
  - **«ПОСТРОИТЬ СЛОЙ» ОКАЗАЛОСЬ «ЗАДАТЬ ВОПРОС».** Данные лежали готовыми, и никто не спросил.
    **Правило-кандидат: перед тем как объявлять слой отсутствующим, искать его ингредиенты — они
    могут уже лежать в структуре, просто не читаться.**
  - **ЗАДАЧА НАРЕЗАНА С ЯВНЫМ ПРАВОМ ОКАЗАТЬСЯ НЕНУЖНОЙ.** Обычно нога доказывает свою нужность
    работой. Здесь ненадобность объявлена нормальным исходом заранее, и это дешевле, чем узнать
    об этом после восьми правок.

decisions_needed: |
  **ОДИН: ЗАМАХ.** Предложено четыре задачи, его слова на число нет. Волна открыта, но замах ждёт
  его подтверждения — так же, как девятая ждала «Открывай как есть, семь задач».

play_check: |
  1. OPEN — его слово «открывай волну про слух».
  2. SHAPE — прочитаны обе карточки, которых требует правило, и решение принято вслух; все пять
     замеров цели сняты первой рукой на байтах, а не взяты из улик; порядок смерти написан против
     ошибки девятой волны; вырезы названы поимённо, чтобы нога не расширяла предмет молча.
  3. (owner) — слово на открытие получено; **слово на замах не получено и запрошено**.
  4. CLOSE — RESULT, применение, коммит; экран волны уходит владельцу.

log: |
  десятая волна открыта его словом: слух решается расстоянием — и главный замер нарезки показал,
  что строить нечего, шум уже несёт точку и громкость, а сравнить их с местом хозяина в боевом коде
  не догадался никто; вторая половина волны публикует четыре готовых куска девятой, которые держит
  один красный тест

next: |
  Экран волны владельцу: цель, две полосы, четыре задачи, пять порогов смерти, девять вырезов и
  довод против. **Ждём его слова на замах.**

  Девятая волна остаётся открытой: руки ждут его глаза, утро — кандидат.
