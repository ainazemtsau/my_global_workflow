# RESULT — s-work-g-5a7c-sight-scope-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-sight-thing-1, полоса `починка`
track: починка
date: 2026-08-31
outcome_kind: scope — СТОП исполнителя разобран: одна из двух причин была его же строкой приёмки, вторая разрешена как расширение

owner_approved: |
  Отдельного слова не требуется и не выдумывается. Расширение остаётся ВНУТРИ файла, уже
  выданного ноге в `boundaries` (`Network/HouseholderSightSource.cs`), и служит строке приёмки 2,
  которую владелец уже одобрил при нарезке волны 2026-08-30.

  Ни одна строка `done_when` не изменена и ни одна не добавлена.

outcome: |
  **СТОП СРАБОТАЛ РОВНО ТАК, КАК НАПИСАН, И ЭТО ГЛАВНАЯ ЦЕННОСТЬ НОГИ.** Наряд говорил: «после
  правки число осталось нулём → СТОП и вопрос домой: значит причина не одна». Исполнитель
  передвинул прицел с `observer.Y` на `state.Y` в обоих местах, число не сдвинулось — `0 из 14` до,
  `0 из 14` после — и он остановился вместо того, чтобы копать дальше молча. **Порог сработал
  на второй причине, а не на десятой.**

  **ПРИЧИН ДЕЙСТВИТЕЛЬНО ДВЕ, И ОДНА ИЗ НИХ — ЕГО ЖЕ СТРОКА ПРИЁМКИ, А НЕ РАСШИРЕНИЕ.**

  1. **Самоперекрытие.** После правки точки тела цели встали В саму вещь: `RayIsClear`
     (`HouseholderSightSource.cs:298-320`) отказывает на ЛЮБОМ коллайдере, а запас на конце луча —
     `RayEndEpsilon = 0.001f`, один миллиметр. Луч входит в коллайдер самой вещи и падает.
     Улика исполнителя «`Tin can: 0 of 3 body points clear`» — ровно этот случай, и порог
     `requiredVisible = sampleCount / 2 + 1` его подтверждает.
     **`done_when` 3 дословно: «ЛУЧ НЕ ПЕРЕКРЫВАЕТСЯ САМОЙ ВЕЩЬЮ И ОПОРОЙ ПОД НЕЙ».** Разрешения
     не требовалось — требовалось доделать.

  2. **`OnTopOfIt` судит по планарному расстоянию и игнорирует высоту.**
     `HouseholderSightSource.cs:173-179`: если `toTargetX² + toTargetZ² <= 1e-12`, исход
     `OnTopOfIt` ставится **без единого пущенного луча**. Значит вещь, стоящая прямо НАД ним —
     кружка на столе, у которого он стоит, — невидима по построению. Ветку добавила предыдущая
     нога (прибор), и она была безвредна ровно до тех пор, пока прицел смотрел в пол: совпадение
     по X/Z тогда значило «он стоит на этом месте», а после правки стало значить «вещь над ним».
     **Это расширение, и оно разрешено:** файл уже в его поверхности, без него не закроется
     строка 2.

  **СПЕЦИАЛЬНЫЙ МЕХАНИЗМ ПОД «НАБЛЮДАТЕЛЬ И ВЕЩЬ СОВПАЛИ ПО X/Z» ЗАПРЕЩЁН, И ЭТО НАЗВАНО ПРЯМО.**
  Его вопрос звучал именно так, и ответ на него — нет. Случай `Mug` **круговой**: хозяин стоит на
  её X/Z **потому что там замер**, а замер потому, что не мог её увидеть. При работающем зрении он
  туда не придёт. Строить машинерию под симптом собственной поломки значит закрепить поломку в коде.

  **ТРИ ЗАМЕРА СДЕЛАНЫ ЗА НЕГО, ЧТОБЫ ОН НЕ ТРАТИЛ НА НИХ НОГУ:** `CargoState` габаритов не несёт
  (значит «целиться в поверхность» потребует новых данных через шов — путь дорогой, и он назван
  дорогим); механизма исключения коллайдера сегодня НЕТ (`occluderRoot` уходит только в положение
  глаза через `TryGetFrame`, в луч не попадает); наращивание `RayEndEpsilon` магическим числом
  прямо не рекомендовано — оно пропустит настоящие тонкие преграды.

  **НОВЫЙ СТОП ЖЁСТЧЕ ПРЕЖНЕГО.** Если после обеих причин число всё ещё ноль — это третья причина,
  и тогда срабатывает **порог смерти волны**, а не порог задачи: значит слой убеждений сломан
  глубже, чем адресация, и волну надо пересматривать, а не доделывать.

  **ВОЗВРАТ ТЕПЕРЬ ТРЕБУЕТ ЧЕТЫРНАДЦАТЬ СТРОК, А НЕ ИТОГ.** «0 из 14» скрывает, какая причина
  бьёт по какой вещи; полный отчёт `why` до и после плюс разбор по причинам это показывает.
  Ровно этой слепоты стоил сегодняшний круг.

evidence: |
  Перемерено направлением в рабочем дереве исполнителя `C:\projects\Unity\GasCoopGame_win-u1`,
  на его незакоммиченной правке, а не по его рассказу:

  - `HouseholderSightSource.cs:173-179` — ранний отказ по `planarDistanceSquared <= Tiny`,
    `Tiny = 1e-12f`, высота в условие не входит;
  - `HouseholderSightSource.cs:298-320` — `RayIsClear` возвращает `false` на первом же коллайдере;
    `RayEndEpsilon = 0.001f`;
  - `HouseholderSightSource.cs:211` — `requiredVisible = sampleCount / 2 + 1`;
  - `HouseholderSightSource.cs:285-296` — `TryGetFrame(out Transform occluderRoot)`: `occluderRoot`
    используется для положения глаза и в `Raycast` не передаётся;
  - `Probe/Cargo/CargoState.cs` — поля X/Y/Z, поворот, дом, целостность; габаритов нет;
  - правка исполнителя: две строки в `Network/NetworkHouseholder.cs`, `observer.Y` → `state.Y`,
    плюс новый файл тестов; не закоммичена.

  Из его отчёта, как улика: `HE SEES 0 OF 14 THINGS.` до и после;
  `Mug — HE IS STANDING ON IT`; `Tin can — 0 of 3 body points clear`; лиза WIN-U1 сохранена.

state_changes: |
  ДОПИСАНО (1 документ наряда):
  - `work/2026-08-31-call-sight-thing-1.md` — раздел «ДОПОЛНЕНИЕ 2026-08-31: две причины вместо
    одной». Прежний текст не тронут: он остаётся уликой того, что нога знала на выдаче.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-sight-thing-1` остаётся `active`, `c-exec-g-5a7c-sight-thing-1-001` остаётся `ready`.
    Работа не закончена, и закрывать нечего.
  - Порог правок прежний: больше пяти мест — СТОП. Две строки потрачены, три остались.

captures: |
  - **Разрешение записано в ФАЙЛ, а не осталось в чате.** Это тот же класс, что стоил сегодня
    половины дня трижды: устная договорённость не переживает переход в другой слот и другую
    сессию. Дополнение лежит рядом с нарядом, журнал карточки на него указывает.
  - **«Расширить наряд?» оказалось наполовину неверным вопросом.** Половина спрошенного уже
    стояла в его `done_when`. Стоит помнить при следующем СТОПе: сперва перечитать приёмку, потом
    решать про расширение.

decisions_needed: |
  Ни одного. Владельцу тут нечего выбирать: обе причины внутри уже выданной поверхности.

play_check: |
  1. OPEN — возвращённый СТОП по идущей ноге; наряд и приёмка перечитаны до ответа.
  2. WORK — обе причины перемерены первой рукой в дереве исполнителя; отчёт читался как улика.
  3. (owner) — не требуется: расширение внутри выданной поверхности, служит уже одобренной строке.
  4. CLOSE — RESULT, применение, коммит; нога зрения продолжается.

log: |
  СТОП зрения разобран: причин две, и первая — не расширение, а невыполненная строка 3 про
  самоперекрытие луча; вторая разрешена — OnTopOfIt отказывает по планарному расстоянию и не видит
  вещь над головой; механизм под совпадение X/Z запрещён как круговой

next: |
  Ответ уходит в WIN-U1. Идут четыре ноги: зрение U1, доработка гейта U2, «почему» U3,
  безопасное закрытие U4.

  Ждут зрения три задачи: `t-morning-content-1`, `t-carry-back-1`, `t-bench-by-fact-1`.
