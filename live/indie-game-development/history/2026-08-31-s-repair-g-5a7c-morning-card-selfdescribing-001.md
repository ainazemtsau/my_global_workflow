# RESULT — s-repair-g-5a7c-morning-card-selfdescribing-001

direction: indie-game-development
play: repair
node/task: g-5a7c / t-morning-content-1, полоса `утро`
track: утро
date: 2026-08-31
outcome_kind: repair — карточка утра не говорила, где лежит готовая работа и что её держит; исправлено, чтобы свежий чат нашёл это без чужой памяти

owner_approved: |
  Не требуется: приведение записи в соответствие с фактом. Ни одна строка `done_when` не изменена.

  Повод — его прямой вопрос о том, продолжать ли в этом чате или начинать следующую волну в новом.
  Ответ на него зависит от того, самоописывается ли состояние.

outcome: |
  **КАРТОЧКА НЕ ГОВОРИЛА НИ ГДЕ ЛЕЖИТ РАБОТА, НИ ЧТО ЕЁ ДЕРЖИТ.** Она стояла `active` с пустым
  `unblock_when`, хотя нога закончена, слот отпущен, а публикация снята решением. Свежий чат,
  открывший эту карточку, увидел бы задачу «в работе» без исполнителя и без причины — и был бы
  вынужден восстанавливать историю по отчётам.

  **ЭТО ТОТ САМЫЙ КЛАСС, ПРОТИВ КОТОРОГО НАПРАВЛЕНИЕ РАБОТАЛО ВЕСЬ ДЕНЬ.** Требование помнить
  устройство — дефект системы, а не свойство человека. Сегодня ревизия показала, что 22 улики из 38
  протухли; карточка, не описывающая себя, — та же болезнь на другом органе.

  Исправлено: `blocked` с условием, которое называет и держатель, и место работы.

evidence: |
  До правки: `status: active`, поля `unblock_when` в шапке нет вовсе — прочитано направлением в
  байтах карточки.

  Факт на момент правки: `slot/win-u4` = `abfbccfe`, лиза отпущена, слот `AVAILABLE`, дерево
  чистое; публикация снята решением направления после того, как гигиена запретила пометку.

state_changes: |
  ИСПРАВЛЕНО (1 карточка):
  - `t-morning-content-1` → `blocked`, `unblock_when: красный тест зеленеет починкой слышимости;
    работа готова и лежит кандидатом на slot/win-u4`.

captures: |
  - **СОСТОЯНИЕ, КОТОРОЕ ЖИВЁТ ТОЛЬКО В ОТЧЁТАХ, НЕ ЖИВЁТ.** Отчёты в `history/` полны и точны, но
    карточка — то, что читают первым. Правило-кандидат: **если нога закончилась не публикацией, её
    карточка обязана назвать, ГДЕ лежит результат и ЧТО его держит** — одной строкой, в шапке.

decisions_needed: |
  Ни одного.

play_check: |
  1. Назвать противоречие — карточка «в работе» при законченной ноге и отпущенном слоте.
  2. Восстановить новейшим-первым — состояние слота и ветки прочитано напрямую.
  3. Предложить исправленное состояние — `blocked` с самоописывающим условием.
  4. Подтверждение (owner) — не требуется.
  5. Дыра в OS → кандидат в правило записан, отдельной заявки не открывается.

log: |
  карточка утра приведена в соответствие: она стояла «в работе» при законченной ноге и отпущенном
  слоте, и не говорила ни где лежит готовая работа, ни что её держит — теперь говорит обоё одной
  строкой, и свежий чат найдёт это без чужой памяти

next: |
  Волна: восемь задач из девяти закрыто. Живых три: утро (готово, ждёт слышимости), полигон
  (идёт в WIN-U1), перенос вещи на место (не выдан).
