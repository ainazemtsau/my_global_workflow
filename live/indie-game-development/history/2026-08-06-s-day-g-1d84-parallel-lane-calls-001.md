# RESULT — s-day-g-1d84-parallel-lane-calls-001

play: day · direction: indie-game-development · node: g-1d84 · дата: 2026-08-06

## outcome

Владелец попросил параллельные запуски и сказал «сейчас надо запускать». Выпущены ДВА наряда на две
разные полосы — больше волна сегодня законно не даёт. Забанкована и поднята на сервер работа по пробе
наборов, которую он велел сохранить, но не продолжать.

**Его посылка «сейчас все слоты свободны» неверна и исправлена измерением:** свободны два из четырёх.

## evidence

### Слоты — перемерено 2026-08-06 первой рукой

`C:/projects/Unity/GasCoopGame_slot-state/gascoop-slot-state.v1.json` плюс состояние каждого каталога:

| слот | реестр | дерево | HEAD | против базы |
|---|---|---|---|---|
| `WIN-U1` | `AVAILABLE`, `lease: none` | чисто | `032ebb17` | 19 позади |
| `WIN-U2` | `CLAIMED`, `c-exec-g-1d84-cargo-tight-spot-001:BUILD` | чисто | `2eee9719` | 17 позади |
| `WIN-U3` | `CLAIMED`, `c-exec-g-1d84-free-kits-probe-001:BUILD` | чисто | `f980dce9` | 122 вперёд / 22 позади |
| `WIN-U4` | `AVAILABLE`, `lease: none` | чисто | `d60c2f04` | **ровно на базе** |

`origin/main` = `origin/dev` = `d60c2f04`. `WIN-U2` держит аренду под ВЫРЕЗАННУЮ задачу и не построил
ничего (дерево чисто, HEAD там же, где был при выдаче). `WIN-U3` держит аренду под отложенную полосу.

### Сохранение работы по пробе — исполнено, не отложено

Владелец: «пусть они, наверное, сохраняются… может, мы можем как-то закрыть, чтобы данные
сохранились», и при этом «я не хочу сейчас заниматься… дом обустраивать». Его довод записан целиком,
потому что он верный: «AI как бы как мы можем расставлять, когда мы даже не представляем, что, где и
как будет выглядеть».

Сделано ровно сохранение, без единой строки продолжения:

1. Шесть незакоммиченных файлов (`AUTHORING.md` + пять заготовок-обёрток) сложены коммитом
   `f980dce9` в `GasCoopGame_win-u3`. Дерево слота стало чистым.
2. `e9f23cef` (вчерашние 50 файлов) и `f980dce9` подняты на сервер новой веткой
   `refs/heads/probe/free-kits-2026-08-06`. Ветка ничего не трогает: ни `main`, ни `slot/win-u3`, ни
   аренду.
3. Проверено ПОСЛЕ пуша: `git branch -r --contains e9f23cef` и `... --contains f980dce9` оба дают
   `origin/probe/free-kits-2026-08-06`.

**Поправка к вчерашней оценке риска, сказанной владельцу.** Было сказано «удалишь каталог — 50 файлов
исчезнут». Это сильнее, чем есть: `GasCoopGame_win-u3` — СВЯЗАННЫЙ рабочий каталог (`.git` в нём файл,
не каталог), объекты лежат в общем хранилище основного репозитория, и удаление папки коммит бы не
убило. Настоящий риск был другой и конкретный: `unblock_when` наряда на пробу предписывает перевести
`WIN-U3` ровно на `main`, а ветка ушла на 121 коммит вперёд — то есть перевести её можно только силой,
и это осиротило бы `e9f23cef`. Плюс шесть незакоммиченных файлов, которые терялись от любого
`checkout`. Обе дыры закрыты.

### Почему параллельно ровно ДВЕ, а не больше

Правило `os/plays/work.md`: «each lane has ≤1 ordinary root». Разбор живых задач против полос:

- **дом** — открыты `t-house-4` (дом как данные) и `t-house-6` (точка доставки), плюс вырезанная
  владельцем `t-house-2` и уходящая из волны `t-house-3`. Полоса даёт ОДИН корень: взят `t-house-4`,
  названный владельцем приоритетным.
- **хозяин** — `t-host-3` разблокирована сегодня его решением о форме файла. Остальные восемь задач
  полосы ждут либо `t-house-4`, либо закрытия `t-host-3`.
- **сцена** — встала: `t-scene-1`, `t-scene-2`, `t-scene-3` закрыты, остался только exe на двадцать
  третьем месте.
- **игрок** — `t-player-1` закрыта, `t-player-2` (третье лицо) уходит из волны решением о урезании.
- **внешний-вид** — отложена им же сегодня.

Третьего законного корня сегодня нет. Он появится ПОСЛЕ перенарезки
(`i-wave-recut-to-core-owner-decided-001`), которая переформирует полосы — то есть у перенарезки есть
прямая выгода в скорости, а не только в порядке.

### Наряды — база и пути

Оба объявляют базу `d60c2f04228bdfa161d6e122c56a2c5ccfec89f3`, спрошенную у сервера. Все шестнадцать
названных путей проверены поштучно `git cat-file -e d60c2f04:<путь>` — существуют все. Это прямая
профилактика класса `i-call-named-path-absent-on-declared-basis-004` (четыре бунса).

Измерения, положенные в наряды и добытые этой ногой:

- `Assets/TunnelCrew/Core/TunnelCrew.Core.asmdef` = `"references": []`, `noEngineReferences: true` —
  слой правил не ссылается НИ НА ЧТО. Отсюда два следствия, разведённые по нарядам: описание дома
  обязано лечь в правила, не затащив движок; JSON профиля читать в правилах НЕЛЬЗЯ.
- `Assets/TunnelCrew/World/HouseWall.cs` — коллайдер стены служит одновременно физической формой и
  данными о проходимости. Это ровно то, из чего `t-house-4` велит выйти.
- `Assets/TunnelCrew/Core/Householder/Householder.cs` — распорядок и способности сегодня это ВОСЕМЬ
  перечислений C#; чтения файлов у хозяина нет вовсе.
- `Assets/TunnelCrew/Core/Householder/AuthoritativeHouseholder.cs:42` — `IHouseholderWorldPort`,
  готовый образец шва «объявлено в правилах, реализовано сценой». Обоим нарядам предписано повторить
  его, а не изобретать свой.

### Разведение параллельных нарядов — названо в обоих

Словарь дома (имена комнат, мест, проходов) принадлежит `t-house-4`. Наряд на профили ссылается на
места непрозрачными именами, имён не выдумывает, и при невозможности продолжить обязан чекпойнтить, а
не решать. Обратная граница: наряд на дом не трогает правила хозяина.

### Два дважды непоехавших решения владельца — прицеплены

Откат Unity Cloud (`m_Enabled: 1` → `0`) и терминальная квитанция мёртвому корню
`c-exec-grid-v1-g01-document-authority-001` уехали пунктами в наряд на дом, с требованием назвать
причину прямым текстом, если снова не поедут. Оба решены им 2026-08-05 и пропущены двумя нарядами
подряд.

## state_changes

1. `NOW.open_calls`: заведены `c-exec-g-1d84-house-as-data-001` (`t-house-4`, полоса дом, `ready`,
   слот `WIN-U4`) и `c-exec-g-1d84-householder-profile-file-001` (`t-host-3`, полоса хозяин, `ready`,
   слот `WIN-U1`).
2. Созданы два артефакта наряда в `work/` по тем же путям.
3. `NOW.issues`: `i-probe-commit-exists-only-locally-001` СНЯТА — её `review_when` выполнен делом,
   оба коммита на сервере, проверено после пуша.
4. `NOW.md` шапка `updated:` — на эту ногу.
5. `LOG.md` — одна строка.

Не тронуто: карточка, задачи, вырезы, полосы, `CHARTER.md`, `TREE.md`. Аренды `WIN-U2` и `WIN-U3` не
снимались — это слово владельца.

## captures

- Удар грузом ПО ДРУГОМУ ИГРОКУ — отдельная механика или частный случай «контакт производит шум»?
  Спрошено трижды, не отвечено. Нужен ответ до правки карточки.
- Его фраза «я её [неразборчиво], ожидаем» осталась неразобранной: неясно, про снятый наряд на тесное
  место или про запущенную проверку панели. Спрошено, ответа нет; ни одна аренда из-за этого не
  трогалась.
- `Householder.cs` уже содержит перечисления ПО ВИДАМ реакций (`HouseholderOccupation`,
  `HouseholderNoticedFactName` и ещё шесть). `t-host-5` требует «в коде хозяина нет ветвления по
  конкретным видам реакций» — возможное расхождение, проверяется своей задачей, не этой ногой.

## decisions_needed

Нет.

## play_check

- Шаг 1: `NOW`, `LOG`, продукт, реестр слотов и состояние всех четырёх каталогов перечитаны из Git
  этой ногой; посылка владельца о свободных слотах проверена и исправлена измерением.
- Шаг 5: разрешение — его слова «Дай мне тогда задачи, которые можно запустить параллельно… Там текст
  для них дай… Сейчас надо запускать». Сохранено ровно это: два `ready` наряда внутри активной ставки
  (`day` это прямо разрешено) плюс снятие записи, которую собственная улика ноги закрыла.
- Границы `day`: карточка, задачи, вырезы и полосы не правились; `CHARTER`/`TREE` не тронуты;
  `knowledge/` не писалась; чужой стратегической правки не выпущено.
- WIP: после запуска работают две полосы из трёх разрешённых.

## log

g-1d84: выпущены два параллельных наряда на разные полосы — `t-house-4` (дом как данные) в `WIN-U4` и
`t-host-3` (профили хозяина текстом) в `WIN-U1`; база `d60c2f04`, все 16 путей проверены поштучно,
разведение словаря дома названо в обоих. Посылка владельца «все слоты свободны» исправлена
измерением: свободны два, `WIN-U2` держит аренду под вырезанную задачу и не построил ничего, `WIN-U3`
— под отложенную полосу. Работа по пробе наборов сохранена по его слову и не продолжена: 6
незакоммиченных файлов забанкованы `f980dce9`, обе точки подняты на сервер веткой
`probe/free-kits-2026-08-06`, проверено после пуша — запись `i-probe-commit-exists-only-locally-001`
снята делом. Вчерашняя оценка риска поправлена: связанный рабочий каталог коммит бы не убил, убил бы
силовой перевод ветки. Параллельно больше двух корней волна сегодня не даёт — третий появится только
после перенарезки.

## next

Владельцу: оба наряда вставляются в отдельные чаты и запускаются. Открыто и ждёт его слова —
освободить `WIN-U2` из-под вырезанной задачи, ответить про удар по другому игроку, затем `map` на
карточку и `shape` на перенарезку.

END_OF_FILE: live/indie-game-development/history/2026-08-06-s-day-g-1d84-parallel-lane-calls-001.md
