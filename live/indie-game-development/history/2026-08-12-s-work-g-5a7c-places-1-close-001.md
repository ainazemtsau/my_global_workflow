# RESULT — s-work-g-5a7c-places-1-close-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-places-1
date: 2026-08-12
call: c-work-g-5a7c-places-1-001
close: light — because все семь строк `done_when` этой задачи суть факты, перевыведенные ЭТОЙ ногой
  из закоммиченных байтов и из прогона, который она выполнила сама: равенство деревьев отката,
  состав диффа, отсутствие правок в игровом профиле, сценах и префабах, нулевой дифф каталога
  реакций, имена и содержание пяти новых headless-тестов и их зелёный прогон. Ни одна строка не
  требует суждения, которого никто не выносил: шов невидим глазом, владелец по нему вердикта не
  должен, и наряд прямо записал «manual-acceptance: none owed». Свежий чат опровержения не
  открывался.

owner_approved: |
  «выполнена задача c-exec-g-5a7c-places-1-001 закрывай» — 2026-08-12.

  Его слово запустило закрытие, но НЕ является его доказательством: по правилу направления
  handback исполнителя и прозаическое «сделано» — это улика, а не Direction-OS close. Всё, что ниже,
  перемерено этой ногой.

outcome: |
  **`t-places-1` ЗАКРЫТА `done` СВЕТЛЫМ ПУТЁМ** на опубликованной голове
  `4e5858b4b7cf4a6c8c9cad08e3fde4a6915200f3` (`origin/main` = `origin/dev` = `origin/slot/win-u2`).

  Шов сделан СИЛЬНЕЕ, чем требовала карточка. Требовалось, чтобы профиль мог назвать место и хозяин
  запускался. Сделано так, что профиль называет **роль**, а место под неё подставляет дом:
  `IsKnownPlace(string)`, годами возвращавший `false` с комментарием «Deliberately empty», **исчез
  целиком** и заменён на `TryResolvePlaceRole(string role, out string placeName)`, читающий словарь
  из плана дома.

  **Главное поведенческое изменение — инверсия.** Раньше профиль, назвавший любое место, НЕ ЗАПУСКАЛ
  хозяина вовсе: порт хоста оставался нечитаемым и авторитетный Core его не сажал. Теперь
  неизвестная роль просто делает свой шаг пустым, а хозяин живёт дальше. Это ровно то, что было
  главным содержанием задачи.

  **Все семь строк карточки перевыведены здесь.**

  1. **Профиль называет роль, хозяин запускается** — тест `WaitAtResolvedRoleWalksThereAndThenHolds`:
     разрешённая роль не просто принимается, а действительно ведёт его туда и удерживает.
  2. **Словарь из описания дома, а не из загрузчика** — `HouseholderProfileLoader.cs:145-147`
     вызывает `_house.TryFindPlaceByRole(role, out var place)`; сам поиск живёт в
     `Core/House/HousePlan.cs:139`; поле роли у маркера — `World/HousePlaceMarker.cs:30`.
  3. **Неизвестная роль молчит, а не убивает** — `:804-805` не проверяет результат разрешения,
     поэтому шаг остаётся с пустым местом; тест
     `MissingRoleLoadsAndOnlyItsRoutineStepBecomesANoOp`.
  4. **ГЛАВНЫЙ ГЕЙТ ВЗЯТ И ОН ИМЕННО HEADLESS** — тест
     `OneProfileFileUsesTheSameRoleInTwoDifferentHousePlans` в
     `tests/TunnelCrew.Core.Tests/HouseholderPlaceRoleTests.cs`. Требование наряда «проверено в
     редакторе строку не закрывает» исполнено буквально: Unity к работе не привлекался вовсе.
  5. **Кривая строка отвергается названно и с путём** — `MalformedRoleIsRejectedWithItsJsonPath` и
     `BlankRoleStringIsRejectedWithItsJsonPath`; в загрузчике `:794-799` пустая роль даёт
     «a non-empty place role is required» с путём.
  6. **Поведение по умолчанию не изменилось ни на грамм** — доказано ОТСУТСТВИЕМ правок, а не их
     разбором: `git diff --stat f3c40c40 origin/main -- Assets/StreamingAssets/` пуст, то есть
     игровой профиль не тронут; дифф `HouseholderReaction.cs` — **НОЛЬ строк**, и пять реакций
     читаются поимённо: `daily-route`, `suspicious-noise`, `quiet-rustle`, `repeated-heavy-noise`,
     `landing`.
  7. **ОТКАТ ИСПОЛНЕН НАСТОЯЩИЙ И СОШЁЛСЯ ЗНАК В ЗНАК** — четвёртый раз подряд в этом узле:
     дерево `406dcba2` (revert) = `5ec74916b0f1b8db9dd1973c46082c411b6a1b0b` = дерево базы
     `f3c40c40`; дерево `0c21e74d` (reapply) = `6b4a23ed7132881d0b4bed5d170b0e89ad2ec09d` = дерево
     работы `428f1d58`.

  **ГРАНИЦЫ ДЕРЖАЛИСЬ ПОЛНОСТЬЮ.** В диффе `f3c40c40..origin/main` **ноль** файлов `.unity`,
  `.prefab` и `.asset`. Изменённых тестовых файлов ноль: тестовый дифф — ровно одно ДОБАВЛЕНИЕ
  (`A tests/TunnelCrew.Core.Tests/HouseholderPlaceRoleTests.cs`), поэтому «ни одно существующее
  ожидание не переписано» доказано отсутствием правок — самая сильная из доступных форм.

  **ЧТО СТОИЛО ДОРОЖЕ ОЖИДАЕМОГО, и это надо назвать.** Улика
  `i-profile-cannot-name-a-house-place-001` оценивала починку в «~10 строк». Фактически вышло ~700
  строк по восьми файлам продукта плюс 402 строки нового теста. Оценка была занижена, потому что
  считала только загрузчик и не считала ни словарь в плане дома, ни поле у маркера, ни проводку
  через `NetworkHouseholder` и `HouseholderRoutineProfile`. Рост при этом остался плумбингом:
  в `Householder.cs` прибавились ровно параметр `HouseholderPlaceReference RoutinePlace` у входа шага
  и один приватный помощник; ни одного нового члена перечисления, ни одной новой ветки поведения.

  **ДОРОЖКИ РАЗБЛОКИРОВАНЫ.** Шов был единственным, что их держало.

evidence: |
  **ВСЁ ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ, НЕ ВЗЯТО С ОТЧЁТА.**

  - `git ls-remote origin` → `origin/main` = `origin/dev` = `origin/slot/win-u2` = `4e5858b4`.
  - Шесть коммитов сверх базы: `428f1d58` работа, `406dcba2` revert, `0c21e74d` reapply, три
    документных.
  - Деревья: база `f3c40c40` → `5ec74916…`; revert `406dcba2` → `5ec74916…` (**совпало**);
    работа `428f1d58` → `6b4a23ed…`; reapply `0c21e74d` → `6b4a23ed…` (**совпало**).
  - `git diff --stat f3c40c40 origin/main` → 11 файлов, +900 / −55; ни одного `.unity`, `.prefab`,
    `.asset`; `Assets/StreamingAssets/` пуст в диффе.
  - `git diff --name-status … -- tests/` → `A tests/TunnelCrew.Core.Tests/HouseholderPlaceRoleTests.cs`,
    и больше ничего.
  - `git diff … -- Core/Householder/HouseholderReaction.cs | wc -l` → **0**; в файле пять
    `Name = "…"`, прочитаны поимённо.
  - `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release`, прогнано
    ЗДЕСЬ на выгруженном дереве публикации → **219 passed, 0 failed, 0 skipped** против базовых
    **214**, разница ровно пять — по числу новых тестов.
  - `tools/hygiene.ps1` → `hygiene OK`, прогнано здесь.
  - `tools/select-slot.ps1 -Slot WIN-U2` → `head 4e5858b4`, `state: CLEAN`, `lifecycle: AVAILABLE`,
    `lease: none` — слот отпущен.
  - Расписка исполнителя существует и прочитана:
    `docs/results/c-exec-g-5a7c-places-1-001.md` §Расписка — «Получено ровно три строки `done_when`;
    сборка начата с названного базиса `f3c40c40`».
  - Там же §manual-acceptance: «None owed» — что совпадает с маршрутом светлого закрытия, а не
    противоречит ему.

  **ЧТО ОТЧЁТ НАЗВАЛ САМ, И Я ЭТО ПРОВЕРИЛА ОТДЕЛЬНО.** Он честно пишет, что независимая проверка
  нашла дыру до коммита — разрешённые роли сначала игнорировались умением `wait-at-route-point`, —
  и что один промежуточный прогон был `216/217` из-за фикстуры. Проверено: **ни одно существующее
  ожидание не переписано**, изменённых тестовых файлов ноль, а поправленная фикстура живёт внутри
  нового файла. Дыра закрыта тестом `WaitAtResolvedRoleWalksThereAndThenHolds`, который я вижу в
  байтах и который прошёл в моём прогоне.

state_changes: |
  1. `t-places-1`: `active → done`, журнальная строка, перенесена в `cards/closed/`.
  2. `c-exec-g-5a7c-places-1-001`: `ready → done`, журнальная строка, перенесён в `cards/closed/`.
  3. `c-work-g-5a7c-places-1-001`: `waiting → done`, снят `waiting_on`, журнальная строка, перенесён
     в `cards/closed/`.
  4. `i-profile-cannot-name-a-house-place-001`: **ЗАКРЫТА** — `status: done`, добавлен блок
     `## disposition` с перевыведенными фактами и с честной пометкой, что оценка «~10 строк» была
     занижена; перенесена в `cards/closed/`. Это разрешено плеем `work`: улика, которую собственная
     улика ноги закрывает, закрывается со своей диспозицией.
  5. **РАЗБЛОКИРОВАНЫ швом:** `t-rooms-1`, `t-sight-1`, `t-occupations-1`, `t-place-fact-1` —
     `blocked → open`.
  6. `c-exec-g-5a7c-rooms-1-001`: `blocked → ready`, снят `unblock_when`; текст наряда
     `work/c-exec-g-5a7c-rooms-1-001-call.md` обновлён — базис `4e5858b4`, база прогона 219,
     блок «почему заблокирован» заменён на «разблокирован» с именами уже существующих точек
     расширения.
  7. Остаются `blocked` по существу: `t-handlers-1` (ждёт зрения, места-в-факте и предмета),
     `t-lab-2` (показывает то, чего ещё нет), `t-house-asset-1` (нечего переносить без дома),
     `t-loot-1` (столкнётся с идущей `t-thing-1` по грузу и настройкам).
  8. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-places-1-close-001.md`.

captures:
  - |
    ОЦЕНКА УЛИКИ БЫЛА ЗАНИЖЕНА В СЕМЬДЕСЯТ РАЗ: «починка ~10 строк» против фактических ~700 строк
    продукта плюс 402 теста. Причина видна и повторяема: улика считала место, где симптом ВИДЕН
    (`IsKnownPlace` возвращает `false`), и не считала места, куда придётся тянуть данные. Стоит
    помнить при чтении любой оценки «одна строка» в карточке улики.
  - |
    `IsKnownPlace` исчез, а `docs/householder-profile-format.md` обновлён тем же коммитом. Значит
    описание формата профиля живо и следует за кодом — это единственный документ продукта, про
    который сегодня доказано, что он не устарел.
  - |
    Слот WIN-U2 отпущен корректно, а `mcp-endpoint: unrecorded` в файле состояния слотов по-прежнему
    врёт про мир. Это третий раз за день, когда это поле пришлось обходить чтением живого моста.
    Класс уже дважды записан; следующий повтор — MAINTENANCE на файл состояния, а не на ногу.

decisions_needed: []

play_check:
  - 1 recite — done. Цель и семь строк `done_when` пересказаны; обязательство ставки названо: это общая задача-шов, разблокирующая две дорожки.
  - 2 owner inputs — не требуется, и это не пропуск: шов невидим глазом, вердикта владельца по нему не существует, и наряд заранее записал «manual-acceptance: none owed».
  - 3 do the work — done в прошлой ноге (выдача); эта нога принимает возврат.
  - 4 self-check — done. Каждая из семи строк карточки перевыведена ЗДЕСЬ из байтов и прогона, а не пересказана с отчёта; расхождений с отчётом не найдено, а два его собственных признания (дыра до коммита и промежуточный 216/217) проверены отдельно и оказались безвредны.
  - 5 close — done, светлым путём. Основание записано в поле `close` дословно и по тесту плея, а не по бюджету и не по нетерпению.
  - G5 — выполнен через светлое закрытие. Свежий чат опровержения не открывался законно: ни одна строка не требует суждения, которого никто не выносил. Это маршрут закрытия ЗАДАЧИ; ставка по-прежнему закрывается через `review`.
  - G7 — done. Ни одного игрового решения не принято.
  - G9 — не применяется: одобрения владельцем содержания здесь не требовалось.
  - G10 — done. RESULT финальный.

log: t-places-1 закрыта СВЕТЛЫМ путём на опубликованной голове 4e5858b4, и все семь строк перевыведены этой ногой из байтов и собственного прогона, а не приняты с отчёта: откат исполнен настоящий и СОШЁЛСЯ ЗНАК В ЗНАК четвёртый раз подряд — дерево revert 5ec74916 равно дереву базы f3c40c40, дерево reapply равно дереву работы; 219/219 прогнаны здесь против базовых 214, разница ровно пять по числу новых тестов; шов сделан СИЛЬНЕЕ карточки — профиль называет не место, а РОЛЬ, IsKnownPlace с комментарием «Deliberately empty» исчез целиком и заменён на TryResolvePlaceRole со словарём из HousePlan, и главное поведенческое изменение это ИНВЕРСИЯ: неизвестная роль больше не убивает хозяина молча, а делает свой шаг пустым; главный гейт взят именно headless, как требовал наряд, и Unity к работе не привлекался вовсе; границы держались полностью — ноль сцен, префабов и ассетов в диффе, игровой профиль не тронут, дифф каталога реакций НОЛЬ строк и пять реакций целы поимённо, а «ни одно существующее ожидание не переписано» доказано отсутствием правок: тестовый дифф это ровно одно добавление файла; названо и то, что стоило дороже ожидаемого — оценка улики «~10 строк» оказалась заниженной против фактических ~700 строк по восьми файлам, потому что считала место, где симптом виден, и не считала места, куда тянуть данные; улика i-profile-cannot-name-a-house-place-001 закрыта со своей диспозицией, четыре задачи разблокированы, наряд на дом разблокирован и его базис обновлён

next: |
  **Разблокировано и готово к выдаче:** `t-rooms-1` (наряд `c-exec-g-5a7c-rooms-1-001` уже написан,
  переведён в `ready`, базис и база прогона обновлены — слот WIN-U2 свободен и стоит на `4e5858b4`),
  а также `t-sight-1`, `t-occupations-1`, `t-place-fact-1` — на них нарядов ещё нет.

  **Идут:** `t-body-1` в WIN-U1, `t-thing-1` в WIN-U3.

  Порог смерти 3 считает ноги: на общую задачу и первые задачи всех трёх дорожек положено не больше
  двенадцати, иначе первой на паузу встаёт дорожка `body`, следом `t-loot-1`.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-places-1-close-001.md
