# RESULT — s-work-g-5a7c-response-bound-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-a-fact-knows-who-made-it-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — нога опровергла посылку собственного наряда и посылку записки направления; предмет разделён надвое, наряд переиздан на верной посылке, самошум сознательно оставлен нечиненым

close: light — because несущее решение перевыведено направлением первой рукой на байтах: у активного ответа единственный временной механизм — `HoldRemainingSeconds`, и `AcknowledgeOnArrival` его не получает вовсе, то есть жизнь факта не спрашивается нигде. Числа живого замера (TTL 8 с, переживание 52,17 с, расстояние 0.333 м) читались как улика исполнителя, и вывод подпёрт независимым чтением механизма. Ничего не закрыто как сделанное. G5-чат не открывался.

owner_approved: |
  Отдельного слова не требовалось и не запрашивалось: выбор между двумя инженерными маршрутами —
  не его предмет. Владельцу сообщено фактом, включая **видимое им последствие**: хозяин перестанет
  замирать, но по-прежнему будет уходить разбираться с шумом, который произвёл сам.

  Замах волны не менялся.

outcome: |
  **НОГА ОПРОВЕРГЛА ПОСЫЛКУ СВОЕГО ЖЕ НАРЯДА, И НАПРАВЛЕНИЕ ЭТО ПРИЗНАЁТ.** Наряд утверждал, что
  `SourceId` — готовый ингредиент и задача сводится к тому, чтобы его прочитать. **Замер показал
  обратное:** это идентификатор **предмета**, а не действующего лица. Мышь `4`, уронив груз `7`,
  даёт `SourceId == 7` — груз, а не мышь. **Различить «я сам поставил» и «мышь уронила ту же вещь в
  той же точке» по нему нельзя.**

  **ОБОБЩЕНИЕ НАПРАВЛЕНИЯ ОКАЗАЛОСЬ ДОГАДКОЙ, ВЫДАННОЙ ЗА ЗАМЕР.** Два подряд случая — точка шума и
  громкость — действительно лежали готовыми. **Третий был обобщён без проверки** и записан в цель
  волны и в два наряда как факт. Поймала это нога, а не направление.

  **ЗАПИСКА ПРО `FurnitureInTheWay` ТОЖЕ ПЕРЕОЦЕНИЛА СВОЮ УЛИКУ, И НОГА ПРАВИЛЬНО ЕЁ НЕ ПРИНЯЛА.**
  Записка говорила «точка недостижима, потому что вещь легла внутрь следа мебели» как о доказанном.
  Наблюдение соседнего слота реально, но **расстояние 0.333 м ему скорее противоречит** — хозяин
  стоял вплотную. Нога отказалась объявлять геометрию доказанной, потеряв readback `Path.Refusal`
  при выходе из Play. **Правильный отказ; направление сделало из наблюдения вывод, которого оно не
  несло.**

  **НО ГЛАВНОЕ ЧИСЛО ЗАМЕРА РЕШАЕТ ВСЁ, И ГЕОМЕТРИЯ ЕМУ НЕ НУЖНА.**

  > **TTL факта `landing` — 8 секунд. Активная реакция пережила свой факт минимум на 52,17 с.**

  Причина, поднявшая хозяина, умерла через восемь секунд; он отвечал на неё ещё минуту.
  **Достижима точка или нет — вторично: ответ, переживший свой повод, неправ в любом случае.**
  Перевыверено направлением: единственный временной механизм активного ответа —
  `HoldRemainingSeconds`, и `AcknowledgeOnArrival` **его не получает вовсе**. Жизнь факта активный
  ответ не спрашивает нигде.

  **ПРЕДМЕТ РАЗДЕЛЁН НАДВОЕ, И ЭТО ГЛАВНОЕ РЕШЕНИЕ НОГИ.**
  Нога предложила два маршрута: открыть границу к файлам WIN-U2 ради честного владения
  происшествием, или «двухфайловый failsafe отказанного пути», оставив самошум нерешённым.
  **Взят второй — но переименован и переоснован.** «Failsafe» — это заплатка на симптом; здесь есть
  правило: **у каждого активного ответа есть граница; у ответов с удержанием она есть, у «признать
  по прибытии» её нет, и его естественная граница — жизнь его факта.** Число уже авторское, уже
  настраиваемое, уже равно восьми секундам. **Новый регулятор объявлен откатом** — в том же файле
  прямым текстом написано, почему: «one more number to tune is one more thing that can be wrong
  without anybody noticing».

  **САМОШУМ ОСТАВЛЕН НЕЧИНЕНЫМ СОЗНАТЕЛЬНО, И ВЛАДЕЛЬЦУ ЭТО СКАЗАНО.** Он увидит, как хозяин уходит
  разбираться с шумом, который произвёл сам, — **и возвращается, а не замирает**. Честная починка
  требует одноразовой причинности происшествия и проходит через границу WIN-U2; открывать её ради
  задачи, **переставшей быть блокером ровно в тот момент, когда исчезло замирание**, значит
  столкнуть два слота без нужды.

evidence: |
  **ПЕРЕВЫВЕДЕНО НАПРАВЛЕНИЕМ** (`slot/win-u2` = `b4b010c1`, тот же слитый состав):

  - `Householder.cs:914-919` — `AcknowledgeOnArrival` ставит `responseCompleted = true`
    **только внутри `if (reachedTarget)`**; все прочие виды ответа получают `holdDurationSeconds`;
  - разбор ветки удержания (`Householder.cs:855-890`) — единственный временной механизм активного
    ответа — `HoldRemainingSeconds`; **обращения к жизни факта нет ни одного**;
  - `HouseholderWalkTuning.cs:219,242` — `HouseholderFactLifetime(Landing, …)`: срок жизни факта
    существует как авторское настраиваемое число.

  **Улика исполнителя, подлежащая проверке следующей ногой:** заблокированное дерево
  `023377ab`, сообщение «Merge main into morning tree (blocked: householder freeze)»,
  не опубликовано, дерево чистое; три устаревшие строки карты исправлены; два снимка за 60.17 с —
  перемещение **0 м**; планарное расстояние до будильника **0.333 м**; TTL факта **8 с**;
  headless baseline `744 / 743 / 1` — известный чужой красный слуха.

state_changes: |
  СОЗДАНО (1 улика + 1 наряд + 1 документ):
  - `i-sourceid-names-the-thing-not-the-doer-001` → `open`: `SourceId` называет предмет, а не
    действующее лицо; нужна одноразовая причинность происшествия; фильтр по `CargoId` сломал бы
    игру там, где она должна работать;
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-002` → слот **WIN-U4**, база `023377ab`;
  - `work/2026-09-01-answer-a-response-must-not-outlive-its-fact.md`.

  ЗАКРЫТО КАК ОТОЗВАННОЕ (1 наряд):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-001` → `dropped`: посылка опровергнута его же ногой,
    переиздан как `-002` на верной посылке.

  ОБНОВЛЕНО (1 улика, НЕ закрыта):
  - `i-he-investigates-the-noise-he-made-himself-and-stalls-001` — переписана числом: ответ
    переживает свой факт на 52,17 с, геометрия не нужна; признано, что записка направления про
    мебель переоценила улику; предмет разделён надвое.

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-a-fact-knows-who-made-it-1` остаётся `active` — предмет сузился, задача жива;
  - `t-noise-reaches-by-distance-1` остаётся `active`, ждёт G5;
  - `t-morning-content-1`, `t-carry-back-1`, `t-everything-lands-1` — без изменений.

captures: |
  - **НАПРАВЛЕНИЕ ОБОБЩИЛО ДВА ЗАМЕРА В ПРАВИЛО И ВЫДАЛО ПРАВИЛО ЗА ТРЕТИЙ ЗАМЕР.** «Ингредиент
    наверняка уже лежит» дважды подтвердилось и на третий раз было записано в цель волны без
    проверки. **Правило-кандидат: удачное обобщение проверять на КАЖДОМ следующем случае отдельно —
    иначе оно превращается в догадку с хорошей родословной.** Это второй раз за день, когда
    родословная замера оказалась важнее самого замера.
  - **ЗАПИСКА НАПРАВЛЕНИЯ СДЕЛАЛА ИЗ ЧУЖОГО НАБЛЮДЕНИЯ ВЫВОД, КОТОРОГО ОНО НЕ НЕСЛО.**
    `FurnitureInTheWay` было записано соседним слотом мимоходом; направление превратило его в
    доказанную причину. **Нога отказалась принять — и была права.**
  - **«FAILSAFE» И «ПРАВИЛО» — ЭТО ОДНА И ТА ЖЕ ПРАВКА С РАЗНОЙ СУДЬБОЙ.** Нога предложила
    подпорку и сама сомневалась в ней. Та же правка, обоснованная как «ответ не переживает свой
    факт», перестаёт быть подпоркой. **Правило-кандидат: прежде чем разрешать заплатку, искать
    правило, частным случаем которого она является; не нашлось — тогда это действительно заплатка.**
  - **ЗАДАЧА ПЕРЕСТАЛА БЫТЬ БЛОКЕРОМ РОВНО ТОГДА, КОГДА РАСПАЛАСЬ НАДВОЕ.** Утром она стала
    держателем всего видимого; к вечеру выяснилось, что держит не она, а её половина. **Разделение
    предмета — способ снять блокировку без расширения границ.**

decisions_needed: |
  Ни одного владельческого. Маршрут выбран направлением, видимое последствие владельцу названо.

play_check: |
  1. OPEN — СТОП исполнителя с двумя предложенными маршрутами.
  2. WORK — обе опровергнутые посылки признаны поимённо; несущее решение перевыведено чтением
     механизма первой рукой; заплатка переоснована как правило и лишена нового регулятора;
     предмет разделён, чтобы не открывать границу к чужому слоту.
  3. (owner) — не запрашивался: инженерный маршрут; видимое последствие сообщено.
  4. CLOSE — RESULT, применение, коммит; переизданный наряд уходит владельцу.

log: |
  нога опровергла посылку собственного наряда: SourceId называет предмет, а не действующее лицо, и
  обобщение направления «ингредиент наверняка лежит» оказалось догадкой с хорошей родословной;
  записка про мебель тоже переоценила чужое наблюдение и правильно не принята — но главное число
  замера решает без геометрии: ответ пережил свой факт на 52 секунды при сроке жизни в 8; предмет
  разделён надвое, заплатка переоснована как правило «ответ не переживает свой факт», самошум
  сознательно оставлен нечиненым, чтобы не сталкивать два слота

next: |
  Переизданный наряд `c-exec-g-5a7c-a-fact-knows-who-made-it-1-002` уходит владельцу для **WIN-U4**.

  Волна: голова ждёт опровержения в свежей сессии, ходьба мертва, граница ответа в работе,
  публикация ждёт её. Девятая волна ждёт дерева, на которое можно смотреть.
