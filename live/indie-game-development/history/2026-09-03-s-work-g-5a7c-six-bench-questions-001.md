RESULT s-work-g-5a7c-six-bench-questions-001 (call: — владелец-инициированная, без родительского CALL)
direction: indie-game-development   track: тело   play: work   node/task: t-the-bench-shows-why-he-fell-1

outcome: |
  **ШЕСТЬ ВОПРОСОВ ВОЗВРАТА СТЕНДА ЗАКРЫТЫ: ЧЕТЫРЕ РЕШИЛО НАПРАВЛЕНИЕ, ДВА — ВЛАДЕЛЕЦ.**
  Продолжение выдано. Возврат: `work/2026-09-03-return-the-bench-polygon-and-open-questions.md`.

  **В1 — воспроизводимость остаётся в этой задаче, но сначала замер.** Строка 2 `done_when`
  требует, чтобы удар был воспроизводим; один бросок за Play — нет. Граница ноги принята дословно:
  стенд ВОЗВРАЩАЕТ его в стойку между прогонами — сброс, а не выздоровление; строка 8 не
  нарушается. Нога сама назвала диагноз чтением кода, а не замером, поэтому порядок обязателен:
  померить причину числом → строить сброс → если причина внутри механики равновесия, СТОП.

  **В2 — `running → paused → resumed` снимается со стенда, вариант (а).** На полигоне без дома у
  хозяина нет дела вообще. Строка не пропадает: она уже стоит в рубеже Р2 своими словами
  («роняет удерживаемое, поднимает и возобновляет то же дело»).

  **В3 — второй экран это второй редактор, сторож не трогать.** Причина строки 7 наряда 02.09
  выполняется по существу: во втором редакторе MPPM настоящий клиент с настоящими полученными
  данными. Стенд в поставочной сборке был бы дефектом гигиены.

  **В5 — полку не сериализуем; U4 вливается первым, U1 перебазируется.** Померено на общей базе
  `2d2c27ff`: обе ноги правят `IntegrationLabWindow.cs` (U4 +45/−0, U1 +75/−31) и
  `NetworkWalkerCourier.cs` (U4 +5/−0, U1 +72/−7); пробное трёхстороннее слияние даёт **ноль
  маркеров конфликта**. Сериализовать значило бы остановить полосу ради конфликта, которого нет.
  Но правило `empty-file-overlap-does-not-prove-independence` говорит, что пустое пересечение
  независимости не доказывает — чистое автослияние тем более: U1 перенёс глаголы полки на тулбар,
  U4 дописал полке 45 строк.

  **В4 и В6 закрыты его словами, и оба уточнили мой же вопрос.**

  По В4 он поправил рамку: полигон для анимации — **«на вырост»**, «завести как идею, вернуться
  потом, и естественно делаем в соответствии с планом». И задал встречный вопрос: присутствует ли
  Final IK и анимация в плане вообще. **Ответ по плану — да, и с датами:** Р10 (шлепок и бросок =
  клип плюс прицел Final IK), полоса «Тело» отвечает за Final IK на всём пути, рубежи Р1 шлепок →
  Р2 подъём и возврат → Р3 бросок и вылет → Р4 финальная модель, риск 2 назван. Первые клипы —
  вход раздела 8 со сроком **18.09**, и это вход, который остаётся за владельцем.

  По В6 он поправил повод: летающая камера просилась не против броска, а против неудобного
  Scene View — «я хочу под разными углами посмотреть, но именно когда уже условно поставлено».
  Свободный бросок руками и камера от первого лица уходят в ту же идею; переключатель не строится.

  **ОТДЕЛЬНО: ЕГО ВОЗВРАТ БЫЛ ЗАКОММИЧЕН НАПРАВЛЕНИЕМ СЛУЧАЙНО.** Файл лежал неотслеженным, а
  предыдущая нога стейджила `git add live/indie-game-development` целиком — документ уехал в
  `64b3e872` под сообщением про другое. Не потерян и лежит в правильном месте, но заехал не своим
  ходом; это ровно класс улики `i-parallel-session-commit-swallows-another-legs-edit-001`. Эта
  нога стейджит пофайлово.

evidence: |
  - Его слова в этой сессии, дословно: «я имел в виду на вырост… можем завести как идею, вернуться
    потом, то есть это в любом случае надо делать, и естественно делаем в соответствии с планом»;
    «я хочу под разными углами посмотреть, но именно когда уже условно поставлено — я кнопку жму,
    на него что-то падает, и я могу с разных ракурсов посмотреть»; «можно там свободную камеру
    добавить, но сейчас она действительно будет удобнее».
  - Его же слово, зафиксированное ногой в возврате: «это не нужное требование, чтобы он оставался
    копией игровой сцены… это как раз-таки будет полигон для тестирования всего вместе»;
    «я вообще не понимаю, зачем он должен условно ходить».
  - Возврат ноги: `work/2026-09-03-return-the-bench-polygon-and-open-questions.md` — прогон
    `УДАР ПРИНЯТ → 8° → 35° → УПАЛ → ВСТАЁТ → СНОВА СТОИТ`; и упор: наклон 83–89°, 1461 момент.
  - Замер пересечения ног, `merge-tree` от базы `2d2c27ff` между `slot/win-u4` и `slot/win-u1`:
    два файла «changed in both», **ноль** маркеров `<<<<<<<`.
  - План по Final IK, перечитан этой ногой: строки 62 (тело — PuppetMaster + Final IK), 157 (Р10),
    284 (полоса «Тело» U4), 318 (клипы: шлепок и подъём к 18.09, бросок и питьё к 22.09), 340
    (риск 2), 428 (входы раздела 8 остаются за владельцем).
  - `git log -1 -- work/2026-09-03-return-the-bench-polygon-and-open-questions.md` → `64b3e872`,
    коммит про другое.

state_changes: |
  1. ADD idea `idea-owner-a-polygon-where-the-body-is-watched-001` (`from: владелец`,
     `about: t-the-bench-shows-why-he-fell-1`, `source` → возврат) — полигон, на котором смотрят
     тело: ходьба по команде, управление от третьего лица, подъём вещей, свободный бросок;
     с его словами и с местом в плане (начало Р2, когда придут первые клипы).
  2. BLOCK `t-the-bench-shows-why-he-fell-1` → `done_when` переписан: восемь подписанных строк
     сохранены дословно, ниже добавлены четыре поправки П1–П4, каждая с источником (его слово или
     решение направления) — так сверка 11.09 видит и подписанное, и изменение.
  3. ADD `work/2026-09-03-call-the-bench-throw-repeats.md` и карточку
     `c-exec-g-5a7c-the-bench-throw-repeats-001` (`to: executor`, `status: ready`, слот WIN-U4,
     `basis: 90fbc3ea`, `parent: c-exec-g-5a7c-bench-polygon-proba-1`).
  4. Журнальные строки на ставку и затронутые карточки.
  5. План не тронут. Отмена Р14 по-прежнему живёт уликой
     `i-the-bench-failed-his-eye-and-moved-to-its-own-scene-001` с `route: review`; вопрос патча —
     сверке 11.09.
  6. `NOW.md` не менялся.

captures:
  - Наряд стенда 02.09 стоял на протухшем замере: он ссылался на `09cb3c9e` и требовал класть набор
    в `MouseLab`, тогда как `a345904f` того же дня разложила стенды по актёру и нарочно удалила
    оттуда хозяина, а `8d7013bf` вынесла дом. Порог 4 наряда сработал правильно и вопрос ушёл
    владельцу — то есть механизм отработал, но замер наряда был старше самого наряда. Это второй за
    день случай, когда буквальная строка направления оказалась в продукте другой.
  - Первые клипы (шлепок и подъём) — вход раздела 8 со сроком 18.09, и он остаётся за владельцем.
    Заказа направление не видело. Строка Р0 «источник модели фокусника назван и заказан» тоже
    красная. До 18.09 тринадцать дней.

decisions_needed:
  - q: Заказ базовых клипов (шлепок/пинок и подъём с пола) — вход раздела 8 со сроком 18.09.
      Без них Р2 упрётся, и запасной путь плана (риск 2) — покупка набора за его деньги.
    options: [заказать на этой неделе; решить на сверке 11.09]
    recommendation: заказать на этой неделе — тринадцать дней до срока, а вход не наш.

play_check:
  - 1 recite: done — работа названа возвратом ноги и его словами по В4 и В6.
  - 2 owner inputs (owner): done — В4 и В6 закрыты его цитируемыми словами; за него ничего не
    решено. Его встречный вопрос про Final IK в плане отвечен строками плана, а не мнением.
    В1, В2, В3, В5 — решения направления о форме работы, не о содержании игры.
  - 3 do the work: done — идея заведена, поправки записаны в задачу, продолжение написано и выдано,
    пересечение ног померено.
  - 4 self-check: done — собственный случайный коммит его возврата назван, а не умолчан;
    стейджинг этой ноги пофайловый. Подписанные строки задачи не стёрты, поправки лежат рядом.
  - 5 close: done — `close: light — because` каждая строка выведена из его слов в этой сессии, из
    возврата ноги, из строк принятого плана и из замера `merge-tree` по двум веткам продукта.
    Поведения продукта эта нога не утверждает: диагноз «почему не встаёт» намеренно оставлен
    незакрытым и отправлен замером в наряд. G5-чат не открывается.

log: шесть вопросов возврата стенда закрыты — четыре решением направления, два его словом: воспроизводимость остаётся в задаче, но сначала замер и только потом сброс в стойку; строка «возобновляет то же дело» уезжает в Р2, где ей место; второй экран это второй редактор и сторож билд-настроек не трогается; полку не сериализуем, померено — автослияние чистое, но U4 вливается первым и U1 перебазируется, потому что чистое слияние независимости не доказывает; полигон для анимации заведён идеей его словами «на вырост», и на его встречный вопрос отвечено планом — Final IK на критическом пути, первые клипы вход раздела 8 к 18.09 и он за владельцем; продолжение U4 выдано

next: |
  `c-exec-g-5a7c-the-bench-throw-repeats-001` готов к запуску в WIN-U4 на `90fbc3ea`.
  Порядок слияния: U4 первым, затем U1 перебазируется — в WIN-U2 идущая нога четырёх экранов
  ждёт приборов стенда, которых в `main` ещё нет.
  Ждёт его решения: заказ базовых клипов (шлепок, подъём) со сроком 18.09.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-work-g-5a7c-six-bench-questions-001.md
