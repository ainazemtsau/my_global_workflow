# RESULT — s-repair-g-5a7c-publication-block-001

direction: indie-game-development
play: repair
node/task: g-5a7c / bet-g-5a7c-wave-9 — блокировка публикации
track: починка
date: 2026-08-31
outcome_kind: repair — СТОП исполнителя разобран: он прав по существу и неверен в трёх деталях, а настоящий блокер публикации оказался вообще другим и лежит в dev

owner_approved: |
  Решение о публикации — инженерное, внутри объявленных гейтов, и владельцу оно передано фактом,
  а не вопросом. Исполнитель предлагал ЕМУ выбор из двух вариантов; проверка показала, что оба
  не ведут к публикации, поэтому выбор ему не передаётся в такой форме.

  Ни одна строка `done_when` не изменена. Ничего не опубликовано.

outcome: |
  **ИСПОЛНИТЕЛЬ WIN-U1 ПРАВ ПО СУЩЕСТВУ: КРАСНОЕ ПРЕДСУЩЕСТВУЕТ ВОЛНЕ.** Доказано тремя способами,
  ни один из которых не опирается на его пересказ:
  - **байты** — все отслеживаемые входы упавшего теста побайтово идентичны на `c7e926cf` и
    `fb747970`; вся волна не тронула ни одного файла арта, ассетов или меты;
  - **хронология** — 29 августа в ТОМ ЖЕ чекауте `testrun` тест был зелёным, слов «has no Animator»
    в логе не было ни разу; сегодня они там 28 раз;
  - **чужая улика** — соседняя нога гейта записала те же два падения на коммите `bb32f3f8` в
    06:10:58, за 22 и 33 минуты ДО обоих коммитов волны.

  **ЕГО РАЗБОР НЕВЕРЕН В ТРЁХ МЕСТАХ, И ОДНО МЕНЯЕТ РЕМОНТ.**
  1. `animationType: 2` — это **Generic**, а не Humanoid. Human — это 3. Доказано изнутри репозитория:
     заведомо неригованная мебель Quaternius несёт ровно `2`/`0`/ноль костей.
  2. Поломка не «два поля», а **сброс в дефолт**: было 48 758 байт / 1 305 строк / 54 сопоставления
     костей, стало 2 993 байта / 114 строк / ноль. Стёрто 45 765 байт рига. `CreationTime` =
     31.08 05:58:41 — файл создан заново, а не переписан.
  3. **«Файл не в git, значит история не отвечает» — опровергнуто.** Файл БЫЛ отслеживаемым и
     вынесен коммитом `6fafb0da`. Хорошая версия цела: блоб `5af6cc24…`, 48 960 байт, тот же guid,
     `animationType: 3`, `avatarSetup: 1`, полные `human`/`skeleton`. **Починка — возврат известных
     байтов, а не догадка про поле.**

  **НАПРАВЛЕНИЕ ТОЖЕ ОШИБЛОСЬ, И НАЗЫВАЮ ЭТО ПЕРВЫМ.** Первая проверка объявила, что файла не
  существует нигде: `git ls-files` пусто, `find` по четырём каталогам пусто. Файл существует. Он
  лежит за **NTFS-junction** в облачном складе `C:\Users\Anton\iCloudDrive\TunnelCrew-Assets\…`, и
  junction стоит в `win-u1..u4` и в `testrun`, но ОТСУТСТВУЕТ в `GasCoopGame`, где направление и
  искало. Из этой ошибки был сделан вывод «настройки не версионируются вовсе» — он тоже неверен.

  **НАСТОЯЩИЙ БЛОКЕР ПУБЛИКАЦИИ — НЕ АРТ, И ЕГО НЕ ВИДЕЛ НИКТО.**
  В `dev` лежит **намеренно сломанный сторож**: `HouseholderDecisionPipelineTests.cs:206` несёт
  `Is.EqualTo(3L)` вместо `2L` — посевная мина ноги гейта (`bb32f3f8` «Seed failing headless close
  proof»). Наряд требовал посеять, показать красное закрытие и **вернуть**. Возврат написан
  (`03cd6c87`), но `git branch -a --contains` даёт **ровно одну ветку: `slot/win-u4`**. В `dev` он
  не влит.

  **И сломан ровно тот сторож, который охраняет глухоту хозяина** — класс дефекта, из-за которого он
  на прошлой неделе проходил мимо крысы. Публикация в нынешнем виде выключила бы защиту от отказа,
  ради которого нога гейта и затевалась.

  **КРАСНЫХ ДВА, А НЕ ОДИН.** Второй — `CargoPhysicsMeasurementTests.HeldAlarmClockFollowsWheel…`,
  «Errors occurred during domain reload», пережил автоматический перепрогон. **Любой ремонт арта
  оставит закрытие красным**, а `close-leg.ps1:268` отказывает безусловно и ключа обхода не имеет.
  Поэтому оба варианта, предложенные исполнителем владельцу, к публикации не ведут.

  **НОГ В `dev` ТРИ, А НЕ ДВЕ.** Нога гейта влилась ПЕРВОЙ, до обеих названных. В `main` легли бы
  восемь коммитов.

evidence: |
  Проверено направлением лично, поверх фан-аута:
  - `git show dev:tests/TunnelCrew.Core.Tests/HouseholderDecisionPipelineTests.cs` строка 206 →
    `Assert.That(result.DecisionWeight, Is.EqualTo(3L));`
  - `git branch -a --contains 03cd6c87` → `slot/win-u4`, и только она.
  - `git log --oneline -1 main` → `c7e926cf`; `git log --oneline -1 dev` → `fb747970`.

  Из фан-аута, с указанием способа:
  - junction: `Get-Item … | Select LinkType,Target` → `Junction` →
    `C:\Users\Anton\iCloudDrive\TunnelCrew-Assets\TunnelCrew\Art\Bodies\Sources\Victor`;
  - `.gitignore:233` — `/Assets/TunnelCrew/Art/Bodies/Sources/Victor/`, под комментарием про
    склад вне git;
  - живая мета: sha1 `8f2d979a…`, 2 993 байта, 114 строк, `animationType: 2`, `avatarSetup: 0`,
    `human: []`, `skeleton: []`, `grep -c boneName` → 0;
  - историческая: блоб `5af6cc24…` (коммит `75ac8fca`), 48 960 байт, `animationType: 3`,
    `avatarSetup: 1`, 55 `boneName`; вынесена коммитом `6fafb0da`;
  - времена: FBX 25.08 13:17, мета CreationTime 31.08 05:58:41; по всему складу за сутки тронут
    один файл;
  - цена переимпорта именно этого FBX: 14,2 с в `win-u1`, 18,8 с в `win-u3`. Сносить `Library`
    (8,6 ГБ и 7,2 ГБ) НЕ требуется: испорчен источник, а не кэш — оба слота получили один и тот же
    Import Result ID.

state_changes: |
  СОЗДАНО (2 улики):
  - `i-a-seeded-mine-still-lives-in-dev-001` — сломанный сторож в `dev`, маршрут `work`,
    снимается влитием `03cd6c87`; пока строка 206 несёт `3L`, публикация запрещена;
  - `i-shared-art-store-has-no-guard-001` — склад один на пять чекаутов через junction, меты вне
    git и без контрольной суммы; немедленный возврат байтов снимает красное, но не улику.

  НЕ СДЕЛАНО НАМЕРЕННО:
  - **ничего не опубликовано, `main` не тронут**;
  - мета НЕ восстановлена этой ногой: склад общий и живой у пяти чекаутов, три из которых заняты
    чужими ногами. Восстановление — отдельное действие с названным исполнителем, а не побочный
    эффект разбора;
  - `03cd6c87` НЕ влит: это работа ноги гейта, её слот за ней, и доделывать чужую ногу ремонт не
    вправе.

captures: |
  - Второй красный (`domain reload`) может оказаться следствием той же сломанной меты: сломанный
    импорт роняет домен. Проверять после возврата байтов, а не до — иначе два ремонта смешаются.
  - Ни одна из трёх ног в `dev` не прошла штатное закрытие: закрывальщик упирается в красное и до
    публикации не доходит. Значит `dev` сегодня — не «почти опубликованное», а «три недоделанных
    закрытия в одной ветке».

decisions_needed: |
  Ни одного для владельца. Порядок действий определён фактами и передан ему как решение
  направления, а не как выбор.

play_check: |
  1. Назвать противоречие — исполнитель заявил чужой блокер и предложил владельцу выбор из двух;
     проверка показала, что блокер другой, а оба варианта к публикации не ведут.
  2. Восстановить новейшим-первым — состояние веток, содержимое `dev` и байты меты прочитаны
     напрямую; разбор исполнителя перепроверен, не пересказан.
  3. Предложить исправленное состояние — две улики, порядок действий, ничего не опубликовано.
  4. Подтверждение (owner) — не требуется: гейты объявлены заранее, решение внутри них.
     Владельцу передан факт и порядок.
  5. Дыра в OS → нет. Дыры здесь продуктовые, обе записаны уликами.

log: |
  СТОП прибора разобран: красное действительно предсуществует волне, но диагноз исполнителя неверен
  в трёх местах, а настоящий блокер другой — в dev лежит намеренно сломанный сторож глухоты, чей
  возврат остался на ветке слота

next: |
  Домой. Порядок, в котором снимается блокировка, назван владельцу:
  сначала влить `03cd6c87` в `dev` (работа ноги гейта, WIN-U4), потом вернуть байты меты из блоба
  `5af6cc24…` в склад, потом перепроверить второй красный — он мог быть следствием той же меты.

  Публикация до этого запрещена: `dev` несёт сломанный сторож класса, из-за которого хозяин глох.
