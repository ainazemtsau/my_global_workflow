# RESULT — s-work-g-5a7c-loot-foundation-return-001

play: work
direction: indie-game-development
node: g-5a7c
track: house-and-things
task: t-loot-1
returning_call: c-exec-g-5a7c-loot-foundation-001
date: 2026-08-14
close: НЕТ — checkpoint. Светлое закрытие недоступно по прямой строке карточки `t-loot-1`.

## outcome

**Лут приехал и опубликован; задача — нет.** Наряд `c-exec-g-5a7c-loot-foundation-001` вернулся
поставкой, которая существует в опубликованном коде и перемерена этой ногой побайтно. Разводит
поставку и закрытие не процедура, а две неисполненные строки его же наряда, вырезанные исполнителем
без слова владельца, плюс запрет светлого закрытия, написанный на самой карточке задачи.

Владелец сказал «задача завершена». В той части, которую можно перемерить байтами, он **прав** —
и это записано прямо, потому что предыдущие два раза он ошибался и правило требует мерить, а не
верить.

## evidence

Всё ниже прогнано этой ногой на опубликованной голове, а не перенесено из возврата.

**Публикация.** `git ls-remote origin` в `C:\projects\Unity\GasCoopGame`:
`origin/main` = `origin/dev` = `53afdc2dace3e814b6b8d02ee6de6479bf6d6bea`.
Возврат называл последним чтением `27c98d14` — это чтение снималось ДО интеграции; голова с тех пор
уехала на два документных коммита дальше названного кандидата, и оба трогают только
`docs/results/`.

**Кандидат.** `8240329c` — предок `53afdc2d` (`git merge-base --is-ancestor` = да).
`git rev-list --count 1307aa3e..53afdc2d` = **22**; `git diff --shortstat 1307aa3e 8240329c` =
**49 файлов, +3764 / −404** — совпадает с возвратом до знака. На полном диапазоне до головы
+3800 / −404, разница — те же два документных коммита.

**Десять предметов.** `Assets/TunnelCrew/Settings/GameRulesSettings.asset` на `53afdc2d`:
`_definitionId` 1…10, массы `48 48 48 48 48 / 100 100 100 / 240 240` — **ровно 5/3/2, числа
48/100/240 не тронуты**, как требовали границы наряда.

**Гейт второго вида.** `git show --stat f841b52a` — **ровно три файла, 305 строк добавления**:
`Core/Cargo/ToppledCargoThing.cs` (93), его `.meta` (2), `tests/…/SecondThingKindTests.cs` (210).
Ни `CargoThing.cs`, ни `CargoRules.cs`, ни `AlarmClockCargoThing.cs`, ни `CargoState`/`CargoSnapshot`
в коммите нет. Гейт исполнен в объявленной форме.

**Ревью.** `docs/reviews/review-c-exec-g-5a7c-loot-foundation-001.md` существует на голове,
`Reviewed-commit: 1aa536d0` (предок головы), **22 поднято / 13 опровергнуто / 9 подтверждено**, все
девять с диспозицией `fixed 8a247a7f`, раздел учёта всего, что тронуло охраняемый корень после
ревьюенного коммита, на месте.

**Preserve.** `refs/heads/preserve/c-exec-g-5a7c-loot-1-001-win-u1-local-20260813` = `e0a30194`,
и `e0a30194` **НЕ является предком** `8240329c`. Замороженный кандидат не открывали и в дерево не
вливали — это факт графа, а не заявление.

**Прогон.** `dotnet test tests/TunnelCrew.Core.Tests/TunnelCrew.Core.Tests.csproj -c Release` на
опубликованной голове моей рукой: **293 пройдено, 0 упало, 0 пропущено, 293 всего** (271 ms).
Ревью фиксирует 280 → 293 за эту ногу.

**Файлы в продакшене.** `LootDefinition.cs`, `LootHeft.cs`, `LootVisualProfile.cs`,
`World/LootSupport.cs`, `Core/Cargo/ToppledCargoThing.cs`, `Editor/CargoSpawnPointEditor.cs`,
`Art/Loot/AlarmClockView.prefab` — все на `53afdc2d`. `CargoClass.cs` из дерева ушёл.

## сверка наряда со строками — deliverable reconciliation

| строка `done_when` наряда | диспозиция |
|---|---|
| 1 — один валидируемый путь данных и опор | **исполнена**, с одним разделением: громкий отказ до старта Host исполнен для каталога внутри кнопки Host, а для раскладки — на загрузке сцены со сносом хоста. Причина названа исполнителем и техническая (сцену поднимает FishNet после старта сервера) |
| 2 — идентичности не смешиваются, вид и поведение доезжают | **исполнена** в редакции самого наряда (поправка 16: идентичностей ДВЕ, не три). Гейт второго вида доказан составом коммита |
| 3 — физика и публикация доказаны на настоящих путях | **ИСПОЛНЕНА ЧАСТИЧНО.** Публикация, откат, ревью, headless — есть. **Unity EditMode/PlayMode матрица не написана вовсе**; **пункт 2 ручной приёмки (снять временное покрытие пола) не прогонялся** |

**Вырез без owner-ack.** Возврат вырезает Unity-матрицу с объяснением, но без настоящего слова
владельца. Правило сверки называет такой вырез конфликтом, а не пропуском. Дословный токен
владельца на превышение бюджета перечислял «headless + Unity tests» — то есть вырезано из ЕГО
списка. Проверено отдельно: венью существовало —
`Assets/Tests/TunnelCrew/ThreeRoomHouse/TunnelCrew.ThreeRoomHouse.EditorTests.asmdef` лежит в репо,
то есть это не «инструмент недоступен», а невыполненная работа.

**Два внутренних противоречия возврата** (названы, но ни на что не влияют): таблица строки 3
объявляет owner-eye матрицу NOT DONE, а раздел §owner verdict сообщает, что владелец прогнал; и
раздел §cuts объявляет ревью неполученным, а §evidence его цитирует. Обе правки въехали позже
самим исполнителем; на состояние это не влияет, но означает, что подиспунктной диспозиции по трём
пунктам ручной приёмки в возврате НЕТ.

## state_changes

1. `c-exec-g-5a7c-loot-foundation-001` — **закрыт**. Причина с числами в журнале карточки.
2. `c-exec-g-5a7c-loot-1-001` — **закрыт `superseded`**, `superseded_by:
   c-exec-g-5a7c-loot-foundation-001`. Его `unblock_when` исполнен буквально и перемерен; `ready` он
   стать не мог, потому что собственный текст наряда запрещает переоткрывать замороженного
   кандидата.
3. `t-loot-1` — **остаётся `active`**, журнал пополнен.
4. `i-householder-climbs-loot-and-stuck-on-it-001` — **заведена** (product/work/open).
5. `i-observation-lab-no-longer-mirrors-house-loot-001` — **заведена** (execution/work/open).
6. `i-cargo-sinks-into-the-floor-when-the-householder-arrives-001` — журнал: второе независимое
   наблюдение за сутки, рецепт воспроизведения расширен.
7. `i-alarm-binds-to-weight-class-not-to-the-thing-001` — журнал, **не закрыта**.
8. `i-loot-candidate-emits-stale-contact-on-pickup-001` — журнал, **не закрыта**.
9. `d-loot-unity-matrix-cut-001` — **заведено решение** владельцу, два варианта, рекомендация (а).

Почему 7 и 8 не закрыты, хотя их `review_when` говорит «закрыть после публикации»: мой замер
доказывает **публикацию**, а не **поведение**. «Звонит один, а не два» и «протухший удар не
стреляет» — суждения, которых никто ещё не выносил в собранной игре; обе идут строками в
биндинговую проверку. Ровно этот класс подмены — «опубликовано, значит верно» — правило и
запрещает.

## captures

Четыре находки назвал сам исполнитель в §Next, а не проверка:

1. **Груз проваливается в пол после переноски** — `Tool Box` уехал на `y = -198`. Это **второе
   независимое наблюдение того же класса за одни сутки**: первое — его собственный прогон, где
   груз провалился при подбежавшем хозяине, заведено ногой наряда на обработчики. Дописано в
   существующую улику, новой не заводил.
2. **Хозяин залезает на лут, один раз залип и задрожал** — новая улика, вместе с уже известной
   механикой верхов груза (`AuthoritativeCargoRoster.cs:297-322`, проходимы задолго до этой ноги;
   нога дала верхи мебели и учетверила число твёрдых предметов с четырёх до десяти) и родством с
   починенной дрожью погони.
3. **Лаборатория перестала показывать тот же лут, что игра** — те же десять описаний при пустом
   массиве расстановки. Новая улика, маршрут в `t-lab-2`.
4. **Unity-матрица отсутствует** — не улика, а неисполненная строка; ушла в решение.

## decisions_needed

`d-loot-unity-matrix-cut-001` — принять вырез Unity-проверок как названный (а) или дописать их
отдельной короткой ногой до закрытия (б). Рекомендация (а) с доводом: три из четырёх находок всё
равно приведут сюда ногу, которая обязана тронуть лут, и проверки, написанные поверх настоящего
воспроизведённого дефекта, стоят вдвое дешевле и ловят больше, чем написанные «для полноты» на
код, который никто не собирается менять.

## play_check

1. **Recite** — цель и три строки `done_when` наряда восстановлены из его собственной карточки и
   файла наряда, а не из возврата; обязательство перед ставкой `g-5a7c`, дорожка `house-and-things`.
2. **Owner inputs (owner)** — владелец сказал **«задача завершена»**. Это его слово о задаче, и
   оно принято как улика, а не как факт: перемерено девятью числами выше. Подиспунктной диспозиции
   по трём пунктам ручной приёмки его слово не даёт, и это названо ему прямо. Единственный
   оставшийся вопрос к нему вынесен решением, а не угадан.
3. **Do the work** — сверка возврата с собственной копией `done_when` наряда; девять первичных
   замеров; четыре находки разведены на «дописать в существующую» и «завести новую».
4. **Self-check** — строки 1 и 2 исполнены, строка 3 исполнена частично; расхождение названо
   поимённо, а не сглажено.
5. **Close** — checkpoint. Возвращающийся наряд закрыт, задача открыта, дорожка держится решением.

`close: heavy` — светлое закрытие запрещено собственной строкой карточки `t-loot-1`:
«светлое недоступно — реальная опора, client presentation, stale-contact behavior и owner-eye
matrix требуют свежей биндинговой проверки; публикация продукта сама задачу не закрывает».

## next

Ответ владельца на `d-loot-unity-matrix-cut-001`. Он выбирает, что открывается следующим:

- **(а)** — свежая биндинговая проверка `t-loot-1` в отдельном чате: пробует опровергнуть один
  звонящий будильник, протухший удар, реальную опору и клиентскую отрисовку, и требует от владельца
  непрогнанный пункт про покрытие пола;
- **(б)** — короткая нога исполнителя на шесть Unity-проверок, и только потом та же биндинговая
  проверка.

Наряда не выпускаю: строки решения — это то, что должно стоять в его `done_when`, и выпускать его
до ответа значит выдумывать ответ.

END_OF_FILE: live/indie-game-development/history/2026-08-14-s-work-g-5a7c-loot-foundation-return-001.md
