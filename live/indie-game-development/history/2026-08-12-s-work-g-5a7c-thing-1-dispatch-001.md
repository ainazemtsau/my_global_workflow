# RESULT — s-work-g-5a7c-thing-1-dispatch-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-thing-1
track: house-and-things
date: 2026-08-12
outcome_kind: checkpoint — наряд выдан, задача НЕ закрыта

owner_approved: |
  «давай наряд t-thing-1» — 2026-08-12, после того как я на его реплику «параллельно первым двум
  задачам мы больше ничего запустить не можем» ответила, что это моя недоработка, а не свойство
  волны, и что `t-thing-1` свободна.

outcome: |
  Наряд `c-exec-g-5a7c-thing-1-001` написан и выдан в слот **WIN-U3** от опубликованной головы
  `f3c40c40`. Задача переведена из `blocked` в `active`. Это третий параллельный фронт.

  **СНЯТА СОБСТВЕННАЯ НЕВЕРНАЯ БЛОКИРОВКА.** Нарезая волну, я пометила `blocked` ВСЕ задачи обеих
  дорожек «до шва». Проверка по существу показала, что это верно не для всех: шов даёт словарь
  ролей, и блокирует он ровно то, что этим словарём пользуется. `t-thing-1` им не пользуется —
  звонящий предмет рождает обычную ситуацию мира, а знание «в какой комнате» это отдельная задача
  `t-place-fact-1`. Пересечение по файлам с обоими работающими нарядами — ноль: шов живёт в
  `Profiles/**`, `Core/House/**`, `World/HousePlaceMarker.cs`; тела — в `Art/**`, презентации ходока
  и `World/Householder.prefab`; эта задача — в `Core/Cargo/**`, `Core/Situations/**` и контакте.

  Честно названо владельцу и здесь: заблокированными по существу остаются `t-rooms-1`,
  `t-occupations-1`, `t-place-fact-1`, `t-house-asset-1`, `t-loot-1`, `t-lab-2`; `t-sight-1`
  формально могла бы, но её судит глаз владельца, и третий вердикт в тот же вечер — плохая покупка.

  **ВЫДАЧА НАШЛА ТРИ ВЕЩИ, КОТОРЫЕ ДЕЛАЮТ ЗАДАЧУ ДЕШЕВЛЕ, ЧЕМ ОНА ВЫГЛЯДЕЛА, И ОДНУ ЛОВУШКУ.**

  1. **ПОЛОВИНА ЕГО ПРИМЕРА УЖЕ РАБОТАЕТ.** `Network/CargoBodyContact.cs:117-179` — `Record`
     вызывается из `OnCollisionEnter`/`OnCollisionStay` и **не фильтрует, во что врезались**; поиск
     `IgnoreCollision`, `excludeLayers`, `includeLayers`, `gameObject.layer`, `LayerMask` по всему
     `Assets/TunnelCrew` даёт НОЛЬ совпадений. Значит тела груза сталкиваются друг с другом обычным
     образом и удар груза о груз **уже сегодня рождает шум**, помеченный `ContactedHouse`. Докстрока
     «Only the house is physical here» — утверждение о мире на момент написания, а не фильтр.
     Строить надо состояние и его событие, а не механику столкновения.
  2. **ДВЕРЬ ДЛЯ НОВОГО ПРОИЗВОДИТЕЛЯ УЖЕ ПРОРУБЛЕНА.** `Core/Multiplayer/CarryWorld.cs:66-68,
     97-103,149-153` — есть `_situations` и `_queuedSituations`, и докстрока прямо говорит, что
     положенное туда «enters the tick list as cargo-produced situations and is reduced by the same».
     Дверь построила закрытая `t-observe-1` под ручные маркеры лаборатории; наряд велит ей
     пользоваться, а не рубить новую.
  3. **ФОРМА «СВОЁ ЗНАЧЕНИЕ У ЭКЗЕМПЛЯРА» СУЩЕСТВУЕТ.** `Core/Cargo/CargoState.cs` уже несёт
     `Integrity`, `IsActive` и полный кватернион ориентации — то есть и изменяемое значение ездит в
     состоянии, и ориентация для второго способа срабатывания уже под рукой. `Integrity` при этом
     наряд запрещает использовать: повреждение и уничтожение вырезаны строкой узла.

  **ЛОВУШКА, НАЗВАННАЯ ДО ПОСТРОЙКИ, а не после.** `Core/Multiplayer/CarryWorld.cs:167-168` сводит
  весь тик-лист к ОДНОМУ самому громкому стимулу и только его отдаёт хозяину; порядок громкости в
  `WorldSituationNoise.cs:168-183` — `Landing` 4 > `RepeatedHeavyNoise` 3 > `BoardCreaked` 2 >
  `QuietRustle` 1. **Звон, рождённый в тот же тик, что и удар, который его вызвал, будет проглочен
  ударом.** Без этого предупреждения исполнитель либо доложил бы поломку, либо «починил» бы её
  правкой сведения — а сведение принадлежит дорожке хозяина и трогать его запрещено. Правильный
  ответ совпадает с требованием владельца про конечность звона: звон живёт несколько тиков и будет
  услышан позже, когда удар не соревнуется. Это делает длительность осмысленной, а не декоративной,
  и наряд требует подтвердить её числом.

evidence: |
  **ПЕРЕМЕРЕНО ЭТОЙ НОГОЙ ПЕРВОЙ РУКОЙ 2026-08-12 на `f3c40c40`.**

  - `Network/CargoBodyContact.cs:117-179` прочитан целиком; фильтра по цели столкновения нет.
  - поиск `IgnoreCollision|excludeLayers|includeLayers|gameObject.layer|LayerMask` по всем `.cs` в
    `Assets/TunnelCrew` → **0 совпадений**.
  - `Core/Multiplayer/CarryWorld.cs:66-68,90,97-103,109-112,149-153,162,167-168`.
  - `Core/Situations/WorldSituationNoise.cs` прочитан целиком: полосы, правило «порог ≤ 0 выключает
    полосу», ранжирование громкости, `Loudest`.
  - `Core/Cargo/CargoState.cs` — полный список полей.
  - `Settings/GameRulesSettings.asset` — `_cargoRustleForce 60`, `_cargoKnockForce 150`,
    `_cargoHeavyNoiseForce 400`; три класса груза `48 / 100 / 240`.
  - `tools/select-slot.ps1` по четырём слотам: все `CLEAN / AVAILABLE / lease none`; WIN-U3 `head
    352f96b0` — отставание названо в наряде первым действием.
  - `dotnet test ... -c Release` → 214 passed, прогнано этой ногой ранее сегодня на том же дереве.

  **G5:** нога ничего не закрывает — она выдаёт наряд.

state_changes: |
  1. Создан `work/c-exec-g-5a7c-thing-1-001-call.md` — полный текст наряда.
  2. Создана `cards/c-exec-g-5a7c-thing-1-001.md` — `status: ready`, `slot: WIN-U3`,
     `basis: f3c40c40`, `engineering_contract: 36`.
  3. `t-thing-1`: `blocked → active`; в `note` добавлен блок «поправка 2026-08-12 при выдаче
     наряда» — снятая блокировка, три измеренных подарка и ловушка; добавлен `## журнал`.
  4. Полный RESULT сохранён в `history/2026-08-12-s-work-g-5a7c-thing-1-dispatch-001.md`.

  **Текст ставки `bet-g-5a7c-wave-3` НЕ редактирован** — `Writes:` плея `work` его не покрывает. Его
  строка про блокировку обеих дорожек швом остаётся слишком широкой; поправит `review`.

captures:
  - |
    СТАВКА БЛОКИРУЕТ ШИРЕ, ЧЕМ НУЖНО. `bet-g-5a7c-wave-3` §tracks говорит, что шов блокирует
    `householder-life` и `house-and-things` целиком. По существу он блокирует только то, что
    пользуется словарём ролей. Как минимум одна задача (`t-thing-1`) была заблокирована зря, и
    владелец потерял на этом полдня параллельной работы. Поправить формулировку на review: блокирует
    не дорожки, а перечисленные задачи.
  - |
    Докстрока `CargoBodyContact` «Only the house is physical here» описывает мир, а не код, и уже
    один раз чуть не была прочитана как фильтр. Когда в доме появится мебель (`t-rooms-1`) и
    предметы с состоянием (эта задача), утверждение станет прямо ложным. Стоит поправить её тем
    нарядом, который первым сделает его ложным.
  - |
    Правило «за тик хозяин слышит ровно одно самое громкое» (`CarryWorld:167`) — это ограничение,
    которое будет мешать КАЖДОМУ новому источнику, а не только звону. Зрение (`t-sight-1`) упрётся в
    него следующим: увиденная мышь в тик громкого удара будет проглочена. Дорожке `householder-life`
    стоит знать это до постройки.

decisions_needed: []

play_check:
  - 1 recite — done. Цель и семь строк `done_when` карточки `t-thing-1` пересказаны; обязательство ставки названо.
  - 2 owner inputs — не требуется: единственное, что принадлежит только владельцу, — нужно ли выключать будильник, и он это уже сказал («наверное, даже может и не надо выключать»). Наряд превращает его слово в конечную длительность звона, а не спрашивает заново.
  - 3 do the work — done. `call:executor` выпущен полностью; названный подход зафиксирован одним токеном; механизм не предписан, ограничен границами.
  - 4 self-check — done. Три строки контракта покрывают все семь строк карточки; бюджет контракта 36 соблюдён без овербюджетного токена.
  - 5 close — checkpoint. Задача НЕ закрыта.
  - G1 — done. Дорожек по-прежнему три, лимит четыре его словом; третий фронт внутри лимита.
  - G2 — done. Задача служит активной ставке.
  - G5 — не применяется.
  - G7 — done. Ни одно игровое решение ногой не принято; каталог предметов не изобретён.
  - G10 — done. RESULT финальный.

log: наряд на предмет с состоянием выдан в WIN-U3 третьим параллельным фронтом, и первым делом снята собственная неверная блокировка — нарезая волну, я пометила blocked ВСЕ задачи обеих дорожек «до шва», хотя шов блокирует только то, что пользуется словарём ролей, а этой задаче он не нужен вовсе, и пересечение по файлам с обоими работающими нарядами ноль; выдача нашла три вещи, делающие задачу дешевле, чем она выглядела — контакт груза НЕ фильтрует, во что врезались, и ни одного IgnoreCollision или LayerMask в проекте нет, то есть удар груза о груз уже сегодня рождает шум и строить надо состояние, а не механику столкновения; дверь для внешнего производителя ситуаций уже прорублена закрытой t-observe-1; и CargoState уже несёт изменяемые значения плюс кватернион ориентации, то есть форма существует и ориентация для второго способа срабатывания под рукой; и одну ловушку, названную ДО постройки — мир сводит весь тик-лист к одному самому громкому стимулу, поэтому звон в тот же тик будет проглочен ударом, и лечится это длительностью звона, а не правкой сведения, которое принадлежит чужой дорожке

next: |
  Ждём возврата исполнителя по `c-exec-g-5a7c-thing-1-001`. Первая строка возврата — расписка с
  числом полученных строк `done_when` (три) и базисом.

  Три фронта идут одновременно: шов в WIN-U2, тела в WIN-U1, предмет в WIN-U3. Порог смерти 3
  считает ноги: больше двенадцати — первой на паузу встаёт дорожка `body`, следом `t-loot-1`.

  Возвраты по всем трём разбираются в отдельных свежих чатах.

END_OF_FILE: live/indie-game-development/history/2026-08-12-s-work-g-5a7c-thing-1-dispatch-001.md
