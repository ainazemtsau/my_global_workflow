# RESULT — s-work-g-5a7c-tracks-dispatch-001

play: work · выдача двух нарядов ОДНОЙ ногой, потому что дорожки открываются одновременно его
словом и делят один общий контракт параллельности: разрезать выдачу надвое значило бы написать
границы по файлам дважды и рискнуть написать их по-разному
direction: indie-game-development · node: g-5a7c · tracks: cargo-hands, householder ·
tasks: t-cargo-1, t-react-1
date: 2026-08-10

base_read: OS — `wt/indie-game-development`, рабочее дерево чисто. Продукт перемерен
`git ls-remote origin` в этой ноге: `origin/dev` = `origin/main` =
`4cf2b5dc5b7d6b22d580d57d97e3254fb061f230`, дерево `58105a5887d7131db608fd2831396c245882d5e5`.
Слоты перемерены НАСТОЯЩИМ селектором, только чтение: WIN-U1 `CLEAN / AVAILABLE / lease none`,
`head: 4cf2b5dc` знак в знак базис; WIN-U2 `CLEAN / AVAILABLE / lease none`, `head: 352f96b0`.

## outcome

Написаны и выпущены ДВА наряда — по одному на дорожку, оба от одной базы `4cf2b5dc`, в двух разных
слотах. Обе дорожки волны открыты одновременно впервые в этой ставке.

**Наряды НЕ СУЩЕСТВУЮТ, пока исполнители не расписались.** По букве `evaluator` этой волны (пункт
3) выдача — это готовая паста/вложение с ПОЛНЫМ текстом плюс расписка первой строкой возврата.
Расписка по построению не может существовать внутри этой ноги: она приходит от исполнителя. Поэтому
обе задачи остаются `open`, обе карточки нарядов — `ready`, и до расписок ни один наряд не
считается выданным.

## evidence — перемерено первой рукой перед выпуском

### Слоты и единственное настоящее расхождение

WIN-U1 стоит ровно на базе — синхронизации не требует. **WIN-U2 отстаёт на 49 коммитов**
(`git rev-list --count 352f96b0..4cf2b5dc` = `49`), но `git merge-base --is-ancestor 352f96b0
4cf2b5dc` проходит, то есть догоняется чистым fast-forward без слияния и без force. **Селектор
fast-forward НЕ делает** — это не догадка, а строка его собственного вывода `fast-forwards: false`.
Поэтому наряд WIN-U2 несёт явный пронумерованный порядок первых действий: claim → fetch → ff-only
до `4cf2b5dc` → readback дерева `58105a58` → и только потом работа; отказ ff-only = СТОП домой.
Названо и то, что первый Unity-запуск в этом слоте переимпортирует проект после 49 коммитов и
займёт время — чтобы это не приняли за отказ инструмента.

### Разделение файлов между дорожками — механизм, а не пожелание

В волне `g-1d84` полосы разошлись на `IntegratedHouse.unity` дважды, и слитые байты не запускал
никто. Поэтому в обоих нарядах написан ВЗАИМНЫЙ поимённый список чужих файлов, и оба говорят
«упёрся в чужой файл — СТОП домой», а не «аккуратно поправь».

- `cargo-hands`: `Core/Cargo/**`, `Network/CargoBody.cs`, `NetworkPlaySettings.{cs,asset}`, тесты
  `CargoHaul/CargoCarry/CargoLiftLaw/CargoPose/CargoWalkSpace/CargoClassSeam/TelekineticPickup/FirstPersonCarry`.
- `householder`: `Core/Householder/**`, `Core/Situations/**`, `Core/Multiplayer/CarryWorld.cs`,
  `World/Householder*`, `Settings/HouseholderWalkSettings.asset`, тесты `Householder*` и
  `CargoContactNoiseTests.cs`.
- Ничей: `Network/CargoBodyContact.cs` (чинён общей задачей до расхождения), сцена,
  `ProjectSettings/**`.

**Одна настоящая мина найдена и обезврежена текстом наряда:** `CarryWorld.cs` отдан дорожке
`householder`, но его `Advance` вызывают тесты дорожки `cargo-hands` (`CargoCarryTests.cs`, десятки
вызовов) и `Network/NetworkWalkerCourier.cs:160`. Поэтому наряд `householder` несёт прямой запрет
менять ПУБЛИЧНУЮ СИГНАТУРУ `CarryWorld.Advance` — иначе чужая полоса ломается на слиянии, а не в
своей работе.

**Вторая мина, поменьше:** данные реакции просились бы в `NetworkPlaySettings.asset` по образцу
класса груза из `t-scale-7`, но этот файл подписанной границей ставки принадлежит `cargo-hands`.
Наряд `householder` запрещает класть их туда и требует назвать выбранный дом с причиной.

### `t-cargo-1` — что перемерено в байтах

- Груз физически: класс `standard-cargo` `0.192 × 0.04 × 0.04`, масса `48`
  (`NetworkPlaySettings.asset:39-44`). Полудлина `0.096`, полувысота `0.02`.
- **Третья координата захвата УЖЕ ПОСЧИТАНА И ВЫБРОШЕНА.** `CargoGeometry.TryRaycast` пересекает
  вертикальную плиту (`:110`, `IntersectSlab(localOriginY, directionY, -halfHeight, halfHeight …)`)
  и получает `near`, но наружу отдаёт только `localGripX` (`:114`) и `localGripZ` (`:115`).
  Полувысота класса существует с `t-scale-7`. Это самый полезный факт наряда: строка 1 задачи не
  требует новой геометрии, она требует вернуть уже вычисленное.
- **Обход центра масс сделан НАРОЧНО, и код сам называет причину.** `CargoBody.cs:224-227` бьёт
  боковой тягой на высоте `worldCenterOfMass.y` с комментарием «tilt is frozen, so the solver would
  spend every step fighting itself»; `:232-238` шлёт подъём через центр масс с комментарием «roll
  and pitch that the unchanged tilt lock must then reject»; `:244-245` даёт момент только вокруг Y.
  Снимаются заморозки — исчезает основание обхода.
- **Заморозки живут в ТРЁХ местах, и одно из них литерал, а не константа:** `CargoBody.cs:24-25`
  (константа `TiltConstraints`), `:83-84` (**флаги выписаны литералом при создании тела**), `:158`,
  `:175`. Правка одной константы оставила бы `:83` живым — названо заранее, гейт поставлен на поиск.
- `CargoPull` (`:31-48`) не имеет точки по высоте вовсе и несёт момент одним числом.
- Тест строки 6 существует и его не надо писать: `CargoHaulTests.cs:95`
  `TwoHandsPullingOppositeEndsOppositeWays_TurnTheCargoWithoutMovingIt`.

**Физика провисания названа наряду ЧИСЛАМИ, чтобы исполнитель не вложился не туда.** Вертикальное
плечо захвата ±`0.02` м, горизонтальное ±`0.096` м — почти в пять раз больше. Провисание, которое
он описал, рождается прежде всего от переноса ПОДЪЁМНОЙ силы в точку захвата на конце бруска, а не
от третьей координаты: вертикальная сила на горизонтальном плече и есть тангаж. Третья координата —
подписанная строка и обязана появиться, но она меньший источник наклона. Это вывод из измеренных
чисел, а не предпочтение ноги, и в наряде он помечен как таковой.

### `t-react-1` — что перемерено в байтах

- `Householder.cs` 1674 строки, `AuthoritativeHouseholder.cs` 383.
- **Одна реакция сегодня стоит: ПЯТЬ членов в пяти перечислениях** (`HouseholderOccupation:5`,
  `HouseholderPriority:15`, `HouseholderCauseName:42`, `HouseholderNoticedFactName:52`,
  `HouseholderEventName:62`), **ТРИ ветки `switch`** (решатель `:487-505`, событие→факт+время жизни
  `:1021-1023`, причина→факт `:1667-1668`) и **ОДНО поле времени жизни в трёх файлах**
  (`Householder.cs:663,691`, `World/HouseholderWalkSettings.cs`, `Settings/…asset:21`).
- **ПОПРАВКА К ПОДПИСАННОЙ КАРТОЧКЕ, названная вслух, а не молча исправленная.** `t-react-1`
  `done_when` 2 говорит «размазана по ШЕСТИ перечислениям плюс ветка решателя». Перемерено: пять
  перечислений и три ветки. Требование строки не меняется, карточка не правится, но цифра в наряде
  верная, а не переписанная.
- Ещё шесть перечислений реакция ИСПОЛЬЗУЕТ, но не расширяет (`HouseholderPhysicalActionName`,
  `HouseholderActionTargetName`, `HouseholderMovementPace`, `HouseholderResponseKind`, две фазовых):
  их члены общие, новая реакция их переиспользует. Готовый словарь физического действия уже есть.
- **Строка 5 карточки подтверждена первой рукой:** `default.householder.json` — 21 строка и ТОЛЬКО
  распорядок (`walk-route` 12 с, `wait-at-route-point` 3 с). Ни реакции, ни порога, ни времени
  жизни там нет.
- Вход дорожки — слой из `t-situation-1`; в наряд вставлена дословная запись про порядок, слияние и
  вытеснение, чтобы её не изобрели заново.

### Форма обоих нарядов

Оба под контракт 36, режим ПРОБА, `done_when` свёрнут в **три** строки (потолок контракта),
шесть/пять строк задачи разложены по ним таблицей `проверка происхождения` без потерь. У каждого —
свой откат к дереву `58105a58`, свой гейт-поиск с ожидаемым выводом, свой запрет предъявлять
зелёные ворота как поведение игры.

**У `t-cargo-1` отдельно написано то, чего нет у второго наряда:** его строку 5 судит ГЛАЗ
владельца, поэтому светлое закрытие ей недоступно по букве волны, работа исполнителя кончается на
«построено, опубликовано, откат исполнен», и врать про «работает как в R.E.P.O.» в возврате прямо
запрещено. Также запрещено писать «поведение не изменилось»: оно ОБЯЗАНО измениться, вопрос — где
именно и насколько.

## state_changes

1. Заведена карточка наряда `c-exec-g-5a7c-cargo-1-001` (`to: executor`, `for: t-cargo-1`,
   `track: cargo-hands`, `slot: WIN-U1`, `basis: 4cf2b5dc`, `status: ready`).
2. Заведена карточка наряда `c-exec-g-5a7c-react-1-001` (`to: executor`, `for: t-react-1`,
   `track: householder`, `slot: WIN-U2`, `basis: 4cf2b5dc`, `status: ready`).
3. Написаны тексты нарядов `work/c-exec-g-5a7c-cargo-1-001-call.md` и
   `work/c-exec-g-5a7c-react-1-001-call.md`.
4. `t-cargo-1` и `t-react-1` остаются `open`: наряд не выдан, пока нет расписки.
5. `NOW.md`, `g-5a7c` и текст ставки не трогать.
6. Одним `osctl leg close` сохранить этот RESULT и добавить log ровно один раз в журналы `g-5a7c`,
   `t-cargo-1`, `t-react-1` и обеих новых карточек нарядов.

## captures

- **Карточка `t-react-1` называет шесть перечислений, а их пять.** Карточка не правится (её текст
  подписан), поправка живёт в наряде и здесь. Если `review` этой волны будет чинить формулировки —
  вот место.
- **WIN-U3 и WIN-U4 тоже стоят на `352f96b0`** и отстанут дальше. Карточки не стоит; но третий
  наряд в эту волну потребует того же явного ff-шага.
- Удалённая `refs/heads/slot/win-u1` осталась на `43648b2d`, локальная — на `4cf2b5dc`. Селектор
  читает локальное, выдача не ломается.
- **Две дорожки — это два Unity-редактора и два чата исполнителей одновременно.** Довод против
  двойной волны (`bet-g-5a7c-wave-2` `## against`, вторая половина) назвал это заранее: множитель —
  владелец, и если волна встанет, она встанет в чате, а не в коде. Порог смерти (5) считает ноги:
  общая задача плюс первые задачи обеих дорожек — не больше двенадцати, иначе вторая дорожка на
  паузу. Потрачено: 3.

## decisions_needed

Нет. Число одновременных дорожек — два — его слово 2026-08-10, и «выдай наряды на обе дорожки» —
его слово сегодня.

## play_check

- 1 recite: done — шесть строк `t-cargo-1`, пять строк `t-react-1`, границы по файлам из `## tracks`
  и все пять пунктов `evaluator` сведены
- 2 owner inputs (owner): done — новых фактов от него не требуется; порядок выдачи он назвал сам
- 3 do the work: done — оба наряда написаны по контракту 36; продукт перемерен в байтах по обеим
  поверхностям, слоты перемерены настоящим селектором, взаимные границы написаны поимённо
- 4 self-check: done — каждая строка обеих карточек разложена таблицей происхождения; расхождение
  карточки `t-react-1` с байтами названо вслух; отставание WIN-U2 названо числом и снабжено
  порядком действий
- 5 close: checkpoint — расписки исполнителей по построению вне этой ноги; обе задачи остаются
  `open`, обе карточки `ready`

## Гейты

- **G1.** Два одновременных корня — ровно по одному на дорожку, лимит `NOW.track_wip_limit: 2` не
  превышен; общая задача закрыта, дорожки законно расходятся.
- **G2.** Обе задачи служат активной ставке; ничего вне её не втянуто.
- **G3.** Аппетит не тронут: наряды закрывают ровно подписанные строки, ни одна не расширена.
- **G5.** Ни одна задача здесь не закрывается; закрытие — отдельные ноги после публикации.
  `t-cargo-1` заранее объявлена НЕдоступной для светлого закрытия.
- **G7.** Ничего продуктового или геймплейного этой ногой не решено: физика провисания названа
  числами как измеренный факт, а КАК её строить — оставлено продуктовому плану.
- **G9.** Карточки задач и ставки не редактировались.
- **evaluator, пункт 3.** Исполнен формой выдачи: полный текст отдаётся вложением/пастой, и первая
  строка возврата обязана быть распиской. До неё наряд не выдан.

## log

обе дорожки волны открыты одновременно впервые в ставке: два наряда от одной базы 4cf2b5dc в два слота, с взаимными поимённо написанными границами по файлам и найденной миной — CarryWorld отдан householder, но его Advance вызывают тесты cargo-hands, поэтому смена публичной сигнатуры прямо запрещена; для груза перемерено, что третья координата захвата уже вычисляется лучом и просто выбрасывается, что заморозки живут в трёх местах и одно из них литерал мимо константы, и что провисание рождается переносом ПОДЪЁМА в точку захвата, а не третьей координатой — плечи 0.096 против 0.02; для хозяина перемерено, что реакция стоит пяти членов в пяти перечислениях, трёх веток switch и поля в трёх файлах, а карточка называет шесть — поправка вынесена вслух; WIN-U2 отстаёт на 49 коммитов и селектор fast-forward не делает, поэтому порядок первых действий написан по шагам

## next

1. **Владелец отдаёт каждый наряд своему исполнителю** — полный текст, и первая строка возврата
   обязана быть распиской. Без расписки наряд не выдан, и это стоило первой волне круга работы.
2. Возвраты приходят HOME по одному; каждый закрывается своей ногой Направления после публикации.
3. **`t-react-1` закрывается светлым путём, если все её строки перемеряются по байтам.**
4. **`t-cargo-1` светлым путём НЕ закрывается**: её строку 5 судит его глаз. После публикации ему
   нужно взять груз за конец и сказать своими словами — только тогда задача закрывается.
5. Порог смерти (5) считает ноги: потрачено 3 из двенадцати на общую задачу плюс первые задачи
   обеих дорожек.
