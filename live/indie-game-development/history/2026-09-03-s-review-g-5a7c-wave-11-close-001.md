# RESULT — s-review-g-5a7c-wave-11-close-001

direction: indie-game-development
play: review
node/task: g-5a7c / bet-g-5a7c-wave-11
date: 2026-09-03
outcome_kind: review — одиннадцатая волна закрыта `partial`: три её обещания владельцу из четырёх выдержали опровержение, четвёртое опровергнуто байтами продукта, и самая дорогая часть волны в её собственную карту не входила

close: full — вердикт вынесен свежим чатом, не тем, что вёл волну. Каждая несущая строка перемерена первой рукой на `2d2c27ff` (продукт) и на `10f55a7a` (направление): прогон тестов сделан этой ногой лично, ссылки продукта сверены четырьмя командами, префабы и ассеты прочитаны по guid, записка чата волны проверена и в двух местах опровергнута.

owner_approved: |
  Этот RESULT НЕ несёт нового слова владельца и ни одной строки на нём не строит.

  Цитируются только уже закоммиченные его слова:

  - **«берём Б»** (2026-09-02) — волна закрывается с названными долгами приёмки, стенд строится
    ПОСЛЕ review;
  - **«груз может там упасть на мышь, ударить её сбоку. Когда ударяет, она отлетает. Это работает
    в мультиплеере»** (2026-09-02) — приёмка удара по мыши;
  - **«я вижу смысл, мне нравится, можем по ней работать»** (2026-09-03) — план демо принят как
    карта нарезки;
  - **«да»** (2026-09-02) — сохранение отложенного production batch до после этого review.

  **Выбор следующего узла ему НЕ навязан: он вынесен решением `d-what-comes-after-wave-11-001`
  и ждёт его слов.**

evidence: |
  **БАЗИС, СВЕРЕННЫЙ ЧЕТЫРЬМЯ ССЫЛКАМИ.**
  `main = dev = origin/main = origin/dev = 2d2c27ff7e15430590fe75565c57d4a4b697b2f6`,
  дерево чисто (`git status --porcelain` пуст до и после прогона).

  **ПРОГОН ТЕСТОВ СДЕЛАН ЭТОЙ НОГОЙ, А НЕ ПРОЦИТИРОВАН.**
  `dotnet test tests/TunnelCrew.Core.Tests` → **819 пройдено / 0 не пройдено / 0 пропущено**.
  Замер до волны был `752 / 752 / 0`. **Порог `kill_by 6` не сработал: +67 тестов, ни один не
  погашен.** Единственные `Assert.Ignore` в проекте — два условных, в `Assets/Tests/**`, оба
  срабатывают только на машине без сетевого адреса, кроме loopback.

  **ЧЕТЫРЕ ИЗ ШЕСТИ ПОРОГОВ ПЕРЕПРОВЕРЕНЫ ПУСТЫМИ ПОИСКАМИ, А НЕ ПО ОТЧЁТУ.**

  1. `kill_by 1` (источник вида «событие отняло N секунд») — поиск по `Assets/TunnelCrew/**` на
     `eventpenalty|penalty|penalise|penalize|spendreserve|deductseconds|losetime` → **ноль**.
     `S` выводится: `HouseholderTimeReserve.cs:135-137` считает его из буфера и запаса.
  4. `kill_by 4` (PuppetMaster потребовал трогать `Probe`) — **не сработал**:
     `TunnelCrew.Probe.asmdef` ссылается только на `TunnelCrew.Core`, `noEngineReferences: true`,
     диффа asmdef за всю волну ноль.
  5. Ветвление поведения по режиму — поиск на `Mode ==` даёт только вычисление самого режима внутри
     `HouseholderTimeReserve.cs:86-87`; ни один потребитель на нём не ветвится.
  6. Гашение тестов — см. прогон выше.

  **ПОРОГ 3 СРАБОТАЛ, НО ОТВЕТИЛА НА НЕГО НОГА, А НЕ ВЛАДЕЛЕЦ.** Строка звучала: «Unity не собирает
  Humanoid-аватар — СТОП и вопрос владельцу». Аватар не собрался, и нога взяла Mixamo Y Bot вместо
  того, чтобы остановиться. Замерено сегодня: `Quaternius/Casual_Male.fbx.meta` по-прежнему несёт
  `animationType: 2` и `avatarSetup: 0` — переимпорта не было; а `Anton/homeowner.fbx.meta` несёт
  `animationType: 3` и `avatarSetup: 1` — то есть **тело, ради которого разворачивалась вся ветка,
  было валидным Humanoid всё это время**. Три префаба `Mixamo_YBot*` лежат в поставочной
  `Art/Bodies/Prefabs` и не используются: `PresentationSettings.asset` даёт хозяину
  `_puppetPrefab` guid `65cc9d73…` (= `Householder_Puppet.prefab`), а guid Y Bot — `f8f93ec8…` —
  не встречается в настройках вовсе.

  **ЧЕТЫРЕ ЗАЯВЛЕНИЯ ЗАПИСКИ ЧАТА ВОЛНЫ ПРОВЕРЕНЫ; ДВА ОПРОВЕРГНУТЫ.**

  | заявление записки | проверка |
  |---|---|
  | `i-second-lab-surface-blocked-by-owner-policy-001` протухла | **подтверждено** — её собственный текст несёт снятие блокировки; ретируется этим RESULT |
  | `i-observation-lab-no-longer-mirrors-house-loot-001` протухла, «в MouseLab дома нет» | **ОПРОВЕРГНУТО: названа не та лаборатория.** Улика про стенд наблюдения. `HouseholdLab.unity` существует и по-прежнему несёт `FirstHouse`; ноль вхождений — в `MouseLab.unity`, о котором улика не говорит |
  | `i-the-bench-map-...` протухла | **ОПРОВЕРГНУТО ПО СУЩЕСТВУ: улика не протухла, а выросла.** `docs/householder-walking-bench-map.md` даёт одно совпадение на `observation\|bench\|стенд` (это заголовок) и ноль на `MouseLab`/`lobby`. Вход в стенд с тех пор стал только через лобби — карта стала неверной ещё в одном месте |
  | `i-unity-test-runs-cost-five-minutes-...` не проверена | **подтверждено** — число не перемерялось; помечается как непригодное для цитирования |
  | «Пробел 1» наряда стенда: не сказано, что клиентская кукла выключена | **ОПРОВЕРГНУТО ЦИТАТОЙ:** наряд, строка 70, дословно — «Client остаётся Kinematic, collision masks пустые» |
  | «Пробел 2» наряда: не сказано, чем ставится удар | **подтверждено** — в разделе «что решается при нарезке» три пункта, удара среди них нет |

  **И ТРЕТИЙ ПРОБЕЛ НАРЯДА, КОТОРОГО ЗАПИСКА НЕ НАЗВАЛА.** Наряд оставляет открытым вопрос
  «не свести ли стенд с `ReachLab`» — а закрытая задача `t-one-lab-he-can-set-up-himself-1`
  строкой 7 уже ответила на него: **«не сводить»**. Наряд переоткрывает решённое.

  **МЕХАНИКА КЛИЕНТСКОЙ КУКЛЫ — ПО БАЙТАМ.** `Presentation/HouseholderBalanceBody.cs`,
  `SetAuthority(bool)`: без авторитета `collisionLayers = 0`, `groundLayers = 0`,
  `PuppetMaster.Mode.Kinematic`. Клиент **рисует полученное**, а не симулирует.

  **ШАГ ФИЗИКИ ВЕДЁТ РОВНО ОДНУ КУКЛУ.** `Network/CargoBody.cs:36` —
  `private HouseholderBodyPresenter _householderPuppet;` — одно поле, не список; в цикле шага
  `:797` вызывается ровно оно.

  **У СЛУХА НЕТ ШВА, У ЗРЕНИЯ ЕСТЬ.** `GameRulesSettings.cs:263-265` — три радиуса
  (`_cargoRustleHearingRadius`, `_cargoKnockHearingRadius`, `_cargoHeavyNoiseHearingRadius`).
  Поиск по `Assets/TunnelCrew/**` на `occlu|linecast|raycast`, пересечённый со
  `hear|noise|sound|rustle|knock`, → **ноль**. У зрения при этом полноценный шов:
  `Network/HouseholderSightSource.cs` несёт `_fixedOccluderRoot` и `SightOutcome.Occluded`.

  **СЦЕНАРИЙ НА ЧЕТВЕРЫХ НЕ СУЩЕСТВУЕТ.** `Assets/Settings/PlayMode/` содержит ровно один файл —
  `HostAndOneClient.asset`.

  **КОНЕЦ УТРА — НЕ ПОЛЕ.** `leave-for-work` встречается в `Assets/**` пять раз: дважды как данные
  профиля (`nextRequiredState`, `timePressure`) и трижды в комментариях загрузчика. **Ни одной
  строки кода, приводящей хозяина в это состояние, нет.**

  **ПОСТАВОЧНОЕ ТЕЛО ХОЗЯИНА НЕ СОБИРАЕТСЯ ИЗ РЕПОЗИТОРИЯ.** `PresentationSettings.asset` даёт
  хозяину `_prefab` guid `ede90d4d…` = `Art/Bodies/Prefabs/Householder.prefab`. Этот префаб —
  **вариант**, и его источник назван guid `ed10d0e3cb6b7e245880793a95212970`. Этого guid не несёт
  **ни один из 1545 `.meta` в `Assets/**`** этого чекаута, и ни один отслеживаемый файл коммита
  `2d2c27ff`, кроме самого префаба. `Assets/TunnelCrew/Art` при этом отслеживается git целиком и
  джанкшенов не содержит. **Что НЕ проверено этой ногой: открывается ли проект в Unity — файловый
  замер запуском не подтверждался.**

  **ДВА ЗАПИСАННЫХ УТВЕРЖДЕНИЯ ОБ ОДНОМ ТЕСТЕ, И ОБА НЕВЕРНЫ.**
  `EitherMomentAnswersTheFirstNoiseAndTheSecondStillTakesHim` —
  `tests/TunnelCrew.Core.Tests/HandlersRecipeTests.cs:227`.

  - `bet-g-5a7c-wave-11`, блок `lenses`: «на `main` больше не находится». **Ложь.** Тест заведён
    `60e760d6` от 2026-08-30 и является предком базиса самой волны `ff37312f`.
  - `work/2026-09-03-demo-plan-to-october-5.md`, риск 12: «Красный тест слышимости стоит с 31.08 и
    не даёт утру лечь в main». **Тоже ложь.** Прогон под фильтром: **1 пройден, 0 не пройдено**.

  **КОММИТЫ, НА КОТОРЫХ СТОИТ ВЕРДИКТ (все — предки `2d2c27ff`, проверено `merge-base`):**
  `18b5d897` (цена помехи, вторая редакция после его приёмки), `c97ee735` (удар по мыши),
  `09cb3c9e` (стенд), `96402f95` (панель стенда, нашедшая дефект игры), `d03f6253` (равновесие).
  В направлении — `21e30b26` (запись первой оплаченной приёмки).

outcome: |
  # ВЕРДИКТ: `partial`

  ## 1. ЧЕМ ЗАКОНЧИЛОСЬ ОБЕЩАНИЕ ВОЛНЫ

  Цель волны обещала владельцу три вещи, которые он увидит сам, и одну общую строку заголовка.
  Опровергал каждую отдельно.

  | обещание | исход | на чём стоит |
  |---|---|---|
  | прячет одну и ту же вещь рано и поздно — поведение разное | **ВЫДЕРЖАЛО** | его глаз 2026-09-02; свободное утро — Kitchen, тесное — Living room, между прогонами менялось одно число |
  | мышь можно сбить летящим предметом, и она летит | **ВЫДЕРЖАЛО** | его глаз 2026-09-02 на стенде мыши; независимо — нога `t-lab-hover-and-two-mice-1` |
  | хозяин задевает что-то, теряет равновесие и пытается устоять | **НЕ ВЫДЕРЖАЛО ПРИЁМКОЙ** | доставлено `2d2c27ff`, доказано физикой на production-пути; глазом не смотрел никто |
  | «и оба тела наконец физические» | **ОПРОВЕРГНУТО БАЙТАМИ** | `Network/WalkSpaceProbe.cs:9-11` дословно: «A mouse is **NOT** a physical body and is never simulated». `Rigidbody` вне груза — ноль вхождений |

  **Поэтому не `met`.** Две строки из четырёх не закрыты, и одна из них закрыта быть не может —
  она опровергнута собственным кодом продукта.

  **И не `killed`.** Ни один из шести порогов смерти не сработал по существу; аппетит не кончился
  раньше работы — волна выдала больше задач, чем в неё нарезали.

  **И не `obsolete`.** Принятый им 03.09 план демо строит на выходах этой волны прямо и поимённо:
  раздел 1.1 (давление сжимает план), 1.2 (сбивание мыши), 1.3 (кукла и равновесие), раздел 4
  (стенд как растущий инструмент). Цель волны не отменена — она достигнута наполовину.

  **`partial`. Уцелевшая проверенная ценность, названная точно:** четыре механики в `main`, из них
  **две приняты его собственным глазом**, плюс стенд, который окупился, найдя дефект **игры**, а не
  себя (груз считался препятствием только по верху — коробка на полке загораживала пол под полкой;
  `96402f95`).

  ## 2. НЕОЖИДАННОСТЬ ПРОТИВ ПРОГНОЗА — ОДНА, И ОНА ПРО КАРТУ СТОИМОСТИ

  Блок `forecast` ставки нёс четыре утверждения. **Три опровергнуты, и ошибки разошлись в разные
  стороны.**

  | строка прогноза | что оказалось |
  |---|---|
  | «Задача 1 не строит нового слоя: правка в пределах `Probe/Householder` и профиля» | **выдержала** — `HouseholderTimeReserve.cs` лежит внутри `Probe/Householder`, числа в профиле |
  | «Задача 2 **не пишет нового кода вовсе**» | **опровергнуто** — `4e6c6914`: 326 строк исходника в семи файлах, в т.ч. `+167` в `HouseholderProfileLoader.cs` и `+114` в `HouseholderStanding.cs`; и понадобилась вторая нога `18b5d897`, потому что первая опубликованная версия не работала, и нашёл это он |
  | «Полоса `хозяин спотыкается` не трогает мозг ни одной строкой» | **опровергнуто** — `96402f95..2d2c27ff` меняет `Probe/Multiplayer/CarryWorld.cs` на `+44 / −28`. Граница при этом соблюдена точно (asmdef не тронут, `HouseholderPhysicalInterruption` несёт одни секунды), но прогноз был неверен |
  | «Тело мыши потянет камеру и столкновения — самая непредсказуемая задача волны» | **опровергнуто в обратную сторону** — снята без единой ноги: предмет был построен до выдачи наряда |

  **ГЛАВНАЯ НЕОЖИДАННОСТЬ, ОДНА И НАЗВАННАЯ ЧЕСТНО: ВОЛНА ОШИБЛАСЬ НЕ В РАБОТЕ, А В КАРТЕ
  СТОИМОСТИ — И САМАЯ ДОРОГАЯ ЕЁ ЧАСТЬ В КАРТУ ВООБЩЕ НЕ ВХОДИЛА.**

  Полоса `стенд` — **восемь опубликованных ног**, больше любой полосы механики — появилась
  четвёртой, посреди волны, его словом, и только после того, как **две полосы независимо упёрлись
  в её отсутствие**. Она же оплатила один из двух долгов приёмки в тот же день, как появилась.
  А единственная механика, оставшаяся непринятой, — ровно та, для которой стенд не построили.

  Волна нарезалась на шесть задач в трёх полосах. Закончилась **четырнадцатью карточками в четырёх
  полосах**: 12 `done`, 1 `dropped`, 1 `waiting`. Семь из четырнадцати заведены **задним числом**
  02.09 при сверке `main` — направление не видело половины собственной работы, пока не пересчитало
  продукт по файлам.

  ## 3. УРОЖАЙ ПО СЕМИ ЛИНЗАМ УСТАВА

  **1. Приближает ли демо 05.10, публичное демо 26.10 и платный выпуск?**
  Да, и впервые измеримо: четыре механики в `main`, и принятый план ставит на них Р1–Р3.
  **Но ни одной строки рубежа Р1 волна не закрыла.** Перемерено: конца утра нет (`leave-for-work` —
  только имя срока, глаголов четыре и ни один не значит «уйти»); локализации нет (пакета
  `com.unity.localization` в манифесте нет); сценария на четверых нет; своих звуковых файлов под
  `Assets/TunnelCrew` — **ноль**; `Feel` в `IntegratedHouse.unity` — четыре упоминания; сборка
  одной командой есть (`tools/unity-build.ps1` → `Assets/Editor/PlayerBuildCommand.cs`), но номера
  версии «дата + коммит» и папки выдачи в ней нет.

  **2. Понятность и качество для игрока?**
  Наполовину, и половина названа. Давление читается его глазом — принято. Равновесие глазом не
  проверял никто, и это единственная строка волны, которую волна сознательно оставила долгом.

  **3. Реально ли одному закончить и поддерживать?**
  Картина изменилась, и это главный вывод волны о собственной способности: **инструмент наблюдения
  стоит столько же, сколько механика.** Восемь ног стенда против четырёх ног механики. Это надо
  считать в замахе, а не обнаруживать при сверке.

  **4. Новый одобренный канон?**
  Не прибавилось ни строки. `EXP-20260831-001` остаётся единственным неизменяемым входом. Экспорт
  документа демо не пришёл, и он держит **все** ноги содержания — это записано и в плане.

  **5. Платный продукт и коммерческое знание?**
  Для самой волны `not_needed`. Но review нашёл, что две карточки противоречат уставу от 02.09:
  `i-steam-appid` («страница НЕ НАЧАТА», `review_when` 2026-08-15) и
  `i-steam-demo-gates-unverified` (`review_when` 2026-08-20, спрашивает про слот Next Fest).
  Устав прямо говорит: «ревью страницы Steam не прошло, игра в фестивале не участвует».

  **6. Надёжность работы владельца?**
  Разведение полос по файлам сработало: за волну **ни одного конфликта слияния** — ровно та мина,
  ради которой четвёртая полоса была отклонена при нарезке. И отдельно: его собственный отложенный
  production batch (`idea-production-batch-after-wave-11-review-001`, его «да» 02.09) ждал именно
  этого review и сегодня разблокирован.

  **7. Проще или сложнее?**
  Сложнее на один плагин, как и объявлено. Взамен вырезано десять пунктов. **Чего не объявляли:**
  тринадцатая сцена всё-таки построена (`MouseLab`) — и он сам это разрешил, отменив мой запрет.
  Замер «двенадцать сцен», которым запрет обосновывался, был верен как диагноз и неверен как вывод.

  ## 4. ПРОВЕРКА СПИСКА ВЫРЕЗОВ: ОДИН РОБКИЙ, ОДИН БЕЗ ОСНОВАНИЯ, ДВА С НАСТУПИВШИМ ВОЗВРАТОМ

  **РОБКИЙ ВЫРЕЗ — №7, «Слышимость и звук».** Волна ответила: «Предмет исполнен десятой волной…
  **Работы не осталось**; остались две несведённые карточки, и это учёт, а не работа».
  **Это неверно, и опровергается за одну команду:** у слуха три радиуса в настройках и **ноль**
  упоминаний окклюзии во всём слуховом пути, тогда как у зрения полноценный шов
  (`_fixedOccluderRoot`, `SightOutcome.Occluded`). Работы осталось на **второй шов размером со
  зрение**, и принятый план ставит его на критический путь (Р16, 1.5, Д4, вход «фон комнат» к
  29.09). Отдельно: правило `the-sound-decision-is-taken-at-every-cut` предлагает ровно **два**
  законных ответа — взять или отложить вслух. Волна дала третий, которого правило не знает.

  **ВЫРЕЗ БЕЗ ОСНОВАНИЯ — №2, «Полярный импульс».** Отложен «потому что у мыши сегодня нет тела
  вовсе». Тело оказалось построенным до выдачи наряда, задача снята, а принятый план решением Р1
  вообще снимает потребность в физическом теле мыши. **Главный глагол мыши отложен по причине,
  которой больше нет**; план назначает его на Д3 к рубежу Р2 (18.09).

  **ВОЗВРАТ НАСТУПИЛ — №3, «Конец захода, победа и поражение».** Вырезаны как «не решены
  владельцем». Он их решил 03.09, приняв план: утро кончается выходом за дверь, экран результата,
  ★ по опозданию. Вырез снят его словом, и это сказано здесь, а не обнаружится при нарезке.

  **ВОЗВРАТ НАСТУПИЛ ЧАСТИЧНО — №4, «Стресс как содержание».** План описал его целиком (1.3, Р8,
  Д7). Механизм готов и авторится из профиля. Содержание по-прежнему заперто за экспортом Concept
  Lab — очередь держит `knowledge/what-the-time-pressure-spec-still-owes.md`, `read_by: shape`
  проверен и настоящий.

  **ВЫРЕЗ №10, «Виктор», СЕГОДНЯ НЕ ВЫРЕЗ, А ФАКТ НАОБОРОТ.** «Рабочим телом больше не будет» — а
  он им и есть, и кукла построена прямо на нём. Хуже: поставочный префаб этого тела — вариант
  отсутствующего в репозитории источника (см. `evidence`). Запасной путь плана (риск 9) называет
  «текущее низкополигональное тело», а в сборке стоит не оно.

  **ПРОПУЩЕННЫХ ВЫРЕЗОВ НЕ НАШЁЛ.** Девять из десяти строк несут причину и условие возврата — это
  лучший список вырезов, что видело это направление.

  ## 5. ДЕРЕВО

  **Этим review я дерево не меняю ни одной карточкой** — узловая перестройка идёт плеем `map`, и
  предлагаю её ему как один из вариантов следующего узла. Но review обязан назвать, что четыре
  карточки держат маршрут, который устав закрыл сам:

  - **`g-7b42` «Демо на фестивале»** — вся цель про октябрьский Steam Next Fest 2026, а устав от
    02.09 говорит: «закрыт как маршрут… игра в фестивале не участвует. Это установленный факт».
    Её `done_when 3` требует публичного демо не позднее **15.09**.
  - **`g-8a41` «Внешние плейтесты»**, `by: 2026-09-15` — требует действующего Steam Playtest
    «с середины августа». Его нет, а принятый план заменяет механизм целиком (раздел 7: жена,
    четверо, холодные).
  - **`g-2b7f` «Страница в Steam»**, `by: 2026-08-31` — её собственный пункт 8 уже сработал, и это
    записано в уставе, но не в карточке.
  - **`i-full-run-node-to-be-authored-001`** — дыра между ядром и фестивалем, оставленная
    намеренно. Принятый план её заполняет шестью рубежами, а узла под неё до сих пор нет.

  ## 6. ПРОГНОЗ

  `direction_forecast` пересчитан и остаётся **`no_basis`**. Численная вероятность не публикуется:
  эмпирического знаменателя нет, а устав это прямо запрещает без калибровки. Одиннадцать волн дают
  распределение исходов волн, а не reference class для даты.

  **Что калибруется честно — это разрыв, и он датирован.** Из строк рубежа Р1 (11.09), которые
  можно проверить чтением, **ни одна не закрыта**: конца утра нет, таблицы строк нет, сценария на
  четверых нет, стенд хозяина не построен, номера версии в сборке нет. Восемь дней до Р1.

  ## 7. ЧТО РАСХОДИТСЯ МЕЖДУ ПЛАНОМ ДЕМО И СОСТОЯНИЕМ ПРОДУКТА

  План не правлю ни в одном символе — всё ниже уходит уликами. Семь расхождений, каждое
  перемерено сегодня:

  1. **Риск 12 плана называет несуществующий блокер.** Красного теста слышимости нет: прогон
     819/819/0, названный тест проходит поодиночке. Полосу «утро» ничто не держит.
  2. **Р16 недооценён на целый шов.** «Звук слышен по расстоянию и через стены» — у слуха нет
     окклюзии вовсе, и вводить её значит переписать каждый тест, утверждающий поведение чистого
     радиуса, потому что правда меняется, а не уточняется.
  3. **Р2 описывает не то, что построено.** «Каждая копия крутит свою куклу от того же импульса» —
     клиентская кукла кинематическая с пустыми масками и рисует полученную позу корня. Запасной
     путь плана — не запасной, а основной для полного падения, и он требует его слова.
  4. **Р3 «шаг физики не трогать» не выдержит второй куклы.** Поле одно, не реестр.
  5. **Д2 «четыре экрана»: дешёвая половина не сделана.** Сценария MPPM на четверых нет как файла.
  6. **1.5 и Р4 требуют Виктора вне сборки, а он и есть тело в сборке** — и его источник не лежит
     в репозитории.
  7. **Д5/Р9 «конец утра» недооценён по объёму.** Это не поле в профиле: нужен пятый глагол,
     терминальная строка распорядка, конец раунда и экран результата. У Д5 запасного пути нет.

state_changes: |
  **СТАВКА ЗАКРЫТА, ЧЕТЫРЕ ПОЛОСЫ РАСПУЩЕНЫ.**
  - `bet-g-5a7c-wave-11` → `closed/`, `status: partial`.
  - Полосы `утро давит`, `мышь становится телом`, `хозяин спотыкается`, `стенд` **распущены**:
    после закрытия ни одна живая карточка не несёт поля `track` (проверено поиском). `NOW.md` не
    меняется — он указывает на узел `g-5a7c` (активен) и несёт его число `track_wip_limit: 4`;
    занятых полос ноль.

  **И РАСПУСТИТЬ ИХ БЫЛО НЕЧЕМ, ПОКА НЕ УБРАЛИ ДВАДЦАТЬ КАРТОЧЕК — ЭТО НАЙДЕНО ПРИ ПРИМЕНЕНИИ.**
  Тринадцать задач и семь нарядов волны несли терминальный статус (`done`/`dropped`), но **остались
  в живых** `cards/` и продолжали нести поле `track`. То есть полосы формально жили после того, как
  вся их работа кончилась, и `osctl context` считал бы их занятыми при следующей нарезке. Все
  двадцать убраны в `closed/` этим review; содержание ни одной не менялось, в журнал каждой ушла
  одна строка о причине переезда.

  **ШЕСТЬ КАРТОЧЕК НАРЯДОВ НЕ ПУСКАЛ В `closed/` СОБСТВЕННЫЙ ДЕФЕКТ ФОРМЫ.** Поле `description`
  было длиннее 120 знаков, и валидатор отказывал. Исходный текст каждой **сохранён в её журнале
  дословно** и заменён однострочным; смысл не менялся. Это ремонт формы, а не редактура.

  **ПОСЛЕДНЯЯ ЖИВАЯ ЗАДАЧА ПОЛУЧАЕТ ТЕРМИНАЛЬНЫЙ ИСХОД.**
  - `t-he-loses-his-balance-and-tries-to-stay-up-1` → `closed/`, `status: dropped`.
    **`done` запрещён гейтом G5:** строка приёмки «причина читается его глазом» не закрыта.
    «Снята» здесь значит «волна её больше не держит», а не «брошена»: механика опубликована и
    доказана физикой, долг переходит уликой, наряд написан.

  **ДОЛГ ПРИЁМКИ ПОЛУЧАЕТ НОСИТЕЛЯ, ЧТОБЫ НЕ ПРОПАСТЬ.**
  - новая улика `i-the-balance-acceptance-is-unpaid-and-its-order-is-written-001`, `route: shape`.

  **СЕМЬ РАСХОЖДЕНИЙ ПЛАНА И ПРОДУКТА — УЛИКАМИ, `route: shape`:**
  - `i-both-records-of-the-hearing-test-are-wrong-001`
  - `i-hearing-has-no-occlusion-seam-and-sight-does-001`
  - `i-the-client-puppet-does-not-simulate-001`
  - `i-one-field-carries-the-only-stepped-puppet-001`
  - `i-no-four-screen-scenario-exists-001`
  - `i-the-shipped-householder-body-is-not-in-the-repository-001`
  - `i-the-morning-has-no-end-and-it-is-a-verb-not-a-field-001`

  **ДЕРЕВО — ОДНА УЛИКА, `route: map`:**
  - `i-three-parked-nodes-and-two-cards-describe-a-closed-route-001`

  **РЕТИРОВАНО (2 карточки):**
  - `i-second-lab-surface-blocked-by-owner-policy-001` → `done`: её собственный текст несёт снятие
    блокировки, и владелец 02.09 переопределил форму ещё раз. Учёт, не работа.
  - `q-two-return-claims-rest-on-unverified-owner-words-001` → `done`: спрашивает о его словах
    от 2026-08-11. По собственному правилу направления
    `knowledge/owner-word-boundary-is-asked-the-same-day.md` такой вопрос через 23 дня
    неотвечаем, а сама карточка объявляет утверждение неНЕсущим. Умерла правилом, а не ответом.

  **ОСТАЮТСЯ ОТКРЫТЫМИ И НАЗВАНЫ ПОИМЁННО:**
  - `q-core-rework-authorization-lives-only-in-the-product-001` — вопрос владельцу от 2026-08-17,
    без ответа семнадцать дней. Принятый план переработки ядра не содержит; вопрос больше ничего
    не блокирует, но снимать его без его слова я не стал.
  - `i-observation-lab-no-longer-mirrors-house-loot-001` — записка назвала не ту лабораторию;
    в журнал улики уходит поправка, статус `open` сохранён.
  - `i-the-bench-map-names-the-console-and-misses-the-observation-bench-001` — не протухла,
    выросла; поправка в журнал, статус `open`.
  - `i-unity-test-runs-cost-five-minutes-for-one-second-of-work-001` — число непроверено;
    поправка в журнал, ссылаться на него как на факт запрещено.

  **ЗНАНИЕ — ТРИ ФАЙЛА, У КАЖДОГО НАСТОЯЩИЙ ПОТРЕБИТЕЛЬ:**
  - `knowledge/an-eye-acceptance-needs-its-surface-cut-in-the-same-wave.md` — `read_by: shape`,
    `review`.
  - `knowledge/a-cut-that-says-no-work-left-must-name-the-measurement.md` — `read_by: shape`,
    `review`.
  - `knowledge/a-forecast-line-is-a-cost-claim-and-must-be-refutable.md` — `read_by: shape`,
    `review`.
  - и **слияние**: в `knowledge/the-sound-decision-is-taken-at-every-cut.md` добавлен третий,
    незаконный ответ поимённо, со ссылкой на замер этой ноги.

  **ПРОГНОЗ:** `direction_forecast` → `as_of: 2026-09-03`, `status: no_basis` сохранён, drivers и
  closed_facts пересобраны по замерам этой ноги.

  **РЕШЕНИЕ ВЛАДЕЛЬЦУ:** `d-what-comes-after-wave-11-001` — три варианта следующего узла с
  рекомендацией. **Ни одного CALL не открыто:** плей велит открывать наряд только после того, как
  узел выбран его словом.

  **НАРЯД ЗАКРЫТ:** `c-review-g-5a7c-wave-11-001` → `done`.

captures: |
  - **ДВА ЗАПИСАННЫХ УТВЕРЖДЕНИЯ ОБ ОДНОМ ТЕСТЕ РАЗОШЛИСЬ В ПРОТИВОПОЛОЖНЫЕ СТОРОНЫ, И ОБА БЫЛИ
    ЛОЖНЫ.** Волна сказала «теста нет», план сказал «тест красный и блокирует». Тест есть и
    зелёный. Оба утверждения были сделаны про то, что проверяется одной командой за секунду.
  - **ПОЛОВИНУ СОБСТВЕННОЙ РАБОТЫ ВОЛНА УВИДЕЛА ТОЛЬКО ПРИ СВЕРКЕ ПРОДУКТА ПО ФАЙЛАМ.** Семь ног из
    четырнадцати заведены задним числом 02.09. Это второй раз в этом узле (первый — четвёртая
    волна, четырнадцать ног без карточек). Класс не лечится вниманием: продолжения исполнитель
    называет сам по правилу плея, и направление узнаёт о них только если специально считает.
  - **ЗАПИСКА СДАЧИ СМЕНЫ ОКАЗАЛАСЬ ТОЧНОЙ ТАМ, ГДЕ МЕРИЛА КОД, И НЕТОЧНОЙ ТАМ, ГДЕ СУДИЛА
    КАРТОЧКИ.** Все её продуктовые замеры подтвердились; три из шести её суждений об уликах и
    наряде — нет. Это довод за форму «сдача смены = факты», а не «сдача смены = вердикт», и форма
    была соблюдена: записка сама назвала себя уликой.
  - **ПОРОГ «СТОП И ВОПРОС ВЛАДЕЛЬЦУ» БЫЛ СЪЕДЕН НОГОЙ, И ЭТО СТОИЛО ВЕТКИ.** Аватар не собрался,
    нога заменила модель вместо остановки, и вся цепочка Quaternius → Y Bot → отложенный переезд
    оказалась ненужной. Порог не бракован — он не был исполнен, и ни один гейт этого не заметил.
  - **«ЗАКРЫТО» И «УБРАНО» — РАЗНЫЕ СОБЫТИЯ, И РАСХОЖДЕНИЕ НАКАПЛИВАЛОСЬ ВСЮ ВОЛНУ.** Двадцать
    карточек несли терминальный статус и жили в `cards/`. Ни один гейт этого не показывал, потому
    что каждая по отдельности выглядела закрытой. Видно стало только при роспуске полос — то есть
    ровно на том шаге, который review и существует, чтобы делать.

decisions_needed: |
  **ОДНО, И ОНО ЕГО: `d-what-comes-after-wave-11-001` — какой узел следующий.**

  Три варианта, рекомендация — А.

  **А. Нарезать двенадцатую волну по списку рубежа Р1 принятого плана.** Рекомендуется.
  Р1 — 11.09, восемь дней. Ни одна его строка сегодня не закрыта. Стенд равновесия входит в Р1
  первым и закрывает единственный долг волны 11; наряд написан и ждёт выдачи в слот, отличный от
  U2. Ноги содержания при этом не выдаются — экспорт Concept Lab не пришёл, и план это запрещает
  сам; технические ноги (стенд, сеть, сборка, локализация) идут без него.

  **Б. Одна нога `map`: привести дерево в соответствие с уставом и принятым планом.**
  Три припаркованных узла держат закрытый октябрьский маршрут и прошедшие даты; узла под «полный
  заход демо» до сих пор нет, хотя план его описал шестью рубежами. Стоит одну ногу и не даёт ни
  одной строки продукта — а устав в разделе pre-mortem №3 прямо называет это способом провалиться.

  **В. Его собственный отложенный production batch.** Шесть вопросов выпуска и организации работы,
  которые он сам отложил словом «да» до после этого review: статус Steam page и имени, сетевой
  порог на четверых, минимальный звук для публичного демо, источники ассетов и бюджет, release
  cleanup, форма работы. Разблокирован сегодня.

  **Почему А:** дата 05.10 — единственный жёсткий ограничитель, и Р1 в восьми днях. Б и В не
  производят продуктовой улики; оба помещаются внутрь ближайшей сверки 11.09, а Б — ещё и внутрь
  следующего review.

play_check: |
  1. **Опровержение (шаг 1).** Каждое из четырёх обещаний цели опровергалось отдельно; два
     выдержали его собственным словом, одно не закрыто приёмкой, одно опровергнуто кодом продукта.
     Прогноз сверен с реальностью построчно: три строки из четырёх неверны. Вердикт `partial`
     с названной неожиданностью.
  2. **Урожай (шаг 2).** Все семь линз устава получили вердикт, включая `not_needed` по пятой —
     и она же дала находку про две противоречащие уставу карточки.
  3. **Дерево (шаг 3).** Ни одной карточки узла не тронул; четыре несоответствия названы и
     отправлены одной уликой в `map`, перестройка предложена ему вариантом Б. Задач не заведено.
  4. **Вырезы (шаг 4).** Десять строк проверены поимённо: один робкий, один без основания, два с
     наступившим условием возврата, один перевёрнут фактом. Пропущенных не найдено.
  5. **Знание (шаг 5).** Три файла, у каждого настоящий `read_by`; плюс слияние в существующее
     правило про звук. Всё, что осталось рассуждением, ушло в `captures`.
  6. **Прогноз (шаг 6).** `no_basis` сохранён по уставу; пересобраны drivers и closed_facts,
     добавлен датированный замер разрыва до Р1. Предложено три будущих узла.
  7. **Закрытие (шаг 7).** Ставка закрыта, четыре полосы распущены, последняя живая задача
     получила терминальный исход, наряд закрыт. **CALL не открыт: выбор узла — его, и он вынесен
     решением.** Гейт G7 соблюдён — вердикт по узлу `g-5a7c` не выносился, узел остаётся активным.

  **ГРАНИЦЫ НАРЯДА СОБЛЮДЕНЫ.** Следующая волна не нарезана, задач не заведено, план демо не
  правлен ни в одном символе, содержание игры не достраивалось, `archive/**` не открывался.
  Продукт открывался только на чтение; единственное действие в нём — прогон headless-тестов, после
  которого `git status` остался пуст.

log: |
  одиннадцатая волна закрыта partial: из четырёх обещаний цели два выдержали его собственным
  глазом, равновесие осталось неоплаченным по его же выбору «Б», а строка «оба тела наконец
  физические» опровергнута байтами — WalkSpaceProbe дословно говорит, что мышь не физическое тело;
  прогноз ошибся три строки из четырёх в обе стороны, и главное: самая дорогая часть волны — восемь
  ног стенда — в её карту не входила и появилась только после того, как две полосы независимо
  упёрлись в отсутствие инструмента; четыре полосы распущены, семь расхождений плана и продукта
  ушли уликами, три знания заведены, прогноз остаётся no_basis, выбор следующего узла вынесен ему
  решением

next: |
  **Ждём его слова на `d-what-comes-after-wave-11-001`.** До него ни один наряд не открывается —
  это требование плея, а не осторожность.

  **Что он должен знать до выбора, тремя строками:**

  - долг волны 11 один, он про равновесие глазом, и наряд под него **уже написан** — платится
    первой же ногой любого из трёх вариантов, кроме В;
  - экспорт документа демо из Concept Lab **не пришёл**, и он держит все ноги содержания в любом
    варианте; технические идут без него;
  - до рубежа Р1 (11.09) восемь дней, и ни одна его строка сегодня не закрыта.

END_OF_FILE: live/indie-game-development/history/2026-09-03-s-review-g-5a7c-wave-11-close-001.md
