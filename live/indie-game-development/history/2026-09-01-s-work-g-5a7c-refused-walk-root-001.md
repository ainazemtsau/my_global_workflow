# RESULT — s-work-g-5a7c-refused-walk-root-001

direction: indie-game-development
play: work
node/task: g-5a7c / t-a-fact-knows-who-made-it-1, полоса `слух`
track: слух
date: 2026-09-01
outcome_kind: work — корень всех замираний дня найден и он один: отказанный путь не слышен никем, хозяин ждёт вечно; TTL-починка верна, но сменила ярлык, а не поведение

close: light — because корень перевыведен направлением первой рукой на байтах: три живых отчёта прочитаны из файлов и положены рядом, строка ожидания и её комментарий прочитаны в коде, и полным поиском доказано, что `IsRefused` читает ровно одно место. Ничего не закрывается как достигнутое; наряд `-002` закрыт как сделанный по своему предмету, задача остаётся открытой. G5-чат не открывался.

owner_approved: |
  Не требовалось. Владельцу сообщается фактом, и **первым делом — что предыдущее утверждение
  направления было неверным**: сегодня ему было сказано «замирание уйдёт». Оно не ушло.

outcome: |
  **НАПРАВЛЕНИЕ СКАЗАЛО ВЛАДЕЛЬЦУ «ЗАМИРАНИЕ УЙДЁТ». ОНО НЕ УШЛО — СМЕНИЛСЯ ЯРЛЫК.**
  Это первое, что должно быть названо, и оно названо.

  **ЧТО ВИДНО, ЕСЛИ ПОЛОЖИТЬ ТРИ ЖИВЫХ ОТЧЁТА НОГИ РЯДОМ** — прочитано направлением из файлов, а не
  из её сводки:

  ```
  38.83s   (3.00, 0.67)   Moving        → WalkToward "Alarm clock" (2.85, 4.25)
  64.64s   (2.92, 3.92)   sit-up        → WalkToward RoutePoint (-4.30, 3.80)
  126.68s  (2.92, 3.92)   sit-up        → WalkToward RoutePoint (-4.30, 3.80)   ← ВСЁ ТО ЖЕ
  ```

  **Он шёл к будильнику, встал в 0.33 м от него и не сдвинулся ни на сантиметр за 62 секунды —
  ни вперёд, ни назад.** Позиция совпадает с утренним зависанием до второго знака. **Сменился
  только ярлык:** был `InvestigateLanding`, стал `DailyRoute`. В обоих случаях печатается
  `doing: WalkToward` — **он всё это время «идёт».**

  **КОРЕНЬ — ОДНА СТРОКА, НАПИСАННАЯ НАМЕРЕННО, С ХОРОШИМ ДОВОДОМ И БЕЗ ГРАНИЦЫ.**
  `Householder.cs:1304`: отказанный путь возвращает «не прибыл», и **хозяин ждёт**. Довод верен —
  считать отказ прибытием значит дать ему «выполнить» дело там, где он стоит. **Но вторая половина
  собственного комментария не выполняется: «причина лежит на пути для того, кто спросит» —
  перевыверено полным поиском, `IsRefused` читает ровно одно место, эта же строка. Никто не
  спрашивает.** При этом `HouseWalkRefusal` уже различает причины, включая `FurnitureInTheWay` —
  ровно то слово, которое соседний слот видел в этом же прогоне.

  **ОДНА ПРИЧИНА ОБЪЯСНЯЕТ ВЕСЬ ДЕНЬ.** 31 августа — три сантиметра от точки прослушивания, сто
  секунд. Утром — 0.33 м от будильника, шестьдесят. Сейчас — та же точка, шестьдесят две.
  **Три ярлыка, одно место, один молчаливый отказ.**

  **ЧЕТЫРЕ ДИАГНОЗА ПОДРЯД, И ТОЛЬКО ЧЕТВЁРТЫЙ БЫЛ ДЕФЕКТОМ — НО НЕ ЭТИМ.** Слепота, отсутствие
  рук, владение фактом, ответ переживший факт. TTL-починка настоящая, тесты есть, она остаётся.
  **Она сменила слово, печатающееся рядом со стоящим человеком.**

  **НАРЯД `-002` ЗАКРЫТ КАК СДЕЛАННЫЙ ПО СВОЕМУ ПРЕДМЕТУ, ЗАДАЧА — НЕТ.** Нога выполнила то, что ей
  поручили, и честно назвала остаток. **Остаток оказался исходной задачей.** Строка приёмки 4
  требовала «за контрольную минуту хозяин ни разу не замирает» — не выполнена, и это не
  формальность: владелец не может смотреть на игру, где хозяин стоит столбом, каким бы словом это
  ни называлось.

  **И ВЛОЖЕНО УКАЗАНИЕ, КОТОРОГО НЕ БЫЛО В ПРЕЖНИХ НАРЯДАХ:** правка не закоммичена — коммитить
  **первым действием**, до всякой новой работы. Сегодня в соседнем слоте едва не потеряли готовую
  ногу ровно из-за незакоммиченного состояния.

evidence: |
  **ПРОЧИТАНО НАПРАВЛЕНИЕМ ИЗ ЖИВЫХ ФАЙЛОВ** (`C:\Users\Anton\AppData\LocalLow\DefaultCompany\
  GasCoopGame\householder-why-20260901-*.txt`):

  - `080345` — `real time 38.83s`, `he stands (3.00, 0.00, 0.67)`,
    `doing: WalkToward toward SituationPoint "Alarm clock" at (2.85, 4.25)`;
  - `080411` — `64.64s`, `(2.92, 0.00, 3.92)`, `busy with: get-up-from-the-bed /
    sit-up-on-the-edge`, `doing: WalkToward toward RoutePoint at (-4.30, 3.80)`;
  - `080513` — `126.68s`, **`(2.92, 0.00, 3.92)`, та же фаза, та же цель, то же `facing`**;
  - в `080411` также: `Alarm clock — thinks on Kitchen counter (HE SAW IT THERE, at 47.3s)` —
    перенос состоялся, дело выполнено.

  **ПЕРЕВЫВЕДЕНО ПО КОДУ** на `slot/win-u2` = `b4b010c1` (та же база механизма):

  - `Householder.cs:1298-1305` — комментарий «A REFUSED WALK IS NOT A FINISHED ONE… He waits
    instead, and the reason is on the walk for whoever asks» и `if (path.IsRefused) return false;`;
  - `grep "IsRefused\|\.Refusal"` по `Assets/TunnelCrew` за вычетом `HouseWalk.cs` и `Cargo` →
    **ровно одна строка: `Householder.cs:1304`.** Никто не спрашивает;
  - `Probe/House/HouseWalk.cs:16-35` — `enum HouseWalkRefusal`, значение
    **`FurnitureInTheWay = 3`** существует.

  Из возврата ноги, как улика: четыре изменённых файла, `744 / 743 / 1` с известным чужим красным,
  `check.ps1` зелёный, повторное независимое ревью без блокеров, правка не закоммичена.

state_changes: |
  СОЗДАНО (1 улика + 1 наряд + 1 документ):
  - `i-a-refused-walk-is-heard-by-nobody-001` → `open`: корень всех замираний дня, с таблицей трёх
    симптомов и доказательством единственного читателя;
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-003` → слот **WIN-U4**;
  - `work/2026-09-01-answer-a-refused-walk-must-be-heard.md`.

  ЗАКРЫТО КАК СДЕЛАННОЕ (1 наряд):
  - `c-exec-g-5a7c-a-fact-knows-who-made-it-1-002` → `done` по своему предмету: TTL-починка верна и
    сохраняется. **Задача не закрыта: строка приёмки 4 не выполнена.**

  БЕЗ ИЗМЕНЕНИЙ:
  - `t-a-fact-knows-who-made-it-1` остаётся `active`;
  - `t-noise-reaches-by-distance-1` остаётся `active`, ждёт третьего захода G5;
  - `t-morning-content-1`, `t-carry-back-1`, `t-everything-lands-1` — без изменений.

captures: |
  - **НАПРАВЛЕНИЕ ЗА ОДИН ДЕНЬ НАЗВАЛО ЧЕТЫРЕ ПРИЧИНЫ ОДНОГО СИМПТОМА, И ТРИ БЫЛИ НЕВЕРНЫ.**
    Каждая опиралась на настоящий замер и каждая была правдоподобна. **Правило-кандидат: когда
    симптом переживает свою починку, первым делом сравнивать КООРДИНАТЫ, а не ярлыки — ярлык
    меняется от любой правки, место не меняется.** Здесь совпадение позиции до второго знака было
    видно сразу и не было замечено ни ногой, ни направлением до сегодняшнего чтения.
  - **«ПРИЧИНА ЛЕЖИТ ДЛЯ ТОГО, КТО СПРОСИТ» — ЭТО НЕ МЕХАНИЗМ, А НАДЕЖДА.** Комментарий написан
    добросовестно, значение `FurnitureInTheWay` заведено, и **не спросил никто ни разу.**
    **Правило-кандидат: если код объявляет, что оставляет информацию «для того, кто спросит»,
    проверять поиском, что спрашивающий существует. Иначе это молчаливый отказ.**
  - **НОГА СДЕЛАЛА СВОЙ ПРЕДМЕТ И ЧЕСТНО НАЗВАЛА ОСТАТОК, А ОСТАТОК ОКАЗАЛСЯ ЗАДАЧЕЙ.**
    Это не её ошибка: наряд разрешил не блокироваться на геометрии, и она не блокировалась.
    **Формулировка «не блокируйся на этом» оказалась шире, чем задумывалась.**
  - **ЗАМИРАНИЕ ПЕРЕЖИЛО ПОЧИНКУ И БЫЛО ПОДАНО ВЛАДЕЛЬЦУ КАК УСТРАНЁННОЕ.** Направление сказало
    «замирание уйдёт» до того, как это стало фактом. **Правило-кандидат: обещание владельцу о том,
    что он УВИДИТ, давать только после живого замера, а не после разрешения наряда.**

decisions_needed: |
  Ни одного владельческого.

play_check: |
  1. OPEN — возврат ноги с TTL-починкой и честно названным остатком.
  2. WORK — три живых отчёта прочитаны направлением из файлов и положены рядом; совпадение позиции
     найдено; корень перевыверен по коду, включая доказательство единственного читателя `IsRefused`;
     наряд закрыт по предмету, задача оставлена открытой по невыполненной строке приёмки.
  3. (owner) — не запрашивался; неверное обещание направления названо прямо.
  4. CLOSE — RESULT, применение, коммит; переизданный наряд уходит владельцу.

log: |
  корень всех замираний дня найден и он один: отказанный путь возвращает «не прибыл», хозяин ждёт
  вечно, а вторая половина собственного комментария — «причина лежит для того, кто спросит» — не
  выполняется никем, потому что IsRefused читает ровно одна строка; три симптома дня оказались
  одним местом с точностью до второго знака и тремя разными ярлыками; TTL-починка верна и остаётся,
  но она сменила слово рядом со стоящим человеком, а не сдвинула его

next: |
  Переизданный наряд `c-exec-g-5a7c-a-fact-knows-who-made-it-1-003` уходит владельцу для
  **WIN-U4**, первым действием — локальный коммит TTL-починки.

  Волна: голова без вердикта третий заход, ходьба мертва, замирание в работе, публикация ждёт.
  Девятая волна по-прежнему ждёт дерева, на которое можно смотреть.
