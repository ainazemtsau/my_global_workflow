# RESULT — s-repair-g-6b13-call-named-absent-basis-001

play: repair
direction: indie-game-development
node: g-6b13
task: a-1b
date: 2026-08-02

## outcome

Наряд на фундамент был дефектным: он назвал существующим то, чего на объявленной базе нет.
Исполнитель остановился до плана и вернул вопрос. Наряд исправлен, ничего не потрачено.
Это четвёртый бунс одного класса и первый, который ничего не стоил.

## evidence

**Дефект подтверждён прямыми командами:**

- `git ls-tree -r main -- Assets/TunnelCrew core/TunnelCrew.Core.csproj` — ПУСТО. На `c75015a8`
  нет ни каркаса, ни проекта;
- `git branch --contains e0000c5b` — только `slot/win-u2`. Каркас задачи `a-1` никогда не сливался
  в `main`; туда уехало исключительно урезание проверок;
- состав `slot/win-u2`: `955e5e62` (отклонённая работа) поверх `e0000c5b` (принятый каркас).

**Как сессия ошиблась, названо точно:** она проверяла файлы в рабочем каталоге
`GasCoopGame_win-u2`, а базой наряда объявила `main`, и не сверила одно с другим. Формулировка
«проверено первой рукой» была правдой о ТОМ каталоге и ложью о ЗАЯВЛЕННОЙ базе.

**Это нарушение уже написанного правила, а не новая ошибка.** Запись
`i-direction-to-product-call-contract-001` после второго бунса предписывает: «до неё каждый named
path проверяется на объявленной базе». Правило было. Сессия его не исполнила.

**ИСПОЛНИТЕЛЬ ОТРАБОТАЛ ОБРАЗЦОВО, и это надо записать отдельно.** Он остановился ДО плана; не
создал ни одного файла; не стал догадываться и молча расширять объём до «заодно и каркас построю»;
освободил слот и подтвердил его доступность; вернул ровно один вопрос с двумя названными вариантами
решения. Это и есть поведение, которого требует строка наряда «упёрся — стоп и вопрос домой», и
здесь она сработала впервые.

**Цена бунса: ноль.** Ни строки кода, ни потраченного времени сборки — только поправка наряда.
Три предыдущих бунса этого класса стоили работы; этот не стоил ничего, потому что стоп сработал.

## state_changes

Наряд `c-exec-rules-layer-and-single-walker-001` переписан в редакцию 2: состав базы назван честно
(на `c75015a8` из новой игры нет ничего), создание каркаса в этой же задаче разрешено ЯВНО, план
вырос с пяти ответов до шести — добавлен пункт про заводимый каркас, с обязательным
`noEngineReferences: true` на asmdef. `NOW.md`: новая запись
`i-call-named-path-absent-on-declared-basis-004`, маршрут `maintenance`; заметка наряда обновлена.

## captures

Коммит `e0000c5b` (принятый каркас задачи `a-1`) остаётся заброшенным на `slot/win-u2` — каркас
переписывается заново, потому что это восемь файлов шаблонного текста и писать своё чище, чем тянуть
через сдвинувшуюся базу. Ветка `slot/win-u2` вместе с отклонённой работой ждёт отдельной уборки и
никого не блокирует.

## decisions_needed

Нет.

## play_check

- Дефект подтверждён командами на объявленной базе, а не рассуждением.
- Вина названа своей: ошибка сессии, не исполнителя, и сказано, в чём именно механика ошибки.
- Правильное поведение исполнителя записано явно — иначе в следующий раз стоп покажется дороже
  догадки.
- Запись отправлена в `maintenance`, потому что правило существовало и не сработало: это дефект
  контура выпуска нарядов, а не разовая невнимательность.
- Наряд исправлен, а не выпущен заново под новым именем: работа по нему не начиналась.

## FRICTION

«Проверено первой рукой» без указания, ГДЕ именно проверено, — формулировка, которая скрывает этот
класс ошибки, а не ловит его. Четвёртый случай.

## log

наряд на фундамент назвал несуществующее на объявленной базе; исполнитель законно остановился до плана, наряд исправлен, потрачено ноль

## next

Запуск редакции 2 в свободном слоте. Возврат плана домой, затем постройка.

END_OF_FILE: live/indie-game-development/history/2026-08-02-s-repair-g-6b13-call-named-absent-basis-001.md
