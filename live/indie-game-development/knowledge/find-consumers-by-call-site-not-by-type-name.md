---
name: find-consumers-by-call-site-not-by-type-name
description: Поиск потребителей типа по ЕГО ИМЕНИ пропускает вызовы через out var, где имени нет вовсе. Искать надо по местам вызова — имя метода в тексте есть всегда.
read_by: каждая нога переноса между сборками; каждая нога, меняющая публичную поверхность типа
written: 2026-08-30

# Потребителей типа ищут по месту вызова, а не по имени типа

**ЗАМЕР.** Нога `c-exec-g-5a7c-bedrock-boundary-1-001` переносила три типа в отдельную сборку.
Дописать ссылку пришлось **семи** сборкам, а не пяти. Две — `TunnelCrew.Profiles` и
`TunnelCrew.Networking` — берут типы через `out var`: **имя типа встречается в их файлах НОЛЬ раз.**

Нашёл их только компилятор, ошибкой `CS0012`.

## Что из этого следует

**Текстовый поиск по имени типа даёт ЛОЖНОЕ «потребителей больше нет».** Это не редкий случай и не
экзотика: `out var`, `var`, лямбды и цепочки методов прячут имя типа штатно.

Работает поиск **по местам вызова**: имя метода (`TryFindRoomAt`) в тексте есть даже там, где тип
выведен. Это и есть способ подмести класс целиком, а не первый его случай.

## ВТОРОЙ ЗАМЕР ТОГО ЖЕ КЛАССА, 2026-09-03: РЕГИСТР

Направление считало объём выреза цели «вынести груз» и назвало наряду **8 мест** в
`NetworkWalkerCourier.cs`. Считало по заглавному `Delivery` — по имени типа.

Исполнитель WIN-U4 при вырезе нашёл ещё **13 строк**: параметры и локальные переменные со
строчной буквы (`deliveries`, `deliveryPoint` и подобные), обслуживающие ту же фичу. Его слова:
«перемерять находки аудита надо без учёта регистра — локальные переменные и параметры,
обслуживающие фичу, в счёт по типу не попадают».

**Это тот же класс, что `out var`, только дешевле и чаще.** Имя типа пишется с заглавной, а всё,
что его носит по коду, — со строчной. Поиск по типу видит объявления и не видит обслуживание.

**Правило дополняется одной строкой: считать без учёта регистра.** `grep -i`, а не `grep`. И
по-прежнему — по местам вызова, а не по имени.

## Почему это ещё и довод против самописных сканеров

У нас записано правилом: самописный сканер как улика запрещён. Здесь видно, почему именно.
Сканер по имени типа нашёл бы пять сборок, доложил бы «всё», и сборка встала бы после слияния.
**Компилятор нашёл семь и не может ошибиться в эту сторону.**

END_OF_FILE: live/indie-game-development/knowledge/find-consumers-by-call-site-not-by-type-name.md
