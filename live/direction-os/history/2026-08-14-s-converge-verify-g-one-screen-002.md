RESULT s-converge-verify-g-one-screen-002 (call: c-converge-verify-g-one-screen-002)
direction: direction-os   play: converge-verify   node: g-one-screen

outcome: |
  Набор `work/converge-g-one-screen-v3.md` **НЕ ПРОШЁЛ** вторую проверку.
  `verify_target: build` · **complete=PASS · smuggling=FAIL**.

  Полнота впервые в истории узла выдержала: все 11 строк `done_when` и goal-блок,
  разобранные на атомы, доходят до строк набора; ни один атом не потерян, ни одна
  цитата не искажена при переносе. Первая проверка (2026-08-12) валила ОБЕ атаки.

  Смертельная находка одна, и она в том же классе, что убил два предыдущих круга:
  вывод ноги, надетый на слово владельца. Строка **W06** («Сводка становится входным
  экраном направления»), тег `acceptance` — то есть строка, которую `shape` копирует
  в наряд ДОСЛОВНО, — сослана на «его слова 2026-08-13 «да, берём» на показанном
  описании «заходишь в направление, попадаешь на его сводку»». Описания с такими
  словами не существует ни в одном файле репозитория, кроме самой строки W06.

  ЭТО ВТОРОЙ ПРОВАЛ УЗЛА (счёт по `history/`). Сработал потолок шага 4 плея:
  бумажная фаза закрыта, в `converge` не возвращено НИ ОДНОЙ строки, поверхность
  заморожена уликой, и написан ОДИН владельческий вопрос — `d-one-screen-paper-phase-001`.

evidence: |
  ## СМЕРТЕЛЬНОЕ — подтверждённое опровержение

  **[1] W06 подписан цитатой, которой не существует. (smuggling=FAIL)**

  Строка (`work/converge-g-one-screen-v3.md:126`):
  `| W06 | Сводка становится входным экраном направления | acceptance | answered |
  его слова 2026-08-13 «да, берём» на показанном описании «заходишь в направление,
  попадаешь на его сводку»; механику подтверждает M7 |`

  Что на диске в названном источнике
  (`history/2026-08-13-s-map-g-one-screen-split-001.md:23`), дословно:
  `- сводка каждого направления — «да, берём» (после двух его правок: сначала
  «не только утром, когда угодно… больше графически», затем «своя у каждого
  направления, никакой общей»);`

  Его «да, берём» сказано про СУЩЕСТВОВАНИЕ сводки, и обе названные при этом правки —
  про время захода и про «своя у каждого». Ни слова про то, КУДА попадаешь, входя
  в направление.

  Поиск по всему репозиторию (без `archive/**`):
  `grep -rn "попадаешь на его сводку\|заходишь в направление\|входным экраном\|входной экран" --include=*.md .`
  → четыре попадания: `history/2026-08-14-s-converge-g-one-screen-004.md:96`,
  `work/converge-g-one-screen-v2.md:17`, `work/converge-g-one-screen-v2.md:162` —
  все три пересказывают ЗАМЕР M7, — и `work/converge-g-one-screen-v3.md:126`, то есть
  сама W06. **Фраза, приписанная владельцу, существует только внутри строки,
  которая на неё ссылается.**

  Набор противоречит сам себе: его же §SIGNOFF (`:204-205`) регистрирует это самое
  «да, берём» как сказанное «на описании сводки» и «вторую верни, первую отдельной
  вещью» — реестр подписей ноги фразы про входной экран НЕ содержит.

  Как это выросло. В замороженном v2 тот же факт стоял строкой W05
  (`work/converge-g-one-screen-v2.md:162`):
  `| W05 | Сводка становится входным экраном автоматически; это принято сознательно | — | answered | M7 |`
  — тег `—` (в наряд не едет), источник M7 (следствие кода). В v3 строка получила тег
  `acceptance` И владельческую цитату. Механический факт превращён в требование,
  подписанное несуществующим словом.

  Замер M7 при этом ВЕРЕН и перевыведен своими руками: `panel/app/app.js:951` берёт
  умолчанием ПЕРВЫЙ готовый раздел (`const first = (direction.sections || []).find((s) => s.ready)`),
  а `dashboard` стоит первым в `serve.py:122` `SECTIONS`. Но это СЛЕДСТВИЕ включения,
  а не его решение: M7 объясняет, что произойдёт, и не даёт права требовать, чтобы
  это произошло. По G7 «что он видит, войдя в направление» — продуктовое, всегда его.

  Ремонт по плею (демоция в `open` с названным отвечающим) вернул бы во владельческие
  открытые строки то, чего в наборе, по его же заявлению (`:175`) и по наряду, НЕТ.
  Ниже потолка это была бы отбитая строка; на потолке это владельческий вопрос.

  Ни один атом `done_when` за W06 не стоит: строка покрывает НОЛЬ владельческих
  условий — она добавлена ногой целиком.

  ## НАЙДЕНО, НО НЕ СМЕРТЕЛЬНО

  **[2] W24 — `acceptance`-строка, отдающая часть своей области `open`-строке.**
  `W24 | Ни одного машинного кода; где имени нет — «без имени» | acceptance | answered |
  D1. Область шире десяти мест M5: `app.js:87` и `:122` печатают слаг направления —
  их закрывает W23`. W23 — `open`. Правило самого набора (`:117`) и `os/plays/converge.md:16`
  («one citing `open` is itself `open`») требуют разрезать такую строку; W11/W11a и W13
  разрезаны, W24 — нет. Перевыведено: `app.js:87` `el("a", …, d.id)` и `:122`
  `el("div", "title", d.id)` печатают слаг направления, и человеческого имени у
  направления нет НИГДЕ — `serve.py:182-185` строит запись из имени каталога. То есть
  за W23 стоит не дешёвый параметр PLAN, а схемный вопрос (где живёт имя направления),
  а он упирается в W31 «новый тип файла состояния не заводится» и критерий 4 хартии.
  Исполнитель, получивший W24 дословно, обязан показать имя направления вместо кода,
  а поля с этим именем не существует.

  **[3] M3: столбец коммитов не воспроизводится.** Дерево и база сверены: `origin/main`
  сегодня ровно `2c19a2a6` — как объявлено в наборе, так что расхождение не от дрейфа.
  Набор объявляет (`:68`): «в таблице стоит форма с временем». Перевыведено на `2c19a2a6`:

  | направление | в наборе | `--since=…T00:00:00` | `--since=…` без времени |
  |---|---|---|---|
  | indie-game-development | **541** | 546 | 540 |
  | solmax | **9** | 11 | 9 |
  | direction-os | 1 | 1 | 1 |

  Число 541 не даёт НИ ОДНА форма объявленной команды: `--no-merges` → 542,
  `--until=2026-08-14T00:00:00` → 543, без `--until` → 546. А ячейка solmax (9) взята
  формой БЕЗ времени, то есть ровно той, которую набор объявил неиспользуемой.
  Вывод M3 при этом выживает и даже крепнет: чувствительность к форме даты реальна
  (540 против 546, 9 против 11), поэтому отклонение коммитов как носителя «что сделано»
  остаётся обоснованным. Ломается не вывод, а требование done_when 10 — «число без
  способа счёта фактом не является» — нарушенное в самом наборе, который это требование
  и несёт.

  **[4] W13a — ссылка на строку, которой в наборе нет.** M3 (`:55`) пишет «какое окно
  нужно, спрашивает строка W13a». Механический счёт: строк 41, идентификаторы
  W01…W39 плюс W11a и W17a; W13a среди них нет. Остаток от редакции ДО его подписи
  2026-08-14 (окно закрылось в W12/W13). Существо покрыто, указатель повис.

  **[5] §GLOSSARY: «ЦЕЛИКОМ» — термин карточки, читаемый двумя способами, в таблицу
  не попавший.** Goal-блок: «видит это направление целиком»; несёт его строка W34
  (`acceptance`/`answered`). Два прочтения ровно те же, что у «детально»: ОХВАТ (все
  стороны направления представлены) и ПЛОТНОСТЬ (всё сразу на одном экране). Под вторым
  «целиком» сталкивается с подписанным «заход даёт мало и просто» (W20) — тем же
  столкновением, ради которого «детально» получило строку глоссария, три колонки
  разбора И его подпись 2026-08-14. Близнец остался неразобранным.

  **Названные существительные `done_when`, о которых карточка не спрашивает** (обязательный
  шаг 2 плея):
  - **«внутрь»** (`done_when` 9, «клик по строке ведёт внутрь»). Фраза родилась в
    замороженном v1 (`work/converge-g-one-screen.md:120`, строка W01) про СВЕТОФОР ПО
    НАПРАВЛЕНИЯМ, где строка = направление, и подписана «МНЕ НРАВИТСЬЯ 1Й ВАРИАНТ».
    Тот экран отменён его словом 2026-08-13. Referent фразы исчез вместе с экраном:
    на сводке ОДНОГО направления строка — это отчёт, запись или цель, и куда ведёт
    клик по каждой, не спрашивает ни строка, ни глоссарий.
  - **«рядом»** (`done_when` 10, «рядом видно, из чего оно посчитано и каким способом»).
    Где именно сидит способ счёта — на экране или под «подробнее» — не спрашивает никто,
    а от ответа зависит, уживаются ли W27 и W20.

  **[6] Ребро к соседу, не названное.** W25 («Два записанных правила, объявляющих коды
  правильными, снимаются» — `panel/PLAN.md:126` и `:71`) тег `acceptance`. Сосед
  `g-eight-pages`, `done_when` 4, требует СНЯТЬ ТЕ ЖЕ ДВА ПРАВИЛА теми же номерами строк.
  Набор честно называет три ребра соседей (W36, W37, W38), это — четвёртое, и оно
  единственное, где два живых узла держат одно и то же `acceptance`. Снять правила
  можно один раз; второй узел получит строку, зелёную ещё до начала работы.

  **[7] W20 ссылается на D2 за свойством, которого у D2 нет.** D2 (`converge-g-one-screen.md:120`)
  несёт «мало и просто» и «подробнее» — проверено, строка резолвится дословно. Свойства
  «клик по строке ведёт внутрь» в D2 нет; оно пришло из `done_when` 9. TERM-PROPERTY RULE
  (`os/plays/converge.md:16`): строка ссылается на термин только за свойством из его строки,
  новое свойство переоткрывает и переподписывает.

  ## СНЯТЫЕ ПОДОЗРЕНИЯ — с причиной

  **Полнота (`complete=PASS`).** 11 строк `done_when` и goal-блок разобраны на атомы,
  включая запреты; каждый доходит до строки. Выборочно: 1→W01+W02+W03(+D6); 2→W07+W08
  (+W09 `open`, W10); 3→W11+W11a(`open`)+W15(`deferred`); 4→W12+W13+W14(`open`);
  5→W16; 6→W18+W19+W28; 7→W22+W26; 8→W24; 9→W20; 10→W27; 11→W28+W29. Goal-блок:
  две работы про одно направление→W01/W18, «когда угодно»→W07, «целиком»→W34,
  «графически»→W22, «никакой общей»→W02, «файлов не открывает»→W34, сценарий→W18,
  разграничение «свод — работа чата»→W35. Дословность цитат в строках сверена с
  карточкой: искажений нет — тот отказ прошлого круга не повторился.

  **Счёт строк — механический, не на глаз.** `grep -cE '^\| W[0-9]+[a-z]? \|'` → **41**.
  По тегам и статусам: `acceptance`/`answered` — **27**, `—`/`answered` — 3 (W30, W31, W32),
  `—`/`open` — **10**, `—`/`deferred` — 1 (W15). Итого 41. Заявление набора (`:173`)
  верно во всех числах; `acceptance`-строк, стоящих `open`, нет.

  **Его подпись 2026-08-14 записана ВЕРНО.** `history/2026-08-14-s-converge-g-one-screen-006.md:24-25`
  несёт дословно: **«ноги за окно, окно 30 дней, детально это охват, планы это волна»** —
  «в ответ на сообщение, перечислявшее ровно W12, W17a и W33». Отчёт закоммичен
  (`dc5a4126`), то есть слова лежат в состоянии, а не в чате. Одна фраза на три строки —
  законно: она дана на сообщение, перечислявшее ровно эти три.

  **Цена, названная ему при подписи, — правда.** Перевыведено: отчётов ног в окне
  2026-07-15…2026-08-14 у indie — **238** (`ls live/indie-game-development/history/`,
  отбор по дате в имени, `serve.py:428` `LEG_NAME`), у direction-os — 10 на дереве
  набора `04ce3c99` (сегодня 12, обычный дрейф), у solmax — 10. Число 238, которым
  куплена подпись W12 и на котором держится честное следствие «объём входа (W21)
  становится обязательным к решению», воспроизводится точно.

  **Замеры M1, M2, M4, M5, M6, M7, M8 — воспроизведены своими руками, все семь верны.**
  - M1: `serve.py:122-124` — `("dashboard","СВОДКА")` первым из девяти; `:129-130`
    `READY_SECTIONS` — те же девять минус `dashboard`; причина комментарием `:126-128`;
    ручка `:1036-1037`→`section_dashboard` `:980`; клиент `app.js:213-242`, роутинг
    `:896-899`, невидимость флагом `:877-884`. ✔
  - M2: `staleness_note` молчит при `behind == 0` (`serve.py:163-164`), `behind` =
    `git rev-list --count HEAD..origin/main` (`:174-175`). Демонстрация перевыведена:
    `git show main:live/direction-os/CHARTER.md | grep -c 'приостановлена как датчик'` → **0**;
    `git diff --shortstat main 04ce3c99 -- live/direction-os/` → **34 файла, 2309 вставок**
    (на сегодняшнем HEAD уже 39/2876). Приёмка удостоверяет ложь: `verify.py:113`
    требует `(stale is None) == (behind == 0)`. ✔ Честное ограничение тоже проверено:
    `osctl.py:179-180` выходит раньше перебора при отсутствии ветки, `:181-186` разбирает
    `git worktree list --porcelain`; ветки `slot/*` есть только у indie (1–4), у
    direction-os и solmax их нет. ✔
  - M4: `serve.py:360-374`/`:362` считает и исключает, `app.js:546-548` печатает одно
    число; живых записей — indie 44, direction-os 6, solmax 0. ✔
  - M5: все десять мест перевыведены построчно; `:271` подпись цели ВСЕГДА кончается
    кодом (`sub.textContent = (n.hook||"") + … + n.id`), `:254` `r.goal || r.id` и `:269`
    `n.label || n.id` ставят код ВМЕСТО имени. Оба правила резолвятся: `PLAN.md:126`
    «панель честно показывает id, пока их нет», `:71` «Машинное моноширинным…». ✔
    Независимое подтверждение вне ноги: `g-eight-pages` `done_when` 4 называет те же
    десять номеров и те же два правила.
  - M6: `grep -nE 'svg|canvas|<path|polyline|chart' panel/app/app.js` → ровно `:775` и
    `:807`, оба — `data.charter.*`; в `style.css` — ноль. ✔
  - M7: `app.js:951` умолчание = первый готовый раздел. ✔
  - M8: все пять поверхностей на диске — `serve.py:129-130`; `section_dashboard` `:980`,
    отбрасывание не-`call` на `:988`; `app.js:213-242`; `PLAN.md:104-107` и `:119`;
    `verify.py:124-125` (список готовых зашит поимённо). Обе приёмки реальны:
    `test_docs.py:233` `case_sections` сверяет план с кодом, парсер `:166-182` требует
    жирную метку «**Закрыт…**». ✔

  **Импорты born-closed резолвятся, все восемь.** D1 (`…converge-001.md:21`,
  «любые коды… я же не помню все эти коды»), D2 (`converge-g-one-screen.md:120`,
  дословные слова про «подробнее»), D3 (`PLAN.md:177` «Показывать скорость… нет»),
  D4 (`PLAN.md:35` «Панель ничего не оценивает и не советует»), D5 (`PLAN.md:58-82`,
  стиль 2026-08-06), D6 (`CHARTER.md`, граница премортема 5: «Достройка раздела,
  объявленного и выключенного ДО этого решения, есть починка»), D7 (`CHARTER.md`,
  мера премортема 1: «доля работы в систему приостановлена как датчик»),
  D8 (`…frame-001.md:70`, captures: «владелец назвал её первой ставкой и проектирует сам»).

  **Рёбра соседей W36/W37/W38 — проверены на диске и точны.** `g-no-silent-rot`
  `done_when` 2 называет `g-one-screen` носителем поимённо и перечисляет четыре вида
  (открытые узлы, происшествия, неистраченные наряды, незаданные вопросы) — W16 несёт
  один; `done_when` 4 требует сторожа над простым изложением; `g-eight-pages` `done_when` 1
  дословно приравнивает свои восемь к `READY_SECTIONS` («Восемь — это `READY_SECTIONS`»),
  и это равенство ломается при включении сводки, а `done_when` 5 по-прежнему несёт
  неподписанную оговорку ноги. Всё, что W38 обещал проверить, проверено и верно.

  **Чек-листа класса решений не применялось:** `live/direction-os/knowledge/` пуст,
  канона нет, а чек-лист, написанный этой ногой, по плею был бы `capture`, никогда стандартом.

state_changes: |
  1. ЗАМОРОЗИТЬ ПОВЕРХНОСТЬ УЛИКОЙ. В `live/direction-os/work/converge-g-one-screen-v3.md`
     сразу после заголовка (строка 1) вставлен блок-цитата «⛔ НЕ ПРОШЁЛ ВТОРУЮ ПРОВЕРКУ —
     БУМАЖНАЯ ФАЗА ЗАКРЫТА»: `complete=PASS smuggling=FAIL`, указатель на этот отчёт,
     названа единственная опровергнутая строка W06 и то, что остальные 40 выжили,
     и записано, что строить и переписывать файл нельзя до решения
     `d-one-screen-paper-phase-001`. Файл в `converge` НЕ возвращается.
  2. ЗАВЕСТИ ВЛАДЕЛЬЧЕСКОЕ РЕШЕНИЕ. Новая карточка `d-one-screen-paper-phase-001`,
     `_kind: decision`, `about: g-one-screen`, `_pos: 6`, с блоками `q` / `options` /
     `recommendation`: резать · делить в `map` · строить на отвеченном.
  3. ЗАКРЫТЬ НАРЯД. `c-converge-verify-g-one-screen-002` уходит в `cards/closed/`
     с причиной: проверка проведена, набор не прошёл, второй провал закрыл бумажную фазу.
  4. ЖУРНАЛ УЗЛА. В `cards/g-one-screen.md` строка от 2026-08-14 про второй провал
     и потолок, указателем на этот отчёт.
  5. ОТЧЁТ. Полный RESULT в `history/2026-08-14-s-converge-verify-g-one-screen-002.md`.
  6. `NOW.md` не трогается: ставки нет, узел остаётся `parked`.

captures:
  - Класс дефекта повторился третий раз подряд на одном узле: вывод ноги, надетый на слово владельца (v1 — светофор, v2 — вырезанные половины подписанных критериев, v3 — W06). Три случая одного шва — это уже не случай; при его решении стоит подумать, не заявка ли это в MAINTENANCE о том, что `converge` не умеет механически отличать «его слово» от «следствия замера».
  - У НАПРАВЛЕНИЯ нет человеческого имени нигде в состоянии: `serve.py:182-185` строит запись из имени каталога, `app.js:87` и `:122` печатают слаг. Это схемный вопрос, а не параметр PLAN, и он упирается в W31 и критерий 4 хартии.
  - Снятие `panel/PLAN.md:126` и `:71` держат два живых узла одновременно (`g-one-screen` W25 и `g-eight-pages` done_when 4). Кому оно принадлежит — вопрос `map`/`review`, не этой ноги.

decisions_needed:
  - q: |
      Узел `g-one-screen` провалил вторую проверку — бумажная фаза по правилу закрыта.
      Что делать дальше: резать узел, делить его в `map` или строить на том, что уже
      отвечено (40 строк из 41 выжили)?
    options: [резать, делить в map, строить на отвеченном]
    recommendation: |
      Строить на отвеченном. Ни одна находка не указывает на лишний объём (резать нечего)
      и ни одна — на неразрешённую развилку (делить нечего: настоящую развилку снял его
      же раздел 2026-08-13). Все семь находок — дефекты ссылок и подписей, а не размера.

play_check:
  - 1 Recite the claim: done — `verify_target: build`. Атаки заявлены: (1) каждый атом `done_when` доходит до строки, отвеченной или названной `open`; (2) каждая `answered`-строка стоит на источнике ВНЕ решавшей ноги.
  - 2 Attack completeness: done — **PASS**. 11 строк `done_when` и goal-блок разобраны на атомы, каждый доходит до строки; дословность цитат сверена с карточкой. Чек-лист класса решений не применялся: `knowledge/` пуст. Названы существительные, о которых карточка не спрашивает: «целиком» (нет в §GLOSSARY, близнец подписанного «детально»), «внутрь» (referent — отменённый светофор), «рядом» (место способа счёта относительно «мало и просто»).
  - 3 Attack smuggling: done — **FAIL**. W06 (`acceptance`/`answered`) стоит на цитате владельца, которой нет ни в одном файле репозитория; её собственный §SIGNOFF её не содержит; в замороженном v2 тот же факт стоял без тега с источником M7. Перевыведены все восемь замеров: семь верны, M3 в столбце коммитов не воспроизводится (541 не даёт ни одна форма объявленной команды; ячейка solmax взята объявленно-неиспользуемой формой).
  - 4 Close: done — второй провал узла (счёт по `history/`: `2026-08-12-s-converge-verify-g-one-screen-001.md` нёс `complete=FAIL smuggling=FAIL`). Потолок применён: в `converge` не возвращено ни строки, поверхность заморожена уликой, написан ОДИН владельческий вопрос. §SIGNOFF не пишется — он только при PASS. `shape` не открывается.
  - verify_target: build complete=PASS smuggling=FAIL

log: набор не пережил вторую проверку — строка про входной экран подписана несуществующей цитатой; второй провал закрыл бумажную фазу узла и оставил владельцу один вопрос

next: |
  awaiting_decision — `d-one-screen-paper-phase-001`. Третий бумажный круг открывают
  только его слова. Наряда исполнителю нет; `shape` не открыт.

END_OF_FILE: live/direction-os/history/2026-08-14-s-converge-verify-g-one-screen-002.md
