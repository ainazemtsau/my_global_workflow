RESULT s-shape-g-one-screen-001 (call: c-shape-g-one-screen-001)
direction: direction-os   play: shape   node: g-one-screen

Нога прошла в агентском CLI на этом репозитории и после выпуска этого RESULT стала собственным
писателем: `state_changes` применены командами `osctl` и закоммичены той же сессией.

outcome: |
  Узел `g-one-screen` стал активной ставкой направления. Аппетит, вход в направление, прочтение
  слова «целиком» и имена направлений на экране закрыты словом владельца, а не догадкой ноги.
  Ставка несёт шесть задач по половине сфокусированного дня, четыре реза, вердикт по каждой из
  пяти линз устава, три порога смерти событием (дат нет — устав снял срок) и первый наряд
  карточкой.

  Definition-of-Ready для `shape` закрыт ЕГО СЛОВОМ «а» от 2026-08-14, а не пройденной
  проверкой: набор требований провалил вторую проверку по контрабанде, и третий круг `converge`
  не открывался. Полнота набора при этом проверку прошла. Это названо вслух, а не подразумевается.

  Первый наряд `c-work-one-screen-canon-001` заведён и НЕ запущен: запуск его словом.

evidence: |
  **Его слова этой ноги, дословно.** На брифинг шага 2, где четырьмя вопросами были показаны
  аппетит, вход в направление, прочтение «целиком» и имена направлений, он ответил сначала
  одной буквой **«б»**. Одна буква на четыре вопроса не разрешается однозначно, поэтому нога
  не толковала её, а переспросила. Ответ:

  **«отвечал только на послежний вопрос а так принимаю все твои рекоменжации»**

  Отсюда, и только отсюда:
  - имена направлений — вариант (б) его собственным ответом: имена называет он, и предложенные
    в рекомендации «Игра» · «Система» · «Solmax» он не заменил;
  - аппетит — рекомендация (а): 3 ноги (≈3 дня);
  - вход в направление — рекомендация (а): вход ведёт на сводку;
  - «целиком» — рекомендация (а): ОХВАТ, а не плотность.

  На показанную ставку одним экраном — аппетит, шесть задач, полосы, рез, вердикты линз,
  критерии смерти, прогноз и довод против — он ответил **«да»**. Одобрение относится к
  показанному артефакту; ни одна строка ставки не выведена из фразы про рекомендации задним
  числом.

  **Перевыведено первой рукой на дереве a3f69ea3** (не взято на веру из замороженного набора):
  - графических примитивов в панели ноль: `grep -n 'svg\|canvas\|<path\|polyline\|chart'
    panel/app/app.js` → две строки, `:775` и `:807`, обе суть слово «charter» внутри себя;
    в `panel/app/style.css` совпадений ноль;
  - живых записей вида `issue`: indie 44, direction-os 6, solmax 0 (`ls live/<id>/cards/i-*.md`);
  - отчётов ног за 2026-07-15…2026-08-14: indie 238, direction-os 14, solmax 10
    (`ls live/<id>/history/`, отбор по дате в имени файла);
  - человеческого имени у направления нет ни в одном источнике: первая строка каждого
    `CHARTER.md` — это `# CHARTER — <слаг>`; у корня solmax нет даже `label`;
  - `app.js:87` и `:122` печатают слаг направления; `app.js:951-952` берёт умолчанием первый
    готовый раздел, а `dashboard` стоит первым в `SECTIONS` (`serve.py:122-124`), поэтому
    включение делает сводку входным экраном без единой строки кода;
  - `extra` — законный вид карточки (`osctl.py` `CARD_KINDS`), и им уже живёт
    `cards/direction_forecast.md`: имя направления не требует нового типа файла состояния.

  **Опровергнутая строка не поехала.** W06 («Сводка становится входным экраном направления»)
  стояла на владельческой цитате, которой нет ни в одном файле репозитория. В наряд она не
  скопирована ни в каком виде. Механический факт под ней проверен заново и остался фактом,
  а не требованием; сам вопрос задан владельцу как открытый и закрыт его словом.

  **Цитаты, на которых стоит первый наряд, сверены с диском:**
  «ноги за окно, окно 30 дней, детально это охват, планы это волна» —
  `history/2026-08-14-s-converge-g-one-screen-006.md:8`; «Для каждого направления я хочу видеть
  свою сводку… никакой нахуй общей» — `history/2026-08-13-s-map-g-one-screen-split-001.md:9`;
  «какой-то свод я могу получить через чат, а вот это мне нужно» — `cards/g-one-screen.md:19`.

state_changes: |
  1. `cards/bet-g-one-screen.md` — новая карточка ставки (`_kind: bet`, `node: g-one-screen`,
     `opened: 2026-08-14`) с блоками `goal`, `appetite`, `kill_by`, `forecast`, `against`,
     `cut_list`, `lens_verdicts`.
  2. `cards/g-one-screen.md` — `status: parked` → `active`; добавлены `appetite` и `kill_by`
     короткой строкой (полные — на карточке ставки); строка журнала.
  3. `NOW.md` — `bet: null` → `bet: g-one-screen`.
  4. Шесть карточек задач, каждая `_bet: g-one-screen`, `status: open`, с блоками `goal` и
     `done_when`: `t-one-screen-1` (канон значений до сборки, нога), `t-one-screen-2` (скелет
     на реальных данных — проверка самого рискованного допущения, исполнитель),
     `t-one-screen-3` (экран: один вид, мало и просто, исполнитель), `t-one-screen-4` (имена
     направлений и снятие двух правил, исполнитель), `t-one-screen-5` (полоса свежести
     перестаёт врать, исполнитель), `t-one-screen-6` (включение: пять поверхностей одним шагом,
     исполнитель).
  5. `cards/c-work-one-screen-canon-001.md` — новая карточка наряда (`status: ready`,
     `to: session`, `play: work`, `for: t-one-screen-1`) и файл
     `work/c-work-one-screen-canon-001-call.md`, самодостаточный, с дословными
     `acceptance`-строками и его цитатами.
  6. `cards/i-eight-pages-counts-itself-through-ready-sections-001.md` — новая запись
     (`route: map`, `blocks: g-eight-pages`): сосед определяет свои восемь через
     `READY_SECTIONS`, а включение сводки делает их девятью; и снятие двух правил
     `panel/PLAN.md:71` и `:126` стоит требованием у обоих узлов, а снять их можно один раз.
  7. `cards/i-state-accepts-values-it-does-not-declare-001.md` — блок `review_when` переписан
     целиком: ссылка на «строку 6» узла `g-one-screen` сгнила при починке карточки 2026-08-14
     (шестым номером стал другой сценарий), теперь ссылка держится на ИМЕНИ условия. Это строка
     W39 набора, закрытая без заведения новой карточки.
  8. `cards/c-shape-g-one-screen-001.md` → `cards/closed/` с причиной.
  9. `work/converge-g-one-screen-v3.md` НЕ ТРОНУТ — он заморожен уликой.
  10. Карточки соседних узлов НЕ ТРОНУТЫ: `g-eight-pages` правят только `map`/`review`.

captures:
  - Проигравшие подходы шага 3, чтобы их не изобретали заново. Б — «сводка показывает компактные версии готовых разделов»: даёт четыре куска четырёх разных страниц вместо одного вида и всё равно упирается в каждое число, чтобы показать способ счёта. В — «один набор данных для экрана и для сводки в чате»: склеивает экран и чат ровно там, где его подписанное слово их разделяет (строка W35 набора — свод есть работа чата).
  - Имя направления ложится карточкой не только в `live/direction-os/`, но и в две чужие папки направлений — это единственное место, где ставка выходит за своё направление. Названо честно: граница премортема 6 запрещает трогать `live/**` НЕ СВОЕЙ ставкой, а здесь это работа самой ставки, одобренная его словом. Если при исполнении задачи 4 выяснится, что чужие папки трогать нельзя, — это порог, а не повод для обхода.
  - Строка W21 (объём входа) из дешёвого параметра стала обязательной к решению: при подписанном окне в 30 дней у indie 238 отчётов ног. Решена внутри задачи 3 потолком в пять строк на блок; если потолок не сойдётся с «охватом», это первый кандидат в рез.
  - Строки W36 и W37 (рёбра к `g-no-silent-rot`) остаются открытыми и адресованы `map`/`review`: этой ставкой они не закрываются и в её задачи не входят.

decisions_needed: []

play_check:
  - Definition-of-Ready: закрыт ЕГО СЛОВОМ «а» (2026-08-14), а не пройденной проверкой. Набор провалил вторую проверку по контрабанде (`complete=PASS smuggling=FAIL`), полнота при этом прошла. Третий круг `converge` не открывался, набор не пересобирался, замороженный файл не правился.
  - шаг 1 (пересказ): done — цель узла и цель родителя пересказаны; цель есть исход, а не деятельность, починки не потребовалось.
  - шаг 2 (аппетит первым): done — аппетит спрошен ДО разговора о решении и зафиксирован его словом: 3 ноги. Внешний взгляд назван — восемь разделов панели по одному примерно за раз (2026-08-09 и 2026-08-10), и бумажная фаза этого узла в восемь ног за три дня. Конфликта с внешним взглядом нет: он говорит, что три ноги — это тесно, и это записано в аппетите как нулевой запас.
  - шаг 3 (подходы): done — три структурно разных способа, у каждого названо допущение, на которое он ставит; выбран один с однострочным «почему он, а не те»; проигравшие ушли в captures. Минимальная версия очерчена, и что в неё НЕ входит — сказано явно.
  - шаг 4 (рез): done — четыре реза, каждый с тем, чем он больно. Входящие открытые строки набора стали резом, строкой PLAN внутри задачи или адресом соседа — ни одна не стала «сама собой понятной». Владельчески-должные строки («целиком», вход, имя направления) получили его слово ДО того, как поехали.
  - шаг 5 (линзы): done — вердикт по каждой из пяти линз устава, три задачи и два `not_needed` с причиной. Молчаливых пропусков нет.
  - шаг 6 (самое рискованное допущение): done — допущения перечислены по убыванию убойности, первое взято из выбранного подхода, и на него заведена задача `t-one-screen-2`, стоящая второй и стоящая полдня. Глубина проверки соразмерна свободному режиму риска устава.
  - шаг 7 (задачи): done — шесть задач, каждая ≤ половины сфокусированного дня, у каждой проверяемое `done_when`, вид каждой назван (нога / исполнитель). Задача рискованного допущения стоит второй. Оценщик и откат названы заранее, как требует ставка с исполнителем.
  - шаг 8 (критерии смерти): done — три порога событием, без дат: устав снял срок, потолок и темп, и строка W32 набора это повторяет. `next_if_true` и `next_if_false` названы оба.
  - шаг 9 (закрытие): done — ставка показана одним экраном и одобрена его словом «да».
  - G1: done — активная ставка была одна и та же одна: `NOW.md` держал `bet: null`, теперь держит `g-one-screen`. Полос не заведено — предложены и отклонены с рекомендацией, его слово полос не называло, а без его точных слов полосу не объявляет никто.
  - G3: done — аппетит зафиксирован до разговора о решении и в ходе ноги не рос.
  - G4: done — у ставки есть и `done_when` (условия узла), и `kill_by`.
  - G5: не применялся — нога ничего не объявляет сделанным. Доказательство закрытия узла соберут задачи, и закрывает его отдельное слово владельца.
  - G6: done — рез настоящий (четыре, не ноль), вердикт по каждой линзе, задача на самое рискованное допущение есть.
  - G7: done — четыре владельческих вопроса заданы пачкой, с ценой каждого варианта и рекомендацией. Одна буква на четыре вопроса НЕ была истолкована: нога переспросила. Ни одно продуктовое решение не изобретено за него.
  - G9: done — узел стал активным только после его «да» на показанный артефакт.
  - G10: done — шаги показаны, RESULT последний, состояние менялось только после его выпуска и только командами `osctl`.

log: узел сводки стал активной ставкой — аппетит три ноги, шесть задач, четыре реза и три порога смерти событием; вход в направление, «целиком» и имена направлений закрыты его словом

next: |
  `c-work-one-screen-canon-001` — свежая сессия, play `work`, задача `t-one-screen-1`: канон
  значений пяти слов в `knowledge/` направления ДО сборки. Наряд заведён и НЕ запущен: запуск
  его словом.

  Дальше по порядку: `t-one-screen-2` (скелет на реальных данных — она же проверка порога
  смерти 2), затем `t-one-screen-3`, `t-one-screen-4`, `t-one-screen-5`, `t-one-screen-6`.

END_OF_FILE: live/direction-os/history/2026-08-14-s-shape-g-one-screen-001.md
