RESULT s-capture-panel-per-direction-001
direction: direction-os   play: work (capture-ход ядра §2)   bet: g-one-screen (НЕ тронута)

outcome: |
  Его наблюдение 2026-08-27 получило дом карточкой `idea-panel-per-direction-own-port-001`.
  Он попросил именно записать, а не строить, и это записано его же словами вместе с его
  пометкой «не прям чтобы обязательно».

  Проблема под идеей оказалась НАСТОЯЩЕЙ и перемерена: панель показывает ОДНО число свежести
  на все направления, и это число врёт в обе стороны сразу.

evidence: |
  Замер моей рукой из `C:/wf-direction-os`, дерево `d33f6990`:
  - полоса панели (`serve.build_info()`) говорит «ЭТА КОПИЯ ОТСТАЛА НА 141»;
  - по-настоящему (`git rev-list --count HEAD..origin/main -- live/<id>/`):
    `direction-os` 0, `solmax` 0, `indie-game-development` 140.
  Одно число пугает за два свежих направления и молчит о том, что по третьему экран показывает
  позавчерашний мир.

  Не гипотеза: в этот же день в эту яму провалилась нога этой сессии — читала состояние игры из
  этой копии, получила устаревшее и заметила только сверкой с `origin/main`.

  Отграничено от уже запланированного, а не свалено в кучу: `t-one-screen-5` действующей ставки
  чинит ЛОЖЬ ПОЛОСЫ и уже считает по каждому направлению отдельно (её условие 1 — коммиты,
  «трогающие `live/<направление>/`»). Его идея — другое решение той же проблемы: не честная
  общая панель, а разделённые процессы со своим портом. Записано, что одно другого не требует.

state_changes: |
  1. `cards/idea-panel-per-direction-own-port-001.md` — НОВАЯ карточка (`_kind: idea`,
     `about: g-one-screen`, `from` и `source` заполнены).
  2. НЕ ТРОНУТО: ставка, четыре задачи, наряд на проверку вида, `NOW.md`, `t-one-screen-5`.
     Задачу под идею не заводил и в ставку её не подмешивал (G2/G3).

captures: []

decisions_needed: []

play_check:
  - capture-ход ядра §2: его отложенное содержание стало карточкой `idea` с `from` и `source`,
    а не строкой прозы. В ставку не поехало ничего.
  - G2: работа вне ставки осталась записью и не превратилась в задачу.
  - G3: аппетит не тронут.
  - G10: RESULT последний, состояние менялось только после выпуска.

log: его наблюдение про отдельную панель на направление записано идеей, а под ней найдена измеренная ложь: полоса свежести показывает одно число на все направления — «отстала на 141», тогда как система и solmax отстают на 0, а игра на 140

next: |
  Ждёт его буквы по развилке порядка работ: (а) дописать очередь в сводку сейчас и проверить
  один раз, (б) сначала проверка вида, (в) дописать очередь и включить сводку, чтобы он увидел
  экран глазами. Рекомендация — (в).
