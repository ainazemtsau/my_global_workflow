RESULT s-knowledge-external-control-planes-001
direction: direction-os   play: work (knowledge-ход ядра §2)   bet: g-one-screen (НЕ тронута)

Нога прошла в агентском CLI и после выпуска RESULT стала своим писателем: применено и
закоммичено той же сессией.

outcome: |
  У research'а по LoopX появился дом. До этой ноги его не существовало нигде: `grep -rli loopx`
  по всему репозиторию давал ноль — работа была сделана 2026-08-15, выложена в чат и умерла там,
  и заметил это САМ ВЛАДЕЛЕЦ через двенадцать дней, спросив «мы там ещё лупы XR рассматривали».
  Это ровно тот класс дефекта, ради которого в направлении заведено правило про дом для
  принятого. Триггер — его слово «делаю, делаю» 2026-08-27.

  Запись `knowledge/external-control-planes-are-not-borrowed-in.md` несёт правило (внешний
  control plane внутрь не берётся), доказательство (LoopX реален и того же класса, что мы),
  пять убитых заимствований с причиной у каждого и ДВА УЦЕЛЕВШИХ ОСТАТКА — оба наши, не
  заимствованные: правило про `running` без проверки, и уцелевшая половина улики про
  недоехавший наряд.

evidence: |
  Дерево 090825b5→df966297, рабочая копия `C:\wf-direction-os`.

  **Дом не существовал — перевыведено:** `grep -rli "loopx\|loop-x" live/ os/` → пусто.
  После ноги — один файл, `osctl check` = «механических проблем нет», раздел ЗНАНИЯ панели
  видит запись и её поле читателя (`section_knowledge` вернул `reader` непустым).

  **Два числа агентов НЕ приняты на веру и перемерены моей рукой; оба разошлись:**
  - «49 живых карточек из 51 несут `review_when`» → на самом деле **69 из 72** на `origin/main`
    (`grep -l review_when` по `i-*.md`). Агент мерил устаревший набор;
  - «13 392 → 4 695 слов, −65%» → мой прогон дал **13 392 → 4 772, −64%**
    (`osctl context --direction indie-game-development`, затем `card show` по каждому id).
  Вывод в обоих случаях устоял, но в запись легли МОИ числа, с командой и датой.

  **Живые замеры в записи помечены живыми, а не выданы за вечные:** обе строки прямо говорят,
  что число протухнет и как его пересчитать в момент чтения. Это требование раздела `knowledge/`
  схемы про операционные факты чужой authority.

  **Ссылки перепроверены по байтам, не пересказаны:** `os/FRICTION.md:232` (очередь внимания
  построена — `outcome_dispatch`, экран `focus / running / launch now`), `:238` (снята после
  «third owner complaint», формулировка «no new state type, dashboard or engineering gate»),
  `osctl.py:503` (`[:600]`), `os/KERNEL.md:70` и `os/schema/direction-files.md:353` (расписка
  запуска для `running`), коммит `a50d9651` (его слова «у меня в u2 никакой работы не запущено»).

state_changes: |
  1. `knowledge/external-control-planes-are-not-borrowed-in.md` — НОВЫЙ файл, вторая запись в
     знаниях направления.
  2. НЕ ТРОНУТО: `NOW.md`, ставка, четыре открытые задачи, наряд на проверку вида, все карточки.
     Ставка `g-one-screen` этой ногой не двигалась ни на строку.

captures:
  - Владелец обнаружил пропажу research'а раньше системы и раньше меня. Механизма, который
    ловит «нога сделала работу и не оставила дома», у направления нет: `osctl check` считает
    механику карточек и про бездомную работу ничего не знает. Уликой не завожу — случай пока
    один, а правило требует двух; записано, чтобы второй нашёл первый.
  - Он же назвал вслух более общий дефект: чтобы узнать, что лежит в направлении, ему пришлось
    прийти и спросить. Сводка (ставка `g-one-screen`) показывает текущую ставку и проблемы, но
    НЕ показывает очередь — запаркованные цели и ждущие записи. Он попал ровно в эту дыру:
    запись про выбор модели живёт в очереди, а очередь на сводке не видна.

decisions_needed: []

play_check:
  - knowledge-ход ядра §2: запись добавлена, не промотирована. Каждая несущая строка несёт
    цитату или разрешимый указатель; выведенное ногой ушло в captures, а не в знание.
  - `read_by` реален: назван потребитель (нога, которой предлагают внешний движок) и триггер (ДО
    оценки пользы), плюс две поимённо названные улики, где лежат уцелевшие остатки.
  - G2: ставка не тронута. Ход разрешён ядром в любой ноге и запущен прямым словом владельца.
  - G5: не применялся — нога ничего не объявляет сделанным, кроме существования файла, которое
    перевыведено байтами здесь же.
  - G10: RESULT последний, состояние менялось только после выпуска.

log: research по LoopX получил дом в знаниях — до этой ноги слова «loopx» не было ни в одном файле, и пропажу заметил сам владелец через двенадцать дней; два числа агентов перемерены моей рукой и оба разошлись (69 из 72 вместо 49 из 51, −64% вместо −65%), в запись легли мои

next: |
  Второе его поручение — дописать очередь в сводку — ЖДЁТ его решения о порядке: вид сводки
  сейчас стоит под непройденной проверкой `c-work-one-screen-view-g5-001` на `ae72e7dc`, и
  правка кода обесценит её мишень.
