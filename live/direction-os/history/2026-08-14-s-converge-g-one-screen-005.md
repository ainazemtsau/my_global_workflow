# RESULT — s-converge-g-one-screen-005

play: converge · direction: direction-os · node: g-one-screen · date: 2026-08-14

## outcome

Набор требований собран заново — `work/converge-g-one-screen-v3.md`, 41 строка — и в
отличие от двух предыдущих **пережил собственную пре-проверку**: шесть смертельных
находок починены внутри ноги, а не обрушили набор.

Разница с v1 и v2 не в объёме, а в том, где стоят решения. **Ни одна строка не помечена
`answered` на прочтении, которое выбрала нога.** Где выбирала нога — там `open` с
названным должником: три строки на владельце (W12, W17a, W33), пять на PLAN, четыре
рёбрами соседям, одна отложена записи.

Корень, убивавший v1 и v2, закрыт настоящим основанием: разрешение достраивать СВОДКУ
приходит born-closed из подписанного 2026-08-14 устава (D6), а не из оговорки, которую
нога написала себе сама в чужой карточке.

Узел остаётся `parked`. Три владельческие строки блокируют активацию, но не закрытие
ноги (`os/plays/converge.md`, Done when).

## evidence

### Что поймала пре-проверка и что починено

Шесть смертельных, все подтверждены первой рукой перед починкой:

1. **D2 стоял на источнике, которого в нём нет.** `grep -c 'мало и просто'` по
   `history/2026-08-12-s-converge-g-one-screen-001.md` → **0**. Слова про «подробнее»
   живут только в двух замороженных наборах. Источник переставлен на действующую
   карточку (done_when 9), а замороженный набор назван записью его слов, но не
   поставщиком требования.
2. **Разграничение «свод — работа чата» не дошло ни до одной строки.** Атом goal-блока,
   которым карточка ПРЯМО велит судить содержимое сводки. Заведена W35.
3. **«окно» — несущее слово done_when 4 — нигде не определено**, а W13 стояла
   `acceptance`/`answered` и уехала бы в наряд с неопределённым параметром. Окно 30 дней
   в замере M3 выбрано ногой и источника не имеет. Разрезано: W13 стала `open`, а вопрос
   «какое окно» сложен в W12 к владельцу.
4. **«детально» вырезано при переносе цитаты в строку.** Карточка несёт слово дважды,
   W01 его теряла — тот же класс, что убил v2, только шов переехал с «редакция → редакция»
   на «цитата → строка». Слово возвращено в W01, заведён термин и владельческая строка W33.
5. **Три `acceptance`-строки стояли на `open`-строках** (W13←W12, W18←W17, W22←W23).
   Плей запрещает: «one citing `open` is itself `open`». Разрезаны; проверено машинно —
   `acceptance`-строк 22, все `answered`.
6. **Входящее ребро от `g-no-silent-rot` было вывезено соседям вместо покрытия строкой.**
   Плей требует «every criterion + edge → a row, `covered_by`, or a named `open`». Все
   четыре ребра стали строками W36–W39.

### Что перевыведено первой рукой и исправлено в замерах

- **«Разброс до 500×» завышен в восемь раз.** Настоящий разброс прочтений ВНУТРИ
  направления: indie 541 против 9 = **60×**, у двух других 10×. «500×» получалось
  делением коммитов indie на коммиты direction-os — числа разных направлений. Моя же
  предыдущая нога записала 60× верно; эта регрессировала и была поймана до записи.
- **Замер команда-чувствителен:** `--since=2026-07-15` даёт indie 540, `T00:00:00` — 546.
  В таблицу вписана точная форма и команда для каждого из четырёх столбцов.
- **D8 называл ставку целью.** Источник (`…frame-direction-os-001.md:70`) говорит
  «первой СТАВКОЙ»; в этом OS `bet` и `node` — разные виды. Исправлено.
- **W24 шире, чем думал M5.** У направления человеческого имени нет вовсе: `app.js:87`
  и `:122` печатают машинный слаг как единственное имя, и оба места вне десяти,
  посчитанных M5. Исполнив W24 буквально, исполнитель подписал бы все три направления
  «без имени». Область названа, вопрос отдан W23.
- **Предусловие предыдущей ноги не выполнено:** оговорка в `g-eight-pages` done_when 5
  обещана к снятию и не снята — проверено `grep`. Стала строкой W38.

### Что держится без изменений

Замеры M1, M2, M4, M5, M6, M7, M8 воспроизводятся первой рукой. Самая тяжёлая улика —
M2: корневая копия стоит ровно на `origin/main`, показывает `behind = 0` и объявляет себя
свежей, при этом `git diff --stat main..HEAD -- live/direction-os/` → **34 файла, 2309
вставок**, включая подписанный устав. Приёмка `panel/verify.py:113` удостоверяет это
молчание как правильное поведение.

Оба импорта из устава проверены на диске: `CHARTER.md` несёт «Достройка раздела,
объявленного и выключенного ДО этого решения, есть починка» (D6) и «доля работы в систему
приостановлена как датчик» (D7).

## state_changes

1. `work/converge-g-one-screen-v3.md` — набор: заголовок с triage и восемью импортами,
   восемь замеров с командами, §GLOSSARY на 12 терминов, §WHAT на 41 строку, вердикты по
   пяти линзам хартии, §SIGNOFF с разделением подписанного и решённого ногой.
2. `card new c-converge-verify-g-one-screen-002` — `call`, `to: session`,
   `play: converge-verify`, `for: g-one-screen`, `status: ready`.
3. `work/c-converge-verify-g-one-screen-002-call.md` — бриф проверки.
4. `log add` на `g-one-screen`.
5. `NOW.md` НЕ меняется: `bet: null`. Статус узла НЕ меняется: `parked`.

## captures

1. **Пре-проверка окупилась второй раз подряд, но по-разному.** На v2 она вынесла «не
   выживет» и отправила узел в устав; на v3 всё найденное починилось внутри ноги. Разница
   в том, что у v3 основание настоящее, а дефекты были в исполнении.
2. **Атака должна знать, что она атакует.** На v2 ей скормили ещё не применённый черновик
   как будущий текст карточки, и её верные находки два сообщения подряд докладывались
   владельцу как дефекты карточки. На v3 в задание записано «читай карточку с диска, не
   принимай ни одной процитированной версии» — ложных адресов не было. Кандидат в заявку.
3. **Нога регрессировала относительно собственного предыдущего замера** (60× → «500×»).
   Числа, уже выведенные в этом же узле, стоит переносить цитатой с указанием отчёта, а не
   пересчитывать по памяти.
4. **Правило «строка, ссылающаяся на open, сама open» механически проверяемо** — одна
   строка кода по таблице. Сегодня его не проверяет никто, и на нём падали оба
   предыдущих набора. Кандидат в заявку.

## decisions_needed

Три владельческие строки, все с обеими сторонами и без подставленного ответа:

- **W12 — что означает «что сделано» и за какое окно.** Четыре живых прочтения, разброс
  до 60×. Числовое прочтение его решением 2026-08-06 НЕ запрещено — прошлая редакция
  карточки ставила «что сделано» первым в ряду чисел и объявляла то решение
  подтверждённым, а не переигранным. Довод предыдущей ноги был ложным и снят.
- **W33 — что означает «детально»: охват или плотность.** Под «плотностью» слово
  сталкивается с его же подписанным «заход даёт мало и просто».
- **W17a — что такое «планы» в его сценарии.** Раздела с таким именем нет; кандидаты
  ВОЛНА и ЦЕЛИ; третье прочтение — файл `panel/PLAN.md`, который межнаправленческий.

Блокируют активацию узла, не закрытие ноги.

## play_check

- Шаг 1 (triage + import) — `standard`, converge ON; восемь импортов, каждый с источником
  вне этой сессии, два из них проверены на диске после починки.
- Шаг 2 (Define) — миньер запущен ногой `-004`, его находки («имя», «стоит», «планы»,
  «что сделано») внесены в §GLOSSARY этой. Термины, читаемые двумя способами, — 12 строк;
  три оставлены владельцу, четыре отданы PLAN.
- Шаг 3 (Resolve) — плоский список, одна строка один вопрос; `acceptance`-строк 22, все
  `answered` (проверено машинно); ни одна не ссылается на `open`. Firewall по цене отката:
  пять строк в PLAN, владельческие остались здесь при любой цене.
- Шаг 4 (Close & route) — наряд исполнителю НЕ выдан. Следующий — `converge-verify`
  свежей отдельной сессией. `converge_coverage`: каждый из 11 критериев и каждое из
  четырёх рёбер дают строку либо названную `open`.
- G7 — ни одна развилка не решена за владельца; три честно висят открытыми.
- Честно: пре-проверка проведена субагентами В ЭТОЙ сессии и биндинговой не является.
  Гейт G5 требует свежего физического чата, и он впереди.
- Честно: нога проведена в том же чате, что и предыдущие, по его указанию.

## log

набор сводки собран заново и впервые пережил собственную пре-проверку: шесть смертельных починены внутри ноги, ни одна строка не стоит решённой на выборе ноги, три развилки честно висят на владельце

## next

`c-converge-verify-g-one-screen-002` — свежей отдельной сессией попытаться опровергнуть
набор. Три владельческие строки до его слов блокируют активацию узла, но не проверку.

END_OF_FILE: live/direction-os/history/2026-08-14-s-converge-g-one-screen-005.md
