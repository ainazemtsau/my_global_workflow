# RESULT — s-converge-g-one-screen-001

play: converge · direction: direction-os · node: g-one-screen · date: 2026-08-12

## outcome

Узел доведён до набора требований на бумаге. Владельческая развилка, ради которой
нога существовала, закрыта его словом: первый экран — **светофор по направлениям**
(«МНЕ НРАВИТСЬЯ 1Й ВАРИАНТ»), выбран из трёх показанных раскладок — светофор по
направлениям, три коробки по состоянию, одно главное крупно.

Выбор снял два вопроса, на которые владелец в первом заходе ответить не смог («ни
один из них не могу выбрать»): устройство важности и носитель простого изложения
определяются видом экрана, а не спрашиваются отдельно. Диагноз владельца принят
буквально: вопросы о механике были заданы раньше вопроса о виде, и потому ни один
вариант не был выбираем.

**Добыто новое требование, которого в `done_when` узла нет и которое цитируется
чаще всех остальных:** на экране не появляется ни один машинный код. Его слова:
«я вообще не хочу, чтобы в веб-сайте были какие-то коды, типа знаешь, там
G-0-12-58. Любые коды… я же не помню все эти коды». Требование сквозное — оно и
есть общий первый пункт прохода по всем восьми страницам.

Отдельно снята его поправка к методу: продиктованный функционал есть идея, а не
спека — «я просто тебе рассказывал примерно функционал… тебе нужно идею понять».
Формулировка критерия 2 («каждая запись несёт короткую человеческую строку»)
разобрана как описание чувства («не хочу видеть тонну текста»), а не как требование
завести поле.

Отдельным вопросом владелец спросил, куда носить проблемы: в заявку на починку или
в это направление. Ответ дан по границе, записанной в его же хартии, и с
практическим правилом «не сортируй сам, говори проблему любыми словами — разводит
нога», потому что заставлять его классифицировать есть ровно тот дефект, на который
он жалуется. Состояния этот ответ не менял.

`triage: standard — converge ON — because` ≥1 термин читается двумя способами
(«запись», «важность», «работа в систему», «залочено», «расхождение») И узел
потребляет от соседа `i-state-accepts-values-it-does-not-declare-001`. НЕ heavy:
узел не разлагается на подсистемы, которые надо сводить архитектурно.

`imported:` D1–D8 — решения владельца 2026-08-12
(`history/2026-08-12-s-map-direction-os-001.md`) и 2026-08-06 (`panel/PLAN.md`);
внесены born-closed, ни одно не переоткрыто.

## evidence

Все числа выведены первой рукой в этой ноге.

**M1 ЖУРНАЛЬНАЯ СТРОКА.** `os/schema/direction-files.md:41` требует «one line/≤2
short sentences per leg… index, not a second summary». Замер: **175 из 394** живых
журнальных строк длиннее 200 символов (44%); максимум **2312 символов**
(`live/indie-game-development/cards/c-exec-g-5a7c-react-2-001.md`). Правило
написано и не держится.

**M2 РАСХОЖДЕНИЕ КОПИЙ.** `git worktree list` = 8 копий. Замер:
`wt/indie-game-development` **+22** коммита над `origin/main` (все 22 трогают
`live/`, 21 — `cards/`), `wt/direction-os` +1, корневая `C:/my_global_workflow`
ровно на `origin/main`. `panel/serve.py:158-166` сравнивает ТОЛЬКО с `origin/main`
→ из корневой копии полоса **молчит**, а состояние indie-game-development на
экране старее на 22 коммита. Критерий 7 стоит на замере, не на гипотезе.

**M3 ДОЛЯ РАБОТЫ В СИСТЕМУ.** База хартии воспроизведена: коммиты, тронувшие
`os/`, `panel/`, `osctl.py`, против ВСЕХ коммитов ЭТОГО репозитория.

| месяц | система | этот репо всего | репо игры | по хартии | с игровым репо |
|---|---|---|---|---|---|
| июнь | 95 | 649 | 199 | 14–15% | 11% |
| июль | 134 | 773 | 673 | 17% | 9% |
| август | 80 | 288 | 170 | 27–28% | 17% |

`C:/projects/Unity/GasCoopGame` на диске и читается. **На июле знак меры
перевёрнут:** хартия говорит «хуже июня» (17 > 15), счёт с игрой — лучший месяц
(9 < 11). Датчик премортема 1 сегодня врёт знаком на 1 точке из 3.

**M4 КОДЫ НА ЭКРАНЕ.** `panel/app/app.js` печатает машинные id в десяти местах:
`:206 :254 :262 :269 :271 :363 :459 :476 :515 :577`. Худшее — `:271` (подпись цели
ВСЕГДА оканчивается кодом, даже при живых `label`+`hook`) и `:269`/`:254` (код
ВМЕСТО отсутствующего имени). `panel/PLAN.md` записывает последнее как правильное
поведение («панель честно показывает id, пока их нет») — прямое противоречие его
сегодняшним словам.

**M5** 44 живые карточки происшествий в indie-game-development.

**M6** Часть критерия 6 УЖЕ СТОИТ: `panel/serve.py:62` кладёт `_closed` из папки,
`:988` исключает закрытые наряды — отработавший наряд готовым к запуску не
показывается. Ломается счётчик: `:294` исключает записи из «ЖДЁТ ТЕБЯ».

**M7** Проход по цепочке цели существует: `panel/serve.py:797` `node_events` строит
ленту из git по id узла; `goal_page` отдаёт parent/children/events.

## converge_coverage

30 строк. Покрытие полное: каждый из девяти критериев `done_when` даёт ≥1 строку,
каждый шов и каждая граница из наряда — свою.

- критерий 1 → W01 W02 W03 W04 W05
- критерий 2 → W06 W07 W08 W09
- критерий 3 → W10 W11 W12 W13 W14 W15
- критерий 4 → W16
- критерий 5 → W17 W18
- критерий 6 → W19 W20 W21
- критерий 7 → W22 W23
- критерий 8 → W24 W25 W26
- критерий 9 → W27
- швы и границы → W09 (`g-no-silent-rot`) · W21 (`i-state-accepts-values-…-001`) ·
  W12 (`frame`) · W28 (бюджет пяти типов) · W29 (без дат) · W30 (граница заявки)

## open_rows: 8

**Владельческие — блокируют активацию узла, не закрытие ноги:**

- W06 — простое изложение есть короткая журнальная строка, а не второе поле рядом
- W11 — доля работы в систему считает ОБА репозитория
- W20 — 44 записи о проблемах уходят с первого экрана на свою страницу

По всем трём позиция ноги показана владельцу 2026-08-12 с ценой каждой; он не
возразил и НЕ ПОДПИСАЛ. Молчание подписью не считается.

**→ PLAN (дёшево в откате, `rewrites:`):** W05 (признаки трёх полок), W13 (окно —
скользящие 30 дней), W15 (календарный разрез по дням), W18 (что добавляется к
существующей ленте цепочки), W23 (вид полосы расхождения).

## deferred: 4

- W08 — чем порождается короткая строка → владелец, позже (наряд снял с ноги)
- W09 — сторож на длину журнальной строки → `g-no-silent-rot`
- W12 — пересчёт строки хартии «15 → 17 → 28» → `frame`; converge хартию не трогает
- W21 — «где залочено и почему» → `i-state-accepts-values-it-does-not-declare-001`

## canon_proposed

- `k-panel-shows-no-machine-codes` — на экране направления не появляется ни один
  машинный код; где имени нет, панель говорит «без имени», а не показывает id
- `k-system-share-counts-both-repos` — доля работы в систему считается по обоим
  репозиториям; знаменатель только этого репозитория переворачивает знак меры

Предложение, не запись: `converge` в `knowledge/` не пишет.

## state_changes

Применено писарем этой же ноги (agent-CLI):

1. `work/converge-g-one-screen.md` — набор: заголовок с triage и imported, семь
   замеров, §GLOSSARY (10 терминов), §WHAT (30 строк), §SIGNOFF.
2. `card new c-converge-verify-g-one-screen-001` — call, `to: session`,
   `play: converge-verify`, `for: g-one-screen`, `status: ready`.
3. `work/c-converge-verify-g-one-screen-001-call.md` — бриф проверки.
4. `log add` на `g-one-screen` и на новый наряд.
5. `NOW.md` НЕ меняется: `bet: null`. converge ставки не активирует.
6. Статус узла НЕ меняется: `parked`. `shape` идёт только после проверки.

## captures

1. `done_when` узла не несёт требования «ни одного кода на экране», хотя оно теперь
   самое цитируемое. Правку узла делает `map`/`review`, не `converge`: требование
   едет строкой W02 с тегом `acceptance`, и `shape` копирует такие строки в наряд
   дословно. Отдельной карточки не заводил.
2. `panel/PLAN.md` несёт строку, прямо противоречащую его словам («панель честно
   показывает id, пока их нет»). Снимается внутри прохода по страницам, W02/W26.
3. Строка хартии «15% → 17% → 28%» вводит в заблуждение знаком на июле (M3).
   Хартию правит только `frame`; здесь только замер и строка W12.
4. **Методическая.** Владелец назвал дефект брифинга прямо: механика спрошена
   раньше вида, и потому ни один вариант не был выбираем. Второй случай того же
   класса за две ноги — первый — тяжёлая карта в `s-map-direction-os-001`,
   остановленная его же вопросом о форме. Третий случай того же класса — маршрут
   заявки на починку, а не правка здесь.

## decisions_needed

Батч 1 закрыт его словом (вид экрана). Открытыми к нему остаются три строки —
W06, W11, W20. Они блокируют активацию узла, а не закрытие этой ноги.

## play_check

- Шаг 1 (triage + import): тип записан в набор и в этот отчёт; восемь решений
  импортированы born-closed, ни одно не переоткрыто.
- Шаг 2 (Define): термины засеяны механически из слов `done_when`; в §GLOSSARY
  попали только читаемые двумя способами — 10 из примерно 20 кандидатов.
  **Отступление от плея, названное честно:** плей требует ОДНОГО миньера
  `call:research` по вторым прочтениям на диске; харнесс этой сессии запрещает
  поднимать субагента без явной просьбы владельца, поэтому добыча вторых прочтений
  сделана в сессии руками и целиком вынесена в M1–M7 с путями и номерами строк.
  Ни одно правило не ослаблено; результат проверяется перечитыванием тех же файлов.
- Шаг 3 (Resolve): плоский список, одна строка — один вопрос; каждая `answered`
  строка несёт источник ВНЕ этой ноги. Строки, выведенные рассуждением ноги, стоят
  `open` с названным отвечающим, а не выданы за `answered`. Firewall по цене
  отката: пять дешёвых строк ушли в PLAN, владельческие остались здесь при любой
  цене.
- Шаг 4: наряд исполнителю НЕ выдан. Следующий — `converge-verify`.
- G7: ни одна развилка не решена за владельца; три строки честно висят без подписи.
- G9: вид экрана изменён только после его точных слов на показанном артефакте.
- G10: RESULT — финальное сообщение ноги; состояние писалось писарем после него.

## log

экран правды получил вид его словом — светофор по направлениям; из выбора выпали
два вопроса, на которые он не мог ответить, и добылось сквозное требование «ни
одного кода на экране», сегодня нарушенное в десяти местах

## next

`c-converge-verify-g-one-screen-001` — свежей отдельной сессией попытаться
опровергнуть набор. Три владельческие строки (W06, W11, W20) до его слов блокируют
активацию узла, но не проверку.

END_OF_FILE: live/direction-os/history/2026-08-12-s-converge-g-one-screen-001.md
