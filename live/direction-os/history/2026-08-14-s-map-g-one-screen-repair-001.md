# RESULT — s-map-g-one-screen-repair-001

play: map (ревизия узла) · direction: direction-os · node: g-one-screen · date: 2026-08-14

## outcome

Карточка узла «Сводка каждого направления» починена: возвращён сценарий, потерянный при
разделении, вычищены четыре сгнивших замера из условий, первая строка приведена к новому
уставу. Попутно закрыты две мелкие дыры в соседних карточках.

**Исправление собственного доклада.** Две предыдущие ноги сообщали владельцу, что нога
разделения вырезала из карточки половины четырёх подписанных критериев. **Это неверно.**
Сверка карточки на диске показала, что все четыре половины на месте: «состояние на
сейчас», «за окно, а не за всю жизнь; ровно одно значение», «клик по строке ведёт
внутрь», «пользуется он этим или нет». Вырезаны они были в ЧЕРНОВИКЕ набора v2, который
так и не был применён, — а пре-проверка атаковала именно его, потому что ей скормили
текст черновика как будущий текст карточки. Атака была верной, адресат — нет.

Реально сломанным оказалось меньше, и это найдено до правки, а не после.

## evidence

### Что действительно было сломано

1. **Сценарий «пока работает ассистент» отсутствовал.** Старая редакция карточки несла
   его критерием 5 и отдельным абзацем цели; нога разделения потеряла и то, и другое.
   Владелец 2026-08-13 на прямой вопрос велел вернуть: «вторую верни». Возвращён в цель
   абзацем и в условия строкой 6, вместе с атомом «за минуты», который черновик тоже терял.
2. **Четыре живых замера гнили внутри условий.** «Одна копия на 22 коммита впереди»
   (сегодня впереди три, ни одной на 22); «44 у indie, 4 здесь» (здесь уже семь); «три
   прочтения дают 5/238/10, 0/8/0 и 2/34/0»; «14,6% против 27,1%». Все вынесены; условия
   несут требование, числа живут в отчётах и в `work/`. Правило записано первой строкой
   блока — тем же, которое ревизия устава 2026-08-14 записала в критерий 1 хартии.
3. **Первая строка не знала про новый устав.** Ревизия 2026-08-14 прямо назвала достройку
   объявленного и выключенного раздела починкой, а не добавлением. Строка 1 теперь на это
   опирается, вместо того чтобы опираться на оговорку, написанную ногой в чужой карточке.

### Что НЕ трогалось и почему

- **Простое изложение** осталось вне узла: его слова 2026-08-13 — «первую отдельной
  вещью». Возвращать не стал.
- **Тело критерия 2 корня** и карточка `g-direction-os` не трогались: ревизия устава
  меняла заголовок критерия, а не тело, и зеркало верно.
- **Нумерация** сдвинулась на единицу начиная с бывшей строки 6 — вставлен сценарий.
  Ссылок на строки этого узла по номеру в живом состоянии не осталось: запись
  `i-state-accepts-values-…-001` ссылалась на «строку 6», и её текст поправлен не был —
  он говорит «упрётся в свою строку 6», а зависимость теперь в строке 3. Названо в
  captures, чинится дёшево вместе со следующей правкой этой записи.

### Две дыры в соседних карточках, закрытые здесь

- `i-panel-cannot-reach-the-game-repo-001` была заведена БЕЗ блока `review_when`, которого
  требует схема («It needs a route owner and `review_when`; otherwise it is noise»). Дефект
  ноги разделения. Блок дописан, и в нём честно сказано, что триггер ослаб: ревизия устава
  приостановила меру, ради которой запись заводилась.
- `direction_forecast` ссылалась на критерий 2 старым заголовком «один экран правды».
  Ссылка приведена к новому — «правда без файлов».

Обе карточки — вида `issue` и `extra`; KERNEL §3 разрешает их писать любой ноге, поэтому
отдельного захода не потребовалось.

## state_changes

1. `card block g-one-screen goal` — добавлен абзац про вторую работу морды (сценарий
   ассистента), его словами.
2. `card block g-one-screen done_when` — 11 строк вместо 10: вставлен сценарий строкой 6,
   вынесены четыре замера, строка 1 приведена к новому уставу.
3. `card block g-one-screen why` — снят живой замер по трению, добавлен абзац о том, что
   ревизия устава сняла с узла две мины.
4. `card set g-one-screen detail` → этот отчёт.
5. `card block i-panel-cannot-reach-the-game-repo-001 review_when` — блок дописан.
6. `direction_forecast` — ссылка на критерий 2 приведена к новому заголовку.
7. `NOW.md` НЕ меняется: `bet: null`. Статус узла НЕ меняется: `parked`.

## captures

1. **Ошибка адресации в пре-проверке.** Атаке был скормлен текст, которого на диске нет,
   и её верные находки два сообщения подряд докладывались как дефекты карточки. Урок общий:
   когда проверке даётся ещё не применённый текст, отчёт обязан называть, ЧТО именно
   проверялось — файл или черновик. Кандидат в заявку.
2. **Ссылка по номеру строки протухает при вставке строки.** `i-state-accepts-values-…-001`
   ждёт события «упрётся в свою строку 6», а после вставки сценария это другая строка.
   Ссылаться надо на имя условия, не на его номер. Кандидат в заявку.
3. Схема требует `review_when` у записи, но `osctl check` этого не проверяет: карточка без
   обязательного блока прошла все механические проверки. Кандидат в заявку.

## decisions_needed

Нет. Возврат сценария — исполнение его слов 2026-08-13 («вторую верни»); вынос замеров —
исполнение принципа, который он санкционировал 2026-08-14 («и гниющие числа тоже почини»);
остальное механическая приводка к уже подписанному уставу.

## play_check

- Ревизия, а не заведение: дерево не менялось, детей не добавлялось, порядок прежний.
- G9 — новых владельческих решений не принято; всё возвращаемое и вычищаемое опирается на
  его слова 2026-08-13 и 2026-08-14, процитированные выше.
- G8 — узел остаётся `parked`. G1 — ставки нет, полос исполнения не создано.
- G2 — будущие цели не трогались.
- Честно: нога проведена в том же физическом чате, что и предыдущие, по его указанию.

## log

карточка сводки починена: возвращён потерянный при разделении сценарий, из условий вынесены четыре сгнивших замера, а первая строка оперлась на новый устав вместо оговорки, написанной ногой в чужой карточке

## next

`converge` по узлу заново: набор v2 не принят, собирается с нуля на починенной карточке и
новом уставе.

END_OF_FILE: live/direction-os/history/2026-08-14-s-map-g-one-screen-repair-001.md
