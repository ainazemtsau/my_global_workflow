# RESULT — s-converge-g-one-screen-006

play: converge (§SIGNOFF, продолжение s-converge-g-one-screen-005) · direction: direction-os · node: g-one-screen · date: 2026-08-14

## outcome

Три последние владельческие строки набора подписаны. Его слова 2026-08-14, дословно:
**«ноги за окно, окно 30 дней, детально это охват, планы это волна»** — сказаны на трёх
строках, показанных ему по одной, с числами и ценой каждой.

- **W12** — «что сделано» есть ОТЧЁТЫ НОГ за скользящие 30 дней;
- **W33** — «детально» означает ОХВАТ, подробно про одно направление, а не плотный экран;
- **W17a** — «планы» в его сценарии есть раздел ВОЛНА.

Вместе с ними закрылись две зависимые строки, стоявшие `open` только потому, что
цитировали открытые: W13 (окно скользящее, не календарный месяц) и W18 (сценарий).

**Владельческих открытых строк в наборе не осталось.** Активацию узла ничто не блокирует.
Десять оставшихся открытых адресованы PLAN (пять), соседям (три), записи (одна) и одна
зависит от отложенной развязки статуса.

## evidence

Слова владельца в этой сессии, дословно: **«ноги за окно, окно 30 дней, детально это
охват, планы это волна»** — в ответ на сообщение, перечислявшее ровно W12, W17a и W33,
с таблицей четырёх прочтений «что сделано» (10/238/10 · 1/541/9 · 0/9/1 · 6/34/0),
с прямым указанием, что довод предыдущей ноги против числового прочтения был ложным,
и с названным столкновением «детально» против подписанного «мало и просто».

Новых замеров эта нога не делала: M1–M8 стоят в
`history/2026-08-14-s-converge-g-one-screen-005.md` и не переоткрывались.

**Что подпись отклонила, записано явно:** коммиты (единица зависит от ветки и формы
даты), карточки `status: done` (дают ноль у двух направлений из трёх), закрытые карточки
любого вида; «детально» как плотность; «планы» как файл `panel/PLAN.md`.

**Снятое возражение, названное прямо.** Первый набор отклонял `history/` как носитель
«записи». Возражение касалось ПРОСТОГО ИЗЛОЖЕНИЯ, которое его словом 2026-08-13 вынесено
из узла отдельной вещью, и на «что сделано» больше не распространяется. Записано в
глоссарий, чтобы проверка не приняла старое возражение за живое.

**Две цены подписи, названные честно и записанные в набор:**

1. При окне 30 дней отчётов ног у indie — **238**. Столько строк на входе валит
   подписанное «заход даёт мало и просто» (W20). Поэтому объём входа (W21) из дешёвого
   параметра PLAN становится обязательным к решению при сборке.
2. Дата ноги берётся из ИМЕНИ файла (`panel/serve.py:456-458`), то есть объявлена самой
   ногой, а не наблюдена; расхождение с датой добавившего коммита встречается. Записано
   несущим свойством термина, а не спрятано.

**Арифметика набора сверена машинно, не на глаз:** 41 строка · answered 30 · open 10 ·
deferred 1; acceptance-строк 27, все `answered`; ни одна `acceptance`-строка не ссылается
на `open`.

## state_changes

1. `work/converge-g-one-screen-v3.md` — W12, W17a, W33: `open → ВЛАДЕЛЕЦ` → `answered`,
   тег `acceptance`, источником проставлены его слова; W13 и W18 закрылись как зависимые;
   §GLOSSARY получил значения и несущие свойства для «что сделано», «окно», «детально»,
   «планы» с явно отклонёнными прочтениями; §SIGNOFF дополнен его словами; итог пересчитан;
   названы обе цены подписи.
2. `log add` на `g-one-screen`.
3. `NOW.md` НЕ меняется: `bet: null`. Статус узла НЕ меняется: `parked` — активирует
   `shape`, и только после прохождения проверки.
4. Наряд `c-converge-verify-g-one-screen-002` НЕ меняется: он заведён предыдущей ногой и
   остаётся `ready`. Его бриф говорит, что три строки висят открытыми, — теперь это
   неверно, и поправка вносится строкой ниже.
5. `work/c-converge-verify-g-one-screen-002-call.md` — граница проверки исправлена: три
   строки больше не открыты, проверять надо, что подпись записана верно и что цена,
   названная при ней, соответствует правде.

## captures

Нет. Ничего нового не всплыло.

## decisions_needed

Нет. Владельческих открытых строк в наборе не осталось.

## play_check

- §SIGNOFF Resolve получен полностью: несущие строки подписаны его словами, ни одна не
  выведена из молчания и ни одна не закрыта выбором ноги.
- G7 — решения принадлежат владельцу; нога записала его слова, а не свои выводы. Три
  строки были показаны ему с обеими сторонами, с числами и с ценой.
- G9 — изменён только набор в `work/`, который есть улика, а не состояние; карточки узла
  нога не трогала.
- Честно о порядке: правка `work/` сделана до того, как RESULT произнесён, и только потом
  закоммичена. Существо правила соблюдено — ничего не ушло в Git до объявленного RESULT,
  и объявленные изменения совпадают с применёнными строка в строку.
- Честно: нога проведена в том же физическом чате, что и предыдущие, по его указанию.
  Биндинговая проверка обязана быть свежим чатом.

## log

три последние владельческие строки подписаны его словом: «что сделано» это отчёты ног за тридцать дней, «детально» это охват, «планы» это волна — владельческих открытых строк в наборе не осталось

## next

`c-converge-verify-g-one-screen-002` — свежей отдельной сессией попытаться опровергнуть
набор. Активацию узла больше ничто не блокирует; `shape` открывается только после
прохождения проверки.

END_OF_FILE: live/direction-os/history/2026-08-14-s-converge-g-one-screen-006.md
