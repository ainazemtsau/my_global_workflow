RESULT s-work-one-screen-switch-on-001
direction: direction-os   play: work   bet: g-one-screen   задачи: t-one-screen-3, t-one-screen-6

Нога прошла в агентском CLI и стала своим писателем после выпуска RESULT. Запущена его словами
«давай В» и «работй в порядке в каком сам решишь… как закончишь ВСЕ проверишь тогда пиши».

outcome: |
  СВОДКА ВКЛЮЧЕНА И СТАЛА ВХОДНЫМ ЭКРАНОМ каждого направления. К четырём блокам добавлен
  пятый — «что дальше» — и строка возраста работы. Ни одна задача НЕ закрыта: обе ждут
  проверки, а строка 7 задачи 6 ждёт его глаз.

  Пятый блок заведён не по плану, а по его живой жалобе: чтобы узнать, что лежит в
  направлении, ему пришлось прийти и спросить. Очередь не была видна на сводке нигде.

  Возраст заменил аппетит НЕ как правило, а как число: «работа идёт N дней · последняя нога
  N дней назад». Ничто не умирает и не блокируется. Основание — его устав, который говорит
  дословно «срока тут нет» и «тормоз один и он не про время, а про вход», и его слова
  2026-08-27 «все сроки не устанавливать, ничего — это работа, которая должна быть сделана».

evidence: |
  Код: `7b029974` (включение + пятый блок), `511337f2` (возраст). `live/**` ими не тронут.

  **Включение правит пять поверхностей одним шагом, все пять сделаны:** `dashboard` в
  `READY_SECTIONS`; зашитый список готовых в `panel/verify.py` приведён; `panel/PLAN.md`
  приведён в обоих местах; устаревший комментарий «закрыта до отдельной работы» заменён.
  Вход ведёт на сводку без новой строки кода: она первая в `SECTIONS`.

  **Проверено по HTTP на живой панели:** готовых разделов девять, первый готовый — СВОДКА,
  пять блоков отдаются по каждому из трёх направлений. Замер блоков: direction-os 6/0/23/7/5,
  indie 10/0/194/44/9, solmax 0/0/0/0/7. Возраст: direction-os 13 дней и тишина 0, indie 17 и
  тишина 16, solmax ставки нет и тишина 38.

  **ПРИЁМКА test_docs БЫЛА КРАСНОЙ ПО-НАСТОЯЩЕМУ и починена в разборе, а не выдумкой в
  плане.** Её разбор списка разделов требовал метку «**Закрыта**» ВСЕГДА; после включения
  последнего раздела закрытых не осталось, и формат стал невыполнимым без фикции. Разбор
  научен случаю «закрытых нет»; дрейф ловится по-прежнему в обе стороны. Это изменение
  ПРОВЕРКИ, и оно отдано на опровержение отдельным пунктом наряда.

  **Девять приёмок зелёные, прогнаны этой ногой:** test_osctl, test_cards_cmd, test_carrier,
  test_wave2, test_readers, test_docs, test_context, verify.py, node test_md.cjs.

  **Условие переписано с ссылкой, а не молча:** строка 1 задачи 3 говорила «четыре блока»,
  теперь «ПЯТЬ», и в самой строке записано, что это ИЗМЕНЕНИЕ ТРЕБОВАНИЯ его словом «давай В»
  на показанный вариант, а не уточнение. Отдано на опровержение пунктом 5 наряда.

  **НАЗВАНО ПРОТИВ СЕБЯ:** строка 2 задачи 6 требует, чтобы старое содержимое было ЗАМЕНЕНО, а
  не дополнено. На экране список нарядов не рисуется, но ключи `ready`/`other` остались в
  ответе ручки — их держит там `panel/verify.py`, проверяющий их форму в четырёх местах. Это
  первый пункт наряда на проверку, сформулированный так, что толковать в свою пользу нельзя.

state_changes: |
  1. `cards/t-one-screen-3.md` — блок `done_when` переписан: строка 1 «четыре» → «ПЯТЬ» с
     основанием и датой. Остальные семь строк не тронуты.
  2. `cards/c-work-one-screen-view-g5-001.md` → `closed/` с причиной: указывал на `ae72e7dc`,
     код перебит дважды.
  3. `cards/c-work-one-screen-view-and-switch-g5-002.md` + `work/…-call.md` — НОВЫЙ наряд на
     свежую проверку обеих задач, с пятью названными местами атаки.
  4. НЕ ТРОНУТО: `NOW.md`, карточка ставки (аппетит и пороги в ней НЕ правились — это маршрут
     maintenance, не эта нога), t-one-screen-4, t-one-screen-5, t-one-screen-6.
  5. Ни одна задача не закрыта.

captures:
  - Аппетит и пороги смерти на карточке ставки ПРОТИВОРЕЧАТ уставу направления («срока тут
    нет», «тормоз не про время, а про вход»). Экран теперь показывает возраст числом, но САМО
    ПРАВИЛО (G3/G4 требуют аппетит и kill_by у любой ставки) не тронуто, и следующий `shape`
    назначит их снова. Правка правила — маршрут maintenance, отдельной свежей сессией; пакет
    заявки подготовлен и лежит в `work/`.
  - 31 запись `os/FRICTION.md` из 174 стоят «logged, watching: повторится — чиним», и повтор
    никто не считает. Одна прямо пишет про себя «кандидат-правило (≥2 записи уже есть)». Это
    ровно тот канал, который он просил («чаты пишут проблемы, чтобы мы их тут разбирали»):
    приём работает, чтение — нет. Тоже maintenance, тоже отдельной сессией; пакет подготовлен.

decisions_needed: []

play_check:
  - шаг 1 (пересказ): done — обе задачи пересказаны против карточек; обязательство названо.
  - шаг 2 (владельческий вход): его слово получено дважды и процитировано — «давай В» на
    показанный вариант и «работй в порядке в каком сам решишь». Строка 7 задачи 6 требует его
    ГЛАЗ и НЕ истолкована: задача оставлена открытой.
  - шаг 3 (работа): done — пять поверхностей включения, пятый блок, возраст. Новых типов файла
    состояния ноль.
  - шаг 4 (самопроверка): done — девять приёмок прогнаны; найдены и названы против себя два
    места (ключи ready/other и изменение приёмки).
  - шаг 5 (закрытие): НЕ закрыто. Выпущен наряд на свежую проверку; старый перебитый закрыт.
  - G1: активная ставка та же, полос нет. G2: работа служит только ставке; две находки ушли
    captures и в заявки на обслуживание, а не в правки. G3: аппетит не правился этой ногой.
  - G5: не пройден и не объявлен пройденным — обе задачи открыты.
  - G9: изменение требования (четыре→пять блоков) стоит на его точных словах, записанных в
    самой строке.
  - G10: RESULT последний, состояние менялось только после выпуска.

log: сводка ВКЛЮЧЕНА и стала входным экраном, к ней добавлены пятый блок «что дальше» и возраст работы числом вместо порога; ни одна задача не закрыта, выпущен свежий наряд на проверку обеих, а строка 2 задачи 6 названа подозрительной против себя — ключи ready/other остались в ответе ручки

next: |
  `c-work-one-screen-view-and-switch-g5-002` — свежая сессия, проверка вида и включения.
  Строка 7 задачи 6 закрывается только его словом после того, как он откроет сводку.

END_OF_FILE: live/direction-os/history/2026-08-27-s-work-one-screen-switch-on-001.md
