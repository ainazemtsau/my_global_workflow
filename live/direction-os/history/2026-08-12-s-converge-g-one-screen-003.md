# RESULT — s-converge-g-one-screen-003

play: converge (починочная нога по `c-converge-g-one-screen-repair-001`) · direction: direction-os · node: g-one-screen · date: 2026-08-12

## outcome

**Набор НЕ починен, и это правильный исход: посылка наряда опровергнута.** Наряд велел закрыть восемь находок проверки. Нога закрыла две, а на остальных обнаружила, что чинить нечего — под ними лежат дыры уровня ИСПОЛНИМОСТИ, которых формулировкой не закрыть.

Три из четырёх починок, которые нога собиралась записать, не пережили опровержения. Два основания, на которых владелец в этой же сессии принял два решения, оказались НЕВЕРНЫ — и оба выдала эта нога, а не наряд. Сверх восьми найдено ещё пять, из них четыре бьют по подписанным строкам.

**Решение владельца, его слова: «разделяй в map».** Узел уходит в `map` на разделение по шву «готовое против неисполнимого». Набор `work/converge-g-one-screen.md` заморожен как улика с шапкой REFUTED; его тело не переписано, чтобы не потерять подписанные строки, но числа, известные неверными, названы прямо в шапке.

**Отступление плея, названное честно и в свою пользу:** нога `-001` записала, что не смогла выполнить шаг 2 плея (`Run ONE miner call:research over every done_when/lens noun and second reading on disk`), потому что харнесс запрещал субагентов. Эта нога миньера ЗАПУСТИЛА — шесть параллельных детей по срезам `done_when` и линз, плюс четыре опровергателя и критика полноты. Именно миньер и опроверг три починки из четырёх. Пропуск шага 2 стоил направлению одной ноги проверки и одной ноги починки.

## evidence

### A. Опровергнуто из собственных починок этой ноги (3 из 4)

**A1. Починка F4 (светофор) — ОПРОВЕРГНУТА.** Нога собиралась записать: запрет D3/D8 бьёт по ВЫДУМАННОЙ важности, а не по механической раскладке из объявленных фактов. Не держится:

- `cards/g-one-screen.md:15` — «Важность приходит ИЗ ЗАПИСИ, а не вычисляется мордой: **посчитанная важность есть выдумка**». Это тождество «вычислено = выдумка», а не оговорка про наличие данных.
- `work/converge-g-one-screen.md:74` — «ничем не вычисляется». «Ничем» щели не оставляет.
- Собственная проверка этой пары ног уже постановила обратное: `history/2026-08-12-s-converge-verify-g-one-screen-001.md` (F4) назвала механическую раскладку ПЕРВОЙ ВЕТКОЙ развилки, ценой которой падают D3 и D8. Нога противоречила своему же вердикту суточной давности.
- W20 (подписан) идёт ДАЛЬШЕ формулы: 44 записи — объявленный факт, и всё равно на первый экран не идут.
- Опоры, на которые починка ссылалась, фактами состояния не являются: `WAITING_GROUPS` объявлен в КОДЕ (`panel/serve.py:282-287`), флаг «блокирует» выбран кодом (`:348`), то есть это суждение панели, а не запись; `NOW.md:3` — `bet: null`.
- Панель уже ОТКАЗЫВАЕТСЯ раскладывать по объявленному ровно по этому мотиву: `panel/serve.py:355-356` «Угадывать „пора/не пора“ нельзя»; `:404` «Раздел не оценивает и не сортирует по важности».

Вывод: нога ответила за владельца на вопрос, про который сама же написала, что он владельческий. Починка снята, развилка возвращена ему.

**A2. Основание решения «что сделано» — ОПРОВЕРГНУТО.** Ноге было сказано владельцу, что у прочтения «ноги за окно» есть готовый носитель. Проверено в коде:

- `panel/serve.py:489-505` `section_history` перечисляет ФАЙЛЫ `live/<id>/history/*.md` по маске `LEG_NAME` (`:428`), а не сообщения коммитов. Коммит — третий необязательный источник (`:465-486`), при промахе кладётся `None` (`:460-461`).
- Источник ошибки найден: `panel/PLAN.md:116` пишет «строка = сообщение коммита ноги». **План описывает собственный код неверно, и нога повторила план вместо кода** — тот самый класс, который эта же пара ног днём ранее ловила у других (`panel/PLAN.md:130-133`, «читатель переживает свой источник», трижды подряд).
- **Окна нет**: в `:489-514` ни одного временнóго фильтра; `app.js:656` печатает «Всего ног», итог, не окно. И «за 30 дней» в `cards/g-one-screen.md:17` стоит при ДОЛЕ РАБОТЫ В СИСТЕМУ, а не при «что сделано» — нога приклеила окно не к тому числу.
- Набор УЖЕ отклонил этот носитель: `work/converge-g-one-screen.md:75` — «ОТКЛОНЕНО: `history/` как носитель — это отдельный тип файла».
- Носитель дырявый: коммит `fb72837b` (2026-07-26) переименовал **63 отчёта** из `live/indie-game-development/history/` в `archive/**`, читать который запрещено политикой — для панели эти ноги не существуют.
- Числа, показанные владельцу (6 / 491 / 16), были КОММИТАМИ. Ног за окно 2026-07-15…08-13: **direction-os 5 (2 дня из 30), indie 238 (19 дней), solmax 10 (4 дня)**.

**A3. Основание решения о записях — ОПРОВЕРГНУТО.** Владельцу было сказано, что путь «показывать запись под её целью» невозможен, привязки нет. Проверено первой рукой:

- Нога проверяла поля, которых у вида `issue` не бывает: `about`/`node`/`_parent` принадлежат другим видам (`os/schema/direction-files.md:187, :211, :238, :37`). Поле привязки записи по схеме — `blocks` (`:140`), и его нет ни у одной из 44. Это и есть настоящая находка.
- **В ТЕКСТЕ привязка есть: 28 записей из 44 называют РОВНО ОДНУ живую цель, 13 — несколько, 3 — ни одной** (пересчитано против 16 живых узлов indie). Дефект — не отсутствие привязки, а её неоднозначность у 13.
- Цена десятого раздела выше названной: `SECTIONS` (`panel/serve.py:122-124`) — ОДИН глобальный список, `directions()` (`:182-184`) отдаёт его каждому направлению, поэтому страница ЗАПИСИ появится у всех трёх (44 / 4 / 0). Тронуть надо 6 общих файлов, включая `panel/verify.py:22, :123-125` и абзац `panel/PLAN.md:104-105`, который разбирает `panel/test_docs.py` — **две зелёные сегодня приёмки краснеют**, если файлы не двинуть одним шагом. `panel/PLAN.md:20` («направление может добавить свой раздел, не трогая код панели») — незакрытый критерий успеха, а не свойство.
- И десятый раздел прямо запрещён born-closed решением D6 (`work/converge-g-one-screen.md:20`) и `cards/g-one-screen.md` done_when 8 — это живое противоречие с подписанным W20, а не механическое добавление.

**A4. Починка чисел — ВЫЖИЛА, с пятью оговорками.** Все 15 ячеек M1 и M3 воспроизведены независимо, ячейка в ячейку. Оговорки — в разделе C.

### B. Новые находки уровня исполнимости

**B1. Число, ради которого узел существует, — артефакт неназванного ключа git.** Перевыведено своей рукой на `0fb36da6`:

| | июнь | июль | август |
|---|---|---|---|
| `git log -- os/ panel/ osctl.py` (умолчание) | 94/645 = **14,6%** | 135/779 = **17,3%** | 80/294 = **27,2%** |
| то же с `--full-history` | 175/645 = **27,1%** | 179/779 = **23,0%** | 83/294 = **28,2%** |
| мерджей в месяце | 149 | 90 | 3 |

Умолчание `git log` применяет упрощение истории поверх мерджей. Растущий тренд «15 → 17 → 28», стоящий в `CHARTER.md:92` датчиком премортема 1 «инструмент съел игру», существует ТОЛЬКО при умолчании; при `--full-history` тренда нет. Знаменатель от ключа не зависит.

Сверх того: 80/294 = 27,2% → **27%, а не 28%**. Старые 28% шли из знаменателя 288. Пересчёт строку хартии не подтверждает, а опровергает.

**B2. У подписанного W11 нет входа.** `panel/serve.py:19` прибивает `ROOT`, `:148-151` — единственный вызов git с этим корнем. Конфигурационной поверхности нет: в `panel/` ни одного файла настроек, `getenv`/`environ` в `serve.py` — **ноль вхождений**, строки `GasCoopGame` нет нигде в `panel/`, `os/`, `live/`, `osctl.py` вне архива. И единственный тип файла, где такой факт мог бы жить, его запрещает: `os/schema/direction-files.md` — «repo paths, branches, worktrees … are read from that authority at use time, **never frozen here**». `live/direction-os/knowledge/` пуст, а `converge` в него не пишет. Считать оба репозитория сегодня физически нечем, и ни одна строка набора носителя не называет.

**B3. Светофор сегодня пуст у двух направлений из трёх.** Померено на текущем дереве: **direction-os** — `bet: null`, карточек `decision` 0, `question` 0, единственный наряд `to: session` (в группу `owner_call` не попадает), `stalled` пуст, все цели с именами → ни одна полка не загорается, хотя по узлу прямо сейчас идёт нога. **solmax** — ставки нет, блокирующего нет, записей нет → полка не загорается. Загорается только **indie** (1 вопрос + активная ставка `g-5a7c`). То есть подписанный светофор по трём направлениям сегодня рисует два пустых ряда из трёх.

**B4. Подписи 2026-08-12 не дошли до состояния.** `live/direction-os/cards/owner_approved.md` содержит только даты **2026-08-11**, ни одной записи 2026-08-12. Все четыре подписи этого узла (W01, W06, W11, W20) живут исключительно в `work/converge-g-one-screen.md:126-152`, а `os/schema/direction-files.md` объявляет `work/` — «Outputs/evidence, **not state**». Реестр, который ядро называет носителем G9, о них не знает.

**B5. Критерий 1 хартии этот узел не покрывает.** `os/FRICTION.md` — **174 датированные записи, последняя 2026-08-10**, ни одной про «владелец не начинает день с панели». Три попадания по слову «панель» — про формат code review, про перегенерацию владельческой панели писарем и про потолок журнала. Единственная улика узла — одна фраза владельца в `why` (`cards/g-one-screen.md:25`). А настоящий датчик премортема 5 (`CHARTER.md:132`, «если за месяц он ни разу не начал день с панели») инструмента не имеет: `panel/serve.py:169-179` пишет коммит, дату, неотправленные, отставание и путь — событие захода не фиксируется нигде.

### C. Оговорки к выжившим числам (записаны, чтобы не поехали дальше)

1. **Числитель метод-зависим** — см. B1. Точная команда обязана стоять в требовании.
2. **Знаменатель зависит от набора ссылок:** `git log --all` даёт июнь 645, июль 782, август 355 (числитель 94/136/82) — в репозитории лежат ветки `codex/*`, недостижимые из `0fb36da6`.
3. **Август у игры движется:** 354 верно только на `c485b30e`. В наборе стояло 170 — счёт был снят на более старом `main`. Между двумя прогонами ЭТОЙ сессии число выросло.
4. **M1 привязано к дереву:** на `fb0a87c8` — 394 строки, 121 длиннее 200 символов, 171 длиннее 200 байт, максимум 1377 симв. = 2312 байт. На `0fb36da6` те же правила дают 398 / 124 / **175**. Найдено происхождение старого числа: **«175 из 394» смешало счёт БАЙТ с одного дерева со счётом СТРОК с другого**, и подпись «символов» неверна для обоих.
5. **Часы репозиториев расходятся:** последний коммит здесь помечен 2026-08-12 14:46, в репозитории игры есть коммиты 2026-08-13 04:01. На месячные корзины не влияет, на скользящее окно 30 дней — влияет на границе.

### D. Ребро от соседа, не покрытое ни одной строкой

`cards/g-no-silent-rot.md` дважды называет `g-one-screen` в своём `done_when`, а §WHAT несёт про соседа ровно одну строку (W09, и только про сторож длины):

- строка 2: «„что осталось" **выводится** из карточек — открытые узлы, **происшествия**, неистраченные наряды, незаданные вопросы — и показывается **на морде (`g-one-screen`)**». W20 (подписан) уводит происшествия с первого экрана; про три остальные категории набор молчит.
- строка 4: «простое изложение из `g-one-screen` под тем же сторожем: **запись изменилась, изложение — нет**». После подписи W06 изложение ЕСТЬ журнальная строка, второго носителя нет — сторож непостроим по определению. **Подпись владельца сделала условие соседней живой карточки пустым**, и это нигде не записано. Тот же класс, что W12 (подпись W11 сделала неверной строку хартии).

`os/plays/converge.md` требует строку на каждое названное ребро и `converge_coverage` по каждому. Проверка `converge-verify` этого тоже не поймала: она разбирала условия своего узла и границы наряда, но не ходила в `done_when` соседа за обратными ссылками.

### E. Что подтвердилось из наряда

F2 и F3 закрыты фактом и вошли в шапку замороженного набора: единица M1 названа неверно (правда — 121 из 394 в символах, максимум 1377), возраст правила — 19 дней (`0a15c378`, 2026-07-24), а не три месяца. F5 (три непокрытых атома) и F8 (референт W01 вне набора) остаются верными и уходят в `map` как входные требования к детям. L9 (притязание W26 на порядок) верна и не спорна.

## state_changes

1. `work/converge-g-one-screen.md` — ДОБАВЛЕНА шапка `REFUTED` перед телом: набор проверку не прошёл, починка не состоялась, узел разделяется; названы числа, известные неверными, и указатели на три отчёта. Тело НЕ переписано — в нём живут подписанные строки, и они улика.
2. `card new c-map-g-one-screen-split-001` — `call`, `to: session`, `play: map`, `for: g-one-screen`, `status: ready`, `issued: 2026-08-12`.
3. `work/c-map-g-one-screen-split-001-call.md` — бриф разделения: шов, улики, что подписано и не переоткрывается, что неисполнимо и почему.
4. `card close c-converge-g-one-screen-repair-001` — наряд починки отработал; починка не состоялась по опровергнутой посылке, маршрут изменён словом владельца.
5. `log add` на `g-one-screen`.
6. `NOW.md` НЕ меняется: `bet: null`. Статус узла НЕ меняется: `parked` — узел разделяет `map`, не эта нога.
7. Карточки узла и хартия НЕ трогаются: `converge` их не правит.

## captures

1. **Правило живёт не там, где его читают** (перенесено из отчёта проверки, не потеряно): механизм `acceptance` записан только в `os/plays/converge.md:16`; `os/plays/shape.md` слова `acceptance` не содержит. Маршрут — `MAINTENANCE REQUEST`.
2. **`panel/PLAN.md` описывает собственный код неверно** как минимум в двух местах: `:116` («строка = сообщение коммита ноги» — на деле файл отчёта) и `:17` («все семь разделов» при девяти в `serve.SECTIONS`). Это ровно класс `g-no-silent-rot` и улика в его пользу; отдельной карточки не заводил.
3. **Пропуск обязательного шага плея дорого стоит.** Шаг 2 `converge` (миньер) был пропущен ногой `-001` из-за запрета харнесса; расплата — одна нога проверки и одна нога починки, и три из четырёх её выводов пришлось отзывать. Когда плей называет обязательного ребёнка, пропуск обязан быть не «названным отступлением», а стопом. Кандидат в заявку.
4. **Проверка не ходит в `done_when` соседей за обратными рёбрами.** `converge-verify` разбирает свой узел и границы наряда; ребро D нашлось только потому, что нога открыла карточку соседа по другому поводу. Кандидат в заявку.
5. Реестр подписей (`owner_approved.md`) отстал на сутки — ни одной записи 2026-08-12 при четырёх подписях. Кандидат в заявку или в `repair`.

## decisions_needed

Нет открытых. Владелец решил в этой сессии: **«разделяй в map»** — сказано на сообщении, назвавшем шов (готовый проход по восьми страницам против светофора и чисел, упирающихся в отсутствующие входы). Шов есть ПРЕДЛОЖЕНИЕ; вердикт по каждой карточке берёт `map` (G9, по одной).

Два вопроса, поставленных владельцу в этой сессии, ответов НЕ получили и уходят в `map` как открытые: вычисляет ли морда полку, и что означает «что сделано». Оба ответа этой ногой были ошибочно подготовлены на неверных основаниях; основания исправлены выше, вопросы остались.

## play_check

- Шаг 1 (triage + import) — не переигрывался: тип и восемь импортов стоят в наборе с ноги `-001`, ни один не переоткрыт.
- Шаг 2 (Define) — **миньер ЗАПУЩЕН**, чем закрыто названное отступление ноги `-001`: шесть параллельных детей по срезам `done_when` и линз хартии, вторые прочтения искались на диске с `path:line`. Найдено 7+ терминов со вторым прочтением, из них вне §GLOSSARY — «что сделано», «средней важности», «каждая запись», «короткая человеческая строка». Термины НЕ записаны в набор: набор замораживается, они уходят входом в `map`.
- Шаг 3 (Resolve) — **НЕ завершён намеренно.** Набор не переписывается: три починки опровергнуты, две владельческие развилки без ответа, а под подписанными строками найдены дыры исполнимости. Записывать §WHAT поверх этого значило бы заморозить неверное.
- Шаг 4 (Close & route) — маршрут изменён словом владельца: не `converge-verify`, а `map`. Наряд исполнителю не выдан.
- G7 — ни одна развилка за владельца не решена; починка A1, которая это делала, отозвана самой ногой до записи.
- G9 — разделение одобрено его словами; карточки детей пишет `map` с повердиктом по каждой.
- Честно о порядке: RESULT произнесён до применения писарем, состояние в Git не уходило до объявленного RESULT.

## log

починка набора не состоялась: три из четырёх её собственных выводов не пережили миньера, а под подписанными строками нашлись дыры исполнимости — узел уходит в map на разделение его словом

## next

`c-map-g-one-screen-split-001` → session, play `map`, узел `g-one-screen`. Разделить по шву «готовое против неисполнимого», вердикт по каждой карточке — владельца. `shape` и `converge-verify` до разделения не открываются.

END_OF_FILE: live/direction-os/history/2026-08-12-s-converge-g-one-screen-003.md
