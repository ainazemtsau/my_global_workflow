RESULT s-work-one-screen-skeleton-g5-001 (call: c-work-one-screen-skeleton-g5-001)
direction: direction-os   play: work   bet: g-one-screen   task: t-one-screen-2

outcome: |
  **БИНДИНГОВАЯ СВЕЖАЯ G5 ЗАДАЧУ НЕ ЗАКРЫЛА.** Строки 1–4 `done_when`
  `t-one-screen-2` пережили попытку опровержения; строка 5 провалена. Задача остаётся
  `open`. В `panel/**` эта нога не исправила ни байта и к `t-one-screen-3…6` не переходила.

  Провал точный: ответ ручки сам называет `gap` отсутствием ИСТОЧНИКА и строит
  `blocks_without_source` из всех блоков с `gap`, но для solmax ставит `gap` блоку
  `running` только потому, что в `NOW.md` нет активной ставки. Поэтому на коммите
  `4b026c0c` ручка отвечает `[] / [] / ["running"]` для direction-os / indie / solmax,
  то есть её собственное число — **один блок из четырёх**, а RESULT решавшей ноги заявляет
  **ноль из четырёх**. Физические источники при этом есть и читаются: это честный ноль
  данных, не отсутствие источника. Два разных ответа на один порог не удовлетворяют строке
  «итог прямо отвечает».

  **Число под порог смерти 2 по полю самой ручки: 1 из 4, не 0 из 4.** Оно ниже порога
  «два и более», но вердикта о судьбе ставки эта нога не выносит: это дело `review`.

evidence: |
  **Венью и дерево.** Проверка прошла в отдельном физическом чате, не в чате сборки
  `4b026c0c`: это binding fresh-session G5, не in-session pre-pass. Для неё поднято отдельное
  detached-дерево `C:\Users\Anton\AppData\Local\Temp\direction-os-g5-4b026c0c`;
  `git status --short --branch` → `## HEAD (no branch)`, HEAD и `build.commit` ручки →
  `4b026c0c`. `git diff --numstat 8e005b32..4b026c0c` → `133  1  panel/serve.py`, других
  файлов в коммите нет. Основная рабочая копия до RESULT чиста.

  **Пять строк задачи — вердикт по каждой.**

  1. **ВЫПОЛНЕНА.** Настоящий HTTP-маршрут на detached-дереве дал 200 для всех трёх
     направлений. В каждом ответе ровно `running / stalled / done_in_window / problems`, у
     каждого блока непустой `how`, а `build` несёт commit/date/root/stale. На дате машины
     2026-08-24 (+03) числа: direction-os `7/0/16/7`, indie `10/0/234/44`, solmax
     `0/0/0/0`. При фиксированной дате кода 2026-08-15 исходные числа воспроизводятся:
     `7/0/16/7`, `10/0/238/44`, `0/0/4/0`.
  2. **ВЫПОЛНЕНА ДЛЯ ЧЕТЫРЁХ ЧИСЕЛ.** Независимый обход файлов через `cards.read_card`, без
     вызовов `dash_*`, дал те же множества строк: ЧТО ИДЁТ 7/10/0; ЧТО СТОИТ 0/0/0;
     ПРОБЛЕМЫ 7/44/0, причём у всех 51 issues первая непустая строка есть; отчёты ног по
     именам в окне 2026-07-17…2026-08-15 — 16/238/4. Фильтры и единицы в четырёх `how`
     совпадают с ручным рецептом. Ошибка ниже не в этих четырёх count, а в отдельной
     классификации честного нуля как отсутствия источника.
  3. **ВЫПОЛНЕНА.** Нулевой `running` solmax не молчит: названо отсутствие активной ставки.
     Полный путь «СТОИТ» атакован настоящей карточкой
     `closed/c-work-g-5a7c-situation-1-001.md`, временно подложенной в отдельный temp-source,
     не в `live/**`: статус `waiting`, id и `waiting_on` дошли в строку блока. По схеме
     `parked` — будущий узел, не остановка; task `blocked` и call
     `blocked|waiting|paused` входят по status; `unblock_when` законен именно при blocked.
  4. **ВЫПОЛНЕНА.** Дифф коммита — только `panel/serve.py`, `panel/app/**` отсутствует;
     `git diff` не добавляет записи, POST/PUT/PATCH/DELETE или новый тип state-файла.
     Источники прежние: `load_cards`, `read_now`, существующая папка history и
     `_history_commits`; панель остаётся read-only.
  5. **ПРОВАЛЕНА.** `dash_block` документирует `gap` как «ИСТОЧНИКА НЕТ»; затем
     `dash_running` ставит этот `gap` при `bet is None`; затем `section_dashboard` считает
     `blocks_without_source` по `b["gap"]`. Фактические ответы: direction-os `[]`, indie
     `[]`, solmax `["running"]`. Одновременно ручной аудит показал существующие и читаемые
     `cards/`, `NOW.md`, `history/`, `unread=[]` у всех трёх. Поэтому заявленное `0/4` не
     совпало с числом ручки `1/4`, и прямого непротиворечивого ответа у задачи нет.

  **Пять названных слабых мест — атака поимённо.**

  1. «Способ счёта верен» — **частично не устояло**: четыре row-count и их `how` совпали с
     независимым обходом, но способ итогового source-gap tally смешал отсутствие данных с
     отсутствием источника; это и валит строку 5.
  2. Пустой блок ЧТО СТОИТ — **устояло**: диск действительно даёт 0/0/0, а реальная карточка
     `waiting` прошла весь путь; законные stalled-виды схемы фильтр не теряет.
  3. Окно solmax = 4 — **устояло на дате кода**. Хост: Belarus Standard Time, +03;
     `date.today()` на коммите был 2026-08-15, окно включительно 2026-07-17…08-15, ровно 30
     дней, 4 файла. Прогнозное 2026-07-15…08-14 даёт 10: выпадают две записи 15-го и четыре
     16-го. На текущую дату 2026-08-24 окно 07-26…08-24 законно даёт уже 0.
  4. Новых источников нет — **устояло**: один изменённый файл, прежние карточки/NOW/history,
     новых state-файлов и путей записи нет.
  5. Старое содержимое ручки не заменено — **устояло как граница**. `t-one-screen-6`
     `done_when` 2 дословно владеет заменой; `verify.py` читает dashboard в четырёх местах
     (:188, :287, :345, :406) и проверяет прежние `ready/other/cards_total/cards_closed`.
     Симуляция их удаления сделала четыре старых predicate ложными у direction-os и indie;
     замена сейчас действительно покраснила бы приёмку чужой задачей.

  **Девять приёмок повторены на detached `4b026c0c`, каждая exit 0 / ПРИНЯТО:**
  `C:\wf-direction-os\.venv\Scripts\python.exe panel/test_osctl.py`;
  `panel/test_cards_cmd.py`; `panel/test_carrier.py`; `panel/test_wave2.py`;
  `panel/test_readers.py`; `panel/test_docs.py`; `panel/test_context.py`;
  `panel/verify.py` (шаги 00, 01b, 01c); `node panel/test_md.cjs`.
  Поиск `blocks_without_source|done_in_window|dash_*` в этих девяти приёмках дал ноль
  совпадений: зелень регресс-набора не проверяет найденное противоречие.

state_changes: |
  1. `c-work-one-screen-skeleton-g5-001` закрыть как возвращённый CALL, не меняя его
     исторический `status: ready`; в журнале назвать провал и этот history-pointer.
  2. `t-one-screen-2` оставить `status: open`; добавить в журнал точный провал строки 5 и
     указатель на этот RESULT. Строки 1–4 не переписывать, другие задачи не трогать.
  3. Создать ready CALL `c-work-one-screen-source-gap-001`: `_bet: g-one-screen`,
     `to: session`, `play: work`, `for: t-one-screen-2`, `issued: 2026-08-24`,
     `call: work/c-work-one-screen-source-gap-001-call.md`, описание — починка противоречия
     `gap`/честного нуля без расширения на соседние задачи. `_pos` назначает `osctl` как
     следующую свободную позицию call.
  4. Создать полный наряд `live/direction-os/work/c-work-one-screen-source-gap-001-call.md`:
     он ограничен устранением найденного source-gap противоречия и focused acceptance,
     запрещает `t-one-screen-3…6` и оставляет задачу открытой до новой binding G5.
  5. Сохранить этот полный RESULT один раз как
     `live/direction-os/history/2026-08-24-s-work-one-screen-skeleton-g5-001.md`; той же
     транзакцией добавить `log` в журналы задачи и нового CALL; закрытый старый CALL уже несёт
     отдельную строку закрытия с тем же history-pointer. NOW, bet/node, `panel/**`, knowledge
     и все посторонние карточки сохранить без изменений.

captures: []

decisions_needed: []

play_check:
  - step 1 recite: done — проверены пять дословных done_when активной ставки g-one-screen/t-one-screen-2.
  - step 2 owner inputs: skipped — владелец не управляет этим артефактом; все факты перевыводимы из коммита, диска и команд.
  - step 3 do the work: done — отдельная физическая binding G5 попыталась опровергнуть пять строк, пять слабых мест и девять приёмок; panel/** не менялся.
  - step 4 self-check: done — один провал отделён от четырёх устоявших строк, число API 1/4 перемерено первой рукой.
  - step 5 close: done — heavy close отклонён: задача остаётся open, возвращённый CALL закрывается, выпущена одна локальная починка той же задачи.
  - G1-G4: done — активная ставка/NOW/appetite/kill_by не меняются, продолжение служит только t-one-screen-2.
  - G5: FAIL для закрытия задачи — свежая binding refutation нашла противоречие в evidence строки 5; это не light close и не in-session pre-pass.
  - G7: done — судьба ставки не решалась; решений владельца не изобретено.
  - G10: done — RESULT выпущен до writer apply; state меняется только его объявленными state_changes.

log: свежая G5 t-one-screen-2 нашла противоречие — четыре числа и строки 1–4 устояли, но ручка считает solmax running блоком без источника и даёт 1/4 против заявленных 0/4; строка 5 провалена, задача остаётся open, выпущена точечная починка source-gap

next: |
  `c-work-one-screen-source-gap-001` — одна свежая `work`-нога той же задачи: устранить
  противоречие между честным нулём solmax и `blocks_without_source`, добавить focused
  acceptance, не трогая вид, имена, свежесть, включение или замену старого содержимого.

END_OF_FILE: live/direction-os/history/2026-08-24-s-work-one-screen-skeleton-g5-001.md
