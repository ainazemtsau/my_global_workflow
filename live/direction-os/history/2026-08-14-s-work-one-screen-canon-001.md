RESULT s-work-one-screen-canon-001 (call: c-work-one-screen-canon-001)
direction: direction-os   play: work   bet: g-one-screen   task: t-one-screen-1

Нога прошла в агентском CLI на этом репозитории и после выпуска этого RESULT стала собственным
писателем: `state_changes` применены командами `osctl` и закоммичены той же сессией.

outcome: |
  Значения пяти слов, которыми судится сводка, лежат в знаниях направления ДО сборки. Файл
  `knowledge/five-words-that-judge-the-summary.md` — первая и единственная запись в
  `knowledge/` направления, которое до этой ноги было пустым не по факту чтения, а по факту
  индекса: `git ls-files live/direction-os/knowledge/` возвращал 0.

  Строка W14 набора («значение записано в канон ДО сборки») получила носителя. Носителя у неё
  не было по устройству, а не по недосмотру: `converge` в `knowledge/` не пишет, `review`
  промотирует уже ПОСЛЕ сборки.

  Закрытие ЛЁГКОЕ (`close: light`). Маршрут выбран по тому, ЧЕМ ЯВЛЯЮТСЯ строки `done_when`,
  а не по фразе наряда: все шесть — либо существование файла и его байты, либо разрешимость
  указателя, либо уже закоммиченные дословные слова владельца. Ни одна не требует суждения,
  которого никто не выносил. Каждая перевыведена этой ногой первой рукой, ниже.

  **Две поправки к наряду, найденные при проверке, а не принятые на веру.**

  1. Наряд (и отчёт `shape`) адресует цитату 2026-08-12 как `cards/g-one-screen.md:19`.
     Строка уехала: сегодня она `:22` — карточка получила `appetite` и `kill_by` в шапку той же
     ногой `shape`. Цитата на месте, номер строки сгнил за сутки. В записи стоит §-якорь
     («§goal, третий абзац»), а не номер.
  2. Наряд утверждает, что «целиком» подписано его словом. Это ВЕРНО, но подписано ОПТОМ:
     прямой цитаты про слово «целиком» не существует ни в одном файле. Существует его фраза
     «принимаю все твои рекоменжации» плюс закоммиченный протокол того, ЧТО стояло
     рекомендацией. Запись говорит это вслух и показывает всю цепочку — иначе следующая нога
     пойдёт искать прямую цитату, не найдёт и переоткроет вопрос. Ровно на несуществующей
     цитате этот узел валился 2026-08-14.

evidence: |
  **Перевывод `done_when` первой рукой. Дерево 673cf921, рабочая копия `C:\wf-direction-os`.**

  1. **Запись есть и несёт пять слов.** `ls live/direction-os/knowledge/` → один файл,
     `five-words-that-judge-the-summary.md`. Пять заголовков-разделов: «что сделано» · «окно» ·
     «детально» · «целиком» · «планы» — по одному вхождению каждого в своём заголовке.

  2. **Каждое значение несёт дословную цитату с датой либо разрешимый артефакт.** Девятнадцать
     указателей открыты по одному и сверены с байтами, не пересказаны:

     - `history/2026-08-14-s-converge-g-one-screen-006.md:8` → «ноги за окно, окно 30 дней,
       детально это охват, планы это волна» — держит слова 1, 2, 3 и 5;
     - `history/2026-08-14-s-converge-g-one-screen-006.md:33` → «Что подпись отклонила,
       записано явно: коммиты…»;
     - `history/2026-08-13-s-map-g-one-screen-split-001.md:9` → «Его слова 2026-08-13,
       дословно: "Для каждого направления я хочу видеть свою сводку…"»;
     - `history/2026-08-14-s-shape-g-one-screen-001.md:26` → «отвечал только на послежний
       вопрос а так принимаю все твои рекоменжации»; `:29` → «имена направлений — вариант (б)
       его собственным ответом»; `:33` → «"целиком" — рекомендация (а): ОХВАТ, а не плотность»;
     - `work/converge-g-one-screen-v3.md` `:120` `:121` `:122` `:126` (глоссарий),
       `:150` W12, `:151` W13, `:152` W14, `:156` W17a, `:172` W33, `:173` W34;
     - `cards/g-one-screen.md:20` (сценарий, «вторую верни»), `:22` (свод через чат),
       `:24` (запрет живых замеров внутри условий).

     Ни одного указателя, который не открылся или открылся не тем. Файлы `history/` неизменяемы
     по схеме, поэтому номер строки там держится; в `cards/` номеров нет намеренно — см.
     поправку 1.

  3. **Отклонённые прочтения названы поимённо.** «что сделано» — коммиты, карточки
     `status: done`, закрытые карточки любого вида; «окно» — календарный месяц; «детально» —
     плотность; «целиком» — плотность; «планы» — `panel/PLAN.md`. Список сверен со строкой
     `history/2026-08-14-s-converge-g-one-screen-006.md:33-35` и глоссарием набора, а не
     составлен заново.

  4. **`read_by` называет реального потребителя и триггер, и потребители перемерены байтами:**
     `t-one-screen-2` — строка 15 её карточки требует блок «что сделано за скользящие 30 дней
     (отчёты ног)»; `t-one-screen-3` — строка 20 требует полосу за скользящие 30 дней, строка
     25 требует прочтение «целиком» как ОХВАТ. Обе задачи живые (`status: open`) и обе в этой
     ставке. Триггер назван: момент, когда задача решает, ЧТО показывает блок; и открытие любой
     следующей ноги узла. «Все» потребителем не названо.

  5. **Трейлер на месте:** последняя строка файла —
     `END_OF_FILE: live/direction-os/knowledge/five-words-that-judge-the-summary.md`.
     Проверка поймала настоящий дефект: первая запись файла кончалась мусорной строкой
     транспорта, трейлер не был последним. Исправлено и перепроверено байтами; посторонних
     тегов в файле ноль.

  6. **До — пусто, после — ровно одна запись, и это она.** До:
     `git ls-files live/direction-os/knowledge/` → 0, и `git status --porcelain` показывал
     `?? live/direction-os/knowledge/` целиком новой папкой. После: `ls` → один файл.

  **`osctl check` → «механических проблем нет», 31 карточка (живых 22, закрытых 9).**

  **Границы наряда соблюдены и перепроверены:** `work/converge-g-one-screen-v3.md` не тронут
  (заморожен уликой), в `panel/**` не тронуто ничего, замеров внутрь записи не положено,
  запись одна, а не пять.

  **Свежесть состояния перечитана перед записью, и это не было формальностью.** Ветка
  `wt/direction-os` стоит `ahead 17, behind 64` относительно `origin/main`. Проверено, чем
  именно: `git log <merge-base>..origin/main -- live/direction-os/ os/` даёт три коммита, все
  чужие — `live/direction-os/` не тронут ни одним, а в `os/` разошлись три файла
  (`FRICTION.md`, `plays/shape.md` одна строка про экран подписи, `schema/direction-files.md`
  два новых имени полей `done_when` и `tasks`). Ни одно расхождение не касается раздела
  `knowledge/` схемы, по которому писалась запись. Рабочей базой осталась текущая ветка.

state_changes: |
  1. `knowledge/five-words-that-judge-the-summary.md` — НОВЫЙ файл, первая запись в
     `knowledge/` этого направления. Ключ шапки по схеме: `accepted` · `source` · `read_by` ·
     `status: current`.
  2. `cards/t-one-screen-1.md` — `status: open` → закрыта как `done` с причиной.
  3. `cards/c-work-one-screen-canon-001.md` → `cards/closed/` с причиной: наряд вернулся
     RESULT'ом.
  4. `history/2026-08-14-s-work-one-screen-canon-001.md` — полный текст этого RESULT.
  5. Строки журнала на `t-one-screen-1`, `c-work-one-screen-canon-001` и `g-one-screen`.
  6. НЕ ТРОНУТО намеренно: `NOW.md` (ставка та же), карточка ставки, пять остальных задач,
     `work/converge-g-one-screen-v3.md` (заморожен), карточки соседних узлов, `panel/**`.
  7. Наряда `review` НЕ заводится: `t-one-screen-1` не последняя открытая задача ставки —
     `t-one-screen-2`…`t-one-screen-6` остаются `open`. Перемерено, а не предположено.

captures:
  - Ссылка вида `cards/<id>.md:<номер>` протухает быстрее, чем живёт то, на что она ссылается:
    цитата 2026-08-12 уехала с `:19` на `:22` за одни сутки, потому что `shape` дописал в шапку
    карточки два поля. Файлы `history/` неизменяемы по схеме, и номер строки там держится
    надёжно; `cards/` меняются каждой ногой. Правило, которое отсюда напрашивается — цитировать
    `cards/` §-якорем, а номером строки только `history/` и код, — этой ногой НЕ вводится: это
    правка `os/**`, её маршрут `os/MAINTENANCE.md`, и одного случая мало (нужны две записи
    FRICTION). Записано, чтобы второй случай нашёл первый.
  - В замороженном наборе строка глоссария «окно» (`work/converge-g-one-screen-v3.md:121`)
    несёт в колонке конкурирующих прочтений фразу «Окно замера M3 (30 дней) выбрано ЭТОЙ ногой
    и источника не имеет», хотя заголовок той же строки говорит «скользящие 30 дней.
    Подписано». Противоречия нет — колонка описывает состояние ДО подписи, — но читается как
    признание, что у окна источника нет. Набор заморожен уликой и правке не подлежит; отсюда и
    ценность записи в `knowledge/`: подписанное прочтение теперь лежит там, где его читают, а
    не только в наборе, который сам себя опровергает на вид.
  - Наряд следующей задачи (`t-one-screen-2`, исполнительская) этой ногой НЕ написан и не
    заведён карточкой. Причина названа, чтобы никто не искал потерянный файл: наряд наряда —
    это техническое КАК по панели, границы этой задачи прямо запрещают ей трогать `panel/**`,
    а бюджет её — один файл. Карточка задачи уже несёт полные `goal` и `done_when` от `shape`.

decisions_needed: []

play_check:
  - шаг 1 (пересказ): done — цель и шесть строк `done_when` пересказаны против карточки задачи
    и карточки наряда; обязательство названо — `t-one-screen-1` активной ставки `g-one-screen`,
    `NOW.md` держит `bet: g-one-screen`. Наряд не обсолетен: он внутри ставки.
  - шаг 2 (владельческий вход): нового входа не требуется, и это не отговорка — артефакт
    целиком СОСТОИТ из его уже закоммиченных слов, ни одна строка не просит у него нового
    суждения. Его слова, на которых стоит запись, процитированы дословно и по адресам:
    «ноги за окно, окно 30 дней, детально это охват, планы это волна» (2026-08-14),
    «отвечал только на послежний вопрос а так принимаю все твои рекоменжации» (2026-08-14),
    «Для каждого направления я хочу видеть свою сводку… никакой нахуй общей» (2026-08-13),
    «какой-то свод я могу получить через чат, а вот это мне нужно» (2026-08-12). Саму ногу он
    запустил своим словом: «run c-work-one-screen-canon-001».
  - шаг 3 (работа): done — одна запись, не пять. Пять слов сведены в одно решённое: чем судится
    содержимое сводки. Строк без источника в записи нет; выведенное ногой ушло в captures, а не
    в знание.
  - шаг 4 (самопроверка): done — шесть строк `done_when` сверены по одной, каждая перевыведена
    из байтов (evidence). Проверка нашла настоящий дефект — отсутствующий трейлер — и он
    исправлен до закрытия, а не описан.
  - шаг 5 (закрытие): done — маршрут лёгкий, основание в outcome. Продолжение названо в `next`.
    Ставка `review`'ом не закрывается: задача не последняя.
  - G1: done — активная ставка одна и та же, `NOW.md` не тронут, полос нет и не заведено.
  - G2: done — работа служит только активной ставке; ничего постороннего не подмешано, две
    находки ушли captures, а не в правку.
  - G3: done — аппетит не тронут; нога уложилась в один файл, как и было нарезано.
  - G5: `close: light — because` все шесть строк `done_when` перевыводятся первой рукой из
    закоммиченных байтов и уже закоммиченных дословных слов владельца: существование и
    содержимое файла, разрешимость девятнадцати указателей, наличие трейлера, пустота
    `knowledge/` до ноги по `git ls-files`, живость двух названных потребителей. Ни одной
    строки про поведение, вид, качество, «работает» или невынесенный вердикт владельца —
    поэтому отдельный чат опровержения не открывался. Маршрут выбран по строкам, не по фразе
    наряда.
  - G7: done — ни одного продуктового решения за него. Там, где наряд утверждал подпись
    сильнее, чем она есть («целиком»), нога не сгладила, а показала цепочку целиком.
  - G9: done — записаны только уже одобренные им вещи; нового одобрения запись не создаёт и
    `owner_approved` не требует.
  - G10: done — шаги показаны, RESULT последний, состояние менялось только после его выпуска и
    только командами `osctl`.

log: канон пяти слов сводки лёг в знания направления до сборки — строка W14 получила носителя, девятнадцать указателей открыты байтами, и наряд поправлен дважды: цитата 2026-08-12 уехала с 19-й строки на 22-ю, а «целиком» подписано оптом, а не прямой цитатой

next: |
  `t-one-screen-2` — проверка самого рискованного допущения ставки: все нужные сводке числа
  уже лежат в читалках панели, и работа есть СБОРКА, а не заведение источников. Задача
  исполнительская, её карточка несёт полные `goal` и `done_when`. Наряда под неё эта нога не
  писала — техническое КАК по панели вне её границ.

  Дальше по порядку `shape`: `t-one-screen-3`, `t-one-screen-4`, `t-one-screen-5`,
  `t-one-screen-6`. Ставка закрывается `review`'ом после последней, не раньше.

END_OF_FILE: live/direction-os/history/2026-08-14-s-work-one-screen-canon-001.md
