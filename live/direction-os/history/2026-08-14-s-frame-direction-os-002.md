# RESULT — s-frame-direction-os-002

play: frame (ревизия устава) · direction: direction-os · call: c-frame-direction-os-002 · date: 2026-08-14

## outcome

Устав пересмотрен в восьми местах и подписан владельцем на точный показанный текст.
Его слова 2026-08-14: **«да, подписываю, и гниющие числа тоже почини»**.

Главное этой ноги — не правки, а то, что **датчик премортема 1 признан слепым, а его
тревога — неподтверждённой**. Устав утверждал, что доля работы в систему выросла
15 → 17 → 28 и потому октябрь под угрозой из-за инструмента. Перевыведено первой рукой:
при любом счёте, где набор измеряемых путей не меняется внутри окна, **пик приходится
на июнь**, а не на август. Рост существовал только за счёт двух артефактов учёта.

Второе: **наряд этой ноги промахнулся мимо дефекта**, и это найдено до правки. Он
требовал переписать критерий успеха 2, потому что тот якобы требует общего экрана.
Тело критерия («понимает, что происходит по всем направлениям, не открывая ни одного
markdown-файла») решению владельца не противоречит: три отдельные страницы его
выполняют. Слово «один» живёт только в ЗАГОЛОВКЕ. Настоящий конфликт сидел тремя
строками ниже — в премортеме 5, где стояли утренний ритуал и запрет на девятый раздел.
Правка сжалась с «переписать критерий» до «переименовать заголовок».

Третье: **разрешение достраивать СВОДКУ уже существовало**, и предыдущая нога зря
объявила его самовольным. Отчёт `2026-08-11-s-frame-direction-os-001.md:70` фиксирует:
«СВОДКА… владелец назвал её первой ставкой и проектирует сам» — сказано в тот же день,
что и граница про девятый раздел. Граница означала «не лепи новое вместо починки».
Правка её не отменяет, а привязывает к факту: достройка раздела, объявленного и
выключенного ДО решения, есть починка.

## evidence

### Числа, на которые теперь ссылается устав

Устав больше не носит чисел в критериях; он указывает на отчёт ноги, которая их
снимала. Это тот отчёт. Все числа выведены первой рукой, каждое с командой и ref.

**Ref: `origin/main` = `2c19a2a6` (интеграционная ветка, не рабочая копия).**
Рабочая ветка `wt/direction-os` в момент замера отставала на 64 коммита и опережала на 8;
счёт с рабочей копии смещён в пользу своего же направления — поэтому взята общая ветка.

**Доля работы в систему, три способа счёта:**

| набор путей | июнь | июль | август (по 14-е) |
|---|---|---|---|
| набор хартии `os/ panel/ osctl.py` | 94/645 = 14,6% | 135/779 = 17,3% | 83/353 = 23,5% |
| честный, неизменный внутри окна | 187/645 = **29,0%** | 135/779 = **17,3%** | 91/353 = **25,8%** |
| он же, `--no-merges` с обеих сторон | 170/496 = **34,3%** | 127/689 = **18,4%** | 91/350 = **26,0%** |

Команда: `git log <ref> [--no-merges] --since=2026-MM-01T00:00:00 --until=2026-NN-01T00:00:00 [-- <пути>] | wc -l`.
Честный набор путей: `os/ os2/ panel/ osctl.py workflow_v3/ workflow/`.

**Почему набор хартии даёт рост, а честный — нет.** Два артефакта, оба проверены:

1. **Пути рождались внутри окна.** `git log --diff-filter=A --format=%ad --date=short --reverse -- <путь> | head -1`:
   `os/` — 2026-06-10, `panel/` — 2026-08-06, `osctl.py` — 2026-08-07, `os2/` — 2026-08-07,
   `workflow_v3/` — 2026-05-30, `workflow/` — 2026-05-11. Для июня и июля числитель
   хартии есть фактически один `os/`, которого до 10 июня не существовало: система жила
   в `workflow_v3/`. И `os2/` (19 августовских коммитов, лежит в корне рядом с `os/`) не
   считается вовсе.
2. **Мерджи исчезли.** `git log --merges --format=%ad --date=format:%Y-%m | sort | uniq -c`:
   июнь **149**, июль **90**, август **3**. Умолчание `git log` применяет упрощение
   истории поверх мерджей, поэтому июнь теряет числитель, а август — нет. У августа с
   тремя мерджами разброс между способами 1,0 пункта; у июня — 12,5.

**Знаменатель недостижим по построению.** Работа над игрой лежит во втором
репозитории, до которого панель не дотягивается: конфигурации в `panel/` нет,
`getenv`/`environ` в `panel/serve.py` — ноль вхождений, а `os/schema/direction-files.md`
запрещает морозить пути к репозиториям в состоянии. Отдельно: у репозитория игры нет
единственной канонической ветки.

**Число нестабильно на горизонте часа.** За время этой сессии `origin/main` сдвинулся
(push в 05:54), августовский знаменатель вырос, доля изменилась на 3,4 пункта. Живому
замеру не место внутри критерия — это и записано теперь в самом уставе.

**Дополнительный замер, честный по смыслу, а не только по путям.** Если считать работой
над системой её код И её бумагу о себе (`live/direction-os/`), против всего остального
включая репозиторий игры (`53afdc2d`): июнь **26,3%**, июль **9,8%**, август **12,4%**.
Пик снова июнь. Ни один из четырёх способов роста не даёт.

### Что именно изменено в уставе

| место | было | стало |
|---|---|---|
| `:3-4` | утверждено 2026-08-11 | добавлена строка о пересмотре 2026-08-14 |
| `:20` крит. 1 | «173 записи…, 3.2 / 2.8 / 1.8» | числа убраны, указатель на отчёт |
| `:22` крит. 2 | заголовок «Один экран правды» | «Правда без файлов» |
| `:58-59` преимущества | «318 ног, ~2100 коммитов»; «173 записи» | без чисел, указатель на отчёт |
| `:93` премортем 1 | «доля уже выросла 15 → 17 → 28» | «роста не измерено… пик на июне; риск остаётся, подтверждения нет» |
| `:95` премортем 1 | мера — число в СВОДКЕ | мера приостановлена как датчик, с названной причиной |
| `:99` премортем 1 | «ловим числом и его глазами» | «ловим его глазами и линзой: числа, которому можно верить, пока нет» |
| `:131` премортем 5 | «раз в день он понимает всё» | «он понимает происходящее» |
| `:133` премортем 5 | «ни разу не начал день с панели… не добавляет девятый раздел» | «ни разу не открыл панель ради ответа на свой вопрос… чинит уже объявленное, а не заводит новое; достройка объявленного и выключенного ДО решения есть починка» |

**ТЕЛО критерия 2 НЕ ТРОГАЛОСЬ.** «Владелец раз в день понимает, что происходит по всем
направлениям, не открывая ни одного markdown-файла. Проверяет это он сам, а не приёмка» —
осталось посимвольно. Три клаузы несущие и сохранены: универсальный квантор «по всем»,
единственный фальсифицируемый тест «не открывая ни одного markdown-файла» и владельческая
клауза «проверяет он сам». Зеркало этого критерия в карточке корня (`g-direction-os.md`
done_when 2) по той же причине не тронуто — оно копирует тело, а не заголовок.

**Миссия НЕ ТРОГАЛАСЬ.** `CHARTER.md:9` («владелец появляется раз в день: совещание,
отчёт с числами, план на день») говорит о ритме автономности, а не о ритме сводки. Это
другое утверждение, и «гармонизировать» его следующей ноге нельзя без отдельного G9.

### Что осталось несогласованным (не чинится здесь)

- `cards/direction_forecast.md:11` ссылается на критерий 2 старым заголовком — «один
  экран правды». Ссылка повисла. Карточка вида `extra` вне поверхности `frame`.
- `cards/i-panel-cannot-reach-the-game-repo-001` заведена без блока `review_when`,
  которого требует схема, — дефект ноги разделения. Плюс её движущая причина ослабла:
  число, ради которого она заводилась, приостановлено.

## state_changes

1. `CHARTER.md` — восемь правок выше, подписаны его словами 2026-08-14. Файл 151 строка
   (мягкий флаг схемы ~150): прибавилась одна строка отметки о пересмотре, прочие правки
   меняли текст в границах существующих строк.
2. `card close i-charter-system-share-number-is-a-git-artifact-001` — запись истрачена:
   число из устава убрано, датчик приостановлен, способ счёта разобран в этом отчёте.
3. `card close c-frame-direction-os-002` — наряд отработал.
4. `log add` на `g-direction-os`.
5. Карточка корня НЕ меняется: тело критерия 2 не тронуто, зеркало верно.
6. `NOW.md` НЕ меняется: `bet: null`.

## captures

1. **Наряд промахнулся мимо дефекта, и это стоило бы переписывания рабочего критерия.**
   Он локализовал конфликт в критерии 2, а конфликт был в премортеме 5. Спасло только то,
   что нога перечитала строку целиком вместо того, чтобы поверить наряду. Правило из
   `os/schema/packets.md` — «grounds are not authority» — сработало ровно как задумано.
2. **Устав носил три гниющих замера в трёх разных местах** (записи трения, ноги и
   коммиты, доля работы в систему) и все три протухли за трое суток. Правило «живой замер
   не живёт внутри критерия» теперь записано в самом уставе, в критерии 1.
3. **Датчик поощрял то, за чем следил.** Знаменатель «все коммиты репозитория» на две
   трети состоит из коммитов, пишущих только состояние, — то есть из бумаги самой
   системы. Чем больше Direction OS писал о себе, тем лучше выглядело число «инструмент
   не съел игру». Класс дефекта общий и стоит проверки на других мерах направления.
4. Премортем 7 («никто не мерил») получил живой экземпляр внутри устава: после
   приостановки премортема 1 единственное оставшееся число — частота трения, а она
   молчит с 2026-08-10 при восьми проведённых с тех пор ногах.

## decisions_needed

Нет. Владелец подписал точный текст в этой сессии: **«да, подписываю, и гниющие числа
тоже почини»** — на восьми местах, показанных ему построчно в виде «было → станет».

## play_check

- Шаги 1-2 (опрос и домашка) — не применялись: это РЕВИЗИЯ существующего устава, а не
  заведение направления. Внешний взгляд, линзы и преимущества не переоткрывались.
- Шаг 3 (черновик устава) — показан построчно, «было → станет», по каждому из мест.
  Итерация была одна: наряд предлагал переписать критерий 2, нога показала, что править
  надо заголовок и премортем 5, и владелец подписал сжатую версию.
- Шаг 4 (премортем) — премортемы 1 и 5 изменены; остальные пять не тронуты. Ни один не
  удалён, ни одна мера не ослаблена: премортем 1 честно объявлен слепым вместо того,
  чтобы носить неверное число.
- Шаг 5 (корневой узел) — не трогался: тело критерия 2 не менялось, зеркало верно.
- Шаг 6 (закрытие) — G9 получен на точный текст его словами. `map_evidence` и наряд карты
  НЕ регистрируются: дерево существует, это ревизия, а не заведение.
- G7 — развилка «править устав или оставить» вынесена ему с обеими сторонами и
  рекомендацией; вторая правка (гниющие числа) им явно санкционирована.
- Честно: нога проведена в том же физическом чате, что и предыдущие, по его указанию.

## log

устав пересмотрен его словом в восьми местах: датчик «инструмент съел игру» признан слепым, а его тревога — артефактом учёта, потому что пик доли приходится на июнь, а не на август

## next

Починка карточки узла «Сводка каждого направления»: вернуть четыре вырезанные половины
подписанных критериев и переписать их под новый текст устава. Затем `converge` заново.
Отдельно и дёшево — повисшая ссылка в `direction_forecast` и отсутствующий `review_when`
в записи про репозиторий игры.

END_OF_FILE: live/direction-os/history/2026-08-14-s-frame-direction-os-002.md
