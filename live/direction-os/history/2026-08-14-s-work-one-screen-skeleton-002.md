RESULT s-work-one-screen-skeleton-002 (call: c-work-one-screen-skeleton-002)
direction: direction-os   play: work   bet: g-one-screen   task: t-one-screen-2

Нога прошла в агентском CLI на этом репозитории и после выпуска этого RESULT стала собственным
писателем: `state_changes` применены командами `osctl` и закоммичены той же сессией.

ЧЕСТНО О ВЕНЬЮ: эта нога прошла в ТОМ ЖЕ физическом чате, который собрал её наряд. Наряд
требовал свежей сессии, и это требование не выполнено — запуск шёл его словом «запускай» в
том же окне. Скрытого знания это не добавило (вся карта местности была выписана в наряд
заранее), но биндинговой проверке следует знать: там, где наряд и нога СОГЛАСНЫ друг с другом,
согласие ничего не подтверждает. Гейт G5 это не отменяет и не ослабляет — он ниже, и он
отдельным чатом.

outcome: |
  **ДОПУЩЕНИЕ ПОДТВЕРДИЛОСЬ, ПОРОГ СМЕРТИ 2 НЕ СРАБОТАЛ. Блоков, которым нечем наполниться из
  существующих читалок, — НОЛЬ из четырёх при пороге «два и более».** Ставка продолжается.

  Ручка `/api/section/<направление>/dashboard` отдаёт по каждому из трёх направлений шапку
  свежести и четыре блока — ЧТО ИДЁТ · ЧТО СТОИТ · ЧТО СДЕЛАНО ЗА 30 ДНЕЙ · ПРОБЛЕМЫ, — и рядом
  с каждым лежит строка способа счёта. Новых источников не заведено ни одного, нового типа файла
  состояния тоже: всё собрано из уже существующих читалок и карточек. Код — коммит `4b026c0c`,
  единственный тронутый файл `panel/serve.py`, +133/−1.

  **Задача НЕ закрыта. Закрытие тяжёлое, и это не формальность:** строка 2 требует, чтобы способ
  счёта был ВЕРНЫМ, а не присутствовал, а строка 1 — чтобы ручка отдавала четыре блока по трём
  направлениям. Первое — суждение, которого никто не выносил. `t-one-screen-2` остаётся `open`,
  выпущен биндинговый наряд в свежую сессию.

  **Наряд был неточен в одной строке, и она поправлена, а не исполнена молча.** Он требовал
  ЗАМЕНИТЬ содержимое ручки, ссылаясь на строку W04 набора. W04 описывает состояние ПОСЛЕ
  включения, и замена — это `done_when` 2 задачи `t-one-screen-6`, которая правит пять
  поверхностей одним шагом. В `panel/verify.py` сегодня зашита форма ответа (`ready`, `other`,
  `cards_total`, `cards_closed`) в четырёх местах: `:188`, `:287`, `:345`, `:406`. Замена здесь
  покраснила бы зелёную приёмку чужой задачей, что прямо запрещено границей этого же наряда.
  Поэтому старый список нарядов едет рядом с четырьмя блоками до задачи 6 — и это записано в
  коде комментарием, а не оставлено на память.

evidence: |
  **Дерево работы `8e005b32`, код `4b026c0c`, рабочая копия `C:\wf-direction-os`.**

  **Таблица четырёх блоков — по строке на блок, числа сняты прогоном ручки по трём
  направлениям (`python -c "import serve; serve.section_dashboard(d)"`).**

  | блок | direction-os | indie | solmax | чем наполняется | источника нет? |
  |---|---|---|---|---|---|
  | ЧТО ИДЁТ | 7 | 10 | 0 | живые карточки: узел `active`, задачи ставки `open`, наряды `ready\|running` | НЕТ (у solmax назван пробел, см. ниже) |
  | ЧТО СТОИТ | 0 | 0 | 0 | живые карточки со статусом `blocked\|waiting\|paused` | НЕТ — ноль честный, доказано ниже |
  | ЧТО СДЕЛАНО ЗА 30 ДНЕЙ | 16 | 238 | 4 | файлы `live/<id>/history/<дата>-<нога>.md` | НЕТ |
  | ПРОБЛЕМЫ | 7 | 44 | 0 | живые карточки вида `issue`, первая фраза блока `## issue` | НЕТ |

  **Число под порог смерти 2: НОЛЬ блоков из четырёх без источника.** Порог — «двум и более
  блокам нечем наполниться». Не сработал. Вердикта о судьбе ставки эта нога не выносит.

  **Единственный названный пробел — solmax, блок ЧТО ИДЁТ.** Ответ несёт его дословно: «активной
  ставки нет: NOW.md направления не называет bet, поэтому задач у блока взяться неоткуда». Это
  ПРАВДА О SOLMAX, а не отсутствие читалки: у направления действительно нет ни ставки, ни задач,
  ни нарядов — 12 живых карточек, из них узлы и `extra`. Блок говорит, чего нет и почему, а не
  отдаётся молча пустым — это строка 3 задачи.

  **ЧТО СТОИТ пуст на всех трёх, и это доказано как ЧЕСТНЫЙ НОЛЬ, а не сломанная читалка.**
  Пустой блок и мёртвый блок с экрана неразличимы, поэтому проверено двумя руками:
  - читалка стреляет, когда признак есть: скормлены четыре синтетические карточки в памяти
    (`blocked`, `waiting`, `paused`, `open`), диск не тронут — вернулось РОВНО три, `open`
    отсеян, и у каждой поднято своё поле причины (`unblock_when` / `waiting_on` / `paused_by`);
  - признак не выдуман и на диске жил: `live/indie-game-development/cards/closed/`
    `c-work-g-5a7c-situation-1-001.md:8` несёт `status: waiting`, а `git log -S"status: blocked"`
    даёт живую историю, включая перевод `t-handlers-1` в `blocked` (коммит `842d3806`).
  Значит сегодня в системе действительно ничто не стоит.

  **Числа сверены с закоммиченным прогнозом ставки, и расхождение НАЙДЕНО И ОБЪЯСНЕНО.**
  Карточка ставки (§forecast) обещала 238 / 14 / 10 в окне 2026-07-15…2026-08-14. Пересчитано
  обоими окнами одной командой:

  | направление | окно прогноза 07-15…08-14 | скользящее окно ноги 07-17…08-15 |
  |---|---|---|
  | direction-os | 16 | 16 |
  | indie-game-development | 238 | 238 |
  | solmax | 10 | **4** |

  - indie 238 — совпало с прогнозом знак в знак;
  - direction-os 16 против обещанных 14 — прогноз считался ДО того, как легли два отчёта:
    сам `shape` и `s-work-one-screen-canon-001`;
  - **solmax 10 → 4 — это работа самого скользящего окна, и это находка, а не ошибка.** У
    solmax последний отчёт ноги датирован 2026-07-20, а до того плотная череда 2026-07-15…17.
    За двое суток сдвига окна из него выпало шесть отчётов. Через шесть дней блок solmax
    станет НУЛЁМ — и это будет правдой, а не поломкой.

  **Строки способа счёта — дословно, как они лежат в ответе (пример direction-os):**
  - ЧТО ИДЁТ: «узел со статусом active, задачи ставки g-one-screen со статусом open и наряды со
    статусом ready|running — считано по одной живой карточке live/direction-os/cards/; ставка
    взята из NOW.md»;
  - ЧТО СТОИТ: «живые карточки со статусом blocked|waiting|paused — тот же признак, которым
    waiting_group метит группу СТОИТ; считано по карточкам live/direction-os/cards/»;
  - ЧТО СДЕЛАНО: «отчёты ног = файлы live/direction-os/history/<дата>-<нога>.md; окно скользящее,
    30 дней, с 2026-07-17 по 2026-08-15 включительно; дата берётся ИЗ ИМЕНИ файла, то есть
    объявлена ногой, а не наблюдена; текст строки — сообщение коммита, добавившего файл
    (git log --diff-filter=A)»;
  - ПРОБЛЕМЫ: «живые карточки вида issue из live/direction-os/cards/ (закрытые не в счёт); строка
    — ПЕРВАЯ ФРАЗА блока `## issue` дословно из карточки, не пересказ и не число».

  **Маршрут проверен по HTTP, а не только вызовом функции.** Панель поднята на порту 8791,
  `/api/section/<направление>/dashboard` отдал код 200 по всем трём, в каждом ответе четыре
  блока с id `running` / `stalled` / `done_in_window` / `problems`, у каждого непустая строка
  способа счёта, шапка свежести на месте.

  **Девять приёмок зелёные, все прогнаны этой ногой:** `test_osctl`, `test_cards_cmd`,
  `test_carrier`, `test_wave2`, `test_readers`, `test_docs`, `test_context` — «ПРИНЯТО» каждая;
  `panel/verify.py` — «ПРИНЯТО», 3 шага (00, 01b, 01c), включая её собственную проверку «live/
  не изменился за прогон»; `node panel/test_md.cjs` — «ПРИНЯТО».

  **Границы соблюдены и перепроверены `git status`:** тронут РОВНО один файл, `panel/serve.py`.
  Ни строки в `panel/app/**` (не рисовали), `dashboard` в `READY_SECTIONS` НЕ добавлен,
  свежесть не чинилась, имена и коды не чинились, в `live/**` не тронуто ничего.

  **Строки, отданные PLAN, названы и оставлены, а не решены:** W21 (сколько строк на входе у
  блока) — ручка отдаёт ВСЕ строки, потолок входа не выбирался; W17 (чем группируются записи и
  в каком порядке) — порядок взят по id как единственный механический, и это записано в `note`
  блока; W09 (область и порог неполноты) — шапка свежести взята как есть, вместе с её известной
  ложью; W23 (какие числа получают график; чем именуется направление) — не трогалось, рисования
  в задаче нет.

state_changes: |
  1. `panel/serve.py` — код уже закоммичен отдельно (`4b026c0c`): `DASH_WINDOW_DAYS`,
     `DASH_STALLED`, `dash_block`, `dash_running`, `dash_stalled`, `dash_done`, `dash_problems`
     и четыре блока плюс шапка свежести в ответе `section_dashboard`.
  2. `cards/t-one-screen-2.md` — ОСТАЁТСЯ `open`. Закрытие тяжёлое, доказательства собирает
     биндинговая проверка. Строка журнала.
  3. `cards/c-work-one-screen-skeleton-002.md` → `cards/closed/` с причиной: наряд вернулся
     RESULT'ом, задача осталась открытой намеренно.
  4. `cards/c-work-one-screen-skeleton-g5-001.md` — НОВЫЙ наряд (`to: session`, `play: work`,
     `for: t-one-screen-2`, `status: ready`) плюс файл
     `work/c-work-one-screen-skeleton-g5-001-call.md`: свежая сессия, попытка ОПРОВЕРГНУТЬ пять
     строк задачи против коммита `4b026c0c`.
  5. `history/2026-08-14-s-work-one-screen-skeleton-002.md` — полный текст этого RESULT.
  6. Строки журнала на `t-one-screen-2`, `c-work-one-screen-skeleton-002` и `g-one-screen`.
  7. НЕ ТРОНУТО намеренно: `NOW.md`, карточка ставки (порог смерти 2 не сработал — менять в ней
     нечего), четыре остальные задачи, `work/converge-g-one-screen-v3.md` (заморожен), карточки
     соседних узлов.
  8. Наряда `review` НЕ заводится: `t-one-screen-2` не последняя и вдобавок остаётся открытой.

captures:
  - Наряд этой ноги требовал ЗАМЕНИТЬ содержимое ручки сводки, опираясь на строку W04 набора, и
    в той же своей границе требовал держать зелёные приёмки зелёными. Эти два требования
    несовместимы: `panel/verify.py` зашивает сегодняшнюю форму ответа в четырёх местах
    (`:188`, `:287`, `:345`, `:406`). Разрешено по тому, ЧЕМ ЯВЛЯЮТСЯ строки задач: замена —
    `done_when` 2 задачи `t-one-screen-6`, W04 описывает состояние после включения. Класс ошибки
    именуется прямо, потому что собирал наряд тот же контур: строка НАБОРА про конечное
    состояние затащена в границу ОДНОЙ задачи как её рез. Правки в `os/**` отсюда не следует —
    случай один, а не два.
  - Блок ЧТО СТОИТ пуст на всех трёх направлениях сегодня. Для задачи `t-one-screen-3` (вид) это
    прямой вход: на экране появится блок-заголовок с нулём строк, и его вид надо решить заранее —
    честное «ничего не стоит» или отсутствие блока. Решает задача 3, здесь только замер.
  - У solmax скользящее окно опустеет через шесть дней (последний отчёт ноги 2026-07-20). Тогда
    у направления обнулятся ТРИ блока из четырёх — ЧТО ИДЁТ уже ноль, ПРОБЛЕМЫ ноль, ЧТО СДЕЛАНО
    станет нулём. Порогу смерти 2 это не считается: источники есть, направление спит. Но для
    вида это тот же вопрос, что и выше, и для владельца это отдельный сигнал о самом solmax.
  - Наряд был исполнен в том же физическом чате, который его собрал, вопреки его собственной
    строке «Свежая сессия». Названо в шапке отчёта, чтобы биндинговая проверка знала цену
    согласия между нарядом и ногой.

decisions_needed: []

play_check:
  - шаг 1 (пересказ): done — пять строк `done_when` пересказаны против карточки `t-one-screen-2`;
    обязательство названо: задача активной ставки `g-one-screen`, `NOW.md` держит её.
  - шаг 2 (владельческий вход): не требуется, и причина не процедурная — задача целиком про
    механику панели, ни одна её строка не просит у владельца факта, который знает только он.
    Его слово в этой ноге ровно одно и оно про запуск: «запускай». Владельческая приёмка сводки
    глазами стоит в `t-one-screen-6` строкой 7 и в узле условием 11 — сюда она не переносится.
  - шаг 3 (работа): done — построена ручка на реальных данных, четыре блока плюс шапка свежести,
    у каждого блока строка способа счёта. Новых источников ноль, новых типов файла состояния
    ноль. Рисования ноль.
  - шаг 4 (самопроверка): done — пять строк сверены по одной (evidence). Проверка нашла
    настоящий дефект НЕ в коде, а в наряде (требование замены против требования зелёных
    приёмок) и разрешила его по строкам задач, а не по фразе наряда.
  - шаг 5 (закрытие): НЕ закрыто намеренно. Задача остаётся `open`, выпущен биндинговый наряд в
    свежую сессию. Ставка `review`'ом не закрывается: задача не последняя и не закрыта.
  - G1: done — активная ставка одна и та же, `NOW.md` не тронут, полос нет.
  - G2: done — работа служит только активной ставке; четыре находки ушли captures, ни одна не
    стала правкой.
  - G3: done — аппетит не тронут, один файл кода, в полдня уложилось.
  - G5: НЕ пройден и не объявлен пройденным. Закрытие тяжёлое: строка 2 требует ВЕРНОСТИ способа
    счёта, а не его присутствия. Это суждение, которого никто не выносил, поэтому `open_call`
    закрыт, а задача оставлена открытой до отдельного свежего чата. Отдельно записано, что сама
    нога прошла не в свежей сессии относительно своего наряда.
  - G6: done — задача самого рискованного допущения исполнена и допущение проверено фактом:
    источники есть, порог смерти 2 не сработал.
  - G7: done — ни одного продуктового решения за владельца. Вердикт о судьбе ставки не вынесен,
    хотя число под порог посчитано: решает `review`.
  - G10: done — шаги показаны, RESULT последний, состояние менялось только после его выпуска и
    только командами `osctl`.

log: скелет сводки собран на реальных данных и допущение ПОДТВЕРДИЛОСЬ — блоков без источника ноль из четырёх при пороге два, порог смерти 2 не сработал; пустой блок «что стоит» доказан честным нулём, а расхождение solmax 10→4 объяснено работой самого скользящего окна; задача НЕ закрыта, выпущен биндинговый наряд

next: |
  `c-work-one-screen-skeleton-g5-001` — свежая сессия: попытка опровергнуть пять строк
  `t-one-screen-2` против коммита `4b026c0c`. Наряд заведён карточкой и НЕ запущен: запуск его
  словом.

  После него по порядку `shape`: `t-one-screen-3` (вид экрана), `t-one-screen-4`,
  `t-one-screen-5`, `t-one-screen-6`. Ставка закрывается `review`'ом после последней.

END_OF_FILE: live/direction-os/history/2026-08-14-s-work-one-screen-skeleton-002.md
