CALL → session · play: map · direction: direction-os · node: g-one-screen

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-map-g-one-screen-split-001

goal
Разделить узел `g-one-screen`. Его слова 2026-08-12: **«разделяй в map»** —
сказаны на сообщении, назвавшем шов: готовая работа отдельно, работа с
отсутствующими входами отдельно. Узел сегодня везёт четыре разные вещи в разном
состоянии, и бумажная фаза дважды подряд находила больше дыр, чем закрывала.

context
- узел: live/direction-os/cards/g-one-screen.md (status parked, не менялся)
- набор требований, ЗАМОРОЖЕН как улика с шапкой REFUTED:
  live/direction-os/work/converge-g-one-screen.md — в нём живут подписанные строки,
  но числа M1 и M3 в теле неверны, верные стоят в шапке
- отчёты, все три законны как вход:
  - history/2026-08-12-s-map-direction-os-001.md — разбор узла, ребро и риск
  - history/2026-08-12-s-converge-g-one-screen-001.md — вид экрана его словом; -002.md — три подписи
  - history/2026-08-12-s-converge-verify-g-one-screen-001.md — восемь опровержений
  - history/2026-08-12-s-converge-g-one-screen-003.md — отзыв трёх починок и пять новых находок
- устав: live/direction-os/CHARTER.md
- соседняя карточка, у которой ребро В этот узел: live/direction-os/cards/g-no-silent-rot.md

шов, названный владельцу и одобренный им как направление разделения
(ПРЕДЛОЖЕНИЕ, не карта: вердикт по каждой карточке берёт эта нога, по одной)

- **ГОТОВОЕ.** Проход по восьми существующим страницам и снятие машинных кодов.
  Ничего нового не требуется: требование подписано его словами («любые коды… я же
  не помню все эти коды»), места известны — десять в `panel/app/app.js`
  (`:206 :254 :262 :269 :271 :363 :459 :476 :515 :577`) плюс два записанных правила
  в `panel/PLAN.md` (`:126` и `:71`), которые придётся снять оба.
- **УПИРАЕТСЯ ВО ВХОДЫ.** Светофор и числа. Полку не из чего читать, второй
  репозиторий нечем достать, базовое число оказалось артефактом ключа git.

что заведомо неисполнимо сегодня (проверено первой рукой, перевыводится)

1. **W11 подписан, входа нет.** `panel/serve.py:19` прибивает корень, `:148-151` —
   единственный вызов git. Конфигурации в `panel/` нет, `getenv`/`environ` в
   `serve.py` — ноль вхождений, строки `GasCoopGame` нет нигде вне архива, а
   `os/schema/direction-files.md` запрещает замораживать пути к репозиториям в
   состоянии («never frozen here»). `knowledge/` направления пуст.
2. **Базовое число — артефакт.** `git log -- os/ panel/ osctl.py` по умолчанию даёт
   14,6 / 17,3 / 27,2%; с `--full-history` — 27,1 / 23,0 / 28,2%. Мерджей в июне
   149, в июле 90, в августе 3. Растущий тренд премортема 1 живёт только при
   умолчании. Плюс август = 27%, не 28%, и копий этого числа в состоянии три:
   `CHARTER.md:92`, `cards/g-one-screen.md:17` (внутри done_when), `cards/direction_forecast.md`.
   Хартию правит только `frame`, и носителя у этой ноги сегодня нет.
3. **Светофор рисует пустоту.** Померено: у direction-os и solmax сегодня не
   загорается ни одна из трёх полок (ставки нет, блокирующего нет); загорается
   только indie. При этом по direction-os прямо сейчас идёт нога.
4. **Развилка входа светофора не решена.** W04 («полки ничем не вычисляются»)
   против W05 («точный список признаков»). Набор отклонил ОБЕ альтернативы:
   и вычисление мордой (D3+D8), и поле важности на карточке («новая обязанность
   на владельце»). Нога `-003` пыталась разрешить это сама и была опровергнута;
   вопрос владельческий и остался без ответа.
5. **«что сделано» — термин без значения и без носителя.** Три прочтения дают
   5/238/10 (ноги), 0/8/0 (задачи `done`), 2/34/0 (закрытые карточки). Раздел
   ИСТОРИЯ строит ленту из ФАЙЛОВ `history/*.md`, не из коммитов, окна в нём нет,
   а `history/` как носитель набор уже отклонил. Вопрос владельческий, без ответа.
6. **Ребро от соседа.** `cards/g-no-silent-rot.md` строка 2 требует показывать
   «что осталось» — узлы, происшествия, наряды, вопросы — на этой морде; строка 4
   заказывает сторож на расхождение записи с изложением, который после подписи
   W06 непостроим по определению (носитель один). Строки на это ребро нет.

что переносится в детей как готовое требование
Из проверки остались верными и не спорны: три атома `done_when`, не дошедшие до
строки («график там, где число само молчит»; «с владельцем ПО ОДНОЙ» — выпало из
требования о проходе; «правило записано» — где живёт после закрытия узла);
референт W01 («1-й вариант») в наборе отсутствует и должен быть внесён дословно;
притязание W26 на порядок («первый пункт, известен заранее») снимается, само
требование снять коды остаётся.

boundaries
- **Подписанное 2026-08-12 не переоткрывается**: вид экрана (светофор по
  направлениям), отсутствие сводного балла, цвет сигналом, важность из записи,
  простое изложение полем существующей карточки, проход по восьми страницам,
  W06, W11, W20. Если ребёнок наследует подписанную строку — он наследует её как есть.
  Неисполнимость W11 — не повод его отменить, а работа, которую надо назвать.
- За владельца ничего не решать. Пять открытых вопросов выше — его, и два из них
  ему уже задавались.
- **Числа в этом наряде — основания, не авторитет.** Перевыведи своей рукой и
  запиши команду; неверное — исправь и сообщи об исправлении.
- `live/**` меняется только применением RESULT. Хартию `map` не правит.
- Дефекты правил здесь не чинятся: `MAINTENANCE REQUEST`, отдельной сессией.
  Названы в отчёте `-003` (`captures` 1, 3, 4).

done_when
Как в os/plays/map.md.

return
RESULT. Наряд исполнителю НЕ выдавать.

budget
Как определяет плей.

END_OF_FILE: live/direction-os/work/c-map-g-one-screen-split-001-call.md
