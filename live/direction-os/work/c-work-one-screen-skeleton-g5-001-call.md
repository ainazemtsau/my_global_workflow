CALL → session · play: work (биндинговая проверка G5) · direction: direction-os · ставка: g-one-screen · задача: t-one-screen-2

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-work-one-screen-skeleton-g5-001
что проверяется: коммит `4b026c0c` (код) на дереве работы `8e005b32`
отчёт ноги: `live/direction-os/history/2026-08-14-s-work-one-screen-skeleton-002.md`

**СВЕЖАЯ СЕССИЯ ОБЯЗАТЕЛЬНА, И ЗДЕСЬ ЭТО НЕ ФОРМАЛЬНОСТЬ.** Предыдущая нога прошла в том же
физическом чате, который собирал ей наряд, и сама это записала. Значит там, где наряд и отчёт
СОГЛАСНЫ, согласие не подтверждает ничего: оба вышли из одного контура. Этот чат обязан быть
другим.

goal
Попытаться ОПРОВЕРГНУТЬ, что пять строк `done_when` задачи `t-one-screen-2` выполнены. Не
подтвердить — опровергнуть. Если после честной попытки ни одна строка не падает, задача
закрывается; если падает хоть одна, она названа и задача остаётся открытой.

## ЧТО ИМЕННО УТВЕРЖДАЕТ ПРЕДЫДУЩАЯ НОГА

Главное её утверждение, и оно решает судьбу ставки: **блоков, которым нечем наполниться из
существующих читалок, — НОЛЬ из четырёх** при пороге смерти 2 «двум и более». То есть порог не
сработал и ставка продолжается. Если это число на самом деле два или больше, узел обязан уйти
в `map`, а сегодня он этого не делает.

Числа, которые она предъявила (свои, не проверенные никем):

| блок | direction-os | indie | solmax |
|---|---|---|---|
| ЧТО ИДЁТ | 7 | 10 | 0 (пробел назван: у solmax нет ставки) |
| ЧТО СТОИТ | 0 | 0 | 0 |
| ЧТО СДЕЛАНО ЗА 30 ДНЕЙ | 16 | 238 | 4 |
| ПРОБЛЕМЫ | 7 | 44 | 0 |

## ГДЕ ОНА ВЕРОЯТНЕЕ ВСЕГО НЕПРАВА — бить сюда первым

Это не список подозрений вообще, а места, где утверждение дороже всего и проверено слабее.

1. **«Способ счёта ВЕРЕН», строка 2.** Нога проверила, что строка способа счёта ПРИСУТСТВУЕТ у
   каждого блока. Что она говорит ПРАВДУ — не проверял никто, и это ровно та подмена, ради
   которой закрытие сделано тяжёлым. Возьми каждую строку способа счёта и пересчитай блок
   РУКАМИ по её же рецепту: сходится ли число, тот ли это набор карточек, тот ли фильтр.
2. **Пустой блок ЧТО СТОИТ.** Нога объявила ноль честным и доказывала это синтетическими
   карточками в памяти. Синтетика доказывает функцию, а не путь целиком: проверь, что реальная
   карточка со статусом `blocked|waiting|paused` дошла бы до ответа ручки, и что ни один живой
   вид карточки со «стоящим» смыслом не потерян по дороге (например узел `parked`, задача,
   которая ждёт чужой работы, наряд с `unblock_when`).
3. **Окно и число solmax = 4.** Нога объяснила расхождение с прогнозом ставки (10) сдвигом
   скользящего окна на двое суток. Пересчитай сам: границы окна, включительность обоих концов,
   часовой пояс/дата машины, и совпадает ли «сегодня» кода с датой, от которой считал прогноз.
   Ошибка на один день здесь меняет число.
4. **«Новых источников не заведено».** Проверь по диффу `4b026c0c`, что ничего не читается
   мимо существующих читалок и карточек, что не появилось нового типа файла состояния (строка
   W31 набора, критерий 4 хартии) и что панель осталась ТОЛЬКО ДЛЯ ЧТЕНИЯ.
5. **Решение не заменять старое содержимое ручки.** Нога отказалась исполнить строку своего
   наряда про замену, сославшись на то, что замена — `done_when` 2 задачи `t-one-screen-6`, а
   `panel/verify.py` зашивает сегодняшнюю форму в четырёх местах. Проверь оба факта: правда ли
   verify.py упал бы, и правда ли замена принадлежит задаче 6. Если нога здесь ошиблась, она
   обошла собственную границу.

## ЧТО ПРОВЕРЯТЬ ДОСЛОВНО — пять строк задачи

Скопированы с `live/direction-os/cards/t-one-screen-2.md`, не редактируются.

1. Серверная ручка сводки отдаёт для КАЖДОГО из трёх направлений четыре блока — что идёт · что
   стоит · что сделано за скользящие 30 дней (отчёты ног) · проблемы содержанием — и шапку
   свежести.
2. У каждого числа в ответе рядом лежит строка способа счёта: из чего и КАКИМ СПОСОБОМ оно
   получено.
3. Блок, которому не хватило источника, назван в ответе поимённо и с причиной, а не отдан
   молча пустым.
4. Рисования в этой задаче нет: проверяется наполнимость, а не вид.
5. Итог задачи прямо отвечает на порог смерти 2 ставки: скольким блокам из четырёх нечем
   наполниться из существующих читалок.

boundaries
- **Меряй сам, руками.** Числа отчёта — заявления, а не факты, пока ты не снял их своей
  командой. Каждое число в твоём отчёте идёт с командой и деревом.
- **Не чинить найденное.** Проверка находит и называет; починку заводит следующая нога. Строка
  падает — она названа падающей, а не подпёрта.
- **Не расширяться на чужие задачи.** Вид (`t-one-screen-3`), имена и коды (`t-one-screen-4`),
  свежесть (`t-one-screen-5`), включение и замена содержимого (`t-one-screen-6`) сюда не
  входят. Известная ложь шапки свежести — НЕ дефект этой задачи.
- **Не выносить вердикт о судьбе ставки.** Твоё дело — верно ли число под порогом. Что с ним
  делать, решает `review`.
- `work/converge-g-one-screen-v3.md` заморожен уликой: читать можно, править нельзя.
- В `panel/**` не писать: это проверка, а не сборка.

done_when
1. Каждая из пяти строк получила вердикт: выполнена / провалена, с уликой первой руки.
2. Число под порог смерти 2 перемерено ТВОЕЙ рукой и названо: сошлось с нолём или нет.
3. Пять мест из раздела «где она вероятнее всего неправа» атакованы поимённо, и по каждому
   сказано, устояло оно или нет.
4. Если всё устояло — `t-one-screen-2` закрывается как `done`. Если нет — остаётся `open`, и
   провалившиеся строки названы на карточке.

return
RESULT. Диспозиция задачи в нём явная: `done` или `open` с перечнем провалов. Прогон девяти
приёмок повторить своей рукой и назвать результат каждой.

budget
Треть сфокусированного дня. Это проверка, а не сборка: если она разрослась до переделки —
это checkpoint, а не продолжение.

END_OF_FILE: live/direction-os/work/c-work-one-screen-skeleton-g5-001-call.md
