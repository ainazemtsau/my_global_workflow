# CONVERGE v3 — g-one-screen «Сводка каждого направления»

leg: s-converge-g-one-screen-005 · date: 2026-08-14 · direction: direction-os

Собран заново на починенной карточке (`04ce3c99`) и пересмотренном уставе (`d44c5e8b`).
Предыдущие два набора заморожены уликами и требований не поставляют:
`work/converge-g-one-screen.md` (REFUTED) и `work/converge-g-one-screen-v2.md` (НЕ ПРИНЯТ).

triage: standard — converge ON — because ≥1 термин читается двумя способами («что
сделано», «свежесть», «имя», «стоит», «планы») И узел делит счёт разделов с соседом
`g-eight-pages`, потребляет от `i-state-accepts-values-it-does-not-declare-001` и несёт
входящее ребро от `g-no-silent-rot`. НЕ heavy: одна страница, на подсистемы не разлагается.

**imported (born-closed; источник ВНЕ этой сессии — записанное решение или код):**

| id | решение | источник |
|---|---|---|
| D1 | ни одного машинного кода на экране; где имени нет — не id | его слова 2026-08-12, `history/2026-08-12-s-converge-g-one-screen-001.md` |
| D2 | заход даёт мало и просто; глубже — по кнопке «подробнее» | `cards/g-one-screen.md`, done_when 9 (действующая редакция). Его слова 2026-08-12 записаны в `work/converge-g-one-screen.md:120` — файл заморожен как требование, но остаётся записью того, что он сказал |
| D3 | скорость (задач за неделю) не показываем | `panel/PLAN.md:177`, его решение 2026-08-06 |
| D4 | панель ничего не оценивает и не советует | `panel/PLAN.md:35` |
| D5 | цвет только сигналом; чёрный OLED, тонкие линии, лаймовый акцент | `panel/PLAN.md:58-82`, 2026-08-06 |
| D6 | достройка объявленного и выключенного раздела есть починка, а не добавление | `CHARTER.md`, граница премортема 5, ревизия 2026-08-14 |
| D7 | мера премортема 1 приостановлена как датчик | `CHARTER.md`, мера премортема 1, ревизия 2026-08-14 |
| D8 | СВОДКУ владелец назвал первой СТАВКОЙ и проектирует сам | `history/2026-08-11-s-frame-direction-os-001.md:70` |

Его слова ЭТОЙ сессии (2026-08-13/14) в таблицу импорта не входят — им место в §SIGNOFF.

## ЗАМЕРЫ

Дерево `04ce3c99`, интеграционная ветка `origin/main` = `2c19a2a6`. У каждого замера
команда: число без способа счёта фактом не является.

**M1 РАЗДЕЛ УЖЕ ОБЪЯВЛЕН И ТОЛЬКО ВЫКЛЮЧЕН.** `panel/serve.py:122-124` — `SECTIONS`
содержит `("dashboard", "СВОДКА")` первым из девяти; `directions()` (`:182-185`) раздаёт
список каждому направлению. `:129-130` — `READY_SECTIONS` это те же девять минус
`dashboard`; причина записана комментарием `:126-128`. Серверная ручка живёт
(`:1036-1037` → `section_dashboard` `:980`), клиентская тоже (`app.js:213-242`
`renderDashboard`, роутинг `:896-899`); невидимость даёт только флаг (`:877-884`).

**M2 ПОЛОСА СВЕЖЕСТИ МЕРЯЕТ НЕ ТУ СТОРОНУ.** `staleness_note` молчит при `behind == 0`
(`serve.py:163-164`), где `behind` = `git rev-list --count HEAD..origin/main` (`:174-175`).
Демонстрация первой рукой: корневая копия `C:/my_global_workflow` стоит ровно на
`origin/main` → `behind = 0`, `unpushed = 0`, полоса молчит. При этом
`git show main:live/direction-os/CHARTER.md | grep -c 'приостановлена как датчик'` → **0**,
а `git diff --stat main..HEAD -- live/direction-os/` → **34 файла, 2309 вставок**.
Панель, поднятая оттуда, объявит себя безупречно свежей, не видя ни подписанного устава,
ни починенной карточки. Приёмка удостоверяет это: `panel/verify.py:113` требует
`(stale is None) == (behind == 0)`.
Возможность существует: `osctl.py:181-186` уже разбирает `git worktree list --porcelain`.
Ограничение честное: `slot_facts` выходит на `:179-180` раньше перебора, если нет ветки
`slot/<направление>/<n>`, а таких веток нет ни у `direction-os`, ни у `solmax`.

**M3 «ЧТО СДЕЛАНО»: РАЗБРОС ПРОЧТЕНИЙ ДО 60×.** Окно 2026-07-15…2026-08-14 выбрано ЭТОЙ
ногой и источника не имеет — какое окно нужно, спрашивает строка W13a. Команды:
отчёты — `ls live/<id>/history/*.md`, отбор по дате в имени (`serve.py:428` `LEG_NAME`);
коммиты — `git log origin/main --format=%h --since=2026-07-15T00:00:00 --until=2026-08-15T00:00:00 -- live/<id> | wc -l`;
карточки — `grep -rl '^status: done' live/<id>/cards/` (`serve.py:101-119` `is_done`);
закрытые — `ls live/<id>/cards/closed/*.md`.

| направление | отчётов ног | коммитов по `live/<id>` | карточек `status: done` | закрытых карточек |
|---|---|---|---|---|
| direction-os | 10 | 1 | 0 | 6 |
| indie-game-development | 238 | 541 | 9 | 34 |
| solmax | 10 | 9 | 1 | 0 |

**Разброс считается ВНУТРИ направления, между прочтениями одного экрана:** indie 541 против 9 = **60×**, direction-os 10 против 1 = 10×, solmax 10 против 1 = 10×. Деление чисел РАЗНЫХ направлений разбросом прочтений не является.
Замер команда-чувствителен: `--since=2026-07-15` без времени даёт indie **540**, с `T00:00:00` — **546**; в таблице стоит форма с временем.
Столбец коммитов взят на `origin/main`, где ещё нет десяти коммитов этой копии, — сама единица зависит от ветки. Носителя С ОКНОМ в панели нет ни одного: `section_history`
(`:489-514`) читает папку целиком, `_history_commits` (`:465-486`) — `git log` без
`--since`, `section_numbers` (`:959-977`) и `section_dashboard` (`:994-996`) считают итоги.

**M4 ЗАПИСИ О ПРОБЛЕМАХ НЕ ПОКАЗЫВАЮТСЯ НИГДЕ.** `serve.py:360-374` считает
`issues_parked`, `:362` исключает их из выдачи, `app.js:546-548` печатает одно число.
Ни один раздел карточки `issue` не рендерит. Живых записей: indie 44, direction-os 6,
solmax 0 (`ls live/*/cards/i-*.md`).

**M5 КОДЫ НА ЭКРАНЕ.** `app.js` печатает машинные id в десяти местах: `:206 :254 :262
:269 :271 :363 :459 :476 :515 :577`. Худшее — `:271` (подпись цели ВСЕГДА оканчивается
кодом даже при живых имени и подзаголовке) и `:254`/`:269` (код ВМЕСТО отсутствующего
имени). Два записанных правила объявляют это правильным: `panel/PLAN.md:126` и `:71`.

**M6 ГРАФИЧЕСКИХ ПРИМИТИВОВ НЕТ.** `grep -n 'svg\|canvas\|<path\|polyline\|chart'
panel/app/app.js` → две строки, `:775` и `:807`, и обе суть `data.charter.sections` /
`data.charter.changed`, то есть подстрока «chart» внутри слова «charter».
`panel/app/style.css` → ноль совпадений. Любой график — новый механизм.

**M7 ВКЛЮЧЕНИЕ СВОДКИ ДЕЛАЕТ ЕЁ ВХОДНЫМ ЭКРАНОМ.** `app.js:951-952` берёт умолчанием
ПЕРВЫЙ ГОТОВЫЙ раздел, а `dashboard` стоит первым в `SECTIONS`.

**M8 ЧТО ТРОГАЕТ ВКЛЮЧЕНИЕ.** `serve.py:129-130`; `serve.py:980-998` (`section_dashboard`
сегодня выбрасывает всё, что не `call`, — `:988`); `app.js:213-242`; `panel/PLAN.md:104-107`
и `:119` (обязательно: `panel/test_docs.py:232-241` сверяет списки из плана с кодом, а
парсер `:166-182` требует жирную метку «**Закрыт…**»); `panel/verify.py:124-125` (список
готовых зашит поимённо без `dashboard`). Пять поверхностей, две из них — зелёные приёмки.

## §GLOSSARY

| Термин | значение здесь | несущие свойства | конкурирующие прочтения |
|---|---|---|---|
| **сводка** | существующий раздел `dashboard`, достроенный до приборной панели одного направления | (a) не новый id и не новая страница; (b) своя у каждого направления; (c) включается флагом в `READY_SECTIONS` | ОТКЛОНЕНО: новая страница рядом с закрытой — на экране осталось бы две «сводки»; ВНЕ ЭКРАНА: сводка через чат (`idea-summary-through-chat-001`, его слово «условно», требованием не является); сегодняшнее содержимое `dashboard` — список нарядов — заменяется, а не дополняется |
| **раздел** | элемент `SECTIONS`; их девять, готовых восемь | (a) сводка — девятый, УЖЕ объявленный; её включение есть починка (D6); (b) соседний узел `g-eight-pages` СЕГОДНЯ приравнивает свои восемь к `READY_SECTIONS`, и это равенство ломается при включении сводки — строка W38 | ОТКЛОНЕНО уставом: «раздел = готовая страница», по которому сводка становится девятой и запрещённой |
| **детально** | ОХВАТ: подробно про ОДНО направление, а не поверхностно про все. Подписано | (a) слово ничего не говорит о плотности входного экрана и с «мало и просто» (W20) не спорит; (b) противопоставление идёт с ОБЩИМ экраном | прочтение А, ОХВАТ: «детально именно по ЭТОМУ направлению, никакой общей» — противопоставление идёт с общим экраном (его слова 2026-08-13, `history/2026-08-13-s-map-g-one-screen-split-001.md:9`). Прочтение Б, ПЛОТНОСТЬ: много на одном экране. Под Б слово сталкивается с его же подписанным «заход даёт мало и просто» (done_when 9, D2): обе строки acceptance, обе едут в наряд дословно, зелёными одновременно быть не могут |
| **окно** | скользящие 30 дней. Подписано | (a) не календарный месяц; (b) одно и то же окно у всех направлений | done_when 4 требует «за окно, а не за всю жизнь», но какое окно — не сказано нигде. Окно замера M3 (30 дней) выбрано ЭТОЙ ногой и источника не имеет. Второе живое окно в системе — `os/adapters/coding-agent.md`, digest «since yesterday» |
| **что сделано** | ОТЧЁТЫ НОГ за окно. Подписано | (a) единица — одна нога, один файл `live/<id>/history/<дата>-<id>.md`; (b) носитель уже построен: `serve.py:489-514` `section_history`, раздел в `READY_SECTIONS`; (c) окно накладывается фильтром поверх существующих строк, нового источника не заводится; (d) ЧЕСТНО: дата берётся из ИМЕНИ файла (`serve.py:456-458`), то есть объявлена ногой, а не наблюдена — расхождение с датой коммита встречается | ОТКЛОНЕНЫ его подписью 2026-08-14: коммиты (единица зависит от ветки и формы даты, M3), карточки `status: done` (дают 0 у двух направлений из трёх), закрытые карточки любого вида. СНЯТОЕ ВОЗРАЖЕНИЕ: первый набор отклонял `history/` как носитель «записи» — то возражение касалось ПРОСТОГО ИЗЛОЖЕНИЯ, которое его словом 2026-08-13 вынесено из узла отдельной вещью, и на «что сделано» больше не распространяется. Разброс прочтений внутри направления был до 60× (M3), поэтому строка и уходила к нему |
| **свежесть** | честность экрана о полноте показанного | (a) молчание допустимо только когда показанному нечего добавить из других рабочих копий ЭТОГО репозитория — дословно done_when 2 | ОТКЛОНЕНО: `behind origin/main` — корневая копия при `behind = 0` не видит 34 файлов направления (M2). НАЗВАНО ЧЕСТНО: буквальная (a) сегодня истинна для КАЖДОЙ копии по каждому направлению, то есть полоса горела бы всегда, а это тот же отказ, что молчать всегда. Область и порог — строка W09 `open` → PLAN; ни то, ни другое эта нога не выбирает. ВНЕ УЗЛА: чужой репозиторий игры (`i-panel-cannot-reach-the-game-repo-001`) |
| **имя** | человеческое поле карточки, которое панель показывает вместо id | (a) где имени нет — на экране «без имени», никогда id | сегодня в коде ТРИ ответа для карточек: `serve.py:311-312` считает именем только `label`; `serve.py:288` `WAIT_TITLE_FIELDS` принимает пять полей; `app.js:254` берёт `r.goal`. У НАПРАВЛЕНИЯ человеческого имени нет вовсе: `serve.py:182-185` строит запись из имени каталога, а `app.js:87` и `:122` печатают машинный слаг как единственное имя — оба места ВНЕ десяти, посчитанных M5. Строка W23 `open` → PLAN |
| **стоит** | дело, которое не едет и само не поедет | (a) факт остановки и её причина едут вместе | сегодня ДВА прочтения: `serve.py:285` плюс `:305-309` — наряд со статусом `blocked/waiting/paused` (группа «СТОИТ», помечена неблокирующей); `serve.py:219-221` `NODE_STATE` — узел `parked` показывается как «ДАЛЬШЕ», то есть стоящим не считается. Развязка ждёт `i-state-accepts-values-…-001` (W15) |
| **планы** | раздел ВОЛНА. Подписано | (a) раздел по направлению, уже готовый; (b) `panel/PLAN.md` этим словом здесь НЕ называется | раздела с таким именем в `SECTIONS` нет; кандидаты — ВОЛНА и ЦЕЛИ; третье прочтение — `panel/PLAN.md`, файл межнаправленческий, что сталкивается с атомом цели «обе работы про ОДНО направление за раз». Слово его собственное, из его сценария |
| **цепочки** | лента событий цели, уже существующая | `serve.py:797` `node_events` строит её из `git log --grep=<id>` по направлению | — |
| **проблемы** | живые карточки вида `issue` этого направления | (a) видны тем, что в них написано; (b) живут на сводке, не в «ЖДЁТ ТЕБЯ» | ОТКЛОНЕНО: «ЖДЁТ ТЕБЯ» — раздел намеренно означает «тут нужен ты», а записи ждут события (`serve.py:293-294`); ОТКЛОНЕНО: одно число — сегодняшнее поведение, которое и чинится. Сколько их на входе и чем разворачиваются — W21 → PLAN |
| **приёмка** | автоматические проверки `panel/test_*.py` и `panel/verify.py` | узел не закрывают ни при каком исходе | НЕ ОЗНАЧАЕТ гейт «опровергнуть в свежей сессии» — он живёт в ядре и этой строкой не затрагивается |

## §WHAT

Тег `acceptance` = строку `shape` копирует в наряд дословно (`os/plays/converge.md:16`).
Строка, ссылающаяся на `open`, сама `open` — поэтому зависимые половины разрезаны.

| id | требование | тег | статус | источник / отвечающий |
|---|---|---|---|---|
| W01 | Сводка открывается ВНУТРИ направления и говорит только о нём, ДЕТАЛЬНО | acceptance | answered | его слова 2026-08-13, `history/2026-08-13-s-map-g-one-screen-split-001.md:9` |
| W02 | Общего экрана по всем направлениям НЕ СУЩЕСТВУЕТ — ни одной страницы, сводящей направления вместе | acceptance | answered | там же: «никакой общей» |
| W03 | Строится существующий раздел `dashboard`; нового id раздела не заводится | acceptance | answered | M1 плюс D6 плюс D8 |
| W04 | Сегодняшнее содержимое `dashboard` (список нарядов) заменяется, а не дополняется | acceptance | answered | `serve.py:126-128` |
| W05 | Включение правит ПЯТЬ поверхностей одним шагом: `serve.py:129-130`, `serve.py:980-998`, `app.js:213-242`, `panel/PLAN.md:104-107` и `:119`, `panel/verify.py:124-125` | acceptance | answered | M8 — иначе две зелёные приёмки краснеют |
| W06 | Сводка становится входным экраном направления | acceptance | answered | его слова 2026-08-13 «да, берём» на показанном описании «заходишь в направление, попадаешь на его сводку»; механику подтверждает M7 |
| W07 | Заход в любой момент показывает состояние на сейчас | acceptance | answered | done_when 2 плюс его слова 2026-08-13 «когда угодно» |
| W08 | Экран не утверждает свежести, которой у него нет | acceptance | answered | done_when 2 плюс M2 |
| W09 | Чем меряется неполнота, по какой области и каков порог | — | open | → PLAN. Буквальная область «все копии по всем направлениям» даёт вечно горящую полосу; сужение выбирает PLAN, не эта нога |
| W10 | `panel/verify.py:113` меняется вместе с определением свежести | acceptance | answered | M2 |
| W11 | Видно, над чем идём и что едет | acceptance | answered | done_when 3 плюс `serve.py:219-221`, `NOW.md` |
| W11a | Видно, что стоит | — | open | зависит от W15; сегодня «стоит» читается двумя способами |
| W12 | «Что сделано» — это ОТЧЁТЫ НОГ за скользящие 30 дней | acceptance | answered | его слова 2026-08-14: «ноги за окно, окно 30 дней, детально это охват, планы это волна» |
| W13 | Оно показано за окно, а не за всю жизнь; окно скользящее, не календарный месяц | acceptance | answered | W12 подписан его слова 2026-08-14: «ноги за окно, окно 30 дней, детально это охват, планы это волна» |
| W14 | Значение «что сделано» записано в канон направления ДО сборки | — | open | носителя нет: `converge` в `knowledge/` не пишет, `review` промотирует ПОСЛЕ сборки, `knowledge/` пуст. Кто и чем записывает — отвечает PLAN |
| W15 | «Где залочено и почему» — причина едет вместе с фактом | — | deferred | → `i-state-accepts-values-it-does-not-declare-001` |
| W16 | Записи о проблемах видны тем, что в них написано, а не одним числом | acceptance | answered | done_when 5 плюс M4 |
| W17 | Чем группируются записи и в каком порядке | — | open | → PLAN. У `issue` нет ни важности, ни срочности: схема даёт `level`, `route`, `review_when`, `blocks`. Сортировка по строке id запрещена W24 |
| W17a | «Планы» в сценарии — это раздел ВОЛНА | acceptance | answered | его слова 2026-08-14: «ноги за окно, окно 30 дней, детально это охват, планы это волна» |
| W18 | Сценарий: он ЗА МИНУТЫ проходит ВОЛНУ и цепочки, собирает список непонятного и идёт с ним в чат | acceptance | answered | существо — его слово 2026-08-13 «вторую верни»; «планы» разъяснены 2026-08-14 |
| W19 | Панель остаётся ТОЛЬКО ДЛЯ ЧТЕНИЯ; требований к его поведению нет | acceptance | answered | goal-блок узла плюс D4 |
| W20 | Заход даёт мало и просто; глубже — по кнопке «подробнее», и клик по строке ведёт внутрь | acceptance | answered | D2 |
| W21 | Сколько строк на входе у каждого блока и чем разворачивается | — | open | → PLAN, дёшево в откате |
| W22 | График стоит там, где он читается лучше числа; где число говорит само — просто число | acceptance | answered | done_when 7 — его критерий, не выбор ноги |
| W23 | Какие числа получают график; какие поля считаются «именем» у какого вида; чем именуется НАПРАВЛЕНИЕ | — | open | → PLAN |
| W24 | Ни одного машинного кода; где имени нет — «без имени» | acceptance | answered | D1. Область шире десяти мест M5: `app.js:87` и `:122` печатают слаг направления — их закрывает W23 |
| W25 | Два записанных правила, объявляющих коды правильными, снимаются | acceptance | answered | M5 — `panel/PLAN.md:126` и `:71` |
| W26 | Цвет только сигналом, никогда украшением; стиль 2026-08-06 | acceptance | answered | D5 |
| W27 | Рядом с каждым числом видно, из чего и КАКИМ СПОСОБОМ оно посчитано | acceptance | answered | done_when 10 плюс линза 3 хартии |
| W28 | Закрывает его слово: пользуется он этим или нет | acceptance | answered | done_when 11 плюс хартия, критерий 2 |
| W29 | Автоматическая приёмка панели узла не закрывает | acceptance | answered | done_when 11 |
| W30 | График — новый механизм: примитивов в панели нет | — | answered | M6 |
| W31 | Новый тип файла состояния не заводится | — | answered | хартия, критерий 4 |
| W32 | Узел не назначает дат и сроков | — | answered | хартия: «ни срока, ни потолка, ни темпа» |
| W33 | «Детально» означает ОХВАТ — подробно про одно направление, а не плотный экран; с «мало и просто» (W20) не спорит | acceptance | answered | его слова 2026-08-14: «ноги за окно, окно 30 дней, детально это охват, планы это волна» |
| W34 | Он видит направление ЦЕЛИКОМ, и файлов для этого не открывает | acceptance | answered | goal-блок узла плюс хартия, критерий 2 |
| W35 | Свод — работа чата и на экране не живёт; экран несёт визуальную иерархию, числа, графики и проход по цепочке | acceptance | answered | goal-блок узла, его слова 2026-08-12: «какой-то свод я могу получить через чат, а вот это мне нужно» |
| W36 | Ребро соседа: на морде выводятся из карточек открытые узлы, неистраченные наряды и незаданные вопросы — не только происшествия | — | open | → `map`/`review`. `g-no-silent-rot` done_when 2 называет этот узел носителем поимённо; W16 несёт один носитель из четырёх |
| W37 | Ребро соседа: сторож «запись изменилась, изложение — нет» после выноса простого изложения требует переписи | — | open | → `map`/`review`. `g-no-silent-rot` done_when 4 |
| W38 | Ребро соседа: `g-eight-pages` считает свои восемь поимённо, а не через `READY_SECTIONS`; и неподписанная оговорка ноги из его done_when 5 снимается | — | open | → `map`/`review`. Предыдущая нога это обещала и не сделала — проверено на диске |
| W39 | Ребро записи: `i-state-accepts-values-…-001` ждёт события по НОМЕРУ строки этого узла; ссылаться надо на имя условия | — | open | → `repair` |

**ЛИНЗЫ ХАРТИИ — вердикт по каждой:**

| линза | вердикт |
|---|---|
| 1. Трение | Записи в `os/FRICTION.md` про «не начинаю день с панели» НЕТ ни одной. Оправдание держится на замерах M4, M5, M7 — критерий 1 хартии это допускает. Названо честно |
| 2. Что придётся помнить | НИЧЕГО не добавляется: сводка только читает, полей заполнять не надо. Убирается — коды (W24) |
| 3. Проверяемость | Несёт W27: рядом с числом виден способ счёта |
| 4. Автономность | `not_needed`: хартия несёт дальний север в миссии; живёт записью `i-autonomy-eligible-classes-list-is-empty-001` |
| 5. Цена для игры | Работа над инструментом, ограниченная списком. Мера, которой цену меряли, приостановлена ревизией устава как слепая — значит цену судят линза и его глаза, а не число |

**Итого:** 41 строка · answered 30 · open 10 · deferred 1; acceptance-строк 27, все `answered` — правило `converge.md:16` соблюдено механически (счёт по таблице, не на глаз).

**Владельческих открытых строк НЕТ.** Три последние — W12, W17a, W33 — подписаны его
словами 2026-08-14; зависимые W13 и W18 закрылись вместе с ними. Активацию узла ничто
не блокирует; оставшиеся открытые строки все адресованы PLAN, соседям или записи.

**Следствие подписи W12, названное честно:** при окне 30 дней отчётов ног у indie — 238.
Столько строк на входе валит «мало и просто» (W20), поэтому объём входа (W21) из
дешёвого параметра становится обязательным к решению при сборке.

## §SIGNOFF

**Define — что закрыто и чем.**

- Закрыты кодом и записанными решениями: «сводка», «раздел», «проблемы», «приёмка»,
  «цепочки», «стоит» (частично, с отложенной развязкой), «свежесть» (требование, но не механизм).
- **Закрыты его словом 2026-08-14:** «что сделано» и «окно» (W12), «детально» (W33),
  «планы» (W17a). Прочтения, отклонённые этой подписью: коммиты, карточки `status: done`,
  закрытые карточки; «детально» как плотность; «планы» как `panel/PLAN.md`.
- **НЕ закрыто, отдано PLAN:** механизм и порог свежести (W09), группировка записей
  (W17), объём входа (W21), поля имени и имя направления (W23), носитель канона (W14).
- **Решения этой ноги, названные прямо:** окно 30 дней в замере M3 выбрано ногой и
  источника не имеет — поэтому оно ушло в вопрос W12, а не в требование.

**Resolve — его слова.** Разделены по тому, лежат ли они в закоммиченном состоянии.

Записаны в состоянии:

- 2026-08-13, `history/2026-08-13-s-map-g-one-screen-split-001.md:9`: **«Для каждого
  направления я хочу видеть свою сводку. Детально именно по этому направлению, никакой
  нахуй общей. У каждого направления своя сводка»** — запирает W01, W02;
- 2026-08-13, там же: **«да, берём»** на описании сводки, и **«вторую верни, первую
  отдельной вещью»** — возвращает сценарий, выносит простое изложение;
- 2026-08-14, `history/2026-08-14-s-frame-direction-os-002.md`: **«да, подписываю, и
  гниющие числа тоже почини»** — ревизия устава, откуда born-closed D6 и D7.

Сказаны в этой сессии и в состояние лягут отчётом ноги-продолжения:

- **«чини карточку и собирай набор заново»** — санкция на починку узла и пересборку;
- **«ноги за окно, окно 30 дней, детально это охват, планы это волна»** — закрывает три
  последние владельческие строки, показанные ему по одной с их ценой и числами.

Ранее подписанное и не переоткрытое: коды (2026-08-12), «подробнее» (2026-08-12),
скорость (2026-08-06), стиль панели (2026-08-06).

END_OF_FILE: live/direction-os/work/converge-g-one-screen-v3.md
