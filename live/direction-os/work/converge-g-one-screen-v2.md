# CONVERGE v2 — g-one-screen «Сводка каждого направления»

> ## ⛔ НЕ ПРИНЯТ — В СОСТОЯНИЕ НЕ ВОШЁЛ, СТРОИТЬ НЕЛЬЗЯ
>
> Набор провалил собственную пре-проверку в той же ноге: **52 находки, 12 смертельных**,
> и симуляция биндинговой проверки вынесла «не выживет» — четыре проблемы из шести
> внутри `converge` не чинятся. Разбор: `history/2026-08-14-s-converge-g-one-screen-004.md`.
> Владелец решил править устав; следующая нога — `frame` (`c-frame-direction-os-002`).
> Файл остаётся уликой ради замеров; требования из него не переносить.
>
> **Что из замеров ВЫЖИВАЕТ** (перевыведено на дереве `df13f0b8`): M1 — раздел
> `dashboard` уже объявлен, маршрутизирован и только выключен флагом, новый заводить не
> надо; M2 — полоса свежести молчит структурно (`behind = 0` во всех восьми копиях), а
> `panel/verify.py:113` удостоверяет молчание как правильное поведение; M3 — разброс
> «что сделано» до 60× внутри одного окна; M4 — карточки `issue` не рендерятся нигде;
> M5 — коды в десяти местах `app.js`; M7 — включение сводки автоматически делает её
> входным экраном; M8 — включение трогает пять поверхностей, две из них зелёные приёмки.
>
> **Что СНЯТО как неверное:**
> - **M6** — два совпадения grep в `app.js` это подстрока «chart» внутри слова «charter»
>   (`:775`, `:807`), а не имя переменной `bar`. Вывод «графических примитивов нет» верен,
>   улика была неверна.
> - **W10 и M2, часть про механизм** — «механизм уже написан в `osctl.py:181-191`» неверно:
>   `slot_facts` выходит на `if not exists: return facts` РАНЬШЕ перебора копий, а веток
>   `slot/direction-os/*` не существует. Для двух направлений из трёх функция возвращается
>   пустой.
> - **§SIGNOFF, довод про «что сделано»** — опровергнут фактом: прошлая редакция карточки
>   (`993b32b4`, критерий 3) ставит «что сделано» первым в ряду ЧИСЕЛ.
> - **W06, W10** — стояли `acceptance`/`answered` на прочтениях, выбранных самой ногой;
>   плей это запрещает, а признание в §SIGNOFF правило не выполняет.
> - **D3, D4** — стоят в таблице «источник вне этой ноги» неправомерно: их источник и есть
>   эта сессия.

leg: s-converge-g-one-screen-v2-001 · date: 2026-08-13 · direction: direction-os

Предыдущий набор (`work/converge-g-one-screen.md`) заморожен под баннером REFUTED и
описывал ОТМЕНЁННУЮ им форму — общий светофор по направлениям. Этот набор к нему не
пристраивается: он собран заново на узле после разделения.

triage: standard — converge ON — because ≥1 термин читается двумя способами
(«что сделано», «свежесть», «сводка», «раздел») И узел делит счёт разделов с соседом
`g-eight-pages` и потребляет от `i-state-accepts-values-it-does-not-declare-001`.
НЕ heavy: одна страница, на подсистемы не разлагается.

imported (born-closed, источник вне этой ноги; не переоткрывались):

| id | решение | источник |
|---|---|---|
| D1 | у каждого направления СВОЯ сводка; общей по всем направлениям нет | его слова 2026-08-13 |
| D2 | заходит когда угодно, не только утром; «больше графически представлено» | его слова 2026-08-13 |
| D3 | сценарий «пока работает ассистент» остаётся в этом узле | его слова 2026-08-13: «вторую верни» |
| D4 | короткая человеческая строка у каждой записи уходит ОТДЕЛЬНОЙ вещью | его слова 2026-08-13: «первую отдельной вещью» |
| D5 | ни одного машинного кода на экране | его слова 2026-08-12 |
| D6 | заход даёт мало и просто; глубже — по кнопке | его слова 2026-08-12 |
| D7 | скорость (задач за неделю) не показываем | `panel/PLAN.md:177`, его решение 2026-08-06 |
| D8 | панель ничего не оценивает и не советует | `panel/PLAN.md:35` |
| D9 | цвет только сигналом; чёрный OLED, тонкие линии, лаймовый акцент | `panel/PLAN.md:58-82`, 2026-08-06 |
| D10 | наряды и происшествия — содержание, а не счётчик | `done_when` узла, критерий 6 старой редакции |

## ЗАМЕРЫ ЭТОЙ НОГИ

Все выведены первой рукой на дереве `df13f0b8` (2026-08-13). У каждого записана
команда: замер без способа счёта фактом не является — это урок предыдущего набора,
где одно и то же число давало 14,6% и 27,1% в зависимости от неназванного ключа.

**M1 СВОДКА УЖЕ ЗАВЕДЕНА И ТОЛЬКО ВЫКЛЮЧЕНА.** `panel/serve.py:122-124` — `SECTIONS`
содержит `("dashboard", "СВОДКА")` ПЕРВЫМ из девяти, и `directions()` (`:182-185`)
раздаёт этот список каждому направлению. `:129-130` — `READY_SECTIONS` это те же
девять минус `dashboard`; причина записана комментарием `:126-128` («сегодняшний её
вид врал»), а не «её нет». Ручка живёт: `:1036-1037` маршрутизирует
`/api/section/<направление>/dashboard` в `section_dashboard` (`:980`). Клиент готов:
`panel/app/app.js:213-242` `renderDashboard` существует, `:896-899` на неё роутит,
невидимость даёт только флаг (`:877-884`, `:892-895` «РАЗДЕЛ ЕЩЁ НЕ СДЕЛАН»).
**Вывод: это заполнение объявленного места, а не десятый раздел.**

**M2 ПОЛОСА СВЕЖЕСТИ МОЛЧИТ СТРУКТУРНО, А ПРИЁМКА ЭТО УДОСТОВЕРЯЕТ.**
`staleness_note` возвращает `None` при `behind == 0` (`panel/serve.py:163-164`), а
`behind` = `git rev-list --count HEAD..origin/main` (`:174-175`).
Замер: `for b in main wt/direction-os wt/indie-game-development wt/solmax; do
git rev-list --count $b..origin/main; git rev-list --count origin/main..$b; done`
→ **behind = 0 у всех; ahead = 0 / 7 / 59 / 9.** Работа лежит ВПЕРЕДИ в чужих копиях,
а полоса умеет только «позади».
Худший случай проверен: `git show main:live/direction-os/cards/g-one-screen.md` →
файла в `main` НЕТ; `git diff --stat main..wt/direction-os -- live/direction-os/`
→ **26 файлов, 1493 вставки**. Панель, поднятая из корневой копии, показала бы
всё зелёным, не видя ни одного из них.
Приёмка прибивает дефект: `panel/verify.py:113` требует
`(stale is None) == (behind == 0)`, `panel/test_readers.py:380-381` дёргает только
`staleness_note(0,…)` и `(16,…)`.
Возможность существует и уже написана: `osctl.py:181-191` `slot_facts` гоняет
`git worktree list --porcelain`, разбирает branch→worktree и считает ahead —
ограничение только в том, что смотрит ветки `slot/<direction>/<n>`.

**M3 «ЧТО СДЕЛАНО»: РАЗБРОС ДО 60× ВНУТРИ ОДНОГО ОКНА.** Окно 2026-07-14…2026-08-13,
правило счёта взято из кода (`panel/serve.py:428` `LEG_NAME`, `:101-119` `is_done`):

| направление | отчётов ног | коммитов по `live/<id>` | карточек `status: done` |
|---|---|---|---|
| direction-os | 7 | 8 | 0 |
| indie-game-development | 238 | 483 | 9 |
| solmax | 15 | 14 | 1 |

Единица «коммит» сама неоднозначна (8/9, 483/497, 14/24 — с `--full-history` и без).
Носителя с ОКНОМ в панели нет ни одного: `section_history` (`:489-514`) читает папку
целиком через `os.listdir` без отсечки по времени; `_history_commits` (`:465-486`) —
`git log` без `--since`; `section_numbers` (`:959-977`) и `section_dashboard`
(`:994-996`) считают итоги за всю жизнь; единственная длительность `bet_days` (`:973`)
— возраст ставки, не окно.

**M4 ЗАПИСИ О ПРОБЛЕМАХ НЕ ПОКАЗЫВАЮТСЯ НИГДЕ.** `panel/serve.py:360-374` считает
`issues_parked`, `:362` исключает их из выдачи, `panel/app/app.js:546-548` печатает
одно число. Ни один раздел карточки `issue` не рендерит.
Замер `ls live/*/cards/i-*.md | wc -l`: indie **44**, direction-os **7**, solmax **0**.
Число по direction-os выросло с 4 до 7 за сутки, и три из семи завела нога разделения.

**M5 КОДЫ НА ЭКРАНЕ.** `panel/app/app.js` печатает машинные id в десяти местах:
`:206 :254 :262 :269 :271 :363 :459 :476 :515 :577`. Худшее — `:271`, где подпись цели
ВСЕГДА оканчивается кодом даже при живых имени и подзаголовке, и `:254`/`:269`, где
код встаёт ВМЕСТО отсутствующего имени. Два записанных правила объявляют это
правильным: `panel/PLAN.md:126` и `:71`. Снятие кодов — общая работа с `g-eight-pages`.

**M6 ГРАФИЧЕСКИХ ПРИМИТИВОВ В ПАНЕЛИ НЕТ.** `grep -c 'svg\|canvas\|chart\|<path\|polyline'`
→ `panel/app/style.css` 0; два совпадения в `app.js` — имя переменной `bar` в блоке
сроков (`:45-68`) и в полосе проблемы (`:107-108`), не графика. Любой график в
строке 7 — новый механизм, а не настройка существующего.

**M7 ВКЛЮЧЕНИЕ СВОДКИ ДЕЛАЕТ ЕЁ ВХОДНЫМ ЭКРАНОМ АВТОМАТИЧЕСКИ.**
`panel/app/app.js:951-952` берёт умолчанием ПЕРВЫЙ ГОТОВЫЙ раздел, а `dashboard`
стоит первым в `SECTIONS` (`panel/serve.py:122`). Это никем не заказано и усиливает
натяжение строк 1 и 9: сводка становится тем экраном, к которому строка 9 требует
«мало и просто».

**M8 ЧЕГО ВКЛЮЧЕНИЕ СВОДКИ КАСАЕТСЯ.** Полный список поверхностей: `serve.py:129-130`
(`READY_SECTIONS`), `serve.py:980-998` (`section_dashboard` сегодня выбрасывает всё,
что не `call`, — `:988`), `app.js:213-242` (`renderDashboard`), `panel/PLAN.md:104-107`
и `:119` (обязательно: `panel/test_docs.py:232-241` сверяет списки из плана с кодом,
а парсер `:166-182` требует жирную метку «**Закрыт…**» даже если закрытых не
осталось), `panel/verify.py:124-125` (список готовых зашит поимённо без `dashboard`).

## §GLOSSARY

| Термин | значение здесь | несущие свойства | конкурирующие прочтения |
|---|---|---|---|
| **сводка** | существующий раздел `dashboard`, достроенный до приборной панели одного направления | (a) не новый id и не новая страница; (b) своя у каждого направления; (c) включается флагом в `READY_SECTIONS` | ОТКЛОНЕНО: новая страница рядом с закрытой — на экране осталось бы две «сводки»; сегодняшнее содержимое `dashboard` — список нарядов (`serve.py:980-998`) — заменяется, а не дополняется |
| **что сделано** | ЧТО произошло, человеческими словами, списком | (a) содержание, не счётчик; (b) не скорость и не «столько-то за период» (D7); (c) на входе — короткий срез, остальное по кнопке | ОТКЛОНЕНО: число задач `status: done` — даёт 0 у двух направлений из трёх (M3); ОТКЛОНЕНО: число коммитов — сама единица неоднозначна (M3); ОТКЛОНЕНО: «за 30 дней» числом — это скорость под другим именем, запрещена D7 |
| **свежесть** | честность экрана о полноте показанного, а не «отставание от общей ветки» | (a) меряется по ДРУГИМ рабочим копиям ЭТОГО репозитория, достижимым через `git worktree list`; (b) сужена путями показываемого направления; (c) молчит, когда двигается чужое направление | ОТКЛОНЕНО: `behind origin/main` — сегодня ноль во всех восьми копиях, полоса недостижима (M2); ОТКЛОНЕНО: `git fetch` перед сравнением — сеть на каждый опрос и ничего не чинит, все копии уже behind=0; ВНЕ УЗЛА: чужой репозиторий игры (`i-panel-cannot-reach-the-game-repo-001`) |
| **раздел** | элемент `SECTIONS`; их девять, готовых восемь | (a) сводка — девятый, УЖЕ заведённый, её включение не есть «добавить раздел»; (b) соседний узел `g-eight-pages` считает свои восемь ПОИМЕННО, а не через `READY_SECTIONS` | сегодня `g-eight-pages` приравнивает восьмёрку к `READY_SECTIONS` — после включения сводки это равенство ломается; строка `→ соседу` ниже |
| **проблемы** | живые карточки вида `issue` этого направления | (a) видны тем, что в них написано; (b) живут на сводке, не в «ЖДЁТ ТЕБЯ»; (c) на входе свёрнуты, разворот по кнопке | ОТКЛОНЕНО: «ЖДЁТ ТЕБЯ» — раздел намеренно означает «тут нужен ты», а записи ждут события, не его (`serve.py:293-294`); ОТКЛОНЕНО: одно число — это сегодняшнее поведение и прямо то, что чинится |
| **приёмка** | автоматические проверки `panel/test_*.py` и `panel/verify.py` | узел не закрывают ни при каком исходе | НЕ ОЗНАЧАЕТ гейт «опровергнуть в свежей сессии»: он этой строкой не отменяется |
| **детально** | глубина, доступная по кнопке, а не плотность входного экрана | строки 1 и 9 зелены одновременно только под этим прочтением | ОТКЛОНЕНО: «всё сразу на входе» — валит строку 9 и его же слова 2026-08-12 про «мало и просто» |

## §WHAT

Тег `acceptance` = строку `shape` копирует в наряд дословно (`os/plays/converge.md:16`).

| id | требование | тег | статус | источник / отвечающий |
|---|---|---|---|---|
| W01 | Строится существующий раздел `dashboard`; новый id раздела не заводится | acceptance | answered | M1 — уже в коде |
| W02 | Сводка своя у каждого направления и говорит только о нём | acceptance | answered | его слова 2026-08-13 «никакой общей» |
| W03 | Сегодняшнее содержимое `dashboard` (список нарядов) заменяется, а не дополняется | acceptance | answered | `serve.py:126-128` — причина закрытия записана самим кодом |
| W04 | Включение сводки правит `READY_SECTIONS`, `panel/PLAN.md` и `panel/verify.py` ОДНИМ шагом | acceptance | answered | M8 — иначе две зелёные приёмки краснеют |
| W05 | Сводка становится входным экраном автоматически; это принято сознательно | — | answered | M7 |
| W06 | «Что сделано» показано содержанием: что произошло, человеческими словами | acceptance | answered | D7 + D10 + M3 |
| W07 | Ни счётчика, ни скорости, ни «столько-то за период» | acceptance | answered | D7 — его решение 2026-08-06 |
| W08 | Сколько строк «что сделано» на входе и чем разворачивается | — | open | → PLAN, `rewrites:` дёшево |
| W09 | Экран не утверждает свежести, которой у него нет | acceptance | answered | M2 |
| W10 | Свежесть меряется по другим рабочим копиям этого репозитория, сужённая путями показываемого направления | acceptance | answered | M2 — механизм уже написан в `osctl.py:181-191` |
| W11 | `panel/verify.py:113` меняется вместе с определением свежести | acceptance | answered | M2 — иначе приёмка продолжит удостоверять молчание |
| W12 | Точная формула свежести и её порог | — | open | → PLAN |
| W13 | Что идёт и что стоит — видно | acceptance | answered | done_when 3 узла |
| W14 | «Почему стоит» ждёт развязки статуса | — | deferred | → `i-state-accepts-values-it-does-not-declare-001` |
| W15 | Проблемы этого направления видны содержанием, на сводке | acceptance | answered | D10 + M4 |
| W16 | На входе они свёрнуты; разворот по кнопке | acceptance | answered | D6 + M7 |
| W17 | Чем группируются и в каком порядке | — | open | → PLAN. У `issue` нет ни важности, ни срочности: схема даёт `level`, `route`, `review_when`, `blocks`. Сортировка по строке id запрещена W20 |
| W18 | Сценарий «пока работает ассистент»: проходит планы и цепочки, собирает список непонятного, уносит в чат | acceptance | answered | его слова 2026-08-13 «вторую верни» |
| W19 | Панель остаётся ТОЛЬКО ДЛЯ ЧТЕНИЯ; требований к его поведению нет | acceptance | answered | goal-блок узла + его слова |
| W20 | Ни одного машинного кода; где имени нет — «без имени» | acceptance | answered | его слова 2026-08-12 |
| W21 | Два записанных правила, объявляющих коды правильными, снимаются | acceptance | answered | M5 — `panel/PLAN.md:126` и `:71` |
| W22 | График — новый механизм, а не настройка: примитивов в панели нет | — | answered | M6 |
| W23 | Какие именно числа получают график | — | open | → PLAN |
| W24 | Цвет только сигналом; стиль 2026-08-06 | acceptance | answered | D9 |
| W25 | Рядом с каждым числом видно, из чего оно посчитано | acceptance | answered | линза 3 хартии + урок предыдущего набора |
| W26 | Закрывает его слово; автоматическая приёмка этого не покупает | acceptance | answered | done_when 11 + хартия, критерий 2 |
| W27 | Гейт «опровергнуть в свежей сессии» строкой W26 НЕ отменяется | — | answered | `os/KERNEL.md` G5 |
| W28 | Новый тип файла состояния не заводится | — | answered | хартия, критерий 4 |
| W29 | Узел не назначает дат и сроков | — | answered | хартия: «ни срока, ни потолка, ни темпа» |

**→ СОСЕДЯМ (рёбра, найденные этой ногой; их чинит не converge):**

| id | что | кому |
|---|---|---|
| E1 | `g-eight-pages` приравнивает свои восемь к `READY_SECTIONS`; после включения сводки равенство ломается. Восемь должны считаться ПОИМЕННО — имена в карточке уже есть | `map`/`review` |
| E2 | `g-no-silent-rot` ссылается на «простое изложение из `g-one-screen`», а этого термина в узле больше нет — он ушёл отдельной вещью его словом | `map`/`review` |
| E3 | `g-no-silent-rot` вменяет сводке четыре списка («что осталось» — узлы, происшествия, наряды, вопросы); ребра в узле нет, и они бьют в строку 9 | `map`/`review` |
| E4 | `i-state-accepts-values-…-001` ждёт события «упрётся в свою строку 6»; после переписи строка 6 — сценарий ассистента, а зависимость в строке 3 | `repair` |
| E5 | `i-panel-cannot-reach-the-game-repo-001` заведён БЕЗ блока `review_when`, которого схема требует. Дефект ноги разделения | `repair` |
| E6 | Хартия и карточка корня требуют «раз в день по всем направлениям»; его решение 2026-08-13 это отменяет. Хартию правит только `frame` | `frame` |
| E7 | Хартия требует, чтобы доля работы в систему стояла числом В СВОДКЕ; число межнаправленческое, а межнаправленческий экран отменён | `frame` |

**Итого:** 29 строк · answered 24 · open 4 · deferred 1 · плюс 7 рёбер соседям.
Владельческих открытых строк НЕТ. Все четыре открытые уходят в PLAN.

## §SIGNOFF

**Define** — спорные термины закрыты так:

- **«что сделано»** — прочтение выбрано НОГОЙ, не владельцем, и это названо прямо.
  Основание: любое другое прочтение переоткрывает два его собственных решения —
  «скорость не показываем» (2026-08-06) и «содержание, а не счётчик». Выбор не
  расширяет его обязательств и не требует от него нового слова; если он не согласен,
  строка W06 падает вместе с прочтением.
- **«свежесть»** — прочтение выбрано ногой на замере M2: сегодняшнее определение
  делает полосу недостижимой во всех восьми копиях.
- **«сводка», «раздел», «проблемы», «приёмка», «детально»** — закрыты кодом и его
  ранее записанными решениями, не суждением ноги.

**Resolve** — владелец подписал в этой сессии, его словами 2026-08-13:

- **«для каждого направления я хочу видеть свою сводку. Детально именно по этому
  направлению, никакой нахуй общей. У каждого направления своя сводка»** — запирает
  W02 и снимает общий экран;
- **«да, берём»** — на показанном описании сводки (актуальность, что идёт и что
  стоит, что сделано, проблемы содержанием, графика, ни одного кода, мало и просто,
  закрывает его слово);
- **«вторую верни, первую отдельной вещью»** — на прямом вопросе о двух условиях,
  выпавших при разделении: возвращает сценарий ассистента (W18) и выносит короткую
  строку у каждой записи отдельной вещью (D4).

Ранее подписанное и не переоткрытое: «любые коды… я же не помню все эти коды»
(2026-08-12); «нажал кнопку подробнее… максимально просто написано» (2026-08-12);
«скорость не показываем» (2026-08-06); стиль панели (2026-08-06).

END_OF_FILE: live/direction-os/work/converge-g-one-screen-v2.md
