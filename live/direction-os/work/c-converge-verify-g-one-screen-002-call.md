CALL → session · play: converge-verify · direction: direction-os · node: g-one-screen

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-converge-verify-g-one-screen-002

**Свежая отдельная сессия.** Проверка существует, чтобы ПОПЫТАТЬСЯ ОПРОВЕРГНУТЬ набор,
а не подтвердить его. Тот же чат, что его собирал, для неё не годится.

goal
Опровергнуть набор `work/converge-g-one-screen-v3.md`: найти атом `done_when` или
goal-блока, не дошедший ни до одной строки; строку, выдающую вывод ноги за источник;
термин, читаемый двумя способами и не попавший в глоссарий; замер, который не
воспроизводится.

context
- набор: live/direction-os/work/converge-g-one-screen-v3.md (замеры M1–M8 в нём же)
- узел: live/direction-os/cards/g-one-screen.md — **читать с диска**; карточка починена
  2026-08-14, её 11 строк `done_when` и goal-блок и есть предмет покрытия
- устав: live/direction-os/CHARTER.md — пересмотрен и подписан 2026-08-14; из него
  born-closed приходят два импорта, оба подлежат проверке на диске
- соседи с рёбрами в этот узел: cards/g-no-silent-rot.md, cards/g-eight-pages.md,
  cards/i-state-accepts-values-it-does-not-declare-001.md

НЕ ЧИТАТЬ отчёты решавших ног — плей это запрещает («It does NOT read deciding-session
reasoning»). Всё, на что набор имеет право опираться, лежит в самом наборе; если чего-то
там нет — это и есть находка. Два замороженных набора (`converge-g-one-screen.md`,
`…-v2.md`) требований не поставляют и как источник не годятся.

boundaries
- Владельческие решения 2026-08-12/13/14 не переоткрываются: вид экрана, отсутствие
  общей сводки, заход когда угодно, ни одного кода, возврат сценария, вынос простого
  изложения отдельной вещью, ревизия устава.
- Три строки — W12, W17a, W33 — подписаны его словами 2026-08-14: «ноги за окно, окно
  30 дней, детально это охват, планы это волна». Сами решения НЕ переоткрываются.
  Проверяется другое: что подпись записана верно и что цена, названная ему при ней,
  соответствует правде — особенно у W12, где при окне 30 дней у indie получается 238
  отчётов ног, и это валит подписанное «мало и просто» (W20), из-за чего объём входа
  (W21) назван обязательным к решению при сборке.
- Владельческих открытых строк в наборе НЕТ. Остальные десять открытых адресованы PLAN,
  соседям и записи. Открытая строка ПОКРЫВАЕТ свой атом: неотвеченный вопрос дырой не
  является. Проверять надо, что за ней не спрятано решение, уже принятое ногой.
- За владельца ничего не решать.
- Числа набора — основания, не авторитет: перевыведи и сообщи о расхождении.

что проверить в первую очередь
1. Восемь замеров перевывести своими руками. Особенно M2 (демонстрация «зелёной лжи»
   корневой копии) и M3 (четыре прочтения «что сделано»; набор утверждает разброс 60×
   внутри направления и признаёт чувствительность к форме даты).
2. Разобрать 11 строк `done_when` И goal-блок на атомы, включая запреты, и проверить,
   что каждый доходит до строки. Предыдущие наборы падали именно здесь, причём один раз
   атом потерялся при переносе цитаты в строку — проверь дословность.
3. Каждую `answered`-строку — на источник ВНЕ ноги. Набор заявляет, что таких нарушений
   нет и что acceptance-строк 27 и все `answered` при 41 строке всего (30 answered, 10 open,
   1 deferred); проверь счётом, а не на глаз.
4. §GLOSSARY: найти термин из карточки, читаемый двумя способами и не попавший в таблицу.
5. Проверить, что ни одна `acceptance`-строка не ссылается на `open`-строку.

done_when
Как в os/plays/converge-verify.md. Каждая находка — либо подтверждённое опровержение с
уликой, либо снятое подозрение с причиной.

return
RESULT. Наряд исполнителю НЕ выдавать: после прохода следующим идёт `shape`, и только по
готовности. Владельческих открытых строк в наборе нет — активацию блокирует только
непройденная проверка.

budget
Как определяет плей.

END_OF_FILE: live/direction-os/work/c-converge-verify-g-one-screen-002-call.md
