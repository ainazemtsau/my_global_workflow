CALL → session · play: work · direction: direction-os · ставка: g-one-screen · задача: t-one-screen-2

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-work-one-screen-source-gap-001
основание дефекта: коммит `4b026c0c`, binding G5
`history/2026-08-24-s-work-one-screen-skeleton-g5-001.md`

goal
У ручки сводки и у итога `t-one-screen-2` один непротиворечивый ответ о наличии источников:
честный ноль данных не объявляется отсутствием источника, а число под порог смерти 2
воспроизводится из ответа ручки без отдельной трактовки.

context
- `panel/serve.py`: `dash_block` определяет `gap` как «ИСТОЧНИКА НЕТ»;
  `dash_running` на коммите `4b026c0c` ставит этот `gap`, когда у направления нет ставки;
  `section_dashboard` включает всякий такой блок в `blocks_without_source`.
- На том же коммите физические `cards/`, `NOW.md` и `history/` у solmax существуют и читаются,
  но ответ содержит `blocks_without_source: ["running"]` только из-за `bet: null`.
- Четыре row-count и их `how` binding G5 подтвердила; чинится не их набор, а смысл честного
  нуля и итоговой source-gap классификации.
- Пять исходных done_when дословно остаются на `cards/t-one-screen-2.md`.

boundaries
- Только найденное противоречие `gap` / честный ноль и узкая приёмка этого поведения.
- Не рисовать (`t-one-screen-3`), не менять имена и коды (`t-one-screen-4`), не чинить
  свежесть (`t-one-screen-5`), не включать dashboard и не заменять прежние
  `ready/other/cards_total/cards_closed` (`t-one-screen-6`).
- Не заводить новых источников или типов state-файлов; панель остаётся только для чтения.
- `live/**` не править. Замороженный `work/converge-g-one-screen-v3.md` не править.
- Не выносить вердикт о судьбе ставки и не закрывать `t-one-screen-2`: после исправления ей
  снова нужна отдельная свежая binding G5.

done_when
1. Для каждого из трёх направлений HTTP-ручка по-прежнему отдаёт код 200, шапку свежести и
   ровно четыре блока с непустыми `how`; четыре подтверждённых row-count не регрессируют.
2. Отсутствие активной ставки у solmax остаётся названным честным нулём, но не попадает в
   `blocks_without_source`: на исправном дереве все три ответа дают `[]`, а итог под порог
   прямо и однозначно говорит `0 из 4`.
3. Настоящее отсутствие источника, если оно моделируется приёмкой, по-прежнему называет блок
   и причину; отсутствие строк внутри существующего источника с ним не смешивается.
4. Есть focused acceptance, которая падает на `4b026c0c` именно на
   `solmax blocks_without_source == ["running"]` и зелёная после исправления.
5. Дифф не содержит новых источников, state-файлов, записывающих маршрутов, рисунка или работы
   соседних задач. Прежнее содержимое ручки остаётся рядом до `t-one-screen-6`.
6. Все девять штатных приёмок зелёные; итог называет каждую и отдельно — focused acceptance.

return
RESULT с коммитом кода, диффом, исходным красным и итоговым зелёным focused acceptance,
повторным ручным числом по трём ответам, девятью штатными приёмками и явной передачей в новую
свежую G5. Задача остаётся `open` до этой проверки.

budget
Треть сфокусированного дня. Если для согласования одного смысла требуется переделка контракта
четырёх блоков или работа соседней задачи — checkpoint, не расширение.

END_OF_FILE: live/direction-os/work/c-work-one-screen-source-gap-001-call.md
