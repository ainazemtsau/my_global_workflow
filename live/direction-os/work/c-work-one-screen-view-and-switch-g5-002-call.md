CALL → session · play: work (биндинговая проверка) · direction: direction-os · ставка: g-one-screen · задачи: t-one-screen-3 и t-one-screen-6

рабочая копия: C:/wf-direction-os, ветка wt/direction-os
карточка наряда: c-work-one-screen-view-and-switch-g5-002
что проверяется: коммиты `7b029974` (включение + пятый блок) и `511337f2` (возраст на экране)

**СВЕЖАЯ СЕССИЯ ОБЯЗАТЕЛЬНА.** Всю эту работу делал один чат — он же писал себе наряды, он же
писал отчёты. Там, где наряд и отчёт согласны, согласие не подтверждает ничего.

Этот наряд ЗАМЕНЯЕТ `c-work-one-screen-view-g5-001`: тот указывал на `ae72e7dc`, а с тех пор
код перебит дважды. Проверять надо то, что лежит сейчас.

goal
Попытаться ОПРОВЕРГНУТЬ, что выполнены восемь строк `t-one-screen-3` (вид) и семь строк
`t-one-screen-6` (включение). Не подтвердить — опровергнуть.

## ЧТО СЛУЧИЛОСЬ ЗА ЭТИ ДВА КОММИТА

Сводка ВКЛЮЧЕНА и стала входным экраном: `dashboard` добавлен в `READY_SECTIONS`, и она стоит
первой в `SECTIONS`. Добавлен ПЯТЫЙ блок «что дальше» (запаркованные цели и идеи). Добавлена
строка возраста: «работа идёт N дней · последняя нога N дней назад». Приёмка `test_docs`
чинилась — её разбор требовал, чтобы в плане всегда была хотя бы одна ЗАКРЫТАЯ секция, а после
включения последнего раздела закрытых не осталось.

## ГДЕ ЭТА РАБОТА ВЕРОЯТНЕЕ ВСЕГО НЕПРАВА — бить сюда первым

1. **Строка 2 задачи 6 — «содержимое ЗАМЕНЕНО, а не дополнено» — под подозрением, и это
   называет сама нога.** Старые ключи `ready`/`other` (список нарядов) ПО-ПРЕЖНЕМУ едут в ответе
   ручки; на экране их не рисует никто, но из API они не убраны, и держит их там ровно то, что
   `panel/verify.py` проверяет их форму в четырёх местах. Реши сам: «содержимое раздела» — это
   экран или ответ ручки? Если ответ — строка 2 ПРОВАЛЕНА, и её надо назвать проваленной, а не
   истолковать в пользу ноги.
2. **Починка приёмки `test_docs`.** Нога изменила ПРОВЕРКУ, чтобы она стала зелёной. Это всегда
   подозрительно. Проверь: правда ли старый разбор требовал закрытую секцию всегда, правда ли
   после включения последнего раздела формат стал невыполнимым без выдумки, и правда ли новый
   разбор по-прежнему ловит дрейф в обе стороны. Если это подпорка, а не починка — скажи прямо.
3. **Пятый блок против «мало и просто».** Условие 9 узла и подписанное «заход даёт мало и
   просто» — пятый блок сделал вход больше. Проверь, что на входе у каждого блока по-прежнему
   не больше пяти строк и что экран не стал плотным.
4. **Возраст и свежесть.** Строка возраста считает «последнюю ногу» по файлам в `history/` ЭТОЙ
   копии. Копия отстаёт от `origin/main`; по indie отставание 140 коммитов. Значит число тишины
   для чужого направления может врать. Проверь, врёт ли, и НЕ чини — это задача `t-one-screen-5`.
5. **Строка 1 задачи 3 переписана этой же ногой** с «четыре блока» на «ПЯТЬ». Основание —
   слово владельца «давай В» 2026-08-27 на показанный вариант «дописать очередь и сразу
   включить». Проверь, что основание настоящее и что нога не переписала себе условие задним
   числом под уже сделанное.

## ЧТО ПРОВЕРЯТЬ ДОСЛОВНО

### t-one-screen-3 (вид)

1. Раздел рисует ПЯТЬ блоков; на входе у каждого не больше пяти строк и число со способом
   счёта рядом. Пятый блок — «что дальше» — добавлен его словом «давай В» 2026-08-27 на
   показанный ему вариант «дописать очередь и сразу включить сводку»; до этого дня условие
   говорило «четыре», и это изменение требования, а не уточнение.
2. «Подробнее» разворачивает блок, не уводя с экрана.
3. Клик по строке ведёт внутрь: цель — на свою страницу, запись — в свой текст, отчёт ноги — в
   раздел ИСТОРИЯ.
4. Ни одного машинного кода на экране раздела; где имени нет — «без имени», никогда id.
5. Ровно один график — полоса активности за скользящие 30 дней; библиотек не добавлено,
   примитив написан руками. Больше графики на экране нет.
6. Цвет только сигналом, никогда украшением; стиль берётся из `panel/app/style.css` —
   инлайн-стилей и цветов в JS ноль.
7. Экран остаётся ТОЛЬКО ДЛЯ ЧТЕНИЯ: ни одного действия, меняющего состояние.
8. «Целиком» прочитано как ОХВАТ — его слово 2026-08-14: все стороны направления представлены,
   но вход остаётся маленьким. С подписанным «заход даёт мало и просто» экран не спорит.

### t-one-screen-6 (включение)

1. `dashboard` стоит в `READY_SECTIONS` (`panel/serve.py:129-130`), и раздел виден в меню
   каждого направления.
2. Сегодняшнее содержимое раздела — список нарядов — ЗАМЕНЕНО, а не дополнено.
3. `panel/PLAN.md` приведён в двух местах: список «Разделы панели» (`:104-107`) и строка 6
   таблицы «Что осталось» (`:119`).
4. `panel/verify.py:124-125` приведён: список готовых разделов зашит там поимённо и без
   `dashboard` не сойдётся.
5. Все девять приёмок зелёные: `panel/verify.py`, `test_osctl`, `test_cards_cmd`,
   `test_carrier`, `test_wave2`, `test_readers`, `test_docs`, `test_context` и
   `node panel/test_md.cjs`.
6. Вход в направление ведёт на сводку — его слово 2026-08-14. Умолчание остаётся «первый
   готовый раздел», отдельного имени раздела в коде не заводится: включение делает сводку
   входной само, потому что она стоит первой в `SECTIONS`.
7. Владелец открыл сводку по всем трём направлениям. Это условие задачи, а не закрытие узла:
   узел закрывает отдельное его слово о том, пользуется он этим или нет.

boundaries
- **Меряй сам.** Числа отчётов — заявления. Каждое число в твоём отчёте идёт с командой и деревом.
- **Не чинить найденное.** Проверка находит и называет; починку заводит следующая нога.
- **Строка 7 задачи 6 — ВЛАДЕЛЬЧЕСКАЯ.** «Владелец открыл сводку по всем трём направлениям» —
  это его глаза, не твои и не приёмка. Без его слова строка НЕ выполнена, и задача 6 остаётся
  открытой независимо от всего остального. Не толкуй его молчание.
- Не расширяться: имена и коды — `t-one-screen-4`, свежесть — `t-one-screen-5`.
- `work/converge-g-one-screen-v3.md` заморожен уликой: читать можно, править нельзя.
- В `panel/**` не писать.

done_when
1. Каждая строка обеих задач получила вердикт: выполнена / провалена, с уликой первой руки.
2. Пять мест из раздела «где эта работа вероятнее всего неправа» атакованы поимённо.
3. Девять приёмок прогнаны твоей рукой, результат каждой назван.
4. Диспозиция явная: что закрывается, что остаётся открытым и почему.

return
RESULT. Задача 6 закрывается ТОЛЬКО вместе со словом владельца по строке 7 — если его нет,
так и напиши, и оставь её открытой.

budget
Треть сфокусированного дня.

END_OF_FILE: live/direction-os/work/c-work-one-screen-view-and-switch-g5-002-call.md
