CALL → session · play: converge · direction: direction-os · node: g-one-screen

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-converge-g-one-screen-repair-001

goal
Пересобрать набор требований `work/converge-g-one-screen.md` так, чтобы он
выдержал повторную проверку. Проверка `s-converge-verify-g-one-screen-001`
вернула его с `complete=FAIL smuggling=FAIL` и восемью находками (F1–F8).
Набор существует, работа — не заводить его заново, а закрыть эти восемь дыр.

context
- набор: live/direction-os/work/converge-g-one-screen.md
- узел: live/direction-os/cards/g-one-screen.md
- устав: live/direction-os/CHARTER.md
- отчёт проверки: live/direction-os/history/2026-08-12-s-converge-verify-g-one-screen-001.md
  (находки, улики и перевыведенные числа — там)
- разбор узла: live/direction-os/history/2026-08-12-s-map-direction-os-001.md

что вернулось (восемь находок)
1. **F1 — M3 не воспроизводится.** Ни один вариант ref/границы не даёт чисел
   таблицы, и июнь/июль противоречат закоммиченному замеру ноги `frame`
   (94/645 и 135/779 на дереве 6041c36c), на котором стоит число хартии.
   Вывод (перевёрнутый знак на июле) независимо подтверждён и НЕ падает.
   Чинится таблица и её проверяемость: ячейки + точная команда, ref и дерево.
2. **F2 — M1 меряет байтами, называет символами.** «175 из 394 длиннее 200
   символов, максимум 2312» — байтовые числа под символьной подписью. В
   символах: 121 из 394 (31%), максимум 1377. Знаменатель 394 и максимум 2312
   байт воспроизвелись точно. Число 175 не даёт ни один порог.
   Чинится и сам замер, и §SIGNOFF: это цена, названная владельцу за W06.
3. **F3 — «правило написано три месяца назад»** неверно: строка
   `os/schema/direction-files.md:41` вошла 2026-07-24 (19 дней), самая ранняя
   формулировка где-либо — 2026-07-02 (~6 недель).
4. **F4 — W04 против W05.** W04 говорит «полки… ничем не вычисляются», W05
   заказывает «точный список признаков, по которым направление попадает в
   полку». Список признаков и есть вычисление. Под этим — неназванное
   допущение, что в записанном есть чем поставить полку; критерий 6, W21 и сам
   W20 («важности в записи у них нет и придумать её нечем») говорят обратное.
   W05 — несущее свойство подписанного W01, а не параметр PLAN.
5. **F5 — три атома done_when без строки:** «График там, где число само
   молчит» (критерий 3); «С ВЛАДЕЛЬЦЕМ ПО ОДНОЙ» (критерий 8 — выпало из W24 и
   W16, а W24 уходит в наряд дословно); «Правило записано» (критерий 4 —
   слабейшая, встречное прочтение записано в отчёте). Плюс не спрошенные у
   карточки линзы хартии 5 «Цена для игры» и 4 «Автономность», плюс
   существительное «планы» из критерия 5.
6. **F6 — W20 требует страницу, которой нет.** Карточки `issue` панель не
   показывает нигде (`serve.py:294`, `:360–374`, исключение на `:362`);
   `READY_SECTIONS` — ровно восемь, девятая СВОДКА закрыта. «Своя страница» —
   десятый раздел, и цена этого не названа (D6, граница премортема 5, критерий
   хартии 4). Решение владельца не оспаривается — оспаривается умолчание цены.
7. **F7 — §GLOSSARY: «что сделано»** — термин done_when с двумя прочтениями, в
   таблицу не попавший, и это ПЕРВОЕ из четырёх чисел W10. Конкурирующие
   прочтения уже в коде (`serve.py:104, 116–119, 909, 974`; `app.js:247–249`).
   Вторым: «запись» стоит в таблице с одним значением, а критерий 6 и W20
   используют второе (карточка `issue`) — W20 оба в одном предложении.
8. **F8 — W01 подписан на картинке, которой в наборе нет.** Несущая строка
   стоит на «МНЕ НРАВИТСЬЯ 1Й ВАРИАНТ», сказанном на показанной раскладке;
   раскладки в наборе нет, только пересказ. Нужен перенос того, что он одобрил,
   в сам набор — дословной раскладкой или ссылкой на закоммиченный артефакт.

Плюс частичная находка **L9**: содержание W26 («снять коды») законно следует из
подписанного W02, а притязание «первый пункт, известен заранее» — нет; порядок
владельцем не подписан и предвосхищает его повердикт по критерию 8.

boundaries
- **Владельческие решения 2026-08-12 не переоткрываются**: вид экрана, отсутствие
  сводного балла, цвет сигналом, важность из записи, простое изложение полем
  существующей карточки, проход по восьми страницам, а также W06, W11 и W20,
  подписанные словами «делай как сказал по всем трем». Ни одна находка их не
  трогает: все восемь бьют по строкам, которых он не подписывал, либо по числам,
  которые ему назвали неверно.
- За владельца ничего не решать. F4 и F6 могут упереться в развилку, которую
  может закрыть только он, — тогда она выносится ему как выбор, а не как догадка.
- **Числа в этом наряде — основания, не авторитет.** Перевыведи их своей рукой
  и запиши команду; если моё число неверно, исправь его и сообщи об исправлении.
- `live/**` меняется только применением RESULT.
- Дефект правила (`acceptance` записан только в `converge.md:16`, а `shape.md`
  этого слова не содержит) здесь НЕ чинится: это `MAINTENANCE REQUEST`,
  отдельной сессией. Записан в captures отчёта проверки.

done_when
Как в os/plays/converge.md. Дополнительно: по каждой из восьми находок в наборе
видно, чем она закрыта — правкой строки, новой строкой, названной ценой или
явно записанной причиной, почему находка снята.

return
RESULT. Наряд исполнителю НЕ выдавать. Следующим идёт `converge-verify` свежей
сессией; `shape` до прохождения проверки не открывается.

budget
Как определяет плей.

END_OF_FILE: live/direction-os/work/c-converge-g-one-screen-repair-001-call.md
