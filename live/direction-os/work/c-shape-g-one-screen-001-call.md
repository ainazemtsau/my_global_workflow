CALL → session · play: shape · direction: direction-os · node: g-one-screen

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-shape-g-one-screen-001

**Свежая сессия.** Это первая нога после потолка бумажной фазы.

goal
Превратить узел `g-one-screen` («Сводка каждого направления») в активную ставку по
`os/plays/shape.md`: аппетит, подходы, рез, линзы, самое рискованное допущение, задачи,
критерии смерти — и первый наряд задачи.

## ЧЕМ ОТКРЫТ ЭТОТ ПУТЬ — читать до первого шага

Набор требований **не проходил** проверку. Он провалил её ВТОРОЙ раз
(`history/2026-08-14-s-converge-verify-g-one-screen-002.md`, `complete=PASS smuggling=FAIL`),
и по потолку шага 4 плея `converge-verify` бумажная фаза узла закрыта: третий круг
`converge` открывают только слова владельца.

Владелец их сказал 2026-08-14 на решение `d-one-screen-paper-phase-001`, где вариант (а)
был назван «СТРОИТЬ НА ОТВЕЧЕННОМ», а вариантами (б) и (в) — «резать» и «делить в map».
Его слово, дословно: **«а»**.

Поэтому Definition-of-Ready (`os/plays/converge.md`) здесь **закрыт его словом, а не
пройденной проверкой**. Это надо назвать вслух в `play_check`, а не подразумевать.
Полнота набора при этом проверку ПРОШЛА (`complete=PASS`): все 11 строк `done_when`
и goal-блок доходят до строк, дословность цитат сверена с карточкой.

context
- вход: `live/direction-os/work/converge-g-one-screen-v3.md` — **ЗАМОРОЖЕН УЛИКОЙ.**
  Читать можно, править НЕЛЬЗЯ: файл несёт блок-улику и остаётся доказательством.
  Из 41 строки в сборку едут **40**. Строка **W06** опровергнута и НЕ едет (ниже).
- узел: `live/direction-os/cards/g-one-screen.md` — читать с диска, починен 2026-08-14
- устав: `live/direction-os/CHARTER.md` — пересмотрен и подписан 2026-08-14; линзы для
  шага 5 берутся оттуда, все пять
- указатель: `live/direction-os/NOW.md` — `bet: null`, активной ставки нет, G1 свободен
- соседи с рёбрами: `cards/g-no-silent-rot.md`, `cards/g-eight-pages.md`,
  `cards/i-state-accepts-values-it-does-not-declare-001.md`
- отчёт проверки: `history/2026-08-14-s-converge-verify-g-one-screen-002.md` — все
  улики, команды и перевыведенные замеры лежат там

## ЧТО ЕДЕТ В СБОРКУ ИЗМЕНЁННЫМ — семь находок проверки

Ни одну из них сборка НЕ чинит в наборе (файл заморожен). Они едут строками плана:
открытым вопросом, резом или строкой `→ PLAN`.

1. **W06 НЕ СУЩЕСТВУЕТ как требование.** «Сводка становится входным экраном
   направления» стояла с тегом `acceptance` на владельческой цитате, которой нет ни в
   одном файле репозитория. В наряд исполнителю она НЕ копируется ни в каком виде.
   Механический факт под ней верен и остаётся фактом, не требованием: включённая
   сводка станет первым экраном, потому что `app.js:951` берёт умолчанием первый
   готовый раздел, а `dashboard` стоит первым в `serve.py:122` `SECTIONS`.
   **Владельческий вопрос на шаге 3 или 4:** оставляем так — вход в направление ведёт
   сразу на сводку — или сводка живёт отдельной вкладкой, а входным остаётся ВОЛНА?
   Одной строкой, с ценой каждого варианта. За него не решать (G7).
2. **W24 разрезать.** «Ни одного машинного кода; где имени нет — «без имени»» стоит
   `acceptance`, но её собственный источник отдаёт часть области `open`-строке W23.
   Часть про карточки исполнима; часть про ИМЯ НАПРАВЛЕНИЯ — нет.
3. **W23 — не параметр PLAN, а схемный вопрос.** Человеческого имени у направления нет
   НИГДЕ: `serve.py:182-185` строит запись из имени каталога, `app.js:87` и `:122`
   печатают слаг. Завести это имя = тронуть схему, а там ждут W31 («новый тип файла
   состояния не заводится») и критерий 4 хартии. Дешёвым откатом это не является,
   поэтому в PLAN не сваливать: либо владельческий вопрос, либо явный рез.
4. **«ЦЕЛИКОМ» подписать так же, как подписано «детально».** Слово из goal-блока
   («видит это направление целиком»), несёт строка W34, в §GLOSSARY не попало. Два
   прочтения те же: ОХВАТ (все стороны представлены) и ПЛОТНОСТЬ (всё сразу на экране).
   Под вторым сталкивается с подписанным «мало и просто» (W20). Его слово — или рез.
5. **Ребро к `g-eight-pages`.** Снятие двух правил `panel/PLAN.md:126` и `:71` требуют
   ОБА живых узла (здесь W25, у соседа `done_when` 4, теми же номерами строк). Снять их
   можно один раз. Кому принадлежит — вопрос `map`/`review`, сборка его только называет.
6. **Числами M3 из столбца коммитов не пользоваться.** Замер не воспроизводится: 541 не
   даёт ни одна форма объявленной команды (`T00:00:00` → 546, без времени → 540,
   `--no-merges` → 542), а клетка solmax взята формой, которую набор объявил
   неиспользуемой. Вывод замера выжил (чувствительность к форме даты реальна), число —
   нет. Отчётов ног 238 у indie за 30 дней — воспроизводится, этим числом пользоваться
   можно.
7. **Мелочи, чтобы не всплыли снова:** ссылка на строку W13a, которой в наборе нет
   (остаток от редакции до его подписи); W20 ссылается на импорт D2 за свойством «клик
   по строке ведёт внутрь», которого у D2 нет — свойство пришло из `done_when` 9, а
   родилось в замороженном v1 про СВЕТОФОР ПО НАПРАВЛЕНИЯМ, отменённый его словом
   2026-08-13. Куда ведёт клик на сводке ОДНОГО направления — не спрашивает никто.

boundaries
- Владельческие решения не переоткрываются: вид экрана, отсутствие общей сводки, заход
  когда угодно, ни одного кода, возврат сценария, вынос простого изложения, ревизия
  устава, и подписи 2026-08-14 — «ноги за окно, окно 30 дней, детально это охват, планы
  это волна».
- **Третьего круга `converge` нет.** Сборка не пересобирает набор, не переписывает
  замороженный файл и не «дочиняет» строки. Всё, что не отвечено, становится резом,
  строкой `→ PLAN` или его вопросом — по правилу шага 4 плея.
- `acceptance`-строки копируются в наряд исполнителя ДОСЛОВНО (`os/plays/converge.md:16`).
  Строка, чья область упирается в неотвеченное, дословно не копируется — сначала режется.
- Аппетит, критерии смерти, полосы и их лимит — его слова, не догадка (G3, G4, G7).
- За владельца ничего не решать. Батчить его вопросы: их набирается три-четыре.

done_when
Как в `os/plays/shape.md`: `NOW.md` называет одобренную ставку, карточка ставки проходит
G1–G6, первый наряд задачи заведён карточкой и стоит в `RESULT.next`.

return
RESULT. Первый наряд задачи — карточкой, но не запускать: запуск его словом.

budget
Как определяет плей. Аппетит спрашивается на шаге 2 и после не растёт.

END_OF_FILE: live/direction-os/work/c-shape-g-one-screen-001-call.md
