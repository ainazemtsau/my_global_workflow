MAINTENANCE REQUEST
what: |
  СВЕРКА «ЧТО ПРОСИЛ НАРЯД» С «ЧТО ПРИВЁЗ ОТЧЁТ» НАПИСАНА, РАБОТАЕТ И ИМЕЕТ ЗУБЫ — но включена
  ровно для инженерных возвратов. Сессионные возвраты, которыми закрываются ВОЛНЫ, не проверяет
  никто.

  Правило живёт в `os/adapters/coding-agent.md:87` и начинается словами:

      **deliverable reconciliation (engineering RESULTs only)** — diff the RESULT against the
      CALL's OWN `done_when` … Every `done_when` bullet of the CALL must carry a disposition in
      the RESULT: a named evidence line, or an out-of-scope line with an owner-ack token …
      A missing bullet … = conflict → bounce with the specific bullet, not applied.

  Это ровно та проверка, которой не хватило. Она не сработала не потому, что плохо написана, а
  потому что её область — `engineering RESULTs only`. Для `to: session` во всём файле есть ровно
  ОДИН guard, `owner-verdict CALL guard (session RESULTs)` (строка 35), и он про другое: про то,
  что вердикт владельца нельзя подменить пересказом.

  ДОКАЗАННЫЙ ПРОМАХ, перемерен 2026-08-28 на дереве `6d6215db`.

  Наряд `live/indie-game-development/work/2026-08-20-call-review-wave-5-steam-minimum.md`
  несёт СЕМЬ пунктов `done_when` (проверяется командой ниже). Возврат
  `live/indie-game-development/history/2026-08-27-s-review-g-5a7c-wave-5-close-and-concept-reset-001.md`
  разобрал пять и НЕ разобрал два:

    4. «Собрана полная матрица A–F из этого CALL и сверена с реальным App checklist; у каждой
       строки есть ready/missing/unknown, action, actor и pass evidence» — не привезена.
    5. «Владельцу показан короткий human brief: … публичный title, если его ещё нет» — не привезён.

  Проверка отсутствия механическая:

      grep -ni "матриц\|A–F\|A-F\|title\|ready/missing" \
        live/indie-game-development/history/2026-08-27-s-review-g-5a7c-wave-5-close-and-concept-reset-001.md
      -> ноль совпадений

  Карточка наряда `live/indie-game-development/cards/closed/c-review-g-5a7c-wave-5-steam-minimum-001.md`
  при этом несёт `to: session`, `play: review`, `status: done`. Отскока не было, конфликта не было,
  применилось чисто.

  ЦЕНА, ИЗМЕРЕННАЯ ВЛАДЕЛЬЦЕМ, А НЕ ВЫВЕДЕННАЯ. 2026-08-28 утренняя нога `day`, не найдя ни имени
  страницы, ни её текстов ни в одном файле состояния, задала их владельцу вопросами. Его ответ
  дословно: «я записал то, что надо было записать… опять что ты это переспрашиваешь… Я не хочу по
  кругу отвечать на какие-то, блять, одинаковые вопросы» и «либо workflow что-то не так, в нашем
  структуре файлов что-то не так». Он прав в обе стороны: он ответил тогда, ответ не доехал, и
  спросили заново.

  ВТОРАЯ ЗАПИСЬ ТОГО ЖЕ КЛАССА, требуемая порогом `os/MAINTENANCE.md` шаг 2 («≥2 FRICTION entries»
  для самоинициированного улучшения). Карточка
  `live/indie-game-development/cards/i-call-text-never-reached-the-executor-001.md` (2026-08-10,
  `route: maintenance`) описывает соседний случай — наряд физически не доехал до исполнителя, тот
  догадался о содержании и сказал владельцу «можно закрывать по исходной постановке», которой не
  читал. Её собственный `review_when` называет фикс из ДВУХ половин: «нога выдачи по построению
  отдаёт готовую пасту с полным текстом наряда, А первая строка возврата исполнителя — расписка,
  называющая число полученных строк `done_when`». Первая половина построена — наряды шестой волны
  лежат готовыми пастами. Вторая половина не построена до сих пор, и настоящая заявка — она же.

  ТРЕТИЙ ЗАМЕР, того же семейства, уже записанный в прогнозе направления
  (`cards/direction_forecast.md`, drivers): «из 29 отчётов, приехавших в продукт за четвёртую
  волну, 14 не имели карточки Направления, а 13 закрытий не оставили RESULT в history». То есть
  закрытие без учёта привезённого — не разовый срыв, а поведение по умолчанию там, где сверки нет.
where: |
  `os/adapters/coding-agent.md:87` — само правило `deliverable reconciliation`, и слово
  `engineering` в его области. Это ЕДИНСТВЕННОЕ место, которое обязано измениться.

  `os/adapters/coding-agent.md:35` — соседний `owner-verdict CALL guard (session RESULTs)`: образец
  того, как в этом же файле уже сформулирован guard именно для сессионных возвратов. Форму брать
  оттуда, не изобретать.

  `os/adapters/coding-agent.md:26` — список обязательных полей RESULT и `play_check`. Проверить,
  не дешевле ли повесить сверку туда; решение — за ногой обслуживания, заявка на нём не настаивает.

  `os/MAINTENANCE.md` шаг 3 — граница семантического ревью. Она здесь несущая, см. `expected`.

  БЮДЖЕТА У ЭТОГО ФАЙЛА НЕТ: `panel/test_docs.py::case_budgets` считает потолки только для
  `os/KERNEL.md` и для файлов, объявивших «≤N-word budget». `os/adapters/coding-agent.md` (6177
  слов на 2026-08-28) ни того, ни другого не несёт, поэтому добавление оплачивать нечем не надо.
expected: |
  Сверка привезённого распространяется на сессионные возвраты: если возвращающийся CALL несёт
  `done_when`, каждый его пункт обязан получить в RESULT расположение — названную улику ЛИБО
  строку «не привезено, потому что …» с токеном. Пункт без расположения — конфликт и отскок с
  названием конкретного пункта, как это уже работает для инженерных возвратов.

  МИНИМАЛЬНАЯ ФОРМА — РАСШИРЕНИЕ ОБЛАСТИ, А НЕ НОВЫЙ МЕХАНИЗМ. Ничего не изобретается: текст,
  зубы, формулировка отскока и escape-люк уже написаны и обкатаны на инженерных возвратах. Меняется
  одно слово области. Это прямо соответствует постоянному требованию владельца от 2026-08-11:
  ядро только РАСШИРЯЕТСЯ, не переписывается.

  ЧЕТЫРЕ ГРАНИЦЫ, КОТОРЫЕ ОБЯЗАНЫ УСТОЯТЬ.

  1. ПРОВЕРКА ОСТАЁТСЯ МЕХАНИЧЕСКОЙ — наличие строки, никогда суждение о качестве. Так она
     сформулирована сегодня («Mechanical token-presence per bullet — the writer never judges»), и
     так обязана остаться. `os/MAINTENANCE.md` шаг 3 запрещает заводить парсер/сканер/схему под
     семантическое ревью без отдельного одобрения владельца; настоящая заявка такого одобрения НЕ
     просит и парсера НЕ заводит.

  2. ЛЮК «НЕ ПРИВЕЗЕНО, ПОТОМУ ЧТО» ОБЯЗАН ОСТАТЬСЯ ОТКРЫТЫМ, и это не смягчение, а суть. Из двух
     пропавших пунктов пятой волны как минимум один было ФИЗИЧЕСКИ невозможно привезти: реальный
     App checklist Steamworks читается только под логином владельца, у ноги доступа нет. Цель
     правки — чтобы такой пункт стал ВИДЕН строкой «не привезено, потому что читается только под
     его логином», а не чтобы его заставили привезти. Правка, превращающая люк в требование,
     сделает хуже: ноги начнут выдумывать.

  3. НИ ОДНА ЗАКРЫТАЯ ВОЛНА НЕ ПЕРЕСМАТРИВАЕТСЯ. Пятая волна закрыта словом владельца «выполнено,
     принято» и остаётся закрытой; шестая идёт и правкой не задета. Заявка чинит правило вперёд,
     а не переоткрывает прошлое. `live/**` эта нога не трогает вовсе.

  4. НИ ОДНОГО НОВОГО ФАЙЛА СОСТОЯНИЯ, ни шестого типа, ни второго журнала.

  ЛИТМУС, по которому видно, встал фикс или нет: следующий закрытый сессионный наряд с `done_when`
  обязан показать расположение по КАЖДОМУ своему пункту. Нет расположения хотя бы у одного —
  писарь отскакивает с именем этого пункта. Проверяется одним взглядом в отчёт, без раскопок и без
  инструмента.

  ЧЕСТНЫЙ ОСТАТОК, который надо назвать вслух. Правка ловит пункт, ПРОПАВШИЙ из отчёта. Она не
  ловит пункт, разобранный формально — строкой «сделано» без улики. Против этого уже стоят другие
  зубы (`(owner)`-шаги в `play_check` несут дословные слова; `owner-verdict CALL guard`; G5), и
  расширять их эта заявка не просит: одна проблема на сессию.

## КАК ЧИТАТЬ УЛИКИ, ЕСЛИ СЕССИЯ ИДЁТ НА `main`

`os/FRICTION.md:133` несёт запись-кандидат «os/**-коммиты только на main». Если следовать ей,
сессия работает в `C:/my_global_workflow` — а улики этой заявки лежат в направлении
`indie-game-development`, чья ветка `wt/indie-game-development` на 2026-08-28 НЕ ЗАПУШЕНА
(локально `6d6215db`, `origin/main` = `5b6a6925`).

Читать их с ветки, не переключаясь:

    git show wt/indie-game-development:live/indie-game-development/work/2026-08-20-call-review-wave-5-steam-minimum.md
    git show wt/indie-game-development:live/indie-game-development/history/2026-08-27-s-review-g-5a7c-wave-5-close-and-concept-reset-001.md
    git show wt/indie-game-development:live/indie-game-development/cards/closed/c-review-g-5a7c-wave-5-steam-minimum-001.md
    git show wt/indie-game-development:live/indie-game-development/cards/i-call-text-never-reached-the-executor-001.md

Правило, которое чинится, лежит на `main` и читается прямо:

    sed -n '87p' os/adapters/coding-agent.md

Перепроверить сам промах двумя командами:

    sed -n '/^## done_when/,/^## return/p' <наряд> | grep -c '^[0-9]\.'
    # -> 7
    grep -ci "матриц\|A–F\|title\|ready/missing" <отчёт>
    # -> 0

END_OF_FILE: live/direction-os/work/MAINTENANCE-3-session-returns-have-no-deliverable-reconciliation.md
