CALL → executor · play: work · direction: direction-os · ставка: g-one-screen · задача: t-one-screen-2

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-work-one-screen-skeleton-002
дерево, на котором наряд собран и на котором перемерены все указатели ниже: 5b80ec56

**Свежая сессия.** Задача исполнительская. Работа только в `panel/**`; `live/**` не трогается
вообще.

goal
Ответить фактом, а не мнением, на допущение, ради которого выбран весь подход: **все нужные
сводке числа уже лежат в существующих читалках панели, и работа есть СБОРКА, а не заведение
новых источников.** Серверная ручка сводки собирает четыре блока по реальным данным всех трёх
направлений и рядом с каждым числом кладёт строку способа счёта. Если допущение ложно — это
выясняется сейчас, в первые полдня, а не на третьей ноге, когда аппетит уже потрачен.

## ПОЧЕМУ ЭТА ЗАДАЧА СТОИТ ВТОРОЙ И ЧТО ОНА НА САМОМ ДЕЛЕ РЕШАЕТ

Это одновременно проверка самого рискованного допущения (G6) и прямой замер под порог смерти 2
ставки, дословно: «Задача `t-one-screen-2` показала, что двум и более блокам из четырёх нечем
наполниться из существующих читалок панели → ставка не продолжается, узел уходит в `map` на
разделение» (`cards/bet-g-one-screen.md` §kill_by, порог 2).

Отсюда главное свойство наряда: **отрицательный результат здесь — это УСПЕХ ноги, а не её
провал.** «Блоку нечем наполниться» — законный, ожидаемый и ценный ответ. Обходить пустоту,
дорисовывать источник, лепить заглушку или молча отдавать пустой блок — значит сломать
единственный замер, ради которого задача и стоит второй.

## ЧТО УЖЕ ПОДПИСАНО И ПЕРЕРЕШЕНИЮ НЕ ПОДЛЕЖИТ

Значения слов, которыми судится содержимое, лежат в
`live/direction-os/knowledge/five-words-that-judge-the-summary.md` — прочитать ДО первой строки
кода, это её прямой потребитель, названный в её же `read_by`:

- **что сделано** — ОТЧЁТЫ НОГ, единица = один файл `live/<id>/history/<дата>-<id>.md`. НЕ
  коммиты, НЕ карточки со статусом `done`, НЕ закрытые карточки любого вида;
- **окно** — скользящие 30 дней, НЕ календарный месяц;
- **детально** и **целиком** — ОХВАТ (все стороны направления представлены), НЕ плотность
  экрана;
- **планы** — раздел ВОЛНА, НЕ файл `panel/PLAN.md`.

Отклонённые прочтения перечислены там поимённо. Ни одно из них не возвращается «справочно» —
это рез 4 нарезки: «Он подписал одно значение; два рядом вернули бы три ответа на один вопрос».

## ВЕНЬЮ — что уже есть, перемерено на дереве 5b80ec56

Не карта решения, а карта местности, чтобы полдня не ушло на её составление. Номера строк
названы вместе с деревом и протухают; имена функций держатся.

- `panel/serve.py` (1105 строк) — читалки разделов: `section_slots:188`, `section_goals:224`,
  `section_waiting:354`, `section_ideas:402`, `section_history:489`, `section_direction:674`,
  `section_knowledge:735`, `section_wave:848`, `section_numbers:959`.
- **Ручка сводки уже существует и её содержимое подлежит замене, а не дополнению:**
  `section_dashboard:980` сегодня отдаёт список нарядов (`ready`/`other`) плюс
  `section_numbers`. Это рез, записанный набором строкой W04.
- Общая загрузка карточек — `load_cards(direction, kind=None):36`; состояние узла —
  `NODE_STATE:219`; наряд, который стоит, — `waiting_group:292` (ветка `stalled` для
  `blocked|waiting|paused`).
- **Записи вида `issue` своей читалки НЕ имеют:** `waiting_group:292` исключает их дословным
  комментарием «Записи (issue) сюда не зовутся: у них свой отвечающий и условие-событие».
  Сырьё при этом лежит и грузится — `load_cards(direction, kind="issue")`. Наполним ли из
  этого блок «проблемы» — отвечает эта задача, а не наряд.
- Шапка свежести уже есть: `build_info():169` и `staleness_note():157`.
- Приёмки: `panel/verify.py`, `test_osctl`, `test_cards_cmd`, `test_carrier`, `test_wave2`,
  `test_readers`, `test_docs`, `test_context`, `node panel/test_md.cjs`. Запуск —
  `uv run --locked python panel/<файл>.py`; панель поднимается `python panel/serve.py`.

done_when

Пять строк ниже скопированы дословно с карточки задачи
(`live/direction-os/cards/t-one-screen-2.md`). Наряд их не редактирует.

1. Серверная ручка сводки отдаёт для КАЖДОГО из трёх направлений четыре блока — что идёт · что
   стоит · что сделано за скользящие 30 дней (отчёты ног) · проблемы содержанием — и шапку
   свежести.
2. У каждого числа в ответе рядом лежит строка способа счёта: из чего и КАКИМ СПОСОБОМ оно
   получено.
3. Блок, которому не хватило источника, назван в ответе поимённо и с причиной, а не отдан
   молча пустым.
4. Рисования в этой задаче нет: проверяется наполнимость, а не вид.
5. Итог задачи прямо отвечает на порог смерти 2 ставки: скольким блокам из четырёх нечем
   наполниться из существующих читалок.

boundaries

Каждая граница ниже — чужая задача этой же ставки или чужой узел. Перешагнуть её значит
потратить чужой аппетит и покраснить чужую приёмку.

- **Не рисовать.** Ни строки в `panel/app/**`. Это строка 4 задачи и задача `t-one-screen-3`.
- **НЕ включать `dashboard` в `READY_SECTIONS`** (`panel/serve.py:129`). Включение — задача
  `t-one-screen-6`, и оно правит ПЯТЬ поверхностей одним шагом; сделанное здесь в одиночку оно
  краснит две зелёные приёмки и ломает счёт соседнего узла `g-eight-pages`
  (запись `i-eight-pages-counts-itself-through-ready-sections-001`).
- **Не чинить свежесть.** Сегодняшняя шапка врёт (`behind origin/main` — не то же, что
  неполнота), и это ИЗВЕСТНО: чинит её задача `t-one-screen-5`. Здесь шапка берётся как есть,
  какая есть.
- **Не чинить имена и машинные коды** — задача `t-one-screen-4`.
- **Не заводить новых источников и НИ ОДНОГО нового типа файла состояния** (строка W31 набора,
  критерий 4 хартии). Нет источника — так и написать. Заведение источников, если оно
  понадобится, становится отдельной целью через `map`, и решает это порог смерти 2, а не эта
  нога.
- **Не решать открытые строки набора, отданные PLAN.** Упрёшься — назови и оставь: W09
  (чем меряется неполнота, область и порог), W17 (чем группируются записи и в каком порядке),
  W21 (сколько строк на входе у блока), W23 (какие числа получают график; чем именуется
  НАПРАВЛЕНИЕ).
- **Вердикт о смерти ставки не выносится этой ногой.** Нога СЧИТАЕТ и НАЗЫВАЕТ, скольким блокам
  нечем наполниться. Что с этим числом делать — решает Направление через `review`.
- **`live/**` не трогается вообще**, включая `work/converge-g-one-screen-v3.md` — он заморожен
  уликой: читать можно, править нельзя.
- Приёмки, зелёные сегодня, остаются зелёными. Красная приёмка — это не «потом», это часть
  работы.

return

RESULT. В нём обязаны быть, иначе наряд не закрыт:

1. **Таблица четырёх блоков** — по строке на блок: наполнимо / нечем наполниться, и ЧЕМ
   именно (имя функции-читалки или имя карточки-сырья), с причиной у каждого «нечем».
2. **Число под порог смерти 2** — скольким блокам из четырёх нечем наполниться. Просто число и
   перечисление, без вердикта о судьбе ставки.
3. **Строки способа счёта** — как они звучат в ответе ручки, дословно, хотя бы по одной на блок.
4. **Что упёрлось в строки PLAN** (W09/W17/W21/W23) — названо и оставлено, а не решено.
5. **Числа с командой и деревом.** Любой замер в отчёте — вместе с командой, которой снят, и
   деревом, на котором снят. Внутрь `done_when` живые замеры не кладутся: они протухают за
   сутки.
6. **Коммит** и результат прогона названных приёмок.

**Закрытие ТЯЖЁЛОЕ: биндинговый G5 в отдельной свежей сессии.** Лёгкий путь здесь незаконен, и
причина конкретная, а не процедурная: строка 2 требует, чтобы способ счёта был ВЕРНЫМ, а не
просто присутствовал, а строка 1 — чтобы ручка реально отдавала четыре блока по трём
направлениям. Это суждения, которых никто ещё не выносил, а не байты, которые нога может
перевывести сама. Никакая фраза про экономию бюджета этого не снимает.

budget
Половина сфокусированного дня — как у всех шести задач этой ставки. Аппетит ставки 3 ноги и НЕ
РАСТЁТ (G3), запас нулевой: первый же перерасход означает рез, а не добавку. Вылезло за
полдня — это checkpoint с честным «вот докуда дошло», а не тихое продолжение.

END_OF_FILE: live/direction-os/work/c-work-one-screen-skeleton-002-call.md
