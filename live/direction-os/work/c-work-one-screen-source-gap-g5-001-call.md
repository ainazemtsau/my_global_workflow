CALL → session · play: work (биндинговая свежая G5) · direction: direction-os · ставка: g-one-screen · задача: t-one-screen-2

рабочая копия: C:\wf-direction-os, ветка wt/direction-os
карточка наряда: c-work-one-screen-source-gap-g5-001
проверяемый код: `96ff902b`, parent `5ab780d6`
основание дефекта: `4b026c0c`, binding G5
`history/2026-08-24-s-work-one-screen-skeleton-g5-001.md`
отчёт исправляющей ноги:
`history/2026-08-24-s-work-one-screen-source-gap-001.md`

**ОБЯЗАТЕЛЬНА ОТДЕЛЬНАЯ СВЕЖАЯ ФИЗИЧЕСКАЯ СЕССИЯ.** Self-check и focused acceptance
выполнила нога, написавшая исправление. Они воспроизводимы, но не являются binding G5.

goal
Дать один биндинговый вердикт по пяти исходным `done_when` `t-one-screen-2` на точном коммите
`96ff902b`: попытаться опровергнуть, что HTTP-ручка и итог задачи теперь непротиворечиво
отвечают **0 из 4 блоков без источника**, не смешивая честный ноль данных с отсутствием
источника. PASS закрывает задачу; любой найденный провал оставляет её `open`.

context

- Исправление не меняет четыре row-набора: в `dash_running` отсутствие `bet` перенесено из
  `gap` в существующий `note`; `gap` остался признаком отсутствующего источника.
- Focused acceptance — `panel/focused_dashboard_source_gap.py`. Финальная версия на
  `4b026c0c` дала exit 1 ровно на
  `solmax blocks_without_source == ['running']`; на `96ff902b` дала exit 0 и `[]` для всех
  трёх направлений.
- Ручной HTTP-замер исправляющей ноги на 2026-08-24: direction-os `7/0/18/7`, indie
  `10/0/234/44`, solmax `0/0/0/0`; у всех четыре блока, непустые `how`, шапка свежести и
  `blocks_without_source=[]`.
- Источник `history/`, временно отсутствующий в отдельном temp-source, по-прежнему дал блоку
  `done_in_window` непустой `gap` с причиной. Проверяй это сам: отчёт ноги — заявление, не
  замена своей попытке опровержения.

пять исходных done_when — дословно с `cards/t-one-screen-2.md`

1. Серверная ручка сводки отдаёт для КАЖДОГО из трёх направлений четыре блока — что идёт · что
   стоит · что сделано за скользящие 30 дней (отчёты ног) · проблемы содержанием — и шапку
   свежести.
2. У каждого числа в ответе рядом лежит строка способа счёта: из чего и КАКИМ СПОСОБОМ оно
   получено.
3. Блок, которому не хватило источника, назван в ответе поимённо и с причиной, а не отдан
   молча пустым.
4. Рисования в этой задаче нет: проверяется наполнимость, а не вид.
5. Итог задачи прямо отвечает на порог смерти 2 ставки: скольким блокам из четырёх нечем
   наполниться из существующих читалок.

boundaries

- Проверять, не чинить. В `panel/**` и `live/**` до RESULT не писать; найденный провал
  получает узкий repair CALL, а не исправление внутри G5.
- Не рисовать (`t-one-screen-3`), не менять имена и коды (`t-one-screen-4`), не чинить
  свежесть (`t-one-screen-5`), не включать dashboard и не заменять старые поля
  (`t-one-screen-6`).
- Не заводить источники или типы state-файлов; `work/converge-g-one-screen-v3.md` не править.
- Не выносить вердикт о судьбе ставки. Проверка называет только число под порогом; применение
  порога принадлежит `review`.
- Не принимать focused GREEN как доказательство самого себя: отдельно пройти HTTP, источники,
  `how`, missing-source negative control, дифф и девять штатных приёмок.

done_when

1. Каждая из пяти исходных строк получила `PASS` или `FAIL` с уликой первой руки; один FAIL
   делает общий вердикт FAIL.
2. Для трёх настоящих HTTP-ответов названы status, build/freshness, ids/count/how четырёх
   блоков, `blocks_without_source` и прямое пороговое число; четыре row-count сверены с
   дисковыми источниками независимо от `dash_*`.
3. Честный ноль solmax атакован отдельно: физические `cards/`, `NOW.md`, `history/` читаются,
   `running.count=0`, причина названа, `gap=null`, блок не входит в source tally.
4. Настоящее отсутствие источника смоделировано вне `live/**`: блок, поле `gap` и конкретная
   причина дошли до результата; существующий источник без строк с ним не смешан.
5. Финальная focused acceptance своей командой даёт точный RED на `4b026c0c` и GREEN на
   `96ff902b`; дифф проверен на границы и старые поля ответа остались рядом.
6. Все девять штатных приёмок повторены и названы по одной. PASS закрывает
   `t-one-screen-2`; FAIL оставляет её `open` и выпускает только узкую починку найденного.

return

RESULT с явным binding fresh-session G5 verdict; вердиктом по пяти исходным строкам и шести
строкам этого наряда; коммитом/parent/диффом; исходным RED и итоговым GREEN focused acceptance;
ручным HTTP/source tally; девятью штатными приёмками; диспозицией `t-one-screen-2`. Никакой
переход к соседней реализации внутри этой проверки.

budget

Треть сфокусированного дня. Это refutation, не новая реализация: обнаруженная переделка —
FAIL и следующий узкий CALL.

END_OF_FILE: live/direction-os/work/c-work-one-screen-source-gap-g5-001-call.md
