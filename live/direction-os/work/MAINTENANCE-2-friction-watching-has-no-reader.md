MAINTENANCE REQUEST
what: |
  У `os/FRICTION.md` работает ПРИЁМ и не работает ЧТЕНИЕ.

  Замер 2026-08-27 на дереве `4e73e2fa`, командами ниже:
    grep -c "^20[0-9][0-9]-" os/FRICTION.md        -> 174 записи всего
    grep -c "logged, watching" os/FRICTION.md      -> 31 запись в состоянии «повторится — чиним»

  То есть 31 запись из 174 (18%) поставлены на условие «если повторится — чиним», и НИКТО НЕ
  СЧИТАЕТ, повторилось ли. Ни один плей, ни `digest`, ни `audit`, ни `osctl check` не смотрят
  на эти строки. Условие есть, носителя у него нет.

  ДВА ДОКАЗАННЫХ ПРОМАХА, оба найдены за один день:

  1. `os/FRICTION.md:357` (2026-08-07) говорит дословно: «logged, watching: repeat means the
     map-to-shape continuation needs an enforced handoff». ПОВТОР СЛУЧИЛСЯ 2026-08-11 — правило
     девять дней знало, что должно переехать в `knowledge/`, и не имело исполнителя переезда, —
     но был записан в сообщение коммита `82faebd8`, а не в FRICTION. Счётчик повтора не увидел,
     триггер не сработал, чинить никто не пришёл.

  2. Одна запись прямо пишет про себя «кандидат-правило (≥2 записи уже есть)» — то есть её
     собственный порог УЖЕ достигнут по её же признанию, и она всё равно лежит.

  ЭТО РОВНО ТОТ КАНАЛ, КОТОРЫЙ ВЛАДЕЛЕЦ ПРОСИЛ. Его слова 2026-08-27, дословно:
    «Я могу кинуть просто в чат, сказать: бля, там в WorkFlow не работает это, или вот сделать
     это. И там сами чаты пишут какие-то проблемы, чтобы мы их тут разбирали.»
  Приём этого уже существует и уже работает — 174 записи от ног всех направлений. Разбор — нет.
where: |
  `os/FRICTION.md` — сам журнал.
  `os/MAINTENANCE.md` шаг 6 — место, где ставится метка `→ logged, watching`.
  `os/MAINTENANCE.md` шаг 2 — правило «self-initiated improvements need ≥2 FRICTION entries»,
  то есть счёт повторов уже НЕСУЩИЙ, а считать его нечем.
  `os/adapters/coding-agent.md` — команды `digest` и `audit`, кандидаты на носителя чтения.
expected: |
  У записи «повторится — чиним» появляется читатель. Минимальная форма — та, где ничего нового
  не заводится: существующая утренняя команда (`digest`) или сторож (`audit`) показывает, что
  в FRICTION лежит N записей на ожидании и какие из них уже имеют свои ≥2.

  ГРАНИЦЫ, которые владелец уже подписал и которые тут действуют:
  — премортем 6 устава `direction-os`: дефект правила чинится маршрутом maintenance, а не
    внутри направления; `live/**` из-за этого не двигается. Значит НЕ надо заводить карточки
    в направлении под каждую запись FRICTION;
  — `os/MAINTENANCE.md` шаг 3, граница семантического ревью: судить СМЫСЛ записи — работа ИИ,
    читающего файл, и новый парсер/сканер/схему под это заводить запрещено без отдельного
    одобрения владельца с одним объективным машинным предикатом. Поэтому «посчитать строки с
    меткой» — механический факт и законен; «решить, повторилось ли по существу» — суждение и
    остаётся человеку/ИИ;
  — не заводить шестой тип файла состояния и второй журнал.

  Честный остаток, который надо назвать вслух: метка `logged, watching` не несёт ни ключа, по
  которому повтор узнаётся, ни владельца. Пока её формат не даёт зацепки, счётчик покажет
  только «31 ждёт», но не «вот эта дождалась». Решить, достаточно ли этого для первой версии,
  или метке нужен минимальный ключ — часть этой заявки.

END_OF_FILE: live/direction-os/work/MAINTENANCE-2-friction-watching-has-no-reader.md
