RESULT s-life-reset-research-build-manager-001 (call: c-life-reset-research-build-manager-001)
direction: life-reset   play: research   node/task: g-life-reset-root

outcome: |
  HOW-research for the strict self-improving manager is done: each of the 10 deferred
  mechanisms has evidence + 2-3 structurally-distinct options + a recommendation (the WHAT —
  the 6-node tree — is untouched). All 10 recommendations were adversarially refuted in a
  separate pass; ALL returned holds_with_fix (none refuted, none clean). The refuters caught
  ONE real guardrail breach — Q7's hollow-week veto could condemn a genuine recovery week
  (G3 burnout-loop) → a recovery carve-out is a MUST-FIX — plus 9 anti-paper/scoping/wording
  fixes. A whole-set guardrail audit passes on all 8 invariants CONDITIONAL on those fixes;
  a coherence critic confirms the 10 mechanisms COMPOSE (composes=true) into one tiny v1
  thin slice (a single composed pulse on real data: compose-week → derive-day+R11 → one
  sealed-core file → one real protect episode → one review→decision → durable+portable →
  owner attests). A completeness critic surfaced two LOAD-BEARING gaps outside the 10
  (g-lr-integrate's summary-contract + conflict-routing; the recovery-vs-slide distinction
  several recommendations silently depend on) — surfaced, not silently expanded, no tree
  change. Owner decided (2026-06-21) to resolve both gaps co-creatively INSIDE shape (with a
  tiny-probe fallback). Full product: work/life-reset-implementation-research-v1.md.

evidence: |
  done_when of the CALL = "research RESULT with evidence + 2-3 options (each with a
  recommendation) for [the 10 mechanisms]; next = shape CALL." Met:
  - Q1 week-graph/day-derivation (g-lr-run): A flat 4-layer position-ordered list + one
    `displaces:` line; B typed-DAG; C lane-board+gate → REC A (+B's displaces); fix: keep
    Musts/Floors distinct, make displacement test non-vacuous.
  - Q2 technique-selection R32 (g-lr-grow): A diagnose-blocker→best-bet lookup; B run-&-
    measure N-of-1; C owner-menu → REC A-spine+B-loop+C-veto; fix: selection firing ONCE
    meets v1, owner-reported signal, pause-not-fail a recovery week.
  - Q3 balance min/routine/wishes/rest R29 (g-lr-run): A lexicographic jar-fill; B reserved-
    partition; C surge-governor → REC A+B-fold, defer C; fix: tier-1 = FLOORS+ROUTINE-REST+
    RECOVERY (rest is a floor, not a wish).
  - Q4 safe self-rewrite (g-lr-learn): A layered-immutability file; B post-rewrite invariant
    gate; C lexicographic dominance → REC A+B (C as wording); fix: rewrite sourced from a
    REAL review, manual git-revert, no automation overclaim (undecidability → "every actual
    rewrite gate-checked", never "provably sealed forever").
  - Q5 collapse-grip detection (g-lr-protect): A owner-pre-committed tripwires; B silence/
    absence reach-in; C trace-proxy → REC A+B's-absence, defer C; tripwires+reach-in license
    in sealed core; fix: REAL lapse not simulated, author at first-run intake.
  - Q6 earned/demotable autonomy (g-lr-learn): A stage-gated rung ladder; B track-record
    ledger; C per-domain accounts → REC A+C's-pin (floors/override outside the ladder); fix:
    don't require demotion to fire in v1; R2 owner-gated, still hits Q4's invariant replay.
  - Q7 hollow-week/aliveness (g-lr-learn): A self-rated+qualitative veto; B behavioral
    tripwire; C two-key OR → REC A built as C's subjective half; MUST-FIX recovery carve-out
    (the one real G3 breach) + keep 46/94% statistic internal only.
  - Q8 ceremony budget (g-lr-grow): A overhead-ratio; B hard WIP cap; C usage-prune + "too
    heavy" word → REC C with G1/G3 exempt; fix: probe success = a real SHED (not "kept");
    one-run "didn't fire" ≠ prune basis.
  - Q9 non-punitive re-plan + re-entry (g-lr-trust): 1 resume-state + delta-only; 2 gap-
    length ladder; 3 owner ritual → REC 1+3's-binding; fix: host the non-punishing-return as
    a SEALED-CORE invariant, NOT on the override path (category error); decouple from protect.
  - Q10 realism bar (g-lr-run/root): A artifact checklist; B stakes/bite; C adversarial self-
    cert → REC A+one-bite-slot, reserve C; fix: 5-slot list illustrative (sized at shape),
    bar = anti-paper artifact-existence + one bite event; gate rests on owner attestation.
  Cross-cutting: guardrail audit 8/8 pass-conditional; coherence composes=true + a concrete
  A-G thin slice + a 7-point shape bundle; completeness = 2 load-bearing + 7 under-built gaps
  + 6 top uncertainties. Owner gap-handling decision: "Внутрь shape со мной (реком.)".
  Multi-agent provenance: workflow wf_41aaf9c5-c4c, 23 agents (10 gen + 10 refute + 3 critic).

state_changes: |
  TREE.md: NO CHANGE (research never touches the tree; the 6 outcomes are owner-approved;
    gaps surfaced as captures, not edits).
  NOW.md:
    - active_bet.note rewritten: research DONE; 10 mechanisms answered (all holds_with_fix);
      one G3 breach (Q7) caught + patched; composes=true thin slice (A-G); two load-bearing
      gaps surfaced (integrate summary-contract; recovery-vs-slide) to be resolved inside
      shape (owner-chosen); one-real-run gate + guardrails UNCHANGED; route research → shape.
    - preserved_evidence += work/life-reset-implementation-research-v1.md ;
      history/2026-06-21-s-life-reset-research-build-manager-001.md
    - open_calls: REMOVE c-life-reset-research-build-manager-001 (answered/done) → [].
    - decisions: [] (the gap-handling decision was answered in-session: "внутрь shape со мной").
    - next: CALL c-life-reset-shape-run-v1-001 (shape on g-lr-run).
  LOG.md: appended the 2026-06-21 research line.
  history/: this file.
  work/: life-reset-implementation-research-v1.md (the product).

captures:
  - LOAD-BEARING gap (owner routed to shape): g-lr-integrate has NO mechanism here — needs a
    summary-contract (shape/refresh of a neighbor "agreed summary" without storing raw, G4)
    + conflict-surfacing-and-routing. Carries criterion ③ "3 streams". Resolve in shape.
  - LOAD-BEARING gap (owner routed to shape): the recovery-vs-slide distinction (protected
    recovery vs rationalized zone-out) is undesigned, yet G3 + Q3 + Q5 + Q7's carve-out all
    depend on it; personal → co-create with owner. Likely the shape riskiest-assumption task.
  - UNDER-BUILT (size at shape / later): day-derivation (drift-detection, rebuild, adaptive
    intensity); R11 storage + R12 fresh-day-chat; durable running-state FORMAT for portability;
    the review's analytic "find the weak link" step; the {hold|mutate|kill|route|research|
    simplify} verdict procedure; backlog lifecycle + process-extension gate (grow, deferrable);
    the non-caving WEIGHING POLICY ("достаточный довод").
  - Three reconciliations shape must apply: ONE week-compose header layout (Q1+Q3); host Q9's
    non-punishing-return in the sealed core not the override path; an R2 "act-unless-vetoed"
    rewrite still passes Q4's post-rewrite invariant replay.
  - Deferred-upgrade backlog (evidence-earned, for g-lr-grow/later, NAMED not built): Q1 DAG/
    board, Q2 swap-loop, Q3 surge-governor, Q4 auto-gate, Q5 trace-detection, Q6 ledger/
    accounts, Q7 two-key veto, Q8 binding auto-prune, Q9 gap-ladder, Q10 adversarial self-cert.

decisions_needed: []   # the one batched decision (gap-handling) was answered in-session.

play_check:
  - "1 Recite: done — restated the 10-mechanism question, the evidence+2-3-options+recommendation return format, and the one-pass multi-agent budget; reported that the CALL is 10 sub-questions answered together (not silently expanded) per its explicit multi-agent authorization."
  - "2 Investigate: done — web-grounded evidence per mechanism (verified > hype), tracked sources; method = the parallel-verify discipline (10 generators + 10 adversarial refuters + 3 critics, workflow wf_41aaf9c5-c4c); each option carries a one-line 'bad, because'."
  - "3 Confidence: done — established(+source) vs inference separated per finding; every recommendation returned holds_with_fix with a stated required-fix; one real G3 breach (Q7) and the load-bearing gaps named as what-would-change; top uncertainties listed (only a real run resolves them)."
  - "4 Close: done — RESULT with findings in the requested format + sources (work product) + confidence/limits; no decision made for the owner (research informs); next = shape CALL on g-lr-run. Knowledge candidate not written (proposed via captures)."

log: "research: HOW for the manager — 10 deferred mechanisms each got evidence+2-3 options+rec (multi-agent wf_41aaf9c5-c4c, 23 agents); all 10 refuted to holds_with_fix, 1 real G3 breach (Q7 hollow-veto) caught+patched; composes=true thin slice (A-G); 2 load-bearing gaps surfaced (integrate summary-contract; recovery-vs-slide) → owner routed to shape; tree untouched; next c-life-reset-shape-run-v1-001."

next: |
  CALL c-life-reset-shape-run-v1-001
  to: session
  direction: life-reset
  play: shape
  node: g-lr-run
  goal: |
    g-lr-run carries an owner-approved, shaped v1 bet: the smallest real slice that, run ONCE
    on real owner data, would close the one-real-run gate — a real composed week in strict
    order with the filter biting, a derived day with R11, a real review yielding one decision,
    durable artifacts surviving a chat restart, and owner attestation — with one real protect
    episode and live-state portability INSIDE it, integrate present-minimal, grow excluded.
  context: |
    - live/life-reset/work/life-reset-implementation-research-v1.md (the 10 recommendations +
      their required-fixes + §3 A-G thin slice + §5 shape bundle + §4 gaps + §6 deferrals)
    - live/life-reset/CHARTER.md (authority) ; live/life-reset/TREE.md (g-lr-run + the 6 nodes)
    - live/life-reset/history/2026-06-21-s-life-reset-map-manager-tree-001.md (cards)
    - live/life-reset/history/2026-06-21-s-life-reset-research-build-manager-001.md (this RESULT)
  boundaries: |
    Do NOT re-open the owner-approved 6-node tree (surface, never silently change). Apply ALL
    10 required-fixes from the research as bet conditions. Reconcile to ONE week-compose header
    layout (tier-1 = Musts+Floors+Routine-rest+Recovery off the top, uncuttable → Routine →
    Wishes-through-filter [≤1 experiment] → Cuts[+displaces:]). Size ONE shared sealed-core
    file (override+R11-refusal+protected-class+rollback+"rules change only via gate"+one
    collapse-grip tripwire+non-punishing-return invariant). Realness card = anti-paper
    artifact-existence + one mandatory BITE slot; 5-slot list illustrative. Resolve the two
    load-bearing gaps (recovery-vs-slide distinction; g-lr-integrate summary-contract +
    conflict-routing) CO-CREATIVELY WITH THE OWNER inside shape (owner-chosen 2026-06-21),
    with a tiny-probe fallback if either proves too big. PRESERVE: one-real-run gate (no week,
    no N-cycle; architecture-exists never counts); G1 sealed core + inviolable override; G2 no
    surfaced numbers; G3 recovery+rest protected first-class; G4 summaries-not-raw; STAYS
    LIGHT (single mentor voice, cost less attention than it saves); one entity per chat.
  done_when: |
    A shape RESULT (G6-valid) with: an owner-approved shaped v1 bet on g-lr-run carrying
    appetite + kill_by; ≥1 real cut (the deferred-upgrade backlog is the cut list); a lens-
    sweep verdict per charter lens; and a task testing the RISKIEST assumption (recommended:
    the recovery-vs-slide distinction AND whether the owner-authored tripwire fires on a real
    lapse). The v1 thin slice is sized as the §3 A-G compose-pulse with every required-fix
    folded; the two load-bearing gaps are resolved (or a tiny pre-build probe is routed).
    Next = a work/executor CALL (or the first real run) — owner-paced, no calendar block.
  return: RESULT with the shaped bet, cut list, lens sweep, riskiest-assumption task, next CALL.
  budget: one shape pass (co-creation; subagents only where the play calls for it).

---

Full research product (evidence + all options + recommendations + required-fixes + the
cross-cutting guardrail audit / coherence thin-slice / completeness gaps / deferred-upgrade
backlog / sources): live/life-reset/work/life-reset-implementation-research-v1.md
Multi-agent transcript: workflow wf_41aaf9c5-c4c (23 agents).

END_OF_FILE: live/life-reset/history/2026-06-21-s-life-reset-research-build-manager-001.md
