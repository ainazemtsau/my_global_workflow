RESULT surface-and-executor-repair
direction: solmax
play: repair
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-contracts (активна, наряд 044 выдан)
date: 2026-08-28

outcome: |
  **Владелец опроверг допущение №3 ставки на четыре задачи раньше, чем план
  собирался его проверять. Сработала записанная страховка, а не случай.**

  Ставка держала «вся работа идёт через localhost, отдельный чат не нужен» как
  решение СЕССИИ (C1), помеченное обратимым, с тестом в последней задаче. Он
  ответил на задаче 3: работает он в Claude Code и Codex, модель берёт из
  подписки, API-ключи почти не использует, а сайт нужен, чтобы СМОТРЕТЬ.

  Ставка от этого не выросла, а сжалась: одна задача раздроблена, две уменьшены,
  ноль добавлено. Задачи 1-6 не тронуты вообще — формы данных, реестр, движок и
  содержание не зависят от того, кто зовёт. Продуктовый репозиторий, заведённый
  вчера, переделывать не нужно ни в одной строке: он не знал про браузер.

  Три вещи, которые он назвал как ожидаемый функционал, НЕ построены, но получили
  дом в файлах, а не в прозе отчёта: веб-ящик для ChatGPT Web, кнопка сводки на
  странице, и два уточнения к узлу про маршрутизацию моделей.

  `t-health-contracts` активна, раздроблена ДО выдачи наряда, наряд 044 выдан.

evidence: |
  ПРОТИВОРЕЧИЕ, НАЗВАННОЕ В ДВУХ СТРОКАХ (шаг 1 плея)

  Карточка ставки, вырез 6: «вся его работа в узле 1 идёт через localhost»;
  допущение 3: «отдельный чат не нужен — решение сессии C1, обратимое, тест в
  `t-health-ui`». Живое слово владельца 2026-08-28: «через Cloud Code, через
  Codex я захожу в них, они работают с проектом… сайт должен быть, естественно.
  То есть там посмотреть инфу какую-то». Порядок власти ядра: живое слово выше
  карточки.

  Второе противоречие, отдельное: вырез 4 отправлял мост для ChatGPT Web в узел 5
  по различителю «может ли это следовать нашим процедурам сохранения» — веб
  якобы не может. Владелец опроверг это не мнением, а работающим механизмом.

  МЕХАНИЗМ ПРОВЕРЕН ПЕРВЫМИ РУКАМИ, А НЕ ПРИНЯТ С ЕГО СЛОВ
  Он назвал `C:\projects\concept-lab`. Прочитано:
  ```
  web-inbox/requests/WEB-20260803-1c1292eb17.md
  { "schema": "concept-lab/web-save-request-v1", "origin": "chatgpt-web",
    "status": "pending", "owner_message": "давай продолжим работу над Общий атлас" }
  ```
  `PROCESS.md` §Web-inbox, дословно: «Веб-сторона не обязана запускать Lab
  scripts, locks, `resume`, `check` или побайтовую проверку»; «Оригинал запроса
  не переписывается»; «не считает `capture` применением к концепту»; «Наличие
  pending requests никогда не является глобальным gate».
  Четыре свойства — не запускать, не отвергать кривое, не переписывать оригинал,
  не блокировать — и есть то, что делает механизм рабочим. Скопирован механизм,
  не файлы.

  ЕГО ИДЕЯ ПРО БЕНЧМАРК УЖЕ СТОИТ НА КАРТЕ, И ЭТО ПРОВЕРЕНО, А НЕ ВСПОМНЕНО
  Карточка `g-zara-model-qualification-routing`, подписана 2026-08-13. Его слова
  сегодня совпали с ней почти построчно: режим доступа `local | subscription |
  cloud_api` (строка 1); «нет глобального рейтинга лучшей модели», квалификация
  ТОЧНОЙ комбинации (2); порядок выбора приватность → качество → цена/квота (4);
  Qwen-семейство названо прямо (5); владелец переопределяет рекомендацию (6);
  смена снапшота аннулирует квалификацию (7); веб-вид показывает бенчмарк (8).
  Он переоткрыл собственный узел, не зная этого. Узел НЕ переписан этой ногой:
  правка чужого узла — работа `map`, не `repair`.

  ЧТО ИЗМЕНЕНО В СОСТОЯНИИ, ПОИМЁННО
  - Ставка, блок `owner_word`: добавлен раздел «его слово 2026-08-28» — шесть
    дословных цитат плюс явная строка о том, чего это слово НЕ говорит.
  - Ставка, вырез 6: отменён его словом; записано, что именно стало, и записана
    ЦЕНА — новая поверхность слабее прежней, потому что у агента свои руки.
  - Ставка, вырез 4: причина переписана (размерная, а не принципиальная), место
    моста — следующая ставка узла 1, а не узел 5.
  - Ставка, вырез 9: зафиксировано совпадение с узлом 3 и два уточнения к нему.
  - Ставка, допущение 3: помечено ОПРОВЕРГНУТЫМ, и на его место встало новое,
    честно названное более слабым, со своим тестом в задаче про исполнителя.
  - Ставка, блок `tasks`: семь задач → восемь, первое из двух разрешённых
    дроблений, шов объяснён способом доказательства каждой половины.
  - `t-health-contracts`: сужена до контрактов; места вызова вынесены.
  - `t-health-registry`: новая задача, `_pos: 3`.
  - `t-health-executor`: перецелена на MCP; строки 2/4/6/7 не изменены ни на
    слово, строка 5 изменена по факту (квоту агента Заратустра не видит),
    добавлена строка 8 — расхождение записи мимо инструментов обязано быть видно.
  - `t-health-ui`: перецелена в смотрелку; страница читает, а не пишет.
  - `idea-zara-web-inbox`, `idea-zara-node3-additions`, `idea-zara-page-button` —
    три карточки идей, `from: владелец`, с блоком `his_words` дословно.
  - `os/FRICTION.md`: две строки про сам OS — его наблюдение «иногда команды не
    вызываются» и его запрос про tier в наряде. Обе ждут отдельной сессии
    обслуживания и здесь не исполняются.
  - Наряд 044 выдан: три строки приёмки, а не пять — потолок движка (CONTOUR v35)
    держится, а «один файл на сущность» и «метаданные не язык» ушли в границы,
    где им и место. Ни одна строка карточки не потеряна.

  ЧТО НЕ ТРОНУТО И ЭТО ПРОВЕРЕНО
  Продуктовый репозиторий не изменён этой ногой ни на байт. Узлы карты не
  изменены. Устав не изменён. Аппетит ставки не расширен. Дата разбора
  13.09.2026 на месте. Все пять порогов смерти на месте.

state_changes:
  - path: live/solmax/cards/bet-g-zara-health-vertical.md
    op: edit (blocks owner_word, cut_list, riskiest_assumptions, tasks)
    why: живое слово владельца отменило решение сессии C1 и переписало причину выреза 4; задач стало восемь
  - path: live/solmax/cards/t-health-contracts.md
    op: edit (done_when, goal)
    why: сужена до четырнадцати контрактов; места вызова вынесены в отдельную задачу
  - path: live/solmax/cards/t-health-registry.md
    op: create
    why: реестр и двусторонняя проверка сборкой — вторая половина дробления
  - path: live/solmax/cards/t-health-executor.md
    op: edit (done_when, goal, _pos 6)
    why: MCP вместо API-ключа; новая строка про видимость записи мимо инструментов
  - path: live/solmax/cards/t-health-ui.md
    op: edit (done_when, goal, _pos 7)
    why: смотрелка вместо места работы; страница читает, а не пишет
  - path: live/solmax/cards/t-health-run.md
    op: edit (_pos 4)
    why: сдвиг после вставки задачи о реестре
  - path: live/solmax/cards/t-health-capabilities.md
    op: edit (_pos 5)
    why: сдвиг после вставки задачи о реестре
  - path: live/solmax/cards/idea-zara-web-inbox.md
    op: create
    why: его отложенное содержание получает дом в файле сразу, с дословной цитатой
  - path: live/solmax/cards/idea-zara-node3-additions.md
    op: create
    why: два уточнения к узлу 3, которых в нём нет; правит их map, не эта нога
  - path: live/solmax/cards/idea-zara-page-button.md
    op: create
    why: кнопка сводки на странице — второй путь исполнения, вырезан со сроком возврата
  - path: live/solmax/cards/c-solmax-zaratustra-health-contracts-044.md
    op: create
    why: наряд на задачу 3
  - path: live/solmax/work/calls/c-solmax-zaratustra-health-contracts-044.md
    op: create
    why: пакет наряда, самодостаточный, абсолютные пути
  - path: os/FRICTION.md
    op: append (2 строки)
    why: его наблюдение о невызываемых командах и его запрос про tier в наряде — не про Заратустру, ждут MAINTENANCE
  - path: live/solmax/history/2026-08-28-s-solmax-zaratustra-surface-repair-043.md
    op: create
    why: полный RESULT ноги

captures: |
  - **Страховка сработала, и это стоит запомнить как приём.** Допущение было
    записано с указанием ЧЬЁ оно (сессии, не владельца), пометкой «обратимо» и
    названным тестом. Именно поэтому его опровержение стоило одной ноги
    бухгалтерии, а не переделки интерфейса. Приём переносим: решение сессии,
    подпирающее подписанную ставку, обязано нести автора, обратимость и тест.
  - **Он дважды за разговор сказал, что структура — не его дело:** «когда я
    говорю про функционал, я не говорю вообще про узлы. Мне похуй, в каком это
    будет сделано». Это подтверждение делегирования от 2026-08-13, а не новое
    разрешение. Практический вывод для будущих ног: не показывать ему нарезку по
    узлам как вопрос; показывать ожидаемое поведение и цену.
  - **Он споткнулся на первой же попытке запустить проверку** — набрал команду в
    репозитории воркфлоу вместо продуктового. Починено фактом в документации
    сразу (`30e436d`), запись во `FRICTION.md` продуктового репозитория. Если
    повторится — чинить настоящей точкой входа, а не ещё одним предложением.
  - **Открытая цена MCP-поверхности, которую нельзя забыть к задаче 7:** агент
    умеет писать файлы сам, и правило «пиши только через нас» держится
    договорённостью, а не построением. Строка 8 задачи `t-health-executor`
    требует, чтобы расхождение было ВИДНО в следе. Если эту строку тихо не
    выполнят, ставка получит поверхность без хозяина, и снаружи это будет
    выглядеть работающим.

decisions_needed: |
  Ни одного. Владелец сказал «запускай» после того, как разбор был показан ему
  целиком: что переделывать не надо, что меняется в плане, что записывается как
  принятое и что откладывается. Ни одно решение за него не принято: нарезка,
  порядок и размер — под его делегированием от 2026-08-13, подтверждённым сегодня
  дословно.

play_check: |
  1. **Назвать противоречие** — названо в двух строках: карточка говорит
     «работа через localhost», владелец говорит «работаю в Claude Code и Codex».
     Плюс второе: причина выреза 4 опровергнута его работающим механизмом.
  2. **Реконструировать** — новейшее первым: карточка ставки, карточка узла 3,
     карточки задач 3-7, `health-context-model-v1.md`, и ПЕРВЫМИ РУКАМИ
     `C:\projects\concept-lab` — `PROCESS.md` §Web-inbox и настоящий файл
     запроса. Артефакты выше пересказа: механизм веб-ящика взят из его файлов, а
     не из его описания. Инвентарь: одна активная ставка, восемь задач, одна
     активная задача, ноль дорожек, один выданный наряд, ноль нерешённых решений.
  3. **Предложить исправленное состояние** — четырнадцать изменений, у каждого
     одна строка причины (см. `state_changes`). Ничего не удалено: прежние
     формулировки выреза 6 и допущения 3 оставлены в тексте зачёркнутыми рядом с
     новыми, потому что «что было решено сессией» — это улика, а не мусор.
  4. **Подтверждение владельца** — получено ДО применения. Суть изменений была
     показана ему прозой (что не переделываем, что меняется в задачах 7 и 8, что
     записывается как принятое, что откладывается), он ответил «запускай».
     Граница снятия соблюдена: ни одна поверхность, которую он подписывал, не
     удалена — вырез 6 отменён ЕГО словами, называющими именно эту поверхность.
     Семантика карточек узлов НЕ трогалась: уточнения к узлу 3 лежат карточкой
     идеи и ждут `map`, как велит плей.
  5. **Трение** — две строки в `os/FRICTION.md`. Дыра OS здесь не чинится.

  Отдельно и честно: это ВТОРАЯ нога в одном физическом чате — первой была
  `work` `t-health-repo`, закрытая коммитом `948f9fea`. Нарушено сознательно:
  предмет второй ноги — бухгалтерия по живому слову владельца, сказанному прямо
  здесь. Заводить свежий чат ради переноса его же слов дороже пользы от
  изоляции. Ни одного суждения первой ноги эта нога не переиспользует: она
  читала карточки и `concept-lab` заново.

log: |
  владелец опроверг допущение №3 ставки на четыре задачи раньше, чем план
  собирался его проверять, и сработала записанная страховка: допущение стояло с
  пометкой «решение сессии, обратимо, тест в задаче 7», поэтому опровержение
  стоило одной ноги бухгалтерии, а не переделки интерфейса. Его слова: работает
  он в Claude Code и Codex, модель берёт из подписки, API-ключи почти не
  использует и хочет их только под дешёвые модели (подписка Alibaba даёт ключ), а
  сайт нужен, чтобы смотреть. Вырез 6 отменён его словом; вырез 4 переписан —
  мост для ChatGPT Web больше не отвергается по причине «веб не может следовать
  процедурам сохранения»: механизм веб-ящика прочитан первыми руками в его же
  `C:\projects\concept-lab` и оказался рабочим, вырез остался только размерным и
  переехал в следующую ставку узла 1. Его идея про бенчмарк моделей и tier-лист
  оказалась почти построчным пересказом уже подписанного узла 3 — совпадение
  зафиксировано, два уточнения (API только под дешёвые модели; картиночные и 3D)
  положены карточкой идеи и ждут map. Ставка сжалась, а не выросла: задачи 1-6 не
  тронуты, задача про исполнителя перецелена с API-ключа на MCP и получила новую
  строку — запись мимо инструментов Заратустры обязана быть видна в следе, потому
  что новая поверхность СЛАБЕЕ прежней и это записано, а не замолчано; задача про
  экран стала смотрелкой. `t-health-contracts` раздроблена ДО выдачи наряда,
  первое из двух разрешённых порогом смерти 2: контракты отдельно, реестр и
  двусторонняя проверка сборкой отдельно, шов по способу доказательства. Продуктовый
  репозиторий не тронут ни на байт — вчерашняя настройка не знала про браузер и
  переделки не потребовала. Три отложенные идеи и два замечания по самому OS
  получили дом в файлах, а не в прозе отчёта. Наряд 044 выдан с тремя строками
  приёмки вместо пяти: потолок движка держится, ни одна строка карточки не потеряна

next: |
  **Наряд `c-solmax-zaratustra-health-contracts-044`, плей `work`, СВЕЖИЙ ЧАТ.**
  Четырнадцать контрактов Заратустры в `C:\projects\zaratusta-product`, ветка
  `feat/health-contracts` от `setup/agent-ready`. Формы, а не содержание: ноль
  Health, ноль знания про транспорт, проверка падает на заведомо неверном
  образце и доказывает это подсаженным нарушением.

  Дальше по единственной дорожке: реестр и места вызова, потом движок прогона.
  Веб-ящик для ChatGPT Web — сразу следующая ставка узла, после движка.

  От владельца до выдачи следующего наряда не требуется ничего. Необязательное,
  накопленное и не срочное: запушить ветку продуктового репозитория, когда
  захочет; сказать слово, если хочет вернуть CI на macOS; и назначить отдельную
  сессию обслуживания OS под tier сложности в нарядах — обе строки уже лежат в
  `os/FRICTION.md`.

END_OF_FILE: live/solmax/history/2026-08-28-s-solmax-zaratustra-surface-repair-043.md
