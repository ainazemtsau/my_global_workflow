RESULT repo-accepts-code
direction: solmax
play: work
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-repo → t-health-contracts
date: 2026-08-28
close: light — because каждая из семи строк приёмки перевыводится первыми руками
  из закоммиченных байтов и вывода команд: имена и содержимое файлов, число строк,
  ключи `validation.config`, вывод одной проверяющей команды, коды возврата 0 и 1,
  пустой `git diff` по старой поверхности и пустой вывод поиска по `src/`. Ни одна
  строка не ждёт суждения, которого никто ещё не вынес: приёмка «работает на macOS»
  наряд сам свёл к механическому основанию, а не к прогону.

outcome: |
  **Репозиторий Заратустры принимает код и проверяется одной командой.**

  Было: 20 файлов, 18 markdown, ноль исполняемых строк, и собственный корневой
  `AGENTS.md`, прямо запрещавший `src/`, тесты, скрипты и CI. Стало: 41
  отслеживаемый файл, две ветки, одна команда.

  ```
  uv run --locked python tools/check.py
  ```

  Она гоняет по возрастанию цены: статические гейты (ключи `validation.config`,
  семь правил гигиены, две строки запуска в README), `ruff format --check`,
  `ruff check`, `lint-imports`, `mypy --strict`, `pytest`. Файловый вариант —
  та же команда с `--files <пути>`; `--deliver` добавляет гейт закрывающего
  отчёта и проверку, что отчёт не ссылается на несуществующую улику.

  **Границы модулей держит граф зависимостей, а не документ.** `foundation` —
  лист и не имеет права импортировать ничего из `src/`; `runtime` может
  импортировать `foundation`, обратно — никогда. Подсаженный `import runtime`
  внутрь `foundation` роняет прогон с кодом возврата 1.

  **У каждого гейта есть негативный контроль.** `tools/selfcheck.py` подсаживает
  нарушение каждого гейта и падает, если гейт остался зелёным — а там, где
  ложный красный так же плох, кормит его чистым деревом и падает, если он
  покраснел зря. 30 контролей, все зелёные.

  **Кроссплатформенность держит инструмент, а не абзац в документации.** Первый
  `uv lock` молча взял системный `C:/Python313` — это ровно та тихая поломка,
  из-за которой обещание «работает на обеих» перестаёт быть правдой. Починено
  строкой `python-preference = "only-managed"`: uv теперь скачивает и пинует
  СВОЙ CPython, и обе машины берут один интерпретатор из одного `uv.lock`.

  Ноль продуктовой функциональности: в отслеживаемых файлах `src/` нет ни
  одного имени из предметной области Health. Старая markdown-поверхность байт
  в байт не тронута.

  Коммиты `9cae3a0` и `4f16113` на ветке `setup/agent-ready`. **Не запушены** —
  пушит владелец.

evidence: |
  СЕМЬ СТРОК ПРИЁМКИ, КАЖДАЯ ПЕРЕВЫВЕДЕНА ПЕРВЫМИ РУКАМИ

  **1 — принимает код, проверяется одной командой.**
  ```
  zaratusta check | contract 36 | python 3.13.7
  --- static (config, hygiene, deliver): ok
  --- format      11 files already formatted
  --- lint        All checks passed!
  --- boundary    Analyzed 8 files, 10 dependencies.
                  Layers: runtime may use foundation, never the reverse KEPT
                  foundation is independent of every other module KEPT
                  Contracts: 2 kept, 0 broken.
  --- types       Success: no issues found in 9 source files
  --- tests       14 passed in 0.03s
  GREEN: static, format, lint, boundary, types, tests
  ```
  Файловый вариант `--files src/foundation/ids.py tests/foundation/test_ids.py`
  → `2 files already formatted`, `no issues found in 2 source files`, `6 passed`.

  Граница — не документ. С подсаженным `from runtime import repo_root` внутри
  `src/foundation/`:
  ```
  Layers: runtime may use foundation, never the reverse BROKEN
  foundation is not allowed to import runtime:
  - foundation._seeded -> runtime (l.1)
  Contracts: 0 kept, 2 broken.
  RED: boundary
  exit code with seeded violation: 1
  exit code clean: 0
  ```

  `uv run --locked python tools/selfcheck.py` →
  `SELFCHECK GREEN: 30 controls, every gate provably fails on a seeded miss`.
  Контроли: тест вне `tests/`; `tests/<x>/` без `src/<x>/`; `pytest.mark.skip`;
  тест без единого утверждения; файл, вылезший из `_scratch/`; отсутствие
  самоигнорирующегося сторожа; `.ps1` в дереве; обратный слэш в строковом
  литерале; абсолютный путь с буквой диска; обратный слэш в конфигурации;
  разъехавшиеся строки запуска; отсутствующая `validation.config`; отсутствующий
  обязательный ключ; отсутствующий `RESULT.md`; пустое обязательное поле;
  ссылка на несуществующую улику; и граф на настоящем дереве. Плюс шесть
  контролей «на чистом дереве обязан остаться зелёным».

  **2 — корневой `AGENTS.md` заменён.** 118 строк при потолке 150, команды
  первым разделом. Несёт: что за проект, точные команды (проверены прогоном),
  раздел контракта прогона из десяти пунктов, соглашения, определение
  готовности, конституцию из семи принципов. Прежний запрет снят и остался
  уликой в `b92de14:AGENTS.md`. `CLAUDE.md` — 12 строк: `@AGENTS.md` плюс три
  платформенные заметки.

  **3 — служебный контур.** `validation.config` в TOML, и он ЧИТАЕТСЯ
  `tools/check.py` через `tomllib`, а не лежит украшением: `synced_contract_version = 36`,
  `retry_budget = 3`, пять правил эскалации, `mutation_kill_floor = 70` рядом с
  честным `mutation_runner = "none"`, `[result_report]` с путём и списком полей,
  которые гейт берёт оттуда, `[obligation_inventory]` с SHA
  `2a46d7753ea9463df5b07513bab330f870202768`, и `[gate_translation]` — все
  шестнадцать гейтов, размеченные `wired_executable` / `wired_by_review_discipline` /
  `not_applicable_until_first_opora_leg`. Отсутствие любого из трёх обязательных
  ключей роняет прогон (контроль подсаживает оба случая).
  Плюс `REVIEW.md` (планка улики `file:line`, четыре уровня тяжести с потолком в
  пять придирок, восемь «всегда проверяй», пять «пропусти»),
  `docs/adr/0001-python-uv-stack.md`, `docs/adr/0002-setup-items-deliberately-not-installed.md`,
  `docs/FRICTION.md` с четырьмя ловушками этой же ноги, `_scratch/.gitignore`.

  **4 — профиль стека.** `os/engineering/profiles/python.md`, 92 строки при
  потолке 100, пять разделов формата `profiles/README.md`. Полный текст —
  в `state_changes`.

  **5 — старая поверхность на месте.**
  `git diff b92de14 HEAD -- contracts checks examples docs/decisions docs/history`
  печатает ПУСТО. `README.md` дописан сверху и несёт строку: «The markdown layer
  below the divider … is evidence only and holds no authority over the runtime,
  its state or its schema»; прежний текст сохранён ниже разделителя и байт в
  байт равен `b92de14:README.md` (проверено `diff`).

  **6 — ноль продуктовой функциональности.**
  `git ls-files src | xargs grep -niE 'health|nutrition|training|meal|workout|diet|clinic|питани|тренировк'`
  → ноль совпадений. Под `src/` шесть файлов: `foundation/{AGENTS.md,__init__.py,ids.py,paths.py}`,
  `runtime/{AGENTS.md,__init__.py}`. `foundation` умеет слаг с версией и два
  пути; `runtime` не умеет ничего и говорит об этом в своей docstring.

  **7 — одинаково на Windows и на macOS.**
  - `git ls-files | grep -iE '[.](ps1|psm1|sh|bash|zsh|bat|cmd)$|(^|/)(Makefile|makefile|GNUmakefile)$'`
    → ноль совпадений, и гейт роняет прогон, если такой файл появится.
  - Интерпретатор пинуется инструментом: после `python-preference = "only-managed"`
    повторный `uv lock` напечатал `Downloading cpython-3.13.7-windows-x86_64-none`
    вместо прежнего `Using CPython 3.13.7 interpreter at: C:\Python313\python3.exe`.
  - Разделителей путей строкой нет: проверка идёт по `ast.Constant` над всеми
    `.py` в `src/`, `tests/`, `tools/` плюс текстом по конфигурации; сама
    проверка строит символ как `chr(92)` и потому подчиняется собственному
    правилу.
  - Две строки запуска в `README.md` (Windows и macOS) СРАВНИВАЮТСЯ гейтом, а не
    принимаются на слово: `uv run --locked python tools/check.py` обе.
  - Живой прогон — только Windows, и это сказано прямо. **Прогона на macOS нет,
    и нога его не заявляет.**

  ЧТО СДЕЛАНО НЕ ПО `PROJECT_SETUP.md` И ПОЧЕМУ — всё в ADR-0002, ни одного
  молчаливого пропуска: OpenSpec (тянет Node — вторую среду, ради отказа от
  которой и выбран стек; при PROBA замороженная папка изменения никому не
  должна), G2-мутации / негативные контроли / слой свойств (раннера нет; ключ
  порога стоит с честным `mutation_runner = "none"` вместо выдуманного счёта),
  `PROGRESS.md` и реестр стадий (маршрут v29-v34, к этому репозиторию не
  приколотый), профиль прав и подписки агента (это конфигурация платформы, а
  копия в репозитории была бы вторым источником правды).

state_changes:
  - path: os/engineering/profiles/python.md
    op: create
    why: побочный продукт настройки; профиля Python не было, формат profiles/README.md, 92 строки при потолке 100
    content: |
      (полный текст файла — 92 строки, положен писателем в этой же ноге;
      разделы: 1 module conventions · 2 default validation.config thresholds ·
      3 test layout · 4 known landmines + mechanical fixes · 5 setup notes.
      Ловушки, вынесенные из этой ноги: uv молча берёт системный интерпретатор →
      python-preference = "only-managed"; `python -m importlinter` выходит с
      нулём ничего не проверив → резолвить консольный скрипт lint-imports через
      shutil.which и СТОП по имени; плавающие версии инструментов гейта →
      пин через ==; обратный слэш в литерале → проверка по ast.Constant с
      люком `# check: allow-backslash` под запись во FRICTION; разъехавшиеся
      строки запуска → таблица в README, которую гейт сравнивает; CI — трата и
      потому решение владельца, предлагаемая ногой настройки, но не ставимая ею)
  - path: live/solmax/cards/t-health-repo.md
    op: close -> cards/closed/, status done
    why: семь строк приёмки перевыведены первыми руками; закрытие светлое
  - path: live/solmax/cards/c-solmax-zaratustra-health-repo-041.md
    op: close -> cards/closed/, status closed
    why: наряд исполнен полностью, возврат — этот RESULT
  - path: live/solmax/cards/t-health-contracts.md
    op: edit, status active
    why: третья задача единственной дорожки; G1 держится — одна активная задача
  - path: live/solmax/history/2026-08-28-s-solmax-zaratustra-health-repo-042.md
    op: create
    why: полный RESULT ноги

captures: |
  - **CI вынесен ему вопросом, а не поставлен.** Матрица `windows-latest` +
    `macos-latest` — самый дешёвый способ превратить механическое основание про
    macOS в наблюдение. Файл был написан в этой ноге и СНЯТ до закрытия: он
    тратит его минуты GitHub Actions (macOS-раннеры считаются вдесятеро), а он
    одобрял стек, а не трату; порог смерти 5 ставки стоит ровно на молчаливой
    трате. Названо в отчёте — не значит одобрено: он пушит не читая каждый файл.
    Ставится обратно одним его словом и одним коммитом.
  - **`CONTRACT_VERSION` НЕ поднят до 37, и это решение, а не забывчивость.**
    Файл говорит, что нагруженное изменение в `profiles/**` поднимает число.
    Новый профиль под стек, которого не было, ничьих обязательств не меняет, а
    подъём немедленно пометил бы все существующие репозитории отставшими и
    потребовал бы от них ре-синка ради изменения, их не касающегося. Сам
    `PROJECT_SETUP.md` велит создавать профиль побочным продуктом каждой
    настройки — если бы это требовало подъёма, поднимала бы каждая настройка.
  - **Имена модулей `foundation` и `runtime` — временный каркас**, а не решение
    об архитектуре продукта. Они существуют, чтобы правило границ было
    непустым. Что там живёт на самом деле, решают задачи 3 и 4; ADR-0001
    называет их временными прямо.
  - **Строку «ноль упоминаний Health в общем коде» стоит превратить в гейт в
    задаче 3.** Здесь она проверена командой поиска и приложена числом, но
    гейта нет: словарь предметной области появляется только в задаче 3, и
    держать его список тут было бы содержанием ниже по течению. Это же строка 5
    приёмки `t-health-contracts` и измерение линзы расширяемости ставки.
  - **Порог 70 для mutation_kill_floor — решение сессии под его делегированием**,
    обратимое. Он ни к чему не обязывает, пока нет ни одной ноги OPORA.

decisions_needed: |
  Одно, маленькое, и оно НЕ блокирует задачу.

  **Ставить ли CI, который гоняет ту же команду на macOS?**
  - **А — не ставить (рекомендация).** Механическое основание наряд признаёт
    достаточным: скриптов нет, интерпретатор пинует uv, пути только `pathlib`,
    строки запуска сравнивает гейт. Ноль трат. Цена: прогона на macOS не будет
    вообще, пока ты сам туда не сядешь.
  - **Б — поставить.** Матрица на двух твоих платформах, ~25 строк, файл готов.
    Цена: минуты GitHub Actions на каждый пуш, macOS-раннеры считаются
    вдесятеро; на приватном репозитории это ест бесплатную квоту.

  Спрашиваю потому, что это трата, а не потому, что не знаю, как сделать.

play_check: |
  1. **Recite** — обязательство ставки: `t-health-repo`, вторая задача
     единственной дорожки, наряд `c-solmax-zaratustra-health-repo-041`, статус
     `ready`, `to: session`. Пересказана владельцу в открытии ноги.
  2. **Owner inputs** — не потребовались, и это обосновано, а не пропущено.
     Единственный вопрос к владельцу по этой задаче — стек — закрыт его
     дословными словами 2026-08-27: «стек тот который выберешь, мне главное
     чтобы оптимально работал и работал на windows и macos». Остальное — под
     его делегированием от 2026-08-13. Замена ЯЗЫКА была бы вопросом к нему;
     язык не менялся. Одна трата, которую нога встретила по ходу (CI),
     ВЫНЕСЕНА ему, а не принята за него.
  3. **Do the work** — построено в продуктовом репозитории, на ветке
     `setup/agent-ready`, локальными коммитами, без пуша. Фан-аут не
     разворачивался: наряд прямо говорит «фан-аут не требуется», а правило
     репозитория относит одиночную задачу `work` к механическим ногам.
  4. **Self-check** — семь строк сверены поимённо; каждая перевыведена первыми
     руками (вывод команд и байты, а не пересказ). G5: **светлое закрытие**,
     свежий чат опровержения НЕ открывается и не обходится — ни одна строка не
     требует суждения, которого никто не вынес. Приёмка про macOS сведена самим
     нарядом к механическому основанию, и нога не заявляет прогона, которого не
     было. Что свежее опровержение всё-таки стоило бы атаковать, названо честно:
     скелет из двух модулей тонок, и граф границ опирается на одно ребро.
     G7: единственное найденное решение (трата на CI) вынесено владельцу.
  5. **Close** — `t-health-repo` done, наряд 041 закрыт, `t-health-contracts`
     активирована. G1 держится: одна активная задача. Наряд на неё в этой ноге
     НЕ выдан — сознательно: карточка уже несёт шесть строк приёмки, но перед
     выдачей нужно решить, дробится ли задача (правило размера ставки и порог
     смерти 2), а это чтение артефакта модели контекста и отдельное суждение.
     Дробить положено ДО выдачи наряда, а не по ходу.

log: |
  продуктовый репозиторий начал принимать код: одна команда
  `uv run --locked python tools/check.py` гоняет статические гейты, ruff format,
  ruff check, lint-imports, mypy --strict и pytest, есть файловый вариант и
  deliver-вариант; границы модулей держит граф зависимостей, а не документ —
  подсаженный `import runtime` внутрь `foundation` роняет прогон с кодом 1, а
  `tools/selfcheck.py` доказывает 30 подсаженными нарушениями, что краснеет
  КАЖДЫЙ гейт и что на чистом дереве ни один не краснеет зря; стек Python 3.13
  под uv, и кроссплатформенность держит инструмент, а не документация:
  `python-preference = only-managed` заставила uv скачать и запинить свой CPython
  вместо системного, который первый `uv lock` молча взял; корневой AGENTS.md
  заменён — 118 строк, команды первыми, контракт прогона, конституция из семи
  принципов, прежний запрет на src/ снят и остался в истории Git; служебный
  контур собран: validation.config со стампом 36 и честным mutation_runner=none
  вместо выдуманного счёта, REVIEW.md, ADR-0001 и ADR-0002 с каждым непоставленным
  пунктом PROJECT_SETUP и условием его возврата, FRICTION с четырьмя ловушками,
  самоигнорирующийся _scratch/; ноль продуктовой функциональности — поиск
  Health-словаря по src/ возвращает ноль, старая markdown-поверхность байт в байт
  не тронута; CI написан и СНЯТ до закрытия, потому что тратит его минуты
  GitHub Actions, а он одобрял стек, а не трату, и вынесен ему вопросом; профиль
  python-uv на 92 строки возвращён и положен; коммиты 9cae3a0 и 4f16113 на ветке
  setup/agent-ready, не запушены; закрытие светлое — каждая из семи строк приёмки
  перевыведена первыми руками из закоммиченных байтов и вывода команд

next: |
  **Задача 3 — `t-health-contracts`** — активирована, дорожка та же, лимит
  одновременных задач держится: одна.

  Наряд на неё в этой ноге НЕ выдан сознательно. Её карточка уже несёт шесть
  строк приёмки (четырнадцать контрактов, один файл на сущность, объявленные
  места вызова с проверкой в обе стороны, детерминированные образцы, ноль
  Health в общем коде, метаданные не становятся языком), но перед выдачей надо
  решить одно: **дробится ли она.** Четырнадцать контрактов плюс двусторонняя
  проверка мест вызова плюс образцы — это кандидат на перебор полдня, а правило
  размера ставки велит дробить ДО выдачи наряда, и порог смерти 2 считает такие
  дробления. Это чтение `work/health-context-model-v1.md` и отдельное суждение —
  открытие следующей ноги, а не хвост этой.

  Что следующая нога получает готовым и чего у неё раньше не было:
  ветка `setup/agent-ready` с зелёной одной командой, границы, ломающиеся на
  подсаженном импорте, и негативный контроль, который не даст добавить гейт,
  умеющий только зеленеть.

  От владельца до её выдачи ничего не требуется. Одно необязательное:
  ответить про CI, и запушить ветку, когда захочет.

END_OF_FILE: live/solmax/history/2026-08-28-s-solmax-zaratustra-health-repo-042.md
