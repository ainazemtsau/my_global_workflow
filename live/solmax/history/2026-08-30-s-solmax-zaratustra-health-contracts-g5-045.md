RESULT s-solmax-zaratustra-health-contracts-g5-045
direction: solmax
play: review
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-contracts
call: c-solmax-zaratustra-health-contracts-g5-045

outcome: |
  verdict: partial

  `t-health-contracts` закрывать НЕЛЬЗЯ. Строки приёмки 2 и 3 наряда 044
  выполнены. Строка 1 выполнена частично: четырнадцать контрактов, их
  идентичности, один файл на сущность, машинная проверка полей и отсутствие
  транспорта устояли; объявленная идентичность формы `context_item` подписанному
  источнику не устояла.

  Два расхождения несущие:

  1. Подписанное свойство `граница` означает «кто читает и кто пишет».
     Контракт сохраняет только `boundary_read`. Подстановка `owner` вместо
     пишущей половины неверна: источник отдельно различает «кто вправе менять»
     и фактическую границу записи. Например, `owner=system`, но пишет только
     принятая коррекция/смена версии; `owner=observation`, но пишет захват дня.
     Кроме того, три значения `boundary_read` грубее подписанных ограничений
     вроде «читает разбор дня по запросу».
  2. Подписанная `актуальность` — окно. Закрытое перечисление
     `not_applicable|day|week|month|until_next_version` не выражает уже
     подписанные формы: конкретный диапазон дат, фазу, годы, «до отмены
     владельцем», «до следующего принятого разбора».

  Заявление «метаданные не являются исполняемым языком» в буквальной форме
  также не устояло. `workflow`, `step`, `transition` и `effect` уже задают
  конечный декларативный автомат: точку входа, операцию, исход, переход и
  эффект. Обычный исполнитель на `while`/`match` пройдёт текущий
  `vocabulary/no-interpreter`, потому что гейт запрещает только
  `eval|exec|compile|__import__|importlib|runpy`.

  Два кросс-правила сами по себе не делают язык общим или расширяемым:
  `_validate.py` интерпретирует контрактную схему для валидации, но не исполняет
  workflow. Однако аргумент ADR «функции — не интерпретатор, таблица —
  интерпретатор» границу не доказывает: форма диспетчеризации не меняет
  семантику.

  Владелец разрешил точную трактовку дословным ответом
  «согласен с твоей рекомендацией» на рекомендацию:
  конечный декларативный автомат допустим; запрещены расширяемый язык процедур,
  пользовательские условия/выражения и новые инструкции из данных.
  Поэтому задача не требует уничтожать transition-граф; она требует исправить
  формулировку и доказать закрытость его набора инструкций.

evidence: |
  ПРОДУКТОВЫЙ СНИМОК

  Репозиторий `C:\projects\zaratusta-product`, ветка
  `feat/health-contracts`, HEAD
  `5b46bd3b007932fe4a12ea6040b5823df5add4c8`, родитель
  `30e436d0e46e84f649b68e7af7387377e11e57c6`.
  До проверки и после всех подсадок `git status --short` не вывел ни одного
  пути. `git diff --exit-code` и `git diff --cached --exit-code` завершились
  с кодом 0. Временные файлы и intent-to-add записи удалены; ни коммита, ни
  push не было.

  ВЕРДИКТЫ ПО ТРЁМ СТРОКАМ НАРЯДА 044

  1. ВЫПОЛНЕНА ЧАСТИЧНО — 14 контрактов и версионированных идентичностей
     существуют; один файл на сущность, машинная проверка полей и транспортная
     чистота доказаны. Форма `context_item` не идентична подписанному источнику:
     потеряна пишущая половина `границы`, читающая половина огрублена,
     `актуальность` заменена неполным закрытым перечислением.
  2. ВЫПОЛНЕНА — нормальные и отвергаемые образцы проходят в обе стороны;
     `missing_required`, `unknown_enum_value`, `broken_ref` и
     `effect_out_of_bounds` реально проверяются. Чистая команда зелёная,
     selfcheck красит подсаженную ложь и возвращает файл байт в байт.
  3. ВЫПОЛНЕНА — поиск по `src/ tests/` вернул ноль строк (`EXIT=1`);
     `tools/check.py --deliver` зелёная на чистом дереве.

  РАЗБОР СТРОКИ 1 ПО ЧАСТЯМ

  - четырнадцать контрактов и идентичности — выполнено;
  - один файл на сущность — выполнено;
  - машинная проверка полей — выполнено;
  - форма элемента контекста из подписанного источника — выполнено частично;
  - отсутствие транспорта — выполнено.

  Фактический импорт закоммиченных байтов:

      contracts=14
      area=area-v1
      capability=capability-v1
      operation=operation-v1
      workflow=workflow-v1
      step=step-v1
      run=run-v1
      transition=transition-v1
      effect=effect-v1
      artifact=artifact-v1
      owner_projection=owner-projection-v1
      trace_event=trace-event-v1
      context_item=context-item-v1
      context_ref=context-ref-v1
      permission=permission-v1
      context_fields=id,context_class,owner,kind,origin,version,sensitivity,freshness,uses,boundary_read
      context_classes=owner_shared,area_scoped,opaque
      valid_world_findings=0
      samples=area:1/2,capability:1/2,operation:1/2,workflow:1/2,step:1/3,run:1/2,transition:1/2,effect:1/2,artifact:1/2,owner_projection:1/2,trace_event:1/2,context_item:1/3,context_ref:1/2,permission:1/2

  ПОСТРОЧНАЯ СВЕРКА ДЕВЯТИ СВОЙСТВ

  | подписанное свойство | место в контракте | вид | вердикт |
  |---|---|---|---|
  | `id` | `id` | обязательный `IDENT` | Соответствует форме: устойчивое машинно проверяемое имя. Неизменность во времени этим контрактом сама по себе не доказывается. |
  | `владелец` | `owner` | обязательный enum `owner|system|observation` | Соответствует по именованным значениям и смыслу «кто вправе менять». |
  | `тип` | `kind` | обязательный enum `fact|version|rule|observation|policy` | Соответствует. |
  | `происхождение` | `origin` | обязательный `LABEL`, carried-only | Соответствует форме подписанного свободного текста; проверяются наличие и строковый тип, не смысл. |
  | `версия/наблюдение` | `version` | обязательный `LABEL`, carried-only | Значение может нести обе подписанные разновидности, хотя имя `version` их не различает. Потери представимости нет; машинного различения источник не требовал. |
  | `чувствительность` | `sensitivity` | обязательный enum `ordinary|bodily|opaque` | Соответствует трём подписанным значениям. |
  | `актуальность` | `freshness` | обязательный закрытый enum из пяти значений | НЕ соответствует: нельзя без потери представить конкретный интервал, фазу, годы, «до отмены» и «до следующего принятого дня». |
  | `применения` | `uses` | обязательный повторяемый `LABEL`, carried-only | Соответствует свободнотекстовой форме источника; машина проверяет наличие/непустоту, но намеренно не толкует разрешение. Это ограничение, не расхождение. |
  | `граница` | `boundary_read` | обязательный enum `owner_shared|area_scoped|private` | НЕ соответствует: пишущая половина отсутствует, а читающая огрубляет объявленных читателей. `owner` не является эквивалентом `boundary_write`. |

  Класс — отдельный дискриминатор сверх девяти свойств:
  `context_class=owner_shared|area_scoped|opaque`. Ровно три класса сохранены;
  `owner/shared` и `area/health` нейтрально переименованы для общего словаря.

  ОБЯЗАТЕЛЬНАЯ КОМАНДА 1 — ДОСЛОВНЫЙ ВЫВОД

      $ uv run --locked python tools/check.py --deliver
      zaratusta check | contract 36 | python 3.13.7
      --- static (config, hygiene, deliver): ok
      --- format
      33 files already formatted
      --- lint
      All checks passed!
      --- boundary

      ╔══╗─────────▶╔╗ ╔╗      ╔╗◀───┐
      ╚╣╠╝◀─────┐  ╔╝╚╗║║────▶╔╝╚╗   │
       ║║   ╔══╦══╦╩╗╔╝║║  ╔╦═╩╗╔╝╔═╦══╗
       ║║╔══╣╔╗║╔╗║╔╣║ ║║ ╔╬╣╔╗║║ ║│║╔═╝
      ╔╣╠╣║║║╚╝║╚╝║║║╚╗║╚═╝║║║║║╚╗║═╣║
      ╚══╩╩╩╣╔═╩══╩╝╚═╝╚═══╩╩╝╚╩═╩╩═╩╝
        └──▶║║                    ▲
            ╚╝────────────────────┘


      ---------
      Contracts
      ---------

      Analyzed 29 files, 67 dependencies.
      -----------------------------------

      Layers: runtime over vocabulary over foundation, never upwards KEPT
      foundation is independent of every other module KEPT
      vocabulary is a dictionary: it knows nothing of the layer above it KEPT

      Contracts: 3 kept, 0 broken.
      --- types
      Success: no issues found in 30 source files
      --- tests
      ........................................................................ [ 51%]
      ....................................................................     [100%]
      140 passed in 0.13s

      GREEN: static, format, lint, boundary, types, tests

  Код возврата 0.

  ОБЯЗАТЕЛЬНАЯ КОМАНДА 2 — ДОСЛОВНЫЙ ВЫВОД

      $ uv run --locked python tools/selfcheck.py
      ok   test-layout red
      ok   test-layout green
      ok   orphan-tests red
      ok   orphan-tests green
      ok   skipped-test red
      ok   skipped-test green
      ok   empty-test red
      ok   empty-test green
      ok   scratch red
      ok   scratch keeper red
      ok   scratch green
      ok   shell-script red
      ok   backslash red
      ok   drive-path red
      ok   config-backslash red
      ok   allow-hatch green
      ok   launch-lines green
      ok   launch-drift red
      ok   launch-missing red
      ok   launch-no-readme red
      ok   config missing red
      ok   config key missing red
      ok   config green
      ok   report missing red
      ok   report field red
      ok   cited-artifact red
      ok   report green
      ok   domain-word red
      ok   domain-stem red
      ok   domain green
      ok   transport-word red
      ok   transport-stem red
      ok   transport green
      ok   transport outside src green
      ok   eval red
      ok   importlib red
      ok   re.compile green
      ok   one-per-file green
      ok   two-in-a-file red
      ok   empty-entity-file red
      ok   unnamed-contract red
      ok   entity-in-machinery red
      ok   negative-controls green
      ok   no-rejected-sample red
      ok   no-conforming-sample red
      ok   uncovered-violation red
      ok   boundary green
      ok   boundary red (runtime)
      ok   boundary red (vocabulary)
      ok   boundary cleanup
      ok   conformance green
      ok   conformance red
      ok   conformance cleanup

      SELFCHECK GREEN: 53 controls, every gate provably fails on a seeded miss

  Код возврата 0.

  СОБСТВЕННАЯ ПОДСАДКА 1 — ПЯТНАДЦАТЫЙ КОНТРАКТ БЕЗ ОТВЕРГАЕМОГО ОБРАЗЦА

      zaratusta check | contract 36 | python 3.13.7
      --- static (config, hygiene, deliver): FAIL
        [vocabulary/negative-controls] src/vocabulary/seeded_fifteenth.py: no rejected sample
      --- format
      34 files already formatted
      --- lint
      All checks passed!
      --- boundary

      ╔══╗─────────▶╔╗ ╔╗      ╔╗◀───┐
      ╚╣╠╝◀─────┐  ╔╝╚╗║║────▶╔╝╚╗   │
       ║║   ╔══╦══╦╩╗╔╝║║  ╔╦═╩╗╔╝╔═╦══╗
       ║║╔══╣╔╗║╔╗║╔╣║ ║║ ╔╬╣╔╗║║ ║│║╔═╝
      ╔╣╠╣║║║╚╝║╚╝║║║╚╗║╚═╝║║║║║╚╗║═╣║
      ╚══╩╩╩╣╔═╩══╩╝╚═╝╚═══╩╩╝╚╩═╩╩═╩╝
        └──▶║║                    ▲
            ╚╝────────────────────┘


      ---------
      Contracts
      ---------

      Analyzed 30 files, 68 dependencies.
      -----------------------------------

      Layers: runtime over vocabulary over foundation, never upwards KEPT
      foundation is independent of every other module KEPT
      vocabulary is a dictionary: it knows nothing of the layer above it KEPT

      Contracts: 3 kept, 0 broken.
      --- types
      Success: no issues found in 31 source files
      --- tests
      ........................................................................ [ 51%]
      ....................................................................     [100%]
      140 passed in 0.14s

      RED: static

  Код возврата 1: покраснело ровно на отсутствии rejected sample.

  СОБСТВЕННАЯ ПОДСАДКА 2 — ТРАНСПОРТНОЕ СЛОВО В КОНТРАКТЕ

      zaratusta check | contract 36 | python 3.13.7
      --- static (config, hygiene, deliver): FAIL
        [vocabulary/transport-free] src/vocabulary/seeded_transport.py: transport vocabulary: http
      --- format
      34 files already formatted
      --- lint
      All checks passed!
      --- boundary

      ╔══╗─────────▶╔╗ ╔╗      ╔╗◀───┐
      ╚╣╠╝◀─────┐  ╔╝╚╗║║────▶╔╝╚╗   │
       ║║   ╔══╦══╦╩╗╔╝║║  ╔╦═╩╗╔╝╔═╦══╗
       ║║╔══╣╔╗║╔╗║╔╣║ ║║ ╔╬╣╔╗║║ ║│║╔═╝
      ╔╣╠╣║║║╚╝║╚╝║║║╚╗║╚═╝║║║║║╚╗║═╣║
      ╚══╩╩╩╣╔═╩══╩╝╚═╝╚═══╩╩╝╚╩═╩╩═╩╝
        └──▶║║                    ▲
            ╚╝────────────────────┘


      ---------
      Contracts
      ---------

      Analyzed 30 files, 68 dependencies.
      -----------------------------------

      Layers: runtime over vocabulary over foundation, never upwards KEPT
      foundation is independent of every other module KEPT
      vocabulary is a dictionary: it knows nothing of the layer above it KEPT

      Contracts: 3 kept, 0 broken.
      --- types
      Success: no issues found in 31 source files
      --- tests
      ........................................................................ [ 51%]
      ....................................................................     [100%]
      140 passed in 0.14s

      RED: static

  Код возврата 1: транспортный гейт покраснел на `http`.

  СОБСТВЕННАЯ ПОДСАДКА 3 — КОНТРАКТ ЧЕРЕЗ ФУНКЦИЮ-ПОМОЩНИК

      zaratusta check | contract 36 | python 3.13.7
      --- static (config, hygiene, deliver): FAIL
        [vocabulary/one-per-file] src/vocabulary/seeded_helper.py: declares 0 contracts; an entity file declares exactly one
      --- format
      34 files already formatted
      --- lint
      All checks passed!
      --- boundary

      ╔══╗─────────▶╔╗ ╔╗      ╔╗◀───┐
      ╚╣╠╝◀─────┐  ╔╝╚╗║║────▶╔╝╚╗   │
       ║║   ╔══╦══╦╩╗╔╝║║  ╔╦═╩╗╔╝╔═╦══╗
       ║║╔══╣╔╗║╔╗║╔╣║ ║║ ╔╬╣╔╗║║ ║│║╔═╝
      ╔╣╠╣║║║╚╝║╚╝║║║╚╗║╚═╝║║║║║╚╗║═╣║
      ╚══╩╩╩╣╔═╩══╩╝╚═╝╚═══╩╩╝╚╩═╩╩═╩╝
        └──▶║║                    ▲
            ╚╝────────────────────┘


      ---------
      Contracts
      ---------

      Analyzed 30 files, 68 dependencies.
      -----------------------------------

      Layers: runtime over vocabulary over foundation, never upwards KEPT
      foundation is independent of every other module KEPT
      vocabulary is a dictionary: it knows nothing of the layer above it KEPT

      Contracts: 3 kept, 0 broken.
      --- types
      Success: no issues found in 31 source files
      --- tests
      ........................................................................ [ 51%]
      ....................................................................     [100%]
      140 passed in 0.13s

      RED: static

  Код возврата 1. Слабое место автора 044 здесь опровергнуто: helper-файл не
  был пропущен — он классифицирован как entity-файл с нулём прямых
  `Contract(...)`, и гейт упал.

  ПОИСК ОБЩЕГО КОДА — ДОСЛОВНЫЙ ВЫВОД

      $ git grep -niE 'health|nutrition|training|diet|calorie|meal|menu|workout|exercise|питани|тренировк|здоров' -- src/ tests/
      EXIT=1

  Между командой и `EXIT=1` строк нет: совпадений ноль.

  АТАКА УТВЕРЖДЕНИЯ ПРО ЯЗЫК

  - `_field_findings` обходит описанные данными `Field`;
    `_scalar` диспетчеризует поведение по `FieldKind`, `choices` и `ref`.
    Это уже интерпретация схемы, допустимая потому, что она только валидирует.
  - `_effect_bounds` — второе специальное правило. Количество кросс-правил не
    отвечает на вопрос о языке; отвечает то, какие новые семантические операции
    способны добавить данные.
  - `workflow.entry_step`, `step.operation`, `step.transitions`,
    `transition.on`, `transition.to_step` и `effect.kind` вместе являются
    синтаксисом конечного автомата. Будущий runner неизбежно читает их.
  - Текущий AST-гейт доказывает только отсутствие нескольких механизмов
    динамического выполнения Python. Он не доказывает отсутствие
    интерпретатора конечного автомата.
  - После слова владельца правильный инвариант: записи могут составлять только
    заранее закрытые инструкции; сценарий не может определить из данных новый
    opcode, предикат, действие или способ исполнения.

state_changes: |
  1. Закрыть карточку
     `c-solmax-zaratustra-health-contracts-g5-045`, терминальный статус `done`:
     опровержение выполнено, вердикт задачи `partial`.

  2. `t-health-contracts` оставить `active`.

  3. В `t-health-contracts.done_when` заменить только строку 5.

     Было:
     «Метаданные валидируют, маршрутизируют, проецируют и трассируют — и не
     являются исполняемым языком. Ни компилятора, ни интерпретатора, ни общего
     языка процедур; добавление сценария не может их породить».

     Стало:
     «Метаданные задают конечный декларативный автомат, но не расширяемый язык
     процедур. Сценарий может составлять записи только из заранее закрытых
     типов шага, исполнителя, исхода, перехода и эффекта; данные не могут
     добавить новый opcode, пользовательское условие/выражение, действие или
     способ исполнения. Динамической компиляции и общего языка процедур нет;
     изменение набора инструкций требует ревьюируемого изменения Python-кода,
     а не добавления сценария».

     owner approval: владелец ответил дословно
     «согласен с твоей рекомендацией» на показанный вариант:
     конечный декларативный автомат допустим, запрещён расширяемый язык
     процедур, пользовательские условия/выражения и новые инструкции из данных.

  4. Завести карточку
     `c-solmax-zaratustra-health-contracts-rework-046`,
     kind `call`, `to: session`, `play: work`, `for: t-health-contracts`,
     `status: ready`, `_bet: g-zara-health-vertical`, `_pos: 11`,
     `call: work/calls/c-solmax-zaratustra-health-contracts-rework-046.md`.

  5. Создать полный пакет
     `live/solmax/work/calls/c-solmax-zaratustra-health-contracts-rework-046.md`
     из `RESULT.next` ниже.

  6. NOW, ставка, узлы, дорожки, остальные задачи, решения, вопросы и знания
     сохранить без изменений. Это переработка той же задачи, не второе
     дробление и не закрытие ставки.

captures: |
  Ничего. Все найденные дефекты входят прямо в наряд-переработку. Helper-дыра,
  названная автором 044, не существует в проверенной форме: гейт её ловит.

decisions_needed: |
  Ничего. Семантическую развилку владелец закрыл словами
  «согласен с твоей рекомендацией». Рекомендованный и теперь утверждённый смысл:
  конечный декларативный автомат допустим; расширяемый язык процедур и новые
  инструкции из данных запрещены.

play_check: |
  1. Verify by refutation — каждая из трёх строк наряда 044 атакована отдельно;
     строка 1 разобрана на пять частей, девять свойств сверены построчно.
     Вердикт `partial`: две семантические потери формы найдены.
  2. Harvest per lens —
     - Extensibility: конечный закрытый автомат совместим с добавлением сценария
       без новых opcodes; текущая абсолютная формулировка это скрывала.
     - Real depth: непосредственного доказательства глубины эта словарная задача
       не даёт и не должна.
     - Agent-buildability: команды, selfcheck и три собственных подсадки
       воспроизводимы; семантическая идентичность лексическим гейтом не закрывается.
     - Daily use/dogfood: живого использования эта задача не добавляет.
     - Privacy/trust/safety: потеря пишущей половины границы и огрубление читателя
       являются настоящим дефектом доверия, поэтому задача не закрыта.
     - Cost discipline: изменений и новых расходов нет.
  3. Tree diff (owner) — узлы и ставка не меняются. После нормального человеческого
     брифа владелец утвердил рекомендованную трактовку дословно:
     «согласен с твоей рекомендацией». Меняется только строка 5 задачи и
     открывается переработка.
  4. Add-back check — cut list ставки не расширяется: реестр, runner, предметное
     содержимое и реальные карточки сюда не возвращаются. Переработка остаётся
     внутри исходной задачи.
  5. Knowledge — нового принятого знания с отдельным `read_by` не требуется;
     доказательство и слово владельца сохраняются в истории этой ноги и в
     исправленной строке задачи.
  6. Forecast & next — прогноз ставки остаётся `no_basis`; одна словарная задача
     требует локальной переработки. Следом только свежая отдельная G5-проверка;
     `t-health-registry` до неё не запускается.
  7. Close — returning CALL закрывается, задача остаётся active, готовый CALL
     переработки открыт. Продуктовый и Direction-репозитории этой сессией не
     изменялись.

log: |
  свежая G5-проверка дала задаче `t-health-contracts` вердикт partial: строки 2
  и 3 наряда 044 устояли, строка 1 — нет; четырнадцать идентичностей, один файл
  на сущность, проверка полей и транспортная чистота подтверждены, но подписанная
  форма context_item переведена с потерями — у границы исчез писатель и огрублён
  читатель, а открытое окно актуальности заменено перечислением, которое не
  выражает интервалы, фазы и «до отмены»; три собственные подсадки покраснели,
  включая helper-контракт, поэтому названная автором helper-дыра опровергнута;
  буквальное «метаданные не язык» не устояло — контракты уже задают конечный
  декларативный автомат, а AST-гейт запрещает только несколько механизмов
  динамического Python; владелец словами «согласен с твоей рекомендацией»
  утвердил точную границу: конечный автомат допустим, расширяемый язык процедур,
  пользовательские условия и новые инструкции из данных запрещены; задача
  остаётся active, выдан готовый наряд переработки 046, оба дерева возвращены
  чистыми, HEAD продукта остался 5b46bd3 и ничего не запушено

next: |
  CALL c-solmax-zaratustra-health-contracts-rework-046
  to: session
  direction: solmax
  play: work
  node: g-zara-health-vertical
  bet: bet-g-zara-health-vertical
  task: t-health-contracts
  status: ready

  goal: |
    Исправить два опровергнутых места словаря: без потерь перевести девять
    свойств подписанного `context_item` и доказать утверждённую владельцем
    границу конечного декларативного автомата. Все устоявшие свойства коммита
    сохранить.

  context: |
    АБСОЛЮТНЫЕ ПУТИ. Искать ничего не надо.

    ПРОДУКТОВЫЙ РЕПОЗИТОРИЙ — работа только здесь:
    `C:\projects\zaratusta-product`.
    Ветка `feat/health-contracts`, исходный коммит
    `5b46bd3b007932fe4a12ea6040b5823df5add4c8`, родитель `30e436d`.
    Перед работой прочитать корневой `AGENTS.md` и `src/vocabulary/AGENTS.md`.
    Не пушить.

    РЕПОЗИТОРИЙ НАПРАВЛЕНИЯ — только читать:
    `C:\my_global_workflow_worktrees\solmax`.

    Источники:
    - `live/solmax/work/health-context-model-v1.md` — подписанная схема;
    - `live/solmax/cards/t-health-contracts.md` — актуальные строки задачи;
    - `live/solmax/history/2026-08-30-s-solmax-zaratustra-health-contracts-g5-045.md`
      — G5-улика с таблицей девяти свойств и выводами подсадок;
    - продуктовые `RESULT.md`,
      `docs/adr/0003-the-fourteen-contracts.md`,
      `src/vocabulary/context_item.py`, `_spec.py`, `_validate.py`,
      `tools/check.py`, `tools/selfcheck.py`, `tests/vocabulary/`.

    Утверждённый владельцем смысл границы, его ответ дословно:
    «согласен с твоей рекомендацией». Рекомендация, на которую он ответил:
    конечный декларативный автомат допустим; запрещены расширяемый язык
    процедур, пользовательские условия/выражения и новые инструкции из данных.

    G5 установила:
    1. `boundary_read` потерял пишущую половину подписанного свойства
       «кто читает и кто пишет»; `owner` её не заменяет.
    2. Три значения читателя огрубляют подписанные operation-specific границы.
    3. Закрытый `freshness` не выражает конкретный интервал, фазу, годы,
       «до отмены» и «до следующего принятого разбора».
    4. `workflow`/`step`/`transition` образуют допустимый конечный
       декларативный автомат. Нельзя продолжать заявлять, что языка вообще нет.
    5. Текущий `no-interpreter` доказывает лишь отсутствие нескольких средств
       динамического исполнения Python, а не закрытость инструкций автомата.

  boundaries: |
    - Не расширять задачу до реестра, объявленных call sites, runner или
      предметного Health-содержимого.
    - Сохранить ровно четырнадцать контрактов, их имена и versioned identities.
    - Сохранить один literal `CONTRACT = Contract(...)` на entity-файл,
      одинаковую шапку, нормальные и отвергаемые образцы.
    - Сохранить transport-free и domain-free границы.
    - `context_class` остаётся отдельным дискриминатором трёх классов сверх
      девяти свойств.
    - Каждое из девяти подписанных свойств должно иметь явное отображение.
      `граница` может раскладываться на read/write части, но ни одна часть не
      может исчезнуть или подменяться `owner`.
    - Не изобретать закрытый словарь там, где подписанный источник оставил
      свободную форму. `актуальность` обязана без потери нести все уже
      подписанные виды окон, но Health-значения в общий код не копируются.
    - Не доказывать поведение самописным поиском исходного текста.
      Лексический гейт может остаться только с узким честным названием того,
      что он действительно доказывает.
    - Новый или изменённый гейт обязан получить собственный seeded miss в
      `tools/selfcheck.py`.
    - Инструмент недоступен — СТОП и вопрос владельцу, без обходного пути.
    - Коммит локальный, не пушить.

  done_when: |
    1. `context_item` построчно соответствует девяти свойствам подписанного
       источника. В отдельном тесте/таблице названы все девять отображений.
       `граница` сохраняет отдельно читателя и писателя; `owner` остаётся
       отдельным свойством. Форма читателя/писателя не теряет ограничения уровня
       конкретной операции/процесса.
    2. `актуальность` представляет без потери как минимум пять структурных
       разновидностей уже подписанных карточек: конкретный интервал, фаза,
       длительность (`дни`/`годы`), до отмены/версии, до следующего события.
       Примеры нейтральные, без Health-словаря. Закрытое неполное перечисление
       удалено либо заменено формой, которая эти окна действительно несёт.
    3. Документация и ADR говорят правду: метаданные задают конечный
       декларативный автомат, но не расширяемый процедурный язык. Машинно
       доказано, что запись сценария не может добавить новый opcode,
       пользовательское условие/выражение, действие или способ исполнения:
       подсаженные неизвестная инструкция и undeclared condition/action
       краснят проверку. Из данных нельзя расширить закрытые наборы.
    4. Текущий запрет `eval|exec|compile|importlib|runpy`, если сохранён, назван
       узким доказательством отсутствия динамического исполнения, а не
       доказательством отсутствия любого интерпретатора. Довод «функция не
       интерпретатор, таблица интерпретатор» удалён или исправлен.
    5. Все устоявшие свойства не регрессировали: 14 versioned identities, один
       literal contract на entity-файл, оба вида образцов у каждого, четыре
       обязательных класса нарушений, ноль транспорта и предметного словаря.
       `uv run --locked python tools/check.py --deliver` и
       `uv run --locked python tools/selfcheck.py` зелёные на чистом дереве.
       В selfcheck есть новые подсадки для границы записи, представимости окна
       и закрытого набора инструкций.
    6. Результат закоммичен локально, не запушен. `t-health-contracts` остаётся
       active, а возврат открывает новый свежий физический G5-review; переход к
       `t-health-registry` до него запрещён.

  return: |
    Один RESULT плея `work`. В evidence:
    - локальный commit и родитель;
    - полный дословный вывод `--deliver` и `selfcheck`;
    - построчная таблица девяти свойств;
    - вывод новых seeded misses;
    - точная формулировка границы конечного автомата;
    - подтверждение чистого дерева и отсутствия push.
    Закрытие не светлое: верность исправленного перевода и границы языка снова
    требует свежего отдельного G5-чата.

  budget: |
    Одна сосредоточенная сессия, не больше полудня. Это переработка той же
    задачи, не новое дробление. Если точная форма окна или границы требует
    выбора смысла, которого нет в подписанном источнике, СТОП и вопрос
    владельцу — не изобретать.

END_OF_FILE: live/solmax/history/2026-08-30-s-solmax-zaratustra-health-contracts-g5-045.md
