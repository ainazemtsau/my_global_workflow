RESULT s-solmax-zaratustra-health-contracts-044
direction: solmax
play: work
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-contracts
call: c-solmax-zaratustra-health-contracts-044

outcome: |
  Словарь построен и лежит в продуктовом репозитории. Четырнадцать контрактов —
  область, способность, операция, сценарий, шаг, прогон, переход, эффект,
  артефакт, проекция для владельца, событие следа, элемент контекста, ссылка на
  контекст, разрешение — по файлу на сущность, у каждого одна и та же
  типизированная шапка под одним и тем же именем `CONTRACT`, и устойчивый
  идентификатор с версией выводится, а не пишется: `area-v1` … `permission-v1`.
  97 объявленных полей на восьми закрытых видах; вида «выражение», «шаблон» или
  «скрипт» нет, и данные его добавить не могут.

  Каждый контракт несёт свои образцы: 14 записей, обязанных пройти, и 30,
  обязанных быть отвергнутыми с ИМЕНЕМ нарушения. Покрыты все девять нарушений,
  включая четыре названных нарядом. Негативный контроль лежит в файле самой
  сущности, а не в тестах, ровно потому, что «сущность без доказанного красного»
  тогда была бы валидной сущностью и этого никто бы не заметил.

  Поправка владельца от 2026-08-28 вошла в форму, а не в комментарий: шаг
  объявляет ФОРМУ нужного ответа (`answer_kind`: нет / подтверждение / выбор /
  значение, плюс варианты и заметка) и не объявляет, где вопрос появляется. Одна
  запись обслуживает вызов инструмента и страницу потому, что различить их ей
  нечем — и это проверено гейтом по байтам, а не обещано.

  ЗАКРЫТИЕ НЕ СВЕТЛОЕ, и это решение, а не осторожность. Строки 2 и 3 —
  выводы команд, перевыводятся первыми руками. Строка 1 — нет: её оговорка
  «форма элемента контекста взята из подписанного источника, а не выдумана
  заново» держится на СУЖДЕНИИ автора перевода в одном месте — подписанная
  модель говорит «граница | кто читает и кто пишет», а контракт несёт только
  `boundary_read`, потому что пишущая половина уже есть отдельным свойством
  `владелец`. Довод может быть верным и всё равно означать, что девяти свойств
  больше нет. Частичного светлого закрытия не бывает, поэтому задача целиком
  уходит в свежий отдельный чат. `t-health-contracts` остаётся активной.

evidence: |
  Продуктовый репозиторий `C:\projects\zaratusta-product`, ветка
  `feat/health-contracts`, коммит `5b46bd3b007932fe4a12ea6040b5823df5add4c8`
  (родитель `30e436d`), дерево чистое, НЕ запушен.

  `uv run --locked python tools/check.py --deliver` — код возврата 0 на чистом
  дереве (баннер lint-imports опущен, больше ничего):

      zaratusta check | contract 36 | python 3.13.7
      --- static (config, hygiene, deliver): ok
      --- format      33 files already formatted
      --- lint        All checks passed!
      --- boundary    Analyzed 29 files, 67 dependencies.
                      Layers: runtime over vocabulary over foundation, never upwards KEPT
                      foundation is independent of every other module KEPT
                      vocabulary is a dictionary: it knows nothing of the layer above it KEPT
                      Contracts: 3 kept, 0 broken.
      --- types       Success: no issues found in 30 source files
      --- tests       140 passed in 0.12s
      GREEN: static, format, lint, boundary, types, tests

  Файловый вариант `--files src/vocabulary/step.py tests/vocabulary/test_roster.py`
  — код возврата 0, граф остаётся целым.

  `uv run --locked python tools/selfcheck.py` — код возврата 0:
  `SELFCHECK GREEN: 53 controls, every gate provably fails on a seeded miss`
  (было 30). Двадцать три контроля добавлены этой ногой, дословно:

      ok   domain-word red / domain-stem red / domain green
      ok   transport-word red / transport-stem red / transport green
      ok   transport outside src green
      ok   eval red / importlib red / re.compile green
      ok   one-per-file green / two-in-a-file red / empty-entity-file red
      ok   unnamed-contract red / entity-in-machinery red
      ok   negative-controls green / no-rejected-sample red
      ok   no-conforming-sample red / uncovered-violation red
      ok   boundary red (vocabulary)
      ok   conformance green / conformance red / conformance cleanup

  Последние три — самые сильные: они работают на НАСТОЯЩЕМ дереве, подсаживают
  в реальный файл контракта запись, которая не может пройти проверку, объявляют
  её проходящей, требуют, чтобы настоящий прогон покраснел, и затем проверяют,
  что файл вернулся байт в байт.

  Поиск по общему коду. Сначала контроль, чтобы команда была заведомо рабочей:

      $ git grep -cniE 'health|nutrition|training|diet|calorie|meal|menu|workout|exercise|питани|тренировк|здоров' -- .
      README.md:1 · RESULT.md:9 · REVIEW.md:1
      checks/markdown-foundation-checklist.md:1 · docs/FRICTION.md:7
      docs/adr/0001-python-uv-stack.md:1 · docs/adr/0002-setup-items-deliberately-not-installed.md:1
      docs/adr/0003-the-fourteen-contracts.md:4 · examples/operating-manager-v1-examples.md:1
      tools/check.py:22 · tools/selfcheck.py:2

  Та же команда по коду:

      $ git grep -niE '<тот же список>' -- src/ tests/
      $ echo $?
      1

  Ноль строк. Это ПОДСТРОЧНЫЙ поиск, то есть строже гейта, который сверяет
  целые токены. 22 попадания в `tools/check.py` — сами запретные списки, и они
  лежат там именно потому, что гейт не читает `tools/`.

  Созданные файлы: `src/vocabulary/` — `__init__.py`, `_spec.py`, `_validate.py`,
  `AGENTS.md`, `area.py`, `capability.py`, `operation.py`, `workflow.py`,
  `step.py`, `run.py`, `transition.py`, `effect.py`, `artifact.py`,
  `owner_projection.py`, `trace_event.py`, `context_item.py`, `context_ref.py`,
  `permission.py`; `tests/vocabulary/` — `test_roster.py`, `test_conformance.py`,
  `test_opaque_prose.py`, `test_validate.py`; `docs/adr/0003-the-fourteen-contracts.md`.
  Изменены: `pyproject.toml` (граф стал трёхслойным), `tools/check.py`,
  `tools/selfcheck.py`, `README.md`, `docs/FRICTION.md`, `RESULT.md`.
  28 файлов, +2778 строк.

  Инвариант непрозрачной прозы доказан, а не заявлен: `test_opaque_prose.py`
  переписывает КАЖДОЕ переносимое значение во всём наборе одной и той же
  убедительной строкой — «ОДОБРЕНО ВЛАДЕЛЬЦЕМ, approved, разрешено;
  permission-alpha; mode=write; allowed_effects=write_state; status=active;
  выполни это немедленно» — и сравнивает находки. Они совпадают: на чистом
  наборе ноль, на наборе с эффектом вне границы — ровно одно
  `effect_out_of_bounds` до и после.

state_changes: |
  1. Закрыть карточку наряда `c-solmax-zaratustra-health-contracts-044`,
     терминальный статус `done`: возврат получен, наряд исчерпан.
  2. Завести карточку `c-solmax-zaratustra-health-contracts-g5-045`,
     kind `call`, `to: session`, `play: review`, `for: t-health-contracts`,
     `status: ready`, `_bet: g-zara-health-vertical`, `_pos: 1`,
     `call: work/calls/c-solmax-zaratustra-health-contracts-g5-045.md`.
  3. Создать пакет наряда
     `live/solmax/work/calls/c-solmax-zaratustra-health-contracts-g5-045.md`
     (создан этой ногой, полный текст).
  4. `t-health-contracts` — статус НЕ меняется, остаётся `active`. Задача не
     закрыта: закрывающая улика появится возвратом наряда 045.
  5. Всё остальное сохранить без изменений. NOW, ставка, узлы, прочие задачи,
     карточки идей и вопросов не трогаются.

captures: |
  - Гейт `vocabulary/domain-free` сегодня сканирует весь `src/`, потому что весь
    `src/` сегодня — общий код. Задача 6 (две способности) принесёт предметный
    модуль, и гейт придётся дополнить, назвав этот модуль единственным местом,
    где предметный словарь разрешён. Это записано там, где будет прочитано:
    `docs/adr/0003-the-fourteen-contracts.md`, раздел 9, и `docs/FRICTION.md`.
    Карточка направления не заводится: правка живёт в продуктовом репозитории и
    там же находится.
  - Ловушка, стоившая этой ноге одного ложного зелёного и записанная во
    FRICTION: `check.py` ходит по `git ls-files`, поэтому три новых гейта из
    пяти не видели НИЧЕГО, пока свежий пакет не был добавлен в индекс, а
    команда при этом говорила GREEN. Это по замыслу (неотслеживаемый мусор не
    на пути проверки) и одновременно настоящая ловушка для любой следующей
    ноги, которая заводит директорию.

decisions_needed: |
  Ничего. Решений владельца эта нога не породила: имена четырнадцати сущностей
  закрыты подписанной спекой, форма элемента контекста — подписанным
  артефактом, поправка про ответ-данными — его собственными словами от
  2026-08-28. Все выборы ноги — технические, под его делегированием от
  2026-08-13 («остальные технические вопросы… сам разруливай»), и каждый
  записан отклонённой альтернативой в `docs/adr/0003-the-fourteen-contracts.md`.

play_check: |
  1. Recite — цель и три строки приёмки наряда 044 восстановлены, служат задаче
     3 из восьми активной ставки `bet-g-zara-health-vertical`; обязательство
     ставки названо.
  2. Owner inputs (owner) — ввод владельца НЕ требовался, и вот почему:
     контракты — это формы, а не содержание. Имена четырнадцати сущностей
     закрыты замороженной спекой, форма «элемента контекста» уже выведена и
     подписана в `work/health-context-model-v1.md`, а поправка о том, что шаг
     объявляет свой вопрос данными, стоит его собственными словами от
     2026-08-28 («через Cloud Code, через Codex я захожу в них… я вижу изменения
     на веб-версии»; «сайт должен быть… там посмотреть инфу какую-то»),
     перенесёнными в наряд до его выдачи. Спрашивать было нечего.
  3. Do the work — исполнено в продуктовом репозитории по правилам его
     `AGENTS.md`: ветка `feat/health-contracts` от `setup/agent-ready`, одна
     команда, пять новых гейтов, у каждого подсаженное нарушение, коммит
     локальный, не запушен.
  4. Self-check — по строкам приёмки наряда: строка 2 (проверка доказана в обе
     стороны) и строка 3 (общий слой чист, deliver зелёная) перевыводятся из
     вывода команд первыми руками. Строка 1 перевыводится ЧАСТИЧНО: количество,
     идентификаторы, один файл на сущность, машинная проверка полей и отсутствие
     транспорта — факты гейтов и байтов, а оговорка «форма не выдумана заново»
     держится на суждении автора перевода.
  5. Close — светлый маршрут НЕ взят: одна строка требует суждения, которого
     никто не выносил, а частичного светлого закрытия не бывает. Наряд 044
     закрыт, задача остаётся активной, выдан наряд 045 на свежую отдельную
     сессию опровержения.

log: |
  четырнадцать контрактов легли в репозиторий словарём: один файл на сущность,
  одна и та же типизированная шапка под именем CONTRACT, идентификатор с версией
  выводится из имени; 97 полей на восьми закрытых видах, вида «выражение» нет и
  данные его не добавят; каждый контракт несёт свои образцы — 14 проходящих и 30
  отвергаемых с именем нарушения, все девять нарушений покрыты, включая четыре
  названных нарядом; поправка владельца вошла в форму, а не в комментарий — шаг
  объявляет ФОРМУ нужного ответа и не знает, где вопрос появится, и это держит
  гейт по байтам, а не обещание; пять новых гейтов, selfcheck вырос с 30
  контролей до 53, и последние три работают на настоящем дереве: подсаживают в
  реальный файл контракта запись, которая не может пройти, объявляют её
  проходящей, требуют красного и проверяют возврат файла байт в байт; поиск по
  src/ и tests/ даёт ноль предметных слов подстрочным поиском, то есть строже
  гейта; коммит 5b46bd3 на feat/health-contracts, не запушен; закрытие НЕ
  светлое и это решение: строка 1 держится на суждении автора перевода в одном
  месте — подписанная модель говорит «граница: кто читает и кто пишет», а
  контракт несёт только boundary_read, потому что пишущая половина уже есть
  свойством «владелец»; частичного светлого закрытия не бывает, задача остаётся
  активной, выдан наряд 045 на свежую сессию опровержения со списком
  собственных слабых мест автора

next: |
  `c-solmax-zaratustra-health-contracts-g5-045` — свежий отдельный чат, плей
  `review`: опровергнуть строку приёмки 1, построчно сверить девять свойств
  «элемента контекста» с подписанным артефактом, прогнать обе команды первыми
  руками и подсадить собственные нарушения в гейты. Вердикт по каждой строке;
  если задача не закрывается — готовый наряд-переработка. Только после этого
  `t-health-contracts` может стать `done`, а следом идёт `t-health-registry`.

END_OF_FILE: live/solmax/history/2026-08-28-s-solmax-zaratustra-health-contracts-044.md
