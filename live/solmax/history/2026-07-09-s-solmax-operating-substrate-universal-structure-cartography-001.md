RESULT s-solmax-operating-substrate-universal-structure-cartography-001 (call: c-solmax-operating-substrate-universal-structure-cartography-001)
direction: solmax   play: local/architecture-cartography   node/task: g-operating-substrate/c-solmax-operating-substrate-universal-structure-cartography-001

outcome: |
  Owner-approved-with-steering working question graph exists for the universal
  adaptive operating-substrate process-structure cluster.

  The owner approved the question map as a working graph and added a required
  route correction: the graph must explicitly support recursive architecture
  decomposition. Answering Q1/Q2 is expected to create new architectural entities
  and child questions; this is normal graph growth, not automatically failure.
  The map is therefore amended so Q15 becomes "graph growth / recursive
  decomposition / gap rebalance rule", and the next Q1 architecture-forge handoff
  must classify spawned entities/questions instead of trying to answer everything
  in one card.

  No substrate architecture answer is frozen. No implementation, repo/backend,
  carrier, proof harness, Direction OS kernel change or fixed service-block list
  is activated.

evidence: |
  Owner approval / steering evidence:
  - "я с вопросами согласен"
  - "будут возникать новые вопросы"
  - "за одним вопросом последуют другие, особенно при ответе на первое"
  - "мы определим какую-то сущность, которая тоже должна быть отдельно определена"
  - "мы должны быть готовы к такому"
  - "процесс ... будет с этим нормально работать"

  Interpretation:
  - status: approved_as_working_graph_with_recursive_growth_steering
  - The owner approves the questions, but requires that the process not treat the
    graph as a closed finite checklist. The route must support new questions,
    child entities and subgraphs emerging from architecture-forge answers,
    especially q01_core_invariant.

  Source locks preserved:
  - live/solmax/work/operating-substrate/cards/operating-substrate-scope-boundary-001.md:
    accepted scope/boundary; concrete structure, state/history/procedure/source/
    evidence/test/repo/backend/carrier not frozen.
  - live/solmax/history/2026-07-09-c-solmax-operating-substrate-structure-discovery-architecture-forge-001.md:
    discovery-loop primary answer rejected; rough block sketch not accepted as truth.
  - live/solmax/plays/architecture-forge.md:
    hidden prerequisites / unclear answer shape / owner correction must checkpoint
    gap_event and return to cartography rather than force-freezing architecture.
  - live/solmax/plays/architecture-cartography.md:
    graph maps questions and routes forge; no graph is accepted without owner words.

  Working cartography artifact content to create/update:
  - See state_changes item 1.
  - Artifact status: owner_approved_working_graph_with_recursive_growth_steering.
  - The artifact is a question map only, not an architecture card.

state_changes: |
  1. Add work artifact:
     file: live/solmax/work/operating-substrate-universal-structure-cartography-001.md
     action: create
     content: |
       # Operating-substrate architecture/spec cartography — universal adaptive process structure cluster

       status: owner_approved_working_graph_with_recursive_growth_steering
       session: s-solmax-operating-substrate-universal-structure-cartography-001
       call: c-solmax-operating-substrate-universal-structure-cartography-001
       cluster_id: os-substrate-universal-structure-001

       ## Owner sign

       Owner approved the question map as a working graph, with steering.

       Owner words:
       - "я с вопросами согласен"
       - "будут возникать новые вопросы"
       - "за одним вопросом последуют другие, особенно при ответе на первое"
       - "мы определим какую-то сущность, которая тоже должна быть отдельно определена"
       - "мы должны быть готовы к такому"
       - "процесс ... будет с этим нормально работать"

       Interpretation:
       The graph is approved for forward movement, but it must be treated as a
       living architecture frontier, not a closed finite checklist. Q1/Q2 are
       expected to spawn new entities, child questions and possible subgraphs.
       That graph growth is a normal successful outcome unless it reveals a hard
       prerequisite that prevents the current card from being answered.

       ## Non-freeze note

       This artifact maps questions. It does not answer or freeze the universal
       process structure, service block list, implementation route, repo/backend,
       carrier, proof harness, Direction OS kernel changes or consumer product
       behavior. The previous assistant sketch is explicitly treated as
       non-authority.

       ## Frame

       Cluster name:
       Universal adaptive operating-substrate process structure.

       Anchor question:
       What universal adaptive process structure / process pack should
       operating-substrate provide so that new custom AI-processes can reuse
       common service layers without copying any one consumer's architecture or
       reinventing persistence, history, routing, procedures, context, results,
       gates and communication from scratch?

       Why cartography is needed:
       The previous forge surfaced a wrong answer shape. A discovery loop alone
       would make every new process re-decide common service concerns from scratch.
       A fixed block list would prematurely freeze unproven structure. The route
       needs a graph that separates core invariant, service-zone questions,
       configuration/adaptation, communication, proof and carrier boundaries before
       any architecture card is forged.

       ## Inventory

       Existing decisions:
       - Solmax is an umbrella direction.
       - Zaratusta is the first product/application branch and first evidence/failure source.
       - Operating-substrate is a separate sibling architecture/spec route.
       - Scope/boundary card is accepted.
       - Operating-substrate is for abstract customizable AI-process construction.
       - The frozen parent invariant is a reusable way to design, reason about,
         test and evolve custom AI-processes without making one consumer's
         structure authoritative.
       - Concrete process structure, state/history/procedure/source/evidence/test/
         repo/backend/carrier are not frozen.
       - Current architecture mode is owner-led cartography, then one
         architecture-forge card at a time.

       Partial evidence:
       - Zaratusta live trial shows surface completeness and handback artifacts are
         not sufficient evidence of live owner-facing capability.
       - Direction OS shows useful practices: one-session/one-job, durable state,
         CALL/RESULT, gates, history and writer separation; these are evidence only.
       - Health work shows pressure for strong domain-specific policy, ownership
         boundaries, non-blocking questions and safety gates; it is an example
         pressure source, not substrate authority.
       - Indie-game canon cartography shows graph-first route, plain questions,
         dependencies, proof/visual anchors, gap rule and owner sign discipline;
         it is process analogy only.
       - CASC/Confluence direct artifacts were searched for but not found in the
         available repo search; they require owner-supplied artifacts before use.

       Rejected alternatives:
       - Discovery loop as primary substrate answer.
       - Freeze previous assistant block list.
       - Implement from rough sketch.
       - Copy Direction OS / Zaratusta / Health / canon map.
       - Pick GitHub/Markdown/repo/backend now.
       - Start with proof harness/test plan.
       - Treat the question graph as a closed finite checklist.

       Known non-authorities:
       - Previous assistant block sketch.
       - Direction OS kernel/play/CALL/RESULT mechanics.
       - Current Zaratusta artifacts and contracts.
       - Health converge artifacts.
       - Indie-game canon cartography/forge artifacts.
       - Any future Confluence/CASC/Game artifacts unless separately accepted as
         evidence anchors.
       - This cartography artifact itself as architecture answer.

       ## Graph nodes

       ### Q1 — Core invariant

       Plain question:
       Какая core invariant делает operating-substrate универсальной адаптивной
       структурой процесса?

       Why it matters:
       Without this, every later service-block discussion either becomes a fixed
       component list, a vague discovery method, or a copy of a known consumer.

       Answer shape:
       invariant.

       Status:
       ready.

       Dependency type:
       hard_prerequisite.

       Blocks:
       - Q2 service-zone rule.
       - Q3 block relationships.
       - Q4 configuration/domain adaptation.
       - Q13 proof examples/evaluation.

       Do not solve here:
       Do not enumerate the final service block list, define schemas, choose
       carrier/repo/backend, create process syntax, or settle consumer-specific
       policies.

       Needs research:
       comparable_systems.

       Needs anchor:
       source_artifact.

       Confusion traps:
       - Calling a discovery loop the invariant again.
       - Treating persistence/history/procedures as already accepted architecture.
       - Using Direction OS as the invariant because it currently works.
       - Making the invariant so abstract that no proof scenario can refute it.

       Forge handoff:
       - plain_question: Какая core invariant делает operating-substrate универсальной адаптивной структурой процесса?
       - why_now: This is the hard prerequisite before service blocks, relationships, adaptation and proof can be forged.
       - must_decide: The invariant / kind of object: process pack, service-layer skeleton, adaptive process kernel, or another formulation.
       - must_not_decide: Final block list, schemas, procedures, repo/backend, carrier, implementation, proof harness.
       - parent_locks: Scope/boundary card accepted; concrete structure not frozen; examples evidence not authority; previous sketch non-authority.
       - expected_answer_shape: invariant.
       - first_owner_question: Когда ты говоришь "должна быть структура", что в ней обязано оставаться тем же самым при смене domain/process?
       - recursive_growth_requirement: If Q1 introduces new architectural entities, the card must classify each as inline minimal definition, child question, hidden prerequisite, downstream elaboration, proof question, consumer-specific detail, or rejected premature freeze. Returning new questions is success unless they are hard prerequisites that prevent Q1 from being answered.
       - return_to_graph_if: The answer depends on one consumer, collapses into discovery loop, requires naming fixed blocks before the invariant exists, or spawns a hard prerequisite that must be mapped first.

       ### Q2 — Service-zone rule

       Plain question:
       Какие service zones могут быть reusable structure, и по какому правилу
       candidate block becomes substrate-level?

       Why it matters:
       The owner named persistence, history, routing, procedures, context, results,
       gates and communication as common service concerns. They must be
       investigated without treating that rough list as accepted architecture.

       Answer shape:
       taxonomy.

       Status:
       ready after or alongside Q1.

       Dependency type:
       co_frame.

       Blocks:
       - Q6 persistence/history.
       - Q7 routing/procedure.
       - Q9 result/handback.
       - Q10 authority/gates.
       - Q12 communication.

       Do not solve here:
       Do not freeze that the named blocks are mandatory components. Decide only
       the classification rule, vocabulary and promotion criteria for candidate
       service zones.

       Needs research:
       best_practice.

       Needs anchor:
       diagram.

       Confusion traps:
       - Treating every repeated thing as a substrate block.
       - Treating every optional thing as consumer-specific.
       - Confusing service zone with implementation module.
       - Freezing the owner's rough examples as canonical list.

       Forge handoff:
       - plain_question: Какие service zones могут быть reusable structure, и по какому правилу candidate block becomes substrate-level?
       - why_now: Ready after Q1; it prevents premature block-list freeze while preserving common-service pressure.
       - must_decide: Service-zone definition, candidate/promote/defer criteria, and what evidence is needed to promote a zone.
       - must_not_decide: Full final block list, schemas, APIs, implementation packages, exact process syntax.
       - recursive_growth_requirement: If Q2 turns a rough service concern into several separable questions, classify them as child questions or downstream block-specific cards rather than solving all blocks in Q2.
       - return_to_graph_if: The rule cannot classify at least persistence, live interface and domain-specific safety policy without contradiction.

       ### Q3 — Service-block relationships

       Plain question:
       Как candidate service zones communicate end-to-end inside one process?

       Why it matters:
       A pile of blocks is not a structure. The relationship model decides whether
       process flow, state access, procedure selection, live response, result
       persistence and evolution cohere.

       Answer shape:
       call_protocol.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q8 live interface.
       - Q9 result/handback.
       - Q12 communication/multi-agent model.

       Do not solve here:
       Do not design APIs, message schemas, orchestration engine, scheduler,
       queue, graph runtime or concrete transport.

       Needs research:
       best_practice.

       Needs anchor:
       diagram.

       Confusion traps:
       - Drawing implementation architecture instead of responsibility flow.
       - Making every block talk to every block.
       - Hiding owner-facing behavior behind internal bureaucracy.
       - Copying CALL/RESULT as universal communication model.

       Forge handoff:
       - plain_question: Как candidate service zones communicate end-to-end inside one process?
       - why_now: After Q1/Q2; relationships only make sense once the structural kind and zone rule are known.
       - must_decide: Responsibility-flow model and allowed relationship types between zones.
       - must_not_decide: Transport, schema, tooling, event bus, repo layout, runtime execution plan.
       - return_to_graph_if: The answer becomes a tool architecture or requires fixing the service-zone list first.

       ### Q4 — Configuration / domain adaptation

       Plain question:
       Как новый domain/process instantiates the universal structure without
       reinventing common service layers?

       Why it matters:
       This is the adaptation problem. If each consumer rewrites the structure,
       substrate fails; if the substrate dictates local policy, it overreaches.

       Answer shape:
       scenario_grammar.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q5 shared owner context.
       - Q10 authority/gates.
       - Q13 proof examples.

       Do not solve here:
       Do not define actual Health/Zaratusta/game configs, domain schemas, UI,
       product behavior, exact safety policies, setup wizard or config format.

       Needs research:
       comparable_systems.

       Needs anchor:
       scenario.

       Confusion traps:
       - Calling consumer policy "configuration" and smuggling it into substrate.
       - Making every process start with blank discovery.
       - Turning the process pack into a rigid product template.

       Forge handoff:
       - plain_question: Как новый domain/process instantiates the universal structure without reinventing common service layers?
       - why_now: After Q1/Q2; this is the main adaptation card before consumer-specific examples can prove the structure.
       - must_decide: What is configured, inherited, locally owned and checked at instantiation.
       - must_not_decide: Actual configs for Zaratusta/Health/game, storage format, setup UI, product policy.
       - return_to_graph_if: The answer cannot handle at least two different example domains without copying one into the other.

       ### Q5 — Shared owner context

       Plain question:
       Что может быть shared owner context across processes, and what must stay local?

       Why it matters:
       Multiple processes may need owner-wide facts, preferences, resources or
       constraints. A shared context layer could reduce duplication, but risks
       privacy leakage, stale assumptions and accidental authority.

       Answer shape:
       state_model.

       Status:
       downstream.

       Dependency type:
       owner_policy.

       Blocks:
       - Q6 persistence/history.
       - Q8 live interface.
       - Q10 authority/gates.

       Do not solve here:
       Do not select a shared repo/source, define data schema, import personal
       facts, choose privacy tech, or decide actual owner preferences.

       Needs research:
       failure_cases.

       Needs anchor:
       scenario.

       Confusion traps:
       - Treating shared context as a global dump of all owner data.
       - Letting one process's local assumption become owner-wide truth.
       - Confusing memory, source, state and evidence.
       - Choosing a repository before authority/provenance is defined.

       Forge handoff:
       - plain_question: Что может быть shared owner context across processes, and what must stay local?
       - why_now: After Q4; shared context is an adaptation and authority boundary, not a repo choice.
       - must_decide: Categories of shareable context, local-only context, provenance and promotion/retirement rules.
       - must_not_decide: Actual shared repo, schemas, indexing, tools, personal fact content.
       - return_to_graph_if: The answer becomes a storage design or cannot prevent local facts from becoming global authority.

       ### Q6 — Persistence/history model

       Plain question:
       Какие persistence/history responsibilities are substrate-level, and what
       remains consumer-specific?

       Why it matters:
       Durable continuity is likely central to stateful processes, but
       persistence/history can mean event log, current state, audit, decision
       history, evidence ledger, recovery snapshots or context summaries.

       Answer shape:
       state_model.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q7 routing/procedure.
       - Q9 result/handback.
       - Q11 evidence/evolution.
       - Q13 proof examples.

       Do not solve here:
       Do not choose database, Markdown, files, event store, schema, fields,
       retention policy, sync, backup or exact state surfaces.

       Needs research:
       best_practice.

       Needs anchor:
       scenario.

       Confusion traps:
       - Assuming durable state equals every context token.
       - Copying Direction OS LOG/history/NOW as substrate.
       - Copying Zaratusta workspace surfaces as substrate.
       - Choosing storage/HOW while asking responsibility/WHAT.

       Forge handoff:
       - plain_question: Какие persistence/history responsibilities are substrate-level, and what remains consumer-specific?
       - why_now: After Q1/Q2/Q4; it is a major service-zone candidate and proof dependency.
       - must_decide: Responsibility categories: continuity, audit, evidence, recovery, current state, decision history, provenance.
       - must_not_decide: Storage technology, schemas, repo layout, exact field names, migration plan.
       - return_to_graph_if: The answer depends on one consumer's file layout or storage implementation.

       ### Q7 — Routing/procedure model

       Plain question:
       Как process routes intake to procedures, and what is reusable procedure
       structure vs local play/process?

       Why it matters:
       Routing/procedure is where an AI-led process stops being a generic chat.
       It must decide what kind of work is happening, which rule/procedure applies,
       what context is needed, and when to escalate.

       Answer shape:
       procedure_model.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q8 live interface.
       - Q9 result/handback.
       - Q10 authority/gates.
       - Q12 communication/multi-agent model.

       Do not solve here:
       Do not define a universal play catalogue, exact process syntax, scheduler,
       automation, router algorithm, or copy Direction OS plays.

       Needs research:
       comparable_systems.

       Needs anchor:
       diagram.

       Confusion traps:
       - Treating Direction OS play names as universal.
       - Making routing a classifier implementation rather than architecture responsibility.
       - Letting every local process invent routing from scratch.
       - Ignoring uncertainty and owner-steer paths.

       Forge handoff:
       - plain_question: Как process routes intake to procedures, and what is reusable procedure structure vs local play/process?
       - why_now: After service-zone rule and persistence/context responsibilities are framed.
       - must_decide: Reusable procedure/routing responsibilities and local-consumer boundary.
       - must_not_decide: Specific play list, syntax, implementation router, tool/provider routing.
       - return_to_graph_if: The answer requires choosing concrete Direction OS or Zaratusta procedures.

       ### Q8 — Live interface model

       Plain question:
       Как live owner interface uses the internal structure without becoming
       bureaucracy?

       Why it matters:
       The Zaratusta failure showed that clean internal surfaces can still fail in
       live behavior. The structure needs an interface boundary so service layers
       support useful owner-facing operation instead of replacing it.

       Answer shape:
       interface_model.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q9 result/handback.
       - Q10 authority/gates.
       - Q13 proof examples.

       Do not solve here:
       Do not design UI, message templates, prompt format, voice/chat behavior,
       exact report layout or product experience.

       Needs research:
       failure_cases.

       Needs anchor:
       owner_transcript.

       Confusion traps:
       - Equating complete handback with useful operation.
       - Making the owner read internal audit to get help.
       - Hiding uncertainty/evidence entirely.
       - Solving UX instead of interface responsibility boundary.

       Forge handoff:
       - plain_question: Как live owner interface uses the internal structure without becoming bureaucracy?
       - why_now: After persistence/procedure responsibilities; before any claim of live process readiness.
       - must_decide: Boundary between owner-facing behavior, internal evidence, audit and handback.
       - must_not_decide: Exact UI/prompt/report, product-specific interaction rhythm.
       - return_to_graph_if: The answer becomes UI design or ignores the live-use failure.

       ### Q9 — Result/handback model

       Plain question:
       Что is the reusable result/handback contract at process boundaries?

       Why it matters:
       Stateful processes must end or hand off work without losing decisions,
       evidence, next actions, unresolved questions and context. Direction OS
       RESULT itself is not automatically substrate.

       Answer shape:
       call_protocol.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q6 persistence/history.
       - Q11 evidence/evolution.
       - Q12 communication/multi-agent model.
       - Q14 artifact/carrier boundary.

       Do not solve here:
       Do not copy RESULT schema, define exact packet format, markdown template,
       transport or writer mechanism.

       Needs research:
       best_practice.

       Needs anchor:
       source_artifact.

       Confusion traps:
       - Copying Direction OS RESULT literally.
       - Returning a narrative instead of machine-actionable outcome/evidence.
       - Making handback so heavy that live usefulness collapses.
       - Forgetting unresolved decisions/gaps.

       Forge handoff:
       - plain_question: Что is the reusable result/handback contract at process boundaries?
       - why_now: After procedure/routing and interface boundary; it links live work to persistence/history/evolution.
       - must_decide: Reusable handback responsibilities and minimal outcome/evidence/gap/next semantics.
       - must_not_decide: Exact packet schema, file format, repo, writer tool, transport.
       - return_to_graph_if: The answer becomes a copy of Direction OS packets or a carrier-specific template.

       ### Q10 — Authority/gate model

       Plain question:
       Какие authority, approval and evidence gates are substrate-level, and which
       are domain-specific?

       Why it matters:
       The owner decides high-impact freezes and side effects. A reusable process
       structure needs gate categories without copying Direction OS G9 or hardcoding
       Health/Zaratusta-specific rules.

       Answer shape:
       evaluation_rubric.

       Status:
       downstream.

       Dependency type:
       owner_policy.

       Blocks:
       - Q8 live interface.
       - Q11 evidence/evolution.
       - Q13 proof examples.

       Do not solve here:
       Do not define all effect tiers, legal/medical/product-specific rules,
       exact owner approval language, or copy Direction OS hard gates as universal.

       Needs research:
       failure_cases.

       Needs anchor:
       owner_transcript.

       Confusion traps:
       - Letting AI silently freeze architecture or process changes.
       - Asking owner for every uncertainty because defaults are absent.
       - Treating local safety policy as substrate invariant.
       - Copying G9 without proving applicability.

       Forge handoff:
       - plain_question: Какие authority, approval and evidence gates are substrate-level, and which are domain-specific?
       - why_now: After core/service-zone framing; before evolution/proof can be trusted.
       - must_decide: Reusable gate categories: architecture freeze, process mutation, irreversible/external action, evidence sufficiency, owner decision.
       - must_not_decide: All domain policies, exact approval packet, medical/safety rules, product permissions.
       - return_to_graph_if: The answer becomes full governance for one product or cannot prevent silent architecture freeze.

       ### Q11 — Evidence/evolution model

       Plain question:
       Как process evolves procedures/configuration from evidence without hidden
       self-mutation?

       Why it matters:
       Adaptive structure must improve. But if it mutates silently, it becomes
       untrustworthy; if every change requires redesign, it becomes a perpetual draft.

       Answer shape:
       procedure_model.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q13 proof examples.
       - Q15 graph growth / recursive decomposition / gap rebalance.
       - implementation_readiness.

       Do not solve here:
       Do not design self-modifying prompts, version control workflow, automated
       experiments, review cadence, exact mutation syntax or implementation tool.

       Needs research:
       evaluation_methods.

       Needs anchor:
       prototype_trace.

       Confusion traps:
       - Hidden prompt edits.
       - Treating every failure as architecture churn.
       - Capturing ideas and acting on them in the same session.
       - Conflating evidence review with implementation deployment.

       Forge handoff:
       - plain_question: Как process evolves procedures/configuration from evidence without hidden self-mutation?
       - why_now: After gate, handback and persistence responsibilities are understood.
       - must_decide: Evidence-to-change loop, accepted/rejected change record, owner-gated mutation categories and rollback/gap behavior.
       - must_not_decide: Tooling, repo workflow, exact versioning, scheduler, automation.
       - return_to_graph_if: The answer hides mutation or requires implementation details.

       ### Q12 — Communication / multi-agent model

       Plain question:
       Как processes, service zones and agents communicate without baking in current
       providers/tools?

       Why it matters:
       The owner works through ChatGPT, Codex, Claude and other agents, but tool
       capabilities change. The architecture needs stable communication
       responsibilities and adapter boundaries.

       Answer shape:
       call_protocol.

       Status:
       downstream.

       Dependency type:
       downstream.

       Blocks:
       - Q14 artifact/carrier boundary.
       - implementation_readiness.
       - Q13 proof examples.

       Do not solve here:
       Do not choose provider, API, CLI, routing engine, cost model, connector,
       queue, scheduler or exact adapter interface.

       Needs research:
       tool_capability.

       Needs anchor:
       prototype_trace.

       Confusion traps:
       - Making ChatGPT/Codex/Claude current behavior into architecture law.
       - Mixing process architecture with agent orchestration implementation.
       - Ignoring human-owner communication as part of the protocol.

       Forge handoff:
       - plain_question: Как processes, service zones and agents communicate without baking in current providers/tools?
       - why_now: After relationship, routing and handback semantics; before carrier/implementation.
       - must_decide: Stable communication roles, adapter boundary and provider-independent handoff principles.
       - must_not_decide: Provider choice, API/CLI, routing engine, scheduling, budget mechanism.
       - return_to_graph_if: The answer depends on today's tool feature or selects an orchestration implementation.

       ### Q13 — Proof examples / evaluation

       Plain question:
       Какие proof examples and evaluation anchors are required to check that the
       structure works?

       Why it matters:
       The structure must be checked across different process pressures. Zaratusta
       failure proves artifact completeness is not enough; proof must include live
       behavior, continuity, adaptation and domain differences.

       Answer shape:
       proof.

       Status:
       research_first.

       Dependency type:
       proof_downstream.

       Blocks:
       - architecture_card_gate.
       - implementation_readiness.
       - Q14 artifact/carrier boundary.

       Do not solve here:
       Do not create a detailed test plan, benchmark, dataset, test harness, repo,
       provider evaluation plan or automation.

       Needs research:
       evaluation_methods.

       Needs anchor:
       external_case.

       Confusion traps:
       - Treating Markdown completeness as proof.
       - Using only Zaratusta and overfitting structure to it.
       - Designing tests before the architecture claims exist.
       - Treating proof examples as consumer authority.

       Forge handoff:
       - plain_question: Какие proof examples and evaluation anchors are required to check that the structure works?
       - why_now: Research-first after Q1/Q2; proof pressure must be present before architecture hardens.
       - must_decide: Proof categories and representative examples: Zaratusta reboot, Health-like, game/productive-play, Direction-OS-like, and arbitrary future process.
       - must_not_decide: Detailed harness, exact scripts, provider benchmark, implementation repo.
       - return_to_graph_if: Proof needs reveal missing state/interface/source/gate questions.

       ### Q14 — Artifact/carrier boundary

       Plain question:
       Как represent structure/cards now without freezing GitHub/Markdown/repo/backend
       as architecture?

       Why it matters:
       Current work lives in Direction OS state/work artifacts, but the architecture
       must not confuse carrier convenience with substrate truth. Repo/backend/product
       placement is downstream.

       Answer shape:
       repo_boundary.

       Status:
       downstream.

       Dependency type:
       implementation_downstream.

       Blocks:
       - implementation_bet_activation.
       - source_authority.

       Do not solve here:
       Do not select/create a repo, backend, storage engine, URL service, runtime,
       schema, package, deploy target or product boundary.

       Needs research:
       none.

       Needs anchor:
       source_artifact.

       Confusion traps:
       - Treating GitHub/Markdown as the architecture.
       - Moving substrate under Zaratusta for convenience.
       - Letting draft work artifacts become accepted specs.
       - Choosing carrier to feel like progress.

       Forge handoff:
       - plain_question: Как represent structure/cards now without freezing GitHub/Markdown/repo/backend as architecture?
       - why_now: Only after core/service/proof responsibilities create real carrier pressure.
       - must_decide: Artifact status model, when carrier/repo becomes a legitimate question, and what must remain carrier-neutral.
       - must_not_decide: Actual repo/backend/storage/runtime/file hierarchy.
       - return_to_graph_if: The answer chooses a carrier instead of defining the boundary.

       ### Q15 — Graph growth / recursive decomposition / gap rebalance rule

       Plain question:
       Как graph grows and rebalances when forge answers create new entities, child
       questions, hidden prerequisites or wrong answer shapes?

       Why it matters:
       The owner explicitly expects Q1/Q2 to spawn new questions. The route must not
       treat the top-level map as a closed checklist, but it also must not allow
       infinite uncontrolled branching.

       Answer shape:
       evaluation_rubric.

       Status:
       ready.

       Dependency type:
       co_frame.

       Blocks:
       - future_forge_order.
       - Q11 evidence/evolution.
       - owner_trust.

       Do not solve here:
       Do not write a full planning methodology, implementation roadmap, active bet
       plan, or new Direction OS procedure.

       Needs research:
       failure_cases.

       Needs anchor:
       owner_transcript.

       Confusion traps:
       - Treating every new question as failure.
       - Treating every new question as active graph growth.
       - Letting forge invent architecture when a prerequisite is missing.
       - Rebalancing too often and never freezing any card.
       - Ignoring owner correction because the graph exists.
       - Treating Q1-Q15 as a linear checklist that will finish the architecture.

       Graph growth rule:
       Every architecture-forge card must return any spawned entities/questions and
       classify each as:
       - inline minimal definition;
       - child question;
       - hidden prerequisite;
       - downstream elaboration;
       - proof question;
       - consumer-specific detail;
       - rejected premature freeze;
       - capture for later.

       A new question is added to the graph only if it:
       - blocks the next forge;
       - changes the meaning/order of existing nodes;
       - is needed for proof;
       - prevents premature freeze;
       - separates substrate from consumer-specific;
       - records owner correction;
       - defines a new entity without which a card would remain ambiguous.

       Top-level graph rule:
       The top-level graph remains capped at 15 major nodes. If Q1/Q2 spawn many
       questions, create child questions under the relevant node, merge/split
       existing nodes, or route to a subcluster cartography pass rather than adding
       unlimited top-level nodes.

       Forge handoff:
       - plain_question: Как graph grows and rebalances when forge answers create new entities, child questions, hidden prerequisites or wrong answer shapes?
       - why_now: The owner approved the questions with explicit steering that Q1/Q2 will spawn further questions and the process must handle that normally.
       - must_decide: Return-to-cartography triggers, child-question classification, top-level graph cap behavior, and when graph growth is success vs gap.
       - must_not_decide: Full roadmap, implementation tasks, OS kernel/play changes.
       - parent_locks: local/architecture-forge already requires gap_event on hidden prerequisite; this map adds normal recursive graph-growth handling.
       - expected_answer_shape: evaluation_rubric.
       - first_owner_question: When should a new entity/question be handled inline, spawned as child, or returned to cartography as hidden prerequisite?
       - return_to_graph_if: The rebalance rule becomes too heavy, changes the play/kernel, or makes every term into a blocking new card.

       ## Dependency edges

       - Q1 -> Q2 because service zones can only be classified after the invariant says what stays stable across domains.
       - Q1 -> Q4 because adaptation needs a stable core-vs-variable boundary.
       - Q1 -> Q15 because the first invariant will likely spawn new entities and the graph must classify them.
       - Q2 -> Q6 because persistence/history are tempting fixed blocks and need the service-zone rule.
       - Q2 -> Q7 because routing/procedure may be structural zones, local procedures or both.
       - Q2 -> Q10 because gates can be universal categories or domain policy.
       - Q3 -> Q8 because live interface depends on internal relationship flow.
       - Q4 -> Q5 because shared context depends on what process instances inherit versus own locally.
       - Q6 -> Q9 because handback must know what becomes durable history/current state.
       - Q7 -> Q9 because result semantics depend on procedure outcome type and routing boundary.
       - Q8 -> Q13 because proof must test live owner-facing behavior, not only internal artifacts.
       - Q9 -> Q12 because agent/process communication needs stable handback semantics.
       - Q10 -> Q11 because evolution cannot be trusted until mutation/approval/evidence gates are known.
       - Q11 -> Q15 because evidence-driven change needs a return path when architecture assumptions break.
       - Q12 -> Q14 because carrier/repo decisions depend on what must be communicated and preserved.
       - Q13 -> Q14 because proof artifacts can create carrier pressure, but only after proof categories exist.

       ## Ready forge nodes

       Ready now:
       1. Q1 Core invariant.
          Reason: first hard prerequisite. Must be forged before service blocks or implementation.
       2. Q2 Service-zone rule.
          Reason: ready after Q1 or tightly co-framed as context, but must not freeze full block list.
       3. Q15 Graph growth / recursive decomposition / gap rebalance rule.
          Reason: route discipline is now owner-steered and should be embedded in every future forge.

       Recommended next:
       Run local/architecture-forge for Q1 Core invariant.

       ## Recommended next CALL

       CALL c-solmax-operating-substrate-core-invariant-architecture-forge-001
       to: session
       direction: solmax
       play: local/architecture-forge
       node: g-operating-substrate
       goal: |
         One owner-approved architecture card exists for q01_core_invariant:
         what core invariant makes operating-substrate a universal adaptive
         process structure / process pack for custom AI-process construction,
         without freezing the service-block list, implementation, carrier or
         consumer-specific policy. The card also classifies any new entities or
         child questions spawned by the invariant.
       context: |
         Read:
         - live/solmax/CHARTER.md
         - live/solmax/TREE.md
         - live/solmax/NOW.md
         - live/solmax/work/operating-substrate/cards/operating-substrate-scope-boundary-001.md
         - live/solmax/work/operating-substrate-universal-structure-cartography-001.md
         - live/solmax/history/2026-07-09-c-solmax-operating-substrate-structure-discovery-architecture-forge-001.md
         - live/solmax/knowledge/zaratusta-live-use-failure-and-operating-substrate-route.md

         Parent locks:
         - Scope/boundary card accepted.
         - Concrete process structure, service blocks, state/history/procedure/source/evidence/test/repo/backend/carrier are not frozen.
         - Current artifacts are evidence, not authority.
         - Previous assistant block sketch is not accepted truth.
         - Owner approved the universal-structure question map with steering:
           Q1/Q2 are expected to spawn new entities/questions; graph growth is
           normal if classified and routed.

         Forge handoff:
         - plain_question: "Какая core invariant делает operating-substrate универсальной адаптивной структурой процесса?"
         - why_now: "Hard prerequisite before service blocks, relationships, adaptation and proof."
         - must_decide: "The invariant / kind of object: process pack, service-layer skeleton, adaptive process kernel, or another formulation."
         - must_not_decide: "Final block list, schemas, procedures, repo/backend, carrier, implementation, proof harness."
         - expected_answer_shape: invariant
         - first_owner_question: "Когда ты говоришь 'должна быть структура', что в ней обязано оставаться тем же самым при смене domain/process?"
         - recursive_growth_requirement: "If the invariant introduces new architectural entities, classify each as inline minimal definition, child question, hidden prerequisite, downstream elaboration, proof question, consumer-specific detail, rejected premature freeze, or capture. Returning new questions is success unless they are hard prerequisites that prevent Q1 from being answered."
         - return_to_graph_if: "The answer depends on one consumer, collapses into discovery loop, requires naming fixed blocks before the invariant exists, or spawns a hard prerequisite that must be mapped first."
       boundaries: |
         Do not implement.
         Do not freeze service-block list.
         Do not treat previous assistant sketch as accepted.
         Do not copy Direction OS/Zaratusta/Health/indie-game artifacts as authority.
         Do not select repo/backend/carrier.
         Do not change Direction OS kernel.
         Do not try to answer all child questions spawned by Q1.
         Discuss Q2 service-zone rule only as orientation unless owner explicitly redirects.
       done_when: |
         A single architecture card is owner-approved with owner words. The card
         freezes only the Q1 core invariant / structure-kind answer, records
         rejected alternatives, preserves downstream service-block/config/proof/
         carrier questions, classifies any spawned entities/questions, states
         whether graph rebalance is needed, and emits gap_event if Q1 proves unready.
       return: |
         RESULT with accepted architecture card if approved, owner approval evidence,
         spawned entities/questions, rejected alternatives, child/downstream questions,
         exact state_changes and next CALL.
       budget: one focused session
       parent: s-solmax-operating-substrate-universal-structure-cartography-001
       surface: chatgpt

       END_OF_FILE: live/solmax/work/operating-substrate-universal-structure-cartography-001.md

  2. Update live/solmax/NOW.md:
     action: update
     changes:
       route_status: operating_substrate_universal_structure_cartography_owner_approved
       owner_directive_append: |
         Universal-structure cartography owner sign:
         - Owner approved the universal adaptive process-structure question map as
           a working graph.
         - Owner steering: Q1/Q2 are expected to spawn new entities and questions;
           the process must treat this as normal recursive architecture graph
           growth, not as failure by default.
         - Q15 is amended to graph growth / recursive decomposition / gap rebalance
           rule.
         - Next recommended forge is Q1 core invariant. Q1 must classify spawned
           entities/questions and must not attempt to answer all downstream questions.
         - No implementation, repo/backend/carrier, proof harness, Direction OS
           kernel change or final service-block list is activated.
       preserved_evidence_append:
         - live/solmax/work/operating-substrate-universal-structure-cartography-001.md
         - live/solmax/history/2026-07-09-s-solmax-operating-substrate-universal-structure-cartography-001.md
         - owner approval/steering in chat: "я с вопросами согласен ... будут возникать новые вопросы ... за одним вопросом последуют другие ... мы определим какую-то сущность, которая тоже должна быть отдельно определена ... мы должны быть готовы к такому"
       decisions: []
       open_calls:
         - c-solmax-operating-substrate-core-invariant-architecture-forge-001
       next: work/calls/c-solmax-operating-substrate-core-invariant-architecture-forge-001.md

  3. Add next CALL:
     file: live/solmax/work/calls/c-solmax-operating-substrate-core-invariant-architecture-forge-001.md
     action: create
     content: |
       CALL c-solmax-operating-substrate-core-invariant-architecture-forge-001
       to: session
       direction: solmax
       play: local/architecture-forge
       node: g-operating-substrate
       goal: |
         One owner-approved architecture card exists for q01_core_invariant:
         what core invariant makes operating-substrate a universal adaptive
         process structure / process pack for custom AI-process construction,
         without freezing the service-block list, implementation, carrier or
         consumer-specific policy. The card also classifies any new entities or
         child questions spawned by the invariant.
       context: |
         Read:
         - live/solmax/CHARTER.md
         - live/solmax/TREE.md
         - live/solmax/NOW.md
         - live/solmax/work/operating-substrate/cards/operating-substrate-scope-boundary-001.md
         - live/solmax/work/operating-substrate-universal-structure-cartography-001.md
         - live/solmax/history/2026-07-09-c-solmax-operating-substrate-structure-discovery-architecture-forge-001.md
         - live/solmax/knowledge/zaratusta-live-use-failure-and-operating-substrate-route.md

         Parent locks:
         - Scope/boundary card accepted.
         - Concrete process structure, service blocks, state/history/procedure/source/evidence/test/repo/backend/carrier are not frozen.
         - Current artifacts are evidence, not authority.
         - Previous assistant block sketch is not accepted truth.
         - Owner approved the universal-structure question map with steering:
           Q1/Q2 are expected to spawn new entities/questions; graph growth is
           normal if classified and routed.

         Forge handoff:
         - plain_question: "Какая core invariant делает operating-substrate универсальной адаптивной структурой процесса?"
         - why_now: "Hard prerequisite before service blocks, relationships, adaptation and proof."
         - must_decide: "The invariant / kind of object: process pack, service-layer skeleton, adaptive process kernel, or another formulation."
         - must_not_decide: "Final block list, schemas, procedures, repo/backend, carrier, implementation, proof harness."
         - expected_answer_shape: invariant
         - first_owner_question: "Когда ты говоришь 'должна быть структура', что в ней обязано оставаться тем же самым при смене domain/process?"
         - recursive_growth_requirement: "If the invariant introduces new architectural entities, classify each as inline minimal definition, child question, hidden prerequisite, downstream elaboration, proof question, consumer-specific detail, rejected premature freeze, or capture. Returning new questions is success unless they are hard prerequisites that prevent Q1 from being answered."
         - return_to_graph_if: "The answer depends on one consumer, collapses into discovery loop, requires naming fixed blocks before the invariant exists, or spawns a hard prerequisite that must be mapped first."
       boundaries: |
         Do not implement.
         Do not freeze service-block list.
         Do not treat previous assistant sketch as accepted.
         Do not copy Direction OS/Zaratusta/Health/indie-game artifacts as authority.
         Do not select repo/backend/carrier.
         Do not change Direction OS kernel.
         Do not try to answer all child questions spawned by Q1.
         Discuss Q2 service-zone rule only as orientation unless owner explicitly redirects.
       done_when: |
         A single architecture card is owner-approved with owner words. The card
         freezes only the Q1 core invariant / structure-kind answer, records
         rejected alternatives, preserves downstream service-block/config/proof/
         carrier questions, classifies any spawned entities/questions, states
         whether graph rebalance is needed, and emits gap_event if Q1 proves unready.
       return: |
         RESULT with accepted architecture card if approved, owner approval evidence,
         spawned entities/questions, rejected alternatives, child/downstream questions,
         exact state_changes and next CALL.
       budget: one focused session
       parent: s-solmax-operating-substrate-universal-structure-cartography-001
       surface: chatgpt

       END_OF_FILE: live/solmax/work/calls/c-solmax-operating-substrate-core-invariant-architecture-forge-001.md

  4. Append to live/solmax/LOG.md:
     action: append_line
     line: |
       2026-07-09 — operating-substrate universal-structure cartography owner-approved with recursive graph-growth steering; Q15 amended to graph growth / recursive decomposition / gap rebalance; next forge = q01_core_invariant with spawned-question classification.

  5. Add history:
     file: live/solmax/history/2026-07-09-s-solmax-operating-substrate-universal-structure-cartography-001.md
     action: create
     content: |
       Store this full RESULT.

  6. Do not edit live/solmax/CHARTER.md or live/solmax/TREE.md.

captures:
  - "Owner emphasized that Q1 will likely define new entities that themselves require separate definition; future architecture-forge sessions must treat spawned questions as normal graph growth unless they block the current card."
  - "The top-level graph should stay capped at 15 major nodes; child questions and subcluster cartography are the preferred mechanism for recursive decomposition."
  - "Direct CASC/Confluence artifacts were not found in the available repo search; owner-supplied artifacts can become evidence anchors later."

decisions_needed: []

play_check:
  - "1 Frame cluster: done — cluster named as universal adaptive operating-substrate process structure; anchor question and failed-forge owner symptom recorded."
  - "2 Inventory: done — accepted decisions, partial evidence, rejected alternatives, owner corrections, candidate source artifacts and non-authorities listed; no design answer frozen."
  - "3 Plain questions: done — 15 candidate nodes rewritten as owner-facing plain questions."
  - "4 Classify graph: done — every node has plain_question, why_it_matters, answer_shape, status, dependency_type, blocks, do_not_solve_here, needs_research, needs_anchor, confusion_traps and forge_handoff; Q15 amended to graph growth / recursive decomposition / gap rebalance per owner steering."
  - "5 Route: done — next recommended architecture-forge is q01_core_invariant; alternatives rejected with reasons; gap/rebalance and recursive graph-growth rules stated."
  - "6 Owner sign (owner): done — owner approved with steering. Owner words: 'я с вопросами согласен ... будут возникать новые вопросы ... за одним вопросом последуют другие ... мы определим какую-то сущность, которая тоже должна быть отдельно определена ... мы должны быть готовы к такому'."

log: "Operating-substrate universal-structure cartography owner-approved with recursive graph-growth steering; Q15 amended; next forge = q01_core_invariant with spawned-question classification."

next: |
  CALL c-solmax-operating-substrate-core-invariant-architecture-forge-001
  to: session
  direction: solmax
  play: local/architecture-forge
  node: g-operating-substrate
  file: live/solmax/work/calls/c-solmax-operating-substrate-core-invariant-architecture-forge-001.md

END_OF_FILE: live/solmax/history/2026-07-09-s-solmax-operating-substrate-universal-structure-cartography-001.md
