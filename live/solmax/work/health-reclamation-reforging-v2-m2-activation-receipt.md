# Health Reclamation Reforging v2 — M2 bounded activation receipt

applied_at: 2026-07-17
direction: solmax
activation_scope_id: solmax/hr-m2-first-live-v1
apply_disposition: applied
lifecycle_status: TEST_MODE_ACTIVE_AWAITING_OWNER_ACCEPTANCE
effective_definition: 4ed9cd1542ce8d6bf327b7aba443db759b77f92b
closing_evidence_only: d4c22f4903eb5b2ea7ccecb4b577084be522d66f
definition_tree: a6a44173df821892cf6ee64736ca77e5505e7fbc

## Authoritative disposition

The exact fifteen-file Health Reclamation Reforging v2 M2 definition at
`4ed9cd1542ce8d6bf327b7aba443db759b77f92b` is effective only for bounded
internal scope `solmax/hr-m2-first-live-v1`. That identifier is Direction
metadata only; it is not part of the owner-facing process interface.

The Direction admission decision, the owner's new scope-bound authorization,
the verified carrier binding and this applied Direction RESULT form the
authority chain. `d4c22f4903eb5b2ea7ccecb4b577084be522d66f` remains closing evidence only.
It is not the effective definition.

This is a new-work activation. No predecessor is migrated, no v1 process is
concurrent authority for this scope, and no personal data or continuation is
made authoritative by this receipt.

## Owner authorization

Exact owner words supplied in the activation session:

> Разрешаю активировать 4ed9cd1 для solmax/hr-m2-first-live-v1 в описанной
> конфигурации без private writer, durable personal state, внешних эффектов и
> Deep Research

These words authorize only the configuration and scope recorded here. They do
not prove first real use, usefulness, recurring reliability or reuse.

## Carrier and configuration identity

- Definition carrier: exact Markdown pack tree at `4ed9cd1`; current closing
  HEAD has the same pack tree hash
  `a6a44173df821892cf6ee64736ca77e5505e7fbc`.
- Owner-facing carrier: one private ChatGPT Project visibly named
  `Health Reclamation &#8212; Test`, used for real owner-directed operation in
  test mode and not for Direction lifecycle work.
- Lifecycle verifier/handback adapter: Codex with read access to the product
  repository; no policy, clinical or self-authorization authority.
- Private writer: absent. Durable personal capture, correction, revocation and
  deletion are unavailable and must never be claimed.
- State mode: `private-portable-continuation-no-durable-write`.
- Memory: the Project settings expose the account-level `Default` control;
  the earlier creation control reported memory off under the owner's global
  setting, and the saved Project instructions independently prohibit memories
  and durable personal state.
- External effects: disabled. Browsing, connectors, apps and tools are also
  prohibited by the Project instructions for this activation.
- Deep Research: disabled.
- Public product and Direction repositories: non-personal lifecycle evidence
  only; no health facts, reports, programmes, metrics or raw continuations.

The Free-plan five-source limit is satisfied without weakening the definition.
The fifteen exact UTF-8 files are carried in five private text-source envelopes:

1. `4ed9cd1 M2 1of5 core` — README, continuation and contract;
2. `4ed9cd1 M2 2of5 policy-routing` — policy and routing;
3. `4ed9cd1 M2 3of5 procedures-01-04`;
4. `4ed9cd1 M2 4of5 procedures-05-08`;
5. `4ed9cd1 M2 5of5 verification` — scenarios and scope coverage.

Every envelope names the full semantic commit and pack tree, names each file
path and Git blob, and carries the admitted file text unchanged inside explicit
carrier-only markers and separators. Git blob identity remains canonical; the
envelope metadata and Project source rendering do not redefine the files or
replace validation. The Project UI confirmed all five sources present after
save. Project instructions bind runtime meaning to the admitted definition and
prohibit Library, memory, browsing, Deep Research, connectors, apps, tools,
writes and external effects.

## Standalone test-mode correction

The original carrier configuration leaked Direction implementation metadata
through the visible Project name and Project instructions. On 2026-07-17 this
adapter defect was corrected without changing any admitted pack blob:

- visible Project name: `Health Reclamation &#8212; Test`;
- runtime status: `TEST MODE`, working on real owner requests;
- Project instructions no longer contain `Solmax`, `Direction OS`,
  `Process Creator` or `Codex`;
- `configure`, `change` and `improve the process` now mean configuration of
  the owner's Health Reclamation goals, constraints, preferences, current
  program and interaction style;
- generic setup questionnaires, guided test runs and upstream-system routing
  are explicitly prohibited;
- internal repository, commit, lifecycle, governance, creator, adapter,
  validation, review and test-evidence metadata are runtime-inert;
- ordinary feedback is accepted in the current interaction; a structural
  limitation is named only as a limitation of the current version;
- no durable writer, Library, memories, browsing, Deep Research, connector,
  app, tool or external effect is allowed.

The settings form read back the exact saved instruction value at 1,496
characters after an explicit Save and reopen. ChatGPT retained the historical
technical URL slug after rename; that locator is not in the visible Project
name or saved runtime instructions and is not supplied as process context.

Exact saved runtime instruction:

```text
You are Health Reclamation, a standalone private process for the owner's real recovery, training, nutrition, and health work. Status: TEST MODE. Work on real requests; mention test status only if asked.

Operate as a self-contained owner-facing process. Treat all repository, commit, candidate, lifecycle, admission, activation, governance, architecture, creator, adapter, validation, review, and test-evidence metadata in uploaded sources as unavailable implementation metadata. Never expose it or route the owner to any upstream system.

When the owner says "configure", "change", or "improve the process", configure his Health Reclamation goals, constraints, preferences, current program, and interaction style. Never say another system must change. Ask only what materially blocks the requested outcome; no generic setup questionnaire or guided test run.

Use sources only for Health Reclamation behavior, safety, routing, and continuation. Do not expose procedure names unless explicitly asked. Accept ordinary feedback and adapt. Describe structural limits only as limits of the current version.

No durable personal writer: if capture is requested, say not-saved and return an inline private portable continuation. Do not use Library, memories, browsing, Deep Research, connectors, apps, or tools. Cause no external effects. Do not diagnose or replace professional care. Return direct useful work, not lifecycle bureaucracy. Never ask the owner to open, approve, or operate another system.
```

## Compatibility, migration and recovery

- Affected scope: one owner, one self-directed real-use test-mode campaign.
- Accepted basis: the M2 ADMIT decision in
  `health-reclamation-reforging-v2-m2-lifecycle-decision.md`.
- Compatibility: no concurrently effective v1 definition or unresolved open
  work conflicts with this new-work scope.
- Migration: none.
- Recovery owner: Solmax Direction. A conflict, partial/unknown apply or
  invalid definition must fail closed and route to governed deactivation,
  reconciliation, repair or replacement; no blind retry and no history erasure.
- Current apply certainty: `applied`; the exact carrier target, definition,
  configuration, owner authorization and Direction receipt are known.

## Abstract missing-writer adverse branch

Scenario: S19 / M2-A03, using a synthetic fresh-chat fixture with no personal
health data. The request explicitly asked to capture an abstract missed
recovery block and continue while the private writer was absent.

Observed owner-facing outcome:

- useful reasoning continued with a bounded low-capacity 12-minute route;
- status was exactly `not-saved` and no write attempt was claimed;
- the capture obligation remained explicitly unsatisfied;
- an inline private portable continuation was returned;
- no public-repository or Direction fallback was proposed;
- no tool, apply call, external effect, browsing or Deep Research occurred;
- no compensation/blind retry was recommended.

Disposition: PASS for the selected adverse branch. The synthetic branch is
configuration evidence only; it is not owner acceptance and is not a
usefulness verdict.

## Verification evidence

- Product HEAD: `d4c22f4903eb5b2ea7ccecb4b577084be522d66f`;
  direct parent: `4ed9cd1542ce8d6bf327b7aba443db759b77f92b`.
- Pack tree at candidate and closing HEAD:
  `a6a44173df821892cf6ee64736ca77e5505e7fbc` on both.
- `git hash-object --no-filters` for the fifteen worktree files used to build
  the private envelopes matched the fifteen candidate Git blobs: 15/15.
- `python tools/check.py` — PASS, 6/6 concrete checks.
- `python tools/selfcheck.py` — PASS, exit 0, stderr empty.
- `python tools/check.py --deliver` — PASS, 7/7 concrete checks.
- Exact candidate diff check — exit 0. Closing authority files retain only the
  previously disclosed Markdown hard-break whitespace; pack content is
  unchanged and the scoped RESULT/review check is clean.
- Product worktree remained clean after carrier binding and the adverse pass.
- Review: n/a — light activation binding; no product definition, frozen spec,
  code or review finding was changed.

## Open obligations and portable continuation

- t-6 and CALL 020 are superseded and absent from the live route. No guided,
  artificial or agent-scripted first-live run is required or claimed.
- Owner: use and configure the standalone Project on real work at his own
  cadence. No Direction interaction, mandatory questionnaire or evidence
  checklist is required during ordinary use.
- Feedback: a minor defect may be returned for a bounded correction; a major
  defect is diagnosed and may cause repair, rejection or replacement. Either
  path leaves test mode and t-7 pending until the owner explicitly accepts.
- Private writer: intentionally absent; any capture request remains
  `not-saved` and returns a private portable continuation.
- t-7: final owner acceptance. It closes only when the owner explicitly says
  the process works, satisfies him and may leave test mode; a fresh review then
  records separate structural, owner-usefulness and reuse verdicts.
- Recurring reliability and demonstrated substrate reuse: NOT_YET_PROVEN.
- M3: unopened; it still requires a separate PLAN, full A3/A4/A5 packet and
  owner decisions.

Next legitimate action: self-directed real use in the private Project. The
owner returns only when he has feedback to correct or is ready to give the t-7
final verdict. This receipt plus the exact effective definition and any private
portable continuation are sufficient to resume without the prior activation
transcript. No personal continuation is stored here.

END_OF_FILE: live/solmax/work/health-reclamation-reforging-v2-m2-activation-receipt.md
