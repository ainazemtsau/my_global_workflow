CALL c-solmax-zaratustra-health-repo-041
to: session
direction: solmax
play: work
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-repo
status: ready

goal: |
  Продуктовый репозиторий Заратустры перестаёт быть markdown-слоем и становится
  репозиторием, который ПРИНИМАЕТ КОД и проверяет его одной командой.

  Это первая задача ставки, производящая машину, а не бумагу. Её продукт — не
  функциональность, а ПЕТЛЯ ОБРАТНОЙ СВЯЗИ: сборка, границы модулей, форматтер,
  линтер, типы и тесты, которые фоновый агент не может обойти.

  Владелец 2026-08-27 своим словом «А, режем бумагу, идём в ядро» закрыл
  доказательную фазу задачи `t-health-context` и перевёл ставку на ядро. Эта
  нога — исполнение того слова.

context: |
  АБСОЛЮТНЫЕ ПУТИ. Ничего искать не надо.

  ПРОДУКТОВЫЙ РЕПОЗИТОРИЙ — здесь идёт вся работа:
  `C:\projects\zaratusta-product` (это `github.com/ainazemtsau/zaratusta`).
  Сейчас: ветка `main`, синхронна с `origin/main`, HEAD `b92de14` от 2026-07-03.
  Содержимое — 20 файлов: 18 markdown плюс `.gitignore` и `.gitattributes`.
  Ни одной строки исполняемого кода. Действующий корневой `AGENTS.md` прямо
  ЗАПРЕЩАЕТ `src/`, тесты, скрипты и CI — снять этот запрет входит в задачу.

  ИСТОРИЯ, КОТОРУЮ НАДО ЗНАТЬ, ЧТОБЫ НЕ ПОВТОРИТЬ:
  - 2026-06-16 репозиторий был заведён со скелетом Registry-Ledger Kernel на
    TypeScript (`src/envelope`, `src/capability-registry`, `src/effect-gate`,
    `src/jsonl-ledger`, `src/engine-store-seams`, тесты, CI);
  - коммит `b9c735c` «Reset to Markdown-only manager foundation» снёс его целиком;
  - 2026-07-06 живая проба провалилась, маршрут поставлен на паузу
    (`live/solmax/knowledge/zaratusta-live-use-failure-and-operating-substrate-route.md`).
  Разбор той ошибки: скелета было выбрано СЛИШКОМ МНОГО под невыясненный маршрут.
  Здесь маршрут выяснен, и объём задачи ограничен строками приёмки ниже.

  РЕПОЗИТОРИЙ НАПРАВЛЕНИЯ (только читать и вернуть один файл в RESULT):
  `C:\my_global_workflow_worktrees\solmax`
  - `os/engineering/PROJECT_SETUP.md` — процедура, которую исполняешь. Четыре шага.
  - `os/engineering/CONTRACT_VERSION` — `current: 36`. Это число идёт в стамп.
  - `os/engineering/VALIDATION.md`, `CONTOUR.md` — контракт прогона и правила гейтов.
  - `os/engineering/profiles/README.md` — формат профиля стека, ≤100 строк.
    Профилей есть три: `typescript-node.md`, `unity.md`, `markdown-substrate.md`.
    Профиля Python НЕТ — он создаётся в этой ноге.
  - `live/solmax/cards/t-health-repo.md` — шесть строк приёмки, они же ниже.
  - `live/solmax/cards/bet-g-zara-health-vertical.md`, раздел W48 — дешёвые
    реализационные решения по умолчанию.

  СТЕК — ВЫБРАН И ЗАКРЫТ. Обсуждению в этой ноге не подлежит.

  Владелец 2026-08-27: **«стек тот который выберешь, мне главное чтобы оптимально
  работал и работал на windows и macos»**. Это единственное его требование к
  стеку, и оно ЖЁСТКОЕ. Остальное — решение сессии под его делегированием от
  2026-08-13 («остальные технические вопросы… сам разруливай»).

  **Выбрано: Python 3.13 под управлением `uv`.** Основания, проверяемые:
  - его машина уже это несёт и это уже работает: `uv 0.8.22`, `Python 3.13.7`,
    сам workflow-репозиторий стоит на `requires-python = ">=3.13,<3.14"` и
    гоняется командой `uv run --locked python osctl.py`;
  - `uv` сам скачивает и пинует интерпретатор, поэтому Windows и macOS получают
    ОДИН И ТОТ ЖЕ Python из одного `uv.lock` — кроссплатформенность обеспечивается
    инструментом, а не инструкцией в документации;
  - остальное по W48: сервер отдаёт готовые страницы без фронтенд-фреймворка,
    тесты `pytest`, следы — JSONL по прогону, идентификаторы — читаемые слаги
    плюс версия.

  **ГЛАВНАЯ ЛОВУШКА КРОССПЛАТФОРМЕННОСТИ, И ОНА УЖЕ КУСАЛА В ЭТОМ КОНТУРЕ.**
  Windows-only проверки на `.ps1` — реальная боль игрового направления. Поэтому
  здесь запрет: **ни одного `.ps1`, `.sh`, `Makefile` или любого шелл-скрипта на
  пути проверки.** Каждая команда — `uv run …`, кроссплатформенная по построению.
  Пути в коде — только через `pathlib`, никаких разделителей строкой.

  Исполнитель вправе заменить любой пункт КРОМЕ языка и `uv`, с записанной
  причиной в ADR-0001, пока держатся строки приёмки. Замена языка — вопрос
  владельцу, а не решение исполнителя.

boundaries: |
  - Работать на ветке в продуктовом репозитории, коммитить локально, **НЕ пушить**:
    push делает владелец. Ветку назвать `setup/agent-ready`.
  - **Старую markdown-поверхность НЕ удалять и НЕ переписывать.** `contracts/`,
    `checks/`, `examples/`, `docs/` остаются уликой. Прежний корневой `AGENTS.md`
    заменяется, но его прежний текст остаётся в истории Git.
  - **Ни одной строки продуктовой функциональности.** Ни Health, ни контрактов
    предметной области, ни сценария «Свод дня». Это следующие задачи.
  - В репозиторий направления `C:\my_global_workflow_worktrees\solmax` исполнитель
    НЕ пишет. Профиль Python возвращается ТЕКСТОМ в `state_changes` RESULT, его
    кладёт писатель Направления.
  - Инструмент недоступен — СТОП и вопрос владельцу, никогда обходной путь.
  - Скелет держать минимальным. Прошлый раз проект умер от избытка каркаса:
    берётся только то, что нужно, чтобы одна команда проверяла репозиторий.

done_when: |
  1. **Репозиторий принимает код и проверяется одной командой.** Заведён по
     `os/engineering/PROJECT_SETUP.md`: скелет модулей под `src/<module>/`,
     границы модулей, проверяемые СБОРКОЙ (граф зависимостей, не документацией),
     форматтер, линтер, проверка типов и тесты — всё запускается одной командой
     и в файловом варианте. Команда названа дословно и её вывод приложен.
  2. **Корневой `AGENTS.md` заменён** (≤150 строк, команды первыми): что за
     проект, точные команды сборки/тестов/проверок, раздел контракта прогона,
     соглашения, определение готовности, раздел конституции 3-7 принципов.
     Прежний запрет на `src/` перестаёт быть действующим правилом.
     `CLAUDE.md` — одна строка импорта плюс платформенные заметки.
  3. **Служебный контур создан:** `validation.config` с
     `synced_contract_version: 36`, порогом kill-rate мутаций, бюджетом повторов
     и правилами эскалации; `REVIEW.md` с рубрикой ревью ЭТОГО репозитория;
     `docs/adr/0001-*` с решением о стеке; `docs/FRICTION.md`; `_scratch/` с
     самоигнорирующимся `.gitignore`.
  4. **Профиль стека Python написан** в формате `os/engineering/profiles/README.md`,
     ≤100 строк, и возвращён ЦЕЛИКОМ в `state_changes` RESULT как
     `os/engineering/profiles/python.md`.
  5. **Старая markdown-поверхность на месте**, и в `README.md` одной строкой
     записано, что власти над новым рантаймом и состоянием она не имеет.
  6. **Ноль продуктовой функциональности.** Проверяется тем, что в `src/` нет ни
     одного имени из предметной области Health.
  7. **Проверка идёт одинаково на Windows и на macOS — требование владельца от
     2026-08-27, дословно: «мне главное чтобы оптимально работал и работал на
     windows и macos».** Механически: на пути проверки нет ни одного `.ps1`,
     `.sh`, `Makefile` или иного шелл-скрипта; интерпретатор пинуется через `uv`
     и `uv.lock`, а не берётся из системы; в коде и конфигурации нет ни одного
     разделителя пути строкой и ни одной абсолютной платформенной тропы. В
     `README.md` записаны обе строки запуска — они должны совпадать дословно.
     Проверить вживую можно только на Windows: команда прогоняется здесь, а для
     macOS предъявляется механическое основание (нет скриптов, пин через `uv`,
     `pathlib`), а не заявление «должно работать».

return: |
  Один RESULT плея `work`. В evidence — дословный вывод одной проверяющей команды
  на чистом дереве, список созданных файлов, хеш локального коммита и текст
  профиля Python. Задача закрывается светлым закрытием, если каждая строка
  приёмки перевыводится из закоммиченных байтов и вывода команд первыми руками;
  строка, требующая суждения, отправляет задачу в свежий отдельный чат.

budget: |
  Одна сосредоточенная сессия, по правилу размера ставки — не больше полудня
  фокусной работы. Переросла — дробить ДО выдачи следующего наряда, а не по ходу.
  Фан-аут не требуется.

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-health-repo-041.md
