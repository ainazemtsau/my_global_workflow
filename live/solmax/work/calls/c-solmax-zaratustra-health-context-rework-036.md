CALL c-solmax-zaratustra-health-context-rework-036
to: session
direction: solmax
play: work
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-context
status: ready

goal: |
  Гейт главного допущения ставки стоит на честной улике: вердикт соответствует
  фактам, вход прогонов не содержит ответов, а метод пригоден для повторного
  применения на следующих днях.

  Сейчас это не так. Предпроход тремя враждебными агентами нашёл в вердикте
  `health-gate-verdict-v1.md` подтверждённые ошибки, и он не может идти на
  биндинговое опровержение в таком виде.

context: |
  АБСОЛЮТНЫЕ ПУТИ. Продуктовый репозиторий не нужен.

  Состояние (C:/my_global_workflow_worktrees/solmax):
  - live/solmax/work/health-gate-verdict-v1.md — вердикт, который правится.
  - live/solmax/work/health-gate-runs/run-2026-07-20.md, -21.md, -22.md — прогоны.
  - live/solmax/work/health-context-model-v1.md — объявленный список ссылок.
  - live/solmax/cards/t-health-context.md — восемь строк done_when, контракт задачи.
  - live/solmax/history/2026-08-14-s-solmax-zaratustra-health-context-035.md — нога,
    выдавшая вердикт. history/2026-08-14-s-solmax-zaratustra-health-context-036.md —
    нога, зафиксировавшая находки предпрохода со всеми проверочными командами.

  Улики владельца (СТРОГО read-only, C:/projects/solmax-operating-substrate,
  пин f1289413bf29eaf9bf205daf0d1506198e8183fd; срезы 4aad56d, 6192699, 78f8607).

  Запечатанные входы прошлого прогона (вне git, могут быть пересозданы):
  C:/Users/Anton/AppData/Local/Temp/claude/C--my-global-workflow-worktrees-solmax/
  a4943d96-bfa9-480e-93b6-f0740a0d546a/scratchpad/gate/

  ПОДТВЕРЖДЁННЫЕ НАХОДКИ, каждая перепроверена первыми руками и воспроизводима
  командой из ноги 036. Это вход, а не гипотезы:
  1. Билет 22-го нарезан из секции `## Owner authority` ПРИНЯТОГО разбора и несёт
     ярлык `durable correction authority` — то есть саму классификацию, которую день
     должен был проверить.
  2. Проверка утечки для 22-го была пустой: греп искал `Accepted observations`, а в
     отчёте за 22-е такой секции нет вовсе (0 вхождений).
  3. Прогон 22-го назвал целями мутации четыре правки внутри
     `first-phase-2026-07-20-v1.md`, но коммит f1289413 этот файл НЕ трогал: он создал
     `nutrition-menu-2026-07-22-v2.md`. Механизм назван неверно, а вердикт засчитал ✅.
  4. Прогон 20-го самочинно переписал заготовку понедельник-среда «без крупы» и ввёл
     правило про хлеб, заявив «меню не переписывается». В версии ровно три штатные
     замены, крупы среди них нет; эталон в тот же день постановил «no menu change».
  5. Совпадения дня 21 в большинстве объясняются шаблоном: пять из восьми распоряжений
     эталона уже лежали в отчёте за 20-е, выданном как контекст непрерывности, а фраза
     «обычный голод отвечается обычной едой» — перевод строки `ordinary hunger may be
     answered normally` из того же отчёта (строка 207).
  6. Прогон 21-го потерял из ужина рис и замороженные овощи; разрыв оценки 175-265 ккал
     — ровно они. Это изменило решение: овощи объявлены дефицитом и приоритетом закупки,
     тогда как в ужине было 300 г овощей.
  7. В вердикте перекрёстная цитата: строка дня 20 процитирована фразой, которая
     существует только в `run-2026-07-21.md`. Строка «дыня → в обзор 26.07 ✅» текстом
     прогона не подтверждается.
  8. При сохранении прогонов потеряны строки `ПРОЧИТАНО` — во всех трёх файлах 0
     вхождений. Комплаенс сохранённой уликой не подкреплён.
  9. УСТОЯЛО: слова «тунец» в отчёте за 20-е ноль вхождений, поэтому решение прогона
     21-го о тунце скопировать было неоткуда. Это единственный неподделываемый элемент.

boundaries: |
  - Ничего не писать в C:/projects/solmax-operating-substrate и ни в один продуктовый
    репозиторий.
  - Прогоны 20, 21 и 22 — замороженная улика. Их текст НЕ редактируется: восстановление
    строк `ПРОЧИТАНО` идёт отдельной пометкой о том, что они были утеряны при переносе,
    а не тихой правкой задним числом.
  - Не пересматривать одобренный владельцем список переносим/откладываем/не берём.
  - Не расширять сценарий за четыре шага и пять операций.
  - Новые прогоны запускать только изолированными субагентами — конфигурация выбрана
    владельцем 2026-08-14 словом «Изолированные субагенты».
  - Не закрывать задачу этой ногой: закрытие требует свежего биндингового опровержения.

done_when: |
  1. `health-gate-verdict-v1.md` переписан по девяти находкам выше: снятые строки сняты,
     завышенные переведены в ⚠️ или ✗ с указанием причины, перекрёстная цитата исправлена,
     несамостоятельные совпадения помечены как шаблонные. Ни одна строка вердикта не
     утверждает больше, чем несёт улика.
  2. День 22 разрешён однозначно: либо билет перенарезан так, что во входе остаются
     ТОЛЬКО дословные реплики владельца без единого авторского ярлыка и день перепрогнан,
     либо день объявлен непроверяемым вслепую с указанием причины. Промежуточного статуса
     нет.
  3. Проверка утечки заменена на такую, которая не может дать ложный зелёный: она
     проверяет присутствие ожидаемого маркера, а не отсутствие произвольной строки.
  4. Контекст непрерывности в объявленном списке заменён: вместо полного принятого отчёта
     предыдущего дня подаются карточки `health.state.next_action` и
     `health.observation.latest_training`. `health-context-model-v1.md` обновлён.
  5. День 21 перепрогнан на исправленном входе, и сказано прямо, устояло ли решение по
     тунцу без выданного отчёта за 20-е. Это главный вопрос всей переработки.
  6. Строки `ПРОЧИТАНО` восстановлены как отдельная пометка о комплаенсе, либо записано,
     что улика утеряна безвозвратно и комплаенс не подтверждён.
  7. Сказано прямо, срабатывает ли порог 1 ставки на исправленной улике и на чём именно
     он теперь держится.

return: |
  Один RESULT плея work. В evidence: переписанный вердикт, разрешение дня 22, результат
  перепрогона 21-го с прямым ответом про тунец, обновлённый объявленный список и ответ
  про порог 1. Если после исправления порог 1 срабатывает — это валидный результат:
  вернуть как опровержение и маршрутизировать на `review`, не чинить внутри ноги.

budget: |
  Одна сосредоточенная сессия. Свежий чат: сессия, выдавшая вердикт и предпроход,
  исчерпана и не может переписывать собственную улику беспристрастно.

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-health-context-rework-036.md
