CALL c-solmax-zaratustra-health-shape-031
to: session
direction: solmax
play: shape
node: g-zara-health-vertical
status: ready

goal: |
  Cut the FIRST small bet for node 1 as a walking skeleton the owner can launch
  and see: he opens localhost, starts a Run, answers its bounded question in
  free language, and gets one saved, visible, useful result with its trace.
  Nothing that is not needed for that first visible result belongs in this bet.

  The owner's words on 2026-08-13 that authorize this framing:
  "я хочу, чтобы мы работали ... главное, чтобы работа шла ... чтобы мы небольшую
  работу сделали, я вижу какой-то результат вот прям подтверждённый. То есть я
  зашёл, это запустил. Возможно, сразу графически я вижу ... чтобы у нас сразу был
  веб-интерфейс, который ... даже нас дисциплинировал, да, чтобы поля везде
  использовать, разбивать файлы нормально, да, там не сваливать всё в кучу ...
  истории огромные могут быть, чтобы получал контекст важный."

context: |
  Read fresh through osctl context, plus:
  - live/solmax/work/converge-g-zara-health-vertical.md — the FROZEN WHAT
    revision 2. Its A1-A21 acceptance surface is the contract for node-1
    CLOSURE. It is evidence with five named corrections below; it is not
    reopened and is not edited by this leg.
  - live/solmax/history/2026-08-13-s-solmax-zaratustra-health-converge-verify-030.md
    — the second FAIL, findings V1-V4.
  - live/solmax/history/2026-08-13-s-solmax-zaratustra-health-ceiling-applied-031.md
    — the owner's ceiling verdict and the five session decisions this CALL
    carries.
  - live/solmax/CHARTER.md, cards/g-zara-health-vertical.md, cards/g-zara.md,
    cards/g-zara-daily-owner-use.md.

  BINDING CORRECTIONS that override the frozen WHAT where they conflict. They
  were decided by the session under the owner's explicit delegation of technical
  and spec decisions ("остальные технические вопросы, что-то решение в спеке, сам
  разруливай"), not signed by him line by line. Every one is reversible and each
  will be shown to him inside concrete work, not as another paper package.

  C1 — Node 1 ships NO standalone chat surface. The owner's whole node-1
    interaction is the localhost UI: start a Run; answer its bounded question in
    free language; confirm a pending step; read everything else. This is exactly
    his "4А" and his "зашёл, запустил". Where the frozen WHAT says node 1 builds
    "ordinary conversation" as a shared function (W1, W5, W6, W12, A3), read it
    as: free-language input INSIDE a Run step, interpreted by a model into typed
    proposed actions, never parsed by deterministic code.

  C2 — Node 1's localhost surface is classified PROCEDURE-CAPABLE under the
    owner's own discriminator ("может ли это следовать нашим процедурам
    сохранения"): it writes through the same validated apply as any other
    surface. Therefore the non-authoritative save/change-REQUEST bridge from a
    procedure-incapable surface (his named example: ChatGPT Web) is NOT node-1
    work. It moves to g-zara-daily-owner-use done_when 3 with the second ingress
    surface. What STAYS in node 1: validated apply, writer receipts, the six
    distinguishable outcomes, saved-vs-not-saved truth and "nothing is saved
    until an observed receipt exists" (W27 apply half, W29, W30, A11 receipt
    half).

  C3 — W6's surviving clause "or request/create an Area in free language" is
    VOID. Node 1 authors Health directly; nothing in node 1 accepts a
    create-an-Area request, in chat or anywhere else. This is the owner's own
    "1Б" (finding V1).

  C4 — Node-1 minimum contracts are the node card's eleven (Area, Capability,
    Operation, Workflow, Step, Run, state transition, effect, artifact, owner
    projection, trace event) PLUS context item/reference/grant. No
    Area-creation-proposal contract and, following C2, no save/change-request
    contract. W31's claim to "restore the exact contract list" was wrong
    (finding V3); this is the corrected list.

  C5 — Node-1 CLOSURE still needs the full A1-A21 surface, including both
    Nutrition and Training breadth (his O1/O2), the eval suite and three real
    Runs. The FIRST BET does not. Stage it: bet 1 is one Capability path
    end-to-end; breadth, the full suite and the three real Runs are later bets
    under the same node (finding V4 — nothing in the artifact ever asked how big
    node 1 is).

boundaries: |
  Do not reopen converge, do not edit the frozen WHAT, do not re-litigate the
  owner's 1Б / procedure-capability / 4А verdicts, and do not weaken the
  non-clinical stop, the opaque-prose invariant, the registered-handler/no-DSL
  boundary, the Direction-OS read-only boundary or the effect/spend gate (A21).
  Do not import the legacy Health pack as runtime or state authority. Do not
  build an Area-creation flow. Do not build an external-chat bridge. Do not
  invent the owner's product content: the exact first Workflow is his (W14).

  Keep the bet inside the owner's build mode: increments a background agent can
  build and that are independently verifiable by test/contract/done_when
  (CHARTER lens 3, W54). No increment the owner cannot review in spare time.

done_when: |
  1. One bet card with done_when, appetite and kill_by, activated on
     g-zara-health-vertical, plus a real cut list naming what is deliberately
     NOT in bet 1 (breadth, full eval suite, three real Runs, second ingress
     surface, Area creation).
  2. Tasks each ≤ half a focused day, ordered so the earliest one tests the
     riskiest assumption (G6), and the last one produces the visible result:
     browser → start Run → answer one bounded question → saved result + trace
     visible on screen.
  3. ONE concrete smallest first-Workflow proposal, in plain owner language,
     grounded in the working Health evidence, presented to the owner as a single
     simple question with a recommendation — this is W14 and it is the ONLY
     owner question this bet opens. It must be MULTI-STEP (more than one
     observable state/decision) and low-consequence.
  4. A verdict per CHARTER lens, and a PLAN agenda answering W7, W13, W26, W48,
     W54 at the cheapest reversible level. On W7 record the owner's stated
     preference as the constraint: typed fields everywhere, state split into
     small files per entity rather than one heap, and big histories loaded by
     declared reference only, so a Run receives what matters instead of
     everything ("разбивать файлы нормально, не сваливать всё в кучу ... истории
     огромные могут быть, чтобы получал контекст важный").
  5. Every task carries the acceptance rows it must satisfy, copied from the
     frozen A1-A21 EXCEPT A2 (node 4) and as amended by C1-C5.

return: |
  One shape RESULT with the bet, cut list, tasks, lens verdicts, PLAN agenda and
  exactly one owner question (the first Workflow). Next route: the first task's
  work/executor CALL after his answer.

budget: one focused shape session

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-health-shape-031.md
