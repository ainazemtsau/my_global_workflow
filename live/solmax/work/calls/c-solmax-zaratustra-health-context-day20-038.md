CALL c-solmax-zaratustra-health-context-day20-038
to: session
direction: solmax
play: work
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-context
status: ready

goal: |
  Догнать гейт до всех трёх дней на объявленном списке и убрать расхождение
  между двумя артефактами задачи. День 20 на карточках ещё не прогонялся.

context: |
  АБСОЛЮТНЫЕ ПУТИ. Продуктовый репозиторий не нужен.

  Нога 037 прогнала гейт на объявленном списке для дней 21 и 22. Решение по
  тунцу на дне 21 устояло: слово «тунец» в карточках дня ноль раз, у обоих
  прогонов `tool_uses: 0`. День 22 решение не воспроизвёл и точно назвал
  недостающую ссылку. День 20 остался на надмножестве.

  Готовое, что переиспользуется как есть:
  - C:/my_global_workflow_worktrees/solmax/live/solmax/work/health-gate-cards/ —
    фикстура; для дня 20 добавляется папка `day20/` тем же способом.
  - .../health-gate-cards-check-v3.py — проверки. Гейты D и P уже умеют новый
    день: добавить `day20` в `SLICE`, `DATE` и `PROBES`, срез `4aad56d`.
  - .../health-gate-runs/run-2026-07-21-cards.md, run-2026-07-22-cards.md —
    возвраты, с которыми сверяется новый прогон.
  - .../health-gate-verdict-v1.md — вердикт версии 3.
  - .../health-context-model-v1.md — объявленный список, 18 карточек.
  - live/solmax/history/2026-08-14-s-solmax-zaratustra-health-context-cards-037.md

  Улики владельца (СТРОГО read-only, C:/projects/solmax-operating-substrate,
  пин f1289413bf29eaf9bf205daf0d1506198e8183fd). Срез ДО дня 20 — `4aad56d`.

  РЕШЕНИЕ ВЛАДЕЛЬЦА ПО ДНЮ 22 ЗАКРЫТО 2026-08-14 его словом «A», вариант А:
  день 22 в гейте закрывается ВТОРОЙ ветвью `done_when` 5 («прямо названа
  недостающая ссылка»), смена версии меню остаётся ОТДЕЛЬНОЙ операцией и уезжает
  в следующую ставку, сценарий «Свод дня» и потолок «четыре шага, пять операций»
  НЕ меняются. Артефакт —
  live/solmax/cards/closed/d-zara-health-day22-outside-scenario-001.md.
  Переоткрывать это в ноге 038 не нужно и нельзя.

boundaries: |
  - Ничего не писать в C:/projects/solmax-operating-substrate и ни в один
    продуктовый репозиторий.
  - Прогоны v1, v2 и на карточках — замороженная улика, их текст НЕ редактируется.
  - Не пересматривать одобренный владельцем список переносим/откладываем/не берём.
  - Не расширять сценарий за четыре шага и пять операций и не менять его состав
    без его слова.
  - Прогон запускать только изолированным субагентом с входом, поданным текстом
    в задание: конфигурация выбрана владельцем 2026-08-14 словом «Изолированные
    субагенты», а `tool_uses: 0` — единственная машинная улика слепоты.
  - Карточки инстанцировать ИЗ улики по срезу ДО дня, не сочинять содержимое и не
    подсматривать в принятый разбор проверяемого дня.
  - Не закрывать задачу этой ногой: закрытие требует свежего биндингового
    опровержения в ОТДЕЛЬНОМ чате после того, как гейт пройдёт на всех трёх днях.

done_when: |
  1. День 2026-07-20 прогнан на объявленном списке по срезу `4aad56d`,
     изолированным субагентом, `tool_uses: 0` записан в возврат.
     `health-gate-cards-check-v3.py` зелёный, включая гейты D, P, E и самотест G.
  2. Сказано прямо: воспроизведено решение дня 20 или названа недостающая ссылка.
     Отдельной строкой — устояли ли на карточках две вещи, которые вердикт держит
     за дном 20: «5+ км не открывают прогрессию на среду» и «мёртвый жук RPE 8,5 —
     сигнал остановиться раньше». И отдельно: повторил ли прогон дефект v1, где
     он переписал заготовку «без крупы» под заголовком «меню не переписывается».
  3. Расхождение нумерации устранено: `health-workflow-svod-dnya-v1.md` ссылается
     на «все 14 обязательных» и на номера 15–19, а объявленный список — это 15
     обязательных и 3 необязательные, номера 1–18. Правится ссылочная часть
     сценария под список; шаги, операции и потолок не трогаются.
  4. Вердикт дополнен результатом дня 20 на карточках; раздел «Главное
     ограничение» снимается целиком либо остаётся с точным указанием, что от него
     уцелело.
  5. Сказано прямо, срабатывает ли порог 1 на объявленном списке теперь, когда
     проверены все три дня, и на чём он держится. День 22 при подсчёте идёт как
     ВЫПОЛНЕННЫЙ второй ветвью — так его засчитал владелец 2026-08-14.

return: |
  Один RESULT плея work. В evidence: собранный вход дня 20, прогон, ответ по двум
  именованным решениям и по дефекту заготовки, обновлённый вердикт, ответ про
  порог 1 и состояние решения по дню 22.
  Если порог 1 срабатывает — это валидный результат: вернуть как опровержение и
  маршрутизировать на `review`, не чинить внутри ноги.
  Если все три дня прошли — следующим шагом идёт свежее биндинговое опровержение
  в ОТДЕЛЬНОМ чате, а не закрытие задачи здесь.

budget: |
  Одна сосредоточенная сессия. Свежий чат: сессия 037 собирала фикстуру и судила
  собственные прогоны, беспристрастной по дню 20 она не будет.

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-health-context-day20-038.md
