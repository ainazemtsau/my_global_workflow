CALL c-solmax-zaratustra-v2-w0-head-058
to: session
direction: solmax
play: work
node: g-zara-w0-core-text
bet: bet-g-zara-w0-core-text
task: t-zara-w0-head
status: ready

goal: |
  Завести публичный репозиторий продукта Zaratustra и написать в нём ГОЛОВНОЙ СЛОЙ
  `CORE.md` — один экран, ≤700 слов, — так, чтобы владелец мог судить замысел
  сценарием, а не списками полей. Положить файл ему и записать его ответ дословно.

  Эта задача проверяет самое сильное допущение ставки: его слово даётся сценарию,
  а не полям. Промах здесь стоит одну задачу вместо всей волны.

context: |
  Ставка `bet-g-zara-w0-core-text` открыта его словом «иду» 2026-09-04. Читать:
  - `cards/bet-g-zara-w0-core-text.md` — блоки `owner_word` (его инструкция о форме
    планов и три его поправки), `approach` (шесть кусков CORE.md), `riskiest_assumptions`
    (допущение A1), `kill_by` (пять порогов);
  - `cards/t-zara-w0-head.md` — шесть строк приёмки;
  - `cards/g-zara-w0-core-text.md` — цель, её шесть строк done_when, блок `triage`;
  - `work/zaratustra-v2-plan-2026-09-03.md` §1-§2 — ИСТОЧНИК содержания;
  - `knowledge/one-public-product-assistant-driven.md`,
    `knowledge/core-md-is-the-spec-converge-off.md`.

  Имя репозитория: `ainazemtsau/zaratustra`. Сессия предложила его словами «молчание
  приму за согласие», он ответил «иду» без возражения. Это МОЛЧАНИЕ, а не его выбор:
  если он назовёт другое имя, оно меняется без потери работы.

  Converge не запускается. В `play_check` копируется дословно:
  `triage: trivial — converge OFF — because CORE.md is the spec`

boundaries: |
  - Содержание НЕ изобретается. Шесть кусков `CORE.md` перечислены в блоке `approach`
    ставки и все взяты из §1-§2 плана. Строка, которой в плане нет и которой он не
    говорил, идёт ему вопросом — это порог смерти 4 ставки: одна такая строка
    останавливает задачу, три подряд останавливают волну.
  - **Пример дня ОБЕЗЛИЧЕН.** Абстрактный пользователь, выдуманная область, ноль его
    данных: ни здоровья, ни игры, ни его папок, ни его имени. Это строка 1 done_when
    цели и его собственная поправка 2026-09-04 («это же не мой личный репо это
    публичный»).
  - В `CORE.md` на этом шаге НЕТ полей сущностей, команд, схем и раскладки папок. Они
    приходят задачей `t-zara-w0-machine`. Если в файле появилась таблица полей —
    строка 4 приёмки не выполнена.
  - `README.md` — витрина на ~15 строк, НЕ объяснение устройства. Второй правды с
    `CORE.md` быть не должно.
  - Ноль строк исполняемого кода. Никакого CI — вырез 2 ставки, тратит его минуты
    GitHub Actions.
  - Требований к его поведению задача не ставит. Она закрыта, когда текст положен и
    его ответ записан, КАКИМ БЫ ОН НИ БЫЛ. Если он откладывает — задача ждёт, и это
    записывается ожиданием, а не провалом.
  - Форма показа владельцу — по его инструкции 2026-09-04: очень просто, про то, что
    он получит, важное подсвечено. Длинные разборы живут в карточках, не в чате.

done_when: |
  Шесть строк приёмки `cards/t-zara-w0-head.md`: репозиторий публичен и заведён;
  `README.md` на ~15 строк; `CORE.md` ≤700 слов с шестью кусками и обезличенным
  примером дня; ни одного поля, команды, схемы и раскладки; файл положен владельцу и
  его ответ записан дословно; ноль строк кода.

return: |
  RESULT по схеме: в `state_changes` — карточка задачи со статусом и журналом, его
  ответ дословно в `owner_approved`, и наряд на следующую задачу
  (`t-zara-w0-machine`) в `next`, если его ответ позволяет идти дальше. Замер
  `wc -w CORE.md` в evidence.

budget: one session

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-v2-w0-head-058.md
