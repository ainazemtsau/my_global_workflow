CALL c-solmax-zaratustra-health-context-rework-040
to: session
direction: solmax
play: work
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-context
status: ready

goal: |
  Устранено то, что свежее опровержение сняло с вердикта главного гейта, и день 20
  доведён до состояния, в котором его результат — какой бы он ни был — можно
  записать как честный.

  Три дефекта, каждый закрывается отдельно и ни один не отменяет другого:
  (1) фикстура дня 20 несёт авторскую строку, которой нет в срезе, и именно она
  даёт опору порога 1; (2) слепота прогона дня 20 машинной распиской не
  подтверждена; (3) проверка v3 не краснеет на русской утечке.

  ЭТО НЕ «ПЕРЕПРОГОН ДО ЗЕЛЁНОГО». Результат нового прогона засчитывается любой.
  Прежний возврат остаётся замороженной уликой и НЕ заменяется и НЕ правится.

context: |
  АБСОЛЮТНЫЕ ПУТИ. Продуктовый репозиторий — ТОЛЬКО для чтения улик.

  Состояние направления — C:/my_global_workflow_worktrees/solmax:
  - live/solmax/work/health-gate-refutation-g5-039.md — отчёт опровержения.
    Он несущий: всё, что здесь названо, там доказано командами и цитатами.
  - live/solmax/work/health-gate-verdict-v1.md — вердикт версии 4, который надо
    переписать до версии 5 по списку снятого (раздел 6 отчёта, семь пунктов).
  - live/solmax/work/health-gate-cards/day20|day21|day22/ — фикстура.
  - live/solmax/work/health-gate-cards-check-v3.py — проверка входа.
  - live/solmax/work/health-gate-runs/ — замороженные возвраты и CHECKS-cards.md.
  - live/solmax/work/health-context-model-v1.md — объявленный список, 18 карточек.
  - live/solmax/cards/t-health-context.md — восемь строк done_when.
  - live/solmax/history/2026-08-27-s-solmax-zaratustra-health-context-g5-039.md

  Улики владельца, СТРОГО read-only: C:/projects/solmax-operating-substrate,
  пин f1289413bf29eaf9bf205daf0d1506198e8183fd. Принятые разборы —
  packs/health-reclamation/workspace/reports/day-report-2026-07-2{0,1,2}.md.
  Срезы ДО дня: `4aad56d`, `6192699`, `78f8607`.

  РЕШЕНО ВЛАДЕЛЬЦЕМ И ПЕРЕОТКРЫТИЮ НЕ ПОДЛЕЖИТ: день 22 засчитан ВТОРОЙ ветвью
  `done_when` 5 его словом «A» 2026-08-14; смена версии меню уезжает отдельной
  операцией в следующую ставку; сценарий «Свод дня» и потолок «четыре шага, пять
  операций» не меняются.

  УЖЕ УСТАНОВЛЕНО ОПРОВЕРЖЕНИЕМ, ПЕРЕПРОВЕРЯТЬ НЕ НАДО:
  - порог 1 ставки НЕ срабатывает; ставка не умирает;
  - прогоны дней 21 и 22 машинно слепы (`totalToolUseCount = 0` в стенограмме
    сессии f969bd91, строки 232 и 237);
  - следов принятого ответа нет ни в одном из трёх возвратов (проверка по числам,
    которых нет во входе);
  - билеты всех трёх дней полны и без авторских ярлыков;
  - дефект v1 на дне 20 не повторён.

boundaries: |
  - Ничего не писать в C:/projects/solmax-operating-substrate и ни в один
    продуктовый репозиторий.
  - Замороженные возвраты не редактировать и не удалять. Новый прогон дня 20
    сохраняется ОТДЕЛЬНЫМ файлом рядом со старым.
  - Не пересматривать решение по дню 22 и одобренный владельцем список
    переносим/откладываем/не берём.
  - Не переписывать `done_when` задачи и не смягчать их формулировки.
  - Самописный сканер, выносящий суждение о качестве разбора, уликой не является.
    Свои проверки — только инструмент пересчёта уже имеющейся улики.
  - Если результат нового прогона снова не закрывает день 20 — это НЕ повод для
    третьего прогона. Тогда решение идёт владельцу вопросом.

done_when: |
  1. Из карточки `05-health.nutrition.substitutions` дня 20 убрана строка
     «Других разрешённых замен на этот момент не объявлено»: в срезе `4aad56d`
     её нет, а `done_when` 2 требует названного происхождения. Карточка 5 всех
     трёх дней проверена на строки, не выводимые из своего среза, и расхождения
     либо убраны, либо названы в отчёте с указанием исходного предложения.
  2. День 20 перепрогнан на исправленном входе ИЗОЛИРОВАННЫМ субагентом в
     ПЕРЕДНЕМ ПЛАНЕ (не фоново), и машинная расписка `totalToolUseCount`
     выписана из стенограммы в шапку возврата вместе с номером строки. Фоновый
     запуск запрещён: его уведомление счётчика не несёт, а файл вывода удаляется.
  3. По новому возврату сказано прямо, каждое отдельной строкой:
     (а) воспроизведено ли принятое указание «второй подход мёртвого жука на
     RPE 8,5 — сигнал в следующий раз остановиться раньше», либо названа
     недостающая ссылка; (б) разрешён ли статус 219 г макарон как готового веса
     по реплике «Макароны приготовлены», либо названа недостающая ссылка;
     (в) устоял ли отказ легализовать замену крупы БЕЗ авторской закрывающей
     строки. Ответ «нет» на любой пункт записывается как есть.
  4. `health-gate-cards-check-v3.py` починена по трём точкам: русские пробы
     `PROBES_RU` КРАСНЯТ, а не печатают; самотест G получил РУССКОЕ отравление
     несущей фразой ответа и краснеет на нём; в счёт «годных проб» английская
     проза по русским карточкам больше не идёт одна — у каждого дня есть годная
     русская проба. Опыт из отчёта опровержения (раздел 3) повторён на копии и
     теперь даёт КРАСНЫЙ.
  5. Вердикт переписан до версии 5. Все семь снятых пунктов внесены поимённо, в
     том числе: «ровно один раз» заменено на пять вхождений в двух репликах;
     219 г перенесены из находок в расхождения; опоры 2 и 4 порога 1 сняты и
     порог переписан на том, что осталось; сказано, что расписка `tool_uses: 0`
     есть на двух днях, а на третьем была инструкцией.
  6. Вынесен вердикт по `done_when` 5: выполнена или нет, по каждому дню
     отдельно. Если день 20 снова не закрывается — сформулирован ОДИН вопрос
     владельцу с вариантами и рекомендацией, и задача остаётся открытой.

return: |
  Один RESULT плея `work`. В evidence — новый возврат целиком, расписка из
  стенограммы с номером строки, вывод починенной v3 на чистой и на отравленной
  копии, и построчная сверка дня 20 с принятым отчётом. Задача не закрывается в
  этой ноге: закрытие требует свежего опровержения нового состояния.

budget: |
  Одна сосредоточенная сессия. Фан-аут не требуется: прогон — один субагент,
  остальное механика. Биндинговый G5 — отдельным свежим чатом после.

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-health-context-rework-040.md
