CALL c-solmax-zaratustra-health-contracts-044
to: session
direction: solmax
play: work
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-contracts
status: ready

goal: |
  Четырнадцать версионированных контрактов Заратустры: один файл на сущность,
  одинаковая типизированная шапка, устойчивый идентификатор — и проверка полей,
  которая падает на заведомо неверном образце.

  Это первая нога, кладущая в репозиторий что-то, кроме проверяющей машинерии.
  Её продукт — СЛОВАРЬ, из которого потом собираются реестр, прогон и сценарий.
  Ни одного Health-содержания: формы, а не наполнение.

context: |
  АБСОЛЮТНЫЕ ПУТИ. Искать ничего не надо.

  ПРОДУКТОВЫЙ РЕПОЗИТОРИЙ — вся работа здесь:
  `C:\projects\zaratusta-product` (`github.com/ainazemtsau/zaratusta`).
  Ветка-основа `setup/agent-ready`, HEAD `30e436d`, **не запушена** (пушит
  владелец). Начни новую ветку `feat/health-contracts` ОТ НЕЁ, а не от `main`:
  на `main` нет ни тулчейна, ни проверки.

  ЧТО В РЕПОЗИТОРИИ УЖЕ ЕСТЬ И ЧЕМ ПОЛЬЗОВАТЬСЯ:
  - `AGENTS.md` (122 строки) — команды первыми, контракт прогона, конституция.
    Читается ПЕРВЫМ и он главнее этого наряда в вопросах КАК.
  - Одна команда: `uv run --locked python tools/check.py`
    (статические гейты + ruff format + ruff check + lint-imports + mypy --strict
    + pytest). Файловый вариант `--files <пути>`, deliver-вариант `--deliver`.
  - `tools/selfcheck.py` — негативный контроль, 30 подсаженных нарушений.
    **Добавил гейт — добавь ему подсаженное нарушение.** Гейт, который не умеет
    краснеть, выглядит точно так же, как работающий.
  - `src/foundation/` — лист (`slug`, `versioned_id`, `repo_root`, `scratch_dir`).
    `src/runtime/` — верхний слой, пустой. Границу держит `[tool.importlinter]`
    в `pyproject.toml`: новый модуль БЕЗ строки в контракте не охраняется.
  - `validation.config` читается проверкой; `docs/FRICTION.md` — журнал трения;
    `docs/adr/` — решения; `RESULT.md` — закрывающий отчёт с проверяемыми полями.
  - Режим `PROBA` (умолчание): замороженной пары, мутаций и отдельной сессии
    опровержения эта нога НЕ должна. Нужны: сборка, гигиена и тест там, где
    владелец не проверит глазом.

  РЕПОЗИТОРИЙ НАПРАВЛЕНИЯ — ТОЛЬКО ЧИТАТЬ, не писать:
  `C:\my_global_workflow_worktrees\solmax`
  - `live/solmax/work/health-context-model-v1.md` — **главный источник**. Форма
    контракта «элемент контекста» уже выведена и подписана: девять свойств
    (`id`, владелец, тип, происхождение, версия/наблюдение, чувствительность,
    актуальность, применения, граница), ровно три класса (`owner/shared`,
    `area/health`, `opaque`), восемнадцать настоящих карточек и объявленный
    список ссылок операции «разбор дня». Форму бери оттуда; сами карточки — НЕТ,
    это содержание задачи 6.
  - `live/solmax/cards/t-health-contracts.md` — строки приёмки задачи целиком.
  - `live/solmax/cards/bet-g-zara-health-vertical.md` — блоки `approach`,
    `owner_word` (в нём слово владельца от 2026-08-28) и `cut_list`.

  ЧЕТЫРНАДЦАТЬ КОНТРАКТОВ, ИМЕНА ЗАКРЫТЫ СПЕКОЙ:
  область · способность · операция · сценарий · шаг · прогон · переход состояния ·
  эффект · артефакт · проекция для владельца · событие следа · элемент контекста ·
  ссылка на контекст · разрешение.
  Контракта «предложение создать область» и «заявка на сохранение» здесь НЕТ.

  ПОПРАВКА ВЛАДЕЛЬЦА ОТ 2026-08-28, И ОНА МЕНЯЕТ ФОРМУ ШАГА.
  Он работает не в браузере, а в Claude Code и Codex: «через Cloud Code, через
  Codex я захожу в них, они работают с проектом… я вижу изменения на веб-версии»;
  «сайт должен быть, естественно. То есть там посмотреть инфу какую-то».
  Отсюда жёсткое требование к контрактам: **контракт ШАГА объявляет свой вопрос
  владельцу ДАННЫМИ, а не страницей.** Шаг, ждущий ответа, возвращает
  типизированное «нужен ответ такой формы», и одна и та же запись обслуживает и
  вызов инструмента, и страницу. **Ни один контракт не знает про HTTP, браузер,
  форму, MCP или транспорт вообще.** Если знает — поверхность протекла в словарь.

boundaries: |
  - Ветка `feat/health-contracts` от `setup/agent-ready`, коммиты локальные,
    **НЕ пушить**: пушит владелец.
  - **Один файл на сущность.** Сущность нельзя добавить, дописав её в общий файл.
    Это строка приёмки карточки, вынесенная сюда границей, чтобы не считаться
    четвёртым пунктом.
  - **Метаданные не становятся исполняемым языком.** Ни компилятора, ни
    интерпретатора, ни своего языка процедур; добавление сценария не может их
    породить. Прямой запрет спеки (W33/A12), не размерное решение.
  - **Ни одного разбора прозы.** Проза, содержащая похожие на одобрение слова,
    идентификаторы или команды, не меняет ни факта, ни разрешения, ни состояния,
    ни эффекта. Проверки — по типам и перечислениям, не по тексту.
  - **Ноль Health-содержания.** Восемнадцать карточек, меню, программа, сценарий
    «Свод дня» — задачи 5 и 6. Здесь только формы.
  - **Реестра и мест вызова здесь НЕТ** — это задача `t-health-registry`,
    следующая. Контракт может объявить ПОЛЕ под место вызова, но проверка,
    роняющая сборку, строится не здесь.
  - Правила репозитория сильнее этого наряда: ни `.ps1`, ни `.sh`, ни `Makefile`;
    пути только `pathlib`; новый модуль — строкой в контракте границ, иначе он
    не охраняется.
  - Инструмент недоступен — СТОП и вопрос владельцу, никогда обходной путь.

done_when: |
  1. **Четырнадцать контрактов существуют и проверяются машиной.** Один файл на
     сущность, одинаковая типизированная шапка, устойчивый идентификатор с
     версией, машинная проверка полей. Форма «элемента контекста» — девять
     свойств и три класса из `health-context-model-v1.md`, не выдуманная заново.
     Ни один контракт не упоминает транспорт.
  2. **Проверка доказана детерминированно, в обе стороны.** Нормальные образцы
     проходят; заведомо неверные — неизвестное перечисление, битая ссылка,
     отсутствующее обязательное свойство, эффект за границей — роняют проверку.
     Доказано подсаженным нарушением в `tools/selfcheck.py`, а не заявлено; на
     чистом дереве ни один гейт не краснеет зря. Всё внутри той же одной команды.
  3. **Общий слой чист, и это число, а не обещание.** Поиск по `src/` возвращает
     ноль упоминаний Health, питания и тренировок — вывод команды приложен.
     `uv run --locked python tools/check.py --deliver` зелёная на чистом дереве.

return: |
  Один RESULT плея `work`. В evidence — дословный вывод одной проверяющей команды
  и `selfcheck` на чистом дереве, вывод поиска по `src/`, список созданных файлов
  и хеш локального коммита. Светлое закрытие, если каждая строка перевыводится из
  закоммиченных байтов и вывода команд первыми руками; строка, требующая суждения,
  отправляет задачу в свежий отдельный чат.

budget: |
  Одна сосредоточенная сессия, не больше полудня фокусной работы. Задача уже
  раздроблена (реестр вынесен отдельно) — это первое из двух дроблений,
  разрешённых порогом смерти 2. Переросла ещё раз — СТОП и вопрос владельцу,
  а не третье дробление по ходу. Фан-аут не требуется.

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-health-contracts-044.md
