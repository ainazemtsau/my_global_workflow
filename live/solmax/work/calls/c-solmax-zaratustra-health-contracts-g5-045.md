CALL c-solmax-zaratustra-health-contracts-g5-045
to: session
direction: solmax
play: review
node: g-zara-health-vertical
bet: bet-g-zara-health-vertical
task: t-health-contracts
status: ready

goal: |
  Известно, выдерживает ли строка приёмки 1 наряда 044 попытку её опровергнуть.

  Строки 2 и 3 закрываются выводом команд и перевыводятся первыми руками — их
  проверять надо, но они дешёвые. Настоящая работа этой ноги в другом: форма
  контракта «элемент контекста» ОБЪЯВЛЕНА взятой из подписанного артефакта, а
  не выдуманной заново, и это суждение вынес АВТОР перевода. Никто третий его
  не атаковал. Пока не атаковал — задача не закрыта.

context: |
  АБСОЛЮТНЫЕ ПУТИ. Искать ничего не надо. Свежий отдельный чат — это условие,
  а не пожелание: нога 044 и эта нога не имеют права быть одной сессией.

  ПРОДУКТОВЫЙ РЕПОЗИТОРИЙ, только читать и запускать:
  `C:\projects\zaratusta-product`. Ветка `feat/health-contracts`, коммит
  `5b46bd3b007932fe4a12ea6040b5823df5add4c8`, родитель `30e436d`, НЕ запушен.
  Дерево чистое. Ничего не коммитить и не пушить: эта нога — опровержение.
  - `RESULT.md` — отчёт ноги 044 со всеми числами и выводами команд.
  - `docs/adr/0003-the-fourteen-contracts.md` — девять решений HOW, каждое с
    отклонённой альтернативой. Это места для удара, а не защита.
  - `src/vocabulary/` — четырнадцать контрактов плюс `_spec.py`, `_validate.py`
    и `AGENTS.md` модуля.
  - `tools/check.py` — пять новых гейтов; `tools/selfcheck.py` — 53 контроля.
  - Команды: `uv run --locked python tools/check.py --deliver` и
    `uv run --locked python tools/selfcheck.py`, обе из корня ЭТОГО репозитория.

  РЕПОЗИТОРИЙ НАПРАВЛЕНИЯ, только читать:
  `C:\my_global_workflow_worktrees\solmax`
  - `live/solmax/work/health-context-model-v1.md` — ПОДПИСАННЫЙ ИСТОЧНИК.
    Разделы «Схема карточки» (девять свойств) и «Классы» (ровно три).
  - `live/solmax/cards/t-health-contracts.md` — пять строк задачи.
  - `live/solmax/work/calls/c-solmax-zaratustra-health-contracts-044.md` — наряд.
  - `live/solmax/history/2026-08-28-s-solmax-zaratustra-health-contracts-044.md`.

  ЧТО АВТОР 044 СЧИТАЕТ СВОИМ САМЫМ СЛАБЫМ МЕСТОМ, названо им самим, чтобы
  опровержение начиналось не с нуля. Это НЕ защита и не список разрешённых
  претензий — свои находки важнее:

  1. **`граница` разделена не пополам, а на одну половину.** Подписанная модель
     говорит «граница | кто читает и кто пишет». В контракте есть только
     `boundary_read`. Довод автора: пишущая половина — это уже отдельное
     свойство `владелец` («кто вправе менять»), и нести его дважды значило бы
     два источника одного факта. Довод может быть верным и всё равно означать,
     что девяти свойств больше нет.
  2. **`актуальность` стала закрытым перечислением** (`not_applicable`, `day`,
     `week`, `month`, `until_next_version`), тогда как модель говорит «окно».
     Окно и перечисление окон — не одно и то же.
  3. **`применения` остались свободным текстом** и машиной не проверяются
     вообще, кроме непустоты.
  4. **Гейты — самописные сканеры исходного текста.** `AGENTS.md` продуктового
     репозитория прямо запрещает такой сканер «как улику поведения». Позиция
     автора: лексический гейт доказывает ЛЕКСИЧЕСКОЕ отсутствие и как улика
     поведения не предъявляется. Позицию надо атаковать, а не принять.
  5. **Гейт «один контракт на файл» считает вызовы `Contract(...)` по AST.**
     Файл, собирающий контракт через функцию-помощник, гейт не увидит.
  6. **`effect_out_of_bounds` отрабатывает ровно в одном образце** одного
     контракта.

boundaries: |
  - **Свежий отдельный физический чат.** Это гейт G5, и он про место, а не про
    модель. Другая модель — необязательное разнообразие, не условие.
  - Ничего не чинить и не коммитить ни в одном репозитории. Находка — это
    находка, а не правка. Продуктовое дерево вернуть ровно тем, каким взял.
  - Опровергать, а не подтверждать. Строка, по которой не нашлось ни одного
    удара, отмечается «атакована, устояла», а не «выглядит нормально».
  - **Запуск команд — обязательная часть, а не опция.** Отчёт, где команды не
    запускались, ничего не опровергает и ничего не подтверждает.
  - Читать `archive/**` не нужно и нельзя без токена владельца.
  - Инструмент недоступен — СТОП и вопрос владельцу, никогда обходной путь.

done_when: |
  1. **По каждой из трёх строк приёмки наряда 044 — вердикт с уликой:**
     выполнена / выполнена частично / не выполнена, и чем это доказано.
     Строка 1 разбирается по частям: четырнадцать контрактов и их
     идентичность; один файл на сущность; машинная проверка полей; форма
     «элемента контекста» из подписанного источника; отсутствие транспорта.
  2. **Верность перевода «элемента контекста» проверена построчно** против
     раздела «Схема карточки» подписанного артефакта: девять свойств названы
     поимённо, для каждого сказано, где оно в контракте и в каком виде.
     Расхождения названы расхождениями, даже если довод автора убедителен.
  3. **Обе команды прогнаны первыми руками**, вывод приложен дословно. Отдельно
     проверено, что гейты не вырождены: подсади нарушение САМ (пятнадцатый
     контракт без отвергаемого образца; транспортное слово в контракте;
     контракт, собранный помощником вместо литерала) и скажи, покраснело ли.
     Дерево вернуть в исходное состояние; сказать, что вернул.
  4. **Заявление «метаданные не являются исполняемым языком» атаковано**
     по существу, а не по гейту: два кросс-правила — это ещё не интерпретатор
     или уже он, и почему.
  5. **Вердикт задачи целиком:** можно ли `t-health-contracts` закрывать. Если
     нет — какие ровно правки нужны, в каком объёме, и наряд-переработка
     готовым текстом. Если да — прямо сказать, что задача закрывается этой
     уликой.

return: |
  Один RESULT плея `review`. В evidence — дословный вывод обеих команд, вывод
  собственных подсаженных нарушений, и построчная таблица девяти свойств.
  Вердикт по каждой строке приёмки отдельной строкой. Если задача не
  закрывается — готовый наряд-переработка, а не описание того, каким он мог бы
  быть.

budget: |
  Одна сосредоточенная сессия. Фан-аут не требуется: предмет — четырнадцать
  небольших файлов и две команды. Если опровержение упирается в вопрос к
  владельцу — СТОП и вопрос, а не догадка.

END_OF_FILE: live/solmax/work/calls/c-solmax-zaratustra-health-contracts-g5-045.md
