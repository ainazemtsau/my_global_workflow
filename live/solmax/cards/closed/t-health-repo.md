---
id: t-health-repo
_kind: task
_bet: g-zara-health-vertical
status: done
_pos: 1
goal: Продуктовый репозиторий способен принимать код и проверять его одной командой
---

## done_when
1. **Репозиторий `github.com/ainazemtsau/zaratusta` (локально `C:\projects\zaratusta-product`)
   заведён по `os/engineering/PROJECT_SETUP.md`** и способен принимать код: скелет модулей, границы
   модулей, проверяемые сборкой, форматтер, линтер, проверка типов и тесты одной командой.
2. **Корневой `AGENTS.md` заменён.** Действующий сторож markdown-маршрута прямо запрещает `src/`,
   тесты, скрипты и CI — он остаётся уликой в истории Git, но перестаёт быть действующим правилом.
   Новый корневой файл несёт раздел контракта прогона, точные команды сборки, тестов и проверок,
   соглашения и определение готовности.
3. **`validation.config` создан и содержит `synced_contract_version: 36`** плюс пороги повторов и
   правила эскалации; созданы `REVIEW.md` с рубрикой ревью этого репозитория, `docs/adr/` с первым
   решением о стеке, `docs/FRICTION.md` и `_scratch/` с самоигнорирующимся `.gitignore`.
4. **Профиль стека под Python создан побочным продуктом** в `os/engineering/profiles/python.md`
   (≤100 строк, формат из `profiles/README.md`) и возвращён в `state_changes` RESULT — в
   workflow-репозиторий его записывает писатель Направления, исполнитель туда не пишет.
5. **Старая markdown-поверхность (`contracts/`, `checks/`, `examples/`, `docs/`) не удалена и не
   переписана.** Она остаётся уликой; в `README.md` записано одной строкой, что власти над новым
   рантаймом и состоянием она не имеет.
6. **Ни одной строки продуктовой функциональности.** Задача заводит петлю обратной связи, а не
   Health.
## журнал
2026-08-28 · закрыта светлым закрытием: все семь строк приёмки перевыведены первыми руками из закоммиченных байтов и вывода команд, ни одна не ждёт суждения. Репозиторий принимает код и проверяется одной командой uv run --locked python tools/check.py; границы модулей держит граф зависимостей — подсаженный import runtime внутрь foundation роняет прогон с кодом 1; tools/selfcheck.py доказывает 30 контролями, что каждый гейт умеет краснеть и на чистом дереве не краснеет зря; корневой AGENTS.md заменён и прежний запрет на src/ снят; validation.config со стампом 36 читается проверкой, а не лежит украшением; профиль python-uv на 92 строки возвращён; ноль продуктовой функциональности и старая markdown-поверхность байт в байт не тронута. Прогона на macOS НЕТ и он не заявлен: наряд сам свёл эту приёмку к механическому основанию — ни одного скрипта в дереве, интерпретатор пинует uv, пути только pathlib, две строки запуска сравнивает гейт · history/2026-08-28-s-solmax-zaratustra-health-repo-042.md
2026-08-28 · продуктовый репозиторий начал принимать код: одна команда `uv run --locked python tools/check.py` гоняет статические гейты, ruff format, ruff check, lint-imports, mypy --strict и pytest, есть файловый вариант и deliver-вариант; границы модулей держит граф зависимостей, а не документ — подсаженный `import runtime` внутрь `foundation` роняет прогон с кодом 1, а `tools/selfcheck.py` доказывает 30 подсаженными нарушениями, что краснеет КАЖДЫЙ гейт и что на чистом дереве ни один не краснеет зря; стек Python 3.13 под uv, и кроссплатформенность держит инструмент, а не документация: `python-preference = only-managed` заставила uv скачать и запинить свой CPython вместо системного, который первый `uv lock` молча взял; корневой AGENTS.md заменён — 118 строк, команды первыми, контракт прогона, конституция из семи принципов, прежний запрет на src/ снят и остался в истории Git; служебный контур собран: validation.config со стампом 36 и честным mutation_runner=none вместо выдуманного счёта, REVIEW.md, ADR-0001 и ADR-0002 с каждым непоставленным пунктом PROJECT_SETUP и условием его возврата, FRICTION с четырьмя ловушками, самоигнорирующийся _scratch/; ноль продуктовой функциональности — поиск Health-словаря по src/ возвращает ноль, старая markdown-поверхность байт в байт не тронута; CI написан и СНЯТ до закрытия, потому что тратит его минуты GitHub Actions, а он одобрял стек, а не трату, и вынесен ему вопросом; профиль python-uv на 92 строки возвращён и положен; коммиты 9cae3a0 и 4f16113 на ветке setup/agent-ready, не запушены; закрытие светлое — каждая из семи строк приёмки перевыведена первыми руками из закоммиченных байтов и вывода команд · history/2026-08-28-s-solmax-zaratustra-health-repo-042.md
2026-08-13 · shape закрыт и ставка активирована: владелец ответил на единственную открытую строку «вариант А» (сценарий «Свод дня» на его трёх настоящих июльских днях), до подписи поправил «тренировки тоже должны быть включены как я писал» — это переопределило done_when 8 наряда и решение сессии C5 и превратило линзу расширяемости из бумажного вердикта в измеряемый коммит с нулевым диффом внутри ставки, поставил границу старого пакета на свой токен («идеи и функционал, но не структуру») и подписал исправленный экран словом «да»; семь задач с проверкой главного допущения первой и видимым результатом последним, десять вырезов, шесть вердиктов по линзам, пять порогов и 13.09.2026, все шесть открытых строк перенесены поимённо, а продуктовый репозиторий оказался не заведён под код — его настройка заняла место, освободившееся от сворачивания его прогона в последнюю задачу · history/2026-08-13-s-solmax-zaratustra-health-shape-033.md
END_OF_FILE: live/solmax/cards/closed/t-health-repo.md
