[project]
name = "direction-os-tools"
version = "0.1.0"
description = "Local Python runtime for Direction OS repository tools"
requires-python = ">=3.13,<3.14"
dependencies = [
    "PyYAML==6.0.3",
]

[tool.uv]
package = false
